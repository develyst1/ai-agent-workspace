# TEST-003: Submit an idea and see the AI analysis
- Source REQ: REQ-003 v2 (AC-1..AC-12); SPEC-003 read for the contract only
- Status: TEST_PASSED — 2026-09-22 (round 2 on SIT with self-service `qa-tanya-pw*` accounts, REQ-006); AC-7/AC-12 NOT_TESTED-on-SIT per Porter (Q-2 answer), AC-11 on record evidence only
- Environment: **SIT only** (owner rule 2026-09-21): `https://possibility.develyst.online` (FE + API `/api/v1` same host; `health` db:ok); DB = shared `possibility_db` (read-only harness, declared)
- Tested: 2026-09-21 (round 1: records) and 2026-09-22 (round 2: UI on SIT) by Tanya

## Scope
All 12 ACs of REQ-003 on the deployed SIT app with the real gateway. **Blocked this round:** SIT rejects a QA-minted cookie (different `SESSION_SECRET`), and I cannot obtain a Google session myself — so nothing that needs `requireUser` has been run on SIT yet. What I could do: verify the saved records of the six existing ideas (AC-2 hand recompute, AC-10 step outputs, AC-11 per-step model recorded), and the signed-out guard on every REQ-003 page. Run notes from Porter: ~10 s per analysis; AC-7 (dead gateway) cannot be forced on SIT → NOT_TESTED with the local pointer (TASK-006 #7).

## Cases
| # | Case (from AC) | Type | Steps | Expected | Actual | Result |
|---|----------------|------|-------|----------|--------|--------|
| 1 | AC-1 valid idea → loading (W-4) → result with 3 integer scores 0–100, tier, discount, non-empty reason | happy | Round 2 on SIT as `qa-tanya-pw1`: three real submissions (236-char TH, 109-char EN, 133-char EN) | As stated | Each: click W-5 → loading (spark + W-4) → `POST /ideas → 201` → `/ideas/<id>`: scores `90/90/85` Visionary 20% (TH reason) · `90/70/20` Ordinary 0% (EN reason) · `85/70/75` Raw Diamond 10% (EN reason); integers, labels, tier, discount line, reason all present. 12–25 s per analysis | PASS |
| 2 | AC-2 tier on the result = REQ-001 rule on the three shown scores (hand recompute) | happy | `tests/harness/test-003-ideas-select.ts` (read-only, SIT DB) over every saved idea + the three SIT results by eye | `idea_tier` = rule(min) | SIT results: min 85 → Visionary ✓, min 20 → Ordinary ✓, min 70 → Raw Diamond ✓ (shown tier = recomputed). Records: 10/10 real ideas correct (6 engineers' + Fern's 3rd + my 3); scores integers 0–100 | PASS |
| 3 | AC-3 < 20 chars → submit disabled + W-2; > 3000 → cannot type further, W-3 counter red | negative | SIT `/ideas/new` signed in: `short idea` (+6 spaces = 16); value set to 3050 chars by script (bypasses `maxLength`), then type one more char | As stated | 16 chars: `เขียนอย่างน้อย 20 ตัวอักษร`, box in error state, W-5 `disabled=true`; 3050: counter `3050 / 3000` in red, submit disabled, typing blocked (`maxLength` 3000; length stayed 3050) | PASS |
| 4 | AC-4 empty / whitespace → submit disabled | negative | Empty box; 6 spaces | Disabled | `0 / 3000` disabled; `6 / 3000` + W-2, disabled | PASS |
| 5 | AC-5 "My ideas": 3 ideas newest first with date, tier, 80-char preview; open one → saved result, no re-analysis | happy | `/ideas` as pw1 after 3 ideas; click the oldest card; network watched | As stated | Order newest → oldest (`cd40b60d`, `2f0931be`, `675689e9`); each row: date `22 Sep 2026` / `22 ก.ย. 2026` (W-9 already in), tier, preview exactly 80 chars (`[QA] A mobile app … and lets us`), tier thumbnail; clicking a card → only `GET /ideas/675689e9 → 200`, **no POST**, result identical to the original. Empty state as pw2: `Nothing here yet — your first possibility is waiting.` + spark | PASS |
| 6 | AC-6 user B opens A's idea by address → "not found", never A's content | negative | As `qa-tanya-pw2` (UI + API cookie) open pw1's `/ideas/675689e9-…` | 404 page, A's text absent | API `404 NOT_FOUND`; UI: W-8 page `Page not found — This page does not exist, or is not yours. — Back to my ideas` (TH: `ไม่พบหน้านี้…`), pw1's Thai idea text absent from the DOM; signed-out visit → landing | PASS |
| 7 | AC-7 AI failure → W-6, nothing saved, retry re-sends the same text | negative | Needs a dead gateway — cannot be forced on SIT | W-6 + retry, no row | **NOT forceable on SIT.** Local evidence (not a QA run): TASK-006 #7 — `POST /ideas → 502 AI_FAILED`, W-6 + "Try again" re-sent the same text, `GET /ideas` unchanged; TASK-005 dead-port run, counts unchanged | NOT_TESTED (pointer: `tasks/TASK-006 §Implementation Notes` #7) |
| 8 | AC-8 reason language follows the UI language at analysis time | happy | SIT: idea 1 submitted in TH, ideas 2–3 in EN | Reason in that language | Idea 1 reason Thai (`โครงการนี้มีความชัดเจน…`), ideas 2–3 English (`The project is highly feasible…`, `The app is feasible…`); switching the UI afterwards relabels the page but keeps the saved reason (by design) | PASS |
| 9 | AC-9 regression: user tier updates only upward after a result (REQ-001 AC-4/5) | regression | SIT, pw1: Visionary idea → Ordinary idea → Raw Diamond idea | Badge rises once, never falls | Badge `Ordinary 0%` → `Visionary 20%` after idea 1; stayed `Visionary 20%` after ideas 2 (Ordinary) and 3 (Raw Diamond); `/auth/me` + DB `users.tier` agree (TEST-002 #4/#5) | PASS |
| 10 | AC-10 outputs of steps 1–4 + final scores saved for every idea | happy | `test-003-ideas-select.ts` after round 2 | 5 `idea_steps` per real idea with the SPEC-003 output keys | My 3 SIT ideas each carry steps 1–5 (gpt-4o-mini ×3, gpt-4o ×2, `promptVersion 2026-09-19.1`, latency 0.8–5.4 s per step); all 10 real ideas (engineers' 7 + mine) have steps 1–5: `understand{why,forWhom,summary,wantsWhat,assumptions}`, `goalClarity{clarity,missing,feasibilityNotes}`, `goodForWorld{harms,benefit,whoBenefits}`, `companyFit{fit,reason,systemType}`, `synthesis{impact,reason,feasibility,interestingness}`; each with provider/model/maxTokens/temperature/promptVersion `2026-09-19.1`/latencyMs | PASS (records; admin-page view is REQ-004) |
| 11 | AC-11 per-step model changed in config only → that step's saved record shows the new model, others unchanged | happy | Config on SIT is the owner's — I cannot change it. Records show the effect of Jason's local config change | New model on one step only | Idea `34615909-…` (2026-09-19): step 4 `openai/gpt-4.1`, steps 1/2/3/5 `gpt-4o-mini`/`gpt-4o` as every other idea — exactly one step differs (TASK-005 AC-11 run). Not repeatable by QA on SIT | PASS (record evidence from the engineer's run) / NOT_TESTED on SIT |
| 12 | AC-12 any one step failing → whole analysis fails per AC-7 | negative | Same block as #7 | Nothing saved | Local pointer only: TASK-005 gemini-403 run failed at step 3 with `AI_FAILED: step goodForWorld`, nothing saved | NOT_TESTED |
| 13 | Regression: signed-out `/ideas/new`, `/ideas`, `/ideas/<id>` on SIT → landing | regression | Browser pane, no cookie | Landing | PASS (TEST-002 #8) | PASS |

## Observations (for Porter)
- O-1 The six saved ideas are the engineers' local-run fixtures (dev-jason-a/b, dev-fern-a); none was created through SIT yet. Once signed in on SIT I create my own (`[QA] …` text) and re-run #2/#8/#10 on those.
- O-2 W-8 (not-found wording) and W-9 (date format) are TASK-010 items — will be recorded as observations, not defects, per Porter's note.

## Defects
None so far.

## Test data created
| What | Where | End state |
|------|-------|-----------|
| 3 real ideas under `qa-tanya-pw1@example.com` (text prefixed `[QA]`), created through the SIT app on 2026-09-22 (ids `675689e9-…` TH Visionary, `2f0931be-…` EN Ordinary, `cd40b60d-…` EN Raw Diamond) + 15 `idea_steps`; pw1 tier now Visionary | SIT DB | left as-is (no delete exists; declared) |
| reads: `tests/harness/test-003-ideas-select.ts` | SIT DB | — |

## Verdict
**`TEST_PASSED`** — REQ-003 holds on SIT end-to-end: three real analyses through the deployed app and the owner's gateway, validation, list, not-found isolation between users, reason language, upward-only tier, five step records per idea. 10 PASS · 0 FAIL · 2 NOT_TESTED-on-SIT (AC-7, AC-12 — accepted on local evidence, Porter's Q-2 answer) · AC-11 on record evidence (Jason's config change; not repeatable by QA on the owner's SIT config). Gaps stated, nothing rounded up. Note for TASK-010: W-8/W-9 are already live on SIT in the redesign build. **Cross-reference:** the W-6 failure alert uses the same antd `Alert` that TEST-004 DEF-1 found unreadable — if W-6 ever renders on SIT it will be illegible too; the fix for DEF-1 should cover it (TASK-006 built W-6 as an `Alert`).

## Questions
- **Q-1 @Porter (Tanya, 2026-09-21) — same as TEST-002 Q-1:** I need a signed-in session on SIT. Proposed: the owner clicks "Sign in with Google" **in my browser pane** (already open at `https://possibility.develyst.online/ideas/new`) with a **test Google account** (ideas raise the account's tier permanently; no delete exists) — or his own account if he prefers, then I label every idea `[QA] …`. Two accounts would let me run AC-6 (user B opens A's idea) properly; with one account I can only prove AC-6 against the engineers' existing ideas.
- **Q-2 @Porter (Tanya, 2026-09-21):** AC-7 and AC-12 need a dead / failing gateway, which nobody can arrange on SIT. Do you accept the engineers' local evidence (TASK-005 §Notes dead-port + gemini-403 runs, TASK-006 #7 W-6 + retry) as the proof for these two, recorded here as NOT_TESTED-on-SIT with pointers, or should the owner arrange a way to break the gateway on SIT for one run?
  > answer (Porter 2026-09-21) to Q-2: accept the engineers' local evidence for AC-7/AC-12, recorded as **NOT_TESTED on SIT** with the exact pointers (TASK-005 §Notes dead-port + gemini-403 runs; TASK-006 #7). Breaking the owner's SIT gateway is not something I will ask for. The verdict may still be TEST_PASSED if everything runnable on SIT passes; the gap is stated in §Verdict and in the DELIVERED summary.
  > answer (Porter 2026-09-21) to Q-1: carried to the owner — sign in inside your browser pane with a test Google account (two if possible). Waiting for his word + the email(s).
  > closed (Tanya, 2026-09-22): superseded — the owner chose REQ-006; round 2 run with `qa-tanya-pw1/2` on SIT.
