# TEST-006: "Interested in hiring" request + admin page
- Source REQ: REQ-004 (R1–R5, AC-1..6); SPEC-005 read for the contract only
- Status: **TEST_PASSED** — 2026-09-23, round 1 (user side, SIT) + round 2 (admin half, SIT, as `qa-tanya-pw1@example.com` on ADMIN_EMAILS, after the owner fixed the DEF-1 outage). DEF-1 verified fixed (health `db:ok`, login 200). Gaps and copy mismatches stated in §Verdict.
- Surfaces: SIT `https://possibility.develyst.online` (FE + API `/api/v1`, build deployed by the owner 2026-09-23) · desktop web 1280×800 · mobile web 375×812 · SIT DB `possibility_db` (one read-only select, one scoped delete — declared)
- Tested: 2026-09-23 by Tanya

## Scope
AC-1..6 on the deployed SIT build with fresh self-service QA accounts (`qa-tanya-hire1/2@example.com`, email+password, created through the real SIT `POST /auth/register`). **Deliberately not covered:** anything that requires being signed in as `siegkung@gmail.com` (the admin page render, the contacted toggle, the admin list content) — QA has no Google session and cannot mint one.

## Cases
| # | Case (from AC) | Type | Surface | Steps | Expected | Actual | Result |
|---|---|---|---|---|---|---|---|
| 1 | AC-1: W-1 press → W-2 confirmation (email + discount), button replaced by disabled W-3 | happy | SIT desktop, TH | As hire1, open `/ideas/46265726-…` (Ordinary idea, 85/70/30), click `สนใจจ้างทำ` | W-2 exact wording with `{email}` and `0%`; W-3 disabled; W-1 gone | `POST /ideas/:id/hire-request → 201` `{"hireRequest":{"id":"d369d59d-…","userTierAtRequest":"Ordinary","discountPercentAtRequest":0,"contactedAt":null}}`; page shows the exact W-2 `ส่งคำขอแล้ว — เราจะติดต่อกลับทางอีเมล qa-tanya-hire1@example.com พร้อมข้อเสนอราคาที่รวมส่วนลด 0% ของคุณ` + disabled `ส่งคำขอแล้ว` button; W-1 gone. Screenshot `../project-docs/qa-2026-09-23/test-006-ac1-th-after-hire.png`. (Hit-test note: before scrolling the button sat below the 800 px fold so `elementFromPoint` returned null at its un-scrolled position; Playwright's real click scrolled it into view — a user scrolls the result page the same way. Not a reachability defect.) | PASS |
| 2 | AC-1 (record half): the request on record carries all Requirement-2 fields | happy | SIT DB (read-only) | `tests/harness/sit-test-006-hire-select.ts` | idea, scores, idea tier, user tier at request, email, name, date | Row present: `user_tier_at_request Ordinary`, `discount 0`, `contacted_at null`, user `QA Tanya Hire1 / qa-tanya-hire1@example.com`, idea `46265726-…` scores 85/70/30 tier `Ordinary`, created `2026-09-22T22:16:34Z` (UTC). **The admin-page rendering of these fields is NOT_TESTED (case 6).** | PASS (record) |
| 3 | AC-2: reopen → W-3; second request impossible | negative | SIT desktop TH+EN, API | Reload the result page; `POST /hire-request` again via API; `GET /ideas/:id` | W-3 shown, no W-1; 409; count unchanged | Reload TH: disabled `ส่งคำขอแล้ว`, no W-1. EN: disabled `Request sent` (exact W-3 EN), no W-1. Second POST → `409 ALREADY_EXISTS`. `GET /ideas/:id` → `hireRequested: true`. DB select after: still exactly 1 row. Screenshots `test-006-ac2-th-reopen.png`, `test-006-ac2-en-reopen.png` | PASS |
| 4 | AC-3: non-admin → "not found"; signed-out → sign-in; admin API refused | negative | SIT desktop TH | As hire1: open `/admin`, `GET /api/v1/admin/hire-requests`, `PATCH /admin/hire-requests/:id`. Signed out: `/admin` + admin API | Not-found; 404; 404. Signed-out → no admin content | Page: W-8 `ไม่พบหน้านี้ — หน้าที่คุณเปิดไม่มีอยู่ หรือไม่ใช่ของคุณ`, no admin copy (screenshot `test-006-ac3-nonadmin-admin.png`). API list → `404 NOT_FOUND`; PATCH → `404`. Signed out: page shows landing/sign-in (no admin copy); API → `401 NOT_SIGNED_IN` (screenshot `test-006-ac3-signedout-admin.png`) | PASS (non-admin + signed-out halves) |
| 5 | AC-6: no admin link anywhere for a non-admin | negative | SIT desktop + 375 px | Crawl `a[href]` on `/ideas`, `/me`, result page; header text | Zero admin links | Zero `admin` hrefs on all three pages at both widths; header never mentions admin; no horizontal scroll at 375 px | PASS |
| 6 | AC-3 admin half + AC-4 + admin list rendering (AC-1 display half) | happy | SIT desktop, TH+EN | Round 2 (after DEF-1 fix): sign in as `qa-tanya-pw1@example.com` (on ADMIN_EMAILS, owner 09-23) → `isAdmin:true`; open `/admin` | List renders, all Requirement-2 fields per row | Page `คำขอจ้างงาน` / EN `Hire requests`; API `GET /admin/hire-requests → 200` with 6 rows (2 mine + the owner's own + Fern's + Jason's); **newest first** verified (hire2 row above hire1 row); my hire1 row shows date `23/ก.ย./26 12:02`, name, email, 120-char preview, scores 85/70/30, idea tier `Ordinary`, user tier at request `Visionary · 20%`; row expand icon reveals the full idea text + `ขั้นตอนของ AI` button → drawer with all 5 steps (understand/goalClarity/goodForWorld/companyFit/synthesis, each `provider/model · latencyMs`). Screenshots `test-006-admin-th-list.png`, `test-006-admin-th-expanded.png`, `test-006-admin-th-steps-drawer.png`, `test-006-admin-en-list.png`. **Copy mismatch (not failed, per Porter's instruction):** the deployed build predates REQ-004 §Questions copy — headers are `ชื่อที่แสดง/ความเป็นไปได้/ผลกระทบต่อสังคม/ความน่าสนใจ/ระดับไอเดีย/ระดับผู้ใช้ตอนส่งคำขอ/ติดต่อแล้ว` (EN equivalents), expand is an antd row-expand icon (not `ดูทั้งหมด`/`ย่อ`), drawer title `ขั้นตอนของ AI` / `AI steps` (not `ขั้นตอนที่ AI วิเคราะห์`), step labels are the raw step keys, meta is `provider/model · Nms` (not `โมเดล … มิลลิวินาที`) | PASS (function) / copy mismatch noted for Porter |
| 6b | AC-4: owner marks "contacted" | happy | SIT desktop TH | Toggle the switch on hire2's row → PATCH; reload; toggle back | 200; contacted state; persists; undo works | `PATCH → 200` `contactedAt: 2026-09-23T05:06:28Z`; switch `aria-checked=true`, label `ติดต่อแล้ว`, row stays in the list; **after reload still contacted**; undo → `200 contactedAt:null`, label back to `ยังไม่ติดต่อ`. Screenshot `test-006-admin-th-contacted.png` | PASS |
| 6c | AC-3 regression after the admin-list change: non-admin still refused | regression | SIT | As hire1 (not on the list): API + `/admin` page | 404 / not-found | API `404 NOT_FOUND`; page `ไม่พบหน้านี้` | PASS |
| 6d | Admin page at 375 px | edge | SIT mobile web | Open `/admin` at 375×812; measure; hit-test the contacted switch | Usable | Page itself has no hscroll (375=375); the table scrolls inside its container (`scrollWidth 2461` vs `clientWidth 343`, `overflow-x:auto` — user-scrollable, not clipped); before scrolling the switch is off-view (x=2342, hit null — expected), **after a natural horizontal swipe it lands at x=224 and is hittable**. Screenshot `test-006-admin-th-375.png` + `-scrolled.png` | PASS (with O-4) |
| 6e | Empty state copy (`ยังไม่มีคำขอ` / `No requests yet`) | happy | SIT | Needs an empty `hire_requests` table | Empty copy shows | **Cannot run:** the table holds 4 pre-existing rows that are not mine (the owner's own request, Fern's ×2, Jason's) — I never touch other people's data, so the list can never be emptied by me | NOT_TESTED (would need a fresh DB or the owner's consent to clear the shared table) |
| 7 | AC-5: tier-at-request snapshot survives a later tier rise | edge | SIT API + DB | hire1 hired at `Ordinary/0%`; then submitted a strong idea (`455a3750-…`, 85/90/80 → idea tier Visionary) → user tier rose to `Visionary/20%`; re-select the row | Row still `Ordinary` / 0% | Row unchanged: `user_tier_at_request "Ordinary"`, `discount_percent_at_request 0`, `user_tier_now "Visionary"` (harness `sit-test-006-ac5-tier-rise.mjs` + `sit-test-006-hire-select.ts`) | PASS (record level; admin-page display of it = case 6, NOT_TESTED) |
| 8 | Edge: user B hires user A's idea | negative | SIT API | As hire2: `POST /ideas/<hire1's idea>/hire-request` | 404, no row | `404 NOT_FOUND`; DB count for my rows unchanged | PASS |

## Defects
### DEF-1 — SIT: every DB-touching API request fails since the 2026-09-23 admin-list deploy (login 500, health 502) — BLOCKER — **FIXED by the owner 2026-09-23 ("fix done"), verified on SIT: `GET /api/v1/health` → 200 `{"status":"ok","db":"ok"}`; `POST /auth/login` → 200 for `qa-tanya-pw1@example.com` (with `isAdmin:true`) and `qa-tanya-hire1@example.com`**
- Environment / surface: SIT `https://possibility.develyst.online`, all surfaces.
- Repro (from a clean state): 1. `POST /api/v1/auth/login` with a known-good account (`qa-tanya-hire1@example.com`, working on SIT one hour earlier) → **500 `INTERNAL_ERROR`** (3/3 tries, same for `qa-tanya-pw1@example.com`). 2. `GET /api/v1/health` → **Cloudflare 502** (was `{"status":"ok","db":"ok"}` at ~05:00). 3. Non-DB API paths still answer: `/auth/me` without cookie → 401, `POST /auth/register` with a bad body → 400; the static FE landing → 200.
- Expected: login 200, health `db:ok`.
- Actual: sign-in (Google and email+password alike — both end in `toUserDto`/DB) is down for **every** user, so the whole signed-in product is down on SIT.
- Localisation evidence (not a fix proposal): the **same accounts log in fine** (200) against a local `possibility-back` running current `main` **on the same SIT database** — and `isAdmin:true` resolves correctly for `qa-tanya-pw1@example.com` there. So the DB, the data and the repo code are fine; the failure is specific to the SIT deploy/runtime (its env or its DB reachability). SIT BE logs are not readable by me — the owner can see them.
- Consequence for this TEST: the admin half (case 6) is BLOCKED, not NOT_TESTED-by-access anymore. Also a live regression of R-15/R-16 on SIT right now.

## Observations (for Porter, not defects)
- O-1 The result page still shows the REQ-007 photographic tier image — expected; REQ-008/TASK-015 replaces it.
- O-2 Signed-out admin API answers `401 NOT_SIGNED_IN` while a signed-in non-admin gets `404 NOT_FOUND` — sensible (you must be signed in before "not found" applies); recording so nobody "fixes" it.
- O-3 **Admin copy mismatch (round 2, case 6):** the deployed admin page predates the REQ-004 §Questions copy — different table headers, expand via antd row icon instead of `ดูทั้งหมด`/`ย่อ`, drawer title `ขั้นตอนของ AI`/`AI steps`, raw step keys instead of the Thai/English step labels, meta `provider/model · Nms` instead of `โมเดล … มิลลิวินาที`. Not failed per your instruction; listed here so you can decide whether a wording TASK is wanted.
- O-4 The admin table is wide (scrollWidth 2461 at 375 px); the name/email columns sit under the fixed date column until scrolled — normal antd fixed-column behaviour, and the contacted switch is reachable after a swipe. Admin-only screen; noting, not failing.
- O-5 The admin list on SIT contains the **owner's own hire request** (siegkung@gmail.com, 2026-09-22) plus Fern's TASK-009 evidence rows and one dev-jason-b row — pre-existing, not mine, left untouched.

## Test data created
| What | Where | Removed? |
|---|---|---|
| users `qa-tanya-hire1@example.com` (tier now Visionary), `qa-tanya-hire2@example.com` (Ordinary) — created via real SIT register | SIT DB `users` | left as-is (reusable QA accounts; no delete exists; passwords held in session only) |
| 3 ideas under hire1/hire2 (`[QA] …`: `46265726-…`, `7c7702ab-…`, `455a3750-…`) + their `idea_steps` | SIT DB `ideas` | left as-is (no delete exists; declared) |
| 1 hire_request `d369d59d-…` (idea `46265726-…`, snapshot Ordinary/0%) | SIT DB `hire_requests` | **deleted after evidence capture** (`sit-test-006-cleanup.ts`, scoped to `qa-tanya-hire%` rows only) — the owner's admin list stays clean; consequence: that idea's button is W-1 again |
| 2 hire_requests `3e0358e9-…` (hire1, snapshot Visionary/20%) + `aebc50bc-…` (hire2, snapshot Ordinary/0%) — round-2 admin evidence | SIT DB `hire_requests` | **deleted after evidence capture** (same scoped cleanup; post-delete select shows 0 rows for `qa-tanya-hire%`). The other four rows (owner's, Fern's ×2, Jason's) were **not touched** |
| harnesses `sit-test-006-setup.mjs`, `sit-test-006-hire-ui.mjs`, `sit-test-006-ac5-tier-rise.mjs`, `sit-test-006-hire-select.ts` (read-only), `sit-test-006-cleanup.ts`, `sit-test-006-admin-reset.ts`, `sit-test-006-admin-ui.mjs`, `sit-test-006-admin-ui2.mjs`, `sit-test-006-admin-probe.mjs`, `sit-test-006-admin-probe2.mjs` | coordination repo `tests/harness/` | kept |
| password reset of **my own** account `qa-tanya-pw1@example.com` (old password lost with a dead session; new argon2id hash written via `sit-test-006-admin-reset.ts`, touches only that row) | SIT DB `users` | declared; the account stays a reusable QA admin account (owner put it on ADMIN_EMAILS) |
| screenshots ×10 | `../project-docs/qa-2026-09-23/` | kept (evidence) |

## Verdict
**`TEST_PASSED`** — REQ-004 holds on SIT, both halves, on the deployed build.
- **User side (round 1):** W-1 → W-2 (exact TH wording, correct email + discount) → disabled W-3 in TH and EN; idempotency (409, count unchanged, `hireRequested` DTO flag); cross-user 404; zero admin links at desktop and 375 px; tier-at-request snapshot proven against a real tier rise (`Ordinary/0%` frozen while the user became `Visionary/20%`).
- **Admin side (round 2, as `qa-tanya-pw1` on ADMIN_EMAILS):** `/admin` renders for an admin (API 200); six rows newest-first with date, name, email, 120-char preview, three scores, idea tier, and user-tier-at-request + discount; row expand → full idea text; steps drawer with all five steps and per-step model/latency; contacted switch → PATCH 200 → state persists across reload → undo works; non-admins still get 404/not-found after the admin-list change; usable at 375 px (table scrolls horizontally, toggle reachable).
- **DEF-1** (SIT login outage during the admin-list deploy) was fixed by the owner and verified fixed before round 2 ran.
- **Stated gaps, nothing rounded up:** the empty-state copy (`ยังไม่มีคำขอ` / `No requests yet`) is NOT_TESTED — the shared table holds four rows that are not mine and I never touch other people's data (case 6e). The deployed admin copy predates REQ-004 §Questions wording — noted as O-3 for Porter to decide, not failed per his instruction. The admin mechanism is now email-list based; the owner's own Google account was not clicked by me, but his hire request renders correctly in the list (O-5).

## Questions
- **Q-1 — SUPERSEDED 2026-09-23** (Porter inbox: the owner widened admin to an email list and put `qa-tanya-pw1@example.com` on it — no owner click needed). Original text kept for the record:~~the admin half needs the owner's Google identity, once.~~ Options listed were: owner signs in inside my browser / owner click-through with my checklist / QA-minted admin token (not recommended).
- **Q-2 — CLOSED 2026-09-23:** the owner fixed the SIT outage ("fix done"); verified on SIT (health `db:ok`, login 200 ×2 accounts). ~~DEF-1 — SIT sign-in is down for everyone~~ — kept for the record in §Defects.
- **Q-3 @Porter (Tanya, 2026-09-23):** O-3 — the deployed admin page predates your REQ-004 §Questions copy (headers, expand control, drawer title, step labels, meta line all differ). Do you want a wording TASK, or is the current admin copy accepted? (Not failed per your "note the mismatch" instruction.)
  > answer (Porter 2026-09-23) Q-3: **yes, I want the copy applied** — but not as its own deploy. The admin page is the owner's private screen, so the mismatch is low harm; I've asked Sober to fold the REQ-004 §Questions wording into the next FE task (TASK-015/010) rather than ship a wording-only build. REQ-004 → DELIVERED with this recorded, plus the empty-state copy NOT_TESTED (shared table, your rule not to touch others' rows — correct).
