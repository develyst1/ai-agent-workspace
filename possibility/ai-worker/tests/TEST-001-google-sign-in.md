# TEST-001: Sign in with Google
- Source REQ: REQ-002 (AC-1..AC-6); SPEC-002 read for the contract only
- Status: TEST_PASSED — 2026-09-21 (round 1 by Tanya + round 2 = the owner's real Google run, evidence read by Tanya). Q-3 closed: W-3 after a real dismiss observed NOT shown (known best-effort gap, AC-5 amended). REQ-002 DELIVERED by Porter 2026-09-21.
- Environment: local — round 1: FE `possibility-front` dev server on **:3002** (see note) · round 2 (the owner's real Google run): FE on **:3000**, no overrides; BE `possibility-back` `bun src/index.ts` on :4000 both rounds; DB = shared SIT `possibility_db` (owner's `.env` / `.env.local`, values never read)
- Tested: 2026-09-21 by Tanya

**Run note (environment, not a defect):** port 3000 on this machine was held by another desk's dev server (`safe-goods-front`, node PID 30116); I did not kill it. FE therefore ran on :3002 and the BE was started with `FRONTEND_ORIGIN=http://localhost:3002` as a shell env override (no file in either repo touched). CORS confirmed: `Access-Control-Allow-Origin: http://localhost:3002` + `Allow-Credentials: true` on `/auth/me`.

## Scope
Covers REQ-002 AC-1..AC-6 against the real running FE + BE + SIT DB, in two rounds. **Round 1 (Tanya)** — the one thing it could not do is obtain a real Google ID token**: the in-app browser has no Google session and blocks the GIS popup unless a human clicks (`[GSI_LOGGER]: Failed to open popup window … Maybe blocked by the browser?`, pane note: "popups open only from the user's own clicks"); the Claude-in-Chrome extension (the owner's Chrome, with his Google session) was **not connected**; and I do not type anyone's Google password. Everything after Google's signature check (BE upsert, cookie, header, guard, sign-out) was run with declared `qa-` fixtures. **Round 2 (the owner, TEST-001 Q-1 option b, 2026-09-21 ~13:22 local)** — he signed in with his real Google account at `http://localhost:3000/ideas/new`, signed out, signed in again, signed out, then dismissed the popup; I read the DB diff, the BE log and his four screenshots afterwards. Deliberately not covered: REQ-003/REQ-001 FE (idea list, tier badge — TASK-006/007 not built), admin (REQ-004).

## Cases
| # | Case (from AC) | Type | Steps | Expected | Actual | Result |
|---|----------------|------|-------|----------|--------|--------|
| 1 | AC-1 signed-out visitor opens the idea page (EN) | happy | Fresh browser, no session cookie → `GET /ideas/new` | Google button; no idea box; no W-3 | `GET /auth/me → 401`; page renders landing: GIS iframe (`hl=en`, label "Sign in with Google"), `textarea`=0, `[data-page=ideas-new]` absent, `.ant-alert`=0, header = `Possibility · TH · EN` | PASS |
| 2 | AC-1 same in TH | happy | Click TH, same page | Same, button in Thai | `lang=th` cookie; GIS iframe re-rendered `hl=th`, Google's own label "ลงชื่อเข้าใช้ด้วย Google" (rendered by Google — per Porter's note not W-1, not a defect); no idea box, no alert | PASS |
| 3 | AC-2 first sign-in creates a user with tier `Ordinary` — BE upsert (after Google verification) | happy | `tests/harness/test-001-upsert-path.ts` calls the BE's own `upsertUserFromGoogle` with a verified-shaped payload for new sub `qa-tanya-sub-b` | New row, `isNew`, tier Ordinary, name from payload | `users before: 3` → `1st sign-in: isNew:true, tier:"Ordinary", name:"QA Tanya B"` → `users after 1st: 4` | PASS (BE half) |
| 4 | AC-2 header shows name + email of the signed-in account | happy | Fixture `qa-tanya-a@example.com` (`test-001-qa-fixture.ts`), cookie minted with the BE's `signSession`, placed in the browser; open `/ideas/new` | Header: display name, email, W-2 | `GET /auth/me → 200`; header = `Possibility · QA Tanya A · qa-tanya-a@example.com · ออกจากระบบ · TH · EN`; `[data-page=ideas-new]` present; no admin link | PASS (FE half) |
| 5 | AC-2 **end-to-end with a real Google account** (`POST /auth/google` with a real ID token → 200 + Set-Cookie → user row) | happy | Round 2 (Q-1 option b): the owner clicked the button at `http://localhost:3000/ideas/new` and picked `siegkung@gmail.com`; I read DB / BE log / screenshots afterwards | New user, tier `Ordinary`, header shows that account's name + email | DB diff: `users` 5 → 6, new row `siegkung@gmail.com` name `Nichaphon Sayvav` tier `Ordinary` created `2026-09-21T06:22:25.868Z` (google_sub = a real Google id, not pasted); BE log `sign-in user.id=37407e40-… email=siegkung@gmail.com new`; screenshot `../project-docs/evidence/2026-09-21-owner-google-signin-2.png` header = `Nichaphon Sayvav · siegkung@gmail.com · ออกจากระบบ` | PASS |
| 6 | AC-3 returning user: no second row, tier kept — BE upsert (harness) **and the real run** | happy | Harness: bump my own row `qa-tanya-sub-b` to `Seeker` by SQL, call upsert again with a changed display name. Real: the owner signed out and signed in again with the same account | Same id, not new, tier untouched, name refreshed, count unchanged | Harness: `2nd sign-in: sameId:true, isNew:false, tier:"Seeker", name:"QA Tanya B (renamed)"` → `users after 2nd: 4`. Real: BE log `sign-in user.id=37407e40-… email=siegkung@gmail.com returning` (same id as #5); `users` still 6; row `updated_at 2026-09-21T06:22:59Z` (34 s after create), tier still `Ordinary`; screenshot `…signin-4.png` header shows the account again | PASS |
| 7 | AC-3 returning session keeps the header after reload | regression | With the fixture cookie, navigate to `/ideas/new` again | Header still shows name/email | `afterReloadHeader = Possibility · QA Tanya A · qa-tanya-a@example.com · ออกจากระบบ …`, `/auth/me → 200` | PASS |
| 8 | AC-3 "previous ideas and tier are shown" (FE) | happy | — | Ideas + tier visible | **No FE for it yet** (TASK-006/007 TODO) — belongs to REQ-003/REQ-001 tests; BE `/auth/me` does return the stored tier (`dev-jason-a` → `"tier":"Visionary"` seen in `test-001-users-select.ts` + `/auth/me`) | NOT_TESTED (out of REQ-002 FE scope) |
| 9 | AC-4 sign out (real click) → next visit shows sign-in | happy | Switch EN (button reads `Sign out`), click it, then `GET /ideas/new` again | Header cleared, cookie gone, landing with Google button | Network: `/auth/me 200, 200` (StrictMode double) then `POST /auth/logout → 204`; `document.cookie` has no `possibility_session`; header = `Possibility · TH · EN`; landing in place (GIS iframe=1, page absent); fresh `/ideas/new` → landing again, no alert | PASS |
| 10 | AC-4 regression — logout focus-refetch race (TASK-004 bug) | regression | Same real click as #9; inspect order/outcome | Header cleared immediately, not left stale | Header cleared within the 2 s wait; no stale user after the 204 | PASS |
| 11 | AC-5 BE: bad/forged token creates no user | negative | `POST /auth/google` with `"garbage"`, with `{}`, and with an unsigned `alg:none` JWT carrying `email_verified:true` | 401 / 400 / 401; users count unchanged | `401 INVALID_GOOGLE_TOKEN` · `400 VALIDATION_FAILED "idToken is required"` · `401 INVALID_GOOGLE_TOKEN`; `users count: 2` before and after (pre-fixture) | PASS |
| 12 | AC-5 UI: cancel the Google popup → back on landing with W-3, no user | negative | Round 2: the owner (signed out) clicked the button and dismissed the popup | Landing + W-3 (TH); no new user | **No user created**: `users` = 6 before and after the dismiss (only the owner's own row beyond the fixtures), no extra BE `sign-in` line. **W-3 render UNVERIFIED**: the owner did not report whether W-3 appeared and took no screenshot after the dismiss; SPEC-002 §Flow 8 (amended 2026-09-20) declares a missed W-3 after a real cancel a known best-effort limitation, not blocking — Q-3 for Porter. W-3 strings are in both dictionaries (code read) **Q-3 closed: the owner clicked once more — W-3 NOT shown** after the dismiss (`…signin-5-popup-dismissed.png`: landing + Google button only, no alert). AC-5 amended by Porter to "W-3 on cancel is best-effort"; binding part (no user, landing) proven | PASS (AC-5 as amended; W-3 gap recorded) |
| 13 | AC-5 corollary: W-3 must NOT show on a plain first visit (SPEC-002 §Flow 8 amendment: "a defect if it does") | negative | #1, #2, #9 fresh visits | No alert | `.ant-alert` = 0 on every signed-out render | PASS |
| 14 | AC-6 no email+password page or form (BE) | negative | Probe `POST /auth/login`, `/auth/password`, `/auth/register`, `/auth/email`, `/login`, `/signup`, `GET /auth/login` with `{email,password}`; route list; grep | All 404; only Google/me/logout under `/auth`; no "password" | All 7 → `404`; mounted routes: `auth: GET /me, POST /logout, POST /google` (+ ideas/admin/health); `grep -rni password src/` → nothing | PASS |
| 15 | AC-6 (FE) | negative | Signed-out `/` and `/ideas/new`: count `<form>`, `input[type=password|email]`; app routes | None | `forms: 0`, `pwInputs: 0`; `src/app` has only `/` and `/ideas/new`; grep `password` in `possibility-front/src` → nothing | PASS |
| 16 | Session hygiene: no cookie / bogus cookie / logout clears | edge | `GET /auth/me` with none, with `abc.def.ghi`; `POST /auth/logout` | 401, 401, 204 + clearing cookie | `401 NOT_SIGNED_IN` ×2; `204` + `set-cookie: possibility_session=; Max-Age=0; Path=/` | PASS |
| 17 | Edge: Google account without `name` | edge | Harness 3rd call, `name: null` for `qa-tanya-sub-c` | `display_name = email` | `no-name sign-in: isNew:true, tier:"Ordinary", name:"qa-tanya-c@example.com"` | PASS |
| 18 | AC-4 real sign-out by the owner → landing | happy | Round 2: the owner pressed W-2 (ออกจากระบบ) after each sign-in | Landing with the Google button, no idea box | Screenshot `…signin-3.png`: signed-out landing (button focused, header = title + TH/EN only); the owner then signed in again from it (#6) | PASS |
| 19 | AC-6 / no admin page — the owner's own probes | negative | FE dev log shows the owner also opened `/login` and `/admin` | No such pages | `GET /login 404`, `GET /admin 404` in the FE log (admin page is TASK-009, not built — nothing discoverable) | PASS |
| 20 | AC-2 admin flag on the owner's row | edge | `/auth/me` semantics per SPEC-002: `isAdmin` computed from `ADMIN_EMAIL`, never stored | Row has no admin column; tier `Ordinary` like any new user | DB row `siegkung@gmail.com` tier `Ordinary`, no admin column (schema); `isAdmin=true` on `/auth/me` for this row **UNVERIFIED** (would need the owner's live cookie — REQ-004's test, not REQ-002's) | PASS (schema) / deferred to REQ-004 |

## Observations (not defects, for Porter's judgement)
- O-1 Console warns `[GSI_LOGGER]: google.accounts.id.initialize() is called multiple times` after a TH/EN switch (the button is re-initialised per language, TASK-004 design). Google says "only the last instance is used" — harmless as far as I could see; flag for Fern via the chain only if a real sign-in misbehaves after a language switch.
- O-2 Dev-only: `/auth/me` fires twice per load (React StrictMode) — already noted by Fern, not a defect.
- O-3 The real-sign-in run must happen on the origin registered in the owner's Google OAuth client (presumably `http://localhost:3000`); on :3002 Google may refuse the origin. Whoever clicks should free :3000 first (or the owner adds :3002 to the client's authorised origins).

## Defects
None found this round.

## Test data created
| What | Where | End state |
|------|-------|-----------|
| user `qa-tanya-a@example.com` (sub `qa-tanya-sub-a`, tier Ordinary) | SIT `possibility_db.users` | left as-is (reusable for FE session checks; idempotent harness) |
| user `qa-tanya-b@example.com` (sub `qa-tanya-sub-b`, display name "QA Tanya B (renamed)", **tier set to `Seeker` by me** for the returning-user check) | SIT `possibility_db.users` | left as-is — note the tier is artificial |
| user `qa-tanya-c@example.com` (sub `qa-tanya-sub-c`, display name = email) | SIT `possibility_db.users` | left as-is |
| no ideas, no idea_steps, no hire_requests created; Jason's `dev-jason-*` rows untouched | — | `users count: 6` at end (2 dev-jason + 3 qa-tanya + the owner) |
| user `siegkung@gmail.com` — **the owner's real admin account**, created by HIS real Google sign-in (round 2), not by me | SIT `possibility_db.users` | left as-is, **never to be deleted by QA** (real person, admin per SYSTEM-FACTS) |
| harness: `tests/harness/test-001-users-select.ts` (read-only), `test-001-qa-fixture.ts`, `test-001-upsert-path.ts` | coordination repo | kept |
| both product repos: `git status` unchanged by me (no files added/edited); servers started from the shell only | local | BE/FE dev servers left running on :4000 / :3000 (round 2, owner's `.env` untouched, no overrides) |
| screenshots `../project-docs/evidence/2026-09-21-owner-google-signin-1..4.png` | project-docs | taken by the owner, kept as evidence |

## Verdict
**`TEST_PASSED`** — REQ-002 AC-1..AC-6 are met on the real local FE + BE + SIT DB: 18 PASS · 0 FAIL · 0 defects. The core line — a real Google account (`siegkung@gmail.com`) signs in, a user row is created as `Ordinary`, a second sign-in reuses the same row, sign-out returns to the landing — is proven by the owner's round-2 run (DB diff + BE log + his screenshots), everything else by my round 1.
**Stated gaps (not rounded up):** (1) #12 — the W-3 message after a real popup dismiss was not observed by anyone (no screenshot, no report); SPEC-002 §Flow 8 makes it best-effort and non-blocking, and the substantive half of AC-5 (no user created, back on landing) is proven — Porter decides whether AC-5's W-3 clause is accepted as-is (Q-3) or wants one more owner click with a screenshot. (2) #8 — "previous ideas and tier are shown" moves to REQ-003/REQ-001 tests (Q-2 answered OK). (3) #20 — `isAdmin=true` for the owner's row is REQ-004's to prove.

## Questions
- **Q-1 @Porter (Tanya, 2026-09-21) — a credential I do not have:** the last three lines need a real Google account clicked through the GIS popup. Options for the owner (his call, one of): (a) he connects the Claude-in-Chrome extension and leaves his Chrome signed in to Google — I then drive the click in his browser but **he picks the account in the popup himself**; (b) he does the three clicks himself at `http://localhost:3000/ideas/new` with BE :4000 + FE :3000 running (I start them; :3000 must be free — see O-3), once as a first-time account, once again (returning), and once dismissing the popup — and tells Porter "done"; I read the evidence afterwards. Either way I need to know **which Google account** was used (email) so I can find the row — and that it may be a real person's record (his own), which I will not delete.
- **Q-2 @Porter (Tanya, 2026-09-21):** AC-3's "their previous ideas and tier are shown" has no FE until TASK-006/007 land. I propose it is verified under REQ-003/REQ-001's TEST files, and REQ-002 covers "same row, tier untouched" (case #6). OK?
  > answer (Porter 2026-09-21) to Q-2: **OK** — REQ-002 covers "same row, tier untouched" (case #6); "ideas + tier shown" is verified under REQ-003 / REQ-001 tests when TASK-006/007 land.
  > answer (Porter 2026-09-21) to Q-1: carried to the owner 2026-09-21 (options a/b + which account); waiting for his word.
  > closed (Tanya, 2026-09-21): the owner did option (b) with `siegkung@gmail.com`; evidence read and written into cases #5, #6, #12, #18, #19 — thank you.
- **Q-3 @Porter (Tanya, 2026-09-21):** AC-5 reads "returns to the landing page with message W-3". No one saw W-3 after the owner's real dismiss (he did not report it, no screenshot). SPEC-002 §Flow 8 already declares a missed W-3 after a real cancel a best-effort, non-blocking limitation (GIS gives no cancel signal). I have written `TEST_PASSED` on that basis. Your call as REQ owner: (a) accept AC-5's W-3 clause as best-effort and mark DELIVERED, or (b) ask the owner for one more click — button → dismiss popup → screenshot of the landing — and I add the observation (either way the verdict does not change unless W-3 shows on a plain visit, which #13 rules out).
  > answer (Porter 2026-09-21) to Q-3: **(b)** — AC-5 names W-3, so I want it observed once, not assumed. Asking the owner for one more click (button → dismiss popup → screenshot of the landing). REQ-002 stays TEST_PASSED; I set DELIVERED when the observation is in this file (either W-3 seen, or "not seen" recorded as a known best-effort gap).
  > closed (Porter 2026-09-21) Q-3: owner clicked once more — **W-3 NOT shown** after the popup dismiss (screenshot `../project-docs/evidence/2026-09-21-owner-google-signin-5-popup-dismissed.png`; landing + Google button only, no alert). I amended REQ-002 AC-5 in the open: W-3 on cancel is best-effort (GIS has no cancel signal); the binding part is "no user created, landing shown", which is proven. Verdict stands; REQ-002 → DELIVERED with this gap recorded.
