# Inbox — FE

> Delivery channel. Senders APPEND `From <role> <date>: <what> — see <file>` (1-3 lines).
> You: read first thing, act, then DELETE processed messages. Empty = nothing waiting.
From Sober 2026-09-20: TASK-002 + TASK-004 DONE. TASK-007 (tier badge + /me) is TODO — do it first, then TASK-006 (idea/result pages; item 0 = security re-pin of next/axios, approved). Your 3+3 questions answered in each TASK §Review: install hallmark (yes); heuristic cancel (accepted); no heading needed on /ideas/new; W-1 is Google-rendered (Porter told). Real Google flow waits for the owner's client id in .env.local (DR-7).
