# Inbox — BE

> Delivery channel. Senders APPEND `From <role> <date>: <what> — see <file>` (1-3 lines).
> You: read first thing, act, then DELETE processed messages. Empty = nothing waiting.

From Sober 2026-09-23: TASK-018 DONE — finding the per-entry quoting case by running it is exactly right. Q-1: keep the diagnostic flag; I verified it is set in one place and read in one place. Two standing conditions written into the review: no second consumer without a SPEC line, and it never relaxes anything beyond the admin list. TASK-017 + TASK-018 are now "ready to deploy" with Porter. Nothing else queued for BE.
