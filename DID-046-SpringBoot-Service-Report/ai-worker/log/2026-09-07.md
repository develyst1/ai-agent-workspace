# Log — 2026-09-07

## Jason (BE) — TASK-053 / DEF-25 done: all five forms now split label from write-in
Shared base builder (`A9CheckListReportBuilderBase` item 13) + the same form-agnostic gated element added to
a9-transport, a15 and a9-destroy; mock updated in the same change. Before reusing the a4 placement I checked
all three share the title-field geometry (x=48 w=457) — their second-line fields differ (transport y=13/16,
destroy x=94) but that's a different line. **Both branches rendered on all three**: populated → value on the
dotted run 165→545; empty → the run still prints. I probed the empty branch explicitly since you flagged it as
the one that looks fine today. Fail-on-revert proven — re-gluing fired **6** assertions. structure_check
**34/34** with the DEF-25 loop now covering all five forms; 18/18 + BUILD SUCCESS; clip PASS.

🔒 **Freeze discipline.** `git diff HEAD` on a9-transport is **7 insertions, 0 deletions, one file** — the new
element and nothing else; refrow4/refrow5 and all geometry byte-identical. `frozen_check.sh` failed after the
change, correctly, because it didn't know about the ruling. Skipping transport would have blinded it
permanently, so I **encoded the sanctioned exception** instead: nothing outside the evidence subreport may
differ, that file may only *gain* lines, and every added line must belong to the DEF-25 element. Falsified it —
renamed refrow4→refrow4X in transport's evidenceSub → `FAIL … changed outside the sanctioned file`, exit 1.

Worth recording: my **first** falsification passed and I nearly wrote that down as proof. The probe's `print`
ran unconditionally, so nothing had actually changed — a green from a check that never ran. Re-ran with an
assert on the edit. A falsification needs its own sanity check that the defect was really injected.

a6 mock hygiene done (the optional item): the mock carried the literal-dot string while the real a6 builder was
already correct, so the a6 preview was showing a shape the DB path never produces. Real a6 builder untouched.

Open, not mine: real /download per form, both branches — confirm the sample has item-13 data first.
SecurityConfig still unreverted (6th report).
