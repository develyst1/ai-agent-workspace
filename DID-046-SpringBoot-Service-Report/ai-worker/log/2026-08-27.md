# Log — 2026-08-27 — DID-046-SpringBoot-Service-Report

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [09:59] Jason (BE)

- TASK-036 (DEF-17): applied Sober's answer — เลขที่ (item-12 00014 ตามหนังสือขอซื้อ) now reads
  `DOCUMENT_NAME_OTHER`, not `DOCUMENT_NO` (renamed helper `documentNo`→`documentReferenceNo` to match,
  no fallback preference per Sober). a14 item-12(3/5/7) left blank as the accepted gap.
- Verify: test-compile SUCCESS (52/52 jrxml); all 5 PreviewTests render (a6/a9-d/a9-t/a14/a15).
- @Sober: ready for the quick re-review. See TASK-036.

## [10:45] Jason (BE) — TASK-037: 7 REQ-031 smoke tokens delivered, throwaway reverted

- Plain JUnit test (no Spring context, DB-free), key read from `application.yml` at runtime, never printed.
  Ran once, captured `id -> encrypt(id)` for the 7 ids, deleted the file immediately. `git status` on
  `src/test` clean; test-compile re-confirmed green after the delete.
- Tokens are in TASK-037 (table, not the key). @Sober: confirm 0 key leakage + TEMP reverted, then the 7
  tokens → Porter → human runs the REQ-031 six-builder UI smoke.

## [later] Sober (SA) — TASK-036 / DEF-17 re-review: CODE-COMPLETE.
- Verified Jason's เลขที่ change: A9CheckListReportBuilderBase line 307 uses `documentReferenceNo(doc00014)` →
  line 423 `nz(d.getDocumentNameOther())`, no DOCUMENT_NO fallback (comment cites Porter's real-data finding). Helper
  renamed documentNo→documentReferenceNo cleanly; no dangling getDocumentNo. a14 item-12(3/5/7) blank (accepted gap).
  test-compile + 5 PreviewTests green.
- **DEF-17 is CODE-COMPLETE + SA-verified:** entity re-mapped to DID_SPF-surviving cols (500-fix), item-12 dates/nums/
  ticks from the doc row, GRANTOR_ID_CARD_NO fixes :271/:278, เลขที่=DOCUMENT_NAME_OTHER, a14 §4 dates accepted-blank.
- Ball → **QA (Tanya via Porter):** real `/download` อ.9-transport / อ.14 / อ.15 → **200 with values** (เลขที่ where
  DOCUMENT_NAME_OTHER present); a6/38272 canary unchanged; a9-destroy 200. That closes the prod 500.

## [later] Porter (PM) — @Tanya: DEF-17 close-out QA. You CAN do this leg without `/download`.

DEF-17 is code-complete and SA-verified. The QA leg is yours, and one clarification up front so you
don't stall on access:

**You do not need the real `/download` for this.** DEF-17 is a defect in the **builder's** buyer query
— the ORA-00904 fires while assembling the report data, before Jasper is involved. The `/aN/db/{id}`
seams run that same builder, so they exercise it fully. Routing was already proven separately under
REQ-028 and is not what is being tested here.

### Cases — clean build, own instance, read-only
| # | Form | Request | Expected |
|---|---|---|---|
| 1 | อ.9 transport | 38336 (or 37956) | **200** — was 500 |
| 2 | อ.14 | 27300 | **200** — was 500 |
| 3 | อ.15 | 18041 | **200** — was 500 |
| 4 | อ.9 destroy | 38362 | 200, ticks still the correct 7 |
| 5 | อ.6 | 38272 | **identical to before** — canary |

### ⚠️ "200" is the floor, not the pass
DEF-15 returned 200 the whole time it was printing ticks on the wrong lines. So for cases 1–3 also
check, on page 2's item-12 block:

- **Values actually print** where a document exists — `เลขที่` (now from `DOCUMENT_NAME_OTHER`),
  `ลงวันที่` / `วันหมดอายุ` (from the doc row's `ISSUE_DATE` / `EXPIRY_DATE`). If every one of them is
  blank, that is a **fail**, not a pass — it would mean the new wiring reads nothing.
- **`บัตรประชาชนนายกสมาคม` and `บัตรประชาชนนายกสมาคม/ผู้มอบอำนาจ` now show DIFFERENT values** where the
  data differs. They printed identical values before (the `:271`/`:278` duplicate); `GRANTOR_ID_CARD_NO`
  is meant to have fixed that. Same value on both is only acceptable if the request genuinely has the
  same person in both roles — flag it either way and let me judge.
- No literal `null`; 4 signature slots; structure/labels unchanged.

**Known and NOT a bug — do not raise:** อ.14 item-12 (3)/(5)/(7) dates render blank (accepted gap, no
source exists); อ.14 ticks all blank (no attachments exist anywhere in that family); the view's
duplicated word / trailing `-` in รายการ.

**If cases 1–3 return 200 but with nothing in the item-12 write-ins, say so plainly rather than
passing it.** That distinction is the whole point of this run.

## [2026-08-27 10:20] Tanya (QA) — DEF-17 QA-CONFIRMED FIXED (200 + item-12 values populate)
Clean build, own dev instance, read-only /aN/db seams (same builder path as /download).
- **500-fix:** a9-transport/38336 · a14/27300 · a15/18041 → all **200** (were 500); a6/38272 +
  a9-destroy/38362 canaries 200 unchanged; **0 null, 0 ORA-00904** across all five.
- **"200 is the floor" — item-12 values print:** used **37956** (the transport req with item-12 data).
  Real values render — `เลขที่` on 3 lines + **`เลขที่ e0001` from DOCUMENT_NAME_OTHER**, ชื่อนายกสมาคม
  (the DEF-14 blank-name fix), วันหมดอายุ 12/08/2570, one ticked box. Wiring reads data. ✅
- **:271/:278 fix:** the two assoc-president ID lines now differ (บัตรนายกสมาคม `1234455678903` vs
  นายกสมาคม/ผู้มอบอำนาจ blank) — not the old identical duplicate. ✅
- 18041 & 27300 item-12 blank = genuine no-data / the documented a14 accepted gap — not raised.
- TEST-006 + board updated (DEF-17 ✅, TASK-036 QA-confirmed). @Porter: **DEF-17 closed.** REQ-031's
  only remaining blocker is the no-auth-seam gap for a1/a3/open/expand/personChange/planChange — I need
  a reachable path (/download key+ids, or temp seams) to finish that smoke. REQ-009 still parked.

## [later] Porter (PM) — DEF-17 CLOSED. REQ-031's last gap goes to the human, not to more code.

@Tanya: accepted, and you tested the right thing. Switching to **37956** because 38336 had no item-12
data was the judgment that made this a real pass — checking "does it read data" on a request with no
data would have proved nothing, and 200-with-everything-blank is exactly the false pass I was worried
about. `เลขที่ e0001` coming through `DOCUMENT_NAME_OTHER`, the ชื่อนายกสมาคม appearing after DEF-14,
and the two ID lines finally differing are three independent confirmations that the new wiring is live.

**DEF-17 → CLOSED.** The production 500 on อ.9-transport / อ.14 / อ.15 is gone, verified on real data.

### REQ-031's remaining gap — my call, and it is not a code change
Six builders (a1, a3, open, expand, personChange, planChange) have no no-auth seam, so their
freshly-precompiled `.jasper` are unverified at render. Two ways to close it:

- **(a) the human runs a download for each from the UI** — they already did exactly this for the
  REQ-028 routing proof, so the path is known to work and it costs no code.
- **(b) add temporary preview seams** — six new endpoints, on reports nobody has asked us to change,
  in a codebase where we are already carrying one deferred security gap (DEF-16). Adding unauthenticated
  seams to six more forms to satisfy a smoke test is a worse trade than asking for six clicks.

**Going with (a).** Taking it to the human now.

To be clear about what this smoke is actually for: those six reports have **never been re-rendered
since their `.jasper` were regenerated from source**. If any of them was carrying a stale binary — the
DEF-15 condition — this is the moment it surfaces. A different-looking output there is a *discovery*,
not damage: it means that form has been printing from an outdated layout in production and nobody knew.

Until that runs, **REQ-031 stays open.** The build machinery is verified; the blast radius is not.

## [later] Porter (PM) — REQ-031 smoke needs ENCRYPTED ids. Requesting a one-off, not an endpoint.

The human went to run the six-builder smoke and hit the obvious wall: `/api/v1/download/checklist/{enc}`
takes an **encrypted** request id, and they have plain ids (38237 / 38192 / 38280 / 38271 / 38233 /
38275 / 38364).

`CryptoService.encrypt(String)` already exists (`security/service/CryptoService.java:33`, AES with
`crypto.key-base64` from config), so the capability is there — it just isn't reachable from outside.

@Sober — **TASK request: a ONE-OFF that prints the encrypted form of those seven ids.** A throwaway
test/main that autowires `CryptoService`, encrypts the seven, prints them, and is **removed after
use**. Please do it that way rather than the two obvious alternatives:

- **Not a permanent endpoint.** We already carry one deferred data-exposure item (DEF-16) and six
  unauthenticated preview seams I declined to add yesterday for exactly this smoke. An "encrypt any
  id" endpoint is worse than either — it hands anyone the ability to mint valid download tokens for
  arbitrary requests. That is not a trade I'll make for a test run.
- **Not the key handed to me.** I don't need it and shouldn't have it; producing the ciphertext inside
  the app is both safer and guaranteed to match what `decryptToLong` expects.

Constraints: temporary, marked as such (like TASK-031's debug instrumentation), reverted once the
smoke is done. Print only the seven ids — nothing else about those requests.

Once the human has the seven strings they can complete the last REQ-031 leg from the UI, and REQ-031
closes.

## [later] Sober (SA) — TASK-037: encrypt-ids one-off for the REQ-031 smoke
- Confirmed `CryptoService.encrypt(String)` (security/service/CryptoService.java:33) → URL-safe Base64, the inverse of
  `decryptToLong` — so `encrypt("<id>")` yields a valid `/download/checklist/{enc}` token.
- @Jason — **TASK-037 (throwaway, per Porter's guardrails):** a temp test/main that gets `CryptoService` and prints
  `id → encrypt(id)` for 38237/38192/38280/38271/38233/38275/38364. Prefer DB-free (construct with the key from
  application.yml at runtime — do NOT hardcode/print the key); print ONLY the 7 tokens; tag TEMP; revert after. Hand the
  7 tokens up (tokens are the deliverable; the key is not). Back to me to confirm 0 key leakage + TEMP tag.
- Then the 7 tokens → Porter → human runs the REQ-031 UI smoke → REQ-031 closes (last leg).

## [later] Sober (SA) — TASK-037 DONE (SA-verified). 7 tokens → Porter for the REQ-031 smoke.
- Verified: throwaway `Task037EncryptIdsTemp.java` deleted (find = none), 0 lingering TASK-037 refs, no crypto KEY
  hardcoded/printed (only normal CryptoProperties.keyBase64 field usage), `git status src/test` clean. ✅
- @Porter — the 7 encrypted `/api/v1/download/checklist/{token}` tokens for the six-builder smoke:
  | id | token |
  |---|---|
  | 38237 | `1y8xyi2jRXuM7jcEXPi-gsI1Fga-ybEwSb5f-Ba2qtE` |
  | 38192 | `rgCRxMy9uaOd0tsYAvQ_vcOOxbMruaeDr6ns6HvacP4` |
  | 38280 | `jAhS_I3AcoL3o7PsAqaMJNakA5mTDhi15duklgKpoTs` |
  | 38271 | `OdrCK_UCOSm3SDdkI5CIqztPZ5d4iexuU6kHcb0y5tU` |
  | 38233 | `-L-pwr9jip9K4FQjeNhQ_xhnLa9Bdl0NpYFlBJpz6hk` |
  | 38275 | `f-UgQ0n72EYNp17bBLQcRs3dzstI4mbYLO5ZLEYw-wA` |
  | 38364 | `AgcDc-WOCcZ4yyyXmzdKLuY9yIsdl-nhA1LTocb7gtM` |
  Human runs the UI smoke (render one real report per builder from the packaged jar) → REQ-031's last leg → REQ-031 closes.

## [later] Porter (PM) — REQ-031 CLOSED. New work: the two forms whose UI button errors.

### REQ-031 → DELIVERED
Human ran the six-builder smoke from the UI. **Five of the six render fine** — อ.1/อ.3/อ.20 (38280),
เปิดดำเนินการ (38271), ขยายสายการผลิต (38233), เปลี่ยนแปลงโรงงาน (38275), เปลี่ยนกรรมการ (38364).

That answers the question the smoke existed to ask: **none of those forms was carrying a stale
`.jasper`.** The DEF-15 condition was isolated to อ.9 destroy; the rest were already in sync. Worth
recording that the broad smoke came back clean — I argued for it expecting to find something, and
finding nothing is a real result, not a wasted round.

The build machinery is verified end to end: 52 `.jrxml` → 52 `.jasper`, packaged in the jar, `src`
purged, nothing tracked in git, loud-fail if the precompile is skipped. **The stale-`.jasper` failure
mode that caused DEF-7 and DEF-15 is now structurally impossible.**

The two remaining ids (38237, 38192) errored — and that is a **different problem**, not a REQ-031
blocker: `400 Unsupported requestType: CHECKPERSON`. The request resolved, its data loaded fine, and
the failure is "no checklist report exists for this form type". Nothing to do with templates.

### NEW — the human has asked us to take that on
> *"งั้นปิดเลย แล้วมาช่วยเขาแก้เคสนี้"*

**The UI has a checklist button on both forms, and clicking it returns an error.** So this is not
"correct by design" — it is a live, user-facing defect that pre-dates our involvement.

What we know:

| form | REQUEST_TYPE | checklist master | resolver | report |
|---|---|---|---|---|
| ตรวจสอบประวัติ | 0 | **`BgChk` — 7 rows, seeded** | `case 0 → "CHECKPERSON"` | **missing** |
| ตรวจโรงงาน | 1 | **none** | **no case at all** | **missing** |

**ตรวจสอบประวัติ looks like abandoned work, not an oversight** — someone seeded 7 checklist rows and
wired the resolver, then never built the report. The same shape as `ReqSaleInt` being seeded before we
started อ.14.

Raising as **REQ-032**, and I need two things from the human before it can be specced — the **official
form document** for each (we do not invent form structure; that rule has held all project), and
whether ตรวจโรงงาน is genuinely supposed to have a checklist at all, given it has no master and no
resolver case. Going to them now.

## [later] Porter (PM) — Priority set by human: REQ-032 (ตรวจสอบประวัติ) FIRST, อ.4 waits.

Human: *"a4 รอก่อน ทำตรวจสอบประวัติ (PDF) error ก่อน"*

**REQ-029 (อ.4) → back ON HOLD** even though REQ-031 unblocked it. @Sober: do not start TASK-034.
**REQ-032 is the active item.**

I had suggested running them in parallel so the team wouldn't idle; the stakeholder chose otherwise
and their reasoning is sound — REQ-032 is a **live user-facing error** (a button in the UI that
returns 400), while อ.4 is a form nobody can use yet. A broken button beats an unbuilt feature.

**Blocked on one thing: the official form document for ตรวจสอบประวัติ.** Requested from the human.
We do not invent form structure — that rule has caught real errors twice (DEF-6, and the "ขอ/ของ"
question) and I am not relaxing it for a form we know nothing about.

What we already have, so nothing gets re-derived when the form arrives:

| | |
|---|---|
| REQUEST_TYPE | **0** |
| Resolver | `case 0 → "CHECKPERSON"` — already wired, no routing work needed |
| Checklist master | **`BgChk`, 7 rows** — already seeded |
| Failure | `400 Unsupported requestType: CHECKPERSON` — thrown **downstream** of the resolver; the request resolves and its data loads fine, there is simply no report registered for `CHECKPERSON` |
| Sample request | **38237** (has document rows) |
| Sample token | `1y8xyi2jRXuM7jcEXPi-gsI1Fga-ybEwSb5f-Ba2qtE` |

So the gap is genuinely just **the report itself** — definition, builder, template. Routing and master
data are already in place, which is why I read this as abandoned work rather than something never
started.

@Sober: **do not start yet** — I will not hand you a form-truth task without the form. Once the PDF
arrives I'll survey it, map the 7 `BgChk` codes to its evidence lines the way I did for `ReqSpecial` /
`ReqSaleInt` / `ReqImport`, and release it complete.

**ตรวจโรงงาน stays out of scope for now** — no master, no resolver case, no evidence anyone started it.
I've asked the human to check with the system owner whether that button should exist at all; removing
a button is far cheaper than building a report nobody specified.

## [later] Porter (PM) — REQ-032 RELEASED. `BgChk` mapped; nothing left blocking.

Official form filed as `project-docs/PersonCheck-form-official.pdf` and the `BgChk` master mapped
line by line. **REQ-032 → READY_FOR_SA, fully specified.**

@Sober — the shape of this one, so it isn't approached as another อ.9 clone:

1. **This is finishing abandoned work, not a new build.** Routing (`case 0 → CHECKPERSON`), the
   checklist master (`BgChk`), and **all six `.jrxml` templates** already exist. The only missing
   piece is the **report definition + builder**. `request-personCheck` is the only template folder in
   the project without a builder — that is what identifies it as the unfinished document.
2. **Do not clone an อ.9-family builder.** This form is one page with **no signature block, no law
   references, no annex**, and it has a **ครบ/ไม่ครบ verification-result** section none of our forms
   have. The nearest shape is **a1/a3**, which is also what the abandoned templates mirror.
3. **The 7-vs-5 count resolves and is informative:** form item ๔ is the person table's **two columns**
   — `BgChk10204` (บัตรประชาชน) and `BgChk10305` (ทะเบียนบ้าน) — each its own code, matching the two
   tick columns already in `request-personCheck-list.jrxml`. Template and master agree; the original
   design was coherent.
4. ⚠️ **`SEQUENCE` is duplicated in this group** (`BgChk00001` and `BgChk00701` are both `SEQUENCE 1`).
   Bind by `CHECKLIST_CODE` only — as the TICK RULE already requires, but here SEQUENCE isn't merely
   unstable, it is **not unique**.
5. ⚠️ **A hardcoded sample person is sitting in the template** —
   `1. นายทศนิยม หน้าสองหลังสาม` in `request-personCheck-list.jrxml`. Leftover test data that would
   print on a real government document. Must be replaced by the real binding; treat it as part of the
   task, not a cosmetic follow-up.

Acceptance: **38237 renders instead of 400**, output matches the official form, ticks follow the TICK
RULE, **no sample person**, and a1/a3/a6/a9/a14/a15 unaffected.

REQ-029 (อ.4) stays on hold behind this, per the human.

## [later] Sober (SA) — REQ-032 spec'd (SPEC-035 / TASK-038). Actionable now (REQ-031 DELIVERED).
- Finishing the abandoned personCheck report: routing (case 0→CHECKPERSON), BgChk master (7 rows), and the 6
  request-personCheck templates already exist; the ONLY gap is the report definition + builder. Modeled on a1/a3
  (one page, no signature/law/annex, has a ครบ/ไม่ครบ verification-result), NOT อ.9.
- Pinned the data contract from the templates (main: documentTitle/objectiveType/submittedBy/receivedBy/contactDate/
  note + documentItems + verificationResult; documentItems: orderNo/title/checked/optional; verificationResult:
  completed + sections→code/label/status/items). BgChk codes mapped (00701/00302/00203/00606 + item-๔ person table
  10204 บัตร/10305 ทะเบียนบ้าน; skip 00001 อ.2 inactive).
- Traps baked in: **bind by CHECKLIST_CODE only — BgChk SEQUENCE is DUPLICATED/not unique**; **remove the hardcoded
  sample person `นายทศนิยม หน้าสองหลังสาม` from -list.jrxml** (real binding, part of the task); person rows from
  T_T_REQUEST_PER; ครบ/ไม่ครบ + missing/expired from T_T_REQUEST_LOC_CHK; **verify every @Column against the live
  DID_SPF connection, not just the dict (DEF-17 lesson)**; blank never null.
- @Jason — **TASK-038 is yours, actionable now** (REQ-031 DELIVERED → the precompile builds the personCheck .jasper on
  a plain clean package; unlike อ.4 these templates already exist). Acceptance: 38237 → 200 (not 400), matches the form,
  ticks by TICK RULE, no sample person. @Porter — REQ-032 SPEC_DONE.

## [later] Jason (BE) — TASK-038 done, DB-free green. See TASK-038 for full detail.
- Built `PersonCheckReportData`/`PersonCheckReportBuilder` (TICK RULE via `ChecklistCodeBinder`, group `BgChk`,
  by CHECKLIST_CODE only) + wired `ReportDefinition.CHECKPERSON` / `ReportResourceService` (6 methods) /
  `JasperPdfReportService.exportPdfPersonCheck` / `DocumentController` (case + help string).
- Removed the hardcoded sample person from `-list.jrxml`, real `$F{name}`-bound rows now; also fixed the 2nd/3rd
  column headers (both said สำเนาบัตรประชาชน — 3rd is now สำเนาทะเบียนบ้าน).
- Found + fixed **2 more wiring bugs** the abandoned templates had beyond what REQ-032 flagged: (1)
  `documentItems.jrxml`'s nested person-list subreport used `JREmptyDataSource()` (always empty) + a hardcoded
  literal `.jasper` filename; (2) `main.jrxml`'s own documentItems subreport element pointed at a hardcoded
  literal `.jrxml` *source* path instead of its `$P{...}` parameter. Both fixed per the working
  `request-a6-evidence.jrxml` nested-subreport convention (`subDataSource` off the current row, no `<field>`
  needed).
- New `PersonCheckPreviewTest` green (1-page mock PDF, no exceptions). Full regression: all 5 `*PreviewTest`s
  green, `clean package` green (jar built, 52/52 jrxml precompiled).
- **4 judgment calls flagged in TASK-038 for Sober** — not presented as settled: verificationResult computed
  from documentItems (not `T_T_REQUEST_LOC_CHK`, which was unconfirmed); added a PER_TYPE-agnostic person query
  (`findActivePersonsAll`) since `PER_TYPE` 1/2 is signer/attorney-specific and doesn't fit personCheck's
  combined กรรมการ/ผู้ถือหุ้น/ผู้จัดการ list; item ๔'s own checked-flag is aggregated from its person rows (no
  single BgChk code covers it); no History builder (REQ-032 doesn't ask for one, same as อ.9D/อ.9T/อ.14/อ.15).
- @Sober — ready for review, see `inbox/SA.md`. Then QA on 38237 (real DB, not my leg).

## [later] Sober (SA) — TASK-038 REVIEW: DONE (SA-verified). REQ-032 code-complete.
- Verified against dict + code. Ruled Jason's 4 judgment calls (all sound): JC1 compute verificationResult from items
  (T_T_REQUEST_LOC_CHK does NOT exist — no result table; matches a1/a6); JC2 findActivePersonsAll correct (T_T_REQUEST_PER
  IS the check list, PER_TYPE orthogonal); JC3 per-person ticks join docs by REF_ID=person+DOCUMENT_ID+hasFile gated by
  BgChk10204/10305 (genuinely per-person, RequestPerEntity maps only real cols → NO DEF-17 risk); JC4 no History = scope.
- Sample person `นายทศนิยม` removed (grep 0); mislabeled col header fixed; 2 template wiring bug-fixes
  (JREmptyDataSource→subDataSource, literal path→$P{param}) necessary + a6-convention-correct. Wiring present
  (ReportDefinition.CHECKPERSON, DocumentController case + help string). PreviewTest + clean package green.
- **Accepted limitation (record it):** the "เอกสารที่หมดอายุ ๑–๓" verification sub-section has no tick-derivable source
  and no table → renders empty. Like a14 §4 / อ.7-routing accepted gaps.
- Ball → QA (Tanya via Porter): **38237 → 200** (not 400), matches PersonCheck-form-official.pdf, per-person ticks land
  for persons with real บัตร/ทะเบียนบ้าน docs, NO sample person; a1/a3/a6/a9/a14/a15 unaffected. That closes REQ-032.

## [later] Porter (PM) — REQ-032 code accepted → QA. @Tanya, one case, with the traps named.

@Sober: accepted. Two things I want on the record because they are the reason this went smoothly:

- **Jason surfaced four judgment calls instead of silently deciding them**, and you ruled each with
  evidence — notably JC1, where `T_T_REQUEST_LOC_CHK` turned out **not to exist**. That is the DEF-11
  failure mode caught *before* it was written, which is exactly the guard we added SPEC-027 for.
- **The sample person is gone** (`grep 0`). That was the one thing in this task that could have put a
  fabricated name on a real government document.

**Accepted limitation recorded:** the `เอกสารที่หมดอายุ ๑–๓` sub-section has no derivable source and
renders empty. Same class as the อ.14 §4 dates and the อ.7 routing gap — an explicit blank on the
board, not a silent hole. I'll re-raise it only if the stakeholder asks for that section to populate.

### @Tanya — REQ-032 QA, single case
Clean build, own instance, read-only. **`/download` with the token below, or `/personCheck/db/38237`
if a seam exists** — ask me rather than improvising if neither reaches it.

- Request **38237** · token `1y8xyi2jRXuM7jcEXPi-gsI1Fga-ybEwSb5f-Ba2qtE`
- **200 with a PDF, not `400 Unsupported requestType: CHECKPERSON`** — that 400 is the defect.
- Compare against `project-docs/PersonCheck-form-official.pdf`: heading, the 5 evidence items,
  the person table, the `เอกสารประกอบเรื่อง ครบ/ไม่ครบ` block, and the
  ผู้มายื่นเรื่อง / เจ้าหน้าที่รับเรื่อง / วันที่มาติดต่อ footer.
- **Two things specific to this form — check them explicitly:**
  1. **No `นายทศนิยม หน้าสองหลังสาม` anywhere.** A leftover sample name was embedded in the template.
     If it appears, that is a stop-everything finding.
  2. **The person table's ticks are PER PERSON**, across two columns (บัตรประชาชน / ทะเบียนบ้าน).
     A person with a real บัตร doc should tick that column only. All-ticked or all-blank across every
     row is suspicious — say so rather than passing it.
- Regression: a1 (38280) plus one of a6/a9/a14/a15 still render.

**Known and NOT a bug:** `เอกสารที่หมดอายุ ๑–๓` renders empty (no source exists).

This is the last thing standing between us and REQ-029 (อ.4), which the human parked behind it.

## [2026-08-27 14:45] Tanya (QA) — REQ-032: core PASS, one null-leak (DEF-18) blocks close
Reached 38237 via the real `/download/checklist/{token}` (download gate is OFF in dev — no key needed).
- **38237 → HTTP 200 PDF** (was `400 Unsupported requestType: CHECKPERSON`). The primary fix works.
- Matches `PersonCheck-form-official.pdf`: heading, items ๑–๕, person table (headers บัตรประชาชน /
  ทะเบียนบ้าน), เอกสารประกอบเรื่อง ครบ/ไม่ครบ/…, footer strings. **Per-person ticks land** — item ๔:
  `1. นายแพทย์ มงคล มีชัย`, both columns ticked; items 1/2/3/5 ticked; ครบ ticked.
- **Hardcoded sample person `นายทศนิยม` is gone** ✓. Header 3rd col corrected to สำเนาทะเบียนบ้าน ✓.
  Canary a6/38272 → 200 ✓.
- **🟠 DEF-18 (blocks clean close):** footer `วันที่มาติดต่อ` prints literal **`"null"`** (contactDate null,
  not blanked). Violates SPEC-035 "blank never null" + the no-literal-null must-hold. Only that one field;
  `เจ้าหน้าที่รับเรื่อง` blanks correctly. Likely just a missing `nz()` guard. QA can't fix (boundary).
- TEST-007 + board updated. @Porter: REQ-032 is one `nz()` away — route **DEF-18** to Sober/Jason, then
  I re-render 38237 and close. Nothing else queued for QA (REQ-009 still parked).

## [later] Porter (PM) — DEF-18 → Sober. And "blank never null" keeps coming back; worth a guard.

@Tanya: good pass — you proved the thing that mattered (**per-person ticks actually land**: item ๔ shows
`1. นายแพทย์ มงคล มีชัย` with both columns ticked, not all-on or all-blank), and you held the close for
one literal `"null"` rather than waving it through. That is the right call: a `null` printed on a Thai
government form is not cosmetic.

@Sober — **DEF-18**: footer `วันที่มาติดต่อ` renders the literal string `"null"` on 38237.
`เจ้าหน้าที่รับเรื่อง` blanks correctly, so it is that one field missing its `nz()` guard. One-line fix,
then Tanya re-renders and REQ-032 closes.

**The part I care about more than the fix:** this is the *fourth* time literal `"null"` has reached a
rendered form — DEF-5 (อ.9 "ลงวันที่ null"), the REQ-021 sweep across อ.6, the blank-never-null rule
written into every SPEC since, and now a brand-new form reintroduces it on day one. The rule is
written down and everyone agrees with it; it still doesn't hold, because it depends on a human
remembering `nz()` at every binding site.

Please propose something structural rather than another sweep — e.g. a shared render helper that
cannot emit `null`, and/or a PreviewTest assertion that fails the build if any rendered text contains
the literal `null`. We already have PreviewTests rendering all forms DB-free; asserting on their text
would have caught this before QA ever saw it, and would catch the next one automatically. Cheap, and
it converts a discipline problem into a build failure.

Not a blocker for DEF-18 itself — fix that first, propose the guard after.

**Noted separately:** Tanya reached `/download` without a key because the dev-profile gate is
deliberately OFF (REQ-004/REQ-018). Convenient for QA, and still on the pre-production checklist
alongside DEF-16.

## [later] Porter (PM) — REQ-032 ADDENDUM: stakeholder revised the layout. Write-in rows + หมายเหตุ.

Human adjusted the layout/margins themselves and handed back a revised target. Filed as an addendum
in REQ-032. @Sober — four changes, plus one correction of mine.

1. **Person table: minimum 5 rows**, growing beyond 5 when the request has more people. It currently
   prints only the rows that exist (the sample showed one).
2. **Minimum 3 numbered write-in rows** under each of: ไม่ครบ ขาดเอกสารดังนี้ · **แก้ไข** ·
   เอกสารที่หมดอายุ · **เอกสารเพิ่มเติม อื่น ๆ**.
3. **Add a `หมายเหตุ` section** after เอกสารเพิ่มเติม อื่น ๆ, before the footer.
4. Their revision shows `วันที่มาติดต่อ 08/05/2569` with **no time**; ours renders `… 14:22`. Match
   theirs — but if the time is deliberate, raise it rather than deciding silently.

Note their version has **two verification options the official PDF I surveyed does not** — `แก้ไข` and
`เอกสารเพิ่มเติม อื่น ๆ`. **Their revision supersedes my PDF reading for this block.**

### Correcting myself: the "accepted limitation" was never a data problem
I recorded `เอกสารที่หมดอายุ ๑–๓` as an accepted gap because no table sources it, and I reasoned about
it the way we reason about อ.14's §4 dates. Wrong frame. **This is a paper form and those lines are
filled in by hand** — they need blank ruled rows, not a data binding. Same for the person table's
empty rows.

Withdrawing that limitation from the board. Worth noticing the pattern in my own error: I've spent
this project hunting for a data source behind every printed field, and here the answer was that some
fields have no source *by design*. Ask what the paper is for before asking which table feeds it.

DEF-18 (`วันที่มาติดต่อ` printing literal `null`) still stands and should land in the same pass.

## [later] Sober (SA) — DEF-18 + personCheck revised-form layout spec'd (TASK-039, closes REQ-032)
- Took Porter's frame correction: the verification write-in rows + extra person rows are hand-filled BLANK ruled rows,
  not data bindings. Withdrawing my "expired-docs accepted limitation" — it was the wrong frame (I kept hunting a table
  behind a paper field). @Jason — remove the compute-from-items for missing/expired; render fixed blank rows instead.
- TASK-039 (one pass, closes REQ-032):
  1. DEF-18: `วันที่มาติดต่อ`/contactDate prints literal "null" → nz/blank guard; **sweep the whole path, no literal null anywhere**.
  2. วันที่มาติดต่อ = **date-only** (ours shows 14:22; the human's is date-only) — ThaiDateFormatUtil date formatter.
  3. Person table **min 5 rows** (pad blank to 5 in the builder; grow if >5).
  4. Verification: **4 sub-sections × ≥3 blank numbered rows** — ขาดเอกสาร / **แก้ไข (NEW)** / หมดอายุ / **เพิ่มเติมอื่นๆ (NEW)**.
  5. Add **หมายเหตุ** before the footer. Template edits sanctioned (revised form requires them); REQ-031 precompile builds the .jasper.
- @Jason — TASK-039 is yours. Then QA re-verifies 38237 → 200, no literal null, date-only, min-5 persons, 4 sub-sections + หมายเหตุ → REQ-032 closes.

## [later] Jason (BE) — TASK-039 done, DB-free green. See TASK-039 for full detail.
- **DEF-18 root cause**: not a missing `nz()` — a null String field serialized as JSON `null`, and
  JsonDataSource/Jackson's `NullNode.asText()` returns the literal `"null"`. Real fix: `@JsonInclude(NON_NULL)`
  on the top-level `PersonCheckReportData` record (nested records already had it) so the key is dropped
  entirely, not just emptied. Kept `nz()` guards on `note`/`contactDate` too, defense-in-depth.
- `contactDate` now date-only (`ThaiDateFormatUtil.formatOrNull` on `.toLocalDate()`, no time).
- Person table pads real persons to min 5 blank rows (grows past 5 as-is); item ๔'s own checked-flag still
  computed from real persons only, pad rows excluded.
- Removed the old compute-from-items missing/expired logic entirely (dead code deleted) — all 4 verification
  sub-sections (ไม่ครบ / **แก้ไข**(new) / เอกสารที่หมดอายุ / **เอกสารเพิ่มเติมอื่นๆ**(new)) now always render 3 blank
  numbered write-in rows (hand-fill paper lines, not data), per Porter's frame correction.
- Added `หมายเหตุ` as a static blank-ruled band in main.jrxml, before the footer — no data binding, same
  treatment as the write-in rows.
- `PersonCheckPreviewTest` extended with a DEF-18 regression guard (`contactDate=null` in the mock +
  `assertFalse(json.contains("null"))`) so this class of bug fails the build going forward, not just this
  once. All 5 PreviewTests green, `clean package` green (52/52 jrxml).
- @Sober — ready for review, see `inbox/SA.md`. Then QA re-verifies 38237 to close REQ-032.

## [later] Sober (SA) — TASK-039 REVIEW: DONE (SA-verified). REQ-032 code-complete.
- Verified: @JsonInclude(NON_NULL) on top-level PersonCheckReportData (line 7) — the correct DEF-18 fix (NullNode.asText()
  returns literal "null"; dropping the key is what actually blanks it; nz is defense-in-depth). PreviewTest now asserts
  `!json.contains("null")` — good regression guard at the JSON layer. contactDate=dateOnly(...) (date-only, no time).
  padPersons→min 5 (pad rows don't count toward item-๔ aggregate). 4 verify sub-sections (missing/แก้ไข NEW/expired/
  เพิ่มเติมอื่นๆ NEW) each 3 blank numbered ruled rows; compute-from-items dead code deleted. หมายเหตุ band in main. All
  5 PreviewTests green, clean package green, PDF now 2pp.
- **DEF-18 FIXED + revised layout done → REQ-032 code-complete.**
- Minor flag (low-priority, not a blocker) @Porter: the top-level `ครบ/ไม่ครบ` checkbox is still **computed** from
  documentItems, while the rest of the verification block is now hand-fill-blank. Confirm whether ครบ should stay
  auto-computed or also be a blank checkbox for hand-marking — either is defensible; Jason kept it computed.
- Ball → QA (Tanya via Porter): re-verify **38237 → 200**, no literal null, date-only, ≥5 person rows, 4 sub-sections +
  หมายเหตุ match the human's revised form, real person ticks, no sample person → **closes REQ-032**.

## [later] Porter (PM) — 📌 ครบ/ไม่ครบ RULE, from the stakeholder. This is a real behaviour change.

Human's answer to my ครบ/ไม่ครบ question — it is not "keep it computed", it is a fuller rule than we
had, and it changes what TASK-039 just built.

### The rule
1. **Completeness is judged on items ๑–๔ only.** Item ๕ (หนังสือมอบอำนาจ) is **optional** and is
   **excluded** from the calculation.
2. **Item ๔'s own checkbox ticks only if at least one person exists in the table** — presence of a
   person row, not attachment state.
3. **Item ๔ passing is not enough for ครบ.** Every person inside the table must have their attachments
   complete. If a person is missing บัตรประชาชน or ทะเบียนบ้าน, the form is **ไม่ครบ** — even though
   item ๔ itself is ticked.
4. When anything in ๑–๔ is incomplete: tick **ไม่ครบ** and **write what is missing** into the
   "ขาดเอกสารดังนี้" lines — **naming the person** when the gap is a person's attachment.

### ⚠️ This partly reverses what we just shipped
TASK-039 made all four verification sub-sections blank hand-fill rows. Under this rule the
**"ไม่ครบ ขาดเอกสารดังนี้" section is system-populated**, not hand-written.

**My reading, stated so it can be corrected rather than guessed at silently:** the system fills the
missing items into those numbered rows, and any rows it doesn't need stay blank and ruled for
hand-writing. `แก้ไข` / `เอกสารที่หมดอายุ` / `เอกสารเพิ่มเติม` / `หมายเหตุ` remain fully hand-fill.
I've told the human that is how I read it; if wrong, it's a small correction, but I'm not shipping it
as an assumption nobody saw.

@Sober — points for the SPEC:
- **`ครบ` = items ๑,๒,๓ ticked AND item ๔ ticked AND every person row complete.** Anything else ⇒
  `ไม่ครบ`. Item ๕ excluded entirely — do not let it influence either box.
- **Padding rows must not count.** The table pads to 5; only real person rows participate in the
  ครบ/ไม่ครบ judgement or in item ๔'s tick. Jason already kept pad rows out of the item-๔ aggregate —
  the same exclusion has to hold here.
- **A person-level gap must name the person** in the missing list, not just the document type.
  "ขาดสำเนาทะเบียนบ้าน" is not actionable; "ขาดสำเนาทะเบียนบ้าน — นายแพทย์ มงคล มีชัย" is.
- Zero people ⇒ item ๔ unticked ⇒ ไม่ครบ. Make sure that path is handled and not treated as "nothing
  missing".

QA is held until this lands — no point testing a rule we already know is changing.

## [later] Porter (PM) — 🔴 REQ-032 ADDENDUM 2: two entry points, and the verification block is DATA

Stakeholder corrected my understanding. This is a scope correction, not a detail — filed in full in
REQ-032 Addendum 2.

**The form is reached two ways:** (1) the normal ดู checklist button on the request — the live path we
built; (2) the ดู checklist button **inside the request's history/activity log** — which reads a
**stored snapshot** in the `T_T_REQUEST_CHECKLIST_FORM` family. The resolver already has the hook
(`resolveChecklistRequestTypeByChecklistFormId`); we had simply never known what it was for.

The snapshot family carries everything the form prints, already flattened:
`…_FORM` (footer: `REQUEST_PERSON_NAME` / `CREATE_NAME` / `REQUEST_CONTACT_DATE`, plus
`FORM_CODE='BgChk'`) · `…_DOC` (`CHECKLIST_NAME` + **`HAS_FILE`**) · `…_PER`
(`REQUEST_CHECKLIST_PERSON` + **`HAS_ID_CARD_NO_FILE`** + **`HAS_HOUSE_REG_NO_FILE`**) ·
**`…_DTL`** (the verification block, keyed by `TITLE_CHECKLIST_CODE` = DocIncomp / DocEdit /
DocExpire / DocExtra / Other, titles in `T_S_COMMON_CODE` `TitleChecklist`).

### Correcting myself — twice on the same block, in opposite directions
This morning I recorded `เอกสารที่หมดอายุ` as an **accepted limitation** (no data source). Then I
withdrew that and called the whole block **hand-fill**. Both were wrong: **it is stored data**, in a
table family none of us had looked at.

The through-line in my errors is the same each time: I reasoned about where a field *should* come
from instead of asking what the screen the user clicks actually does. The stakeholder's answer here
wasn't a new decision — it was a description of a feature that already exists.

@Sober — the design consequence, and it matters:
- **Entry 2 must render the snapshot verbatim.** Labels, ticks, persons and verification text as
  stored. **Do not recompute anything for a historical record** — the ครบ/ไม่ครบ rule applies to the
  *live* path only. Recomputing a history entry from today's data would silently rewrite what was
  recorded, which is worse than showing nothing.
- One template, two data paths.
- Minimum row counts (5 persons, 3 per section) and หมายเหตุ apply to both — those are page furniture.

**Two things to determine rather than assume:**
1. Does entry 1 also *write* a snapshot, or do only history entries have them?
2. `…_DTL` carries **`REASON` and `DESCRIPTION`**. In the samples `DESCRIPTION` reads like the document
   name and `REASON` like the officer's note — but confirm before wiring. A swapped pair renders
   perfectly plausibly, which is how DEF-12's `-` survived.

QA stays held. Sample to build against: `REQUEST_CHECKLIST_FORM_ID = 211` (REQUEST_ID 37832) — it has
6 doc rows, 1 person with both ticks, and real footer values.

## [later] Sober (SA) — REQ-032 Addendum 2 spec'd (SPEC-036 / TASK-040): personCheck HISTORY snapshot leg
- Confirmed the snapshot family already exists in code and a1/a3/a6 history builders use it — so this is a CLONE of
  `A1CheckListHistoryReportBuilder`, not new machinery. Entities present: RequestCheckListForm/Doc/Per/Dtl.
- **Resolved Porter's 2 questions from working code (no guessing):** Q2 — a1 renders `formatDetailTitle(DESCRIPTION,
  REASON)` → DESCRIPTION is primary (doc name), REASON secondary (note); personCheck uses the same (the exact swap
  DEF-12 warned about). Q1 — a1 has separate live+history builders (two-path model) → personCheck live stays computed
  (TASK-038/039), history reads the snapshot; whether live also writes a snapshot doesn't change the design.
- TASK-040: new `PersonCheckHistoryReportBuilder` (verbatim from …_FORM/_DOC/_PER/_DTL, DESCRIPTION-primary, ครบ from
  snapshot HAS_FILE not live, NO TICK RULE/ChecklistCodeBinder), + `RequestCheckListPerRepository.findByRequestChecklistFormId`
  if absent, + DocumentController HISTORY switch `case "CHECKPERSON"`. Reuses PersonCheckReportData + 6 templates + exportPdfPersonCheck.
  Min-5 persons / 3-per-section / หมายเหตุ apply to both paths (page furniture).
- Reverses my earlier JC4 ("no history builder") — the history leg IS needed. **Verify …_PER cols (HAS_ID_CARD_NO_FILE/
  HAS_HOUSE_REG_NO_FILE) against live DID_SPF** (DEF-17 — snapshot family not in dict; Form/Doc/Dtl proven by a1, _PER new).
- @Jason — TASK-040 yours. Sample: FORM_ID 211 (REQUEST_ID 37832). @Porter — REQ-032 Addendum 2 SPEC_DONE.

## [later] Jason (BE) — TASK-040 done, DB-free green. See TASK-040 for full detail.
- `PersonCheckHistoryReportBuilder` cloned from `A1CheckListHistoryReportBuilder`'s pattern: `…_FORM` → footer
  + nz-guarded note/contactDate, `…_DOC` → documentItems verbatim (title=CHECKLIST_NAME, checked=HAS_FILE==1),
  `…_PER` → persons padded to min 5, `…_DTL` per TITLE_CHECKLIST_CODE → 4 sections padded to min 3,
  DESCRIPTION-primary via `formatDetailTitle`. ครบ = all documentItems checked, snapshot-only — no
  ChecklistCodeBinder, no live repos. `RequestCheckListPerEntity`/`Repository` already existed, no new
  repo method needed. Wired `DocumentController` HISTORY switch `case "CHECKPERSON"`.
- **⚠️ Flagging a structural gap TASK-040 didn't spec**: the task says map `…_DOC` verbatim 1:1, but the
  *template* (unchanged) can only render the person table by pulling it off ONE specific documentItems
  row — and a verbatim snapshot dump has no row that's structurally "item ๔" the way the live builder's
  folded row is. Added a `CHECKLIST_NAME`-contains-"บัตรประชาชน" heuristic to pick the row (with a
  last-row fallback so the table never silently vanishes) — unconfirmed against a real snapshot's actual
  text. Full detail + rationale in TASK-040 "Done".
- Extended `PersonCheckPreviewTest` with a second (history-shape) render scenario — 6 raw doc rows, DEF-18
  no-null guard passes for both. All 5 PreviewTests green, `clean package` green.
- **Also flagging, separately, not part of this task**: Porter's ครบ/ไม่ครบ RULE (items ๑–๔ only, item ๔
  ticks on person-existence, every person's attachments must be complete, missing list must name the
  person) hasn't landed in a SPEC/TASK yet. It's a **live-path** behavior change (TASK-039's live builder
  still computes the old way and still renders blank hand-fill rows for ไม่ครบ) — history is unaffected
  since a snapshot is frozen by definition. Not touching the live builder without a task for it.
- @Sober — ready for review, see `inbox/SA.md`. Then QA: 38237 (live) + FORM_ID 211 (history) → closes REQ-032.

## [later] Sober (SA) — TASK-040 REVIEW: DONE (interim); + spec'd Porter's ครบ/ไม่ครบ rule (SPEC-037/TASK-041)
- TASK-040 (history): verified verbatim-from-snapshot, DESCRIPTION-primary (cloned a1), no live repos, history-switch
  case CHECKPERSON wired. DONE. **Person-row heuristic accepted as INTERIM** (safe fallback) — can't harden DB-free
  (…_DOC.CHECKLIST_ID drifts on rebuild, SEQUENCE BgChk-duplicated; frozen CHECKLIST_NAME is the only stable key).
  @Porter — DATA REQUEST: FORM_ID 211's …_DOC rows (CHECKLIST_ID/NAME/SEQUENCE/HAS_FILE) so Jason matches the two
  person-doc lines by exact stored name, not a substring guess; QA confirms the person table lands on the correct line.
  Told Jason NOT to resolve via the live master (that's the coupling the history path must avoid).
- **Porter's ครบ/ไม่ครบ RULE → SPEC-037/TASK-041 (LIVE builder only):** ครบ ⟺ ๑,๒,๓ ticked AND item๔ ticked AND every
  real person complete (both docs); ๕ EXCLUDED; item๔ ticks on person-presence; pad rows don't count; zero people ⇒
  ไม่ครบ; **system-populate "ขาดเอกสารดังนี้" naming the person** (`ขาด<doc> — <name>`), rest blank hand-fill. Partly
  reverses TASK-039 (that section was all-blank; now DocIncomp is system-filled). History path (TASK-040) UNAFFECTED — snapshot frozen.
- @Jason — TASK-041 (live only). Two REQ-032 leftovers now tracked: person-row exact-match (needs 211 doc-names) + this rule.

## [later] Jason (BE) — TASK-041 done, DB-free green. See TASK-041 for full detail.
- `completed` now = item1 && item2 && item3 && item4Ticked && everyPersonComplete — item ๕ never enters the
  formula. Item ๔'s tick flipped from attachment-based to **presence-based** (≥1 real person), pad rows
  excluded from both the tick and the completeness check (computed from realPersons pre-pad).
- New `buildMissingList()` system-populates "ไม่ครบ ขาดเอกสารดังนี้": unticked ๑/๒/๓ by title, then one line
  per person per missing doc (`ขาด<doc> — <name>`), padded blank to the existing min-3 floor. Other 3
  sub-sections + หมายเหตุ stay hand-fill, unchanged. `PersonCheckHistoryReportBuilder` untouched.
- **New test precedent, flagged as deliberate**: added `PersonCheckReportBuilderTest` — plain JUnit+Mockito,
  no Spring context, the first builder-level unit test in this codebase (everything else is
  PreviewTest-layout-only or reading). Did this because a business-rule change's branching logic isn't
  provable by a visual render and BE has no DB to check against. 4/4 scenarios green (all-complete→ครบ,
  missing-houseReg→named line+ไม่ครบ, item5-absent→still ครบ, zero-people→item4 untick+ไม่ครบ).
- All 5 PreviewTests green (bypass the builder, unaffected by this change), `clean package` green.
- @Sober — ready for review, see `inbox/SA.md`. Then QA: 38237 (live, rule applies) + FORM_ID 211 (history,
  unchanged) → closes REQ-032.

## [later] Sober (SA) — TASK-041 REVIEW: DONE (SA-verified). REQ-032 live path fully complete.
- Verified against SPEC-037: `item4Ticked=!realPersons.isEmpty()` (presence); `everyPersonComplete` on real persons;
  `completed=item1&item2&item3&item4Ticked&everyPersonComplete` — **item ๕ not in the formula**; `buildMissingList`
  names person per missing doc + titles unticked ๑/๒/๓, pad rows excluded, min-3 preserved. History builder untouched.
- **Endorsing the new precedent:** Jason added `PersonCheckReportBuilderTest` (Mockito, no Spring) — the first
  builder-level UNIT test in this codebase, covering all 4 rule scenarios. Correct call: branching business rules
  aren't provable by a visual PreviewTest and BE has no DB — a unit test is the right proof layer. Good pattern to reuse.
- **REQ-032 live path = CODE-COMPLETE (TASK-038 + 039 + 041); history = SA-verified (TASK-040, interim person-row match).**
- Ball → QA (both paths: 38237 live rule + FORM_ID 211 history verbatim) + @Porter (DATA REQ: 211 …_DOC names to harden
  the person-row match). Those two close REQ-032. Nothing else for Sober.

## [later] Porter (PM) — REQ-032 accepted → QA. Blocking the DATA REQUEST: I already have FORM_ID 211's docs.

@Sober: live path accepted. Two things worth naming:

**The unit test is the right precedent and I'm endorsing it as a standard, not a one-off.** The
ครบ/ไม่ครบ rule is branching business logic — a visual PreviewTest can show *a* result but cannot show
that all four branches are right, and BE has no DB. Every defect this project has shipped came from
exactly that gap: DEF-13's tick alignment, DEF-15's placement, DEF-17's column — all rendered
plausibly and all passed the checks we had. **Where a rule has branches, prove the branches.**

**Blocking your DATA REQUEST — I already hold FORM_ID 211's `_DOC` rows.** The human pasted them
earlier; no need to ask again:

| SEQ | CHECKLIST_NAME | HAS_FILE | CHECKLIST_ID |
|---|---|---|---|
| 1 | บัญชีรายชื่อผู้ถือหุ้นซึ่งแสดงรายละเอียดเกี่ยวกับชื่อ สัญชาติ จำนวนหุ้นที่ถือ | 1 | 17 |
| 2 | สำเนาหนังสือบริคณห์สนธิ | 1 | 18 |
| 3 | สำเนาหนังสือรับรองบริษัทฯ | 1 | 19 |
| 4 | สำเนาบัตรประจำตัวประชาชนของกรรมการ, กรรมการซึ่งมีอำนาจลงนามผูกพันนิติบุคคล, ผู้จัดการ หรือผู้ถือหุ้น | 1 | 20 |
| 5 | สำเนาทะเบียนบ้านของกรรมการ, กรรมการซึ่งมีอำนาจลงนามผูกพันนิติบุคคล, ผู้จัดการ หรือผู้ถือหุ้น | 1 | 21 |
| 6 | หนังสือมอบอำนาจ ในกรณีที่ผู้อื่นคำขอได้รับมอบอำนาจให้ยื่นคำขอแทน | 1 | 22 |

Note `CHECKLIST_ID` 17–22 is a **stable per-row key on the snapshot** — if you are matching person
rows by name today, that column is worth looking at first; matching Thai names as strings is fragile
in a way an id is not. Your call, but decide it deliberately rather than leaving the interim match in.

That is the fourth time a data request has gone up for something already in the log. Not a criticism
of this instance — it is a signal that the log is now too long to re-read. If you want, I'll keep a
short "known DB facts" section on the board so you can check there first; say the word.

### @Tanya — REQ-032 QA, both paths
1. **Live:** request **38237** — the ครบ/ไม่ครบ rule. Check it is judged on items ๑–๔ only (item ๕
   excluded), item ๔ ticks only when a real person exists, and a person missing บัตร or ทะเบียนบ้าน
   forces **ไม่ครบ** with that **person named** in the missing list.
2. **History:** **FORM_ID 211** — must render the snapshot **verbatim**: 6 doc rows all ticked, person
   `นายแพทย์มงคล มีชัย` with both columns ticked, footer
   `ผู้ช่วยศาสตราจารย์วิชาญ ศรีพัฒนา` / `ส.อ.หญิง พัชราภรณ์ ผดุงขวัญ` / `07/05/2569`.
   **Nothing recomputed** — if any value differs from the table above, that is the finding.
3. Still: no literal `null`, no sample person, ≥5 person rows, ≥3 rows per section, หมายเหตุ present.
4. Canary: a6/38272 unchanged.
