# Log — 2026-08-18 (Porter / PM)

> Continues the thread in `2026-08-05.md`. Read that one for everything before today.

## DEF-10 — อ.9 item 7 uses the wrong source (Porter's error)

Human reported, from a **real, system-produced** อ.9 checklist for request **38362**
(their words: *"อันนี้คือใบจริงแล้ว ข้อมูลจริงๆ ไม่มีการแก้ไขเองเลย ฉันให้เขาทำเองมาจากระบบเลย"*):

- PDF prints: `7. ระยะเวลาการอนุญาต  01/01/2569 ถึง 01/07/2569`
- Must print: `7. ระยะเวลาการอนุญาต  180 วัน นับแต่วันที่ได้รับอนุญาต`

`SELECT * FROM T_T_LICENSE WHERE REQUEST_ID = 38362` returns one row, ID 23160, with
`PERIOD_TEXT = "180 วัน นับแต่วันที่ได้รับอนุญาต"` — and **`FORM_ID = 10`** (the licence's form id,
not the request's).

**Root cause is my call, not Jason's.** Under DEF-4 I saw that sample 18847 had no `T_T_LICENSE`
row but did have MOVE `START_DATE`/`END_DATE`, and I generalised from that one incomplete sample
that อ.9 uses the MOVE date range — directing "item7 = MOVE START/END (dropped LICENSE.PERIOD_TEXT)".
18847 simply had no licence issued yet. Logged as such; the lesson matches DEF-5: a sample with
missing data cannot establish a rule.

## Human ruling — one concept for every form

> *"ขอ ให้เป็น คอนเซ้ปนี้หมดนะ"*

Raised as **REQ-023** (READY_FOR_SA) and written into `board.md` as a standing **FORM-RULE**:

1. ข้อ 7 = `T_T_LICENSE.PERIOD_TEXT` **verbatim** — every form: อ.6, อ.9 (both variants),
   อ.14–อ.16, and อ.4–อ.8 when built. No computed range, no `" วัน"` suffix, no reformatting.
2. Join **by `REQUEST_ID` only**. Do **NOT** filter on `T_T_LICENSE.FORM_ID` — 38362 proves the
   licence row carries the licence's form id (10) for an อ.9 request. Same join the human corrected
   us on in REQ-005; `REF_LICENSE_ID` is not the linkage.
3. **No licence row ⇒ blank** (human decision). Never fall back to MOVE `START_DATE`/`END_DATE`,
   never the string "null".
4. Revert the DEF-4 change on อ.9.
5. Oracle 11.2-safe: `List` + `firstOrNull`, never `FETCH FIRST` (DEF-2).

## Relay

@Sober: **REQ-023 is READY_FOR_SA — please pick it up alongside REQ-022.** Two things I want in
the SPEC beyond the fix itself:

- **Make it shared, not per-report.** อ.6 already does this correctly (REQ-005); อ.9 deviates;
  อ.14 is being written right now. Please land it as one place all builders call, so อ.4–อ.8
  inherits it instead of each form re-deriving item 7. If that's a bigger refactor than REQ-023
  should carry, tell me and I'll split it.
- **TASK-018 (อ.14 foundation) should adopt the rule now, in flight** — cheaper than patching after.
  Jason is mid-TASK-018; if that means a small re-cut of his page-1 mapping, better today than after
  QA. Flag to me if it disturbs his current work and I'll sequence it.

Acceptance I'll hold you to: 38362 → the PERIOD_TEXT string on the real DB seam; 18847 → blank;
อ.6 38272/38273/38240 unchanged.

REQ-022 is otherwise unaffected — TASK-018/019 continue.

## [later] Porter (PM) — picking up my three items from Sober's TASK-018 review

1. **`ReqSaleInt` seed hand-off — DELIVERED.** Written to
   `project-docs/A14-checklist-seed-spec-for-data-team.md` (Thai, for the human's data colleague).
   Deliberately asks them to **copy `ReqMove` SEQ 1–11 verbatim** rather than retype the Thai labels —
   items 1–11 are identical between อ.9 transport and อ.14, so copying removes a whole class of
   typo risk. Only genuinely new rows: the 3 tick-only item-12 docs (buyer company reg / govt approval /
   END-USER CERT) + item 13. Asks back for: GROUP_CODE name, the 3 DOCUMENT_IDs, and which of
   27300/34380/35966/36711/36741 they backfilled.
2. **a9 resolver latent defect — recorded on the board** under REQ-014 (your ask). Not folded into
   REQ-022. I'll raise it as its own REQ; it is a **production blocker for อ.9** (real download can't
   route a real อ.9), so it goes ahead of อ.4–อ.8 in my order.
3. **item-5 = BUYER_NAME** — going to the human for confirmation on a real sample. Until they answer,
   BUYER_NAME stands as spec'd; if it turns out to be IMPORTER_NAME it is a one-line change.

## [later] Porter (PM) — item-5 CONFIRMED + raising the a9 resolver defect

**Item-5 source is settled: `T_T_REQUEST_SALE_INT.BUYER_NAME`** — human's decision ("buyer name ไป").
Note for the record: on sample 27300 `BUYER_NAME` and `IMPORTER_NAME` hold the *same* value
("บริษัท สหการวิศวกร จำกัด"), so that sample could not discriminate; the human ruled rather than hunt
for a differing row. No code change needed — TASK-018 already built it this way. SPEC-023's
"confirm during build" note is now closed.

**REQ-024 raised** — the อ.9 resolver leg (see the board row Sober asked me to log). Own REQ, not
folded into REQ-022, and it sits **ahead of อ.4–อ.8** in my order because it blocks อ.9 production.

@Sober: good call pinning a14 item-7 to PERIOD_TEXT inside TASK-018 rather than letting it ship wrong
and patching after.

## [later] Porter (PM) — SEQUENCING DECISION: close อ.9 before returning to อ.14

Human asked whether อ.9 is actually finished before we carry on with อ.14. It is not — two pieces are
open — and they chose to **close อ.9 first**.

@Sober: **please put อ.9 ahead of TASK-019.** Order:

1. **REQ-024** (READY_FOR_SA) — the `resolveFromMove` leg. This is the one that matters: อ.9 is
   otherwise finished and verified, but the production download button still cannot reach it, so อ.9
   **cannot ship**. Copy the shape of the `resolveFromSaleInt` leg you just reviewed in TASK-018 —
   add a leg, don't touch `resolveFromSpecial` or the `REQUEST_TYPE` fallback. Also kill (or comment
   as unreachable) the dead `9/10 → A9` branch so nobody trusts it again.
2. **TASK-020** — the item-7 revert to `PERIOD_TEXT`. Already spec'd, small, independent.
3. **TASK-019** (a14 item-12) resumes after those two. It is not waiting on anything from อ.9, so
   nothing is lost by the detour.

Both อ.9 pieces are small. If REQ-024 turns out bigger than it looks — e.g. the resolver needs a real
strategy refactor rather than one more leg — tell me before Jason starts and I'll re-plan rather than
let it eat the a14 momentum.

## [later] Porter (PM) — REQ-024 + REQ-023 closed in code; NEW REQ-025 (item list from VW_REQUEST_DTL)

Sober delivered both อ.9 pieces: TASK-020 (item-7 → PERIOD_TEXT) and TASK-021 (`resolveFromMove`).
Worth recording that REQ-024 was **worse than I described it**: Sober found the resolver's
`default → throw`, so the real `/download` **500'd for every อ.9**, not merely served mock. Both are
SA-verified in code; the real-routing proof is a DB leg (human/QA).

### NEW — REQ-025 (READY_FOR_SA)
Human's instruction, verbatim:
> *"แก้ไขให้มาใช้ตรงนี้แทนนะ ในส่วนของ รายการ ใน checklist a6 a9destroy a9 and everything"*

All checklist reports must read their **รายการ** from the existing view **`VW_REQUEST_DTL`**
(full definition filed in REQ-025), replacing our own `T_T_REQUEST_DTL` query. Applies to
อ.6, อ.9 destroy, อ.9 transport, อ.14 **and every report after**.

@Sober: three things I want from the SPEC.

1. **Print `PRODUCT_NAME_DISPLAY_LICENSE`** as the item name — the view already assembles the exact
   licence-facing string (skips `'-'`, and skips `PRODUCT_GROUP_NAME` when it duplicates
   `PRODUCT_TYPE_NAME`). If any form genuinely needs a different column, name it and justify it in the
   SPEC rather than deciding quietly.
2. **Use the view's resolved lookups** — `QUANTITY_UNIT_NAME1/2`, `USAGE_TYPE_NAME` etc. — and delete
   whatever local joins/derivations they replace. This is a straight swap of the item-list source;
   page-1, ticks, signatures and law refs must not move.
3. ⚠️ **Flag, don't inherit blindly:** the view's `WHERE DTL.STATUS != 'D'` **drops NULL-status rows**
   (Oracle `NULL != 'D'` → UNKNOWN) — the identical trap that produced REQ-015, where the human ruled
   *"ทุกสถานะ ยกเว้น D ถ้า null ก็เอาขึ้นปกติ"*. Its own `INNER JOIN` on `NVL(DTL.STATUS,'C')` shows the
   view *intends* to tolerate NULL, so the `WHERE` looks like an oversight in their view.
   **The view is not ours — do not modify it.** Report to me how many rows it actually drops and I
   take the question to the human. If it drops nothing in practice we adopt as-is.

Read-only mapping (`@Immutable` / projection — your call). We never create/alter/drop the view.

Sequencing: REQ-025 touches every report, so I'd rather land it **before** TASK-019 adds more item-12
code on top of the old source — but that's your read; tell me if the two don't actually collide.

## [later] Porter (PM) — REQ-025 NULL question: CLOSED by the human, adopt the view as-is

> *"เอางี้ เรื่องแค่นี้ปล่อยตามเขาไปเลย ถ้าผิดก็ให้เขามาแก้วิวกันเอง"*

@Sober: **drop point 3 of my last relay.** Do NOT count the NULL-status rows, do not raise it, do not
work around it. Adopt `VW_REQUEST_DTL` exactly as it is — whatever the view returns is what the
รายการ prints. If the `WHERE STATUS != 'D'` behaviour turns out to be wrong, that is the view owners'
fix to make, not ours.

Rationale worth keeping: the whole point of moving to the view is that our reports show the **same**
rows as the rest of the system. Compensating for a quirk on our side would re-create exactly the
divergence we're removing.

REQ-025 now has no open questions — points 1 and 2 stand as written.

## [later] Porter (PM) — REQ-025 DATA REQUEST: mostly already answered; blocking the duplicate half

@Sober: **the column list is already in your hands — see `requirements/REQ-025-...md`, section
"Source — view definition as supplied by the human".** The human pasted the view's full `SELECT`,
so every column name, alias and join is documented there. Do not send that half up to the human again.

Mapping you need, straight off that definition:
- `ID`, `REQUEST_ID`, `ITEM_NO` (already `NVL(...,0)`), `PRODUCT_CODE`, `PRODUCT_NAME`, `QUANTITY`,
  `QUANTITY2`, `QUANTITY_UNIT_ID`, `QUANTITY_UNIT_ID2`, `STATUS`
- resolved names: `QUANTITY_UNIT_NAME1`, `QUANTITY_UNIT_NAME2`, `USAGE_TYPE_NAME`, `STATUS_NAME`
- **print name = `PRODUCT_NAME_DISPLAY_LICENSE`** (as already relayed)
- order = `REQUEST_ID ASC, ITEM_NO ASC`

The **one genuinely open question** — does the view object actually exist in the DB, or did the human
hand us the SQL of a view that lives only in their other codebase — is fair, and I am taking exactly
that to them. Nothing else.

Your `T_T_REQUEST_DTL`-shape assumption is right: the view's base table **is** `T_T_REQUEST_DTL`
(`FROM T_T_REQUEST_DTL DTL`), enriched by LEFT JOINs to product/unit/common-code masters.

### Your a9 finding is the valuable part here
> *"a9's items may live in a destroy/sale detail table, not `T_T_REQUEST_DTL` → current a9 annex could
> be wrong/empty"*

Flagging that to the human as a possible **live defect on อ.9**, not just a REQ-025 side effect. Note
the implication cuts the other way too: since the view is built on `T_T_REQUEST_DTL`, if อ.9's rows
genuinely live elsewhere then the view **won't** fix a9 — it would only confirm the annex is empty.
Don't promise the fix in the SPEC; state it as the open possibility it is.

Board: REQ-025 stays IN_SPEC, blocked on the existence check only.

## [later] Porter (PM) — REQ-025 UNBLOCKED: the view exists, and it answers the a9 doubt too

Human ran it:
```
SELECT COUNT(*) FROM VW_REQUEST_DTL WHERE REQUEST_ID = 18847;  →  1
```
Human's note: they picked 18847 over 38362 because **38362 has no item rows at all** ("เขาไม่ได้ทำรายการมา")
— so 38362 is not a usable annex sample, and an empty annex on that request is correct behaviour, not a bug.

@Sober: **DATA REQUEST answered — TASK-022 is unblocked.**

1. **`VW_REQUEST_DTL` exists in this DB.** The query returned a count, not an ORA error. Map to it.
2. **Columns** — off the definition already filed in REQ-025; no further ask to the human.
3. **Your a9 doubt is resolved, and the good way round:** 18847 is an **อ.9** request and it *does*
   return a row from the view. So อ.9 items **do** live in `T_T_REQUEST_DTL` (the view's base table) —
   they are not hiding in a destroy/sale detail table. The view will serve อ.9 correctly.
   Drop the "a9 annex may be wrong/empty" caveat from SPEC-026; it was a reasonable hypothesis and the
   data has now falsified it.

Sample guidance for the build/QA legs: use **18847** for an อ.9 with a real item row. Do **not** use
38362 for annex work — it legitimately has none.

Nothing else is open on REQ-025. It stays ahead of TASK-019 per my earlier sequencing note unless you
tell me the two don't collide.

## [later] Sober (SA)
- **REQ-025 UNBLOCKED** (Porter confirmed the view exists; columns in the filed REQ-025 view definition).
  Finalised **SPEC-026** + **TASK-022** — dropped the blocked/DATA-REQUEST + the NULL-status flag
  (human-closed: adopt the view as-is) + the a9-wrong/empty caveat (falsified — 18847 is อ.9 and returns a
  view row, so a9 items are in T_T_REQUEST_DTL). Also earlier entries this session were mistakenly appended
  to log/2026-08-05.md — switching to today's log here.
- **TASK-022 (ready for Jason):** one `@Immutable RequestDtlViewEntity` (VW_REQUEST_DTL) + repo
  (`findByRequestIdOrderByItemNoAsc` + `countByRequestId`); swap a6/a9/a14 `buildComponents` + item-4 count
  to it. Per REQ-025: **name = `PRODUCT_NAME_DISPLAY_LICENSE`** (the view's assembled licence string),
  **unit = `QUANTITY_UNIT_NAME1`** (drop the local `unitRepository`/`getUnitNameAbbr` lookup —
  `formatQuantity` takes the unit-name string now). ComponentItem shape unchanged; only the รายการ changes.
- **Flagged (not hidden):** the unit display shifts **abbr → full name** on a6/a9 (the view uses
  `T_M_UNIT.UNIT_NAME`, we printed the abbr) — intended per REQ-025 pt 3 ("same as the rest of the system");
  QA confirms it reads acceptably on 18847. Nothing outside the item list moves.
- **Answered Porter's sequencing question:** TASK-022 (annex/`buildComponents`) and TASK-019 (a14 item-12/
  `buildTransportItem12`) touch **different methods → they don't collide**; either order works, and TASK-022
  is unblocked so it can land first cleanly as you prefer.
- @Jason: **TASK-022 is yours** (clean swap; keep everything except รายการ identical; sample 18847, not 38362).
  Ping me for review. TASK-019 (a14 item-12) still open at your pace.
- @Porter: REQ-025 spec'd + unblocked; no open questions. Board: REQ-025 → SPEC_DONE.

## [later] Porter (PM) — `ReqSaleInt` IS ALREADY SEEDED. Seed request CANCELLED.

I asked the human to check the checklist master and I was wrong to have requested a seed: **the data
team already created `ReqSaleInt`** (15 rows, IDs 145–159). Human's read: they likely seeded it about
three days ago, after our survey — so this is stale-fact, not a mis-read. Recording the lesson:
**re-verify master/DB facts immediately before asking the data team for anything.** Our SPEC-023
Delta 4 statement "no export-sale group exists" is now false and should be struck.

Also visible in the same query: `ReqSaleDom` (13 rows) exists too, despite the dictionary marking
อ.15 as PAMF-retired. Not acting on it — noting it so nobody re-derives "อ.15 has no master".

### The real `ReqSaleInt` master (GROUP_CODE `ReqSaleInt`, ordered by SEQUENCE)

| SEQ | CODE_NAME | DOCUMENT_ID | DOC_TYPE | OPTIONAL | IS_ACTIVE |
|----|---|---|---|---|---|
| 1 | สำเนาหนังสือรับรองการจดทะเบียน ฯ กรมพัฒนาธุรกิจการค้า (ไม่เกิน 6 เดือน) | 1 | 1 | 0 | 1 |
| 2 | หนังสือมอบอำนาจ | 6 | 2 | **1** | 1 |
| 3 | สำเนาใบอนุญาตประกอบกิจการโรงงาน (ร.ง.4) (ลำดับ 9) | 8 | 3 | 0 | 1 |
| 4 | สำเนาใบอนุญาตประกอบกิจการโรงงานทำอาวุธ แบบ อ.2 | 122 | 40 | 0 | **0** |
| 5 | สำเนาสลักหลัง (แบบ อ.2) | 123 | 40 | 0 | **0** |
| 6 | สำเนาบัตรประจำตัวผู้เสียภาษีของนิติบุคคล | 4 | 1 | 0 | 1 |
| 7 | ใบทะเบียนภาษีมูลค่าเพิ่ม (ภ.พ. 20) | 100 | 1 | 0 | 1 |
| 8 | สำเนาเอกสารแสดงคุณลักษณะและคุณสมบัติของอาวุธ | NULL | 22 | 0 | 1 |
| 9 | แผนที่แสดงสถานที่ตั้งโรงงาน รายการ และแบบของสถานที่เก็บฯ | NULL | 21 | 0 | 1 |
| 10 | แผนผังโรงงาน | 121 | 3 | 0 | 1 |
| 11 | แผนการขนย้ายตาม มาตรการฯ พ.ศ.2556 | NULL | 22 | 0 | 1 |
| 12 | **สำเนาหนังสือรับรองการจดทะเบียนเป็นนิติบุคคลของบริษัทผู้ซื้อ (ไม่เกิน 6 ปี)** | NULL | 22 | 0 | 1 |
| 13 | **หลักฐานการขอซื้ออาวุธที่ได้รับความเห็นชอบแล้วจากเจ้าหน้าที่รัฐบาลของประเทศผู้ซื้อ** | NULL | 22 | 0 | 1 |
| 14 | **หนังสือรับรองผู้ใช้ปลายทาง (END - USER CERTIFICATE)** | NULL | 22 | 0 | 1 |
| 15 | ภาพถ่ายสนามยิงปืนและช่องยิงปืน ฯ | NULL | 22 | **1** | 1 |

@Sober: read this closely, it changes TASK-019's shape.

1. **The 3 "NEW tick-only" item-12 docs you pinned are ALREADY IN THE MASTER** — SEQ 12, 13, 14, and
   their CODE_NAMEs match your Delta-3 lines 1, 2 and 4 essentially verbatim. Your ภาพถ่ายสนามยิงปืน
   line is SEQ 15. So there is **nothing to seed and nothing to invent** — bind them like any other
   master-driven tick.
2. **SEQ 4 and 5 are `IS_ACTIVE = 0`** — excluded, per our standing IS_ACTIVE=1 filter. That leaves
   **13 active rows**, and the form has 13 evidence items. Check whether that is a genuine 1:1 or a
   coincidence before you rely on it — do **not** assume SEQ maps to the printed item number, because
   our structure is locked in the report and the master only supplies ticks.
3. **`DOCUMENT_ID` is NULL on 7 of the active rows** (SEQ 8, 9, 11, 12, 13, 14, 15). So ticks for
   those **cannot** bind by DOCUMENT_ID — they must bind through `T_T_REQUEST_DOC.REQUEST_CHECKLIST_ID`.
   Confirm the a6/a9 tick path already works that way; if it falls back to DOCUMENT_ID anywhere, those
   seven can never tick and that is a defect to raise, not to work around.
4. **New column on this table: `CHECKLIST_CODE`** (e.g. `ReqSaleInt12204` = group + document + seq).
   Not in our model. Flag if it is the intended join key rather than the ID.
5. `OPTIONAL = 1` on SEQ 2 (มอบอำนาจ) and SEQ 15 (ภาพถ่ายสนามยิงปืน) only.

**Still outstanding, and it is the only thing left on the data side:** whether
`T_T_REQUEST_DOC.REQUEST_CHECKLIST_ID` is actually **backfilled** on real อ.14 requests. A seeded
master with no backfill still ticks nothing. I'm taking that to the human.

`project-docs/A14-checklist-seed-spec-for-data-team.md` is now obsolete — do not send it.

## [later] Porter (PM) — อ.14 ticks are UNVERIFIABLE with real data (no attachments exist at all)

Chased the tick question to the end so nobody asks the data team for the wrong thing:

1. `T_T_REQUEST_DOC` for 27300/34380/35966/36711/36741 → **TOTAL 0, LINKED 0** (no docs at all).
2. Every อ.14 request joined to `T_T_REQUEST_DOC` → **empty result set**.

⇒ **No อ.14 request in this DB has a single attached document.** So there is nothing to backfill and
nothing to ask the data team for. `REQUEST_CHECKLIST_ID` backfill is a non-question until somebody
actually uploads documents on an อ.14 request.

**This is the อ.9-destroy situation again, and we handle it the same way (REQ-020 precedent):** finish
อ.14 by construction against the official PDF + the real `ReqSaleInt` master, render blank-and-unticked
for real requests, and treat that as the correct steady state rather than a defect.

@Sober: consequences for TASK-019 —
- **Do not** wait on data. The master is real and complete; bind against it and ship.
- **Blank + unticked on every real อ.14 today is the EXPECTED result**, not a failure. Say so
  explicitly in the SPEC's acceptance notes so QA doesn't file it as a bug.
- Page-1 and the annex **can** still be verified on real data (27300 etc. have header rows, and
  REQ-025's view covers the รายการ) — so the real-DB leg is not worthless, it is just scoped to
  page-1 + annex + structure.
- The tick path itself is provable **without** อ.14 data: it is the same code a6/a9 use, and a6 has
  real ticked requests. If your DOCUMENT_ID-is-NULL check (7 of 13 rows) shows the tick binds only via
  `REQUEST_CHECKLIST_ID`, that is provable on a6 and inherited by a14.

Standing follow-up (not a blocker): re-run the tick check when real อ.14 usage begins.

## [later] Sober (SA)
- Reviewed **TASK-022** (รายการ from VW_REQUEST_DTL) — **DONE**, verified code + build:
  `RequestDtlViewEntity` (@Immutable/@Id/VW_REQUEST_DTL) maps PRODUCT_NAME_DISPLAY_LICENSE + QUANTITY_UNIT_NAME1;
  **all 3 builders (a6/a9/a14) consistently** read the view for buildComponents + item-4 count; name =
  display-license string, unit = resolved name; `formatQuantity` takes the unit-name string; `unitRepository`
  removed. View adopted as-is (NULL-status not compensated — human-closed). test-compile + A6/A9/A14
  PreviewTests green (3/0); app boots; only the item list changed.
- **REQ-025 code COMPLETE.** Row content (name/unit/order/count match the view) is QA's DB leg.
- @Porter: annex standardisation done our side. QA on **18847** (อ.9 with a real item row; NOT 38362): a6/a9/a14
  รายการ rows+order+count match VW_REQUEST_DTL; item name = PRODUCT_NAME_DISPLAY_LICENSE; **a6/a9 unit now the
  full name (was abbr) — intended per REQ-025; confirm it reads acceptably**; nothing outside รายการ moved.
- Nothing waits on Sober now. Open (others' legs): TASK-019 (a14 item-12, Jason) + ReqSaleInt seed (Porter);
  QA close-proofs across the SPEC_DONE items (a6, a9-destroy/transport, a14, REQ-023/024/025).

## [later] Porter (PM) — REQ-025 accepted in code; two corrections + TASK-019 is the only build left

@Sober: **stale item in your close-out** — you still list "ReqSaleInt seed (Porter)" as open. It is
**CANCELLED**: the data team had already seeded `ReqSaleInt` (15 rows, SEQ 12/13/14 = your 3 "NEW"
item-12 docs). See my two entries above; the hand-off doc is renamed `OBSOLETE-...`. Nothing on the
data side is outstanding for อ.14 — and per the entry above, no อ.14 request has any attachment at
all, so ticks are unverifiable **by design**, not blocked.

**The unit change is going to the human before I accept it.** `a6/a9` now print the full unit name
where they printed the abbreviation. อ.6 is **DELIVERED and in use on real paperwork**, so a visible
change to its printed output is the human's call, not a silent consequence of REQ-025. Good that you
flagged it rather than shipping it quietly. If they dislike it, the fix is to map back to the
abbreviated column — do not change the view.

**TASK-019 (อ.14 item-12) is now the only build work left in the queue.** After it lands, the whole
remaining set is QA close-proofs plus the two production gates (REQ-004 dev-profile, REQ-024 routing
proof). @Jason's pace; nothing from me blocks it.

## [later] Porter (PM) — REQ-025 ACCEPTED on real output. Unit change approved; display quirk = view owners'

Human rendered a6-38272 on the real DB and I reviewed the extracted text.

**Accepted:**
- **Unit change approved.** The full unit names print fine — กิโลกรัม / นัด / ชุด / ดอก — short,
  legible, no overflow. REQ-025's abbr→full change stands; no rollback.
- Annex renders 5 rows with code, name, quantity + unit; page-1, evidence, signatures all intact.
  Nothing outside the รายการ moved. **REQ-025 → DELIVERED on the a6 leg.**

**Display quirk found and deliberately NOT fixed:**
```
1  P-0001  วัตถุระเบิด วัตถุระเบิด EMULSION SLURRY TYPE  -
2  P-0003  วัตถุระเบิด วัตถุระเบิด SLURRIES
```
"วัตถุระเบิด" printed twice, and row 1 carries a trailing `-`. Likely cause: the view dedups
`PRODUCT_GROUP_NAME` against `PRODUCT_TYPE_NAME` with a plain `=`, and skips `'-'` with a plain `=` —
so trailing whitespace in the data defeats both guards.

Human's ruling: *"ปล่อยไปเลย เดี๋ยวบอกเจ้าของ view เอง"* — consistent with their earlier REQ-025
decision. **@Sober: do not trim, dedup, or post-process `PRODUCT_NAME_DISPLAY_LICENSE`.** Print it
verbatim. The human owns telling the view's owners. Compensating on our side would re-introduce
exactly the divergence REQ-025 removed.

## [later] Sober (SA)
- **REQ-025 DELIVERED** (a6 leg, human-approved on real 38272 output). Acknowledged the display quirk
  (duplicated "วัตถุระเบิด" + trailing "-") — **view-side, WON'T FIX by us** per the human; I will NOT
  trim/dedup/post-process `PRODUCT_NAME_DISPLAY_LICENSE` (compensating would re-create the divergence
  REQ-025 removed). No task raised. a9/a14 legs still to be eyeballed by QA (same code path, verified in review).
- **Board reconciled:** TASK-018/020/021/022 rows had drifted back to REVIEW (concurrent board edits) but
  all four are SA-reviewed DONE (verdicts in their task files; REQ-023/024/025 rows already reflect it) →
  set to DONE (SA-verified).
- @Porter (housekeeping, your call — I don't move REQ status): two stale rows —
  * **REQ-014** row still reads "REWORK — Sober→Jason: DEF-4 page-1 blank"; that DEF-4 work is long done
    (a9 DELIVERED via REQ-019/020) and the a9-resolver latent defect it tracked is fixed by REQ-024/TASK-021.
    Looks closeable/DELIVERED.
  * **TASK-008** row still "BLOCKED — Sober→QA (diagnostic)"; that diagnostic was overtaken by the full a9
    build long ago. Likely DONE/SUPERSEDED.
- Nothing waits on Sober now. Open (others' legs): TASK-019 (a14 item-12, Jason) + `ReqSaleInt` seed
  (Porter); QA close-proofs across the SPEC_DONE/DELIVERED items (a9-destroy/transport real-DB, a14 real
  render, REQ-023 item-7, REQ-024 routing, REQ-025 a9/a14 annex).

## [later] Sober (SA)
- Jason re-read the board for his next task = **TASK-019** (a14 item-12) — but the **task file was missing**
  (only a board row + SPEC-023 Delta 3 existed). Wrote **TASK-019** now so he builds it right.
- Key correction baked in: the a14 export item-12 is **NOT a copy of the อ.9 transport item-12**. Same helper
  *sources*, different *order/labels*: DROP ส.ค.4/บัตรนายกสมาคม/คณะกรรมการ2553/แผนกระสุน; ADD 3 tick-only NEW
  export docs (บริษัทผู้ซื้อจดทะเบียน / govt approval / **END-USER CERTIFICATE**); KEEP มอบอำนาจ/บัตรผู้รับมอบ/
  person2/ขอซื้อ/ภาพถ่ายสนามยิง/ป.3/ป.5/มท./ยุทธภัณฑ์ ((1)/(2)). Full verbatim 12-line list + per-line sources
  in the task (from the official A14 PDF pp.2-3).
- Reuses the shared domain repos from TASK-016 (BUYER/P3/P5/MOI/ARMS + EXAMPLE_SIGN); the 3 NEW ticks come
  from `T_T_REQUEST_DOC` under `ReqSaleInt` → blank/graceful until Porter's seed (do NOT block on it).
- @Jason: **TASK-019 file is up** — ready to build now (DB-free provable in A14PreviewTest; sample 27300/…).
  Ping me for review.
- @Porter: TASK-019 was un-filed (my miss on REQ-022's split) — now written; no scope change, just the
  missing file. Board TASK-019 title refined ("NOT a copy of a9 transport").

## [later] Sober (SA)
- Reviewed **TASK-019** (a14 item-12 export) — **DONE**, verified independently (read buildExportItem12 +
  rendered a14/a9 previews): header "เอกสารขอผู้ซื้อ"; #1 นิติบุคคลผู้ซื้อ + #4 **END-USER** present; ป.3/
  ยุทธภัณฑ์ + (1)/(2); **dropped items (ส.ค.4/คณะกรรมการ/แผนกระสุน/นายกสมาคม) ABSENT** = export set not the
  a9-transport copy; 0 "null"; a9 item-12 intact; A14/A9/A6 PreviewTests green.
- Answered Jason: **Q1** provisional mid.apply(12..15) fine (master unseeded→blank); I'll supply the
  authoritative ReqSaleInt SEQ→line map with Porter's seed hand-off and align indices then. **Q2** keep
  "เอกสารขอผู้ซื้อ" verbatim (match-the-form rule) — flagged to Porter as a probable official-form typo.
- **a14 (REQ-022) is now CODE-COMPLETE + preview-verified** end-to-end (foundation + export item-12);
  a9/a6 untouched.
- @Porter — REQ-022 done our side; two items on your leg:
  1. **Seed `ReqSaleInt`** — I'll hand you the SEQ→line map (evidence 1-11 + item-12 ticks #1/#2/#4/#8);
     the 3 NEW-doc ticks stay blank-graceful until then.
  2. **Form-typo ruling:** the official A14 PDF header is **"เอกสารขอผู้ซื้อ"** (อ.9 uses "ของผู้ซื้อ"). We
     print it verbatim per match-the-form; ask the human whether to keep as-is or "correct" to "ของ" — one-line change.
  QA on real a14: 27300/34380/35966/36711/36741 (item-12 populated; confirm item-5=BUYER_NAME).
- Nothing waits on Sober. Open: Porter (ReqSaleInt seed + typo ruling) + QA close-proofs across the stack.

## [later] Porter (PM) — TASK-019 questions answered: Q2 is NOT a typo; Q1 is stale

@Sober, both of Jason's open questions are answerable without going to the human.

**Q2 — "เอกสารขอผู้ซื้อ" vs อ.9's "เอกสารของผู้ซื้อ": NOT a typo. Keep it verbatim.**
I checked the official form myself (`project-docs/A14-A16-form-official.pdf`, page 2):
```
12. เอกสารขอผู้ซื้อ
```
The อ.14 form genuinely reads "ขอผู้ซื้อ" where อ.9 reads "ของผู้ซื้อ". This is exactly the DEF-6
situation — I once raised a page-heading difference as a bug and the human's ruling was **follow the
official PDF, not our sense of what reads correctly**. The form is the spec. Do not "correct" it.

**Q1 — the `ReqSaleInt` seed is NOT pending. It already exists.**
Jason's note "provisional until ReqSaleInt seed" is stale; see my earlier entry with the full master
(15 rows, IDs 145–159). Bind the 3 NEW tick-only docs to their **real** rows now:

| item-12 line | master SEQUENCE | CHECKLIST_CODE |
|---|---|---|
| #1 สำเนาหนังสือรับรองการจดทะเบียนเป็นนิติบุคคลของบริษัทผู้ซื้อ | **12** | `ReqSaleInt00012` |
| #2 หลักฐานการขอซื้ออาวุธที่ได้รับความเห็นชอบแล้วจากเจ้าหน้าที่รัฐบาลของประเทศผู้ซื้อ | **13** | `ReqSaleInt00013` |
| #4 หนังสือรับรองผู้ใช้ปลายทาง (END-USER CERTIFICATE) | **14** | `ReqSaleInt00014` |
| ภาพถ่ายสนามยิงปืนและช่องยิงปืน ฯ | **15** | `ReqSaleInt00015` |

All four have `DOCUMENT_ID = NULL`, so they can only bind via `T_T_REQUEST_DOC.REQUEST_CHECKLIST_ID` —
this is the check I asked you for earlier and it is now load-bearing, not theoretical. If the a6/a9
tick path falls back to DOCUMENT_ID anywhere, these four can never tick; raise it rather than
work around it.

Reminder so nobody misreads the result: **no อ.14 request has any attachment at all**, so every tick
renders blank today regardless. That is expected, not a failure of this binding.

## [later] Porter (PM) — DEF-11: `T_T_REQUEST_SALE_INT` has NO `STATUS` column → real /download 500s for อ.9

Human ran the real download test. **อ.6 works. Both อ.9 variants 500.** Root cause is unambiguous
from their log — this is not a routing problem, it is a bad entity mapping:

```
ORA-00904: "RSIE1_0"."STATUS": invalid identifier
select rsie1_0.id, rsie1_0.buyer_name, rsie1_0.form_id, rsie1_0.importer_name,
       rsie1_0.request_id, rsie1_0.status
from t_t_request_sale_int rsie1_0
where rsie1_0.request_id=? and (rsie1_0.status is null or rsie1_0.status<>'D')
order by rsie1_0.id
  at RequestTypeResolverService.resolveFromSaleInt(RequestTypeResolverService.java:135)
  at RequestTypeResolverService.resolveChecklistRequestType(:49 / :38)
  at DocumentController.downloadChecklistPDF(DocumentController.java:106)
```

**`RequestSaleIntEntity` maps a `status` column that does not exist on the table**, and
`findActive` filters on it. Confirmed against real data: the human's earlier queries on this table
(`REQUEST_ID, FORM_ID` and `BUYER_NAME, IMPORTER_NAME`) both ran fine, and a `SELECT *` on the
sibling tables never showed a STATUS column here.

### Why this is worse than it looks — and why อ.6 masked it
`resolveFromSaleInt` sits **before** `resolveFromMove` in the chain. So it throws for **every request
that is not อ.6** — อ.9 both variants, and อ.14 too. อ.6 only passes because `resolveFromSpecial`
matches first and returns before the broken leg is reached. **The one form we tested during the build
is the only one that could never hit the bug.**

Net effect: TASK-018 introduced a **new** 500 on the exact path TASK-021 had just repaired.

@Sober — three things, in this order:

1. **Fix (small):** drop `status` from `RequestSaleIntEntity` and stop filtering on it in
   `findActive`. Verify the real column list first (`DESCRIBE T_T_REQUEST_SALE_INT` via Porter if you
   need it) rather than assuming the a6/a9 shape again — that assumption is what caused this.
2. **Sweep the same class of bug:** every entity added in TASK-018/TASK-019/TASK-022
   (`RequestSaleIntEntity`, `RequestDtlSaleIntEntity`, `RequestDtlViewEntity`) was written by
   mirroring a different table. Check each mapped column actually exists. `RequestDtlViewEntity` is
   the one I'd check hardest — `VW_REQUEST_DTL`'s real columns are filed verbatim in REQ-025.
3. **Process gap worth naming:** `test-compile` + PreviewTests are all DB-free, so a wrong column name
   is invisible until a real request hits it. Our "SA-verified" on TASK-018 could not have caught
   this. I am not asking for a test-infrastructure project — but say in the SPEC how a new entity gets
   its columns confirmed **before** review, so this class of defect stops at the desk instead of at
   the human's screen.

This is now the **top** item: it blocks the อ.9 production proof, which is the thing the human is
actively trying to close. TASK-019 and everything else waits.

## [later] Sober (SA)
- **DEF-11 (🔴 TOP) — spec'd + tasked (SPEC-027 / TASK-023).** Confirmed against the dictionary:
  `T_T_REQUEST_SALE_INT` has **no `STATUS`** (only `SALE_INT_STATUS`/`CHECKING_STATUS`), but
  `RequestSaleIntEntity` maps `STATUS` + `findActive` filters it, and `resolveFromSaleInt` runs before
  `resolveFromMove` → **ORA-00904 → real /download 500s for อ.9 (both) + อ.14**; อ.6 escapes only because
  resolveFromSpecial returns first. Introduced by TASK-018.
- **I own the review miss:** my "SA-verified" on TASK-018 was compile + boot + DB-free PreviewTests, which
  cannot execute JPQL, so an invalid column name was invisible. Stated plainly in the SPEC.
- **Fix (TASK-023):** drop `status` from `RequestSaleIntEntity`; repo → plain `findByRequestId` (routing needs
  only REQUEST_ID + FORM_ID, no status filter); resolver updated.
- **Sweep:** `RequestDtlSaleIntEntity` is now dead (annex→view in TASK-022) → delete or column-verify;
  `RequestDtlViewEntity` re-confirm (its cols all exist in the filed VW def — on the live annex path);
  spot-check MOVE.AUTHORITY_NAME + the 5 permit entities (their tables DO have STATUS).
- **Process guard adopted (Porter's ask):** every task touching an @Entity/@Column must cite the dict sheet
  (or filed view DDL / Porter DESCRIBE) confirming each mapped column — not "mirrored from a6/a9". On review I
  now diff mapped columns against the dictionary, since compile+preview can't. This stops the class at the desk.
- @Jason: **TASK-023 is TOP** — small fix + sweep; unblocks the real อ.9/อ.14 /download proof (and the REQ-024
  proof it was blocking). Ping me for review.
- @Porter: DEF-11 owned + tasked; QA proof after Jason+my review = real อ.9 & อ.14 via /download → PDF, no 500.

## [later] Sober (SA)
- Reviewed **TASK-023 (DEF-11)** — **DONE**, verified per the new SPEC-027 guard (**diffed mapped columns
  vs the dictionary**, not just compile/preview):
  * `RequestSaleIntEntity` = ID/REQUEST_ID/FORM_ID/BUYER_NAME/IMPORTER_NAME — all 5 dict-confirmed; `STATUS`
    removed and dict confirms it's absent (real = `SALE_INT_STATUS`). ✅
  * repo→`findByRequestId`; resolver + a14 both use it → ORA-00904 path gone. ✅
  * `RequestDtlSaleInt*` deleted (0 refs); `RequestDtlViewEntity` cols all in the filed VW SELECT. ✅
  * test-compile + A14/A9/A6 PreviewTests 3/0; boots. ✅
- **DEF-11 fixed in code.** Unblocks the real อ.9/อ.14 `/download` proof (and the REQ-024 proof it blocked)
  — QA's DB leg now.
- @Porter: DEF-11 closed our side. QA (real DB): a real **อ.9** (18847/37956) + **อ.14** (27300/34380) via
  `/api/v1/download/checklist/{encId}` → PDF, **no 500/ORA-00904**; อ.6 still routes. That single run also
  proves REQ-024. New standard in effect: new entities cite the dict per column; I diff on review.
- Nothing waits on Sober. Ball → Porter (route DEF-11/REQ-024 QA proof + `ReqSaleInt` seed + the "ขอ/ของ"
  typo ruling).

## [later] Porter (PM) — DEF-11: real column list for `T_T_REQUEST_SALE_INT` (no more guessing)

Human ran `ALL_TAB_COLUMNS`. **Confirmed: there is no `STATUS` column.** (The list came back doubled —
two schema owners hold the table — harmless, ignore the duplication.)

Columns that matter to us:
`ID`, `REQUEST_ID`, `FORM_ID`, `BUYER_NAME`, `IMPORTER_NAME`, `IMPORTER_ADDRESS_NO/STREET/CITY/STATE/
POSTCODE/PHONE_NO/FAX_NO/COUNTRY_CODE`, `EXPORT_TRANSPORT_TYPE_CODE`, `EXPORT_DETAIL`,
`REF_LICENSE_NO/ISSUE_DATE/EXPIRY_DATE/TYPE`, `PLANT_*`, `SALE_INT_STATUS`, `CHECKING_STATUS`,
`SUBMIT_*`, `UPDATE_*`.

@Sober — the fix, precisely:

1. **Delete the `status` field from `RequestSaleIntEntity` and drop the filter from `findActive`.**
   The resolver only needs row-presence + `FORM_ID`; that is all `resolveFromSaleInt` uses.
2. **Do NOT substitute `SALE_INT_STATUS`.** It is the family's *workflow* status (the analogue of
   `MOVE_STATUS`/`REQUEST_STATUS`), not the soft-delete flag. Our `<> 'D'` convention comes from
   `T_T_REQUEST_PER.STATUS` / `T_T_REQUEST_DTL.STATUS`, which are different columns with different
   meaning. Swapping one in would compile, run, and silently filter the wrong thing — worse than the
   crash we have now.
3. Note there is **no `REF_LICENSE_ID`** here either, consistent with REQ-005: licence linkage is by
   `REQUEST_ID`.
4. Naming pattern for the sweep: each family names its own status column —
   `T_T_REQUEST_SPECIAL.REQUEST_STATUS`, `T_T_REQUEST_MOVE.MOVE_STATUS`,
   `T_T_REQUEST_SALE_INT.SALE_INT_STATUS`. Assume nothing is called plain `STATUS` on a request-header
   table until you have seen it.

I can pull `ALL_TAB_COLUMNS` for any other table on request — ask before assuming, it costs one query.

## [later] Sober (SA)
- Board drift fix: **TASK-023 row → DONE** (was stale at REVIEW; verdict already in the task file, DEF-11 row
  shows FIXED). Also reconciled **TASK-008 → SUPERSEDED** (the a9 DB build was long delivered via
  TASK-012/015-017; its "QA diagnostic" leg is overtaken) — a task I own; REQ-014's REQ-status stays Porter's call.
- Folded **Porter's status-column naming note into SPEC-027** (the process guard): request-**header** tables
  name status per family (`REQUEST_STATUS`/`MOVE_STATUS`/`SALE_INT_STATUS`); plain `STATUS`/`<>'D'` is a
  child-table convention (PER/DTL/permit/BUYER/EXAMPLE_SIGN). Assume no header table has plain `STATUS` until
  seen; **routing legs use no status filter (row-presence + FORM_ID)**. Directly targets the coming อ.4–8
  header `T_T_REQUEST_IMPORT` so DEF-11 doesn't recur. (Porter can pull ALL_TAB_COLUMNS on request.)
- Nothing waits on Sober. Ball → Porter: QA the DEF-11/REQ-024 real-/download proof (อ.9+อ.14 → PDF, no 500);
  `ReqSaleInt` seed; "ขอ/ของ" typo ruling; and the stale REQ-014 status.

## [later] Porter (PM) — DEF-11 CONFIRMED FIXED on real data + NEW REQ-026 (split the two อ.9 variants)

**Human re-ran the real `/download` test: it works now.** อ.9, อ.14 and อ.6 all return their PDF, no
500. That closes DEF-11 and, with it, **REQ-024's proof leg** — the อ.9 production routing blocker is
now verified end-to-end on real data, not just in code.

### NEW — REQ-026 (READY_FOR_SA, MEDIUM)
> *"a9 destroy กับ a9 move มันใช้ใบเดียวกันใช่มั้ย อยากให้แยกกัน เพราะในอนาคต เผื่อมีการแก้ไข
> มันแก้ยากกว่าหากรวมกันไว้"*

Split the two อ.9 variants into two independent reports, in the a14 single-variant shape. Full
requirement filed as REQ-026.

@Sober — the thing I care about most in your SPEC:

**Draw the shared/split line explicitly, and defend it.** The goal is to separate what differs *by
form*, not to fork the report. If persons / law refs / signatures / annex / item-7 / the tick rule get
duplicated in the process, we will have made maintenance **worse**, not better — and we have direct
evidence of that cost: REQ-023's item-7 rule had to be fixed report-by-report precisely because each
one re-derived it. Splitting the wrong seam recreates that.

- **Split:** headings, item-2 text, item-5 label+source, the item-12 sub-item set/order, the master
  GROUP_CODE (`ReqMoveDestroyer` vs `ReqMove`), the templates.
- **Keep shared:** persons 3/4, law refs, signature block, annex (`VW_REQUEST_DTL`), item-7
  `PERIOD_TEXT`, tick rule, blank-when-null.

Two more constraints:
1. **Output must not change.** This is a restructure. Both PDFs render exactly as today; prove it on
   real data (transport is delivered and verified — regression is the real hazard here, not the split
   itself).
2. **Routing stays in `resolveFromMove`** — it resolves to one of two reports by `MOVE_REQUEST_TYPE`
   rather than handing a variant flag downstream. Do not touch the อ.6/อ.14 legs.

If the survey shows the split is materially bigger than it looks — e.g. the template genuinely can't
be cleanly cut — tell me the size before Jason starts and I'll take it back to the human rather than
let it run.

## [later] Sober (SA)
- Noted DEF-11 + REQ-024 **confirmed on real data** (human re-ran /download: อ.9/อ.14/อ.6 → PDF, no 500). ✅
- Picked up **REQ-026** (split อ.9 destroy/transport) — surveyed + wrote **SPEC-028** + **TASK-024**.
- **Size verdict for @Porter (you asked before Jason starts): NOT materially bigger — and one item on your
  split list should NOT be split.** Survey: `grep กำจัด|ทำลาย|สถานที่ทำการ` over the a9 jrxml = **0 hits** →
  the **templates are already variant-agnostic** (all variant diffs are data-driven: title/item5Label/item-12
  built in the builder). ⇒ **templates STAY SHARED** (one request-a9 set for both); forking them would
  duplicate ~5 jrxml for zero gain. The whole split surface is the builder's `boolean destroy` branches
  (L84-88/111/205-224) + wiring. Clean MEDIUM.
- **Defended the shared/split line** (your core ask): SPLIT = documentTitle/item5 label+value/item-12
  set+order/GROUP_CODE. SHARED (one place, no dup) = persons 3/4, lawrefs, signatures, annex(VW), **item-7
  PERIOD_TEXT**, tick rule, blank-null, evidence 1-11, person2, permit/BUYER helpers, plumbing, templates —
  directly avoiding the REQ-023 "each report re-derived item-7" trap.
- **Architecture:** abstract `A9…BuilderBase` (shared + repos) + variant hooks; two thin builders
  `A9Destroy`/`A9Transport` (**no destroy flag** — variant = the class); `A9D`/`A9T` ReportDefinition codes
  sharing the same templates; `resolveFromMove` → A9D/A9T by MOVE_REQUEST_TYPE. Constraint #2 (routing in
  resolveFromMove, no downstream flag) honoured.
- **DoD = byte-identical output** (regression is the hazard, not the split): diff destroy+transport previews
  pre/post; real transport (delivered) must match.
- @Jason: **TASK-024 is yours** — refactor, prove output unchanged (capture previews before you start).
- @Porter: REQ-026 spec'd, size = clean MEDIUM, no need to take it back to the human. Board: REQ-026 → SPEC_DONE.

## [later] Sober (SA)
- TASK-024 (a9 split) is Jason's active work — **IN_PROGRESS, plan locked + 4 preview md5 baselines pinned,
  no code changed yet** (he paused to do the behaviour-preserving refactor of the DELIVERED a9 builder in one
  careful pass; resumes on his nudge). His plan (extract-superclass + template-method, move code verbatim,
  A9D/A9T codes, resolveFromMove→type) matches SPEC-028 — approved.
- **Owned a gap in my spec:** SPEC-028/TASK-024 omitted the **history path**. Jason flagged that
  `A9CheckListHistoryReportBuilder` delegates to the single builder (confirmed). Added an explicit clause to
  TASK-024: repoint history to pick destroy/transport by the **same MOVE_REQUEST_TYPE rule**, history output
  **also byte-identical**, wire the history case for A9D/A9T. Blessed his approach.
- Nothing else waits on Sober — TASK-024 comes back at REVIEW (I'll diff the 4 preview md5s pre/post +
  confirm no shared-logic duplication + SPEC-027 column citations). Ball → Jason (execute TASK-024).

## [later] Sober (SA)
- Reviewed **TASK-024 (a9 split)** — **DONE**. Verified structure + resolver + content-equivalence:
  shared abstract base + 2 thin builders (no destroy flag in prod), old single+history builders deleted,
  `resolveFromMove`→A9D/A9T, **history variant resolves via the same rule** (form→requestId→resolveFromMove),
  content-equivalent render (destroy 4pp / transport 5pp / a6/a14 unchanged / no null), tests green + boots.
- **Approved Jason's two deviations** — both cut duplication (= REQ-026's whole point; my task steps were
  over-specified): single `exportPdfA9` (shared templates+record, no per-variant delegate) + deleted the 1-line
  history builder (DocumentController calls the variant builder directly). Good calls.
- **Adopted his process fix:** `.jasper` recompile is non-deterministic → md5 is a BROKEN oracle for
  byte-identical (a6/a14 md5s drifted with zero changes; my independent md5s confirm). Byte-identical checks
  now use **PyMuPDF text-equivalence + page count + no-null**, not `.jasper` md5. Standing convention.
- @Porter: REQ-026 code-complete our side. QA leg (rule #4): Tanya re-renders real **transport (38336/37956)**
  + a **destroy** sample via `/download` + `/a9/db` → must match the delivered output, and both route (A9D/A9T);
  อ.6/อ.14 unaffected. This closes the restructure.
- Nothing waits on Sober. Ball → Porter (QA the a9-split real render + still-open: ReqSaleInt seed, "ขอ/ของ"
  typo ruling, REQ-014 stale-status close).

## [later] Porter (PM) — REQ-026 accepted our side; closing REQ-014; two stale items returned to Sober AGAIN

TASK-024 reviewed and closed by Sober. Recording what I think is genuinely good here, because it is
the opposite of the failure mode that produced DEF-11:

- **The `.jasper` md5 finding is the valuable part.** Jasper recompilation is non-deterministic, so
  a6/a14 md5s drifted with zero source changes — md5 was a **broken oracle** and would have produced
  false "you changed it" alarms on every future refactor. Replacing it with PyMuPDF
  text-equivalence + page count + no-null is the right call. **Adopted as the standing convention for
  byte-identical claims.**
- Approving Jason's two deviations (single `exportPdfA9`, deleting the 1-line history delegate) was
  correct — REQ-026's whole point was *less* duplication, and over-specified task steps shouldn't
  override that. Good that the SA said so plainly rather than enforcing his own step list.

### REQ-014 — CLOSED (stale status, no work left)
REQ-014 has sat at `REWORK` since DEF-4, but every part of it has since been delivered elsewhere:
the a9 DB build (TASK-008), page-1 fields (TASK-012/015), item-7 (REQ-023/TASK-020), the
download-mock defect and the resolver leg (REQ-024/TASK-021, now proven on real data), and the
variants (REQ-019, now split under REQ-026). Marking **DELIVERED — superseded**; no code remains.

### @Sober — the same two stale items came back in your close-out. Please stop carrying them forward.

1. **`ReqSaleInt` seed: CANCELLED, not open.** The data team had already seeded it (15 rows,
   IDs 145–159; SEQ 12/13/14 are the three "NEW" item-12 docs). I logged this twice, filed the real
   master table, and renamed the hand-off doc `OBSOLETE-...`. There is nothing to seed.
2. **"ขอผู้ซื้อ" vs "ของผู้ซื้อ": ANSWERED — it is NOT a typo.** I checked the official form myself:
   `A14-A16-form-official.pdf` p.2 reads `12. เอกสารขอผู้ซื้อ` verbatim. Keep it. DEF-6 precedent —
   the form is the spec.

Both were answered in this log before your close-out was written. Please read to the end of the day's
log before composing the hand-back; re-raising settled items costs a round trip each time and risks
sending a resolved question up to the human.

### What I am actually taking
The real-data re-render (38336 / 37956 / a destroy sample) — that is a DB leg, so it is mine to route
to the human. Going to them now.

## [later] Sober (SA)
- **Ack Porter's feedback — my miss, corrected.** I re-raised the `ReqSaleInt` seed + the "ขอ/ของ" typo as
  open when Porter had already settled both earlier in the day's log. Cause: I composed hand-backs off the
  board + memory without reading to the **end of today's log**. Recorded it as a standing rule (memory) — I'll
  read the log to the end before every hand-back so I don't re-escalate resolved items. Won't recur.
- **Both now closed on my side:**
  * `ReqSaleInt` = **already seeded** (15 rows, SEQ12-15). Verified Jason's a14 item-12 tick indices
    `mid.apply(12/13/14/15)` map **exactly** to Porter's SEQ12(#1)/13(#2)/14(#4)/15(ภาพถ่าย) → ticks are
    correctly wired, NOT provisional. REQ-022 has no seed dependency left — only QA real render.
  * "เอกสารขอผู้ซื้อ" = **verbatim from the official form, NOT a typo** (Porter checked) → keep as-is. My
    verbatim choice stands; nothing to change.
- Board reconciled: **TASK-024 → DONE** (was stale at REVIEW). REQ-022 row updated (seed done, ticks confirmed).
- Nothing waits on Sober. Ball → Porter/QA: real a9 destroy+transport re-render (a9-split byte-identical) +
  real a14 render (27300/…). Only genuinely-open non-QA item across the board = REQ-014 stale status (Porter's call).

## [later] Porter (PM) — REQ-026 follow-up (templates not actually split) + NEW REQ-027 (อ.15)

### REQ-026 is only half done — I verified this myself
Human checked `src/main/resources/reports-045/request-a9` and found the templates **still shared**.
Confirmed: TASK-024 split the Java side but left one `request-a9/` folder serving both variants.

Evidence that separating them is the *house convention*, not a new fork pattern:
```
request-a6/    main/ + subreport/   (own component, evidence, evidenceSub, lawRef, signature)
request-a14/   main/ + subreport/   (own full set)
request-a9/    ONE set, shared by destroy + transport   ← the odd one out
```
Every other form already owns its complete template set. อ.9 is the only exception, purely because it
was built as one form and split later. So this resolves the shared/split tension I raised earlier:
**templates are per-form by convention; Java logic is what stays shared.**

@Sober: split the resources into `request-a9-destroy/` and `request-a9-transport/`, each with its own
`main/` + `subreport/`, mirroring a6/a14 exactly. **Resource files only — do not touch the shared Java
base class.** Regenerate `.jasper` into `src/` + `clean compile`; verify with the PyMuPDF
text-equivalence convention, not md5.

### NEW — REQ-027: อ.15, cloned from อ.9 **transport**
Human's ask, and they chose the base explicitly when I offered อ.14 vs อ.9 transport:
> *"ขอ a15 ด้วยนะ คือลอก transport มาเลย มันคือเหมือนๆ กันนั่นแหละ ต่างกันแค่ source data"*

Filed as REQ-027. Three things I want you to note:

1. **Sequence it after the template split.** Cloning อ.9 transport today means cloning a *shared*
   folder and redoing it afterwards.
2. **`ReqSaleDom` is ALREADY SEEDED (13 rows)** — from the same `T_S_REQUEST_CHECKLIST` query that
   settled `ReqSaleInt`. Do not raise a seed request. Verify before asking; that is now twice.
3. **Verify the live-ness assumption before designing.** The dictionary marks
   `T_T_REQUEST_SALE_DOM` "ระบบ PAMF ไม่ได้ใช้แล้ว" — which is exactly why we excluded อ.15 from
   REQ-022's scope. The seeded master argues the opposite. Check the table has real rows and its own
   `FORM_ID` before writing the resolver leg. If it is genuinely empty this is a build-by-construction
   job (REQ-020 shape), not a clone — tell me and I will re-scope with the human rather than let
   Jason discover it mid-build.

**Blocked on the official อ.15 form PDF** — requesting it from the human now. We do not invent
headings or evidence wording; both times we tried, we were wrong (DEF-6, and Jason's "ขอ/ของ" question).

## [later] Sober (SA)
- (Read to end of log first — new habit.) Two items:
- **REQ-026 template split — accepted Porter's reversal of my "shared templates" call, wrote TASK-025.**
  He's right: every other form owns its template set; a9 is the lone shared-folder exception, and keeping it
  shared undermines REQ-026's future-divergence goal (an edit to destroy's template would ride on transport).
  The duplication I was avoiding is the point — each report owns its template to diverge freely. Recorded the
  reversal in SPEC-028. **TASK-025** (@Jason): copy `request-a9/` → `request-a9-destroy/` + `-transport/`,
  repoint `ReportResourceService`, regen `.jasper`, **resource-only — Java base untouched**; verify by
  text-equivalence (not md5), content-equivalent to pre-split.
- **REQ-027 (อ.15) pre-work — stays blocked, but teed up the liveness question.** Dict:
  `T_T_REQUEST_SALE_DOM` has `FORM_ID` + `SALE_DOM_STATUS` (family-specific status — matches the SPEC-027
  header-table pattern, so the resolver leg uses row-presence + FORM_ID, no status filter) + `REQUEST_ID`,
  but **no `BUYER_NAME`** → a15's item-5 source differs from a9-transport's `MOVE.AUTHORITY_NAME`; TBD from the
  official PDF + dict. `ReqSaleDom` already seeded (13 rows — will NOT request). **Still blocked on:** the
  official อ.15 PDF (won't invent headings/wording — DEF-6 + "ขอ/ของ" lessons) **and** a DB COUNT of
  `T_T_REQUEST_SALE_DOM` (dict marks it PAMF-retired) — if empty, it's build-by-construction (REQ-020 shape),
  not a clone; I'll flag before Jason starts. Sequenced AFTER TASK-025.
- @Jason: **TASK-025 is yours** (small, resource-only). @Porter: template split tasked; REQ-027 pre-work done —
  need the อ.15 PDF + the SALE_DOM row-count from the human before I can SPEC it.

## [later] Porter (PM) — REQ-027 UNBLOCKED: อ.15 uses the SAME form as อ.9 transport (verified, not assumed)

Human supplied the official document:
`(1)Checklist (อ.9-อ.10) (อ.15-อ.17) ที่ ผอ.แก้ไข 16 ก.ค.69.pdf`

Two findings, both verified rather than eyeballed:

1. **One document covers อ.9-อ.10 AND อ.15-อ.17.** The title says so, and the body heading is
   `หลักฐานที่ใช้ประกอบคำขอรับหนังสืออนุญาตขายและขนย้ายอาวุธ` — the อ.9 transport heading we already
   implement. So อ.15 is not a similar form, it is **the same form**.
2. **It is textually identical to our filed `A9-form-TRANSPORT-official.pdf`.** I diffed every
   non-empty line of both PDFs: same page count, and **zero lines present in the revised document
   that are absent from ours**. Despite being labelled "ที่ ผอ.แก้ไข 16 ก.ค.69", it introduces no
   change to what we built.

I therefore did **not** file it as a new form document — it would be a byte-duplicate of one we
already hold. `A9-form-TRANSPORT-official.pdf` remains the reference for both อ.9 transport and อ.15.

@Sober: **REQ-027 is unblocked and simpler than specced.** The human's instinct was exactly right —
> *"มันคือเหมือนๆ กันนั่นแหละ ต่างกันแค่ source data"*

Confirmed: **form-truth is identical; only the data source differs.** So อ.15 is a genuine
data-source clone of อ.9 transport — same headings, same 13 evidence items, same item-12 set
(ส.ค.4 / นายกสมาคม / คณะกรรมการ พ.ศ.2553 / แผนการใช้กระสุน included), same signature block.

What actually differs, and all that should differ:
- header table `T_T_REQUEST_SALE_DOM` (**484 rows — live**; the dictionary's "PAMF ไม่ได้ใช้แล้ว" note
  is wrong, treat the dictionary as a starting point only)
- checklist master `ReqSaleDom` (13 rows, already seeded)
- its own resolver leg + its own `request-a15/` template folder

Sequencing unchanged: **after** the REQ-026 template split, so a15 clones the split
`request-a9-transport/` rather than the shared folder.

Do not re-derive the evidence labels from scratch — reuse the transport implementation.

## [later] Sober (SA)
- REQ-027 (อ.15) — wrote **SPEC-029**. Porter's form-identical finding holds and ~90% reuses a9-transport,
  BUT — per your "tell me before Jason starts if it's bigger than a clone" — **it is not a trivial clone.**
  Two things to resolve first:
  1. **item-5 source is NOT in `T_T_REQUEST_SALE_DOM`.** a9-transport reads item-5 from `MOVE.AUTHORITY_NAME`;
     SALE_DOM has **no AUTHORITY_NAME and no BUYER_NAME** (a14's SALE_INT had BUYER_NAME — SALE_DOM doesn't).
     So a15's buyer-org value must come from elsewhere (likely `T_T_REQUEST_BUYER`, or blank). **DATA REQUEST /
     dict confirm — I won't guess** (DEF-11 + the a14 BUYER_NAME-still-unconfirmed lesson). Also item-2:
     a9-transport derives it from `MOVE_REQUEST_TYPE`; SALE_DOM has none → a15 item-2 is almost certainly a
     fixed constant (like a14) — confirm the wording from the form.
  2. **The a9 base is MOVE-typed** (`A9CheckListReportBuilderBase` passes `RequestMoveEntity` to every hook) →
     a15 (no MOVE row) can't extend it as-is. Recommend generalising the base (header entity abstract,
     byte-identical refactor like TASK-024) over re-duplicating the shared logic REQ-026 just consolidated.
- Verified from the dict: SALE_DOM has FORM_ID + `SALE_DOM_STATUS` (family status, SPEC-027 pattern) →
  resolver leg = row-presence + FORM_ID, no status filter. `ReqSaleDom` seeded (won't request).
- @Porter: REQ-027 is a MEDIUM clone with **one DATA REQUEST (a15 item-5/item-2 source)** + one design call
  (base generalisation) before Jason starts — flagging now as you asked, rather than mid-build. TASK-026 stays
  BLOCKED on that + TASK-025. Board: REQ-027 → IN_SPEC.
- @Jason: TASK-025 (a9 template split) is still your active next; TASK-026 (a15) is gated.

## [later] Sober (SA)
- Reviewed **TASK-025** (a9 template split per-form) — **DONE**. Verified: request-a9-destroy/ + -transport/
  each with main+5 subreports; old request-a9/ deleted; ResourceService openers take a `folder` arg; **base +
  variant builders untouched** (grep-clean); each folder renders its correct distinct variant (destroy
  ขนย้าย/กำจัด/9-subs; transport ขายและขนย้าย/หน่วยงานผู้ซื้อ/export), cross-checked no leakage, no null, 4/5 pages.
- Accepted Jason's DoD-2 note — my "only ReportResourceService.java" was over-specified; per-folder routing
  necessarily touches JasperPdfReportService + the 2 controllers. Real intent (base+builders untouched) fully met.
- **REQ-026 is now fully code-complete** — builder split (TASK-024) + per-form templates (TASK-025). อ.9 is
  split like every other form. QA leg = real destroy+transport identical + both route.
- @Porter: REQ-026 done our side (both tasks). REQ-027 (อ.15) template-clone step is now unblocked (clones the
  split request-a9-transport/), but REQ-027 still needs the item-5/item-2 source DATA REQUEST + the base-class
  decision I flagged before TASK-026 can start.
- Nothing waits on Sober. Ball → Porter (QA REQ-026 real render + the REQ-027 DATA REQUEST to the human).

## [later] Porter (PM) — REQ-027 DATA REQUEST answered, and it does NOT need a new source

`ALL_TAB_COLUMNS` for `T_T_REQUEST_SALE_DOM` (484 rows, live). Sober's read was right: **there is no
buyer/authority column on the header table.** Nothing resembling `BUYER_NAME` / `AUTHORITY_NAME` /
`IMPORTER_NAME` exists. Relevant columns are:
`ID, REQUEST_ID, FORM_ID, REF_LICENSE_NO/ISSUE_DATE/EXPIRY_DATE/TYPE, PLANT_*, NOTE1,
SALE_DOM_STATUS, CHECKING_*, DEST_INFORM_*, BKK_INFORM_*, REQUEST_MODE, SUBMIT_*, UPDATE_*`.

@Sober — **but this is not a blocker, because อ.9 transport doesn't take item-5 from its header table
either.**

Per the board (TASK-015), อ.9 transport's item-5 "หน่วยงานผู้ซื้อ/ผู้รับอาวุธ" is data-driven from
**`AUTHORITY_NAME`, which lives on the request *detail* rows** — and those are now read through
`VW_REQUEST_DTL`, which exposes `AUTHORITY_NAME`, `AUTHORITY_NAME_ABBR` and `BUYER_AUTHORITY_ID`
(see the view definition filed in REQ-025).

So **อ.15 inherits item-5 for free** by keeping the transport implementation as-is. No DATA REQUEST,
no new entity, no new column. Please verify that against the a9 transport builder before designing
around it — if I've misread which field it binds, say so and I'll go back to the human rather than
have you build on my assumption.

Two smaller points from the same column list:
- **`SALE_DOM_STATUS`** — the per-family status naming pattern holds again (`REQUEST_STATUS` /
  `MOVE_STATUS` / `SALE_INT_STATUS` / `SALE_DOM_STATUS`). **Do not map a plain `STATUS`** — that is
  precisely DEF-11.
- **No permit-type column**, so item-2 is a fixed per-form constant, same as อ.14. Confirm the exact
  wording from the official form (which we hold — it is the shared อ.9/อ.15 document), don't invent it.

Still open and genuinely mine: the `FORM_ID` **value** on SALE_DOM rows, for the resolver leg.
Going to the human for that one query.

## [later] Porter (PM) — REQ-027 fully unblocked: FORM_ID confirmed

```
SELECT FORM_ID, COUNT(*) FROM T_T_REQUEST_SALE_DOM GROUP BY FORM_ID;
  15 → 483
   0 →   1
```

**อ.15 = `FORM_ID 15`**, per-family pattern holds (board ARCHITECTURE FACT). 483 real rows.

@Sober: one design call I want made deliberately rather than by default — **there is one stray row
with `FORM_ID = 0`.**

- If `resolveFromSaleDom` matches on **row presence** (the `resolveFromMove` shape), that row routes
  to a15 and renders whatever it has. Degrades gracefully.
- If it matches on **`FORM_ID = 15`**, that row falls through to the `REQUEST_TYPE` fallback and may
  well end up in the `default → throw` path — i.e. a 500, which is exactly the failure mode we just
  spent two rounds removing (DEF-11 / REQ-024).

**Recommend row-presence**, consistent with `resolveFromMove`. If you prefer FORM_ID matching, make
sure the miss path cannot throw. Either way, say which you chose and why in the SPEC.

Everything REQ-027 needs is now answered: form (identical to อ.9 transport, verified), header table
(`T_T_REQUEST_SALE_DOM`, live, 483 rows at FORM_ID 15), status column (`SALE_DOM_STATUS`, **not**
`STATUS`), master (`ReqSaleDom`, 13 rows, already seeded), item-5 (inherited via
`VW_REQUEST_DTL.AUTHORITY_NAME`), annex (`VW_REQUEST_DTL`), item-2 (fixed constant from the official
form). No open DATA REQUESTs. TASK-026 gates only on TASK-025.

## [later] Porter (PM) — @Sober: TASK-026 (อ.15) is UNBLOCKED. Both SPEC-029 blockers are answered above.

Human's instruction: **proceed with อ.15 now.** Your two flagged blockers are both resolved in this
log — please read them before re-raising:

1. **Item-5 source — no DATA REQUEST needed.** `T_T_REQUEST_SALE_DOM` indeed has no buyer column, but
   อ.9 transport never took item-5 from its header table either: it is data-driven from
   `AUTHORITY_NAME` on the **detail** rows, which we now read via `VW_REQUEST_DTL` (the view exposes
   `AUTHORITY_NAME` / `AUTHORITY_NAME_ABBR` / `BUYER_AUTHORITY_ID`). อ.15 inherits it by keeping the
   transport implementation unchanged. Verify against the a9 transport builder and correct me if I
   misread it — but do not send it to the human.
2. **TASK-025 is done**, so the base/template decision is no longer gated: clone the split
   `request-a9-transport/` into `request-a15/`.

Also confirmed and ready to use: `FORM_ID 15` (483 rows), `SALE_DOM_STATUS` (**never** map a plain
`STATUS` — DEF-11), `ReqSaleDom` master already seeded (13 rows), item-2 = fixed constant taken
verbatim from the official form (the shared อ.9/อ.15 document we hold).

Open design call I asked you to make deliberately: the stray `FORM_ID = 0` row — recommend
row-presence matching so it degrades instead of hitting a throwing fallback. State your choice in the
SPEC.

## [later] Sober (SA)
- Board: **TASK-025 → DONE** (was stale at REVIEW). **REQ-026 fully code-complete** (builders + per-form templates).
- **REQ-027 unblocked (human: proceed). Verified Porter's item-5 claim as he asked — and corrected the mechanism:**
  a9-transport `item5Value(move)` = `move.getAuthorityName()` → item-5 reads from **MOVE.AUTHORITY_NAME (header),
  NOT VW_REQUEST_DTL**; `RequestDtlViewEntity` doesn't map AUTHORITY_NAME. So a15 does NOT inherit it unchanged.
  **BUT Porter's bottom line (no human DATA REQUEST) is right:** the `VW_REQUEST_DTL` SQL exposes
  `DTL.AUTHORITY_NAME` — the column exists, just unmapped. ⇒ **a15 reads item-5 from the view** (map AUTHORITY_NAME
  on RequestDtlViewEntity); **leave a9-transport on MOVE (byte-identical).** QA confirms DTL==buyer-org on real a15.
- Resolved the other two: **base** — a15 extends the MOVE-typed base with move=null (overrides ignore it) + one
  output-preserving change: extract item-2 into a `permitTypeLabel(move)` hook (a9 returns `moveRequestTypeLabel`
  = byte-identical; a15 returns the fixed const). **FORM_ID=0** — chose **row-presence** matching (Porter's rec):
  stray row degrades gracefully, no throwing fallback; no status filter (SALE_DOM_STATUS, DEF-11).
- Wrote **TASK-026** (@Jason, UNBLOCKED): a15 shell + builder(extends base) + resolveFromSaleDom row-presence +
  RequestSaleDomEntity(no STATUS) + AUTHORITY_NAME on the view entity + item-2 hook; clone request-a9-transport/.
  DoD: a9 destroy/transport byte-identical + SPEC-027 column citations.
- @Porter: verified + corrected the item-5 mechanism (per your ask) — net effect matches your call (no human ask).
  REQ-027 tasked. @Jason: TASK-026 is yours. Ball → Jason. (QA legs open: REQ-026 real render, then a15.)

## [later] Sober (SA)
- Reviewed **TASK-026 (อ.15 clone)** — **DONE** (code-complete + verified):
  a9 **byte-identical** (my 7808/7610 = TASK-025 baseline → item-2 hook + item-12 relocation didn't move a9);
  a15 renders the transport form (5pp, no null); `RequestSaleDomEntity` no-STATUS + view `AUTHORITY_NAME`
  dict-cited (SPEC-027); `resolveFromSaleDom` row-presence → A15; tests green; a6/a9/a14 untouched.
- **Approved Q2** (buildTransportItem12 moved to the base, shared by a9-transport + a15) — shared-logic-in-one-place,
  output-preserving; correct over duplicating.
- **Q1 / owning my SPEC-029 error:** I told Jason item-2 = "fixed constant from the form" — **wrong.** The form
  value is `ขึ้นตามประเภทที่เลือกในระบบ` (system-driven placeholder, no fixed string). For a9 it's
  moveRequestTypeLabel(type); a15 has no MOVE type + SALE_DOM has no obvious type column. Jason correctly did
  NOT guess (blank/graceful). ⇒ **1 OPEN DATA ITEM → @Porter/human:** what is a15's ประเภทการขออนุญาต value —
  a fixed domestic-sale type string, or a SALE_DOM-driven field? 1-line set when it lands.
- @Porter: REQ-027 code-complete our side. Two things on your leg: (1) route the a15 item-2 wording question to
  the human; (2) QA a real a15 render (item-5 = view AUTHORITY_NAME = buyer-org; a9/a14 unaffected). Board:
  REQ-027 → SPEC_DONE. Nothing waits on Sober.

## [later] Porter (PM) — closing the stale อ.6 REQs from evidence already in hand

Nothing waits on me from the team, so I went back to the five อ.6 requirements that have sat at
`SPEC_DONE / IN_TEST` since the start. Rather than ask the human for another test pass, I re-read the
real a6-38272 PDF they already gave me (rendered from the real DB after all อ.6 work landed).

**Closable from that evidence — no new test needed:**

- **REQ-011 (item-8 "เอกสารอื่นๆ" dynamic)** → **DELIVERED.** The rendered line reads
  `เอกสารอื่น ๆ (ถ้ามี) ทดสอบ2, ทดสอบ 1` — dynamic, comma-joined, multiple type-99 docs. That is the
  requirement's behaviour on real data. (Board expected the proof on 38314; 38272 demonstrates the
  same behaviour, so the sample choice doesn't matter.)
- **REQ-012 (dotted write-in render)** → **DELIVERED.** The label and its value extract as separate
  runs with the value on the write-in line and no label/value collision — the Type-B behaviour the
  requirement asked for. Type-A items (6, 7) still render label-only.

**NOT closable from a PDF — leaving open, honestly:**

- **REQ-009 (tick reflects a real attachment)** — text extraction cannot reliably distinguish a
  ticked from an unticked box. Needs a visual check or the emitted JSON.
- **REQ-010 / REQ-015 (person filtering: exclude 'D', keep NULL)** — the PDF shows three persons
  across items 3/4, which is consistent, but I cannot confirm *which* rows were included/excluded
  without the DB. Needs `/a6/db/37940` (NULL-status persons must appear) + 38272 (unchanged).

I am not marking those three DELIVERED on a "looks fine" basis — that is exactly how DEF-11 slipped
through. They stay open with a precise, one-pass check recorded for whenever the human wants it.

## [later] Porter (PM) — REQ-027 last item answered: อ.15 item-2 string

Human confirmed the อ.15 "ประเภทการขออนุญาต" text:

> **`ขายและขนย้ายให้บุคคลอื่นนอกหน่วยงานตามมาตรา 7`**

@Sober: use that **verbatim as a fixed per-form constant**, the same way อ.14 carries its own. The
form's "ขึ้นตามประเภทที่เลือกในระบบ" is authoring guidance, not printable text — your correction of
SPEC-029 was right, and `SALE_DOM` genuinely has no type column (I hold the full column list).

⚠️ **One thing to hold exactly as given, and to flag rather than normalise:** the human wrote
**"นอก·หน่วยงาน"**, whereas อ.14's constant reads **"นอก·จาก·หน่วยงาน"**. Do **not** "align" them —
type it exactly as the human wrote it. If it turns out to be a slip, that is theirs to correct, and I
have raised it to them explicitly so they had the chance. This is the DEF-6 rule applied to a
stakeholder-supplied string: we do not improve wording we were handed.

That closes the last open item on REQ-027. Nothing from me blocks Jason.

## [later] Porter (PM) — อ.15 item-2 resolved properly: it is อ.9's EXISTING type-1 label, not a new constant

Human clarified:
> *"อ15 คือ อ9 type ขายและขนย้ายให้บุคคลอื่นนอกหน่วยงานตามมาตรา 7"*

I traced it in the code rather than take the typed string at face value:

`A9CheckListReportBuilderBase.java:363-364`
```java
case 0 -> "ขนย้ายให้หน่วยงานตามมาตรา 7";
case 1 -> "ขายและขนย้ายให้บุคคลอื่นนอกจากหน่วยงานตามมาตรา 7";
```

So **อ.15 = อ.9 `MOVE_REQUEST_TYPE` 1**, and the canonical wording already lives in our code as
**"นอก·จาก·หน่วยงาน"** — with จาก. My earlier flag is closed: the human's message dropped จาก in
passing; the existing map string is the authoritative one, and it is consistent with อ.14's constant.
Nothing to correct, and no need to trouble them again.

@Sober — this changes the implementation, and for the better:

**Do NOT add a new hardcoded `PERMIT_TYPE` constant to the a15 builder.** Bind item-2 to the
**existing type-1 label already in `A9CheckListReportBuilderBase`**. อ.15 is a clone of อ.9 transport,
so it should inherit the same label source rather than carry a second copy of the same Thai string.

This is the REQ-023 lesson applied ahead of time: item-7 had to be fixed report-by-report *because*
each report re-derived it. A duplicated permit-type string would age the same way — the day someone
amends that wording, they would have to find two copies and would miss one.

Say in the SPEC how a15 selects type 1 (fixed for the form vs read from a column), so the choice is
explicit rather than incidental.

## [later] Porter (PM) — 🔑 THE ANSWER: `T_T_REQUEST.REQUEST_TYPE` is the real discriminator. Our resolver is backwards.

The human walked me to this rather than telling me, and it is the most important structural finding
of the project so far.

**Every request family writes a row into the parent `T_T_REQUEST`, and `REQUEST_TYPE` says which form
it is.** From `T_S_COMMON_CODE` GROUP_CODE `RequestType`:

| REQUEST_TYPE | Form |
|---|---|
| 0 | ตรวจสอบประวัติ (BgChk) |
| 1 | ตรวจโรงงาน |
| 2 | อ.1 / อ.3 / อ.20 |
| **3** | **อ.9 ขนย้าย** |
| **4** | **อ.4 นำเข้า** |
| **5** | **อ.15 ขายในราชอาณาจักร** |
| **6** | **อ.14 ขายส่งออกนอกราชอาณาจักร** |
| 7 | อ.11 |
| **8** | **อ.6 ผลิตส่วนประกอบเฉพาะคราว** |
| 50/51/52/53 | เปิดดำเนินการ / ขยายสาย / เปลี่ยนโรงงาน / เปลี่ยนกรรมการ |

### Why this matters — we built the resolver inside-out
`RequestTypeResolverService.resolveChecklistRequestType` probes **four family tables in sequence**
(SPECIAL → SALE_INT → MOVE → SALE_DOM) and only *then* falls through to a `REQUEST_TYPE` switch —
a switch that already existed and already handled 0/2/50/51/52/53.

**The fallback was the right mechanism the whole time.** We kept bolting a per-family probe in front
of it because we never looked at `T_T_REQUEST`. Consequences we actually paid for:

- **DEF-11** (real `/download` 500 on อ.9 and อ.14) existed *only* because a probe leg ran ahead of
  the switch and blew up on a column that didn't exist. With a single `REQUEST_TYPE` lookup there is
  no leg to break.
- Four extra DB round-trips on every download, three of which normally miss.
- Every new form needs a new leg + a new entity + a new repo — which is exactly the cost I was about
  to sign up for again with อ.4–อ.8.

### Raising REQ-028 — collapse the resolver to one lookup
`T_T_REQUEST.REQUEST_TYPE` → report, with **one** sub-lookup that genuinely remains:
type 3 (อ.9) still needs `MOVE_REQUEST_TYPE` to choose destroy vs transport.

⚠️ **The one thing I do NOT yet know, and will not assume:** อ.6 and อ.7 share the A6 report and are
today told apart by `T_T_REQUEST_SPECIAL.FORM_ID` (6 vs 7). `RequestType` has a single code 8 for
อ.6. So either อ.7 is not a separate request at all (it is a *licence*), or it carries its own
REQUEST_TYPE we haven't seen. **SA must resolve this before deleting the SPECIAL leg** — deleting it
blindly could break อ.7, which is delivered and in use.

Do this **before** อ.4–อ.8: adding type 4 then costs one switch case instead of an entity + repo +
leg + a fresh chance at DEF-11.

## [later] Porter (PM) — 🔗 FOUND IT: อ.14/อ.15 are PARENTS of an อ.9 request via `T_T_REQUEST.REF_REQUEST_ID`

```
CHILD_TYPE  PARENT_TYPE   N
    3            5       426     อ.9 ขนย้าย  →  อ.15 ขายในประเทศ
    3            6        20     อ.9 ขนย้าย  →  อ.14 ขายส่งออก
    1            2        91     ตรวจโรงงาน →  อ.1/อ.3/อ.20
    1           50        51     ตรวจโรงงาน →  เปิดดำเนินการ
    0            2        33     ตรวจประวัติ →  อ.1/อ.3/อ.20
```

**A sale request (อ.14 / อ.15) spawns a CHILD อ.9 move request**, linked by
`T_T_REQUEST.REF_REQUEST_ID` on the child. This is what the form title has been telling us all along —
"ขาย**และขนย้าย**อาวุธ": the sale is the parent, the move is its child.

### ⚠️ This directly challenges how we built อ.15's item-5

`A15ReportBuilder` reads item-5 (หน่วยงานผู้ซื้อ/ผู้รับอาวุธ) from **`VW_REQUEST_DTL.AUTHORITY_NAME`**,
on the reasoning "อ.15 has no MOVE row". That premise was **half right and half wrong**:

- Correct: the อ.15 request itself has no MOVE row (the JOIN on REQUEST_ID returned 0 — I ran it).
- **Wrong:** there *is* a MOVE row in the picture — it belongs to the **child** อ.9 request
  (`REF_REQUEST_ID` = the อ.15 id). 426 อ.15 requests have one.

So the natural source for อ.15 item-5 is very likely **the child อ.9's `MOVE.AUTHORITY_NAME`** —
i.e. exactly the same field อ.9 transport uses — rather than the first detail row.

@Sober: **do not change anything yet — verify first.** Compare, on a real อ.15 request:
`VW_REQUEST_DTL.AUTHORITY_NAME` (first row) vs the child อ.9's `MOVE.AUTHORITY_NAME`. Three outcomes:

1. They agree → our output is already correct; switch the source anyway for correctness of intent, or
   document why we keep the view. Either way, say which.
2. They differ → **our อ.15 item-5 is printing the wrong value** — a live defect, raise it.
3. Some อ.15 requests have no child อ.9 → the view fallback still earns its place; keep both, ordered.

Same question applies to **อ.14**, which reads `SALE_INT.BUYER_NAME`. That one the human confirmed
directly, so I am not reopening it — but note 20 อ.14 requests also have child อ.9 rows, so the
relationship exists there too.

### Also worth carrying into REQ-028
`REF_REQUEST_ID` is a real parent/child spine across the whole system (inspection ← factory licence,
background-check ← factory licence). Whatever we do to the resolver should not assume a request is
standalone.

## [later] Sober (SA)
- Board: TASK-026 → DONE reconciled. REQ-027 item-2 resolved by human (Jason set the confirmed const) — noted.
- **REQ-028 (resolver collapse) — wrote SPEC-030 + TASK-027 (GATED).** Design locked: one `switch(REQUEST_TYPE)`
  (0/2/3/4/5/6/8/50-53), only remaining sub-lookup = MOVE_REQUEST_TYPE for a9 (`resolveA9` → A9D/A9T); delete
  the 4 per-family probes; `RequestSaleDomEntity` becomes dead (routing-only — a15 reads item-5 from VW) → delete;
  KEEP RequestSaleInt (a14 item-5) + RequestMove (a9 + resolveA9).
- **Flagged the real risk (verification gate):** the per-family probes exist *because routing broke* (REQ-024/
  DEF-11) — they're robust; REQUEST_TYPE is one code that could be null/inconsistent, and the probes currently
  take precedence. So the collapse is safe ONLY if REQUEST_TYPE is reliably populated + consistent with the
  family tables. **MANDATORY real-data confirm before deleting probes** (Porter/QA, rule #4): อ.6 AND **อ.7**
  both →A6 (Porter's explicit must-resolve — his map lists 8=อ.6 but not อ.7's code), a9=3 (both variants),
  a14=6, a15=5; then QA every family via the REAL /download.
- Separately — **Porter's a15 item-5 verify note is a real-DB comparison** (VW.AUTHORITY_NAME vs child-a9
  MOVE.AUTHORITY_NAME) → **QA's leg, not Sober's** (rule #4). Framed in SPEC-029 already (3 outcomes). Routing to Porter.
- @Porter: REQ-028 spec'd but **gated** — need the REQUEST_TYPE→family confirm (one SELECT per family + the
  อ.6/อ.7 codes) before Jason collapses the resolver; otherwise we risk re-introducing the DEF-11-class misroute.
  Board: REQ-028 → IN_SPEC; TASK-027 BLOCKED. @Jason: TASK-027 gated — nothing to start yet.

## [later] Porter (PM) — 🔴 DEF-12: อ.15 item-5 IS printing the wrong value. Confirmed on real data.

The comparison came back unambiguous — 10/10 rows, no exceptions:

| REQUEST_ID | child อ.9 `MOVE.AUTHORITY_NAME` | `VW_REQUEST_DTL.AUTHORITY_NAME` (what we print) |
|---|---|---|
| 18041 | สมาคมกีฬานักยิงปืนสมัครเล่นภูเก็ต | `-` |
| 18043 | สมาคมยิงปืนเชียงราย สุวิทย์ เม่นแย้น | `-` |
| 18045 | สมาคมนักกีฬายิงปืนคลองหลวง | `-` |
| … | … (all real association names) | `-` |

**Outcome 2 of the three I gave Sober: they differ, and ours is the wrong one.** The view's
`AUTHORITY_NAME` on the detail rows is the placeholder `"-"`; the real buyer/receiver organisation
lives on the **child อ.9 request's MOVE row**. So อ.15 currently prints `-` in ข้อ 5
"หน่วยงานผู้ซื้อ/ผู้รับอาวุธ" on every request.

**Raising DEF-12.** This is on code we just delivered, and it is exactly the defect my own reasoning
introduced: I told Sober "อ.15 has no MOVE row, so read AUTHORITY_NAME from the view". The premise was
true of the อ.15 row itself and false of the request family. The human's questions
("รู้มั้ยว่า อ.14 กับ อ.15 เชื่อมกับ อ.9 ยังไง") are what surfaced it — I would not have found it from
the a15 render alone, because `-` looks like plausible data, not like a bug.

@Sober — the fix, and it makes อ.15 *more* like อ.9 transport rather than less:

1. `A15ReportBuilder.item5Value` → resolve the **child อ.9** request
   (`T_T_REQUEST.REF_REQUEST_ID = <a15 requestId>`, child `REQUEST_TYPE = 3`), then read
   `MOVE.AUTHORITY_NAME` from that child's `T_T_REQUEST_MOVE` row. Same field อ.9 transport already
   uses — so bind it through the shared path rather than a second copy.
2. **Keep a graceful fallback:** not every อ.15 necessarily has a child (426 of 483 do). No child ⇒
   blank, never `-`, never "null".
3. **`-` is a placeholder, not data.** Worth checking whether any other field we print can carry it —
   if the annex/รายการ can surface a bare `-`, tell me and I will take it to the human as a display
   question (the view's own `PRODUCT_NAME_DISPLAY_LICENSE` already strips `'-'`, which is evidence the
   system treats it as "empty").
4. Verify on real data, not the mock: 18041 must print `สมาคมกีฬานักยิงปืนสมัครเล่นภูเก็ต`.

Also: **this changes REQ-028's shape.** `RequestSaleDomEntity` was going to be deleted as
"routing-only" — but อ.15 now needs the parent/child hop, so re-check what the a15 builder actually
requires before deleting anything.

## [later] Sober (SA)
- **DEF-12 (🔴 TOP) — spec'd + tasked (SPEC-031 / TASK-028). I own the root cause: my SPEC-029 item-5 call
  was wrong.** I wired a15 item-5 to `VW_REQUEST_DTL.AUTHORITY_NAME`, but on real data that column is the
  placeholder `-`; the buyer org lives on the **child อ.9**'s `MOVE.AUTHORITY_NAME` via `T_T_REQUEST.REF_REQUEST_ID`.
  Verified the mechanism: RequestEntity.refRequestId mapped; T_T_REQUEST has REF_REQUEST_ID+REQUEST_TYPE; a15
  item5Value hook has requestId to do the hop.
- **Fix (TASK-028):** a15 item5Value → `findByRefRequestIdAndRequestType(requestId, 3)` → child MOVE.AUTHORITY_NAME
  (same field a9-transport prints); no child (57/483) → blank, never `-`/"null". Remove the dead
  `RequestDtlViewEntity.AUTHORITY_NAME` mapping I introduced. a9-transport item-5 UNCHANGED.
- **Corrected Porter's REQ-028 worry:** DEF-12 uses `REF_REQUEST_ID` + child MOVE — **NOT** SALE_DOM. a15 still
  reads no field from SALE_DOM ⇒ `RequestSaleDomEntity` stays routing-only and REQ-028's delete-plan still holds
  (re-confirm at REQ-028 time). Noted on the board.
- Porter #3 ("-" placeholder elsewhere): annex `PRODUCT_NAME_DISPLAY_LICENSE` already strips `-`; item-5 was
  the leak; no other bare-`-` source found. Do NOT blanket-strip on our side (view owners' concern, REQ-025).
- @Jason: **TASK-028 is TOP** — one-hook repoint + repo finder + delete the dead view mapping. QA proof =
  18041 → สมาคมกีฬานักยิงปืนสมัครเล่นภูเก็ต.
- @Porter: DEF-12 owned (my SPEC-029 miss) + tasked. REQ-028 stays gated on the REQUEST_TYPE-map verify;
  DEF-12 doesn't change its shape. Ball → Jason (TASK-028).

## [later] Sober (SA)
- Reviewed **TASK-028 (DEF-12)** — **DONE**. a15 item-5 = child อ.9 hop (findByRefRequestIdAndRequestType(id,3)
  → child MOVE.AUTHORITY_NAME; no child → blank, never `-`/"null"); finder returns List (11.2-safe);
  **a9-transport item-5 unchanged** (move.getAuthorityName); dead VW AUTHORITY_NAME mapping (my SPEC-029 miss)
  removed; test-compile + A15/A9 PreviewTests green.
- **DEF-12 fixed in code** (my SPEC-029 error corrected). QA leg: 18041 → สมาคมกีฬานักยิงปืนสมัครเล่นภูเก็ต.
- @Porter: DEF-12 closed our side. QA (real DB): 18041 item-5 = the association name (not `-`); a15-no-child →
  blank; a9-transport item-5 unchanged. Nothing waits on Sober. REQ-028 (TASK-027) still gated on your
  REQUEST_TYPE-map verify. Ball → Porter (QA + the REQ-028 data).

## [later] Porter (PM) — ✅ REQ-028 verification gate PASSED. Collapse is safe. Plus: the อ.7 branch is dead code.

Human ran both checks. The mapping is **perfectly 1:1 with no nulls and no mixing**:

```
T_T_REQUEST_SPECIAL   → REQUEST_TYPE 8, FORM_ID 6      70 rows   (and NOTHING else)
T_T_REQUEST_MOVE      → REQUEST_TYPE 3               2,885 rows
T_T_REQUEST_SALE_INT  → REQUEST_TYPE 6                  38 rows
T_T_REQUEST_SALE_DOM  → REQUEST_TYPE 5                 484 rows
```

@Sober: **your gate is satisfied — TASK-027 is unblocked.** Every family table maps to exactly one
`REQUEST_TYPE`, every row has one, and no family appears under two codes. The concern that
REQUEST_TYPE might be null or inconsistent does not hold in this database. Collapse the resolver.

### And the อ.6/อ.7 question you both flagged is now answered — by absence

`T_T_REQUEST_SPECIAL` contains **only FORM_ID 6**. There is **no FORM_ID 7 row at all.** So:

- `FORM_ID_A7` and the `case FORM_ID_A6, FORM_ID_A7 -> "A6"` branch in `resolveFromSpecial` are
  **unreachable** — the same dead-branch shape as the `9/10 → A9` branch we deleted under REQ-024,
  and the same shape that let DEF-11 hide.
- อ.7 is a **หนังสืออนุญาต (licence)**, not a request — which is why it never appears in a request
  table. The report is reached for อ.7 through the licence/checklist-form path, not through
  `resolveChecklistRequestType`.

⚠️ **Do not treat that as licence to break อ.7.** Before deleting the constant, confirm the อ.7 entry
point (`resolveChecklistRequestTypeByChecklistFormId` → requestId → resolver) still lands on A6 for a
licence-side อ.7. Deleting an unreachable *branch* is safe; deleting อ.7 *support* is not. If you
cannot establish the อ.7 path from code alone, say so and I will get the human to open one.

**Net for REQ-028:** one `switch (REQUEST_TYPE)`; `resolveA9` sub-lookup for destroy/transport; the
four probes go. Remember DEF-12 changed the entity picture — `RequestSaleDom`/the parent-child hop may
still be needed by the a15 *builder* even though it is no longer needed for *routing*. Check before
deleting.

## [later] Sober (SA)
- Reviewed **TASK-028 (DEF-12) → DONE** (already verified last turn; reconciled the board row).
- **REQ-028 gate PASSED → un-blocked TASK-027.** Porter's real-data verify is 1:1 (SPECIAL=type8/FORM_ID6·
  MOVE=3·SALE_INT=6·SALE_DOM=5, no nulls/mixing). Folded the map + two resolutions into TASK-027:
  * **อ.7:** confirmed `FORM_ID_A7` is only in `resolveFromSpecial` (being deleted) + no FORM_ID 7 row → dead
    branch, safe to delete. อ.7 licence reaches A6 via the checklist-form path → requestId → type 8 → A6. **But
    I can't prove the อ.7 request carries type 8 from code alone** → made it a **DoD QA gate** (render a real อ.7
    licence → A6); if no sample, Porter opens one. (Followed Porter's "deleting a dead branch is safe; deleting
    อ.7 support is not.")
  * **RequestSaleDom:** grep confirms it's referenced ONLY by `resolveFromSaleDom` (a15 builder uses
    REF_REQUEST_ID+child MOVE post-DEF-12, not SALE_DOM) → deletable with the probe. Porter's DEF-12 worry cleared.
  * `resolveA9` no-MOVE default = "A9T" (matches today's else).
- @Jason: **TASK-027 is yours** (resolver collapse) — one switch, delete 4 probes + RequestSaleDom, resolveA9;
  DoD includes the อ.7-licence-→A6 QA gate. Ping me for review.
- @Porter: gate passed, TASK-027 unblocked. One residual for QA: a real อ.7 licence must render A6 under the
  collapse (I couldn't prove type-8 for อ.7 from code). Board: REQ-028 → SPEC_DONE. Ball → Jason.

## [later] Sober (SA)
- Reviewed **TASK-027 (resolver collapse)** — **collapse correct + verified**: one `switch(REQUEST_TYPE)`
  (0/2/3/4/5/6/8/50-53), `resolveA9` (MOVE 2→A9D else A9T; no-MOVE→A9T), 4 probes + RequestSaleDom deleted;
  A6/A9/A14/A15 PreviewTests green (4/0); boots.
- **Answered Jason's flag — DELETE RequestSpecial.** Grep: `RequestSpecialEntity/Repository` have **0 refs
  anywhere** (resolver dropped it; no a6 builder use) → orphaned by this change. Remove both (same as
  RequestSaleDom); no speculative keep. Sent back for that one cleanup → then code-DONE.
- **Still on DoD:** QA อ.7-licence-→A6 real render (can't prove อ.7=type8 from code).
- @Jason: delete RequestSpecial (entity+repo), ping me → I close the review. @Porter: REQ-028 code-complete
  bar one dead-code deletion; QA per-family (esp. อ.6 AND อ.7 → A6) is the close. Ball → Jason (tiny), then QA.

## [later] Sober (SA)
- **TASK-027 → DONE (collapse SA-verified).** RequestSpecial deletion is **harness-blocked** (BE can't delete
  pre-existing files w/o human consent) — the 2 files are confirmed dead (0 refs, builds+boots), so keeping them
  is harmless; carved out as a **human-consent housekeeping** item, not a functional blocker. Didn't let cosmetic
  dead-code hold a verified functional change.
- REQ-028 code-complete. Two non-Sober leftovers: (a) human deletes/accepts the 2 orphaned RequestSpecial files;
  (b) QA per-family real render (esp. อ.6 AND อ.7 → A6).
- @Porter: REQ-028 collapse done+verified our side. Nothing waits on Sober. Ball → Porter (QA per-family + put
  the RequestSpecial dead-file delete to the human).

## [later] Porter (PM) — 🔴 DEF-13: อ.9 ticks are bound BY POSITION, and ReqMove's inactive rows already shift them

Human is about to **renumber `T_S_REQUEST_CHECKLIST` SEQUENCE** and wants the อ.9 transport checklist
output to stay exactly as it is. I went to look at how we bind ticks. It is worse than a
future-proofing request — **the binding is already wrong.**

`A9CheckListReportBuilderBase.buildEvidences`:
```java
List<RequestCheckListEntity> master =
    requestCheckListRepository.findByGroupCodeAndIsActiveOrderBySequenceAsc(checklistGroup(), ACTIVE);
// "master index (0-based) = SEQ-1"
IntFunction<Long> mid = i -> (i < master.size()) ? master.get(i).getId() : null;
```
The list is **filtered to IS_ACTIVE=1**, so index ≠ SEQ-1 the moment any row is inactive.

`ReqMove` has 15 rows, **SEQ 4, 5 and 6 are `IS_ACTIVE = 0`** → only **12 active rows**
(SEQ 1,2,3,7,8,9,10,11,12,13,14,15). So:

| code index | code expects | actually gets |
|---|---|---|
| 0,1,2 | SEQ 1,2,3 | SEQ 1,2,3 ✅ |
| 3 | อ.2 (SEQ4) | **SEQ7 บัตรผู้เสียภาษี** ❌ |
| 4 | อ.7 (SEQ5) | **SEQ8 ภ.พ.20** ❌ |
| 5 | เปิดดำเนินการ (SEQ6) | **SEQ9 คุณลักษณะอาวุธ** ❌ |
| 6…11 | SEQ7…12 | SEQ10…15 (all shifted) ❌ |
| 12,13,14 | ส.ค.4 / แผนกระสุน / ภาพถ่าย | **out of range → null, can NEVER tick** ❌ |

⇒ On อ.9 transport, an attached document ticks **the wrong line**, and the last three item-12 lines
can never tick at all. It renders as a perfectly plausible checklist, which is why nobody caught it —
and why the "no อ.14 attachments exist" situation has been hiding the same class of bug there.

**Raising DEF-13 as TOP.** Same root cause family as DEF-11: a positional/structural assumption about
someone else's data that compiles, runs, and quietly produces wrong paper.

### The fix — and it is exactly what the human is asking for
@Sober: **bind ticks by a stable key, never by list position.**

- `T_S_REQUEST_CHECKLIST.ID` is the stable identity (ReqMove = 117–131). **`SEQUENCE` is presentation
  order and the human is about to change it.** `CHECKLIST_CODE` is NOT stable either — it encodes
  DOCUMENT_ID + SEQUENCE (`ReqMove12111` = doc 121, seq 11), so it moves when SEQ moves.
- Our report structure is **locked in the template** (the human's standing rule), so each printed line
  needs an explicit, durable pointer to its master row. Propose: map each printed line to the master
  row **by `ID`**, resolved once, with the mapping written down in one place per form.
- Whatever key you choose, it must survive: SEQUENCE renumbering, rows toggling `IS_ACTIVE`, and new
  rows being inserted mid-list.
- **`ReqMoveDestroyer` (19 rows) and `ReqSaleInt` (SEQ 4,5 inactive) use the same positional pattern —
  audit all four groups**, not just ReqMove.

Acceptance: อ.9 transport output must be **unchanged in structure** but with ticks landing on the
correct lines; and re-running after the human renumbers SEQUENCE must produce identical output.

## [later] Porter (PM) — DEF-13 CANCELLED by stakeholder. Won't fix.

Human's decision, after I laid out the misalignment and the fix:
> *"ไม่มีแก้อะไรละ ช่างมันไป"*

**DEF-13 → CANCELLED. @Sober: do not spec it, do not start it.** No code change to the tick binding.

Two corrections of mine that stand regardless, recorded so the analysis isn't lost:

1. **I was wrong about `CHECKLIST_CODE`.** I told the team not to bind on it because it embeds
   DOCUMENT_ID + SEQUENCE. The human corrected me: the code is assigned at creation and not
   recomputed, so it **is** stable across a SEQUENCE renumber. If this is ever revisited,
   `CHECKLIST_CODE` is a valid key — my objection was wrong.
2. **The master data is correct.** I checked `ReqMove`'s 15 rows against the official อ.9 transport
   form: SEQ order maps 1:1 onto the form's evidence items (1, 2, 5(1)(2)(3), 6, 7, 8, 9, 10, 11,
   and the three item-12 lines). Nothing is wrong on the data side — the misalignment was ours alone.

**Accepted consequence, stated once and left alone:** อ.9 evidence ticks land on the wrong lines from
the 4th item onward, and ส.ค.4 / แผนการใช้กระสุน / ภาพถ่ายสนามยิงปืน cannot tick. This is the
current, deliberate steady state. It is on the board so that if someone reports "ติ๊กผิดช่อง" later,
we recognise it immediately instead of re-investigating from scratch.

## [later] Porter (PM) — TEST-004 assigned to Tanya (QA): full-family regression, form-truth first

Human: *"ให้ tanya ทดสอบสิ"* + *"เอาให้ตรงกับตัวอย่าง"*.

Written to `tests/TEST-004-full-family-regression.md`. Six cases — อ.15 (18041), อ.14 (27300), อ.6
(38272), **อ.7**, อ.9 transport, อ.9 destroy — each via the real `/download` **and** the `/db` seam.

**Framed so the main check is form-truth, not smoke-testing.** Routing + "no 500" is the floor;
the actual job is opening the official PDF beside each render and comparing heading, item order,
verbatim labels, (1)/(2) rows, page count and annex columns. Rule stated explicitly: **if our text
differs from the form, the form wins — report it even if ours reads better.** That is the DEF-6 lesson
turned into a QA instruction.

Two things I deliberately put in writing so QA doesn't burn a cycle on them:

- **อ.7 is the highest-risk case** and I said so. Sober could not prove from code that an อ.7 licence
  still resolves to the A6 report after the collapse. It is the one case where a real render is the
  only evidence available.
- **A "do NOT raise" list** covering the five known-and-accepted behaviours: อ.14 ticks all blank
  (no attachments exist anywhere), DEF-13 mis-aligned อ.9 ticks (stakeholder-cancelled), 18847's
  item-7 change (intended, DEF-10), the view's duplicated word / trailing `-` (view owners' fix), and
  empty รายการ on requests with no detail rows. Without that list QA would correctly-but-uselessly
  file five bugs we already decided about.

Dependency flagged in the test file: the service must be rebuilt (`clean compile`) and restarted with
the `dev` profile first, or she is testing stale code — the BUILD FACT lesson.

## [later] Sober (SA)
- **REQ-030 (CHECKLIST_CODE tick-binding + DEF-14) — wrote SPEC-032 + TASK-029.** Confirmed root cause in code:
  base `mid = master.get(i).getId()` is pure positional over the IS_ACTIVE-filtered master → the 22-row rebuild
  shifts every tick. `RequestCheckListEntity` doesn't even map CHECKLIST_CODE. Dict-checked (SPEC-027): CHECKLIST_CODE
  + all DEF-14 buyer cols (ASSOC_PRES_NAME_*, 4 *_ATT_FILE_ID) EXIST → map them.
- **Part A (clear, actionable now):** add CHECKLIST_CODE; replace `mid` with an `idByCode` map; rebind items 1-11
  + ส.ค.4/แผนกระสุน/ภาพถ่าย by the REQ-030 suffix table (prefix=checklistGroup() → one path serves ReqMove/
  ReqSaleDom/ReqMoveDestroyer). Print order LOCKED.
- **DEF-14:** wire ชื่อนายกสมาคม from ASSOC_PRES_NAME_* (owning my TASK-019 "no column" miss — the cols exist);
  resolved the :271/:278 duplicate = **documented, not invented** (table has ONE president/grantor card set; นายกสมาคม
  IS the ผู้มอบอำนาจ, so 00016 & 00018 legitimately share ASSOC_PRES_ID_CARD_*).
- **2 genuine confirms → @Porter (didn't guess):** (1) **item-12 buyer-line tick source** — REQ-030 gives these
  lines CHECKLIST_CODEs but DEF-14 says tick from BUYER *_ATT_FILE_ID; proposed CODE-for-identity + ATT_FILE_ID-for-
  tick-value (consistent with REQ-030 §4 "tick from their own tables") — confirm. (2) **scope** — items 1-11 fix
  destroy+transport+a15 via the shared base; include a14 (its ReqSaleInt codes); leave destroy item-12 positional
  (codes not in REQ-030, DEF-13-destroy cancelled) unless you supply them; a6 check. OK?
- @Jason: **TASK-029 Part A + DEF-14 name/duplicate actionable now**; item-12 buyer-ticks + a14/destroy scope after
  Porter's confirms. @Porter: 2 confirms above gate the item-12/scope parts. Board: REQ-030 → SPEC_DONE.

## [later] Sober (SA)
- Reviewed **TASK-029 Part A + DEF-14 name/dup — SA-VERIFIED.** CHECKLIST_CODE mapped; positional `mid`→
  `idFor(suffix)` for all items 1-11 (REQ-030 suffixes); ชื่อนายกสมาคม from ASSOC_PRES_NAME_*; 00016/00018
  documented dup; compile + 4 previews green; print order locked. Fixes transport+a15 items-1-11 tick landing.
- **Flagged (non-blocking → Porter/QA):** rendered item-5 4th line = "เปิดดำเนินการผลิตอาวุธ" (unticked, no code)
  vs REQ-030 table's "สลักหลัง อ.2/12305" (inactive anyway) — possible LABEL discrepancy vs the official form;
  confirm at QA; outside REQ-030's tick-binding scope.
- **Gated rest still pending Porter's 2 confirms** (#1 buyer-doc ticks → BUYER *_ATT_FILE_ID; #2 a14/destroy/a6
  scope) — then Jason finishes + back to me. Tick-landing = QA real-data.
- @Porter: Part A verified; the 2 confirms unblock the item-12/scope remainder. Ball → Porter (confirms + QA); Jason (rest after).

---
## ⏩ CONTINUED IN `log/2026-08-21.md`
This file is CLOSED. Everything after Jason's TASK-029 Part A — including the stakeholder's
**TICK RULE** (which rewrites REQ-030's specification) and Porter's consolidated instruction —
is in **`log/2026-08-21.md`**. Read that file to the end before acting.
