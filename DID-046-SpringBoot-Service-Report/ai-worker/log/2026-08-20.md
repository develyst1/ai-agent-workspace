# Log — 2026-08-20 (Jason / BE)

## TASK-024 — split อ.9 into destroy/transport (shared base), EXECUTED → REVIEW

Behaviour-preserving refactor of the DELIVERED 444-line `A9CheckListReportBuilder`. Done in one pass
(a half-done extract-superclass = broken build, so it had to land whole).

**Result:**
- `A9CheckListReportBuilderBase` (abstract) holds every shared method ONCE + 6 abstract hooks
  (documentTitle / item5Label / item5Value(move) / checklistGroup / item12Title / buildItem12).
- `A9DestroyReportBuilder` (group `ReqMoveDestroyer`, DEST_PLACE_NAME, 9 destroy subs) +
  `A9TransportReportBuilder` (group `ReqMove`, AUTHORITY_NAME, transport export set). No `boolean destroy` flag.
- `ReportDefinition` +A9D/+A9T; `resolveFromMove` → `MOVE_REQUEST_TYPE==2 ? "A9D" : "A9T"`;
  DocumentController download+history cases A9D/A9T; PreviewController `/a9/db` picks variant via resolver.
  Deleted old `A9CheckListReportBuilder` + `A9CheckListHistoryReportBuilder`.

**Two deviations from the locked plan (flagged for Sober):**
1. Kept single `exportPdfA9` — both variants share templates + the `A9CheckListReportData` record, so
   `exportPdfA9Destroy/Transport` would be byte-identical duplicates. `ReportResourceService` untouched.
2. Removed the history-builder class (was a 1-line delegate); history cases now call the variant builder's
   `createData` directly (same behaviour). Will restore named seams if Sober prefers symmetry with a14.

**Verify (DB-free):** `test-compile` SUCCESS; boot :33091 Started clean (all beans wire); preview re-render
a9=4pg / a9-transport=5pg / a6=3pg / a14=4pg, **0 literal "null"**. Preview path (mock builder + templates)
is untouched by the split → content identical.

**⚠️ Finding — the pinned md5 baseline is a broken oracle.** All four preview md5s drifted this run,
**including a6 and a14 which TASK-024 never touches**. Cause: `A9PreviewTest` recompiles the `.jasper`
each run and JasperReports embeds non-reproducible bytes, so md5 differs run-to-run with zero real change.
The a6/a14 control proves it. Verified content-equivalence (page count + no-"null" + structure) instead.
Recommend future byte-identical DoDs use a PyMuPDF text diff, not md5-of-freshly-compiled-PDF.

**Left for QA (Tanya, real DB — BE rule #4):** diff real destroy + transport pre/post split via
`/download/checklist/{encId}` + `/a9/db` (38336 transport, 37956 item-12, a destroy sample); confirm
both route through `resolveFromMove` and อ.6/อ.14 are unaffected. Every label/source was moved verbatim,
so no field value should change.

→ TASK-024 REVIEW, back to Sober. Board + task file updated.

## TASK-025 — split the อ.9 template folder per-form, EXECUTED → REVIEW

Sober reviewed TASK-024 → DONE and queued TASK-025 (resource-only template split; base untouched). Done.

**Result:** `request-a9/` → `request-a9-destroy/` + `request-a9-transport/` (verbatim copies; a9 subreports
load via `$P{SUB_*}` params, so **no path refs to fix**). Old `request-a9/` **deleted** (asked the human first —
the auto-guard blocked the pre-existing-tree delete; approved). `ReportResourceService` A9 openers now take a
`folder` arg; `JasperPdfReportService` → `exportPdfA9Destroy`/`exportPdfA9Transport` over a private
`exportPdfA9(data, folder)`; DocumentController (4 cases) + PreviewController (seam + mock) repointed;
`A9PreviewTest` compiles+renders each folder against its own templates. **Java base + variant builders UNTOUCHED.**

**⚠️ DoD wording note (Sober):** DoD-2 says "only ReportResourceService.java changed", but per-folder routing
also needs the JasperPdfReportService split + the two controllers — which step-2 itself calls for. The real
intent (base + builders untouched) is met; the literal "only ReportResourceService" line is unachievable since
the shared record carries no variant token. Flagged, not worked around.

**Verify (DB-free):** clean test-compile SUCCESS; boot :33092 Started clean; A9PreviewTest destroy=4pg /
transport=5pg each from its OWN folder; PyMuPDF content-equivalent (chars 7805/7606 — identical to the TASK-024
run), 0 "null"; shared folder gone, both new folders have `.jasper`; a6/a14 untouched. Real byte-identical = QA.

→ TASK-025 REVIEW, back to Sober. This unblocks REQ-027/TASK-026 (อ.15 clones `request-a9-transport/`).
Board + task file updated.

## TASK-026 — อ.15 checklist (clone of อ.9 transport), EXECUTED → REVIEW

Sober reviewed TASK-025 → DONE and unblocked TASK-026. Done.

**Result:** `A15ReportBuilder` extends `A9CheckListReportBuilderBase`; documentTitle=A15 (= transport heading),
group=`ReqSaleDom`, item-12 = transport set, item-5 = first `VW_REQUEST_DTL` row's `AUTHORITY_NAME` (a15 has no
MOVE row → move=null flows harmlessly). Shell: `ReportDefinition.A15`, `request-a15/` (clone of
request-a9-transport), `exportPdfA15`, DocumentController A15 download+history, PreviewController `/a15/db`,
`A15PreviewTest`. Routing: `resolveFromSaleDom` row-presence → "A15"; `RequestSaleDomEntity` (no STATUS, DEF-11) + repo.

**Base changes (output-preserving):** item-2 → abstract `permitTypeLabel(move)` hook (a9 destroy/transport return
`moveRequestTypeLabel(move)` = byte-identical); `item5Value` hook now `(requestId, move)`. **Moved
`buildTransportItem12` back into the base** as `protected` so a9-transport + a15 share it in one place — partially
reverses TASK-024's placement but output-preserving; flagged to Sober (Q2).

**DEF-11 guard held:** before mapping `AUTHORITY_NAME` on the SHARED `RequestDtlViewEntity` (blast radius = every
report's annex query), I verified the column is in the filed VW_REQUEST_DTL SELECT — REQ-025 line 95 `DTL.AUTHORITY_NAME`. Real column, cited.

**Verify (DB-free):** test-compile + clean compile SUCCESS; boot :33093 Started clean (3 distinct builders, no
NoUniqueBean); A9PreviewTest destroy 7805 / transport 7606 chars = **identical to pre-change** (item-2 hook + item-12
relocation moved nothing); A15PreviewTest 5pg, 0 "null". Real a15 correctness = QA's leg.

**⚠️ Open (Sober):** Q1 — item-2 exact wording was never given in SPEC-029 (§62 says "verbatim from the form,
Porter-confirmed" but no string). Did NOT guess: a15 item-2 = blank (graceful) until Sober pastes the text (1-line
const). Q2 — approve keeping `buildTransportItem12` in the base.

→ TASK-026 REVIEW, back to Sober. Board + task file updated.

## TASK-026 follow-up — a15 item-2 constant SET

Sober reviewed TASK-026 → DONE (Q2 buildTransportItem12-in-base APPROVED; a9 byte-identical confirmed
independently). Q1 (item-2 wording) went up to the human via Porter; human confirmed the verbatim string.
Set `A15ReportBuilder.ITEM2_PERMIT_TYPE = "ขายและขนย้ายให้บุคคลอื่นนอกหน่วยงานตามมาตรา 7"` (was blank interim).
NB it reads **"นอกหน่วยงาน"** — NOT อ.14's "นอกจากหน่วยงาน" and NOT `moveRequestTypeLabel` case-1's "นอกจาก…";
human flagged the difference and said do NOT align. Comment in code records this. test-compile green.
(Previews unaffected — the const only shows on the real /a15/db path; DB render + item-5 correctness = QA's leg.)

## TASK-028 — DEF-12: a15 item-5 = child อ.9 MOVE.AUTHORITY_NAME (fix the `-`), EXECUTED → REVIEW

QA/Porter confirmed on real data (10/10) that my SPEC-029 item-5 source was wrong: `VW_REQUEST_DTL.AUTHORITY_NAME`
is the placeholder `-` for a15's own rows; the buyer org lives on the **child อ.9** request's MOVE, reachable via
`T_T_REQUEST.REF_REQUEST_ID`. My premise "a15 has no MOVE" was true of the *row*, false of the *family*. Fixed per SPEC-031:
- `RequestRepository.findByRefRequestIdAndRequestType(Long, Integer)` (derived List, 11.2-safe; boot-validated).
- `A15ReportBuilder.item5Value` → child อ.9 (REF_REQUEST_ID=id, REQUEST_TYPE=3) → `MOVE.AUTHORITY_NAME`; no child ⇒
  blank (57/483 have none), never `-`, never "null". Same field a9-transport prints, via the shared move repo.
- Removed the dead `RequestDtlViewEntity.AUTHORITY_NAME` mapping (0 refs after repoint).

**Verify (DB-free):** test-compile + clean compile + boot :33094 all green (derived finder resolved at startup);
all PreviewTests pass; **a9-transport item-5 unchanged (7606)**; a15 no "null"/no bare `-`. Real 18041 →
สมาคมกีฬานักยิงปืนสมัครเล่นภูเก็ต = QA's leg. SALE_DOM stays data-free ⇒ REQ-028's routing-only note holds.

→ TASK-028 REVIEW, back to Sober. Board + task file updated. (This owns/corrects my own SPEC-029 mistake.)

## TASK-027 — collapse the checklist resolver to ONE REQUEST_TYPE switch, EXECUTED → REVIEW

Sober closed TASK-028 → DONE, which unblocked TASK-027 (gate had passed: Porter real-data 1:1 map). Done.

**Result:** `resolveChecklistRequestType(Long)` is now a single `switch(REQUEST_TYPE)`
(`0→CHECKPERSON, 2→PlantEst, 3→resolveA9, 4→A4, 5→A15, 6→A14, 8→A6, 50-53→OPEN/EXPAND/PLANTCHANGE/PERSONCHANGE,
default→throw`). Only sub-lookups left: `resolveFromPlantEst` (อ.1/3) + new `resolveA9` (MOVE_REQUEST_TYPE==2?
A9D:A9T; no MOVE row → A9T, safe default). Deleted the 4 probe legs + 3 unused repo fields + dead FORM_ID_A6/A7
consts + imports; deleted `RequestSaleDomEntity`/repo (routing-only, 0 refs). Kept RequestSaleInt (a14) + RequestMove.

**Verify (DB-free):** test-compile + clean compile + boot :33095 all green (resolver DI now 5 deps); all 5
PreviewTests render. Routing correctness is a DB path → **QA per-family on real data is the DoD gate** (esp. อ.6
AND อ.7 → A6 — I can't prove อ.7=type-8 from code; + อ.9 d/t, อ.14, อ.15, one legacy). Any mis-route = revert.

**⚠️ Flagged to Sober:** removing `resolveFromSpecial` orphaned `RequestSpecialEntity`/repo too (0 refs). SPEC-030
only named RequestSaleDom for deletion, so I LEFT RequestSpecial in place (CLAUDE.md cites T_T_REQUEST_SPECIAL as
the อ.6 record — may be wanted for future a6 DB). Asked Sober: keep or delete.

→ TASK-027 REVIEW, back to Sober. Board + task file updated.

## TASK-027 follow-up — RequestSpecial deletion blocked by harness

Sober reviewed the collapse → correct+verified, and answered my flag: **DELETE `RequestSpecial` (entity+repo)**
(0 refs, orphaned). I acked but **could not perform it** — the local harness guard blocks BE deletion of
**pre-existing** source files without explicit human consent (RequestSpecial* predate this session; RequestSaleDom
I created this session, so that delete went through). I did NOT work around it, and per the chain I don't ask the
human directly. The 2 files are fully dead (0 refs, compiles, boots) → no runtime impact. Routed the decision up
(Sober → Porter → human): human deletes them / grants a Bash permission, or accept the harmless dead files. The
resolver collapse itself is code-DONE and does not depend on this cleanup.
