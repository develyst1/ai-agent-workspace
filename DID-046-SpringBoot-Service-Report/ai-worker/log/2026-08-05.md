# Log — 2026-08-05 — DID-046-SpringBoot-Service-Report

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Porter (PM)
- Set up the AI-team desk for a new project: DID-046-SpringBoot-Service-Report
  (copied from `_templates/project`).
- Filled `board.md` project info: description, real code repo path
  (`C:\Users\Admin\sa-project\service-report2\DID-046-SpringBoot-Service-Report`),
  tech stack (Java 21 / Spring Boot 3.5 / Oracle / JasperReports 7.0.4), run cmd.
- Team: Porter (PM) · Sober (SA Lead) · Jason (BE). Backend-only service, no FE.
- No requirements yet — waiting for the human's first requirement.

## [--:--] Porter (PM)
- Received first requirement from stakeholder: investigate & verify the อ.6 (a6)
  checklist PDF report — (1) explain how it works, (2) verify output is correct,
  (3) suspected bug/defect to find.
- Created REQ-001-verify-a6-checklist-report.md, status READY_FOR_SA, Priority HIGH.
- Open DATA REQUEST: stakeholder will provide a sample อ.6 requestId/data (into
  project-docs/). AC #1 (flow docs) can start now; AC #2/#3 (verification) blocked
  until the sample lands.
- @Sober: please pick up REQ-001. You can begin documenting the อ.6 flow from the
  code now; verification waits on the sample I'll drop into project-docs/.

## [--:--] Porter (PM)
- DATA REQUEST closed: human provided 34 plain-integer REQUEST_IDs for อ.6 →
  saved to project-docs/REQ-001-a6-sample-request-ids.md. Answered the Question in
  REQ-001 and cleared the Blocked row on the board.
- Heads-up flagged in REQ-001: the ids are plain (not encrypted) — if the อ.6
  endpoint expects an encrypted requestId, encrypt via CryptoService first.
- @Sober: REQ-001 is now fully unblocked (AC #1, #2, #3). Please confirm the ids
  all resolve to อ.6 and flag any that don't.

## [--:--] Porter (PM)
- Team change (human decision): added **Tanya (Senior Tester / QA)** to this
  project — trial role, hangs off PM (Human ↔ PM ↔ Tanya).
- Provisioned Tanya's desk: created ai-worker/QA.md and ai-worker/tests/; wired
  Tanya into PROTOCOL.md (team table, hard-chain, REQ statuses
  IN_TEST/TEST_PASSED/TEST_FAILED, status ownership, and a new "Tester's
  environment" section). Updated board team line and workspace README.
- QA environment constraint from human: local + a dev/local instance wired to the
  **UAT** DB; Tanya tests **READ-ONLY** (GET/read + report generation only) —
  no create/update/delete, no writing SQL, never production. Encoded as a hard
  rule in QA.md and PROTOCOL.md.
- REQ-001: added a "QA / test routing" section — Tanya owns the run-and-verify
  legs (AC #2/#3) after Sober documents how the อ.6 report is invoked (endpoint +
  whether requestId must be encrypted). REQ-001 stays with Sober for now; QA picks
  up once the invocation details are known.
- Note: Tanya has not been given the UAT URL / test X-API-KEY yet — will collect
  from the human and drop into project-docs/ when QA is ready to run.

## [--:--] Sober (SA Lead)
- Picked up REQ-001 (READY_FOR_SA → worked → SPEC_DONE). Read the real อ.6 code
  path (DocumentController, RequestTypeResolverService, ReportDefinition,
  A6CheckListReportBuilder, JasperPdfReportService, ReportResourceService).
- Wrote specs/SPEC-001-a6-checklist-report-flow-and-verification.md:
  - AC#1: full end-to-end flow (endpoint → resolve via T_T_REQUEST_SPECIAL.FORM_ID
    6/7 → build from DIDPERMIT → Jasper fill/export).
  - Invocation details QA needs: the 34 sample ids are PLAIN but the endpoint
    expects an ENCRYPTED requestId → must encrypt first (or use createDataRaw seam).
  - AC#3: 6 ranked candidate-defect leads (D1 person expiry source, D2 history
    path decrypts a form-id as a request-id, D3 permitDuration source, D4 signature
    cell order, D5 tick rule, D6 stale Swagger doc) — each with a code location for
    QA to check. All are suspects (no DB access), to be confirmed at runtime.
- Board: REQ-001 → SPEC_DONE; added a waiting row for the QA-run prerequisites.
- @Porter: REQ-001 SA leg done — AC#1 delivered in SPEC-001. To start AC#2/#3,
  Tanya needs three things from the human (please collect into project-docs/):
  (1) the 34 ids ENCRYPTED via CryptoService, (2) the UAT-wired base URL,
  (3) a test X-API-KEY. Optional: run SQL Q1 in SPEC-001 to confirm all 34 are
  FORM_ID 6/7 and to settle Q2 (item-7 duration source: TOTAL_DAYS vs
  OPERATION_PERIOD). Then route AC#2/#3 to Tanya (IN_TEST).

## [--:--] Porter (PM)
- Received Sober's SPEC-001 (AC#1 delivered; 6 candidate-defect leads D1–D6).
  Relayed the substance to the human in Thai and put the DATA REQUESTs to them:
  (1) the 34 ids in ENCRYPTED form (or from real UAT download URLs), (2) UAT base
  URL, (3) test X-API-KEY; plus optional Q1 SQL to run.
- Asked the human the Q2 business decision (item 7 "ระยะเวลาการอนุญาต" source:
  TOTAL_DAYS vs OPERATION_PERIOD). Will answer SPEC-001 Q2 once the human decides.
- Waiting on the human before routing AC#2/#3 to Tanya (IN_TEST).

## [--:--] Porter (PM)
- Q2 answered by human: item 7 source is **T_T_LICENSE.PERIOD_TEXT** (NOT
  TOTAL_DAYS, NOT OPERATION_PERIOD). Recorded in SPEC-001 §Questions. This
  **confirms D3 is a real defect** (code uses EMPLOYER.TOTAL_DAYS + " วัน"; the
  hardcoded " วัน" suffix is likely wrong too since PERIOD_TEXT is already text).
- Fix is out of scope for REQ-001 (investigate+verify). Porter will raise a
  follow-up fix REQ once QA confirms the printed value against the sample output.
- Still waiting on the 3 data items (encrypted ids + UAT URL + test API key)
  before routing AC#2/#3 to Tanya.

## [--:--] Porter (PM)
- New requirement from human: expose `GET /api/v1/preview/checklist/a6?disposition=inline`
  (currently disabled/missing) mirroring the working a9 preview — full อ.6 layout,
  sample data, **no auth / no X-API-KEY** — to serve as the correctness baseline.
- Created REQ-002-add-a6-preview-endpoint.md, status READY_FOR_SA, Priority HIGH.
  Captured the human's verbatim Thai as intent evidence.
- Relationship: REQ-002 preview is the reference REQ-001 AC#2/#3 compares against;
  REQ-001 verification is best done after REQ-002 lands.
- @Sober: please pick up REQ-002 — check whether an a6 preview once existed (now
  disabled/removed) and restore it, or build it mirroring the a9 preview path.
  Note the human's ask to keep it no-auth like a9.

## [--:--] Sober (SA Lead)
- Picked up REQ-002 (READY_FOR_SA → IN_SPEC). Traced the preview code + git
  history: the mock GET /api/v1/preview/checklist/a6 was removed in commit b0d9a84
  (a6 DB integration), which swapped it for /checklist/a6/db/{requestId} and
  deleted a6's buildMock. The a9 mock preview (/checklist/a9) still works and is
  the pattern to mirror. SecurityConfig already permitAll on /api/v1/preview/**
  → no security change needed. A6CheckListReportData record shape is unchanged
  since the mock existed (verified by diff), so the old buildMock drops back in.
- Wrote specs/SPEC-002-a6-preview-endpoint.md and tasks/TASK-001-restore-a6-mock-preview.md.
  Approach: restore the mock as a separate self-contained A6CheckListPreviewBuilder
  (keep the DB builder untouched) + add previewA6() mirroring previewA9().
- Board: REQ-002 → IN_SPEC; TASK-001 → TODO (Jason).
- @Jason: TASK-001 is ready — restore the a6 mock preview. The exact source of the
  mock data is `git show b0d9a84~1:.../a6/builder/A6CheckListReportBuilder.java`
  (its buildMock body). Do NOT touch A6CheckListReportBuilder (DB) or SecurityConfig.
  DoD includes compile + a manual no-auth GET check. Ping me for REVIEW when done.

## [11:28] Jason (BE)
- Picked up TASK-001 (TODO → IN_PROGRESS → REVIEW).
- Restored the a6 mock preview as a separate self-contained provider
  (new `A6CheckListPreviewBuilder`, mock lifted verbatim from `b0d9a84~1`'s
  `buildMock`) + added `previewA6()` in `PreviewController` mirroring `previewA9()`.
  Did NOT touch `A6CheckListReportBuilder` (DB) or `SecurityConfig` (verified: git diff empty).
- Evidence: `./mvnw -DskipTests compile` → BUILD SUCCESS. Booted my build on :33099
  (`:33000` was already in use by a pre-existing instance); no-auth GET
  `/api/v1/preview/checklist/a6?disposition=inline` → HTTP 200 application/pdf,
  272,739 bytes, filename a6-preview.pdf, valid 3-page อ.6 PDF. a9 parity: 200 pdf.
- Full evidence + build/run output in TASK-001 `## Implementation Notes`.
- @Sober: TASK-001 ready for review.

## [--:--] Sober (SA Lead)
- Reviewed TASK-001 (REVIEW → DONE). Verified independently, not on trust:
  git diff = only PreviewController (+23) + new A6CheckListPreviewBuilder; DB
  builder A6CheckListReportBuilder and SecurityConfig = empty diff (untouched);
  previewA6() mirrors previewA9() at /checklist/a6; constructor passes all 8
  record fields in order; re-ran `./mvnw -o -DskipTests compile` myself → exit 0.
  Accepted Jason's runtime evidence (no-auth GET → 200 application/pdf, 3-page
  full อ.6 layout). Verdict + evidence written in TASK-001 §Review.
- All tasks of SPEC-002 are DONE → REQ-002 set to SPEC_DONE.
- @Porter: REQ-002 is ready for your acceptance check. The no-auth a6 preview is
  restored — GET /document-service/api/v1/preview/checklist/a6?disposition=inline
  now renders the full อ.6 layout (mock data), parity with the a9 preview. This
  also unblocks REQ-001's correctness baseline: Tanya can compare the real
  download/checklist/{requestId} output against this a6 preview. (Reminder: REQ-001
  AC#2/#3 still waits on the 3 data items — encrypted ids + UAT URL + test API key.)

## [--:--] Porter (PM)
- Accepted Sober's handoff on REQ-002 (SPEC_DONE, TASK-001 DONE). Per our status
  flow a build needs an independent QA run before DELIVERED — routing REQ-002 to
  Tanya for IN_TEST. This is her first real test on this project and has **zero
  dependency on the human's data** (no auth, no requestId, no DB write).
- @Tanya: please pick up REQ-002 and run it read-only on **local**.
  - Start the service locally (`./mvnw spring-boot:run`; note :33000 may already be
    in use — Jason ran his build on :33099, use another port if needed).
  - GET (no auth / no X-API-KEY) `/document-service/api/v1/preview/checklist/a6?disposition=inline`
    → expect HTTP 200, `application/pdf`, the full อ.6 layout (~3 pages, mock data).
  - Confirm parity with `/preview/checklist/a9` (also no-auth 200 pdf).
  - Write tests/TEST-001, save the produced PDF into ../project-docs/ as evidence,
    and give a verdict (TEST_PASSED / TEST_FAILED / NOT_TESTED). Report to @Porter.
  - Bonus (optional, no data needed yet): from this a6 preview, start drafting the
    REQ-001 test cases (the field/section checklist you'll later compare the real
    download output against). REQ-001 AC#2/#3 still waits on 3 data items from the
    human — I'll deliver them when ready.

## [11:40] Tanya (QA)
- Picked up REQ-002 (SPEC_DONE → IN_TEST → **TEST_PASSED**). My first test on this project.
- Ran it **read-only on local**, no human data needed. Built the current working tree
  (Jason's change still uncommitted) → compile exit 0; booted my OWN instance on :33001
  (a pre-existing instance holds :33000 — did NOT rely on it); stopped it after; :33000 untouched.
- All 3 REQ-002 AC verified with **no X-API-KEY / no bearer**:
  - AC#1: GET /api/v1/preview/checklist/a6?disposition=inline → **HTTP 200 application/pdf**,
    272,739 B, valid %PDF-1.5…%%EOF, filename a6-preview.pdf.
  - AC#2: text-extracted the PDF → **complete อ.6 layout, 3 pages** — front items 1–8, legal
    refs, back checklist items 1–8 + 4 signature blocks, annex table (9 rows). ✅
  - AC#3: a9 parity → GET /checklist/a9 → 200 application/pdf; same response shape (disposition
    handling, no-auth). Also verified disposition=attachment and default(no-param) edge cases.
- Wrote **tests/TEST-001-a6-preview-endpoint.md** (6 cases, all PASS) + started **tests/REGRESSION.md**
  (R1–R4 green; R5–R6 blocked on REQ-001 data). Evidence PDFs + headers + extracted text saved to
  `../project-docs/REQ-002-evidence/`.
- Board: REQ-002 → **TEST_PASSED** (owner of next step = Porter → DELIVERED).
- @Porter: REQ-002 passes — the no-auth a6 preview is a valid correctness baseline for REQ-001.
  Three FYI notes in TEST-001 §Notes (not REQ-002 defects): (1) mock item-7 prints a hardcoded
  "180 วัน" — a reminder of the D3 lead, to be confirmed under REQ-001 against real data;
  (2) placeholder filler (sss/กกก) is expected mock; (3) visual fidelity checked via structure+text,
  saved PDFs available for a human eyeball if you want one. REQ-001 AC#2/#3 still blocked on the 3
  data items (encrypted ids + UAT URL + test X-API-KEY) — standing by for your routing.

## [--:--] Porter (PM)
- Accepted Tanya's TEST_PASSED on REQ-002 and set it **DELIVERED**. Independent
  local QA (own :33001 instance, 6/6 cases pass, evidence in
  project-docs/REQ-002-evidence/) meets all 3 AC. The no-auth a6 preview is now the
  agreed correctness baseline for REQ-001.
- Caveat recorded on the board: the change is **verified local + uncommitted** in
  Jason's working tree — committing/deploying to UAT is a separate step if the
  human wants it permanent (raise as a follow-up if so).
- Noted Tanya's FYI: mock item-7 prints hardcoded "180 วัน" — consistent with the
  D3 lead (real item-7 source = T_T_LICENSE.PERIOD_TEXT); to be confirmed under
  REQ-001 against real data.
- Reported REQ-002 delivery to the human in Thai. Re-focused the human on the
  REQ-001 blocker: the 3 data items (encrypted ids + UAT URL + test X-API-KEY).

## [--:--] Porter (PM)
- Human answered the 3 data items, reshaping the approach:
  1. Encryption: does NOT want to hand-encrypt → team should encrypt, or add a
     no-encryption input path. → raised as REQ-003.
  2. URL: use `http://localhost:33000/document-service` — human confirms this
     instance is **wired to the UAT DB**. (UAT-URL blocker resolved.)
  3. X-API-KEY: unsure if required ("worked without it before") → REQ-003 resolves.
- Created REQ-003-run-a6-from-plain-request-ids.md, READY_FOR_SA, HIGH. SA Lead
  picks the approach (encrypt-helper vs plain-id seam); PM constraint recorded:
  do NOT weaken the real production endpoint's security — prefer a test/dev-only
  seam. REQ-003 unblocks REQ-001 AC#2/#3.
- New human directive for QA: when Tanya tests, she must **also test with
  Playwright** (drive endpoints in a real browser, verify rendered อ.6) for
  correctness vs the REQ-002 preview. Recorded in REQ-001 §QA/test routing.
- @Sober: please pick up REQ-003. Note the security constraint (don't make the
  live download endpoint accept unencrypted ids) and that localhost:33000 is
  UAT-wired. Once REQ-003 lands, I'll route REQ-001 AC#2/#3 to Tanya.

## [--:--] Sober (SA Lead)
- Picked up REQ-003 (READY_FOR_SA → SPEC_DONE). Chose approach (b): reuse the
  EXISTING dev-only seam — GET /api/v1/preview/checklist/a6/db/{requestId} (raw
  plain Long id, no auth) → A6CheckListReportBuilder.createDataRaw → same
  buildFromDb as the real endpoint = identical PDF body. ZERO code change; does
  NOT weaken the production /api/v1/download endpoint (separate permitAll preview
  chain). Wrote specs/SPEC-003-run-a6-from-plain-request-ids.md.
- Auth answer (from SecurityConfig + ApiKeyFilter): real /api/v1/download REQUIRES
  auth (valid X-API-KEY OR bearer); preview /api/v1/preview/** is permitAll (no
  key). ⇒ the plain-id seam needs NO X-API-KEY (REQ-003 Q3 resolved).
- Did NOT hit the UAT-wired :33000 myself (SA never touches real environments);
  analysis is code-only. Flagged one caveat: the /db seam bypasses the FORM_ID
  resolver, so it won't prove an id "is อ.6" — QA treats a clean อ.6 render as
  sufficient / flags any error.
- @Porter: REQ-003 done — no code needed. Relay the QA recipe in SPEC-003 to
  Tanya; this simultaneously UNBLOCKS REQ-001 AC#2/#3 (seam URL on localhost:33000,
  no key, Playwright-friendly). Also raised a SECURITY FOLLOW-UP (out of REQ-003
  scope, your call to REQ it): /api/v1/preview/** is unauthenticated and the
  /a6/db/{id} seam reads real UAT data by raw id — the preview controller is
  self-marked TEMPORARY and should be removed / dev-profile-gated before any prod
  deploy.

## [--:--] Porter (PM)
- Accepted REQ-003 (SPEC-003): no code needed — reuse existing dev-only seam
  GET /api/v1/preview/checklist/a6/db/{plainId} (raw plain id, no auth, permitAll
  preview chain, identical PDF body to the real endpoint). Q3 resolved: seam needs
  NO X-API-KEY. Set REQ-003 → DELIVERED (final proof-in-use comes with REQ-001 run).
- Routed REQ-001 AC#2/#3 to Tanya (SPEC_DONE → she takes IN_TEST).
- @Tanya: run REQ-001 verification, read-only, on localhost:33000 (UAT-wired).
  - Recipe (from SPEC-003): for each of the 34 plain ids in
    project-docs/REQ-001-a6-sample-request-ids.md, GET (no key)
    `http://localhost:33000/document-service/api/v1/preview/checklist/a6/db/{plainId}`
    → an อ.6 PDF built from real UAT data.
  - Compare each against the REQ-002 preview baseline (tests/TEST-001 layout).
  - Check the D1–D5 leads from SPEC-001 §AC#3, especially **D3** (item 7
    "ระยะเวลาการอนุญาต" — real source must be `T_T_LICENSE.PERIOD_TEXT`, code uses
    TOTAL_DAYS + " วัน"; confirm what actually prints).
  - **Also test with Playwright** (human directive): drive the seam/preview URLs in
    a real browser and verify the rendered อ.6 output.
  - Caveat (SPEC-003): the /db seam bypasses the FORM_ID resolver, so it can't prove
    an id "is อ.6" — treat a clean อ.6 render as sufficient; flag any id that errors.
  - Write tests/TEST-002 (REQ-001), save evidence PDFs/screenshots to
    ../project-docs/, and return a TEST_PASSED / TEST_FAILED (with defects) to @Porter.
- SECURITY follow-up (Sober flagged): /api/v1/preview/** is unauthenticated and the
  /a6/db/{id} seam reads REAL UAT data by raw id (preview controller self-marked
  TEMPORARY). Surfacing to the human as a decision — raise a hardening REQ (remove /
  dev-profile-gate before prod) now or defer. Tracked on the board Blocked/waiting.

## [--:--] Porter (PM)
- Human decision: raise the security hardening REQ now. Created
  REQ-004-secure-preview-db-seam-before-prod.md, READY_FOR_SA, HIGH (must be fixed
  before any prod deploy; does NOT block current QA — seam stays in dev/test).
- @Sober: please pick up REQ-004 — remove / dev-profile-gate / auth-gate the
  unauthenticated real-data preview db seam(s) so anonymous access to real report
  data can't reach production, without breaking the REQ-001 QA workflow.
- Two REQs now in flight for the team: REQ-001 (Tanya, QA run in progress) and
  REQ-004 (Sober). REQ-002 & REQ-003 delivered.

## [12:05] Tanya (QA)
- Picked up REQ-001 AC#2/#3 (SPEC_DONE → **IN_TEST**). Ran the SPEC-003 seam
  **read-only** on the UAT-wired :33000: GET (no key) `/api/v1/preview/checklist/a6/db/{plainId}`.
- **Sample only: 3 of 34 ids** (38240, 38272, 38273) — human chose a small sample first to
  limit real-PII exposure. GET-only, no writes, no test data created.
- Results (sample): all 3 → HTTP 200 application/pdf, valid 4–5-page อ.6 PDFs built from real
  data; front items 1–8 populate, annex table row-count matches item-4 count (38272: 9↔9).
- **DEF-1 (= SPEC-001 D3) CONFIRMED in real output — MAJOR.** Item 7 "ระยะเวลาการอนุญาต" prints
  **111 วัน / 90 วัน / 350 วัน** = the current code's `EMPLOYER.TOTAL_DAYS + " วัน"` path, NOT the
  business-confirmed source `T_T_LICENSE.PERIOD_TEXT` (Q2). Format conclusively matches the wrong
  source; proving each value differs needs the PERIOD_TEXT values (DATA REQUEST).
- Could NOT complete: (1) **Playwright/browser render** — Browser pane won't composite here
  (screenshot times out) and no PDF→image renderer (pdftoppm/gs/mutool) installed, so D4 signature
  quadrant placement + pixel layout parity are unverified; (2) D1/D5 need DB truth; (3) D2 needs
  form-ids (none). All logged honestly as BLOCKED/NOT_TESTED, not passed.
- Wrote **tests/TEST-002** (9 cases + DEF-1 + 4 Questions). Evidence PDFs in
  `../project-docs/REQ-001-evidence/` (⚠️ contain real PII; left untracked — see below).
- REQ-001 held at **IN_TEST** (not final) pending Porter's answers.
- @Porter — need decisions (TEST-002 §Questions): **Q1** run remaining 31 ids or is the sample
  enough given D3 is confirmed? **Q2** DATA REQUEST: PERIOD_TEXT for the 3 ids to nail DEF-1.
  **Q3** how to satisfy the Playwright render leg (this env can't) — display-capable env / install
  renderer / accept human visual review of saved PDFs? **Q4** ⚠️ `project-docs/` is NOT gitignored
  and the evidence PDFs hold real applicant PII (names/national-ID) — please gitignore before anyone
  commits. Also FYI: TASK-002 (dev-profile-gate preview) may change the /a6/db seam's anonymous
  availability — I'll re-verify the seam after it lands.

## [--:--] Sober (SA Lead)
- Picked up REQ-004 (READY_FOR_SA → IN_SPEC). Confirmed the project has NO profiles
  today (single application.yml, no @Profile, no spring.profiles.active). Chose a
  FAIL-CLOSED dev-profile gate: preview endpoints exist only when `dev` is active,
  so a prod build (default, no dev) has no anonymous real-data seam.
- Wrote specs/SPEC-004-secure-preview-db-seam.md + tasks/TASK-002-dev-profile-gate-preview.md.
  Fix = @Profile("dev") on PreviewController + dev-gate the /api/v1/preview/** permitAll
  in SecurityConfig (Environment.acceptsProfiles) + default `./mvnw spring-boot:run` to
  the `dev` profile in pom so QA's local workflow is unaffected. Whole controller gated
  (mock + db), since all of it is TEMPORARY and QA runs under dev anyway.
- Board: REQ-004 → IN_SPEC; TASK-002 → TODO (Jason).
- @Jason: TASK-002 ready. Do NOT touch the /api/v1/download chain, ApiKeyFilter, or
  CORS. DoD requires a TWO-STATE proof: WITHOUT dev → preview GET is not served
  anonymously (404/401, not a 200 PDF); WITH dev (default `./mvnw spring-boot:run`)
  → 200 PDF. Use the mock /checklist/a6 route for the presence check (no real data
  needed). Ping me for REVIEW.
- @Porter (heads-up, for when TASK-002 lands & redeploys): the UAT-wired
  localhost:33000 instance QA uses must run with `dev` active after this change — if
  started from a packaged jar, set SPRING_PROFILES_ACTIVE=dev, else the /a6/db seam
  (REQ-001/REQ-003) will 404. Current running instance is unaffected until redeploy.

## [12:08] Jason (BE)
- Picked up TASK-002 (TODO → IN_PROGRESS → REVIEW). Dev-profile-gated the preview seam.
- Changes (3 files, exact SPEC-004 scope): PreviewController `@Profile("dev")`;
  SecurityConfig injects `Environment` and dev-gates the `/api/v1/preview/**` permitAll;
  pom spring-boot-maven-plugin defaults `./mvnw spring-boot:run` to `dev`. Download chain,
  ApiKeyFilter, CORS untouched (git diff = only those 3).
- Evidence: compile BUILD SUCCESS. Two-state proof (booted my build on alt ports since
  :33000 is a pre-existing instance):
  - prod profile (:33098): no-auth GET /preview/checklist/a6, /a6/db/1, /a9 → all HTTP 401
    (real-data seam closed) ✅
  - dev default (:33099, plain spring-boot:run): /a6 → 200 pdf 272,739 B; /a9 → 200 pdf ✅
  Full output in TASK-002 `## Implementation Notes`.
- @Sober: TASK-002 ready for review. Also relayed a deploy/QA note in the TASK — after
  deploy, the UAT instance (if run from a jar) needs SPRING_PROFILES_ACTIVE=dev or the
  /a6/db seam 401/404s; please route that up to Porter for QA.

## [--:--] Sober (SA Lead)
- Reviewed TASK-002 (REVIEW → DONE). Verified independently, not on trust:
  git diff = only PreviewController (@Profile("dev")), SecurityConfig (preview
  permitAll gated by environment.acceptsProfiles("dev"), rest of both chains +
  download chain intact), pom.xml (spring-boot:run defaults to dev). ApiKeyFilter
  + DocumentController = empty diff. Re-ran `./mvnw -o -DskipTests compile` → exit 0.
  Accepted Jason's two-state proof: WITHOUT dev → preview routes (incl. /a6/db/1)
  HTTP 401; WITH dev → 200 pdf. Verdict + evidence in TASK-002 §Review.
- All tasks of SPEC-004 DONE → REQ-004 set to SPEC_DONE.
- @Porter: REQ-004 ready for acceptance. The anonymous real-data preview hole is
  closed fail-closed (prod build → 401/absent; dev → works). DEPLOY REQUIREMENT to
  carry to the human: the UAT-wired localhost:33000 instance QA uses must run with
  `dev` active after this lands — local `./mvnw spring-boot:run` is automatic (pom),
  but a packaged jar needs SPRING_PROFILES_ACTIVE=dev, else the /a6/db seam
  (REQ-001/REQ-003) returns 401. Current running instance unaffected until redeploy.
- Note: REQ-004 change is verified-local + uncommitted (same as TASK-001); a
  commit/deploy step is separate if the human wants it permanent.

## [--:--] Porter (PM)
- Read TEST-002 (REQ-001 QA sample) and REQ-004 SA handoff. Two things at Porter now:
  1. REQ-001: **DEF-1 (D3) CONFIRMED live** on 3 real ids — item 7 prints
     TOTAL_DAYS ("111/90/350 วัน") not T_T_LICENSE.PERIOD_TEXT. This is the
     stakeholder's suspected bug, now proven. Escalating Tanya's Q1–Q4 to the human.
  2. REQ-004: SPEC_DONE, fail-closed dev-profile gate, Sober-verified two-state
     (prod→401, dev→200). Carrying the DEPLOY REQUIREMENT to the human: the
     UAT-wired :33000 instance must run with `dev` active after this lands (local
     mvnw is automatic; a packaged jar needs SPRING_PROFILES_ACTIVE=dev) or the
     /a6/db seam 401s and breaks QA. Current instance unaffected until redeploy.
- Put Q1 (run scope), Q3 (Playwright env), Q4 (PII gitignore) to the human as
  decisions; Q2 (PERIOD_TEXT DATA REQUEST) offered as optional airtight proof.
- Reported the DEF-1 bug + REQ-004 closure to the human in Thai. Awaiting answers
  before finalizing the REQ-001 verdict and raising the D3-fix REQ.

## [--:--] Porter (PM)
- Human answered TEST-002 Q1/Q3/Q4 (recorded in TEST-002 §Questions):
  - Q1: 3-id sample is enough — do not run the remaining 31 (limit PII). AC#2/#3
    closed on the sample; DEF-1 confirmed.
  - Q2: PERIOD_TEXT SQL = optional (DEF-1 already stands); offered to human.
  - Q3: set up a renderer/env so QA can do the visual/Playwright leg → REQ-007.
  - Q4: gitignore project-docs/ (PII) → REQ-006; hold all commits until it lands.
- Raised three REQs to Sober (all READY_FOR_SA):
  - **REQ-005** (HIGH) — fix อ.6 item 7 to source `T_T_LICENSE.PERIOD_TEXT`
    (not TOTAL_DAYS + " วัน"); closes DEF-1.
  - **REQ-006** (HIGH) — gitignore `project-docs/` before any commit (real PII).
  - **REQ-007** (MEDIUM) — enable QA visual/Playwright render path; unblocks the
    REQ-001 visual leg (D4 + layout parity).
- Accepted **REQ-004 → DELIVERED** on Sober's independent two-state verification
  (prod→401, dev→200). Carried the DEPLOY REQUIREMENT to the human (run UAT :33000
  with `dev` profile post-redeploy) and the uncommitted-code caveat.
- REQ-001 stays IN_TEST: functionally the investigation is done (bug found), but the
  visual leg is parked on REQ-007 before Tanya's final verdict.
- @Sober: REQ-005, REQ-006, REQ-007 are ready — please pick up (REQ-005 & REQ-006
  are HIGH; REQ-006 gates commits).

## [--:--] Sober (SA Lead)
- Picked up the three SA-owned REQs (READY_FOR_SA). Read the code/repo/env state first.
- REQ-006 (PII gitignore) → SPEC_DONE, executed as workspace hygiene. Confirmed only
  project-docs/.gitkeep was tracked (no PII in history), appended `project-docs/` to the
  workspace .gitignore; git status now hides all evidence PDFs/sample-ids, .gitkeep stays
  tracked. Wrote specs/SPEC-006. @Porter: PII commit-hold can lift.
- REQ-005 (item-7 duration fix) → BLOCKED. Root cause confirmed (A6CheckListReportBuilder
  L83-85 uses EMPLOYER.TOTAL_DAYS + " วัน"; business source = T_T_LICENSE.PERIOD_TEXT).
  BUT no T_T_LICENSE entity exists and the requestId→license join is unknown from code
  (RequestSpecial.REF_LICENSE_ID is a candidate, unconfirmed). Wrote specs/SPEC-005 with
  DATA REQUEST DR-1 (join + PERIOD_TEXT for 38240/38272/38273 — also the DEF-1 proof) and
  DR-2 (REF_LICENSE_ID nullable edge). NO BE TASK until answered — won't let Jason guess schema.
  @Porter: please relay DR-1/DR-2 to the human.
- REQ-007 (visual/Playwright enablement) → SPEC_DONE. Env has NO PDF rasterizer
  (pdftoppm/gs/mutool absent) but has node+python. Wrote specs/SPEC-007 with a read-only
  recipe: rasterize via PyMuPDF (pure wheel, one-time `pip install pymupdf`) + a ready
  render_a6.py + optional Playwright driver; steps to check D4 quadrants + layout parity vs
  the REQ-002 baseline. Team delivers scripts; human installs pymupdf. @Porter: relay to
  Tanya + arrange the pip install.
- Board updated (REQ-005 BLOCKED, REQ-006/007 SPEC_DONE; blocked/waiting rows refreshed).

## [--:--] Porter (PM)
- Accepted **REQ-006 → DELIVERED**: project-docs/ is gitignored & verified (no PII
  tracked). PII commit-hold lifted.
- **REQ-005 (bug fix) is BLOCKED on a DATA REQUEST** — SA correctly won't guess the
  T_T_LICENSE join. Relaying DR-1/DR-2 (SPEC-005) to the human: confirm the
  requestId→T_T_LICENSE join (REF_LICENSE_ID→ID?) + PERIOD_TEXT for 38240/38272/38273
  (also serves as the DEF-1 proof), and whether REF_LICENSE_ID can be NULL for a
  valid อ.6. No BE TASK until answered. Asked the human in Thai.
- **REQ-007**: SPEC-007 recipe ready (PyMuPDF rasterize + optional Playwright).
  Routed to Tanya; asked the human for a one-time `pip install pymupdf` on the QA
  machine. @Tanya: once pymupdf is installed, run the REQ-001 visual leg (D4
  signature quadrants + layout parity vs the REQ-002 baseline) per SPEC-007, then
  finalize the REQ-001 verdict.
- Net state: REQ-002/003/004/006 DELIVERED; REQ-001 near-complete (bug found, only
  visual leg left, on REQ-007); REQ-005 waits on the human's DR-1/DR-2 answer.

## [--:--] Porter (PM)
- Human process feedback: DID-046 feels heavier than smart-scheduler (there QA just
  screenshotted the web). Honest read: part is inherent (PDF output + real UAT/Oracle
  PII we don't own the schema of → read-only + gitignore + DATA REQUEST for the fix),
  part was over-engineering by me on the visual leg.
- SIMPLIFIED REQ-007: primary method is now "open the preview/seam URL in a real
  browser (renders the PDF inline) and screenshot" — same as smart-scheduler. Dropped
  the `pip install pymupdf` requirement to a fallback-only for headless runs. Updated
  REQ-007 §Preferred method + the board waiting row.
- @Tanya: for the REQ-001 visual leg, prefer a real browser + screenshot; only use the
  SPEC-007 PyMuPDF fallback if you have no display browser. No pip install required otherwise.
- Only genuinely-irreducible ask left for the human: REQ-005 DR-1/DR-2 (Oracle join +
  PERIOD_TEXT) to fix the bug — because we must not guess someone else's DB schema.

## [12:35] Tanya (QA)
- Completed the **REQ-001 visual leg** and closed REQ-001. Tried the real-browser+screenshot
  path first (your simplified REQ-007) — but the Browser pane here still won't composite the
  PDF viewer (screenshot times out), i.e. no display browser in this session, so per your own
  instruction I used the **SPEC-007 PyMuPDF fallback** (1.28.0 was already installed) —
  `render_a6.py` @150dpi → PNGs, inspected directly. Fully read-only.
- **D4 (signature quadrants): PASS — no rendering defect.** Mock baseline renders the 4 signers
  row-major exactly as SPEC-001 expects (TL=ตั้งเรื่อง, TR=ผอ, BL=หน, BR=จก). Real 38272 fills a
  clean 2×2 with the real approvers — no overlap/blank/misplacement.
- **Layout parity vs REQ-002 baseline: PASS.** Real 38272 front/evidence/signature/annex pages
  match the baseline structure; values populate correctly (law-ref subreport renders the real
  variable row count). DEF-1 also confirmed **on screen**: item 7 = "90 วัน" (= TOTAL_DAYS path).
- D5: render shows an **unticked** "เอกสารอื่นๆ(ถ้ามี)" box → the tick logic discriminates
  (visual PARTIAL; full DB-truth still pending). D1/D2 remain uncovered (DB truth / form-ids).
- **REQ-001 → TEST_FAILED (final)**: AC#1 delivered (SPEC-001), AC#2 (generation + full layout)
  PASS, **AC#3 = DEF-1 (item-7 wrong source), MAJOR** → fix already tracked as REQ-005. Updated
  TEST-002 (cases 6/10 + verdict), REGRESSION (R5–R11), board (REQ-001 TEST_FAILED; REQ-007 ready
  for your accept→DELIVERED; visual-leg waiting row RESOLVED).
- PII hygiene: real-data PNGs were rendered, inspected, then **deleted**; kept PII-free mock PNGs
  + source PDFs (project-docs/ is gitignored per REQ-006, verified — nothing with PII is tracked).
- @Porter: REQ-001 is done from QA — verdict **TEST_FAILED**, single MAJOR defect DEF-1, fix =
  REQ-005 (needs your DR-1/DR-2 to the human). REQ-007 proved out (PyMuPDF path works) — ready for
  you to accept→DELIVERED. Nothing else waiting on QA right now.

## [--:--] Porter (PM)
- Human answered REQ-005 DR-1/DR-2 (saved to project-docs/REQ-005-DR1-license-join-result.md;
  answered in SPEC-005 §Questions):
  - **Correct join = `T_T_LICENSE.REQUEST_ID = T_T_REQUEST_SPECIAL.REQUEST_ID`** (by request
    id). `REF_LICENSE_ID` is NULL — the SPEC's assumed FK was wrong; use REQUEST_ID.
  - PERIOD_TEXT is a full sentence → drop the " วัน" suffix.
  - No-license case (38240 → no row) → item 7 blank. DEF-1 proven 100% (38272 real
    PERIOD_TEXT vs printed "90 วัน").
  - Flagged to SA: join-by-REQUEST_ID may return >1 license row — SA picks the selection rule.
  - Set SPEC-005 → ACTIVE; REQ-005 → IN_SPEC. @Sober: write the TASK for Jason and unblock.
- Accepted **REQ-007 → DELIVERED** (Tanya proved the PyMuPDF render path; D4 clean + parity).
- Closed **REQ-001 → DELIVERED**: the investigation goal is met — the stakeholder's suspected
  bug is found & proven (DEF-1, MAJOR), flow documented (AC#1), generation + full layout verified
  (AC#2). QA product-verdict on อ.6 is TEST_FAILED; the product fix is REQ-005. Traceability:
  REQ-001 (find) → REQ-005 (fix).
- State: REQ-001/002/003/004/006/007 DELIVERED; REQ-005 the only open item (BE fix, Sober→Jason).

## [--:--] Sober (SA Lead)
- REQ-005 unblocked (DR-1/DR-2 answered). Finalized SPEC-005 confirmed design and
  wrote tasks/TASK-003-item7-period-text-source.md.
- Confirmed facts baked into the TASK: join = T_T_LICENSE.REQUEST_ID = requestId
  (REF_LICENSE_ID is NULL, not used); permitDuration = PERIOD_TEXT verbatim (drop the
  hardcoded " วัน"); no license row → blank; >1 row → latest by ID DESC (my SA call —
  needs only PK ID; flagged that a status/date rule would be a follow-up to Porter).
- TASK-003 scope: new LicenseEntity(@Table T_T_LICENSE: ID/REQUEST_ID/PERIOD_TEXT) +
  LicenseRepository.findFirstByRequestIdOrderByIdDesc + swap A6CheckListReportBuilder
  item-7 block (~L83-85). DoD includes the DEF-1 close proof via the /a6/db seam
  (38272/38273 → real PERIOD_TEXT, 38240 → blank).
- Board: REQ-005 → IN_SPEC (owner Jason); TASK-003 → TODO.
- @Jason: TASK-003 ready — item-7 fix only, don't touch other fields. Ping me for REVIEW.

## [14:46] Jason (BE)
- Picked up TASK-003 (TODO → IN_PROGRESS → REVIEW). Fixed DEF-1 item-7 sourcing.
- Changes (item-7 only, exact SPEC-005 scope): new LicenseEntity (T_T_LICENSE: ID,
  REQUEST_ID, PERIOD_TEXT) + LicenseRepository (findFirstByRequestIdOrderByIdDesc);
  A6CheckListReportBuilder now sets permitDuration = PERIOD_TEXT verbatim (join by
  REQUEST_ID, latest-by-ID, blank when none), dropped EMPLOYER.TOTAL_DAYS + " วัน".
  No other field changed. Compile → BUILD SUCCESS.
- @Sober: TASK-003 code ready for review. BUT the DoD's runtime proof
  (/a6/db/38272|38273|38240 against real UAT T_T_LICENSE data) is a real-DB check —
  outside BE boundary (workspace rule #4) and QA's leg. Raised Q1 in TASK-003:
  please route the item-7 runtime proof to Tanya (QA) via Porter. I'll take any REWORK
  if the output shows a code issue.

## [--:--] Sober (SA Lead)
- Reviewed TASK-003 (REVIEW → DONE, code leg). Verified independently: change scoped to
  A6CheckListReportBuilder item-7 + new LicenseEntity (T_T_LICENSE: ID/REQUEST_ID/PERIOD_TEXT)
  + LicenseRepository.findFirstByRequestIdOrderByIdDesc. Matches SPEC-005 confirmed design
  (join by REQUEST_ID, PERIOD_TEXT verbatim, no " วัน", blank when none, latest-by-ID). Only
  item-7 touched; other working-tree files = uncommitted TASK-001/002. Re-ran compile → exit 0.
- Jason's Q1 is correct: the DoD's real-data item-7 proof (/a6/db/38272,38273,38240) reads real
  UAT data — that is QA's read-only leg, not BE's (rule #4), and SA doesn't run it either.
  Answered Q1 in TASK-003, kept his code leg complete.
- All SPEC-005 code DONE → REQ-005 → SPEC_DONE.
- @Porter: REQ-005 code is done + SA-reviewed + compiles, but DEF-1 is only CLOSED by QA's
  runtime proof. Please route to Tanya (IN_TEST): with dev profile active, GET (no auth)
  /a6/db/38272 & /a6/db/38273 → item 7 = the real T_T_LICENSE.PERIOD_TEXT sentence (NOT "90 วัน"
  / no stray " วัน"); /a6/db/38240 → item 7 blank. Her TEST_PASSED closes DEF-1; any mismatch
  returns to me as REWORK. (Note: this run needs the REQ-004 dev-profile deploy on :33000.)

## [--:--] Porter (PM)
- Routed REQ-005 DEF-1 close-proof to Tanya (SPEC_DONE → she takes IN_TEST).
- @Tanya: verify the item-7 fix, read-only. Build the CURRENT working tree (it has
  Jason's uncommitted TASK-003 fix) and run it on the **dev** profile on your own port
  — do NOT rely on the pre-existing :33000 instance (that's the OLD build without the
  fix). Then GET (no auth):
  - `/api/v1/preview/checklist/a6/db/38272` and `/a6/db/38273` → item 7 must now show
    the real `T_T_LICENSE.PERIOD_TEXT` sentence (e.g. 38272 = "ใช้ได้จนถึง 27 ก.ค. 2569
    นับแต่วันที่ได้รับอนุญาต"), **NOT "90 วัน"**, and **no stray " วัน"** suffix.
  - `/a6/db/38240` → item 7 **blank** (no license row).
  - Your TEST_PASSED closes DEF-1 → I set REQ-005 DELIVERED; any mismatch = REWORK to Sober.
  - Reuse render/text extraction as needed; keep PII hygiene (project-docs/ gitignored).

## [14:55] Tanya (QA)
- Ran the REQ-005 DEF-1 close proof. Built the fixed tree (TASK-003 present: LicenseEntity +
  LicenseRepository.findFirstByRequestIdOrderByIdDesc + item-7 rewire) → compile exit 0; booted my
  own instance :33002 with **dev** profile (PreviewController is @Profile("dev")); read-only, no key;
  stopped after; :33000 untouched.
- **RESULT: TEST_FAILED — the fix introduces DEF-2, a BLOCKER.** All 3 ids (38272/38273/38240) →
  **HTTP 500 application/json, no PDF.** Server log: `InvalidDataAccessResourceUsageException` →
  **ORA-00933: SQL command not properly ended** on
  `... from t_t_license where request_id=? order by id desc **fetch first ? rows only**`.
- Cause (evidence, not a proposed fix — repair is Sober's): `findFirstByRequestIdOrderByIdDesc`
  emits `FETCH FIRST … ROWS ONLY`, which is Oracle **12c+** syntax; the UAT DB is **Oracle 11.2**
  (Hibernate also warns the 11.2 dialect is unsupported) → it rejects the clause. So the license
  lookup throws and the **entire อ.6 PDF fails to generate** — worse than DEF-1 (which at least
  rendered a wrong value). **DEF-1 remains open** (can't be re-checked while generation 500s).
- Wrote **tests/TEST-003** (5 cases, DEF-2 BLOCKER, verdict TEST_FAILED). Evidence: JSON error
  bodies in `../project-docs/REQ-005-evidence/` (SQL error text, no PII). Updated REGRESSION (R8
  still FAIL, added R9b DEF-2) and board (REQ-005 → TEST_FAILED; rework row for Sober).
- @Porter: REQ-005 **fails re-test** — route the rework to Sober (Jason's `findFirstBy…` derived
  query is Oracle-11.2-incompatible; a6/db 500s for every request). No new data needed from the
  human — DR-1 already gives the expected item-7 values. I'll re-test the /a6/db seam for the 3 ids
  the moment a new build lands. Nothing else waiting on QA.

## [--:--] Porter (PM)
- REQ-005 re-test = **TEST_FAILED / DEF-2 (BLOCKER)**. QA proved Jason's
  `findFirstByRequestIdOrderByIdDesc` emits `FETCH FIRST … ROWS ONLY` (Oracle 12c+),
  but the **UAT DB is Oracle 11.2** → ORA-00933 → a6/db 500s, whole PDF fails.
  Worse than DEF-1 (which at least rendered). DEF-1 stays open until this is fixed.
- Routing the rework down the chain (TEST_FAILED → Porter → Sober). No new human
  data needed — DR-1 already has the expected item-7 values; QA re-tests on next build.
- **New project ENV fact recorded on the board:** UAT DB = Oracle **11.2** — no
  `FETCH FIRST`, no 12c-only SQL. Applies to all future queries.
- @Sober: REQ-005 REWORK — please redesign the "latest license by REQUEST_ID" lookup
  to be Oracle-11.2-safe (e.g. a native top-1 via `ROWNUM` over an `ORDER BY id DESC`
  subquery, or an equivalent that avoids FETCH FIRST), keep the rest of TASK-003's
  item-7 fix, and hand the corrected TASK to Jason. Tanya re-tests /a6/db for
  38272/38273 (→ real PERIOD_TEXT, no " วัน") and 38240 (→ blank), which also finally
  closes DEF-1.
- Reported to the human in Thai: fix caught by QA, internal rework loop, no action
  needed from them.

## [--:--] Sober (SA Lead)
- REQ-005 came back REWORK (QA TEST-003 / DEF-2): the derived `findFirstBy…OrderByIdDesc`
  makes Hibernate emit `FETCH FIRST 1 ROWS ONLY` (12c+), but UAT DB = Oracle 11.2 → ORA-00933,
  /a6/db 500s. Code was otherwise correct; only the top-1 mechanism is wrong.
- Checked the repo: there is ALREADY an Oracle-11g convention — LicenseInformRepository returns
  a List with the comment "เลี่ยง fetch first … Oracle 11g ไม่รองรับ", and buildSignatures picks
  the top row via the firstOrNull(...) helper. So the fix = match that pattern, not a native ROWNUM.
- Reworked TASK-003 (DONE → REWORK): LicenseRepository → `List<LicenseEntity>
  findByRequestIdOrderByIdDesc` (no First/limit); builder item-7 → `firstOrNull(list)` then
  `nz(periodText)` else "". Same semantics (latest-by-ID, blank, no " วัน"), 11.2-safe.
- Added ENV note relevance: Oracle 11.2 — no FETCH FIRST / no 12c-only SQL for all future queries.
- Board: REQ-005 → IN_SPEC (Jason); TASK-003 → REWORK.
- @Jason: TASK-003 rework — swap to List + firstOrNull (see "Rework (round 2)" in the TASK).
  Compile, then it re-hands to QA: the /a6/db/38272,38273,38240 run must now be 200 (no ORA-00933)
  and closes DEF-1 + DEF-2. Ping me for REVIEW.

## [15:05] Jason (BE)
- TASK-003 REWORK (DEF-2: Oracle 11.2 has no FETCH FIRST) → fixed → REVIEW.
- Switched to the project's 11.2-safe convention: LicenseRepository now returns
  `List<LicenseEntity> findByRequestIdOrderByIdDesc` (no `First` → no FETCH FIRST);
  builder takes the top row via the existing `firstOrNull(...)` helper (same pattern as
  buildSignatures). Semantics unchanged: latest-by-ID, blank when none, no " วัน".
- Evidence: `./mvnw -o -DskipTests compile` → BUILD SUCCESS; grep on LicenseRepository
  shows no First/Top/Pageable/Limit (only the explanatory comment) → no 12c-only SQL.
- @Sober: TASK-003 rework ready for review. Runtime /a6/db proof is still a real-UAT-DB
  read (QA's leg, not BE) — please re-route to Tanya; her 200-PDF run closes DEF-1 + DEF-2.

## [--:--] Sober (SA Lead)
- Reviewed TASK-003 rework round 2 (REVIEW → DONE). Verified independently: LicenseRepository
  now `List<LicenseEntity> findByRequestIdOrderByIdDesc` (no First/Top/Pageable/Limit — the only
  "FETCH FIRST" text is the explanatory comment), builder uses firstOrNull(...) + nz(periodText)
  else "". Same semantics (latest-by-ID, blank, no " วัน"), Oracle-11.2-safe (plain ORDER BY,
  no FETCH FIRST). Re-ran compile → exit 0. Verdict in TASK-003 §Review (round 2).
- All SPEC-005 code DONE → REQ-005 → SPEC_DONE.
- @Porter: REQ-005 rework done + SA-reviewed + compiles. Re-route the runtime proof to Tanya
  (IN_TEST): with dev profile on :33000, GET (no auth) /a6/db/38272 & /a6/db/38273 → item 7 =
  real T_T_LICENSE.PERIOD_TEXT sentence (no stray " วัน"); /a6/db/38240 → item 7 blank; all
  return 200 (no ORA-00933). Her TEST_PASSED closes BOTH DEF-1 and DEF-2. (Needs the REQ-004
  dev-profile deploy on :33000.)

## [--:--] Porter (PM)
- REQ-005 rework done + SA-reviewed (List + firstOrNull, no FETCH FIRST — matches the
  repo's existing Oracle-11.2 convention in LicenseInformRepository/buildSignatures).
  Re-routing the runtime proof to Tanya (SPEC_DONE → IN_TEST).
- @Tanya: same close-proof as before, now covering BOTH defects. Build the CURRENT
  working tree (has the reworked TASK-003) and run it read-only on the **dev** profile
  on your own port (not the old :33000 build). GET (no auth):
  - `/a6/db/38272` & `/a6/db/38273` → HTTP **200 PDF** (no ORA-00933), item 7 = the real
    T_T_LICENSE.PERIOD_TEXT sentence, **no stray " วัน"**.
  - `/a6/db/38240` → **200 PDF**, item 7 **blank**.
  - TEST_PASSED closes **DEF-1 + DEF-2** → I set REQ-005 DELIVERED and the อ.6 job is done.
  - Any 500/mismatch → REWORK back to Sober.

## [15:12] Tanya (QA)
- Re-tested the REQ-005 rework → **TEST_PASSED. DEF-1 and DEF-2 both closed.** The rework swaps
  `findFirstByRequestIdOrderByIdDesc` for `findByRequestIdOrderByIdDesc` returning `List` +
  first-in-Java (`firstOrNull`) → **no `FETCH FIRST`**, so the Oracle 11.2 issue is gone.
- Built the current tree (compile exit 0), booted my own :33003 (dev), read-only, no key; stopped
  after; :33000 untouched. Results — all match DR-1 exactly:
  - 38272 → 200 pdf, item 7 = "ใช้ได้จนถึง 27 ก.ค. 2569 นับแต่วันที่ได้รับอนุญาต" (text + visual render — fits, no overflow)
  - 38273 → 200 pdf, item 7 = "ใช้ได้จนถึง 28 ก.ค. 2569 นับแต่วันที่ได้รับอนุญาต"
  - 38240 → 200 pdf, item 7 **blank** (no license row)
  - **0 ORA-00933**, no stray " วัน" suffix. Old "90/350 วัน" values are gone.
- Updated TEST-003 (round-2 re-test section + verdict TEST_PASSED), REGRESSION (R8 & R9b → PASS,
  with an Oracle-11.2 `FETCH FIRST` regression-watch note), board (REQ-005 → TEST_PASSED; close-proof
  row RESOLVED). Evidence: retest PDFs in project-docs/REQ-005-evidence/ (gitignored, PII); render
  PNG inspected then deleted.
- @Porter: REQ-005 **passes** — item 7 is correct on real UAT data and the report generates cleanly.
  Ready for you to accept→DELIVERED. That closes the original อ.6 defect the stakeholder suspected.
  Nothing waiting on QA now.

## [--:--] Porter (PM)
- Accepted Tanya's TEST_PASSED → **REQ-005 DELIVERED**. Item 7 now prints the real
  T_T_LICENSE.PERIOD_TEXT (38272/38273 match DR-1 exactly, 38240 blank), report
  generates 200 with 0 ORA-00933 on Oracle 11.2. **DEF-1 + DEF-2 both closed.**
- 🏁 **อ.6 work complete.** REQ-001 (find the bug) → REQ-005 (fix it, QA-verified).
  All 7 REQs DELIVERED. The stakeholder's original suspected อ.6 defect is fixed and
  proven on real UAT data.
- Open handoff items for the human (not blockers, flagged for closure):
  1. **All code is uncommitted** in Jason's working tree (TASK-001/002/003). To make
     it permanent, commit + deploy. project-docs/ is gitignored (PII safe) — safe to commit.
  2. **Deploy note:** run the :33000 UAT instance with the `dev` profile after redeploy
     (jar → SPRING_PROFILES_ACTIVE=dev) or the /a6/db QA seam 401s (REQ-004 gate).
  3. REQ-004's preview /db seam is dev-only by design — fine for QA, closed in prod.
- Reported the completion + these 3 handoff items to the human in Thai.

## [--:--] Porter (PM)
- Human asked for the exact SQL + where each field lands on the อ.6 form (for
  requestId 38273 via the /a6/db seam). Gave them the table-level mapping from
  SPEC-001 §AC#1 as an interim answer.
- Raised **REQ-008** (MEDIUM, doc-only): a concrete อ.6 SQL + field-mapping reference
  (actual/effective SQL per query + bind params → which form section/subreport cell),
  including the REQ-005 item-7 PERIOD_TEXT path.
- @Sober: please pick up REQ-008 — documentation only, extend SPEC-001 §AC#1 with the
  verbatim/effective SQL and field mapping. (QA can capture show_sql read-only if you
  want runtime confirmation.)

## [--:--] Porter (PM)
- Sober delivered SPEC-008 (8 queries in execution order, effective SQL + bind params,
  field-by-field mapping to the อ.6 form; item 7 reflects REQ-005 PERIOD_TEXT).
  Accepted **REQ-008 → DELIVERED** (doc-only) and relayed a Thai summary to the human.
- All 8 REQs now DELIVERED. อ.6 work fully complete.

## [--:--] Porter (PM)
- Human ran the report with Hibernate show_sql on (id 38273) and shared the emitted
  SQL + final JSON. Two takeaways:
  1. **Confirms the REQ-005 fix live**: JSON `permitDuration` = "ใช้ได้จนถึง 28 ก.ค.
     2569 นับแต่วันที่ได้รับอนุญาต" (matches DR-1; no " วัน"); the license query is
     `t_t_license … order by id desc` (no FETCH FIRST, 11.2-safe). All 8 SPEC-008 core
     queries appear in order.
  2. **SPEC-008 is INCOMPLETE vs runtime** — reopening REQ-008 (DELIVERED → IN_SPEC).
     The captured log shows queries SPEC-008 did not list (table + where, PII-free):
     - `t_s_common_code WHERE (code_str,group_code) IN (?,?)` ×2 (label/enum decode)
     - `t_m_trader_plant WHERE trader_id=?`
     - `t_t_request_loc_chk WHERE request_id=?`
     - `t_t_attach_file WHERE id=?` ×~12 — the REAL tick check loads each attachment
       row (an N+1 bigger than the T_M_UNIT one SPEC-008 noted).
- @Sober: please reconcile SPEC-008 with the actual runtime SQL above — add the missing
  queries, correct the "tick = ATTACH_FILE_ID not null" note to reflect the real
  t_t_attach_file lookup, and place trader_plant/loc_chk/common_code in the flow (state
  which อ.6 field each feeds, or mark "loaded but not printed on อ.6" if so). Doc-only;
  QA/human already captured the SQL (no need to hit the env). PII stays in dev only.
- Reminded the human the shared JSON/log holds real PII — dev-only, don't commit/share.

## [--:--] Porter (PM)
- Sober reconciled SPEC-008 with the runtime show_sql. Added a "Runtime-only queries"
  section: the extra selects (T_T_REQUEST_LOC_CHK, T_M_TRADER_PLANT, T_S_COMMON_CODE ×2,
  T_T_ATTACH_FILE ×N) are **ORM side-effects of nullable @OneToOne/@ManyToOne** on the
  loaded entities — **loaded but NOT printed on อ.6**. Corrected my note: the evidence
  tick is decided from the `ATTACH_FILE_ID` **column**, NOT the t_t_attach_file lookup;
  those per-doc selects are a lazy-proxy N+1, not the tick mechanism.
- Accepted **REQ-008 → DELIVERED**. All 8 REQs delivered; SPEC-008 now matches runtime.
- Noted a candidate future perf REQ (make the nullable @OneToOne assocs truly lazy /
  map scalar FKs only, to kill the attach_file N+1) — offered to the human as optional,
  not raised unless they want it.

## [--:--] Sober (SA Lead)
- Picked up REQ-008 (doc only, READY_FOR_SA → SPEC_DONE). Wrote
  specs/SPEC-008-a6-sql-field-mapping.md: every query buildFromDb issues for
  /a6/db/{req}, in execution order, with effective SQL + bind params → the exact
  อ.6 form field/section each result populates (front items 1-8, law refs,
  signatures, evidence checklist incl. persons 3/4, annex table).
- Confirmed from code: NO @Query anywhere — all Spring Data derived queries, so the
  effective SQL is mechanical (10 entities, @Table names verified). Item 7 documented
  as the REQ-005 fix (T_T_LICENSE.PERIOD_TEXT, join by REQUEST_ID, latest-by-ID via
  List+firstOrNull), NOT the old TOTAL_DAYS. Noted the two 11.2-safe top-1 spots and
  the T_M_UNIT N+1 (perf out of scope).
- No DB hit (SA doesn't touch real DBs) — derived from code; noted QA can optionally
  confirm literal SQL via Hibernate show_sql read-only.
- Board: REQ-008 → SPEC_DONE.
- @Porter: REQ-008 done — SPEC-008 is the concrete SQL + field-mapping reference the
  human asked for (extends SPEC-001 §AC#1). Please relay to them.

## [--:--] Sober (SA Lead)
- Reconciled SPEC-008 with the runtime show_sql (id 38273) Porter relayed (REQ-008
  IN_SPEC → SPEC_DONE). The 4 "missing" queries are NOT explicit repo calls — they are
  ORM side-effects of nullable @OneToOne/@ManyToOne associations on the loaded entities
  (verified in the entity mappings), which is why a code-derivation of the repo methods
  didn't list them:
  - T_T_REQUEST_LOC_CHK ← RequestEntity.requestLocInfo (@OneToOne LAZY mappedBy)
  - T_M_TRADER_PLANT ← RequestEntity.factoryInfo (@OneToOne LAZY, JoinColumn TRADER_ID)
  - T_S_COMMON_CODE ×2 ← @ManyToOne CommonCode assocs (RequestEntity.activeCode + RequestDoc's)
  - T_T_ATTACH_FILE ×N ← RequestDocEntity.attachFile (@OneToOne LAZY) — one per evidence doc = N+1
- All are loaded-but-not-printed (A6 builder reads only scalar columns). Added a
  "Runtime-only queries" section to SPEC-008 mapping each to its association + "not printed".
- Corrected the tick note: the ✓ is decided from the ATTACH_FILE_ID **column** (hasFile),
  NOT the t_t_attach_file lookup — those per-doc selects are the nullable-@OneToOne side-effect.
- Root cause noted (nullable @OneToOne can't lazy-proxy without bytecode enhancement →
  eager resolve). Flagged the attach_file N+1 as a perf/cleanup candidate follow-up REQ
  (correctness unaffected). Doc-only; no DB hit by me.
- Board: REQ-008 → SPEC_DONE.
- @Porter: SPEC-008 reconciled with runtime — please relay the updated doc to the human.
  (Optional follow-up to consider: REQ to fix the @OneToOne eager N+1 — your call.)

## [--:--] Porter (PM)
- **New defect from the human** — อ.6 evidence checkbox doesn't reflect the real
  attachment. Repro: for 38272, the human removed the ATTACH_FILE_ID from
  T_T_REQUEST_DOC row ID=46784 (drives page-2 item 1); the checkbox **stayed ticked**
  when it should go **unticked**. Note: this directly tests SPEC-008's claim that the
  tick reads the ATTACH_FILE_ID column (hasFile) — observed behavior contradicts it.
- Raised **REQ-009** (HIGH): tick ⟺ that document actually has an attachment file.
  SA to find the real cause (row-existence vs column vs multiple doc rows per item)
  and fix. Included the human's repro (row 46784) + a Question on the exact rule
  (per-row vs per-checklist-item).
- @Sober: please pick up REQ-009 — trace the actual "checked" logic in the a6 builder
  (buildEvidences / hasFile) against this repro and fix so removing an attachment
  unticks the box; confirm whether the intended rule is per-doc-row or per-item.
- (The N+1 perf follow-up stays optional/unraised pending the human.)

## [--:--] Porter (PM)
- **Second new defect from the human** — the อ.6 person query (items 3/4,
  `T_T_REQUEST_PER`) is **missing `STATUS <> 'D'`**, so soft-deleted person rows leak
  into the report. Repro: 38272 has ~10 rows STATUS='D' (92557–92566) and only 3
  STATUS='A' (92567–92569); SPEC-008 §7c confirms the query has no status filter,
  while the doc query §7b already filters `STATUS <> 'D'` — inconsistent.
- Raised **REQ-010** (HIGH): add the soft-delete filter to the person query AND audit
  the other a6 queries for the same missing filter. Included the repro (PII-free: row
  IDs + STATUS only) and a Question on the exact active/deleted convention.
- Noted REQ-009 + REQ-010 are the same class (evidence/persons correctness) — told
  Sober he may bundle them into one SPEC/TASK if efficient.
- @Sober: please pick up REQ-009 and REQ-010 (both HIGH, page-2 correctness). Trace
  the actual queries, fix, and audit siblings for the deleted-row leak.

## [--:--] Sober (SA Lead)
- Picked up REQ-009 + REQ-010 (both HIGH, page-2 correctness) — bundled into
  SPEC-009 + TASK-004 (Porter OK'd bundling).
- REQ-009 root cause (from code): hasFile() = getAttachFileId() > 0 (FK column only), so a
  dangling ATTACH_FILE_ID (file removed, column left set) keeps item ticked (38272/row 46784
  repro). Fix: hasFile → d.getAttachFile() != null (the already-mapped @OneToOne resolves null
  for null/dangling FK) — also repurposes the wasted attach_file N+1 from SPEC-008. One helper
  drives both evidence + person doc ticks.
- REQ-010 root cause: buildPersons query has no status filter AND RequestPerEntity maps no STATUS
  column at all. Fix: add STATUS field to entity + findByRequestIdAndPerTypeAndStatusNotOrderByIdAsc
  + call with "D". Audit (entity level): only RequestDocEntity maps STATUS (already filtered); the
  rest map none — flagged Q3 DATA REQUEST to confirm whether LAW_REF/DTL/EMPLOYER DB tables have
  unmapped STATUS='D' rows (follow-up if yes).
- Questions to Porter: Q1 tick rule per-row vs per-item (fix satisfies the repro either way),
  Q2 status convention (defaulting <> 'D'), Q3 audit DATA REQUEST above.
- Board: REQ-009/010 → IN_SPEC (Jason); TASK-004 → TODO.
- @Jason: TASK-004 ready — 2 scoped fixes (hasFile→association; person STATUS filter). Compile,
  then it re-hands to QA for the 38272 real-data proof. Ping me for REVIEW.

## [15:40] Jason (BE)
- Picked up TASK-004 (TODO → IN_PROGRESS → REVIEW). Two page-2 อ.6 fixes (REQ-009+010).
- Fix A (REQ-009): hasFile() now `getAttachFile() != null` (real T_T_ATTACH_FILE row) instead
  of trusting the FK → dangling FK no longer keeps the box ticked. Drives evidence + person ticks.
- Fix B (REQ-010): added STATUS column to RequestPerEntity; RequestPerRepository →
  findByRequestIdAndPerTypeAndStatusNotOrderByIdAsc; buildPersons filters STATUS_DELETED("D").
  StatusNot = plain WHERE, 11.2-safe (no FETCH FIRST).
- Evidence: ./mvnw -o -DskipTests compile → BUILD SUCCESS; git scope = builder + RequestPerEntity
  + RequestPerRepository only.
- @Sober: TASK-004 ready for review. Runtime /a6/db/38272 proof (item1 unticked, persons
  92567-69 only) is real-UAT-DB = QA's leg — please route to Tanya to close REQ-009+010. Also
  flagged SPEC-009 Q1/Q2/Q3 (Porter/human decisions) in the TASK for you to route.

## [--:--] Sober (SA Lead)
- Reviewed TASK-004 (REVIEW → DONE). Verified independently: hasFile() = getAttachFile() != null
  (real attachment-row check, handles null+dangling FK); RequestPerEntity.status added + repo
  findByRequestIdAndPerTypeAndStatusNotOrderByIdAsc + buildPersons calls with "D" (plain WHERE,
  11.2-safe). Scope = exactly the 3 files. Re-ran compile → exit 0. Verdict in TASK-004 §Review.
- REQ-009 + REQ-010 code DONE → both SPEC_DONE.
- @Porter: route the runtime proof to Tanya (dev profile on :33000):
  - REQ-009: /a6/db/38272 → item 1 UNTICKED (row 46784, attachment removed) + a doc that still
    has a real file stays TICKED (no regression).
  - REQ-010: /a6/db/38272 → items 3/4 = only active persons 92567/92568/92569; deleted 92557–92566 gone.
  TEST_PASSED closes both. Also carry the open decisions: Q1 (tick per-row vs per-item — fix
  satisfies repro either way), Q2 (status convention <> 'D'), Q3 DATA REQUEST (do LAW_REF/DTL/
  EMPLOYER/LICENSE tables have unmapped STATUS='D'? → possible follow-up to fully close REQ-010 AC#2).

## [--:--] Porter (PM)
- REQ-009+010 code done + SA-reviewed (hasFile→getAttachFile()!=null; RequestPerEntity
  STATUS + StatusNot query, 11.2-safe). Answered Q2 in SPEC-009 (use `<> 'D'`).
- Routed REQ-009/010 to Tanya (IN_TEST) — the fix satisfies the repro regardless of Q1.
- @Tanya: verify read-only, dev profile, build the current tree (has TASK-004), your own port:
  - REQ-009: GET /a6/db/38272 → page-2 item 1 **UNTICKED** (row 46784, attachment removed);
    a doc that still has a real file stays **TICKED** (no regression).
  - REQ-010: same render → items 3/4 = only active persons **92567/92568/92569**; the
    deleted 'D' rows (92557–92566) are **gone**.
  - TEST_PASSED closes both → I set REQ-009+010 DELIVERED. Any mismatch → REWORK to Sober.
- Relayed Q1 (tick per-row vs per-item) and Q3 (soft-delete audit SQL) to the human.
- Q1 answered by human: **per-item** (tick if any doc row for the item has a file) —
  matches the current fix; recorded in SPEC-009, no further change.

## [--:--] Porter (PM)
- Human is digging into REQ-009 on item 1 (38272). Their SELECT of T_T_REQUEST_DOC
  ID=46784 now shows **ATTACH_FILE_ID=39895** (STATUS='A', REQUEST_CHECKLIST_ID=89) —
  i.e. it currently HAS an attachment, so item 1 ticking is correct for this data; the
  earlier "set to 0" isn't reflected (didn't persist / reverted). Also flagged to the
  human: if they test on the old :33000 instance they see pre-fix behavior (fix is
  uncommitted in Jason's tree).
- @Sober: the human wants a **read-only diagnostic SQL** (for them to run) that shows
  exactly what drives item 1's per-item tick for req 38272 — please author it:
  (a) the master checklist row for item 1 (T_S_REQUEST_CHECKLIST GROUP_CODE='ReqSpecial'
  → the ID that maps to อ.6 item 1; note 46784 has REQUEST_CHECKLIST_ID=89 — confirm 89 is item 1),
  (b) ALL T_T_REQUEST_DOC rows for req 38272 with that REQUEST_CHECKLIST_ID and STATUS<>'D',
  (c) for each, whether ATTACH_FILE_ID resolves to a real T_T_ATTACH_FILE row (LEFT JOIN,
  show file id / null). So the human can see whether the tick comes from row 46784 or a
  sibling row, and whether any FK is dangling. Read-only; drop in project-docs if handy.

## [--:--] Porter (PM)
- Human self-corroborated REQ-009: the earlier "still ticked" was just an uncommitted
  edit — after committing the ATTACH_FILE_ID removal, item 1's checkbox disappears =
  fix works as intended. (Formal close still awaits Tanya's TEST_PASSED.)
- Answered a how-to: the person id-card/house-reg ticks (items 3/4) come from
  T_T_REQUEST_DOC (DOCUMENT_ID=102 บัตรปชช. / 103 ทะเบียนบ้าน, linked by REF_ID),
  same attachment rule as REQ-009. Gave the human a read-only SELECT to locate the rows
  and told them to null ATTACH_FILE_ID + commit to untick. Flagged that if REF_ID
  doesn't match the person IDs (92567/92568), SA should confirm the exact FK.
- Confirmed from the human's data: **T_T_REQUEST_DOC.REF_ID = T_T_REQUEST_PER.ID**
  exactly (persons 92557–92569 ↔ their 102/103 doc rows). Active persons 92567/68 (item 3),
  92569 (item 4); their doc rows: 92567→46815(102)/46816(103), 92568→46817/46818,
  92569→46819/46820. Deleted persons' doc rows exist but are unreachable once the person
  is filtered (REQ-010). Gave the human the exact rows + UPDATE...ATTACH_FILE_ID=NULL to
  untick per person/doc for testing.
- Human confirmed items 3/4 person checkboxes also behave correctly (test passed on
  their side). Now asking how to untick item 8's sub-items (1)-(4).
- @Sober: quick read-support question — how are อ.6 **item 8 (1)-(4)** ("เอกสารของ
  ผู้ว่าจ้าง") checkboxes determined? In the render JSON they carry `"type":"employer"`
  (unlike items 1/2/5/6/7 which are regular T_S_REQUEST_CHECKLIST + T_T_REQUEST_DOC).
  Please confirm the exact source (a) master checklist SEQ 6-9 IDs → matching
  T_T_REQUEST_DOC.REQUEST_CHECKLIST_ID + attachment (same as others), OR (b) an
  employer-specific source (T_T_REQUEST_EMPLOYER / a fixed rule), and tell the human
  which row/field to edit to untick each of (1)-(4). Read-support only; the human is
  self-testing the REQ-009 tick behavior on item 8.

## [--:--] Porter (PM)
- Item-8 question RESOLVED by the human's own data (master checklist query). อ.6 item 8
  uses the SAME mechanism as items 1/2/5/6/7 (T_S_REQUEST_CHECKLIST → T_T_REQUEST_DOC
  by REQUEST_CHECKLIST_ID + attachment); the `"type":"employer"` is just a render label.
  Master ID map for GROUP_CODE='ReqSpecial' (IS_ACTIVE=1): SEQ1=ID89→item1, SEQ2=90→item2,
  SEQ3=91→item5, SEQ4=92→item6, SEQ5=93→item7, **SEQ6=94→item8(1), SEQ7=95→item8(2),
  SEQ8=96→item8(3), SEQ9=97→item8(4)**. (Items 3/4 = persons, T_T_REQUEST_PER, not here.)
  So @Sober's item-8 question is answered — no SA action needed.
- Corrected the human's query: they filtered REQUEST_CHECKLIST_ID IN (1..9) = SEQUENCE
  values (empty result); the real IDs are 89-97. Gave them the fixed query (IN 94,95,96,97)
  + UPDATE...ATTACH_FILE_ID=NULL to untick each item-8 sub-row.
- Human confirmed item 8 (1) unticks correctly after nulling its doc's ATTACH_FILE_ID.
  Now asks how the **"เอกสารอื่น ๆ (ถ้ามี)"** line (the last row under item 8) works — it is
  NOT one of the 9 master checklist SEQ (89-97), so it's handled separately.
- @Sober: read-support — how is อ.6 item 8's **"เอกสารอื่น ๆ (ถ้ามี)"** line determined
  (its checked state + the free-form label/dotted line)? It has no T_S_REQUEST_CHECKLIST
  SEQ row; likely driven by T_T_REQUEST_DOC with DOCUMENT_NAME_OTHER / a special
  DOCUMENT_ID or a fixed "always empty" rule. Please confirm the exact source and, if it
  is data-driven, which row/field the human would edit to tick/untick it. Also add this
  "other documents" line to SPEC-008 so the field mapping is complete. Read/doc only.

## [--:--] Sober (SA Lead)
- Read-support (Porter's question): how อ.6 item 8 "เอกสารอื่น ๆ (ถ้ามี)" line works.
  ANSWER: it's HARDCODED in buildEvidences (A6CheckListReportBuilder ~L184-186) — a fixed
  dotted-line write-in label with `checked=false` always. No SQL, no T_S_REQUEST_CHECKLIST
  SEQ, no T_T_REQUEST_DOC, no DOCUMENT_ID. It is NOT data-driven → there is **no row/field
  the human can edit to tick/untick it**; it prints always-unticked by design. (Making it
  dynamic = a new REQ.)
- Added §7d to SPEC-008 documenting this static line, so the field mapping is complete.
- @Porter: item-8 "other documents" line = static/always-unticked (not data-driven). SPEC-008
  updated. No SA action beyond this; nothing else waiting for Sober.

## [--:--] Porter (PM)
- Human decided the item-8 "เอกสารอื่น ๆ" line should be DYNAMIC (chose option 2 from my
  earlier question) and gave the exact rule from their own investigation (request 38314):
  other-docs = T_T_REQUEST_DOC where DOCUMENT_ID=0 AND DOCUMENT_TYPE=99 AND
  REQUEST_CHECKLIST_ID=0; show DOCUMENT_NAME comma-joined; include ONLY rows with a real
  attachment (REQ-009 rule); exclude STATUS='D'; blank if none. Example row 47317 "wdw"
  (has file) → line shows "wdw".
- Raised **REQ-011** (HIGH) → Sober. Included the verbatim rule + example + two Questions
  (checkbox tick behavior when ≥1 shown; confirm DOCUMENT_NAME vs DOCUMENT_NAME_OTHER for
  type-99 rows). Supersedes SPEC-008 §7d static behavior.
- @Sober: please pick up REQ-011 — replace the hardcoded §7d line with the query-driven
  value (11.2-safe, reuse getAttachFile()). Flag the checkbox-tick Q back to me.
- (Noted in passing: 38314's item-8 (1)-(4) rows carry STATUS='C' with ATTACH_FILE_ID=0 —
  a status value beyond 'A'/'D'; not a blocker now since no-file → unticked, but relevant
  to the REQ-010 Q3 soft-delete audit. Left as-is.)

## [--:--] Sober (SA Lead)
- Picked up REQ-011 (READY_FOR_SA → IN_SPEC) — make the item-8 "เอกสารอื่น ๆ" line dynamic
  (supersedes SPEC-008 §7d static). Wrote SPEC-011 + TASK-005.
- Design (verified fields exist in RequestDocEntity): reuse the docs list already loaded in
  buildEvidences (findByRequestIdAndStatusNot → STATUS<>'D' already applied) + the REQ-009
  hasFile() rule; filter DOCUMENT_ID=0 & DOCUMENT_TYPE=99 & REQUEST_CHECKLIST_ID=0 & has file;
  comma-join DOCUMENT_NAME; replace the hardcoded sub-row. NO new query → 11.2-safe.
  DOCUMENT_NAME confirmed by example row 47317 ("wdw"), not DOCUMENT_NAME_OTHER.
- Board: REQ-011 → IN_SPEC (Jason); TASK-005 → TODO.
- @Jason: TASK-005 ready — one sub-row change in buildEvidences, reuse docs+hasFile. Compile,
  then QA proves on /a6/db/38314 → line shows "wdw". Ping me for REVIEW.
- @Porter: SPEC-011 Q1 (checkbox) — I defaulted to "tick when ≥1 other-doc shown, blank+unticked
  when none". If the human wants a pure write-in (names, no tick), it's a 1-line change — please
  confirm; non-blocking so I let Jason proceed with the default.

## [16:10] Jason (BE)
- Picked up TASK-005 (TODO → IN_PROGRESS → REVIEW). อ.6 item-8 "เอกสารอื่น ๆ" now dynamic.
- One-block change in buildEvidences: reuse the already-loaded docs list (STATUS<>'D' applied),
  filter DOCUMENT_ID=0 & TYPE=99 & CHECKLIST_ID=0 & hasFile (REQ-009), map DOCUMENT_NAME,
  join ", ". Label + checked derived from result (tick when >=1, SA default per Q1). No new
  query → 11.2-safe. git scope = A6CheckListReportBuilder only.
- Evidence: ./mvnw -o -DskipTests compile → BUILD SUCCESS.
- @Sober: TASK-005 ready for review. Runtime /a6/db/38314 → "wdw" ticked (+ no-doc → blank) is
  real-UAT-DB = QA's leg; please route to Tanya to close REQ-011. Flagged SPEC-011 Q1/Q2 for you.

## [--:--] Sober (SA Lead)
- Reviewed TASK-005 (REVIEW → DONE). Verified independently: item-8 "เอกสารอื่น ๆ" line now
  filters the already-loaded docs by DOCUMENT_ID=0 & DOCUMENT_TYPE=99 & REQUEST_CHECKLIST_ID=0
  & hasFile(d) (REQ-009 rule), comma-joins DOCUMENT_NAME, ticks when ≥1. No new query (reuses
  docs, STATUS<>'D' applied) → 11.2-safe. Scope = A6CheckListReportBuilder only. Re-ran compile
  → exit 0. Verdict in TASK-005 §Review.
- REQ-011 code DONE → SPEC_DONE.
- @Porter: route the runtime proof to Tanya (dev profile): /a6/db/38314 → item-8 "เอกสารอื่น ๆ"
  line = "wdw" (row 47317), box ticked; a request with no attached type-99 other-docs → line
  blank + unticked. TEST_PASSED closes REQ-011. Carry SPEC-011 Q1 (checkbox tick-when-≥1 default —
  human confirm; 1-line if they want write-in only) and Q2 (DOCUMENT_NAME confirmed by example).

## [--:--] Porter (PM)
- REQ-011 code done + SA-reviewed (item-8 "เอกสารอื่น ๆ" now dynamic: filter DOCUMENT_ID=0
  & DOCUMENT_TYPE=99 & REQUEST_CHECKLIST_ID=0 & hasFile, comma-join DOCUMENT_NAME, tick when
  ≥1; reuses loaded docs → 11.2-safe). Accepted the SPEC-011 Q1 default (tick-when-≥1 — I
  recommended it; human said "go"; they can still flip to write-in-only, a 1-line change).
- Consolidating the QA pass: REQ-009 + REQ-010 + REQ-011 are all SPEC_DONE on the same
  builder — routing all three to Tanya in one run.
- @Tanya: verify read-only, dev profile, build the current tree, your own port:
  - REQ-009: /a6/db/38272 → item 1 UNTICKED (row 46784, attachment removed) + a doc that
    still has a real file stays TICKED.
  - REQ-010: /a6/db/38272 → items 3/4 = only active persons 92567/92568/92569 (deleted 'D'
    rows gone).
  - REQ-011: /a6/db/38314 → item-8 "เอกสารอื่น ๆ" line = "wdw" (row 47317), box ticked; and a
    request with no attached type-99 other-docs → line blank + unticked (no regression).
  - Note: the human already self-verified REQ-009/010 behavior; your run is the formal close.
  - TEST_PASSED on each → I set REQ-009/010/011 DELIVERED. Any mismatch → REWORK to Sober.

## [--:--] Porter (PM)
- REQ-011 value confirmed correct by the human ("wdw"/"ทดสอบ2, ทดสอบ 1" shows + ticks). New
  presentation request: the value should render on a **dotted write-in line** (govt-form
  style), not appended after the label. More generally, evidence sub-items should support
  two render types: (A) checkbox+label, (B) checkbox+label+dotted-fill carrying a value.
  "เอกสารอื่น ๆ" = Type B.
- Raised **REQ-012** (MEDIUM) → Sober. Flagged it's a Jasper template (.jrxml/.jasper) +
  builder-field change (render-type flag + fill-in value), not a Java one-liner; data/tick
  from REQ-011 unchanged. Added a Q on whether to build a general per-sub-item renderType
  model or a targeted "เอกสารอื่น ๆ"-only template change.
- @Sober: please pick up REQ-012 — scope the template change; confirm the dotted-line style
  and the general-vs-targeted model with me before big template work if ambiguous.
- Restated my understanding to the human for confirmation before Sober goes deep.

## [--:--] Sober (SA Lead)
- Picked up REQ-012 (READY_FOR_SA → IN_SPEC) — render the item-8 "เอกสารอื่น ๆ" value on a
  dotted write-in line (Type B) vs label-only (Type A). Read request-a6-evidenceSub.jrxml
  (fields type/seq/label/houseReg/idCard/checked/detail; two bands gated by
  "person"/"employer".equals(type)). Wrote SPEC-012 + TASK-006.
- Design decision (answers the general-vs-targeted Q): TARGETED — reuse the existing `detail`
  field as the fill-in value + null/non-null sentinel. "เอกสารอื่น ๆ" row → detail=otherDocNames
  ("" when empty, never null) → always Type B; items (1)-(4) → detail=null → Type A. NO new
  model field, no speculative renderType model.
- TASK-006: builder tweak (value → detail, fixed label) + evidenceSub jrxml (add a $F{detail}
  textField with a dotted bottomPen, gated printWhenExpression detail!=null) + recompile jasper
  via A6PreviewTest. Flagged the JR7 caveats (uuid/band-height/dotted pen) + preview loop.
- Board: REQ-012 → IN_SPEC (Jason); TASK-006 → TODO.
- @Jason: TASK-006 ready — first template (.jrxml) change of this project; use A6PreviewTest to
  recompile + eyeball target/a6-preview.pdf. Ping me for REVIEW.
- @Porter: SPEC-012 Q1 — dotted-line exact style/width to match the printed form is a visual
  detail; SA default = thin dotted bottom border across the write-in area, Jason tunes in preview.
  Confirm with the human if they have a specific style; non-blocking.

## [16:45] Jason (BE)
- Picked up TASK-006 (TODO → IN_PROGRESS → REVIEW). อ.6 "เอกสารอื่น ๆ" now Type B (dotted write-in).
- Builder: the REQ-011 row uses fixed label "เอกสารอื่น ๆ (ถ้ามี)" + value moved to detail=otherDocNames
  ("" when none → always Type B); (1)-(4) keep detail=null → Type A. otherDocNames/checked unchanged.
- jrxml (request-a6-evidenceSub, employer band): added a $F{detail} textField gated by
  $F{detail}!=null, x=185 w=351 h=16, dotted bottomPen 0.75; uuid omitted (JR7 auto-gen).
- Evidence: A6PreviewTest recompiled all 6 a6 jrxml→jasper (no JR7 error), Tests run:1 Failures:0,
  a6-preview.pdf pages=3 (pagination intact); ./mvnw -DskipTests compile → BUILD SUCCESS.
  NB: sibling request-a6-*.jasper show modified — recompiled by the preview test, their jrxml source
  unchanged (project's compile loop).
- @Sober: TASK-006 ready for review. Visual Type-B-with-value proof needs real data (mock has
  detail=null) → QA's leg: /a6/db/38314 → value on the dotted line, none → blank line. Please route
  to Tanya to close REQ-012. Q1 dotted-style = SA default (tunable, non-blocking).

## [--:--] Sober (SA Lead)
- Reviewed TASK-006 (REVIEW → REWORK). Code correct + verified: builder row = fixed label +
  detail=otherDocNames; jrxml employer band adds a $F{detail} textField (x=185 w=351 h=16,
  dotted bottomPen 0.75, gated detail!=null, uuid omitted, fits band 18); scope = builder +
  evidenceSub.jrxml only (.jasper regen is expected A6PreviewTest output). A6PreviewTest compiles
  all a6 jrxml (no JR7 error), pages=3.
- REWORK reason (one narrow gap): the A6PreviewTest employer mock has detail=null → the preview
  proved only Type-A; NOBODY has visually confirmed the Type-B dotted-line-with-value renders. My
  DoD required the preview to SHOW the value on the dotted line. First jrxml layout change → prove
  it in the fast preview loop, don't defer all risk to QA. Also a concrete overlap risk to check:
  label textField x=64 w=472 vs value x=185.
- Rework asked (small): give the "เอกสารอื่น ๆ" preview mock a non-null detail (e.g. "ทดสอบ2, ทดสอบ 1"),
  re-run A6PreviewTest, confirm value sits on the dotted line with NO label/value overlap + (1)-(4)
  still label-only; report the preview result. No data-logic change.
- Board: TASK-006 → REWORK; REQ-012 stays IN_SPEC (Jason).
- @Jason: TASK-006 rework — see "Review (round 1)" in the TASK. Ping me for REVIEW after the
  Type-B preview check.

## [17:15] Jason (BE)
- TASK-006 REWORK round 1 (prove Type-B in preview) → fixed → REVIEW.
- A6PreviewTest mock: item-8 employer now has a detail=null row (Type A) + a
  "เอกสารอื่น ๆ (ถ้ามี)" row with detail="ทดสอบ2, ทดสอบ 1" (Type B). Test-fixture only, no data logic.
- Re-ran preview: compiled all a6 jrxml (no JR7 error), Tests run:1 Failures:0, pages=3.
  PyMuPDF check on a6-preview.pdf: label "เอกสารอื่น ๆ (ถ้ามี)" and value "ทดสอบ2, ทดสอบ 1" are
  separate runs, value sits right-of/after the label on the same row → no overlap with the x=185
  dotted line; (1)-(4) still label-only; no x/width tweak needed.
- @Sober: TASK-006 rework ready for review. Real-data value-on-line proof (38314) still QA's leg —
  please route to Tanya to close REQ-012.

## [--:--] Sober (SA Lead)
- Reviewed TASK-006 rework round 2 (REVIEW → DONE). Jason added a Type-B mock row to A6PreviewTest
  ("เอกสารอื่น ๆ (ถ้ามี)", detail="ทดสอบ2, ทดสอบ 1") + kept a Type-A row. Verified independently:
  re-ran A6PreviewTest → 6 a6 jrxml compiled (no JR7 error), pages=3, exit 0. Extracted the preview
  PDF text with PyMuPDF: page 2 shows "เอกสารอื่น ๆ (ถ้ามี)" and "ทดสอบ2, ทดสอบ 1" as SEPARATE text
  runs → the value renders via the new $F{detail} textField (Type-B, own element), NOT appended to
  the label; the Type-A "(1)…" row stays label-only. Dotted bottomPen present in jrxml (border, not
  text). Verdict in TASK-006 §Review (round 2).
- REQ-012 code DONE → SPEC_DONE.
- @Porter: route the final visual close to Tanya (dev profile, PyMuPDF render per REQ-007):
  /a6/db/38314 → the "เอกสารอื่น ๆ" value sits ON the dotted underline (not inline), NO overlap
  between the label and the value/line; a request with no other-docs → blank dotted line; items
  (1)-(4) unchanged. TEST_PASSED closes REQ-012. (SPEC-012 Q1 dotted-style is a non-blocking tune.)

## [--:--] Porter (PM)
- REQ-012 done: Sober took the targeted approach (only "เอกสารอื่น ๆ" → Type B, value on a
  dotted write-in line; label/value as separate runs, no overlap; Type-A lines unchanged).
  TASK-006 (jrxml) DONE + SA-reviewed. Proceeded without waiting on my 2 confirm questions —
  targeted scope is low-risk; if the human wanted the general renderType model, that'd be a
  follow-up. Value/tick data from REQ-011 unchanged.
- Adding REQ-012 to Tanya's batch. All four (REQ-009/010/011/012) now SPEC_DONE on the same
  builder/template — one QA pass.
- @Tanya: in the same run, also verify REQ-012 (dev, /a6/db/38314): item-8 "เอกสารอื่น ๆ (ถ้ามี)"
  value ("ทดสอบ2, ทดสอบ 1") sits ON the dotted write-in line (not appended inline), no
  label/value overlap; a request with no attached type-99 docs → blank dotted line; Type-A
  sub-items (item 8 (1)-(4)) unchanged; layout/pagination intact. TEST_PASSED closes REQ-012.

## [--:--] Porter (PM)
- Human is satisfied with the อ.6 work from their own testing (REQ-009/010/011/012 look
  correct to them); they declined to run Tanya's formal pass for now — those four stay
  SPEC_DONE, Tanya's close proof still open on the board if we want it later.
- New direction: the human moves to **อ.9 (a9)** and hit
  GET /api/v1/preview/checklist/a9/db/37940 → doesn't work. Per SPEC-002/003, a9 only has
  the MOCK preview; the plain-id /db/{requestId} seam exists only for a6 (commit b0d9a84).
- Raised **REQ-013** (HIGH) → Sober: add the a9 db seam mirroring a6 (plain id, no key,
  dev-profile-gated per REQ-004, same builder as the real download so verification is
  meaningful). Added a Question: confirm whether an a9 DB builder exists (a9 may be
  mock-only today) and flag the size back to me before coding if it doesn't.
- @Sober: please pick up REQ-013. If a9 has no DB builder at all, stop and tell me the
  scope — that's a much bigger REQ than the a6 seam was.

## [--:--] Sober (SA Lead)
- Picked up REQ-013 (a9 db preview seam) → STOPPED before coding per your instruction, because
  the scoping check answered your Question: **a9 is MOCK-ONLY.** A9CheckListReportBuilder.createData
  → buildMock; no repository, no buildFromDb/createDataRaw (a9/a10 mock since project start).
- So "mirror the a6 seam" doesn't apply: the a6 seam worked only because a6 already had
  createDataRaw→buildFromDb (real DB). For a9 the seam (Part B, ~10 lines) is trivial and already
  covered by the REQ-004 dev-gate/no-key, but it needs a FULL A9 DB builder first (Part A) — a much
  bigger REQ than the a6 seam.
- Wrote SPEC-013 (scope assessment). Part A reuses ~60% of a6 patterns (applicant/law/signatures/
  components/persons/item-7/tick/STATUS) but has real Data-Dictionary unknowns needing DATA REQUESTs:
  a9's 13-item evidence master GROUP_CODE + SEQ→item mapping (item 12 has (1)-(9)); destroyLocation
  source; item-5 factory-doc structure; item-12(2) person2 source. (Resolver already maps FORM_ID
  9/10 → "A9".)
- Answered the Question in REQ-013; set REQ-013 → BLOCKED. NO BE task — a seam over the mock builder
  would return the same mock PDF for every requestId (fails AC#1/#3, "looks real, isn't").
- @Porter: scope decision needed. Recommend splitting: REQ-013a = A9 DB integration (big; needs the
  DATA REQUESTs first, same investigate→spec→build the a6 work followed), REQ-013b = the a9/db seam
  (trivial, depends on 013a). Confirm with the human whether to fund the full A9 DB build; if yes,
  I'll raise the DATA REQUESTs and spec it in parts.

## [--:--] Porter (PM)
- Relayed SPEC-013's scope finding to the human (a9 = mock-only; seam alone would return the
  same fake PDF for every id). **Human decision: build the full A9 DB integration.**
- Raised **REQ-014** (HIGH) = SPEC-013 Part A: real buildFromDb/createDataRaw for
  A9CheckListReportBuilder, reusing the อ.6-proven rules (REQ-009 tick, REQ-010 STATUS<>'D',
  REQ-005 PERIOD_TEXT, 11.2-safe), covering อ.9's 13 evidence items incl. item 12 (1)-(9).
  REQ-013 (seam) stays BLOCKED as REQ-014's final step.
- Answered SPEC-013 §Questions with the decision. Asked Sober to (a) author the exact SQL for
  DATA REQUESTs #2/#3/#4 so I can hand the human copy-paste queries, and (b) confirm whether
  the REAL download endpoint for อ.9 also returns mock data — if yes that's a live
  production-facing defect I want stated plainly for the human.
- Gave the human the #1 query now (list all T_S_REQUEST_CHECKLIST groups/SEQ) since it needs
  no a9 schema knowledge.
- @Sober: please pick up REQ-014 (investigate → SPEC, same pattern as the อ.6 work).

## [--:--] Porter (PM)
- DATA REQUEST #1 (a9 evidence master) came back with a **blocking finding**: the human ran
  the checklist-group query (IS_ACTIVE=1) and there is **NO อ.9 group at all**. Groups present:
  A1, A3, BgChk, ReqExpand, ReqOpen, ReqPersonChange, ReqPlantChange, ReqSpecial. (ReqSpecial
  SEQ1-9 / IDs 89-97 re-confirms the อ.6 mapping we already use.)
- So REQ-014's backbone (อ.9's 13-item evidence master, incl. item 12 (1)-(9)) has no master
  data in the DB — either the rows exist but IS_ACTIVE<>1, or they were never seeded.
- Asked the human to run one follow-up (GROUP BY GROUP_CODE without the IS_ACTIVE filter) to
  distinguish the two cases. If never seeded, someone must create the อ.9 master data — this
  team does not write to DBs, so that's a human/DBA decision and it changes REQ-014's size.
- @Sober: FYI for REQ-014 — plan for both cases. Also please still author the SQL for DATA
  REQUESTs #2/#3/#4 (destroyLocation, item-5 factory docs, item-12(2) person2) so I can hand
  the human copy-paste queries; and confirm whether the real download endpoint for อ.9 returns
  mock data today (my earlier question).

## [--:--] Porter (PM)
- Follow-up confirmed: **อ.9 has NO master checklist rows at all** (not merely inactive).
  T_S_REQUEST_CHECKLIST holds only 8 groups: A1(15), A3(22), BgChk(7), ReqExpand(11),
  ReqOpen(10), ReqPersonChange(10), ReqPlantChange(13), ReqSpecial(9). No อ.9/destroy group.
- Before concluding "the master must be seeded", asked the human for one more read-only query:
  all T_T_REQUEST_DOC rows for an อ.9 request (37940) showing DOCUMENT_ID / DOCUMENT_TYPE /
  REQUEST_CHECKLIST_ID / ATTACH_FILE_ID / REF_ID / STATUS. That distinguishes:
  (a) REQUEST_CHECKLIST_ID populated but pointing at missing master rows → master really is
      missing, someone must seed it (human/DBA; this team doesn't write DBs), or
  (b) REQUEST_CHECKLIST_ID = 0 with varied DOCUMENT_IDs → อ.9 binds documents by DOCUMENT_ID
      instead, and the 13 item labels can be fixed in the report — no seeding needed, REQ-014
      proceeds without a DB-data blocker.
- @Sober: this is the deciding fact for REQ-014's shape; I'll relay the result. Note ReqPlantChange
  has 13 rows — do NOT assume it is อ.9's; อ.9 (ทำลาย/กำจัดอาวุธ) is a different form.

## [--:--] Porter (PM)
- Human ran the doc query for 37940: **zero rows** in T_T_REQUEST_DOC. So either 37940 is not
  an อ.9 request, or that request has no attached documents at all — the checklist-binding
  question (DOCUMENT_ID vs REQUEST_CHECKLIST_ID) is still unanswered.
- Asked the human for two short reads: (1) T_T_REQUEST_SPECIAL.FORM_ID for 37940 (อ.9 should be
  FORM_ID 9 or 10 per RequestTypeResolverService), and (2) the list of REQUEST_IDs with
  FORM_ID IN (9,10) — i.e. which requests really are อ.9. If (2) returns nothing, there is no
  real อ.9 data in UAT at all, which materially changes REQ-014 (nothing to build against or
  verify with).
- @Sober: holding REQ-014 specification until we know we have a real อ.9 request to work from.

## [--:--] Porter (PM)
- **Key schema correction from the human: อ.9 data lives in `T_T_REQUEST_MOVE`, NOT
  `T_T_REQUEST_SPECIAL`.** (T_T_REQUEST_SPECIAL is the อ.6/อ.7 table.) This invalidates the
  assumption in SPEC-013 that อ.9 mirrors the อ.6 sourcing, and explains the earlier empty
  results (we were querying the wrong table family for 37940).
- Asked the human for (a) the T_T_REQUEST_MOVE row/columns for 37940 — the real A9 payload
  source the builder must map — and (b) whether a T_T_REQUEST_SPECIAL row exists for 37940 at
  all, since RequestTypeResolverService resolves "A9" from T_T_REQUEST_SPECIAL.FORM_ID 9/10;
  if there is no such row, the REAL download endpoint cannot even resolve อ.9 today (a separate
  live defect worth stating).
- @Sober: for REQ-014, re-baseline on T_T_REQUEST_MOVE. Please re-check the resolver path for
  อ.9 against this (does FORM_ID 9/10 in T_T_REQUEST_SPECIAL ever exist for MOVE requests, or is
  a9 routed some other way?) and revise SPEC-013/REQ-014's DATA REQUESTs accordingly — the
  earlier ones (destroyLocation, item-5 docs, item-12(2) person2) may map to MOVE columns.

## [--:--] Porter (PM)
- Decisive data from the human for REQ-014:
  1. **T_T_REQUEST_MOVE holds อ.9** — row for 37940 has **FORM_ID = 9**, plus the whole อ.9
     payload: REF_LICENSE_NO/ISSUE/EXPIRY, MOVE_REQUEST_TYPE, PLANT_* (โรงงาน), ORIGIN_* (ต้นทาง),
     BUYER_AUTHORITY_ID + AUTHORITY_NAME/_ABBR, DEST_* (ปลายทาง), START/END + ACTUAL dates,
     MOVE_STATUS, WRITE_OFF_DESTROY_DATE, REF_PRODUCT_TYPE_GROUP_*. This is the A9 source to map.
  2. **T_T_REQUEST_SPECIAL has NO row for 37940** (empty result).
- Consequence to verify: RequestTypeResolverService.resolveChecklistRequestType resolves the
  report type from **T_T_REQUEST_SPECIAL.FORM_ID** only (SPEC-001 §Step 1: 6/7→A6, 9/10→A9).
  If อ.9 requests never have a SPECIAL row (their FORM_ID lives in MOVE), then the REAL
  download endpoint can never resolve "A9" — it falls through to the REQUEST_TYPE switch and
  returns the wrong report or throws. That would be a **live defect** and would explain why
  a9 stayed mock-only.
- @Sober: please CONFIRM this code path before I raise a fix REQ (do not guess): does any
  path read FORM_ID from T_T_REQUEST_MOVE, or is T_T_REQUEST_SPECIAL the only source? What
  does /download/checklist/{enc} actually do today for an อ.9 request like 37940?
  Then re-baseline REQ-014 on T_T_REQUEST_MOVE and revise the DATA REQUESTs (destroyLocation
  likely maps to DEST_*/WRITE_OFF_DESTROY_DATE; item-5/item-12(2) may map to MOVE columns).

## [--:--] Sober (SA Lead)
- Picked up REQ-014 (A9 DB integration; human approved the full build). Wrote SPEC-014.
- ANSWERED Porter's download-endpoint question — CONFIRMED LIVE DEFECT: DocumentController
  case "A9" → a9CheckListReportBuilder.createData(requestId), and createData(String) IGNORES the
  id and returns buildMock. So the REAL /api/v1/download/checklist/{encryptedId} for any อ.9
  (FORM_ID 9/10) returns mock "บริษัท ตัวอย่าง จำกัด" data, not the real report — production-facing.
  The history path has the same mock delegation. REQ-014 fixes it as a byproduct (once createData
  decrypts + builds from DB). Stated at the top of SPEC-014 for the human.
- Design in SPEC-014 mirrors a6's structure: new A9CheckListPreviewBuilder (mock, keeps
  /preview/checklist/a9 green) + A9CheckListReportBuilder → DB (buildFromDb + createDataRaw;
  createData = decrypt→buildFromDb). Mapped the reuse-a6 fields (applicant/law/sig/components/
  persons+STATUS<>'D'/item-7/tick/11.2-safe) vs the a9-specific parts blocked on DRs.
- AUTHORED the DATA REQUEST SQL Porter asked for (#2 destroyLocation, #3 item-5 factory docs,
  #4 item-12(2) person2) + restated #1 — all copy-paste, sample req 37940, results → project-docs
  (PII, gitignored). In SPEC-014 §DATA REQUESTs.
- Board: REQ-014 → IN_SPEC (BE TASK blocked on DR#1-4); REQ-013 scope RESOLVED (→ REQ-014).
- @Porter: (1) hand the human DR#1-4 SQL from SPEC-014; (2) state the A9-download mock defect to
  the human. Once the DRs land I'll spec the build TASK(s) + the a9 SQL/field-mapping doc, and
  REQ-013 seam as the final step. Mock /preview/checklist/a9 stays green throughout.

## [--:--] Porter (PM)
- Received SPEC-014. Relayed the CONFIRMED LIVE DEFECT to the human in plain terms: the real
  /api/v1/download/checklist/{enc} for any อ.9 returns MOCK data ("บริษัท ตัวอย่าง จำกัด")
  because createData ignores the requestId — production-facing; REQ-014 fixes it as a byproduct.
- @Sober — DO NOT re-ask the human for DR#1/#2/#3; they are already answered by data they ran
  earlier (all in this log):
  - **DR#1 = ANSWERED, and the answer is "there is no อ.9 group".** T_S_REQUEST_CHECKLIST has
    only 8 groups (A1 15, A3 22, BgChk 7, ReqExpand 11, ReqOpen 10, ReqPersonChange 10,
    ReqPlantChange 13, ReqSpecial 9) — checked WITHOUT the IS_ACTIVE filter too, so the rows
    don't exist at all. Note ReqPlantChange also has 13 rows but is a different form — do not
    assume it is อ.9's.
  - **DR#3 (request docs) = ANSWERED: T_T_REQUEST_DOC has ZERO rows for 37940.**
  - **DR#2:** T_T_REQUEST_SPECIAL has no row for 37940 either; the อ.9 payload is the
    T_T_REQUEST_MOVE row I already pasted you (DEST_PLACE_NAME/DEST_* address block,
    WRITE_OFF_DESTROY_DATE, ORIGIN_*, PLANT_*, AUTHORITY_*) — please map destroyLocation from
    those columns and tell me if none fit.
  - Only **DR#4 (person2 PER_TYPE)** is genuinely open — I've asked the human for it.
- **New question this raises for you:** with no evidence master and no doc rows for 37940, where
  do อ.9's 13 evidence items + their ticks come from at all? Options: (a) master must be seeded
  (human/DBA work — we don't write DBs), (b) labels fixed in the report + ticks bound by
  DOCUMENT_ID instead of REQUEST_CHECKLIST_ID, (c) อ.9 evidence isn't document-driven and comes
  from MOVE columns. Please assess and tell me which, with what it costs — this decides whether
  REQ-014 can proceed at all or needs the human to create master data first.

## [--:--] Porter (PM)
- **DR#4 ANSWERED:** T_T_REQUEST_PER for 37940 has PER_TYPE 1 (ID 92136) and PER_TYPE 2
  (ID 92135) — same 1/2 convention as อ.6. All four DATA REQUESTs are now answered (or
  answered-as-absent). @Sober: map item-12(2) person2 accordingly and confirm which PER_TYPE
  is ผู้รับอาวุธ for the อ.9 form.
- ⚠️ **NULL-STATUS trap spotted in that same result — flagging as a probable defect risk:**
  both อ.9 person rows have **`STATUS = null`** (not 'A'). Our REQ-010 fix filters persons with
  `findByRequestIdAndPerTypeAndStatusNotOrderByIdAsc(..., "D")` → SQL `STATUS <> 'D'`, and in
  Oracle `NULL <> 'D'` evaluates to UNKNOWN, so **NULL-status rows are excluded**.
  - For อ.9 (REQ-014): reusing the a6 person query as-is would render items 3/4 with **no
    persons at all** for 37940. Must be handled in the design.
  - For **อ.6 (REQ-010, already delivered)**: any request whose person rows have NULL STATUS
    would **silently drop those people from the form** — a regression we may have introduced.
    38272's rows were 'A'/'D' so our test didn't cover it.
- @Sober: please (1) confirm the NULL semantics in the generated SQL, (2) assess the อ.6
  exposure (is `STATUS IS NULL OR STATUS <> 'D'` the correct rule?), and (3) tell me if this
  warrants a fix REQ for อ.6 — I'll raise it immediately if so. Also fold the correct rule into
  the REQ-014 design.
- Suggested DATA REQUEST for me to send the human if you want the exposure measured:
  a count of T_T_REQUEST_PER rows by STATUS (including NULL) across requests.

## [--:--] Sober (SA Lead)
- Processed the DR results + Porter's 3 asks. Three findings; SPEC-014 updated.

- (1) ⚠️ NULL-STATUS trap — CONFIRMED, and it's an อ.6 regression WE introduced (REQ-010).
  * Semantics confirmed: derived `...StatusNotOrderByIdAsc(...,"D")` → SQL `status <> 'D'`; in Oracle
    `NULL <> 'D'` = UNKNOWN → NULL-status rows EXCLUDED. 37940's persons are STATUS=NULL → a6-style
    query returns them empty.
  * อ.6 exposure: any อ.6 request with NULL-status active persons silently drops them from items 3/4.
    38272 was 'A'/'D' so our test missed it. YES this warrants a fix.
  * Correct rule = `(status IS NULL OR status <> 'D')` — not expressible as a derived name → use a
    @Query (JPQL). Same latent trap in the pre-existing doc query findByRequestIdAndStatusNot (drops
    NULL-status docs → could hide evidence) — assess when touched.
  * @Porter: please RAISE AN อ.6 FIX REQ now (REQ-010 follow-up): switch the person filter to the
    IS-NULL-safe rule. I'll spec it small. I've folded the correct rule into SPEC-014.

- (2) destroyLocation MAPPED: `T_T_REQUEST_MOVE.DEST_PLACE_NAME` (+ DEST_* for the full address).

- (3) 🚫 BIG: อ.9 is NOT the a6 data model — REQ-014 can't proceed as specced.
  * DR#1: NO อ.9 checklist group exists (only 8 groups; อ.6=ReqSpecial; อ.9 none). DR#3: T_T_REQUEST_DOC
    ZERO rows for 37940. DR#2: no T_T_REQUEST_SPECIAL row either — อ.9 payload is T_T_REQUEST_MOVE.
  * So the whole อ.9 evidence section (13 items + ticks) has NO document/checklist source for the sample.
    Porter's options: (a) seed master (DBA/human work — we don't write DBs), (b) fixed labels + ticks by
    DOCUMENT_ID (but 0 docs → all false), (c) evidence from MOVE columns.
  * SA assessment: can't choose from ONE sample that has no docs/no special row/NULL persons — 37940
    looks incomplete or a different flow. RECOMMEND: re-scope REQ-014 → an อ.9 INVESTIGATE/data-model
    REQ first (REQ-001-style). Human/BA to give a COMPLETE อ.9 sample (docs populated) OR state the
    evidence business model. Building now = guessing page 2-3. Set REQ-014 → BLOCKED.

- (4) person2 (item 12(2) ผู้รับอาวุธ): 37940 has only PER_TYPE 1 & 2 (= items 3/4, same as a6) — no
  distinct receiver type. person2 source UNRESOLVED — need a sample with a receiver or confirm it comes
  from T_T_REQUEST_MOVE. (Folded into the investigate ask.)

- Board: REQ-014 → BLOCKED; added rows for the a6 NULL regression, the evidence-model gap, person2.
- @Porter: net — (i) raise the อ.6 NULL fix REQ (delivered defect), (ii) REQ-014 needs an investigate
  phase + a complete อ.9 sample before any build; the download-mock defect still stands (REQ-014 closes
  it once we can actually build อ.9 from DB).

## [--:--] Porter (PM)
- Acted on Sober's three findings:
  1. **Raised REQ-015 (HIGH)** — fix the NULL-STATUS regression WE introduced in REQ-010:
     person filter must be `(STATUS IS NULL OR STATUS <> 'D')` (JPQL @Query; not expressible
     as a derived name), plus assess the same trap in the document query
     (findByRequestIdAndStatusNot dropping NULL-status docs). Owning this openly — our 38272
     test only had 'A'/'D' rows, so we shipped อ.6 with people silently droppable.
  2. **Raised REQ-016 (HIGH)** — อ.9 data-model investigation (REQ-001-style) before any build:
     no checklist master for อ.9, zero docs on 37940, payload in T_T_REQUEST_MOVE, person2
     source unresolved. REQ-014 → BLOCKED on REQ-016; REQ-013 stays blocked behind REQ-014.
  3. Recorded destroyLocation = `T_T_REQUEST_MOVE.DEST_PLACE_NAME` (+ DEST_* address block).
- @Sober: REQ-015 and REQ-016 are ready. REQ-015 first (delivered-defect, small); REQ-016 needs
  the human's complete อ.9 sample — I'm asking for it now.
- Asked the human for: a COMPLETE/representative อ.9 request id (with documents attached, and a
  receiver person if the form has one), or a statement of the อ.9 evidence business model.
- Reported both items to the human in Thai, including that the อ.6 NULL issue is our own
  regression and that the อ.9 download-returns-mock defect still stands.

## [--:--] Porter (PM)
- **REQ-016 unblocked on the sample question:** the human's scan shows อ.9 requests DO have
  documents — hundreds of them (top: **33630 = 14 docs**, 33533 = 13, 35192 = 13, many at 10-12).
  **37940 was simply an incomplete request**, not evidence that อ.9 lacks a document model.
  Adopting **33630 as the primary complete อ.9 sample**.
- Asked the human for 33630's full T_T_REQUEST_DOC rows (REQUEST_CHECKLIST_ID / DOCUMENT_ID /
  DOCUMENT_TYPE / DOCUMENT_NAME / ATTACH_FILE_ID / REF_ID / STATUS). That single result decides
  the evidence-model question (a/b/c) in REQ-016: whether อ.9 evidence binds by
  REQUEST_CHECKLIST_ID (and to which master, since no อ.9 group exists) or by DOCUMENT_ID.
- @Sober: REQ-016's blocking premise ("no docs for อ.9") is WRONG — it was a bad sample. Re-plan
  the investigation around 33630. The evidence-master gap may still be real (no อ.9 GROUP_CODE),
  so the DOCUMENT_ID-binding option (b) is now the leading hypothesis — confirm against 33630's
  rows when I post them.

## [--:--] Porter (PM)
- **REQ-016 evidence-model question ANSWERED** by sample 33630 (14 doc rows) + a human ruling:
  1. **อ.9 binds evidence by `DOCUMENT_ID`, NOT by REQUEST_CHECKLIST_ID.** Every row of 33630 has
     `REQUEST_CHECKLIST_ID = NULL` with varied `DOCUMENT_ID` (18, 48-55, 95-99), `DOCUMENT_NAME`
     NULL, `STATUS='A'`. ⇒ option (b). **No checklist master needs to be seeded** — the earlier
     "no อ.9 GROUP_CODE" finding is a non-issue for the build.
  2. **Tick rule = SAME as อ.6** (real attachment / REQ-009 `getAttachFile() != null`). Human's
     ruling on the all-NULL ATTACH_FILE_ID in 33630: *"ATTACH_FILE_ID เหมือนเดิม เพียงแค่ว่า A9
     ที่ database ต่อไปเขายังไม่มีข้อมูลให้เฉยๆ เลยเป็น null"* — i.e. that data simply isn't
     populated yet; do NOT invent a "row exists = ticked" rule.
  3. Also note `REF_ID` is NULL on these rows (อ.6 uses REF_ID for person docs) — factor in.
- @Sober: REQ-016's blockers are down to ONE — **the `DOCUMENT_ID` → อ.9 form-item mapping**
  (which DOCUMENT_ID is item 1..13, incl. item 12 (1)-(9)). Please find the source from CODE
  first (is there a document-master entity/table, e.g. a T_S_/T_M_ document table, or an enum /
  constants map? อ.6 used DOCUMENT_ID 102/103 for person docs, so some mapping convention
  exists). If code doesn't hold it, tell me exactly what to ask the human for and I'll raise the
  DATA REQUEST. Everything else for REQ-014 is now unblocked: payload = T_T_REQUEST_MOVE,
  destroyLocation = DEST_PLACE_NAME, persons = T_T_REQUEST_PER (with the REQ-015 NULL-safe rule),
  evidence = T_T_REQUEST_DOC by DOCUMENT_ID + REQ-009 tick.
- Primary อ.9 sample for the build/QA = **33630**; 37940 is incomplete, keep as an edge case.

## [--:--] Porter (PM)
- **BUSINESS RULING from the human — this settles REQ-016's last unknown:**
  *"เรื่องพวกนี้ ฉันว่าเหมือนกันได้เลยนะ แต่เพียงว่า พี่ที่ทำข้อมูลให้ อาจจะยังไม่ทำให้ก็ได้"*
  ⇒ **อ.9 uses the SAME model as อ.6** (T_S_REQUEST_CHECKLIST master group +
  T_T_REQUEST_DOC.REQUEST_CHECKLIST_ID binding + REQ-009 real-attachment tick). The missing
  อ.9 GROUP_CODE and the NULL REQUEST_CHECKLIST_ID / NULL ATTACH_FILE_ID we saw are simply
  **data not yet seeded by the data team** — NOT a different design.
- Consequences for the build:
  * Build อ.9 mirroring อ.6 — do NOT invent a DOCUMENT_ID-based evidence mechanism, and do NOT
    hardcode the 48-55/95-99 ids. (Supersedes my earlier "option (b) is leading" note.)
  * The code must **degrade gracefully while the data is missing**: no master group / NULL
    REQUEST_CHECKLIST_ID / NULL attachment ⇒ blank labels + unticked boxes, never an exception.
    Rendering an empty evidence section for อ.9 right now is CORRECT behavior, not a defect.
  * End-to-end อ.9 verification of pages 2–3 is therefore **gated on the data team seeding**
    the อ.9 checklist group + backfilling REQUEST_CHECKLIST_ID — a human-side dependency, not
    a blocker for writing the code.
- @Sober: REQ-016 can close on this ruling — re-plan REQ-014 as "อ.6 pattern applied to อ.9,
  sourcing the applicant block from T_T_REQUEST_MOVE". Two things I still need from you:
  (1) what **GROUP_CODE** value the code should look for (the data team will need to be told
  exactly what to name it — propose one, e.g. the อ.9 analogue of 'ReqSpecial'), and the
  SEQ→form-item mapping the 13 items require; (2) confirm the graceful-degradation behavior is
  in the design. I'll then hand the human a precise "please seed this" spec for their data team.
- Sample: 33630 (complete-ish, 14 doc rows) primary; 37940 edge case.

## [--:--] Sober (SA Lead)
- Picked up REQ-015 + REQ-016 (both from my earlier findings).
- REQ-015 (NULL-status person fix) → SPEC-015 + TASK-007. Fix = replace the derived person query
  with a JPQL @Query `(p.status IS NULL OR p.status <> 'D')` → findActivePersons; buildPersons drops
  the "D" arg. 11.2-safe. QA proof: /a6/db/37940 (NULL persons) now appear; 38272 unchanged.
- AC#3 doc-query audit: `findByRequestIdAndStatusNot` (+ the RefId person-doc variant) is used by
  ALL 7 report builders (a1/a3/a6/expand/open/personChange/planChange) — same NULL trap, so
  NULL-status DOCS would be dropped from EVERY report's evidence. It's PRE-EXISTING (not our REQ-010
  regression) and cross-report → higher risk. Scoped OUT of REQ-015. @Porter: recommend a SEPARATE
  project-wide NULL-safe REQ for the doc queries (+ optional DATA REQUEST to measure NULL-status docs).
- REQ-016 (อ.9 data-model investigation) → SPEC-016, BLOCKED. Pinned what code can tell (a9 field
  inventory; destroyLocation = T_T_REQUEST_MOVE.DEST_PLACE_NAME; persons use the NULL-safe rule) but
  the CORE unknown — where อ.9's 13 evidence items + ticks come from — is undecidable from 37940 (no
  docs/no special row/NULL persons → looks incomplete). Wrote the DATA REQUEST: a COMPLETE อ.9 sample
  (with docs + receiver) → dump T_T_REQUEST_MOVE + docs + persons, OR the evidence business model;
  confirm 37940 is/ isn't complete. No build until then (building pages 2-3 now = guesswork).
- Board: REQ-015 → IN_SPEC (Jason/TASK-007); REQ-016 → BLOCKED; TASK-007 → TODO; added doc-query
  cross-report row.
- @Jason: TASK-007 ready (small NULL-safe @Query + one call-site). Ping me for REVIEW.
- @Porter: (1) route TASK-007's QA proof after Jason; (2) raise the separate doc-query NULL REQ;
  (3) get the human a complete อ.9 sample / evidence model to unblock REQ-016 → REQ-014.

## [17:45] Jason (BE)
- Picked up TASK-007 (TODO → IN_PROGRESS → REVIEW). Fixed the REQ-010 NULL-STATUS regression.
- RequestPerRepository: replaced the derived StatusNot method with a NULL-safe @Query
  findActivePersons (WHERE requestId & perType AND (status IS NULL OR status <> 'D') ORDER BY id).
  buildPersons calls it (dropped STATUS_DELETED arg). Plain WHERE → 11.2-safe. STATUS_DELETED
  still used by the doc queries (no orphan). Doc-query NULL-trap left for the separate cross-report REQ.
- Evidence: ./mvnw -o -DskipTests compile → BUILD SUCCESS; git scope = RequestPerRepository +
  A6CheckListReportBuilder only.
- @Sober: TASK-007 ready for review. QA leg (real UAT DB): /a6/db/37940 → NULL-status persons now
  appear in items 3/4; /a6/db/38272 → still only 92567-69 ('D' excluded). Please route to Tanya to
  close REQ-015.

## [--:--] Sober (SA Lead)
- Reviewed TASK-007 (REVIEW → DONE). Verified independently: RequestPerRepository.findActivePersons
  = JPQL @Query (p.status IS NULL OR p.status <> 'D') ORDER BY p.id — keeps NULL + non-'D', excludes
  only 'D'; field names match RequestPerEntity; 11.2-safe. buildPersons call site updated;
  STATUS_DELETED still used 3× by the doc queries (no orphan); doc-query trap correctly left to the
  separate cross-report REQ. Scope = 2 files. Re-ran compile → exit 0 (JPQL validates at Hibernate
  bootstrap on QA's run). Verdict in TASK-007 §Review.
- REQ-015 code DONE → SPEC_DONE.
- @Porter: route the close proof to Tanya (dev): /a6/db/37940 → its NULL-status persons now APPEAR
  in items 3/4 (were dropped by the <> 'D' regression); /a6/db/38272 → unchanged (only 92567/68/69,
  'D' excluded). TEST_PASSED closes REQ-015. Standing reminders: (i) raise the separate cross-report
  doc-query NULL REQ; (ii) get the human a complete อ.9 sample to unblock REQ-016 → REQ-014.

## [--:--] Porter (PM)
- Acted on Sober's three asks:
  1. **REQ-015 routed to Tanya (IN_TEST).** @Tanya: build the current tree, dev profile, own port,
     read-only. (a) `/a6/db/37940` → its NULL-status persons now APPEAR in items 3/4 (they were
     dropped by our REQ-010 regression); (b) `/a6/db/38272` → unchanged, only 92567/92568/92569
     ('D' still excluded). TEST_PASSED closes REQ-015.
  2. **Raised REQ-017 (HIGH)** — the cross-report doc-query NULL trap SA found:
     `findByRequestIdAndStatusNot` (+ RefId variant) → `STATUS <> 'D'` drops NULL-status DOCS, and
     it is used by **all 7 builders (a1/a3/a6/expand/open/personChange/plantChange)**. Pre-existing,
     not ours, but live and wide. Included the optional sizing query (count docs by STATUS).
  3. อ.9: see below — REQ-016's blocker is already answered in this log.
- @Sober: **REQ-016 is NOT blocked any more — please re-read my two entries above yours.** The
  human already supplied (a) a complete sample list (33630 = 14 docs, 33533/35192 = 13, hundreds
  more; 37940 was just an incomplete request) and (b) the business ruling: **อ.9 uses the SAME
  model as อ.6** — the missing อ.9 GROUP_CODE / NULL REQUEST_CHECKLIST_ID / NULL ATTACH_FILE_ID
  are just data the data team hasn't seeded yet, not a different design. So: build อ.9 mirroring
  อ.6 (checklist master + REQUEST_CHECKLIST_ID + REQ-009 tick), applicant block from
  T_T_REQUEST_MOVE, destroyLocation = DEST_PLACE_NAME, persons via the REQ-015 NULL-safe rule,
  and **degrade gracefully while the data is absent** (blank + unticked, never an exception).
  Still needed from you: the GROUP_CODE value the code should look for + the SEQ→13-item mapping,
  so I can hand the human a precise "please seed this" spec for their data team.

## [--:--] Porter (PM)
- Sized REQ-017 with a human DATA REQUEST. T_T_REQUEST_DOC.STATUS distribution:
  A 24,933 · C 8,884 · W 7,070 · R 122 · **(null) 215** · D 109.
  ⇒ **215 document rows are currently dropped from every report** by the NULL trap. Real but
  bounded; the fix is still correct and cheap.
- I raised the wider question (should C/W/R count as valid? 16,076 rows ride on `<> 'D'`).
  **Human ruling: *"ทุกสถานะ ยกเว้น D ถ้า null ก็เอาขึ้นปกติ"*** — allow everything except 'D',
  and include NULL. Recorded in REQ-017 §Status semantics.
- ⇒ REQ-017 needs NO scope change: the intended rule is exactly `(STATUS IS NULL OR STATUS <> 'D')`,
  same shape as the REQ-015 person fix; C/W/R keep their current (included) behavior.
- @Sober: REQ-017 is fully specified and unblocked — proceed to SPEC/TASK. Reminder that this
  touches the shared doc queries used by all 7 builders, so please state the call-site audit and
  the regression set for QA (อ.6 at minimum) in the SPEC.

## [--:--] Porter (PM)
- Human context on the NULL-status docs: *"null น่าจะเอกสารเก่าๆ ที่ทำไว้ แบบมีปัญหาเฉยๆ
  เอกสารใหม่ๆ ล่าสุด มักจะไม่มีแล้ว"* — legacy rows from a past problem; current documents no
  longer produce NULL status.
- PM call: **REQ-017 HIGH → MEDIUM**, queued **after** the อ.9 work (REQ-014). Rationale recorded
  in the REQ: not an ongoing problem, but still worth fixing (old requests can be re-printed, and
  the fix is the same small shape as REQ-015). Board + REQ file updated.
- @Sober: priority order is now (1) อ.9 — REQ-016 close → REQ-014 build, (2) REQ-017. REQ-015 is
  already with Tanya.

## [--:--] Porter (PM)
- Stakeholder decision: **REQ-017 is CANCELLED (won't fix)** — *"A9 เลย เรื่อง null ไม่ต้องแก้เลย
  ปล่อยไปเลย"*. NULL-status docs are legacy-only; they accept the behavior and want the effort on อ.9.
  Marked CANCELLED on the board and in the REQ (kept on file with the accepted consequence
  recorded: 215 doc rows stay invisible to all 7 reports if an old request is re-printed).
- Also re-confirmed the document status rule with the human before cancelling: include
  **A / C / W / R / null**, exclude **only 'D'** — and separately, the ✓ tick still requires a real
  attachment (REQ-009). My understanding was confirmed correct.
- @Sober: **DO NOT spec REQ-017.** Priority is now singular: **อ.9** — close REQ-016 using the
  data already in this log (33630 sample + the human's ruling that อ.9 follows the อ.6 model with
  data merely unseeded), then REQ-014 build, then REQ-013 seam.
- NOTE: REQ-015 (person NULL fix) is **NOT** cancelled — it is our own REQ-010 regression, already
  coded + SA-reviewed, and sits with Tanya for the close proof.

## [--:--] Sober (SA Lead)
- CLOSED REQ-016 on the 33630 sample + the human ruling (SPEC-016 RESOLUTION). Go/no-go = GO.
  Answer: อ.9 uses the SAME model as อ.6 (T_S_REQUEST_CHECKLIST master + REQUEST_CHECKLIST_ID +
  REQ-009 tick); the missing GROUP_CODE / NULL REQUEST_CHECKLIST_ID / NULL attachment = data the
  data team hasn't seeded yet, NOT a different design. 37940 = incomplete edge case; primary = 33630.
- Answered Porter's two asks:
  * Q1 (seed spec): proposed **GROUP_CODE = 'ReqMove'** (อ.9 analogue of a6's 'ReqSpecial') + a
    SEQ→13-item mapping table (20 tickable document rows; persons items 3/4 & 12(2) are NOT master).
    Full "please seed this" table in SPEC-016 — hand to the data team once you confirm the name.
  * Q2 (graceful degradation): CONFIRMED in the design — empty master / NULL binding / NULL
    attachment → blank labels + unticked, never throw. An empty อ.9 evidence section now is CORRECT
    (gated on the data team seeding), not a defect.
- Field-source map for อ.9 delivered (SPEC-016): applicant/page-1 = T_T_REQUEST_MOVE
  (destroyLocation=DEST_PLACE_NAME); persons via REQ-015 NULL-safe rule; law/sig/components/item-7
  reuse a6. Open (non-blocking): person2 (item 12(2)) PER_TYPE still unconfirmed → renders empty gracefully.
- Re-planned REQ-014 (SPEC-014 → UNBLOCKED): build a9 mirroring a6 (new A9CheckListPreviewBuilder for
  the mock preview + A9CheckListReportBuilder→DB; createData=decrypt→buildFromDb ALSO fixes the live
  download-mock defect) + graceful degradation.
- Board: REQ-016 → SPEC_DONE; REQ-014 → IN_SPEC (build TASK pending your GROUP_CODE confirm); REQ-013
  still trails REQ-014.
- @Porter: (1) confirm GROUP_CODE='ReqMove' (or the data team's preferred name) — then I set the
  constant + write the a9 build TASK; (2) hand the data team the seed spec (SPEC-016). Build can start
  in parallel (graceful) — the seeding just lights up evidence pages 2-3 later.

## [--:--] Porter (PM)
- Nudge → did Porter's waiting work.
- **Accepted REQ-016 → DELIVERED.** SPEC-016's RESOLUTION answers it: GO; อ.9 = the อ.6 model
  (evidence via T_S_REQUEST_CHECKLIST + REQUEST_CHECKLIST_ID + REQ-009 tick), applicant block from
  T_T_REQUEST_MOVE, destroyLocation = DEST_PLACE_NAME, persons NULL-safe (REQ-015); the "missing"
  bits are unseeded data, not a different design; 37940 incomplete, primary sample = 33630;
  graceful degradation confirmed in the design.
- **Delivered the seed spec to the human** as a standalone Thai hand-off document:
  `project-docs/A9-checklist-seed-spec-for-data-team.md` — GROUP_CODE='ReqMove' (to be confirmed),
  the 20 SEQ→form-item rows with CODE_NAMEs, the REQUEST_CHECKLIST_ID backfill instruction, what
  NOT to seed (persons/items 3,4,12(2); the "เอกสารอื่นๆ" line), and the two open questions
  (GROUP_CODE name; person2 PER_TYPE).
- Board housekeeping: REQ-014 DR#1-4 row → RESOLVED; doc-query NULL trap row → CLOSED (REQ-017
  cancelled); REQ-016 rows updated to point at the hand-off doc.
- @Sober: REQ-016 accepted. **You may start the REQ-014 build TASK now** on the design as specced —
  do NOT wait for the data team: the constant can be set/changed when the human confirms the
  GROUP_CODE name, and graceful degradation means the unseeded evidence section is expected.
  Flag if you'd rather hold the evidence part until the name is confirmed.
- Still open with the human: (i) GROUP_CODE name confirmation, (ii) person2 (item 12(2)) source,
  (iii) 4 อ.6 REQs (009/010/011/012) + REQ-015 awaiting Tanya's formal close.

## [--:--] Porter (PM)
- Human's answers: (1) the อ.9 checklist master seeding is **in progress on their side** — their
  colleague is preparing the data; we wait. (2) Pointed the team at a **Google Sheets DATA
  DICTIONARY** for the DIDPERMIT schema.
- Tried to fetch the sheet: **login-restricted**, only table names visible. Confirmed from the
  visible list: `T_S_REQUEST_CHECKLIST` = "ข้อมูล Master เอกสารหลักฐานในการยื่นคำขอต่างๆ",
  `T_T_REQUEST_PER` = person section, and อ.9 ↔ `T_T_REQUEST_MOVE` — consistent with our findings,
  but no column/value detail.
- Asked the human to **export it (File → Download → .xlsx) into `project-docs/`** (gitignored).
  Once it lands, Sober can read the dictionary directly and self-answer: PER_TYPE value set
  (→ person2 / item 12(2)), the existing GROUP_CODE values (an อ.9 group may already be named,
  which could replace our proposed 'ReqMove'), and the full T_T_REQUEST_MOVE column list.
- @Sober: hold the evidence-section constant ('ReqMove') as provisional until either the data
  dictionary lands or the human's data team confirms the name. Everything else in REQ-014 (the
  applicant block from T_T_REQUEST_MOVE, persons, law refs, signatures, components, and the
  createData decrypt→DB fix that also closes the download-mock defect) is unaffected — proceed.

## [--:--] Porter (PM)
- Human exported the DIDPERMIT data dictionary; I filed it as
  **`project-docs/DIDPERMIT-data-dictionary.xlsx`** (4.4 MB, 94 sheets, gitignored).
- Sanity-checked the hand-off is useful (occurrence counts in sharedStrings, no analysis):
  `T_S_REQUEST_CHECKLIST` ×3, `T_T_REQUEST_MOVE` ×4, `T_T_REQUEST_PER` ×2, `PER_TYPE` ×1,
  `GROUP_CODE` ×23 — everything REQ-014 is missing should be in there.
- @Sober: the data dictionary is in project-docs. Please read it directly (xlsx) and self-answer
  the three open items, then update SPEC-014/016 and start/adjust the build TASK:
  1. **`PER_TYPE` value set** → the source for item 12 (2) person2 (ผู้รับอาวุธ).
  2. **The existing `GROUP_CODE` values** → an อ.9 group may ALREADY be named in the dictionary;
     if so use it and drop my provisional 'ReqMove' (and tell me so I can correct the seed spec
     I gave the human's data team).
  3. **Full `T_T_REQUEST_MOVE` column definitions** → finalise the อ.9 page-1 field mapping
     (destroyLocation currently = DEST_PLACE_NAME).
  Raise a DATA REQUEST only for what the dictionary genuinely doesn't answer.
- Note for the human's side: their colleague is still preparing the checklist master data; that
  remains the only hard dependency for อ.9 pages 2-3.

## [--:--] Sober (SA Lead)
- Read the DIDPERMIT data dictionary (project-docs/DIDPERMIT-data-dictionary.xlsx, human-provided
  reference — not a DB hit) and self-answered Porter's 3 items. Findings in SPEC-016.
  1. **person2 (item 12(2) ผู้รับอาวุธ) = T_T_REQUEST_EXAMPLE_SIGN** ("ข้อมูลตัวอย่างลายมือชื่อผู้รับอาวุธ")
     — its OWN table, NOT T_T_REQUEST_PER. Cols: EXAMPLE_SIGN_TYPE(1/2/3), PERSON_NAME_*, ID_CARD_NO,
     ATTACH_FILE_ID (→ REQ-009 tick), STATUS. ⇒ new entity/repo in the build.
  2. **PER_TYPE = 1/2** for items 3/4 (same as a6); receiver is NOT in PER.
  3. **No existing อ.9 GROUP_CODE** in T_S_REQUEST_CHECKLIST (free master data) → keep proposed
     'ReqMove' as the new seed (data team confirms/creates the name).
  + **T_T_REQUEST_MOVE page-1 map** (table desc literally "อ.9"): destroyLocation=DEST_PLACE_NAME,
     MOVE_REQUEST_TYPE (2=ทำลาย), WRITE_OFF_DESTROY_DATE→item12(1), PLANT/ORIGIN/DEST blocks,
     REF_LICENSE_* for item 5, START/END dates (possible item-7 duration).
- Wrote TASK-008 (the a9 build): split mock → A9CheckListPreviewBuilder; A9CheckListReportBuilder→DB
  (createData decrypt→buildFromDb = also fixes the download-mock defect); applicant from
  T_T_REQUEST_MOVE; persons via REQ-015 NULL-safe; evidence mirror a6 (master 'ReqMove' + doc +
  REQ-009 tick); person2 from new T_T_REQUEST_EXAMPLE_SIGN; new RequestMove/RequestExampleSign
  entities+repos; **graceful degradation mandatory**; + the REQ-013 seam (/a9/db/{id}) as §4.
- Board: REQ-014 & REQ-013 → IN_SPEC (Jason/TASK-008); TASK-008 → TODO; person2 row RESOLVED.
- @Jason: TASK-008 is the big one — use A6CheckListReportBuilder as the template; graceful
  degradation is mandatory (empty master/NULL binding → blank+unticked, never throw). QA proof on
  /a9/db/33630. Flag permitDuration source / item-12 MOVE mapping for me if unsure. Ping for REVIEW.
- @Porter: 3 data items resolved from the dictionary (no new DATA REQUEST). Still needed from the
  human side: the data team seeds the 'ReqMove' master + backfills REQUEST_CHECKLIST_ID (lights up
  อ.9 evidence pages 2-3). Confirm the GROUP_CODE name so I can lock the constant.

## [18:25] Jason (BE)
- Picked up TASK-008 (TODO → IN_PROGRESS → REVIEW). อ.9 DB integration — mirrored a6.
- Split mock into A9CheckListPreviewBuilder (preview unchanged); A9CheckListReportBuilder → DB
  (createData decrypts → buildFromDb, which ALSO fixes the live download-mock defect; createDataRaw
  + previewA9Db seam added). page-1 from T_T_REQUEST_MOVE; law/sig/components/persons/permitDuration
  reuse a6; persons NULL-safe (REQ-015); evidence 13 items bound to master 'ReqMove' by SEQ + REQ-009
  tick; item-13 dynamic (REQ-011); item-12(2) person2 from NEW T_T_REQUEST_EXAMPLE_SIGN. Graceful
  degradation throughout (empty master/NULL/missing MOVE → blank+unticked, never throw).
- NEW: RequestMoveEntity/Repo, RequestExampleSignEntity/Repo (attachFile @NotFound IGNORE, NULL-safe
  @Query). A9 history builder unchanged (delegates to createData — same as a6 history).
- Evidence: compile BUILD SUCCESS; app BOOTS clean on :33097 (entities + JPQL + all beans validate at
  bootstrap, no error). Scope = PreviewController + A9CheckListReportBuilder + 5 new files; a6 untouched.
- @Sober: TASK-008 ready for review. 3 flags in the TASK Questions: Q1 permitDuration source
  (LICENSE.PERIOD_TEXT vs MOVE dates — confirm at 33630), Q2 evidence tick SEQ-mapping (graceful until
  data team seeds 'ReqMove'; item-5 sub-doc date-inlines not fabricated), Q3 heads-up: PreviewController
  @Profile("dev") is currently COMMENTED OUT in the working tree (REQ-004 gate disabled) — left as-found,
  out of my scope, please route to the REQ-004 owner. QA leg: /a9/db/33630 + 37940 (dev profile).

## [--:--] Sober (SA Lead)
- Reviewed TASK-008 (REVIEW → DONE) — the big a9 build. Verified independently:
  * Scope: PreviewController + A9CheckListReportBuilder + 5 new (RequestMove/RequestExampleSign
    entities+repos, A9CheckListPreviewBuilder). The a6/RequestPerRepository mods in the tree are the
    earlier TASK-007 work, NOT TASK-008 — a6 logic untouched.
  * createData(enc)=buildFromDb(decrypt) → FIXES the live download-mock defect; createDataRaw for the seam.
  * Preview split: previewA9()→A9CheckListPreviewBuilder.createPreviewData() (mock kept green).
  * Graceful degradation: move=findByRequestId().orElse(null) + null-guards (destPlaceName, MOVE_REQUEST_TYPE);
    master 'ReqMove' empty → ticks false; only missing T_T_REQUEST throws (same as a6).
  * NULL-safe/11.2: persons via TASK-007 findActivePersons; new RequestExampleSignRepository @Query
    (status IS NULL OR status <> 'D'); license/sig list+firstOrNull. No FETCH FIRST.
  * Seam previewA9Db /checklist/a9/db/{id} → createDataRaw → exportPdfA9 (REQ-013).
  * Re-ran compile → exit 0; accepted Jason's clean boot (validates entities/JPQL/beans).
- REQ-014 + REQ-013 code DONE → both SPEC_DONE.
- @Porter: route the combined a9 close proof to Tanya (dev): /a9/db/33630 → 200 อ.9 PDF, page-1 from
  T_T_REQUEST_MOVE (destroyLocation etc.), persons/law/sig/components present; evidence pages 2-3
  blank/unticked (master 'ReqMove' unseeded — EXPECTED, not a defect); /a9/db/37940 → 200 graceful;
  /preview/checklist/a9 mock unchanged. Also confirm what permitDuration prints on 33630 (code uses
  T_T_LICENSE.PERIOD_TEXT; MOVE START/END is the alt — small follow-up if wrong).
- Standing: the data team seeds GROUP_CODE='ReqMove' (SPEC-016 table) + backfills REQUEST_CHECKLIST_ID
  to light up อ.9 evidence pages 2-3 — human-side, graceful until then. Confirm the name → I lock the constant.
