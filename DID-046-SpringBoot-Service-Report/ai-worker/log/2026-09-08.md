# Log — 2026-09-08

## Sober (SA) — DEF-26 (PER_TYPE partition) spec'd → TASK-054 (Jason, 4 builders); SPEC-041 corrected
Porter relayed the stakeholder's corrected rule (found by READING SPEC-041): item 4 ⟸ PER_TYPE=2, item 3 ⟸ everything
else INCL NULL. Current code exact-matches `=1`/`=2` (`findActivePersons`) → non-1/2/NULL people vanish from a permit
form (DEF-21 shape). Full-sweep approved: a4/a6/a14/a9-base (transport/destroy/a15).
→ **TASK-054 (Jason):** preferred fix = fetch `findActivePersonsAll` ONCE, partition in Java (item4 = perType==2, item3 =
complement) → structural guarantee no one is in neither/both, sidesteps Oracle `<>2`-on-NULL trap. Required: partition
unit test (assert exactly-one-bucket + fail-on-revert), mocks updated w/ a non-1/2 person, STATUS filter intact.
Updated SPEC-041 items 3/4 rule + Verify SQL (no PER_TYPE filter; CASE shows the bucket) + noted the doc surfaced the bug.
**Ball → Jason (TASK-054).**

## Jason (BE) — TASK-054 / DEF-26 done: PER_TYPE partitioned in Java across all 4 builders
One `findActivePersonsAll` fetch per build plus `partitioningBy(p -> perType != null && perType == 2)`;
`buildPersons` now takes the bucket rather than a per-type int. "Every active person lands in exactly one item"
is now a **structural** property instead of two SQL predicates kept in sync, and the Oracle `<> 2`-on-NULL trap
cannot arise. STATUS filter and pad-to-2 slots unchanged. a4 / a14 / a6 / a9-base (transport, destroy, a15).

**Partition test added and falsified** — fixture with PER_TYPE {1, 2, 0, NULL}; asserts item 4 is exactly the
PER_TYPE-2 person, the other three fall to item 3, no overlap, nothing dropped. Reverting item 3 to `=1` fails it
with `TYPEZERO (not PER_TYPE 2) must fall to item 3 - got [TYPEONE]`. 19/19 + BUILD SUCCESS; structure 34/34.

**Removed the orphaned `findActivePersons(requestId, perType)`.** My change left it unreferenced in production
(only two test stubs), and it encodes the exact-equality semantics that ARE DEF-26 - leaving it is a trap: the
next person reuses it and recreates the defect. Removed it and both stubs, with a note in the repository saying
not to reintroduce a per-type query and why. `PER_TYPE_AUTHORIZED` is orphaned by the complement rule and gone
from all four builders.

**@Sober - the mock requirement cannot be met, and I did not fake it.** The task asks for a non-1/2 person in a
mock "so the preview exercises it". The preview cannot: grepping `perType|PerRepository|buildPersons` over every
`*PreviewBuilder` returns nothing - the mocks build person rows directly with hardcoded (1)/(2) seqs and never
touch PER_TYPE or the repository. A "PER_TYPE 0 person" there would change no code path and would look like
coverage while proving nothing. The unit test is the coverage. If previews should exercise builder logic at all,
that is a separate decision, not something to paper over with a decorative fixture.

Open: real /download of a request with a non-1/2 or NULL PER_TYPE person - confirm one exists first.
SecurityConfig still unreverted (7th report).

## Sober (SA) — TASK-054/DEF-26 SA-verified CLOSED (code), all 4 builders
Verified: no 2-arg findActivePersons/PER_TYPE_AUTHORIZED in production; a4/a14/a6/a9-base partition via findActivePersonsAll;
exact-match repo method+stubs removed (DEF-26 note left) = trap deleted; partition test falsified, 19/19. Endorsed Jason's
refusal to add a decorative mock — the preview bypasses buildPersons/PER_TYPE entirely, so a mock person proves nothing;
unit test is the coverage (my task's "mock a non-1/2 person" line was mis-specified — owned). **DEF-26 code CLOSED.**
QA (real non-1/2/NULL person → item 3) pending, currently halted. **Ball → Porter** (QA when unhalted). Jason idle.

## Sober (SA) — Query 3 delivered (DEF-26 "did it bite real data" + QA sample finder)
Stakeholder asked for the SQL proving DEF-26 mattered. Appended Query 3 to DATA-QUERY-2026-09-07-samples.md: active
T_T_REQUEST_PER with (PER_TYPE IS NULL OR PER_TYPE NOT IN (1,2)) — wrote the NULL guard explicitly (Porter's trap: bare
NOT IN excludes NULL) — joined to T_T_REQUEST.REQUEST_TYPE + per-family COUNT. a6=REQUEST_TYPE 8 confirmed from
RequestTypeResolverService:41-51 (not guessed; CLAUDE.md FORM_ID note is stale). Reading rule stated: 0 rows = nobody lost
(say plainly); rows = QA renders a type-3/4/5/6/8 one to see the recovered person in item 3. **Ball → Porter → stakeholder (run Q3).**

## [17:30] Tanya (QA) — TEST-013 (DEF-26) → PASS. Counted, did not eyeball.
Unhalted for this. Rendered on the real-DB seams, clean build (`compiled 58/58`). **Counting is the check** (a dropped
person leaves no gap — 38336 looked complete for weeks missing 2):
- **อ.9 38238 (richest):** item 3 (ผู้มีอำนาจลงนาม/มอบอำนาจ) shows **all 8** dropped `PER_TYPE=0` people, numbered (1)–(8),
  every one named. Item 4 = **0 named** (2 blank pad slots) → no duplication into item 4; this request has 0 `PER_TYPE=2`
  people so item 4 is legitimately empty.
- **Spot-checks, 2 recovered each under item 3:** อ.4 38304 · อ.15 38305 · อ.14 38307 (a4/a14 independent builders +
  a9-base all carry the partition — source-checked before rendering). All 200 / 0 ORA.
- **อ.6 = 0 affected** (Query result) — stated, not skipped; no render.
- ⚠️ **Routed up (data, not a DEF-26 fault):** 38238 has 8 `T_T_REQUEST_PER` rows but only **5 distinct identities** —
  3 rows repeat an identical name+ID-card (rows 1≡6, 2≡7, 4≡8). The fix correctly renders every active row; the dupes
  are upstream data. Stakeholder to decide if the duplicate PER rows want cleaning. (IDs not logged — PII.)
- @Porter: **DEF-26 = QA-PASS.** tests/TEST-013 + board row done. Also FYI the board's TEST-012 row still reads "38448
  NOT RUN" — I **did** render 38448 on 2026-09-08 (transport A9T, `13.☑ … ทดสอบ001` on the write-in = populated PASS on
  the a9 base); it's in tests/TEST-012 + my PM.md report. Left your row for you to reconcile rather than overwrite it.
