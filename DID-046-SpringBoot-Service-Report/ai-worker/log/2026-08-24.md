# Log — 2026-08-24 (Tanya / QA)

> Continues the thread in `2026-08-05.md` / `2026-08-18.md` / `2026-08-20.md` / `2026-08-21.md`.
> Append-only. Format: see PROTOCOL.md.

## [17:55] Tanya (QA) — ⏰ date-discipline correction + REQ-015 PASS + REQ-013 closed

**⏰ Clock correction (flagging, not guessing).** TODAY is **2026-08-24** — both my session's
current-date context and the machine clock now agree on it. Earlier this working session I mis-settled
the date as 2026-08-21 and wrote today's Tanya entries (and you wrote your REQ-010/TEST-004 routing
replies) into `2026-08-21.md`. Per the hard date rule I've opened `2026-08-24.md` and will write here
from now on; `2026-08-21.md` is history. **@Porter — a clock question for you (allowed to ask directly):**
is the team genuinely on 2026-08-24 now, and do you want the 08-21 entries left as-is (history) or
noted as actually-08-24? I did not move them.

**REQ-015 → TEST_PASSED.** Clean build + fresh restart (`:33007`, dev), read-only. The REQ-010
NULL-status regression is fixed:
- `RequestPerRepository` now uses JPQL `(p.status IS NULL OR p.status <> 'D')` (11.2-safe).
- **37940** (persons are STATUS=NULL): items 3/4 now show **2 persons** — the NULL rows are no longer
  dropped (old `STATUS <> 'D'` returned 0 because `NULL <> 'D'` = UNKNOWN). Matches the spec's "both null".
- **38272** unchanged: **3 persons** (92567/68/69), 'D' rows still excluded — no regression.
- Both 200, JPQL executed clean (no ORA error). Counted from the items-3/4 block via text extraction,
  names not dumped (PII). tests/TEST-005 written.
- **AC#3 still open:** the doc-query NULL trap audit (`findByRequestIdAndStatusNot`) is a code-side
  assessment I can't verify read-only without a request that has NULL-status *document* rows. If SA
  already assessed it, note it; otherwise it's outstanding.

**REQ-013 → TEST_PASSED (proof-in-use).** The a9 db seam is proven working by my TEST-004 runs:
`/a9/db/38336` (transport) and `/a9/db/38362` (destroy) both returned valid A9 PDFs for plain ids,
no auth, under the dev profile. The seam does what REQ-013 asked. (Didn't need /a9/db/33630 specifically.)

**@Porter:** REQ-015 + REQ-013 are yours to accept→DELIVERED. Remaining QA item: **REQ-009**, still
parked on the human's DATA REQUEST (46784 attachment state / a no-file sample). If nothing else is
queued for QA, that's the board clear except REQ-009. Awaiting your clock answer + REQ-009 data.

## [later] Porter (PM) — ⚠️ TODAY'S LOG WAS OVERWRITTEN. Reconstructing the DEF-15 record.

`log/2026-08-24.md` went from ~430 lines to 34. **Every DEF-15 entry today is gone from the logs** —
Sober's two diagnoses, my evidence tables, TASK-030/031, the `ReqMoveDestroyer` mapping, and my QA
assignment. `grep -l DEF-15 log/*.md` now returns nothing. The file was rewritten rather than appended
to; this entry is deliberately an append.

Not assigning blame — the date-discipline correction was the right instinct and the loss is a
mechanical accident. But recording it as a process failure: **this log is append-only. Never rewrite
it.** `board.md` survived because it is edited row-wise, which is why the DEF-15 row is still there —
that is the reason the protocol says the board is the source of truth, and today proves it.

### DEF-15 — reconstructed record (status: OPEN, diagnosis in progress)

**Symptom.** อ.9 **destroy** (38362) ticks land on the wrong lines. Verified against the real
`T_T_REQUEST_DOC` ⋈ `T_S_REQUEST_CHECKLIST` table for that request.

| | |
|---|---|
| Rule says tick | จดทะเบียน `00101` · มอบอำนาจ `00602` · ร.ง.4 `00803` · **บัตรผู้เสียภาษี `00407`** (file 39897) · ภ.พ.20 `10008` · **แผนผังโรงงาน `12111`** (file 39905, STATUS C) · **อ.10 12(7) `00017`** (file 40339) |
| PDF actually ticks | จดทะเบียน · มอบอำนาจ · ร.ง.4 · **อ.2** · **อ.7** · ภ.พ.20 · **สถานที่กำจัด 12(3)** |
| Wrong both ways | `12204` อ.2 ticked with **no doc row at all**; `00006` อ.7 ticked with file **0**; `00013` ticked with file **0** |

**Ruled out so far**
1. *Suffix divergence between groups* — false. Lines 1–11 use **identical** suffixes in `ReqMove` and
   `ReqMoveDestroyer`; the mismatches sit inside that range.
2. *`STATUS = 'A'` filtering* — false. Doc query is `STATUS IS NULL OR <> 'D'`, so `12111`'s STATUS=C
   should tick.
3. *Stale artifact* — false. `A9DestroyReportBuilder.class` 17:15:20 → jar 17:15:26 → PDF 17:17:36,
   and the new PDF is byte-identical in size (197,690) to the pre-rebuild one.
4. *Builder logic* — Sober hand-traced it against the real table and it yields the **correct** 7 ticks.

**Therefore:** the tick values are computed correctly and mis-**placed** downstream. Leading
hypothesis (mine, unproven): the destroy form's item-5 block prints **ร.ง.4 / อ.2 / อ.7 / เปิดดำเนินการ**
while the master holds **ร.ง.4 `00803` / อ.2 `12204` / สลักหลัง `12305` / อ.7 `00006`** — the form and
the master do not contain the same four entries, so any position-for-position pairing desynchronises
from there on, which matches the observed symptom exactly.

**Next step (already built, needs running):** TASK-031 added temporary instrumentation that dumps the
JSON handed to Jasper (`A9D-JSON`, gated to `request-a9-destroy`, marked `// TASK-031 TEMP DEBUG`).
Capturing it decides between *wrong booleans in the JSON* (builder/linkage) and *right booleans,
wrong placement* (template).

### @Tanya — re-issuing the assignment that was lost
One task. Render **38362** once (`/a9/db/38362` is fine — same builder + template, no auth needed),
then `grep A9D-JSON` the app log (or take it from stdout) and paste back the **`evidences`** array:
each item's `orderNo` + `checked`, plus `subItems[].label` + `checked` for **item 5** and **item 12**.

Report it faithfully; do not interpret — the diagnosis is Sober's. If the JSON shows the *correct*
ticks while the PDF shows the wrong ones, say so explicitly: that is the finding. The instrumentation
is temporary and gets reverted after DEF-15, so capture it this run.

### Your other points
- **Clock: yes, today is 2026-08-24.** You were right to flag rather than guess, and right to open
  today's file. **Leave the 08-21 entries where they are** — rewriting history to fix a filename costs
  more than it's worth. I've closed 08-18 and 08-20 with forward pointers; treat 08-24 as current.
- **REQ-015 → accepted, DELIVERED.** 37940 showing the NULL-status persons and 38272 unchanged is
  exactly the proof that requirement needed.
- **REQ-013 → accepted, DELIVERED** (proof-in-use).
- **REQ-015 AC#3** (doc-query NULL trap audit) — not outstanding: that is **REQ-017**, which the
  stakeholder **CANCELLED** on 2026-08-05 (won't fix, legacy data only). Drop it from your list.
- **REQ-009** stays parked on the human's data.

## [18:05] Tanya (QA) — DEF-15: A9D-JSON captured from 38362 (reporting, not interpreting)
First: I'm sorry about the overwrite — my date-correction used `Write` on `2026-08-24.md` without
checking it already existed, and it clobbered today's DEF-15 records. Understood: **this log is
append-only; I append only from now on, never Write over it.** Recording the lesson so it doesn't recur.

Captured the TASK-031 instrumentation: clean build → own `:33008` (dev) with
`logging.level.…JasperPdfReportService=DEBUG` → GET `/a9/db/38362` → 200 pdf (197,687 B) → grabbed the
`A9D-JSON` line. Full JSON saved to `project-docs/REQ-015-evidence/def15.json` (gitignored; has PII in
applicant/signature blocks — I only quote the non-PII `evidences` below).

**`evidences` array (13) — orderNo | checked (verbatim from the JSON):**
- [1] สำเนาหนังสือรับรองการจดทะเบียน — **true**
- [2] หนังสือมอบอำนาจ — **true**
- [3] เอกสารของผู้มีอำนาจลงนาม/มอบอำนาจ — null (person header)
- [4] เอกสารของผู้รับมอบอำนาจ — null (person header)
- [5] สำเนารับรองการประกอบกิจการโรงงาน — null (parent; subItems below)
- [6] สำเนาบัตรประจำตัวผู้เสียภาษีของนิติบุคคล — **true**
- [7] ใบทะเบียนภาษีมูลค่าเพิ่ม (ภ.พ. 20) — **true**
- [8] สำเนาเอกสารแสดงคุณลักษณะ/คุณสมบัติของอาวุธ — false
- [9] แผนที่แสดงสถานที่ตั้งโรงงาน — false
- [10] แผนผังโรงงาน — **true**
- [11] แผนการขนย้าย — false
- [12] เอกสารประกอบการขออนุญาตขนย้ายเศษวัตถุ — null (parent; subItems below)
- [13] เอกสารอื่น ๆ (ถ้ามี) — false

**item 5 `subItems[].label` | checked:**
- ร.ง.4 (ลำดับ 9) — **true**
- อ.2 (ฉบับต่ออายุ) — false
- อ.7 — false
- หนังสืออนุญาตให้เปิดดำเนินการผลิตอาวุธ — false

**item 12 `subItems[].label` | checked:**
- (1) วัน เดือน ปี ที่จะทำการกำจัด/ทำลาย — false
- (2) ตัวอย่างลายมือชื่อผู้รับอาวุธ — false
- (3) สถานที่ที่จะทำการกำจัด/ทำลาย พร้อมแผนผัง — false
- (4) ชื่อเจ้าหน้าที่ควบคุมกำกับการกำจัด/ทำลาย — false
- (5) รูปถ่ายเศษวัตถุ — false
- (6) ขั้นตอน/วิธีการ/กระบวนการกำจัด/ทำลาย — false
- **(7) สำเนาหนังสืออนุญาตขนย้าย (แบบ อ.10) — true**
- (8) กรณีอยู่ใต้บังคับกฎหมายอื่น — false
- (9) กรณีต้องใช้ผู้เชี่ยวชาญ/สถานที่เฉพาะ — false

**The finding you asked me to state plainly:** the JSON's `true`s are **[1],[2],[6],[7],[10]** plus
**item5→ร.ง.4** and **item12→(7)อ.10** = exactly the **7** entries your "Rule says tick" row lists
(จดทะเบียน, มอบอำนาจ, ร.ง.4, บัตรผู้เสียภาษี, ภ.พ.20, แผนผังโรงงาน, อ.10-12(7)). So **the booleans handed
to Jasper are correct**; the PDF's observed wrong ticks (อ.2, อ.7, 12(3), …) are **not present as `true`
in the JSON** — the divergence is downstream of the JSON (template/placement), not in the builder's
computed values. Reporting only; the diagnosis + fix are Sober's. Instrumentation left in place for
this capture; revert it when DEF-15 closes.

@Porter — that's the A9D-JSON you needed. Nothing else queued for QA (REQ-009 still parked).

## [later] Porter (PM) — DEF-15 LOCALISED: the JSON is correct. It is a TEMPLATE defect.

@Tanya: exactly the capture I needed, and thank you for stating the finding plainly instead of
hedging it. Also — the overwrite: you owned it, you named the cause, and you stated the rule you'll
follow. That's the end of it as far as I'm concerned; the reconstruction cost me one entry and the
board never lost anything.

### The result splits DEF-15 cleanly
The JSON handed to Jasper carries **exactly the 7 correct ticks** — `[1] [2] [6] [7] [10]` plus
`item5→ร.ง.4` and `item12→(7) อ.10`. The values the PDF prints (อ.2, อ.7, 12(3)) **do not appear as
`true` anywhere in the JSON.**

⇒ **The builder, the code binding, the master lookup and the doc query are all correct and are now
proven so on real data. The defect is entirely in the destroy template's rendering of the evidence
list.** Nothing upstream of Jasper needs to change.

@Sober — **re-opening to you for the template fix**, which is the branch you named yourself.

What the evidence tells you about where to look:
- The **parent rows carry `checked = null`** (items 3, 4, 5, 12) while their children carry the real
  booleans. If the destroy template renders a checkbox for a parent row, or renders parent and child
  rows through the same band without distinguishing them, the sequence of printed boxes and the
  sequence of JSON entries diverge — which is precisely the observed symptom.
- The printed ticks land on **อ.2** and **อ.7** — the 2nd and 3rd children of item 5 — while the JSON's
  `true` in that block is on the **1st** child. And item 12's printed tick lands on **(3)** while the
  JSON's is on **(7)**. Both are consistent with children being consumed against the wrong rows,
  not with random corruption.
- `request-a9-destroy` is the only template involved; **transport and a15 share the same base builder
  and are NOT affected** (Tanya's TEST-004 renders were correct), so the difference between the
  destroy template and the transport one is the smallest place to look.

Constraints on the fix, same as always: **printed structure, order and labels must not change** —
only which box is ticked. `.jasper` regenerated into `src/main/resources` + `clean compile` + full
restart. Verify with 38362 against the table in the reconstructed DEF-15 record above; and per the
rule I adopted, the close condition is the **rendered PDF on a clean build**, not a green test.

Revert the TASK-031 instrumentation once the fix is verified — it has done its job.

## [later] Sober (SA) — DEF-15 ROOT CAUSE: deployed destroy `.jasper` was out of sync with the `.jrxml` (DEF-7 family)
Traced the full render pipeline (builder → JSON → template → compiled `.jasper` → classpath). Findings:
- **Builder correct** (QA already proved JSON booleans = the right 7 ticks). Confirmed `createData→buildEvidences` is the
  only evidence source, per printed line, code-bound.
- **Templates correct & identical to transport:** `request-a9-destroy` vs `request-a9-transport` `evidence.jrxml` +
  `evidenceSub.jrxml` are byte-identical; `main.jrxml` differs only cosmetically (page-1 field x/width, bold order).
- **Compiled `.jasper` correct now:** extracted embedded expressions from the destroy `.jasper` (both `src/main/resources`
  AND `target/classes`) — identical to transport's for evidence + evidenceSub + main evidence-wiring.
- **Load path:** `ReportResourceService` loads via `ClassPathResource` ⇒ runtime reads **`target/classes`** (not
  `src/`); `JasperPdfReportService` re-reads it **per request** (no startup cache).
- **Timing:** the destroy `.jasper` in BOTH `src` and `target/classes` were regenerated at **17:32** — AFTER the last
  wrong render (17:17). The `.jrxml` haven't changed since 08-20. So the wrong renders used a **pre-17:32 destroy
  `.jasper` that was out of sync with the fixed `.jrxml`.** I can't inspect it (Porter's 17:15 jar is gone).
- **Why clean builds didn't help (Porter's byte-identical result):** per CLAUDE.md, **Maven does NOT compile
  `.jrxml`→`.jasper`** — `.jasper` are precompiled resources. `clean package` just COPIES the stale `.jasper`; only a
  PreviewTest run regenerates them. This is the **DEF-7 family** (a `.jasper` out of sync with its source).

**⇒ Decisive confirmation (QA, not a blind retry — every artifact is now verified correct):** @Tanya, download **38362**
(A9D) on the CURRENT service and check **page-2 ticks** (it re-reads the now-correct 17:32 `target/classes` `.jasper`).
Expected: จดทะเบียน·มอบอำนาจ·ร.ง.4·**บัตรผู้เสียภาษี**·ภ.พ.20·**แผนผังโรงงาน**·**อ.10 12(7)**; อ.2/อ.7/12(3) UNticked.
- **Correct** ⇒ close DEF-15; the deployed `.jasper` was stale, fixed by the 17:32 regen.
- **Still wrong** (with the verified-correct JSON + `.jasper`) ⇒ paste me the PDF **and** the `A9D-JSON` from the SAME
  run; I'll dig into `loadJasperReport`/Jasper report caching.

**Prevention (Porter, process):** `.jasper` are precompiled and Maven-invisible, so a `.jrxml` fix does NOT reach prod
via `clean package` alone — the deploy MUST regenerate `.jasper` from `.jrxml` (PreviewTest) and verify the deployed
binary matches. Recommend committing the compiled `.jasper` or adding a jasper-compile build step so this can't recur.

## [later] Porter (PM) — DEF-15 root cause accepted. My "clean build" test was the wrong instrument.

@Sober: that's the answer, and it explains every observation including the one that misled me.

**Owning my part:** I told the human a `clean package` + full restart would be decisive, and treated
the byte-identical result as proof the builder was at fault. **It proved nothing** — Maven does not
compile `.jrxml` → `.jasper`; it copies the precompiled binary. So a clean build cannot fix, and
cannot rule out, a stale `.jasper`. I sent the human to run a test that could not answer the question,
and then reasoned from its result. This is the **DEF-7 lesson recurring** — and the board has carried
that BUILD FACT since 2026-08-05, so I had the information and failed to apply it.

Adding to my own checklist, not just the team's: **when a rendering symptom survives a rebuild, the
`.jasper` is a suspect, not an exonerated party.**

### @Tanya — one confirmation run
Download/render **38362** (A9D) on the **current** service — no rebuild needed, the service re-reads
the `.jasper` per request and `target/classes` now holds the 17:32 regeneration.

- **Expected ticks:** จดทะเบียน · มอบอำนาจ · ร.ง.4 · **บัตรผู้เสียภาษี** · ภ.พ.20 · **แผนผังโรงงาน** ·
  **อ.10 12(7)**; อ.2 / อ.7 / 12(3) **unticked**.
- If correct → DEF-15 closes.
- If still wrong → paste me the PDF **and** the `A9D-JSON` from the *same* run, so Sober can compare
  one run's input against that same run's output rather than across runs.

### Prevention — raising it, because this is now twice
Sober's point stands and it is a deployment-safety issue, not a tidiness one: **a `.jrxml` fix does
not reach production through `clean package`.** The compiled `.jasper` is invisible to Maven, so a
correct source fix can ship as a stale binary and nobody finds out until someone reads a rendered
form. That is exactly what DEF-7 was, and exactly what DEF-15 turned out to be.

Two options, and it is the human's call: (a) commit the compiled `.jasper` and make regeneration a
required, verified deploy step, or (b) add a jasper-compile step to the build so `.jrxml` is the only
source of truth. **(b) removes the failure mode; (a) only documents it.** Taking it to them now.

## [later] Porter (PM) — REQ-031 raised: compile `.jrxml` → `.jasper` in the build. Human approved.

Human approved after I put the tradeoff plainly: the build step **removes** the failure mode; a
documented manual step only records it. Filed as REQ-031.

@Sober: scope is one file (`pom.xml`) plus removing 28 tracked `.jasper` and gitignoring them. Small
change, high blast radius — every report in the project renders through these binaries, so treat the
verification as the real work, not the plugin config.

Three things I want handled explicitly rather than assumed:

1. **Pin the compiler to JasperReports 7.0.4.** The plugin's transitive default is not good enough —
   a `.jasper` built by a different major version can fail to load or render differently, and the
   symptom would look exactly like the bug we just spent a day on.
2. **Do not verify with md5.** `.jasper` recompilation is non-deterministic; we established that under
   REQ-026 when a6/a14 hashes drifted with zero source changes. Verify by **rendered PDF text +
   page count** across all forms, including the older builders (a1/a3/open/expand/personChange/
   planChange) that nobody has touched in weeks — those are the ones most likely to be sitting on a
   stale binary right now and to "change" when regenerated. If one of them does change, that is a
   pre-existing latent defect surfacing, not a regression this REQ caused — report it, do not paper
   over it.
3. **Explain the 52-vs-28 gap before deleting anything.** 24 `.jrxml` have no committed `.jasper`.
   Find out what they are (runtime-compiled subreports? unused? genuinely missing?) and say so. That
   gap is itself evidence of how loose the current arrangement is.

Not urgent against any feature — DEF-15 is already fixed by the 17:32 regeneration. This is about it
never happening a third time.

## [18:52] Tanya (QA) — DEF-15 CONFIRMED FIXED on a clean-build render of 38362
Confirmation run done the rigorous way (per the "clean-built full-restart" rule, since a `.jasper`
staleness was the whole point): verified the destroy `.jasper` in `src/main/resources` are the **17:32**
regeneration → `./mvnw clean compile` (copied them into `target/classes`, timestamp confirmed 17:32) →
fresh `:33009` (dev) → GET `/a9/db/38362` → 200 pdf (**197,687 B**, vs the pre-fix 197,690) → rendered
page 2 (PyMuPDF) and checked every box against Sober's ground-truth table.

**Result: every tick is now correct.**
- **Ticked (7):** [1] จดทะเบียน · [2] มอบอำนาจ · item5→**ร.ง.4 (ลำดับ 9)** · [6] **บัตรผู้เสียภาษี** ·
  [7] ภ.พ.20 · [10] **แผนผังโรงงาน** · item12→**(7) อ.10** — exactly the 7 the rule requires.
- **Now correctly UNticked** (these were the wrong ticks before): item5→**อ.2**, item5→**อ.7**,
  item12→**(3) สถานที่กำจัด**. Also item5→เปิดดำเนินการ and all other item-12 sub-lines unticked.
- Structure/order/labels unchanged (4 pages, destroy layout intact). No literal `null`.

⇒ **DEF-15 closes.** Root cause was exactly as Sober found — the deployed destroy `.jasper` was out of
sync with the fixed `.jrxml` (DEF-7 family; Maven doesn't compile jasper), and the 17:32 regeneration
resolved it. The builder/JSON were correct all along (my earlier capture) and the template now renders
them on the right lines. Evidence: `project-docs/REQ-015-evidence/confirm-38362.pdf`.

@Porter (for Sober): DEF-15 is QA-confirmed fixed. **TASK-031's `A9D-JSON` debug instrumentation can now
be reverted** (it did its job). Nothing else queued for QA — REQ-009 still parked on the human's data.
Note: REQ-031 (compile `.jrxml`→`.jasper` in the build) is the real prevention — until it lands, any
future `.jrxml` fix still needs a manual `.jasper` regen + a QA render to prove it reached the binary.

## [later] Porter (PM) — DEF-15 CLOSED. REQ-030 DELIVERED. Revert the debug instrumentation.

@Tanya: confirmed and accepted. Doing the clean-build/full-restart properly *because* staleness was
the suspect — rather than just re-downloading — is what makes this a real close instead of a hopeful
one. **DEF-15 → CLOSED.**

**REQ-030 → DELIVERED.** All five forms now bind ticks by the stakeholder's TICK RULE, and the destroy
variant is proven correct on real data, tick by tick, against the actual document table. That was the
requirement's whole point.

@Sober: **revert the TASK-031 `A9D-JSON` instrumentation** — it did exactly what it was for. Worth
recording that it was the decisive move in this investigation: it split "wrong values" from "wrong
placement" in one run, after we had spent the day arguing about which it was from code inspection
alone. Reach for that pattern earlier next time.

### What this investigation cost, and the two lessons I'm keeping
DEF-15 took a full day. The builder, the code binding, the master lookup, the doc query and the
emitted JSON were **all correct the entire time**. The defect was a stale compiled binary.

1. **A rebuild does not exonerate the artifact.** I told the human `clean package` + restart would be
   decisive and then reasoned from its result — but Maven never compiles `.jasper`, so that test
   could not answer the question. The board has carried that BUILD FACT since DEF-7 on 2026-08-05; I
   had it and didn't apply it.
2. **Instrument the boundary instead of arguing across it.** Once "code says X, output says Y", the
   fastest move is to dump what actually crosses the line, not to re-read either side.

REQ-031 is the structural fix so this cannot recur; until it lands, Tanya's caveat stands — **any
`.jrxml` fix needs a manual `.jasper` regeneration plus a rendered-PDF check to prove it reached the
binary.** I've added that to the board.

## [later] Sober (SA) — DEF-15 CLOSED / REQ-030 DELIVERED. Reverting instrumentation (TASK-032).
- Confirmed Porter/Tanya's close — root cause was the stale destroy `.jasper` (DEF-7 family), fixed by the 17:32
  regen; QA verified 38362 tick-by-tick (confirm-38362.pdf). Builder/JSON/`.jrxml` were correct throughout.
- @Jason — **TASK-032:** revert the two TASK-031 temp edits (the `A9D-JSON` `log.debug` block in
  `JasperPdfReportService.exportPdfA9` + the `application.yml` logging.level block). Both carry the TEMP tag; leave the
  pre-existing `System.out.println` untouched. Back to me to confirm 0 refs + PreviewTests green.
- Adopted Porter's two lessons (saved to memory): a rebuild does NOT exonerate a precompiled `.jasper` (Maven never
  compiles it — [[jasper-precompiled-not-built-by-maven]]), and instrument the boundary (dump what crosses the line)
  instead of arguing code-vs-output — reach for it earlier.
- Prevention: **REQ-031** (compile `.jrxml`→`.jasper` in the build) is the structural fix; until it lands, every
  `.jrxml` change needs a manual `.jasper` regen + a rendered-PDF check. If you want me to spec REQ-031, say the word.

## [later] Jason (BE) — TASK-032: reverted the DEF-15 debug instrumentation

DEF-15 closed (QA-confirmed on 38362; root cause was a stale destroy `.jasper`, not the code). Sober asked
for a clean revert of TASK-031's temp instrumentation. Done:
- Removed the temp block (comment + `if("request-a9-destroy"...) log.debug("A9D-JSON {}", json)`) from
  `JasperPdfReportService.exportPdfA9`. Pre-existing `System.out.println(json)` untouched. Also removed the
  `Logger`/`LoggerFactory` field+imports (unused after the revert).
- Removed the temp `logging.level` block from `application.yml`.

**Verify:** `grep -rn "A9D-JSON\|TASK-031" src application.yml` = 0 hits; test-compile SUCCESS; all 5
PreviewTests render; clean compile + boot :33102 clean.

→ TASK-032 REVIEW, back to Sober. Board + task file updated.

## [later] Sober (SA) — TASK-032 REVIEW: DONE. DEF-15 fully wrapped.
- Verified the revert: `grep A9D-JSON|TASK-031` in src+application.yml = 0; pre-existing `System.out.println(json)`
  untouched; Logger/LoggerFactory field+imports removed with no orphan `log.` refs; no stray `logging` block;
  test-compile + all 5 PreviewTests + boot green. ✅ TASK-032 DONE.
- **DEF-15 CLOSED, REQ-030 DELIVERED, instrumentation reverted** — Sober side fully done.
- Minor, out of scope (leaving as-is, flagging only): the pre-existing `System.out.println(json)` runs for EVERY
  download across all 9 export methods — prints request JSON to stdout. Pre-dates this work; housekeeping candidate,
  not part of DEF-15.
- @Porter — ball back to you. Only open prevention item from my side: **REQ-031** (compile `.jrxml`→`.jasper` in the
  build). Say the word and I'll spec it; otherwise the manual regen + rendered-PDF caveat stands on the board.

## [later] Porter (PM) — REQ-031: GO. And your "minor" stdout finding is not minor — raising DEF-16.

@Sober: **REQ-031 is READY_FOR_SA and approved by the human — spec it.** It is already filed with the
constraints (pin JasperReports 7.0.4, verify by rendered PDF text not md5, explain the 52-vs-28 gap,
re-render the older builders too). My relay is a few entries above yours; no further go-ahead needed
from me beyond this.

### Your stdout note — I'm escalating it, and I think you undersold it
> *"the pre-existing `System.out.println(json)` runs for EVERY download across all 9 export methods —
> prints request JSON to stdout. Pre-dates this work; housekeeping candidate."*

You were right to flag it and right that it is out of DEF-15's scope. But **what that JSON contains
changes the category**: it is the full report payload — applicant names, **national ID card numbers**,
card expiry dates, addresses, signatory names and positions. Tanya has been careful all project to
keep exactly this data out of her evidence files (she deleted rendered PNGs and kept PII out of the
log), and meanwhile every single download has been printing it to stdout.

This is a Thai government permit system handling citizens' ID numbers. Console output typically lands
in a service log, gets rotated, shipped, and read by people who have no business seeing it — and it
happens on **every** download, not on error paths.

**Raising as DEF-16.** Not "housekeeping" — a data-exposure defect. It is pre-existing and nothing is
on fire, so it does not preempt REQ-031; but it should be fixed deliberately rather than left as a
comment in a log. The fix is small (delete the prints, or gate them behind the debug logger the way
TASK-031 did properly), which is another reason not to leave it.

Taking it to the human, with the honest framing: our team did not introduce it, we found it, and it
is their call how fast it moves.

## [later] Porter (PM) — DEF-16 DEFERRED by the stakeholder. Staying on the board, not dropped.

Human: *"ปล่อยไปก่อน"* — **DEF-16 deferred.** @Sober: do not spec it, do not fold it into REQ-031.
REQ-031 remains the active item.

Recording it as **DEFERRED rather than cancelled**, deliberately. The stakeholder's decision is about
sequencing, not about the finding being wrong — they were given the PII framing and chose to wait,
which is a legitimate call for a pre-existing issue on an internal-network system that is not yet in
production.

It stays visible on the board because it belongs on the **pre-production checklist** alongside
REQ-018 step 2 (restoring the `@Profile("dev")` gate). Both are the same shape: acceptable while this
is internal-only, not acceptable once real users touch it. When Porter triggers the production
readiness pass, these two go together.

I will raise it once more at that point, and not before.

## [later] Porter (PM) — อ.4–อ.8 prep: both blocking questions answered from the DB

Human ran the two checks. REQ-029 updated in place.

1. **No import checklist group exists** (full list: A1, A3, BgChk, ReqExpand, ReqMove 22,
   ReqMoveDestroyer 19, ReqOpen, ReqPersonChange, ReqPlantChange, ReqSaleDom 21, ReqSaleInt 15,
   ReqSpecial 9). So อ.4 genuinely needs a seed — and this time I verified *before* asking, unlike
   `ReqSaleInt`/`ReqSaleDom` where I requested seeds that already existed. The seed hand-off is mine.
   It does not block the build: unseeded ⇒ all boxes unticked, which is the correct steady state.
2. **The annex's three extra columns live on `T_T_REQUEST_DTL_REF_IMPORT`** — `LICENSE_NO` /
   `ISSUE_DATE` / `EXPIRY_DATE`, plus `ATTACH_FILE_ID` and **`REQUEST_DTL_ID`**. They hang off an
   individual detail row. @Sober: establish 1:1 vs many-per-item before designing the annex — the
   official form shows one line per item, so a many case needs an explicit decision, not a silent
   `firstOrNull`.

**Also worth noting: `ReqSaleDom` was 22 rows this morning and is 21 now.** The data team is editing
the master while we work. Under the old positional binding that would have silently shifted every
tick after the deleted row; under REQ-030's code binding, the affected line simply stops ticking.
The design change is already paying for itself — and it means any master snapshot we hold is
short-lived, so keep verifying rather than reusing dumps.

Sequencing unchanged: **REQ-031 first**, then release REQ-029 — a new form should be born into the
build-compiles-jasper pipeline rather than needing manual regeneration from day one.

## [later] Porter (PM) — อ.4–อ.8 ON HOLD until the data team seeds the import checklist master

Human: *"อ.4 รอก่อน ไว้เดี๋ยวเขาทำข้อมูล checklist อ.4 มาค่อยเอามาทำ"*

**REQ-029 → ON HOLD.** @Sober: do not spec it; nothing to start. REQ-031 stays the only active build
item.

I had noted that an unseeded master does not technically block the build (อ.4 would render with all
boxes unticked, same as อ.14 today). The stakeholder chose to wait anyway, and on reflection that is
the better call for this form specifically: อ.4's evidence section is **17 items** — the largest of
any form we've built — and the seeded labels are what tell us the real structure. Building it against
the PDF alone and then reconciling against a master that arrives later is how we ended up doing อ.14's
item-12 twice.

**Also dropping my own pending item:** I owed a seed hand-off document for the import group. The data
team is producing it themselves, so **I will not send one** — sending an unsolicited spec for work
they've already started is exactly the mistake I made with `ReqSaleInt`. I'll wait for what they
produce and map the form lines to their codes, the way I did for `ReqSpecial` and `ReqSaleInt`.

**What I need from the human when it lands** (recording it now so nobody re-derives it):
a dump of `T_S_REQUEST_CHECKLIST WHERE GROUP_CODE = '<the new import group>'` with
`SEQUENCE · IS_ACTIVE · CHECKLIST_CODE · CODE_NAME` — same shape as the other groups. That plus the
official form is everything REQ-029 needs.

### Current state of the board
- **Active:** REQ-031 (compile `.jrxml` → `.jasper` in the build) — Sober.
- **On hold:** REQ-029 (อ.4–อ.8) — waiting on the data team's master.
- **Parked:** REQ-009 — needs a request with a genuinely file-less evidence item.
- **Pre-production checklist:** REQ-018 step 2 (restore the `@Profile("dev")` gate) + DEF-16 (stdout
  PII). Both re-raised together when the stakeholder decides to ship.
- **Delivered:** อ.6, อ.9 transport, อ.9 destroy, อ.14, อ.15 — built, routed, tick-verified on real data.

## [later] Sober (SA) — REQ-031 spec'd (SPEC-033 / TASK-033); TASK-032 confirmed DONE
- TASK-032 (revert TASK-031 debug): re-confirmed DONE — 0 refs to A9D-JSON/TASK-031 in src+application.yml,
  pre-existing System.out.println(json) intact, Logger removed clean, compiles/boots. (Board row reconciled REVIEW->DONE.)
- REQ-031 -> SPEC-033 + TASK-033 (Jason). Grounded it: pom has JR 7.0.4 (no compile plugin), .jasper NOT gitignored,
  52 .jrxml tracked vs 28 .jasper (24 drifted), PreviewTests already compile via JasperCompileManager. Design: compile
  with the project's OWN JR 7.0.4 (exec-maven-plugin -> tiny JasperPrecompiler main, bound to process-classes before
  spring-boot repackage) rather than a version-mismatched plugin -> satisfies Porter's "pin to 7.0.4" for free. Plus
  .gitignore **/*.jasper + git rm --cached the 28. Acceptance = edit a .jrxml label, PLAIN clean package (no
  PreviewTest), change appears in the PDF; verify by PDF text+page count, never md5; no template drift on the 5 forms.
- Flagged for Jason: the 24 .jrxml with no committed .jasper may include non-compiling drafts -> first run compiles
  all; fix or scope to reports/+reports-045/ and LIST exclusions (no silent skip).
- @Jason -- TASK-033 is yours. @Porter -- DEF-16 (stdout PII from the pre-existing println) noted on the pre-prod checklist.

## [2026-08-25] Jason (BE) — TASK-033: compile .jrxml→.jasper in the Maven build (REQ-031), EXECUTED → REVIEW

Sober spec'd REQ-031 (SPEC-033/TASK-033) after DEF-15's root cause (stale precompiled .jasper — Maven only
COPIES the binary, never compiles it). Implemented the build-time fix.

**Result:**
- New `com.smart.report.build.JasperPrecompiler` (plain main, not a Spring bean): walks
  `target/classes/{reports,reports-045}/**/*.jrxml`, compiles each via `JasperCompileManager` (project's own
  JR 7.0.4), fails the build (`System.exit(1)`) on any compile error — no silent stale binary possible again.
- `pom.xml`: exec-maven-plugin bound to `process-classes` (after resources copied, before package/repackage).
- `.gitignore` +`**/*.jasper`; `git rm --cached` the 28 tracked `.jasper` (working-tree copies kept, staged
  for review — not committed).

**⚠️ Deviation (documented, required by this offline env):** the spec's preferred `exec:java` goal never
forks in exec-maven-plugin 3.2.0, so JasperReports' internal expression compiler (which reads
`java.class.path`, not the classloader) saw only Maven's own bootstrap classpath — every subreport failed
with "package net.sf.jasperreports.engine does not exist". Switched to `exec:exec` (real child JVM, proper
`-cp` via the `<classpath/>` magic argument) — fixed it, all 52 `.jrxml` compile. `maven-antrun-plugin` (the
spec's alt) was also tried first but is unusable — no `ant`/`ant-launcher` jar cached in this offline `.m2`.

**Verify (DB-free):**
- `52/52 .jrxml` compile clean (`process-classes` alone).
- **Fails-on-error proof:** corrupted one `.jrxml` attribute → build genuinely FAILS (exit 1); reverted clean.
- **Acceptance probe (per spec):** first attempt (appending text to an existing static label) hit a
  **pre-existing, unrelated JasperReports rendering quirk** — reproduced identically even via the long-proven
  `A6PreviewTest` path, so it predates this task; flagged rather than debugged (out of scope, no `.jrxml`
  content changes are the deliverable). Re-probed with a brand-new element instead: `clean package
  -DskipTests` (no PreviewTest ran) → booted the packaged jar → `/api/v1/preview/checklist/a6` → **PyMuPDF
  confirms the new content reached the PDF** (never md5). Reverted, confirmed 0 diff.
- **Full regression:** `clean package` (with tests) → BUILD SUCCESS, 5/5 tests; all 5 PreviewTests render
  their expected page counts. `git ls-files | grep -c '\.jasper$'` = 0.

→ TASK-033 REVIEW, back to Sober. Flagged the unrelated a6-main template quirk for anyone who touches those
2 labels later. Board + task file updated.

## [later] Sober (SA) — TASK-033 REVIEW: DONE. REQ-031 code-complete + SA-verified.
- Reviewed Jason's REQ-031 build machinery, verified DB-free (my leg):
  - pom: `exec-maven-plugin` @ `process-classes`, goal `exec`, `${java.home}/bin/java`, `classpathScope=compile`,
    `<classpath/>` → child JVM gets the project compile classpath. The `exec:java`→`exec:exec` deviation is SOUND:
    exec:java doesn't fork in 3.2.0 and JR's expression compiler reads `java.class.path`, so exec:exec (real child
    -cp) is the correct choice. antrun alt genuinely unusable offline (no ant jar in `.m2`). Accepted.
  - `JasperPrecompiler` walks reports+reports-045, compiles via JR 7.0.4, `System.exit(1)` on any failure.
  - **Independently re-ran `./mvnw -o process-classes` → `[JasperPrecompiler] compiled 52/52 .jrxml`, exit 0.** ✅
  - `.gitignore` has `**/*.jasper`; `git ls-files '*.jasper'` = 0; 28 staged deletions pending (uncommitted, correct);
    `git diff --stat '*.jrxml'` = 0 (probes reverted clean). All 52 compiled clean → the 24-drift risk was "never
    regenerated," not "won't compile"; no exclusions needed.
- **TASK-033 DONE.** REQ-031 is code-complete + SA-verified. Remaining (NOT Sober):
  1. @Porter/human — **commit the 28 `.jasper` untrack** (staged deletions) so the drift is actually gone in git.
  2. @Tanya — **build-only QA smoke:** render ONE real report from the packaged jar to confirm fresh `.jasper` ships.
- Noted Jason's flag (out of scope, not a REQ-031 blocker): a **pre-existing** a6-main rendering quirk — appending
  text to the 2 static labels `"1. ชื่อผู้ขออนุญาต"` / `"3. ส่วนประกอบ…แนบ"` is dropped in the render, reproduces under
  the original `A6PreviewTest`/`JasperFillManager` path too. Logging as a latent template observation; no ticket unless it bites.

## [later] Porter (PM) — REQ-031 accepted in code. But ONE smoke render does not meet the AC.

@Sober: build machinery accepted — `52/52 .jrxml compiled`, exit 0, `.jasper` untracked and gitignored,
`.jrxml` unchanged. Good call on `exec:exec` over `exec:java` and on saying *why*; and the 52-vs-28
question is now answered properly — the 24 were **never regenerated**, not "won't compile". That was
one of my three conditions and it's discharged.

**But I'm not accepting a one-report smoke test as the close, and I want to be explicit about why.**

REQ-031's acceptance criterion is *"every existing report renders identically to today"*. That
requirement exists because of what we just lived through: **a committed `.jasper` can silently differ
from its `.jrxml`, and nothing surfaces it until a human reads a rendered page.** We have now
regenerated **all 52** from source. Any report whose committed binary was stale is, as of this change,
going to render *differently* — and the reports most likely to be in that state are precisely the ones
nobody has touched in weeks: **a1, a3, open, expand, personChange, planChange**.

Rendering one report proves the plumbing works. It does not test the thing this change actually
risks.

@Tanya — the QA leg is: **render one real request through each builder that has one available**, not
one report total. Compare against the current output where you have a baseline (a6/38272, a9
transport, a9 destroy/38362, a14/27300, a15/18041 — all recently verified), and for the older
builders just confirm they still render a sane, complete PDF with no error and no `null`.

**If an older report comes out different from what it produced before: that is a FINDING, not a
regression to hide.** It means its committed `.jasper` was stale and the form has been printing from
an outdated layout — exactly DEF-15's shape, sitting undiscovered. Report it to me with the before/
after; do not "fix" it and do not suppress it. I would rather learn that now, from us, than from
someone holding a wrong government form.

Jason's a6-main label quirk: noted, pre-existing, reproduces on the old path — agreed, no ticket
unless it bites. Recording it so it isn't rediscovered as new.

**The git untrack of the 28 `.jasper` needs the human's approval** — it is a repo-level change and
theirs to authorise. Taking it to them now.

## [later] Porter (PM) — `ReqImport` EXISTS now (16 rows). อ.4 unblocking; requesting the dump.

Human: the data team has seeded the import master — **`GROUP_CODE = 'ReqImport'`, 16 rows.** That was
the only thing REQ-029 was waiting on.

Requesting the full dump (`SEQUENCE · IS_ACTIVE · CHECKLIST_CODE · CODE_NAME`) so I can map form line
→ code the way I did for `ReqSpecial`, `ReqSaleInt` and `ReqMoveDestroyer`. **Not sending it to SA
until that mapping is done** — handing over a group name without the per-line mapping is what left
Jason guessing on อ.14's item-12.

One thing I want to look at when it arrives, flagged now so I don't gloss over it: the official form
has **17 evidence items**, the master has **16 rows**. That is not necessarily wrong — items 3 and 4
are persons and never have master rows on any form, and some lines tick from their own tables — but
the arithmetic doesn't land on an obvious explanation yet. I'll reconcile line by line against
`A4-A8-form-official.pdf` and report which form lines have no code rather than assume a tidy 1:1.

Sequencing unchanged: REQ-031 still lands first (its QA leg is open), then REQ-029 is released with a
complete mapping.

## [later] Porter (PM) — REQ-029 RELEASED. `ReqImport` mapped line-by-line; the 17-vs-16 gap explained.

Full `ReqImport` dump received and reconciled against `A4-A8-form-official.pdf`. Mapping written into
REQ-029; **REQ-029 → READY_FOR_SA.**

**The 17-vs-16 discrepancy I flagged resolves cleanly, and not as a data error:**
- No master row for form items **3, 4** (persons), **5(4)** เปิดสายการผลิต, **6** อ.8 ฉบับเดิม, and
  **17** เอกสารอื่น ๆ.
- The master carries one row the form never prints — `ReqImport12305` สลักหลัง (แบบ อ.2) — same extra
  row `ReqMove` and `ReqSaleDom` carry.
- `SEQUENCE` skips 9 entirely.

So it is **not** a 1:1 list, and anyone who assumes it is will mis-bind. Same trap as every other
group; the mapping table in REQ-029 is the answer, not the row count.

@Sober, three things to carry into the SPEC:

1. **Form item 6 (อ.8 ฉบับเดิม (1)(2)) is an own-table line, not a code line** —
   `T_T_REQUEST_DTL_REF_IMPORT` (`LICENSE_NO`/`ISSUE_DATE`/`EXPIRY_DATE`/`ATTACH_FILE_ID`). It renders
   and ticks exactly like อ.9's ป.3/ป.5/มหาดไทย/ยุทธภัณฑ์ rows — the §4 carve-out. That same table also
   feeds the annex's three extra columns, so it gets read once and used twice.
2. **The master's order is not the form's order** (master 10/11/12 = แผนที่/แผนผัง/สถานที่จัดเก็บ; the
   form prints 9/10/11 = แผนที่/สถานที่จัดเก็บ/แผนผัง). Harmless under code binding — but do **not**
   reorder the report to match the master. The form wins, as always.
3. Four rows are `IS_ACTIVE = 0` (`12204`, `00006`, `00016`, `00017`). Per the TICK RULE that is
   irrelevant — bind them anyway.

Sequencing: REQ-031's QA leg is still open, so Jason should finish that before starting อ.4. If they
overlap, อ.4's new templates would be born mid-change to the very pipeline that builds them.

## [later] Sober (SA) — REQ-029 spec'd (SPEC-034 / TASK-034, BLOCKED on REQ-031 QA)
- Grounded from Porter's ReqImport mapping + the official PDF (extracted via PyMuPDF, 4 pages) + the dict. Resolved
  all 4 SA open questions:
  - Q4 variants: ONE report (อ.8 = granted-license counterpart referenced in evidence item-6, like อ.6/อ.7) → code "A4".
  - Q3 item-5 วัตถุประสงค์: T_T_REQUEST_IMPORT has NO objective col (PLANT_*/REF_LICENSE_* only) → T_T_REQUEST.OBJECTIVE (a6 pattern).
  - Q2 annex/item-6: T_T_REQUEST_DTL_REF_IMPORT = ID(PK)/REQUEST_ID/REQUEST_DTL_ID(nullable)/LICENSE_NO/ISSUE_DATE/
    EXPIRY_DATE/ATTACH_FILE_ID. Only ID unique → many-per-item POSSIBLE; form = 1 line/item → firstOrNull + flag for
    QA to confirm on real data. Read twice: evidence item-6 (§4 own-table by REQUEST_ID, a9 LicRow pattern) + annex
    (join by REQUEST_DTL_ID). BE to confirm VW_REQUEST_DTL exposes the item DTL id or read T_T_REQUEST_DTL — state which.
- Key traps baked into the spec: ระยะเวลา at **item 6** not 7 (no buyer item — off-by-one); bind the 4 IS_ACTIVE=0
  ReqImport rows anyway (TICK RULE); do NOT reorder to master SEQ (form order locked); no item-12 buyer block; item-14
  long label no-truncate (DEF-8); หมายเหตุ footnote + 4 signature slots. Routing REQUEST_TYPE=4→"A4" already exists (REQ-028).
- Modeled on a14 (standalone + ChecklistCodeBinder), not the move-centric a9-base. New folder request-a4.
- @Jason — TASK-034 is yours but **BLOCKED until REQ-031 QA closes** (Porter's sequencing). @Porter — REQ-029 SPEC_DONE.

## [later] Porter (PM) — HOLD the commit: 46 stale `.jasper` still sit in `src/main/resources`

Human is at the commit screen with the 28 staged deletions. I told them to hold, because I checked
the working tree and found something the untrack alone does not solve:

```
src/main/resources  → 46 .jasper still on disk
target/classes      → 52 .jasper (generated by the new precompiler)
git index           → 28 staged deletions (tracked count goes to 0)
```

**Untracking removes them from git; it does not remove them from the checkout.** So after this commit
a developer's working copy still contains 46 stale compiled binaries, now invisible to git, sitting
exactly where the resource-copy step reads from.

Today that is harmless — the precompiler runs at `process-classes`, after resources are copied, so it
overwrites them. But the safety of the whole change then rests on *ordering*, not on the stale files
being gone. Anything that copies `src/main/resources` without running `process-classes` — a partial
build, an IDE resource sync, packaging a directory by hand — reintroduces DEF-15 exactly.

@Sober: **question before the human commits, and I'd like it answered rather than assumed:**

1. **Should the 46 be deleted from disk as part of this change?** My instinct is yes — if `.jrxml` is
   the single source of truth, a compiled artifact has no business living in a source directory. But
   deleting files in a shared checkout affects the whole team, so I want your read on whether
   anything still legitimately reads from `src/main/resources/**/*.jasper` at runtime or in tests.
2. **Why 46 on disk vs 28 tracked?** 18 were already untracked and nobody noticed — which is its own
   small illustration of how loose this was.
3. If they should go, say whether the deletion belongs in this commit or as a follow-up, and whether
   `.gitignore` alone is sufficient to keep them from creeping back.

Not urgent — the human is simply holding at the commit prompt. But I would rather resolve it now than
ship a change whose correctness depends on nobody ever building in an unusual way.

## [later] Sober (SA) — answering Porter's REQ-031 questions: YES delete the 46, but make it durable (TASK-035)
- **Q1 "delete the 46 stale `.jasper` in src?" → YES, and you're right that it's about ORDER not absence.** Removing
  them converts a skipped-precompile from silent-stale (DEF-7/DEF-15) into a LOUD report-not-found. **Caveat I found:**
  a one-time delete is undone by the next PreviewTest run — the 4 PreviewTests compile AND load `.jasper` straight into
  `src/main/resources` (`BASE = "src/main/resources/..."`). So the complete fix is two parts, both in **TASK-035**:
  (a) redirect the PreviewTests to compile+load under `target/` (they self-compile jrxml, need no pre-existing jasper),
  (b) delete the 46 src `.jasper`. After that `src` holds only `.jrxml`, permanently — "copy the folder directly" can
  never carry a stale binary, and a build that skips the precompile fails loud.
- **Q2 "46 on disk vs 28 in git → 18 fell out?"** Not a separate bug — it's the manual-regen drift itself: `.jasper`
  were regenerated locally by PreviewTests into `src` and `git add`ed inconsistently (28 committed, 18 only local).
  Nobody noticed because runtime loads `target/classes`, so committed-vs-local never errored — the same silent drift
  behind DEF-7/DEF-15. After TASK-033 + TASK-035: **0 `.jasper` in git, 0 in `src`** → the gap is structurally impossible.
- **Sequencing:** TASK-035 lands with TASK-033 in your ONE REQ-031 commit; run the build-only QA smoke on the FINAL
  state (src purged, tests redirected), then commit. @Jason — TASK-035 is yours (this one is NOT blocked; it finalizes REQ-031).

## [2026-08-25] Jason (BE) — TASK-035: purge src `.jasper` + redirect PreviewTests to target/, EXECUTED → REVIEW

Porter caught a real gap after TASK-033: untracking `.jasper` from git doesn't remove the 46 stale binaries
still sitting in `src/main/resources` — the fix's correctness rested on ORDER (precompile always overwriting
them at `process-classes`), not on the stale files being gone. Sober specced the hardening (TASK-035): make
it correct by ABSENCE.

**Result:**
- Redirected all 4 PreviewTests' `BASE`/`ROOT` constants from `src/main/resources/...` to `target/classes/...`
  — they still self-compile `.jrxml`→`.jasper`, just never write back into source.
- Deleted all 46 `.jasper` under `src/main/resources`. `src` now holds only `.jrxml`.
- Runtime unaffected (`ReportResourceService` only ever reads the classpath/`target`, never `src` directly).

**⚠️ One flagged deviation:** used `target/classes/<folder>` (where `process-resources`/`process-classes`
already put things) instead of a new `target/jasper-preview/` folder the task suggested — avoids a parallel
location to keep in sync and doubles as proof the precompiler's own output loads correctly. Flagged for
Sober's call, not decided unilaterally.

**Verify (DB-free):**
- `find src/main/resources -iname '*.jasper'` = 0 after `clean` AND after running all 4 PreviewTests (proves
  no write-back).
- `process-classes` alone: 52/52 compile.
- **Loud-fail proof (without touching pom.xml mid-task):** `clean resources:resources` (stops before
  `process-classes`) → target/classes has 52 `.jrxml`, **0 `.jasper`** → with src also empty, there is
  **zero `.jasper` anywhere** in that state — any code path needing one gets a clear resource-not-found,
  never a silently-stale render. Same failure mode as literally disabling the plugin execution.
- Full `clean package`: BUILD SUCCESS, 5/5 tests, 0 tracked `.jasper`. Restored a normal built state after.

→ TASK-035 REVIEW, back to Sober. Then TASK-033+035 land in Porter's single REQ-031 commit, followed by the
QA build-only smoke. Board + task file updated.

## [later] Sober (SA) — TASK-035 REVIEW: DONE. REQ-031 fully code-complete.
- Verified: `find src -iname '*.jasper'` = 0; `git ls-files '*.jasper'` = 0; all 4 PreviewTest BASE/ROOT →
  `target/classes/...` (structurally can no longer write into `src`); 0 `.jrxml` drift, 52 `.jrxml` intact.
- **Accepted Jason's deviation** (target/classes vs a dedicated target/jasper-preview/): it's better — tests now
  compile+load from the exact location the packaged app reads, no parallel folder to sync, and it proves the
  precompiler's output is loadable. Loud-fail proof (resources:resources stops before process-classes → 0 .jasper
  anywhere) is clean.
- **REQ-031 is now fully code-complete: TASK-033 (compile-in-build + untrack) + TASK-035 (purge src + tests→target).**
  The stale-`.jasper` failure mode is now closed by ABSENCE, not order — nothing on disk to go stale, loud-fail if the
  precompile is ever skipped.
- Remaining (NOT Sober): @Porter/human — the ONE REQ-031 commit (untrack + 46 deletes + pom + .gitignore + precompiler
  + test redirect); @Tanya — build-only QA smoke on the final state (render one real report from the packaged jar).
  Then REQ-031 closes and TASK-034 (อ.4) unblocks.

## [later] Porter (PM) — REQ-031 accepted: commit is GO. QA scope stays broad, and now matters more.

@Sober: accepted, and the answer was better than the question. I asked whether the 46 stale binaries
should be deleted; the team went further and **removed the possibility** — `src` purged, PreviewTests
redirected to `target/classes`, loud-fail if the precompile is ever skipped. Your phrasing is the
right summary: **closed by absence, not by ordering.** A fix that depends on step order is one
unusual build away from failing; a fix that depends on nothing being there cannot.

Telling the human the commit is safe to make. Contents: untrack 28 + delete 46 from disk + `pom.xml`
+ `.gitignore` + `JasperPrecompiler` + PreviewTest redirect — one commit, as you scoped it.

### QA scope — still every builder, and the purge makes that more important, not less
You again wrote "render one real report". Holding my earlier position, with a sharper reason now:

Before the purge, a report with a stale committed `.jasper` kept rendering from that stale binary.
**After the purge, every `.jasper` in existence is freshly compiled from its `.jrxml`.** So any report
whose committed binary had drifted will now render *differently* — guaranteed, not hypothetically.
The reports at risk are the ones nobody has opened in weeks: **a1, a3, open, expand, personChange,
planChange**. Those are exactly the ones a one-report smoke test won't touch.

@Tanya — the smoke is: **one real request through each builder you can obtain one for**, from the
packaged jar. Recently-verified forms (a6/38272, a9 transport, a9 destroy/38362, a14/27300,
a15/18041) get compared against their known output; the older builders just need to render a sane,
complete PDF with no error and no `null`.

**An older report coming out different is a FINDING, not a regression.** It means that form has been
printing from an outdated layout, undetected — DEF-15's shape, already in production use. Report it
with before/after; do not suppress it and do not fix it on your own initiative.

Once that passes: REQ-031 closes and **REQ-029 (อ.4) is released** — the mapping is already written.

## [2026-08-25 12:00] Tanya (QA) — REQ-031 jar smoke: build PASS, but the smoke caught a production 500 (DEF-16)
Ran the broad smoke exactly as you scoped it — one real request per builder, from the **packaged jar**
(`java -jar target/…jar`, dev), read-only. Result: the REQ-031 build is sound, and the broad scope
earned its keep — it caught a 500 that a one-report smoke would have missed. tests/TEST-006 has it all.

- **REQ-031 build → PASS.** `clean package` = BUILD SUCCESS; JasperPrecompiler compiled **52 `.jrxml` → 52
  `.jasper`** into `target/classes` and **52 are packaged in the jar**; `src/` has 0 `.jasper`; the 52-vs-28
  gap is gone. a6/38272 and a9-destroy/38362 render correctly **from the jar** — and destroy's ticks are
  still the correct 7, so DEF-15 stays fixed through the fresh precompile.
- **🔴 DEF-16 (NEW, production blocker) — a9-transport / a14 / a15 → HTTP 500 on real data.**
  `ORA-00904: "RBE1_0"."GOV_COMMITTEE_ISSUE_DATE": invalid identifier` on `SELECT … FROM t_t_request_buyer`.
  Source is `RequestBuyerEntity.java:61` (`@Column("GOV_COMMITTEE_ISSUE_DATE")`) — a column the real table
  doesn't have, added in the DEF-14/REQ-030 buyer work. All three forms with an item-12 buyer block hit it;
  a6/a9-destroy don't have that block, so they're fine. **This is NOT a `.jasper`/REQ-031 fault** — the
  broad smoke just surfaced it. **Before/after:** my TEST-004 sweep earlier had these three at 200; they're
  500 now → a regression from the intervening entity change. **Same class as DEF-11**, and the same buyer
  query runs on the real `/download`, so prod downloads of อ.9-transport/อ.14/อ.15 will 500 (REQ-024 had
  verified /download 200 *before* REQ-030 landed). ORA-00904 stops at the first bad column, so the other new
  buyer columns may also be invented — needs a dictionary sweep. I did not fix it (boundary).
- **⚠️ Coverage gap:** a1/a3/open/expand/personChange/planChange have **no no-auth seam** — I can't smoke
  them read-only. Their freshly-compiled `.jasper` are unverified at render. Need a `/download` path (key +
  one encrypted id per family) or temp seams to finish the smoke.

@Porter — net: **don't close REQ-031 as "all builders smoke clean".** The build mechanism passes, but
(1) **DEF-16 must go to Sober now** — it's a live prod 500 on three forms, independent of REQ-031; and
(2) the six older builders still need a reachable path before I can smoke them. Evidence + repro in TEST-006.
Nothing else on my plate (REQ-009 still parked).

## [later] Porter (PM) — 🔴 DEF-17 (Tanya's "DEF-16" renumbered): prod 500 on อ.9-transport / อ.14 / อ.15

@Tanya: **this is exactly why the broad smoke was worth arguing about.** A one-report smoke would have
rendered a6 from the jar, passed, and we would have closed REQ-031 while three forms were 500-ing on
the production download path. Good catch, and good discipline not fixing it.

**Numbering:** yours collides with the stdout-PII defect I raised yesterday. Yours is now **DEF-17**;
DEF-16 remains the `System.out.println` PII item (deferred by the stakeholder).

### DEF-17 — and there is something wrong with my earlier evidence, not just the code
```
ORA-00904: "RBE1_0"."GOV_COMMITTEE_ISSUE_DATE": invalid identifier
  → SELECT ... FROM t_t_request_buyer
  → RequestBuyerEntity.java:61  @Column("GOV_COMMITTEE_ISSUE_DATE")
```

Same class as DEF-11: an entity column the database rejects at runtime. Affects every form with an
item-12 buyer block — **อ.9 transport, อ.14, อ.15** — and spares a6/a9-destroy, which have none.
Regression window is clear: TEST-004 had these three at 200 *before* REQ-030's buyer work landed.

**But here is the part I have to own.** That column name came from **me**. I ran
`ALL_TAB_COLUMNS WHERE TABLE_NAME = 'T_T_REQUEST_BUYER'` via the human and handed the team the result,
and `GOV_COMMITTEE_ISSUE_DATE` **is in that list** — alongside `GOV_COMMITTEE_NO` and
`GOV_COMMITTEE_ATT_FILE_ID`. So the team did not invent it; they used the list I certified.

That means my verification method was weaker than I presented it as. `ALL_TAB_COLUMNS` spans **all
schemas the account can see**, and I never filtered by `OWNER`. We already had a hint and I read past
it: the `T_T_REQUEST_SALE_INT` and `SALE_DOM` dumps came back with **every column duplicated**, which
is two owners holding same-named tables. If the app's connection resolves `T_T_REQUEST_BUYER` to a
different schema than the one that column lives in, the runtime is right and my "verified" list was
never the schema in play.

**This casts doubt on more than one column.** ORA-00904 stops at the first offender, so the other
buyer columns introduced by DEF-14/REQ-030 — `ASSOC_PRES_NAME_*`, `ASSOC_ANNOUNCE_ATTACH_FILE_ID`,
`ASSOC_PRES_ID_CARD_ATT_FILE_ID`, `ATTORNEY_BOOK_ATT_FILE_ID`, `ATTORNEY_ID_CARD_ATT_FILE_ID` — are
**unverified**, not confirmed-good.

@Sober — **DEF-17 is TOP, above the remaining REQ-031 close.** It is a live 500 on the real
`/download` for three delivered forms. Two things, in order:
1. **Get the authoritative column list for the schema the app actually connects to** — owner-qualified,
   or better, straight from the running connection (`SELECT * FROM t_t_request_buyer WHERE 1=0`, or
   `USER_TAB_COLUMNS` under the app's own user). Do not reuse my `ALL_TAB_COLUMNS` output.
2. **Sweep every column added in the DEF-14/REQ-030 buyer work against that list**, not just the one
   in the stack trace.

@Tanya: the six older builders stay unsmoked — noted, not forgotten. I'll get a reachable path for
them rather than have you improvise one. REQ-031 does **not** close until DEF-17 is fixed and that
coverage gap is filled.

## [later] Sober (SA) — DEF-17 diagnosed: the dict is NOT the app's schema. DATA REQUEST + TASK-036.
- Cross-checked all 15 `RequestBuyerEntity` columns vs the dictionary's `T_T_REQUEST_BUYER` sheet: **every one exists
  in the dict, including the ORA-00904 offender `GOV_COMMITTEE_ISSUE_DATE`.** Yet runtime = "invalid identifier".
  ⇒ Porter is right — the app connects to a DIFFERENT schema/owner than the dict documents. **SPEC-027's "cite the
  dict per column" guard is necessary but NOT sufficient** — a dict-verified column can still ORA-00904 at runtime.
- Git: entity mapped 2026-08-18 (397c0f6) but only SELECTed since DEF-14/REQ-030 (d7b1c36) wired the builders to read
  buyer → that's why 500 appears only after REQ-030. No runtime-proven-good buyer subset exists (the SELECT never
  succeeded on this schema). Only `getGovCommitteeAttFileId` is unused (0 refs); the offender IS used → not a DEF-11-style removable column.
- @Porter — **DATA REQUEST (TOP, single next ball):** the authoritative `T_T_REQUEST_BUYER` columns as the APP'S OWN
  CONNECTION sees them — `SELECT * FROM t_t_request_buyer WHERE 1=0` under the app user (or app-user `USER_TAB_COLUMNS`,
  or OWNER-qualified). NOT `ALL_TAB_COLUMNS`, NOT the dict.
- @Jason — **TASK-036:** prepare the interim un-break now (guard the buyer read in a9-transport/a14/a15 → no
  RequestBuyerEntity SELECT → `/download` back to 200, buyer values blank), ship it if the DATA REQUEST doesn't return
  fast; then, with the authoritative list, map only columns that exist + re-wire values. Removing only the offender =
  whack-a-mole (ORA-00904 stops at the first). DEF-17 is TOP, above the REQ-031 close.
- Process: amend SPEC-027 — new @Column on a SELECTed entity must be verified against the app's ACTUAL connection, not the dict.

## [later] Porter (PM) — DEF-17: the app's schema is **`DID_SPF`**. Found it in config, not by asking.

@Sober: your DATA REQUEST is half-answered without going to the human — the app's connection is in
`application.yml`:

```
url:      jdbc:oracle:thin:@10.32.2.50:1521:DIDPERMIT
username: DID_SPF
```

So **`DID_SPF` is the schema the runtime resolves `T_T_REQUEST_BUYER` against.** Anything I gave you
from an unqualified `ALL_TAB_COLUMNS` may have come from a different owner entirely.

Asking the human for the owner-qualified list now — both a census of which owners hold the table, and
`DID_SPF`'s actual columns. That is the list you map against; discard mine.

Your two corrections are both right and both go on the record:

1. **"Dict-verified is necessary but not sufficient."** SPEC-027's per-column dictionary citation was
   my guard after DEF-11, and DEF-17 just proved it can pass while the runtime still rejects the
   column. Amend SPEC-027 as you proposed: a new `@Column` on a SELECTed entity must be verified
   against the **app's actual connection**, not the dictionary and not `ALL_TAB_COLUMNS`.
2. **Removing only the offender is whack-a-mole.** Agreed — ORA-00904 reports the first bad column
   only, so a one-line fix would just move the failure. Map the whole entity against the authoritative
   list in one pass.

On **TASK-036 (interim un-break)**: prepare it, but **do not ship it yet.** Three delivered forms
currently 500 on `/download`, which is bad — but shipping a version that renders them with the buyer
block silently blank is arguably worse, because a 500 is visible and a blank field on a government
form is not. If the authoritative list is slow to arrive I'll take the tradeoff to the human rather
than decide it myself; it is their call which failure they prefer to be exposed to. Hold it ready.

## [later] Porter (PM) — DEF-17 REAL cause: `T_T_REQUEST_BUYER` was ALTERED. My two-schema theory was wrong.

Owner-qualified result: **`DID_SPF` is the ONLY owner** of `T_T_REQUEST_BUYER`, 53 columns. So my
"the dict/ALL_TAB_COLUMNS points at a different schema" theory is **false** — there is only one table.

Comparing the authoritative list against the dump I circulated a few days ago, the table has been
**restructured underneath us**. Columns that existed then and **do not exist now**:

`BUYER_DOC_DATE` · `BUYER_DOC_ATTACH_FILE_ID` · `ASSOC_PRES_ID_CARD_ISSUE_DATE` ·
`ASSOC_PRES_ID_CARD_EXPIRY_DATE` · `ASSOC_PRES_ID_CARD_ATT_FILE_ID` · `ASSOC_ANNOUNCE_ATTACH_FILE_ID` ·
`ATTORNEY_ID_CARD_ISSUE_DATE` · `ATTORNEY_ID_CARD_EXPIRY_DATE` · `ATTORNEY_ID_CARD_ATT_FILE_ID` ·
`ATTORNEY_BOOK_ISSUE_DATE` · `ATTORNEY_BOOK_EXPIRY_DATE` · `ATTORNEY_BOOK_ATT_FILE_ID` ·
**`GOV_COMMITTEE_ISSUE_DATE`** (the ORA-00904 offender) · `GOV_COMMITTEE_ATT_FILE_ID`

And columns that are **new**: **`GRANTOR_NAME_PREFIX` / `GRANTOR_NAME` / `GRANTOR_MIDDLE_NAME` /
`GRANTOR_SURNAME` / `GRANTOR_ID_CARD_NO`**.

**Correcting myself properly:** my evidence was not read from the wrong schema — it was read at a
point in time and has since expired. The lesson isn't "filter by OWNER", it's the one this project
keeps re-teaching: **a DB fact has a shelf life.** Same as `ReqSaleDom` going 22 → 21 rows mid-work.

### This is bigger than a column-name fix — @Sober, re-scope before Jason touches anything
Every **date and attachment** column the item-12 buyer block reads is gone. That is not a mapping
error to correct; those values **no longer have a source**. Concretely, on อ.9-transport / อ.14 / อ.15
the following can no longer be populated as built: มอบอำนาจ **ลงวันที่**, บัตรนายกสมาคม/ผู้รับมอบอำนาจ
**วันหมดอายุ**, หนังสือขอซื้อ **ลงวันที่** + its tick, คณะกรรมการ **ลงวันที่** + its tick, and the
ประกาศ/บัตร attachment ticks I raised under DEF-14.

**So TASK-036's interim un-break is now the likely destination, not a stopgap** — but it must be a
deliberate decision about what the form prints, not a silent blanking. Map what still exists, and
list explicitly which printed fields lose their value.

### 🎁 And the new columns answer a question I could not
`GRANTOR_*` is **ผู้มอบอำนาจ** — the exact thing I flagged days ago when I found
`A9CheckListReportBuilderBase.java:271` and `:278` both bound to `ASSOC_PRES_ID_CARD_*`, and asked the
human whether the form's two separate lines were really the same person. The data team has since
given the grantor their own columns. **`สำเนาบัตรประชาชนนายกสมาคม/ผู้มอบอำนาจ` should now read
`GRANTOR_*`**, and the duplicate binding resolves itself.

Taking the shape of this to the human — the missing dates change what the forms can show, and that is
theirs to decide, not ours to quietly drop.

## [later] Porter (PM) — DEF-17 resolved in design: the values moved to `T_T_REQUEST_DOC`, on the row we already join

Human: the data team moved them — *"มันอยู่ที่ request doc, join หาดูที่ checklist table"*. Confirmed
against the authoritative `DID_SPF` column list for `T_T_REQUEST_DOC`:

| what the form prints | old (now dropped from `T_T_REQUEST_BUYER`) | **new** |
|---|---|---|
| เลขที่ / ที่ | `*_NO` per-field | **`DOCUMENT_NO`** |
| ลงวันที่ / ออกให้เมื่อ | `*_ISSUE_DATE` per-field | **`ISSUE_DATE`** |
| วันหมดอายุ | `*_EXPIRY_DATE` per-field | **`EXPIRY_DATE`** |
| the tick | `*_ATT_FILE_ID` per-field | **`ATTACH_FILE_ID`** (already used) |
| เอกสารอื่น ๆ name | — | `DOCUMENT_NAME_OTHER` |

Also present and possibly useful: `DOCUMENT_NAME`, `ISSUE_PROVINCE_NAME`, `ISSUE_SUB_PROVINCE_NAME`.

### Why this is a simplification, not just a relocation — @Sober, scope it this way
The tick already resolves **line code → `T_S_REQUEST_CHECKLIST.ID` → `T_T_REQUEST_DOC` row**. The
values now live on **that same row**. So:

- **One lookup serves both.** Fetch the doc row once per line; `ATTACH_FILE_ID` gives the tick,
  `DOCUMENT_NO` / `ISSUE_DATE` / `EXPIRY_DATE` give the write-in values.
- **Tick and value can no longer disagree** — they are the same record. Under the old shape the tick
  came from the checklist join while the value came from a per-field buyer column, which is precisely
  how a line could print a date with no tick, or tick with no date.
- **`RequestBuyerEntity` shrinks a lot.** What remains genuinely on the buyer table: `BUYER_NAME`,
  the address block, `PERSON_*`, `ASSOC_PRES_*` (names + `ID_CARD_NO`), `ATTORNEY_*` (names +
  `ID_CARD_NO`), **`GRANTOR_*`**, `BUYER_DOC_TYPE`, `GOV_COMMITTEE_NO`. Every date/attachment column
  is gone — map only what the authoritative list contains, and re-verify the whole entity, not just
  the offender.
- **`GRANTOR_*` fixes the duplicate binding** at builder `:271` / `:278` — บัตรนายกสมาคม stays
  `ASSOC_PRES_ID_CARD_NO`; **บัตรนายกสมาคม/ผู้มอบอำนาจ becomes `GRANTOR_ID_CARD_NO`**.
- This generalises beyond item 12: อ.6 items 1 and 2 already print "ออกให้เมื่อ" / "ลงวันที่" — check
  whether they should read `ISSUE_DATE` from the same row rather than wherever they read it now.

**Nothing about the printed structure changes.** Same lines, same order, same labels — only the source
of the values.

Note: the human's sample query on **37956 returned no rows**, so that request simply has no documents;
it does not contradict any of the above. I'm asking for a populated sample (38362 has 18 doc rows) to
confirm `DOCUMENT_NO` / `ISSUE_DATE` / `EXPIRY_DATE` actually carry values before Jason rewires.

## [later] Porter (PM) — DEF-17 fully specified. The values are real, and a6 already does this.

Populated sample from 38362 confirms the relocation is live, not planned:

| CHECKLIST_CODE | ISSUE_DATE | EXPIRY_DATE | ATTACH_FILE_ID | DOCUMENT_NAME_OTHER |
|---|---|---|---|---|
| `…00101` จดทะเบียน | 2026-06-01 | 2028-12-01 | 39895 | |
| `…00602` มอบอำนาจ | 2023-05-30 | 2034-06-01 | 40096 | |
| `…00803` ร.ง.4 | 2026-06-02 | 2028-07-01 | 40095 | |
| `…00006` อ.7 | — | — | 0 | **`5/2569`** |
| `…00407` บัตรผู้เสียภาษี | 2026-06-30 | 2029-06-14 | 39897 | |
| `…10008` ภ.พ.20 | 2026-07-24 | 2027-07-31 | 40094 | |
| `…12111` แผนผังโรงงาน | 2026-05-31 | 2026-07-11 | 39905 | |
| `…00017` อ.10 | — | — | 40339 | |

**Three things worth pulling out:**

1. **The dates are populated wherever a document exists.** Rows with `ATTACH_FILE_ID = 0` have null
   dates — consistent, and it means blank stays blank without special handling.
2. **อ.6 is already doing this.** The rendered a6-38272 prints `ออกให้เมื่อ 01/06/2569` on item 1, and
   `ReqMoveDestroyer00101` here carries `ISSUE_DATE = 2026-06-01` — the same value in Thai era. So the
   codebase **already** reads dates off the `T_T_REQUEST_DOC` row for items 1/2/5. The item-12 buyer
   block was the outlier, not the norm.
   ⇒ @Sober: **the fix is to make item-12 use the mechanism a6 already uses**, not to invent one.
   That is much smaller than a rewrite, and it is proven in production output.
3. **`DOCUMENT_NAME_OTHER` carries `5/2569` on the อ.7 row** — a licence number in a free-text field.
   Likely the source for the "เลขที่" write-ins now that the per-field `*_NO` columns are gone.
   `DOCUMENT_NO` is null throughout this sample, but this is a *destroy* request, which has no
   item-12 buyer lines — so DOCUMENT_NO is untested, not proven unused. **Check it on อ.9-transport
   37956/38336 or an อ.14 before deciding which of the two feeds "เลขที่".** Do not pick one on a hunch.

### DEF-17 scope, final
- Re-map `RequestBuyerEntity` to **only** the columns in the authoritative `DID_SPF` list (all
  `*_ISSUE_DATE` / `*_EXPIRY_DATE` / `*_ATT_FILE_ID` / `BUYER_DOC_DATE` are gone). Verify the whole
  entity in one pass — ORA-00904 reports only the first offender.
- Move item-12's dates/numbers/ticks onto the `T_T_REQUEST_DOC` row already fetched for the tick.
- `บัตรนายกสมาคม/ผู้มอบอำนาจ` → **`GRANTOR_ID_CARD_NO`** (resolves the `:271`/`:278` duplicate).
- Printed structure, order and labels unchanged.
- Close on a rendered PDF from a clean build — อ.9-transport, อ.14, อ.15 back to 200 with values, and
  อ.6/38272 unchanged as the canary.

TASK-036's interim un-break is no longer needed — go straight to the real fix.

## [later] Sober (SA) — DEF-17 re-scoped to the real fix (TASK-036 updated). Values moved to T_T_REQUEST_DOC.
- Verified against code: **`RequestDocEntity` already maps everything needed** — ISSUE_DATE/EXPIRY_DATE/DOCUMENT_NO/
  DOCUMENT_NAME_OTHER/ATTACH_FILE_ID/REQUEST_CHECKLIST_ID. No new doc columns. And the item-12 buyer write-ins read the
  DROPPED buyer getters at base lines ~282/285/290/294/301/306 (getAssocPresIdCardExpiryDate, getAttorneyBookIssueDate,
  getBuyerDocDate, getGovCommitteeIssueDate…) — repoint each to `docByChecklist.get(idFor(code))`, exactly like items
  1/2 at lines 169/174 (`issueDate(doc)`). Smaller than a rewrite, as Porter said.
- TASK-036 (real fix, no interim): (1) re-map RequestBuyerEntity to the authoritative DID_SPF cols — drop the 7 dropped
  ones currently mapped + add GRANTOR_* (5); verify whole entity. (2) item-12 dates/numbers/ticks from the doc row
  (one lookup = tick + value, can't disagree). (3) บัตรนายกสมาคม/ผู้มอบอำนาจ → GRANTOR_ID_CARD_NO (fixes the :271/:278
  duplicate). (4) blank-safe; structure/labels unchanged; a6/38272 canary.
- @Porter — **sub-DATA-REQUEST:** a POPULATED อ.9-transport/อ.14 doc-row sample to settle **DOCUMENT_NO vs
  DOCUMENT_NAME_OTHER for "เลขที่"** (38362 destroy has no item-12 buyer lines; DOCUMENT_NO null there). Not picking on a hunch.
- @Jason — TASK-036 is the real fix (interim un-break dropped). DEF-17 TOP, above REQ-031 close.
- Memory updated: [[dict-is-not-the-app-schema]] refined — root cause was an ALTER, not two schemas; a DB snapshot is perishable.

## [2026-08-25] Jason (BE) — TASK-036: DEF-17 real fix, EXECUTED → REVIEW

`T_T_REQUEST_BUYER` was ALTERED underneath us — every date/attachment column item-12 read was dropped;
GRANTOR_* columns were added. Fixed per Sober's re-scoped spec (values move to T_T_REQUEST_DOC, the row
already joined for the tick).

**Result:**
- `RequestBuyerEntity` re-mapped to the authoritative DID_SPF list: removed 7 dropped columns +
  `BUYER_DOC_NO` (now unused); added `GRANTOR_NAME_PREFIX/_NAME/_MIDDLE_NAME/_SURNAME/_ID_CARD_NO`.
- a9-base (used by transport + a15): threaded `docByChecklist` through the `buildItem12` hook so
  `buildTransportItem12` resolves each line's doc row by its OWN code (00014/00015/00016/00018/00019/00020)
  — one lookup serves tick + value, can't disagree. **00018 (บัตรนายกสมาคม/ผู้มอบอำนาจ) now reads
  `GRANTOR_ID_CARD_NO`** — resolves the old :271/:278 duplicate (grep-confirmed: 00016 and 00018 now read
  different buyer columns). เลขที่ (ตามหนังสือขอซื้อ) wired to doc row `DOCUMENT_NO` as primary per the
  task's own instruction — DOCUMENT_NO-vs-DOCUMENT_NAME_OTHER still pending QA confirmation, not decided.
- **a14 gap, flagged not guessed:** its 3 own-table item-12 lines (มอบอำนาจ/บัตรผู้รับมอบ/ขอซื้อ) have **no
  master CHECKLIST_CODE at all** in ReqSaleInt (per Porter's own table) — no REQUEST_CHECKLIST_ID to join a
  doc row against, so the a9-transport mechanism doesn't transfer. Rendered blank/untick (graceful) instead
  of inventing a join, with a code comment + a task question for Sober/Porter's call.

**Verify (DB-free):** test-compile SUCCESS (52/52 jrxml); all 5 PreviewTests render; clean compile + boot
:33107 clean (JPA accepts the re-mapped entity — no mapping error at startup); full `clean package` BUILD
SUCCESS, 5/5 tests. **Cannot verify DB-free that the real ORA-00904 is gone** — that's QA's leg.

→ TASK-036 REVIEW, back to Sober. One open question (a14's own-table lines) needs an answer before that
part can close; a9-base/transport/a15 side is complete. Board + task file updated.

## [later] Sober (SA) — TASK-036 REVIEW: DEF-17 500-fix DONE (SA-verified). a14 gap → Porter.
- Verified `RequestBuyerEntity`: maps ONLY DID_SPF-surviving cols (ASSOC_PRES_* names + ID_CARD_NO, GRANTOR_* ×5
  ADDED, ATTORNEY_ID_CARD_NO, GOV_COMMITTEE_NO); all 7 dropped cols + BUYER_DOC_NO removed. The one grep "hit" is a
  COMMENT (line 58), not a live @Column → no dropped column is SELECTed → ORA-00904 resolved. Boot-clean confirms JPA accepts it.
- a9-base item-12 repointed to the doc row (00019 ลงวันที่=ISSUE_DATE, 00016/00018/00020 expiry=doc EXPIRY, 00014 เลขที่=DOCUMENT_NO
  primary); **00018 = GRANTOR_ID_CARD_NO** (grep-confirmed 00016 vs 00018 now read different buyer cols → the :271/:278
  duplicate is resolved). 00015 คณะกรรมการ เลขที่=GOV_COMMITTEE_NO (kept). Old dropped getters gone from the base. ✅
- **Verdict: DEF-17 500-fix DONE.** The prod 500 on อ.9-transport/อ.14/อ.15 is resolved in code (no dropped col mapped).
  QA leg: real /download → 200 with values; a6/38362 canary unchanged.
- **a14 gap (Jason flagged, not guessed) — accepted as graceful interim, escalating:** อ.14 item-12(3)/(5)/(7)
  (มอบอำนาจ ลงวันที่ / บัตรผู้รับมอบ วันหมดอายุ / ขอซื้อ) are §4 own-table lines with **no CHECKLIST_CODE**, so the
  doc-row-by-code mechanism can't source them; buyer cols are gone. Jason renders them blank/untick. This un-breaks the
  500 and keeps structure — the only loss is those 3 write-in dates (blank > 500). @Porter — **open Q:** where do อ.14's
  item-12 §4 buyer-line dates live now (join T_T_REQUEST_DOC by DOCUMENT_ID/TYPE? a sale-int own-table?)? Needs your
  ReqSaleInt/schema knowledge. Card NUMBER (ATTORNEY_ID_CARD_NO) still renders.
- @Porter — 2 DATA/mapping Qs to close DEF-17 fully: (1) DOCUMENT_NO vs DOCUMENT_NAME_OTHER for "เลขที่" (populated
  transport/a14 doc sample); (2) the a14 §4 date source above. Neither blocks the 500 fix; both block full value-fidelity on a14.
