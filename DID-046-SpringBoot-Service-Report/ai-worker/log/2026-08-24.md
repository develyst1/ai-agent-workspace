# Log — 2026-08-24 (Jason / BE)

> Continues `2026-08-21.md`. TASK-029 respec (TICK RULE) execution for the a9-base family.

## TASK-029 (respec'd) — evidence ticks per the stakeholder TICK RULE, a9-family EXECUTED → REVIEW

Sober reviewed my Part A then relayed Porter's RESPEC: REQ-030 now follows the stakeholder's canonical **TICK
RULE** (line's hardcoded CHECKLIST_CODE → T_S_REQUEST_CHECKLIST.ID → T_T_REQUEST_DOC by REQUEST_CHECKLIST_ID →
tick iff a doc with ATTACH_FILE_ID>0 & STATUS≠D exists). IS_ACTIVE and the BUYER `*_ATT_FILE_ID`-OR are DEAD.
Redid the binding to the rule across the a9-base family (transport / a15 / destroy):

- **Mechanics:** master = `findByGroupCode(checklistGroup())` (IS_ACTIVE filter deleted); docs = new NULL-safe
  `@Query findActiveByRequestId`; `hasFile` = `ATTACH_FILE_ID != null && != 0` (was the getAttachFile() join);
  `idFor(idByCode, suffix)` resolves code→ID once per report. Positional `mid` fully removed (hook signature dropped it).
- **Code-bound (codes in SPEC-032):** items 1-11; transport/a15 item-12 buyer-doc lines 00016/00017/00018/00019/00020
  (00016 vs 00018 now distinct code-ticks, value still the one shared ASSOC_PRES_ID_CARD_* set); ส.ค.4=00013,
  แผนกระสุน=00021, ภาพถ่าย=00022; **destroy item-12 (3)-(9) = 00013-00019** ((1)/(2) no code → untick). DEF-14
  ชื่อนายกสมาคม from ASSOC_PRES_NAME_* (value).
- **Scope:** a9 transport + a15 + destroy (shared base). a14/a6 NOT touched (a6 = separate builder → 38272 canary safe).

**Verify (DB-free):** test-compile + clean compile + boot :33097 all green (the new JPQL + derived finders
validated at startup); 5 PreviewTests render. Previews are the mock path → no-break only; **tick landing = QA's
real-data leg** (real ReqMove + ReqSaleDom + a destroy sample; toggle SEQUENCE/IS_ACTIVE → nothing changes; a6/38272 unchanged).

**⚠️ Raised a code request (did NOT guess):** three item-12 transport/a15 lines have NO CHECKLIST_CODE in
SPEC-032 — **ตามหนังสือขอซื้อ**, **ตามหนังสือคณะกรรมการ**, and the **4 permit-row headers (ป.3/ป.5/มหาดไทย/
ยุทธภัณฑ์)**. The rule forbids their current own-table-ATT tick, but with no code I left them as-is + flagged in
code, and asked Sober for their codes (+ confirm the destroy (3)-(9)→00013-00019 order mapping). a14/a6 remain
gated on their full `ReqSaleInt`/`ReqSpecial` code lists.

→ TASK-029 a9-family REVIEW, back to Sober. Board + task file updated.

## [later] Sober (SA)
- Reviewed **TASK-029 a9-family (TICK RULE)** — mechanism verified: IS_ACTIVE filter gone (`findByGroupCode`),
  NULL-safe `findActiveByRequestId`, `hasFile`=ATTACH_FILE_ID>0, `idFor` code→ID, positional `mid` removed,
  items 1-11 + ส.ค.4/มอบอำนาจ/บัตร code-bound, DEF-14 name; compile + A9/A15/A6 previews green.
- **Answered Jason's code request:**
  1. **ขอซื้อ=00014, คณะกรรมการ=00015** — they ARE in the REQ-030 table (my SPEC-032 omission, now fixed).
     Code-bind them; own-table BUYER_DOC/GOV_COMMITTEE ATT → value-only. **One small fix to finish a9-family.**
  2. **Permits (ป.3/ป.5/มท./ยุทธภัณฑ์) — keep own-table tick** (REQ-030 §4 carve-out: no master row). Jason's
     addLicenseRows is correct as-is. (Confirming with Porter that §4 survives the "one source" wording.)
  3. **destroy (3)-(9) = 00013-00019 CONFIRMED** sequential (สถานที่กำจัด00013 … ผู้เชี่ยวชาญ00019); (1)/(2) no code.
- @Jason: one fix (code-bind ขอซื้อ 00014 / คณะกรรมการ 00015) → then a9-family code-DONE. Ping me.
- @Porter: (a) confirm permit lines keep own-table tick under the final rule (REQ-030 §4 vs "one source"); (b)
  still need **ReqSaleInt (a14) + ReqSpecial (a6) full per-line CHECKLIST_CODE lists** to convert those two.
  a9 transport/a15/destroy tick-landing = QA real-data (real ReqMove+ReqSaleDom+38362; a6/38272 canary).

## [later] Jason (BE) — Sober's one fix applied → a9-family CODE-DONE
Code-bound the two lines Sober confirmed were in the table: **ตามหนังสือขอซื้อ = 00014**, **ตามหนังสือคณะกรรมการ
= 00015** (tick now via `checked(checkedIds, idFor(..))`; BUYER_DOC/GOV_COMMITTEE ATT = value-only). **Permit rows
kept own-table** (`addLicenseRows` unchanged, §4 carve-out). destroy (3)-(9)=00013-00019 already matched Sober's
confirm. Verify: test-compile + A9/A15/A6 previews + boot :33098 all green. **a9-family (transport/a15/destroy)
TICK RULE code-DONE.** Remaining on TASK-029: a14/a6 conversion (gated on their ReqSaleInt/ReqSpecial code lists)
+ QA real-data tick-landing. → back to Sober.

## [later] Sober (SA)
- Reviewed Jason's fix — **a9-family (transport/a15/destroy) TICK RULE = code-DONE, SA-verified.** ขอซื้อ→
  idFor("00014"), คณะกรรมการ→idFor("00015") (values still BUYER); permits own-table (§4); destroy 00013-00019;
  IS_ACTIVE gone; DEF-14 name. test-compile + A9/A15/A6 previews green.
- REQ-030 a9-family closed on our side. Remaining on TASK-029: a14/a6 conversion (gated on ReqSaleInt/ReqSpecial
  code lists) + QA real-data tick-landing.
- @Porter — **single next ball to you:** QA the a9-family tick-landing on real data (real ReqMove + ReqSaleDom
  + 38362; toggle SEQUENCE/IS_ACTIVE → nothing moves; a6/38272 unchanged). Then, to unblock a14/a6, the
  ReqSaleInt + ReqSpecial per-line CHECKLIST_CODE lists. (Permit own-table §4 confirm is low-priority.)

## [later] Porter (PM) — (a) permits confirmed; (b) `ReqSpecial` list delivered, `ReqSaleInt` pending a fresh dump

@Sober: a9-family closure noted. Both your asks:

### (a) Permit lines keep their own-table tick — CONFIRMED
§4 survives the "one source" wording, and there is no conflict between them. The rule the stakeholder
gave applies to a line **that has a `CHECKLIST_CODE`**: for such a line, the code→doc join is the only
tick source. ป.3 / ป.5 / มหาดไทย / ยุทธภัณฑ์ have **no master row at all** — they are rendered from
their own tables with their own `ATTACH_FILE_ID`. There is no code to match, so nothing was withdrawn
for them. `addLicenseRows` stays as-is.

The distinction to hold: my withdrawn "OR" ruling was about lines that **do** have a code, where I had
proposed also consulting `T_T_REQUEST_BUYER.*_ATT_FILE_ID`. That is dead. The permit rows are a
different case entirely.

### (b) `ReqSpecial` (อ.6) — full list, verified against the official form
9 rows, from the human's query. Mapping to the อ.6 form:

| อ.6 form line | CHECKLIST_CODE |
|---|---|
| 1 สำเนาหนังสือรับรองการจดทะเบียน ฯ (ไม่เกิน 6 เดือน) | `ReqSpecial00101` |
| 2 หนังสือมอบอำนาจ | `ReqSpecial00602` |
| 3 / 4 เอกสารผู้มีอำนาจลงนาม / ผู้รับมอบอำนาจ | *(no master row — persons, rendered from `T_T_REQUEST_PER`)* |
| 5 สำเนาใบอนุญาตประกอบกิจการโรงงาน (ร.ง.4) (ลำดับ 9) | `ReqSpecial00803` |
| 6 สำเนาบัตรประจำตัวผู้เสียภาษีของนิติบุคคล | `ReqSpecial00404` |
| 7 ใบทะเบียนภาษีมูลค่าเพิ่ม (ภ.พ. 20) | `ReqSpecial10005` |
| 8(1) หนังสือของส่วนราชการผู้ว่าจ้างให้ผลิตส่วนประกอบของอาวุธฯ | `ReqSpecial00006` |
| 8(2) สำเนาสัญญาจ้างผลิต พร้อมคุณลักษณะเฉพาะฯ | `ReqSpecial00007` |
| 8(3) สำเนาหนังสือที่ รมว.กห. (ปล.กห.รับคำสั่ง) เห็นชอบฯ | `ReqSpecial00008` |
| 8(4) รายละเอียดส่วนประกอบของอาวุธที่จะทำการผลิต | `ReqSpecial00009` |

Cross-checked against the rendered a6-38272 output — the form's item order and these nine rows agree
1:1. Note `ReqSpecial00404` / `10005` differ from อ.9's `00407` / `10008` for the same documents:
**codes are group-scoped**, as flagged. Do not reuse suffixes across groups.

อ.6 is the regression canary: it is correct today, so after conversion **38272 must render identically**.

### `ReqSaleInt` (อ.14) — deliberately not giving you the old list
I hold a `ReqSaleInt` dump from before the rebuild (15 rows). The data team has since rebuilt
`ReqMove` and `ReqSaleDom` to 22 rows each, and I have no evidence about whether `ReqSaleInt` was
touched in the same pass. Handing you a possibly-stale list is exactly the failure that produced the
cancelled seed request and the "ID is stable" mistake. **Requesting a fresh dump from the human now** —
a14 conversion stays gated until it arrives.

## [later] Porter (PM) — `ReqSaleInt` (อ.14) full list. TASK-029 is now fully unblocked.

Fresh dump received. **`ReqSaleInt` was NOT touched by the rebuild** — still 15 rows, same codes as
the pre-rebuild copy I was holding. So the caution cost one query and bought certainty; worth it, and
now the list is confirmed current rather than assumed.

### อ.14 form line → `CHECKLIST_CODE`

| อ.14 form line | CHECKLIST_CODE |
|---|---|
| 1 สำเนาหนังสือรับรองการจดทะเบียน ฯ (ไม่เกิน 6 เดือน) | `ReqSaleInt00101` |
| 2 หนังสือมอบอำนาจ | `ReqSaleInt00602` |
| 3 / 4 ผู้มีอำนาจลงนาม / ผู้รับมอบอำนาจ | *(no master row — `T_T_REQUEST_PER`)* |
| 5 ร.ง.4 (ลำดับ 9) | `ReqSaleInt00803` |
| 5 อ.2 | `ReqSaleInt12204` |
| 5 สลักหลัง (แบบ อ.2) | `ReqSaleInt12305` |
| 6 สำเนาบัตรประจำตัวผู้เสียภาษีของนิติบุคคล | `ReqSaleInt00406` |
| 7 ใบทะเบียนภาษีมูลค่าเพิ่ม (ภ.พ. 20) | `ReqSaleInt10007` |
| 8 สำเนาเอกสารแสดงคุณลักษณะและคุณสมบัติของอาวุธ | `ReqSaleInt00008` |
| 9 แผนที่แสดงสถานที่ตั้งโรงงาน ฯ | `ReqSaleInt00009` |
| 10 แผนผังโรงงาน | `ReqSaleInt12110` |
| 11 แผนการขนย้ายตาม มาตรการฯ พ.ศ.2556 | `ReqSaleInt00011` |
| 12(1) หนังสือรับรองการจดทะเบียนเป็นนิติบุคคลของบริษัทผู้ซื้อ | `ReqSaleInt00012` |
| 12(2) หลักฐานการขอซื้ออาวุธ ฯ จากเจ้าหน้าที่รัฐบาลของประเทศผู้ซื้อ | `ReqSaleInt00013` |
| 12(4) หนังสือรับรองผู้ใช้ปลายทาง (END-USER CERTIFICATE) | `ReqSaleInt00014` |
| 12 ภาพถ่ายสนามยิงปืนและช่องยิงปืน ฯ | `ReqSaleInt00015` |
| 12 มอบอำนาจ / บัตรผู้รับมอบ / person2 / ขอซื้อ / ป.3 / ป.5 / มท. / ยุทธภัณฑ์ | *(no master row — own tables, §4 carve-out)* |
| 13 เอกสารอื่น ๆ (ถ้ามี) | *(no master row)* |

**Watch the numbering trap:** `ReqSaleInt` has **no อ.7 row**, so its sequence closes up relative to
`ReqMove` — บัตรผู้เสียภาษี is `00406` here but `00407` on อ.9, and ภ.พ.20 is `10007` vs `10008`.
Three groups, three different suffixes for the same two documents. Copying a suffix across groups
would mis-bind silently, which is the whole failure mode we are removing.

`12204` / `12305` are `IS_ACTIVE = 0` — bind them anyway; `IS_ACTIVE` is not part of the rule.

@Sober: **TASK-029 is fully unblocked** — you now hold `ReqSpecial` (previous entry) and `ReqSaleInt`
(above). Permit carve-out confirmed. Nothing further from me.

Remaining after code: QA real-data tick-landing (a9 transport / destroy 38362 / a15) with **a6/38272
as the unchanged canary**, plus the toggle test — renumber `SEQUENCE` or flip an `IS_ACTIVE` and
confirm the rendered PDF does not move.

## [later] Sober (SA)
- Porter delivered both code lists → **TASK-029 a14/a6 conversion UNBLOCKED.** Folded the full ReqSpecial(a6, 9
  codes) + ReqSaleInt(a14, 16 codes) mappings into TASK-029 with the group-scoped-suffix warning
  (บัตรผู้เสียภาษี a6=00404/a14=00406/a9=00407 — do NOT reuse), permit §4 carve-out, and a6/38272 = canary.
- @Jason — **single next ball to you:** convert a14 + a6 builders to the TICK RULE (same mechanism as the a9
  base; per-group suffixes in the task). Recommend a shared tick helper over a 3rd copy. a6/38272 must render
  identically. Then whole TASK-029 back to me → QA real-data tick-landing closes REQ-030.

## [later] Jason (BE) — a14 + a6 converted to the TICK RULE → whole TASK-029 code-DONE
Sober relayed Porter's ReqSpecial(a6) + ReqSaleInt(a14) code lists. Converted both builders. Per Sober's steer,
extracted a shared **`ChecklistCodeBinder`** util (idByCode / checkedChecklistIds[ATTACH_FILE_ID>0] / tick) instead
of a 3rd copy; a6 + a14 use it (a9-base keeps its verified inline copy).
- **a6 (ReqSpecial):** items 1/2/5/6/7/8(1-4) = 00101/00602/00803/00404/10005/00006-00009. **a14 (ReqSaleInt):**
  1-11 = 00101/00602/00803/12204/(อ.7+เปิดดำเนินการ→untick)/00406/10007/00008/00009/12110/00011; item-12(1)(2)(4)+
  ภาพถ่าย = 00012/00013/00014/00015; permits/ขอซื้อ/person2 = §4 own-table. Group-scoped suffixes respected
  (00404 vs 00406 vs 00407). master→findByGroupCode, docs→NULL-safe findActiveByRequestId, hasFile→ATTACH_FILE_ID>0.
- Verify: test-compile + clean compile + boot :33099 green; 5 PreviewTests render. **a6 predicate changed →
  38272 must be QA-confirmed identical (mandated canary)**; a14 has no attachments → ticks stay blank (no change).
→ whole TASK-029 code-DONE, all 5 forms on the TICK RULE. Back to Sober → QA real-data tick-landing.

## [later] Sober (SA)
- Reviewed a14+a6 — **whole TASK-029 code-DONE, SA-verified: all 5 forms on the TICK RULE.** Shared
  `ChecklistCodeBinder` (one place); a6=ReqSpecial 00101/00602/00803/00404/10005/00006-00009, a14=ReqSaleInt
  1-11 + 12(1)(2)(4)+ภาพถ่าย 00012-00015; **group-scoped suffixes correct/distinct** (00404 a6 / 00406 a14 /
  00407 a9); IS_ACTIVE gone; hasFile=ATTACH_FILE_ID>0; positional `mid` removed everywhere; DEF-14 name.
  test-compile + 4 previews green.
- **REQ-030 = code-complete on all 5 forms.** @Porter — single next ball: QA real-data tick-landing (ReqMove +
  ReqSaleDom + 38362 → ticks on their own lines; toggle SEQUENCE/IS_ACTIVE → PDF unchanged) and the mandated
  **a6/38272 canary = identical** (a6 predicate changed positional→code, so this is the key regression check).
  Nothing waits on Sober.

## [later] Porter (PM) — 🔴 DEF-15: อ.9 DESTROY ticks still land on the WRONG lines. TICK RULE not honoured.

Human supplied `T_T_REQUEST_DOC` ⋈ `T_S_REQUEST_CHECKLIST` for **38362**, and I compared it line by
line against the rendered PDF (`Downloads/20260824/a9-38362.pdf`, page 2). **They do not agree.**

### Attached documents on 38362 (rule: tick ⟺ ATTACH_FILE_ID > 0 AND STATUS ≠ 'D')
| CHECKLIST_CODE | document | file | status | rule says |
|---|---|---|---|---|
| `…00101` | จดทะเบียน | 39895 | A | **TICK** |
| `…00602` | หนังสือมอบอำนาจ | 40096 | A | **TICK** |
| `…00803` | ร.ง.4 | 40095 | A | **TICK** |
| `…00006` | อ.7 | **0** | A | no |
| `…00407` | บัตรประจำตัวผู้เสียภาษี | 39897 | A | **TICK** |
| `…10008` | ภ.พ.20 | 40094 | A | **TICK** |
| `…12111` | แผนผังโรงงาน | 39905 | C | **TICK** (C ≠ D) |
| `…00017` | อ.10 | 40339 | A | **TICK** |
| `…00009 / 00010 / 00012 / 00013 / 00014 / 00015 / 00016 / 00018 / 00019` | — | 0 | C | no |
| *(no checklist id)* | — | 40338 | **D** | no |
| `…12204` (อ.2) | **no doc row at all** | — | — | no |

**Expected: 7 ticks** — จดทะเบียน · มอบอำนาจ · ร.ง.4 · **บัตรผู้เสียภาษี** · ภ.พ.20 ·
**แผนผังโรงงาน** · **อ.10 (ข้อ 12(7))**

**Rendered: 7 ticks** — จดทะเบียน · มอบอำนาจ · ร.ง.4 · **อ.2** · **อ.7** · ภ.พ.20 ·
**สถานที่กำจัด (ข้อ 12(3))**

### The mismatches
| line | rule | rendered | |
|---|---|---|---|
| 5 อ.2 (`12204`) | untick — **no doc row exists** | **ticked** | ❌ |
| 5 อ.7 (`00006`) | untick — `ATTACH_FILE_ID = 0` | **ticked** | ❌ |
| 6 บัตรผู้เสียภาษี (`00407`) | **tick** — file 39897 | unticked | ❌ |
| 10 แผนผังโรงงาน (`12111`) | **tick** — file 39905, status C | unticked | ❌ |
| 12(3) สถานที่กำจัด (`00013`) | untick — file 0 | **ticked** | ❌ |
| 12(7) อ.10 (`00017`) | **tick** — file 40339 | unticked | ❌ |

The tick count happens to match (7 vs 7) and the first three land correctly, which is exactly why this
renders plausibly — but from the 4th line on it is still shifted. **This is the original defect,
unfixed on the destroy variant.**

@Sober — **DEF-15, TOP priority.** Points:
1. Jason reported destroy as covered ("destroy (3)-(9) = 00013-00019 already matched"). The rendered
   output says otherwise, so **the destroy path is not going through the code binder** — most likely
   the destroy builder still uses a positional list, or its `idFor` suffixes are the transport set.
2. **`STATUS = 'C'` must still tick** (`12111` proves it: file 39905, status C, rule says tick). If
   the implementation filters to `STATUS = 'A'`, that is a second bug hiding behind the first — the
   rule is "≠ D", nothing else.
3. **`12204` has no doc row at all and still ticked.** A line whose code matches nothing must be
   unticked; that cannot happen under a correct code lookup, which is further evidence the destroy
   path never reaches the binder.
4. Re-verify with this same request and this same table — it is now our best tick fixture, and it is
   real data, not a mock.

**อ.6 (38272) is unaffected and rendered identically to the pre-change output** (verified visually,
page 2 — all boxes ticked, structure intact). The canary holds, so this is isolated to อ.9 destroy.

## [later] Sober (SA)
- **DEF-15 diagnosed (TASK-030, BLOCKED).** Read the destroy code path: it's genuinely code-bound (no
  buildEvidences override → base idFor; buildItem12 idFor 00013-00019; doc query `STATUS IS NULL OR <>'D'` so
  STATUS=C ticks — the STATUS=A theory is OUT). **Root cause: the base hardcodes ONE suffix set (ReqMove's)
  shared by transport+destroy, varying only by group prefix — but codes are GROUP-SCOPED, and ReqMoveDestroyer's
  codes were Porter-STATED, never freshly dumped/verified** (a6/a14 got fresh dumps; destroy did not). QA's
  12204-ticks-with-no-doc + 12111-STATUS-C-untick are exactly a code-mismatch (idFor(ReqMoveDestroyerNNNNN)
  finds the wrong/no DB row), not positional and not a STATUS filter.
- **Owned my review gap:** I verified the mechanism + transport/a15/a6/a14 codes and flagged tick-landing as
  QA's leg (which caught it), but should have flagged that destroy's suffixes were ASSUMED = transport's, unverified.
- @Porter — **DATA REQUEST (single next ball):** a fresh `T_S_REQUEST_CHECKLIST WHERE GROUP_CODE='ReqMoveDestroyer'`
  dump (ID + CHECKLIST_CODE + CODE_NAME + SEQ + IS_ACTIVE), mapped 1:1 to the destroy form like a6/a14 — plus
  (optional) 38362's T_T_REQUEST_DOC REQUEST_CHECKLIST_IDs to pinpoint. Then Jason gives destroy its own per-line
  codes (the base can't reuse ReqMove's for a group-scoped ReqMoveDestroyer), re-verify 38362. a6/38272 + transport/a15 unaffected.

## [later] Porter (PM) — DEF-15: no data request needed, I already hold the dump. And the diagnosis is only half right.

@Sober: **blocking the DATA REQUEST — I have the fresh `ReqMoveDestroyer` dump already** (the human
ran it in the same pass as `ReqSpecial`). Here it is; nothing goes back to the human for this.

### `ReqMoveDestroyer` — 19 rows, form line → code
| destroy form line | code | SEQ | IS_ACTIVE |
|---|---|---|---|
| 1 จดทะเบียน ฯ | `ReqMoveDestroyer00101` | 1 | 1 |
| 2 หนังสือมอบอำนาจ | `…00602` | 2 | 1 |
| 5 ร.ง.4 (ลำดับ 9) | `…00803` | 3 | 1 |
| 5 อ.2 | `…12204` | 4 | 0 |
| 5 สลักหลัง (แบบ อ.2) | `…12305` | 5 | 0 |
| 5 อ.7 | `…00006` | 6 | 0 |
| 6 บัตรประจำตัวผู้เสียภาษี | `…00407` | 7 | 1 |
| 7 ภ.พ.20 | `…10008` | 8 | 1 |
| 8 คุณลักษณะและคุณสมบัติของอาวุธ | `…00009` | 9 | 1 |
| 9 แผนที่แสดงสถานที่ตั้งโรงงาน ฯ | `…00010` | 10 | 1 |
| 10 แผนผังโรงงาน | `…12111` | 11 | 1 |
| 11 แผนการขนย้าย ฯ พ.ศ.2556 | `…00012` | 12 | 1 |
| 12(3) สถานที่ที่จะทำการกำจัดหรือทำลาย พร้อมแผนผัง | `…00013` | 13 | 1 |
| 12(4) ชื่อเจ้าหน้าที่ควบคุมการกำจัด | `…00014` | 14 | 1 |
| 12(5) รูปถ่ายเศษวัตถุ ฯ | `…00015` | 15 | 1 |
| 12(6) ขั้นตอน วิธีการ กระบวนการกำจัด ฯ | `…00016` | 16 | 1 |
| 12(7) สำเนาหนังสืออนุญาตขนย้าย ฯ (แบบ อ.10) | `…00017` | 17 | 1 |
| 12(8) กรณีอยู่ภายใต้บังคับกฎหมายอื่น | `…00018` | 18 | 1 |
| 12(9) กรณีต้องใช้ผู้เชี่ยวชาญ/สถานที่เฉพาะ | `…00019` | 19 | 1 |
| 12(1) วัน เดือน ปี ที่จะกำจัด · 12(2) ตัวอย่างลายมือชื่อ · 13 เอกสารอื่น ๆ | *(no master row)* | — | — |

### Your root-cause is only half right — please re-check before Jason changes anything
You wrote that the base reuses **ReqMove's suffix set** and that this is the fault. Compare the two
groups: **for form lines 1–11 the suffixes are IDENTICAL** —
`00101 / 00602 / 00803 / 12204 / 12305 / 00006 / 00407 / 10008 / 00009 / 00010 / 12111 / 00012` in
both `ReqMove` and `ReqMoveDestroyer`. They only diverge from `00013` onward, where transport has
ส.ค.4 / ขอซื้อ / คณะกรรมการ / buyer docs and destroy has its own 13–19 set.

So a shared 1–11 suffix set **cannot** explain the two mismatches I measured in that range:
- **บัตรผู้เสียภาษี `00407`** has file 39897 and did **not** tick.
- **แผนผังโรงงาน `12111`** has file 39905 and did **not** tick.
- **อ.2 `12204`** has **no doc row at all** and **did** tick.

Those three are inside the identical-suffix range, so something else is wrong there. Two candidates
worth checking first: (a) the item-5 sub-lines may be bound to the wrong suffixes (the destroy form's
3rd sub-line is **อ.7**, but the master's 3rd row in that block is **สลักหลัง `12305`** — the form and
the master do not have the same four entries); (b) the code→ID lookup may be resolving against the
wrong `GROUP_CODE` prefix, so a suffix hits another group's row.

**Please diagnose against the real 38362 evidence in my DEF-15 entry rather than from the code alone**
— that table is ground truth and it disagrees with "suffix set divergence" as the sole cause.

Also, for the record: your review-gap note is fair, but the more useful lesson is the one this run
just demonstrated — **mechanism-verified plus code-list-verified still isn't verified.** Only the
rendered output against the real document table caught it. Keep tick-landing on real data as a
mandatory close step for every form, not an optional QA leg.

## [later] Sober (SA) — DEF-15: my suffix diagnosis was wrong; the builder is exonerated
- Porter is right — lines 1-11 suffixes are IDENTICAL across ReqMove/ReqMoveDestroyer, so "suffix divergence" is
  NOT the cause. I withdraw the DATA REQUEST (Porter already had the dump) and the "base can't reuse ReqMove
  suffixes" claim.
- **Read the committed code (d7b1c36) end-to-end and hand-traced it against your 38362 table.** The destroy builder
  is genuinely code-bound and CORRECT: group=`ReqMoveDestroyer`, `findByGroupCode` (all rows, no IS_ACTIVE),
  `idFor=idByCode.get(group+suffix)`, `checked=checkedIds.contains(id)`, `hasFile=ATTACH≠null&≠0`, doc query
  `STATUS IS NULL OR <>'D'` (status C ticks). Hand-trace → the CORRECT 7 ticks (00101/00602/00803/00407/10008/
  12111/00017), correctly EXCLUDING 12204(no doc)/00006(file0)/00013(file0). That's your "rule says" column, not
  the render. **⇒ the wrong 15:18 PDF did not come from this code path.**
- Timing: service up 15:15:45, fix .class built 15:15:44, bad PDF 15:18 — a 1s compile→fork gap ⇒ likely an
  **incremental/partial build or devtools hot-swap that didn't load the committed destroy builder**. a6/38272 can't
  detect this (renders identically old-vs-new).
- @Porter/@Tanya — **decisive test (your leg, rule #4): `./mvnw -o clean package -DskipTests` + FULL restart (not
  devtools reload), re-render 38362.** PASS (ticks on บัตร/แผนผัง/อ.10-12(7); อ.2/อ.7/สถานที่กำจัด unticked) ⇒ stale
  artifact, close DEF-15. STILL FAIL ⇒ builder exonerated → it's the destroy .jasper evidence-line alignment;
  re-open to me and I'll spec the template fix. See TASK-030.
- Adopted your lesson: real-data tick-landing on a CLEAN-built artifact is a mandatory close step per form.

## [later] Porter (PM) — DEF-15: stale-artifact theory FALSIFIED. Clean build, same wrong output.

Human ran the decisive test. **It still fails**, and the timestamps rule out a stale artifact:

```
17:15:20  A9DestroyReportBuilder.class   rebuilt
17:15:26  SpringBoot-Service-Report-0.0.1-SNAPSHOT.jar   packaged
17:17:36  a9-38362 (1).pdf   rendered      ← 2m10s AFTER the jar
```
Also telling: the new PDF is **197,690 bytes — byte-for-byte the same size as the 15:18 one.** Same
output from a demonstrably fresh build.

Rendered ticks, unchanged: จดทะเบียน · มอบอำนาจ · ร.ง.4 · **อ.2** · **อ.7** · ภ.พ.20 ·
**สถานที่กำจัด 12(3)**.
Still missing: **บัตรผู้เสียภาษี `00407`** (file 39897), **แผนผังโรงงาน `12111`** (file 39905),
**อ.10 12(7) `00017`** (file 40339).

@Sober — **taking your own second branch: builder exonerated, re-opening to you.** Your hand-trace
produced the correct 7 ticks from the same inputs, and a clean-built artifact still renders the wrong
7, so the divergence is downstream of the builder: **the evidence list and the destroy template's
printed lines are not in the same order**, i.e. checkbox N is being paired with label M.

Two concrete things the evidence points at, offered as leads rather than conclusions:

1. **The destroy form's item-5 block has four printed sub-lines — ร.ง.4 / อ.2 (ฉบับต่ออายุ) / อ.7 /
   เปิดดำเนินการ — but `ReqMoveDestroyer`'s corresponding rows are ร.ง.4 `00803` / อ.2 `12204` /
   สลักหลัง `12305` / อ.7 `00006`.** The form and the master do **not** contain the same four
   entries, so any position-for-position pairing across that block desynchronises everything after it.
   Note the observed symptom fits: two ticks land inside item 5 where no documents exist, and exactly
   the lines after it lose theirs.
2. **`สลักหลัง (แบบ อ.2)` has a master row but no printed line**, and **`เปิดดำเนินการผลิตอาวุธ` has a
   printed line but no master row.** Whatever mechanism pairs them must handle both cases explicitly
   — a list that silently skips one and not the other is exactly how a one-off shift appears.

Please confirm whether the destroy evidence list is built with one entry per **printed line** (each
carrying its own code, blank code allowed) or one entry per **master row**. If it is the latter, that
is the defect, and it explains every observation without needing anything else to be wrong.

**Do not re-test on 38362 until you can hand-trace the pairing, not just the tick computation.** The
tick values were already correct; it is the placement that is wrong.

## [later] Sober (SA) — DEF-15: static pairing traced & CLEAN → going to the emitted JSON (TASK-031)
- @Porter — did the static pairing trace you asked for. Answer to your question: the destroy evidence list is built
  **one entry per PRINTED LINE, each carrying its own code** (`idFor(code)`), NOT one per master row. So `สลักหลัง 12305`
  (master row, no line) is never referenced and `เปิดดำเนินการ` (line, no master row) is hardcoded false — there is no
  position-for-position pairing to desync. Your #1/#2 don't match the code.
- Full static chain verified CLEAN: `createData→createDataRaw→buildEvidences` (only source, per-line code-based) →
  `request-a9-evidence.jrxml` (main, `$F{checked}` per item) + `request-a9-evidenceSub.jrxml` (doc/employer subs,
  `Boolean.TRUE.equals($F{checked})`). Hand-trace vs your 38362 dump = the CORRECT 7 ticks. But your clean-built
  17:17 render (byte-identical to 15:18) shows the WRONG 7. ⇒ **the divergence is in the RUNTIME JSON, not the pairing.**
  Record is `@JsonInclude(NON_NULL)` — one suspect; a data-linkage where the real CHECKLIST_CODE / doc REQUEST_CHECKLIST_ID
  differ from the dump is another. Reading further can't decide it — I need the actual emitted booleans.
- @Jason — **TASK-031 (tiny, temp):** log the exact JSON handed to `JsonDataSource` in `exportPdfA9Destroy` (`log.debug`).
  @Tanya — download 38362 (A9D) once, paste the `evidences` array (each line's orderNo/title/checked + item-5/item-12
  subItems checked) back here. Then it's mine to diagnose + real-fix. Not a blind re-render — capturing the intermediate JSON.

## [later] Jason (BE) — TASK-031: DEF-15 diagnostic instrumentation landed

Sober's TASK-031 asked for the EXACT `json` string fed to `JsonDataSource` in the A9 destroy export path (not
a re-serialization) — landed exactly that, tiny + temp:
- `JasperPdfReportService.exportPdfA9(data, folder)` (shared private method): added
  `if ("request-a9-destroy".equals(folder)) log.debug("A9D-JSON {}", json);` using the SAME `json` var that
  feeds `JsonDataSource` a few lines below (the pre-existing `System.out.println(json)` for all variants is
  untouched). Tagged `// TASK-031 TEMP DEBUG — remove after DEF-15`.
- DEBUG isn't visible under Spring Boot's default INFO root level, so added a temp `logging.level` block to the
  repo-root `application.yml` scoping DEBUG to just `JasperPdfReportService` — same removal tag.
- No other file touched; log-only, zero behavior change.

**Verify (DB-free):** test-compile SUCCESS; A9PreviewTest green (destroy 4pg/transport 5pg — preview bypasses
this service so it only proves no regression, can't exercise the new line); clean compile + boot :33101 clean
(the yml key parses). **Emitting on a real A9D download = QA's leg** (BE rule #4, never touch the real DB/seam).

→ TASK-031 REVIEW. @Sober/@Tanya: download 38362 (A9D), grep `A9D-JSON`, paste the `evidences` array back —
that's the ground truth Sober needs to tell code-bug from template-fill-bug. Both edits marked TEMP for easy revert.

## [later] Sober (SA) — TASK-031 REVIEW: DONE (correct instrumentation) → QA please capture
- Verified: `JasperPdfReportService` line 166 `log.debug("A9D-JSON {}", json)` uses the SAME `json` (line 160 =
  `objectMapper.writeValueAsString(data)`) that feeds `JsonDataSource` at line 174, gated to `request-a9-destroy`;
  `application.yml` scopes DEBUG to `...JasperPdfReportService` only. Log-only, zero behavior change, both marked
  `// TASK-031 TEMP DEBUG — remove after DEF-15`. ✅ Good to run.
- @Tanya — **your leg (rule #4):** download **38362** (A9D) once, `grep A9D-JSON` the app log, and paste back the
  `evidences` array — specifically, for each item: `orderNo` + `checked`, and for item 5 & item 12 the
  `subItems[].label` + `checked`. (Jason notes a pre-existing `System.out.println(json)` also dumps it to stdout if
  the DEBUG log is inconvenient.) That's the ground truth I need.
- Then it's mine: **correct booleans in the JSON ⇒ Jasper/template fill bug; wrong booleans ⇒ builder/data-linkage**
  (I'll trace `idFor` vs the real master ID/CODE + docs' REQUEST_CHECKLIST_ID). Real fix follows, then revert TASK-031.
