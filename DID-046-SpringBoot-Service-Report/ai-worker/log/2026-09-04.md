# Log — 2026-09-04

## Sober (SA) — studied bfc4b76; split-rule refined; DEF-25 → TASK-051; person-row re-measure folded in
Porter relayed 2 stakeholder items:
- **bfc4b76 hand-fixed อ.9-destroy with GEOMETRY ONLY (0 new bands, still 11)** — width-reclaim ×4 (captions shrunk to
  their text, value fields gained the slack + shifted left). **Refinement to the split rule:** split for SHAPE
  differences; right-shape-but-cramped = geometry (reclaim caption slack), NOT a band. Recorded → SPEC-039 + memory
  [[subevidence-split-rows-not-share]]. This also answers routed Q3: the person-row "inline impossible (x633 vs 551)"
  assumed caption widths were fixed — they're not.
- **DEF-25** (stakeholder-directed): a4 item 17 `เอกสารอื่น ๆ (ถ้ามี)` is one concatenated string with literal dots
  (A4CheckListReportBuilder:203-204) → value glues to label, dotted line vanishes when populated. REQ-033 class.
  → **TASK-051 (Jason):** split into own band (label + real dotted write-in field), + fold in the person-row
  caption-reclaim re-measure (อ.14 item-6, destroy item-12(2)) — geometry if it fits, else back to me with numbers.
- **a4 status → Porter:** clean except DEF-25; fix that, then a4 is ready for the stakeholder test.
**Ball → Jason (TASK-051).**

## Sober (SA) — DEF-25 a4 verified; ruled 5-form scope (a14 now / a9-family → stakeholder); person row = keep 2-line
a4 item-17 fix SA-verified (builder split, gated dotted element, 27/27 fail-on-revert) → a4 ready for stakeholder test.
DEF-25 confirmed 5-form (a14:189 + A9 base:234 = transport/a15/destroy). Ruling: **a14 → TASK-052 (independent, now)**;
**a9-family coupled to frozen transport** (base-builder change alters transport output → its frozen jrxml needs the
element) → **stakeholder ruling via Porter**, don't touch transport. Person-with-idcard re-measure (Jason, caption-reclaim):
inline ~7px short on SAFE reclaim, fits only by eating data-dependent name slack → clip risk. **Recommend keep 2-line**
(routed to stakeholder w/ numbers). **Ball → Jason (TASK-052) + Porter (a9-family ruling + person cosmetic).**

## Sober (SA) — TASK-052 (DEF-25 a14) SA-verified CLOSED
a14 item-13 fix verified: builder split (0 literal dots), gated element present (evidence.jrxml:59, same as a4),
structure_check 28/28 (a4+a14 loop, fail-on-revert), transport frozen. **a14 DEF-25 = code CLOSED → QA.**
(My first grep missed it — pattern didn't match `$F{}`; re-checked, element is there. Jason's work correct.)
DEF-25 status: a4 ✅ + a14 ✅ (code); a9-family (transport/a15/destroy) still on stakeholder ruling (Porter).
**Ball → Porter** (a14 + a4 QA /download; a9-family DEF-25 ruling; person 2-line; REQ-034 2 open Qs). Jason idle.

## Sober (SA) 2026-09-07 — DEF-25 all-forms ruled → TASK-053; a6 verdict; dropped person-wrap Q3 (owned)
Porter relayed: อ.4 ACCEPTED; **DEF-25 ruling = fix EVERY "เอกสารอื่นๆ" form, a9-transport freeze lifted for that one element.**
- **TASK-053 (Jason):** a9-transport/a15/destroy item 13 via `Base:234`; frozen_check proves only the new element differs
  on transport; both branches (empty+populated) on real /download per form.
- **a6 item 8 verdict (checked):** real a6 builder correct (`A6…ReportBuilder:199-201` Type B, no concat); only a6 MOCK
  has literal dots — cosmetic, not a /download defect. Not a DEF-25 form.
- **Q3 destroy person row — I was wrong on process, Porter wrong on the code:** destroy DOES call buildPerson2
  (`A9DestroyReportBuilder:71`) → row exists; 38362 just has no example-sign data (empty). BUT I measured a "wrap" on the
  MOCK, never a real destroy render → per Porter's new rule (a stakeholder question must point at a render), **dropped the
  person-wrap question** (destroy + a14). Owned. Rule saved to memory.
- **Q2 a14 ตามหนังสือขอซื้อ:** gave Porter the pointer (official A14 p2 item 12; real a14 sample 27300); offered to drop it too.
**Ball → Jason (TASK-053) + Porter (Q2 pointer, decide or drop).**

## Sober (SA) 2026-09-07 — TASK-053 accepted: DEF-25 code-complete on all 5 forms
Verified: base builder 0 literal dots; transport evidence.jrxml +7/-0; transport evidenceSub byte-identical; element in
all 3 forms; structure_check 34/34 fail-on-revert; a6 mock aligned. **frozen_check.sh exception reviewed + endorsed** —
tight (additions-only, element-whitelisted, fails on anything else, refrow4/5 re-asserted); Jason encoded the sanctioned
exception vs skipping. DEF-25 = code CLOSED a4/a14/transport/a15/destroy. → QA real /download both branches per form.
**Ball → Porter (QA DEF-25 renders).** REQ-034 last open = Q2 (a14 ตามหนังสือขอซื้อ, Porter to point or drop).

## Sober (SA) 2026-09-07 — delivered sample-finder SQL (stakeholder runs it); DEF-21 render confirmed
Stakeholder asked me to WRITE the SQL that finds populated samples (I write, they run — rule #4 intact; the better move).
→ `ai-worker/DATA-QUERY-2026-09-07-samples.md`: Q1 finds DEF-25 item-13/17 `เอกสารอื่นๆ` requests (a14 type 6 + a9 family
3/5) from T_T_REQUEST_DOC (DOCUMENT_ID=0/TYPE=99/CHECKLIST_ID=0/has-file/active) — all conditions source-derived
(`Base:225-231`, hasFile `A14:391`); Q2 finds a15 ReqSaleDom 00014/00020 tick+file via T_S_REQUEST_CHECKLIST (TICK RULE).
Oracle 11.2, no FETCH FIRST, `(STATUS IS NULL OR <>'D')`; each column's meaning stated. If a family returns nothing =
accepted gap with evidence, not padded. Logged Tanya's TEST-012: DEF-25 empty branch PASS 5/5, populated PASS a4 only
(38427); DEF-21 refrow4/refrow5 render 9/9 อ.15 (200/0 ORA), all unticked/null = tick branch accepted gap.
**Ball → Porter → stakeholder (run the queries).** Jason idle.

## Sober (SA) 2026-09-07 — REQ-036 delivered: SPEC-041 อ.4 correctness/field-mapping
Walked A4CheckListReportBuilder end-to-end (not a render); every section → Rule/Source/table.COLUMN/Code(A4:line)/
Verify SQL/Status. All columns confirmed from entities (no guesses, no UNKNOWN); a4 code untouched (REQ-036 rule).
Oracle 11.2, (STATUS IS NULL OR <>'D'), default 38427, one line per query stating what it proves. Gaps IN-table: item6
อ.8 + annex 3-col = T_T_REQUEST_DTL_REF_IMPORT EMPTY (+ the query to prove it later); annex join key unconfirmed on live;
item17 populated=PASS 38427. Flagged the deliberate 1,3,2,4 signature order. **Ball → Porter → stakeholder (run SPEC-041).**
