# TASK-012: อ.9 DESTROY finish — DEF-4a + DEF-5 + item12(1) + ReqMoveDestroyer + item12(2) label

- Source: SPEC-020 (REQ-020). **Supersedes TASK-010** (this covers it + more).
- Status: REVIEW
- Depends on: none (no mock, no data team, no human — all verifiable in the A9PreviewTest loop; ticks/
  item-12(1) value confirmed later by QA on a real render)

Repo: `C:\Users\Admin\sa-project\service-report2\DID-046-SpringBoot-Service-Report`.
**Verify via `./mvnw -o -Dtest=A9PreviewTest -DfailIfNoTests=false test` → `target/a9-preview.pdf`** and
`./mvnw -o -DskipTests=false test-compile`. Do NOT hit the DB. Do NOT regress a6 or the a9 mock preview.

1. **DEF-4a — item 1 (ชื่อผู้ขอ) & item 5 (สถานที่กำจัด/ทำลาย) blank** (`request-a9-main.jrxml`).
   Reproduces in the mock preview (mock name "บริษัท ตัวอย่าง จำกัด" + destroyLocation don't print on p.1).
   Field decls + JSON expressions are CORRECT — the `$F{applicantName}` (x=17, same as its label) and
   `$F{destroyLocation}` (x=148, inside label x=17–197) textFields **overlap their static labels**. Move/
   resize them clear of the labels (mirror item 2's value field placed after the label). **DoD: the mock
   name + destroyLocation now appear on page 1 of the preview PDF.**
2. **DEF-5 — literal "null"**: page-2 item 2 prints "ลงวันที่ null". Null date/text → blank (or dotted),
   never the string "null". Sweep the other evidence date/text inlines. Fix at source (builder passes ""
   or subreport guards `$F{x}==null?"":$F{x}`). **DoD: no "null" text anywhere in the preview PDF.**
3. **item 12(1) date**: in `buildEvidences`, wire the "(1) วัน เดือน ปี ที่จะทำการกำจัดหรือทำลาย" value =
   `T_T_REQUEST_MOVE.WRITE_OFF_DESTROY_DATE` (Thai date via `ThaiDateFormatUtil`), **blank when null** (no mock).
4. **`ReqMoveDestroyer`**: rename `A9CheckListReportBuilder.CHECKLIST_GROUP` from `"ReqMove"` →
   `"ReqMoveDestroyer"` (+ update the comments referencing 'ReqMove').
5. **item 12(2) label**: in `buildPerson2`, show the `EXAMPLE_SIGN_TYPE` doc-type label per receiver —
   `1`→สำเนาบัตรประจำตัวประชาชน · `2`→สำเนาบัตรประจำตัวข้าราชการกลาโหม · `3`→สำเนาบัตรประจำตัวเจ้าหน้าที่ของรัฐ ·
   null/other→"" . (Keep the existing STATUS<>'D' + attachment tick.)
6. **DEF-6 headings — NO CHANGE** (verified: documentTitle "…ขนย้ายอาวุธ" = official p.1; page-2 static
   "…ขายและขนย้ายอาวุธ" = official p.2). Do not touch.

## Definition of Done
- [ ] Preview PDF: page-1 item 1 shows the mock name; item 5 shows the mock destroyLocation; item 12(2)
      shows the EXAMPLE_SIGN_TYPE label; **no "null" anywhere**.
- [ ] item 12(1) wired to `WRITE_OFF_DESTROY_DATE` (blank when null); constant = `ReqMoveDestroyer`.
- [ ] `./mvnw -o -DskipTests=false test-compile` green; A9PreviewTest green; a6 + a9 mock preview unaffected.
- [ ] Re-hand: SA reviews the preview PDF; QA later confirms item-12(1) value + ticks on a real destroy render.

## Implementation Notes
**Changed:** `request-a9-main.jrxml` (DEF-4a) + `A9CheckListReportBuilder.java` (item12(1), rename);
`.jasper` regenerated by A9PreviewTest. A9PreviewTest.java reverted to clean (debug print removed).

**1. DEF-4a (item 1 name + item 5 destroyLocation) — ROOT CAUSE was NOT overlap.** I verified the
emitted JSON (temp print, since reverted): applicant.name / itemCount / destroyLocation are all
**populated correctly**. Yet they rendered blank while permitType/objective (same `applicant.X` expr
pattern) rendered. The real cause: those textFields had **height 14–16** with `fontSize=14` +
`vTextAlign="Bottom"` → the glyph is clipped out of the too-short box (permitType works at h=18). Fix =
raise heights to 18/18/17 (applicantName/itemCount/destroyLocation) — plus I still moved the value
fields clear of their labels (applicantName x=17→118 Left, destroyLocation x=148→200) as the SPEC asked.
**Proof (PyMuPDF on the preview PDF, mark-safe counts/search):** `'ตัวอย่าง'` count 2→**4**;
`search_for('บริษัท ตัวอย่าง จำกัด')`→2; `search_for('สถานที่ทำการกำจัดหรือทำลาย')`→2 (label+value);
itemCount "3" now present. All three page-1 values now render. (Note for SA: the same height-clip likely
lurks in any other h≤16 fontSize-14 value field.)
**2. DEF-5 (no "null"):** full-text of the preview PDF contains **no literal "null"**. Swept all a9
templates for unguarded `+` concatenations — every one is already null-guarded (`==null?"":...`), and
builder date fields use `nz(...)`/blank-on-null. Nothing to change; verified clean.
**3. item 12(1) date:** wired in `buildEvidences` — the "(1) วัน เดือน ปี ที่จะทำการกำจัดหรือทำลาย"
label now appends `T_T_REQUEST_MOVE.WRITE_OFF_DESTROY_DATE` (Thai date), blank when null (no mock). DB
path → QA confirms the value on a real render.
**4. ReqMoveDestroyer:** `CHECKLIST_GROUP` renamed `"ReqMove"`→`"ReqMoveDestroyer"` (+ comments).
**6. DEF-6 headings:** not touched.

**Verify:** `./mvnw -o -DskipTests=false test-compile` → BUILD SUCCESS; `A9PreviewTest`+`A6PreviewTest`
→ Tests run: 2, Failures: 0 (a9 pages=4, a6 pages=3 — no a6 regression, a9 mock preview intact).

## Questions
- **Q1 (part 5 — item 12(2) EXAMPLE_SIGN_TYPE label): needs a design decision, not done yet.** In
  `request-a9-evidenceSub.jrxml` the person2 doc-type is a **hardcoded `staticText` "สำเนาบัตรประจำตัว
  ประชาชน"** (x=239). Varying it per receiver (1→บัตรประชาชน · 2→บัตรกลาโหม · 3→เจ้าหน้าที่ของรัฐ ·
  null→"") from `buildPerson2` needs either (a) a **new nullable field on the a9 `EvidenceSub` record**
  (churns every `new EvidenceSub(...)` call in both the mock + DB builders — ~15 sites) bound to a new
  `textField` replacing that static, or (b) repurposing an existing person2-unused field. I didn't want
  to guess a model-wide change. **Which do you prefer** — add `docTypeLabel` to the record, or another
  binding? I'll wire it + the builder switch once you confirm. (Everything else in TASK-012 is done +
  verified above.)

## Questions
(Jason asks; Sober answers as `> answer: ...`)
> **answer to Q1 (item 12(2) doc-type label) — option (a), but zero-churn via a delegating constructor:**
> Add `docTypeLabel` (String) as the **new last component** of the a9 `EvidenceSub` record, AND add an
> explicit 9-arg convenience constructor with the OLD signature that delegates with `docTypeLabel=null`:
> ```java
> public EvidenceSub(String type, String seq, String label, Boolean houseReg, Boolean idCard,
>                    Boolean checked, String detail, String detail2, String note) {
>     this(type, seq, label, houseReg, idCard, checked, detail, detail2, note, null);
> }
> ```
> → the ~15 existing `new EvidenceSub(...)` / helper sites (mock + DB) **do not change** (they match the
> 9-arg ctor); only `buildPerson2` uses the full 10-arg form. In `request-a9-evidenceSub.jrxml`, replace
> the hardcoded `staticText "สำเนาบัตรประจำตัวประชาชน"` (x=239) with a `$F{docTypeLabel}` textField
> (declare the field with json expression `docTypeLabel`, `@JsonInclude(NON_NULL)` already on the record).
> In `buildPerson2`, map `EXAMPLE_SIGN_TYPE`: `1`→"สำเนาบัตรประจำตัวประชาชน" · `2`→"สำเนาบัตรประจำตัวข้าราชการกลาโหม"
> · `3`→"สำเนาบัตรประจำตัวเจ้าหน้าที่ของรัฐ" · null/other→"" (blank, no default — no-mock rule). Verify in the
> A9PreviewTest preview PDF (give the mock person2 a docTypeLabel so it shows). This is the only remaining item.

## Review (round 1) — 4/5 done + verified; item 12(2) pending my Q1 answer
**Accepted (SA-verified):**
- **DEF-4a** — I re-ran A9PreviewTest + PyMuPDF: the mock **name renders** ("บริษัท ตัวอย่าง จำกัด" present),
  destroyLocation label+value both present (count 2), itemCount shows. Your **height-clip root cause is
  correct** — my "overlap" hypothesis was wrong; good catch (short box h14–16 + fontSize14 + Bottom clips
  the glyph). ✅ (Noted your heads-up: other h≤16 fontSize-14 value fields may share it — I'll watch for it.)
- **DEF-5** — preview PDF has **no literal "null"**; you swept the concats. ✅
- **item 12(1)** = WRITE_OFF_DESTROY_DATE (blank when null); **ReqMoveDestroyer** rename; **DEF-6** untouched. ✅
- test-compile + A9PreviewTest (pages=4) + a6 (pages=3) green — no regression. ✅

**REWORK:** just wire **item 12(2)** per the Q1 answer above, then re-hand. Verdict on the rest = good.

## Implementation Notes (round 2 — part 5 done per your Q1 answer, Jason)
Implemented item 12(2) EXAMPLE_SIGN_TYPE label exactly as you specced (option a, delegating ctor):
- `A9CheckListReportData.EvidenceSub` — added `docTypeLabel` as the new **10th** component + an explicit
  **9-arg delegating constructor** (`docTypeLabel=null`). All ~15 existing `new EvidenceSub(...)` sites
  (mock + DB helpers) are untouched — they bind the 9-arg ctor; only person2 uses the 10-arg form.
- `request-a9-evidenceSub.jrxml` — declared `<field docTypeLabel>` (json expr `docTypeLabel`) and
  replaced the hardcoded person2 `staticText "สำเนาบัตรประจำตัวประชาชน"` (x=239) with a
  `$F{docTypeLabel}` textField (same geometry; Top-aligned → no height-clip).
- `A9CheckListReportBuilder.buildPerson2` — passes `exampleSignTypeLabel(e.getExampleSignType())`:
  1→"สำเนาบัตรประจำตัวประชาชน" · 2→"สำเนาบัตรประจำตัวข้าราชการกลาโหม" · 3→"สำเนาบัตรประจำตัวเจ้าหน้าที่ของรัฐ"
  · null/other→"" (blank, no default).
- Mock `A9CheckListPreviewBuilder.person2` now passes `docTypeLabel="สำเนาบัตรประจำตัวประชาชน"` so the
  preview demonstrates it.

**Verify:** `./mvnw -o -DskipTests=false test-compile` → BUILD SUCCESS (delegating ctor → no call-site
churn); `A9PreviewTest`+`A6PreviewTest` → Tests run: 2, Failures: 0 (a9 pages=4, a6 pages=3, no regression);
PyMuPDF on the preview PDF: `search_for('สำเนาบัตรประจำตัวประชาชน')` > 0 (item-12(2) label renders),
no literal "null", name still renders. **All 6 TASK-012 parts now done + verified.**
@Sober: TASK-012 fully ready for review; QA later confirms item-12(1) value + ticks + the per-receiver
doc-type label on a real destroy render.

## Review (round 2) — item 12(2) DONE → TASK-012 COMPLETE (Sober, 2026-08-05)
Verified independently:
- `EvidenceSub` gained `docTypeLabel` (last component) + the **9-arg delegating convenience constructor**
  (delegates docTypeLabel=null) — exactly the zero-churn recipe; existing ~15 call sites unchanged. ✅
- `buildPerson2` → `exampleSignTypeLabel(EXAMPLE_SIGN_TYPE)` switch: 1/2/3 → the three doc-type labels,
  null/other → "" (no default, no-mock). ✅
- `request-a9-evidenceSub.jrxml`: person2 static replaced by `$F{docTypeLabel}` (field declared); the
  line-62 static "สำเนาบัตรประจำตัวประชาชน" is the *person*-type (items 3/4) label, correctly untouched. ✅
- Re-ran A9PreviewTest (pages=4) + PyMuPDF: doc-type label renders, still no literal "null"; full
  `test-compile` → exit 0 (delegating ctor compiles across mock + DB). a6 not touched (scope a9-only). ✅
**Verdict: DONE (code leg).** All 5 SPEC-020 items complete + preview-verified. REQ-020 → SPEC_DONE.
Remaining for destroy DELIVERED = QA on a real **MOVE_REQUEST_TYPE=2** sample (item-12(1) value + ticks)
+ data-team seed of `ReqMoveDestroyer` — both human-side, not code.
