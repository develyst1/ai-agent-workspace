# SPEC-090 — `REQ-104` gap-check: GROUP parity · CAMP per-day coach rate · CAMP check-in credit + parent notice · CAMP redeem picker — DONE vs GAP + sizes (Sober, 2026-09-22). No build; the next slice, not the `sid` batch.

| # | Customer's line | DONE today | GAP | Size |
|---|---|---|---|---|
| 1 | GROUP: confirm whole course · weekly confirm to coach · standing schedule | see §1 | a GROUP manage surface = the OTHER series modal keyed on `group_key`; a weekly coach digest does not exist | BE S–M · FE S (+ weekly digest BE S) |
| 2 | CAMP coach rate per DAY | nothing — no camp rate anywhere | `camp_week_days` per-teacher rate per date, behind key 59 | BE S–M (`0052`) · FE S |
| 3 | CAMP check-in shows remaining credit + tells the parent like Private | the scan consumes the units; no credit shown; no parent message (Private's "tell mum" is the DAY-END deduction, not the scan) | the scan page reads the credit; a camp deduction notice at day-end (the `course_deduction` shape) | BE S · FE S |
| 4 | CAMP redeem lists only kids WITH credit | ✅ DONE — `RedeemDialog` filters `credit > 0` (FE); the server refuses a redeem past the credit | — (server-side filter optional, S) | 0 |

## §1 GROUP parity (the read)
- A GROUP series (TASK-397/399) = one GROUP row per date (`group_key`, `head_count`, the coach + extras + rates) born **CONFIRMED** (it is the shop's standing slot — no per-row confirm needed), and each child's COURSE seat rows (`group_id`) drawn from that child's own course package. So:
  1. **"Confirm whole course"** — per CHILD it exists: `POST /courses/:id/confirm` (`course_confirmed`, one message per course) works on a course sold into a group exactly as on a Private. The GROUP row itself has nothing to confirm. ✅ (if the customer means "one button that confirms every child's course in the group": that is a loop over the seats' courses — BE S, FE S — say if she means that.)
  2. **"Confirm weekly for coach"** — the coach gets the DAILY reminder (08:15) naming the day's rows, and `booking_confirmed` per row when a child's course is confirmed. There is **no weekly coach message** for anything (Private included). GAP if wanted: a Monday 08:15 `weekly_schedule_teacher` digest (the daily reminder's builder over 7 days) — BE S, new bytes (📖 owner), no FE.
  3. **"Standing schedule"** — ✅ the GROUP series is the standing slot (weekly rows by `group_key`); `PATCH /bookings/:id/group-teacher` swaps the coach from a date on (TASK-397). What is MISSING vs the OTHER series (REQ-101): a manage surface — **Confirm-all n/a · Cancel-all (the group rows AND every seat on them ⇒ CANCELLED, each family told — the seat cancel's notice, one per family; the coach once) · Add/Remove extra teacher from a date · Add dates · Edit header (name/cap/rates)**. Design: the OTHER series service generalised to a `seriesKeyOf(row)` = `other_series_key ?? group_key`, the same modal with a GROUP face (seats shown per row). BE S–M (the seat cascade is the new part) · FE S (the modal already exists; a GROUP entry point + the seats list).
- Owner decisions: (a) is "confirm whole course" per child (exists) or one button for the group; (b) the weekly coach digest — wanted, and its words; (c) cancel-all on a group cancels every child's seats and tells the families — confirm.

## §2 CAMP coach rate per day (the read)
- `camp_week_days { date, teacher_ids[], window }` (TASK-418) carries **no rate**; the derived `OTHER/CAMP` rows carry **no `teacherRates`** (pinned by absence in the camp-on-grid tests); `camp_weeks` has no rate either. So today a camp coach's pay is **not set anywhere** — neither per week nor per day. GAP, entirely.
- Design: `camp_week_day_rates (camp_week_day_id, teacher_id, rate_minor)` (or a jsonb on `camp_week_days`; I prefer the table — one row per coach per day, the `booking_teachers.rate_minor` shape) written by `PATCH /camp/weeks/:id/days/:date { teacherRates }` (the existing per-day route, `camp.week-open` + **key 59** for the rate field — the coach-rate rule, see ⇔ edit), read on the day roster; `syncCampDayRows` copies the rate onto the derived rows' `teacher_rate_minor` / `booking_teachers.rate_minor` so the calendar's OTHER DTO shows it under the same mask. Default for a new day = the previous day's rate for that coach (or blank). Nothing posts (stored only, the §13.3 rule). BE S–M (`0052`, the route field, the sync copy, the mask by construction) · FE S (a rate box per coach in the day editor, behind 59).
- Owner decisions: (a) default rule for a new day (copy the previous day vs blank); (b) a week-level default with per-day override (the §13.3 course/session shape) or per-day only — she said per day; I recommend per-day only, simplest.

## §3 CAMP check-in: credit + the parent (the read)
- **What Private does:** the QR scan (`POST /checkin { token }`) marks ATTENDED and awards CRM points; **the scan page shows no remaining and sends nothing to the family**. The family's message is the **day-end `course_deduction`** (17:30, REQ-087: `Remaining 14/15`) — that is the "แจ้งแม่".
- **What Camp does:** the scan (`POST /checkin/camp { token }`) marks the day ATTENDED through `markDay` (units consumed); the response carries the day only; **no credit shown; no family message at all** (the camp reminder is the morning one; no camp deduction exists — the day-end job cuts camp days but sends nothing).
- GAPs: (a) the camp scan response + page show `credit` (`creditOf(pkg)` after the mark — half-day units ⇒ "เหลือ 3.5 วัน"): BE S (one field), FE S (one line on the check-in page, both kinds — Private can show `Remaining n/N` the same way for free); (b) a **`camp_deduction`** family notice at the day-end cut (the `course_deduction` shape: `CAMP DEDUCTION · <child> · <date> · <half/full> · Remaining x/y days` — new bytes 📖, the owner's words) — BE S–M (the kind, the payload, the day-end enqueue per attended camp day, the family accounts through `householdLineUserIds`), no FE. Optional: (c) tell the family AT the scan instead of day-end — not how Private works; recommend day-end for parity.
- Owner decisions: (a) day-end deduction (parity) vs at-scan; (b) the notice words.

## §4 CAMP redeem picker — ✅ DONE
`RedeemDialog` lists `packages.filter(p => p.credit > 0)` (FE) and the server refuses a redeem beyond the credit (`markDay`/`addDays` units check). Nothing to build. (If the customer means the KID picker on Sell/Redeem should hide kids without a package at all — that is the same filter one level up, FE S.)

## §5 Sizes for the next slice (if all taken)
BE: §1.3 group manage S–M + §1.2 weekly digest S + §2 camp day rate S–M (`0052`) + §3a credit S + §3b camp deduction S–M ⇒ **BE M+** · FE: §1 S + §2 S + §3 S ⇒ **FE M**. Two new notice kinds with owner words (weekly coach digest; camp deduction). One migration.
