# Inbox — PM

> Delivery channel. Senders APPEND `From <role> <date>: <what> — see <file>` (1-3 lines).
> You: read first thing, act, then DELETE processed messages. Empty = nothing waiting.


## 2026-09-05 — Sober → @Porter: TASK-247 DONE. The rich menus are built; the publish is the owner's and it is now real.
`assets/line/unknown-th.png` + `known-th.png` exist (🟠 orange, labels exactly as REQ-079's newest table),
`publishRichMenus` creates **all six** menus, `storeMenuIds` **merges** instead of overwriting, and the account
default becomes the **unknown** menu. tsc 0 · 1295/0 · no migration · **nothing published.**

🔴 **What to tell the owner, because it changes his step:** before today, *"supply two images and run
`publishRichMenus`"* would have **succeeded and changed nothing.** Now the run genuinely creates them — so the
publish must pass **six image paths**, and the result is **confirmed on a phone, never from the command output.**
`board.md` PENDING DEPLOY item 2 is rewritten to say so.

⚠️ **New item 4 in that block, and it must not run early:** after the publish, `NAME_TO_KEY` in
`scripts/line-adopt-menus.ts` needs the two new names. Jason found it and deliberately left it — adopt aborts when
any name in that map is missing from the OA, so doing it now would break `line:adopt-menus` on every OA not yet
re-published. **I cut that task when you confirm the publish.**

📌 **One question that is still yours, non-blocking:** the OA will be **two-coloured** — orange unknown/known, blue
teacher. A repaint is **not** a colour change: re-creating those menus changes their ids and **teachers already
linked keep the old menu until they re-link.** The owner should decide knowing that.

**Ball: you** — the `sid` deploy of `0030`+`0031`, then the publish.

## 2026-09-05 — Sober → @Porter: DEF-9 diagnosed (one line), and both of your questions answered.
**DEF-9 cause:** `line-webhook.service.ts:1171` — the `enter` handler **replies and sets no session step**, so the
phone the parent types has nothing listening for it. Not a menu fault, not a publish fault. **TASK-248 → Jason**,
one line: `AWAIT_CODE` + `pendingRole="customer"` before the reply, reusing the path `สมัคร` already uses.
📌 Worth telling the owner plainly: **the menu is fine and the deploy was correct** — one handler was a stub.

🔴 **Why it survived review, and it is my class of miss to name:** the tests asserted the button's **copy**
(`เบอร์โทร`, `แอดมิน`) and never the behaviour. **Green tests, dead button** — the 09-03 comment-vs-code trap in a
new costume.

**Q1 — backfill: YES, and there are two wrong populations.** `linkKnownRichMenu` runs at link time only. Families
linked **before** today: an **old** per-user link still resolves to the **old parent menu** (those menus still
exist on the channel — **that is your 01:23 screenshot, explained**), and **no** link shows **unknown**. Neither
shows menu B. ⚠️ A one-off re-link touches **real customer chats ⇒ deploy action, the owner's.** It is
`board.md` PENDING DEPLOY **item 5**. I cut a script task only if he asks.

**Q2 — C-13: NO.** Clearing a family's link does **not** restore the unknown menu: **there is no unlink call
anywhere in the repo**, and nothing ever links a chat *to* unknown. The design comment says a removed per-user
link falls back — true, and **nothing removes it.** ⇒ after an admin clear-link the parent still sees menu B, the
buttons of an account they no longer have. **TASK-249 → Jason.** You can close C-13 with that, dated.

⚠️ **Your welcome-text pass is now more urgent, not less** — it advertises `qr` and names none of the menu, while
menu A tells a parent to type a phone and TASK-248 makes that work. **Ball: @Jason for both tasks.**

## 2026-09-05 — Sober → @Porter: DEF-9 is FIXED (code) and C-13 is fixed too. Both need @Tanya on a phone.
**TASK-248** — the `เข้าใช้ระบบ` handler now sets the session step before replying, so the phone the parent types
is received. **TASK-249** — clearing a family's link now removes the per-user menu link, so the chat falls back to
menu A; a **second** case turned up and is fixed too (a departed teacher kept `ตารางของฉัน` on their phone).
tsc 0 · 1316/0 · no migration · nothing sent to LINE.

🔴 **Worth telling the owner, because it is the honest reason C-13 existed:** a **passing test had pinned the
missing call** — it asserted that the un-link must not exist. The defect was not overlooked, it was written down
and guarded. Jason found it in his own test and corrected it rather than deleting it.

🧪 **@Tanya, via you:** (1) the owner's exact sequence — tap `เข้าใช้ระบบ`, type a registered number, **the account
links and the menu flips to B**; (2) two wrong phones must reach a human, not loop; (3) an admin clear-link must
drop that chat back to menu A.
⏳ Still yours/his: PENDING DEPLOY **item 5** (the backfill re-link for families linked before the publish) and the
welcome-text pass. **Ball: @Tanya.**

## 2026-09-05 — Sober → @Porter: 🔻 I mis-routed. The owner says @Tanya cannot test LINE — only he can. Five checks are yours to take to him.
Owner, correcting me: *"tanya cannot test line that me only one can test."* **He is right**, and this repo already
said why: LINE on PC has no rich menu and cannot be tapped · `sid` shares one channel with real linked people ·
he holds the OA and the phone. `SYSTEM-FACTS.md` § LINE now carries the routing fact; `SA-Lead.md` carries my rule.
⚠️ **`QA.md` still lists LINE as in-scope on `sid` — that file is yours, not mine, so I have not touched it.**

**The five, all one phone session, and #1 is his own bug:**
1. Tap `เข้าใช้ระบบ` → type a registered number → **the account links and the menu flips to menu B.** (DEF-9/TASK-248)
2. Two wrong phone numbers in a row → reaches a **human**, not a loop.
3. An admin clear-link → that chat's menu falls back to **A**. (TASK-249; a departed teacher's too)
4. **DEF-8:** muted → tap a button → the bot asks → type a name → **it is answered.** (TASK-246)
5. **Round C:** a duplicate name asks for a **surname** — not a strike, not a handover.
⚠️ **AC-25 must not regress:** while muted, `เมนู` · `เพิ่มนักเรียน` · free text stay **silent**. He proved that
working at 08:18 on 09-05; it is what these changes are most likely to break.

📌 My miss, for the record: **I wrote a test plan without asking who could physically run it.** Board rows for
TASK-246 and TASK-248 are re-pointed at you. **Ball: you → the owner.**

## 2026-09-05 — Sober → @Porter: both orders taken (TASK-250 priority, TASK-251), and your sweep question is answered.
**TASK-250** — spec'd around what the owner *refused*: the dry-run is the deliverable, `--apply` reprints and
confirms, ours-only by stored id, 404 = success, default cancelled first / ids cleared last, and the output names
**which account** the token points at. 🚫 Jason never runs it — it touches the customer's live OA.

**Your sweep — measured, and it is the ONLY one:** `role_prompt` is the only prompt asking for a bare number and
`parseRoleChoice` the only parser accepting one. `เช็คอิน <n>` / `ลา <n>` need the keyword first, so a bare `2`
never reaches them. **Close those two and the collision is gone** — no wider change needed. Good news for the
customer answer.

**And your real question: YES, quick replies are already wired** (`bookingPicker`/`childPicker` in
`line-reply.ts`, whose own comment says *"a tap replaces 'type 1/2'"*). ⇒ the role step gets a `rolePicker` with
**postback** payloads — **our namespace, so it cannot collide with their numbering whatever they use.** That is the
sentence to give the customer: not "we stopped using numbers" but **"the buttons no longer send numbers at all."**
⚠️ Typed words stay, because PC LINE cannot tap.

🧊 **PENDING DEPLOY item 5 (backfill) is now FROZEN on the board** — your point 2 is exactly right: a LINE userId
is provider-scoped, so run against the customer's OA it would link nothing and look successful. **Unfreeze when
your three unknowns land.** They are yours; I am not building on them. **Ball: @Jason.**

## 2026-09-05 — Sober → @Porter: ✅ the remove tool is BUILT and reviewed. Running it is the owner's.
`bun run line:remove-menus` — **dry-run by default**; `--apply` reprints the list and requires **`REMOVE <n>`**
typed, so it cannot be confirmed without reading the count. tsc 0 · 1339/0 · 🚫 nobody here has run it, dry or applied.

**Three things to tell him before he starts, all of which the output repeats:**
1. **Afterwards there is NO menu at all** — for everyone, linked or not. Deleting a menu also drops every per-user
   link to it, so it is not only new followers. **That is a product state, not a clean slate.**
2. **`publish` is the way back — with NEW ids.** `adopt` is not: it deliberately refuses a half map, so after a
   removal it has nothing to adopt.
3. **It prints which account the token points at** before it touches anything — demo OA vs the customer's.

🔴 **A correction worth passing on, because it is about their account, not ours:** my task said "cancel the channel
default"; Jason caught that this collides with "ours only" — **if the customer had set their own default, my
wording would have silently cancelled it.** Fixed: a foreign default is **reported and left**. Their configuration
is not ours to change, and a tool built for review must not make an unreviewed change.

**Ball: you → the owner.** @Jason moves to TASK-251 (§16).

## 2026-09-06 — Sober → @Porter: AC-5 read done (it is NOT a code defect) · ours-only answered (TASK-252) · REQ-083 + REQ-077 still queued.
**AC-5 — the money one, and it is a missing ROW, not a missing branch.** The typed-amount branch looks up
`bo.item` where `external_ref='other-booking'`; if that row is absent it logs *"NOT POSTED — no bo.item for
external_ref='other-booking' … Run `bun run sale:ensure-items` on this box"* and returns false. The bucket is in
`SALE_ITEMS` and `ensure-sale-items.ts` iterates `SALE_ITEMS`, **so no code is missing.** ✅ Your own evidence
kills the alternative: *ops off* would have skipped everything, and the First Trial ฿1,390 posted in the same pass.

🧪 **DATA REQUEST, two checks (owner's or yours — never mine):**
1. `som-back-out.log` around **18:19:06 on 09-05** — is that `NOT POSTED … 'other-booking'` line there?
2. Does `bo.item` hold a row with `external_source=<SALE_SOURCE>` and `external_ref='other-booking'`?
   **Absent = confirmed. Present = I am wrong and I want to know.**

✅ **Fix + recovery, safe:** `bun run sale:ensure-items` on `sid` (INSERT-only, never overwrites a price), then
**re-run the day-end for 2026-09-05**. Nothing double-posts — auto-attend touches only `CONFIRMED`, and revenue
posts on `rev:<bookingId>` (a replay returns `skipped:"duplicate"`, with a 23505 fallback). ⚠️ `uat` needs the
same seed **before** any typed-amount อื่นๆ booking happens there.

📌 **My miss, and it is now a control:** TASK-225 stated this deploy step twice in its own file and **it never
reached `board.md`'s PENDING DEPLOY block** — the block people actually read. That is the board's own rule 4.
**Added as PENDING DEPLOY item 6**, with the recovery.

**Ours-only inversion → TASK-252.** A menu is ours by its **name** (`NAME_TO_KEY`, derived from the definitions),
not by a row we own and mutate. `ours-only` does not relax. `publish` will **report** the litter, never delete it.

⏳ **Still on my desk, in your order:** REQ-083 sizing read, then REQ-077's notification design review.
**Ball: you** — the two AC-5 checks are money and ahead of everything.

## 2026-09-06 — Sober → @Porter: REQ-077 notifications specced. Your five decisions stand. Three things the templates could not know.
`SPEC-072` + **TASK-253** (renderer + fields) and **TASK-254** (`COURSE DEDUCTION`), sequenced. @Jason is on 253.

✅ **Your gap analysis is right and stronger than it sounded:** sending is `enqueueLine → outbox row → the worker
renders`. Five of six already have a trigger, a payload **and** a row ⇒ **this is mostly a renderer change.**

🔴 **1. The renderer has no `recipientType`** — so *"the teacher's copy minus two lines"* is not expressible today.
And it collides with a deliberate invariant at `scheduler.service.ts:3325` (*"the payload is built ONCE and sent
to both people… one of them would be missed"*). **Resolved: one payload, two renderings** — the invariant is about
**facts**, your rule is about **audience**. 📌 It also keeps your promise to the customer literally true: either
answer stays **one line**.

🔴 **2. `COURSE DEDUCTION` has two triggers.** Quota is deducted at check-in **and at the day-end auto-attend** —
and since REQ-070 the day-end is the **majority** path. Wired to check-in alone it would be missing for most
sessions and would still pass every test.

🔴 **3. A day-end re-run must not send it twice** — we have just told the owner a re-run is the safe AC-5
recovery, and a duplicate *"1 session used"* would make that untrue.

⚠️ **One question that may be for the customer, not us:** `daily_reminder` **groups** sessions
(`groupReminders`); your `TODAY'S SCHEDULE` is written as one class. **What does a parent with two children, or
two classes in a day, get today — one message or several?** I have asked @Jason for the current behaviour first.
**If their template assumes one class per message, that is theirs to confirm.**

🅿️ AC-5 untouched and unfolded: the two data checks stay open, and **`uat` still needs `sale:ensure-items` before
any typed-amount อื่นๆ booking there** — PENDING DEPLOY item 6. **Ball: @Jason (TASK-253).**

## 2026-09-06 — Sober → @Porter: TASK-253 DONE. One concrete question for the customer, and it is the only one.
The renderer now knows its audience: `visibleFields = TEMPLATE_FIELDS − TYPE_OMITS − AUDIENCE_OMITS`. **Your five
decisions are lines of data** — reverting one is deleting or flipping a line, so the promise you made the customer
is now structurally true rather than asserted. tsc 0 · 1359/0 · nothing sent.

🔴 **The question, and it is concrete enough to put to them as-is:** their `TODAY'S SCHEDULE` is written as one
class, but the system sends **one message per person per day listing every class** — deliberately: *"per-booking
would send a teacher eight separate pushes before 08:20… a muted channel is worse than no channel"*, and the
send-once key is built on one-per-person. **I have ruled we repeat the block inside the one message** (their
template describes what one class looks like, not how many messages arrive — and for a parent with one class it
renders identically).
⚠️ **But a coach with eight classes would get eight blocks where they get a list today.** ⇒ **Ask them: "a coach
with eight classes — eight blocks, or the list they get today?"** Either answer is one line; the wiring stays held
until they answer, and the owner-verified list composer is not being deleted meanwhile.

📌 Minor, only if their template is meant to be exhaustive: @Jason kept two lines it does not list — the confirmed
`Sessions` count and the note typed at booking (TASK-219). **I read their template as a layout, not a whitelist**,
so I have kept both. **Ball: @Jason (TASK-254), and you for that one customer question.**

## 2026-09-06 — Sober → @Porter: TASK-254 DONE. REQ-077's notification half is code-complete bar ONE customer answer.
`COURSE DEDUCTION` now fires from **both** deduction paths — the manual check-in and the day-end auto-attend (the
majority path since REQ-070) — from one helper, with `Remaining` read from the write. tsc 0 · 1377/0 · no
migration · nothing sent.

🔴 **A correction against me, worth telling the owner only if he asks how the review works:** my task instructed
an idempotency key on the outbox row; @Jason found that mechanism is **transaction-unsafe** and both sites run
inside transactions — following me literally would have aborted the day-end. He checked before obeying.

⏳ **The only thing outstanding is your customer question:** *a coach with eight classes — eight blocks, or the
list they get today?* Until they answer, `daily_reminder` (Parent 2 · TODAY'S SCHEDULE) stays as it is; the
machinery is built and the owner-verified list composer is untouched, so either answer is small.

📌 Also cut, no priority: **TASK-255** — three copies of *"student → parent → LINE user id"* in the repo. Since
TASK-230 a family may hold several accounts, so this may be a live defect rather than a tidy-up: **one parent
silently not getting a message the other one got.** The task asks for the differences to be reported before
anything is unified.

**Ball: you** — the eight-block question, then REQ-083 whenever you want it. **Nothing is on @Jason.**

## 2026-09-06 — Sober → @Porter: Decision 6 taken (one correction inside it) · REQ-083 read done — it is NOT a display fix.
**TASK-256 cut, @Jason has it.** Decision 6 accepted as the layout of record; one class renders as their template
minus the numbering, asserted against their literal. ✅ And your confirmation of the two keeps **as requirement
rather than tolerance** is the right call — *"a layout, not a whitelist"* is the sentence I will reuse.

🔴 **One correction, and it is factual rather than stylistic.** *"`Date` and `Coach` are constant for the whole
message"* — `Date` is; **`Coach` is not.** A parent with two children (or one child in two programmes) can have
**two coaches in a day**, so hoisting would print one coach's name above another coach's class, in the message a
parent reads to know who is teaching their child. Your own line — *"the parent's copy needs none of this in
practice, one or two classes"* — lands on it: **two is where it breaks.** ⇒ the task says **hoist only when the
blocks actually agree, computed not assumed.** Your layout is unchanged and your one-shared-rule reason is why I
did **not** special-case parent vs teacher.

🔴 **REQ-083 — the answer is the expensive one. It is a real un-reversal.**
1. **The entitlement is never given back:** there is **no `usedSessions - 1` / `usedHours - 1` anywhere in the
   repo.** The only writes are the two `+ 1`s. ⇒ not a stale display; the number outside is honestly reporting a
   wrong number.
2. **Revenue has no reversal mechanism at all** (`sale-post.ts` has no reverse/refund/void). ⇒ a **1st Trial / 1HR
   / อื่นๆ** whose day-end already posted **keeps the money in the books**; a course/voucher booked its revenue at
   sale, so there is nothing to reverse. **The two cases differ and his answer may differ with them.**
3. 🔴 **A third question I am adding, because the code already answers it and I doubt he means it:** the leave path
   **charges leave quota** ⇒ an `ATTENDED → SICK_LEAVE` correction **costs the family twice today — the session
   stays used AND a leave day is spent.** That is your `C-22`, answered by the code in the direction I doubt he
   wants. **A correction is not a leave request; it is an admission the first record was wrong** — worth putting
   to him in those words.

⇒ **Size: a reversal path, not a patch** — one place that undoes what attending did, with TASK-254's *"read the
post-value from the write"* discipline. **Ball: you** (his two answers, plus #3), **@Jason on TASK-256.**

## 2026-09-06 — Sober → @Porter: ✅ REQ-077's NOTIFICATION HALF IS COMPLETE. The customer has something to look at.
TASK-256 passes: tsc 0 · **1395/0** · no migration · nothing sent. With TASK-253 (audience) and TASK-254
(`COURSE DEDUCTION`), **all six of your decisions are live in a working thing — each still one line to revert.**
📌 That is the owner's instruction discharged rather than merely followed: *"แล้วแต่นายช่วยเขาดีไซน์ได้ แล้ว
เขาดูทีหลัง"* — **they now review a working message, not a document.**
🚫 `renderSchedule` is untouched and still serves the teacher's `ตาราง` command, so the fallback survives if they
prefer the list they have.

⚠️ **The OA-move / rich-menu half of REQ-077 stays DRAFT** — not in this.

**Ball: you** — put the six in front of the customer. **Nothing is on @Jason.**
⏳ Queue behind it, unchanged: **REQ-083** (his two answers + the third I added: an `ATTENDED → SICK_LEAVE`
correction spends leave quota today) · **TASK-255** · TASK-251 / 252.
🅿️ AC-5 still parked — and **`uat` still needs `sale:ensure-items` before any typed-amount อื่นๆ booking there**
(PENDING DEPLOY item 6). Leaving it visible every time I write, because that one is silent when it bites.

## 2026-09-06 — Sober → @Porter: the three defects are TASK-257 (+1 you did not see). Both `ยืนยันตารางสอน` questions answered.
**TASK-257 → @Jason.** Heading → `CONFIRMED SCHEDULE:` (emoji kept). 🔴 **The `Date`/`Time` one is worth telling the
owner about, because it explains itself:** the renderer puts `weekday + startTime` into `Date` and `startTime`
alone into `Time` — **and the payload has no `endTime` at all.** The range was never *carried*, not lost in
rendering. Fixed on the same payload, derived once.
🔴 **A fourth with the identical cause, which nobody has screenshotted yet:** `ob_l_note` renders **`หมายเหตุ`**
under the same eight English labels, exactly like `Sessions`. **Both fixed** — and I asked @Jason to name the
cause (*two labelling conventions in one message*) rather than fix my list, or the next screenshot reports the one
we left. 📌 On *"my omission, not his"*: the label was never in the requirement, so neither of you could have read
it there. **It was a gap in what got written down; it is written down now.**

✅ **Q1 — `ยืนยันตารางสอน` is BESIDE REQ-077, not in it.** It is `booking_confirmed`, the **per-booking** confirm;
the customer's Parent 1 is explicitly *"คอนเฟิร์มทั้งคอร์ส"*. **Nothing is broken — you can tell them so.**

🔴 **Q2 — neither "small change" nor "different REQ": a small change with a RE-VERIFICATION attached.** The code is
a template key and a swapped case body, because TASK-253 already built the block. **But `booking_confirmed` is
owner-verified (TASK-228 / AC-16) and pinned byte-for-byte so it cannot drift** — converting it means deliberately
breaking that pin and asking him for another round on a phone. ⇒ **The question to put to the customer is not
"may we convert it" but "do you want the whole OA in one style?"** — you are right that they will notice the
mismatch before they notice what we got right. **If they say yes it is one cheap task; everything it needs exists.**

✅ **REQ-083:** your #3 ruling turns my read into a **defect against his ruling** rather than an open question, and
your #2 is right — `AC-5` is conditional on *posted* revenue, so course/voucher falls to `AC-6`: **no movement at
all, not a ฿0 row.** **I am speccing it next and not waiting on your one confirm.** **Ball: @Jason (TASK-257).**

## 2026-09-06 — Sober → @Porter: ✅ TASK-257 DONE. The three defects are fixed at the CAUSE. One copy word is yours.
tsc 0 · **1402/0** · no migration · nothing sent. **REQ-077's notification half now matches the customer's format
on a phone.**
📌 `Sessions` and `Note` were **one** bug, not two — two labelling conventions coexisting in one message. Fixed by
a net: **no REQ-077 message may contain the old label helper at all.** And @Jason found the coupling that forced
new keys rather than edits: `ob_l_note` also renders `ยืนยันตารางสอน`, which is **owner-verified and byte-frozen**
— editing the key would have fixed one message by breaking the other.
📌 The `Time` fix is asserted by **comparing the two messages to each other**, because the defect was the
disagreement, not the format. And he covered a window I had not thought of: **outbox rows queued before the deploy
have no `endTime`** — they print `Time : 10:00`, never a dangling `10:00-`.

⚠️ **One copy line, and it is yours — one word.** `TODAY'S SCHEDULE`'s empty state says *"ไม่มีคาบสอนในช่วงนี้"*;
**"ช่วงนี้" is the wrong noun under a heading that says TODAY'S.** Borrowed from the weekly composer. Unreachable
in practice, and @Jason **reported rather than edited it because copy is yours.**

**Ball: you** — the customer review of the six · the *"whole OA in one style?"* question · that word.
⏳ **Me next: REQ-083's spec**, not waiting on your one confirm. **@Jason: nothing assigned.**

## 2026-09-06 — Sober → @Porter: the `uat` batch is SEQUENCED. And the two items I would cut, named in advance.
Full reasoning in today's log. **The principle, because it is not the obvious one: one engineer + a held batch ⇒
order does not change the total.** Everything ships together, so *"which matters most"* buys nothing. **What order
changes is when we learn things** ⇒ **information first, uncertainty second, certainty last.**

**1. TASK-255 (read only)** — it can **ADD** to the batch, so it goes before the scope is fixed · **2. REQ-083** —
spec is fast, my read is done, and you settled both questions · **3. REQ-076** — 🔴 ~20 ACs, never designed or
started, **the highest-uncertainty item; early not because it is important but because this is the last point at
which "it is twice the size we thought" can still be said** · **4. REQ-084 defect half** · **5. TASK-251** ·
**6. TASK-252** · `sale:ensure-items` is a deploy step, not work.

⚠️ **The part you actually need: 5 and 6 are last BECAUSE they are the descope candidates.** If it runs long I
would cut **TASK-252, then TASK-251** — 252 prevents a problem nobody has hit; 251 is a real customer request but
a collision they can survive another week. 🔴 **Nothing above line 4 is cuttable.** **You own telling the owner
what a delay costs — that is the list.**

✅ **Your recovery order is right**; a ledger with MORE rows than the journal is the safe direction (the dangerous
one is fewer, which is what `0028`–`0031` are). ⚠️ **One discrepancy to settle now:** TASK-225's runbook is
`db:migrate → db:verify ✅ → sale:ensure-items → restart`; your path puts `sale:ensure-items` **after** the
restart. **Both safe** (the only reader is the 18:30 day-end) — **use the recorded order so the runbook stays
true instead of becoming a second version of itself.**

📌 Your ledger-vs-schema pattern is recorded: **`db:verify` red means "the bookkeeping is wrong", not "the change
is absent" — and its output does not draw that distinction.** Not a task today.

**Ball: @Jason** (TASK-255's read). **Me: REQ-083's spec, then REQ-076's.**

## 2026-09-06 — Sober → @Porter: 🔴 RULED. **Do NOT correct the customer's list — the promise is buildable.**
**Your last line changed my ruling while I was writing it, and it changed it for the better.** I was about to hand
the owner a choice: one account per family made explicit, **or** genuinely several. **You told me the customer has
already been told in writing that several parents can link and act for each other** ⇒ **that choice does not
exist.** Offering it would be offering a retraction dressed as a design option, and I will not put that in front
of him.

✅ **The answer you are waiting for is YES: fixed, it works as advertised.** Nothing in the schema is missing —
**`family_line_links` and its unique index were built for exactly this at TASK-230.** It is wiring, not design.
**Leave the list alone.**

**The size, honestly:** outbound is **small** (three sites → `familyLineUserIds`); **inbound is the larger half** —
`findParentByLineUserId` at **seven call sites plus a hand-rolled copy** — and it is the half that makes the
promise true. ⚠️ **Both halves ship or neither**, and that is now a requirement, not a preference: an
outbound-only fix makes the second phone *inconsistently alive*, and the family reports *"the bot is broken"*
instead of *"my husband gets nothing."*

🔄 **Batch re-sequenced — a live defect against a written promise beats an unstarted feature:**
**1. this · 2. REQ-083 · 3. REQ-076 · 4. REQ-084 defect · 5. TASK-251 · 6. TASK-252.**
🔴 **New descope list: 252, then 251, then REQ-076.** **This item is not cuttable at all** — cutting it means
correcting the list you have just declined to correct.

📌 And you are right that the read is why we know: under any other order this gets unified early and **`uat` ships
the outbound half alone** — the half that looks fixed.
**Ball: me** (REQ-083's spec, then this one as a defect). **@Jason: REQ-084's defect half meanwhile.** Nothing is
on you or the owner.

## 2026-09-06 — Sober → @Porter: REQ-083 is specced (SPEC-073 + TASK-258). Your open confirm does NOT block it.
✅ **Your question — "does the money stay for a course?" — is already answered by AC-6:** a course or voucher posts
its revenue **at sale**, so the attendance posted nothing and there is nothing to reverse **either way.** ⇒ the
build can start before he answers; a surprise changes a sentence, not the design.

🔴 **One AC cannot pass today and nobody had noticed — worth telling the owner as evidence the read was needed.**
**AC-8** (re-attend after a correction posts again) is impossible: revenue posts on `rev:<bookingId>`, **fixed for
the life of the booking**, so a re-attend hits the duplicate check and **returns success while writing nothing.**
Fixed by giving the keys a generation, derived from the ledger — **no migration.**

⚠️ **AC-3 I have flagged as a reading rather than a proof.** I believe the outside counter is wrong simply because
the entitlement was never returned — but @Jason must name where that number comes from and confirm it derives.
**A balance right in the database and wrong on a screen has not been fixed**, and that is the AC the owner
actually reported.

**Ball: @Jason** — REQ-084's defect half, then TASK-258. **Mine: TASK-255's defect spec, then REQ-076.**
Nothing is on you or the owner.

## 2026-09-06 — Sober → @Porter: TASK-258 (REQ-083) DONE. One consequence for you to know, not to fix.
tsc 0 · **1426/0** · no migration · nothing sent. AC-1…AC-10 built.

🔴 **Worth telling the owner, because it is the clearest evidence yet that the reviews are load-bearing:** my own
spec would have shipped **two money defects**, and @Jason found both by doing a sweep I asked for as a question.
(1) SPEC-069's *"revenue already posted"* warning would have said **"no money posted"** about a booking that had
just been charged. (2) The **discount** key is fixed for the life of a booking too — a re-posted sale would have
written the **list price and skipped the discount**, over-charging the family by exactly that amount, **silently.**
**Neither would have failed a test, thrown, or logged.**

✅ **AC-3 — the one the owner actually reported — is now proven, not assumed:** one column read by every outside
surface through one function, no cache. Fixing the balance fixes every screen at once.

⚠️ **The consequence to know:** after an undo, the plan is **one scheduled row short of its balance** until
someone books it ⇒ **a parent may see "4 remaining" with 3 classes on the calendar.** Correct behaviour — a
make-up row would hand the family the same session twice — but a plausible support question. **Yours to know.**

**Ball: me** — TASK-255's defect spec, then REQ-076. **@Jason: REQ-084's defect half.** Nothing on you.

## 2026-09-06 — Sober → @Porter: the multi-account defect is specced (SPEC-074 + TASK-259). It is wiring, and no data is touched.
✅ **Confirming what I told you, now with the code checked:** both accessors already exist and are already
correct, and `familyOfLineUser` **falls back to the column** — so **a family whose link row was never written
still resolves. No backfill, no migration expected.** **The promise the customer was given is reachable without
touching a row of their data.**

🔴 **One trap worth knowing about, because it is the kind of thing that makes a fix look like a lie:** the
reminder's send-once key is **per PERSON**, and today one parent means one device — so the moment we send to two
accounts, the family's two rows collide on the unique index and **the second is silently dropped.** The father
would get nothing, exactly as before, while every internal test passed. **It is in the task as the first thing to
handle.**

⚠️ **Named for you rather than designed away:** the regression that matters most is **a family with ONE account
behaving exactly as today** — that is every family we currently have.
🚫 **And I have refused one thing on purpose:** no "who receives what" preference. The promise was *ทำแทนกันได้*
— everyone — **and building a settings panel out of a defect would be answering a question nobody asked.**

**Ball: @Jason (TASK-259, both halves or neither).** **Mine: REQ-076's spec** — the last unspecced item in the
batch. Nothing on you or the owner.

## 2026-09-06 — Sober → @Porter: ✅ TASK-259 DONE — **the promise in the customer's list is now true in the code.**
Both halves shipped together. tsc 0 · **1445/0 on four runs** · **no migration, no backfill, and not a row of
their data touched.** A family's second account now receives messages **and** can use the bot.
⚠️ **Do not correct the list** — that answer stands, and it is now built rather than predicted.

📌 The trap I warned about was real and is handled with a detail I had not asked for: the **primary** account
keeps the **old** key shape, so **nobody gets messaged twice on deploy day**, while a second account's key has
never existed and cannot collide.
⚠️ **The regression that mattered most is asserted:** a family with ONE account behaves exactly as today — that is
every family you currently have.

**Two things reported rather than fixed, now on the board with their triggers:** the test-data cleanup script's
*"has LINE ⇒ not test data"* refusal (it guards **deleting real customer data**; safe today, and the reason it is
safe is now a tested property) · and a stale link row when a parent becomes a teacher (rare, not routing).

**`uat` batch:** ✅ #1 TASK-259 · ✅ #2 TASK-258 · ⏳ #3 REQ-076 — **mine, the last unspecced item** · #4 REQ-084
defect · #5 TASK-251 · #6 TASK-252, all specced and with @Jason. **Nothing on you or the owner.**

## 2026-09-06 — Sober → @Porter: REQ-076 specced — and it needs a decision from you that is not technical.
`SPEC-075` + `TASK-260`. **Every item in the `uat` batch is now specced.**

🔴 **Two things you need for the deploy note.** REQ-076 is **the batch's only schema change**, and it is **two
migrations, not one** — `bookingStatus` is a Postgres enum and a new value cannot be *used* in the transaction
that adds it, so one adds `PAUSED` and the next rebuilds the slot index. ⚠️ **The index rebuild takes a lock on a
live customer table** — I have asked @Jason for an actual duration, because *"it should be quick"* is not
something you can put in front of the owner.

⚠️ **And the one that is yours, not mine: REQ-076 has an FRONTEND half — the tray — and @Fern has not been
briefed on this batch at all.** It is the **only** item in the batch with an FE half, and the batch was sequenced
for one engineer. **It can run in parallel, which helps** — but it changes what *"the batch is done"* means, and
placing her is your call. **I am naming it now rather than when the backend lands and the tray does not exist.**

📌 I refused one shortcut worth knowing about: an existing legacy status (`PENDING_RESCHEDULE`) would have made
this need **no migration at all** — and would have put 2023's abandoned reschedules in the same tray as today's
pauses, permanently indistinguishable. **One migration saved, an ambiguous status forever.**

**Ball: you** — place @Fern. **@Jason:** REQ-084 defect → TASK-260 → 251 → 252.
