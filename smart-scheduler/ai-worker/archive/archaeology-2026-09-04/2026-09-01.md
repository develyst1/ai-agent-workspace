# ORDER 3 archaeology — extraction from `smart-scheduler/ai-worker/log/2026-09-01.md`

> Source: `smart-scheduler/ai-worker/log/2026-09-01.md` — **202,093 bytes / 2,550 lines, read in full.**
> Deduped against `smart-scheduler/ai-worker/SYSTEM-FACTS.md` as it stood on 2026-09-04.
> Scratch output only. Nothing in the repo was written. Marie merges.

---

## Product limits

- **A charged อื่นๆ amount is WRITE-ONLY.** After saving, the typed amount cannot be read back anywhere — the booking detail modal shows no amount, and the `POST /api/bookings` 201 response carries **no price field at all**, so a ฿20 booking and a free one come back **structurally identical**. ⇒ an admin can never check, correct or even notice a charged อื่นๆ before the day-end turns it into money; a `2000`-for-`20` typo is invisible until it is in the books. (Tanya, observed on `sid`, 2026-09-01 — logged as OBS-3, deliberately **not** raised as a defect because it breaks no written AC)
- **A typed อื่นๆ amount has NO CEILING.** Every other auto-posting type is bounded by the price card; a trial tops out at **฿1,390**. An unattended school visit can invoice itself overnight for ฿50,000 with nobody present at either end. (Sober, 2026-09-01)
- **Nobody but the owner can reverse a `bo.movement`.** QA has no reversal available at all; the sanctioned ฿20 test was approved only because *the owner* would reverse it in the backoffice himself. (Porter/Tanya, 2026-09-01)
- **A reversal carries no `refId`**, so nothing — screen or data — can distinguish *"not reversed yet"* from *"already reversed"*. The concrete harm is a **double** reversal: two IN movements against one sale, and the P&L nets negative. Inherent to the data, not a defect. (Fern, confirmed by Sober, 2026-09-01)
- **`/catalog-items` returns every active backoffice INCOME item, with no notion of "is this appropriate to charge a customer for."** If the backoffice carries INCOME rows nobody should put in front of staff, that is a requirement, not a predicate. (Sober, 2026-09-01)
- **A freelance teacher cannot be booked before a pay rate is set** — the teacher screen states `SET PAY BEFORE BOOKING`. (observed on `sid`, 2026-09-01)
- 🔴 **Engineers cannot reach any authenticated screen in `smart-scheduler-front`.** `/scheduler/*` sits behind an auth proxy whose only way through is a password field an agent may not fill, and **there is no QA-session route in that repo.** ⇒ every rendered check and every viewport measurement is QA's or the owner's, permanently — engineers can only supply *source analysis*, which must be labelled as such and never as a measurement. (Fern, twice, 2026-09-01; ruled a standing routing rule by Sober the same day)
- **LINE links are per-OA and do not survive a move to a new Official Account.** REQ-077's move to the customer's real OA breaks every existing link; family, children and our own state live in **our** DB and do survive. (Porter, 2026-09-01)
- **There is no path by which this system learns a chat's identity without an inbound message.** Every `lineUserId` ever stored traces back to `eventUserId(ev)` on the inbound webhook. ⇒ our own back-office screen can authorise a **family**; it can never address a **chat** we have never heard from. Whatever an admin does, the door is something the **parent** presents. (Sober, read from source, 2026-09-01)
- **An admin working in LINE OA Manager sees a chat, not a user id.** (Sober, 2026-09-01)
- 🔴 **The full BE `bun test` suite reaches the LIVE `sid` database** through `eligible.route.test.ts:13`. Sober refused to run the full suite all day for this reason and ran only targeted files. Still open and untasked at the end of 2026-09-01. (Sober, 2026-09-01)

## Schedules

- **`drizzle-kit migrate` runs the whole pending set in ONE transaction**, so `ALTER TYPE … ADD VALUE` needs PostgreSQL 12+. (Jason, 2026-09-01)
- ⇒ **`sid` runs PostgreSQL 12 or newer** — `0029` added an enum value inside that single transaction and did **not** throw. (observed, 2026-09-01)

## Telling the two boxes apart

- **`.env` decides which box a command hits, and it does not travel in git.** Nobody may reason about numbers off a screen until the box that produced them is named. (Porter, 2026-09-01)
- **`sid` is Tanya's box. `uat` is not hers**, and the two are never touched in the same sitting. (Sober/Porter, 2026-09-01)
- 🔴 **QA has NO backoffice access.** `sm-test-access.txt` covers the **frontoffice only**; `backoffice-som.develyst.online` is not in it. ⇒ QA can confirm a booking flips to `ATTENDED` but **can never read what a job posted** — every money AC stays `NOT_TESTED` after the job runs unless the owner reports the movement or read access is granted. *"The run is not the evidence; reading what it posted is."* (Tanya, 2026-09-01)
- **`sid`'s data is not QA's to spend:** its nine Active courses are all **real customers**, no QA-owned student has an active course or voucher, and **all 10 `sid` freelance teachers read `฿0/h`** with no pay rate. (observed, 2026-09-01)
- **The `uat` remote-DB whitelist line, opened for diagnostics on 2026-08-16, is still open.** Cited as the standing example of a permission that outlives its conditions. (Porter, 2026-09-01)
- 🔴 **`db:verify` can never go red on a SURPLUS ledger row.** It asserts *journal ⊆ ledger* plus one schema witness per journal entry (`missingMigrations`, `src/lib/migration-ledger.ts:89`); an extra row satisfies both. ⇒ the ledger count can drift upward forever and the check stays green. Observed on `sid`: journal **30** vs ledger **40** rows, unexplained at the end of the day. (Sober, 2026-09-01)
- **`db:verify` witnesses an index by NAME, not by predicate** — a quietly changed predicate passes verification while the running index and the schema disagree. Named as *"`0022`'s blindness one layer over."* (Jason/Sober, 2026-09-01)

## LINE

- 🔴 **The 2026-09-01 rehearsal permission EXPIRES on a TRIGGER, not a date.** Owner: *"ตอนนี้ซ้อมได้ เพราะเดี๋ยว uat เขาเปลี่ยนเป็นอันใหม่ทีหลัง ตอนนี้ uat ก็ไม่มีลูกค้าจริง ๆ ใช้"*. Written down as: **this freedom ends the instant either (a) the customer's real OA is connected, or (b) a real parent links on `uat` — whichever comes first.** From that moment `sid` needs its own channel or rehearsal stops. (owner + Porter, 2026-09-01)
- **"No real customers on `uat`" is true of PARENTS ONLY — 0 of ~180.** It is **not** true of teachers: 2 real teachers are linked. (Porter, 2026-09-01)
- **`uat`'s LINE inbound is DEAF** while the webhook points at `sid`. Accepted by the owner. (Porter, 2026-09-01)
- 🔴 **LINE goes to EVERY assigned teacher, not just the primary.** Owner: *"ส่งไลน์หาครูทุกคน แก้กล่องยืนยันด้วย"* (2026-09-01). Verified in source the same day: the confirm path loops `assignedTeacherIds()` and enqueues **one outbox row per teacher**; the single `notification` object in the API response is the **response shape**, not the send, and describes only the booking's own teacher.
- **LINE has an account-wide Bot/Chat response mode and a webhook `mode` (`active`/`standby`) field. Whether PER-CONVERSATION muting exists on this OA's plan was never established** — it is a fact about the owner's OA Manager, not about our code. **Account-wide muting is not acceptable**: one admin answering one parent must not silence every family. (Sober, 2026-09-01)
- **Per-user rich menus are already in production**, not theoretical — `linkRichMenuToUser` / `setDefaultRichMenu`, ids in `app_settings.line_rich_menu_ids`; REQ-042 was exactly "a role change did not switch the rich menu", fixed and **owner-verified 2026-08-16**. (Sober, 2026-09-01)
- **Taps and typed keywords go through the SAME handlers** (`line-webhook.service.ts`), and `CHOOSE_ROLE` already accepts a typed `2` ⇒ the owner's PC constraint costs nothing to satisfy. (Sober, 2026-09-01)

## Business rules the owner set earlier and that were never written down

- 🔴 **Overbooking is only permitted when the previous student is on leave.** The `SICK_LEAVE` carve-out is deliberate **UC-004 overbooking**, handled on purpose at `getCalendar:494` — *"you can only overbook when the previous student is on leave"* is the product's own refusal message. A slot guard even slightly too wide breaks it, and would surface as an unrelated regression. (observed on `sid` + Sober/Jason, 2026-09-01)
- **The owner has TWICE refused to let money move as a side effect.** Cited by both Jason and Sober as the standing pattern to check any auto-posting change against. (2026-09-01)
- **The owner's own use cases for the อื่นๆ type are ประชุมทีม and ปิดปรับปรุงลาน** — neither consumes an entitlement, so the entitlement-deduction branch is not on the path he asked for. (Porter, 2026-09-01)
- **A trial is ฿1,390; a discounted trial is ฿1,190** (a −฿200 discount, stored as a **negative** `discountMinor`). The rule is *render `amountMinor`, never re-derive it* — re-deriving shows ฿1,590, a wrong number plausible enough to survive a test. (2026-09-01)
- **REQ-051's admin code today is the shared static `"229"`** — its weakest point, and it got no cheaper when the family code was cut. (Sober/Porter, 2026-09-01)
- **One LINE account belongs to ONE family.** Enforced by `family_line_links_user_uq`; it extends the existing *"one LINE user ⇒ one active roster link"* rule `moveRosterLink` already enforces for teacher/parent. Kept explicitly even after the 6-digit code was cut. (Sober, ratified by Porter, 2026-09-01)
- 🔴 **A child has more than one guardian, and the product models ONE LINE account per family.** The owner named it as ordinary family life, not an edge case: *"แม่ผูกไว้แล้ว แต่แม่ป่วย พ่อจะลา ไลน์พ่อไม่ได้ผูก ไลน์แม่มี PIN"* — the PIN detail kills every "just use mum's phone" workaround. (owner, 2026-09-01)
- **After the 6-digit code was cut there is NO self-service path into LINE.** A LINE account joins a family only when an **admin opens the door for that chat** — mother, father, a new phone: the same single admin action every time. **Known cost, recorded when decided:** the sick-mother case now depends on an admin being reachable; on a Sunday evening that means the phone. *"If it ever becomes a complaint, the answer is not to reopen this."* (Porter on the owner's cut, 2026-09-01)
- **Owner's queue order, 2026-09-01** — *"GO เอาเลย ให้อยู่คิวถัดจาก REQ-005"* ⇒ **REQ-005 → REQ-079 → REQ-076 → REQ-051 → REQ-077 → REQ-028 → the `REQ-BO` block.**

## Working agreements the owner has stated

- 🔴 **End every session with where the ball is — and the ball is ONE name, not a list.** The human, direct to Sober, 2026-09-01, twice: first *"end every session with where the ball is"*, then immediately *"do not split the ball"* after Sober posted a five-row per-role block. **A ball on five people is a ball on nobody.** Queues, parked decisions, DATA REQUESTs and QA items live on the board and in the REQ/TASK files and do not compete for it. Written into `SA-Lead.md`.
- **Every migration is proven on `sid` FIRST** — owner: *"ต้องลองที่ sid ก่อน ห้ามพลาด"* (quoted 2026-09-01). And a `sid` migration run is **watched, never fired**.
- **`develop` is canonical because another team also builds on `develop`** — owner's standing rule of **2026-08-28**, restated 2026-09-01 when the batch sat on `dong` and `develop` did not have `0028`/`0029`. (Sober, 2026-09-01)
- **The PM never touches an environment.** Anything that must run against a box goes to the owner. (Porter, 2026-09-01, refusing to run the OA-Manager webhook test himself)
- **A session that crosses midnight switches log files at midnight** (`PROTOCOL.md`). Three roles hit this on 2026-09-01 and one QA member nearly "corrected" a correctly-dated test file and sent the owner to check a healthy server clock over it.
- 🔴 **Concurrent sessions appending to ONE log file lose entries — last writer wins.** Confirmed by three separate losses (Jason ×2 on 08-30, Porter ×1 on 08-31). ⇒ **evidence lives in the TASK file's `## Implementation Notes`, never only in the log.** That is the only reason TASK-218/221/223 and TASK-147 survived being destroyed. (2026-09-01)
- 🔴 **An uncommitted working tree is not storage.** Agents never commit, so every engineer's output lives only as uncommitted working-tree changes; a routine branch sweep (`dong → develop → ff merge → dong`) deleted finished work **in both repos**, and it surfaces as *"the engineer never built it."* ⇒ **nobody concludes "it was never built" from an empty diff alone** — check `git reflog` and file mtimes first. (Jason + Sober + Fern, 2026-09-01)
- **`SPEC_DONE` means BUILT and SA-REVIEWED. It does not mean it works.** Only a run on the deployed build turns it into "working", and only QA declares that. (Porter, PROTOCOL, 2026-09-01)
- **Parked ≠ delivered ≠ done.** A parked REQ stays at its real board status and reads **"In Progress — parked"** in `OWNER-LIST.md`, never "Completed" — a park must not quietly read as a delivery in the list the owner shows the customer. (Porter, 2026-09-01)
- **A named gap is a decision; an unnamed one is the thing that bites.** Scope is cut explicitly and recorded as `NOT_TESTED` with the risk named — never by quietly declaring something tested. Precedent deliberately followed from REQ-058. (Porter, 2026-09-01)
- **Board rule 2 (ledger recovery), executed exactly as written on 2026-09-01:** `db:seed-ledger` dry-run → **read it** → `--apply` → `db:migrate` → `db:verify`. The gate is *"once the table above looks right"*, and every row must carry a concrete schema witness. Nobody green-lights `--apply` before that output exists.
- **`db:verify` is BLOCKING before any `pm2 restart`** — and *"no migration in this batch"* is a claim that expires, so it runs every time. (Porter, 2026-09-01)
- **Deploy ordering, per box:** `db:migrate` → `db:verify` ✅ → **`bun run sale:ensure-items`** → restart. Without the seed, every typed-amount charge logs `NOT POSTED` and vanishes. (Jason, 2026-09-01)
- **Board rule 3: never ship a new server-side gate without the screen that opens it.** Cited to refuse a restart that would have deployed 1 of 5 tasks. (Porter, 2026-09-01)
- **This repo forbids `db:generate`.** (Sober, 2026-09-01)
- 🔴 **A risk that is safe "for now" needs a TRIGGER, not a note.** Sober's own stated lesson after a "not blocking" residue went live unnoticed the moment the FE half landed. Explicitly modelled on the REQ-079 §8 rehearsal permission, which is written as a trigger for this reason. (Sober, 2026-09-01)

---

## ⚠️ REFINES EXISTING

**1. The commit rule — SYSTEM-FACTS has the ruling; the log has the words and the rule it replaced.**
- SYSTEM-FACTS: *"The owner commits, on his own schedule. Nobody asks about commit state, ever (2026-09-01)."*
- Log, verbatim, 2026-09-01: **owner — *"commit แล้ว บอกทีม เลิกยุ่งเรื่อง commit ฉันจะทำเองเมื่อถึงเวลาของฉัน"***. Porter's added detail: *"'This batch is code-complete' is still worth writing"* — ordinary status makes a commit point visible without anyone being chased. **The difference is the ask: state your work, do not request his.**

**2. The customer's refusal of the 6-digit code — the log carries an EARLIER date and a different quote.**
- SYSTEM-FACTS dates the refusal **2026-09-02** (*"ใช่ฉันเข้าใจว่ามันไม่ปลอดภัย แต่เราทำอะไรไม่ได้…"*).
- Log, **2026-09-01**: owner — ***"ลูกค้า confirm มาว่าไม่เอา 2FA 6 หลักที่เราคิด งั้นก็ยกเลิกออกจากแผน"*** + ***"ปล่อยเลย"***. Recorded as `REQ-079` §15, which supersedes §4–§14; the earlier sections were deliberately **not** deleted.

**3. The weak-code acceptance — SYSTEM-FACTS references it; the log has the owner's words and the reopen triggers.**
- Owner, **2026-09-01**: ***"ลูกค้ายังไม่มากขนาดนั้นที่จะโดน hack"***, overruling Porter's recommendation. Asked for the check to be **built and shipped disabled**, one `app_settings` switch away (REQ-031 / TASK-101/102).
- **Written reopen triggers:** the family count growing well past **~180**, or the code guarding more than *see children / leave / check-in*.

**4. The webhook flip — SYSTEM-FACTS has the fact; the log has the owner's verbatim and the expiry.**
- Owner, 2026-09-01: ***"ตอนนี้ไลน์ webhook ผูกที่ sid ถาวรแล้ว"***. Same expiry trigger as REQ-079 §8 (see LINE section above).

**5. The old bot behaviour SYSTEM-FACTS says was "replaced" — the log has the evidence and the date it was witnessed.**
- 2026-09-01, from the **owner's own OA Manager screenshot** on `sid`, in an **idle** chat: `เมนู` → **`เบอร์โทรไม่ถูกต้อง กรุณาพิมพ์เบอร์ที่ลงทะเบียน`** · `yo` → **`ไม่พบครูชื่อเล่น "yo" — ตรวจสอบอีกครั้ง`**. Porter's ruling: *"no reply to unrecognised text in an idle chat" is a behaviour **CHANGE** from what is deployed, not a new feature* — it needs a regression test.

**6. `uat` ≈ 201 / `sid` two digits — the log records why the exact number was retired.**
- The team's own reference said *"`sid` ≈ 20"* and was **stale** (actual 32 on 2026-09-01, growing as QA fixtures land). Fixed in `PROJECT-STATUS.md` on the day: **the tell is the ORDER OF MAGNITUDE, never the exact value** — *"a stale landmark is how somebody eventually concludes they are on the wrong box and stops a correct deploy."* (Porter, 2026-09-01)

---

## ⚠️ CONTRADICTIONS

**C1 — The day-end run time. SYSTEM-FACTS says 18:30; this whole log says 23:30, and money was planned on it.**
- **SYSTEM-FACTS (as it stands):** *"`end-of-day` 🔴 **18:30** — **The OWNER changed it himself**, 2026-08-29 (was 23:30 until 08-28)"*, and *"18:30 is DELIBERATE and CORRECT."* It further states that fixtures confirmed at **22:39/22:45 on 09-01** cost REQ-078 a test round because Porter had told QA "23:30" from stale documents.
- **Log, 2026-09-01, four separate roles, used operationally:** Jason — *"An อื่นๆ booking that nobody marks is auto-attended at **23:30**, and then it CHARGES"*; Sober — *"auto-attends at **23:30** and charges"*; Porter to Tanya — *"an unmarked session auto-attends at **23:30** and posts ⇒ a fixture you create today and forget charges real money on `sid` tonight"*; Tanya's ฿20 fixture was *"left to post at the **23:30** day-end on `sid`"*, with her own caveat *"I do not know the server's time of day."*
- **Not resolved here.** Recording that the 23:30 figure was not a stray line — it was the operative planning number for the entire ฿20 money run and for the QA footprint rules issued that day.

**C2 — The owner's teacher-clash ruling reversed within the same day.**
- **Earlier, 2026-09-01 (Porter):** *"Teacher clash on an อื่นๆ booking: **warn, never refuse** — his words, ***'เตือนพอ ไม่ห้าม'***."* AC-24 + AC-25 written into REQ-078 with Thai wording; `บันทึกต่อไป` chosen deliberately as the affirmative button *"so the default path must not read as an error being overridden."*
- **Later, 2026-09-01 (Porter), after QA found AC-24 shipped inverted:** owner ruled **option ข — *"ตามนั้น"***: **the hard refusal STAYS**, with an honest message (`ครู{ชื่อ} มีคาบสอนช่วงเวลานี้อยู่แล้ว ({ชื่อคาบ} {เวลา}) กรุณาเลือกเวลาอื่น`); the full overlap capability (guard + warning + two items in one slot) is **deferred to a follow-up REQ**, with the calendar's hidden-session fix (DEF-4) as its precondition. Original AC wording preserved in the REQ for that day.
- **Both are the owner, both 2026-09-01.** Reported, not resolved — but note the second is explicitly a *deferral*, not a repudiation, and the reason given was that relaxing the guard before the calendar can show two things in one slot would produce **invisible double-bookings**.

**C3 — The commit rule reversed within the same day.**
- **Earlier, 2026-09-01 (Porter):** *"**The owner will COMMIT at the end of every batch.** Written up as a board standing rule."* Acted on all day — Sober and Fern each reported the front repo's uncommitted state as an open item, and Sober put *"the front repo is still uncommitted"* in his BALL twice.
- **Later, 2026-09-01 (Porter):** owner — ***"commit แล้ว บอกทีม เลิกยุ่งเรื่อง commit ฉันจะทำเองเมื่อถึงเวลาของฉัน"*** ⇒ *"Nobody reports, chases, or asks about commit state — not in the log, not in a hand-off, not as a 'just a reminder'."* Porter: *"I am the one who put commit state into the reporting loop, so I am the one removing it."*
- SYSTEM-FACTS carries **only the second half**. The first half — and the board standing rule that was written for it — is not recorded there.

**C4 — Whether a phone number alone may reveal a family's children. The 09-01 log and SYSTEM-FACTS' 09-02 accepted risk point opposite ways.**
- **Log, 2026-09-01 (Sober, read from source):** `parentChildrenNote` (`line-pairing.ts:19`, REQ-020 / **TASK-047**) answers a phone number with **a count, never names** — with the reason in the file: *"Parent linking matches on phone alone, so anyone who types a phone number would otherwise be told that family's children's names."* Sober's ruling: showing names is *"an existing, deliberate, shipped control being switched off, and it is **one function** away"*; if the owner wants names with no door, *"he is knowingly re-opening TASK-047 — and it should be put to him in exactly those terms."*
- **SYSTEM-FACTS, 2026-09-02:** 🔴 accepted risk — *"**Anyone who knows a family's phone number can see their children and act for them** (leave, check-in). This is a decision taken with the risk on the table, not an oversight."*
- **Not resolved here.** The two may simply be consecutive states (a shipped control, then an owner acceptance that turned it off) — but SYSTEM-FACTS does not record that a deliberate shipped control existed and was switched off, nor that the SA asked for the decision to be put to the owner in those exact terms. **Marie's call.**

---

## Unsorted — 2026-09-01

- **`OTHER` is an overloaded token in this codebase** — `cancel-reason.test.ts:26` asserts `isEndReason("OTHER") === false`, unrelated to the `OTHER` booking type. Left as is; whichever task next touches that file renames it. (Sober, 2026-09-01)
- **Terminology used all day and worth pinning:** *"a join is a **FILTER** the moment its column can be null"* (Jason, 2026-09-01) — the sentence left at the site of the DEF-3 fix; and *"a `Record<BookingType,…>` fails the build when the union widens; a hand-written array of the same type does not"* — the trap found **three** times in `smart-scheduler-front` in one day.
- **`TASK-085` is the named incident** in which the migration-ledger area took the customer's calendar down. It is the reason nobody guesses at ledger state and the reason an unexplained row count is chased. (cited repeatedly, 2026-09-01)
- **`TASK-047` is the named incident** in which a phone-number lookup would have revealed a family's children's names. (2026-09-01)
- **`TASK-169` is the named 100× money-magnitude bug**; `TASK-170`/`TASK-183` the named allow-list-mapper drops. Used as shorthand for those defect shapes. (2026-09-01)
- **A studentless อื่นๆ confirm writes a SKIPPED *parent* outbox row for a parent who cannot exist.** Harmless as a record, but it colours `unlinkedParents` and every *"how many parents are unlinked?"* number reported to the owner. Left deliberately. (Jason, 2026-09-01)
- **`sid` fixture teachers named in the record:** **Bank** (= the owner's own LINE link) and **Dewy** (no LINE link). The owner linked to the existing fixture teacher `Bank` rather than creating a "ครูQA-Test" — so fixtures name **Bank**, not a QA-Test teacher. (Porter, 2026-09-01)
