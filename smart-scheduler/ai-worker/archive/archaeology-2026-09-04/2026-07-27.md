# Extraction — `log/2026-07-27.md` (11,943 bytes / 145 lines, read in full)

> Five Porter entries, one session. This is the day the protocol gap that produced the mixed-date
> `2026-07-20.md` file was fixed.

---

## Schedules

- *(No schedule facts in this file.)*

## Product limits

- 🔴 **The teacher↔backoffice sync was NEVER de-wired when it was put on hold.** `createTeacher` / `updateTeacher` / `archiveTeacher` / `reactivateTeacher` call `/internal/teacher-sync/*` through `opsSyncOr502`, which **throws 502 and rolls the transaction back on any non-2xx**. With the `ops` routes retired those calls 404 ⇒ **teacher add/edit/archive/reactivate 502 once the ops-retired backoffice is deployed** (Porter, read-only code check, 2026-07-27).
- 🔴 **Blanking `OPS_API_URL` does NOT rescue it** — `ops-client.ts:171` throws on an unset value too (Porter, 2026-07-27). **⇒ "just unset the env var" is not an escape hatch in this codebase.**
- **The live freelance cap at booking confirm/cancel does NOT use the remote client** — it reads the **local `bo.item`** in the scheduling app's own DB, so it is unaffected by the backoffice being down (Porter, verified, 2026-07-27).
- **`recordSale` income posting is best-effort and degrades silently** when its target is gone (Porter, 2026-07-27).
- **The remote freelance/quota `ops-client` functions are DEAD CODE — referenced only by tests** (Porter, 2026-07-27).
- **Retiring the `ops` routes was forced, not chosen: the `ops` tables do not exist in the shared `smart_scheduler` DB, so those routes would 500.** Their files were left dormant rather than deleted (observed, 2026-07-27).

## LINE

- *(No LINE facts in this file.)*

## Telling the two boxes apart

- 🔴 **The shared-database topology is the OWNER'S OWN DIRECTIVE, and his stated reason is "easy + scalable."** He confirmed it and said he had observed it working from another machine (owner, 2026-07-27). ⇒ **the separate `smart_backoffice_db` is retired by decision, not by accident.**
- **`backoffice-back` serves only `/auth` and `/bo` after this date** (observed, 2026-07-27).
- 🔴 **The owner works from MULTIPLE MACHINES CONCURRENTLY and the coordination files sync between them.** Porter's board edit **failed on a write conflict** because `board.md` had just synced newer content from the other machine — "effectively two Porter contexts on one board" (Porter, 2026-07-27). 📌 **The founding-era proof of the amnesia/multi-machine problem the workspace `CLAUDE.md` now legislates.**
- **When that happens the correct move is to HOLD all edits and ask the human who owns the file** — Porter did, and the human then cleared him because the other machine was idle (Porter + owner, 2026-07-27).

## Working agreements the owner has stated

- 🔴 **The agent NEVER logs into the live site with the owner's credentials, even when the owner offers them.** The owner offered a link plus username and password; Porter declined and **asked him not to send the password**, on two grounds: (a) he does not enter passwords or authenticate as the user, and (b) it breaks the established pattern (owner offered / Porter refused, 2026-07-27).
- 🔴 **The established pattern, named here explicitly: for REQ-001 and REQ-002 the HUMAN ran live acceptance and the agent VERIFIED the reported results.** That is the brownfield pattern for every live check (Porter, 2026-07-27).
- 🔴 **The owner may deploy at any time, on his own judgement, before the team's pre-deploy blockers have landed** — he did exactly that here, shipping while the teacher-CRUD 502 and the known drawdown-idempotency bug were still open (owner, 2026-07-27). ⇒ **"we told him to wait" is never a guarantee that he waited.** *(Marie: I am flagging this as a working-agreement-shaped observation rather than pure history — see my report.)*
- **Deploying is easy on the owner's side: `bun run build` plus a copy to the Windows server** (owner, relayed 2026-07-27).
- **Acceptance checklists handed to the owner are RISK-ORDERED** — the highest-risk check first, so the most dangerous answer arrives soonest (Porter, 2026-07-27).
- **The owner's reconcile instruction: "if it's true, then reconcile" — i.e. verify in code FIRST, change the board only after** (owner, 2026-07-27).
- **Whether to route work through the ai-worker flow or build it directly is the OWNER'S choice, and Porter must ask rather than assume** (Porter, 2026-07-27).

## Business rules the owner set earlier and that were never written down

- *(No new business rules in this file.)*

## Unsorted — 2026-07-27

- **The owner's git identity is `dong <nichaphon@smartalliance.co.th>`** (observed from the commit log, 2026-07-27).
- **All substantive code across 2026-07-20…07-27 was committed DIRECTLY by dev identities — `dong`, `neeeeroooo`, `dev` — not produced through role-session TASK files** (Porter, git sweep, 2026-07-27).
- 🔴 **Work happens outside the ai-worker flow and lands as commits referencing task numbers that are NOT in the board** — "TASK-027" arrived this way. **The board's REQ→SPEC→TASK tracking DRIFTS from the real repo, and the code is often further ahead than the board** (Porter, 2026-07-27).
- **The domain is `smartalliance.co.th`** (observed, 2026-07-27).
- **A whole working day can pass with ZERO ai-worker team activity while the code still moves** (observed, 2026-07-27).
- **Term — "the other machine":** the owner's second working machine, which runs its own role sessions against the same synced coordination files. On 2026-07-27 it was ahead on REQ-006 and had already produced TASK-027 and TASK-028 (observed, 2026-07-27).
- **The other machine's working rhythm at this date: idle now, resumes next week** (owner, 2026-07-27).

## ⚠️ REFINES EXISTING

- **SYSTEM-FACTS:** *"The owner commits, on his own schedule. Nobody asks about commit state, ever (2026-09-01). Agents never commit."*
  **This log, 2026-07-27, six weeks earlier and with the mechanism:** the owner does not merely commit on his own schedule — **substantially all code is written and committed outside the ai-worker flow entirely**, under untracked task numbers, so **the board is expected to lag the repo** and reconciling the board is a periodic chore, not an anomaly (Porter, git sweep across all 5 repos, 2026-07-27).

- **SYSTEM-FACTS `## Telling the two boxes apart`** describes per-server divergence (`C:\sm-jobs\*.ps1` differing between boxes).
  **This log adds the same hazard one level up:** **the COORDINATION files themselves diverge and race between the owner's machines** — a board write can fail on a sync conflict mid-session, and two role sessions of the same name can be live at once (Porter, 2026-07-27).

## ⚠️ CONTRADICTIONS

- **No contradiction against SYSTEM-FACTS.md originates in this file.**
- ⚠️ **This file supplies SIDE B of contradiction #3 recorded in `2026-07-20.md`** (staging vs production). Repeating both sides verbatim so they travel together:
  - Side A, `2026-07-20.md [06:45]`: *"คุณฟีน: this is a **side/staging site, no backup**, "ไม่เป็นไร" → **skip the rollback; fix-forward.**"*
  - Side B, `2026-07-27.md [22:55]`: *"Stakeholder reports they **already deployed** the current build to the **real server** and core usage works. ⚠️ This went out **BEFORE REQ-005 + TASK-028 landed** — so the teacher-CRUD 502 risk and the known freelance-drawdown idempotency bug are potentially **live in production**."*
  - **Not resolved.** Whether `som.develyst.online` was staging, production, or re-designated in between is not determinable from these logs, and SYSTEM-FACTS.md never names these hostnames.
