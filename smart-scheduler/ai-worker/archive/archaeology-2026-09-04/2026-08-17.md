# Facts extracted from `log/2026-08-17.md` (45,108 bytes · 475 lines · read in full)

> ⚠️ **DATING CAVEAT.** This file is itself contested. Sober declared it canonical and told the team
> to *"Stamp entries `2026-08-17` unless the human/Porter says today is actually 08-18"*; **Porter
> then wrote into this same file that his session clock said `2026-08-18`**, and the human later
> confirmed 08-18 (see the 08-18 file). The last entry here says everything after a point is
> **stranded and re-surfaced in `log/2026-08-19.md`**. Tags below say `2026-08-17`; read them as
> "2026-08-17 or 2026-08-18". See `## ⚠️ CONTRADICTIONS`.

---

## Schedules

*(nothing new — this log carries no job schedule)*

## Product limits

- 🔴 **There is NO rate limiter anywhere in the codebase** — established here, while grounding REQ-051's public walk-in QR page. Sober's words: *"this is a **public, no-login page that can consume a paid session and expose children's names, and there is NO rate-limiter in the codebase**"*. (Sober, 2026-08-17)
- 🔴 **The only admin code that exists today is the shared static `"229"`.** There is no per-admin code and no other admin secret. (Sober, grounding REQ-051, 2026-08-17)
- **A VOUCHER has no make-up.** A course cancel re-owes a make-up so the family eventually gets the session back; **a voucher hour that was wrongly consumed was simply gone.** That asymmetry is why the corrected-check-in refund is the real money case. (Jason, 2026-08-17)
- **A course cannot be born mixed-program.** One course = one subject/program, enforced at both the validation boundary and the service. (Jason/Sober, 2026-08-17)
- **The settings-row LABEL is supplied by the backend in Thai only** — every row, pre-existing, so the English screen shows a Thai heading with English options. (Sober, 2026-08-17)
- **A FE engineer cannot render anything that lives inside a modal** — the headless harness will not composite it. Every modal-hosted behaviour is verified by QA, never by the FE role. (Sober, standing limit, 2026-08-17)

## Telling the two boxes apart

- 🔴 **`uat` will hold REAL FAMILIES once REQ-055 lands.** Porter, quoting the owner on why the migration order must not be shortcut: *"ห้ามพลาด เพราะหลังเทสเสร็จต้องขึ้น uat เลย"*. (owner via Porter, 2026-08-17)
- 🔴 **Standing owner rule for every migration: `sid` FIRST, verified, THEN `uat` — and a VERIFIED NON-ZERO backup before either.** The rule exists because **the board records four 0-byte "backups" from 2026-08-11**: a backup command can succeed and still produce nothing. (owner, via Porter, 2026-08-17)
- **The team NEVER touches a real environment's database. Migrations are an owner-run deploy step**, in this exact order: `db:backup` → `db:migrate` → `db:verify` → check the data condition the migration was for. (Sober/Porter, 2026-08-17)
- **21 real teachers exist on the production side and must never receive a rehearsal LINE message** — LINE testing uses a *test* teacher account. (Sober, 2026-08-17)

## Working agreements the owner has stated

- **Agents never commit. Work sits uncommitted on branch `dong` until the owner says so** — stated three separate times in this file as *"commit on the owner's word"*. (Jason/Sober, 2026-08-17)
- 🔴 **The owner has a STANDING PRE-AUTHORISATION mechanism: if a feature turns out to be hard, remove it rather than come back for a round-trip.** Verbatim, on the COURSE tab: *"ถ้ามันยาก เอาออกไปเลย"*. Porter treated that as authority to remove without a new decision — *"that is the standing-decision mechanism working."* (owner, 2026-08-17)
- **The owner DELEGATES visual/design choices to the team but keeps the final eye.** Verbatim on the calendar palette: *"ไปคิดเอา ทำออกมาให้ดี"*. ⇒ the mechanism is the team's call; the hues are the owner's sign-off. (owner, 2026-08-17)
- **PROTOCOL date discipline: settle TODAY from the session clock, NEVER from the newest log filename. New date → new log file, newest wins. Nothing in a closed log is ever rewritten.** (PROTOCOL, cited by Sober and Porter, 2026-08-17)
- **Asking the human a clock question is explicitly NOT a chain violation.** (Porter, 2026-08-17)
- 🔴 **PII discipline for the customer's student list:** the source file and its report live **only** in git-ignored `project-docs/`; **no real row ever appears in any tracked file, task, log or output**; the console prints **counts and row numbers only** — never a name, phone or DOB — so a pasted screenshot cannot leak a family; the rule functions are unit-tested with **synthetic rows only**; and **the team never runs the importer against the customer's environment — the owner runs every step.** (Sober, 2026-08-17)

## Business rules the owner set earlier and that were never written down

- 🔴 **A corrected check-in MUST give the consumed session/hour back.** Treated explicitly as *"money owed to a family, not tidiness"*. (Porter's decision, 2026-08-17)
- 🔴 **No historical back-fill of wrongly-attributed check-ins.** Because a wrong attribution is not detectable from stored data, there is no honest list to correct — and a guess will not be authorised against real family balances. (Porter's decision, 2026-08-17)
- **Planned absences DECLARED AT COURSE CREATION are FREE (they do not consume leave quota). An absence marked later still consumes quota.** Owner chose option (B). Consequence the owner accepted: the maximum-weeks-by-size ceiling becomes the ONLY limit. (owner, via Porter, 2026-08-17)
- **The leave cut-off is configured PER TEACHER TYPE, not as one school-wide number** — the owner overruled Porter's lean for a single value. Two buckets: full/part-time, and freelance. Default **3 hours** each. (owner, 2026-08-17)
- **Leave notification goes through the EXISTING admin channel, IMMEDIATELY — not in the daily digest — and a late leave reads the same as any other.** (owner, via Porter, 2026-08-17)
- 🔴 **A walk-in leave INSIDE the cut-off is allowed, but only with an admin in the loop** — the owner's own addition, preferred over a clean exemption: the request becomes `PENDING_APPROVAL`, an admin is notified, and there is an on-the-spot admin-code fast path. Outside the cut-off, nothing changes. **A pending request must never silently become a leave, and never silently vanish.** (owner, 2026-08-17)
- **`notify_on_leave` ships defaulting to `admin_only`** — the teacher push stays OFF until a school opts in, so production stays silent through the deploy. Deliberate. (Jason/Sober, 2026-08-17)
- **Wave-1 student-import rules, all answered by the owner:** yellow rows → **excluded entirely** · no phone → **held back**, never invented · parent rows → become the **family name**, never a student · column A → **preserved verbatim and interpreted by nobody** · phone → **prefixed with 0 or held back** · DOB → **never guessed** · **nothing is invented — people only** (no courses, vouchers or bookings are created by the import). (owner, via Porter/Sober, 2026-08-17)
- **The import is run day-by-day by the owner, dry-run first, and it stops rather than writes if its own reconciliation (`imported + held + yellow = total`) does not balance.** The report is *"a work checklist for the owner, never a letter to the customer."* (Sober, 2026-08-17)
- **One parent per phone number (phone-uniqueness), and a cap of 5 students per parent — both already self-enforcing in `createStudentForParent` / `findOrCreateParentByPhone`** as of 2026-08-17. (Sober, verifying TASK-150, 2026-08-17) → see `## ⚠️ CONTRADICTIONS`.

## Unsorted — 2026-08-17

- 🔴 **NEVER run `bunx drizzle-kit generate` in this repo — `drizzle/README.md` says so explicitly.** `meta/` holds snapshots for **0000–0003 only** while the journal runs past 0017, so `generate` diffs against a four-migration-old state and tries to re-emit the whole schema, stopping at an interactive column-conflict prompt. **Every migration since 0004 is hand-authored and manually registered in the journal.** Jason recorded it because *"the README's warning is easy to skim past and the failure looks like a tooling glitch rather than a design decision."* (Jason, 2026-08-17)
- **Every migration needs a SCHEMA WITNESS** — a named object whose existence proves the migration ran. A back-fill or a conditional `SET NOT NULL` has no reliable footprint by design, so the witness is the last *unconditional* object (e.g. an FK), and one transaction makes the FK imply the back-fill. (Jason, 2026-08-17)
- 🔴 **The status colour wheel is FULL.** `BOOKING_STATUS_COLOR` already uses **all six** semantic hues, so a booking *type* cannot take a different semantic colour without colliding with a status — including the owner's own examples (first trial = red = danger, course = dark green ≈ success). ⇒ **type must be distinguished by channel + text (a muted edge stripe in a dedicated token set + an always-visible label), never by hue.** (Sober, 2026-08-17)
- **The owner's stated fear about the calendar cell, which the design has to answer:** *"is this green a status or a type?"* (owner, via Sober, 2026-08-17)
- **A LINE refusal that has no catch makes the BOT GO SILENT.** `doLeaveBooking` had no catch, so any refusal propagated to the webhook's outer handler, which only `console.error`s ⇒ **the parent received no reply at all.** Any new refusal on a LINE path must be caught and rendered back, or the acceptance criterion is unreachable on the path parents actually use. (Jason, ratified by Sober, 2026-08-17)
- **A LINE refusal reaches the parent in THAI regardless of their language**, because the booking service does not know the caller's language. (Jason, 2026-08-17)
- Observed scale, may drift: the wave-1 import covers roughly **176 students**, with a Saturday/Sunday bulk of **135**. (Sober, 2026-08-17)

## ⚠️ REFINES EXISTING (SYSTEM-FACTS already says it; this log supplies the date/attribution it lacks)

1. **"No rate limiter" — this log is where and when it was established.**
   - **SYSTEM-FACTS:** *"There is NO rate limiter anywhere in the codebase (established while speccing REQ-051). Any attempt-counting or throttling is new infrastructure, never a library flag."* — no date, no attributor.
   - **This log:** it was **Sober, on 2026-08-17**, grounding REQ-051 (the public walk-in QR page) — and he refused to cut build tasks until the owner ruled on the rate-limit model, calling it *"built from scratch, must be spec'd, not defaulted."*

2. **"The owner commits, on his own schedule" — attested two weeks earlier than SYSTEM-FACTS records.**
   - **SYSTEM-FACTS:** *"The owner commits, on his own schedule. Nobody asks about commit state, ever (2026-09-01)."*
   - **This log, three times on 2026-08-17:** *"Uncommitted on `dong` — commit on the owner's word."* ⇒ the agreement was already operating on 2026-08-17, and it names the branch: **`dong`**.

## ⚠️ CONTRADICTIONS

**C2 — the 5-student cap: SYSTEM-FACTS says it appears in no requirement, spec or task; this log has an SA citing it as a known, self-enforcing constraint while reviewing a task.**
Both sides verbatim, unresolved:
- **SYSTEM-FACTS (Porter, 2026-09-02):** *"**Max 5 students per phone number.** 🔴 The owner's own decision, given 'a long time ago' … 📌 **It appears in NO requirement, spec or task in this repo** — Porter searched. It was implemented from an instruction that only ever lived in chat."*
- **This log (Sober, 2026-08-17, reviewing TASK-150, the wave-1 importer):** *"the script writes **only** parents/students via the existing `findOrCreateParentByPhone`/`createStudentForParent` (so phone-uniqueness + **the 5-per-parent cap** self-enforce)."*

I am not deciding which is right. Two things a merger must look at: (a) whether TASK-150 / SPEC-051 in fact mention the cap, which would falsify the "no task mentions it" half; and (b) the **unit**: SYSTEM-FACTS says "per phone number", this log says "per parent" — they coincide only because phone-uniqueness makes one parent = one phone, which is itself a fact worth recording.

**C3 — this file's own date.** Sober: *"`2026-08-17.md` (this file) is canonical … **Stamp entries `2026-08-17`** unless the human/Porter says today is actually 08-18."* Porter, in the same file: *"🔴 **my session's own current-date context says 2026-08-18, not 08-17.** PROTOCOL says settle TODAY from the session clock and never from the newest filename — so by my clock this entry belongs in `2026-08-18.md`. I am **not** opening a fourth file over it."* The human later confirmed **2026-08-18**. Additionally the final entry states: *"⏭️ THESE ENTRIES ARE STRANDED. Canonical file is log/2026-08-19.md."* ⇒ **this one file contains entries written across 08-17, 08-18 and 08-19.**
