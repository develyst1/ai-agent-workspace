# Extraction — `log/2026-08-30.md` (27,824 bytes · 340 lines · read in full)

> Archaeology run, ORDER 3. Candidate SYSTEM-FACTS lines only. Marie merges; nothing here was written into the repo.
> ⚠️ Note: this file's entries were **written on 2026-08-31 and misfiled into the 08-30 file** (Porter's and Sober's own
> date-discipline admissions, recorded in `log/2026-08-31.md`). Dates below are the log-file date unless the entry says otherwise.

---

## LINE

- 🔴 **INBOUND and OUTBOUND LINE are different problems and were being confused.** **Inbound** (linking, `เช็คอิน` tap, `ลา`, rich menu) needs the webhook pointed at the box. **Outbound** (course-confirm, booking-confirm, the 08:15 reminder) is a push on the channel token and needs **no** webhook (Porter, 2026-08-30). ⇒ most of the customer's notification list is testable without a webhook window.
- 🔴 **Two teachers are linked on `uat` and they are REAL** ⇒ an outbound "test" fired from `sid` reaches real people if the boxes share a channel token. **Nobody fires an outbound LINE test from `sid` until that is answered** (Porter, 2026-08-30).
- **LINE reach on 2026-08-30: 2 of 20 teachers, 0 of 180 parents** (`PROJECT-STATUS.md`, surfaced by Porter). ⇒ every parent notification built to that date reached nobody on `uat`.
- 🔴 **A human admin LIVES IN the LINE OA account and answers customers by hand.** Owner: *"admin เขาจะสิงไลน์นั้นแหละ ไปตอบลูกค้า"* (owner, 2026-08-30). **Everything built until then assumed a bot talking to parents with nobody else in the thread.** He is not asserting the design is wrong (*"อาจจะ"*) — he refuses to assume it is right. ⇒ a design conversation precedes scope on the real-OA work.
- **REQ-050's check-in message today names the child and the session time ONLY** — the remaining count, program, teacher and expiry appear in no check-in message anywhere in the product (Porter, gap analysis, 2026-08-30).

## Schedules

- ⚠️ **Triggering the daily reminder MANUALLY before 08:15 silently eats that day's reminders for real families on `uat`** (carried as a known-unverified item in `PROJECT-STATUS.md`, surfaced by Porter 2026-08-30).
- **The 08:15 daily job carries TWO of the customer's six requested notifications, and it has never been watched actually delivering** (Porter, 2026-08-30). "Built" is not "working".

## Product limits

- 🔴 **There is NO rental-reversal endpoint.** QA's 6 rental rows stay on `sid` permanently; only the raw keys can be handed over if the owner wants them removed by hand (Porter, 2026-08-30).
- **Students cannot be deleted** — QA reused an existing fixture *"precisely because students cannot be deleted"* (Tanya, 2026-08-30).
- **A CLI run on a server is not available to QA at all** — running `teacher-subjects:link-all` would make Tanya an operator of `sid`, which her charter forbids. It needs the owner (Tanya, 2026-08-30).
- 🔴 **`bun test` in `smart-scheduler-back` reaches the LIVE `sid` DB** through `eligible.route.test.ts:13` (Bun loads `.env` before the `??=`). It was refused at auth (`28000`) on Sober's machine — **but on a whitelisted machine the same command reads real rows, and it is the command every DoD tells an engineer to run** (Sober, 2026-08-30 entry, restated 08-31).
- **DB access is IP-whitelisted; a `uat` remote-DB whitelist line was opened for the LINE work and was still open** (Porter's outstanding list, 2026-08-30).
- ⚠️ **The `sm-jobs` secret is in PLAINTEXT and committed to git** (`PROJECT-STATUS.md`, surfaced by Porter 2026-08-30).

## Telling the two boxes apart

- 🔴 **The customer's own STAFF are trialling on `sid`.** Owner: *"ช่างมันเถอะหน่า ลูกค้าลองใช้ที่ sid อยู่ หมายถึงลูกค้าเรานะ ไม่ใช่ลูกค้าของลูกค้า"* (owner, 2026-08-30). ⇒ `sid` is not a private team box; the people evaluating the product are on it.
- **Scheduled tasks / scripts can be run on one box and not the other, and one being a certain way proves nothing about the other** — the rental ledger had 6 rows on `sid` and ZERO on `uat`, which says the QA footprint never reached the customer AND says nothing whatever about rentals working on `uat` (Porter, 2026-08-30).
- **A program was added to both boxes with NO code change and NO redeploy** — `subjects:add --group bike-skate` plus a named-list link INSERT, scripts against the DB, no build, no `pm2 restart` (Porter's own record of running it, 2026-08-29, restated 2026-08-30).

## Terminology

- 🔴 **"ลูกค้า" in the owner's mouth means OUR customer — the school — not the school's parents.** He had to correct the team on it explicitly: *"หมายถึงลูกค้าเรานะ ไม่ใช่ลูกค้าของลูกค้า"* (owner, 2026-08-30). A true number about the wrong population yields a false conclusion.
- 🔴 **`DONE` on the board means BUILT, not WORKS** (Porter, 2026-08-30, on TASK-220).
- **`ระยะที่ 1` = the frontoffice phase (the owner's REQ-001…, 12 items). `REQ-BO-00x` = the backoffice block.** The owner's own numbering is `REQ-001…012 · REQ-BO-001…006 · FIX-001…007` (2026-08-30).

## Business rules the owner set

- 🔴 **The owner's ORDER of work, his words: *"เก็บหน้าบ้านอ่ะ ให้จบ"* and *"หลังบ้าน ไว้หลังเลย ทำหน้าให้จบก่อน"*** — finish the frontoffice; the backoffice waits (owner, 2026-08-30). **Final order set that day: REQ-005 → REQ-013 → REQ-015 → REQ-014 → REQ-004 → the `REQ-BO` block.**
- **His standing sequencing rule: the backoffice phase waits for a STABLE frontoffice** (owner, restated 2026-08-30).
- **REQ-004 (rental) is deliberately parked to LAST by him: *"ยังไม่ต้องหยิบมาคุย"*** (owner, 2026-08-30) — not blocked, not queued, deliberately not discussed yet.
- **The customer reported rentals are wrong and want them back for rework: *"เรื่องของเช่า rent ยังไม่ถูกนะ ลูกค้าแจ้ง นำกลับมาแก้"*** (owner relaying the customer, 2026-08-30) — **on the same day the ledger came back matching SPEC-031 exactly.** The mechanism was verified; the customer is reporting the outcome. **What is wrong was not known.**
- **Rental prices as stored on `sid`, matching SPEC-031 (minor units):** set `20000` · ride `15000` · helmet `5000` · pads `5000` · ride3 `45000` (= 3 h × ฿150). Every row carries `revenue_kind = RENTAL` (owner's DATA REQUEST result, 2026-08-30).
- 🔴 **An อื่นๆ (OTHER) booking is flexible per instance** — charged or not, consumes or not, with or without a student (owner's REQ-005 answers, 2026-08-30).
- **The price of an อื่นๆ booking can be EITHER a typed amount OR a catalogue item — the owner's own third option (ค), which was not on the list offered to him** (owner, 2026-08-30).
- **A studentless booking is named by the ADMIN TYPING A TITLE, and that title is the display name everywhere — the system must never substitute "อื่นๆ" for it** (owner, 2026-08-30).
- **Voucher program exclusions still hold: `Onewheel E-Skate` and BOTH `Balance Play` variants are not voucher-eligible** — 16 of 19 programs appear in the voucher picker (REQ-027's rule, verified running 2026-08-30).
- **Course leave quota is set by course SIZE, from the customer's card: size 4 → 1 leave · size 6 → 2 · size 10 → 3** (`Surfskate & Inline Skate` priced 4,790 / 6,490 / 9,790 at 4/6/10, *"price includes VAT"*, quota 1→wk5 · 2→wk8 · 3→wk13) (Tanya, byte-for-byte against the customer's `bike-skate` card, 2026-08-30).
- **A 1 HR on `Bike & Scooter` prices at ฿1,390 and saves** — the 1-hour gap REQ-058 flagged is closed in the running system (Tanya, 2026-08-30).

## Working agreements

- 🔴 **`teacher-subjects:link-all` is `sid`-ONLY. It can never unlink. `uat` gets a program by an owner-approved NAMED LIST** (policy in force 2026-08-30; the revoked wording *"don't forget to run on both sid and uat"* was removed from the script's header AND its `console.log`s, and a test now greps to keep it gone — Jason/Sober, 2026-08-30).
- 🚫 **No box-detection guard was added to that script and none may be** — a test asserts the absence of `process.env.NODE_ENV`, `DATABASE_URL`, `isUat`, `--force`, so the ruling cannot be quietly undone (Jason, 2026-08-30).
- 🔴 **Reading a query result is NOT a verdict. Only QA sets `TEST_PASSED`** (`PROTOCOL.md`, applied by Porter against himself, 2026-08-30).
- **`board.md` has a 40 KB hygiene line; compaction is Marie's, and a role over the line FLAGS rather than trims** (Porter, 2026-08-30).
- 🔴 **Append-only means APPEND: create the day's log file only if it does not exist.** Porter wrote an entry that day, a later session **recreated the file with the header and destroyed it** (Porter, 2026-08-30, restating the lost content rather than losing it twice).
- **`PROJECT-STATUS.md` is the resume-here file and comes BEFORE the board in Porter's startup ritual, not after the log** — he answered "what is outstanding" twice without reading it and the owner had to point him at his own file (Porter, added to his ritual 2026-08-30).
- 🔴 **Owner: *"พิมพ์ไม่รู้เรื่องเข้าใจยาก"*** ⇒ standing rule written into `PM.md` §"How Porter writes to the owner": **under 15 lines, plain Thai, lead with what he must decide, one decision at a time, reasoning stays in the files** (owner, 2026-08-30).
- **The owner's customer-facing list lives in `ai-worker/OWNER-LIST.md`** — created 2026-08-30 verbatim from his own numbering and mapped to board numbers, because until then it existed only in `../project-docs/2026-08-23-timeline-current-picture.md`, stale on status since 08-23, and **he had to re-explain his own team and numbering to the PM** (Porter, 2026-08-30).
- **Nothing is started against a machine summary of speech** — new scope arriving mid-lane is captured as DRAFT **without acceptance criteria**, deliberately, because every AC would encode a guess (Porter, 2026-08-30, on REQ-076/077).

## Unsorted — 2026-08-30

- **The customer's notification list arrived and 5 of its 6 items already existed** — the only new work was the parent's check-in message (Porter, 2026-08-30). The deliverable on that REQ is **linking, not messages**.
- **A pause/resume capability for 1 HR / Voucher / 1st Trial does not exist** (raised as REQ-076, 2026-08-30). Its sharp question: **a 1 HR / 1st Trial bills at day-end, so "pause after it was attended" is a money decision, not a UI state.**
- **A public no-login QR check-in that BURNS A PAID SESSION is an unanswered owner risk decision**, alongside the admin code (*"the only one today is the shared static `229`"*) and the rate-limit model (**nothing exists in the codebase**) — all three held by Porter, none of them design problems (Porter, 2026-08-30).
- 📌 **The shared static admin code in the product today is `229`** (Porter, 2026-08-30).

---

## ⚠️ CONTRADICTIONS — reported, not resolved

### C4 — What does day-end do to a booking nobody marked? (same as C1 in the 08-23 file)
- **(A) `NO_SHOW`,** consuming the session and posting no revenue — Porter, from `jobs.service.ts:47–61`, **2026-08-23**.
- **(B) It auto-attends.** *"day-end auto-attends it like everything else"* — Porter, relaying the owner's REQ-005 answers, **2026-08-30**; and `SYSTEM-FACTS.md` today: *"The day-end auto-attends what it is designed to pick up."*
- Recorded in both files because the 08-30 entry is one of the two sides. **Not resolved.**

### C5 — Where does the LINE webhook point?
- **(A) `uat`, permanently, borrowed briefly at night.** Owner, **2026-08-30**: *"webhook point ไป uat ค้างไว้รอตลอด … จะเทสไลน์ เราจะค่อยปรับกลับมา แปปๆ ตอนดึกๆ"* — owner-only, late-night window, then flipped back.
- **(B) `sid`, permanently, since 2026-09-01.** `SYSTEM-FACTS.md` (owner, 09-01) — *"It used to live on `uat` and be borrowed at night; that arrangement is over."*
- These are consistent as a timeline and SYSTEM-FACTS already says so; I list it here only because **(A) supplies the owner's verbatim words and the date the old arrangement was still in force**, which the existing line does not carry. See REFINES below.

---

## ⚠️ REFINES EXISTING — same fact, earlier date or the missing attribution

- **"Write to him short. Under ~15 lines, one decision at a time."** SYSTEM-FACTS carries this with no attribution and no date. **Origin found:** the owner said *"พิมพ์ไม่รู้เรื่องเข้าใจยาก"* after three long answers in a row, on **2026-08-30**, and Porter wrote the rule into `PM.md` §"How Porter writes to the owner" that day.
- **"Quote the owner's OWN requirement numbers to him … the mapping is in `OWNER-LIST.md`."** SYSTEM-FACTS states the rule. **Origin found:** `OWNER-LIST.md` was created on **2026-08-30** because the owner had to re-explain his own numbering to the PM; his list then read `REQ-001…012 · REQ-BO-001…006 · FIX-001…007` (SYSTEM-FACTS now says `REQ-001…016`, i.e. it grew after this date).
- **The LINE webhook's pre-09-01 arrangement.** SYSTEM-FACTS says only *"It used to live on `uat` and be borrowed at night."* **The owner's verbatim words and the date it was still policy:** *"webhook point ไป uat ค้างไว้รอตลอด … จะเทสไลน์ เราจะค่อยปรับกลับมา แปปๆ ตอนดึกๆ"* (owner, **2026-08-30**) — owner-only, and it was the concrete shape of the LINE blocker QA had carried since 08-04.
- **"There is NO student-delete anywhere in the product."** SYSTEM-FACTS states it undated. **Earlier attribution:** Tanya, **2026-08-30**, reusing a fixture *"precisely because students cannot be deleted."*
- **"2 real teachers are linked on `uat`. They must never receive a rehearsal message."** SYSTEM-FACTS states it. **Earlier date + the reasoning:** Porter, **2026-08-30** — and the open question that produced the rule was *"does `sid` share `uat`'s LINE channel/token?"*, asked and unanswered on that date. The reach figures that day were **2/20 teachers, 0/180 parents**.
