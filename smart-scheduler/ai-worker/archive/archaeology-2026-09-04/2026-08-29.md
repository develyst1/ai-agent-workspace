# Archaeology extract — `smart-scheduler/ai-worker/log/2026-08-29.md` (108,103 bytes, 1362 lines, read in full)

> Extracted for Marie, ORDER 3. Format matches `SYSTEM-FACTS.md`. Nothing merged; nothing resolved.
> 🔴 **Read `## ⚠️ CONTRADICTIONS` first — the `end-of-day` 18:30 claim SYSTEM-FACTS attributes to this
> day is not in this log, and this log says 23:30 twice on this date.**

---

## Schedules — the jobs, and when they really run

- **`sm-daily-reminder` runs at 08:15**, and registering it is a **Windows `schtasks` OPS step on each box, not a deploy** (Sober's handoff + Porter's runsheet, 2026-08-29).
- **`daily-reminder.ps1` has to be copied to the `uat` box separately, because the secret differs per box** (Porter's runsheet, 2026-08-29).
- **First proof the Windows schedule fires by itself:** a `job_runs` row at **08:52:16 · 08:52:12 · 08:15:01** — *"the 08:15:01 row is the Windows task firing by itself — the first time we have proof the schedule works, not just the endpoint"* (owner-run query, reported by Porter, 2026-08-29).
- **`sm-daily-reminder` was registered at 08:15 on `uat` on 2026-08-29**, and *"once `sm-daily-reminder` is registered, LINE-linked teachers start receiving a message every morning at 08:15"* (Porter, 2026-08-29).
- 🔴 **THE REMINDER GUARD EATS THE DAY — an operational trap, not a defect:** the job is idempotent per business date, so **anyone who triggers it manually before 08:15 silently cancels that day's reminders for real families. No error, no sign.** Observed on `sid` on 2026-08-29 (*"every row today reads `already-sent` because our own 05:08 test consumed the day's single attempt"*). ⚠️ **Do not trigger it manually on `uat` in the morning** (Porter, 2026-08-29).
- ⚠️ **The obvious fix is the bug Jason had just removed** — keying the guard on `sent` instead of `attempted` makes the job **re-run all morning on exactly the days it reaches nobody**. **Sober's decision, 2026-08-29: idempotency moves to per-(recipient, day)**, like the day-end's `rev:<id>` key — cut as TASK-218, low priority, still unbuilt at end of day.
- **Every scheduled job now inserts a `job_runs` row on every run** — the guard gates the SEND, never the RECORD, so *"ran, nothing to send"* and *"never ran"* are distinguishable. A re-run writes `{attempted:false, sent:0, reason:"already-sent"}`. **`month-reset` wrote no row at all until this day; it does now.** Asserted as a cross-file property test across all three service files (TASK-209, 2026-08-29).
- **`sent` in `job_runs` is the DELIVERED COUNT, with `skipped` beside it; `attempted` is the separate fact that the job ran** — before this day a run that reached zero people recorded `sent: true` (fixed 2026-08-29).
- **`0027` applied WITHOUT a `seed-ledger` round because its `when` sat above the ledger's newest row** ⇒ 📌 **the recurring migration skip is not every migration — only ones whose timestamp falls below the ledger's newest** (Porter, observed 2026-08-29). **4 of the last 5 migrations needed a seed-ledger round; `0027` was the exception.**
- **A green migration verify reads `journal N · ledger M · witnesses N ✅`** — observed `27 · 37 · 27` then `28 · 38 · 28` on `sid`, and `28 · 37 · 28` on `uat` (2026-08-29).

## Product limits

- **The `booking_type` enum has NO `OTHER`** (`schema.ts:40`) — adding the owner's "อื่นๆ" booking type is a migration plus a label plus a cell token, not a config change (Sober, grounded from code, 2026-08-29).
- **There is NO rental-reversal endpoint** — a posted rental movement cannot be retired; QA's 6 rental movements had to be left on `sid`'s `bo.movement` with identifiable keys (observed, 2026-08-29).
- **NO API exposes `bookings.cancel_reason`** — the machine reason is deliberately separate from the human `note`, and reading it requires SQL (a DATA REQUEST) (observed, 2026-08-29).
- **QA cannot trigger the scheduled jobs at all:** `/jobs/daily-reminder` → **404**, `/api/jobs/daily-reminder` → **401**, and **`INTERNAL_JOB_SECRET` is not in the QA access file.** ⇒ **every job run is the owner's hands** (observed, 2026-08-29).
- **QA cannot read `bo.movement` / `bo.item`** — no read API and no backoffice credentials; anything in the money ledger is a DATA REQUEST (observed, 2026-08-29).
- **dayjs' Thai locale has no three-letter weekday form** — `ddd` returns the FULL Thai name (จันทร์, อังคาร, พฤหัส), which is *longer* than `dd` (จ. อ. พ.). ⇒ **"abbreviate the weekday in both languages" is met by different tokens: `ddd` in English, `dd` in Thai** (Fern, checked before changing, 2026-08-29).
- **A COURSE session's cancel is deliberately NOT offered on the booking** — it lives in the plan (TASK-105), where the re-owe consequence is visible. **A course cancel is a reschedule that re-owes a make-up, a different act with its own rules** (Sober/Fern/Jason, held three times, 2026-08-29).
- **The DB no longer constrains course size** — `0027` replaced `course_size_chk (size in (4,6,10))` with `course_size_sanity_chk (size 1..100)`; **the APP is the size authority** (`decideImportSize` for imports, `isCourseSize` for sales). *"The card belongs where the owner can change it, not in the one place that needs a migration to move."* ⚠️ **Relaxing the DB did not make off-card courses sellable — the sale path is untouched and a test pins that** (2026-08-29).
- **The `sm-jobs` secret is in git** — a known open item as of 2026-08-29, unfixed.

## LINE

- 🔴 **`uat`: 0 of 180 parents have a LINE link** ⇒ **the entire parent-notification half of the product reaches nobody on the customer's box.** **Owner's ruling — a KNOWN STATE, not a defect:** *"uat ลูกค้าแค่ยังไม่ใช้เฉย ๆ ปล่อยไปอย่าไปยุ่ง"*. **The linking flow exists and works; no family has used it. That is the customer's own adoption, not ours to push.** ⚠️ **No broadcast, no prompt, no nudge to 180 families, unless the owner asks** (owner, 2026-08-29).
- 🔴 **`uat`: 2 of 20 teachers are LINE-linked.** Porter flagged this as *different* — **teachers are the customer's STAFF, not their customers**; asking 18 staff to type `สมัคร` is a five-minute ask. **The owner parked it too:** *"ยังไม่ผูกช่างมันไปเถอะ"* (owner, 2026-08-29). ⚠️ Consequence on the record: everything built across 2026-08-28/29 runs every morning and is seen by almost nobody.
- **Measured reach of one real `uat` reminder run:** `sessions 68 · teachers 20 · parents 49 · **sent 2** · skipped 67` — 2+67 = 69 = 20+49, every person accounted for (2026-08-29).
- **A LINE message's LAYOUT is only judgeable on a phone** — repeatedly stated, and the daily-reminder message was unseen by any human until it was finally rendered on a device on 2026-08-29 (Porter, 2026-08-29).
- **One payload object is sent to teacher and parent for a booking confirmation** ⇒ **a note typed on a booking now reaches the PARENT as well as the teacher.** ⚠️ **If a staff note is ever meant to be teacher-only, that is a second payload and it must be decided BEFORE somebody writes something internal in that box** (Jason's open Q1, 2026-08-29 — never answered by the owner).
- **An empty note line is a defect, not a cosmetic issue** — *"หมายเหตุ:"* with nothing after it reads as a note the teacher **failed to receive**. The line is omitted for `undefined`, `null` **and `""`** (2026-08-29).

## Telling the two boxes apart

- **`uat` is the customer's box with real data** — 2026-08-29 screen counts: **Active 165 · Paused 0 · Completed 18 · Expired 0 · Cancelled 18 = 201** course packages · **180 parents** · **20 teachers** · **532 `PENDING` bookings**.
- **`sid` is where QA writes; `uat` is outside QA's charter entirely** — a `uat` gate is signed as *"evidence-from-`sid` on an identical build, not a Tanya sign-off on the customer's box"* (Porter, 2026-08-29).
- ⚠️ **Writes on `sid` are intermittently refused mid-run; retry** (Porter to QA, 2026-08-29).
- 🔴 **`teacher-subjects:link-all` is now DANGEROUS on `uat` and is for `sid` only.** On `uat` a new program must be linked to a **named list** instead. **The tool can never unlink — pruning is manual work in the product**, so a wrong `--commit` is a silent, hard-to-reverse erasure of a deliberate roster decision (Porter, POLICY CHANGE, 2026-08-29). See CONTRADICTIONS C2.
- 📌 **Per-row dry-run output is what makes an outlier visible** — `Pop: +5/=14 · DC: +16/=3 · everyone else: +1/=18` caught the restricted roster. **A summary line ("46 links will be created") would have looked entirely normal** (Porter, 2026-08-29).

## Business rules the owner set earlier and that were never written down

- 🔴 **`MAX_WEEK = size + leaveQuota`, at EVERY size — the owner's own rule**: **4+1=5 · 6+2=8 · 10+3=13** (owner, recorded by Porter 2026-08-29). ⇒ **"max week" is never a stored field and never a second table** — it is derived. *(This confirms the "6 → week 8" constant that Sober flagged as an unconfirmed guess on 2026-08-02.)*
- **Card course sizes are 4 / 6 / 10. Off-card sizes ARE allowed** — the owner's design for the `เรียนอยู่แล้ว (ย้ายข้อมูล)` dialog is: **default = the fixed 4/6/10 choice, plus an explicit "off-card" option revealing a free size AND a manual leave-quota field.** **ONE extra input, not two** (owner, 2026-08-28, recorded 2026-08-29).
- **An import expiry a human actually TYPED is meaningful and must be honoured** — it may encode an agreement no rule of ours can see. **This is why 164 imported course expiries were kept out of the FIX-007 repair** (owner's ruling, restated 2026-08-29). ⚠️ **What was killed was the form's `today + 2 months` default — a number from no rule.** The server computes the default; a typed date wins.
- 🔴 **"popup ทุกที่ที่ควรมีการ confirmation"** — the owner set a **principle, not a per-button call**, and in doing so **overruled Porter's objection to a `มาเรียน` popup** (owner, 2026-08-29).
- **The criterion that makes "ควรมี" decidable — confirm when the action is at least one of: (1) hard to undo, (2) reaches a human, (3) moves money.** **Do NOT confirm** navigation · filtering · the display toggle · opening a form · a note/badge edit · a plain move — *"a popup on a harmless action teaches staff to dismiss popups, which costs us the ones that matter"* (Porter's criterion under the owner's principle, 2026-08-29).
- 🔴 **Never tax the `มาเรียน` click.** **On 2026-08-23, 15 real sessions went `NO_SHOW` because staff pressed `confirm` and never pressed `มาเรียน`.** ⇒ its confirm is deliberately **the cheapest dialog in the app** — one line, no reason field, primary focused, **click → Enter → done**, Esc dismisses (owner's ruling + the team's mitigation, 2026-08-29).
- **A dismissed confirm must never read as consent** — Esc and the backdrop both settle to `false` (2026-08-29).
- **There is ONE cancellation-reason enum across the whole product: `ADMIN_ERROR` · `CUSTOMER_CANCELLED` · `PROGRAM_CHANGED`** — the owner's own three reasons **แอดมินซื้อผิด · แม่ไม่มาแล้ว/ยกเลิก · เปลี่ยนกิจกรรม**, identical for a course cancel and a booking cancel, so *"find every admin-error cancellation"* is one query across both (owner, mapped by Porter 2026-08-29; verified queryable on `sid` the same day).
- 🔴 **RECORD THE REASON; DO NOT BUILD REFUND OR REVERSAL LOGIC.** *"The owner has twice refused to let us move money as a side effect, and twice insisted the system must not be silent about what it did."* **Reversing revenue is a finance decision and stays a BACKOFFICE action — no reversal button in the frontoffice** (Porter, held on REQ-036 and again 2026-08-29).
- **Revenue timing, enumerated:** a **`FIRST_TRIAL`** and a **`SINGLE_SESSION`** post revenue **at day-end when `ATTENDED`**; a **VOUCHER** and a **COURSE** post **at sale**. ⇒ cancelling a 1HR/trial before day-end posted nothing; cancelling a voucher session cannot un-post it (2026-08-29).
- 🔴 **Cancelling a booking does NOT reverse its posted revenue and says nothing about it** — the schedule is fixed, the freelance hold released, the quota returned, **and the money sits in the books forever until a month-end number does not add up.** The owner asked *"what if the money already went in?"*; the answer ordered was **a warning naming the real amount, detected via the sale's `idempotencyKey = 'rev:<bookingId>'` — never inferred from status or date** (owner's question, Porter's order, 2026-08-29; not built that day).
- **A first trial BILLS ฿1,390** — the day-end job selects `bookingType IN ('FIRST_TRIAL','SINGLE_SESSION')` when `ATTENDED` and posts `first-trial`. **A trial is a standalone session that moves money, exactly like a 1HR** (Jason, checked at source, 2026-08-29).
- **Cancelling a DELIVERED / `ATTENDED` session is allowed BY DESIGN** (TASK-105) precisely so a mis-marked attendance can be undone — the gate is only `status !== "CANCELLED"`, never the attendance state (Porter, grounded in `BookingModal.tsx:287`, 2026-08-29).
- 🔴 **A raw `UPDATE bookings SET status='CANCELLED'` is NOT equivalent to pressing the cancel button** — the service path also **releases the teacher's freelance hold** (`reconcileBookingHolds`; `CANCELLED` is a releasing status). **Hand-SQL leaves a freelance ceiling drawn down for a class that no longer exists — money wrong, silently.** 📌 **The owner asked for a one-off SQL fix on 2026-08-29 and Porter talked him out of it** (2026-08-29).
- **Bulk-confirming arms day-end auto-attend for everything it touches** — on `uat` that meets **532 `PENDING`** bookings. **REQ-070, the owner's own design, acknowledged by him three times** (2026-08-29).
- **Rental prices, VAT-inclusive, four codes:** **`rental-set` 200 · `rental-ride` 150 · `rental-helmet` 50 · `rental-pads` 50**. A rental goes through the **existing `recordSale` path — no new money mechanism** — tagged **`metadata.revenueKind = "RENTAL"`**, which sits on the **`bo.item`**, not the movement row (SPEC-031, verified on `sid` 2026-08-29).
- 🔴 **A rental failure must be VISIBLE — the post IS the event, not downstream bookkeeping.** This is the one place the best-effort default is wrong: an unknown code returns **400 `"รหัสอุปกรณ์เช่าไม่ถูกต้อง"`**, and a known code whose post fails throws **502 `RENTAL_NOT_POSTED`**, never a silent 200 (SPEC-031, verified 2026-08-29).
- **A rental's entry point is BOTH** — a standalone walk-in button and a booking add-on button; **the money model is identical either way** (the owner's unanswered Q2 turns out to be already answered by the build; observed 2026-08-29).
- **Rental hour bounds:** max **24**, integers only, non-positive rejected — all 400s with a message, never a 500 (observed, 2026-08-29).
- 🔴 **`REQ-005` "booking type OTHER (อื่นๆ)" — the owner's answers, ON RECORD AND NOT TO BE RE-ASKED:** **"other" = a flexible booking the admin configures per instance** — **charged or not · consumes a course/voucher or not · with or without a student**. **Day-end: the same rule as everything else — unmarked ⇒ auto-attend at 23:30** (owner, 2026-08-29). ⚠️ See CONTRADICTIONS C1 for the time.
- **Still unanswered on REQ-005 and explicitly NOT to be invented:** (a) **if charged, what price** — a typed amount vs a catalogue item; (b) **what names the cell and the teacher's LINE line when there is no student** (open at end of 2026-08-29).
- **REQ-005 is PARKED, not dropped** — owner: *"งานขนาดระดับนึง ก็ปล่อยไปก่อน มา deploy เรื่องนั้นก่อน"* (owner, 2026-08-29).
- **The owner chose REQ-005 over the six `REQ-BO` backoffice items** as the next real lane (owner, 2026-08-29). **The six `REQ-BO` items — link programs · dashboard · freelance ceiling · teacher salary · course deduction from the frontoffice · cancel 1HR/Voucher from the back office — are untouched and unplanned, and are the largest thing left on his list** (Porter, 2026-08-29).
- **"Weekly course" is dead as a label — the booking type is "Course" / "คอร์ส"** (owner's REQ-010/REQ-075, shipped 2026-08-29). ⚠️ **`course.formTitle` still read `สมัครคอร์สรายสัปดาห์` at end of day** — the rename is half-done and the owner will see it (Fern's Q1, 2026-08-29).
- **English weekday abbreviations are `Mon/Tue/Wed`, not `Mo/Tu/We`** (owner's REQ-010, shipped 2026-08-29).
- **`1st Trial` has been a DELIBERATELY INACTIVE subject since REQ-065** — `subjects:add` counts all **20** programs, `link-all` counts the **19 active** ones (observed, 2026-08-29).
- **A new program "Surfskate & Skateboard" exists as of 2026-08-29**, added with `subjects:add --group bike-skate`; **prices come from the group, so a new program is sellable across course / 1HR / 1st Trial / voucher by construction.**
- 📌 **6-session Surfskate & Skateboard = ฿6,490.** The customer first quoted **฿6,790**; Porter asked instead of "correcting" the system, **and she came back with ฿6,490 — ours was right.** ⇒ **ask before changing a price the system already holds** (2026-08-29).
- **`Expired` on `uat` is 0, and the two families that once showed as expired were never expired** — *"our own off-by-one invented them"* (confirmed after FIX-007, 2026-08-29). 📌 Porter had told the owner to phone those two families and withdrew it; **nobody called.**
- **A note on a booking must reach the teacher in LINE** — the owner's REQ-007 is explicit: *"ให้มีช่อง Note ... **และส่งถึงครูในไลน์ด้วย**"*, and **the useful half is the single-booking confirm**, because a note like *"คุณยายมารับตอน 16:00"* matters to the teacher **on the day of that class** (owner's requirement, closed 2026-08-29).

## Working agreements the owner has stated

- **Owner: added work must be finished before `uat`** — *"งานเพิ่ม ต้องทำให้จบก่อนขึ้น uat"* ⇒ **the `uat` deploy is a single, deliberate, one-shot deploy that waits for the whole gating set** (owner, 2026-08-29).
- **Deploys go `sid` first, then `uat`; migrations are applied migrate-before-code; the owner runs every migration and every deploy himself** (standing, exercised repeatedly 2026-08-29).
- **The owner's 2026-08-19 rule: a written UAT-GATE block before any `uat` deploy — both names, and what was NOT tested, stated plainly** (owner's rule, applied 2026-08-29).
- 🔴 **Use the LABEL ON THE SCREEN, never a term the team coined.** The owner did not recognise his own request when it was called "ฟอร์มนำเข้า": *"form นำเข้าคืออะไร ไม่น่าจะมีที่ฉันสั่งนะ"* — he had ordered it from the **`เรียนอยู่แล้ว (ย้ายข้อมูล)`** dialog. **The anchor is the button text, and it belongs in the task files** (Porter's failure, recorded 2026-08-29).
- 📌 **The owner's list numbers and the board numbers are different namespaces, and they have already been collided once** — on this day REQ-006→REQ-073 · REQ-009→REQ-074 · REQ-010→REQ-075 (Porter, 2026-08-29).
- **Orders live in TODAY's log file. A previous day's file is CLOSED for orders** — three days of appending to a stale file had Sober reconstruct priorities from it and park live work (Porter, 2026-08-29).
- **Write to the owner SHORT.** Porter's own verdict that day: *"my entries have been 40 lines each in a 78KB file; the instruction gets buried in my own prose."* (Porter, 2026-08-29).
- **The owner verifies by USING the product, and that beats the team's inference** — he found REQ-007's missing half by typing the note `asdawdwada` and reading the message that came back without it (2026-08-29).
- **A screenshot that carries its own control case is the standard of proof** — the note-bearing booking rendered `หมายเหตุ: awdawdaaaa` while two earlier note-less confirms showed no line at all, both branches in one image, *"nobody had to be believed"* (2026-08-29).
- **QA retires her `sid` fixtures with cancel + `ADMIN_ERROR`** (standing practice, 2026-08-29).

## ⚠️ CONTRADICTIONS

**C1 — 🔴 `end-of-day` at 18:30 vs 23:30, on this exact date.**

- **`SYSTEM-FACTS.md` says:** *"| **`end-of-day`** | 🔴 **18:30** | **The OWNER changed it himself**, 2026-08-29 (was 23:30 until 08-28) |"* and *"🔴 `end-of-day` at 18:30 is DELIBERATE and CORRECT."*
- **This log, 2026-08-29, contains NO mention of 18:30, no mention of the owner changing any schedule, and no mention of an `end-of-day` job by name.** (Grepped: `18:30`, `end-of-day`, `endOfDay`, `sm-end` — zero hits.)
- **What this log DOES say, twice, on 2026-08-29:**
  - Porter recording the owner's own REQ-005 answers: *"**Day-end: the same rule as everything else** — unmarked ⇒ auto-attend at **23:30**."*
  - Porter, on a live `uat` booking: *"that booking is `ATTENDED` `FIRST_TRIAL` **today**, so **day-end posts ฿1,390 at 23:30** unless it is cancelled first."*
- **Not resolved.** Either the change happened outside this log, or the 2026-08-29 attribution in SYSTEM-FACTS is wrong. Marie's call.

**C2 — "every teacher can teach every program" vs "ตั้งใจจำกัด".**

- **The earlier side, on the board and in the script's own header:** *"The board records the owner's **REQ-058 decision** as **'every teacher can teach every program'** — that is *why* `link-all` exists, and the script's own header says *'open-by-default is the trade-off the owner accepted'*."*
- **The later side, owner, 2026-08-29:** a `uat` dry run showed `Pop: +5 / =14 · DC: +16 / =3 · (everyone else: +1 / =18)` — **Owner: *"ตั้งใจจำกัด"* — DC and Pop are deliberately restricted.**
- Porter frames it as a policy CHANGE (*"That is no longer true"*) and notes **the script's own comment now asserts a policy that has changed**, which he calls worse than no comment. **Both sides recorded; not resolved here.**

## ⚠️ REFINES EXISTING

**1. `daily-reminder` 08:15 — "stable since 08-30".**

- SYSTEM-FACTS: *"| `daily-reminder` | **08:15** | `job_runs`, stable since 08-30 |"*
- **This log dates it one day earlier and adds who and how:** the 08:15 task was **registered on both boxes on 2026-08-29** as an ops step, and its **first proven self-firing run is the `08:15:01` `job_runs` row of 2026-08-29** (owner-run query, Porter). **It also names the per-box catch: `daily-reminder.ps1` must be copied to each box because the secret differs per box.**

**2. "Scheduled tasks live in `C:\sm-jobs\*.ps1` on each server separately."**

- SYSTEM-FACTS states the location; this log supplies the operational reason, on 2026-08-29: **the SECRET differs per box**, so the script must be copied to `uat` rather than assumed present — and separately, **the `sm-jobs` secret is in git** (open item).

**3. "`uat` ≈ 201 course_packages · `sid` two digits" (observed 09-01).**

- **This log carries the same `uat` order of magnitude three days earlier, with its derivation:** on 2026-08-29 the customer's screen showed **Active 165 · Paused 0 · Completed 18 · Expired 0 · Cancelled 18 = 201**, confirmed as the unfiltered total. ⇒ **the ≈201 figure predates 09-01 and is a chip-sum, not a bare count.**

**4. "`job_runs.byBookingType` … ⚠️ Whether the day-end also *skips* `OTHER` when selecting what to auto-attend is **open with @Sober**."**

- **The owner answered the SELECTING half himself on 2026-08-29**, while specifying REQ-005: *"**Day-end: the same rule as everything else** — unmarked ⇒ auto-attend."* ⇒ **an `OTHER` booking is intended to be auto-attended like any other type.** ⚠️ **This says nothing about REPORTING** (`byBookingType`), which is the other half of the open question — and the sentence carries **23:30**, see C1.

## Unsorted — 2026-08-29

- **Roster detail behind the `link-all` policy change:** **Pop** teaches Surfskate **and** Skateboard **and every combined `Surfskate & …` program**; **DC** teaches Surfskate but **not** Skateboard and **no combined programs at all** ⇒ 26 teachers were linked to the new program and DC was excluded (owner-confirmed `SELECT`, 2026-08-29).
- 📌 **A query worth keeping — it answers *"which programs can't be booked?"* in one line** (Porter, 2026-08-29):
  ```sql
  SELECT s.name, s.active, count(ts.teacher_id) AS linked FROM subjects s
  LEFT JOIN teacher_subjects ts ON ts.subject_id = s.id
  GROUP BY s.id, s.name, s.active ORDER BY count(ts.teacher_id), s.name;
  ```
  *"I guessed twice and was wrong twice; the query answered it instantly."*
- **A DB `CHECK` can silently outlive the app rule it encoded** — `course_packages.course_size_chk (size in (4,6,10))` made every off-card import 500 **after a green preview**, because the app layer had been made the size authority and the constraint had not moved (root-caused 2026-08-29).
- 🔴 **A replaced constraint must get a NEW NAME or the migration is unwitnessable** — *"does `course_size_chk` exist?"* is true before **and** after an in-place replacement, so a box where the migration never ran looks identical to one where it did. `course_size_sanity_chk` exists only after `0027` ran (Jason, 2026-08-29).
- **`0027` is the only migration in that batch that DROPS anything** (a CHECK); leaving it applied under older code is harmless because the old code never creates off-card sizes. **Rollback for the whole 2026-08-29 batch is a code redeploy — there is no data migration to unwind** (Porter's runsheet, 2026-08-29).
- **A stale FE bundle passed for a fresh one once during this week** ⇒ **confirm a deploy from the SCREEN** — five status chips render, and a course card shows `ปกติ`, not `NORMAL` (Porter's runsheet, 2026-08-29).
- **`outbox == 1 on a LINE-linked recipient` is owner-parked and KNOWN-UNVERIFIED** as of 2026-08-29.
- **Migration numbers delivered to `uat` on 2026-08-29: `0025` (booking cancel reason) · `0026` (import off-card quota) · `0027` (size sanity check).** `0024` (course_dropped) was already there. *(Included only because the runsheet distinguishes what `uat` had from what `sid` had — history otherwise.)*
