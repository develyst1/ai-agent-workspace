# Archaeology extract — `log/2026-09-03.md` (84,590 bytes, 1,080 lines, read in full)

> Extracted for Marie, ORDER 3. Deduped against `SYSTEM-FACTS.md` as of 2026-09-03.
> ⚠️ **SYSTEM-FACTS' last section — "QA cannot test LINE. The owner is the only LINE-capable tester (established 2026-09-03)" — already carries the whole owner-is-the-hands / Tanya-is-the-verdict split, its planning cost, and the two ways out. NONE of that is re-extracted here.**
> Format matches SYSTEM-FACTS: one fact, one line, each carrying who said it and when.
> **Nothing here is merged anywhere — Marie places it.**

---

## LINE

- 🔴 **The MUTE is PER-CHAT, never account-wide** — proven by two accounts side by side on `sid`: one chat silent at 23:35 while another was served at 23:41 (observed, 2026-09-03). 📌 This was the failure mode Porter refused at design time and the specific reason LINE's **account-wide Bot/Chat switch was rejected as an implementation** — *"180 families going quiet because an admin replied to one of them"* was the disaster case.
- 🔴 **The premise the whole LINE bot rests on: the bot NEVER talks over a human.** While muted, everything typed stays silent — including command words the bot advertises itself (`นักเรียน`, `เมนู`, `เพิ่มนักเรียน`, free text), proven on the owner's device 2026-09-03 at 08:18 and again at 23:35.
- 🔴 **A muted chat must be re-openable BY TYPING, because customers use PC and PC has no rich menu** — the owner's own order: *"มันต้องกลับมาพิมพ์ได้ เพราะลูกค้าอาจจะใช้คอม ไม่มี rich menu … ไปคิดกันเอา"* (owner, 2026-09-03).
- **The re-open word is `เปิดเมนู`** — Porter's choice over the owner's proposed `เปิดคำสั่ง`, because it reuses vocabulary the parent already has (observed/decided, 2026-09-03). **It must stay DISTINCT from plain `เมนู`: while muted, `เมนู` is deliberately ignored — the un-mute must be a thing you CHOOSE, not a thing you reach for.**
- **The four doors out of a mute, as shipped (2026-09-03):** **button/postback → un-mutes, then runs normally** · **`เปิดเมนู` → un-mutes, shows the list, starts nothing** · **`ยกเลิก` → honoured, session cleared, MUTE UNCHANGED** (*"get me out" is not "let me back in"*) · **anything else, including plain `เมนู` → silence.**
- **Un-muting CLEARS any in-flight flow — coming back is a fresh start** — because the mute means a human took over, and whatever the parent was half-way through was superseded by that (shipped rule, 2026-09-03). ⚠️ Scoped to a chat that is **muted right now**, so a parent who was never muted and merely tapped a button does not lose their half-typed registration.
- **`สมัคร` is the ONLY way in and works from ANY state — including while muted, where it un-mutes** (shipped, 2026-09-03; TASK-231's rule extended).
- 🔴 **The reserved-command set is ONE list, and a reserved word can NEVER be stored as a child's name** — the refusal quotes the word back and names the way round it (*"ถ้าเป็นชื่อน้องจริง ๆ รบกวนแจ้งแอดมิน"*). A command added tomorrow is reserved the same day (shipped + proven on the owner's device, 2026-09-03).
- **The words the bot reserves/advertises, as of 2026-09-03:** `สมัคร · เมนู · ช่วยเหลือ · เพิ่มนักเรียน · นักเรียน · เช็คอิน · ลา · qr · ยกเลิก · ข้าม · เปิดเมนู · เสร็จ/done` (observed).
- ⚠️ **Two skip vocabularies coexist and disagree: `ไม่มี` / `-` skip but are NOT reserved; `เสร็จ` / `done` are reserved but do NOT skip; `ข้าม` is the advertised skip** (observed, 2026-09-03 — flagged as open, deliberately not merged because it would change shipped behaviour at the province step).
- **`ยกเลิก` exits at EVERY step and DELETES the draft, and every prompt says so** — a silent cancel is indistinguishable from a saved one (shipped + proven, 2026-09-03).
- 🔴 **A flow abandoned mid-way — by handoff or by cancel — writes NOTHING. No partial student record anywhere** (verified in the database by QA, 2026-09-03: two handoffs, `QA ทดสอบ` and `adwa`, left no record; 111 parents unchanged). ⚠️ This matters because such a record would be **unremovable**.
- 🔴 **Two unexpected replies hand the chat to a human** — a **rejection is an unexpected reply**; the counter fires on refused input, not only on unrecognised commands (shipped + proven, 2026-09-03).
- 🔴 **A DUPLICATE name is NOT a strike.** *"A strike is for 'I did not understand you', never 'I understood you and need more'."* Counting it would hand a two-child family to a human for having two children; the bot asks for a surname or nickname instead (rule stated 2026-09-03, confirmed in the product the same day).
- **One LINE account belongs to exactly ONE family** (`family_line_links_user_uq`), and the guarantee works in both directions: **a CLEARED account may re-bind to a DIFFERENT family** (proven 23:49) and **an already-linked account entering another family's phone is REFUSED** (proven 23:55) — *"บัญชี LINE นี้ผูกกับอีกครอบครัวหนึ่งไว้แล้วค่ะ หากไม่ถูกต้องกรุณาติดต่อแอดมิน"*. **That pair is the whole guarantee that a parent never sees another family's children.**
- 🔴 **That refusal deliberately reveals nothing: it does not name the other family and does not say whether the entered number exists in the system at all** — the same neutral-message discipline `REQ-051` specified for the walk-in page, applied here without anyone asking for it (observed, 2026-09-03). ⇒ **someone trying numbers learns nothing.**
- **Clearing a family's LINE link KEEPS students, bookings, notes and message history** — the admin dialog promises exactly that immediately before the red button, and it was verified true (all three students survived the clear) (observed, 2026-09-03).
- **The bot carries NO stale linked state: the moment a link is cleared it treats that account as a stranger** (*"พิมพ์ สมัคร เพื่อผูกบัญชี LINE"*) — nobody specified this, and the opposite (a cleared account still showing another family's children) would have been far worse than a broken button (observed, 2026-09-03).
- **The admin People screen re-checks the LINE link LIVE (*"Checking this family's LINE link…"*) rather than trusting the stored row, and states the link state before offering any destructive action** (observed, 2026-09-03).
- **The People search covers STUDENT names as well as family/phone** — `มิลล่` returns the family *and* its three student names (observed, 2026-09-03). 📌 Recorded because it is what makes a "no match" result evidence rather than a broken search box.
- **A rich-menu postback and a typed text message are DIFFERENT handler branches** — so a flow reached by typing does not prove the same flow reached by a button (observed, 2026-09-03).
- **The bot's copy exists in BOTH languages and there is a language toggle** — every new sentence (mute message, menu body) is added in both (observed, 2026-09-03).
- 🔴 **The owner uses HIS OWN live LINE account on `sid` as the running test fixture. It must not be cleared or disturbed by anyone else while he is testing with it** (established 2026-09-03 — QA declined to press `Clear LINE link` on it for exactly this reason).
- ⚠️ **A `NOT_TESTED` on a LINE AC is not a failure and must be recorded by name** — the two-state rich menu, leave, check-in, course view and every notification-regression AC were untested on 2026-09-03 and were recorded that way rather than inferred (working practice, 2026-09-03).

## Product limits

- 🔴 **The two-state rich menu (`ยังไม่รู้จัก` / `รู้จักแล้ว`) IS NOT PUBLISHED, and cannot be — it is blocked on the OWNER supplying two images.** `linkKnownRichMenu` is a silent no-op; what is live is the **OLD 6-cell parent menu from REQ-015/042** (read from source by @Sober 2026-09-02, restated 2026-09-03).
- **The `[เข้าใช้ระบบ]` entry button DOES NOT EXIST until those menus are published** — every entry to date has been by typing `สมัคร` (observed, 2026-09-03). ⇒ **the "only a button starts a flow" rule cannot be tested at all yet.**
- **Junk data the owner's own testing left permanently on `sid`: a student named `asda` on family `0900000092`** — unremovable, because there is no student-delete (observed, 2026-09-03).

## Schedules

- 🔴 **The 08:15 daily notification has been "known-unverified" on this project since before QA arrived** — nobody has ever observed it arrive on a phone (Tanya, 2026-09-03). It is one morning's observation away from being closed and it is the cheapest verification left.
- **The four existing notifications that share the message path the LINE work rewired: teacher schedule · course-confirm · booking-confirm · the 08:15 daily** (observed, 2026-09-03).

## Unsorted — 2026-09-03

- 🔴 **Terminology — ours vs the customer's: `บอท` and `คำสั่ง` are OUR words, not a parent's. Parents experience the thing as a `เมนู`** (Porter, 2026-09-03). ⇒ copy that says *"ถ้าต้องการใช้บอทอีกครั้ง"* is using the team's vocabulary on a customer.
- **A structural count is only evidence if it can tell CODE from PROSE** — `grep -o 'strikeOrPrompt('` (with the paren) is a measurement; the bare identifier is a word search that also counts comments. This repo already has the convention (`const code = (s) => s.replace(...)`, comments stripped before source assertions) and it has now caught four people (established 2026-09-03, after `4 → 11 call sites` reached the owner and the true figure was `4 → 6`).
- 🔴 **Reproducing a measurement with the same flawed method is not verification** — the reviewer re-ran the same bad `grep -c` and reported it as *reproduced* (@Sober's own rule about himself, 2026-09-03).
- **Reading code is NOT testing** — QA declined an offered source read as a substitute for a live check, on the grounds that a source read adds no fact (Tanya's charter rule, restated 2026-09-03).
- **Date discipline, now a hard step in `PM.md`: before reading or writing ANY log, run `date` and `ls -t log/` and confirm the newest filename matches — no exceptions, including "I just looked five minutes ago"** (Porter, binding on himself, 2026-09-03, after a false status report caused by grepping yesterday's file).
- **QA footprint rule for LINE rounds: create as few records as the ACs require, name them obviously (`QA-079-…`), and declare every one** — because a student created in LINE is permanent (Porter, 2026-09-03).
- **Pre-existing junk found on a test box is EVIDENCE, not mess — report it, do not tidy it** (Porter, 2026-09-03).
- **`sid` fixture identities as of 2026-09-03: family `0900000092` = มิลล่า · มิลลิม · asda · family `0905622548` = เตาไป้ · the second LINE account is `support08 Dong`** (observed).
- **`sid` held 111 parents on 2026-09-03** (observed) — a possible additional box discriminator alongside the `course_packages` count.
- **A verdict of `TEST_PASSED` on a subset must never travel without the sentence naming what is NOT covered** — REQ-079 was signed `TEST_PASSED` on 15 of 26 ACs and the PM carried the qualifying sentence verbatim rather than the two words alone (Tanya + Porter, 2026-09-03).
- 📌 **The bot saying "success" and the admin screen agreeing are TWO different facts** — a half-written link hides precisely between them, so the data side is checked separately (Tanya, 2026-09-03).

## ⚠️ REFINES EXISTING

- **SYSTEM-FACTS says: *"🔴 Flows are started by TYPED KEYWORDS, not buttons … **The rich menus do not exist yet**, so a keyword is currently the only possible trigger." (2026-09-02)***
  **Refinement with the reason and the blocker:** *a rich menu **is** live — the OLD 6-cell parent menu from REQ-015/042 — and the two-state (`ยังไม่รู้จัก` / `รู้จักแล้ว`) menu the design needs is unpublished because **it is waiting on the OWNER for two images**; `linkKnownRichMenu` is a silent no-op* — **(read from source by @Sober 2026-09-02, restated by Porter 2026-09-03)**. ⇒ the existing line's "do not exist yet" is true of the *new* menus only; buttons *do* exist and postbacks *do* work.
- **SYSTEM-FACTS says: *"**Max 5 students per phone number.** 🔴 The owner's own decision, given 'a long time ago' … enforced in the LINE add-student flow."***
  **Additional surface for the same rule:** *it is also shown as a live counter on the rich-menu student list — `นักเรียนของคุณ (3/5)`* — **(observed, 2026-09-03)**.
- **SYSTEM-FACTS says: *"🔴 Accepted security risk — LINE entry is the phone number alone (owner, 2026-09-02). Anyone who knows a family's phone number can see their children and act for them."***
  **Corroboration that the acceptance is settled, not merely recorded:** *when Porter re-raised the same risk on 2026-09-03 as though it were new, the owner did not understand why it mattered — Porter withdrew it on the record as **"I re-dramatised a decided risk as if it were new"***. ⇒ **do not bring this back to him as news; only a NEW mechanism reopens it.**
- **SYSTEM-FACTS says: *"**LINE on PC: no rich menu**, and buttons cannot be tapped at all — text only (owner, 09-01)"* — already corrected once at the foot of that file.**
  **Third consequence, now shipped against:** *a PC parent who triggers the mute has nothing to tap and would sit in silence until the timeout, which is why the typed `เปิดเมนู` re-open exists at all* — **(owner's order, 2026-09-03)**.

## ⚠️ CONTRADICTIONS

**1. How long a mute lasts — two figures from two roles on the same day. Reported, not resolved.**

- **Side A (Porter, 2026-09-03, 08:3x):** a PC parent who triggers the mute *"wait[s] for the timeout (**@Sober's ~30-minute window from the last message**)."*
- **Side B (Jason's measurement, endorsed by Sober, 2026-09-03, later):** *"His measurement was exactly what I asked for: **mute 60 min, TTL 30 min**, so an **early** un-mute leaves a live row…"* — i.e. **the mute is 60 minutes; 30 minutes is the SESSION inactivity TTL (TASK-231), a different clock.**

*Two different clocks may have been conflated in Side A, but which figure is the mute is not this worker's call. It matters operationally: it decides how long a PC parent with no rich menu is left in silence.*

**2. How many LINE recipients are linked on `sid`, and where the real teachers are. Reported, not resolved.**

- **Side A — SYSTEM-FACTS, currently standing:** *"**2 real teachers are linked on `uat`.** They must never receive a rehearsal message."* and *"**The owner is linked on `sid` as teacher `Bank`** (2026-09-01) — the isolatable test recipient. **Only ONE recipient is linked**, so 'every assigned teacher got it' still cannot be proven."*
- **Side B — Porter and Tanya, 2026-09-03:** *"**Never message `Haris`** (a real teacher linked on **`sid`**). **`Bank`** is the owner and is fine."* and, in Tanya's footprint: *"**AC-18 holds — the two linked accounts are Bank (him) and Haris**, and every fixture I have ever confirmed used Bank/Camp/Dewy/Ek, never Haris."*
- **Side B, later the same day (2026-09-03, 23:4x):** *"**A SECOND LINE account exists** — `support08 Dong` followed the OA and started `สมัคร`"*, and by 23:49 it had **linked as a parent** to family `0905622548`.

*So `sid` carries at least one real teacher (`Haris`) that SYSTEM-FACTS places on `uat` only, and the "only ONE recipient is linked" line was already false on 2026-09-03 and is more so after `support08 Dong`. Which of the three statements is the correct picture is Marie's / the owner's to settle — the operational risk (a rehearsal message reaching a real teacher on `sid`) points the same way whichever wins.*
