# Facts extracted from `log/2026-08-04.md` (175,015 bytes · 1,942 lines · read in full)

> ⚠️ **DATING CAVEAT — READ BEFORE MERGING.** This log file's NAME is wrong for most of its
> contents. Porter ruled inside the file that **the machine clock (then `2026-08-10`) is
> authoritative and the filename had been lagging** because the team kept appending to one open
> file across several days. **Every `(who, 2026-08-04)` tag below therefore means "on or before
> 2026-08-10"**, not "on 2026-08-04". I kept the required date format rather than invent per-entry
> dates. See `## ⚠️ CONTRADICTIONS`.

---

## Product limits

- **The frontoffice web is used on LAPTOP + PC ONLY. Phone width (375 px) is out of scope.** Owner, 2026-08-04: *"web front office เราใช้ base on laptop + pc"*. ⇒ a layout defect that only exists at phone width is MINOR by definition, never go-live-blocking. (owner, 2026-08-04)
- 🔴 **There is ONE shared staff login. No per-user auth exists anywhere in the product** ⇒ **"who did it" cannot be recorded honestly** on any action. Separate logins is a named prerequisite, not a nicety. (Jason + Sober, 2026-08-04)
- **There is no cancel-course / refund / early-termination flow.** Since a per-session cancel re-owes a make-up and never shrinks a course, **ending a course early has no path at all** — the escape is an admin `override`. (Jason, 2026-08-04)
- **A cancelled session LEAVES the plan** (it is neither LIVE nor DELIVERED, so it is filtered out and the make-up replaces it) ⇒ **there is no plan row on which a cancellation reason can ever be shown**. Reason-surfacing needs a booking-history view, which does not exist. (Sober, 2026-08-04)
- **A voucher has no plan endpoint and no reconcile — by design.** A voucher move is a plain move; only courses have a plan engine. (Sober, 2026-08-04)
- **A voucher is issued as program-agnostic HOURS** — there is no program field at voucher-issue time; the program is only chosen at booking time. (Fern, verified in code, 2026-08-04)
- **This repo has NO database-integration tests** (brownfield) — every DB path is verified by reading code, never by an automated run. Any "verified" claim about a DB path is an inspection claim. (Jason, 2026-08-04; restated by Sober)
- **Mock mode cannot refuse anything.** In FE mock mode NextAuth accepts any local input and the mock returns a COURSE shape for any entitlement id ⇒ **no behavioural acceptance criterion can be tested on mock — only layout.** (Tanya, 2026-08-04)

## Telling the two boxes apart

- 🔴 **`som.develyst.online` = the `sid` test frontoffice. `frontoffice.develyst.online` = the CUSTOMER's production frontoffice.** QA's production-refusal guard is literally this string check: `APP_URL` must be `som.develyst.online` and never `frontoffice.develyst.online`. (Porter/Tanya, 2026-08-04)
- **The customer's real environment is a SEPARATE box from `sid`** — "deploy to the customer's real env (their production, not just sid)" was a distinct future step throughout this log. (Porter, 2026-08-04)
- **Four deployable apps exist:** scheduling-back, scheduler-front, backoffice-back, backoffice-front. A deploy names which of the four changed; they move independently. (Sober's deploy gate, 2026-08-04)
- **`bo` (backoffice) is the SAME database** as scheduling — that is why an in-transaction write across the two is safe, and the freelance ceiling has written `bo` in-tx without ever breaking. (Sober, 2026-08-04)
- **Test/staff credentials live in `C:\Users\Admin\sm-test-access.txt`** (the owner moved them there from `H:\sm-test-access.txt` when drive `H:` turned out not to exist on the QA machine). It holds the `sid` frontoffice `APP_URL`, `STAFF_EMAIL`/`STAFF_PASSWORD`, `AUTH_SECRET`, **and** the backoffice URL + user + pass. (owner, 2026-08-04)
- Observed then, may drift: `sid` teacher roster = **FULL_TIME 7 · PART_TIME 6 · FREELANCE 10**, and **21 teachers are not linked to LINE**. (Tanya, 2026-08-04)

## Working agreements the owner has stated

- 🔴 **The OWNER runs every deploy himself, by his own flow: build local → copy → `pm2 restart`; migrations run locally.** The team writes a runbook; the owner executes it. (owner's flow, 2026-08-04)
- **The owner chose ONE clean deploy of a whole set over shipping the core early and the rest later.** (owner, 2026-08-04)
- **QA may create throwaway QA-owned rows on `sid` when explicitly authorised, and MUST archive them afterwards and declare them in the footprint ledger.** QA never touches a real teacher's/family's row and never works around a refused write. (owner authorisation, 2026-08-04)
- **`db:migrate` is run on EVERY deploy as a self-verifying no-op, even when the batch has zero migrations** — "a 'no migration' claim expires the moment code lands; the guard confirms it." (Sober, standing rule, 2026-08-04)
- **`bun run sale:ensure-items` must be re-run on deploy whenever new sale items are added** (insert-only, safe to repeat). (Jason, 2026-08-04)
- **The FE must NEVER carry a second copy of the price card.** Prices are read from the server; a price the FE cannot get from an endpoint is not displayed rather than hardcoded. (repo rule, cited by Fern and enforced by Sober, 2026-08-04)
- **STANDING RULE — any new shared-row control is measured at 1600 / 1280 / 768 / 375 px** before it is accepted. (board standing rule, cited throughout 2026-08-04)
- **Engineers do not authenticate against `sid`.** Live/painted rendering of an auth-gated screen is QA's job, not the FE engineer's — FE verifies by typecheck + build against frozen contracts. (Fern + Sober, 2026-08-04)
- **The machine/session clock is authoritative for dates, NOT the newest log filename; a new day rolls a new log file.** Porter's ruling after QA flagged the mismatch. (Porter, 2026-08-04)
- **Nobody re-dates evidence after the fact** — "re-labelling results after the fact is how a record stops being one." (Tanya, 2026-08-04)

## Business rules the owner set earlier and that were never written down

*(These are owner rulings that live only in this log — each was given in chat and applied straight to code.)*

- 🔴 **Past-dated bookings are INTENDED, not a defect.** Standing owner ruling; QA is told not to file them. (owner, via Porter, 2026-08-04)
- 🔴 **A per-session cancel is a RESCHEDULE, not a forfeit — every course-session cancel re-owes a make-up**, whether the session was live or already delivered. A cancel therefore costs 0 freelance hours until the make-up is taught. (owner reversal, 2026-08-04)
- 🔴 **An already-delivered session (ATTENDED / NO_SHOW) CAN be cancelled, but only with a non-empty reason — it is audited.** Edit/move of a delivered session stays refused. (owner reversal, 2026-08-04)
- 🔴 **SICK_LEAVE RELEASES the freelance hour** (owner reversal of the earlier rule). ⇒ a sick leave costs **1** freelance hour (its make-up), not 2. (owner, 2026-08-04)
- **Insert ("แทรกคาบชดเชย") reschedules WITHIN the course size by pulling from the last week — the total never grows.** The owner refused to have this disabled or blocked: it is the designed flow, not a bug. Verbatim: *"บอกว่ามันจะเอา last week มานะ … แผนจะเป็น week1 ปกติ week2 ปกติ week3 ปกติ +เพิ่มอีกวัน เอามาจากตัวหลังสุด week4 week5."* (owner, 2026-08-04)
- **What the owner demanded instead of a block: the resulting PLAN must be shown before the user commits** — a plan diff, not a "continue?" prompt: *"บอกว่าแผนจะเป็นแบบนี้นะ"*. (owner, 2026-08-04)
- **A separate, second kind of "add a day" exists and must never be confused with the quota reschedule: an EXTRA PAID SESSION.** The family buys one more session; it is charged as a single-session sale, draws freelance like any booking, and **leaves `size` and the end date unchanged**. It must be a visibly distinct action from Insert. (owner, 2026-08-04)
- **Fuel allowance is handled as a MANUAL backoffice EXPENSE — no code, ever.** (owner, 2026-08-04)
- **Vouchers may not be used for Onewheel or Balance Play** (excluded price groups `onewheel`, `balance-private`, `balance-group`), and a booking with no price group (e.g. 1st Trial) is refused by the same rule. **Enforced at BOOKING time, not at attendance**, and already-sold vouchers are never rewritten. (owner requirement, 2026-08-04)
- **Equipment rental is recorded BOTH ways: as an add-on at booking/attendance AND as a standalone quick-sale.** (owner, 2026-08-04)
- **Rental revenue is reported separately from tuition and is NOT attributed to a program.** (owner requirement, 2026-08-04)
- **A teacher change requires 3 days' notice by default — but it is a NOTICE, not a block: an admin `override` bypasses it**, and the threshold is a staff-editable setting. Coded defaults: teacher-change notice **3 days**, check-in early window **30 minutes**. (owner rule + Jason, 2026-08-04)
- **A course cannot extend past its maximum-weeks ceiling.** When a cancel's re-owed make-up cannot fit inside the ceiling, the cancel is refused; the only escape today is an admin `override`. The owner accepted this as two of his own rules meeting, not a bug — only the wording had to stop saying "extension" on a cancel. (owner, via Porter, 2026-08-04)
- **A course-deduction history WITHOUT "who" is ACCEPTED** — the shared-login limitation is shown on screen rather than faked; a full actor/hop-by-hop audit waits until separate logins exist. (owner, 2026-08-04)
- **An acceptance criterion may be closed on PREVENTION rather than on demonstration.** The owner ruled that an expired voucher never appearing in the eligible list *is* the promise kept — better than an alert — so the alert itself never had to be demonstrated. (owner, 2026-08-04)

## Terminology — the owner's / the customer's vocabulary

- **"ตัดคอร์ส"** = deduct from a course's quota. **"ประวัติการตัดคอร์ส"** = the course-deduction history (what was consumed / cancelled / re-owed / added, with when + why). The customer's own words. (customer, via Porter, 2026-08-04)
- **"แทรกคาบชดเชย" (Insert make-up)** vs **"เพิ่มคาบ (คิดเงิน)" (Add extra, charged)** — two deliberately different actions: the first reschedules inside the quota, the second is a paid addition. Never treat them as one. (owner, 2026-08-04)
- **The customer's phrase for the multi-course case:** *"เรียนเพิ่มระหว่างสัปดาห์ ขึ้นให้เลือกว่าจะตัดคอร์สไหน"* — when a student holds more than one course, staff must be able to see WHICH course a mid-week session draws from. The owner pinned the calendar entry point specifically: *"ถ้าจะใช้สิทธิ์จองแทรกที่หน้า calendar … ถ้าเลือกโหมด course + นักเรียน แล้ว หากมีสองอันล่ะ"*. (owner/customer, 2026-08-04)

## Unsorted — 2026-08-04

- **A teacher's `workDays` list being EMPTY means "works all seven days"**, not "works no days". Narrowing an empty list to a subset therefore removes the complement. This convention is easy to get backwards and is load-bearing for the orphaned-session check. (Jason/Sober, 2026-08-04)
- **`bo.movement` is the durable, timestamped consumption/refund ledger** (`refId` = bookingId, signed qty, reason, `createdAt`), with `refType` ∈ `BOOKING` / `BOOKING_REVERSAL` / `SALE`. A `SALE` row is revenue, not a course deduction. (Jason + Sober verified against the real writers, 2026-08-04)
- **Only a booking's CURRENT status is stored** — no transition log exists, so a session that flip-flopped shows its current state and last change, never every hop. (Sober, 2026-08-04)
- **QA's dev-cookie path is `mint-session.mjs`: staff login → backend token → minted session cookie.** It refuses a blank or forged token by design, so it cannot be used without real credentials. (Tanya, 2026-08-04)

## ⚠️ REFINES EXISTING (SYSTEM-FACTS already says it; this log carries an earlier date or the missing attribution)

1. **LINE testing is gated on the owner's phone — attested 2026-08-04, five weeks earlier than SYSTEM-FACTS records.**
   - **SYSTEM-FACTS:** *"## 🔴 QA cannot test LINE. The owner is the only LINE-capable tester. (established 2026-09-03)"* … *"It had been true for weeks and was never written down."*
   - **This log (Tanya, 2026-08-04), on REQ-020:** *"the queue is empty, so claim→queue→approve / reject / pick-on-collision is NOT TESTED — only a real LINE claim can create one. That is the owner's phone, not mine."* And on REQ-023: the delivery ACs *"require sending to real admins, and rule 4 says I don't."*
   - **Same date, Porter:** the LINE ACs are *"the only ACs left that need a real phone"*, and QA wrote `tests/CLICK-SCRIPTS-owner.md` Scripts 4 + 5 **for the owner to run on his own phone** — i.e. the owner-is-the-hands / QA-is-the-verdict split was already in operation on 2026-08-04.

2. **"No delete anywhere" is broader than student-delete, and was attested 2026-08-04.**
   - **SYSTEM-FACTS:** *"There is NO student-delete anywhere in the product. Nothing with history can be removed — only hidden."*
   - **This log (Tanya, 2026-08-04), declaring her `sid` footprint:** *"**No delete endpoint exists**, so it stays — QA-owned rows only."* ⇒ QA rows accumulate on `sid` permanently; archiving is the only cleanup.

## ⚠️ CONTRADICTIONS

**C1 — the log's own date contradicts itself, and it is unresolved at the level of individual entries.**
Both sides verbatim:
- **Tanya, in this file:** *"⏱ Clock mismatch — please rule. This machine's clock reads **2026-08-10**, while today's log file is `2026-08-04.md`. All my relative evidence ('a class tomorrow' = 2026-08-11) comes from the machine clock and is internally consistent, but the two disagree."*
- **Porter, in this file:** *"⏱ Clock — the machine clock (2026-08-10) is AUTHORITATIVE. It matches the system's real 'today'; the log **filename `2026-08-04.md` has been lagging** (we kept appending to the open file across days). Your evidence dates are correct as-is. **Process fix going forward:** new days roll a new `2026-08-10.md`+ log; **I'm not renaming this file mid-flight.**"*

I am **not** resolving which date belongs to which entry. What is certain: this single file covers **at least 2026-08-04 through 2026-08-10**, the filename was never corrected, and one entry inside it is explicitly headed *"EOD HANDOFF (real date 2026-08-10; log file lags)"*. Marie must decide whether facts tagged `2026-08-04` here are re-tagged as a range before they enter SYSTEM-FACTS.
