# Extracted facts — from `log/2026-08-28.md` (137,492 bytes, 1,722 lines, read in full)

> Scratch output for Marie's archaeology run (ORDER 3). Nothing here has been written into the repo.
> Every line carries who said it and when. Sections match `SYSTEM-FACTS.md`.

## Schedules

- **`sm-daily-digest` at 08:00 is for ADMINS / ops items; the 08:15 push is for TEACHERS + PARENTS about today's classes — different audiences entirely** (Porter, 2026-08-28).
- **A scheduled job here is a thin compiled trigger exe → an internal endpoint; all logic is server-side so the exe never touches the DB** (Jason, 2026-08-28, describing `daily-digest` and the new `daily-reminder`).
- **Registering a job = Task Scheduler entry (e.g. `sm-daily-reminder`), daily at its time, "run whether user logged on or not", with `SCHEDULER_API_URL` + `INTERNAL_JOB_SECRET` set — and it must be done on BOTH boxes, by the owner.** It is an ops step, not part of a deploy (Jason + Porter, 2026-08-28).
- 🔴 **Code being merged is not a job existing: two scheduled jobs on this project were never registered on a box and nobody noticed for weeks — the day-end job had NEVER run on `uat` at all** (Jason, 2026-08-28).
- **The way to prove a job fired is a `job_runs` row for that date; no row = it was never registered** (Jason, 2026-08-28).
- **`sent: true` on a job result means the JOB ran, not that anyone was reached** — deliberately separated, with a test (Jason, 2026-08-28).

## Product limits

- **There is NO course-delete.** Cancelling with a reason (e.g. `ADMIN_ERROR`) is **retirement, not removal** — the course leaves the active view and stays findable. `course:cleanup` refuses anything with a posted sale, **and a created course always has one** (Porter + Tanya, 2026-08-28).
- **There is NO student-delete** — a QA fixture student cannot be removed, only left behind (Tanya, 2026-08-28).
- **`notification_outbox` has no public API** — nobody can read it except by SQL on the box (Tanya, 2026-08-28).
- 🔴 **`PATCH /parents/:id` does not accept `lineUserId`. A LINE link can only be created through the real OA flow, i.e. from a phone.** ⇒ a LINE-linked recipient cannot be manufactured through the API (Tanya, 2026-08-28).
- **The internal job endpoints (e.g. `POST /jobs/daily-reminder`) are server-internal — 404 on the public host, 401 on `/api`, and gated by `INTERNAL_JOB_SECRET`** (Tanya, 2026-08-28).
- **There is NO `course.note` column.** The course-level note staff actually write is `attendeeNote`, set once at creation and carried onto every session (Jason, 2026-08-28).

## LINE

- **`ตารางวันนี้`'s composer is the one owner-verified-on-a-real-phone format for a day's schedule** (REQ-067 Part B). Any push of "today's classes" reuses it; a second format is forbidden (Porter, 2026-08-28).
- **A course-confirm message goes to the teacher AND the parent — one outbox row per PERSON, never one per booking** (owner's instruction, 2026-08-28; see Business rules).
- **An unlinked recipient is a reported SKIP, not an error** — the skip reason renders in Thai (*"ผู้รับยังไม่ผูก LINE userId"*) (Jason + Tanya, 2026-08-28).
- ⚠️ **The LINE SEND path was never observed by a human as of 2026-08-28** — every QA run hit the skip path because QA has no linkable recipient. Layout can only be judged on the owner's device (Porter, 2026-08-28).

## Telling the two boxes apart

- **`sid` is the rehearsal box; `uat` is the customer's box.** Divergence between them is acceptable and is recorded rather than silently re-synced (Porter, 2026-08-28).
- 🔴 **`sid` imports do NOT match `uat` imports.** The expiry repair was committed on `sid` under the old scope (imports recomputed); on `uat` imported expiry was left exactly as the customer's staff typed it. **Worth knowing when testing anything expiry-related** (Porter, 2026-08-28).
- **`uat` at 2026-08-28: 201 course packages = 164 `source=IMPORT` + 37 native `source=SALE`; sizes 4 (17) · 6 (39) · 10 (145). No off-card size existed** (owner's own query, relayed by Porter, 2026-08-28).
- **`uat` at 2026-08-28 held 532 `PENDING` sessions and 15 `CONFIRMED`** (Porter, 2026-08-28).
- **A Saturday on `uat` carries ~60 bookings; a busy teacher has ~8 classes in a day** (Porter, 2026-08-28) — the number that makes per-booking messaging unacceptable.
- **Many `uat` parents were imported and have NEVER linked LINE** ⇒ any parent-notification feature must count its reach before shipping (Porter + Jason, 2026-08-28).
- **Migrations are owner-run, `sid` first, and before the code that needs the column** (Sober + Porter, 2026-08-28, for `0024_course_dropped`).
- **The deploy order on `uat` is: `db:migrate` → `db:verify` GREEN (expect a `seed-ledger` round — it has been needed on all three of the last migrations) → restart BE → then FE → confirm from the SCREEN, not from the deploy command** (Porter, 2026-08-28; a stale FE bundle passed for a fresh one once that week).

## Working agreements the owner has stated

- 📌 🔴 **STANDING RULE — every defect is attributed and the person is told** (owner, 2026-08-28):
  *"ต่อจากนี้งานผิดที่ใคร ให้ลงไว้ด้วยว่าใครทำผิดอะไร และแจ้งให้เขาทราบด้วย"*
  Two limits Porter attached the same day: **attribute the mistake, not the person**, and **a self-reported mistake counts the same as a caught one.**
- 🔴 **The chain is the owner's, not a formatting preference: Human → Porter → Sober → (Jason/Fern); QA hangs off Porter.** *"แกไม่มีสิทธิ์ส่งบอลตรงไปที่ Fern นะ"* (owner, 2026-08-28). Porter routes only to @Tanya directly.
- **The SA Lead is a LEAD, not a worker running a queue — he must re-orient to the newest activity and the PM's latest directive before doing any task** (owner instruction, 2026-08-28; written into `SA-Lead.md` as RULE ZERO).
- 🔴 **QA may WRITE on `sid`; `uat` is production and is never QA's — "not read, not write, not 'just a GET'"** (คุณฟีน in writing, **2026-08-04**, quoted from `board.md:154-159` on 2026-08-28): the remote Postgres host `154.197.124.206:5432` is **dev/staging with test data, NOT production**. Conditions: remove every record she creates, declare the footprint in the TEST file, never touch data she did not create, never restart/redeploy.
- **The dev-server exception belongs to Tanya ALONE** — engineers (FE/BE) may not verify on `sid`; a "rendered check on the box" is routed to QA, not to the builder (PROTOCOL, restated by Sober against himself, 2026-08-28).
- **Batch the deploys: `uat` waits until a REQ is whole — one deploy, not three.** *"รอครบ"* (owner, 2026-08-28).
- ⚠️ **`sm-jobs/*.ps1` carry the internal secret in plaintext, in git, since 2026-08-20. The owner asked for the file in that exact format** — flagged once, deliberately not changed (recorded by Porter, 2026-08-28).

## Business rules the owner set earlier and that were never written down

- **Course size is a FIXED CHOICE of 4 / 6 / 10**, tied to price, leave quota and week ceiling (owner's design, 2026-08-28). **A size outside that set has no leave quota and no week ceiling** — so an off-card size can only exist if a human states the quota.
- **`MAX_WEEK = size + leave quota`, exactly, at every size** — 4→1→5 · 6→2→8 · 10→3→13. **The owner's own sentence:** *"ซื้อ 4 คาบ ลาได้ 1 ก็ 5 สัปดาห์"* (owner, quoted 2026-08-28).
- **The start week counts as WEEK 1** ⇒ expiry = start + (ceiling − 1) × 7 days. **The owner's own number, read off a screen: a 6-session course starting 2026-09-04 expires 2026-10-23** (owner, 2026-08-28).
- **Owner's design for an off-card import size:** *"ก และ ข เป็นปุ่ม option ว่าเป็นการเพิ่มแบบนอกเหนือจาก ก และมีช่องให้ใส่ quota ลาเอง"* — default is the fixed choice; an explicit "off-card" option reveals a free size input **plus a manual leave-quota field**. **The admin must state the quota, because the system cannot derive it for an unknown size** (owner, 2026-08-28).
- 🔴 **NEVER retro-rewrite a value a human typed.** *"ฉันว่าแก้แค่คอร์สธรรมดาได้มั้ย คอร์สนำเข้ากลางตันอ่ะ เละเทะกว่าเยอะ … ระบบคอร์สนำเข้าห่วยแบบนี้ เขายังทำลงไปได้ แสดงว่าลูกค้าเขาคิดบางอย่างมาดีแล้ว"* and *"ข้ามการแก้วันexpire คอร์สนำเข้าไปก่อน"* (owner, 2026-08-28). **What is banned is retro-rewriting rows a human already filled in — not the formula**; `importedCourseExpiry` stays for FUTURE imports.
- 📌 **The principle behind it, and it is the SECOND time he has enforced it: when a value looks wrong but a human entered it deliberately, "wrong" is a hypothesis about their business, not a fact about our data.** The first time was **2026-08-19**, the `Ari3y(V)'MOM` row, which Porter inferred was a data error and the owner corrected as a real new family (Porter, naming the pattern, 2026-08-28). The protected set turned out to be **164 courses — four times larger than the 37 we actually needed to fix.**
- **Drop = *"เหมือนขอหยุดคอร์สชั่วคราว"*** (owner's own definition, quoted 2026-08-28). ⇒ **a dropped course REFUSES session-writes (`409 COURSE_DROPPED`, "press resume first") but ACCEPTS lifecycle writes (resume, cancel).** Ruled by Porter from the owner's definition, 2026-08-28: *"a course that is paused but still accepts new sessions is not paused."*
- **Resume never moves a family's slot.** It rebuilds on the course's own weekday/start time; if someone took the slot during the pause it refuses with `SLOT_TAKEN` — *"มีคาบชน… ระบบไม่ย้ายคาบให้เอง"* — rather than silently double-booking a teacher (design + verified, 2026-08-28).
- 🔴 **A `CONFIRMED` session that nobody marks AUTO-ATTENDS at day-end (REQ-070, the owner's own design). He knows and accepts it** — bulk-confirming arms auto-attend for everything confirmed. **It is not a defect to report** (owner via Porter, 2026-08-28).
- **Confirming a course must NOT change `used_sessions` at confirm time** — if it does, that IS a defect (Porter, 2026-08-28).
- **Confirmation notifications go to the TEACHER AND THE PARENT, both, and it is two mechanisms not one:** *"ครูและก็ผู้ปกครองด้วย … ถ้า confirm ยกคอร์ส หรือ confirm รายครั้งก็ตาม ก็ส่งเลย และก็มีตั้ง task schedule ให้รัน api เช็คทุกวันว่ามีใครมีนัดวันนั้นบ้าง หากมี ก็แจ้งไปเลยที่ลูกค้าและครู"* (owner, 2026-08-28).
- 🔴 **ONE message per PERSON per day, NEVER one per booking** — a parent with two children in class the same day gets ONE message (the rule that follows from the owner's volume concern, specified 2026-08-28).
- **The customer's first classes are ~09:00–10:00 on weekends** — the fact that made 08:15 the right push time (Porter, 2026-08-28).
- **The confirm message must name WHICH DAYS the planned leaves are, not how many.** The owner's REQ lists five things: *"คอร์สที่ลง · วันที่เริ่ม · ตาราง (ทุกวันอะไร กี่โมง) · ลาล่วงหน้าวันไหนบ้าง · Note เพิ่มเติม"* (owner's wording, quoted 2026-08-28).

## Unsorted — 2026-08-28

- **The owner tests by opening the actual screen and reading the actual value** — every defect that reached him this stretch (the raw `endCourse.drop` key, the green `ปกติ` badge on a cancelled course, the stale `พักคอร์ส (0)` chip, the wrong expiry date) was found that way, not from a report (Porter, 2026-08-28).
- **A count-invariant is not a per-bucket check:** each filter chip's number must equal the rows that chip returns. "The counts sum to the total" passes in both the correct and the broken state when a course merely moves buckets (Porter's own AC correction, 2026-08-28).
- **The owner's LINE messages are judged on a device, by him** — the same way REQ-067 Part B was judged; QA cannot substitute for it (Porter, 2026-08-28).

## ⚠️ REFINES EXISTING — earlier date or the missing "who said it and when"

1. **Schedules table — `daily-reminder` 08:15.**
   - SYSTEM-FACTS: *"`daily-reminder` | **08:15** | `job_runs`, stable since 08-30"*.
   - This log: **08:15 is the OWNER'S OWN CHOICE, made 2026-08-28**, when Porter put the time to him ("`08:00` shares the slot with the existing digest…"). He picked 08:15 so the two jobs are legible in Task Scheduler. **The existing line records an observation; this records the decision and its author.**

2. **Product limits — no student-delete.**
   - SYSTEM-FACTS: *"There is NO student-delete anywhere in the product."* (undated, unattributed)
   - This log: **stated 2026-08-28 by Tanya** as a footprint consequence, together with **no course-delete** and the mechanism (`cancel` = retirement; `course:cleanup` refuses anything with a posted sale).

3. **"QA cannot test LINE" (SYSTEM-FACTS, established 2026-09-03).**
   - This log carries an **earlier date and the structural reason**: **2026-08-28**, Tanya — *"`PATCH /parents/:id` does not accept `lineUserId` (link is the real OA flow = a phone), so I cannot manufacture a linked recipient"*, and `POST /jobs/daily-reminder` is server-internal, gated by a secret she does not hold, and would message real people. **It was already true and already stated five days before it was written down.**

4. **`uat` ≈ 201 course packages (SYSTEM-FACTS, observed 09-01).**
   - This log: **the same 201 observed on 2026-08-28**, with the breakdown **164 IMPORT / 37 SALE** and **sizes 4 (17) · 6 (39) · 10 (145)** — from the owner's own `SELECT size, count(*)` query.

5. **Working agreement — "Quote the owner's OWN requirement numbers to him".**
   - SYSTEM-FACTS states the rule and the ranges (REQ-001…016 · REQ-BO-001…006 · FIX-001…007).
   - This log is **where it came from: 2026-08-28**, when Porter's use of the owner's numbers collided with the board's namespace (his REQ-011/012 vs the board's). **At that date his list ran to REQ-012.** Canonical mapping recorded that day: owner REQ-011 → board REQ-071 (Drop Course) · owner REQ-012 → board REQ-072 (Confirm whole course) · REQ-004 → REQ-028 · REQ-007 → REQ-068 · REQ-008 → REQ-036 · FIX-007 keeps its number.

## ⚠️ CONTRADICTIONS — reported, not resolved

**C-1 — does `month-reset` write a `job_runs` row?**
- **SYSTEM-FACTS (undated line, source given as `job_runs`, observed 09-01):**
  > `| `month-reset` | **00:05** | `job_runs`, observed 09-01 |`
  i.e. the row exists in `job_runs`.
- **Log 2026-08-28, Porter:**
  > *"The existing jobs write `job_runs`; **this one must too** — unlike `month-reset`, which still does not (queued)."*
  and again, 2026-08-28, Porter to Tanya:
  > *"Fires once, writes a `job_runs` row (**the `month-reset` gap** — a job we cannot prove ran is a job we do not have)."*

Both sides stand as written. I have not tried to decide whether it was fixed between 08-28 and 09-01 or whether one of the two observations is wrong.
