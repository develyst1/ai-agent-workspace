# Extraction — `log/2026-08-10.md` (smart-scheduler) — ORDER 3 archaeology

> Source read in full: 589 lines / 49,533 bytes.
>
> ⚠️ **Reading caveat for Marie, not a contradiction:** on 2026-08-10 the team called the customer's live box
> **"customer-prod" / `frontoffice.develyst.online`**. On **2026-08-16** Porter recorded the owner's correction that
> **that box IS `uat`** and that "customer-prod" was never a separate environment. Every 08-10 line below that says
> "prod" refers to the box later named `uat`. Both records are preserved as written.

## Telling the two boxes apart

- **The customer's live system is `frontoffice.develyst.online`; its DB host is `154.197.124.29`** (Porter, 2026-08-10).
- **The owner OWNS that server, and the DB plus all four apps run on it** — the DB is `localhost` there (owner via Porter, 2026-08-10).
- **That server is Windows Server, reached by RDP, `cmd.exe` as Administrator** — no SSH, no scp; file transfer is RDP clipboard/drive-redirect (owner, 2026-08-10).
- **It carries `pg_dump` PostgreSQL 18.4 and `bun` 1.3.14** (owner's own check, 2026-08-10).
- 🔴 **The live box's `pg_hba.conf` rejects remote clients; `sid`'s accepts any host with the password.** The owner's laptop IP `110.171.40.169` was refused by the live DB and allowed by `sid` (observed, 2026-08-10). **The order of the two boxes' network posture is the tell — never assume what works on `sid` works there.**
- **The live DB listens on the network already** — the failure reached the `pg_hba` stage, so only a `pg_hba.conf` line + reload would ever be needed, never a `listen_addresses` change (Sober, 2026-08-10).
- **The local DSN shape on that server: `postgresql://postgres:<PWD>@localhost:5432/smart_scheduler`** (Sober/Porter, 2026-08-10).
- **The server holds only the COMPILED apps** (`bun build --compile` .exe / `.next`) — **no source, no `drizzle/` migrations, no `scripts/`** until someone copies them there (Porter, 2026-08-10).
- **One shared database, two backends, both reading `DATABASE_URL`, with two separate migration ledgers** — `__drizzle_migrations_bo` and `__drizzle_migrations_scheduling` (REQ-032), so they cannot clobber each other (Sober, 2026-08-10).
- **`PUBLIC_CALENDAR_BASE_URL` must be set in that environment** or the ics feed (TASK-044) is wrong (Sober, 2026-08-10).

## Working agreements the owner has stated

- 🔴 **No hand-pasted connection strings. Operational commands must be `package.json` scripts that read `.env`** — owner's own push-back: *"ทำไมไม่ทำเป็น package.json script … ให้มันอ่านจาก env ไปเลย."* (owner, 2026-08-10).
- **The owner says he does not know DB infra — everything handed to him must be copy-paste and `cmd.exe`-friendly** (owner via Porter, 2026-08-10).
- **The owner executes every deploy and every DB operation himself; the team never runs anything against a real environment** (Sober + Jason both stated it as the brownfield rule, 2026-08-10).
- **The owner declines IP-whitelisting as a working practice** — he does not want to manage `pg_hba`/tunnels, and prefers running backup + migrate **on the server** where the DB is `localhost` (owner via Porter, 2026-08-10).
- 🔴 **Sober will not author a production security loosening off a decision that lives only in workspace files — it needs the real human's explicit go-ahead** (Sober, 2026-08-10). **If it ever happens the only safe form is a single-host `/32` line, never `0.0.0.0/0`, temporary by design, with a mandatory close + verify.**
- **The machine clock is authoritative for which log file a day belongs to** (Porter, 2026-08-10).

## Product limits

- **Who made each change is NOT tracked in the course history** — *"Who made each change isn't tracked yet — the branch shares one login."* The **branch shares one login**, which is why (observed in the shipped UI text, 2026-08-10).
- **Vouchers cannot be moved as a chain** — *"Voucher sessions can be moved one at a time (no make-up chain)"*, and a voucher plan is `insertable=false` (observed on `sid`, 2026-08-10).
- **A voucher cannot be booked onto an excluded program.** The server is the backstop: `409 VOUCHER_PROGRAM_EXCLUDED` — *"วอยเชอร์ใช้กับคลาส Onewheel หรือ Balance Play ไม่ได้"*. The allowed set comes from the server's own `voucherAllowedGroups` (`["bike-skate"]`), not from names in the FE (observed on `sid`, 2026-08-10).

## Unsorted — 2026-08-10

- **The owner's laptop is Windows** — shell `date +%Y%m%d-%H%M%S` is unreliable under Bun's shell there, so timestamps in tooling must be pure JS (Sober, 2026-08-10).
- **A `>` redirect writes a file even when the command fails** — a partial file that looks like a backup; the reason `db:backup` uses `pg_dump -f` and exits non-zero on failure or a 0-byte file (Sober, 2026-08-10).
- **The deploy mechanism, unchanged across every deploy to date: build locally → copy to the box → run migrations → `pm2 restart`, same sitting** (Sober, 2026-08-10).
- **A frontend-only deploy is `bun run build` → copy → `pm2 restart scheduler-front`; no `db:migrate` (the front never touches schema)** (Porter, 2026-08-10).
- 🔴 **`backoffice-back` must migrate BEFORE `scheduling-back`** — backoffice's `0004_bo_schema.sql` creates the `bo` schema and scheduling's `0017_entitlement_source` references `bo.*`. Wrong order → `relation "bo.…" does not exist`. Restart order mirrors it (Sober, 2026-08-10).
- **`db:migrate` = `drizzle-kit migrate && db:verify`; `db:verify` red means STOP, and the witness fallback is `db:seed-ledger-from-schema` → re-verify** (Sober, 2026-08-10).
- **A real environment's ledger rows can disagree with its migration files even when the files are internally consistent** — pre-existing tables the ledger never witnessed. `db:verify` establishes the state; it never assumes it. This exact failure mode already happened on `sid` (Sober, 2026-08-10).
- **A DB snapshot/backup before any migrate on the customer's box is a mandatory gate** — the owner's data is real (Porter's instinct, adopted by Sober as GATE 0, 2026-08-10).
- **A team standing rule: the calendar filter row is measured at 1600 / 1280 / 768 / 375** (Sober, 2026-08-10).
- **A QA FAIL is a claim about the product and must survive checking before it leaves QA's desk** — *"when a check says the product is broken, prove the harness isn't first"* (Tanya, 2026-08-10; three of her own false negatives that day).
- **A REQ-name match is a hypothesis, not a verdict — runtime confirms** (Porter, 2026-08-10, after mapping REQ-038 #3 to REQ-011 and being wrong).

## ⚠️ CONTRADICTIONS

**C1 — What the owner decided about opening the live DB to his laptop (both records dated 2026-08-10, same log file).**

- Porter, mid-file: *"**DECISION — Owner chose Option A:** temporarily open prod's `pg_hba` like sid (allow-all-with-password), run the deploy from the laptop as usual, then **CLOSE it again after**."*
- Sober, next entry: *"The temp-open-`pg_hba` path (Option A) is a **prod security loosening**, and I won't author it … It's also unnecessary … So backup + migrate run **on the server against `localhost`** … **This was the owner's own first instinct.**"*

Both are on record for the same day; the log does not show the owner restating his choice after Sober's refusal. **Not resolved here.**
(Downstream note, for placement only: on **2026-08-16** the log records the owner adding a *different* IP, `49.237.170.101`, on the server himself — so a whitelist did get opened at some point, and the board still listed the 2026-08-11 temp-open as unclosed.)
