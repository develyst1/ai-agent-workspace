# Extracted facts — from `log/2026-07-30.md` (73,451 bytes, 782 lines, read in full)

> Scratch output for Marie's archaeology run (ORDER 3). Nothing here has been written into the repo.
> ⚠️ **Vocabulary note for Marie:** on this date the owner/stakeholder is written as **คุณฟีน**, and the second
> server is called **`production` / frontoffice.develyst.online** — the name **`uat`** does not appear anywhere in
> this log. See `## ⚠️ CONTRADICTIONS`.

## Telling the two boxes apart

- 🔴 **There are TWO servers, each a COMPLETE stack** — frontoffice (front+back) + backoffice (front+back) + **its own database** + pm2 + nginx (owner, 2026-07-30).
  - **`sid` → `som.develyst.online`** — where the owner deploys, where **every acceptance test has been run**, and where the LINE webhook was re-pointed on **2026-07-30**.
  - **`production` → `frontoffice.develyst.online`** — the box the customer actually opens.
- **The two databases diverge, and each box migrates separately** (owner, 2026-07-30).
- 🔴 **The split is INTENTIONAL, not a delivery gap** (owner, 2026-07-30): we are **before the first real serve**; **the customer deliberately uses the OLDER version on `production` to try it and send comments**, while the team builds and verifies on `sid`. Production is brought up to date at the first serve.
  📌 Porter recorded his own over-alarm here: he read a stale box as a delivery gap and corrected the board from *"⚠️ open risk"* to *"✅ intentional"*.
- ⚠️ **Direct consequence, stated by the owner's answer: the customer's comments come from the OLD version.** ⇒ **a reported "bug" may already be fixed on `sid`; check it against the current `sid` build before turning a customer comment into a REQ** (Porter's binding rule taken from the owner's fact, 2026-07-30).
- **"DELIVERED" at this date meant "live on `sid`", not "live everywhere"** — REQ-001/002/004/005/006/007/008/010/011/015/016 and their migrations were all absent from `production` (Porter, 2026-07-30).
- 🔴 **The first-serve deploy is a real event, not a step:** `production` is behind on **code AND DB migrations**, has **its own database**, and the REQ-006 `bo`/shared-DB change sits in between (Porter, 2026-07-30, recorded and deliberately not actioned).
- 📌 **An environment fact invalidates code-level hypotheses.** The whole dead-menu investigation (three code hypotheses, instrumentation, a DATA REQUEST) was moot: the webhook pointed at the other box. **Check which box is serving before believing any "works locally / dead in prod" report** (Sober, 2026-07-30).

## LINE

- 🔴 **Unfollowing and re-following the OA does NOT unlink an account.** The link lives in OUR database, not on LINE's side — two follow events appear in the owner's transcript and the link survived them (observed in the owner's own chat export, 2026-07-30).
- **`bun run line:publish-menus` is re-runnable by design, and every re-publish leaves the previous 4 menus behind on the channel.** `app_settings.line_rich_menu_ids` keeps only the newest 4 ids; the channel held **8 menus** on 2026-07-30. **A menu the script flags as "created outside our publish" may simply be OUR orphan from an earlier run** — check the name before believing it came from the OA Manager (Porter, 2026-07-30).
- **The rich menus are channel-level: fixing the app does not require republishing them** (Porter, 2026-07-30).
- **One LINE user ⇒ ONE active roster link.** Linking as a teacher clears the parent link and vice versa — a role change **MOVES** the link rather than adding one (design, built and reviewed 2026-07-30, TASK-046). **Role precedence when resolving who you are is teacher → customer → admin.**
- **`app_settings.line_admin_user_ids` is a NOTIFICATION SUBSCRIPTION, not a roster identity.** It is checked last, cannot shadow a parent/teacher surface, and clearing it silently unsubscribes someone from leave alerts and is unrecoverable without the admin code (Sober + Jason, 2026-07-30).
- **An unlinked LINE conversation that types an unknown phone CREATES a parent record** (`findOrCreateParentByPhone`); typing an existing phone links you as that parent **and the bot replies with the children's names**; typing a roster nickname links you as that teacher, **first match wins on a collision** (Porter's code verification of the owner's concern, 2026-07-30).
- **A departed teacher whose account is still bound keeps receiving schedule pushes on their personal LINE** — there is no staff-side unbind (Porter, 2026-07-30).

## Product limits

- **Google Calendar refreshes a subscribed `.ics` feed slowly (hours); iPhone is ~15 minutes.** A calendar-feed feature is therefore near-real-time on iOS and not on Android, and that expectation has to be set with teachers rather than fixed in code (Sober, 2026-07-30).
- **A malformed `.ics` feed fails SILENTLY in calendar apps** — nothing reports the error (Sober, 2026-07-30).

## Working agreements the owner has stated

- 🔴 **The standard path must work; do not route around it.** The owner rejected a `psql` side-channel for a migration: *"ฉันไม่ชอบแบบนี้เลย มันเหมือนไม่มั่นใจในหลักการทำงานแบบปกติ แบบนี้ผู้พัฒนาคนอื่นมาทำ มันจะยุ่งยาก — ทำตามปกติเลย"* (คุณฟีน, 2026-07-30). ⇒ **The DB step is `bun run db:migrate`, re-runnable, no psql.** A workaround hides the defect instead of fixing it — and checking it properly is what found the real one.
- **The PM does not direct or sequence engineers.** The owner caught Porter doing it for the second time (first: 2026-07-28). ⇒ Porter's binding rule from that day: **in status reports name no engineer and state no build order — say "with the team (via @Sober)"**; priority input goes to the SA Lead, who decides (2026-07-30).
- **The owner prefers batching a release so there is one test round:** *"เทสทีเดียวได้ เพราะมีอีกหลายงาน"* (quoted 2026-07-30). ⚠️ **On this day he nevertheless chose to ship immediately** (option ก: REQ-015 + REQ-016 now, REQ-017 later on its own deploy) — **the preference is real but it is put to him each time, not assumed.**
- **The backoffice is reserved for money / executive work; daily staff work belongs in the frontoffice** — the owner's own earlier ruling, cited and re-applied on 2026-07-30 when people-management was placed in the frontoffice.
- **Older operational items are PARKED at the owner's word — *"รอไปก่อน"*** (2026-07-30): scheduled tasks, real numbers vs placeholders, the freelance roster's budgets in FE, FE lint on Next 16. **Stop surfacing them until he raises them.**
- **Real customer data read for verification is not copied into the workspace.** Porter verified the acceptance against the owner's exported OA transcript (a parent phone number + student names) and deliberately kept none of it in any file (2026-07-30).

## Business rules the owner set earlier and that were never written down

- 🔴 **A person who messages the bot may be lying about who they are, and names collide.** The owner's reasoning for putting an admin approval / pairing step in front of a LINE teacher claim: *"คนทักมาอาจจะขอเป็นครูมั่ว ๆ ก็ได้"* and *"มีคนชื่อ off สองคน"* (owner, 2026-07-30).
- **Parents get NO approval step** — self-create stays; staff only need to **view + suspend** them (owner, 2026-07-30).
- **Students sit UNDER the parent, and are managed on the web** (owner, 2026-07-30).
- **People (teachers, parents, students) are managed on the web, the way teachers already are — not through a LINE/LIFF form** (owner's reframing, 2026-07-30; it superseded the LIFF-form requirement outright).

## Unsorted — 2026-07-30

- **Migrations in this repo are HAND-AUTHORED and must be registered in `drizzle/meta/_journal.json`; `drizzle-kit migrate` applies the JOURNAL, not the folder** — an unregistered `.sql` is silently skipped (established 2026-07-30).
- 🔴 **Do NOT run `bun run db:generate`.** `drizzle/meta/` holds snapshots for only `0000–0003` while the journal lists 12+ entries, so `generate` diffs against 0003-era state — a live probe emitted **one full-schema migration re-creating all 18 tables**. Deliberately left in that state and documented in `drizzle/README.md` rather than hand-fabricating snapshots (proven and documented, 2026-07-30).
- **The week convention is Sunday-start (Sun–Sat), from the existing `weekRange` helper. No second week convention is to be invented** (Sober's design rule, 2026-07-30).
- **A LINE user is always resolved from their OWN `lineUserId` — never from a postback payload** (security rule carried from TASK-038, restated 2026-07-30). A forged payload may change a view, never an identity.
- **Both scheduled jobs are hit over HTTP on port `:4006` with the header `x-internal-secret` = `INTERNAL_JOB_SECRET`.** The old ops job on `:4010` was RETIRED — REQ-004 moved the freelance reset into scheduling and REQ-006 retired `ops` entirely (Porter's board correction, 2026-07-30).
- **Inbound LINE observability: one `[line-in]` line per inbound event, logged BEFORE dispatch, with a sha-prefix user marker — never the raw LINE userId, never message text, never a token.** `bun run line:inspect-menus` is a permanent, strictly read-only diagnostic (built and reviewed 2026-07-30).
- **A LINE keyword must keep working after a button exists for the same thing** (the REQ-015 principle, restated 2026-07-30) — old typed keywords and `/checkin?token=` are a standing regression check.

## ⚠️ CONTRADICTIONS / unresolved naming — reported, not resolved

**C-1 — what the second box is called, and where the LINE webhook used to point.**
- **Log 2026-07-30 (owner's answer, relayed by Porter):**
  > *"**Two servers**… **`sid`** → som.develyst.online… **`production`** → frontoffice.develyst.online. 🔴 **Not updated for ~2 weeks**… **It is the box that was serving the LINE webhook until today**"* — and: *"Stakeholder re-pointed the LINE webhook at the correct server"* (i.e. `production` → `sid`, on 2026-07-30).
- **`SYSTEM-FACTS.md` (owner, 2026-09-01):**
  > *"The webhook points at `sid` PERMANENTLY since 2026-09-01 (owner). **It used to live on `uat`** and be borrowed at night; that arrangement is over."*
- Also unresolved: the name **`uat`** appears nowhere in the 07-30 log, and **`production` / frontoffice.develyst.online** appears nowhere in `SYSTEM-FACTS.md`. Whether `uat` IS the box called `production` here (renamed), or a third environment, is **not established by anything I read**. Both statements are recorded above as written; I have not decided which naming is current, nor reconciled the two accounts of where the webhook lived between 07-30 and 09-01.
