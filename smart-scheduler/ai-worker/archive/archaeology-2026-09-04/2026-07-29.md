# Extracted durable facts — `log/2026-07-29.md` (smart-scheduler, founding era)

> Scratch output for Marie's ORDER 3 archaeology run. **Not merged into anything.** Source file read in
> full: 82,457 bytes / 932 lines. Deduped against `SYSTEM-FACTS.md` as of 2026-09-04 — this log predates that
> file by five weeks and contributed nothing to it, so almost everything here is new.
> Attribution key: `(owner, …)` = คุณฟีน / "the stakeholder" · `(observed, …)` = verified by a role in the log ·
> role names where the statement is a role's own standing rule.

---

## Terminology

- In this era the business decision-maker is called **`คุณฟีน`** and, interchangeably, **"the stakeholder"** — the same person today's files call **the owner**; **"the customer"** is a separate party whose asks he relays (observed, 2026-07-29).
- **The customer is a wheeled-sports / skate activity centre — not a tutoring school.** Stated as standing design context for anything customer-facing (owner via PM, 2026-07-29).
- 🔴 **"QR" here is a naming holdover — no QR code is generated anywhere.** There is no `qrcode` dependency in either repo; the `qr` command replies with a **plain check-in URL** (`/checkin?token=…`) (observed, 2026-07-29).
- **"Brownfield" means nothing is ever run against a live database or a live backend.** Tests are DB-free; LINE-API paths are verified only as "OA smoke" by a human after deploy (observed, 2026-07-29).

## Product limits

- **There is no venue scanner and no kiosk anywhere** ⇒ check-in proves no presence and the link adds **zero** anti-abuse (observed, 2026-07-29).
- **`/checkin?token=` is the system's only unauthenticated write endpoint**; the link is long-lived, forwardable, and carries no LINE identity (observed, 2026-07-29).
- **The backoffice is single-admin** — there is no role model in it (observed, 2026-07-29).
- **Sales carry no subject and prices live only in `bo.item`** ⇒ revenue-by-sport is not answerable from the sales data alone (observed, 2026-07-29).
- **`course_packages` has no `subjectId` column** — a course's sport program is recoverable only through the bookings it generated (observed, 2026-07-29).

## LINE

- **LINE rich-menu images: max 1 MB each.** Parent menu **2500×1686** (3×2; x-splits 833 / 833 / **834**, row height 843) · teacher menu **2500×843** (2 × 1250). **The tap bounds are fixed in code**, so artwork that does not line up puts buttons on the wrong labels (observed, 2026-07-29).
- **Publishing a rich menu is a manual operator step**: `bun run line:publish-menus`, needing `LINE_CHANNEL_ACCESS_TOKEN` and the images committed under `smart-scheduler-back/assets/line/`. It is deliberately **re-runnable**, so artwork can be republished later (observed, 2026-07-29).
- **The pre-existing menu set is FOUR menus — parent and teacher × TH/EN — with parent-TH as the LINE default** (observed, 2026-07-29).
- **Bot language is TH + EN only**: an explicit toggle button, persisted per user, default TH, seeded from the LINE profile locale (owner, 2026-07-29).
- **Thai is the primary language; the EN menus mirror the TH layout** (owner via PM, 2026-07-29).
- **Menu labels reuse the `line-i18n` keys so the menu and the bot's replies can never disagree** — the owner saw the rendered labels and accepted them (SA + owner, 2026-07-29).
- **A tap and the equivalent typed keyword must produce identical business effects** — postbacks route into the same handlers rather than a parallel implementation (SA, 2026-07-29).
- **A postback's `bookingId` is re-derived and authorised against that parent's own today-bookings** ⇒ a forged or replayed postback cannot act on another family's booking (observed, 2026-07-29).
- **The bot's language toggle must flip the replies AND re-link the matching-language rich menu, and persist** — that pairing is part of the owner's own acceptance check (owner acceptance checklist, 2026-07-29).

## Business rules the owner set earlier and that were never written down

- 🔴 **A freelance teacher who is FULL is HIDDEN from the calendar** (auto-disabled, exactly like inactive) — **not** bookable-with-override. **The ceiling exists to stop giving a full freelance more work**, so override-to-book was removed outright (owner, 2026-07-29 — reversing the 2026-07-11 "keep bookable" reading, which is on the record as superseded).
- **A hidden full freelance reappears on a top-up, a durable limit-override, or the monthly reset** — the freelance ceiling is **monthly** (owner + observed, 2026-07-29). ⇒ this is what the `month-reset` job at 00:05 is for.
- **The freelance colour strip reads %-of-ceiling-USED** (🟢 ≤30 · 🟡 30–70 · 🔴 70–<100), not money remaining (owner, 2026-07-29).
- 🔴 **Access is separated BY SYSTEM, not by roles.** The **backoffice login IS the executive's**; front staff work only on the frontoffice and have **no backoffice login**. ⇒ "finance data is executive-only" is satisfied simply by keeping it on the backoffice. **There is no RBAC/role system and none is to be built** (owner, 2026-07-29, correcting the PM's over-thinking).
- **Demographics come from the signup form, never from guessing at names; per-customer spend belongs on the backoffice; finance and sales data are executive-only** (owner relaying the 2026-07-25 meeting, 2026-07-29).
- **Bulk-confirm is partial-success by decision:** an over-budget freelance's booking is **skipped-and-reported**, never a whole-batch failure — the owner had the behaviour explained and OK'd it (owner, 2026-07-29).
- **QR stays in the product but is removed from the LINE MENU only** — option (ก). The `/checkin?token=` link, the token mechanism and the `qr` keyword all keep working; **no retire-QR requirement was opened**, and the contracted scope in `requirement.html` (UC-009 / SCR-008 / API-015) is unchanged (owner, 2026-07-29).
- **The TEAM produces the LINE rich-menu artwork — the customer supplies no designs:** *"ทีมทำมาเลย"* (owner, 2026-07-29).
- **Design assets get the owner's yes/no on the RENDERED images before a release.** He approved the first set unchanged: *"ไม่แก้ ตอนนี้เอาไปก่อน"* (owner, 2026-07-29).
- 🔴 **Student registration actually happens on LINE** — which is why the owner ordered the whole LINE experience improved **before** the demographics/dashboard chain (owner, 2026-07-29).
- **A course is ONE sport program by construction** — course creation stamps a single subject on every booking it generates, so "one course spanning several sports" does not exist; the SA flagged it to the owner as a check and nothing contradicted it (observed + SA, 2026-07-29).

## Working agreements the owner has stated

- 🔴 **The human applies migrations at deploy; agents never apply them.** Migrations are written additive/idempotent (`ADD COLUMN IF NOT EXISTS`) and shipped **un-applied** (standing rule, 2026-07-29).
- **Deploys are build + copy + restart, run by the owner, never by an agent. The backend is `smart-scheduler-back` on `:4006`** (observed, 2026-07-29).
- **The owner prefers ONE test round:** ready REQs are batched into a single deploy and a single acceptance pass (owner, 2026-07-29).
- **A REQ becomes DELIVERED only after the owner runs acceptance and the PM verifies it** (observed, 2026-07-29).
- **Contracted scope lives in `requirement.html`** (UC / SCR / API ids) plus the dated spec entries — anything sold there is removed only by its own REQ, never quietly inside another build (PM, owner-confirmed, 2026-07-29).
- **`lint` is known-broken on Next 16 in `smart-scheduler-front` and is NOT a gate.** The gates are `bunx tsc --noEmit`, `bun test`, and `bun run build` (observed, 2026-07-29).
- **Cross-engineer contracts (file paths, image sizes, tap bounds) are written identically into BOTH tasks and into the SPEC by the SA; engineers never sync directly** (SA standing rule, 2026-07-29).

## Unsorted — 2026-07-29

- **The LINE bot as originally shipped was a Thai-only, name-only text-keyword bot with no menu, no quick-reply, no flex, no LIFF, no teacher commands (push-only) and no phone-calendar sync** (observed, 2026-07-29). Kept because it dates the baseline every later LINE requirement is measured against.
- **A LINE-only error string that is thrown by `parent.service` and shared with the REST API stays un-keyed** — pulling it into a LINE-only i18n table would be the wrong coupling (SA ruling, 2026-07-29). The example is the "…สูงสุด…" message, i.e. the 5-students-per-phone refusal, which therefore reaches BOTH the REST API and LINE.

## ⚠️ CONTRADICTIONS

**Typed keywords: a permanent fallback (2026-07-29) vs forbidden (2026-09-02).** Not resolved here — both sides recorded as written.

- **2026-07-29 side** — REQ-015 / SPEC-012, designed and accepted with the owner in the loop: *"add … a **postback layer** so rich-menu/quick-reply taps route to the **same** handlers (**keyword input stays a fallback**)"* (SA, 2026-07-29); and in the owner's own acceptance checklist for that release: *"**regression:** old typed keywords still work + `/checkin?token=` still works"* (PM, owner ran it, 2026-07-29). ⇒ typed keywords were a deliberate, owner-accepted, permanent path.
- **2026-09-02 side** — `REQ-079` rule 2, quoted repeatedly by the PM: *"Only a button starts a flow. A typed keyword never does."* — and the PM's reason: *"That rule exists because the customer's own call named 'commands triggered accidentally' as their fear"* (PM, 2026-09-02). The deployed build nonetheless starts flows from `สมัคร`, which `SYSTEM-FACTS.md` already records as open with @Sober.
- **What the contradiction actually is:** REQ-079 rule 2 does not merely describe unbuilt behaviour — **it reverses a path the owner accepted in a previous release and regression-tested himself.** Whoever closes the open question needs to know that removing keyword entry is a *removal of accepted behaviour*, not a *gap in a new feature*.
