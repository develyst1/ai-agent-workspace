# Extraction — `log/2026-08-31.md` (26,361 bytes · 334 lines · read in full)

> Archaeology run, ORDER 3. Candidate SYSTEM-FACTS lines only. Marie merges; nothing here was written into the repo.

---

## Business rules the owner set

- 🔴 **EVERY booking must have a teacher. An อื่นๆ (OTHER) booking may carry SEVERAL teachers.** Owner: *"ทุกการจองต้องมีครู แค่การจองนั้น อาจจะครูหลายคนได้"* (owner, 2026-08-31). This **widened** the requirement: the teacher-less case is gone, and multi-teacher is a **new capability** — the four existing booking types have always been exactly one teacher.
- **Multi-teacher is อื่นๆ ONLY.** Owner confirmed on the follow-up: *"1. yes"* ⇒ the other four types still take exactly one teacher (owner, 2026-08-31).
- 🔴 **An อื่นๆ booking draws NO teacher's freelance budget, for every assigned teacher.** Owner: *"2. yes ถ้าครูหลายคน ไม่ต้องมีให้ใส่การได้ตังค์"* (owner, 2026-08-31). A meeting or a closure is not a taught lesson.
- **With MORE THAN ONE teacher the teacher-pay input is not offered — ABSENT, not disabled-and-ignored.** A rule the owner added unprompted (owner, 2026-08-31). A field that silently discards its value tells the admin a payment was recorded when none was.
- **`การได้ตังค์` in that sentence means the TEACHER'S PAY, not the customer charge.** ⇒ **the `คิดเงินรายการนี้` (bill the customer) toggle is unaffected by the teacher count** — a multi-teacher อื่นๆ can still bill the customer, it just cannot pay a teacher (owner, Q7 answer, 2026-08-31).
- **An อื่นๆ booking with a teacher sends the LINE; one without sends NOTHING — by design, with no failed outbox row, no retry and no alert.** Owner: *"ถ้ามีครูก็ส่งไลน์ ไม่มีครูก็ไม่ต้อง"* (owner, 2026-08-31).
- **TASK-220 (cancelling a 1st Trial) shipped to `uat` long ago and the customer uses it daily.** Owner: *"deploy TASK-220 ไปนานแล้วโว้ย เขาใช้แล้ว"* (owner, 2026-08-31), correcting the PM who had twice reported it as never deployed.

## Product limits

- 🔴 **There is NO per-booking teacher-pay input anywhere in the product — BE or FE.** Verified rather than remembered on 2026-08-31 (Sober): the `createBooking` request schema has no pay/earning/rate field; the `bookings` table has no pay column; a BE grep for `teacherPay|payMinor|earning|teacher_rate` finds only `rateMinor`, **which is set on the TEACHER, never on a booking**; an FE grep for `teacherPay|payMinor|earning|ค่าสอน|ค่าตอบแทน` returns **zero matches in `src/`**.
- 🔴 **Teacher money in this product is ONLY the freelance monthly ceiling, drawn automatically by attendance (REQ-001/004). There is no other mechanism** (Sober, 2026-08-31). ⇒ if the owner ever expects to *type* a teacher's pay on a booking, that is a NEW requirement — and on his screen **"the field is absent" looks identical to "the feature was built."**
- 🔴 **The calendar is a GRID OF TEACHER COLUMNS** — `getCalendar` keys every cell `date|teacher.id|startTime`. **A teacher-less booking would save and then be invisible on the one screen the feature exists to fix**; making it visible is a calendar redesign, not a nullable column (Sober, 2026-08-31). This is what produced the owner's "every booking must have a teacher" ruling.
- 🚫 **There is deliberately NO placeholder `อื่นๆ` row in `subjects`.** REQ-065 exists *because* `1st Trial` sitting in that table leaked into the program picker and had to be filtered back out at `toTeacherDTO`; a fake program would leak the same way — picker, `link-all`, `price_group` (Sober, 2026-08-31).
- **Today, ANY booking on a FREELANCE teacher draws an hour the moment its status holds** — `reconcileBookingHolds` → `heldTarget(status)` → `fl:<bookingId>:<item>:held1`. **`heldTarget` is status-only; it has never had a booking-type opinion** (Sober, 2026-08-31). Without the OTHER guard, a meeting would quietly eat an hour of a freelance ceiling and the first symptom would be a teacher hitting their cap early.
- **`recordSale` cannot post an arbitrary amount** — it derives the amount from a `SALE_ITEMS` product code (Sober, 2026-08-31). Posting by item id with an explicit amount needs a sibling function.
- **"Free" and "sold for nothing" are different claims to the books: no charge means NO MOVEMENT AT ALL, never a ฿0 row** (Sober, 2026-08-31).
- **There is still no student-delete anywhere in the product** (Porter, restated 2026-08-31 while flagging that LINE would become a WRITE path into the student roster).
- 🔴 **`bun test` in `smart-scheduler-back` reaches the LIVE `sid` DB** via `eligible.route.test.ts:13` (Bun loads `.env` before the `??=`). It is refused at auth (`28000`) from a non-whitelisted machine, **but on a whitelisted machine the same command reads real rows — and it is the command every DoD we write tells an engineer to run** (Sober, 2026-08-31, promoted out of TASK-218 as its own risk).

## Platform / migration behaviour

- 🔴 **`ALTER TYPE … ADD VALUE` must not share a transaction with any statement that USES the new value** (Sober, migration trap written into TASK-224, 2026-08-31).
- 🔴 **`DROP NOT NULL` is NOT witnessable by existence.** *"Does `student_id` exist?"* is true before and after — which is exactly how migration `0022` hid. **Witness on the new CHECK instead** (Sober, 2026-08-31).
- **Two tasks can both claim the same migration number; whichever lands second re-counts and takes the next one** (Sober, on `0028`, 2026-08-31).
- **A `booking_type` enum migration means `sid` FIRST, verified, and the TASK must state how it was proven there** (standing rule restated twice, Porter 2026-08-31).
- **The FE lives in the SHARED `smart-scheduler-front` repo ⇒ read `develop` before building** (standing rule restated twice, Porter 2026-08-31).

## Terminology

- **`อื่นๆ` (OTHER) is the owner's REQ-005 and the board's REQ-078** — he keeps calling it REQ-005; use his number when talking to him (Porter, 2026-08-31).
- **"DONE" from the SA means the code is written and reviewed, not that it reached the customer.** *"Deployed to `uat`" has NO HOME FILE* — TASKs hold code state, the board holds requirement state, and the single fact the owner cares most about is recorded only if someone happens to write a line (Porter, 2026-08-31).

## Working agreements

- 🔴 **Porter's new standing habit: every `uat` deploy is written into the day's log AND `PROJECT-STATUS.md`** (Porter, 2026-08-31, after asserting a shipped task was undeployed because no file recorded it).
- 🔴 **Absence of evidence is not evidence.** Silence in the record was read as "not done" and asserted as fact; the owner corrected it. Sober made the mirror-image error the same day and **reported the observation, not a conclusion** (Porter and Sober, 2026-08-31).
- **An impossible acceptance criterion is WITHDRAWN, not deferred** — *"an impossible case handed to QA gets marked 'pass'"*, and a branch guarding a state the schema forbids is dead code the next reader takes for a supported case (Porter and Sober, 2026-08-31, on AC-17).
- **Nothing is started against a machine summary of speech.** REQ-079 came from a NotebookLM summary of a customer call (`call-20260831.md`, in the code repo) and was filed **CAPTURED, not `READY_FOR_SA`** — the document's own next step is the owner producing a design flow + timeline for approval **before** development (Porter, 2026-08-31).
- **A summary bullet does not get to quietly undo a safety decision.** The call summary said to show real names; **TASK-047 fixed a LINE pairing PII leak and REQ-051 holds the rule that a phone number is not a secret** — Porter asked whose eyes rather than accepting it (Porter, 2026-08-31).
- **Housekeeping precedent: `board.md` is split by SHAPE, not wording** — every row whose status is DONE / DELIVERED / CODE ACCEPTED moves verbatim to `archive/board-closed.md`; rows with QUALIFIED statuses (e.g. `DELIVERED (+1 fix pending)`, `Part A DONE; B.1 cut`) STAY, and **when in doubt the row stays** (Porter, under Marie's owner-approved order, 2026-08-31). Nothing is reworded, re-ordered, re-numbered or re-statused; a verbatim archive is taken first and md5-checked.

## Unsorted — 2026-08-31

- 🔴 **Three "built" tasks (TASK-218/221/223) could not be found in the repo `machine.local.md` names** — clean working tree, all branches at the same commit, no matching code on `develop`/`dong`/`main`/`production`/`origin/*`, migration count 28 with newest `0027` so **no `0028` existed** (Sober, 2026-08-31). **Reported as an observation, not a conclusion.** ⚠️ If the work went to a path this checkout cannot see, that is a **workspace problem, not a task problem** — I could not determine from this log whether it was ever resolved.
- **A multi-teacher booking is ONE booking row plus a `booking_teachers` join table holding only the ADDITIONAL teachers, `OTHER` only** — `bookings.teacher_id` stays `NOT NULL` and holds the first teacher (Sober's data model, 2026-08-31). ⇒ "cancels as one" is free, and "the other four types take exactly one teacher" is a property of the schema rather than a guard someone must remember.
- **One accessor, never two reads:** a single mapper returns `teachers = [row.teacher, ...additional]` and nothing outside it reads either source (Sober, 2026-08-31). Two call sites reading two sources is how the two get to disagree.
- **The DTO carries `teachers[]` for EVERY booking type (length 1 for four of them)** so the FE gets one shape, not a special case (Sober, 2026-08-31).
- **The day-end engine has NO booking-type condition** (`jobs.service.ts:42-72`), and "consume" simply IS setting `courseId` / `voucherId` — the existing deduction already honours it (Sober, grounding REQ-078, 2026-08-31). ⚠️ Bears on the day-end contradiction reported below.
- **Cancel-with-a-stored-reason is one line — migration `0025`'s column and CHECK already carry the values** (the TASK-220 shape) (Sober, 2026-08-31).
- **One computed `displayName` (`title ?? nickname ?? name`) exists on every booking type rather than letting 31 FE call sites each invent a fallback** (Sober, 2026-08-31).
- **`rev:<bookingId>` is reused by the new posting path, so the "revenue already posted" warning covers อื่นๆ the day it lands — no second lookup, no type list** (Sober, 2026-08-31).
- **LINE becoming a WRITE path into the student roster (a parent creating a student) is a NEW capability** inheriting every REQ-059/060 lesson (Porter, 2026-08-31).
- **The 08-31 customer call independently confirmed the owner's 08-30 reframe: a hybrid bot + human admin in ONE account, reachable from the customer's side too** — now settled from two directions rather than one (Porter, 2026-08-31).
- **The pieces REQ-079 would need mostly exist already:** phone→children (REQ-051), duplicate-child safety (REQ-050), re-linking (REQ-020), admin+bot (REQ-077). Recommended to the owner as **sequenced WITH REQ-051** — same lookup, same PII question, same neutral-message rule (Porter, 2026-08-31).

---

## ⚠️ CONTRADICTIONS — reported, not resolved

### C6 — Does the day-end job auto-attend, or mark `NO_SHOW`? (third sighting; see C1/C4)
- **(A) `NO_SHOW`.** *"It flips today's `CONFIRMED` bookings whose end time has passed to `NO_SHOW` and increments `used_sessions`/`used_hours`"* — Porter, from `jobs.service.ts:47–61`, **2026-08-23**.
- **(B) It auto-attends.** *"day-end auto-attends it like everything else"* — Porter relaying the owner, **2026-08-30**; and **2026-08-31**, Sober grounding REQ-078: *"The day-end engine has **no booking-type condition** (`jobs.service.ts:42-72`) — so **AC-9 (auto-attend)** … cost nothing."* Same file, same job, opposite verb, eight days apart. SYSTEM-FACTS today carries (B).
- Reported in all three of my files because all three carry a side of it. **Not resolved.**

### C7 — Is the teacher-pay control "absent" a build or a verification?
- **(A) It must be built.** The owner's rule, **2026-08-31**: with more than one teacher the money input *"ไม่ต้องมีให้ใส่"* — absent, not ignored; Porter wrote AC-22/AC-23 on it.
- **(B) Nothing to build — the control has never existed.** Sober, **2026-08-31**, verified by grep in both repos: no pay field on any booking type. *"AC-22 and AC-23 collapse to a verification."*
- Not a contradiction of fact, but **a contradiction of expectation that the log itself flags as dangerous**: *"on his screen 'the field is absent' will look identical to 'the feature was built.'"* Recorded so the distinction is not lost.

---

## ⚠️ REFINES EXISTING — same fact, earlier date or the missing attribution

- **"There is NO rate limiter anywhere in the codebase."** SYSTEM-FACTS attributes this to REQ-051 speccing, undated. **Earlier corroboration:** Porter, **2026-08-30**, listing the three REQ-051 risk decisions he holds — *"the rate-limit model (nothing exists in the codebase)"*.
- **The accepted security risk: "LINE entry is the phone number alone."** SYSTEM-FACTS dates the acceptance to the owner on **2026-09-02**. **Earlier root:** on **2026-08-31** Porter records that **REQ-051 already held the rule that a phone number is not a secret**, and that **TASK-047 fixed a LINE pairing PII leak** — so the phone-is-not-a-secret position predates the 09-02 acceptance, and the PII lesson it rests on is older still.
- **"Nothing with history can be removed — only hidden."** SYSTEM-FACTS states it for students. **Second instance, dated:** **2026-08-31**, Sober on why no placeholder `อื่นๆ` subject row may exist — a fake program leaks into the picker, `link-all` and `price_group`, exactly as `1st Trial` did (REQ-065).
