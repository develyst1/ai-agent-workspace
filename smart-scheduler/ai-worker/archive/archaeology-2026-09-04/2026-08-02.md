# Archaeology extract — `smart-scheduler/ai-worker/log/2026-08-02.md` (110,476 bytes, 1473 lines, read in full)

> Extracted for Marie, ORDER 3. Format matches `SYSTEM-FACTS.md`. Nothing merged; nothing resolved.
> ⚠️ Note on pronouns: **this log refers to the owner throughout as "she/her"**, while `SYSTEM-FACTS.md`
> and the 2026-08-29 log say "he/him". Recorded under CONTRADICTIONS, not resolved.

---

## Telling the two boxes apart

- **`154.197.124.206` IS `sid`, the dev server** — owner, 2026-08-02, verbatim: *"154.197.124.206 = sid (dev server) ใช้ได้เลย เต็มที่ แค่นายต้องตามการใช้ของเขาได้ ต้อง track ได้ แค่นั้น พอ"*
- **`frontoffice.develyst.online` is PRODUCTION and is off-limits to every role** (Porter, restated four times, 2026-08-02).
- **`som.develyst.online` is `sid`'s frontoffice**, and it sits behind a NextAuth login form (observed, 2026-08-02).
- **`sid` has ONE shared staff login** — the owner works on the same box under the same account, and **the system has no "other user" to distinguish an agent by** (owner's condition + Porter, 2026-08-02).
- 🚨 **`env -u DATABASE_URL bun run …` DOES NOT isolate you from the live box — Bun auto-loads `.env`, and `.env` wins.** Measured by Jason when two scripts connected to `sid` unintentionally; **raised to PROTOCOL above the no-real-database rule** (2026-08-02).
- **The checked-in `.env` of the "local" backend sends a "local" run to the REMOTE `sid` database** (found by Tanya, confirmed 2026-08-02). ⇒ *"I'm not connected"* is never provable by unsetting a variable; **the safe form of a run that must touch nothing is not to start it.**
- **Anything such an unintended run displays is NOT a sanctioned DATA REQUEST answer** (PROTOCOL, Porter, 2026-08-02).
- **Credentials live in `H:\sm-test-access.txt`, outside the workspace, and never enter any tracked file, log, screenshot or paste** (owner-provided; Porter, 2026-08-02). **`AUTH_SECRET` was added to that same file by the owner on 2026-08-02.**
- **The backoffice and scheduling repos each keep their own migrations ledger since 2026-08-02** (`migrations: { table, schema }` in both `drizzle.config.ts`; they shared one default `__drizzle_migrations` table before that, which silently blocked every scheduling migration).
- **`db:migrate` is `drizzle-kit migrate && bun run db:verify`** since 2026-08-02 — it cannot report "done" without the verify, which **exits 1** and distinguishes *silently-skipped* from *never-ran* (observed, 2026-08-02).

## Product limits

- **There is no delete endpoint for a parent, a student, a voucher, or a posted `bo.movement`** — QA could not remove any of them after a test round and had to leave them inert and marked (observed on `sid`, 2026-08-02).
- **There is no voucher import route** — `POST /vouchers/import` returns 404 on `sid` (observed, 2026-08-02).
- **The API accepts a booking dated months in the past** — this is what let QA create a genuinely expired voucher, and it also means a mistyped first-booking date can set a voucher's window to already-expired (observed, 2026-08-02; flagged to the owner as a business call, never answered that day).
- **`backoffice-front` has NO i18n layer** — "TH+EN" there is not a translation-key change; it needs its own task or an explicit "Thai-only is fine" (observed, 2026-08-02).
- **Course and voucher items carry NO finite stock balance** — `ceilingQty`/`remainingQty` exist only for freelance ceilings. **A course is a service and cannot run out** (Porter, checked in `lib/sale-post.ts`, 2026-08-02).
- **The backoffice `ops` schema is RETIRED** (by REQ-006/TASK-027) — `routes/api.ts:11` mounts only `/auth` and `/bo`, and `routes/catalog.ts`, the ops surface, **is mounted nowhere**, so those tables are unreachable over HTTP (verified 2026-08-02).
- ⚠️ **But `resolveOrganization` reads `organizations`, and `pricing.service` / `parties.service` import it** — so at least one table in the `ops` family may still be load-bearing. **"Retire `ops`" is an investigation with a destructive ending, not a delete** (Sober's ruling, 2026-08-02).
- **Backoffice migrations `0001_item_pl` and `0002_item_external` are permanently `retired`** — they touch only `ops.*`, they never applied, they never will, **no ledger row is written for them, and `db:verify` prints them with their reason on every run and stays green** (Sober's scope decision, 2026-08-02). ⚠️ **If they ever stop being printed, that is the regression** — the value is in their being visible, not excused.
- **A full page reload resets the in-memory mock** in the FE mock mode — a save made before a reload "vanishes", which silently invalidates any mock-mode check done that way (Fern, 2026-08-02).

## LINE

- **LINE and email on `sid` reach REAL humans** — "never message a real person" was already non-negotiable on 2026-08-02 (owner's rule, carried by Porter).

## Business rules the owner set earlier and that were never written down

- **All prices on both cards are VAT-INCLUSIVE and are posted as-is** (Porter restating the owner's rule, 2026-08-02).
- **The real per-program price list is `project-docs/real-price-list-2026-08-01.md`**, and `sale:ensure-items` is only safe once it carries those prices — running it earlier creates items at placeholder prices and every sale from that moment posts 30–55 % high (owner: *"ยังไม่รัน"*, 2026-08-02).
- **`1st Trial` is a one-hour one-off product, not a package** — it correctly has **no price group**, and an attempt to sell a course or voucher against it **must be refused like any other combination that does not exist**. Owner: *"ได้ ก็ตามเดิม ตามกฎเลย"* … *"ตามกฎเลย"* (owner, 2026-08-02).
- **Revenue by sport lives on the BACKOFFICE; the frontoffice answers "how many students per sport", the backoffice answers "how much money per sport."** Same dimension, different question, different audience — consistent with the 2026-07-25 meeting's decision to keep finance off the frontoffice (owner's placement call, 2026-08-02).
- **The owner's own model of a course sale**, verbatim: *"คอร์สพวกนี้มี stock อยู่ฝั่ง backoffice นะ มีคนมาสมัครคอร์ส ก็ตัดของออกผ่าน API แล้วได้รายได้"* ⇒ a sale writes a movement with **`qty = −1` (OUT)** and `value_minor = −qty × unit_price`; **a course sale is literally stock going out and revenue coming in** (owner, 2026-08-02; confirmed against `lib/sale-post.ts`).
- **A voucher's expiry is recomputed from the date of its FIRST booked session** — a 5 h voucher whose first session was booked on 2026-01-05 had its expiry recomputed to **2026-04-05** (observed on `sid`, 2026-08-02).
- **REQ-009 is LOW priority because the feature is barely used** — owner: *"ไม่ด่วน ใช้น้อยมาก"* (owner, 2026-08-02).
- **Owner on the `sid` outage — not urgent, fix the cause:** *"ไม่ด่วน ทำความเข้าใจว่าปัญหาเกิดจากอะไร แล้วแก้ให้ไม่เกิดขึ้นอีก แบบนี้ไปเกิดที่ production ไม่ได้เด็ดขาด"* ⇒ **`sid` being down is not an emergency; the same fault reaching production is unacceptable** (owner, 2026-08-02).

## Working agreements the owner has stated

- 🔴 **The owner's ONE condition on QA using `sid`: usage must be TRACEABLE** — *"ต้อง track ได้"*. Not "be careful" — a mechanism (owner, 2026-08-02).
- **The mechanisms that implement "ต้อง track ได้", all still in force:** every record QA creates carries a visible **`QA-` prefix** in a human-readable field · **never edit a row you did not create** · **`## Test data created` in every TEST file, including what could NOT be removed and why** · **one running ledger, `tests/DEV-SERVER-FOOTPRINT.md`** · **never message a real person** (Porter, implementing the owner's condition, 2026-08-02).
- **QA writes on `sid` were approved by the owner under the rules already in force, loosening nothing** — owner: *"ได้ ก็ตามเดิม ตามกฎเลย"* (owner, 2026-08-02).
- **The owner asked that QA take over the testing from her** (owner, 2026-08-02).
- **Nobody on the team runs an operator sequence against a real box — the owner runs it.** Every migration/deploy sequence is written verbatim for her to execute, dry-run first (standing rule, repeated by Porter and Sober all day, 2026-08-02).
- **QA will not type a credential into a login field** — a hard harness safety rule; the owner's credentials are used only for programmatic API calls (Tanya, 2026-08-02; Porter: *"correct and I'm not asking you to revisit it"*).
- **A QA write on `sid` needs the human to approve the harness permission prompt as it comes** (established 2026-08-02).
- 🔴 **`AUTH_SECRET`, the backend token and the minted session cookie never go into a TEST file, `REGRESSION.md`, `DEV-SERVER-FOOTPRINT.md`, a log entry, a screenshot, or any pasted output** (Porter, 2026-08-02).
- **`scripts/mint-session.mjs` refuses production (`frontoffice.develyst.online` → exit 1) and that refusal is never worked around** — if someone thinks they need it, that is a decision routed up the chain (2026-08-02).
- 📌 **"An unblock is not a verification."** The standing phrasing from that day: *"This makes the painted checks TESTABLE. It does not make them TESTED."* — item 5 stayed open four rounds because the two kept being conflated (Porter, 2026-08-02).
- **A stakeholder acceptance is not a QA pass** — the owner testing something herself and saying *"ฉันเทสไปแล้วนะ ก็ดูดี และผ่าน"* is recorded as her acceptance, not as a QA verdict; both are worth having and neither launders into the other (Porter, 2026-08-02).

## ⚠️ REFINES EXISTING

**1. "QA cannot test LINE. The owner is the only LINE-capable tester."**

- SYSTEM-FACTS says: *"(established 2026-09-03) … It had been true for weeks and was never written down."*
- **This log carries it on 2026-08-02, five weeks earlier, in Porter's own words:** *"the claim arrives through LINE and **you have no LINE client** — so end-to-end is `NOT TESTED`, in those words. If you can describe the click-path the owner would need to follow on her phone, hand it to me and I'll pass it on; **she is the only one who can press those buttons.**"*
- **And the owner-is-the-hands / QA-is-the-verdict split is already the stated pattern on 2026-08-02**, named by Porter as *"the same pattern you already use for LINE"* when he chose option (a) for painted screens.

**2. "There is NO student-delete anywhere in the product."**

- SYSTEM-FACTS states it undated and student-only.
- **This log dates it 2026-08-02 and widens it:** Tanya, after Round 2 on `sid` — *"❌ **Cannot remove** (no delete endpoint): the parent, student, voucher, and the voucher-sale `bo.movement`"*. ⇒ **the absence covers parents, vouchers and posted movements too, not only students.**

## ⚠️ CONTRADICTIONS

**C1 — the owner's gender / identity.**

- **2026-08-02 log, throughout:** the owner is *"she"* / *"her"* — e.g. *"the owner told me plainly it is not urgent"* … *"nearly had **her** do ten minutes of rollback work"*; *"**She** gave a worked example"*; *"**she** is the only one who can press those buttons"*.
- **`SYSTEM-FACTS.md` and the 2026-08-29 log:** the owner is *"he"* / *"him"* — e.g. *"**The OWNER changed it himself**, 2026-08-29"*; *"**he** replied, and no `[line-in]` was logged"*; 2026-08-29: *"**The owner is starting his `sid` test pass now.**"*
- Also on 2026-08-02 the **customer** is named separately as **คุณปุ้ม**, referred to as *"he"*: *"🆕 REQ-034 — คุณปุ้ม's follow-up after seeing it. **He** wants the dashboard filterable…"*
- **Not resolved.** Either the role changed hands, or one of the two is a persistent writing error. Marie's call.

## Unsorted — 2026-08-02

- **The catalogue's 8 named subject/price groups**, as `0016` backfills them by matching subject **name exactly**: **Bike / Scooter / Balance Cruiser · Surfskate · Freeskate · Skateboard · Inline Skate · Onewheel E-Skate · Balance Play (Private) · Balance Play (Group)**. **`1st Trial` is a ninth subject and deliberately carries no price group** (verified on `sid` by QA, 2026-08-02). ⚠️ **A subject name off by one character keeps a NULL price group and loses its prices later, silently.**
- **Sellable (program, size) combinations, verified server-side on `sid`:** the **bike/skate family offers 4 / 6 / 10** · **Onewheel offers no 10 h** (1/4/6) · **Balance Play, private and group, offers no 4 h** (1/6/10). The server refuses a non-existent combination with a Thai 400 (`"ไม่มีแพ็กเกจ 10 ชั่วโมง"`), not just a hidden dropdown option (observed, 2026-08-02). *(Classification note: this is REQ-027's subject matter, so it may belong in the REQ rather than here.)*
- **Go-live was 18 days out on 2026-08-02** ⇒ the target date in force that day was **2026-08-20**, and QA was told a real defect stops a REQ closing *"regardless of the 2026-08-20 date"* (Porter/Sober, 2026-08-02).
- **`MAX_WEEK_BY_SIZE`'s "6 → week 8" was an unconfirmed guess as of 2026-08-02** — harmless while leave was rare, but it starts refusing real families once planned absences become a normal purchase-time action (Sober, 2026-08-02, re-raised to the owner). *(See the 2026-08-29 file: the owner later stated the rule as `MAX_WEEK = size + leaveQuota`, 6+2=8 — which confirms this guess.)*
- **The FE role's browser pane does not composite — screenshots have failed for Fern on every task** — while **QA's browser does paint**; she is stopped only by the login form. *"My inability to screenshot is evidence about me, not about her"* (Fern + Sober, 2026-08-02). ⇒ **a role's environment ceiling is not evidence about another role's environment.**
- **`db:migrate` cannot half-apply** — `drizzle-orm/pg-core/dialect.cjs:62` wraps the whole migrate run in one `session.transaction(...)`, so any failure rolls the batch back. **The exposure is out-of-band application**, and there was exactly one confirmed instance: the deleted `scripts/db-check-migrate.ts`, which ran statements individually with no transaction (verified independently by Jason and Sober, 2026-08-02). *(Classification note: derivable from library code — included because it bounds what an operator has to fear during a migration.)*
- **An emptied ledger is NOT protected by ordering** — with no rows, drizzle attempts everything from `0000`. **What actually stops it is that the baseline `0000_friendly_moonstone` opens with `CREATE SCHEMA "ops";` and carries 42 `CREATE`s and zero `IF NOT EXISTS`** → `42P06`, and the one-transaction rule rolls the whole batch back. **Loud, harmless and structural** (Jason, corrected Sober's framing, 2026-08-02).
- **In Auth.js v5 the session-cookie encoder's `salt` IS the cookie name**, and the `__Secure-` prefix depends on the protocol — so the name must be derived from `APP_URL`. **The wrong salt yields a structurally valid cookie the app cannot decode — a broken session that looks fine** (Fern, read from `@auth/core` source and proved, 2026-08-02). *(Classification note: library behaviour, arguably derivable from code.)*
