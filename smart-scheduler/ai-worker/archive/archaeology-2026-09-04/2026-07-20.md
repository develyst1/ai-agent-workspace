# Extraction — `log/2026-07-20.md` (182,042 bytes / 2,113 lines, read in full)

> ⚠️ **Date caveat, from the log's own header:** this is a **MIXED-DATE file**. A protocol gap (fixed
> 2026-07-27) let every session keep appending here. Its 99 entries span **several working days**
> (2026-07-20 → ~2026-07-25); the `[HH:MM]` headings wrap past midnight about five times and the
> entries carry no dates of their own. **Every fact below is tagged `2026-07-20` because that is the
> only date the file asserts** — the true day may be up to five days later. Marie: do not treat these
> timestamps as precise, only as "founding week".
>
> ⚠️ **Vocabulary caveat:** this era's logs call the decision-maker **"the stakeholder"** and
> **"คุณฟีน" / "พี่ฟีน"**, apparently interchangeably. One entry ([19:10]) distinguishes them —
> *"Stakeholder gave placeholder go-live numbers (customer พี่ฟีน will refine later)"* — implying
> พี่ฟีน is the **customer**, not the owner. I have attributed each line to `owner` where the log says
> "stakeholder" and `owner-as-"คุณฟีน"` where it says คุณฟีน, **without resolving whether they are the
> same person.** See `## ⚠️ CONTRADICTIONS`.

---

## Schedules

- **The whole product has exactly TWO scheduled jobs in this era: `end-of-day` (nightly) and `month-reset` (1st of the month), both on scheduling-back `:4006`** (Porter, 2026-07-20).
- **`end-of-day` = `POST :4006/internal/jobs/end-of-day`, header `x-internal-secret: $INTERNAL_JOB_SECRET`** (Sober, verified against code, 2026-07-20).
- **`month-reset` = `POST :4006/internal/jobs/month-reset`, run on the 1st of the month** (Porter, 2026-07-20).
- **The old backoffice `month-start` job on ops `:4010` (`POST /api/v1/internal/jobs/month-start`, `X-Service-Token`, body `{month:"YYYY-MM"}`) is RETIRED** — freelance reset moved to `:4006`, FT/PT salary deferred, backoffice being rebuilt (Porter, 2026-07-20).
- **`month-reset` is deliberately idempotent-per-month**: it records the last-reset month in `app_settings` and no-ops if already reset; a `force` flag exists for a deliberate re-run (Sober, 2026-07-20). **Reason it was mandated: an accidental mid-month re-run would wipe that month's drawdowns.**
- **The `end-of-day` job is best-effort and must never fail**: re-run safe, idempotent per booking (`rev:<bookingId>`); if the revenue item is unseeded or the target is down it **skips silently** (Jason, 2026-07-20).
- **Scheduled tasks are Windows Task Scheduler tasks registered by hand on the server by the owner** — registering them is a human step, never an agent step (Porter, 2026-07-20).
- 🔴 **"The app being up ≠ the cron being registered."** Porter had to warn explicitly that a successful deploy proves nothing about whether the two scheduled tasks exist (Porter, 2026-07-20).
- **The `job_runs` summary has recorded `revenuePosted` since the day-end revenue tally was first built** (Jason, 2026-07-20).

## Product limits

- **Bookings are ALWAYS created `PENDING`. The commit point is the `confirm` action (PENDING→CONFIRMED); there is NO direct-CONFIRMED path** (Jason, read from the real booking flow, 2026-07-20).
- **Every booking is exactly 1 hour and carries NO teaching-mode field** (Sober + Jason, 2026-07-20). This is why the freelance job amount is simply `rate × 1h`.
- **Teachers can NEVER be hard-deleted** — the bookings FK blocks it, so removal is always a soft archive that keeps all history (Sober, 2026-07-20). See `## ⚠️ REFINES EXISTING`.
- **There is no subjects/programs catalog in the product.** The teacher form's subject picker is populated from the **union of existing teachers' subjects** — you cannot create a brand-new program/subject through the UI (Fern, 2026-07-20; accepted for launch by Sober, routed to the owner as a known limitation).
- **The teacher↔backoffice sync (`/internal/teacher-sync/*`) was built but NEVER deployed and later unmounted** — no teacher-identity sync exists in the running product (Porter + Jason, 2026-07-20 / 2026-07-27).
- **There is no cross-system 2-phase commit anywhere.** The accepted reason it does not matter: **this centre books manually, at low volume** (Sober, 2026-07-20).
- **The staff `/scheduler/teachers` page cannot be rendered locally.** It is NextAuth-gated and the configured API is the live frontoffice env, so a local run redirects to the **production** login at `frontoffice.develyst.online` (Fern, 2026-07-20).
- **`smart-scheduler-front/.env.example` deliberately points at a prod domain (`som.develyst.online/api`), not a localhost port** (Sober, verified, 2026-07-20).
- **Both frontends' `bun run lint` is broken repo-wide** because Next 16 removed `next lint` — pre-existing, not caused by any task (Fern, 2026-07-20).

## LINE

- *(This log predates any LINE work. No LINE facts. Earliest LINE evidence in this batch is in `2026-07-26.md`.)*

## Telling the two boxes apart

- **`som.develyst.online` = the scheduling / front-office app. `backoffice-som.develyst.online` = the backoffice app.** Both were live on the same server in this era (Porter, 2026-07-20).
- 🔴 **`frontoffice.develyst.online` is the PRODUCTION login domain** for the staff front end — distinct from `som.develyst.online` (Fern, 2026-07-20).
- **The canonical port map, given by the owner and made canonical in `docs/monorepo-overview.md` + root `CLAUDE.md`: scheduling-front `3016` · scheduling-back `4006` · backoffice-front `3018` · backoffice-back `4010`. Scheme: backends `40xx`, frontends `30xx`** (owner, 2026-07-20).
- ⚠️ **The old/stale ports `3000`, `3001`, `3002`, `3100` appear all over the founding docs and are WRONG** — Porter had to correct them twice (owner + Porter, 2026-07-20).
- 🔴 **ONE PostgreSQL database, `smart_scheduler`, holds BOTH schemas: `public` (frontoffice) and `bo` (backoffice). The separate `smart_backoffice_db` and its `ops.*` schema are ABANDONED** (owner's directive; incident-corrected 2026-07-20).
- 🔴 **The "one PostgreSQL, two schemas" line was WRONG as-deployed for a while — production actually ran TWO SEPARATE DATABASES**, which broke the atomic-drawdown premise. The lesson Sober logged verbatim: **"for a 'shared DB' design, verify the actual `DATABASE_URL`s match — not just the schema code"** (Sober, 2026-07-20).
- **The code monorepo lives at a DIFFERENT absolute path on each of the owner's machines** — `C:\Users\Admin\develyst\smart-scheduler` on one, `H:\scheduler` on another (observed, 2026-07-20 / 2026-07-26).
- **The two backends SHARE one drizzle journal (`drizzle.__drizzle_migrations`)**, so `bun run db:migrate` **silently skips** a migration whenever the journal's latest `created_at` is newer than the new file's. It has bitten the project at least twice (ops `0003`, bo `0004`) (observed, 2026-07-20). ⇒ **`psql -f drizzle/<file>.sql` is the PRIMARY migration method, not a fallback** — the deploy doc was changed to say so (Porter, 2026-07-20).

## Working agreements the owner has stated

- 🔴 **"Everyone must understand everything first so I can assign work correctly."** The owner required the whole team to build a shared, verified understanding of the project **BEFORE any work was assigned** (owner, 2026-07-20).
- 🔴 **DESIGN-FIRST on anything big.** For the backoffice rebuild the owner demanded to **see the DB design (as-built + proposed) before any build**; the deliverable of that requirement was a design doc, not code (owner, 2026-07-20).
- 🔴 **The owner deploys and enters the real data himself, after the team finishes** — *"ฉันจะทำเองหลังจากทีมเสร็จ"* (owner, 2026-07-20). A dev seed is therefore optional; numbers can be typed through the admin screens.
- 🔴 **Live acceptance is run by the HUMAN; the agent only verifies the reported results.** Established on REQ-001/REQ-002 and named as "the brownfield pattern" (Porter, 2026-07-20).
- 🔴 **"Brownfield rule" is this project's own term for: engineers NEVER connect to the shared Postgres or any real environment.** DB-runtime behaviour is verified by inspection + typecheck + unit tests only, and that posture is explicitly *accepted* by the reviewer rather than treated as a gap (Sober, 2026-07-20).
- **Deploying is a Porter→human step, and order-sensitive deploys get a written runbook the human can copy-paste** (Porter, 2026-07-20).
- **Everything written for the owner is translated to Thai; everything inside the team stays English** (observed throughout, 2026-07-20).
- **`docs/` files are SYNCED (copied) into all 4 repos' `docs/` directories** whenever one changes — a rule that lives in the monorepo root `CLAUDE.md` (observed, 2026-07-20).
- **`docs/requirement-timeline.md` is "the living spec, newest entry wins"** (System entry, 2026-07-20).
- **The chain is HARD and was made explicit ON 2026-07-20** after Porter over-reached: allowed pairs are Human↔Porter, Porter↔Sober, Sober↔Jason, Sober↔Fern. **Porter must never `@Jason`/`@Fern`; engineers must never `@Porter`; Jason↔Fern coordinate only through Sober's SPEC/TASK contracts.** A message that skips the chain is not acted on — you log `Routing violation: please send this via <role>` (System, 2026-07-20).
- **The PM does not set task status and does not assign engineers.** Porter reverted his own entry for doing exactly that (Porter, self-correction, 2026-07-20).

## Business rules the owner set earlier and that were never written down

- 🔴 **Revenue recognition is PER BOOKING TYPE, not one global rule** (owner, 2026-07-20):
  **`FIRST_TRIAL` → revenue at ATTENDANCE · `SINGLE_SESSION` → revenue at ATTENDANCE · `COURSE_PACKAGE` → revenue at PURCHASE/CONFIRM · `VOUCHER` → revenue at PURCHASE/CONFIRM.** Booking a course/voucher onto the calendar only **consumes a pre-bought entitlement** and is NOT revenue.
- 🔴 **⇒ The day-end revenue tally counts ATTENDED `FIRST_TRIAL` + `SINGLE_SESSION` ONLY. Course and voucher post NOTHING at day-end** — that is what prevents a double-count (owner via Porter, 2026-07-20).
- 🔴 **The end-of-day summary is REVENUE ONLY.** It does **not** compute expense and does **not** touch freelance budgets. The owner corrected Porter's write-up specifically to say this (owner, 2026-07-20).
- 🔴 **P&L = revenue (attended, at day-end) − expense (freelance at booking + FT/PT monthly). Nothing else** (owner, 2026-07-20).
- 🔴 **The freelance cap counts from BOOKING time, not attendance.** A committed booking draws the monthly cap down immediately (real-time over-booking prevention); cancel/leave releases it. The owner **split this out as a separate concern from the day-end summary** and it reversed the team's first design (owner, 2026-07-20).
  - 📌 The owner had earlier **accepted** the trade-off that a day-end deduction reacts *after* the day's cut rather than intra-day — then reversed it hours later in favour of booking-time. **The acceptance did not survive the mechanism change.**
- 🔴 **Freelance is the ONLY dynamic expense in the whole product** (owner, 2026-07-20).
- 🔴 **FT/PT salary is a per-teacher RECURRING monthly fixed cost: set once, auto-posted every month, never re-keyed.** The owner rejected monthly manual keying as too much user burden, and chose **per-teacher granularity over a single lump sum** so cost is visible per person (owner, 2026-07-20).
- 🔴 **FT/PT salary changes are EFFECTIVE-DATED and past months are FROZEN.** Admin enters the new amount plus the month it takes effect from; **any past month's P&L must always reflect the salary in effect THEN, not the latest.** Per-teacher salary history (amount + effective-from/to) is kept (owner, accounting-correctness requirement, 2026-07-20).
- 🔴 **FT/PT salary NEVER enters any per-booking calculation, cap, or day-end tally. The entire per-booking mechanic is FREELANCE-ONLY** (owner, 2026-07-20).
- 🔴 **Monthly reset OVERWRITES to the base budget — mid-month top-ups do NOT carry into the next month** (owner, confirmed answer, 2026-07-20).
- 🔴 **Editing a freelance's budget sets the NEXT-RESET target; it does not change the current remaining.** To change remaining now you use **เติมงบ (top-up)** (product rule shipped and surfaced in the UI, 2026-07-20).
- **A near-cap warning is REQUIRED and configurable per teacher; the hard stop stays at 0** (owner, answering Sober's question, 2026-07-20).
- 🔴 **At budget 0 the frontoffice AUTO-HIDES the freelance from booking and flags them; an admin can unlock by topping up OR by an explicit allow-negative override** (owner, REQ-001, 2026-07-20).
- **Group/Camp flat rates are ฿625 / ฿1,250 — deferred, not deleted.** The owner ruled the hourly rate sufficient for launch (owner, 2026-07-20).
- 🔴 **A teacher whose money is not yet set is "setup incomplete" and is NOT BOOKABLE.** Money may be set **later** than teacher creation, but a half-created record (a teacher with no counterpart) is forbidden (owner, 2026-07-20).
- 🔴 **Removing a teacher is BLOCKED if active/future bookings exist** — the user is warned to clear them first. Otherwise it is a **soft archive** that keeps all history, deactivates the money side going forward, and is **re-activatable** (owner, 2026-07-20).
- 🔴 **Changing a teacher's type FL↔FT/PT is supported and effective-dated — past P&L is untouched** (owner, 2026-07-20).
- 🔴 **Teacher identity is mastered in the FRONTOFFICE; money is mastered in the backoffice; they are linked by id.** The owner explicitly **REJECTED** moving the roster into the backoffice ("Path A") as too heavy (owner, 2026-07-20).
- 🔴 **THE LOCKED DRAWDOWN STATE MACHINE** (owner, routed by Porter and re-confirmed with a positively-phrased question, 2026-07-20):
  **CONSUMING (holds 1 hour, the freelance IS paid): `CONFIRMED`, `ATTENDED`, `SICK_LEAVE`.**
  **RELEASING (holds 0, NOT paid): `NO_SHOW`, `CANCELLED`.**
- 🔴 **`SICK_LEAVE` KEEPS the drawdown — the freelance is still paid, because they committed / the session is made up later.** The owner stated this as a **deliberate reversal** of the earlier "leave → refund" rule (owner, 2026-07-20). See `## ⚠️ CONTRADICTIONS`.
- 🔴 **A `NO_SHOW` freelance is NOT paid** — i.e. when the student no-shows a rostered freelance's slot, the hour is returned to the ceiling (owner, 2026-07-20).
- 🔴 **Invariant the owner's rule implies and that the team locked: a booking holds exactly 0 or 1 drawdown, every status change reconciles to target, and `remaining` must NEVER exceed `ceiling`** (Sober, 2026-07-20).
- 🔴 **The owner REFUSED an approval / maker-checker system in the backoffice: every action is direct.** Verbatim: *"แก้ไข ไม่เอาระบบ approval แบบนี้ ดีกว่า ทำได้เลยทุกอย่าง"* (owner-as-"คุณฟีน", 2026-07-20). This **reversed his own approval two entries earlier** (*"ทำเลย ถ้าไม่ควรค่อยเอาออก"*).
- **The backoffice has ONE single admin login, no role distinction** — REQ-002-style JWT (owner, 2026-07-20).
- **The backoffice credential is SEPARATE from the scheduling credential** (owner's choice over reusing one, 2026-07-20).
- 🔴 **The universal backoffice model the owner dictated: "everything is an `item` with a unit, whose quantity goes in and out via API."** Two type axes: **income/expense** and **fixed (per month/day/quarter) / not-fixed**. An item can carry a **ceiling/remaining like stock** (owner, 2026-07-20).
- 🔴 **An item's unit is FULLY FLEXIBLE — the user's free choice per item (บาท / ชั่วโมง / ครั้ง / ขวด / ชิ้น / แก้ว). Do NOT lock freelance to hours.** Both framings are valid: unit=บาท with a ฿10,000 ceiling, or unit=ชั่วโมง at ฿500/hr with a ceiling in hours (owner, explicit refinement, 2026-07-20).
- **Units are free-text input PLUS an option to save a chosen unit into a master-unit list; the picker is a combobox** (owner, 2026-07-20).
- 🔴 **Income and expense are SYMMETRIC.** Every item declares income or expense, then gets `unit_price` + a stock quantity **in its own unit**; **one rule for every movement: `value = qty × unit_price`** (owner, 2026-07-20).
- 🔴 **Multi-branch (and later country/region) grouping REUSES the frontoffice BADGE system attached to items — NOT an `organizations` table.** The owner chose badges explicitly for flexibility (owner, 2026-07-20).
- **The two admin audiences, in the owner's own words: backoffice admin = accounting + HR + CEO (controls money); frontoffice admin = ops + support** (owner, 2026-07-20). ⚠️ Stated in the same breath as the approval flow he cancelled hours later — the *audience* statement was never withdrawn.
- 🔴 **The owner's verdict on the first backoffice: it "got a lot wrong", is hard to use and not scalable → tear it down and rebuild from scratch. Finish the frontoffice FIRST, standalone** (owner, 2026-07-20).
- **Consequences the owner accepted with that pivot:** the freelance limit moves fully into the frontoffice and **stops calling the backoffice**; **limit-only for now — no freelance expense/P&L** until the new backoffice exists; **FT/PT salary deferred** (owner, 2026-07-20).

## Unsorted — 2026-07-20

- **Term — "คุณฟีน" / "พี่ฟีน":** the person Porter routes every business decision to. Used interchangeably with "the stakeholder" everywhere except one entry that calls พี่ฟีน "the customer" (observed, 2026-07-20).
- **Term — "the pivot":** the 2026-07-20 decision to tear down the backoffice and finish the frontoffice standalone. "The pivot's first deliverable" = the standalone freelance limit (observed, 2026-07-20).
- **Term — "the deploy gate":** the named set of real-environment actions only the human can perform (apply migration, register scheduled tasks, seed/enter real numbers) that stands between `SPEC_DONE` and `DELIVERED` (Sober, 2026-07-20).
- **Term — "meta-drift":** this project's word for the shared `__drizzle_migrations` journal causing migrations to be silently skipped or mis-tracked (observed, 2026-07-20).
- **Term — "the end-of-day cut job":** the nightly job; "the cut" is the no-show cut of course/voucher quota. Revenue posting was ADDED to it later, after the cut (observed, 2026-07-20).
- **Term — "setup incomplete":** a teacher who exists but whose money is not yet set; it is a computed gate, not a status (observed, 2026-07-20).
- **Term — `bo` / `ops`:** `ops.*` is the ORIGINAL backoffice schema (superseded, dormant); `bo.*` is the rebuilt universal item/movement schema (observed, 2026-07-20).
- **The four booking types have existed since the founding: `FIRST_TRIAL`, `SINGLE_SESSION`, `COURSE_PACKAGE`, `VOUCHER`** (observed, 2026-07-20).
- **The centre's teacher roster at founding: 8 freelance, 7 full-time, 8 part-time** (owner, DATA REQUEST, 2026-07-20).
- **Voucher sizes are 5 / 10 / 15 hours priced ฿6,000 / ฿10,500 / ฿13,500 — EXCLUDING the Onewheel and Balance Play programs.** Resolved from the customer's own rate-card image; it confirmed the code was already right (owner-supplied rate card, 2026-07-20).
- **The real single-session price is PROGRAM-DEPENDENT, ฿1,090–฿1,690** per the rate card — a single flat single-session price does not exist in the business (rate card, 2026-07-20).
- **First-Trial was seeded at ฿1,390 and single-session at ฿1,390 as PLACEHOLDERS**, with real figures owed by the customer (owner, 2026-07-20). ⚠️ Placeholder numbers went LIVE and were still live when REQ-001 was marked DELIVERED.
- **Placeholder go-live numbers, still unreplaced at delivery: freelance budget ฿70,000 each + rate ฿500/hr each; full-time ฿50,000/month each; part-time ฿15,000/month each.** Captured in `project-docs/seed-data-placeholder-2026-07-20.md` (owner, 2026-07-20).
- **The "Freelance-Private" default rate in the project docs is ฿500/hr** — used because the owner gave no rate (observed, 2026-07-20).
- **"โต๊ด" is a teacher-type EXCEPTION the owner parked as non-urgent and never resolved** (owner, 2026-07-20). Meaning still undefined in the repo.
- **The monorepo is FOUR app repos** — `smart-scheduler-front` (staff calendar), `smart-scheduler-back` (front-office API), `smart-scheduler-backoffice-front` (admin/money), `smart-scheduler-backoffice-back` (ops/finance API) (System, 2026-07-20).
- **This project has a Frontend Engineer role (Fern) specifically because the monorepo has TWO Next.js frontends** (System, 2026-07-20).
- **`SKIP_ADMIN_AUTH` is the backoffice auth kill-switch: `true` in dev, and must be `false` in prod** or every admin write 403s (observed, 2026-07-20).
- ⚠️ **Config footgun (era of `ops`, may now be obsolete): scheduling's `OPS_API_URL` must NOT end in `/api` (the code appends `/api/v1`), while backoffice-front's `NEXT_PUBLIC_BACKOFFICE_API_URL` MUST end in `/api`.** A double `/api` cost a live acceptance round (Porter, root-caused 2026-07-20).
- **Durable operational shape of the override: the freelance limit override is persisted in `app_settings` under the key `limit-override:<teacherId>`, deliberately NOT a `teachers` column** — chosen because BE cannot apply a migration under the brownfield rule (Jason, accepted by Sober, 2026-07-20).
- **The booking-time freelance draw is guarded by an idempotency key of the form `fl-book:<bookingId>` / `fl-unbook:<bookingId>`; the day-end revenue post by `rev:<bookingId>`; the monthly reset by `fl-reset:<teacherId>:<month>`; salary materialisation by `salary:<teacherId>:<month>`** (Jason, 2026-07-20).
- **`bo.movement.value_minor` is SIGNED (OUT negative), so `SUM(value_minor)` nets a draw against its reversal for free** — the SPEC originally said `|qty|` and was corrected to match the code (Sober, 2026-07-20).
- **`FIXED_COST` items are `track_stock=false` — P&L only** (Jason, 2026-07-20).
- 📌 **Design lesson the SA logged verbatim and that is still worth keeping: "cadence jammed into the type enum" — the old `FIXED_COST` mixed direction and cadence, so a fixed monthly *income* was inexpressible.** The rebuild split them into two axes (Sober, 2026-07-20).

## ⚠️ REFINES EXISTING

- **SYSTEM-FACTS (undated, no attribution):** *"There is NO student-delete anywhere in the product. Nothing with history can be removed — only hidden."*
  **This log, earlier and with a mechanism:** **teachers cannot be hard-deleted either — the bookings foreign key blocks it, so archive must be soft and keeps all history; a reconcile report exists because "no hard-delete" means orphans can only ever be active rows** (Sober, 2026-07-20). ⇒ the no-delete rule is **older and wider than "student"**, and it is enforced by the schema, not by policy alone.

- **SYSTEM-FACTS:** *"`job_runs.byBookingType` reports only the four original types — `FIRST_TRIAL · SINGLE_SESSION · COURSE_PACKAGE · VOUCHER`. Observed 09-02."*
  **This log, 2026-07-20, with the WHY:** those four are the original types **and the day-end deliberately handles only two of them** — it posts revenue for attended `FIRST_TRIAL`/`SINGLE_SESSION` and **deliberately posts nothing for `COURSE_PACKAGE`/`VOUCHER`, to avoid double-counting revenue already booked at sale** (owner's per-type rule, 2026-07-20). ⇒ the open question in SYSTEM-FACTS about whether day-end *skips* types has a founding-era answer for revenue, if not for auto-attend.

- **SYSTEM-FACTS:** *"A booking left `PENDING` is not the same as one left unmarked-but-`CONFIRMED`."*
  **This log, earlier and more basic:** **every booking is CREATED `PENDING`; `confirm` is the only commit point and there is no direct-CONFIRMED path** (Jason, 2026-07-20). ⇒ a `PENDING` fixture is not an odd state, it is the default state.

- **SYSTEM-FACTS:** *"Scheduled tasks live in `C:\sm-jobs\*.ps1` on each server separately."*
  **This log, earlier:** the scheduled tasks are **Windows Task Scheduler** tasks the **owner registers by hand after deploying**, and the count in this era was **two** (end-of-day, month-reset), both against `:4006` (Porter, 2026-07-20).

- **SYSTEM-FACTS:** *"The owner commits, on his own schedule. Nobody asks about commit state, ever (2026-09-01)."*
  **This log's founding-era version:** **the owner also DEPLOYS on his own schedule and enters the real data himself** — *"ฉันจะทำเองหลังจากทีมเสร็จ"* (owner, 2026-07-20), and **live acceptance is run by the human while the agent only verifies reported results.**

## ⚠️ CONTRADICTIONS

**1. `SICK_LEAVE` — is the freelance paid or not? Both sides are in THIS file.**

- Side A (earlier in the file, REQ-004 era):
  > *"Releasing: `SICK_LEAVE` (customer-leave — don't pay, per REQ-004), `CANCELLED`, `PENDING` (not yet committed)."* (Sober, stating the then-current rule)
  and, from the bug repro: *"mark leave → refund → 10,000 / 100 ✅"* — i.e. leave refunding the drawdown was treated as **correct** behaviour.
- Side B (the `[08:00]` entry, explicitly labelled a reversal):
  > *"**CONSUMING (held=1, freelance is paid): CONFIRMED, ATTENDED, SICK_LEAVE.**"*
  > *"⚠️ **Business-rule CHANGE:** **SICK_LEAVE now KEEPS the drawdown** (freelance still paid — they committed / the session is made up later). This **reverses REQ-004/TASK-019's refund-on-sick-leave**."*
- Also recorded in the same entry: *"surfaced a contradiction (their earlier 'leave→refund is correct' vs the answer)"* — **the owner himself gave both answers**, and Porter re-confirmed with a positively-phrased question.
- **Not resolved here.** SYSTEM-FACTS.md says nothing about `SICK_LEAVE` or the consuming/releasing sets, so I cannot tell whether Side B still stands.

**2. Approvals in the backoffice — approved, then refused, within one evening.**

- Side A, `[23:30]`: *"**A) Include the OWNER/STAFF approval flow now** — คุณฟีน: **"ทำเลย ถ้าไม่ควรค่อยเอาออก."**"* plus a full maker-checker scope (STAFF requests top-up / adjustment / item create-edit / over-ceiling; OWNER approves).
- Side B, `[23:45]`: *"คุณฟีน reversed the A decision: **NO approval system.** Every action is direct — remove the OWNER/STAFF maker-checker flow. (**"แก้ไข ไม่เอาระบบ approval แบบนี้ ดีกว่า ทำได้เลยทุกอย่าง"**)"*
- The log labels B a reversal, so this is likely settled — **but the OWNER/STAFF *role* vocabulary and the "backoffice admin = accounting + HR + CEO" audience statement were never explicitly withdrawn.** Reported, not resolved.

**3. Is the deployment target staging or production? Two owner statements, same week, same host family.**

- Side A, `[06:45]` (2026-07-20 file): *"คุณฟีน: this is a **side/staging site, no backup**, "ไม่เป็นไร" → **skip the rollback; fix-forward.**"* — said of the box running `som.develyst.online` / `backoffice-som.develyst.online`.
- Side B, `2026-07-27 [22:55]`: *"Stakeholder reports they **already deployed** the current build to the **real server** and core usage works… the teacher-CRUD 502 risk and the known freelance-drawdown idempotency bug are potentially **live in production**."*
- Also relevant: REQ-001/REQ-002 were marked **DELIVERED** based on the owner confirming them working on those same hosts, and the staff FE's own config calls `frontoffice.develyst.online` "**production**".
- **I cannot tell whether `som.develyst.online` is a staging box, the production box, or was re-designated between 07-20 and 07-27.** SYSTEM-FACTS.md describes the current world in terms of `sid` and `uat` and never mentions these hostnames, so the mapping (if any) is unknown. **Reported, not resolved.**

**4. Who is "คุณฟีน" — the owner, or the customer?**

- Side A (the dominant usage, dozens of entries): คุณฟีน is the decision-maker Porter routes every business question to, who approves designs, reverses decisions and deploys — i.e. **the owner**.
- Side B, `[19:10]`: *"Stakeholder gave placeholder go-live numbers (**customer พี่ฟีน** will refine later)"*, and `[13:00]` in the same era: *"Real numbers from **พี่ฟีน** to replace placeholders (DATA REQUEST still open)"* — i.e. **the customer**, distinct from the stakeholder.
- SYSTEM-FACTS.md consistently separates "the owner" from "the customer" (*"He raised it with the customer… the customer refused"*), so the distinction matters. **Reported, not resolved** — but note that **every "owner decision" I extracted above is attributed to "the stakeholder"/"คุณฟีน" and could, on Side B's reading, be a CUSTOMER decision relayed by the owner.**
