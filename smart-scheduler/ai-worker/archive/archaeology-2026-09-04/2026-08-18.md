# Facts extracted from `log/2026-08-18.md` (29,477 bytes · 345 lines · read in full)

> **Date note:** this file's date is *settled* — Porter's first entry records *"The human confirmed:
> today is 2026-08-18"*, and the closing entry hands over to `log/2026-08-19.md` when the clock
> rolled. Tags below are trustworthy, except that the final two Jason entries may belong to 08-19.

---

## Telling the two boxes apart

- 🔴 **You cannot tell from a command WHICH box it ran against. The owner has one shell and switches boxes by editing `.env`** — so the same command, the same machine, the same output shape can be `sid` or `uat`. **Ask him; never assume.** Owner, verbatim, when Porter asked: *"รันที่เครื่องนี้ และแก้ env เป็นของ sid แล้วค่อยรัน"*. (owner, 2026-08-18)
- **Three env files exist and are all correctly git-ignored: `.env`, `.env.sid`, `.env.uat`.** No credentials have ever been committed. (Jason, verified, 2026-08-18)
- 🔴 **`sid`'s migration ledger legitimately carries MORE rows than the journal** (23 ledger rows vs 19 journal migrations, 19 witnesses) — the expected residue of the 2026-08-11 witness-seeding recovery, and `db:verify` accounts for it. **That is GREEN, not a defect.** (Porter, quoting the owner's run, 2026-08-18)
- **Observed 2026-08-18: a full `pg_dump -Fc` of one of these databases is ~0.1 MB (117 KB).** The whole customer dataset is tiny — a "backup" bigger than a few hundred KB, or of zero bytes, is the anomaly. (Porter/Jason, 2026-08-18)
- **`sid` and `uat` are routinely one migration apart, and that is expected and fine** — but **nothing that reads a newly-added column may be deployed to `uat` before that box's own migration run.** (Porter, 2026-08-18)

## Working agreements the owner has stated

- 🔴 **NOBODY on the team touches the customer's box. Every remedy is handed to the owner as copy-paste, owner-run steps, dry-run first, with an explicit "what you should see" for each step** — and if the honest answer is "restore the backup and redo it", the team says so. (Porter, 2026-08-18)
- **A script's own suggested remedy is NOT auto-authority.** When `db:verify` printed *"run `bun run db:seed-ledger`"*, Porter told the owner to **stop, change nothing, restart nothing — including not running the suggested command**, because it is a write against the customer's database and whether it is right *here* is an SA call on evidence, not a copy-paste. (Porter, 2026-08-18)
- **Facts are kept separate from interpretation when reporting an incident to the owner** — Porter's entry literally separates *"Facts, kept separate from interpretation"* from *"Plausible origin (SA to confirm, not my call)"*. (Porter, 2026-08-18)
- **A role corrects only its OWN date stamps; nobody rewrites another role's lines, and no closed log is ever rewritten.** (Fern, 2026-08-18)
- **`git ls-files` only lists what is tracked NOW — it cannot prove a file was never committed.** To prove absence you need `git log --all --diff-filter=A --name-only`. Fern recorded this as a correction against himself: *"that check did not support the conclusion I drew from it."* (Fern, 2026-08-18)

## 🔴 Accepted security risk — a customer-data database dump is committed and pushed (owner, 2026-08-18)

**`sm-prod-backup-2026-08-18T14-21-48-407Z.dump` (117 KB) is committed as the sole file of commit `0b8966c` in `smart-scheduler-back` and is pushed to `origin/dong` at `github.com/develyst1/smart-scheduler-back`.**

- **What is in it:** `pg_dump -Fc` of whatever `DATABASE_URL` pointed at — **full schema AND all table data**: `parents`, `students`, `teachers` rows, i.e. **names, phone numbers and LINE userIds of real people, mostly children**. 🟢 **No credentials** — the `.env*` files are correctly ignored. This is a **personal-data** exposure, not an account-takeover one.
- 🔴 **The owner's explicit decision: PARK IT. No history rewrite, no security work — go-live first.** Verbatim: *"ช่างมันเถอะ รีบโฟกัสเรื่องเอาข้อมูลขึ้นกันเถอะ จะเคลียร์ข้อมูลเทสออกจาก uat แล้วเอาข้อมูลนักเรียนขึ้นให้ลูกค้า"*. Porter recorded it **as a decision, not an oversight**, and committed to raising it **once more after go-live — not repeatedly, and never as a blocker.** *"It is his repo, his customer, his call."* (owner, 2026-08-18)
- **The one thing the owner DID do:** he ran `echo "*.dump" >> .gitignore` in `smart-scheduler-back`. That stops the next accident; **it does not untrack or remove the already-pushed file**, which stays on `origin/dong` exactly as it was. (owner, 2026-08-18)
- **The honest framing given to the owner:** *"anything pushed must be treated as disclosed to everyone who has had access to that repo since, and a history rewrite does not un-disclose it — it only stops further copies."* Whether that GitHub repo is **private** was asked of the owner and **is not knowable from inside the workspace**. (Porter, 2026-08-18)
- **Blast radius, verified:** the two frontend repos (`smart-scheduler-front`, `smart-scheduler-backoffice-front`) have **never had a `.dump` / `.sql` / `.bak` file added in any commit on any branch** (checked with `git log --all --diff-filter=A`). (Fern, 2026-08-18)
- 🔴 **THE CAUSE IS STILL UNFIXED (parked):** `scripts/backup.ts` writes `sm-prod-backup-<stamp>.dump` to the **current working directory**, and the runbook has the owner run `bun run db:backup` **from inside the repo** — so the backup script has been dropping customer data into a git working tree **by design since TASK-126**. Making `backup.ts` write outside the repo is the real fix and is **unbuilt**. ⇒ **every backup the owner takes still lands in the repo folder**; only `.gitignore` stops it being committed. (Jason, 2026-08-18)

## Product limits / platform behaviour

- 🔴 **Drizzle will SILENTLY SKIP a journal migration forever if the ledger's newest `created_at` is ≥ that migration's `when` — and `drizzle-kit migrate` still reports "migrations applied successfully".** This is the failure mode that **cost this project an outage on 2026-08-02**; `db:verify` (REQ-032) exists solely to expose it, and on 2026-08-18 it caught it again on the customer box. The schema can look complete and the deploy can look clean while four migrations can never apply. (Porter/Jason, 2026-08-18)
- **Observed on `uat`, 2026-08-18** (state, will be fixed — recorded because the *shape* recurs): Journal 19 · ledger 19 rows · 19 schema witnesses, yet **0011_freelance_budgets, 0015_teacher_link_requests, 0016_subjects_price_group and 0017_entitlement_source had no matching ledger record**, with the ledger's newest `created_at` (…014) ≥ each of their `when` values. Plausible origin: the **2026-08-11 emergency witness-based `db:seed-ledger` recovery** ran against that box and its rows now shadow those four. (Porter, 2026-08-18)
- 🔴 **The migration WITNESS MAP is order-checked against the journal** — appending a `0019` witness *before* `0018` fails the test suite. Worth knowing before anyone adds a migration. (Jason, 2026-08-18)
- **`ADD COLUMN IF NOT EXISTS`, `DROP … IF EXISTS` and "schema/ledger already exists" NOTICEs during `db:migrate` are benign idempotency messages, not errors.** (Porter, quoting the owner's run, 2026-08-18)

## Business rules the owner set earlier and that were never written down

- 🔴 **Go-live means: wipe the TEST data out of `uat` and put the customer's real students in.** Owner: *"จะเคลียร์ข้อมูลเทสออกจาก uat แล้วเอาข้อมูลนักเรียนขึ้นให้ลูกค้า"*. ⇒ **`uat` stops being a test box at that moment.** (owner, 2026-08-18)
- **Import gates, restated with the numbers:** phones are **0-prefixed and must be 10 digits** or the row is held back · **DOB sanity range 2005–2026**, never guessed · no-phone → hold-back list · parent rows → the family name · **yellow rows excluded entirely** · day-by-day batches, top-down · row-keyed reconciliation. (Porter, 2026-08-18)
- Observed readiness at that moment: **110 rows import-ready · 39 need the customer · 27 not ready.** (Porter, 2026-08-18)
- **A course born with declared absences has `leaveUsed = 0` and is NOT locked**, while an ordinary sick leave still consumes quota exactly as before. An over-ceiling create is **refused, never trimmed**. (Jason, implementing the owner's REQ-045 (B), 2026-08-18)

## Unsorted — 2026-08-18

- **The repo set and remote:** `smart-scheduler-back`, `smart-scheduler-front`, `smart-scheduler-backoffice-front` under `github.com/develyst1/`; the working branch is **`dong`** (`origin/dong` exists). (Jason/Fern, 2026-08-18)
- **The documented way to pick up work is the BOARD, not a log entry** — Jason took TASK-146 off the board when no log entry announced it, and refused to start TASK-148 while it had a board row but no task file. *"That is the documented way to pick up work."* (Jason, 2026-08-18)
- **The team's own reading of why the date rule exists:** *"The rule exists precisely because this is cheap to get wrong and expensive to unpick"* — one wrong filename at session start cost three roles their effort routing around it. (Porter, 2026-08-18)

## ⚠️ REFINES EXISTING

1. **`sid` first, then `uat` — SYSTEM-FACTS has no entry for the migration/deploy order at all, and this log has it as an owner rule that was tested and held.**
   - **This log (Porter, 2026-08-18):** *"The standing rule he set yesterday — `sid` first, verified, then `uat` — was followed on its first real outing, including on a migration that was safe enough to be tempting to shortcut. **Worth recording as a rule that works rather than a rule that exists.**"*
   - Pairs with SYSTEM-FACTS' existing *"Scheduled tasks live in `C:\sm-jobs\*.ps1` on each server separately — the boxes can differ"*: **the boxes differ in migration state too, by design and routinely.**

## ⚠️ CONTRADICTIONS

**C4 — no true contradiction found inside this file.** The one candidate is resolved *inside the log itself by the owner*, so I am recording it rather than raising it: Porter did not know which environment the `0018` run had touched (*"the one thing I do not know, and will not assume: WHICH environment that ran against"*), and the owner answered that it was `sid`. Both statements stand; nothing is left in conflict.

**Carried forward, not resolved here:** C1 (the `2026-08-04.md` filename vs the machine clock) and C3 (the 08-16/17/18 file split) are recorded in the other two files of this run. This file is the one that *settles* C3 — Porter: *"The human confirmed: today is 2026-08-18"* — but the entries already written into `2026-08-16.md` and `2026-08-17.md` were **never re-dated**, by explicit decision: *"nothing in them gets rewritten."*
