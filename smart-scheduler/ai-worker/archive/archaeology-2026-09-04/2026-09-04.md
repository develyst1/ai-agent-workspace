# Extracted facts — from `log/2026-09-04.md` (8,305 bytes, 106 lines, read in full)

> Scratch output for Marie's archaeology run (ORDER 3). Nothing here has been written into the repo.
> The final entry (`[--:--] Porter (PM) — workflow-operations edit`) was **skipped by instruction** — workspace
> bookkeeping, not system facts.
> This file is thin on purpose: almost everything in this log is status, verdicts and routing, i.e. history.

## LINE

- **`Haris` is a REAL teacher and is LINE-linked** ⇒ the never-message-in-rehearsal rule applies to him; **he is not usable as a fixture** (Porter, 2026-09-04).
- **The fixture teachers are `Camp`, `Dewy`, `Ek`** — the names to reach for when a test needs a teacher nobody real is behind (Porter, 2026-09-04; whether any of them is linked to an account we control was still being asked).
- 🔴 **Proving "every assigned teacher received it" needs TWO DIFFERENT teachers, both observable.** Two LINE accounts that both resolve to the same teacher are still **one** teacher (Porter, 2026-09-04).
- **A linked PARENT receives no teacher confirmation** — a parent link and a teacher link are two different links, and one run cannot close both (Porter, 2026-09-04, stated as his position with Tanya's read still unanswered).

## Unsorted — 2026-09-04

- **Reading the code is not testing** — QA declined a "the SA confirms it from source" shortcut, and the PM accepted the refusal as correct: source-reading adds no fact the run had not already shown (Tanya's refusal, accepted by Porter, 2026-09-04). *Classification uncertain — a team working agreement, not an owner statement.*
- **"The bot said success" and "the admin screen agrees" are two different facts, and a half-written link hides between them** — a link change is verified in the DATA, not in the confirmation message (Porter, 2026-09-04).
- 📌 **Pointer for Marie, NOT a verdict:** Porter states as an open product question — *"can one LINE account hold a teacher role and a parent role at the same time? It belongs in `SYSTEM-FACTS.md` either way"* (2026-09-04). **The 2026-07-30 log appears to answer it** (TASK-046: linking as one role clears the other, "one LINE user ⇒ one active roster link", precedence teacher → customer → admin). I have extracted the 07-30 statement into that day's file and am **not** asserting it is still true on 2026-09-04 — a lot of LINE work has landed since. Someone should confirm it against the current build before it is written down as settled.

## ⚠️ REFINES EXISTING

1. **`SYSTEM-FACTS.md` → LINE: *"The owner is linked on `sid` as teacher `Bank` (2026-09-01) — the isolatable test recipient. Only ONE recipient is linked."***
   - **This log, 2026-09-04 (owner's report, relayed by Porter):** that account **changed role during the night** — *"it was teacher `Bank` on 09-01, then became a **parent** (family `0900000092` → cleared → `0905622548`)"* — and the owner has since **linked a second account, `support08 Dong`, as teacher `Bank`**.
   - ⚠️ **Porter explicitly declines to assume what that gives us**, and had asked QA to read the actual link state (which accounts are linked, to whom; whether `Bank` is linked to one account or two; whether account 1's teacher link survived its parent link). **As of the end of this log that read had not come back** — so the existing SYSTEM-FACTS line is **stale as written**, but the replacement is not yet established.

## ⚠️ CONTRADICTIONS

None found in this log.
