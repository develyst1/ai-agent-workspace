# TASK-418 — Camp ON the teacher grid, BE (`REQ-095 §11`, `SPEC-085` A + B): `camp_week_days` (teachers + window per date) · real `OTHER`/`CAMP` rows per hour per teacher · the per-day swap · the reminder collapsed to one camp line per day — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-20) · **Size M + S** · migration `0047_camp_week_days` (48 = 48). `sid`; joins the pending `uat` batch.

## §0 Owner's rulings (Porter 09-20): HARD block via real rows · one window per day, **default 10:00–15:00, editable per WEEK** (the kids' AM/PM stays the package's) · default teachers = the week's list, a day with no teacher ⇒ no block · a lesson already inside the window ⇒ **REFUSE naming the clash** (the camp never overwrites).

## §1 Verify before the contract
1. `createOtherSeries` / `insertBooking` for OTHER rows (TASK-394): what a camp row needs that an ECA row has (title, kind, teachers, no student) and what it must NOT carry (rates, head count — or should `head_count` mirror the day's kids? say). Where the 4th kind `CAMP` lands in `lib/other-kind.ts` and the `type ⇔ kind` pin.
2. The week's lifecycle (`camp.service.ts`): open (`POST /weeks`) · `PATCH /weeks/:id` (dates/teachers/status) · close — where the day rows and the booking rows are created / re-derived / deleted, and how a PATCH that shrinks the dates or drops a teacher cascades.
3. `swapGroupTeacher` (TASK-397): what is reusable for a per-DAY camp swap (slot check per row, one tx, `409` naming the clash); the cancel path for a camp row (an admin cancelling one camp hour by hand — allowed? recommend NO: the block is owned by the day object; `409 CAMP_ROW_OWNED "แก้ที่สัปดาห์แคมป์"`).
4. The coach's 08:15 reminder (`groupReminders` / the renderer): where OTHER rows of one title on one day collapse into one line — the `Seats` block precedent.
5. The grid payload: how a row tells the FE it is a camp hour (`campWeekId` + `other.kind = "CAMP"`) so the FE can merge contiguous cells.

## §2 The contract I propose — confirm/correct before code
- **`0047_camp_week_days`:** `camp_week_days { id, camp_week_id FK RESTRICT, date, teacher_ids uuid[] NOT NULL DEFAULT '{}', start_time time NOT NULL, end_time time NOT NULL, UNIQUE (camp_week_id, date) }` + `bookings.camp_week_day_id uuid NULL FK camp_week_days RESTRICT` + `bookings_camp_week_day_idx` (partial, `WHERE camp_week_day_id IS NOT NULL`) = the witness · `camp_weeks.window_start/window_end time NULL` (the week's default window; NULL ⇒ 10:00–15:00 by code). Catalog-only (nullable adds on `bookings` — a blink, no rewrite; say the lock).
- **`CAMP`** joins `OTHER_KINDS` (ECA · FREE · KOL · CAMP); a CAMP row = `booking_type OTHER`, `other_kind CAMP`, `other_title` = the week's name, `camp_week_day_id`, the teacher, no student/course/money/rate/head count; `end_time` = the hour + 1 (the OTHER rule).
- **Lifecycle — ONE `syncCampDayRows(tx, dayId)`:** computes the wanted set `{ teacher × hour }` from the day row (teachers × the window in whole hours; a day with no teacher ⇒ empty) and diffs against the existing CAMP rows of that day: inserts the missing through `insertBooking` (first slot clash ⇒ `409 SLOT_TAKEN "วันที่ D HH:MM ครู X มีคาบแล้ว"` — the whole call rolls back), deletes the surplus (hard delete — these rows are DERIVED, not history; ⚠ the product's "no delete" rule is for human records — say if you'd rather CANCEL them). Called from: open a week (creates one day row per date with the week's teachers + window, then syncs each), `PATCH /weeks/:id` (dates added ⇒ new day rows; removed ⇒ their rows gone; window changed ⇒ re-sync all days; teachers changed at week level ⇒ ⚠ does NOT touch days already edited — say), `PATCH /weeks/:id/days/:date { teacherIds?, startTime?, endTime? }` (the per-day edit = the swap; re-sync that day), close a week (all rows gone; reopen re-syncs).
- **Refusals:** a clash names the date, hour and teacher; a window outside 06:00–22:00 or not whole hours ⇒ `400`; an admin cancel/move/edit on a CAMP row ⇒ `409 CAMP_ROW_OWNED` (the day object owns it — pinned across the status/move/other-edit routes).
- **Grid payload:** a CAMP row carries `campWeekDayId`, `campWeekId`, `other.kind = "CAMP"`; the day banner unchanged. `GET /camp/weeks` + `GET /camp/weeks/:id/days` gain each day's `teacherIds`, `startTime`, `endTime`.
- **Reminder (B):** the coach's block for CAMP rows collapses to ONE line per day per week — `Camp : <name> 10:00-15:00` (the owner's approved camp label `Camp` / `แคมป์`) — no hour-by-hour list; the kids' camp reminder (TASK-403/405) untouched; the day-end attends the CAMP rows harmlessly (no money by absence — pinned).
- **Scope (REQ-097):** a linked teacher sees their CAMP rows in their column by the existing predicate; the swap route is admin-only (not in `TEACHER_ALLOWED`).
- 🚫 No change to the kids' side; no camp status; no money on a CAMP row.

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · **48 = 48** · the sync by value (wanted set · diff · clash ⇒ rollback · no-teacher ⇒ none · window change re-syncs) · the lifecycle by source (one sync function, five callers) · `CAMP_ROW_OWNED` on the three routes · the reminder line by value · no money by absence · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-20) — §1 verified (three findings change §2), §2 confirmed with corrections and your two ❓ answered; no code until you confirm

## §1 Verified (read only)
1. **An OTHER row through `insertBooking(tx, null, input)`** needs `teacherId`, `date`, `startTime` (`endTime = addHour(start)` — the OTHER rule holds), `bookingType: "OTHER"`, `otherTitle`, `otherKind`; optional `headCount`, `otherPriceMinor/ItemId`, `teacherRates` (⇒ `teacher_rate_minor`), `note`. It calls `assertTeacherBookable` (archived ⇒ 400; **not a work day ⇒ 400 `ครูX ไม่มาสอนวันนี้`** — a camp on a coach's off day is REFUSED naming them, the camp never overrides the roster — say if the owner wants the camp to override), `assertKindForType` (the `type ⇔ kind` pin in `lib/other-kind.ts`: `OTHER_KINDS = ["ECA","FREE","KOL"]`), and maps the unique index's 23505 to `409 SLOT_TAKEN` + `describeSlotClash`. 🔴 **Finding A — status:** `insertBooking` defaults `status: "PENDING"`. A PENDING camp row would (a) NOT appear on the coach's 08:15 reminder (REQ-096 made it CONFIRMED-only — your §2 B would print nothing) and (b) sit in the "to confirm" tray. **A CAMP row is born `CONFIRMED`** (`status: "CONFIRMED", confirmedAt: now` — there is no confirm step for a block). `head_count` = **NULL, not mirrored** (the kids are `camp_days`; a copy would drift — the roster is the day banner's); rates NULL; no price ⇒ `postOtherBookingSale` returns `false` at the day-end (no money by construction) and `reconcileBookingHolds` returns early for `bookingType === "OTHER"` (no freelance hold — pinned). The 4th kind lands in `OTHER_KINDS`; ⚠️ the three HUMAN routes that take a kind (`createOtherSeries`, `editOtherBooking`, the walk-in's `otherKind`) validate with `z.enum(OTHER_KINDS)` — they would accept `CAMP` by hand ⇒ **`HUMAN_OTHER_KINDS = ["ECA","FREE","KOL"]` for the validators; `CAMP` only through the sync** (pinned).
2. **The week's lifecycle today:** `createWeek` inserts ONE row (`name, startDate, endDate, capacity, teacherIds, status OPEN`); `updateWeek` patches `name / capacity / teacherIds / status` (CLOSED stamps `closedAt`); **there is NO dates edit** — `updateCampWeek` has no `startDate/endDate`. 🔴 **Finding B:** your §2's "dates added ⇒ new day rows; removed ⇒ their rows gone" has no route to hang on, and the kids' `camp_days` reference the week's dates too (a redeem checks `datesOfWeek`), so a dates edit is a bigger rule than this task. **I propose the dates stay immutable** (open a new week); the cascade points are: OPEN (create the day rows + sync each) · `PATCH /weeks/:id` (teachers at week level — your ❓2 below; status CLOSED ⇒ every CAMP row of the week deleted; OPEN again ⇒ re-sync all days) · the per-day PATCH · the week's window (the new `window_start/end` on `camp_weeks` — a change re-syncs every day that has NOT been edited by hand, see ❓2).
3. **`swapGroupTeacher`** updates `teacher_id` in place inside one tx, per row: `assertTeacherBookable` → the update → 23505 ⇒ `409 SLOT_TAKEN "วันที่ D ครูไม่ว่าง — ไม่ได้ย้ายรายการใด"` (the whole tx rolls back) → `reconcileBookingHolds` (a no-op for OTHER). **Reusable: the shape, not the function** (it walks a `groupKey` series). The camp's per-day swap is the SYNC itself: the day row's `teacher_ids` changes ⇒ the wanted set changes ⇒ the diff deletes the old teacher's rows and inserts the new one's (each slot-checked; the first clash names date · hour · teacher; nothing written). ONE function for open / edit / close — no separate swap code. **A human write on a CAMP row:** none of the row routes refuses OTHER rows today. Recommend NO, as you do: **`assertNotCampRow(row)` ⇒ `409 CAMP_ROW_OWNED "บล็อกแคมป์แก้ที่สัปดาห์แคมป์"`** at EVERY row-write entry: status (all four actions — the day-end's cut sets ATTENDED by direct update, not through `updateBookingStatus`, so nothing is lost), move, other-edit, group-teacher, note, badges, pause/resume, rental ×3 — pinned as a table like the ended-course one (every write route classified: refused / unrelated).
4. **The coach's reminder:** `groupReminders` builds ONE `TodayRow` per booking; an OTHER row renders as its own block (`student: title`, `program: programLabel(other → the title)`, `Date`, `Time start-end`) — six camp hours = six blocks today. **The collapse point is the builder** (`daily-reminder.ts`, before the rows reach the renderer): CAMP rows of one `(teacher, date, campWeekId)` fold into ONE row whose `startTime` = the earliest and `endTime` = the latest — so the existing OTHER block prints `Camp A · 10:00-15:00` with no new copy, no new kind, no renderer change (the `Seats` block precedent is a per-row fold; this is a rows-to-one fold, the `groupId` seat-skip's shape). Your "`Camp : name 10:00-15:00` one line" — the block's `Student :` line is the title (`Camp A`) and the `Time :` line the range; say if you want the literal single line instead (that IS new copy).
5. **The grid payload:** `toBookingDTO` carries `other: otherFacts(b)` (`kind`, `headCount`, rates) and `bookingType`; the FE keys a cell on `teacher × date × startTime`. A CAMP row will carry **`other.kind = "CAMP"`, `campWeekDayId`, `campWeekId`** (two new DTO fields, null on every other row) — enough to merge contiguous cells of one teacher and to open the per-day swap.

## §2 The contract — as you wrote, with the corrections
- **`0047_camp_week_days`** (48 = 48): `camp_week_days { id, camp_week_id FK RESTRICT, date, teacher_ids uuid[] NOT NULL DEFAULT '{}', start_time time NOT NULL, end_time time NOT NULL, edited_at timestamptz NULL, UNIQUE (camp_week_id, date) }` (+ `camp_week_days_week_idx`) · `camp_weeks.window_start / window_end time NULL` (NULL ⇒ `10:00 / 15:00` by code — `CAMP_WINDOW_DEFAULT`) · `bookings.camp_week_day_id uuid NULL REFERENCES camp_week_days ON DELETE RESTRICT` · `bookings_camp_week_day_idx` partial = **the witness**. **Locks, said plainly:** the two `ADD COLUMN … NULL` on `bookings` are catalog blinks under ACCESS EXCLUSIVE (no default, no rewrite); the FK adds SHARE ROW EXCLUSIVE on the new (empty) `camp_week_days`; ⚠️ **`CREATE INDEX` on `bookings` (the hot table) takes SHARE — writes to `bookings` wait for the one scan** (sub-second at `uat`'s size; not `CONCURRENTLY` — it cannot run in the migration's tx, the 0033 trade). `camp_days` untouched.
- **`CAMP`** in `OTHER_KINDS` (the `type ⇔ kind` pin); `HUMAN_OTHER_KINDS` for the three human validators (CAMP only via the sync). A CAMP row: `OTHER` · `other_kind CAMP` · `other_title` = the week's name · `camp_week_day_id` · the teacher · **`CONFIRMED` at birth** (Finding A) · no student/course/money/rate/head count · `end = start + 1h`.
- **ONE `syncCampDayRows(tx, dayId)`:** wanted = `teacher_ids × [start, end)` in whole hours (no teacher ⇒ ∅); existing = the live CAMP rows with that `camp_week_day_id`; diff: insert the missing through `insertBooking` (`409 SLOT_TAKEN "วันที่ D HH:MM ครูX มีคาบแล้ว"` — the first clash, the whole call rolls back), **hard-DELETE the surplus (your ❓1 — YES, delete:** the rows are DERIVED from the day object, carry no money/history, and a CANCELLED camp hour would pollute the cancelled tray and every cancel count; the "no delete" rule protects human records — the day row IS the record). Callers (five): `createWeek` (one day row per date with the week's teachers + window, sync each) · `updateWeek` (status CLOSED ⇒ delete every CAMP row of the week; OPEN again ⇒ sync all; window change ⇒ re-sync the UN-edited days; `teacherIds` ⇒ ❓2) · **`PATCH /weeks/:id/days/:date { teacherIds?, startTime?, endTime? }`** (the per-day swap: stamps `edited_at`, re-syncs that day) · (the dates — immutable, Finding B).
- **Your ❓2 — a week-level teacher change and hand-edited days:** `edited_at` on the day row is the fact. **A week-level `teacherIds` change re-derives ONLY the days with `edited_at IS NULL`; an edited day keeps its list** — predictable, and the FE can offer "apply to every day" as N per-day PATCHes. The same rule for the window. Say if you would rather it re-derive all (then `edited_at` is informational only).
- **Refusals:** the clash (date · hour · teacher); a window not in `06:00–22:00`, not whole hours, or `start >= end` ⇒ `400`; a teacher not on the roster that day ⇒ `400` (Finding A's `assertTeacherBookable`); `409 CAMP_ROW_OWNED` on every row write (the table); a day-PATCH on a CLOSED week ⇒ `409 CAMP_WEEK_CLOSED`; a date outside the week ⇒ `404`.
- **Grid payload:** `campWeekDayId`, `campWeekId`, `other.kind = "CAMP"`; the day banner unchanged. `GET /camp/weeks` and `/weeks/:id/days` gain per day `{ teacherIds, startTime, endTime, editedAt }`; the week DTO gains `windowStart / windowEnd` (the effective values).
- **Reminder (B):** the builder's fold (§1.4) — one OTHER block per `(coach, date, week)` with the merged time; the kids' camp reminder untouched; the day-end attends CAMP rows and posts nothing (pinned by value: `postOtherBookingSale` false on a null price; by absence: no `recordSale`/`recordRevenue` on the camp path).
- **Scope (REQ-097):** unchanged predicate; the day-PATCH and the week routes stay outside `TEACHER_ALLOWED`.
- 🚫 No change to the kids' side; no camp status on the rows beyond CONFIRMED/ATTENDED; no money.

## ❓ for you (five)
1. Finding A — CAMP rows born `CONFIRMED`; a coach's off day refuses (no roster override) — OK?
2. Finding B — the week's dates immutable in this task (a new week instead) — OK?
3. Your ❓1 — hard delete of the derived rows — as argued; OK?
4. Your ❓2 — `edited_at`: a week-level change re-derives un-edited days only — OK, or re-derive all?
5. The reminder fold = the existing OTHER block with the merged time (`Camp A` / `10:00-15:00` lines), no new copy — OK, or the literal one-liner (new copy via Porter)?

## For @Fern (draft, final after confirm)
CAMP rows in the calendar payload: `bookingType: "OTHER"`, `other.kind: "CAMP"`, `otherTitle` = the week name, `campWeekDayId`, `campWeekId`, `status CONFIRMED`; merge contiguous cells of one teacher (render-only); the swap door ⇒ `PATCH /api/camp/weeks/:id/days/:date { teacherIds?, startTime?, endTime? }` (`action:camp.week-open`); every row write on a CAMP row ⇒ `409 CAMP_ROW_OWNED`; `GET /camp/weeks` / `/weeks/:id/days` carry the per-day teachers + window + `editedAt`; the week's `windowStart/windowEnd` editable on `PATCH /camp/weeks/:id`.

⛔ Waiting on your confirm (the five ❓) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-20) — all five yes; build
1. **Finding A — yes:** CAMP rows born `CONFIRMED` (a block has no confirm step); a coach's off day REFUSES naming them — the roster is the roster (flagged to the owner as a consequence, not a decision to make now). `HUMAN_OTHER_KINDS` for the three human validators — good catch.
2. **Finding B — yes:** the week's dates are immutable in this task (open a new week); flagged to Porter.
3. **Hard delete of the derived rows — yes**, as argued: the day row is the record; a cancelled camp hour would lie in the tray and the counts.
4. **`edited_at` — re-derive un-edited days only — yes.** The FE may offer "apply to every day" as N per-day PATCHes (Fern's task says so).
5. **The reminder fold = the existing OTHER block with the merged time — yes**, no new copy; the block reads `Camp A` / `10:00-15:00` through today's lines.
Note the `CREATE INDEX` SHARE lock on `bookings` in the header as you wrote it (sub-second; the cutover-minute rule does not apply, but say it). `0047` ⇒ 48. **Go.**

---

# 📤 REPORT — @Jason → @Sober (2026-09-20)

✅ **DONE (code), against the confirmed contract (all five yes).** **2583 pass / 0 fail**, **199 files** (+1) · `tsc --noEmit` clean · 🔴 **migration `0047_camp_week_days` — 48 = 48** · offline preflight `0047` alone `[]`, the batch `0038 → 0047` `[]`.

## What was built
- **`drizzle/0047_camp_week_days.sql`** (6 statements): `camp_week_days { id, camp_week_id FK RESTRICT, date, teacher_ids uuid[] NOT NULL DEFAULT '{}', start_time, end_time, edited_at NULL, created_at, UNIQUE (week, date) }` + its week index · `camp_weeks.window_start / window_end time NULL` · `bookings.camp_week_day_id uuid NULL FK RESTRICT` · LAST `bookings_camp_week_day_idx` partial = **the witness**. Header: the order, expects 48, no enum, **the `CREATE INDEX` on `bookings` named plainly — SHARE for its one scan, writes wait (sub-second), not CONCURRENTLY (the 0033 trade)**. Schema: the table, the two columns, the FK column, the relations (`bookings.campWeekDay`, `campWeeks.weekDays`).
- **The kinds:** `OTHER_KINDS = [ECA, FREE, KOL, CAMP]` (the `type ⇔ kind` pin knows all four); **`HUMAN_OTHER_KINDS`** for the three human validators (the series, the edit, the walk-in) — CAMP only through the sync. `CAMP_KIND`.
- **`lib/camp.ts`** (pure): `CAMP_WINDOW_DEFAULT 10:00–15:00`, `CAMP_WINDOW_BOUNDS 06:00–22:00`, `assertCampWindow` (whole hours, in bounds, start < end ⇒ else 400), `windowHours` (`[start, end)`), `wantedCampSlots(teachers, start, end)` (teachers × hours; no teacher ⇒ ∅), `campSlotDiff(wanted, existing)` (insert the missing, remove the surplus), **`foldCampRows`** (the reminder fold — one row per day object spanning the earliest start to the latest end; copies, never mutates).
- **`camp.service.ts` — ONE `syncCampDayRows(tx, dayId)`:** wanted (empty when the week is not OPEN) vs the live rows linked to the day; deletes the surplus (hard — your ❓1); inserts the missing through the ONE `insertBooking` (now exported) as `OTHER · CAMP · the week's name · CONFIRMED at birth · no student/price/rate/head count`, then links the row (`camp_week_day_id`, `confirmedAt`); a `SLOT_TAKEN` from the inserter is re-thrown as **`409 SLOT_TAKEN "วันที่ D HH:MM ครูX มีคาบแล้ว — ไม่ได้บันทึกอะไร"`** (the teacher's nickname) — it propagates, so the CALLER's tx rolls back. `assertTeacherBookable` inside the inserter refuses a coach's off day (Finding A, as confirmed). **The callers:** `createWeek` — ONE tx: the week (+ its window, validated), one day row per date with the week's teachers + window, the sync per day · `updateWeek` — CLOSED ⇒ every day's rows deleted; OPEN again ⇒ every day re-synced; a week-level `teacherIds` / window change ⇒ **only the days with `edited_at IS NULL`** re-derived (your ❓2) · **`PATCH /camp/weeks/:id/days/:date { teacherIds?, startTime?, endTime? }`** (`action:camp.week-open`; not in `TEACHER_ALLOWED`) = the per-day swap: a CLOSED week ⇒ `409 CAMP_WEEK_CLOSED`, a date outside ⇒ 404, the window validated, `edited_at` stamped, the sync. The dates are immutable (Finding B — `updateCampWeek` still has no start/end).
- **`CAMP_ROW_OWNED`** — ⚠️ one correction to my own §2: I first put the guard at the ROUTES; that costs an extra read before every write AND breaks every root-app route test that spies the service. It now sits **INSIDE the eleven services, on the row already in hand** (a pure check, no second read): `updateBookingStatus` (all four actions — the day-end's cut writes ATTENDED directly, untouched), `moveBooking`, `editOtherBooking`, `swapGroupTeacher`, `setAttendeeNote`, `pauseBooking`, `resumeBooking`, `setBookingBadges`, `recordBookingRental`, `payBookingRental`, `removeBookingRental` — pinned as a table of eleven. Message `บล็อกแคมป์แก้ที่สัปดาห์แคมป์`.
- **The DTOs:** the booking DTO + `campWeekDayId`, `campWeekId` (null on every other row; `other.kind = "CAMP"`); the shared relation set + `campWeekDay`; the week DTO + the effective `windowStart / windowEnd`; the roster (`GET /camp/weeks/:id/days`) + per day `campWeekDayId, teacherIds, startTime, endTime, editedAt`; `createCampWeek` / `updateCampWeek` + `windowStart/End` (HH:MM).
- **Reminder (B):** the fold in the BUILDER (`groupReminders`: `foldCampRows` after the REMINDABLE filter); the job carries `campWeekDayId` + `otherKind`; five CONFIRMED camp hours ⇒ ONE OTHER block `Camp A` / `10:00-15:00` through today's lines — no new copy (your ❓5). The kids' reminder untouched.
- **No money by construction** (pinned): the derived row has no price/rate/head count ⇒ the day-end's `postOtherBookingSale` returns false; `reconcileBookingHolds` returns early for OTHER. **REQ-097:** unchanged predicate; the new route outside the teacher set.
- 🔻 **Pins moved:** the census 47 → 48 in 18 files; `OTHER_KINDS` three → four (two files) + `isOtherKind("CAMP")`; the shared relation set + `campWeekDay` (two files); the camp access rows 10 → 11.

## Pinned (`lib/camp-on-grid-req095-11.test.ts`, 18 tests)
- **The migration by text** (six statements byte-for-byte, the SHARE-lock sentences, the witness, the schema's column/index/relation).
- **The kinds** by value: the four, the three human, the type ⇔ kind pin accepting CAMP on OTHER and refusing it on GROUP, the series/edit validators refusing CAMP, no `z.enum(OTHER_KINDS)` left in the validators (three `HUMAN_OTHER_KINDS`).
- **The pure rules** by value: the default, the bounds, six bad windows ⇒ 400, the hours, the wanted set, the diff (insert/remove/empty), the fold (three inputs ⇒ one span; other rows untouched; two days stay two; no mutation), **and through `groupReminders`**: five CONFIRMED hours + a PENDING ⇒ ONE coach row 10:00-15:00.
- **THE ONE SYNC by VALUE through a fake tx** (`insertBooking` spied): inserts exactly the missing hours as OTHER/CAMP/CONFIRMED/no student, links each to the day, deletes the surplus; a clash ⇒ the 409 naming date · hour · nickname and the error propagates; a CLOSED week and a no-teacher day ⇒ everything deleted, nothing inserted.
- **The lifecycle by source:** createWeek's one tx and per-day sync; updateWeek's three branches and the `edited_at IS NULL` filter; no dates edit; the per-day swap's four refusals/stamps; `syncCampDayRows(tx,` four call sites; the route + access row + outside `TEACHER_ALLOWED`; the DTO lines; the fold wired; no money words in the sync; the OTHER early-return in the holds.
- **`CAMP_ROW_OWNED`:** the guard by value (pass / null / throw with the message); the table of eleven services by source (each function body contains the call on its loaded row); the guard has no `await`/`db.`; through the root app the 409 envelope; the kids' cut untouched.

## 🔑 Mutation — thirty, `finally`, checksum — all bite
A the default 09:00 · B a half-hour window · C start == end · D the last hour included · E the diff never removes · F the diff re-inserts · G the fold keeps every hour · H the fold spans one hour · I CAMP missing from the kinds · J CAMP a human kind · K born PENDING · L not linked to the day · M a closed week derives · N a clash swallowed · O open without the sync · P edited days re-derived · Q CLOSED keeps rows · R no `edited_at` stamp · S the swap on a closed week · T the guard passes all · U/V/W/X the status/move/badges/rental skip the guard · Y the fold not wired · Z the job drops the key · AA the index not partial · AB the FK CASCADE · AC `H:MM` accepted · AD a dates edit accepted. Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final
- **The grid:** a camp hour is `bookingType: "OTHER"`, `other.kind: "CAMP"`, `otherTitle` = the week's name, `status: "CONFIRMED"`, `campWeekDayId`, `campWeekId` (both null on every other row). Merge contiguous cells of one teacher (render-only). Every row write on it ⇒ `409 { code: "CAMP_ROW_OWNED", message: "บล็อกแคมป์แก้ที่สัปดาห์แคมป์" }` — hide the doors when `campWeekDayId` is set.
- **The swap door:** `PATCH /api/camp/weeks/:id/days/:date { teacherIds?: uuid[], startTime?: "HH:MM", endTime?: "HH:MM" }` (key `action:camp.week-open`; admin only) ⇒ `{ day: { date, campWeekDayId, teacherIds, startTime, endTime, editedAt }, inserted, deleted }`; `409 SLOT_TAKEN` names the date, hour and teacher (nothing changed); `409 CAMP_WEEK_CLOSED`; `400` on a bad window (whole hours, 06:00–22:00, start < end); `404` a date outside the week. "Apply to every day" = N calls.
- **Weeks:** `POST/PATCH /api/camp/weeks` + `windowStart` / `windowEnd` ("HH:MM"; default 10:00 / 15:00); the week DTO carries the effective `windowStart` / `windowEnd`; a week-level `teacherIds` / window change re-derives only the days not edited by hand; CLOSED removes every block, OPEN restores them. The dates cannot be edited (open a new week).
- **The roster** `GET /api/camp/weeks/:id/days`: each day + `campWeekDayId, teacherIds, startTime, endTime, editedAt` beside the kids' entries. The day banner unchanged.

📦 Deploy (the human's, after the deploy): `db:migrate` (verify **48**) — the one index build on `bookings` holds writes for its scan. 🚫 No deploy request. ⛔ Only you mark this DONE. ▶️ Next: whatever waits on the board.
