# TASK-406 — Teacher login, own calendar (C-1 + C-2), BE: the user↔teacher link · the `ownScope` predicate on every calendar/bookings read + the scoped `attend` · report own leave with `TEACHER_LEAVE` (`REQ-097`, `SPEC-083`) — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-19) · **Size M + S** · migration `0044_user_teacher_link` (45 = 45) · key 55 · reason code 4. `sid`; a later `uat` deploy.

## §0 Read `SPEC-083` §1 first (the design) — the owner confirmed every §3 decision as recommended: admin-created account with a teacher picker · two accounts for an admin-teacher · leave = a cancel with `TEACHER_LEAVE` · whole day with per-session un-tick · a swapped teacher sees the group from the swap date · no advance-notice cut-off.

## §1 Verify before the contract
1. Every calendar/bookings READ that returns booking rows to the admin app: list them (`GET /calendar` incl. `campWeeks`; `GET /bookings`; `GET /bookings/:id`, `/checkin`, `/posted-sale`; the cancelled tray; the search/paging queries; the attention queries?) — which of them a Teacher role would even be granted (`menu:calendar` + `CAL_BOOK`), and where ONE predicate can be injected without a second copy (the loader / `withBookingRelations` / a shared `where` builder).
2. `booking_teachers` for extras: an EXISTS subquery vs a join — which shape the grid loader already uses.
3. `updateBookingStatus`: where a per-action gate for a linked user sits (`attend` only) and how the QR check-in path (`checkinByToken`) is affected (public — NO scope; a token is a token).
4. The teacher-cancel path for a course seat/session: `cancelSeatsOfGroup`'s shape (cancel + `reconcileCoursePlan`) and the family notice on cancel (`sendClassCancelledToTeacher` is the COACH notice — which one tells the family?) — the leave must send the family the same notice a cancel does, and NOT tell the teacher about their own leave.
5. `END_REASONS` (`course-plan.ts:383`) + `REASON_ENUM_REQUIRED` — where a fourth code lands and what reads the set (the tray labels, the SOM report?).

## §2 The contract I propose — confirm/correct before code
- **Migration `0044`:** `users.teacher_id uuid NULL REFERENCES teachers(id) ON DELETE RESTRICT` + `users_teacher_id_uq` partial UNIQUE (`WHERE teacher_id IS NOT NULL`) = the witness. Catalog-only.
- **`AuthUser.teacherId: string | null`** loaded with the row (one read as today); `/me` returns it. **`lib/own-scope.ts`:** `isScoped(user) = !!user.teacherId`; `ownScopeWhere(user, b)` = `or(eq(b.teacherId, me), exists(booking_teachers where booking_id = b.id and teacher_id = me))` — ONE predicate; every read in §1.1 appends it when scoped; pinned by source (every listed read names `ownScopeWhere`; no read restates `teacherId = ` by hand). Out-of-scope by-id reads ⇒ **404** (the row is not theirs to know about). `campWeeks` filtered to weeks whose `teacher_ids` name me.
- **Users page:** `POST /users` / `PATCH /users/:id` accept `teacherId?: uuid | null` (super admin only, as the rest of the user fields); `409 TEACHER_LINKED` if the teacher already has a user; the users list/DTO carries `teacherId` + `teacherName`.
- **Scoped writes:** `PATCH /bookings/:id/status` for a linked user: the row must be in scope (404 otherwise) and `action` must be `attend` (else `403 SCOPE_TEACHER "บัญชีครูทำได้เฉพาะเช็คอิน"`); every other calendar write refuses a linked user with the same 403 — enforced in ONE place (the guard reads `isScoped` + a `TEACHER_ALLOWED` route set: the reads, `status` (attend), `checkin`, `teachers/me/leave`) — fails closed: a linked user reaches nothing else even if a role grants it. Pinned by value: a linked user with every key still gets 403 on `POST /bookings`.
- **Report own leave (C-2):** `POST /teachers/me/leave { date, sessionIds?: uuid[], reason: string(3..200) }` under **`action:calendar.teacher-leave`** (55th; TH `แจ้งลาสอน (ครู)` / EN `Report own teaching leave`) — linked users only (`403` otherwise); the sessions = my own LIVE rows that day (or the named subset, each verified own + that date); for each: `CANCELLED` + `cancel_reason_code = TEACHER_LEAVE` + `note = reason` + the course's make-up via `reconcileCoursePlan` + the FAMILY notice the cancel sends; a GROUP row I teach ⇒ `cancelSeatsOfGroup` then the row (the 2a shape); ONE tx; a delivered row (ATTENDED) ⇒ `409 SESSION_DELIVERED` naming it, nothing written; ⇒ `{ cancelled: n, bookingIds }`. **No cut-off.** No coach notice (I am the coach).
- **`TEACHER_LEAVE`** joins `END_REASONS` (4th) and the validator's enum; the tray label TH `ครูลา` / EN `Teacher leave`; readers of the set updated by construction (the type).
- 🚫 A teacher creates/moves/cancels nothing else; the ICS feed, the reminder, the public QR untouched; no scope on the backoffice.

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · **45 = 45** · the predicate by value (primary · extra · neither ⇒ 404) and by source across every read · the allowed-route set by value (a linked user with all 55 keys: reads + attend + leave only) · the leave by value (cancels own only, make-up per child, family told, coach not, 409 on delivered, whole day vs subset) · `TEACHER_LEAVE` in the set and the tray · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-19) — §1 verified (one premise is FALSE — §1.4), §2 confirmed with corrections; no code until you confirm

## §1 Verified (read only)
1. **Every read that returns booking rows to the admin app, and what a Teacher role (`menu:calendar` = `CAL_BOOK`) would reach:** (a) `GET /calendar` → `getCalendar`: THREE booking queries — the grid (`findMany` at 494, the `CALENDAR_HIDDEN` / `groupId IS NULL` / `pendingSlot` where), the cancelled tray (539, `?includeCancelled`), and `liveEndDatesForCourses` (a course-level helper — rows never returned, only "is this the last"; no scope needed); plus `campWeeks` via `weeksForCalendar` (camp weeks, not bookings) and the teacher COLUMNS (`teacherRows`). (b) `GET /bookings` → `getBookings`: the page select (850, a `conds[]` `and(...)`) **and its `total` count (882) — two statements, one `cond`**. (c) `GET /bookings/:id/checkin` → `checkin.getCheckinQr` (`bookings.findFirst` by id). (d) `GET /bookings/:id/posted-sale` → `lib/sale-post.ts postedSaleForBooking` (a sale read keyed on the booking — returns money, not the row). There is **no `GET /bookings/:id`** in `api.ts` (the cell's modal opens on the calendar payload). `GET /attention` is `menu:attention` — a Teacher role is not granted it; the search/paging IS `getBookings` (one query, `q`). **The ONE place:** there is no shared where-builder — the grid and the tray use the relational `where` callback, `getBookings` a `conds[]` array. A drizzle condition built once (`ownScopeWhere(me)` returning `or(eq(bookings.teacherId, me), exists(select 1 from booking_teachers where booking_id = bookings.id and teacher_id = me))`) drops into BOTH shapes unchanged (the relational callback may return any SQL condition on the root table), so ONE predicate, appended in four places (grid · tray · page · count) + the two by-id reads through `isOwnBooking(id, me)` (the same predicate + `eq(id)`). Pinned by source: every site names `ownScopeWhere` / `isOwnBooking`; no site writes `eq(bookings.teacherId, me)` by hand.
2. **`booking_teachers` extras:** the grid loader reads them as the RELATION `additionalTeachers: { with: { teacher: true } }` inside `withBookingRelations` (no join in the query); `getBookings` reads them in a second grouped query (`extraTeachers.get(r.b.id)`). Neither shape has a join to hang a filter on ⇒ **the EXISTS subquery** is the fit (no row multiplication, no second copy). ✔ your §2.
3. **`updateBookingStatus(id, action, reason, override, reasonCode)`** loads `current` inside its tx and branches on `action` (`confirm` / `attend` / `cancel` / `sick-leave` …). The per-action gate for a linked user sits at the ROUTE beside `assertMayOverrideLeave` (the same shape: a small lib guard reading `c.get("user")` before the service) — `action !== "attend"` ⇒ `403 SCOPE_TEACHER`; the row-in-scope check goes INSIDE the service (a `scope` argument; after `current` is loaded: `isOwnBooking` ⇒ else `404`) so the scoped write and the scoped read agree on one predicate. The public QR (`POST /api/checkin`, mounted at `index.ts:48` BEFORE `authMiddleware` at 57) never sees a user — **no scope; a token is a token.** ✔ your §1.3.
4. 🔴 **THE FALSE PREMISE — "the family notice the cancel already sends": there is none.** A single cancel writes `CANCELLED` + `note` + `cancel_reason`, calls `sendClassCancelledToTeacher` (`class_cancelled_teacher` — the COACH), returns the consumed unit, and `reconcileCoursePlan` re-owes the make-up. **No parent row is enqueued on a cancel anywhere** (`enqueueParentCopies` has two callers: the confirm and `confirmCourse`); `cancelSeatsOfGroup` (the 2a shape: each live seat `CANCELLED` + `reconcileCoursePlan`) tells nobody. The `sick_leave` / `leave_teacher` / `reschedule_requested` renderers in `line-message.ts` have NO producer in product code (the walkers call them dead kinds). So the leave cannot "send the family the same notice a cancel does" — it would send nothing. ⇒ §2 below proposes the family notice as a NEW outbox kind, PLACEHOLDER by form (the TASK-403 precedent: the path ships, the words come from the owner).
5. **`END_REASONS`** (`course-plan.ts:383`, `["PROGRAM_CHANGED","CUSTOMER_CANCELLED","ADMIN_ERROR"]`) is read by: `isEndReason` (the service's cancel branch, with `REASON_ENUM_REQUIRED = {SINGLE_SESSION, VOUCHER, FIRST_TRIAL, OTHER, GROUP}` — a COURSE_PACKAGE session takes no code today), **`validation.ts:371` `reasonCode: z.enum([...the three])` — a SECOND copy of the set**, and `line-i18n.ts` `ob_reason_<CODE>` (the coach notice's reason line via `cancelReasonText`). The SOM report does not read it. The tray label is the FE's (Fern, by code). A fourth code lands in `END_REASONS` + the validator (which I will re-point at `END_REASONS` so the copy dies) + `ob_reason_TEACHER_LEAVE`.

## §2 The contract — as you wrote, with the corrections
- **`0044_user_teacher_link`** (45 = 45): `ALTER TABLE users ADD COLUMN IF NOT EXISTS teacher_id uuid NULL REFERENCES teachers(id) ON DELETE RESTRICT` + `CREATE UNIQUE INDEX IF NOT EXISTS users_teacher_id_uq ON users (teacher_id) WHERE teacher_id IS NOT NULL` = the witness (kind `index`), rerunnable. Locks: the FK statement takes SHARE ROW EXCLUSIVE on `teachers` for a blink (the 0035/0042 sentence); `users` is tiny. No hot table.
- **`AuthUser.teacherId: string | null`** from the same row read (`toAuthUser`); `DEV_USER.teacherId = null`; `/me` returns `teacherId`. **`lib/own-scope.ts`:** `isScoped(user)`, `scopeOf(user) = user.teacherId ?? null`, `ownScopeWhere(me)` (the OR-EXISTS above), `isOwnBooking(exec, id, me)`. Applied when scoped: the grid, the tray, `getBookings`' page AND count, `getCheckinQr` + `postedSaleForBooking` (404 outside — the row's existence is not theirs), `campWeeks` → weeks whose `teacher_ids` name me. **Correction A — the grid's COLUMNS:** a scoped calendar returns only MY teacher column (rows are scoped anyway; the other coaches' empty columns would render as "nothing booked" — a false statement — and the FE's picker collapses to me regardless). Say if the owner wants every column with only mine filled.
- **Users page:** `createUser` / `updateUser` + `teacherId?: uuid | null`; `409 TEACHER_LINKED "ครูคนนี้มีบัญชีแล้ว"` on a second user for the same teacher (the partial unique index is the backstop, mapped from 23505); the users list/DTO + `teacherId`, `teacherName`. Super-admin only (the group's existing guard).
- **The allowed-route set, fails closed — ONE place, the `accessGuard`:** `TEACHER_ALLOWED` in `route-access.ts` = `GET /calendar`, `GET /bookings`, `GET /bookings/:id/checkin`, `GET /bookings/:id/posted-sale`, `PATCH /bookings/:id/status`, `POST /teachers/me/leave`, + the read-only lookups the calendar page itself needs to render (`GET /teachers`, `GET /subjects`, `GET /settings`? — I will list exactly what the calendar page calls on load from Fern's client and pin the set; nothing that returns another person's booking rows). After the menu + action checks: `if (isScoped(user) && !TEACHER_ALLOWED.has(key)) throw 403 SCOPE_TEACHER "บัญชีครูทำได้เฉพาะดูตารางตัวเอง เช็คอิน และแจ้งลา"`. Pinned by value through the root app: a linked user with ALL 55 keys ⇒ 403 on `POST /bookings`, `PATCH /bookings/:id`, `POST /bookings/:id/pause` …; 200 on the allowed. **Correction B — the `status` body:** the guard cannot read the body, so `action !== "attend"` for a linked user is refused at the route (`assertScopedStatusAction`, beside `assertMayOverrideLeave`) with the same `403 SCOPE_TEACHER`; the row-in-scope 404 inside the service.
- **Report own leave (C-2):** `POST /teachers/me/leave { date, sessionIds?: uuid[], reason: 3..200 }` under `action:calendar.teacher-leave` (55th; TH `แจ้งลาสอน (ครู)` / EN `Report own teaching leave`); a non-linked user ⇒ `403 SCOPE_TEACHER` (the act alone is not enough — the link is the identity). The sessions = my LIVE rows that date (`COURSE_LIVE_STATUSES`, primary OR extra, `groupId IS NULL` — seats ride their GROUP row), or the named subset (each must be one of them, else `404`). Pre-check ALL first: any `ATTENDED` ⇒ `409 SESSION_DELIVERED "คาบ HH:MM สอนไปแล้ว"` naming the first, nothing written. Then ONE tx, per row: a GROUP row ⇒ `cancelSeatsOfGroup` first; `CANCELLED` + `cancel_reason = TEACHER_LEAVE` + `note = reason`; the consumed-unit return (`returnsConsumedUnit` — never true here, pre-checked); `reconcileCoursePlan` for a course row. **Correction C — the notices:** (i) the COACH notice (`class_cancelled_teacher`) goes to the OTHER teachers on the row (primary + extras minus me) — a row where I am an EXTRA still has a primary coach who loses the session; when I am the only teacher, no coach notice (I am the coach); (ii) **the FAMILY notice is NEW** (§1.4): outbox kind `class_cancelled_parent` (the 20th), one row per linked family device (`enqueueParentCopies`, the confirm's shape), rendered through **PLACEHOLDER labels `cl_*` by form** (child · date · time · the reason line `ครูลา` / `Teacher leave`) — the words are Porter's → the owner's, after; a seat's family on a GROUP row gets one too. 🚫 I do NOT wire the new family kind into the ADMIN's cancel (that is a behaviour change the owner has not asked for — it stays coach-only); say if you want it there too. ⇒ `{ cancelled: n, bookingIds, familiesNotified: n }`. No cut-off.
- **`TEACHER_LEAVE`:** `END_REASONS` 4th; `validation.ts` `reasonCode: z.enum(END_REASONS)` (the copy retired); `ob_reason_TEACHER_LEAVE` TH `ครูลา` / EN `Teacher leave`. **Correction D:** `REASON_ENUM_REQUIRED` is unchanged — an ADMIN cancelling a COURSE_PACKAGE session still needs no code; the LEAVE writes the code on every row it cancels regardless of type (it is the leave's own fact).
- 🚫 A linked user creates/moves/cancels/pauses/notes/badges/rents nothing (the set); the ICS feed, the 08:15 reminder, the public QR, the backoffice untouched; no scope for an unlinked user (today's admins byte-identical).

## ❓ for you (five)
1. The FAMILY notice does not exist today (§1.4) — build it as a NEW placeholder kind on the leave path (the TASK-403 precedent), words to follow from the owner? And keep the admin's cancel coach-only as today?
2. Correction A — a scoped grid returns only my column?
3. Correction C(i) — the other teachers on a row I leave are told with the existing coach notice?
4. The exact `TEACHER_ALLOWED` read set — I will take it from Fern's calendar-page client calls and pin it; say if any lookup must be excluded.
5. Size: with the new family kind + its placeholder + the walkers' census (19 → 20 kinds) this is **M + M**, not M + S. OK?

## For @Fern (draft, final after confirm)
`/me` + `teacherId`; users create/edit + `teacherId`, list + `teacherId`/`teacherName`, `409 TEACHER_LINKED`; a scoped `GET /calendar` = my column, my rows, my camp weeks; `GET /bookings` my rows; `403 SCOPE_TEACHER` on every other calendar write (and on `status` with `action ≠ attend`); `POST /teachers/me/leave { date, sessionIds?, reason }` ⇒ `{ cancelled, bookingIds, familiesNotified }`, `409 SESSION_DELIVERED`; reason code `TEACHER_LEAVE` in the tray (label TH `ครูลา` / EN `Teacher leave`); key `action:calendar.teacher-leave`.

⛔ Waiting on your confirm (the five ❓) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-19) — the five answers; build
1. **The FAMILY notice — YES, a NEW outbox kind `class_cancelled_parent` on the leave path, PLACEHOLDER by form**; the words come from the owner via Porter (I am sending the draft now). **The admin's cancel stays coach-only** — that gap is the owner's to close, flagged to Porter, not built here. Build the kind so wiring it into the admin cancel later is one call site (`enqueueParentCopies` from the cancel branch), nothing more.
2. **Correction A — YES:** a scoped grid returns only my column. Empty columns for other coaches would be a false statement.
3. **Correction C(i) — YES:** the other teachers on a row I leave get the existing coach notice; none when I am the only teacher.
4. **`TEACHER_ALLOWED` from Fern's calendar-page client calls — YES**, pinned by value. Exclude anything that returns another person's booking rows or people data (`GET /students*`, `/parents*`, `/courses*`, `/attention`, `/reports*`); a lookup list (`/teachers`, `/subjects`, `/settings` reads the page needs) is fine. List the set in the report so Fern's page and the set are the same list.
5. **M + M — OK.**
Also: SPEC-083 §1.4's "the family notice the cancel already sends" is WRONG — I am correcting the SPEC. **Go.**

---

# 📤 REPORT — @Jason → @Sober (2026-09-19)

✅ **DONE (code), against the confirmed contract (all five yes).** **2535 pass / 0 fail**, **196 files** (+1) · `tsc --noEmit` clean · 🔴 **migration `0044_user_teacher_link` — 45 = 45** · offline preflight: `0044` alone `[]`, the batch `0038 → 0044` `[]`.

## What was built
- **`drizzle/0044_user_teacher_link.sql`** (2 statements): `users.teacher_id uuid NULL REFERENCES teachers(id) ON DELETE RESTRICT` + `users_teacher_id_uq` partial UNIQUE = **the witness** (kind `index`, rerunnable). Header: the order, `db:verify` expects 45, SHARE ROW EXCLUSIVE on `teachers` for the FK's blink, no hot table, no enum. Schema: the column, the partial index, `usersRelations.teacher`.
- **`lib/own-scope.ts`** (new) — `isScoped(u) = !!u.teacherId` (**the link, not a grant — fails closed**; a super admin WITH a link is scoped too), `scopeOf(u)`, **`ownScopeWhere(me)`** = `bookings.teacher_id = me OR EXISTS (select 1 from booking_teachers where booking_id = bookings.id and teacher_id = me)` — one drizzle condition that drops into the relational `where` callbacks and `getBookings`' `conds[]` unchanged (never a join); `isOwnBooking(exec, id, me)` / `assertOwnBooking(id, me)` (outside ⇒ **404** `ไม่พบคาบเรียน`; an unscoped caller never reads); `SCOPE_TEACHER()` = `403 "บัญชีครูทำได้เฉพาะดูตารางตัวเอง เช็คอิน และแจ้งลา"`; `assertScopedStatusAction(u, action)` (a linked user: `attend` only); `assertLinked(u)` (the leave needs the link, not just the key).
- **`AuthUser.teacherId`** from the same row read (`toAuthUser`; `DEV_USER.teacherId = null`); `/me` returns `teacherId`. **The access guard** — after the menu and action checks: `if (isScoped(user) && !TEACHER_ALLOWED.has(key)) throw SCOPE_TEACHER()`. **`TEACHER_ALLOWED`** (`route-access.ts`, from Fern's calendar page + its booking modal — the same list for TASK-407): `GET /calendar` · `GET /bookings` (the month + paused reads) · `GET /teachers` (the roster lookup) · `GET /badges` (the legend) · `GET /bookings/:id/checkin` · `GET /bookings/:id/posted-sale` · `PATCH /bookings/:id/status` (`attend` only, at the route) · `POST /teachers/me/leave`. Nothing that returns people/courses/attention/reports. `/me` and `/permissions` are outside the table as before.
- **The scoped reads** (`scope` argument, `null` = byte-identical for every admin): `getCalendar` — **MY column only** (Correction A), the grid and the cancelled tray each `and(…, ownScopeWhere(scope))`, `campWeeks` filtered to weeks whose `teacher_ids` name me; `getBookings` — `conds.push(ownScopeWhere(scope))` on the ONE `cond` the page and the `total` count share; the two by-id reads (`/checkin`, `/posted-sale`) and the status write go through `assertOwnBooking` at the route (`sale-post.ts` and `checkin.service.ts` untouched). The public QR is mounted before the auth guard — unscoped, pinned by source order.
- **Users page:** `createUser` / `updateUser` + `teacherId?: uuid | null` (null clears the link); `assertTeacherFree` ⇒ `404` unknown teacher / **`409 TEACHER_LINKED "ครูคนนี้มีบัญชีแล้ว"`** (the partial index's 23505 mapped as the race backstop); the DTO + `teacherId`, `teacherName` (`listUsers` loads `teacher: true` in the same read; `userDTO` one small read).
- **The leave (C-2)** — `POST /teachers/me/leave { date, sessionIds?: uuid[1..50], reason: 3..200 }` under **`action:calendar.teacher-leave`** (55th; TH `แจ้งลาสอน (ครู)` / EN `Report own teaching leave`), inserted BEFORE `/teachers/:id` (TASK-029). `reportOwnLeave(me, input)`: my rows that date (`ownScopeWhere(me)`, `groupId IS NULL`, LIVE + ATTENDED read), the named subset verified (else 404); **any ATTENDED ⇒ `409 SESSION_DELIVERED "คาบ HH:MM สอนไปแล้ว — แจ้งลาไม่ได้"` BEFORE the tx, nothing written**; ONE tx, per LIVE row: a GROUP row's seats via `cancelSeatsOfGroup` first; `CANCELLED` + `cancel_reason = TEACHER_LEAVE` + `note = reason` (regardless of type — Correction D; the admin's `REASON_ENUM_REQUIRED` unchanged); `reconcileCoursePlan` for a course row; **the other teachers' coach notice** (`sendClassCancelledToOtherTeachers`: primary + extras minus me, CONFIRMED-only like the admin's — Correction C(i)); **the FAMILY's NEW notice** `class_cancelled_parent` per linked device (`enqueueParentCopies`; a GROUP row's seat families too), CONFIRMED-only (a PENDING session was never announced). ⇒ `{ cancelled, bookingIds, familiesNotified }`. No cut-off. Never the single-coach notice (me).
- **`class_cancelled_parent`** (the 20th outbox kind, `line-message.ts`): the coach's `class_cancelled` block under **PLACEHOLDER `cl_title`** (📖 MINE, owner has NOT seen — by form), the audience rule hides `coach` for a parent, the reason line renders `ครูลา` / `Teacher leave`. **ONE producer — the leave**; the admin's cancel stays coach-only (pinned: the cancel branch has no `enqueueParentCopies`), and wiring it later is one call there.
- **`TEACHER_LEAVE`** — `END_REASONS` 4th; **`validation.ts` `reasonCode: z.enum(END_REASONS)`** (the second copy retired); `ob_reason_TEACHER_LEAVE`.
- 🔻 **Pins moved (all with the reason):** `44 = 44` → `45 = 45` in 16 files; the outbox kind census 19 → 20 in three walkers; the registry 54 → 55 (two files); `END_REASONS` three → four (two files); the ended-course classification table + `POST /teachers/me/leave: "unrelated"` (an ended course's rows are never LIVE); the tray's where/route lines (+ the scope term / argument); `enqueueParentCopies` callers 2 → 3; the user DTO's key list + `teacherId`/`teacherName`; `actorOf` 14 → 15; the Stage-2 exclusion regex ANCHORED at the path root (it was unanchored and would have matched `/teachers/me/leave` as `/me`); `listUsers` `with: { role: true, teacher: true }`; `/me` shapes + `teacherId: null` (4 files); `DEV_USER`/the loaded row + `teacherId`. The `|| undefined` census (nine) untouched — the family branch reads the student from the worker's ctx only.

## 📦 Deploy steps (the human's, after the deploy)
`bun run db:migrate` (0038 → 0044 in journal order; `db:verify` expects **45**). Then the super admin creates the "Teacher" role (`menu:calendar` + `action:calendar.status` + `action:calendar.teacher-leave`) and links each teacher's account on the Users page. 🚫 No deploy request.

## Pinned (`lib/teacher-own-calendar-req097.test.ts`, 17 tests)
- **The migration by text** (45 = 45, two statements byte-for-byte, RESTRICT, no DEFAULT/enum/bookings, the lock sentence, the witness, the schema column/index/relation).
- **Own scope by value:** `isScoped`/`scopeOf` (the dev user, a linked user, a linked SUPER ADMIN is scoped); **the predicate's SQL** via `toSQL()` — `teacher_id = $1 or exists (select 1 from booking_teachers …)`, no join, params `[me, me]`; `isOwnBooking`/`assertOwnBooking` through a fake exec (mine · not mine ⇒ 404 with the message · an admin never reads); by source: the ONE predicate twice in `getCalendar`, the column filter, the camp-week filter, `getBookings`' push on the one `cond`, no hand-written `eq(bookings.teacherId, scope)`, the three `assertOwnBooking` route sites, the public QR before the auth guard.
- **The fail-closed set through REAL tokens** (a mini app with the real `authMiddleware` + `accessGuard` and EVERY table route registered): a linked user with ALL 55 keys ⇒ **200 on exactly the eight, `403 SCOPE_TEACHER` on the other 95**; an unlinked user with the same keys ⇒ 200 on writes and people reads; the set by value; every member in the table; no people/courses/attention/reports; the guard's order (menu → action → scope) by source.
- **`attend` only / the link / the users page / `/me`:** `assertScopedStatusAction` four actions × two users; `assertLinked`; the validators; the DTO's `teacherId`/`teacherName`; the service's `TEACHER_LINKED` lines; the 55th key with its labels; through the root app: an unlinked account's `POST /teachers/me/leave` ⇒ 403 before the service, the body shape 400, an admin's `cancel` still reaches the service.
- **The leave:** the four reasons + the validator reading the set (no copy) + the labels; the source (the select with the predicate, the subset 404, the 409 BEFORE `db.transaction`, one tx, the cancel's four writes, the other-teachers notice minus me, the family kind's ONE producer, the admin's cancel branch without `enqueueParentCopies`, the route before `/teachers/:id`); **the family message by FORM** in TH/EN (child · `05-10-2026` · time · `ครูลา`/`Teacher leave` · the placeholder title · no raw ISO · trimmed) and the coach message untouched; through the root app with a linked DEV user: the leave reaches the service with `me`, the `409 SESSION_DELIVERED` envelope, and a linked user's `cancel` on the status route ⇒ 403 with the service untouched.

## 🔑 Mutation — thirty, `finally`, checksum — all bite (one runner bug of mine fixed on the way: a JS `&&` that changed no SQL)
A scope as a flag · B the predicate forgets the extras · C outside ⇒ 403 · D an admin is checked · E cancel allowed · F the leave without a link · G the guard's scope line dropped · H `teacherId` not carried · I `POST /bookings` in the set · J `GET /students` in the set · K the grid unscoped · L the tray unscoped · M the columns unscoped · N a hand-written predicate · O the camp weeks unscoped · P a delivered session cancelled · Q no reason code · R no make-up · S the family not told · T the coach notice to me · U the leave reaches others' rows · V no tx · W the route shadowed by `/teachers/:id` · X the QR read unscoped · Y the index not unique · Z `ON DELETE CASCADE` · AA two users one teacher · AB `TEACHER_LEAVE` missing · AC the validator's own copy · AD the admin's cancel tells the family. Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final
- **`GET /api/me`** + `teacherId: string | null` — set ⇒ the account is scoped (the FE shows it, never decides by it).
- **Users:** `POST /users` / `PATCH /users/:id` + `teacherId?: uuid | null` (null clears); `409 TEACHER_LINKED` "ครูคนนี้มีบัญชีแล้ว"; the list/DTO + `teacherId`, `teacherName`. The picker = `GET /teachers`.
- **Scoped reads:** `GET /calendar` returns only my teacher column, my rows (grid + `cancelled` tray), my `campWeeks`; `GET /bookings` (incl. `?status=PAUSED`) my rows + count; `GET /bookings/:id/checkin` and `/posted-sale` ⇒ `404` outside my scope.
- **Scoped writes:** `PATCH /bookings/:id/status` — `attend` only (`403 SCOPE_TEACHER` otherwise, `404` outside scope); EVERY other calendar/people/course/sales route ⇒ `403 { code: "SCOPE_TEACHER", message: "บัญชีครูทำได้เฉพาะดูตารางตัวเอง เช็คอิน และแจ้งลา" }` regardless of role. The allowed set = `GET /calendar`, `GET /bookings`, `GET /teachers`, `GET /badges`, `GET /bookings/:id/checkin`, `GET /bookings/:id/posted-sale`, `PATCH /bookings/:id/status`, `POST /teachers/me/leave` — the calendar page must call nothing else when `teacherId` is set.
- **Leave:** `POST /api/teachers/me/leave { date: "YYYY-MM-DD", sessionIds?: uuid[1..50], reason: 3..200 }` (key `action:calendar.teacher-leave`) ⇒ `{ cancelled: n, bookingIds: uuid[], familiesNotified: n }`; `409 SESSION_DELIVERED` (message names the session's time; nothing cancelled), `404` for a session not mine/that date, `403 SCOPE_TEACHER` for an unlinked account.
- **Reason code:** `TEACHER_LEAVE` appears in `cancelReason` on cancelled rows (tray label TH `ครูลา` / EN `Teacher leave`); the admin's cancel dialog does NOT offer it.

⛔ Only you mark this DONE. ▶️ Next for me: whatever waits on the board.
