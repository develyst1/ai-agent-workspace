# TASK-445 — 🔴 DEFECT (Tanya, blocks Khwan's invite): camp-week create INCLUDING today ⇒ **500** on a coach clash (must be a 409 naming the clash) · group cancel-all's family notices: de-dupe per HOUSEHOLD per row and make `familiesTold` honest — BE, S

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-22) · **Size S.** No migration (53 = 53). `sid`; before the customer-UAT.

## §1 The 500
- Repro (Tanya): `POST /camp/weeks` for a week that includes TODAY, the coach has near-term sessions inside the 10:00–15:00 window ⇒ **500 INTERNAL**; a future week ⇒ 201. `syncCampDayRows` (`camp.service.ts:154`) catches only `ApiException SLOT_TAKEN` from `insertBooking` and rethrows anything else — the clash on a today-week is arriving as something else (my reads: a raw pg `23505` from the unique slot index that `insertBooking`'s own check does not pre-empt for a CONFIRMED-born row against a PENDING holder? or `assertTeacherBookable` on a day already past its hours? or the per-day `date < today` guard missing — the create loops every date of the week including ones BEFORE today when the week started yesterday). Find the actual exception (reproduce with a today-week over a coach who has a session in the window; and a week whose `startDate` is BEFORE today), then: **every clash on the create/update/sync path ⇒ `409 SLOT_TAKEN` with the date · hour · coach sentence the sync already builds; the whole create rolls back (nothing written)**; a week whose range starts before today: derive rows only for dates ≥ today (say if you read the §11 rule otherwise — a past camp day has no coach block to derive). Pin both by value through the root app (409 envelope, no rows).

## §2 The family notices on group cancel-all
- Read: `sendClassCancelledToFamilies` builds `households = seats.map(s => [s.studentId])` per row ⇒ two SIBLINGS seated on the same row ⇒ the same LINE account told TWICE for the same class (a real dup); and cancel-all calls it per ROW ⇒ one household with one child on 6 rows gets 6 bubbles (each a true, dated "class cancelled") — Tanya's `familiesTold: 6`.
- Fix: (a) **per row, union the seats' household LINE ids and enqueue each id ONCE** (the DUO helper's de-dupe shape — `householdLineUserIds` over ALL the row's seat students, then one copy per distinct id); the single-seat cancel path shares it. (b) **Cancel-all's response = `{ cancelled, seatsCancelled, familyNotices, householdsTold }`** — `familyNotices` = outbox rows enqueued (row × distinct account), `householdsTold` = distinct LINE accounts across the whole cancel-all. The per-row notice STAYS (each names its date; the family message family has no series form — a one-summary-per-household kind would be new bytes, the owner's — I flag it, not built).
- Pins: two siblings on one row ⇒ one copy per account; one child on six rows ⇒ six notices, `householdsTold: 1`; the Private/DUO path byte-identical.

## Definition of Done
- [ ] The 500's cause named · both fixes by value · suite **count** · tsc 0 · 53 = 53 · Fern's line (the response's two new fields; TASK-442's toast reads `householdsTold`) · 🔑 Break-and-watch, `finally`, CHECKSUM · report here + `inbox/SA.md` + log.

---

# ✅ CODE DONE — @Jason → @Sober (2026-09-22) — the 500's cause named and fixed; the family de-dupe + honest counts; 2783 pass / 0 fail; 9/9 mutations bite

**Numbers:** `bun test` **2783 pass / 0 fail**, 213 files (+1: `src/lib/camp-week-500-family-dedupe-req104.test.ts`, 7 tests) · `tsc --noEmit` **0** · 53 = 53 (no migration) · 9 pins moved (the two camp fake txs gain `teachers.findMany`; the family sender's source/by-value pins in the DUO and own-leave files — a GROUP row now counts `1` for "its families were told" with all the accounts still landing; TASK-441's cancel-all shape and the route spy → `familyNotices` / `householdsTold`).

## §1 The 500 — the cause, by reading and by value
**Not a today rule, not a raw `23505` escaping, not a past-date guard — an ABORTED-TRANSACTION read.** The clash arrives exactly as designed: `insertBooking`'s catch turns the slot index's `23505` into `409 SLOT_TAKEN`. But that `23505` has already put the transaction into the aborted state, and `syncCampDayRows`' catch then ran `tx.query.teachers.findFirst(...)` **on that same tx** to print the coach's nickname ⇒ Postgres `25P02` ("current transaction is aborted, commands ignored until end of transaction block") — a raw pg error, not an ApiException ⇒ the router's 500. A future week gave 201 only because the coach had nothing to clash with; ANY clash on the create / day-edit path was a 500 (the OTHER series' add-dates catch never touched the tx, which is why it stayed a 409). **Fix:** the coach names are read ONCE before the insert loop (from the wanted set, one `findMany`), and the catch touches memory only. Pinned by value with a fake tx that throws `25P02` on every read after the clash: the sync answers `409 SLOT_TAKEN` "วันที่ <date> <hour> ครูเอก มีคาบแล้ว — ไม่ได้บันทึกอะไร"; through the ROOT app, `POST /camp/weeks` for a week including today over a coach with a 13:00 session ⇒ the 409 envelope naming the clash, the transaction callback rejected (nothing committed — the rows attempted before the clash roll back with it). Mutation A (the old read restored) bites; B (names read after the loop) bites.
**The past-date line (your §11 reading, taken):** `syncCampDayRows` returns `{ inserted: 0, deleted: 0 }` for a day dated before today — nothing derived (a week created mid-week cannot clash on yesterday's sessions) and **nothing deleted** (a past block that exists is history; a later edit of the week leaves it alone). Pinned: no insert, no delete, no read beyond the day row. (If you would rather a past day's rows be deleted on a re-sync, one line — mutation D shows the pin.)

## §2 The family notices
- **The core — `classCancelledFamilyAccounts(tx, row, reason): string[] | null`:** the row's household set is ONE set — `[studentId, coStudentId]` for a Private/DUO row (byte-identical), **ALL the seats' students for a GROUP row** (TASK-420 built a set PER SEAT, which enqueued the same LINE account once per sibling) — through the ONE accessor, so two siblings on one row reach their family ONCE (by value: `["U1", "U1b", "U2"]`, U1 not twice); `null` when nothing was sent (not CONFIRMED / no student), `[]` when the household has no linked account (the skipped row still lands). **`sendClassCancelledToFamilies`** stays for its two single-row callers (the own-leave, the admin's cancel) as `0 | 1` — 1 = "this row's families were told" (an unlinked household still 1, the skipped row landed — as before). ONE `class_cancelled_parent` producer (pinned).
- **Cancel-all's response = `{ cancelled, seatsCancelled, familyNotices, householdsTold }`** — `familyNotices` = outbox rows enqueued (rows × distinct accounts), `householdsTold` = distinct LINE accounts across the whole call. By value: one child on six rows ⇒ `familyNotices` 13 with a second family on the sixth row, `householdsTold: 3` (the union); Tanya's case (one family, six seats) reads `familyNotices: 6 · householdsTold: 1`. `familiesTold` is gone (pinned by absence). The per-row notice STAYS — each names its own date; a one-summary-per-household kind would be new words (the owner's; flagged, not built).

## Break-and-watch — `mut445.mjs`, 9 mutations, **9 bite** (`try/finally`, sha-256 restore byte-identical)
A the catch reads the aborted tx again · B the names read after the loop · C a past date derives rows · D a past day's rows deleted · E a set per seat again · F the co-student dropped · G the wrapper counts accounts · H `householdsTold` counts notices · I `familyNotices` counts rows.

## Contract lines for @Fern (TASK-442's toast)
- `POST /group-series/:key/cancel-all` ⇒ `{ cancelled, seatsCancelled, familyNotices, householdsTold }` — the toast reads `householdsTold` ("N households told"); `familiesTold` no longer exists.
- `POST /camp/weeks` / `PATCH /camp/weeks/:id/days/:date` on a clash ⇒ `409 { code: "SLOT_TAKEN", message: "วันที่ … <hour> ครู<nick> มีคาบแล้ว — ไม่ได้บันทึกอะไร" }`, nothing created.

📦 Deploy (the human's): the batch as is (53). 🚫 No deploy request. ⛔ Only you mark this DONE.

---

# ✅ DONE — REVIEWED by @Sober (2026-09-22)
Re-run by me: **2783 pass / 0 fail** · tsc 0 · 53 = 53 · `familiesTold` gone, `householdsTold` = distinct accounts. The cause (a read on an aborted tx ⇒ `25P02` ⇒ 500) is a general lesson — recorded in SYSTEM-FACTS. Past rows on a re-sync: leave as is (a past camp day's rows are history). Fern's one-line toast follow now.
