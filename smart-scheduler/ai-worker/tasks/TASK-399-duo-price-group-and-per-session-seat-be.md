# TASK-399 — DUO price group + the price follows the GROUP's kind + the per-session seat, BE (`REQ-095` Stage 2b, `SPEC-081` §3 2b) — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-19) · **Size S+S** · no migration (42 = 42) · `sale:ensure-items` +4 (a deploy step). `sid`; a later `uat` deploy.

## §0 Owner's rulings (REQ-095 §8, via Porter)
DUO prices (VAT-incl): 1h **1,900** · 4 **6,800** · 6 **9,360** · 10 **14,200**. Group = the existing `balance-group` (1,090 / 5,290 / 7,790) — nothing to add. Teacher rate = backoffice (stored only, as Stage 1). **Student revenue posts here like Private.** The per-session seat = walk-in at the 1h tier.

## §1 Verify before the contract
1. `CARD` in `lib/sale-items.ts` accepts a 4-size (the type says `1 | 4 | 6 | 10`) — `bike-skate` has one; confirm `isSellable` and the course-size validators accept `size 4` for a new group without a second list.
2. `resolvePriceGroup(subjectId)` callers (`:1202`, `:1213`, `:1306`, the course create) — list every call; each needs the group kind in hand or a `null` (solo). Where does the caller know it is inside a group (`groupKey` on the course; `groupId` on a single session)?
3. `VOUCHER_EXCLUDED_GROUPS` — DUO joins; `voucherAllowsProgram` refuses it by construction.
4. The single-session sale path (`revenueItemRef("SINGLE_SESSION", group)` ⇒ `session-{group}`) and the day-end revenue sweep (`inArray(bookingType, [FIRST_TRIAL, SINGLE_SESSION, OTHER])`) — a seat that is a `SINGLE_SESSION` is IN that list and posts `session-balance-duo` / `session-balance-group` at ATTENDED. Confirm that is the walk-in shape the owner named ("pay at shop", posted when attended, like Private).

## §2 The contract I propose — confirm/correct before code
- **`lib/sale-items.ts`:** `PriceGroup` gains `"balance-duo"`; `CARD["balance-duo"] = { 1: 1900, 4: 6800, 6: 9360, 10: 14200 }`; `VOUCHER_EXCLUDED_GROUPS` +DUO; the items list grows by four (`session-balance-duo`, `course-balance-duo-4/6/10`) through the SAME flatMap — no hand-written rows. `sale:ensure-items` adds them (additive; never overwrites a price) — the deploy step after the build, noted in the report.
- **`resolvePriceGroup(subjectId, groupKind?: "DUO"|"GROUP"|null)`** — `DUO ⇒ "balance-duo"`, `GROUP ⇒ "balance-group"`, else the subject's group as today. ONE function, every caller passes what it knows: the course create passes the group's kind when `groupKey` is present (read from the group's first live row — the same read as `assertCourseMatchesGroup`); the single-session create passes the group row's kind when `groupId` is present. Pinned by value (three branches) and by source (no caller computes a price group any other way).
- **The per-session seat:** `POST /bookings { bookingType: "SINGLE_SESSION", groupId, … }` — the group row of THAT date (`404` if not a GROUP row; `400` if the body's teacher/date/start differ from the group's — `GROUP_MISMATCH`, the same sentence); cap via the SAME `seatOnGroup` count (`409 GROUP_FULL`); inserted with `groupId` through `insertBooking` (outside the index). Money: exactly today's single-session path with the group-kind price — nothing new posts. The seat rides the reminder/DTO shapes of TASK-397 (a seat is a seat).
- 🚫 A `SINGLE_SESSION` seat cannot EXTEND the group (a walk-in on a date with no group row ⇒ `404 GROUP_NO_DATE "ไม่มีกลุ่มในวันที่ …"`) — extension is the course's privilege (a planned series), not a walk-in's. Say if you read the owner otherwise.
- DTO: unchanged (`groupId`/`groupName` on the seat; the group's `seats[]` carries it with `courseId: null`).

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · 42 = 42 · the four items by value (prices, refs) · the three-branch resolver by value + every caller by source · the walk-in seat by value (cap, mismatch, no-extend, the posted ref) · `sale:ensure-items` dry-run output in the report · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-19) — §1 verified (one caller your list missed), §2 confirmed with corrections; no code until you confirm

## §1 What I found
1. **A 4-size on a new group** — `CARD`'s value type is `Partial<Record<1 | 4 | 6 | 10, number>>` (`bike-skate` and `onewheel` carry a 4); `SALE_ITEMS` and `sellablePackages` flatMap over `[1, 4, 6, 10]` per group; `isSellable(group, size)` reads the same card; `isCourseSize` (recurring.ts) is `4 | 6 | 10`. ⇒ `balance-duo: { 1, 4, 6, 10 }` produces four items and sells at every size with NO second list. ✅
2. **`resolvePriceGroup(subjectId)` has FIVE callers, not four** — `insertBooking`'s voucher gate (`:1202`) and single-session sellable gate (`:1213`), `captureBookingDiscount` (`:1306` — the % discount's line total), the course create (`:1884` — the price group + the sale), and 🔴 **the day-end REVENUE SWEEP (`jobs.service.ts:173`)** — the one that actually POSTS a single session's revenue at ATTENDED. Your list stopped at the create; if the sweep kept pricing by subject, a DUO walk-in would post `session-balance-private` (฿1,390) instead of `session-balance-duo` (฿1,900) — the price would follow the kind at booking and the subject at posting. **The sweep must resolve by the group's kind too.** Where each caller knows: the course create has `input.groupKey` (the template's `other_kind`, the same read as `assertCourseMatchesGroup`); the single-session create has `input.groupId` (the group row's `other_kind`) — resolved ONCE in `createBooking` and handed down to `insertBooking`'s two gates and `captureBookingDiscount` as `input.groupKind`; the sweep selects `groupId` and reads the kind per seat row (a walk-in only — a course seat never posts at day-end).
3. **`VOUCHER_EXCLUDED_GROUPS`** — `balance-group` is already in it; `balance-duo` joins; `voucherAllowsProgram` refuses it by construction (`voucherAllowedGroups()` filters the same set). ✅ (A VOUCHER seat is not 2b anyway.)
4. **The walk-in shape** — `revenueItemRef("SINGLE_SESSION", group)` ⇒ `session-{group}`; the sweep's list is `[FIRST_TRIAL, SINGLE_SESSION, OTHER]`, so a `SINGLE_SESSION` seat IS swept and posts `session-balance-duo` / `session-balance-group` on the `rev:<bookingId>` key at ATTENDED — "pay at shop, posted when attended, like Private" — **once the sweep prices by kind (§1.2)**. ✅ The seat draws no freelance hour (TASK-397's rule); the group row draws.

## §2 The contract — confirmed with corrections
- **`lib/sale-items.ts`** — `PriceGroup` + `"balance-duo"`; `PRICE_GROUPS` + it; `CARD["balance-duo"] = { 1: THB(1900), 4: THB(6800), 6: THB(9360), 10: THB(14200) }`; `VOUCHER_EXCLUDED_GROUPS` + it. The four items (`session-balance-duo`, `course-balance-duo-4/6/10`) fall out of the SAME flatMap — pinned by value (refs + prices). `sale:ensure-items` is insert-only (its own header) ⇒ +4 on each box; **the dry-run output goes in the report** (the script has no dry-run flag — ❓ I'll run it against no DB and print the seeds it WOULD insert via the pure `SALE_ITEMS` diff; say if you want a real `--dry-run` flag added to the script instead).
- **`resolvePriceGroup(subjectId, groupKind?, exec?)`** — `"DUO" ⇒ "balance-duo"`, `"GROUP" ⇒ "balance-group"`, else the subject's group as today. **ONE function; the FIVE callers pass what they know** (pinned by source: every `resolvePriceGroup(` call site names its second argument; the count of call sites is pinned). `groupKindOf(exec, groupId)` = the group row's `other_kind` (`DUO | GROUP | null`) — the one reader the create and the sweep share.
- **The walk-in seat — `POST /bookings { bookingType: "SINGLE_SESSION", groupId, student, teacherId, subjectId, date, startTime, … }`** — `groupId` is accepted on `SINGLE_SESSION` ONLY (validation refuses it on the other four types — the TASK-224 shape); the service: the row must be a LIVE GROUP row ⇒ else `404 "ไม่พบกลุ่ม"`; the body's teacher / date / start must equal the row's ⇒ else `400 "คาบต้องใช้ครู/วัน/เวลาเดียวกับกลุ่ม"` (the course's sentence, one word different); **the cap through the SAME count** — `seatOnGroup` splits into `assertSeatFree(tx, row, date)` (the cap half: live seats < `head_count` ⇒ else `409 GROUP_FULL` naming the date) and the extend half; the walk-in calls the cap half with the row it was given — **no extend by construction** (a walk-in names a row, never a date without one; the "no group on that date" case IS the 404 above — ❓ so no `GROUP_NO_DATE` code is reachable; I'll omit it unless you want the sentence for a date-shaped FE error). Inserted with `groupId` through `insertBooking` (outside the index; the kind pin passes — a lesson carries no `other_kind`). Money: today's single-session path — the sellable gate, the discount capture and the day-end post all priced by the kind; nothing new posts.
- **DTO / reminder / guards:** unchanged from 2a — the seat's `groupId`/`groupName`, the group's `seats[]` with `courseId: null`; the coach's folded entry (the child's line with no balance); the parent's entry; archive + suspension guards apply (they sit in `createBooking` / `insertBooking`).
- 🚫 No migration; no change to the group row; no VOUCHER seat; `FIRST_TRIAL` stays one price (it ignores the group as it ignores the subject).

## For @Fern (draft, final after confirm)
`GET /sellable-packages` gains the four `balance-duo` items (the course form inside a DUO group reads its prices from the kind — the BE prices the sale by the group, not the form) · `POST /api/bookings { bookingType: "SINGLE_SESSION", groupId, … }` ⇒ `201 { booking }` | `404 "ไม่พบกลุ่ม"` | `400 "คาบต้องใช้ครู/วัน/เวลาเดียวกับกลุ่ม"` | `409 GROUP_FULL` naming the date — the seat's price is the group's 1h tier (DUO ฿1,900 · Group ฿1,090), posted at ATTENDED like Private · a DUO course's sale posts `course-balance-duo-{size}`.

⛔ Waiting on your confirm (the sweep as the fifth caller · the dry-run shape · dropping `GROUP_NO_DATE`) before code. TASK-401's contract follows once this is in, per your order.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-19) — build to your §2, four notes
1. **The sweep as the fifth caller — YES, and it is the important one.** Pin it by value: a `SINGLE_SESSION` seat under a DUO group ATTENDED by the day-end posts `session-balance-duo` (190000), never `session-balance-private`; the mutation that lets the sweep price by subject must bite.
2. **Dry-run: add a real `--dry-run` flag to `scripts/ensure-sale-items.ts`** (prints the refs it WOULD insert, touches nothing). The human runs the deploy step, and "what will this add?" is his question every time — a flag is worth more than a one-off diff in the report. Put both the flag's output and the `+4` in the report.
3. **Drop `GROUP_NO_DATE` — YES.** A walk-in names a row; "no group that day" is the 404. TASK-400's §0 loses the code.
4. **One addition for Fern: the group DTO gains `priceGroup`** (`"balance-duo"` | `"balance-group"`), derived from the kind by the SAME mapping the resolver uses (one function, exported, pinned) — so the FE picks the card by a server-given name and never maps kind → price group itself.
Everything else stands. **Go.** TASK-401's contract after this is in.

---

# 📤 REPORT — @Jason → @Sober (2026-09-19)

✅ **DONE (code), against the confirmed contract and your four notes.** **2480 pass / 0 fail**, **193 files** (+1) · `tsc --noEmit` clean · 🚫 **no migration (42 = 42)**.

## What was built
- **`lib/sale-items.ts`** — `PriceGroup` + `"balance-duo"`, `PRICE_GROUPS` + it, `CARD["balance-duo"] = { 1: 1900, 4: 6800, 6: 9360, 10: 14200 }` (VAT-incl., the owner's §8 figures), `VOUCHER_EXCLUDED_GROUPS` + it, and **`GROUP_KIND_PRICE_GROUP = { DUO: "balance-duo", GROUP: "balance-group" }`** — the ONE mapping the resolver and the DTO both read (your note 4). The four items (`session-balance-duo`, `course-balance-duo-4/6/10`) fall out of the existing flatMap — no hand-written rows (pinned: the card is the only place the prices appear).
- **`resolvePriceGroup(subjectId, groupKind?, exec?)`** — `DUO ⇒ balance-duo`, `GROUP ⇒ balance-group` (no subject read), else the subject's group; `null` subject ⇒ `null`. **`groupKindOf(exec, groupId)`** — a LIVE GROUP row's kind or `null` (an OTHER row with a DUO kind ⇒ `null` — the TYPE decides). **The FIVE callers, each naming what it knows** (your note 1): `insertBooking`'s voucher gate + sellable gate (`input.groupKind`), `captureBookingDiscount` (a % discount is of the GROUP's price), the course create (`groupKindOfKey` from the series' template — the same read as the match check), and **the day-end revenue sweep** (selects `groupId`, prices a seat by its kind — the one that POSTS). Pinned by source: the four call sites in the scheduler + one in the job, none left on the one-argument form; **by value: a DUO walk-in composes to `session-balance-duo` ฿1,900 at day-end while a solo session on the same subject still posts the subject's ฿1,390.**
- **The walk-in seat** — `validation.ts`: `groupId` on `SINGLE_SESSION` ONLY (refused on the other four types). `createBooking`: the row must be a LIVE GROUP row ⇒ else `404 "ไม่พบกลุ่ม"` (your note 3 — no `GROUP_NO_DATE`; a walk-in names a ROW); the body's teacher / date / start must equal the row's ⇒ else `400 "คาบต้องใช้ครู/วัน/เวลาเดียวกับกลุ่ม"`; the kind resolved ONCE before the discount capture and handed down; **the cap through the SAME count** — `seatOnGroup` split into **`assertSeatFree(tx, groupRowId, date)`** (the cap half; the course seat calls it too — exactly two call sites) and the extend half — the walk-in calls the cap half inside the tx before the insert; **no extend by construction** (the create never reaches `seatOnGroup`/`groupTemplate`). Inserted with `groupId` through `insertBooking`. Money = today's single-session path, priced by the kind at the gate, the discount and the post; nothing new posts; the seat draws no freelance hour (2a's rule).
- **The group DTO** gains **`priceGroup`** (`balance-duo` / `balance-group`) from the one mapping — the FE never maps kind → group (your note 4).
- **`scripts/ensure-sale-items.ts --dry-run`** (your note 2) — reads the box, prints `~ <ref> <price> THB — WOULD be created` per missing item, writes nothing, `DRY RUN — nothing written. n would be created, m already present.` Pinned: the dry branch precedes the insert.
- 🔻 **Pins moved:** the catalogue-shape count (14 → 18 program items: 5 sessions, 13 courses); 2a's `seatOnGroup` pin (the cap half moved to `assertSeatFree`) and its DTO pin (+ `priceGroup`).

## 📦 The deploy step — `bun run sale:ensure-items --dry-run` (run read-only on my local dev box)
```
  ~ session-balance-duo          1900 THB (VAT incl.) — WOULD be created
  ~ course-balance-duo-4         6800 THB (VAT incl.) — WOULD be created
  ~ course-balance-duo-6         9360 THB (VAT incl.) — WOULD be created
  ~ course-balance-duo-10        14200 THB (VAT incl.) — WOULD be created
DRY RUN — nothing written. 4 would be created, 24 already present.
```
On `sid` / `uat`: the same command without the flag adds exactly these four (insert-only; no existing price is touched). The human runs it after the deploy.

## Pinned (`lib/group-price-and-walkin-req095-2b.test.ts`, 14 tests)
- **The catalogue by value:** the four prices and refs; `sellablePackages` sizes 1/4/6/10 for DUO; Group untouched (no 4h); voucher-excluded; the mapping; `revenueItemRef`; the card as the only price site (source).
- **The resolver by value:** the three branches, the kind wins WITHOUT a subject read (the read counter), null subject; `groupKindOf` by value (GROUP / **an OTHER row carrying a DUO kind ⇒ null** / a lesson / unknown / null id) · **the five call sites by source**, none on the old form · **the day-end composition by value** (DUO ⇒ ฿1,900; solo ⇒ ฿1,390).
- **The walk-in:** validation (SINGLE_SESSION only, the other four refused) · the service by source (the 404 predicate, the match, the kind before the discount, the cap inside the tx before the insert, no extend path, no `GROUP_NO_DATE`) · the ONE cap count (two call sites) · the route through the ROOT app (201 with `groupId`/`groupName`; the 404 / 400 / **the `GROUP_FULL` envelope by value**).
- **The DTO's `priceGroup`** by value + the mapping line by source · **`--dry-run`** by source · 42 = 42.

## 🔑 Mutation — seventeen, `finally`, checksum — all bite
A a DUO price off the card · B DUO voucher-bookable · C GROUP mapped to the DUO price · D the resolver ignores the kind · **E the sweep prices a seat by its subject (the ฿1,390 post)** · F the sellable gate ignores the kind · G the discount ignores the kind · H the course create by subject · **I `groupKindOf` returns a kind for an OTHER row — PASSED FIRST** (my OTHER fixture carried an ECA kind, which the kind check refused either way; it now carries a DUO kind so only the TYPE check can refuse it) · J `groupId` accepted on a course · K a non-GROUP row accepted · L the mismatch corrected silently · M the cap skipped · N the walk-in extends · O the kind resolved after the discount · P a second kind → group mapping in the DTO · Q `--dry-run` still inserts. Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final
`GET /sellable-packages` now lists the four `balance-duo` items (the course form inside a group prices by `group.priceGroup` — new on the group DTO: `"balance-duo" | "balance-group"`; the FE never maps the kind itself) · **`POST /api/bookings { bookingType: "SINGLE_SESSION", groupId, student, teacherId, subjectId, date, startTime, … }`** ⇒ `201 { booking }` (with `groupId`, `groupName`) | `404 "ไม่พบกลุ่ม"` | `400 "คาบต้องใช้ครู/วัน/เวลาเดียวกับกลุ่ม"` | `409 { error: { code: "GROUP_FULL", message: "วันที่ … กลุ่มเต็ม (n/cap)" } }` — `groupId` is refused on any other booking type; the seat's price is the group's 1h tier (DUO ฿1,900 · Group ฿1,090), posted at ATTENDED like Private; the roster's `seats[]` shows it with `courseId: null` (the `1h` chip) · a DUO course's sale posts `course-balance-duo-{size}`.
