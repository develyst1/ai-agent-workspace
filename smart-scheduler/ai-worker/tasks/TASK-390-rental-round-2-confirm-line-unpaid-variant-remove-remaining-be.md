# TASK-390 — Rental round 2 (`REQ-091 §14`), BE: the rental line in the course-confirm message, the whole-course UNPAID variant, remove the rental from the remaining sessions (no money) with a course marker (migration) — contract first

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-18)
**Owner rulings (§14 via @Porter):** (1) the rental line in the whole-course CONFIRM message — BOTH audiences, as the reminder; (2) a whole-course UNPAID variant (pay per session ⇒ born RED, each paid press posts); (3) remove the rental from the REMAINING sessions mid-course — **NO money change**: future rows lose their rental (paid or not), past rows and the ledger untouched.
**Size M.** ⛔ Chain stopped. **Contract to @Fern through me first** (TASK-391 in parallel). **Migration `0038` → 39 = 39** (§3).

---

## §1 (1) The confirm message
`course_confirmed` (both copies) gains `Rental : Rent 200 / Full Set (…)` after its block, the `*ถ้ามี` shape, from `rentalPrintLine` on the course's derived rental (`courseRentalOf`) — the payload carries the rendered line as the reminder does. Pinned: rented ⇒ the line on both audiences; unrented ⇒ byte-for-byte today. If a template field is added the walkers move — say.

## §2 (2) The UNPAID variant
`POST /courses { rental: { code, remark?, paidUpfront: boolean } }` (default `true` = today). `false` ⇒ every live row born with an UNPAID rental row (no `paid_at`), **NO post at creation**; each session's paid press posts `hours 1` as a session rental (TASK-371's path, unchanged); a make-up inherits the UNPAID state (`inheritCourseRental` copies `paid_at` from its source — assert an unpaid source yields an unpaid copy). The course DTO's `rental` gains `paidUpfront`. Pinned: unpaid ⇒ rows unpaid, zero ledger; a paid press ⇒ one line per session; paid-upfront unchanged.

## §3 (3) Remove from the remaining sessions — and the marker (my decision)
- **`DELETE /courses/:id/rental`** — action key: **a NEW one, `action:bookings.course-rental`** (47th; "set or remove a course's rental" — one act, tickable on its own; say if you see a better home) ⇒ deletes the rental rows of the course's **future live sessions** (`date >= today` Bangkok, live status), paid or not — **no ledger call, asserted by absence**; past rows untouched ⇒ `{ removed: n }`.
- **The marker — `course_packages.rental_removed_at timestamptz null`** (migration `0038`: one nullable column add on `course_packages` — `ACCESS EXCLUSIVE` for the statement, metadata-only, no rewrite; say the table's size order in the header). `inheritCourseRental` returns `null` when the course's `rental_removed_at` is set (the course row is already in hand at both call sites — say). **Why the marker, not "copy only from future rows":** a leave on the LAST session appends a make-up when no future row exists, and an honestly rented course would lose its rental — the marker records what happened; a rule would guess. Re-adding a rental after removal is NOT this task (list).
- The course DTO's `rental` is `null` once `rental_removed_at` is set (the marker wins over the rows).

## Definition of Done
- [ ] Contract confirmed first (the action key named)
- [ ] Suite, **count** · tsc · **39 = 39** · migration header
- [ ] Pinned as above + remove ⇒ future rows gone, past paid rows and the ledger byte-identical, a later make-up does NOT inherit, the DTO null
- [ ] 🔑 Break-and-watch, `finally`, CHECKSUM

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-18) — confirm/correct before I build

**The tree agrees with §1–§3**, with three specifics to confirm (marked ❓).

## §1 The confirm line
- `confirmCourse` already holds the course's rows WITH their rental rows (`withBookingRelations.rental: true`), so the payload gains **`rental: string | null` = `courseRentalOf(rows)` rendered by `rentalPrintLine`** — the RENDERED line, as the reminder carries it (`daily-reminder.ts`'s `rental` field), built once where the payload is built.
- `line-message.ts` `course_confirmed`: **`extra(t("ob_f_rental", lang), payload.rental)`** appended after the block — `extra` is the omit-when-empty shape (`*ถ้ามี`), so an unrented course renders **byte-for-byte today**; both audiences share the one renderer, so both get it (the `recipientType` only picks block fields). The existing `ob_f_rental` label (TASK-375) — no new template key. No new outbox KIND — the walkers' kind count (18) stands.
- ❓ **Position:** after `Remark` (the last `extra`), i.e. the last line of the message — the reminder puts `Rental` after `Remark` too. Say if you want it before `Remark`.
- The field walker (`line-message-fields.test.ts`) enumerates `course_confirmed`'s payload fields — `rental` joins its list with the reason; nothing else moves.

## §2 `paidUpfront`
- `validation.ts`: `createCoursePackage.rental` = `recordBookingRental & { paidUpfront: boolean }` with **default `true`** (today, no FE change needed until Fern ships the switch).
- `createCoursePackage`: rows born with `paidAt/paidActor = paidUpfront ? now/actor : null`; **the `recordRental(hours = size)` post runs ONLY when `paidUpfront`** — `false` ⇒ zero ledger at creation; each session's paid press posts `hours 1` through `payBookingRental` (TASK-371, unchanged). `inheritCourseRental` already copies `paidAt` from its source row ⇒ an unpaid source yields an unpaid copy (pinned by value on the copied columns).
- ❓ **The DTO's `rental.paidUpfront` — derive or store?** Proposed: **derive, no column**: `paidUpfront = every live rental row has paid_at` is WRONG mid-course (a pay-per-session course that has been paid up so far would read "upfront"). So: **`rental: { code, remark, paidUpfront, unpaidSessions: n }`** where `paidUpfront` = *the course's rental rows were all paid at creation* — derivable as **`every live row's paid_at is not null AND equals its created_at`**? No — brittle. **Honest answer: store it.** Since `0038` already touches `course_packages`, add **`rental_paid_upfront boolean NULL`** in the SAME statement list (two nullable column adds, one lock, same header) — null = not rented at creation. The DTO then reads it, plus `unpaidSessions` (count of live rows without `paid_at` — the FE's "n to collect"). ❓ confirm the second column, or say "derive as *any live unpaid row ⇒ false*" and I build that one-liner instead.

## §3 Remove from the remaining sessions + the marker
- **`DELETE /courses/:id/rental`** → `removeCourseRental(courseId, actor)`: 404 unknown course; **409 `RENTAL_NOT_ON_COURSE "คอร์สนี้ไม่มีค่าเช่าอุปกรณ์"`** when the course has no rental (no rows, or already removed); else ONE transaction: **set `rental_removed_at = now()`** on the course, then **delete `booking_rentals` rows whose booking is the course's, `date >= today` (Bangkok), live status (`rentalBookingLive`)** — paid or not; past rows untouched ⇒ `{ removed: n }`. **No ledger call — asserted by absence** (`removeCourseRental`'s region contains no `recordRental`/`recordSale`/`bo.`; the mutation "post a refund" bites).
- **`inheritCourseRental`** — rather than threading the course row into both call sites, its ONE source query joins `course_packages` and adds **`isNull(coursePackages.rentalRemovedAt)`** to the WHERE: a removed course yields no source ⇒ `null`, both writers (reconcile + sick-leave append) covered by the one copy, signature unchanged. ❓ say if you want the row threaded instead.
- **The course DTO's `rental`** is `null` once `rental_removed_at` is set (the marker wins over any past paid rows still on the course) — the FE's card stops offering "remove" (Fern hides by `rental === null`) and a re-add is not this task (listed).
- **Key: `action:bookings.course-rental`** (47th) — TH `ถอดค่าเช่าอุปกรณ์ออกจากคอร์ส` / EN `Remove a course's rental` — ❓ your label was "set or remove"; today the SET is on `POST /courses` (gated by `course-create`), so the key gates the REMOVE only; I propose the label say what it gates. `ROUTE_ACCESS`: `"DELETE /courses/:id/rental": act(BOOKINGS, "action:bookings.course-rental")`; the enumeration pins move to 47 / 59 mutate routes.

## Migration `0038_course_rental_marker.sql` — 39 = 39
`ALTER TABLE course_packages ADD COLUMN IF NOT EXISTS rental_removed_at timestamptz NULL` (+ `rental_paid_upfront boolean NULL` if confirmed) — **ACCESS EXCLUSIVE on `course_packages` for the statement; nullable, no default ⇒ catalog-only, no rewrite; `course_packages` is hundreds to low thousands of rows** (one per course sold); the queueing hazard named (the calendar and the bookings page read it — a read-blocking blink; quiet moment). **Witness: `{ kind: "column", table: "course_packages", column: "rental_removed_at" }`** (a column this migration invents — existence is valid; with two columns the LAST one is the witness). One statement, one run, rerunnable.

## For @Fern (draft, final after your confirm)
`POST /courses { rental: { code, remark?, paidUpfront?: boolean = true } }` · course DTO `rental: { code, remark, paidUpfront, unpaidSessions } | null` · `DELETE /api/courses/:id/rental` ⇒ `{ removed: n }` | 404 | `409 RENTAL_NOT_ON_COURSE`, gated by `action:bookings.course-rental` (in `/permissions` with labels) · the confirm message gains a `Rental :` line for both audiences.

⛔ Waiting on your confirm (position · the second column vs derive · the join vs threading · the key's label) before code.

---

# 📤 REPORT — @Jason → @Sober (2026-09-18)

✅ **DONE (code), against the confirmed contract (your four answers applied).** **2400 pass / 0 fail**, **190 files** (+1) · `tsc --noEmit` clean · **MIGRATION `0038_course_rental_marker` → 39 = 39** · preflight scanner clean · witness registered.

## The migration — `drizzle/0038_course_rental_marker.sql`
Two nullable column adds on `course_packages`, in ONE transaction, both `IF NOT EXISTS`: **`rental_removed_at timestamptz NULL`** (the marker) then **`rental_paid_upfront boolean NULL`** (the variant; NULL = not rented at creation — every pre-0038 course) — **the LAST column is the witness** (`kind: "column"`). 🔒 **The header:** each ALTER takes ACCESS EXCLUSIVE on `course_packages` for its statement; nullable, no default ⇒ catalog-only, no rewrite, no backfill; the table is **hundreds to low thousands of rows** (one per course ever sold) ⇒ two blinks of milliseconds; the queueing hazard named (the calendar, the bookings page and every course read touch it — a read-blocking blink; quiet moment, not during an import). No other table touched.

## What was built
- **(1) The confirm line** — `loadCourseForEnd` loads the rows `with: { …, rental: true }`; `confirmCourse`'s ONE payload gains **`rental: string | null`** = `courseRentalOf(rows)` → `rentalPrintLine` (rendered once where the payload is built; `null` when unrented **or when the marker is set**). `line-message.ts` `course_confirmed`: `extra(t("ob_f_rental", lang), fieldValue(payload.rental))` **after `Remark`, the last line** (answer 1) — the omit-when-empty shape, so an unrented course renders **byte-for-byte as before**; both audiences through the one renderer. No new kind, no new template key (TASK-375's `ob_f_rental`); the walkers' kind count stands.
- **(2) The variant** — `validation.ts`: `createCoursePackage.rental = recordBookingRental.extend({ paidUpfront: z.boolean().default(true) })` (today's callers unchanged). `createCoursePackage`: **`rental_paid_upfront` STORED** on the course (answer 2); rows born with `paidAt/paidActor = paidUpfront ? now/actor : null`; **the ONE `recordRental(hours = size)` post runs only when paid upfront** — a pay-per-session course posts nothing at creation; each paid press posts one through `payBookingRental` (unchanged). `inheritCourseRental` copies `paidAt`/`paidActor` from its source ⇒ an unpaid source yields an unpaid make-up (pinned). **The course DTO's `rental`** = `{ code, remark, paidUpfront, unpaidSessions } | null`: `paidUpfront` from the column (a pre-0038 rented course ⇒ `true` — it was, by construction), `unpaidSessions` = COURSE-LIVE rows (PENDING · CONFIRMED · EXTENDED) whose rental row has no `paid_at` (`courseRentalSummary`, pure; the list's grouped read `courseRentalsByCourse` reduces to the same shape — still ONE query).
- **(3) Remove from the remaining sessions** — `rental.service.ts` **`removeCourseRental(courseId, actor)`**: 404 unknown course · **`409 RENTAL_NOT_ON_COURSE "คอร์สนี้ไม่มีค่าเช่าอุปกรณ์"`** when already marked or no rental row at all · ONE transaction: **set `rental_removed_at = now()`**, then delete the `booking_rentals` rows of the course's bookings with **`date >= today` (Bangkok) ∧ status ∈ `COURSE_LIVE_STATUSES`** — paid or not; past rows untouched ⇒ `{ removed: n }`. **No money moves — asserted by absence** (no `recordRental`/`recordSale`/`boMovement`/refund in the remover; it never updates a row, one `delete` only). **`inheritCourseRental`** honours the marker **in its one source query** (answer 3): `.innerJoin(coursePackages, and(eq(id, bookings.courseId), isNull(rentalRemovedAt)))` — both writers (reconcile + the sick-leave append) covered, signature unchanged. **The DTO's `rental` is `null` once marked** (the marker wins over past paid rows). Route **`DELETE /courses/:id/rental`** ⇒ `{ removed: n }`, in `ROUTE_ACCESS` under **`action:bookings.course-rental`** (47th; TH `ถอดค่าเช่าอุปกรณ์ออกจากคอร์ส` / EN `Remove a course's rental` — answer 4; the SET stays under `course-create`). Classified `unrelated` in the ended-course table with the reason (an ended course has no live row; the marker on it is inert; no ledger to protect).
- 🔻 **Pins moved, with reasons:** nine `38 = 38` pins → `39 = 39`; the Stage 1/4 "last witness / last file" pins now find their entry by tag / index (0038 is last); `actorOf(c)` count 12 → 13 (the new route); the action-key count 46 → 47 and the mutate-route floor 58 → 59; TASK-373's DTO pins (+ `paidUpfront`, `unpaidSessions`) and its create-order pin (the variant line comes first; the paid stamps conditional); the ended-course classification (+ the new route).

## Pinned (`lib/course-rental-round2-req091-14.test.ts`, 19 tests)
- **(1) by value:** rented ⇒ `Rental : Rent … / …` is the LAST line for parent AND teacher in TH and EN, `Remark` the line before · unrented / `rental: null` ⇒ **byte-identical** to before, no `Rental` anywhere · the two audiences share the same rental line · source: the payload line, `loadCourseForEnd`'s `rental: true`, the renderer's `extra`.
- **(2):** the shape (default true; false accepted; non-boolean refused) · the create's variant stored, `paidAt` null when not upfront, the actor too, the post guarded, still ONE post site · `courseRentalSummary` by value (live rows only — leave / cancelled / attended never count) · **the DTO by value: stored `false` with 0 unpaid (a paid-up pay-per-session course) is STILL `false`** — the trap the column exists for (mutation K passed until this pin) · pre-0038 ⇒ true · the marker ⇒ null even with rows · the inherit copies the paid stamps and stamps nothing.
- **(3):** the remover by source (404 · both 409s · today Bangkok · marker BEFORE delete · the date floor + live statuses · one delete · `{ removed }`) · **no money by absence** · the marker join in `inheritCourseRental`, signature unchanged, TASK-376's two call sites still two · the route through the ROOT app (`{ removed: 3 }` with actor `dev`; **the 409 envelope by value**) · the key with its labels gating exactly this route; `POST /courses` still `course-create`.
- **The migration:** 39 = 39, 0038 last at idx 38 · exactly two ALTERs in order, no DEFAULT / NOT NULL, one breakpoint · the header's lock sentences by text · the witness = the LAST column · the schema mirrors both.

## 🔑 Mutation — nineteen, `finally`, checksum — all bite
A the confirm line dropped (2) · B rendered before `Remark` (1) · C an empty `Rental :` on an unrented course (4) · D the payload ignores the marker (1) · E pay-per-session rows born paid (1) · F the post runs for pay-per-session too (1) · G the variant not stored (1) · H default flips to false (1) · I leave rows counted as unpaid (1) · J the DTO ignores the marker (1) · **K `paidUpfront` derived from the rows — PASSED FIRST**: my DTO cases never had "stored false with 0 unpaid"; the pin now has it — bites (1) · L the remover deletes past rows (1) · M the remover refunds (1) · N the marker never set (1) · O the inherit ignores the marker (1) · P a marked course removable again (1) · Q the route gated by `course-create` (2) · R the variant column gets a DEFAULT (1) · S the witness is the first column (1). Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final
`POST /api/courses { rental: { code, remark?, paidUpfront?: boolean } }` (default `true`; `false` = pay per session) · course DTO **`rental: { code, remark, paidUpfront: boolean, unpaidSessions: number } | null`** (null once removed — hide the button by it) · **`DELETE /api/courses/:id/rental`** ⇒ `200 { removed: n }` | `404` | `409 { error: { code: "RENTAL_NOT_ON_COURSE", message: "คอร์สนี้ไม่มีค่าเช่าอุปกรณ์" } }`, gated by **`action:bookings.course-rental`** (in `GET /permissions` with its TH/EN labels; the FE hides by `can(...)`) · no money moves on remove · the confirm message's `Rental :` line is the BE's (nothing for the FE) · a make-up appended after removal has no rental; before removal it inherits the course's (paid or unpaid) state.
📦 **After the migration:** nothing to set. Every existing course reads `rental_paid_upfront = NULL` ⇒ the DTO says `paidUpfront: true` for a rented one (it was), `rental: null` for the rest — today's behaviour.
