# TASK-443 — `REQ-104 §2` items 4–5 (SPEC-090 §2–§3): CAMP coach rate per-coach-per-day (`0052`, behind key 59, copied onto the derived rows; new-day default 0) · the scan response carries the remaining credit (camp AND Private) · the `camp_deduction` family notice at DAY-END (ENGLISH ONLY) — BE, M, CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-22) · **Size M.** Migration `0052_camp_day_rates` (53 = 53). No new key. One new outbox kind (25th): `camp_deduction`. After TASK-441. The held batch.

## §0 Owner rulings (Porter 09-22, REQ-104 §2/§3)
4. A per-coach-per-day rate on the week's day editor, behind **key 59** (see ⇔ edit), copied onto the derived `OTHER/CAMP` rows; a new day's default = **0**. 5a. The scan page shows the remaining credit. 5b. A CAMP deduction family notice at DAY-END (parity with Private's `course_deduction`, not at scan) — **ENGLISH ONLY regardless of `line_lang`**: `🏕️ CAMP CREDIT USED` · `Student : {name}` · `Date : {date}` · `Remaining : {remaining}/{total} days` (REQ-104 §3, by value; date `DD-MM-YYYY`; remaining in DAYS — half-day units ⇒ `3.5`).

## §1 Verify before the contract
1. `camp_week_days` (TASK-418) + `syncCampDayRows`: where `teacher_ids[]` is written (`PATCH /camp/weeks/:id/days/:date`) and how the derived rows are (re)built — the seam to write `teacher_rate_minor` (primary) / `booking_teachers.rate_minor` (extras) on each derived row from the day's rates; the camp-on-grid pins that assert `teacherRates` ABSENT (they move — say which).
2. The credit: `creditOf(pkg, planned)` (`camp.service.ts:187`) — units → days (`/2`); the scan (`checkinCampByToken`) returns `{ day }` — add `credit`; the Private scan (`checkin.service.ts`) returns `{ booking }` — add `remaining: { used, total }` from the course/voucher on the row (read-only, one field; the page shows both kinds the same way).
3. The day-end job's camp cut (TASK-403: `camp_days` PLANNED ⇒ ATTENDED/NO_SHOW at 17:30, units consumed) — the seam to enqueue `camp_deduction` per ATTENDED camp day to the child's family accounts (`householdLineUserIds`, the ONE read); the `course_deduction` producer's shape for parity (`bookingContext`? or its own payload — a camp day has no booking row; the payload carries everything: `studentName · date · remainingDays · totalDays`).
4. Key 59 on the day editor: `PATCH /camp/weeks/:id/days/:date` carries `teacherRates` ⇒ `assertMayEditCoachRate` (TASK-434's check, the `teacherRates` field already in `COACH_RATE_BODY_FIELDS`) — so the write is gated by construction; the READ: the day DTO's `teacherRates` goes through the `/api/*` mask by key name — confirm both hold with zero new code beyond the field.

## §2 The contract I propose
- **`0052_camp_day_rates`:** `camp_week_day_rates (camp_week_day_id uuid FK ON DELETE CASCADE, teacher_id uuid FK, rate_minor integer NOT NULL DEFAULT 0, PRIMARY KEY (camp_week_day_id, teacher_id))` — the witness = the table. (A table, not a jsonb: one row per coach per day, the `booking_teachers` shape.)
- **Write:** `PATCH /camp/weeks/:id/days/:date { teacherIds?, window?, teacherRates? }` (`camp.week-open`; `teacherRates` gated by key 59 via the existing body check) — upserts the rates for the day's coaches (a coach not on the day ⇒ 400); a coach added to a day without a rate ⇒ `0`; `syncCampDayRows` copies each coach's day rate onto the derived rows (primary → `teacher_rate_minor`, extras → `booking_teachers.rate_minor`) so the calendar's `other.teacherRates` shows them under the 59 mask. Nothing posts.
- **Read:** the week's day DTO carries `teacherRates: Record<teacherId, minor>` (masked without 59 by the middleware — pinned by value).
- **The scan:** `POST /checkin/camp` ⇒ `{ …, credit: { remainingDays, totalDays } }`; `POST /checkin` ⇒ `{ …, remaining: { used, total, unit: 'sessions'|'hours' } }` (no mask — the scan is the family's own; the page is public by token).
- **`camp_deduction`** (25th kind): at the day-end cut, per camp day that became ATTENDED, one outbox row per family account: payload `{ studentName, date, remainingDays, totalDays }`; render English only (both langs) per §0; behind no flag (the course deduction has none); `REMINDABLE` untouched.
- 🚫 No money posting; the camp reminder untouched; the Private deduction byte-identical.
- ❓ for you: (1) the day-end seam and whether the NO_SHOW cut also deducts (does a camp no-show consume units today? then it deducts too and the notice says the same — say the rule); (2) the moved camp-on-grid pins.

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite **count** · tsc 0 · **53 = 53** · preflight `[]` · the rate write/read/copy by value (default 0; masked without 59; 403 on the field without 59) · the two scan payloads by value · the deduction by value (English only under both langs; half-day `3.5`; per family account) · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-22) — §1 verified; one 🔴 finding on the day-end premise; two simplifications; no code yet

## §1 Verified in the repo
1. **The seam (§1.1):** `camp_week_days.teacher_ids[]` is written by `updateWeekDay` (`PATCH /camp/weeks/:id/days/:date`, `camp.service.ts:167`) and the ONE sync `syncCampDayRows` `:134` re-derives the rows: wanted = **teachers × the window's hours, ONE teacher per derived row** (`wantedCampSlots` keys `teacherId|HH:MM`; the row is born CONFIRMED via `insertBooking`, then stamped `campWeekDayId`). ⇒ **Simplification 1: a derived camp row has NO extras** — the copy is `teacher_rate_minor` on each row only; `booking_teachers.rate_minor` never enters (your draft's "extras" line does not apply). The diff inserts the missing and deletes the surplus — it never touches a KEPT row, so a rate change on a day whose rows already exist needs one more step in the sync: `update bookings set teacher_rate_minor = <coach's day rate> where camp_week_day_id = day and teacher_id = coach` (idempotent, every sync). A coach on the day without a rate row ⇒ `0` (the new-day default, by absence — no row written until a rate is set). **The moved pins (❓2):** `camp-on-grid-req095-11.test.ts:163` (`expect(input.teacherRates).toBeUndefined()` on the sync's insert ⇒ becomes `{ [teacherId]: rate }`) and `:232` (`S` not.toMatch `teacherRates` ⇒ re-pinned without that word; `otherPriceMinor|otherPriceItemId|headCount|recordSale|recordRevenue` stay absent — the day-end's OTHER post still answers false on a camp row (`otherPriceMinor == null`), a RATE is not a PRICE, pinned). Nothing else asserts it.
2. **The credit (§1.2):** `creditOf(p, planned) = totalUnits − usedUnits − plannedUnits` (`lib/camp.ts:39`, UNITS: `unitsPerDay(FULL) = 2`, AM/PM = 1 ⇒ days = units / 2). The scan (`checkinCampByToken` `:334`) answers `{ already, day }` after `markDay(ATTENDED)` — ⇒ `+ credit: { remainingDays, totalDays }` where **remaining = `(totalUnits − usedUnits) / 2`** (what is left to consume — a future PLANNED day is still the family's credit; the package DTO's `credit` subtracts planned, which is the SALE's question, not the family's — say if you want `creditOf`'s number instead). The Private scan (`checkin.service.ts:56`) answers `{ already, booking, crmAwarded }`; its `withBookingRelations` loads `course` but NOT `voucher` ⇒ `+ remaining: { used, total, unit: "sessions" | "hours" }` from the course (on the DTO already) or ONE voucher read; `null` on a trial/single. No mask on either — the scan is the family's own, by token.
3. **🔴 The day-end premise (§1.3, ❓1) — Private's `course_deduction` is NOT day-end-only:** it fires at the CONSUMPTION moment — `updateBookingStatus(id, "attend")` `:3382/:3399` (manual attend AND the Private scan: `checkinByToken` → `updateBookingStatus(row.id, "attend")`) and the day-end cut `jobs.service.ts:92/:111` for rows never attended. So "parity with Private, not at scan" is two rulings that disagree: parity = at scan too. And a day-end-only notice has a hole: a camp day scanned at 09:00 is ATTENDED already, so `cutCampDays` (PLANNED ⇒ ATTENDED, `:374`) never sees it. **Two honest shapes — I recommend (b), it honours §0 as written and stays idempotent:**
   (a) true parity — enqueue in `markDay` on every CONSUMING transition (PLANNED → ATTENDED / ABSENT: scan, manual, undo-then-remark) + in `cutCampDays`; nothing else; no column.
   (b) **day-end only — `camp_days.deduction_notified_at timestamptz NULL` (in `0052`); the cut gains a second pass: every camp day dated ≤ runDate in a CONSUMING status (`ATTENDED | ABSENT` — `consumes()` `lib/camp.ts:42`: a no-show IS charged, §8, so it deducts and the notice reads the same) with `deduction_notified_at IS NULL` ⇒ ONE `camp_deduction` per family account (`householdLineUserIds(tx, [studentId])`, the ONE read; no account ⇒ one skipped row, Private's shape) + the stamp, in the same tx.** Idempotent by the stamp (a re-run enqueues nothing; no idempotency key rides the tx — `line.ts`'s rule). An undo before day-end (ATTENDED → PLANNED) ⇒ not consuming ⇒ nothing; an undo after the notice ⇒ nothing (Private sends nothing on an undo either). The Private deduction byte-identical.
4. **Key 59 (§1.4):** the READ holds with zero code — the day DTO's `teacherRates` key is masked by `middleware/coach-rate-mask.ts` by NAME on every `/api/*` response (pinned by value through the root app). The WRITE does NOT hold by construction today: `routes/camp.ts:18` calls `updateWeekDay` with no `assertMayEditCoachRate` (TASK-434's six writers did not include it — it carried no rate) ⇒ **ONE line at the route** (`teacherRates` is already in `COACH_RATE_BODY_FIELDS`; the census pins move: `assertMayEditCoachRate(` 11 → 12, `viewerOf(c)` 20 → 21).

## §2 The contract — confirmed with the above
- **`0052_camp_day_rates`** (53 = 53): `camp_days.deduction_notified_at timestamptz NULL` (IF NOT EXISTS) then `camp_week_day_rates (camp_week_day_id uuid NOT NULL REFERENCES camp_week_days ON DELETE CASCADE, teacher_id uuid NOT NULL REFERENCES teachers ON DELETE RESTRICT, rate_minor integer NOT NULL DEFAULT 0, PRIMARY KEY (camp_week_day_id, teacher_id))` — the witness = the table (`kind: "table"`), the LAST object; both catalog-only, rerunnable. (**Simplification 2:** the `0` default is the ABSENCE of a row, so the table holds only set rates; `DEFAULT 0` kept as the column's floor.)
- **Write:** `PATCH /camp/weeks/:id/days/:date { teacherIds?, startTime?, endTime?, teacherRates? }` (`camp.week-open` + `assertMayEditCoachRate` ⇒ 403 `COACH_RATE_FORBIDDEN` with the field and no 59) — upserts each `teacherRates[t]` for the day; a coach not on the day (after this patch's `teacherIds`) ⇒ 400 `RATE_TEACHER_NOT_ON_DAY`; then the ONE sync copies every coach's day rate onto the derived rows (insert with it; kept rows updated). A closed week ⇒ 409 as today. Nothing posts.
- **Read:** `GET /camp/weeks/:id/days` days`[].teacherRates: Record<teacherId, minor>` (0 for a coach on the day without a row); masked without 59 (by value). The calendar's derived rows show `teacherRateMinor` under the same mask as any row (no new code).
- **The scans:** `POST /checkin/camp` ⇒ `{ already, day, credit: { remainingDays, totalDays } }` (half-day units ⇒ `3.5`); `POST /checkin` ⇒ `{ already, booking, crmAwarded, remaining: { used, total, unit } | null }`.
- **`camp_deduction`** (25th kind): the (b) pass at day-end; payload `{ studentName, date, remainingDays, totalDays }`; the render ENGLISH ONLY (no `t()`, no `lang`, pinned under both): `🏕️ CAMP CREDIT USED` / `Student : <name>` / `Date : DD-MM-YYYY` / `Remaining : 3.5/5 days` (a whole number prints `4`, never `4.0`); recipient `parent`, no `bookingId` (a camp day has no row; the payload carries everything); no flag; `REMINDABLE` and the camp reminder untouched.
- 🚫 No money posting; the Private deduction byte-identical; the camp reminder untouched.

## DoD as I will run it
Suite count · tsc 0 · 53 = 53 · preflight `[]` · the rate write/read/copy by value (upsert; default 0 by absence; a kept row updated by the sync; the insert carries it; 400 off-day; 403 without 59; masked without 59 through the root app) · the two scan payloads by value (`3.5`; course vs voucher vs null) · the deduction by value (the pass picks ATTENDED and ABSENT, skips PLANNED and already-stamped, stamps, one row per family account, no account ⇒ skipped; English only under both langs) · the Private deduction and `cutCampDays`' first pass byte-identical · break-and-watch with sha-256 · @Fern's lines · report ×4.

**For @Fern (TASK-444, draft):** the day editor's `teacherRates` on `GET /camp/weeks/:id/days` (absent without 59 — render nothing) and on the PATCH body (403 `COACH_RATE_FORBIDDEN` without 59; 400 `RATE_TEACHER_NOT_ON_DAY`); the scan pages: `credit.remainingDays/totalDays` (camp) and `remaining.used/total/unit` (Private, `null` on a trial/single).

⛔ Waiting on your confirm — (a) vs **(b)** for the notice (§1.3) · ABSENT deducts and notifies the same · `remaining = (total − used) / 2` vs `creditOf` · the two simplifications · the one route line for 59 · the moved pins — before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-22) — build to §2; the finding is right and (b) is the shape
1. **(b) — day-end only, idempotent by `deduction_notified_at`.** The owner's ruling was "at day-end, not at scan"; your finding (Private also fires at the attend moment) is recorded for Porter as a difference the owner accepted knowingly — the words say "CREDIT USED", true at 17:30 either way. The second pass picks every consuming day ≤ runDate not yet stamped, so a scanned-at-09:00 day is covered.
2. **ABSENT deducts and notifies with the same words — YES** (a charged no-show is a deduction; parity with the course cut).
3. **`remaining = (total − used) / 2` — YES**, the family's number (planned days are still theirs).
4. Both simplifications taken (no extras on derived rows; the `0` by absence). The one route line for 59 — yes (`COACH_RATE_FORBIDDEN`). The two pins move as you listed.
Fern's lines final = your draft; TASK-444 §0 set.

---

# ✅ CODE DONE — @Jason → @Sober (2026-09-22) — built as confirmed ((b) day-end pass + stamp); `0052` ⇒ 53 = 53; 2776 pass / 0 fail; 19/19 mutations bite

**Numbers:** `bun test` **2776 pass / 0 fail**, 212 files (+1: `src/lib/camp-day-rate-req104.test.ts`, 10 tests) · `tsc --noEmit` **0** · 🔴 **`drizzle/0052_camp_day_rates.sql` — 53 = 53** (idx 52, the last) · offline preflight `0052` alone `[]` and the batch 0038 → 0052 `[]` · kinds **25** (+ `camp_deduction`) · 36 pins moved (the migration census 52 → 53 in 28 files; the kinds census 24 → 25 ×5; the seven camp pins named in the contract: the sync's insert now carries `teacherRates` and re-stamps the kept rows, `S` re-pinned without the word, the day PATCH route's new shape, the scan's `already` line, the day-end return + `campDeductionsNotified`; TASK-439/441's "the last witness" pins found by tag).

## What was built
- **`0052_camp_day_rates`:** `camp_days.deduction_notified_at timestamptz NULL` (IF NOT EXISTS), then **`camp_week_day_rates (camp_week_day_id FK CASCADE, teacher_id FK RESTRICT, rate_minor int NOT NULL DEFAULT 0, PK (day, teacher))`** — the table is the witness (`kind: "table"`), the LAST object; both catalog-only, rerunnable. Schema: the table + `campWeekDays.rates` relation + the stamp column.
- **The rate (behind key 59):** `GET /camp/weeks/:id/days` days`[].teacherRates: Record<teacherId, minor>` — every coach ON the day, `0` by absence (a rate row for a coach no longer on the day is not shown; pinned). `PATCH /camp/weeks/:id/days/:date { …, teacherRates? }` — **the ONE route line** `assertMayEditCoachRate` (403 `FORBIDDEN` with the field and no 59; teachers/window alone pass — pinned through the ROOT app, the service never reached); the service upserts per coach (`ON CONFLICT (day, teacher) DO UPDATE`), a coach not on the day (after this patch's `teacherIds`) ⇒ 400 `ตั้งค่าเรทได้เฉพาะครูที่อยู่ในวันนี้` BEFORE any write; the upsert runs BEFORE the ONE sync (pinned by order; mutation B bites). **The sync copies:** the inserted rows carry `teacherRates: { [coach]: dayRate }` (`insertBooking` writes `teacher_rate_minor`) and **the KEPT rows are re-stamped** (one `update … where camp_week_day_id = day and teacher_id = coach` per coach on the day — the diff never touched a kept row). The READ mask: `teacherRates` is nulled by name without 59 and shown with (pinned through the root app, zero new code). A RATE is not a PRICE: the day-end's OTHER post still answers false on a camp row (`otherPriceMinor == null`, pinned).
- **The scans:** `POST /checkin/camp` ⇒ `+ credit: { remainingDays, totalDays }` = `(totalUnits − usedUnits) / 2` and `totalUnits / 2` (`3.5` on a half-day; both the `already` and the attend paths; the attend path's number is post-`markDay`); `POST /checkin` ⇒ `+ remaining: { used, total, unit: "sessions" | "hours" } | null` — the course from the DTO's summary, ONE voucher read for a voucher row (its relations never carried it), `null` on a trial/single. No mask.
- **`camp_deduction` (25th kind) — the DAY-END pass, (b):** `notifyCampDeductions(tx, runDate)` in `camp.service.ts`, called INSIDE the end-of-day tx AFTER `cutCampDays` (so the credit it prints is post-consumption; mutation L bites): every camp day in a CONSUMING status (`ATTENDED | ABSENT` — a no-show is charged, §8) dated ≤ runDate with `deduction_notified_at IS NULL` ⇒ ONE outbox row per family account (`householdLineUserIds`, the ONE read), a family without an account ⇒ one skipped row (Private's shape), no `bookingId` (a camp day has no row — the payload carries everything: `{ studentName, date, remainingDays, totalDays }`), then the stamp in the same tx — the idempotency (a re-run enqueues nothing; mutation H bites). `markDay` (scan / manual) sends nothing — the owner's ruling, pinned by absence. The day-end returns `+ campDeductionsNotified`. No flag. The cut's first pass and the Private deduction (its two day-end sites) byte-identical.
- **The bytes (REQ-104 §3), ENGLISH ONLY:** `🏕️ CAMP CREDIT USED` / `Student : <name>` / `Date : DD-MM-YYYY` / `Remaining : 3.5/5 days` (a whole number prints `4`) — pinned identical under TH and EN, no Thai code point, no ISO date; the branch takes no `t()`/`lang`; an empty payload renders (the leak walkers pass).
- 🚫 No money posting; the camp reminder untouched; `REMINDABLE` untouched.

## Break-and-watch — `mut443.mjs`, 19 mutations, **19 bite** (`try/finally`, sha-256 restore byte-identical)
A an off-day coach accepted · B the upsert after the sync · C inserted rows without the rate · D kept rows never re-stamped · E a stale rate row shown · F the credit subtracts nothing · G ABSENT skipped · H the stamp ignored · I the stamp never written · J no skipped row · K the notice at `markDay` · L the pass before the cut · M a Thai byte · N an ISO date · O units instead of days · P rendered bilingually · Q the 59 check dropped · R the voucher hours lost · S the table without its PK.

## Contract lines for @Fern (TASK-444) — final
- `GET /camp/weeks/:id/days` → `days[].teacherRates: Record<teacherId, minor> | null` (`null` = masked without key 59 — render nothing; `0` = no rate set).
- `PATCH /camp/weeks/:id/days/:date { teacherIds?, startTime?, endTime?, teacherRates? }` → 403 `FORBIDDEN` when `teacherRates` rides without 59; 400 (message `ตั้งค่าเรทได้เฉพาะครูที่อยู่ในวันนี้`) for a coach not on the day; ⇒ `{ day: { …, teacherRates }, inserted, deleted }`.
- `POST /checkin/camp { token }` → `{ already, day, credit: { remainingDays, totalDays } }` (days; `3.5` possible).
- `POST /checkin { token }` → `{ already, booking, crmAwarded?, remaining: { used, total, unit: "sessions" | "hours" } | null }`.

📦 Deploy (the human's): `db:migrate` (verify **53**). The whole held batch: 0038 → 0052 in one run. 🚫 No deploy request. ⛔ Only you mark this DONE.

---

# ✅ DONE — REVIEWED by @Sober (2026-09-22)
Re-run by me: **2776 pass / 0 fail** · tsc 0 · **53 = 53** (`0052_camp_day_rates`) · the 59 check on the camp route · the 25th kind in the renderer. **The BE side of the WHOLE batch (REQ-094…104) is complete: 2776/0, 53 = 53, 59 keys, 25 kinds.** Fern: 442 → 444.
