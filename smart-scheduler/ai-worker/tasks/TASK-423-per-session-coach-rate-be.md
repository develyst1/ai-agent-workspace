# TASK-423 — Coach rate PER SESSION (`REQ-095 §13.3`): the course holds the DEFAULT, a session may OVERRIDE it; effective = override ?? default · + the Bookings-table surface carries both DUO names — BE, CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-21) · **Size S–M.** Migration: **none if `bookings.teacher_rate_minor` is reused (§2) — say if you see why it cannot be.** `sid`; the `uat` deploy WAITS for this.

## §0 The customer's rule (Porter 09-21): the ค่าสอน is edited class by class — the course is ครู A @ 7; the week ครู B substitutes, THAT session is 3, the course stays 7. Today `PATCH /bookings/:id { classRateMinor }` rewrites the COURSE (`scheduler.service.ts:3729`) — every session changes. Wanted: course default + per-session override; the session popup edits that session only; the course card edits the default. Applies to DUO and any course carrying a coach rate. **There is no reader that posts or reports the rate today** (stored only — the backoffice lock) — so "freelance/sale/report reads use the effective rate" means: the DTOs expose the effective rate; nothing posts.

## §1 Verify before the contract
1. `bookings.teacher_rate_minor` (Stage 1, TASK-394): today = the PRIMARY teacher's rate on an OTHER/GROUP row (`mappers.ts:143` builds `other.teacherRates` from it; `:1266` writes it; `:1464` edits it; `:1575` reads it for the group DTO). **Can a course session's override live in the SAME column** (the meaning is the same: "the coach's pay for THIS session, stored only")? List every reader of the column and whether a non-null value on a COURSE row would confuse it (the `other` DTO is built only for OTHER/GROUP — confirm; the day-end's no-money pins; the reminder). If any reader breaks, say so — then it is a new column `bookings.rate_override_minor` (`0049` ⇒ 50).
2. `getBookings` (`:850`, the Bookings-table surface) — its own select, no `withBookingRelations`; it lacks `coStudent` (Tanya's note: `coStudent: null` + one `displayName` on a DUO row). The join/second read to add, and `displayName` = both names through the same rule the calendar uses (`mappers.ts` — one place).
3. The session writers (the `insert(bookings)` census from TASK-420): a NEW session row is born with override NULL (the default applies) — confirm nothing copies the course rate onto rows (it must not; the default is read from the course).

## §2 The contract I propose — confirm/correct before code
- **The model:** `course_packages.class_rate_minor` = the DEFAULT (nullable; DUO sets it at create; any course may set it via `PATCH /courses/:id { classRateMinor }` — the `NOT_DUO` refusal is REMOVED: "applies to any course carrying a coach rate"). `bookings.<override>` (the reused `teacher_rate_minor` or the new column) = the per-session OVERRIDE, NULL = inherit. **`effectiveRateMinor(row, course) = row.override ?? course.classRateMinor ?? null`** — ONE pure function in `lib/coach-rate.ts`, the only place the rule lives.
- **Writes:** `PATCH /bookings/:id { classRateMinor }` now writes THE SESSION's override (never the course); `null` clears the override (back to the default); allowed on any COURSE_PACKAGE row (`400 NOT_A_COURSE_SESSION` otherwise); the move + rate in one body still allowed. `PATCH /courses/:id { classRateMinor }` writes the default only (existing overrides untouched — pinned).
- **Reads:** the booking DTO carries `rate: { effectiveMinor, overrideMinor, defaultMinor }` (three facts, no arithmetic on the FE); the course DTO's `classRateMinor` = the default (unchanged name). The plan reader (TASK-422) unchanged. Nothing posts (pinned by absence — the rate never reaches `recordSale`/`recordRental`/a `bo.*` write).
- **The Bookings-table surface:** `getBookings` rows carry `coStudent` + `displayName` = `A & B` through the calendar's rule (one helper, pinned that the two surfaces render the same string for the same row).
- 🚫 No posting; no report; no change to OTHER/GROUP rows' rates (if the column is reused, the OTHER/GROUP semantics stay byte-identical — pinned).
- ❓ for you: (1) reuse `teacher_rate_minor` or a new column (§1.1); (2) whether a per-session override should also be editable on a DUO's `PATCH /bookings/:id/other`-style route or only the move/popup body — I propose the popup body only.

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · the migration count as decided · `effectiveRateMinor` by value (override · default · neither) · the two writes by value (session ⇒ the row only; course ⇒ the default only; overrides survive a default change) · the DTO's three facts · the Bookings surface's `coStudent` + `displayName` = the calendar's string · no posting by absence · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-21) — §1 verified; §2 confirmed with two corrections and one finding; no code yet

## §1.1 `bookings.teacher_rate_minor` — EVERY reader/writer of the column (grep, non-test)
| # | site | what it does with the column | a non-null value on a COURSE row? |
|---|---|---|---|
| 1 | `db/mappers.ts:139` `otherFacts(b)` | builds `other.teacherRates[primary] = b.teacherRateMinor` | **gated `if (b.bookingType !== "OTHER") return null`** on line 140 — a COURSE row never reaches it |
| 2 | `db/mappers.ts:123` `groupFacts(b)` | calls `otherFacts({...b, bookingType: "OTHER"})` for the group DTO | gated `bookingType !== "GROUP" ⇒ null` on line 124 — same |
| 3 | `scheduler.service.ts:1266` `insertBooking` | writes `input.teacherRates?.[input.teacherId] ?? null` | every course caller passes no `teacherRates` ⇒ NULL at birth (§1.3) |
| 4 | `:1464` `editOtherBooking` | `patch.teacherRateMinor = input.teacherRates[current.teacherId]` | OTHER-only by its own `bookingType` assertion — cannot touch a course row |
| 5 | `:1575` `seatOnGroup` | copies the GROUP template's rate onto the next group row | reads a GROUP row only |
| 6 | `jobs.service.ts` (day-end, reminder) | **no read** of the column (the reminder's mapping has no rate key; the day-end's select is `id · courseId · voucherId · studentId · coStudentId`) | — |
| 7 | the money pins (`other-schedule-req095`, `group-session-req095-2a`) | `ratePostedAt` never written; `other.teacherRates` by value for OTHER rows | untouched — none asserts the column is NULL on a course row |
**Verdict: REUSE.** Meaning identical on every row — *the coach's pay for THIS session, stored only* — and the only reader that would print it (`otherFacts`) is type-gated. The one semantic difference — on a COURSE row NULL means *inherit the course default*, on an OTHER/GROUP row NULL means *no rate* — lives in `effectiveRateMinor` alone, which reads the course. **No migration; 49 = 49.** Pin: `otherFacts`'s gate by source + the DTO by value (a COURSE row with `teacherRateMinor: 3` ⇒ `other: null`, `rate.overrideMinor: 3`); the OTHER DTO byte-identical by value (the existing TASK-394 test stays as is).

## §1.2 `getBookings` (`:812`) — its own `select({ b, s, t, sub, c })` with `leftJoin(students…)`, `innerJoin(teachers)`, `leftJoin(subjects)`, `leftJoin(coursePackages)`; then `toBookingDTO({...r.b, student: r.s, teacher, subject, course: r.c, additionalTeachers, rental})` — **no `coStudent`** ⇒ the DTO's `coStudent: null` and `displayName` = one name. Add: `const coStudents = alias(students, "co_students")` + `leftJoin(coStudents, eq(coStudents.id, bookings.coStudentId))`, hand `coStudent: r.cs` to the DTO. Same shape as TASK-420's course-search alias — one more join, no second read. (While there: the surface's `q` matches `studentId IN …` only — I propose `OR coStudentId IN …`, the same one-line rule as the course list; say no if you want the search untouched.)
**🔴 Finding A — the `displayName` rule has THREE copies today**, not one: `mappers.ts:175` (the DTO — the calendar AND the Bookings table both go through it), `jobs.service.ts:415` (the reminder — TASK-420 already prints `A & B` there via `joinChildNames`), and `scheduler.service.ts:1059` `describeSlotClash` (hand-copied `otherTitle ?? nickname ?? name`). The DTO copy does NOT join two names yet — so today the calendar shows one name on a DUO row too (Tanya's note is the DTO, not only `getBookings`). Proposal: ONE `displayNameOf(b)` in `db/mappers.ts` = `otherTitle ?? (coStudent ? joinChildNames(student, coStudent) : null) ?? nickname ?? name ?? ""`; the DTO, the reminder and the slot-clash sentence all call it (the reminder's row already loads `coStudent`; `describeSlotClash` loads `with: { teacher, student }` — gains `coStudent`). Pinned: no other `otherTitle ??` chain remains in src. Three copies → one is the same widening you accepted on TASK-420 (`householdLineUserIds`); say no and I fold only the DTO + `getBookings`.

## §1.3 The writers (TASK-420's `insert(bookings)` census)
`insertBooking` writes `teacherRateMinor: input.teacherRates?.[teacherId] ?? null` — the four course planners pass no `teacherRates` ⇒ NULL; the two clones' `.values` (reconcile append, sick-leave make-up) do not name the column ⇒ NULL. **Nothing copies the course rate onto a row; a make-up of a substituted session is born with the DEFAULT** (the substitute's 3 stays on the cancelled/leave row, not on its make-up — the right reading of "THAT session only"; say if the owner sees it otherwise). Pinned by absence (`classRateMinor` never in an `insert(bookings).values`; the clones' values byte-pinned).

## §2 The contract — confirmed, with two corrections
- **Model:** `course_packages.class_rate_minor` = the DEFAULT; `bookings.teacher_rate_minor` = the per-session OVERRIDE on a COURSE_PACKAGE row (NULL = inherit). **`effectiveRateMinor(row, course) = row.teacherRateMinor ?? course?.classRateMinor ?? null`** — ONE pure function in `lib/coach-rate.ts`, plus `rateFacts(row, course)` = `{ effectiveMinor, overrideMinor, defaultMinor }` beside it (the DTO's three facts from the same file — no arithmetic anywhere else).
- **Writes:** `PATCH /bookings/:id { classRateMinor: int ≥ 0 | null }` ⇒ the ROW's override (`null` clears); `400 NOT_A_COURSE_SESSION` when `courseId` is null (before the tx); the move + rate in one body allowed; `moveBooking`'s course write from TASK-420 REMOVED. `PATCH /courses/:id { classRateMinor }` ⇒ the default only; the `NOT_DUO` refusal REMOVED (any course); overrides untouched (by value: a row with override 3 keeps 3 after the default moves 7 → 8). **Correction 1:** the body key stays `classRateMinor` on both routes (Fern's popup already sends it); the DTO names the three facts under `rate`.
- **Reads:** `toBookingDTO` + `rate: { effectiveMinor, overrideMinor, defaultMinor } | null` — **Correction 2:** `rate` is non-null ONLY on `COURSE_PACKAGE` rows (an OTHER/GROUP row keeps its `other.teacherRates` / `group` facts and reads `rate: null` — the two meanings never share a key); `defaultMinor` comes from `b.course` (loaded by `withBookingRelations` and by `getBookings`'s join — no new read). The course DTO's `classRateMinor` unchanged (= the default). The plan reader unchanged. Nothing posts (by absence: the rate never on a line with `recordSale`/`recordRental`/`bo`; `ratePostedAt` never written — the existing pins).
- **The Bookings-table surface:** §1.2 + Finding A.
- **OTHER/GROUP byte-identical:** `otherFacts`/`groupFacts`/`editOtherBooking`/`seatOnGroup` untouched (pinned: the TASK-394/397 tests unchanged, the DTO by value).
- **Your ❓2:** popup body only — yes. `editOtherBooking` stays OTHER-only; no rate on any other route.
- 🚫 No migration; no posting; no report; the reminder's text unchanged (it prints no rate).

## DoD as I will run it
Suite count · tsc 0 · 49 = 49 · `effectiveRateMinor`/`rateFacts` by value (override · default · neither · non-course ⇒ null) · the two writes by value through fake reads (session ⇒ `bookings.teacher_rate_minor` only; course ⇒ `class_rate_minor` only; overrides survive) · the DTO's three facts by value on both surfaces (the relational path and `getBookings`'s hand-built row) · `displayName` = `A & B` on both surfaces through the ONE helper · OTHER DTO byte-identical · no posting by absence · break-and-watch in `try/finally` with sha-256 restore · @Fern's lines · report ×4.

**For @Fern (draft):** booking DTO + `rate: { effectiveMinor, overrideMinor, defaultMinor } | null` (COURSE_PACKAGE rows only) and `coStudent` on `GET /bookings` too; `displayName` = `A & B` on a DUO row everywhere; `PATCH /bookings/:id { classRateMinor: n | null }` edits THIS session (null = back to default; a non-course row ⇒ 400 `NOT_A_COURSE_SESSION`); `PATCH /courses/:id { classRateMinor }` edits the default for ANY course (no `NOT_DUO`).

⛔ Waiting on your confirm (reuse the column · Finding A's one helper · the `q` widening) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-21) — all three yes; build
1. **Reuse `teacher_rate_minor` — yes** (the readers are type-gated; the only semantic difference lives in `effectiveRateMinor`). No migration; 49 = 49. Correction 2 (`rate` non-null on COURSE_PACKAGE rows only) — yes; the two meanings never share a key.
2. **Finding A — ONE `displayNameOf(b)` — yes**, and pin by absence that no other `otherTitle ??` chain remains in the repo (the third copy in `describeSlotClash` included).
3. **The `q` widening on `getBookings` — yes**, the same one-line rule as the course list.
Also yes, as facts: a make-up of a substituted session is born with the DEFAULT (the 3 stays on the substituted row) — flagged to Porter; the body key stays `classRateMinor` on both routes. **Go.**

---

# ✅ CODE DONE — @Jason → @Sober (2026-09-21) — built as confirmed; no migration (49 = 49); 2647 pass / 0 fail; 19/19 mutations bite

**Numbers:** `bun test` **2647 pass / 0 fail**, 201 files (+1: `src/lib/coach-rate-req095-13-3.test.ts`, 16 tests) · `tsc --noEmit` **0** · **49 = 49** (no migration — the column is the reused `teacher_rate_minor`; pinned that no `rate_override` column exists) · 7 pins moved (the `getBookings` select list + alias; the two clash chains; the reminder chain; TASK-420's DUO-only rate block retired and its route test re-pointed; `NOT_DUO` removed from src — it had no caller left).

## What was built
- **`lib/coach-rate.ts` — the ONE rule:** `effectiveRateMinor(row, course) = row.teacherRateMinor ?? course.classRateMinor ?? null`; `rateFacts(row, course)` = `{ effectiveMinor, overrideMinor, defaultMinor }` **non-null only on a `COURSE_PACKAGE` row** (Correction 2). By value: override · default · neither · zero-is-a-rate · every other type ⇒ null. Pinned that no other file spells `teacherRateMinor ?? classRateMinor` (a scan of `src`), and that the file imports no db/sale/outbox.
- **The DTO:** `toBookingDTO` + `rate: rateFacts(b, b.course ?? null)` — `course` is already on both surfaces (the relational set; `getBookings`'s join) ⇒ no new read. By value: a course row with override 3 over default 7 ⇒ `{3, 3, 7}` and `other: null`; no override ⇒ `{7, null, 7}`; an OTHER row keeps `other.teacherRates` and reads `rate: null`; `otherFacts`'s type gate pinned (a mutation that un-gates it bites — a course row's override would print as a teacher rate).
- **The session write:** `PATCH /bookings/:id { classRateMinor: n | null }` ⇒ `patch.teacherRateMinor` on the ROW (null clears); a row without `courseId` ⇒ `400 NOT_A_COURSE_SESSION "คาบนี้ไม่ได้อยู่ในคอร์ส — ตั้งค่าสอนไม่ได้"` BEFORE the tx (by value: the tx never opens). TASK-420's course write from the move is GONE (by source: no `update(coursePackages)` in `moveBooking`). The move + the rate in one body allowed (validator by value).
- **The course write:** `PATCH /courses/:id { classRateMinor: n }` ⇒ the DEFAULT on ANY course — `NOT_DUO` removed (by value: a Private course takes 800; only `class_rate_minor` is set). Overrides survive (by value: a row's 3 is still effective after the default moved 7 → 8). `null` is refused on the course body (the default is set, never cleared) — say if you want it clearable.
- **Born with NULL:** `insertBooking` writes `teacherRates?.[teacherId] ?? null` (course callers pass none); the two clones' `.values` never name the column — pinned over the census (a mutation copying the override onto the make-up bites).
- **The Bookings-table surface:** `getBookings` — `alias(students, "co_students")` LEFT-joined (the DEF-3 rule: nullable ⇒ LEFT), `cs` in the select, `coStudent: r.cs` to the DTO; `q` matches `student_id IN … OR co_student_id IN …`.
- **🔴 Finding A — `displayNameOf(b)` in `db/mappers.ts`:** `otherTitle ?? (coStudent ? joinChildNames(student, coStudent) : null) ?? nickname ?? name ?? ""` — the DTO, the reminder (`|| "-"` for its own fallback) and BOTH slot-clash sentences (`describeSlotClash` + the additional-teacher guard — there were FOUR copies, not three; both reads now load `coStudent`) call it. Pinned: a scan of `src` finds the `otherTitle ?? … nickname` chain exactly once, in the definition. By value: the calendar (relational) and the table (hand-built row) render the same `Ploy & Pun`.
- **OTHER/GROUP byte-identical:** `otherFacts` / `groupFacts` / `editOtherBooking` / `seatOnGroup` untouched; the TASK-394/397 tests pass unchanged.
- 🚫 Nothing posts (by absence: the rate never on a line with `recordSale`/`recordRental`/`enqueueLine`/`bo.`; `ratePostedAt` never written — the existing pins + one more).

## Break-and-watch — `mut423.mjs`, 19 mutations, **19 bite** (`try/finally`, sha-256 restore byte-identical each)
A default wins over the override · B `rate` on every type · C `defaultMinor` reports the override · D the DTO drops `rate` · E `otherFacts` un-gated · F `displayNameOf` forgets the co-student · G the DTO uses a second chain · H the move writes the course again · I a non-course row takes an override · J `updateCourse` refuses a Private again · K/L `getBookings` drops the join / the DTO key · M `q` matches the primary only · N/O/P a clash sentence / the reminder keeps a hand chain · Q `null` refused on the session body · R a clone copies the override onto the make-up · S a clash read drops the co-student.

## Contract lines for @Fern (TASK-424) — final
- Booking DTO (every reader: calendar, Bookings table, plan sessions, create/PATCH responses) + `rate: { effectiveMinor, overrideMinor, defaultMinor } | null` — non-null on COURSE_PACKAGE rows only; render `effectiveMinor`, mark "override" when `overrideMinor != null`.
- `GET /bookings` rows now carry `coStudent` too, and `q` matches either child; `displayName` = `A & B` on a DUO row on EVERY surface (calendar, table, reminder, the clash sentence).
- `PATCH /bookings/:id { classRateMinor: n | null }` = THIS session's override (`null` = back to the course default); other fields may ride in the same body; a non-course row ⇒ `400 NOT_A_COURSE_SESSION`.
- `PATCH /courses/:id { classRateMinor: n }` = the course DEFAULT, any course (no `NOT_DUO` any more); existing overrides are untouched. The course DTO's `classRateMinor` = the default (name unchanged).

⛔ Only you mark this DONE. ▶️ Next: whatever waits on the board.
