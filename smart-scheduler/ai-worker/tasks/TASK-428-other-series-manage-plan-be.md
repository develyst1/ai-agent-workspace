# TASK-428 — `REQ-101` ECA/Free/KOL manage-plan, BE (`SPEC-088` Part A): the OTHER SERIES key (`0049`) · the series reads/writes (confirm all · cancel all · add/remove/swap teacher · add dates · edit title/kind/heads) · the two teacher notices · the backfill script — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-21) · **Size M+** · migration `0049_other_series_key` (50 = 50) · key 58 `action:calendar.other-cancel-all` · two outbox kinds (21st/22nd). `sid`; the next cycle.

## §0 Owner's rulings (Porter 09-21, all four SPEC-088 §A3 as recommended): Cancel-all behind its own key · Add-teacher from TODAY forward (future rows only) · existing ECA rows backfilled into series by a one-off script · the notice words = Porter's TH+EN draft in `REQ-101 §4` (the owner may tweak bytes later — so NOT placeholders: the draft's bytes, by value).

## §1 Verify before the contract
1. `createOtherSeries` (`scheduler.service.ts:1485`) — where a series key is minted and stamped on every row (the `group_key` shape); what the FE-facing 201 returns today (`{ created, bookingIds }` — gains `seriesKey`).
2. The rows a series manage-plan must list: every `OTHER` row with the key, any status, date order — which reader shape (the group's `seats`-style relation vs a plain select) and where `additionalTeachers` + rates ride.
3. **Add teacher on N rows in one tx:** `attachAdditionalTeachers(exec, bookingId, teacherIds, rates)` per row + the extra's slot check (`assertAdditionalTeacherFree`) — the first clash names date · hour · teacher and rolls the whole call back (the camp sync's shape). **Remove teacher:** the reverse (the primary cannot be removed — `409 PRIMARY_TEACHER`, swap instead). **Swap:** `swapGroupTeacher`'s shape by series key from a date on (the primary moves; extras untouched).
4. **Confirm all** = the existing bulk-confirm loop over the key's PENDING rows (the coach notice per row as today). **Cancel all** = every live row ⇒ CANCELLED with `cancelReason` (one of the closed set — `ADMIN_ERROR` is the "wrong entry" the customer means) in one tx; the coach told ONCE per teacher with the row list (a new notice kind, or the existing `class_cancelled_teacher` per row? — say which is cheaper and honest; N rows ⇒ N coach messages is noise).
5. Move on an OTHER row: does `moveBooking` tell the coach today (TASK-406 read: it sends nothing to families; the coach path — verify)? "Notify the teacher on edit/move/remove like a normal course" — list what a normal course sends the coach on move and whether OTHER rows already ride it.
6. The backfill: rows with `booking_type = OTHER`, `other_kind IN (ECA, FREE, KOL)`, `other_series_key IS NULL` grouped by `(other_title, other_kind, start_time, teacher_id)` ⇒ one key per group — a `scripts/backfill-other-series.ts` with `--dry-run` (the `ensure-sale-items` shape), the human runs it after `db:migrate`. CAMP rows excluded (they belong to `camp_week_days`).

## §2 The contract I propose — confirm/correct before code
- **`0049_other_series_key`:** `bookings.other_series_key uuid NULL` + partial index (`WHERE other_series_key IS NOT NULL`) = the witness. Catalog-only; the index on `bookings` = a SHARE scan (say it).
- **Create:** `createOtherSeries` mints ONE key and stamps every row; ⇒ `201 { seriesKey, created, bookingIds }`. `HUMAN_OTHER_KINDS` unchanged.
- **Reads:** `GET /other-series/:key` ⇒ `{ key, title, kind, headCount, startTime, teacherId, additionalTeacherIds, teacherRates, rows: [{ bookingId, date, status, teacherId, additionalTeacherIds }] }` (the header facts from the FIRST live row; rows in date order, all statuses); `GET /other-series?from&to` ⇒ the series overlapping a range (one line per key: title · kind · dates · live count) for a list page. Under `menu:calendar`.
- **Writes (all under `menu:calendar`; one tx each; a clash names date · hour · teacher and rolls back):** `POST /other-series/:key/confirm-all` (`calendar.status`) · `POST /other-series/:key/cancel-all { reasonCode, note? }` (**key 58** `action:calendar.other-cancel-all`, TH `ยกเลิกตารางอื่นๆ ทั้งชุด` / EN `Cancel a whole Other series`) · `POST /other-series/:key/teachers { teacherId, rateMinor?, fromDate = today }` (`booking-edit`) · `DELETE /other-series/:key/teachers/:teacherId { fromDate = today }` (`booking-edit`; the primary ⇒ 409) · `PATCH /other-series/:key/teacher { from, to, fromDate }` (`booking-edit`; the primary swap) · `POST /other-series/:key/dates { dates[] }` (`other-series` — the creator's key; the header facts copied) · `PATCH /other-series/:key { title?, otherKind?, headCount?, teacherRates? }` (`booking-edit`; every live row).
- **Notices:** the coach on add/remove = **`other_teacher_added` / `other_teacher_removed`** (the 21st/22nd kinds), ONE message per teacher listing the dates (not one per row), the bytes = REQ-101 §4's draft by value; move/edit/confirm/cancel ride the existing coach notices (per §1.4/§1.5 — say what is missing).
- **Scope (REQ-097):** none of the series routes in `TEACHER_ALLOWED`; a linked teacher reads their OTHER rows through the existing predicate.
- **Backfill:** `scripts/backfill-other-series.ts --dry-run | --apply` (§1.6); pinned by value on a fixture (three groups ⇒ three keys; CAMP rows untouched; a row with a key untouched).
- 🚫 No money; no change to the GROUP or CAMP objects; Free/KOL by construction (the kind is a tag).
- ❓ for you: (1) Cancel-all's coach message — one summary kind or the existing per-row notice; (2) whether `PATCH /other-series/:key` should also allow `startTime` (a time change = a move of every row with slot checks — I lean NO in this task; move rows individually); (3) the backfill's grouping key — is `teacher_id` needed in it, or would `(title, kind, start_time)` over-merge two coaches' series with the same name?

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · **50 = 50** · key 58 · the two kinds by value (the draft's bytes) · every series write by value (one tx; the clash rollback; from-today; the primary refusal) · the reads by value · the backfill by value on a fixture + `--dry-run` output in the report · nothing on GROUP/CAMP (by absence) · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-21) — §1 verified; §2 confirmed with three corrections and one finding; no code yet

## §1 Verified in the repo
1. **`createOtherSeries` (`scheduler.service.ts:1485`)** — mints NOTHING: one `insertBooking` per date (`bookingType OTHER`, `otherTitle`, the kind, heads, the primary + `teacherRates[primary]`) then `attachAdditionalTeachers` per row; the whole loop in ONE `db.transaction` (a clash names the date and rolls back — the shape §2's writes reuse); returns `{ created, bookingIds }`. No key of any kind is stamped — `group_key` is the GROUP row's only; an OTHER row has no series column today. ⇒ `0049` adds `other_series_key`; the create mints ONE uuid before the loop and passes it through `insertBooking` (a new input field, written in `.values` — the census pin from TASK-420 covers the site) ⇒ `201 { seriesKey, created, bookingIds }`.
2. **The rows to list:** a plain relational read — `db.query.bookings.findMany({ where: otherSeriesKey = key AND bookingType = OTHER, with: { teacher, additionalTeachers: { with: { teacher } } }, orderBy date, startTime })`, ALL statuses. The extras + their rates ride `additionalTeachers[].rateMinor` (the `booking_teachers` row); the primary's rate is `bookings.teacher_rate_minor` — the same shape `otherFacts` reads for the DTO. The header facts (title · kind · heads · start · primary · extras · rates) come from the FIRST LIVE row (`COURSE_LIVE_STATUSES` = PENDING/CONFIRMED/EXTENDED), else the first row.
3. **Add / remove / swap:** `attachAdditionalTeachers(exec, bookingId, ids, rates)` (`:1140`) does per extra `assertTeacherBookable` + `assertAdditionalTeacherFree` (the slot clash through `slotHolderWhere`, the clash sentence via `displayNameOf`) then ONE insert with `onConflictDoNothing` — so an add over N rows = the same call per row inside one tx; the first clash throws `SLOT_TAKEN` naming teacher · booking (I will prefix the DATE, as the create does) and the tx rolls back. **Remove** = delete the `booking_teachers` row per target; the primary ⇒ `409 PRIMARY_TEACHER`. **Swap** = `swapGroupTeacher`'s loop (`:1629`: per row `assertTeacherBookable`, `update teacherId` — the unique slot index answers the primary's clash as 23505 ⇒ `SLOT_TAKEN` with the date, `reconcileBookingHolds`), keyed by `other_series_key` from a date on; extras untouched. **Note:** the swap must also refuse when the NEW primary is already an EXTRA on the row (it would hold the slot twice) — `409 ALREADY_ON_ROW`; and the add must refuse the primary (`409 ALREADY_ON_ROW`).
4. **Confirm all / cancel all:** confirm = the per-row confirm branch (`updateBookingStatus(id, "confirm")` sends `booking_confirmed` to every teacher on the row + `issueCheckinToken`) in a loop over the key's PENDING rows — the `confirmCourse` shape (`results[]`, `confirmed`, `skipped`); the coach gets N confirm messages as today (they are per-row facts the coach already receives). Cancel all = every live row ⇒ `CANCELLED` + `cancelReason` (the closed `END_REASONS` — the validator reads the set) in one tx; today's per-row cancel sends `class_cancelled_teacher` per row per teacher (`:2917`). **❓1 answered — my proposal: ONE new summary kind `other_series_cancelled`** per teacher (title · reason · the dates list), NOT N × `class_cancelled_teacher`: honest (the series was cancelled, not each row by hand), cheaper (one outbox row per teacher), and the FE's per-row cancel keeps its per-row notice. Its words are net-new — the owner accepted only ADDED/REMOVED — so `other_series_cancelled` ships with a PLACEHOLDER (`📖`) marked for the owner, the same gate as item 7. That makes THREE kinds (21st–23rd); say "per-row" and I drop it.
5. **Move on an OTHER row (§1.5): 🔴 Finding A — `moveBooking` tells NO coach today, for ANY type.** The ordinary row move (`PATCH /bookings/:id`) writes the patch + `reconcileBookingHolds` and enqueues nothing (TASK-406 found the family side; the coach side is the same: nothing). The only move that tells coaches is the COURSE PLAN edit (`applyPlanChange` `:3215`: `teacher_unassigned` to the old, `teacher_assigned` to the new — on a TEACHER change only; a date/time move tells nobody). So "like a normal course" = on a teacher change, old + new coach told; on a date/time change, nobody. ⇒ for the series: the SWAP sends `teacher_unassigned`/`teacher_assigned` per row (the existing kinds and renderer — `Student :` prints nothing for a studentless row, `Program` the title); ADD/REMOVE send the two new kinds ONCE per teacher with the dates. `moveBooking`'s silence is pre-existing and out of this task's scope (say if you want a TASK for it — it affects lessons too).
6. **The backfill:** `booking_type = OTHER`, `other_kind IN (ECA, FREE, KOL)` (CAMP excluded by the kind — its rows belong to `camp_week_days`), `other_series_key IS NULL`. **❓3 answered: include `teacher_id`.** `(other_title, other_kind, start_time)` alone merges two coaches' same-named 15:00 clubs into one series, and a later swap-from-date would then move the wrong coach's rows; `(title, kind, start_time, teacher_id)` can at worst SPLIT a series whose primary was once moved by hand — the smaller harm (two keys the admin can see, vs one key over two coaches nobody can untangle). The script prints the groups (`--dry-run`), stamps one uuid per group (`--apply`), idempotent (keyed rows are skipped).

## §2 The contract — confirmed, with three corrections
- **`0049_other_series_key`** (50 = 50): `bookings.other_series_key uuid NULL` + `bookings_other_series_idx` partial (`WHERE other_series_key IS NOT NULL`) = the witness (the LAST object; the SHARE scan on `bookings` said in the header; every object IF NOT EXISTS; no default ⇒ no rewrite).
- **Create:** as §1.1; `HUMAN_OTHER_KINDS` unchanged; the validator unchanged.
- **Reads (`menu:calendar`):** `GET /other-series/:key` ⇒ `{ key, title, kind, headCount, startTime, teacherId, additionalTeacherIds, teacherRates, rows: [{ bookingId, date, status, teacherId, additionalTeacherIds }] }`; `GET /other-series?from&to` ⇒ `[{ key, title, kind, startTime, teacherId, firstDate, lastDate, liveCount, total }]` for the series with ≥ 1 row in the range (one `GROUP BY other_series_key`).
- **Writes (`menu:calendar`; one tx each; a clash ⇒ `409 SLOT_TAKEN` naming date · hour · teacher, nothing written):**
  · `POST /other-series/:key/confirm-all` (`calendar.status`) ⇒ `{ confirmed, skipped, results[] }`.
  · `POST /other-series/:key/cancel-all { reasonCode, note? }` (**key 58** `action:calendar.other-cancel-all` — TH `ยกเลิกตารางอื่นๆ ทั้งชุด` · EN `Cancel a whole Other series`) ⇒ `{ cancelled }`; ATTENDED rows untouched (history); the coach notice per ❓1.
  · `POST /other-series/:key/teachers { teacherId, rateMinor?, fromDate? = today }` (`booking-edit`) ⇒ `{ added }` — live rows with `date ≥ fromDate`; the primary or an existing extra ⇒ `409 ALREADY_ON_ROW`.
  · `DELETE /other-series/:key/teachers/:teacherId?fromDate=` (`booking-edit`) ⇒ `{ removed }`; the primary ⇒ `409 PRIMARY_TEACHER`.
  · `PATCH /other-series/:key/teacher { from, to, fromDate? = today }` (`booking-edit`) ⇒ `{ moved }` — the primary swap; `from` must be the primary (else 400); `to` an extra on the row ⇒ `409 ALREADY_ON_ROW`.
  · `POST /other-series/:key/dates { dates[] }` (`calendar.other-series`) ⇒ `{ created, bookingIds }` — the header facts + extras + rates copied from the template row (§1.2), the same clash rule as the create.
  · `PATCH /other-series/:key { title?, otherKind?, headCount?, teacherRates? }` (`booking-edit`) ⇒ `{ updated }` on every LIVE row; **❓2 answered: no `startTime`** — a time change is N slot moves; the FE moves rows one by one (or cancel-all + re-create).
  **Correction 1 — `fromDate` is applied by the SAME live-rows-from-date reader on add / remove / swap** (one `seriesRowsFrom(tx, key, fromDate)` helper; `fromDate` defaults to today for all three — the owner's ruling #2 said "add"; remove and swap follow it, since removing a coach from a past, attended row would rewrite history).
- **Notices:** `other_teacher_added` / `other_teacher_removed` (21st/22nd) — ONE per teacher, the payload carrying `{ title, kind, startTime, dates[] }`, the renderer printing REQ-101 §4's draft bytes (ADDED: `📅 เพิ่มตารางสอน / ADDED TO SCHEDULE` · คุณถูกเพิ่มเข้าตารางสอน · รายการ/วันที่/เวลา · กรุณาตรวจสอบตารางของคุณ + EN; REMOVED likewise) — by value; **Correction 2:** the date list prints one line per date (DD/MM/YYYY per REQ-087) with the hour once; no `bookingId` on the outbox row (a series notice is not a row's). Swap ⇒ the existing `teacher_unassigned`/`teacher_assigned` per row (§1.5). Confirm ⇒ the existing per-row confirm. Cancel-all ⇒ ❓1. **Correction 3 — the outbox `case` census (20 kinds today) becomes 22 (or 23 with ❓1); the `REMINDABLE`/reminder fold untouched (an OTHER row already rides the reminder).**
- **Scope:** none of the `/other-series*` routes in `TEACHER_ALLOWED`; the RBAC rows: reads under `menu:calendar`, writes as listed; 57 → 58 keys (the five count pins move).
- **Backfill:** `scripts/backfill-other-series.ts` — `--dry-run` (default) prints the groups as `title · kind · HH:MM · <teacher nickname> · N rows (dates …)`; `--apply` stamps; pinned by value on a fixture (three groups ⇒ three keys · a CAMP row untouched · a keyed row untouched · two coaches with the same title ⇒ two keys). The human runs it once after `db:migrate` (verify 50).
- 🚫 No money; GROUP and CAMP objects untouched (by absence: `groupKey`/`campWeekDayId` never written by any series function; `swapGroupTeacher`/`syncCampDayRows` byte-identical); Free/KOL by construction.

## ❓ for you
1. Cancel-all: ONE `other_series_cancelled` summary per teacher (placeholder words, 23rd kind) — or N per-row `class_cancelled_teacher`? (I propose the summary.)
2. `fromDate` on REMOVE and SWAP too (Correction 1) — yes?
3. Finding A (`moveBooking` tells no coach, any type) — a separate TASK, or fold "the OTHER row move tells the coaches" here? (I say separate: it is a lessons defect too.)

**For @Fern (draft):** `POST /bookings/other-series` ⇒ `201 { seriesKey, created, bookingIds }`; the booking DTO + `otherSeriesKey` (the Manage-plan link from a row); the reads/writes above with their codes (`SLOT_TAKEN` with the date · `PRIMARY_TEACHER` · `ALREADY_ON_ROW`); key 58 in `/api/me`'s `actions` gates the Cancel-all button.

⛔ Waiting on your confirm (three ❓, three corrections) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-21) — build to §2 as corrected, with the three ❓ answered and ONE correction of mine

1. **❓1 Cancel-all ⇒ ONE `other_series_cancelled` summary per teacher (the 23rd kind), placeholder words (`📖`, the item-7 gate — the owner has not seen them; Porter gets the draft request).** Payload `{ title, kind, startTime, reasonCode, dates[] }`, one outbox row per teacher (primary + extras), no `bookingId`. The per-row cancel keeps its per-row notice — pin that a cancel-all enqueues NO `class_cancelled_teacher`. ATTENDED rows untouched, as you wrote.
2. **❓2 `fromDate` on remove AND swap — YES,** through the ONE `seriesRowsFrom(tx, key, fromDate)`; default today for all three; pinned by value that a past row keeps its teachers on each of the three writes.
3. **❓3 Finding A — SEPARATE.** `moveBooking`'s silence is a lessons defect and an owner call (does the coach want a message on every date/time move?) — I put it on the owner's list via Porter; nothing of it in this task. The series swap rides `teacher_unassigned`/`teacher_assigned` per row, as §1.5.
4. **🔴 Correction of mine — the date line is `DD-MM-YYYY`, not `DD/MM/YYYY`.** The customer's format is `REQ-085 §16d` / `REQ-087` (`Date : 10-09-2026`; SYSTEM-FACTS "the LEAVE NOTICE date format is the CUSTOMER's"). One line per date, the hour once, on both new kinds and the cancel summary; pinned by value on the bytes.
5. Everything else as you wrote: `0049` (50 = 50, the partial index the witness, SHARE scan said), `201 { seriesKey, created, bookingIds }`, the reads' shapes, `ALREADY_ON_ROW` both ways, `PRIMARY_TEACHER`, no `startTime` on the header PATCH, key 58 (57 → 58, the five pins move), the backfill keyed with `teacher_id` (idempotent; the four fixture cases), nothing on GROUP/CAMP by absence. Plus: the booking DTO carries `otherSeriesKey` (your Fern line — make it explicit in §2's Reads so the row → page link exists).

**Fern's lines are final as your draft + `otherSeriesKey` on the DTO + the 23rd kind.** TASK-429 (FE) is cut against them. Report here + `inbox/SA.md` + log when done.

---

# ✅ CODE DONE — @Jason → @Sober (2026-09-21) — built as confirmed; `0049` ⇒ 50 = 50; key 58; three kinds (23); 2690 pass / 0 fail; 25/25 mutations bite

**Numbers:** `bun test` **2690 pass / 0 fail**, 204 files (+1: `src/lib/other-series-req101.test.ts`, 20 tests) · `tsc --noEmit` **0** · 🔴 **`drizzle/0049_other_series_key.sql` — 50 = 50** (idx 49; the partial index = the witness; the SHARE scan on `bookings` in the header; every object IF NOT EXISTS) · offline preflight `0049` alone `[]` and the batch 0038 → 0049 `[]` · 30 pins moved (the migration census 49 → 50 in 22 files; the key census 57 → 58 in 5; the outbox kinds 20 → 23 in 3; the create's stamped key + return; the ended-course-writes table + 7 series doors; `actorOf` 16 → 17; `max(500)` 6 → 7; `HUMAN_OTHER_KINDS` enums 3 → 4).

## What was built
- **The key:** `bookings.other_series_key uuid NULL` + `bookings_other_series_idx` (partial). `createOtherSeries` mints ONE `crypto.randomUUID()` before its loop and passes it through the ONE inserter (`insertBooking` writes `otherSeriesKey: input.otherSeriesKey ?? null` — TASK-420's census covers the site) ⇒ `201 { seriesKey, created, bookingIds }`. The booking DTO carries `otherSeriesKey` (your correction). `HUMAN_OTHER_KINDS` unchanged.
- **`services/other-series.service.ts`** (new — every read and write BY KEY; `scheduler.service` exports `assertTeacherBookable` / `attachAdditionalTeachers` / `reconcileBookingHolds` for it): ONE reader `seriesRows(exec, key)` (OTHER rows only, any status, date order, extras + rates from `booking_teachers`); ONE `seriesRowsFrom(rows, fromDate)` = the LIVE rows from the date on, used by add / remove / swap (`fromDate ?? today` — pinned ×3).
  · `GET /other-series/:key` ⇒ the header from the first LIVE row (else the first) + every row. `GET /other-series?from&to` ⇒ one line per series in the range (`GROUP BY other_series_key`).
  · **confirm-all** = `bulkConfirm` over the key's PENDING rows (the existing loop — per row atomic, a skipped row reported) ⇒ `{ confirmed, skipped, results }`.
  · **cancel-all** — ONE tx: every LIVE row ⇒ `CANCELLED` + `cancelReason` (the closed set; the validator reads `END_REASONS`) + the note, ATTENDED untouched, `reconcileBookingHolds` per row; the coach told ONCE per teacher on the CONFIRMED rows (`other_series_cancelled`), **no per-row notice** (by value: two teachers ⇒ two rows, zero `class_cancelled_teacher`; mutation H bites). Key 58 `action:calendar.other-cancel-all` (TH `ยกเลิกตารางอื่นๆ ทั้งชุด` · EN `Cancel a whole Other series`); `actorOf` rides.
  · **add teacher** — ONE tx over the live rows from the date: the primary or an existing extra ⇒ `409 ALREADY_ON_ROW` (names the date); `attachAdditionalTeachers` per row (the extra's slot check + `assertTeacherBookable`); the first clash ⇒ `409 SLOT_TAKEN "วันที่ <date> <HH:MM> — <the clash sentence> — ไม่ได้บันทึกอะไร"` and the tx rolls back; ONE `other_teacher_added` to the teacher with the dates.
  · **remove teacher** — the extra's `booking_teachers` rows from the date deleted (ONE statement); the primary ⇒ `409 PRIMARY_TEACHER`; ONE `other_teacher_removed`; a teacher not on the rows ⇒ `{ removed: 0 }`, no notice.
  · **swap the primary** — from the date: `from` must be the primary (400 otherwise), `to` an extra ⇒ `409 ALREADY_ON_ROW`; per row `assertTeacherBookable` + `update teacherId` (23505 ⇒ `SLOT_TAKEN` naming the date) + `reconcileBookingHolds` + the existing `teacher_unassigned` / `teacher_assigned` per row (the course plan's shape — §1.5).
  · **add dates** — the template row's facts + extras + rates copied through the ONE inserter with the key; a date already live ⇒ `409 DATE_EXISTS`; the same clash rule as the create.
  · **the header PATCH** — title / kind / heads on every LIVE row, the primary's rate on `bookings`, each extra's on its row; a stray rate ⇒ 400 (`assertRatesOnBooking`); **no `startTime`** (validator + source pinned).
- **The three kinds (21st–23rd):** `other_teacher_added` / `other_teacher_removed` print REQ-101 §4's owner-accepted bytes (TH+EN, by value: `📅 เพิ่มตารางสอน / 📅 ADDED TO SCHEDULE` · `คุณถูกเพิ่มเข้าตารางสอน` · `รายการ/Item` · `Time` · `วันที่/Dates` one line per date **`DD-MM-YYYY`** (your correction; mutation T bites on ISO) · `กรุณาตรวจสอบตารางของคุณ`); `other_series_cancelled` is a **📖 PLACEHOLDER** (marked in `line-i18n.ts`, the reason printed through the existing `ob_reason_*` labels). Everything is in the payload; no `bookingId` on the outbox row (a series notice is not a row's — mutation S bites). The three census tests (placeholder-leak · trailing-whitespace · ISO-leak) walk them.
- **RBAC:** the nine rows (reads under `menu:calendar`; confirm-all `status`; cancel-all key 58; add/remove/swap/header `booking-edit`; add-dates `other-series` — the creator's key); none in `TEACHER_ALLOWED`.
- **The backfill** `scripts/backfill-other-series.ts`: `groupForBackfill(rows, mint)` pure — `(title · kind · start_time · teacher_id)` ⇒ one key each (by value: three groups ⇒ three keys; two coaches' same-named clubs ⇒ two keys; date order inside); the query = `OTHER` + a HUMAN kind + `other_series_key IS NULL` (CAMP never reaches it; keyed rows skipped); `--dry-run` (the default) prints `<key>  <title> · <kind> · HH:MM · <nickname> · N rows (first … last)` and writes nothing; `--apply` stamps in ONE tx, re-checking `IS NULL`. **`--dry-run` output:** not run here — it reads `sid`'s database and I never touch a real environment; the human runs `bun scripts/backfill-other-series.ts` after `db:migrate` (verify **50**), reads the groups, then `--apply`. The pure grouping is what the suite pins.
- 🚫 By absence: the series file never names `groupKey` / `groupId` / `campWeekDayId` / any poster; GROUP and CAMP objects untouched (their tests byte-identical).

## Break-and-watch — `mut428.mjs`, 25 mutations, **25 bite** (`try/finally`, sha-256 restore byte-identical)
A the creator stamps no key · B the inserter drops it · C/D `seriesRowsFrom` includes past / cancelled rows · E cancel-all cancels ATTENDED · F drops the reason · G skips the holds · H a per-row notice instead of one per teacher · I add-teacher accepts the primary · J swallows the clash · K notifies per row · L remove takes the primary · M the swap accepts an extra as the new primary · N the swap tells no coach · O add-dates forgets the key · P forgets the extras · Q the header PATCH writes cancelled rows · R confirm-all confirms every row · S a `bookingId` on a series notice · T the date line in ISO · U an ADDED byte changed · V cancel-all behind the ordinary key · W the index not partial · X the backfill merges two coaches · Y the backfill touches keyed rows.

## Contract lines for @Fern (TASK-429) — final
- `POST /bookings/other-series` ⇒ `201 { seriesKey, created, bookingIds }`; the booking DTO + `otherSeriesKey: uuid | null` (the Manage-plan link from any OTHER row; null on un-backfilled rows until the human runs the script).
- `GET /other-series?from&to` ⇒ `[{ key, title, kind, startTime, teacherId, firstDate, lastDate, liveCount, total }]`; `GET /other-series/:key` ⇒ `{ key, title, kind, headCount, startTime, teacherId, additionalTeacherIds, teacherRates, rows: [{ bookingId, date, status, teacherId, additionalTeacherIds }] }` (404 `ไม่พบตารางชุดนี้`).
- `POST …/confirm-all` ⇒ `{ confirmed, skipped, results[] }` · `POST …/cancel-all { reasonCode: END_REASON, note? }` ⇒ `{ cancelled }` (key 58 in `/api/me`'s `actions` gates the button) · `POST …/teachers { teacherId, rateMinor?, fromDate? }` ⇒ `201 { added }` · `DELETE …/teachers/:teacherId?fromDate=` ⇒ `{ removed }` · `PATCH …/teacher { from, to, fromDate? }` ⇒ `{ moved }` · `POST …/dates { dates[] }` ⇒ `201 { created, bookingIds }` · `PATCH …/:key { title?, otherKind?, headCount?, teacherRates? }` ⇒ `{ updated }`.
- Codes: `SLOT_TAKEN` (the message names the date + hour; nothing written) · `PRIMARY_TEACHER` (409) · `ALREADY_ON_ROW` (409, names the date) · `DATE_EXISTS` (409) · a `from` that is not the primary ⇒ 400. `fromDate` defaults to today on add / remove / swap.

📦 Deploy (the human's, after the deploy): `db:migrate` (verify **50**) → `bun scripts/backfill-other-series.ts` (dry-run) → `--apply`. 🚫 No deploy request. ⛔ Only you mark this DONE.

---

# ✅ DONE — REVIEWED by @Sober (2026-09-21)
Re-run by me: **2690 pass / 0 fail, 204 files** · tsc 0 · **50 = 50** (`0049_other_series_key`, the partial index the last object) · key 58 in `permissions.ts` · the three kinds in ONE renderer (`line-message.ts:634–639`), CANCELLED marked 📖 · `other-series.service.ts` never names `class_cancelled_teacher` or `startTime` as a write · `scripts/backfill-other-series.ts` present. The `--dry-run` output absent for the right reason (never a real DB) — the human runs it after `db:migrate`; the pure grouping is pinned. Fern's lines are final = TASK-429 §0 (one addition: `409 DATE_EXISTS` on add-dates, and the 404 text). Contract held on every line.
