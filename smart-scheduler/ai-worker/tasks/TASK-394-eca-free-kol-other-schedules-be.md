# TASK-394 — ECA · Free/KOL "Other" schedules, BE: the `OTHER` booking gains a kind, a head count, a per-teacher rate (stored, not posted), and a bulk creator (`REQ-095` Stage 1, `SPEC-080`) — contract first

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-18)
**Owner (`REQ-095 §4`):** a NON-REVENUE schedule that reserves a teacher's slot(s): a label, a note, a head COUNT, no students, no course, no sale; the teacher rate entered/editable on the schedule, stored, passed to the backoffice as an expense (the PASS is an owner decision — SPEC-080 §3.1 — **this task STORES and posts nothing**); sessions fixed up front but editable; one slot, many heads.
**Size M.** ⛔ Chain stopped. **Contract to @Fern through me first** (TASK-395 in parallel). **Migration `0040` → 41 = 41.**

---

## §1 The prior facts (verify)
`bookingType OTHER` (REQ-078/TASK-224): no student/program, `title` required, several teachers via `bookingTeachers`, no money/entitlement/expiry, blocks the slot, on the calendar and in the coach's reminder; created one at a time through `POST /bookings`. No rate anywhere in the schema.

## §2 The contract (proposed; confirm/correct first)
- **Columns (migration `0040`, nullable adds — the header names each lock):** `bookings.other_kind text null` (`ECA` | `FREE` | `KOL` — a code constant list, not an enum label, so no split run), `bookings.head_count int null`, `bookings.teacher_rate_minor int null` (the PRIMARY teacher's rate for this session), `booking_teachers.rate_minor int null` (each additional teacher's), `bookings.rate_posted_at timestamptz null` (reserved for the backoffice pass — always null this stage). Witness = the last column.
- **`POST /bookings` for `OTHER` accepts** `otherKind`, `headCount` (≥ 0), `teacherRateMinor`, and `additionalTeachers[].rateMinor`; `PATCH /bookings/:id` edits them (an `OTHER` only). Validation shape-only; unknown kind ⇒ `400`.
- **The bulk creator — `POST /bookings/other-series { title, otherKind, headCount, note?, teacherId, additionalTeachers?, startTime, endTime, dates: string[] }`** ⇒ creates one `OTHER` row per date in ONE transaction, refusing the whole set on any `SLOT_TAKEN` (`409` naming the date — "fixed up front" means all or nothing); `{ created: n, bookingIds }`. Each row is an ordinary `OTHER` afterwards (editable/cancellable one by one — the existing routes). Action key: **`action:calendar.other-series`** (49th; EN `Create an Other schedule series`) — the single `OTHER` create stays under `calendar.book`.
- **DTO:** `otherKind`, `headCount`, `teacherRateMinor`, `additionalTeachers[].rateMinor` on the booking; the calendar's cell already shows `title` for `OTHER`.
- 🚫 No `recordSale`/expense post anywhere in this task (asserted by absence); no student, no entitlement; the coach's reminder line for `OTHER` unchanged (say if the head count should print — I lean yes: `Heads : 12` after the title; the owner's words on the message go through @Porter, so PLACEHOLDER-gated like TASK-370).

## Definition of Done
- [ ] Contract confirmed first · Suite, **count** · tsc · **41 = 41** · header
- [ ] Pinned: the series creates n rows atomically, a clash refuses all with the date · the fields round-trip on create/edit/DTO · a lesson type refuses `otherKind`/`headCount` · no money call · the key
- [ ] 🔑 Break-and-watch, `finally`, CHECKSUM

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-18) — confirm/correct before I build

**§1 verified:** `OTHER` = no student/program, `otherTitle` required, extras via `booking_teachers` (`additionalTeacherIds`, TASK-239's per-teacher clash gate), `otherPriceMinor | otherPriceItemId` (TASK-225's sale — the ONE money path an `OTHER` has; untouched), blocks the slot, one at a time through `POST /bookings` → `insertBooking`. No rate anywhere. `endTime` is DERIVED (`addHour(startTime)`) for every booking — an `OTHER` is one hour like everything else.

## Migration `0040_other_schedule.sql` — 41 = 41
Five nullable column adds, one transaction, all `IF NOT EXISTS`: `bookings.other_kind text` · `bookings.head_count integer` · `bookings.teacher_rate_minor integer` (the PRIMARY teacher's) · `bookings.rate_posted_at timestamptz` (reserved, always NULL this stage) · **`booking_teachers.rate_minor integer` — the LAST column, the witness.** 🔒 The header: **`bookings` is the HOT table** (every calendar read, the reminder job, thousands of rows) — four ACCESS EXCLUSIVE blinks, each catalog-only (nullable, no default, no rewrite), but the queueing hazard is real here: run at a quiet moment, never during the 17:30 job or an import; `booking_teachers` is tiny. The cutover order line: after `0039`, one run, verify 41. `other_kind` is a CODE list (`ECA | FREE | KOL`, `lib/other-kind.ts`), not a PG enum — no split run, ever.

## The shapes
- **`POST /bookings` (`OTHER`) accepts** `otherKind` (the three), `headCount` (int ≥ 0), **`teacherRates?: Record<teacherId, minor>`** — ONE map keyed by teacher for the primary AND the extras (the primary's lands on `bookings.teacher_rate_minor`, each extra's on its `booking_teachers` row; an id in the map that is not on the booking ⇒ 400). ❓ your §2 had `teacherRateMinor` + `additionalTeachers[].rateMinor` — two shapes for one fact; the map keeps `additionalTeacherIds` as the FE already sends it and adds one field. Say if you prefer the two.
- **The four lesson types REFUSE** `otherKind` / `headCount` / `teacherRates` (TASK-224's refine, extended) — refused, not dropped.
- **Editing — a dedicated `PATCH /bookings/:id/other { otherKind?, headCount?, teacherRates? }`** rather than `PATCH /bookings/:id`: that route is `moveBooking` (re-times and TELLS the teacher); a kind/count/rate edit is not a move and must notify nobody — the same reasoning that gave the note its own route (TASK-178). `OTHER` only (a lesson type ⇒ 400); under `action:calendar.booking-edit`. ❓ confirm the separate route.
- **The series — `POST /bookings/other-series { title, otherKind, headCount, note?, teacherId, additionalTeacherIds?, teacherRates?, startTime, dates: string[] }`** (`dates` 1–60, unique, `DATE`; `endTime` derived +1h as everywhere — a longer block is several rows, say if the owner wants a duration) ⇒ ONE transaction calling the existing `insertBooking(tx, null, …)` per date in date order — every gate reused (teacher bookable, the extras' clash gate, the slot UNIQUE); the FIRST clash aborts the whole set with **`409 SLOT_TAKEN "วันที่ <date> ครูไม่ว่าง — ไม่ได้สร้างรายการใด"`** (the date named, nothing created) ⇒ `201 { created: n, bookingIds: string[] }`. Each row is an ordinary `OTHER` afterwards (move/cancel/edit one by one). Key **`action:calendar.other-series`** (49th; TH `สร้างตารางอื่นๆ เป็นชุด` / EN `Create an Other schedule series`); the single create stays under `calendar.book`.
- **DTO:** the booking gains **`other: { kind, headCount, teacherRates: Record<teacherId, minor>, ratePostedAt: null } | null`** (null for a lesson type; the rates keyed by teacher so the FE's per-teacher inputs read one object). The calendar cell already prints `title`; `displayName` untouched.
- 🚫 **No money:** no `recordSale` / `bo.movement` / expense anywhere in the new code (by absence — the series creator and the edit); `rate_posted_at` never written (by absence). No student, no entitlement. The coach's reminder line for `OTHER` — ❓ **`Heads : 12` after the title, PLACEHOLDER-gated** (built behind the same gate as TASK-370, rendered only once @Porter brings the owner's word) — or leave the message entirely alone this stage; I lean gated-in.

## For @Fern (draft, final after confirm)
`POST /api/bookings` (`OTHER`) + `otherKind` · `headCount` · `teacherRates` · `PATCH /api/bookings/:id/other` · `POST /api/bookings/other-series` ⇒ `201 { created, bookingIds }` | `409 SLOT_TAKEN` naming the date · booking DTO `other: { kind, headCount, teacherRates, ratePostedAt }` · keys `action:calendar.other-series` (the series) and `calendar.book` / `calendar.booking-edit` (single create / edit).

⛔ Waiting on your confirm (the rates map · the separate edit route · the reminder line gated-in or not) before code.

---

# 📤 REPORT — @Jason → @Sober (2026-09-18)

✅ **DONE (code), against the confirmed contract (your three answers applied: the ONE `teacherRates` map · the dedicated `PATCH /bookings/:id/other` · `Heads : n` gated IN as a placeholder).** **2441 pass / 0 fail**, **192 files** (+1) · `tsc --noEmit` clean · **MIGRATION `0040_other_schedule` → 41 = 41** · preflight scanner clean (no enum, nothing non-transactional) · witness registered.

## The migration — `drizzle/0040_other_schedule.sql`
Five nullable column adds in one transaction, all `IF NOT EXISTS`: `bookings.other_kind text` · `bookings.head_count integer` · `bookings.teacher_rate_minor integer` (the PRIMARY teacher's) · `bookings.rate_posted_at timestamptz` (RESERVED — nothing writes it, pinned by absence) · **`booking_teachers.rate_minor integer` — the LAST column, the witness.** 🔒 The header, verbatim as confirmed: **`bookings` IS THE HOT TABLE** — four ACCESS EXCLUSIVE blinks, each catalog-only (nullable, no default, no rewrite), the queueing hazard named as REAL here with the forbidden windows (never during the 17:30 job, the morning reminder, or an import); `booking_teachers` tiny. The cutover order `0038 → 0039 → 0040`, one run, verify 41. `other_kind` is a CODE list (`lib/other-kind.ts`: `ECA | FREE | KOL`), never a PG enum — the header says why (0029's split-run lesson); the mutation that makes it an enum bites.

## What was built
- **`lib/other-kind.ts`** — `OTHER_KINDS`, `isOtherKind`, **`assertRatesOnBooking(rates, teacherIds)`** (a rate for a teacher not on the booking ⇒ `400 "อัตราค่าสอนของครูที่ไม่ได้อยู่ในรายการ: <ids>"`; pure).
- **`validation.ts`** — `createBooking` gains `otherKind` (`z.enum(OTHER_KINDS)`), `headCount` (int ≥ 0), **`teacherRates: Record<ID, int ≥ 0>`**; TASK-224's lesson-type refine extended — **the four lesson types REFUSE all three** (the same sentence). New `editOtherBooking` (≥ 1 field) and `otherSeries` (title · kind · headCount · note? · teacherId · additionalTeacherIds? · teacherRates? · startTime · `dates` 1–60 unique; no `endTime` — derived +1h as for every booking; the extras' duplicate / repeat-of-primary refusals as on create).
- **`scheduler.service.ts`** — `insertBooking` writes the three columns (the primary's rate out of the ONE map); `attachAdditionalTeachers(…, rates)` writes each extra's on its row; `createBooking` refuses a stray rate id BEFORE the transaction. **`editOtherBooking(id, input)`** — 404 · a lesson type ⇒ 400 · the map checked against the teachers ON the booking · one tx updates `bookings` (kind/count/primary rate) and each extra's `booking_teachers.rate_minor` · **no notification, no move fields** (the note's precedent). **`createOtherSeries(input)`** — the map checked first · dates sorted · **ONE `db.transaction`** · the existing **`insertBooking(tx, null, { …, bookingType: "OTHER", date })`** per date + the extras (every gate reused: teacher bookable, the extras' clash gate, the slot UNIQUE — pinned: no raw `tx.insert(bookings)`) · **the FIRST `SLOT_TAKEN` ⇒ `409 SLOT_TAKEN "วันที่ <date> ครูไม่ว่าง — ไม่ได้สร้างรายการใด (<the clash>)"`** and the transaction rolls every earlier row back · `{ created, bookingIds }`. The hand-built loader `additionalTeachersByBooking` carries `teacherId` + `rateMinor` so the list path reads the same (still only the extras, still never composing the list — the AC-18 pin extended with the reason: a fourth touch point that only UPDATES a rate cannot change "who teaches").
- **`db/mappers.ts`** — the booking DTO gains **`other: { kind, headCount, teacherRates: Record<teacherId, minor>, ratePostedAt } | null`** (null for a lesson; the primary's rate from `bookings`, each extra's from its row; a pre-0040 OTHER reads nulls + `{}`).
- **Routes + key** — `PATCH /bookings/:id/other` ⇒ `{ booking }` (under `calendar.booking-edit`) · **`POST /bookings/other-series`** ⇒ `201 { created, bookingIds }` under **`action:calendar.other-series`** (49th; TH `สร้างตารางอื่นๆ เป็นชุด` / EN `Create an Other schedule series`); the single OTHER create stays under `calendar.book`. Both classified `unrelated` in the ended-course table with the reason (an OTHER never names a course).
- **The reminder — `Heads : n`** (answer 3): `jobs.service` maps `headCount` off the row it already holds; `line-today-schedule` prints `<label> : n` as the FIRST appended line, before `Remark`, only when the row has a count (`0` prints; a lesson never has one); the label `ob_f_heads` is a **📖 PLACEHOLDER — MINE, the customer has NOT seen it** (the i18n comment says so; pinned by FORM, not bytes — the moment the owner's word comes back through @Porter the line changes and the pin flips to bytes).
- 🚫 **No money:** no `recordSale`/`recordRental`/`boMovement`/`postBookingSale`/discount in the edit or the series; `ratePostedAt` never written anywhere in the service — all by absence.
- 🔻 **Pins moved, with reasons:** the `40 = 40` pins → `41 = 41` (ten files; the "last witness / last file" pins of 0039 by tag / index); the key count 48 → 49; the mutate-route floor 61 → 63; the ended-course classification (+ two); TASK-224's AC-18 join-table pin (+ the rate writer, with the reason above).

## Pinned (`lib/other-schedule-req095.test.ts`, 21 tests)
- **The migration (source):** 41 = 41, 0040 last, the order 0038 → 0039 → 0040 by tag · the five ALTERs in order, the witness LAST, no DEFAULT / NOT NULL / `CREATE TYPE` / `ADD VALUE` · the header's hot-table sentences, the forbidden windows, the cutover line, the count line · the witness entry · the schema mirrors all five.
- **Pure:** the kinds; `assertRatesOnBooking` by value (pass / stray ⇒ 400 naming it / no map).
- **Validation by value:** OTHER accepts the three; unknown kind / negative count / negative rate refused; **every lesson type refuses each of the three with the sentence**; the edit ≥ 1 field; the series' date rules and no `endTime`.
- **The DTO by value:** kind, count, the primary's rate from `bookings` and each extra's from its row, keyed by teacher; `teachers[]` still the ONE accessor's answer; a pre-0040 OTHER; a lesson ⇒ null; the loader carries the two fields (source).
- **The writes (source):** `insertBooking`'s three columns (and no `ratePostedAt`), the extras' rates, the stray-rate refusal BEFORE the tx · the edit's 404 / lesson 400 / the on-booking check / both updates / **no notification, no move fields** · the series: rates first, sorted, ONE transaction, through `insertBooking` only (no raw insert), the extras, the 409 sentence with the date, the return · **no money by absence** in both, `ratePostedAt` never written.
- **The routes through the ROOT app:** the series ⇒ 201 by value with the actor-free body passed through; **the 409 envelope naming the date by value**; an unknown kind ⇒ 400 · the edit ⇒ `{ booking }`, empty body 400, a lesson's 400 passes through · the key gates exactly the series; the edit is a `booking-edit`; the single create stays `book`.
- **The reminder:** `Heads : 12` before `Remark` for an OTHER with a count; absent without; `0` prints; a lesson never prints it; the label is a placeholder (the comment), the job maps it, the renderer's line (source).

## 🔑 Mutation — twenty, `finally`, checksum — all bite
A a lesson silently accepts `headCount` · B free-text kind · C duplicate dates in the series · D the primary's rate dropped · E the extras' rates dropped on attach · F a stray rate id not refused · G the edit accepts a lesson · H the edit tells the teacher · I the series not one transaction · J the 409 loses the date · K the series bypasses `insertBooking` (raw insert) · L the series posts a sale · M `rate_posted_at` written at creation · N `other` on a lesson · O the extras' rates dropped from the DTO · P the series gated by `book` · Q `Heads` dropped · R `Heads : 0` swallowed · S the witness a `bookings` column · T `other_kind` as a PG enum. Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final
`POST /api/bookings` (`bookingType: "OTHER"`) + `otherKind: "ECA" | "FREE" | "KOL"` · `headCount: int ≥ 0` · `teacherRates: { [teacherId]: satang }` (the primary AND the extras in ONE map; an id not on the booking ⇒ 400) — the lesson types refuse all three · `PATCH /api/bookings/:id/other { otherKind?, headCount?, teacherRates? }` ⇒ `{ booking }` (no teacher notice; a lesson ⇒ 400) · **`POST /api/bookings/other-series { title, otherKind, headCount, note?, teacherId, additionalTeacherIds?, teacherRates?, startTime, dates[] }`** ⇒ `201 { created, bookingIds }` | **`409 { error: { code: "SLOT_TAKEN", message: "วันที่ YYYY-MM-DD ครูไม่ว่าง — ไม่ได้สร้างรายการใด (…)" } }`** (nothing created; each row afterwards is an ordinary OTHER — move / cancel / edit one by one) · booking DTO **`other: { kind, headCount, teacherRates, ratePostedAt: null } | null`** · keys: `action:calendar.other-series` (the series), `calendar.book` (a single OTHER), `calendar.booking-edit` (the edit) — all in `/permissions` · `endTime` is +1h (a longer block = several rows; the duration question is on the owner's list) · the coach's reminder prints `Heads : n` (placeholder label).
📦 **After the migration:** nothing to set — every existing OTHER reads `other: { kind: null, headCount: null, teacherRates: {}, ratePostedAt: null }`. The cutover = `0036`…`0040` in one `db:migrate`, verify 41.
⚠ **For the OWNER'S list (via you):** the `Heads` label word; the duration of an OTHER block (today +1h like every booking); the expense PASS (SPEC-080 §3.1 — `rate_posted_at` is waiting for it).
