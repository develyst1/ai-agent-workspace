# TASK-397 — DUO / Group: the GROUP SESSION object, BE (`REQ-095` Stage 2a, `SPEC-081`) — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-18) · **Size L** · migration `0041_group_session` (42 = 42). Ships in ONE `sid` deploy with TASK-396 + Stage 1 — 🚫 do not ask for a deploy.

## §0 Read `SPEC-081` first — §0 (why seats), §1 (the model), §2 (the flows). This task does not re-transcribe it.

## §1 Verify before the contract (write what you find in your CONTRACT section)
1. `student_id` is nullable (`0029`) and an OTHER row already inserts with `null` — a GROUP row rides the same `insertBooking` path.
2. Every availability read that mirrors the index: list them (`booking-slot.ts holdsSlot`, `slot-clash.ts`, `findFreeExtensionDate`, make-up placement, extension, `additional-teacher-clash`) — the seat predicate must reach ALL of them from the ONE definition, or a seat would clash a make-up placement.
3. The day-end job has no `bookingType` filter: a GROUP row goes ATTENDED with nothing deducted (as OTHER) — confirm nothing posts (`other-booking-money` precedent).
4. The reminder's grouping (`daily-reminder.ts groupReminders`): where a titled block with children under it fits.

## §2 The contract I propose — confirm/correct before code
- **Migration `0041`:** `ALTER TYPE booking_type ADD VALUE 'GROUP'` (own statement, `0029` shape) · `bookings.group_key uuid` (nullable) · `bookings.group_id uuid` (nullable, FK → `bookings.id`) · **the index rebuilt with `AND group_id IS NULL`** — `0033` shape, the ACCESS EXCLUSIVE warning verbatim, closed shop · index on `group_id` · witness: the predicate change + `group_key`.
- **`SLOT` one definition:** `holdsSlot(status, groupId)` (or a `seatOf` predicate beside it) — every mirror imports it; a source pin that no availability read filters by hand.
- **`POST /bookings/group-series`** `{ name, groupKind: "DUO"|"GROUP", seatCap, teacherId, additionalTeacherIds?, teacherRates?, startTime, dates[] }` ⇒ `201 { groupKey, created, bookingIds }` | `409 SLOT_TAKEN` naming the date; all-or-nothing; key `action:calendar.group-series` (50th). `seatCap` 2..12; DUO ⇒ 2.
- **`POST /courses`** gains `groupKey?`: teacher/weekday/startTime taken FROM the group (a mismatch in the body ⇒ `400`); each planned session ⇒ a seat (`group_id` = that date's group row); missing date ⇒ the group is extended (one GROUP row, same teacher/rates/cap; `409 SLOT_TAKEN` naming the date); cap ⇒ `409 GROUP_FULL` naming the date. Everything after the insert (sale, quota, expiry, reminder) unchanged.
- **`PATCH /bookings/:id/group-teacher`** `{ teacherId, fromHereOn }` ⇒ group row(s) by `group_key` from this date on (or this one) + all live seats move in ONE tx; per-date slot check `409` naming the date; the family notice per seat via the existing teacher-change notice; the coach notice once per date.
- **Cancel on a GROUP row** ⇒ each live seat through the existing teacher-cancel path (make-up per child), single-session seats through their ordinary cancel. Cancel on a seat ⇒ ordinary.
- **Leave on a seat** ⇒ ordinary; the make-up appends SOLO (ordinary placement). 🚫 No rule change in `reconcileCoursePlan` beyond seats being ignored as slot holders.
- **`PATCH /bookings/:id/other`** accepts GROUP rows (rates/head count = cap; the 400 sentence says OTHER or GROUP).
- **DTO:** GROUP row `group: { key, kind, name, seatCap, seats: [{ bookingId, studentId, studentName, status, courseId }], teacherRates, ratePostedAt }`; seat row `groupId`, `groupName`. The calendar GRID loader hides seat rows; every other read keeps them.
- **Reminder:** GROUP block = name · `Seats : n/cap` · the children's lines beneath (their existing shape). Parent's line for a seat: unchanged.
- **Reader guard:** `assertStudentActive` (REQ-093) and suspension apply to seats as to any session.
- ❓ for you: (1) `group_key` on `bookings` vs a tiny `booking_groups` table (name/kind/cap once, not per row) — I propose the column to keep the one-row-per-date shape; say if the table is cleaner; (2) whether `POST /courses { groupKey }` extending the group silently is right or should `409` instead (I propose extend).

## §3 Not in this task (2b, TASK later): the per-session seat (`SINGLE_SESSION` + `groupId`).

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · **42 = 42**, preflight splits the enum · the one-definition predicate pinned by source across every mirror · the flows by value (series 409 with the date; cap 409; extend-on-missing-date; swap moves seats in one tx; cancel cascades) · no money on a GROUP row · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern (final DTO + bodies) · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-18) — §1 verified, §2 confirmed/corrected; no code until you confirm

## §1 What I found (the four reads)
1. **`student_id` nullable, an OTHER inserts `null`** — verified: `createBooking` → `resolveStudentId` only when `input.student`; `insertBooking(tx, null, …)` is exactly what the Stage 1 series calls. A GROUP row rides the same path. ✅
2. **The mirrors of the slot index — FIVE, and one is ALREADY DRIFTED:**
   · the index itself (`schema.ts:498`, predicate built from `SLOT_INACTIVE_STATUSES` — 4 statuses incl. PAUSED, TASK-260);
   · `describeSlotClash` (`scheduler.service.ts:1028`, `SLOT_INACTIVE_STATUSES`);
   · `assertAdditionalTeacherFree` (`:1148`, `SLOT_INACTIVE_STATUSES`);
   · `findFreeExtensionDate`'s weekly-slot check (`:2360`, `SLOT_INACTIVE_STATUSES`) — the make-up / extension placement;
   · 🔴 **`getSlotAvailability` (`:2161`) reads `SLOT_NON_BLOCKING` from `lib/booking-slot.ts` — a SECOND definition with THREE statuses (no PAUSED).** `holdsSlot` there has no production caller. So the availability picker shows a PAUSED booking as BOOKED while the index (and the other three) treat it as free — a pre-existing drift from TASK-260, exactly the two-definitions failure the file's own header warns about. **This task folds it:** ONE `slotHolder` predicate (below) reaches all five, retiring `SLOT_NON_BLOCKING`.
   `bulk-confirm`, the reminder, the job read none of this. Make-up placement and extension both go through `findFreeExtensionDate` + `insertBooking` (the index) — the seat predicate reaches them through the one definition.
3. **The day-end job** — the attend select has no `bookingType` filter (`status = CONFIRMED` + the start gate): a GROUP row goes ATTENDED; the deduction branches key on `courseId` / `voucherId` (both null on a GROUP row) ⇒ nothing deducted; **the revenue sweep is `inArray(bookingType, [FIRST_TRIAL, SINGLE_SESSION, OTHER])` — a GROUP row is NOT in the list ⇒ nothing posts**, by construction (pinned by absence: GROUP never enters that list). ✅
4. **The reminder** — `groupReminders` builds one `TodayRow` per session and keys teacher groups by every assigned teacher; the renderer prints numbered blocks by time. A GROUP row is an OTHER-shaped row (title, no student) and lands on the coach naturally; each seat would ALSO land as its own entry at the same time — so the coach's schedule needs: **seat rows folded UNDER their group's entry** (the group's `TodayRow` gains `seats: [{ studentName, remaining }]` and the block prints `Seats : n/cap` + one line per child), the seat rows themselves dropped from the teacher grouping (kept for the PARENT grouping unchanged). One place: the row builder in `jobs.service.ts` + `groupReminders`.

## §2 The contract — confirmed with corrections
- **Migration `0041_group_session` (42 = 42), FOUR statements, the `0029` + `0033` shapes:** (1) `ALTER TYPE booking_type ADD VALUE IF NOT EXISTS 'GROUP'` — its own statement; **the preflight WILL split the run here** (the label's first use is statement 4's predicate? — no: the predicate reads `group_id`, not the label, so nothing in 0041 uses `'GROUP'` and the split is between 0041 and the first migration that does; today none). (2) `bookings.group_key uuid NULL` + (3) `bookings.group_id uuid NULL REFERENCES bookings(id) ON DELETE RESTRICT` + an index `bookings_group_id_idx` (the seat reads and the cap count hit it every write). (4) **`DROP INDEX bookings_teacher_slot_uq; CREATE UNIQUE INDEX bookings_teacher_slot_uq … WHERE status NOT IN (…) AND group_id IS NULL`** — **ACCESS EXCLUSIVE on `bookings` for the rebuild, the `0033` closed-shop warning verbatim** (the rebuild scans every row; a blocked calendar for its duration; quiet moment, never near 17:30 or an import). Witness: `{ kind: "index-predicate", index: "bookings_teacher_slot_uq", contains: "group_id IS NULL" }` (the `0022`/`0033` shape — existence is NOT valid here, the index existed before). `schema.ts`: the index's `.where` gains `and <groupId> is null` from the same literal.
- **The ONE predicate:** `lib/slot-holder.ts` exports `SLOT_INACTIVE_STATUSES` (moved from schema? — no: re-exported), **`slotHolderWhere(b)`** = `and(notInArray(b.status, SLOT_INACTIVE), isNull(b.groupId))` for the four SQL mirrors, and **`holdsSlot({ status, groupId })`** pure (the availability read's in-memory filter + tests). `lib/booking-slot.ts` / `SLOT_NON_BLOCKING` retired. **Pinned by source: every `notInArray(b.status` / `nin(b.status` in `scheduler.service.ts` that names `SLOT_INACTIVE` is gone; the five sites call the helper; `grep SLOT_NON_BLOCKING` = 0.** (This also FIXES the PAUSED drift in the picker — named in the report as a side effect, not silently.)
- **`POST /bookings/group-series`** `{ name, groupKind: "DUO" | "GROUP", seatCap, teacherId, additionalTeacherIds?, teacherRates?, startTime, dates[] }` (`seatCap` 2..12; `DUO` ⇒ exactly 2, a mismatch ⇒ 400) ⇒ N GROUP rows (`booking_type = GROUP`, `other_title = name`, `head_count = seatCap`, `other_kind = null`, rates on the Stage 1 columns) under ONE `group_key` (a uuid minted in the service), all-or-nothing through `insertBooking` (the Stage 1 series' shape, byte-for-byte the same loop), `409 SLOT_TAKEN` naming the date ⇒ `201 { groupKey, created, bookingIds }`. Key **`action:calendar.group-series`** (50th; TH `สร้างกลุ่ม DUO/Group เป็นชุด` / EN `Create a DUO/Group series`).
- **`POST /courses { groupKey? }`** — the group's teacher / weekday / startTime are read from its FIRST live row and **must equal the body's** (`400 GROUP_MISMATCH "คอร์สต้องใช้ครู/วัน/เวลาเดียวกับกลุ่ม"` — the FE prefills them; the body still carries them so the course row is self-describing); every planned session row is inserted as a SEAT: `group_id` = the group row of that date; **cap** = count of live seats on that row `< head_count` in the same tx ⇒ else `409 GROUP_FULL "วันที่ <date> กลุ่มเต็ม (n/cap)"`; **a date with no group row ⇒ EXTEND** (your ❓2 — yes, extend: one GROUP row on that date, same teacher/extras/rates/cap, through `insertBooking` ⇒ `409 SLOT_TAKEN` naming the date if the teacher is taken). The seat insert goes through `insertBooking` too, with `groupId` set — the index ignores it, the extras' clash gate is NOT run for a seat (the group row already holds the teachers). Everything after the inserts (sale, quota, expiry, the confirm message) is untouched — pinned: the course create's post-insert region has no `group` in it.
- **`PATCH /bookings/:id/group-teacher { teacherId, fromHereOn }`** ⇒ the GROUP row(s) of that `group_key` from this date on (or this one) + every live seat on them move `teacher_id` in ONE tx; per date the slot check through the same predicate (`409 SLOT_TAKEN` naming the date, nothing moved); the family notice per seat via the existing teacher-change notice (`moveBooking`'s), the coach notice once per date. Under `calendar.booking-edit`.
- **Cancel a GROUP row** (`PATCH /bookings/:id/status { action: "cancel" }`) ⇒ every live seat goes through the existing teacher-cancel path (make-up per child by `reconcileCoursePlan`; a single-session seat its ordinary cancel), then the group row itself. In one tx. (Owner §4.2 pending — built as recommended.)
- **Leave on a seat** ⇒ ordinary; the make-up appends SOLO (ordinary placement — `findFreeExtensionDate` sees the group row as a holder through the one predicate, so it never lands on the group's slot). 🚫 `reconcileCoursePlan` unchanged beyond seats holding no slot.
- **`PATCH /bookings/:id/other`** accepts GROUP rows; the 400 sentence becomes "อื่นๆ / กลุ่ม".
- **DTO:** a GROUP row: `group: { key, kind, name, seatCap, seats: [{ bookingId, studentId, studentName, status, courseId }], teacherRates, ratePostedAt }` (`other` stays null on it); a seat row: `groupId`, `groupName`. **The calendar GRID loader filters `group_id IS NULL`** (the ONE grid read; every other read keeps seats). `kind` derives from `head_count === 2 ? "DUO" : "GROUP"`? — ❓ no: store it — `other_kind` reuses the Stage 1 column with `DUO` / `GROUP` added to the code list (`OTHER_KINDS` grows by two; a GROUP row's kind is one of those two, an OTHER's one of the three — pinned). Say if you'd rather a separate column.
- **Reminder:** as §1.4 — the coach's GROUP block = name · `Seats : n/cap` · the children's lines beneath (name + their `remaining`, the existing shapes); seat rows dropped from the TEACHER grouping only; the parent's entry for a seat unchanged.
- **Guards:** `assertStudentActive` + the suspension gate apply to a seat exactly as to any session (they are in `createCoursePackage` / `insertBooking` already — nothing new).
- **❓1 `group_key` column vs a `booking_groups` table:** the column, as you propose — the name/kind/cap ARE per date (the owner's "editable rate per date", §4.5, argues the same for the rest), and the one-row-per-date shape keeps every existing read honest. A table would be a second place for a name that the group row's `other_title` already carries.
- **❓2 extend vs 409:** EXTEND (above) — a course sold into a group that runs shorter than the course is the common case (a 10 Hr course into a group planned for 8 weeks); a 409 would send staff to create rows by hand first.

## For @Fern (draft, final after confirm)
`POST /api/bookings/group-series` (Stage 1 series body + `name`, `groupKind`, `seatCap`) ⇒ `201 { groupKey, created, bookingIds }` · `POST /api/courses { …, groupKey }` (teacher/weekday/startTime prefilled from the group; `409 GROUP_FULL` / `409 SLOT_TAKEN` naming the date; `400 GROUP_MISMATCH`) · `PATCH /api/bookings/:id/group-teacher { teacherId, fromHereOn }` · booking DTO `group {…}` on a GROUP row, `groupId`/`groupName` on a seat; seats absent from the grid; key `action:calendar.group-series`.

⛔ Waiting on your confirm (the PAUSED-drift fold · `other_kind` reused for DUO/GROUP · the four-statement migration shape) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-18) — build to your §2 as written, with three notes
1. **The PAUSED drift fold — YES.** `SLOT_NON_BLOCKING` was the second definition the header of `booking-slot.ts` itself forbids; retire it, one `lib/slot-holder.ts`, the five sites by source. Name it in the report as a **pre-existing defect fixed in passing** (the picker showed a PAUSED booking as BOOKED since TASK-260) — I am telling Porter now so Tanya can check it on `sid`: a PAUSED session's slot must show free in the picker.
2. **`other_kind` reused for `DUO`/`GROUP` — YES**, with the pin you named made explicit: `booking_type = GROUP ⇔ other_kind ∈ {DUO, GROUP}` and `OTHER ⇔ {ECA, FREE, KOL}` — `assertKindForType` (or the equivalent) refuses the cross, by value both ways. No separate column.
3. **The four-statement migration — YES.** Your reading of the enum split is right (nothing in `0041` uses the label; the preflight splits nothing today). Witness = the predicate change. One addition: a line in `0041`'s header stating that `0042+` must never use `'GROUP'` in the same run without the preflight's split — the `0029` TRAP 1 sentence, verbatim.
Everything else stands: extend on a missing date; the column not a table; cancel cascade as recommended (owner §4.2 pending — if he reverses, it is one branch). **Go.**

---

# 📤 REPORT — @Jason → @Sober (2026-09-18)

✅ **DONE (code), against the confirmed contract and your three notes.** **2466 pass / 0 fail**, **192 files** (+1 new, −1 retired) · `tsc --noEmit` clean · **MIGRATION `0041_group_session` → 42 = 42** · **the preflight scanner run offline on the WHOLE cutover batch `0038 → 0041`: no blocker, no split** · witness registered (`index-predicate`).

## ⚠️ Two things that moved while building — for you BEFORE Fern wires (your instruction)
1. **The swap sends NO notice.** The contract said "the family notice per seat via the existing teacher-change notice, as `moveBooking` sends today" — **`moveBooking` sends nothing today** (I read it: it re-times, re-checks the teacher, reconciles the holds; no `enqueueLine`). There is no existing notice to reuse; a family/coach notice for a teacher swap is a NEW message and needs the owner's words through @Porter. Built: the swap moves the rows silently, pinned `not.toMatch(/enqueueLine|notify/)`. If a notice is wanted, it is a PLACEHOLDER task like `Heads`/`Seats` — say.
2. **A SEAT draws no freelance hour — a money rule the contract did not name.** `reconcileBookingHolds` draws one freelance hour per CONFIRMED booking; a group row + two seats would have drawn THREE hours off a freelance ceiling for one taught hour. Built: the helper returns before the ledger read when the row has a `groupId` (the same shape as TASK-224's OTHER rule, in the ONE function); the GROUP row draws like any lesson. Pinned; the mutation that removes it bites. Say if the owner wants it otherwise (I see no reading where three draws is right).

## The migration — `drizzle/0041_group_session.sql` (four statements, the `0029` + `0033` shapes)
`ALTER TYPE booking_type ADD VALUE IF NOT EXISTS 'GROUP'` (alone; the label appears ONCE in the file — pinned) · `bookings.group_key uuid` · `bookings.group_id uuid REFERENCES bookings(id) ON DELETE RESTRICT` + `bookings_group_id_idx` · **`DROP INDEX` + `CREATE UNIQUE INDEX bookings_teacher_slot_uq … WHERE status NOT IN (the four) AND group_id IS NULL`** — the LAST statement. 🔒 The header: the rebuild's **ACCESS EXCLUSIVE on `bookings`, reads AND writes, a full scan — CLOSED SHOP** (the `0033` sentences verbatim, not CONCURRENTLY, the queueing hazard); statements 1–3 named per lock; **TRAP 1 for `0042+`** (your note 3): never use `'GROUP'` in the same run without the preflight's split; the cutover order `0038 → 0039 → 0040 → 0041`, verify 42. **Witness = the PREDICATE** (`index-predicate`, contains `group_id`; the why names the `0022` blindness) — the existence-probe mutation bites.

## What was built
- **`lib/slot-holder.ts` — the ONE predicate** (`slotHolderWhere(b)` = `status NOT IN (SLOT_INACTIVE) AND group_id IS NULL`; `holdsSlot(row)` pure) **across the five mirrors**: the index (schema `.where` + `group_id` index), `describeSlotClash`, `assertAdditionalTeacherFree`, `findFreeExtensionDate` (make-up / extension placement), `getSlotAvailability`. **`lib/booking-slot.ts` (`SLOT_NON_BLOCKING`) RETIRED with its test** — 🔴 **a pre-existing defect fixed in passing (your note 1):** the availability picker read that second list (three statuses, no PAUSED, from before TASK-260), so a PAUSED booking showed as BOOKED in the picker while the index and the other three reads freed the slot. Pinned by value (`holdsSlot({ status: "PAUSED" })` = false) and by source (no availability read restates a status list; `SLOT_NON_BLOCKING` gone).
- **`lib/other-kind.ts`** — `GROUP_KINDS` (DUO · GROUP) in the same `other_kind` column (your note 2); **`assertKindForType(bookingType, kind)`** refuses the cross both ways (GROUP needs DUO|GROUP; OTHER takes ECA|FREE|KOL or none; a lesson none) — called in `insertBooking` on EVERY insert and in the edit; by value both ways.
- **`validation.ts`** — `groupSeries` (name · kind · `seatCap` 2..12, DUO ⇒ exactly 2 · the Stage 1 series body) · `groupTeacherSwap { teacherId, fromHereOn }` · `createCoursePackage.groupKey?`. `POST /bookings` cannot make a GROUP row (its enum has no GROUP — only the series does).
- **`scheduler.service.ts`** — `insertBooking` writes `groupKey` / `groupId`; **`createGroupSeries`** (one uuid key, sorted dates, ONE tx, `insertBooking` per date as GROUP with `other_title` = name, `head_count` = cap, `other_kind` = kind, rates on the Stage 1 columns, the extras; first clash ⇒ `409 SLOT_TAKEN` naming the date, nothing created) ⇒ `201 { groupKey, created, bookingIds }`. **`createCoursePackage { groupKey }`**: `assertCourseMatchesGroup` BEFORE the tx (teacher · weekday · start must equal the group's first live row ⇒ else `400 "คอร์สต้องใช้ครู/วัน/เวลาเดียวกับกลุ่ม"`); each non-absent planned session takes a seat via **`seatOnGroup(tx, key, date)`**: the group row of that date (**cap**: live seats < `head_count` in the same tx, else `409 GROUP_FULL "วันที่ <date> กลุ่มเต็ม (n/cap)"`) or — a missing date — **the series EXTENDED** by one GROUP row through `insertBooking` with the template's teacher/extras/rates/cap/kind (`409 SLOT_TAKEN "วันที่ <date> ครูไม่ว่าง — ขยายกลุ่มไม่ได้ (…)"`); the seat is inserted with `groupId` (outside the index by the predicate); everything after the inserts untouched. **`swapGroupTeacher(id, { teacherId, fromHereOn })`**: GROUP rows of the key from this date on (or this one), ONE tx, `assertTeacherBookable` per date, the group row moves (23505 ⇒ `409 SLOT_TAKEN` naming the date, nothing moved), its holds reconciled, **every live seat of that date follows** ⇒ `{ moved, booking }`. **The cancel cascade**: in `updateBookingStatus`'s cancel branch, a GROUP row first runs `cancelSeatsOfGroup` (each live seat ⇒ CANCELLED + the note + **its course's make-up via `reconcileCoursePlan`**, in the same tx; no per-seat coach notice — the group row's own cancel tells the coach once), then the group row; GROUP joins `REASON_ENUM_REQUIRED` (audited like an OTHER). `reconcileBookingHolds`: **a seat draws nothing** (above). `editOtherBooking` accepts GROUP rows (the 400 sentence says อื่นๆ / กลุ่ม; the kind pin applies). The grid's ONE read hides seats (`isNull(b.groupId)`); `withBookingRelations` carries `seats` (+ student) and `group`.
- **`db/mappers.ts`** — a GROUP row: **`group: { key, kind, name, seatCap, seats: [{ bookingId, studentId, studentName, status, courseId }], teacherRates, ratePostedAt }`** (`other` stays null on it); a seat: `groupId`, `groupName`; a lesson: none.
- **Routes + key** — `POST /bookings/group-series` ⇒ 201 under **`action:calendar.group-series`** (50th; TH `สร้างกลุ่ม DUO/Group เป็นชุด` / EN `Create a DUO/Group series`) · `PATCH /bookings/:id/group-teacher` under `calendar.booking-edit`. Both classified `unrelated` in the ended-course table with the reason.
- **The coach's reminder** — the job loads a GROUP row's seats (+ student + course) in the SAME query and maps `seats` (filtered by the reminder's own exported `REMINDABLE` — no second list in the job, which keeps REQ-094's byte-freeze of `"EXTENDED"` intact); `groupReminders` builds NO teacher entry for a seat row (its group carries it) and keeps the PARENT entry unchanged; the renderer prints **`Seats : n/cap` + one line per child** (name, their balance) before `Remark`; the label `ob_f_seats` is a 📖 PLACEHOLDER (by form). A GROUP entry never prints `Heads`.
- 🔻 **Pins moved, with reasons:** the `41 = 41` pins → `42 = 42` (ten files; 0040's "last" pins by index / tag); the key count 49 → 50, the mutate-route floor 63 → 65; the four slot-mirror source pins (TASK-239 / TASK-260 / TASK-092 files) now assert `slotHolderWhere(b)` and NO restated list; the cancel-reason set (+ GROUP); the shared-relation-set pin (+ `seats`, `group`); `booking-slot.test.ts` retired with its file (2 tests).

## Pinned (`lib/group-session-req095-2a.test.ts`, 25 tests)
- **The migration (source):** 42 = 42, the order 0038 → 0041 by tag · the six statements in order, the rebuild last with the four statuses + `group_id IS NULL` · **the label appears once** (TRAP 1) · the header's closed-shop / not-CONCURRENTLY / hazard / TRAP-1-for-0042+ / cutover / predicate-witness sentences · the witness entry · the schema mirrors (enum, columns, `.where`, the `group_id` index, the self-relation).
- **The ONE predicate:** `holdsSlot` by value incl. a seat and PAUSED (the drift) · `slotHolderWhere` rendered through `PgDialect` (`"status" not in (…)`, `"group_id" is null`, the four params) · **the five mirrors by source, no restated list, `booking-slot.ts` gone.**
- **type ⇔ kind:** by value both ways; `insertBooking` asks it; the validators (DUO cap, 2..12, dates, extras, swap shape, `groupKey`, no direct GROUP create).
- **The writes (source):** the series (rates first, one key, sorted, ONE tx, through `insertBooking`, the 409 sentence) · the course-into-group (match before the tx, the seat with `groupId`, the sentence) · `seatOnGroup` (**the extend branch reached — mutation K passed until this**, the template carried, the cap sentence, the extend 409) · the swap (GROUP only, the target rule, ONE tx, bookable per date, the 409, holds, **every seat follows**, no notice) · the cascade (before the group's own write, GROUP in the audited set, each seat's status + make-up, no per-seat notice) · **a seat draws nothing** (before the ledger read) · **no money** (the sweep's list has no GROUP — the one `"GROUP"` in the job is the seats mapper; none of the four new functions post; `ratePostedAt` never written) · the grid hides seats; the shared set.
- **The DTO by value:** a GROUP row's `group` with two seats (a nickname, a name-only child), `other` null; a seat's `groupId`/`groupName`; a lesson's nulls.
- **The reminder:** `Seats : 2/2` + the two child lines before `Remark`, no `Heads` on a group · `groupReminders`: the seat builds no teacher entry, the group entry carries `seats`, the parent entry survives · the job's query + mapper + the shared `REMINDABLE` + the placeholder comment.
- **The routes through the ROOT app:** the series 201 by value, the 409 naming the date by value, DUO/cap-3 ⇒ 400 · the swap `{ moved, booking }`, a lesson's 400, the date 409, a missing `fromHereOn` ⇒ 400 · the key gates exactly the series; the swap is a `booking-edit`.

## 🔑 Mutation — twenty-four, `finally`, checksum — all bite
A the predicate forgets `group_id IS NULL` · B the pure mirror ignores the seat · C the picker back on its own list (the PAUSED drift returns) · D the placement leaves the one predicate · E the schema's index predicate drifts from 0041 · F the migration USES the label (a CHECK naming 'GROUP') · G the witness is the index's EXISTENCE · H a GROUP accepts ECA · I the series bypasses `insertBooking` · J the cap dropped · **K a missing date refused instead of extended — PASSED FIRST** (my pin asserted the extend branch's lines existed, not that they were reached; now: no `throw` before `if (!row) {`, no `GROUP_NO_DATE`/`if (false)`) · L the mismatch corrected silently · M the seat inserted without `groupId` · N the swap forgets the seats · O the swap not one tx · P no cascade · Q the cascade re-owes no make-up · R a seat draws a freelance hour · S the grid shows seats · T the seats lose the student name · U a seat is its own coach entry · V the series under the OTHER series' key · W GROUP leaves the audited set · X GROUP joins the revenue sweep. Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final; TASK-398's copy from my §2 holds, with the two notes above
`POST /api/bookings/group-series { name, groupKind: "DUO"|"GROUP", seatCap, teacherId, additionalTeacherIds?, teacherRates?, startTime, dates[] }` ⇒ `201 { groupKey, created, bookingIds }` | `409 SLOT_TAKEN` naming the date (nothing created) · `POST /api/courses { …, groupKey }` (teacher/weekday/startTime prefilled + locked from the group; a stale body ⇒ `400 "คอร์สต้องใช้ครู/วัน/เวลาเดียวกับกลุ่ม"`; `409 GROUP_FULL "วันที่ … กลุ่มเต็ม (n/cap)"`; `409 SLOT_TAKEN "วันที่ … ครูไม่ว่าง — ขยายกลุ่มไม่ได้ (…)"` when the extension clashes) · `PATCH /api/bookings/:id/group-teacher { teacherId, fromHereOn }` ⇒ `{ moved, booking }` | `409 SLOT_TAKEN` naming the date | `400` on a non-group — **no notice is sent** · the cancel on a GROUP row is the ordinary cancel route with a `reasonCode` (GROUP is audited like OTHER); it cascades · booking DTO: a GROUP row `group: { key, kind, name, seatCap, seats: [{ bookingId, studentId, studentName, status, courseId }], teacherRates, ratePostedAt }` (`displayName` = the name; `other` null); a seat `groupId`, `groupName`; **seats are absent from the grid**, present everywhere else · `PATCH /bookings/:id/other` works on a GROUP row (kind DUO|GROUP, head count = cap, rates) · keys: `action:calendar.group-series` (in `/permissions`), the swap and the edit under `calendar.booking-edit`.
📦 **After the migration:** nothing to set — no GROUP rows exist; every existing booking has `group_id = NULL` and behaves exactly as before (the predicate's new clause is vacuously true). The cutover = `0036 … 0041` in one `db:migrate` (preflight: no split), verify 42.
⚠ **Owner's list (via you):** the swap notice (words, if wanted); the `Seats` label word; SPEC-081 §4's five stand.
