# TASK-401 — Balance camp: weeks · day packages · redeem · per-day status · the day cut · the sale, BE (`REQ-095` Stage 3a, `SPEC-082`) — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-19) · **Size L** · migration `0042_camp` (43 = 43) · `sale:ensure-items` +4. `sid`; a later `uat` deploy. Starts AFTER TASK-399's contract is confirmed (399 is S — do it first).

## §0 Read `SPEC-082` §1–§2 first. Owner's rulings (REQ-095 §8): half-day UNITS (half 1 · full 2, one currency); camp is INFORMATIONAL on teacher slots in 3a (no block); a no-show is CONSUMED; the four products (VAT-incl): **Full-day/Full-week 11,500 · Half-day/Full-week 5,900 · Full-day/Daily 2,600 · Half-day/Daily 1,300**; **Early bird 10,500 = a DISCOUNT at sale through the existing discount mechanism (reason "early bird, 1 month ahead"), NOT a product.** Assumed as recommended (not yet ruled — Porter flagged): capacity = a nullable number per week (none = unlimited); revenue = the one sale at purchase, no per-day posting; no credit ↔ hours conversion.

## §1 Verify before the contract
1. The sale post for a course (`createCoursePackage` → `postBookingSale`/`recordSale` with `validateSaleDiscount`) — which parts are reusable as-is for a package that is not a course (item ref + discount + the `bo.movement` row), and which are course-shaped (session rows, expiry).
2. The end-of-day tx (`jobs.service.ts`): where a second sweep (camp days) sits and how `job_runs` counters extend (the audit pin says `autoAttended`, TASK-180).
3. `assertStudentActive` (REQ-093) + suspension — apply to the camp sale and to redeeming.
4. The RBAC registries (`lib/permissions.ts` `menu:*` 12 → 13, `action:*` 50 → 54; `lib/route-access.ts` the new area) — the FE pins the snapshot equal.

## §2 The contract I propose — confirm/correct before code
- **Migration `0042_camp`** (three tables, no enum, nullable FKs; catalog-only — no hot-table touch): `camp_weeks { id, name, start_date, end_date, capacity int null, teacher_ids uuid[] null, status text OPEN|CLOSED (code list), opened_by, opened_at, closed_at }` · `camp_packages { id, student_id FK, kind FULL|HALF, plan FULL_WEEK|DAILY, total_units int, used_units int default 0, sale_id text null, discount_* (the booking's four columns' shape), note, created_by, created_at }` — **no expiry column** (pinned by absence) · `camp_days { id, camp_package_id FK, camp_week_id FK, date, half AM|PM|FULL, units int, status PLANNED|ATTENDED|ABSENT|CANCELLED, marked_by, marked_at, UNIQUE (camp_package_id, date) }` + index on `(camp_week_id, date)`. Witness = `camp_days` (the last object).
- **Items:** `camp-full-week` 11,500 · `camp-half-week` 5,900 · `camp-full-day` 2,600 · `camp-half-day` 1,300 in `sale-items.ts` as a small `CAMP_CARD` (one authority; `campPriceList()` for the FE like the rental/voucher lists); `sale:ensure-items` adds them.
- **Keys:** `menu:camp` (13th) · `action:camp.week-open` · `action:camp.sell` · `action:camp.redeem` · `action:camp.day-mark` (51st–54th), TH/EN labels; `route-access.ts` maps `/camp/*` to the menu and each write to its action.
- **Routes (`/api/camp`):** `GET /weeks?from&to` · `POST /weeks` · `PATCH /weeks/:id { name?, capacity?, teacherIds?, status? }` · `GET /weeks/:id/days` (the roster: per date, the children with kind/half/status) · `POST /packages { studentId, kind, plan, days?, discount?, firstWeek?: { weekId, dates[], half } }` ⇒ units = (plan FULL_WEEK ? 5 : days) × (kind FULL ? 2 : 1); the sale posts once (item by kind×plan × `days` for DAILY; discount via `validateSaleDiscount` — early bird is a discount with a reason) ⇒ `201 { package, planned }` · `GET /packages?studentId` (credit = total − used − planned) · **`POST /packages/:id/days { weekId, dates[], half }`** — the ONE writer of PLANNED rows (the sale's `firstWeek` calls it): every date inside the OPEN week, capacity per date, units ≤ credit ⇒ all-or-nothing, `409 CAMP_FULL` / `409 CAMP_NO_CREDIT` / `400 CAMP_WEEK_CLOSED` naming the date · `PATCH /days/:id { status: ATTENDED|ABSENT|CANCELLED }` — ATTENDED/ABSENT consume (`used += units`, a no-show is consumed by the ruling), CANCELLED before the day releases; a status may be corrected the same day (the check-in-correction precedent).
- **The day cut:** in `runEndOfDayJob`'s tx, today's `PLANNED` camp days whose week's date is today ⇒ ATTENDED, `used += units` (past dates: all PLANNED; future: none — the same three-branch shape as bookings, start-based = the day has begun); counters `campDaysAutoAttended`.
- **Reads for the FE:** the student detail gains `camp: { packages: [{ id, kind, plan, totalUnits, usedUnits, plannedUnits, credit }] }`; the calendar's day loader gains `campWeeks: [{ id, name, dates, count }]` for the banner (no bookings rows).
- 🚫 No expiry anywhere; no per-day revenue; no teacher-slot block; no LINE messages (3b).
- ❓ for you: (1) `discount_*` on `camp_packages` vs reusing a shared discount table — I propose the four columns (the booking precedent, `safeStoredDiscount`); (2) whether ABSENT and ATTENDED should be one status ("consumed") or two with the same effect — I propose two (the label is a fact; the effect is one function).

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · **43 = 43** · the items by value · keys 13/54 with the FE snapshot in mind · the flows by value (units arithmetic incl. a FULL package spending 1 unit on a half day; redeem 409s naming the date; the day cut three branches; no-show consumed; cancel releases) · no expiry by absence · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · `sale:ensure-items` dry-run in the report · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-19) — §1 verified, §2 confirmed with corrections; no code until you confirm

## §1 What I found
1. **The reusable sale shape is the VOUCHER's, not the course's.** `createVoucher` (`scheduler.service.ts:2500–2535`) = `validateSaleDiscount(input.discount, listPriceMinor(item), actor)` BEFORE any write → the row in a tx → `void recordSale(item, 1, { refId, idempotencyKey: "voucher-sale:<id>", discount })` after the tx (best-effort, the ledger row is the money; `sale:ensure-items` seeds the item). A camp package is exactly that — a prepaid bucket — with `quantity = days` for a DAILY plan. The course's parts (session rows, the reconcile, expiry, the confirm message, the freelance draw) have no camp counterpart and are NOT reused. `assertMayDiscount(body.discount, c.get("user"))` sits at the four sale routes — the camp sale is a FIFTH site (early bird is a discount ⇒ `action:sales.discount` gates it like every other discount; pinned by count 4 → 5).
2. **The end-of-day tx** — `runEndOfDayJob`'s `marked = db.transaction(…)` returns `{ autoAttended, coursesAutoAttended, vouchersAutoAttended }`; a second sweep (camp days) is one more block INSIDE that tx before the return, its counter `campDaysAutoAttended` added to the object — the TASK-180 audit pin (`autoAttended` present, no `noShow`/`coursesCut`) holds; REQ-094's byte-frozen select line is untouched (a separate select on `camp_days`); TASK-396's start-gate is untouched. A camp day has no start time — **"the day has started" ⇔ `date <= today`**: the three branches collapse to one predicate for days (past: all PLANNED; today: all PLANNED; future: none).
3. **Guards** — `assertStudentActive` (REQ-093) and `assertHouseholdNotSuspended` (TASK-058) are exported service helpers; the camp sale calls both (the voucher's order), the redeem calls both too (a suspended household plans no days; an archived child redeems nothing). Pinned by source.
4. **The registries** — `MENU_KEYS` 12 → 13 (`menu:camp`), `ACTION_AREAS` + `camp` (a menu tail — the area rule holds), `ACTION_REGISTRY` 50 → 54, `ROUTE_ACCESS` + the `/camp/*` rows (reads → `menu:camp`, each write → its action); the Stage 2/3 enumeration pins move (13 / 54 / the mutate floor 65 → 70); `GET /permissions` hands the FE the new labels — the FE snapshot pin reads the same registry.

## §2 The contract — confirmed with corrections
- **Migration `0042_camp`** (43 = 43): three tables, every status/kind/plan/half a TEXT code list (`lib/camp.ts`), no enum — the preflight splits nothing; **witness = the LAST object `camp_days_package_date_uq`** (a unique index over a table this file invents — 0034's two rules; not the table). `camp_weeks { id, name, start_date, end_date, capacity int null, teacher_ids uuid[] null, status text, opened_by, opened_at, closed_at, created_at }` · `camp_packages { id, student_id FK students RESTRICT, kind, plan, total_units, used_units default 0, sale_id text null, discount_kind/value/reason/actor (the booking's four — ❓1 yes), note, created_by, created_at }` — **no expiry column** (pinned by absence in the SQL, the schema and the DTO) · `camp_days { id, camp_package_id FK RESTRICT, camp_week_id FK RESTRICT, date, half, units, status, marked_by, marked_at, created_at }` + `camp_days_week_date_idx (camp_week_id, date)` + the UNIQUE. 🔒 The header: three NEW tables lock nothing existing except the FK statement's **SHARE ROW EXCLUSIVE on `students` for its duration** (the 0035 shape: reads continue, writes to `students` wait — milliseconds, no scan); no hot table touched; one run, one transaction, all `IF NOT EXISTS`; the cutover order `0038 → 0042`, verify 43.
- **Items** (`sale-items.ts`): a small `CAMP_CARD` = `camp-full-week` 11,500 · `camp-half-week` 5,900 · `camp-full-day` 2,600 · `camp-half-day` 1,300 (VAT-incl.); `campItemRef(kind, plan)`; `campPriceList()` for the FE; the four seeds join `SALE_ITEMS` through their own map (the shape count 18 → 22 program-ish… I'll pin the four separately); `sale:ensure-items --dry-run` output in the report.
- **Units** (`lib/camp.ts`, pure): `unitsPerDay(half) = half === "FULL" ? 2 : 1`; a package's `total_units = (plan === "FULL_WEEK" ? 5 : days) × (kind === "FULL" ? 2 : 1)` — ❓ **FULL_WEEK = 5 days** (the owner's "week"; a week CONTAINER may span 1–7 dates but the product is five days — confirm); `credit = total − used − planned` (planned = PLANNED rows' units). ONE currency: a HALF package may plan a FULL day (2 units) if it has the credit, a FULL package a half (1 unit) — pinned by value.
- **Keys**: `menu:camp` — ❓ nav position: **after `menu:badges`, before `menu:som`** (the operational menus, before the reports) — say if elsewhere; `action:camp.week-open` (TH `เปิด/แก้ไข/ปิดสัปดาห์แคมป์` / EN `Open, edit & close a camp week` — open/PATCH/close are one act) · `action:camp.sell` (`ขายแคมป์` / `Sell a camp package`) · `action:camp.redeem` (`ใช้เครดิตแคมป์` / `Redeem camp days`) · `action:camp.day-mark` (`บันทึกสถานะวันแคมป์` / `Mark a camp day`); area `camp`.
- **Routes (`/api/camp`, `routes/camp.ts`, service `camp.service.ts`):**
  · `GET /weeks?from&to` ⇒ `{ weeks: [{ id, name, startDate, endDate, capacity, teacherIds, status, dayCounts: { [date]: n } }] }` (weeks overlapping the range; the counts = PLANNED+ATTENDED per date) · `POST /weeks { name, startDate, endDate, capacity?, teacherIds? }` ⇒ 201 (1–7 consecutive days, end ≥ start; `400` otherwise) · `PATCH /weeks/:id { name?, capacity?, teacherIds?, status? }` (CLOSED ⇒ no new planning; existing days untouched).
  · `GET /weeks/:id/days` ⇒ `{ week, days: [{ date, entries: [{ dayId, packageId, studentId, studentName, kind, half, units, status }], count, capacity }] }`.
  · `POST /packages { studentId, kind, plan, days?, discount?, firstWeek?: { weekId, dates[], half } }` ⇒ `201 { package, planned: n }` — `days` required for DAILY (1..30), refused for FULL_WEEK; the guards; the discount validated against the LINE total (`qty × list`) BEFORE any write, `assertMayDiscount` at the route; the package + its first-week PLANNED rows through the ONE redeem writer in one tx; the sale posts once after the tx: `recordSale(campItemRef(kind, plan), plan === "DAILY" ? days : 1, { refId: package.id, idempotencyKey: "camp-sale:<id>", discount })`; the discount stored in the four columns. **Early bird = `{ kind: "BAHT", value: 1000, reason: "early bird, 1 month ahead" }` typed by staff — no product, no date rule in code** (the owner's "1 month ahead" is the reason text; ❓ say if you want the BE to REFUSE early bird when the first week is < 1 month away).
  · `GET /packages?studentId` ⇒ `{ packages: [{ id, kind, plan, totalUnits, usedUnits, plannedUnits, credit, days: [{ dayId, weekId, weekName, date, half, units, status }], discount, createdAt }] }` — the student's Camp card reads THIS (no change to the parent detail — ❓ confirm, or say the card rides `GET /parents/:id`).
  · **`POST /packages/:id/days { weekId, dates[], half }`** — the ONE writer for planned days: the week OPEN (`409 CAMP_WEEK_CLOSED`) and each date inside it (`400` naming the date); per date: capacity (PLANNED+ATTENDED on that week+date `< capacity`, null = unlimited ⇒ `409 CAMP_FULL "วันที่ … เต็ม (n/cap)"`), the UNIQUE (`409 CAMP_DAY_TAKEN "วันที่ … มีวันแคมป์อยู่แล้ว"`), and the credit (`units ≤ credit` running down the list ⇒ `409 CAMP_NO_CREDIT "วันที่ … เครดิตไม่พอ (เหลือ n หน่วย)"`); all or nothing; ⇒ `201 { planned: n, package }`.
  · **`PATCH /days/:id { status: "ATTENDED" | "ABSENT" | "CANCELLED" }`** — from PLANNED only (3a): ATTENDED and ABSENT both CONSUME (`used += units` — ❓2 two statuses, one effect: yes); CANCELLED RELEASES (`used` untouched; the row stays as history) — **refused once the day has started** (`date <= today` ⇒ `409 CAMP_DAY_STARTED "วันแคมป์เริ่มแล้ว — บันทึกขาดแทน"`): the owner's §4.4 (before the day ⇒ back; on the day ⇒ consumed). A transition FROM ATTENDED/ABSENT (an undo) is 3b — say if 3a needs it.
- **The day cut** (`runEndOfDayJob`, the same tx): `camp_days` with `status = PLANNED AND date <= runDate` ⇒ ATTENDED + `used += units` per package; `campDaysAutoAttended` in `job_runs` (the audit pin holds). No message (3b).
- **The calendar banner:** `GET /calendar`'s payload gains `campWeeks: [{ id, name, startDate, endDate, dayCounts }]` for weeks overlapping the range — no booking rows, no hour cells, no slot block (informational, §0).
- 🚫 No expiry anywhere (by absence); no per-day revenue (by absence — no `recordSale` outside the sale); no teacher-slot block; no LINE; no credit ↔ hours.

## For @Fern (draft, final after confirm)
`/api/camp/weeks` (GET · POST · PATCH), `/api/camp/weeks/:id/days`, `/api/camp/packages` (GET ?studentId · POST), `/api/camp/packages/:id/days` (POST), `/api/camp/days/:id` (PATCH); `campPriceList()` via `GET /sellable-packages`-style read (❓ or its own `GET /camp/prices` — I lean its own); the calendar's `campWeeks`; `menu:camp` + the four actions in `/permissions`.

⛔ Waiting on your confirm (FULL_WEEK = 5 · the nav position · the early-bird date rule · the Camp card's read · the prices read · the 3a transitions) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-19) — build to your §2; the six answers
1. **FULL_WEEK = 5 days — YES** (the product; the container may span 1–7 dates).
2. **Nav: after `menu:badges`, before `menu:som` — YES.**
3. **Early bird: NO date rule in code.** The reason text is the rule; staff type the discount. If the owner later wants it refused under one month, it is one guard — on the owner's list, not built.
4. **The Camp card reads `GET /camp/packages?studentId` — YES**; the parent detail is untouched.
5. **Prices: its own `GET /camp/prices` — YES** (`campPriceList()`); do not overload `/sellable-packages`.
6. **3a transitions:** from PLANNED as you wrote; plus **ATTENDED ↔ ABSENT same day** (both consume — a label correction with no credit effect, the check-in-correction precedent). An undo back to PLANNED is 3b.
❓1 the four discount columns — yes. ❓2 two statuses, one effect — yes. The `SHARE ROW EXCLUSIVE on students` sentence in the header — yes, the 0035 shape. **Go.**

---

# 📤 REPORT — @Jason → @Sober (2026-09-19)

✅ **DONE (code), against the confirmed contract and your six answers.** **2499 pass / 0 fail**, **194 files** (+1) · `tsc --noEmit` clean · 🔴 **migration `0042_camp` — 43 = 43** (files = journal tags) · offline preflight: `0042` alone `[]`, the batch `0038 → 0042` `[]` (nothing refused, no enum to split).

## What was built
- **`drizzle/0042_camp.sql`** (7 statements, every object `IF NOT EXISTS`): `camp_weeks` (+ dates idx) · `camp_packages` (`student_id` FK RESTRICT; `kind`/`plan` TEXT; `total_units`/`used_units`; `sale_id`; the four discount columns; `note`; `created_by/at` — **no expiry column, by structure**) (+ student idx) · `camp_days` (`camp_package_id`/`camp_week_id` FK RESTRICT; `date`; `half`; `units`; `status` default PLANNED; `marked_by/at`) (+ week+date idx) · LAST `camp_days_package_date_uq` UNIQUE (package, date) = **the witness** (`migration-witness.ts`, kind `index`, rerunnable). Header: the 0038 → 0042 order, `db:verify` expects 43, the `SHARE ROW EXCLUSIVE on students` blink for the FK statement (the 0035 sentence), no hot table, no enum (`'GROUP'` appears in the header's TRAP-1 sentence only — the statements are clean).
- **`lib/camp.ts`** (pure): the five code lists; `FULL_WEEK_DAYS = 5` · `MAX_WEEK_DAYS = 7` · `MAX_DAILY_DAYS = 30`; `unitsPerDay(half)` (FULL 2, AM/PM 1) · `unitsPerKind` · `packageUnits(kind, plan, days?)` (FULL_WEEK ⇒ 5 × kind; DAILY ⇒ days × kind, 1..30 else 400) · `saleQuantity` · `creditOf = total − used − planned` · `consumes` (ATTENDED | ABSENT) · `assertDayTransition` (PLANNED → the three; **ATTENDED ↔ ABSENT** per your 6; CANCELLED with `date <= today` ⇒ `409 CAMP_DAY_STARTED "วันแคมป์เริ่มแล้ว — บันทึกขาดแทน"`; anything else `409 CAMP_DAY_TRANSITION` — the undo to PLANNED is 3b) · `unitsDelta` · `datesOfWeek`.
- **`lib/sale-items.ts`** — `CAMP_CARD` (`camp-full-week` 11,500 · `camp-half-week` 5,900 · `camp-full-day` 2,600 · `camp-half-day` 1,300, VAT-incl.), `campItemRef(kind, plan)`, `campPriceList()` (`{ externalRef, kind, plan, priceMinor }`), the four `CAMP_ITEMS` (`revenueKind: "CAMP"`) appended to `SALE_ITEMS`.
- **Keys** — `menu:camp` after `menu:badges` (13 menus); `action:camp.week-open` · `.sell` · `.redeem` · `.day-mark` (54 acts, area `camp`); `route-access.ts`: nine rows — every read on `menu:camp` (`GET /camp/packages` also on `menu:people` for the Camp card), every write on its own act. The Stage-2/3 enumeration tests now also read `routes/camp.ts` under the `/camp` prefix, so an unmapped camp route is caught the same way.
- **`routes/camp.ts`** (mounted `/api/camp`, behind the guard) + **`services/camp.service.ts`**: `GET /prices` · `GET /weeks?from&to` · `POST /weeks` (1–7 consecutive dates else 400) · `PATCH /weeks/:id` (name/capacity/teacherIds/status; CLOSED stamps `closedAt`) · `GET /weeks/:id/days` (the per-date roster) · `GET /packages?studentId` · `POST /packages` (**the voucher's sale shape**: `validateSaleDiscount` vs the line total BEFORE the write → `assertStudentActive` + `assertHouseholdNotSuspended` → the package row + its `firstWeek` days through the ONE writer, in one tx → `recordSale(ref, qty, { refId, idempotencyKey: "camp-sale:<id>", discount })` ONCE after it; `assertMayDiscount` at the route — the fifth site) · `POST /packages/:id/days` (both guards + **`planDays`**, the ONE inserter of `camp_days`: 404s → `409 CAMP_WEEK_CLOSED` → `400` date outside the week → per date `409 CAMP_FULL "วันที่ D เต็ม (n/cap)"` → `409 CAMP_NO_CREDIT "วันที่ D เครดิตไม่พอ (เหลือ n หน่วย)"` → 23505 ⇒ `409 CAMP_DAY_TAKEN`; the credit counts PLANNED as reserved and decrements per date inside the loop) · `PATCH /days/:id { status }` (`assertDayTransition` + `unitsDelta` on the package's `used_units`).
- **The day cut** — `cutCampDays(tx, runDate)` INSIDE the end-of-day `marked` tx (TASK-396's START ruling — a day has no start time, so `date <= runDate`): PLANNED ⇒ ATTENDED, `marked_by = "end-of-day"`, `used += units`; `marked` returns `campDaysAutoAttended` beside the three counts. The REQ-094 byte-freeze on `jobs.service.ts` still holds (`"EXTENDED"` absent).
- **The calendar banner** — `GET /calendar` gains `campWeeks: [{ id, name, startDate, endDate, status, dayCounts }]` for weeks overlapping the range (no booking rows, no slot block).
- 🚫 By absence: no expiry (SQL, schema, service all `/expir/i`-free) · no per-day revenue (`recordSale` appears once, in the sale) · no teacher-slot block · no LINE · no credit ↔ hours.
- 🔻 **Pins moved:** `42 = 42` → `43 = 43` in 14 files (the group-session file's `files[41]` / `slice(38, 42)` / witness by tag `0041_group_session` — no `at(-1)`); `MENU_KEYS` 12 → 13 with `menu:camp` after `menu:badges`; `ACTION_REGISTRY` 50 → 54 (mutate floor 65 → 70); `SALE_ITEMS` length + 4.

## 📦 The deploy step — `bun run sale:ensure-items --dry-run` (run read-only on my local dev box, whose box still lacks TASK-399's four too)
```
  ~ camp-full-week               11500 THB (VAT incl.) — WOULD be created
  ~ camp-half-week               5900 THB (VAT incl.) — WOULD be created
  ~ camp-full-day                2600 THB (VAT incl.) — WOULD be created
  ~ camp-half-day                1300 THB (VAT incl.) — WOULD be created
DRY RUN — nothing written. 8 would be created, 24 already present.   ← 8 = TASK-399's four duo + these four on my box
```
On `sid` / `uat`: `bun run db:migrate` (0038 → 0042 in journal order; `db:verify` expects **43**), then `bun run sale:ensure-items` (+4 camp, insert-only; +4 duo too if TASK-399's step has not run yet). The human runs both after the deploy. 🚫 No deploy request from me.

## Pinned (`lib/camp-req095-3a.test.ts`, 19 tests)
- **The migration by text:** 43 = 43, `0042_camp` the 43rd file / idx 42 / the last, the cutover order and "expects 43" in the header; 7 statements, the three tables in order, the UNIQUE index LAST, no `CREATE/ALTER TYPE` or `'GROUP'` in the statements, no `"bookings"`, the `students` lock sentence, "No lock on any hot table"; the witness entry by tag; **no expiry** in the `camp_packages` DDL nor the schema; the four discount columns; `units`/`status` on `camp_days`.
- **The pure rules by value:** the five lists, 5/7/30, units (a FULL package spends ONE unit on a half day: 9 left of 10), `packageUnits` incl. the 400 on 0/31/1.5, `saleQuantity`, credit with PLANNED reserved, two statuses one effect, every transition incl. `CAMP_DAY_STARTED` on today and on a past date, `datesOfWeek` across a month end.
- **The catalogue & validation:** the four prices, `campItemRef`, `revenueKind: CAMP`, `campPriceList`; `days` ⇔ DAILY, 1–7 unique dates, PLANNED refused as a mark, end ≥ start, capacity ≥ 1, an empty PATCH refused.
- **The service by source:** `tx.insert(campDays)` exactly once; the refusals in order with their messages; the credit loop; both guards on the redeem; `planDays(tx,` twice (the sale's firstWeek + the redeem); the discount validated BEFORE `db.transaction`; `recordSale` once, after the last `planDays`; the route's `assertMayDiscount`; `markDay`'s two calls; the cut's `lte(runDate)` / `end-of-day` / `used + units` / count; the cut INSIDE the `marked` tx and in its return; the byte-freeze; the calendar's `campWeeks`.
- **RBAC by value:** 13 / `menu:camp` right after badges / 54 / the four keys with labels / the nine access rows exactly / the mount line.
- **The routes through the ROOT app:** `GET /prices` ⇒ the four (no service); `POST /packages` ⇒ 201 and a DAILY without `days` ⇒ 400 before the service; `POST /packages/:id/days` ⇒ 201 `{ planned }` and the `CAMP_FULL` / `CAMP_NO_CREDIT` / `CAMP_WEEK_CLOSED` envelopes by value; `PATCH /days/:id` ⇒ the `CAMP_DAY_STARTED` envelope.

## 🔑 Mutation — twenty-one, `finally`, checksum — all bite, none passed first
A a full day costs 1 unit · B the week = 7 · C PLANNED does not reserve · D ABSENT releases · E CANCELLED on the day · F undo to PLANNED · G DAILY accepts 31 · H the credit check dropped · I the capacity check dropped · J a CLOSED week accepts · K the redeem skips the guards · L the discount validated inside the tx · M the cut consumes nothing · N the cut takes tomorrow (`lt`) · O the cut dropped from the day-end tx · P a price off the card · Q the sale on the read menu · R `menu:camp` renamed · S an expiry column · T the unique index made non-unique · U the discount gate dropped. Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final
- **Prices:** `GET /api/camp/prices` ⇒ `{ items: [{ externalRef, kind: "FULL" | "HALF", plan: "FULL_WEEK" | "DAILY", priceMinor }] }` (VAT-incl. satang; 11,500 / 5,900 / 2,600 / 1,300).
- **Weeks:** `GET /api/camp/weeks?from&to` ⇒ `{ weeks: [{ id, name, startDate, endDate, capacity, teacherIds, status: "OPEN" | "CLOSED", openedBy, openedAt, closedAt, dates: [ISO…], dayCounts: { [date]: n } }] }` (n = PLANNED + ATTENDED + ABSENT). `POST /api/camp/weeks { name, startDate, endDate, capacity?, teacherIds? }` ⇒ `201 { week }` (1–7 consecutive dates). `PATCH /api/camp/weeks/:id { name? | capacity? | teacherIds? | status? }` ⇒ `{ week }`. `GET /api/camp/weeks/:id/days` ⇒ `{ week, days: [{ date, entries: [{ dayId, packageId, studentId, studentName, kind, half, units, status }], count, capacity }] }`.
- **Packages:** `GET /api/camp/packages?studentId` ⇒ `{ packages: [{ id, studentId, kind, plan, totalUnits, usedUnits, plannedUnits, credit, saleId, note, discount: { kind, value, reason, actor } | null, days: [{ dayId, weekId, weekName, date, half: "AM" | "PM" | "FULL", units, status }], createdBy, createdAt }] }` — **credit is in UNITS** (a full day = 2, a half = 1): render as `Math.floor(credit / 2)` days + `credit % 2` half. `POST /api/camp/packages { studentId, kind, plan, days? (DAILY only, 1..30), discount?, note?, firstWeek?: { weekId, dates[], half } }` ⇒ `201 { package, planned }`; the discount is the sale's `{ kind, value, reason }` shape and needs `action:sales.discount`.
- **Redeem:** `POST /api/camp/packages/:id/days { weekId, dates: [1..7 unique ISO], half }` ⇒ `201 { planned, package }`. Refusals name the date: `409 CAMP_WEEK_CLOSED` · `400` (date outside the week) · `409 CAMP_FULL "วันที่ D เต็ม (n/cap)"` · `409 CAMP_NO_CREDIT "วันที่ D เครดิตไม่พอ (เหลือ n หน่วย)"` · `409 CAMP_DAY_TAKEN`; also `409 STUDENT_ARCHIVED` / the household-suspended code from the existing guards. Nothing is written on a refusal (one tx).
- **Mark:** `PATCH /api/camp/days/:id { status: "ATTENDED" | "ABSENT" | "CANCELLED" }` ⇒ `{ package }`. Allowed: PLANNED → any of the three; ATTENDED ↔ ABSENT. `409 CAMP_DAY_STARTED` (CANCELLED on/after the date — show "บันทึกขาดแทน"), `409 CAMP_DAY_TRANSITION` otherwise. The end-of-day job turns past PLANNED days into ATTENDED by itself.
- **Calendar:** `GET /calendar` payload has `campWeeks: [{ id, name, startDate, endDate, status, dayCounts }]` for the range — a day banner, no cells.
- **Keys:** `menu:camp` (nav after Badges, before Som); `action:camp.week-open` · `action:camp.sell` · `action:camp.redeem` · `action:camp.day-mark`. All errors in the `{ error: { code, message } }` envelope.

⛔ Only you mark this DONE. ▶️ Next for me: whatever waits on the board.
