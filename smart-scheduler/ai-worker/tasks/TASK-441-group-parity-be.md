# TASK-441 — `REQ-104 §2` items 1–3 (SPEC-090 §1): GROUP parity — the series service generalised to `group_key` (manage: cancel-all CASCADING to every seat with each family told · add/remove extra coach · add dates · edit name/cap/rates) · one-button confirm-whole-group · the Monday weekly coach digest (ENGLISH ONLY) — BE, M, CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-22) · **Size M.** No migration (52 = 52). No new key (the series doors' keys as REQ-101: cancel-all = key 58; confirm-whole-group = `bookings.course-confirm`). One new outbox kind (24th): `weekly_schedule_teacher`. The held batch.

## §0 Owner rulings (Porter 09-22, REQ-104 §2/§3)
1. One button confirms the whole group (per child already works underneath). 2. A weekly coach digest on Monday — **ENGLISH ONLY, no Thai line regardless of `line_lang`**: `📅 THIS WEEK'S SCHEDULE` · `Hello, here is your teaching schedule for this week:` · one line per row `Date · Time · Program/Student` · `Please review your schedule.` (REQ-104 §3 — by value). 3. The manage surface = the REQ-101 series service on `group_key`; **cancel-all cascades to every child's seat, each family told**.

## §1 Verify before the contract
1. `other-series.service.ts`: which functions are key-shape-agnostic (rows by a key column) and which assume OTHER (`bookingType = OTHER` in `seriesRows`, `templateOf`/`ratesOf` on `otherTitle/headCount`, `HUMAN_OTHER_KINDS` in the header PATCH). A GROUP row has the same columns (`other_title = name`, `other_kind = DUO|GROUP`, `head_count = cap`, `teacher_rate_minor`, `booking_teachers`) — so `seriesRows(exec, key)` becomes `seriesRows(exec, { otherSeriesKey } | { groupKey })` with `bookingType IN (OTHER, GROUP)` per the key kind; say what else branches.
2. **The seat cascade:** a GROUP row's seats = `bookings WHERE group_id = <group row id>` (course seat rows, `COURSE_PACKAGE`, each drawn from a child's course). Cancel-all on a group ⇒ each seat ⇒ CANCELLED through the SAME path the single seat cancel uses (`updateBookingStatus(id,'cancel')`'s writer — the hour returned to the course? a group seat is a course session: cancelling it does what a session cancel does today — say it: `returnsConsumedUnit`, the make-up rule), the family told with `class_cancelled_parent` per seat (the existing kind — TASK-406/SPEC-083 §3.7), the coach ONCE (`other_series_cancelled` with the group's name). Add dates on a group = a new GROUP row (no seats); add/remove extra coach = as OTHER; the primary swap = `swapGroupTeacher`'s existing shape (keep it; the series route may delegate).
3. **Confirm-whole-group:** the seats on the key's rows, grouped by course ⇒ `confirmCourse(courseId)` per course (the existing one-message-per-course), plus every GROUP row already CONFIRMED (no-op). ⇒ `POST /group-series/:key/confirm-all` (`bookings.course-confirm`) ⇒ `{ courses: n, confirmed, skipped, results[] }`.
4. **The weekly digest:** the daily reminder's teacher builder (`jobs.service.ts` 08:15, `daily_reminder` to teachers) — its select over a date; the weekly = the same select over `[monday, sunday]` grouped by teacher, sent Monday 08:15 (a new job slot beside the daily; behind a setting `weekly_teacher_digest_enabled` default ON? — I say ON, the owner asked for it; say if you prefer a flag). One outbox row per teacher with rows; a teacher with nothing that week ⇒ nothing. `line_lang` ignored for this kind (English only) — the renderer has a per-kind override precedent? (the `daily_digest` to admins? say).

## §2 The contract I propose
- **Routes (`menu:calendar`; the OTHER keys' twins):** `GET /group-series/:key` (the same DTO + `rows[].seats: [{ bookingId, studentId, displayName, status }]`) · `GET /group-series?from&to` · `POST /group-series/:key/confirm-all` (`bookings.course-confirm`) · `POST …/cancel-all { reasonCode, note? }` (**key 58**) ⇒ `{ cancelled, seatsCancelled, familiesTold }` · `POST …/teachers` · `DELETE …/teachers/:teacherId` · `PATCH …/teacher` (delegates to `swapGroupTeacher`) · `POST …/dates` · `PATCH …/:key { title?, headCount?, teacherRates? }` (no `otherKind` — the group kind is fixed). ONE service module with the key kind as a parameter (no copy of the OTHER functions — pinned by source: one `seriesRows`, one `cancelDoomedRows`-style writer).
- **Cascade rule (by value):** cancel-all ⇒ every live GROUP row from the key CANCELLED + every live seat on them CANCELLED (the session-cancel semantics) + `class_cancelled_parent` per seat (household accounts) + `other_series_cancelled` per coach once; ATTENDED rows/seats untouched. A seat's course keeps its plan otherwise (no course end).
- **`weekly_schedule_teacher`** (24th kind): payload `{ weekStart, rows: [{ date, startTime, endTime, program, studentName? }] }`, English only by value (no Thai byte in the render for either `line_lang`); the Monday 08:15 job; `REMINDABLE` untouched.
- 🚫 No migration; no change to the OTHER series behaviour (its tests byte-identical); no money.
- ❓ for you: (1) the branching list (§1.1); (2) the seat-cancel semantics (§1.2); (3) the job slot + flag (§1.4).

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite **count** · tsc 0 · 52 = 52 · the group reads/writes by value · the cascade by value (seats · families · coach once · ATTENDED kept) · confirm-whole-group by value · the digest by value (English only under both langs; nothing for an empty week) · OTHER series byte-identical · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-22) — §1 verified; your three ❓ answered; one finding; no code yet

## §1 Verified in the repo
1. **The branching list (❓1) — `other-series.service.ts`:** the OTHER assumption lives in exactly FOUR places, everything else is key-shape-agnostic. (a) **`seriesRows(exec, key)`** `:38` — `otherSeriesKey = key AND bookingType = OTHER`; (b) **`listOtherSeries`** `:81` — `select groupBy(otherSeriesKey) … bookingType = OTHER`; (c) **`addDatesToOtherSeries`** `:210` — the insert says `bookingType: "OTHER", otherSeriesKey: key`; (d) **`updateOtherSeries`** `:226` — accepts `otherKind` (a group's kind is fixed — refused/omitted for a group). Agnostic already: `seriesRowsFrom`, `templateOf`, `extrasOf`, `ratesOf` (columns, not types), `confirmAll` (`bulkConfirm` on the ids), `cancelAll`'s row loop + holds, add/remove teacher (`bookingTeachers`), `notifySeriesTeachers` (`title = otherTitle` = the group's name — the coach reads the group's name on `other_series_cancelled` with no new bytes), `ALREADY_ON_ROW` / `PRIMARY_TEACHER` / `DATE_EXISTS`. ⇒ **ONE module, the key kind a parameter:** `type SeriesKey = { otherSeriesKey: string } | { groupKey: string }`; `seriesRows(exec, k)` and the list's groupBy pick the column + type from the kind; `addDates` copies the template with the kind's column/type (a GROUP date = a new GROUP row, no seats — `seatOnGroup` fills them when a child's course lands); the header PATCH drops `otherKind` for a group. **No copy of any function** (pinned by source: one `seriesRows`, one cancel loop, one `notifySeriesTeachers`). The OTHER routes keep calling the same functions with `{ otherSeriesKey }` ⇒ byte-identical behaviour (their 21 tests untouched).
   🔍 **Finding (not folded):** `swapOtherSeriesTeacher` `:168` updates `teacherId` on the rows and NEVER moves seats; `swapGroupTeacher` `:1635` does (every live seat follows its group row) — so a group's primary swap MUST go through `swapGroupTeacher`, exactly as you wrote. The series route delegates: `PATCH /group-series/:key/teacher { to, fromDate? }` ⇒ the first live GROUP row ≥ `fromDate` (default today) ⇒ `swapGroupTeacher(row.id, { teacherId: to, fromHereOn: true })`. No `from` in the body (a group has ONE primary; the extras go through add/remove).
2. **The seat cascade (❓2) — it already EXISTS for a single date and I reuse it byte-for-byte:** `cancelSeatsOfGroup(tx, groupRowId, note)` `:1669` — every LIVE seat ⇒ `CANCELLED` + the note + **`reconcileCoursePlan(courseId)`** (SPEC-028 §11.3: the child's course re-owes a make-up — a live seat was never consumed, so `returnsConsumedUnit` never enters; nothing is returned because nothing was taken); the family per seat via **`sendClassCancelledToFamilies(tx, row, reason)`** `:2946` (TASK-420: a household per seat on a GROUP row, `class_cancelled_parent`, the existing kind). This is the per-row status cancel's own path (`:3453` + `:3471`) and the teacher-leave path's (`:3040` + `:3044`). ⇒ cancel-all on a group = for every live GROUP row: the row `CANCELLED` + `cancel_reason` + holds (the OTHER loop as is) + `cancelSeatsOfGroup` + `sendClassCancelledToFamilies`; then `other_series_cancelled` ONCE per coach (the OTHER rule; the seats send no teacher notice — the row's own comment). `cancelSeatsOfGroup` is not exported today ⇒ exported (one word). ATTENDED rows and their seats untouched (not live). A seat's course keeps its plan otherwise — no course end, no `endedAt`.
3. **Confirm-whole-group:** the live GROUP rows' seats (`bookings.groupId IN rows`, live) grouped by `courseId` ⇒ `confirmCourse(courseId)` per course (its ONE `course_confirmed` LINE per course, unchanged), the GROUP rows themselves through `bulkConfirm` on the PENDING ones (as OTHER's confirm-all; a CONFIRMED row a no-op). `{ courses, confirmed, skipped, results[] }` — `confirmed/skipped/results` = the group rows' bulk outcome; `courses` = the count of courses confirmed. A course already fully CONFIRMED ⇒ `confirmCourse` answers its own no-op (I will read its exact shape and pin it; if it throws on "nothing pending" I catch that ONE code and count it as skipped — say if you would rather see it surface).
4. **The weekly digest — the mechanics, and one recommendation:** the jobs are NOT in-process — every job is a **Windows Task Scheduler exe** (`scripts/daily-reminder.ts` → `POST /internal/jobs/daily-reminder` with `INTERNAL_JOB_SECRET`; `job_runs` always written so a job never registered on the box stays visible — TASK-208's lesson). ⇒ the weekly = `runWeeklyTeacherDigestJob(date?)` in `jobs.service.ts` + `POST /internal/jobs/weekly-teacher-digest` + `scripts/weekly-teacher-digest.ts` (+ the `package.json` script) — **and the human registers the Monday 08:15 task on the box (a deploy line, not code)**. The builder: `weekStart` = the Monday of the run date's week (a Tuesday manual run still builds the current Mon–Sun); rows = bookings in `[mon, sun]` with status in `REMINDABLE` (= `CONFIRMED` only — `lib/daily-reminder.ts`, the daily's rule; say if the weekly should include PENDING) grouped per teacher — primary AND additional (the daily's grouper, reused); a GROUP row is ONE line with the group's name (the seats are not listed — the coach's week, not the roll); `studentName` = `displayNameOf` (the ONE name rule); one outbox row per teacher, idempotency key `weekly-teacher:<teacherId>:<weekStart>` (a second Monday run sends nothing twice); a teacher with no rows ⇒ no row at all; a `job_runs` row `weekly-teacher-digest` with the summary. **Flag (❓3): I recommend NONE** — `camp_reminder_enabled` existed because its words were PLACEHOLDERS awaiting the owner; here the words are the owner's (REQ-104 §3), and a flag defaulting ON that nobody asked for is a second switch to forget. If you want one anyway: `weekly_teacher_digest_enabled` `on|off` default `on` — one line in `lib/settings.ts`, the `camp_reminder_enabled` shape. **English only:** there is NO per-kind language override precedent — `t(key, lang)` falls back TH→EN per key (`line-i18n.ts:645`); the daily's `renderTodaySchedule` is bilingual. So the 24th kind's case takes NO `t()` and NO `lang` — its bytes are constants in the renderer, pinned by value under BOTH `line_lang` values (no Thai code point in the output).
   **The bytes I will print (REQ-104 §3 — confirm the separators):** line 1 `📅 THIS WEEK'S SCHEDULE` · line 2 `Hello, here is your teaching schedule for this week:` · one line per row `DD-MM-YYYY · HH:MM-HH:MM · <program or title> / <student>` (a GROUP or OTHER row: `<title>` alone — no student; a DUO row `A & B`; the date DD-MM-YYYY per REQ-085 §16d, the customer's) · last line `Please review your schedule.`; a blank line after the greeting and before the closing. `no-iso-date-leak` / `no-placeholder-leak` / `message-trailing` census pins move 23 → 24.

## §2 The contract — confirmed with the above
- **Routes (`menu:calendar`, the OTHER twins' keys):** `GET /group-series?from&to` · `GET /group-series/:key` (the series DTO + `rows[].seats: [{ bookingId, studentId, displayName, status }]` — every seat, any status, so the modal can show a cancelled one greyed; say if live-only) · `POST /group-series/:key/confirm-all` (**`action:bookings.course-confirm`**) · `POST …/cancel-all { reasonCode, note? }` (**key 58**) ⇒ `{ cancelled, seatsCancelled, familiesTold }` · `POST …/teachers { teacherId, rateMinor?, fromDate? }` · `DELETE …/teachers/:teacherId?fromDate` · `PATCH …/teacher { to, fromDate? }` (→ `swapGroupTeacher`) · `POST …/dates { dates[] }` · `PATCH …/:key { title?, headCount?, teacherRates? }` — the OTHER validators reused where the shape is the same; a group-specific one only for the PATCH (no `otherKind`) and the teacher swap. Keys: the OTHER routes' keys one-for-one (`calendar.booking-edit` for the edits, 58 for cancel-all — I will list the exact map from `route-access.ts` in the build report).
- **Cascade rule (by value):** as §1.2 — every live GROUP row + every live seat CANCELLED (the seat's course re-owes via `reconcileCoursePlan`), `class_cancelled_parent` per household per seat, `other_series_cancelled` once per coach with the group's name; ATTENDED rows/seats untouched; no course end.
- **`weekly_schedule_teacher`** (24th kind): payload `{ weekStart, rows: [{ date, startTime, endTime, program, studentName }] }`; the job + route + exe as §1.4; no flag (or the one-liner if you say so); `REMINDABLE` and the daily job untouched.
- 🚫 No migration (52 = 52); OTHER series byte-identical (its 21 tests + TASK-430's untouched); no money; `swapGroupTeacher` untouched (delegated to).

## DoD as I will run it
Suite count · tsc 0 · 52 = 52 · the reads/writes by value through fake txs (the group DTO with seats; cancel-all: rows + seats + `reconcileCoursePlan` per seat + a family row per household + ONE coach row; confirm-all: `confirmCourse` per course, the group rows' bulk; add dates = GROUP rows under the key, no seats; the swap delegates) · the digest by value (Mon–Sun window; per-teacher incl. additional; a GROUP row one line; no Thai code point under TH and EN; an empty week ⇒ nothing; the key idempotent; `job_runs` written) · the OTHER tests byte-identical · break-and-watch with sha-256 · @Fern's lines · report ×4.

**For @Fern (draft):** the group modal = the REQ-101 modal on `/group-series/:key` with `rows[].seats`; `Confirm whole group` ⇒ `POST /group-series/:key/confirm-all` ⇒ `{ courses, confirmed, skipped }`; the cancel-all dialog's line: `{ cancelled, seatsCancelled, familiesTold }`; the teacher swap body `{ to, fromDate? }` (no `from`); the header PATCH has no kind field.

⛔ Waiting on your confirm (§1.1 the four branches + one module · §1.2 the existing seat path reused · §1.3 the no-op shape · §1.4 no flag, `CONFIRMED`-only rows, the exact bytes/separators, the box registration as the human's line · the seats DTO any-status) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-22) — build to §2 as written; every point taken
1. ONE module, `SeriesKey` as the parameter, the four branches as you found; `swapGroupTeacher` delegated to (the finding is right — seats must follow); no `from` on the group swap.
2. The existing seat path reused (`cancelSeatsOfGroup` exported + `sendClassCancelledToFamilies`); `reconcileCoursePlan` per seat — pin that a seat's course re-owes a make-up and nothing is "returned".
3. `confirmCourse`'s no-op caught by its ONE code and counted as skipped (pinned; surface nothing).
4. **No flag.** Rows = `CONFIRMED` only (the coach is told of what is his — the daily's rule). The bytes and separators as you wrote (DD-MM-YYYY; a GROUP/OTHER row = the title alone; DUO `A & B`; blank line after the greeting and before the close). **The Monday 08:15 Task Scheduler registration is the human's deploy line** — I put it on Porter's list. English only by constants — no `t()`, pinned under both langs.
5. Seats DTO any-status — yes (the modal greys a cancelled one).
Fern's lines final = your draft; TASK-442 §0 set.

---

# ✅ CODE DONE — @Jason → @Sober (2026-09-22) — built as confirmed; no migration (52 = 52); 2766 pass / 0 fail; 21/21 mutations bite

**Numbers:** `bun test` **2766 pass / 0 fail**, 211 files (+1: `src/lib/group-series-req104.test.ts`, 17 tests) · `tsc --noEmit` **0** · **52 = 52** (no migration) · kinds **24** (+ `weekly_schedule_teacher`) · 12 pins moved (the kinds census 23 → 24 in three files ×5; `viewerOf(c)` 18 → 20; `assertMayEditCoachRate(` 9 → 11 ×2; `actorOf(c)` 18 → 19; the `calendar.group-series` route set + `POST /group-series/:key/dates`; the ended-course VERDICT table + the seven group writes as `unrelated`; `seriesRowsFrom` readers 3 → 4; the OTHER file's "nothing on GROUP" pin re-pinned to "nothing on CAMP, no money" + the ONE read by key kind).

## What was built
- **ONE module, keyed — `other-series.service.ts`:** `export type SeriesKey = string | { otherSeriesKey } | { groupKey }`; `keyOf` names the column + the row type; **a bare string = the OTHER series** — every OTHER caller and all 21 of TASK-428's tests byte-identical. The four OTHER branches you confirmed, and no others: `seriesRows` (the column + type from the kind; a group's read carries `seats` with student/coStudent), the list's `groupBy` column (`listOtherSeries(range, "GROUP")`), `addDates`' insert (`bookingType: k.type, [k.field]: k.value` — a GROUP date is a GROUP row, no seats, no `otherSeriesKey`; pinned by value), the header PATCH (a group + `otherKind` ⇒ 400; `groupSeriesPatch` carries no kind). Pinned by source: one `seriesRows`, one cancel loop, one `notifySeriesTeachers`, exactly one group-shaped export (the delegating swap).
- **The DTO:** `GET /group-series/:key` = the series DTO + `rows[].seats: [{ bookingId, studentId, displayName, status }]` — every seat, any status (the modal greys a cancelled one), `displayNameOf` the child. The OTHER DTO has no `seats` key (pinned).
- **The CASCADE (cancel-all on a group), by value through a fake tx:** every LIVE row ⇒ `CANCELLED` + `cancel_reason` + holds (the OTHER loop as is), then **`cancelSeatsOfGroup`** (exported — one word; the existing per-date path: seat `CANCELLED` + note + `reconcileCoursePlan` re-owes the make-up, pinned by source) and **`sendClassCancelledToFamilies`** per row (a household per seat; a CONFIRMED row only — the family rule); ATTENDED rows and their seats untouched; the coach ONCE per teacher (primary + extras) with `other_series_cancelled` carrying the group's NAME (no new bytes). Result `{ cancelled, seatsCancelled, familiesTold }`; the OTHER call still `{ cancelled }` and never reaches the seat/family senders (pinned by spies).
- **Confirm-whole-group:** the group rows' PENDING through `bulkConfirm` (as OTHER), then every seated course ONCE (the live seats on the live rows, deduped by `courseId`) through `confirmCourse` — its own `course_confirmed` per course, unchanged; a refused course (`COURSE_ENDED`, budget…) is `skipped` with its reason, never thrown. `{ confirmed, skipped, results, courses, courseResults[] }`; OTHER's shape unchanged.
- **The primary swap DELEGATES:** `swapGroupSeriesTeacher({ groupKey }, { to, fromDate? })` — the first live row on/after `fromDate` (default today) is the anchor ⇒ `swapGroupTeacher(anchor.id, { teacherId: to, fromHereOn: true })` (every live seat follows — pinned); the same teacher ⇒ `ALREADY_ON_ROW`; no row from the date ⇒ `{ moved: 0 }`; the OTHER swap refuses a group key (400) — it never moves seats (pinned by absence).
- **Routes (`menu:calendar`):** the nine `/group-series…` twins; the access map pinned EQUAL to the OTHER entries one-for-one, except `confirm-all` = **`action:bookings.course-confirm`** (it confirms courses) and `dates` = `action:calendar.group-series`; cancel-all = key 58. `assertMayEditCoachRate` on add-teacher and the header PATCH (key 59 when a rate rides). Through the ROOT app: all nine reach the ONE service with `{ groupKey }` (spied, by value).
- **The Monday weekly coach digest — `weekly_schedule_teacher` (24th kind):** `lib/weekly-digest.ts` (pure): `weekOf` (Mon–Sun containing the run date — a Sunday belongs to the week before it; pinned four ways), `groupWeekRows` (CONFIRMED only = `REMINDABLE`; the primary AND every additional teacher; date + time order; a GROUP/OTHER row = its title alone; a DUO row `A & B`; a teacher with nothing is absent), `weeklyDigestKey` (`weekly-teacher:<id>:<weekStart>`), `renderWeeklySchedule` — **the bytes exactly as confirmed:** `📅 THIS WEEK'S SCHEDULE` / `Hello, here is your teaching schedule for this week:` / blank / `DD-MM-YYYY · HH:MM-HH:MM · <program> / <student>` per row / blank / `Please review your schedule.`. **English only, pinned by value:** identical under TH and EN, no Thai code point, no ISO date; the branch takes no `t()` and no `lang` (pinned by source); an empty payload renders (the leak walkers pass). `jobs.service.ts` `runWeeklyTeacherDigestJob(date?)`: the Mon–Sun read, one outbox row per teacher with rows (send-once by key — a second run is `duplicate`), nothing for a teacher with nothing, `job_runs` `weekly-teacher-digest` always written; **no flag** (pinned by absence). `POST /internal/jobs/weekly-teacher-digest` (the secret gate; 401 without), `scripts/weekly-teacher-digest.ts` (the daily's exe shape), `bun run job:weekly-teacher-digest`. The daily reminder untouched (`REMINDABLE` pinned).
- 🚫 No migration; no money; OTHER series byte-identical; `swapGroupTeacher` untouched.

## Break-and-watch — `mut441.mjs`, 21 mutations, **21 bite** (`try/finally`, sha-256 restore byte-identical)
A the read ignores the key kind · B the cascade skips the seats · C the families never told · D ATTENDED rows cascaded · E a course confirmed per seat · F a refused course throws · G group dates inserted as OTHER · H the swap ignores `fromDate` · I the swap moves one date · J the DTO drops seats · K the PATCH accepts a kind · L PENDING rows in the digest · M the extras dropped · N the week starts Sunday · O a Thai byte · P an ISO date · Q the send-once key dropped · R `job_runs` not written · S the kind rendered bilingually · T confirm-all under the OTHER key · U the swap route calls the OTHER swap.

## Contract lines for @Fern (TASK-442) — final
- `GET /group-series?from&to` — the OTHER list's shape (`key · title · kind (DUO|GROUP) · startTime · teacherId · firstDate · lastDate · liveCount · total`).
- `GET /group-series/:key` — the OTHER DTO + `rows[].seats: [{ bookingId, studentId, displayName, status }]` (every status).
- `POST /group-series/:key/confirm-all` (`action:bookings.course-confirm`) ⇒ `{ confirmed, skipped, results, courses, courseResults: [{ courseId, outcome: "confirmed"|"skipped", confirmed?, reason? }] }`.
- `POST /group-series/:key/cancel-all { reasonCode, note? }` (key 58) ⇒ `{ cancelled, seatsCancelled, familiesTold }`.
- `POST …/teachers { teacherId, rateMinor?, fromDate? }` (201 `{ added }`) · `DELETE …/teachers/:teacherId?fromDate` (`{ removed }`; the primary ⇒ 409 `PRIMARY_TEACHER`) · `PATCH …/teacher { to, fromDate? }` (`{ moved }`; no `from`) · `POST …/dates { dates[] }` (201 `{ created, bookingIds }`) · `PATCH …/:key { title?, headCount?, teacherRates? }` (`{ updated }`; no kind field). The OTHER modal's 409s (`ALREADY_ON_ROW`, `SLOT_TAKEN`, `DATE_EXISTS`) unchanged.

📦 Deploy (the human's): no migration · **register the Monday 08:15 Windows Task Scheduler task running `bun run job:weekly-teacher-digest`** (the daily-reminder task's shape; `SCHEDULER_API_URL` + `INTERNAL_JOB_SECRET` as the others). 🚫 No deploy request. ⛔ Only you mark this DONE. ▶️ TASK-443's contract follows in its TASK.

---

# ✅ DONE — REVIEWED by @Sober (2026-09-22)
Re-run by me: **2766 pass / 0 fail** · tsc 0 · 52 = 52 · nine `group-series` routes in the access map · `scripts/weekly-teacher-digest.ts` present · the 24th kind in the renderer. Fern's lines = TASK-442 §0. Next: TASK-443's contract.
