# TASK-403 — Camp Stage 3b + REQ-096, BE: UNDO an attended camp day · camp check-in QR · the camp-day LINE reminder (send path behind GATED copy) · the 08:15 reminder CONFIRMED-only — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-19) · **Size M** · no migration expected (say if the QR token needs a column — then `0043`, 44 = 44). `sid`; the last REQ-095 piece before the held `uat` deploy.

## §0 Rulings (Porter 09-19)
(1) Camp LINE reminder to PARENT + TEACHER on a camp day, house format — **the copy is GATED: draft → Porter → owner BEFORE the send path goes live** (the item-7 precedent: build the path behind a placeholder, the words land later). (2) UNDO an attended camp day: returns the day's units to credit, **no money change**. (3) Camp check-in QR, the session QR's pattern. 🚫 (4) per-day revenue NOT wanted. **REQ-096:** the 08:15 reminder must be CONFIRMED-only.

## §1 Verify before the contract
1. `REMINDABLE = {PENDING, CONFIRMED, EXTENDED}` (`lib/daily-reminder.ts:77`, unchanged since the job was born) — the one list both audiences and the GROUP seats use. Confirm no other reminder/send path selects by a status list (my read: the cancel-to-teacher is `CONFIRMED`-gated; every other notice is event-driven; the parent LIFF schedule uses `COURSE_LIVE` but is a VIEW, not a send).
2. The session QR: `checkin_token` / `checkin_token_expires_at` on `bookings`, issued on confirm, `bookings_checkin_token_uq`; the check-in route and `lib/checkin-token.ts` — what is reusable for a camp DAY (a token per `camp_days` row).
3. `assertDayTransition` (`lib/camp.ts`) — where the undo (ATTENDED|ABSENT → PLANNED) sits; `unitsDelta` for the reverse.
4. The camp day cut's ATTENDED rows (`marked_by = "end-of-day"`) vs a staff mark — the undo treats both the same.

## §2 The contract I propose — confirm/correct before code
- **REQ-096:** `REMINDABLE = {CONFIRMED}` — ⚠ **EXTENDED too?** REQ-094 made bulk-confirm turn EXTENDED → CONFIRMED and kept the day-end CONFIRMED-only, i.e. EXTENDED is "not yet confirmed". My read: drop it with PENDING — the reminder goes for CONFIRMED only, both audiences, seats included (one constant). Pinned by value (a PENDING and an EXTENDED row today ⇒ no entry). Say if you read EXTENDED otherwise.
- **Undo:** `PATCH /camp/days/:id { status: "PLANNED" }` from ATTENDED|ABSENT ⇒ `used −= units` (never below 0 — pinned), `marked_by/at` = the actor, a required `reason` (`{ status, reason }`, 3..200 chars — the check-in-correction precedent); the row keeps history via `marked_by` (no new table). Key: `action:camp.day-mark` (the same act — a mark is a mark) — ❓ or a separate `action:camp.day-undo` (55th)? I propose the same key. Refused for a CANCELLED row (`409 CAMP_DAY_TRANSITION`). No money path touched (pinned by absence: `recordSale` count unchanged).
- **QR:** `camp_days.checkin_token` + `checkin_token_expires_at` (if the session's columns live on `bookings`, this needs `0043_camp_checkin_token` — two nullable columns + a partial unique index, catalog-only; say). Token issued when a day is PLANNED (at redeem), expires end of that date; `GET /checkin/camp/:token` (the session's public check-in shape) ⇒ ATTENDED via the SAME `markDay` (consumes units; idempotent on a second scan; `410` expired; `409` for a CANCELLED day); the QR image/link by the session's `lib/checkin-token.ts` helper — one helper, two row kinds.
- **The camp reminder (send path, copy gated):** in the 08:15 job, beside the session reminder: for each OPEN week covering today, one entry per TEACHER on the week (`Camp : <name> — n kids (AM x · PM y)`) and one per PARENT with a PLANNED day today (`<child> — Camp <name> · <Full/AM/PM>`). Rendered through `lib/line-message.ts` with **placeholder labels** (`cp_*` keys, 📖 PLACEHOLDER by form — the `Heads`/`Seats` shape); the `dueSends`/idempotency per recipient per day as the session reminder (REQ-094's freeze respected — a separate select on `camp_days`). **The send is behind a settings flag `camp_reminder_enabled` (default OFF)** until the copy is approved — ❓ or shipped dark by a code constant; I propose the settings key (the owner flips it after the words land, no deploy).
- 🚫 No per-day revenue; no change to the session reminder beyond REQ-096's list.

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · migrations count · REQ-096 by value (PENDING/EXTENDED excluded, CONFIRMED in, seats through the same list) · the undo by value (units back, floor 0, reason required, no sale) · the QR by value (issue/expire/idempotent/second scan) · the reminder rows by value with the placeholder labels + the flag OFF ⇒ nothing enqueued · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-19) — §1 verified, §2 confirmed with four corrections; no code until you confirm

## §1 Verified (read only)
1. **`REMINDABLE = {PENDING, CONFIRMED, EXTENDED}`** at `lib/daily-reminder.ts:77`; two readers — `groupReminders` (the sessions, both audiences: line 89) and the job's GROUP-seat filter (`jobs.service.ts:447`). ✔ **No other send path selects by a status list:** the day-end sweep is `eq(bookings.status, "CONFIRMED")` (a state change, not a send); every `enqueueLine` outside the reminder is event-driven (confirm / cancel / pause / leave / teacher-assign …); the parent LIFF views use `COURSE_LIVE` and send nothing. Your read holds.
2. **The session QR:** `bookings.checkin_token` + `checkin_token_expires_at` (0005) with the partial unique `bookings_checkin_token_uq`; `lib/checkin-token.ts` (`issueCheckinToken(bookingId)` — reads the BOOKING and stamps it; `formatCheckinPayload` builds `{ token, url: PUBLIC_CHECKIN_BASE_URL + /checkin?token=, expiresAt, window }`); `lib/checkin.ts` (`generateCheckinToken()`, the early-window rule); `checkin.service.ts` (`getCheckinQr(bookingId)` issues lazily if absent; `checkinByToken`: 404 unknown → `already: true` on ATTENDED → 400 not CONFIRMED → 400 expired → the window → `attend` + CRM points); the public route `POST /checkin { token }` (no JWT). **Reusable for a camp day: `generateCheckinToken`, the payload builder's URL shape, the public-route pattern. NOT reusable as-is: `issueCheckinToken` (hard-wired to `bookings`), the window rule (a camp day has no start time), the CRM award, the 400-not-409 error codes.** The columns live on `bookings` ⇒ a camp token needs its own columns ⇒ **`0043_camp_checkin_token`** (see §2).
3. **`assertDayTransition`** (`lib/camp.ts`): the allowed set is PLANNED → {ATTENDED, ABSENT, CANCELLED} and ATTENDED ↔ ABSENT; everything else `409 CAMP_DAY_TRANSITION` — so `→ PLANNED` is refused today by that one line. The undo is one more clause there + `unitsDelta` already returns `−units` for a consuming → non-consuming move (no new arithmetic). `markDay` applies the delta with `used += delta` — the floor is not there yet.
4. **The cut's rows** carry `marked_by = "end-of-day"`; a staff mark carries the username. `markDay` reads only `d.status` — it does not look at `marked_by` — so the undo already treats both the same by construction; pinned by value with both fixtures.

## §2 The contract — as you wrote, with four corrections
- **REQ-096:** `REMINDABLE = new Set(["CONFIRMED"])` — **EXTENDED dropped with PENDING, agreed**: REQ-094's ruling is that EXTENDED is "not yet confirmed" (bulk-confirm turns it CONFIRMED; the day-end ignores it); a reminder for a session the shop has not confirmed is the same wrong-status send the owner reported. One constant, both audiences, the seats (`jobs.service.ts:447` reads the same set). Pinned by value: a PENDING and an EXTENDED row today ⇒ no entry, no seat; CONFIRMED ⇒ one.
- **Undo:** `PATCH /camp/days/:id { status: "PLANNED", reason }` from ATTENDED | ABSENT — `reason` required **only for the undo** (3..200 chars; the check-in-correction precedent), `used = max(0, used − units)` (floor pinned), `marked_by/at` = the actor. **Correction 1 — where the reason lives:** `camp_days` has no reason column and the repo has NO audit-log table (checked — nothing to append to), so the reason is a **column: `camp_days.undo_reason text` (nullable), added in the same `0043`** with the token columns; a re-mark after an undo clears it. Read back on the day row (`undoReason`) so the roster shows why. Same key `action:camp.day-mark` (a mark is a mark — agreed, 54 stays). Refused for CANCELLED and PLANNED rows (`409 CAMP_DAY_TRANSITION`). No money: pinned by absence (`recordSale` count in `camp.service.ts` stays 1; no `sales` write in the undo).
- **QR — `0043_camp_checkin_token`** (44 = 44): `ALTER TABLE camp_days ADD COLUMN IF NOT EXISTS checkin_token text, checkin_token_expires_at timestamptz, undo_reason text` + `CREATE UNIQUE INDEX IF NOT EXISTS camp_days_checkin_token_uq ON camp_days (checkin_token) WHERE checkin_token IS NOT NULL` — catalog-only (nullable adds, no rewrite; the partial index builds on a young table), witness = the index (kind `index`), rerunnable. **Correction 2 — when the token is issued:** not at redeem. The session issues **lazily on the first `GET …/checkin`** (`getCheckinQr`), and a PLANNED camp day may be planned weeks ahead — a token minted at redeem sits live for weeks. I propose the same lazy issue: `GET /camp/days/:id/checkin` (staff, `menu:camp`) mints if absent and returns `{ dayId, token, url, expiresAt, studentName, date, half }`; **expiry = 23:59:59+07:00 of the day's date** (no start time). **Correction 3 — the public route and its codes:** `POST /checkin/camp { token }` beside the session's (same public app, no JWT) ⇒ `{ already, day }`; unknown `404`; **expired `410 CAMP_TOKEN_EXPIRED`** (you asked 410 — the session uses 400; I follow your 410 for the camp only and say so in Fern's lines); CANCELLED `409 CAMP_DAY_TRANSITION`; **scanned before the date `409 CAMP_DAY_NOT_TODAY`** (a token exists from the first QR view, so a scan on the wrong day must refuse — the camp's window rule, one line); ATTENDED ⇒ `already: true` (idempotent; a second scan writes nothing); PLANNED ⇒ **the SAME `markDay(dayId, "ATTENDED", "checkin-qr")`** (consumes units through the one transition). ABSENT ⇒ also `markDay(…, "ATTENDED")` (the ATTENDED ↔ ABSENT correction — the child turned up). 🚫 No CRM points for a camp scan (the session awards `ON_TIME_CHECKIN`; a camp day has no "on time" — say if the owner wants points anyway). One helper: I lift the URL/QR shape out of `formatCheckinPayload` into a `checkinUrl(path, token)` used by both — `lib/checkin-token.ts` keeps its booking issuer.
- **The camp reminder (send path, copy gated):** in `runDailyReminderJob`, AFTER the session sends, a **separate select on `camp_days`** (REQ-094's byte-freeze on the session select untouched): every PLANNED day with `date = runDate` in an OPEN week, joined to the student's LINE accounts and the week's `teacher_ids`. Pure builder `campReminderSends(days, weeks, date)` in a new `lib/camp-reminder.ts`: per TEACHER on a week covering today — one entry `{ kind: "camp_reminder", audience: "teacher", week, counts: { total, am, pm, full } }`; per PARENT (per linked device — `deviceReminderKey` reused with a `camp:` prefix so a family with a session AND a camp day gets both) — `{ kind: "camp_reminder", audience: "parent", rows: [{ child, week, half }] }`. Rendered by the outbox worker (`formatOutboxMessage` gains `case "camp_reminder"`) through **placeholder labels `cp_title` / `cp_teacher_line` / `cp_parent_line`** — 📖 PLACEHOLDER by form (pinned by shape: the child's name, the week name and the half appear; the bytes are Porter's). **Correction 4 — the flag:** the settings key `camp_reminder_enabled` (`type: "enum"`, options `off | on`, default `off`, the `line_parent_2fa` shape — the registry has no boolean type) read ONCE per run; **OFF ⇒ the camp block builds nothing and enqueues nothing** (pinned: outbox untouched, `campReminded: 0` in the run summary); ON ⇒ `enqueueLine` per send with its own idempotency key. The owner flips it in Settings after the words land; no deploy.
- 🚫 No per-day revenue; the session reminder changes ONLY by REQ-096's list; no slot block; the camp QR draws no CRM points.

## ❓ for you (four)
1. The undo's reason as a nullable `undo_reason` column on `camp_days` in 0043 (there is no audit-log table to write to) — OK?
2. Token issued lazily on the first QR view, expiry 23:59:59 of the date, `409 CAMP_DAY_NOT_TODAY` on a wrong-day scan — OK?
3. `410 CAMP_TOKEN_EXPIRED` for the camp (the session stays 400) — OK, or align both?
4. No CRM points on a camp scan — OK?

## For @Fern (draft, final after confirm)
`PATCH /api/camp/days/:id { status: "PLANNED", reason }` (undo; `reason` required only here) · `GET /api/camp/days/:id/checkin` ⇒ `{ dayId, token, url, expiresAt, studentName, date, half }` (the QR image is the FE's, as the session's) · public `POST /checkin/camp { token }` ⇒ `{ already, day }` with 404 / 410 `CAMP_TOKEN_EXPIRED` / 409 `CAMP_DAY_NOT_TODAY` / 409 `CAMP_DAY_TRANSITION` · Settings gains `camp_reminder_enabled` (off | on) through the existing settings API · no other shape changes.

⛔ Waiting on your confirm (the four ❓ + the 0043 call) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-19) — all four, plus `0043` — build
1. `undo_reason` nullable on `camp_days` in `0043` — **yes** (no audit table exists; the row is the history; cleared on a re-mark).
2. Lazy token on the first QR view, expiry 23:59:59+07:00 of the date, `409 CAMP_DAY_NOT_TODAY` on a wrong-day scan — **yes.**
3. `410 CAMP_TOKEN_EXPIRED` for the camp; the session stays 400 — **yes, and say it in the public route's header** (two routes, two codes, on purpose: the session's page is live and unchanged). Fern's public check-in page handles both.
4. No CRM points on a camp scan — **yes**; on the owner's list as a question, not built.
`0043_camp_checkin_token` (three nullable columns + the partial unique index, witness = the index) ⇒ **44 = 44.** The copy stays PLACEHOLDER by form — the bytes come from Porter after the owner; the flag default `off`. **Go.**

---

# 📤 REPORT — @Jason → @Sober (2026-09-19)

✅ **DONE (code), against the confirmed contract (all four yes + `0043`).** **2516 pass / 0 fail**, **195 files** (+1) · `tsc --noEmit` clean · 🔴 **migration `0043_camp_checkin_token` — 44 = 44** · offline preflight: `0043` alone `[]`, the batch `0038 → 0043` `[]`.

## What was built
- **REQ-096 (`lib/daily-reminder.ts`)** — `REMINDABLE = new Set(["CONFIRMED"])`: PENDING and EXTENDED out (REQ-094's "not yet confirmed" ruling), one constant, both audiences, the GROUP seats (`jobs.service.ts` reads the same set). The comment names the owner's report and the ruling. The day-end select (`eq(bookings.status, "CONFIRMED")`) and REQ-094's byte-freeze are untouched.
- **`drizzle/0043_camp_checkin_token.sql`** (4 statements, every object `IF NOT EXISTS`): `camp_days` + `checkin_token text NULL` · `checkin_token_expires_at timestamptz NULL` · `undo_reason text NULL` (no defaults — catalog-only, no rewrite) · LAST `camp_days_checkin_token_uq` partial UNIQUE (the 0005 shape) = **the witness** (kind `index`, rerunnable). Header: the 0038 → 0043 order, `db:verify` expects 44, the young-table lock note, no hot table, no enum, no `bookings`, no `camp_packages` (the package still has no expiry — its 3a pin now excludes the token's words).
- **The UNDO (`lib/camp.ts` + `markDay`)** — `isUndo(from, to)` (ATTENDED | ABSENT → PLANNED) is one more clause in `assertDayTransition`; CANCELLED stays final. `unitsDelta` already returns `−units`; **`usedAfter(used, delta) = max(0, used + delta)`** — the floor, applied on the package read inside the tx. `markDay(dayId, status, actor, reason?)`: the undo REQUIRES a reason (validation `3..200`, and the service's own `400` — two fences), stores it in `undo_reason`; any mark clears it (`null`); `marked_by/at` = the actor. Only `status` is read, so the day-end cut's rows (`marked_by = "end-of-day"`) and a staff mark undo alike. **No money:** no sale, no `recordSale` (still exactly one, in the sale — by absence). The day rows (package DTO + roster) carry `undoReason`. Same key `action:camp.day-mark` (54 unchanged).
- **The QR** — `GET /camp/days/:id/checkin` (staff, `menu:camp`, the tenth access row): mints the token **lazily on the first view** (never at redeem — `planDays` untouched), `expires_at = 23:59:59+07:00` of the day's date (`campTokenExpiry`), returns `{ dayId, token, url, expiresAt, studentName, date, half }`; the QR image is the FE's. **`checkinUrl(path)`** lifted out of `formatCheckinPayload` — ONE helper for `/checkin?token=` and `/checkin/camp?token=`. **Public `POST /api/checkin/camp { token }`** beside the session's (no JWT): `campScanOutcome` (pure) — ATTENDED ⇒ `{ already: true }` (idempotent, nothing written, even if expired); CANCELLED ⇒ `409 CAMP_DAY_TRANSITION`; expired ⇒ **`410 CAMP_TOKEN_EXPIRED`** (the session keeps its `400` — said in the route header: *two routes, two codes, on purpose*); another date ⇒ `409 CAMP_DAY_NOT_TODAY` naming the day's date; PLANNED or ABSENT ⇒ **the SAME `markDay(dayId, "ATTENDED", "checkin-qr")`** (consumes the units through the one transition). 🚫 No CRM points (by absence: no `awardCrmPoints` in `camp.service.ts`).
- **The camp-day reminder** — `lib/camp-reminder.ts` (pure): `campReminderSends(days, weeks, date)` — per TEACHER on an OPEN week covering today, one row `{ weekName, total, am, pm, full }` (a teacher on two weeks: two rows, one send); per PARENT one send per DEVICE with one row per child `{ child, weekName, half }`; an unlinked person is a SKIPPED row (`lineUserId: null`) so the reach is counted; only PLANNED days (`CAMP_REMINDABLE`); keys `camp-reminder:<audience>:<id>:<date>[:device]` — their own prefix, so a family with a session AND a camp day today gets both. `camp.service.ts campReminderInputs(runDate)`: a SEPARATE select on `camp_days` (+ OPEN weeks, their teachers, the families via `familyLineUserIdsBulk`) — the session select stays REQ-094's frozen one. `line-message.ts case "camp_reminder"` renders through **PLACEHOLDER labels `cp_title` / `cp_teacher_line` / `cp_parent_line` / `cp_half_*`** (📖 marked MINE, owner has NOT seen — pinned by FORM). **`settings.ts camp_reminder_enabled`** (`enum off | on`, default `off`, TH label) — the job reads it ONCE after the session sends; **OFF ⇒ nothing read, built or enqueued**; ON ⇒ `enqueueLine` per send with its key; the run summary gains `campEnabled, campReminded, campSkipped, campAlready`.
- 🚫 No per-day revenue; the session reminder changed ONLY by REQ-096's list; no slot block; no CRM on a camp scan.
- 🔻 **Pins moved:** `43 = 43` → `44 = 44` in 15 files; the outbox kind census 18 → 19 (`camp_reminder`) in three files (trailing-whitespace / placeholder-leak / ISO-date walkers now cover the new branch too); the settings census + `camp_reminder_enabled`; the 3a camp file: the undo is now a transition, ten access rows, "0042 is the last" → "idx 42", the package's no-expiry pin excludes the token's words.

## 📦 Deploy steps (the human's, after the deploy)
`bun run db:migrate` (0038 → 0043 in journal order; `db:verify` expects **44**). No sale items this time. The flag stays `off` until Porter's copy is approved — the owner flips `ส่งแจ้งเตือน LINE วันแคมป์ (08:15)` in Settings; no deploy. 🚫 No deploy request from me.

## Pinned (`lib/camp-3b-undo-qr-reminder-req095-3b.test.ts`, 17 tests)
- **The migration by text:** 44 = 44, the 44th file / idx 43 / the last, the order and "expects 44"; four statements — three `ADD COLUMN IF NOT EXISTS … NULL` without DEFAULT, the partial unique index LAST; no enum / `'GROUP'` / `bookings` / `camp_packages`; the witness by tag; the schema's three columns + index.
- **REQ-096 by value:** `[...REMINDABLE] = ["CONFIRMED"]`; `groupReminders` over PENDING + EXTENDED + CONFIRMED + SICK_LEAVE + CANCELLED ⇒ exactly one coach row and the CONFIRMED family; PENDING + EXTENDED alone ⇒ `[]`; by source: the seats read the same set, no second list, no `"EXTENDED"` in the job.
- **The undo by value:** `isUndo` four ways, the transition from a PAST day, CANCELLED refused, `unitsDelta` negative, **`usedAfter` floors at 0**, the validation both ways (PLANNED needs 3..200; a mark refuses a reason); by source: the reason stored/cleared, the floor on the package, `markedBy` never read, no sale words, `recordSale` still once, `undoReason` on the DTO.
- **The QR by value:** the expiry instant (`2026-10-05T16:59:59.000Z`), `checkinUrl` with and without the base, the scan rule six ways (already even when expired · attend from PLANNED and ABSENT · 409 CANCELLED · 410 expired with its message · 409 NOT_TODAY); by source: minted only if absent, `planDays` mints nothing, the scan through `markDay(…, "ATTENDED", "checkin-qr")`, no CRM, the public route line, the header's "two routes, two codes", the session's 400 untouched, the access row.
- **The reminder by value:** the fixture (an OPEN and a CLOSED week, a teacher on both, an unlinked teacher, a two-device family with two children, an unlinked family, ATTENDED / CANCELLED / closed-week children): the coach's counts `{3, am 1, pm 1, full 1}`, the closed week absent, two sends two keys for the family, the SKIPPED rows, the absent children; the keys' prefix vs the session's; the words by FORM in TH and EN (child, week, count present; no `{`; trimmed; title + one line per child); the flag's spec/default/parse; the job's gate region (one `enqueueLine`, inside `if (campEnabled)`, after the session sends, the frozen select untouched); the inputs select OPEN weeks and never `bookings`.
- **The routes through the ROOT app:** the undo's `400` before the service, the reason reaching `markDay`, a mark passing `null`; the QR payload; the public scan's `{ already, day }` both ways, the `410` and `409 CAMP_DAY_NOT_TODAY` envelopes by value, the body shape `400`.

## 🔑 Mutation — twenty-six, `finally`, checksum — all bite, none passed first
A PENDING back · B EXTENDED kept · C the seats on their own list · D undo from CANCELLED · E the floor dropped · F no reason at the service · G a mark keeps the old reason · H the validation refine removed · I the token lives to noon · J a wrong-day scan attends · K expired as 400 · L a second scan not idempotent · M the token minted at redeem · N the scan writes status by hand · O a CLOSED week reminds · P ATTENDED counted as planned · Q the camp key collides with the session's · R an unlinked parent dropped · S the flag defaults ON · T the gate dropped · U no idempotency key · V the child's name dropped · W the token index not unique · X a NOT NULL DEFAULT column · Y the public route removed · Z CRM points on a scan. Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final
- **Undo:** `PATCH /api/camp/days/:id { status: "PLANNED", reason }` (`reason` 3–200, required ONLY for PLANNED; a mark with a reason is `400`) ⇒ `{ package }`; from ATTENDED | ABSENT only (`409 CAMP_DAY_TRANSITION` otherwise); the day row now carries `undoReason: string | null` (package DTO days + the roster entries). The credit comes back in the same `package.credit`. Key: `action:camp.day-mark`.
- **QR:** `GET /api/camp/days/:id/checkin` ⇒ `{ dayId, token, url, expiresAt, studentName, date, half }` (`url` = the public page with `?token=`; render the QR from it, as the session's). Public **`POST /api/checkin/camp { token }`** (no JWT) ⇒ `{ already: boolean, day: { dayId, weekId, date, half, units, status, undoReason } }`; errors `404` unknown · **`410 CAMP_TOKEN_EXPIRED`** (the camp's code — the session page keeps `400`) · `409 CAMP_DAY_NOT_TODAY` (message names the day's date) · `409 CAMP_DAY_TRANSITION` (a CANCELLED day) — all in the `{ error: { code, message } }` envelope.
- **Settings:** the existing settings API gains `camp_reminder_enabled` (`enum`, `off | on`, default `off`, label `ส่งแจ้งเตือน LINE วันแคมป์ (08:15)`) — renders like `line_parent_2fa`.
- **Nothing else changed shape.** The 08:15 reminder simply stops listing PENDING / EXTENDED sessions (REQ-096) — no FE work.

⛔ Only you mark this DONE. ▶️ Next for me: whatever waits on the board.
