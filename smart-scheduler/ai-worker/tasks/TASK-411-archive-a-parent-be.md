# TASK-411 — Archive a PARENT (`REQ-098`, `SPEC-084` as recommended), BE — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-19) · **Size S+** · migration `0046_parent_archive` (47 = 47) · key 56. `sid`; joins the batch. **After TASK-410.**

## §0 Owner's rulings (Porter 09-19): `parents.archived_at/by`; hidden from every active-parent read incl. the LINE lookups + `familyLineUserIdsBulk`; a restore view; KEEP the phone; **CLEAR the LINE id on archive** (keep a copy for the audit); **CASCADE to the students** (restore restores only those the same act archived); coexists with suspend; refused while any of its students has a live future session.

## §1 Verify before the contract
1. Every read that returns a PARENT row or resolves one: the People list + search (`parent.service.ts` ×3), the parent detail, the LINE registration/phone lookup (`line-register.service.ts`), the webhook's `line_user_id` resolution (`line-webhook.service.ts`), `familyLineUserIdsBulk` + `parentLineUserIds` (every notice), the attention queries, the SOM report — list them; where ONE `activeParentWhere` predicate drops in (the REQ-093 `archivedIds()` shape or a where term).
2. REQ-093's student archive (`archiveStudent`, `assertStudentActive`, the count of live future sessions) — what is reusable for the cascade and the refusal count.
3. `parents_line_user_id_uq` — clearing `line_user_id` on archive frees it; the LINE link flow: what happens when that LINE user next opens the bot (must land on "not linked", never on the ghost).
4. `lib/suspend.ts` — suspend + archive coexist; does any suspend read need the archive term (a suspended-and-archived parent must not appear in the suspended list either).

## §2 The contract I propose — confirm/correct before code
- **`0046_parent_archive`:** `parents.archived_at timestamptz NULL`, `archived_by text NULL`, `archived_line_user_id text NULL` (the cleared id, audit only — never read by a lookup) — catalog-only; witness = `archived_line_user_id`.
- **`POST /parents/:id/archive`** (key `action:people.parent-archive`, 56th; TH `เก็บผู้ปกครอง` / EN `Archive a parent`): live future sessions across its students ⇒ `409 PARENT_HAS_SESSIONS "มีคาบเรียนในอนาคต n คาบ"`; one tx: the parent's `archived_at/by`, `line_user_id → archived_line_user_id` then NULL, every non-archived student ⇒ archived with `archived_by = "parent:<id>"` (REQ-093's `archiveStudent` shape, its own refusal skipped — the parent's count already covered it). **`POST /parents/:id/unarchive`:** the parent back, the LINE id NOT restored (the family re-links), the students whose `archived_by = "parent:<id>"` restored, others untouched.
- **Reads:** ONE `activeParentWhere` at every §1.1 site; the parent detail by id: archived ⇒ `404` unless `?archived=1` (the restore view); the People page's parents list gains `archived=true` (the students' precedent); the DTO carries `archivedAt`.
- **Guards:** `assertParentActive` at: create a student under a parent, the LINE link/claim flow, a booking for its student (already refused by the student's archive after the cascade — pinned that the cascade makes it so).
- 🚫 No merge, no delete, no change to suspend; the ledger untouched (a posted sale keeps its `refId`).
- ❓ for you: (1) the audit column vs writing the cleared LINE id into `note` — I propose the column (a note is prose); (2) `familyLineUserIdsBulk`'s shape — a where term or a post-filter; which keeps it ONE definition.

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · **47 = 47** · the predicate by source at every site · the archive by value (refused with the count; cascade; the LINE id cleared + kept; restore restores only the cascaded) · the ghost never resolves (webhook + register + notices) · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-19) — §1 verified (two findings change §2), §2 confirmed with corrections; no code until you confirm

## §1 Verified (read only)
1. **Every read that returns or resolves a PARENT row** (16 sites, by file): `parent.service.ts` — `findParentByPhone` (28), `findParentByLineUserId` (51, via `familyOfLineUser`), `findOrCreateParentByPhone` (calls the first), `createStudent`'s parent read (230), `loadParentWithStudents` (258 — the detail), `listParents` (284 search-by-name/phone · 303 the page · 312 the `count(*)`), `clearParentLineLink` (341), `setParentSuspended` (473), `findParentOfStudent` (489), `suspendedStudentIds` (the join at 519); `lib/family-link.ts` — `familyLineUserIdsBulk` (46 — EVERY notice's recipients) and `familyOfLineUser` (85 — EVERY inbound LINE message's family: link rows first, then `parents.line_user_id`); `lib/line-lang.ts` (13 — the reply language by LINE id); `attention.service.ts` (77 — `parents.findMany()` for the nag list); `som-report.service.ts` (33 — `parents.findMany()` for the counts); `jobs.service.ts` (389 — today's reminder parents by id); `line-register.service.ts` (265 — the note read, by id). **Where ONE predicate drops in:** there is no shared where-builder here either; the REQ-093 shape is `archivedStudentIds()` + `isNull(students.archivedAt)` per site. I propose **`activeParentWhere = isNull(parents.archivedAt)`** exported from `parent.service.ts` and appended at: the three `listParents` statements, `findParentByPhone`, the two `findMany()` sweeps (attention, SOM), `familyLineUserIdsBulk`, `line-lang` — and a **post-check `assertParentActive`** where a row is fetched BY ID for a write (`createStudent`, `setParentSuspended`, `clearParentLineLink`, the LINE link). By-id READS (the detail, `findParentOfStudent`) return the archived row with `archivedAt` set (the restore view needs it); the detail `404`s unless `?archived=1`. Pinned by source: every listed site names `activeParentWhere` or `assertParentActive`; no site writes `isNull(parents.archivedAt)` by hand.
2. **REQ-093's reusable parts:** `archiveStudent(id, actor)` (its own live-future count `date >= today AND status IN COURSE_LIVE`, `409 STUDENT_HAS_LIVE_SESSIONS`, the `archived_at/by` write) and `unarchiveStudent` (+ `assertCanAddStudent` — the household's student cap); `assertStudentActive`; `listStudentsOfParent(…, { includeArchived })`. The cascade cannot call `archiveStudent` as-is (it opens its own statements on `db`, not a tx, and re-counts per child) — I lift its two halves into `liveFutureSessionCount(exec, studentIds[])` (ONE grouped count for the whole household — the parent's refusal number) and `markStudentArchived(tx, id, by)` (the write), and `archiveStudent` calls both — so REQ-093's path and the cascade share the count and the write.
3. 🔴 **Finding A — clearing `parents.line_user_id` alone leaves the ghost RESOLVABLE.** `familyOfLineUser` reads `family_line_links` FIRST (every account since REQ-079, incl. the primary — `bindFamilyLine` inserts it there too) and only then the column. So the LINE user would still land on the archived family. **The existing ONE unlinker already does the whole job: `clearFamilyLine(parentId, actor, tx)`** (`lib/family-link.ts`) — deletes the link rows, NULLs the column, unlinks the rich menu, returns the cleared ids. The archive calls it inside the tx and stores the returned list — so the ghost is unresolvable BY DATA (no accessor needs to know about archives on the inbound side), and both unique indexes (`parents_line_user_id_uq`, `family_line_links_user_uq`) are freed for the surviving/re-created row. What that LINE user sees next: `familyOfLineUser` ⇒ `null` ⇒ the bot's existing "not linked" path (the register prompt) — no new copy.
4. 🔴 **Finding B — `parents_phone_uq`: the phone is UNIQUE, so "keep the phone" has a consequence.** With the archived row holding the number: (a) the admin's `POST /parents` with that phone ⇒ the insert's 23505 ⇒ today's generic `409` — I map it to **`409 PARENT_ARCHIVED "เบอร์นี้เป็นของผู้ปกครองที่ถูกเก็บแล้ว — คืนสถานะแทน"`** (the row is found by `findParentByPhone` WITHOUT the predicate first — one extra read only on the conflict path); (b) **the LINE register flow** (`line-register.service.ts:102/128`: `findParentByPhone` → link, or `findOrCreateParentByPhone` → insert): with the predicate the ghost is not found ⇒ the insert hits 23505 ⇒ today a 500 in the bot. It needs a **new outcome `phone-archived`** on the register result (the bot replies "please contact the shop" — 📖 a PLACEHOLDER string in `line-i18n.ts`, the owner's words to follow via Porter; the TASK-403/410 precedent) — the family cannot self-restore (Sober's §1: "restore first"). Say if you would rather the LINE register silently RESTORE the parent (I advise not: an archive is the shop's decision).
5. **Suspend coexists:** `suspendedStudentIds` (the bookings guard) joins parents — an archived+suspended parent's students are archived by the cascade, so they never reach a booking anyway; the People list has no separate "suspended" read (the flag rides the row), and the archive predicate hides the row. `setParentSuspended` on an archived parent ⇒ `409 PARENT_ARCHIVED` (restore first). No suspend read needs the term beyond `listParents`.
6. The SOM report's `parents.findMany()` feeds the household COUNTS — with the predicate an archived family drops out of "households", which is what the owner's archive means; say if the report should keep counting them.

## §2 The contract — as you wrote, with the corrections
- **`0046_parent_archive`** (47 = 47), catalog-only on `parents` (small): `archived_at timestamptz NULL` · `archived_by text NULL` · **`archived_line_user_ids text[] NULL`** (Correction 1 — ALL the family's accounts, primary first, as `clearFamilyLine` returns them; audit only, never read by a lookup — `familyOfLineUser` cannot see it by construction) · a partial index `parents_archived_idx ON parents (archived_at) WHERE archived_at IS NOT NULL` (the restore view's read) = **the witness**. No hot table.
- **`POST /parents/:id/archive`** (key 56 `action:people.parent-archive`, TH `เก็บ/คืนสถานะผู้ปกครอง` / EN `Archive & restore a parent` — one key for both doors, the students' precedent): the household's live future sessions in ONE grouped count ⇒ `409 PARENT_HAS_SESSIONS "มีคาบเรียนในอนาคต n คาบ — ยกเลิก/ย้ายก่อน"`; else ONE tx: `clearFamilyLine(id, actor, tx)` → `archived_line_user_ids = cleared`, `archived_at = now`, `archived_by = actor`; every NON-archived student ⇒ `markStudentArchived(tx, s.id, "parent:<id>")`. Idempotent (already archived ⇒ the row, no writes). ⇒ `{ parent, archivedStudents: n, clearedLineAccounts: n }`.
- **`POST /parents/:id/unarchive`** (the same key): the parent's three columns cleared (**the LINE ids NOT restored** — the family re-links; the audit list stays), every student with `archived_by = "parent:<id>"` restored (`archived_at/by = NULL`), a student archived on its own untouched; `assertCanAddStudent` is NOT applied to the cascade's restore (they were the household before). ⇒ `{ parent, restoredStudents: n }`.
- **Reads:** `activeParentWhere` at the §1.1 sites; `GET /parents?archived=1` ⇒ the archived rows only (the students' `archived=true` shape) with their `archivedStudents`; `GET /parents/:id` ⇒ `404` on an archived row unless `?archived=1`; the parent DTO + `archivedAt`, `archivedBy` (never the ids list — audit stays server-side).
- **Guards — `assertParentActive(exec, id)` ⇒ `409 PARENT_ARCHIVED`:** `createStudent` under a parent · `setParentSuspended` · `clearParentLineLink` · `updateParent` · the LINE link/claim (`linkParentLine` + the register's found-by-phone branch — Finding B's outcome) · a booking for its student is refused by the CASCADE (`assertStudentActive` — pinned that the cascade makes it so).
- **Finding B's two refusals:** admin create ⇒ `409 PARENT_ARCHIVED`; LINE register ⇒ outcome `phone-archived` with a PLACEHOLDER reply.
- 🚫 No merge, no delete, no change to suspend's rules, the ledger untouched, the phone kept.

## ❓ for you
1. Your (1): the column — yes, but as `text[]` of ALL accounts (Finding A: the family may hold several) — OK?
2. Your (2): `familyLineUserIdsBulk` — a **where term** (`activeParentWhere` in its `parents.findMany`), not a post-filter: one definition, and the outbound side needs it even though the data is cleared (a restore without a re-link must not resurrect old ids — there are none, but the term makes it a rule, not a coincidence). OK?
3. Finding B — the LINE register's `phone-archived` outcome with a placeholder reply (copy via Porter), and the admin's `409 PARENT_ARCHIVED` — OK? (Alternative: the register silently restores — I advise not.)
4. §1.6 — the SOM report stops counting archived households — OK?
5. The witness = the partial index (a column is fine too; the index is the LAST object and the restore view's read) — OK?

## For @Fern (draft, final after confirm)
`POST /api/parents/:id/archive` ⇒ `{ parent, archivedStudents, clearedLineAccounts }` / `409 PARENT_HAS_SESSIONS` (message names n) · `POST /api/parents/:id/unarchive` ⇒ `{ parent, restoredStudents }` · `GET /api/parents?archived=1` (the restore view) · `GET /api/parents/:id` 404 when archived unless `?archived=1` · the parent DTO + `archivedAt`, `archivedBy` · `409 PARENT_ARCHIVED` on create-with-that-phone / suspend / edit / add-student / clear-link of an archived parent · key `action:people.parent-archive`.

⛔ Waiting on your confirm (the five ❓) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-19) — all five yes; build (after 410, which is IN)
1. `archived_line_user_ids text[]` — **yes** (Finding A is right: the family holds several accounts and `family_line_links` is read first — `clearFamilyLine` inside the tx is the only honest clear).
2. `familyLineUserIdsBulk` — **a where term, yes**: the rule, not the coincidence.
3. Finding B — **yes both**: admin create ⇒ `409 PARENT_ARCHIVED`; the LINE register ⇒ `phone-archived` with a PLACEHOLDER reply (copy via Porter; my draft goes to him now). **Never a silent restore** — an archive is the shop's decision.
4. The SOM report stops counting archived households — **yes** (that is what "archived" means; flagged to Porter as a fact, not a decision).
5. The partial index as the witness — **yes.**
**Go.** `0046` ⇒ 47, key 56.

---

# 📤 REPORT — @Jason → @Sober (2026-09-19)

✅ **DONE (code), against the confirmed contract (all five yes).** **2554 pass / 0 fail**, **197 files** (+1) · `tsc --noEmit` clean · 🔴 **migration `0046_parent_archive` — 47 = 47** · offline preflight `0046` alone `[]`, the batch `0038 → 0046` `[]`.

## What was built
- **`drizzle/0046_parent_archive.sql`** (4 statements, catalog-only on `parents`): `archived_at timestamptz NULL` · `archived_by text NULL` · `archived_line_user_ids text[] NULL` (audit only) · LAST `parents_archived_idx` partial on `archived_at` = **the witness** (kind `index`, rerunnable). The phone column and `parents_phone_uq` untouched. Header: the order, expects 47, no hot table.
- **`lib/parent-archive.ts`** (new, its own file so `family-link.ts` and `parent.service.ts` share it without a cycle): **`activeParentWhere() = isNull(parents.archivedAt)`** — the ONE definition; `isParentArchived`; `PARENT_ARCHIVED()` = `409 "ผู้ปกครองรายนี้ถูกเก็บแล้ว — คืนสถานะก่อน"`; `cascadeMarker(id) = "parent:<id>"`.
- **The predicate at every working read** (pinned by source; no site writes the term by hand — pinned by absence across seven files): `listParents` — the search half, the page and the count share ONE `where` (`?archived=1` flips it to `isNotNull` = the restore view); `findParentByPhone`; `familyLineUserIdsBulk` (a where TERM — every notice's recipients); `line-lang`; the attention nag; the SOM households (an archived household is not counted — your ❓4). **`assertParentActive` / `isParentArchived` at every by-id write:** add a student (both doors), `updateParent`, `setParentSuspended`, `clearParentLineLink`, `linkParentLine` (the LINE claim), and `unarchiveStudent` (a child cannot come back under an archived household — restore the parent first). `getParent` ⇒ `404` on an archived row unless `?archived=1`.
- **REQ-093's two halves lifted** so the cascade shares them: `liveFutureSessionCount(exec, studentIds[])` (ONE grouped statement — the parent's refusal number and the student's are the same count) and `markStudentArchived(exec, id, by)`; `archiveStudent` calls both; the REQ-093 pins re-pointed with the reason.
- **`POST /parents/:id/archive`** (key 56 `action:people.parent-archive`, TH `เก็บ/คืนสถานะผู้ปกครอง` / EN `Archive & restore a parent` — one key, both doors): 404 · idempotent (an archived parent ⇒ the row, no writes) · the household's live future sessions ⇒ **`409 PARENT_HAS_SESSIONS "มีคาบเรียนในอนาคต n คาบ — ยกเลิก/ย้ายก่อน"`** BEFORE the tx · ONE tx: **`clearFamilyLine(id, actor, tx)`** (Finding A — the link rows + the column + the rich menu; the ghost is unresolvable BY DATA) → `archived_at/by` + **`archived_line_user_ids = the cleared list`** → every non-archived child `markStudentArchived(tx, id, "parent:<id>")`. ⇒ `{ parent, archivedStudents, clearedLineAccounts }`. The phone is never written.
- **`POST /parents/:id/unarchive`** (the same key): the parent's `archived_at/by` cleared — **the LINE ids NOT restored** (the audit list stays; the family re-links); ONLY the children with `archived_by = "parent:<id>"` restored (a child archived on its own stays); no household-cap check. ⇒ `{ parent, restoredStudents }`.
- **Finding B (the phone is unique and stays):** `findArchivedParentByPhone` is the ONE read past the predicate; the admin's `createParent` ⇒ `409 PARENT_ARCHIVED "เบอร์นี้เป็นของผู้ปกครองที่ถูกเก็บแล้ว — คืนสถานะแทน"`; `findOrCreateParentByPhone` refuses before its insert (never a 23505, never a silent restore); **the LINE register** — both doors (`lookupFamilyByPhone`, `linkFamilyByPhone`) return the new outcome **`phone-archived`** before any write; the chat maps it to `verify_parent_archived` (📖 PLACEHOLDER by form — names the admin; the owner's words via Porter), the page to `409 PHONE_ARCHIVED`.
- 🚫 No merge, no delete, no change to suspend's rules (a suspended-and-archived row is simply hidden), the ledger untouched.
- 🔻 **Pins moved:** the census 46 → 47 in 17 files; the registry 55 → 56 (three files); `actorOf` 15 → 16; the ended-course classification + the two archive routes (`unrelated`); REQ-093's three pins (the lifted count/write; the parents' search half now carries the PARENT's own scope — asserted as "never the student's"); ITEM 12's chat-reply census 5 → 6 (+ `verify_parent_archived`).

## Pinned (`lib/archive-parent-req098.test.ts`, 14 tests)
- **The migration by text** (47 = 47, the four statements byte-for-byte, `text[]`, the partial index, no phone/line_user_id touched, the witness, the schema's three columns + index + the phone's unique index).
- **The predicate by value** (`toSQL` ⇒ `"parents"."archived_at" is null`), the refusal, the marker; **by source** at every read site (the list's three statements share `scope`; the phone lookup; family-link; line-lang; attention; SOM) and every write site (seven), the detail's 404; **no hand-written term** in seven files; the ONE definition.
- **The archive/restore by VALUE through a fake db/tx:** refused with the household count and NOTHING written; then the tx — the unlinker's own `lineUserId: null` write first, the archive's write with `archivedLineUserIds: ["Up1","Up1b"]`, the cascade marking ONLY the active child (the self-archived one untouched); idempotent; the restore clearing only `archived_at/by` and bringing back ONE child. By source: the shared count (three call sites), the archive's order (count → tx → unlinker → columns → cascade), the restore's selectivity and its absence of any LINE write.
- **Finding B by source** (the one read past the predicate; the three refusals; both register doors before any write; no silent restore; the chat/page mappings) **and by value** (`lookupFamilyByPhone` ⇒ `phone-archived` for an archived number, `new` for a fresh one); the reply by FORM in both languages.
- **The key, the two access rows, the validators, the routes through the ROOT app** (the counts, the 409 envelope, the restore, `?archived=1` reaching the list and the detail).

## 🔑 Mutation — twenty-seven, `finally`, checksum — all bite
A the predicate inverted · B the count unscoped · C the search unscoped · D the phone lookup finds the ghost · E the notices reach an archived family · F the nag · G the SOM · H the LINE language · I a hand-written term · J the household count skipped · K the LINE accounts not cleared · L the audit list dropped · M the cascade dropped · N the cascade marked with the actor · O the restore brings back every child · P the restore puts the LINE ids back · Q not idempotent · R the detail leaks · S the admin's create duplicates · T find-or-create duplicates · U the register's link door inserts · V the lookup says `new` · W a student under an archived parent · X an archived parent re-linked · Y a single-text audit column · Z a non-partial witness · AA REQ-093's count bypassed. Every restore byte-identical.

## 📌 Contract lines for @Fern (via you) — final
- `POST /api/parents/:id/archive` ⇒ `{ parent, archivedStudents: n, clearedLineAccounts: n }`; `409 PARENT_HAS_SESSIONS` (message names n); idempotent. `POST /api/parents/:id/unarchive` ⇒ `{ parent, restoredStudents: n }`. Key `action:people.parent-archive` for both.
- `GET /api/parents?archived=1` (or `true`) ⇒ ONLY the archived parents, each with `archivedStudents` (the restore view); the default list hides them and `total` matches. `GET /api/parents/:id` ⇒ `404` when archived unless `?archived=1`. The parent DTO carries `archivedAt: string | null`, `archivedBy: string | null` (never the ids list).
- `409 PARENT_ARCHIVED` on: edit, suspend/unsuspend, add a student, clear-line-link of an archived parent; on `POST /parents` with an archived family's phone (message: restore instead); on a student's own restore under an archived parent.
- The LINE register page: `409 PHONE_ARCHIVED` when the number belongs to an archived family (show "contact the shop").
- REQ-093's student endpoints are unchanged in shape.

📦 Deploy (the human's, after the deploy): `db:migrate` (verify **47**). 🚫 No deploy request. ⛔ Only you mark this DONE. ▶️ Next for me: whatever waits on the board.
