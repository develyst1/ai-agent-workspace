# TASK-420 — DUO = ONE course, TWO kids, BE (`REQ-095 §13.1/§13.2`, `SPEC-087`): `co_student_id` + `class_rate_minor` · the create with a second child + the DUO price · every reader/notice names and reaches both · retire the GROUP-row DUO — CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-21) · **Size L** · migration `0048_duo_course` (49 = 49). `sid`; joins the pending `uat` batch.

## §0 Read `SPEC-087` §0–§1. Locked by the customer: one course, one pool (hours · leave quota · expiry), two children on every session; an editable coach rate on the course; Private in every other way. Group (3–12) untouched.

## §1 Verify before the contract
1. Every read that resolves a session's or a course's STUDENT: the grid loader / `withBookingRelations` (`student`), the booking DTO, the reminder builder (`parentLineUserIds(studentId)` per row — both households must be reached), the confirm / cancel / deduction / leave / rental / teacher-change notices (which take `studentId` from the row), the parent LIFF reads (`parent.service.ts:143` upcoming by `studentId`; the course view), People's course list per student, the eligible picker (`getEligibleStudents`), check-in + CRM (`awardPoints(studentId)`), the SOM report, history. List them with line numbers; say which take a `studentId` argument (add the co-student) and which read the row (add `OR co_student_id`).
2. `createCoursePackage`: where `duo` fits (after the student resolve, before the plan), the sale (one sale, `refId` the course — child 1's household), the price group (Stage 2b's `resolvePriceGroup(subjectId, groupKind)` — how to key it by a course kind instead without a second mapping).
3. The session writers that copy course facts onto rows (`insertBooking` from the plan, `reconcileCoursePlan`'s appends, the sick-leave make-up, `moveBooking`, `resumeCourse`) — each must carry `co_student_id` (the REQ-091 `inheritCourseRental` lesson: ONE chokepoint, not per writer — where is it?).
4. The GROUP-row DUO's creation sites (`createGroupSeries` validator `groupKind`, the FE-facing `GROUP_KINDS`) and `assertKindForType` — retire DUO for creation, keep it readable.

## §2 The contract I propose — confirm/correct before code
- **`0048_duo_course`:** `course_packages.co_student_id uuid NULL REFERENCES students RESTRICT` · `course_packages.class_rate_minor integer NULL` · `bookings.co_student_id uuid NULL REFERENCES students RESTRICT` + `bookings_co_student_idx` partial (`WHERE co_student_id IS NOT NULL`) = the witness. Catalog-only; the `bookings` column add is a blink; the partial index a SHARE scan (say it).
- **Create:** `POST /courses { …, duo?: { coStudentId: uuid, classRateMinor: int ≥ 0 } }` — `coStudentId ≠ studentId` (400), both `assertStudentActive`, both households not suspended, the same `assertCanAddStudent`-type guards as a Private for BOTH; `courseKind = duo ? "DUO" : "PRIVATE"`; the price group by course kind (`DUO ⇒ balance-duo`, else the subject's; the Stage 2b group-kind branch keeps serving Group courses sold into a GROUP) — ONE mapping; **one sale** at the DUO card price (`course-balance-duo-{size}`), `refId` = the course; every planned session row carries `co_student_id` through the ONE chokepoint you name in §1.3.
- **The rate:** `PATCH /courses/:id { classRateMinor }` (course-edit; a Private ⇒ 400 `ไม่ใช่คอร์ส DUO`); `moveBooking` accepts `classRateMinor?` and writes it to the COURSE (one number — SPEC §3.2). Never posted (pinned by absence).
- **Readers/notices:** per §1.1 — every household-facing notice enqueues for BOTH children's devices (`enqueueParentCopies` with the union of both `parentLineUserIds`, de-duplicated — siblings in one household get one copy); the coach's reminder line names both (`Student : คราม & พราว`) — through the existing student line (a join of the two names, the `&` the customer used — no new label); the DTOs carry `coStudentId`, `coStudentName`; the parent LIFF reads include rows/courses where `co_student_id` is the family's child; check-in on a DUO session awards CRM to both (SPEC §3.3 pending — build both; one branch if reversed).
- **Leave / make-up / expiry / pause / end / rental:** untouched by construction — pinned: the reconcile, the leave path, the day-end deduction see ONE row and deduct ONCE; a make-up row carries `co_student_id` (the chokepoint).
- **Retire the group DUO:** `groupSeries.groupKind` ⇒ `z.enum(["GROUP"])`; `GROUP_KINDS_CREATABLE = ["GROUP"]`; `assertKindForType` keeps `DUO` readable; the group-kind price branch keeps `GROUP`; `balance-duo` now reached only by course kind. Existing DUO groups untouched.
- 🚫 No per-child hours; no new key; no posting; Group (3–12) byte-unchanged.
- ❓ for you: (1) the chokepoint for `co_student_id` on every session writer — name it; (2) `parentLineUserIds` union: a helper `householdsOf(row)` or a second call at each site — which keeps it ONE definition; (3) the SOM report: one course two students — where the count changes.

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite, **count** · tsc 0 · **49 = 49** · the create by value (guards; the DUO price; one sale; every session with `co_student_id`) · the readers by source (every §1.1 site names the co-student — a listed table) · the notices by value (both households, de-duplicated) · the rate by value (course-level; the move writes the course) · the pool untouched by value (one deduction, one leave) · the group DUO retired for creation, readable · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-21) — §1 verified (the TABLE below; four findings), §2 confirmed with corrections and your three ❓ answered; no code until you confirm

## §1.1 THE TABLE — every read/notice that resolves a session's or a course's student (line numbers as of today)
| # | site | how it gets the student | DUO change |
|---|---|---|---|
| 1 | `scheduler.service.ts:424` `withBookingRelations.student` (the grid, the modal, every relational reader) | the relation | + `coStudent: true` (a second relation on `bookings.co_student_id`) |
| 2 | `db/mappers.ts:164` `toBookingDTO.student` · `:234` `toCourseWithStudent` | the row | + `coStudent` (the same `studentRef` shape) · the course DTO + `coStudent`, `classRateMinor`, `courseKind` |
| 3 | `jobs.service.ts:409` the reminder builder (`studentId`, `studentName`, `parentId`, `parentLineUserIds` — ONE household per row) | the row | 🔴 a DUO row must reach TWO households: the builder emits the row ONCE per household (child 1's, child 2's) with `studentName` = both names joined `&`; the coach's entry is one row (both names) |
| 4 | `scheduler.service.ts:3324` the CONFIRM's family copy (`parentLineUserIds(tx, current.studentId)`) | argument | the union helper (❓2) |
| 5 | `:2916` the CANCEL's family copy (`sendClassCancelledToFamilies` — `[current.studentId]` or the seats) | argument | `[studentId, coStudentId]` through the union helper |
| 6 | `lib/course-deduction.ts:135/185` `notifyCourseDeduction` → `familyAccountsOfStudent(studentId)` — 🔴 **a FOURTH private copy of "the family's accounts"** (callers `jobs.service.ts:89/107`, `scheduler.service.ts:3341/3357`) | argument | the same union helper (the copy retired — REQ-091's lesson) |
| 7 | `:3583` sick-leave's CRM award (`awardCrmPoints(current.studentId)`) · `checkin.service.ts:78` check-in's award | argument | both children (§3.3 pending — built as both; one branch to flip) |
| 8 | `:4297` `confirmCourse`'s family copy (`student?.id ?? course.studentId`) | argument | the union helper on the course's pair |
| 9 | `checkin.service.ts:109/129` the parent LIFF's today/upcoming (`inArray(b.studentId, ids)`) | the row's column | `OR inArray(b.coStudentId, ids)` — ONE `familyRowsWhere(ids)` predicate |
| 10 | 🔴 `line-webhook.service.ts:942` the LIFF LEAVE's authorize (`b.studentId === studentId`) and `lib/line-leave.ts:26` `childrenWithSessions` (keyed on `b.studentId`) | the row's column | **Finding B** — child 2's family cannot take leave on a DUO session today (the filter finds nothing; the child list shows child 1 only) ⇒ `b.studentId === id \|\| b.coStudentId === id`; the child list adds the co-student |
| 11 | `line-webhook.service.ts:1017` the LIFF course view (`inArray(c.studentId, kids)`) | the course's column | `OR inArray(c.coStudentId, kids)` |
| 12 | `search.queries.ts:44/73` People's course list per student (`innerJoin students ON students.id = coursePackages.studentId`) | a join | the join stays for child 1; the list per student gains `OR co_student_id = :id`; the co-student's card says "DUO with X" |
| 13 | `:729` `getEligibleStudents(COURSE_PACKAGE)` (from `getCourses()` — `course.studentId`) | the course | a DUO course is bookable under EITHER child's name (both ids in the set) |
| 14 | `som-report.service.ts:59/72/94` (`bookings.map(b => b.studentId)`) | the row | ❓3 below |
| 15 | `parent.service.ts:165` the archive's live-future count (`inArray(bookings.studentId, ids)`) | the row | + `OR co_student_id IN ids` — an archive must see the DUO sessions of the household's child too |
| 16 | `:3138` the plan's INSERT (`rows[0]?.studentId`) · `:2053` seat/absent rows · the teacher-change notice `:3173` (per row, to the coach — no family) | the row | the chokepoint (§1.3); the coach notice unchanged |

## §1.2 `createCoursePackage` (`:1945`)
The order today: size → `courseGroupKind` (from `groupKey`) → `resolvePriceGroup(subjectId, courseGroupKind)` → `isSellable` → the discount vs `listPriceMinor(courseItemRef(priceGroup, size))` → the rental check → the tx: `resolveStudentId` → `assertStudentActive` → `assertHouseholdNotSuspended` → the plan (`insertBooking` per session) → after the tx `recordSale(courseItemRef(priceGroup, size), 1, { refId: course })` (`:2142`). **`duo` fits after `resolveStudentId`** (the co-student must be a different, active, unsuspended child — the same two guards) and **before the plan** (every planned row carries it). The price: `resolvePriceGroup`'s first branch is `groupKind === "DUO" || "GROUP" ⇒ GROUP_KIND_PRICE_GROUP[kind]` — **no second mapping is needed:** the create passes `input.duo ? "DUO" : courseGroupKind` — a DUO course and a legacy DUO group both price as `balance-duo` through the ONE object; Group courses sold into a GROUP keep their branch. The sale is one `recordSale` with `refId` = the course (child 1's household is the course's `studentId` — the attribution is by construction).

## §1.3 🔴 Finding A — there is NO chokepoint for course facts on a session row today
Three sites write a course row: **`insertBooking` (`:1236`)** for the four planners (create `:1890`, create-with-sessions `:2053`, the plan's insert `:3138`, resume `:4741`); **the reconcile's EXTENDED append (`:2728`)** — a direct `tx.insert(bookings)` cloning a template row; **the sick-leave make-up (`:3560`)** — a direct insert cloning `current`. `inheritCourseRental` is called AFTER each of the two appends (`:2747`, `:3578`) — per writer, the very shape REQ-091 warned about. **Proposal:** `co_student_id` rides through **`insertBooking`** for the planners — it reads `course_packages.co_student_id` ONCE when `courseId` is set and `coStudentId` was not given (ONE place; the four planners change nothing) — and the two clones copy it from the row they already clone (`template.coStudentId`, `current.coStudentId` — a clone copies every column; adding one is the clone's own rule, not a lookup). Pinned as a census: EVERY `insert(bookings)` site (three) either is `insertBooking` or sets `coStudentId` from its template; no fourth site.

## §1.4 The GROUP-row DUO's creation sites
`validation.ts:467` `groupSeries.groupKind: z.enum(GROUP_KINDS)` (the only human door); `assertKindForType` (`other-kind.ts:23`) refuses a non-group kind on a GROUP row — keeps `DUO` readable; `GROUP_KIND_PRICE_GROUP.DUO` keeps pricing existing DUO groups' seats. ⇒ `GROUP_KINDS_CREATABLE = ["GROUP"]` for the validator; `GROUP_KINDS` unchanged.

## §2 The contract — as you wrote, with the corrections
- **`0048_duo_course`** (49 = 49): `course_packages.co_student_id uuid NULL REFERENCES students RESTRICT` · `course_packages.class_rate_minor integer NULL` · `bookings.co_student_id uuid NULL REFERENCES students RESTRICT` · `bookings_co_student_idx` partial = the witness. Locks: the `students` FK ⇒ SHARE ROW EXCLUSIVE on `students` for a blink (×2); the `bookings` column add a catalog blink; **the partial index on `bookings` — SHARE for its one scan, writes wait (the 0047 sentence)**.
- **Create:** `POST /courses { …, duo?: { coStudentId, classRateMinor: int ≥ 0 } }`; `coStudentId ≠ studentId` ⇒ 400; both `assertStudentActive` + `assertHouseholdNotSuspended`; `courseKind = duo ? "DUO" : "PRIVATE"` (a DTO fact derived from `co_student_id`, no column); `resolvePriceGroup(subjectId, duo ? "DUO" : courseGroupKind)` (the ONE mapping — §1.2); one sale at `course-balance-duo-{size}`, `refId` = the course; every planned row via the chokepoint. **Correction A:** a `duo` with a `groupKey` ⇒ 400 (a DUO course is not sold into a group — the two DUO shapes never meet).
- **The rate:** `PATCH /courses/:id { classRateMinor }` (a Private ⇒ `400 "ไม่ใช่คอร์ส DUO"`); `moveBooking { classRateMinor? }` writes the COURSE. Never posted (by absence — no `recordRental`/`recordSale`/`ratePostedAt` touches it).
- **Your ❓2 — the union:** ONE helper **`householdLineUserIds(exec, studentIds: (string|null)[])`** in `lib/family-link.ts` (beside `familyLineUserIdsBulk`): the union over the pair, de-duplicated, primary-first per family — replacing the FOUR private reads (`parentLineUserIds` ×3 sites in `scheduler.service.ts`, `familyAccountsOfStudent` in `course-deduction.ts` — Finding: the fourth copy). Siblings in one household get one copy by the de-dup.
- **Readers (the table):** #1–#16 as listed; the LIFF's reads through ONE `familyRowsWhere(ids)` predicate (`studentId IN ids OR coStudentId IN ids`); the leave flow's authorize + child list (Finding B); the eligible picker; People's list; the SOM (❓3).
- **Your ❓3 — the SOM:** "active students" (`:59/72`) counts BOTH children of a DUO session (a session is each child's); the course/entitlement counts are per course (unchanged — one course); the first-trial line unchanged. So the change is exactly the three `b.studentId` maps ⇒ `[b.studentId, b.coStudentId]`. One course, two active students — say if the owner wants the students counted once per course instead.
- **Leave / make-up / expiry / pause / end / rental:** untouched by construction — one row, one deduction, one make-up (which carries the co-student by the clone rule); pinned.
- **Retire the group DUO:** `GROUP_KINDS_CREATABLE = ["GROUP"]` on the series validator; `GROUP_KINDS` and the price branch unchanged for existing rows.
- 🚫 No per-child hours; no new key; no posting; Group (3–12) byte-unchanged; no data migration for existing DUO groups (§3.5 — a DATA REQUEST to count them, not mine).

## ❓ for you (four)
1. Finding A — the chokepoint as `insertBooking` (reads the course's `co_student_id` when `courseId` is set) + the two clones copying their template — OK? (The alternative — routing the two EXTENDED appends through `insertBooking` — would put `assertTeacherBookable` and the suspension check on a make-up row, a behaviour change I advise against.)
2. Finding B — the LIFF leave flow gains the co-student (authorize + child list) — in scope? (Without it child 2's family cannot take leave on a DUO session at all.)
3. The FOURTH family-accounts copy in `course-deduction.ts` retired into the ONE union helper — OK?
4. Correction A — `duo` + `groupKey` ⇒ 400 — OK?

## For @Fern (draft, final after confirm)
`POST /courses` + `duo?: { coStudentId, classRateMinor }`; the course DTO + `courseKind: "PRIVATE" | "DUO"`, `coStudent: { id, name, nickname } | null`, `classRateMinor`; the booking DTO + `coStudent`; `PATCH /courses/:id { classRateMinor }`; `PATCH /bookings/:id { classRateMinor? }` (writes the course); the group series' `groupKind` accepts `GROUP` only; `GET /sellable-packages` unchanged (balance-duo already listed); the eligible picker lists a DUO course under both children.

⛔ Waiting on your confirm (the four ❓) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-21) — all four yes; build
1. **Finding A — yes:** `insertBooking` reads the course's `co_student_id` once when `courseId` is set; the two clones copy their template. Routing the appends through `insertBooking` would change make-up behaviour — agreed, not that. **Pin the census:** every `insert(bookings)` site in the repo is either `insertBooking` or a clone whose `.values` names `coStudentId` — a fourth writer fails the suite.
2. **Finding B — in scope, yes:** the LIFF leave's authorize + child list gain the co-student. Without it a DUO family could not take leave — that is not optional.
3. **The fourth family-accounts copy retired into `householdLineUserIds` — yes.** Pin by source that no private "family's accounts" read remains (`parentLineUserIds`, `familyAccountsOfStudent` gone or delegating).
4. **Correction A — yes:** `duo` + `groupKey` ⇒ 400; the two DUO shapes never meet.
SOM: both children counted as active students, one course — yes (flagged to Porter as a fact). `0048` ⇒ 49. **Go.**

---

# ✅ CODE DONE — @Jason → @Sober (2026-09-21) — built as confirmed (all four); 49 = 49; 2630 pass / 0 fail; 40/40 mutations bite

**Numbers:** `bun test` **2630 pass / 0 fail**, 200 files (+1: `src/lib/duo-course-req095-13.test.ts`, 47 tests) · `tsc --noEmit` **0** · 🔴 **`drizzle/0048_duo_course.sql` — 49 = 49** (files = journal tags; idx 48) · offline preflight (`scanBatch`) `0048` alone `[]` and the batch 0038 → 0048 `[]` · witness = the LAST object `bookings_co_student_idx` (the SHARE-lock scan on `bookings` named in the header) · 20 census pins moved 48 → 49, no rule changed.

## What was built (the confirmed §2, site by site)
- **`0048`:** `course_packages.co_student_id uuid NULL REFERENCES students RESTRICT` · `course_packages.class_rate_minor integer NULL` · `bookings.co_student_id uuid NULL REFERENCES students RESTRICT` · `bookings_co_student_idx` partial (`WHERE co_student_id IS NOT NULL`). Every object `IF NOT EXISTS`; no default, no rewrite, no enum. Schema: `coStudent` relation on both tables — **both `one(students)` NAMED** (`course_student`/`course_co_student`, `booking_student`/`booking_co_student`) and the students' `many()` named to match (drizzle refuses two unnamed relations to one table).
- **🔴 The chokepoint (your #1):** `insertBooking` — `coStudentId = input.coStudentId ?? (input.courseId ? await courseCoStudentId(exec, courseId) : null)` (one `columns: { coStudentId }` read; the create passes it explicitly, so the four planner rows cost nothing extra) — written in `.values`. The two clones copy their template (`template.coStudentId` / `current.coStudentId`). **The census is pinned:** every `.insert(bookings)` in `src` (outside seed/tests) is found by scan — exactly three, all in scheduler.service — and each is classified `inserter` / `clone:reconcile` / `clone:sick-leave` by its `.values`; a fourth site that names neither reads `UNCOVERED` and fails.
- **Create:** `duo?: { coStudentId, classRateMinor: int ≥ 0 }` on `createCoursePackage`; `duo` + `groupKey` ⇒ 400 (validator refine, your #4); `coStudentId === studentId` ⇒ 400 `DUO_SAME_CHILD`; the co-student passes `assertStudentActive` + `assertHouseholdNotSuspended` (the same two guards, in the tx, before any row); `resolvePriceGroup(subjectId, input.duo ? "DUO" : courseGroupKind)` ⇒ `balance-duo` with NO subject read ⇒ **one sale at `course-balance-duo-{size}`, refId = the course**; the course row carries `coStudentId` + `classRateMinor`. **By value** through a fake tx (course row, four rows, both guards, the sale item + refId, the DTO); Private by value (null ×3, the subject's item).
- **The rate:** `PATCH /courses/:id { classRateMinor }` — a Private ⇒ `400 NOT_DUO "ไม่ใช่คอร์ส DUO"` (by value: no write); `PATCH /bookings/:id { classRateMinor }` — refuses a non-DUO row BEFORE the tx (by value: the tx never opens), writes `coursePackages` inside it, never the row (`patch` never carries it; the row update is skipped when `patch` is empty). Never posted (by absence: no `recordSale`/`recordRental`/`enqueueLine` on the same line as the rate; sale-items has no `class_rate`).
- **The ONE accessor (your #3):** `householdLineUserIds(exec, studentIds)` in `lib/family-link.ts` — students → parents (one `findMany`), then `familyLineUserIdsBulk`; union, de-duplicated, **primary-first**; nulls / unparented / unlinked add nothing. **The four private copies are GONE:** `parentLineUserIds` (scheduler ×3 call sites) and `familyAccountsOfStudent` (course-deduction) deleted — pinned by source (`not.toMatch(/parentLineUserIds\(|familyAccountsOfStudent\(|familyLineUserIds\(/)` on both files; exactly 3 `householdLineUserIds(` in scheduler + 1 in deduction).
- **The notices — both households:** the single confirm `[current.studentId, current.coStudentId]`; `confirmCourse` `[student?.id ?? course.studentId, course.coStudentId]`; the family cancel (`sendClassCancelledToFamilies`) — ONE household set for a Private/DUO row `[studentId, coStudentId]`, a set per seat on a GROUP row (by value: DUO ⇒ U1, U2, U2b in one batch; siblings ⇒ the household once; GROUP ⇒ 2 batches); the deduction: `DeductionInput.coStudentId?` handed by all FOUR call sites (the two status writes; the day-end's explicit select now carries `co_student_id`).
- **The reminder — ONCE per household, `A & B`:** the job loads `coStudent`, folds both parents into the ONE bulk read, prints `joinChildNames(student, coStudent)` (nickname-first, ` & `) before the DTO rule, and hands `coParentId` + `coParentLineUserIds` to `groupReminders`, which iterates the row's households `[parent, coParent ≠ parent]` — two families ⇒ the row in both groups with their own accounts; siblings ⇒ one group, the row once (by value, both).
- **LIFF — ONE predicate:** `familyRowsWhere(ids)` = `student_id IN … OR co_student_id IN …` (by value: the SQL text + params), used by BOTH windows (`findToday…`/`findUpcoming…`), which load `coStudent`. **Finding B (your #2):** `doLeave` authorizes `b.studentId === id || b.coStudentId === id`; the child picker `childrenWithSessions(sessions, own)` offers the primary AND the co-student **but only THIS family's linked children** (`own` = `linkedStudentIds(lineUserId)`, now exported) — child 2's family sees Pun, not Ploy (by value). `doMyCourses`: `studentId IN kids OR coStudentId IN kids`.
- **CRM to both:** the check-in and the sick-leave award loop `duoStudentIds(row)`.
- **The readers (the 16-site table, each pinned by source — `test.each`):** `withBookingRelations` + `coursesByIds` + `updateCourse` load `coStudent`; `toBookingDTO.coStudent`; `toCourseWithStudent` + `coStudent` · `classRateMinor` · `courseKind` (DERIVED from `co_student_id`, never stored); the course list/count queries match EITHER child (the co-student + its parent ride as `alias()`es; the ONE search rule parameterised as `studentSearchConditionsOn(q, s, p)` — the term appears 4× in the course query's params, 2× in the others); the eligible picker offers BOTH children (each under its own name, the same course context); the live-future count (archive) `OR co_student_id`; the delete-student history count `OR co_student_id` where the table has it; SOM: `byCourse` both children, `bookingsByStudent` both, entitlements both (one course).
- **Group DUO retired for CREATION only:** `GROUP_KINDS_CREATABLE = ["GROUP"]` on `groupSeries.groupKind`; `GROUP_KINDS` (readers), `groupKindOf` (by value: an existing DUO series still reads DUO), `GROUP_KIND_PRICE_GROUP`, `OTHER_KINDS` unchanged. The now-unreachable "DUO ⇒ cap 2" refine kept (cast) — the rule's shape, for the day the kinds widen. Group (3–12) byte-unchanged (`seatCap min(2).max(12)` pinned).
- **🚫 By absence:** no seat draw from `duo`, no per-child hours column, no new settings key, no posting of the rate, no data migration for existing Stage-2a DUO groups (§3.5 — the DATA REQUEST to count them is still open on your side).

## Break-and-watch — `mut420.mjs`, 40 mutations, **40 bite** (restore byte-identical each)
A the inserter never reads the course · B the column dropped from `.values` · C/D each clone forgets the second child · E a DUO priced from the subject's group · F the same child twice · G the co-student unguarded · H the rate not stored · I the course born without its second child · J/K the rate on a Private (course / row) · L the rate written on the ROW · M/N/O confirm / cancel / confirmCourse reach the primary only · P/AE the CRM to the primary only (leave / check-in) · Q the picker offers the primary only · R the accessor not de-duplicated (⚠ passed on the first fixture — **pin gap closed**: one device linked to BOTH households ⇒ one copy) · S the accessor loses primary-first · T the deduction to the primary only · U the day-end select drops the co-student · V the reminder prints one name · W the bulk read skips the second household · X the grouper sends the second household nothing · Y siblings get the row twice · Z `duo`+`groupKey` accepted · AA a negative rate · AB group DUO creatable · AC the LIFF predicate reads the primary only · AD `courseKindOf` always PRIVATE · AF the picker ignores the co-student · AG the picker offers the OTHER family's child · AH the leave refuses child 2's family · AI the FK CASCADE · AJ the index not partial · AK the search matches the primary only · AL the DTO hides the kind · AM the live-future count ignores the co-student · AN SOM counts one child.

## 🟡 Two facts for you (not findings — decisions I made inside the contract; say if either is wrong)
1. **The leave on a DUO row is ONE leave for the row** (one deduction from the shared pool, one make-up carrying both kids — by the clone rule) — so child 2's family taking leave cancels the session for child 1's family too. That IS the customer's "shared pool" (REQ-095 §13.2), but the family who did not ask gets no notice of it today beyond the make-up's own flow (the cancel notice covers admin cancel + teacher leave only, unchanged). If the owner wants "the other family is told", that is a new placeholder kind — Porter's call.
2. **`sendClassCancelledToFamilies` returns the number of enqueue BATCHES** (a DUO row = 1, even when two households are reached) — the response's `familiesNotified` reads 1 for a DUO row. Cheap to change to "distinct parents reached" if the FE wants the count; left as the simplest true fact.

## Contract lines for @Fern (TASK-421) — final
- `POST /courses` body + `duo?: { coStudentId: uuid, classRateMinor: int ≥ 0 }` (with `groupKey` ⇒ 400 `VALIDATION "คอร์ส DUO ขายเข้ากลุ่มไม่ได้"`; same child ⇒ 400 `DUO_SAME_CHILD`; the co-student's archive/suspend ⇒ the same 409/400 as the primary's).
- The course DTO (every course reader: list, view, create, PATCH, confirm…) + `courseKind: "PRIVATE" | "DUO"` · `coStudent: { id, name, nickname, crm… } | null` (the same shape as `student`) · `classRateMinor: int | null`.
- The booking DTO (every relational reader) + `coStudent: { … } | null` — a DUO course row; render both names (`nickname ?? name`, joined ` & `) where the row shows a student.
- `PATCH /courses/:id { classRateMinor }` and `PATCH /bookings/:id { classRateMinor }` (the session popup) — the SAME course column; a Private ⇒ 400 `NOT_DUO "ไม่ใช่คอร์ส DUO"`. `moveBooking` still needs ≥ 1 field; `classRateMinor` alone is a valid body.
- `GET /students/eligible?type=COURSE_PACKAGE` lists BOTH children of a DUO course (two entries, one `context.courseId`).
- `GET /courses?q=` matches either child's name/nickname/phone.
- `POST /bookings/group-series` accepts `groupKind: "GROUP"` only (DUO ⇒ 400); existing DUO series still render as before.
- The price for a DUO course is `course-balance-duo-{4|6|10}` (6,800 / 9,360 / 14,200 THB) — the sale preview should price by `duo ? "balance-duo" : the subject's group`.

📦 Deploy (the human's, after the deploy): `db:migrate` (verify **49**) — the one index build on `bookings` holds writes for its scan. 🚫 No deploy request. ⛔ Only you mark this DONE. ▶️ Next: whatever waits on the board.
