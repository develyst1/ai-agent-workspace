# TASK-439 — `REQ-103` (SPEC-089 B): cancel a WHOLE voucher on the course-end shape — `0051_voucher_end`, `POST /vouchers/:id/cancel` + `/preview`, future draws ⇒ CANCELLED, balance frozen, ENDED cannot draw, one coach notice per coach — BE, S–M, CONTRACT FIRST

**Repo:** `smart-scheduler-back` · **Assignee:** @Jason · **From:** @Sober (2026-09-22) · **Size S–M.** Migration `0051_voucher_end` (52 = 52). No new key (reuse `action:bookings.course-cancel`). After TASK-437. The held batch.

## §0 Owner rulings (Porter 09-22, all as SPEC-089 B recommended)
Reuse `bookings.course-cancel` · coach notice = the course-end message with a voucher line (no new bytes) · family SILENT · the card reads `ENDED · Nh left`. Voucher `ended_at/by/reason` (the same closed `END_REASONS`); cancel + preview routes; every FUTURE live draw ⇒ CANCELLED; past draws and `used_hours` untouched; the balance frozen and readable; an ENDED voucher cannot draw. No refund/conversion (theirs).

## §1 Verify before the contract
1. `endCourse` (`scheduler.service.ts` — the `/courses/:id/cancel` pair): its exact steps (the doomed-rows predicate, the fixed note, `reconcileBookingHolds`, `sendCourseDroppedToTeachers(tx, course, doomed, "ended", …)`, the preview's shape `{ doomed[] … }`) — which of them take a COURSE-shaped object and would need a voucher twin vs. a shared helper taking `{ rows, label }`. Say the smallest honest split.
2. `sendCourseDroppedToTeachers` / the `course_dropped_teacher` renderer: what prints on the `Program` line for a course (`subject · size`), and what a voucher would print with **no new bytes** (`Voucher 10h`? the existing `ob_voucher_*` label from REQ-087's `VOUCHER DEDUCTION` header?) — the exact existing label to reuse; `cause` gains `"voucher_ended"` (the closed cause set's pin moves).
3. Every place a voucher is DRAWN from or judged usable: the booking creator (`POST /bookings` with `voucherId`), the voucher import, the reminder/day-end (they act on ROWS — an ENDED voucher's remaining live rows are CANCELLED by the end, so nothing to do — confirm), the Bookings page's voucher list (`GET /vouchers` — the DTO gains `status`), `voucherRemaining`, any `voucherStatus`-style derivation (if none exists, this task creates the ONE: `ENDED > EXPIRED > EXHAUSTED > ACTIVE`, the course-status precedence).
4. REQ-100's single-draw cancel/undo (`:3476/:3520` return hours) — an UNDO of a past draw on an ENDED voucher: allowed (history correction, `used_hours` moves) or refused? I say allowed (the end freezes the future, not the past); confirm.

## §2 The contract I propose
- **`0051_voucher_end`:** `vouchers.ended_at timestamptz NULL · ended_by text NULL · end_reason text NULL` + `CHECK (end_reason IS NULL OR end_reason IN (<END_REASONS>))` `NOT VALID` + `VALIDATE` (the witness; ⇔ `END_REASONS` pinned by value — the same set as `0023`'s and the cancel_reason CHECK's).
- **Reads:** the voucher DTO gains `status: 'ACTIVE'|'EXHAUSTED'|'EXPIRED'|'ENDED'` + `endedAt · endReason` (`remaining` unchanged — frozen, readable); `lib/voucher-status.ts` the ONE derivation.
- **`POST /vouchers/:id/cancel/preview { }`** (`action:bookings.course-cancel`, `menu:bookings`) ⇒ `{ doomed: [{ bookingId, date, startTime, teacherId, status }], remaining }` — the FUTURE live draws (`voucher_id = :id AND status IN LIVE AND date ≥ today`; a today row whose start has passed? — the course preview's rule, say it and mirror).
- **`POST /vouchers/:id/cancel { reasonCode, note? }`** (same key) ⇒ `{ ended: true, cancelled: n }` — one tx: the doomed rows ⇒ `CANCELLED` + `cancel_reason = reasonCode` + the fixed note (`ยกเลิกบัตรชั่วโมง (ยกเลิกทั้งใบ)` — say if the course's note is reused verbatim instead), holds reconciled; `used_hours` untouched; the voucher stamped `ended_at = now · ended_by = actor · end_reason`; already ENDED ⇒ `409 ALREADY_ENDED`; then ONE `course_dropped_teacher` per coach (`cause: "voucher_ended"`, naming the CONFIRMED draws lost — the course rule; no family notice, pinned by absence).
- **ENDED cannot draw:** the booking creator refuses `409 VOUCHER_ENDED` before any write; the import untouched; a single-row undo on a past draw allowed (§1.4).
- 🚫 No refund/conversion; no change to courses; REQ-100 untouched.
- ❓ for you: (1) the split (§1.1); (2) the `Program` label (§1.2); (3) §1.4.

## Definition of Done
- [ ] Contract confirmed via @Sober BEFORE code · suite **count** · tsc 0 · **52 = 52** · preflight `[]` · the CHECK ⇔ `END_REASONS` · the pair by value (doomed set; past untouched; `used_hours` unchanged; the stamp; ALREADY_ENDED; VOUCHER_ENDED on a draw) · the notice per coach with the voucher line, no family notice (absence) · `status` precedence by value · 🔑 Break-and-watch, `finally`, CHECKSUM · contract lines for @Fern · report here + `inbox/SA.md` + log.

---

# 📋 CONTRACT — @Jason → @Sober (2026-09-22) — §1 verified; your three ❓ answered; two corrections; no code yet

## §1 Verified in the repo
1. **`endCourse` (`scheduler.service.ts:4828`) + `previewCourseEnd` (`:4142`) — the exact steps:** validate the reason (`isEndReason`, 400 `INVALID_REASON`) → tx → `loadCourseForEnd` (the course + its rows with teacher/subject/student/rental) → `isCourseEnded ⇒ 409 ALREADY_ENDED` → `doomed = endableSessions(rows)` (`lib/course-plan.ts:409`: `isCoursePlanRow && COURSE_LIVE` — **no date rule**: every live row, past-dated included) → each doomed row `CANCELLED` with the fixed note `ยกเลิกคอร์ส (จบคอร์สก่อนกำหนด)` (**no `cancel_reason` on the rows** — the reason lives on the course; **no `reconcileBookingHolds`** either — the course end never reconciled holds, a pre-existing gap I will NOT copy: the voucher end reconciles each doomed row's hold, the same call the status cancel makes) → `sendCourseDroppedToTeachers(tx, course{id,size}, doomed, "ended", { cancelReason, note })` → the course stamped `endedAt/endReason/endNote/endedBy` → `{ cancelled: true, removedSessions, course }`. The preview: `{ alreadyEnded, removedSessions, sessions: [{ date, time, teacher }], student, program }` — no hour rule on a today row (a today row still live is doomed) — mirrored. **The smallest honest split (❓1):** the COURSE-shaped pieces are three — `loadCourseForEnd`, `endableSessions` (its `isCoursePlanRow` filter EXCLUDES a VOUCHER row), and the stamp on `course_packages`; `sendCourseDroppedToTeachers` is already row-shaped except its `course: { id, size }` argument. So: a voucher twin `loadVoucherForEnd` + `endableVoucherDraws(rows, today)` in `lib/voucher.ts` (live AND `date ≥ today` — the owner's "FUTURE" rule; a past-dated live draw is left to the day-end) + the stamp on `vouchers`; and `sendCourseDroppedToTeachers` generalised in place to take `{ id, size }` (a voucher passes `{ id, size: totalHours }`) and the wider `cause` — ONE sender, no twin. The cancel branch itself (rows ⇒ CANCELLED + note + holds) becomes a shared `cancelDoomedRows(tx, rows, note, reasonCode?)` both ends call — the course end gains nothing but the shared shape (its behaviour byte-identical: same note, still no `cancel_reason`, still no holds — say if you want the course end to reconcile holds too; I would, as a separate line).
2. **The renderer (❓2):** `course_dropped_teacher` prints `Program` as `programLabel("COURSE", { subject: ctx.subject, size: payload.size })` = `<subject> <size> HR` — the subject from the worker's `bookingContext` (the first doomed row's), the size from the payload. **No new bytes, exactly:** the voucher passes `size = totalHours` ⇒ the line reads `<subject> 10 HR` — the same words a 10-hour voucher already prints on `VOUCHER DEDUCTION`'s `Program` line? No — that renderer uses `programLabel("VOUCHER")` = the subject alone, and the hours ride on `Remaining`. There is NO existing "Voucher" word label in `line-i18n.ts` (only the deduction TITLE `💡VOUCHER DEDUCTION` and the digest's `คอร์ส/วอยเชอร์…`). So the honest no-new-bytes line is `<subject> 10 HR` (the coach reads a 10-hour entitlement ended — true either way); the TITLE stays `ob_course_ended_title`. If the owner wants the word "voucher" on the line, that is one new key (📖) — his call via Porter; I build the no-new-bytes version. `cause` gains `"voucher_ended"` (renders as `"ended"` — the same title; the closed-cause pin moves to three).
3. **Every draw / usable judgement:** ONE gate — `voucherUsable(v, onDate)` (`lib/voucher.ts:33`: hours left + not expired) — reached by the booking creator (`prepareVoucherBooking` `:1321`), the eligible picker and the SOM (`voucherEligible` = `voucherUsable(...).ok`). ⇒ `VoucherLike` gains `endedAt?` and `voucherUsable` refuses first (`บัตรถูกยกเลิกแล้ว`) — the draw (409 `VOUCHER_ENDED` — I map the gate's reason to the code at the creator), the picker and the SOM all follow from the one line. The import (`importVoucher`) creates a fresh row — untouched. The reminder / day-end act on rows — the end cancels the future live rows, so nothing to do (confirmed by reading: neither reads a voucher's status). **`GET /vouchers` (`toVoucherDTO` `mappers.ts:293`)** gains `status` + `endedAt` + `endReason`; there is NO `voucherStatus` derivation today (only `voucherRemaining` + `voucherUsable`) ⇒ **`lib/voucher.ts` `voucherStatus(v, today)`: `ENDED > EXPIRED > EXHAUSTED > ACTIVE`** (the course-status precedence), the ONE derivation, the DTO reads it.
4. **REQ-100's undo on an ENDED voucher (❓3): allowed.** The status cancel (`:3470` `returnsConsumedUnit`) returns the hour to `used_hours` for a PAST draw — the end freezes the FUTURE (no new draws), not history; a correction of an attended hour stays a correction. Pinned by value (an ENDED voucher's past ATTENDED row cancelled ⇒ `used_hours − 1`, the voucher stays ENDED).

## §2 The contract — confirmed, with two corrections
- **`0051_voucher_end`** (52 = 52): `vouchers.ended_at timestamptz NULL · ended_by text NULL · end_reason text NULL` + `vouchers_end_reason_chk CHECK (end_reason IS NULL OR end_reason IN (<END_REASONS>))` `NOT VALID` → `VALIDATE` — the witness (`constraint-def`, contains `TEACHER_LEAVE`); ⇔ `END_REASONS` by value (the same parse-and-compare pin as `0045`'s).
- **Reads:** `toVoucherDTO` + `status · endedAt · endReason` (`remaining` unchanged — frozen, readable); `voucherStatus` the ONE derivation (by value: the precedence, an ENDED-and-expired voucher reads ENDED).
- **`POST /vouchers/:id/cancel/preview`** (`bookings.course-cancel`, `menu:bookings`) ⇒ `{ alreadyEnded, removedSessions, sessions: [{ date, time, teacher }], student, program, remaining }` — the course preview's shape + `remaining` (**Correction 1:** the same field names as the course preview, so Fern's dialog is one component; `doomed[]` in your draft = `sessions[]` here).
- **`POST /vouchers/:id/cancel { reason, note? }`** (**Correction 2:** the body field is `reason`, the course cancel's name — `v.endCourse` reused byte-for-byte; the validator is the same object) ⇒ `{ cancelled: true, removedSessions, voucher }` — ONE tx: the doomed rows (live, `date ≥ today`) ⇒ `CANCELLED` + the fixed note `ยกเลิกบัตรชั่วโมง (ยกเลิกทั้งใบ)` + `cancel_reason = reason` (the voucher rows DO get the code — an OTHER-style single draw is audited by code today, the course rows were not; say if you want byte-parity with the course instead) + `reconcileBookingHolds` per row; `used_hours` untouched; the voucher stamped `ended_at = now · ended_by = actor · end_reason`; already ENDED ⇒ `409 ALREADY_ENDED`; ONE `course_dropped_teacher` per coach (`cause: "voucher_ended"`, the CONFIRMED draws lost — `confirmedOnly`, the course rule), no family notice (pinned by absence).
- **ENDED cannot draw:** `voucherUsable` refuses ⇒ the creator answers `409 VOUCHER_ENDED` before any write; the picker/SOM exclude it; the import untouched; a past-draw undo allowed.
- 🚫 No refund/conversion; no course change (the course end byte-identical unless you take the holds line); REQ-100 untouched.

## DoD as I will run it
Suite count · tsc 0 · 52 = 52 · preflight `[]` · the CHECK ⇔ `END_REASONS` · the pair by value (the doomed set = future live only; a past live row untouched; `used_hours` unchanged; the stamp; ALREADY_ENDED; VOUCHER_ENDED on a draw; the picker/SOM exclusion) · the notice per coach with `<subject> 10 HR`, cause `voucher_ended`, no family notice · `voucherStatus` precedence · the undo on an ENDED voucher · break-and-watch with sha-256 · @Fern's lines · report ×4.

**For @Fern (draft):** `GET /vouchers` items + `status: "ACTIVE"|"EXHAUSTED"|"EXPIRED"|"ENDED"`, `endedAt`, `endReason` (the card reads `ENDED · Nh left` from `status` + `remaining`); `POST /vouchers/:id/cancel/preview {}` and `POST /vouchers/:id/cancel { reason, note? }` — the course-cancel dialog's shapes and key; 409 `ALREADY_ENDED`; a draw on an ENDED voucher ⇒ 409 `VOUCHER_ENDED`.

⛔ Waiting on your confirm (the split · `<subject> 10 HR` as the no-new-bytes line · the undo · the two corrections · `cancel_reason` on the voucher rows · the course end's holds) before code.

---

# ✅ CONFIRMED — @Sober → @Jason (2026-09-22) — build to §2 as corrected; six answers
1. **The split — YES:** `loadVoucherForEnd` + `endableVoucherDraws(rows, today)` (live AND `date ≥ today`) + the stamp; `sendCourseDroppedToTeachers` generalised in place (`{ id, size }`, the wider `cause`) — ONE sender; `cancelDoomedRows` shared.
2. **`<subject> 10 HR` — YES**, the no-new-bytes line; the title stays `ob_course_ended_title`. The "voucher" word is the owner's (📖 via Porter) — not built.
3. **The undo on an ENDED voucher — allowed**, pinned as you wrote.
4. **Corrections 1 + 2 — YES:** the preview's field names = the course preview's + `remaining`; the body field `reason` (`v.endCourse` reused).
5. **`cancel_reason` on the voucher rows — YES** (audited by code; no byte-parity with the course needed).
6. **The course end's holds — TAKE IT, as its own line + pin:** the course end reconciles each doomed row's hold too (a pre-existing gap; a cancelled row that still holds the slot is the bug REQ-100/slot-holder work chased). Say it in the report as a separate bullet so Tanya can check it (a cancelled course's future slots are bookable again).
Fern's lines final = your draft; TASK-440 §0 set.

---

# ✅ CODE DONE — @Jason → @Sober (2026-09-22) — built as confirmed; `0051` ⇒ 52 = 52; 2749 pass / 0 fail; 18/18 mutations bite

**Numbers:** `bun test` **2749 pass / 0 fail**, 210 files (+1: `src/lib/voucher-end-req103.test.ts`, 20 tests) · `tsc --noEmit` **0** · 🔴 **`drizzle/0051_voucher_end.sql` — 52 = 52** (idx 51, the last) · offline preflight `0051` alone `[]` and the batch 0038 → 0051 `[]` · 28 pins moved (the migration census 51 → 52 in 26 files; `actorOf(c)` 17 → 18; the ended-course VERDICT table gains the two routes as `unrelated`).

## What was built
- **`0051_voucher_end`:** `vouchers.ended_at timestamptz · ended_by text · end_reason text` (three nullable columns, IF NOT EXISTS, a small table — catalog blinks, no rewrite) + `vouchers_end_reason_chk CHECK (end_reason IS NULL OR end_reason IN (…END_REASONS))` dropped-if-exists, `NOT VALID`, then `VALIDATE`d (the 0045 shape; every existing row is NULL so the scan cannot fail). The witness = the CHECK's definition (`constraint-def`, contains `TEACHER_LEAVE`), the LAST entry. **The CHECK ⇔ `END_REASONS` by value** — the test parses the IN-list and compares it to the code's set (mutation O bites). No `end_note` column — per your contract the operator's optional `note` rides the coach notice only (say if you want it persisted; one column, one line).
- **`lib/voucher.ts`:** `VoucherLike.endedAt?`; `isVoucherEnded` — the ONE predicate; `VOUCHER_ENDED_MESSAGE`; **`voucherUsable` refuses an ended voucher FIRST** (hours left / unexpired change nothing) ⇒ the eligible picker and the SOM (`voucherEligible` = `voucherUsable(...).ok`, pinned) exclude it with no line of their own; `VOUCHER_STATUSES` + **`voucherStatus(v, today)`: ENDED > EXPIRED > EXHAUSTED > ACTIVE** (an ended-and-expired voucher reads ENDED, by value; mutation B bites); **`endableVoucherDraws(rows, today)`** = VOUCHER row AND `COURSE_LIVE` (the same "live" the course end uses — PENDING/CONFIRMED/EXTENDED) AND `date ≥ today` — a past live draw is left to the day-end, a TODAY row is doomed (mutations C and D bite from both sides).
- **The creator (`prepareVoucherBooking`):** `409 VOUCHER_ENDED` BEFORE the first-booking expiry write (pinned by position; mutation E bites) — the same predicate the gate reads.
- **The shared cancel branch — `cancelDoomedRows(tx, rows, { note, cancelReason })`:** each row ⇒ `CANCELLED` + the caller's fixed note (+ `cancel_reason` when given) + **`reconcileBookingHolds` per row**. `endCourse` calls it with `cancelReason: null` — its rows byte-identical to before (still no code, pinned by value; mutation L bites) — **and now reconciles holds (your ruling), pinned by VALUE through a fake tx: one reconcile per doomed row** (mutation H bites both ends). `dropCourse` (the pause) untouched — its own loop, its own note, no reconcile (TASK-198's rule), pinned.
- **`endVoucher(id, { reason, note? }, actor)`:** the course's two 400s (`REASON_REQUIRED` / `INVALID_REASON` with `allowed`) before any tx (pinned: the tx never opens) → ONE tx: `loadVoucherForEnd` (the voucher + student, its rows with teacher/subject/student/coStudent in date order) → `409 ALREADY_ENDED` with zero writes → the doomed rows through the shared branch with the fixed note `ยกเลิกวอยเชอร์ (ยกเลิกทั้งใบ)` + `cancel_reason = reason` → `sendCourseDroppedToTeachers(tx, { id: voucher.id, size: voucher.totalHours }, doomed, "voucher_ended", …)` → the stamp `endedAt = now · endReason · endedBy = actor` → `{ cancelled: true, removedSessions, voucher: toVoucherDTO(reloaded) }`. By value: the past CONFIRMED row and the ATTENDED row untouched; **`usedHours` never written** (no write in the tx mentions it; mutation I bites); ONE outbox row per coach for his CONFIRMED draws only (a PENDING-only coach gets none — the course rule), `bookingId` = his first date, the payload `{ kind: course_dropped_teacher, courseId: <voucherId>, cause: "voucher_ended", size: 10, dates, startTime, endTime, cancelReason, note }`; **no family row** (every recipient `teacher`, no family sender in the region; mutation R bites).
- **`previewVoucherEnd`:** `{ alreadyEnded, removedSessions, sessions: [{ date, time, teacher }], student, program, remaining }` — the course preview's field names + `remaining`; writes nothing (the tx never opens); a today row counted.
- **The ONE sender, generalised in place:** `cause: "dropped" | "ended" | "voucher_ended"`; the parameter was already `{ id, size }`. Pinned: exactly one `send…Dropped…` function exists.
- **The renderer:** `voucher_ended` takes the END title. **Pinned byte-identical to an `ended` render in TH and EN** — `COURSE ENDED / ยกเลิกคอร์ส ‼️`, `Program : Freeskate 10 HR`, the dates `DD-MM-YYYY`, the reason — and the word "voucher" appears nowhere (no new bytes; mutation P bites).
- **The DTO (`GET /vouchers` items, the create/import answers):** + `status` (the ONE derivation) + `endedAt` + `endReason`; `remaining` unchanged and readable on an ENDED voucher ("ENDED · Nh left"). `endedBy` not exposed.
- **Routes:** `POST /vouchers/:id/cancel/preview` (`v.endCoursePreview`) and `POST /vouchers/:id/cancel` (`v.endCourse` — the body field is `reason`, the same validator object) under **`action:bookings.course-cancel` on `menu:bookings` — pinned equal to the course routes' entries** (mutation Q bites). Through the ROOT app: both reach their services with `actorOf(c)`; a bodiless cancel is the validator's 400.
- **The undo on an ENDED voucher stays open (❓3):** the status cancel's voucher return (`returnsConsumedUnit` → `usedHours: afterReturn(...)`) carries no `endedAt` guard — pinned by absence. The end freezes the future, not history.
- 🚫 No refund / conversion; no new key; no new i18n bytes; REQ-100 untouched; the import untouched (a fresh row).

## Break-and-watch — `mut439.mjs`, 18 mutations, **18 bite** (`try/finally`, sha-256 restore byte-identical)
A the gate stops refusing ENDED · B ENDED yields to EXPIRED · C the date rule dropped (a past draw cancelled) · D a today row skipped · E the creator's 409 after the expiry write · F ALREADY_ENDED dropped · G the voucher rows lose `cancel_reason` · H the holds reconcile dropped (both ends) · I the end rewrites `used_hours` · J the notice says `ended` · K `size` = remaining hours · L the course end writes a code (parity) · M the preview drops `remaining` · N the DTO drops `status` · O a fifth code in the CHECK · P `voucher_ended` renders PAUSED · Q the route under another key · R a family notice added.

## Contract lines for @Fern (TASK-440) — final
- `GET /vouchers` items (and the create / import answers' `voucher`) gain `status: "ACTIVE" | "EXHAUSTED" | "EXPIRED" | "ENDED"`, `endedAt: string | null`, `endReason: string | null`; `remaining` unchanged — the card reads `ENDED · {remaining}h left` from `status` + `remaining`.
- `POST /vouchers/:id/cancel/preview` body `{}` ⇒ `{ alreadyEnded, removedSessions, sessions: [{ date, time, teacher }], student, program, remaining }` — the course-cancel dialog's shape + `remaining`.
- `POST /vouchers/:id/cancel` body `{ reason: <END_REASONS>, note? }` ⇒ `{ cancelled: true, removedSessions, voucher }`; `400 REASON_REQUIRED` / `400 INVALID_REASON { allowed }`; `409 ALREADY_ENDED`. The key is `action:bookings.course-cancel` — the same button gate as the course.
- A draw on an ENDED voucher (`POST /bookings` type VOUCHER) ⇒ `409 { code: "VOUCHER_ENDED" }`; the eligible-students picker and the SOM already omit it.

📦 Deploy (the human's): `db:migrate` (verify **52**). 🚫 No deploy request. ⛔ Only you mark this DONE.

---

# ✅ DONE — REVIEWED by @Sober (2026-09-22)
Re-run by me: **2749 pass / 0 fail** · tsc 0 · **52 = 52** (`0051_voucher_end`, the VALIDATE the last statement) · preflight `[]`. The note not persisted (no `end_note`) — accepted: the course's `end_note` exists, but the voucher's note is on the notice and the reason on the row; if the owner asks to see it later, one column. **The BE side of the locked batch is complete: 2749/0, 52 = 52, 59 keys, 23 kinds.** Fern: 435 → 438 → 440.
