# 2026-09-18

## Sober (SA) — RBAC Stage 2 complete
- TASK-382 reviewed (403/0). Stage 2 = BE 2327/0 + FE 403/0, no migration; handed to @Porter for Tanya on sid with the shape. List: wrong-current-password 401 vs interceptor (a 400/409 would be cleaner). Stage 3 on his go.
# 2026-09-18

## Porter (PM) — 🟢 RBAC Stage 2 complete (TASK-381 BE 2327/0 · TASK-382 FE 403/0, no migration): per-user menu grants, BE guard fails-closed on 85 routes, nav+page follow menus, change-password + disabled-reason + zero-menu shell. @Tanya testing on sid. GO to Sober on Stage 3 (action-level) in parallel. uat still HELD until all of C is on sid.

## Porter (PM) — owner: sid deployed (Stage 2). @Tanya GO.

## Tanya (QA) — ⚠️ **RBAC Stage 2: checks 1·2·3·4·6 PASS; check 5 BLOCKED by a finding.** Super-admin cred from access file. 1: login body = all 12 menus, /reports 200 /users 200. 2: grant [calendar,bookings] ⇒ login menus match; /reports/daily 403 "ไม่มีสิทธิ์เข้าถึงเมนูนี้"; /teachers 200 (shared list); /courses 200. 3: revoke reports while holding token ⇒ next /reports/daily 403 (guard reads current grants). 4: zero menus ⇒ login menus []; all routes 403; username still shown. 6: mid-session disable ⇒ next call 401 "บัญชีนี้ถูกปิดใช้งาน"; cold login generic. 🔴 5 FINDING: FE change-password calls POST /auth/me/password on NEXT_PUBLIC_API_URL=som.develyst.online/api ⇒ /api/auth/me/password returns NextAuth 400 (no rewrites, [...nextauth] catch-all owns /api/auth/*; only /login routed to backend). GET /api/auth/me same. So change-password + /me grant-refetch UNREACHABLE through frontoffice on sid — check 5 likely broken; nav auto-refresh (check 3, MenuGuard) shadowed. Backend routes exist/pass BE tests — a sid proxy/routing gap for the Stage-2 /auth/me* routes. Sober/owner to fix before uat. FE nav/shell/refusal renders driven by confirmed menu data + 403 (not screenshotted for restricted user — no form login). Footprint: qa-rbac-admin left DISABLED (0e9c38e6). Report in inbox/PM.md.

## Porter (PM) — Stage 2 QA: enforcement PASS (menu guard 403, nav-by-grant, shell, disable-reason); 🔴 FINDING — /api/auth/me + /api/auth/me/password shadowed by the frontoffice NextAuth catch-all (only /api/auth/login proxied) ⇒ change-password broken + /auth/me grant-refetch shadowed (security still holds via BE 403). Sent to Sober to fix (next.config rewrite OR move routes off /auth/*) as part of Stage 2 before uat; then Tanya re-checks. Stage 3 in progress.
- Stage 2 QA: enforcement pass; routing bug (`/api/auth/me*` shadowed by NextAuth) verified → moved to `/me` (TASK-383 BE, TASK-384 FE, XS; wrong-current ⇒ 400 WRONG_PASSWORD). Stage 3 cut: TASK-385 (BE action keys/route table/guard/permissions registry) @Jason, TASK-386 (FE can()/sweep/Actions checklist) @Fern.

## Jason (BE) — ✅ TASK-383 DONE (code): `/auth/me*` → `/api/me` + `/api/me/password` (new `routes/me.ts` under the normal guard, `/me*` excluded from the menu table, no alias — `/api/auth/me*` ⇒ 404); wrong current ⇒ `400 WRONG_PASSWORD`
**2328 pass / 0 fail**, 186 files · tsc 0 · 37 = 37. 24 pins in the Stage 2 test (re-pointed + the 404 / zero-menu-reaches-both / envelope-by-value pins); seven mutations bite; contract line for Fern in the TASK. Moving to TASK-385 (contract + key list first).

## Jason (BE) — 📋 TASK-385 contract + full key list (45: 44 route keys + `action:sales.discount`) in the TASK; one naming rule (preview shares the act; act + undo one key; create ≠ edit); three confirm questions to Sober (discount sentence, the status route's four body acts, the leave-override flag). Waiting on confirm before code.
- TASK-383 reviewed (2328/0). TASK-385 contract confirmed: 45 keys + 46th leave-override (body-level); status stays one key (list). Contract lines to @Fern for 384/386.

## Jason (BE) — ✅ TASK-385 DONE (code): RBAC Stage 3 — 46 `action:*` keys with TH/EN labels, `ROUTE_ACCESS` (menus + action, 58 mutate routes), `accessGuard` (menu then action, own sentence), discount + leave-override by key, `PUT /users/:id/actions`, `/me.actions`, `GET /permissions`
**2350 pass / 0 fail**, 187 files · tsc 0 · 37 = 37. `route-menus.ts` → `route-access.ts`; the enumeration pins every mutate route to a known key and every key to a route (the two body-level keys to none). Fifteen mutations bite. Contract lines for Fern in the TASK. Nothing waits for BE.
- TASK-385 reviewed (2350/0). 46 keys incl. leave-override; discount by key. Told @Porter; Fern has the contract. Waiting on 384/386.

## [--:--] Fern (FE) — ✅ **TASK-384 done — `/me` · `/me/password`; the 401 exemption retired; no `/auth/me` left in `src` (walk-pinned).** → @Sober
403/0 (count unchanged; pins re-pointed + one walk negative) · tsc 0 · build ok · 4 modified + a comment sweep. Mutations ×3
in `try/finally` (each path back, the exemption back) ⇒ 1–2 fails; md5 identical. Notes in the TASK §2. Starting TASK-386.

## [--:--] Fern (FE) — ✅ **TASK-386 done — ONE gate `can(action)` on every mutate control (68 key literals, 30 files, 44 of 46 keys; the two unused named); the `Actions` checklist from `GET /permissions`; the three action sentences told apart.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  411 pass / 0 fail   (was 403; +8)
bun run build     → ok
```
Since the TASK-382 commit: 53 modified · 2 new (TASK-384's 12 among them). Notes + the full site list by area in the TASK §3.

**§1:** `lib/rbac/actions.ts` `can()` (value-tested), `useCan()` reads `/me` (`actions` beside `menus`; the login body seeds).
🚫 No FE list of names: the sites carry key STRINGS; the test-only `ACTION_KEYS_SNAPSHOT` (46) is the BE's list — checked
equal, key for key, in order, by a script on this machine (46 = 46 `true`), stated in the report; `action-gate.test.ts`
walks `src` for every `"action:…"` literal and refuses one the snapshot lacks (a typo cannot open a control), pins 68 / 30
and that exactly two keys have no site: `people.student-create` (`POST /students` — the FE creates under the parent) and
`teachers.calendar-link` (no FE caller, the BE's ✱). Hidden, never disabled (a walk pins no `disabled={!can(`); where a hole
would show, the read stays (empty cell keeps its dashed border, work-days as one line, expiry as text). `calendar.status`
= Attended · Confirm · Sick leave · Cancel-with-reason (ONE act, as the BE); the ⋯ menus vanish when empty; the discount
gate is by key — the Stage-1 role reader is retired (no `role` in `DiscountSection`).
**§2:** `Actions` count per row ⇒ `ActionsModal` from `usePermissions()`: grouped by the registry's `area` under the
menu's own nav label (`sales` ⇒ one new word), rows = the BE's label in `lang`, select all per group / overall, the
orange "menu not granted" badge (ticking allowed, nothing auto-granted) ⇒ `PUT /users/:id/actions { keys }` filtered to
the registry.
**§3:** action / discount / **leave-override** (a third sentence the contract line did not name —
`ไม่มีสิทธิ์ยกเว้นกฎแจ้งลาล่วงหน้า`) pinned distinct from the menu one and ABSENT from both dictionaries; every site shows the
server's words. Keys: `users` +8 (54) ×2.
🔑 **Break-and-watch ×12 in `try/finally`** ⇒ 1–3 fails each (12 slipped the first run — a create/edit-form pin added);
md5 identical on 9 files. Four earlier pins moved to the keyed shapes (TASK-374, 372, 287, 365), each named in its file.

### Q — no control calls two mutate routes. The inverse, twice: `student-create` and `calendar-link` are keys with no
control (ticks that change nothing on a screen) — hide from the checklist or retire, the owner's/BE's call. And one key
broader than its control: `calendar.status` bundles attended/confirm/leave/cancel — a registry split if the owner wants
"attended but not cancel". Named, not built.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-384 + 386 reviewed (411/0). Stage 3 complete on both sides; handed to @Porter for Tanya on sid. Stage 4 on go.

## Porter (PM) — 🟢 RBAC Stage 3 complete (TASK-383/384/385/386): 46 action keys, per-action guard, discount=grant, /me routing patch. BE 2350/0 · FE 411/0 · no migration. Owner to deploy sid; Tanya tests Stage 3 + the held Stage-2 checks (change-password, proactive nav). Then Stage 4 (roles + per-user matrix, migration 0037) = the last of option C. Noted for owner: leave-override is now a tickable permission (46th key).

## Porter (PM) — owner: sid deployed (Stage 3). @Tanya GO.

## Tanya (QA) — ✅ **RBAC Stage 3 + Stage-2 /me re-checks ALL PASS. My Stage-2 finding fixed (routes moved to /api/me).** /me re-check: GET /api/me 200; change-password wrong current ⇒ 400 WRONG_PASSWORD "รหัสผ่านปัจจุบันไม่ถูกต้อง", token stays valid (not signed out); right ⇒ ok, new login works. Stage 3: 1) no actions ⇒ POST /bookings 403 "ไม่มีสิทธิ์ทำรายการนี้". 2) grant action:calendar.book ⇒ 201. 3a) no sales.discount ⇒ discounted create 403 "ไม่มีสิทธิ์ให้ส่วนลด"; no-discount create 201. 3b) no leave-override ⇒ sick-leave override:true 403 "ไม่มีสิทธิ์ยกเว้นกฎแจ้งลาล่วงหน้า". 4) super admin everything; GET /api/permissions 200 {menus,actions[{key,area,labelTh,labelEn}]}. FE renders (+/⋯/discount/override/checklist) follow /me actions + 403 guards (not screenshotted for restricted user — no form login). Footprint: qa-rbac-admin DISABLED (pw now qa-admin-pw-3); bookings swept. Stage 3 clean; ready for Stage 4/uat. Report in inbox/PM.md.

## Porter (PM) — 🟢 RBAC Stage 3 GREEN on sid + Stage-2 /me finding fixed (routes moved to /api/me). GO to Sober on Stage 4 (role builder + per-user matrix, migration 0037) = finale of option C. After Stage 4 green on sid → single uat cutover for all of C. qa-rbac-admin pw now qa-admin-pw-3 (disabled).
- Stage 3 green on sid. Stage 4 cut: TASK-387 (BE roles/live model/migration 0037) @Jason, TASK-388 (FE Roles page/assign/matrix) @Fern. Decisions: live role, additive overrides, delete-in-use refused with count.

## Jason (BE) — 📋 TASK-387 contract in the TASK: migration `0037_roles.sql` (five statements, witness `role_permissions_role_key_uq`, `users.role_id` RESTRICT — the ALTER's lock named), ONE-statement effective read (own UNION role), `RoleDTO`/`/roles` CRUD/`PUT /users/:id/role`/`grants { fromRole, own }`/`/me.roleName`, no matrix route. Two small ❓ (name case-insensitivity, an index on `role_id`). Waiting on confirm before code.
- TASK-387 contract confirmed (case-insensitive role names; one UNION read). Contract lines to @Fern for 388.

## Jason (BE) — ✅ TASK-387 DONE (code): RBAC Stage 4 — migration `0037_roles` (38 = 38, witness `role_permissions_role_key_uq`, the `users` ALTER's lock named), a LIVE role (`effectiveGrantKeys` = one UNION; the guard reads it per request), `/roles` CRUD (delete-in-use ⇒ 409 with the count), `PUT /users/:id/role`, `grants { fromRole, own }`, `/me.roleName`
**2375 pass / 0 fail**, 188 files · tsc 0 · 38 = 38 · preflight clean. 25 new pins; seven `37 = 37` pins and the guard's spies re-pointed. Seventeen mutations bite (one passed first — an `indexOf` order pin without a presence assert; fixed). Contract lines for Fern + the owner's-list line in the TASK. Nothing waits for BE.
- TASK-387 reviewed (2375/0; 38=38 0037_roles). Migration facts to @Porter. Waiting on TASK-388.

## [--:--] Fern (FE) — ✅ **TASK-388 done — the Roles page (builder on the SHARED checklists + the matrix tab), a `Role` Select per user, two-tone checklists (role keys locked, own rows editable, save writes own only), the role's name in the header. Option C's FE is complete.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  420 pass / 0 fail   (was 411; +9)
bun run build     → ok — `○ /scheduler/roles`
```
13 modified · 11 new. Built against Jason's confirmed contract (TASK-387). Notes in the TASK §4.

**§1:** `roles` nav entry, `superAdminOnly` beside `users` (the Stage-1 "one flag-gated entry" pin ⇒ two). The checklist
bodies EXTRACTED into `partials/Users/GrantChecklists.tsx` (`MenusChecklist` · `ActionsChecklist`); the Users dialogs and
the Roles builder mount the same — no third list (asserted). Create ⇒ `POST /roles { name, description?, keys }`; edit ⇒
`PATCH` only what changed, `keys` (REPLACE) only when the set differs; delete two taps, `ROLE_IN_USE`'s count sentence in
the dialog (the server's). A role is LIVE: every write invalidates `roles` + `users`.
**§2:** a `Role` Select per row ⇒ `PUT /users/:id/role { roleId | null }` (`null` on *none*); the two dialogs seed OWN
rows and LOCK `grants.fromRole` ("from role X"); the save lines are byte-identical — own rows only. Value-tested on the
pure `isTicked` / `withKeys`: a locked key never moves, select-all covers only the free keys, the save never carries the
role's key. Header: `roleName` under the display name from `/me`.
**§3:** `MatrixTable` read-only (● own · ▲ role · grey ● super admin), sticky lead column, registry order; `matrixCell`
pure (own beats role) value-tested; a RENDERED assertion (2 users × 15 columns: 2 ▲ · 1 ● · 12 blank / 15 grey ●, no
`<input>`/`<button>`); from `GET /users` + `GET /permissions`, no new route; filter by role. Keys: `roles` 26 · `nav.roles`
· `users` +4 (58) ×2.
🔑 **Break-and-watch ×14 in `try/finally`** ⇒ 1–4 fails each; md5 identical on 8 files.

### Q — three example roles named in the TASK (Front desk · Coach lead · Accountant) with their menus/actions off the
registry's own labels — examples only, the owner creates them at runtime.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-388 reviewed (420/0). OPTION C COMPLETE (BE 2375/0, 38=38; FE 420/0). SPEC-079 §5 uat cutover note written; facts recorded; handed to @Porter for Tanya then uat.

## Porter (PM) — 🎉 RBAC option C COMPLETE (all 4 stages built): Stage 4 TASK-387 BE 2375/0 (MIGRATION 38, 0037_roles) + TASK-388 FE 420/0 — role builder, assign, locked-from-role ticks, live role edits, delete-in-use refused, matrix. Owner to deploy sid (migrate 0037 → verify=38 → restart). Tanya tests Stage 4. Then the single uat cutover (SPEC-079 §5: migrate 0036+0037 one run → bootstrap → create roles+staff in one sitting → remove ADMIN_*). Then back to rental/other fixes.

## Porter (PM) — owner: sid Stage 4 deployed, db:verify=38 (0037_roles, clean single run). @Tanya GO on Stage 4.

## Tanya (QA) — ✅ **RBAC Stage 4 (roles) ALL 5 PASS — Option C QA-clean end to end on sid.** 1: role[2 menus+3 actions] assigned ⇒ effective menus[calendar,bookings]+actions[book,status,discount]; DTO grants.fromRole=5 keys, own=[]. 2: PUT own menus [reports] ⇒ grants.own=[reports] only, effective=3 (role∪own). 3: PATCH role +people ⇒ same token /me has it (live, no re-login); detach ⇒ own [reports] remains. 4: delete HELD role ⇒ 409 ROLE_IN_USE "มีผู้ใช้ 1 คนถืออยู่ — ย้ายก่อนลบ"; dup name by case ⇒ 409 ROLE_NAME_TAKEN "มีบทบาทชื่อนี้แล้ว". 5: matrix grants{fromRole▲,own●}; super admin menus 12/actions 46, grants empty (all grey ●, no role). Footprint: qa-rbac-admin DISABLED (pw qa-stage4-pw); all created roles DELETED (none left). Option C (all 4 stages) verified on sid; ready for uat cutover. Report in inbox/PM.md.

## Porter (PM) — 🎉 RBAC OPTION C QA-CLEAN end-to-end on sid (Stages 1-4 all passed). Ready for the single uat cutover (SPEC-079 §5). Asked the owner: cutover uat now (prep staff accounts) OR clear the pending rental/other fixes first and cutover later. Both engineers idle.

## Porter (PM) — 🔵 REQ-093: customer wants to delete students WITH history (REQ-089 §3 blocks it) — real case is a duplicate child with only a cancelled course. Flagged the money tension (course creation posts a sale; cancel may not reverse it → a delete strands a ledger row). Asked Sober: does cancel reverse the sale?, what a delete strands, size soft-archive vs delete-when-no-live-money vs merge. Asked owner to lean archive (ก) vs delete-when-no-money (ข). Nothing dispatched.

## Porter (PM) — 🔵 two more from the customer: REQ-094 (extended/purple auto check-in — read why excluded) + REQ-091 §14 rental feedback round 2 (1 teacher told in course-confirm; 2 whole-course UNPAID/pay-per-session variant; 3 remove rental from remaining sessions mid-course, NO money change per owner). Sent to Sober for reads/sizing. Nothing dispatched; ordering vs RBAC cutover/REQ-093 open.

## Porter (PM) — owner sequencing: finish ALL pending cases (REQ-093 delete-with-history, REQ-094 extended auto check-in, REQ-091 §14 rental round 2) on sid FIRST, THEN the single uat cutover (RBAC option C + all of these together — one staff-login break). Awaiting Sober's reads before dispatching builds.
- Sober: three reads to @Porter — REQ-093 (cancel reverses no money; shapes a archive S / b hard-delete breaks principle / c merge M; recommend a now, c if recurring), REQ-094 (bulk-confirm skips EXTENDED → never CONFIRMED → job never attends; fix = bulk-confirm accepts EXTENDED, XS), REQ-091 §14 (three S-sized; derive-and-copy would re-inherit after removal — marker needed). Nothing cut.

## Porter (PM) — GO to Sober on REQ-094 (bulk-confirm EXTENDED like PENDING, XS) + REQ-091 §14 (teacher+parent rental line in course-confirm; whole-course UNPAID variant; DELETE course rental on future sessions no-money — Sober picks derive-vs-marker fix). REQ-093 held for owner's ก(archive, recommended) vs ค(merge) pick; (b) delete-when-no-money dropped (cancelled course keeps its sale). All builds on sid; single uat cutover after.

## Porter (PM) — owner: REQ-093 = shape (a) ARCHIVE (deferred to my rec). GO to Sober: students.archived_at (migration +1), hide from pickers/lists/calendar/roster count, history+ledger untouched, un-archive one tap; existing history-free Delete stays. Joins 094+§14 on sid; single uat cutover after all. Merge(c) listed for later; hard-delete(b) not built.
- GO on REQ-094, REQ-091 §14, REQ-093(a). Cut TASK-389 (bulk-confirm EXTENDED, XS), TASK-390/391 (rental round 2: confirm line, paidUpfront, remove-remaining + `rental_removed_at` marker migration 0038, key course-rental), TASK-392/393 (archive student, migration 0039, key student-archive, refused with live future sessions). Cutover = 4 migrations one run, verify 40.

## Jason (BE) — ✅ TASK-389 DONE (code): `preCheckBulkConfirm` proceeds for EXTENDED like PENDING (one line); the job's CONFIRMED-only select byte-frozen by pin. 2382/0, 189 files, tsc 0, 38 = 38. Five mutations bite. Moving to TASK-390's contract.

## Jason (BE) — 📋 TASK-390 contract in the TASK: rendered `rental` line on the `course_confirmed` payload (last line, both audiences); `paidUpfront` (default true; a second column `rental_paid_upfront` proposed in `0038` beside the marker — or derive, Sober's call); `DELETE /courses/:id/rental` = marker + delete future live rows, no ledger; `inheritCourseRental` honours the marker by join; key `action:bookings.course-rental` (47th). Waiting on confirm.
- TASK-389 reviewed (2382/0). TASK-390 contract confirmed (store paidUpfront in 0038; join for the marker; key label). Contract lines to @Fern for 391.

## Jason (BE) — ✅ TASK-390 DONE (code): rental round 2 — `Rental :` last line in `course_confirmed` (both audiences, unrented byte-identical); `paidUpfront` variant stored (`0038`), rows born unpaid, no post at creation; `DELETE /courses/:id/rental` = marker + future live rows, no money; the marker honoured by join in `inheritCourseRental`; key `action:bookings.course-rental` (47th)
**2400 pass / 0 fail**, 190 files · tsc 0 · **39 = 39** (`0038_course_rental_marker`, witness the last column, the `course_packages` lock named) · preflight clean. 19 new pins; nineteen mutations bite (one passed first — the stored-vs-derived trap; pinned). Contract lines for Fern in the TASK. Moving to TASK-392's contract.

## Jason (BE) — 📋 TASK-392 contract in the TASK: `0039_student_archive` (two columns, witness `archived_by`, cutover order named), archive/unarchive under one key (48th), the live-future-sessions 409 with the count, the working reads enumerated (picker + `?archived=true`, eligible, `listStudentsOfParent` and its seven callers, attention) vs the history reads untouched. Four ❓ to Sober. Waiting on confirm.
- TASK-390 reviewed (2400/0; 39=39). TASK-392 contract confirmed (voucher allowed; archivedStudents; SOM untouched; create guard built). Lines to @Fern for 391/393.

## Jason (BE) — ✅ TASK-392 DONE (code): archive a student — `0039_student_archive` (40 = 40, witness `archived_by`, cutover order in both headers), archive/unarchive under one key (48th), `409 STUDENT_HAS_LIVE_SESSIONS` with the count, the working reads hidden by name (picker + `?archived=true`, eligible, the one `listStudentsOfParent`, attention, check-in; parent detail splits `students`/`archivedStudents`), history untouched, the create guard at three sites
**2420 pass / 0 fail**, 191 files · tsc 0 · 40 = 40 · preflight clean. 20 new pins; eighteen mutations bite. Contract lines for Fern in the TASK. Nothing waits for BE.
- TASK-392 reviewed (2420/0; 40=40). All BE for the cutover in. SPEC-079 §5 updated to 0036–0039 / verify 40. Waiting on Fern 391+393.

## [--:--] Fern (FE) — ✅ **TASK-391 done + TASK-393 done — rental round 2 (paid upfront / pay per session; remove from remaining sessions) and archive a student (Archive · Show archived · Restore).** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  429 pass / 0 fail   (was 420; +4 round 2, +5 archive)
bun run build     → ok
```
18 modified · 3 new across the two. Built against Jason's confirmed contracts (TASK-390 · TASK-392). Notes in each TASK §2.
**391:** the `SegmentedControl` beside the tier picker (default upfront); `paidUpfront` in the body both ways (asserted on true and
false through `JSON.stringify`); the card's line `(Paid upfront)` / `(Pay per session · n to collect)`; the red `Remove from
remaining sessions` inside `c.rental && (`, by `action:bookings.course-rental` + the lifecycle guard, two taps ⇒ `DELETE
/courses/:id/rental`, "no money is changed" in the dialog (pinned), the notice = `res.removed`, `invalidateAll`. `rental` +8 ×2.
**393:** the red `Archive` beside the trash (`action:people.student-archive`) ⇒ dialog (history and money stay) ⇒ `POST
/students/:id/archive`; the 409 count sentence in the dialog; `Show archived` remembered through the calendar's `boolStore`
(exported — the only change there) ⇒ `archivedStudents` from the same read, dimmed, one-tap `Restore` ⇒ `…/unarchive`; a walk of
`src` pins NO client-side `archivedAt` outside People (the server filters). `people` +9 ×2.
The snapshot went 46 → 48 (47th + 48th) — 48 = 48 vs the BE's `ACTION_KEYS` by script, in order. Pins moved with the reason:
TASK-374's body pins (+`paidUpfront`), TASK-365's gap negative (strips the archive block), the sweep 68 → 71 / 30 files.
🔑 **Break-and-watch ×9 + ×9 in `try/finally`** ⇒ 1–4 each; md5 identical on all files.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-391 + 393 reviewed (429/0). All cutover work built (BE 2420/0, 40=40; FE 429/0). Handed to @Porter for Tanya → uat. Facts recorded.

## Porter (PM) — 🟢 all three cases BUILT: REQ-094 (TASK-389), REQ-091 §14 (TASK-390 BE mig 0038 + 391 FE), REQ-093 archive (TASK-392 BE mig 0039 + 393 FE). FE 429/0 · BE 2420/0 · migrations to 40. Two new action keys (47 course-rental, 48 student-archive). Owner to deploy sid (0038+0039, verify=40); Tanya tests the three. Then single uat cutover (SPEC-079 §5: 0036-0039 one run, verify 40, bootstrap, roles+staff, remove ADMIN_*). Two Sober defaults noted for owner (archive refused with classes ahead; STUDENT_ARCHIVED post guard).

## Porter (PM) — owner: sid deployed, db:verify=40 (0038+0039 clean single run). @Tanya GO on REQ-094/§14/REQ-093.

## Tanya (QA) — ✅ **Three-piece round (REQ-094 · REQ-091 §14 · REQ-093) ALL PASS.** REQ-094: bulk-confirm an EXTENDED make-up ⇒ CONFIRMED (was manual); 17:30 attend is the internal job (not QA-triggerable). §14: 1) pay-per-session ⇒ all RED, unpaidSessions=4, ZERO ledger at create; 2 paid ⇒ unpaidSessions=2 + 2 SALE lines @200. 2) remove-remaining ⇒ {removed:2}, delivered-paid stay green, ledger unchanged, later sick-leave make-up noR (marker honoured); remove targets future PENDING/CONFIRMED/EXTENDED (ATTENDED kept). 3) paidUpfront ⇒ ONE SALE @800 at create (async post), remove posts nothing. 4) confirm Rental: = DTO/enqueue; text owner-on-uat. REQ-093: 1) archive w/ class ahead ⇒ 409 STUDENT_HAS_LIVE_SESSIONS "มีคาบเรียนข้างหน้า 1 คาบ…"; cancel ⇒ archive; hidden from picker, struck under Show-archived. 2) unarchive ⇒ back; 6th on restore ⇒ 400 "เพิ่มนักเรียนได้สูงสุด 5 คนต่อเบอร์". 3) booking+course POST for archived ⇒ 409 STUDENT_ARCHIVED; past history reads. 🔎 access-file BACKOFFICE pw wrong: admin-som fails, backoffice uses admin/admin (frontoffice super-admin=admin/admin-som; separate systems). Footprint: parent 32eeed5f SUSPENDED + children ARCHIVED; test courses cancelled; ledger sales declared. Report in inbox/PM.md.

## Porter (PM) — 🎉 ALL cases QA-clean on sid: Option C (RBAC 4 stages) + REQ-094 + REQ-091 §14 + REQ-093. Gave the owner the single uat cutover checklist (SPEC-079 §5: migrate 0036-0039 one run→verify 40→BOOTSTRAP_ADMIN_* 8+ in frontoffice\back→restart→bootstrap super admin→create roles+ALL staff accounts in one sitting (staff locked out until done, do at a quiet hour)→remove ADMIN_*; rental tier already on uat). Owner to pick the cutover hour. Post-cutover: owner checks rental LINE text on phone. Note: H:/sm-test-access.txt backoffice pass is wrong (backoffice still admin/admin; frontoffice admin-som).

## Porter (PM) — 🔵 REQ-095 "Other" schedules (ECA/Camp/DUO/Group/Free-KOL) from customer PDF. Recorded + axes table. Asked owner 5 Qs (teacher pay ours/backoffice; ECA/Free non-revenue slot; Camp packages; DUO/Group grouping; order). Asked Sober feasibility read. Nothing dispatched.

## Porter (PM) — REQ-095 §4 answers: money→backoffice (teacher rate on the schedule, editable, passed to backoffice as expense — PM's lock, owner confirming the "which part"); ECA/Free = reserve teacher slot, no class/kids (count), blocks slot; Camp = NEW day-based "Balance camp" course (Full/Half × Full-week/Daily), cut by day, unused days carry over / pause; DUO = 2 kids same course, DUO/Group also per-session, teacher swappable, rate editable; order ECA/Free→DUO/Group→Camp. Enriched Sober's read. One open: owner confirms the Q1 "rate on schedule not course-create" lock.

## Porter (PM) — REQ-095 §4.1: owner LOCKED Q1 (teacher rate on the schedule, editable, passed to backoffice; no money reporting here) + "lets go". GO to Sober: feasibility read + staged plan + cut ECA/Free first (non-revenue teacher-slot booking, label+note+count, rate stored-for-backoffice), then DUO/Group, then Camp (day-based carry-over/pause). All on sid; joins the held uat cutover.

## Porter (PM) — 🔴 uat PRODUCTION BUG: 17:00 class not cut by end-of-day though set to 17:30 and job_runs has a row. Paused REQ-095. Escalated to Sober: does jobs.service.ts gate per-class-end (⇒ 17:30 too early for a 17:00-18:00 class, a config call) or attend all-of-today (⇒ a timezone/business-date bug)? Requested from owner: job_runs row timestamp + count, and the 17:00 class's slot end + status.

## Porter (PM) — 🔴 uat BUG: 17:00 class not cut though end-of-day=17:30 and job_runs has a row. Paused REQ-095. Escalated to Sober (per-class-end gate ⇒ 17:30 too early/config, vs all-of-today ⇒ tz/date bug). Asked owner for job_runs timestamp+count + class slot/status.
- uat 'bug' decided: jobs.service gates per-class on end_time <= now (Bangkok); a 17:00–18:00 class at 17:30 is correctly skipped and cut next day. Config call: 18:30 back, or accept next-day. No build.
- REQ-095: SPEC-080 written (OTHER exists = ECA/Free shape; no pay rate; one-live-booking-per-slot blocks DUO/Group → group-session object L+L; Camp = day entitlement L+L). Stage 1 cut: TASK-394 BE (migration 0040, 49th key) + TASK-395 FE. Expense pass = owner decision; stored only.

## Jason (BE) — 📋 TASK-394 contract in the TASK: `0040_other_schedule` (five nullable columns, witness `booking_teachers.rate_minor`, the HOT `bookings` lock named), the rates as one map keyed by teacher, a dedicated `PATCH /bookings/:id/other`, the series = one tx over `insertBooking` per date with the clash date in the 409, key `action:calendar.other-series` (49th), DTO `other {…}`, no money by absence. Three ❓ to Sober. Waiting on confirm.
- TASK-394 contract confirmed (rates map; separate /other edit route; Heads line gated-in). Lines to @Fern for 395.

## Jason (BE) — ✅ TASK-394 DONE (code): ECA/Free/KOL on the OTHER booking — `0040_other_schedule` (41 = 41, five nullable columns, witness `booking_teachers.rate_minor`, the HOT `bookings` lock named), `otherKind`/`headCount`/one `teacherRates` map (lesson types refuse), `PATCH /bookings/:id/other` (no notice), `POST /bookings/other-series` (one tx over `insertBooking`, first clash ⇒ 409 naming the date), key `action:calendar.other-series` (49th), DTO `other {…}`, `Heads : n` placeholder in the reminder, no money by absence
**2441 pass / 0 fail**, 192 files · tsc 0 · 41 = 41 · preflight clean. 21 new pins; twenty mutations bite. Contract lines for Fern + the owner's-list items in the TASK. Nothing waits for BE.
- TASK-394 reviewed (2441/0; 41=41 0040 on the hot bookings table). SPEC-079 §5 → 0036–0040, verify 41, quiet-moment line. Waiting on TASK-395.

## [--:--] Fern (FE) — ✅ **TASK-395 done — ECA · Free · KOL on the OTHER form (Kind · Head count · Rate per teacher, stored not posted), `Create series` with a multi-date picker (one call, all or nothing, the 409 keeps the ticks), a details editor through its own route, the kind tag on the cell + legend.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  435 pass / 0 fail   (was 429; +6)
bun run build     → ok
```
15 modified · 5 new. Built against Jason's confirmed contract as it arrived via you (the ONE `teacherRates` map · `PATCH
/bookings/:id/other` · `other-series` without `endTime`) — the TASK's proposed line differed on three names; the confirmed
shapes won. Notes in the TASK §2.
`lib/scheduler/other-schedule.ts` pure + value-tested (facts only when set, head count 0 is a value, satang via the ONE
`bahtToMinor`, the patch = only what changed). ONE shared `OtherScheduleFields` on the create form / the series dialog / the
details editor; the rate's hint says "Stored for the backoffice; not posted here." (pinned both languages). `Create
series` behind `action:calendar.other-series` ⇒ `DatePicker type="multiple"` with the count ⇒ ONE `POST /bookings/other-series`
(dates sorted, no `endTime`); a 409 shows the sentence and the ticks stay. The OTHER view shows Kind/Heads + a pencil
(booking-edit) ⇒ `PATCH /bookings/:id/other` — its own route, not the move; Save disabled until dirty. `OtherKindTag` on
both grids from `other.kind` only; the legend's three tags. Keys `booking` +19 · `calendar` +3 ×2. Snapshot 49 = 49 vs the
BE by script.
🔑 **Break-and-watch ×13 in `try/finally`** ⇒ 1–3 each (one slipped first — the `<=` negative — tightened); md5 identical.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-395 (FE, OTHER kind/heads/rate + series + details editor + legend) re-verified: 435/0, tsc 0, build ok, snapshot 49 = 49 ⇒ **✅ DONE — REVIEWED**. **REQ-095 Stage 1 complete both sides** (BE 2441/0, 41 = 41). Told @Porter with Tanya's `sid` shape and the owner's list (expense pass timing, DUO own courses, Camp "open week", OTHER block duration). Chain stopped; ball @Porter.

## Porter (PM) — uat "17:00 not cut" RESOLVED = NOT a bug. jobs.service.ts:40 gates per-class end_time<=bangkokNow(); a 17:00-18:00 class ends 18:00 > 17:30 run ⇒ correctly skipped, cut the NEXT day's run (1 day late, nothing lost). Config call to the owner: (a) end-of-day back to 18:30 (correct value, after the 18:00 booking limit — PM recommends) vs (b) keep 17:30, last two slots cut next morning. Sober reconciles the SYSTEM-FACTS 17:30/18:30 blocks once owner picks. Note: Sober already built REQ-095 Stage 1 (TASK-394 BE mig 0040 / 395 FE) — waiting; the uat cutover note is now 0036-0040 verify 41, bookings hot ⇒ quiet moment.

## Porter (PM) — owner's decision on end-of-day: NOT 18:30 — change the job to gate on START time (start_time<=now) not END, so the 17:00/17:30 slots are cut at the 17:30 run before the team leaves. Conscious override of REQ-070 (attend-after-end), justified by staff on-site reviewing first. Dispatched to Sober (XS + check nothing else relied on end-gate + reconcile SYSTEM-FACTS to 17:30/start-based). Deploys to uat (BE); with the cutover or sooner if owner wants.
- **@Sober** — Porter relayed the owner's ruling: end-of-day attends on `start_time <= now` (team leaves 17:30). Pre-flip read: the end-time gate has two mirrors only (`jobs.service.ts` SQL · `lib/auto-cut.ts` pure/test-only); nothing else reads it. **TASK-396 cut to @Jason** (XS + pins, no migration); board row added; SYSTEM-FACTS reconcile queued for after the build. Ball @Jason.

## Jason (BE) — ✅ TASK-396 DONE (code): end-of-day auto-attend gates on START time (`startTime <= now`) — both mirrors flipped (`jobs.service.ts` SQL + `lib/auto-cut.ts`), the owner's ruling recorded as a conscious REQ-070 override; the ruling pinned as a value (17:00 class at 17:30 ⇒ due; 17:45 ⇒ not) + the SQL branch by source. 2443/0, tsc 0, 41 = 41. Five mutations bite. Nothing waits for BE.
- **@Sober** — TASK-396 re-verified (2443/0, tsc 0, 41 = 41; both gate mirrors on START) ⇒ **✅ DONE — REVIEWED**. SYSTEM-FACTS end-of-day blocks reconciled: 17:30, start-based, 18:30 paragraphs marked history verbatim, new coupling rule (trigger ≥ last slot START). Told @Porter it is deploy-ready (BE only). Ball @Porter.

## Porter (PM) — TASK-396 (start-based end-of-day) IN, BE-only no migration. Plan: deploy BE sid → Tanya verifies → deploy BE uat ALONE (the customer's reported issue; no migration, no need to wait for the RBAC cutover). @Tanya pre-staged.

## Porter (PM) — owner: GO Stage 2 (DUO/Group), batch sid deploy (396 + Stage1 + Stage2). Confirmed §5: DUO/Group each child keeps own course, grouped in one slot, deduct own; per-session variant; teacher swappable; rate STORED only (not posted, backoffice later). Sober building Stage 2; then Tanya tests all three; then Stage 3 Camp; then uat cutover.
- **@Sober** — Porter: GO Stage 2 (DUO/Group), decisions REQ-095 §5. Wrote **SPEC-081** (a GROUP booking row holds the slot; children's sessions are SEATS with `group_id`, outside the unique index — `0041` rebuilds the index, closed shop) and cut **TASK-397** (BE L, contract-first) to @Jason; FE task after the confirmed contract. Five owner decisions flagged to @Porter (§4). Ball @Jason.

## Jason (BE) — 📋 TASK-397 contract in the TASK: §1 verified — FIVE slot-index mirrors, one already drifted (`getSlotAvailability` reads `SLOT_NON_BLOCKING`, no PAUSED); the one `slotHolder` predicate folds them (drift fixed as a named side effect). `0041` four statements (enum label alone · `group_key` · `group_id` + index · the index rebuild, closed shop, predicate witness); column not table; extend on a missing date; `other_kind` reused for DUO/GROUP; the flows confirmed with sentences. Waiting on confirm.
- **@Sober** — TASK-397 contract from @Jason verified and CONFIRMED (four-statement `0041`; ONE `slot-holder` predicate across five mirrors — folds a pre-existing PAUSED drift in the picker; `other_kind` reused with the type⇔kind pin; extend on missing date). **TASK-398 (FE, L)** cut to @Fern against the final contract. Porter told of the drift for Tanya. Ball @Jason (build).

## Jason (BE) — ✅ TASK-397 DONE (code): the GROUP SESSION — `0041_group_session` (42 = 42; the label alone, the slot index rebuilt `AND group_id IS NULL` — closed shop, predicate witness, TRAP 1 for 0042+), the ONE `slot-holder` predicate over five mirrors (the PAUSED picker drift fixed in passing), type ⇔ kind, the series, seats via `POST /courses { groupKey }` (cap / extend), the swap (seats follow), the cancel cascade, no seat freelance draw, DTO `group {…}`, the coach's folded entry, key `action:calendar.group-series` (50th)
**2466 pass / 0 fail**, 192 files · tsc 0 · 42 = 42 · the preflight on the whole 0038 → 0041 batch clean. ⚠️ Two deltas for Sober before Fern wires: the swap sends NO notice (`moveBooking` never did); a seat draws no freelance hour (a rule the contract did not name). 25 new pins; twenty-four mutations bite (one passed first — the extend branch's reachability; pinned). Nothing waits for BE.
- **@Sober** — TASK-397 re-verified (2466/0, tsc 0, 42 = 42; `booking-slot.ts` retired; `slotHolderWhere` at the mirrors) ⇒ **✅ DONE — REVIEWED**. Accepted the two build rulings (seat draws no freelance hour; swap sends no notice — owner's list for the words). Released @Fern to wire TASK-398 with the two notes. SYSTEM-FACTS: the one-predicate rule + the fixed PAUSED drift + `0041` closed shop. Porter told (cutover now `0036…0041`, verify 42; Tanya's `sid` checks). Ball @Fern.
