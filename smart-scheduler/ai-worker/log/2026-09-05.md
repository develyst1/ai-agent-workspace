# 2026-09-05

## [--:--] Porter (PM) — 📋 Fast-confirm batch: 7 contradictions where the owner's own words already exist in a log → @owner
Checked all 45 entries in `SYSTEM-FACTS-CONTRADICTIONS.md` for the `_(unanswered)_` marker: **6 answered, 39 still open** (matches the file's own ledger).

Of the 39, **7 have the owner's words already on the record** — I am NOT settling them (the file forbids that);
I am putting his own quote in front of him so he can close each with one word. Quotes located:

| # | question | owner's words on record | where |
|---|---|---|---|
| C-35 | canonical branch | *"ฉันจะรวม develop ทั้งหมด ทุก repo... branch กลาง คือ develop"* | log/2026-08-25.md:1358 |
| C-29 | teacher/program restriction | *"ตั้งใจจำกัด"* | log/2026-08-29.md:1340 |
| C-23 | teacher clash: warn or refuse | *"ตามนั้น"* (= option ข, refuse-for-now) | log/2026-09-01.md:1839 |
| C-41 | commit rule | *"commit แล้ว บอกทีม เลิกยุ่งเรื่อง commit ฉันจะทำเองเมื่อถึงเวลาของฉัน"* | log/2026-09-01.md:1094 |
| C-12 | where the LINE webhook points | *"ตอนนี้ไลน์ webhook ผูกที่ sid ถาวรแล้ว"* | log/2026-09-01.md:1852 |
| C-27 | max 5 students per phone | *"ฉันสั่งนายทำไว้เองแหละนานแล้ว"* | log/2026-09-02.md:1807 |
| C-03 | day-end time | ⚠️ **no verbatim in the log** — only my write-up that he changed it himself and the app books to 18:00 | log/2026-09-02.md:1726 |

⚠️ **C-03 is the weak one** — the log carries *my* sentence, not his. It needs a real answer, not a confirmation.

🔴 **Blocking today: C-01 (what the day-end does with an unmarked booking) and C-03 (what time).**
Both are genuinely unanswered. Answering them unblocks REQ-078 **AC-9 · AC-4 · AC-5 · AC-21**, which are parked.

**Ball: the owner.** Nothing here may be written into the contradictions file by me — only his dated answer closes an entry.

## [--:--] Porter (PM) — ✅ Owner closed EIGHT contradictions. REQ-078's block is LIFTED. → @Sober @Tanya
Owner, verbatim: *"ใช่ทั้ง 6 ข้อ C-01 auto-attend C-03 18:30 ฉันปรับเอง"*.
Recorded as dated `Owner's answer (2026-09-05)` lines in `SYSTEM-FACTS-CONTRADICTIONS.md` — **31 left open.**
Table of all eight: `SYSTEM-FACTS.md` § Owner-confirmed on 2026-09-05.

**What changes for the work:** `end-of-day` **auto-attends** (never `NO_SHOW`) at **18:30**.
⇒ `REQ-078` AC-9 · AC-4 · AC-5 · AC-21 are **no longer blocked** — every `23:30` in that REQ corrected to 18:30,
and the lift is written at the foot of the file.
⚠️ **If the code still writes `NO_SHOW`, that is now a DEFECT, not a fact** (`jobs.service.ts:47–61` read it that way on 08-23).

**Not settled by this, and still owed:** does the day-end's selection **exclude `OTHER`**? A source read.
Plus the fixture shape — `CONFIRMED`, not `PENDING`, created **before 18:30** — and one freelance rate for AC-21.

**Ball: @Sober** — the one source read (day-end: `NO_SHOW` or auto-attend in code, and does it skip `OTHER`).

## [--:--] Porter (PM) — 📋 Owner's list reconciled. 3 NEW items, 3 disagreements. → @owner
Owner pasted his full customer-facing list and asked for an update. Written into `OWNER-LIST.md`
§ RECONCILED 2026-09-05. **Not merged silently — the disagreements are listed.**

**NEW, never captured here:** **REQ-017** (ขยับวันหมดอายุทุกคอร์ส) · **FIX-008** (เช็คอิน→ลาป่วย ยังกินสิทธิ์) ·
**FIX-009** (ปุ่มนำคอร์สที่พักกลับมา — the un-drop half of his REQ-011, which we shipped drop-only).
⚠️ **FIX-008 is `C-24` in the contradictions file** (*can an attended session be undone?*) — his asking for it is
not the same as his answering it; the entry stays `_(unanswered)_` until he dates it.

**Disagreements:** REQ-005 he says ToDo → build complete, 18 ACs pass, parked · REQ-016 he says ToDo →
15/26 ACs verified, waiting on HIS rich-menu images · REQ-004 he says In Progress → reopened but **nobody is
working it**, the customer's actual complaint has never been stated.

Order unchanged. The three new items are **not** queued — placing them is the owner's call.
**Ball: the owner** (place the 3 new items · rich-menu images) · **@Sober** still owes the day-end source read.

## [--:--] Porter (PM) — 🔻 My customer-facing REQ-016 text carried a CANCELLED design. Owner caught it. → @Tanya
Owner: *"ฉันบอกให้เอา 2FA ออกละนี่"*. **Correct.** The 6-digit family code was cancelled 2026-09-02 (customer
refused; entry = **phone alone**), yet I copied his own stale note into a customer update **verbatim, unchecked.**
Fix + standing rule in `OWNER-LIST.md` § "The owner's own REQ-016 text is STALE".
**The rule: his list is the source for NUMBERS and for what the customer was told. `REQ-079` is the source for
what the system DOES.** Never copy the body of his items forward without checking the REQ.

## [--:--] Porter (PM) — 📤 The customer update sent on 2026-09-05 — what they were told
Owner approved and is sending it. Full text: `project-docs/2026-09-05-customer-update.md`. **This is now the
record of what the customer BELIEVES**, and any future status must be reconciled against it, not against the board.

**Two corrections I made to his draft before it went:** REQ-014 said *"รอรายการจากลูกค้า"* — **wrong, their list
landed 08-30** and we would have been tumbling back for something they already gave · the Completed count said
**18**; it is **17** (REQ 10 + FIX 7). The extra came from a vague *"+ งานย่อยอื่น"* I wrote myself.
**Two open questions with the customer:** REQ-004 *what exactly is wrong* · REQ-014 the OA move + rich-menu shape.

## [--:--] Porter (PM) — 📸 Owner's screenshot: a LINKED chat is still showing the OLD 6-cell menu → @Sober
`SOM-Balance-Demo`, 01:23. Bot had just replied `นักเรียนของคุณ (1/5) · 1. เตาไป๊` ⇒ **the chat is linked.**
Menu below it: `เช็คอิน · แจ้งลา · นักเรียนของฉัน / เพิ่มนักเรียน · ภาษา · ช่วยเหลือ` — **the REQ-015/042 menu,
cell for cell**, exactly what Sober read from `PARENT_RICH_MENU` on 09-02.
⇒ **Recorded as `C-13` Side E.** An observation, not a ruling — I did not close the entry.
**What it means for the ask on the table:** the A/B question is **moot until `publishRichMenus` runs** — a linked
chat and an unlinked chat see the SAME menu today, so the owner does not need to unlink anything to see menu A.
⚠️ **The live menu has no `คุยกับแอดมิน` cell** — REQ-079 §12 calls that the one cell whose position is not
negotiable, and `ภาษา`/`ช่วยเหลือ` are not in REQ-079 at all.

## [--:--] Porter (PM) — 🔻 The rich-menu images are OURS. My illustrator brief was wrong. → @Sober
Owner: *"ก็พวกนายนั่นแหละ ทำ ... อยากได้สีส้ม แค่นั้นแหละ"*. He is right.
`smart-scheduler-back` → `assets/line/` holds four PNGs **generated** by `generate-rich-menus.mjs` (palette,
icons and labels are edited in the script and re-run). **REQ-015 shipped this and I never read the README.**
⇒ `REQ-079` §12 corrected at the foot of the file; request re-aimed at Sober in `inbox/SA.md`.
🔴 **Root cause, same shape as the stale-2FA miss this morning:** I wrote a plan from what the requirement said
**without checking what the repo already had.** Twice in one day, both caught by the owner, not by me.
**What actually changes:** no illustrator, no commission. **Two new generator entries + orange + one publish run.**
**Still unasked, and it is mine:** `ภาษา`/`ช่วยเหลือ` exist on the live menu and REQ-079 drops them — my choice,
never put to him.

## [--:--] Porter (PM) — ✅ Owner: keep `ภาษา`/`ช่วยเหลือ`. Menu B re-laid out, artwork now fully decided. → @Sober
Owner: *"เก็บไว้"* — reverses my own undocumented removal. Seven labels wanted, **six cells is the hard ceiling**
(2 rows at 2500×1686) ⇒ merged `ภาษา/ช่วยเหลือ` into one cell, **the pattern the teacher menu already ships**.
New table in `REQ-079` § "`ภาษา` and `ช่วยเหลือ` STAY"; supersedes §12 for menu B only. `คุยกับแอดมิน` still
bottom-right. Menu A unchanged.
**Artwork is now fully specified: layout · labels · orange.** Nothing on it is owed by the owner any more.

## [--:--] Sober (SA) — ✅ Day-end source read. **Both answers are the good ones, and there is a third the question did not ask.** → @Porter

Read `smart-scheduler-back` → `src/services/jobs.service.ts` (`runEndOfDayJob`) and `src/lib/time.ts`. No SQL, nothing run.

### 1. Auto-attend or `NO_SHOW`? — **it already auto-attends. NOT a defect.**

`jobs.service.ts:52` — `tx.update(bookings).set({ status: "ATTENDED" })`. **`NO_SHOW` has no writer left in the
file.** The 08-23 read was true when it was made; **REQ-070 / TASK-180 changed it since**, and the header says why
in the strongest terms the repo has:

> *"this used to write `NO_SHOW`, and that was a false claim about a child … it only told a family their child had
> not turned up because nobody pressed a button. On `uat` it did that to **15 real children in one weekend**."*

⇒ **The owner's 2026-09-05 ruling and the code already agree.** Nothing to fix, nothing to build. `NO_SHOW` stays
in the enum so historical rows still render — that is deliberate, not a leftover.

### 2. Does the sweep exclude `OTHER`? — **no. `OTHER` is in BOTH halves.** AC-9 is reachable.

There are **two** selects, and this is the answer to a question that has been open since 09-02:

| | filter | `OTHER` |
|---|---|---|
| **auto-attend** (`:47`) | `date = runDate AND status = CONFIRMED AND ended` | ✅ **no `bookingType` filter at all** — nothing can be excluded |
| **revenue** (`:110`) | `status = ATTENDED AND bookingType IN (FIRST_TRIAL, SINGLE_SESSION, OTHER)` | ✅ **named explicitly**, by TASK-225 |

📌 The อื่นๆ path takes its own branch (`postOtherBookingSale`) because **its price comes from the booking, not
from a product code** — and it posts on the same `rev:<bookingId>` key as the trial/single path, which is what
makes SPEC-069's *"revenue already posted"* warning cover อื่นๆ with no second lookup.
⇒ **@Tanya's ฿20 fixture will post.** `CONFIRMED`, before the run, is the correct shape — as you already had it.

### 3. 🔴 The thing nobody asked, and it is the one with a trap in it

**There is no `18:30` anywhere in the source.** `grep` over `src/` and `scripts/`: **zero hits** for `18:30`, and
zero for the old `23:30`. The job's own gate is **per booking** — `endTime <= now` — and **18:30 is a Windows Task
Scheduler trigger, an OS setting outside the repo.** That is exactly consistent with the owner's *"ฉันปรับเอง"*:
he changes it himself because it is his to change, and **no code change is owed by that ruling.**

⚠️ **But the trigger time and the bookable hours are now coupled, and nothing enforces it.** A booking whose end
time is **after** the trigger is not swept by that run — and the next day's run selects the **next day's** date, so
it is never swept at all: it stays `CONFIRMED` and **its revenue never posts.**

✅ **Safe today, with 30 minutes to spare:** `time.ts:5` `TIME_SLOTS` ends at **17:00**, and `endTime` is derived
as start **+1h** ⇒ **the last possible class ends at 18:00.** 18:30 clears it.
📌 **Why it is worth writing down anyway:** at the old **23:30** this gap could not exist; **at 18:30 it is 30
minutes wide.** Add one 18:00 slot to `TIME_SLOTS` — a one-line, entirely reasonable change — and **the last class
of every day silently stops being attended and stops posting revenue.** Nobody would connect the two.
⇒ **Not a defect and not a task today.** It is a fact for `SYSTEM-FACTS.md`: **the day-end trigger must stay later
than the last `TIME_SLOTS` end + 1h**, and the two live in different places (Task Scheduler / the repo).
🔁 Backfill exists if it ever bites: `runEndOfDayJob(date)` for a past date sweeps **all** of it (`runDate < today
→ true`).

**Ball back to @Porter** — nothing here needs a build. AC-9 · AC-4 · AC-5 · AC-21 are clear to test.

## [--:--] Sober (SA) — 🔴 The rich menus were never going to appear, and the missing images were not the reason. TASK-247 cut. → @Porter @Jason

Read `assets/line/generate-rich-menus.mjs` + its `README.md` + `src/lib/line-rich-menu.ts` (290 lines, all of it).
Nothing run, nothing published.

### The finding, and it changes what the deploy step is

`publishRichMenus` (`line-rich-menu.ts:191`) **creates only the four REQ-015 menus.** `UNKNOWN_RICH_MENU` and
`KNOWN_RICH_MENU` are defined by TASK-234, `MenuIds` has `unknownTH`/`knownTH` slots, `linkKnownRichMenu` **reads**
`ids.knownTH` — and **nothing anywhere writes them.** Three separate consequences:

1. **Neither new menu is ever created**, so `linkKnownRichMenu` finds no id and — best-effort by design — does
   **nothing, silently.**
2. `setDefaultRichMenu(parentTH)` — **the account default is the OLD 6-cell parent menu.** The file's own comment
   says *"ยังไม่รู้จัก is the DEFAULT menu … with no code running at all"*; the one call that sets an account
   default points somewhere else. **The design and the code disagree, and the code is what runs.**
3. `storeMenuIds` writes the **whole** object ⇒ a re-publish **erases** ids it did not create.

📌 **This is the owner's screenshot, fully explained.** A **linked** chat showing the old menu was never a
link-vs-default puzzle — **the menu it should have switched to has never existed on the channel.** Your C-13
Side E observation was right and needed no ruling; the cause was one file away.

🔴 **What it means for the deploy queue, and please carry this to the owner:** *"supply two images and run
`publishRichMenus`"* would have **completed successfully and changed nothing** — no error, no menu, and the OA
still on the old default. **Images were never the blocker.** ⇒ `board.md` PENDING DEPLOY item 2 is corrected;
**the publish step now waits on TASK-247, not on artwork.**

### 📌 The shape, because it is the third time this week

**A defined constant that nothing constructs is indistinguishable from a working feature** — until someone looks
at the call site. `UNKNOWN_RICH_MENU` reads as done: it has bounds, a name, a `selected: true`, an invariant
helper and a paragraph of design rationale. **Everything except a caller.** Same family as `menuHasAdminButton`,
which exists precisely to be asserted and **is asserted nowhere** — TASK-247 fixes both.

### TASK-247 — @Jason. Artwork is the smaller half.
`tasks/TASK-247-req079-rich-menus-artwork-and-publish.md`. §0 the publish path · §1 two orange PNGs
(`unknown-th` 2 cells · `known-th` **6** cells, per your *"ภาษา/ช่วยเหลือ STAY"* table — the code's `KNOWN_RICH_MENU`
is currently **5**, so `admin` narrows and `action=lang` takes the middle; `คุยกับแอดมิน` stays bottom-right) ·
§2 publish all six + default to **unknown** + `storeMenuIds` **merges** · §3 a source-assertion test pinning the
generator's bounds to the code's, because *"never one side alone"* is a README sentence and **a sentence is not a
control** — the `strikeOrPrompt` 6-vs-11 correction two days ago is the argument.

### Decided so nobody is blocked — one is yours to take to the owner
- **TH only.** An id stored without an image renders **blank** on a phone, which is worse than falling back.
- **The four REQ-015 images stay blue.** ⚠️ A repaint is **not free**: re-creating those menus changes their ids,
  and **teachers already linked keep the OLD id until they re-link** — a migration, not a colour change.
  ⇒ **@Porter, this is the one open artwork question and it is yours:** the OA will be **two-coloured** — orange
  unknown/known, blue teacher. **It does not block TASK-247.**
- **`ภาษา/ช่วยเหลือ` is NOT on menu A** — REQ-079 says an unlinked follower has exactly two things to do.

**BALL: @Jason — TASK-247.**

## [--:--] Jason (BE) — ✅ TASK-247 → REVIEW. The images were the smaller half; the publish path was the defect. → @Sober

Repo **`smart-scheduler-back`** (path in `machine.local.md`), HEAD **`b47142a`**. **Verified:** `tsc --noEmit`
**0** · `bun test` **1295 pass / 0 fail** (17 new) · **no migration** — 32 `drizzle/*.sql` = 32 journal tags,
re-counted. 🚫 **Nothing published**: no `publishRichMenus`, no upload, no link, no `setDefaultRichMenu`, no LINE
API call at all, no SQL. The OA run is the owner's deploy step.

**§2 — the fix that matters.** `publishRichMenus` now creates and uploads **six** menus, stores
`unknownTH`/`knownTH`, and sets the account default to **unknown** — the line that turns the file's own
*"ยังไม่รู้จัก is where a chat lands with no code running"* from aspiration into behaviour. `storeMenuIds`
**merges** through a pure `mergeMenuIds(current, incoming)` that **omits absent keys instead of writing
`undefined`** — the half a naive spread gets wrong, since every field of `MenuIds` is optional.

**§1 — artwork.** Orange (`#f76707`) on the white ground with the dark label. The accent became a **parameter**
of the icon set rather than a second copy of every glyph, so the two new menus are orange and the shipped four
are untouched — and the proof is better than a promise: regenerating **all six** leaves the four REQ-015 PNGs
**byte-identical** in `git status`. New glyphs: an open course card and a speech-bubble-with-person. For
`เข้าใช้ระบบ` I did not reuse `register` — a person-with-a-plus reads as *"add a child"*, a different act.

**§3 — the guard, and I proved it fails.** The test reads the generator **as text**, strips comments, and
compares parsed rectangles against the code's `areas`. It takes `KW`/`CH` from the generator's own source rather
than retyping them, so the test is not a third copy that could drift from both. I then **moved one cell 4px**,
watched exactly that test fail, and restored it.

📌 **The finding worth keeping, and it is @Sober's, not mine:** a "done" feature can be invisible in production
with every test green, because nothing asserted that the thing was ever *created*. `linkKnownRichMenu` read an
id nobody wrote and — best-effort by design — did nothing, silently, and the publish run printed ✓. **Supplying
the artwork alone would have changed nothing.**

⚠️ **Two of my own earlier tests pinned the five-cell known menu and were corrected, not loosened**
(`line-menus-flows`, and TASK-245's scope guard). The guard's intent is unchanged: the menus change only in the
task that owns them, never as a side effect.

**Reported, not fixed — your call (Q1).** `line-adopt-menus.ts` has the same whole-object overwrite. The
destructive half is closed at the write site now that `storeMenuIds` merges, but I did **not** add the two new
names to `NAME_TO_KEY`: adopt aborts when any name in that map is missing from the OA, so adding them before the
menus exist would break `line:adopt-menus` on every OA not yet re-published. After the owner publishes it is a
two-line change and its own decision. (Q2: nothing links the unknown menu per user — it is reachable only as the
default, which is why §2's default line is the whole fix. Q3: the README recipe still works — `sharp` 0.34.5 in
`smart-scheduler-front`; I ran it and recorded the version.)

**BALL: @Sober — TASK-247 review.**

## [--:--] Sober (SA) — ✅ TASK-247 **DONE (code)**. The menus exist, the publish path creates them, and the deploy step is now real. → @Jason @Porter

**Reproduced independently:** `tsc` **0** · `bun test` **1295 pass / 0 fail** (108 files) ·
`git status assets/line/` → only the README + generator modified, two new PNGs, **the four REQ-015 images
untouched** — *"byte-identical"* was checkable and I checked it · `KNOWN_RICH_MENU` is **six** cells,
`action=lang` at `833,843,833,843`, `action=admin` at `1666,843,834,843` — REQ-079's table exactly ·
**I opened both PNGs**: orange, labels right, `คุยกับแอดมิน` in the corner it was promised.

### What actually changed, in one line
`setDefaultRichMenu(unknownTH)`. **That call is the whole of it.** Before today the file's comment said
*"ยังไม่รู้จัก is the DEFAULT menu … no code running at all"* and the code set the old parent menu as default.
Now they agree, and a brand-new follower will land on the right menu with nothing running — **once the owner
publishes.** 🚫 Nothing was published; that step is his.

### 🔴 The judgement worth recording is something Jason did NOT do
He found that `scripts/line-adopt-menus.ts` has the **same** whole-object overwrite — and **deliberately did not
add the two new names to `NAME_TO_KEY`.** I verified why and he is right: `selectMenuIds` computes `missing` over
**every** key of that map and `line-adopt-menus.ts:56` aborts on any gap (*"Nothing was stored"*). Adding the
names **before the menus exist on the channel** would break `line:adopt-menus` **on every OA not yet
re-published** — a working operator command broken by a task that was not about it.
✅ He closed the destructive half **at the write site** instead (`storeMenuIds` merges), so adopt now preserves
what it does not know about. Both writers go through that function; fixing adopt alone would have left the next
writer to rediscover the same edge.

⚠️ **And the part that is mine.** *"After the publish it is a two-line change"* is a **follow-up with a trigger**,
and I have shipped a note-without-a-trigger before — that is how the additional-teacher clash got past me.
⇒ It is now **step 4 of the PENDING DEPLOY block**, attached to the publish it depends on. `board.md` item 2 is
rewritten too: the menus are **built**, and **still not live**.

### 📌 He built a better guard than I asked for
I asked him to prove the bounds test *can* fail; he proved it once by hand **and** left a permanent negative
control (`line-rich-menu-artwork.test.ts:86` — a moved cell must NOT match), so the guard's own liveness is
re-checked on every run rather than once in a session nobody can replay. **That is the difference between
evidence and a receipt.** He also reads `KW`/`CH` out of the generator's source instead of retyping them — I
would have accepted a third copy of the geometry — and shape-guards the eval so a source-assertion test cannot
execute whatever the file grows into.

📌 **Two small calls, both his, both right:** `เข้าใช้ระบบ` is a **door**, not the `register` glyph, because
*"a person-with-a-plus reads as 'add a child', a different act"* — the same distinction discipline that has run
through this whole week. And the label is **`ภาษา/ช่วยเหลือ` unspaced**, `btn_langhelp` verbatim, not my spaced
version: **one string in one place beats two spellings of one label.** His wins.

### 👁️ Noticed and deliberately kept
Menu A's content sits high — `cellSvg` puts the icon at 40% and the label at 74%, which centres in a **two-row**
menu and leaves an empty band under a **single-row** one. **Not a defect, and I do not want it changed:** the
shipped teacher menu is the same height through the same function, so menu A matches what is already on the OA.
Recording it so that whoever first sees it on a phone knows it was noticed, not missed.

### ⏳ @Porter — what is left, and it is all deploy
1. `0030` + `0031` on `sid` → `db:migrate` → **`db:verify` ✅ before any restart**.
2. **`publishRichMenus` with all SIX image paths**, then **confirm on a phone — never from the output.**
3. Then, and only then, PENDING DEPLOY **4** (`NAME_TO_KEY`) — I will cut it when the publish is confirmed.
⚠️ Still owner-owed and unrelated to this: **2FA delivery**, `province` as a household field, the two-coloured OA
(orange unknown/known vs blue teacher — a repaint is a migration, not a colour change).

**BALL: @Porter — the REQ-079 deploy.** Nothing is left in the code.

## [--:--] Porter (PM) — ✅ Sober's read lands 3 things. Money round RELEASED to Tanya. → @Tanya @Sober
1. **Auto-attend confirmed in code** — `NO_SHOW` has no writer left (REQ-070/TASK-180 removed it). Owner's ruling
   and the code already agreed; **nothing to build.** 2. **`OTHER` is in both selects** ⇒ AC-9 is reachable.
3. **New standing fact → `SYSTEM-FACTS.md`:** the day-end trigger lives in Task Scheduler, the bookable hours in
   `time.ts`, **nothing spans them** — one added 18:00 slot would silently stop the last class of every day being
   attended or posting. Safe today by 30 minutes.
⇒ `inbox/QA.md` — ฿20 round released, fixture shape stated (`CONFIRMED`, ends before 18:30).
**Owed by the owner and carried by me:** one freelance rate on `sid` for AC-21 (all 10 are ฿0/h).
**Still open, not answered by this read:** `C-13`'s question — does clearing a family's link put that chat back on
the unknown menu? It only becomes observable after the publish.

## [--:--] Porter (PM) — 📦 Owner deployed `sid` (2026-09-05). Awaiting the `db:verify` line before anything else.
Owner: *"deploy sid แล้ว"*. Recorded here because a deploy that nobody writes down is a deploy the next session
guesses at — the TASK-220 lesson.
🔴 **Not confirmed yet and NOT assumed:** that `0030`+`0031` applied and `db:verify` printed ✅. `db:migrate` can
exit 0 having applied nothing (TASK-085/086 — that took the customer's calendar down on both boxes on 08-24).
**I asked him for the output rather than reading "deployed" as "migrated".** Next step after ✅: `publishRichMenus`
with **all six** image paths, then confirm on a phone.

## [--:--] Porter (PM) — 🚀 `sid` DEPLOYED and the SIX rich menus are PUBLISHED (owner, 2026-09-05). → @Sober @Tanya
`db:verify` ✅ — journal 32 · **32 witnessed** ⇒ `0030`+`0031` are in. Then `bun run line:publish-menus` printed
six ids; `unknown-TH` is the **account default**, `known-TH` linked per user.
**Recorded here because a deploy nobody writes down is one the next session guesses at** (the TASK-220 lesson).
🔴 **NOT confirmed: the menus on a phone.** The standing rule is that the command's output is not the evidence.

⚠️ **A prediction I am putting on the record BEFORE he looks, so a surprise is not read as a new defect:**
**chats that were linked BEFORE this publish may now show the UNKNOWN menu.** `linkKnownRichMenu` runs *at link
time*; for an already-linked chat it never ran, and the account default is now `unknown-TH`. If that is what he
sees, the fix is to re-link that chat — **and the question of whether existing links need a backfill is @Sober's,
not a defect report from me.** 📌 This also makes `C-13`'s open question observable for the first time: does
clearing a family's link put that chat back on the unknown menu?

## [--:--] Porter (PM) — ✅ MENU A IS LIVE ON A PHONE. First time REQ-079's two-state menu has ever rendered. → @Sober @Tanya
Owner's screenshot, `SOM-Balance-Demo`, 13:47. **Confirmed ON THE SCREEN, not from the command output** — the
standing rule, and this is the case it was written for.

**What is proven:** 🟠 orange · **two cells** · `เข้าใช้ระบบ` (arrow) · `คุยกับแอดมิน` (chat) · exact labels from
`REQ-079` §12 · `คุยกับแอดมิน` on the right. **`unknown-TH` is genuinely the account default.**
📌 The chat carries LINE's new-follower greeting at 13:47, so this is a **fresh follow** — the cleanest possible
proof of the unknown state, better than the unlink route I proposed.

🔴 **Still NOT proven: menu B.** Nobody has seen the six-cell known menu render. Next: `สมัคร` → link → the menu
must flip. **`C-13` and the "do existing links need a backfill" question stay open until that flip is observed.**

⚠️ **Wording gap, mine (UX writer):** the welcome text still reads *"หลังผูกแล้ว (ผู้ปกครอง): เพิ่มนักเรียน ·
เช็คอิน · ลา · qr"* — it advertises **`qr`**, names none of the menu, and predates `คอร์สของฉัน` and
`ภาษา/ช่วยเหลือ`. **Now that a menu exists, a message telling parents to type words is describing the old world.**
Not urgent, not a defect — a wording pass I owe once menu B is confirmed.

## [--:--] Porter (PM) — ✅ MENU B IS LIVE TOO. 🔴 And the first button on menu A is DEAD. → @Sober @Tanya
Owner's screenshots, 13:47–13:53. **Both states of REQ-079's rich menu have now rendered on a real phone.**

### ✅ Menu B — confirmed, exactly as specced
🟠 orange · six cells · `แจ้งลา · เช็คอิน · คอร์สของฉัน` / `เพิ่มนักเรียน · ภาษา/ช่วยเหลือ · คุยกับแอดมิน` ·
`คุยกับแอดมิน` bottom-right. **The menu flipped on linking**, so `linkKnownRichMenu` fires and is no longer a
no-op. ⇒ **The whole two-state mechanism works end to end.** `C-13`'s premise is now observable at last.

### 🔴 DEF — tapping `เข้าใช้ระบบ` on menu A leads nowhere. Reported by the owner, in his words:
> *"เมื่อกดปุ่มเข้าสู่ระบบที่ richmenu แล้ว มันบอกให้พิมพ์เบอร์ แต่พอพิมพ์แล้วไม่ทำงานต่อ"*

**What he observed (13:50–13:51):** tap `เข้าใช้ระบบ` → bot replies *"กรุณาพิมพ์เบอร์โทรที่ลงทะเบียนไว้ค่ะ
หากยังไม่เคยลงทะเบียนกรุณาติดต่อแอดมิน"* → he typed `0900000092` → **silence** → he typed `สมัคร` → the role
prompt (`1 = ลูกค้า/ผู้ปกครอง · 2 = ครู · 3 = แอดมิน`) appeared and the flow ran normally to a linked account.

**Observation I am NOT turning into a diagnosis:** that prompt's wording — *"กรุณาติดต่อแอดมิน"* — is the
**pre-REQ-079 sentence**, the exact one `REQ-079` §13 ("the parent is never stuck") was written to remove. The
keyword `สมัคร` reaches the new flow; the button appears not to. **Cause is @Sober's read, not my claim.**

🔴 **Severity: this is the FIRST thing a new parent will do.** The menu is the only entry REQ-079 designs for, and
its first button dead-ends into an instruction the bot then ignores. **A keyword rescues it — but a parent has no
reason to know a keyword exists, which is the whole argument for the menu.**

## [--:--] Porter (PM) — ✍️ Wording pass written (`REQ-079` §15). Two strings, one read, one cost. → @Sober @Tanya
The messages were written when typing was the only way in; a button exists now. **Copy only — no keyword removed**
(the owner's 09-03 ruling: typing must keep working for PC users).
**Changed:** the welcome (says *"พิมพ์ สมัคร"* under an `เข้าใช้ระบบ` button) · the after-linking message (a
six-line keyword list under a six-cell menu). **`เมนู`'s full list stays** as the PC reference.
**Not guessed, sent to @Sober:** are `นักเรียน` (keyword) and `คอร์สของฉัน` (menu cell) the same destination? If
not, **the menu cannot reach `นักเรียน` at all.**
**Not guessed, the owner's when it matters:** `qr` has no cell and there is no seventh — and his REQ-015 may
re-scope it anyway. Not urgent.
🔴 **Cost I am naming rather than hiding:** `AC-19`/`AC-22`/`AC-24` were @Tanya's passes on this exact copy ⇒
**re-checked, not carried over.** Told her directly. Sequenced behind the `เข้าใช้ระบบ` defect.

## [--:--] Sober (SA) — 🔴 DEF-9 diagnosed: the button sets no state. Plus both of your open questions, answered from source. → @Porter @Jason @Tanya

Read `line-webhook.service.ts` (the postback dispatch, `CHOOSE_ROLE`/`AWAIT_CODE`), `line-i18n.ts`,
`line-menus-flows.test.ts`, and every rich-menu link call site. Nothing run, nothing sent.

### 🔴 DEF-9 — `เข้าใช้ระบบ` replies and sets no step. **TASK-248 → @Jason.**

`line-webhook.service.ts:1171`, in full:

```ts
if (action === "enter") return send(replyToken, [textReply(t("enter_ask_admin", lang), lang)]);
```

**It sends a sentence and returns.** No `setStep`, no `pendingRole` ⇒ the phone the parent types arrives with **no
session step**, falls past every flow branch and is treated as idle chat. **Silence — exactly what the owner saw.**
`สมัคร` works because it *does* set a step (`:979`).

📌 **The copy is right; the handler is a stub.** `enter_ask_admin` says *"type your registered phone number; if you
have never registered, contact an admin"* — which **is** REQ-079 after §15 deleted Flow 2 and amendment #2 made the
entry the phone alone. ⚠️ And the key is **named** `enter_ask_admin` while its text asks for a phone — **the name
probably is why the step was never wired.** TASK-248 renames it `enter_ask_phone`.

✅ **The fix is one line and no new flow.** `AWAIT_CODE` + `pendingRole = "customer"` **already is** *"type your
phone and get linked"* (`code_customer`, then `:1095` verifies, strikes on failure, links the account and the known
menu). The button must set that step before replying. **Decided by me, so nobody designs around it:**
`เข้าใช้ระบบ` presumes **customer** — a teacher who taps it fails into the AC-18 handover, which is what the
button's own copy promises, and asking a stranger *"parent or teacher?"* as the first question is the friction
REQ-079 removed.

### 🔴 And the reason nobody caught it: the tests were green about the WORDS

`line-menus-flows.test.ts:270–272` asserts the service contains `t("enter_ask_admin", lang)`, that the TH string
contains `เบอร์โทร`, and that it contains `แอดมิน`. **Three assertions about the copy, none about the behaviour.**
The suite proved the bot **says** *"type your phone number"* and never asked whether anything **receives** it.

📌 **This is the comment-vs-code trap in a new costume.** On 09-03 it was counting an identifier in prose; here it
is asserting the sentence instead of the state transition. **Both times the test matched the author's intention
rather than the machine's behaviour, and both times it was green while the thing was broken.** ⇒ The rule, and it
is now in the task: **a source assertion must test what the code DOES — and where it cannot, it must not be
mistaken for the test that does.**

### ✅ Your question 1 — do existing links need a backfill? **Yes, and there are TWO wrong populations.**
`linkKnownRichMenu` runs **at link time only**. Families linked **before** today's publish never had it run for
menus that did not exist. ⇒ a chat with an **old** per-user link still resolves to the **old parent menu** (those
menus still exist on the channel — **this is exactly your 01:23 screenshot, and it was never a puzzle**), and a
chat with **no** per-user link now shows **unknown**. **Neither shows menu B.**
⚠️ **A one-off re-link runs against real customer chats ⇒ it is a deploy action and it is the owner's.** Added to
`board.md` PENDING DEPLOY. If he wants a script, that is its own task — I have told @Jason not to write one.

### ✅ Your question 2 — C-13: does clearing a family's link restore the unknown menu? **No. TASK-249 → @Jason.**
Every link call in the repo is a **link**: `:1110`, `:1113`, `:1179`, `teacher-link.service.ts:152`. **There is no
unlink call anywhere**, and nothing ever links a chat to the unknown menu. `line-rich-menu.ts` states the design —
*"a chat whose per-user link is ever removed falls back to it"* — and **the premise never happens, because nothing
removes it.** ⇒ after TASK-243's admin clear-link, the family is unlinked in our DB and the parent's phone still
shows **menu B: the buttons of an account they no longer have.**
📌 **Third time this week: a documented fallback with no caller** — `UNKNOWN_RICH_MENU` before TASK-247,
`menuHasAdminButton` before its test, this now. **The comment is not the mechanism.**

### ✅ What is genuinely proven today, and it is a lot
Both states of REQ-079's menu have rendered on a real phone, the flip on linking fires, `unknown-TH` is truly the
account default, and `linkKnownRichMenu` is no longer a no-op. **The two-state mechanism works end to end** — DEF-9
is one dead button on a menu that is otherwise doing what it was designed to do.

⚠️ @Porter — your welcome-text pass (`qr`, no menu words) is **more urgent now, not less**: menu A tells a parent
to type a phone, and TASK-248 makes that work. **A welcome message that advertises keywords is describing the world
before today.**

**BALL: @Jason — TASK-248.** DEF-9 first; TASK-249 after it.

## [--:--] Porter (PM) — 🔻 §15's welcome WITHDRAWN. The shop already greets on follow. → @Sober
Owner: *"ลูกค้ามีข้อความทักทายอยู่แล้วนะ"* + a screenshot of the real OA. **He is right.** Their greeting carries
the program list, branch, the 100%-prepayment rule (TH+EN), phone, *"NO WALK IN"* and a price-table image.
⇒ Mine would have been **a second welcome on one follow, branding a system the parent never heard of.**
**Revised in `REQ-079` §15's correction note: the bot says NOTHING on follow** (menu A's `เข้าใช้ระบบ` is the
instruction), or one unbranded line if pressed. **The after-linking message stands** — it fires inside a flow the
parent chose and competes with nothing.
📌 **Raised for the owner, not decided:** the shop's greeting lives in **LINE OA Manager**, ours is a follow-event
push from our code — **two senders, neither aware of the other.** At REQ-014's OA move the greeting **must be
re-created on the new account**; it does not travel, and losing it would be a visible regression on move day.

## [--:--] Porter (PM) — 📌 Rental price lead PARKED in `REQ-028`, not acted on. Owner's instruction.
Owner: *"เรื่องเช่า โน็ตไว้ก่อน มีต้องกลับไปทำอยู่แล้ว รอ รวดเดียว"* — REQ-004 comes back as one piece.
The customer's own published table (in the OA-greeting screenshot he sent about something else): **set 200/hr;
ride 150 · helmet 50 · pads 50.** Written into `REQ-028` as a PARKED LEAD with the instruction to compare it
against SPEC-031's four codes **before** any question goes back to the customer.
⚠️ **Recorded as a lead, not a defect** — SPEC-031 matched the ledger on 08-30, and four prices vs four codes is
a resemblance, not a comparison. **Nothing is being built or asked.**

## [--:--] Jason (BE) — ✅ TASK-248 + TASK-249 → REVIEW. Two dead buttons and a fallback with no caller. → @Sober

Repo **`smart-scheduler-back`** (path in `machine.local.md`), HEAD **`864ef9c`**. **Verified:** `tsc --noEmit`
**0** · `bun test` **1316 pass / 0 fail** · **no migration** — 32 `drizzle/*.sql` = 32 journal tags, re-counted.
🚫 Nothing sent to LINE: no publish, no adopt, no link, no unlink, no SQL. Every assertion is pure or reads
source.

### TASK-248 (DEF-9) — the button now sets the step it was asking a question for
Your three lines exactly: `setStep(AWAIT_CODE, "customer")` before the reply, no second phone flow, and the
`AWAIT_CODE` branch asserted to be the **only** one that answers a typed phone. `enter_ask_admin` →
`enter_ask_phone`; the text is unchanged in both languages.

📌 **I took your §4 rule literally and split the new file into BEHAVIOUR and WIRING, labelled in the source**, so
nobody can later mistake the second for the first. The behaviour half runs the real `decideMessageRoute` —
including the contrast that *is* the defect: with no step, an unknown follower's typed phone routes to
`silence`. ✅ **And I proved the file catches the original bug**: reverting the handler to the old one-liner
makes it fail; restoring passes.

**Your question, traced not assumed — an already-linked chat that taps `เข้าใช้ระบบ`:** it re-links to itself
(harmless, ends at the add-student prompt), and someone else's number is refused with a strike, so the
no-silent-re-pointing guarantee holds on this path too. 🔴 **The one real cost, named rather than guarded:**
while that session is live a linked parent's next ordinary message is read as a phone attempt, so a mistaken tap
costs them two strikes or a 30-minute wait. ⚠️ And `ยกเลิก` does **not** rescue them — the exit exists in the
add-student wizard and in a muted chat, but **the unmuted linking branch has no exit at all** (`สมัคร` has the
identical gap, so this is pre-existing, not new). Out of scope here; your call whether it earns a follow-up.

### TASK-249 (C-13) — the fallback got its caller, and the grep found a second one
`unlinkRichMenuFromUser` (`DELETE /user/{id}/richmenu`) is wired to **both** DB link-clears: `clearFamilyLine`
(every account the family held — a family can hold several since TASK-230) and **`unlinkTeacherLine`**, which had
the same defect in the other role: a departed teacher kept `ตารางของฉัน` on their phone. 🚫 `moveRosterLink` is
deliberately **not** wired — it is a *move*, and the caller links the new role's menu immediately after;
unlinking there would blank a real phone and re-link it. Asserted as an absence so nobody completes the set later.
⚠️ **A trap inside the teacher fix:** `.returning()` hands back the row as it now is, where `lineUserId` is
already `null` — reading it there would unlink nothing and look like it worked. The account is read before the
write, and the test pins that order. Both calls run after the commit and are `.catch`-wrapped: a LINE hiccup must
not roll back an admin's database act, and a clear the database refused must never reach a phone.

§4: `NAME_TO_KEY` has all six, and the guard **reads `publishRichMenus` itself** rather than a list someone must
remember — the next menu added cannot forget the adopt map.

📌 **The finding worth keeping, and it is uncomfortable:** one of my own TASK-234 tests asserted
`not.toContain("unlinkRichMenu")`. **That passing assertion WAS the C-13 defect** — I had pinned the absence of
the exact call the design's own fallback sentence depends on. Corrected, not loosened: it now asserts the half
that must stay true (nothing **links** a chat *to* unknown — that would be a second definition of the state)
while the **removal** exists. *Two different acts; only one was ever meant to be impossible.* Three of my earlier
tests were corrected across these two tasks, all of them pinning behaviour that a later task deliberately changed.

**BALL: @Sober — TASK-248 + TASK-249 review.**

## [--:--] Sober (SA) — ✅ TASK-248 + TASK-249 both DONE. 🔴 And the week's sharpest finding: a passing test had PINNED the C-13 defect. → @Jason @Porter @Tanya

**Reproduced for both:** `tsc` **0** · `bun test` **1316 pass / 0 fail** (110 files) · `unlinkRichMenuFromUser` at
`line-rich-menu.ts:185`, called from `family-link.ts:158` and `teacher-link.service.ts:195` · the `enter` handler
sets `AWAIT_CODE` + `customer` **before** replying (`:1185`) · `NAME_TO_KEY` at six. Nothing sent to LINE.

### 🔴 The finding that outranks both fixes

> Jason: *"One of my own TASK-234 tests asserted `not.toContain("unlinkRichMenu")` and now fails — corrected, not
> loosened. **That assertion WAS the defect**, written down and passing."*

**C-13 was not an oversight. It was pinned.** A green test stood guard over the missing call, so anyone who added
it would have been told by the suite that they were wrong.

📌 **The class, and it is worth more than the case:** *an assertion that something does NOT exist is only as good
as the reason it doesn't.* When the reason is a design rule, the test is a **control**. When the reason is merely
*"nobody has written it yet"*, the test **freezes an accident into a rule** — and the two are indistinguishable at
a glance. ⚠️ **Every `not.toContain` in this repo is now suspect until someone can say which kind it is.** Not a
task today; the question to ask whenever one is touched.

✅ **And he corrected it at the right seam**, not by deleting it: *"removing a link and linking a chat TO unknown
are different acts — the first is the fallback's caller, the second would be a second definition of the state."*
The removal now exists; `linkUnknownRichMenu` is still asserted absent. **Two acts a coarser fix would have
merged.**

### ✅ TASK-249 — three clear paths, and the third is the judgement
`clearFamilyLine` ✓ · `unlinkTeacherLine` ✓ — **a defect nobody had reported:** a departed teacher kept
`ตารางของฉัน` on their phone, same shape, other role · `moveRosterLink` 🚫 **deliberately untouched** because it is
a **move** (the caller links the new role's menu straight after, so unlinking would blank the phone and re-link
it), **asserted as an absence with its reason attached** — which is exactly the good kind of `not.toContain`.
⚠️ **The `.returning()` trap he named is real and I confirmed it:** the returned row already has
`lineUserId: null`, so reading it there would have unlinked **nothing and looked like it worked**. The account is
read before the write and the order is pinned.
✅ Every account in `result.cleared`, not just the primary (a family can hold several since TASK-230), and both
calls run after the commit — *a clear the database refused must never reach a phone.*

### ✅ TASK-248 — DEF-9 closed, and the test file answers §4 properly
The split is labelled **in the source**: *"BEHAVIOUR ones run the real decision function; WIRING ones read the
source and are honestly only that."* 📌 **The second clause is the lesson.** The old tests were not bad for
reading source — they were bad because **nothing said they were not the coverage.**
**And the behavioural line IS the defect as a value:** `decideMessageRoute(undefined, null)` → `"silence"` —
**the owner's typed phone number, as a number in a test instead of a story in a log.** Beside it,
`decideMessageRoute(undefined, "customer")` → `"linked"` shows the miss was *"no step"*, not *"not recognised"*.
✅ He proved the file catches the original bug by reverting the handler and watching the right test fail.

✅ **Already-linked chat: traced, and NO guard added** — a linked parent who taps it re-lands where they already
were (`verifyAndLink` sees the same `lineUserId`, `bindFamilyLine` is idempotent). **A guard there would be code
defending a harmless path, and every guard is a branch someone must later understand.** Not adding it, with the
trace to show why, is the right answer.

📌 **One I am carrying rather than changing:** `enter_ask_admin` was a key whose **name argued with its own text**,
and that is my best guess at why the step was never wired — the author read the name, saw *"ask an admin"*, and
wrote a reply. **A misleading name is a defect with a long fuse.** The rename was worth more than the line beside it.

### 🧪 @Tanya — three replays, and the first is the owner's
1. **DEF-9 on a phone:** tap `เข้าใช้ระบบ` → type a registered number → **the account links and the menu flips to B.**
2. **The wrong-phone path:** two bad answers reach a human, not a loop.
3. **TASK-249:** an admin clear-link → **the parent's menu falls back to A** (and a departed teacher's does too).

⏳ @Porter: PENDING DEPLOY **item 5** — the backfill re-link for families linked before the publish — is still the
owner's, and your welcome-text pass is still owed.

**BALL: @Tanya — the DEF-9 phone replay.** It is the owner's own sequence and he is the one who reported it.

## [--:--] Sober (SA) — 🔻 Correction from the owner: **@Tanya cannot test LINE. Only he can.** Three replays re-routed. → @Porter @Tanya

Owner, verbatim, correcting me: *"tanya cannot test line that me only one can test."*

**He is right and I should have known it from this repo's own facts.** I put three LINE replays on QA today
(DEF-9's tap-and-type, the wrong-phone handover, the clear-link fallback) and earlier the DEF-8 sequence and
Round C. **None of them are runnable by @Tanya** — and not as a matter of permission:
- **LINE on PC has no rich menu and its buttons cannot be tapped at all** (already in `SYSTEM-FACTS.md`). A menu
  test needs a phone.
- **`sid` and `uat` share ONE LINE channel** and **`sid` is not a safe isolated box** — real people are linked,
  and QA's own standing rule is *"never message real people."*
- The owner holds the OA and the phone. **Nobody else can produce the evidence.**

⚠️ **What misled me is in the file too, and it is too loose:** *"inbound LINE is testable on `sid` any time."*
That is true of the **webhook** and false of the **experience** — the sentence has been narrowed where it sits.

### 📌 The mistake underneath, which is worth more than the re-routing
**I wrote a test plan without asking who can physically run it.** Every item was well-formed, and it was parked
with someone who could never close it. ⇒ **A test nobody on the team can perform is not a plan — it is a request
wearing a checklist's clothes.** Written into `SA-Lead.md` as a standing rule, and into `SYSTEM-FACTS.md` § LINE
as the routing fact.

⇒ **The rule, from now on:** anything needing a real LINE account on a phone goes **@Sober → @Porter → the owner**
and returns the same way. **@Tanya keeps everything reachable without a phone** — API and DB behaviour, the web
app, and the outbox rows a flow is supposed to write. **The split is the phone, not the feature.**
@Porter — `QA.md` still describes LINE as in-scope for `sid`; **that is yours to correct, not mine.**

### The five checks, re-addressed to the owner via @Porter
1. 🔴 **DEF-9, his own sequence:** tap `เข้าใช้ระบบ` → type a registered number → **the account links and the menu
   flips to B.** He reported it; he is the one who can confirm it.
2. **Two wrong phones in a row** must reach a human, not loop.
3. **An admin clear-link** → that chat's menu falls back to A (and a departed teacher's does too).
4. **DEF-8:** muted → button → the bot asks → type a name → **it is answered.**
5. **Round C:** a duplicate name asks for a surname — not a strike, not a handover. ⚠️ **AC-25 must not regress:**
   while muted, `เมนู` · `เพิ่มนักเรียน` · free text stay silent.

**BALL: @Porter — take the five to the owner.** They are all one phone session.

## [--:--] Porter (PM) — 🔧 QA.md corrected: LINE is out of scope for QA on EVERY box. C-13 NOT closed. → @Tanya @Sober
1. **`QA.md` fixed** — it still listed LINE as in-scope on `sid`; @Sober flagged it and correctly did not touch my
   file. New section: LINE is the one `sid` exception, with the three reasons (no rich menu on PC · one shared
   channel with real people · the owner holds the phone) and the **hands/verdict/carrier** division spelled out.
   **Everything else on `sid` is unchanged for her** — backoffice, database, money paths.
2. **`C-13`: @Sober told me to close it. I did not, and said so in the file.** TASK-249 fixes the code; **that is
   not the same as the owner closing an entry.** Recorded as Side F, the strongest side, still a side.
📌 **@Sober's own miss, recorded because it is a good one:** he wrote a test plan without asking who could
physically run it. He caught it, re-pointed the board rows and wrote the rule into `SA-Lead.md`.
