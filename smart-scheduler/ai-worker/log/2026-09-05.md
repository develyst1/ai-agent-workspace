# 2026-09-05

## [--:--] Porter (PM) — 📋 Fast-confirm batch: 7 contradictions where the owner's own words already exist in a log → @owner
Checked all 45 entries in `SYSTEM-FACTS-CONTRADICTIONS.md` for the `_(unanswered)_` marker: **6 answered, 39 still open** (matches the file's own ledger).

Of the 39, **7 have the owner's words already on the record** — I am NOT settling them (the file forbids that);
I am putting his own quote in front of him so he can close each with one word. Quotes located:

| # | question | owner's words on record | where |
|---|---|---|---|
| C-35 | canonical branch | *"ฉันจะรวม develop ทั้งหมด ทุก repo... branch กลาง คือ develop"* | log/2026-08-25.md:1358 |
| C-29 | teacher/program restriction | *"ตั้งใจจำกัด"* | log/2026-08-29.md:1340 |
| C-23 | teacher clash: warn or refuse | *"ตามนั้น"* (= option ข, refuse-for-now) | log/2026-09-01.md:1839 |
| C-41 | commit rule | *"commit แล้ว บอกทีม เลิกยุ่งเรื่อง commit ฉันจะทำเองเมื่อถึงเวลาของฉัน"* | log/2026-09-01.md:1094 |
| C-12 | where the LINE webhook points | *"ตอนนี้ไลน์ webhook ผูกที่ sid ถาวรแล้ว"* | log/2026-09-01.md:1852 |
| C-27 | max 5 students per phone | *"ฉันสั่งนายทำไว้เองแหละนานแล้ว"* | log/2026-09-02.md:1807 |
| C-03 | day-end time | ⚠️ **no verbatim in the log** — only my write-up that he changed it himself and the app books to 18:00 | log/2026-09-02.md:1726 |

⚠️ **C-03 is the weak one** — the log carries *my* sentence, not his. It needs a real answer, not a confirmation.

🔴 **Blocking today: C-01 (what the day-end does with an unmarked booking) and C-03 (what time).**
Both are genuinely unanswered. Answering them unblocks REQ-078 **AC-9 · AC-4 · AC-5 · AC-21**, which are parked.

**Ball: the owner.** Nothing here may be written into the contradictions file by me — only his dated answer closes an entry.

## [--:--] Porter (PM) — ✅ Owner closed EIGHT contradictions. REQ-078's block is LIFTED. → @Sober @Tanya
Owner, verbatim: *"ใช่ทั้ง 6 ข้อ C-01 auto-attend C-03 18:30 ฉันปรับเอง"*.
Recorded as dated `Owner's answer (2026-09-05)` lines in `SYSTEM-FACTS-CONTRADICTIONS.md` — **31 left open.**
Table of all eight: `SYSTEM-FACTS.md` § Owner-confirmed on 2026-09-05.

**What changes for the work:** `end-of-day` **auto-attends** (never `NO_SHOW`) at **18:30**.
⇒ `REQ-078` AC-9 · AC-4 · AC-5 · AC-21 are **no longer blocked** — every `23:30` in that REQ corrected to 18:30,
and the lift is written at the foot of the file.
⚠️ **If the code still writes `NO_SHOW`, that is now a DEFECT, not a fact** (`jobs.service.ts:47–61` read it that way on 08-23).

**Not settled by this, and still owed:** does the day-end's selection **exclude `OTHER`**? A source read.
Plus the fixture shape — `CONFIRMED`, not `PENDING`, created **before 18:30** — and one freelance rate for AC-21.

**Ball: @Sober** — the one source read (day-end: `NO_SHOW` or auto-attend in code, and does it skip `OTHER`).

## [--:--] Porter (PM) — 📋 Owner's list reconciled. 3 NEW items, 3 disagreements. → @owner
Owner pasted his full customer-facing list and asked for an update. Written into `OWNER-LIST.md`
§ RECONCILED 2026-09-05. **Not merged silently — the disagreements are listed.**

**NEW, never captured here:** **REQ-017** (ขยับวันหมดอายุทุกคอร์ส) · **FIX-008** (เช็คอิน→ลาป่วย ยังกินสิทธิ์) ·
**FIX-009** (ปุ่มนำคอร์สที่พักกลับมา — the un-drop half of his REQ-011, which we shipped drop-only).
⚠️ **FIX-008 is `C-24` in the contradictions file** (*can an attended session be undone?*) — his asking for it is
not the same as his answering it; the entry stays `_(unanswered)_` until he dates it.

**Disagreements:** REQ-005 he says ToDo → build complete, 18 ACs pass, parked · REQ-016 he says ToDo →
15/26 ACs verified, waiting on HIS rich-menu images · REQ-004 he says In Progress → reopened but **nobody is
working it**, the customer's actual complaint has never been stated.

Order unchanged. The three new items are **not** queued — placing them is the owner's call.
**Ball: the owner** (place the 3 new items · rich-menu images) · **@Sober** still owes the day-end source read.

## [--:--] Porter (PM) — 🔻 My customer-facing REQ-016 text carried a CANCELLED design. Owner caught it. → @Tanya
Owner: *"ฉันบอกให้เอา 2FA ออกละนี่"*. **Correct.** The 6-digit family code was cancelled 2026-09-02 (customer
refused; entry = **phone alone**), yet I copied his own stale note into a customer update **verbatim, unchecked.**
Fix + standing rule in `OWNER-LIST.md` § "The owner's own REQ-016 text is STALE".
**The rule: his list is the source for NUMBERS and for what the customer was told. `REQ-079` is the source for
what the system DOES.** Never copy the body of his items forward without checking the REQ.

## [--:--] Porter (PM) — 📤 The customer update sent on 2026-09-05 — what they were told
Owner approved and is sending it. Full text: `project-docs/2026-09-05-customer-update.md`. **This is now the
record of what the customer BELIEVES**, and any future status must be reconciled against it, not against the board.

**Two corrections I made to his draft before it went:** REQ-014 said *"รอรายการจากลูกค้า"* — **wrong, their list
landed 08-30** and we would have been tumbling back for something they already gave · the Completed count said
**18**; it is **17** (REQ 10 + FIX 7). The extra came from a vague *"+ งานย่อยอื่น"* I wrote myself.
**Two open questions with the customer:** REQ-004 *what exactly is wrong* · REQ-014 the OA move + rich-menu shape.

## [--:--] Porter (PM) — 📸 Owner's screenshot: a LINKED chat is still showing the OLD 6-cell menu → @Sober
`SOM-Balance-Demo`, 01:23. Bot had just replied `นักเรียนของคุณ (1/5) · 1. เตาไป๊` ⇒ **the chat is linked.**
Menu below it: `เช็คอิน · แจ้งลา · นักเรียนของฉัน / เพิ่มนักเรียน · ภาษา · ช่วยเหลือ` — **the REQ-015/042 menu,
cell for cell**, exactly what Sober read from `PARENT_RICH_MENU` on 09-02.
⇒ **Recorded as `C-13` Side E.** An observation, not a ruling — I did not close the entry.
**What it means for the ask on the table:** the A/B question is **moot until `publishRichMenus` runs** — a linked
chat and an unlinked chat see the SAME menu today, so the owner does not need to unlink anything to see menu A.
⚠️ **The live menu has no `คุยกับแอดมิน` cell** — REQ-079 §12 calls that the one cell whose position is not
negotiable, and `ภาษา`/`ช่วยเหลือ` are not in REQ-079 at all.

## [--:--] Porter (PM) — 🔻 The rich-menu images are OURS. My illustrator brief was wrong. → @Sober
Owner: *"ก็พวกนายนั่นแหละ ทำ ... อยากได้สีส้ม แค่นั้นแหละ"*. He is right.
`smart-scheduler-back` → `assets/line/` holds four PNGs **generated** by `generate-rich-menus.mjs` (palette,
icons and labels are edited in the script and re-run). **REQ-015 shipped this and I never read the README.**
⇒ `REQ-079` §12 corrected at the foot of the file; request re-aimed at Sober in `inbox/SA.md`.
🔴 **Root cause, same shape as the stale-2FA miss this morning:** I wrote a plan from what the requirement said
**without checking what the repo already had.** Twice in one day, both caught by the owner, not by me.
**What actually changes:** no illustrator, no commission. **Two new generator entries + orange + one publish run.**
**Still unasked, and it is mine:** `ภาษา`/`ช่วยเหลือ` exist on the live menu and REQ-079 drops them — my choice,
never put to him.

## [--:--] Porter (PM) — ✅ Owner: keep `ภาษา`/`ช่วยเหลือ`. Menu B re-laid out, artwork now fully decided. → @Sober
Owner: *"เก็บไว้"* — reverses my own undocumented removal. Seven labels wanted, **six cells is the hard ceiling**
(2 rows at 2500×1686) ⇒ merged `ภาษา/ช่วยเหลือ` into one cell, **the pattern the teacher menu already ships**.
New table in `REQ-079` § "`ภาษา` and `ช่วยเหลือ` STAY"; supersedes §12 for menu B only. `คุยกับแอดมิน` still
bottom-right. Menu A unchanged.
**Artwork is now fully specified: layout · labels · orange.** Nothing on it is owed by the owner any more.

## [--:--] Sober (SA) — ✅ Day-end source read. **Both answers are the good ones, and there is a third the question did not ask.** → @Porter

Read `smart-scheduler-back` → `src/services/jobs.service.ts` (`runEndOfDayJob`) and `src/lib/time.ts`. No SQL, nothing run.

### 1. Auto-attend or `NO_SHOW`? — **it already auto-attends. NOT a defect.**

`jobs.service.ts:52` — `tx.update(bookings).set({ status: "ATTENDED" })`. **`NO_SHOW` has no writer left in the
file.** The 08-23 read was true when it was made; **REQ-070 / TASK-180 changed it since**, and the header says why
in the strongest terms the repo has:

> *"this used to write `NO_SHOW`, and that was a false claim about a child … it only told a family their child had
> not turned up because nobody pressed a button. On `uat` it did that to **15 real children in one weekend**."*

⇒ **The owner's 2026-09-05 ruling and the code already agree.** Nothing to fix, nothing to build. `NO_SHOW` stays
in the enum so historical rows still render — that is deliberate, not a leftover.

### 2. Does the sweep exclude `OTHER`? — **no. `OTHER` is in BOTH halves.** AC-9 is reachable.

There are **two** selects, and this is the answer to a question that has been open since 09-02:

| | filter | `OTHER` |
|---|---|---|
| **auto-attend** (`:47`) | `date = runDate AND status = CONFIRMED AND ended` | ✅ **no `bookingType` filter at all** — nothing can be excluded |
| **revenue** (`:110`) | `status = ATTENDED AND bookingType IN (FIRST_TRIAL, SINGLE_SESSION, OTHER)` | ✅ **named explicitly**, by TASK-225 |

📌 The อื่นๆ path takes its own branch (`postOtherBookingSale`) because **its price comes from the booking, not
from a product code** — and it posts on the same `rev:<bookingId>` key as the trial/single path, which is what
makes SPEC-069's *"revenue already posted"* warning cover อื่นๆ with no second lookup.
⇒ **@Tanya's ฿20 fixture will post.** `CONFIRMED`, before the run, is the correct shape — as you already had it.

### 3. 🔴 The thing nobody asked, and it is the one with a trap in it

**There is no `18:30` anywhere in the source.** `grep` over `src/` and `scripts/`: **zero hits** for `18:30`, and
zero for the old `23:30`. The job's own gate is **per booking** — `endTime <= now` — and **18:30 is a Windows Task
Scheduler trigger, an OS setting outside the repo.** That is exactly consistent with the owner's *"ฉันปรับเอง"*:
he changes it himself because it is his to change, and **no code change is owed by that ruling.**

⚠️ **But the trigger time and the bookable hours are now coupled, and nothing enforces it.** A booking whose end
time is **after** the trigger is not swept by that run — and the next day's run selects the **next day's** date, so
it is never swept at all: it stays `CONFIRMED` and **its revenue never posts.**

✅ **Safe today, with 30 minutes to spare:** `time.ts:5` `TIME_SLOTS` ends at **17:00**, and `endTime` is derived
as start **+1h** ⇒ **the last possible class ends at 18:00.** 18:30 clears it.
📌 **Why it is worth writing down anyway:** at the old **23:30** this gap could not exist; **at 18:30 it is 30
minutes wide.** Add one 18:00 slot to `TIME_SLOTS` — a one-line, entirely reasonable change — and **the last class
of every day silently stops being attended and stops posting revenue.** Nobody would connect the two.
⇒ **Not a defect and not a task today.** It is a fact for `SYSTEM-FACTS.md`: **the day-end trigger must stay later
than the last `TIME_SLOTS` end + 1h**, and the two live in different places (Task Scheduler / the repo).
🔁 Backfill exists if it ever bites: `runEndOfDayJob(date)` for a past date sweeps **all** of it (`runDate < today
→ true`).

**Ball back to @Porter** — nothing here needs a build. AC-9 · AC-4 · AC-5 · AC-21 are clear to test.

## [--:--] Sober (SA) — 🔴 The rich menus were never going to appear, and the missing images were not the reason. TASK-247 cut. → @Porter @Jason

Read `assets/line/generate-rich-menus.mjs` + its `README.md` + `src/lib/line-rich-menu.ts` (290 lines, all of it).
Nothing run, nothing published.

### The finding, and it changes what the deploy step is

`publishRichMenus` (`line-rich-menu.ts:191`) **creates only the four REQ-015 menus.** `UNKNOWN_RICH_MENU` and
`KNOWN_RICH_MENU` are defined by TASK-234, `MenuIds` has `unknownTH`/`knownTH` slots, `linkKnownRichMenu` **reads**
`ids.knownTH` — and **nothing anywhere writes them.** Three separate consequences:

1. **Neither new menu is ever created**, so `linkKnownRichMenu` finds no id and — best-effort by design — does
   **nothing, silently.**
2. `setDefaultRichMenu(parentTH)` — **the account default is the OLD 6-cell parent menu.** The file's own comment
   says *"ยังไม่รู้จัก is the DEFAULT menu … with no code running at all"*; the one call that sets an account
   default points somewhere else. **The design and the code disagree, and the code is what runs.**
3. `storeMenuIds` writes the **whole** object ⇒ a re-publish **erases** ids it did not create.

📌 **This is the owner's screenshot, fully explained.** A **linked** chat showing the old menu was never a
link-vs-default puzzle — **the menu it should have switched to has never existed on the channel.** Your C-13
Side E observation was right and needed no ruling; the cause was one file away.

🔴 **What it means for the deploy queue, and please carry this to the owner:** *"supply two images and run
`publishRichMenus`"* would have **completed successfully and changed nothing** — no error, no menu, and the OA
still on the old default. **Images were never the blocker.** ⇒ `board.md` PENDING DEPLOY item 2 is corrected;
**the publish step now waits on TASK-247, not on artwork.**

### 📌 The shape, because it is the third time this week

**A defined constant that nothing constructs is indistinguishable from a working feature** — until someone looks
at the call site. `UNKNOWN_RICH_MENU` reads as done: it has bounds, a name, a `selected: true`, an invariant
helper and a paragraph of design rationale. **Everything except a caller.** Same family as `menuHasAdminButton`,
which exists precisely to be asserted and **is asserted nowhere** — TASK-247 fixes both.

### TASK-247 — @Jason. Artwork is the smaller half.
`tasks/TASK-247-req079-rich-menus-artwork-and-publish.md`. §0 the publish path · §1 two orange PNGs
(`unknown-th` 2 cells · `known-th` **6** cells, per your *"ภาษา/ช่วยเหลือ STAY"* table — the code's `KNOWN_RICH_MENU`
is currently **5**, so `admin` narrows and `action=lang` takes the middle; `คุยกับแอดมิน` stays bottom-right) ·
§2 publish all six + default to **unknown** + `storeMenuIds` **merges** · §3 a source-assertion test pinning the
generator's bounds to the code's, because *"never one side alone"* is a README sentence and **a sentence is not a
control** — the `strikeOrPrompt` 6-vs-11 correction two days ago is the argument.

### Decided so nobody is blocked — one is yours to take to the owner
- **TH only.** An id stored without an image renders **blank** on a phone, which is worse than falling back.
- **The four REQ-015 images stay blue.** ⚠️ A repaint is **not free**: re-creating those menus changes their ids,
  and **teachers already linked keep the OLD id until they re-link** — a migration, not a colour change.
  ⇒ **@Porter, this is the one open artwork question and it is yours:** the OA will be **two-coloured** — orange
  unknown/known, blue teacher. **It does not block TASK-247.**
- **`ภาษา/ช่วยเหลือ` is NOT on menu A** — REQ-079 says an unlinked follower has exactly two things to do.

**BALL: @Jason — TASK-247.**

## [--:--] Jason (BE) — ✅ TASK-247 → REVIEW. The images were the smaller half; the publish path was the defect. → @Sober

Repo **`smart-scheduler-back`** (path in `machine.local.md`), HEAD **`b47142a`**. **Verified:** `tsc --noEmit`
**0** · `bun test` **1295 pass / 0 fail** (17 new) · **no migration** — 32 `drizzle/*.sql` = 32 journal tags,
re-counted. 🚫 **Nothing published**: no `publishRichMenus`, no upload, no link, no `setDefaultRichMenu`, no LINE
API call at all, no SQL. The OA run is the owner's deploy step.

**§2 — the fix that matters.** `publishRichMenus` now creates and uploads **six** menus, stores
`unknownTH`/`knownTH`, and sets the account default to **unknown** — the line that turns the file's own
*"ยังไม่รู้จัก is where a chat lands with no code running"* from aspiration into behaviour. `storeMenuIds`
**merges** through a pure `mergeMenuIds(current, incoming)` that **omits absent keys instead of writing
`undefined`** — the half a naive spread gets wrong, since every field of `MenuIds` is optional.

**§1 — artwork.** Orange (`#f76707`) on the white ground with the dark label. The accent became a **parameter**
of the icon set rather than a second copy of every glyph, so the two new menus are orange and the shipped four
are untouched — and the proof is better than a promise: regenerating **all six** leaves the four REQ-015 PNGs
**byte-identical** in `git status`. New glyphs: an open course card and a speech-bubble-with-person. For
`เข้าใช้ระบบ` I did not reuse `register` — a person-with-a-plus reads as *"add a child"*, a different act.

**§3 — the guard, and I proved it fails.** The test reads the generator **as text**, strips comments, and
compares parsed rectangles against the code's `areas`. It takes `KW`/`CH` from the generator's own source rather
than retyping them, so the test is not a third copy that could drift from both. I then **moved one cell 4px**,
watched exactly that test fail, and restored it.

📌 **The finding worth keeping, and it is @Sober's, not mine:** a "done" feature can be invisible in production
with every test green, because nothing asserted that the thing was ever *created*. `linkKnownRichMenu` read an
id nobody wrote and — best-effort by design — did nothing, silently, and the publish run printed ✓. **Supplying
the artwork alone would have changed nothing.**

⚠️ **Two of my own earlier tests pinned the five-cell known menu and were corrected, not loosened**
(`line-menus-flows`, and TASK-245's scope guard). The guard's intent is unchanged: the menus change only in the
task that owns them, never as a side effect.

**Reported, not fixed — your call (Q1).** `line-adopt-menus.ts` has the same whole-object overwrite. The
destructive half is closed at the write site now that `storeMenuIds` merges, but I did **not** add the two new
names to `NAME_TO_KEY`: adopt aborts when any name in that map is missing from the OA, so adding them before the
menus exist would break `line:adopt-menus` on every OA not yet re-published. After the owner publishes it is a
two-line change and its own decision. (Q2: nothing links the unknown menu per user — it is reachable only as the
default, which is why §2's default line is the whole fix. Q3: the README recipe still works — `sharp` 0.34.5 in
`smart-scheduler-front`; I ran it and recorded the version.)

**BALL: @Sober — TASK-247 review.**

## [--:--] Sober (SA) — ✅ TASK-247 **DONE (code)**. The menus exist, the publish path creates them, and the deploy step is now real. → @Jason @Porter

**Reproduced independently:** `tsc` **0** · `bun test` **1295 pass / 0 fail** (108 files) ·
`git status assets/line/` → only the README + generator modified, two new PNGs, **the four REQ-015 images
untouched** — *"byte-identical"* was checkable and I checked it · `KNOWN_RICH_MENU` is **six** cells,
`action=lang` at `833,843,833,843`, `action=admin` at `1666,843,834,843` — REQ-079's table exactly ·
**I opened both PNGs**: orange, labels right, `คุยกับแอดมิน` in the corner it was promised.

### What actually changed, in one line
`setDefaultRichMenu(unknownTH)`. **That call is the whole of it.** Before today the file's comment said
*"ยังไม่รู้จัก is the DEFAULT menu … no code running at all"* and the code set the old parent menu as default.
Now they agree, and a brand-new follower will land on the right menu with nothing running — **once the owner
publishes.** 🚫 Nothing was published; that step is his.

### 🔴 The judgement worth recording is something Jason did NOT do
He found that `scripts/line-adopt-menus.ts` has the **same** whole-object overwrite — and **deliberately did not
add the two new names to `NAME_TO_KEY`.** I verified why and he is right: `selectMenuIds` computes `missing` over
**every** key of that map and `line-adopt-menus.ts:56` aborts on any gap (*"Nothing was stored"*). Adding the
names **before the menus exist on the channel** would break `line:adopt-menus` **on every OA not yet
re-published** — a working operator command broken by a task that was not about it.
✅ He closed the destructive half **at the write site** instead (`storeMenuIds` merges), so adopt now preserves
what it does not know about. Both writers go through that function; fixing adopt alone would have left the next
writer to rediscover the same edge.

⚠️ **And the part that is mine.** *"After the publish it is a two-line change"* is a **follow-up with a trigger**,
and I have shipped a note-without-a-trigger before — that is how the additional-teacher clash got past me.
⇒ It is now **step 4 of the PENDING DEPLOY block**, attached to the publish it depends on. `board.md` item 2 is
rewritten too: the menus are **built**, and **still not live**.

### 📌 He built a better guard than I asked for
I asked him to prove the bounds test *can* fail; he proved it once by hand **and** left a permanent negative
control (`line-rich-menu-artwork.test.ts:86` — a moved cell must NOT match), so the guard's own liveness is
re-checked on every run rather than once in a session nobody can replay. **That is the difference between
evidence and a receipt.** He also reads `KW`/`CH` out of the generator's source instead of retyping them — I
would have accepted a third copy of the geometry — and shape-guards the eval so a source-assertion test cannot
execute whatever the file grows into.

📌 **Two small calls, both his, both right:** `เข้าใช้ระบบ` is a **door**, not the `register` glyph, because
*"a person-with-a-plus reads as 'add a child', a different act"* — the same distinction discipline that has run
through this whole week. And the label is **`ภาษา/ช่วยเหลือ` unspaced**, `btn_langhelp` verbatim, not my spaced
version: **one string in one place beats two spellings of one label.** His wins.

### 👁️ Noticed and deliberately kept
Menu A's content sits high — `cellSvg` puts the icon at 40% and the label at 74%, which centres in a **two-row**
menu and leaves an empty band under a **single-row** one. **Not a defect, and I do not want it changed:** the
shipped teacher menu is the same height through the same function, so menu A matches what is already on the OA.
Recording it so that whoever first sees it on a phone knows it was noticed, not missed.

### ⏳ @Porter — what is left, and it is all deploy
1. `0030` + `0031` on `sid` → `db:migrate` → **`db:verify` ✅ before any restart**.
2. **`publishRichMenus` with all SIX image paths**, then **confirm on a phone — never from the output.**
3. Then, and only then, PENDING DEPLOY **4** (`NAME_TO_KEY`) — I will cut it when the publish is confirmed.
⚠️ Still owner-owed and unrelated to this: **2FA delivery**, `province` as a household field, the two-coloured OA
(orange unknown/known vs blue teacher — a repaint is a migration, not a colour change).

**BALL: @Porter — the REQ-079 deploy.** Nothing is left in the code.

## [--:--] Porter (PM) — ✅ Sober's read lands 3 things. Money round RELEASED to Tanya. → @Tanya @Sober
1. **Auto-attend confirmed in code** — `NO_SHOW` has no writer left (REQ-070/TASK-180 removed it). Owner's ruling
   and the code already agreed; **nothing to build.** 2. **`OTHER` is in both selects** ⇒ AC-9 is reachable.
3. **New standing fact → `SYSTEM-FACTS.md`:** the day-end trigger lives in Task Scheduler, the bookable hours in
   `time.ts`, **nothing spans them** — one added 18:00 slot would silently stop the last class of every day being
   attended or posting. Safe today by 30 minutes.
⇒ `inbox/QA.md` — ฿20 round released, fixture shape stated (`CONFIRMED`, ends before 18:30).
**Owed by the owner and carried by me:** one freelance rate on `sid` for AC-21 (all 10 are ฿0/h).
**Still open, not answered by this read:** `C-13`'s question — does clearing a family's link put that chat back on
the unknown menu? It only becomes observable after the publish.

## [--:--] Porter (PM) — 📦 Owner deployed `sid` (2026-09-05). Awaiting the `db:verify` line before anything else.
Owner: *"deploy sid แล้ว"*. Recorded here because a deploy that nobody writes down is a deploy the next session
guesses at — the TASK-220 lesson.
🔴 **Not confirmed yet and NOT assumed:** that `0030`+`0031` applied and `db:verify` printed ✅. `db:migrate` can
exit 0 having applied nothing (TASK-085/086 — that took the customer's calendar down on both boxes on 08-24).
**I asked him for the output rather than reading "deployed" as "migrated".** Next step after ✅: `publishRichMenus`
with **all six** image paths, then confirm on a phone.

## [--:--] Porter (PM) — 🚀 `sid` DEPLOYED and the SIX rich menus are PUBLISHED (owner, 2026-09-05). → @Sober @Tanya
`db:verify` ✅ — journal 32 · **32 witnessed** ⇒ `0030`+`0031` are in. Then `bun run line:publish-menus` printed
six ids; `unknown-TH` is the **account default**, `known-TH` linked per user.
**Recorded here because a deploy nobody writes down is one the next session guesses at** (the TASK-220 lesson).
🔴 **NOT confirmed: the menus on a phone.** The standing rule is that the command's output is not the evidence.

⚠️ **A prediction I am putting on the record BEFORE he looks, so a surprise is not read as a new defect:**
**chats that were linked BEFORE this publish may now show the UNKNOWN menu.** `linkKnownRichMenu` runs *at link
time*; for an already-linked chat it never ran, and the account default is now `unknown-TH`. If that is what he
sees, the fix is to re-link that chat — **and the question of whether existing links need a backfill is @Sober's,
not a defect report from me.** 📌 This also makes `C-13`'s open question observable for the first time: does
clearing a family's link put that chat back on the unknown menu?

## [--:--] Porter (PM) — ✅ MENU A IS LIVE ON A PHONE. First time REQ-079's two-state menu has ever rendered. → @Sober @Tanya
Owner's screenshot, `SOM-Balance-Demo`, 13:47. **Confirmed ON THE SCREEN, not from the command output** — the
standing rule, and this is the case it was written for.

**What is proven:** 🟠 orange · **two cells** · `เข้าใช้ระบบ` (arrow) · `คุยกับแอดมิน` (chat) · exact labels from
`REQ-079` §12 · `คุยกับแอดมิน` on the right. **`unknown-TH` is genuinely the account default.**
📌 The chat carries LINE's new-follower greeting at 13:47, so this is a **fresh follow** — the cleanest possible
proof of the unknown state, better than the unlink route I proposed.

🔴 **Still NOT proven: menu B.** Nobody has seen the six-cell known menu render. Next: `สมัคร` → link → the menu
must flip. **`C-13` and the "do existing links need a backfill" question stay open until that flip is observed.**

⚠️ **Wording gap, mine (UX writer):** the welcome text still reads *"หลังผูกแล้ว (ผู้ปกครอง): เพิ่มนักเรียน ·
เช็คอิน · ลา · qr"* — it advertises **`qr`**, names none of the menu, and predates `คอร์สของฉัน` and
`ภาษา/ช่วยเหลือ`. **Now that a menu exists, a message telling parents to type words is describing the old world.**
Not urgent, not a defect — a wording pass I owe once menu B is confirmed.

## [--:--] Porter (PM) — ✅ MENU B IS LIVE TOO. 🔴 And the first button on menu A is DEAD. → @Sober @Tanya
Owner's screenshots, 13:47–13:53. **Both states of REQ-079's rich menu have now rendered on a real phone.**

### ✅ Menu B — confirmed, exactly as specced
🟠 orange · six cells · `แจ้งลา · เช็คอิน · คอร์สของฉัน` / `เพิ่มนักเรียน · ภาษา/ช่วยเหลือ · คุยกับแอดมิน` ·
`คุยกับแอดมิน` bottom-right. **The menu flipped on linking**, so `linkKnownRichMenu` fires and is no longer a
no-op. ⇒ **The whole two-state mechanism works end to end.** `C-13`'s premise is now observable at last.

### 🔴 DEF — tapping `เข้าใช้ระบบ` on menu A leads nowhere. Reported by the owner, in his words:
> *"เมื่อกดปุ่มเข้าสู่ระบบที่ richmenu แล้ว มันบอกให้พิมพ์เบอร์ แต่พอพิมพ์แล้วไม่ทำงานต่อ"*

**What he observed (13:50–13:51):** tap `เข้าใช้ระบบ` → bot replies *"กรุณาพิมพ์เบอร์โทรที่ลงทะเบียนไว้ค่ะ
หากยังไม่เคยลงทะเบียนกรุณาติดต่อแอดมิน"* → he typed `0900000092` → **silence** → he typed `สมัคร` → the role
prompt (`1 = ลูกค้า/ผู้ปกครอง · 2 = ครู · 3 = แอดมิน`) appeared and the flow ran normally to a linked account.

**Observation I am NOT turning into a diagnosis:** that prompt's wording — *"กรุณาติดต่อแอดมิน"* — is the
**pre-REQ-079 sentence**, the exact one `REQ-079` §13 ("the parent is never stuck") was written to remove. The
keyword `สมัคร` reaches the new flow; the button appears not to. **Cause is @Sober's read, not my claim.**

🔴 **Severity: this is the FIRST thing a new parent will do.** The menu is the only entry REQ-079 designs for, and
its first button dead-ends into an instruction the bot then ignores. **A keyword rescues it — but a parent has no
reason to know a keyword exists, which is the whole argument for the menu.**

## [--:--] Porter (PM) — ✍️ Wording pass written (`REQ-079` §15). Two strings, one read, one cost. → @Sober @Tanya
The messages were written when typing was the only way in; a button exists now. **Copy only — no keyword removed**
(the owner's 09-03 ruling: typing must keep working for PC users).
**Changed:** the welcome (says *"พิมพ์ สมัคร"* under an `เข้าใช้ระบบ` button) · the after-linking message (a
six-line keyword list under a six-cell menu). **`เมนู`'s full list stays** as the PC reference.
**Not guessed, sent to @Sober:** are `นักเรียน` (keyword) and `คอร์สของฉัน` (menu cell) the same destination? If
not, **the menu cannot reach `นักเรียน` at all.**
**Not guessed, the owner's when it matters:** `qr` has no cell and there is no seventh — and his REQ-015 may
re-scope it anyway. Not urgent.
🔴 **Cost I am naming rather than hiding:** `AC-19`/`AC-22`/`AC-24` were @Tanya's passes on this exact copy ⇒
**re-checked, not carried over.** Told her directly. Sequenced behind the `เข้าใช้ระบบ` defect.

## [--:--] Sober (SA) — 🔴 DEF-9 diagnosed: the button sets no state. Plus both of your open questions, answered from source. → @Porter @Jason @Tanya

Read `line-webhook.service.ts` (the postback dispatch, `CHOOSE_ROLE`/`AWAIT_CODE`), `line-i18n.ts`,
`line-menus-flows.test.ts`, and every rich-menu link call site. Nothing run, nothing sent.

### 🔴 DEF-9 — `เข้าใช้ระบบ` replies and sets no step. **TASK-248 → @Jason.**

`line-webhook.service.ts:1171`, in full:

```ts
if (action === "enter") return send(replyToken, [textReply(t("enter_ask_admin", lang), lang)]);
```

**It sends a sentence and returns.** No `setStep`, no `pendingRole` ⇒ the phone the parent types arrives with **no
session step**, falls past every flow branch and is treated as idle chat. **Silence — exactly what the owner saw.**
`สมัคร` works because it *does* set a step (`:979`).

📌 **The copy is right; the handler is a stub.** `enter_ask_admin` says *"type your registered phone number; if you
have never registered, contact an admin"* — which **is** REQ-079 after §15 deleted Flow 2 and amendment #2 made the
entry the phone alone. ⚠️ And the key is **named** `enter_ask_admin` while its text asks for a phone — **the name
probably is why the step was never wired.** TASK-248 renames it `enter_ask_phone`.

✅ **The fix is one line and no new flow.** `AWAIT_CODE` + `pendingRole = "customer"` **already is** *"type your
phone and get linked"* (`code_customer`, then `:1095` verifies, strikes on failure, links the account and the known
menu). The button must set that step before replying. **Decided by me, so nobody designs around it:**
`เข้าใช้ระบบ` presumes **customer** — a teacher who taps it fails into the AC-18 handover, which is what the
button's own copy promises, and asking a stranger *"parent or teacher?"* as the first question is the friction
REQ-079 removed.

### 🔴 And the reason nobody caught it: the tests were green about the WORDS

`line-menus-flows.test.ts:270–272` asserts the service contains `t("enter_ask_admin", lang)`, that the TH string
contains `เบอร์โทร`, and that it contains `แอดมิน`. **Three assertions about the copy, none about the behaviour.**
The suite proved the bot **says** *"type your phone number"* and never asked whether anything **receives** it.

📌 **This is the comment-vs-code trap in a new costume.** On 09-03 it was counting an identifier in prose; here it
is asserting the sentence instead of the state transition. **Both times the test matched the author's intention
rather than the machine's behaviour, and both times it was green while the thing was broken.** ⇒ The rule, and it
is now in the task: **a source assertion must test what the code DOES — and where it cannot, it must not be
mistaken for the test that does.**

### ✅ Your question 1 — do existing links need a backfill? **Yes, and there are TWO wrong populations.**
`linkKnownRichMenu` runs **at link time only**. Families linked **before** today's publish never had it run for
menus that did not exist. ⇒ a chat with an **old** per-user link still resolves to the **old parent menu** (those
menus still exist on the channel — **this is exactly your 01:23 screenshot, and it was never a puzzle**), and a
chat with **no** per-user link now shows **unknown**. **Neither shows menu B.**
⚠️ **A one-off re-link runs against real customer chats ⇒ it is a deploy action and it is the owner's.** Added to
`board.md` PENDING DEPLOY. If he wants a script, that is its own task — I have told @Jason not to write one.

### ✅ Your question 2 — C-13: does clearing a family's link restore the unknown menu? **No. TASK-249 → @Jason.**
Every link call in the repo is a **link**: `:1110`, `:1113`, `:1179`, `teacher-link.service.ts:152`. **There is no
unlink call anywhere**, and nothing ever links a chat to the unknown menu. `line-rich-menu.ts` states the design —
*"a chat whose per-user link is ever removed falls back to it"* — and **the premise never happens, because nothing
removes it.** ⇒ after TASK-243's admin clear-link, the family is unlinked in our DB and the parent's phone still
shows **menu B: the buttons of an account they no longer have.**
📌 **Third time this week: a documented fallback with no caller** — `UNKNOWN_RICH_MENU` before TASK-247,
`menuHasAdminButton` before its test, this now. **The comment is not the mechanism.**

### ✅ What is genuinely proven today, and it is a lot
Both states of REQ-079's menu have rendered on a real phone, the flip on linking fires, `unknown-TH` is truly the
account default, and `linkKnownRichMenu` is no longer a no-op. **The two-state mechanism works end to end** — DEF-9
is one dead button on a menu that is otherwise doing what it was designed to do.

⚠️ @Porter — your welcome-text pass (`qr`, no menu words) is **more urgent now, not less**: menu A tells a parent
to type a phone, and TASK-248 makes that work. **A welcome message that advertises keywords is describing the world
before today.**

**BALL: @Jason — TASK-248.** DEF-9 first; TASK-249 after it.

## [--:--] Porter (PM) — 🔻 §15's welcome WITHDRAWN. The shop already greets on follow. → @Sober
Owner: *"ลูกค้ามีข้อความทักทายอยู่แล้วนะ"* + a screenshot of the real OA. **He is right.** Their greeting carries
the program list, branch, the 100%-prepayment rule (TH+EN), phone, *"NO WALK IN"* and a price-table image.
⇒ Mine would have been **a second welcome on one follow, branding a system the parent never heard of.**
**Revised in `REQ-079` §15's correction note: the bot says NOTHING on follow** (menu A's `เข้าใช้ระบบ` is the
instruction), or one unbranded line if pressed. **The after-linking message stands** — it fires inside a flow the
parent chose and competes with nothing.
📌 **Raised for the owner, not decided:** the shop's greeting lives in **LINE OA Manager**, ours is a follow-event
push from our code — **two senders, neither aware of the other.** At REQ-014's OA move the greeting **must be
re-created on the new account**; it does not travel, and losing it would be a visible regression on move day.

## [--:--] Porter (PM) — 📌 Rental price lead PARKED in `REQ-028`, not acted on. Owner's instruction.
Owner: *"เรื่องเช่า โน็ตไว้ก่อน มีต้องกลับไปทำอยู่แล้ว รอ รวดเดียว"* — REQ-004 comes back as one piece.
The customer's own published table (in the OA-greeting screenshot he sent about something else): **set 200/hr;
ride 150 · helmet 50 · pads 50.** Written into `REQ-028` as a PARKED LEAD with the instruction to compare it
against SPEC-031's four codes **before** any question goes back to the customer.
⚠️ **Recorded as a lead, not a defect** — SPEC-031 matched the ledger on 08-30, and four prices vs four codes is
a resemblance, not a comparison. **Nothing is being built or asked.**

## [--:--] Jason (BE) — ✅ TASK-248 + TASK-249 → REVIEW. Two dead buttons and a fallback with no caller. → @Sober

Repo **`smart-scheduler-back`** (path in `machine.local.md`), HEAD **`864ef9c`**. **Verified:** `tsc --noEmit`
**0** · `bun test` **1316 pass / 0 fail** · **no migration** — 32 `drizzle/*.sql` = 32 journal tags, re-counted.
🚫 Nothing sent to LINE: no publish, no adopt, no link, no unlink, no SQL. Every assertion is pure or reads
source.

### TASK-248 (DEF-9) — the button now sets the step it was asking a question for
Your three lines exactly: `setStep(AWAIT_CODE, "customer")` before the reply, no second phone flow, and the
`AWAIT_CODE` branch asserted to be the **only** one that answers a typed phone. `enter_ask_admin` →
`enter_ask_phone`; the text is unchanged in both languages.

📌 **I took your §4 rule literally and split the new file into BEHAVIOUR and WIRING, labelled in the source**, so
nobody can later mistake the second for the first. The behaviour half runs the real `decideMessageRoute` —
including the contrast that *is* the defect: with no step, an unknown follower's typed phone routes to
`silence`. ✅ **And I proved the file catches the original bug**: reverting the handler to the old one-liner
makes it fail; restoring passes.

**Your question, traced not assumed — an already-linked chat that taps `เข้าใช้ระบบ`:** it re-links to itself
(harmless, ends at the add-student prompt), and someone else's number is refused with a strike, so the
no-silent-re-pointing guarantee holds on this path too. 🔴 **The one real cost, named rather than guarded:**
while that session is live a linked parent's next ordinary message is read as a phone attempt, so a mistaken tap
costs them two strikes or a 30-minute wait. ⚠️ And `ยกเลิก` does **not** rescue them — the exit exists in the
add-student wizard and in a muted chat, but **the unmuted linking branch has no exit at all** (`สมัคร` has the
identical gap, so this is pre-existing, not new). Out of scope here; your call whether it earns a follow-up.

### TASK-249 (C-13) — the fallback got its caller, and the grep found a second one
`unlinkRichMenuFromUser` (`DELETE /user/{id}/richmenu`) is wired to **both** DB link-clears: `clearFamilyLine`
(every account the family held — a family can hold several since TASK-230) and **`unlinkTeacherLine`**, which had
the same defect in the other role: a departed teacher kept `ตารางของฉัน` on their phone. 🚫 `moveRosterLink` is
deliberately **not** wired — it is a *move*, and the caller links the new role's menu immediately after;
unlinking there would blank a real phone and re-link it. Asserted as an absence so nobody completes the set later.
⚠️ **A trap inside the teacher fix:** `.returning()` hands back the row as it now is, where `lineUserId` is
already `null` — reading it there would unlink nothing and look like it worked. The account is read before the
write, and the test pins that order. Both calls run after the commit and are `.catch`-wrapped: a LINE hiccup must
not roll back an admin's database act, and a clear the database refused must never reach a phone.

§4: `NAME_TO_KEY` has all six, and the guard **reads `publishRichMenus` itself** rather than a list someone must
remember — the next menu added cannot forget the adopt map.

📌 **The finding worth keeping, and it is uncomfortable:** one of my own TASK-234 tests asserted
`not.toContain("unlinkRichMenu")`. **That passing assertion WAS the C-13 defect** — I had pinned the absence of
the exact call the design's own fallback sentence depends on. Corrected, not loosened: it now asserts the half
that must stay true (nothing **links** a chat *to* unknown — that would be a second definition of the state)
while the **removal** exists. *Two different acts; only one was ever meant to be impossible.* Three of my earlier
tests were corrected across these two tasks, all of them pinning behaviour that a later task deliberately changed.

**BALL: @Sober — TASK-248 + TASK-249 review.**

## [--:--] Sober (SA) — ✅ TASK-248 + TASK-249 both DONE. 🔴 And the week's sharpest finding: a passing test had PINNED the C-13 defect. → @Jason @Porter @Tanya

**Reproduced for both:** `tsc` **0** · `bun test` **1316 pass / 0 fail** (110 files) · `unlinkRichMenuFromUser` at
`line-rich-menu.ts:185`, called from `family-link.ts:158` and `teacher-link.service.ts:195` · the `enter` handler
sets `AWAIT_CODE` + `customer` **before** replying (`:1185`) · `NAME_TO_KEY` at six. Nothing sent to LINE.

### 🔴 The finding that outranks both fixes

> Jason: *"One of my own TASK-234 tests asserted `not.toContain("unlinkRichMenu")` and now fails — corrected, not
> loosened. **That assertion WAS the defect**, written down and passing."*

**C-13 was not an oversight. It was pinned.** A green test stood guard over the missing call, so anyone who added
it would have been told by the suite that they were wrong.

📌 **The class, and it is worth more than the case:** *an assertion that something does NOT exist is only as good
as the reason it doesn't.* When the reason is a design rule, the test is a **control**. When the reason is merely
*"nobody has written it yet"*, the test **freezes an accident into a rule** — and the two are indistinguishable at
a glance. ⚠️ **Every `not.toContain` in this repo is now suspect until someone can say which kind it is.** Not a
task today; the question to ask whenever one is touched.

✅ **And he corrected it at the right seam**, not by deleting it: *"removing a link and linking a chat TO unknown
are different acts — the first is the fallback's caller, the second would be a second definition of the state."*
The removal now exists; `linkUnknownRichMenu` is still asserted absent. **Two acts a coarser fix would have
merged.**

### ✅ TASK-249 — three clear paths, and the third is the judgement
`clearFamilyLine` ✓ · `unlinkTeacherLine` ✓ — **a defect nobody had reported:** a departed teacher kept
`ตารางของฉัน` on their phone, same shape, other role · `moveRosterLink` 🚫 **deliberately untouched** because it is
a **move** (the caller links the new role's menu straight after, so unlinking would blank the phone and re-link
it), **asserted as an absence with its reason attached** — which is exactly the good kind of `not.toContain`.
⚠️ **The `.returning()` trap he named is real and I confirmed it:** the returned row already has
`lineUserId: null`, so reading it there would have unlinked **nothing and looked like it worked**. The account is
read before the write and the order is pinned.
✅ Every account in `result.cleared`, not just the primary (a family can hold several since TASK-230), and both
calls run after the commit — *a clear the database refused must never reach a phone.*

### ✅ TASK-248 — DEF-9 closed, and the test file answers §4 properly
The split is labelled **in the source**: *"BEHAVIOUR ones run the real decision function; WIRING ones read the
source and are honestly only that."* 📌 **The second clause is the lesson.** The old tests were not bad for
reading source — they were bad because **nothing said they were not the coverage.**
**And the behavioural line IS the defect as a value:** `decideMessageRoute(undefined, null)` → `"silence"` —
**the owner's typed phone number, as a number in a test instead of a story in a log.** Beside it,
`decideMessageRoute(undefined, "customer")` → `"linked"` shows the miss was *"no step"*, not *"not recognised"*.
✅ He proved the file catches the original bug by reverting the handler and watching the right test fail.

✅ **Already-linked chat: traced, and NO guard added** — a linked parent who taps it re-lands where they already
were (`verifyAndLink` sees the same `lineUserId`, `bindFamilyLine` is idempotent). **A guard there would be code
defending a harmless path, and every guard is a branch someone must later understand.** Not adding it, with the
trace to show why, is the right answer.

📌 **One I am carrying rather than changing:** `enter_ask_admin` was a key whose **name argued with its own text**,
and that is my best guess at why the step was never wired — the author read the name, saw *"ask an admin"*, and
wrote a reply. **A misleading name is a defect with a long fuse.** The rename was worth more than the line beside it.

### 🧪 @Tanya — three replays, and the first is the owner's
1. **DEF-9 on a phone:** tap `เข้าใช้ระบบ` → type a registered number → **the account links and the menu flips to B.**
2. **The wrong-phone path:** two bad answers reach a human, not a loop.
3. **TASK-249:** an admin clear-link → **the parent's menu falls back to A** (and a departed teacher's does too).

⏳ @Porter: PENDING DEPLOY **item 5** — the backfill re-link for families linked before the publish — is still the
owner's, and your welcome-text pass is still owed.

**BALL: @Tanya — the DEF-9 phone replay.** It is the owner's own sequence and he is the one who reported it.

## [--:--] Sober (SA) — 🔻 Correction from the owner: **@Tanya cannot test LINE. Only he can.** Three replays re-routed. → @Porter @Tanya

Owner, verbatim, correcting me: *"tanya cannot test line that me only one can test."*

**He is right and I should have known it from this repo's own facts.** I put three LINE replays on QA today
(DEF-9's tap-and-type, the wrong-phone handover, the clear-link fallback) and earlier the DEF-8 sequence and
Round C. **None of them are runnable by @Tanya** — and not as a matter of permission:
- **LINE on PC has no rich menu and its buttons cannot be tapped at all** (already in `SYSTEM-FACTS.md`). A menu
  test needs a phone.
- **`sid` and `uat` share ONE LINE channel** and **`sid` is not a safe isolated box** — real people are linked,
  and QA's own standing rule is *"never message real people."*
- The owner holds the OA and the phone. **Nobody else can produce the evidence.**

⚠️ **What misled me is in the file too, and it is too loose:** *"inbound LINE is testable on `sid` any time."*
That is true of the **webhook** and false of the **experience** — the sentence has been narrowed where it sits.

### 📌 The mistake underneath, which is worth more than the re-routing
**I wrote a test plan without asking who can physically run it.** Every item was well-formed, and it was parked
with someone who could never close it. ⇒ **A test nobody on the team can perform is not a plan — it is a request
wearing a checklist's clothes.** Written into `SA-Lead.md` as a standing rule, and into `SYSTEM-FACTS.md` § LINE
as the routing fact.

⇒ **The rule, from now on:** anything needing a real LINE account on a phone goes **@Sober → @Porter → the owner**
and returns the same way. **@Tanya keeps everything reachable without a phone** — API and DB behaviour, the web
app, and the outbox rows a flow is supposed to write. **The split is the phone, not the feature.**
@Porter — `QA.md` still describes LINE as in-scope for `sid`; **that is yours to correct, not mine.**

### The five checks, re-addressed to the owner via @Porter
1. 🔴 **DEF-9, his own sequence:** tap `เข้าใช้ระบบ` → type a registered number → **the account links and the menu
   flips to B.** He reported it; he is the one who can confirm it.
2. **Two wrong phones in a row** must reach a human, not loop.
3. **An admin clear-link** → that chat's menu falls back to A (and a departed teacher's does too).
4. **DEF-8:** muted → button → the bot asks → type a name → **it is answered.**
5. **Round C:** a duplicate name asks for a surname — not a strike, not a handover. ⚠️ **AC-25 must not regress:**
   while muted, `เมนู` · `เพิ่มนักเรียน` · free text stay silent.

**BALL: @Porter — take the five to the owner.** They are all one phone session.

## [--:--] Porter (PM) — 🔧 QA.md corrected: LINE is out of scope for QA on EVERY box. C-13 NOT closed. → @Tanya @Sober
1. **`QA.md` fixed** — it still listed LINE as in-scope on `sid`; @Sober flagged it and correctly did not touch my
   file. New section: LINE is the one `sid` exception, with the three reasons (no rich menu on PC · one shared
   channel with real people · the owner holds the phone) and the **hands/verdict/carrier** division spelled out.
   **Everything else on `sid` is unchanged for her** — backoffice, database, money paths.
2. **`C-13`: @Sober told me to close it. I did not, and said so in the file.** TASK-249 fixes the code; **that is
   not the same as the owner closing an entry.** Recorded as Side F, the strongest side, still a side.
📌 **@Sober's own miss, recorded because it is a good one:** he wrote a test plan without asking who could
physically run it. He caught it, re-pointed the board rows and wrote the rule into `SA-Lead.md`.

## [--:--] Tanya (QA) — ⏳ ฿20 round is LIVE in the **right** fixture shape. 📌 And Round 4's open question was my error, not the product's. → @Porter

Inbox read and cleared. Evidence: **`tests/TEST-064` §Round 5.**

### 📌 First, the correction — because it is mine
Your release named the fact I had been reasoning without: **the day-end selects `CONFIRMED` only; a `PENDING` row
sits forever.** In §Round 4 I offered three readings for F1/F2 sitting unattended, one of which was *"the job only
promotes CONFIRMED ⇒ my fixtures were the wrong shape, which is my error to own."* **That is the one that was
true.** Flagging it as a possibility instead of filing a defect was right; **building the fixture wrong was not,
and it cost the round two days.** F1 and F2 were never going to post — no amount of waiting would have changed it.
(The 23:30 → **18:30** correction I take as yours, not mine: I was working off the number you gave me.)

### ⏳ Live now, and shaped to your spec
Server clock read **14:35** when I started — ~4h before the pass. Both **`CONFIRMED`**, both **ending 16:00**:

| | อื่นๆ booking | id | proves |
|---|---|---|---|
| **M1** | **Ek** · 2026-09-05 15:00–16:00 · no student · **charge ON ฿20** | **`5788d6fe-6099-40a4-8440-712ed7ceac5e`** | **AC-5** + **AC-9** |
| **M2** | **Kowjoe** · 2026-09-05 15:00–16:00 · no student · **charge OFF** | **`6ac8c7d4-95e0-4370-bf93-0534df5ed5de`** | **AC-4** + **AC-9** |

**The key the owner needs is `rev:5788d6fe-6099-40a4-8440-712ed7ceac5e` (฿20).** M2 is the control — without it
"M1 posted ฿20" proves much less.

🟢 **Teacher choice was deliberate and the API confirmed it:** Ek and Kowjoe are **unlinked**, so both confirms
returned **`notification {status: skipped, reason: "ผู้รับยังไม่ผูก LINE userId"}`** — **nothing reached a real
person.** Bank (you/the owner) and **Haris** were not used. 📌 Small good thing worth keeping: the outbox records
**`skipped` with a reason** rather than silently doing nothing — a send that never happened is visible as such.

### 🔴 The one thing that decides whether this round produces a verdict
**I still cannot read the money.** Backoffice access is the board's own open item for the human. After 18:30 I can
close **AC-9** myself from the calendar — but **AC-4 and AC-5 stay `NOT_TESTED` even after a perfect sweep**
unless the owner reports what `rev:5788d6fe-…` posted, or I get read access. **I will not infer a money outcome I
could not observe** — your instruction and my rule all week. **The fixture is finally correct; the observation is
the only missing piece** (Q25).

**AC-21** still needs **one freelance rate** on one `sid` freelancer (all 10 are ฿0/h). I did **not** park the
round on it, as you said. It is the last cheap AC in REQ-078 (Q26).

### 🧹 F1/F2 are now KNOWN-DEAD, not merely unread
`4014e65e-…` and `25d695c3-…` are `PENDING` on 09-01 ⇒ with the rule known, **unsweepable**. M1/M2 supersede them.
**Recorded so no future session re-adopts them as live evidence**; they should be cancelled.

### On `uat` — the new grant, and what I did with it
I read the updated `QA.md`/`CLAUDE.md` at session start. **Read-only on `uat` is noted; I made no contact with it
of any kind today.** The frontoffice `PRODUCTION_HOSTS` guard still refuses, and **I did not look for another
route around it** — and the backoffice's *absence* of a guard is not permission either.
⚠️ **One stale line to fix when you next touch the board:** `board.md` §Team still reads *"tests on local + `sid`
(never `uat`)"*, which the owner's 09-04 grant supersedes. **Yours to correct — I don't edit that row.**

**Ball: @Porter** — Q25 (who reads the ฿20 after 18:30) · Q26 (one freelance rate). REQ-078's Q13–Q16 and
REQ-079's Q23–Q24 remain open. I will check the ATTENDED flip myself after the pass.

## [--:--] Porter (PM) — 📱 Owner's phone run, 15:58–16:02: 4 of 5 observed. Check 4 not run (my instruction was unclear). → @Tanya
**Owner's own words: 1 ผ่าน · 2 ผ่าน · 3 ผ่าน · 4 งง · 5 เคยเห็นแล้ว ผ่าน.** Screenshots held.
⚠️ **These are OBSERVATIONS, not verdicts.** He is the hands; **the verdict is @Tanya's** — evidence below.

- **1 (DEF-9/TASK-248)** — tap `เข้าใช้ระบบ` → `0900000092` → *"ผูกบัญชีผู้ปกครองสำเร็จ ✅ … มิลล่า, มิลลิม, asda"*.
  **The bug he reported this morning is gone.**
- **2** — `นพดยนกนก` → *"เบอร์โทรไม่ถูกต้อง … (เช่น 0812345678)"* · `Kfkfkfkf` → *"ขอโทษค่ะ ขอส่งให้แอดมินช่วยดูนะคะ
  🙏 (ถ้าต้องการใช้บอทอีกครั้ง พิมพ์ เปิดเมนู ค่ะ)"*. **Two wrong phones reach a human, and the way back is told.**
- **3** — clear-link → menu A. **5** — duplicate-name → surname.
- 📌 **Incidental evidence he did not report:** 16:00 `แจ้งลา` → *"วันนี้ไม่มีคาบที่แจ้งลาได้"*. **That is the
  leave flow answering** — one of the three REQ-079 flows that has never been opened. Not a criterion pass;
  recorded so it is not re-discovered.

🔴 **Check 4 (DEF-8/TASK-246) was not run — my fault, not his.** I wrote it as *"ตอนบอทถูกปิดเสียง → กดปุ่ม →
บอทถาม → พิมพ์ชื่อ"* without saying how to get muted or which button. **Re-issued as five literal steps.**
📌 **He is muted right now** (16:02 handover), so the state the check needs already exists.

⚠️ **One thing to ask, not to report as a defect:** at 16:01 *"กรุณาพิมพ์เบอร์โทรที่ลงทะเบียนไว้ค่ะ"* appears
**twice, back to back**. Could be a double tap. **Asked; not filed.**

## [--:--] Porter (PM) — ✅ Check 4 observed. 🔴 But the SAME screenshot may show AC-25 broken. Asking, not filing. → @Tanya
Owner: *"ข้อ4 ผ่าน"*. **The tap-while-muted path works** — `เพิ่มนักเรียน` → name → birthdate → the typed reply is
received. DEF-8/TASK-246 does what it says.

🔴 **What is also in that screenshot, at 4:07 PM, and I cannot resolve it from the image:**
`Hhhh` (2nd bad birthdate) → **`ขอโทษค่ะ ขอส่งให้แอดมินช่วยดูนะคะ 🙏 (… พิมพ์ เปิดเมนู ค่ะ)`** — the handover —
**and immediately after it, `พิมพ์ชื่อนักเรียนที่ต้องการเพิ่ม…`**, then `Gg` → **`วันเกิดของน้อง…`**.
**The bot hands the chat to a human and is talking again in the same minute.**

**The single fact that decides whether this is a defect: did he TAP `เพิ่มนักเรียน` between the two?**
- **Tapped ⇒ correct.** An explicit tap is allowed to wake the bot — that is check 4's entire premise.
- **Not tapped ⇒ 🔴 AC-25 is broken** and it is the worst kind: the bot barges into a conversation it just
  handed to a person, which is the one thing this REQ exists to prevent.

⚠️ **I am NOT filing it and NOT telling @Sober yet.** A screenshot does not record taps. **Asked the owner.**
📌 **Same discipline as the 16:01 duplicate prompt** — also still unanswered, also not filed. **Two open questions,
zero defect reports.** Reading a screen as proof of something it does not show is how C-13 started.

## [--:--] Porter (PM) — ✅ Owner answered: he DID tap. No defect. And he then proved AC-25 unasked. → @Tanya
Owner: *"ใช่ ก็นายบอกให้ทดสอบว่า ถ้ามันปิดปากแล้ว ไปใช้ปุ่ม มันจะกลับมาทำงานได้มั้ย ก็ลองแล้ว ได้"*.
⇒ **The 4:07 sequence is correct behaviour, not a barge-in.** Asking beat filing; had I sent it to @Sober it
would have been a false defect built on a screenshot that could not show a tap.

🔴 **Then he did the harder half without being asked to.** 4:08–4:09, second run: got himself muted again
(`ดด` → `ดด` → `ผพ` → handover), then typed **`เมนู`** — a command the bot advertises about itself — and
**`ๅๅๅๅๅๅๅๅ`**. **Neither got a reply. Silence, both.**
⇒ **AC-25 holds in its strong form, on today's build.** That is the criterion @Sober warned these changes were
most likely to break, and it is the premise the whole REQ rests on: **an admin can hold a conversation and the
bot will not interrupt.**

**Owner's own reading, and it is right:** *"แสดงว่าถูกแล้ว นี่เพราะเราถูกปิดอยู่ มั้ย?"* — yes.
**⇒ All five checks observed. Handed to @Tanya for the verdict; the verdict is hers, not his and not mine.**
⚠️ **Still unanswered and still not filed:** the 16:01 duplicate `กรุณาพิมพ์เบอร์โทร`.

## [--:--] Porter (PM) — 🔴🔴 THE BOT IS ON THE CUSTOMER'S REAL LINE OA. Owner switched it today. → @Sober @Tanya
**This is the single biggest environment change since I have been PM and it happened between two messages.**
Owner: *"ลูกค้ามีไลน์ OA มาให้ … ฉันก็ลองขึ้นไปให้ลูกค้าแล้ว … แค่แก้ env บน server ให้เป็นของเขาแล้ว restart
ส่วนของ secret + access token และเขาก็ชี้ webhook มาแล้ว … พวกคำสั่งใช้งานได้หมด"*.

**What is true right now:** the channel secret + access token in the server's env are **the customer's**, the
customer has pointed **their** webhook at us, the bot restarted, and **the typed commands answer on their OA.**
**Their own greeting and notifications are already on that account** — the ones in his 09-05 screenshot.
**Not yet done: the rich menus.** They were published to the demo OA; **menus do not travel with a token.**

🔴 **THREE things nobody has stated, and I am stating them before anyone acts:**
1. **WHICH BOX has the customer's token?** `sid` or `uat`? **I do not know and will not guess.** If it is `sid`,
   then every test on `sid` — including tonight's ฿20 run — now speaks through **the customer's live account.**
   **Asked, before anything else.**
2. **Every existing `family_line_links` row may be dead.** A LINE `userId` is scoped to the **provider**. If the
   customer's OA sits under their own provider — it almost certainly does — **no existing link matches any
   userId on the new account.** His test families and the 2 real teachers included. **Needs verifying, not
   assuming**, and it changes what "linked" means for every open AC.
3. **PENDING DEPLOY item 5 (the backfill re-link) may be obsolete or may have become the whole migration.**
   It was written for one OA. **Do not run it until 1 and 2 are answered.**

📌 **The customer's first change request, relayed by the owner:** the role step's `1 / 2 / 3` **collides with
numbered replies their OA already uses.** They want words. **That is mine — see the REQ-079 entry.**

## [--:--] Tanya (QA) — 🎉 **AC-9 CLOSES (no waiting needed) · REQ-079 → 16 of 26, AC-2 finally falls.** 🔴 And one caveat outranks both counts. → @Porter

Inbox read and cleared. Rulings: **`TEST-064`** (AC-9) and **`TEST-065` §Round 4** (the phone run).

### 🎉 AC-9 is PROVEN — and the evidence was already on the box
I went to check whether the 18:30 pass had run (it had not — server clock **17:16**), and found the experiment
already complete in the status column:

| อื่นๆ | left as | now |
|---|---|---|
| F3 · F4 · **R4a** | **CONFIRMED**, unmarked | ✅ **all three ATTENDED** |
| F1 · F2 | **PENDING**, unmarked | ✗ **both still Pending** |

⇒ **Three confirmed sessions auto-attended across two nights; two pending ones never were.** **AC-9 PASS**, and
the `CONFIRMED`-only rule is now confirmed **from the running product**, not only from @Sober's source read —
the two pending rows are the control that makes the three attended ones mean anything.
📌 **The fixtures I got wrong and the ones I built for another purpose together formed exactly the comparison
AC-9 asks for.** It had been sitting there since 03-09; I only had to read the status column. **A botched
fixture is still data.** 🟢 All three were charge OFF ⇒ **no stray movements**.

⇒ **Tonight's 18:30 pass is now needed only for the MONEY.** M1 `5788d6fe…` (฿20) and M2 `6ac8c7d4…` (free) are
`Confirmed` and waiting. **AC-4/AC-5 still stay `NOT_TESTED` unless someone reads `rev:5788d6fe…`** — I have no
backoffice access, and I will not infer it.

### ✅ REQ-079 — ruling on his five checks: **16 of 26. AC-2 falls at last.**
- 🎉 **AC-2 PASSES.** He **tapped `เข้าใช้ระบบ`** → `0900000092` → children returned, menu flipped to B. **This is
  the AC I refused to close on 09-03** because that entry was *typed* and a postback is a different handler
  branch. **The button now exists and it works — waiting was right.** ✅ DEF-9/TASK-248 fixed in the same action.
- ✅ Re-proved **on today's build**: AC-14/21 (two strikes) · AC-24 (the way back) · DEF-8 (tap-while-muted) ·
  **AC-25 in the strong form** — muted, then `เมนู` and `ๅๅๅๅๅๅๅๅ` → silence. He did that one unprompted; it was
  worth re-running because TASK-248/249 shipped after its last pass.
- ✅ **`C-13` ANSWERED** — clear-link puts the chat back on menu A (TASK-249). Observed, not inferred.
- 🔴 **Two I did NOT count, and both matter:**
  - **Check 2 does not close AC-3.** `นพดยนกนก` / `Kfkfkfkf` are **malformed**, not *valid-but-unknown*. AC-3 is
    about a well-formed number the system does not know, and its "reveals nothing" clause is the point.
  - **Check 5 is not today's evidence.** *"เคยเห็นแล้ว"* is a **remembered sighting**. AC-6 stands as PASS from
    the 09-03 run; today adds nothing. You flagged it yourself and you were right to.
- 📌 `แจ้งลา` → *"วันนี้ไม่มีคาบที่แจ้งลาได้"* — the leave flow **answering**. **Not an AC-10 pass**: AC-10 wants
  child → session → confirm → **teacher notified**. Reachable ≠ tested.

📌 **On the 4:07 "barge-in":** you asked instead of filing, and the tap made it correct behaviour. **A screenshot
cannot show a tap** — had it gone to @Sober it would have been a false defect. Same discipline on the 16:01
duplicate prompt, still unanswered and still unfiled. That is the right shape.

### 🔴🔴 The caveat that outranks both counts — please don't let the numbers travel without it
**Every one of REQ-079's 16 passes was observed on the DEMO OA.** The bot is now on the **customer's real OA**.
Your own three unknowns decide how much survives: **which box holds the token**, whether **every
`family_line_links` row is dead** (a `userId` is provider-scoped), and the fact that **the menus do not travel
with a token**.
⇒ **AC-2 · 13 · 23 · 24 · 25 · 26 — everything resting on a menu button or on a link existing — needs
re-validating on the customer's OA**, where the menus are not published and the links may not resolve.
**Not a retraction and not a defect: it is the scope the evidence actually has.**

⚠️ **And it reaches my own footprint.** Every *"no real person was messaged"* line I have written was reasoned
about the **demo** channel. **From the switch onward that reasoning must be redone**, and **I will not create
another LINE-touching fixture until you tell me which box carries the customer's token.**
🟢 **It does not affect tonight's money run** — M1/M2 were confirmed before the switch, both returned
`notification: skipped` (Ek and Kowjoe unlinked), and ฿20 posts to **`sid`'s `bo.movement`**, a DB write, not a
message. **I am leaving them to run** rather than destroy the only correctly-shaped money fixture over a
delivery risk.

### ⚠️ Not mine, and you should see it
**`ECA ABC - Balance Play` — Ek, Haris, Camp, Kowjoe — 04/Sep — Attended.** **No `QA-` marker: not mine.**
⇒ **staff are already using อื่นๆ with FOUR teachers in earnest**, and the day-end swept it normally. The feature
under test is in real use — and **a real linked teacher (`Haris`) is on it.** I did not create it and have not
touched it.

### Footprint
**Read-only.** One filtered read of `All bookings` (Type=Other, 11 rows, both pages). No booking created, no LINE,
no setting, no script, **no `uat` contact of any kind**. M1/M2 remain live by design; F1/F2 remain the
known-dead pending pair.

**Ball: @Porter** — Q25 (who reads `rev:5788d6fe…` after 18:30) · Q26 (one freelance rate for AC-21) · and the
new one: **which box holds the customer's token**, because it gates every further LINE fixture I would build.

## [--:--] Porter (PM) — 🔴 Answered: the customer's token is on BOTH boxes. Inbound is safe; OUTBOUND is not. → @Sober @Tanya
Owner: *"env token + secret ไลน์ ของลูกค้าคือก็จะเป็นทั้งคู่ … ฉันบอกลูกค้าไปแล้วว่าจะทดสอบให้สลับ webhook เป็น url uat"*.
Written into `SYSTEM-FACTS.md` § "The customer's LINE OA".
**The asymmetry nobody had named: the webhook controls INBOUND only. Every PUSH rides the token, and both boxes
hold it** ⇒ `sid`'s 08:00 / 08:15 / 18:30 jobs can reach the customer's real users irrespective of the webhook.
**Probably inert today** only because `sid`'s stored userIds come from the demo OA — **an accident of data, not a
guard.** 🔴 **And it stops being true the first time the webhook points at `sid`:** real people link into `sid`'s
DB and `sid`'s daily jobs keep messaging them afterwards, forever.
**Recommendation to the owner: `sid` keeps the DEMO token; only `uat` gets the customer's.** Costs nothing — the
webhook switch already gives him what he wanted — and it is the only thing that makes an accident impossible.
⚠️ **Tonight's 18:30 ฿20 run is on `sid`. Whether auto-attend pushes anything to LINE is @Sober's read — asked.**

## [--:--] Porter (PM) — 🔻 Owner overruled my `sid`-token recommendation, with better reasons. Correcting myself.
His two: **(1) the customer's OA is configured differently from the demo in many ways** — a green demo run proves
nothing about theirs, and **the 1/2/3 collision is the proof: it could only ever be found on their account.**
**(2) The people on that OA today are the shop's ADMIN STAFF doing the testing — no parent has been let in.**
🔻 **I said a stray push would reach "real parents". It would reach the admins running the test.** The risk was
real; **the severity was not what I claimed** — and overstating a risk is its own defect. I have done it on this
project before (AC-4, 09-02). `SYSTEM-FACTS.md` updated; my recommendation is marked **superseded, not deleted.**
🔴 **Kept as a TRIGGER, not an argument: the day the shop opens that OA to real parents, `sid`'s live token
matters again.** Whoever reads it that day raises it then.

## [--:--] Porter (PM) — 🟢 Owner cleared the publish on the customer's OA. ⚠️ It must run ONCE, not once per box. → @Sober
Owner: *"ไม่มี rich menu … เขายอมให้เราทำก่อน แล้วเดี๋ยวเขาใช้ทดสอบไป เขาจะบอกให้แก้เอง"*. **Green light, and the
customer expects to send changes back — the menu is a draft to them, not a delivery.**

🔴 **Caught before he ran it, from the source:** `publishRichMenus` **creates six NEW menus every run and deletes
nothing.** Both boxes hold the customer's token ⇒ **running it on `sid` and again on `uat` leaves TWELVE menus on
the customer's account**, six orphaned, each box's DB pointing at a different set. On a customer's live account.
⇒ **Told him: run it ONCE, on the box the customer's webhook points at (`uat`).**

⚠️ **The gap that leaves, and it is @Sober's:** `sid`'s `app_settings` would hold **no ids for the customer's OA**,
so anything served from `sid` finds no `knownTH` and `linkKnownRichMenu` is a **silent no-op there** — the exact
failure C-13 was. **How the ids reach the second box without a second publish is a question, not a guess.**
📌 Also from the same read: `*_EN` menus are created only when their images exist — **a stored id with no uploaded
image renders BLANK**, which is worse than falling back to the default.

## [--:--] Porter (PM) — 📤 Published on `uat`. Wrote the customer-facing flow guide for the shop's admins.
Owner: *"publish uat แล้ว ต้องแจ้ง flow ลูกค้าหน่อย"*. Guide handed to him in chat; text saved to
`project-docs/2026-09-05-customer-line-flow-guide.md` — **this is what the customer will be told, so later status
reconciles against it, not against the board.**
**Two things I deliberately did NOT claim in it:** that แจ้งลา · เช็คอิน · คอร์สของฉัน work (**never opened in
test — 11 ACs still `NOT_TESTED`**), and that the role step is finished (**their 1/2/3 request is not built yet;
the guide says so in their own terms**). Framed as *"what to try"*, never *"what works"* — the customer is the
first person to run these flows, and a guide that promises passes we do not have would be the worst possible
first impression.

## [--:--] Porter (PM) — 📉 REQ-076's 7 blocking questions cut to 3. Four were already answered. → @owner
The owner said *"ไปเรื่องอื่นกันเลย"* while the ฿20 run waits for 18:30, so I took the next queue item
(**REQ-013 → board `REQ-076`, #3 in his order**) and did the work that does not need him.

**Four of my seven questions had already been answered by decisions on the record** — priority (09-01), the money
rule (*money never moves as a side effect of a staff click*), the expiry clock (REQ-071's resume-by-editing-expiry
**plus his brand-new REQ-017**, which asks for exactly that control), and the teacher LINE message (*"ถ้ามีครูก็ส่ง
ไลน์ ไม่มีครูก็ไม่ต้อง"*, 09-02). **Each is now written as a proposal carrying the decision it rests on**, for him
to correct rather than answer.
🔴 **Asking a man to repeat a decision he has already made is a defect in me, not a question.** He has been made
to do that more than once on this project.
**Three remain and none can be derived:** all-three-types-or-two · voucher quota give-back-or-hold · where the box
lives. **ACs get written the day they land.**

## [--:--] Porter (PM) — ✅ REQ-076 ANSWERED and fully specified. 18 ACs + wording. New `REQ-080` captured. → @Sober
Owner answered all three same-day: **all 3 types** · **HOLD the voucher quota** · **the tray lives on the CALENDAR**.
📌 **He answered my objection better than I asked it.** I argued for the bookings page because a **dateless item
cannot sit in a calendar grid** (REQ-078's DEF-4). **A tray is not in the grid** — it sits beside it. Both his
visibility requirement and my constraint are satisfied, and **AC-10 now forbids it ever becoming a grid cell**,
so nobody re-introduces DEF-4 in six months.
🔴 **He also split a REQ off while answering** — voucher cancel-with-quota-return is *"อีกเรื่องนึงต่างหาก"* ⇒
`REQ-080`, **captured, not queued, not sized, and flagged as NOT on his customer list.**
💡 **His floated calendar→bookings button is recorded as a CANDIDATE, not scope** — he said *"ลองคิดเฉย ๆ นะ"* and
that is not a requirement. Parked in REQ-076 § Open next to the small-items batch.
**REQ-076 is `READY_FOR_SA`.** Two Open items, neither blocking. **It is #3 and the queue is still on LINE.**

## [--:--] Porter (PM) — 🔍 `line:inspect-menus` on the customer's channel: CLEAN. One question for @Sober. → @Sober
Owner ran the read-only diagnostic. **Exactly SIX menus on the channel, no duplicates** — the publish ran once,
as instructed; the twelve-menu trap did not happen. **default = `unknownTH`** ✅. **And the list contains nothing
but ours** ⇒ the customer's OA genuinely had no rich menu of its own, confirming what he said.

🔴 **One thing in the dump I am asking about rather than reporting as a defect:**
`knownTH` cell **#4 is `action=lang`** and **#5 is `action=admin`**. The old `parentTH` carries `lang` (#4) and
**`help` (#5)** as separate cells. ⇒ **`help` has no cell on `knownTH`, but the label I specced for that cell
reads `ภาษา/ช่วยเหลือ`.** Either `lang` serves both — which is what the **teacher** menu's `btn_langhelp` already
does — or **the artwork promises ช่วยเหลือ and the tap cannot deliver it.** **@Sober's read. I am not calling it.**
📌 This is the owner's *"เก็บไว้"* ruling landing in the geometry: he kept both, and six cells only fit if they
share one.

⚠️ **Also to confirm with him, because it decides whether menu B can ever render for a real parent:** the publish
and this inspect were run from **his laptop shell**, and the ids matched. **Which database holds them — `uat`'s?**
If the ids are not in the DB of the box serving the customer's webhook, `linkKnownRichMenu` finds no `knownTH`
there and is a silent no-op. **Asked. Not assumed either way.**

## [--:--] Porter (PM) — 🧰 Asked @Sober for a rich-menu REMOVE tool. Pre-emptive, not urgent. → @Sober
Owner: *"ถ้าลูกค้าบอกให้เอาออกก่อน ฉันจะลองเอาออกดู"* — he wants it **on the shelf before the customer asks.**
**We have no way to remove them today** (publish · inspect · per-user unlink only). Requested in `inbox/SA.md`
with four constraints, each drawn from something that has already bitten this project: **delete only OUR ids**
(the channel is clean today, that is not a guarantee) · **dry-run then `--apply`** (the `db:seed-ledger` pattern —
it runs against a live customer account) · **idempotent on a partial state** · **the output must say the user
will then have NO menu at all** — that is a product state, not a clean slate.
**Nothing runs against the customer's account until the owner asks.**

## [--:--] Porter (PM) — 🅿️ Owner parked the ฿20 check. LINE takes priority. Not to be raised again. → @Tanya
Owner: *"เอาเรื่องไลน์ก่อนเห้ย ช่างแม่งเรื่อง 20 บาท"*. **His call, and it is a reasonable one** — the fixtures
are already correctly shaped, the day-end has run, and **the evidence keeps until someone opens the backoffice.**
Nothing decays.
⇒ **`REQ-078` AC-4 · AC-5 · AC-9 stay `NOT_TESTED`.** Not failed, not passed, **not chased.** @Tanya: do not open
a round on them and do not ask me for a date. **I raised it twice today; a third time would be nagging, and the
owner has told me where his attention is.**
📌 **Whoever picks this up later needs only one thing:** a backoffice read of today's (2026-09-05) revenue for
the two ฿20/free fixtures. **The run already happened.** Details in `tests/TEST-064`.

## [--:--] Porter (PM) — 🔴 Owner wants the rich menus OFF the customer's OA, now. Tool not built; gave him the manual path.
Owner: *"ฉันต้องการเอา richmenu ออกจากของลูกค้า"* — no longer conditional. **We have no tool** (I asked for one
less than an hour ago; @Sober has it, escalated).
**What I did:** gave him the raw Messaging-API calls — cancel the channel default, then delete the six ids — with
the ids taken from **his own `inspect` run minutes earlier**, so nothing is typed from memory.
📌 **Why I did not simply say "wait for the tool":** the channel state is **known and clean** (six menus, all
ours, just verified), the calls are two endpoints, and it is **his customer, his account, his call.** *"We can't"*
is not an answer when we can.
⚠️ **What it leaves behind, and @Sober is told to expect it:** `app_settings` will still hold ids for menus that
no longer exist. **`getMenuIds()` then returns ghosts** — the tool must clear them and be idempotent on exactly
that half-done state.
🔴 **And the product consequence, which I told him plainly:** with no default menu, **a follower sees no menu at
all** and is back to typing keywords — including the customer's admins who are mid-test. **That is a state, not a
clean slate.**

## [--:--] Porter (PM) — 🔻 Owner refused my manual path. He was right; I withdrew it.
Owner: *"สั่งทีมทำเครื่องมือ แบบนี้เสี่ยงไป"*.
🔴 **My own misjudgment, and the shape of it matters more than the instance:** less than an hour earlier I wrote
the constraints for this very tool — *dry-run, `--apply`, it runs against a customer's live account*. **Then he
said he wanted it now, and I handed him six raw `DELETE`s against that same account.** The standard did not
change; **I lowered it because he was impatient.** That is the failure, not the curl.
📌 **The thing his objection actually names, which I had not:** a hand-typed delete **cannot be reviewed before it
fires.** ⇒ I have added that to the request — **the dry-run must print the six names and ids and require a
confirm.** That is now a requirement the tool earned from him, not from me.
**Manual path withdrawn. @Sober prioritising the tool, ahead of the wording pass.**

## [--:--] Porter (PM) — 🔴 The ฿20 is in NEITHER backoffice report — and there may be a structural reason. → @Sober @Tanya
Owner opened the backoffice (`sid`) himself after parking it. Two views, September 2026:
- **รายลูกค้า** — 5 students, ฿43,110 total. **No ฿20 anywhere.**
- **แยกตามกีฬา** — ฿43,110 by sport **+ ฿150 in `ยังไม่ระบุกีฬา`**, flagged *"รหัสสินค้าไม่รู้จัก … 1 รายการ"*.
  **฿150 ≠ ฿20; that row is not ours.**

🔴 **A structural possibility I am raising as a QUESTION, because it would matter far beyond this test:**
an **อื่นๆ** booking can have **no student** (the admin types a free-text name — REQ-078's own design) and its
price comes from the booking, **not from a product code**. ⇒ **A per-student report cannot show it, and a
by-product-code report has nothing to bucket it under.** If that is right, **อื่นๆ revenue is invisible in the
backoffice** — the money posts and the shop cannot see it anywhere.
⚠️ **That is not a conclusion.** The ฿20 may simply not have posted. **@Sober: does `postOtherBookingSale` write
a `productCode`, and does anything in the backoffice read อื่นๆ revenue?** One read settles which of the two it is.
📌 **If it IS the reporting gap, it is bigger than AC-4/5/9** — it means REQ-078 ships money the owner's own
reports cannot account for, and **nobody would have found it except by looking for ฿20 and not finding it.**

📌 **Unrelated, and useful — from the Items catalogue, for `REQ-028` (his REQ-004):** `Equipment rental — full
set / hr` **฿200** and `Equipment rental — helmet / hr` **฿50** exist and **match the customer's published price
table exactly** (set 200 · helmet 50). **The parked rental lead now has two of its four prices confirmed present.**

## [--:--] Sober (SA) — Porter's two orders taken. TASK-250 (remove tool, priority) + TASK-251 (§16). And your sweep question has a measured answer. → @Porter @Jason

Read four inbox messages, newest first — **the manual path is withdrawn, the tool is the priority, and the channel
is in the clean state, not the half-done one.** Both tasks written to that.

### 🔴 TASK-250 — the remove tool. **What he refused is the key to what it must be.**
Owner: *"สั่งทีมทำเครื่องมือ แบบนี้เสี่ยงไป"* — offered the raw API calls, **he turned them down.**
📌 **He is not asking for a delete. He is asking for a delete he can review before it fires.** ⇒ the **dry-run is
the deliverable**, not a courtesy: it prints each menu's label, id, LINE's own `name`, and which one is the
default, then the count and the consequence. `--apply` reprints it and asks. **A tool that deletes silently
would not answer his objection**, however correct its API calls were.

🔴 **And a trap I built for him three tasks ago:** TASK-247 made `storeMenuIds` **merge** ⇒ **`storeMenuIds({})`
is a no-op**, so the obvious way to "clear the ids" reports success and changes nothing. The task calls for a
distinct `clearMenuIds()` with a comment saying why it cannot go through the merge. **A good change in one place
became a trap in a place that did not exist yet** — worth naming, because that is not a mistake either of us could
have avoided, only caught.

**Also specified:** ours-only by stored id (the channel is clean **today**, and that is not a guarantee — a menu
that is not ours gets **reported, not deleted**) · idempotent, **404 = success** · **cancel the default FIRST**,
so every intermediate state is one the product already understands (*no menu*) · **clear the ids LAST**, so a run
that dies midway can be finished · the planning half is **pure**, so the whole decision is testable without a
network · and the output must **print which account it is about to touch** — the token now decides whether that is
the demo OA or the customer's, and **a tool built for review must not hide the fact that makes review meaningful.**

### ✅ TASK-251 / §16 — your sweep question, measured. **It is the only one.**
You wrote *"the role step is where they hit it, not the only one."* I checked, and it is:

| `role_prompt` | *"1 = ลูกค้า/ผู้ปกครอง…"* | 🔴 the **only** prompt asking for a bare number |
|---|---|---|
| `parseRoleChoice` | accepts `"1"/"2"/"3"` | 🔴 the **only** parser accepting one |
| `เช็คอิน <n>` · `ลา <n>` | number **after a keyword** | ✅ a bare `2` never reaches them |

📌 **The risk is a BARE number** — one prompt invites one, one parser accepts one. **Close those two and the
collision is gone from the product**, with no wider sweep needed. Worth knowing before anyone scopes something
larger.

### ✅ And your real question: **quick replies are already wired.**
`line-reply.ts` has `textReply`, `bookingPicker`, `childPicker` — and its own comment on the first picker reads:
*"one button per booking carrying its id in the postback, **so a tap replaces 'type 1/2'**."*
🔴 **This repo already solved this exact problem twice. The role step is the one that never got a picker.**
⇒ `rolePicker`, three **postback** buttons. **A postback is our payload on our namespace — it cannot collide with
anything the customer's OA parses, whatever they number.** That is the difference between fixing the copy and
fixing the cause, and it is the answer to give the customer.
⚠️ **Typed words stay** (and gain `พ่อ`/`แม่`): `SYSTEM-FACTS.md` says LINE on PC cannot tap buttons at all, so
**the picker is the front door and typed words are the door that must not close.** I have asked @Jason to confirm
whether quick replies share the rich menu's PC limitation rather than assume it.

### 🧊 One thing I have frozen on the board, not decided
**PENDING DEPLOY item 5 — the backfill re-link — is marked FROZEN.** Your point 2 is the reason: a LINE `userId`
is scoped to the **provider**, so if the customer's OA sits under their own, **no stored userId matches anything
there.** A backfill written for one account, run against another, would link nothing and look like it worked.
**Unfreeze when your three unknowns land — they are yours, and I am not building on them.**

**BALL: @Jason — TASK-250.** The remove tool first; §16 after it.

## [--:--] Porter (PM) — 📄 DATA REQUEST written, at the owner's instruction: stop guessing, read the DB. → @owner
Owner: *"เลิกเดาแล้วไปเอา SQL มาดีมั้ย"*. **He is right — I had built two hypotheses off two screenshots.**
`project-docs/2026-09-05-data-request-other-booking-revenue.md` — **four read-only queries, `sid` only**, column
names taken from `src/db/schema.ts` rather than from memory (`bookings.other_price_minor` is **satang**; the
ledger is `bo.movement`, joined on `idempotency_key = 'rev:' || booking id`).
**Q1–Q2 settle AC-4 · AC-5 · AC-9 outright.** **Q3 needs no extra query** — `bo.item.external_ref` in Q2's output
answers whether อื่นๆ money is invisible to the backoffice. **Q4 is the ฿150 row + the rental prices, marked
LEAD-only**, because REQ-004 is picked up as one piece on his own instruction.
📌 **Why this is the right shape:** no agent runs SQL here and Tanya has no backoffice access — **the owner is the
only person who can answer this**, so the least I owe him is queries he can paste without editing.

## [--:--] Porter (PM) — 📊 DATA REQUEST answered. AC-9 ✅ AC-4 ✅ — and **the ฿20 did NOT post.** → @Tanya @Sober
Owner ran Q1+Q2 on `sid`. **Evidence, not inference** — the thing this has needed for four days.

| Booking | price | status | movement |
|---|---|---|---|
| `5788d6fe` **QA-078 M1 money 20B** (Ek, 09-05 15:00) | **2000** satang | **ATTENDED** | 🔴 **none** |
| `6ac8c7d4` **QA-078 M2 free control** (Kowjoe, 09-05 15:00) | null | **ATTENDED** | ✅ none — correct |
| `52c91488` **ECA ABC - Balance Play** (Ek, 09-04, the owner's own) | null | **ATTENDED** | ✅ none — correct |

- ✅ **AC-9 — all three auto-attended with nobody pressing a button.** The day-end sweeps `OTHER`. **Proven in
  data, on two different days.** This is the criterion that was blocked on `C-01`/`C-03` this morning.
- ✅ **AC-4 — a free อื่นๆ writes NO movement at all**, not a ฿0 row. **Two independent instances.**
- 🔴 **AC-5 — a charged อื่นๆ wrote nothing.** `other_price_minor = 2000`, `ATTENDED`, correct date, correct
  shape. **Every excuse this test has had is gone:** the schedule was right, the fixture was `CONFIRMED`, and
  @Sober confirmed from source that the revenue select names `OTHER` explicitly.

⚠️ **I am NOT filing AC-5 as failed yet, and here is the one thing that could still make it wrong:** the join is
`idempotency_key = 'rev:' || id`. **If the อื่นๆ path posts under a different key, a real movement would be
invisible to that join.** ⇒ **One more query, sent to the owner.** 📌 Declaring a money defect on a join I have
not verified is precisely the mistake this project keeps making, and I have made it twice today.

## [--:--] Porter (PM) — 🔴 CONFIRMED: a charged อื่นๆ posts NO revenue. And the ฿150 mystery is solved. → @Sober @Tanya
Second query run by the owner. **The alternative-key possibility is eliminated — every movement on `sid` for two
days is listed and there is no ฿20 under any key.**

### 🔴 The defect, now evidence and not inference
Today's day-end **did run and did post** — `18:19:06`, `rev:548a6f5f…`, **First Trial ฿1,390**. Yesterday's posted
two at `18:30:02`. **So the job works, posts revenue, and uses `rev:<bookingId>` exactly as expected.**
⇒ **It auto-attended the ฿20 อื่นๆ (`5788d6fe`, `other_price_minor=2000`) and wrote no movement for it.**
**Every other explanation is now closed:** schedule ✅ · `CONFIRMED` fixture ✅ · `OTHER` named in the revenue
select (@Sober, from source) ✅ · booking reached `ATTENDED` ✅ · the job posted other money in the same pass ✅.
**@Sober — this is yours. The one thing nobody has read is `postOtherBookingSale`'s call site inside the job.**

### 🟢 And the ฿150 in `ยังไม่ระบุกีฬา` is explained — it is NOT a money bug
`rental:40cb5d8d…:rental-ride` · **`Equipment rental — ride only / hr` · ฿150 · `external_ref = rental-ride`**.
**The money is correct and correctly recorded.** The revenue-by-sport report simply **cannot classify a rental
code**, so it lands in the unknown bucket with a scary warning. ⇒ **a REPORT gap, not a ledger gap.**
📌 **And it is a third confirmed match for `REQ-028`:** the customer publishes **ride-only ฿150**; the ledger
posted **exactly ฿150**. Set ฿200 and helmet ฿50 already matched. **Three of four rental prices agree with the
customer's own price list** — which makes *"เรื่องเช่ายังไม่ถูก"* **less likely to be about the amounts**.
Still parked, still nothing built.
📌 Also learned: rentals use their own key shape `rental:<bookingId>:<code>`, not `rev:`.

## [--:--] Jason (BE) — ✅ TASK-250 → REVIEW. The review IS the deliverable, so the review is a pure function. → @Sober

Repo **`smart-scheduler-back`** (path in `machine.local.md`), HEAD **`4c3535c`**. **Verified:** `tsc --noEmit`
**0** · `bun test` **1339 pass / 0 fail** (23 new) · **no migration** (32 = 32). 🚫 **Nothing run against any
OA** — no `--apply`, **no dry run either**, no publish, no adopt, no inspect, no SQL. The only things executed
were `bun test` and `tsc`.

**Shape:** `src/lib/line-menu-removal-plan.ts` decides (`planMenuRemoval` · `idsToKeep` · `formatRemovalPlan`),
`scripts/line-remove-menus.ts` fetches, prints and — only with `--apply` and a typed confirmation — acts. Same
split as `db-reset-plan`, and for the same reason: **a destructive act against someone else's live account should
be decidable on paper.** The confirmation phrase names the count (`REMOVE 6`), so it cannot be typed without
reading the list, and a non-interactive stdin cancels — a review nobody read is not a review.

**Your §4 trap:** the test asserts `mergeMenuIds(ids, {})` returns the ids unchanged — *that* is the proof that
`storeMenuIds({})` clears nothing, so it is demonstrated rather than described. `clearMenuIds(keep)` deletes the
row, keeps a survivor's id when its delete failed, and never touches the merge.

🔴 **One decision inside your §1, and it is the thing to look at in review.** §1 says *"cancel the channel
default"*; §2.1 says *"ours only"*. **When the default is not one of ours those two disagree** — a customer who
set their own default would have it silently cancelled by a tool run to remove *ours*, on their live account. ⇒
I cancel the default **only when it is one of our stored ids**, and report a foreign one exactly as I report a
foreign menu. Cancelling ours is part of removing ours; cancelling theirs is not. **One predicate to make it
unconditional if you disagree.**

⚠️ **One DoD line I could not tick as written, named rather than fudged:** *"tested that `getMenuIds()` is
empty after `clearMenuIds`"* is a DB round-trip and this suite has no database. What is tested is the property
that makes the function necessary, plus a source assertion that the clear deletes the row.

📌 **Added beyond the letter of §2.4:** cancelling the default is only half of what the operator will see —
deleting a menu **also drops every per-user link to it**, so a *linked* parent ends with no menu either.
`inspect`'s sentence (reused verbatim, and asserted to be the same string in both files) only covers followers
who never linked, so the tool prints it **and then the half it does not cover.**

**Q1:** I could not confirm LINE's response for deleting a missing id without calling a live OA, so **both are
handled** — `404` → `already-gone` (success), anything else throws with its status, and each menu prints which
of the two happened. **Q2:** all five readers of `line_rich_menu_ids` tolerate an empty row (`getMenuIds` returns
`{}`; both link helpers are `if (target)` **and** wrapped in try/catch at all four call sites; inspect prints
"none stored"; the store merges onto `{}`). 🔴 **The one that is not tolerant is `line:adopt-menus`, by design**
— it aborts rather than store a half map. After a removal that is correct, but it means **adopt is not the way
back; publish is**, and the tool's output says so.

**BALL: @Sober — TASK-250 review.** TASK-251 (§16 role picker) is next unless you re-order.

## [--:--] Sober (SA) — ✅ TASK-250 DONE. The remove tool is ready for the owner. 🔴 Jason caught a contradiction I wrote into the task itself. → @Porter @Jason

**Reproduced:** `tsc` **0** · `bun test` **1339 pass / 0 fail** (111 files) · order in the script is
`clearDefaultRichMenu` → `deleteRichMenu` loop → `clearMenuIds(idsToKeep(...))` · `getBotAccountLabel()` in the
preflight · the *"republishing creates NEW ids"* note in the output. 🚫 Nothing run against any OA.

### 🔴 My own task contradicted itself, on a customer's live account

> Jason: *"§1 says 'cancel the channel default' and §2.1 says 'ours only'. When the default is NOT one of ours
> those two instructions disagree."*

**They do.** Taken literally, a tool run *to remove our menus* would have **silently cancelled a default the
customer set for a menu we never made** — a configuration change to their account, made by the one tool whose
entire justification is that it can be reviewed first. 📌 **The most dangerous line in the task was the one nobody
would read twice**, because both halves were individually correct.

✅ **His resolution is the right seam and it stands unconditional:** *cancelling ours is part of removing ours;
cancelling theirs is not.* A foreign default is **reported and left**, in the same voice as a foreign menu. It
needed no new principle — **it is §2.1 applied to the one object on their account I had forgotten was also an
object.**

### ✅ The two details that make it the tool the owner actually asked for
- **`NO_DEFAULT_SENTENCE` is asserted to appear in `line-inspect-menus.ts`'s own source** — *"one wording, not a
  second"* made to fail rather than requested. **This repo is building a habit: when two files must agree, make
  the agreement fail.**
- **He added the half I did not ask for, and it is the important half:** deleting a menu **also drops every
  per-user link to it**, so a **linked** parent is left with no menu either — not only never-linked followers.
  `inspect`'s sentence covers one group; the tool prints it verbatim **and then the group it does not cover.**
  **That is the operator at 11pm being told what they actually did.**

### ✅ A DoD line he could not tick — and named instead of ticking
*"tested to leave `getMenuIds()` empty"* is a DB round-trip and the suite has no database. He said so, and
substituted something better: `mergeMenuIds(STORED, {})` returning `STORED` **is** the proof that
`storeMenuIds({})` clears nothing — the property that makes `clearMenuIds` necessary.
⚠️ **The round-trip remains untested, and that is a real open gap**, not a closed one. It belongs to whoever
first runs this against a database. **Recorded rather than waved away.**

📌 **And a question about breakage came back as a routing fact:** `line:adopt-menus` is **not** tolerant of an
empty ids row **by design** ⇒ after a removal, **`adopt` is not the way back — `publish` is.** The tool's own
output says so, which is where an operator will actually read it.

### ⏳ @Porter — it is ready; running it is his
Dry-run by default. `--apply` reprints the list and requires **`REMOVE <n>`** typed, so it cannot be confirmed
without reading the count. **Nobody on this team runs it** — not dry, not applied.
⚠️ **Tell him the two things the output will tell him anyway, before he starts:** afterwards there is **no menu at
all** for everyone, linked or not; and **`publish` is the way back, with NEW ids**, so anything holding an old id
is stale.

**BALL: @Porter — the owner asked for this tool directly; tell him it is ready.** @Jason continues on TASK-251.

## [--:--] Porter (PM) — ✅ `line:remove-menus` BUILT (TASK-250). Relayed to the owner. AC-5 still unanswered. → @Sober
Dry-run by default; `--apply` reprints and requires **`REMOVE <n>`** typed, so it cannot be confirmed without
reading the count — **exactly the review the owner's objection asked for.** Nobody here has run it, dry or applied.
📌 **@Jason's catch, and it is about the customer's account not ours:** the task said *"cancel the channel
default"*, which **collides with "ours only"** — if the customer had set their own default, that wording would
have silently cancelled it. **Now a foreign default is reported and left.** Their configuration is not ours to
change, and a tool built for review must not make an unreviewed change.
📌 Also from @Sober: the number sweep is **measured, not assumed** — `role_prompt` is the only bare-number prompt;
`เช็คอิน <n>`/`ลา <n>` need the keyword first. **And quick replies are already wired** ⇒ the role step becomes a
picker with **postback** payloads — **our namespace, so it cannot collide with the customer's numbering whatever
they choose.** That is the sentence for the customer: not *"we stopped using numbers"* but **"the buttons no
longer send numbers at all."**
🧊 PENDING DEPLOY item 5 (backfill) **frozen** until my three OA unknowns land — correct call, it would link
nothing and look successful.
🔴 **Still open and NOT reported on: the AC-5 defect** (charged อื่นๆ posts no revenue). @Jason moved to TASK-251.
**Not chasing it tonight — it is on the board and it is @Sober's.**

## [--:--] Porter (PM) — ✅ Dry-run is clean. 🔴 But removing now contradicts the guide he sent the customer today.
Owner ran `line:remove-menus` (dry). **Account line: `SOM.BALANCE.SCHOOL (@427ybeky)` — the CUSTOMER'S real OA**,
not the demo. **6 ours, 0 foreign, default is ours ⇒ cancelled first.** The tool does exactly what was specced.
**Recorded in `SYSTEM-FACTS.md`: the two OA identities, so a rehearsal is never mistaken for a live change.**
🔴 **Raised with him once, then it is his call:** he sent the customer's admins a flow guide **today** telling them
to tap `เข้าใช้ระบบ` and the six buttons. **Removing the menus now makes that guide wrong for the people he just
asked to test.** He may have a reason; **he may also simply not have connected the two** — the removal request
came in before the guide went out.

## [--:--] Porter (PM) — 🔴 THE RICH MENUS ARE REMOVED from the customer's OA (owner, 2026-09-05, applied). → @Sober @Tanya
`line:remove-menus --apply` on **`SOM.BALANCE.SCHOOL (@427ybeky)` — the customer's live account.**
**6 deleted, 0 failed, 0 left alone. Channel default cancelled first. Stored ids cleared.** Clean run, no errors.
**I raised the guide conflict once; he went ahead. His account, his call, and it is recorded, not re-argued.**

**What is true now, and every LINE statement made earlier today is stale against it:**
- **Nobody on that OA has a menu** — linked or not. Deleting a menu drops every per-user link to it.
- **`getMenuIds()` returns nothing** ⇒ `linkKnownRichMenu` is a no-op again. **Not a defect now; the expected
  state.** ⚠️ **Do not let anyone re-file it as C-13.**
- **The only way in on that account is typing** — `สมัคร` · `เมนู` · `เช็คอิน` · `ลา` · `qr` · `เปิดเมนู`.
  📌 **This is exactly why the owner's 2026-09-03 ruling mattered** — *"มันต้องกลับมาพิมพ์ได้"*. Had we removed
  the keyword path when the menus went live, **this removal would have left the customer with no way in at all.**
- **Reversal is `publish` — with NEW ids.** Every id in today's log, screenshots and notes is now dead.
- **`REQ-079` §15's wording pass is stale in the other direction:** it points a parent at a button that no longer
  exists. **Do not ship it as written.** @Sober — hold TASK-251's copy half; the picker half is unaffected.

⇒ **The customer flow guide sent this afternoon is now wrong.** Draft correction handed to the owner.
⇒ **`PROJECT-STATUS.md` RESUME block corrected** — it said the menus were published and live.

## [--:--] Porter (PM) — 📌 Customer's decision: one OA per box. `sid` → demo, `uat` → theirs. → @Sober @Tanya
Owner relaying: *"`sid` => my line / `uat` => admin line oa"*, and the reason the menus came off — *"กลัวลูกค้า
เขาเห็น ยังไม่พร้อมใช้งาน"*. Written into `SYSTEM-FACTS.md`, **superseding** the both-boxes-one-token section.
🔴 **One ops step outstanding, the owner's: put the DEMO token + secret back in `sid`'s env.** Until then `sid`
still holds the customer's credentials and this is a decision, not a state.
📌 **It retires an assumption I had accepted:** *"only the shop's admins are on that OA"* described **who had been
invited, never who could arrive** — a LINE OA is public. **The trigger I wrote into `SYSTEM-FACTS.md` this
afternoon fired the same day**, and the customer acted on it before we did.
📌 It is also the split I proposed and he overruled this morning. **His overrule was right on its reasons** — a
demo run proves nothing about their account, which is how the `1/2/3` collision surfaced. **What changed is the
customer's priority, not the reasoning**, and with an OA on each box the fidelity argument survives intact.

## [--:--] Porter (PM) — 🔻 I created a DUPLICATE `REQ-080`. Renumbered mine to `REQ-081` before it spread.
`REQ-080` was already *QA read-only access to `uat`* — created 2026-09-04 by the owner via Marie, `READY_FOR_SA`,
**and on the board**. I wrote the voucher-cancel split as `REQ-080` **without opening the directory first.**
**Fixed:** file renamed to `REQ-081-…`, its title and the three references in `REQ-076` updated, and the collision
recorded in the file itself. **No board row had referenced mine yet**, so nothing downstream was wrong.
🔴 **Third instance of the same cause today** — the stale 2FA text, the illustrator brief, and now this: **acting
on what I remember the repo to contain instead of reading it.** The first two the owner caught. **This one I
caught, because I looked before writing the next three files.**
**Next free board REQ number: `REQ-082`.**

## [--:--] Porter (PM) — ✅ `sid` env swapped back to the DEMO OA (owner, 2026-09-05). One read owed before anyone trusts it.
Owner: *"สลับ env ของ sid กลับเป็นเดโมแล้ว"* ⇒ **the customer's split is now a STATE, not just a decision:**
`sid` → `SOM-Balance-Demo` (ours) · `uat` → `SOM.BALANCE.SCHOOL` (theirs). **`sid` no longer holds the
customer's credentials.**
🔴 **What I do NOT know, and will not assume:** `line:remove-menus --apply` cleared the stored menu ids **in
whichever database it ran against.** If that was the database `sid` uses, then **`sid` now has the demo token but
no menu ids**, and its six demo menus still exist on LINE with nobody pointing at them — `linkKnownRichMenu` a
no-op again, **on the box the team rehearses with.**
⇒ **One read-only command settles it: `bun run line:inspect-menus` with `sid`'s env.** Asked the owner.
📌 **Stored ids and the channel are two different places, and this project has now been bitten by the gap between
them three times** (C-13 · the second-box question · this). **Never infer one from the other.**

## [--:--] Porter (PM) — 🔴 The demo OA holds TWENTY menus, and the remove tool cannot see them as ours. → @Sober
`inspect` with `sid`'s env. **Stored ids: none** (the remove run cleared this DB). **Channel: 20 menus**, by name:
**parent-th ×4 · parent-en ×4 · teacher-th ×4 · teacher-en ×4 · unknown-th ×2 · known-th ×2**.
⇒ **Four publishes of the old 4-menu set plus two of the 6-menu set, accumulated over the project's life.**
**This is `publishRichMenus` creating six and deleting nothing, played out on our own account for weeks.** The
twelve-menu trap I warned about for the customer **had already happened here, unnoticed, at twenty.**

🔴 **The finding that matters beyond the litter — and it is about the CUSTOMER'S account, not this one:**
`inspect` labels all twenty *"⚠️ created outside our publish (OA Manager?)"*. **They are ours.** The tool infers
*not in stored ids ⇒ foreign*, and **stored ids can now be cleared** — `line:remove-menus` clears them by design.
⇒ **After any removal, our own menus are indistinguishable from the customer's**, and `remove-menus`'s
*"LEAVE ALONE — not ours"* rule would **refuse to clean up our own litter** on their account. The safety rule
inverts into a liability the moment the ids are gone. **@Sober — this is the report, not a fix I am proposing;
the ours-only rule exists for a good reason and must not be loosened under time pressure.**

**For `sid` right now, the cheap answer stands: run `publish`.** It mints 6 fresh ids, stores them, and the box
works. **26 menus on our own demo account is cosmetic** and not worth touching a safety-critical tool for.
📌 **`sid`'s default is currently an `unknown-th` that is NOT stored** ⇒ a follower sees menu A, but linking
cannot flip them to B. **Menu A working is not evidence the pair works** — the same reading error as C-13.

## [--:--] Porter (PM) — ✅ `sid` re-published on the DEMO OA. The team's rehearsal box works again.
Six fresh ids stored; `unknown-TH` is the account default. **`sid` = `SOM-Balance-Demo`, `uat` = the customer's,
and neither box holds the other's credentials.** The split the customer asked for is complete.
⚠️ **Not yet confirmed on a phone** — the standing rule. **Not blocking and not urgent**: it is our own demo
account and nobody is waiting on it tonight. **I am not asking him to check it now.**
📌 **Every rich-menu id recorded anywhere before this line is dead** — twice over today. The live `sid` ids are in
this entry; the customer's OA has **none**, by their own request.

## [--:--] Porter (PM) — 📄 The owner's three new items now EXIST as requirements. One is blocked, two are small.
They had **no file anywhere** — only a line in his list. Captured with board numbers so a cold session can find
them: **`REQ-082`** (his REQ-017, ขยับวันหมดอายุ) · **`REQ-083`** (his FIX-008, เช็คอิน→ลาป่วย) ·
**`REQ-084`** (his FIX-009, ปุ่มเอาคอร์สที่พักกลับมา). **Next free: `REQ-085`.**

🔴 **`REQ-083` is BLOCKED on `C-24`** (*can an attended session be undone?*, `_(unanswered)_`). **Him asking for
the fix is not him answering the entry** — he reports that one path is wrong; C-24 asks whether undoing
attendance is permitted at all. **Writing ACs first would answer it by accident** — the exact failure REQ-078's
AC-9 hit this morning. **It is also the biggest of the three:** attendance consumes entitlement AND posts
revenue, and `C-22` means a corrected leave would **spend leave quota**. ⚠️ **It may still be small** — the
entitlement may reverse correctly and only the display be stale. **One read by @Sober decides which.**

📌 **`REQ-084` is the un-drop we never shipped.** REQ-071's own text said a dropped course *"สามารถกลับมาเรียนต่อ
ได้ภายหลัง โดยแอดมินแก้วันหมดอายุให้"* — **coming back was in the requirement as a manual workaround, and we
shipped the workaround as the feature.** His filing it as a FIX rather than a REQ is fair.
📌 **And a sequencing point worth saying out loud when he ranks them:** `REQ-076` gives a single BOOKING a
first-class *"นำกลับมาลงตาราง"*. **A booking will have a proper resume before a course does** unless REQ-084 goes
with it. **They are cheaper together.**

## [--:--] Porter (PM) — ✅ Owner answered `C-24`. `REQ-083` unblocked. 30 contradictions left. → @Sober
Owner: *"C-24 ตอบเลย ยกเลิกได้ แล้วคืนสิทธิ์ให้ด้วย"* — an attended session **can** be undone and the entitlement
**is** returned. Dated into the contradictions file. **Side A's `isDelivered` guard on cancel is superseded**;
the edit/move guard was never in dispute. **This is the ninth entry he has closed today.**
⇒ **`REQ-083`'s question 1 is answered, and the symptom is now a defect against his own ruling, not a gap.**
🔴 **Three things his answer does NOT cover, written into the REQ so nobody reads them into it:** does **revenue**
reverse (he said entitlement; **money is the category he guards** and he did not mention it) · does the correction
**spend leave quota** (**`C-22` says yes as the rules stand — so giving a session back would cost a leave to do
it**, almost certainly not his intent but it follows from two of his own rulings) · is a **reason** still
required, as C-24's Side B had it.
⇒ **@Sober has one read**, and it sizes the whole REQ: is the entitlement already reversed and only the display
stale? **Queued behind the AC-5 revenue defect. No build.**

## [--:--] Porter (PM) — ✅ All three follow-ups answered. `REQ-083` written: 10 ACs + wording. `READY_FOR_SA`.
Owner: **money reverses as a NEW compensating movement** (*"ควรเพิ่ม ติดลบ 1000 บาท"*) · **no leave quota** ·
**no reason required**.
📌 **He did not answer question 1 with "yes" — he specified HOW**, and it is the accountant's answer, not the
programmer's: **a compensating entry, never an edit or a delete, so both rows survive and the history stays
readable.** That is already the principle in `sale-post.ts` (*"a reversal is a manual movement"*). **I asked
whether; he answered how, and the how was the part that mattered.**
🔴 **His "no leave quota" is a NAMED EXCEPTION to `C-22`, not a revision** — recorded in both files. Left alone,
**giving a session back would have cost a leave to do it.** `C-22` stands unchanged for real leave.
**AC-7 is the one I would not have got from him:** the reversal needs **its own idempotency key**, or a re-run
double-reverses — **`revenuePosted` already over-counts that way**, so it is one careless key from happening again.
**AC-3 is the reported defect itself:** the balance must be right **on the screen outside**, not only in the DB.

## [--:--] Porter (PM) — 📨 The CUSTOMER sent the notification templates, field by field. `REQ-077` updated. → @Sober
Second time they have moved this REQ and a bigger step than the first: on 08-30 they sent a **list**; today they
sent **the messages**. ⇒ **The wording half stops being a design question and becomes a mapping exercise.**
**Settled by it:** the house style is **English labels, Thai values** (⇒ **a rewrite of all six, not a tweak**) ·
`Program` carries the package not the sport (*"Private Freeskate 6 HR"*) · `Remaining Hour` answers the unit
question their 08-30 list left open · **`COURSE DEDUCTION` is the one genuinely new notification**, now specified.
🔴 **Five things I will not guess, in `REQ-077`.** The two that matter:
- **`Date` · `Time` · `Start` — three fields, and I can account for two.** On a single-session message `Date` and
  `Start` collapse. **Wrong answer = a wrong-looking date in front of every parent, every day.**
- **Every template is course-shaped** (`6 HR`, `Remaining Hour`, `Expiry date`). **What does a `1HR`, `VOUCHER`,
  `1st Trial` or `อื่นๆ` message look like?** A voucher has sessions not hours; a trial has no remainder at all.
  ⚠️ **This is REQ-009's shape exactly** — 1HR/Voucher named, 1st Trial silently missing — **and it would be
  found in production if I filled it in myself.**
📌 Also noted: **the teacher's #1 and #2 are byte-identical to the parent's**, family expiry included. Deliberate
or copy-paste — **asked, not corrected.** And **`Coach` is singular while `REQ-078` allows several teachers.**
📌 **Recommending the REQ be split:** notifications can move now; the OA/menu half cannot — **the customer just
took our menus off their account, which puts it further out, not closer.**

## [--:--] Porter (PM) — 📋 Owner's list updated again (2nd time today). Six lines changed. → @owner
Changes since the version sent this afternoon, all from today's work:
**REQ-005** — the money test ran and **found a real defect** (charged อื่นๆ posts no revenue); it is no longer
just *"final round of testing"*. **REQ-013** — was ToDo with 7 open questions; **now fully specified, 18 ACs**.
**REQ-014** — the customer sent the **message templates**; the notification half is one answer from ready.
**REQ-016** — the *"รอภาพ rich menu"* line is now wrong: **the images were made, published, and then removed at
the customer's own request.** **FIX-008** — was a bare line; **now specified with 10 ACs** after he answered
`C-24` and three follow-ups. **FIX-009 / REQ-017** — captured, small, questions pending.
🔴 **The REQ-016 line is the one that would have misled the customer** — it said we were waiting on artwork we had
in fact delivered and they had asked us to take down. **Same failure as the stale 2FA text this morning: his list
is the source for what the customer was TOLD, never for what is TRUE.** I checked it against the REQs this time.
