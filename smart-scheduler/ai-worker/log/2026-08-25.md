# 2026-08-25 — log

## [00:0x] Porter (PM) — 🟢 REQ-036 acceptance on `sid`: **every check passed, measured before and after.** → @Tanya to sign
Owner ran the irreversible half on **มิลล่า's Skateboard course** (`5d36e8c9…`) — the awkward case Tanya chose: it
had taken a leave, so it carried an `EXTENDED` make-up.

| field | before | after | required | |
|---|---|---|---|---|
| `pending` | 5 | **0** | 0 | ✅ |
| `extended` | 1 | **0** | 0 | ✅ |
| `used_sessions` | 0 | **0** | unchanged | ✅ |
| `size` | 6 | **6** | unchanged | ✅ |
| **`bo.movement` rows** | 26 | **26** | unchanged | ✅ **no money moved** |
| `end_reason` | null | **`ADMIN_ERROR`** | recorded | ✅ |
| `ended_at` | null | 2026-08-25 00:02:59 | set | ✅ |

### 🎯 The number-one risk is cleared, and it was cleared by measurement
**No make-up was re-owed** — `extended` went **1 → 0** and **`size` did not move**. The ordinary cancel path
re-owes by design (SPEC-028 §11.3); this one does not. **That was the single most likely way this feature could
have shipped wrong, and it is now a number, not an opinion.**

### 📌 The preview proved itself before the cancel, which is the whole point of the feature
Tanya's dialog read **"6 remaining sessions"**; the DB before showed **`pending 5` + `extended 1` = 6**. **The
server's promise and the database agreed, and then the cancel removed exactly those 6.** The safety mechanism is
not decorative — it was checked against reality on both sides of the action.

### 📌 Sober shipped the SOFT cancel — `last_date` stayed `2026-10-01`
The rows were **marked, not deleted** (a delete would have pulled `last_date` back to the 20/Aug leave row). That
is the R1 recommendation, and it matters most for exactly the reason we cancelled with: **`ADMIN_ERROR`**. A course
ended by a typo can now be inspected — and recovered — instead of being gone.

**Outstanding: idempotency (item 5)** — re-cancel the same course, expect *already ended*, no Confirm button,
nothing changed. Then **@Tanya signs 2–8 on these numbers** (she signed the preview + reason-gate read-only at
23:45) and **REQ-036 is `TEST_PASSED` on `sid`.** `uat` remains a separate owner decision.

**Ball: owner** — the one re-cancel click. **@Tanya** — sign on the table above. **@Fern** — TASK-179 → 142 → 183.

## [Jason (BE)] — TASK-184 BUILT → REVIEW. **Took the type, not the test — the test was the weaker guard.** → @Sober @Fern
Fern's Q1 fixed, and the class closed rather than the instance.

**The one line** — `attendeeNote: b.attendeeNote ?? null` — was never the interesting part. The task offered a
choice: annotate the mapper, or add a round-trip test. **A test only ever covers the fields someone remembered
to assert, and the next dropped field is by definition one nobody thought of** — which is exactly how this one
got through. The annotation makes that a compile error, for free, and it cannot rot.

So `toSessionRow` (and `teacherRef`/`subjectRef`) are now typed to a new **`PlanSessionRow`** in the contract.
Named that rather than `PlanSession` because `lib/course-plan.ts` already owns that name for the pure planner's
input, and two different `PlanSession`s in one codebase is its own trap. Its doc comment records what it is for:
this was the **fourth** compiler-silent allow-list in this feature set, and **an editor that can save what it
cannot show is worse than no editor** — staff overwrite what they cannot see.

**I did still add a test, for the half a type can't cover**: that the mapper actually *reads* the column, rather
than declaring the field and always filling it with `null` — which would type-check perfectly and be exactly as
broken for the person using the editor.

**`studentRef` left alone deliberately** — it feeds several DTOs with different shapes, so pinning it to this
one would be wrong. A separate cleanup, not a drive-by.

**📌 Flagged, not fixed:** `toSessionRow` returns `startTime` **as stored** (`HH:mm:ss`) while `toBookingDTO`
formats it to `HH:mm`. That predates this task and Fern is already living with it — changing the wire format
under him mid-task would be the wrong kind of tidy. It is noted in the type so the next reader sees it.

**Verified:** tsc **0** · `bun test` **784 pass / 0 fail** (+3). No migration; no behaviour change beyond
surfacing a column that already existed. ⚠️ Nothing run against a database.

**Ball: @Fern** — the per-session half of TASK-179 is unblocked; every plan session now carries `attendeeNote`
(you'll want it on the FE's own `PlanSession` too). **@Sober** — review; TASK-181 rev 1 is also still with you.
**@Porter** — nothing new; migration queue unchanged (`0020`–`0023` + backoffice `0006`).

## [00:1x] Porter (PM) — 🔴 REQ-036 **Part B** — the owner broke the feature in three minutes by using it. → @Sober
He ended a course, then **pressed the next button on the same screen** — something none of us did, including me,
including the acceptance pass I wrote. **All three findings are one failure: the course is ended in the data and
nowhere else.**

### B1 — the defect: an ended course still takes writes, and one of them takes money
`เพิ่มคาบ (คิดเงิน)` **worked** on the cancelled course — a **25/Aug Onewheel E-Skate** session now sits on it.
`addExtraSession` (`scheduler.service.ts:920`) has **no `ended_at` check** and creates a **`SINGLE_SESSION`**,
which **posts revenue at day-end once attended**. ⇒ **a cancelled course can still bill a family.**
⚠️ Sober's *"re-booking is already server-guarded"* holds for the **plan** path and **not** for this one.
**Guard every write path server-side** (`409 COURSE_ENDED`), enumerated from the routes — **not from my list**.

### B2 — the card still says `ปกติ` in green
The list card shows a **normal** badge on a cancelled course. **That is the most misleading pixel in the product**
and it is the screen staff decide from. BE already emits `endedAt`/`endReason`; **FE type + mapper only.** Same for
the plan header now reading `สิ้นสุด ยังไม่มีคาบ` — it reads *never started* on a course deliberately *ended*.

### B3 — filter active / inactive, default active (owner's instruction)
❓ **One question routed to the owner, not assumed:** is `inactive` **cancelled only**, or also **expired** and
**fully used**? My lean — cancelled + expired are inactive; a **fully-used** course stays active until it expires,
because a finished course is still a live relationship. **His call.**

### 🔻 What I got wrong, stated plainly because the shape repeats
I wrote five "รัดกุม" checks and **all five asked "does the cancel break anything?"** — **none asked "what can
still be done to the course afterwards?"** A cancel that still accepts paid sessions has not cancelled anything.
**The rigour pass tested the action and never tested the resulting state.** That is the gap, and it is mine, not
Sober's or Jason's — they built exactly what I specified.

**Ball: @Sober** — Part B tonight, **B1 first** (it is the only one that touches money). B2 is FE-only, B3 needs the
owner's one answer but the FE scaffolding does not wait for it. **Ball: owner** — the `inactive` definition.

## [--:--] Sober (SA) — TASK-184 ✅ DONE. REQ-036 Part B cut: B1 (money) → Jason, B2/B3 → Fern. → @Jason @Fern @Porter
**TASK-184 (toSessionRow attendeeNote) — BE DONE.** Reproduced `tsc` 0 (pinned `typescript@5.6.3`; the env's `latest`
tsgo binary panics — noted for the team) · `bun test` 784/0 (+3). Jason took the **type** over the test — `toSessionRow`
typed to a new `PlanSessionRow` so the next dropped field is a compile error (stronger than a fixture test, can't rot),
plus a test that the mapper actually *reads* the column (the half a type can't cover). `PlanSessionRow` not `PlanSession`
(the planner already owns that name). Flagged, not fixed: `startTime` is `HH:mm:ss` here vs `HH:mm` in `toBookingDTO` —
pre-existing, correctly left alone mid-task. **TASK-179's per-session half is unblocked → @Fern.**

### REQ-036 Part B — I own the miss, and here is the fix, B1 first (money)
Porter's self-critique ("all five checks asked *does cancel break anything*, none asked *what can be done after*") is
right, and the same gap is mine: my R4 tested the **action**, and I told him "re-booking is already server-guarded" when
that held for the **plan-insert path only**. Grounded the truth: `addExtraSession` (`scheduler.service.ts:917`) loads the
course and calls `createBooking(… SINGLE_SESSION, courseId)` **with no `isCourseEnded` check** — and a SINGLE_SESSION
bills at day-end. `isCourseEnded` is consulted in just three spots today (DTO, preview, endCourse). **Every direct write
route is unguarded.** That is B1, and it is real.

- **TASK-185 (BE, 🔴🔴 money, priority): guard every course write path.** Principle, not a fixed list: *an ended course
  accepts no write that adds a session, revives a forfeited one, or bills.* One shared `assertCourseWritable` →
  **`409 COURSE_ENDED`** (distinct from `ALREADY_ENDED`). **Enumerate from the router, not from Porter's list nor mine**
  — extra-session first, then createBooking-with-courseId, plan (reject loudly, don't silently no-op 200), moveBooking,
  reviving status transitions, updateCourse; note/badges likely allowed but **stated per path**. DoD = **one test per
  guarded route + the allow-listed paths tested to still work** — testing the *resulting state*, the exact gap Porter
  named. This is defense-in-depth: the server rejects even when the FE forgets to hide the button.
- **TASK-183 upgraded queued→🔴 B2 (FE, parallel): an ended course must LOOK ended everywhere.** The green `ปกติ` badge
  on a cancelled course is the headline; plus the plan header reading "never started" on an ended course, hide the write
  actions, surface `COURSE_ENDED`. BE-free (the DTO already carries `endedAt`/`endReason`; the FE mapper drops them —
  same pattern as TASK-179 Q2/184).
- **TASK-186 (FE, B3): active/inactive filter, default active.** Scaffolding proceeds; the **predicate** (is `inactive`
  = cancelled only, or + expired, or + fully-used?) is the **owner's** call — Porter routed it, don't assume it. One
  swappable `isCourseInactive` predicate.

**Ball:**
- **@Jason — TASK-185 (B1) first, it is the only one touching money.** Enumerate from the router and report the verdict
  per route.
- **@Fern — TASK-183 (B2) in parallel** (BE-free), then TASK-186 (B3) scaffolding; and TASK-179's per-session half is
  now unblocked by TASK-184. Sequence at your discretion, but the misleading `ปกติ` badge is the one the owner is
  staring at.
- **@Porter** — B3's `inactive` definition is with the owner (your routing); REQ-036 Part A `sid` acceptance table looks
  fully passed pending the owner's one re-cancel click + Tanya's sign — Part B does not block that sign, it hardens what
  she signed. Nothing routes to the owner from me.

## [Jason (BE)] — TASK-185 BUILT → REVIEW. 🔴 **The billing hole is closed, and the enumeration is machine-checked.** → @Sober @Porter
Walked **every write route in `routes/api.ts`** as you both insisted — not your list, not mine — and classified
all **44**. One shared `assertCourseWritable` → `409 COURSE_ENDED`.

**The enumeration is not a list in a task file; it is a test.** It reads the router, extracts every write route,
and **fails if any is missing from the verdict table** — so a route added next month that nobody thought about
breaks the build by omission. A written-down enumeration is only true on the day it is written; this one
survives whoever wrote it.

**Guarded:** extra-session (the money path) · `POST /bookings` when `courseId` is set · plan · plan/preview ·
move · status · bulk-confirm · `PATCH /courses/:id`.

### The three judgement calls
1. **The plan path rejects LOUDLY.** The compute already returns no moves for an ended course — so without an
   explicit throw the route answers **200 "nothing changed"**, telling a staff member their leave was accepted
   when it wasn't. **A silent success on a dead course is its own lie.**
2. **Status is guarded by ACTION, not by route.** `confirm`/`attend` revive a forfeited session and let it
   consume quota or bill; `cancel`/`sick-leave` stay allowed, because they only move a session further from
   delivery and blocking them would trap a row nobody can clear. Guarding the action is also what makes
   **bulk-confirm covered by construction** rather than by my remembering it exists.
3. 🔴 **`POST /rentals` is NOT guarded — stated, not assumed (Q1).** A rental's `refId` is opaque (the service
   never loads the booking) and a **standalone rental has no course at all**, so the guard would add a DB read
   to every rental while protecting nothing: anyone wanting to bill equipment could just omit `refId`. Rentals
   bill for equipment handed over, not for the course. If you disagree, the fix is a booking lookup in
   `recordRental` — say so and it's yours.

**`COURSE_ENDED` ≠ `ALREADY_ENDED`** deliberately: "you wrote to a dead course" and "you cancelled a course that
was already cancelled" are different sentences to a user, and collapsing them makes one of them wrong.

⚠️ **The honest limit, said plainly:** these are source-level and **ordering** assertions — the guard is proven
to be reached *before* `createBooking`, *before* the transaction opens. Without a database I cannot execute
"ended course + POST extra-session → 409, zero rows written". **@Porter / @Tanya — that one is yours on `sid`,
and it takes the same three minutes the owner needed to find the hole.**

**Verified:** tsc **0** · `bun test` **795 pass / 0 fail** (+11). No migration. Nothing run against a database.

**Ball: @Sober** — review + Q1 (rentals) and Q2 (`PATCH /courses/:id` refused outright rather than per-field);
TASK-181 rev 1 and TASK-184 are also with you. **@Porter** — the `sid` re-break check above is the acceptance
for Part B. **@Fern** — B2/TASK-183 is unaffected by this; `COURSE_ENDED` is the code to surface.

## [--:--] Sober (SA) — TASK-185 (B1, the money guard) reviewed: ✅ PASS. → @Porter (sid re-break check) · @Fern (B2/B3)
Reproduced `tsc` 0 (pinned 5.6.3) · `bun test` **795/0** (+11). The billing hole is closed and — better than I asked —
the enumeration is a **machine-checked completeness test**: it reads `routes/api.ts`, classifies all 44 write routes,
and **fails on any unclassified new route** (`:81`) or stale verdict (`:86`). It even handles multi-line route
declarations (the exact omission class this was about). The guard (`assertCourseWritable`, `:909`) sits at the
`createBooking` chokepoint that extra-session funnels through, and the test proves the guard runs **before**
createBooking and **before** the transaction (`:107`/`:113`) — the right way to prove "no row, no sale" without a DB.
REVIVING={confirm,attend} guarded by action (so bulk-confirm is covered by construction; cancel/sick-leave stay
allowed). `COURSE_ENDED` ≠ `ALREADY_ENDED` kept distinct.

**Both Jason's judgment calls accepted:** Q1 — **rentals stay unguarded** (a rental bills equipment, not a course; a
standalone rental has no course; guarding protects nothing since `refId` is optional) — escape hatch noted if the owner
ever reports otherwise. Q2 — **`PATCH /courses/:id` refused outright** ("ended is ended"; no field needs editing on a
dead course; per-field is YAGNI).

**The one thing not provable from here, and it is exactly Part A's lesson:** source+ordering assertions can't execute
"ended course + POST extra-session → 409, **zero rows**" without a DB. **That live re-break check must run on `sid`
before Part B is signed** (Tanya/owner, ~3 min — the same path the owner used to find the hole). Routed to @Porter.

**Ball:**
- **@Porter** — B1 code DONE; the `sid` re-break check (extra-session on the cancelled course → 409, nothing written)
  is Part B's acceptance → @Tanya. Add it to her REQ-036 sign alongside the Part-A table + the idempotency re-cancel.
- **@Fern** — B2 (TASK-183, the `ปกติ`→`ยกเลิกแล้ว` badge + surface `COURSE_ENDED`) and B3 (TASK-186) continue;
  TASK-179 per-session half still unblocked by TASK-184. `COURSE_ENDED` is the new code to surface.
- Nothing routes to the owner from me. REQ-036 Part A `sid` acceptance stands; Part B hardens it.

## [--:--] Porter (PM) — ✅ B1 verified LIVE on `sid`. Owner ruled B3: **a status type, not a toggle.** → @Sober @Fern
**B1 PASS, by pressing the button that broke it.** `เพิ่มคาบ (คิดเงิน)` on มิลล่า's cancelled course now returns
*"คอร์สนี้ถูกยกเลิกแล้ว — เพิ่ม/แก้ไข/คิดเงินคาบในคอร์สนี้ไม่ได้"*. **The billing hole is closed in the running
system, not only in the tests** — which is the distinction Sober was right to insist on.
Still open from Part A: the stray **25/Aug** session. The owner can cancel it via `⋯` — cancel/sick-leave stay
permitted on an ended course by design.

### The owner overruled my binary and he is right
I offered *active / inactive* and leaned on a guess about which cases count as inactive. His answer:
> *"แยกประเภทไป ทำประเภทขึ้นมาคลุมเลย"*

**Four statuses, precedence in this order:** `CANCELLED` (ended_at set) → `COMPLETED` (used ≥ size) →
`EXPIRED` (past expiry, sessions unused) → `ACTIVE` (default filter).
**`COMPLETED` beats `EXPIRED` deliberately:** a family who used everything has no problem, while **expired with
sessions left is a family that paid for classes they never received.** A binary would have merged those two and
hidden the only one that costs the customer money. **My toggle would have thrown that away.**

### 📌 And checking first paid off: **B2 and B3 are one piece of work, not two**
There is **no course-status concept in the BE** — `ปกติ` is computed in the FE. **That is exactly why a cancelled
course still wore a green `ปกติ` badge:** the badge's idea of "over" and the data's idea of "over" were never the
same thing. If we now add a filter that computes it separately again, **we build the same bug twice.**
⇒ **One `status` field on the course DTO, server-side. The badge renders it; the filter filters on it.**
AC-B6 pins it: the four filtered counts must **sum to the unfiltered total** — nothing between two categories,
nothing in both.

**Ball: @Sober** — B3 needs a small BE piece now (the computed `status`), so re-cut: BE → @Jason, badge+filter →
@Fern off that one field. **@Tanya** — B1's live re-break is done; you can sign REQ-036 (Part A table + idempotency
+ this). **Ball: owner** — cancel the stray 25/Aug session when convenient.

## [02:04] Tanya (QA) — ✍️ SIGN REQ-036 Part A = TEST_PASSED (sid); item-5 UI verified by me; Part B + re-cancel owner. → @Porter
Signing Part A on your measured before/after table + my own read-only checks. Detail: `tests/TEST-058`.

**Signed — items 2/3/4/7/8 (your numbers, owner-attested):** pending 5→0 · extended 1→0 · used_sessions 0→0 · size 6→6
· `bo.movement` 26→26 · end_reason ADMIN_ERROR · ended_at set. And the point that matters: my preview said **6 remaining**
and the DB before was **pending 5 + extended 1 = 6** — the promise matched reality, then exactly those 6 went CANCELLED.
**No make-up re-owed** (extended→0, size unmoved). **Soft-cancel confirmed visually** — the 6 rows read **CANCELLED**
(marked, not deleted) and the **20/Aug ON LEAVE row is preserved**; header now "Ends no live sessions".

✅ **Item 5 UI (idempotency) — PASS, verified read-only by me.** Re-opening the dialog on the ended course shows
**"This course has already been cancelled."** and **NO "Cancel the course" button** — the UI cannot re-cancel by design.

🔴 **Observation (worth your eyes):** the ended course carries a **PENDING session marked `EXTRA` — 25/Aug 09:00 Haris ·
Onewheel E-Skate** — a live session hanging off a course that reads "no live sessions". This is the **Part B billing-hole
residue** (an extra added to the ended course before TASK-185's guard). Two things: (1) it's an inconsistent state now on
`sid` (ended course + one live PENDING extra) — cleanup is the owner's; (2) it's exactly what **Part B's re-break check
must prove is no longer possible.**

🔻 **Still owner-run (writes I can't do), then I sign:**
- **Part B acceptance:** `POST extra-session` on the ended course → **409 `COURSE_ENDED`, zero rows** (your/Sober's ~3-min
  live re-break). The "Add extra (charged)" button still shows in the ended plan footer, so this is a real check.
- **Item 5 API re-cancel:** the belt-and-suspenders 409 `ALREADY_ENDED` (UI already blocks it).
- **Item 7:** only **ADMIN_ERROR** round-tripped this run; PROGRAM_CHANGED / CUSTOMER_CANCELLED not individually cancelled.

⇒ **REQ-036 Part A = `TEST_PASSED` on `sid`.** Part B pending the re-break; `uat` remains your + owner's call.

**Ball: owner** (Part B extra-session→409 + API re-cancel; clean up the stray 25/Aug EXTRA) **& @Porter** (Part A signed; carry Part B).

## [--:--] Fern (FE) — TASK-183 (B2) done: the misleading badge is gone, and the cause was a dropped field. → @Sober
Took B2 first, as you said — it's the pixel the owner is staring at.

### The cause wasn't badge logic
`dtoToCourseView` is an **allow-list** and never mapped `endedAt`/`endReason`. The green `ปกติ` was a **course object
that had never heard of being cancelled**. The BE had been sending both fields all along.

🟢 **So I let the type carry it instead of a comment** — the same move Jason made in TASK-184: both fields are now
**required** on `CoursePackageView`. Optional is precisely how they became `undefined`; making them required broke the
build in the one place that needed fixing, which is the behaviour I wanted.

### What it does now
- **The badge branch is FIRST** — before locked / special-unlock / normal — so no other state can outrank "this course
  was cancelled". It reads `ยกเลิกแล้ว — {reason}`, reusing the `endCourse.<reason>` keys rather than inventing a
  second vocabulary for the same three reasons.
- **The plan header** no longer says `ยังไม่มีคาบ` on an ended course. That string means *never started* — the exact
  **opposite** of what happened, since those sessions were forfeited on purpose.
- **No write action is offered**: the `เพิ่มคาบ`/insert/owed row becomes one dimmed line saying why, and I withdrew
  the **per-row** edit too (made `onEdit` optional on `SessionTable`) so the row genuinely offers nothing, rather than
  keeping a control that would fail.
- **`409 COURSE_ENDED` already shows the server's own message** — all six catches in `PlanModal` are
  `ApiClientError ? e.message : generic`. I deliberately added **no** special-case: the server's sentence beats mine,
  and a new branch would be a second place to keep in sync.

**Evidence:** `bunx --package typescript@5.6.3 tsc --noEmit` **0** (pinned per your TASK-184 note — the env's `latest`
tsgo does panic here too) · `bun run build` **ok** · `bun test src/lib/scheduler/ src/services/` **40/0** · §3.5
**0/0/0/0** · no raw key. Rendered → @Tanya with the rest of REQ-036.

### Q1 — four is a pattern, not bad luck
This is the **fourth** compiler-silent response-mapper drop in this feature set (`createBooking` body → TASK-170;
`dtoToBooking` → Part 2; `toSessionRow` → TASK-184; now `dtoToCourseView`). TASK-172 guards **request** bodies. The
symmetric guard is either types — making DTO-derived view fields **required by default**, which caught this one the
moment I tried it — or a fixture test per mapper. I lean on types: it can't rot. Still not doing it unasked.

**Ball: @Sober** (review · Q1). Next in my lane: **TASK-179's per-session half** (TASK-184 unblocked it), then
**TASK-186 (B3)** scaffolding — noting its `inactive` predicate is the owner's call via Porter, so I'll build it as the
one swappable predicate you specced and not assume the definition.

## [--:--] Sober (SA) — TASK-183 (B2, the ปกติ→ยกเลิกแล้ว badge) reviewed: ✅ PASS. → @Fern (B3 + 179) · @Porter (Tanya)
Reproduced `tsc` 0 (pinned) · build ok · 40/0. Fern fixed it at the **root**, not the badge: the bug was
`dtoToCourseView` (an allow-list) dropping `endedAt`/`endReason`, so the "green ปกติ on a cancelled course" was a
course object that had never heard it was cancelled. Both mappers now set the fields (`lib/api/mappers.ts:78` +
`lib/scheduler/leave.ts:27`), and they're **required** on `CoursePackageView` so a future mapper can't silently drop
them. ENDED badge branch is first (nothing outranks "cancelled"); plan header no longer reads *never-started* on an
ended course; write actions withdrawn (incl. per-row edit); `COURSE_ENDED` rides the existing `ApiClientError.message`
path. Rendered verdict rides @Tanya.

**Fern's Q1 — the 4th response-mapper omission — ENDORSED.** Her lean (a) is right and I'm making it a standing
convention: **a `*View`/app type built from a DTO makes its fields required**, so an allow-list mapper that forgets one
fails the build (the symmetric guard to TASK-172's request-body guard). Cut **TASK-187** (low-pri sweep) so it's applied
to the remaining view mappers, not just declared.

**REQ-036 Part B status:** B1 ✅ (TASK-185, needs live re-break on sid) · B2 ✅ (TASK-183, needs rendered pass) · B3
(TASK-186) still to build + owner's `inactive` predicate. Part B code is nearly whole.

**Ball:**
- **@Fern** — TASK-186 (B3 active/inactive filter — build the toggle/default-active now, the predicate waits on the
  owner) and TASK-179's per-session half (unblocked by TASK-184); TASK-187 is opportunistic, not urgent.
- **@Porter** — for Tanya's REQ-036 sign, two live checks now ride together: B1's **extra-session-on-cancelled → 409,
  zero rows** and B2's **rendered badge/header** (a cancelled course shows ยกเลิกแล้ว, offers no write button). B3's
  `inactive` definition is still the owner's one answer.
- Nothing routes to the owner from me.

## [--:--] Fern (FE) — TASK-186 (B3) scaffolding + TASK-179 finished. One honest limit on B3. → @Sober
Took them in your order.

### TASK-186 (B3) — built the scaffolding, left the definition alone
🆕 `lib/scheduler/course-status.ts` holds `isCourseInactive` **and** the filter that applies it, side by side so they
can't drift. **What ships is the subset nobody disputes: a cancelled course is inactive.** Porter's lean (+ expired;
fully-used stays active) is written into the doc comment as the likely extension but **is not implemented** — it's a
lean, not the owner's answer. Cancelled-only is true under *every* candidate definition, so it can be **incomplete but
never wrong**, and it already delivers B3's purpose: an `ADMIN_ERROR` cancellation stops cluttering the daily view
while staying findable. Each extension is one line, spelled out in the comment.

Toggle defaults to **active** and resets to page 1 on change. The empty state **names which view is empty** — under
the Inactive filter, a generic "no courses" would read as *"nothing was ever cancelled"*, which is a different claim.

🔴 **Q1 — the limit I won't paper over:** `GET /courses` has **no status param** (`validation.ts:140`), so the filter
runs **client-side over the current page**. The toggle is right, but the **counts are not**: `total` still counts
both kinds, a page can render fewer cards than its limit, and an inactive course on page 3 won't show under
"Inactive" until you page to it. That's the same "screen quietly lies" class we've spent the week removing, so I'm
not calling B3 done on it. The fix is a server-side `status=active|inactive` — but it can only be specced **after**
the owner defines `inactive`, because the server needs the same predicate. **Worth cutting as one BE task together
with the owner's answer, rather than twice.**

### TASK-179 — complete now
TASK-184 put `attendeeNote` on the plan rows, so the manage-course editor can **show** the note it saves — the exact
reason I stopped instead of shipping it. It renders **only for an existing session** (a create-mode draft has no
booking id, so offering it there would be a control that cannot work), has **its own Save disabled until the text
actually differs**, and sends **`null`** to clear rather than `""`. Only that booking id is touched (AC-3), which the
BE enforces structurally.

**Evidence (both):** `bunx --package typescript@5.6.3 tsc --noEmit` **0** · `bun run build` **ok** ·
`bun test src/lib/scheduler/ src/services/` **40/0** · no raw key. Rendered → @Tanya.

**Ball: @Sober** (review both · **Q1: pair the `status` param with the owner's `inactive` answer as one BE task**) ·
**@Porter** (TASK-179's PII hint wording is still owed; the `inactive` definition is with the owner). TASK-187 is
opportunistic — say the word and I'll take it, otherwise my lane is clear.

## [--:--] Sober (SA) — TASK-186 (B3) scaffolding reviewed: ✅ accepted, PARKED on owner's definition. → @Fern (TASK-179)
Reproduced tsc 0 · build ok · 40/0. Clean single-seam predicate (`isCourseInactive = isCourseEnded` today; +expired /
+fully-used written as one-liners in the comment for when the owner answers). Cancelled-only is true under every
candidate definition — incomplete but never wrong, and already delivers B3's purpose (an `ADMIN_ERROR` cancel stops
cluttering the daily view, stays findable). Q1 confirmed real: `GET /courses` has no status param (`validation.ts:140`),
so the toggle filters client-side over the current page — `total` and paging are inaccurate. Fern rightly did NOT call
B3 done on it. Her sequencing is correct: the server-side `status=active|inactive` filter needs the SAME predicate the
owner hasn't defined, so it's **one BE task cut together with the owner's answer**, not two.

**B3 = scaffolding done, parked.** REQ-036 Part B: B1 ✅ (live re-break pending) · B2 ✅ (rendered pending) · B3
scaffolding ✅ (owner's `inactive` definition + a BE server-filter task pending). The owner's one answer (cancelled
only? +expired? +fully-used?) unblocks the last piece — Porter has that routing.

**Next unblocked FE work is TASK-179's per-session note editor** (unblocked by TASK-184's `toSessionRow` fix). That's
Fern's concrete next build while B3's definition sits with the owner.

## [--:--] Sober (SA) — TASK-179 per-session editor reviewed: ✅ DONE (both halves). Fern's lane is clear. → @Porter
Reproduced tsc 0 (pinned) · build ok · 40/0. The per-session note editor is exactly the shape that was blocked: it
shows **on real sessions only** (`target.kind === "move" && !onLocalSave`, :775 — a create-mode draft has no booking id
so it offers nothing rather than a dead control), **seeds the saved note** (`seed?.attendeeNote`, :620 — the display
TASK-184 unblocked), has its **own Save disabled until the text actually differs** (:783), sends **`null` on empty to
clear** not `""` (:790), and touches **only that booking id** via `PATCH /bookings/:id/note` (AC-3, BE-enforced). This
closes REQ-068 on the FE.

**Fern's lane is now clear** (TASK-179/183/186-scaffold all done; TASK-187 is low-pri opportunistic, not assigned).

**What remains on REQ-036/068 is all owner/QA coordination — Porter's to move:**
1. TASK-179 **Q3 PII-hint wording** — Fern's draft is live as a placeholder; the final copy is Porter's BA/UX-writer hat.
2. REQ-036 **B3 `inactive` definition** (cancelled only? +expired? +fully-used?) — the owner's one answer; it unblocks
   the last B3 piece (one BE task: server-side `status=` on `GET /courses` using the finalised predicate — Q1's paging fix).
3. REQ-036 **Tanya passes**, now bundled: Part-A table + idempotency re-cancel, B1 **live re-break** (extra-session on a
   cancelled course → 409, zero rows), B2 **rendered** badge/header.

## [02:2x] Porter (PM) — ✅ **REQ-036 Part B verified on `sid`. The feature is done.** → @Tanya to close your sign
Owner ran the last two. **Both pass.**

**1. The re-break — `409` AND zero rows.** Pressing `เพิ่มคาบ (คิดเงิน)` on the ended course returns
*"คอร์สนี้ถูกยกเลิกแล้ว — เพิ่ม/แก้ไข/คิดเงินคาบในคอร์สนี้ไม่ได้"*, and the booking count for มิลล่า is **8** —
which reconciles exactly to the visible plan: **20/Aug ลา/ป่วย + 25/Aug + six cancelled = 8.** The screenshot taken
**at the moment of the refusal** showed those same 8 rows and no new one. ⇒ **the refusal wrote nothing.** Proven
from two independent directions (the rendered list and a count), which is why I did not ask the owner to click it
a third time at 02:00.

**2. Residue cleared.** The stray 25/Aug `EXTRA` — the one created through the hole before TASK-185 — now reads
**ยกเลิก**. `sid` is consistent again: an ended course with no live sessions.

### 📌 Deliberately NOT tested, recorded as an accepted risk rather than an oversight
**Item 7 — `PROGRAM_CHANGED` and `CUSTOMER_CANCELLED` were not individually round-tripped.** Only `ADMIN_ERROR` was.
Proving the other two costs **two more real courses ended on `sid`**, and all three travel the **identical code
path** — the enum value is the only difference, and it is validated server-side against the same list. **Not worth
two dead courses.** ⚠️ If a reason ever comes back wrong in the data, **this line is where to look first.**

⇒ **REQ-036 = feature complete on `sid`.** Part A `TEST_PASSED` (Tanya, 02:04) · **Part B verified live** · B2 badge
done (Fern). **`uat` remains a separate owner decision** — and it still waits on the customer saying *which* course
and what happens to the money.

**Ball: @Tanya** — close your sign with the two items above. **@Sober/@Fern** — the four-status work (B3 + the
server-side `status` field) is the remaining lane. **Ball: owner** — nothing tonight.

## [--:--] Sober (SA) — caught Porter's B3 ruling I'd skated past; re-cut B3 as a computed four-status field. → @Jason
On the nudge I re-read the log and found **Porter's directive I had not actioned** (the "read Porter's latest" lesson,
again): the owner **overruled the binary toggle** — *"แยกประเภทไป ทำประเภทขึ้นมาคลุมเลย"*. B3 is now a **computed
`status`**, precedence **CANCELLED → COMPLETED → EXPIRED → ACTIVE**. COMPLETED beats EXPIRED on purpose: expired-with-
sessions-left is a family that paid for classes they never got — the only status that costs the customer money, which a
binary would have hidden.

**Porter's architecture call is the important part and I'm following it:** there is no course-status in the BE — `ปกติ`
is FE-computed, which is *why* the cancelled course wore green. Re-computing it for a filter builds the same bug twice.
⇒ **one `status` field on the course DTO, server-side; badge renders it, filter filters on it.** AC-B6: the four counts
sum to the unfiltered total (the precedence makes each course exactly one status).

Grounded it: `CourseSummary` (`contract.ts:99`) already carries `size`/`usedSessions`/`endedAt`/`expiryDate`, built in
one place (`toCourseSummary`, `lib/leave.ts`) — so `courseStatus(course, today)` is a pure function with a clean home,
no migration. Re-cut:
- **TASK-188 (BE, first):** the pure `courseStatus` + `status` on `CourseSummary` + `GET /courses?status=` (server-side
  filter/counts — closes TASK-186 Q1's paging lie). AC-B6 tested.
- **TASK-189 (FE, blocked on 188):** badge + filter read the ONE server field; **rip out the FE's own "over"/`ปกติ`
  compute** (the bug-twice source); reuse TASK-186's toggle/empty-states, drive them from the server status+counts.
- **TASK-186 SUPERSEDED** (client-side predicate replaced by the server field; its UI is reused).

REQ-036: Part A signed by Tanya (`TEST_PASSED` sid); B1 verified live (billing hole closed by pressing the button);
B2 done; **B3 is the last lane**, now correctly a server-computed status. (Owner still to clean up the stray 25/Aug EXTRA
on มิลล่า's course — that's the Part-B residue, his write.)

## [Jason (BE)] — TASK-188 BUILT → REVIEW. **One status, computed once. @Fern unblocked.** → @Sober @Fern
Porter's architecture call followed exactly: the badge and the filter now read the **same server field**, because
computing it twice is how a cancelled course wore green in the first place.

**The rule is pure and takes `today`** — the clock is resolved once, in `toCourseSummary`, in Bangkok. A rule
that reads the clock itself is a rule that cannot be tested and can quietly inherit the server's timezone.

**I tested the precedence where two statuses could both look true**, since that is the only place it can be
wrong: cancelled+completed+expired → CANCELLED · completed-past-expiry → COMPLETED · and the one that matters,
**used 7 of 10, past expiry → EXPIRED**, with the reason written into the test: *that family paid for classes
they never received*, and the binary the owner overruled would have hidden exactly them.

**🔴 AC-B6 is asserted two ways.** "The counts sum to the total" can pass while a course sits in two buckets, so
there is also a test that filtering by each status **partitions** the set — nothing counted twice, nothing
dropped — over a fixture deliberately full of ambiguous rows.

### The trade I made, written in the code and not just here
The filter and counts are computed **in TypeScript, before paging**. `status` is derived, so filtering it in SQL
would mean writing the precedence **a second time in a second language** — which is the bug this task exists to
kill, rebuilt one layer down. The cost is that the endpoint hydrates every matching course before slicing a
page; at this school's scale that is nothing (`getCourses()` already loads them all for the attention checks).
**If the list ever reaches thousands, the answer is a stored/generated status column — not a second
hand-written rule.** Q2 asks you to ratify that, or name the scale you want it to hold at.

Counts run over the search-filtered set **before** the status filter, so the chips show what switching would
find rather than "0" everywhere you aren't looking; all four appear even at zero, because a chip that vanishes
reads as a missing feature. And `total` is finally honest — which closes TASK-186 Q1 (the FE filter could only
ever filter the page it had been handed).

**Verified:** tsc **0** · `bun test` **806 pass / 0 fail** (+11). No migration. ⚠️ Nothing run against a
database, so the filter/count path is un-exercised end to end.

**Ball: @Fern** — `course.status` is on every course DTO and `GET /courses` returns `counts` (Q1: additive, not
in the contract, flagging rather than assuming). TASK-189 can now **delete** the FE's own "over"/`ปกติ` compute
rather than adjust it. **@Sober** — review + Q1/Q2; TASK-185's Q1 (rentals) is also still open.

## [--:--] Sober (SA) — TASK-188 (computed course status, B3 BE) reviewed: ✅ PASS. → @Fern (TASK-189)
Reproduced tsc 0 · `bun test` **806/0** (+11). `courseStatus` is pure and precedence-ordered exactly as the owner ruled
(CANCELLED→COMPLETED→EXPIRED→ACTIVE), `today` resolved once in `toCourseSummary` (Bangkok). Both ambiguous seams tested
(used-7-of-10-past-expiry → EXPIRED — the family that paid for classes they never got; completed-past-expiry →
COMPLETED). **Expiry is inclusive of the day** (expiring today = ACTIVE) — a real edge, tested. **AC-B6 is asserted as a
partition** (no overlap, nothing dropped), not just a sum — the stronger check. Orthogonality proven (a leave-locked
course is still ACTIVE). `total` is now honest, closing TASK-186 Q1's paging lie at the source.

Both Qs accepted: **Q1** counts additive → keep (FE needs them for the chips + visible AC-B6). **Q2** TS-side filtering
ratified — writing the precedence a second time in SQL is the exact drift this task exists to kill; hydrate-then-page is
fine at this scale, and the escape hatch (generated column) is documented in the code for if it ever hits thousands.

**B3 BE done. TASK-189 (FE badge+filter off the one field) is unblocked** — this is REQ-036's last build.

## [--:--] Fern (FE) — TASK-189 done: the badge now reads ONE server field, and the client predicate is deleted. → @Sober
TASK-188 landed, so I built B3's real brain.

### One source of lifecycle truth
`status` is threaded through to the view (**required**, so it can't silently vanish — the TASK-183 lesson), and the
badge simply renders it. **I removed the FE's own over/`ปกติ` computation — including TASK-183's `isCourseEnded`
branch on that card.** That branch was *correct*, but it was a **second source**, which is the same class of bug the
green-`ปกติ` was; keeping it because it happened to agree would have been keeping the hazard.

🔴 **What I deliberately did not collapse:** the leave-lock / special-unlock chip. That's **quota** state, not
lifecycle — a locked course is still `ACTIVE` — so they now sit **side by side**. Merging them into one chip would
have recreated the original sin in a new place: a badge that means two things and can only show one.

### Filter + counts
Four statuses, default ACTIVE, driven by `GET /courses?status=…`, so paging and totals are the server's. **TASK-186's
client-side predicate is deleted** (`course-status.ts` gone, not left to rot) — which also closes **TASK-186 Q1**: the
cross-page miscount I flagged disappears because the filtering moved to where the data lives. Thanks for cutting 188
that way; it made Q1 a deletion rather than a workaround.

**AC-B6 is visible, not merely true:** each chip carries the **server's** count (`ปกติ (12)`), computed over the
search-filtered set before paging — so the chips say what switching would actually find, and the four sum to the
unfiltered total. **I do not recount client-side.**

One honest note: offline `toCourseView` sets only `CANCELLED`/`ACTIVE` — the only thing that shape knows. It does not
guess COMPLETED/EXPIRED, because inventing them there is exactly the re-derivation this task kills.

**Evidence:** `bunx --package typescript@5.6.3 tsc --noEmit` **0** · `bun run build` **ok** ·
`bun test src/lib/scheduler/ src/services/` **40/0** · §3.5 **0/0/0/0** · no raw key · grep for
`isCourseInactive|filterByActivity|course-status|course.normal|isCourseEnded` in the panel → **zero hits**.

🔴 **For @Tanya specifically:** a cancelled course (red + reason), a used-up one (จบแล้ว), an
expired-with-sessions-left one (หมดอายุ), and a **leave-locked ACTIVE** course showing **both** chips — that last is
the case a careless merge of the two badges would have silently hidden.

**Ball: @Sober** (review). My lane is clear after this — TASK-187 still opportunistic if you want it taken.

## [--:--] Sober (SA) — TASK-189 (badge+filter off server status) reviewed: ✅ PASS. REQ-036 fully code-complete. → @Porter
Reproduced tsc 0 · build ok · 40/0. The badge reads the ONE server `c.status` (`CoursePackagePanel:159`), chips show the
server's `counts` (no client recount), the client-side `course-status.ts` predicate is **deleted** (TASK-186 Q1 miscount
gone — filtering moved to the data), and the leave-lock chip is kept **separate** (a locked course is still ACTIVE —
the one merge that would have re-created the original sin). Offline `toCourseView` only sets CANCELLED/ACTIVE (honest —
doesn't invent statuses it can't know). One trivial nit: `isCourseEnded` (`types/...:280`) is now dead — folded its
removal into TASK-187, not worth a REVISE.

**REQ-036 is now fully code-complete: Part A ✅ (signed sid) · B1 ✅ (verified live) · B2 ✅ · B3 ✅.** The single source
of "what state is this course" now lives server-side and both the badge and the filter read it — the bug-twice class is
closed.

**Last gate before DELIVERED = Tanya's rendered pass** on B2/B3: a cancelled course (red + reason), a used-up one
(จบแล้ว), an expired-with-sessions-left one (หมดอายุ), and — the case a careless merge would have hidden — a
**leave-locked ACTIVE** course showing **both** chips. Plus the owner's residual writes still open from Part B (clean up
the stray 25/Aug EXTRA on มิลล่า's course; API-level re-cancel belt-and-suspenders; round-trip the other two end reasons).
All of that is Porter's to route to @Tanya / the owner.

## [03:32] Tanya (QA) — ✅ REQ-036 CLOSED: TEST_PASSED on `sid` (Part A + Part B). → @Porter
Closing my sign on your two Part-B items. `tests/TEST-058` finalised.

- **Part A** — `TEST_PASSED` (signed 02:04): preview/reason-gate/item-5 UI verified read-only by me; items 2/3/4/7/8 on
  the owner-measured before/after (no re-owe, no money moved, soft-cancel, leave preserved; preview's 6 = DB's 6).
- **Part B** — accepting your live evidence: `เพิ่มคาบ(คิดเงิน)` on the ended course → **409 `COURSE_ENDED`** and
  **zero rows** (count held at 8 = 20/Aug ลา + 25/Aug + 6 cancelled; the refusal-moment screenshot showed no new row —
  proven from the list AND the count). The **stray 25/Aug EXTRA is now ยกเลิก** — `sid` is a consistent ended course.
- **Accepted risks, on the record (not oversights):** item 7 — only **ADMIN_ERROR** round-tripped; PROGRAM_CHANGED /
  CUSTOMER_CANCELLED share the identical server-validated path, not worth two dead courses — **this is where to look
  first if a stored reason is ever wrong.** Item-5 **API** re-cancel not separately run; the **UI makes it unreachable**
  (no Confirm on an ended course) and the `ALREADY_ENDED` guard is distinct from `COURSE_ENDED`.

⇒ **REQ-036 = `TEST_PASSED` on `sid`, feature complete.** **`uat` remains a separate owner decision** (still waits on the
customer: which course + what happens to the money) — **not mine; production stays off my hands.**

**Ball: @Porter** (REQ-036 signed off on `sid`; the `uat`/DELIVERED call is yours + owner's) **& @Sober/@Fern** (the
four-status B3 lane). Nothing waiting on me.

## [--:--] Sober (SA) — TASK-142 green-lit for Fern (Porter: Fern free, start it). → @Fern
Porter says Fern's free for TASK-142 (calendar-cell program+type+note, REQ-052+REQ-068). Checked it's genuinely ready:
deps all met (TASK-141 nickname closed no-change · TASK-178/184 put `attendeeNote` on the booking DTO · palette signed),
and I **pre-verified the data path** so Fern doesn't burn a cycle on the mapper trap that hit this feature four times —
`booking.attendeeNote` reaches the calendar cell (BE `toBookingDTO` `mappers.ts:109` → FE flatten `lib/api/mappers.ts:27`
→ type `:141`). Task is spec-complete from the 2026-08-23 re-cut; added a short go-ahead note (pinned tsc; 375px is the
primary AC-3 case — it adds a 2nd line + stripe + note to the narrowest cell). No re-cut needed.

## [--:--] Fern (FE) — TASK-142 BUILT → REVIEW. One cell, two dimensions, and one toggle item with no data. → @Sober
Thanks for pre-verifying the data path — `attendeeNote` reached the cell first try, which is the first time this
feature set hasn't cost me a mapper cycle.

### Keeping the two dimensions from colliding
🆕 **`--booking-type-*` tokens**: a dedicated four-hue set, **deliberately none of the status hues**. A cell shows
status *and* type at once, so sharing a colour makes one of them unreadable. The **type text is always present** —
colour is redundant reinforcement, never the carrier — and it's **icons, not emoji**, with the icon never carrying
meaning alone.

🆕 **`BookingCellBody`** holds everything beyond `time · name`, used by **both** grids so the week and day cells
can't drift apart. Its ordering encodes AC-3: **the program shortens first, the type label never does** — the type
is what changes what a session means commercially.

🔴 **A design call I want on the record (Q2).** The **day** cell's left stripe was **already status** — it predates
REQ-052. Adding a type stripe there would have put two stripes on one card and made **neither** readable, which is
the very collision REQ-052 exists to prevent. So the day cell gives type its own channel — **icon + type-hued
label** — and keeps the full program (AC-4). Overrule me if you'd rather status moved to a dot there for symmetry
with the week cell; I chose not to change an affordance staff already read.

**Legend now names both dimensions** (AC-9): one that explained only status would teach staff the other channel is
decoration.

### The toggle
`useCellDisplay` + `CellDisplayMenu` — the **fixed five**, default all on, persisted like the language toggle.
**Display-only; none of it filters bookings.** That distinction is the point: a "filter" quietly removing sessions
from a calendar would make the screen lie about what's booked. Saved preferences merge **over** the defaults, so one
stored before a field existed can't leave that field hidden forever.

### 🔴 Q1 — one of the five has nothing behind it
**`rental` is inert.** I checked the wire instead of assuming: `toBookingDTO` carries **no** rental marker, so
there's nothing to render. I did **not** silently drop it to four — the set being exactly five was explicit. Either a
cheap BE field (`hasRental` — rentals already carry the booking as `refId`), or the re-cut drops it. Your call.

**Evidence:** `bunx --package typescript@5.6.3 tsc --noEmit` **0** · `bun run build` **ok** ·
`bun test src/lib/scheduler/ src/services/` **40/0** · §3.5 **0/0/0/0** on all four changed/new components · no raw key.
🔴 **AC-3 not measured** (headless) → @Tanya, and **375 is the case**: this adds a second line + stripe + note to the
narrowest cell. The rule to check is that when space runs out the **program** shortens and the **type label** never does.

**Ball: @Sober** (review · Q1 rental · Q2 the day-cell stripe call). My lane is clear apart from TASK-187, still
opportunistic.

## [--:--] Sober (SA) — TASK-142 (calendar cell, REQ-052+068) reviewed: ✅ code accepted; pending Tanya rendered. → @Porter
Reproduced tsc 0 · build ok · 40/0. One shared `BookingCellBody` (week+day can't drift), dedicated `--booking-type-*`
tokens (none of the status hues, so status+type never fight for colour), type label always present, toggle = the fixed
five persisted + **display-only** (never filters bookings), note wired off the pre-verified path. Solid.

**Q1 (rental inert): keep the five, wire the data — TASK-190 (BE) cut.** The booking DTO has no rental marker (Fern
grounded it), so the 5th toggle renders nothing. Dropping to four contradicts the explicit five-item decision and
rental is a real thing staff want on the cell; `hasRental` on `toBookingDTO` is cheap (rentals carry `refId`). Ship 142
now, rental interim-inert. She was right not to silently drop it.
**Q2 (day-cell type channel): accepted her call.** Day cell's stripe was already *status*; a 2nd stripe = the collision
REQ-052 fights, so type gets an icon+label channel there. Won't destabilise a status affordance staff read. Whether the
week/day channel difference confuses is a **rendered** question → Tanya.

**REQ-052+REQ-068 (TASK-142) code-complete; last gate = Tanya's rendered pass (AC-3 @375 first + the Q2 week/day check).**

**The bottleneck is now QA, not code.** Everything the owner asked for this stretch is code-complete; what's left to
reach DELIVERED is Tanya's rendered/live verification, which reaches her through Porter:
- **REQ-036** B2/B3 rendered (cancelled=red+reason · used=จบแล้ว · expired=หมดอายุ · leave-locked ACTIVE shows BOTH
  chips) + the Part-B live re-break (extra-session on cancelled → 409, zero rows) + owner residuals (stray 25/Aug EXTRA).
- **REQ-052+068** (TASK-142) AC-3 @375 + Q2 week/day channel.
Background lanes (not blocking delivery, no ball): TASK-190 (BE hasRental) waiting for Jason; TASK-187 (FE mapper sweep +
dead `isCourseEnded`) opportunistic for Fern.

## [--:--] Porter (PM) — 🔻 Tanya is right: I told the owner to wake her without routing anything, and nothing is deployed.
She refused to start, and her reasons are both correct: **nothing had been routed to her through me**, and
**TASK-142 is not on `sid`** — it passed review and stopped there. **I skipped my own job in the chain and then
told the owner to go around it.** Recorded because that is exactly the kind of gap that loses a day.

### 📌 Deploy check done before routing — **no new migration**
`drizzle/` ends at **`0023_course_ended`**, journal **24**, and `0023` is already applied and verified on `sid`.
⇒ **BE + FE code deploy only.** `db:migrate`/`db:verify` should be run anyway (they are now the blocking step) —
they will simply confirm 24/24 rather than apply anything.

### 📌 @Tanya — routed, formally, and it is TWO features on one screen
Start once the owner confirms `sid`. **Begin at 375px** — the narrowest cell is where this fails first, and it now
has to carry program + booking type + status + note at once.

**1. REQ-052 + REQ-068 (TASK-142) — the calendar cell**
- **AC-3 @375 first.** One shared `BookingCellBody` for week and day, so if they drift that is a defect.
- **Status and type must never fight for colour** — dedicated `--booking-type-*` hues, none of them status hues,
  and the **type label is always present as text** (not colour-only).
- **The display toggle is display-only.** ⚠️ **If flipping it ever changes which bookings appear, that is a
  serious defect** — it must change what is *shown on* a cell, never what is *on the calendar*.
- **Sober's Q2 → yours to judge rendered:** the week cell carries type as a stripe, the **day cell as icon+label**
  (a second stripe would collide with status). **Does that difference confuse someone reading both views?**
- ⚠️ **Known and accepted: the 5th toggle item (rental) renders nothing** — the DTO has no rental marker yet
  (TASK-190 cut). **Not a finding.** Fern flagged it rather than silently dropping to four, which was right.

**2. REQ-036 B2/B3 — the four statuses on the course list**
- **ยกเลิกแล้ว** (red + the reason) · **เรียนครบแล้ว** · **หมดอายุ** (expired with sessions left) · **กำลังเรียน**.
- 🔴 **The case a careless merge would have hidden: a leave-locked course must read `ACTIVE` and show BOTH chips.**
  A locked course is still running. **If it renders as a fifth status, that is the original `ปกติ`-on-a-cancelled-
  course bug coming back by another door.**
- Filter defaults to **กำลังเรียน**; the four counts must **sum to the unfiltered total** — nothing in two
  categories, nothing in none.

**Scope `sid`. `uat` is not yours** — unchanged.

**Ball: owner** — deploy BE+FE to `sid` (`db:migrate` → `db:verify` 24/24 → restart → FE), then say so.
**Then @Tanya.** Nothing else waits on anyone.

## [--:--] Porter (PM) — ✅ **DEPLOY CONFIRMED on `sid` — BE restarted + FE up.** @Tanya, you are cleared to start.
Owner confirmed both halves explicitly, which is what you asked for and were right to ask for:
- **`db:migrate` → `db:verify`: journal 24 · ledger 32 · witnesses 24 · ✅ green.** No new migration — `0023` was
  already in; this run confirmed rather than applied.
- **BE restarted** (the ended-course write guard + the computed `status` field are live).
- **FE deployed** (four-status badge · filter · the new calendar cell).

⇒ **`sid` carries everything under test. The brief in my previous entry is now live — go.**

Two things I want to be explicit about, so the run is not wasted:
1. **Start at 375px.** Both features share that constraint and it is where either breaks first.
2. **The two "if this happens it is serious" cases** are the display toggle changing *which* bookings appear
   (it must only change what a cell *shows*), and a leave-locked course rendering as a fifth status instead of
   **ACTIVE + a separate lock chip**. Everything else is a normal finding; those two are regressions of bugs we
   have already paid for once.

**Ball: @Tanya.** **@Jason** — TASK-190 (`hasRental` on the booking DTO) is free to pick up; it is the only thing
standing between the 5th toggle item and real data. **@Fern** — TASK-187 (mapper sweep + the dead `isCourseEnded`)
when convenient. **Ball: owner** — nothing until Tanya reports.

## [--:--] Porter (PM) — 🟢 `uat` schema brought up to **24/24**, code deliberately held back. Risk split in two.
The owner flagged the real gap: **`uat` had received nothing since the cancel work began.** `db:verify` on `uat`
confirmed it — **23 applied, `0023_course_ended` missing**, i.e. the feature the customer asked for could not have
worked there even if the code had gone up.

**Notably, this time the tool said `"drizzle would apply it"`** — the ledger's newest row sits *below* `0023`, so it
was a genuine not-yet-run, **not** the silent-skip class that took the calendar down last night. The distinction
matters and the verifier made it for us.

### Why the migration went ahead of Tanya, deliberately
`0023` only adds **`ended_at` / `end_reason`**, columns **no deployed code reads**. Applying it changes nothing for
the customer, and it is the correct order anyway (migration before code, now a blocking step on the board).
⇒ **The risk splits cleanly: the zero-risk half is done now; the half that needs a QA verdict waits for one.**
When Tanya passes, `uat` is a plain code deploy — no migration to get wrong at the moment we are in a hurry.
**Both boxes are now on the same schema: journal 24 · witnesses 24 · ✅.**

**Still NOT on `uat` (all of tonight's work):** REQ-036 (cancel button · ended-course write guard · four-status ·
filter) and REQ-052/068 (calendar cell + note). **Gate: Tanya's rendered pass on `sid`.**

**Ball: @Tanya** (in progress) → then **owner** (BE restart + FE to `uat`).

## [04:12] Tanya (QA) — 🔴 BLOCKED: the FE under test is NOT on `sid` — partial deploy (BE new, FE old). → @Porter
I can't run either rendered pass: **neither REQ-052+068 (TASK-142) nor REQ-036 B2/B3 (TASK-188/189) is rendering on
`sid`.** Cross-checked against the API — it is specifically an **FE deploy gap**, not a code fault.

**Evidence — FE is a pre-task build:**
- **Course list:** every card badge reads **"NORMAL"** (green) or **"LOCKED"** (red) — strings that **do not exist in the
  current code** (which renders `Active/Completed/Expired/Cancelled` + a separate `Locked` chip). **No four-status
  filter** on the page, and the view is **not** defaulting to ACTIVE (มิลล่า's cancelled course would be hidden if B3
  were live). ⇒ the deployed FE predates TASK-188/189.
- **Calendar cell:** a booking renders as **"10:00 Aileen" (time + name only)** — the OLD cell, not REQ-052's
  program + type + status + note; **no display toggle** (REQ-068). ⇒ the deployed FE predates TASK-142.

**Evidence — the BE IS deployed (so the data is ready, the FE just isn't consuming it):**
- `GET /courses` DTO now carries **`status`** with live values **`ACTIVE` / `CANCELLED`** (+ `endedAt`/`endReason`).
- `GET /bookings` DTO carries **`bookingType`** and **`attendeeNote`** — exactly the fields the new cell needs.

⇒ **Partial deploy: BE current, FE stale.** The `db:migrate`/`db:verify` you cited confirmed the BE/migrations; the
**FE build serving `sid` is older than TASK-142/188/189** (an old bundle deployed, or the FE wasn't rebuilt). Nothing to
fix in code — the correct FE needs to reach `sid`.

**Not verified (blocked on the FE deploy) — including your two "serious" cases:** the display toggle changing *which*
bookings show, and a leave-locked course rendering as a 5th status vs ACTIVE + lock chip. Both need the UI that isn't there.

**Ball: @Porter / owner** — redeploy the current FE to `sid` (verify the bundle post-build), then say so **& @Tanya** —
I run the full pass (375-first, the two serious cases, four-status counts) the moment the real FE is up.

## [04:24] Tanya (QA) — ✅ FE redeployed (my finding was right) → rendered pass DONE: REQ-052+068 + REQ-036 B2/B3 PASS on sid. → @Porter
The FE is now current on `sid` — I re-verified it myself (the earlier stale-FE finding was correct; the redeploy fixed
it: the four-status filter, "Active" badges, and the new cell all render now, where before it was NORMAL/LOCKED + time+name).
Ran the full pass, **375-first**, at 1440 too. `tests/TEST-059`.

**Feature 2 — REQ-036 B2/B3 (four-status course list) — PASS:**
- Filter **Active (9) · Completed (0) · Expired (0) · Cancelled (1)** — the four counts **sum to 10** (the unfiltered
  total: nothing double-counted, nothing uncounted). Default view = **Active**.
- **Cancelled** filter → มิลล่า's course badges **"Cancelled — Admin entered it by mistake"** (status + the reason, red).
- 🎯 **Your #2 serious case — PASS:** the leave-locked Aileen course reads **"Active" + a separate "Locked" chip** (two
  chips side by side), **not a fifth status.** Holds at 1440 and 375.

**Feature 1 — REQ-052+068 (calendar cell) — PASS:**
- Type legend renders with **SVG icons, NOT emoji** (1st Trial / 1 HR / Weekly course / Voucher) — your emoji check passes,
  and no emoji character anywhere in the legend/cells. Type label is present as **text**, not colour-only.
- Cells now carry program + **type** (e.g. "🎓 Weekly course" icon+label) + status; a **"Cell display"** toggle (REQ-068) is present.
- 🎯 **Your #1 serious case — PASS:** flipping **Cell display** kept the **same 9 bookings** on the grid (before = after) —
  it changes what a cell *shows*, never *which* bookings appear.

**375 (both):** no horizontal scroll; four-status filter present; locked course shows both chips; type legend + toggle present.
🟡 **Minor (cosmetic, 375 only):** the course-card badges truncate to "AC…" / "LO…" at 375 — both chips are still present and
distinguishable, but the labels clip. Your call whether that's worth a tweak.
⚪ **Lightly assessed (not a finding):** Sober's Q2 — week cell shows type as a stripe, day cell as icon+label; both views
carry the type clearly and I did not find the difference confusing, but it's a judgment call you may want the owner's eye on.

**Ball: @Porter** — REQ-052 + REQ-068 + REQ-036 B2/B3 signed on `sid`; `uat`/DELIVERED is your + owner's call. **Nothing waits on me.**

## [--:--] Porter (PM) — 🚦 **UAT GATE — GREEN.** REQ-036 + REQ-052 + REQ-068 cleared for the customer's box.
Written block per the owner's 2026-08-19 rule. **Both names, and what was NOT tested, stated.**

- **Build:** `smart-scheduler-back` + `smart-scheduler-front`, the `sid` build Tanya passed at 04:24.
- **Migrations: NONE outstanding.** `0023_course_ended` was applied to `uat` ahead of time and verified —
  **both boxes are journal 24 · witnesses 24 · ✅.** This is a **plain code deploy**, deliberately: the risky half
  was done separately hours ago so nothing has to go right under time pressure now.
- **Tested on `sid` (Tanya, `TEST-058` + `TEST-059`):** REQ-036 Part A `TEST_PASSED` (measured before/after — no
  re-owe, no money moved, soft-cancel, leave row preserved) · Part B live re-break (`409 COURSE_ENDED`, zero rows)
  · four-status filter (**counts sum to the total**) · **both of the cases I flagged as serious PASS** — the
  display toggle does not change *which* bookings appear (9 = 9), and a leave-locked course reads **ACTIVE + a
  separate Locked chip**, not a fifth status · icons not emoji (REQ-052 AC-9) · 375 and 1440, no h-scroll.
- **NOT tested, on the record:** `PROGRAM_CHANGED` / `CUSTOMER_CANCELLED` end-reasons were never individually
  round-tripped (identical server-validated path; **first place to look if a stored reason is ever wrong**) ·
  the API-level re-cancel `409 ALREADY_ENDED` (the UI makes it unreachable) · the 5th toggle item (**rental**)
  renders nothing until TASK-190 lands — **known and accepted, not a defect.**
- **Rollback:** code-only, so redeploy the previous build. **`0023` stays** — it is additive and nothing else reads
  those columns. **No data migration to unwind.**
- **Customer impact:** staff gain a cancel button and a four-status view. **Nothing changes for existing courses
  until someone presses cancel.** No notification, no money movement, no change to any booking.
- **Porter — GREEN.** **Tanya — signed on `sid`** (`TEST-058`/`TEST-059`); `uat` remains outside her charter, so
  this is *evidence-from-`sid` on an identical build*, **not** a Tanya sign-off on the customer's box. Recorded that
  way so nobody later reads it as a waived gate.

🟡 **One cosmetic defect shipping knowingly:** at **375px the course-card badges clip to "AC…" / "LO…"**. Both chips
are present and distinguishable, but **the label of a feature whose entire purpose is "read the status at a glance"
is unreadable on a phone.** Not a blocker; **queued as a small FE fix.** Flagging it rather than letting it be
discovered by staff.

**Ball: owner** — deploy BE + FE to `uat` (`db:verify` should read 24/24 unchanged → restart → FE), then open the
course list and confirm the four filter chips render. **@Fern** — the 375 badge clip + TASK-187. **@Jason** —
TASK-190 (`hasRental`).

## [--:--] Porter (PM) — 🟢 `uat` schema green (24·33·24). And a confusion worth writing down: **we could not tell the boxes apart.**
`uat` now verifies **journal 24 · ledger 33 · witnesses 24 ✅**, seeded from its own schema (`0023` was physically
applied — `course_packages_end_reason_chk` present — only its ledger row was missing). **Code deploy is next; the
owner has correctly not deployed yet.**

### 📌 The order question the owner asked, answered for the record
> *"ฉันต้อง deploy back ก่อนรันพวกนี้?"*

**No — the opposite.** These scripts run from the local repo **straight at the target database**; deployment is
irrelevant to them. **Migration must precede the code that reads the new columns** — reversing exactly that took
the customer's calendar down last night. The order is, and stays: **migrate → verify green → deploy BE → deploy FE.**

### 🔴 The thing that cost us two rounds tonight: **nothing tells us which environment we are talking to**
Both boxes converged on **`journal 24 / ledger 32 / witnesses 24`** — *identical numbers*. So when the same command
returned **green** earlier and **red** later, it looked like a regression and was actually **a different database**
(the local `.env` had moved between `sid` and `uat`). I could not tell from the output, and **the discriminator I
offered first was wrong** — I quoted student counts from PROJECT-STATUS (137 / 133) and the box answered **205**,
because the customer has been adding students all week. **I used a two-day-old number as an identity check.**

What actually settled it: `SELECT count(*) FROM course_packages WHERE ended_at IS NOT NULL` — **1 on `sid`
(มิลล่า), 0 on `uat`** — a fact created by our own work tonight and therefore current.

⇒ **Small task, two stamps, one place:** the app should show **which environment** and **which build/commit** it is
serving. The missing **build stamp** already cost two rounds before tonight (it is on PROJECT-STATUS as a known
gap) and cost a third at 04:00 when a stale FE passed for a fresh one. The **environment stamp** is the same class
and cost two more. **Neither is a feature; both are how you stop guessing.** @Sober — cut it when the lane clears.

**Ball: owner** — deploy BE then FE to `uat`, then **confirm from the screen** (four filter chips), not from the
deploy command. **`sid` is unaffected and stays green.**

## [--:--] Porter (PM) — 🟢 `uat` LIVE. 🔴 New: the display toggle does not re-render. **And my test brief is why QA missed it.** → @Sober
**Deploy confirmed from the screen, not the command.** The `uat` calendar loads (the page that went down last
night) and the new cell renders: **`09:00 Alex · 1 HR · Skateboard · wdwda note`** and **`10:00 Aileen ·
คอร์สรายสัปดาห์ · Inline Skate`** — program + booking type + note, all present.
⇒ **REQ-052 + REQ-068 = DELIVERED** (built, `sid`-tested, live and re-checked on the customer's box).
⇒ **REQ-036** — code is live on `uat`, but the **course-list four-status view has not been looked at there yet**;
one screen away from DELIVERED. Not claiming it until someone sees it.

### 🔴 The finding: `แสดงในแต่ละการจอง` ticks, but the cells do not update
All five items are checked and the cells honour them — but **toggling does not redraw**. The owner: *"มันติ๊กๆ
แล้ว ทำไมไม่ refresh"*.

### 🔻 Why QA passed it, and it is my fault not Tanya's
I gave her one instruction about this toggle: **"if flipping it ever changes which bookings appear, that is a
serious defect."** She verified exactly that — **9 bookings before, 9 after. PASS.**
**But a toggle that does nothing at all passes that test perfectly.** I wrote the check that proves it must not
break something, and **never wrote the check that it must actually work.** Same shape as the REQ-036 rigour pass,
where all five of my checks asked *"does the cancel break anything?"* and none asked *"what can still be done
afterwards?"* — **twice now I have specified the negative and left the positive untested.**
⇒ **Rule for my own briefs from here: every "must not" needs its paired "must".**

### @Sober — one question decides the fix, and it is faster to read than to test
Does the toggle **persist and only fail to re-render**, or **not persist at all**? Fern's note said the toggle is
"the fixed five persisted + display-only" — so state is being stored somewhere. **Read it rather than asking the
owner to click; you will know in a minute and he has been at this since yesterday morning.**
**Priority: normal.** The owner called it not urgent and he is right — **no data is wrong and no money is wrong**;
the cells show the right content, they just do not respond live.

**Ball: @Sober** — cut the toggle re-render fix. Also still queued: **TASK-190** (`hasRental` — the 5th toggle item
has no data), **TASK-187** (mapper sweep), the **375 badge clip**, and the **build + environment stamp** (three
rounds lost to it in 24 hours). **Ball: owner** — one look at the `uat` course list to close REQ-036.

## [--:--] Porter (PM) — 🟢 **REQ-036 · REQ-052 · REQ-068 = DELIVERED.** Confirmed on the customer's own box.
Owner opened the `uat` course list. **Active (151) · Completed (18) · Expired (2) · Cancelled (0)** — default view
is Active, and **AJ's card carries `ACTIVE` + a separate `🔒 LOCKED` chip on real customer data.** The case I
flagged as the most likely regression is proven where it actually matters. Together with the calendar cell
confirmed earlier (`1 HR · Skateboard · wdwda note`), all three REQs have a post-deploy re-check.

### 📌 The feature paid for itself in its first minute: **Expired (2)**
Two of the customer's courses are **past expiry with sessions never taught** — **two families who paid for classes
they did not receive.** Nobody could see that yesterday.
**And it is exactly what my own first proposal would have buried.** I offered a binary *active / inactive*; the
owner overruled it with *"แยกประเภทไป ทำประเภทขึ้นมาคลุมเลย"*. Under my version these 2 would sit inside a bucket
with the 18 normally-completed courses and never be found. **The distinction he insisted on is the only reason
they are visible**, and the reasoning I wrote into AC — *"COMPLETED beats EXPIRED because expired-with-sessions-
left is the one that costs the customer money"* — was me catching up to his call, not leading it.

⇒ **Routed to the owner as a business item, not an engineering one:** two named families worth a phone call.

**Status: REQ-036 · REQ-052 · REQ-068 → DELIVERED** (built · `sid` `TEST_PASSED` by Tanya · deployed to `uat` ·
re-checked on screen there). Board + PROJECT-STATUS updated next.

**Open, none of it urgent:** display-toggle re-render · `hasRental` (TASK-190) · 375 badge clip · build +
environment stamp · TASK-187. **Ball: @Sober** for all five. **Ball: owner** — the two expired families, when the
sun is up.
