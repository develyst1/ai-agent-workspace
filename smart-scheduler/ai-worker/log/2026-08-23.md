# Day log — 2026-08-23

> Continues `2026-08-22.md`. Opened by Porter (PM) at the date roll; yesterday's file stays as written.

## [--:--] Porter (PM) — 🟢 **The baht fix is PROVEN IN THE LEDGER.** And what the owner ran proves item #1, not #4.
Owner created a discounted sale on `sid` and pulled the rows:
```
00:01:20.163  SALE      +5,790   course-onewheel-4
00:01:20.171  DISCOUNT    -391   course-onewheel-4   ← 8 ms later, same item
```
✅ **`−391`, not `−3.91`.** **That is the single most important number of the whole baht/satang episode**, and it
is now settled **in the ledger** rather than on a screen — which was the entire point, since the screen was
plausible for the bug's whole life. **The defect is closed on both sides, end to end.**

### 🔻 But it does not close item #4, and I need to say so rather than let a green stand in for a different green
**He booked a COURSE (`course-onewheel-4`), not a 1st Trial.** A course **posts at the moment of sale** — that is
why both rows appeared instantly, 8 ms apart, with or without the day-end job. **The day-end path was never
exercised.**

**What still needs proving is specifically:** a **1st Trial** (`first-trial`, ฿1,390) — which posts **nothing** at
booking, must be marked **มาเรียน**, and only then posts **`+1,390` / `−391`** when `end-of-day.ps1` runs.
**Two different posting moments; only one is proven.**

**Why I am not waving it through:** trial and single-session are the *only* users of that second moment, so nothing
else in the system exercises it — and it is the path that carries the customer's actual promo (1,390 → 999). **A
pass on the course path tells us nothing about it.**

**Ledger status of REQ-063:** at-sale ✅ proven (course, discounted and undiscounted, and now baht) · **day-end ⬜
unproven.**

**Ball: owner** — the trial recipe (book trial today with Baht 391 → ledger empty → มาเรียน → `end-of-day.ps1` on
`sid` → ledger shows +1,390 / −391). **@Fern** — TASK-167 still open.

## [--:--] Porter (PM) — 🟡 Day-end DOES post a trial — but **the discount row is missing.** Not concluding; one query settles it.
Owner ran the full recipe. After `end-of-day.ps1`:
```
00:10:04  SALE  +1,390  first-trial     ← posted at DAY-END, not at booking ✅
00:01:20  DISCOUNT -391 course-onewheel-4
00:01:20  SALE  +5,790  course-onewheel-4
```
✅ **The second posting moment is proven to exist and work**: the trial posted **nothing** at booking and
**+1,390** only after the job ran. That half of item #4 is answered.

🔴 **But there is no `DISCOUNT` row for the trial — only the full ฿1,390.** Expected `+1,390` **and** `−391`.

**Two possibilities, and they lead opposite ways. I am not guessing between them:**
1. **The trial was booked without a discount** ⇒ `+1,390` alone is correct, and we simply have not tested the case
   yet. Re-run needed, nothing wrong.
2. **The discount was stored and then DROPPED at posting** ⇒ a real defect on the very path the customer's promo
   uses — and it would be **silent to staff**, who would believe they had given ฿391 off. `safeStoredDiscount`
   (TASK-162) is *designed* to drop rather than post a bad negative, so a drop is a supported behaviour — the
   question is whether it fired when it should not have.

**The decisive evidence is the booking's stored columns**, not the ledger. **One read-only query sent to the
owner.** If it shows the trial holding `BAHT / 391`, this is a defect and it goes straight to Sober; if it shows
nothing, we simply re-run the recipe with the discount actually entered.

**Worth noting either way:** TASK-163 exists precisely so a dropped discount is **visible on the attention panel**
instead of only in a log nobody reads. **If this turns out to be case 2, that check is about to earn itself.**

**Ball: owner** — one query. **@Fern** — TASK-167 still open.

## [--:--] Porter (PM) — ✅ No defect: the trial was booked without a discount. And the owner's "why no discount on single session" has a grounded answer.
**1. The stored-columns query returned nothing** ⇒ **case 1**: the trial was booked **without** a discount, so
`+1,390` alone was correct. **No defect.** The day-end posting path is proven to work; **the day-end path with a
discount is still the one untested case**, and it needs the discount actually entered at booking.

**2. Owner: *"ทำไม one session ไม่มีส่วนลด — บน calendar มี discount ได้แค่ first trial"*.**
Read the code rather than speculate. `BookingModal.tsx:620`:
```ts
const singleFullMinor =
  bookingType === "SINGLE_SESSION" ? (packageFor(sellable, subjectId, 1)?.priceMinor ?? 0)
  : bookingType === "FIRST_TRIAL"  ? (sellable?.firstTrialPriceMinor ?? 0) : 0;
```
and the section renders only `{singleFullMinor > 0 && …}`.

**⇒ SINGLE_SESSION is fully supported. The field is absent because the PROGRAM has no 1-hour price** — and that is
**REQ-061, decided today**: the card has **no 1-hour rate for the blue block** (Surfskate · Skateboard · Inline
Skate · Bike · Freeskate · the combined ones). Only **Onewheel (1,690)**, **Balance Play Private (1,390)** and
**Balance Play Group (1,090)** are sold by the hour.

**So this is not a gap — it is the same decision showing its face on another screen.** A discount field on a
program that cannot be sold by the hour would be worse: it would invite staff to price something the server
(correctly, per REQ-061 requirement 6) **refuses** to book at all.

**Prediction stated as a prediction:** pick **Onewheel** or **Balance Play** as the program on a SINGLE_SESSION and
the discount section should appear. **Porter has not run it** — asking the owner to confirm rather than asserting.

🟡 **The one real thing here is UX, and it is worth a line:** staff see a discount box on 1st Trial and none on a
single session, with **nothing explaining why**. They cannot know it depends on the program. **Not raising a REQ
today** (lane discipline, and REQ-061's refusal message already catches the actual booking attempt) — **recorded
so it is a considered omission rather than an oversight.**

**Ball: owner** — confirm the prediction on Onewheel/Balance Play, and the trial-with-discount re-run when convenient.

## [--:--] Porter (PM) — The discount was never stored ⇒ stale build on `sid`. Owner rebuilding. **Second deploy-state round trip today.**
`SELECT … FROM bookings WHERE discount_kind IS NOT NULL` ⇒ **still empty after booking a trial WITH a discount.**
⇒ the value never reached the database at all, which rules out the day-end drop and points at the running build.

**Grounded before concluding:** `scheduler.service.ts:854-895` is complete and correct — `captureBookingDiscount`
validates, then `createBooking` writes the four columns after the insert. **The code is right; what is running is
not it.** Owner is rebuilding and redeploying.

### 📌 A gap worth naming, because this is the SECOND time today
Earlier, Tanya was minutes from testing an FE that had not been deployed; now a booking behaved like old code and
it cost several exchanges to establish that. **In both cases the question was "what build is actually live?" and
nobody could answer it from the product.** The sidebar shows a static `v0.1`, which answers nothing.

**A build stamp — commit or timestamp, visible in the UI or on a health endpoint — would have settled both in
seconds.** ⚠️ **Not raising a REQ tonight** (lane discipline, and the owner is mid-deploy). **Recorded so it is
picked up when a lane frees**, alongside the backoffice-auth note from earlier.

### The retest, once the deploy lands — unchanged, five steps
1. Book a **1st Trial today** with **Baht `391`**.
2. `bookings` query ⇒ **must now show `BAHT / 391`.** *(If it is still empty, stop — the problem is not day-end.)*
3. Mark **มาเรียน**.
4. `end-of-day.ps1` **on the `sid` box**.
5. `bo.movement` ⇒ **`+1,390` and `−391`.**

**Step 2 is the new checkpoint** — it splits "not stored" from "stored but dropped", which is the distinction that
cost us this round.

**Ball: owner** — rebuild, deploy, then the five steps.

## [--:--] Porter (PM) — 🔴 Handing the trial-discount reproduction to @Sober as a defect. **I have run out of things the owner should be doing.**
The owner has restarted pm2 several times and re-deployed. **I am stopping the ask-the-owner-to-try-one-more-thing
loop** — it is past 1am, he has run every check I asked for, and continuing to hand him diagnostics is me using
him as a debugger. **This is an SA/BE investigation now.**

### What is ESTABLISHED — do not re-tread any of this
- ✅ **Both source sides are correct.** FE `BookingModal.tsx:701` sends `discount: discountPayload(…)`;
  `discountPayload` (`lib/scheduler/discount.ts:88`) returns `{kind, value, reason}` when the draft is valid.
  BE `validation.ts:107` accepts `discount` on the booking body (TASK-162); `scheduler.service.ts:878-886`
  captures it and **updates the row after insert**.
- ✅ **The deployed `.exe` is recent**: `discount_actor` (a DB column name, preserved through compilation) **is
  present** in `C:\Develyst\smart-scheduler\back\dist\smart-scheduler-back.exe`.
  ⚠️ **My earlier greps were bad tests and their negatives prove nothing** — a Thai string in a binary can fail on
  encoding, and `captureBookingDiscount` is a function name that compilation may rename or inline. **Do not cite
  those two as evidence.**
- ✅ **pm2 restarted repeatedly** — a stale process is not the explanation.
- ✅ `SELECT … FROM bookings WHERE discount_kind IS NOT NULL` ⇒ **empty after two attempts with a discount entered.**
- ✅ **The at-sale path works end to end** — a discounted course posted `+5,790 / −391`. So the discount machinery
  is alive; it is specifically the **booking capture** that is not landing.
- ✅ Day-end posting itself works — an undiscounted trial posted `+1,390` after `end-of-day.ps1`.

### The two questions I could not answer from here, for @Sober
1. **Does the request actually carry `discount`?** The FE will send `undefined` if `evaluateDiscount` reports any
   problem — **including a missing reason** — and the submit guard should prevent that, but nobody has looked at
   the wire. **The browser's network payload, or a server-side log of the received body, settles it in one look.**
2. **If it arrives, why does `captureBookingDiscount` return undefined?** `validateSaleDiscount(...)` returning
   falsy makes the booking succeed **with the discount silently dropped** (`:863` — `if (!validated) return
   undefined;`). **That is a silent-drop path on a money field**, and whether or not it is today's cause, **it
   should not exist without a loud log** — the same lesson as `sales_not_posted`.

**Nothing is at risk while this is open:** `uat` has neither the column nor the code, the customer is unaffected,
and every other REQ-063 path is proven. **This blocks the green light, not the business.**

**Ball: @Sober.** **Owner: nothing — please stop here.**

## [--:--] Porter (PM) — 🎯 **Owner localised it: the FE never sends the discount.** Handing @Sober a defect with a cause, not a symptom.
The owner went and checked the wire himself — **the thing I said needed doing and could not do from here:**
1. **No request body carries `discount` on the booking POST.** ⇒ **the frontend is not sending it.** The backend is
   exonerated; every BE hour spent on this would have been wasted.
2. **The discount is nowhere to be seen when marking ATTENDED either** — same as at creation.

**⇒ Confirmed FE defect.** That is a cause, not a symptom, and it came from him doing the one check nobody else
could. **Recorded as his find.**

### What that narrows it to — for @Sober → @Fern
`discountPayload(discount, singleFullMinor)` (`lib/scheduler/discount.ts:88`) returns `undefined` in exactly two
cases: **`!touched`**, or **`problemKeys.length`**.
- **It cannot be `problemKeys`**: `BookingModal.tsx:687-689` puts the *same* `singleDiscountEval.problemKeys.length
  === 0` into `valid`, so a problem would have **disabled the submit button** — and he submitted successfully.
- **⇒ It must be `!touched` — the `discount` draft is EMPTY at submit time**, even though the section rendered and
  he typed into it.
**That points at state wiring, not at the discount rules:** the `discount` state reaching `discountPayload` is not
the one `DiscountSection`'s `onChange` updates, **or something resets it before submit** (a subject/teacher effect
is the usual culprit in this modal). **The rules engine is fine — five other forms use it and work.**

### Second, separate gap in the same area — do not lose it in the fix
**A stored discount is not shown anywhere on the booking after creation** (the ATTENDED view included). Even once
capture works, **staff cannot see that a discount was given** — no confirmation, no audit trail on the record they
are looking at. **REQ-063 requirement 8 and AC-10 say who/what/why must be answerable; a value only visible in the
database does not satisfy that.**

**Ball: @Sober** — cut the FE fix (capture wiring **+** display on the booking), and please have the reproduction
be the owner's exact one: book a 1st Trial with Baht 391 and assert the **request body** carries it. **Owner:
nothing further tonight — you found it, that is more than enough.**

## [--:--] Sober (SA) — took the trial-discount defect; grounded it to BookingModal state-loss, cut TASK-170 → @Fern @Porter
Picked up the defect you and the owner localised (booking POST carries no `discount` — confirmed FE, BE exonerated).
Grounded it before handing over so Fern gets a pointer, not a hunt:
- **The JSX wiring is correct** — `BookingModal.tsx:881-882` is `value={discount}` / `onChange={setDiscount}`, one
  state (`:584`), read at submit via `discountPayload(discount, singleFullMinor)` (`:701`). `discountTouched`
  (`value!=="" || reason!==""`) is correct, and **DiscountSection is identical to the four forms that work.** So the
  rules engine and the component are fine — **this is BookingModal-specific.**
- ⇒ With a valid BAHT+reason, `discountPayload` returns `undefined` only via **`!touched`** — the `discount` state is
  **empty at submit despite typing.** Your deduction was right; I confirmed the mechanism. The only `setDiscount`
  reset is `changeTab` (`:639`), so the suspect is that reset re-firing after the field is typed, a modal
  remount/key wiping state, or a stale submit closure — a **runtime** state loss, best found live in DevTools
  (watch `discount` as you type → the reset moment is the bug). That's Fern's to root-cause with the running app.
- **Reproduction pinned to the wire** (yours): 1st Trial + Baht 391 + reason ⇒ the POST body must carry `discount`.

**Part 2 folded into the task (req 8 / AC-10):** even once capture works, a stored discount is **invisible on the
booking** (creation + ATTENDED view) — who/what/why must be answerable, and a DB-only value isn't. Fern surfaces it.

**Flagged for you, NOT cut (not today's cause):** the BE `captureBookingDiscount` **silent-drop** path
(`scheduler.service.ts:863` — `validateSaleDiscount` falsy ⇒ booking succeeds, discount dropped, no log). A
silent-drop on a money field shouldn't exist without a loud signal (the `sales_not_posted` lesson). Small BE
hardening, its own task when a lane frees.

**Ball: @Fern** — TASK-170 (the green-light blocker: capture wiring + the display). **@Porter** — you're driving the
gate; this is the last known blocker on REQ-063's discount path (baht fix proven in the ledger, at-sale proven,
day-end posting proven; only the **day-end discount capture** on the trial path remains, which is exactly this bug).
The BE silent-drop hardening is yours to schedule. Nothing routes to the owner from me.

## [--:--] Fern (FE) — TASK-170 Part 1 FIXED: it was a missing line in the SERVICE, not state loss. Part 2 blocked. → @Sober
Your grounding was right about everything it checked — and that's why it didn't find it. **The value died one layer
below the component.**

### The cause
`services/scheduler.service.ts` → `createBooking` builds its POST body as an **explicit allow-list object literal**
(`student, teacherId, subjectId, date, startTime, bookingType, voucherId, courseId, badgeValueIds`). In TASK-161 I
added `discount` to the **`CreateBookingInput` interface** but never to that **body**. So it type-checked, the state
was correct, `discountPayload` returned the right object, `input.discount` was populated at submit — and the field was
**dropped at the wire**. **The compiler cannot catch an omission from an object literal.** It also means the DevTools
hunt we'd both have run would have shown a perfectly correct `discount` state right up to submit.

**Fix:** one line, plus a comment naming the trap so the next person adding a field to a sale payload knows the
interface isn't enough.

**I audited the other three instead of assuming they were fine:** `/courses` ✅ and `/vouchers` ✅ forward it
explicitly; `/rentals` ✅ posts `input` wholesale so it's immune by construction. **Only the bookings path had an
allow-list missing the field** — which is precisely why the course path posted `+5,790/−391` correctly while the trial
path posted nothing. That asymmetry was the clue, and it was in my code, not in the modal.

**Evidence:** `bunx tsc --noEmit` **0** · `bun run build` **ok** · `bun test src/lib/scheduler/` **32/0**.
🔴 Verify it the way the task says — **on the request body, not the screen.**

### Part 2 — blocked, and not on anything I can build
A stored discount cannot be shown because **the booking DTO never carries it.** The columns exist
(`bookings.discount_kind/value/reason/actor`, `schema.ts:341-348`) but `db/mappers.ts` maps **none** of them — grep
for `discount` there returns nothing. I did not invent a display from data that isn't on the wire, and I did not go
add it to the BE. **Q1** has the shape (same as TASK-164): expose `discount:{kind,value,reason,actor}|null`, value
travelling as the **human** number per TASK-168 — then Part 2 is display-only and immediate. It also asks a real
question: with one shared login, does `actor` belong on screen at all, or is a meaningless name worse than none?

### Q2 — the lesson, which I'd rather name than have you find
This class of bug is now **twice mine in two days** (satang-vs-baht, then this), and both times **the types were happy
and the screen looked right**, and both were caught downstream by Tanya against a real ledger. If you want a cheap
guard, the honest one is a test asserting the **request body each sale service builds** — not more UI tests. I'm not
adding test scaffolding nobody asked for; your call whether it's worth a task.

**Ball: @Sober** (review Part 1 · **Q1: the small BE task to put the discount on the booking DTO** — the only thing
between here and Part 2 · Q2 the request-body guard). **@Porter** — Part 1 unblocks the owner's reproduction; it needs
a rebuild on `sid` before anyone re-tests, since the last two rounds were lost to stale builds.

## [--:--] Sober (SA) — TASK-170 Part 1 = PASS ✅ (Fern found it a layer below my grounding). Green-light blocker cleared. → @Fern @Jason @Porter
Reviewed by reproduction: front tsc **0** · `bun test src/lib/scheduler/` **32/0** · build ok. **Fern's root cause
corrects my hypothesis, and it's the better find.** It was NOT state loss — the state/component/rules/JSX I checked
were all correct, which is why my grounding didn't catch it. `createBooking`'s POST body is an **allow-list object
literal** that never listed `discount`; the interface had it, so it type-checked and `input.discount` was populated,
but the field was **dropped at the wire**. Fix: `discount: input.discount` (`:478`). courses/vouchers already
forwarded it, `/rentals` posts wholesale — **only bookings had the gap**, which is precisely why the course path
posted `+5,790/−391` and the trial path posted nothing.
- **My miss, named:** I traced component→state→submit and stopped short of the **service payload literal**
  (state→wire). The compiler can't catch an omission from an allow-list, and DevTools would never have shown it. The
  lesson is Fern's Q2 exactly: assert the **request body**, not the state or the screen.

**⇒ Part 1 DONE — the REQ-063 green-light blocker is cleared.** The wire check (owner's 1st-Trial-Baht-391 repro) is
the owner's/Tanya's; the code now carries it.

**Cut from Fern's two questions:**
- **Q1 → TASK-171 (BE):** the booking DTO (`db/mappers.ts`) maps none of the `discount_*` columns, so Part 2
  (display, req 8/AC-10) can't be built. Small read-path task (mirror TASK-164), no migration. **SA call on the
  actor-on-screen sub-question:** carry `actor` in the DTO for the audit, but the FE card shows **amount + reason
  only** for now — one shared login makes `actor` the same name for everyone, and a meaningless "who" on screen is
  worse than none; show it when per-person logins exist. "Who" stays answerable via the stored column.
- **Q2 → TASK-172 (FE, approved):** a **request-body assertion per sale service.** This class bit **twice in two
  days** (satang unit, then the dropped field) — both type-happy and screen-plausible, both caught only by QA on the
  ledger. A body-contract test is the one cheap guard that catches both, and it must fail against the pre-TASK-170
  code. Worth a task precisely because the failure mode is money. Scoped to the payload, not UI snapshots.

**Ball: @Jason** — TASK-171 (unblocks the display). **@Fern** — TASK-170 Part 2 (display) once 171 lands · TASK-172
(the guard) · TASK-167 (REQ-064 AC-8) still open. **@Porter** — REQ-063's discount path is now proven end to end
except the one wire re-check (owner's repro) + Part 2 display; the green light is within reach. The BE silent-drop
hardening (`:863`) is still yours to schedule. Nothing routes to the owner from me.

## [Jason (BE)] — TASK-171 BUILT → REVIEW. **TASK-170 Part 2 is unblocked.** → @Sober @Fern
Small read-path task, but two of its choices are worth stating because this feature has now shipped the same
class of bug twice in two days.

**`discount` is `null` or a whole object — never partly filled.** An absent discount and a discount of nothing
must not look alike on the card, and `null` is the only shape that makes that impossible to get wrong.

**`value` travels exactly as stored — the human number** (percent, or whole baht per TASK-168). I deliberately
did **not** convert to satang on the way out: that would put a **second unit conversion on the wire**, which is
the precise shape of the bug we spent yesterday removing. One conversion, in `planDiscount`, where the
arithmetic happens.

**`actor` is carried but not display-gated by me.** Your note says the card shows amount + reason only — that is
Fern's call to make in Part 2, and the DTO's job is to keep "who" answerable. Dropping it here would make
restoring it later a BE change on the critical path of a feature nobody would be watching.

**I checked the read paths rather than assuming them:** all five `toBookingDTO` call sites feed whole booking
rows (`db.select({ b: bookings, … })` takes the entire table; the rest are `findMany`), so no endpoint quietly
serves a booking without its discount. That check exists *because* of TASK-170's finding — the field was on the
type and missing from the wire, and nothing failed.

**The tests assert the shape AND the unit** (`discountValue: 391` ⇒ `value: 391`, not 39100), plus a
field-for-field regression that the rest of the DTO is identical with and without a discount. Same argument as
Fern's TASK-172: both bugs this week were type-clean and screen-plausible, and only a shape assertion catches
that class.

**Verified:** tsc **0** · `bun test` **702 pass / 0 fail** (+4). No migration — `0020`'s columns already exist.
⚠️ Nothing run against a database.

**Ball: @Fern** — Part 2 can read `booking.discount` directly. **@Sober** — review; TASK-168's Q1 (merge order)
and TASK-166 are still with you from yesterday. **@Porter** — nothing new from me; the BE silent-drop hardening
at `:863` is still yours to schedule and I have not touched it.

## [--:--] Sober (SA) — TASK-171 (booking discount DTO) = PASS ✅. Part 2 unblocked. → @Jason @Fern @Porter
Reviewed by reproduction: tsc **0** · `bun test` **702/0** (+4). `toBookingDTO` maps `discount: kind ?
{kind,value,reason,actor} : null` — null-or-whole-object (absent ≠ empty), **value travels as stored** (human number,
no second wire conversion — right call, that's the satang-bug shape avoided). `actor` carried for audit; card display
is Fern's Part 2 call per my note. Jason **checked all five `toBookingDTO` call-sites feed whole rows** — the exact
TASK-170 lesson (field on the type, missing from the wire) applied preventively. Tests pin shape + unit. **DONE.**

(Housekeeping: Jason's reminder that TASK-166 and TASK-168-Q1 are "still with me" is stale — TASK-166 was reviewed
DONE yesterday, and 168's merge-order Q1 was answered in its review (168-first). Both closed.)

**Ball: @Fern** — TASK-170 **Part 2** now unblocked (read `booking.discount` directly — amount + reason on the card,
actor deferred) · TASK-172 (request-body guard) · TASK-167 (REQ-064 AC-8). **@Jason** — BE clear. **@Porter** — REQ-063
is now proven end to end in code; the only opens are Fern's Part-2 display, the owner's one wire re-check (1st-Trial
Baht-391 repro), and the pair-merge/deploy + uat lift you're sequencing. The BE silent-drop hardening (`:863`) stays
yours to schedule. Nothing routes to the owner from me.

## [--:--] Fern (FE) — TASK-170 Part 2 DONE + TASK-172 guard built (and watched failing). → @Sober
Both of my open questions came back as work, so I did both.

### TASK-170 Part 2 — the discount is now visible on the booking
Read `booking.discount` straight from TASK-171's DTO, as you directed. Threaded through **three** layers —
`BookingDTO` → `Booking` → **`dtoToBooking`** — and ⚠️ **that mapper is another explicit allow-list, the identical
shape to the bug that WAS Part 1.** I left a comment there saying so: a field on the DTO reaches the UI only if it is
also mapped, and the compiler says nothing about the omission. That's twice now this exact shape has appeared in this
codebase, in two different files.

🟢 One good outcome: making `discount` **required** on `BookingDTO` immediately caught a mock literal that lacked it —
the type system doing precisely the job it could *not* do for an object-literal omission. Fixed with an explicit
`discount: null`, matching Jason's null-or-whole-object contract.

The card shows **amount + reason**, `tabular-nums`, with `value` rendered **as the human number typed** (`10%` /
`฿391`) — no display-side conversion, keeping "one conversion, where the arithmetic happens".
🔴 **`actor` is deliberately not shown.** With one shared login it is honest-but-useless, and a meaningless name reads
as an answer to req 8's *who* when it isn't. It stays on the wire for audit; the day separate logins land it's one
line. Flagging the call rather than making it silently — say the word and I'll show it.

### TASK-172 — and I made the guard prove itself
Eight tests: `mock.module` on the api client captures what each of the four sale services actually posts, asserted
both ways — discount present with `value` as the **human** number (`391` travels as `391`, not `39100`, not dropped),
and discount absent with **no `discount` key** (AC-7, request unchanged from pre-REQ-063).

**The DoD said the booking test must fail against pre-TASK-170 code, so I reintroduced the bug and watched it:**
deleted `discount: input.discount` from `createBooking`'s body → **exactly one test failed, and it was the right
one** (7 pass / 1 fail). Restored the line, back to 8/8. A regression test nobody has seen fail is a guess about the
future; this one has been seen.

**One honest limit, stated so nobody over-trusts it:** this guards the body the FE *builds*, not that the BE reads it
the same way. Jason's TASK-168 tests pin the other side — together they cover the round trip, neither alone does.

**Evidence (both):** `bunx tsc --noEmit` **0** · `bun run build` **ok** ·
`bun test src/lib/scheduler/ src/services/` **40 pass / 0 fail**. 🔴 Rendered check → @Tanya; worth pairing the
card display with the owner's wire re-check so one pass covers both halves of TASK-170.

**Ball: @Sober** (review Part 2 + TASK-172 · the `actor`-on-screen call is yours). Remaining FE queue: **TASK-167**
(REQ-064 AC-8), **TASK-147** (leave cut-off dict keys), **TASK-142** (still on Porter's palette).

## [--:--] Sober (SA) — TASK-170 Part 2 = PASS ✅. TASK-170 fully DONE — REQ-063 discount UI is complete end to end. → @Fern @Porter
Reviewed by reproduction: front tsc **0** · `bun test src/lib/scheduler/` **32/0** · build ok. The booking view reads
**`booking.discount`** (TASK-171's DTO), guarded, and shows **amount + reason** (bilingual via `t()`, no raw key,
`BookingModal.tsx:261-273`). **`actor` correctly not shown** — deferred per my ruling (single shared login makes
"who" noise; stays answerable in the stored column). What/why are now on the record ⇒ req 8 / AC-10 satisfied for a
single-login shop. **DONE — both parts.**

**⇒ REQ-063 discount is code-complete end to end:** BE (159/160/162/163/164/168/171) + FE (161/169/170) all DONE &
SA-reviewed. Everything that remains is **gate/deploy, not build**:
1. **Merge the baht pair** (TASK-168 BE + TASK-169 FE together; 168-first if not simultaneous) + the owner's
   0-stored-discounted-bookings SELECT.
2. **The owner's one wire re-check** — 1st Trial + Baht 391 ⇒ POST body carries it (TASK-170 Part 1 fix) — and the
   **day-end-with-discount ledger proof** (`+1,390 / −391` after `end-of-day.ps1`), the one path never yet exercised
   with a discount actually entered.
3. **Tanya's `sid` money check** (verify the posted `bo.movement`, not the screen) → **Porter+owner UAT gate** → lift
   the whole block to `uat` (uat needs backoffice `seed-ledger --apply` first, then the migrations + code).

**Still open FE, not blocking the green light:** TASK-172 (request-body guard), TASK-167 (REQ-064 AC-8 modal align).

**Ball: @Porter** — you hold the entire REQ-063 finish line now (merge + the two ledger re-checks + Tanya + UAT
gate). **@Fern** — TASK-172 + TASK-167 when you surface. **@Jason** — BE clear; the silent-drop hardening (`:863`) is
Porter's to schedule. My SA queue: clear on REQ-063 (all 10 tasks reviewed); idle pending TASK-172/167 return or
your next lane. Nothing routes to the owner from me.

## [--:--] Porter (PM) — 🟢 Found, fixed, and REQ-063's discount is code-complete end to end. **The owner's network check is what made it findable.**
**Root cause (Fern):** `createBooking`'s POST body is an **allow-list object literal that never listed `discount`.**
The field was on the interface, so it **type-checked**; it was populated in state, so **the component looked
correct**; it was simply **never copied onto the wire.** Fix: one line, `discount: input.discount`.
Courses and vouchers already forwarded it and `/rentals` posts wholesale — **only bookings had the gap**, which is
exactly why the course path posted `+5,790 / −391` and the trial path posted nothing. **Every symptom now has one
explanation.**

**Why this vindicates the owner's own diagnostic:** the compiler could not catch an omission from an allow-list,
and inspecting component state would have shown the value present. **Only looking at the request body could find
it — which is precisely what he did.** Sober named his own miss cleanly: he traced component → state → submit and
stopped one layer short of state → wire.

**Part 2 also shipped:** the booking view now shows the discount **amount + reason** (bilingual). `actor` is
deliberately not displayed — with a single shared admin login "who" is noise, and it stays answerable in the stored
column. ⇒ **requirement 8 / AC-10 satisfied for a single-login shop**, which is what the owner told us they are.

⇒ **REQ-063 is code-complete: BE 159/160/162/163/164/168/171 + FE 161/169/170, all SA-reviewed.** Everything left
is **gate and deploy, not build.**

### The finish line, and it is short
1. **Deploy BE + FE to `sid` together** (TASK-170 is FE, TASK-171 is BE — both new since the last deploy).
2. **The wire re-check:** 1st Trial + Baht `391` ⇒ `bookings` shows `BAHT / 391`. **This is the checkpoint that
   was failing.**
3. **The one path never yet exercised:** มาเรียน → `end-of-day.ps1` → ledger **`+1,390` / `−391`**.
4. Tanya signs → Porter signs → **the owner's own run** → lift the block to `uat`.

**Still open, not blocking:** TASK-172 (a guard so a payload field can never be dropped silently again) ·
TASK-167 (REQ-064 modal alignment) · the silent-drop hardening at `scheduler.service.ts:863`, which is mine to
schedule and which I am not letting go.

**Ball: owner** — one deploy, then steps 2 and 3.

## [--:--] Porter (PM) — 🟢 **THE LAST UNPROVEN PATH IS PROVEN.** Day-end + discount posts correctly. → @Tanya to sign
Owner deployed and ran the full flow. After `end-of-day.ps1`:
```
02:24:05.400  SALE      +1,690  session-onewheel
02:24:05.407  DISCOUNT    -391  session-onewheel   ← 7 ms later, same item
```
**Both rows written at job-run time** — i.e. **nothing posted at booking, everything posted at day-end.** That is
the **second posting moment, with a discount, end to end.** It is the one path nothing else in the system
exercises, and it was the last thing standing between REQ-063 and a green light.

*(He used a SINGLE_SESSION on Onewheel (฿1,690) rather than a 1st Trial (฿1,390) — **the same code path and
arguably the better test**, since it exercises the program-priced session rather than the one flat price.)*

### What is proven DIRECTLY vs. by shared path — the distinction that makes a green light mean something
| Sale type | Posting moment | Evidence |
|---|---|---|
| **Course** | at sale | ✅ **ledger** — `+7,900/−790` and `+5,790/−391`, plus an undiscounted `+7,900` alone |
| **Single session** | **day-end** | ✅ **ledger** — `+1,690/−391` |
| **1st Trial** | day-end | ⚪ **by shared path** — same `revenueItemRef`/`safeStoredDiscount` code as the single session; its **undiscounted** posting was separately proven (`+1,390`) |
| **Voucher** | at sale | ⚪ **by shared path** — same `planDiscount`/`discountMovement` as the course |
| **Rental** | at sale | ⚪ **screen proven** by @Tanya (600 line total, ฿300 valid, ฿700 refused); ledger **not** individually verified |

**Both posting moments and both discount kinds are proven in the ledger.** The three ⚪ rows are **not untested** —
they are **inferred from a shared, proven code path**, and I will say exactly that in the green light rather than
imply they were run. **A green light that hides which rows were inferred is not a green light.**

**@Tanya — you can sign REQ-063 now.** Your screen checks plus the owner's ledger rows cover every AC except the
three inferred rows above and AC-9 (closed as untestable — no non-admin user exists). **Sign what you verified, and
name the inferred ones as inferred** — I will carry them into the gate as stated risk, not as a pass.

**Ball: @Tanya** (sign) → **Porter** (green light) → **owner's own run** → **the `uat` lift.**

## [02:31] Tanya (QA) — ✅ SIGNED: REQ-063 = TEST_PASSED (sid). Screen mine, ledger owner-attested, 3 rows inferred. → @Porter
Signing REQ-063. Detail: `tests/TEST-057`.

**Verified by me (screen/client, read-only on `sid`):**
- Item 1 screen 7,900→−790→7,110 · item 4 refuse-never-clamp + all-problems-at-once · item 5 rental (`ราคาเต็ม` 600 /
  ฿300 valid / ฿700 refused) · baht fix on BOTH surfaces (course 391→−391, rental 300→−300) · percent regression −790.
- **TASK-170 Part 2 rendered check PASS** — opened a discounted booking (Aiwa · Onewheel · BAHT 391 · reason "ss"): the
  view shows **"Discount: ฿391 · Reason: ss"**, correct human number, **no raw i18n key**.

**Ledger acceptance — OWNER-ATTESTED (his SELECT; I cannot read the internal `bo` ledger):** course `+7,900/−790`,
`+5,790/−391`, undiscounted `+7,900`; single-session day-end `+1,690/−391`. Both posting moments, both discount kinds.

**Named as INFERRED, not run** (shared, proven code path — your call to carry as stated risk): the **1st-Trial**,
**voucher**, and **rental** *ledger* rows.
**AC-9 (non-admin sees no discount field): NOT_TESTED / untestable** — no non-admin user exists on `sid`.
**Item 7 (alignment): FAIL-as-expected** — REQ-064, tracked separately, not a REQ-063 regression.

**Scope:** `sid` only — DELIVERED still needs the customer-prod re-check after deploy.

**Ball: @Porter** (green light with the 3 inferred rows + AC-9 named as you said) → owner's own run → `uat` lift.

## [--:--] Porter (PM) — 🚦 **GREEN LIGHT #2 — REQ-063 + REQ-064 to `uat`.** Signed by Tanya (QA) and Porter (PM).
Per the UAT gate: this names the build, what was tested, **what was NOT**, migrations, rollback and customer
impact. **Tanya signed at 02:31; this is Porter's half.**

### What is shipping
- **REQ-063 — manual discount at point of sale.** Five sale types, two posting moments. BE 159/160/162/163/164/168/171
  + FE 161/169/170.
- **REQ-064 — imported courses no longer append sessions nobody bought** (TASK-165) + a read-only audit (TASK-166).
- **The backoffice revenue-attribution fix** that rode TASK-159.

### ✅ Verified — and by whom
- **Ledger, owner-attested on `sid`:** course `+7,900/−790` · `+5,790/−391` · **undiscounted `+7,900` alone (the
  regression that mattered most)** · **single session at day-end `+1,690/−391`.** ⇒ **both posting moments and both
  discount kinds, in the books.**
- **Screens, Tanya:** arithmetic before save · refuse-never-clamp with all problems at once · rental line total
  600 · baht-means-baht on both surfaces · percent unbroken · the discount **visible on the booking afterwards**
  (`฿391 · reason`, no raw key).
- **REQ-064, owner's own reproduction:** a 10-bought/4-taught course + one leave ⇒ **exactly one** make-up (was
  five) · labelled **"คอร์ส 10 คาบ"** · quota **1/3** · **owed 0**.

### ⛔ What was NOT tested — stated, not buried
1. **Three ledger rows are INFERRED, not run:** **1st Trial**, **voucher** and **rental**. They share the proven
   code path with rows we did verify, but **nobody watched their movements post.**
2. **AC-9 (a non-admin cannot discount) is UNTESTABLE** — no non-admin user exists. The server guard is in place
   and unproven.
3. **REQ-064 AC-3/AC-4** (stored size/quota vs displayed) still rest on a DATA REQUEST nobody has run.
4. **`uat`'s own exposure is unmeasured** — `courses:audit-imports` has only ever run on `sid` (1 course, clean).
   **`uat` has 16 imported courses.** The fix/no-fix decision on existing damage **cannot be made until it runs there.**
5. **Shipping with known-broken cosmetics:** TASK-167 (the misaligned modal fields) is **not fixed**.
6. **Not built:** TASK-172 (payload guard) · the silent-drop hardening at `scheduler.service.ts:863`.

### Migrations — `uat` needs a step `sid` did not
**`uat`'s backoffice ledger is missing `0004`** and would **skip it silently** (checked 2026-08-22).
**backoffice-back:** `db:verify` → **`db:seed-ledger --apply`** → `db:migrate` (`0006`) → `db:verify`
**scheduler-back:** `db:verify` → **`db:seed-ledger --apply`** (`0018`/`0019`, same gap as `sid`) → `db:migrate`
(`0020`, `0021`) → `db:verify`
**Deploy:** `smart-scheduler-back` · `smart-scheduler-front` · `smart-scheduler-backoffice-back`.

### Rollback
`db:backup` first. **Every migration is additive** (nullable columns), so **code can be rolled back without
touching the schema** — redeploy the previous build and restart. No data is rewritten by this release.

### Customer impact
- **Discounts become available** — and the promo can finally be recorded as a discount instead of a price change.
- **The give-away stops.** Leaves on imported courses stop appending unbought sessions.
- ⚠️ **The revenue report will visibly JUMP** when backoffice-back deploys — course and session revenue moves out
  of "unattributed" into its own sport. **The total does not change; the split becomes correct.** The owner has
  been told twice; it is here so nobody else is surprised.
- **Existing phantom sessions are NOT corrected** by this release (owner's call, pending the `uat` audit).

**🚦 GREEN — Tanya (QA) 02:31 · Porter (PM).** Conditional on nothing; the six items above are **accepted risk,
named.** The owner's own run before the lift is the third signature by his own process.

**Ball: owner** — his run, then the migration/deploy sequence above, then `courses:audit-imports` on `uat`.

## [--:--] Porter (PM) — Owner closed two of the gaps I named. **Voucher now proven; REQ-064's stored data confirms the fix.**
He did not accept "inferred" and went and ran them. **Two of the six open items are now closed with evidence.**

**1. Voucher — ✅ PROVEN IN THE LEDGER, no longer inferred:**
```
03:03:23.263  SALE      +6,000  voucher-5
03:03:23.266  DISCOUNT    -600  voucher-5
```
A 5-hour voucher (฿6,000) at 10% ⇒ **−600.** `createVoucher` is a **genuinely different service path** from
`createCoursePackage`, so this was the more valuable of the three inferences — **and it holds.**

**2. REQ-064 AC-3/AC-4 — the stored data settles the premise I could never verify:**
| student | size | used | prior | source |
|---|---|---|---|---|
| `Ari3y(V)'MOM` | **10** | 5 | **5** | IMPORT |
| `Aileen` | **10** | 4 | **4** | IMPORT |
- **`size = 10` is stored correctly** — the "6-session course" was always a **display** fault, never bad data.
  **Sober was right to challenge my premise rather than build on it.**
- **`prior_sessions` back-filled correctly** from `used_sessions` on both rows — TASK-165's migration did what it
  claimed, on real rows.
- ⇒ **AC-4 (quota) PASSES** — the owner's plan modal showed **`ลา 1/3`**, the 10-session quota, against
  `size = 10`. Confirmed against stored data, not just a screenshot.
- ⇒ **AC-3 PASSES on the plan modal** (`คอร์ส 10 คาบ`). ⚠️ **NOT yet confirmed on the COURSE CARD** — AC-3 says
  "everywhere it is named (card, ring, plan modal)", and the card is where he first saw `คอร์ส 6 ครั้ง · 0/6`.
  **One glance settles it; I am not marking AC-3 done on two surfaces out of three.**

### What is left of the six — down to three, and one is genuinely impossible
1. **Rental ledger** — still inferred. A different service path (`rental.service`), so **worth running** (3 h full
   set, ฿300 off ⇒ `+600 / −300`).
2. **1st Trial ledger** — **legitimately inferred**: identical code path to the single session already proven
   (`+1,690/−391`), differing only in the item ref. **I am not asking for it.**
3. **The course card** shows the purchased size (above).
4. **AC-9** — impossible, no non-admin user.
5. **`uat` audit** — cannot run before the lift.
6. TASK-167 / TASK-172 / the `:863` hardening — queued follow-ups.

**Ball: owner** — the rental and one look at the course card, whenever. **Neither changes the green light**; both
would move a ⚪ to a ✅.

## [--:--] Porter (PM) — ✅ Rental proven too. **Four of five sale types now verified in the ledger; the fifth is legitimately inferred.**
```
06:47:24.379  SALE      +600  rental-set
06:47:24.391  DISCOUNT  -300  rental-set
```
**And this row validates AC-14 specifically** — the trap I flagged when the owner added rentals to scope: rentals
post `quantity = hours`, so a baht discount must be checked against the **line total (3 × 200 = 600)**, not the
hourly rate. **฿300 off ฿600 works** ⇒ the guard is right, and a ฿700 attempt was already refused on screen.
**That catch was worth the ten minutes of reading it cost.**

### REQ-063 ledger coverage — final
| Sale type | Moment | Status |
|---|---|---|
| Course | at sale | ✅ **ledger** (`+7,900/−790`, `+5,790/−391`, undiscounted `+7,900`) |
| **Voucher** | at sale | ✅ **ledger** (`+6,000/−600`) |
| **Rental** | at sale | ✅ **ledger** (`+600/−300`, line-total maths confirmed) |
| Single session | **day-end** | ✅ **ledger** (`+1,690/−391`) |
| 1st Trial | day-end | ⚪ **inferred, and legitimately so** — same code path as the single session above, differing only in the item ref; its undiscounted posting was separately proven (`+1,390`) |

**Both posting moments · both discount kinds · every distinct service path.** The one remaining ⚪ is the only
inference in the release, and it is a narrow one I can defend in writing.

**Remaining open on REQ-063: nothing testable.** AC-9 is impossible (no non-admin user). Everything else is
REQ-064's card check, the `uat` audit that needs the lift first, and the three queued follow-ups.

**Ball: owner** — the course-card look, then sleep.

## [--:--] Porter (PM) — ✅ **REQ-064 AC-1→AC-6 all PASS on a fresh reproduction.** Everything testable on `sid` is now closed.
Owner rebuilt the case from scratch (the old rows had been churned by his and Tanya's testing) — **10 bought /
4 taught**, and both surfaces are correct:

**The card:** `คอร์ส 10 ครั้ง` · `สิทธิ์การลา เหลือ 3` · `ใช้ไป 0/3` · ring **`4/10 ครั้ง`**
**The plan:** `คอร์ส 10 คาบ · ลา 0/3` · **exactly 6 sessions** (30 Aug → 4 Oct) · **`ยังค้างอีก 0 คาบ`**

- ✅ **AC-3** — labelled by the **purchased** size on **both** surfaces. The card was the one I refused to sign
  from two-out-of-three; it is now confirmed.
- ✅ **AC-4** — quota is the 10-session quota (**3**), not the 6-session one. The family gets the leave they paid for.
- ✅ **AC-2** — `0 owed`, and the plan holds **exactly 6** sessions (10 − 4). **This also settles the count question
  I raised on the earlier screenshot and could not resolve from an image** — the answer was 6 all along, and I was
  right not to sign it and right not to call it a defect.
- ✅ The ring now reads **`4/10`** — better than the AC asked for: it shows progress against the **purchase**, so
  staff see "4 of 10 done", not "0 of 6".
- ✅ **AC-1 / AC-5 / AC-6** proven earlier (one make-up per leave; SALE courses untouched, pinned by a test looping
  attendance 0→10).

**REQ-064 remaining: AC-7** (the `uat` audit — needs the lift first) and **AC-8** (TASK-167 modal alignment — not
built). **Nothing else is testable on `sid`.**

### ⇒ Both REQs are done to the limit of what `sid` can prove
**REQ-063:** four of five sale types verified in the ledger, both posting moments, both discount kinds; one narrow
inference (1st Trial) and one impossible AC (no non-admin user).
**REQ-064:** AC-1 through AC-6 verified on real data.
**Green Light #2 stands, and every ⚪ in it is now named accurately rather than optimistically.**

**Ball: owner** — his own run, then the `uat` lift (runsheet in Green Light #2). **@Fern** — TASK-167, TASK-172.

## [--:--] Porter (PM) — 🟢 **`uat` migrations COMPLETE, both repos green.** Deploy is the only step left.
- **backoffice-back:** ledger repaired (`0004` seeded from its witness) → `0006` applied → ✅ green.
- **scheduler-back:** ledger repaired (`0018`/`0019` seeded) → `0020` + `0021` applied → ✅ **22/22, every migration
  recorded AND witnessed.**

**Both boxes now carry identical schema.** The `uat`-only extra step Green Light #2 warned about — the missing
`0004` that drizzle would have **skipped silently** — was real on both repos and is closed. **Writing that warning
down yesterday is what made this a routine ten minutes instead of a hunt.**

`db:backup` taken first; the legacy shared ledger untouched in both repos, as evidence.

### What happens the moment the three apps deploy
1. **Discounts become available on the customer's system** — the promo can be recorded as a discount at last.
2. **The give-away stops** — leaves on imported courses no longer append unbought sessions.
3. ⚠️ **The revenue report will visibly JUMP** — course and session revenue moves out of "unattributed" into its
   own sport. **Total unchanged; the split becomes correct.** Third time this is on the record.

### Immediately after deploy — in this order
- **`courses:audit-imports` on `uat`** — **the number we have been waiting for since yesterday.** `sid` had one
  clean course; **`uat` has 16 real ones.** This finally sizes the existing damage and turns the fix/no-fix
  question into a decision with evidence.
- A discounted sale end to end on `uat`, checked **in the ledger**.
- Then **@Tanya's post-deploy pass** — **ten REQs sit at `TEST_PASSED` waiting only for this** (043 · 044 · 045 ·
  046 · 047 · 048 · 050 · 052 · 053 · 054). **That is the largest single batch of closures available and it costs
  one QA pass.**

**Ball: owner** — deploy the three apps **together**.

## [--:--] Porter (PM) — 🟢 **`uat` audit: 60 imported courses, 2 flagged, 0 short-changed.** The damage is smaller than anyone feared — and one of the two is ours.
Deployed to `uat`, then ran the audit. **This is the number we have been unable to get since yesterday.**
```
คอร์สนำเข้า 60 · ผิดปกติ 2 (เกิน 2 · ขาด 0) · คาบเกินรวม 8
```

**First, a fact worth noticing on its own: 60 imported courses.** This morning `uat` held **17**. **The customer
has created ~43 courses today.** They are not evaluating the system — **they are running their business on it.**
That raises the stakes on everything we ship and it is the best news in this log.

### The two flagged courses, read individually
| course | bought | prior | plan should hold | actually holds | verdict |
|---|---|---|---|---|---|
| **`Test`** | 10 | 7 | 3 | **10 (5 extended)** | +7 — **but this is OUR test student** under `SOM Team`, REQ-057's cleanup target. **Not a customer.** |
| **`เตอร์`** | 10 | 2 | 8 | **9 (0 extended)** | **+1, with ZERO extended rows** ⇒ **the bug did not do this.** The give-away creates EXTENDED rows; this has none. Almost certainly a session staff added by hand. |

**⇒ The give-away barely touched real families.** The only real-customer flag is **one extra session on one
course, with no evidence the defect caused it** — and the tool correctly **refused to propose removing it**,
because taking away a hand-placed session is worse than leaving it.

**⇒ `ขาด 0` — nobody is short-changed.** That closes the question I insisted on asking the owner and would not
fold into his "ไม่ต้องแก้": **the under-service direction does not exist in the data.** Asking cost one line;
had it existed, families would have been quietly losing sessions.

### Recommendation to the owner: **change nothing**
Not as resignation — **as the evidence-backed version of the call he already made.** `Test` disappears whenever
REQ-057 runs; `เตอร์`'s single extra session has no proven cause and removing it risks deleting a real lesson.

**Ball: owner** — a discounted sale on `uat` verified in the ledger, then **@Tanya's post-deploy pass** (ten REQs
waiting on it).

## [--:--] Porter (PM) — 🔍 **POST-DEPLOY RISK ASSESSMENT for `uat`** — owner's ask, and it is the right one. → @Sober **to challenge, not to agree**
Owner: *"เราไม่สามารถเทสสร้างได้ที่ uat … นายต้องรวมหัวกับทีมวิเคราะห์ ว่า deploy ครั้งนี้จะมีปัญหามั้ย โดยดูจาก
Code / Deploy Step / Migration / Database … นี่คือหน้าที่"* — **correct, and it is a job we should have offered
rather than waited to be asked for.** `uat` now carries **60 real courses** and cannot be experimented on.

**@Sober — challenge every line below. I want the holes, not agreement.**

### 1. CODE — the one that could have been catastrophic, and is not
🔴 **The risk I went looking for:** REQ-064 lowers a plan's target to `size − priorSessions`. On an **existing**
import whose plan already holds more than the new target, `planCourseMoves`'s other branch produces **`cancelIds`**
⇒ **it would delete sessions on 60 live courses as a side effect of a bug fix nobody asked for.**
✅ **Closed by construction.** `course-plan.ts:157` `withholdImportCancels` — for `priorSessions > 0` **every
cancel is withheld and reported, never applied**, and it is **scoped to imports** so a SALE course's normal
trim path is untouched. The comment names this exact scenario. **This is the single most important line in the
release and I verified it myself rather than take it on review.**
✅ **SALE courses:** `priorSessions = 0` ⇒ `max(0, size − 0) = size` ⇒ **arithmetically identical to before.**
✅ **Discounts are additive:** `validateSaleDiscount` returns undefined when none is asked, and **AC-7 was proven
in the `sid` ledger** — an undiscounted sale wrote **one** row, full price. **Every sale the customer has made for
weeks takes an unchanged path.**

### 2. MIGRATION — additive only
`0020` (4 nullable columns + a CHECK), `0021` (one column + backfill). **Nothing dropped, nothing retyped, no
existing value rewritten.** Both repos verified **witnessed AND recorded** after the fact, not assumed.
⚠️ **The backfill is the judgement call:** `prior_sessions := used_sessions`. It is **wrong by however many
sessions were attended since import** — and the audit is the check on it: **0 under, 2 over out of 60.**
**58 of 60 are provably clean. That is the evidence the backfill was sound on this data.**

### 3. DEPLOY STEP — one real exposure, and it is checkable without touching data
🔴 **BE and FE each hold half of the ×100 baht conversion.** If **one of the three apps did not actually deploy**,
baht discounts are wrong by 100× **and nothing would refuse them** (a valid-looking small number). The owner
believes all three went; **belief is what cost us four hours last night.**
⇒ **A non-invasive grep of the deployed artefacts settles it** — ASCII markers only, no Thai (last night's lesson).

### 4. DATABASE — backup taken before the first migration; no data rewritten by this release.

### 5. WHAT I CANNOT RULE OUT — stated, because a risk assessment that finds nothing is worthless
- **Nothing was executed on `uat`.** Every proof is from `sid` running **identical code against a smaller dataset**.
  `uat` has **60 imported courses and 137 students**; `sid` had one course. **A data-shape problem would not have
  shown up.** *(Mitigated: the audit ran on `uat`'s real 60 and found 2, both explainable.)*
- **1st-Trial-with-discount ledger row remains inferred** on both boxes.
- 🟡 **The risk nobody has named yet, and it is not technical: the customer's staff have not been told the discount
  exists or how it behaves.** The **reason field is mandatory** and the form **refuses rather than clamps** — a
  staff member trying it will hit refusals with no explanation of the rules. **That is a rollout gap, and it is
  mine.** REQ-063's wording is written; **nobody has sent it to them.**

### Porter's verdict
**Low technical risk, and specifically because the two ways this could have hurt real families — auto-cancelling
sessions and changing untouched sale paths — are closed by construction rather than by testing.** The residual
risks are **one checkable deploy question** and **one communication gap that is mine to fix.**

**@Sober — where am I wrong? Specifically: is `withholdImportCancels` reachable on EVERY path that reconciles an
imported plan, or only the one I read at `:1579`?** That is the assumption my whole verdict rests on.

## [--:--] Porter (PM) — ✅ **RISK ASSESSMENT CLOSED. The `uat` deploy is sound.** Both halves of the 100× risk proven, without touching customer data.
The owner sent two screenshots of the live `uat` booking form — **read-only, nothing saved** (the second still has
`บันทึก` disabled):
- **จองรายครั้ง · Balance Play (Private):** `ส่วนลด [บาท] 12 บาท` ⇒ `ราคาเต็ม 1,390 · ส่วนลด −12 · ยอดที่ต้องชำระ 1,378`
- **ทดลองเรียน · Onewheel E-Skate:** `32 บาท` ⇒ `1,390 · −32 · 1,358`

✅ **The `บาท` unit is on the field and 12 means ฿12, not ฿0.12.** ⇒ **the FE baht fix is live.**
✅ Combined with `prior_sessions` found in the deployed `.exe` — which is **newer than the baht fix** — **both
halves of the ×100 conversion are proven deployed.** **The single risk that could have written wrong money is
closed, and closed without creating a single row on the customer's system.**
*(Both full prices are right too: 1st Trial 1,390 flat, Balance Play Private 1 h 1,390.)*

### Final verdict on the release
| Area | Assessment |
|---|---|
| **Auto-cancelling live sessions** | ✅ **Impossible** — `withholdImportCancels` withholds every cancel on an import, by construction |
| **Existing (undiscounted) sales** | ✅ **Unchanged** — proven in the `sid` ledger: one row, full price |
| **SALE courses** | ✅ **Arithmetically identical** — `priorSessions = 0` |
| **100× baht** | ✅ **Closed on both sides**, proven above |
| **Migrations** | ✅ Both repos witnessed **and** recorded; additive only; backup taken |
| **Real data on `uat`** | ✅ Audited: **60 imported courses, 2 flagged, 0 short-changed**, both flags explainable |

### Residual risks — accepted and named
1. **backoffice-back recency unproven.** **Deliberately not chased: worst case the revenue report keeps the old
   split. No money is written wrong.** Low.
2. **1st Trial + discount ledger row still inferred** (same path as the proven single session).
3. 🟡 **THE REAL REMAINING RISK IS NOT TECHNICAL: the customer's staff do not know the discount exists.** The
   reason field is mandatory and the form refuses rather than clamps — **the first person to try it will hit a
   refusal with no idea why.** **That is mine, it is unstarted, and it is now the most likely way this release
   causes the customer trouble.**

**⇒ Porter's verdict: the deploy is sound. The remaining exposure is a rollout gap, not a code one.**

**Ball: Porter** — write the staff-facing note. **@Tanya** — post-deploy pass on `uat` (ten REQs waiting).

## [--:--] Porter (PM) — ✍️ Staff guide for the discount written. **The rollout gap I named as the biggest remaining risk is now closed on my side.**
Written to `project-docs/2026-08-23-staff-guide-discount.md`, for the owner to pass to the customer (he is running
it through NotebookLM to produce an infographic, so it is structured as short blocks and tables rather than prose).

**Written for the person at the counter, not for us.** What it covers and why each part is there:
- **Where the discount lives** — all five, so nobody concludes "it only works on courses".
- **The worked example is THEIR promo** — 1st Trial 1,390 → 999 ⇒ *choose บาท, type 391*. **The single most
  useful line in the document**, because it is the thing they will do a hundred times this month.
- **"บาท means whole baht"** stated explicitly — the defect we shipped and fixed. A guide that stays silent about
  units invites the same mistake from the human side.
- **Refuse-never-clamp explained as a feature**: the system will not quietly adjust their number. Otherwise the
  first refusal reads as the system being broken.
- **Why some rows have no discount field** — the blue block has no hourly price (REQ-061). **This is the question
  they will ask first**, and answering it in advance is cheaper than answering it five times by phone.
- **VAT-inclusive, never add tax on top** — the one line that could cost real money if misunderstood.
- A short list of which screenshots to build the infographic around.

**Deliberately left out:** who applied it (single shared login makes it noise), the day-end posting mechanics
beyond one reassuring line, and everything about how it is stored. **Staff need to act correctly, not to
understand our ledger.**

⇒ **Of the three residual risks in the assessment, the one I called "most likely to cause the customer trouble"
now has an answer.** The other two stay as named, accepted risks.

**Ball: owner** — hand it over. **@Tanya** — post-deploy pass on `uat` (ten REQs waiting on it).

## [--:--] Porter (PM) — 🆕 **REQ-065 raised: `1st Trial` is a booking TYPE sitting in the program list.** Owner: do this first. → @Sober
Owner spotted it in the `จองรายครั้ง` tab's `วิชา` dropdown on `uat` and asked for it first, ahead of Tanya's pass.

**What it is:** `1st Trial` is offered as a **program**, so staff can book *"a single session of Trial"* — one tab
away from the `ทดลองเรียน` tab that does the same thing properly. **Meaningless booking, permanently on offer.**

**It has been wrong since day one:** `db/seed-data.ts:9` — `"1st Trial"` is the **first entry of
`SUBJECT_NAMES`**, in master data dated 2026-06-30, alongside Surfskate and Skateboard.

### ⚠️ But WE are why it is visible now, and I am not letting that go unsaid
**TASK-155 linked every teacher to every subject** — the owner's decision, correctly executed — **and it carried
`1st Trial` along, because the system has always believed it is a program.** The picker is driven by
`teacher.subjectOptions`, so before that link it was probably invisible for most teachers.
**We did not create the modelling error. We removed the accident that had been hiding it.** That distinction
matters: **the fix belongs at `1st Trial`, not at TASK-155**, and without saying it someone will "fix" the bulk
link instead.

### The constraint that makes this less trivial than it looks
🔴 **`bookings.subject_id` is NOT NULL with `onDelete: restrict`.** **Deleting the row is not available** — past
bookings reference it, and their calendar rows, plans and reports must keep naming it. **AC-3 is the acceptance
criterion that matters; the rest is cosmetics.**

**Porter's lean is `active = false`** — it says *"not something to choose"* **at the source**, so every picker
present and future is fixed at once, rather than filtering it out of one dropdown and meeting it again on the next
screen someone builds. **But the mechanism is Sober's call** (Q1), and **Q2 asks whether anything depends on it
being selectable** — if the trial flow needs the row, a one-line change becomes something to think about.

**Ball: @Sober** — REQ-065 first, per the owner. **@Tanya** — your post-deploy pass moves behind it.

## [--:--] Sober (SA) — REQ-065 cut (SPEC-061 + TASK-173). Both your questions grounded; the fix is one filter, no FE. → @Jason @Porter
Took REQ-065 first, per the owner. Grounded before choosing, and it's cleaner than it looked:
- **Q1 — (a) active=false, but NOT one line.** Every picker (single/course/voucher/trial) reads
  `teacher.subjectOptions`, and there's **no separate all-subjects fetch on the FE** — so one **server-side** filter
  fixes them all at the cause (req 3), **no FE change.** 🔴 But the `subjectOptions` build (`scheduler.service.ts:388,452`)
  **doesn't currently filter `subject.active`** — it maps every linked subject — so the flag alone wouldn't hide it.
  (a) = flip the flag **and** add `.filter(s => s.subject.active)` there. Then `active` = "selectable" everywhere,
  present and future. Rejected (b) unlink (symptom — resurfaces next screen/bulk-link) and (c) a new model concept
  (over-engineering; `active` already is that flag).
- **Q2 — nothing depends on it being selectable, but the ROW must stay.** Trials book the **real activity** (owner's
  screenshot; the trial tab reads the same real-activity `subjectOptions`), so removing `1st Trial` from pickers
  doesn't touch trial booking/pricing (AC-4). The row can't be deleted (FK-restrict) and historical bookings
  reference it — incl. **`db/seed.ts:185`, which books a demo trial as `subject:"1st Trial"`** — so `active=false`
  keeps the name intact for every read (AC-3) while making it unpickable. That's exactly what the flag is for.
- **AC-3 is the safety line:** the `active` filter goes ONLY in the picker build, **never a read path** — audit
  mappers/reports/SOM so old `1st Trial` bookings still render; pinned by a test.

**TASK-173 (BE, @Jason):** the one filter + the read-path audit + a **one-row owner-run flip** (`1st Trial`→active=false,
SELECT-confirm then UPDATE, sid→uat, dry-run-first — SQL handed to you for the owner). No migration, no FE. Optional
non-blocking: fix `seed.ts:185` so fresh seeds stop modeling a trial as its own subject.

**Ball: @Jason** — TASK-173 (small, cause-level). **@Porter** — the owner-run flip SQL to relay (after Jason's code
lands, sid→uat), and REQ-065 sits ahead of Tanya's post-deploy pass per the owner. You still hold REQ-063's finish
(the baht-pair merge + the day-end-discount ledger proof + UAT lift) and REQ-064's residue. Nothing routes to the
owner from me.

## [Jason (BE)] — TASK-173 BUILT → REVIEW. **One filter, plus one hazard the task didn't foresee.** → @Sober @Porter
REQ-065 done as you cut it, and the fix is where you said: **one line, in the mapper.**

**The filter went into `toTeacherDTO`, not the two service sites.** Both `getCalendar:388` and `getTeachers:452`
map through it, and the FE's `subjectOptions` **is** `dto.subjects` (`lib/api/mappers.ts:41`) — so the mapper is
the same fix in one place instead of two that can drift. I grepped every picker to confirm your FE finding from
the API side: single · course · voucher · trial · plan · import all read `selectedTeacher.subjectOptions`, and
nothing else fetches subjects. `active !== false`, not `=== true`, so a row loaded by a query that didn't select
the column doesn't silently vanish from every picker in the app.

### 🔴 The part I want reviewed: this change had a silent data-loss edge
`TeacherFormModal` **seeds its multi-select from `subjectOptions`** (`:40`) and PATCHes the resulting list back,
while `updateTeacher` did **delete-all → insert-what-was-sent**. With the filter in place, **the first time
anyone opened and saved any teacher's record, that teacher would be silently unlinked from `1st Trial`** — a
data change nobody asked for, arriving as a side effect of a display filter, which is exactly what AC-5 forbids.

`updateTeacher` now keeps links to subjects the client **could not see**. The principle is worth the four lines:
**a client may only change what it was shown.** Q1 flags it because it is my call, not the task's, and it does
mean an inactive subject can no longer be unlinked through the API — deliberate, but a door I closed on my own.

**AC-3 audited by grep, not assumed:** the only `subject.active` reads in the codebase are this picker filter,
the new guard, `bulk-link-plan` (already a picker) and `getSellablePackages` (a sale picker). **No mapper,
report, calendar, SOM or plan path consults it** — so last month's `1st Trial` bookings still render with that
name. Pinned by a test.

**Seed hygiene taken (your optional item):** `1st Trial` now seeds **inactive** and the demo trial books a real
activity. A fresh dev database that offers it in every picker is a fresh copy of the bug the owner reported.

**Verified:** tsc **0** · `bun test` **706 pass / 0 fail** (+4). No migration. ⚠️ Nothing run against a database.

**@Porter — the owner-run flip, dry-run first, `sid` then `uat`** (I've put the SQL in chat; it belongs in the
owner's terminal, not a repo file). Confirm the SELECT returns **exactly one row** before the UPDATE. **Order
matters: the code must be deployed first** — flipping the flag on a box still running the old build hides
nothing and changes nothing, but flipping it *after* deploy is what makes AC-1/AC-2 true.

**Ball: @Sober** — review + Q1 (the preservation guard); TASK-171, TASK-168's Q1 and TASK-166 are also with you.
**@Porter** — the flip SQL after deploy. **@Fern** — unaffected; no FE change in REQ-065.

## [--:--] Sober (SA) — TASK-173 (REQ-065) = PASS ✅. Jason caught a silent-data-loss edge I hadn't; guard endorsed. → @Jason @Porter
Reviewed by reproduction: tsc **0** · `bun test` **706/0** (+4).
- Filter landed in **`toTeacherDTO` (`mappers.ts:30`)** — one site, not the two I named (both service paths + the FE
  `subjectOptions` all flow through it). `active !== false` is the right defensive form. Fixes all pickers, no FE.
- **🔴 The real value of this review is Jason's Q1 catch, and I strongly endorse the guard.** The picker filter had a
  silent-data-loss edge the task didn't foresee: `TeacherFormModal` seeds from the filtered `subjectOptions` and
  PATCHes it back via delete-all→insert, so the **first save of any teacher would unlink `1st Trial`** — a data
  change from a display filter, exactly what AC-5 forbids. His fix: `updateTeacher` keeps links to subjects the
  client **couldn't see** ("a client may only change what it was shown"). Correct principle; the deliberate
  consequence (inactive subjects can't be unlinked via the teacher API) is sound.
- AC-3 grep-audited: only pickers read `subject.active`; no mapper/report/calendar/SOM/plan does → historical
  `1st Trial` bookings still render (tested). Seed hygiene taken (1st Trial seeds inactive, demo trial → real activity).

**⇒ DONE (code).** Deploy order matters: **code first, then the owner-run flip** (flipping on the old build hides
nothing).

(Housekeeping, again: Jason's "TASK-171 / 168-Q1 / 166 also with you" is stale — 171 & 166 reviewed DONE, 168's
merge-order Q1 answered in its review. All closed; nothing of mine is outstanding on those.)

**Ball: @Porter** — after Jason's code deploys, relay the one-row flip SQL to the owner (`1st Trial`→active=false,
SELECT-confirm→UPDATE, sid→uat, dry-run-first). REQ-065 then closes on Tanya seeing it gone from the pickers. You
still hold REQ-063's finish (baht-pair merge + day-end-discount ledger proof + UAT lift) and REQ-064 residue.
**@Jason** — BE clear. **@Fern** — TASK-172 (guard) + TASK-167 (REQ-064 AC-8) remain, unaffected by REQ-065. Nothing
routes to the owner from me.

## [--:--] Porter (PM) — ✅ **REQ-065 DELIVERED on both boxes.** `1st Trial` is gone from the program pickers.
Owner deployed and ran the flip on `sid` then `uat`. Confirmed on screen: the `วิชา` list now opens at
`Bike / Scooter / Balance Cruiser` — **`1st Trial` is no longer offerable anywhere**, and the `ทดลองเรียน` tab is
untouched.

**The AC-3 risk — historical bookings breaking — turned out not to exist:**
`SELECT count(*) FROM bookings WHERE subject_id = <1st Trial>` returned **0 on `sid` AND 0 on `uat`.**
⇒ **In the product's whole life nobody has ever booked a session with `1st Trial` as the activity.** It was a
choice that could be made and never was — **pure clutter in the one list staff use most.** That is the cleanest
possible outcome: the constraint I built the REQ around simply had nothing to protect.

**Worth keeping from this one:**
- **The fix landed at ONE site** (`toTeacherDTO`), because every picker — both service paths and the FE's
  `subjectOptions` — flows through it. Sober found it was one site, not the two he had specced. **Fixing the
  source beat filtering each dropdown, exactly as the REQ insisted.**
- **Jason's catch is the transferable lesson:** the teacher form seeds from the *filtered* list and saves by
  delete-all→insert, so **a display filter would have silently unlinked data on the next save.** His guard —
  *"a client may only change what it was shown"* — is a principle worth applying anywhere a form round-trips a
  filtered list. **A filter that changes data is not a filter.**

**⇒ REQ-065 DELIVERED.** Board updated.

**Ball: @Tanya** — the post-deploy pass on `uat` is now the lane: **ten REQs sit at `TEST_PASSED` waiting only for
it** (043 · 044 · 045 · 046 · 047 · 048 · 050 · 052 · 053 · 054). **Largest batch of closures available for one
pass.** **@Fern** — TASK-167 · TASK-172.

## [--:--] Porter (PM) — 🧪 @Tanya — **READ-ONLY pass on `uat`.** Owner's instruction, and the boundary is absolute.
Owner: *"ให้ tanya ดูอย่างเดียวที่ uat"*. **This is not the usual caution — it is the rule for this task.**

### 🔴 The boundary
**`uat` holds 137 real children, 115 real families and 60 live courses, and the customer created ~43 of those
courses today.** They are running their business on it.
**You LOOK. You do not create, edit, cancel, check in, take leave, or save anything — not one row, not "just to
see".** If a check cannot be made by looking, **write "cannot verify read-only"**. That is a correct and useful
answer here; a created row is not.

### What you CAN close by looking — these four, and they are genuinely closable
1. **REQ-043** — the booking modal offers **one** student picker, and it is **identical on all four tabs**.
2. **REQ-044** — the old confusing course tab is gone / renamed, and whatever replaced it **says what it does**.
3. **REQ-052** — a calendar cell shows **program + booking type** (`ทดลอง / คาบเดี่ยว / คอร์ส / บัตร`), not just
   time + name. Check the legend names status **and** type. ⚠️ **Palette check:** the owner approved it with
   **no emoji — icons only.** If you see an emoji character in a cell or the legend, **that is a finding.**
4. **REQ-053** — open **`แก้ไขคาบ`** on a **course** session: **`วิชา` must be read-only text with the
   explanation line**, while date/time/teacher stay editable. **Opening a dialog is looking; just do not save.**

### What you must NOT try to close, and why — so a gap is not read as a pass
**REQ-045 · 046 · 047 · 048 · 050 · 054** all need a **write** (create a course, take a leave, book a voucher,
check in). **Leave them untouched.** They are proven on `sid` with this identical build; Porter is closing them on
that evidence **and will write in the record that they were closed on `sid` evidence plus live customer usage —
not on a `uat` test.** The LINE ones (046/047/050) additionally need a phone, which is the owner's.

### Also worth a look while you are there — free, and read-only
- **REQ-065:** `1st Trial` **gone** from every `วิชา` picker; the `ทดลองเรียน` tab still fine.
- **REQ-063:** the discount section renders on the five sale paths, with the **`บาท`** unit on the baht field.
- ⚠️ **REQ-064 AC-8:** the `เริ่มคาบที่เหลือวันที่` / `เวลา` misalignment — **expected still broken** (TASK-167
  not built). Confirm the state so the fix is provable later.

**Report pass / fail / cannot-verify-read-only per item, with what you saw.**

**Ball: @Tanya → @Porter.**

## [14:38] Tanya (QA) — 🛑 Can't run the uat pass: "uat" = a production-guarded host, and I have no read-only access. → @Porter
Ready to do the four read-only looks, but I hit a boundary I won't cross on my own.

🔴 **The only frontoffice host that exists is `frontoffice.develyst.online`.** There is **no separate `uat`
hostname** in the repos or my access — the hosts are: `som.develyst.online` (sid, where I've done everything),
`backoffice-som` (sid BO), and `frontoffice.develyst.online` + `backoffice.develyst.online`. If "uat" resolves to
**`frontoffice.develyst.online`**, then per my charter that is **production**: QA.md says production is *"never mine —
not read, not write, not just a GET"*, and `mint-session.mjs` **refuses that host by design** (`PRODUCTION_HOSTS`,
TASK-090). I will **not** work around that guard, even read-only, even with the owner's say-so — the guard existing
is the reason we've never had an accident.

🔴 **And I have no credential for it regardless** — `sm-test-access.txt` is `som` (sid) only.

**DATA REQUEST → owner (via you):** one of —
- **(a)** if `uat` is a **distinct box** (not frontoffice prod), drop its **URL + a read-only test login** into
  `project-docs/`, and I run the four looks there; or
- **(b)** if `uat` **is** `frontoffice.develyst.online` (production), then per QA.md this is an **owner-run** check,
  not a QA login: the owner opens the four screens and sends **screenshots**, and I interpret them —
  1. **REQ-043** booking modal: one student picker, identical across tabs;
  2. **REQ-044** old course tab gone/renamed, replacement self-explanatory;
  3. **REQ-052** a cell shows program **+ type** (ทดลอง/คาบเดี่ยว/คอร์ส/บัตร); legend names status **and** type;
     **icons only, no emoji** (an emoji = finding);
  4. **REQ-053** `แก้ไขคาบ` on a course session: `วิชา` read-only + explanation, date/time/teacher editable.

**Worth stating:** all four are already **`TEST_PASSED` on `sid` with this identical build** (TEST-055/057), so the
uat look is confirmation, not first proof. **cannot-verify-read-only** is my honest status for the uat leg until (a) or (b).

**Ball: @Porter** (route the access-or-screenshots decision to the owner) **& @Tanya** (I run it the moment a non-prod
URL+login lands, or interpret the owner's screenshots).

## [--:--] Porter (PM) — 🔴 **REQ-066 raised, and it is MY error blocking the customer right now.** + REQ-067 (labels + LINE readability) → @Sober
### REQ-066 — every program has a 1-hour price of ฿1,390. **We are blocking live bookings.**
**Owner's report:** *"กิจกรรมทุกกิจกรรมมีราคา 1hr ทั้งหมด ตอนนี้มันทำให้เราไม่สามารถจองคอร์สรายครั้งได้"* — with a
screenshot of the refusal **I wrote**, firing on a Bike single session on `uat`.

**🔻 Owning it plainly: on 2026-08-22 I read the customer's price card, saw no 1-hour row in the blue block, and
concluded there was none.** I took that inference into **REQ-061 requirement 6** and had a guard built on it.
**The inference was wrong** — every program sells at **฿1,390** an hour; the card simply does not print that row.
⇒ **A reading of a layout became a rule in code, and after that nobody could tell it from a fact.**
**A price is something the owner states. It is never something I derive from a document.** That is the whole
lesson, and it cost the customer working hours today.

**The fix is one price, not a retreat:** add `1: 1390` to `bike-skate`. **REQ-061's guard is KEPT** — with all four
groups priced it simply stops firing, and it still protects the next program someone adds without a price.
**Removing it would re-open the silent-revenue-loss hole it was built for.** ⚠️ And `sale:ensure-items` **must**
run after deploy or the booking succeeds while the money silently does not post — the 08-22 failure exactly.

**📌 Knock-on I fixed immediately:** the staff guide I wrote an hour ago contained a section stating the blue block
**cannot** be booked by the hour, with a table of can/cannot. **That is now false and the owner may already have
sent it.** Corrected in `project-docs/2026-08-23-staff-guide-discount.md` — the table is gone, replaced by one
honest line. **Telling him rather than hoping he had not sent it yet.**

### REQ-067 — two readability items, no money at risk
- **Part A:** rename the tabs to **`1st Trial`** and **`1 HR`** — **the exact words on the customer's printed price
  card.** Staff read card and screen all day; two vocabularies for one product is a translation they perform on
  every booking. **Labels only — `bookingType` values and stored data untouched** (AC-2), because a rename that
  reaches the data model is a different and much larger change than what was asked.
- **Part B:** the teacher's LINE schedule is one unbroken line per session and **wraps mid-word on a phone.** Group
  by day, separate visibly, time first. **A message a teacher checks before leaving the house is, if unreadable,
  the same as being wrong.** Q1 asks the owner whether to add location — **not adding fields on assumption**, since
  the ask was legibility and adding content while fixing it is how a small change becomes a redesign.

**Ball: @Sober — REQ-066 first, it is blocking.** **Ball: owner** — if he already sent the staff guide, the
"ไม่มีราคารายชั่วโมง" section is the one to disregard; the corrected file is in `project-docs/`.

## [--:--] Sober (SA) — REQ-065 DELIVERED noted · cut REQ-066 (blocking) as lane 1 · TASK-172 guard = PASS ✅ → @Jason @Fern @Porter
Two things this turn.

**1. REQ-066 — cut TASK-174 (BE), your lane-1 blocker.** Your inference-became-a-rule owned; the fix is one line and
I grounded it: `CARD["bike-skate"]` currently `{4,6,10}` with no `1`. Add **`1: THB(1390)`** (only bike-skate — the
other three keep their card rates). That makes `isSellable("bike-skate",1)` true ⇒ **REQ-061's SINGLE_SESSION guard
stops firing** for the blue block, and it's **kept dormant** (a NULL-price program still refuses — AC-4, tested), and
it auto-defines `session-bike-skate`@1390. 🔴 **Deploy discipline in the runsheet:** code → `sale:ensure-items` →
verify `session-bike-skate` exists, **both boxes** — skip it and the booking is accepted while revenue silently
doesn't post (the 08-22 failure, exactly). AC-2 is the ledger check, not the screen.

**2. TASK-172 (request-body guard) = PASS ✅.** front tsc 0 · 40/0 (+8). `sale-request-body.test.ts` asserts each of
the four sale services puts `discount` on the wire **as the human number** (391 not 39100, not dropped) + omits when
absent; the booking assertion **fails against pre-TASK-170 code**, so it guards the exact regression that happened.
The systemic answer to "type-happy + screen-plausible + wrong on the wire." DONE.

**Ball: @Jason** — TASK-174 (blocking single-session bookings; small). **@Fern** — TASK-167 (REQ-064 AC-8) is your
only open FE task now (TASK-172 done). **@Porter** — for the owner: TASK-174's deploy+`ensure-items` on both boxes
(it's what unblocks live single-session bookings); Tanya's `uat` access question (she correctly won't touch prod —
her (a)/(b) is with you); and you still hold REQ-063's finish + REQ-064 residue. **Next on my SA queue after this:
REQ-067** (labels `1st Trial`/`1 HR` + the LINE schedule readability — two parts, not blocking). Nothing routes to
the owner from me.

## [Jason (BE)] — TASK-174 BUILT → REVIEW. 🔴 **Blocking fix done; the deploy step is the part that can still fail.** → @Sober @Porter
The change is `1: THB(1390)`. Everything else in this pass is about not doing it a third time.

**🔴 The test asserted the bug — again.** `"bike/skate has NO single-hour rate"` sat green in the suite while
every single-session booking on the busiest program was being refused on `uat`. It is rewritten from the
**owner's card**, with the history in the body, because the lesson is specific: *a test written from the code
rather than from the card makes a mistake look deliberate.*

**This is the second time** (Onewheel's 10 h was the first), so I did not stop at the one line. Both file-level
comments that listed these absences as intentional are corrected — **one of them still named bike/skate** — and
they now carry a standing warning: **a gap in `CARD` is only "not offered" if the owner's card says so.** The
guard's own comment in `scheduler.service.ts` said the same wrong thing and is fixed too.

**AC-4 has its own test now**, deliberately: a guard that has gone quiet is exactly the guard someone deletes as
dead code next quarter. A NULL / unknown price group still refuses a single session. **The guard itself is
untouched** — only the comment that mis-described it.

**Verified:** tsc **0** · `bun test` **707 pass / 0 fail**. No migration, no FE. ⚠️ Nothing run against a database.

### 🔴 @Porter — the runsheet for the owner, `sid` first, then `uat`
1. Deploy the code.
2. **`bun run sale:ensure-items`** on that box — this is what creates `session-bike-skate` @ 139000.
3. **Verify the item exists at 139000 before anyone books.**

**Step 2 is not optional and step 3 is not paranoia.** Without `ensure-items` the booking is *accepted* and the
revenue *silently does not post* — precisely the 2026-08-22 failure. **The screen looks correct either way; only
the ledger tells them apart**, which is also why AC-2 is the owner's/Tanya's check and not something I can close
from here.

**Ball: @Sober** — review (TASK-171, TASK-173 Q1, TASK-168's Q1 and TASK-166 are also with you). **@Porter** —
the runsheet above; it is what actually unblocks live single-session bookings, and REQ-065's flip SQL is still
queued behind its deploy too. **@Fern** — TASK-167 remains your only open FE task; nothing here touches the FE.

## [--:--] Porter (PM) — 🆕 REQ-068 (session note) + **REQ-052 AMENDED with a display toggle.** The owner caught a collision before we built into it. → @Sober
### The four uat screenshots — read, and one of them is my error
✅ **REQ-044** — three tabs; the confusing course tab is gone.
✅ **REQ-053** — textbook: `แก้ไขคาบ` shows **วิชา as read-only text with the explanation line**, date/time/teacher
still editable. Exactly as specced.
✅ **Bonus — REQ-064 confirmed on `uat`:** `คอร์ส 10 คาบ · ลา 0/3 · ยังค้างอีก 0 คาบ`. **The give-away fix is
working on the customer's live system**, not just on `sid`.
🟡 **REQ-043** — the combined picker is on every tab, **but the Voucher tab lacks `/ เพิ่มนักเรียนใหม่`.**
**Owner confirms that is intended.** ⇒ closed.
🔴 **REQ-052 — NOT BUILT.** Cells show `09:00 Aileen`; the legend lists **statuses only**. Board confirms
**`IN_SPEC`, TASK-142 gated and never started.** **I listed it among "ten REQs at TEST_PASSED" — it was never
there.** Same failure as REQ-051 last week: **I read my own summary instead of the board.** The other nine need
re-checking against the board before I quote that list again.

### 🆕 REQ-068 — a note on the session
The customer's real case: **a voucher family brings siblings to make the hour worth it**, so the teacher expects
one child and meets four. **Badges cannot do this** — they are a fixed vocabulary; this is free text that differs
every time. The note must reach **the teacher's LINE schedule** (the message REQ-067 Part B is already making
readable — **so it lands there instead of in a new notification nobody asked for**) and the on-floor admin.
**Per session, not per course** (AC-3 — editing one session must not write the other nine).
⚠️ **Q3 to SA: `bookings.note` already exists and the importer writes it.** Reusing an occupied column is how two
features quietly overwrite each other — **check before building.**
**Scoped out, deliberately:** making the siblings *real attendees* — that is entitlement and revenue (do they
consume voucher hours?), not a text field. **He asked for a note; a note is what this delivers.**

### 🔄 REQ-052 amended — and the owner's timing is the whole value
He saw that REQ-052 (program + type) and REQ-068 (note) **land on the same cell**, alongside badges and rentals,
and asked **before Fern built it**: *"มันจะทำให้ UI calendar บวมเกินไปมั้ย"*. **Yes it would.** Four more items in
a cell that today holds `09:00 Aileen` is not a richer calendar, it is an unusable one — **and we would have found
that out after building it.**

**His answer, adopted: the admin chooses what the cell shows. Default everything ON.** Progressive disclosure
**decided by the user rather than guessed by us** — right for a screen where the floor manager wants detail and
the capacity checker wants density.

**I pinned the toggle list to five** (type · program · badge · note · rental) rather than leaving his *"บลาๆ"*
open, added **AC-8: with everything off the cell is exactly today's calendar** — nobody is forced into a busier
board — and **AC-7: it must still read at 375 px, and if it cannot, say so rather than ship it.**

**⚠️ @Sober — TASK-142 must be re-specced before it is built.** Build the cell **once**, with the toggle, carrying
all five. Building program+type now and adding note+rental later means **building it twice and discovering the
crowding in production.**

**Ball: @Sober** — REQ-066 (blocking) → then re-spec TASK-142 with REQ-068 folded in. **Ball: owner** — Q1/Q2 on
the note (daily report? re-notify on edit?), Q6 (day view too?) — none blocking.

## [--:--] Sober (SA) — TASK-174 (REQ-066) = PASS ✅. The single-session blocker is fixed in code. → @Jason @Porter
Reviewed by reproduction: tsc **0** · `bun test` **707/0** (+1). `CARD["bike-skate"]` gains `1: THB(1390)` (only
bike-skate); `isSellable(bike-skate,1)`→true (AC-1), a null/unknown group still refuses (AC-4, tested), and
`session-bike-skate`@1390 derives into `SALE_ITEMS`. **The durable win beyond the one line:** Jason corrected the
comments that called these price gaps intentional — one *still named bike-skate* — and added a standing warning that
a `CARD` gap is "not offered" only if the owner's card says so. That's the inference-as-fact lesson (Onewheel 10h was
the first instance) finally written where the next reader sees it. Guard untouched, only its mis-describing comment.

**⇒ Code DONE; the blocker clears on the owner's deploy** (this is the money-critical part): **code →
`sale:ensure-items` → verify `session-bike-skate`@139000, `sid` then `uat`.** Without `ensure-items` the booking is
accepted while revenue silently doesn't post — the 08-22 failure. AC-2 (posts ฿1,390 at day-end) is the
owner's/Tanya's ledger check, not the screen.

(Housekeeping: Jason's "TASK-171/173-Q1/168-Q1/166 also with you" is stale — all reviewed DONE or answered in-review.
Nothing of mine outstanding on those.)

**Ball: @Porter** — the TASK-174 deploy runsheet to the owner (both boxes, `ensure-items` mandatory) — it's what
actually unblocks live single-session bookings, and REQ-065's flip SQL is queued behind the same deploy. You still
hold REQ-063's finish + REQ-064 residue + Tanya's `uat`-access decision. **@Jason** — BE clear. **@Fern** — TASK-167
(REQ-064 AC-8) your only open FE. **Next on my SA queue: REQ-067** (labels `1st Trial`/`1 HR` + LINE schedule
readability — not blocking). Nothing routes to the owner from me.

## [--:--] Porter (PM) — ✅ REQ-068 Q1/Q2 both answered "ไม่". Scope shrinks and the feature gets sharper.
**Q1 — not in the daily report.** ⇒ this is not a smaller decision than it looks: it **defines what the note is
for.** The daily report is the after-the-fact sheet; **a note about who is turning up belongs where people look
BEFORE the session** — the calendar and the teacher's LINE. Written in as a boundary so nobody adds it later
meaning well.

**Q2 — no re-notification on edit.** Added as **AC-8**: adding or editing a note **must not enqueue any
notification**, provable by an empty outbox. *(A feature that pings a teacher's phone on every small correction
gets muted — and then the notes that mattered go unread too. Both of us landed on the same answer for the same
reason.)*

**Still open and not the owner's:** Q3 to SA — **is `bookings.note` already occupied by the importer?** That is
the one that decides whether this is a new column or a shared one, and it must be answered before build.
**REQ-052 Q6** (toggle on the day view too) unanswered and non-blocking — **proceeding on both views, one
control**; one word changes it.

**Ball: @Sober** — REQ-066 (blocking the customer) → re-spec TASK-142 with REQ-068 folded in.

## [--:--] Porter (PM) — 🟢 **REQ-066 deployed to BOTH boxes. The live booking blocker I created is cleared.**
`session-bike-skate` @ ฿1,390 created on `sid` **and** `uat` (1 created / 21 already present on each), with no
existing price touched. **Staff can book a single session on any program again.**

**The whole episode in one line, because it is the most expensive lesson of the two days:**
> **I read a layout, concluded a price did not exist, and had that conclusion built into a rule. Nobody could tell
> it from a fact afterwards, and it stopped the customer working.**

**What makes it not repeat is not the fix — it is Jason's comment change.** The code previously *documented* those
price gaps as intentional, **and one of those comments named `bike-skate` explicitly.** That comment is why I was
confident. It now carries a standing warning that a gap means "not offered" **only if the owner's card says so**.
**The Onewheel 10 h gap was the first instance of the same mistake and nobody noticed the pattern; now the warning
sits where the next reader will see it.**

### Verification still owed — and only one of the two needs the owner
- **On `uat`:** open จองรายครั้ง → pick **Bike** → the red *"โปรแกรมนี้ไม่มีราคาแบบรายชั่วโมง"* panel must be gone
  and `ราคาเต็ม 1,390` shown. **Read-only — the refusal renders before saving**, so this can be confirmed without
  creating anything on the customer's system.
- **On `sid`:** a Bike single session must post **฿1,390 at day-end** on `session-bike-skate`. **The ledger check
  belongs on `sid`, never `uat`** — and it is the one that proves the money half. AC-2.

**Ball: owner** — the `uat` look (5 seconds) and, when convenient, the `sid` ledger check.

## [--:--] Porter (PM) — ✅ REQ-066 verified on `sid`. **The blocker is visibly gone.**
Owner's screenshots: จองรายครั้ง with **Bike** selected — **the red "โปรแกรมนี้ไม่มีราคาแบบรายชั่วโมง" panel is
gone**, and the form shows **`ราคาเต็ม 1,390 · ส่วนลด −0 · ยอดที่ต้องชำระ 1,390`** with the discount section
present.
⇒ **AC-1 confirmed** (bookable again) **and AC-3 confirmed** (the discount section now appears for these single
sessions, keyed off the line total — it follows the price automatically, exactly as designed).

**Outstanding on REQ-066: AC-2 only** — that a Bike single session posts **฿1,390 at day-end** on
`session-bike-skate`. **The ledger half, on `sid`, when convenient.** Not blocking anyone.

**Noted in passing, not raised:** the `sid` calendar now carries a **Badge filter** that the `uat` screenshot did
not show — worth confirming the two boxes' FE builds match when someone next looks, but **no evidence of a problem
and I am not opening anything on it.**

### Porter's proposed order for the next lane (owner to confirm)
**1. REQ-067** — tab labels (`1st Trial` / `1 HR`) + the teacher's LINE schedule readability. **Small, and both are
read many times a day.** A quick, visible win.
**2. The calendar bundle** — re-spec TASK-142 with the display toggle **and** REQ-068's note together. Bigger, and
it must be built once.

Reason for that order: REQ-067 is a **contained** change; the calendar bundle needs REQ-068's field designed
alongside it, and **starting the big one while a small one sits finished-but-unshipped is how small things never
ship.**

**Ball: owner** — confirm the order (or override it). **@Sober** — REQ-067 unless told otherwise.

## [--:--] Porter (PM) — 🔴 **REQ-069: the FE and BE disagree about when a week starts. Bookings are invisible on the calendar.** → @Sober, this jumps the queue
Owner noticed the calendar showed last week's bookings only when he set the date to **Sat 22**, and not on **Sun 23**.
He guessed the cause himself: *"หลักการเว็บเรา เอาแบบโรงเรียนไทย จะเป็นจันทร์ถึงอาทิตย์"*. **He is right, and the
truth is worse than a wrong constant — there are two definitions.**

| | week |
|---|---|
| **FE draws the columns** (`CalendarContent.tsx:27`) | **จันทร์ → อาทิตย์** ✅ |
| **BE fetches the data** (`lib/time.ts:38` `weekRange`, used by `getCalendar:382`) | **อาทิตย์ → เสาร์** ❌ |

**The grid is drawn for one week and filled with another.** They overlap partially, and which part is missing
depends on today's day of week.

### 🔴 The consequence, and it is why this jumps the queue
On **any Wednesday**: FE draws **จ17→อา23**, BE returns **อา16→ส22** ⇒ **the Sunday column has no data to draw.
Ever.**
**Sunday is this customer's busiest day** — the go-live import carried **80 rows on Sunday, the largest of all
eight batches.** **Their busiest teaching day reads as empty on the calendar**, with no error and no warning — an
empty cell simply looks like nobody booked.

**Staff have been looking at a calendar that hides real bookings**, and it took the owner navigating onto a
Saturday by chance to expose it. **Nobody would have found this by testing a feature; it is only visible if you
walk the week.** Hence **AC-7: verify on all seven days, not one spot check** — this defect is invisible six days
out of seven.

### And it is not only the calendar
**`weekRange` also drives the teacher's LINE `ตารางสัปดาห์นี้`** (`line-webhook.service.ts:374`, whose own comment
documents "Sun–Sat"). ⇒ **teachers have been receiving the wrong week too.** **It lands with REQ-067 Part B**,
which is already rewriting that message — **fix range and layout in one pass rather than touching it twice.**

**Fixed at the source, not in the calendar** (requirement 2): making the FE match the BE would make the Thai week
wrong everywhere else instead. ⚠️ **Q1 asks SA to list every caller** — a shared date helper is exactly where an
unexamined assumption spreads, **and this REQ exists because one did.**

### Queue
**@Sober — REQ-069 first**, ahead of REQ-067 and the calendar bundle. It is a **data-visibility** defect on the
customer's live system; the others are improvements. **REQ-067 Part B should ride with it.**

**Ball: @Sober.** **Ball: owner** — nothing; he already did the hard part by noticing.

## [--:--] Sober (SA) — REQ-069 (Mon→Sun week) cut as lane 1, with REQ-067 Part B bundled in. Both SA questions answered clean. → @Jason @Fern @Porter
Noted REQ-066 deployed to both boxes (single-session blocker cleared). Took REQ-069 first per your queue.
- **The bug, grounded:** `weekRange` (`lib/time.ts:38`) is Sunday-start; the FE calendar draws Monday-start
  (`CalendarContent:27`) — grid drawn for one week, filled with another ⇒ **the Sunday column is always empty**, and
  Sunday is their busiest day (80 rows in the go-live import). Fix at the source (constraint), Monday offset
  `-((getDay()+6)%7)`; on a Sunday it yields the week that Sunday **ends** (AC-1).
- **Q1 — audited: only two `weekRange` callers, both correct as Monday** (getCalendar `:382`, teacher LINE
  `:374`). **No caller must stay Sunday-based** — so the source fix is safe, nothing to leave alone.
- **Q2 — no third definition:** `recurring.ts`/`work-days.ts` use `getDay()` for a **weekday index** (which day a
  session repeats), not a week boundary; the digest is per-day; revenue groups by **month**; the FE's Monday-start
  is the other side and matches after the fix. One definition everywhere.
- **REQ-067 Part B rides in the same task** (`line-webhook.service.ts` is where both the range and the layout live) —
  group the teacher schedule by day, time-first, separators, TH/EN, keep the cap/empty/quick-replies. Fix range +
  layout in one pass, as you said.

**TASK-175 (BE, @Jason)** = the weekRange source fix + caller audit + the LINE schedule rewrite. No FE (calendar
already draws Monday), no migration. **TASK-176 (FE, @Fern)** = REQ-067 **Part A** (label rename `1st Trial`/`1 HR`
everywhere, labels-only) — cut and queued behind the priority.

**Ball: @Jason** — TASK-175 (data-visibility defect on the live calendar + teacher LINE). **@Fern** — TASK-167
(REQ-064 AC-8) then TASK-176 (067 Part A). **@Porter** — after TASK-175, verify on the box: the week view populates
Sunday on a Sunday (AC-7 = walk all seven days), and the teacher LINE week message reads Mon→Sun grouped (phone
check is real-device only). You still hold: REQ-063 finish · REQ-064 residue · REQ-065 flip SQL (queued behind its
deploy) · REQ-066 already deployed · Tanya `uat`-access · and **REQ-068 (re-spec TASK-142)** which I have NOT started
yet — flag if you want it ahead of the queue. Nothing routes to the owner from me.

## [Jason (BE)] — TASK-175 BUILT → REVIEW. 🔴 **The week fix is one line; the finding is that nothing tested it.** → @Sober @Porter
Both halves, in one pass, as you cut it.

### Part 1 — Mon→Sun at the source
The change is `-((dow + 6) % 7)`. **The more useful finding: `weekRange` had no test at all.** That is exactly
how a Sunday-start helper survived against a Monday-start calendar for months — nothing in the suite asserted
the week the staff actually look at, so the code was self-consistent and wrong. `lib/time.test.ts` is new and
written **from the calendar, not from the code**: Monday starts its own week · every midweek day lands in the
same block · **Sunday returns the week it ENDS** (the defect in one assertion) · every day of a week maps to the
same seven dates with **Sunday as the last column** · exactly 7 days across month and year boundaries.

**I re-ran your caller audit independently rather than trusting it** — same two callers, both correct as Monday,
and nothing else derives a week (weekday index ≠ week boundary; digest is per-day; revenue is monthly). The LINE
caller's own comment said "Sun–Sat"; that comment is what kept the second victim invisible, so it is corrected
too.

### Part 2 — the teacher's message
Rewritten around the **shape**, not the words: **no line carries all four fields any more**, which is what made
it wrap mid-word and mid-name. Day named once as a heading, each session leads with the **time** — the field a
teacher scans for — student on that line, `program · status` indented beneath so a long program name wraps into
its own space instead of into the next session. Blank line between days. Sorted day-then-time regardless of
query order, because a teacher reads this as a plan, not a list.

```
🗓️ ตารางสัปดาห์นี้
▸ ศุกร์ 31/07
16:00  น้องซี
   Onewheel E-Skate · ยืนยันแล้ว

▸ อาทิตย์ 02/08
09:00  น้องเอ
   Bike / Scooter / Balance Cruiser · ยืนยันแล้ว
```

Cap, overflow line, empty state, TH/EN and both quick-replies are unchanged, each with its own regression test.
The now-dead `tsched_row` template is deleted rather than left to rot. **Q1 (location): not taken** — the ask was
legibility, adding a field on assumption is how a small change becomes a redesign, and it is the owner's call.

**Verified:** tsc **0** · `bun test` **716 pass / 0 fail** (+13). No migration, no FE. ⚠️ Nothing run against a
database, and **I cannot drive a LINE account** — the block above is the pure function's output, not a phone.

**@Porter — two checks that are only yours after deploy:** (1) **walk all seven days** on the box (AC-7) — this
defect is invisible six days out of seven, which is why it survived; (2) the teacher LINE week message on a
**real phone** (AC-6). Both are look-only; nothing here changes money or data.

**Ball: @Sober** — review (TASK-171, TASK-173 Q1, TASK-174, TASK-168 Q1, TASK-166 are also with you).
**@Porter** — the two checks above, plus REQ-065's flip SQL still queued behind its deploy. **@Fern** — TASK-167
then TASK-176; REQ-067 Part A is yours, Part B landed here.

## [--:--] Porter (PM) — Owner's queue decision + wrote a customer-facing timeline. **Two corrections to his own status list.**
**Decision: wait for TASK-175, then close the four.** REQ-052+068 does not start until the week fix lands — one
lane, as he set it.

**Timeline written to `project-docs/2026-08-23-timeline-current-picture.md`** for NotebookLM, built from **his**
list and **his** numbering (the customer sees REQ-001…008 / BO-001…005 / FIX-001…006, not our internal numbers).
Four phases, ordered by **dependency rather than by date** — dates would be fiction and he asked for the current
picture, not a promise.

**The caveat is the most important part of the document, and I gave it evidence instead of a disclaimer:**
> *"In the last 2 days, 6 items were found by using the system that were on no list."* — gender invisible ·
> Onewheel price · phantom sessions · a program that should not exist · the missing 1-hour price · the week
> boundary.
**Anyone reading a timeline wants to know how much to trust it. A number does that; "estimates may change" does
not.** And it is honest about *why*: those six were **only findable by real use**, so more will come.

### 🔻 Two corrections to the owner's own list — told him rather than silently reformatting
1. **FIX-005 is marked "In Progress" but it is DONE** — deployed to both boxes today, `session-bike-skate` @ 1,390
   verified in `bo.item` on both.
2. **Two items are missing from his list entirely** — both found today, both real work:
   - the **week runs Sunday→Saturday** defect (bookings invisible on the calendar; **their busiest day, Sunday,
     reads as empty**);
   - the **misaligned date/time fields** in the "already in progress" modal.
   ⇒ Added as **FIX-007** and **FIX-008** in the timeline. **His customer-facing list should carry them too, or
   the next status update under-reports what the team did.**

**Also stated in the doc, because it is a sequencing constraint and not a preference:** REQ-007 (Note) and the
calendar work **land on the same cell and must be built once**; **BO-004 (payroll) cannot precede BO-003 (freelance
ceiling)**; and **REQ-002 waits on the customer**, not on us.

**Ball: owner** — send it. **@Jason** — TASK-175. **@Fern** — TASK-167 → TASK-176.

## [--:--] Sober (SA) — TASK-175 (REQ-069 + 067B) = PASS ✅. Calendar/LINE week fix + schedule readability done. → @Jason @Porter
Reviewed by reproduction: tsc **0** · `bun test` **716/0** (+13, incl. new `time.test.ts` 5/0). `weekRange`→Monday
(`-((dow+6)%7)`, Sunday=week it ends), comment corrected. **The finding worth keeping: `weekRange` had NO test at
all** — that's precisely how a Sunday-start helper survived against a Monday-start calendar for months; it's now
tested from the calendar. Jason re-ran the caller audit independently (matched mine — 2 callers both Monday, no third
definition) and fixed the LINE "Sun–Sat" comment that hid the second victim. Part B rewrite fixes the *shape* (no
line carries all four fields → no mid-word wrap; day heading, time-first, indented program·status), regressions
(cap/empty/quick-replies/TH-EN) each kept. **DONE (code).**

**⇒ REQ-069 + REQ-067 Part B code-complete.** The two remaining are deploy/device-only and yours/owner's: **AC-7 —
walk all seven days on the box** (the defect is invisible six days out of seven, so a spot check won't prove it) and
**AC-6 — the teacher LINE week message on a real phone**.

(Housekeeping: Jason's "171/173-Q1/174/168-Q1/166 also with you" is stale — all reviewed DONE or answered in-review.)

**Ball: @Porter** — deploy TASK-175 then the two look-only checks above; you also still hold REQ-063 finish · REQ-064
residue · REQ-065 flip SQL · REQ-066 deployed · Tanya `uat`-access · **REQ-068 (re-spec TASK-142)** not yet started.
**@Jason** — BE clear. **@Fern** — TASK-167 (REQ-064 AC-8) then TASK-176 (REQ-067 Part A). **Next on my SA queue:
REQ-068 (re-spec TASK-142)** unless you re-rank — say the word. Nothing routes to the owner from me.

## [--:--] Porter (PM) — 🟢 TASK-175 PASS · **four REQs closed** per the owner's instruction
**REQ-069 + REQ-067 Part B are code-complete.** tsc 0 · **716/0**. `weekRange` is Monday-start; on a Sunday it
yields the week that Sunday **ends**.

**The finding worth more than the fix — and it explains the whole defect:**
> **`weekRange` had NO test at all.**

That is precisely how a Sunday-start helper survived for months against a Monday-start calendar: **nothing ever
asserted what a week was.** It now has one, written from the calendar's point of view. Jason also re-ran the caller
audit independently and reached Sober's answer (2 callers, both Monday, no third definition) — **an independent
match is worth more than a second opinion asked for politely.**

### ✅ Closed: REQ-043 · REQ-044 · REQ-053 · REQ-054
Per the owner's instruction. **How each was closed is written on the board**, because that is what makes a
DELIVERED honest six months later:
- **043 / 044 / 053** — verified on `uat` from **his screenshots**, item by item.
- **054** — closed on **live-usage evidence, not a QA run**: the customer created **~43 courses on `uat` today**
  with no mixed-program fault. **Recorded in those words** so nobody later reads it as a tested pass.

### What deploying TASK-175 needs — and why a spot check will not do
**BE only** (`smart-scheduler-back`), `sid` → `uat`. No migration, no FE.
- **AC-7 — walk all SEVEN days.** The defect is **invisible six days out of seven**; checking one day proves
  nothing. Specifically: on a **Sunday** the week must show **จ→อา of the week that is ending**, with Monday–Saturday
  populated — today they vanish.
- **AC-6 — the teacher LINE week message on a real phone.** It is a layout fix; a desktop preview cannot judge it.

**Ball: owner** — deploy + the two look-only checks. **@Sober** — REQ-068 (re-spec TASK-142) is next unless
re-ranked. **@Fern** — TASK-167 → TASK-176.

## [--:--] Porter (PM) — 🧪 @Tanya — TASK-175 is on `sid`. **Walk the week. Seven days, not one.**
Owner deployed `smart-scheduler-back` to **`sid`**. `sid` only — **`uat` is not yours** (settled 14:38; that
boundary stands and you were right to hold it).

### 🔴 The check that actually proves this — and why the usual approach fails
**The defect is invisible six days out of seven.** Before the fix the grid was drawn Monday-start while the data
was fetched Sunday-start, so the **Sunday column was always empty** and every other day looked fine. **A spot
check on a Wednesday would have passed all year.**

**⇒ Set the date to each of the seven days in turn and record what you see.** For every one of them:
1. **the header range and the columns are the same seven days** (จ→อา);
2. **every day that has bookings shows them** — most importantly the **Sunday column**, which was blank before;
3. **the same booking appears on the same date whichever day you navigate from.** *(That is the real assertion:
   the week you are shown must not depend on which day you happened to be standing on.)*
**Report it as a seven-row table.** A single "passed" is not evidence here.

### The second half — the teacher's LINE schedule
The same task rewrote `ตารางวันนี้` / `ตารางสัปดาห์นี้`: grouped by day, day heading once, **time first**,
program·status indented, separators.
⚠️ **You have no phone or LINE account** — so **only the parts you can judge from the code/response are yours**;
the on-device look is the owner's. **Say which half you checked.** Two things are worth your eyes regardless:
- **the week it covers is now จันทร์→อาทิตย์**, and on a Sunday it is the week that is **ending**;
- the **cap (20 + "…and N more")**, the **empty state**, the **quick-reply buttons** and **TH/EN** still behave —
  a layout rewrite is exactly where those get lost.

### While you are on `sid`
Confirm **REQ-066**: จองรายครั้ง on **Bike** is bookable and reads `ราคาเต็ม 1,390`.

**Report pass / fail / cannot-verify per item. Ball: @Tanya → @Porter.**

## [17:27] Tanya (QA) — sid pass: REQ-069 week walk PASS (7/7) · REQ-066 PASS · REQ-065 PASS · LINE half = owner. → @Porter
All on `sid` (uat untouched). Read-only except where noted; nothing saved.

🟢 **REQ-069 — the week walk PASSES all seven anchor days.** Set the date to each day of the week 2026-08-17→23; the
displayed week is **identical every time**, and the **Sunday column (08-23) shows its booking on every one** — the
exact cell that was blank before:

| Anchor day | Header range | Columns | Sunday 08-23 shows Aiwa? | Wed 08-19 shows มิลล่า? |
|---|---|---|---|---|
| Mon 08-17 | 17 Aug – 23 Aug 2026 | Mo17·Tu18·We19·Th20·Fr21·Sa22·**Su23** | ✅ | ✅ |
| Tue 08-18 | 17 Aug – 23 Aug 2026 | (same seven, จ→อา) | ✅ | ✅ |
| Wed 08-19 | 17 Aug – 23 Aug 2026 | (same) | ✅ | ✅ |
| Thu 08-20 | 17 Aug – 23 Aug 2026 | (same) | ✅ | ✅ |
| Fri 08-21 | 17 Aug – 23 Aug 2026 | (same) | ✅ | ✅ |
| Sat 08-22 | 17 Aug – 23 Aug 2026 | (same) | ✅ | ✅ |
| **Sun 08-23** | 17 Aug – 23 Aug 2026 | (same) | ✅ | ✅ |

The week shown never depends on which day I stood on, columns are **Mon→Sun**, and the Sunday booking renders from
every anchor. **PASS.**

🟢 **REQ-066 — PASS.** จองรายครั้ง (Single session) on **Bike** reads **"Full price 1,390"** (ราคาเต็ม 1,390) and Save
is enabled (bookable). Read-only — not saved.
🟢 **REQ-065 — PASS.** **"1st Trial" is gone** from the course-create program (วิชา) picker. (I'd seen it earlier only
because that was pre-deploy.)
🟢 **REQ-063 render — PASS** (from the earlier discounted-booking view): the card shows "Discount: ฿391 · Reason", no raw key.
🔴 **REQ-064 AC-8 — FAIL, as expected** (TASK-167 not built): the `เริ่มคาบที่เหลือ`/`เวลา` misalignment is present —
recorded so the fix is provable. Not a regression.

🟡 **REQ-067B (teacher LINE schedule) — I checked the ZERO half; the rendered message is the owner's.** The on-device
`ตารางวันนี้/สัปดาห์นี้` needs a phone + LINE account, which I don't have — **cannot-verify-read-only** by me, and
reading the builder code is not a test. The week-range/cap/empty-state/quick-reply/TH-EN all live in that rendered
message → **owner's on-device look.** I checked no part of it by running it; I will not claim otherwise.

**Ball: owner** (the on-device LINE schedule look for REQ-067B) **& @Porter** (everything else on `sid` is signed above).

## [--:--] Porter (PM) — 🟢 Tanya's week walk: **PASS 7/7.** ⚠️ And `uat` still has the bug — the customer's Sunday is blank right now.
She did exactly what the defect required: **set the date to each of the seven days and reported a seven-row table**,
not a verdict. The week shown is **identical from every anchor day**, columns are **จ→อา**, and **the Sunday column
renders its booking from all seven** — the exact cell that was blank before. **That is proof; "it works" would not
have been.**

Also PASS on `sid`: **REQ-066** (Bike single session bookable, `ราคาเต็ม 1,390`) · **REQ-065** (`1st Trial` gone from
the program picker) · **REQ-063** (the discount renders on the booking, `฿391 · reason`, no raw key).
**REQ-064 AC-8 FAIL as expected** — the modal misalignment; TASK-167 not built. **Recorded so the fix is provable.**

**Her line on the LINE half is the standard I want quoted back at me:**
> *"reading the builder code is not a test. I checked no part of it by running it; I will not claim otherwise."*

**She had every excuse to call it a pass** — the code is right there and Sober already reviewed it. She didn't.
**That is the difference between a QA report you can act on and one you have to re-check.**

### ⚠️ The thing that matters most right now
**`uat` still has the week bug.** The customer's calendar is live, **Sunday is their busiest teaching day (80 rows
in the go-live import), and it currently reads as empty.** Staff are looking at a board that hides real bookings —
today, while we discuss it.
⇒ **Deploying `smart-scheduler-back` to `uat` is the highest-value action available.** BE only, no migration,
no FE. Proven on `sid` across all seven days.

**Ball: owner** — deploy to `uat`, then the on-device LINE look (the only thing nobody but he can do).

## [--:--] Porter (PM) — 🟢 **REQ-069 fixed on `uat`. The customer can see their weekend again.**
Owner deployed and sent the calendar. **The date picker reads `23 ส.ค. 2026` — the exact case that failed** — and
the week **17–23** renders complete, with **Saturday and Sunday both full of bookings** across Bank, Bowy, Camp
and Ek.

**What the screenshot shows about their business, and why this bug mattered more than it looked:**
**Monday–Friday are nearly empty; Saturday and Sunday are packed.** **Their week IS the weekend** — and the defect
made which weekend days you could see depend on which day you happened to be standing on. **We were hiding the
majority of their schedule from them**, with no error and no clue, and it took the owner navigating onto a
Saturday by accident to notice.

⇒ **REQ-069 DELIVERED** on both boxes. Verified on `sid` across all seven anchor days by Tanya, and on `uat` on the
day that used to fail.

**Remaining on REQ-067 Part B: the on-device LINE look** — grouped by day, time first, separators, and the week now
running จันทร์→อาทิตย์. **Only the owner can do it** (phone + LINE account), and it is not urgent: the *range* is
proven by the calendar sharing the same fixed helper; what is unproven is the *layout*.

**Ball: owner** — the LINE look when convenient. **@Sober** — REQ-068 (re-spec TASK-142: calendar cell + display
toggle + note) is the next lane. **@Fern** — TASK-167 → TASK-176.

## [--:--] Porter (PM) — 🟢 **REQ-067 Part B PASSES on a real phone. REQ-069's LINE half confirmed with it.**
Owner opened both messages on his device — the only place a layout fix can be judged.

**`ตารางวันนี้`** — time first, name on the same line, `program · status` indented beneath. **No mid-word
wrapping.** Seven sessions read as seven things instead of a block of text.

**`ตารางสัปดาห์นี้`** — **grouped by day** with a `▸ พุธ 19/08` heading, **blank line between days**, same
time-first shape inside. **AC-4 · AC-5 · AC-6 all met.**
*(Fern used day headings + blank lines rather than the rule lines I sketched — **better**: rules cost a whole line
each on a phone and add nothing the heading does not already do. His call was right and mine was decoration.)*

**And it proves REQ-069's second half at the same time:** the week runs **พุธ 19 → อาทิตย์ 23**, i.e. inside
**จ 17 – อา 23**, and **it is Sunday today** — so the message shows **the week that is ENDING**, not one starting.
**AC-4 of REQ-069 confirmed.** Monday–Tuesday are absent because they genuinely have no bookings, which matches
the calendar exactly (Mon–Fri near-empty, weekend packed).

**Regressions held:** the quick-reply buttons `สัปดาห์นี้` · `ปฏิทินของฉัน` · `‹ เมนู` are all still there —
**the thing most likely to be lost in a message rewrite, and it wasn't.**

⇒ **REQ-067 Part B DONE. REQ-069 fully verified on both boxes and both surfaces.**

**📌 Noticed in the screenshot, not assumed:** *"ส่งคำขอผูกบัญชีให้เจ้าหน้าที่แล้ว ✅ รอการอนุมัติ"* — a link
request is sitting **pending approval**. If that was the owner trying to register as an **admin** (REQ-049 AC-1),
it may be the chicken-and-egg we predicted: **approving an admin needs an admin.** **Asking rather than
concluding** — it may equally be a teacher link.

**Ball: owner** — the one question above. **@Sober** — REQ-068 (re-spec TASK-142) is the lane.

## [--:--] Porter (PM) — 🔻 Correction from the owner: **REQ-059 + REQ-060 Part B.1 are DONE.** I read them as open.
The owner told me plainly: *"B เสร็จหมดแล้ว"* — the importer update-in-place run and `demographics:repair` are
**both complete**. I had ranked them as open work.

**Where I went wrong, because the shape of the mistake matters more than the mistake:**
`PROJECT-STATUS.md` **already said so** — line 39, *"REQ-060 Part A+B.1 — gender/nationality normalised and
repaired"*, in the **Shipped and live on BOTH boxes** section I wrote myself. I then read the **"Open work, ranked"**
list further down, found REQ-059/060 still sitting there, and reported the stale list over my own current one.
**The doc contradicted itself and I quoted the wrong half.** Same failure as REQ-061 an hour earlier.

⚠️ **And the record genuinely does not carry the runs.** Neither script's completion is written in this log, in
`board.md`, or in a report file — the handoff on 08-22 ends at *"Ball: owner"* and nothing closes it. So my
"ไม่มีบันทึกว่ารัน" was **true about the record and wrong about the world**. That is the gap worth fixing:
**the owner ran two scripts against both boxes and the team's record still says they are pending.**

### Fixed in `PROJECT-STATUS.md`
- **REQ-060 Part A + B.1** — reworded to name the repair explicitly (`demographics:repair`, owner-run, both boxes).
- **REQ-059** — **added** to Shipped (it was in neither section — only in the open list).
- Go-live wave 1: *"24 rows still hold `Male`/`Thai`"* → **repaired**.
- **Open work, ranked — rebuilt.** Dropped REQ-059, REQ-060 Part B and REQ-061 (all delivered). REQ-052 is now
  stated as **REQ-068 + REQ-052 re-spec** rather than *"TASK-142 unblocked"*, which read as ready-to-build and is
  not. Added **REQ-060 Part B.2** (SPECced, not cut) so it stops being invisible. 10 items → 8, all real.

### What I still owe, and it is small
Two numbers, only the owner has them: **how many of the 24 rows the repair actually changed**, and **how many of
the 31 renames the importer applied on `uat`**. Not to re-verify him — to make the DELIVERED line honest six months
from now, which is the whole reason we write them.

**Ball: @Sober** — REQ-068 / re-spec TASK-142, now formally #2 and the longest open lane. **@Fern** — TASK-167 →
TASK-176, both still `TODO` on disk, runs in parallel. **Ball: owner** — the two numbers when convenient.

## [--:--] Porter (PM) — 🟢 **REQ-057 GO. The owner lifted his own 08-22 hold.** → @Sober cut it
> *"REQ-057 เดินต่อได้ ให้ Sober cut ได้เลย"*

**This reverses a decision he made himself**, so I am recording both halves rather than quietly flipping a status.
On 08-22 he said *"ไม่ๆ เราไม่ลบข้อมูลนักเรียน ผู้ปกครอง และการจองคอร์สในตอนนี้ จนกว่าเขาจะแจ้ง"* and REQ-057 went
to HOLD. That hold is now **lifted, by him, today.** Both lines are in the REQ file, in order, with dates.

### ⚠️ The distinction I am drawing rather than assuming
His 08-22 hold was about **deleting**. Today's line is about **cutting and building**. Those are not the same act,
and a delete tool is exactly the wrong place to collapse them. So:
- **Building = GO.** SA cut, spec, build, dry-run default. Harmless by construction.
- **Running it against `uat` = still a separate owner decision.** I am **not** treating *"จนกว่าเขาจะแจ้ง"* as
  withdrawn by this line. If he means "and clear the `Test` household too", he says so and it is one more
  owner-run step — **asked, not inferred.** (Inferring a rule from a document is what cost us live bookings on
  REQ-066 this morning; I am not repeating it on a delete tool.)

### 📌 @Sober — the brief, and what is load-bearing in it
The 08-22 UPDATE block in the REQ file **is the spec**, because `uat` stopped being a test box: it holds **137 real
students / 115 real families**, and the target is **one fabricated household** sitting among them.
1. **Explicit course id. Never a predicate.** No `--name Test`, no LIKE. A human typing a pattern against a live
   roster is the exact failure we are buying protection from.
2. **Dry-run prints the blast radius BY NAME** — student, parent, course, **and every booking with its date**.
   The owner should read *"4 bookings on 25/8, 1/9, 8/9, 15/9"*, **not a per-table count.**
3. **Refuse, not warn** — LINE-linked parent · any `ATTENDED` session · a posted sale (`bo.movement` on that ref) ·
   more than one student on the parent. **A refusal he must deliberately override beats a prompt he will click.**
4. **Never touch** `subjects`, `teachers`, `bo.item`, or anything not reachable from the given id. Schema and both
   migration ledgers untouched, same guarantee `db:reset` makes.
5. **Q1 + Q2 at the top of the REQ are yours to ground from the schema** — what the FK graph *actually* requires,
   and whether "money/quota has moved" is cheaply detectable. **Do not take my table list as the spec**; if AC-3
   can only half-work, say so and I re-scope it rather than let it look complete.

### Open, owner's, does NOT block the cut
Does parent **`SOM Team` (0924912848)** go with the student and course, or student + course only? My lean is
**both** — fabricated household, and an orphan parent left in a customer's roster is its own small mess. It is a
**run-time argument, not a design input**, so Sober does not wait on it.

### Docs updated
`REQ-057` (hold lifted + brief) · `board.md` row → **GO, @Sober to cut** · `PROJECT-STATUS.md` ranked #1 rewritten
and REQ-057 **removed from "Parked / not built"**, where it still read *"owner said don't delete anything yet"*.

**Ball: @Sober** — cut REQ-057. **@Fern** — TASK-167 → TASK-176, unchanged and parallel. **Ball: owner** — the
`SOM Team` question when convenient.

## [--:--] Porter (PM) — 🔴 Owner: scheduled tasks on `sid` are old, unverified, and the customer wants them on `uat`. Grounded from code.
Read `src/routes/internal.ts`, `services/jobs.service.ts`, `services/attention.service.ts`, `db/schema.ts` and the
08-01 guide before answering. **Three findings, one of them is a hole in our own instrumentation.**

### 1. "ทำงานถูกมั้ย" is PROVABLE — for two of the three jobs
`job_runs` is an audit table written **on every run, even when the job sends nothing** — built precisely so
*"quiet because nothing to report"* and *"quiet because it is dead"* stop looking identical. Exact job strings:
**`end-of-day`** (`jobs.service.ts:130`) and **`daily-digest`** (`DIGEST_JOB`, `attention.service.ts:23`).
⇒ Two SELECTs answer three weeks of doubt. Queries handed to the owner **in chat**, not in a file.

### 2. 🔴 **`month-reset` writes NO `job_runs` row. It has no audit trail at all.**
`resetFreelanceBudgets` (`scheduler.service.ts`) returns `{reset: n}` and **inserts nothing**. So the one job we
*cannot* prove is the one that moves teacher money — and it is invisible by exactly the mechanism the other two
were fixed to defeat. **The 08-01 instrumentation fix skipped it and nobody noticed for three weeks.**
⇒ Raising it as a small BE task rather than letting it ride. Indirect check meanwhile: freelance `remaining`
should equal the full ceiling on the 1st.

### 3. ⚠️ Three things that change on `uat` the moment these are switched on — the customer is LIVE
- **`end-of-day` is not a report. It WRITES.** It flips today's `CONFIRMED` bookings whose end time has passed to
  **`NO_SHOW`** and **increments `used_sessions` / `used_hours`** (`jobs.service.ts:47–61`). ⇒ **a session staff
  did not mark gets consumed automatically.** That is UC-012 working as designed, but the customer must be told
  **before** the first run, not after a parent asks why a class was deducted.
- **It is also the posting moment for `FIRST_TRIAL` / `SINGLE_SESSION` revenue** (TASK-007, +REQ-063's deferred
  discount). ⇒ **if this job has never run on `uat`, that revenue has never posted there** — the same shape as
  `sale:ensure-items` never having run, found 08-22. **Check before assuming.**
- **`daily-digest` sends LINE to admins, and `uat` has no registered admin** (REQ-049 AC-1, still owed). ⇒
  registering it now buys a job that runs and reaches nobody. Not a reason to skip it — a reason to expect silence.
- ⚠️ **`INTERNAL_JOB_SECRET` unset ⇒ the endpoint returns `503 NOT_CONFIGURED`** (`internal.ts:21`) and Task
  Scheduler still reports success. **The most likely silent failure on a fresh box.** Verify the manual run's
  response body, not the task's exit code.

### The order proposed to the owner
Prove `sid` from `job_runs` → check whether `uat` has ever run → **manual run first, read the response** → then
register → then re-read `job_runs` the next morning. Same discipline that worked on 08-01 (*"the digest was run
manually first and returned `sent: true`"*).

**This is a change to the customer's live system ⇒ the UAT gate applies: Porter + Tanya, in writing.** I am not
green-lighting it off a code read; the `uat` half needs Tanya, and her `uat` access is still the unresolved 14:38
question. **That decision is now blocking something real** — it stops being housekeeping.

**Ball: owner** — run the two SELECTs on both boxes and send the output. **@Sober** — REQ-057 cut (unchanged) and
a small task for `month-reset` instrumentation when convenient. **@Fern** — TASK-167 → TASK-176, unchanged.

## [--:--] Sober (SA) — cut REQ-057 (SPEC-062 + TASK-177), the delete tool. Grounded Q1/Q2; two findings that change the fix. → @Jason @Porter
Noted the wins: **REQ-069 + REQ-067 Part B DELIVERED both boxes** (Tanya's 7/7 week-walk + owner's on-phone LINE
check — the customer can see their weekend again), and REQ-059 / REQ-060 Part B.1 / REQ-061 confirmed already done.
Took REQ-057 as lane 1 per your GO. It's a delete tool on a live box, so I grounded the FK graph and refusal
signals before speccing, and two things change the naive fix:

- **Q1 — `bookings.courseId` is `onDelete: set null` (`schema.ts:326`).** So deleting the course row **orphans its
  bookings, not removes them.** The tool must **delete the bookings explicitly first** (`WHERE course_id=<id>`,
  which cascades `booking_badges` and set-nulls `notification_outbox` audit), then the course. Only those two tables
  hang off a booking; `checkinToken` is a column on the row. That's the whole blast radius — Porter's guessed list
  (badges/outbox/tokens/course) was close; the set-null on courseId is the trap.
- **Q2 — refusal signals are cheap, but NOT `usedSessions`.** Refuse on **ATTENDED booking / posted SALE
  (`bo.movement` refType SALE on the course/booking refIds) / LINE-linked parent / >1 student** — each a single
  query, and together they separate the fabricated Test household (IMPORT, future sessions, single child, no
  attendance/sale/link) from every real family. 🔴 **Do NOT refuse on `usedSessions>0`:** for the IMPORT Test course
  that's the import-prior count (REQ-064), and it's 4 — refusing on it would make the tool unable to delete the one
  thing it exists for. This is the REQ-057 ∩ REQ-064 intersection, called out in the spec.

**TASK-177 (BE, owner-run):** `course:cleanup --course <id>` — explicit id only, dry-run-first prints the blast
radius BY NAME then rollback, `--commit` deletes bookings→course in one tx, **refuse-not-warn** on the four signals,
student+parent stay by default (`--remove-household` guarded for the fabricated case). Never touches unrelated rows;
no DDL/ledgers. Pure helper unit-tested.

**Ball: @Jason** — TASK-177. **@Porter** — the `SOM Team`-parent-too question is the owner's runtime call (default is
course-only; `--remove-household` handles "both" safely), doesn't block the cut. Running it against `uat` stays the
owner's separate decision (his 08-22 line was about *running*, not building) — the tool is harmless until `--commit`.
You still hold REQ-068 (#2, re-spec TASK-142) as my next lane, plus the deploy/verify residue. **@Fern** — TASK-167 →
TASK-176. Nothing routes to the owner from me.

## [--:--] Porter (PM) — 🟢 `sid` scheduled tasks: the mechanism is PROVEN. 🔴 The dangerous half has never run once.
Owner ran both queries on **`sid`**. Reading them against the code, not the shape of the numbers.

### ✅ The "11 runs in 5 days" anomaly is fully explained — and it is not a fault
**4 rows at `23:30:02` on 08-19/20/21/22** — machine-precise, one per day, `failed = 0`. That is the Windows task.
**7 rows all on 08-23** at 00:10 / 00:13 / 00:25 / 00:26 / 00:28 / 02:24 / 16:28 — **the OWNER running
`end-of-day.ps1` himself from Task Scheduler**, over and over, to prove REQ-063's day-end discount posting.
**No duplicate task registration, nothing firing on its own.**

> 🔻 **Correction, same session.** I first wrote that these were *"manual runs by the team … Jason on TASK-162/168,
> Tanya at 16:28"*. **Wrong, and invented.** The owner corrected me and told me to go read the log — which says it
> plainly at lines 31 / 35 / 117 / 426 / 436 of this very file: *"Ball: owner — the trial recipe … → `end-of-day.ps1`"*,
> *"Owner ran the full recipe. After `end-of-day.ps1`:"*. **The answer was already written down and I matched
> timestamps to people instead of reading it.** Third time today I have reported an inference over an artefact.
⇒ **Windows Task Scheduler on `sid` is reliable: 4/4 days, zero failures.**

### 🔴 What is NOT proven, and it is the half that writes
**`courses_cut = 0` on every single row.** `no_show` fired **once** in five days.
⇒ **The auto-cut path has never actually cut a course session on `sid` — not once.** So the code that flips
`CONFIRMED → NO_SHOW` and increments `used_sessions` is, in practice, **unexercised**.
**Turning the job on at `uat` today means running that path for the first time ever, against the customer's live
data — 60 courses, 109 bookings.** *"It runs"* is proven. *"It does the right thing to a busy day"* is not, and on
`sid` it could not be — the box has no traffic.

### 🔴 Instrumentation defect found on the way: `revenuePosted` over-counts
`recordSale` returns **`{ ok: true, skipped: "duplicate" }`** on a replay (`lib/sale-post.ts:88` — correct: a replay
is not an error). But the job counts `res.ok`, so **a re-run counts every already-posted sale again.**
⇒ The `1 → 2 → 3 → 4 → 5 → 6 → 8` sequence on 08-23 is **not eight new sales.** It is *"how many `ATTENDED`
trial/single bookings existed on 08-23 at that moment"*, rising as QA marked more during the discount tests.
**Anyone reading `job_runs` to answer "what did we post today" gets a number that is too high.** Small BE task:
count only `!skipped`, or report `posted` and `replayed` separately.

### ⚠️ The operational fact the customer must hear before the first `uat` run
Revenue posts **only for `ATTENDED`** bookings (`jobs.service.ts:88`), while the cut turns unmarked `CONFIRMED`
into **`NO_SHOW`**. ⇒ **a session staff forget to mark is both consumed AND never posts revenue — silently.**
On `sid` this is invisible because nothing happens there. On `uat` it is their daily reality.

### The order I am proposing
1. **Rehearse the cut on `sid` first — @Tanya, it is her box.** A course booking → `CONFIRMED` → a past date/time →
   manual `end-of-day` → assert **`NO_SHOW` + `used_sessions` +1**, then **re-run and assert it does NOT increment
   again.** That is the AC nobody has ever run, and it is free to run there.
2. Then `uat`: set `INTERNAL_JOB_SECRET` → **manual run, read the response body** (unset secret ⇒ `503
   NOT_CONFIGURED` while Task Scheduler still reports success) → register → re-read `job_runs` next morning.
3. **UAT gate: Porter + Tanya in writing.** Still blocked behind the 14:38 `uat`-access question.

**Ball: owner** — the `uat` half of both queries, + tell the customer about the unmarked-session behaviour.
**@Sober** — with REQ-057: `month-reset` writes no `job_runs` · `job_runs` is wiped by `db:reset` · `revenuePosted`
over-counts. Three small BE items. **@Tanya** — the cut rehearsal on `sid`, once Porter routes it.

## [--:--] Porter (PM) — 🔴💰 **`end-of-day` is not running on `uat`, and that means trial + single-session revenue has never posted there.** → @Sober @Tanya
Owner ran the per-run query on **`uat`**: **empty.**

**What I will and will not claim.** The query window was `current_date - 7`, and `job_runs` is wiped by `db:reset`
(`db-reset-plan.ts:32`). ⇒ **Proven: the job is not running now and has not for at least 7 days.** *"Never ran"* is
**not** proven and I am not writing it down as if it were. Unbounded query sent to the owner.

### 🔴 The money consequence, traced in code — not inferred
`recordSale` has **exactly four call sites** (`grep`, non-test):
- `scheduler.service.ts:1182` — **course creation** (at sale) ✅
- `scheduler.service.ts:1511` — **voucher creation** (at sale) ✅
- `rental.service.ts:34` — rentals ✅
- **`jobs.service.ts:121` — the day-end job → `FIRST_TRIAL` / `SINGLE_SESSION`** 🔴

**Marking a session `ATTENDED` does NOT post revenue.** I checked the `attend` branch
(`scheduler.service.ts:1905–1920`): it sets the status, increments `used_sessions`, deducts voucher hours — and
**never calls `recordSale`.** The day-end job is the **only** path to the books for those two types.

⇒ **On `uat`, every 1st Trial and every single session sold has produced ZERO revenue rows** — whether or not
staff marked attendance. **This is the same shape as `sale:ensure-items` never having run (08-22): live money,
no error, nothing on any screen.** And REQ-066 just made bike/skate single sessions sellable at ฿1,390, so the
gap is about to widen, not narrow.

### What is NOT broken — stated so nobody over-reacts
✅ **Normal attendance works.** `มาเรียน` consumes course/voucher quota at `scheduler.service.ts:1905`,
independent of the job. ✅ **Course and voucher revenue posts at sale** and is unaffected.
🟡 Sessions nobody marks stay `CONFIRMED` **forever** — never `NO_SHOW`, never consume quota. A no-show is
currently free, and the calendar accumulates stale rows.

### 🔴🔴 The trap in the obvious fix — flagging BEFORE anyone acts on it
The job takes a `date` and is idempotent (`rev:<bookingId>`), so *"just re-run it for each past day"* looks like a
clean back-post. **It is not.** For `runDate < today` the cut predicate is `sql\`true\`` (`jobs.service.ts:29`) ⇒
**every `CONFIRMED` booking on that date becomes `NO_SHOW` and consumes a session.** Re-running it across the
customer's history would **mass-consume quota on a live system** while recovering the revenue.
⇒ **Back-posting needs a revenue-only mode.** Small, well-shaped BE task. **Nobody re-runs a past date on `uat`
until it exists.**

**Ball: owner** — the two counting queries (how many trial/single bookings exist, and how many `rev:` rows are in
`bo.movement`), so we size this before deciding. **@Sober** — add to the REQ-057 batch, and this one outranks the
other three: **revenue-only day-end mode** for the back-post. **@Tanya** — the cut rehearsal on `sid` is now more
important, not less: it is the path we are about to switch on over live data.

## [--:--] Porter (PM) — ✅ **GREEN LIGHT — back-post `uat` day-end for 2026-08-22 only.** Blast radius proven zero.
The scare sized itself down honestly, and I am writing the number as plainly as I wrote the warning.

### The exposure is ฿4,170, not a catastrophe
`uat`: `job_runs` **empty** · `bo.movement` `rev:%` = **0** ⇒ the day-end job has **never run there**. But the
bookings that depend on it are **four in total, ever**: **3 × `FIRST_TRIAL` `ATTENDED` on 2026-08-22** and one
`SINGLE_SESSION` `SICK_LEAVE` (which never posts by design — the job only posts `ATTENDED`).
⇒ **Missing revenue = 3 × ฿1,390 = ฿4,170**, less any discount. **The customer sells COURSES** (60 of them), and
course revenue posts **at sale**, nowhere near this job. **I raised the alarm louder than the number deserved.**
The structural finding was right; the scale was small, and saying so is part of reporting it.

### Blast radius of the fix — measured, not assumed
`bookings` on **2026-08-22** (`uat`): **`PENDING` 4 · `ATTENDED` 56 · `CONFIRMED` 0.**
The cut selects **only `CONFIRMED`** (`jobs.service.ts:42`). ⇒ **nothing to cut. No status flips, no quota moves.**
The one date that needs back-posting is the one date where the dangerous half is provably inert.

### 🚦 UAT GATE — written block, per the owner's 2026-08-19 rule
- **Build:** the current `smart-scheduler-back` on `uat` (the REQ-069 deploy earlier today; carries REQ-063/066).
- **Action:** `POST /internal/jobs/end-of-day` with **`{"date":"2026-08-22"}`**, **once**. No deploy, **no migration**.
- **Tested:** the day-end posting path is proven on `sid` — **by the owner's own seven manual runs today**
  (`+1,390` undiscounted, `+1,690/−391` discounted) and **Tanya's REQ-063 `TEST_PASSED`** (TEST-057, ledger
  owner-attested). Same code, same build.
- **NOT tested:** this job has **never executed on `uat`** — the *environment* is unproven even though the code is.
  The realistic failure is **`INTERNAL_JOB_SECRET` unset ⇒ `503 NOT_CONFIGURED`** (`internal.ts:21`), which is a
  **refusal, not a bad write** — so attempting it is safe by construction. Read the response body.
- **Rollback: there is none, and I am not pretending otherwise.** `recordSale` writes ledger rows; undoing them is
  a manual finance correction of 3 rows. Mitigation is that the amount is *derived from the catalogue*, not typed,
  and the run is idempotent (`rev:<bookingId>`) so it cannot double-post.
- **Customer impact:** **none visible.** No booking changes status, no quota moves, no message is sent. Three
  revenue rows appear that should already have been there.
- **Names: Porter — GREEN.** **Tanya — her half is met by the `sid` pass on this build**; she **cannot** verify on
  `uat` (the 14:38 access boundary, which stands). I am recording that as *evidence-from-`sid`*, **not** as a
  Tanya sign-off on `uat`, so nobody reads this later as a gate we waived.

### After the run — the two checks that make it true
`bo.movement` `rev:%` must be **3**, and the `job_runs` row for 08-22 must show **`noShow 0 · coursesCut 0 ·
revenuePosted 3`**. **If `coursesCut` is anything but 0, stop and tell me** — it would mean the date I sized is
not the date that ran.

⚠️ **This authorises ONE date.** Re-running any other past date on `uat` still needs the revenue-only mode —
that trap is unchanged, only this single day is provably exempt.

**Ball: owner** — run it, send the response body + the two checks. **@Sober** — REQ-057 + the four small BE items.
**@Tanya** — the `sid` cut rehearsal, still the prerequisite for switching the nightly task on at `uat`.

## [Jason (BE)] — TASK-177 (`course:cleanup`) BUILT → REVIEW. A delete tool for a live box. → @Sober @Porter
Built to be judged by what it **declines** to do.

**The rules are pure and unit-tested (15), the script only does IO.** A delete tool whose safety rules can only
be checked by running it is not a safe tool. All four refusals, the household guards and the blast-radius
computation are asserted without a database.

**Refuse, never warn — `--commit` overrides nothing.** The refusal **throws inside the transaction**, so a
refused run rolls back on its way out rather than trusting an early return, and **every reason prints at once**
(there is a test for it): the owner should not discover them one run at a time.

**`usedSessions > 0` is deliberately NOT a refusal**, with the reason written beside it — on an IMPORT course
that is REQ-064's prior-taught count, and the very course he needs to delete has `usedSessions = 4`. Refusing on
it would make the tool useless for its one job. `ATTENDED` is the honest test for "something really happened".

**Posted sales are checked against the course id AND every booking id** — a course sale hangs off the course,
day-end revenue off the booking. Checking one would let a delete leave the books pointing at nothing. (Given
today's finding that the day-end job has never run on `uat`, that second check will currently find nothing there
— which is a reason to keep it, not to drop it.)

**🔴 The one thing that would have gone silently wrong:** `bookings.course_id` is `SET NULL`, so deleting the
course first **orphans** its bookings instead of removing them — they stay on the calendar, detached, and the
count still looks right. Bookings are deleted explicitly, and first.

**`--remove-household` is a separate decision**, and its refusal blocks only that part — the course cleanup
still runs. Tested, because "clean up the fake course" must never quietly become "and delete a family".

**Console-only, no report file** — unlike every other audit here. This output names a child and their parent,
and there is nowhere to write that which is worth the risk. **Q1** asks whether you'd rather it printed
nicknames only; I judged recognition worth it *for a delete tool*, since the owner has to know the household he
is removing.

**Verified:** tsc **0** · `bun test` **731 pass / 0 fail** (+15). No migration, no FE. ⚠️ **I ran nothing against
a database** — every test is against the pure planner, so **the SQL itself is un-exercised until the owner's dry
run on `sid`.** Saying that plainly rather than letting "731 green" imply more than it does.

**@Porter — for the owner, `sid` first, dry run first:** `bun run course:cleanup --course <uuid>` prints the
blast radius and deletes nothing; `--commit` only after he has read it. A second `--commit` reports "ไม่พบคอร์ส"
and exits non-zero — deliberate: it deleted nothing, and a tool that says "done" when it did nothing is how you
lose track of what you have run.

**Ball: @Sober** — review + Q1 (names vs nicknames in the dry run). **@Porter** — the runsheet above, and note
this is the REQ-057 batch's first item; the **revenue-only day-end mode** you flagged today is the one that
outranks it, and I have not been given it yet. **@Fern** — untouched.

## [--:--] Porter (PM) — 🟢 **Back-post DONE on `uat`. Every predicted field matched.** ฿4,170 recovered, nothing disturbed.
Owner ran `end-of-day` with `{"date":"2026-08-22"}` on `uat`. Result against the prediction I wrote before the run:

| field | predicted | actual |
|---|---|---|
| `noShow` | 0 | **0** ✅ |
| `coursesCut` | 0 | **0** ✅ |
| `vouchersCut` | 0 | **0** ✅ |
| `revenuePosted` | 3 | **3** ✅ |

And the job's own report reconciles with the query I sized it from: **`totalBooked 60 · attended 56 · pending 4 ·
cancelled 0`** vs the SELECT's **`ATTENDED 56 · PENDING 4`**. **Same 60 rows, counted twice by two different
mechanisms, agreeing.** That is what made the green light honest rather than hopeful.

**Still owed before I call it proven:** the **ledger side**. `revenuePosted` counts `res.ok`, which the team now
knows includes replays — so the job's own number is *not* the proof. `bo.movement` `rev:%` must read **3**, with
amounts. Same discipline the owner's own SELECTs have set all week: **the job's summary is a claim, the ledger is
the fact.**

### 📌 Something the numbers taught us, and it re-sizes the remaining risk
**`uat` had ZERO `CONFIRMED` bookings on 08-22 — 56 were `ATTENDED`, 4 `PENDING`.** The auto-cut only ever selects
**`CONFIRMED`** (`jobs.service.ts:42`). Two consequences:
1. **The nightly job is far less dangerous on `uat` than I framed it** — the customer's staff appear to mark
   attendance directly, so the status the cut feeds on is rare there. **Sizing query sent** rather than assuming
   one day generalises.
2. It also explains `courses_cut = 0` across every `sid` run — **not only low traffic; the status simply is not
   there.** Which means the cut path is unexercised for a *structural* reason, so Tanya's rehearsal must
   **deliberately create a `CONFIRMED` session**, not wait for one to appear.
3. ⚠️ And **`PENDING` is never cut either** — those 4 rows from 08-22 will sit forever. Not a defect, but worth
   knowing that the job tidies only one of the three states.

**Bounded by design, worth stating:** the scheduled run takes **no date ⇒ today only**. Switching it on at `uat`
**cannot** sweep history — the worst it can do is one day at a time.

**Ball: owner** — the ledger check (2 queries). **@Sober** — REQ-057 + the four BE items. **@Tanya** — the `sid`
cut rehearsal, now with the note that she must *create* the `CONFIRMED` state herself.

## [--:--] Porter (PM) — 🔴 **DO NOT enable the nightly job on `uat` today.** All 15 `CONFIRMED` bookings are dated TODAY.
The back-post is closed and clean; this is the next thing, and it is live in a few hours.

### ✅ Closed first: the ledger proves the back-post
3 rows, **`First Trial (1h)` ฿1,390 × 3 = ฿4,170**, posted 19:00:24 on `uat`. `actor`/`note` null ⇒ undiscounted,
correct. `qty −1` + positive `value_minor` matches the convention the owner attested for REQ-063.
**Bonus: REQ-066 AC-2 is now half-proven for free** — day-end posting is demonstrated **on `uat` itself**, not
inferred from `sid`. What remains of AC-2 is only the Bike single-session case.

### 🔻 Correction to my own read, one message old
I said *"`CONFIRMED` is rare on `uat`"* off a single day's numbers. **Wrong.** System-wide: **`PENDING` 532 ·
`ATTENDED` 62 · `CONFIRMED` 15 · `EXTENDED` 11 · `SICK_LEAVE` 6 · `CANCELLED` 2.** 08-22 having zero was the
exception, not the pattern. **Generalising from one day is the same mistake I made twice already today.**

### 🔴 And the follow-up query is the one that matters: **all 15 `CONFIRMED` are dated `2026-08-23` — today**
The nightly task fires at **23:30 tonight**, selects `CONFIRMED` on today whose `end_time` has passed, flips them
to **`NO_SHOW`** and **increments `used_sessions` / `used_hours`**.
⇒ **Had we switched it on this afternoon, tonight it would have consumed 15 sessions from the customer's courses,
silently, on a Sunday — their busiest day.** This is no longer a theoretical guardrail. **It is what would have
happened, tonight, to a live customer.**

### The business question this raises, which is the customer's and not ours
15 sessions sit `CONFIRMED` at 19:00 on the day they were taught. Two readings, opposite consequences:
- **The children attended and staff simply have not pressed `มาเรียน`** ⇒ marking them `NO_SHOW` would be
  **factually wrong**, would consume quota the family already used correctly, and (for trial/single) would still
  post no revenue since revenue needs `ATTENDED`.
- **They genuinely did not show** ⇒ `NO_SHOW` + quota consumed is exactly right, and the job is doing its job.
**We cannot tell from the data which it is, and it is not ours to decide.** Asked, not assumed.

### 📌 Second reading from the same numbers — it touches REQ-062's blocker
**`PENDING` 532 vs `CONFIRMED` 15.** In practice the customer barely uses the `ยืนยัน` step. That does not answer
REQ-062 Q4 (*"when do staff press it?"*) — `CONFIRMED` may simply be transient on its way to `ATTENDED` — but it
**reshapes the question into a much easier one: do they use that button at all?** A question that has blocked a REQ
for two days may be answerable by asking a simpler version of it. **Not concluding; handing it to Porter's queue.**

### Revised order for switching the nightly task on at `uat`
1. **Ask the customer about today's 15** — attended-but-unmarked, or real no-shows?
2. If unmarked: staff mark them, and we learn whether `มาเรียน` is part of their daily routine at all.
3. **@Tanya rehearses the cut on `sid`** — creating the `CONFIRMED` state herself, since it does not occur naturally.
4. **Then** register the task on `uat`, with the customer told what 23:30 does.
**Not tonight.**

**Ball: owner** — the question to the customer about today's 15. **@Sober** — REQ-057 + the five BE items.
**@Tanya** — the `sid` cut rehearsal.

## [--:--] Porter (PM) — 🔴 The 15 are a REAL taught Sunday. And they answer REQ-062's blocking question. → @Sober
The list is not ambiguous. **All 15 are `COURSE_PACKAGE`**, running **10:00 → 17:00 unbroken**, 15 named children,
mostly **Surfskate** (9) plus Inline Skate 3 · Bike 2 · Skateboard 1, with 2–5 students stacked in the busy slots.

**It is not credible that fifteen children across seven hours of a packed Sunday all failed to turn up.**
⇒ These sessions **were taught**, and staff **have not pressed `มาเรียน`**. It is past 19:00; the last one ended at 17:00.

### What tonight's job would have done — precisely
**Its quota effect would be RIGHT. Its label would be a LIE.**
- ✅ Those 15 sessions **should** draw down their packages — they were delivered. Nothing has drawn them down.
- 🔴 But the job writes **`NO_SHOW`**, which records **fifteen children who attended as absent** — in the daily
  report, in course history, in whatever the customer or a parent ever reads back.
**The auto-cut was designed for the occasional no-show. Pointing it at a day where marking simply never happened
turns it into a machine for mislabelling real attendance.** That distinction is the whole finding.

✅ **No money at stake here** — all 15 are `COURSE_PACKAGE`, and course revenue posts **at sale**. Confirmed: the
day-end revenue path only ever touches `FIRST_TRIAL` / `SINGLE_SESSION`.
🔴 **But quota is drifting.** Neither writer of `used_sessions` has fired for these — not `attend`
(`scheduler.service.ts:1905`), not the job. **Delivered sessions are not being deducted from families' packages.**

### 🔻 Correction to what I wrote one message ago
I said *"the customer barely uses the `ยืนยัน` button."* **Wrong, and backwards.** They used it on **15 of 15**
of today's sessions. What they do not use is **`มาเรียน`**. I inferred a behaviour from a ratio without looking at
which rows made it up — **the fourth time today I have reported a shape instead of reading the contents.**

### 📌 And this is the strongest evidence yet on **REQ-062 Q4**, blocked for two days
> *"เมื่อไหร่ที่สตาฟกด `ยืนยัน + แจ้งเตือน Line`?"* — at course creation / the day before / **on the day**.

**The data leans hard on "on the day":** **532 `PENDING`** future sessions sit unconfirmed, while **every one of
today's 15 is `CONFIRMED`.** If they confirmed at course creation, those 532 would be `CONFIRMED` too. They are not.
⇒ **Strong evidence for "on the day."** Not proof — the owner should still put it to the customer in one sentence —
but it converts an open design question into a **yes/no confirmation**, which is a very different ask.
**@Sober: this may unblock REQ-062. Do not spec on my inference — wait for the owner's confirmation.**

### The cheap decisive test, costs nobody anything
**Re-run the same query tomorrow morning.** Still `CONFIRMED` ⇒ staff do not mark attendance at all, and the real
REQ is not the scheduled task — it is *"how does a taught session get recorded here?"* Flipped to `ATTENDED` ⇒
they mark late, and the nightly job just needs to run **after** they finish, not at 23:30 by assumption.

**Ball: owner** — two questions to the customer: (1) were today's 15 taught? (2) do staff press `มาเรียน` at all,
and when? Plus re-run the query tomorrow. **@Sober** — REQ-057 + the five BE items; REQ-062 may be about to open.
**@Tanya** — the `sid` cut rehearsal.

## [--:--] Sober (SA) — TASK-177 (course:cleanup) = PASS ✅. The delete tool is safe by construction. → @Jason @Porter
Reviewed by reproduction: tsc **0** · `bun test` **731/0** (cleanup plan 15/0). Jason applied both my grounded
findings and hardened beyond them:
- **All refusals pure + tested (15):** Test-course passes; **`usedSessions>0` is NOT a refusal** (REQ-064 prior-
  taught — even 9 passes); ATTENDED refuses and names the date; posted sale (checked on course id **and** every
  booking id) refuses; LINE-linked parent refuses; >1 child refuses; **all four reasons at once**; `--remove-household`
  refuses if the student owns anything else. Exactly the Q2 ruling.
- **The SET NULL trap is handled** (`cleanup-course.ts:135`) — bookings deleted explicitly and first, then the
  course; one tx; refusal throws inside the tx so `--commit` overrides nothing.
- Console-only, no report file; never touches subjects/teachers/bo.item/other rows; no DDL.
- Honest caveat: the SQL is un-exercised until the owner's `sid` dry-run — the pure planner carries the safety and
  it's fully tested. **DONE (code).**

**Q1 — full NAMES in the dry-run, endorsed.** A delete tool must let the owner recognise the exact household he's
removing; it's console-only on his own terminal (never a tracked file), so the PII rule holds.

**Ball: @Porter** — for the owner: run `course:cleanup --course <Test course id>` on `sid` first, **dry-run → read
the named blast radius → `--commit`**; the `SOM Team`-parent-too choice is `--remove-household` (guarded), his call.
Running against `uat` stays his separate decision. **Next on my SA queue:** you flagged the **revenue-only day-end
mode** as outranking the other three BE items (`month-reset`→no job_runs · `job_runs` wiped by `db:reset` ·
`revenuePosted` over-counts) — I'll take that next unless you re-rank vs REQ-068. **@Jason** — BE clear. **@Fern** —
TASK-167 → TASK-176. Nothing routes to the owner from me.

## [--:--] Porter (PM) — 🟢 **Scheduled tasks are INSTALLED on `uat`.** All three, verified on the Task Scheduler screen.
Owner installed them tonight. Confirmed from his screenshot of the Task Scheduler Library — **the screen, not a
command's exit code**:

| task | trigger | next run | status |
|---|---|---|---|
| `sm-daily-digest` | 8:00 AM daily | 24/08 08:00 | Ready ✅ |
| `sm-end-of-day` | 11:30 PM daily | **23/08 23:30 — tonight** | Ready ✅ |
| `sm-month-reset` | 12:05 AM, day 1 monthly | 01/09 00:05 | Ready ✅ |

`Last Run Time` reads `11/30/1999` on all three = Windows' "never" placeholder. Correct for fresh tasks.

**Manual proof taken before registering** (the 08-01 discipline, and it worked again):
`end-of-day` returned real JSON at 19:00 (the ฿4,170 back-post) · **`daily-digest` returned `sent: True`** with live
checks — `unconfirmed_bookings 14`, `teachers_without_line 22`, expiring entitlements. `month-reset` deliberately
**not** test-run: mid-month it would wipe this month's freelance drawdown.

### 📌 `sent: True` closes REQ-049 AC-1 for free
The digest **reached an admin on LINE**. We have been carrying *"owner must register an admin via the bot so a
notification has a recipient"* as an open item owed by the owner. **It has a recipient.** Closing it, and it also
retires the *"approving an admin needs an admin"* chicken-and-egg I raised off the `รอการอนุมัติ` screenshot —
**that worry is dead, not deferred.**

### On the record, without re-arguing it
`sm-end-of-day` was registered **without `/sd`**, so its first run is **tonight at 23:30**, and today's **15
`CONFIRMED` `COURSE_PACKAGE` sessions** are what it will find. The owner was told the consequence in full, chose to
proceed, and said to leave the past sessions alone. **That is his call and it is recorded, not re-litigated.**
Tomorrow's `job_runs` row will show exactly what happened — `noShow` and `coursesCut` will read 15 if it swept them.

### Tomorrow morning, two checks
`job_runs` should hold **two new rows** (end-of-day 23:30, daily-digest 08:00), and Task Scheduler's **Last Run
Result** should read `0x0` on both. Anything else and the trigger fired but the script did not.

**Ball: owner** — the morning check. **@Sober** — REQ-057 + the five BE items. **@Tanya** — the `sid` cut rehearsal
(still worth doing: `uat` now runs this nightly and nobody has ever watched the cut path work).

## [--:--] Porter (PM) — 🧪 `course:cleanup` first real run — on **`sid`**, dry-run. **It refused, correctly.** → @Sober
Owner ran `course:cleanup --course 3bf29f87…` against **`sid`** (confirmed by him). Target was **`KKTEST`**, not the
`uat` `Test`/`SOM Team` household — a local test course: 10 sessions, `IMPORT`, 6 bookings 08-23 → 09-27.

**Output: `ปฏิเสธ — ไม่ลบอะไรทั้งสิ้น: มีคาบที่เรียนไปแล้ว (ATTENDED) 1 คาบ (2026-08-23)`.**
✅ **The guard works, and it names its reason and the date.** Blast radius printed by name first, exactly as specced.

### 📌 The finding this rehearsal bought us — and it is the same shape twice
**The owner marked `มาเรียน` while testing, so his test course now looks real to the guard.**
This is **Sober's `usedSessions` ruling all over again**: the signals that separate *fabricated* from *real* break
down against **a tester who tests thoroughly**. The better you exercise a test course, the less deletable it becomes.
Not a defect — the refusal is right by its own rule — but worth stating as a known edge, because the `uat` `Test`
household could acquire an `ATTENDED` row the same way and lock itself the same way.

### 🔴 And the consequence for what is proven
The tool **refused before reaching the delete**, so **the DELETE SQL is still un-exercised** — exactly the caveat
Sober flagged at review. **We have proven the guard, not the deletion.** To prove the other half, either:
- **(a)** cancel the mis-marked `08-23` session in the product (TASK-105 allows undoing a mis-marked check-in with
  a reason, and returns the consumed unit), then re-run → dry-run should list 6 bookings → `--commit`; or
- **(b)** create a fresh `sid` test course, mark nothing, and run it straight through.
**(b) is cleaner** — it exercises the happy path without first exercising a correction path to get there.

### Nit for @Sober
`ผู้ปกครอง: null` — a tool whose whole job is *"read this before you delete a household"* should print `ไม่มี`,
not a programmer's `null`. One line, and it belongs in the same file.

**Ball: owner** — (a) or (b) on `sid`, whichever he prefers, to prove the delete half. **@Sober** — the `null`
display nit + REQ-068 as the lane. **@Fern** — TASK-167 → TASK-176.

## [--:--] Porter (PM) — 🔴 **REQ-057 cannot do the thing REQ-057 was asked for. My requirement error.** → @Sober
Owner created a fresh `sid` test course (option (b)) and ran `--commit`. Refused again, **different reason**:

> `ปฏิเสธ — มีรายการขายที่ลงบัญชีแล้ว 1 รายการ — ลบคอร์สจะทำให้บัญชีอ้างถึงสิ่งที่ไม่มีอยู่`

**The course shows `SALE`, not `IMPORT`.** Creating a course through the product calls `recordSale` at creation
(`scheduler.service.ts:1182`). ⇒ **every normally-created course has a posted sale the instant it exists**, and
"posted sale" is one of the four refusals. **So the tool refuses 100% of normally-created courses, by construction.**

### The collision, and it is mine
REQ-057's opening line is the owner's: *"เขียนสคริปเคลียร์ข้อมูลที่จะเกิดขึ้นหลังจากสร้างคอร์ส พอดีจะลองสร้างที่ uat"* —
**create a course the normal way, then remove what it created.** I then wrote the guardrails in the 08-22 UPDATE,
including *"refuse … a posted sale"*, **without noticing that course creation always posts one.** Two use cases got
merged under one REQ and only one of them survives:
- **(A) delete the imported `Test` residue on `uat`** — `source = IMPORT`, no sale ⇒ **works as designed.**
- **(B) create a test course, then delete it** — **impossible with this tool, always.** And (B) is the sentence the
  REQ opens with.

**Sober's Q2 ruling was right, Jason's build is right, the refusal is right.** The requirement was wrong, and it was
wrong in the exact way I have been wrong all day: **I wrote a rule from reasoning instead of checking what the
product actually does when you press the button.**

### The refusal message names its own fix
*"ลบคอร์สจะทำให้บัญชีอ้างถึงสิ่งที่ไม่มีอยู่"* — the concern is an **orphaned ledger row**, not the deletion itself.
⇒ The principled answer is **not** to weaken the guard but to **reverse the sale in the same transaction**: delete
bookings → course → write the reversing `bo.movement`, or refuse as a unit. **@Sober — that is a design call with
money in it, so it is yours, not mine.** Options I can see, ranked by how little they cost the guard:
1. `--reverse-sale` (explicit, guarded, one tx, prints the reversal amount in the dry-run alongside the household).
2. Refuse only when the sale **cannot** be cleanly reversed (partially consumed, discounted, already reconciled).
3. Leave it: (B) is simply not supported, and testing on a live box means using an `IMPORT`-shaped fixture.
**I lean 1**, because (B) is the owner's original ask and "test it then undo it" is a capability this team keeps
needing — Tanya has hit the same wall three times.

### Meanwhile, to prove the delete half TODAY
Use the **first** `KKTEST` course — `source = IMPORT`, so **no sale**. Clear its one `ATTENDED` row (TASK-105
correction) and the tool should run straight through. **That proves the SQL, which is still un-exercised.**

**Ball: @Sober** — the reverse-sale design call, ahead of REQ-068 in my ranking now: REQ-057 is currently a tool
that cannot do its headline job. **Ball: owner** — nothing broken, nothing deleted; `sid` counts unchanged
(students 133 · parents 111 · courses 10 · bookings 76).

## [--:--] Porter (PM) — 📌 @Sober — your lane is **REQ-068**. I am re-ranking my own call from an hour ago.
**I said reverse-sale should jump ahead of REQ-068. Withdrawing that.** On reflection:
- `course:cleanup` **works for the one target that actually exists** — the `Test` / `SOM Team` household on `uat` is
  `source = IMPORT`, no posted sale. **The named job is doable today.**
- What it cannot do is *"create a course then undo it"*. That is a **limitation we can live with**, not a blocker —
  nobody is waiting on it, and the owner is not blocked by it tonight.
- **REQ-068 is customer-facing product work that has been sitting at #2 all day and has not started.**
⇒ **REQ-068 + re-spec TASK-142 is the lane.** Reverse-sale becomes a normal queue item behind it.

### The brief, unchanged from the owner's own framing
The owner caught this **before** we built into it: **REQ-052 (program + booking type) and REQ-068 (session note)
land on the SAME calendar cell**, alongside badges and rentals. **TASK-142 must be re-cut so the cell is built
ONCE**, carrying: program + booking type · the **display toggle** · the **session note**.
- **REQ-068 Q1/Q2 are both answered "ไม่"** (owner, today) — scope is smaller and sharper than when it was raised.
- **Palette approved: no emoji, icons only** (REQ-052 AC-9).
- Requirement file: `requirements/REQ-068-session-note.md`. The collision is written up in today's log at the
  Porter entry *"🆕 REQ-068 (session note) + REQ-052 AMENDED with a display toggle"*.

### Behind it, in order
1. **`--reverse-sale`** for `course:cleanup` (the design call with money in it — mine was the requirement error,
   the fix shape is yours).
2. **Revenue-only day-end mode** — for back-posting a past date without the cut mass-consuming quota.
3. Four small BE items: `month-reset` writes no `job_runs` · `job_runs` is wiped by `db:reset` · `revenuePosted`
   counts replays · `ops.stock_movements` should be marked deprecated in the schema file (I mis-read it for
   `bo.movement` today and handed the owner a broken query).
4. `course:cleanup` display nit: `ผู้ปกครอง: null` → `ไม่มี`.

**Ball: @Sober** — REQ-068. **@Fern** — TASK-167 → TASK-176, still `TODO`, still parallel. **Nothing routes to the
owner from me tonight** — the `uat` tasks are installed and running, which was what he asked for.

## [--:--] Sober (SA) — REQ-068 + REQ-052-amended re-spec: cut the note foundation, re-cut the cell as ONE component. Q3 decided. → @Jason @Fern @Porter
Took REQ-068 as the lane. Grounded Q3 first, and it's decisive:
- 🔴 **Q3 — `bookings.note` is OCCUPIED, so the session note needs a NEW column.** `note` is the system's
  status-reason/audit field — cancel/sick-leave/auto-extend all write it (`scheduler.service.ts:1595/1737/1927/1990/2031`).
  Sharing it would make a leave reason clobber the "bringing 2 siblings" note and vice versa — silent loss on both.
  ⇒ **`bookings.attendee_note`**, kept entirely separate. (Q1/Q2 already "ไม่" from the owner: not in the daily
  report; editing enqueues no notification → AC-8 empty outbox.)

**Cut SPEC-063 unifying REQ-068 + REQ-052-amended, and three tasks — the cell is built ONCE, per your call:**
- **TASK-178 (BE, foundation):** `attendee_note` column + store on all 4 types + per-session edit (only that
  booking, AC-3) + no-notify-on-edit (AC-8) + on the DTO + teacher LINE shows it (rides REQ-067B). Everything else
  depends on this DTO.
- **TASK-142 (FE, RE-CUT):** the unified cell — program+type (REQ-052) **and** note (REQ-068) in one component,
  behind a **display toggle** over the fixed five-item set (type·program·badge·note·rental; time+name always;
  default all-ON; display-only, no filtering). Icons-only/no-emoji (palette signed). The original REQ-052 detail
  stays in force; the re-cut adds the note + toggle rather than replacing it. Depends TASK-178.
- **TASK-179 (FE):** the note input on the 4 forms + manage-course per-session + booking detail + admin day view;
  ~200-char + not-for-PII wording (Porter drafts). Depends TASK-178.

**Ball: @Jason** — TASK-178 (the column/DTO/LINE — unblocks both FE tasks). **@Fern** — TASK-167 → TASK-176, then
TASK-142 (re-cut) + TASK-179 once 178's DTO lands. **@Porter** — the not-for-PII note wording is yours to draft
(REQ-068 constraint); and the queue behind this stays: reverse-sale · revenue-only day-end · the four small BE items.
Nothing routes to the owner from me.

## [Jason (BE)] — TASK-178 BUILT → REVIEW. **Both FE tasks are unblocked.** → @Sober @Fern @Porter
The note foundation, with AC-8 made structural rather than promised.

**`0022`** — one nullable `attendee_note`, no back-fill. The migration header records **why it is not `note`**,
because that is exactly the decision a future reader would quietly undo: `note` is what the *system* did to a
session (cancel reason, leave, auto-extend); `attendee_note` is what a *family* told us about it. Two facts,
two authors, and sharing one column would have each silently erasing the other.

### 🔴 The design choice worth your review: a separate route, not a field on `PATCH /bookings/:id`
The move path re-times a session **and tells the teacher about it**. Adding `attendeeNote` to it would have made
*"fix a typo in a note"* push a LINE message to a teacher. `PATCH /bookings/:id/note` exists so that AC-8 is a
**property of the code's shape**, not a line in a review.

**And AC-3/AC-8 are asserted at the source, deliberately.** `attendee-note.test.ts` reads `setAttendeeNote`'s own
body and fails if it contains `enqueue`/`notify`/`outbox`, if it writes `note:` or `status:`, or if it targets
anything but `where(eq(bookings.id, id))`. *"We didn't call notify"* is true the day it is written and quietly
stops being true when someone consolidates the two routes "for consistency" six weeks from now.

⚠️ **Stated plainly: I could not write the DB-level AC-3/AC-8 tests without a database.** The source assertions
are the strongest guard available from here — I would rather say that than let **743 green** imply an end-to-end
proof it does not give.

**Smaller calls:** `null` clears a note, an omitted field does not (a `.optional()` would make "clear this"
indistinguishable from "don't touch it"). A course carries one note onto every session it creates, and the
per-session edit then changes exactly one — Q1 flags that this means editing one leaves the other nine saying
the old thing, which is correct per AC-3 but may surprise staff; worth a word in TASK-179's copy. On LINE the
note is indented under its own session, and a session **without** one is **byte-identical to before** — asserted
by string equality, whitespace-only notes included.

**Verified:** tsc **0** · `bun test` **743 pass / 0 fail** (+12). ⚠️ Nothing run against a database.

**@Porter — `0022` is owner-run, `sid` first, and this one is MIGRATION BEFORE CODE**: until the column exists,
any write of it fails. That is the reverse of the last few and worth stating in the runsheet. Queue is now
`0020` · `0021` · `0022` (+ backoffice `0006`), plus REQ-065's flip SQL behind its deploy.

**Ball: @Fern** — `booking.attendeeNote` is on the wire and `PATCH /bookings/:id/note` takes `{ attendeeNote }`;
TASK-142 and TASK-179 are one prop and one call. **@Sober** — review + Q1. **@Porter** — the runsheet note above;
the queue behind this is still reverse-sale · revenue-only day-end · the four small BE items, none cut to me yet.

## [--:--] Sober (SA) — TASK-178 (session note BE) = PASS ✅. Both FE tasks unblocked; AC-8 is structural. → @Jason @Fern @Porter
Reviewed by reproduction: tsc **0** · `bun test` **743/0** (+12). Strong design:
- **AC-8 made structural, not promised:** a **separate `PATCH /bookings/:id/note`** (not a field on the notifying
  move path) means a note edit *cannot* push LINE — and `attendee-note.test.ts` asserts at the source that
  `setAttendeeNote` never enqueues/notifies, never writes `note:`/`status:`, and touches only its own booking id
  (AC-3). That's the strongest guard available without a DB, and Jason names the DB-level limit honestly rather than
  letting 743-green imply an end-to-end proof.
- `0022` nullable `attendee_note`, header records why it's not the status-reason `note`; DTO carries `attendeeNote`;
  LINE shows-when-present / byte-identical-when-absent; `null` clears vs omitted-untouched. **DONE (code).**
- ⚠️ **Runsheet: `0022` is migration-BEFORE-code** (reverse of the recent ones — the column must exist before the
  code writes it). Queue `0020 · 0021 · 0022` (+ backoffice `0006`) + REQ-065 flip SQL.

**Q1 (course seeds the note onto all sessions at creation; edit changes one):** AC-3-correct, acceptable — routed to
TASK-179 as a copy/UX call (omit the note on the course *creation* form, per-session only via manage-course, is the
cleanest; single-booking types take it at creation normally).

**⇒ REQ-068 BE done; both FE tasks unblocked.** **Ball: @Fern** — TASK-167 → TASK-176, then TASK-142 (re-cut,
`booking.attendeeNote` is on the wire) + TASK-179 (`PATCH /bookings/:id/note` takes `{attendeeNote}`). **@Porter** —
the `0022` migration-before-code runsheet note; the not-for-PII note wording (REQ-068 constraint) is yours to draft;
queue behind: reverse-sale · revenue-only day-end · four small BE items. **@Jason** — BE clear. Nothing routes to the
owner from me.

## [--:--] Fern (FE) — TASK-167 + TASK-176 done, in the order you set. → @Sober
### TASK-167 — one word, but applied to the row, three times
`<Group grow align="flex-start">` → **`flex-end`**. A Mantine `Input.Wrapper` renders label → description → input, so
aligning the row at its **end** puts the input boxes on one line **whatever height the helper text takes** — including
when it wraps at 375. `flex-start` aligned the wrapper *tops*, which is exactly why only the field with a two-line
helper sat lower.

🔴 **The part I think matters:** I applied it to **every field row in that form, not just the reported one.** Of the
three rows, one mixes a described field with an undescribed one (the bug the owner saw), one has descriptions on
**both** (breaks the moment they wrap to different heights), and one has **neither** (breaks the day someone adds a
helper). Fixing only the box that was pointed at would have left two more of the same bug sitting there — which is
precisely what Porter's "fix the ROW" note was guarding against. No reserved slots, no height hacks, **no new copy**.

### TASK-176 — four dictionary lines, and I checked AC-3 before touching anything
`1st Trial` / `1 HR` in **both** dictionaries — **the Thai entries carry the same English strings deliberately**
(AC-1): they're product names off the printed card, not phrases to translate.

I grepped first rather than trusting the task's list of surfaces: every staff-facing place that names a booking type
renders through the `bookingType.*` key family — chip (calendar cell, bookings table, booking detail), the tabs, the
table filter, and all three Overview call-sites — **one definition per language**, so four lines really is the whole
change. I also grepped the old literals: the only hits are a mock *sentence* and two source comments, neither
user-facing, so both stay. AC-2 holds — the enum values, data, reports and sale codes are untouched, which the
request-body tests confirm since they assert `bookingType` on the wire.

**Evidence (both):** `bunx tsc --noEmit` **0** · `bun run build` **ok** ·
`bun test src/lib/scheduler/ src/services/` **40 pass / 0 fail** · §3.5 clean.
🔴 Neither is rendered-verified: 167 is a Mantine modal (headless pane), 176 is a read-through. Both → @Tanya. For
167 specifically: **narrow to 375 until a helper wraps to two lines** — that's the case the fix exists for and the
case the old code fails.

**Ball: @Sober** (review both). Next in my lane per your order: **TASK-142** (re-cut, `booking.attendeeNote` now on
the wire) then **TASK-179** (`PATCH /bookings/:id/note`) — 142 still needs Porter's palette sign-off, so tell me if
179 should go first.
