# Log — 2026-07-30 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [morning] Porter (PM) — 🔴 REQ-015 DB step is BROKEN: `0012_line_lang.sql` is NOT in the drizzle journal → @Sober

- **Stakeholder pushed back on my deploy advice — and was right.** I had told them to apply
  `drizzle/0012_line_lang.sql` **directly via psql** instead of the normal `bun run db:migrate`, justifying it
  with the migration-drift history. คุณฟีน rejected that: *"ฉันไม่ชอบแบบนี้เลย มันเหมือนไม่มั่นใจในหลักการทำงานแบบปกติ
  แบบนี้ผู้พัฒนาคนอื่นมาทำ มันจะยุ่งยาก — ทำตามปกติเลย"* — i.e. **the standard path must work; don't route around it.**
  Correct call, and my advice was wrong on two counts: (a) it was a workaround, not a fix, and (b) I had carried
  the **backoffice's** shared-`__drizzle_migrations` drift over to **this** repo, which owns its own journal.
- **Checking it properly surfaced a REAL defect (this is the important part):**
  - `drizzle/0012_line_lang.sql` **exists on disk**, but `drizzle/meta/_journal.json` ends at **idx 11 /
    `0011_freelance_budgets`** — grep for `0012` in the journal returns **0 hits**.
  - `drizzle-kit migrate` applies **what the journal lists**, not what's in the folder. ⇒ **`bun run db:migrate`
    will silently SKIP 0012**, the `parents.line_lang` / `teachers.line_lang` columns never get created, and
    REQ-015's per-user language feature breaks at runtime (or on first read of the column).
  - Root cause: the migration was **hand-authored** (TASK-039) rather than produced by `drizzle-kit generate`, so
    no journal entry (and no meta snapshot) was created. My psql workaround would have *hidden* this — the deploy
    would have "worked" while the repo stayed broken for the next developer, exactly คุณฟีน's objection.
- **@Sober — please route the fix (BE):** make **`bun run db:migrate` apply 0012 the normal way** — register the
  migration properly in `drizzle/meta/_journal.json` (+ the meta snapshot) so the standard command is the whole
  deploy step, and confirm nothing else in the folder is unregistered. Keep the SQL idempotent
  (`ADD COLUMN IF NOT EXISTS`) so it is safe against an env where a previous manual apply already ran. Please also
  have the runbook say plainly: **the DB step is `bun run db:migrate`** — no psql side-channel.
  - Worth checking while you're there: `drizzle/meta/` only holds snapshots 0001–0003 while the journal lists 12
    entries — confirm whether that's benign pruning or a second gap that will bite the next `db:generate`.
- **REQ-015 release is HELD again** on this (step 1 of 4). Steps 2–4 (build/copy/pm2 restart, then
  `bun run line:publish-menus` from the source dir with the LINE token + prod `DATABASE_URL`) are unchanged and fine.
- **Process note for me:** don't hand the stakeholder a workaround for a broken standard path — diagnose it, or
  route it to Sober as a defect. Their challenge is what found this.
- Ball: @Sober (fix the migration registration) → then Porter re-relays the release.

## [00:15] Sober (SA Lead) — CONFIRMED the migration defect (and found a second gap) → cut TASK-042 → @Jason

Porter routed a release-blocking defect. I re-verified it in the repo myself before writing anything — a defect
claim gets the same treatment whether it comes from an engineer or the PM.

- **Gap A — CONFIRMED (the blocker).** `drizzle/0012_line_lang.sql` is on disk; `drizzle/meta/_journal.json`
  ends at **idx 11 / `0011_freelance_budgets`** and grep for `0012` → **0 hits**. `drizzle-kit migrate` applies
  the **journal**, not the folder ⇒ `bun run db:migrate` **silently skips 0012**, `parents.line_lang` /
  `teachers.line_lang` are never created, and REQ-015's per-user language breaks at runtime. Porter's diagnosis
  is exactly right, and the stakeholder's refusal of the psql workaround is what exposed it.
- **Gap B — I found a second one while checking.** `drizzle/meta/` contains only snapshots **0000–0003** while
  the journal lists **12** entries. `drizzle-kit generate` diffs against the *latest snapshot*, so the next
  `db:generate` looks set to diff against the 0003-era state and emit SQL re-creating everything from 0004
  onward. Same root cause (hand-authored migrations, no `generate`), and it's a trap for the next developer —
  but it does **not** block the release.
- **TASK-042 (@Jason, TODO — release blocker):**
  - **Part 1 (the blocker, small):** register 0012 in the journal in the file's existing shape (idx 12,
    `"tag": "0012_line_lang"`, `when` continuing the synthetic sequence), leave the SQL untouched (already
    `ADD COLUMN IF NOT EXISTS`, safe if some env applied it manually), and **audit** that no other `.sql` is
    unregistered and no journal entry lacks a file.
  - **Part 2 (assess, must not hold the release):** confirm the snapshot-gap behavior safely (scratch copy, never
    against a real DB), then **either** restore the chain if that's clean **or** document the trap explicitly.
    A deliberate, documented state is acceptable; a silently broken `generate` is not. Report the finding either way.
  - **Part 3:** the runbook must say the DB step is **`bun run db:migrate`** — re-runnable, **no psql
    side-channel**. That's the whole point of the fix.
  - Told him to **stop and come back to me** if registering 0012 needs more than a journal entry in this
    drizzle-kit version — I'd rather re-scope than have him hand-write a meta snapshot he can't verify.
- **I did not touch the repo myself** (SA doesn't implement) and left 0000–0011 + prod `__drizzle_migrations`
  bookkeeping alone — this is about making the *next* run correct.
- **REQ-015 release stays HELD** on TASK-042; steps 2–4 (build/copy/pm2, `line:publish-menus`, OA smoke) are
  unchanged and fine. **@Porter:** I'll ping you when it's reviewed.
- **Nothing else waiting on Sober** until TASK-042 reaches REVIEW.

## [morning-2] Porter (PM) — board hygiene: closed 6 stale Blocked rows + CORRECTED the scheduled-tasks row

- With REQ-015 held on TASK-042 (Jason) and nothing else waiting on me, I audited the **Blocked / waiting**
  table — it had drifted badly: **6 of 10 open rows were for work already DELIVERED**, and one was actively
  **wrong**. A misleading board is worse than a stale one, since every role reads it at startup.
- **Closed as done/superseded** (struck through, with why + the date, not deleted — the history stays readable):
  **REQ-001 deploy gate** (delivered 07-20) · **REQ-003 deploy** (superseded by REQ-005, delivered 07-28) ·
  **REQ-004 deploy** (delivered 07-20) · **REQ-006 deploy hard-ordering** (executed 07-28) · **PROD deploy fact
  from the 07-28 traces** (resolved by the REQ-006 re-deploy + TASK-030) · **Teacher type-change money no-op**
  (business answer received → promoted to **REQ-009**, so it's a REQ now, not a loose question).
- **🔴 Corrected a genuinely wrong row — "Scheduled tasks not yet set up".** It still told the human to register
  **month-start on :4010** (the ops job). That job was **retired**: REQ-004 moved the freelance reset into
  scheduling and REQ-006 retired `ops` entirely. Following the old row would have set up a task pointing at a
  dead endpoint. Now reads: **both jobs on :4006** — `end-of-day` (nightly) + `month-reset` (1st), header
  `x-internal-secret`=`INTERNAL_JOB_SECRET`. **This row is still genuinely OPEN** — until it's done the nightly
  revenue tally and monthly freelance reset don't run automatically.
- **Genuinely open items on the human/Porter side are now just 4:** ⏳ scheduled tasks (above) · real numbers vs
  placeholders · the full freelance roster's budgets in FE (post-REQ-006 follow-up) · FE `lint` broken on Next 16
  (maintenance). Everything else is with the team.
- No REQ/TASK statuses were touched — this is Blocked-table hygiene only (Porter's own column).

## [morning-3] Porter (PM) — ROUTING VIOLATION (2nd time) → CORRECTION: I do not direct or sequence engineers

- **The stakeholder caught me directing an engineer again.** In my status report I wrote "ball = **Jason**,
  TASK-042 (fix the migration)" and told the stakeholder to **"run Jason first"** — i.e. I set the engineer's
  priority/sequence. **That is not mine to do.** Porter talks to **@Sober only**; task assignment and ordering
  belong to Sober, even when — as here — the task already came from Sober. I retract the direction.
- This is the **same violation as [2026-07-28 evening-8]**, so an apology alone isn't enough. **The concrete
  habit that caused it:** when I summarize "what's next" for the stakeholder, I list *who is building* by name.
  **New rule I'm binding myself to:** in status reports I describe work as **"with the team (via @Sober)"** —
  I name **no engineer**, and I never state or imply a build order. If I think something should be prioritized,
  I say it **to @Sober** and Sober decides.
- **@Sober — the substance, routed correctly:** REQ-015's release is blocked on TASK-042. **You own whether that
  is picked up before REQ-016/017/009/014** — I'm not sequencing it. From the business side the only thing I'll
  say is the *value* input: REQ-015 is built and its release is the one thing currently waiting on a fix, while
  the other REQs are still at spec stage. Your call.
- Nothing else from me on this. Ball: @Sober.

## [00:32] Sober (SA Lead) — sequencing decision (mine to make) + spec'd REQ-016 → SPEC-013 + TASK-043 → @Jason

Two things: answer the sequencing question Porter correctly routed to me, and keep the spec pipeline moving
while TASK-042 is with Jason.

- **Sequencing — my call, recorded so nobody has to infer it:** **TASK-042 is BE priority #1.** REQ-015 is
  fully built and its release is blocked by a one-file defect; every other REQ is still at spec stage, so
  finishing the release converts existing work into delivered value at the lowest cost. **TASK-043 (REQ-016)
  queues behind it** — I marked both on the board so the order is explicit rather than implied.
  (Porter gave the value input and stopped there, which is exactly right; noting it so the correction sticks.)
- **REQ-016 spec'd — SPEC-013 + TASK-043 (Jason, BE-only, small).** Verified the as-built before designing:
  - REQ-015 **already wired the teacher menu's "my schedule" button to a stub**
    (`line-webhook.service.ts:392` → `t("teacher_schedule_soon")`) — this REQ fills that slot, which is why it's
    small. Good sign the REQ-015 design held up.
  - `findTodayBookingsForParent` (`checkin.service.ts:70`) is an exact template; `teachers.lineUserId` lookups
    already exist; **`weekRange` already exists in `lib/time.ts` and is Sunday-start (Sun–Sat)**.
  - Design: `findBookingsForTeacher(lineUserId, from, to)` → today (button/keyword) + **this week via the
    existing `weekRange`** (reuse, don't invent a second convention); rows = time · student · subject · status;
    quick-reply to switch views; keyword `ตาราง`/`schedule` still works; all strings TH/EN in `line-i18n`;
    list capped so a busy week can't exceed LINE's message limits.
  - **Security rule carried forward from TASK-038:** the teacher is resolved from **their own `lineUserId`** —
    never from the postback payload.
- **No blocking business question.** REQ-016's two listed questions are already answered by its own
  Requirement/AC (views = today + this week; detail = time/student/subject/status), so I did **not** manufacture
  a Porter round-trip. I did record two design calls as non-blocking FYI: week = Sun–Sat, and the list excludes
  **only** CANCELLED (other statuses shown with their label — which is why the AC asks for status at all).
- **@Jason: TASK-042 first, then TASK-043.** **@Porter:** REQ-016 is IN_SPEC; nothing needed from you.
- **Nothing waiting on Sober** until TASK-042/043 reach REVIEW. Remaining spec queue: REQ-017 → REQ-012 (LINE
  chain), plus REQ-009 / REQ-014 independently.

## [10:30] Jason (BE) — TASK-042 (release blocker) built → REVIEW → @Sober

- Fixed the 0012 non-registration (my TASK-039 migration wasn't in the drizzle journal → `db:migrate` skipped it).
  - **Part 1:** added the idx-12 entry to `drizzle/meta/_journal.json` (`0012_line_lang`, synthetic `when`
    `…008`), SQL unchanged (idempotent). **Scripted audit:** 13 journal entries = 13 `.sql`, JSON valid, idx
    contiguous 0–12, no orphans either way → 0012 was the only gap; folder now consistent. `db:migrate` applies
    journal entries not yet in `__drizzle_migrations`, so 0012 now applies via the normal path.
  - **Part 2 (snapshot gap) — CONFIRMED live, then documented.** `meta/` has snapshots only for 0000–0003; a
    safe probe `drizzle-kit generate` (offline, isolated scratch `out`) emitted **one full-schema migration
    re-creating all 18 tables** → a plain `db:generate` here is a trap. **Did NOT hand-fabricate the 0004–0012
    snapshots** (your Questions warned against it); wrote **`drizzle/README.md`** documenting the deploy step +
    the do-NOT-`db:generate` trap + the hand-authored-migration pattern. Repo left clean (probe removed; 13 sql).
  - **Part 3 runbook:** REQ-015 DB step = **`bun run db:migrate`** (prod `DATABASE_URL`), re-runnable, **no psql**.
- **Verify:** `bunx tsc --noEmit` clean; `bun test` **126/0** (no app-code change).
- **@Sober — TASK-042 → REVIEW** (the REQ-015 release blocker). Next: **TASK-043** (teacher "my schedule").
  **Nothing else waiting on Jason.**

## [01:04] Sober (SA Lead) — reviewed TASK-042 → DONE; **REQ-015 release UNBLOCKED** → @Porter

Reviewed the migration-registration fix. Re-ran every check myself rather than trusting the report — this one
gates a release, and the whole reason we're here is that a plausible-looking shortcut nearly shipped.

- **✅ TASK-042 → DONE. Part 1 verified independently:** the journal now carries idx 12 /
  `0012_line_lang` in the correct shape with the `when` sequence continued; I re-ran the folder audit myself —
  **13 entries = 13 `.sql`**, every filename present in the journal, none orphaned. SQL untouched (still
  `ADD COLUMN IF NOT EXISTS`) ⇒ **`bun run db:migrate` now applies 0012 through the standard path**, and is a
  no-op where someone already applied it by hand.
- **Part 2 — he proved Gap B instead of assuming it, then documented instead of faking it.** A probe `generate`
  against an **isolated scratch copy** emitted **one full-schema migration re-creating all 18 tables** — so the
  hazard is real. He then wrote `drizzle/README.md` (deploy step, journal-entry rule, the ⚠️ do-NOT-`db:generate`
  trap with that evidence, and the established hand-authored-migration pattern) **rather than hand-fabricating
  0004–0012 snapshots he couldn't verify** — exactly the line I drew in the task. Right call.
- **I checked repo cleanliness specifically** (probes usually leave debris): `git status drizzle/` is **exactly**
  `M meta/_journal.json` + `?? README.md` — no probe config, no scratch dir, no generated migration leaked in.
- **Part 3:** the runbook wording now lives in the repo: DB step = `bun run db:migrate`, re-runnable, **no psql
  side-channel**. คุณฟีน's objection is answered structurally, which is the outcome she actually asked for.
- **Verified myself:** tsc 0; `bun test` **126/0** (no app-code change, as expected).
- **Tech-debt recorded, deliberately NOT cut as a task:** rebuilding the 0004–0012 snapshot chain so
  `db:generate` is usable again. It blocks nothing, it's now *safe* (a developer is warned before it can bite),
  and doing it carelessly is worse than leaving it — so I added a **maintenance row on the board** instead of
  putting it in front of the release or the feature backlog. Someone will need it eventually; it won't be lost.
- **@Porter — REQ-015's release is unblocked.** Sequence unchanged: show คุณฟีน the 4 rich-menu images (yes/no)
  → **(1)** `bun run db:migrate` (prod `DATABASE_URL`) → **(2)** build/copy/pm2 restart → **(3)**
  `bun run line:publish-menus` (LINE token) → **(4)** OA smoke incl. the regression that the **old keywords and
  `/checkin?token=` still work**.
- **@Jason: TASK-043** (teacher "my schedule", SPEC-013) is next per the sequencing I set.
- **Nothing waiting on Sober** until TASK-043 reaches REVIEW.

## [01:08] Sober (SA Lead) — spec'd REQ-017 (teacher → phone calendar) → SPEC-014 + TASK-044 → @Jason

Nothing was in REVIEW (TASK-043 still TODO with Jason), so I kept the spec pipeline moving with **REQ-017** —
which Porter's sequence explicitly puts in parallel with REQ-016.

- **Verified as-built before designing:** **no `.ics`/iCal/Google integration exists** (grep: 0 hits) — this is
  greenfield. But two existing patterns carry it: an **auth-free public route** (`publicCheckin` is mounted at
  `index.ts:41`, *before* `app.use("/api/*", authMiddleware)` at `:48`) and a **token-on-a-row** precedent
  (`bookings.checkinToken` + unique index).
- **Design = the light path the REQ itself defaults to:** a per-teacher **tokenized `.ics` subscription**
  (`GET /api/calendar/:token.ics`, auth-free — the token *is* the credential), window −30d/+90d, delivered to
  the teacher via LINE (`ปฏิทิน`/`calendar` + a quick reply on TASK-043's schedule reply) or a staff endpoint
  with **rotate** (old link 404s). No Google OAuth — that would be a different, much heavier REQ.
- **The two details that decide whether this actually works in the wild, so I specced them explicitly rather
  than leaving them to chance:** (1) a **stable `UID` per booking** (+ `SEQUENCE`/`LAST-MODIFIED`) so an edit
  *updates* the event instead of spawning a duplicate; (2) **cancelled bookings emitted with
  `STATUS:CANCELLED`** rather than dropped — otherwise a cancelled class sits on the teacher's phone forever.
  Also: explicit Asia/Bangkok TZ (no floating times) and RFC-5545 escaping/folding, since a malformed feed fails
  *silently* in calendar apps.
- **Direct consequence of TASK-042, carried into the task:** the new `teachers.calendar_token` column needs
  migration **0013 hand-authored AND journal-registered**, with an explicit **do NOT run `db:generate`** — the
  rule we just wrote into `drizzle/README.md`. Nice to see it bind immediately.
- **On the REQ's open question — I did not manufacture a round-trip, but I sharpened it.** The REQ asked
  "`.ics` or Google two-way?"; its own Constraints already default to read-only `.ics`, so I specced that and
  proceeded. What I *did* route to @Porter as a **non-blocking FYI** is the thing that will actually determine
  whether คุณฟีน is happy: **Google Calendar refreshes subscribed feeds slowly (hours)** while iPhone is
  ~15 min — technically within the AC, but a real expectation to set with teachers on Android. If she wants
  near-real-time there, that's a separate Google-API REQ and this feed is still useful either way.
- **@Jason: TASK-044 after TASK-043** (it reuses the teacher lookup and hangs the link off that reply).
  **@Porter:** REQ-017 is IN_SPEC; the only thing for you is the refresh-latency FYI above.
- **Nothing waiting on Sober** until TASK-043/044 reach REVIEW. Spec queue left: REQ-012 (LIFF form) → REQ-013,
  plus REQ-009 / REQ-014 independently.

## [12:40] Jason (BE) — TASK-043 built → REVIEW → @Sober (teacher "my schedule")

- Filled the REQ-015 teacher-menu `action=schedule` stub. Read-only; no endpoint/migration/config.
  - **`checkin.service.ts` `findBookingsForTeacher(lineUserId, from, to)`** (beside the parent one): teacher
    resolved from `lineUserId` (→ `[]` if unlinked), bookings in `[from..to]`, `ne(CANCELLED)`, `{student,subject}`,
    ordered date→startTime. **Never takes a teacher id from the payload.**
  - **`lib/line-schedule.ts` `renderSchedule` (pure)**: `{when}·{student}·{subject}·{status}` — today = time only,
    week = date-prefixed; empty state; caps at 20 + "…and N more" (LINE size guard); all copy via `t()`.
  - **`doTeacherSchedule`**: today = Bangkok date; week = **`weekRange` (Sun–Sat)**; toggle quick-reply
    (This week ↔ Today) + back-to-menu. Wired `action=schedule[&range=week]` + keyword `ตาราง`/`schedule`.
  - **i18n:** new TH+EN keys (titles/row/empty/more/toggle + `status_*`); **retired `teacher_schedule_soon`**
    (grep → 0 refs).
- **Verify:** `bunx tsc --noEmit` clean; `bun test` **131/0** (+ `line-schedule.test.ts`: today/week/empty/cap +
  bilingual status; postback `range=week` parse). LINE delivery = OA-smoke (brownfield).
- **@Sober — TASK-043 → REVIEW.** Design calls per SPEC (week Sun–Sat via existing `weekRange`; exclude only
  CANCELLED, others shown with label). Next: **TASK-044** (`.ics` feed, dep 043). **Nothing else waiting on Jason.**

## [01:27] Sober (SA Lead) — reviewed TASK-043 → DONE; **REQ-016 SPEC_DONE**; TASK-044 unblocked → @Jason

Reviewed the teacher "my schedule" build. Checked the isolation rule first (that's the one with a real blast
radius here), then the reuse constraints, then ran everything.

- **✅ TASK-043 → DONE.** `findBookingsForTeacher` resolves the teacher **only** from `teachers.lineUserId`
  (→ `[]` if unlinked), filters `teacherId` + date range + `ne(status,"CANCELLED")`, relations/order as specced.
  `doTeacherSchedule` takes **`range` from the postback but never a teacher id** — a forged payload can flip
  today↔week and nothing else. That's the TASK-038 rule holding across a new surface, which is what I wanted.
- **Reuse held:** week = the existing **`weekRange` (Sun–Sat)**, no second convention invented; the renderer is
  a **pure** module (today = time-only, week = date-prefixed, empty state, **cap 20 + "…and N more"** so a busy
  week can't exceed LINE's limits — a detail he added, and the right one).
- **Stub genuinely retired:** I re-ran the grep — `teacher_schedule_soon` → **0 refs**. Keyword fallback
  `ตาราง`/`ตารางสอน`/`schedule` sits alongside the button (REQ-015 principle: keywords keep working). New copy
  TH+EN in `line-i18n`.
- **Verified myself:** tsc 0; `line-schedule.test.ts` 4/0; full `bun test` **131/0**.
- **His two flagged design calls were mine and both stand** — week Sun–Sat, and exclude *only* CANCELLED (the
  other statuses are shown with their label, which is the whole reason the AC asks for status).
- **REQ-016 → SPEC_DONE.** It's BE-only in the same repo as REQ-015, so **@Porter: it rides along with the LINE
  release** — no separate deploy. Acceptance = the OA smoke: a linked teacher taps ตารางของฉัน → today's own
  bookings → toggle This week → empty state where applicable → TH and EN both render.
- **@Jason: TASK-044 (`.ics` feed) is unblocked** — it reuses this lookup and hangs the subscription link off
  this reply. Remember migration **0013 must be hand-authored + journal-registered, and do NOT run
  `db:generate`** (the rule from TASK-042).
- **Nothing waiting on Sober** until TASK-044 reaches REVIEW. Spec queue left: REQ-012 → REQ-013, plus REQ-009 /
  REQ-014 independently.

## [afternoon] Porter (PM) — REQ-015 + REQ-016 ready to release; asking the stakeholder to choose the release batch

- Picked up two SPEC_DONE items: **REQ-015** (LINE tap UX + bilingual — release was unblocked by TASK-042) and
  **REQ-016** (teacher "my schedule"). Per Sober, REQ-016 is **BE-only in the same repo**, so it **rides along
  with the LINE release — no separate deploy**. The 4 rich-menu images were already **approved by คุณฟีน**
  (2026-07-29, "ไม่แก้ ตอนนี้เอาไปก่อน"), so that gate is closed.
- **The migration objection is answered structurally** — the DB step is now plainly `bun run db:migrate`
  (registered in the journal, re-runnable, no psql side-channel), which is what คุณฟีน actually asked for.
- **Decision I put to the stakeholder (theirs, not mine):** ship the LINE release **now with REQ-015 + REQ-016**,
  or **wait** so **REQ-017** (teacher `.ics` phone calendar, still being built) joins and all three LINE features
  go out in **one deploy + one test round**. She has previously said she prefers batching ("เทสทีเดียวได้ เพราะมี
  อีกหลายงาน"), so I laid out the trade-off rather than assuming: shipping now gets the most-felt UX win to staff
  and parents sooner; waiting means one round of deploy/testing instead of two, but REQ-017 also carries a new
  migration (0013) so it isn't zero-risk to bundle blind.
- **Release steps relayed (unchanged, 4 steps):** `bun run db:migrate` → build/copy/pm2 restart →
  `bun run line:publish-menus` (from the `smart-scheduler-back` source dir; needs `LINE_CHANNEL_ACCESS_TOKEN` +
  prod `DATABASE_URL`) → OA smoke.
- **Acceptance I'll verify after deploy:** rich-menu taps (check-in / leave / my children) · booking picker by
  tap (no "type a number") · **language toggle flips replies AND the menu, persisted** · **REQ-016:** a linked
  teacher taps ตารางของฉัน → today's own bookings → This week → empty state → TH+EN · **regression:** old typed
  keywords and `/checkin?token=` still work.
- Ball: stakeholder (choose the batch, then run the release). Remaining LINE/dashboard work stays with the team
  via @Sober.

## [afternoon-2] Porter (PM) — stakeholder chose (ก): release REQ-015 + REQ-016 NOW; REQ-017 ships later separately

- **Decision: option (ก) — ship now.** The LINE release goes out with **REQ-015 + REQ-016**; **REQ-017** (teacher
  `.ics` phone calendar) will get its **own deploy + test round** when it's ready, rather than holding this one.
- Release handed to the stakeholder (4 standard steps, no side-channel): `bun run db:migrate` → build/copy/pm2
  restart → `bun run line:publish-menus` (from the `smart-scheduler-back` source dir, needs
  `LINE_CHANNEL_ACCESS_TOKEN` + prod `DATABASE_URL`) → OA smoke.
- **Acceptance Porter will verify from the stakeholder's results:** menu taps (check-in / leave / my children) ·
  tap-to-pick a booking (no typed number) · **language toggle flips replies AND the menu, and persists** ·
  **REQ-016:** linked teacher taps ตารางของฉัน → own bookings today → This week → empty state → TH+EN ·
  **regression:** old typed keywords + `/checkin?token=` still work. On pass → **REQ-015 + REQ-016 DELIVERED**.
- **@Sober — FYI for your sequencing (not a direction):** REQ-017 is now a **standalone release** rather than
  riding along, so its migration (0013) and `.ics` feed can be planned on their own deploy. Everything else in
  the spec queue is unchanged.
- Ball: stakeholder (run the release + acceptance) → Porter verifies.

## [afternoon-3] Porter (PM) — 🔴 REQ-015 acceptance FAILED in prod: rich menu renders but NO button does anything → @Sober

- **REQ-015 + REQ-016 were deployed. Acceptance FAILS.** The rich menu **renders correctly** (Fern's artwork,
  right labels/layout — stakeholder screenshot) but **tapping ANY button does nothing**: no reply, no visible
  effect. Live impact: **staff/parents now see a menu that does nothing** (typed keywords still work, so it's a
  degradation, not an outage).
- **Facts established (so this doesn't get re-litigated):**
  1. **Deploy is confirmed by the stakeholder** — code deployed, `pm2 restart` done; server log shows the app
     started on :4006 with the outbox worker.
  2. **The webhook is reachable and the backend is live** — typing `เมนู` gets a correct reply, and the account
     is linked (parent menu text returned). So this is **not** the "old build still running" theory I first gave;
     I checked that and it's ruled out.
  3. **Tapping produces no log movement.** ⚠️ Note this is **not diagnostic on its own** — see the observability
     gap below; a *successful* postback would also log nothing.
- **Porter read-only code review — the whole static path looks CORRECT, so the fault is runtime/config, not
  obviously the source:** rich-menu areas are built as `{type:"postback", data:"action=…"}` with sane bounds
  (`line-rich-menu.ts` `PARENT_RICH_MENU`); `createRichMenu` POSTs the full definition **including `areas`**;
  `routes/webhooks.ts` forwards **all** events unfiltered after signature check; `handleLineWebhookEvents`
  dispatches `ev.type === "postback"` → `handlePostback`; `parsePostback` reads `action=` via `URLSearchParams`.
  I could not find the defect by reading — it needs live inspection.
- **@Sober — please take this and direct the diagnosis** (hypotheses offered, not instructions; your call):
  - **What did LINE actually store?** `GET /v2/bot/richmenu/{richMenuId}` for the published ids (they're in
    `app_settings.line_rich_menu_ids`) — do the stored menus really contain the `areas`, or did they land
    image-only? That would match the symptom exactly (menu shows, taps are dead zones).
  - **Which menu is actually linked?** `GET /v2/bot/user/{userId}/richmenu` + the default menu. If a rich menu
    configured in the **LINE OA Manager** is in play, it can take precedence over API-created ones and its taps
    would never produce our postbacks.
  - **Is the OA delivering postback events at all** (OA response-mode / webhook settings)?
  - **🔧 Observability gap worth fixing regardless:** inbound events are logged **only on error**
    (`handleLineWebhookEvents` catch), and `handlePostback` has a **silent `return`** when
    replyToken/userId/data is missing. That's why "no logs" told us nothing. A one-line inbound-event log
    (type + action) would have made this a 30-second diagnosis.
- **Question I'm putting to the stakeholder** (business call, will relay the answer): leave the dead menu up
  while this is fixed, or unpublish/unlink it so users aren't confused (keywords keep working either way)?
- **REQ-015 + REQ-016 are NOT delivered** — status stays SPEC_DONE, acceptance failed. Ball: @Sober.

## [afternoon-4] Porter (PM) — stakeholder answer: LEAVE the menu published while it's fixed

- **Answer to my question: option (ก) — leave the rich menu up.** Do **not** unpublish/unlink it; the fix goes
  out on top of it. Typed keywords keep working meanwhile, so parents/teachers aren't blocked — just confused by
  dead buttons, which she accepts for now.
- **⇒ @Sober: no rollback action needed** — the menu stays live exactly as published, so whatever diagnosis you
  direct can inspect the *real* published menus (`app_settings.line_rich_menu_ids`) as-is rather than a
  re-created one. If the fix turns out to require republishing the menus, that's fine — the publish command is
  re-runnable by design (TASK-040).
- Nothing else from Porter on this; the defect and its diagnosis sit with the team. Ball: @Sober.

## [02:38] Sober (SA Lead) — dead-menu defect: verified the static path myself, then cut TASK-045 (diagnose, don't guess) → @Jason

Took the prod defect (menu renders, taps do nothing). Re-traced the fail path in code before directing anything.

- **Static path is genuinely correct end-to-end — I confirmed Porter's read rather than assuming it:** menu
  areas are `{type:"postback", data:"action=…"}` with bounds that tile the image exactly; `createRichMenu` POSTs
  the definition **including `areas`**; the webhook route forwards all events post-signature;
  `handleLineWebhookEvents` dispatches `ev.type === "postback"` → `handlePostback`; `eventPostbackData` reads
  **`ev.postback.data`**, which is exactly what the LINE API delivers. And typing `เมนู` works ⇒ webhook,
  signature, account-linking and the reply path are all fine.
  ⇒ **The fault is runtime/config, not the source.** Nothing in the repo will reveal it.
- **The real obstacle is that we're blind:** inbound events are logged **only on error**, and `handlePostback`
  has a **silent `return`** when replyToken/userId/data is missing. So "tapping produced no logs" told us
  *nothing* — a perfectly successful postback would look identical. Porter flagged this and he's right; it's the
  thing to fix first.
- **I deliberately did NOT cut a fix.** Three hypotheses remain and they're only separable by evidence:
  **(A)** the published menu landed without/with broken `areas` (image-only → dead zones, no event ever sent);
  **(B)** the menu the user actually has linked isn't ours — a stale per-user link from an earlier publish, or a
  menu made in the **LINE OA Manager**, which takes precedence over API-created ones; **(C)** the OA isn't
  delivering postback events at all. Changing menu/postback code now would muddy the evidence and could mask
  the real cause.
- **TASK-045 (@Jason, BE priority #1) = instrumentation, not repair:** (1) log **one line per inbound event**
  (type + postback action) and turn the silent early-exit into a **logged** one naming the missing field, plus
  log unknown actions — after this, a single tap definitively separates "LINE never sent it" from "we dropped
  it"; (2) a **read-only `bun run line:inspect-menus`** (same convention as `line:publish-menus`) that dumps, for
  every stored menu id, the **`areas`** (bounds + action data), the **default** menu, the **full menu list**, and
  — given a userId — **that user's linked menu**. That tests (A) and (B) directly, from data, without anyone
  guessing in a console. No writes; token never logged.
- **Sequencing (my call):** TASK-045 jumps ahead of TASK-044 (`.ics`). A live degradation that staff and parents
  can see outranks a new feature, and REQ-015/016 can't be accepted until it's resolved.
- **@Porter — heads-up, no action yet:** once TASK-045 is deployed the diagnosis needs a **short operator run**
  (one tap + `bun run line:inspect-menus`, with the stakeholder's LINE userId if they'll share it) and the output
  sent back. I'll raise that as a proper DATA REQUEST with exact commands once the tooling is reviewed — no point
  asking before it exists. Menu stays published, as คุณฟีน decided.
- **Nothing waiting on Sober** until TASK-045 reaches REVIEW.

## [15:50] Jason (BE) — TASK-045 built → REVIEW → @Sober (dead-menu diagnostics; NO speculative fix)

- Took TASK-045 as priority #1 (board re-sequenced TASK-044 behind it). **Instrumentation only — I did not touch
  the menu-building or postback routing.** Nothing provably defective surfaced while instrumenting, so there was
  nothing to stop-and-report per your Questions; the static path matches your verification.
- **1) Inbound observability** — new pure `lib/line-log.ts`: `[line-in] type=… u:<sha8> data=action=…` logged
  **once per inbound event before dispatch** (so a *successful* tap finally leaves a trace); the silent `return`
  in `handlePostback` is now a **logged early-exit naming the missing field**; plus an `UNHANDLED action=` warning
  driven by a new `KNOWN_POSTBACK_ACTIONS` set. **Never logs the raw userId** (stable sha-prefix marker) or any
  token, and message *content* is not logged (type only). One line per event.
- **2) `bun run line:inspect-menus [lineUserId]`** — new `scripts/line-inspect-menus.ts`, **strictly read-only**
  (GET-only helpers added to `lib/line-rich-menu.ts`; nothing creates/links/deletes/republishes). Prints per
  stored id: name/size/chatBarText + **the full `areas`** (bounds + `action.type`/`data`) with a loud
  `⚠️ NO AREAS → every tap is a dead zone (hypothesis A)`; the **channel default** (flagged if not ours); the
  **full `/richmenu/list`** (flags menus `created outside our publish (OA Manager?)`); and with a userId, **that
  user's linked menu** (flagged if stale/not ours) → **hypothesis B**. Missing token → clean error, exit 1,
  **before any API call** (smoke-verified).
- **Verify:** `bunx tsc --noEmit` clean; `bun test` **139/0** (+ `line-log.test.ts`: marker stable/never leaks the
  id, dropped-postback names the exact missing field(s), unhandled-action line; + `line-inspect-format.test.ts`:
  areas rendered, NO-AREAS warning, missing-menu handled). LINE GETs + the live log line are runtime.
- **@Sober — TASK-045 → REVIEW.** The task's Implementation Notes carry the **exact operator instructions** (run
  `line:inspect-menus` twice → send output; deploy → tap once → send the `[line-in]` lines) **plus a
  read-the-result table** mapping the evidence to hypotheses (A)/(B)/(C) — ready to drop straight into your
  DATA REQUEST. Next after this: **TASK-044** (`.ics` feed). **Nothing else waiting on Jason.**
## [16:22] Sober (SA Lead) — reviewed TASK-045 → DONE; raised the DATA REQUEST (exact commands) → @Porter

Reviewed the diagnostic instrumentation. The check I spent the most effort on was **read-only**, because this
script is about to be run against the live OA — a stray write would make a live degradation worse.

- **✅ TASK-045 → DONE. Read-only verified two levels deep:** the script has **no POST/PUT/DELETE/PATCH and no
  create/link/upload/publish/delete call**, and all four new helpers (`getRichMenu`, `listRichMenus`,
  `getDefaultRichMenuId`, `getUserRichMenuId`) call `fetch` **with no `method`** ⇒ plain GETs. Missing token →
  clear error + `exit 1` **before** any API call. Safe to hand to an operator.
- **Observability verified in place:** the `[line-in]` line is emitted **before** dispatch, so a tap that
  *succeeds* now leaves a trace too — that was the entire point. The silent `return` in `handlePostback` is now a
  **logged** early-exit naming the missing field, and unknown actions are logged against a known-action set (a
  typo can no longer look identical to "nothing arrived").
- **Privacy honored, and isolated in one testable place:** `userMarker` logs a **sha256 prefix**, never the raw
  LINE userId; message **text is never logged** (only `msgType`); no tokens. Good instinct putting it in a pure module.
- **He held the line I drew:** instrumentation only, **no speculative fix** — nothing provably defective turned
  up, so nothing was changed. That keeps the evidence uncontaminated, which matters more here than speed.
- **Verified myself:** tsc 0; full `bun test` **139/0** (up from 131 — new formatter/marker tests).
- **🚧 DATA REQUEST raised (on the board, with exact commands) — this is the ball now.** Deliberately withheld
  until the tooling existed; now it's real:
  - **Step 1 (no deploy needed — do this first):** `bun run line:inspect-menus` (+ optionally
    `bun run line:inspect-menus <userId>` using `parents.line_user_id`/`teachers.line_user_id`) → send the full
    output. It prints each published menu's **`areas`**, the channel **default**, the **full menu list** (flagging
    menus made outside our publish, e.g. **OA Manager**), and that user's **linked** menu.
  - **Step 2 (needs this build deployed):** deploy + restart :4006 → tap **one** button → send the `[line-in]` lines.
  - **How I'll read it:** empty `areas` ⇒ (A) the publish landed image-only; a foreign/stale linked or default
    menu ⇒ (B); Step-1 clean but **no `[line-in]` on a tap** ⇒ (C) the OA isn't delivering postbacks. Step 1 may
    be conclusive on its own.
- **@Jason: nothing on you for this** until the evidence returns — **TASK-044** (`.ics` feed) is your next item.
- **Nothing waiting on Sober** until the DATA REQUEST output comes back (then I cut the fix task) or TASK-044
  reaches REVIEW.

## [afternoon-5] Porter (PM) — relayed the dead-menu DATA REQUEST to the stakeholder (Step 1 first, no deploy)

- Relayed Sober's DATA REQUEST in Thai, **ordered so the cheapest step comes first**: **Step 1 =
  `bun run line:inspect-menus`, which needs NO deploy** and may be conclusive on its own; **Step 2** (deploy the
  TASK-045 build → restart → tap one button → send the `[line-in]` lines) only if Step 1 doesn't settle it.
- **Assurances I passed on, because this runs against the LIVE OA:** the inspect script is **read-only** —
  Sober verified two levels deep (no POST/PUT/DELETE/PATCH, all helpers are plain GETs, and it exits before any
  API call if the token is missing). **Privacy:** the new logging records a **hashed** user marker, never the raw
  LINE userId, and **never logs message text** — worth stating plainly since I'm asking her to run diagnostics on
  her own live account.
- **Optional userId:** the per-user check is optional, and the id can be read from
  `parents.line_user_id` / `teachers.line_user_id` in the DB rather than asked for personally — I offered that
  route so she isn't put on the spot to hand over an account identifier.
- Told her what each outcome means so the result isn't a black box: empty `areas` ⇒ the publish landed
  image-only; a foreign/stale linked-or-default menu ⇒ something outside our publish (e.g. OA Manager) is
  winning; Step-1 clean but no `[line-in]` on a tap ⇒ the OA isn't delivering postbacks at all.
- Ball: stakeholder (run Step 1, send output) → Porter relays it back to @Sober, who then cuts the fix.

## [afternoon-6] Porter (PM) — DATA REQUEST Step 1 ANSWERED: hypothesis (A) ruled out, (B) largely too → @Sober

- Stakeholder ran `bun run line:inspect-menus` and redeployed :4006. **Step-1 output relayed in full to Sober**
  (raw output in this entry's summary below; ask me if you want it verbatim in `project-docs/`).
- **What the evidence settles:**
  - ❌ **Hypothesis (A) — "publish landed image-only" is RULED OUT.** All 4 stored menus report correct
    `areas`: parent TH/EN = **6 areas** (`action=checkin/leave/children/register/lang/help`) at the exact 833/1666
    × 843 bounds; teacher TH/EN = **2 areas** (`action=schedule`, `action=lang`) split at 1250. Every action is
    `action.type=postback`. The tap targets are real and correctly shaped.
  - ✅ **The channel default IS ours:** `default = richmenu-5297…0348 = our parentTH`, `selected=true`.
  - ⚠️ **But the channel holds 8 menus, not 4** — the script flags 4 as "created outside our publish". **My read:
    they are NOT OA-Manager menus — they carry OUR naming** (`smart-scheduler-parent-th/-en`,
    `-teacher-th/-en`) and correct area counts (6/6/2/2). They're almost certainly **orphans from an earlier run
    of `line:publish-menus`** (it's re-runnable by design; `app_settings` keeps only the newest 4 ids, the older
    set stays on the channel). So the ⚠️ label is a **false alarm in wording** — worth correcting so it doesn't
    mislead the next reader. **Hygiene issue:** each re-publish leaves 4 more orphans behind.
  - ⇒ **(B) mostly falls too:** even if a user were linked to a stale menu, **all 8 menus carry correct postback
    areas**, so a tap should still fire a postback either way.
  - ⇒ **(C) "the OA isn't delivering postback events" is now the leading hypothesis.**
- **What's still MISSING — the decisive datum:** there is **no `[line-in]` line anywhere** in the pm2 output the
  stakeholder sent. But I can't yet call that evidence: it isn't established that they *interacted* after the
  restart, and the pasted log is mostly historical `Started development server` / `[outbox]` lines.
  - **Refinement I'm asking her for (my addition, since it separates two causes in one shot):** with logs open,
    **(1) type any text** (e.g. `เมนู`) and **(2) tap a button**. Since `[line-in]` is emitted for **every**
    inbound event, this distinguishes cleanly: *no `[line-in]` on either* ⇒ the TASK-045 build isn't actually the
    one running; *`[line-in]` on the text but none on the tap* ⇒ **(C) confirmed** — LINE delivers messages but
    not postbacks, which points at the OA channel/webhook configuration rather than our code.
  - Also flagged for whoever reads the log: the pm2 output shows **many repeated `Started development server`
    lines** — possibly just restart history, but worth a glance for a restart loop, and the message suggests the
    process runs the dev entry rather than the compiled binary.
- Ball: stakeholder (the two-part tap/type test) → Porter relays → @Sober cuts the fix. Menu stays published.

## [afternoon-7] Porter (PM) — 🎯 ROOT CAUSE FOUND: the LINE webhook points at a DIFFERENT (production) server → @Sober

- **The dead-menu cause is NOT a code defect.** Stakeholder confirmed: **the LINE webhook is wired to the
  production server** — i.e. **not** the `som-back` instance she had been deploying to, restarting and
  inspecting. So the TASK-045 instrumentation and the REQ-015/016 build went to one box while LINE was
  delivering events to another. **REQ-015's code was never actually exercised.**
- **How the evidence converged** (recording it because the wrong turns cost time and the pattern is reusable):
  1. Menus have correct `areas` + our menu is the channel default ⇒ (A) and (B) ruled out.
  2. The `.exe` on the server byte-matched the local build and contained the new strings ⇒ "stale binary" and my
     "Windows can't overwrite a running .exe" theory both **wrong**.
  3. `[outbox] sent=1 …` appears in `out.log` ⇒ stdout is fine, so my **buffering theory was wrong too**.
  4. Decisive combination: **bot replies to text ✅ but `[line-in]` never appears ❌.** Since `[line-in]` is
     emitted for *every* inbound event before dispatch, the instance answering LINE could not be the instance
     writing these logs ⇒ a **different process/server** is serving the webhook.
  5. Corroborating: the error log still shows `invalid input syntax for type uuid: "availability"` — the bug
     **TASK-029 fixed** — i.e. whatever serves that traffic is running **pre-REQ-005 code**.
- **⚠️ The bigger issue this exposes — raising it deliberately, it outranks the menu bug:** we appear to have
  **more than one deployment**, and **which one a feature is live on depends on which surface you test**. Several
  REQs were accepted against the web app; if the LINE-serving box runs older code, some "DELIVERED" work may not
  be live everywhere. The `availability` error is direct evidence of old code still running somewhere. **We need
  the environment topology written down** — how many instances exist, which serves the web app, which serves the
  LINE webhook, and what the deploy target is for each.
- **@Sober — two things (business framing mine, technical judgement yours):**
  1. **No fix task is needed for the dead menu itself** — the code was never in play. Once the build reaches the
     webhook-serving instance, REQ-015/016 acceptance can simply be re-run. Please confirm you agree before we
     close it out, in case the build reveals something new when it finally does execute.
  2. **The topology/`availability`-error point deserves your look** — if a production instance is running
     pre-TASK-029 code, that's a live defect on a delivered REQ, not a documentation gap. I'd rather you judge
     than have me assume it's benign.
- **Deploy correction relayed to the stakeholder:** deploy the current build to the instance the **webhook**
  points at, then re-run the LINE acceptance. The rich menus themselves are channel-level and already correct —
  no republish needed.
- REQ-015 + REQ-016 remain **not delivered** pending that re-test. Ball: stakeholder (deploy to the right
  instance + re-test) · @Sober (the two judgement calls above).

## [afternoon-8] Porter (PM) — 🐛 NEW BUG found during LINE testing: an already-linked user can NEVER change role → @Sober

- **Good news first:** stakeholder re-pointed the LINE webhook at the correct server — **all REQ-015 buttons now
  work**. The dead-menu issue is confirmed environmental, not code.
- **While testing, she hit a real defect.** Repro (hers, exactly): typed `สมัคร` → chose **2 (teacher)** →
  **without typing anything else**, the bot replied with **parent options**. She could not switch role to test
  the teacher features.
- **I traced it in `line-webhook.service.ts` `handleMessage` — the branch order is the bug:**
  1. `สมัคร` matches first and correctly does `setStep(CHOOSE_ROLE)` + the role prompt ✅
  2. …but the **"Already-linked routing"** block (`detectLinkedRole` → `if (linked === "customer") return
     handleParentCommand(...)`) sits **BEFORE** the **"Linking conversation"** block that handles
     `session.step === "CHOOSE_ROLE"`.
  3. So her next message (`2`) is swallowed by `handleParentCommand("2")` → parent menu. **The `CHOOSE_ROLE`
     branch is unreachable for anyone already linked**, and `AWAIT_CODE` after it likewise.
  ⇒ **Any already-linked user can start `สมัคร` but can never complete a role change.** Looks **pre-existing**
  (the ordering predates REQ-015), surfaced now because REQ-015 finally made people exercise the bot.
- **Compounding gap (verified separately): there is NO unlink / logout / change-role command at all** — grep for
  unlink/logout/เปลี่ยนบทบาท returns nothing. Combined with the above, **once a LINE account is linked, its role
  is permanent from the user's side** — only a DB edit can undo it.
- **Real-world impact, not just testing:** a parent who links the wrong phone, a teacher who leaves with their
  account still bound, or someone who is **both** a teacher and a parent, all have no self-service path. Note
  `detectLinkedRole` checks **teacher before parent**, so a dual-role person silently loses the parent surface.
- **@Sober — routing this as a defect, with the business framing:** the customer-visible promise of REQ-015 is
  "the bot is easy to use"; "you can start registering but never finish, and can never undo a wrong link"
  undercuts that. **Whether it's one fix (branch order) or also needs an unlink/switch affordance is your call**
  — I'd rather you scope it than have me pre-decide. If you judge it a separate REQ rather than a fix under
  REQ-015, tell me and I'll write it.
- Immediate workaround given to the stakeholder so testing isn't blocked: clearing `teachers.line_user_id` /
  `parents.line_user_id` in the DB is the only current way to re-link (human-run; BE can't touch prod data).
- Ball: @Sober (scope the fix) · stakeholder (finish the rest of the LINE acceptance meanwhile).

## [evening] Porter (PM) — 🎉 REQ-015 + REQ-016 acceptance PASSED (verified against the real chat transcript) → DELIVERED

- Stakeholder completed the LINE acceptance and **exported the OA chat** for me to verify rather than just
  reporting a verdict — so this acceptance is evidence-based, not self-reported. (Transcript is a personal LINE
  export containing a parent phone number + student names; I read it for verification only and am **not**
  copying any of it into the workspace.)
- **REQ-015 — PASS.** In the transcript: rich-menu taps produce real actions (เช็คอิน → correct empty state
  "วันนี้ไม่มีคาบที่พร้อมเช็คอิน"; แจ้งลา → correct empty state; นักเรียนของฉัน → "นักเรียนของคุณ (1/5)");
  the back-to-menu quick reply works; **the language toggle flips both directions and persists**
  ("Switched to English ✅" → "เปลี่ยนเป็นภาษาไทยแล้ว ✅"); **regression holds** — typed keywords
  (`เช็คอิน`/`แจ้งลา`/`เมนู`) and the `qr` check-in link still work.
- **REQ-016 — PASS.** Teacher linked by nickname, then **🗓️ ตารางวันนี้** (correct empty state) and
  **🗓️ ตารางสัปดาห์นี้** listing five real bookings as `date · student · sport · status`. Exactly the AC.
- **Honest gap in coverage (recording it rather than glossing):** the **quick-reply booking picker** was never
  exercised — there was no bookable slot on the test day, so only the empty-state path ran. The "tap a booking
  instead of typing a number" behavior is therefore **built and reviewed but not observed live**. Worth a look
  the first day there's a real slot; I don't consider it a blocker for delivery.
- **REQ-015 → DELIVERED · REQ-016 → DELIVERED.** Board updated. **Delivered total: 11 REQs.**
- **The transcript also CONFIRMED the role-switch bug from [afternoon-8], and revealed the workaround:** at
  19:13 and 19:16 `สมัคร` → `2` both returned the **parent** menu; at 19:31 the same sequence **worked** and
  prompted for the teacher nickname — and immediately before that the bot had shown the **unlinked** welcome
  text, i.e. the parent link had been cleared in the DB in between. That is exactly the mechanism I described
  (already-linked routing swallows the role choice). **Also learned: unfollowing/refollowing the OA does NOT
  unlink** — two follow events appear in the transcript and the link survived them, because the link lives in
  our DB, not on LINE's side. Useful for whoever fixes it.
- **@Sober — three small observations from the transcript, for you to judge (not instructions):**
  1. The teacher's catch-all reply is still "บัญชีครูผูกแล้ว — รอรับแจ้งเตือนตารางจากระบบ" — it doesn't mention
     that they can now check their schedule. Post-REQ-016 that reads as stale.
  2. The `เมนู` help text still lists `qr — รับลิงก์เช็คอิน` even though we deliberately removed QR from the
     rich menu. Not wrong (the plumbing is intentionally kept), just inconsistent with the menu.
  3. One duplicated reply appears ("วันนี้ไม่มีคาบที่พร้อมเช็คอิน" twice in a row) — may be a double-tap by the
     tester rather than a defect; flagging only so it isn't missed if it recurs.
- Ball: @Sober (role-switch/unlink scoping + the three observations). Nothing blocking on Porter.

## [evening-2] Porter (PM) — ⚠️ ENVIRONMENT TOPOLOGY answered: TWO full servers, one ~2 weeks stale → @Sober

- Stakeholder answered my topology question. **Recorded on the board (Project info) because every role needs it
  at startup — the team has been reasoning as if there were one environment.**
  - **Two servers, each a COMPLETE stack** — frontoffice (front+back) + backoffice (front+back) + **its own
    database** + pm2 + nginx.
  - **`sid`** → **som.develyst.online**. This is where the stakeholder deploys, where **every acceptance test we
    have run** happened, and where the **LINE webhook now points** (moved 2026-07-30).
  - **`production`** → **frontoffice.develyst.online**. 🔴 **Not updated for ~2 weeks — no app deploys and no DB
    migrations.** It is the box that was serving the LINE webhook until today, and the source of the
    `availability` UUID error (a bug TASK-029 fixed) — so **old code is genuinely still running there**.
- **What this means for our delivery record — stating it plainly rather than letting it sit implicit:**
  **every REQ marked DELIVERED was verified on `sid` only.** ~2 weeks back is ≈2026-07-16, so
  **REQ-001/002/004/005/006/007/008/010/011/015/016 are all absent from the `production` server**, as are their
  migrations. The two DBs also diverge. "DELIVERED" currently means **"live on `sid`"**, not "live everywhere".
- **❓ The one question that decides how serious this is (asked to the stakeholder, still open): which URL do the
  school's staff actually use every day?** If they work on `som.develyst.online` (`sid`), our delivery record
  stands and `production` is a stale spare that needs cleaning up or retiring. If they work on
  `frontoffice.develyst.online`, then **none of the last two weeks of work is live for the customer** — a very
  different situation. I have deliberately **not guessed**: the naming ("production") points one way, the
  evidence (SOM's LINE OA + check-in URL both on `som.develyst.online`) points the other.
- **@Sober — no task from me yet, and deliberately so.** Until the stakeholder says which box the customer uses,
  any "sync production" work could be either urgent or pointless. What I'd like from you once the answer lands is
  a judgement on **what a safe catch-up would involve** for a box that is 2 weeks behind on **both code and
  migrations** (ordering, the `bo`/shared-DB change from REQ-006, and whether its separate DB makes it a
  migration or a rebuild). Flagging early so it isn't a surprise.
- Stakeholder also confirmed the older operational items (scheduled tasks, real numbers, freelance budgets, FE
  lint) are **parked for now** — "รอไปก่อน". I'll stop surfacing them until they raise them again.
- Ball: stakeholder (which URL do staff use) → then Porter routes the follow-up. @Sober: unchanged queue.

## [evening-3] Porter (PM) — topology question ANSWERED: the split is INTENTIONAL (pre-first-launch) — concern closed

- **Stakeholder's answer: the customer will use `frontoffice.develyst.online` (production) — but this is NOT a
  problem and NOT urgent.** We are **before the first real serve**. The customer deliberately uses the **older
  version** on production to **try it and send comments**; the team builds and verifies on `sid`. Production gets
  brought up to date at the first serve. Board note corrected from "⚠️ open risk" to "✅ intentional".
- **My earlier framing was over-alarmed** — I read a stale box as a delivery gap when it's a deliberate
  demo/feedback environment. Corrected on the board rather than left to stand.
- **🔑 The genuinely useful thing this surfaces — and it changes how I do intake:** the customer's comments come
  from the **OLD version**. So a reported "bug" may already be fixed on `sid`. **From now on, before I turn any
  customer comment into a REQ, I check it against the current `sid` build first** — otherwise we spend a build
  cycle re-fixing something already done. (Worth noting REQ-010/REQ-011 both came in this way; they happened to
  be genuine, but that was luck, not process.)
- **Still true and worth keeping visible (not raised as urgent):** the eventual **first-serve deploy is a real
  event** — production is behind on **code and DB migrations**, has its **own database**, and the REQ-006
  `bo`/shared-DB change sits in between. When the stakeholder does schedule it, I'll ask @Sober for a planned
  sequence rather than letting it be improvised. Recorded on the board; **not** actioned now.
- Older operational items stay parked per the stakeholder ("รอไปก่อน").
- Nothing blocking on Porter. Ball: @Sober (role-switch/unlink scoping, the 3 transcript observations,
  REQ-017 in flight, then the REQ-009/012/013/014 spec queue).

## [17:05] Sober (SA Lead) — closed the moot DATA REQUEST; scoped the role-change defect → TASK-046 + a REQ ask → @Jason / @Porter

Read the whole afternoon thread before acting — the situation moved a long way while the DATA REQUEST was open.

- **✅ Closed my DATA REQUEST as MOOT — nobody should run it.** The root cause turned out to be environmental:
  the **LINE webhook pointed at the stale `production` server**, whose ~2-week-old build **predates the postback
  layer** — which is why typed keywords worked (old code has them) while **every tap was silently dropped**.
  Re-pointing the webhook fixed all buttons. Hypotheses (A)/(B)/(C) never needed testing. I struck the row
  rather than deleting it so the reasoning stays readable. **TASK-045's instrumentation keeps its value** — the
  `[line-in]` logging is exactly what would have identified this in seconds, and `line:inspect-menus` stays as a
  permanent read-only diagnostic.
  - Worth naming for the record: the two-server split (`sid` current vs `production` stale) is the kind of
    environment fact that invalidates code-level hypotheses. Now that it's captured at the top of the board, I'll
    check it first on any "works locally / dead in prod" report.
- **Scoped the new defect Porter routed (he explicitly left the split to me).** I verified both halves in code:
  - **The bug is real and mechanical:** in `handleMessage`, the **already-linked routing** block sits *before*
    the **linking conversation** block, so after `สมัคร` sets `CHOOSE_ROLE` the user's next message (`2`) is
    swallowed by `handleParentCommand("2")` ⇒ `CHOOSE_ROLE`/`AWAIT_CODE` are unreachable for anyone linked.
    Notably **the file already contains the correct pattern** — `AWAIT_STUDENT_NAME` is handled *above*
    linked-routing for exactly this reason, so the fix follows the existing design rather than inventing one.
  - **Second-order issue I found while verifying:** `verifyAndLink` only refuses when the *target* record belongs
    to a **different** LINE user — it never clears the user's **previous** link. So fixing the order alone would
    leave people **linked as both**, and since `detectLinkedRole` checks **teacher before parent**, their parent
    surface would vanish silently. A role *change* must **move** the link. Folded into the same task — that's the
    difference between "the flow completes" and "the flow is correct".
- **My split (answering Porter's question):**
  - **TASK-046 (@Jason, BE priority #1) = the fix.** Reorder + move-the-link. No business question in it.
  - **The unlink / switch-role *affordance* is a product decision, so it gets a REQ, not my invention** — who may
    unbind what (a parent's phone vs a departed teacher), and what a genuinely **dual-role** person should see.
    I asked **@Porter to write that REQ** (he offered); it's not urgent once TASK-046 lands, because the DB-edit
    workaround stops being needed.
- **@Jason: TASK-046 first**, then TASK-044 (`.ics`). **@Porter: the REQ ask above** is the only thing on you.
- **Nothing waiting on Sober** until TASK-046/044 reach REVIEW or that REQ arrives.

## [17:15] Jason (BE) — TASK-046 built → REVIEW → @Sober (already-linked user can't finish `สมัคร`)

- Your diagnosis matched the code exactly — both parts fixed in `line-webhook.service.ts`, with the two
  previously-implicit decisions extracted into a new pure `lib/line-routing.ts` so the **ordering itself is
  unit-tested** (a reorder is otherwise invisible to tests).
  - **Reorder:** `decideMessageRoute(sessionStep, linkedRole)` → `add-student | linking | linked | welcome`,
    encoding the rule the file already used for `AWAIT_STUDENT_NAME`: an in-progress conversation wins over
    already-linked routing. The already-linked block is now gated behind `route === "linked"`. **Branch bodies
    unchanged** — only when they run. `สมัคร`-restart, `AWAIT_STUDENT_NAME`, and the `AWAIT_CODE`-without-
    `pendingRole` fall-through all preserved.
  - **Move, don't duplicate:** `moveRosterLink` at the **3** roster link-success points in `verifyAndLink` —
    linking as teacher clears `parents.line_user_id`, as customer clears `teachers.line_user_id`. One LINE user
    ⇒ one active roster link, so `detectLinkedRole` (teacher-before-parent) can't hide the other surface. No new
    table/migration; no unlink *command* (out of scope, per the task).
- **⚠️ One judgement call for you (you asked me to state it): I left the ADMIN list untouched.** It's a
  notification subscription in `app_settings`, and `detectLinkedRole` checks admin **last** → it can never shadow
  a parent/teacher surface, i.e. it can't cause this bug. Clearing it would silently unsubscribe someone from
  leave alerts because they registered a child, and it's not restorable without the admin code. **Two-line change
  if you want admin exclusive too** — I didn't take the destructive default unasked.
- **Verify:** `bunx tsc --noEmit` clean; `bun test` **145/0** (+ `line-routing.test.ts` — **the reported repro is
  a test**: linked-customer + `CHOOSE_ROLE` → `linking`; plus linked+no-session → normal routing,
  `AWAIT_STUDENT_NAME` still wins, and the `otherRosterTable` mapping). DB writes + LINE = OA smoke; steps in the
  task notes. The human's manual `line_user_id` workaround is now unnecessary (and I automated nothing for it).
- **@Sober — TASK-046 → REVIEW.** Next: **TASK-044** (`.ics` feed). **Nothing else waiting on Jason.**

## [23:05] Sober (SA Lead) — reviewed TASK-046 → DONE (admin judgement call confirmed) → @Porter deploy; @Jason TASK-044

Reviewed the role-change fix. Both halves verified in code, and I checked the fact his judgement call rested on
rather than accepting the reasoning at face value.

- **✅ TASK-046 → DONE. Reorder:** `decideMessageRoute(sessionStep, linkedRole)` encodes exactly the rule the
  file already applied to `AWAIT_STUDENT_NAME` — an in-progress conversation beats the existing link — and
  `handleMessage` gates the already-linked block behind it with **branch bodies unchanged**. Pulling it into a
  pure helper was the right instinct: the reorder is otherwise invisible to tests, and now **the stakeholder's
  exact repro is a test case** (`decideMessageRoute("CHOOSE_ROLE","customer") === "linking"`).
- **Link-move:** `moveRosterLink` is wired at **all three** success points (teacher; parent-existing-phone;
  parent-new) — I checked each one. One LINE user ⇒ one active roster link, so `detectLinkedRole` can no longer
  silently hide a surface. No new table/migration, and he correctly left the unlink *command* out of scope.
- **✅ His flagged judgement call — CONFIRMED.** He left `app_settings.line_admin_user_ids` untouched. I verified
  the load-bearing fact instead of trusting it: `detectLinkedRole` checks **teacher → customer → admin**, so an
  admin entry genuinely **cannot shadow** a parent/teacher surface and cannot cause this bug. His framing is
  right — the admin list is a **notification subscription, not a roster identity**; clearing it would silently
  unsubscribe someone from leave alerts because they registered a child, and it's unrecoverable without the
  admin code. **Picking the less-destructive default and asking rather than assuming was exactly right**, and I
  told him so — I want that instinct repeated, not second-guessed.
- **Verified myself:** tsc 0; full `bun test` **145/0** (up from 139).
- **Status handling:** REQ-015 is already DELIVERED, so I did **not** reopen it. This is a **post-delivery fix**
  → the board now says it needs a **`sid` deploy + a quick repro re-check** (already-linked parent: `สมัคร` →
  `2` → should ask for the teacher nickname; afterwards they hold one role only), **not** a fresh acceptance round.
- **@Porter:** that deploy + re-check is the only REQ-015 item left; the **unlink/switch-role REQ** I asked you
  for is still open (product decision, not urgent now the DB-edit workaround is unnecessary).
- **@Jason: TASK-044** (`.ics` teacher calendar feed) is your next item — remember migration **0013 must be
  hand-authored + journal-registered, no `db:generate`**.
- **Nothing waiting on Sober** until TASK-044 reaches REVIEW or that REQ lands.

## [evening-4] Porter (PM) — wrote REQ-018 (unlink / dual-role) as asked; relaying the TASK-046 deploy + re-check

- **Both items Sober left on me are handled.**
- **1) Wrote `REQ-018` — LINE account unlink + dual-role** (status **DRAFT**, deliberately *not* READY_FOR_SA
  yet: it's a genuine product decision and I'm not answering it for คุณฟีน). Sober was right to bounce it to me.
  What TASK-046 already solved vs what's left is stated explicitly in the REQ so nobody re-litigates it:
  - **Solved:** "I linked the wrong thing" — a linked user can now re-run `สมัคร` and the link **moves**.
  - **Still open, and all three are business calls:** (a) nobody can fully **unbind**, only move; (b) **staff
    can't unbind someone else** — a **departed teacher keeps receiving schedule pushes on their personal LINE**,
    which is the case with real consequences; (c) **one LINE account now holds only ONE role**, so a genuinely
    dual-role person (teacher *and* parent) silently loses the other surface — role precedence is teacher→parent.
  - 4 questions routed to คุณฟีน with my leanings attached (self-unlink vs staff-only; staff unbind of a departed
    teacher; how real the dual-role case is → keep one-role / switch / show both; notify on unlink).
  - Noted Sober's distinction in the REQ: `line_admin_user_ids` is a **notification subscription, not a roster
    identity**, so it shouldn't be swept into "unlink a person".
- **2) Relaying the TASK-046 deploy to the stakeholder** — and framing it correctly per Sober: REQ-015 stays
  **DELIVERED**; this is a **post-delivery fix**, so it needs a **`sid` deploy + a targeted repro re-check**, not
  a fresh acceptance round. Re-check I'll verify: an **already-linked parent** types `สมัคร` → `2` → the bot now
  **asks for the teacher nickname** (previously it returned the parent menu); after linking, they hold **one**
  role only.
- Ball: stakeholder (deploy `sid` + the one repro re-check; and REQ-018's 4 questions whenever convenient — not
  urgent) · @Sober (unchanged: TASK-044 in flight, then the REQ-009/012/013/014 queue).

## [evening-5] Porter (PM) — stakeholder redirected the whole area → REQ-019 + REQ-020 (supersede REQ-012 & REQ-018) → @Sober

- **The stakeholder reframed this better than my REQ did**, so I rewrote the area rather than defending it.
  Instead of a LINE/LIFF form plus a small unlink feature, the direction is: **manage people (teachers, parents,
  students) on the web like teachers already are**, and put an **admin approval / pairing step** in front of LINE
  claims — because "คนทักมาอาจจะขอเป็นครูมั่ว ๆ ก็ได้" and because **names collide** ("มีคนชื่อ off สองคน").
- **I verified their concern in code before writing anything — it is real, and worse than they assumed:**
  - **Teacher:** typing a roster **nickname** links you **as that teacher** (if unlinked) → you receive their
    schedule and their students' names.
  - **Collision:** `rows.find(...)` = **first match wins** → two teachers nicknamed "off" always bind the first.
    Exactly the case they raised, confirmed.
  - **Parent:** typing an existing **phone** links you as that parent **and the bot replies with the children's
    names** — a privacy leak, not just an access issue.
  - **Unknown phone** → `findOrCreateParentByPhone` **creates a record**, so anyone can generate junk parents.
  ⇒ This is a **privacy/security** issue, not cosmetic. Reflected in REQ-020's priority (MEDIUM–**HIGH**).
- **Placement — I recommended frontoffice and they took it:** teacher management already lives there, this is
  **daily staff work**, and the **backoffice is reserved for money/executive** by their own earlier ruling. Asked
  Sober to sanity-check the technical side at spec time rather than treating my recommendation as settled.
- **Stakeholder's decisions, now baked into the REQs:** **parents = NO approval** (self-create stays; staff just
  need to **view + suspend**) · **students sit under the parent**, managed on the web too · **normal queue**, not
  urgent.
- **Split into two REQs by the value each delivers** (rather than one unmanageable blob):
  - **REQ-019 — People management on the frontoffice** (parents + students, demographics, suspend).
    **Supersedes REQ-012**; feeds REQ-013's demographics.
  - **REQ-020 — Secure the LINE pairing** (teacher approval queue, collision-safe matching, unlink/suspend links).
    **Supersedes REQ-018**, carrying its unanswered dual-role question forward as Q3.
  Board rows for REQ-012/REQ-018 struck through with the reason; files kept for history.
- **@Sober — REQ-019 + REQ-020 are READY_FOR_SA** with my code analysis and the open business questions in each.
  They're related but separable; **the sequencing between them (and against REQ-009/013/014/017) is yours**.
- Ball: @Sober (spec when you reach them) · stakeholder (the 4+4 questions in the two REQs, at their convenience).
