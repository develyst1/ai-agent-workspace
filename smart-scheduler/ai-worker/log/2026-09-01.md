# Log — 2026-09-01 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [00:0x] Jason (BE) — 🔴 The three tasks were REAL and were DESTROYED. Rebuilt, all three. → @Sober
**@Sober — your observation was correct and mine was too. Both.** I built TASK-218/221/223 and you could not
find them, because between my build and your review **the working tree was swept.**

📌 First: my session crossed midnight mid-work — this entry belongs to **2026-09-01**, which is why this file
exists. (And my 08-30 entries went into `log/2026-08-30.md` when today was already 08-31 — the same
date-discipline slip you owned. Not moving them; 08-30 is closed history.)

### The three facts you asked for — full evidence is in **TASK-221 §Review**
| Your question | Answer |
|---|---|
| Absolute path I built in | `H:\scheduler\smart-scheduler-back` |
| `git rev-parse HEAD` | `7217599` — **the same commit you read** |
| Is it the path in `machine.local.md`? | **Yes**, exactly |

**All three match yours. Not a different machine, not a different repo — so it was never a re-cut.** What
happened, from the repo itself: `git reflog` shows a branch sweep `dong → develop → production → dong` with a
fast-forward merge, and **every tracked file I touched carries the identical mtime `2026-08-30 04:26:26`** —
a bulk restore. My two new files (`0028_…sql`, `outbox-idempotency.test.ts`) are gone too, which takes a clean.
No stash.

### 🔴 The real defect is a workspace rule, and it will happen again — please route to @Porter
**Agents never commit** (`CLAUDE.md` rule 6: *"Git is the human's, alone"*). So every engineer's output here
lives **only as uncommitted working-tree changes** until the human commits. **An uncommitted working tree is not
storage.** Any routine branch switch, merge or clean deletes finished work silently — **and it surfaces as
"the engineer never built it,"** which is a reasonable inference and the one you nearly acted on. That misread
would have cost far more than the rebuild. It needs a rule: either the human commits at the end of each batch,
or engineers keep a patch artifact. **Not my call to make** — it is a workspace rule.

📌 **The same failure hit the log, one layer over:** two of my three 08-30 entries (TASK-218, TASK-221) are no
longer in `log/2026-08-30.md` — concurrent sessions appending to one file, last-writer-wins. **The TASK files
are the only reason anything survived**: all three kept their full `## Implementation Notes`, which is how you
reviewed code you could not find, and how I rebuilt it exactly. That is `CLAUDE.md`'s *"write durable facts to
their home file the moment you learn them"* paying for itself — **keep evidence in the TASK, never only in the log.**

### 🛟 Interim measure I took (tell me if you want it elsewhere)
`ai-worker/archive/patch-TASK-218-221-223-scheduler-back.diff` — all three tasks, base `7217599`, `git apply`
from the repo root. 16 files. **It survives a tree sweep because it is not in the repo.** Pending the human's
decision above.

### Rebuilt — all three, with your review points folded in
- **TASK-218** — the transaction caveat now lives on **`enqueueLine`'s own JSDoc**, as you required, and a test
  asserts it is there. Migration **`0028`** is mine again (29 files = 29 journal tags) ⇒ **TASK-224 takes `0029`**.
- **TASK-221** — `discountMinor` **stays NEGATIVE**, per your ruling; the `PostedSale` doc comment now carries the
  *"render `amountMinor`, never re-derive it"* warning **and a test asserts the warning is present**, so Fern
  meets it in the type rather than in a task file.
- **TASK-223** — 🔴 **you were right and my first cut was wrong.** Item 3 now prints on **both** paths; the
  `if (!commit)` gate is gone and a test asserts its **absence** so it cannot come back.

**Verified:** tsc **0** · `bun test` **972 pass / 0 fail** (944 baseline, +28) · migrations 29 = journal 29 ·
`git diff --stat src/services/scheduler.service.ts` empty. The `link-all` script was **not executed**.

**Ball: @Sober** — the three facts are answered, the code is back, review can proceed. Starting **TASK-224**
next (it takes `0029`). **@Porter, via @Sober:** the uncommitted-work rule, and the `bun test` → live-`sid`-DB
risk you already promoted.

## [--:--] Porter (PM) — 💡 Answered the owner's open design problem on `REQ-079` (names from a phone number).
He said plainly he could not see a way through it, so I put options in the REQ rather than a verdict in chat.

**The reframe:** it is not *"should we show names"* — the customer settled that — it is **"who proves they belong
to that phone before names appear?"** Today: nobody. That is the whole exposure.

**Recommended ก + ข:**
- **ก** — the **admin already opens the conversation** (the call's own step 1 is the admin telling them to type
  "สมัคร"). Let the admin send a one-time code/link into that chat; the bot then treats it as authorised for
  that phone ⇒ **full real names, no masking, zero extra taps for the customer**, and nothing for a stranger.
- **ข** — a **linked LINE account should never be asked for a phone at all.** LINE already authenticates them;
  show the children immediately. That answers the call's item 4 better than the phone lookup, and **removes** a
  step instead of adding one.
- **ค** (partial `น้องร...`) kept as the fallback only — it fights the customer's stated wish for clarity.

⚠️ **Business/UX proposal, not a design.** @Sober owns *how* — and it should be checked against **REQ-020**,
which already solves "prove this LINE account belongs to this person" and may make ก nearly free.

## [--:--] Porter (PM) — 🔴 The owner found the assumption under all our LINE work. Recorded on `REQ-079`.
*"แม่ผูกไว้แล้ว แต่แม่ป่วย พ่อจะลา ไลน์พ่อไม่ได้ผูก ไลน์แม่มี PIN"* — **not an edge case, ordinary family life.**

**The flaw it exposes: we model ONE LINE account per family; a child has more than one guardian.** The PIN detail
is what makes it sharp — every "just use mum's phone" workaround is dead, and we have been silently assuming a
household shares one reachable device.

**Three layers, not alternatives:** (1) let **more than one guardian link to the same child** — the permanent
fix, and where **REQ-020**'s approval/collision work already points; (2) **the admin opens the door**, which is
option ก from yesterday generalised — it turns out to be an escape hatch for *anyone who needs access now*, not
just new customers; (3) **the father rings the shop and the admin files it** — works today, no build, and worth
saying out loud rather than pretending it is a failure.

**Recommended (2) then (1).** (2) is nearly free because ก is being built anyway.
**Q5 raised to the owner:** does a second guardian get the **same rights** — including seeing what was bought and
what is left — or "can act, cannot see money"? That is a business rule and it decides how much of REQ-020 moves.

## [--:--] Porter (PM) — 💡 Owner proposes a 6-digit family code instead of LINE linking. Analysed on `REQ-079`.
His instinct is right — a shared family code matches family behaviour far better than device binding (survives a
new phone, a locked phone, a delegated errand).

🔴 **But I had to put one consequence on the table: dropping the binding costs every parent notification.** LINE
can only push to a known account-to-family mapping — that mapping *is* "ผูกไลน์". No mapping ⇒ no course-confirm,
no 08:15 reminder, no check-in message — **exactly the three things the customer asked for on 08-30** (REQ-077).

✅ **Resolution offered, and it gives him everything:** his objection is not to the mapping, it is to the mapping
being **exclusive and hard to move**. So — **keep the mapping; make the 6-digit code the way you JOIN a family.**
One family, one code, **many** LINE accounts attached. Delegation and portability solved; notifications not only
survive but improve (both parents get them instead of one).

⚠️ **Naming matters:** a reusable family code is a **shared password, not 2FA** — and a true one-time code sent to
mum's phone is useless in precisely the sick-mum case it exists to solve.

**Three decisions raised to him** (fixed vs rotating · what "4 wrong attempts" locks and for how long · who sets
the code and whether staff can see every family's). ⚠️ **No rate limiter exists in this codebase** — REQ-051
already established that; attempt-counting is new infrastructure, not a flag.
📌 **Line I recommend regardless: the code may unlock seeing children, leave and check-in — never anything that
moves money.** A code that ends up on a fridge must not be able to spend.

## [--:--] Jason (BE) — TASK-224 BUILT → REVIEW. **`0029`, not `0028`** — and one FE line is now owed. → @Sober
REQ-078's core is in. tsc **0** · `bun test` **1004 pass / 0 fail** (944 baseline; **+60** across 218/221/223/224)
· `drizzle/*.sql` = **30** = journal tags, no orphan either way.

### 🔴 Numbering, counted at the moment of writing (the board's own rule)
29 files / 29 tags **before** mine ⇒ TASK-218 had already taken `0028`, so REQ-078 is **`0029_other_booking_type`**.
Both migrations are in this one tree; whoever deploys applies them in order.

### The three things I most want you to look at
1. **AC-14 is where the risk was, so half the new tests are about the OLD types.** `0029` dropped two NOT NULLs,
   so the *database* stopped refusing a lesson booking with no student. `LESSON_TYPES` + two refinements put it
   back in the contract, with a test **per type**. Relaxing a column for one case is exactly how the other four
   quietly lose a guard.
2. **AC-21 — one guard, inside `reconcileBookingHolds`, returning before anything is read or written.** Cost is
   one extra `findFirst` per call; I took that over threading a parameter through six call sites. Because it
   returns before the movement read, an อื่นๆ booking never writes an `fl:` row **at all** — which is what the
   DoD actually asks for (the absence of the row, not a number that did not move). `heldTarget` untouched.
3. **AC-18 — one accessor, and a test that COUNTS the readers.** `scheduler.service.ts` names the join table
   exactly twice (import + the single insert) and a test asserts that number, so a second reader fails the suite
   instead of becoming two disagreeing answers on a calendar.

### 🔴 I broke the TASK-220 coupling on purpose — Fern now owes a line
Adding `OTHER` to `REASON_ENUM_REQUIRED` is AC-13 exactly, but that set is coupled **in code** to the FE's
`canCancelWithReason`. **Until Fern adds `OTHER` there, the dialog cancels an อื่นๆ booking without asking for a
reason and the API refuses it with `REASON_REQUIRED`.** A two-line pair, same shape as TASK-220 — **please put it
in TASK-226/227 explicitly.** I did not touch the FE.

### Two more in the TASK's §Questions, both business/ops calls rather than code
- **Multi-teacher has no clash check on the EXTRA teachers.** `bookings_teacher_slot_uq` covers `teacher_id`
  only, so teacher B can be on a 10:00 อื่นๆ meeting *and* teach a 10:00 lesson. I did **not** add one —
  several teachers sharing an hour is the whole feature — but it is now creatable. Soft warning, or fine as is?
- ⚠️ **Watch the `sid` run.** `drizzle-kit migrate` runs the whole pending set in ONE transaction, and
  `ALTER TYPE … ADD VALUE` in a transaction needs **PostgreSQL 12+**. I cannot check the server's version.
  Nothing in the file *uses* `'OTHER'` (asserted by a test), which is the other half of that rule. If `sid`
  throws, splitting the enum value into its own migration is a five-minute fix. **This is what "sid first" is for.**

**🚫 Not proven by me, and not mine to prove:** every DoD line about a row landing needs a database.
Commands for the owner are written in the TASK — `bun run db:migrate` then **blocking** `bun run db:verify` ✅
(it should show `0029` applied, witnessed by `booking_other_price_chk`), then restart. `uat` only after `sid`.

🛟 All four tasks are also captured outside the repo:
`ai-worker/archive/patch-scheduler-back-TASK-218-221-223-224.diff` (base `7217599`).

**Ball: @Sober** — four tasks in REVIEW (218 · 221 · 223 · 224). **@Fern is unblocked for TASK-227** (the DTO
now carries `displayName`, `title`, `teachers[]`, and `student`/`subject` may be `null`). TASK-225 is next for
me unless you say otherwise.

## [--:--] Porter (PM) — ✅ Family-code model decided end to end. One hole found and closed. → `REQ-079`
Owner: **fixed code, shop can reset · 4 wrong attempts ⇒ 3-minute lock · the family sets it on first use.**

🔴 **Decision 3 opened a real hole and I did not let it pass: first-use takeover.** If anyone who knows a phone
number can be the one who sets the code "the first time", **a stranger who gets there first owns that family** —
sets the code, sees the children, files leave; the real parent finds it already set. Not hypothetical: a phone
number is not a secret, and **all ~180 families are currently unclaimed.**

✅ **Closed with something already in the design, at zero cost: the FIRST code is set inside the admin-opened
window (Option ก).** Setting = gated. Using = free, any device, any guardian, forever. Resetting = the shop's.
One gate, at the only moment a gate costs nobody anything.

Two cheap rules added: **refuse obvious codes** (`123456` / repeated / sequential) with non-scolding wording, and
**count failures against the family, not the device** — per-device is defeated by switching. ⚠️ Per-family allows
a nuisance lockout; at 3 minutes that is an irritation, not a breach. **@Sober: design detail, not a re-opened
decision.**

📌 **Still `CAPTURED`, not `READY_FOR_SA`.** The auth model is settled; the registration flow it lives inside is
not. **No task is to be cut from this section alone.**

## [--:--] Porter (PM) — ✅ Auth model CLOSED. Owner accepted the weak-code risk with his reasoning. → @Sober
**Per-family failure counting: yes.** **Weak codes: allowed** — owner: *"ลูกค้ายังไม่มากขนาดนั้นที่จะโดน hack"*.
He overruled my recommendation with a stated reason and that is his call; **recorded as an ACCEPTED RISK, not as
something we missed** — the distinction matters to whoever reads this in three months.

**He asked for it to be *ready*, which is the better shape than either yes or no:** build the weak-code check
**present but disabled**, one setting away — `app_settings` (REQ-031 / TASK-101/102) already does exactly this.
Wording stays in the REQ so nobody re-invents it.

📌 **I wrote down what should reopen it**, because nobody remembers a risk they accepted: the family count growing
well past ~180, or the code guarding more than *see children / leave / check-in*. **My line — the code never
unlocks anything that moves money — is what keeps this risk small. If that line moves, this decision is re-taken,
not inherited.**

**Nothing further on REQ-079** — it stays `CAPTURED` pending the owner's design flow. **@Sober: REQ-078 continues,
unaffected by any of today's LINE discussion.**

## [00:5x] Sober (SA) — ✅ **TASK-218 · 221 · 223 · 224 all PASS.** And the cause of the loss is a WORKSPACE rule. → @Jason @Porter @Fern

My session crossed midnight too — this entry belongs to **2026-09-01**; my earlier work today is in
`log/2026-08-31.md` and is closed history.

### 🔴 First, up to @Porter, because it is not a task and it will happen again

**@Jason found the cause and it is exactly right: agents never commit (`CLAUDE.md` rule 6), so every engineer's
output lives ONLY as uncommitted working-tree changes until the human commits. An uncommitted working tree is
not storage.** A routine branch sweep (`git reflog`: `dong → develop → production → dong`, fast-forward merge)
deleted three finished tasks; every file he touched carries the identical mtime `2026-08-30 04:26:26` — a bulk
restore, plus a clean that took his two new files.

📌 **The part that makes this urgent rather than annoying: it surfaces as *"the engineer never built it"*.** That
is a reasonable inference from a clean tree, it is the one I very nearly acted on, and acting on it would have
cost far more than the rebuild — a re-cut task, a rewritten spec, and a wrong entry in a log everyone treats as
history. **The correct fix is a workspace rule, not a task**: either the human commits at the end of each batch,
or engineers keep a patch artifact outside the repo. Jason has taken the interim measure —
`archive/patch-scheduler-back-TASK-218-221-223-224.diff` (base `7217599`, `git apply` from the repo root).
**@Porter — this is Atlas/human territory, please carry it up.**

📌 **And the same failure hit the log one layer over**: two of his 08-30 entries are simply gone — concurrent
sessions appending to one file, last-writer-wins. **The TASK files are the only reason anything survived.** All
three kept their `## Implementation Notes`, which is how I reviewed code I could not find and how he rebuilt it
exactly. `CLAUDE.md`'s *"write durable facts to their home file the moment you learn them"* paid for itself
tonight. **Evidence goes in the TASK, never only in the log** — that is now a habit I will enforce in reviews.

**I owe Jason a correction:** my finding was *"the code is not in the repo"*, and I framed it as an observation
rather than a conclusion — that was right, and it is the only reason this ended in a diagnosis instead of an
accusation. But I should also record that **his evidence was better than mine**: I proved absence, he proved
*destruction*, from the reflog and the mtimes. Absence of evidence, again, and it was my turn to nearly get it
backwards.

### The four verdicts — all PASS

**Verified myself rather than accepted:** `tsc --noEmit` → **0**; the six task-owned test files → **91 pass / 0
fail**; `drizzle/*.sql` = 30, journal tags = 30, `0028_outbox_idempotency` + `0029_other_booking_type` both
registered — **re-counted, not read off his notes.** `postedSaleForBooking` at `sale-post.ts:199`, the route at
`api.ts:267` (no `catch`), `UAT_WARNING` printed at `:78` **above** `if (!commit)`.

⚠️ **I did not run the full `bun test`, on purpose** — it reaches the live `sid` DB through
`eligible.route.test.ts:13`, and having routed that to @Porter as a standing risk I will not be the one to run
it. His 972/1004 is his evidence; the targeted run is mine.

All three rebuild points I asked for are in: the transaction caveat on **`enqueueLine`'s own JSDoc** (with a test
asserting it is there), `discountMinor` negative with the *"render `amountMinor`, never re-derive it"* warning
**in the type** where Fern will actually meet it, and TASK-223's warning on **both** paths with a test asserting
the `if (!commit)` gate is **absent** so it cannot come back. 🔴 **Item 3 on both paths was my correction to make
— his first cut followed my too-narrow wording.**

### TASK-224 — the part worth reading twice

📌 **`0029` dropped two NOT NULLs, which means the DATABASE stopped refusing a lesson booking with no student.**
The loosening อื่นๆ needs silently widened the other four types. Jason saw that a schema change and the
*contract* are different things and put the contract back in zod (`LESSON_TYPES` + a refinement and a test **per
type**). **Half his tests are about the OLD types.**

That is precisely the failure this project has been bitten by twice — `0016`'s NULL `price_group`, `0022` hiding
behind a witness that could not see it: **a migration correct for the new case and quietly wrong for the old
one.** It is also why he re-numbered to `0029` rather than trusting the `0028` I wrote into the task: he counted
at the moment of writing, which is the board's rule working.

**AC-21 is in one place** (`reconcileBookingHolds`, returning **before** the movement read, so an อื่นๆ booking
never writes an `fl:` row at all), `heldTarget` untouched, and `grep -c bookingTeachers` in the service is **2** —
import + the one insert — with a test that fails if a second reader ever appears.

### His four questions, answered

1. **The TASK-220 cancel coupling is broken until the FE catches up** — he stopped at the boundary and flagged it
   instead of reaching across. ✅ Written into **TASK-227** (not 226: 227 depends only on 224, so the pair lands
   together), with the ⛔ comment at both sites. Without it an อื่นๆ cancel asks for no reason and gets a 400 —
   **a button that silently does nothing**, the REQ-019 defect the owner failed acceptance on.
2. **Additional teachers are not slot-checked.** ✅ Not extending `bookings_teacher_slot_uq` is right — it would
   forbid the feature the owner asked for. The residue (teacher B on a 10:00 meeting *and* a 10:00 lesson) is a
   **business** question → **@Porter, my lean: a soft warning at save, never a refusal.** Not blocking.
3. **`ALTER TYPE … ADD VALUE` in drizzle's single transaction needs PG 12+** — correctly flagged, correctly **not**
   verified by him. In the deploy note below.
4. **`OTHER` is now overloaded** (`cancel-reason.test.ts:26` asserts `isEndReason("OTHER") === false`). ✅ Leaving
   it was right; recorded that whichever task next touches that file renames it.

### 🔴 @Porter — one deploy request, and it must be WATCHED, not fired

`0029` is the first migration since the owner's *"ต้องลองที่ sid ก่อน ห้ามพลาด"* that changes the **bookings**
table. On **`sid` first**, in scheduler-back:

```bash
bun run db:migrate
bun run db:verify
```

- **`db:verify` is BLOCKING** — ✅ before any `pm2 restart`. It should report `0029_other_booking_type` applied,
  witnessed by `booking_other_price_chk`. `0028` (TASK-218's outbox key) ships in the same pending set.
- ⚠️ **The one thing that can go wrong, and its fix:** if it throws `ALTER TYPE ... cannot run inside a
  transaction block`, the box is on PostgreSQL < 12. **Nothing is broken** — Jason splits the enum value into its
  own migration, five minutes. **This is exactly what "sid first" is for.** Do not run it unattended, and never
  touch `uat` in the same sitting.

**Also with you:** the uncommitted-work rule (top of this entry), the additional-teacher clash question, and the
`bun test` → live-`sid` risk.

**Ball: @Jason — TASK-225** (charging อื่นๆ: typed amount OR catalogue item, posted on `rev:<bookingId>`), then
TASK-228. **@Fern — TASK-227 is unblocked** (224's DTO is in) and it carries the cancel-reason line that
completes Jason's half; **TASK-222 is unblocked too** (221 is done). **@Porter — the `sid` run.**

## [--:--] Porter (PM) — 🤝 Bot-and-admin coexistence rules written into `REQ-079` at the owner's request. → @Sober
He asked whether the hybrid design actually lets a customer talk to an admin without the two colliding.
**Answer: yes, on one principle — 🔇 the bot is SILENT by default**, speaking only inside a flow someone
deliberately started, and never answering a general question. Bots collide with humans because they try to be
helpful; a bot that defaults to silence cannot answer over the admin at all.

**Four rules recorded:** (1) the bot never self-starts — a flow begins from a **rich-menu button** or the admin;
(2) every step shows **`คุยกับแอดมิน`**; (3) an admin taking over **mutes** the bot and abandons the flow, with
the customer told; (4) faced with the unexpected the bot **does not guess** — it never treats a stray message as
the answer it was waiting for.

🔴 **Rule 1 changes what the customer call decided, deliberately.** Step 1 there was *"ให้ลูกค้าพิมพ์คำว่า สมัคร"*.
**A typed keyword cannot be made safe** — *"สมัครยังไงคะ"* would have the bot barge into a human conversation,
which is the exact *"triggered accidentally"* risk the call itself named. A keyword list is either too narrow to
work or too broad to be safe. ⇒ **a button.** Flagged to the owner as his flow to change; he asked for it in writing.

⚠️ **@Sober — behaviour, not mechanism.** Check whether LINE's own OA tooling already gives a per-conversation
bot/human mode switch before building Rule 3; if it does, that part is configuration, not code.

## [--:--] Porter (PM) — 📱 Device question closed: no extra work, but door 2 got promoted. → @Sober
Owner: rich menu does not render on LINE for PC — correct, and it would have broken Rule 1. **It does not, because
Rule 1 already had two doors.** Parents are almost all mobile (rich menu ✅); the PC minority goes through
**the admin starting the flow**; admins are on PC and never needed a rich menu. **Nothing new to build.**

🔴 **But door 2 stopped being a fallback and became the only way in for that group** — which promotes a detail
into a requirement: **the admin's "start a flow for this chat" control must work from where the admin actually
sits, LINE on a PC**, not only inside our back-office screen. If staff have to switch windows for it, they will
type the answer by hand instead and the flow quietly dies for that group with nobody noticing.
**@Sober — same constraint as Rule 3 (take-over). Solve them once, together.**

📌 **Recorded as NOT verified:** whether a button inside a message (rather than the rich menu) is tappable on
LINE for PC. If it is, the desktop minority gets self-service with no admin. **I have not tested it, so it is not
a fact** — nobody designs on it until @Sober confirms.

## [--:--] Porter (PM) — 📤 Carrying the uncommitted-work rule to the human. Deploy request relayed. → @Sober
Read your 00:5x and Jason's 00:0x. **Three things came to me; all three are moving:**

1. 🔴 **The uncommitted-work rule → put to the human now.** Framed to him as what it actually is: *finished work
   is being stored in a place that is not storage, and when it vanishes it looks exactly like an engineer who
   never did it.* That second half is the part that makes it urgent — **you nearly acted on that inference, and
   you said so yourself.** It is a `CLAUDE.md` rule, so only he can change it; I am not proposing wording, I am
   giving him the failure and the two options you and Jason named.
2. **The `sid` run of `0029` — relayed with your "watch it, do not fire it" intact**, including that an
   `ALTER TYPE ... cannot run inside a transaction block` means PG < 12, is **not** breakage, and costs Jason
   five minutes. I passed on the instruction not to touch `uat` in the same sitting.
3. **Additional-teacher clash (teacher B on a 10:00 meeting and a 10:00 lesson) — your lean and mine agree: a
   soft warning at save, never a refusal.** Put to the owner as a business call. **Not blocking; do not wait.**

📌 **Noted for my own practice, from your entry and Jason's:** evidence goes in the TASK file, never only in the
log. Two of his 08-30 entries were lost to concurrent writes on one file — I lost a whole entry the same way on
08-31. **The TASK files are why the work was recoverable.**

## [--:--] Porter (PM) — ✅ Both owner decisions in. Commit rule is now a board standing rule. → @Sober @Jason @Fern
**1. The owner will COMMIT at the end of every batch.** Written up as a board standing rule with the incident
that caused it, so it survives this session. Two things it asks of the team, and I want them read as rules not
advice: **when a batch is code-complete, say so plainly in the log** so the commit point is visible; and
**evidence lives in the TASK file's `## Implementation Notes`, never only in the log** — that is the only reason
TASK-218/221/223 were reviewable and rebuildable after the tree was swept.
🔴 **And nobody concludes "it was never built" from an empty diff alone** — check `git reflog` and mtimes first.
That is how Jason proved destruction where absence was the easier story.
*(Recorded at project level. Making it bind every project is a workspace `CLAUDE.md` change — the human's or
Atlas's, not mine. I said so to him rather than editing a charter that is not Porter's.)*

**2. Teacher clash on an อื่นๆ booking: warn, never refuse** — his words, *"เตือนพอ ไม่ห้าม"*. Your lean and mine
were right. **AC-24 + AC-25 added to `REQ-078` with the Thai wording**, and AC-25 matters as much as AC-24: a
warning that fires on every save is trained away within a week, and then the real one is invisible too. The
warning **names the teacher and the clashing booking** — "there is a clash" with no detail is not actionable.
📌 `บันทึกต่อไป` is deliberately the affirmative button: the owner said allow, so the default path must not read
as an error being overridden.

**Still with the owner: the `sid` run of `0029`** (watched, not fired). Nothing else waits on me.

## [--:--] Porter (PM) — 🔴 `0029` ran; **`db:verify` is RED. Nothing restarted.** Diagnosis is yours. → @Sober
The owner ran `bun run db:migrate` in `smart-scheduler-back`. Output pasted below in full — **I am relaying it,
not interpreting it.** Diagnosing a migration ledger is not the PM's job and I am not going to guess at it.

✅ **Your PG-12 worry did NOT happen** — no `ALTER TYPE ... cannot run inside a transaction block`. Drizzle
reported `[✓] migrations applied successfully`, and `booking_other_price_chk ... does not exist, skipping` is a
DROP-IF-EXISTS notice, not an error.

🔴 **`bun run db:verify` exited 1 and the deploy is stopped there.** Verbatim:
```
Journal: 30 migration(s) · ledger drizzle.__drizzle_migrations_scheduling: 40 row(s)
Schema witnesses: 30 applied
🔴 3 migration(s) in the journal are NOT recorded as applied:
  0025_booking_cancel_reason   when=1783000000021  ⚠️ drizzle would SKIP this silently (ledger newest created_at=1783000000025 ≥ when)
  0026_course_leave_quota      when=1783000000022  ⚠️ same
  0027_course_size_sanity      when=1783000000023  ⚠️ same
⚠️ …the TASK-085 failure mode. Check `migrationsTable` in drizzle.config.ts and run `bun run db:seed-ledger`.
Deploy is NOT complete. Do not restart the app against this schema.
```
📌 **Two things I noticed and am reporting as observations, not conclusions** — both are yours to confirm:
1. **Journal 30 vs ledger 40 rows**, while the line above says *"Schema witnesses: 30 applied"*. Those two
   sentences do not obviously agree to me. **This is the number I would not want anyone eyeballing.**
2. The shell prompt shows the repo on branch **`dong`**, while the board's standing rule makes **`develop`**
   canonical. Stated as a fact off the screen, not as a cause.

**The board's rule 1 already prescribes the recovery** (`db:seed-ledger` dry-run → read → `--apply` → migrate →
verify). **I have NOT told the owner to run `--apply`** — that writes to the ledger, the ledger is already the
thing that looks wrong, and TASK-085 is the incident where this exact area took the customer's calendar down.
**I asked him only for the read-only dry-run output, for you.**

🔴 **And one check I asked him for first, because our own file says the two boxes are indistinguishable from this
output:** `SELECT count(*) FROM course_packages` — **`sid` ≈ 20, `uat` ≈ 201**. `.env` decides which box a
command hits, and it does not travel in git. **Nobody should reason about these numbers until we know which
database produced them.**

**Ball: @Sober** — diagnosis + the exact command sequence for the owner. **@Jason** stays on TASK-225; this
blocks the `sid` deploy, not the build.

## [--:--] Porter (PM) — ✅ Dry run is unambiguous. Green-lit `--apply` per the board's own rule 2. → @Sober
**Box confirmed `sid`:** `course_packages` = **32**. Not `uat` (≈201). ⚠️ Our reference said *"`sid` ≈ 20"* and
that number is **stale — `sid` grows** as QA fixtures and imports land. **Fixed in `PROJECT-STATUS.md`: the tell
is the ORDER OF MAGNITUDE — two digits vs three — never the exact value.** A stale landmark is how somebody
eventually concludes they are on the wrong box and stops a correct deploy.

**The dry run answers it by itself:** every one of the **30** migrations verified against a **witness in the
actual schema** — `0025` → `bookings_cancel_reason_chk` exists · `0026` → `course_packages_leave_quota_chk` ·
`0027` → `course_size_sanity_chk` · **`0029` → `booking_other_price_chk` exists.**
**Summary: 30 applied · 0 not applied · 0 need a human. 3 ledger rows to insert.**
⇒ **The schema is complete and correct. Only the ledger's bookkeeping is missing three rows.**

**I green-lit `--apply`** — this is board rule 2 executed exactly as written (dry-run → read → `--apply` →
migrate → verify), the gate it names is *"once the table above looks right"*, and every row carries a concrete
witness. It derives from the box's own schema, which is why the rule calls it safe. **I did not invent a
procedure; I followed an approved one after the evidence it asks for arrived.**

📌 **Two things for you afterwards, neither blocking:**
1. **Ledger has 40 rows against a 30-migration journal.** The tool is untroubled and inserts 3 more. **I still
   want to know what the surplus 10 are** — an unexplained row count is how TASK-085 hid.
2. The repo is on branch **`dong`**; the standing rule makes **`develop`** canonical. Reported as a screen fact.

## [--:--] Porter (PM) — ✅ `db:verify` GREEN on `sid`. `0029` is proven. **Nothing restarted, on purpose.** → @Sober
```
Journal: 30 migration(s) · ledger …: 43 row(s)
Schema witnesses: 30 applied
✅ every migration is recorded in the ledger AND witnessed in the schema.
```
Sequence ran exactly as board rule 2 prescribes: dry-run → read → `--apply` (3 rows inserted, **the shared
`drizzle.__drizzle_migrations` deliberately untouched — the script's own rollback evidence**) → `db:migrate` →
`db:verify` ✅. **`0029` and `0028` are both live and witnessed on `sid`.**

🔴 **I told the owner NOT to `pm2 restart`, and I want the reason on the record rather than in my head:**
restarting now would deploy **one task out of five**. TASK-225 / 226 / 227 / 228 are still open, and **TASK-227
carries the cancel-reason FE line that completes Jason's half of TASK-220's coupling** — shipping 224 alone is
precisely board rule 3, *"never ship a new server-side gate without the screen that opens it."* The migration
needed to be proven early; **the code does not need to go out early with it.** The schema change is live and
inert: nothing creates an อื่นๆ booking yet.

**@Sober — three things back to you:**
1. **Put the `sid` proof into TASK-224** — the board requires a migration TASK to state how it was proven, and
   this is the evidence: `0029_other_booking_type` witnessed by `booking_other_price_chk`, verify ✅ on `sid`,
   PG-12 worry did not materialise.
2. **When the batch IS ready to restart — is a BE-only restart safe against the unchanged FE?** TASK-224 touched
   the booking DTO (`displayName`). **I am asking, not assuming**; TASK-070/071 was a breaking response shape and
   it is the same shape of risk.
3. **The 10 surplus ledger rows are still unexplained** (40 → 43 now). Not blocking, still owed.

## [--:--] Sober (SA) — your three answers. **#2 found a real crash, and it changes what "inert" means.** → @Porter @Fern @Jason

### 1. ✅ The `sid` proof is now in TASK-224, where the board requires it

Full sequence recorded verbatim in the task: box confirmed `sid` (`course_packages` = 32) → `db:migrate` →
`db:verify` 🔴 → `db:seed-ledger` dry run (30 applied · 0 not applied · 0 need a human · 3 rows to insert) →
`--apply` → `db:migrate` → `db:verify` ✅. **`0029_other_booking_type` applied and witnessed by
`booking_other_price_chk`**; `0028` live in the same set. TASK-224 → **DONE**, board updated.

📌 **You read the RED correctly and I want that on the record:** the schema was never in question. The dry run
verified all 30 against witnesses **in the actual schema** before a single row was written, and the recovery was
board rule 2 executed exactly as written. **Refusing to green-light `--apply` before that output existed was the
right call** — the ledger was the thing that looked wrong, and TASK-085 is the incident where guessing here took
the calendar down. **And asking `SELECT count(*) FROM course_packages` before reasoning about any of it was the
best move in the sequence:** our own reference number was stale, `.env` decides the box, and it does not travel
in git. You caught a stale landmark instead of being caught by it.

### 2. 🔴 "Is a BE-only restart safe against the unchanged FE?" — **YES, but only while ZERO อื่นๆ bookings exist. And that is thinner than it sounds.**

You were right to ask instead of assuming. **I checked the FE rather than reasoning about it, and there is a
crash waiting:**

`smart-scheduler-front/src/lib/api/mappers.ts` — `dtoToBooking`:
```
:13   studentName: dto.student.name      ← unguarded
:18   subject:     dto.subject.name      ← unguarded
```
TASK-224's DTO sends **`student: null` / `subject: null`** for an `OTHER` booking. **`dtoToBooking` is the
CALENDAR's mapper**, so the first อื่นๆ booking that reaches it does not break one cell — **it throws a
`TypeError` and takes the whole calendar down.** Same family as TASK-070/071, which is exactly why you asked.

**For the four existing types nothing changes**: `student` and `subject` are always present, the values are
byte-identical, and `title` / `displayName` / `teachers` are additive fields an older FE simply ignores. So:

- ✅ **A BE-only restart is safe today** — because **nothing can create an อื่นๆ booking**: there is no FE form,
  and `createBooking` is staff-authenticated.
- 🔴 **The condition is a behaviour, not a property.** The API accepts `bookingType: "OTHER"` **right now**. One
  `curl`, one QA probe, one enthusiastic owner — and the calendar is down for everyone. **"Inert" is currently
  guarded by nobody trying.**
- ⇒ **Two things follow, please carry both:**
  1. **@Tanya must NOT create an อื่นๆ booking on `sid`** — by any route — until TASK-227 ships. This is the one
     footprint that is not self-cleaning: the damage is to every other user of the calendar, not to a row she can
     cancel.
  2. **I have made this §0 of TASK-227 — the FIRST commit in that task**, ahead of the cell work, with the exact
     lines. `dtoToBooking` is an **allow-list** (its own comment says so; TASK-170 was the defect of forgetting
     that), so the null-guards and `displayName` have to be added there deliberately.

**My recommendation on the restart itself stands with yours: don't.** Not because of this — because shipping 1
of 5 is board rule 3. But if a restart becomes necessary for another reason, **it is safe, under the condition
above**, and TASK-227 §0 is what removes the condition permanently.

### 3. The 10 surplus ledger rows — the mechanism, and the exact SQL to close it

**They are harmless to the check and I still want them named** — you are right that an unexplained row count is
how TASK-085 hid. What `db:verify` asserts is **journal ⊆ ledger** plus a schema witness per journal entry
(`missingMigrations` in `src/lib/migration-ledger.ts:89`); a *surplus* row satisfies both and is invisible to it.
So the count can drift upward forever without ever turning the check red. That is the part worth closing.

**Two mechanisms in this repo's own code explain a surplus, both documented at `migration-ledger.ts:37-70`:**
1. **Legacy tag rows.** The retired `scripts/db-check-migrate.ts` recorded migrations with `hash` set to the
   **tag string** (`'0004_teacher_work_days'`) instead of a sha256. `attributeLedger` matches those via
   `legacy-tag` — so if a migration has **both** its legacy-tag row and its real sha row, that is **2 ledger rows
   for 1 migration.**
2. **An edited `.sql` after it was applied.** The hash is of the file text, so editing a migration that already
   ran leaves the old row and adds a new one. Also 2 rows, 1 migration.

🚫 **I am not going to assert which of the two it is without looking** — that is exactly the guessing this
project keeps paying for. **DATA REQUEST, read-only, three small queries on `sid`** (pure SQL, as you asked;
please have the owner run them in his own psql session):

```sql
SELECT hash, created_at FROM drizzle.__drizzle_migrations_scheduling ORDER BY created_at;
```
```sql
SELECT hash, count(*) FROM drizzle.__drizzle_migrations_scheduling GROUP BY hash HAVING count(*) > 1;
```
```sql
SELECT CASE WHEN hash ~ '^[0-9a-f]{64}$' THEN 'sha256' ELSE 'tag-or-other' END AS kind, count(*)
FROM drizzle.__drizzle_migrations_scheduling GROUP BY 1;
```

The third one alone probably answers it: **if `tag-or-other` ≈ 10, it is mechanism 1** and the rows are ours,
attributable, and safe to leave (or to prune deliberately, in daylight). I will attribute the hashes against the
journal myself once the first query's output is in `../project-docs/`. **Not blocking anything.**

### 📌 Your second observation (`dong` vs `develop`) — it just became true, and it is GOOD news with one catch

I checked it again a minute ago instead of repeating what I found yesterday, and **the answer changed while I was
writing this entry.** Until today `dong`, `develop` and `origin/develop` were the same commit. Now:

```
dong            6dfa5a7  feat: add support for 'OTHER' booking type with flexible attributes
develop         7217599
origin/develop  7217599
git status      clean
```

✅ **The human has COMMITTED all four tasks** — `0028`, `0029`, the link-all script, mappers, schema, the tests,
16+ files in one commit. **That is the uncommitted-work rule being acted on, and it is the real fix**: this batch
can no longer be destroyed by a branch sweep. Jason's patch artifact has done its job and is now a belt over a
brace.

🟠 **The catch, and it is your call not mine:** the work sits on **`dong`**, and the standing rule (owner,
08-28) makes **`develop`** canonical — *"another team also builds on `develop`"*. So `develop` currently does
**not** have `0028`/`0029`. That is fine while nothing is deployed from it, and it is exactly the kind of fact
that stops being fine silently. **Merging is the human's — no agent touches git — so this is a note for him, via
you.**

📌 And it retires the "same commit" line I wrote yesterday: **a branch relationship is a fact with a timestamp,
not a property.** I would have been wrong to repeat it from memory, which is the whole reason I re-ran the
command.

**Ball: @Jason — TASK-225.** **@Fern — TASK-227, and §0 is the first commit** (it closes the calendar-crash
window); TASK-222 is unblocked too. **@Porter — the three SELECTs when convenient, and the Tanya note above.**

## [--:--] Jason (BE) — TASK-225 BUILT → REVIEW. **No migration.** One question gates Fern's picker. → @Sober
Charging an อื่นๆ booking is in. tsc **0** · `bun test` **1028 pass / 0 fail** (+24) · `drizzle/*.sql` = **30** =
journal tags, **unchanged** — TASK-224 already added the columns, so this task is **code only**.

📌 **Code-complete batch, per the new standing rule:** TASK-225's five files are done and reviewed-ready. The
commit point is visible here. (Also noting `dong` is still ahead of `develop` — the human's call, not ours.)

### Three things worth your eye
1. **The `other-booking` item's price is `0`, and that broke a real invariant** — `sale-items.test.ts` asserted
   *"every item has a price > 0"*. Good test; it caught exactly what it exists for. **I did not relax it to
   `>= 0`** — that would let a genuine card price silently become 0 one day. It now carves out **that one code
   by name** and asserts the placeholder is what it claims. And the seeded row carries
   `priceSource: "NOT A PRICE — the amount is typed on each booking"`, so the honesty lives in **`bo.item`**
   itself; `ensure-sale-items.ts` would otherwise have stamped it *"owner price card"*, which would be a lie.
2. 🔴 **A defect I introduced and fixed before submitting.** My first cut had the two `bo.item` lookups
   **outside** any `try`. `postBookingSale` protects its own writes — but those reads were mine, and an
   unreachable `bo` schema would have thrown straight out of `runEndOfDayJob`, losing that day's auto-attend,
   quota deduction **and** its `job_runs` row **to a bookkeeping read**. That is rule 1 of `sale-post.ts`, and I
   had broken it one file over. Wrapped, logged with the booking id, pinned with a test.
3. **Two of my own assertions were wrong, not the code** — recording them because both are source-test traps:
   `toContain("vat")` matches "deacti**vat**ed" in a comment, and an ordering assertion measured my own prose
   (the function's comment names `postBookingSale` before the first lookup). Both now strip comments and use
   word boundaries. **A guard that cries wolf on an English word is a guard someone deletes.**

### 🔴 One question that GATES TASK-226 — please answer before Fern builds the picker
`GET /catalog-items` returns every active INCOME `bo.item`, **including the frontoffice sale items this repo
seeds** (`first-trial`, `course-*`, `voucher-*`, the rentals, and now `other-booking`). Your ruling was that the
picker must not offer the **`sale-items.ts` product codes** — and it does not offer them *as codes*, but those
codes have `bo.item` rows, so **"Course 6h (onewheel)" would appear in Fern's list anyway.** The clean filter is
one predicate (`external_source <> 'smart-scheduler' OR external_source IS NULL`). **I did not add it**: your
recorded call was about *which catalogue*, and excluding this repo's own items is a slightly different decision
I would rather you make than assume.

### ⚠️ And one consequence nobody has walked through — money moving with nobody present
**An อื่นๆ booking that nobody marks is auto-attended at 23:30, and then it CHARGES.** Not new machinery — it is
the same consequence `jobs.service.ts:76-85` already names for trial/single after REQ-070 — but it lands
differently here: a charged อื่นๆ booking might be a school visit cancelled by phone and never touched in the
system, and it will invoice itself overnight. **I implemented the sweep as TASK-225 specifies** (AC-9 says อื่นๆ
auto-attends; I am not inventing an exception). Flagging it because the owner has **twice** refused to let money
move as a side effect. If he wants the charge to need a human mark, that is a carve-out in the auto-attend
block — a different task, and it needs his word.

**🚫 Not proven by me:** every DoD line about a movement landing needs a database. Nothing was run anywhere.
🚦 **Deploy ordering, per box:** `db:migrate` → `db:verify` ✅ → **`bun run sale:ensure-items`** → restart.
Without that seed the `other-booking` bucket does not exist and every typed-amount charge logs `NOT POSTED` and
vanishes — TASK-066 exactly. The code names the script in that error, so the fix is in the log line rather than
in someone's memory.

**Ball: @Sober** — TASK-225 review + the `/catalog-items` filter question (it gates @Fern's TASK-226).
**TASK-228** (teacher LINE for อื่นๆ) is the only BE row left in REQ-078 and I can take it next.

## [--:--] Fern (FE) — ✅ TASK-227 → REVIEW. Two of its stated assumptions were wrong; both checked, not remembered. → @Sober
`smart-scheduler-front`, `H:\scheduler\smart-scheduler-front`, HEAD **`b8b6fde`**. **On this machine `dong` IS
`develop`** — `git rev-list --left-right --count dong...develop` = **`0 0`**, all three refs on `b8b6fde`. Checked
rather than trusted. Full detail + evidence is in **TASK-227 §Implementation Notes**; this is the short version.

**Verified:** `bunx tsc --noEmit` **exit 0** · `bun test` **46 pass / 0 fail** (was 41/0 — +5, one new file) ·
`bun run build` ok.

### 🔴 First — my TASK-147 work was destroyed by the SAME sweep as Jason's, and the same thing saved it
The `dictionaries.ts` help keys and `settings.mock.service.ts` rows I wrote on 08-30 were **gone** from the tree
this morning. The front repo's reflog shows the identical `dong → develop → ff merge → dong` pattern Jason found
in scheduler-back. **They survived only because I had written them verbatim into TASK-147's Implementation Notes**
— which is exactly your and Jason's point, arriving a second time in a second repo. Re-applied; TASK-147 stays
BLOCKED on its Q1 (unanswered), and its board row now records the loss. **This is now two of two repos.**

### Two task assumptions that did not survive contact with `develop`
1. **"`CalendarLegendBar` picks the maps up automatically — check it renders."** It does **not**.
   `BOOKING_TABS_LEGEND` is a hand-written array, so widening `BookingType` adds no legend row and **the compiler
   says nothing**. อื่นๆ would have shipped into the grid under a legend that could not explain it. Added
   explicitly, with the trap written on the line. *The instruction to check instead of assume is what caught it.*
2. **"For อื่นๆ the program slot shows the title."** Grounded in TASK-224 rather than followed: `displayName =
   otherTitle ?? nickname ?? name` (`db/mappers.ts:143`) ⇒ when a title exists it is **already the name row**, and
   when it doesn't there is nothing to show. **The instruction can only ever duplicate the name or print nothing —
   there is no case where it adds information.** What AC-15 actually guards (no empty label, no dangling separator)
   is already true by construction: both cells gate on `!!subject` and the `·` only renders when both sides exist.
   Implemented the non-duplicating reading — **TASK-227 Q1**, one line to reverse if you meant otherwise.

### The rest, briefly
`displayName` on all **11** booking-surface readers (the 81 grep hits are mostly Student/Teacher/Course objects —
enumerated in the TASK); **0** local name fallbacks in the diff. `student`/`subject` made **nullable on purpose** so
tsc named every dereference — each fixed deliberately, no blanket `?.`. AC-18 placement is `teachers.some(...)` in
both grids, safe because the BE indexes the calendar on the **first** teacher only (`scheduler.service.ts:488`) so
a booking arrives **once** and there is nothing to dedupe; length-1 for the four lesson types makes **AC-20 true by
construction**. New `SharedTeachersMarker` **names** the other teachers rather than counting them. ⛔ `OTHER` added
to `canCancelWithReason` with the coupling comment extended to spell out **both** failure directions — and while in
that expression I removed a **duplicated no-op line** that came in with `b8b6fde` (declared in the TASK, not silent).

📌 **One new test file, and I want the reason on the record:** `dtoToBooking` is an allow-list literal that has
silently dropped a new DTO field **three times** (its own comments record TASK-170 and TASK-183). Dropping
`displayName` blanks every อื่นๆ name; dropping `teachers` puts the booking in one column instead of three. **Both
compile, and both look exactly like "the BE didn't send it."** Now pinned.

### 🔴 One DoD item I did NOT do, and I am not dressing it up
**The rendered 1600/1280/768/375 measurement is NOT done.** I ran the app locally in **mock mode only**
(`NEXT_PUBLIC_USE_MOCK=true`, API base overridden to an unroutable address so nothing could reach the live host
`.env.local` points at) and it served `/login` fine — but `/scheduler/*` is behind the auth proxy and the only way
through is a password field, which I may not fill. No QA-session route exists in this repo. **Same wall as TASK-142,
so same route: @Sober, please send it to @Tanya via @Porter** — the exact command and the fixtures (b13 is the
long-title, three-teacher case) are in the TASK. I have put the *static* analysis there too — both grids are
`min-w-max` over `minmax(150px/160px, 1fr)`, so a narrow viewport **scrolls the container instead of squeezing the
cell**, and the name is single-line `truncate` ⇒ ellipsis, never wrap — but **labelled as source analysis, not as a
measurement.** What it cannot tell you is where `ปิดปรับปรุงลานสเก็ตช่วงบ่าย` actually cuts, and that is the point
of the check.

**Also open: TASK-227 Q2** (a teacher can now be on an อื่นๆ **and** a lesson in one slot — additional teachers
aren't slot-checked — and the **day** cell then draws one and silently drops the other; commented in code, not
redesigned) and **Q3** (`BOOKING_TYPE_OPTIONS` is the bookings-table **filter**, hand-written, so staff cannot
filter to อื่นๆ — it belongs to neither 227 nor 226 and would fall through the crack; left unchanged, one line).

**Ball: @Sober** — TASK-227 review + Q1/Q2/Q3, and TASK-147 Q1 is still waiting from 08-30. **TASK-222 is mine
next** (221 is done); TASK-226 still waits on @Jason's TASK-225.

## [--:--] Jason (BE) — TASK-228 BUILT → REVIEW. **REQ-078's BE side is complete.** → @Sober
tsc **0** · `bun test` **1050 pass / 0 fail** (+22) · **no migration** (`drizzle/*.sql` = 30 = journal tags).
📌 **Code-complete batch** (standing rule): TASK-225 + TASK-228 are both done; the commit point is visible here.
That closes **every BE row in REQ-078** — 224 · 225 · 228. Only Fern's 226 remains, plus 227 in your review.

### The parts worth your eye
1. **"No empty label" needed no new code — it needed the right `null`.** `line(label, value)` already omits an
   empty field, and `bookingContext` yields `undefined` for a null student/subject, so the confirmation was
   *already* label-free. What was missing was the **name**. The title now renders on its **own line, no label**
   — it is not a student, and putting it behind `ob_l_student` would state something false. The schedule DID
   have a real bug waiting: `subjectName: r.subject?.name ?? "-"` would have printed `- · CONFIRMED`. It is
   `null` now and the segment is dropped.
2. **Every teacher, one message each.** `assignedTeacherIds` (primary first, then extras) drives the confirm
   loop. Never a shared body naming them all — the message is addressed to its recipient in that person's own
   language, so one body would change what the other four types read too. The returned `notification` still
   describes the booking's **own** teacher, so the FE's "ส่ง LINE แล้ว / ยังไม่ผูก LINE" is unchanged.
3. 🔴 **I had to change one of my own TASK-224 tests, and I want it on the record.** It asserted the join table
   is mentioned in `scheduler.service.ts` exactly twice — *"nothing outside the accessor READS it"*. TASK-228
   needs an **id-level** reader (the LINE paths hold a bare booking row in a transaction, no relation loaded),
   so the mapper accessor cannot serve them. **The test caught exactly what it was built to catch**, and the
   honest fix was not to raise the number: it now requires **every** mention to sit inside one of two *named*
   functions and counts them structurally, so a **third** reader still fails the suite. Two readers, one per
   context, both encoding the same order.
4. **AC-17 stayed unwritten and a test enforces the absence** — no branch for a state the schema forbids.

### Two observations in the TASK's §Questions, neither of them mine to decide
- **A studentless อื่นๆ confirm writes a SKIPPED *parent* outbox row for a parent who cannot exist.** Harmless,
  and I left it — it is the honest record of "we ran the parent path and there was nobody". ⚠️ But it colours a
  number you and @Porter read: `unlinkedParents` reach, and any "how many parents are unlinked?" glance at the
  outbox, now include bookings that never had a parent. One condition fixes it if it matters — separate task.
- **The CHARGE is invisible in the confirmation.** A teacher confirming a ฿5,000 school visit reads the title
  and the time, and nothing about money. Consistent with every other type, so I did **not** add it — but อื่นๆ
  is the first type where the confirmer is often also the person who typed the amount, and *"did I type the
  right number?"* is a question the message could answer. Owner's call via @Porter.

**🚫 Not proven by me:** no LINE message was sent to anybody and nothing ran against any environment. That a
real phone shows 3 teachers → 3 messages with the title where a name belongs is **@Tanya's**, in the owner's
window (REQ-077).

**Ball: @Sober** — TASK-225 + TASK-228 review. Still open from my side and gating others:
**(a)** the `/catalog-items` filter question (gates @Fern's TASK-226), **(b)** the auto-attend-charges-overnight
flag on TASK-225. **Nothing is waiting on me** — say the word if you want anything reworked.

## [--:--] Fern (FE) — ✅ TASK-222 → REVIEW. The posted-revenue band is in; the two states that matter need `sid`. → @Sober
Same tree as TASK-227 (`H:\scheduler\smart-scheduler-front`, HEAD `b8b6fde`, uncommitted). Contract read from
**Jason's code** (`sale-post.ts:226-242`, route `api.ts:267`), not from the task text. Detail in **TASK-222
§Implementation Notes**.

**Verified:** `bunx tsc --noEmit` **exit 0** · `bun test` **51 pass / 0 fail** (was 46/0 — +5) · `bun run build` ok.

**Three states, all visible; Confirm untouched in all three** (`disabled={!reasonCode}` only — this adds a
sentence, never a gate). 🔴 **`retry: false` on the query, deliberately:** react-query's default retries would
leave the dialog looking *clean* for several seconds while staff can already press Confirm, and **a clean dialog
is indistinguishable from "no money posted"** — the exact defect. Failing fast is what makes the band honest.

📌 **On "satang → baht in ONE place, with a test": there already IS one place** — `formatPriceMinor`. Writing a
new helper beside the dialog would have created a **second** one, which is the precise shape of TASK-169's 100×
bug. So I reused it and supplied the missing test: `139000 → "1,390"`, both wrong magnitudes asserted as *not*
the answer, and the discounted-trial case `139000 + (-20000) → "1,190"` — the "render `amountMinor`, never
re-derive it" ruling stated as **numbers** rather than as a comment someone can read past.

**One deliberate deviation (Q1):** the date renders `29/Aug/26` via `formatDateDisplay`, not the task's
`29/08/2026` — same reasoning as above, that helper calls itself "the ONE place a table turns a stored date into
what the user reads", and an inline `DD/MM/YYYY` would put two date conventions on one screen. One line to change.

### 🔴 Two DoD items I did NOT do — and for this task the mock cannot cover them either
Same auth wall as TASK-227 (no password from me, no QA-session route in this repo). **But note the sharper
problem here:** `getPostedSale`'s mock returns `null` **by design** — a throwing mock would leave the offline
dialog permanently in "could not verify" and train staff to ignore the one band that matters. **So offline shows
only the unchanged dialog. The `posted` and `error` bands can be seen ONLY against the real endpoint — `sid`,
which is Tanya's box and nobody else's.**

**@Sober — please route to @Tanya via @Porter with all three states named** (two cannot be reached by accident):
`posted` — 🔴 **pick a DISCOUNTED trial**, the one case where re-deriving instead of rendering `amountMinor`
shows a plausible but too-high number (฿1,590 where the truth is ฿1,190); `posted: null` — no band, no empty
space; **failure** — block the request in devtools, band must APPEAR and Confirm must still work. Plus both
languages **rendered** (TASK-210's lesson) and the 375 case.

**Q2, and I want it recorded rather than discovered:** because a reversal carries no `refId`, the band shows the
**identical** text whether or not someone has already reversed the sale. It cannot distinguish "not reversed yet"
from "already reversed" — which is exactly the situation that could produce a **double** reversal. Inherent to the
data, not a bug in this task; a `refId`-on-reversal change would be a different REQ. Your call whether it goes to
@Porter now or waits for SPEC-069 Q2's Thai to come back.

**Ball: @Sober** — TASK-222 + TASK-227 both in REVIEW (Q1/Q2 each, plus 227's Q3), and **TASK-147 Q1 has been
waiting since 08-30**. Nothing else is currently unblocked for FE: TASK-226 waits on @Jason's TASK-225,
TASK-111 on TASK-110, TASK-118 on TASK-116.

## [--:--] Sober (SA) — ✅ **222 · 225 · 227 · 228 all PASS.** REQ-078's BE side is complete. One follow-up cut. → @Jason @Fern @Porter

**Reproduced, both repos:** BE `tsc` **0** · the task-owned suites **105 pass / 0 fail** · `drizzle/*.sql` = 30 =
journal tags (**no migration** in 225 or 228 — re-counted). FE `tsc` **0** · `bun test` **51 pass / 0 fail** ·
build ok. (Still not running the full BE `bun test` — it reaches the live `sid` DB; that risk is with @Porter.)

### 🔴 The one thing that needs the human TODAY, before anything else

**The entire FE batch — TASK-222 + TASK-227, 20 changed files — is UNCOMMITTED right now.** `git status` in
`smart-scheduler-front`: 20 modified, HEAD `b8b6fde` = `develop`. **This is the exact state that lost three BE
tasks on 08-31**, and @Fern reports the **same sweep destroyed her TASK-147 work in the front repo** — the
reflog shows the identical `dong → develop → ff merge → dong` pattern Jason found. **That is two of two repos.**
The BE side is safe (the human committed `6dfa5a7`); **the FE side is not.** @Porter — please ask for a commit of
`smart-scheduler-front` now, not at deploy time.

📌 And both engineers' work survived only because they had written it into their **TASK files**. Twice in two
days, in two repos, by two people. **That is the workspace rule paying for itself, and it is now the reason I ask
for evidence in the TASK rather than in the log.**

### What each of them did that I would not have specified

- **@Jason, TASK-225 — refusing to relax `price > 0` to `>= 0`.** Widening an invariant to fit one exception is
  how a real card price silently becomes 0 a year from now. He carved out **one code by name** and put
  `priceSource: "NOT A PRICE — the amount is typed on each booking"` into **`bo.item` itself**, so a backoffice
  reader is not told *"owner price card"* about a placeholder. And he caught a defect in his own first cut: two
  bookkeeping **reads** outside the `try` would have thrown out of `runEndOfDayJob` and cost a night its
  auto-attend, its quota deductions **and** its `job_runs` row — rule 1 of `sale-post.ts`, broken one file over.
- **@Fern, TASK-222 — `retry: false`.** react-query's default retries would leave the dialog looking **clean**
  for seconds while Confirm is already pressable, and *a clean dialog is indistinguishable from "no money
  posted"* — the exact defect. Porter's rule was "fail loud"; the default retry obeys the letter and breaks it in
  practice. Neither he nor I had named that.
- **@Fern, TASK-222 — "there already IS one place".** I asked for satang→baht *"in one place, with a test"*; she
  saw that a new helper beside the dialog **creates** the second place, reused `formatPriceMinor`, and supplied
  the missing test — including the discounted case `139000 + (-20000) → "1,190"`. **The "never re-derive" ruling
  is now numbers, not a comment**: ฿1,590 ≠ ฿1,190 cannot be read past.
- **@Fern, TASK-227 — two of my instructions were wrong and she checked instead of following.** (1) *"the legend
  picks the maps up automatically"* — it does **not**; `BOOKING_TABS_LEGEND` is a hand-written array, so widening
  `BookingType` adds no row **and the compiler says nothing**. (2) *"the program slot shows the title"* — given
  `displayName`, that could only duplicate the name or print nothing. **Both corrections accepted.**
- **@Jason, TASK-228 — he had to change one of my TASK-224 tests and did it the hard way.** The LINE paths need
  an **id-level** reader, so the mapper accessor cannot serve them. **He did not raise the count from 2 to 3**;
  he made every mention have to sit inside one of two *named* functions, counted structurally — so a **third**
  reader still fails the suite. The guard kept its teeth. That is the same call as his `price > 0` carve-out,
  twice in one day.

### Answers given (detail in each TASK's §Review)
- **TASK-225 `/catalog-items` filter → EXCLUDE our own sale items.** He was right that it is a *different*
  decision from my recorded one, and it resolves the same way: those codes **have `bo.item` rows**, so without
  the predicate **"Course 6h (onewheel)" appears in the staff picker** and charging an อื่นๆ to it posts course
  revenue with no course. ⇒ **TASK-229 cut** (one predicate + one test). ⚠️ It carries the trap: a plain `<>`
  drops `NULL external_source`, which is *every backoffice item*, so the list would come back empty.
  🔴 **TASK-229 gates TASK-226**, board updated.
- **TASK-227 Q1** ✅ her reading; **Q2** the clash is with @Porter, but the **day cell silently dropping one of
  the two** is the sharper half and is now a named gap that must close **before อื่นๆ reaches `uat`** — a week
  grid and a day grid disagreeing about what exists is how staff stop trusting the calendar; **Q3** the
  bookings-table filter is one line and belongs to neither task ⇒ **put into TASK-226**, with the pattern named:
  *a `Record<BookingType,…>` fails the build when the union widens; a hand-written array of the same type does
  not.* Two found already — worth looking for a third.
- **TASK-222 Q1** ✅ `formatDateDisplay`, same reasoning as the money; **Q2** goes to @Porter **now**, not after
  the Thai comes back — see below.
- **TASK-147 Q1 — ✅ AUTHORISED, unblocked.** The contradiction was **mine**: I wrote *"no component change"* and
  then a DoD that cannot be met without one. Her two-line fix is right, and shipping half of it would have been
  worse than nothing — a help line reading *"…until {n} hours before"* looks like a broken product, not missing
  copy.

### 🔴 Four things for @Porter, none blocking the build

1. **Commit the front repo** (top of this entry).
2. **An อื่นๆ booking that nobody marks auto-attends at 23:30 and CHARGES.** Jason implemented AC-9 as written
   and flagged it rather than inventing an exception — correct. **What is new is not the mechanism but the
   size:** a trial or 1HR auto-posting has been true since REQ-070 and is bounded by the price card at ฿1,390.
   **A typed อื่นๆ amount has no ceiling** — an unattended school visit can invoice itself overnight for ฿50,000,
   with nobody present at either end. The owner has twice refused to let money move as a side effect; **this is
   the sentence to put to him.**
3. **The posted-revenue band cannot tell "not reversed yet" from "already reversed"** (a reversal carries no
   `refId`). Inherent to the data, not a bug. **Take it to the owner WITH the Thai wording (SPEC-069 Q2)** — the
   band says *"check, then reverse"* instead of *"go and reverse"* **because** of this, and the concrete harm is
   a **double** reversal: two IN movements against one sale, and the P&L just nets negative.
4. **A studentless อื่นๆ confirm writes a SKIPPED *parent* outbox row for a parent who cannot exist.** Honest as a
   record, but it colours `unlinkedParents` and every *"how many parents are unlinked?"* glance — **a number you
   report to the owner.** One condition fixes it, as its own task if he wants it.
   *(Also from Jason: the charge is invisible in the teacher's confirmation. Consistent with every other type, so
   not built — but อื่นๆ is the first type where the confirmer often typed the amount.)*

### 🧪 And for @Tanya, via @Porter — three rendered checks the engineers correctly could NOT do
Both hit the same auth wall (no password, no QA-session route), and both said so plainly instead of dressing it up.
- **TASK-222** — the `posted` and `error` bands are **unreachable offline by design** (the mock returns `null` so
  the offline dialog is not permanently in "could not verify"). 🔴 **For `posted`, pick a DISCOUNTED trial** — the
  one input where re-deriving shows a *plausible* wrong number (฿1,590 vs ฿1,190), and plausible is the only kind
  that survives a test. Then `null` (no band, no empty space) and **failure** (block it in devtools — the band must
  APPEAR and Confirm must still work).
- **TASK-227** — the 1600/1280/768/375 measurement, fixture **b13** (long title, three teachers). Fern's static
  analysis is in the TASK and is **labelled as source analysis, not as a measurement**, which is the only reason
  it helps rather than misleads.

**Ball: @Jason — TASK-229** (small, gates Fern). **@Fern — TASK-147 is unblocked** and TASK-226 opens the moment
229 lands. **@Porter — the four above.** REQ-078: BE complete (224 · 225 · 228), FE 227 done, **226 is the last row.**

## [--:--] Porter (PM) — 🧭 Full LINE flow designed at the owner's request. **4 questions for you.** → @Sober
He asked me to design the whole thing and said to consult you. Written into `REQ-079` §THE FLOW — **behaviour
only: what the parent sees, what the bot says, when a human takes over. The mechanism is deliberately yours**,
and it is written to be reviewed rather than to pre-empt you.

**The design rests on one idea:** a chat is either **ยังไม่รู้จัก** or **รู้จักแล้ว**, the menu differs, and in
both states **the bot is silent unless a button was pressed or an admin started something.**
Seven flows: admin-opened first entry (sets the family code, confirm-twice) · second guardian joining with
phone + code · เพิ่มนักเรียน with a **summary-and-confirm before it writes** · แจ้งลา · เช็คอิน · คอร์สของฉัน ·
คุยกับแอดมิน, which sits on every screen and is never removed by any flow.

**Three rules I want you to see specifically, because they are the ones that get engineered away:**
- **Every choice accepts `1` / `2` as well as a tap** — LINE on PC cannot tap **anything** (owner, today).
- **Two unexpected replies and the bot gives up and hands to a human.** A parent must never be trapped in a loop
  with a machine while a person is sitting in the same chat.
- **The wrong-code message states the attempts remaining, and the lockout still offers the admin door.** A silent
  counter is how people get locked out with no warning; a lockout with no human door is a dead end.

**My four questions are at the end of the REQ:** per-user menu switching · whether LINE's own OA tooling already
does the bot-mute-on-admin-reply · anything here we cannot do with the LINE mechanisms we already run · and
**whether this and `REQ-051` should be ONE build** — both are "phone → this family's children → act on one".

⚠️ **Still `CAPTURED`.** A design for the owner to approve, not a spec. **Do not cut tasks.** Your lane is
unchanged: REQ-078.

## [--:--] Sober (SA) — ✅ Your 4 LINE questions answered in `REQ-079`. **Three of four are already built; one thing worries me more than the design.** → @Porter

Answered in the REQ under your *"Open — @Sober"* section, read from the code rather than from memory. **No tasks
cut, REQ-079 stays `CAPTURED`, my lane is unchanged (REQ-078).** Short version:

**Q1 — per-user menus: a LINE feature, and we already run it.** `linkRichMenuToUser` (`line-rich-menu.ts:119`),
`setDefaultRichMenu` (`:110`), ids in `app_settings.line_rich_menu_ids`. **REQ-042 was exactly "a role change did
not switch the rich menu"** — fixed and owner-verified 08-16, so this path is in production, not theoretical.
Two menu sets = two rich menus + one call. 📌 Make **ยังไม่รู้จัก the DEFAULT** and **รู้จักแล้ว the per-user
link** — then a brand-new follower gets the right menu with **no code running at all**.

**Q2 — bot-mute-on-admin-reply: 🚫 I will not answer this from memory, and you should not accept a guess.** We do
not implement it. The platform has an account-wide **Bot/Chat** response mode and a webhook `mode`
(`active`/`standby`) field, but whether **per-conversation** muting exists on this OA's plan is a fact about the
owner's LINE Official Account Manager, not about our code.
🔴 **One concrete thing to ask him:** *open one chat in OA Manager — is there a per-chat bot/manual switch, or
only the account-wide setting?* **Account-wide is not good enough for your Rule 4** — one admin replying to one
parent would silence the bot for **every** family. 📌 If LINE lacks it, ours is small: the state table
(`line_link_sessions`) and the precedence rule (`decideMessageRoute` — *"an in-progress conversation wins"*)
already exist; Rule 4 is one more column read in the same place. **Don't rebuild it if LINE has it; don't assume
it does either.**

**Q3 — nothing in the seven flows needs a mechanism we don't already run.** Push-with-retry, reply tokens, quick
replies, postbacks, per-user rich menus, multi-turn state, per-user language, admin recipient list: all live.
📌 **Your `1`/`2` rule is already satisfied by what is deployed** — taps and typed keywords go through the **same
handlers** (`line-webhook.service.ts:4`), and `CHOOSE_ROLE` already accepts a typed `2`. The owner's PC
constraint costs nothing.
🆕 The **only** new state is a counter (two-strikes-to-human; attempts-remaining then lockout) —
`line_link_sessions` has no attempt column. A column, not a capability. ⚠️ **It must reset on success and expire
with the session**, or a parent who mistypes twice in March is locked out in June.
🟠 **One operational unknown I could not map to anything we run:** Flow 1 has *an admin opening the door in a
specific chat*. We can push to a `lineUserId` — but **an admin in OA Manager sees a chat, not a user id.** How the
admin's action reaches our system is the thing to settle before this is ever specced; a keyword the admin types
in the chat is the cheapest answer, since our webhook already sees every inbound message.

### 🔴 And the thing that worries me more than any of the four

**Your own Q3 answer today: `sid` and `uat` share ONE LINE channel.** ⇒ **There is no safe rehearsal for bot
behaviour.** Every other REQ we ship gets proven on `sid` first; this one cannot be. A half-finished flow tested
on `sid` reaches the **two real linked teachers and any linked parent on `uat`** — real people, in the customer's
account, mid-experiment.

That is a **bigger risk to REQ-079 than anything in the design**, and it is not a design question — it is a
precondition. **Please put it to the owner before he approves the flow, not after**: either a second LINE channel
for `sid`, or an explicit accepted risk with a named window (the same shape as his webhook-flip window). I would
rather he decide this while it is cheap.

**Q4 — REQ-051: share the LOOKUP, not the BUILD — and the dependency runs the OTHER way.** They share a query and
a PII rule (specify once, build once), but the trust models are opposite: REQ-051 is a **public, no-login page
that burns a paid session**; REQ-079 is an **authenticated chat**. Merging parks REQ-079 behind REQ-051's three
unanswered decisions (SPEC-050 — the admin code, the rate-limit model 🔴 *no rate limiter exists anywhere in this
codebase*, and whether a public check-in is acceptable at all).
📌 **The useful direction is the reverse of your question:** REQ-051's weakest point is its admin code, whose only
answer today is the **shared static `"229"`**. REQ-079 introduces a **per-family 6-digit code**. ⇒ **Settle
REQ-079's auth model first, then re-open SPEC-050 decision #1 with it.** Sequencing, not a merge, and free.

📌 On your design itself, since you asked to be reviewed rather than agreed with: **the three rules you singled
out are the right three.** "Two unexpected replies and hand to a human" is the one I would have had to add — a
parent trapped in a loop with a machine *while an admin is sitting in the same chat* is the failure that makes
people stop using the OA entirely, and it is invisible in every happy-path demo.

**Ball: @Porter** — the OA Manager question (Q2), the shared-channel precondition, and the REQ-051 sequencing.
**Unchanged from my last entry:** @Jason TASK-229 · @Fern TASK-147 (unblocked) then TASK-226 · and **the front
repo is still uncommitted.**

## [--:--] Sober (SA) — 🔴 New standing rule from the human, written into `SA-Lead.md`. + the BALL. → @everyone

**The human, direct to me: end every session with where the ball is.** Written into my charter
(`ai-worker/SA-Lead.md`, new standing-rule section) rather than kept in a chat I will not have next session —
the amnesia rule. It binds my role from now on: **every role named, every session, including the ones with
nothing to do and including me.** The role most often left out is the one that is free, which is exactly the
fact a planner needs.

**BALL — as of the end of this session**

- **@Jason — TASK-229.** One predicate + one test on `/catalog-items` (exclude our own seeded sale items).
  🔴 It **gates @Fern's TASK-226**, so it is small and first. Mind the `NULL external_source` trap named in the
  task — a plain `<>` returns an empty list, which will look like a broken endpoint.
- **@Fern — TASK-147** (unblocked today, Q1 authorised: make the two-line change), **then TASK-226** the moment
  229 lands. TASK-226 is **the last open row in REQ-078**.
- **@Porter — six things, one of them today:**
  1. 🔴 **The front repo is still uncommitted** (22 files: TASK-222 + 227 + the re-applied TASK-147 work). This is
     the exact state that destroyed work in BOTH repos on 08-31. The BE side is committed; the FE side is not.
  2. An อื่นๆ booking nobody marks **auto-attends at 23:30 and charges** — and unlike a trial, a typed amount
     has **no ceiling**. The owner has twice refused to let money move as a side effect.
  3. The posted-revenue band **cannot tell "already reversed" from "not yet"** — take it to the owner *with* the
     SPEC-069 Q2 Thai wording; the harm is a double reversal.
  4. A studentless อื่นๆ confirm writes a **SKIPPED parent outbox row** for a parent who cannot exist — it
     colours the unlinked-parents number you report to him.
  5. **LINE (REQ-079):** the OA-Manager per-chat bot/manual question · 🔴 **`sid` and `uat` share one LINE
     channel, so there is no safe rehearsal** — a precondition for the owner *before* he approves the flow ·
     and the REQ-051 sequencing (settle REQ-079's auth model first).
  6. Still owed from earlier: the **three read-only ledger SELECTs** (the 10 surplus rows), and the
     **`bun test` → live `sid` DB** risk needs its own task.
- **@Tanya (via @Porter) — two rendered checks the engineers correctly could not do** (auth wall, no QA route):
  **TASK-222** the three band states — 🔴 for `posted` pick a **DISCOUNTED trial** (the one input where a wrong
  number looks plausible: ฿1,590 vs ฿1,190) · **TASK-227** the 1600/1280/768/375 measurement, fixture **b13**.
  ⚠️ And still standing: **do not create an อื่นๆ booking on `sid` by any route until TASK-227 ships** — the
  current FE would crash the whole calendar on it, not one cell.
- **me (Sober) — nothing blocking.** REQ-078 is BE-complete (224 · 225 · 228) + 227; I review 229 and 226 as they
  land. REQ-079 is answered and stays `CAPTURED` — I cut nothing there until the owner approves the flow.

## [--:--] Fern (FE) — ✅ TASK-147 → REVIEW. Q1's two lines + the regression you made the condition of it. → @Sober
**Your first instruction first: the re-applied 08-30 work IS in the tree.** `grep -c leave_cutoff_hours_fulltime`
→ `dictionaries.ts` **2** (en+th), `settings.mock.service.ts` **1**. HEAD `b8b6fde`, `dong` ≡ `develop`. Checked
before building on it, as you said.

**Verified:** `bunx tsc --noEmit` **exit 0** · `bun test` **58 pass / 0 fail** (was 51/0 — +7) · `bun run build`
ok, `/scheduler/settings` emitted.

**The change is the two lines you authorised — but the miss-check is no longer inline, and that was your own
condition talking.** You wrote *"the whole safety of dropping the gate rests on it"*. A load-bearing rule buried
in a JSX condition cannot be tested, and the regression you asked for would otherwise have needed a DOM harness
this repo does not have (devDeps carry no testing-library; `playwright` is the REQ-001 acceptance harness, not a
unit-DOM setup). So it is now a named pure function — `lib/scheduler/setting-help.ts`, same precedent as
`eligible.ts` being lifted out of `BookingModal` — and the component reads
`const helpText = settingHelp(t, row.key, row.value)`.

**The regression: `setting-help.test.ts`, 8 cases.** It builds `t` from the **real** dictionaries with the
**real** resolver, so it exercises `t()`'s actual miss behaviour instead of my guess about it. The load-bearing
case is the one you named — `teacher_change_notice_days` and `checkin_early_minutes` (the two registered
settings with no help copy) must return **`null`**, so dropping the gate gave them nothing. 📌 And I pinned a
second failure shape you did not name: an unregistered key must return `null` **and explicitly not** the string
`"settings.help.some_future_rule"` — because the bad outcome is not "a line appears", it is **the raw KEY
appears**, which is `keys.test.ts`'s defect arriving from the opposite direction. Plus: `notify_on_leave`
byte-identical to before; `{n}` never left on screen; value `6` ⇒ the sentence says **6** and not 3 (AC-4); and
the two rows' Thai differs and names ครูประจำ / ครูฟรีแลนซ์ (they sit adjacent — identical prose would be useless).

### 🔴 One DoD item not done — but please do NOT route this one like TASK-222
Same auth wall (no password from me). **The difference matters:** the two rows are in the mock, so **the offline
app covers this DoD item completely** — no `sid`, no test account, no real data. It is a one-minute check for
anyone who can log in, and the exact command + what to look for are in the TASK. Sending it to @Tanya's `sid`
queue behind TASK-222/227 would make a trivial check wait on a scarce environment.

**Also still open, unchanged, needs nothing from me:** `unit` renders raw, so these rows read
`ค่าปัจจุบัน: 3 hours` in the Thai screen. Pre-existing (`days`/`minutes` already do it), so not a regression —
but `hours` is new to that screen. Yours whether it joins the settings-label-i18n follow-up.

**Ball: @Sober** — TASK-147 review. **Nothing else is open for FE:** TASK-226 waits on @Jason's TASK-229,
TASK-111 on TASK-110, TASK-118 on TASK-116; 222 and 227 are code-DONE with their rendered checks in @Tanya's
queue. ⚠️ And the front repo is **still uncommitted** — now 24 files, TASK-147 + 222 + 227 together.

## [--:--] Jason (BE) — ✅ TASK-229 → REVIEW. **@Fern is unblocked.** I made the SQL prove your NULL trap. → @Sober
One predicate + one test file, as you cut it. tsc **0** · `bun test` **1058 pass / 0 fail** (+8) · no migration.
📌 **Code-complete** (standing rule) — the commit point is visible here. **TASK-226 is unblocked.**

```ts
sql`${boItem.externalSource} is distinct from ${SALE_SOURCE}`
```

### 🔴 I did not want to take the NULL trap on trust, so I made the SQL show me
Your warning *is* the task, and *"a plain `<>` returns an empty list"* is easy to nod at and hard to verify
without a database — which I cannot use. So the test asserts the **emitted SQL**: `db.select()….toSQL()` builds
a query offline and **executes nothing**, so it is legal for me and it is evidence rather than a source grep.
- the chosen construct compiles to **`is distinct from`**;
- `ne(boItem.externalSource, SALE_SOURCE)` compiles to **`<>`** — the operator that yields NULL against a NULL
  column, so the row is dropped. **The shipped-empty-picker bug is now recorded as evidence in the suite instead
  of as folklore in a task file.**

### On "assert by code" — I read your intent, not your list, and want you to check that
You asked the seeded codes be asserted *"by code, so the next `sale:ensure-items` entry cannot quietly reappear"*.
**Listing `first-trial` / `course-*` / … in a test would be a second list that drifts** — the exact failure this
project keeps paying for. What actually gives the guarantee is that `ensure-sale-items.ts` stamps **every**
`SALE_ITEMS` row with `externalSource: SALE_SOURCE` and the predicate excludes that source — so the test asserts
**the seeder and the filter agree on one value**, plus a spot-check of the named codes. A code added tomorrow is
excluded with nobody editing the test. **If you wanted the literal list, say so and it is a one-line change.**

### 🔴 DATA REQUEST — one read-only SELECT, and I think it should land before TASK-226 is *judged*
The predicate is right and the SQL is witnessed. But **"the picker shows something" is a different claim from
"the filter is right"**, and only data answers it. If every `bo.item` on `sid` carries an `external_source`, the
picker is **correctly empty** — and @Fern will reasonably read an empty dropdown as a broken endpoint and lose an
afternoon. That is the same misdiagnosis-in-the-wrong-place as the NULL trap itself, one layer up.

```sql
SELECT COALESCE(external_source, '(null)') AS source, direction, active, count(*)
FROM bo.item GROUP BY 1, 2, 3 ORDER BY 1, 2, 3;
```
The count of `INCOME` + `active` rows whose source is **not** `smart-scheduler` **is** the length of Fern's
dropdown. **> 0** ⇒ ship. **= 0** ⇒ the endpoint is right and the picker is empty *by data, not by defect* — the
owner must create the chargeable items in the backoffice first, and **TASK-226 then needs an empty-state that
says so** ("ยังไม่มีรายการในแคตตาล็อก — สร้างที่หลังบ้านก่อน") rather than a blank box. **That is a real FE
requirement nobody has written down**, cheap now and expensive after the owner sees an empty dropdown.
Routing it up rather than guessing. **It does not block TASK-229**, which is complete either way.

**Ball: @Sober** — TASK-229 review, the "assert by code" reading above, and the DATA REQUEST → @Porter.
**Nothing is waiting on me.** For your end-of-session ball line: my lane is clear — REQ-078's BE side is 224 ·
225 · 228 · 229, all in your review; the four ancient `TODO`s against my name (**TASK-103 · 110 · 116 · 117**,
POST-GO-LIVE / SPEC-028·032·034) are still on the board claiming to be mine. **Send them or re-mark them
`PARKED`** — I have asked twice and I would rather it be a decision than a habit.

## [--:--] Porter (PM) — 🔴 STOP raising commits with the human. His instruction, effective now. → @everyone
> *"commit แล้ว บอกทีม เลิกยุ่งเรื่อง commit ฉันจะทำเองเมื่อถึงเวลาของฉัน"*

**The front repo is committed.** And from now on:

- **Nobody reports, chases, or asks about commit state — not in the log, not in a hand-off, not as a "just a
  reminder".** He commits on his own schedule. It is his repo and his call, and he has now said so twice.
- **"This batch is code-complete" is still worth writing** — that is ordinary status, and it is what makes a
  natural commit point visible without anyone being chased. **The difference is the ask: state your work,
  do not request his.**
- **What actually protects us is unchanged and is ours, not his:** every load-bearing fact lives in the TASK
  file's `## Implementation Notes`. That is the only reason TASK-218/221/223 survived the 08-31 sweep, and it
  works whether or not anything is committed. Board standing rule updated to say exactly this.

📌 I am the one who put commit state into the reporting loop, so I am the one removing it. It came from a real
incident, but the fix I attached to it made the human's housekeeping into the team's status item — and that is
not what the incident asked for.

## [--:--] Porter (PM) — ✅ LINE flow CLOSED. One decision left, and it is a precondition. → @Sober @everyone
> *"รีบสรุปจบได้แล้ว flow line"* — fair. It was generating questions faster than it was closing them.

**Written into `REQ-079` §FLOW CLOSED. Three of the four open items I closed myself:**

1. **Rule 4 (admin reply mutes the bot) — we build it; question withdrawn.** Your own answer contained the
   resolution: platform switch if it exists, otherwise **one column read** in `decideMessageRoute` where the
   precedence rule already lives. **The flow is unaffected either way**, so it was never the owner's decision —
   I turned an implementation detail into a question and put it on his desk. 🔴 The hard requirement stays:
   **account-wide muting is not acceptable**, one admin answering one parent must not silence every family.
2. **How the admin opens the door — requirement closed, mechanism yours.** *"An admin must start Flow 1 for one
   specific chat from LINE OA Manager on a PC, without leaving it."* **I did not choose between your two
   candidates**, because both turn on a platform fact I have not verified — including your own caution that an
   admin's reply is **outbound** and may never reach our webhook. **Verify before designing on it.**
3. **REQ-051 sequencing — your direction, not my question.** REQ-079's per-family code is the answer to
   SPEC-050's weakest point (the static `"229"`). Settle REQ-079's auth first, then reopen SPEC-050 decision #1.

🔴 **The one thing left is the owner's and it is a precondition, not a follow-up: the shared LINE channel.**
Put to him with my recommendation (**a second channel for `sid`**) and the fallback (an accepted risk with a
named window, like his webhook flip). **You were right to rank this above the design** — building a
conversational system with no rehearsal environment is a decision, and it should be taken rather than discovered.

**`REQ-079` stays `CAPTURED`. Cut nothing.** Your lane is unchanged.

## ✅ The channel precondition — CLEARED by the owner, 2026-09-01

> *"ตอนนี้ซ้อมได้ เพราะเดี๋ยว uat เขาเปลี่ยนเป็นอันใหม่ทีหลัง ตอนนี้ uat ก็ไม่มีลูกค้าจริง ๆ ใช้"*

**Rehearsal on `sid` is allowed now.** The customer's real OA will be connected later as a separate account, and
the current shared channel has no real parents on it. **The precondition is answered; the flow has no blocker
left with the owner.**

🔴 **Two things written down so this permission does not outlive the conditions that make it safe.**

**1. It is not empty — 2 real teachers ARE linked on `uat`.** "No real customers" is true of **parents** (0 of
180) and it is what makes parent-flow rehearsal safe. It is **not** true of teachers.
⇒ **Rehearse the parent flows freely. Never fire an outbound message at the two linked teachers as part of a
rehearsal.** That costs nothing — no flow in this REQ is teacher-facing — and it keeps the sentence honest.

**2. 🔴 The permission EXPIRES, and the trigger is a moment, not a date:**
> **This freedom ends the instant either (a) the customer's real OA is connected, or (b) a real parent links on
> `uat` — whichever comes first.** From that moment, `sid` needs its own channel or rehearsal stops.

📌 **Written as a trigger on purpose.** A permission granted because *"nobody is using it yet"* is the kind that
quietly survives the day someone starts using it — and the person rehearsing will have no way to notice. It is
the same shape as the `uat` remote-DB whitelist line that was opened for diagnostics on 08-16 and is still open.

**@Sober — no blocker remains with the owner on `REQ-079`.** It still needs his GO and a place in his order
before you spec it; I am asking him for both now. **Cut nothing until I say so.**

## [--:--] Sober (SA) — ✅ TASK-229 + TASK-147 both PASS. **@Fern is unblocked; TASK-226 is the last row in REQ-078.** → @Jason @Fern @Porter

**Reproduced, both repos:** BE `tsc` **0** · `catalog-items.test.ts` **8/0** · migrations 30 = 30, no new one.
FE `tsc` **0** · `bun test` **58 pass / 0 fail**. Both verified at source, not from the notes:
`scheduler.service.ts:1216` (`is distinct from`, with the NULL reasoning on the line) and
`setting-help.ts:28` wired at `SettingsContent.tsx:131`.

📌 **@Porter — your commit instruction is taken.** It is out of my reporting, out of my BALL, and I will not raise
it again. Your reasoning is right and it is the better rule: **state your work, do not request his.** What
protects us was never the commit — it is that every load-bearing fact lives in the TASK's Implementation Notes,
which is what actually survived 08-31.

### They did the same thing today, in two repos, and it is worth naming once

**Both of them tested the MECHANISM rather than their belief about it.**

- **@Jason** turned my warning into evidence. *"A plain `<>` returns an empty list"* is the kind of claim that
  gets nodded at and never checked, because checking it looks like it needs a database. He used `.toSQL()` —
  **compiles without connecting**, so it is legal under the no-DB rule — and pinned **both** sides: the chosen
  construct emits `is distinct from`, and `ne()` emits `<>`. **The trap now lives in the suite instead of in
  folklore.** The next person who "simplifies" it gets a red test with the reason attached, instead of an empty
  dropdown and a wasted afternoon.
- **@Fern** built her `t` from the **real** dictionaries and the **real** resolver rather than a stub, so the
  regression exercises `t()`'s actual miss behaviour instead of her model of it.

**And each of them overruled the literal wording of my task in favour of what I actually needed:**
- I asked Jason to assert the seeded codes **by name**; he saw that a list in a test **is a second list that
  drifts** and asserted the property instead — *the seeder and the filter agree on one value*. A code added to
  `SALE_ITEMS` tomorrow is excluded with nobody editing the test. **The guarantee I wanted, not the mechanism I
  named.**
- I made a miss-check *"the whole safety"* of Fern's change and then left it inside a JSX condition this repo
  cannot test. She lifted it into `settingHelp` — **a guarantee that cannot be tested is a guarantee that
  decays** — and added the failure shape I had not named: an unregistered key must not render the **raw key**
  `"settings.help.some_future_rule"` on a staff screen. The bad outcome is not *a line appears*; it is *the key
  appears*.

### Two rulings that follow from their questions

1. 🔴 **TASK-226 gets an explicit EMPTY STATE for the catalogue picker — unconditionally, not as a contingency.**
   `/catalog-items` can legitimately return zero (a box where nobody created one; the day the owner archives the
   last). **An empty dropdown with no words is indistinguishable from a broken endpoint** — which is exactly the
   misdiagnosis Jason's DATA REQUEST exists to prevent, so building it only *if* the query says zero would leave
   the same hole open on a later day. Added to the task, with the typed amount still working in that state.
2. **The routing distinction @Fern drew is right and I am keeping it as a rule.** TASK-222/227 need `sid` because
   their states are **unreachable offline**. TASK-147 is **fully covered by the mock** — it needs a login and
   nothing else. ⇒ Routed to @Tanya as a **LOCAL** check, explicitly **not** in the `sid` batch. Putting a
   one-minute local check behind two environment-bound items would make the trivial thing wait on the scarce
   resource.
   *(`unit` rendering raw — `ค่าปัจจุบัน: 3 hours` — is pre-existing, not a regression; it joins the
   settings-label-i18n follow-up. ⚠️ `hours` is new to that screen, so it is the row a reader notices first even
   though the bug is old.)*

### 🔴 @Porter — one DATA REQUEST, and it should land before TASK-226 is judged

Jason's, and his framing is the useful part: **"the filter is right" and "the picker shows something" are
different claims, and only data answers the second.** Pure SQL, read-only, for the owner in his own psql session:

```sql
SELECT COALESCE(external_source, '(null)') AS source, direction, active, count(*)
FROM bo.item GROUP BY 1, 2, 3 ORDER BY 1, 2, 3;
```

**What we need:** the count of `INCOME` + `active` rows whose source is **not** `smart-scheduler`. **That number
is the length of Fern's dropdown.** `> 0` ⇒ she has real items to render. `= 0` ⇒ the endpoint is right and the
picker is empty **by data, not by defect** — and the owner needs to create the chargeable อื่นๆ items in the
backoffice before this feature means anything to him.

**Also for you, as a decision rather than a discovery** (Jason named it and correctly did not decide it): the
endpoint returns *every* backoffice INCOME item, with no notion of *"is this appropriate to charge a customer
for"*. If his backoffice carries INCOME rows nobody should put in front of staff, that is a REQ, not a predicate.

**BALL**

- **@Jason — nothing.** Every BE row in REQ-078 is done (224 · 225 · 228 · 229). Free for the next thing you cut me.
- **@Fern — TASK-226**, unblocked now. **It is the last open row in REQ-078**, and it carries three extras: the
  catalogue **empty state**, the bookings-table **filter** line (your TASK-227 Q3), and ≥1 teacher (AC-19).
- **@Porter** — the `bo.item` DATA REQUEST above (before 226 is judged) · the "not-customer-facing INCOME items"
  decision · still open from earlier: the อื่นๆ **auto-attend-charges-overnight** exposure (no ceiling on a typed
  amount) · the **reversal-blind band** + SPEC-069 Q2 Thai · the **SKIPPED parent outbox row** colouring your
  unlinked-parents number · the three **ledger SELECTs** · the **`bun test` → live `sid` DB** task ·
  and 🔴 **REQ-079's shared LINE channel** precondition with the owner.
- **@Tanya (via @Porter)** — **local, not `sid`:** TASK-147's help lines (one login, one minute). **In the `sid`
  queue:** TASK-222's three band states (for `posted`, a **discounted** trial) and TASK-227's 1600/1280/768/375,
  fixture b13. ⚠️ Still standing: **do not create an อื่นๆ booking on `sid` until TASK-227 ships.**
- **me (Sober) — nothing blocking.** I review TASK-226 when it lands, which closes REQ-078. REQ-079 stays
  `CAPTURED`; I cut nothing there until the owner settles the channel question.

## [--:--] Sober (SA) — 🔴 Correction: the ball is ONE, not five. Charter fixed. → @everyone

**The human, immediately after my last entry: do not split the ball.** He is right and the correction is sharp.
My previous entry ended with a five-row block — one line per role — which is **a status table wearing the word
"ball"**. A ball on five people is a ball on nobody: it hands the reader the job of working out who actually
moves next, and that is the exact job the line exists to do.

`SA-Lead.md` rewritten: **one name, last line, nothing after it.** A queue is not the ball — decisions parked
with @Porter, DATA REQUESTs, QA items waiting on an environment all live on the board and in the REQ/TASK files,
and they do not compete for it. Everything I listed last time is still recorded there; none of it was the ball.

Restating it properly for right now: TASK-229 is done, so **the only thing anyone is waiting on is TASK-226** —
the last open row in REQ-078. Nothing blocks it, and nothing else moves until it lands.

**BALL: @Fern — TASK-226.**

## [--:--] Porter (PM) — 🟢 **GO on REQ-079 (owner's REQ-016). `READY_FOR_SA`, queued #2.** → @Sober
> *"GO เอาเลย ให้อยู่คิวถัดจาก REQ-005"*

**Order: REQ-005 → REQ-079 → REQ-076 → REQ-051 → REQ-077 → REQ-028 → the `REQ-BO` block.** Board row added,
`OWNER-LIST.md` §THE ORDER updated.

**`REQ-079` is complete and specc-able:** 7 flows · the bot/human rules · **22 acceptance criteria written on
GO** · the Thai wording throughout · and a customer-facing explainer the owner is sending them (§12), which I
will keep in step with §5 — if the flow changes, that copy changes.

**Three ACs I want you to notice, because they are requirements and not polish:**
- **AC-1** — with no admin having opened the door, the bot stays silent **even if the parent types the exact
  menu words**. That is Rule 2 made testable.
- **AC-17** — when an admin takes over one chat, **the bot must still work normally in every other family's
  chat.** That is the account-wide-muting trap, written where Tanya will catch it.
- **AC-8** — the lockout counter **resets on success**. Otherwise a parent who mistypes twice in March is locked
  out in June, and nobody would ever reproduce it.

📌 **A sequencing worry I checked rather than carried:** REQ-077 later moves the customer to a **new** LINE OA
and links are per-OA, so they do not survive it. **This design does** — family, children and the 6-digit code
live in **our** DB, not in LINE. After the move a parent re-enters phone + code from any device. **Building
REQ-079 before REQ-077 costs nothing**, which is why #2 is safe.

⚠️ **Before you cut the spec: settle §6 — how an admin's "open the door" reaches us from OA Manager.** Your own
caution stands (an admin's reply is outbound; our webhook sees inbound). **AC-1 depends on the answer.**
⚠️ Two ACs (**AC-9** duplicate-name, **AC-20** parent deletion) encode **my** recommendation, not his ruling —
flagged in §14, built as recommended unless he says otherwise. If he rules differently only those two change.

**Your lane is still REQ-078** — this is next, not now.

## [--:--] Fern (FE) — ✅ TASK-226 → REVIEW. REQ-078's last row. Found your third array — and two defects tsc could not see. → @Sober
`H:\scheduler\smart-scheduler-front`, HEAD **`4ed910f`**. `dong` is one commit AHEAD of `develop`, so nothing on
`develop` is missing here — checked (`rev-list --left-right --count HEAD...develop` = `1 0`), not assumed.
Full detail in **TASK-226 §Implementation Notes**.

**Verified:** `bunx tsc --noEmit` **exit 0** · `bun test` **76 pass / 0 fail** (was 58/0 — **+18**) ·
`bun run build` ok.

### You said "worth looking for a third one." There is one, and it is the worst of the three
`BOOKING_TABS` in `BookingModal` — the create-form's own tab list. Missing `OTHER` there would have meant
**staff could not create an อื่นๆ booking at all**, with every other piece of REQ-078 shipped and working. All
three arrays now carry a comment naming the trap on the line itself: *a `Record<BookingType,…>` fails the build
when the union widens; an array of the same type does not.*

### 🔴 Two defects the compiler was perfectly happy with
1. **`createBooking` would have refused EVERY อื่นๆ booking client-side.** `resolveSubjectId` **throws** when it
   cannot resolve a program name — and an อื่นๆ has no program. The request would never have been made, and the
   error would have read like a validation rule rather than a code path.
2. **The offline mock built อื่นๆ bookings with `displayName: ""` — a blank calendar cell, the exact AC-10
   failure.** The wire and a `Booking` name these differently (`otherTitle`→`title`,
   `additionalTeacherIds`→`teachers`), so `...input` silently dropped both; `title` is optional and the extra
   keys ride along harmlessly, so **tsc said nothing**. Same family as the allow-list literal that dropped
   `discount` (TASK-170) — a name mismatch across a boundary, invisible to types on both sides.

### AC-12, done structurally rather than as a message (Q1)
The price source is **one `SegmentedControl` value**, so "both set" is **unrepresentable** rather than refused.
The task offered "prevent the combination **or** state which applies" — a state that cannot exist beats a
message explaining a state that can. Cost: switching source hides the other field. One line to reverse.

📌 **New `other-booking.ts` + 18 tests** — the rules as a pure function, same shape and same reason as
`discount.ts`. ฿500 → `50000` is asserted as a **number**, and explicitly not `500` or `5000000` (the TASK-169
magnitude). 🔴 **And one block matters beyond this task:** these problem keys render through a **template**
(`t(k)`), and `keys.test.ts` scans only literal `t("…")` — it names templated keys as a known gap. **So nothing
else in this repo would have noticed a key resolving to nothing.** The test now asserts every emitted key exists
in both dictionaries *and* that the evaluator emits none outside that list.

### Rendered check: **LOCAL, like TASK-147 — not the `sid` queue**
Everything here is reachable offline (mock catalogue, อื่นๆ fixtures, mock auth takes any credentials). Command
and the ordered walkthrough are in the TASK. ⚠️ **One honest caveat:** the mock returns two catalogue items, so
the **empty state is not reachable as shipped** — it needs `delay([])` for one run. Stated so nobody concludes
it was skipped.
📏 And on the 375 case: the DoD names *"the charge row (toggle + amount + item picker)"*, but **in this
implementation that is a vertical stack, not a row** — so the anticipated case does not exist as described. The
real risk is the two-segment control carrying *"ระบุจำนวนเงินเอง"* / *"เลือกจากแคตตาล็อก"* at ~50% each, and the
teacher `MultiSelect` once pills wrap. **Static analysis, not a measurement** — I could not take one.

**Q2 (not blocking):** `detectConflict` runs against `teacherIds[0]` only, mirroring the BE exactly — so an
อื่นๆ can be saved onto a second teacher who already has a lesson in that slot, silently. That is the same
residue you routed to @Porter, arriving through the create form, and the form is where the warning would
actually help because it is where someone picks the teachers. I did not build one: it is not in this task, and a
client-side check the server does not enforce is its own kind of lie. Worth deciding both halves together.

**Ball: @Sober** — TASK-226 review (Q1/Q2). **REQ-078's FE is now complete** (226 + 227), BE was already
(224 · 225 · 228 · 229). Nothing else is open for FE: TASK-111 waits on TASK-110, TASK-118 on TASK-116.

## [--:--] Sober (SA) — ✅ TASK-226 PASS → 🏁 **REQ-078 is SPEC_DONE. All six tasks closed.** → @Porter @Fern @Jason

**Reproduced:** `tsc --noEmit` **0** · `bun test` **76 pass / 0 fail** (+18) · `BOOKING_TABS` at
`BookingModal.tsx:688` carries `OTHER` · the `resolveSubjectId` bypass at `scheduler.service.ts:523` with the
reasoning on the line.

### The third array was the one that would have shipped the feature unusable

I asked @Fern to look for a third hand-written array. She found **`BOOKING_TABS` — the create-form’s own tab
list.** Every other piece of REQ-078 could have shipped and worked — type, migration, money, cell, LINE, filter
— and **staff would have had no way to create an อื่นๆ booking at all.** A feature complete everywhere except
its own entry point, with a green build. All three arrays now carry the trap on the line: *a
`Record<BookingType,…>` fails the build when the union widens; an array of the same type does not.*

Two more the compiler was happy with: `resolveSubjectId` **throws** for a booking with no program (the request
would never have been sent, and the error would have read as a validation rule, so nobody would look at a
client-side helper); and the mock built `displayName: ""` — **AC-10’s exact failure** — because `...input`
silently dropped `otherTitle`→`title` and `additionalTeacherIds`→`teachers`. **A name mismatch across a boundary
is invisible to types on both sides of it** — the TASK-170 family again. 📌 Worth noting the cheap environment
caught the defect the expensive one would have shown the owner.

**Q1 — AC-12 as one `SegmentedControl` value: better than what I specified.** I offered *"prevent the
combination **or** state which applies"*; **a state that cannot exist beats a message explaining a state that
can.** The BE refusal and the DB CHECK go back to being backstops rather than the primary defence.

**Q2 — `detectConflict` on `teacherIds[0]` only.** Right not to build a client-only warning (*"a check the
server does not enforce is its own kind of lie"*), and right that the **form** is where a warning would help.
⇒ **Merged with the clash question already with @Porter** so he decides **both halves at once**. Three separate
reports of the same residue — Jason’s index note, the day-cell drop in TASK-227, this — is one decision, not
three notes.

### 🏁 REQ-078 → SPEC_DONE

**All six tasks DONE:** 224 (core + migration) · 225 (money) · 226 (form) · 227 (cell + naming) · 228 (LINE) ·
229 (catalogue filter). `0029` is applied and witnessed on `sid`.

🔴 **`SPEC_DONE` means built and SA-reviewed — it does NOT mean it works** (PROTOCOL). Outstanding before
anyone calls this delivered:
- **@Tanya, LOCAL (mock covers them, no `sid`):** TASK-226’s form + TASK-147’s help lines. ⚠️ TASK-226’s
  **empty state is not reachable as shipped** — the mock returns two items; it needs `delay([])` for one run.
- **@Tanya, `sid` queue:** TASK-222’s three band states (for `posted`, a **discounted** trial) · TASK-227’s
  1600/1280/768/375, fixture b13.
- **`uat`:** `0028` + `0029` still to run there, with the batch, never in the same sitting as `sid`.
- **@Porter’s open items** are unchanged and all recorded on the board and in the REQs — the อื่นๆ
  auto-attend-charges-overnight exposure (a typed amount has no ceiling) · the reversal-blind band + SPEC-069
  Q2 Thai · the SKIPPED parent outbox row · the `bo.item` DATA REQUEST · the ledger SELECTs · the `bun test` →
  live `sid` DB task · and REQ-079’s shared-channel precondition.

**REQ-078 leaves my desk. @Porter has acceptance.** Per his GO, `REQ-079` is next and it is now mine — and the
first thing in it is **§6: how an admin’s "open the door" reaches us from OA Manager.** His own caution stands:
an admin’s reply is **outbound**, and our webhook sees **inbound**. **AC-1 depends on that answer, so I settle
it before I cut anything.**

**BALL: me (Sober) — REQ-079, starting with §6.**

## [--:--] Sober (SA) — ✅ **§6 settled** — and the code says the question was smaller and the stakes bigger. → @Porter

Written into `REQ-079` §6b. Two findings, both read from the source.

### The question collapses: (a) and (b) are the same mechanism

Every `lineUserId` this system has ever stored traces back to **`eventUserId(ev)`** — the inbound webhook
(`line-webhook.service.ts:466 · :557 · :620` are the only producers; everything else receives it as a
parameter). **There is no path by which we learn a chat’s identity without an inbound message.**

⇒ **Your option (b) — "a control on our own back-office screen, certain, at the cost of switching windows" — is
not implementable as written.** Our screen can authorise a **family**; it can never address a **chat** we have
never heard from. Whatever the admin does, **the door is something the PARENT presents.** That is not a
preference between candidates, it is the only shape the platform permits — and it takes the webhook unknown
off the critical path, because the event we act on is the parent’s reply, which is inbound by definition.

### 🔴 And the finding that matters more than the mechanism

**Flow 1 reverses a shipped safety decision, and one pure function enforces it today.**
`parentChildrenNote` (`line-pairing.ts:19`, REQ-020 / **TASK-047**) answers a phone number with **a count,
never names** — with the reason written in the file: *"Parent linking matches on phone alone, so anyone who
types a phone number would otherwise be told that family’s children’s names."*

**§5 Flow 1 replaces that count with `น้องรดา, น้องต้น`.** So this is not a new risk being weighed — it is an
existing, deliberate, shipped control being switched off, and it is **one function** away.

📌 **That is what the door is FOR, and it is the best argument for having one:** with it, names appear only in a
chat an admin vouched for, and `parentChildrenNote` keeps protecting everyone else **unchanged**. Without it,
Flow 1 is TASK-047’s defect reintroduced in nicer wording. ⚠️ Whoever builds this must not "simplify" that
function away — the tempting edit is one line, in a pure helper with an innocent name. It will be in the task.

### The mechanism

A **one-time, single-use, expiring invite per family**, issued on our People screen (REQ-019), **pasted by the
admin into the chat** — they are already typing there, so it works **from OA Manager on a PC by construction,
with no integration and no platform assumption** — and **typed by the parent**, which is the inbound event that
binds the chat. Only then may names appear. Reuses `line_link_sessions` and the existing precedence rule; the
new state is the invite + the attempt counter §6 already named. **Columns, not capabilities.**

**Honest cost:** the admin fetches an invite from our screen **once per family** — Flow 1 is *first time only*,
and Flow 2 needs no admin at all. Less window-switching than a per-chat control, and unlike a per-chat control,
it can actually be built.

### Two things back to you — neither blocks the spec

1. 🟠 **Does an admin’s OA Manager reply reach our webhook?** Now only a **convenience** (keyword instead of
   paste), not a dependency. **A 30-second test settles it and §8 already permits it:** reply to one `sid` chat
   from OA Manager, then look for a `[line-in]` line in the server log (`line-log.ts:19`). If it appears we can
   add the keyword variant later **without changing the model** — which is the point of settling it this way.
2. 🔴 **The owner’s call, and it needs his words:** names appear **only after an invite is used**. If he wants
   names on a phone number with no door, **he is knowingly re-opening TASK-047** — and it should be put to him
   in exactly those terms, because the customer’s request (§3) does not mention the control it removes.

**§6 is settled for spec purposes. I am cutting SPEC-071 against it next.**

**BALL: me (Sober) — SPEC-071 for REQ-079.**

## [--:--] Sober (SA) — 📐 **SPEC-071 cut for REQ-079.** Six tasks defined, deliberately NOT released — two questions. → @Porter

REQ-079 → **IN_SPEC**. The spec is built on the two §6b findings and on what the code already does.

📌 **The good news first: this is a state machine over pieces we already run.** Two menu sets = `linkRichMenuToUser`
(REQ-042’s path). Typed `1`/`2` = already satisfied — taps and keywords share one handler. Multi-turn = the
`line_link_sessions` + `decideMessageRoute` precedence rule. Leave / check-in / course = REQ-046 · REQ-050 ·
REQ-016, **reused, not rebuilt**. ⇒ **no new LINE capability is required anywhere in the seven flows.** What is
new is state: a hashed family code, a guardians table, an invite, and three columns on the session.

**Two design calls worth your eye:**
- **`family_line_links_user_uq` is load-bearing** — one LINE account belongs to **one** family. Without that
  unique index, a second family’s invite silently re-points an account and a parent sees another family’s
  children: **TASK-047’s failure by a different route.** It extends the *"one LINE user ⇒ one active roster
  link"* rule `moveRosterLink` already enforces for teacher/parent.
- **AC-20 ("no flow touches money") is enforced by a grep-guard test**, TASK-223’s shape: the flow modules must
  not import the sale/discount/movement paths **at all**. A rule the build enforces beats one in prose.

### 🔴 Q1 — AC-17’s trigger is the one thing I cannot ground, and it is NOT the §6 question again

§6b took the OA-Manager unknown off the critical path **for the door**. **AC-17 puts it back for itself:** *"when
an admin replies, the bot stops"* requires us to **observe an outbound message.**

**So I split it rather than blocking on it.** The mute *mechanism* (`muted_until`) gets built, and every trigger
we can actually see — the parent pressing `คุยกับแอดมิน`, and the two-strikes handover (AC-18) — is certain and
inbound. **Only the admin-reply trigger is gated**, and your own 30-second test decides it: reply to one `sid`
chat from OA Manager, look for a `[line-in]` line (`line-log.ts:19`). **Please run it before TASK-231 is
released.** It decides one branch, not the design.

### 🔴 Q2 — a parent on a PC cannot start Flow 2, and it is two of the owner’s own decisions colliding

*"Trigger: a button, not a typed keyword"* + **AC-16** (*silent even if the parent types the exact menu words*)
+ *"LINE PC: no rich menu and buttons cannot be tapped — text only"*.
⇒ **`[เข้าใช้ระบบ]` is unreachable on a PC.** Flow 1 survives (the bot speaks first, and typing inside a flow is
allowed by Rule 3). **Flow 2 does not — and Flow 2 is the returning second guardian, which is exactly the case
most likely to be a father at a desk.** He said a minority of parents are on PC; this does not make it awkward
for them, it makes it **impossible**.

**I am not choosing between his two decisions.** Two cheap ways out: **(a)** the admin issues that guardian an
invite too (Flow 1 already covers it); **(b)** a narrow exception — a typed `เข้าใช้ระบบ` starts the flow **only**
in the ยังไม่รู้จัก state and **only** when no mute is active, which is the one place it cannot talk over an
admin. **My lean is (b) with (a) as the standing fallback** — but the ACs change either way, so it is worth the
one question.

**Q3, smaller:** I store the code **hashed** — compatible with every decision (the shop **resets**, the parent
**tells** the household) and it removes a plaintext shared secret from a table staff can read. ⚠️ The cost: **an
admin cannot read a family’s code back to them**; the remedy is reset-and-tell. If he expects an admin to *read*
it, say so — **two lines now, a migration later.**

**Tasks 230–235 are cut and HELD.** Not released to @Jason or @Fern: Q2 changes Flow 2’s entry (TASK-232) and Q1
changes one branch of TASK-231. Releasing them now would have two engineers building against an answer that is
one message away. Everything else in the spec is stable and they can be released the moment you answer.

**BALL: @Porter — Q1 (the 30-second `sid` test) and Q2 (the owner’s PC decision).**

## [--:--] Porter (PM) — 🔴 The 6-digit family code is CUT by the customer. `REQ-079` §15 supersedes. → @Sober
> Owner: *"ลูกค้า confirm มาว่าไม่เอา 2FA 6 หลักที่เราคิด งั้นก็ยกเลิกออกจากแผน"* + *"ปล่อยเลย"*.

**Written as §15 with a pointer in the Status line — where §4–§14 and §15 disagree, §15 wins.** I did not edit
the earlier sections away: they are how the decision was reached, and this project does not delete its record.

**What it removes:** the code · setting it · the lockout (4 attempts / 3 minutes / per family) · the weak-code
check and its `app_settings` switch · the first-use takeover risk and the gate that closed it · **AC-4, AC-6,
AC-7 and AC-8**, withdrawn.
📌 **Your review named the attempt counter as the ONLY new state this REQ needed.** It is now unnecessary —
nothing left in the seven flows needs storage we do not already have. **The build got smaller, not later.**

**What replaces it — one way in, and it was already Flow 1:** *a LINE account joins a family when an admin opens
the door for that chat.* Mother, father, a new phone — **same single action by an admin, every time.** Flow 2
becomes "ask the shop"; **there is no self-service path.** Multi-guardian survives, attached one at a time.

🔴 **One thing genuinely got worse and I put it on the record rather than in my head: the sick-mother case now
depends on an admin being reachable.** That case is *why* the code existed — dad, unlinked, needing to file
leave tonight. Now he must reach a person, and on a Sunday evening that means the phone, exactly as today.
**Not an objection** — the shop answering is a fallback that has always worked — but it is the kind of trade
that is invisible when decided and obvious the first evening it bites.
⚠️ **If it ever becomes a complaint, the answer is not to reopen this. It is to make "admin opens the door" one
tap from where staff already sit** ⇒ **§6 is now the single most important open item in the file.**

📌 **A free win I offered, and am now withdrawing myself:** §9 said this REQ's per-family code answered
**SPEC-050 decision #1** (REQ-051's admin code, today the shared static `"229"`). **Void.** REQ-051 keeps all
three security decisions and got no cheaper. Withdrawing it here rather than leaving it for whoever picks up
REQ-051 to discover.

**Also rewritten: the customer explainer.** Three of its seven points described the code. §15 carries the
replacement, and the owner has it. **Point 6 is a stronger promise now, not a weaker one:** *"you see it only if
an admin opened it for you"* is simpler to make and to keep than *"a code nobody must guess"* — and the shop
controls it directly.

**Status unchanged: `READY_FOR_SA`, still #2.** Spec from §5 **as amended by §15**.

## [--:--] Porter (PM) — SPEC-071: **Q2 and Q3 are moot** — my §15 cut landed after you wrote it. → @Sober
Timing, not disagreement: you cut SPEC-071 and I posted §15 (the customer killing the 6-digit code) minutes
apart. **Two of your three questions dissolve; one stands.**

- **Q3 (hash the code) — moot.** There is no code. Nothing to hash, nothing for an admin to read back.
- **Q2 (a PC parent cannot reach `[เข้าใช้ระบบ]`) — moot, and your analysis is why it is safe to lose.** You found
  that Flow 2 is *impossible* on PC, not merely awkward, and that it is exactly the returning-second-guardian
  case. **§15 removes Flow 2 entirely:** everyone — mother, father, new phone — joins the same way, by an admin
  opening the door. That is your option **(a)**, now the only option. **No narrow typed-keyword exception is
  needed, so AC-16 stays absolute.** 📌 Your finding stands on the record as the reason (b) was never taken.
- **`family_line_links_user_uq` — keep it.** The code is gone; **one LINE account belongs to one family** is not.
  Your TASK-047-by-another-route argument is unchanged and it is the right index.
- **The grep-guard on AC-20 — keep it too.** A rule the build enforces beats one in prose, and money is the one
  place this REQ must never drift.

⚠️ **Re-cut what §15 touches before release:** the code table, the invite/lockout state, and Flow 2's entry
(TASK-232). **The state you said was new — hashed code, three session columns — is mostly gone**; what remains
is the guardians table and the invite.

**Q1 — routed to the owner, not to me.** Replying in OA Manager and reading `[line-in]` on `sid` is his account
and his box; **Porter does not touch environments.** Sent with your framing intact: it decides one branch of
TASK-231, not the design, and the mute mechanism plus every inbound trigger get built regardless.

## [--:--] Porter (PM) — 🏁 REQ-078 (owner's REQ-005) is SPEC_DONE. **Not "done" — built.** → @Tanya @Sober
All six tasks closed. 📌 **I am not telling the owner it works**, and I am not asking him to accept it: on this
project `SPEC_DONE` means *built and SA-reviewed*, and the only thing that turns it into "working" is a run on
the deployed build.

**@Tanya — REQ-078 is yours the moment it is on `sid`.** The `REQ-078` file has 25 ACs; the ones no engineer
could reach are yours specifically: **the rendered LINE for an อื่นๆ booking** (TASK-228) · **the 1600/1280/768/
375 measurement** (TASK-227) · **the three posted-revenue band states** (TASK-222, and for `posted` use a
**discounted trial** — the one input where a wrong number still looks plausible).
✅ **Sober's standing warning is lifted:** TASK-227 has shipped, so creating an อื่นๆ booking on `sid` no longer
risks the calendar. **Do not start until the deploy is confirmed** — I will say so here.

**Asked the owner for the `sid` deploy.** ⚠️ `0029` is already applied and witnessed there, so this is code only —
**but `db:verify` still runs before any restart.** "No migration in this batch" is a claim that expires.

## [--:--] Porter (PM) — 🧪 @Tanya — **REQ-078 (อื่นๆ) is deployed on `sid`. It is yours.** → @Tanya
The owner deployed and confirmed it. `0029` was applied and witnessed on `sid` earlier today, so this was code
only. **The contract is `requirements/REQ-078-booking-type-other.md` — 25 ACs. Test that list, not this entry.**

✅ **Sober's standing warning is LIFTED.** TASK-227 shipped, so creating an อื่นๆ booking on `sid` no longer
risks the calendar. You are clear to create them.

### 🔴 Money first — this type can charge, and unlike a trial it has NO ceiling

An อื่นๆ booking can carry a **typed amount**, and like everything else **an unmarked session auto-attends at
23:30 and posts**. ⇒ **a fixture you create today and forget charges real money on `sid` tonight**, at whatever
number was typed, and **there is still no reversal you can run.**

**⇒ Default to `คิดเงิน` OFF for every fixture unless the AC under test needs it.** For AC-5 (typed amount) and
AC-6 (catalogue item), which do need it: **use a small amount, cancel the booking the same day**, and declare
the footprint. If a movement does land, name it in the TEST file with its key — do not absorb it quietly.

### The five nobody else could reach

1. **AC-16 / AC-18 — the rendered LINE for an อื่นๆ booking.** Jason asserted the string in tests; **nobody has
   read it on a phone.** The **admin's typed title** must name it — never the word "อื่นๆ", never an empty label.
   With several teachers, **every one of them** gets it.
2. **AC-18 on the calendar — one booking, three columns.** Assign 3 teachers: it must appear in all three and
   still be **one** booking. **Cancel it once and it must vanish from all three.**
3. **AC-15 / the 375 measurement** (1600 · 1280 · 768 · 375), fixture **b13** — the standing FE rule.
4. **AC-24 / AC-25 — the clash warning.** Book a teacher who already has a lesson at that time: the warning must
   **name the teacher and the clashing booking**, and **saving must still work**. Then book one with no clash:
   🔴 **no warning may appear.** A warning that fires every time is trained away in a week.
5. **TASK-222's three posted-revenue band states.** 🔴 For `posted`, use a **DISCOUNTED trial** — it is the one
   input where a wrong number still looks entirely plausible (฿1,590 vs ฿1,190).

### Three where the negative matters more than the happy path

- **AC-10 / AC-11 / AC-12** — no title with no student · a bad amount · **and both price sources set at once**.
  Each must be **refused with a message that says what is missing**. A silent save here is the defect.
- **AC-21 — the freelance figure must be BYTE-IDENTICAL** before and after an อื่นๆ is attended. Use a
  **freelance** teacher; on a full-timer the AC proves nothing.
- **AC-14 / AC-20 — the other four types unchanged.** `0029` dropped two NOT NULLs, so the **database** stopped
  refusing a lesson with no student; the contract was put back in code. **Try to create a 1HR with no student
  and confirm it is still refused.** That is the regression this migration could have caused.

**Footprint as always:** declare it, retire what you can (cancel + `ADMIN_ERROR`), and name what you cannot.
**`uat` is not yours.** Verdict is yours alone — I will not call any of this working before you do.

## [--:--] Porter (PM) — 🔴 **Q1 ANSWERED, and the answer is NO.** An admin's reply is invisible to us. → @Sober
The owner ran your 30-second test on `sid` and sent the screenshot. **He typed `สวัสดีค่ะ` in OA Manager. No
`[line-in]` line appeared — only the customer's own inbound messages did.**

⇒ **Your caution was right and it is now a verified fact, not a worry: an admin's reply is OUTBOUND and our
webhook never sees it.** ⇒ **AC-17's automatic trigger cannot be built.** Not "hard" — not possible on this path.

### What I am ruling, so you are not blocked on it

**Build the mute mechanism (`muted_until`) exactly as you split it. Drop the admin-reply trigger.**
The triggers that remain are both **inbound and certain**:
1. the parent pressing **`คุยกับแอดมิน`**, and
2. the **two-strikes handover** (AC-18).

**AC-17 is REWRITTEN, not withdrawn:**
> **AC-17 (revised)** — **Given** a chat where the bot has been muted (by either trigger), **When** the parent
> types anything at all, **Then** the bot stays silent, **and the bot still works normally in every other
> family's chat.**

📌 **Why I am comfortable shipping without the automatic version, and it is not resignation:** *the bot is
silent by default*. It only ever speaks **inside a flow the parent themselves started**. So the collision this
rule was written against — a bot talking over an admin — can only occur in the seconds while a parent is
mid-flow, and in that window the parent has a **`คุยกับแอดมิน` button on every step** and the bot **gives up
after two unexpected replies anyway**. **The design's own silence does most of the work the trigger was for.**
⚠️ If it turns out to matter in practice, the fallback is a **"หยุดบอทในแชทนี้" control in our own back office**
— certain to work, one click, and it needs no platform capability. **Do not build it now**; note it and see.

### 🔴 The screenshot also showed something I did not ask for, and it is worth more than the answer

Look at what the bot replied to two stray messages in an **idle** chat:
- `เมนู` → **`เบอร์โทรไม่ถูกต้อง กรุณาพิมพ์เบอร์ที่ลงทะเบียน`**
- `yo` → **`ไม่พบครูชื่อเล่น "yo" — ตรวจสอบอีกครั้ง`**

**That is today's bot answering free text with errors, in a chat where a human was about to reply.** It is
exactly the behaviour `REQ-079` Rule 1 and **AC-16** forbid — *silent by default, and silent even when the
parent types the exact menu words*.
📌 **So AC-16 is not a nicety we are adding; it is a defect we are removing**, and the owner has now seen it
happen in his own account. Worth saying in the spec: **"no reply to unrecognised text in an idle chat" is a
behaviour CHANGE from what is deployed**, not a new feature — and it needs a regression test, because the
current handlers clearly do reply.

**Ball: @Sober** — release TASK-231 with the revised AC-17, and fold AC-16 in as a change to existing behaviour.
Nothing else in SPEC-071 is waiting on me. (Q2 and Q3 were already dissolved by §15.)

## [--:--] Tanya (QA) — 🔴 **REQ-078 (อื่นๆ) — TEST_FAILED on `sid`. Two blockers, one of them a requirement conflict.** → @Porter

Picked up your order. Contract read as instructed: the 25 ACs in `requirements/REQ-078-booking-type-other.md`,
not the log entry. Full evidence: **`tests/TEST-064-req078-booking-type-other.md`**.

### 🔴 BLOCKER 1 — DEF-1: the form destroys the page on an ordinary correction
Remove the last teacher chip, then type **one character** in Title → the whole page becomes
**"This page couldn't load"** and everything entered is lost. **3 of 3 from a clean state.** The validation
itself is right (`Select at least one teacher.`, Save disabled) — but an **uncaught
`TypeError: Cannot read properties of null (reading 'value')`** (`2nykiepra7i1k.js:1:95704`) is thrown the moment
the chip goes, and the next keystroke kills it. **This is AC-19**, the case the owner promoted from an edge case
to *the rule* on 08-31 — and picking the wrong teacher then fixing it is exactly what an admin does all day.

### 🔴 BLOCKER 2 — DEF-2: AC-24 shipped INVERTED. This one is yours, not an engineer's.
The owner ruled **"เตือนพอ ไม่ห้าม"**. What is on `sid`:
- **Clash with a CONFIRMED booking → HARD REFUSAL.** *"This slot is already booked · Ally · Bike / Scooter /
  Balance Cruiser (11:00) — you can only overbook when the previous student is on leave."* Buttons: **Back /
  Close only**. There is no way to continue. The booking cannot be created at all.
- **Clash with an ON LEAVE booking → saves with NO warning whatsoever.**
⇒ **AC-24 is unimplemented in both directions**, and AC-25 "passes" only because no warning exists to misfire.
What shipped is the pre-existing slot-uniqueness guard, untouched. 📌 **I am not calling this a coding slip:**
honouring the ruling means relaxing that guard for อื่นๆ, which is a bigger change than a dialog. **Worth putting
back to the owner before it is cut as a task** — Q1 in TEST-064.

### Two more that should not ship either
- **DEF-3 (MAJOR)** — All bookings → Type=**Other** reports **"2 found"** and renders **zero rows**. Verified in
  the DOM. My two studentless อื่นๆ are the two. ⇒ **the central new object of this REQ cannot be reviewed or
  found anywhere**, and it makes AC-13's *"stored and **queryable**"* untrue in the product even though the reason
  is captured correctly.
- **DEF-4 (MAJOR)** — an อื่นๆ over an existing session **hides that session on the calendar**; cancelling mine
  brought it back. 🟢 **Data is intact** — Ally's row stayed `ON LEAVE` throughout, I checked rather than assumed.
  Display fault, not data loss; please don't let it get escalated as the latter.
- **DEF-5 (MAJOR, intermittent)** — the same uncaught error killed the page twice more during ordinary editing.
  🔴 **I could not reduce those two to a reliable repro and I am not pretending otherwise** — reported as unproven,
  with the note that it is the *same* null read as DEF-1, so that fix should be checked against these paths.

### ✅ What genuinely works — worth saying, because it is the new capability
**Multi-teacher is well built.** Three teachers, one booking: it appears in all three columns, each naming the
others (`With Dewy, Ek`), the detail shows one `OTHER` badge, and **one cancel clears all three** (AC-18 ✅).
Also ✅: the type itself · the studentless typed title naming the cell (Thai renders fine) · AC-10 · AC-11 (0,
negative and letters all refused) · **AC-12 by construction** — the two price sources are one segmented control,
so the combination is impossible · AC-13's REQ-074 reason path · AC-15 incl. the measurement you asked for —
**1600 → 1284×73 · 1280 → 964×73 · 768 → 708×99 · 375 → 341×148, no page h-overflow at any width** · AC-20/AC-14:
the other four types still take exactly one teacher, and **a 1 HR with no student is still refused** (the `0029`
regression you flagged — client-side; I did not POST the API directly).
**AC-22/AC-23 confirmed vacuous on the running build:** there is **no teacher-pay control in either state**, so
Sober's "needs no build" holds — nothing is present-and-ignored.

### 🔴 Nine ACs are `NOT_TESTED` — please don't read this as "four defects from done"
**AC-4 · 9** (day-end) · **AC-5 · 6** (the posted amount) · **AC-7 · 8** (entitlement deduction) · **AC-21**
(freelance untouched) all need the **23:30 job**, i.e. a real `bo.movement` I cannot reverse. **AC-16** (the
rendered LINE) is **blocked by your own standing instruction** — `sid` may share `uat`'s channel and 2 real
teachers are linked, so `Confirm + LINE` was never pressed. **AC-3** (อื่นๆ with a student) I simply did not reach.
**Every money criterion in this REQ is still unverified.**

### Footprint
Two อื่นๆ bookings created, **both CANCELLED** (`ADMIN_ERROR` + QA notes), both confirmed gone from the calendar.
Charge **OFF** on every fixture, nothing confirmed or attended ⇒ **no `bo.movement`**. **No LINE sent. No row
touched that I did not create. No `uat` contact.** ⚠️ Cleanup could not be re-verified in the bookings list
because of DEF-3 — verified on the calendar instead, and declared. Ledger in `tests/DEV-SERVER-FOOTPRINT.md`.

### Four questions — TEST-064
1. **DEF-2 is a requirement conflict — which rule stands?** (owner's call, and it changes the size of the fix)
2. **AC-5/AC-6 need a money decision, not more testing.** My recommendation: let one **฿20** อื่นๆ post overnight
   on `sid` and have the owner reverse it in the backoffice. An unverified money path is worse than ฿20.
3. **AC-16 unblocks the moment the LINE channel question is answered** — then it is a 10-minute check, and it is
   the one thing in REQ-078 nobody has ever seen work.
4. **Is a studentless booking meant to be findable at all?** Staff will search *"the team meeting on Tuesday"*,
   not a child's name. If search-by-title is expected that is a requirement gap, better raised now.

### Also closed from earlier today
- **REQ-058 → `TEST_PASSED`** (`tests/TEST-063`, board updated). Your Q2 answer closed AC-6. AC-9/AC-10 remain
  `NOT_TESTED` (owner-run CLI).
- **TASK-220 — stood down** per your 08-31 correction. The customer exercises that path daily; a re-proof is not
  worth a `bo.movement`. Recorded in TEST-063.
- 📌 **My own miss, recorded:** on resuming I thought I had misdated the REQ-058 round and started "correcting"
  `TEST-063` to 2026-09-01 — including drafting a question asking whether `sid`'s **server clock** was two days
  adrift. **Wrong.** This session simply crossed days: the round really did run on 08-30 (`PROTOCOL.md` covers
  this — *"a session that crosses midnight switches files at midnight"*). Reverted in full. I acted on a
  **remembered** screen instead of reloading the real one, and it would have sent the owner to check a healthy
  server. Written into `TEST-063` §Session note so the lesson outlives the mistake.

**Ball: @Porter** — the DEF-2 ruling and the three other questions. **Nothing is waiting on me**: REQ-078 is back
with you, and the LINE-touching work (REQ-020, REQ-023, AC-16) is still blocked on the owner's channel answer.

## [--:--] Porter (PM) — 🔴 REQ-078 `TEST_FAILED`. Routing the defects. **DEF-2 + DEF-4 are ONE problem.** → @Sober
Tanya's verdict and evidence: `tests/TEST-064`. **The failure is not negotiable and I am not softening it.**
Two blockers, two majors, one unproven intermittent. Routing what is yours; the owner has what is his.

### The thing I want you to see before you cut anything: DEF-2 and DEF-4 are the same defect

- **DEF-2** — an อื่นๆ clashing with a CONFIRMED booking is **hard-refused**; clashing with an ON-LEAVE one
  **saves silently**. The owner ruled **"เตือนพอ ไม่ห้าม"**. **AC-24 is unimplemented in both directions**, and
  AC-25 only "passes" because there is no warning to misfire. What shipped is the untouched slot-uniqueness guard.
- **DEF-4** — an อื่นๆ laid over an existing session **hides that session on the calendar** (display only;
  Tanya checked the data and Ally's row stayed `ON LEAVE`).

🔴 **Put them together and the ruling costs more than a dialog: if we relax the guard so an อื่นๆ MAY overlap,
the calendar must be able to SHOW two things in one slot — or we will have built invisible double-bookings,
which is worse than the refusal we started with.** ⇒ **DEF-4 is not a cosmetic follow-up; it is a precondition of
honouring the ruling.** Tanya was right that this is bigger than a coding slip, and that is why I have put the
scope back to the owner rather than having you cut it blind.

### Yours now — not waiting on the owner

- 🔴 **DEF-1 (blocker)** — remove the last teacher chip, type one character in Title ⇒ **the page dies**, all
  input lost, **3 of 3**. Uncaught `TypeError: Cannot read properties of null (reading 'value')`.
  **This is AC-19's path**, and correcting a wrongly-picked teacher is what an admin does all day.
  📌 **DEF-5** (two more page deaths during ordinary editing) is the **same null read** — Tanya could not reduce
  it to a repro **and said so instead of inventing one.** Check the DEF-1 fix against those paths before closing.
- 🔴 **DEF-3 (major)** — All bookings → Type=**Other** shows **"2 found" and zero rows** (verified in the DOM).
  ⇒ **the central new object of this REQ cannot be found anywhere in the product**, and it makes AC-13's
  *"stored and **queryable**"* untrue in practice even though the reason is stored correctly.
  ⚠️ It also cost Tanya her cleanup re-check — she verified on the calendar instead and declared it.

### Not a defect, and worth recording as the new capability

**Multi-teacher is well built.** Three teachers, one booking, in all three columns, each naming the others, one
cancel clears all three — **AC-18 ✅**. Also ✅ AC-10 · AC-11 · **AC-12 by construction** (one segmented control,
so both price sources cannot be set at once — better than the AC asked for) · AC-13 · AC-15 with the
measurements · **AC-14/AC-20: a 1 HR with no student is still refused** — the `0029` regression I was worried
about did **not** happen. **AC-22/AC-23 are vacuous on the running build** — no teacher-pay control exists in
either state, so your "needs no build" call holds.

### 🔴 And the part nobody should skim: NINE ACs are `NOT_TESTED`

**Every money criterion in this REQ is unverified** — AC-4/9 (day-end), AC-5/6 (posted amount), AC-7/8
(entitlement), AC-21 (freelance). They need the 23:30 job and therefore a real `bo.movement`. **AC-16** (the
rendered LINE) is blocked by my own standing instruction about the shared channel. **AC-3** was not reached.
📌 **"Four defects from done" is the wrong reading and I will not report it that way.** Three of the four things
this REQ actually changes — money, LINE, entitlements — have never been exercised.

**Ball: @Sober — DEF-1 (+DEF-5) and DEF-3 now. Hold DEF-2/DEF-4 until the owner rules on scope.**

## [--:--] Porter (PM) — ✅ Owner: ฿20 money test approved + he will link himself as the LINE test teacher. → @Tanya @Sober
Two of TEST-064's four questions are closed by the owner; the third (DEF-2 scope) has my recommendation in front
of him and awaits his word.

**1. The ฿20 test is APPROVED** — your recommendation, taken exactly: one อื่นๆ booking, **typed amount ฿20**,
charge ON, left to post at the 23:30 day-end on `sid`; **the owner reverses it in the backoffice afterwards.**
That is the sanctioned residue — a `bo.movement` of 2000 satang, planned, named, and reversed by the one person
who can. **@Tanya — set it up when ready** (fixture per your own footprint rules; declare the key). This unblocks
**AC-5**, and with the same overnight run **AC-4 · AC-7 · AC-8 · AC-9 · AC-21** become observable. AC-6
(catalogue item) may need a second night — your call whether to run both in one.

**2. The owner will link HIMSELF as a test teacher on the LINE channel.** That is the isolatable LINE test
recipient the board has carried as an open item since 08-04. When his link is confirmed:
- **@Tanya — AC-16 becomes a 10-minute check**: an อื่นๆ booking with the owner as (one of the) teachers,
  `Confirm + LINE`, and read the message on his phone — the **typed title** must name it, never "อื่นๆ", and with
  several teachers every assigned one gets it. **Wait for my line here confirming his link is live before
  pressing Confirm+LINE on anything.**
- ⚠️ The standing rule stays for everyone else: **the 2 real teachers must never receive a rehearsal message.**

**3. DEF-2 (the inverted clash rule): my recommendation to him is (ข)** — keep the refusal for now with an
honest message (*"ช่วงเวลานี้มีคาบสอนอยู่ กรุณาเลือกเวลาอื่น"*), defer overlap+warning+two-items-per-slot to a
follow-up REQ, ship the rest of REQ-078. Rationale: the invisible-overlap failure (DEF-4) is worse than the
refusal, real overlap cases are rare, and this REQ's unproven money path matters more than widening scope
mid-defect. **Waiting on his word — DEF-2/DEF-4 stay HELD, @Sober.**

**@Sober — DEF-1 (+DEF-5's shared null read) and DEF-3 are already yours and are not waiting on any of this.**

## [--:--] Porter (PM) — ✅ Owner ruled on DEF-2: option ข, "ตามนั้น". DEF-2/DEF-4 released to you. → @Sober
**Overlap is DEFERRED; the refusal stays with an honest message.** Written into `REQ-078` as **AC-24 (revised)**:
the refusal must name the teacher and the clashing booking — `ครู{ชื่อ} มีคาบสอนช่วงเวลานี้อยู่แล้ว ({ชื่อคาบ}
{เวลา}) กรุณาเลือกเวลาอื่น` — never a generic error. AC-25 unchanged: no clash ⇒ no message.

**The full overlap capability (guard + warning + two items in one slot) is a FOLLOW-UP REQ**, one piece,
with DEF-4's calendar fix as its precondition. Original AC wording preserved in the REQ for that day.

**⇒ Everything on REQ-078 is now yours or Tanya's, nothing is held:**
- Yours: DEF-1 (+DEF-5's shared null read) · DEF-3 · the AC-24 message revision · ⚠️ confirm no *other* path can
  still create DEF-4's hidden-session state now that the form refuses overlap.
- Tanya's (owner-approved): the ฿20 overnight money run · AC-16 once the owner's own LINE link is confirmed —
  she waits for my line here before any Confirm+LINE.

## [--:--] Porter (PM) — 📡 NEW FACT: the LINE webhook now points at `sid` PERMANENTLY. → @Sober @Tanya
Owner, verbatim: *"ตอนนี้ไลน์ webhook ผูกที่ sid ถาวรแล้ว"*. `PROJECT-STATUS.md` §LINE rewritten — the old
"lives on `uat`, borrowed at night" arrangement is history, kept below the new section.

**What changes:** inbound LINE (linking · taps · typed replies) is now testable on `sid` **any time** — the
night-window constraint is gone. **What does not:** outbound pushes still ride the shared channel token, and the
**2 real teachers are still linked** — the "never message them in rehearsal" rule stands unchanged.
⚠️ And `uat`'s LINE inbound is deaf while this stands — fine per the owner (no real parent linked, real OA comes
later), with the same expiry trigger as `REQ-079` §8.

**Asked the owner to link himself as a TEST TEACHER on `sid`:** create a clearly-marked teacher (e.g.
"ครูQA-Test" — never a name resembling a real teacher), link via the normal REQ-020 flow. **@Tanya — when I
confirm his link here: assign that teacher on an อื่นๆ fixture and run AC-16** (typed title names the message,
never "อื่นๆ"; several teachers ⇒ every one gets it).

## [--:--] Sober (SA) — 🔴 REQ-078 defects: **DEF-3 root-caused in the code**, DEF-1 has an exact-matching lead. 3 tasks cut. → @Jason @Fern @Porter

@Tanya’s verdict stands and I am not softening it either. Three tasks, all released now — nothing is held.

### DEF-3 — found, not guessed. `getBookings` counts อื่นๆ rows and then inner-joins them away.

`scheduler.service.ts:765-783` runs **two** queries:
- rows: `.innerJoin(students, …)` `:768` · `.innerJoin(subjects, …)` `:770`
- total: `.from(bookings).where(cond)` `:779` — **no joins at all**

`0029` made `student_id` and `subject_id` nullable. **So the count sees the อื่นๆ rows and the row query drops
them** ⇒ *"2 found"*, zero rows. Tanya’s DOM evidence matches exactly.

📌 **And the comment two lines above it now lies:** *"Pure sort: nothing is filtered out, so `total` still matches
the filtered set in every direction."* True when written, false since `0029`. **TASK-236 fixes the comment with
the code** — an invariant asserted in prose that the code no longer holds is worse than no comment.

🔴 **This is the blast radius I asked Jason to enumerate in TASK-224 and it was missed** — not carelessly: the
calendar goes through the relational reader and was fine, so the whole feature looked correct. **The dangerous
shape is the hand-written `select().from(bookings).innerJoin(...)`.** TASK-236 makes the sweep for it explicit
and asks for the list, because I do not believe `getBookings` is necessarily the only one.

### DEF-1 — a lead with the property name matching, handed over as a lead

The only `.value` read on a possibly-absent object in that path is `TeacherOption.tsx:32` —
`teachers.find((x) => x.id === option.value)` — passed to Mantine as `renderOption` on the อื่นๆ teacher
`MultiSelect`. **It sits on the control the repro empties, and typing in Title is what forces the re-render.**

⚠️ **I wrote it into TASK-237 as a lead and told @Fern to follow the stack if the stack disagrees.** A plausible
cause that is not the cause is how the real one survives the fix. 📌 `TeacherOption` is **also** the bookings-table
teacher filter — if it is the cause, that is a second live site and it would explain DEF-5’s "ordinary editing"
reports, which Tanya could not reduce to a repro **and correctly declined to invent one for.**

### Three tasks, released

- **TASK-236 (@Jason, blocker)** — DEF-3 + the inner-join sweep + the false comment.
- **TASK-237 (@Fern, blocker)** — DEF-1/DEF-5, with a regression that **fails without the fix**. ⚠️ Explicitly:
  **do not "fix" it by keeping one teacher chip** — AC-19 needs the empty state reachable and *refused*, not
  unreachable.
- **TASK-238 (@Jason)** — AC-24’s revised refusal (names the teacher and the clashing booking, via `displayName`
  so an อื่นๆ clash names its title) **plus the DEF-4 sweep**: the form now refuses overlap, so enumerate the
  *other* writers — move, reschedule/overbook, import — and say whether any can still put two bookings in one
  teacher-slot. **The door is shut; the task is to check the windows.** If one is open it is a defect, not a
  follow-up REQ.

📌 **@Porter — your DEF-2/DEF-4 reading was the right one and it saved us a bad build.** Relaxing the guard
without the calendar being able to show two things in one slot would have produced **invisible
double-bookings** — worse than the refusal we started with. Deferring the capability and shipping an honest
message is the correct order.

📌 **And your line about the nine NOT_TESTED ACs is the one I would repeat to the owner:** money, LINE and
entitlements — three of the four things this REQ actually changes — have never been exercised. **"Four defects
from done" is the wrong reading**, and the ฿20 overnight run is what turns most of it into evidence.

⏸️ **SPEC-071 (REQ-079) is parked exactly where it was** — tasks 230–235 cut and held on your Q1/Q2. A
`TEST_FAILED` on the REQ that is already in the customer’s hands outranks the next one.

**BALL: @Jason — TASK-236** (the blocker that hides every อื่นๆ booking in the product).

## [--:--] Porter (PM) — ✅ **The owner's LINE is linked as teacher "Bank" on `sid`. AC-16 is GO.** → @Tanya
Confirmed with a screenshot: `สมัคร` → role `2 = ครู` → nickname `Bank` → link request approved, and a
`🔔 แจ้งเตือนจากระบบตารางเรียน` push already arrived on his phone — **the outbound path to him demonstrably works.**
📌 He linked to the existing `sid` teacher **Bank** rather than a fresh "ครูQA-Test" — fine on the dev box (Bank
is the same fixture teacher TEST-063 used), just name **Bank** in fixtures, not a QA-Test teacher.

**@Tanya — AC-16 now, your 10-minute check:**
1. An อื่นๆ booking, **teacher = Bank**, typed title (e.g. `ประชุมทีม QA`), charge **OFF** → `Confirm + LINE`.
   The message on the owner's phone must be named by **the typed title** — never "อื่นๆ", never an empty label.
2. The multi-teacher half: อื่นๆ with **Bank + one other `sid` teacher** → confirm → Bank receives it; the
   booking title names it. (The other teacher is a `sid` fixture with no real LINE — nothing real is reachable.)
3. ⚠️ **The 2 real teachers on the shared channel remain off-limits** — unchanged.
Owner reads the message; **the verdict on what it says is yours** — he is the device, not the tester.

## [--:--] Tanya (QA) — ⏳ **฿20 money run is LIVE on `sid`.** + 3 money ACs turn out to be blocked on data, not effort. → @Porter

Took your two orders. **AC-16 not touched** — I am waiting for your line confirming the owner's own LINE link is
live, exactly as instructed. `Confirm + LINE` has not been pressed on anything. Evidence: `tests/TEST-064`
§Round 2.

### ⏳ The overnight run is set up — two fixtures, deliberately left in place
| | What | id | proves |
|---|---|---|---|
| **F1** | อื่นๆ · **Bank** · 2026-09-01 **14:00** · no student · **charge ON, typed ฿20** · unmarked | **`4014e65e-72f2-4fba-b9f2-788c8f76cd22`** | **AC-5** + **AC-9** |
| **F2** | อื่นๆ · **Dewy** · 2026-09-01 **13:00** · no student · **charge OFF** · unmarked | **`25d695c3-3ace-4b8d-9689-4c49d75d1a55`** | **AC-4** + **AC-9** |

Both saved `201`, both render as `Other` with the typed title, neither confirmed. **The movement key the owner
needs is `rev:4014e65e-72f2-4fba-b9f2-788c8f76cd22` (฿20 / 2000 satang).** F2 is the control — it must post
**nothing**, and without it "F1 posted ฿20" would prove much less.

⚠️ **I do not know the server's time of day.** If tonight's 23:30 pass has already gone when these were created,
they simply carry to the next night — nothing is lost, but do not read "no result tomorrow" as a failure.

### 🔴 Tomorrow's read is NOT mine — this is the gap to close before it bites
**I have no backoffice access.** `sm-test-access.txt` covers the frontoffice only; `backoffice-som.develyst.online`
is not in it. So I can confirm the bookings flip to **ATTENDED** (that closes AC-9) — **but I cannot see what
posted.** ⇒ **AC-5 and AC-4 stay `NOT_TESTED` even after the job runs** unless either the owner reports what
`rev:4014e65e…` came out as, or you get me backoffice read access. **Please pick one now rather than tomorrow**,
or we will have spent the owner's ฿20 and still have no verdict.

### 🔴 Three money ACs are blocked on DATA, not on my effort — each with the exact unblock
1. **AC-21 (freelance budget unchanged) — cannot run.** All **10** freelance teachers on `sid` read
   **`฿0/h · SET PAY BEFORE BOOKING · This month's income ฿0 · 0 h`.** None has a pay rate, and the screen says a
   freelancer cannot be booked before one is set. **I did not set one** — that is a teacher row I did not create.
   And 0 → 0 would prove nothing anyway. ⇒ **the owner sets pay + budget on ONE freelance teacher and names it**;
   I run the AC in ten minutes.
2. **AC-7 / AC-8 (consumes / does not consume) — cannot run.** **No QA-owned student has an active course or
   voucher** — the nine Active courses are all real customers. Deducting from a real family's quota is forbidden;
   creating a course for myself writes a **course sale far larger than the sanctioned ฿20**. ⇒ **one disposable
   course, sanctioned like the ฿20 was**, or point me at one that already exists.
3. **AC-6 (catalogue item) — deliberately NOT run.** The owner approved **฿20**. A catalogue item posts **its
   own** price, which he has not authorised. **I did not stretch his approval to cover it.** One line and I will
   run it — naming the cheapest item and the expected amount **before** I do.

### 🔴 New finding while setting this up — OBS-3: the charged amount is WRITE-ONLY
After saving, **the typed ฿20 cannot be read back anywhere**: the booking detail modal shows no amount, and the
`POST /api/bookings` 201 response carries **no price field at all** — F1 (฿20) and F2 (free) come back
**structurally identical**. ⇒ **an admin can never check, correct, or even notice a charged อื่นๆ before 23:30
turns it into money.** With your own warning that this type posts **with no ceiling**, a `2000`-for-`20` typo is
invisible until it is in the books. It breaks no written AC, so I am not raising it as a defect — **but it is
precisely the risk those ACs exist to protect against, and it is a REQ-078 gap worth settling before `uat`.**

### Unchanged
**Verdict stays `TEST_FAILED`** — DEF-1 and DEF-2 are with @Sober and this run does not touch them. Round 2 only
converts four money criteria from "impossible" to "readable tomorrow".

### Questions Q5–Q9 are in `TEST-064`; the two that block tomorrow are:
- **Q5 — who reads the money?** (owner reports, or I get backoffice read access)
- **Q6 — which freelance teacher may he give a pay rate?**
…and Q7 (a disposable course), Q8 (does ฿20 extend to AC-6), Q9 (should the amount be visible at all).

**Ball: @Porter.** Nothing else is waiting on me: REQ-078's defects are @Sober's, AC-16 is held for your line,
and the rest is blocked on the four answers above.

## [--:--] Jason (BE) — ✅ TASK-236 (the blocker) + TASK-238 → REVIEW. **The DEF-4 sweep found an open window.** → @Sober
tsc **0** · `bun test` **1083 pass / 0 fail** (+25) · **no migration** (30 = 30).
📌 **Code-complete batch** — the commit point is visible here. Both BE defect tasks are done; **@Fern's TASK-237
is the last blocker on REQ-078.**

### TASK-236 — DEF-3 fixed, and the sweep turned up a second one
`students` + `subjects` → **`leftJoin`**; `teachers` stays INNER on purpose (`teacher_id` is still `NOT NULL`, so
that one is a real integrity assertion). The generalisation is written at the site: **a join is a FILTER the
moment its column can be null.**

**The full sweep is in the TASK — every `innerJoin` in `src/` and `scripts/`, with a verdict each.** Three
outcomes worth your eye:
- **Nothing else inner-joins a nullable BOOKING column.** `0029`'s blast radius in this shape was exactly the
  two lines Tanya found. The one I checked hardest was `badge.service.ts:193` — the only other place that joins
  *through* `bookings` — and it is safe (`teacher_id`).
- 🔴 **A second defect, same root cause, which I fixed:** the hand-built row carried no `additionalTeachers`, so
  **the calendar showed every teacher of an อื่นๆ booking and the bookings list showed one.** Not an inner join —
  the identical failure: a hand-written `select()` drifted from what the DTO now expects. Batched loader, one
  query per page. I read your *"say what you found **and what you changed**"* as authorising it; **split it out
  if you disagree.**
- ⚠️ **`coursePackages.student_id` is nullable and `courseSearchQuery` inner-joins on it** — the same defect
  shape, **pre-existing** (not `0029`), and the state is reachable (`addExtraSession` refuses with *"คอร์สนี้ไม่มี
  นักเรียนที่ผูกไว้"*, a guard written for a case that exists). **I did not touch it** — outside REQ-078, and I
  will not put an unrelated search regression into a `TEST_FAILED` release. Your call whether it is a task.

**The `q` search:** I added the title to the OR. You framed it as mine to decide — an อื่นๆ booking has neither a
student nor a program, so without it the row matches **nothing**, and *"searched and found nothing"* is
indistinguishable from *"cannot be searched"*. One `ilike` on an existing column, no second query.

📌 **And the comment trap caught me a second time, in my own replacement comment** — my first draft quoted the
retired sentence while explaining it, and the test I had just written failed on my own text. Same lesson as
TASK-223. The old wording must be absent **entirely**, not even as a quotation.

### TASK-238 — the message, and one thing that must not have the `tx`
`ครูหนึ่ง มีคาบสอนช่วงเวลานี้อยู่แล้ว (น้องเอ 10:00-11:00) กรุณาเลือกเวลาอื่น` — pure composer in
`lib/slot-clash.ts`, so the wording is testable without a database.

🔴 **The lookup runs on `db`, NOT on the transaction that just failed.** A `23505` inside a Postgres transaction
leaves it **aborted**; using `tx` would have turned a clean 409 into a **500 on the one path whose entire job is
to explain itself.** Its predicate mirrors the unique index's own `WHERE`, and the test reads that `WHERE` **out
of the schema** so the two cannot drift. It never invents a name — an unidentifiable clash falls back to the
generic sentence. **AC-25 is structural:** one call site, inside the `23505` branch.
**Untouched and stated:** course creation keeps its own message; the **move** and **plan-change** paths still
refuse generically — widening them during a `TEST_FAILED` release is risk with no AC behind it.

### 🔴 THE DEF-4 SWEEP — the door is shut for every writer. ONE WINDOW IS OPEN.
The refusal is a **partial unique index in the database**, so Postgres enforces it on INSERT *and* UPDATE for
every writer — form, course creation, extra session, plan change, **move**, resume, auto-extend, and every status
change. Table with a verdict per path is in the TASK. The `SICK_LEAVE` carve-out is the deliberate UC-004
overbooking and `getCalendar:494` handles it on purpose — not DEF-4.

⚠️ **`bookings_teacher_slot_uq` guards `bookings.teacher_id` ONLY.** An อื่นๆ booking's **additional** teachers
live in `booking_teachers` — **no slot constraint, no application check.** So **teacher B can be an additional
teacher on a 10:00 อื่นๆ and also teach their own 10:00 lesson**, and nothing refuses it.

It hides nothing **today**, because `getCalendar` keys cells on the **primary** teacher. **But AC-18 requires the
booking to appear in every assigned teacher's column, and TASK-227 is what puts it there** — at which point B's
10:00 cell holds two bookings. DEF-4's shape, through the one door the database does not guard.

📌 **I raised this on TASK-224 §Questions and no ruling came back. You said "say so and stop", so I stopped** —
no guard written. **It changes what TASK-227 must do, so it is time-sensitive:** (a) intended, and 227 must
render two things in one cell · (b) check an additional teacher's slot like the primary's and refuse with the
same AC-24 message (the helper is written and reusable) · (c) a soft warning, which needs the calendar
capability the owner already deferred on DEF-2.

### ⚠️ One DoD line I could NOT do
TASK-238 asks me to confirm the create form renders `e.message`. That is `smart-scheduler-front` — **not mine to
read or change.** **Unverified, and I am naming it rather than leaving the box ticked.** One question to @Fern
via you: does the create form surface `ApiClientError.message` like the cancel dialog, or swallow it into a
generic toast? **If it swallows it, the whole value of TASK-238 is invisible to the user** and it is one FE line.

**Ball: @Sober** — 236 + 238 review, and **the open-window ruling (it gates @Fern's TASK-227 shape)**. Also still
open from me: TASK-236's `coursePackages` finding, and the `e.message` question for @Fern.
**Nothing is waiting on me.**

## [--:--] Sober (SA) — ✅ TASK-236 + TASK-238 PASS. 🔴 **His sweep found a live defect, and it is MY miss.** → @Jason @Porter @Fern

**Reproduced:** `tsc` **0** · `slot-clash` + `search-paging` + `other-booking` **51/0** · the leftJoins at `:784`
and `:788` with `teachers` kept INNER at `:787` · `describeSlotClash` reading through **`db`** at `:963`.

### 🔴 The open window is live NOW, and I am the reason nobody knew

@Jason's DEF-4 sweep is the best piece of work in this batch. He established the refusal is a **partial unique
index**, so Postgres enforces it on INSERT *and* UPDATE for every writer present and future — a far stronger
answer than a table of call sites — and then found the one door it does not guard:
`bookings_teacher_slot_uq` covers `bookings.teacher_id`; an อื่นๆ booking's **additional** teachers live in
`booking_teachers`, unconstrained.

He wrote *"today it hides nothing"* because the calendar keyed cells on the **primary** teacher.
🔴 **That stopped being true while he was writing it.** TASK-227 shipped `teachers.some(...)` in both grids, so
an อื่นๆ booking IS drawn in every assigned teacher's column — and @Fern's own TASK-227 Q2 says a day cell
holding two bookings **draws one and silently drops the other**.

⇒ **DEF-4's exact shape — a session that exists and is not on the calendar — is reachable through the form
today.** It is the precise outcome @Porter deferred the whole overlap capability to avoid.

📌 **This is my miss and I want it recorded as mine.** @Jason raised it on TASK-224 §Questions. I routed it to
@Porter as *"not blocking"* — which was true **at the time**, because the FE half that makes it bite had not
landed. **I never revisited it when TASK-227 landed.** Three people reported the same residue from three angles
(his index note, Fern's day-cell drop, her TASK-226 Q2) and I merged them into one decision for @Porter instead
of noticing that one of the three had just become live. **A risk that is safe "for now" needs a trigger, not a
note** — the same lesson as REQ-079 §8's rehearsal permission, which is written as a trigger for exactly this
reason.

⇒ **TASK-239 cut and RELEASED** — an additional teacher may not be attached when they already have a live
booking in that slot, refused with the **same** AC-24 sentence, "live" read from the index's own `WHERE` rather
than restated. **It takes nothing from the feature:** several teachers may still share an hour; what is refused
is one teacher being in two places. **@Porter — this is the owner's DEF-2 ruling applied to a path his ruling
did not name, not new scope. Strike it if you disagree, but it should not ship as it stands.**

### The two reviews

**TASK-236** — *"a join is a FILTER the moment its column can be null"* is the sentence to leave behind, and it
is at the site. The sweep found a **second** defect (the hand-built `select()` had dropped `additionalTeachers`,
so the list showed one teacher where the calendar showed three) — **same root cause, different clothes**, and
fixing it inside the function he was already repairing was right with Tanya mid-re-test. The `q`-title decision
is his and well reasoned. And the comment trap caught him **again** in his own replacement — the retired wording
must be absent entirely, not quoted while being explained.

**TASK-238** — the **`db`-not-`tx`** call would have cost a 500 on the one path whose whole job is to explain
itself: a `23505` aborts the transaction, so the read that names the teacher would have failed `25P02`. Same
Postgres rule as TASK-218's `enqueueLine` caveat, twice in two features. Reading the index's `WHERE` **out of
the schema** in the test, and falling back to the generic sentence rather than inventing a teacher name, are
both right.

### ✅ His open question — and a trap I found answering it

**Yes, the create form renders `e.message`** (`BookingModal.tsx:946`), so AC-24 reaches the screen.
🔴 **But the MOVE path does not:** `:603-609` special-cases `SLOT_TAKEN` and substitutes a **local generic**,
discarding whatever the server said. Harmless today (generic meets generic). **The day anyone extends
`slotClashMessage` to the move path — which he offered — the FE would silently throw it away.** One rule, two
files, again. Recorded in the task: if that extension is ever ordered, it is a **pair**.

### Also cut
**TASK-240** (🟢 after the release) — `coursePackages.student_id` is nullable and the course search inner-joins
it: a studentless course is invisible and count ≠ rows, **the same shape as DEF-3**, pre-existing. He found it
and correctly refused to fix it inside a blocker release. Written down where it cannot be lost.

**BALL: @Jason — TASK-239.** It is the last thing standing between REQ-078 and a re-test that means something.
(@Fern still has TASK-237, the DEF-1 crash — both are release-blocking, and they do not block each other.)

## [--:--] Tanya (QA) — 📤 **AC-16 fired: two LINE messages sent to the owner.** Send path ✅ — content verdict is his phone, not mine. → @Porter

Took your GO. Both fixtures used **Bank** (= the owner) exactly as you said, and **Dewy** — a `sid` fixture with
no LINE link — for the multi-teacher half. 🔴 **The 2 real teachers were never messaged.** Evidence: `TEST-064`
§Round 3.

| | อื่นๆ booking | id | confirm result |
|---|---|---|---|
| **F3** | **Bank** · 2026-09-02 09:00 · charge OFF · title **`ประชุมทีม QA-078 F3`** | `94db6903-5470-42b1-a980-1ff3cf0d3ebd` | **200 · CONFIRMED · `notification {channel: line, status: queued}`** |
| **F4** | **Bank + Dewy** · 2026-09-02 10:00 · charge OFF · title **`ประชุมทีม QA-078 F4 หลายครู`** | `4357f125-1028-429b-944e-2203c25e704b` | **200 · CONFIRMED · `notification {channel: line, status: queued}`** |

Both payloads carry `title` **and** `displayName` = **the typed Thai title**, never "อื่นๆ", and F4's `teachers[]`
holds **both** Bank and Dewy. So the data the message is built from is right.

### 🔴 I am NOT marking AC-16 passed, and here is exactly why
AC-16 is about **what the message says on a phone**. I proved the confirm lands, the booking goes `CONFIRMED`,
and a push is **queued** — ⚠️ note the field says **`queued`, not `sent`**, so a queued-then-failed push looks
identical from where I stand. **You said it yourself: he is the device, I am the tester.** ⇒ **AC-16 stays
`NOT_TESTED` until he reports the two messages.**

**What I need from him — this is the whole of AC-16:**
1. **F3** — is the message named **`ประชุมทีม QA-078 F3`**? Never "อื่นๆ"/"Other", and no blank label where a
   student name would sit (TASK-219's rule).
2. **F4** — same for **`ประชุมทีม QA-078 F4 หลายครู`** — **and did it arrive at all**, it being a two-teacher one?
3. Text pasted is enough; screenshots into `../project-docs/` are better.

### 🔴 DEF-6 (new, MAJOR) — the confirm dialog names ONE teacher on a multi-teacher booking
On F4 (**Bank + Dewy**) the dialog reads **"Confirm this booking? — Bank will be sent the schedule on LINE."**
**Dewy is not mentioned at all.** This is the sentence the admin decides on, so either it **under-reports** a send
that does reach everyone (misleading), or it is **accurate and only one teacher is messaged** — which fails
AC-16 revised outright. **Both readings are bad and I cannot tell which from the UI.**

### 🔴 And a limit I cannot design around — one linked phone cannot prove "every teacher"
Only **one** teacher on `sid` has a LINE link (Bank = him). Dewy has none, so "did Dewy get it?" has nothing to
observe. Two signals point the wrong way and neither settles it: **DEF-6's dialog**, and the API returning a
**single** `notification` object for a two-teacher booking rather than one per teacher.
⇒ **The multi-teacher half of AC-16 cannot be closed as things stand.** I could have called AC-16 passed on F3
alone — **I am not doing that**, because "Bank got his" says nothing about Dewy. It needs a **second** linked
recipient (a second device of his, or one more `sid` fixture teacher linked temporarily).

### Footprint — four QA bookings now live on `sid`, all deliberate
`4014e65e…` **F1 ฿20** (owner reverses the movement) · `25d695c3…` **F2 free control** · `94db6903…` **F3 LINE** ·
`4357f125…` **F4 LINE multi**. **None cleaned up yet, on purpose** — each waits on a reading only tomorrow's
day-end or his phone can give. F3/F4 are **charge OFF**, so they auto-attend tonight and post **nothing**.
⚠️ Cancelling F3/F4 later may itself push a LINE to him; acceptable, and I will report whatever arrives.

### Ball
**@Porter** — three new questions (Q10 the two message texts · Q11 DEF-6: is multi-teacher meant to message
everyone or just the primary — that is the owner's intent, not a code call · Q12 a second linked recipient), plus
Q5–Q9 from earlier still open, of which **Q5 (who reads the ฿20) is the one that bites tomorrow morning**.
Nothing else is waiting on me.

## [--:--] Porter (PM) — ✅ Q10 ANSWERED by the owner's screenshot: **both LINE messages arrived, named correctly.** → @Tanya
He posted his phone. Both pushes landed, and both are named by the **typed title**:
- **F3:** `📋 ยืนยันตารางสอน · ประชุมทีม QA-078 F3 · เวลา: 2026-09-02 09:00-10:00` (22:39)
- **F4:** `📋 ยืนยันตารางสอน · ประชุมทีม QA-078 F4 หลายครู · เวลา: 2026-09-02 10:00-11:00` (22:45)
**Never "อื่นๆ", no blank student label, and the two-teacher F4 DID arrive.** Also visible: `queued` did become
delivered for both — the queue-vs-sent gap you flagged did not bite here. Screenshot noted; the verdict on
AC-16's Bank half is **yours to write**, not mine.

**Still open and not closeable from his phone:** whether **Dewy** (unlinked fixture) would have been messaged —
DEF-6's dialog says only *"Bank will be sent…"*. That is **Q11 (owner's intent: everyone or primary-only?)** and
**Q12 (a second linked recipient to observe it)** — both put to him now.

## [--:--] Porter (PM) — ✅ Q11 ruled: **LINE goes to EVERY assigned teacher. Fix the confirm dialog.** → @Sober
Owner: *"ส่งไลน์หาครูทุกคน แก้กล่องยืนยันด้วย"*. AC-16 revised stands as written — no spec change.

**@Sober — DEF-6 is released to you, and it is TWO checks, not one:**
1. **The dialog** must name every assigned teacher (e.g. *"จะส่งตารางสอนทางไลน์ถึง ครูBank, ครูDewy"*), not just
   the first. The sentence the admin decides on must not under-report.
2. 🔴 **Verify the SEND itself actually fans out to all teachers** — Tanya saw a **single** `notification` object
   returned for a two-teacher booking, and could not tell whether Dewy's push exists. If the send is
   primary-only, the dialog was *accurate* and the defect is in the send path; if it fans out, the defect is
   display-only. **Read the outbox rows for F4 (`4357f125…`) — that answers it without any phone.**

**Q12 (a second linked recipient) stays open with the owner** — needed only for the on-phone proof; the outbox
read above settles the code question first. **@Tanya — hold the multi-teacher verdict until Sober's outbox
answer + my line here.**

## [--:--] Jason (BE) — ✅ TASK-239 → REVIEW. **The window is shut, and I deleted the second list rather than testing it.** → @Sober
tsc **0** · `bun test` **1096 pass / 0 fail** (+12) · **no migration** (30 = 30).
📌 **Code-complete** — the commit point is visible here. **BE has nothing release-blocking left**; @Fern's
TASK-237 (the DEF-1 crash) is the last blocker on REQ-078.

### The guard
`assertAdditionalTeacherFree()`, per teacher, inside `attachAdditionalTeachers` — the one chokepoint — **before
anything is written**, beside the archived-teacher check already there. Refuses with `slotClashMessage`, the
identical AC-24 sentence the primary's clash produces.

**It is deliberately narrow:** it asks *"does **this teacher** already have a live booking at this date+time?"*
and nothing about how many teachers share the booking. **Three teachers on one meeting still saves.** What is
refused is one person in two places — exactly what the index has always refused for the primary. That is the
first test in the file, because it is the one that proves the guard did not eat the feature.

### 🔴 "Do not restate the list" — I went further than a matching test, and I want the change checked
TASK-238's test *compared* two lists and kept them in step. **That is still two lists.** So I removed the second
one: `db/schema.ts` now exports **`SLOT_INACTIVE_STATUSES`**, **the index's predicate is built from it**, and
both application checks — the new guard **and** `describeSlotClash`, which had been restating the three inline —
read that constant. **There is no second definition of "live" left in the repo.**

⚠️ **This is the one change in the task that could in principle touch the database, and my claim is that it
cannot:** `sql.raw` inlines the same text, so the emitted predicate is **byte-identical**
(`not in ('CANCELLED', 'PENDING_RESCHEDULE', 'SICK_LEAVE')`) and no migration is implied — only where the list
lives. **A test asserts that byte-identity, and it is load-bearing:** `db:verify` witnesses this index by
**name**, so a predicate that quietly changed would pass verification while the running index and the schema
disagreed. **That is the `0022` blindness one layer over**, and it is why I would not make this change on
confidence alone. 📌 **If you would rather the constant existed without the index being built from it, say so** —
the fallback is TASK-238's shape, weaker but touching nothing.

### ⚠️ It is an APPLICATION check, and there is a stronger option I did NOT take
Two concurrent requests can both pass it. That is written at the site in the words a reader needs — *"the
database is NOT holding this one; do not assume it is"* — and a test asserts the warning is present. A unique
index on `booking_teachers` would make it a real guarantee, but that table has no `date`/`start_time` of its
own, so it needs denormalised columns or an index across a join — **a design decision, not a tidy-up**, and a
migration on a release that is mid-re-test. **Recorded as a choice, not as the thing nobody thought of.**

### 📌 One runtime risk I checked instead of assuming
The guard uses `notInArray` and `ne` from the relational-query operator set, which is `any`-typed in the
callback — **`tsc` would not have caught a missing operator**, and it would have failed as
`undefined is not a function` **on the refusal path**, the worst possible place. Verified against the installed
source (`node_modules/drizzle-orm/relations.cjs:94`, `getOperators()`): both are there.
`describeSlotClash` has used `notInArray` since TASK-238 on the same unverified assumption — now verified too.

### 🚫 For @Tanya, via you — the case I would ask her to check hardest
Not the refusal; **the NON-refusal.** A `SICK_LEAVE` occupant must **not** block an additional teacher — that is
UC-004 overbooking, a flow far older than this feature. A guard even slightly too wide breaks it, and it would
surface as an unrelated regression that nobody connects to REQ-078.

**Ball: @Sober** — TASK-239 review, and specifically the `schema.ts` predicate change above.
🟢 **TASK-240** (`coursePackages` course search) is still mine and still after-the-release; I have not started it,
and it is the only thing left with my name on it besides the four ancient POST-GO-LIVE `TODO`s
(**TASK-103 · 110 · 116 · 117**) that I have now asked about three times. **Nothing is blocking on me.**

## [--:--] Sober (SA) — ✅ TASK-239 PASS, window shut. 🔴 **DEF-6 answered from the SOURCE: the send is fine.** → @Porter @Fern @Jason

**Reproduced:** `tsc` **0** · `other-booking` + `slot-clash` + `mappers` **51/0** · the constant at `schema.ts:82`,
the predicate built from it at `:453`, the guard called per teacher **before** the insert at `:1051`.

### 🔴 DEF-6 item 2 — no outbox read needed. The send DOES fan out; the defect is display-only.

You asked whether the send is primary-only. **The code answers it, so @Tanya needs no second phone and nobody
needs a DATA REQUEST:**
- `assignedTeacherIds()` (`scheduler.service.ts:1017`) returns **primary first, then the extras**, deduped.
- The confirm path calls it (`:2457`) and **loops**, enqueuing **one outbox row per teacher** (`:2473-2483`).
- `notification` is assigned **only** for the primary (`:2486`) — deliberately, with the reason on the line: that
  chip has always described the booking’s own teacher and TASK-228 did not change what it means.

⇒ **The single `notification` object Tanya saw is the RESPONSE SHAPE, not the send. Dewy’s row exists.**
**Your item 1 is the whole defect**, and it needs no BE change: `booking.teachers` is already on the DTO and
already rendered in the cell. ⇒ **TASK-241 cut and released to @Fern.**

⚠️ **And one thing your DEF-6 wording does not cover, which I am NOT widening on my own:** the *post-confirm*
chip reads that same primary-only `notification`. With two teachers, one linked and one not, it can say
**"ส่ง LINE แล้ว"** while the second teacher’s row is `SKIPPED`. Same under-report, different moment. I have
asked @Fern to **report what it does and whether the FE can fix it alone — and to build nothing.** If it needs
per-recipient results from the BE, that is a separate task and your ruling.

### TASK-239 — he deleted the second list rather than testing it

📌 I asked him to read "live" from the index’s `WHERE` in a test. **He removed the thing the test was
compensating for**: `SLOT_INACTIVE_STATUSES` is now the single source, and the index predicate is *built from
it*. **A test that keeps two definitions in step is a maintenance contract; one definition is a property.**
There is no second definition of "live" left in the repo.

**On the `schema.ts` edit he flagged for me specifically — approved, and it is safer than he argued.** His case
(`sql.raw` inlines the same text ⇒ byte-identical predicate ⇒ no migration) is right, and the byte-identity test
is load-bearing for exactly the reason he gave: **`db:verify` witnesses this index by NAME**, so a quietly
changed predicate would pass verification while the schema and the running index disagreed — `0022`’s blindness
one layer over. **Plus:** a drizzle index definition is metadata for `drizzle-kit`, and this repo forbids
`db:generate`, so nothing reads that `.where()` at runtime — **the blast radius today is zero.** He did not need
to be that careful; he should keep being that careful, because the version of this change made on confidence
alone is indistinguishable from the version that breaks a box.

📌 **He also verified `notInArray`/`ne` exist in the relational operator set against the installed source** —
those callbacks are `any`-typed, so a typo would have been a runtime `undefined is not a function` **on the
refusal path**: a failure that appears only when someone is already being told no.

**Application check, not an index — right call, and naming the stronger option makes it a decision instead of an
oversight.** A unique index on `booking_teachers` needs denormalised `date`/`start_time` — a design change with
its own failure mode, not a tidy-up, and not something to do mid-re-test.

### 🧪 For @Tanya, via @Porter — the one thing to check hardest on the re-test
**`SICK_LEAVE` must NOT refuse an additional teacher.** A guard slightly too wide breaks **UC-004 overbooking** —
a flow far older than อื่นๆ — and it would surface as an unrelated regression nobody connects to this feature.
Jason named it himself as the risk in his own change, which is the right instinct.

📌 **And the board’s new QA line is the one to act on:** without **backoffice read access**, every money AC stays
`NOT_TESTED` even after the ฿20 job runs. The run is not the evidence; **reading what it posted** is.

**BALL: @Fern — TASK-237** (the DEF-1 crash, still the oldest open blocker) **then TASK-241.** @Jason is clear:
every BE row on REQ-078 is done (236 · 238 · 239), and TASK-240 waits for the release by your ruling.

## [--:--] Porter (PM) — 🏁 CLOSING PLAN for REQ-078. Owner wants it finished. **3 PO cuts, one DATA REQUEST.** → @Sober @Tanya
Owner: *"รีบจบเคสจองอื่นๆ นี้สักที แล้วรีบไปต่อเคสไลน์"*. **Speed is the instruction; lying about coverage is not.**
So I am cutting scope explicitly rather than quietly declaring things tested.

### What still genuinely blocks a verdict

| # | Blocked AC | Why | Resolution |
|---|---|---|---|
| 1 | DEF-1 | the form still kills the page | **TASK-237 @Fern** — the last blocker in the build |
| 2 | DEF-6 item 1 | dialog names one teacher | **TASK-241 @Fern** (send already proven from source) |
| 3 | AC-4 · AC-5 · AC-9 | ฿20 posts tonight; **Tanya cannot read `bo.movement`** | **DATA REQUEST below** — the run is not the evidence, reading it is |
| 4 | AC-16 multi | one linked phone cannot prove fan-out | **@Sober's source read already answers it**; one outbox `SELECT` seals it |
| 5 | AC-21 | all 10 `sid` freelancers are ฿0/h | **owner sets ONE rate** — 30 seconds |

### 🔴 Three PO cuts — mine to make, and I am naming them, not hiding them

- **AC-6 (catalogue-item pricing) → `NOT_TESTED`, accepted.** AC-5 proves the posting mechanism; AC-6 differs
  only in **where the amount comes from**. Testing it costs another overnight run and another reversal by the
  owner. **Named risk: revenue attribution for a catalogue-priced อื่นๆ is unverified** — it would show up as an
  unattributed line in a report, not as a wrong number for a customer.
- **AC-7 / AC-8 (consumes / does not consume an entitlement) → `NOT_TESTED`, accepted.** Tanya owns no course on
  `sid`, and buying one posts money she cannot reverse; using someone else's fixture breaks her charter.
  **Named risk: the entitlement-deduction branch of อื่นๆ is unverified.** ⚠️ Mitigating fact, not an excuse:
  **the owner's own use cases — ประชุมทีม, ปิดปรับปรุงลาน — do not consume entitlements**, so this branch is not
  on the path he asked for.
- 📌 **Precedent, deliberately followed:** REQ-058 shipped with AC-9/AC-10 `NOT_TESTED` and said so. **A named
  gap is a decision; an unnamed one is the thing that bites.**

### 📤 DATA REQUEST — for the owner, tomorrow morning, ONE pass

**(a) The ฿20 posting and the free control** — settles AC-4 · AC-5, and AC-9 by implication:
```sql
SELECT m.idempotency_key, i.external_ref, m.value_minor, m.created_at
FROM bo.movement m JOIN bo.item i ON i.id = m.item_id
WHERE m.idempotency_key LIKE 'rev:4014e65e%'
   OR m.idempotency_key LIKE 'rev:25d695c3%'
ORDER BY m.created_at;
```
**Expected: exactly ONE row** — `4014e65e` (F1) at **`value_minor = 2000`**. 🔴 **`25d695c3` (F2, charge OFF)
must NOT appear at all** — that absence is the whole of AC-4, and it is why the free control was created.

**(b) The outbox fan-out for F4** — settles AC-16's multi-teacher half without a second phone.
⚠️ **@Sober — give me the exact `SELECT` for `notification_outbox` on booking `4357f125-1028-429b-944e-2203c25e704b`**
(one row per teacher expected: Bank + Dewy). I am not writing SQL against a table I have not read; you have.
**Post it here and the owner runs both queries together.**

### Then
**@Tanya — re-test scope is: DEF-1 · DEF-6's dialog · the SICK_LEAVE overbooking regression @Sober flagged
(UC-004 — a guard slightly too wide breaks a flow far older than อื่นๆ) · plus the two query results.** Verdict
is yours; AC-6/7/8 are recorded as accepted gaps, not as passes.
**@Sober — TASK-237 and TASK-241 are the whole remaining build.**

## [--:--] Porter (PM) — 🔴 OWNER'S ORDER: **this is the LAST round on REQ-078. Then PARK it either way.** → @everyone
> *"ตอนนี้ fern jason sober กำลังทำอยู่ รอบสุดท้าย พอนะ เทสแล้ว ไม่ผ่าน หรือไม่ผ่าน ก็พักไว้ note ไว้ แล้วไปทำ line ก่อน"*

**Finish what is in your hands. Nothing new gets cut on REQ-078 after it.**
- **@Fern — TASK-237 (DEF-1) + TASK-241 (DEF-6 dialog). That is the end of the build.**
- **@Sober — review those two, and post the `notification_outbox` SELECT I asked for. Then REQ-078 is closed to
  you and your lane is `REQ-079`.**
- **@Tanya — one re-test round, then write the verdict WHATEVER it is.** 🔴 **A second `TEST_FAILED` is a
  legitimate outcome here and I will not lean on you to avoid it.** The owner has pre-accepted the park; what he
  is buying is the *record*, not a green light.

### What "park with a note" has to mean, or it is just abandonment
**@Tanya, in `TEST-064` — and this is the deliverable, not the verdict:**
1. **Every AC's final state**, including the three I cut (AC-6 · AC-7 · AC-8 — accepted gaps, not passes) and
   anything still failing.
2. **The exact fixtures left alive on `sid`** with ids, and for each: what it is waiting on and **who can retire
   it.** F1–F4 are already yours; do not clean up what tomorrow's queries still need to read.
3. **The one thing a fresh session must do first** to resume — written so it survives all of us forgetting.

📌 **REQ-078 stays `TEST_FAILED` on the board unless Tanya passes it.** Parked ≠ delivered ≠ done. It is the
owner's #1 on his customer list, so **`OWNER-LIST.md` will say "In Progress — parked", not "Completed"** —
I am not letting a park quietly read as a delivery in the list he shows the customer.

**Everyone's next lane after this round: `REQ-079` (his REQ-016).**

## [--:--] Fern (FE) — ✅ TASK-237 → REVIEW. **The lead was wrong, the cause was mine, and the test proves it.** → @Sober
`H:\scheduler\smart-scheduler-front`, HEAD **`bda6511`**. Detail in **TASK-237 §Implementation Notes**.

**Verified:** `bunx tsc --noEmit` **exit 0** · `bun test` **82 pass / 0 fail** (was 76/0 — +6) · `bun run build` ok.

### The cause — `BookingModal.tsx:1088`, and it is TASK-226's, i.e. mine
```tsx
onChange={(e) => setOther((p) => ({ ...p, title: e.currentTarget.value }))}
```
React nulls `event.currentTarget` **the instant the listener returns**; a `setState` **updater** is a closure
React calls **later**, during the render it schedules. So that read lands on `null`. `.value`, on `null` —
exactly the reported error. Two siblings had the same shape with `.checked` (the Charge and Consume switches).
All three were mine; all three are fixed.

📌 **Why it is intermittent, which is the part that explains everything else:** `dispatchSetState` has an
eager-evaluation fast path — with no pending update on the hook, React runs the updater **synchronously inside
the handler**, where the event is still alive, and the bug is invisible. Queue an update on that hook first and
the path is gone. **Removing the last teacher chip queues one through the MultiSelect** ⇒ the very next keystroke
dies. That is Tanya's 3-of-3, precisely.

**And her DEF-5 detail is what confirms it rather than my reasoning.** She saw one death *"right after toggling
Charge"* and one *"typing in Title with the charge block open"* — **both reporting `reading 'value'`, not
`reading 'checked'`.** That is the tell: toggling Charge does not crash, it *queues*; the crash lands on the next
read, which is Title. It is also why she could not reproduce them — whether React's fast path is available
depends on what else was queued, which no tester can hold constant. **She was right to report them as
intermittent and unproven instead of inventing a repro; the honest report is what made them diagnosable.**

### 🔴 The task's lead (`TeacherOption:32`) was plausible and wrong — ruled out, not dismissed
`teachers.find()` returning undefined is **already guarded**; for it to throw, Mantine would have to pass a null
`option`, which its own types forbid. **Decisive and needing no browser:** `TeacherOption` has **8 call sites and
seven have been in production for weeks** — if it were the cause, this crash would not be new to the อื่นๆ form.
Following it would have put a defensive guard in a component that was never broken, left the real cause running,
and made the next report much harder to believe because the fix would "look" applied.
⇒ **Sober, your own line — *"if the stack points somewhere else, follow the stack, not this task"* — is the only
reason this landed.** I have asked in Q1 whether that should be the standing shape for every defect task rather
than this one's good instinct.

### The regression FAILS without the fix — I checked instead of claiming it
Restored the buggy one-liner and ran the suite:
```
TypeError: null is not an object (evaluating 'e.currentTarget.value')
(fail) 🔴 DEF-1 … keeps the typed character — the exact repro, and it must not throw   [+3 more]
 20 pass, 4 fail
```
**The same error Tanya saw**, in Bun/JSC's wording of Chrome's *"Cannot read properties of null (reading
'value')"*. Restored the fix → 82/0. The tests drive React's real lifecycle (call handler → null `currentTarget`
→ then run the updater), and one of them asserts the defect's actual cost: **the rest of a half-filled form
survives the keystroke.**

🚫 `e.currentTarget?.value` was rejected as the fix — it stops the crash and silently writes `""`. **A title
that vanishes as you type is worse than a stack trace, because nobody reports it.** Asserted, not just argued.
🚫 AC-19 untouched: emptying the teacher list is still legal; *saving* without one is still refused.
🧹 **Repo sweep: no second instance.** The only other hit is my own comment quoting the defect; all 20+ other
`currentTarget` reads in this repo already read eagerly.

### 🔴 Not done — and Q2, which is against my own work
I could not run the browser repro (auth wall), so DEF-1 is proven at the **mechanism** level and **Tanya's two
DEF-5 paths are not re-walked**. Expecting is not verifying. **Please route to @Tanya via @Porter as a LOCAL
check** (the mock covers this form): the exact repro + values preserved · zero-teacher save still refused · her
two DEF-5 contexts — and **if either still dies it is a separate defect, not to be folded into this one.**

**Q2 — worth recording rather than quietly fixing: this defect was introduced by TASK-226 and passed your
review, my own 18 tests, tsc and the build.** None of them could have caught it — the shape is legal TypeScript,
the types are right, and it works most of the time. **What caught it was Tanya running the form**, and a fix was
possible only because she gave the exact console error and a 3-of-3 sequence. The rendered checks I keep routing
to her are not a formality at the end of my tasks; on this one they were the only detector there was.

**Ball: @Sober** — TASK-237 review + Q1/Q2. Nothing else open for FE: TASK-111 waits on TASK-110, TASK-118 on
TASK-116; 226/227 and the rest are code-DONE with their rendered checks in @Tanya's queue.

## [--:--] Sober (SA) — ✅ TASK-237 PASS. **She disproved my lead and found a defect I had already approved.** + the outbox SELECT. → @Porter @Fern @Tanya

**Reproduced:** `tsc` **0** · `bun test` **82 pass / 0 fail** (+6) · `build` ok · the three handlers named at
`lib/scheduler/other-booking.ts:111,117,122`, wired at `BookingModal.tsx:1091,1111`.

### The lead I gave her was wrong, and her disproof is better than the lead was

I pointed at `TeacherOption.tsx:32` because the property name matched. **Her refutation needed no browser and is
decisive: the component has 8 call sites, 7 of them in production for weeks** — if that were the cause, this
crash would not be new to the อื่นๆ form. She also named the cost of having followed it: **a guard in a component
that was never broken, the real cause still live, and — because the fix "looked" applied — the next report much
harder to believe.**

### The real cause, and why the diagnosis is complete rather than merely correct

React nulls `event.currentTarget` when the listener returns; a `setState` **updater** runs later, during the
render it schedules. Three handlers read the event there.

🔴 **The intermittency half is what makes it a diagnosis.** `dispatchSetState` runs the updater synchronously
**when the hook has no pending update** — so the buggy code works until something queues one. Removing a teacher
chip queues exactly that, so the next keystroke takes the deferred path and dies. **Tanya’s repro, derived from
the mechanism rather than matched to it.**

📌 **And it explains DEF-5 instead of excusing it.** Toggling Charge *queues*; the crash lands on the **next**
read (Title) — which is why her message said `reading 'value'` from a path that started at a switch. **Why she
could not reproduce it on retry is now a property of React’s fast path, not a gap in her testing.** ⚠️ Her caveat
stands and I kept it: if either path survives, it is a separate defect, reported as one.

### 🔴 My miss, on the record

**Those three handlers were in the TASK-226 diff and I passed it.** I read that task closely enough to praise the
`SegmentedControl` and to catch three hand-written arrays, and I read straight past
`onChange={(e) => setOther((p) => ({ ...p, title: e.currentTarget.value }))}` — because **the broken version is
visually indistinguishable from the correct one.** She wrote it; I approved it. That is not a "read harder" fix,
it is a lint-rule-or-named-helper fix, and she has now supplied the helper.

📌 **Two things worth keeping from how she worked:** she **restored the buggy line, ran it, and pasted the four
failures** — the difference between *a test exists* and *a test that fails without the fix*. And she **rejected
`e.currentTarget?.value`**: it stops the crash and silently writes `""`, and **a title that vanishes as you type
is worse than a stack trace, because nobody reports it as a bug** — asserted in the tests, not argued in prose.

### 🔴 @Porter — the `notification_outbox` SELECT you asked for

**The code question is already settled and needs no data** (my last entry): `assignedTeacherIds` →
`scheduler.service.ts:2457` → a **loop** enqueuing one row per teacher `:2473-2483`; `notification` is set only
for the primary `:2486`, deliberately. **The send fans out; DEF-6 is display-only.**

Here is the SELECT anyway, because *"the code says so"* and *"the rows are there"* are different claims and F4 is
the one booking where both can be checked. Pure SQL, read-only, for the owner in his own psql session:

```sql
SELECT recipient_type, recipient_line_user_id IS NOT NULL AS has_line, status, created_at
FROM notification_outbox
WHERE booking_id = '4357f125-...'  -- F4, full id in TEST-064
ORDER BY created_at;
```

**What it answers:** **two** `teacher` rows ⇒ the fan-out is real and DEF-6 is display-only, as the code says.
**One** ⇒ the code and the data disagree, which would be a far more serious finding than the dialog, and I would
want it before Tanya writes any verdict. A `SKIPPED` status on the unlinked teacher is **expected** and is not a
failure — it is the record that we tried.

### 🧪 @Tanya, for the last round — the three I would spend the time on
1. **DEF-5’s two paths** (toggle Charge, then type in Title). The fix predicts they are gone; that prediction is
   the only part of this diagnosis not yet tested against the product.
2. **`SICK_LEAVE` must NOT refuse an additional teacher** — a guard slightly too wide breaks **UC-004
   overbooking**, a flow far older than อื่นๆ, and it would look like an unrelated regression.
3. **The bookings list actually rendering the rows it counts** (DEF-3) — the joins are proven, the page is not.

**BALL: @Fern — TASK-241.** It is the last build item on REQ-078 before the owner’s final round.
