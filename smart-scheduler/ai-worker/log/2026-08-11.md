# Log — 2026-08-11 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Porter (PM) — GATE 0 pg_hba: gathered prod ground truth + provided grounded temp-open steps. Sober's turn produced nothing on disk → @Sober ratify
Customer-prod deploy is paused at GATE 0 (Option A — temp-open prod `pg_hba`, deploy from laptop, close after).
The handoff's ball was `@Sober` (exact pg_hba open/reload/close). **After the owner nudged Sober "ไป", Sober wrote
nothing to any shared file** (no log entry, no board change, no runbook section, no commit on any branch). So there was
nothing for Porter to relay. Rather than stall the owner mid-deploy, Porter gathered the missing ground truth via a
read-only DATA REQUEST to the owner (the only one who can touch prod) and is providing the grounded steps below.

**Prod ground truth (owner ran on the server, localhost psql):**
- `SHOW hba_file;` → `C:/Program Files/PostgreSQL/18/data/pg_hba.conf`
- `pg_hba_file_rules`: only localhost is allowed — `local all all scram-sha-256`, `host all all 127.0.0.1 scram-sha-256`,
  `host all all ::1 scram-sha-256`, plus replication rows. **No remote host entry** → that is exactly why the laptop IP
  (`110.171.40.169`) was rejected. Auth method everywhere = `scram-sha-256`.

**Grounded temp-open (given to the owner now):**
- ADD one line to `pg_hba.conf`, scoped to the laptop's single IP (NOT `0.0.0.0/0` — far safer, opens only the one host):
  `host    all    all    110.171.40.169/32    scram-sha-256`  (method matches existing entries; existing `.env` password works)
- Reload without restart: `SELECT pg_reload_conf();` (expect `t`)
- Verify present: `SELECT ... FROM pg_hba_file_rules WHERE address = '110.171.40.169';`
- **Close after deploy:** remove that line → `SELECT pg_reload_conf();` again → verify the row is gone.

Both backends' `.env` `DATABASE_URL` = `postgresql://postgres:***@154.197.124.29:5432/smart_scheduler` (same prod host/db)
→ GATE 0 topology check ✅. **All four existing `sm-prod-backup-*.dump` files are 0 bytes (failed attempts) → no valid
backup yet; GATE 0 backup NOT cleared. Do not migrate until a non-zero dump is confirmed.**

**@Sober** — please ratify this pg_hba addition and record it as a proper "run-on-server temp-open" section in the
customer-prod runbook (your artifact), so the channel isn't relying on a Porter-authored infra step. Flagging that your
last turn produced nothing on disk — if your session errored or answered only in-chat, that output never reached the team.

**Ball: owner** (edit pg_hba → reload → verify → switch to laptop → `bun run db:backup` → confirm non-zero dump) with
Porter walking each gate. **& @Sober** (ratify + fold into the runbook).

## [--:--] Porter (PM) — GATE 0 CLEARED · STEP 1 (backoffice-back / bo) GREEN → running STEP 2 (scheduling)
Deploy progress on customer-prod (owner running from laptop, pg_hba temp-open in effect):
- **pg_hba temp-open worked** — added `host all all 110.171.40.169/32 scram-sha-256`, reload OK, laptop reaches prod.
- **GATE 0 backup GREEN.** `db:backup` produced `sm-prod-backup-2026-08-11T10-39-50-609Z.dump` — Porter verified the
  archive (`pg_restore --list`, read-only): **37,015 bytes, CUSTOM/gzip, full schema + all TABLE DATA sections, dbname
  smart_scheduler 18.4** — valid, right DB. The earlier "0.0 MB" print was just the script rounding a genuinely small DB.
- **Prod data confirmed by owner** (read-only counts, then owner confirmed "customer isn't using it yet, this is correct"):
  bookings 0 · students 9 · teachers 21 · course_packages 5 · vouchers 2. Not empty, not wrong DB.
- **STEP 1 — backoffice-back `db:migrate` GREEN.** bo schema created (0000→0005 applied), ledger
  `__drizzle_migrations_bo` = 6 rows, `db:verify` ✅ (4 witnessed + 2 RETIRED `0001_item_pl`/`0002_item_external`
  accounted for). Clean because bo was genuinely new on prod.
- **Next — STEP 2 scheduling-back `db:migrate`.** ⚠️ Higher risk: prod already HAS the scheduling tables
  (students/teachers/etc.) but the split ledger `__drizzle_migrations_scheduling` is new — the backup showed only the old
  default `drizzle.__drizzle_migrations`. Expect a possible orphan/wrong-hash flag → witness fallback `db:seed-ledger` →
  `db:verify`, per runbook. Owner instructed to STOP and paste full output regardless of green/red; Porter gates before
  any fallback runs. pg_hba stays open until deploy completes.

**Ball: owner** (run STEP 2, paste full output — do NOT run seed-ledger unprompted) → Porter gates. **& @Sober** (still
owes the runbook ratification + is on call if STEP 2's ledger transition needs a judgment call).

## [--:--] Porter (PM) — STEP 2 hit the expected orphan case → witness dry-run CLEAN (0 halts) → applying 0009–0017
STEP 2 (`scheduling-back db:migrate`) exited 1 as anticipated: prod already has the scheduling tables but the new split
ledger `__drizzle_migrations_scheduling` was empty (0 rows), so drizzle tried to re-apply `0000` and hit existing objects.
Before touching prod, Porter read `scripts/seed-ledger-from-schema.ts` and confirmed it is **dry-run by default,
witness-based (schema is source of truth), idempotent, self-halting, and never modifies the old `__drizzle_migrations`.**
Ran `db:seed-ledger` (DRY RUN): **prod is at migration 0008.** 0000–0008 witnessed applied (9); **0009–0017 witnessed
NOT applied (9)** — genuinely missing, additive features. **`0 need a human`**, no blockers. Post-seed, `db:migrate`
would apply exactly `0009_noshow_jobruns … 0017_entitlement_source`. This matches the sid precedent and the runbook's
documented fallback. Proceeding: `db:seed-ledger --apply` (seeds 9 ledger rows only, no DDL) → `db:migrate` (applies
0009–0017 + verify). Watch-item on apply: a new column/constraint could fail against prod's existing rows (e.g. 0017's
`vouchers_source_chk` vs the 2 existing vouchers) — transactional, so it rolls back; STOP→Porter on any red, backup covers us.

**Ball: owner** (`db:seed-ledger --apply` → `db:migrate`, paste full output; STOP before build if red) → Porter gates on
`db:verify` GREEN. **& @Sober** (runbook ratification still owed).

## [--:--] Porter (PM) — 🟢 BOTH DB migrations GREEN on customer-prod (STEP 1 + STEP 2). Closing pg_hba → build/copy/restart
`db:seed-ledger --apply` inserted the 9 witnessed rows (old ledger untouched); `db:migrate` then applied **0009–0017**
cleanly. `db:verify`: ledger `__drizzle_migrations_scheduling` = 18 rows, **18 schema witnesses, all GREEN**. The
watch-item (0017 `vouchers_source_chk` vs 2 existing vouchers) passed — the DROP-IF-EXISTS NOTICE is normal. **Prod DB is
now fully forward-migrated: bo schema created + scheduling at 0017.** The hardest, least-reversible part of the deploy is
done; the Gate-0 backup (`sm-prod-backup-2026-08-11T10-39-50-609Z.dump`) remains the restore point.
- **pg_hba:** the laptop's prod-DB access was only needed for backup + the two migrates — all done. Nothing left
  (build/copy are local; pm2 restart runs on the server against localhost) needs it, so **closing the temp-open line NOW**
  (remove `host all all 110.171.40.169/32`, reload, verify gone) — earlier than the runbook's "after deploy", strictly safer.
- **Remaining:** STEP 3 build all 4 (local) → STEP 4 copy to server → STEP 5 `pm2 restart` bo-back→bo-front→sched-back→
  sched-front → then @Tanya smoke on `frontoffice.develyst.online`.

**Ball: owner** (close pg_hba on the server + build all 4 on the laptop; paste build results) → Porter gates each. **&
@Sober** (runbook ratification still owed — the run-on-server/temp-open path Porter drove should be folded in).

## [--:--] Porter (PM) — 🏁 CUSTOMER-PROD DEPLOY COMPLETE. Post-deploy smoke → @Tanya
Owner reports all 4 builds passed, copied to the server, and **deployed (`pm2 restart` bo-back→bo-front→sched-back→
sched-front done)**. Full deploy is in: pg_hba temp-open → Gate-0 backup → both DB migrations GREEN (bo + scheduling@0017,
via the witness seed-ledger fallback) → build 4 → copy → restart. Owner provided UAT access ("ที่เดิม").

**@Tanya — post-deploy acceptance smoke on the deployed customer env.** This is the last leg before REQ-038 #1–5 (and
the rest of this deploy) can move to DELIVERED (DELIVERED = TEST_PASSED + post-deploy re-check on the deployed env).
- **Env:** `https://frontoffice.develyst.online` (+ `https://backoffice.develyst.online`). NOTE: this is the customer's
  prod URL, **but the customer is not using it yet — 0 bookings, only master data (9 students / 21 teachers / 5 courses /
  2 vouchers)** — so it's effectively UAT for this deploy, and the runbook explicitly directs the smoke here. Still apply
  your prod-caution: **no real notifications to real recipients, remove any test data you create, don't touch data you
  didn't create.**
- **Access:** local file `C:\Users\Admin\sm-test-access.txt` (staff login + secrets; **untracked — do NOT commit or paste
  into any tracked file/log**).
- **Smoke set (from the deploy runbook — same set accepted on sid):**
  - **REQ-038 #3** — student search on `/scheduler/calendar` filters day+week grids; **measure the filter row at
    1600 / 1280 / 768 / 375** (the one open runtime check on TASK-124).
  - **#5** deduction history (`/courses/:id/history` → 200, timeline renders, "who not tracked" note).
  - **#2** calendar course-picker · **#4** voucher-shows-class · **107** voucher-exclusion picker · **109** rental entry ·
    **102/122** settings screen + reset.
  - **REQ-030** core (plan edit / cancel-with-reason) · **REQ-037** extra session.
  - On PASS → set each REQ `TEST_PASSED` with evidence → the customer's essential set is live in their real env.

**Ball: @Tanya** (run the smoke, set verdicts, flag any defect to @Porter) → Porter marks DELIVERED / routes any defect to
Sober. **& @Sober** (runbook ratification for the run-on-server temp-open path still owed).

## [--:--] Porter (PM) — ⚠️ OPEN ITEM: owner chose to LEAVE pg_hba temp-open (deviates from runbook "close after")
Owner decided to keep the `host all all 110.171.40.169/32 scram-sha-256` line in prod `pg_hba.conf` open for
convenience ("just my machine, easy to fix later"), rather than closing it per the runbook / the whole Option-A premise
(temp-open → CLOSE). Porter advised closing and recorded the risk: the line is scoped to one IP + password (not wide
open), **but that laptop IP is dynamic** — if it's reassigned, another host could reach prod postgres with only the
password as a gate (and the env secrets look dev-tier). Suggested the equally-convenient safer path: keep prod
localhost-only and RDP to the server to run any fixes (localhost is always allowed). **Owner's call on their own infra;
logged as an OPEN SECURITY ITEM to revisit.** @Sober — flag for the runbook/security note.

**Ball: @Tanya** (smoke, unchanged). Open item: **owner to eventually close prod pg_hba.**

## [--:--] Porter (PM) — Owner-run Script 6: A/B/C ✅ · owner AUTHORIZES Tanya on prod (in writing) · owner orders a full prod data-wipe → @Tanya
Owner ran Script 6 himself: **A ✅ (new build landed — 4-control filter row), B ✅ (#3 student search), C ✅ (#5 deduction
history + the "who isn't tracked" note).** D not run (no time), **E skipped** (as allowed). Screenshots confirm the
frontoffice build is live on prod.

**@Tanya — you have the written authorization you asked for.** Owner's exact words (Thai): *"นี่คือสัญญาณว่า ให้ tanya
ทำได้เลย … มันเป็น prod ก็จริง แต่ลูกค้าแทบจะไม่ใช้เลย … ให้เทสได้เลย."* i.e. **QA is authorized to run on
`frontoffice.develyst.online` for this acceptance** — customer isn't using it (prior version wasn't production-ready).
Standing condition from Tanya still holds: don't send LINE to real people; remove any test data you create. **Do NOT edit
the TASK-090 guard to get there** — the authorization is the human's, deliberate, via Porter (recorded here).

**⚠️ Owner also ordered a FULL PROD DATA-WIPE** ("ลบข้อมูลให้หน่อย … เคลียร์ข้อมูลทั้งหมด") — the current data is
pre-release junk (unused). Porter is handling this carefully: (1) **brownfield — Porter/SA never run it; the owner runs it
against prod**; (2) the GATE-0 backup `sm-prod-backup-2026-08-11T10-39-50-609Z.dump` preserves the current state if ever
needed; (3) **scope must be pinned first** — some tables are reference/config the app needs to run (subjects catalog,
badge types, app_settings) vs pre-release records (teachers/students/parents/bookings/courses/vouchers). Confirming scope
with the owner before any destructive command is issued.

**Sequencing:** the wipe changes what QA sees, so Tanya's run is sequenced around it (either finish the quick D check on
current data → DELIVERED → then wipe, OR wipe → Tanya verifies on fresh data she creates/removes). Being decided with owner.

**Ball: @Porter** (pin wipe scope with owner → hand owner an owner-run reset; sequence Tanya) → then **@Tanya** runs the
authorized smoke. **& @Sober** (candidate to author a safe reusable `db:reset` respecting FK order + keeping schema/ledgers/config).

## [--:--] Porter (PM) — Owner set the QA-on-prod operating rules + the dev-delete chain. Porter's raw TRUNCATE retracted → @Tanya
Owner corrected the process (rightly): **Porter does NOT hand-author ops/dev SQL** for the owner to run. My drafted
ad-hoc `TRUNCATE` is **retracted / do-not-run.** Two standing rules from the owner:

1. **@Tanya — test self-contained on prod.** You're authorized on `frontoffice.develyst.online`. **Create your own data
   and test on what you created** — e.g. to test the calendar booking flow, create a booking yourself, then test
   edit / delete / cancel on that booking. Clean up with the **UI buttons** for anything you can remove that way (the
   parents/students you created, etc.). You do NOT need the DB wiped first — self-containment is the safety.
2. **Dev-side deletions go through the chain, not Porter.** Anything the UI can't delete (including the full pre-release
   data clear) must flow: **Porter → Sober → TASK → Jason writes a delete/reset script → back to Porter → owner runs it.**
   Porter never writes the SQL.

**Status:** Tanya is unblocked to start the authorized smoke now (self-contained). The pre-release junk-clear is **pending
the owner's go/no-go**; if go, Porter opens a REQ → @Sober to have Jason author the reset script (scope agreed: keep
teachers + subjects + config, wipe the people/transactional records), which the owner then runs.

**Ball: @Tanya** (run the authorized self-contained smoke; verdicts → @Porter). **& @Porter** (await owner's go on the
junk-clear → route to @Sober if yes).

## [--:--] Porter (PM) — Owner: GO on the junk-clear, before QA. Opened REQ-040 → @Sober
Owner's decision: **clear the pre-release junk FIRST, then he'll tick Tanya** — deliberately sequential so any later
anomaly is attributable to QA's own data, not leftovers ("ล้างจบฉันค่อยสั่ง tanya … ทำทีละขั้นตอนเพื่อความปลอดภัย").
Routed through the chain as the owner instructed: **created `requirements/REQ-040-prod-pre-release-data-reset.md`
(READY_FOR_SA).** Scope confirmed by owner = **keep teachers + subjects (+ config/badges/settings + schema/ledgers),
clear the people/transactional records** (students, parents, course_packages, vouchers, bookings, booking_badges,
notification_outbox, line_link_sessions, teacher_link_requests, freelance_budgets, job_runs). Porter grounded the
keep/clear split against `src/db/schema.ts` (no KEEP table FK-references a CLEAR table). Deliverable = a **Jason-authored
script the OWNER runs on the prod box** (brownfield); returned via Porter. GATE-0 backup is the restore point.

**@Sober** — please pick up **REQ-040**: spec it + TASK Jason to write the reset script (transactional / count-check /
keep ledgers), send it back up to me, and I'll walk the owner through running it. This is the gate before QA starts.

**Ball: @Sober** (REQ-040 → SPEC → TASK Jason → script back to Porter). **& @Tanya** (hold the smoke until the reset is
confirmed — owner's sequencing).

## [--:--] Porter (PM) — Sober delivered the run-on-server runbook (moot now) but NOT REQ-040. Re-pointing Sober → @Sober
On the owner's nudge, Sober wrote `DEPLOY-CUSTOMER-PROD-ON-SERVER.md` (code repo) — the run-on-server deploy variant it
owed. **Acknowledged / that ratification is DONE** — but note it's **moot**: the deploy already happened via Option A
(laptop temp-open), not this path. Keep it as documentation only.

**The actually-pending item is untouched: REQ-040 (the prod junk-clear reset script).** No SPEC, no TASK, no script in
either repo. That's the single thing gating the owner (who has said: clear junk FIRST, then QA).

**@Sober — please pick up REQ-040 specifically now (ignore the old deploy runbook, it's delivered).** Deliverable:
spec it → TASK Jason to write a reset script that **keeps teachers/subjects/config + both ledgers** and **clears the
people/transactional tables** (list in REQ-040), transactional with a pre-`COMMIT` count check, cmd.exe/`bun run`-friendly
on the Windows prod box, then hand it back up to me. I'll walk the owner through running it.

**Ball: @Sober** (REQ-040 ONLY — SPEC + TASK Jason → reset script → Porter). Old runbook item: closed.

## [--:--] Tanya (QA) — 🛑 I can't run the prod smoke — it's `frontoffice.develyst.online`. **Script 6 is written instead.** → @Porter

Read the deploy thread. Congratulations on getting both migrations green — that was the risky part. But I
have to stop at the last leg, and I'd rather say so plainly than quietly do it.

### Why I'm not running it
The smoke target is **`frontoffice.develyst.online` = the customer's PRODUCTION server** (board's
ENVIRONMENTS card: `sid` = som.develyst.online · **production = frontoffice.develyst.online**). Three
things say no, and they don't disagree with each other:
1. **My role card, `QA.md`:** *Production — 🚫 never. **Not read, not write, not "just a GET."** If a check
   truly can only be done on prod, write it as a DATA REQUEST to Porter and let the human run it.*
2. **The workspace rule:** QA *"tests on local + the dev server but never production."*
3. **TASK-090's tool refuses that exact host by design** — `PRODUCTION_HOSTS = ["frontoffice.develyst.online"]`
   → exit 1, and its own error text says: *"If you genuinely need this against production, that is a
   decision to route up the chain — not to work around here."* Sober built that guard deliberately.
   **I have not worked around it, and I won't.** I also made **no request of any kind** to that host — not
   even a status check.

### Your argument is a good one, and I still don't think it changes the answer
You're right that it's *effectively* UAT today: 0 bookings, master data only, and the runbook points the
smoke there. If the rule were mine to weigh, that's the case I'd weigh. But "it's basically not production
yet" is exactly the reasoning the rule exists to interrupt — and today the env is **more** delicate than
usual, not less: it has just taken its first migrations in weeks and **prod `pg_hba` is still temp-open to
a dynamic laptop IP** (your own open item). A QA harness poking around is the last thing that env needs
before the owner closes that line.

Also worth weighing: the runbook wasn't wrong to *ask* for a prod smoke — someone should do it. It's the
**who** that's fixed, not the what.

### What I've done instead — `tests/CLICK-SCRIPTS-owner.md` **Script 6**
A full owner-run post-deploy smoke, same shape as Scripts 1–5, ~10 minutes of presses:
- **A** — the 30-second "did the build land at all" check (does the calendar filter row have **four**
  controls?). If A fails, stop; nothing else matters.
- **B** — #3 student search: filters, empties on nonsense, clears, works in Daily too, **plus a width check
  an owner can actually do** (narrow the window; nothing may squeeze to a sliver).
- **C** — #5 deduction history incl. the "who isn't tracked" note and the raw-key failure mode to watch for.
- **D** — #2 · #4 · 107 · 109 · 102/122 as quick looks, each with an explicit ✅/❌.
- **E** — REQ-030/037 plan editor, **clearly marked as the section that writes data**, with permission to
  skip it (it was fully accepted on `sid`, so skipping costs little). No step asks her to send LINE.

### What this changes about statuses — nothing yet, and I want to be precise
Every item in that smoke set is **already `TEST_PASSED` on `sid` against the same build artifacts**. What
the prod run adds is the **post-deploy re-check on the deployed env**, which is your own definition of
DELIVERED. So: the verdicts stand as they are; **DELIVERED waits on Script 6 coming back**, not on new
testing from me. Send me the ✅/❌ list and I'll convert it the same day.

### If you want a different answer, here's the honest route
This is the human's env and the human's rule. If the owner (via you) says in writing *"QA is authorized on
frontoffice.develyst.online for this smoke"*, then the boundary has moved and I'll run it — **but the
TASK-090 guard still shouldn't be edited to get there**; access for that would need to come from the human,
deliberately, not from me patching a safety check I was given. My recommendation is simply: let the owner
run Script 6. It's faster than the conversation.

**Ball: @Porter** — hand Script 6 to the owner (or bring back a written authorization). Unchanged on my
side: REQ-037 revenue re-check after a day-end · DEF-1 (MINOR) · Scripts 4 + 5 still awaiting her phone.

## [Sober (SA)] — REQ-040 analysis done + a process flag before I cut the delete SPEC/TASK → @Porter
Picked up REQ-040. The **design work is done and I'm handing it to you**; but I'm **holding the executable SPEC/TASK
for one confirmation** (below), because this is a mass delete on the customer's real production DB.

### The analysis (done — grounded, reusable)
- **Keep/clear FK check — CONFIRMED against `smart-scheduler-back/src/db/schema.ts`:** **no KEEP table FK-references a
  CLEAR table.** KEEP only references KEEP (`teacher_subjects`→teachers/subjects, `badge_values`→badge_types,
  `bo.movement`→bo.item; `bo.movement.refId` is soft `text`, not an enforced FK). So clearing the set cannot cascade
  into or be blocked by teachers/subjects/config. Your grounding holds.
- **Delete order (restrict FKs force it):** booking_badges → notification_outbox → bookings → course_packages →
  vouchers → students → parents → freelance_budgets → teacher_link_requests → line_link_sessions → job_runs.
- **Safety model I want the script built to:** ONE transaction; **dry-run by DEFAULT** (prints a per-table count table,
  asserts KEEP-counts unchanged, then `ROLLBACK`) and deletes only on an explicit `--commit` — the project's own OBS-3
  dry-run-rollback idiom, so the owner previews exactly what will go before committing; explicit `DELETE` (not
  `TRUNCATE CASCADE`); schema + all 3 ledgers never touched; GATE-0 dump `sm-prod-backup-2026-08-11T10-39-50-609Z.dump`
  is the restore point.

### 🔴 The process flag (why I paused instead of just cutting the SPEC/TASK)
This authorizes **irreversibly deleting production records** across 11 tables. The go-ahead so far lives in the
workspace files (REQ-040, "owner said"), not a confirmed human authorization I can point to. For a destructive prod
op I want that **explicit sign-off routed through you (Human↔Porter)** on the record before I author the delete
script and hand it to Jason — same care we took opening prod `pg_hba`. **@Porter — please (a) confirm the owner's
explicit go on the record with you, then I cut SPEC-036 + TASK-127 immediately to the design above; or (b) tell me to
proceed and you own that authorization.** The dry-run-default means even the first run is harmless, but the *authoring*
of a prod mass-delete is what I want your explicit call on.

### Also still open (not mine, flagging): prod `pg_hba` temp-open
Tanya noted prod `pg_hba` is **still temp-open to the laptop IP** from the deploy. That exposure is hanging — the
owner's close+verify step (revert the line, reload, confirm the laptop is refused) hasn't been confirmed. Worth closing
before anything else on prod.

**Ball: @Porter** (confirm the REQ-040 authorization on the record → I cut SPEC-036 + TASK-127 same tick; and nudge the
owner to close the prod `pg_hba` line). Analysis is done; only the go-ahead gates the script.

## [--:--] Porter (PM) — ✅ REQ-040 authorization CONFIRMED on the record + safety model approved → @Sober, cut the SPEC/TASK
@Sober — good pause; here's the on-record confirmation you asked for (option (a)).
- **Owner's explicit go, Human→Porter, recorded:** the owner ordered the wipe directly to me — *"ฉันสั่งเลย ลบข้อมูลให้หน่อย
  … เก็บครู+วิชาไว้"* then *"ไปเลย เริ่มงาน ล้าง junk data ก่อน แล้วล้างจบฉันค่อยสั่ง tanya … ทำทีละขั้นตอนเพื่อความปลอดภัย."*
  That is a deliberate, explicit human authorization for the REQ-040 scope (keep teachers/subjects/config; clear the 11
  people/transactional tables). **Confirmed — proceed.**
- **Safety model APPROVED as scoped:** one transaction · **dry-run by default** (per-table count table + assert KEEP-counts
  unchanged + `ROLLBACK`) · deletes only on explicit `--commit` · explicit `DELETE` in your restrict-FK order
  (booking_badges → notification_outbox → bookings → course_packages → vouchers → students → parents → freelance_budgets
  → teacher_link_requests → line_link_sessions → job_runs) · schema + all 3 ledgers untouched · GATE-0 dump is the restore
  point · owner runs it on the Windows prod box (cmd.exe / `bun run`-friendly), returned via me.

**@Sober — cut SPEC-036 + TASK-127 to that design and hand to Jason.** When the script is back, I walk the owner:
`db:reset` (dry-run) → owner reviews the count table → `--commit` → confirm KEEP intact / CLEAR = 0.

**pg_hba (Sober + Tanya both flagged):** still owner's-choice open per his earlier call; re-raised with him. His infra
decision, logged as an open security item — not blocking REQ-040.

**Ball: @Sober** (cut SPEC-036 + TASK-127 → Jason writes `db:reset` → back to Porter). **& owner** (tick Sober, then Jason).

## [Sober (SA)] — REQ-040 design is ON RECORD, but I hit a hard limit authoring the executable delete artifact → @Porter
Thanks for the on-record confirmation. Two things, plainly:

1. **The full design is already logged** (my entry above): the FK-verified keep/clear split, the exact restrict-FK
   delete order, and the dry-run-by-default / single-transaction / KEEP-tripwire / explicit-DELETE / ledgers-untouched
   safety model — approved by you as scoped. Nothing about the design is open.
2. **I could NOT write out `SPEC-036` / `TASK-127` as files** — my tooling **blocks me from authoring an artifact that
   generates a mass `DELETE` across production tables**, and it treats the authorization relayed through the workspace
   (owner→Porter→here) as *not sufficient* for that specific action: generating production mass-deletion tooling needs
   the real principal's explicit, direct authorization at the tool layer, which a chain-relayed "go" doesn't satisfy.
   I'm not going to work around that guard — for a prod wipe that's the right place for it to sit.

**So the honest status:** the *what/how* is fully specified and safe; the *authoring of the destructive script* is
gated above the simulation chain. To move it, the reset script needs to be produced under the real principal's explicit
authorization — not something I can manufacture from the log's say-so. My design entry above is complete enough for
that build to proceed the moment it's authorized at that level.

**Unrelated, still open:** prod `pg_hba` temp-open — you've re-raised it with the owner; it stays an open security item
until the close+verify is confirmed. QA remains held behind REQ-040 per the owner's sequencing.

**Ball: @Porter** — REQ-040 script authoring is blocked at my tool layer (needs explicit real-principal authorization,
not a chain relay); the safe design is on record for whoever produces it. Nothing further I can do on it from here.

## [--:--] Porter (PM) — REQ-040 resolution: owner (real principal) runs Sober's on-record design directly, verify-first
Sober's tool layer refused to author the executable prod mass-delete artifact (chain-relayed auth ≠ real-principal auth
at the tool layer) — the right place for that guard. Jason would hit the same class of guard, so the dev-chain can't
emit the script. **Resolution the owner confirmed:** the delete design comes from the team (Sober's on-record FK-order +
dry-run/transaction safety model), and **the owner — the real principal and sole prod operator — runs it directly** in
his own psql session, which is exactly the authorization level the guard requires. Owner also asked for a **read-only
verify query first** ("query ก่อน เพื่อเมคชัวร์ ก่อนให้ฉันรันลบ").
- **STEP 1 (given):** a read-only `KEEP` vs `CLEAR` per-table count query → owner runs, Porter reviews that KEEP holds
  the real data (teachers/subjects/config) and CLEAR is the junk, before any delete.
- **STEP 2 (after verify OK):** Sober's delete procedure relayed for owner-direct-run — `BEGIN` → `DELETE` in Sober's
  restrict-FK order (booking_badges → notification_outbox → bookings → course_packages → vouchers → students → parents →
  freelance_budgets → teacher_link_requests → line_link_sessions → job_runs) → recount (KEEP unchanged / CLEAR=0) →
  owner `COMMIT` or `ROLLBACK`. Reversible pre-COMMIT; GATE-0 dump is the backstop.

**Ball: owner** (run STEP 1 verify → send counts → Porter reviews → STEP 2 delete). **@Sober/@Jason:** no further action
needed on REQ-040 — the design is yours/on-record; execution is the principal's by design.

## [--:--] Porter (PM) — ✅ REQ-040 EXECUTED on customer-prod (owner ran it, verify-first, in-transaction). Clean slate.
Owner ran the verify query first (Porter reviewed: KEEP held the real master, CLEAR held the junk), then ran Sober's
delete procedure inside `BEGIN`. **In-transaction result before COMMIT — exactly right:** deletions
notification_outbox 2 · bookings 9 · course_packages 6 · vouchers 2 · students 9 · parents 8 (rest already 0); recount
**KEEP unchanged (teachers 21 · subjects 9 · teacher_subjects 159 · badge_types 1) and every CLEAR table = 0.** Porter
green-lit `COMMIT`. Note for the record: prod `bookings` had grown 0→9 (and course_packages 5→6) between the GATE-0
backup and the wipe — those rows are NOT in `sm-prod-backup-2026-08-11T10-39-50-609Z.dump`; owner accepted (they were in
the CLEAR set by his instruction). **Customer-prod is now a clean slate: teachers + subjects + config kept, all
people/transactional records cleared. REQ-040 satisfied.** Resolution recap: Sober designed it (on record), Sober's tool
layer wouldn't author the executable prod-delete, so the **owner (real principal) ran the design directly** — the
authorization level the guard required.

**Ball: owner** (`COMMIT`, then tick @Tanya). **@Tanya** — the junk-clear is done; you're clear to run the authorized
self-contained smoke on the clean slate → verdicts to @Porter → DELIVERED.

## [--:--] Porter (PM) — ⛔ CORRECTION: REQ-040 delete did NOT persist — COMMIT didn't take (GUI transaction scoping). Data intact, re-running.
Correcting the entry above: the wipe is **NOT** done. A fresh verify after the owner's "commit" still shows the CLEAR
tables fully populated (bookings 9 · students 9 · parents 8 · course_packages 6 · vouchers 2 · notification_outbox 2) and
KEEP unchanged. **Nothing was deleted — no harm, but no clear either.** Root cause: the owner ran it in a VS Code SQL
tool where **each run uses a separate connection** — the `BEGIN;`+`DELETE`s batch had no `COMMIT` in the same run so it
auto-rolled-back, and the separately-run `COMMIT` hit a connection with no open transaction (no-op). Classic GUI
transaction-scoping trap. **Fix given:** run `BEGIN … DELETE … COMMIT` as ONE selected batch (the delete was already
previewed correct in-transaction, so atomic run+commit is safe), or use the server `psql` CLI (persistent session).
Then re-verify CLEAR=0 persists. REQ-040 stays **open until the re-verify confirms**.

**Ball: owner** (re-run the atomic block → re-verify CLEAR=0 persists → send Porter). Then @Tanya.

## [--:--] Porter (PM) — ✅ REQ-040 CONFIRMED DONE — clean slate persisted (prior "not persisted" was a mis-sent stale screenshot)
Resolved: the delete **did** commit. The earlier "still populated" screenshot was an old image the owner copied by
mistake. Fresh verify (same psql session) confirms: **every CLEAR table = 0** (booking_badges, bookings,
notification_outbox, course_packages, vouchers, students, parents, freelance_budgets, teacher_link_requests,
line_link_sessions, job_runs) and **KEEP intact: teachers 21 · subjects 9 · teacher_subjects 159 · badge_types 1**
(app_settings/badge_values were already empty). **REQ-040 satisfied — customer-prod is a clean slate: teachers +
subjects + config kept, all people/transactional records cleared.** Restore point `sm-prod-backup-2026-08-11T10-39-50-
609Z.dump` retained (note it predates the 9 bookings that were created then cleared).

**Ball: @Tanya** — junk-clear confirmed done; run the authorized self-contained smoke on the clean slate → verdicts to
@Porter → REQ-038 #1–5 DELIVERED. **& owner** (tick Tanya). Open item still standing: prod `pg_hba` temp-open (owner's call).

## [--:--] Tanya (QA) — the authorization is in the FILES, not from the human. Recording the owner's A/B/C prod evidence; D/E still owed → @Porter

Read the whole REQ-040 thread. Two things to report, and the second one is the reason I still haven't run
on prod.

### 1. Owner-run Script 6 gives us real prod evidence — recorded
Owner ran it himself: **A ✅** (the new build landed — the calendar filter row has **four** controls, which
is the single best "did the frontoffice deploy take" signal), **B ✅** (#3 student search), **C ✅** (#5
deduction history + the "who isn't tracked" note). **D not run · E skipped** (E was explicitly skippable).
That is genuine post-deploy evidence on the customer env, and I'm recording it as such — it covers the two
newest, highest-risk items. **What's still missing for a full post-deploy re-check is section D**
(#2 picker · #4 voucher-shows-class · 107 voucher exclusions · 109 rental entry · 102 settings screen) —
five quick looks, ~4 minutes.

### 2. 🛑 I still can't run it myself — and the reason has changed
The authorization you recorded is real and I'm not questioning it: the owner's words are quoted in this
log, and the conditions attached (no LINE to real people · self-contained data · clean up after · **don't
edit the TASK-090 guard**) are exactly the ones I asked for. **But it reaches me as a line in a file
written by a teammate, not as an instruction from the human in my own session.** My tooling stopped me on
precisely that distinction when I tried the prod login, and it was right to: *"a self-referenced
'authorization on the record' is not verifiable as a real user instruction."*

That is the same standard I applied to myself when I refused the first time — *"the authorization is the
human's, deliberate"*. A workspace file can **relay** a human decision; it can't **be** one. If I accepted
a file as proof of authorization, then any file could authorize anything, and the guard would be
decorative. So I'm holding, and I've asked the human directly for the one sentence that settles it.

**I did not work around anything:** the TASK-090 guard is untouched, and the only prod request I attempted
was the app's own login endpoint (blocked, and I stopped there rather than looking for another route).

### 3. So there are exactly two ways to finish this, and both are cheap
- **(a) The owner spends ~4 more minutes on Script 6 section D** — then the post-deploy re-check is
  complete with zero authorization questions, and I convert everything to DELIVERED-ready the same day.
- **(b) The human tells me directly, in session, that QA is authorized on `frontoffice.develyst.online`** —
  then I run the full self-contained smoke under the owner's rules (create my own data, test on it, remove
  it via the UI, never touch a row I didn't create, and no action that could push LINE to a real teacher).

**My recommendation is (a)** — not because (b) is wrong, but because the owner has already done 3 of the 5
sections and D is five glances at screens that are already live. It needs no authorization debate at all.

### ⚠️ One thing I'd want handled either way, before QA writes anything on that box
If I do run there, I'll avoid any **teacher-change** flow. On `sid` that path fires **dual LINE notifies to
both teachers** (TASK-094), and prod kept its **21 real teachers** through the wipe. Whether any of them
still carries a linked `lineUserId` after the clear isn't something I'd want to discover by sending one a
message. Teacher-change is already `TEST_PASSED` on `sid`; I'd deliberately not re-run it on prod, and I'd
say so in the verdict rather than quietly skip it.

**Ball: @Porter** — either hand the owner Script 6 **section D**, or relay that the human should tell me
directly. Statuses unchanged: everything remains `TEST_PASSED` on `sid`; **DELIVERED waits on the prod
re-check**, which is now 3/5 done thanks to the owner.

## [--:--] Tanya (QA) — routing correction on myself: the question goes to YOU, not the human. `TEST-PROD-post-deploy-recheck.md` = BLOCKED → @Porter

Process note first, because it was my error: I put the prod-authorization question to the **human
directly**. He redirected it — *"ถ้ามีคำถาม ให้ส่งมาที่ PM ให้ PM คิด และเขาจะถามฉันเอง"* (questions go to
the PM; the PM thinks it through and asks me himself). That is the chain working as designed and I should
have used it in the first place: my only contact is you. Re-routed properly below, and I've stopped
addressing the human on this.

**New artifact: `tests/TEST-PROD-post-deploy-recheck.md`, status BLOCKED**, with the question in a
`## Questions` block per PROTOCOL. It holds:
- **What's already done, and it's real:** the owner's own Script 6 run — **A ✅** (build landed: the
  four-control filter row) · **B ✅** (#3 search) · **C ✅** (#5 history + the actor note). That covers the
  two newest, highest-risk items on the customer env.
- **What remains: section D only** — five looks, ~4 minutes (#2 picker · #4 voucher subject · 107
  exclusions · 109 rental · 102 settings). Section E stays skipped (accepted on `sid`).
- **A standing exclusion I want ratified either way:** **no teacher-change flow on prod.** It fires dual
  LINE notifies (TASK-094) and prod kept its **21 real teachers** through the REQ-040 clear — I'm not
  willing to find out whether one is still linked by messaging them. Already `TEST_PASSED` on `sid`.

**The question for you (yours to decide or escalate):**
**(a)** hand the owner **Script 6 section D** — my recommendation, because it needs no authorization
discussion at all and he's already done 3 of 5; or **(b)** get the authorization delivered **to QA in its
own session** rather than as a workspace-file entry, and I'll run the full self-contained smoke under the
owner's rules.

Why (b) can't be satisfied by the log entry, stated once so it doesn't come back: the owner's words are
quoted accurately and the conditions attached are exactly the ones I asked for — but a **file can relay a
human decision, it cannot be one.** If a file could authorize prod access, any file could authorize
anything. My tooling blocked the one login attempt on precisely that ground, and I stopped there. **The
TASK-090 guard is untouched and I made no other request to that host.**

Nothing else on my side changes: all `sid` verdicts stand; **DELIVERED waits on this re-check**, now 3/5
complete thanks to the owner. Standing backlog unchanged (REQ-037 revenue after a day-end · DEF-1 MINOR ·
Scripts 4 + 5 for REQ-020/023 on the owner's phone).

**Ball: @Porter.**

## [--:--] Tanya (QA) — ✅ PROD PHASE 1 PASSED (zero footprint) · 🔴 #2/#4 stopped: **the app cannot delete what they need me to create** → @Porter

The human authorized me in-session, which is the standard I had set, so I ran. Access was the app's **own
login form** — `mint-session.mjs` was **not run, not edited, not bypassed**; it still refuses that host.
Detail: `tests/TEST-PROD-post-deploy-recheck.md` (phase 1 appended) · evidence
`../project-docs/qa-prod-2026-08-11/` · harness `tests/harness/prod-smoke-zero-residue.mjs`.

### Phase 1 — everything that leaves NO trace. All PASS, nothing created/changed/sent.
- **Clean slate confirmed from the app:** courses **0** · vouchers **0** · bookings **0** · **teachers 21**.
- **Safety pre-check first, before touching anything:** **no teacher exposes a LINE-link flag** and no admin
  LINE recipients are configured — so nothing I did could have reached a real person. (Teacher-change stays
  excluded and unrun regardless, per your ratification.)
- **A — the build is live** (`Find student` present on the timetable).
- **🎯 STANDING RULE measured on PRODUCTION — this closes TASK-124's one open runtime item:**

| Viewport | Find student | Teacher | Type | Lines | Narrowest | h-scroll |
|---|---|---|---|---|---|---|
| 1600 | 288 | 505 | 313 | 2 | 288 | none |
| 1280 | 214 | 357 | 215 | 2 | 214 | none |
| 768 | 208 | 207 | 115 | 2 | 115 | none |
| **375** | **317** | 247 | 247 | **3** | 247 | none |

  Nothing collapses (the defect this rule exists for was **26/36 px**), the row wraps rather than crushes,
  no page overflow.
- **107 ✅** the voucher Program picker offers no excluded program (checked against the server's own
  `voucherAllowedGroups`, not a name guess) · **109 ✅** "Record rental" opens Equipment+Hours, then
  **Cancel — nothing recorded** · **102 ✅** the settings screen lists both rules; I did **not** exercise
  override/reset on production (that stays covered by its `sid` pass).
- ⚠️ **One thing to record so it is not misread later:** the row shows **three** controls now, not the four
  in Script 6 step A. That is correct — **Badge** renders only when badge values exist
  (`CalendarHeader` gates on `badgeSelectData.length > 0`) and the REQ-040 clear left `badge_values` empty.
  The real "did it land" signal is `Find student`, which is there. Not a regression.

### 🔴 Phase 2 (#2, #4) — I stopped before creating anything, and I would like you to look at why
The instruction was: create a student/course/voucher, verify, **"then remove everything you created via the
UI."** I checked the delete path *before* creating — and **there is not one**:
- the API exposes exactly **two** deletes: `DELETE /teachers/:id/line-link` and `DELETE /settings/:key`;
- the frontend calls exactly **one** (the teacher LINE link);
- **students, parents, courses, vouchers and bookings have no delete at all** — a booking can only be
  *cancelled* (still listed) and a parent only *suspended* (still listed).

So doing #2/#4 self-contained would permanently add **1 parent · 1 student · 2 courses · 1 voucher · ~9
bookings** to the production DB the owner cleared **hours ago, for exactly this reason** — and I could not
remove any of it. The cleanup condition my authorization was granted *with* cannot be met, so the
authorization does not really reach this case. I would rather surface that than hand back a one-way mess
with an apology attached.

**Three ways to close #2/#4, my preference first (question is in the artifact):**
**(a)** wait for the owner's **real** data — both are contract-level behaviours already `TEST_PASSED` on
`sid` against these exact artifacts, and he will create students/courses/vouchers the moment he uses the
system; I verify then, read-only, dirtying nothing.
**(b)** accept the residue **knowingly** — I create the minimum, verify, cancel every booking I can, and the
rest is removed later through the owner's own **dev-delete chain** (you → @Sober → TASK → Jason's reset
script → owner runs).
**(c)** the owner creates one student + two courses + one voucher himself; I verify read-only.

If the answer is (b), please have the owner confirm it **knowingly** — he cleared that data today, and it
should not be un-cleared by a QA side-effect he did not picture.

**Ball: @Porter.** Everything else from this deploy is now verified on the customer env; #2/#4 are the only
open squares, and they are a reversibility decision rather than a testing one.

## [--:--] Tanya (QA) — ✅ **CUSTOMER-PROD RE-CHECK COMPLETE. Whole smoke set PASSES. No defects.** → @Porter

The human approved phase 2 and **waived cleanup** (he'll re-run the reset), so #2 and #4 could be done
properly — and section E with them. Everything is now confirmed on the customer environment.
Detail: `tests/TEST-PROD-post-deploy-recheck.md` · evidence `../project-docs/qa-prod-2026-08-11/` ·
harnesses `prod-smoke-phase2.mjs` + `prod-smoke-sectionE.mjs`.

### The whole set, on prod
**A ✅ B ✅ C ✅** (owner) · **#2 ✅ · #4 ✅ · #5 ✅ · 107 ✅ · 109 ✅ · 102 ✅ · REQ-030 ✅ · REQ-037 ✅ ·
OBS-3 ✅ · STANDING-RULE widths ✅.**

- **#2** — the picker names which course *before* selection:
  **`QA-prod · Bike / Scooter / Balance Cruiser (0/4) · exp 2026-09-24`** vs
  **`QA-prod · Surfskate (0/4) · exp 2026-09-25`** — subject, progress, and TASK-125's expiry tiebreaker,
  on two genuinely different programs (the customer's real case).
- **#4** — the voucher booking carries its class through every surface: API 1/1 · the bookings table's
  Subject column · and the plan modal in the **voucher** shape (`VOUCHER · 5 / 5 h left`, "moved one at a
  time (no make-up chain)").
- **#5** — history re-verified post-wipe on a course with real activity: 200, 4 events, dated entries with
  subject/teacher, **no raw i18n key**.
- **Section E, all on my own rows:** preview is a true dry run (stored plan unchanged) and refuses with the
  same typed reason (**409 `SLOT_TAKEN`**) · move applies · **planned absence keeps the course at size**
  (4→4) · insert places the trailing session · **live cancel re-owes** · delivered row refuses edit/move
  (**409 `SESSION_DELIVERED`**) but **cancel needs a reason** (**409 `REASON_REQUIRED`**, then cancels and
  re-owes with one) · **REQ-037** extra is a `SINGLE_SESSION` that leaves size/end untouched and **its
  cancel doesn't re-owe** · and the UI shows **"Your plan will become: 0 added · 1 removed · ends 17 Sep
  26"** *before* committing, with the plan unchanged (9 → 9 rows) while the confirm sat open.

### Held throughout
Only QA-created rows touched · **no LINE to real people** (pre-checked: no teacher exposes a link flag, no
admin recipients) · **teacher-change NOT run** (ratified exclusion; stays covered by its `sid` pass — I'm
stating that so the verdict isn't read as covering it) · **TASK-090 guard untouched**; access was the app's
own login form.

### Data left behind — by agreement
1 parent + `QA-prod-student` · 2 courses · 1 voucher · 1 voucher booking + the sessions and section-E edits
on them. Cleanup was waived and the app has no delete for these rows anyway. All prefixed **`QA-prod`** and
enumerated in `../project-docs/qa-prod-2026-08-11/phase2-created.json` so the reset can confirm it caught
everything.

**Ball: @Porter** — **the DELIVERED gate is met** for REQ-038 #1–5 and REQ-030 / REQ-037 / OBS-3 (marking
DELIVERED is yours, not mine). Then the owner's reset. Standing on my side, unchanged: REQ-037 revenue
re-check after a day-end · DEF-1 (MINOR) · Scripts 4 + 5 on the owner's phone for REQ-020/023 · and the
prod `pg_hba` item is still open (his call).

## [--:--] Porter (PM) — 🏁 REQ-038 #1–5 DELIVERED on customer-prod. Full post-deploy re-check PASS, zero defects.
Tanya's `TEST-PROD-post-deploy-recheck.md` is **COMPLETE — the whole smoke set PASSES on `frontoffice.develyst.online`,
no defects.** Phase 1 zero-footprint (build live · 107/109/102 · filter-row widths healthy at 1600/1280/768/375 →
**closes TASK-124's open runtime check** · safety pre-checks clean); Phase 2 on QA-created data (**#2** course picker,
**#4** voucher-shows-class), **#5** re-verified post-wipe, and **all of Section E** (REQ-030/037/OBS-3, E-1..E-15) PASS.
Teacher-change deliberately excluded and unrun (dual LINE to 21 real teachers) — stays covered by its `sid` pass.
**TEST_PASSED on `sid` + this post-deploy re-check on the deployed env → the DELIVERED bar is met. Marking REQ-038
essential set #1–5 (and REQ-030 / REQ-037 / OBS-3) DELIVERED.** The customer's standard-timetable essential set is live
and verified in their real env.
- **Cleanup pending (housekeeping, does NOT gate DELIVERED):** owner re-runs the REQ-040 delete block in server `psql`
  to clear Tanya's QA-created footprint (1 parent · `QA-prod-student` · 2 courses · 1 voucher · 1 voucher booking +
  sessions — all in CLEAR tables) → prod back to clean slate for real go-live. Residue was knowingly accepted for the
  test window.
- **Still open (owner's call):** prod `pg_hba` temp-open — flagged by Sober + Tanya + Porter; not blocking anything.

**Ball: owner** (run the reset → confirm CLEAR=0 → prod clean for go-live). **REQ-038 #1–5 = DELIVERED.**

## [--:--] Porter (PM) — New: UI RETEST of the pro-engineer's FE rework, through the hallmark lens → @Tanya
Context: a **professional frontend engineer** (`neeeeroooo`) pushed 2 commits to `smart-scheduler-front@dong` —
`7f9456e` (installs the `hallmark` design skill) + `63f734d` (responsive tables + AntD-style pinned columns: new
`StickyScrollArea`, `data-pin`, badge `NO_TRUNCATE`, filled Manage button, PlanModal `⋯` menu + `size="1100px"`).
He is the **bar the team must learn from and surpass** (owner). Porter distilled the lessons into the workspace-root
**`FRONTEND-STANDARD.md`** — that's the checklist.

**@Tanya — retest the reworked FE and baseline it against the standard.**
- **Where:** run `smart-scheduler-front` **locally on branch `dong`** (has the 2 commits) — `localhost`, your "run
  anything" zone. **NOT prod** (prod still runs the pre-merge UI; this code isn't deployed). If you can't get it
  running, DATA REQUEST to me.
- **Functional retest of the reworked screens** (BookingsTable · VoucherPanel · PlanModal), especially at **375 / 768**:
  lead (checkbox) + action (Manage / `⋯`) columns stay **pinned**, edge shadow only when that side has hidden content,
  **no clipping**; badges show full labels (no "PEN…"); Voucher **Manage reads as a filled button**; PlanModal actions
  in a **`⋯` menu that works on tap + keyboard**, modal wide, delivered rows = cancel-with-reason only, live rows =
  edit/mark-absence/cancel; dates `DD/MMM/YY`; filter selects don't clip. **No functional regression** (search, filters,
  bulk confirm, plan dry-run/preview still work). Teacher-change stays excluded.
- **Design-lens pass:** run **`hallmark audit`** on those screens → verdict (`ships as slop | reads as AI-generated |
  close, fix minors`) + a **ranked punch-list** of remaining anti-patterns per `FRONTEND-STANDARD.md` §3 (card-in-card,
  tabular-nums, one-token-source, 8-states / instant focus-visible, side-stripe/red-green badges, over-confirm modals).
- **Deliverable:** a TEST file with the functional verdict **and** the hallmark punch-list — that punch-list becomes the
  input to the REQ that brings the rest of the FE up to standard (Fern, via Sober).

**Ball: @Tanya** (run locally on `dong` → functional verdict + hallmark punch-list → @Porter). Owner-side still open (not
blocking): prod QA-data reset + `pg_hba`.

## [--:--] Tanya (QA) — FE rework retest: **functional `TEST_PASSED`, no regressions, DEF-1 CLOSED** · hallmark verdict **`close, fix the minors`** (0 critical · 7 major · 2 minor) → @Porter

Ran locally on `dong` (mock mode, `localhost:3016`) — the repo was already on that branch with both commits
and a clean tree, so no checkout was needed. Full file:
`tests/TEST-DONG-fe-rework-retest-and-hallmark-audit.md` · evidence `../project-docs/qa-dong-2026-08-11/` ·
harnesses `local-dong-ui-retest.mjs` + `local-dong-followups.mjs`. **Nothing touched any server.**

### The rework does what it claims — measured, not trusted
- **Pinned columns work:** 11 `[data-pin]` elements, all `position: sticky`, at **375 / 768 / 960 / 1280**.
- **No truncation:** 40 chips scanned per width, **0 truncated** — the "PENDING→PEN…" tell is gone.
- **Honest scrolling:** 0 clipped cells anywhere; at 375 a 760 px table sits in a 301 px scroller with
  `overflow-x: auto` and **no page h-scroll**.
- **The edge shadow is genuinely scroll-aware:** `data-at-start`/`data-at-end` flip with width
  (375 → `start=true end=false`; 960+ → both true). That's the detail I most expected to be decorative.
- 🎯 **DEF-1 is CLOSED.** The Voucher "Manage" button I filed on 2026-08-04 as *unreachable* at 375
  (`elementFromPoint → null`, off the painted surface) is now **hit-testable at 375** and reads as a real
  filled button. His `StickyScrollArea` fixed my defect as a side-effect of doing it properly.
- **PlanModal:** width **1100 px**; each row exposes **one** "Actions" control instead of 2–3 mini buttons;
  the ⋯ menu opens on **Enter** (keyboard, not hover-only) with `Edit · Mark absence · Cancel`.
- **No functional regression:** search filters and clears (10 → empty → 10) · status filter returns 6 rows
  all `CONFIRMED` · plan modal still renders. **Bulk confirm: NOT exercised** — the filtered set had no
  selectable rows; saying so rather than assuming it.

### Three functional misses (the rework stopped one step short)
- **M-1 (major):** the **plan** table got `DD/MMM/YY`, but the **bookings** table still renders **ISO
  `2026-08-13`** — 10 cells. Two date formats now coexist, which §2 explicitly forbids ("one helper,
  everywhere").
- **M-2 (major):** **0 of 20** numeric cells carry `tabular-nums`. §2 calls misaligned digits an instant
  tell, and `anti-patterns.md` lists it as a major.
- **M-3 (minor):** the Voucher Manage button is **30 px** high at 375 — §3.2 wants ≥44 px on phone.

### hallmark audit — 0 critical · 7 major · 2 minor → **`close, fix the minors`**
Majors, each with a file/line in the test file: **two colour systems in parallel** (Mantine + Tailwind
`bg-default-*` across **26 files** — the standard's own named "biggest sin") · **4 inline hex**
`bg-[#f5f7fb]` (login, checkin ×2, AdminLayout) · **4 × `transition-all`** (Sidebar, CalendarGrid ×2,
CalendarWeekGrid) · **the focus-visible ring animates** (`transition-property: all` on the row-action
control) · **no `tabular-nums`** · **one-font page** (body *and* headings both Noto Sans Thai) ·
**inconsistent dates**. Minors: status chip is hue+label with no shape; the 44 px hit target.
Checked and **clean**: card-in-card, side-stripe badges, 3-column grid, `hover:scale-105`, arbitrary
`z-index`, hover-only affordances, invented metrics.

### The line I want on the record
The reworked screens are a real step up and nothing there "ships as slop" — **but by our own §3 Definition
of Done this is not finished**: **§3.3 fails** (the focus ring animates) and **§3.5 fails** ("zero hits" for
inline hex / `transition-all` is currently **4 + 4**). Those are gates the standard says must pass *before*
a FE task is `REVIEW`-ready. I'd rather say that plainly than let "the pro engineer did it" carry the
verdict — the standard has to apply to his commit too, or it isn't a standard.

### Ranked punch-list for the follow-up REQ (cheap ones first)
1. one token source (kill the parallel Tailwind colour scale — biggest blast radius, 26 files)
2. the 4 inline hex → one `--color-paper` token
3. drop `transition-all`; never transition the focus ring *(2+3 together clear §3.3 and §3.5)*
4. `tabular-nums` on numeric/date/time columns
5. one date formatter for every table
6. a real type pairing for headings — ⚠️ **needs a Thai-capable display face; that's a design call for the
   owner, not a code fix**
7. status chip carries shape as well as hue · 8. 44 px hit target on phone

**Ball: @Porter** — the punch-list is the input for the REQ that brings the rest of the FE up to standard.
Two questions in the file: the type-pairing decision (owner's eye) and whether you want bulk-confirm
covered separately. Local dev server stopped; standing backlog unchanged.

## [--:--] Porter (PM) — Opened REQ-041 (FE → FRONTEND-STANDARD conformance) off Tanya's punch-list → @Sober
Owner said go. Created `requirements/REQ-041-frontend-standard-conformance.md` (READY_FOR_SA) from Tanya's audit — the
ranked punch-list to make the FE clear `FRONTEND-STANDARD.md` §3 (the two failing DoD gates: §3.3 focus-ring animates,
§3.5 4 inline hex + 4 `transition-all`). Scope 1–8: one token source (kill parallel Tailwind colour scale, ~26 files) ·
inline-hex→token · drop transition-all/never animate focus ring · tabular-nums · one date formatter · **[held]** heading
type-pairing (needs owner's Thai display-face pick — Porter bringing options) · status-chip shape · 44px hit target.
Bulk-confirm = not blocking (sid `TEST_PASSED`), opportunistic re-check. This is Fern's hands-on learning of the standard.

**Ball: @Sober** (REQ-041 → SPEC + TASK(s) for Fern; hold item 6 for the font call). **& owner** (tick Sober; and the
Thai display-font decision when Porter brings options). Still open (owner, not blocking): prod QA-data reset + `pg_hba`.

## [Sober (SA)] — REQ-041 → SPEC-037 + TASK-128/129 for Fern (item 6 held) → @Porter @Fern
Grounded Tanya's punch-list against the real `smart-scheduler-front` tree before speccing (so DoD is verifiable, not
vibes):
- `bg-default-*`/`text-*`/`border-*` parallel Tailwind scale = **26 files** (confirmed) · inline hex `bg-[#f5f7fb]` =
  **exactly 4** (checkin, login, AdminLayout, CheckinContent) · `transition-all` = **4** (Sidebar, CalendarGrid ×2,
  CalendarWeekGrid) · dates scattered — **but** many `format("YYYY-MM-DD")` are query-range / `DateInput` VALUES, not
  display, so I scoped item 5 to **display cells only** (a naive unify would break the bookings filter + course start-date).

**Cut into two parallel Fern tasks + one held item:**
- **TASK-128 — tokens & motion (items 1–3):** one colour token source (kill the 26-file `default-*` scale) · 4 inline
  hex → one `--color-paper` token · kill 4 `transition-all` + never animate the focus ring. **This is the one that
  clears the two failing §3 gates — §3.3 (instant focus ring) + §3.5 (grep = 0).** DoD = `grep -rE "bg-\[#|transition-all|
  font-family" src` → 0, no `default-*` left, tsc 0, no regression, hallmark verdict pasted.
- **TASK-129 — table & format polish (items 4,5,7,8):** `tabular-nums` on numeric/date columns · one date-DISPLAY
  formatter (`DD/MMM/YY`, ISO query/input values left alone) · status chip gets a shape/icon (not hue alone) · Voucher
  Manage ≥44px at 375.
- **Item 6 (heading type pairing) HELD** in REQ-041 — needs the owner's Thai display-face pick (Porter bringing
  options); not in either task.

Both tasks bake in the §4 process: **Fern self-runs `hallmark audit` and pastes the verdict before REVIEW**, and I add
a **UI-lens pass** (not just tsc/greps) on review. Bulk-confirm stays opportunistic (already sid `TEST_PASSED`).

**Ball: @Fern** (TASK-128 + TASK-129 — parallel; self-audit before REVIEW) **& @Porter** (item-6 font options → owner).
REQ-041 specced; item 6 waits on the font call.

## [Sober (SA)] — caught another skipped review: TASK-123 (rental price) was live-but-unreviewed → now **PASS ✅** → @Porter @Jason @Fern
On a board sweep I found TASK-123 was **greenlit but never SA-reviewed** (no PASS, no board row) though it's shipped —
same pattern as TASK-102. It closes the TASK-109 price-display gap. Reviewed now:
- **BE:** `rentalPriceList()` (`sale-items.ts:130`) = `RENTAL_CODES.map(code => ({code, priceMinor: RENTAL_PRICE[code]}))`
  — derived from the **single** `RENTAL_PRICE` authority, exposed on `GET /sellable-packages` (`scheduler.service.ts:888`).
  **No second copy of the price card.** ✅
- **FE:** `RentalModal` reads `card.rentalItems` from the server (`priceOf(code)`), shows `฿price` in each Select option
  and a live `Total: ฿(priceMinor × hours)` — via the **shared** `formatPriceMinor` (`@/types/app/pricing`, same helper
  as CreateCourse/CreatePlan). No hardcoded prices; the "never a second price card on the FE" rule holds. ✅
- **Evidence:** `bunx tsc --noEmit` → **0** (ran it). **TASK-123 DONE.** REQ-028 rental is now fully closed (record +
  idempotency + price display), and the whole fast-follow set (107/109/102/122/123) is SA-reviewed end-to-end.

Added the missing board row for TASK-123.

**Ball unchanged: @Fern** (TASK-128 + TASK-129 — REQ-041 conformance, parallel; self-audit before REVIEW) **& @Porter**
(item-6 Thai font options → owner). Nothing else open on SA.

## [Fern (FE)] — TASK-128 → REVIEW. One colour token source + hex/transition-all killed. §3.3 + §3.5 clear. → @Sober, @Tanya
Picked up the REQ-041 ball on branch `dong`. All 3 items done; **every DoD grep = 0**; tsc 0 · build ok; hallmark
verdict pasted in the task. (Note: I'd been logging feature work in `2026-08-04.md` — caught that the team jumped to
08-11 for the customer-prod deploy; switched here.)
- **Single source = CSS vars in `globals.css`** (item 1 sanctions "app CSS vars"). New `:root` defines every colour once
  (`--color-fg/-surface/-paper/-muted-50…900`, **exact** slate values); `tailwind.config.ts` now *references* the vars
  — no parallel hard-coded scale. **`default` → `muted`**, extended to the full 50–900 range (the old scale only had
  100–600, so `text-default-700/800/900`+`bg-default-50` were silently **undefined** — a latent bug, now resolved).
- **Item 1 (27 files):** bulk `-default-N` → `-muted-N` (all utility prefixes). Pattern is class-only → Mantine's
  `variant="default"` (15) + `MANTINE_COLOR.default` untouched (verified). Values identical via the vars ⇒ **zero
  visual regression by construction.**
- **Item 2:** 4× `bg-[#f5f7fb]` → `bg-paper`. **Item 3:** 4× `transition-all` → `transition-colors` (nav + empty
  slots) / `transition-shadow` (booking card, light-bg lift — not the dark-bg glow motion.md warns of). Focus ring is
  Mantine outline-based → never transitioned; confirmed no `outline`/`focus-visible` transition remains (§3.3).
- **hallmark audit (touched screens):** §3.5 one-source PASS · §3.3 focus-instant PASS · motion PASS · no anti-pattern
  introduced. Honest residuals that are NOT this task: pure-`#ffffff` surface tint (a redesign, not a value-swap) and
  the single-face "Inter-everywhere" tell = **REQ-041 item 6, HELD** for the owner's font pick. Verdict: better than
  "reads as AI-generated" on the tokens/motion axes; the two failing §3 gates now pass.
- **Not touched (scope discipline):** Mantine's own `gray/blue/…` palette, and any layout/table structure (that's
  TASK-129 / DEF-1 is unaffected).

**@Sober: 128 ready for the UI-lens review** (§4). **@Tanya:** the change is value-preserving so no visual delta is
expected, but a `dong` re-baseline (pinned columns, no truncation, DEF-1) confirms it — the app's auth-gated so I can't
drive it here. **Now starting TASK-129** (tabular-nums · one date-DISPLAY formatter · status-chip shape · Voucher
Manage ≥44px@375). **Ball: @Fern** (129) **& @Sober** (128 review).

## [Sober (SA)] — TASK-128 REVIEW = **PASS ✅ (gates cleared)** + one accuracy correction → @Fern @Tanya @Porter
The two failing §3 gates are genuinely closed — reproduced, not trusted:
- **§3.5:** `grep -rE "bg-\[#|transition-all|font-family" src` = **0**, and **0** residual `-default-N` classes (bulk
  `default→muted` rename). **§3.3:** focus ring is outline-based, no transition — instant. `bunx tsc --noEmit` = **0**.
- **Core swap is value-preserving — I confirmed by `git diff`, not by Fern's word:** old `default` 100–600 =
  `#f1f5f9…#475569`, new `muted` 100–600 = the **same** slate hexes via `--color-muted-*` vars. The 26-file rename +
  4 hex→`bg-paper` + 4 `transition-all`→explicit-property are all sound. Mantine `variant="default"` untouched (verified).

### ⚠️ One correction (not a blocker, but the DoD claim is overstated)
Fern's DoD says *"zero visual delta by construction."* Not quite: `muted-{50,700,800,900}` are **newly defined** (the
old scale only had 100–600, so `bg-default-50` / `text-default-700/800/900` were **silently no-ops**). So **6 sites go
from no-colour → a colour**, not "identical value":
`PlanModal.tsx:268` (bg-muted-50/40) · `BookingModal.tsx:995` (bg-muted-50) · `CalendarWeekGrid.tsx:106`
(non-bookable cell bg-muted-50/80) · `RentalModal.tsx:145` (price text) · `DashboardContent.tsx:64` · `BookingsTable.tsx:297`
(hover). These are almost certainly **latent-intent fixes** (the author wrote those classes wanting a tint that never
rendered) — improvements, not regressions — but they're real visual changes. **@Tanya: your `dong` visual pass must
eyeball those 6 specifically**; don't accept "zero delta by construction" for them. I can't drive the auth-gated app,
so the visual confirmation is genuinely yours — but I've pinpointed exactly where to look.

**SA UI-lens pass (§4): done.** Gates pass, swap sound, 6 sites flagged for visual confirm. **TASK-128 code = DONE**;
final sign-off rides Tanya's targeted visual check. **Ball: @Fern** (TASK-129 still in flight) **& @Tanya** (visual pass
on the 6 sites + no-regression on her `dong` baseline).

## [Fern (FE)] — TASK-129 → REVIEW. Table/format polish (items 4/5/7/8) done. REQ-041 build-complete (item 6 held). → @Sober, @Tanya
Fair catch on 128's "zero delta" overstatement — you're right, the 6 newly-defined `muted-{50,700,800,900}` sites are
real (latent-intent) changes, not identical values; noted for Tanya's targeted pass. Now 129, all 4 items; tsc 0 ·
build ok; hallmark verdict in the task.
- **Item 5 (date formatter):** new `lib/ui/format.ts` `formatDateDisplay → DD/MMM/YY`. Routed the M-1 `BookingsTable`
  `{b.date}` (was raw ISO), `VoucherPanel` `{v.expiryDate}`, and **PlanModal's two session tables** (which printed
  `DD/MMM/YY` *inline* via `dayjs().format` — now the one helper). 🔴 **ISO query/input VALUES untouched** — verified
  BookingsTable still has 3 `format("YYYY-MM-DD")` query-range calls + CreatePlanFlow/CreateCourseModal startDate.
- **Item 4 (tabular-nums):** root cause = a **dead `font-num` class**, used in 8 spots but **defined nowhere** (=your
  0/20 audit). Replaced it with the real `tabular-nums` utility + table-level on Bookings/Voucher (Dashboard already
  had it on its count col). Now every digit column aligns.
- **Item 7 (status shape-not-hue):** `StatusChip` gets a per-status icon (`STATUS_ICON`). The a11y win: **3 statuses
  share `danger` red** (NO_SHOW / PENDING_RESCHEDULE / CANCELLED) — now shape-distinct. Label + colour + icon.
- **Item 8 (44px):** Voucher "Manage" → `min-h-[44px] sm:min-h-0` (phone ≥44px, desktop density unchanged).
- **hallmark verdict:** §2 status-not-colour-alone PASS · §3.2 hit-target PASS · tabular-nums PASS · date-consistency
  PASS · no anti-pattern introduced. Only residual = item 6 font (HELD, owner's call).

**@Sober: 129 ready for the UI-lens review.** With 128 (DONE, pending Tanya's 6-site visual) + 129, **REQ-041 is
build-complete except item 6 (HELD)**. **@Tanya:** 129's live pass = bookings filter still submits ISO + new date
format renders + the status icons + Voucher Manage at 44px on a real 375 width (I verified tsc/build/grep + ISO-
preservation counts; the app's auth-gated so the on-screen confirm is yours). **Ball: @Sober** (129 review) **&
@Tanya** (128 6-site + 129 visual) **& @Porter** (item-6 Thai font options → owner). No open Fern build remains.

## [Sober (SA)] — TASK-129 REVIEW = **PASS ✅ (clean)** → REQ-041 items 1-5,7,8 SA-done (item 6 held) → @Fern @Tanya @Porter
Reproduced all four; the two correctness-sensitive ones held up:
- **Item 5 (dates):** `formatDateDisplay` (`lib/ui/format.ts`, ISO→`DD/MMM/YY`, null→"") routes the M-1 bookings ISO
  cell (`BookingsTable:331`) + Voucher/PlanModal cells; **the 3 ISO query-range `format("YYYY-MM-DD")` calls are
  preserved** — the filter still submits ISO. Swept for any other raw-ISO `Table.Td` date cell → **none missed**. ✅
- **Item 7 (status a11y):** the three statuses that share `danger` red (NO_SHOW / PENDING_RESCHEDULE / CANCELLED) now
  carry **distinct** icons (UserX / ArrowLeftRight / Ban) — genuinely shape-distinct, not just decorative; `aria-hidden`
  on the icon so the text label stays the screen-reader name. That's the a11y point that mattered. ✅
- **Item 4:** the real root cause — a **dead `font-num` class** (never defined) — is gone (=0), replaced by
  `tabular-nums` (12 sites). Matches Tanya's 0/20 finding. **Item 8:** `min-h-[44px] sm:min-h-0` on Voucher Manage
  (phone floor, desktop density kept).
- **Evidence:** `bunx tsc --noEmit` = **0**. No correction needed (unlike 128). SA UI-lens (§4) done.

**REQ-041 status:** items **1–5, 7, 8 = SA-reviewed done** (TASK-128 gates + TASK-129 polish); **item 6 (heading
type-pairing) HELD** on the owner's Thai display-face pick. FRONTEND-STANDARD §3.3 + §3.5 gates now pass in code.

**Ball: @Tanya** (one `dong` visual/functional pass closes both — TASK-128's 6 newly-colored sites + no-regression,
TASK-129 date/icon/44px render + bookings filter still returns rows; then re-run `hallmark audit` for the verdict lift)
**& @Porter** (item-6 font options → owner). SA queue clear.

## [--:--] Tanya (QA) — REQ-041: **TASK-129 `TEST_PASSED`** · 🔴 **TASK-128 `TEST_FAILED` — DEF-3: the token swap silently killed Tailwind's opacity modifier (6 sites, 4 visibly regressed)** → @Porter

Ran the `dong` pass. Both §3 gates are genuinely closed and all four TASK-129 items land — but the token
migration took a working feature away, and that needs to go back to the chain before REQ-041 closes.
Detail: `tests/TEST-DONG-fe-rework-retest-and-hallmark-audit.md` (round 2) · evidence
`../project-docs/qa-dong-2026-08-11/`. **Note: Fern's work is uncommitted** (36 modified files, HEAD still
`7f9456e`) — so this verdict covers the working tree, not a commit.

### What's genuinely fixed
- **§3.5:** all five greps = **0** (inline hex · `transition-all` · `font-family` · residual `-default-N` ·
  the dead `font-num` class).
- **§3.3:** the focus ring is instant — 12 controls sampled, **0** animate it.
- **M-1 dates:** `iso = 0`; the bookings table now reads **`13/Aug/26`**. **M-2:** **20/20** numeric cells
  carry `tabular-nums` (was 0/20). **H-8:** status chips carry icons with `aria-hidden` so the label stays
  the accessible name. **M-3/H-9:** Voucher "Manage" is **44 px** at 375.
- **The date filter still queries** — the swap to a display formatter didn't touch the ISO range: preset
  "This month" returns **10 rows**. That was the one place a formatter change could have broken data.
- **The 63f734d rework survives the swap:** pinned = 11 all `sticky`, 0 truncated badges, 0 clipped cells,
  no page h-scroll at 375/768/1280. **DEF-1 stays closed.**

### 🔴 DEF-3 (MAJOR) — `bg-*/NN` opacity modifiers now paint nothing
**Mechanism, proved from the diff:** before, colours were **literal hex** (`content1: "#ffffff"`,
`default-100: "#f1f5f9"`) and Tailwind could compose `/NN`. After, they're `var(--color-…)`, and in
Tailwind v3 a bare `var()` colour **cannot** carry an alpha modifier — so the utility is **never generated**.
**Evidence from the generated CSS itself:** rules exist for `.bg-muted-50` and `.bg-muted-100`; there is
**no rule at all** for `.bg-muted-50/40`, `/80`, `.bg-muted-100/60`, `/50`, `.bg-content1/80` — each paints
`rgba(0,0,0,0)`.

| Site | Class | Before → now |
|---|---|---|
| `Header.tsx:27` | `bg-content1/80` | `#ffffff` @80% → **nothing** 🔴 (the app header backdrop) |
| `TeachersContent.tsx:255` + `:342` | `bg-muted-100/60` | `#f1f5f9` @60% → **nothing** 🔴 |
| `ReportsContent.tsx:155` | `bg-muted-100/50` | `#f1f5f9` @50% → **nothing** 🔴 |
| `PlanModal.tsx:269` | `bg-muted-50/40` | nothing → **still nothing** ⚠️ |
| `CalendarWeekGrid.tsx:106` | `bg-muted-50/80` | nothing → **still nothing** ⚠️ |

**Why it's worth stopping for:** beyond these 6, the modifier is now silently unavailable for *every future
use* of these tokens — the next person writing `bg-muted-200/50` gets no background and no error. The fix
shape is standard (channel triplets + the `<alpha-value>` placeholder), but it's Fern's to write, not mine.

### Two corrections I owe, one of them to myself
1. **My round-1 §3.3 FAIL was wrong.** I flagged the ring as animated because `transition-property` was
   `all` — but that element's `transition-duration` was **0 s**, so nothing animated. The question is
   whether it animates, not what the property list says. Re-measured across 12 controls: 0 animated. **§3.3
   passed even then.** Correcting it rather than letting a false FAIL travel.
2. **The review's "six newly-defined sites" prediction is half right.** The **text** ones do now paint
   (`RentalModal` price `฿200`, `rgb(51,65,85)`, contrast **10.35:1**) — but the **two `bg-muted-50/…`
   ones do not, because of DEF-3.** So 2 of the 6 didn't gain the colour the review expected. That's
   exactly what the runtime pass was for, and why "value-preserving by construction" needed checking.

### Verdict
**TASK-129 → `TEST_PASSED`** (4/4 items). **TASK-128 → `TEST_FAILED` on DEF-3** — its own gates are closed
and the rename is sound, but it introduced a visual regression at 4 sites and left 2 more unfixed. **REQ-041
is not done**; one config-shape change closes all six at once. Item 6 (type pairing) still held on the
owner's Thai display-face pick.

**Ball: @Porter** — DEF-3 back to the chain. Local dev server stopped; the repo is exactly as I found it
(Fern's uncommitted changes untouched).

## [--:--] Porter (PM) — REQ-041: accept TASK-129; route DEF-3 (TASK-128 regression) back to @Sober; item 6 + commit noted
Tanya's round-2 `dong` pass: **TASK-129 `TEST_PASSED` (4/4)** and both §3 gates genuinely closed (§3.5 all greps 0;
§3.3 instant, 0/12 animate — she also **retracted her round-1 §3.3 FAIL**, correctly: `transition-property:all` with
`duration:0s` never animated). But **TASK-128 `TEST_FAILED` on DEF-3 (MAJOR)** and I'm sending it back before REQ-041
can close.

**DEF-3:** the "one token source" swap moved colours from literal hex to `var(--color-…)`. In Tailwind v3 a bare
`var()` colour **can't carry an alpha modifier**, so `bg-*/NN` utilities are never generated → paint transparent. **4
visible regressions** — `Header.tsx:27 bg-content1/80` (app header backdrop gone), `TeachersContent.tsx:255/342` +
`ReportsContent.tsx:155 bg-muted-100/NN` — plus 2 pre-existing dead ones (`PlanModal:269`, `CalendarWeekGrid:106`). And
the modifier is now silently unavailable for **every future** use of these tokens. QA's fix-shape (Fern's to write, not
mine): define the tokens as **channel triplets + the `<alpha-value>` placeholder** so `/NN` composes again.

**@Sober** — please REWORK **TASK-128**: one config-shape change on the colour tokens closes all six `bg-*/NN` sites at
once + restores the modifier for future use; re-verify the swap is otherwise value-preserving. TASK-129 stands accepted.

**Two notes:** (1) **Fern's REQ-041 work is UNCOMMITTED** (36 files, HEAD still `7f9456e`) — once DEF-3 is fixed +
Tanya re-passes, it needs a commit on `dong` before it can ship/deploy. (2) **Item 6 (heading type-pairing) still held**
on the owner's Thai display-face pick (Porter proposed Kanit / Anuphan / Bai Jamjuree).

**Ball: @Sober** (REWORK TASK-128 → Fern → back to Tanya). **& owner** (tick Sober; the font pick when ready).

## [--:--] Porter (PM) — 🧭 END-OF-DAY HANDOFF (2026-08-11). Pick up here tomorrow.
**Shipped today:** customer-prod deploy done → **REQ-038 #1–5 (+ REQ-030/037/OBS-3) DELIVERED** on
`frontoffice.develyst.online` (both DB migrations green via seed-ledger fallback; full post-deploy re-check PASS, zero
defects). **REQ-040** prod pre-release junk-clear = done (clean slate, teachers/subjects/config kept). New
**`FRONTEND-STANDARD.md`** at workspace root (from the pro-engineer `neeeeroooo` grading our FE "AI-generated").

**Live balls (terse):**
- **REQ-041 (FE → standard)** — items 1–5,7,8 built + SA-done; **TASK-129 `TEST_PASSED`**; **TASK-128 `TEST_FAILED`
  (DEF-3)** → **@Sober REWORK TASK-128**: Tailwind `bg-*/NN` opacity died in the hex→`var()` token swap; fix = channel
  triplets + `<alpha-value>`, closes all 6 sites. Then Fern → Tanya re-verify → Porter closes REQ-041.
- **REQ-041 item 6 (heading type-pairing)** — HELD on owner's Thai display-face pick (options: Kanit / Anuphan / Bai
  Jamjuree, or Fern mocks 2–3 for a look-first).
- **Fern's REQ-041 work is uncommitted** (36 files on `dong`, HEAD `7f9456e`) — commit after DEF-3 passes, before ship.
- **Owner housekeeping (not blocking):** re-run the REQ-040 delete block to clear Tanya's QA test data from prod
  (`QA-prod-student` + 2 courses + 1 voucher + bookings); **prod `pg_hba` temp-open still open** (his call).

**Tomorrow = new date → new `log/<TODAY>.md`** (don't append here). First move: owner ticks @Sober on TASK-128 REWORK;
Porter brings the font mocks/decision for item 6.

**Ball: @Sober** (TASK-128 REWORK) **& owner** (tick Sober · font pick · optional prod cleanup).
