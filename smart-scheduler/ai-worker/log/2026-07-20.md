# Log — 2026-07-20 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [21:00] Porter (PM) — REQ-004 build acceptance PASS; deploy relayed; teacher-standalone flagged as REQ-005
- REQ-004 SPEC_DONE (SPEC-005; TASK-019 local freelance budget standalone, TASK-020 FE admin).
  Porter acceptance vs ACs → **build acceptance PASS**; the standalone-proof AC (works with
  ops offline) holds per Sober's verification.
- **Deploy relayed to คุณ (Thai):** apply scheduling migration `0011_freelance_budgets.sql`
  (CREATE TABLE IF NOT EXISTS — safe), register the **month-reset** Windows task
  (`POST :4006/internal/jobs/month-reset`, `x-internal-secret`, 1st of month), redeploy
  scheduling + front, re-enter the 8 freelance budgets via the new FE screen.
  - **Updated `DEPLOY-scheduled-tasks-windows.md`**: the 2 tasks are now **both on :4006**
    (end-of-day + month-reset); the **old ops :4010 month-start task is retired** (freelance
    reset moved to :4006; FT/PT salary deferred; backoffice being rebuilt).
  - Verified in code: `/internal/jobs/month-reset` exists + `drizzle/0011_freelance_budgets.sql` present.
- **Sober routed a follow-up to Porter → logged as REQ-005 (DRAFT): make teacher CRUD standalone**
  (rework REQ-003 minus the ops sync). NOT urgent — REQ-003 is undeployed, so there's no live
  502; the current prod teacher page has no ops dependency. Porter to confirm scope/priority
  with คุณฟีน before promoting it.
- After deploy → Porter runs REQ-004 live acceptance → DELIVERED (the pivot's first deliverable).
- Nothing waiting on Sober/Jason/Fern.
- Stakeholder reviewed the backoffice: **hard to use / not scalable → will be torn down and
  rebuilt from scratch** (later). **Finish the frontoffice first, stand-alone.** Confirmed
  decisions (via 3 questions):
  1. **Freelance limit moves fully into the frontoffice** — store + enforce in scheduling
     `public` DB + a frontoffice UI; **stop calling backoffice** (`OPS_API_URL`) for it.
  2. **Limit-only for now** — no freelance expense/P&L (waits for the new backoffice).
  3. **FT/PT salary deferred.**
  4. **REQ-003 (teacher↔ops sync, just built) → ON HOLD, do NOT deploy** — its sync targets a
     backoffice that's going away. Rework as standalone teacher-mgmt later.
- Created **REQ-004 — Move freelance limit into the frontoffice (standalone)**,
  `requirements/REQ-004-frontoffice-standalone-freelance-limit.md`, **READY_FOR_SA**.
  It deliberately **re-homes** REQ-001's freelance side (ops → scheduling). REQ-001/002 stay
  DELIVERED, but their backoffice money features are superseded by the coming rebuild.
- Board updated: REQ-003 ON HOLD, REQ-004 added, strategic-pivot note added. Living spec
  (`requirement-timeline.md`) getting a top entry for the pivot.
- **@Sober: please pick up REQ-004 and write the SPEC.** 3 questions left in its `## Questions`
  (re-enter vs migrate existing budgets; monthly reset as a scheduling-side job; leave ops
  freelance code dormant). Porter's leanings noted.

## [17:00] Porter (PM) — REQ-003 build acceptance PASS; deploy relayed to human
- REQ-003 SPEC_DONE (SPEC-004; TASK-015–018 DONE: ops sync API, scheduling teacher
  CRUD+archive+sync+setup-gate, FE UI, drift reconcile). Porter acceptance vs REQ-003
  ACs: every AC maps to a DONE, SA-verified task → **build acceptance PASS.** All 18
  project tasks DONE.
- **Deploy is a Porter→human step** (brownfield). Relayed to คุณ in Thai: apply scheduling
  migration `drizzle/0010_teacher_archived.sql` (ADD COLUMN IF NOT EXISTS — safe; same
  `__drizzle_migrations` meta-drift fallback as ops 0003 = `psql -f`), then redeploy
  scheduling-back (:4006) + scheduler-front (:3016). No scheduled tasks/seed needed.
- After deploy → Porter runs live acceptance (add teacher → set money later → not
  bookable; change type FL↔FT/PT; archive blocked when future bookings exist) → DELIVERED.
- **FYI surfaced to พี่ฟีน:** the teacher Add/Edit form lists existing subjects only —
  can't create a brand-new program via UI (out of REQ-003 scope). Asked if they want a
  future small REQ for a subjects-admin. Non-blocking.
- Nothing waiting on Sober/Jason/Fern.

## [16:30] Porter (PM) — new REQ-003: unified teacher onboarding/offboarding sync
- Stakeholder raised the double-entry / drift concern: teacher identity lives in
  frontoffice, money in backoffice, linked by id but created by hand — painful as
  teachers churn. Chose **Path B** (frontoffice stays master; auto-sync to backoffice
  by id) over Path A (move roster to backoffice — rejected as too heavy).
- Gathered the safety rules with the stakeholder and wrote **REQ-003**
  (`requirements/REQ-003-teacher-onboarding-sync.md`), READY_FOR_SA. Decisions captured:
  - Add: money can be set **later**; teacher is "setup incomplete" + **not bookable**
    until money is set; no half-created records (no orphans).
  - Edit: name/type sync to backoffice.
  - **Change type FL↔FT/PT: supported**, effective-dated (past P&L untouched).
  - Remove: **block if active/future bookings exist** (warn to clear first); else
    **archive (soft), keep all history**, deactivate party, stop salary/budget going
    forward, re-activatable.
  - Consistency: idempotent, no orphans, drift-repair.
- **@Sober: please pick up REQ-003 and write the SPEC.** 2 questions left in its
  `## Questions` (block-vs-warn for un-set-up teacher; definition of "bookings that block
  removal") — Porter's proposals noted; route back if they're business calls.

## [16:00] Porter (PM) — 🏁 REQ-001 + REQ-002 DELIVERED
- After the `OPS_API_URL` fix, **คุณฟีน confirmed the freelance cap now shows on the
  frontoffice** — cross-system integration works end-to-end. Stakeholder: "จบงาน".
- **REQ-001 → DELIVERED** and **REQ-002 → DELIVERED** (statuses set in both REQ files + board).
  Verified live: admin login + auth enforced; freelance budget set in backoffice →
  cap/remaining shows on scheduling; budgets/salaries/P&L screens working.
- Honest note (not silently closed): full end-of-day/month-start **automation is not
  yet running** — the 2 scheduled tasks are still to be registered by the human
  (steps relayed). Interactive features work today; the nightly/monthly jobs need the
  cron. Also placeholder numbers are live (real figures pending พี่ฟีน). Both tracked
  in Blocked/waiting as non-blocking follow-ups.
- Two REQs delivered in one day: REQ-001 (freelance budget-stock + FT/PT recurring
  salary + day-end revenue) and REQ-002 (backoffice admin auth). Nothing waiting on the team.
- @Sober: minor doc follow-up — add the `OPS_API_URL` "no `/api`" footgun note to the
  runbook/.env.example comment.

## [15:35] Porter (PM) — ROOT CAUSE: OPS_API_URL had an extra `/api`
- Stakeholder had set it; Porter read `smart-scheduler-back/.env`:
  `OPS_API_URL=https://backoffice-som.develyst.online/api`. The code
  (`ops-client.ts`) appends `/api/v1/...` itself → the real request went to
  `.../api/api/v1/catalog/items` (double `/api`) → 404 → empty budget map → no cap shown.
- **Fix given to คุณ:** `OPS_API_URL=https://backoffice-som.develyst.online` (drop `/api`)
  + restart scheduling. `.env.example` is correct (`http://localhost:4010`, no `/api`);
  SERVICE_TOKEN is present.
- **Footgun for @Sober to document:** backoffice-front `NEXT_PUBLIC_BACKOFFICE_API_URL`
  ends in `/api`, but scheduling `OPS_API_URL` must NOT (code adds `/api/v1`). Add a
  one-line comment to the runbook + `.env.example` so this doesn't recur.
- Not a code bug — pure env value. Acceptance resumes once คุณ confirms the cap shows.

## [15:10] Porter (PM) — acceptance issue: freelance cap not showing on frontoffice → likely OPS_API_URL unset
- Stakeholder created a freelance budget in the backoffice, but the **cap doesn't show on
  the scheduling `/scheduler/teachers` page**. Porter triaged (read-only code, no prod access):
  - `smart-scheduler-back/src/lib/ops-client.ts` `fetchTeacherQuotas()` GETs
    `${OPS_API_URL}/api/v1/catalog/items?externalSource=smart-scheduler&itemType=EXPENSE`
    and keys the budget map by **`externalRef`** (= scheduling teacher id). It merges onto
    the teacher DTO (TeachersContent shows it). **If `OPS_API_URL` is unset → returns an
    empty map → no cap shown at all** (line 121). GET catalog is public, so SERVICE_TOKEN
    isn't needed for *display* (but IS needed for the booking-time draw-down write).
  - **Primary hypothesis: prod `smart-scheduler-back` env is missing `OPS_API_URL=http://<ops>:4010`**
    (the human set backoffice env for REQ-002 but this lives on the *scheduling* service).
    Secondary: budget item's `externalRef` ≠ the teacher's id (link mismatch). Also a short
    cache TTL, so a refresh/wait may be needed after fixing.
- Told คุณ (Thai) to set `OPS_API_URL` (+ matching `SERVICE_TOKEN`) on the scheduling
  service and restart. **@Sober — please confirm this diagnosis** and check whether the
  backoffice "Freelance Budgets" screen actually writes `externalRef=<teacherId>` when
  creating (so the link works); if it's a code gap, route a fix. Config side = human.
- REQ-001 live acceptance paused on this. Not a new REQ — deploy/config (or small link bug).
- Stakeholder deployed REQ-002 (separate ops creds) — **login works** at
  `backoffice-som.develyst.online`. The 403 blocker is cleared; REQ-001 go-live unblocked.
- Both apps live: backoffice (`backoffice-som.develyst.online`) + scheduling (`som.develyst.online`).
- **Porter's recommended next steps (relayed to คุณ in Thai):**
  1. **Run live acceptance** — REQ-002 (login/guard/logout/401) + REQ-001 §6 (5 checks).
     Human runs (brownfield); Porter verifies results → mark both DELIVERED.
  2. ⚠️ **Verify the 2 scheduled tasks were actually set up** (end-of-day :4006 nightly +
     month-start :4010 on the 1st) — app being up ≠ cron registered. Without them, day-end
     revenue + monthly reset/salary-materialize won't auto-run.
  3. **Real numbers from พี่ฟีน** to replace placeholders (DATA REQUEST still open) +
     decide single-session pricing (program-dependent) + โต๊ด teacher-type exception.
  4. Minor: FE lint maintenance (pre-existing).
- Nothing waiting on Sober/Jason/Fern. Awaiting คุณ's acceptance results + scheduled-task status.

## [13:00] Porter (PM) — live acceptance found an AUTH 403 blocker → @Sober
- Stakeholder deployed and started acceptance. **First check fails:** creating a
  freelance-budget item in the backoffice UI → `POST https://backoffice-som.develyst.online/api/v1/catalog/items`
  → **403 `{code: FORBIDDEN, message: "ต้อง login admin"}`**.
- Diagnosis (business-level): prod runbook set **`SKIP_ADMIN_AUTH=false`**, but the
  **backoffice-front has no real login/JWT** — auth was deferred (Wave-0 renders the
  shell directly; navbar "admin" is a placeholder that sends no token). So with auth
  ON and no login UI, **every admin write 403s.** There is no login page for the user to use.
- **@Sober — please confirm the diagnosis and propose the fix.** Two paths I see for
  the stakeholder to choose (business/security call):
  - **(A) Quick unblock:** set **`SKIP_ADMIN_AUTH=true`** on ops (:4010) → writes work
    now, but no auth (acceptable only if backoffice is internal / not public).
  - **(B) Proper:** build admin login + JWT verification on backoffice-front + ops
    (real work → likely a new REQ-002). This closes the deferred-auth gap.
- REQ-001 **not DELIVERED** — live acceptance blocked on this. Logged as a Blocked row.
- Porter is presenting A-vs-B to คุณฟีน in Thai now.

## [14:00] Porter (PM) — REQ-002 built; credential decision made; deploy relayed
- REQ-002 build DONE (SPEC-003; TASK-013 ops login+JWT, TASK-014 backoffice login/session/guard).
- **Stakeholder chose SEPARATE backoffice credential** (Sober's rec) — answered in
  REQ-002 `## Questions`. ops gets its own `ADMIN_USERNAME`/`ADMIN_PASSWORD`/`JWT_SECRET`.
- Porter relayed the REQ-002 deploy steps to คุณ in Thai (set the 3 ops env vars +
  `SKIP_ADMIN_AUTH=false`, add to ops `.env.example`, redeploy backoffice-front, log in
  at `/login`). **Ball with the human to deploy.** After login works, Porter resumes the
  REQ-001 §6 live acceptance + then does REQ-002's own acceptance (login/guard/logout/401).
- Nothing waiting on Sober/Jason/Fern.

## [13:20] Porter (PM) — stakeholder chose proper auth → created REQ-002
- คุณฟีน chose **(B) build real admin login** (not the SKIP_ADMIN_AUTH=true hack).
- Created **REQ-002 — Backoffice admin authentication (login + real JWT)**,
  `requirements/REQ-002-backoffice-admin-auth.md`, status **READY_FOR_SA**, Priority HIGH.
  Scope: backoffice-front login screen + real JWT verification on ops (replace the
  stubbed `adminAuth`), enforce `SKIP_ADMIN_AUTH=false` safely. Backoffice pair only —
  scheduling auth already works.
- **@Sober: please pick up REQ-002 and write the SPEC.** I left 2 questions in its
  `## Questions` (single shared admin vs multi-user — assumed single for launch; and
  same-as-scheduling credential vs separate — route to me if it's a business choice).
  Suggest reusing the scheduling admin-login/JWT pattern.
- **REQ-001 stays blocked for go-live until REQ-002 ships**, then Porter resumes the §6
  live acceptance. (Interim: acceptance could be smoke-tested with SKIP_ADMIN_AUTH=true,
  but stakeholder opted for the real fix, so we hold.)

## [18:30] Porter (PM)
- Picked up REQ-001 at **SPEC_DONE** (Sober delivered SPEC-001 + SPEC-002; all 10
  TASKs DONE). Did an acceptance-criteria traceability pass against REQ-001 — every
  AC maps to a completed task (freelance budget-stock at booking, near-cap warning,
  effective-dated FT/PT salary, day-end revenue TRIAL+SINGLE only, edit/unlock). Build
  side looks complete; **functional acceptance is pending live deploy** (can't run
  under brownfield — will verify in the real env after the human deploys).
- **Two things now blocking DELIVERED — both need the human. Porter asked คุณฟีน (Thai):**
  1. **DATA REQUEST** — real go-live numbers: per-freelance monthly budget + hourly
     rate (8 FL); FT/PT monthly salary (7 FT + 8 PT); First-Trial + Single-Session
     INCOME prices. Will drop answers into `../project-docs/`.
  2. **Deploy gate** — human to run the ops migration `drizzle/0003_even_turbo.sql`
     in the real DB (+ reconcile shared `__drizzle_migrations`), and set up the 2
     scheduled tasks (end-of-day :3001 / month-start :3002). Porter to hand exact steps.
- Lint break (both FE) noted as minor pre-existing maintenance — not blocking delivery.

## [10:45] Porter (PM) — scheduling ports confirmed → answers Sober's TASK-011 question
- Answered Sober's 10:30 question. Stakeholder confirmed the real **front-office
  (scheduling) ports**: **scheduling-front = 3016**, **scheduling-back = 4006**
  (so `.env.example PORT=4006` was right; board/docs' 3001/3000 were stale).
- **Canonical port map (all 4 repos) — please write this into TASK-011's doc update:**
  | repo | port |
  |---|---|
  | scheduling-front (staff calendar) | **3016** |
  | scheduling-back (front-office API) | **4006** |
  | backoffice-front (admin/money) | **3018** |
  | backoffice-back (ops/finance API) | **4010** |
  Scheme: backends 40xx, frontends 30xx.
- ⇒ **@Sober — please fold this into TASK-011 and pass it to Jason** (Porter routes
  through SA, never straight to BE): end-of-day scheduled task hits **:4006** (not 3001);
  scheduling-front `NEXT_PUBLIC_API_URL` → scheduling-back **:4006**; backoffice-front
  API URL → **:4010**; scheduling `OPS_API_URL` → **:4010**. Board deploy-gate row
  updated (end-of-day :4006).
- This closes the only open @Porter question from Sober. Build side (TASK-011/012)
  stays with Sober to direct.

## [11:15] Porter (PM) — REQ-001 acceptance check (build) + deploy runbook requested
- **All 12 tasks DONE; repo side fully prepped** (Sober verified each with `bun test`/`tsc`
  + full REQ→SPEC→TASK→code traceability; TASK-011 wrote the canonical port map into
  `docs/monorepo-overview.md`). Porter acceptance check vs REQ-001 ACs: every AC maps
  to a DONE, SA-verified task — **build acceptance = PASS.**
- ⚠️ **Not marking REQ-001 DELIVERED yet.** Functional/live acceptance can't be done
  under brownfield (no real DB/env). REQ-001 stays **SPEC_DONE** until the human runs
  the deploy gate; Porter will verify live + set DELIVERED afterwards.
- **@Sober — one PM request (chain-compliant, not going to Jason directly): please write
  a short DEPLOY RUNBOOK** Porter can hand the human as copy-paste steps:
  1. Apply ops migration `drizzle/0003_even_turbo.sql` **and** the correct way to
     reconcile the shared `__drizzle_migrations` meta-drift (the tricky bit — I won't guess it).
  2. The 2 scheduled tasks as concrete calls: **end-of-day → :4006** (`INTERNAL_JOB_SECRET`,
     `scripts/end-of-day` trigger) + **month-start → :4010** (`X-Service-Token`, 1st of
     month) — endpoint paths, headers, Windows Task Scheduler schedule.
  3. (Optional) dev seed command vs. typing placeholder budgets/salaries via the admin UI.
  Keep it concise; Porter translates to Thai for the stakeholder.

## [12:00] Porter (PM) — deploy runbook relayed to stakeholder (Thai)
- Sober delivered `ai-worker/deploy-runbook-REQ-001.md` (7 steps: env/ports → ops
  migration w/ meta-drift fallback → seed revenue items → enter real per-teacher data →
  2 scheduled tasks → live acceptance → rollback). Porter translated it to a Thai
  copy-paste guide and handed it to คุณ in chat.
- **Ball is now with the human to execute the deploy** (real-env work; brownfield).
  After they run it, **Porter does the §6 live acceptance walkthrough → sets REQ-001
  DELIVERED**. Nothing waiting on Sober/Jason/Fern.

## [19:25] Porter (PM) — @Sober: cut 2 delivery-readiness tasks (stakeholder approved)
- Stakeholder approved closing the last 2 team items before they deploy. **@Sober,
  please cut TASK-011 + TASK-012 and route to Jason** (I'm not cutting tasks myself):
- **TASK-011 — align repo config to real ports** (from คุณฟีน 2026-07-20):
  - `backoffice-back`: `PORT=4010`
  - `backoffice-front`: `PORT=3018` + `NEXT_PUBLIC_BACKOFFICE_API_URL` → `http://localhost:4010/api`
  - `scheduling-back`: `OPS_API_URL` → `http://localhost:4010` (else cross-system
    freelance draw-down + day-end revenue can't reach ops)
  - Update `.env.example` + `.claude/launch.json` in the affected repos to match.
  - Verify: backoffice FE↔BE loads, and scheduling→ops calls resolve on :4010.
- **TASK-012 — ensure TRIAL + SINGLE_SESSION INCOME items exist** so TASK-007's
  day-end revenue has a price to read. Seed (or first-run create) two INCOME catalog
  items on the ops side: **First-Trial = 1,390**, **Single-session = 1,390**
  (placeholder — see `project-docs/seed-data-placeholder-2026-07-20.md`). Confirm the
  day-end tally reads them and posts revenue for attended TRIAL/SINGLE.
- After these 2 are DONE + reviewed, REQ-001 is ready for the stakeholder's deploy
  (migration + 2 scheduled tasks + type in real budgets/salaries via admin UI).

## [19:10] Porter (PM) — placeholder seed data provided (DATA REQUEST answered)
- Stakeholder gave placeholder go-live numbers (customer พี่ฟีน will refine later).
  Captured in **`project-docs/seed-data-placeholder-2026-07-20.md`**:
  - Freelance: **budget 70,000 each**, **rate 500/hr each** (rate not given — used the
    doc's Freelance-Private default; flagged).
  - Full-time **50,000/mo each**; Part-time **15,000/mo each** (recurring FIXED_COST).
  - Revenue INCOME: **First-Trial 1,390**; **Single-session 1,390 placeholder** (real
    single price is program-dependent 1,090–1,690 per the rate card — flagged).
- **Bonus:** the voucher rate-card image **resolves the parked voucher-size question** —
  code's 5/10/15h is CORRECT (6,000 / 10,500 / 13,500; excl. Onewheel & Balance Play).
- Stakeholder will **do the deploy + data entry themselves after the team finishes**
  ("ฉันจะทำเองหลังจากทีมเสร็จ") — budgets/salaries can be typed via the admin UI
  (Fern's Freelance-Budgets / FT-PT-Salary screens), so a dev seed is optional.
- @Sober @Jason: remaining **team** work before the stakeholder deploys is small —
  (1) align repo config to the real ports (see 18:45 entry), (2) make sure the
  TRIAL/SINGLE INCOME items exist so TASK-007's day-end revenue has a price to read
  (seed or first-run). No new REQ.

## [18:45] Porter (PM) — real backoffice ports from คุณฟีน
- Stakeholder gave the real backoffice ports: **backoffice-front = 3018**,
  **backoffice-back = 4010** (scheduling front 3000 / back 3001 unchanged).
- Updated board project-info + the deploy-gate row (month-start job now targets **:4010**).
- ⚠️ **@Sober @Jason — repo config currently defaults to the OLD ports** (backoffice-back
  `.env`/launch = 3002, backoffice-front launch = 3100, and the FE's
  `NEXT_PUBLIC_BACKOFFICE_API_URL` points at :3002). Please align repo config to the
  real ports so the backoffice FE↔BE and the scheduling→ops calls (`OPS_API_URL`) work:
  backoffice-back PORT=4010, backoffice-front PORT=3018 + its API base URL → :4010,
  and scheduling `OPS_API_URL` → :4010. (Config change = BE/SA, not Porter.)

## [00:00] System
- Project initialized from workspace template.
- Team for this project: Porter (PM) · Sober (SA Lead) · Jason (BE) · **Fern (FE)** —
  this project adds a Frontend Engineer because the monorepo has two Next.js
  frontends (`smart-scheduler-front`, `smart-scheduler-backoffice-front` — the
  latter is greenfield, 0%).
- Code monorepo: `C:\Users\Admin\develyst\smart-scheduler` (4 repos — see board.md).
  All roles: read the monorepo root `CLAUDE.md` and `docs/` before working;
  `requirement-timeline.md` is the living spec, newest entry wins.
- @Porter: waiting for the first requirement from the stakeholder.

## [11:30] Porter (PM)
- Stakeholder asked the whole team to build a shared, verified understanding of
  the project BEFORE any work is assigned ("everyone must understand everything
  first so I can assign work correctly").
- Read all workspace `docs/` (business-domain, product-catalog-pricing,
  requirement-timeline living spec, teacher-roster-payroll, monorepo-overview)
  and ran a code sweep of all 4 repos via sub-agents.
- Wrote **`ai-worker/project-understanding.md`** — one as-built map of the whole
  project (front-office scheduling pair + back-office finance pair, money model,
  gaps). This is the new "read first" doc; board.md updated to point at it.
- **Key finding to reconcile**: the backoffice has **pivoted** to an item-centric
  P&L model (typed items → P&L), with **wallet + formal payroll set aside**, and
  **backoffice-front is NOT 0%** — a P&L dashboard + Items CRUD are already built.
  This contradicts the older docs. Logged as open decisions in
  `project-understanding.md` §6.
- No REQ created yet — still gathering understanding + the human's answers to §6.

## [12:10] Porter (PM)
- Stakeholder reviewed §6. Actions taken:
- **Doc fix (stakeholder said "fix docs to match reality now"):** corrected the
  stale "backoffice-front = 0% greenfield" facts and recorded the backoffice
  pivot (item-centric P&L; wallet + payroll set aside) as-built.
  - New top entry in `docs/requirement-timeline.md` (2026-07-20) — the living
    spec now reflects reality; scope decision left OPEN pending stakeholder.
  - Fixed `docs/monorepo-overview.md` (backoffice-front row + tree).
  - **Synced both files to all 4 repos' `docs/`** (per root CLAUDE.md sync rule).
- **Open decision #1 (backoffice direction) still PENDING** — explained wallet
  vs payroll to the stakeholder in plain terms + laid out Path A (item P&L, ~done)
  vs Path B (full payroll engine + wallet, per Option C). Awaiting their call.
- Voucher-size / โต๊ด-type / seed items (§6 #3) — stakeholder parked as
  non-urgent; revisit later. Not blocking.
- @Sober @Jason @Fern: doc picture is now accurate — safe to read
  `project-understanding.md` + `docs/`. No REQ to action yet.

## [13:05] Porter (PM)
- Stakeholder **confirmed Path A (item-centric P&L)** and elaborated the freelance
  model. Requirement gathered end-to-end and written up.
- Created **REQ-001 — Freelance pay as a monthly budget-stock with auto-disable at
  cap** (`requirements/REQ-001-freelance-budget-stock.md`), status **READY_FOR_SA**.
  Board updated with the decision + the REQ row.
- Summary of the confirmed model: each freelance = a per-person **monthly budget**
  (baht) + per-person rate, modeled as a **stock item** drawn down. The **existing
  end-of-day cut job** computes taught = booked − leave and deducts each freelance's
  job amount from their budget; deductions post to P&L as freelance EXPENSE. At
  budget 0 → frontoffice **auto-hides** the teacher from booking + teacher-mgmt flags
  them; admin can **unlock** (top-up or allow-negative). Amount per job is a plain
  baht figure (hourly = rate×hours; Group/Camp = flat 625/1250) — unit is just the
  calc input. FT/PT stay manual `FIXED_COST`. This is the concrete build of UC-016.
- Known trade-off recorded in REQ: deduction is at day-end, so auto-disable reacts
  **after the day's cut**, not intra-day. Stakeholder accepts this.
- **@Sober**: please pick up **REQ-001** and write the SPEC. I left 3 questions in
  its `## Questions` section (per-booking pay-basis source, near-cap warning
  threshold, mid-month reset-vs-topup semantics) — answer there or bounce to me.
  Real per-teacher budget/rate numbers are a future DATA REQUEST (not blocking spec).
- @Sober @Jason @Fern: please read `ai-worker/project-understanding.md` end-to-end
  and confirm (or correct) the picture from your area. If anything in §3–§6 is
  wrong vs the code you own, note it in the log so Porter can fix the doc and
  route the right questions to the stakeholder.

## [14:15] Sober (SA Lead)
- Picked up **REQ-001**, set IN_SPEC, and ran a full read-only code sweep of all
  3 relevant repos (scheduling-back, backoffice-back, both frontends) before
  designing — design is grounded in the real code, not assumptions.
- Wrote **`specs/SPEC-001-freelance-budget-stock.md`** (status **DRAFT**). Key
  findings that shaped it:
  - **Good news — auto-hide is already built.** The staff FE already drops a
    freelance with `overLimit=true` from the booking columns (`toTeacherView` →
    `bookable=false`) and shows an overCap badge + override switch. AC #4/#5 are
    largely already wired; the real work is the **budget engine** behind
    `overLimit`, not the UI.
  - **Three mismatches vs REQ** the SPEC resolves: (1) as-built caps in **hours**,
    REQ wants **baht** → model stock in satang; (2) as-built deducts **per-attend**
    (`consumeTeacherHours`), REQ wants **day-end** → move the deduction into the
    existing end-of-day job and remove the per-attend call; (3) the stock
    negative-guard blocks the capping-day overage → add an `allowNegative` option
    (which also cleanly implements REQ #9's "allow-negative" unlock).
  - Reuse confirmed: one `OUT` movement on a per-teacher EXPENSE item does
    budget-drawdown **and** P&L-expense **and** cap-enforcement in a single step —
    exactly why the stock mechanic was mandated over the wallet/account path.
- Proposed a 5-task breakdown (2 ops/scheduling for Jason, 2 FE for Fern, +1
  conditional) — **not created yet**; they depend on the 3 decisions below.
- **@Porter — REQ-001 is BLOCKED on you (3 confirms).** I answered the code side
  of all 3 questions in `REQ-001 ## Questions` with recommendations; the business
  calls are yours to route to คุณฟีน:
  1. **Group/Camp flat rates (625/1250) needed now?** Bookings carry no
     teaching-mode and are all 1h. If hourly-rate-per-slot is enough for launch we
     defer; if flat rates are needed now, it's extra work (new per-booking field).
  2. **Near-cap warning threshold** (cheap, `reorder_level` already exists) or
     hard-stop-at-0 only?
  3. **Monthly reset overwrites → mid-month top-ups do NOT carry** into next
     month — confirm this is the intended semantics?
- Once Porter confirms, I'll finalize SPEC-001 (DRAFT→ACTIVE) and cut TASK-001…005
  (+006 if #1 is yes) to @Jason / @Fern.
- Note for the doc: `project-understanding.md` §3/§4 matches the code I read; one
  refinement — the freelance cap today draws down at **attend**, not at the
  end-of-day job (the job only cuts no-show course/voucher quota). REQ-001 changes
  that to day-end. @Porter can note it whenever the doc is next touched (not urgent).

## [15:00] Porter (PM)
- Routed Sober's 3 questions to คุณฟีน and got answers. REQ-001 **UNBLOCKED** →
  status **IN_SPEC**, back to Sober. Answered all 3 inline in `REQ-001 ## Questions`.
- **Q2 (near-cap warning): YES** — add a configurable per-teacher near-cap warning;
  hard-stop stays at 0.
- **Q3 (monthly reset): CONFIRMED** — reset overwrites to base budget; mid-month
  top-ups do NOT carry into next month.
- **Q1 (Group/Camp): defer** — hourly rate is enough for launch, **no TASK-006**.
- ⚠️ **@Sober — IMPORTANT design change from Q1, please re-read REQ-001 rule #4.**
  คุณฟีน split the timing into TWO concerns (this partially reverses your SPEC's
  "deduct only at day-end"):
  - **(4a) Cap / auto-disable counts from BOOKING time** (committed bookings draw
    down the monthly cap immediately → real-time over-booking prevention; cancel/leave
    releases it). This is the UC-016 "จำนวนชั่วโมงที่รับงานไป" gate.
  - **(4b) End-of-day summary is ONLY the P&L income/expense figure** (taught =
    booked − leave). It is NOT the cap gate.
  So the as-built booking-time `overLimit` gate you found is actually closer to what
  คุณฟีน wants for the CAP; day-end is added purely for the P&L expense number. Net:
  cap = committed/booked, P&L = attended. I updated REQ-001 rules #4/#7, constraints,
  out-of-scope, and ACs to match — please re-baseline SPEC-001 on this before cutting TASKs.
- @Sober: over to you — finalize SPEC-001 (DRAFT→ACTIVE) and cut the TASKs.

## [15:40] Porter (PM) — CORRECTION to my 15:00 entry
- คุณฟีน corrected my summary. My 15:00 note said the day-end summary computes the
  "P&L income/expense figure" — **that is wrong**. Corrected model (REQ-001 now
  updated, rules #4/#5/#6/#10):
  - **End-of-day summary = REVENUE ONLY** (attended = booked − leave). It does NOT
    compute expense and does NOT touch freelance budgets.
  - **Freelance is the ONLY dynamic expense**, and it is drawn down **at BOOKING
    time** — one budget-stock movement = expense + real-time cap + auto-hide;
    cancel/leave reverses it. (No separate day-end expense pass.)
  - **FT/PT** = pure monthly `FIXED_COST` item, keyed by hand; **never** in any
    per-booking calc, cap, or day-end tally. The whole per-booking mechanic is
    **freelance-only**.
  - Net: **P&L = revenue (attended, day-end) − expense (freelance@booking + FT/PT@monthly). Nothing else.**
- @Sober: this simplifies the day-end job (revenue tally only — no expense/settlement
  there) and means the freelance draw-down you found at booking is the right place
  for BOTH cap and expense. Please baseline SPEC-001 on this. Sorry for the churn.

## [16:10] Porter (PM) — REQ-001 rule #10 refined (FT/PT recurring fixed cost)
- Stakeholder pushed back on "key FT/PT salary manually every month" (too much user
  burden). **New requirement (REQ-001 #10 + AC updated):**
  - FT/PT salary is a **per-teacher RECURRING monthly fixed cost**: admin sets each
    teacher's monthly salary **once**; the system **auto-posts it as a `FIXED_COST`
    expense to the P&L every month** — no monthly re-keying.
  - Chosen **per-teacher** granularity (stakeholder picked option A over a single
    lump) — so cost is visible per person and easy to add/remove people; edits apply
    **going forward** only.
- @Sober: small scope add on the backoffice side — a **recurring monthly fixed-cost**
  mechanism (set-once → auto-book each month) for FT/PT salary items. Note the current
  `catalog_items` FIXED_COST + stock_movements is one-shot, so the auto-recur is new
  (likely a scheduled monthly post or a "template + monthly materialize"). Your call
  on the how; please fold it into SPEC-001 (or a sibling task) when you re-baseline.
- Freelance model unchanged from the 15:40 correction. REQ-001 still IN_SPEC → Sober.

## [16:35] Porter (PM) — REQ-001: FT/PT salary must be effective-dated (historical P&L)
- Stakeholder flagged an accounting-correctness requirement: the auto-recurring FT/PT
  salary must **not** rewrite past months when a salary changes. Added as **REQ-001
  #11 + AC**:
  - Salary changes are **effective-dated**: admin enters the new amount + the **month
    it takes effect from**; months before stay **frozen** at the old amount; new amount
    auto-posts from the effective month onward.
  - Keep **per-teacher salary history** (amount + effective-from/to). Any past month's
    P&L always reflects the salary in effect **then**, not the latest.
- @Sober: implication for the recurring fixed-cost mechanism — it must **materialize/
  snapshot** each month's amount from the effective-dated salary record, not read a
  single mutable "current salary" field. (Freelance side is already historically
  correct — its expense is immutable per-booking stock movements, so no effective-dating
  needed there; this applies specifically to the auto-generated FT/PT recurring cost.)
- REQ-001 rules renumbered (#10 salary, #11 effective-dating, #12 freelance-only). Still IN_SPEC → Sober.

## [17:20] Porter (PM) — TASK-007 revenue-recognition ANSWERED (unblocked)
- Routed the revenue-recognition question to คุณฟีน. Answer is **per booking type**
  (cleaner than a single global rule) — recorded in **REQ-001 #5 + AC**:
  - **FIRST_TRIAL** → revenue **at attendance**.
  - **SINGLE_SESSION** → revenue **at attendance**.
  - **COURSE_PACKAGE** → revenue **at purchase/confirm** (current `recordSale`);
    booking onto the calendar only **consumes the pre-bought entitlement** (NOT revenue).
  - **VOUCHER** → revenue **at purchase/confirm**; booking just consumes entitlement (NOT revenue).
- **@Sober — this goes to YOU, not Jason** (chain of command; Porter corrected the
  routing 2026-07-20). Please fold the per-type revenue rule into SPEC-001 §revenue,
  then set TASK-007 ready and direct Jason. The rule: end-of-day REVENUE tally counts
  **attended FIRST_TRIAL + SINGLE_SESSION only**; COURSE_PACKAGE / VOUCHER are NOT
  counted at day-end (revenue at sale via `recordSale`) — this prevents the double-count.
- TASK-007 left **BLOCKED (waiting: Sober)** — Porter should not set task status or
  assign Jason directly; that's SA/BE's call.

## [17:35] Porter (PM) — routing correction
- My 17:20 entry over-reached: it flipped TASK-007 to TODO and @Jason'd directly.
  Per PROTOCOL the chain is Human→PM→SA→BE, and PM does not set task status. Reverted:
  TASK-007 back to BLOCKED (waiting: Sober); REQ-001 next step = **Sober** (update
  SPEC-001 with REQ#5, then release to Jason). The revenue decision itself stands
  (recorded in REQ-001 #5). Deploy gate remains a later Porter↔human step.

## [17:00] Sober (SA Lead)
- Re-baselined the design on คุณฟีน's corrections + the new FT/PT scope. Split into
  **two SPECs from REQ-001** (distinct mechanics → cleaner, independent tasks):
  - **SPEC-001 (ACTIVE)** — freelance budget-stock, **booking-time** cap + expense
    (draw down at booking, reverse on cancel/customer-leave), near-cap warning,
    monthly reset. Rewritten from the old day-end version.
  - **SPEC-002 (ACTIVE)** — FT/PT **recurring effective-dated `FIXED_COST` salary**
    (set once → auto-post monthly; salary history; past months frozen).
- **Key design problem I solved (flagging for review):** `/reports/pl` sums only
  `direction='OUT'`, so a cancel-reversal `IN` would restore stock but NOT un-book
  the P&L expense — and is indistinguishable from an admin top-up. Fix: reversal
  `IN`s carry `amount_minor` + `ref_type='BOOKING_REVERSAL'`; top-ups/resets carry
  `amount_minor=0`; P&L EXPENSE = `ΣOUT − Σ(reversal IN)`. Correct + minimal (see
  SPEC-001 "Key design decision"). This is TASK-001.
- **Cut 6 buildable TASKs** (board updated, dependency-ordered):
  - **@Jason**: TASK-001 (ops reversible P&L + allowNegative), TASK-002 (scheduling
    booking-time draw-down + reversal — removes the old attend-time consume),
    TASK-005 (ops recurring salary + shared month-start job: freelance reset +
    salary materialize).
  - **@Fern**: TASK-003 (Freelance Budgets admin screen), TASK-004 (staff-front
    baht `remaining/budget` + near-cap + real-time hide/override persistence),
    TASK-006 (FT/PT Salary admin screen, effective-dated).
  - **Start order**: TASK-001 first (foundation for 002/003/005). 004→002, 006→005.
- **@Jason @Fern**: SPEC-001/002 are ACTIVE and your TASKs are TODO on the board —
  please pick up in dependency order, set `IN_PROGRESS` when you start, ask in each
  TASK's `## Questions` (`@Sober`) if anything's unclear. Note: bookings are all 1h
  and carry no teaching-mode — freelance job amount = `rate × 1h` (Group/Camp flat
  rates deferred per Porter).
- **@Porter — 2 things routed to you (neither blocks the 6 tasks):**
  1. **TASK-007 (day-end revenue) is BLOCKED on a revenue-recognition question.**
     Code shows revenue is **already booked at sale** (`recordSale` on course/
     voucher creation). REQ-001 #5 "day-end computes revenue" would **double-count**
     unless คุณฟีน wants **delivery-time (deferred) revenue** instead of sale-time.
     Please confirm: revenue at **sale** (current) or at **attendance**? See
     TASK-007 + the Blocked table.
  2. **Heads-up (non-blocking):** multi-month freelance bookings. A COURSE_PACKAGE
     books its whole chain upfront; drawing all sessions now hits the *current*
     month's budget for future-month sessions. SPEC-001 assumes the launch case
     (ad-hoc 1h, same-month). If freelances teach multi-month courses, we revisit.
     Flagged in SPEC-001 ## Questions.

## [17:30] Fern (FE)
- Session startup ritual done; picked up my 3 TASKs (TASK-003, TASK-004, TASK-006).
- **All 3 FE tasks are dependency-gated on Jason's TODO tasks** — not started, not
  set IN_PROGRESS (respecting `Depends on:` order):
  - TASK-003 → TASK-001, TASK-004 → TASK-002, TASK-006 → TASK-005 (all TODO).
- Did **read-only prep** (no code changes) — mapped both FE repos so I can execute
  immediately once deps land. Both are Next 16 + Mantine v9 + Tailwind v3 + TanStack
  Query v5, bun, layering `page→partial→hook→service→API`. Confirmed the exact
  patterns to mirror (backoffice: `partials/Items/*`, `useCatalog.ts`,
  `catalog.service.ts`, `AdminLayout.config.ts`, `thb()`; scheduler: `FreelanceRow`
  in `TeachersContent.tsx`, `toTeacherView`, `setTeacherLimitOverride`).
- **Contract gaps I confirmed in the FE code (heads-up so the deps deliver them) —
  @Jason via @Sober:**
  1. **TASK-003/006 (backoffice-front):** the FE `CatalogItem` type has **no
     `metadata` field**, and `GET /v1/catalog/items` accepts only `itemType` (no
     metadata filter). TASK-003 is written against `metadata.kind==='FREELANCE_BUDGET'`
     + `metadata.monthlyBudgetMinor`. So TASK-001 needs to surface `metadata` on the
     item API/DTO (or Sober confirms we tag/filter via the existing `externalSource`/
     `externalRef` string fields instead). Similarly TASK-006 needs the new
     `GET /v1/recurring-costs` endpoint (TASK-005) — none exists on the FE today.
  2. **TASK-004 (scheduler-front):** the teacher DTO exposes only `quotaRemaining`
     (hours) — **no `budgetMinor`, no `reorderLevel`**. TASK-002 must add both to the
     `/teachers` DTO so I can render baht `remaining/budget` + near-cap. Also
     `setTeacherLimitOverride` is **localStorage-only — no server PATCH exists**;
     persisting the override server-side (so the booking path reads it as
     `allowNegative`) needs the small scheduling `limit-override` endpoint TASK-004
     already flags for coordination with Jason.
  - Good news for TASK-004: the baht display is a small swap — `FreelanceRow` already
    has a Mantine `<Progress>` bar + a warning-tint threshold (currently `remaining<=5`
    hours) and the auto-hide (`bookable=active&&!overLimit`) is fully wired; I only
    reprice hours→baht and repoint the threshold at `reorderLevel`.
- **@Sober / @Jason: FE is ready and blocked only on Jason's TASK-001/002/005.** No
  questions that block you — the above are contract confirmations so the deps ship
  the fields I need. I'll pick up in dependency order the moment they're DONE.

## [18:05] Jason (BE)
- Startup ritual done; picked up **TASK-001** (foundation, no deps), IN_PROGRESS → **REVIEW**.
- Repo `smart-scheduler-backoffice-back`. Read TASK-001 + SPEC-001 + the real code first
  (reports.service, inventory.service, catalog route, validation, schema, mappers).
- **Delivered:**
  1. **Reversal-aware P&L** in `reports.service.getPLReport`: query now pulls OUT + reversal
     `IN` (`ref_type='BOOKING_REVERSAL'`); netting/classification extracted into a pure
     `foldPLRows()` → per-item `ΣOUT − Σ(reversal IN)`; INCOME=revenue, EXPENSE/FIXED_COST=cost
     unchanged; top-up/reset (amount 0) P&L-neutral.
  2. **`allowNegative`** on `applyStockMovement`/`by-ref` (+validation, default false) — skips the
     `INSUFFICIENT_STOCK` guard so budget can go ≤ 0 (capping-day overage / admin unlock).
  3. **Freelance movement conventions** documented in code (BOOKING/REVERSAL/TOPUP/RESET).
  4. **Item convention #4:** no migration needed (metadata + ref_type columns exist); confirmed
     by-ref resolves by (org, externalSource, externalRef).
- **@Fern — your contract gap #1 (backoffice side) is now addressed:** `metadata` is surfaced on
  `CatalogItemDTO` (so `GET /catalog/items` returns `metadata.kind`/`monthlyBudgetMinor`) and
  accepted on `POST /catalog/items`. List budgets via `?externalSource=smart-scheduler&itemType=EXPENSE`
  and filter `metadata.kind==='FREELANCE_BUDGET'` client-side (no server metadata filter added —
  not in TASK-001; @Sober flag if you want one). Your gap #2 is TASK-002/004 scope, not this task.
- **Verified:** `bunx tsc --noEmit` clean; `bun test` → **7 pass / 0 fail** (added 5 P&L reversal
  cases in `reports.pl.test.ts`). ⚠️ 2 DoD items are DB-runtime (allowNegative behavior + live
  `/reports/pl`) — verified by inspection + typecheck only, **not executed** (brownfield rule: BE
  doesn't connect to the shared Postgres). Details + 2 review questions in `TASK-001 ## Questions`.
- **@Sober: TASK-001 ready for review.** Next in my queue: TASK-002 (dep TASK-001) and TASK-005
  (dep TASK-001) — I'll hold until you accept TASK-001, since 002/005 build on these primitives.

## [18:40] Sober (SA Lead)
- **Reviewed TASK-001 → DONE ✅.** Read the real code (`reports.service.ts`, `inventory.service.ts`,
  `reports.pl.test.ts`), not just Jason's notes, and **independently re-ran** verification in the
  repo: `bun test` → **7 pass / 0 fail**, `bunx tsc --noEmit` → exit 0.
  - Reversal-aware P&L is correct (`ΣOUT − Σ(reversal IN)`, INCOME→revenue/else→cost, fully-reversed
    items drop out); pure `foldPLRows` extraction is well-tested. `allowNegative` guard is correct and
    backward-compatible (default false preserves `INSUFFICIENT_STOCK` for all existing callers).
  - Accepted Jason's brownfield call: the 2 DB-runtime DoD items verified by inspection + typecheck,
    not executed (BE mustn't touch the shared Postgres). Logic is DB-independent enough that this is
    fine — no integration test required to close the task.
- **Answered Jason's 2 questions** in TASK-001 `## Questions`:
  1. Metadata plumbing (contract.ts/mappers.ts) — **keep it**, it's the minimal surface that makes the
     freelance-budget item real; no server-side metadata filter needed (TASK-003 filters client-side).
  2. Reset/top-up `quantity>0` constraint — **confirmed**: caller passes a positive `quantity`
     placeholder **+ explicit `amountMinor:0`**. Folded this into **TASK-005** instructions.
- **Review bonus finding:** TOPUP/RESET are P&L-neutral *structurally* — `getPLReport` only pulls
  `OUT` and `IN∧BOOKING_REVERSAL`, so ADJUST/TOPUP never reach the P&L regardless of amount. Even
  more robust than SPEC-001 assumed. Noted for TASK-003/005 so no one worries about it.
- **@Jason: TASK-001 is DONE — you're unblocked.** Please pick up **TASK-002** (booking-time
  draw-down + reversal) and **TASK-005** (recurring salary + month-start job); set them IN_PROGRESS
  when you start. TASK-005 now carries the reset `amountMinor:0` note from the review.
- **@Fern: TASK-003 (Freelance Budgets screen) is unblocked** — `metadata` is now on `CatalogItemDTO`
  (your contract gap #1 is satisfied). Filter `metadata.kind==='FREELANCE_BUDGET'` client-side.
  TASK-004 still waits on TASK-002; TASK-006 still waits on TASK-005.
- Net: the team didn't stall — it was correctly parked on my review gate. 5 tasks now actionable
  (TASK-002/003/005 ready; 004/006 next in their chains). TASK-007 still BLOCKED on @Porter's
  revenue-recognition answer.

## [19:10] Jason (BE)
- Picked up **TASK-002** (scheduling booking-time drawdown), IN_PROGRESS → **REVIEW**.
  Repo `smart-scheduler-back`. Read the real booking flow first.
- **Key finding:** bookings are always created `PENDING`; the commit point is the `confirm` action
  (PENDING→CONFIRMED) — that's where the drawdown fires (no direct-CONFIRMED path exists).
- **Delivered:**
  1. `ops-client.ts`: `drawFreelanceBudget` (OUT/BOOKING, `fl-book:<id>`, 409→`blocked`) +
     `releaseFreelanceBudget` (IN/BOOKING_REVERSAL, `fl-unbook:<id>`); `opsMovementByRef` now surfaces
     409 as `blocked`. Removed dead `consumeTeacherHours`. Budget/rate now in **satang**; added
     `fetchFreelanceRateMinor`. Made `OPS_API_URL` call-time-read so helpers are mockable.
  2. `scheduler.service.ts/updateBookingStatus`: draw at `confirm` (409 → `throw INSUFFICIENT_BUDGET`
     rolls back the whole confirm = over-booking prevention; `allowNegative` ← existing `override`
     param); reverse at `cancel`/`sick-leave` **only if the booking was confirmed/drawn** (best-effort,
     idempotent); **removed** the attend-time drawdown.
- **Verified:** `bunx tsc --noEmit` clean; `bun test` → **67 pass / 0 fail** (added `ops-client.test.ts`,
  5 cases, mocks `fetch`). ⚠️ 2 runtime end-to-end items (409-rolls-back-confirm, live P&L net-0)
  verified by inspection + mocked contract test, **not executed** (brownfield: no DB / live ops).
- **@Sober: TASK-002 ready for review.** 3 questions in `TASK-002 ## Questions`:
  - **Q1 (needs your call):** `/teachers` DTO field names for Fern's **TASK-004** (baht budget +
    near-cap). Data is available; I held off adding outward DTO fields to avoid guessing the contract
    and causing Fern rework. Recommend folding it into TASK-002 once you confirm names.
  - **Q2:** override persistence is a dependency inversion with TASK-004 (wired to per-request
    `override` for now) — OK?
  - **Q3:** reversal amount vs mid-cycle rate change (non-blocking; SPEC assumes same-month).
- **@Fern:** TASK-004's teacher-DTO fields depend on Sober's Q1 answer — I'll add them fast once he
  confirms the contract.
- Next in my queue: **TASK-005** (recurring salary + month-start job) — depends only on TASK-001
  (DONE), independent of the TASK-002 review. It's the largest task (new `ops.recurring_costs` table +
  migration, 3 endpoints, cross-repo month-start job), so I'm taking it as the next dedicated slice.

## [19:30] Fern (FE)
- Picked up **TASK-003** (Freelance Budgets screen) — unblocked by TASK-001 DONE. IN_PROGRESS → **BLOCKED**.
  Repo `smart-scheduler-backoffice-front`. Read TASK-003 + SPEC-001 + the real ops contract
  (`contract.ts`, `catalog.ts` route, `validation.ts`, `inventory.service.ts`) + the Items pattern first.
- **Built (mirrors the Items pattern; all service/type changes additive → Items screen untouched):**
  - New screen `partials/FreelanceBudgets/*` (list + create modal + top-up modal), route
    `app/(admin)/freelance-budgets`, nav item **งบครูฟรีแลนซ์**, hook `useFreelanceBudgets.ts`.
  - List: teacher + rate + **`remaining/budget` progress bar** + status badge (**เต็มงบ/ซ่อน** at ≤0,
    **ใกล้เต็มงบ** at ≤ `reorderLevel`). Lists via `?externalSource=smart-scheduler&itemType=EXPENSE`,
    filters `metadata.kind==='FREELANCE_BUDGET'` client-side (per your TASK-001 Q&A).
  - Create seeds remaining→full budget with a P&L-neutral `IN amountMinor:0` (a fresh item is qty 0 =
    would read "capped"). Top-up = `IN amountMinor:0 refType:TOPUP` (raises remaining, P&L-neutral).
- **Verified:** `bunx tsc --noEmit` → exit 0; `bun run build` → exit 0 (`/freelance-budgets` prerendered);
  loaded the page in a browser → renders (nav/header/empty-state), and the list query hit the **empty**
  state not the **error** state → the `externalSource+itemType` request round-trips OK against the live
  ops API. DB-write flows (create/top-up/capped states) **not executed** — brownfield rule (shared
  Postgres) + no rows exist yet; same posture Jason took on TASK-001/002 DB-runtime items.
- ⚠️ **@Sober — TASK-003 BLOCKED on one thing (see `TASK-003 ## Questions`):** DoD #2 asks to **edit**
  a budget item's budget/rate/threshold, but the ops catalog route has **no PATCH/update endpoint**
  (create is a plain insert; the Items screen I mirrored has no edit either). Please decide: **(a)** add a
  BE task for Jason — `PATCH /catalog/items/:id` — and I add an Edit modal, or **(b)** defer edit for
  launch. Everything **except** edit is built + build-verified. Also a minor heads-up: repo `bun run lint`
  is broken repo-wide (`next lint` removed in Next 16) — separate cleanup, not from my change.
- Note: **TASK-004** is still not startable for me — it depends on TASK-002 (now in REVIEW, not DONE) and
  on your **Q1 answer** (the `/teachers` DTO field names Jason is holding for my baht display). Once you
  review TASK-002 + confirm those field names, I can pick up TASK-004.

## [20:00] Sober (SA Lead)
- **Reviewed TASK-002 → DONE ✅** and **resolved TASK-003's blocker → DONE (narrowed) ✅**. Read the real
  code for both and **independently re-verified**: `smart-scheduler-back` `bun test` → **67 pass / 0 fail**,
  `tsc` exit 0; `backoffice-front` `tsc` exit 0.
- **TASK-002:** draw at `confirm` (409→`INSUFFICIENT_BUDGET` rolls back the whole confirm), reverse at
  cancel/leave (only when previously confirmed, idempotent), `consumeTeacherHours` removed — all correct.
  - Flagged one **non-blocking tech-debt** in the review: the ops draw isn't atomic with the scheduling
    DB tx (no 2PC) → a rare failure between a successful draw and commit could orphan a drawdown. Low
    risk at manual-booking volume; `fl-book:<id>` idempotency prevents double-draw. Logged for @Porter.
  - **Answered Jason's Q1/Q2/Q3.** Confirmed the teacher-DTO contract: `budgetMinor`, `remainingMinor`
    (rename of `quotaRemaining`, now satang), `reorderMinor`, keep `overLimit`, add `limitOverride`.
    **Split the DTO + durable-override work into a new small task, TASK-008** (rather than reopening the
    tested TASK-002 core). Q3 (recompute-at-cancel) accepted for launch.
- **TASK-003:** built scope (list/create/top-up/display) verified & accepted. The blocker was real —
  ops has **no PATCH/update route**, so "edit" can't live in a pure-FE task. **Resolved by narrowing
  TASK-003 to its built scope (DONE)** and cutting two follow-ups so the REQ's "edit budget/rate" AC is
  still tracked: **TASK-009** (@Jason, ops `PATCH /catalog/items/:id`) + **TASK-010** (@Fern, Edit modal,
  dep 009). Accepted Fern's create-seed approach.
- **New tasks on the board:** TASK-008 (Jason, sched DTO+override), TASK-009 (Jason, ops PATCH),
  TASK-010 (Fern, edit modal). **TASK-004 now depends on TASK-008** (not TASK-002 directly).
- **Status: 3/10 DONE** (001, 002, 003). Actionable now:
  - **@Jason**: TASK-005 (recurring salary + month-start job — your planned next slice), TASK-008 (small,
    unblocks Fern's TASK-004), TASK-009 (small ops PATCH, unblocks TASK-010). All deps satisfied.
  - **@Fern**: TASK-004 waits on TASK-008; TASK-006 waits on TASK-005; TASK-010 waits on TASK-009. Nothing
    startable for you until one of Jason's lands next — pick them up in that order as they complete.
- **@Porter — 2 open items (neither blocks the buildable tasks):** (1) TASK-007 revenue-recognition
  question (still open); (2) NEW non-blocking: the cross-system atomicity heads-up on TASK-002, and the
  repo-wide broken `bun run lint` (Next 16 removed `next lint`) — both logged as maintenance, your call
  on priority.

## [20:30] Jason (BE)
- Prioritised the two Fern-unblockers per stakeholder steer: **TASK-008 + TASK-009**, both → **REVIEW**.
- **TASK-008 (scheduling DTO + durable override):** teacher DTO now ships satang `budgetMinor`,
  `remainingMinor` (renamed from `quotaRemaining`), `reorderMinor`, `overLimit`, `limitOverride`
  (exactly Sober's confirmed contract). `fetchTeacherQuotas` reads `metadata.monthlyBudgetMinor` +
  `reorderLevel`. Durable override stored in **`app_settings`** (`limit-override:<id>`, not a new
  column — avoids a migration BE can't apply under the brownfield rule) via
  `PATCH /teachers/:id/limit-override`; the TASK-002 confirm path now reads it
  (`allowNegative = override || persisted`).
- **TASK-009 (ops PATCH):** `PATCH /catalog/items/:id` (adminOrService) — partial edit of
  name/salePriceMinor/reorderLevel/active/metadata; identity/classification locked; metadata
  **shallow-merged** (extracted pure `mergeMetadata`, so editing `monthlyBudgetMinor` keeps `kind`).
  Editing the budget doesn't touch current stock (applies at next reset) — noted for TASK-010.
- **Verified:** both repos `bunx tsc --noEmit` clean. `smart-scheduler-back` `bun test` → **69 pass/0 fail**
  (+attachTeacherQuotas + toTeacherDTO tests); `backoffice-back` `bun test` → **11 pass/0 fail**
  (+4 mergeMetadata tests). ⚠️ DB-runtime bits (app_settings round-trip, PATCH persist) verified by
  inspection only — no DB (brownfield); pure business logic is unit-tested.
- **@Sober: TASK-008 + TASK-009 ready for review.** One non-blocking note in TASK-008 (override in
  `app_settings` vs a `teachers` column — easy to switch if you want queryability). TASK-009 has no questions.
- **@Fern: TASK-004 and TASK-010 are both unblocked** — teacher-DTO budget fields + the override PATCH
  (TASK-008), and `PATCH /catalog/items/:id` for the edit modal (TASK-009). Field/endpoint contracts are
  in each task's Implementation Notes.
- **Next in my queue:** TASK-005 (recurring salary + month-start job) — the last big BE task; depends only
  on TASK-001 (DONE). Taking it as the next dedicated slice.

## [21:00] Fern (FE)
- Picked up **TASK-004** (scheduler-front baht display). IN_PROGRESS → **REVIEW**. Built against your
  frozen TASK-008 contract — confirmed field **units** by reading Jason's backend (`hourlyRate`=baht,
  `remainingMinor`/`budgetMinor`/`reorderMinor`=satang, `overLimit`=remaining≤0, `limitOverride` on DTO).
- **Done:** synced FE `TeacherDTO`/`Teacher` to the new contract (dropped `quotaRemaining` → satang
  `remainingMinor`+`budgetMinor`+`reorderMinor`+`limitOverride`); `FreelanceRow` now shows baht
  **`฿remaining / ฿budget`** + progress bar + near-cap warning tint (`≤ reorderMinor`) + red over-budget
  (`≤0`), replacing the hours line (also killed a hardcoded-Thai i18n violation). **Override is now
  server-persisted** — `setTeacherLimitOverride` calls `PATCH /teachers/:id/limit-override` (removed the
  localStorage store). Auto-hide left as-is (not rebuilt). Cache invalidation (DoD #3) already handled by
  `invalidateAll`. Updated mock data to satang.
- **Verified:** `bunx tsc --noEmit` → exit 0 (this is the real check the DTO rename propagated across
  mapper/service/component/mock); `bun run build` → exit 0 (`/scheduler/teachers` prerendered).
  - ⚠️ **Could NOT run a live render check:** `/scheduler/teachers` is NextAuth-gated and redirects to the
    **production** login (`frontoffice.develyst.online`); the configured API is the live frontoffice env —
    both barred by the brownfield / no-real-env-login rules. **I did not authenticate.** Display logic is
    pure + fully type-checked; verified by inspection, same posture accepted on your DB-runtime items.
- ⚠️ **@Sober — two flags (neither blocks you; see `TASK-004 ## Questions`):**
  1. **I built on TASK-008 while it's still in REVIEW** (not DONE). Safe because you'd frozen the contract
     and Jason delivered it exactly — but **please review TASK-008 before TASK-004**, and if the review
     renames any DTO field/endpoint I'll adjust (small rename).
  2. **UX call:** override switch now shows whenever a freelance is raw-over-budget (`remainingMinor≤0`),
     not only on effective `overLimit` — else it vanished once override was enabled (couldn't toggle off).
     Makes the persisted override visible + reversible across reloads. Flag if you'd rather revert.
- **@Fern-self / next:** **TASK-010** (Freelance Budgets Edit modal) is unblocked by TASK-009 (also in
  REVIEW) — that's my next pickup. **TASK-006** still waits on TASK-005 (Jason, TODO).

## [21:30] Sober (SA Lead)
- **Reviewed all three REVIEW tasks → DONE ✅ (TASK-008, TASK-009, TASK-004).** Read the real diffs and
  **independently re-ran every suite**: `smart-scheduler-back` **69 pass/0 fail** + tsc 0;
  `backoffice-back` **11 pass/0 fail** + tsc 0; `scheduler-front` tsc 0. Spot-checked the risky bits:
  - **TASK-008:** confirmed the exact DTO contract; `allowNegative = override || readLimitOverride(tx,…)`
    in the confirm path (`scheduler.service.ts:564`); `app_settings` override accepted (right call — no
    migration under brownfield).
  - **TASK-009:** `PATCH /:id` doesn't shadow `by-ref/movements` (declared earlier) — method+ordering
    safe; `mergeMetadata` shallow-merges only when provided (editing `monthlyBudgetMinor` keeps `kind`).
  - **TASK-004:** baht `฿remaining/฿budget`, near-cap at `remainingMinor ≤ reorderMinor`, server-persisted
    override; the `quotaRemaining`→`remainingMinor` (satang) rename propagates cleanly (tsc proves it).
    Approved Fern's UX call (override switch bound to raw `remainingMinor≤0` so it stays reversible) and
    confirmed building on the frozen TASK-008 contract needed no adjustment.
- **Milestone: SPEC-001 (freelance budget-stock) is functionally complete.** DONE: 001,002,003,004,008,009.
  End-to-end the freelance flow now works: admin sets a budget (screen), booking draws it down in real
  time + posts the expense, cancel/leave reverses, cap auto-hides the teacher, staff see baht
  `remaining/budget` + near-cap, admin can top-up or override. **Left on SPEC-001:** TASK-010 (edit modal,
  now unblocked) + TASK-007 (day-end revenue, BLOCKED on @Porter).
- **Status: 6/10 DONE.** Actionable now:
  - **@Jason**: **TASK-005** (recurring FT/PT salary + month-start job) — the last big BE task, all deps met.
  - **@Fern**: **TASK-010** (Freelance Budgets edit modal) — unblocked by TASK-009 DONE. TASK-006 waits on 005.
- **@Porter**: still owns TASK-007 (revenue recognition) + the 2 maintenance items (TASK-002 atomicity
  heads-up, broken FE `lint`). None block Jason/Fern's remaining work.

## [22:00] Jason (BE)
- **TASK-005 (recurring FT/PT salary + month-start job) → REVIEW.** Built entirely in
  `smart-scheduler-backoffice-back` (see D1 — both halves are ops-owned; scheduling untouched).
- **Delivered:** new `ops.recurring_costs` table (effective-dated) + migration `0003_even_turbo.sql`;
  `GET/POST /recurring-costs` (set/change **supersedes** the prior open row → past months frozen; creates
  the per-teacher FIXED_COST item on first set); `POST /internal/recurring/materialize {month}` (posts each
  teacher's salary for the month it's effective in, idempotent `salary:<teacherId>:<month>`); and the shared
  `POST /internal/jobs/month-start {month}` = freelance budget reset (ADJUST=budget, `fl-reset:<id>:<month>`,
  P&L-neutral) + materialize. Effective-date logic extracted to pure `lib/month.ts`.
- **Verified:** migration generated **offline** (no DB); `tsc` clean; `bun test` → **17 pass / 0 fail**
  (+6 `month.test.ts` incl. the supersede/past-month-amount proof). ⚠️ DB-runtime paths (table apply,
  set/materialize/reset round-trips) verified by inspection only — brownfield, no DB; core rule unit-tested.
- **@Sober — 3 decisions to confirm in `TASK-005 ## Questions`** (none block review): D1 month-start in ops
  (not scheduling); D2 `party_id` nullable + idempotency keyed by `teacherId` not `partyId` (stays consistent
  with the party-less freelance model); D3 **migration hand-trimmed** — `db:generate` re-emitted already-existing
  `catalog_items`/`stock_movements` columns due to the repo's **drizzle meta drift** (the hand-patched
  0001/0002 infra issue), so I stripped the SQL to only the new `recurring_costs` object. Applying
  (`db:migrate`) is integration's step. The meta-drift itself is @Porter's pre-existing maintenance item.
- **@Fern:** TASK-006 (FT/PT Salary screen) is now unblocked — endpoints/shape in TASK-005 Implementation
  Notes (`GET/POST /recurring-costs`, effective-dated history).
- **BE queue:** all buildable BE tasks are now in REVIEW/DONE. Only **TASK-007** remains — still BLOCKED on
  @Porter's revenue-recognition answer; I can't start it until that lands. Nothing else waiting for me.

## [22:30] Sober (SA Lead)
- **Reviewed TASK-005 → DONE ✅ (code).** Re-ran `backoffice-back`: `bun test` → **17 pass / 0 fail**,
  `tsc` exit 0. Read the real logic — effective-dating is correct: `setRecurringCost` supersedes the prior
  open row at `prevMonthFirstDay` + opens a new one; `materializeRecurring` picks the row *covering* the
  target month and is idempotent per `salary:<teacherId>:<month>` (past-month re-materialize uses that
  month's amount, not a mutable current); `runMonthStart` = freelance reset (`ADJUST '=budget'`,
  `amountMinor:0`, `fl-reset` key — exactly the TASK-001 contract) + materialize; pure `month.ts` helpers
  unit-tested. FIXED_COST item is `track_stock=false` (P&L-only). Clean.
- **Answered Jason's D1/D2/D3:** D1 (job in ops) ✅, D2 (`teacherId` keys + nullable `party_id`, no
  party auto-create) ✅ — both good calls. D3 (hand-trimmed migration): the SQL is correct by inspection,
  but **"migration applies" can't be closed by BE under brownfield**, and the shared `__drizzle_migrations`
  meta-drift is a real apply-time landmine.
- **⚠️ Escalated a REQ-001 DEPLOY GATE to @Porter → human** (new Blocked/waiting row; does NOT block the
  2 remaining FE tasks): before REQ-001 is DELIVERED, the human must (1) **apply the ops migration**
  `drizzle/0003_even_turbo.sql` in the real env + reconcile the drizzle meta-drift, and (2) **set up the
  two scheduled tasks** — end-of-day (:3001, `INTERNAL_JOB_SECRET`) and the new month-start (:3002,
  `X-Service-Token`, 1st of each month). These are the only real-environment actions the whole feature needs.
- **Milestone: all BE is DONE.** SPEC-001 + SPEC-002 backends complete (TASK-001/002/005/008/009).
- **Status: 7/10 DONE.** Remaining:
  - **@Fern**: **TASK-006** (FT/PT Salary screen — unblocked by TASK-005) and **TASK-010** (Freelance
    Budgets edit modal — unblocked by TASK-009). Both are pure FE, deps met — please pick both up.
  - **@Jason**: nothing buildable left — TASK-007 stays BLOCKED on @Porter.
- **@Porter — you now own the critical path:** (1) the **deploy gate** above (needed to actually ship),
  (2) the **TASK-007 revenue-recognition** decision (to unblock the last BE task), (3) the 2 maintenance
  items. Once Fern finishes TASK-006/010 and you resolve the revenue question, REQ-001 is code-complete
  and ready for your acceptance check.

## [21:40] Fern (FE)
- Picked up **TASK-010** (Freelance Budgets edit modal). IN_PROGRESS → **REVIEW**. Deps met (TASK-009 DONE).
- **Done:** added `updateItem` (`PATCH /catalog/items/:id`) + `useUpdateFreelanceBudget` to the service/hook,
  a new `EditFreelanceBudgetModal` (pre-filled rate/budget/near-cap in baht; teacherId shown disabled —
  identity locked), and a per-row **"แก้ไข"** button on the Freelance Budgets screen. Edit sends
  `{name, salePriceMinor, reorderLevel, metadata:{monthlyBudgetMinor}}` — backend shallow-merges so
  `metadata.kind` is preserved. Modal shows a blue Alert: **budget edit = next-reset target**, current
  remaining unchanged until month-start reset; use **เติมงบ** to change remaining now (per TASK-009's note).
- **Verified:** `bunx tsc --noEmit` → exit 0; `bun run build` → exit 0 (`/freelance-budgets` prerendered);
  loaded the page in a browser → mounts cleanly with the edit modal wired (no React error, header+empty-state
  render — regression check). ⚠️ Live edit round-trip not executed (needs a real ops DB row — brownfield);
  modal reuses the proven Create/TopUp wiring. No questions (flagged one optional: expose `active` toggle? left out).
- **@Sober: TASK-010 ready for review.**
- ✅ **All FE for REQ-001 that's currently buildable is done** except **TASK-006** (FT/PT Salary screen,
  dep TASK-005 DONE) — that's my next pickup. After TASK-006, all 10 tasks except TASK-007 (BLOCKED on
  @Porter's revenue-recognition call) will be built; TASK-004 already DONE, TASK-003/010 in your review queue.

## [23:00] Sober (SA Lead)
- **Reviewed TASK-010 → DONE ✅.** Re-ran `backoffice-front` `tsc` → exit 0. Verified the Edit modal
  sends TASK-009's exact contract (`salePriceMinor`, `reorderLevel` null-when-cleared,
  `metadata:{monthlyBudgetMinor}` → shallow-merge keeps `kind`), identity locked, and the "budget edit =
  next-reset target, use Top-up for now" Alert matches the TASK-009 semantics. Reuses proven Create/TopUp
  wiring. Accepted Fern's scope call to leave `active`/soft-disable out (not in REQ-001's edit AC).
- **Status: 8/10 DONE.** Only two tasks left in the whole REQ:
  - **@Fern — TASK-006** (FT/PT Salary admin screen, dep TASK-005 DONE) — the **last buildable task**.
    Endpoints are live: `GET/POST /recurring-costs` (effective-dated set/change + history). Please build
    it; I'll review on REVIEW.
  - **TASK-007** (day-end revenue) — still BLOCKED on @Porter's revenue-recognition decision.
- **Nothing is waiting on Sober right now.** Once Fern lands TASK-006 I'll review it; that closes all
  buildable work and leaves only TASK-007 (Porter) + the deploy gate (Porter → human) before REQ-001 is
  ready for Porter's acceptance check.
- **@Porter — the critical path is entirely yours now:** (1) revenue-recognition answer → unblocks the
  final TASK-007; (2) the deploy gate (apply migration + 2 scheduled tasks). Everything else is done or
  in Fern's hands.

## [22:20] Fern (FE)
- Picked up **TASK-006** (FT/PT Salary screen) — the last FE build. IN_PROGRESS → **REVIEW**. Dep TASK-005 DONE.
- Confirmed the contract from Jason's backend (`RecurringCostDTO`, `recurring.ts` route, `recurring.service.ts`).
- **Built:** new `recurring.service.ts` (`list`/`set` for `/v1/recurring-costs`) + `useRecurring.ts` hook
  (+`currentOf` helper), a **FtptSalary** screen that **groups the flat effective-dated rows per teacher**
  (headline = current/open row: name, type badge, current salary, effective-from) with a **"ประวัติ" expander**
  showing the full effective-dated history (from→to, amount, "ใช้อยู่" on the open row). Set/change via
  `SetSalaryModal` — teacherId locked in change-mode, monthly amount (baht→satang), and มีผลตั้งแต่เดือน via a
  native `<input type="month">` (exact `YYYY-MM`); blue Alert messages "applies from this month onward, past
  months unchanged" (effective-dating). Route `/ftpt-salary` + nav item **เงินเดือนประจำ/พาร์ทไทม์** (Banknote).
- **Verified:** `bunx tsc --noEmit` → exit 0; `bun run build` → exit 0 (`/ftpt-salary` prerendered); loaded in
  a browser → renders (nav+header+empty-state) and the list query hit the **empty** state not **error** →
  `GET /v1/recurring-costs` round-trips OK against the live ops API. ⚠️ Set/change write not executed (shared
  ops Postgres — brownfield; no rows yet); modal reuses proven wiring, POST/GET is Jason's DONE TASK-005.
- **@Sober: TASK-006 ready for review.** One non-blocking design note in `## Questions` (per-teacher grouping
  vs flat history table).
- 🎉 **All 10 REQ-001 tasks are now built.** FE (003/004/010/006) done — 004 already accepted; 003/010 accepted;
  006 in your queue. Only **TASK-007** remains BLOCKED on @Porter's revenue-recognition call. After you accept
  006, the only things between REQ-001 and DELIVERED are Porter's revenue decision (unblocks TASK-007) + the
  **deploy gate** (migration apply + 2 scheduled tasks in the real env). Nothing left for FE to build.

## [23:30] Sober (SA Lead)
- **Reviewed TASK-006 → DONE ✅.** Re-ran `backoffice-front` `tsc` → exit 0. Verified against TASK-005's
  live contract: lists `/recurring-costs`, set/change POSTs `{externalRef, amountMinor, effectiveFrom:"YYYY-MM",
  teacherType}` and relies on server-side supersede (FE never mutates history — correct), change-mode locks
  `externalRef`, `<input type="month">` emits the exact `YYYY-MM`, effective-dating Alert + history view
  (from→to, "ใช้อยู่") satisfy DoD #2/#3/#4, `currentOf` resolves the in-effect row correctly. Approved the
  per-teacher grouping UX (headline + history toggle). No rework.
- 🎉 **MILESTONE — all 9 buildable tasks DONE (9/10).** REQ-001 is **code-complete** except the single
  Porter-blocked TASK-007.
  - **SPEC-002 (FT/PT recurring salary) is fully DELIVERED** — TASK-005 + TASK-006 both DONE.
  - **SPEC-001 (freelance budget-stock) is DONE except TASK-007** (day-end revenue), which is BLOCKED on
    Porter's revenue-recognition decision, not on any build work.
- **The whole feature, end to end, now exists in code:** admin sets per-freelance budgets + FT/PT salaries
  (both screens, effective-dated for salary); booking draws a freelance's budget down in real time (expense
  to P&L + cap), cancel/leave reverses; cap auto-hides the teacher, staff see baht `remaining/budget` +
  near-cap, admin can top-up / edit / override; month-start job resets budgets + auto-posts salaries; all
  money lands in `/reports/pl`. Every task independently `tsc`/test-verified; DB/live-env paths accepted
  under the brownfield rule.
- **Nothing is waiting on Sober.** No REVIEW queue, no open SA questions.
- **@Porter — REQ-001 is now entirely in your court. Two things stand between it and DELIVERED:**
  1. **Revenue-recognition decision** (sale-time vs delivery-time) → unblocks the final **TASK-007**. Until
     then I can't finalize SPEC-001's last task or set the REQ `SPEC_DONE`.
  2. **Deploy gate** (Porter → human): apply ops migration `0003_even_turbo.sql` + reconcile the
     `__drizzle_migrations` meta-drift; set up the 2 scheduled tasks (end-of-day :3001, month-start :3002).
  Once #1 lands I'll spec/close TASK-007 and, when all SPEC-001 tasks are DONE, set REQ-001 → `SPEC_DONE`
  for your acceptance check. (SPEC-002 is already fully done and could be accepted independently now.)

## [09:00] Sober (SA Lead)
- **Porter answered the revenue-recognition question** (REQ-001 #5, now updated): revenue is **per
  booking type** — `COURSE_PACKAGE`/`VOUCHER` at **sale** (unchanged `recordSale`), `FIRST_TRIAL`/
  `SINGLE_SESSION` at **attendance** (day-end). This is the clean **additive** path: trial/single record
  no revenue today, so recognizing them at day-end adds no double-count and needs **no** deferred-revenue
  rework. Good outcome — my earlier "option A/B" concern resolves to a simple hybrid.
- **Folded it into SPEC-001** — new "Revenue recognition (REQ #5)" section: the end-of-day job posts
  revenue for attended `FIRST_TRIAL`/`SINGLE_SESSION` via the existing `recordSale` path on per-type
  INCOME items (`first-trial`, `single-session`, `track_stock=false`, configurable price), idempotent
  per booking (`rev:<bookingId>`), best-effort. Packages/vouchers untouched.
- **Released TASK-007 → TODO, @Jason** (unblocked). Rewrote it from the old "A vs B" stub into concrete
  instructions + DoD: which bookings, the `recordSale` call, idempotency, best-effort skip, and the
  seed note (the two INCOME items must exist for revenue to post — real prices are a future DATA REQUEST;
  free trial = price 0 = harmless).
- **@Jason — TASK-007 is yours, the final REQ-001 task.** All deps met (TASK-001 DONE; reuses ops
  `recordSale`/INCOME items). Please build it; I'll review on REVIEW.
- **Status: 9/10 DONE, 1 TODO (TASK-007).** Once Jason lands it and I accept, **all SPEC-001 tasks are
  DONE** → I'll set **REQ-001 → `SPEC_DONE`** for @Porter's acceptance check. Only the **deploy gate**
  (Porter → human: migration apply + 2 scheduled tasks) then stands between it and DELIVERED.
- @Porter: no longer blocked on you for build — the revenue answer unblocked the last task. Your remaining
  item is the deploy gate (and the FE `lint` maintenance nit).

## [09:30] Jason (BE)
- **TASK-007 (end-of-day revenue tally) → REVIEW — the final REQ-001 build task.** Built exactly to
  Porter's per-type decision.
- **Delivered** (`smart-scheduler-back`): pure `revenueItemRef(bookingType)` (`FIRST_TRIAL→first-trial`,
  `SINGLE_SESSION→single-session`, course/voucher→`null`); `runEndOfDayJob` now, after the no-show cut,
  posts INCOME revenue for the run date's **ATTENDED `FIRST_TRIAL`/`SINGLE_SESSION`** bookings via the
  existing `recordSale` (`rev:<bookingId>` idempotency, amount priced server-side from the INCOME item —
  no hardcoded price), and records `revenuePosted` in the `job_runs` summary. Best-effort: re-run safe,
  ops-off / unseeded item → skip, job never fails. Packages/vouchers post none at day-end; freelance
  untouched.
- **Verified:** `tsc` clean; `bun test` → **71 pass / 0 fail** (+2: `revenueItemRef` mapping incl.
  course/voucher→null, and `recordSale` INCOME/SALE payload with `rev:<id>` and no explicit amount).
  ⚠️ Job DB query + posting verified by inspection (brownfield, no DB); mapping + ops contract unit-tested.
- **Seed note for @Porter's deploy gate:** the `first-trial` / `single-session` INCOME items must be seeded
  with real prices for revenue to post (free trial = price 0 = harmless). No code blocker.
- **@Sober: TASK-007 ready for review — this closes all 10 REQ-001 tasks on the BE side.** No questions.
- **BE queue is now empty.** Every buildable BE task (001,002,005,007,008,009 mine; +002/007 scheduling)
  is DONE or in REVIEW. Nothing else is waiting for Jason. Next move: your review → REQ-001 `SPEC_DONE`,
  then Porter's deploy gate (migration apply + 2 scheduled tasks + seed the INCOME items).

## [10:00] Sober (SA Lead)
- **Reviewed TASK-007 → DONE ✅ — the final task.** Re-ran `smart-scheduler-back`: `bun test` →
  **71 pass / 0 fail**, `tsc` exit 0. Verified: day-end selects `ATTENDED` `FIRST_TRIAL`/`SINGLE_SESSION`,
  `revenueItemRef` maps only those two (course/voucher → null → skipped, no double-count), `recordSale`
  with `rev:<bookingId>` idempotency + no hardcoded price, best-effort skip, freelance untouched. Matches
  SPEC-001's Revenue-recognition section exactly. No rework.
- 🏁 **ALL 10 REQ-001 TASKS DONE. REQ-001 → `SPEC_DONE`.** Both SPECs closed:
  - **SPEC-001** (freelance budget-stock) → DONE: 001, 002, 003, 004, 007, 008, 009, 010.
  - **SPEC-002** (FT/PT recurring salary) → DONE: 005, 006.
  - Every task independently re-verified by me (`bun test` + `tsc` per repo); DB/live-env paths accepted
    under the brownfield rule (BE never touches the shared Postgres or real env).
- **@Porter — REQ-001 is SPEC_DONE and ready for your acceptance check.** Full traceability:
  REQ-001 → SPEC-001/002 → TASK-001…010 → code, all linked. Summary of what's built end-to-end:
  - **Freelance:** admin sets/edits per-teacher monthly budget + rate + near-cap (backoffice screen);
    booking draws the budget down in real time (expense→P&L + hard cap), cancel/leave reverses it; at 0
    the teacher auto-hides from booking + is flagged; admin unlocks via top-up or persisted override;
    staff see baht `remaining/budget` + near-cap; budgets reset monthly (top-ups don't carry).
  - **FT/PT:** per-teacher recurring monthly salary, effective-dated (past months frozen, history kept),
    auto-posts as FIXED_COST each month.
  - **Revenue:** course/voucher at sale (unchanged); trial/single at attendance (day-end). All money
    flows into `/reports/pl` (revenue − freelance expense − FT/PT fixed cost).
- **Before you can mark it DELIVERED — the deploy gate (Porter → human), the only real-env work left:**
  1. Apply ops migration `drizzle/0003_even_turbo.sql` + reconcile the `__drizzle_migrations` meta-drift.
  2. Set up the 2 scheduled tasks: end-of-day (:3001, `INTERNAL_JOB_SECRET`) + month-start (:3002,
     `X-Service-Token`, 1st of month).
  3. Seed real data (DATA REQUEST): per-teacher budgets/rates, FT/PT salaries, and the
     `first-trial`/`single-session` INCOME item prices.
  Plus the FE `lint` maintenance nit (non-blocking).
- **Nothing is waiting on Sober, Jason, or Fern.** The build phase is complete; the ball is entirely with
  **Porter** (acceptance check → coordinate the deploy gate with the human → `DELIVERED`).

## [10:30] Sober (SA Lead)
- **Porter provided placeholder seed data + changed deploy ports** (ops-back 3002→**4010**, ops-front
  3100→**3018**) and routed me 2 delivery-readiness tasks. Read `project-docs/seed-data-placeholder-2026-07-20.md`
  and grepped the real config — confirmed the drift: scheduling `OPS_API_URL` and backoffice-front
  `NEXT_PUBLIC_BACKOFFICE_API_URL` still template **:3002** in `.env.example`.
- **Cut 2 delivery-readiness tasks (both @Jason, TODO):**
  - **TASK-011 — port alignment:** update the two cross-service URL templates to **:4010**, fix stale
    comments, and write the canonical port map into `docs/monorepo-overview.md` + root `CLAUDE.md`.
    ⚠️ **Flagged a discrepancy to @Porter:** `smart-scheduler-back/.env.example` has `PORT=4006`, but the
    deploy-gate note says the **end-of-day** scheduled task hits **:3001** — Porter to confirm the real
    scheduling port so the scheduled-task URL is right.
  - **TASK-012 — seed the `first-trial` / `single-session` INCOME items** (SERVICE/INCOME,
    `track_stock=false`, placeholder **139000 satang = 1,390 THB** each per the seed doc §4), idempotent,
    so TASK-007's day-end revenue actually posts. Freelance/FT-PT placeholder data (§1–§3) left optional
    (customer can type it into the admin screens post-deploy).
- **Bonus from the seed doc:** it resolves the long-parked **voucher-size question** — the rate card
  confirms the code's **5/10/15h** sizes are correct (6,000 / 10,500 / 13,500). Noted for @Porter to close
  that open item in `product-catalog-pricing.md` §3 (not our task, just surfacing it).
- **Status: 10/12 tasks DONE**, TASK-011 + TASK-012 TODO with @Jason. REQ-001 stays **SPEC_DONE**; these
  two are delivery hardening, not core scope. Once Jason builds them and I review, the repo side of the
  deploy gate is fully prepped; the human then applies the migration + scheduled tasks + (optional) seed.
- **@Jason — 2 small tasks (TASK-011 config/docs, TASK-012 ops seed), both deps met.** Please pick them up.
- **@Porter:** one decision folded in — confirm the scheduling port (4006 vs 3001) for the end-of-day
  scheduled-task URL (TASK-011). Everything else on your deploy-gate checklist is unchanged.

## [11:00] Sober (SA Lead)
- **Porter answered the scheduling-port question → scheduling-back = :4006** (staff-front :3016). Folded
  the **confirmed port map** into TASK-011 and cleared its open flag — Jason now builds to a final,
  unambiguous map: **staff-front :3016 · scheduling :4006 · ops :4010 · ops-front :3018**; end-of-day
  scheduled task → :4006, month-start → :4010.
- Verified `smart-scheduler-front/.env.example` uses a **prod domain** (`som.develyst.online/api`), not a
  localhost port → no change there; only the two ops localhost URL templates need the :4010 fix (TASK-011).
- **Nothing is waiting on Sober.** TASK-011 + TASK-012 are TODO with **@Jason** (both small, deps met, no
  open questions now). Ball is with Jason to build them; I'll review on REVIEW, then the repo side of the
  deploy gate is fully prepped for @Porter → human.

## [11:20] Jason (BE)
- Built the last two delivery-readiness tasks → **REVIEW**.
- **TASK-011 (port alignment, config+docs only, no logic):** `smart-scheduler-back/.env.example`
  `OPS_API_URL→:4010`; `backoffice-front/.env.example` `NEXT_PUBLIC_BACKOFFICE_API_URL→:4010/api` (+comment);
  `backoffice-back/.env.example` comment `3001→4006`. Root `CLAUDE.md` port table → `4006/3016/4010/3018`
  + canonical note. `docs/monorepo-overview.md` → added a **Canonical port map** callout + updated tree/
  tables/mermaid/dev-cmds, then **synced the file to all 4 repos' `docs/`** (cp, per the doc-sync rule).
  Left `smart-scheduler-front/.env.example` (prod domain, not a localhost port).
- **TASK-012 (seed INCOME items):** added `first-trial` + `single-session` to `backoffice-back/src/db/seed.ts`
  (SERVICE/INCOME, `track_stock=false`, `external_source=smart-scheduler`, `external_ref=sku`, **139000 satang
  placeholder**), idempotent `onConflictDoNothing`. Prices flagged PLACEHOLDER in-code (single-session is
  program-dependent 1,090–1,690 — คุณฟีน refines, or edit via Items screen / TASK-009 PATCH).
- **Verified:** `backoffice-back` `bunx tsc --noEmit` clean, `bun test` → **17 pass / 0 fail**; TASK-011 is
  config/docs (no code) — grep-confirmed the 4 real ports across the synced docs, no cross-service `.env`
  still on `:3002`. ⚠️ Running the seed needs the DB → deploy-gate/integration step (brownfield, not executed).
- **@Sober: TASK-011 + TASK-012 ready for review.** One tiny note in TASK-011 (I also corrected the root
  CLAUDE.md backoffice-front "0% greenfield" cell to "built" as it contradicted the real :3018 port — revert
  if you'd rather keep maturity edits in Porter's lane).
- **@Porter:** repo side of the deploy gate is now fully prepped — templates/docs on the real port map, and
  the day-end revenue items are in the seed. Remaining deploy-gate steps are all yours→human (apply migration
  `0003` + meta-drift reconcile, 2 scheduled tasks, run `db:seed`/enter real prices).
- **BE queue is empty again.** All 12 REQ-001 tasks are DONE or in REVIEW; nothing waiting for Jason.

## [11:40] Sober (SA Lead)
- **Reviewed TASK-011 + TASK-012 → both DONE ✅.** Independently verified:
  - **TASK-011:** grep confirms both cross-service templates now point at **:4010** and **no `.env` template
    references `:3002`**; canonical port map (staff :3016 · scheduling :4006 · ops :4010 · ops-front :3018)
    written + synced to all 4 repos' `docs/`. Approved the doc-consistency fix (backoffice-front "0%"→"built"
    — matches reality Porter already set; not scope creep).
  - **TASK-012:** re-ran `backoffice-back` `bun test` → **17 pass / 0 fail**, `tsc` 0; read `seed.ts` — both
    `first-trial`/`single-session` items are `SERVICE`/`INCOME`/`track_stock=false`/`external_ref=sku`,
    `THB(1390)` placeholder, `onConflictDoNothing` (idempotent). Matches TASK-007's `recordSale` refs.
- 🏁 **ALL 12 REQ-001 TASKS DONE. The entire repo side is complete + verified.** SPEC-001 (10 tasks) +
  SPEC-002 (2 tasks) both fully DONE; REQ-001 stays **SPEC_DONE** (build + delivery-readiness all closed).
- **Nothing is waiting on Sober, Jason, or Fern. The build+prep phase is 100% complete.** Every task was
  independently re-verified by me (tests/tsc/grep per repo); all DB/live-env actions correctly deferred to
  the deploy gate under the brownfield rule.
- **@Porter — REQ-001 is entirely yours now: acceptance check → run the deploy gate → `DELIVERED`.** The
  repo is fully prepped; the only remaining actions are real-environment (Porter → human):
  1. Apply ops migration `drizzle/0003_even_turbo.sql` + reconcile the `__drizzle_migrations` meta-drift.
  2. Set up 2 scheduled tasks: **end-of-day → :4006** (`INTERNAL_JOB_SECRET`) + **month-start → :4010**
     (`X-Service-Token`, 1st of month).
  3. Run `bun run db:seed` in ops (or create via the Items screen) for the `first-trial`/`single-session`
     revenue items; enter real per-teacher budgets/rates + FT/PT salaries via the admin screens (or seed);
     swap the placeholder 1,390 when คุณฟีน gives real prices.
  Plus the non-blocking FE `lint` maintenance nit. Once you've accepted + deployed, set REQ-001 → `DELIVERED`.

## [12:10] Sober (SA Lead)
- **Porter passed the build-acceptance check (all 12 tasks) and asked for a deploy runbook.** Wrote
  **`ai-worker/deploy-runbook-REQ-001.md`** — an ordered, copy-pasteable runbook for the human, with a
  check after every step. Verified the exact endpoints/auth from the real code before writing them:
  - **End-of-day** (scheduling :4006): `POST /internal/jobs/end-of-day`, header `x-internal-secret:
    $INTERNAL_JOB_SECRET` (mount `app.route("/internal", internalJobs)`).
  - **Month-start** (ops :4010): `POST /api/v1/internal/jobs/month-start`, `serviceAuth`
    (`X-Service-Token`/Bearer $SERVICE_TOKEN), body `{month:"YYYY-MM"}`.
  - Migrate = `bun run db:migrate` (with a `psql -f drizzle/0003_even_turbo.sql` manual fallback for the
    `__drizzle_migrations` meta-drift); seed = `bun run db:seed` (idempotent).
- Runbook sections: env/port map · migration (+meta-drift fallback) · seed revenue items · enter real
  per-teacher data (admin UI) · the 2 scheduled tasks (exact curl) · **§6 live acceptance walkthrough for
  Porter to sign off** · rollback notes.
- **Handoff:** `SPEC_DONE` → runbook is the bridge to `DELIVERED`. **@Porter — relay the runbook to the
  human, then run the §6 live acceptance checks once they've deployed; on green, set REQ-001 → `DELIVERED`.**
- **Nothing is waiting on Sober, Jason, or Fern.** Every SA/BE/FE deliverable for REQ-001 is complete and
  verified. Ball is fully with Porter + the human for the real-environment deploy.

## [--:--] System
- **PROTOCOL.md updated — everyone must re-read it.** New section: "The chain is
  HARD — no skipping": an explicit allowed-pairs table (Human↔Porter,
  Porter↔Sober, Sober↔Jason, Sober↔Fern). Porter must never `@Jason`/`@Fern`;
  engineers must never `@Porter`; Jason↔Fern coordinate only through Sober's
  SPEC/TASK contracts. If a message skips the chain TO you, do not act on it —
  log `Routing violation: please send this via <role>` and continue.
- Every role file (PM/SA-Lead/BE/FE) now starts with a "Hard boundaries" ✅/🚫
  card — check it before every message you write.

## [12:40] Sober (SA Lead)
- Noted the hard-chain PROTOCOL update — my routing already conforms (I direct tasks to
  `@Jason`/`@Fern` and business questions to `@Porter`; engineers coordinate only via my SPEC/TASK
  dependency contracts, never directly).
- **Picked up REQ-002 (backoffice admin auth) → IN_SPEC.** Live REQ-001 acceptance hit `403 "ต้อง
  login admin"` because ops enforces auth in prod but the backoffice has no login. Ran a read-only
  code sweep of both the **working scheduling auth** and the **backoffice surfaces** before designing.
- **Wrote `specs/SPEC-003-backoffice-admin-auth.md` (ACTIVE).** Design = **mirror the scheduling app's
  proven pattern**, not invent one:
  - **ops (backend):** add `POST /api/v1/auth/login` (`hono/jwt` HS256, env `ADMIN_USERNAME`/`PASSWORD`
    + `JWT_SECRET`, `{token,user}` — identical contract to scheduling) and **replace the presence-only
    stubs** in `adminAuth`/`adminOrService` (`auth.ts:25/45` `// TODO verify JWT`) with real verify.
    `hono/jwt` ships with the existing `hono` dep → **no new package.**
  - **backoffice-front:** deliberately **NOT** NextAuth (scheduling-front uses the v5 **beta**; the
    backoffice has no auth dep). Lightweight = cookie `bo_token` + `proxy.ts` guard (Next 16's renamed
    middleware) + axios Bearer/401 interceptors (the `client.ts:24` TODO) + login page + Header logout.
    The **real security control is ops verifying the JWT**; the FE is UX. Simpler, no beta dep.
- **Cut 2 tasks:** **TASK-013** (@Jason, ops login+verify) → **TASK-014** (@Fern, FE login/guard, dep 013).
- **Answered Porter's 2 REQ-002 questions** (in the REQ): single shared admin = confirmed for launch
  (token contract stays users-table-ready); credential same-vs-separate = **I recommend separate** ops
  creds, but it's a **deploy-time env choice, not a design blocker** — TASK-013/014 proceed either way.
- **@Porter — 1 non-blocking business confirm:** does คุณฟีน want a **separate** backoffice login
  (recommended) or **reuse the scheduling admin credential**? Just sets which env values are used at deploy.
- **@Jason / @Fern:** SPEC-003 is ACTIVE; TASK-013 (Jason, no deps) then TASK-014 (Fern, dep 013) are TODO
  on the board. Please pick up in order; I'll review on REVIEW. After this ships + the ops env is set
  (`SKIP_ADMIN_AUTH=false` + creds/secret), REQ-001 live acceptance resumes.

## [13:10] Jason (BE)
- **TASK-013 (ops admin login + real JWT) → REVIEW.** Mirrored scheduling's proven auth into
  `smart-scheduler-backoffice-back` — same token contract, no new dep (`hono/jwt`).
- **Delivered:** `lib/jwt.ts` (sign/verify HS256, `JWT_SECRET`, TTL 43200); `routes/auth.ts`
  `POST /api/v1/auth/login` (env creds, dev fallback admin/admin, wrong→401, `{token,user}`); rewrote
  `middleware/auth.ts` — `adminAuth` + `adminOrService` now **verify** the Bearer JWT (was presence-only
  stubs), **service-token path unchanged** so scheduling's `X-Service-Token` calls keep working; 401 on
  missing/invalid. `.env.example` += `ADMIN_USERNAME/PASSWORD`, `JWT_TTL_SECONDS`, `SKIP_ADMIN_AUTH=true`
  (dev) with a prod-flip note.
- **Verified:** `tsc` clean; `bun test` → **28 pass / 0 fail** (+11: jwt round-trip/wrong-secret/garbage;
  login ok/bad; adminOrService service-token✓/admin-Bearer✓/neither→401; adminAuth no-token/invalid→401,
  valid→pass — real Hono app with `SKIP_ADMIN_AUTH=false`).
- **@Sober: TASK-013 ready for review.** No questions — mirrors scheduling exactly.
- **@Fern: TASK-014 unblocked** — `POST /api/v1/auth/login {username,password}` → `{token,user}`; send
  `Authorization: Bearer <token>` on admin calls (contract in TASK-013 notes).
- **Deploy note for @Porter:** the 403 fix only takes effect when the ops deploy env sets
  `SKIP_ADMIN_AUTH=false` + real `ADMIN_*`/`JWT_SECRET` (folds into the existing deploy gate).
- **BE queue empty again** — TASK-013 was the only BE task in REQ-002; TASK-014 is Fern's. Nothing waiting for Jason.

## [23:05] Fern (FE)
- Nudged onto **REQ-002 / TASK-014** (backoffice admin login) — the go-live blocker for REQ-001. Dep
  TASK-013 is in REVIEW; built against its frozen SPEC-003 contract (Jason delivered it exactly).
  IN_PROGRESS → **REVIEW**.
- **Built (lightweight cookie+JWT per SPEC-003 — no NextAuth):** `lib/auth.ts` (`bo_token` cookie helpers +
  JWT `sub` decode + `safeNext` open-redirect guard), `services/auth.service.ts` (`POST /v1/auth/login`),
  `app/login/page.tsx` (Mantine form outside `(admin)`), `proxy.ts` (Next 16 guard — no cookie → `/login?next=`,
  matcher = the 8 admin pages), `lib/api/client.ts` interceptors (attach `Bearer bo_token`; on 401 clear+redirect,
  skipped for the login call; also fixed stale `3002`→`4010` default), and `Header.tsx` (signed-in username +
  logout).
- **Verified:** `bunx tsc --noEmit` exit 0; `bun run build` exit 0 (`/login` emitted, `ƒ Proxy (Middleware)`
  active). **Live guard check** (dev, no creds, no real env): cleared cookie, hit `/freelance-budgets` →
  **redirected to `/login?next=/freelance-budgets`** + form renders = DoD #1 confirmed end-to-end. ⚠️ The
  correct-creds login round-trip / 401-redirect / `SKIP_ADMIN_AUTH=false` enforcement need the ops API with real
  `ADMIN_*`/`JWT_SECRET` — a deploy step (I entered **no** credentials, touched no real env).
- **@Sober: TASK-014 ready for review** (please review TASK-013 first — I built on its frozen contract). Two
  non-blocking notes in `## Questions` (built-on-REVIEW; JS-readable cookie is per SPEC).
- **Status: REQ-002 fully built (013 + 014), both in your review queue.** Once you accept them + the ops deploy
  flips `SKIP_ADMIN_AUTH=false`, the REQ-001 auth-403 blocker clears and live acceptance can resume. Nothing
  left for FE to build across REQ-001 + REQ-002.

## [13:40] Sober (SA Lead)
- **Reviewed TASK-013 + TASK-014 → both DONE ✅.** Security-critical, so I verified the actual auth logic,
  not just the tests:
  - **TASK-013:** re-ran `backoffice-back` `bun test` → **28 pass / 0 fail**, `tsc` 0. The stub is genuinely
    gone — `adminAuth` `verifyToken`s (no/invalid token → 401, valid → `c.set("user")`); `adminOrService`
    keeps the `SERVICE_TOKEN` path (scheduling's machine calls unbroken) **and** verifies the admin Bearer.
    Login mirrors scheduling's contract, `hono/jwt`, no new dep.
  - **TASK-014:** `tsc` 0; guard verified live by Fern (unauth → `/login?next=`). Request interceptor
    attaches the token; 401 interceptor clears+redirects but **skips `/auth/login`** (wrong creds inline);
    **`safeNext` blocks open-redirect** on `?next` (an unprompted good instinct); `proxy.ts` cookie guard;
    Header shows the real admin + logout. Approved the JS-readable-cookie design (exactly SPEC-003; the ops
    JWT verify is the real control — httpOnly + echo route would be over-engineering here).
- 🔐 **REQ-002 build COMPLETE → `SPEC_DONE`.** SPEC-003 (TASK-013/014) done; the backoffice now has a real
  admin login and ops genuinely enforces the JWT.
- **Folded the go-live env into the deploy artifacts** so it can't be missed: added ops `JWT_SECRET` +
  `ADMIN_USERNAME`/`ADMIN_PASSWORD` (+ `SKIP_ADMIN_AUTH=false`) to the REQ-001 **runbook §1** env table and
  the board deploy gate.
- **@Porter — REQ-002 is yours: acceptance + a small deploy.** To clear REQ-001's auth-403 blocker and
  resume live acceptance:
  1. Set ops env: **`SKIP_ADMIN_AUTH=false`** + **`JWT_SECRET`** + **`ADMIN_USERNAME`/`ADMIN_PASSWORD`**
     (separate creds recommended — still awaiting คุณฟีน's same-vs-separate call, non-blocking; pick values).
  2. Redeploy backoffice-front (:3018) with the login build.
  3. Log in → the create/edit calls that 403'd now pass → **resume REQ-001 runbook §6 live acceptance.**
- **Nothing is waiting on Sober, Jason, or Fern.** REQ-001 (12 tasks) + REQ-002 (2 tasks) are all built,
  reviewed, and verified. Both REQs sit at `SPEC_DONE`; the only remaining work is Porter + human in the
  real environment (deploy REQ-002 auth → resume REQ-001 §6 acceptance → both `DELIVERED`).

## [15:00] Sober (SA Lead)
- REQ-001 + REQ-002 both **DELIVERED** (live, confirmed by คุณฟีน) — noted. Picked up the new
  **REQ-003 (unified teacher onboarding/offboarding, Path B) → IN_SPEC.**
- Ran a **3-repo read-only code sweep** (scheduling / ops / staff-FE) before designing. Key realities:
  - scheduling has **no teacher create/edit/archive** endpoints + **no archive column** (only `active`);
    hard-delete blocked by the bookings FK → archive must be soft. `ops-client` has **no party sync**.
  - ops onboarding writes (party/budget/salary create) are **adminAuth** (service token can't create);
    **no party-deactivate** endpoint and **no salary-terminate** path exist.
  - FE: `bookable` in `toTeacherView` is the **single choke point** — setting it false auto-suppresses a
    teacher everywhere (calendar/booking/course).
- **Wrote `specs/SPEC-004-teacher-onboarding-sync.md` (ACTIVE)** + cut 4 tasks. Central design decision:
  **a new ops `serviceAuth` "teacher-sync" internal API** (onboard/update/offboard/switch-type) rather
  than loosening the admin-create endpoints or giving scheduling ops admin creds — it encapsulates the
  cross-system writes and fills the party-deactivate + salary-terminate gaps in one machine surface.
  Money **creation** stays in the admin UI (REQ #1.2 "set later"); the bridge only creates/deactivates the
  **party** + **closes** old money. Onboard party-create is **blocking** (REQ #1.4 no half-created), with
  idempotent upsert + a reconcile backstop for the rare orphan.
- **Tasks:** TASK-015 (@Jason ops sync API) → TASK-016 (@Jason scheduling CRUD+archive+sync+setup-gate) →
  TASK-017 (@Fern FE add/edit/change-type/archive UI) ; TASK-018 (@Jason reconcile drift report, dep 016).
- **Answered Porter's 2 REQ-003 questions** in the REQ (block-until-money = confirm; future-dated
  non-cancelled bookings block removal = confirm, exact status set pinned). **No open blockers** — the
  admin-only-writes concern is resolved by the serviceAuth sync API (no Porter decision needed).
- **@Jason / @Fern:** SPEC-004 is ACTIVE. Start order: **TASK-015** (Jason, no deps) → **TASK-016** (Jason,
  dep 015) → **TASK-017** (Fern, dep 016); **TASK-018** (Jason) after 016. Set IN_PROGRESS when you start;
  I'll review on REVIEW.

## [15:30] Jason (BE)
- **TASK-015 (ops serviceAuth teacher-sync API) → REVIEW.** No schema change; new router + orchestrator +
  3 reused-table primitives.
- **Delivered:** `POST /api/v1/internal/teacher-sync/{onboard,update,offboard,switch-type}` (all `serviceAuth`).
  `teacher-sync.service.ts` orchestrates: onboard = upsert party (create-or-reactivate+rename, idempotent);
  update = rename/reactivate (404 if none); offboard = party inactive + deactivate FL budget item + terminate
  open salary; switch-type = close old money only (party untouched). New primitives: `upsertPartyByExternal`
  + `updatePartyByExternal` (parties.service), and **`terminateRecurring`** (recurring.service) — sets
  `effective_to` on the open salary row **without a successor** (the missing "stop salary" path; a later
  materialize posts nothing after it).
- **Verified:** `tsc` clean; `bun test` → **33 pass / 0 fail** (added `teacher-sync.test.ts`, 5: serviceAuth
  401-before-handler + validation 400s). ⚠️ DB-runtime paths (upsert idempotency, offboard composite,
  terminate) verified by inspection — no DB (brownfield); reuse the proven TASK-001/005/009 query patterns.
- **@Sober: TASK-015 ready for review.** One non-blocking note in its Questions: idempotency relies on
  find-by-ref since `parties(external_source,external_ref)` has no unique index (SPEC-004 flagged as optional
  hardening) — reconcile (TASK-018) is the backstop; say the word if you want the unique index added now.
- **Next in my queue:** **TASK-016** (scheduling teacher CRUD + archive + sync + setup-incomplete gate) —
  depends on this TASK-015 contract. Holding until your review so I don't build TASK-016 on an unreviewed
  bridge contract (bigger task: a migration + several endpoints + the setup-incomplete gate).

## [23:30] Fern (FE)
- 🎉 Noted **REQ-001 + REQ-002 DELIVERED** — all my FE tasks (003/004/006/010/014) live & confirmed.
- Picked up the queue for **REQ-003** (teacher onboarding/offboarding). My task **TASK-017** depends on
  **TASK-016** (Jason), which is **TODO — not built** (not even REVIEW). Unlike 004/010/006/014 (where my dep
  was a frozen, already-delivered REVIEW contract I could safely parallelize on), TASK-016 hasn't delivered
  the scheduling API TASK-017 consumes — `setupIncomplete` DTO field, `POST/PATCH /teachers`,
  `/archive` (409 on future bookings), `/reactivate`, `?archived`. **Not startable — not setting IN_PROGRESS**
  (respecting Depends-on; won't invent the contract).
- **Prep done (read-only):** reviewed SPEC-004 + TASK-017; confirmed the FE integration points from my area —
  no surprises. When TASK-016 lands I'll: fold `setupIncomplete` into `toTeacherView.bookable` (same
  Teacher/TeacherDTO/dtoToTeacher/toTeacherView files I touched in TASK-004), add
  create/update/archive/reactivate service fns + hooks (invalidate `TEACHERS_KEY`+`CALENDAR_KEY`), and build the
  Add-teacher modal + per-row kebab (edit / change-type-with-money-warning / archive) + Archived list +
  `setupIncomplete` badge (mirroring the existing `overLimit` pattern). SPEC-004 FE section is sound as written.
- **@Sober / @Jason: FE is ready and blocked only on TASK-016.** It's Jason's move (get TASK-015 accepted →
  build TASK-016 + TASK-018). I'll pick up TASK-017 the moment TASK-016 reaches REVIEW/DONE with its contract.

## [16:00] Sober (SA Lead)
- **Reviewed TASK-015 → DONE ✅.** Re-ran `backoffice-back`: `bun test` → **33 pass / 0 fail**, `tsc` 0.
  Read the two gap-filling primitives: `terminateRecurring` correctly closes the **open** salary row
  (`effectiveTo = prevMonthFirstDay(month)`, **no successor**) so the offboard month + later post nothing —
  matches REQ #4.3 + the existing supersede convention; `syncOffboard/switch-type/onboard/update` compose the
  primitives idempotently, `serviceAuth`-guarded. The sync contract the FE/scheduling consume is sound.
- **Answered Jason's unique-constraint question: defer the index.** find-by-ref idempotency is fine for
  single-admin onboarding; a partial unique index would be another migration on the meta-drift-prone ops DB —
  not worth it for launch; reconcile (TASK-018) is the backstop. Noted as optional future hardening.
- Good call by Jason **holding TASK-016** until this bridge contract was accepted (not building on an
  unreviewed contract), and by Fern **not parallelizing** TASK-017 on the unbuilt TASK-016 (correct — unlike
  the earlier frozen-REVIEW contracts, TASK-016's API doesn't exist yet). Right discipline on both.
- **@Jason: TASK-015 accepted — build TASK-016** (scheduling teacher CRUD + archive + sync + setup-incomplete
  gate) against the frozen sync contract, then **TASK-018** (reconcile). **@Fern: TASK-017 stays blocked on
  TASK-016** — pick it up when 016 reaches REVIEW/DONE. Nothing waiting on Sober until 016 lands.

## [16:40] Jason (BE)
- **TASK-016 (scheduling teacher CRUD + archive + sync + setup-incomplete gate) → REVIEW.** Built on the
  frozen TASK-015 contract.
- **Delivered:** `teachers.archived` column + `drizzle/0010_teacher_archived.sql` (hand-written IF-NOT-EXISTS
  + journal entry — `generate` prompts interactively on the meta-drift, same as ops 0003). ops-client
  **blocking** sync fns (onboard/update/offboard/switch-type, throw on failure) + `fetchOpenSalaryTeacherIds`.
  `POST /teachers` (insert + blocking onboard **inside the tx** → sync fail rolls back, no orphan),
  `PATCH /teachers/:id` (name→rename, type-change→close old money this month), `POST /teachers/:id/archive`
  (**409 if future live bookings**, else archive + offboard), `POST /teachers/:id/reactivate`,
  `GET /teachers?archived=`. `setupIncomplete` = pure `isSetupIncomplete` (FREELANCE⇒budget, FT/PT⇒open salary;
  archived never incomplete), attached on teacher DTOs + calendar, and enforced as a **server-side booking
  guard** in `insertBooking` (also rejects archived).
- **Verified:** `tsc` clean; `bun test` → **78 pass / 0 fail** (+7: `isSetupIncomplete` 5 cases,
  `onboardOpsTeacher` resolves-2xx / **throws-500** = create-rollback proof). ⚠️ DB-runtime paths (rollback,
  future-bookings 409, archive/type-change round-trips, the guard) verified by inspection — no DB (brownfield);
  money-rule + blocking-throw contract are unit-tested.
- **@Sober: TASK-016 ready for review.** 2 non-blocking questions in its Questions: (1) migration journal
  entry without a meta snapshot (rides the known meta-drift the deploy gate reconciles) — keep or omit? (2)
  ops-sync failure currently surfaces as 500 (data stays consistent via rollback) — leave, or map to a 502?
- **@Fern: TASK-017 unblocked** — DTO has `archived`+`setupIncomplete`; endpoints listed in TASK-016 notes.
- **Next in my queue:** **TASK-018** (teacher↔ops drift reconcile report) — depends on TASK-016. Holding for
  your review before building the reconcile on top.

## [17:10] Sober (SA Lead)
- **Reviewed TASK-016 → DONE ✅** (with one tiny fast-follow). Re-ran `smart-scheduler-back`: `bun test` →
  **78 pass / 0 fail**, `tsc` 0. Verified the correctness-critical paths in the real code:
  - `createTeacher` is fully inside `db.transaction` — a blocking `onboardOpsTeacher` throw rolls the whole
    insert back → no teacher without a party (REQ #1.4); the throws-on-500 test proves it.
  - `archiveTeacher` blocks on `date >= today AND status NOT IN (CANCELLED, NO_SHOW)` → 409, else archives +
    offboards; `getTeachers({archived})` filter + reactivate + type-change→switch-type all correct.
  - Booking guard rejects `archived` + `setupIncomplete`; the money-gate's **fail-open-when-ops-down** call is
    the right safety choice (won't hide the whole roster if ops is unreachable). Migration is `ADD COLUMN IF
    NOT EXISTS` (safe).
- **Answered Jason's 2 questions:** keep the migration journal entry (goes in the REQ-003 deploy gate, like
  ops 0003); **map the ops-sync failure to 502 `OPS_SYNC_FAILED`** — small fast-follow under TASK-016, cleaner
  contract for the FE (retry message). Doesn't block TASK-017/018.
- **Tracking for the eventual REQ-003 deploy:** scheduling migration **`0010_teacher_archived.sql`** must be
  applied (I'll fold it into the REQ-003 deploy note when the REQ hits SPEC_DONE).
- **@Fern: TASK-017 is unblocked** — the scheduling contract is DONE (`archived`+`setupIncomplete` on the DTO;
  `POST/PATCH /teachers`, `/archive` [409 `HAS_FUTURE_BOOKINGS`], `/reactivate`, `?archived=true`; treat a
  create/edit/archive **502 `OPS_SYNC_FAILED`** as "backoffice sync failed — retry"). Please build it.
- **@Jason: build TASK-018** (reconcile) + fold in the 502 mapping. Both unblocked by TASK-016.
- Status: REQ-003 = **2/4 tasks DONE** (015, 016). TASK-017 (Fern) + TASK-018 (Jason) now in flight.

## [17:40] Jason (BE)
- **TASK-018 (teacher↔ops drift reconcile) → REVIEW** + folded in the **TASK-016 502 fast-follow**.
- **TASK-018:** `GET /teachers/reconcile` (admin) — no new ops endpoint; three **throwing** ops GETs
  (parties / FREELANCE_BUDGET items / open salary rows) so drift isn't masked by best-effort empties, then a
  pure `reconcileTeacherDrift` → `{missingParty, orphanParty, moneyForArchived, incompleteActive, repairHint}`.
  Read-only; ops unreachable → clean **502 `OPS_UNREACHABLE`** (no false drift). Repair is manual (re-run
  onboard/offboard).
- **TASK-016 fast-follow (your call):** wrapped the 5 blocking sync calls in `opsSyncOr502()` → an ops-sync
  failure now surfaces as **502 `OPS_SYNC_FAILED`** (DB errors keep their own codes; the throw still rolls back
  the tx). @Fern's "backoffice sync failed — retry" message is now backed by a real code.
- **Verified:** `tsc` clean; `bun test` → **83 pass / 0 fail** (added 5 `reconcileTeacherDrift` cases — one per
  bucket + clean-teachers-in-none). ⚠️ HTTP/DB runtime verified by inspection (brownfield); diff logic pure-tested.
- **@Sober: TASK-018 ready for review** — no questions. This is the **last REQ-003 BE task**; all SPEC-004
  backend (015/016/018) is in. One transparency note in its Questions/Notes: reconcile reads the public
  `GET /parties` (active only) — fine since orphans-from-failed-onboard are active and teachers are never
  hard-deleted.
- **BE queue empty for REQ-003.** Only **TASK-017 (Fern)** remains before REQ-003 → SPEC_DONE. Nothing waiting
  on Jason.

## [18:00] Sober (SA Lead)
- **Reviewed TASK-018 → DONE ✅.** Re-ran `smart-scheduler-back`: `bun test` → **83 pass / 0 fail**, `tsc` 0.
  Read the pure `reconcileTeacherDrift` — all four buckets keyed correctly (archived+money→`moneyForArchived`;
  active+no-party→`missingParty`; active+party+no-money→`incompleteActive`; party-ref+no-teacher→`orphanParty`).
  The three ops GETs **throw** (not best-effort) → clean `502 OPS_UNREACHABLE` instead of false drift from
  empties. Read-only, admin-guarded, manual repair via the TASK-016 endpoints. Acked Jason's scope note (public
  `/parties` active-only is sufficient — no hard-delete means the missed combination can't occur).
- **The 502 `OPS_SYNC_FAILED` fast-follow is in** (Jason wrapped the 5 blocking sync calls in `opsSyncOr502()`;
  tx still rolls back) → the TASK-016 amendment is closed, and TASK-017's error contract is now backed by a
  real code.
- 🔄 **All REQ-003 backend is DONE (015/016/018).** Status: **3/4 tasks DONE** — only **TASK-017 (Fern,
  IN_PROGRESS)** remains (the teacher add/edit/change-type/archive UI + `setupIncomplete` gate).
- **Nothing is waiting on Sober** until TASK-017 reaches REVIEW. When it lands and I accept it, REQ-003 →
  `SPEC_DONE` and I'll write the **REQ-003 deploy note** (its one real-env step: apply scheduling migration
  `0010_teacher_archived.sql`, same meta-drift reconcile as ops 0003 — no scheduled tasks/seed needed).
- **@Fern:** carry on with TASK-017; ping via REVIEW. **@Jason:** REQ-003 BE complete, nothing waiting on you.

## [23:55] Fern (FE)
- **TASK-016 landed DONE** → picked up **TASK-017** (teacher lifecycle UI). IN_PROGRESS → **REVIEW**.
- **Built (scheduler-front):** the `setupIncomplete` gate first — added `setupIncomplete`+`archived` to
  DTO/Teacher/mapper and folded into `toTeacherView.bookable = active && !overLimit && !setupIncomplete`
  (single choke point → auto-hides not-set-up teachers from calendar + booking/course modals). Then service
  fns + hooks + mock stubs (create/update/archive/reactivate/archived-list, invalidating teachers+calendar),
  a `syncErrorMessage` helper (409 `HAS_FUTURE_BOOKINGS` / 502 `OPS_SYNC_FAILED`), and the UI: header
  "เพิ่มครู", per-row `setupIncomplete` badge + disabled active switch, a per-row kebab (Edit / Change-type
  with the "switches pay model this month" warning / Archive with the future-bookings block), an Add/Edit
  form modal, and an Archived-teachers section with Re-activate. i18n keys added (en+th).
- **Verified:** `bunx tsc --noEmit` exit 0; `bun run build` exit 0 (`/scheduler/teachers` prerendered — SSG
  ran the new tree, no crash). `bookable` change only *adds* suppression → no regression for existing teachers.
  ⚠️ Live render not driven — page is NextAuth-gated (→ prod login) + real frontoffice API (brownfield); I did
  not authenticate. Same accepted posture as TASK-004.
- **@Sober: TASK-017 ready for review.** One real gap in `## Questions`: **no subjects catalog on the FE** — I
  populated the subjects picker from the union of existing teachers' subjects (can't create a brand-new
  subject). Please pick (a) accept for launch / (b) small `GET /subjects` task / (c) allow subject-names on
  `POST /teachers`. Non-blocking for the lifecycle core.
- **REQ-003 now fully built** (015/016/018 DONE, 017 in your queue). After you accept 017, all three
  requirements (001/002/003) are code-complete — only deploy/ops steps remain. Nothing left for FE to build.

## [18:30] Sober (SA Lead)
- **Reviewed TASK-017 → DONE ✅.** Re-ran `scheduler-front` `tsc` → exit 0. Verified the gate + contract:
  `bookable = active && !overLimit && !setupIncomplete` (single choke point → not-set-up teachers auto-drop
  from calendar/booking/course; existing teachers default false → no regression); `teacher-errors.ts` maps
  `HAS_FUTURE_BOOKINGS`/`OPS_SYNC_FAILED` to the right messages (Jason's exact codes); create/update/archive/
  reactivate/archived + `setupIncomplete` badge all built per SPEC. No rework.
- **Answered Fern's subjects question: (a) union-of-existing for launch** — creating a brand-new program/
  subject is out of REQ-003 scope; not cutting a task. **Routed it to @Porter** as a known limitation (board
  Blocked row) for คุณฟีน to confirm — if she needs to add new programs via the UI, that's a future small REQ.
- 🔄 **REQ-003 COMPLETE → `SPEC_DONE`.** SPEC-004 (015/016/017/018) all DONE; every task independently
  re-verified (tests/tsc per repo). End to end: add a teacher in the frontoffice → ops party auto-created
  (blocking, no orphan); not bookable until money is set (backoffice) → `setupIncomplete` gate; edit syncs the
  party; change type closes old money effective-dated; archive is blocked if future bookings else soft-archives
  + deactivates party + stops money (past P&L frozen); reactivate restores; a reconcile report surfaces drift.
- **@Porter — REQ-003 is yours: acceptance + a small deploy** (board Blocked row): apply scheduling migration
  **`0010_teacher_archived.sql`** (`ADD COLUMN IF NOT EXISTS`; same `__drizzle_migrations` meta-drift reconcile
  as ops 0003) + redeploy scheduling-back (:4006) & scheduler-front (:3016). No scheduled tasks / seed needed.
  Plus the non-blocking subjects limitation to confirm with คุณฟีน.
- **All three requirements (001/002/003) are code-complete.** 001/002 DELIVERED; 003 SPEC_DONE awaiting its
  small deploy. **Nothing is waiting on Sober, Jason, or Fern** — the ball is entirely with Porter + human
  (REQ-003 migration/redeploy; plus the still-open REQ-001 ops follow-ups: 2 scheduled tasks + real numbers).

## [19:00] Sober (SA Lead)
- **STRATEGIC PIVOT noted (คุณฟีน): backoffice is being torn down/rebuilt** → finish the frontoffice
  standalone. **REQ-003 → ON HOLD (do NOT deploy** — its ops sync is moot); its code stays for later rework.
  Picked up the new **REQ-004 (re-home the freelance limit into the frontoffice, standalone) → IN_SPEC.**
- Read REQ-004 + re-read the current `ops-client.ts` freelance wiring (the exact fns to re-home:
  `drawFreelanceBudget`/`releaseFreelanceBudget`/`fetchTeacherQuotas`/`attachTeacherQuotas` + the ops
  month-start reset). **Wrote `specs/SPEC-005-frontoffice-standalone-freelance-limit.md` (ACTIVE)** + 2 tasks.
- **Design:** new scheduling `public` table **`freelance_budgets`** (budget/rate/remaining/near-cap); the
  drawdown/reversal/cap move **into the booking DB tx (now atomic** — removes the cross-system orphan edge
  and the 5-min cache lag I flagged back in TASK-002); a **scheduling** monthly-reset internal job replaces
  the retired ops one; admin set/edit/top-up moves to the **frontoffice** Teachers page. All ops freelance
  calls are switched off (functions left dormant). **Limit-only** — no P&L/expense (deferred); FT/PT deferred.
  Nice property: the FE display is **unchanged** (same DTO field names, just locally sourced) → TASK-020 is
  only the new set/edit/top-up controls.
- **Cut 2 tasks:** **TASK-019** (@Jason, scheduling — table + local service + booking rewire + month-reset +
  drop ops calls) → **TASK-020** (@Fern, scheduler-front — set/edit/top-up controls).
- **Answered Porter's 3 REQ-004 questions** (in the REQ): re-enter budgets (placeholder, small — no migration);
  scheduling-side monthly job (one more Windows task at deploy); leave ops freelance code dormant. No blockers.

## [19:40] Jason (BE)
- **TASK-019 (re-home freelance limit into scheduling, standalone) → REVIEW.** The freelance limit is now
  **100% local — zero ops calls** on the booking/teachers/cap paths.
- **Delivered:** new `public.freelance_budgets` table + `drizzle/0011_freelance_budgets.sql`; pure
  `lib/freelance-budget.ts` (`freelanceDraw`/`overLimit`); `attachFreelanceBudgets` re-sources the DTO budget
  fields from the local table (**same field names → FE unchanged**) + local `setupIncomplete` (freelance-only);
  **draw at confirm + reverse at cancel/leave are now local and in the booking DB tx (atomic)** — removes the
  cross-system orphan + 5-min cache lag I flagged in TASK-002; `setFreelanceBudget`/`topUpFreelanceBudget`/
  `resetFreelanceBudgets` + `PUT /teachers/:id/budget`, `POST …/budget/topup`, and
  `POST /internal/jobs/month-reset`. **Stopped all ops freelance calls** (left dormant in `ops-client.ts` per Porter).
- **Verified:** `tsc` clean; `bun test` → **88 pass / 0 fail** (+5 pure cap-math cases). ⚠️ in-tx draw/reverse,
  upsert/top-up/reset, local setupIncomplete verified by inspection (brownfield); cap math pure-tested.
  Standalone AC holds — the whole freelance path is local, works with `OPS_API_URL` unset.
- **@Sober: TASK-019 ready for review.** One **heads-up for the pivot** (in Questions): the freelance *limit*
  is standalone, but **teacher CRUD (TASK-016) still calls ops** (create/archive → onboard/offboard, blocking) —
  with the backoffice down those 502. That's REQ-003 (ON HOLD) scope, left as-is, but flagged so the REQ-003
  rework makes teacher-management standalone too. Also asked whether month-reset needs a mid-month re-run guard.
- **@Fern: TASK-020 unblocked** — the DTO display already works locally; you only add the set/edit/rate/near-cap/
  top-up controls (`PUT /teachers/:id/budget` `{monthlyBudgetMinor, rateMinor, reorderMinor?}`, `POST …/budget/topup`
  `{amountMinor}`), invalidate `TEACHERS_KEY`(+`CALENDAR_KEY`).
- **BE queue empty for REQ-004** (TASK-019 is the only BE task). Nothing waiting on Jason.
- **@Jason: SPEC-005 ACTIVE — build TASK-019** (no deps); **@Fern: TASK-020** waits on TASK-019. Set
  IN_PROGRESS when you start; I'll review on REVIEW.
- Note: this **supersedes the ops/backoffice freelance-budget** (TASK-001/002/003/010 ops+backoffice parts
  go dormant); scheduling migration for REQ-004 (`freelance_budgets`) + the new month-reset task join the
  REQ-004 deploy note when it reaches SPEC_DONE.

## [20:00] Sober (SA Lead)
- **Reviewed TASK-019 → DONE ✅** (one required fast-follow). Re-ran `smart-scheduler-back`: `bun test` →
  **88 pass / 0 fail**, `tsc` 0. Verified: pure `freelanceDraw`/`overLimit` (unit-tested); the draw/reverse are
  **local + inside the booking tx** (blocked→409 rolls back the confirm) — atomic, no orphan/cache lag (a real
  improvement over the old cross-system path); `setFreelanceBudget` preserves `remaining` on edit; migration is
  `CREATE TABLE IF NOT EXISTS` + guarded FK; **no ops freelance calls on the live path** → standalone AC holds.
- **Answered Jason's 2 questions:**
  1. **Month-reset guard — required fast-follow:** make it **idempotent-per-month** (record last-reset month in
     `app_settings`, no-op if already reset; `force` flag for a deliberate re-run). An accidental mid-month
     re-run would wipe the month's drawdowns — worth guarding before the Windows task goes live. Folds into
     TASK-019; doesn't block TASK-020.
  2. **Teacher-CRUD-not-standalone (his catch) → routed to @Porter.** Correctly out of REQ-004 scope, but a real
     **pivot gap**: teacher add/edit/archive (TASK-016) still calls ops **blocking**, so once the backoffice is
     offline they **502** and break teacher management. Added a board Blocked row — needs a **follow-up REQ** to
     make the teacher↔ops sync best-effort/removed (companion to the REQ-003 rework) **before** the backoffice
     is torn down. Good instinct by Jason to flag it.
- **@Fern: TASK-020 unblocked** — the DTO display already works from local data; add the set/edit (budget/rate/
  near-cap) + top-up controls on the Teachers page. **@Jason:** fold in the month-reset idempotency guard.
- Status: REQ-004 = **1/2 DONE** (TASK-019). TASK-020 (Fern) is the last build task for REQ-004.
- **@Porter — 1 new item for you** (board Blocked): scope a follow-up to make **teacher CRUD standalone**
  (the pivot needs it before the backoffice goes offline). Non-blocking for REQ-004 itself.

## [00:30] Fern (FE)
- Noted the **strategic pivot**: REQ-003 ON HOLD (backoffice being torn down; my TASK-017 accepted but its
  ops-sync is moot), new **REQ-004** re-homes the freelance limit into the frontoffice, standalone.
- Picked up **TASK-020** (frontoffice freelance budget admin) — dep TASK-019 DONE. IN_PROGRESS → **REVIEW**.
- **Built (scheduler-front):** no display change (TASK-019 kept the DTO field names, so `FreelanceRow` renders
  as-is). Added `setFreelanceBudget` (`PUT /teachers/:id/budget`) + `topUpFreelanceBudget`
  (`POST /teachers/:id/budget/topup`) service fns + mock stubs + hooks (invalidate teachers/calendar), and a
  `FreelanceBudgetControls` component on each FreelanceRow: **ตั้ง/แก้งบ** modal (monthly budget / rate / near-cap,
  baht→satang; "edit = next-reset target, use Top-up for remaining now") + **เติมงบ** modal (adds to remaining now).
  i18n keys added (en+th). setupIncomplete badge already handled by TASK-017 — no change.
- **Verified:** `bunx tsc --noEmit` exit 0; `bun run build` exit 0 (`/scheduler/teachers` prerendered, SSG ran
  the new tree). ⚠️ Live render not driven — NextAuth gate (→ prod login) + real API (brownfield); did not
  authenticate. Modals reuse the proven TASK-003 create/top-up patterns; PUT/POST is Jason's DONE TASK-019.
- **@Sober: TASK-020 ready for review.** No blocking questions.
- **REQ-004 now fully built** (TASK-019 DONE, TASK-020 in your queue). The freelance limit is now standalone
  in the frontoffice (no ops dependency) once you accept 020 + the scheduling `0011_freelance_budgets` migration
  is applied. Nothing left for FE to build.

## [20:30] Sober (SA Lead)
- **Reviewed TASK-020 → DONE ✅.** Re-ran `scheduler-front` `tsc` → exit 0. `setFreelanceBudget`/
  `topUpFreelanceBudget` hit exactly TASK-019's endpoints (`PUT /teachers/:id/budget`, `POST …/budget/topup`);
  set/edit + top-up modals with the "edit = next-reset target" messaging; display unchanged (local-sourced DTO)
  → no regression. Approved Fern's per-FreelanceRow control placement (budget is freelance-only → reads better
  than the all-types lifecycle kebab).
- 🔄 **REQ-004 COMPLETE → `SPEC_DONE`.** SPEC-005 (019/020) DONE. The freelance limit is now **fully standalone**
  in the scheduling app — local `freelance_budgets` table, drawdown/reversal/cap **atomic in the booking tx**,
  scheduling monthly-reset, frontoffice set/edit/top-up, **zero ops calls** (works with the backoffice offline).
- **@Porter — REQ-004 is yours: acceptance + deploy** (board Blocked "REQ-004 deploy"): apply migration
  `0011_freelance_budgets`, register the monthly **month-reset** Windows task (:4006), redeploy scheduling +
  front, re-enter budgets. **Pre-deploy:** @Jason's month-reset idempotency guard (TASK-019 fast-follow) should
  land first.
- **Roadmap status:** REQ-001/002 DELIVERED · REQ-003 SPEC_DONE but **ON HOLD** (backoffice sync moot) ·
  **REQ-004 SPEC_DONE** (the standalone freelance limit — the pivot's first deliverable). **Nothing is waiting
  on Sober, Jason, or Fern.** Open for Porter: (1) REQ-004 deploy; (2) the **teacher-CRUD-standalone** follow-up
  REQ (needed before the backoffice goes offline); (3) REQ-001 ops follow-ups (scheduled tasks + real numbers).
