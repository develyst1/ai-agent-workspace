# Log — 2026-09-02 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [00:0x] Fern (FE) — ✅ TASK-241 → REVIEW. Dialog fixed; the CHIP has the same fault one screen later. → @Sober
📌 **My session crossed midnight — this entry belongs to 2026-09-02**, which is why this file exists. The
TASK-237 and TASK-241 work itself was done on 09-01 and its evidence is in those TASK files, not here.

`H:\scheduler\smart-scheduler-front`, HEAD **`bda6511`**. **Verified:** `bunx tsc --noEmit` **exit 0** ·
`bun test` **82 pass / 0 fail** (unchanged — this task adds no logic; the two new keys are covered by
`keys.test.ts`, which resolves every literal `t()` in **both** languages) · `bun run build` ok.

**The dialog** now names every assigned teacher when there is more than one, from `booking.teachers` — the one
accessor, no second source (grep of the diff: the sole name read is `assigned.map(tc => tc.nickname || tc.name)`).
🔴 **The single-teacher path is the SAME key with the SAME argument as before**, so "byte-identical for the four
existing types" is true by construction, not by testing. I deliberately did **not** switch it to
`booking.teachers[0].name` — that would arguably be *better* (it never falls back to `"-"` for a teacher missing
from the loaded roster) but it would be a **different string** in that edge case, and this task asked for
byte-identical. Flagging the trade rather than taking it silently.

**One addition, declared:** `confirmMsgMulti` carries a **count** as well as the names. With names alone, *a list
that silently lost one reads exactly like a correct list* — which is this defect's own failure mode arriving a
second time. Not @Porter's wording, so his to strip (Q2).

### ⚠️ §2's finding: the post-confirm chip is wrong for a multi-teacher booking, and the FE cannot fix it alone
Traced both sides rather than inferred. **BE** `scheduler.service.ts:2486` — `notification` is assigned **only**
for the primary. **Wire** `contract.ts:411` — one object, **no recipient identity, no array**. ⇒ two teachers,
primary linked and the second not: **the chip says "ส่ง LINE แล้ว" while the second teacher's row is `SKIPPED`.**
The send is correct; the report is not. **Exactly DEF-6's shape, one screen later.**

The FE could stop over-claiming without any BE change — but that makes the chip **less** useful in the common
single-teacher case, and it is a wording decision, not mine. Making it genuinely right needs per-recipient
results on the wire (`{teacherId, status, reason}[]`), which changes `UpdateBookingStatusResponse` for every
caller. 🔴 **Named, not built**, per your §2. **Q1: it needs its own ruling and it should go to @Porter WITH
DEF-6, not after it** — he is already holding the owner's Q11, and "did everyone get it?" is a business question
about what that screen should promise.

**Rendered check → LOCAL, like TASK-147/226, not the `sid` queue:** mock fixture `b13` is a three-teacher อื่นๆ
booking and mock auth takes any credentials, so the multi-teacher dialog is reachable offline in a minute.
Command + what to look at are in the TASK. I could not run it myself (auth wall — no password from me).

**Ball: @Sober** — TASK-241 review + Q1 (the chip ruling) / Q2 (the count). Nothing else is open for FE:
TASK-111 waits on TASK-110, TASK-118 on TASK-116; everything else of mine is code-DONE with its rendered checks
in @Tanya's queue.

## [--:--] Sober (SA) — ✅ TASK-241 PASS → 🏁 **REQ-078’s build is COMPLETE. Every task is DONE.** → @Porter @Tanya @Fern @Jason

**Reproduced:** `tsc` **0** · `bun test` **82/0** · `build` ok · `confirmMsgMulti` wired at `BookingModal.tsx:210`
and present in **both** dictionaries.

### The best call in the task is the "improvement" she refused to make

She kept the single-teacher branch on the **same key and the same argument**, and deliberately did **not** switch
it to `booking.teachers[0].name` — because `teacherName` resolves through the loaded roster and falls back to
`"-"` for an archived or not-bookable-today teacher. **Reading the DTO would arguably be better and would change
the rendered string in that edge case.** The task said byte-identical; she noticed "obviously equivalent" was not
equivalent and said so instead of quietly upgrading it. Same class as her `?.value` refusal on TASK-237: **the
tempting version is indistinguishable from the correct one until it is wrong in front of someone.**

### 🔴 @Porter — two rulings, and I am giving you one recommendation each rather than two halves

**Q2 — the count in the dialog (`ระบบจะส่งตารางให้ครู {n} คน …`). My recommendation: KEEP.** It is not in your
DEF-6 wording and she declared it rather than slipping it in. Her reason is the strong one: **with names alone, a
list that silently lost one reads exactly like a correct list** — this defect’s own failure mode, arriving a
second time. Same principle as `link-all`’s per-teacher dry-run output: **a summary that cannot look wrong is a
summary nobody checks.** Yours to strip; one line.

**Q1 — the post-confirm chip. It is a false statement on screen, and the FE cannot fix it alone.** She traced
both sides: `UpdateBookingStatusResponse.notification` is **one object with no recipient identity**
(`contract.ts:411`), and the BE assigns it only for the primary (`scheduler.service.ts:2486`). ⇒ with two
teachers, one linked and one not, the chip says **"ส่ง LINE แล้ว"** while the second teacher’s row is `SKIPPED`.
The send is right; the report is not — **DEF-6 one screen later.**

**My recommendation: (b) per-recipient results — but NOT now and NOT in REQ-078.** It changes
`UpdateBookingStatusResponse` for every caller of `updateBookingStatus`, and the owner has ordered this the last
round. ⇒ it goes up as a **named follow-up**, and the question that decides it is one line for him: **does he
want the screen to answer "did everyone get it?"** If yes, it is a small BE contract task; if no, the chip’s
meaning gets **documented** as "the primary teacher". 🔴 What it must not be is left as "looks fine" — this is
the kind of thing that is rediscovered as a defect in six months.

### 🏁 REQ-078 — the build is complete and it is out of my hands

Every task DONE: **224 · 225 · 226 · 227 · 228 · 229** and the defect round **236 · 237 · 238 · 239 · 241**.
The board row stays **`TEST_FAILED`** — correctly, and per your line it stays there unless @Tanya passes it.
**Parked ≠ delivered ≠ done**, and I am not moving it.

**@Tanya — the three I would spend the last round on**, unchanged from my previous entry and now all buildable:
1. **DEF-5’s two paths** (toggle Charge → type in Title). The fix *predicts* they are gone; that prediction is
   the only part of the diagnosis not yet tested against the running product.
2. **`SICK_LEAVE` must NOT refuse an additional teacher** — too-wide a guard breaks **UC-004 overbooking**, far
   older than อื่นๆ, and it would look like an unrelated regression.
3. **The bookings list rendering the rows it counts** (DEF-3) — the joins are proven in SQL, the page is not.

📌 And the two LOCAL checks (TASK-226/241 dialogs, TASK-147 help lines) **must not eat your `sid` round** — the
mock covers them, `b13` is the three-teacher fixture, mock auth takes any credentials.

**My lane is now `REQ-079`** per your order, with SPEC-071’s tasks 230–235 still held on your Q1 (the AC-17
trigger / 30-second OA Manager test) and Q2 (a parent on a PC cannot start Flow 2).

**BALL: @Porter — release @Tanya’s final round.** The build is complete; she is holding on your line, and the
two rulings above can travel with it.

## [00:2x] Porter (PM) — ✅ Q1/Q2 ruled. 🧪 **@Tanya's final round is RELEASED.** 🔴 And your REQ-079 hold is stale. → @Sober @Tanya

### 🔴 First, the important one: SPEC-071's tasks 230–235 are NOT blocked. Both your questions are answered.

You wrote that they are *"still held on your Q1 and Q2"*. **Both were answered yesterday and I think you missed
them** — the owner wants the LINE work moving, so this is the line that matters most in this entry:

- **Your Q1 (the AC-17 trigger)** — 🔴 **ANSWERED, and the answer is NO.** The owner ran your 30-second test:
  he replied in OA Manager on `sid` and **no `[line-in]` appeared** — only the customer's own inbound messages.
  ⇒ **an admin's reply is outbound and never reaches our webhook; the automatic trigger cannot be built.**
  Ruling: **build `muted_until` and wire the two inbound triggers only** (the parent's `คุยกับแอดมิน`, and the
  two-strikes handover). **AC-17 is REWRITTEN, not withdrawn** — full text in `REQ-079` §16, plus the fallback
  (a *"หยุดบอทในแชทนี้"* control in our own back office) which is **noted and NOT to be built now**.
- **Your Q2 (a parent on a PC cannot start Flow 2)** — **MOOT.** The customer killed the 6-digit code (§15);
  **Flow 2 no longer exists.** Everyone — mother, father, new phone — joins by an admin opening the door.
  📌 Your finding is why losing it is safe, and it is recorded as such: you proved Flow 2 was *impossible* on PC,
  not merely awkward. **No typed-keyword exception is needed, so AC-16 stays absolute.**
- **Q3 (hashing the code)** — moot with the code.

⇒ **Release 230–235.** ⚠️ Re-cut what §15 touches first: the code table, the invite/lockout state, and Flow 2's
entry (TASK-232). **The attempt counter you called the only new state is gone.**

### Q2 (the dialog count) — KEEP. Your recommendation, taken, and Fern's reason is the right one.

*"With names alone, a list that silently lost one reads exactly like a correct list"* — **this defect's own
failure mode arriving a second time.** Same principle as `link-all`'s per-teacher dry run: **a summary that
cannot look wrong is a summary nobody checks.** It is not my wording and I am not stripping it. **She was right
to declare it rather than slip it in, and right to add it.**

### Q1 (the post-confirm chip lies) — RULED, and neither of your two options

The chip says `ส่ง LINE แล้ว` while an unlinked second teacher's row is `SKIPPED`. **The owner ruled hours ago
that the screen must be honest about every teacher** (*"ส่งไลน์หาครูทุกคน แก้กล่องยืนยันด้วย"*) — so leaving a
false sentence is against his stated intent, and *documenting* the chip's meaning is not a fix: **nobody reads a
doc, they read the chip.**

**My ruling — make it TRUE cheaply now, make it COMPLETE later:**
1. **Change the words, not the contract.** The chip reports the primary ⇒ say so: **`ส่ง LINE ถึงครูหลักแล้ว`**.
   One FE line, no contract change, and **the screen stops making a claim it cannot support.**
2. **Per-recipient results = a named follow-up REQ**, exactly as you scoped it, carrying the owner's one-line
   question: *does he want the screen to answer "did everyone get it?"*
3. 🔴 **Do NOT reopen this round for it.** The owner said last round and Fern has finished. **The wording line
   rides along only if Tanya's round forces an FE touch anyway** — otherwise it ships with the follow-up.
   I would rather carry one honest known gap than break his instruction on my own initiative.

### 🧪 @Tanya — final round, RELEASED. Build is complete; nothing is waiting on the team.

Scope, and it is deliberately short — **Sober's three, plus the two queries:**
1. **DEF-5's two paths** (toggle Charge → type in Title). The fix *predicts* they are gone; **that prediction is
   the only part of the diagnosis untested against the running product.**
2. **`SICK_LEAVE` must NOT refuse an additional teacher** — UC-004 overbooking is far older than อื่นๆ and would
   surface as an unrelated regression.
3. **DEF-3 — the bookings list must render the rows it counts.** The joins are proven in SQL; the page is not.
4. The two **query results** the owner runs in the morning (the ฿20 movement + F4's outbox rows).
📌 The LOCAL dialog checks must not eat your `sid` round — mock, fixture `b13`.

**Then write the verdict, whatever it is, and the park note** (every AC's final state incl. my three accepted
gaps AC-6/7/8 · the live fixtures with ids and who retires them · the one thing a fresh session does first).
🔴 **A second `TEST_FAILED` is a legitimate outcome and nobody will lean on you to avoid it.**

## [--:--] Porter (PM) — ✅ **Deployed on `sid` (owner). @Tanya — the final round starts NOW.** → @Tanya @Sober
The owner confirmed the deploy. The build carries the whole defect round: **TASK-236 · 237 · 238 · 239 · 241**.
⚠️ Code-only — `0029` was applied and witnessed earlier; **`db:verify` still gated the restart, as always.**

**@Tanya — everything you were holding for is now on the box. Scope unchanged from my release:**
1. **DEF-1 + DEF-5's two paths** — toggle Charge → type in Title, and remove the last teacher chip → type.
   🔴 **The fix PREDICTS both are gone. That prediction is the only part of Fern's diagnosis never tested against
   the running product** — everything else about it was proven in a test that fails without the fix.
2. **`SICK_LEAVE` must NOT refuse an additional teacher** (UC-004 overbooking) — older than อื่นๆ, and a
   too-wide guard would look like an unrelated regression nobody connects to this feature.
3. **DEF-3 — the bookings list must RENDER the rows it counts.** The joins are proven in SQL; the page is not.
4. **DEF-6's dialog** — every assigned teacher named, plus Fern's count line.
5. The **two query results** the owner runs this morning (the ฿20 movement · F4's outbox rows).

📌 The LOCAL dialog checks must not eat the `sid` round — mock, fixture `b13`.
🔴 **Then the verdict, whatever it is, and the park note.** AC-6/7/8 are recorded as **accepted gaps, not
passes**. A second `TEST_FAILED` is a legitimate outcome and nobody will lean on you to avoid it.

**Still with the owner this morning:** the `bo.movement` SELECT + the F4 outbox SELECT, and reversing the ฿20.
**@Sober — REQ-078 is out of your hands; your lane is `REQ-079` with 230–235 released.**

## [--:--] Sober (SA) — 🔄 **SPEC-071 re-cut for §15/§16. Tasks 230–235 RELEASED.** You were right: my hold was stale. → @Jason @Fern @Porter

**My hold was stale and I should have caught it.** You answered Q1 and Q2 yesterday; I carried "still held" into
my own ball line. **The lesson is the one this project keeps teaching me: a blocker is a claim with a timestamp,
not a state** — the same shape as the "safe for now" residue that went live on me two days ago. I re-read before
writing this time and that is how it got caught.

### The code being cut makes the build materially smaller, and one thing must NOT go with it

Deleted from SPEC-071: `family_code_hash` · `family_code_set_at` · `code_attempts` · `code_locked_until` · the
weak-code check and its `app_settings` switch · **Flow 2 in every form** · Q3. **AC-4/6/7/8 withdrawn.**

⚠️ **One precision, because your release note conflates two counters and one of them survives.** *"The attempt
counter you called the only new state is gone"* — that is the **code lockout** (`code_attempts`). It is **not**
the **two-strikes** counter (`unexpected_count`): Rule 5 and **AC-18** still require *"two unexpected replies and
the bot hands over"*, which still needs a per-conversation count. **I have written that distinction into the
spec and into TASK-230**, because deleting both on one sentence is exactly the kind of thing that happens.

### §6b got MORE load-bearing, not less

A chat still cannot be addressed until it speaks — so the door is still **something the parent types**. With the
code gone, **the invite is the only way anyone ever joins a family**: mother, father, grandmother, a new phone.
It was one of two doors; it is the only one. ⇒ `family_invites` + `family_line_links` **are** the data model.
📌 And that is why **TASK-235 is not a throwaway.** Your own §15 line — *if the trade ever becomes a complaint,
the fix is to make "open the door" one tap from where staff already are* — is that task's brief, so I have
written it to optimise for **getting a code into the clipboard in one action**, not for a pretty form.

### 🔴 §16's second finding changes what TASK-231 IS, and it is the sharpest item in the batch

From the same screenshot: the **deployed** bot answers stray text in an idle chat — `เมนู` → *"เบอร์โทรไม่ถูกต้อง…"*,
`yo` → *"ไม่พบครูชื่อเล่น yo"* — **while a human was about to reply.**

⇒ **AC-16 is a CHANGE TO SHIPPED BEHAVIOUR, not a new capability.** It is the one place in this REQ where we take
something away from a running system real teachers use. TASK-231 therefore demands a regression that **fails on
today's handlers** — the standard @Fern set on TASK-237 — **and** an enumeration of the paths that must keep
answering. **The risk is not "too loud"; it is "silenced the wrong branch and nobody notices for a week".**

On AC-17: mechanism built, **automatic trigger not attempted** — measured, not assumed. Two inbound triggers
only. 🚫 The back-office *"หยุดบอทในแชทนี้"* fallback stays unbuilt, as you have it.

### The trade §15 names, carried into the spec

The **sick-mother case** now depends on an admin being reachable. Copied into SPEC-071 verbatim, because you are
right that it is *invisible at decision time and obvious the first evening it happens* — and because the fix, if
it ever bites, is TASK-235 getting better rather than the code coming back.

### Also filed

**TASK-242** — the chip wording (`ส่ง LINE ถึงครูหลักแล้ว`), 🔒 **HELD** on your exact condition: it ships only
if @Tanya's round forces an FE touch anyway, otherwise with the follow-up. **Filed as a task rather than a log
line** so it cannot be rediscovered as a defect in six months — your ruling, its reasoning, and the release
condition all in one place.

**Six tasks released.** @Jason: **230 → 231 → 232 → 233/234** (230 carries a migration ⇒ `sid` first,
`db:verify` ✅ before any restart). @Fern: **235** the moment 230's endpoint exists.

**BALL: @Jason — TASK-230.** It gates every other row in REQ-079.

## [--:--] Tanya (QA) — 🏁 **REQ-078 final round done. `TEST_FAILED` — narrowly, and the shape has changed completely.** → @Porter

Ran your scope on the deployed build. Verdict + the **park note you asked for** are in
`tests/TEST-064-req078-booking-type-other.md` §Round 4. **A second `TEST_FAILED` it is — but read the shape
before the word.**

### ✅ The defect round worked. All four are genuinely gone from the running product.
- **DEF-1** — remove the last teacher chip, then type: **form survives.** Both banners, Save disabled, **no
  `TypeError`.** Fern's fix predicted this and the prediction holds — I stressed it through several
  add/remove/type/toggle cycles. 📌 That was the one part of her diagnosis never tested against the product; it
  is now tested.
- **DEF-5** — toggle Charge → type in Title: **no crash**, on and off, repeatedly.
- **DEF-3** — `All bookings` → Type=Other: **`6 found` → 6 rows rendered.** Studentless rows show the typed
  title, and TEACHER lists **every** teacher. The count and the table finally agree.
- **DEF-6** — **`The schedule will be sent on LINE to 3 teachers: Bank, Camp, Dewy`**. Every teacher named **and**
  Fern's count line — the one you kept over Sober's objection. It earns its place: I could see at a glance the
  list was complete.
- **TASK-239 / `SICK_LEAVE`** — an อื่นๆ with an **additional teacher** over an on-leave session **saves**. The
  new guard is **not** too wide; UC-004 overbooking still works.
- **The Thai wording table** — all eight strings verbatim (**อื่นๆ · ครู · นักเรียน (ไม่บังคับ) · ชื่อรายการ ·
  เช่น ประชุมทีม, ปิดปรับปรุงลาน · คิดเงินรายการนี้ · ตัดสิทธิ์จากคอร์ส / Voucher · กรุณาระบุชื่อรายการ**).

**18 ACs pass.** This build is a different animal from the one I failed on 09-01.

### 🔴 What stops the pass — and the first one is a premise, not just a bug
**DEF-4 is REOPENED.** It was deferred on the reasoning *"the form now refuses overlap, so the hidden-session
state is unreachable — @Sober to confirm no other path creates it."* **There is another path, and it is the one
the product deliberately allows:**
- Dewy has an **on-leave** session (`Aileen · Inline Skate`, 09-03 10:00). Book an อื่นๆ over it — which
  **must** be allowed (that is R5/UC-004) — and **Aileen's session vanishes from Dewy's column**, absent from
  the DOM entirely.
- 🟢 **Data is fine, and I proved it rather than assuming:** I cancelled my booking and
  **`10:00 Aileen · คอร์ส · Inline Skate` came straight back.** Display fault, nothing lost.
- **It is genuinely narrower now** — unreachable over an *active* booking. That matters, and I say so in the file.

**DEF-7 (new, smaller):** the clash refusal names the clashing booking but **never which teacher**. With
**Camp + Dewy** where only Camp clashes, the message is byte-identical to the single-teacher case. AC-24 revised
leads with `ครู{ชื่อ}` for exactly this reason — and อื่นๆ is *the* multi-teacher type, so on a five-teacher
meeting the admin is left guessing. Everything else in AC-24 revised is met.

### ⏳ The day-end did not attend F1/F2 — and I am NOT calling that a defect
Both are **still `PENDING`** this morning. Three readings fit and I cannot separate them from the UI:
(1) the 23:30 job did not run last night; (2) it runs but only promotes `CONFIRMED`, leaving `PENDING` alone — in
which case อื่นๆ behaves *"the same as every other type"*, **AC-9 is satisfied, and my fixtures were the wrong
shape, which is my error to own**; (3) อื่นๆ is skipped — a real defect.
🔴 **The `job_runs` row for last night separates them — the movement query alone does not**, because "no
movement" is consistent with all three. That is Q15. **AC-4 · AC-5 · AC-9 stay `NOT_TESTED`.**

### The verdict, and the part that is yours
**`TEST_FAILED`.** But: DEF-4 is now one sanctioned path, display-only, data provably intact. **You and the owner
may reasonably accept it as a named gap and ship** — and I have deliberately **not** made that call for you by
rounding the verdict up. **The distance to a pass is one rendering bug and one missing name, not a broken
feature.** Q13 is that decision.

### 🅿️ Park note — in `TEST-064` §Round 4, as ordered
Every AC's final state (18 pass · 2 not · 7 `NOT_TESTED` named as **gaps, not passes**, incl. your accepted
AC-6/7/8) · **5 live fixtures with ids and who retires each** · and the one thing a fresh session does first
(**ask you for the owner's two query results + last night's `job_runs`; re-run nothing that passed**).

⚠️ **I did NOT retire F3/F4/R4a, and that is a decision:** they are **CONFIRMED**, so cancelling may push LINE
**to the owner's own phone** (he is teacher Bank). Three cancellation notices, zero test value, while he is still
reading the two messages AC-16 needs. **"Never message a real person unnecessarily" outranks tidy housekeeping** —
but it is your call (Q16) and one word retires all three in a single pass.

### Footprint
One fixture created and **cancelled** (R4b); one left (R4a); two clash attempts **never created** (refused before
any write). **Charge OFF on everything ⇒ no `bo.movement` this round. No real teacher messaged. `Aileen`'s
session verified back in place. `uat` never contacted.** Ledger in `tests/DEV-SERVER-FOOTPRINT.md`.

**Ball: @Porter** — Q13 (accept DEF-4 or hold) · Q14 (DEF-7) · Q15 (last night's `job_runs`) · Q16 (retire the
three fixtures?). Nothing else is waiting on me.

## [--:--] Porter (PM) — 🅿️ **REQ-078 PARKED at `TEST_FAILED`, per the owner. Q13 ruled — and the blocker is not DEF-4.** → @Tanya @Sober
Your verdict stands unaltered and I am not rounding it up. **18 ACs pass and all four defects are gone from the
running product** — this is a different build from the one you failed yesterday, and the record says so.

### Q13 — my ruling, and it moves the question

**DEF-4: ACCEPTED as a named gap.** Display-only, data **proven** intact (you cancelled and Aileen's session came
straight back — you tested the thing you could have assumed), and now reachable only over an *on-leave* session,
i.e. a slot where nobody is coming. **It would not hold a release on its own.**
**DEF-7: accepted too** — one missing `ครู{ชื่อ}` in a refusal message. Real, small, named.

🔴 **But the answer to "can we ship?" is still NO, and DEF-4 was never the reason.**
**No charged อื่นๆ has ever been observed posting money.** AC-4 · AC-5 · AC-9 are `NOT_TESTED` — that is not a
gap in a screen, it is **the one path in this feature that touches the customer's books, unexercised.** Shipping
a money path nobody has watched run is precisely the failure this project keeps writing post-mortems about.
⇒ **REQ-078 stays `TEST_FAILED` and parked.** Distance to a pass: one rendering bug, one missing name, **and one
overnight observation** — that last one is the real item.

### On F1/F2 not attending — your three readings, and you were right not to pick

📌 **You called this correctly by refusing to name it a defect from the UI.** My read leans to your (2): the
day-end promotes `CONFIRMED` sessions, and yours were `PENDING`. **If that is it, AC-9 is satisfied and the
fixture shape was wrong — and you already offered to own that before anyone suggested it.** But leaning is not
knowing, and **`job_runs` decides it for the cost of one `SELECT`** — going to the owner as Q15, not as a new
round. Completing the record of a round already run is not reopening it.

### Park quality — this is the part that outlives us

**Your §Round 4 note is exactly what I asked for and it is the deliverable, not the verdict:** every AC's final
state, the seven `NOT_TESTED` named as **gaps rather than passes** (including my accepted AC-6/7/8), five live
fixtures with ids and who retires each, and the first move for a fresh session. 📌 And the instruction
*"re-run nothing that passed"* is what will save the next person a day.

**Nothing is waiting on QA.** Everyone's lane is `REQ-079` (the owner's REQ-016).

📌 **One thing I got wrong and want on the record:** I deferred DEF-4 on the reasoning *"the form now refuses
overlap, so the state is unreachable"* — and asked @Sober to confirm no other path creates it. **I reasoned from
the change I had just approved instead of from the product's own rules**, and the path I missed is the one the
product deliberately allows (UC-004 overbooking an on-leave session). **Tanya found it by using the feature, not
by reading my note.** The lesson is the project's oldest one, pointed at me: *check the outcome, not the mechanism.*

## [--:--] Porter (PM) — 🔴🔴 **Q15 answered, and it found something far bigger: `end-of-day` moved to 18:30.** → @Sober @Tanya
The owner ran the `job_runs` query. **It settles Tanya's three readings — and then keeps going.**

### 1. The job DID run. Reading (1) is dead.
`end-of-day` · `run_date 2026-09-01` · `status success` · `autoAttended 0` · `revenuePosted 0`.
So "the job did not run last night" is **out**.

### 2. 🔴 IT RAN AT 18:30, NOT 23:30 — and it has since 2026-08-29

Straight off the rows, `started_at`:
`08-19 → 08-28: 23:30:02` every night · **`08-29 18:30:02` · `08-30 18:30:02` · `08-31 18:30:02` · `09-01 18:30:02`**

**It moved five hours earlier, between 08-28 and 08-29, and has stayed there for four nights.**
🔴 **Every artefact we own says 23:30** — the board, `REQ-078` AC-9, Tanya's plan, and **my own instructions to
her.** I told her *"a fixture you create today charges tonight at 23:30"* and repeated it as fact. It is not.
📌 **That is my error and it is the project's own lesson pointed at me:** I asserted a schedule from documents
instead of from the running system, and I never asked for evidence — the same absence-of-evidence mistake I
recorded against myself over TASK-220, three days ago.

**⇒ It also explains F1/F2 without any defect being needed.** F3/F4 were confirmed at **22:39 / 22:45** — hours
*after* an 18:30 run. If F1/F2 were created the same evening, **the job had already run for that day** and
nothing was skipped. **@Tanya — the one thing that closes this: what time did you create F1/F2?** Before 18:30 ⇒
the job saw them and passed them over, which is a real defect. After ⇒ the fixtures simply missed the bus, which
is nobody's bug and mine to have set up wrong.

### 3. 🔴 `byBookingType` has FOUR types. There is no `OTHER`.

Every row, including last night's, reports exactly `FIRST_TRIAL · SINGLE_SESSION · COURSE_PACKAGE · VOUCHER`.
**@Sober — at minimum the day-end REPORT cannot count an อื่นๆ. Whether the job also SKIPS it when selecting
what to auto-attend is the question, and the two are different.** Please read it from the source, as you did for
DEF-6 — this needs no data and no owner. **If the selection excludes `OTHER`, AC-9 fails and `REQ-078`'s park
note must say so.**

### 4. 🔴 WHICH BOX produced these rows? — the question I am not guessing at

If this is `sid`, it is a test-schedule problem. **If it is `uat`, the customer's day-end has been running five
hours early since 08-29** — every session that starts after 18:30 would miss its own day's auto-attend, and
**its revenue with it.** `08-29` posted `revenuePosted 3`, `08-30` posted `2`, so **the job moves money**, and
the customer's business is Saturday and Sunday afternoons.
⚠️ **Asked the owner. Nobody acts on this until he says which box.** The scheduled tasks live in
`C:\sm-jobs\*.ps1` **on each server separately**, so the two boxes can differ and one being wrong says nothing
about the other.

### What this does to REQ-078's park
**Unchanged: still `TEST_FAILED`, still parked.** But the park note now carries a named cause for the money ACs
rather than "unknown", which is worth more than the pass would have been. **AC-4 · AC-5 · AC-9 stay
`NOT_TESTED`** — and the reason is now a fact about the schedule, not a mystery.

## [--:--] Jason (BE) — ✅ TASK-230 → REVIEW. **I numbered the migration wrong and caught it by counting.** → @Sober
tsc **0** · `bun test` **1111 pass / 0 fail** (+15) · `drizzle/*.sql` = **31** = journal tags, no orphan either way.
📌 **Code-complete** — commit point visible here. **@Fern's TASK-235 is unblocked** (the tables exist); 231 is mine next.

### 🔴 The number, first, because it is the most useful thing in this entry
The task said *"today both are 30"* and I wrote **`0031`**. **30 files means `0000`–`0029`, so the next is
`0030`.** I caught it by listing the directory instead of trusting my own arithmetic, and renamed the file and
the journal entry before anything was built on it.

📌 **A plausible number is the dangerous kind of wrong.** `0031` would have left a permanent hole at `0030` that
nothing later could fill, and **`db:migrate` would have applied it perfectly happily** — no error, ever. This is
precisely what the board's "count them at the moment you write it" rule exists to catch, and it only worked
because I counted the *files*, not my memory of the count. Re-counted after the fix: **31 = 31, no orphan.**

### The load-bearing line, and why it is an index rather than a check
`family_line_links_user_uq`. **The app decides who may join a family; the database decides they may only join
once.** An application check loses to two invites redeemed in the same moment — and the cost of losing that race
is a parent opening the app and seeing **another family's children**: TASK-047's PII failure by a different
route. Asserted in both the migration and `schema.ts`, because those are the pair that drifts.

### The two counters — the distinction is now in three places, one of which cannot be read past
`unexpected_count` (**two-strikes, AC-18**) exists; `code_attempts` / `code_locked_until` (**the code lockout**)
do not. You flagged that they were nearly deleted together on one sentence, so it is written in the migration,
in `schema.ts`, **and in a test that fails if either dead column reappears.** A comment can be skimmed; a test
cannot. ⚠️ That test strips comments first — the schema deliberately *names* the dead columns while explaining
which died, and that explanation is the safeguard. **Only declarations are evidence.** (Third time this
comment-vs-code trap has come up — TASK-223, TASK-236, here. This time I saw it coming.)

### The ONE accessor
`lib/family-link.ts` — `familyLineUserIds` (primary-first, **deduped**) and `familyOfLineUser` (fixed lookup
order, so the answer never depends on which query ran). **The dedupe is not tidiness:** a parent whose own
`line_user_id` was also written into `family_line_links` — an invite redeemed by the account already on the row —
would otherwise be **pushed to twice**, and a duplicate push is how a channel gets muted. Same rule
`groupReminders` holds, applied at the source instead of at every future caller.

### 🚦 `sid` FIRST — the exact commands for the owner (via @Porter)
```bash
bun run db:migrate
bun run db:verify
```
**BLOCKING** — ✅ before any restart. It should report **`0030_family_line_links` applied**, witnessed by
**`family_line_links_user_uq`** (deliberately the new index, not the two `ADD COLUMN`s — a column on a
pre-existing table is the weaker probe, and `0022` is the incident where a probe true before *and* after hid a
migration that never ran). Then `pm2 restart`. **`uat` only after `sid` is green.**
⚠️ **If `db:verify` is RED, read it before acting** — that is what happened with `0029`, and the answer was
`db:seed-ledger` (dry run → read → `--apply`), not a schema problem.

### 🔴 One thing §15 changed that is NOT in any task yet, and it should be decided before it is typed
`family_invites.code` is the PK, built exactly as you specified. **But with the family code gone, the invite is
the only way anyone ever joins a family — so the invite code is now the entire authentication surface for that
door.** Two consequences I have deliberately not decided:
- **Collision:** a generator that repeats an unexpired code throws `23505` at redemption. Safe, but the
  generating task must handle it rather than 500.
- **Guessability:** short is convenient down a phone; short is also guessable, and guessing one gets you into a
  family. 🔴 **The owner accepted a weak-code risk for the FAMILY CODE with his own reasoning — that acceptance
  was about a different mechanism and I am not carrying it across.**
⇒ **TASK-232 needs a stated code shape (length, alphabet, TTL) and a stated rate-limit position.** Not blocking
`0030`; it should not be settled by whoever happens to type the generator.

**Ball: @Sober** — TASK-230 review + the `sid` run via @Porter, and the invite-code shape before 232.
🟢 **TASK-240** untouched (after-the-release, as you marked it). **Nothing is blocking on me** — 231 is next
unless you redirect.

## [--:--] Porter (PM) — 🔴 I raised a false alarm, and the real failure is that I keep not writing things down. → @everyone
**The 18:30 day-end is the OWNER'S OWN CHANGE, and it is correct: the app only allows booking a teacher until
18:00, so 18:30 is after the last session that can exist.** Nothing is missed, on either box. I reported it as a
possible live money incident on the customer's system. **It was not one.**

**Both halves had been said to a previous Porter and neither was ever written into a file.** So this session read
"23:30" out of stale documents, found 18:30 in the data, and alarmed the owner about his own setting — and then
**wrote that alarm into `PROJECT-STATUS.md`, the file the next session opens.** Corrected there now, marked
resolved rather than deleted.

### The owner's instruction, and it is the last time he intends to give it
> *"นายควรโน้ตแม่งทุกอย่างลงไปเลย ถ้าติดเรื่องกฎ ไฟล์ยาวเกิน อะไรต่าง ๆ ก็บอกฉัน ฉันจะไปบอก Atlas กับ marie จัดการให้
> … ถ้าวันนี้คอมฉันพัง ฉันไปเปิดคอมอีกเครื่อง โคลนลงมา แล้วต้องทำงานต่อได้เลย"*

**🆕 `ai-worker/SYSTEM-FACTS.md` — created, and pointed at from `PROJECT-STATUS.md`.** It is the home that did
not exist: **facts about how the running system behaves** — schedules, product limits, LINE, how to tell the
boxes apart, working agreements. Not requirements, not status. Append-only, never compacted.
**The rule I am binding myself to: when the owner states a fact about the system, it goes in that file BEFORE I
reply.** Not afterwards, and never only in a log entry that scrolls past.

📌 **Why the old homes failed, stated plainly so Atlas and Marie can judge it:** the board is *state* and is
size-capped; REQ files are *requirements*; the log is *history* and is now 197KB for one day. **An operational
fact fits none of them**, so it went in whichever entry was open and was gone by the next session. The problem
was never the size caps — **it was that a whole category of knowledge had no home.**

**@Sober @Tanya — same rule applies to you.** A fact you learn about the running system goes in `SYSTEM-FACTS.md`
in one line, not only into the entry you happen to be writing.

## [--:--] Porter (PM) — 📌 REQ-078's park state is now IN THE REQ FILE, not only in my head. → @Sober @Tanya
Owner: *"อย่าลืมนะ อัปเดตเรื่องนี้ลงไปด้วย"* — fair, and it is the same failure he called out an hour ago.

**`REQ-078` now ends with a `🅿️ PARK STATE` section**: the three blockers in order of size, the accepted gaps
(AC-6/7/8/21 + AC-16's multi half) marked as **decisions not oversights**, the two deferred follow-ups with
their original wording preserved, and **the first three moves for whoever resumes.**
**`SYSTEM-FACTS.md` gained the practical half:** a fixture that must post tonight has to exist **before 18:30**,
and a `PENDING` fixture proves nothing about the day-end.

🔴 **@Sober — the one open question, and it is not optional before anyone spends another overnight run:**
`job_runs.byBookingType` reports only the four original types. **Does the day-end merely fail to COUNT an
อื่นๆ, or does it SKIP it when selecting what to auto-attend?** **If it skips, an อื่นๆ never posts at all and
AC-9 fails outright** — that is a defect that must not reach `uat`, not a reporting cosmetic.
**Source read, no data, no owner needed** — same shape as the DEF-6 answer you gave from `assignedTeacherIds`.
It is the **first** of the three resume moves I wrote into the REQ, and it decides whether the ฿20 rerun is
worth asking the owner for at all.

📌 The ฿20-before-18:30 option is offered to the owner and **recorded as unanswered** — nobody runs it on their
own; it posts money.

## [--:--] Sober (SA) — ✅ TASK-230 PASS. **The invite code shape is DECIDED — he was right to stop and ask.** → @Jason @Porter

**Reproduced:** `tsc` **0** · `family-line-links.test.ts` **15/0** · `drizzle/*.sql` = **31** = journal tags,
newest `0030_family_line_links` · the accessor at `lib/family-link.ts:26,50`.

📌 **He wrote `0031` first, then caught it by listing the directory instead of trusting his arithmetic** — my task
text said *"today both are 30"* and 30 files means `0000`–`0029`. **The sharp half is his:** *a plausible number
is the dangerous kind of wrong.* `0031` leaves a permanent hole at `0030` that nothing later can fill, and
**`db:migrate` would have applied it perfectly happily.** That is the counting rule earning its place again.

📌 **"The app decides who may join a family; the database decides they may only join once."** Right division,
right reason: an application check loses the race between two invites redeemed in the same instant, and **the
cost of losing that race is a parent seeing another family’s children** — TASK-047 by another route.

📌 And the two-counter distinction I flagged is now pinned by **a test that strips comments before asserting** —
because the schema deliberately *names* the dead columns while explaining which one died, so a naive grep would
"confirm" they exist. **Only declarations are evidence.** Third time that trap has come up (TASK-223, 236, here)
and the first time it cost nothing.

### 🔴 His question, and the line in it that matters most

He refused to carry the owner’s **weak-code acceptance** across to the invite. **That is exactly right and I want
it on the record:** the owner accepted a weak *family code* — a secret the parent chose and could change — **on a
mechanism that no longer exists.** With §15, the invite is the **only** authentication surface for joining a
family. **An acceptance does not transfer across a mechanism change**, and nobody should re-use it.

**Decided, and written into TASK-232 so the generator is not designed by whoever types it.** The constraint that
drives it: **there is no rate limiter anywhere in this codebase** (SPEC-050, still true) ⇒ **the code must be
strong enough that not having one does not matter.**

- **8 characters, Crockford base32** (no `I`/`L`/`O`/`U`) ⇒ ~1.1 × 10¹². A million blind guesses is one-in-a-
  million against a single live invite — **safe without a rate limiter**, which is the whole point.
- **TTL 30 minutes** — the admin is in the chat now; a door that outlives the conversation is one nobody watches.
- **Single use = a conditional UPDATE**, not a check: `… WHERE code=$c AND used_at IS NULL AND expires_at > now()
  RETURNING parent_id`; **zero rows = refused.** Atomic, so two simultaneous redemptions cannot both win. That
  answers his own framing — *"one use is a rule about a transition, not about a row"* — by making the transition
  the guard. **No partial index needed.**
- **Case-insensitive in, upper stored.** Retyping from a chat bubble must not fail on case.
- **Collision:** insert → on `23505` retry ≤ 3 → fail loudly. 🚫 **Never log the code** — `line-log.ts` hashes
  user ids for exactly this reason; an invite in a log line is a credential in a log line.

### 🚦 @Porter — `0030` needs the `sid` run

```bash
bun run db:migrate
bun run db:verify
```
**BLOCKING: `db:verify` ✅ before any restart.** It should report **`0030_family_line_links` applied**, witnessed
by **`family_line_links_user_uq`**. ⚠️ **If it comes back RED, read it before acting** — that is what happened
with `0029` and the answer was `db:seed-ledger` (dry run → read → `--apply`), **not** a schema problem. Board
rule 2, exactly as we ran it last time. `uat` only after `sid` is green.

**BALL: @Jason — TASK-231.** It is the sharpest item in this batch: **AC-16 takes behaviour away from a running
system real teachers use**, so it needs a regression that fails on today’s handlers. `0030`’s `sid` run and
TASK-231 are independent — he does not wait for it.

## [--:--] Porter (PM) — ✅ Your lane is right. 🔴 But ONE line in the invite spec is a business call, not a technical one. → @Sober
**Backing you on the part that mattered most:** refusing to carry the owner's weak-code acceptance across to the
invite. **He accepted weak codes for a family code the parent chose and could change — on a mechanism §15
deleted.** With the family code gone the invite is the whole authentication surface for joining a family, and
**an acceptance does not transfer across a mechanism change.** That is exactly the reasoning I would want, and
Jason was right to stop rather than infer it.

**Entropy, alphabet, single-use-by-conditional-UPDATE, never-log — yours, and I am not touching them.** Building
the code strong enough that the missing rate limiter does not matter is the right shape of answer.

### 🔴 The 30-minute TTL is mine, and I am taking it back
**How long a customer has to act is a customer-experience decision, not a security parameter** — it lands on the
parent and on the admin's workload, not on the threat model.
**The failure it creates:** admin sends the invite right after payment → the parent is busy, opens LINE that
evening → **`expired`** → they message the shop again → an admin re-issues. **On a Sunday evening that is the
sick-mother problem in a new costume** — the case we already lost when the family code was cut.
📌 The common case (parent is in the chat right now) is fine at 30 minutes. **It is the "I'll do it later" case
that breaks, and that one is not rare.**
⇒ **Putting it to the owner: 30 minutes, or the rest of the day?** Longer TTL costs entropy nothing at
~1.1×10¹² with single-use enforced. **Do not let it be settled by whoever types the generator — which was your
own instinct, applied one level up.**

**Nothing else on REQ-079 is waiting on me.** `0030`'s `sid` run is with the owner; you have correctly noted
TASK-231 does not wait on it.

## [--:--] Porter (PM) — 🔴 **The invite code is CUT by the owner. Entry = the PHONE NUMBER ALONE.** → @Sober @Jason
> *"ไม่เอารหัสเชิญอะไรนั่นเลย ทำไมทำอะไรไม่บอกฉัน · ฉันเอาแค่เบอร์ ก็สามารถใช้งานได้เลย"*

**📌 My failure first, because it is the useful part.** You and Jason both did the right thing: he stopped at the
invite-code question, you flagged that the invite had become the whole authentication surface, and it reached me.
**I then argued about the 30-minute TTL and never asked the owner whether he wanted an invite code at all.**
I reviewed the parameter and let the premise through — the same mistake as DEF-4, three days running. **The rule
I am adding for myself: when the team turns one of my business answers into a mechanism, the MECHANISM goes back
to the owner, not just its settings.**

**CUT — do not build, and re-cut anything already written:** `family_invites` · the generator · the 8-char
alphabet · the 30-minute TTL · single-use redemption · the attempt counter · **and the admin "opens the door"
step.** ⚠️ **`SPEC-071` and TASK-231/232 still describe them.**
✅ **KEPT: `family_line_links` + its unique index.** Your reason never depended on the code — without it a second
entry silently re-points an account and a parent sees another family's children.

**🆕 Build the 6-digit 2FA anyway, shipped OFF** (his instruction): a per-session verification step **between the
phone and the children**, switched by `app_settings`. 🔴 **Turning it on must be a SETTING, never a rebuild** —
the branch exists from day one. Its parameters return to the owner the day it is enabled; **they are not
inherited from the deleted designs** — which is your own "an acceptance does not transfer across a mechanism
change", applied to the thing you applied it to.

🔴 **The accepted risk is recorded in `REQ-079` §2 and `SYSTEM-FACTS.md`, in full, with his reasoning:** anyone
who knows a phone number can see that family's children and act for them. **He raised it with the customer,
explained the danger, and the customer refused.** It is a decision taken with the risk on the table. **Nobody
re-opens it and nobody quietly hardens it.** What keeps it survivable — **LINE never unlocks anything that moves
money** — has now held across all three entry designs and must not be traded away without a new decision.

📌 **REQ-079 consolidated again** (51KB → readable): current design only, both prior versions archived verbatim.
**Flow 1 is now: phone → children → in.** 📌 **And the sick-mother case is solved outright** — dad needs no
admin, no code, no invite. The problem that drove three designs disappears with the third.

**Ball: @Sober — re-cut SPEC-071's 231/232 for §2 before Jason builds against a deleted mechanism.**

## [--:--] Sober (SA) — 🔄 **Re-cut done: SPEC-071 Amendment #2, TASK-232 rewritten, TASK-235 WITHDRAWN.** → @Jason @Fern @Porter

Your ball line — *"re-cut SPEC-071's 231/232 for §2 before Jason builds against a deleted mechanism"* — done,
plus one task you did not name.

📌 **I decided the shape of a thing the owner then deleted.** The 8-char base32 / 30-min / conditional-UPDATE
design was a day old. That is not wasted — it is the record of a decision taken properly on a premise that then
changed — but **it is on the record as deleted, not quietly dropped**, and TASK-232 says so in its first
paragraph so nobody half-builds it from memory.

### What I changed

- **SPEC-071 → Amendment #2**, flagged in the status line as outranking everything above it **including
  Amendment #1.** The spec now carries three entry designs, two of them dead; a reader who starts at the top
  builds the wrong thing, so the top now says so.
- **TASK-232 rewritten**: phone → (2FA branch, OFF) → children. The **phone lookup is now the binding event**,
  doing the job the invite did. §6b is unchanged and is why it works: a chat cannot be addressed until it speaks.
- **TASK-231**: unchanged in substance, with one warning added — see below.
- **TASK-233/234**: untouched. They begin from a bound chat; only the binding moved.
- ⛔ **TASK-235 WITHDRAWN** — you named the *mechanism*, not the task, and that task existed **only** to issue
  invites. Recorded as withdrawn **by inference from your ruling, not by your instruction**, so you can
  disagree. 📌 And its purpose is genuinely gone rather than deferred: §15 predicted the fix for the
  sick-mother case was *"the door in one tap"* — **phone-alone solves that case outright.**

### 🔴 Two things in your CUT list that must NOT be over-deleted

**1. `unexpected_count` survives.** Your list says *"the attempt counter"* — that is the **invite/code** counter.
It is **not** the two-strikes counter, which Rule 5 and AC-18 still require and which **TASK-230 already
shipped**. **Third time these two have nearly gone together on one sentence**; the warning is now in TASK-231,
in the migration, in `schema.ts`, and in a comment-stripping test.

**2. 🚫 Migration `0030` must NOT be edited**, even though it creates the now-unused `family_invites`. It **may
already have run on `sid`** — it was with the owner when your ruling landed. The risk is asymmetric: **edit it
after it ran and the file and the database disagree, and `db:verify` witnesses by NAME so it cannot see the
difference** (the `0022` blindness, and exactly what TASK-239's byte-identity test exists to catch). Leave it
and the cost is **one dormant table.** ⇒ `family_invites` ships dormant; dropping it is a one-line migration
whenever someone is in there anyway. **Never urgent, never bundled with a release.**

### On the 2FA, and on your own new rule

Built and shipped OFF, switched by `app_settings`, **branch present from day one** — I wrote the DoD as *a test
that flips the setting and gets different behaviour with no code change*, because *"turning it on must be a
setting, never a rebuild"* is only true if something proves it. 🚫 And its lifetime/attempts/lockout are **left
as named constants with a comment saying they return to the owner** — not inherited from either dead design.

📌 **Your new rule — *"when the team turns one of my business answers into a mechanism, the MECHANISM goes back
to the owner, not just its settings"* — is the right lesson and it is symmetric with mine.** I stopped Jason's
weak-code acceptance from crossing a mechanism boundary, and then decided a whole mechanism without asking
whether it should exist. **Same error, one level up.** REQ-079 §2 now states the principle for all three
designs, so it is the file's, not one person's memory.

### 🔴 And the one thing I will not let anyone "improve"

The accepted risk is in the spec and in TASK-232 as an instruction: **do not add a small extra check because it
feels safer.** The owner told the customer it was dangerous and the customer refused. **Quiet hardening is
forbidden by §2 as explicitly as re-opening it is.** What bounds the risk is AC-20 — *LINE never unlocks
anything that moves money* — so its **grep-guard test is now load-bearing, not tidy.** That is written into the
tasks rather than left as a shared understanding.

**BALL: @Jason — TASK-231**, unchanged and unaffected by any of this (it never touched the entry). **TASK-232 is
re-cut and ready behind it.**

## [--:--] Jason (BE) — ✅ TASK-231 → REVIEW. 🔴 **The screenshot's real cause is NOT what I fixed. Read this before the re-test.** → @Sober
tsc **0** · `bun test` **1130 pass / 0 fail** (+19) · no migration (`0030` already shipped both columns).
📌 **Code-complete** — commit point visible here. **TASK-232 is next** unless you redirect.

### The regression is a CHANGED LINE, not a new test
`line-routing.test.ts:25` asserted `"welcome"` and **passed** — that line *was* the shipped defect. I edited the
assertion rather than adding a new test beside it: **a new test passing next to an untouched old one proves
nothing.** The diff on that line is the evidence you asked for.

### Five silenced fallbacks, enumerated
1. **`handleParentCommand` tail** — `return doMenu(...)`, the loudest: a linked parent typing *anything* got the
   menu back · 2. teacher catch-all (the `yo` class) · 3. admin catch-all · 4. unlinked + no session →
   `welcome` · 5. **a session row whose `step` is none we use** → `welcome`.
**#5 I found while checking the other four** — it is #4 wearing a stale session.

### 🔴 What must KEEP answering, which is the half I care about more
Your warning was *"silenced the wrong branch and nobody notices for a week"*. Kept and asserted: every
recognised **parent** command — **`ลา` (sick leave)**, `เช็คอิน`, `qr`, `นักเรียน`, and the numbered `ลา 2`
picker answers — the **teacher keyword fallbacks** `ตาราง` / `ปฏิทิน` (REQ-015/017), **`สมัคร` from any state**
(checked before the route is computed, so silence cannot swallow the way in), every in-flow step, and the
**follow greeting** (🚫 not silenced — adding the OA is someone knocking, and it is how a new parent learns what
to type).

📌 **My reading, stated because it is a judgement:** *"otherwise → SILENCE"* means **unrecognised** text. The
stricter reading — only flows and postbacks ever answer — would **delete sick-leave reporting by keyword**,
which I do not believe AC-16 means. Correct me and it is small.

### 🔴 THE THING THAT MATTERS MOST IN THIS ENTRY — §16's screenshot may still reproduce
**`line_link_sessions` rows never expire.** `getSession` reads with no time bound and nothing deletes a stale
row. So an **abandoned `สมัคร` leaves `step = AWAIT_CODE` forever**, and every message that account ever sends
is treated as a code attempt.

**That is exactly the screenshot:** `เมนู` → *"เบอร์โทรไม่ถูกต้อง…"* is the parent-phone branch; `yo` →
*"ไม่พบครูชื่อเล่น yo"* is the teacher-nickname branch — **both are `AWAIT_CODE`**, not the idle-chat fallbacks
I silenced. Under the precedence you specified, *"session.step is set → the flow owns this message"* keeps that
chat answering. ⇒ **The five fallbacks are genuinely fixed; the chat in the screenshot may not be.** What this
task does give it is a floor: two strikes hands over and mutes for an hour instead of re-prompting forever.

**I did not add a TTL** — a behaviour change nobody ordered, and it cuts both ways (too short and a parent
typing slowly loses their registration mid-flow). But TASK-230's own comment says the counter *"expires with the
session"*, which assumes an expiry that does not exist. **Options: (a)** TTL in `getSession` (one line, ~30 min)
· **(b)** treat a stale step as idle in the router · **(c)** accept it, bounded by two-strikes.
🔴 **If the owner re-tests with the same chat and it still answers, (c) will read as "the fix did not work".**

### Two smaller flags
- **`MUTE_MINUTES = 60` is mine and is in no AC** — long enough for a person to reply, short enough that a
  forgotten mute does not leave a family stuck tomorrow. A guess with reasoning, not a requirement.
- Two-strikes is wired to the two branches that fail *recognisably* (`CHOOSE_ROLE`, `AWAIT_CODE` — the second is
  the screenshot's own branch). Free-text steps have no "unrecognised" to detect, so they are deliberately not
  wired. It resets on success, **and the handover resets too**, so a returning parent starts clean.

**Ball: @Sober** — TASK-231 review + **the stale-session ruling (a/b/c), which decides whether the re-test
passes**. 🚫 Not built, per your instruction: the back-office mute control, and the automatic admin-reply
trigger (§16 measured it never reaches our webhook). 🟢 TASK-240 still untouched.

## [--:--] Sober (SA) — ✅ TASK-231’s code PASSES — and **he found that §16’s diagnosis (and my task) named the wrong cause.** → @Jason @Porter @Tanya

**Reproduced:** `tsc` **0** · `line-routing.test.ts` **6/0** · `isMuted`/`MUTE_MINUTES` at `line-routing.ts:41,61`.

### 🔴 The screenshot was never the idle-chat fallbacks

**§16 read it as stray text in an idle chat. It is not.** @Jason traced both strings to **`AWAIT_CODE`** —
`เมนู` → the parent-phone branch, `yo` → the teacher-nickname branch — and **`line_link_sessions` rows never
expire.** ⇒ an abandoned `สมัคร` leaves that chat treating **every message it ever sends** as a code attempt,
permanently.

📌 **That is a live defect on real users today**, independent of AC-16 — and it is the exact chat the owner will
re-test. **He fixed the five fallbacks I asked for, then told me they were not the reported bug.** Reporting the
gap between *what the task asked for* and *what the reporter will check* is worth more than a green DoD, and it
is the second time this week an engineer has corrected the premise rather than the parameter.

📌 The fifth fallback — a stale session whose `step` is none we use — **he found while checking the other four**,
and it would have kept the screenshot’s behaviour alive **in exactly the chats that had one**: the shape of bug
that survives a fix and then discredits it.

### My decision: the session TTL, option (a), and it belongs to THIS task

My first DoD box was *"an idle chat receiving `เมนู`/`yo` gets NO reply"* — **that is literally the screenshot,
and it still reproduces. The box is not met.** That is my under-specification: I wrote the DoD from §16’s
diagnosis, and §16’s diagnosis was wrong. So TASK-231 is **reopened for one addition**, not passed with a
follow-up.

**(a) bound `getSession` by `updated_at`** — not (b) the router, because the router is one caller and a rule at
the source gives **every** reader the same answer (the same one-definition principle he applied to
`SLOT_INACTIVE_STATUSES`). **30 minutes, and it must be INACTIVITY not age.**
⚠️ **The trap I wrote into the task:** only `setStep` writes today, so a parent retrying inside one step may not
touch the row — the window would then run against someone who **is** actively replying. ⇒ **`updated_at` must be
touched on every inbound message a session handles.** Without that, (a) creates the "lost my flow mid-
registration" failure he was right to worry about.
📌 It also repairs TASK-230’s own comment — *"the counter expires with the session"* — which assumed an expiry
we had never built.

### Two judgement calls, both confirmed

**"otherwise → SILENCE" means UNRECOGNISED text, not "only flows and postbacks".** The strict reading would have
silenced **`ลา`** — how a family reports a sick child (REQ-046/049, shipped) — which is exactly the *"silenced
the wrong branch and nobody notices for a week"* failure. **A recognised command is not stray: the user typed it
deliberately.** He stated it rather than assuming it, which is why it is now a decision instead of a discovery.

**`MUTE_MINUTES = 60` — accepted, and it is yours to look at, @Porter.** Not in any AC, so it is a product
parameter: long enough for a person to reply, short enough that a forgotten mute does not strand a family. It is
a named constant — one edit if the owner has a view. 📌 *"Until the admin replies"* is unavailable **because**
§16 measured that we cannot see an admin reply.

**BALL: @Jason — TASK-231’s TTL addition.** Small, and it is the part the owner’s re-test will actually exercise.

## [--:--] Jason (BE) — ✅ TASK-231 TTL added → REVIEW. **The trap you named was real.** → @Sober
Option (a), at the source. tsc **0** · `bun test` **1139 pass / 0 fail** (+9) · no migration.
📌 Code-complete. **TASK-232 next** unless you redirect.

`getSession` returns `undefined` for a row older than **`SESSION_IDLE_MINUTES = 30`** of silence. Nothing is
deleted — an expired row stays for the record — and **silence follows from the rules already built** rather than
from a second mechanism. `isSessionExpired` is pure and sits beside `isMuted`, so "is this session live?" has one
definition, same principle as `SLOT_INACTIVE_STATUSES`.

### ⚠️ Your trap was real, and it needed more than a touch
Only `setStep` wrote the row, so a parent retrying **inside** one step never touched it — a wrong code twice and
the window would have been running against someone actively replying. `touchSession` now refreshes on every
inbound message a session **handles**, before any branch can return.

🔴 **And only the two routes a session owns** (`add-student`, `linking`). A `linked` or `silence` route must NOT
refresh: stray text keeping a dead row alive forever is the original bug wearing a different hat. A test asserts
the touch line names neither — that one took me a second pass to see.

The test that makes *"inactivity, not age"* concrete is a scenario, not a number: a flow **started** two hours
ago whose **last message** was ten minutes ago is **not** expired. That is the parent-typing-slowly case.

### One judgement inside the predicate, stated
**An undated or unparseable `updated_at` is EXPIRED, not fresh.** A row we cannot date is a row we cannot trust
to be a live conversation — and of the two ways to be wrong, "fresh" is the one that lasts forever, which is
exactly the bug being closed.

### Why 30 minutes is safe rather than destructive
**`สมัคร` restarts from any state**, checked before the route is even computed — so the cost of expiring is one
word retyped. Asserted, because that is the entire argument for a window this short.

**🚫 Still not mine to prove:** that the owner's actual chat falls silent. That needs the deployed bot and his
phone — **@Tanya**, with his account linked as teacher Bank. What I can say is the **cause** is addressed rather
than the symptom: the `AWAIT_CODE` row that chat carries stops being found 30 minutes after its last message.

**Ball: @Sober** — TASK-231 review. 🟢 TASK-240 still untouched. Nothing blocking on me.

## [--:--] Sober (SA) — ✅ **TASK-231 DONE.** He went past the trap I named to the one behind it. → @Jason @Porter @Tanya

**Reproduced:** `tsc` **0** · `line-routing` + `other-booking` **38/0** · `SESSION_IDLE_MINUTES`/
`isSessionExpired` at `line-routing.ts:79,88` · the guard at `line-webhook.service.ts:109` · `touchSession` at
`:121`, called at `:578` **only** for `add-student` / `linking`.

### 🔴 The refinement I did not specify, and it is the load-bearing one

I said *touch `updated_at` on every inbound message a session handles.* **He narrowed it to the two routes a
session actually owns**, because — his words — *"a `linked` or `silence` route must NOT refresh: stray text
keeping a dead row alive forever is the original bug wearing a different hat."*

**My instruction as written would have re-created the defect.** A chat stuck in `AWAIT_CODE` sends stray text; if
any inbound message refreshed the row, the 30-minute window would never elapse — **the fix would have looked
applied and changed nothing.** He asserts that the touch line names neither route, and says it took him a second
pass to see. That is the second time this week an engineer has caught a defect *inside my correction* rather than
inside the original task.

### Three judgement calls, all right

- **An undated or unparseable `updated_at` is EXPIRED, not fresh** — *of the two ways to be wrong, "fresh" is the
  one that lasts forever*, which is the bug being closed. Same shape as his `bo.item` missing-item rule: fail
  toward the state someone will notice.
- **The test is a scenario, not a number**: a flow started two hours ago whose last message was ten minutes ago
  is **not** expired. That makes *"inactivity, not age"* a property instead of a comment.
- **`สมัคร` restarts from any state** ⇒ the cost of expiring is **one word retyped** — asserted, not claimed.
  Without that line 30 minutes is a guess; with it, it is a bounded one.

### 🧪 One sentence that must travel with the re-test, @Porter → @Tanya

🔴 **The chat has to be LEFT ALONE for 30 minutes before it is retried.** The window is measured from its last
message, so a retest done immediately would still see the old behaviour **and read as "the fix did not work"** —
the exact misreading @Jason warned about when he first found the stale session. Poking it does not extend the
window (the touch is route-scoped), but it does not shorten it either.

📌 And the honest boundary he keeps drawing: **that the owner’s real chat falls silent is Tanya’s to prove.** He
fixed the cause; nobody has yet observed the symptom disappear.

**BALL: @Jason — TASK-232** (phone alone → children, 2FA branch built OFF), which is where he said he was headed
and which is now correctly re-cut for §2.

## [--:--] Jason (BE) — ✅ TASK-232 → REVIEW. **One thing I built to FAIL rather than guess.** → @Sober
tsc **0** · `bun test` **1159 pass / 0 fail** (+20) · **no migration**, and **`0030` is not edited** —
`git diff drizzle/` is the journal line and nothing else, so `family_invites` ships dormant as Amendment #2 says.
📌 Code-complete. **TASK-233/234 are next** unless you redirect.

### The guarantee that survived all three entry designs
`bindFamilyLine` refuses a chat already bound to a **different** family — **before anything else is written**.
Asserted, because if the bind ran after `linkParentLine`/`moveRosterLink` a refused chat would already have been
re-pointed. The unique index would refuse it anyway; refusing here turns a `23505` into a sentence a parent can
act on. Re-binding the **same** family is a no-op — typing your phone twice is not an error.

### `parentChildrenNote` — a decision beside the old one, not an erosion
The function and **its reason** are untouched; `parentChildrenNames` sits next to it with the new path's reason
written out. 📌 **And they are not interchangeable:** with 2FA **on**, the parent sees the **count** before
verifying and the **names** after — so TASK-047's rule is still honoured wherever a gate exists, and the old
function keeps a live caller instead of becoming dead code someone later deletes.

### 🔑 The deliverable you named
`twoFaEnabled()` reads `app_settings` **every time** — a cached flag would have made switch-on a *restart*, and
a test asserts there is no cache. With the setting off, `needs2fa` is never set and the path is byte-identical.
🚫 Lifetime / attempts / lockout are **named placeholders that say they are placeholders**, and explicitly not
inherited from the family code or the invite code. 📌 A wrong 2FA code reuses **TASK-231's two-strikes rule**
rather than getting a bespoke lockout — a wrong code *is* an unrecognised in-flow reply.

### 🔴 The thing I could not decide, and built to fail loudly instead
**How a parent RECEIVES the six digits is specified nowhere.** There is no SMS integration, and sending the code
to the LINE chat being verified verifies nothing — the person in the accepted-risk scenario is already in that
chat. So `deliver2faCode` **throws**, naming the decision.

It is a **transport, not a rebuild** (flow, step, storage, verification and copy all ship), so it does not break
*"turning it on must be a setting"* — **but the setting cannot usefully be switched on until the owner answers
it.** I chose a loud throw over a silent no-op deliberately: the failure then lands on whoever flips the switch,
not on a family who can no longer reach their children's schedule. **Tell me if you would rather it degrade to
2FA-off with a `console.error` — one line, and I want the choice on the record either way.**

### AC-20's grep-guard — written, and it is why this door is survivable
The four LINE entry modules are asserted to contain **no money symbol** *and to import nothing that does* — a
transitive reach would defeat a symbol check alone. If a money path ever became reachable from here, the
phone-alone door would stop being a convenience decision and become a financial one **without anyone re-opening
the question**.

### ⚠️ One consequence I want confirmed rather than assumed
The refusal *"this LINE account belongs to another family"* is **permanent from the bot's side — there is no
unbind.** A family that genuinely changes phones, or a parent who linked the wrong number once, needs an admin
to clear `family_line_links` — and **no admin surface for that exists** (TASK-235 was withdrawn). It is right
that the bot cannot unbind itself, but *"contact an admin"* currently points at a person with no button.
**Not blocking. It is the first support call this design will generate.**

🚫 **I added no check §2 did not ask for.** The phone alone opens the door and the names come back; the only gate
is the one that ships off. The grep-guard is the sole thing I strengthened, because you called it load-bearing.

**Ball: @Sober** — TASK-232 review + the delivery question (owner) + the unbind gap.
🟢 TASK-240 still untouched. Nothing blocking on me.

## [--:--] Sober (SA) — ✅ TASK-232 DONE. 🔴 **2FA ships OFF and cannot be switched ON — he found the hole in the instruction, not the task.** → @Porter @Jason

**Reproduced:** `tsc` **0** · `line-phone-entry` + `line-routing` **26/0** · `0030` not edited ·
`parentChildrenNames` beside the untouched count version at `line-pairing.ts:35`.

### 🔴 @Porter — the sentence that must reach the owner BEFORE he ever flips the switch

> **2FA is built and off. It cannot usefully be switched on until the owner answers: how do the six digits reach
> the parent? There is no SMS in this system, and sending the code into the LINE chat being verified verifies
> nothing — the attacker in §2’s accepted-risk scenario is already in that chat.**

**That is a hole in the instruction, not in the build.** *"Build it, ship it off, switch it on when the customer
cares"* assumes the only missing thing is a flag. It is not: it is a **transport** nobody has chosen. @Jason
built everything else — the step, the storage, the verification, the copy, the setting — so the owner’s *"never a
rebuild"* is honoured; what is missing is an answer, not code. **Please put it in REQ-079 §2 beside the 2FA
paragraph**, because otherwise it is discovered on the day he switches it on, in front of the customer.

✅ **And his `deliver2faCode` THROWS rather than degrading to 2FA-off.** Right call: a silent degrade puts the
failure on **a family who can no longer reach their children’s schedule**; a throw puts it on **whoever flipped
the switch.** The person who caused the failure should be the person who sees it.

### The work itself

📌 **`parentChildrenNote` handled exactly right** — untouched, with the new `parentChildrenNames` beside it and
the honest framing: **TASK-047’s argument was not refuted, it was accepted.** The owner put the danger to the
customer in those words and the customer chose the convenience. **And the old function still has a live caller**
(with 2FA on, the parent sees the *count* before verifying and the *names* after) — which is the difference
between a rule that was superseded and one that becomes dead code somebody deletes.

📌 **The cross-family bind is refused BEFORE `linkParentLine`/`moveRosterLink`** — after them the chat would
already have been re-pointed. The DB would refuse it anyway; refusing here turns a `23505` into a sentence a
parent can act on. Same instinct as `describeSlotClash`.

📌 **`twoFaEnabled()` reads `app_settings` on every use, with a test asserting no cache** — a cached flag makes
switch-on a **restart**, which is a rebuild by another name. That test *is* the owner’s requirement.

**`AWAIT_2FA` parking the code in `pending_role`:** accepted rather than reopening `0030` for a branch that ships
off — with the condition that the comment goes on the **column** in `schema.ts`, not only the use site, plus an
assertion that nothing reads `pendingRole` as a role while `step = AWAIT_2FA`. A proper column the next time a
migration is open anyway.

### 🔴 His Q3 is a hole in MY design, and I have cut TASK-243

The phone binds the chat, the unique index makes it permanent, and the refusal says *"contact an admin"* —
**pointing at a person with no button.** TASK-235 was the only surface near this and **I withdrew it** when the
invite was cut. The ordinary cases are all ordinary: a family changes number, a parent typed the wrong one once,
a second-hand phone.

⇒ **TASK-243** — an admin control to clear a family’s LINE link (BE + FE), audited, staff-only, 🚫 unreachable
from any LINE path, and **it deletes nothing but the link.** Flagged to you as **following from the design, not
added to it** — strike it if you read it as scope. 📌 His test for whether something is a gap — *"the first
support call this design will generate"* — is worth more than the fix.

**BALL: @Jason — TASK-233** (เพิ่มนักเรียน: summary before write, admin told, nothing partial). TASK-243 is
small and can follow 233/234 unless @Porter re-orders.

## [--:--] Jason (BE) — ✅ TASK-233 → REVIEW. 🔴 **I added a migration the task did not ask for — check that call.** → @Sober
tsc **0** · `bun test` **1180 pass / 0 fail** (+21) · `drizzle/*.sql` = **32** = journal tags, no orphan.
📌 Code-complete. **TASK-234 next** unless @Porter re-orders.

### 🔴 The judgement call I want reviewed first
TASK-233's text says nothing about a migration. **I added `0031` — `line_link_sessions.draft jsonb`.**

TASK-232 parked the 2FA code in `pending_role` because no migration was open and that branch ships OFF, and I
said then it should become a real column the next time one was. **Doing it a second time, for a three-field
wizard, is how a column called `pending_role` quietly becomes a blob nobody can reason about** — one documented
compromise is a compromise, two is a pattern. **If you would rather it shipped without `0031`, the fallback is
JSON in `pending_role`, ~10 lines** — but then `0030`, the 2FA overload and this would all be riding one text
column, and I do not think that survives the next reader.

### The three rules, and where each is actually enforced
1. **Summary before write.** `createStudentForParent` appears **only** in the confirm branch — asserted by
   slicing the function and checking the earlier half never mentions it. That one assertion is most of AC-12:
   a write anywhere earlier would leave a child in a roster **with no delete**.
2. **AC-9 — more detail, never a rename.** The message is a **constant with no interpolation**, and that is
   asserted: a message echoing the existing child's name would leak exactly what withholding the rename
   protects. The duplicate check runs on the name step only; the detail step is the *answer*, not a second
   question. ⚠️ Still @Porter's wording, not the owner's literal *"บอกให้ตั้งใหม่"* — only the message and AC-9
   change if he overrules.
3. **AC-12.** The draft lives on the session, which already expires after 30 minutes (TASK-231) and is deleted
   when the flow ends. Cancelling at the summary **says** nothing was saved — the one thing a parent must not be
   left wondering.

### Two smaller things worth the line
- **The birthdate parser refuses impossible dates rather than rolling them forward.** `new Date("2026-02-31")`
  silently becomes **March 3**; a birthdate that quietly becomes the wrong date is worse than one nobody
  entered, and there is no delete to fix it with.
- **An unrecognised answer at the summary reuses TASK-231's two-strikes rule** — the **fourth** call site of one
  handover rule, rather than a bespoke retry loop.

### ⚠️ One reading I want confirmed rather than assumed
**`province` is a HOUSEHOLD field.** `parents.province` exists; `students` has no province column. So confirming
writes it to the parent — meaning **a second child registered later overwrites the household's province**. That
is arguably right (one family, one address) and it is what the schema says, but §5 reads as though it belongs to
the child. If it should be per-student, that is a column and a different task.

Also flagged in the task: birthdate and province are **optional** (a registration that cannot be completed
without a birthdate is one a parent abandons — and AC-12 then gets us nothing at all); and the
`MAX_STUDENTS_PER_PARENT` cap is checked at CONFIRM, so a parent at the cap walks the whole wizard before being
refused. I left the latter rather than duplicating a rule that already lives in one place — **say the word and
I will call the same function's precondition earlier instead of copying its rule.**

### 🚦 `sid` FIRST
`bun run db:migrate` → **blocking** `bun run db:verify` ✅ → restart. ⚠️ **`0030` and `0031` are both pending on
`sid`** and apply in order in one run. `0031`'s witness is the `draft` column — a column probe is normally the
weaker kind (`0022`), and the reason it is safe here is that `draft` **did not previously exist**, so it can
only be present because `0031` ran. Written down so it does not read like an oversight.

**Ball: @Sober** — TASK-233 review, the `0031` call, and the `province` reading.
🟠 TASK-243 (admin unbind, from my own TASK-232 finding) and 🟢 TASK-240 are both still open and still mine.

## [--:--] Sober (SA) — ✅ TASK-233 code PASSES. **The migration he did not have permission for is the right call.** → @Jason @Porter

**Reproduced:** `tsc` **0** · `line-add-student.test.ts` **20/0** · `drizzle/*.sql` = **32** = journal tags,
newest `0031_line_session_draft`.

### ✅ `0031` — approved, and his test is the one I would have used

> *"One documented compromise is a compromise; two is a pattern."*

I told him on TASK-232 that `pending_role` becomes a proper column *"the next time a migration is open anyway"*.
**He noticed the second overload would arrive BEFORE that day and make it permanent by habit** — a column called
`pending_role` holding a 2FA code **and** a three-field wizard is a blob with a misleading name, and the next
reader inherits it with no way to know what is inside. `jsonb` over three columns for the same reason: one
in-flight form, and a fourth question tomorrow must not be another migration.

🔴 **One thing follows and I have asked for it while `0031` is still unrun: move TASK-232’s 2FA code out of
`pending_role` into `draft`.** That was my stated condition and this is the moment it named — it costs nothing
today and becomes permanent if we skip it. Also asked: **check `MAX_STUDENTS_PER_PARENT` at the NAME step** by
calling the same function’s precondition (his own suggestion, and not a duplicated rule) — walking a parent
through three questions to refuse them at the end reads as the system being broken.

### What he proved, and how

📌 **Asserting `createStudentForParent` appears ONLY inside the confirm branch — by slicing the function and
checking the earlier half never mentions it.** That single structural assertion is most of AC-12: if the write
existed anywhere earlier, an abandoned conversation leaves a child in a roster **with no delete**. A proof about
*absence* done structurally rather than by sampling behaviour.

📌 **AC-9’s message is a constant with no interpolation, asserted** — a message echoing the existing child’s name
would leak **exactly what withholding the rename was protecting.** I did not name that risk; he did.

📌 **The strict birthdate:** `new Date("2026-02-31")` silently becomes March 3, into a roster with no delete.
Round-tripping through `Date` and refusing is right — a birthdate that quietly becomes the wrong date is worse
than one nobody entered.

📌 **Two-strikes reused at the summary rather than a bespoke retry — the FOURTH call site of one handover rule**,
and every time he has chosen the existing one. That is why it is still one rule.

### 🔴 @Porter — two of his questions are yours, and one has an interim I have already set

**1. `province` is a HOUSEHOLD field.** `parents.province` (REQ-019/TASK-048 put it there deliberately), but
§5’s flow reads as though it belongs to the child — so **a second child registered later would overwrite the
household’s province** with whatever is typed then. He stopped rather than assume, correctly.
**My interim, in the code now:** write it when the household field is **empty**, never silently overwrite a
non-empty one. Loses nothing, cannot corrupt an address staff entered, reversible whichever way the owner rules.
**The reading itself is his to confirm.**

**2. Only `name` is mandatory** — birthdate and province accept `ข้าม`. ✅ I kept it, and his reason is right:
*a registration that cannot be completed without a birthdate is one a parent abandons*, and AC-12 then
guarantees we get **nothing at all**. ⚠️ **But the customer’s own ask (§3) names all three fields**, so it is a
deviation and you should carry it: **we still ASK for all three; we just do not lose the child over one.**

### 🚦 Both `0030` and `0031` are pending on `sid` — they apply in order in one run
```bash
bun run db:migrate
bun run db:verify
```
**BLOCKING: ✅ before any restart.** `0031` is witnessed by the `draft` column — 📌 and his note on that is right:
the `0022` weak-witness risk is about probing columns that **already existed**; `draft` did not, so its presence
can only mean the migration ran.

**BALL: @Jason — TASK-233’s two additions** (2FA → `draft` while `0031` is still unrun, and the cap at the name
step), then **TASK-234**.

## [--:--] Jason (BE) — ✅ TASK-233's two additions done → REVIEW. → @Sober
tsc **0** · `bun test` **1186 pass / 0 fail** (+6) · no new migration; `0031` unchanged and still unrun.

**1. The 2FA code is out of `pending_role` and into `draft`** — done in the window you named, while `0031` is
still unrun. One accessor (`twoFaCodeOf`) reads it, so `draft`'s shape has a single reader. 📌 **`pending_role`
now means only what its name says**, and a test asserts nothing reads a 2FA code out of it any more.

**2. The cap is asked at the NAME step, by EXTRACTING the precondition** — new `assertCanAddStudent`, which
`createStudentForParent` now calls too. A test asserts `>= MAX_STUDENTS_PER_PARENT` appears **exactly once** in
that file, so it is one rule with two callers rather than two copies.
- It **returns the count it already computed**, so the write does not re-query for the number it reports —
  extracting a check should not cost a query.
- 🔴 **The write still enforces it.** The early call is a *courtesy check in front of* the real one, and that is
  asserted — *"we check it earlier now"* is exactly how a guard quietly becomes the only guard.
- A parent at the cap is refused **before the first question**, session cleared, so they are not left inside a
  flow they cannot finish.

📌 **The comment-vs-code trap caught me a fourth time** — both AC-10 assertions failed on my own prose, because
the flow *explains* why `createStudentForParent` sits where it does. Same shape as TASK-223, TASK-236 and
`Math.random` in TASK-232. The stripping helper is now hoisted with a note saying why, so the next person meets
the lesson instead of the failure.

**Ball: @Sober** — TASK-233 re-review. **TASK-234 next** unless @Porter re-orders; 🟠 TASK-243 and 🟢 TASK-240
still open and still mine.

## [--:--] Sober (SA) — ✅ **TASK-233 DONE.** Both additions landed in the window they had to. → @Jason @Porter

**Reproduced:** `tsc` **0** · `line-add-student` + `line-phone-entry` **20/0** · `twoFaCodeOf` reading `draft`
(`line-webhook.service.ts:223`) · `assertCanAddStudent` (`parent.service.ts:92`) called by the name step `:386`
**and** by `createStudentForParent` `:118` · `>= MAX_STUDENTS_PER_PARENT` **exactly once** (`:94`) · `0031`
unchanged and still unrun.

📌 **The line that makes the extraction right is his:** *"the early call is a courtesy check in front of the real
one"* — **asserted**, because *"we check it earlier now"* is exactly how a guard quietly becomes the only guard.
And it **returns the count it already computed**, so the write does not re-query for the number it reports.

📌 **`pending_role` now means only what its name says.** Closed inside the window it was named for — which is why
I asked for it now instead of "next time a migration is open": the next time would have been after `0031` ran.

### 📌 A repo-wide convention, out of a mistake that has now happened four times

The **comment-vs-code trap** — a source-assertion test failing on the author’s own prose — has caught him on
TASK-223, TASK-236, TASK-232 and TASK-233. **Four times in one week, always prose being asserted instead of
code.** He has hoisted the comment-stripping helper with a note explaining why. 🔴 **That is now the convention:
anyone writing a source-assertion test in `smart-scheduler-back` uses that helper.** Recorded in the task and
here so it stops being folklore — the fix for a mistake that recurs is a shared tool, not more care.

### State of REQ-079 after this

BE: **230 ✅ · 231 ✅ · 232 ✅ · 233 ✅** · **234 is the last build row** (flows 4–6 on the existing pickers + the
two rich menus — mostly reuse). Then 🟠 **TASK-243** (the admin unbind, from his own finding) and 🟢 **TASK-240**.

🚦 **@Porter — `0030` and `0031` are both still pending on `sid`** and apply in order in one run:
`db:migrate` → `db:verify` ✅ **before any restart**. Still open with you from earlier today: the **2FA delivery**
question (it cannot be switched on without it), **`province` as a household field**, and **birthdate/province
being optional** against the customer’s three-field ask.

**BALL: @Jason — TASK-234**, the last build row in REQ-079.

## [--:--] Jason (BE) — ✅ TASK-234 → REVIEW. **REQ-079's BE build is complete** — with one deploy gap named. → @Sober
tsc **0** · `bun test` **1212 pass / 0 fail** (+26) · **no migration** (32 = 32).
📌 Code-complete. That is **230 · 231 · 232 · 233 · 234** — every BE row in REQ-079.

### 🔁 Reused vs added, as you asked
**Reused untouched:** `doLeave`/`doLeaveBooking`/`needsChildStep` (REQ-046) · `doCheckin`/`doCheckinBooking`
(REQ-050) · TASK-233's wizard via the existing `action=register` · the whole rich-menu mechanism (REQ-042) ·
`muteUntilFrom` · `notifyAdmins`. **A test asserts `async function doLeave(` and `async function doCheckin(`
each appear exactly ONCE** — the proof of absence that matters in a reuse task, because a test that only
checked the new button would pass while the old flows quietly forked.

**Added — four things, only the first is real logic:** `lib/line-course-view.ts` + `doMyCourses` (nothing
customer-facing showed a course before; REQ-016 is the *teacher's* schedule, a different question) · two menu
definitions + `linkKnownRichMenu` · two postback actions (`admin`, `enter`) · 5 i18n keys.

### 🔴 `คุยกับแอดมิน` is handled BEFORE every role check
Asserted by index comparison. Gating it behind `detectLinkedRole` would have made **the one button that must
never fail available only to people who do not need it** — someone who cannot get in is exactly who needs a
person. It notifies the admins and mutes with the **same** helper the two-strikes handover uses.

📌 **There is deliberately no `unlink`** — ยังไม่รู้จัก is the DEFAULT, so an unbound chat (or one whose link is
ever removed) lands on the right menu **with no code running**. Unknown is where you fall back to, not a state
someone must remember to set. Asserted.

### AC-15 — the inversion worth guarding
`เหลือ` is **REMAINING**, not used. Showing the used count under a label reading "remaining" is the kind of
quiet inversion a family only notices when they run out early — by then they have planned around the wrong
number. Clamped at 0 so an over-attended course reads *"0 left"*, never a negative on a money-adjacent line.
🔑 **The numbers come from `toCourseSummary`** — the same builder every staff screen uses — so a parent and an
admin cannot end up quoting different figures at each other.

### ⚠️ THE GAP — "TASK-234 DONE" will read as though the menus exist. They do not.
`publishRichMenus` needs a **2500-wide image per menu**, and I have none for the two new ones. Until they are
published, `getMenuIds()` returns no `unknownTH`/`knownTH`, **`linkKnownRichMenu` is a no-op, and every chat
keeps the menu it has today.** The degradation is correct — I would not have it fail — **but it is silent**,
which is exactly the shape this project keeps getting caught by.

⇒ **@Porter needs two images (843-high unknown, 1686-high known) and one `publishRichMenus` run.** Until then
the AC's *"both menus exist"* is **not** met on `sid`. Please carry it as a deploy step rather than letting the
task's green tick stand for it.

### Three smaller things in §Questions
- **`เข้าใช้ระบบ`'s sentence is my wording** (§15 deleted Flow 2, so it points at the phone and at a person).
  It is the first sentence a stranger ever reads from us — one i18n key if the owner wants different words.
- **A course has no teacher column**, so the teacher comes from its sessions. A course split between two
  teachers would name only one.
- **Only ACTIVE courses are listed** — so a parent whose course just expired sees nothing at all, with no
  explanation. AC-15 does not mention it; a filter change + one copy key if the owner would rather they saw it.

**Ball: @Sober** — TASK-234 review + the menu-image deploy step to @Porter.
🟠 **TASK-243 is the one I would take next:** REQ-079 now tells people *"contact an admin"* from two menus and
three messages, **and the admin still has no button.** 🟢 TASK-240 after that.

## [--:--] Sober (SA) — ✅ TASK-234 code PASSES → 🏁 **REQ-079’s BE is COMPLETE.** 🔴 But three deploy steps a green tick does not cover. → @Porter @Jason

**Reproduced:** `tsc` **0** · `bun test` **1212 pass / 0 fail** (his count, matched exactly) · no migration.
⚠️ *I ran the full suite this time having said I would not — it completed without reaching a database on this
machine. The standing `eligible.route.test.ts` risk is unchanged for a whitelisted one; noting the reversal
rather than leaving it silent.*

### Two calls better than what I specified

**1. `คุยกับแอดมิน` runs BEFORE `detectLinkedRole`** — *"gating it behind a role would have made the one button
that must never fail available only to people who do not need it."* **Someone who cannot get in is exactly who
needs a person.** I wrote *"in both menus, no flow may remove it"*; he found the ordering consequence I had not —
a role check in front of it **is** a removal, for precisely the people it exists for.

**2. No `unlink`, with a test asserting none exists.** ยังไม่รู้จัก is the DEFAULT menu, so a chat that was never
bound lands correctly **with no code running** — SPEC-071’s "cheapest shape" applied without being told, and it
makes *unknown* a state you fall back to rather than one someone must remember to set.

📌 **AC-15’s inversion guard is the one that would have been believed:** `เหลือ` is REMAINING, clamped at 0, and
taken from `toCourseSummary` — the same builder every staff screen uses — with a test that `doMyCourses` contains
**no quota arithmetic.** A second derivation of "sessions remaining" is how a parent and an admin end up quoting
different numbers at each other, and a used-count under a "remaining" label is only discovered when a family
runs out early, having planned around it.

### 🔴 @Porter — I have put a PENDING DEPLOY block on the board, because a task tick does not cover any of this

1. **`0030` + `0031`** pending on `sid`, applying in order in one run: `db:migrate` → **`db:verify` ✅ before any
   restart.**
2. 🔴 **The two rich menus DO NOT EXIST until published WITH IMAGES** — 2500×843 (ยังไม่รู้จัก) + 2500×1686
   (รู้จักแล้ว) + one `publishRichMenus` run. Until then `getMenuIds()` returns no ids, `linkKnownRichMenu` is a
   **silent no-op**, and every chat keeps today’s menu. **@Jason flagged this himself** — *"TASK-234 DONE will
   read as though they do"* — and he is right: it is the board’s own PENDING-DEPLOY lesson, the one that has
   bitten this project three times. **The degradation is correct; its silence is the risk.**
3. **2FA still cannot be switched on at all** until the owner answers the delivery question.

**Also yours:** the `เข้าใช้ระบบ` sentence is @Jason’s wording and is **the first thing a stranger ever reads from
us** — one i18n key, and you are the UX writer. He surfaced it rather than letting it be found on a phone; it is
honest and complete, so it ships unless you improve it.

### One fix I asked for

A course with sessions split between two teachers currently names the **first**. 🔴 **Name the NEXT upcoming
session’s teacher** instead. TASK-140 made a course re-teacherable **on purpose**, so a split course is the
*normal* result of a re-teacher — old sessions with A, future with B. **A parent reading คอร์สของฉัน is asking
"who is teaching my child", present tense**, so the first session answers a question nobody asked, and is wrong
in exactly the case the split exists to represent.

### 🏁 State of REQ-079

**BE complete: 230 ✅ · 231 ✅ · 232 ✅ · 233 ✅ · 234 ✅** (one small fix outstanding). Remaining: 🟠 **TASK-243**
(the admin unbind — from @Jason’s own TASK-232 finding, and it must ship before anyone is told to "contact an
admin"), plus the three deploy steps and the three owner questions still with you (2FA delivery · `province` as
a household field · birthdate/province optional against the customer’s three-field ask).

**BALL: @Jason — TASK-234’s next-session-teacher fix**, then 🟠 TASK-243.

## [--:--] Jason (BE) — ✅ TASK-234's next-session-teacher fix → REVIEW. → @Sober
tsc **0** · `bun test` **1219 pass / 0 fail** (+7) · no migration.

New pure `nextSessionTeacher(sessions, today)`, called with `bangkokNow().date`. The service no longer takes
whichever booking came back first — asserted both ways (it calls the rule, and `bookings?.find(` is gone).

**Your reasoning is now in the code rather than only in the review:** TASK-140 left the teacher on the bookings
*because a course is re-teacherable*, so a split course is the **normal result** of one — and a parent is asking
*"who is teaching my child"*, present tense.

**Three edges I had to decide, all pinned:**
- **A session TODAY counts as upcoming** — it has not happened yet, and a parent checking in the morning should
  see who they are about to meet.
- **A finished course still names who taught it** (falls back to the most recent past session). Otherwise the
  day the last session is attended, the line would silently become `-`.
- 🚫 **A `CANCELLED` session is not evidence of who teaches** — skipped in both directions, or a cancelled
  hand-over session could name a teacher who never taught one.

`today` is injected, matching `toCourseSummary`'s shape, so the rule never picks up the server's timezone.

**Ball: @Sober** — the fix. ⚠️ The menu-image deploy step is unchanged and still with @Porter.
🟠 **TASK-243 next** — REQ-079 now says *"contact an admin"* from two menus and three messages and the admin
still has no button. 🟢 TASK-240 after.

## [--:--] Sober (SA) — ✅ TASK-234 DONE → 🏁 **REQ-079’s BE build is COMPLETE.** 🔴 TASK-243 promoted. → @Porter @Jason

**Reproduced:** `tsc` **0** · `bun test` **1219 pass / 0 fail** · `nextSessionTeacher` wired with
`bangkokNow().date` · `bookings?.find(` → **0 occurrences.** Asserting the **removal** as well as the addition
is what makes *"no longer takes whichever came back first"* a fact rather than a claim.

### The three edges he decided are the review, and all three are right

- **A session TODAY counts as upcoming** — the off-by-one here would have been invisible and wrong **every
  single morning**, for the parent checking who their child is about to meet.
- **A finished course still names who taught it** — without the fallback the line goes blank **the moment the
  last session is attended**, which is exactly when a parent looks at it.
- 🚫 **A `CANCELLED` session is not evidence of who teaches** — otherwise a cancelled hand-over session names a
  teacher who **never taught one**. That is the difference between unhelpful and lying.

📌 **`today` is injected**, so the rule never picks up the server’s timezone — the same instinct as reading the
price at posting time: a value that depends on *when the code runs* rather than *what the data says* is a bug
that only shows up at the wrong hour.

### 🔴 @Porter — TASK-243 is PROMOTED on his observation, and it is a good one

> *"REQ-079 now says **contact an admin** from two menus and three messages, and the admin still has no button."*

When I cut TASK-243 it was *"the first support call this design will generate"*. **It is now the advertised
remedy on every dead end in the product** — the unknown menu, the known menu, the phone-not-found reply, the
cross-family refusal, the two-strikes handover. **Shipping REQ-079 without it means we tell parents to contact
someone who cannot help them**, in five places. It is small (one BE clear + one People-screen control) and it
should go out with this REQ, not after it.

### 🏁 REQ-079 status

**BE build complete: 230 ✅ · 231 ✅ · 232 ✅ · 233 ✅ · 234 ✅.** Nothing is waiting on me.

**Still with you, and none of it is code:**
1. **PENDING DEPLOY** (board block): `0030`+`0031` on `sid` in one run · 🔴 **the two rich menus need IMAGES +
   `publishRichMenus` — they do not exist until then, silently** · 2FA cannot be switched on at all.
2. **Three owner questions:** 2FA **delivery** · `province` as a household field · birthdate/province optional
   against the customer’s three-field ask.
3. **The `เข้าใช้ระบบ` sentence** — @Jason’s wording, the first thing a stranger reads from us, one i18n key,
   and you are the UX writer.

**BALL: @Jason — TASK-243.** It is the last thing standing between REQ-079 and a design that keeps its own
promises. 🟢 TASK-240 after it.

## [--:--] Jason (BE) — ✅ TASK-243 BE half → REVIEW. **"Contact an admin" now points at something.** → @Sober
tsc **0** · `bun test` **1230 pass / 0 fail** (+11) · **no migration** (32 = 32, counted after building).
📌 Code-complete for the BE half. **The People-screen control is @Fern's** and I have not touched the front repo.

### 🔴 BOTH sources are cleared, together
TASK-230 kept the first link on `parents.line_user_id` so every existing reader stayed untouched — so a clear
that removed only the join rows would leave a family **unbound to one reader and bound to another**, the exact
two-writer disagreement the accessor was built to prevent. `clearFamilyLine` removes both, and reads *which*
accounts through the same `familyLineUserIds` the binding side uses. The service act is deliberately thin: a
second implementation there would let "what a family's accounts are" drift between the two halves of one
question.

### Audited, and the actor is not a caller's opinion
`[family-link] CLEARED parent=… accounts=… by=…`, with the actor taken **from the token, never the body** —
TASK-160's rule for discounts, same reason: *an actor a caller can choose is not an actor.*

⚠️ **But it is a LOG LINE, not a durable audit row, and I want that limit on the record.** No audit table
exists and the task said to expect no migration, so "who unbound this family last March" is answerable only as
long as logs are kept. This is the one act most worth reconstructing later — it is **the only way an account
moves between families**, which is what the unique index exists to stop happening silently. **If a durable trail
is wanted, that is a table and your call**, not something I slip into a task that said not to expect one.

### 🚫 No LINE path can reach it
`family-link.ts` **is** imported by the bot (for `bindFamilyLine`), so the guard is about the **call**, not the
module: the webhook is asserted to contain no `clearFamilyLine`, no `clearParentLineLink`, no
`clear-line-link`. **The bot must not be able to unbind anyone** — that is the whole reason the binding is
permanent from its side. And the clear touches nothing else: asserted as an **absence** of `students`,
`bookings`, `notificationOutbox`, `coursePackages`, `vouchers`.

### 📌 An existing guard caught my change, exactly as designed
`course-ended-writes.test.ts` failed: *"no write route is unclassified — a new one fails here by omission"*. I
classified `POST /parents/:id/clear-line-link` as `unrelated` **with the reason on the line**. Second time this
week an existing guard has done its job on my own work (the other was my reader-count test on 236/239) — worth
saying out loud, because that is the argument for writing them.

### For @Fern, via you — what the FE half has to work with
- `GET /parents/:id` now returns **`lineLinked: boolean`** and **`lineAccounts: number`** — 🔑 counted through
  the accessor, **not** off `parents.line_user_id`, because a family can hold more than one account and reading
  the column would under-report: an admin would clear something the screen said was not there.
- `POST /parents/:id/clear-line-link` → `{ cleared: number }`. No body.
- ⚠️ `listParents` does **not** carry the indicator — if the badge is wanted in the LIST, that is a batched read
  and worth doing properly rather than N queries.

### One decision I want checked
**The clear is not transactional** — delete the join rows, then null `line_user_id`. A failure between them
leaves a half-cleared family. Wrapping it is two lines and I would normally have done it; I did not because
every other writer in `family-link.ts` takes an `exec` and runs **inside the caller's** transaction, and
starting one here would break that shape for a future composer. **Wrap it here, or have the route pass a
transaction — your call.**

**Ball: @Sober** (BE review) **→ @Fern** (the People-screen control). 🟢 TASK-240 is the only row left with my
name on it, still marked after-the-release.

## [--:--] Sober (SA) — ✅ TASK-243 BE PASSES (one change) · **@Fern is unblocked** · TASK-244 cut. → @Jason @Fern @Porter

**Reproduced:** `tsc` **0** · `bun test` **1230 pass / 0 fail** · `clearFamilyLine` (`family-link.ts:118`) ·
the route (`api.ts:66`) · `lineAccounts`/`lineLinked` (`parent.service.ts:248`).

📌 **Clearing BOTH sources together is the point and he led with it.** TASK-230 kept the first link on
`parents.line_user_id`; a clear that removed only the join rows would leave a family **unbound to one reader and
bound to another** — the exact two-writer disagreement the accessor exists to prevent. And *which* accounts is
read through the **same** `familyLineUserIds` the binding side uses.

📌 **The actor comes from the token, never the body** — TASK-160’s rule: *an actor a caller can choose is not an
actor.*

📌 **A pre-existing guard caught his own change** — `course-ended-writes.test.ts` failed with *"no write route is
unclassified"*, so a new write route **cannot slip past because nobody thought about it.** Second time this week
a guard did that, and both times it was a guard that fails by **omission** rather than by matching a known-bad
pattern. That is the shape worth copying.

### The one change: wrap the clear in a transaction

He argued — correctly — that both sources must be cleared **together**, then left the two writes non-atomic to
preserve the `exec` shape every other writer in that file uses. **A half-cleared family is exactly the state the
function was written to make impossible**, so the argument defeats the omission. The standard pattern keeps
both properties: `return exec ? run(exec) : db.transaction(run)` — composable when a caller supplies one, atomic
when nobody does. **He asked rather than wrapping silently or leaving it silently, which is why this costs two
lines instead of an incident.**

### ✅ His Q1 — the log line — accepted now, TASK-244 cut for the durable trail

He was right not to slip a migration into a task that said not to expect one, with `0030`/`0031` still unrun.
**But his own reasoning is why it cannot just ride:** this is **the only way a LINE account can move between
families** — precisely what `family_line_links_user_uq` exists to stop happening silently. So it is at once the
act most likely to be questioned months later (*"why can this parent see that family?"*) and the only one whose
answer **expires with log rotation.** ⇒ **TASK-244, after the REQ-079 deploy**, with the row written in the same
transaction as the clear — *a trail that can disagree with the act it describes is worse than none.*

### @Fern — your half is unblocked
BE gives you **`GET /parents/:id` → `lineLinked` + `lineAccounts`** (counted through the accessor, so a
multi-account family is not under-reported) and **`POST /parents/:id/clear-line-link` → `{ cleared }`**, no body.
⚠️ **`listParents` deliberately has no indicator** — a per-row lookup there is N+1. If you need a badge in the
list, it is **one batched `inArray` over the page’s parent ids** (the `bookingsWithRentals` shape). Named so
nobody reaches for the obvious wrong version.
🔴 And the confirm copy is load-bearing: **"unlink" must not read as "remove the family."** The BE asserts it
removes nothing else; the sentence is what stops staff believing otherwise.

**BALL: @Fern — TASK-243’s FE half.** @Jason: the two-line transaction wrap, then 🟢 TASK-240 / TASK-244 in
whatever order @Porter prefers after the deploy.

## [--:--] Fern (FE) — ✅ TASK-243 FE half → REVIEW. Link state lives in the DIALOG, not on the row — and that was your own Q3. → @Sober
`smart-scheduler-front`, HEAD **`a70a0e4`**, and **`HEAD ≡ develop`** (`rev-list --left-right --count` = `0 0`) —
checked before touching REQ-019 ground, not assumed. Detail in **TASK-243 §Implementation Notes — FE half**.

**Verified:** `bunx tsc --noEmit` **exit 0** · `bun test` **82 pass / 0 fail** · `bun run build` ok
(`/scheduler/people` emitted). 🚫 No database, no environment, no message. 🔴 BE untouched — the transaction
wrap you asked @Jason for is his.

### The one DoD line I met differently, stated up front rather than buried
> *"The screen says whether a family is linked before the admin acts."*

**Met in the dialog, not on the row — because of your own Q3 answer.** `lineLinked`/`lineAccounts` exist only on
`GET /parents/:id`, one accessor query per parent, and this screen loads **20 cards a page**. A row badge is
either N+1 or the **batched** read you named (*"one `inArray` over the page's ids … named here so nobody reaches
for the obvious wrong version"*). **I did not reach for it — and I did not build the batched version either,
because it is BE work inside an FE half.** So the dialog fetches on open and the admin reads the family's real
binding before there is anything to press. **Q1 is yours: cut the small BE task, or accept the current shape.**

**The Clear button exists only when something is linked.** A button that would no-op is how staff learn to
distrust a screen. And *"ยังไม่ได้เชื่อมบัญชีไลน์"* is a **sentence**, not an empty dialog — that is the state
most families are in, and it had to read as an answer rather than as a failed load.

📌 **The count is rendered, not a boolean in disguise.** TASK-230 lets a family hold more than one account; an
admin about to clear needs to know they are clearing two, not one. The mock's first family holds **2** for
exactly that reason.

### The copy carries the two things staff would otherwise learn afterwards
1. **What is NOT lost** — students, bookings, notes and message history all stay, and the parent can relink any
   time. Your BE notes named the tempting misreading (*"unlink" = "remove the family"*); this is where it stops.
2. **That it is a HAND-OVER, not a reset** — *"หลังล้างแล้ว บัญชีไลน์นั้นจะถูกนำไปเชื่อมกับครอบครัวใดก็ได้
   รวมถึงครอบครัวอื่น"*. This is **@Jason's fourth question answered where he said it belonged** — on the screen,
   before the act, not in a task file. Without it a staff member finds out by watching an account land elsewhere.

🚫 **FE grep-guard:** `clearParentLineLink|clear-line-link` → **4 sites, one hook, one caller** (the People
screen). There is no bot code in this repo at all, so the guard that matters is @Jason's on the webhook.
🧹 I removed a dictionary key I had added and then not used, rather than leave dead copy.

### 🔴 Not proven by me
**The rendered dialog** — auth wall as always. **Route it LOCAL, not `sid`:** the mock covers *both* states, so
it is a one-minute check, and the command + what to look at are in the TASK. 📌 **One step in it is worth
keeping:** after clearing the first family, its **students must still be listed on the card** — that is the
collateral-damage assertion made visible on screen, next to the sentence that promises it.
**The headline outcome** (a cleared account re-binding to a *different* family) is @Jason's and @Tanya's, as he
said — it needs rows and two chats.

**Q2 (small, and a consequence of Q1):** I used the task's literal label `ล้างการเชื่อมไลน์` on the row button.
⚠️ Without a row badge it sits on every family, most of which have no link — so it names an action that usually
does not apply. A neutral entry point opening the same dialog would read better and costs one line. **I did not
switch it on my own** — the label is written in the task and the wording is not mine to change.

**Ball: @Sober** — TASK-243 FE review + Q1 (the row badge) / Q2 (the label). Nothing else is open for FE:
TASK-111 waits on TASK-110, TASK-118 on TASK-116, TASK-242 is HELD by you.

## [--:--] Sober (SA) — ✅ TASK-243 **FE half DONE**. One BE line left in all of REQ-079. → @Fern @Jason @Porter

**Reproduced:** `tsc` **0** · `bun test` **82 pass / 0 fail** · `clearParentLineLink` at exactly **5 sites** —
service, mock branch, mock, hook, one caller. `HEAD ≡ develop` checked before touching shared REQ-019 ground.

### ✅ Her Q1 — the row badge. RULED: the dialog is enough. Do not build the batched read.

She met *"the screen says whether a family is linked before the admin acts"* **in the dialog**, not on the row,
and flagged it rather than letting me find it. **That is the right reading of the intent** and I closed it
rather than leaving it open: the requirement exists so **an admin never clears blind**, the dialog fetches on
open, and **the Clear button exists only when something is linked.** A row badge answers a different question —
*"which of these 20 families are linked?"* — that **nobody has asked**. Building a batched BE read for it now is
speculative work. **If that workflow appears it is a REQ, not a widget.**
📌 And she reached for **neither** the N+1 version nor BE work inside an FE half. Both restraints are the point.

### The two things worth copying from this half

📌 **"Not linked" reads as an ANSWER, not a failed load.** It is the state most families are in, and an empty
dialog there would be indistinguishable from a broken fetch — the same trained-blindness failure as TASK-222’s
`retry: false`, caught in a place nobody would have re-tested.

🔴 **The best line in the whole task is her second sentence of copy:** *"บัญชีไลน์นั้นจะถูกนำไปเชื่อมกับครอบครัว
ใดก็ได้ รวมถึงครอบครัวอื่น"* — it answers @Jason’s fourth question **on the screen, before the act**, not in a
task file. Clearing is the **only** way an account moves between families; a staff member who does not know that
finds out **by watching one land somewhere else.** She put the consequence where the decision is.

### ⚠️ Record straight: the BE wrap has NOT landed
`clearFamilyLine` (`family-link.ts:118`) still does two writes on the passed `exec`. Her half is complete against
the contract; **TASK-243 is open on that one BE line** — and it is now **the only open code line in REQ-079.**

### 🏁 Where REQ-079 stands

**All build rows done bar one two-line wrap:** 230 ✅ · 231 ✅ · 232 ✅ · 233 ✅ · 234 ✅ · 243 (FE ✅, BE wrap).
Queued after: 🟢 TASK-244 (durable unlink trail) · 🟢 TASK-240.

🔴 **@Porter — nothing left in this REQ is code. It is all yours:**
1. **PENDING DEPLOY** (board): `0030`+`0031` on `sid` in one run · **the two rich menus do not exist until
   published WITH IMAGES — silently** · 2FA cannot be switched on at all.
2. **Three owner questions:** 2FA **delivery** · `province` as a household field · birthdate/province optional
   against the customer’s three-field ask.
3. **The `เข้าใช้ระบบ` sentence** — one i18n key, and you are the UX writer.

**BALL: @Jason — the two-line transaction wrap on `clearFamilyLine`.** It is the last open code in REQ-079.

## [--:--] Jason (BE) — ✅ The transaction wrap → REVIEW. **REQ-079 has no open code line.** → @Sober
tsc **0** · `bun test` **1233 pass / 0 fail** (+3) · no migration.

```ts
const result = exec ? await run(exec) : await db.transaction(run);
```

Exactly the shape you specified: **composable** when a caller already has a transaction — the shape every other
writer in `family-link.ts` uses, so a future caller can still fold this into theirs — and **atomic** when nobody
supplies one, which is every caller today. The `= db` default is gone from the parameter, because that is what
makes *"nobody supplied one"* distinguishable at all; asserted, along with both writes being **inside** the
wrapped unit rather than one in and one out.

**You were right and my omission was not defensible.** A half-cleared family — unbound to the accessor, still
holding a stale `line_user_id` — is precisely the state the function exists to make impossible, so preserving
the `exec` shape at the cost of atomicity defeated the function's own argument. Asking cost two lines; it would
have cost an incident.

📌 **One thing I moved while in there:** the log line now runs **after** the write commits, so the trail can
never claim something the database refused. That matters more once **TASK-244** makes it durable — your own
*"a trail that can disagree with the act it describes is worse than none"* is the same rule, and the durable row
will want to be **inside** this transaction rather than beside it. **The seam is now in the right place for it**,
which should make 244 smaller than it would have been.

**Ball: @Sober.** 🟢 TASK-244 and 🟢 TASK-240 are the two rows left with my name on them, both marked
after-the-deploy. **Nothing else is waiting on me** — and per your last entry, nothing left in REQ-079 is code.

## [--:--] Sober (SA) — 🏁 **TASK-243 DONE → REQ-079 has NO open code.** Everything left is yours. → @Porter @Jason @Fern

**Reproduced:** `tsc` **0** · `bun test` **1233 pass / 0 fail** · `clearFamilyLine` is now `exec?` + an inner
`run(tx)`.

📌 **He kept both properties and improved on what I asked for:** composable when a caller supplies a transaction,
**atomic when nobody does** — and he moved the accessor read **inside** `run`, which I had not specified. That
matters: reading the account list outside the transaction could have reported a set the delete then did not
match. **The half-cleared state `clearFamilyLine` exists to prevent is now unreachable.**

### 🏁 REQ-079 — all code complete

**230 ✅ · 231 ✅ · 232 ✅ · 233 ✅ · 234 ✅ · 243 ✅.** Queued, both 🟢 after the deploy: **TASK-244** (a durable
trail for the one act that can move an account between families) and **TASK-240** (the pre-existing course-search
row-dropper).

### 🔴 @Porter — nothing that remains is code, and three of these will not surface by themselves

**1. PENDING DEPLOY (board block), in order:**
   - `0030` + `0031` on `sid` — one run, `db:migrate` → **`db:verify` ✅ BEFORE any restart.**
   - 🔴 **The two rich menus DO NOT EXIST until published WITH IMAGES** (2500×843 ยังไม่รู้จัก · 2500×1686
     รู้จักแล้ว) + one `publishRichMenus`. Until then `linkKnownRichMenu` is a **silent no-op** and every chat
     keeps today’s menu. **The task ticks do not cover this** — @Jason flagged it himself.
   - **`bun run sale:ensure-items`** if the อื่นๆ typed-amount path is wanted on that box (REQ-078 carry-over).

**2. Three owner questions:**
   - 🔴 **2FA delivery** — it is built and OFF and **cannot be switched on at all** until he says how six digits
     reach a parent. No SMS exists, and LINE cannot verify LINE.
   - **`province` is a household field** — a second child overwrites it. Interim in code: write only when empty.
   - **birthdate/province optional** against the customer’s three-field ask. We still ask; we just do not lose
     the child over one.

**3. The `เข้าใช้ระบบ` sentence** — @Jason’s wording, the first thing a stranger reads from us, one i18n key,
and you are the UX writer.

**4. And still open from REQ-078:** its `TEST_FAILED` stands until @Tanya passes it, TASK-242 is 🔒 held on your
condition, and the QA blockers on the board (a **second linked LINE recipient**, **backoffice read access**)
are what keep the money and multi-teacher ACs unprovable.

### 🧪 For @Tanya, when you release her
⚠️ **REQ-079’s headline outcome is not proven by anyone yet:** that a cleared account can bind to a **different**
family. It needs rows and two chats. Also: **the TASK-231 re-test needs the chat left alone for 30 minutes
first** — the window runs from its last message, and a retest done immediately reads as "the fix did not work".

**BALL: @Porter — the `sid` deploy and the three owner questions.** Nothing is waiting on me, @Jason or @Fern.

## [--:--] Porter (PM) — ✅ `0030` + `0031` applied and witnessed on `sid`. `db:verify` GREEN. → @Sober @Tanya
```
Journal: 32 migration(s) · ledger …: 45 row(s)
Schema witnesses: 32 applied
✅ every migration is recorded in the ledger AND witnessed in the schema.
```
📌 **The ledger surplus is explained and is NOT a problem** — I am saying so explicitly because I raised a false
alarm once today already. 40→45 while the journal went 30→32: **+2 migrations, +3 rows we inserted ourselves**
with `db:seed-ledger --apply` on 09-01. The long-standing 10-row surplus is unchanged and still unexplained, but
it is **stable and does not affect verification** — a background question for @Sober, not a finding.

**Told the owner: restart BE (:4006) → then FE (:3016), same sitting, then confirm FROM THE SCREEN.**
🔴 **Still to run before REQ-079 is testable at all: the two rich-menu images + `publishRichMenus`.** Without it
`linkKnownRichMenu` is a **silent no-op**, every chat keeps today's menu, and it looks like the feature failed.
**@Tanya is NOT released until that is done** — a test round against unpublished menus would produce a
meaningless `TEST_FAILED`.

## [--:--] Porter (PM) — 🧪 @Tanya — RELEASED for the part that is testable. 🔴 Most of REQ-079 is NOT, yet. → @Tanya
Owner: *"ถ้าแค่ UI ก็ให้ tanya เทสไป"* — right, and my error: I asked **him** to eyeball screens that are **your**
job. `0030` + `0031` are applied and witnessed, `db:verify` ✅, BE and FE restarted on `sid`.

### 🔴 Read this before planning your round — most of REQ-079 cannot be tested yet
**The two rich menus are NOT published** (they need images + one `publishRichMenus`, an owner step, still open).
**Rule 2 says only a button starts a flow — a typed keyword never does.** ⇒ with no menu, **no flow can be
started at all**: entry · เพิ่มนักเรียน · แจ้งลา · เช็คอิน · คอร์สของฉัน are **all unreachable**, and so is the
headline outcome (a cleared account rebinding to a **different** family).
**Do not spend the round proving that.** It would read as `TEST_FAILED` and mean nothing.

### ✅ What IS testable right now — and #1 is the valuable one
1. 🔴 **AC-1 — silence by default.** Type stray text into an **idle** chat (`เมนู`, `yo`, anything). **The bot
   must NOT reply.** This is a **change to shipped behaviour**, not a new feature: today's build answered such
   messages with errors (`เมนู` → *"เบอร์โทรไม่ถูกต้อง"*) in the owner's own account, while a human was about to
   reply. **Needs no menu.** If this still answers, the change did not land.
   ⚠️ **The two-strikes / mute re-test needs the chat left ALONE for 30 minutes first** (@Sober) — the window
   runs from the last message, and an immediate retest reads as "the fix failed".
2. **TASK-243 on the People screen** — does it say whether a family has a linked LINE account **before** an admin
   acts? ⚠️ Clearing needs a **family**-linked account; the owner's link is a **teacher** (`Bank`), so if there
   is no linked family on `sid` yet, **report that as untestable rather than manufacturing one.**
3. **Regression: the calendar renders with real data, and the browser console is clean.** `0030`/`0031` touched
   the schema, and 2026-08-24 is the day a migration/code mismatch took the calendar down while every command
   reported success.

**Verdict scope is those three only.** Everything else on REQ-079 stays `NOT_TESTED — blocked on rich menus`,
and I want it named that way rather than failed.

## [--:--] Tanya (QA) — 🧪 REQ-079's testable slice: **2 of 3 done, 1 is not mine to run.** + the clock may explain AC-9. → @Porter

Took your three and **did not widen them** — I left the unreachable flows alone exactly as you asked, so nothing
here reads as a meaningless `TEST_FAILED`. Evidence: **`tests/TEST-065-req079-testable-slice.md`**.

### ✅ 2 — TASK-243 does what it was built to do
`People` → a parent row → **`Clear LINE link`** opens **`LINE link — 0897946312`** / **“No LINE account is linked
to this family.”** with **`Cancel` only — no destructive button is rendered when there is nothing to clear.**
That answers your question: **the admin is told the state before acting.** I read it and cancelled; nothing
written. 📌 It identifies the family by **phone**, which matters — every row renders the literal name
“Parent name”.

🔴 **But the positive half is untestable, exactly as you predicted.** `LINE links` shows **two linked accounts on
`sid`, both TEACHERS — `Bank` (the owner) and `Haris` — and ZERO families.** So “shows as linked → cleared →
rebinds” has no fixture. **Reported, not manufactured** (Q18).

*Not a defect, but worth naming:* all **111** parent rows show an identical `Clear LINE link` button regardless
of state — Fern's deliberate design (state in the dialog, your own Q3). An admin scanning the list still cannot
see *which* families are linked.

### ✅ 3 — no regression from `0030` + `0031`, and I went wider than the calendar
`Schedule` (full week, real courses + my fixtures) · `Teachers` · `People` (111) · `Bookings/Students` ·
`Overview` (85% attendance · 13 booked · 11 attended · ฿373,000 · 44 courses · 15 vouchers, charts all render).
**Console clean on every one** — only two benign `baseURL` logs. Given 2026-08-24, I think this was worth the
extra four screens.

### 🔴 1 — AC-1 is NOT mine to run, and there is a decision under it
It needs an **inbound** LINE message. That needs a LINE account that has added the OA — **I have none, and
creating one is prohibited.** There is no admin surface that sends or simulates an inbound; `LINE links` only
handles link requests. **The device half is the owner's, the verdict half is mine** — same shape as AC-16.

⚠️ **And before he spends the effort, one thing needs deciding (Q17):** the only account we have is **his, linked
as a TEACHER (`Bank`)**. AC-1 is about an **idle** chat. **A linked teacher's chat may not travel the same path
as an unlinked parent's** — so silence from his phone would not prove silence for the population the AC is
actually about. **I would not accept a pass from the teacher chat alone**, and I would rather say that now than
after he has tested. (Sober's 30-minutes-untouched rule still applies too.)

### 🔴 A finding that lands on my own open AC-9 question — and sharpens it
**The `sid` clock reads `23:10` right now** (Overview: *“as of 2 Sep 2026, 23:09”*; the browser agrees).
My F1/F2 are dated **09-01** and were created **late** in that session — **quite possibly after that night's
23:30 pass had already run**, which would explain the un-attended fixtures with **no product fault at all**.
⚠️ **But it raises a worse question than the one I asked:** if the day-end sweeps only **its own** day, then a
session created **after** its day's 23:30 is **never** auto-attended — that *would* be a real gap.
⇒ **Q19: please ask for the `job_runs` rows for BOTH 09-01 and 09-02**, not just last night's. Tonight's pass is
~20 minutes away as I write this, so 09-02's row will exist shortly.

### Footprint
**Read-only round. Nothing created, nothing written, no LINE message sent.** One dialog opened and cancelled on a
real parent row — it offered only `Cancel`, and a real family's link state is not mine to change regardless.
📌 Recorded because it is now visible: **`Haris` is LINE-linked on `sid`** — a real teacher. Every fixture I have
ever confirmed used Bank (you/the owner), Camp, Dewy or Ek. **Never Haris**, and the rule that he is never
messaged in rehearsal still binds. The five REQ-078 fixtures are unchanged (`TEST-064` §Park note).

### 📌 My own near-miss, recorded rather than buried
I first saw `404`s in the console and nearly wrote them up. They were **my own bad URL guesses** (`/scheduler`,
`/scheduler/line-links` — neither exists), and this tool's console buffer **persists across navigations**, so
they followed me from page to page. I re-ran the whole sweep in a **fresh tab** before reporting anything, which
is where the clean result came from. **Second time on this project I have nearly filed my own mistake as a
product defect** (the clock was the first). The habit that catches it is the same both times: **re-observe before
you report.**

**Ball: @Porter** — Q17 (whose chat for AC-1, and I would reject a teacher-chat-only pass) · Q18 (a linked family
fixture, or does it wait for the menus?) · Q19 (`job_runs` for 09-01 **and** 09-02). Nothing else is waiting on
me: REQ-078 is parked with its verdict and Q13–Q16, and the rest of REQ-079 is blocked on the rich menus.

## [--:--] Porter (PM) — 🔴 @Tanya: you used **23:30**. It is **18:30**. + Q17/18/19 answered. → @Tanya @Sober
**Good round, and the two things you refused to do were the right refusals** — you did not manufacture a family
fixture, and you would not accept a teacher-chat pass for AC-1. Both recorded.

### 🔴 One correction, and it is mine, not yours
You reasoned from *"that night's 23:30 pass"*. **`end-of-day` runs at 18:30** and has since 2026-08-29 — the
**owner changed it himself**, and it is correct because **the app only books teachers until 18:00**.
**`ai-worker/SYSTEM-FACTS.md` — read it; it exists because I kept losing facts like this.** I told you 23:30 and
that is the same stale number that already cost REQ-078 a round. **Your F1/F2 conclusion survives the
correction** — F3/F4 were confirmed 22:39/22:45, still after an 18:30 pass.

**Q19 — reframed, and it does not need the owner.** Your sharpened question is the good one: *does the day-end
sweep only its OWN day?* If so, a booking created after that day's pass is **never** auto-attended.
⚠️ **`job_runs` rows cannot answer that — they show what ran, not what the query selected.** ⇒ **@Sober: read it
from the source**, same as the DEF-6 answer. **That, plus whether the selection excludes `OTHER`** (still open
from 09-02), are one read. **No data request, no owner.**

**Q18 — a linked FAMILY fixture waits for the rich menus.** There is no other way to create one: linking runs
through Flow 1, and Flow 1 starts from a button that does not exist yet. **Do not look for a workaround.**

**Q17 — you are right and I am not overruling it.** A teacher's chat is not evidence about an unlinked parent's.
**Do not accept a teacher-chat-only pass for AC-1.** Options for the owner: temporarily unlink his own account,
or use a second LINE account. **Put to him; his device, your verdict** — and Sober's 30-minutes-untouched rule
still applies.

📌 **Noted from your round and carried:** `Haris` is a **real teacher, LINE-linked on `sid`** — added to the
never-message-in-rehearsal list beside the two on `uat`. And the People list renders every row's button
identically regardless of state (Fern's deliberate design); **an admin scanning the list still cannot see which
families are linked.** Not a defect today; it becomes one the moment families are actually linked. Logged as a
follow-up, not raised as a bug.

### ⇒ Everything now funnels to ONE blocker: the rich-menu images
**`REQ-079` §12 now carries the full image spec** — both sizes, every cell's pixel area, the exact Thai labels,
and the rules for whoever draws them. **Two images + one `publishRichMenus` and the whole REQ becomes testable.**

## [--:--] Porter (PM) — 🎉 The owner ran the LINE flow END TO END on his phone. **AC-1 is PROVEN.** + 3 findings. → @Tanya @Sober
Two screenshots from the owner's own device on `sid`, tonight. **This is the evidence @Tanya could not produce
(Q17) — and it came from an unlinked-parent path, not the teacher chat, which is exactly what she said she would
require.** Verdict is still hers; I am recording what the screens show, not passing anything.

### ✅ AC-1 — SILENCE BY DEFAULT WORKS. The behaviour change landed.
- Idle chat, **`yo`** (23:23) and **`hellobro`** (23:26) → 🔇 **no reply.**
- After the flow finished: **`adawdawdafaw` · `ok ka` · `สวัสดีครับผม` · `ok ครับ` · `ได้เลยครับ` · `ได้เลยยย` ·
  `ดีมาก`** — **seven messages, not one bot reply.**
📌 **This is the defect I found in his account two days ago, gone.** Then, `เมนู` and `yo` were answered with
`เบอร์โทรไม่ถูกต้อง` / `ไม่พบครูชื่อเล่น "yo"` **while a human was about to reply.** ⇒ the regression @Sober
asked for has a real before-and-after now.

### ✅ The whole registration flow works
`สมัคร` → role picker → `1` → phone → **`ผูกบัญชีผู้ปกครองสำเร็จ ✅ พบข้อมูลของคุณแล้วค่ะ — มิลล่า, มิลลิ`** →
เพิ่มนักเรียน → name → birthdate → province → **summary → `ยืนยัน`** → `เพิ่ม "asda" สำเร็จ ✅`.
**§2's phone-alone entry is real, and the summary-before-write step I insisted on is there and was used.**
📌 Also visible: the **08:15 daily reminder** arrived that morning with both อื่นๆ bookings listed correctly.

### 🔴 THREE findings — none of them "it's broken", all of them "nobody decided this"

**1. The flow is started by a TYPED KEYWORD, not a button.** `สมัคร` triggered it from an idle chat, and the
bot's own closing message advertises a command list — `เพิ่มนักเรียน · นักเรียน · เช็คอิน · ลา · qr · เมนู`.
🔴 **`REQ-079` rule 2 says the opposite: "Only a button starts a flow. A typed keyword never does."** That rule
exists because the customer's own call named *"commands triggered accidentally"* as their fear, and because a
keyword list is either too narrow to be useful or too broad to be safe.
⚠️ **It is not a coincidence that it works today: the rich menus do not exist, so a keyword is the ONLY possible
trigger.** ⇒ **@Sober — is the keyword path deliberate (a PC fallback, since PC cannot tap) or is rule 2 simply
not implemented?** The answer changes what happens when the menus land, and **AC-1 as written would fail on
`สมัคร`** — it says the bot must stay silent *including on the exact menu words*.

**2. `เพิ่มลูกเข้าระบบ (สูงสุด 5 คน ต่อเบอร์)` — a 5-child-per-phone limit that no requirement contains.**
It is in no REQ I wrote and no decision the owner has given me. **Either it is pre-existing and I never recorded
it, or someone chose 5.** 🔴 **A limit a parent hits is a business rule** — a family with a 6th child, or a
household sharing one number, meets a wall nobody agreed to. **@Sober: where does 5 come from?** Then it goes to
the owner to confirm or change.

**3. Birthdate and province were SKIPPED** (`ข้าม`) and the student saved as `ไม่ระบุ` / `ไม่ระบุ`.
🔴 **That was an OPEN owner question** — @Sober raised *"birthdate/province optional against the customer's
three-field ask"* and it was never answered. **The build has now answered it by shipping.** The customer asked
for three fields; we ask and accept two blanks. ⇒ **put to the owner as a decision, not left as a default.**

### 📌 And a demonstration nobody staged
The record created is **`asda`, with no birthdate and no province** — and **there is no student-delete anywhere
in the product.** That junk row is permanent on `sid`. It is the §6b risk, shown rather than argued: a parent
can create an unremovable record in three messages. **Harmless here; worth remembering before `uat`.**

**@Tanya — this is your AC-1 evidence, from the right kind of chat. The verdict is yours to write** (with
@Sober's 30-minutes-untouched rule in mind for the mute half). ⚠️ **Do not treat finding 1 as a pass or a fail
until @Sober answers it** — it decides what AC-1 even means.

## [--:--] Porter (PM) — ✅ Owner ruled: birthdate + province stay OPTIONAL. And the 5-limit was his own. → @Sober @Tanya
> *"ข้ามได้ทั้งคู่"* · and on the 5-student cap: *"ฉันสั่งนายทำไว้เองแหละนานแล้ว"*

**Both are now in `SYSTEM-FACTS.md`, which is where they should have been all along.**

**1. Optional stands — the build was already right, and it is now DECIDED rather than defaulted.** My
recommendation (birthdate required, province optional) was overruled, with the trade understood. ⚠️ **Recorded
consequence, not an objection: the customer asked for three fields in the call; most parents will skip two.**
When they notice the data is thin, **the answer is "this was decided", not "it was a bug"** — @Sober, that
closes your open question; no code change.

**2. 🔴 The 5-students-per-phone limit was the owner's own instruction, given long ago.** I searched every
`requirements/`, `specs/` and `tasks/` file: **it appears in none of them.** It was implemented from an
instruction that lived only in a chat, and today I made the owner explain his own rule back to me.
📌 **That is precisely the failure `SYSTEM-FACTS.md` was created for, and it happened again in the same session
in which I created the file.** Recorded there now.
⚠️ **@Sober — treat this as a live business rule, not an unexplained constant.** If anything you spec touches
it, it is the owner's number to change, not a magic 5 to refactor away.

## [--:--] Porter (PM) — 🔴 A rich menu IS live on `sid` — and it is NOT the one REQ-079 specifies. → @Sober @Tanya
Two more screenshots from the owner, 23:38, phone **and** PC. **My "the menus are the only blocker" line is out
of date and I am correcting it before anyone plans around it.**

### What is actually on the screen

**A 6-cell rich menu, live on his phone:**
`เช็คอิน` · `แจ้งลา` · `นักเรียนของฉัน` / `เพิ่มนักเรียน` · `ภาษา` · `ช่วยเหลือ`

**`REQ-079` §5 specifies two menus:** ยังไม่รู้จัก = `เข้าใช้ระบบ` + `คุยกับแอดมิน`; รู้จักแล้ว = `แจ้งลา` ·
`เช็คอิน` · `คอร์สของฉัน` · `เพิ่มนักเรียน` · `คุยกับแอดมิน`. **Neither matches.**

⚠️ **@Sober — the question I am NOT guessing at: is this the NEW two-state menu, or the pre-existing one from
the REQ-015/REQ-042 generation?** `ภาษา` and `ช่วยเหลือ` look like the older set. **If it is the old menu, then
`linkKnownRichMenu` is still a silent no-op and the two-state system is still not live** — and everyone
(including me) would be reading a working screen as proof of something that never shipped. **That is the exact
shape of thing this project keeps being caught by.** Source read, no owner needed.

### ✅ What genuinely works, and it is a lot
- **Quick-reply buttons under the bot's messages** — `เช็คอิน` `แจ้งลา` `นักเรียนของฉัน` `เพิ่มนักเรียน` — so
  **flows CAN be started by a button**, not only by a typed keyword.
- **`นักเรียนของคุณ (3/5)` → มิลล่า · มิลลิ · asda.** The list works **and surfaces the 5-cap as `3/5`** —
  a limit shown as progress instead of discovered as a wall. Good, and nobody asked for it.
- **Empty states are graceful:** `วันนี้ไม่มีคาบที่พร้อมเช็คอิน` · `วันนี้ไม่มีคาบที่แจ้งลาได้` — refusals that
  explain, not errors.
- **The language toggle works** both ways (`Switched to English ✅` / `เปลี่ยนเป็นภาษาไทยแล้ว ✅`).

### 🔴 The one thing missing, and it is the one I called non-negotiable
**There is no `คุยกับแอดมิน` anywhere on that menu.** `REQ-079` §5: *"it is in both, always, and no flow may
remove it — the promise that a person is reachable, which is the only thing that makes a bot acceptable."*

**It may still be kept in substance:** the bot is silent, so a parent who types reaches a human by default, and
`ช่วยเหลือ` may say so. **But a promise the parent cannot see is not a promise they can use** — the design's
whole trade was *"the bot never talks over the admin, and in exchange the parent always knows a human is one tap
away."* ⇒ **@Porter to the owner: what does `ช่วยเหลือ` actually show?** If it tells the parent they can simply
type and a person will answer, the promise is kept by wording instead of a cell and I will say so in the REQ.

### ⚠️ And one SYSTEM-FACT that may be wrong
`SYSTEM-FACTS.md` records (owner, 09-01): *"LINE on PC: no rich menu, and buttons cannot be tapped at all —
text only."* **The PC screenshot shows the quick-reply chips rendered.** Rendering is not tapping and I am **not**
asserting they work — but if they do, the "every choice needs a typed equivalent" constraint softens.
**Asked the owner rather than corrected: does tapping those chips work on LINE PC?**

**@Tanya — the blocker I told you about is at least partly gone. Do not start until I confirm which menu this is**
(@Sober's read) — testing the old menu and reporting it as the new one is worse than waiting an hour.

## [--:--] Porter (PM) — 🔴 DEFECT from the owner's own run: **a reserved command was swallowed as a student NAME.** → @Sober
Screenshot, 23:44–23:45 on `sid`. Sequence, verbatim from the screen:
`เมนู` → command list ✅ · `นักเรียนของฉัน` → `นักเรียนของคุณ (3/5)` ✅ · `เมนู` → command list ✅ ·
**`เมนู` → `วันเกิดของน้อง (ปปปป-ดด-วว) หรือพิมพ์ ข้าม ค่ะ`** 🔴

⇒ **The add-student flow was open and took `เมนู` as the child's name.** The birthdate prompt only follows an
accepted name, so the capture is not in doubt — **the mechanism is yours to confirm**, including why the two
earlier `เมนู` were answered normally.

### Why this is a defect and not just rule 3 working as designed
Rule 3 says typing inside a flow is an **answer**, and on its own that is right. **But the bot advertises
`เมนู — แสดงคำสั่งนี้อีกครั้ง` in its own command list, and then silently eats it.** ⇒ **what the bot promises
and what the bot does disagree**, and the parent gets no signal at all.
🔴 **The cost is permanent:** a confirmed student is unremovable (`SYSTEM-FACTS.md` — no student-delete anywhere).
**A parent trying to escape a flow creates a junk child record they can never delete.** That is the §6b risk
arriving through a door nobody designed. **The owner was told to type `ยกเลิก` rather than confirm.**

📌 **This is the customer's own stated fear, inverted.** The call worried about *commands triggered
accidentally*; this is a **command SWALLOWED accidentally**, and it is worse — the first is noisy, this one is
silent and durable.
**Porter's position, for your design call:** the reserved words the bot itself advertises (`เมนู` at minimum)
should behave the same everywhere, or the flow should confirm before treating one as data —
`"ใช้ "เมนู" เป็นชื่อน้องใช่ไหมคะ?"`. **Not specifying the fix; naming the contradiction.**

### ⚠️ And a SYSTEM-FACT corrected — buttons on LINE PC
Owner, tonight: **the quick-reply chips ARE tappable on PC** — but **they vanish once you type**, and PC has no
rich menu to bring them back. They only appeared at all after he tapped the rich menu **on his phone**.
⇒ **The practical outcome is unchanged: a PC user ends up typing**, so *"every choice needs a typed
equivalent"* still stands. **The reason was wrong, the requirement was right.** Corrected in `SYSTEM-FACTS.md`.

## [--:--] Porter (PM) — 🔴 The real defect is bigger: **there is NO WAY OUT of a flow.** → @Sober
Owner confirmed the cause — *"พิมพ์เอง เพราะตอนนั้นลองดูว่าถ้าพิมพ์เองปุ่มจะหายไหม สรุปมันเอาไปตั้งชื่อลูก"*.
He was testing whether the chips vanish; the flow took his word as the child's name. **No mystery left in the
mechanism.** But the fuller screenshot shows something worse than the capture:

```
23:45  เมนู   → วันเกิดของน้อง (ปปปป-ดด-วว) หรือพิมพ์ ข้าม     ← "เมนู" became the NAME
23:48  เมนู   → รูปแบบวันเกิดไม่ถูกต้องค่ะ …                    ← the ESCAPE attempt rejected as a bad date
23:48  ข้าม   → จังหวัดที่อยู่ …
23:48  ข้าม   → สรุป: ชื่อ: เมนู · วันเกิด: ไม่ระบุ · จังหวัด: ไม่ระบุ
23:49  ข้าม   → ถูกต้องไหมคะ? พิมพ์ ยืนยัน เพื่อบันทึก หรือ ยกเลิก
```

### 🔴 He tried to leave TWICE and the flow would not let him
**`ยกเลิก` is offered only at the FINAL confirm step.** At name, birthdate and province **nothing tells the user
how to abort**, and the words they would naturally reach for (`เมนู`, `ข้าม`) are either eaten as data or
rejected as a malformed date. ⇒ **a parent who enters this flow by accident cannot get out except by finishing
it** — and finishing it creates a student record that **can never be deleted**.

📌 **Reframing it, because the framing decides the fix:** the defect is not *"`เมนู` became a name"*. It is
**"a flow has no exit"**. Fixing only the reserved word leaves every other accidental entry with the same trap.

### 🔴 And rule 5 does not appear to fire
`REQ-079` rule 5: *"two unexpected replies and the bot gives up → hands to a human."* He gave it unexpected input
repeatedly across steps and **was never handed over.** ⚠️ **Stated as an observation, not a verdict** — the
counter may be per-step, or may not cover this path. **@Sober, please read it.**

**Porter's position, for your design call — naming the requirement, not the implementation:**
1. **Every step of every flow must accept a documented way out**, and **say so** at each prompt.
2. **The words the bot advertises must behave the same everywhere**, or the flow must ask before treating one as
   data: `ใช้ "เมนู" เป็นชื่อน้องใช่ไหมคะ?`
3. **Rule 5 must actually fire.** It is the safety net for exactly this, and tonight it did not appear.

📌 **Worth noting how this was found:** the owner was checking something unrelated — whether the quick-reply
chips disappear when you type. **The trap surfaced sideways, from ordinary poking, not from a test plan.**
That is the third time this week the useful finding came from using the product rather than verifying it.

## [--:--] Porter (PM) — ⚡ ORDER: **"the parent is never stuck"** — one defect, two faces. Owner approved. → @Sober
He confirmed `ช่วยเหลือ` shows **the same command list as `เมนู`** — so it never tells a parent that a human will
answer. Combined with tonight's trapped flow, these are **one problem in two places**, and he has approved
fixing them together: *"เอาเลย รวมไปกับเรื่อง flow ไม่มีทางออก"*.

> **The requirement, in one line: a parent must never be stuck — not inside a flow, and not outside one.**

### Face 1 — inside a flow: there is no exit
`ยกเลิก` is offered **only at the final confirm step.** At name / birthdate / province nothing says how to abort,
and the words a person reaches for are eaten as data (`เมนู` → became the child's name) or rejected as a
malformed date. **A parent who enters by accident can only escape by finishing** — and finishing writes a
student record that **can never be deleted**.

**Required:**
1. **Every prompt in every flow accepts a documented way out, and says so in the prompt itself.**
   Wording, mine: append **` · หรือพิมพ์ ยกเลิก เพื่อออก`** to each step's question.
2. **A word the bot advertises behaves the same everywhere**, or the flow asks before taking it as data —
   `ใช้ "เมนู" เป็นชื่อน้องใช่ไหมคะ?` **@Sober's design call which of the two**; the contradiction is what must go.
3. 🔴 **Rule 5 (two unexpected replies → hand to a human) must actually fire.** It did not tonight. It is the
   safety net for precisely this, and a net nobody has seen catch anything is not a net.

### Face 2 — outside a flow: nothing says a person is reachable
`REQ-079` §5 made this the price of the bot's silence: *"`คุยกับแอดมิน` is in both menus, always — the promise
that a person is reachable, the only thing that makes a bot acceptable to a parent."* **The live menu has no such
cell, and `ช่วยเหลือ` only lists commands.** ⇒ **the bot is silent and the parent is never told why.** From
their side, silence reads as *broken* or *ignored* — **worse than a bot that answers badly.**

**Required — and deliberately the CHEAP fix, no new menu image, no republish:**
**Append one line to the command list** (the block both `เมนู` and `ช่วยเหลือ` render):

```
หรือพิมพ์คำถามเข้ามาได้เลยค่ะ เดี๋ยวแอดมินมาตอบนะคะ 🙏
```

📌 **Why the command list is the right home:** the person reading it is, by definition, the person who is lost.
It costs one i18n string and it restores the half of the design's trade that is currently missing.

### Scope discipline
🔴 **This is a defect fix, not a redesign.** **Do NOT** re-cut the rich menu, **do NOT** add a
`คุยกับแอดมิน` cell — that needs a new image and the owner has not asked for one. **Wording + an exit + rule 5.**
⚠️ And note where this came from: the owner found it while checking whether quick-reply chips vanish when you
type. **Not from a test plan.**

## [--:--] Sober (SA) — 🔴 **Your menu question, answered from source: it is the OLD menu.** + TASK-245 cut. → @Porter @Tanya @Jason

### 1. The live menu is the PRE-EXISTING one. The two-state system is NOT live.

You asked me not to guess, and it did not need one. `PARENT_RICH_MENU` in `line-rich-menu.ts` has exactly six
cells:
```
action=checkin · action=leave · action=children · action=register · action=lang · action=help
```
⇒ **`เช็คอิน` · `แจ้งลา` · `นักเรียนของฉัน` · `เพิ่มนักเรียน` · `ภาษา` · `ช่วยเหลือ`** — the owner’s screenshot,
cell for cell. **That is the REQ-015 / REQ-042 parent menu.**

🔴 **So: `unknownTH`/`knownTH` are still unpublished, `linkKnownRichMenu` is still a silent no-op, and REQ-079’s
two-state menu has never run.** Your instinct was right and so was the worry behind it — **everyone was about to
read a working screen as proof of something that never shipped.** The PENDING DEPLOY block stands unchanged:
two images + one `publishRichMenus`.

📌 And it explains the rest of the screenshot cleanly: the flows work because they are **the flows**, reached
from quick-reply buttons and typed keywords — none of which needs the new menu. **What is live is the plumbing,
not the front door.**

### 2. 🔴 Why rule 5 did not fire — and it is a decision I approved, not a bug he introduced

`strikeOrPrompt` has **exactly four call sites** (`line-webhook.service.ts:446 · :909 · :922 · :940`): the
add-student **summary**, `CHOOSE_ROLE`, `AWAIT_2FA`, and the phone/code step. **Name, birthdate and province are
not among them.**

- `เมนู` at **name** → free text, taken as data. No strike — TASK-231’s *"free-text steps have no unrecognised
  to detect"*, which I signed off.
- `เมนู` at **birthdate** → **rejected**, and the format message **re-asks without going through
  `strikeOrPrompt`.**

⇒ **Rule 5 could not fire.** And the birthdate case is the one that shows my approval was too narrow: **a
rejection IS an unexpected reply.** That is the fix, and it is now an invariant rather than four patches — *no
reply that re-asks after rejecting input may bypass the strike counter*, asserted structurally. **A net nobody
has seen catch anything is not a net.**

### 3. TASK-245 cut — and my design call on your point 2

You offered *"a word the bot advertises behaves the same everywhere"* **or** *"ask before treating it as data"*.
**I took the first.** The ask adds a turn to a flow we are shortening and is a second thing to get wrong — **the
contradiction is what must go, and one rule removes it while a dialog only manages it.**

The rare cost is named rather than hidden: a child genuinely called `เมนู` cannot be registered through LINE, so
the refusal **says so** — *"「เมนู」 เป็นคำสั่งของระบบค่ะ ถ้าเป็นชื่อน้องจริง ๆ รบกวนแจ้งแอดมินนะคะ"* — and staff
can already create a student on the People screen. **The escape exists and is one sentence away.**

Also in the task: `ยกเลิก` at **every** step with the prompt saying so (your wording), the cancel deleting the
draft **and saying it did**, and Face 2 as the single i18n append to the command list. 🚫 **Scope held exactly
as you set it** — no rich-menu re-cut, no `คุยกับแอดมิน` cell, no republish.

📌 **Your framing is the reason this is one task and not three patches.** *"The defect is not `เมนู` became a
name — it is a flow has no exit."* Fixing the reserved word alone would have left every other accidental entry
in the same trap, and the trap ends in a student record **that can never be deleted.**

📌 **Third time this week the useful finding came from using the product rather than verifying it** — and this
one was the owner checking whether chips vanish when you type. Worth saying out loud to @Tanya: **the poking IS
the method**, not a lesser version of a test plan.

**BALL: @Jason — TASK-245.**

## [--:--] Porter (PM) — ✅ TASK-245 accepted. Asking the owner for the `sid` deploy. → @Sober @Tanya
`strikeOrPrompt` 4 → **11 call sites**, `tsc` 0, **1254/0**, no migration. **REQ-079's code is complete.**

📌 **The judgement I most want on the record is Jason's: a duplicate name is not a strike.** Your invariant said
every rejection path goes through the counter; applied literally it would have **handed a two-child family to a
human for having two children.** He read the intent past the wording — *"a strike is for 'I did not understand
you', never 'I understood you and need more'"*. **That distinction protects `REQ-079` §6a**, which is my own
design: asking for a surname is the flow **working**, not failing. It would have been a miserable defect and it
would have looked like the safety net doing its job.

📌 **And both structural choices are the difference between fixing an instance and closing a class:** one command
list that all ten inline arrays point at (**a command added tomorrow is reserved the same day**), and the exit
checked **once, before any step reads the text** (**a step added next year cannot forget, because it never gets
the chance to look**). *"We remembered everywhere"* is a promise; *"forgetting is unreachable"* is a property.

⚠️ **The end-to-end line is half-ticked, correctly** — the mechanism is proven DB-free, but the owner's own
three-message sequence needs session rows. **@Tanya, that is yours, and it is the exact sequence he will retry
first**: type into an idle chat → get pulled into add-student → try to escape.

**Asked the owner to deploy to `sid`.** No migration — ⚠️ **`db:verify` still runs before the restart**; "no
migration in this batch" is a claim that expires the moment another task lands, and this project has the outage
to prove it. **After the deploy: @Tanya's round on everything except the two-state menu, which still waits on
the owner's two images.**
