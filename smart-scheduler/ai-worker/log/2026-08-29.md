# 2026-08-29 — log

## [--:--] Porter (PM) — 📌 **CURRENT ORDERS — read this file, not 2026-08-28.** → @Sober @Tanya @everyone
**Nothing happened between 2026-08-28 14:07 and now** — I checked the artefacts rather than assuming: no new log
entries, no new task files, no `uat` deploy. **The state is exactly as handed off.** The owner is starting his
`sid` test pass now.

⚠️ **`2026-08-28.md` is CLOSED for orders.** I spent three days appending to a stale file last week and @Sober ended
up reconstructing my priorities from it and parking live work. **Newest orders live in today's file. Always.**

### State
| item | state |
|---|---|
| **FIX-007** expiry | ✅ DELIVERED both boxes |
| **REQ-071** Drop | ✅ signed `sid` — **not on `uat`** |
| **REQ-072** parts 1 · 2 · 3A | ✅ signed `sid` — **LINE send path unverified** |
| **REQ-072** part 3B (08:15) | ✅ code+reviewed — **never executed** |

**Two LINE messages no human has seen** (confirm summary with leave dates · the daily reminder). **The owner's
phone is the only place they can be judged** — QA correctly skipped both (cannot link a recipient).

### → @Sober — the import-form batch is yours now, and it is the only unblocked lane
It has been ToDo since the owner found it on 2026-08-28. **It does not touch anything he is testing**, so it runs
in parallel. Four items, **#1 is the one with consequences**:
1. 🔴 **`จำนวนครั้งที่ซื้อ` is a free number input** and an off-card size **crashes with `เกิดข้อผิดพลาดภายในระบบ`** —
   the validator deliberately allows 1–100 (`validation.ts:496`) while the rest of the system knows only 4/6/10.
   **Intent and implementation disagree.** The owner's design: **default = the fixed 4/6/10 choice**, plus an
   explicit **"off-card" option** revealing a free size **and a manual leave-quota field**.
   📌 **One extra input, not two — `MAX_WEEK = size + leaveQuota` at every size** (4+1=5 · 6+2=8 · 10+3=13, the
   owner's own rule). **Do not add a "max week" field**, and consider collapsing the two constant tables, which
   cannot answer for a size they have never heard of.
2. **Expiry defaults to today + 2 months** — a number from no rule. Compute the default; **keep it editable**, since
   a human's typed import expiry is meaningful (his ruling, and why we skipped 164 courses in the repair).
3. **The dialog does not close after saving.**
4. **A rejected size must say why, in Thai** — not a 500.

**Ball: @Sober** (import form) · **owner** (the `sid` test pass) · **@Tanya: nothing until he reports.**

## [--:--] Porter (PM) — 🆕 Owner adds THREE, all gating `uat`. Mapped, specified, one design question back. → @Sober
Owner: *"งานเพิ่ม ต้องทำให้จบก่อนขึ้น uat"* — so the single `uat` deploy now waits on these too.

**Board numbers (his list and the board are different namespaces — I collided them once already):**
| owner's | board | title |
|---|---|---|
| REQ-006 | **REQ-073** | confirm popup on `มาเรียน` / `confirm ส่งไลน์ครู` from the calendar |
| REQ-009 | **REQ-074** | cancel a **1HR / Voucher** booking, with a reason popup |
| REQ-010 | **REQ-075** | day abbreviations `Mo/Tu/We` → `Mon/Tue/Wed`; **"Weekly course" → "Course"** |

### REQ-073 — confirm popup ❓ **one question back to the owner before this is built**
Both actions deserve a confirm — `confirm` **messages a teacher** and `มาเรียน` **consumes a session** (undoing it
needs a cancel-with-reason).
🔴 **But there is a tension worth naming, and it is his own incident:** on 2026-08-23, **15 real sessions went
`NO_SHOW` because staff pressed confirm and never pressed `มาเรียน`.** We fixed the consequence (REQ-070) — but
**adding a popup to `มาเรียน` puts friction on the exact action we need staff to do MORE of**, while `confirm`
(which they already do) stays one click. **That is backwards from the behaviour we want.**
⇒ **Ask, don't assume: popup on BOTH, or only on `confirm`?** My lean: **`confirm` yes** (it sends a message to a
human), **`มาเรียน` no** — or an *undo* toast instead of a *pre*-confirm, which protects the click without taxing it.
**His call. Do not build until he answers.**

### REQ-074 — cancel a 1HR / Voucher booking 📌 **reuse, do not invent**
His three reasons — **แอดมินซื้อผิด · แม่ไม่มาแล้ว/ยกเลิก · เปลี่ยนกิจกรรม** — are **exactly REQ-036's enum**:
`ADMIN_ERROR` · `CUSTOMER_CANCELLED` · `PROGRAM_CHANGED`. **Use the same enum, the same three labels, the same
dialog shape.** A second parallel reason-set would drift and would make *"find every admin-error cancellation"*
impossible to answer in one query.
- *"ระบุตำแหน่งที่กดให้ชัดเจน (หน้าไหน / ตรงไหน)"* — **that is our answer to give him, not his to specify.**
  Propose the placement in the spec with a screenshot reference.
- **Money:** a `SINGLE_SESSION` posts revenue **at day-end when ATTENDED**; a **voucher posts at sale**. Different
  consequences. ⚠️ **Record the reason; do not build refund logic** — same line we held on REQ-036, and the reason
  enum is what makes cleanup possible later.

### REQ-075 — labels. Small, FE-only
⚠️ **"Weekly course" is the booking-type label we shipped hours ago** (REQ-052/TASK-142, `คอร์สรายสัปดาห์`) — it
lives in the cell, the legend and the toggle. **Change it in the dictionary, both languages, and re-check the
rendered cell**, not just the key.

**Ball: @Sober** — REQ-074 and REQ-075 can be cut now; **REQ-073 waits on the owner's answer.** Import-form batch
still open. **Ball: owner** — the popup question, plus his `sid` test pass.

## [--:--] Porter (PM) — 🔴 3B defect: the idempotency guard also skips the `job_runs` row. + parent-LINE is a KNOWN state. → @Sober
Owner ran `sm-daily-reminder` **twice** on `sid`. **`job_runs` holds ONE row.**

### The defect — it breaks the exact property the other jobs were built to have
The second run correctly sent nothing **and also recorded nothing.** ⇒ **"ran, nothing to send" and "never ran"
are once again indistinguishable** — the failure we have complained about twice (`month-reset` writes no
`job_runs` row at all, and it is still queued for that).
**The existing pattern is right there:** `internal.ts:41` on the digest — *"idempotent per business date, and it
**ALWAYS** writes a `job_runs` row so 'never ran' stays visible."*
⇒ **`reminderAlreadySent` must gate the SENDING, not the RECORDING.** Second run: `job_runs` row written, sends 0.

### 🟡 Second, smaller: `sent: true` when nobody was reached
`sid` summary: `sessions 3 · teachers 1 · parents 3 · unlinkedTeachers 1 · unlinkedParents 3` — **zero messages
delivered — and the row says `sent: true`.** Anyone later reading `job_runs` to answer *"who did we notify
yesterday?"* gets the wrong answer. **Same class as `revenuePosted` counting replays as posts.**
⇒ **`sent` should be the number actually delivered**, with the skip counts beside it.

### 📌 RECORDED AS A KNOWN STATE, NOT A DEFECT — owner's ruling
**`uat`: 0 of 180 parents have a LINE link.** ⇒ **the parent half of REQ-072 (3A confirm-notify and 3B daily
reminder) reaches nobody on the customer's system.**
> Owner: *"uat ลูกค้าแค่ยังไม่ใช้เฉย ๆ ปล่อยไปอย่าไปยุ่ง"*

**The linking flow exists and works; no family has used it.** That is the customer's own adoption, **not ours to
push** and **not a bug to fix**. ⚠️ **Written down so nobody "repairs" it later** — no broadcast, no prompt, no
nudge to 180 families, unless the owner asks. **The teacher half works and is worth shipping on its own.**

**Ball: @Sober** — the `job_runs`-on-every-run fix (and fold in `month-reset`'s missing row while you are there —
it is the same one-line habit), plus the `sent` count. **Ball: owner** — nothing on 3B; REQ-073's popup question is
still open.

## [--:--] Porter (PM) — ✅ REQ-073 answered: **a principle, not a per-button call.** Criterion + the actual list. → @Sober
> Owner: *"popup ทุกที่ที่ควรมีการ confirmation"*

**Broader than the two options I offered — he is setting a rule, not picking buttons.** He also effectively
overruled my `มาเรียน` concern; **recorded once below, then built as asked.**

### The criterion — so "ควรมี" is decidable and not re-argued per button
**Confirm when the action is at least one of:**
1. **Hard to undo** — consumes quota, ends/pauses a course, cancels something delivered.
2. **Reaches a human** — sends LINE to a teacher or a parent.
3. **Moves money** — charges, or posts revenue.

**Do NOT confirm** navigation · filtering · the display toggle · opening a form · anything a second click reverses
for free. **A popup on a harmless action teaches staff to dismiss popups**, which costs us the ones that matter.

### The actual list — enumerate from the routes, do not take mine as complete
**Missing a confirmation today:**
- **`มาเรียน` (attend)** — consumes a session (1)
- **`confirm`** — messages the teacher (2)
- **`ลา/ป่วย`** — consumes leave quota and can append a make-up (1)
- **`เพิ่มคาบ (คิดเงิน)`** — charges (3)
- **`แทรกคาบชดเชย`** — changes the plan (1)
- **`ปลดล็อก (แอดมิน)`** — overrides the leave lock (1)
- **Confirm-whole-course (REQ-072)** — messages teacher **and** parent, in bulk (1+2)

**Already има one — reuse the shape, do not invent a second dialog:** cancel a course · pause · resume · cancel a
delivered session (reason required).

### 🔻 My `มาเรียน` concern, stated once and then dropped
On 2026-08-23 **15 real sessions went `NO_SHOW` because staff pressed `confirm` and never pressed `มาเรียน`.**
A popup taxes the action we most need them to perform. **The owner has ruled; we build it.**
⇒ **Mitigation that honours the decision instead of fighting it: make the `มาเรียน` confirm the CHEAPEST dialog in
the app** — one line, Enter/primary-focused, dismiss on Esc, **no reason field.** The heavy dialogs (reason enum,
named blast radius) belong on cancel/pause/charge. **Same rule, proportionate weight.**

**Ball: @Sober** — REQ-073 now specifiable; with REQ-074, REQ-075, the import-form batch and the 3B `job_runs` fix.
**Ball: owner** — nothing; `uat` still waits for 073/074/075.

## [--:--] Sober (SA) — read today's CURRENT ORDERS. 3B job_runs defect is mine; fix cut (TASK-209). Backlog logged. → @Jason
RULE ZERO: read Porter's 08-29 CURRENT ORDERS first. My lane is a batch — and one item is a defect in code I passed.

🔻 **MINE (attribution): the 3B `job_runs` defect.** `runDailyReminderJob` early-returns on `reminderAlreadySent`
BEFORE the `job_runs` insert, so a second run sends nothing **and records nothing** — "ran, nothing to send" vs "never
ran" indistinguishable. **My TASK-208 review checked the guard fires, not that a row is ALWAYS written** (the digest
states that rule outright, `internal.ts:41`). Mechanism-not-outcome again. **TASK-209 → @Jason:** gate the SEND not the
RECORD (every run writes a row, re-run `sent:0`); make `sent` the delivered count not `true` (sid reached 0 but said
`sent:true` — the `revenuePosted`-replay class); fold in `month-reset`'s missing row.

📌 **Recorded, do NOT fix (owner): `uat` 0/180 parents linked** → REQ-072 parent half reaches nobody there; customer
adoption, not a bug — no nudge to 180 families. Teacher half ships on its own. (In TASK-209 so nobody "repairs" it.)

**My remaining CURRENT-ORDERS backlog (all gating uat, I'll take them in turn):** the **import-form batch** (size
option+quota · expiry default · dialog-close · 500-instead-of-a-message) · **REQ-074** + **REQ-075** (cut now) ·
**REQ-073** (confirm-popup principle — now answered: cheapest possible `มาเรียน` dialog, heavy dialogs only on
cancel/pause/charge). Next turn I take one of these; the 3B fix goes first as the correctness defect.

**Ball: @Jason — TASK-209** (job_runs-always + honest `sent` + month-reset row).

## [Jason (BE)] — TASK-209 BUILT → REVIEW. **The fix had a second bug hiding inside it.** → @Sober @Porter
Your defect, my code — and the useful part is what the repair nearly did.

**The guard now gates the SEND, never the RECORD.** A re-run writes `{ attempted: false, sent: 0, reason:
"already-sent" }` instead of returning early, so two runs leave **two rows**. "Ran, nothing to do" and "never
ran" are distinguishable again, which is the single property these rows exist for.

**`sent` is the delivered count now**, with `skipped` beside it, and **`attempted`** is the separate fact that
the job ran. One boolean was being asked two questions — *did it fire?* and *did it reach anyone?* — and on
`sid` it answered the second one wrongly: a run that reached **zero** people recorded itself as sent.

### 🔴 The bug inside the fix
`reminderAlreadySent` keyed on `summary.sent === true`. The moment `sent` became a **count**, that predicate
would have made the job **re-run all morning on exactly the days it reached nobody** — the days we most need it
to behave. It keys on `attempted` now, with a test pinning that specifically. Worth naming because it is the
shape we keep hitting: a change that is correct in the place you are looking and wrong one line away.

**`month-reset` writes a row too** — it wrote none at all, so "the monthly reset never ran" was invisible in
precisely the way the day-end job's absence was on `uat`.

**And I made it a property instead of a memory:** *"every scheduled job inserts a `job_runs` row"* is now
asserted across all three service files. **Between us we have missed this twice**; a fifth job should fail a
test rather than depend on either of us remembering. That is the honest version of "I'll be careful next time".

**My TASK-208 test asserted `sent: true` and passed** while the run reached nobody — rewritten to assert the
count and the flag. The assertion had to change, not just the code; a test that agrees with the bug is not a
test.

**Verified:** tsc **0** · `bun test` **903 pass / 0 fail** (+4). No migration.

### ⛔ @Porter — the outcome check is one command, run twice
`sm-daily-reminder` twice on `sid`, then read `job_runs` for that date:
- **two rows**, the second `sent: 0` / `attempted: false` / `reason: "already-sent"`;
- the first row's `sent` will be **less than teachers + parents** — on `uat`, teachers only, since 0 of 180
  parents are linked. **That is the number being honest, not a defect** (owner's ruling, carried in the task so
  nobody "repairs" it).

**Ball: @Sober** — review. **@Porter** — the twice-run check; and the TASK-206 phone look at the confirm
message is still unclaimed, which is the last unseen thing in REQ-072.

## [--:--] Sober (SA) — TASK-209 (3B job_runs fix) ✅ PASS. Model fix + a class-guard. → @Porter (sid re-run)
Reproduced tsc 0 · `bun test` **903/0**. Guard gates the SEND not the RECORD — a re-run writes
`{attempted:false, sent:0, reason:"already-sent"}`, so two runs leave **two rows** ("ran" vs "never ran" distinguishable
again). `sent` is the delivered count with `skipped`/reach beside it; `month-reset` writes a row now. 🔴 Jason caught the
bug **inside** the fix: `reminderAlreadySent` had keyed on `sent === true`, which as a COUNT would have made the job
re-run all morning on zero-reach days — re-keyed on `attempted`, pinned by a test. And he made "every scheduled job
inserts a `job_runs` row" a **cross-file property test** — the structural guard that stops the class (we've missed it
twice). He also rewrote his TASK-208 test that asserted `sent:true` while reaching nobody ("a test that agrees with the
bug is not a test").

**Held to my lesson:** source-shape proven; the **two-rows-on-sid** outcome is the owner's re-run (`sm-daily-reminder`
twice → `job_runs` holds 2) — UNVERIFIED until then. Folds into the REQ-072 uat batch.

**Ball: @Porter** — the sid re-run to confirm two rows; TASK-209 joins the one-deploy REQ-072/Drop uat batch.
**My lane next (I proceed on the next go):** REQ-074 + REQ-075 (cut now), REQ-073 (confirm-popup principle), the
import-form batch — all gating uat.

## [--:--] Sober (SA) — started the uat-gating batch: REQ-075, REQ-074, import-form cut. REQ-073 next (route-enumerate). → @Jason + @Fern
Read Porter's specs first (RULE ZERO). Cut the three ready lanes; REQ-073 I take next because Porter said **enumerate
the confirm-list from the routes myself** (don't take his as complete) — that's a grounding pass, not a copy.
- **REQ-075 (labels) → TASK-210 (Fern):** Mo/Tu→Mon/Tue + "Weekly course"→"Course" (the REQ-052 cell label — change the
  dict both langs, **re-check the rendered cell**, not the key).
- **REQ-074 (cancel 1HR/voucher) → TASK-211 (BE) + TASK-212 (FE):** **reuse REQ-036's enum** (ADMIN_ERROR/CUSTOMER_
  CANCELLED/PROGRAM_CHANGED) + the same dialog — a second reason-set would break "find every admin-error cancel".
  **Record the reason, NO refund logic** (SINGLE_SESSION posts at day-end-ATTENDED, voucher at sale — a later human's
  call). Placement is **ours to propose** (screenshot ref for owner confirm).
- **Import-form batch → TASK-213 (BE) + TASK-214 (FE):** the free-number size that **500s** on an off-card value →
  default 4/6/10 + explicit off-card {size, quota} (ONE extra field, `MAX_WEEK = size + quota`, no max-week field);
  computed-editable expiry (not today+2mo); **Thai reason not a 500**; **dialog closes on save**.

**Next turn: REQ-073** — enumerate every confirm-worthy action from the router against the owner's criterion (hard-to-
undo · reaches-a-human · moves-money), reuse the existing dialog shape, and the `มาเรียน` confirm = the **cheapest**
dialog (one line, Enter/Esc, no reason field) so we don't tax the action we need staff to do more of.

**Ball: @Jason** — TASK-211 + TASK-213 (BE). **@Fern** — TASK-210 (small) + then TASK-212 / TASK-214 (after their BE).

## [Jason (BE)] — TASK-211 (REQ-074 cancel with reason) BUILT → REVIEW. **I added a migration you didn't ask for.** → @Sober @Fern
Enumerated the cancel paths from the router before writing anything (the TASK-185 habit): there is exactly
**one** — `PATCH /bookings/:id/status` with `action: "cancel"`. No second door. And the existing path did
**not** already do this, so this is real code rather than the thin confirmation the task allowed for: it
required a reason only for a **delivered** session, and stored it as **free text in `note`**.

### 🔴 The judgement call
You said to store the reason on the field the cancel path already uses. **I added a column instead** (`0025`,
the same closed set and CHECK as `0023`), because the DoD's own promise — *"a query for `ADMIN_ERROR` finds it
alongside course cancels"* — **is not deliverable from free text.** A token inside a human sentence answers
that with `LIKE '%ADMIN_ERROR%'`: it matches a note that merely mentions the words, misses one written in Thai,
and stops working the day somebody rephrases the sentence. **"We can clean this up later" is only true if later
is a `WHERE`.** `note` keeps the human sentence; the column keeps the machine one. Q1 asks you to ratify that,
and names the fallback if you'd rather not ship a migration this week.

**Required for `SINGLE_SESSION` and `VOUCHER` only.** A course session's cancel is a **reschedule that re-owes
a make-up** — a different act with its own rules — so forcing the enum onto it would change a path REQ-074
never asked about. Tested: that path is byte-identical, and the column is written only where the enum applies.

**No refund, no ledger write — asserted by absence.** A 1HR posts revenue at day-end *when ATTENDED*, so a
cancel before that posted nothing; a **voucher posts at sale**, so cancelling a session cannot un-post it. The
consequences differ and are a later human's call; recording the reason is what makes that call possible at all.

`reasonCode` is a **5th optional argument**, so every existing caller of `updateBookingStatus` is untouched, and
the *requirement* lives in the service only — zod holding a second copy of a domain rule is how the two drift.

**Verified:** tsc **0** · `bun test` **911 pass / 0 fail** (+8). Nothing run against a database. `0025` is
owner-run, `sid` first.

### ⚠️ @Fern — this one merges WITH TASK-212, and the order matters if it can't
The server now **refuses** a 1HR/voucher cancel that carries no `reasonCode`, so if the BE lands alone the
current cancel button starts returning 400 on those two types. Same rule as TASK-168/169 — **and if we can't
land them together, land the FE first**: an FE sending a field the server ignores is harmless; the reverse is a
broken button in front of staff.

**Ball: @Sober** — review + Q1 (the migration) and Q2 (existing cancelled rows stay NULL — nobody asked those
people for a reason, and a guessed value would be indistinguishable from a recorded one). **@Fern** — TASK-212
whenever you reach it; the field is `reasonCode`, values identical to the course-cancel dialog. **Next from me:
TASK-213 (the import form's 500).**

## [--:--] Porter (PM) — ✅ The `uat` gate is FOUR items, not three. Sober's ordering stands. → @Sober
Owner confirmed: **"ฟอร์มนำเข้า" gates `uat` too.** I had it outside the gating set and flagged Sober for
sequencing a non-gating item first. **He was right, I was wrong — no re-prioritisation.**

**The `uat` gate, complete and explicit so nobody re-asks:**
| board | owner's | what |
|---|---|---|
| **REQ-073** | REQ-006 | confirm popup wherever a confirmation belongs (7+ actions) — **held**, Sober enumerating from the routes |
| **REQ-074** | REQ-009 | cancel a 1HR / Voucher booking with a reason (**reuse REQ-036's enum**) — TASK-211/212 |
| **REQ-075** | REQ-010 | `Mo/Tu` → `Mon/Tue`; **"Weekly course" → "Course"** — TASK-210 |
| **ฟอร์มนำเข้า** | — | the **`เรียนอยู่แล้ว (ย้ายข้อมูล)`** dialog — TASK-213/214 |

**Already done and waiting in the same deploy:** FIX-007 ✅ · REQ-071 Drop ✅ · REQ-072 all four parts ✅ ·
TASK-209 (3B `job_runs`) ✅ — all signed on `sid`.

### 🔻 A naming failure of mine, worth fixing as a habit
**The owner did not recognise his own request** — he asked *"form นำเข้าคืออะไร ไม่น่าจะมีที่ฉันสั่งนะ"*. He
ordered it on 2026-08-28 from **the `เรียนอยู่แล้ว (ย้ายข้อมูล)` dialog**, naming three defects and then answering
my size question with the off-card `{size, quota}` design. **He gave the requirement; I renamed it.**
⇒ **Use the label on the screen, not a term I coined.** He is fine keeping "ฟอร์มนำเข้า" now that it is anchored —
but **the anchor is the button text, and it belongs in the task files** so nobody else has to translate me.
📌 Same class as the REQ-number collision: **I keep introducing a private vocabulary and then wondering why the
team and the owner are working from different pictures.**

**Ball: @Jason** (TASK-213 import-form BE → TASK-211 cancel BE) · **@Fern** (TASK-210 labels now; 212/214 behind
BE) · **@Sober** (REQ-073 enumeration next) · **owner: nothing until the four land.**

## [--:--] Sober (SA) — TASK-211 (REQ-074 cancel) ✅ PASS, migration ratified (it caught MY spec error). → @Jason (TASK-213)
Reproduced tsc 0 · `bun test` **911/0** (+8). Jason enumerated from the router (one cancel path), required the enum for
SINGLE_SESSION/VOUCHER only (course cancel untouched — it's a re-owe), no refund (asserted by absence), `reasonCode` a
5th optional arg so existing callers are untouched.

**Q1 — ratified, and it's MY spec error he caught:** I said "store the reason on `note` (the field the cancel path
uses)", but the SAME task's DoD promised "a query for ADMIN_ERROR finds it" — free-text `note` can't deliver that
(`LIKE '%…%'` matches mentions, misses Thai, breaks on rephrasing). His `0025` column (closed set + CHECK, like `0023`)
makes the enum queryable; `note` keeps the human sentence. Same shape as my other misses — I wrote a DoD (queryable)
that the mechanism I prescribed (free text) couldn't meet; he checked the outcome and fixed the contradiction. Ship it.
Q2 (existing rows stay NULL, no backfill) accepted.

🔴 **Deploy-order:** BE now 400s a 1HR/voucher cancel with no reasonCode → TASK-211 must land **with TASK-212 (FE)** or
FE-first (else broken cancel button — TASK-168/169 lesson). Into the one-deploy uat batch.

📌 Also: consolidated a duplicate TASK-211 file (Jason's notes were in a `-reason-be.md` fragment) into the canonical
task, nothing lost. And noted Porter's 283 — **the uat gate is FOUR items (REQ-073/074/075 + import-form) and my
ordering stands** (import-form DOES gate; Porter had it wrong, corrected).

**Ball: @Jason — TASK-213** (import-form BE — the off-card size 500, computed expiry, Thai error), his stated next.
@Fern: TASK-210 (labels) any time; TASK-212 lands with/after TASK-211.

## [Jason (BE)] — TASK-213 (import form) BUILT → REVIEW. **The 500 was the symptom; two tables were the cause.** → @Sober @Fern
The crash is fixed, but the interesting part is what was underneath it.

**The refusal is a sentence, not a status code.** `decideImportSize` is pure: 4/6/10 needs nothing, any other
size needs its leave quota stated — and a missing one is refused **in Thai, naming the field and giving the
card's three examples**. A staff member can act on that; `เกิดข้อผิดพลาดภายในระบบ` told them nothing at all.

### 🔴 The cause: two hand-typed tables that could not answer for an off-card size
`LEAVE_QUOTA_BY_SIZE` and `MAX_WEEK_BY_SIZE` both knew only 4/6/10, so an off-card size **fell through both to
`quota = 0, maxWeek = 0`** — a course with no leave allowance and an expiry inside its own first week, and
nothing anywhere said so. It 500'd before anyone could create one, which is the only reason no family has one
today. `MAX_WEEK_BY_SIZE` is now **derived** (`maxWeek = size + quota` — your rule, 4+1=5 · 6+2=8 · 10+3=13),
with a test pinning that the derived table still equals those three numbers. Two tables that must agree are two
tables that eventually don't.

The quota is **stored** for an off-card course (`0026`, nullable + CHECK) because there it is *a fact somebody
entered*; `null` means "use the card's", so nothing existing changes. **`maxWeek` is never a column** — a second
stored number that must agree with the first.

### 🔴 The expiry ruling — I NARROWED TASK-195 rather than reversing it
TASK-195 made import compute expiry and **ignore** the caller's date; this task asks for a computed **default**
that stays **editable**. They look contradictory and are the two halves of one ruling: TASK-195 killed the
form's `today + 2 months`, **a number from no rule**; what you and the owner protected when 164 imported
expiries were kept out of the FIX-007 repair was **a date a human actually typed**, which may encode an
agreement no rule of ours can see. So the server computes the default and honours an explicit date. New
read-only `POST /courses/import/preview` hands the FE that default, so the form never owns a second copy of the
expiry rule in a second language — the exact drift TASK-195 and TASK-197 were both spent on.

**📌 TASK-185's completeness test caught the new route by omission — the fourth time this month.** It is quietly
becoming the most useful test in the repo.

**Verified:** tsc **0** · `bun test` **922 pass / 0 fail** (+11). Nothing run against a database. `0026` is
owner-run, `sid` first, and joins `0025` in the same uat batch.

**Ball: @Sober** — review + **Q1** (`0026` is a migration the task did not ask for: without it an off-card
import has nowhere to keep its quota and would read "0 leaves / week 0" — the silent version of the bug; the
only honest alternative is refusing off-card sizes outright, which contradicts the owner's stated intent) and
Q2 (a quota stated on a card size that differs — special agreement, or typo to refuse?). **@Fern** — TASK-214:
the size field defaults to 4/6/10 with one extra **quota** input for off-card, and the expiry comes from
`POST /courses/import/preview` as an **editable** default.

## [--:--] Sober (SA) — TASK-213 (import-form BE) ✅ PASS. FE batch unblocked. → @Fern
Reproduced tsc 0 · `bun test` **922/0** (+11). Off-card size now returns a **Thai sentence** (field + card examples),
not `เกิดข้อผิดพลาดภายในระบบ`; the two constant tables that couldn't answer for an off-card size are collapsed to a
**derived** `maxWeek = size + quota` (pinned 5/8/13); off-card quota stored (`0026`, null = card default); the expiry is
a **computed default via `/import/preview`, editable** — Jason narrowed TASK-195 (kill the garbage today+2mo default)
rather than reversing it (honour a human's deliberate date), and kept the rule server-side so the form owns no second
copy. TASK-185's completeness test caught the new route (4th time — quietly the most useful test in the repo). **Q1
ratified: `0026`** (same as `0025` — a fact someone entered; null changes nothing). Both migrations join the one uat
batch, owner-run sid-first.

**BE for the whole batch is done: REQ-074 (211) ✅ · import-form (213) ✅.** The FE lanes are now all unblocked:
- **TASK-210** (labels) — standalone.
- **TASK-212** (cancel-1HR/voucher dialog) — reuse REQ-036 dialog; **must land with/after TASK-211's BE** (BE 400s
  without reasonCode).
- **TASK-214** (import-form FE: off-card option + quota field + editable computed expiry + dialog-closes).

**My own next lane: REQ-073** (confirm popups) — a focused route-enumeration pass (Porter: enumerate the confirm-worthy
actions myself against the criterion), which I'll do rather than rush here.

**Ball: @Fern — the FE batch (TASK-210, 212, 214).** @Jason: BE batch clear. Deploy stays one-shot to uat once all
signed (Drop + REQ-072 + FIX-007 + TASK-209 already signed on sid; +074/075/import-form/073 to go).

## [--:--] Fern (FE) — TASK-210 done (1 of the 3-task FE batch), rendered-verified. → @Sober
Took the standalone one first.

### Weekdays — a deviation from the wording, on purpose
English now reads **Mon/Tue/Wed…** (dayjs `ddd`) in both places that format a weekday.
🔴 **Thai stays on `dd` (จ. อ. พ.)** — I checked before changing it: dayjs' Thai locale has **no three-letter form**;
`ddd` returns the **full** name (จันทร์, อังคาร, พฤหัส), which is *longer*. Following "both languages" literally would
have widened every weekday header and fought **REQ-052 AC-3**, the 375px no-truncation rule we measured the cell
against days ago. `dd` already **is** the Thai abbreviation, so the intent — a readable short day — is met in both
languages by different tokens. Saying so explicitly since it departs from the REQ's letter.

### "Weekly course" → "Course" / "คอร์ส"
One key, both languages, which is why it lands on cell + legend + type filter together.

### The rendered check, not the key
Per the `endCourse.drop` lesson I read the page rather than trusting resolution:
`Mon · Tue · Wed · Thu · Fri · Sat · Sun` ✅ · type legend/filter `1st Trial · 1 HR · Course · Voucher` ✅ ·
booking-type chips on the bookings table render **`Course`** ✅ · and **`Weekly course` appears nowhere in the
rendered document** ✅.

⚠️ **One surface I could not eyeball:** the *calendar cell* — the mock produced no booking chips for the current week.
It renders through the **same key** the legend and the table use (both verified) and the string is gone from the DOM,
but that's inference, not sight, and the task singled the cell out. **@Tanya: that's the one to look at.**

**Q1 (out of scope, but the owner will see it):** `course.formTitle` still reads **`สมัครคอร์สรายสัปดาห์`** — the same
"รายสัปดาห์" he asked to drop, surviving in the create-course form title. REQ-075 scoped this to the booking-type
label so I did **not** touch it (user-facing copy = Porter's), but flagging it now rather than having it found later as
"the rename was half-done".

**Evidence:** tsc **0** · build **ok** · suite **41/0**.

**Ball: @Fern** (TASK-212 next, then TASK-214 — both in the same uat batch) **& @Sober** (review · Q1 → Porter).

## [--:--] Fern (FE) — FE batch done: TASK-210 · 212 · 214. One BE one-liner blocks the off-card save. → @Sober
All three built; the batch is FE-complete apart from the gap below.

### TASK-212 — cancel a 1HR/voucher booking
**Placement (ours to propose, his to confirm): the booking detail's `⋯` menu**, as a red `ยกเลิกการจอง` beside
ลา / ย้ายคาบ. That menu is already *the* place a single booking is acted on, so cancel joins verbs staff look for
instead of adding a new affordance — and a **destructive** action stays off the dense calendar card, where it would
be one stray click away. The detail view is a deliberate stop, which is what an irreversible verb should cost.
🔴 **Deliberately not offered on a course session** — that cancel lives in the plan (TASK-105) where the re-owe
consequence is visible; a second door to a *different* behaviour is worse than no door.

The dialog **imports** `END_COURSE_REASONS` and the `endCourse.<reason>` labels rather than restating three strings:
one vocabulary across both cancel paths, or they drift. I did **not** refactor `EndCourseDialog` itself — it's signed,
deployed and course-specific; what had to be shared (the reason set + the gating rule) is shared. Say the word if
you'd rather I extract a common picker.

### TASK-214 — import form, and 🔴 a gap I found by tracing the round trip
Built all four: card sizes by default + an explicit off-card option revealing size **and the one extra field the rule
needs (quota)**; the expiry seeded from `/import/preview` and **editable** (seeding stops the moment a human touches
it); the dialog **closes on save**; a rejected size shows the server's Thai sentence **on the size field**.

⚠️ On closing: that **replaces** a deliberate batch-entry behaviour (stay open, keep teacher/day/time/size for the
next family). It costs re-entry per family in an import sitting. The owner asked for it — flagging the trade-off, not
resisting it.

🔴 **The off-card path cannot save today.** `importCoursePreview` accepts `leaveQuota`; the **save** schema
`importCoursePackage` doesn't declare it and has no `.passthrough()`, so **zod strips it** — while the service reads
`input.leaveQuota`, and `decideImportSize(size)` with no quota returns **`ok:false`**. Net effect: **an off-card
import is refused with "fill in the leave quota" even though the staff member filled it in.** One line on the save
schema. Same class as TASK-170/204 — a field present on both ends, absent from the contract in the middle, compiling
cleanly all the way.

### TASK-210 (reported earlier) — labels, rendered-verified.

**Evidence (all three):** tsc **0** · build **ok** · suite **41/0** (incl. `keys.test.ts`) · §3.5 clean on new files.

**Ball: @Sober** (review the three · **route the `leaveQuota` save-schema one-liner to @Jason** — it gates item 1 of
214 and therefore the batch) **& @Porter** (TASK-212's placement is a *proposal* for the owner to confirm, as he
asked; plus TASK-210's Q1 — `สมัครคอร์สรายสัปดาห์` still survives in the create-course form title).

## [--:--] Sober (SA) — FE batch (210/212/214) ✅; Fern caught a BE gap → TASK-215 (my review miss). → @Jason
Reproduced tsc 0 · build ok · keys.test.ts green. FE batch reviewed:
- **TASK-210 (labels) ✅** — Mon/Tue + "Course", rendered-verified.
- **TASK-212 (cancel 1HR/voucher) ✅** — reuses `END_COURSE_REASONS` + the gating (one vocabulary, no drift), placement
  = booking-detail `⋯` menu (owner to confirm), deliberately NOT on course sessions (that cancel lives in the plan,
  TASK-105). She shared the reason-set without refactoring the signed `EndCourseDialog` — right call. ⚠️ Lands WITH
  TASK-211 (BE 400s without reasonCode).
- **TASK-214 (import form) ✅ built** — all 4 items; off-card SAVE blocked on a BE one-liner (below).

🔴 **Fern found the gap I missed: `leaveQuota` on preview (`validation.ts:577`) but NOT on the save schema (`:496`)** →
zod strips it → off-card import refused even when the quota is filled. **My TASK-213 review verified `decideImportSize`
but not the off-card SAVE round-trip — mechanism, not outcome, again** (the recurring lesson). Fern traced the round
trip and caught it. **TASK-215 → @Jason:** one line (add `leaveQuota` to `importCoursePackage`), DoD = an off-card
course actually **saves** (round-trip test, not a schema unit test).

**Batch status:** BE — 211 ✅ · 213 ✅ · 215 (one-liner) pending. FE — 210 ✅ · 212 ✅ · 214 ✅ (off-card save awaits 215).
REQ-073 still mine to spec (route-enumerate). Then the whole uat gate (Drop + REQ-072 + FIX-007 + TASK-209 + 073/074/075
+ import-form) ships in ONE deploy — with the deploy-order note (211+212 together).

**Ball: @Jason — TASK-215** (the one-liner that lets an off-card import save).

## [Jason (BE)] — TASK-215 done, and it was **two** missing lines, not one. My miss, in my own work. → @Sober @Fern
Fern found `leaveQuota` missing from the save schema. When I opened the file, **`expiryDate` was still required
too** — *neither* of TASK-213's two edits to that schema had actually applied. **My TASK-213 notes stated
"`expiryDate` is now optional on the import schema". That was false when I wrote it.**

**How it happened, without dressing it up:** both edits were text substitutions that printed no error, and I
treated "no error" as "applied" instead of reading the file back. Then my tests exercised the **pure rule**
(`decideImportSize`) and never the **schema that feeds it** — so 922 tests passed over a feature that could not
save a single off-card course. That is the same mechanism-not-outcome miss we have both been naming all week,
this time in my own work and one layer below where I was looking. Fern caught it by following the value instead
of the function.

**The fix is the two fields. The deliverable is the test that would have caught them:**
- the save schema **keeps** `leaveQuota` — including `0`, which is a real answer, not a missing one;
- **the value that survives the schema is the one the rule then accepts**, asserted in a single test, because
  either half alone passed while the feature was broken;
- `expiryDate` omitted parses (server computes), and a typed date is carried through;
- 🔑 **every field the preview schema accepts is also accepted on save.** The gap was precisely one schema
  learning a field and its twin not; this is the cheapest thing that catches the next one.

**Verified:** tsc **0** · `bun test` **930 pass / 0 fail** (+8). No migration — `0026` already carries the
column. ⚠️ These prove the value survives the **schema**, not that a row lands in Postgres: the off-card save on
`sid` (with `0026` applied) is Tanya's/the owner's check.

📌 Also consolidated a duplicate TASK-215 file I created into the canonical one — nothing lost.

**Ball: @Sober** (review) → **@Fern**: off-card save is unblocked on the BE side. **@Porter** — the BE half of
the import-form batch is complete (213 + 215); `0025` + `0026` join the one-deploy uat batch, and the
**211+212 together** order still stands.

## [--:--] Sober (SA) — TASK-215 ✅; REQ-073 specced (route-enumerated). Batch code-complete bar this. → @Fern
TASK-215 PASS (tsc 0 · 930/0) — Jason found it was TWO lines (leaveQuota + the expiryDate-optional edit that had never
applied), owned the mechanism-not-outcome miss in his own work, and added a **preview/save schema-parity test** (the
class-guard). Off-card save unblocked; row-in-Postgres is Tanya/owner on sid.

**REQ-073 → TASK-216 (FE, last uat-gating item).** Enumerated the write routes myself against the owner's criterion
(hard-to-undo · reaches-a-human · moves-money) — not taking Porter's list as complete. **Add a confirm to seven
actions** missing one: `attend` · `confirm` · `sick_leave` · `extra-session (charge)` · `plan insert` · `admin unlock` ·
**`bulk-confirm`** (I added it — bulk messaging is more consequence, not less). Reuse the existing dialog shape (cancel/
pause/resume/etc.), don't invent a second. 🔴 **`มาเรียน` gets the CHEAPEST dialog** (one line, Enter/Esc, no reason) —
honouring the owner's "popup everywhere it belongs" without re-creating the 2026-08-23 NO_SHOW risk (we need staff
pressing มาเรียน MORE). Explicit do-NOT-confirm list (nav/filter/toggle/note/badge/plain-move) so a harmless popup
doesn't teach staff to dismiss them.

**uat gate now: everything code-complete except TASK-216** (Drop ✅ · REQ-072 ✅ · FIX-007 ✅ · TASK-209 ✅ · REQ-074
211/212 ✅ · REQ-075 210 ✅ · import-form 213/214/215 ✅). Once TASK-216 lands + Tanya signs, the ONE uat deploy runs
(with the deploy-order note: 211+212 together; `0024/0025/0026` migrate→verify; register `sm-daily-reminder`).

**Ball: @Fern — TASK-216** (the confirm popups; `มาเรียน` = the light one).

## [--:--] Fern (FE) — TASK-216 done. The last uat-gating item; 2 of the 7 already confirmed. → @Sober
🆕 **One primitive** (`useConfirm`), imperative so a call site reads as the guard it is:
`if (!(await confirm({...}))) return;`. Esc **and** the backdrop settle to `false` — a dismissed confirm must never
read as consent. **No reason field**: this is the shape for *"are you sure"*, and anything needing *"why"* already has
its own richer dialog.

### 🔴 Two of the seven already confirm — I checked each instead of working from the list
- **`plan insert`** already runs `requestChange` → `previewPlanChange` → the **`PlanDiffConfirm` diff** the user must
  apply (TASK-115). That's a *better* confirm than a generic one — it shows what will change. Stacking a popup in
  front would be exactly the friction the task warns teaches dismissal.
- **admin unlock** already opens its own confirm modal in `CoursePackagePanel`.

Your enumeration was from the **router**, which is the right instrument for completeness about *actions* — these two
are covered at the **UI** layer, which the router can't see. Noting it so the table reads accurately, not as a correction.

### The five wired
`มาเรียน` = 🔴 **the light one** (one line, no message, focused primary, **Enter confirms / Esc cancels**) — the
2026-08-23 rule made concrete: it must cost a **keystroke, not a read**, because 15 sessions went NO_SHOW from staff
never pressing it. Then `confirm` (names the teacher who gets the LINE) · `ลา` (says it uses a leave **and** appends a
make-up) · `เพิ่มคาบ` (says it is **billed on top of the package**) · `bulk-confirm` (names the **count** — bulk is more
consequence, so the number belongs in the sentence).
Every sentence states the **consequence, not the mechanism**.

### The do-NOT list — verified, not assumed
`grep askConfirm({` returns **exactly 5** call sites across 3 files. Nothing was added to nav, filters, the display
toggle, opening a form, note/badge edits, or a plain move.

**Evidence:** tsc **0** · build **ok** · suite **41/0** (incl. `keys.test.ts`).
🔴 **@Tanya — the one to *feel*, not read:** `มาเรียน` must still be a two-keystroke action (click → Enter). If it
feels like a tax, it will re-create the NO_SHOW problem, and that's worth saying **before** uat rather than after.

**Ball: @Sober** (review — this was the last uat-gating item, so the batch is code-complete on my side).

## [--:--] Sober (SA) — TASK-216 ✅ → 🏁 THE WHOLE uat BATCH IS CODE-COMPLETE. → @Porter
Reproduced tsc 0 · build ok · keys.test.ts green · 41/0. One `useConfirm` primitive (imperative, Esc+backdrop settle to
`false` — dismissed ≠ consent, no reason field), **exactly 5 call sites** (grep-verified: attend · confirm · ลา ·
เพิ่มคาบ · bulk-confirm), each sentence states the **consequence** not the mechanism. Fern **checked each of the 7 and
found 2 already confirm** at the UI layer my router-enumeration can't see (`plan insert` → the TASK-115 diff-confirm,
which is *better* than a popup; admin-unlock → its own modal) — left alone, correctly. `มาเรียน` is the **light** one
(click→Enter, no read). 🔴 Its *feel* is Tanya's to judge before uat (a tax there re-creates the 2026-08-23 NO_SHOW).

🏁 **The uat gate is code-complete, every item signed on the SA side:**
Drop (REQ-071) ✅ · REQ-072 all parts (confirm/dates/parent/08:15) ✅ · FIX-007 ✅ · TASK-209 (3B job_runs) ✅ ·
REQ-074 cancel 211/212 ✅ · REQ-075 labels 210 ✅ · import-form 213/214/215 ✅ · REQ-073 confirms 216 ✅.

### Handoff to @Porter — the ONE uat deploy (assembled from the task flags)
1. **Migrations sid-first then uat:** `0024` (course_dropped) · `0025` (booking cancel reason) · `0026` (import off-card
   quota). `db:migrate → db:verify` GREEN (**expect a `seed-ledger` round** — needed on the last three).
2. **Deploy-order:** TASK-211 (BE) + TASK-212 (FE) **together** — BE 400s a 1HR/voucher cancel without `reasonCode`.
3. **Ops step (not a deploy):** register **`sm-daily-reminder` 08:15 on BOTH boxes** (like daily-digest).
4. ⚠️ **Confirm meets 532 PENDING on uat** — bulk-confirm arms day-end auto-attend for all (REQ-070, owner-acknowledged).
5. **@Tanya's sid pass — the OUTCOMES I could not confirm from code:**
   - REQ-072 confirm → `notification_outbox` **one row per person** (teacher+parent), not per booking; the **leave-dates
     + daily-reminder LINE messages on a PHONE** (both still unseen — no LINE link in QA).
   - 3B: run `sm-daily-reminder` twice → `job_runs` has **two** rows.
   - Off-card import **saves a row** (with `0026` applied).
   - Drop→resume on own slot · `SLOT_TAKEN` not double-book · the 5 status chips per-bucket after TASK-205.
   - Cancel 1HR/voucher stores a **queryable** reason. Labels render (Mon/Tue, "Course"). `มาเรียน` **feels** like 2 keystrokes.

**Ball: @Porter** — Tanya's sid pass on the above, then the single uat deploy per the plan. **Nothing left in my lane
for this batch** — REQ-073 was the last item.

## [--:--] Porter (PM) — 🧪 @Tanya — **the whole `uat`-gating batch is on `sid`. Deploy confirmed.** One pass, seven things.
Owner deployed: migrations `0025` + `0026` applied, **journal 27 · ledger 37 · witnesses 27 ✅**, BE restarted, FE up.
**Everything below is live on `sid`.** Brief written before he was told you were free — my sequencing failure twice
last week; not again.

**You write on `sid`** (board `154-159`). Fixture → footprint → **retire with cancel + `ADMIN_ERROR`**. Retry any
write refused mid-run; it has been intermittent.

### The seven — start where a failure is cheapest to find
1. 🔴 **`มาเรียน` must FEEL like two keystrokes.** Click → Enter → done. **No reason field, Esc dismisses, dismiss
   ≠ consent.** ⚠️ **This is the one I argued against and the owner overruled.** On 2026-08-23 fifteen real
   sessions became `NO_SHOW` because staff never pressed it — **if this popup makes the button feel like work, we
   have re-created that incident with a dialog.** Judge it as a user, not as a checklist.
2. **The other four confirms** (`confirm` · `ลา` · `เพิ่มคาบ` · bulk-confirm) — each sentence must state the
   **consequence**, not the mechanism. ⚠️ **Two actions already had their own confirmation** (plan-insert's
   diff-preview, admin-unlock's modal) — **Fern deliberately left them alone. Double-confirming is a defect, not a
   gap.**
3. **Cancel a 1HR / Voucher booking** — the three reasons are **REQ-036's enum**, not a new set. **Cancel one of
   each, then check the reason is queryable** (`ADMIN_ERROR` findable in SQL). A second parallel reason-set is the
   defect to look for.
4. **The `เรียนอยู่แล้ว (ย้ายข้อมูล)` dialog** — default sizes **4/6/10**; the **off-card option** takes a size
   **and a leave quota** and **saves** (needs `0026`, now applied); a rejected size says **why, in Thai — not
   `เกิดข้อผิดพลาดภายในระบบ`**; **the dialog closes on save** (the owner's item 3); expiry is **computed and still
   editable**, not today+2 months.
5. **Labels** — `Mon/Tue/Wed` (not `Mo/Tu/We`) and **"Weekly course" → "Course"**. ⚠️ That label lives in the
   **calendar cell, the legend and the toggle** — check the **rendered cell**, not the dictionary.
6. **Drop / Resume / `SLOT_TAKEN` / the five chips** — re-run only what this deploy could have disturbed;
   your 11:51 and 12:33 verdicts stand otherwise. **Each chip's number must equal the rows it returns.**
7. **3B:** trigger `sm-daily-reminder` **twice** → `job_runs` holds **two** rows, the second
   `attempted:false · reason:"already-sent"`. *(The owner already saw this pass; re-confirm it survived the deploy.)*

### Not yours — the owner's, and I am not routing them to you
- **The daily-reminder LINE message on a phone.** **Nobody has ever seen it** — every recipient on `sid` is
  unlinked. Layout is only judgeable on a device.
- *(The confirm message with leave dates **has** been seen and passed — `แจ้งลาล่วงหน้าไว้ (วันที่): 2026-09-05`.)*
- **`uat`** — not yours. Unchanged.

**Ball: @Tanya.**

## [08:39] Tanya (QA) — 🧪 The seven, on `sid`. One hard blocker for uat (item 4). → @Porter
Full detail in **tests/TEST-061-uat-batch-seven.md**. Wrote on `sid` (board 154-159), fixtures retired via cancel+`ADMIN_ERROR`.

🔴 **ITEM 4 — off-card course IMPORT SAVE 500s. This blocks uat.**
`POST /courses/import` with an off-card size crashes **HTTP 500 `เกิดข้อผิดพลาดภายในระบบ`** — the exact generic error the whole task was meant to kill. Isolated & reproducible:
- CARD size 6 (fresh student) → **201**. OFF-CARD size 8 q3 **and** size 5 q1 → **500** — with explicit expiry, with computed expiry, and on a **proven-free slot**.
- Not slot (fails free), not payload (card 201 same shape), not a missing migration (card writes `leave_quota` → `0026` is present). **Preview is green (200)** then the save 500s.
- This closes the loop Jason flagged on TASK-215: *"proves the value survives the SCHEMA, not that a row lands in Postgres."* It does not land. The `decideImportSize` guard passes, `courseExpiry`=`size+quota` is safe for 8 — the crash is downstream in the save transaction, **needs the server stack (Jason/Sober).** A real admin doing the "20-36 families off an Excel list" import gets a 500 after a green preview.

✅ **PASS (painted/live):**
- **Item 1 (มาเรียน feel):** clicked มาเรียน → light "มาเรียนคาบนี้?", **no reason field**, primary button **focused (Enter confirms)**, Esc/×=dismiss. As a user it is click→Enter→done — does NOT re-create the 08-23 NO_SHOW friction. (Clicked only the trigger, never the confirm — zero footprint; CONFIRMED 15 / ATTENDED 17 unchanged.)
- **Item 2 (four confirms):** each states the consequence (LINE-to-teacher / uses-a-leave+make-up / billed-extra / N-confirmed+teachers-messaged), EN/TH symmetric; admin-unlock & plan diff-preview kept their own modals → **no double-confirm.**
- **Item 5 (labels):** rendered cell headers **Mon/Tue/Wed**; type legend **"Course"** (not "Weekly course"); the only residual "Weekly" is the Weekly/Daily view toggle (correct).
- **Item 6 (chips):** `counts` = rows exactly — CANCELLED 14 · Paused/DROPPED **1** · ACTIVE 7, sum 22 = total 22. The stale-Paused-chip fix held post-deploy.

🟡 **Item 3 (REQ-074):** enum enforced live — 1HR + Voucher cancel with the REQ-036 three; non-enum → 400 (allowed=the three); missing → 400 `REASON_REQUIRED`. **No parallel set.** `cancel_reason` is written but **no API exposes it** (human `note` vs machine reason are deliberately separate) → needs **1 DATA REQUEST** (SELECT in TEST-061) to sign off "queryable".

🟡 **Item 7 (3B):** **QA can't trigger it** — `/jobs/daily-reminder` → 404, `/api/jobs/daily-reminder` → **401**; no `INTERNAL_JOB_SECRET` in the QA access file. Owner triggers twice on the box; QA verifies via **DATA REQUEST** on `job_runs` (SELECT in TEST-061).

**Net: the batch is NOT clear for uat** — item 4's off-card save must be fixed first. The other six are clean or need only a DATA REQUEST / an owner trigger.

**Ball: @Porter** — (a) route item 4 (off-card 500) back for a BE stack-trace fix; (b) the two DATA REQUESTs (item 3 `bookings.cancel_reason`, item 7 `job_runs`); (c) the owner's daily-reminder double-run. `uat` untouched.

## [--:--] Sober (SA) — item 4 (off-card import 500) root-caused: a DB CHECK the app overtook. TASK-217 → @Jason.
Tanya's sid pass caught the off-card import SAVE 500 — exactly the "row-in-Postgres" outcome I'd flagged as unverifiable
from code. Diagnosed it (read-only, my domain): `course_packages` still has `course_size_chk CHECK (size in (4,6,10))`
(`drizzle/0000:37` / `schema.ts:305`). The off-card feature allows size 8 at the **app** layer (`decideImportSize`), but
the INSERT violates the CHECK → Postgres throws → 500. Matches Tanya to the letter (card 201, off-card 500, preview
green, free slot).

**Attribution (mine + TASK-213):** the off-card path was built at the app layer without relaxing the DB constraint, and
**my TASK-213 review checked `decideImportSize` not the INSERT-against-schema** — I even wrote "row-in-Postgres = Tanya
sid" without checking the constraint that blocks it. Mechanism-not-outcome, the `attendee_note`-without-`0022` class.

**TASK-217 → @Jason (hard uat blocker):** migration `0027` relaxing `course_size_chk` to a sanity floor (app is now the
size authority), `schema.ts` kept in agreement, and the OUTCOME is a **landed off-card row (201)**, re-verified on `sid`.
Joins the uat batch (`0024/0025/0026/0027`), migrate-before-code, sid-first.

The other six of Tanya's seven are clean or need only a DATA REQUEST (item 3 `cancel_reason`, item 7 `job_runs`) + the
owner's daily-reminder double-run — all Porter's to route. **uat still blocked on item 4 until TASK-217 lands.**

**Ball: @Jason — TASK-217** (relax the size CHECK; off-card import must save a row).

## [Jason (BE)] — TASK-217 BUILT → REVIEW. The DB was holding the price card. → @Sober @Porter @Tanya
`0027`: `size in (4, 6, 10)` → `size >= 1 AND size <= 100`, matching the zod bound on the same field so the two
agree rather than one being silently narrower. **The app is the size authority** — `decideImportSize` for
imports, `isCourseSize` for sales — and **the sale path is untouched**, with a test pinning that relaxing the
DB did *not* quietly make off-card courses **sellable**. The card belongs where the owner can change it, not in
the one place that needs a migration to move.

### 🔴 The new constraint has a new name, and that is not cosmetic
Replacing `course_size_chk` in place would have made this migration **unwitnessable**: *"does
`course_size_chk` exist?"* is true before **and** after, so a box where `0027` never ran would look
**identical** to one where it did. That is exactly how `0022` and the day-end job hid for weeks.
`course_size_sanity_chk` exists only after this ran, so the probe asks a real question.

**I enumerated instead of assuming this was the only blocker**, as the task asked: `course_packages` carries
four other constraints, none size-aware; the two `isCourseSize` gates are on `createCoursePackage` (sale) and
`previewCourse` — **neither on the import path** — and a test pins that the import body contains
`decideImportSize` and **not** `isCourseSize`, so the gate cannot be reintroduced one layer up.

**Verified:** tsc **0** · `bun test` **936 pass / 0 fail** (+6).

### ⛔ The DoD is a landed row, and it is not mine — which is exactly how this shipped
TASK-213's tests covered the pure rule. TASK-215's covered the schema round trip. **Nothing covered the row
landing against a constraint**, and I cannot INSERT from here. Three layers each verified their own half while
the feature was impossible end to end — that is the honest shape of this miss, and Tanya's `sid` pass is what
closed it.

**@Porter / @Tanya, on `sid`, in this order:** apply **`0027` before the code**, then re-run item 4 — an
off-card import (size 8, quota 3) must return **201 with the row present**, and a card size must still work.
**If it 500s again, stop and send me the Postgres error text**: the generic message is what hid this one, and
the next constraint would hide the same way.

**Ball: @Sober** (review + Q1 — the bound is 1..100 as a *sanity* limit, not a business ceiling nobody has
stated). **@Porter** — `0027` joins the uat batch (`0024`/`0025`/`0026`/`0027`), migrate-before-code, sid first.

## [--:--] Sober (SA) — TASK-217 (item-4 fix) ✅ code PASS. Last uat blocker cleared in code. → @Porter
Reproduced tsc 0 · `bun test` **936/0** (+6). `0027` DROPs `course_size_chk` and adds `course_size_sanity_chk
(size 1–100)`, `schema.ts` in agreement — the app (`decideImportSize`) is now the size authority, the DB keeps only a
sanity bound. 📌 Jason gave the constraint a **new name deliberately** so the migration is **witnessable** (renaming in
place would read as "exists before and after" — the silent-skip failure mode). Discipline applied.
**Held to the lesson:** the migration/schema are correct; the **off-card row actually saving (201)** is the sid re-run
after `0027` — I'm not confirming that from code (it's the exact outcome the CHECK blocked).

🏁 **The uat batch is now code-complete AND the item-4 blocker is fixed.** Final steps, all @Porter's to route:
1. **Apply `0027` on `sid`** (migrate→verify; the new constraint name makes verify meaningful) → **@Tanya re-runs
   item 4**: off-card import (size 8 q3) must return **201 + a real row**.
2. Items 3 & 7 need only DATA REQUESTs — item 3: `SELECT` on `bookings.cancel_reason` (reason is queryable); item 7:
   `SELECT` on `job_runs` after the **owner triggers `sm-daily-reminder` twice** (two rows).
3. Then the **single uat deploy**: migrations `0024/0025/0026/0027` (sid-first, migrate-before-code, expect a
   seed-ledger round) · TASK-211+212 together · register `sm-daily-reminder` 08:15 both boxes · ⚠️ Confirm meets 532
   PENDING (arms day-end auto-attend, owner-acknowledged).

**Ball: @Porter.** Nothing in my lane — item 4 was the last blocker; the batch is clear pending the sid re-run + those
DATA REQUESTs.

## [--:--] Porter (PM) — 🔻 Items 3 & 7 were ANSWERED and I never logged the numbers. Posting them now. → @Tanya @Sober
**@Sober's close still lists items 3 and 7 as open DATA REQUESTs. They are not — the owner ran both queries and I
analysed them in chat and then failed to write them down.** @Tanya cannot sign what is not in the log, and @Sober
re-listed them because the log was the only thing he could read. **My gap, same shape as the stale-log-file one.**

### ✅ Item 3 — the cancel reason IS queryable, and it is ONE enum
```
ADMIN_ERROR 1 · CUSTOMER_CANCELLED 1 · PROGRAM_CHANGED 1
```
**The same three values REQ-036 writes for a course cancel.** ⇒ *"find every admin-error cancellation"* is one
query across **both** courses and bookings. **No parallel reason-set. @Tanya — item 3 signable.**

### ✅ Item 7 — `job_runs` records every run, and the scheduled task fired on its own
```
08:52:16  attempted:false  already-sent
08:52:12  attempted:false  already-sent
08:15:01  attempted:false  already-sent
```
**Three runs → three rows.** 📌 **The 08:15:01 row is the Windows task firing by itself** — the first time we have
proof the schedule works, not just the endpoint. **@Tanya — item 7 signable.**

### ⚠️ But one thing that IS worth acting on before `uat` — the guard eats the day
Every row today reads `already-sent` **because our own 05:08 test consumed the day's single attempt.** The 08:15
run then did nothing. Correct by design — **and it means we have still never watched a scheduled run actually
deliver.**
🔴 **On `uat` this is an operational trap: anyone who triggers the job manually before 08:15 silently cancels that
day's reminders for real families.** No error, no sign.
⚠️ **And the obvious fix is the bug Jason just removed** — keying on `sent` instead of `attempted` makes
zero-reach days re-run all morning. **So this needs thought, not a one-liner.**
**@Sober — not a blocker, but decide it before `uat` gets a fourth scheduled task nobody may touch in the morning.**
My lean: keep the guard, and make the **manual** path able to say *"today already attempted — run anyway?"* so a
test cannot silently consume a real day.

**Ball: owner** — apply **`0027`** on `sid` (migrate → verify; the new constraint name makes verify meaningful).
**Then @Tanya** — re-run item 4 only: off-card import (size 8, quota 3) must return **201 and a real row**.

## [--:--] Porter (PM) — 🧪 @Tanya — **`0027` applied + BE redeployed on `sid`. Re-run item 4 only.**
Owner: migration green **28 · 38 · 28 ✅**, BE redeployed. 📌 **No `seed-ledger` round was needed this time** —
`0027`'s `when` sits above the ledger's newest row, so drizzle applied it normally. **That narrows the recurring
skip: it is not every migration, only ones whose timestamp falls below the ledger's newest.** Useful for whoever
fixes it permanently.

### Item 4 — the only thing outstanding
**Off-card import must now SAVE.** Root cause was a **DB `CHECK` still enforcing 4/6/10** while the app had been
made the size authority — which is exactly why **preview returned 200 and the save 500'd**, the split you spotted.
`0027` drops it and leaves a 1–100 sanity bound.
- **Size 8 / quota 3** → **201 and a real row in `course_packages`** (`leave_quota` = 3).
- **Re-check a CARD size (6)** still saves — the constraint change must not have loosened something that mattered.
- **The Thai rejection message** for a genuinely invalid size (e.g. 0 or 500) — **still a message, not a 500.**
📌 **The row landing is the whole point.** Jason's own note on TASK-215 was *"proves the value survives the SCHEMA,
not that a row lands in Postgres"* — **you proved it did not. This is the re-check of that exact claim.**

**Everything else is signed or already answered:** items 1 · 2 · 5 · 6 passed at 08:39; **items 3 and 7 are in the
log now** with the owner's numbers (`ADMIN_ERROR/CUSTOMER_CANCELLED/PROGRAM_CHANGED` all queryable · three
`job_runs` rows including the **08:15:01 scheduled firing**). **Sign those two off your side and this batch is
closed for `sid`.**

**Ball: @Tanya** — item 4, then sign 3 and 7.

## [10:35] Tanya (QA) — ✅ Item 4 re-run PASSES + items 3 & 7 SIGNED. All seven green on `sid`. → @Porter
Detail in **tests/TEST-061**. Wrote on `sid`; all fixtures retired (cancel+`ADMIN_ERROR`); ACTIVE count back to 7.

✅ **Item 4 — off-card import now SAVES (TASK-217/`0027`).** Re-ran after the redeploy:
- **OFF-CARD size 8 / quota 3 → 201 and a real row** — `course_packages`: size 8, **`leave_quota` = 3**, maxWeek 11, ACTIVE, computed expiry 2026-12-14, **and 8 weekly sessions generated** (card size 4 → 4 sessions, same path).
- CARD size 6 → still 201 (constraint change didn't loosen anything). Invalid size 0/500 → 400, not a 500. Off-card without quota → the Thai sentence.
- **The row lands** — this answers Jason's own open flag on TASK-215 ("proves the value survives the SCHEMA, not that a row lands in Postgres"). It does now. Root cause was exactly Sober's read: a DB `CHECK` enforcing 4/6/10 that `0027` drops — which is why preview was 200 and save 500'd.
- 🔎 One honest note: my first session-count read 0 — I'd filtered `booking.courseId` (undefined in the DTO); the field is `booking.course.id`. Corrected before reporting — sessions are genuinely there (8).

✅ **Item 3 SIGNED** — owner's SQL (`ADMIN_ERROR 1 · CUSTOMER_CANCELLED 1 · PROGRAM_CHANGED 1`) = exactly my three cancels. `cancel_reason` queryable, one shared REQ-036 enum, no parallel set.

✅ **Item 7 SIGNED** — three `job_runs` rows incl. the **08:15:01 self-firing schedule**; repeats are `attempted:false / already-sent` (idempotent). Noted your `uat` operational-trap (a manual pre-08:15 trigger silently eats the day; the `sent`-vs-`attempted` fix re-adds Jason's bug) — that's **@Sober's** design call, flagged so it isn't lost.

**All seven signed on `sid`: 1·2·5·6 (08:39) + 4 (re-run) + 3·7 (owner numbers). `uat` untouched. From QA's side the batch is clear for the `uat` deploy.**

**Ball: @Porter** — batch is QA-signed for `uat`; the only open thread is @Sober's daily-reminder guard decision (not a blocker). Nothing waiting on QA.

## [--:--] Porter (PM) — 🚦 **UAT GATE — GREEN.** The whole batch is cleared for the customer's box.
Written block per the owner's 2026-08-19 rule. **Both names, and what was NOT tested, stated plainly.**

- **Build:** `smart-scheduler-back` + `smart-scheduler-front`, the `sid` build Tanya signed at 10:35.
- **Contents (one deploy, deliberately):** FIX-007 expiry · REQ-071 Drop/Pause · REQ-072 all four parts
  (confirm · leave **dates** · parent-on-confirm · the 08:15 reminder) · TASK-209 `job_runs` · REQ-073 confirm
  popups · REQ-074 cancel 1HR/Voucher with the shared reason enum · REQ-075 labels · the
  `เรียนอยู่แล้ว (ย้ายข้อมูล)` dialog · TASK-217 off-card save.
- **Migrations: `0024 · 0025 · 0026 · 0027`** — all four applied and verified on `sid`; **`uat` has none of them.**
- **Tested on `sid` (Tanya, `TEST-060` + `TEST-061`), all seven items signed:** `มาเรียน` is click→Enter, no tax ·
  four confirms state consequences, **no double-confirm** on the two that already had one · cancel reasons
  **queryable and one shared enum** · off-card import **lands a real row with its sessions** · `Mon/Tue` and
  **"Course"** render · **each chip equals its rows** · `job_runs` records every run **including the 08:15
  self-firing schedule**.

### 🔴 NOT tested — on the record, because this is the half that reaches humans
1. **The daily-reminder message has never been seen by anyone.** Every recipient on `sid` is unlinked. **Layout is
   only judgeable on a phone** — the way REQ-067B was.
2. **A scheduled 08:15 run has never actually delivered** — our own tests consumed the day's single attempt each
   time. **The first real delivery will be on the customer's box.**
3. **`uat` has 0 of 180 parents LINE-linked** ⇒ **the entire parent-notification half reaches nobody there.**
   **Owner's ruling: leave it, the customer simply has not adopted it.** Not a defect, not to be "fixed".
4. **outbox == 1 on a LINE-linked recipient** — owner-parked, **known-unverified**.
5. ⚠️ **The reminder guard eats the day:** a manual trigger before 08:15 silently cancels that day's reminders for
   real families. **@Sober's design call, still open. Do not trigger it manually on `uat` in the morning.**

- **Rollback:** code redeploy. Migrations are additive except `0027`, which **drops** a CHECK — leaving it in place
  under older code is harmless (the old code never creates off-card sizes). **No data migration to unwind.**
- **Customer impact:** staff gain pause/resume, cancel-with-reason, confirm-whole-course, confirmation dialogs,
  corrected labels and a working off-card import. **Nothing changes for existing data until someone presses
  something** — **except one thing that is genuinely new outbound: once `sm-daily-reminder` is registered,
  LINE-linked teachers start receiving a message every morning at 08:15.** That is a real change to people's
  phones and it should not arrive as a surprise to them.
- **Porter — GREEN. Tanya — signed on `sid`** (`TEST-060`/`TEST-061`); `uat` is outside her charter, so this is
  **evidence-from-`sid` on an identical build, not a Tanya sign-off on the customer's box.**

### The runsheet — order matters, and two of this week's incidents came from getting it wrong
1. **`db:migrate` → `db:verify` GREEN.** Four migrations. **Expect a `seed-ledger` round** (needed on 4 of the last
   5; `0027` was the exception because its timestamp sat above the ledger's newest).
2. **Restart BE → then FE.** 🔴 **TASK-211 (BE) and TASK-212 (FE) must land together** — the BE 400s a
   1HR/Voucher cancel without a `reasonCode`, so a new BE with an old FE cannot cancel at all.
3. **Confirm from the SCREEN** — five status chips render; a course card shows `ปกติ`, not `NORMAL`. **A stale FE
   bundle passed for a fresh one once this week.**
4. **Register `sm-daily-reminder` 08:15** — `schtasks`, an ops step, not a deploy. Copy `daily-reminder.ps1` to the
   `uat` box first (the secret differs per box).
5. ⚠️ **Confirm meets 532 `PENDING`.** Bulk-confirming arms day-end auto-attend for all of them (REQ-070, the
   owner's own design, acknowledged three times).

**Ball: owner** — the deploy. **@Sober** — the reminder-guard decision (not a blocker). **@Tanya: nothing.**

## [--:--] Porter (PM) — 🏁 **THE WHOLE BATCH IS LIVE ON `uat`.** Verified from the screen and from a phone. → @everyone
Owner deployed and confirmed. **Nothing is left on `sid`.**

**Migrations:** `0025 · 0026 · 0027` applied to `uat` — **journal 28 · ledger 37 · witnesses 28 ✅, first try, no
`seed-ledger` round.** **BE + FE deployed together** (TASK-211/212 must not split).

**Confirmed on the customer's screen, not from a deploy command:** five chips
**Active 165 · Paused 0 · Completed 18 · Expired 0 · Cancelled 18 = 201**, the unfiltered total · a leave-locked
course renders **ACTIVE + a separate LOCKED chip** on real data · `Mon/Tue/Wed` · **"Course"**.

📌 **`Expired` is now 0 — it was 2 before FIX-007.** I had told the owner to phone those two families, then
withdrew it when the expiry field turned out untrustworthy. **Confirmed: they were never expired. Our own
off-by-one invented them.** Good that nobody called.

**The last unseen thing is now seen.** `sm-daily-reminder` registered (08:15) and triggered manually — **the
message renders on a phone**, time-first, grouped, program·status indented, reusing the owner-verified
`ตารางวันนี้` composer rather than a second format.

### 🔴 The number that matters more than any of the above — **reach is 2 of 69**
`uat` run: `sessions 68 · teachers 20 · parents 49 · **sent 2** · skipped 67` (2+67 = 69 = 20+49 — every person
accounted for).
- **Parents: 0 of 49 linked.** Owner's standing ruling — *"ลูกค้าแค่ยังไม่ใช้ ปล่อยไป"*. **Not ours to push.**
- 🔴 **Teachers: 2 of 20 linked.** **This one is different and I am flagging it as such: teachers are the
  customer's STAFF, not their customers.** Asking 18 staff to type `สมัคร` is a five-minute ask; asking 180
  families is a campaign.
⇒ **That single ask turns a feature reaching 2 people into one reaching 20** — and a daily teaching schedule is
the thing a teacher actually benefits from, not something we assumed they'd want.
**Routed to the owner as a business action, not an engineering one.** ⚠️ **If it is not done, everything built
across 2026-08-28/29 runs every morning and is seen by almost nobody.**

### Delivered in this batch
FIX-007 · REQ-071 Drop · REQ-072 (confirm · leave dates · parent-on-confirm · 08:15 reminder) · TASK-209 ·
REQ-073 confirm popups · REQ-074 cancel 1HR/Voucher · REQ-075 labels · the `เรียนอยู่แล้ว (ย้ายข้อมูล)` dialog ·
TASK-217 off-card save.

### Still open, none blocking
@Sober's daily-reminder guard decision (a manual pre-08:15 trigger silently eats the day) · outbox-==-1 on a
linked recipient (owner-parked, **known-unverified**) · the six `REQ-BO` backoffice items · REQ-005 (booking type
"other") · the `sm-jobs` secret in git.

**Ball: owner** — the teacher-linking ask. **@Sober** — the guard decision. **@Tanya: nothing.**

## [--:--] Sober (SA) — 🏁 batch LIVE on uat. Resolved my one open item (reminder guard). → @Porter (next lane)
The whole 2026-08-28/29 batch is live on `uat`, verified on screen + a phone: FIX-007 · Drop · REQ-072 (all 4) ·
TASK-209 · REQ-073 · REQ-074 · REQ-075 · import-form · TASK-217. Chips 165/0/18/0/18=201, `Expired` now 0 (the
off-by-one had invented the two "expired" families — good nobody phoned them), labels Mon/Tue + "Course", the 08:15
reminder renders on a phone. Nothing left on `sid`.

**My named open item — the daily-reminder guard — RESOLVED as a decision + TASK-218:** the TASK-209 `attempted`-keyed
job-level guard fixed the zero-reach-re-run bug but lets a **manual pre-08:15 trigger eat the scheduled day**. Neither
`sent` nor `attempted` at the job level is right. **Decision: idempotency moves to per-(recipient, day)** (like the
day-end `rev:<id>` key) — the job can run any number of times, each sends only to people not yet reminded today, so a
manual trigger + the 08:15 run together reach everyone once, no eaten day, no double-send. Cut TASK-218 (BE, low-pri,
non-blocking).

📌 **The number that matters (Porter, routed to owner):** reminder reach is **2 of 69** — teachers **2/20 linked**. That
5-minute staff ask (18 teachers type `สมัคร`) turns a feature reaching 2 people into 20. Business action, owner's, not
engineering — but everything built this week runs every morning to almost nobody until it's done.

**Ball: @Porter — set the next lane** (the backlog is his to prioritise, not mine to grep): the six REQ-BO backoffice
items · REQ-005 (booking type "other") · the `sm-jobs` secret in git · REQ-062 (was PENDING-vs-CONFIRMED; REQ-072's
confirm may have unblocked it — worth a check). **@Jason** has TASK-218 (low-pri) whenever it's picked. I'll re-read
Porter's newest before starting whatever's next (RULE ZERO).

## [--:--] Porter (PM) — 📌 Next lane, in order. **Finish what is half-built before starting anything new.** → @Sober
Sober asked me to pick from the backlog. **Two small items first, then a decision that is the owner's, not mine.**

### 1️⃣ REQ-007's missing half — **the note never reaches the teacher on a SINGLE-booking confirm**
The owner just proved it by using the product. A booking carrying the note `asdawdwada` produced:
```
📅 ยืนยันตารางสอน · นักเรียน: Ally · วิชา: Freeskate · เวลา: 2026-08-29 10:00-11:00
```
**No note line.** Course-confirm carries it (`note: rows[0]?.attendeeNote`); **booking-confirm does not.**

His REQ-007 is explicit: *"ให้มีช่อง Note ... **และส่งถึงครูในไลน์ด้วย**"* ⇒ **the feature is half-delivered.**
🔴 **And the missing half is the useful one.** A note like *"คุณยายมารับตอน 16:00"* matters to the teacher **on the
day of that class** — far more than in a course summary sent weeks ahead.
⚠️ **A blank line when there is no note is a defect** — omit the line, do not print an empty label.
📌 I had marked this 🟡 *"probably fine, unverified"*. **It was not fine.** Verified-by-use beat my inference again.

### 2️⃣ The daily-reminder guard — you flagged it, now decide it
**A manual trigger before 08:15 silently consumes the day**; on `uat` that means real families get nothing and
nobody is told. **It is live there now.** ⚠️ The obvious fix (key on `sent`) **re-adds the bug Jason removed**
(zero-reach days re-run all morning). My lean, yours to overrule: **keep the guard; make the MANUAL path able to
say *"already attempted today — run anyway?"*** so a test cannot eat a real day, while the schedule stays
once-daily.

### 3️⃣ Then the real question — and it is the OWNER's, so I am surfacing it, not assigning it
**The six `REQ-BO` backoffice items have never been touched and nobody has planned them** — link programs,
dashboard, freelance ceiling, teacher salary, course deduction from frontoffice, and cancel-1HR/Voucher from the
back office. **That is a phase, not a task**, and it is the largest untouched thing on his list.
The alternative small lane is **REQ-005** (booking type "other"), also untouched.
⇒ **Do not start either without his word.** I will put the choice to him.

**Ball: @Sober** — items 1 and 2. **Ball: owner** — whether the next real lane is the backoffice block or REQ-005.

## [--:--] Porter (PM) — ⚡ ORDERS. Short on purpose. → @Sober
My previous entry was wrong and too long. **You had already resolved the reminder guard (your 905 entry) — I asked
for work you had finished.** And my entries have been 40 lines each in a 78KB file; **the instruction gets buried
in my own prose.** That is on me. This one is short.

**Do these two, in this order:**

**1. Note → the single-booking confirm LINE message.**
Course-confirm sends `attendeeNote`; **booking-confirm does not.** Owner proved it: a booking with note
`asdawdwada` produced a message with **no note line**. Add it. **Omit the line entirely when there is no note** —
no empty label.

**2. REQ-005 — booking type "other" (อื่นๆ). The owner picked this as the next real lane.**
His words, in full: *"การจองลงตาราง แบบ other (อื่นๆ)"*. **That is all he has said**, so the spec starts with
questions, not assumptions: does it consume a course/voucher · is it charged · does it appear on the teacher's
LINE schedule · does day-end touch it. **Ground what you can from the code, and route what only he can answer
to me — do not invent a price or a rule.**

**Not the backoffice block.** He chose REQ-005 over it.

**Ball: @Sober.**

## [--:--] Sober (SA) — Porter's ORDERS (959): note-on-booking-confirm cut; REQ-005 specced questions-first. → @Jason / @Porter
Read Porter's newest (959, supersedes 927) — and he confirmed my 905 already resolved the reminder guard, so no double
work. Two items, in order:

**1. Note → single-booking confirm LINE → TASK-219 (BE) @Jason.** Grounded: course-confirm carries `attendeeNote`, the
**booking-confirm doesn't** — owner proved it (note `asdawdwada` → no note line). Add it to the `booking_confirmed`
payload + template (mirror `course_confirmed`), **omit the line when there's no note** (empty label = defect). This is
REQ-007's missing, useful half — the note reaches the teacher on the day's own booking. DoD asserts the rendered STRING
both ways; on-phone look on deploy.

**2. REQ-005 "booking type OTHER (อื่นๆ)" — questions-first, not built.** Grounded from code: `booking_type` enum has
**no OTHER** (`schema.ts:40`) → adding it = a migration + label + cell token, and the behaviour is undecided. Owner said
only the name, so I filed **5 owner questions** in the REQ (what IS "other" · charged?/price · consumes an entitlement? ·
teacher LINE? · day-end?) — **routed to @Porter, invented nothing** (Porter's instruction: never guess a price/rule).
It stays a spec until the owner answers; then I cut BE+FE. NOT the backoffice block (owner chose REQ-005 over it).

**Ball: @Jason — TASK-219** (the note fix). **@Porter** — the REQ-005 five questions to the owner (they decide the
enum's behaviour + whether it's a revenue path). I'll spec the build once they land.

## [Jason (BE)] — TASK-219 BUILT → REVIEW. The note now reaches the message a teacher actually reads. → @Sober @Porter
`course_confirmed` has carried the note since TASK-201; `booking_confirmed` — the one a teacher gets on the
day's own booking — never did. The owner found it the only way it could be found: he typed a note and read the
message that came back without it.

**The note travels in the payload, not from the enriched context.** The worker builds `ctx` from the booking row
the outbox references, so a note read from there would vanish if that row were later edited or deleted. A
message about a moment should carry the moment's facts — the same choice `course_confirmed` and `sick_leave`
already make.

**One payload object, sent to teacher and parent**, with a test asserting exactly two uses of it and **no**
inline literal left behind. Two literals are two places to update and one of them gets missed; that is how a
parent ends up reading a different confirmation from the teacher.

**I widened the omission rule past the task:** the line is absent for `undefined`, `null` **and `""`**. An empty
string is what a field that was focused and cleared actually sends, and *"หมายเหตุ:"* with nothing after it
reads as a note the teacher **failed to receive** — worse than no line at all.

**The tests assert the rendered string both ways**, per the standing lesson: a payload field that is present and
unrendered looks identical to one that was never sent, which is precisely the shape of what the owner found.

**Verified:** tsc **0** · `bun test` **943 pass / 0 fail** (+7). No migration.

**Ball: @Sober** — review + **Q1**: the parent now receives the note as well, because it is one payload for both.
For *"แพ้ถั่ว"* that is the parent's own information coming back; but if a staff note is ever meant to be
teacher-only, that is a second payload and worth deciding **before** somebody writes something internal in that
box. **@Porter** — 👀 this message and TASK-206's confirm summary are **both still unseen on a real phone**;
they should be looked at in the same pass when the batch deploys.

## [--:--] Sober (SA) — TASK-219 (note on booking-confirm) ✅ PASS. REQ-007 closed. → @Porter
Reproduced tsc 0 · `bun test` **943/0** (+7). Verified the **round-trip**, not one layer: the confirm-branch payload
sets `attendeeNote: current.attendeeNote ?? null` (`:2148`) → the `booking_confirmed` template renders it → `line()`
(`:24`) omits it when absent (no empty label). Both cases tested (note present TH+EN; absent → line ABSENT, "an empty
label reads as a note that went missing"). REQ-007's missing, useful half is closed — the note reaches the teacher on
the day's own booking. On-phone look at deploy (owner/Porter), like REQ-067B.

**My lane is clear.** Remaining, both Porter/owner:
- **TASK-219 deploy + on-phone look** — small standalone message fix (no migration); ship whenever.
- **REQ-005 "other" — the 5 owner questions** (what is "other" · charged?/price · consumes? · teacher LINE? · day-end?)
  are with @Porter for the owner; I spec BE+FE the moment they land.
- TASK-218 (reminder per-recipient idempotency) is queued low-pri for @Jason.

**Ball: @Porter** — TASK-219 deploy + the REQ-005 five questions to the owner. I'll read your newest before starting
whatever's next.

## [--:--] Porter (PM) — ⏸️ REQ-005 PARKED by the owner. Deploy TASK-219 instead. → @Sober
> *"งานขนาดระดับนึง ก็ปล่อยไปก่อน มา deploy เรื่องนั้นก่อน"*

**REQ-005 is parked — not dropped.** ⚠️ **His answers are already on record and must not be re-asked:**
- **"other" = a flexible booking the admin configures per instance** — charged **or not** · consumes a
  course/voucher **or not** · **with or without a student**.
- **Day-end: the same rule as everything else** — unmarked ⇒ auto-attend at 23:30.
- **Still unanswered, and blocking the build (do not invent):** (a) **if charged, what price** — typed amount vs
  catalogue item; (b) **what names the cell and the teacher's LINE line when there is no student** (Porter's lean:
  a short required title — *"ประชุมทีม"*, *"ปิดปรับปรุงลาน"*).

**Next: deploy TASK-219** (the note on a single-booking confirm). Small, standalone, **no migration.**

**Ball: owner** — deploy. **@Sober: nothing** until REQ-005's two answers land.

## [--:--] Porter (PM) — ✅ TASK-219 live on `uat`. **REQ-007 CLOSED.** End of the day's work. → @everyone
Verified on a phone before shipping — and **the screenshot carried its own control case**: the note-bearing booking
rendered `หมายเหตุ: awdawdaaaa`, while two earlier confirms **with no note showed no line at all.** Both branches
in one image; nobody had to be believed.

⇒ **REQ-007 is complete** — the note field on every booking type **and** the note reaching the teacher, on a
single booking **and** on a whole-course confirm.

### Delivered today (2026-08-29)
FIX-007 expiry ✅ · REQ-071 Drop ✅ · REQ-072 all four parts ✅ · REQ-073 confirm popups ✅ · REQ-074 cancel
1HR/Voucher ✅ · REQ-075 labels ✅ · the `เรียนอยู่แล้ว (ย้ายข้อมูล)` dialog ✅ · TASK-217 off-card save ✅ ·
TASK-209 `job_runs` ✅ · TASK-219 note-on-confirm ✅ — **all live on `uat`.**

### Open, for whenever the owner wants it
- **REQ-005 "other"** — ⏸️ parked by him. **Answered:** flexible per instance (charge/consume/student all
  optional), day-end auto-attends like everything else. **Still owed, and NOT to be invented:** the price
  mechanism, and what names the cell/LINE line when there is no student.
- **The six `REQ-BO` backoffice items** — untouched, unplanned, **the largest thing left on his list.**
- **Reach:** 2 of 20 teachers and 0 of 180 parents are LINE-linked on `uat`. Parents: his standing ruling, leave
  it. Teachers: he said *"ยังไม่ผูกช่างมันไปเถอะ"* — **also parked, on the record.**
- Low priority: TASK-218 per-recipient reminder idempotency · the `sm-jobs` secret in git · bulkConfirm
  multi-select fan-out.

**Ball: owner.** Nothing is waiting on @Sober, @Jason, @Fern or @Tanya.

## [--:--] Porter (PM) — 🧪 @Tanya — retest **REQ-004 เช่าอุปกรณ์** on `sid`. Owner's call. (= board REQ-028 / SPEC-031)
The owner's list has carried this as *In Progress (Testing)* for weeks and **the team has not touched it this
stretch** — so nobody knows if it works. **He wants it retested rather than assumed.** Right instinct: an item
sitting at "Testing" with no test behind it is just a guess with a nicer label.

**You write on `sid`** (board 154-159) — fixture, footprint, retire with cancel + `ADMIN_ERROR` where applicable.

### What it is meant to do (`SPEC-031`)
Four codes, VAT-inclusive: **`rental-set` 200 · `rental-ride` 150 · `rental-helmet` 50 · `rental-pads` 50**.
`POST /rentals` → the **existing `recordSale` path** (no new money mechanism), tagged
**`metadata.revenueKind = "RENTAL"`** so reports can separate it.

### The five checks
1. **Each of the four codes posts its own amount** — `bo.movement` row per rental, value matches the table above.
   ⚠️ **Do not trust the API's response alone — read the ledger.** *"It returned 200"* has been wrong on this
   project three times.
2. 🔴 **A failure must be VISIBLE.** The spec says it in these words: *"the one place the best-effort default is
   wrong"* — a rental **is** the event, not downstream bookkeeping. **Force a failure (unknown code) and confirm
   staff see an error, not a silent 200.**
3. **Double-submit posts ONCE** — idempotency key. The same shape as the confirm-outbox check.
4. **Unknown code / non-positive hours → rejected**, with a message, not a 500.
5. **`revenueKind = "RENTAL"` is actually set** on the movement — without it a rental is indistinguishable from
   course revenue in any report.

📌 **Prior evidence, not a pass:** during the REQ-063 discount run you saw rentals at `600 / ฿300 / ฿700`, so the
path is alive. **That was a discount test, not a rental test** — it proves nothing about the four codes, the
marker, or idempotency.

⚠️ **If `sale:ensure-items` has never been run since the rental codes were added, the items may not exist and every
rental 404s silently.** That exact thing hid for weeks on `uat` (2026-08-22). **Check the items exist first** — it
will save you a confusing hour.

### Open and non-blocking
**Q2 — the entry point** (rental as an add-on at booking/attendance vs a standalone sale) was never answered by the
owner. **The money model is identical either way**, so it does not block this test; **report which one is actually
implemented** and I will put it to him.

**Ball: @Tanya.**

## [13:52] Tanya (QA) — 🧪 REQ-004 เช่าอุปกรณ์ retested on `sid`. Works; 2 facts need one DATA REQUEST. → @Porter
Detail in **tests/TEST-062**. Wrote on `sid` (posts go to `bo.movement` directly — no HTTP hop).

✅ **Items exist** — all four codes → `201 recorded` (not `502 item-missing`). So `sale:ensure-items` HAS been run for rentals here; the `uat`-2026-08-22 silent-404 trap is not present on `sid`.

✅ **Live checks pass (what QA can drive):**
- **Idempotency (check 3):** same key+code → 1st `recorded`, 2nd `duplicate`. Posts once.
- **Failure is VISIBLE (checks 2/4):** unknown code `rental-jetpack` → **400** `"รหัสอุปกรณ์เช่าไม่ถูกต้อง"`; hours `0`/`-1` → 400, `25` → 400 (max 24), `2.5` → 400 (int). **Never a silent 200, never a 500.** And a known code whose post fails throws **502 `RENTAL_NOT_POSTED`** by design (the post IS the event).

🟡 **Read-the-ledger (checks 1 & 5) — DATA REQUEST.** You said it plainly ("*don't trust the 200, read the ledger — it's been wrong three times*"). I can't read `bo.movement`/`bo.item` (no read API, no backoffice creds), so I posted 5 identifiable rows (keys `rental:qa-rental-20260829-*`). One query signs both:
```sql
SELECT m.idempotency_key, i.external_ref, m.value_minor,
       i.metadata->>'revenueKind' AS revenue_kind
FROM bo.movement m JOIN bo.item i ON i.id = m.item_id
WHERE m.idempotency_key LIKE 'rental:qa-rental-20260829-%'
ORDER BY m.idempotency_key;
-- expect value_minor: set 20000 · ride 15000 · helmet 5000 · pads 5000 · ride3 45000 (satang)
--        revenue_kind = 'RENTAL' for all; the '-dupe' key present EXACTLY ONCE
```
📌 `revenueKind` sits on the **`bo.item`** (the movement points to it), not the movement row — that's the design; it still separates rental from tuition.

📌 **Your Q2 (entry point) — it's BOTH, already built & FE-wired:** standalone walk-in button (BookingsContent, no refId → minted key) AND a booking add-on button (BookingModal, refId=booking.id). Money model identical. (The validation comment even says "owner Q2=both".)

⚠️ **Footprint:** 6 rental movements now sit on `sid`'s `bo.movement` (dev ledger) — **there is no rental-reversal endpoint**, so I can't retire them; all carry the `qa-rental-20260829-*` key so the owner can see/remove them (they're in the DATA REQUEST rows).

**Ball: @Porter** — route the one ledger DATA REQUEST (SQL above) to sign checks 1 & 5; then REQ-004 is a real pass, not a "Testing" label. `uat` untouched. Nothing else waiting on QA.
