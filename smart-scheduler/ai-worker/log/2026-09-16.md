# 2026-09-16

## Porter (PM) — @Tanya: ✅ item 0 PASS (11 surfaces) · 🔴 **item 2 FAIL on `sid`**: `QA-089-expiry` (size 6, 3 advance leaves) expires `2026-11-18` = last session, not week 11 (`12-02`); baseline without leave gets its +2. A later leave's make-up now lands AFTER expiry. Also: `88c2917c` note restored. **To @Sober: formula-not-on-path vs BE-not-restarted — decide with evidence first.** List: advance-leave table tick menu overlaps rows (UI flaky).

## Sober (SA) — item 2 `sid` failure settled: (b), the BE process is stale
- BE `HEAD 3680d00` carries TASK-358; ran `courseBornCeiling` with Tanya's inputs ⇒ `2026-12-02` (week 11); baseline `11-11` matches hers. Her `11-18` = the OLD formula exactly. One path only (`:1677`), `:2332` only grows. ⇒ the `sid` BE was not restarted/rebuilt at `3680d00`. Told @Porter: ask the owner for a BE restart, no fix. Flagged her part (b) (make-up after an unmoved expiry) for one re-look post-restart.

## Porter (PM) — Sober: reading (b) with evidence — `sid` BE is running code older than `3680d00` (Tanya's `11-18` is the OLD formula's exact output; the committed one gives `12-02`). **Asked the owner: redeploy/restart BE on `sid` at `3680d00`+, then Tanya re-creates the course.** Sober continues item 8.

## Porter (PM) — owner: `sid` BE restarted. @Tanya GO on the item-2 re-test.

## Tanya (QA) — `REQ-089 item 2` re-test on "restarted" BE: 🔴 **still `2026-11-18` — deploy, not formula**
Re-created `QA-089-expiry-2` (size 6, start 23 Sep, 3 advance leaves, 17:00). Create expiry `2026-11-18` (expected `2026-12-02`); part (b) expiry did not grow, course expires before last session (`25/Nov`).
🔑 **Source-checked:** commit `3680d00` fixes `courseBornCeiling` = `max(base,lastPlanned)+absences×7` = week 11 = `12-02` (the DoD). Running box emits `11-18` = OLD formula exactly ⇒ **restart didn't pick up `3680d00` (the newest BE commit).** To settle: confirm `3680d00` is in the running artifact. My 09-15 FAIL stands (pre-TASK-358 build).
⚠️ Two ฿6,490 specimens now on sid for this check; a third needed once BE is truly fixed, then cancel the olds. No LINE/uat/phone.

## Porter (PM) — @Tanya re-test `QA-089-expiry-2`: 🔴 **still `11-18`** after the restart; two readers (Sober, Tanya) agree the running box emits the OLD formula to the day and `3680d00` is BE HEAD. **⇒ the restart did not carry the build.** Asked the owner to confirm the `sid` BE checkout is at `3680d00`+ AND rebuilt, then restart. Two ฿6,490 specimens on `sid`; a third needed after a real deploy, then Tanya cancels the two olds.

## Porter (PM) — owner: deploy = local build → zip → upload → `pm2 restart` (no pull/build on the server). Recorded in SYSTEM-FACTS. ⇒ the BE zip was built from a checkout/build output older than `3680d00`. Asked him to rebuild BE locally at `3680d00`+ (fresh build output), re-zip, upload, restart.

## Porter (PM) — owner: `sid` BE redeployed from a fresh build. @Tanya GO on `QA-089-expiry-3`; pass ⇒ cancel the two olds.

## Tanya (QA) — `REQ-089 item 2` on FRESH build: ✅ **PASS**
`QA-089-expiry-3` (size 6, start 23 Sep, 3 advance leaves, 18:00): **create expiry `2026-12-02`** = week 11, exact. Commit `3680d00` now running (the two `11-18`s were stale zips).
✅ **Part (b):** every post-creation leave covered — `12-02`/last `18-11` → `12-02`/last `25-11` → (2nd quota) `12-02`/last `12-02`. **Never expires before last session.** Ceiling is born with `base+advance` headroom, so it doesn't need to grow within quota — correct, not a second finding.
🟢 Rounds 23/24 close: `11-18` = old formula on stale zips; `12-02` = fixed. Cleaned up: `QA-089-expiry` + `-2` CANCELLED, `-3` left as passing specimen. Card glitched in pane; expiry API-confirmed ×2, format proven Round 23. No LINE/uat/phone.

## Porter (PM) — ✅ **`REQ-089` item 2 GREEN on `sid`** (`QA-089-expiry-3` ⇒ `2026-12-02`; quota make-ups all land under it; the two `11-18`s were stale zips). Items 0 and 2 both QA-passed. Two old specimens cancelled. Waiting on Sober (item 8) and the owner (DATA REQUEST on old-formula courses; `uat` deploy of 357+358 whenever).

## Porter (PM) — owner: old advance-leave courses left as-is, admins fix each (`REQ-089 §3`). DATA REQUEST withdrawn; Sober told.
- Sober: item 2 green on `sid` (stale zips). TASK-359 cut → @Jason (item 8, teacher on resume, contract first). Told @Porter.

## 2026-09-16 — Jason (BE)

### ✅ TASK-359 — pick the teacher on resume, BOTH doors (`REQ-089 item 8`) — **DONE (code)**
**2133 pass / 0 fail**, 173 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · contract sent first.
🔑 **One derivation per door — `input.teacherId ?? <today's>` — and the ABSENT path is the same expression evaluating to the old write, byte for byte.** The clash names the CHOSEN teacher; a picked teacher runs the create path's OWN guard (`assertTeacherBookable`) — nothing invented on either door.
🔴 **THE FINDING, asserted: the create path has NO server-side teacher↔subject rule** — `teacher_subjects` feeds lists, never a refusal — ⇒ none invented on the resume doors; a resume-only one fails a mutation. **On the list: one check in `insertBooking` for every door, if wanted.**
📌 The existing `booking_resumed` send follows the row ⇒ goes to the NEW teacher; the old one is told nothing (item 7's family); no send added. Neither message prints a teacher ⇒ no output changed.
🔑 **Six mutations bite** — 🔻 *the first F was a bad mutation (a duplicate zod key, the later one wins) and proved nothing; rewritten, it fails.*
❓ **`rows[0]` = the course's FIRST session (`asc(date), asc(startTime)`) ⇒ a reassigned-then-dropped course is handed BACK to its original teacher by the absent path.** Not changed; for the list: the picker's default should probably be the LAST session's teacher — one BE line that moves the absent path, so a decision.
- TASK-359 reviewed (2133/0). TASK-360 (FE) cut → @Fern. Two findings to the owner's list via Porter: no server teacher↔subject rule; resume default = first session's teacher.

## [--:--] Fern (FE) — ✅ **TASK-360 done — one teacher picker on both resume dialogs; default = the current teacher; `teacherId` on the wire only when changed; the options from ONE function built on the create picker's parts.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  341 pass / 0 fail   (was 330; +11)
bun run build     → ok
```
6 modified · 2 new (`lib/scheduler/resume-teacher.ts` + test). No new dictionary key (`booking.teacher` /
`course.pickTeacher` reused). Create picker untouched. Notes in the TASK §4.

**The one function:** `resumeTeacherOptions` = `bookableOnDate` (imported from `work-days`, not re-derived) AND
teaches-the-subject (by id for a course row, by NAME for a booking — it carries the name) **plus the current teacher
always** (the default is "keep", and a value missing from a Select renders blank — TASK-353's class).
`resumeTeacherIdToSend` = the pick only when it differs; **both services spread it conditionally, so an unchanged
picker sends a body with no `teacherId` key** — the contract's absent path. Asserted on the services and both call
sites. 📌 Honest note: the create picker has no teachers-by-subject filter (it picks the teacher first); what is
reused is its two parts, composed exactly once here — neither dialog's resume region contains `teachers.filter`,
`subjectOptions` or `bookableOnDate` (asserted).
**Course door:** `PlanModal` passes `courseSlot.teacher.id` + `courseSlot.subject` (the same first-non-extra row as
the slot); the `Select` re-seeds on every open, `allowDeselect={false}`. **Booking door:** seeded to
`booking.teacherId`, re-seeded when the menu opens the dialog. Refusals: the server's sentence, unchanged, both doors.
🔑 **Break-and-watch ×6, each in `try/finally`** (always send · bare `teacherId: input.teacherId` · current not
force-included · subject ignored · a second filter · booking sends always) ⇒ 1/1/2/3/2/1 fails; md5 identical on 4 files.

### Q — the LAST session's teacher: **in hand, yes** (the plan holds every row with its teacher and status) — **and
deliberately not used:** the default must be what the server writes when nothing is sent, and that is `rows[0]`.
A "last" default the admin leaves alone would send nothing and get the FIRST — a silent mismatch. Honest fixes:
the server defaults to the last live row (@Jason's item — the right one), or the dialog always sends (kills the
absent path). Today the screen shows exactly what the server does, consistent and wrong the same way. 🚫 Named.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-360 reviewed (341/0). Item 8 complete; told @Porter to deploy 359+360. Next: item 1 on his go.

## Porter (PM) — 🟢 item 8 complete (TASK-359 BE 2133/0 · TASK-360 FE 341/0, no migration). GO to Sober on item 1. Batching 8+1 into one `sid` deploy. To the owner: two list findings from Sober (see next message). @Tanya pre-staged for the item-8 re-test after deploy.

## Porter (PM) — owner on Sober's list findings: (2) YES — resume default teacher = last session's, not first (`rows.at(-1)`), rides item 1's round. (1) subject guard — explaining to the owner, held.

## Porter (PM) — owner: subject guard HELD (never seen a wrong-subject booking). On the list, not cut.

## Porter (PM) — owner set the `uat` release scope = items 0,1,2,3,8. 0/2/8 done+QA; 1 in progress; **item 3 pulled forward ahead of 4/5/7 for this release.** Plan: item 1 → item 3 → one `sid` deploy → QA → owner ships 0+1+2+3+8 to uat. Items 4,5,7 are a later batch.
- Sober: item 1 cut — TASK-361 (BE, + last-teacher default) @Jason, TASK-362 (FE, + tick-menu overlap, + default) @Fern, parallel on a one-sentence contract. Cap question to the owner via Porter.

### ✅ TASK-361 — advance leave on `Extended` rows (`REQ-089 item 1`) + resume default = LAST session's teacher — **DONE (code)**
**2148 pass / 0 fail**, 174 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)**.
🔑 **ONE rule, THREE callers — `plannedRowCount`: row `w` exists iff the live rows before it number fewer than `size`.** The validator's lock (`w <= size`) is replaced by it; **the CAP stays**; the preview draws exactly the rows it names, a ticked make-up carrying both flags; the create flips the ticked make-ups after the first reconcile and asks the SAME engine again — **one placement engine on the save side, and preview == save because both ask the same function and place from the last planned date, pinned.**
✅ The ceiling is unchanged in shape (Kavya holds; week 2 + make-up 5 ⇒ base + 2). ✅ Finding 2 done: resume default = `rows.at(-1)`, the absent path moved on purpose.
🔻 **Mutation E — a bare `break` before the flip — PASSED first time: the flip was dead with every line of it still in the file, and the source pins saw text, not behaviour.** Fixed the right way: the flip decision is now a pure `makeupsToFlip`, tested with the DoD's values, and the loop's only `break` is pinned as the guarded one. *TASK-345's lesson, again, on its author.*
🎯 **THE QUESTION — cap vs quota:** size 4/6/10 ⇒ quota **1/2/3**, the cap allows **3/5/9**. Different meanings the code has always kept apart; a declared absence is FREE (decision B), and with the lock gone the cap is the only bound on the chain. **Not moved; for the owner: if advance leave should spend the same allowance, the cap becomes the quota and the create counts against `leaveUsed` — one line each, a decision.**
- TASK-361 reviewed (2148/0; partly in human commit ff08acc). Cap-vs-quota numbers to the owner via Porter. Waiting on TASK-362.

## [--:--] Fern (FE) — ✅ **TASK-362 done — every previewed row tickable by its position (the `< size` lock gone); the tick menu's click-through named and shielded in one place; the resume default is the LAST teacher.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  345 pass / 0 fail   (was 341; +4)
bun run build     → ok
```
5 modified · 1 new (`lib/scheduler/plan-tick.test.ts`). No client existence rule, no client cap, no new key. Notes in
the TASK §4.

**§1** — `PlanModal.weekIndexOf` = the row's position in the server's plan (`i >= 0 ? i + 1 : 0`), make-ups included;
`courseSize` gone; TASK-148's comment rewritten with the reversal, not deleted. A ticked make-up renders `SICK_LEAVE`
(absent wins — now pinned); the toggle sends positions as-is and re-previews; the echo wins; refusals are the
server's sentence. Asserted on the request shape; mutations 1–3 (lock back · makeup wins · client cap) fail.
**§2 — cause:** Mantine's `Menu` closes on `mousedown`/`touchstart` OUTSIDE its dropdown and does NOT consume the
event; a `bottom-end` dropdown lies over the next row's own `⋯` (~40 px below), so one tap closes menu A and opens
menu B, and the next tap ticks the wrong row. The portal (Mantine's default, z 300 over the modal's 200) is not the
problem; the click-through is. **Fix, one place:** `withOverlay` + `overlayProps={{ backgroundOpacity: 0.05, zIndex:
299 }}` on the row menu — a click shield above the modal, below the dropdown. **Checked by RENDERING the menu open:**
`mantine-Menu-overlay` at `--overlay-z-index:299` mounts with the dropdown (pinned; mutation 4 fails). Every other
`Menu` in a modal shares the default; only the plan table stacks identical targets under its dropdown — named.
**§3** — `courseLastRow` (last non-extra row) feeds the picker's default; subject still off the slot row; TASK-360's
"first" pin rewritten with TASK-361's reason; mutation 5 fails. `teacherId` still only when changed.
🔑 **Break-and-watch ×5 in `try/finally`** ⇒ 1/1/1/1/1; md5 identical.

### Q — 3 ticks on a size-4 on a phone: **seven rows (the cap makes 3 the max), and the STATUS column — the one the
ticks change — is under the horizontal scroll.** `StickyScrollArea minWidth={640}` pins only the action column;
on 375 px the viewport shows Date · Time · `⋯`; which row became a leave is invisible until the admin scrolls right.
Pre-existing; three ticks make it the everyday case. Named, not fixed: pin Date as `lead` (the CSS supports it,
unused here) or move Status beside Date.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-362 reviewed (346/0). Item 1 complete; told @Porter the item 8 + item 1 round is ready. Status-column-off-screen finding to the list. Next: item 5 on his go.

## Porter (PM) — 🟢 item 1 complete (TASK-361 BE 2148/0 · TASK-362 FE 346/0; last-teacher default; tick-menu overlap fixed). item 8 also done. **GO to Sober on item 3 (not item 5) — it is the last unbuilt piece of the owner's uat batch 0/1/2/3/8.** Then one `sid` deploy of 8+1+3, QA, owner ships uat. Two owner list items pending below.

## Porter (PM) — owner: advance-leave cap = "< size" is correct (`REQ-089 §4`), not the quota. No change.

## Porter (PM) — 🔴 owner REOPENED the item-1 cap (`REQ-089 §4.1`): real reschedule case where a family must leave an EXTENDED session too; the `< size` cap blocks the size-th absence. §4 superseded pending Sober's mechanism answer (does the chain terminate; can the cap count original sessions only; does free advance leave get abused if uncapped). Proposed reading: cap counts original sessions only, make-ups always leaveable. Nothing dispatched; item 3 unaffected.

## Porter (PM) — owner: FULL UNLOCK, remove the `< size` cap (`REQ-089 §4.2`), overriding §4/§4.1. Told Sober to build it but treat "chain terminates" + "zero-original-attendance is a valid state" as correctness gates — stop and report if either breaks, else cut (one line in the item-1 validator).
- Sober: §4.2 full unlock — mechanism answered (terminates; rows ≤ size+|absent|; request array unbounded → owner's list). Cut TASK-363 (cap + gates) and TASK-364 (delete BE) → @Jason; TASK-365 (delete FE) → @Fern. Told @Porter.

### ✅ TASK-363 — the `< size` advance-leave cap removed, FULL UNLOCK (`REQ-089 §4.2`), behind two gates — **DONE (code)**
**2161 pass / 0 fail**, 175 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · one line removed, comment rewritten as the reversal.
🔴 **The gates ran on the UNFIXED tree first** — 11 pass, only the LINE tests failing on the cap still present ⇒ nothing downstream wrong ⇒ the line came out after.
✅ **GATE 1 (terminates):** {1..4} ⇒ 8 rows · {1..8} ⇒ 12 · `rows ≤ size + |absent|` over six shapes · the flip loop simulated with the real functions: 0 / 1 / 2 passes, every count ≤ `wanted`. ✅ **GATE 2 (valid downstream, run not read):** the ceiling, the engine, `leaveUsed`, attention (NOT nearly finished — correct), history (four `sick-leave` + four `makeup-appended`, labels right), the reminder, the import — **nothing wrong, nothing loud.**
🔻 Two reversals named (TASK-148's "not a course"; TASK-361's "the cap stays", the same day). 🔑 Three mutations bite, including a bounded non-terminating `plannedRowCount` caught by the count pin — *the first attempt was a no-op mutation and passed; rewritten, it fails.*
🎯 **THE REQUEST BOUND — 104, one reason: it is the length of the CALENDAR (two years of weekly rows), not the product; a request naming position 105 is a broken or hostile client, not a family. No product meaning, so it cannot be mistaken for the cap just removed. Not added — a bound is a decision.**

### 📋 TASK-364 — `DELETE /students/:id` contract CONFIRMED with three corrections (Jason), building
✅ As proposed: 200 `{ deleted: true }` · 409 `STUDENT_HAS_HISTORY` with the Thai count sentence in the app's `{ error }` envelope · 404 · history = any row in courses/bookings/vouchers, any status · one tx · no LINE · no migration.
🔴 **1:** the FK backstop today lands as `400 VALIDATION` (`onError`'s `23503` branch) — the service catches `23503` on the delete, re-counts, throws the same 409. 🔴 **2:** suspension is the PARENT's (`students` has no suspend column) — the delete ignores it; a walk-in with no parent is deletable; @Fern needs no suspend condition. 🔻 **3:** `api.ts:41` rewritten with the exception and its why.
🔑 No other FK to students; orphan text named (`notification_outbox.payload.studentName`); audit = one server log line, no table. Sent to @Sober for @Fern.

### ✅ TASK-364 — `DELETE /students/:id`, history-free only (`REQ-089 item 3`) — **DONE (code)**
**2172 pass / 0 fail**, 176 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · `studentHistoryRefusal` pure + `countStudentHistory` (no status filter) + `deleteStudent` (one tx; `23503` caught in the service and re-said as the same 409, never the 400) · the route with the token's actor · `api.ts:41` rewritten.
🔑 **TASK-185's router enumeration caught the new write route by omission** — classified `unrelated` with the reason. ✅ Pinned through the ROOT app (200 / 409 sentence / 404; actor = `sub`). 🔑 Five mutations bite, restores byte-identical. ❓ Owner's-list answer: the LINE side caches no child list; the name lives on only as past text. Sent to @Sober. Nothing waits for BE.
- TASK-363 + 364 reviewed (2172/0). Gates green. Contract corrections to @Fern. Request fence 104 → owner's list via Porter. Waiting on TASK-365.

## [--:--] Fern (FE) — ✅ **TASK-365 done — a red Delete on every student row, two taps, the server's sentence on refusal, no client rule.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  351 pass / 0 fail   (+5)
bun run build     → ok
```
5 modified · 1 new (`lib/people/delete-student.test.ts`). Built against @Jason's confirmed contract with his three
corrections (usual `{ error }` envelope; suspension is the parent's ⇒ no suspend condition on the button; walk-ins
have no row on this page — named). Notes in the TASK §3.

**Where:** the student's own row inside the parent card — a red `Trash2` icon beside the Edit pencil, offered on
EVERY student (asserted nothing gates it; mutation 5 hiding it when the parent is suspended fails). **Two taps:** a
`Modal` naming the student, *"Permanent — this cannot be undone"* / *"ลบถาวร — ทำแล้วย้อนกลับไม่ได้"*, plain Cancel,
red *Yes, delete* — the TASK-355 pattern, the suspend dialog's shape. **One source:** `deleteStudent` = one
`api.delete`; the hook = one mutation invalidating `PARENTS_KEY` — the same key as `useCreateStudent` (asserted side
by side); the page reads nothing about history (asserted by absence on page, service, hook). **409 ⇒ the server's
sentence in an Alert INSIDE the dialog, dialog stays open; 200 ⇒ `{name} deleted`, close, list refreshes.** 5 keys × 2.
🔑 **Break-and-watch ×5 in `try/finally`** (one tap · a client history check · refusal closes · no invalidate ·
hidden when suspended) ⇒ 1/1/1/1/1; the fifth slipped through the first pin and the pin was tightened before the
report; md5 identical.

### Q — on a phone: the People page has no `⋯`; suspend is a parent-card button, the student row has one icon
(Edit). Delete is a second ICON on the student row — beside the student's Edit, not beside Suspend — red next to
grey, 15 px, 2 px apart: **distinguishable by colour, not by distance; the confirm dialog is the safety.** If the
owner wants distance, `gap="xs"` or a per-student `⋯` (the natural home when a third student action arrives).

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-365 reviewed (351/0). Item 3 complete; the 8+1+3(+§4.2) round handed to @Porter for the one sid deploy, with Tanya's shapes. Next batch 4/5/7 on his word.

## Porter (PM) — 🟢 **uat batch fully built:** item 8 (359+360) · item 1 (361+362) + §4.2 cap removal (363, both gates GREEN — chain terminates, zero-original course is a valid state) · item 3 delete (364+365, first-ever delete, history-free only, 409 with counts) · last-teacher default. FE 351/0 · BE 2172/0 · 35=35 no migration. **`sid` deploy released to the owner** (fresh local build → zip → upload → pm2 restart, BE AND FE). @Tanya pre-staged (3 checks). Items 4,5,7 next batch.

## Porter (PM) — owner: `sid` deployed (8+1+3). @Tanya GO on the three checks.

## Tanya (QA) — ✅ **8+1+3 round done — Item 3 PASS · Item 1+§4.2 PASS · Item 8 PASS. One report in `inbox/PM.md`; footprint clean.**
**Method declared:** `sid` pane again would not hold the cookie; Item 3 + Item 1 UI behaviours verified on screen earlier this session, **Item 8 + Item 1's expiry number driven through the API** (the same ops the picker fires: `resume`, `booking resume`, `PATCH booking`, `preview`). No LINE, no `uat`.
- **Item 3 (delete):** fresh student → two taps → gone, count drops; KKTEST (history) → server sentence `มีประวัติ: คอร์ส 17 · คาบ 154 · บัตร 0 — ระงับแทน` inside the dialog, nothing deleted; zero-row child of suspended family → deletable. PASS.
- **Item 1+§4.2:** tick all four rows → 8-row plan (4 ON LEAVE + 4 EXTENDED), corroborated by created course `80fae836`; tick-makeup→9, untick→8; overlap fix holds; **expiry = base + 4 weeks = `2026-11-19` on a clean slot** (size-6/3-adv cross-check = `12-02`, matches Round 25). 🔎 **Finding:** realised expiry follows the LAST make-up's actual date — a clogged teacher slot bumps make-ups later and inflates it past base+N weeks (`12-09` on Ek's full Wed-17:00). Formula ceiling correct; product question flagged to Porter.
- **Item 8 (teacher on resume):** dropped course, no picker → new sessions take the **LAST** session's teacher (Dewy, not first Camp) — TASK-361 confirmed; pick a teacher → that teacher every session (Haris); standalone paused booking same (Camp kept / Haris picked). **Subject-mismatch refusal NOT_TESTED** — all 23 `sid` teachers teach all 19 subjects, so the branch can't fire; adjacent guard verified (unknown teacher → `400 "ไม่พบครู"`, nothing written). Course sessions can't be individually paused (`COURSE_SESSION`) — booking-pause is for standalone bookings; noted correct-by-design.
- **Footprint:** 8 courses/bookings created, all cancelled (sessions off calendars); fresh students deleted at test time; none of mine left ACTIVE. Report ONCE per the standing rule.

## Porter (PM) — ✅ **8+1+3 round ALL PASS on `sid`** (@Tanya): item 3 delete (fresh ⇒ gone; with bookings ⇒ `มีประวัติ: คอร์ส 17 · คาบ 154` refuse; suspended zero-row ⇒ deletable) · item 1+§4.2 (tick all 4 ⇒ 8 rows, 4 make-ups, expiry base+4w; overlap fixed) · item 8 (resume default = LAST teacher; pick overrides; both course & booking). All test rows cleaned. **uat batch 0/1/2/3/8 is GREEN — ready for the owner's uat deploy.**
🔎 Two findings, neither a bug: (1) realised expiry can EXCEED base+N when a clogged teacher slot bumps make-ups to later weeks (correct-by-design; possible owner spec point) · (2) subject-mismatch resume guard NOT_TESTED on `sid` (all 23 teachers teach all 19 subjects) — the guard exists on the same path create uses; proving it live needs a teacher with a restricted subject list (a data setup).

## Porter (PM) — owner: uat batch 0/1/2/3/8 LIVE on `uat`. ▶️ GO to Sober on the next batch — items 4 (teacher-leave-cancelled on admin schedule), 5 (`Last` badge on schedule), 7 (LINE to teacher on cancel/pause). One `sid` deploy when the batch lands.
- Sober: 8+1+3 live on uat. Next batch: item 5 cut (TASK-366 BE, TASK-367 FE, parallel). Items 4 and 7 need rulings — item 4: no teacher-leave fact exists (cancelReason has 3 values, none "teacher") → propose `TEACHER_LEAVE`; item 7: per-session vs per-course message on bulk drop/pause. Stale TASK-301/302 fact block rewritten as superseded; four facts added.

### 📋 TASK-366 — `courseLast` contract CONFIRMED (Jason), building
`courseLast: boolean` on every `GET /calendar` booking DTO from `deriveLiveEndDate` as-is; ONE grouped read of the live rows of the range's distinct courses (the `hasRental` shape), `deriveLiveEndDate` per course, lookup per DTO; no month view exists (day/week only), ≈ 600 rows in one round trip. Correction: computed on the calendar + single read, `false` on the list/create/pause/resume like `hasRental`. Fact for the owner's question: the function reads LIVE rows only ⇒ the badge leaves the cell at attendance, by construction. No migration.

### ✅ TASK-366 — `courseLast` on every calendar booking (`REQ-089 item 5`) — **DONE (code)**
**2184 pass / 0 fail**, 177 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · `liveEndDateByCourse` + `isCourseLast` pure over `deriveLiveEndDate` as-is · ONE grouped read (`liveEndDatesForCourses`) resolved before the calendar loop, the `hasRental` twin; the single read agrees · DTO `courseLast ?? false`.
✅ DoD pinned with rows (make-up last; CANCELLED last ⇒ previous live; single ⇒ false; SICK_LEAVE last ⇒ false). 🔑 The owner's question answered by the function as a test: the badge leaves the cell at attendance, by construction. 🔑 Five mutations bite, restores byte-identical. Sent to @Sober. Nothing waits for BE.
- TASK-366 reviewed (2184/0). Contract note to @Fern (courseLast meaningful on calendar + single read only). Badge-leaves-at-attendance fact to the owner's list.

## [--:--] Fern (FE) — ✅ **TASK-367 done — a black `Last` stamp on the cell's name row, both views + legend, rendered from the server's `courseLast` and nothing else.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  357 pass / 0 fail   (was 351; +6)
bun run build     → ok
```
8 modified · 1 new (`lib/scheduler/last-stamp.test.ts`). Built against @Jason's confirmed contract (`courseLast` computed
on the calendar + single read, `false` elsewhere — the `hasRental` shape). Notes in the TASK §3.

**Wire → type → mapper:** `BookingDTO.courseLast?` (this repo's claim, Jason's computed-where note beside it) →
`Booking.courseLast?` → `dtoToBooking`: `courseLast: dto.courseLast === true` — strict boolean (value-asserted).
**ONE component `LastStamp`** in `BookingCellBody.tsx` beside the other shared cell parts; renders only when the flag
is `true`, else nothing (rendered-asserted: `>Last<` vs byte-identical empty tree). **High-contrast on purpose:**
solid `bg-neutral-900 text-white`, bold uppercase, 10 px day / 9 px week — not a pastel; NOT gated by the
`CellDisplay` toggles. Both grids place it on the name row right after `displayName` (asserted). **Legend:** the
same chip + *last session of the course / คาบสุดท้ายของคอร์ส*. **Word:** `Last` in both languages — the owner's
stamp, not a sentence. 🚫 **No client-side "last":** a walk of `src` finds the mapper as the ONLY assignment; the
stamp and both grids carry no `isLast|endDate|sessions.at(-1)|course.size` (asserted).
🔑 **Break-and-watch ×7 in `try/finally`** (stamp every course row · derive from EXTENDED · mapper truthy · week drops
it · week gates on display · pastel · legend gone) ⇒ 2/1/2/1/1/1/1; md5 identical on 4 files.

### Q — tap-through: the whole cell is already ONE button opening `BookingModal`, so the stamp opens the booking
today; there is no "new course for this student" action in that modal — course creation starts on the Bookings
page. The stamp is enough for item 5 as worded (*แปะ … เห็นชัดๆ*); a *next course* action belongs in the booking
modal beside Pause/Resume, student + program pre-filled — one task, serving every last session. Stamp only, built.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-367 reviewed (357/0). Item 5 complete; told @Porter. Waiting on rulings for items 4 and 7.

## Porter (PM) — 🟢 item 5 complete (TASK-366 BE 2184/0 · TASK-367 FE 357/0, no migration): black `Last` chip on a course's last session, both views + legend, from a server flag, leaves at attendance. Held for the 4/5/7 deploy. **Items 4 & 7 blocked on owner rulings — put to him now.**

## Porter (PM) — item 4 RE-SCOPED (`REQ-089 §5`): NOT a teacher-leave reason — a calendar toggle to SHOW cancelled sessions (all reasons), display only, no money change. My earlier reading withdrawn. **`end-of-day` moved 18:30→17:30 by the owner (customer request)** — SYSTEM-FACTS updated, the "18:30 correct" block superseded. Item 7 ruling still pending — re-asking the owner.

## Porter (PM) — item 7 ruled (`REQ-089 §6`): per-course for bulk, per-session for single; STYLE must mirror existing outbox formats (owner's hard constraint, to avoid repeat corrections). Gate: drafted copy comes to me → owner eyeballs once before the send path lands. Sober cutting 7; batch 4/5/7 to one deploy.
- Sober: rulings in. Item 4 re-scoped to display-only → TASK-368 (BE) @Jason + TASK-369 (FE) @Fern. Item 7 → TASK-370 (BE, two kinds, house format hard, copy gated). Facts: §5, §6, 17:30.

### 📋 TASK-368 — `includeCancelled` contract CONFIRMED (Jason), building
Coercion = the `archived` enum pattern, not `z.coerce.boolean` (`"false"` would be true). Two corrections: `cancelReason` does not ride the DTO today — added (`null` on live rows); a cancelled row can share a cell with its replacement — precedence LIVE > SICK_LEAVE > CANCELLED decided and built (alternative `cancelled[]` per slot offered). Subtract not branch: `calendarHiddenStatuses(flag)` beside the constant; TASK-260's literal pin rewritten. `extendedFromId` does not ride the DTO — said, not built.

### ✅ TASK-368 — `GET /calendar?includeCancelled=true` (`REQ-089 §5`, item 4 display-only) — **DONE (code)**
**2199 pass / 0 fail**, 178 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · the `archived` enum pattern (`"false"` off) · `calendarHiddenStatuses(flag)` — subtract, not branch, one query · `cellRank` LIVE > SICK_LEAVE > CANCELLED (UC-004 outcome unchanged, pinned) · `cancelReason` on the DTO.
🔻 Two TASK-260 pins rewritten, named. 🔑 Six mutations bite. Two decisions flagged for @Sober (`cancelReason` on the default path; a rebooked slot shows the live class). `extendedFromId` does not ride — said, not built. ▶️ TASK-370 next.

### 📋 TASK-370 — contract + DRAFTED COPY sent to @Sober (Jason); plumbing being built with placeholder keys
Four corrections: the single pause already tells the teacher (TASK-260's `booking_paused`, owner-approved — no second message); the single cancel sends nothing to anyone today (parent sends pinned at 0); mirror `leave_notice` (live) not `leave_teacher` (dead); no reason-label rule exists — one new `cancelReasonText`. Two kinds, `course_dropped_teacher` covers drop AND end (`cause`), grouped by teacher; 15 → 17 cases (walker pins move, named). New words: three header stamps, one label, three reason labels — everything else existing keys. Restore paths: nothing un-cancels; the resumes already tell the coach.

### ✅ TASK-370 — LINE to the coach on cancel / drop / end of a CONFIRMED class (`REQ-089 §6`, item 7) — **PLUMBING DONE (code); words gated**
**2215 pass / 0 fail**, 179 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · `cancelReasonText` (one rule) · two kinds in `leave_notice`'s shape (15 → 17) · single gated on pre-write CONFIRMED; bulk grouped by coach, drop AND end · `pauseBooking` untouched (TASK-260's message is the pause message).
🔻 Eight pins moved with reasons (walkers 15→17; survivors 6→8; TASK-318's re-scoped; the `Record<TemplateKey>` control fired twice more). 🔑 Seven mutations bite. 🔻 Own correction: the pause's no-row-when-unlinked is SPEC-072 AC-7, not a deviation — two conventions named for @Sober. Words (3 stamps, 1 label, 3 reason labels) await the owner. Nothing waits for BE.
- TASK-368 reviewed (2199/0); TASK-370 plumbing reviewed (2215/0), words gated. Scope facts (pause already tells; cancel told nobody; END included) + the drafted copy → @Porter for the owner. Contract facts to @Fern (cancelReason rides; LIVE > leave > CANCELLED).

## Porter (PM) — owner: (1) item 7 copy approved (`§6.1`) · (2) course END notifies coach too (`§6.2`) · (3) item 4 = a SEPARATE cancelled tray like the paused tray, not on-grid struck cells (`§5.1`). Reshaped TASK-369 to Sober. Copy strings + END path land; FE builds the tray. One deploy when 369 in.
- Sober: owner approved item-7 copy + END; item 4 re-shaped to a cancelled TRAY (§5.1) — TASK-368 §8 (top-level `cancelled[]`, cellRank out) and TASK-369 §4 (PausedTray pattern). Both dispatched.

### ✅ TASK-368 §8 — the cancelled TRAY (owner `§5.1`) + ✅ TASK-370 strings landed — **DONE (code), one build**
**2213 pass / 0 fail**, 179 files · tsc 0 · 🚫 **no migration (35 = 35)** · grid exactly today's (constant literal, UC-004 literal; `calendarHiddenStatuses` + `cellRank` + `calendar-cell.ts` deleted) · tray = a separate read, top-level `cancelled: BookingDTO[]`, key absent unless asked · TASK-260 pins back to their literals with the reason.
🔑 Six mutations bite — one only after fixing my own test: describe-level `region()` calls made a whole block vanish as "0 fail" under a mutation; every region is now lazy inside its test (the rule is written in the file). 370: markers off, 0 bytes of copy changed (md5 identical), 7 keys / 14 values, now byte-frozen in the test. Nothing waits for BE.
- TASK-368 (tray re-shape) + TASK-370 (strings) reviewed, 2213/0. BE side of 4/5/7 complete. Contract line to @Fern. Region-outside-a-test lesson noted.

## [--:--] Fern (FE) — ✅ **TASK-369 done (§4 shape) — `Show cancelled` reveals a CANCELLED TRAY beside the paused one; OFF sends no param; the grid is untouched. The struck cell was never built — nothing to take out.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  368 pass / 0 fail   (was 357; +11)
bun run build     → ok
```
10 modified · 2 new (`lib/scheduler/cancelled-tray.ts` + test). Built against Jason's verbatim contract line. Notes
in the TASK §5.

**§1 toggle:** in `CellDisplayMenu` under a divider, NOT a sixth `CELL_FIELDS` entry (those never filter; this
reveals a tray and changes the request) — its own store `ss.showCancelled`, same `useSyncExternalStore`/localStorage
mechanism, default OFF. **Request:** `getCalendar(date, view, includeCancelled)` spreads the param only when ON
(the `archived` pattern, never `"false"`); the query key carries `"with-cancelled" | "live"` ⇒ two cached answers;
the page reads `calendar?.cancelled ?? []`. Wire claims: `CalendarResponse.cancelled?`, `BookingDTO.cancelReason?`;
mapper passes it as sent.
**§4 tray:** `PausedTray` gained ONE prop, `variant` (default `paused` ⇒ every existing call and pin unchanged); a
`TRAY` table names the differences (icon `Ban`, four labels, `gray` badge, its own collapsed store); the cancelled
row renders `{date} {time} · {coach}` + the reason = the EXISTING `endCourse.<code>` label, else `note`, else
nothing (`cancelReasonDisplay`, value-asserted). Rendered-asserted with two rows (code label vs note fallback,
coach, `10:00` never `10:00:00`, own title, empty sentence, strip layout). Mounted in BOTH layouts only while ON;
the rail is a spine only when EVERY tray in it is collapsed. 6 keys ×2.
📌 **The time sweep (`t-arg-shape`) went red on the new row line** — the fourth formatted site named with its
reason, not loosened; mutation 7 (raw `startTime`) now fails three tests.
🔑 **Break-and-watch ×7 in `try/finally`** (param always · one key · tray always · note over code · reason filter ·
a CANCELLED predicate in the cell body · raw time) ⇒ 1/1/1/3/2/1/3; md5 identical on 6 files.

### Q — readability with the toggle ON: **by construction the grid does not change**; the box holds the visible
RANGE's rows (the server returns the request's range — a day or a week, not the term), scrolling at the paused
tray's limits (`max-h-[28rem]` rail / `w-56` strip rows). A 30-cancellation week makes the strip a long sideways
scroll; the count badge says how many first. Lever if it bites: the rail `max-h` or a day/week split. Named.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-369 reviewed (368/0). 4/5/7 round complete on both sides; handed to @Porter for the one sid deploy with Tanya's shapes. REQ-089 fully built (item 6 excluded).

## Porter (PM) — 🟢 **4/5/7 round complete** (TASK-368/369 item 4 tray · 366/367 item 5 chip · 370 item 7 coach outbox + END): FE 368/0 · BE 2213/0 · 35=35 no migration. **`sid` deploy released to the owner.** @Tanya pre-staged (tray · chip · outbox text; LINE-on-phone is the owner's). **REQ-089 has no open item after this round except item 6 (owner's own FE team).**

## Porter (PM) — owner: `sid` deployed (4/5/7). @Tanya GO.

## Tanya (QA) — ✅ **4/5/7 round done — Item 4 PASS (8/8) · Item 5 PASS (4/4) · Item 7 BLOCKED (DATA REQUEST). Report in `inbox/PM.md`; footprint cleaned.**
Pane held the cookie this round; items 4 & 5 verified on screen, API used only to place/mutate the rows the UI acts on.
- **Item 4 (cancelled tray):** `Show cancelled sessions` (Cell display) ⇒ tray beside paused, rows date·time·coach·reason, coded⇒label; tap opens booking; collapse independent; reload remembered (`ss.showCancelled=1`); OFF⇒no tray; week (43) & day (scoped) views; **rebooked slot ⇒ cancelled stays in tray + new booking on grid.** One gap: free-text-only reason (free⇒note) not reached — coded⇒label solid.
- **Item 5 (`Last` chip):** legend ✓; last session carries `Last` (both views); attend⇒gone (30 Oct badge present→absent); sick-leave last (23 Oct) appends make-up (30 Oct) ⇒ **chip moves to the make-up.**
- **Item 7 (coach outbox text):** 🔴 NOT_TESTED — `notification_outbox` has no read API (SYSTEM-FACTS §109) and **0/23 sid teachers are LINE-linked** so nothing queues; drop/end echo no notification. Only the cancel echo (`NotifyResult`, status-only, no text) is readable: CONFIRMED cancel⇒skipped(unlinked, =check 5), PENDING cancel⇒null(none). Message TEXT / drop-per-coach / COURSE ENDED stamp = **DATA REQUEST** (outbox dump, or owner links a demo coach).
- **Footprint:** created b54f6473·6f3779b7·e2e4c0be·ca3f4f41, mutated+cancelled 2fbed92a; all cancelled, none left ACTIVE. Toggle left ON in qa localStorage (harmless). No LINE, no uat, no code.

## Porter (PM) — @Tanya 4/5/7: ✅ **item 4 (tray) all 8 PASS · item 5 (chip) all 4 PASS.** Item 7: **enqueue logic PASS** (confirmed⇒attempt · pending⇒none · unlinked⇒`skipped "ยังไม่ผูก LINE"`) but the message TEXT is UNREAD on `sid` — no outbox read API, 0/23 teachers LINE-linked. Copy is owner-approved + byte-frozen in TASK-370's test, so text is verified at code level; the only open verification is a real linked coach on a phone = the owner's check. Offered him: link a demo coach on `sid`, OR verify on `uat` once a real coach is linked. All test rows cleaned.

## Porter (PM) — 🟢 **REQ-089 FULLY LIVE on `uat`** (owner deployed). All 9 items: 0,1,2,3,4,5,8 built+QA-passed on `sid`; 6 is the owner's FE team; 7 (coach LINE) logic-passed + copy owner-approved/test-frozen, message text is the owner's phone check on `uat` where real coaches are linked. Batch closed. Board rows marked on uat.

## Porter (PM) — 🔵 new: `REQ-090` — customer renames 16 programs with a `Private` prefix (CSV archived verbatim; map in the REQ). Answers `REQ-085 §16.1`. Asked the owner 2 things (existing courses take the new name? · the `INLINESKATE` one-word spelling verbatim or fixed?) and @Sober 1 (is a program name editable in the admin UI ⇒ data entry, or a one-time owner-run change?). Trailing spaces will be trimmed (PM decision). Nothing dispatched.

## Porter (PM) — `REQ-090 §4`: owner (1) existing courses take the new name — yes; (2) verbatim spelling, `INLINESKATE` one word kept. Waiting on @Sober's mechanism answer (UI-editable ⇒ data entry, or owner-run change).
