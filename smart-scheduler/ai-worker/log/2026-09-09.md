# 2026-09-09

---

## Jason (BE) — ✅ TASK-298 DONE (code), with §5. → @Sober tsc **0** · **1797 pass / 0 fail**, 143 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] `tsc --noEmit` → **0** · `bun test` → **1797 / 0**, 143 files · 🚫 no migration (**35 = 35**, counted)
- [x] 🔑 **The preview returns the SAME `ExpiryWarning` the PATCH returns** — **by construction, not by
      comparison**: both read one function, so there is nothing to keep in step
- [x] 🔑 **It WRITES NOTHING** — asserted as an absence across **both** the route body and the shared
      computation it delegates to
- [x] **An earlier date names the sessions it cuts** — the LIST, and a settled session is never in it
- [x] **A later date returns `warn: false`** — the quiet case asserted as a real case
- [x] 🔑 **Break it and watch** — restored, suite green before this number
- [x] 🚫 The PATCH byte-identical · `expiryImpact` and `EXPIRY_SETTLED_STATUSES` untouched — asserted
- [x] **§5: the preview reports the remaining-leave room**, including the SPENT case · **a date that eats the
      room is distinguishable from one that does not**, asserted on both sides · 🚫 **still not a gate**

New: `POST /courses/:id/expiry/preview`, `expiryLeaveRoom` (pure), `src/lib/expiry-preview.test.ts` (12 tests).

### §1 One answer, two callers — rather than two derivations kept in step
`expiryDecision(id, expiryDate)` loads the course and its rows once and returns the impact **and** the leave
room. The preview reports it; the PATCH writes and then reports it.
🔑 **The DoD asks the preview and the PATCH to agree, and I made that unfalsifiable instead of tested:** there
is now exactly **one** `expiryImpact(` call in the service, asserted. Two derivations of one question are two
answers that can disagree, and this file's own header calls three copies of one answer this project's most
frequent defect.
🚫 **The PATCH's response is unchanged** — `leaveRoom` rides on the preview only, so §4 holds.

### §2 ✅ §5 — and the answer to your "say so if it needs a different data path" is **no, it does not**
The leave room needs the **same rows** (for the plan's end) and the **same course row** (for
`courseLeaveQuota − leaveUsed`) that the session impact already required. ⇒ **one load, two answers**, asserted
as `findMany` appearing exactly once in the shared function. **It folded in; it was not wedged in.**
`expiryLeaveRoom` sits beside `expiryImpact`, pure, using the **same `EXPIRY_SETTLED_STATUSES`** and the **same
inclusive boundary** — a boundary meaning one thing in the session warning and another in the leave warning
would be worse than no warning.
🔑 **Numbers, not a verdict:** `remainingLeave · planEnd · neededFor · roomFor · roomForAll`. The screen writes
the sentence, which is the division that already lets `ExpiryWarningAlert` compute nothing.
⚠️ **The SPENT case is the one that keeps it honest** — a family with no leave left loses nothing, so an earlier
date must not stack a leave warning on top of the session warning it already raises. Asserted.

### §3 ✅ Break it and watch
Making the preview save — `db.update(coursePackages).set({ expiryDate })`, the exact mistake a route sitting one
letter from a PATCH invites — fails the writes-nothing assertion **on the route body**, naming it. Restored.

### 🔻 Three tests were defending the old call site — all corrected, none deleted
`course-ended-writes.test.ts`'s **write-route classification caught the new route by omission on the first
run**, exactly as designed; classified `unrelated` with the reason (*nothing is reachable from it because there
is no write*). And two assertions pinned `const impact = expiryImpact(` — both now follow it into
`expiryDecision`, **with the property they protect unchanged**: the edit still warns, and still nothing throws
between having the warning and writing.

### 🔑 The Question — **the acts that commit before they show are the ones where the SYSTEM picks a DATE**
| act | previews? |
|---|---|
| create course · import · end course · workDays change · plan change *(incl. `mark-absence`)* · **expiry (this task)** | ✅ |
| 🔴 **`POST /bookings/:id/status` — `sick-leave`** | ❌ **none** |
| 🔴 **`POST /courses/:id/resume`** — the re-plan | ❌ **none** |
| `POST /bookings/:id/status` — `cancel` on a course session (reconciles and re-owes) | ❌ |
| `bulkConfirm` — partial success | ❌ |

🔴 **The everyday leave is the one with no preview, and it is the highest-frequency date the system chooses.**
📌 **That is how TASK-300 stayed invisible for as long as it did:** nobody could see where a make-up would land
until it had landed. The plan editor previews the same act (`planChange` `mark-absence`) — **so the product
already knows how to answer the question; the per-session path just never asks it.**
⚠️ **And the resume picks N dates at once and can fail mid-way with `SLOT_TAKEN`** ⇒ an admin discovers a clash
only on submit, having already chosen a schedule.

⇒ 🔑 **For `REQ-086`, the line I would draw is not "acts that write" but *"acts where the system, not the
person, decides something the person will be held to"*.** A date the family is told about is exactly that, and
it is why the preview list above splits so cleanly. 🚫 **Named, not built** — none of these is in this task.

**BALL: @Sober — TASK-298 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ✅ **TASK-298 PASSED. `§11.3` is closed.** 🔑 **He answered "assert they agree" by deleting one of them.** ➡️ **`§7.1` starts: TASK-284 amended to carry all three of `§3`.**

**Reproduced:** `tsc` **0** · **1797 pass / 0 fail** (5059 expects, 143 files) · **35 = 35** ·
`POST /courses/:id/expiry/preview` · **exactly ONE `expiryImpact(` call in the service** · `expiryDecision`
shared, `leaveRoom` on the preview only.

### 🔑 The DoD asked for a comparison; he removed the possibility of disagreement
> *"The DoD asks the preview and the PATCH to agree, and I made that unfalsifiable instead of tested."*

✅ **Strictly better than what I asked for: a comparison proves they agree TODAY; one call site means they cannot
diverge.** 📌 **Same move as `lib/validate.ts` owning the validator import in TASK-296.** 🔑 **Twice now he has
answered *"assert X agrees with Y"* by deleting one of them** — → **`SYSTEM-FACTS`.**

### ✅ §5 folded in, and the answer to the question I actually asked
**I said *say so if it needs a different data path*. It does not** — the leave room needs **the same rows and the
same course row** the session impact already loaded ⇒ **one load, two answers**, asserted as `findMany` appearing
once. ✅ **Same `EXPIRY_SETTLED_STATUSES`, same inclusive boundary** — *"a boundary meaning one thing in the
session warning and another in the leave warning would be worse than no warning."*
🔑 **The SPENT case keeps it honest:** a family with no leave left loses nothing, **so an earlier date must not
stack a leave warning on the session warning it already raises.** ⚠️ **A warning that fires either way is not a
warning.**
📌 **And `course-ended-writes.test.ts` caught the new route BY OMISSION on the first run** — **a classification
test noticing a route nobody told it about is the rarest guard we have, and it fired for real.**

### 🔴 His Question answer, and it reframes `REQ-086`
**`sick-leave` and `resume` have NO preview** — **the two highest-frequency dates the system chooses.**
> *"That is how TASK-300 stayed invisible: nobody could see where a make-up would land until it had landed."*

⚠️ **The plan editor previews the SAME act** ⇒ **the product already knows how to answer; the per-session path
never asks.** 🚫 **Cut nothing — routed to @Porter as a candidate, his call whether it goes near the owner.**
🔑 **And his line, better than the one we had:** ***"not acts that WRITE, but acts where the SYSTEM, not the
person, decides something the person will be held to."*** **A date a family is told about is exactly that.**
⇒ **it turns @Porter's *no editor without a preview* into a test for WHICH previews.**

### ➡️ `§7.1` starts — TASK-284 amended rather than a new task
**`uat` is out, so *"first item after `uat`"* is now first**, and **the other two `§3` fixes are to the SAME
message.** 🔑 **Two tasks a week apart on one message is the split this week was spent closing.**
🔴 **@Porter's `§8.1` trap is in the DoD as TWO SEPARATE assertions:** **`Advance Leave Notice` always prints
`(-)`; `Remark` does not print at all.** ⚠️ **One "empty fields" test cannot catch a swap, and a swap is
invisible to anyone who has not read that table.**
📌 **`§8.2` too: English = LABELS and SYSTEM values.** 🚫 **Never a student's Thai name, never the admin's
`Remark`** — *translating a Remark is putting words in an admin's mouth.* **`Date` names a WEEKDAY.**

**BALL: @Jason — TASK-284, the whole of `§7.1`.**

---

## Jason (BE) — ✅ TASK-284 DONE (code) — all three of REQ-085 §3/§7.1. → @Sober tsc **0** · **1809 pass / 0 fail**, 144 files · 🚫 **35 `.sql` = 35 journal tags.**

All three of `REQ-085 §3` / `§7.1`, in one message and one shipment.

- [x] `tsc --noEmit` → **0** · `bun test` → **1809 / 0**, 144 files · 🚫 no migration, no database, no FE change
- [x] 🔑 **A note on a LATER session renders** — the owner's exact case
- [x] **A note on every session renders that string** — the normal path, unchanged
- [x] **Differing notes print the EARLIEST**, with the comment saying why it is a rule and not a guess
- [x] **No note anywhere ⇒ no `Remark` line**
- [x] 🔑 **`Advance Leave Notice` prints `(-)` when there is none** — asserted on the empty case
- [x] 🔑 **`Remark` is ABSENT — the whole line — when there is no note**, asserted **separately**
- [x] **`Date` renders an ENGLISH WEEKDAY** — asserted by the word, on a known date
- [x] **A Thai student name and a Thai `Remark` are byte-identical to what was typed**
- [x] 🔑 **Break it and watch** — both halves; restored, suite green before this number
- [x] 🚫 `booking_confirmed` byte-identical — asserted

New: `courseNote` and `TEMPLATE_LANG` / `TEMPLATE_NONE` (both named, both pure), and
`src/lib/confirmed-schedule-req085.test.ts` (12 tests).

### (a) 🔻 The limitation I documented and chose not to fix
`courseNote(rows)` — **the first NON-EMPTY note, in date order** — replaces `rows[0]?.attendeeNote`. Extracted
and named rather than left inline, so the rule is testable without a database and so the sentence explaining it
has somewhere to live.
📌 **Date order is now the CALLER's stated guarantee** (`loadCourseForEnd` orders `asc(date), asc(startTime)`);
passing unordered rows would make *"earliest"* mean *"whichever the database returned first"*, and that is
written where someone would otherwise pass them.
⚠️ Whitespace is not a note: `"   "` must not become `Remark : ` — a label with nothing after it, which
TASK-219 established reads as information that went missing.

### (b) `Date : Sunday` · (c) `**Advance Leave Notice : (-)`
Both are **values WE generate**, so both follow the template's convention. `TEMPLATE_LANG` and `TEMPLATE_NONE`
sit in `line-message-fields.ts`, beside the customer's labels and their ` : ` separator — 🚫 **not a template
store, two constants**, which is the one definition site §5 asks for without restructuring anything.
🔑 **`REQ-079 §18` had already ruled labels English; `Date : อังคาร` was that ruling never applied to a VALUE.**

### 🔴 §8.1's trap — asserted separately, and the swap is caught
Three assertions, not one: the leave line **always prints**, the `Remark` line **is absent entirely**, and a
third with **both** empty where one survives and the other does not. ✅ **Break it and watch:** making the leave
line vanish when empty — the exact swap §8.1 warns about — fails the *always prints* assertion by name, and
would have passed any single "empty fields" test.
✅ **And the other half:** restoring `rows[0]` reproduces the owner's screen exactly — `Expected "23232sssss",
Received null`, no `Remark` at all.

### ⚠️ One consequence worth stating plainly: **the message is now language-INVARIANT**
Every remaining piece is a label, a value we generate, or a human's own words ⇒ **`TH` and `EN` render byte-for-
byte the same.** That is the ruling rather than an accident, and it is asserted so a translation creeping back
into any of them fails.
🔻 **Two tests asserted `th !== en` on this message.** That was a **PROXY** for *the switch still switches*; the
real property is that no notification renders BOTH languages. Both are kept — the proxy moved to
`booking_confirmed`, whose `ob_l_*` labels are bilingual and byte-frozen — and the invariance is now asserted as
the fact it is. **Seven tests in total were pinning the Thai weekday or `ไม่มี`; all corrected with the reason,
none deleted.**

### 🔑 The Question — **`Coach` is the other field, and it has no guard**
| payload field | from | can the rows differ? |
|---|---|---|
| `note` | `rows[0]?.attendeeNote` | 🔴 **yes — this task** |
| **`coach`** | `rows[0]?.teacher` | 🔴 **YES, and nothing prevents it** |
| `subject` | `rows[0]?.subject?.name` | ✅ no — **guarded twice** |
| `student` | `rows[0]?.student` | ✅ no — every row is created with the course's `studentId` |

🔴 **`coach` can differ two ways:** `createCoursePackage` accepts a per-session `teacherId` in `sessions[]`, and
`planChange`'s `move` accepts one (TASK-094's teacher-change rule exists precisely because it happens).
📌 **The contrast is the useful part:** `subject` is safe **by construction** — SPEC-045/TASK-138 refuses a mixed
course in the zod refine *and* again in the service — while **`coach` has no equivalent rule anywhere.** So a
course taught by two teachers prints the earliest session's coach on `CONFIRMED SCHEDULE`, and unlike the note
it will not look empty; **it will look confidently wrong**, which is harder to notice.
🚫 **Fixed none of them**, as instructed. ⚠️ Whether a course summary should name several coaches, or the
course's main one, is a customer question and not mine.

**BALL: @Sober — TASK-284 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ✅ **TASK-284 PASSED: all three of `§7.1` in one shipment.** 🔑 **His PROXY correction is the keeper.** ❓ **`Coach` goes to the customer, not into a task.** ➡️ **TASK-303 = `§7.3`.**

**Reproduced:** `tsc` **0** · **1809 pass / 0 fail** (5084 expects, 144 files) · **35 = 35** · `courseNote` at
`course-plan.ts:355` · `TEMPLATE_LANG` / `TEMPLATE_NONE` at `line-message-fields.ts:36/47`.

### 🔑 *"That was a PROXY for 'the switch still switches'"*
**Making the message language-invariant broke two tests asserting `th !== en` on it.** ✅ **He did not delete
them:** he **named what they were FOR**, **moved the proxy to `booking_confirmed`** (genuinely bilingual,
byte-frozen), **and asserted the real property in their place** — *no notification renders both languages.*
🔑 **A test that fails because a requirement changed is not a wrong test — it is a test in the WRONG PLACE.**
**Seven corrected with reasons, none deleted.** → **`SYSTEM-FACTS`.**

### ✅ The choices inside the fix
- **`courseNote` extracted and named** ⇒ *"first non-empty, in date order"* testable without a database ·
  ⚠️ **date order recorded as the CALLER's guarantee**, where someone would otherwise pass unordered rows.
- **Whitespace is not a note** — 🔑 *a label with nothing after it reads as information that went missing*
  (TASK-219's lesson, applied unasked).
- **Two constants, not a template store** — the one-definition-site §5 asked for, with nothing restructured.
- ✅ **Break-and-watch on the §8.1 SWAP itself**: making the leave line vanish when empty **fails by name, and
  would have passed any single "empty fields" test.** 📌 **@Porter's table earned its place.**

### ❓ `Coach` — a customer question, correctly not a task
**`coach` comes from `rows[0]?.teacher`, and a course CAN have different teachers per session** (the create form
and `planChange`'s `move` both accept one).
🔑 **The contrast is the finding: `subject` is safe BY CONSTRUCTION — refused twice — and `coach` has no
equivalent rule anywhere.** ⇒ **the product decided one is a promise and the other is not, and nobody chose
that.**
⚠️ ***"Unlike the note it will not look empty; it will look confidently wrong."*** ⇒ **harder to notice.**
✅ **He raised it instead of picking an answer. Routed to @Porter for the customer** — main coach, several, or
enforce one per course. 🚫 **Nothing cut, nothing changed, nothing blocked.**

### ➡️ TASK-303 — `§7.3`, a REPLACEMENT not an edit
**Thai labels today; `เวลา` splits into `Date` + `Time`; `Date` names a WEEKDAY.**
🔴 **`§8.1` written in REVERSE into the task:** **`§7.3` has only ONE empty-field rule**, so **the risk is
CARRYING the `(-)` across from the message he finished an hour ago.**
⚠️ **Highest-volume notification in the product** — a parent, per booking, unrecallable — **so its byte-identical
pin is REWRITTEN to the new text, never deleted.** 📌 *An assertion that changes because a requirement changed is
correct; one deleted because it failed is how this class ships.*
❓ **And its Question: two messages now open with `CONFIRMED SCHEDULE`** — **does the CODE tell them apart, or
only the lines around them?** 🔑 *`REQ-086` will hand these to a customer as editable rows, and two rows with the
same title is where someone edits the wrong one.*

**BALL: @Jason — TASK-303.**

---

## Jason (BE) — ✅ TASK-303 DONE (code). → @Sober tsc **0** · **1818 pass / 0 fail**, 145 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] 🔑 **The whole message pinned BYTE-FOR-BYTE against `§7.3`'s block** — as a **replacement** for the old
      pin, not a deletion of it
- [x] **`Date` renders an ENGLISH WEEKDAY** and 🔑 **the calendar date appears NOWHERE** — asserted as an absence
- [x] 🔑 **`Remark` is ABSENT when there is no note**, and ⚠️ **`(-)` never appears on this message** — asserted
      across every empty combination
- [x] **A Thai student name and a Thai `Remark` are byte-identical to what was typed**
- [x] **`§7.1`'s course-level message is unchanged** — pinned in full, in the same file, beside this one
- [x] 🔑 **Break it and watch** — §3's trap committed on purpose; restored, suite green before this number
- [x] 🚫 No migration, no database, no FE change

New: `src/lib/session-confirmed-req085.test.ts` (8 tests) and the `session_confirmed` template.

### §1 What it renders now
```
📅CONFIRMED SCHEDULE:
Student : Aiwa
Program : Private Freeskate 1 HR
Date : Tuesday
Time : 11:00-12:00
Remark : เตรียมเฉพาะ Safety ให้น้อง
```

### §2 ✅ Helpers REUSED — and nothing written twice
`ob_course_title` · `renderFieldBlock` + `TEMPLATE_FIELDS` · `programLabel` · `notifyTypeOf` · the `ob_dow_*`
table with `TEMPLATE_LANG` · `extra()` with `ob_f_note` · `fieldLines`' omit-empty rule.
🚫 **No second `courseNote`, no second weekday lookup, no second `(-)`, no second omit-empty rule.**
✅ A new `TemplateKey` (`session_confirmed`) rather than reusing `confirmed_schedule`: the two messages have
different field lists, and — the part that matters — **this one has no `**Advance Leave Notice`, so §8.1's
`(-)` rule has nothing here to attach to.**

### §3 ⚠️ The ONE thing I added outside the message, and why
**`bookingType` and `size` on the `booking_confirmed` payload.** §7.3 asks for the booking's program string *as
§7.1 prints it*, and `programLabel` needs both. `MessageContext` carries neither and the worker's enrichment is
out of scope (§5). **Without them every COURSE session would print `1 HR` — a false statement about the package
on the majority of bookings.** Additive, and exactly what `course_confirmed` already carries.

### §4 🔴 One deviation from §7.3's literal block — flagging, not deciding
**Their example says `Private Freeskate 1 Hr`; we render `1 HR`.** §2 told me to use *"the program string as
`§7.1` prints it"*, and §7.1 ships `6 HR` — so I followed the instruction rather than the casing in the example.
⇒ **If the customer meant `Hr`, it is a one-line change in `programLabel` that moves `§7.1` too**, which is why
I did not make it unilaterally. **Your call.**

### §5 ✅ Break it and watch — §3's trap, committed deliberately
Making `Remark` fall back to `TEMPLATE_NONE` — **carrying `§7.1`'s `(-)` across, the exact mistake §3 names** —
fails two assertions: *"`Remark` is ABSENT"* and *"`(-)` NEVER appears"*. It would have looked entirely
deliberate in a diff.

### 🔻 Thirteen tests were pinning the old message. All corrected, none deleted.
The §4 pin was **rewritten to the new text**, as instructed. Two of the corrections are worth naming:
- 🔴 **AC-16's *"no line ends in a bare colon"* began failing on the customer's own header** (`CONFIRMED
  SCHEDULE:`). Narrowed to the FIELD lines — the property was always *a label printed with nothing after it*,
  and the title is not a label.
- 🔴 **The `th !== en` proxy moved AGAIN, in the same session.** TASK-284 moved it onto `booking_confirmed`
  because `course_confirmed` had become language-invariant; **this task made `booking_confirmed` invariant
  too.** It now sits on `reschedule_requested`, which still carries bilingual `ob_l_*` labels. ⚠️ **A proxy that
  has to move twice in one night is a proxy worth retiring** — I have kept it because the property is real, but
  it is measuring the wrong thing and the next REQ-085 format will move it a third time.

### 🔑 The Question — **the code distinguishes them, but not where `REQ-086` will need it to**
| what distinguishes them | strength |
|---|---|
| `payload.kind` — `booking_confirmed` vs `course_confirmed` | ⚠️ a plain string on an untyped payload (`[k: string]: unknown`); a typo falls through to the generic fallback |
| **`TemplateKey`** — `session_confirmed` vs `confirmed_schedule` | ✅ **structural** — `Record<TemplateKey, …>` forces every key to be declared, and it **caught this template today** in `message-time-format.test.ts` |
| the two byte-for-byte pins | ✅ pasting one message's text into the other's builder fails both |

🔴 **What does NOT distinguish them is the TITLE.** Both render `t("ob_course_title")` — **one i18n key, one
string.** ⇒ **for `REQ-086`, two editable rows would carry the same name and the same key**, and nothing in the
data says which message a row belongs to. 📌 **`TemplateKey` is the only real name either message has, and it
never reaches the outbox row or the i18n table** — so the honest answer to *"does the CODE distinguish them?"*
is **yes internally, and no at the layer the customer will edit.**
⇒ 🔑 **My suggestion, named not built: when `REQ-086` makes these rows, key them on `TemplateKey`, not on the
title.** The titles are the customer's words and two of them are identical; the template key is ours and cannot
be.

**BALL: @Sober — TASK-303 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ✅ **TASK-303 PASSED.** 🔑 **A `REQ-086` constraint arrived BEFORE the design: two customer messages share one title.** ➡️ **TASK-304 = `§7.2`.**

**Reproduced:** `tsc` **0** · **1818 pass / 0 fail** (5114 expects, 145 files) · **35 = 35** · no migration, no
database, no FE change.

### 🔑 His Question answer — *"yes internally, and no at the layer the customer will edit"*
**`TemplateKey` is structural and caught the new template the day it was added. `payload.kind` is a plain string
on an untyped payload.** 🔴 **But both messages render `t("ob_course_title")` — ONE key, one string** ⇒
**`REQ-086` would list two editable rows with the SAME NAME, and an edit to the wrong one would look like it
worked.**
✅ **Adopted as a ruling: the editor keys rows on `TemplateKey`, never on the title.** 🔑 **The titles are the
customer's words and two are identical; the template key is ours and cannot be.**
📌 **A design constraint reaching a REQ nobody has started — rather than its first review.**

### ✅ Three calls that were his and were right
- **A new `TemplateKey` instead of reusing `confirmed_schedule`** — 🔑 **because this message has no
  `**Advance Leave Notice`, so `§8.1`'s `(-)` has nothing to attach to.** *A structural reason, not a tidiness one.*
- **`bookingType` + `size` added to the payload** — ⚠️ **without them every COURSE session prints `1 HR`, false
  about the package on most bookings.** **Named as "the one thing outside the message" rather than left to find.**
- 🔴 **AC-16's "no line ends in a bare colon" started failing on the customer's OWN header** ⇒ **narrowed to FIELD
  lines, property restated** — *a label printed with nothing after it; a title is not a label.* 📌 **Second test
  today corrected by naming what it was FOR.**

### ✅ The trap was proven catchable, not merely warned about
He committed §3's mistake deliberately — carrying `§7.1`'s `(-)` into `Remark` — **and it failed two
assertions.** 🔑 ***"It would have looked entirely deliberate in a diff."***

### ⚖️ Two rulings I owed
1. **`1 HR` vs `Hr`: keep ours.** 🔑 **The customer's own examples disagree** — `§7.3` says `Hr`, `§9.1` says
   `HR`, same batch. ⇒ **nothing to follow, so consistency with `§7.1` wins.** ✅ **He flagged rather than
   changed; the change would have moved `§7.1` too.**
2. **The `th !== en` proxy: it does not belong on a NOTIFICATION.** It moved twice in one night as each `§7`
   format made another message invariant. 🔑 **Every notification is English-by-ruling, so any notification-based
   proxy WILL move again** ⇒ **move it to a CONVERSATION flow, where bilingual is the rule.** 🚫 **Not deleted.**
   📌 **His line earned the ruling: *"a proxy that has to move twice in one night is a proxy worth retiring."***

### ➡️ TASK-304 — `§7.2`, opening with @Porter's own correction
🔴 **AUTO is NOT getting "one language"** — *"Format แจ้งเตือน Auto โอเคแล้วค่ะ"*; **it gains `Remark` and nothing
else.** ⚠️ **The wider version of `§4` reached me first, so the DoD asserts AUTO's language is UNCHANGED** —
**the next reader will meet that version too.**
🔑 **Load-bearing DoD line, and it is the owner's own reason:** **`Remaining` / `*Expiry date` asserted ABSENT on
a non-course entry** — *"ไม่งั้นมันจะแยกยังไง"*. **A positive-only test passes while the distinction is broken.**
❓ **Its Question: `§7.2` COMMAND prints a STATUS to a customer's eye** — **which statuses can reach it, and does
every one have a word a parent would understand?** 🔴 *`EXTENDED`, `PENDING_RESCHEDULE` and `PAUSED` mean
something to us and nothing to a parent.*

**BALL: @Jason — TASK-304.**

---

## Jason (BE) — ✅ TASK-304 DONE (code). → @Sober tsc **0** · **1828 pass / 0 fail**, 146 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] 🔑 **BOTH shapes pinned in full** against `§7.2`'s two blocks
- [x] 🔑 **`Remaining` and `*Expiry date` asserted ABSENT on a non-course entry** — and on the one-hour BLOCK of
      a mixed message, not just on the message
- [x] **`Remark` is per BOOKING** — asserted with two entries carrying different notes in one render
- [x] 🔑 **`Remark` absent entirely with no note**, and ⚠️ **`(-)` never appears on either shape**
- [x] **AUTO's language is UNCHANGED** — asserted as *"the message with the note IS the message without it,
      plus one line"*
- [x] **`§7.1` and `§7.3` unchanged** — their full-message pins still hold
- [x] **§5: the proxy moved to a CONVERSATION flow** — see below
- [x] 🔑 **Break it and watch** — restored, suite green before this number
- [x] 🚫 No migration, no database, no FE change

New: `src/lib/daily-schedule-req085.test.ts` (10 tests).

### §1 What "one language" actually was
🔴 **The COMMAND schedule was wrapped in `both((l) => renderSchedule(rows, l, range))`** — a teacher asking for
their schedule received **the whole list twice, once per language.** That is what *"แบบคำสั่งให้เป็นภาษาเดียวพอ"*
is about; the renderer itself was never bilingual and never needed changing.
⇒ One line at the call site, rendering in `TEMPLATE_LANG` — **the same constant every `§7` format uses, rather
than a fourth answer to *"which language is a notification in?"***
🚫 **AUTO untouched**, and asserted so: the note-bearing render equals the note-free render plus one line.

### §2 ✅ Two renderers, one field each — and one shared rule
`remarkLine()` in the AUTO renderer and the `Remark` line in COMMAND both print `ob_f_note` in the customer's
` : ` convention — **the same label §7.1 and §7.3 use, so the word means one thing across every message a family
receives.** 🚫 The two SHAPES were not unified; only the RULE is shared, which is what §2 allows and asks to be
said out loud.
📌 COMMAND's `📝` prefix is gone — their block reads `Remark : …`.
⚠️ **The note had to travel** to reach AUTO at all: `attendeeNote` now flows `jobs.service` → `ReminderSession`
→ `TodayRow`. It is per BOOKING at every step, which is what makes two different remarks in one message
possible.

### §3 ✅ It was already conditional — so the deliverable is the assertion, and it is the NEGATIVE one
`Remaining` and `*Expiry date` come off `TYPE_OMITS`, so a one-hour entry has never carried them. **The ruling
turns an accident into a requirement, and the assertion is what makes that real.** Asserted as an ABSENCE, and
also on the one-hour BLOCK inside a mixed message — 🔑 *a positive-only test passes while the distinction is
broken*, and that is exactly where a mixed day would hide it.

### §4 ✅ Break it and watch — §4's trap, committed on purpose
Making AUTO's `Remark` fall back to `(-)` — **carrying §7.1's rule across, the third message running where that
is the risk** — fails three assertions, including *"AUTO's language is UNCHANGED"*, which catches it as a
LAYOUT change rather than only as a wrong string.

### §5 ✅ The proxy is retired from notifications — and now measures the property DIRECTLY
It sat on `course_confirmed` (TASK-284), then `booking_confirmed` (TASK-303), then `reschedule_requested` — and
your ruling is right: **every notification is English-by-ruling, so any notification-based proxy is guaranteed
to move again.**
⇒ It is now `tb("children_none")` — the children list, a CONVERSATION reply. 🔑 **And it stopped being a proxy
at all:** instead of *"TH ≠ EN, therefore something switches"*, it asserts that `tb()` composes **one string
containing both `t(key,"TH")` and `t(key,"EN")`** — the bilingual property itself. **`§7.4` cannot move it.**

### 🔑 The Question — **seven statuses reach that message, and two of them are our words, not a coach's**
The COMMAND schedule reads `t(\`status_\${r.status}\`)`, and its query excludes `CALENDAR_HIDDEN_STATUSES`
(`CANCELLED`, `PAUSED`) — so of the enum's **nine**, **seven** can appear:

| status | label | a coach reading it |
|---|---|---|
| `PENDING` · `CONFIRMED` · `ATTENDED` · `SICK_LEAVE` · `NO_SHOW` | Pending · Confirmed · Attended · Leave · No-show | ✅ plain |
| 🔴 **`EXTENDED`** | **Extended** | **our word for our mechanism.** It means *a make-up appended because someone took leave* — "Extended" says none of that, and it is the commonest non-obvious row on the list |
| 🔴 **`PENDING_RESCHEDULE`** | **Awaiting move** | better, and still does not say **who is being waited on** — a coach cannot tell whether the next action is theirs |

📌 **`PAUSED` is worth naming precisely because it CANNOT reach here:** it has a label (`Paused`), and the query
hides it. **A label that exists for a row that never renders is the shape TASK-271 found the other way round** —
a row that rendered its own key because no label existed.
🚫 **Changed nothing.** ⚠️ And the reader here is a TEACHER, not a parent — which makes `Extended` more
defensible than it would be on a family's message, and is worth knowing before anyone rewords it.

**BALL: @Sober — TASK-304 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ✅ **TASK-304 PASSED.** 🔴 ***"One language"* was a DEFECT: a teacher asking for their schedule got the whole list TWICE.** ➡️ **TASK-305 = `§7.4`, the last format and the only NEW message.**

**Reproduced:** `tsc` **0** · **1828 pass / 0 fail** (5140 expects, 146 files) · **35 = 35** · no migration, no
database, no FE change.

### 🔴 The requirement was politely worded and the cause was not
**`renderSchedule` was wrapped in `both((l) => …)`** ⇒ **a teacher who asked for their day received it twice, in
one message, once per language.** 🔑 **The customer wrote *"ให้เป็นภาษาเดียวพอ"* — as a preference. It was a
duplicate of their own schedule.**
📌 **Second time in two days:** `REQ-085 §1` said *"make leave unlimited"* while the screen was refusing him at
the week ceiling. ⇒ **a politely-worded request is not evidence that the cause is small.**
✅ **The renderer was never bilingual** ⇒ **one line at the call site, in `TEMPLATE_LANG`** — 🔑 *not a fourth
answer to "which language is a notification in?"* 🚫 **AUTO untouched and asserted.**

### ✅ He went past the DoD in the place it mattered
**`TYPE_OMITS` already made `Remaining` / `*Expiry date` conditional**, so the deliverable was the ASSERTION —
**and he asserted it on the one-hour BLOCK inside a MIXED message, not just on a one-hour message.** ⚠️ **A mixed
day is the normal day, and that is where a broken distinction would hide.**

### 🔑 The proxy stopped being a proxy
**My ruling was "move it somewhere it stops moving". He made it stop being a proxy:** it now asserts `tb()`
composes **one string containing both languages** — *the bilingual property itself*, on a CONVERSATION reply.
⇒ **it fails for the right reason instead of merely continuing to pass.** → **`SYSTEM-FACTS`, generalised:**
***when a proxy keeps moving, the fix is not a better location — it is asserting the property it stood in for.***

### ✅ A good accident worth naming
**AUTO's *"the message with the note IS the message without it, plus one line"* fired on the `(-)` carry-over
mutation** — **catching a wrong STRING as a LAYOUT change.** 🔑 **Stronger than a byte pin: it survives the field
being added and still fails on changes nobody predicted.**

### ❓ The Question — routed to @Porter, not cut
**Seven of nine statuses reach the COMMAND schedule. Five read plainly; `Extended` and `Awaiting move` are our
words for our mechanisms.** ⚠️ **His nuance is why it is not a defect: the reader is a TEACHER, not a parent** —
**I would not have known to say that.** 📌 **`PAUSED` has a label for a row that can never render — the mirror of
TASK-271 — recorded, not actioned.**

### ➡️ TASK-305 — `§7.4`, the only one of the four that ADDS a message
**A parent declares leave, the parent is notified, THE TEACHER IS NOT** ⇒ **a coach can arrive for a cancelled
session.** 🔴 **The owner has raised it twice.**
🔑 **`Coach` is for the ADMIN, not the teacher** — *the teacher already knows it is theirs; the admin covers every
coach* ⇒ ⚠️ **a teacher-only test proves half the requirement, and the DoD says so.**
⚖️ **I ruled `Date` = English WEEKDAY** (the customer's block is blank, so the convention decides) — ⚠️ **and
flagged my own doubt to @Porter: a leave can be declared WEEKS ahead.** 🔑 *One line if they want the date; I
would rather change one line than ship a fourth date format.*
❓ **Its Question is the one that matters: does every path that sets `SICK_LEAVE` go through ONE write?** 🔴 **A
notification wired to some of them is a teacher told SOMETIMES — worse than never. Never is a gap people work
around; sometimes is a promise that fails silently.**

**BALL: @Jason — TASK-305.**

---

## Jason (BE) — ✅ TASK-305 DONE (code). The teacher is told. → @Sober tsc **0** · **1839 pass / 0 fail**, 147 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] 🔑 **Pinned BYTE-FOR-BYTE** against `§9.1`'s block
- [x] 🔑 **BOTH recipients asserted** — the admin chat AND the teacher's, from **one payload object**
- [x] 🔑 **The PARENT does not receive it** — asserted as an absence
- [x] **`Coach` present and correct**, and asserted on the fuller (admin) audience too
- [x] 🔑 **`Remark` absent entirely with no note**, and ⚠️ **`(-)` never appears**
- [x] **`Date` renders an ENGLISH WEEKDAY** — asserted by the word
- [x] **The parent's existing leave confirmation is byte-identical** · **§7.1 and §7.3 still pinned**
- [x] 🔑 **Break it and watch** — restored, suite green before this number
- [x] 🚫 No migration, no FE change, and nothing about the MECHANICS of leave

New: `leave_notice` (template + title + renderer), `src/lib/leave-notice-req085.test.ts` (11 tests).

### §1 🔻 It was not un-built. It was gated off.
A teacher notification already existed — **`kind: "leave_teacher"`, wrapped in
`if (notifyOnLeave === "admin_and_teacher")`, and `notify_on_leave` defaults to `admin_only`.**
⇒ **on a default install that branch never ran**, which is exactly why the owner raised it twice and why it read
as missing. 📌 **A feature behind a default-off setting is indistinguishable from a feature nobody wrote** —
and this one had a comment explaining the default, so it was deliberate and still wrong for what he asked for.
🔑 **His ruling is unconditional** (*"เฉพาะแชทครู / แอดมิน"*), so the setting no longer gates the send.
⚠️ **`notify_on_leave` is now UNREAD by any code path.** Its spec row still exists (`lib/settings.ts:79`) and the
settings screen still offers it. **I did not delete it** — whether it goes or gets repurposed is yours, and a
setting that silently does nothing is the shape we spent this week naming. **Named, not removed.**

### §2 One payload, two sends
`leavePayload` is built once and handed to `notifyAdmins` and to the teacher's `enqueueLine` — **so a coach and
an admin can never read different versions of one leave.** The teacher send is non-throwing: `enqueueLine`
writes a SKIPPED row when the coach has no LINE link, so a leave never fails because of a notification.
📌 `bookingType` and `size` ride on the payload for the same reason as TASK-303: `MessageContext` carries
neither, and without them a COURSE session's `Program` reads `1 HR`.

### §3 ✅ Break it and watch
Removing the teacher send — leaving the admin's, **which is precisely the state that shipped** — fails the
both-recipients assertion and nothing else. 🔑 **That is the half-requirement §3 warned about, reproduced: the
admin-only build looks entirely functional from the admin's side.**

### 🔴 The Question — **FOUR writes set `SICK_LEAVE`, and ONE notifies. This is the finding.**
| # | path | notifies |
|---|---|---|
| `scheduler.service.ts:1703` | **declared at creation** (`absentWeeks`, `plannedAtCreation`) | ❌ nothing |
| `:2382` | **the plan editor**, `mark-absence` | ❌ nothing |
| `:2749` | **attendance correction**, `ATTENDED → SICK_LEAVE` | ❌ nothing |
| `:2810` | `updateBookingStatus` action `sick-leave` | ✅ **this task's `LEAVE NOTICE`** |

⇒ 🔴 **A teacher is now told SOMETIMES**, which is the thing you named as worse than never. Being precise about
how bad, because the three silent paths are not equal:
- **Creation-time declaration** — the leave is declared *before the course exists*; the teacher has not been
  told of the class yet either, so there is nothing to correct. **Arguably fine.**
- **Attendance correction** — the class already happened; the coach was there. A notice would be about the
  past. **Arguably fine, and its own branch says so.**
- 🔴 **The plan editor's `mark-absence` is the real hole.** It cancels a FUTURE session exactly as the
  per-session action does, and an admin using the plan editor is doing the same thing by a different door.
  **That is a coach arriving for a cancelled class, with the notification working.**

🚫 **Restructured nothing and wired nothing**, as instructed. 🔑 **My read: it is one path, not three, and it is
`:2382`** — which makes this smaller than "four writes" sounds, and I would rather say that than hand you a
number with no shape.

### §5 ⚖️ Your date doubt — I share it, and the convention is built
`Date : Tuesday` for a leave declared three weeks out tells a coach very little, and this message is the one of
the four most likely to be about a distant date. **Built to the convention as ruled**, and it is the one line
you said it would be if @Porter comes back with the calendar date.

**BALL: @Sober — TASK-305 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — 🎉 **ALL FOUR `§7` formats are in.** 🔴 **And the teacher notification the owner asked for TWICE already existed — switched off by default.**

**Reproduced:** `bun test` **1839 pass / 0 fail** (5167 expects, 147 files) · **35 = 35** · no migration, no FE
change · **typecheck clean** — ⚠️ *with a PINNED compiler; see below.*

### 🔴 *"It was not un-built. It was gated off."*
**`kind: "leave_teacher"`, behind `if (notifyOnLeave === "admin_and_teacher")`, with `notify_on_leave` defaulting
to `admin_only`** ⇒ **on a default install that branch never ran.**
🔑 ***A feature behind a default-off setting is indistinguishable from a feature nobody wrote.***
⚠️ **It even carried a comment explaining the default** — **deliberate, documented, and still not what he asked
for.** 📌 **A reviewer reading that branch would have found it CORRECT. The defect was the DEFAULT, and nobody
reviews defaults.**
✅ **Ruled: the setting is REMOVED** — the owner's instruction is unconditional, so a control offering that choice
can only ever be wrong. 🚫 **No stored-row deletion, no migration.** 🔑 **A control that does nothing is worse
than a missing feature: whoever sets it believes they changed something.**

### 🔑 His Question answer is why the follow-up is small
**Four writes set `SICK_LEAVE`; one notifies.** ✅ **He TRIAGED instead of handing me a number:** creation-time
and attendance-correction are genuinely fine *(the leave predates the class; the class already happened)*, and
🔴 **`:2382`, the plan editor's `mark-absence`, is the real hole — the same future session, the same act, a
different door.**
📌 ***"It is one path, not three, and I would rather say that than hand you a number with no shape."***
⇒ **TASK-306, with the dead setting folded in — two loose ends of ONE mechanism, one task.**
⚠️ **One thing I deliberately did NOT rule: `mark-absence` can affect several sessions in one edit.** **He can
see whether it batches; I would rather have his answer than my guess.**

### ⚠️ A toolchain fault I nearly read as a code defect
**`bunx tsc --noEmit` PANICS on this machine** — a `typescript-go` binary with a missing `lib.d.ts`, exiting 2.
✅ **`bunx --package typescript@5.6.3 tsc --noEmit` → exit 0, clean.** ⇒ **his `tsc 0` was TRUE when he ran it.**
🔑 **Check the SHAPE of a failure before reading it as a defect** — a Go panic in a compiler binary is not a type
error. → **`SYSTEM-FACTS`, with the working command.**

### ✅ Where the four formats landed
`§7.1` Remark + English weekday + `(-)` · `§7.2` both shapes gain Remark, COMMAND stops sending a teacher's day
TWICE · `§7.3` replaced entirely · 🎉 **`§7.4` NEW — `LEAVE NOTICE` to teacher AND admin, never the parent.**
🔑 **@Porter's `§8.1` trap was the most valuable line in the batch: deliberately triggered in THREE of the four
tasks and it failed by name every time.**

**BALL: @Jason — TASK-306.**

---

## Jason (BE) — ✅ TASK-306 DONE (code). Both doors, one builder. → @Sober **1843 pass / 0 fail**, 147 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0** (the plain `bunx tsc` is the broken one).

- [x] 🔑 **A leave marked in the PLAN EDITOR sends the same `LEAVE NOTICE`** — to both recipients
- [x] 🔑 **Made UNFALSIFIABLE rather than compared:** ONE `sendLeaveNotice`, one `kind: "leave_notice"` in the
      whole service — asserted by count
- [x] **A plan edit still succeeds when the coach has no LINE link** — asserted as the absence of a throw
- [x] **Multi-session behaviour NAMED and asserted** — one message per SESSION
- [x] **`notify_on_leave` gone from the registry and the screen**, asserted absent
- [x] ⚠️ **Grep confirmed nothing else read it** — see below
- [x] 🔑 **Break it and watch** — restored, suite green before this number
- [x] 🚫 The three fine paths, the parent's confirmation and `§7.1`–`§7.4` unchanged — asserted

### §1 ✅ One builder, both doors
`sendLeaveNotice(tx, booking, { size, via })` is now **the only place the message is built**, and both doors call
it. 🔑 **I made your preferred form true rather than asserting the two agree:** there is exactly one
`kind: "leave_notice"` and one `async function sendLeaveNotice(` in the service, both asserted by count — so
there is no second version to keep in step.

**⚠️ Multi-session: ONE message per SESSION, not per edit.** Each `mark-absence` change carries one
`bookingId`, and the notice names a specific class (`Date`, `Time`, `Coach`, `Remark`) — **a single message for
several sessions could not say WHICH.** ⇒ three absences in one edit send three notices. Asserted, and the send
sits inside the `mark-absence` branch so no other change kind fires it.

### §3 ✅ The control is gone — and the grep you asked for
**Nothing outside the registry read it.** The BE hits were the row itself and my own TASK-305 comments; the FE
hits are an i18n label, a help string and a mock row — **labels for a screen that is `Object.keys(SETTINGS)`
(`settings.service.ts:52`)**, so ⇒ **removing the registry row removes the field, and no FE change was needed.**
📌 **The leftover FE label/help/mock entries are orphaned strings in @Fern's repo** — named, not touched.
🚫 No stored row deleted, no migration. **A gravestone comment sits where the row was**, carrying the finding:
*a feature behind a default-off setting is indistinguishable from a feature nobody wrote.*
🔻 `notify-on-leave.test.ts` was **rewritten, not deleted** — it now asserts the removal stays, and its own sweep
strips comments first, **because on the first run my gravestone counted as a reader.**

### 🔴 A consequence I created, and am naming rather than leaving to be found
**TASK-305 replaced the admin's `sick_leave` alert and the gated `leave_teacher` push with one `leave_notice`.**
⇒ **neither kind is enqueued by any non-test code any more.** Their renderers still work and their tests still
pass — **which is exactly the shape that makes dead code look alive.** Asserted so re-wiring either one fails
loudly. 🚫 Not removed: this task says remove nothing else.
⚠️ **And a correction to my own TASK-305 report:** I called `sick_leave` *"the parent's existing leave
confirmation"*. It is not — it was the **admin alert**, which I had just replaced. The parent's confirmation is a
`textReply` in the webhook and was never touched; the assertion was right about the message being unchanged and
wrong about who reads it.

### 🔑 The Question — **a whole-tree sweep IS honest here, and I only know that because my first one lied**
✅ **Every registry key is read**, checked over all of `src` (tests excluded):
`teacher_change_notice_days` · `checkin_early_minutes` · `leave_cutoff_hours_fulltime` ·
`leave_cutoff_hours_freelance` · `line_parent_2fa`.

⚠️ **My FIRST attempt hand-listed the files to search and reported the two `leave_cutoff_hours_*` keys as
UNREAD.** They are read — through `leaveCutoffKey(teacher.type)` in `lib/leave-notice.ts`, a file I had not
listed. ⇒ **the sweep would have told us to delete two live settings**, minutes after we removed a real one.
🔑 **So the honest form of your question has a sharper answer than *"it needs an eye"*:**
- **The mechanism is sound** — every reader names its key as a **string literal**, including the one that looks
  dynamic: `leaveCutoffKey` is a **ternary over two literals**, not a composed string. A whole-tree grep finds it.
- 🔴 **What made it lie was the INPUT, not the technique** — a narrowed file list. **A sweep whose scope is
  hand-written is only as complete as somebody's memory**, which is the same failure as the thing it is looking
  for.
- ⚠️ **The residual risk is a key built by template literal** (`getSetting(\`leave_cutoff_${x}\`)`). **Nothing
  does that today — I checked: zero template-literal `getSetting` calls** — and a grep for that shape is the
  cheap guard if anyone ever starts.

⇒ **It can be asked mechanically, on the condition that the sweep walks the tree rather than a list.** 🚫 Removed
nothing else.

**BALL: @Sober — TASK-306 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ✅ **TASK-306 PASSED.** 🔴 **His first sweep LIED and the diagnosis revises a rule I gave him two tasks ago.** ⚖️ **Two rulings.** ➡️ **TASK-307 = `§6`; `§5` waits on the customer.**

**Reproduced:** `bun test` **1843 pass / 0 fail** (5158 expects, 147 files) · **35 = 35** ·
`bunx --package typescript@5.6.3 tsc --noEmit` → **0** · `notify_on_leave` gone from the registry, gravestone
comment only.

### 🔴 The sweep that lied — and the input, not the technique, was wrong
**His FIRST sweep hand-listed the files and reported two LIVE settings as unread.** ⇒ 🔴 **it would have told us to
delete two working settings, minutes after we removed a real dead one.**
✅ **His diagnosis is better than the answer I expected:** every reader names its key as a **string literal** —
**including the one that looks dynamic** (`leaveCutoffKey` is a ternary over two literals) — **so a whole-tree
grep finds it.** 🔑 ***A sweep whose scope is hand-written is only as complete as somebody's memory — the same
failure as the thing it is looking for.***
📌 **This REVISES what I told him in TASK-297.** I framed sweeps as *"mechanical vs needs an eye"*; **the honest
split is *"walks the tree vs walks a list"*.** ⇒ **`SYSTEM-FACTS`, and carried to @Porter, because it applies to
every "we checked everywhere" either of us says to the owner.**
✅ **And he checked the residual risk instead of asserting it away** — zero template-literal `getSetting` calls,
with the grep shape named for whenever someone starts.

### ✅ The work
**One `sendLeaveNotice`, one `kind`, asserted by count — unfalsifiable rather than compared, third time.**
✅ **One notice per SESSION**, with the reason: *a notice names a specific class, so one message for three
absences could not say which.*
✅ **The settings screen is `Object.keys(SETTINGS)`** ⇒ removing the row removed the field; **no FE change needed,
and he still named the orphaned FE strings.**
🔑 **`notify-on-leave.test.ts` rewritten to assert the removal STAYS — its sweep stripping comments, because his
own gravestone counted as a reader on the first run.** ⚠️ **A guard that catches its own author is a guard that
works.**

### 🔻 His self-correction, and my review repeated it
**He had called `sick_leave` *"the parent's existing leave confirmation"*; it was the ADMIN alert.** The parent's
confirmation is a `textReply` in the webhook and was never touched. ⇒ **the requirement still holds (the parent
not receiving the notice is asserted as an absence), but that DoD item was satisfied by the wrong artifact** —
**and the DoD line was mine.**

### ⚖️ Ruling 1 — the two orphaned kinds stay, and I checked before ruling
`outbox.service.ts:78` renders `row.payload` **at SEND time** ⇒ **a row already queued with `sick_leave` or
`leave_teacher` still needs its branch.** 🔑 **Dead as PRODUCERS, live as CONSUMERS until the queue drains.**
✅ **Keep them; keep the "re-wiring fails loudly" assertions. Removable after a deploy plus a drain, not before.**
⚠️ **Constraint recorded for `REQ-086`: a template kind nothing sends must not appear as an editable row.**

### ⚖️ Ruling 2 — a stale comment of his own says a decision is still mine when it is made. **Folded into TASK-307, not a task.**

### ➡️ TASK-307 — `§6` only
⏸️ **`§5`'s copy is with the CUSTOMER** — *"No engineer may implement this text."* ⇒ **not cut, and TASK-307
asserts `§5`'s prompt is BYTE-IDENTICAL, because a diff there is invisible to us and visible to them.**
🔑 **The owner's reason IS the requirement:** *an account that exists and cannot be used*, with no way for the
parent to know why. ✅ **Skipping a LATER child stays legitimate, asserted** — else "no skip" becomes "no skip
ever". ⚠️ **The re-ask wording is not an engineer's to invent: an existing string, or he stops and I get copy.**
❓ **Its Question may become a customer question: a skip is not the only road to an empty account** — abandonment
mid-flow, or a child DELETED later. 🔑 **"No skip" stops an account being CREATED empty and does nothing about it
BECOMING empty.**

**BALL: @Jason — TASK-307.**

---

## Jason (BE) — ✅ TASK-307 DONE (code). → @Sober **1852 pass / 0 fail**, 148 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**.

- [x] 🔑 **A parent with NO children cannot skip** — **and the two moments ARE one branch**; see below
- [x] 🔑 **A parent WITH a child can still skip** — asserted, which is what keeps the fix narrow
- [x] **The reply is an EXISTING string** — `add_student_name_prompt`, the prompt that asked for the name in the
      first place. 🚫 **No new copy, no new i18n key** — asserted as an absence
- [x] **The reply is BILINGUAL** — `both()`, and the underlying string genuinely differs by language
- [x] 🔑 **Break it and watch** — restored, suite green before this number
- [x] 🚫 `§5`'s role prompt and its three buttons **byte-identical**, `SKIP_WORDS`/`CMD_SKIP` untouched
- [x] ✅ **Ruling 2 — my stale comment at `scheduler.service.ts:2925` is corrected** (it said the
      `notify_on_leave` decision was pending; it was made the same day)

New: `src/lib/no-skip-first-child.test.ts` (9 tests).

### §1 ✅ They are ONE branch, and you were right to expect it
The owner named two moments — *"จังหวะเพิ่มลูกคนแรก และ แรกเริ่มที่ไม่มีลูก"* — and **the step is identical for
the first child and the fifth.** ⇒ the question that decides it is not WHICH STEP but **whether this parent has
a child at all**, so both reduce to `kids.length` and there is one branch rather than two.
📌 The re-ask does **not** clear the session: the flow stays where it was and asks again. Asserted by ORDER —
the early return precedes `clearSession`, so the no-child path cannot reach it.

### §3 ✅ An existing string, and I did not have to stop
`both((l) => withExit(t("add_student_name_prompt", l, { max: MAX_STUDENTS_PER_PARENT }), l))` — **the same
string, wrapped the same way, that the flow already uses to enter that step.** Re-ask, not explain.
⚠️ **One thing worth your eye:** the ORIGINAL prompt is single-language (`t(…, lang)`); my re-ask is bilingual
because §3 required it. **So the same sentence is now bilingual on the re-ask and single-language on the first
ask.** I did not change the original — §4 protects the registration steps — but **it is an inconsistency a
parent could see**, and it is one line if you want it.

### 🔴 The Question — **four roads, and the widest one is not a skip at all**
| road | reaches a usable-looking empty account? |
|---|---|
| 🔴 **Abandoning registration mid-way** | **YES — and this is wider than the one the owner named.** `ensureParentByPhone` (`parent.service.ts:66`) creates the `parents` row **at LINK time, before any child**. A parent who links and then simply stops replying leaves exactly the dead end. **They never type a skip word, so *"no skip"* does not touch it.** |
| 🔴 **Blocking and re-adding the bot** | **YES** — the link persists (`family_line_links` / `parents.line_user_id`), so re-adding lands on the same empty parent. It is road 1 with a different exit. |
| ⚪ **An admin creating a parent** (`POST /parents`) | **YES, deliberately** — an admin may register a household before its children. Not a defect. |
| ✅ **A child DELETED later** | **NO — and this is the good news.** I looked for a student delete/archive and **there is none**: no `DELETE /students`, no archive flag on the row. ⇒ **an account cannot BECOME empty.** |

🔑 **So your sharpest sub-question has a clean answer: *"no skip"* prevents the account being CREATED empty, and
nothing can make it become empty afterwards.** The gap is that it is not the only way to *leave it* empty —
**abandonment is, and it is silent by construction: nobody types anything.**
📌 **The owner's reason applies to road 1 exactly as much** — *"an account that exists and cannot be used"* —
but the remedy is a different shape: not a refused word, something that notices an account has sat childless.
🚫 **Named, not built, and I would not guess at what that something is.**

**BALL: @Sober — TASK-307 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ✅ **`§6` PASSED.** 🔻 **My §3 instruction was wrong and one line goes back.** 🔴 **The widest road to an empty account is abandonment — nobody types anything.** 🎉 **`REQ-085` is down to `§5`.**

**Reproduced:** `bun test` **1852 pass / 0 fail** (5183 expects, 148 files) · **35 = 35** ·
`bunx --package typescript@5.6.3 tsc --noEmit` → **0**.

### ✅ The owner's two moments were ONE branch
*"จังหวะเพิ่มลูกคนแรก และ แรกเริ่มที่ไม่มีลูก"* — **the step is identical for the first child and the fifth** ⇒
**the deciding question is not WHICH step but whether this parent has a child at all.** ✅ **One branch on
`kids.length`.**
✅ **No new copy** — the re-ask reuses the prompt that asked for the name. **Re-ask, not explain.**
🔑 **And the session-clear is asserted BY ORDER** — the early return precedes `clearSession`, so the no-child path
cannot reach it. 📌 *Asserting a property by the order of two statements is cheaper and harder to break than
asserting the state after.*

### 🔻 The inconsistency is MINE
**`add_student_name_prompt` is called FIVE times: four `t(…, lang)`, and his re-ask the only `both()`.** 🔴 **The
same sentence would render in two forms depending on how a parent arrived at it.**
⚠️ **Cause: my §3 said *"keep it bilingual — this is a conversation"* without checking what this flow does.**
🔑 **`REQ-079 §18` is satisfied here a different way — the flow knows the session's `lang` and answers IN it (39
`t(…, lang)` vs 13 `both()`).** ⇒ **`both()` is for a reader whose language is unknown; inside a session it is
known.** ⇒ ✅ **one line back, and the assertion I caused is DELETED rather than left red.**
📌 **Second time today an instruction of mine was refuted by the code it was about** *(the first: the DTO
formatting task `contract.ts` forbade)*. 🔑 ***An instruction about a convention must be checked against the
convention*** — and **he caught it by not assuming I was right.**

### 🔴 His Question — four roads, and the widest is not a skip
🔴 **Abandonment:** `ensureParentByPhone` creates the `parents` row **at LINK time, before any child** ⇒ **a parent
who links and stops replying has exactly the dead end the owner is worried about, and `§6` cannot touch it
because nobody types anything.** ⚠️ **Silent by construction.**
✅ **The good half, checked rather than assumed: a child cannot be DELETED** — no delete route, no archive flag ⇒
**an account cannot BECOME empty.** 🔑 **So `§6` is complete for its own shape.**
📌 **Routed to @Porter in his framing, which is what makes it answerable:** ***the remedy is not a refused word —
it is something that notices an account has sat childless.*** 🚫 **Nothing cut; how long is "sat" and who is told
are the owner's.**
⚪ **An admin creating a parent before its children is deliberate.**

### 🎉 `REQ-085` state
✅ **`§1` · `§2` · `§3` · `§4` · `§6` · `§7.1`–`§7.4` · `§10` · `§11.2`/`§11.3` — all closed and reviewed.**
⏸️ **`§5` is the only item left, and it is @Porter's: the admin phrase and the parent-certainty wording, with the
customer.** 🚫 **No migration in ANY of it.**
📋 **Mine next: the `REQ-086` design pass** — 🔑 **and it is genuinely shapeable now: *"make these four editable"*
rather than *"design a message editor"*.**

**BALL: @Jason — one line of mine to undo. Then `§5` and me.**

---

## Jason (BE) — ✅ TASK-307 §3 correction applied. → @Sober **1852 pass / 0 fail**, 148 files · tsc **0** (`--package typescript@5.6.3`).

✅ **The re-ask now renders in the SESSION'S language**, not `both()`: `withExit(t("add_student_name_prompt", lang, …), lang)` — **the identical expression the first ask uses.**
🔑 **Your reading of the flow is right and mine was not.** `REQ-079 §18` is satisfied here a different way — this flow KNOWS the session's `lang` and answers in it (39 `t(…, lang)` against 13 `both()`). `both()` is for a reader whose language is unknown; **inside a session it is known.** ⇒ my version made the re-ask bilingual while the first ask was not, **which is the inconsistency I had flagged for you — caused by the fix, not found by it.**

🔻 **The bilingual assertion is REPLACED, not left red** — it was asserting the mistake. In its place: the re-ask contains no `both(`, renders `t(…, lang)`, and **the same expression appears more than once in the file**, so the two asks cannot drift into two wordings of one question.
⚠️ **Scoped to the NO-CHILD branch only:** the later-child reply below it is `both((l) => skip_done …)` and always was — §4 protects other steps' skip behaviour, so only the re-ask moved. **The first attempt at this assertion was too broad and caught that line; narrowed rather than loosened.**

**BALL: @Sober — clear on my side.**

## [--:--] Sober (SA) — ✅ **`§6`'s one line is in; `REQ-085` is engineering-complete but `§5`.** 📐 **`SPEC-078` written: the customer edits the WORDS, not the messages.**

**Verified:** all five `add_student_name_prompt` call sites identical · **1852 pass / 0 fail** (148 files) ·
`tsc` 0 · 🚫 no migration.
📌 **He NARROWED his own assertion rather than loosening it when the first version caught the later-child line.**
🔑 *Narrow rather than loosen is the harder choice and the one that keeps a test meaningful.*

### 📐 The design pass — and it fell out of the four formats we just built
🔑 **Those messages are NOT free text with placeholders.** In code they already are **a title + an ORDERED field
list**, each field carrying **a label, a value source and an EMPTY RULE**, with `TYPE_OMITS` / `AUDIENCE_OMITS`
deciding presence.
⇒ **The customer does not need a template. They need the WORDS: the title per `TemplateKey`, the label per
`FieldKey`.**
✅ **This answers all four of @Porter's refusals STRUCTURALLY:** **no placeholders to mistype** (labels do not name
values — fields do, and fields are ours) · **the preview is the REAL builder** · **`TemplateKey` is untouchable**
· **the preview calls the same function that sends.**
🔑 ***The difference between "we will be careful with the text box" and "there is no text box".***

### 🔴 The three decisions inside the spec
1. **Labels are GLOBAL per field.** `Remark` is deliberately one word across all four — **per-message labels would
   hand the customer this week's drift class as a feature.**
2. **The default is NEVER copied** — store only overrides; `Reset` deletes the row ⇒ **"back to default" is the
   ABSENCE of an edit.** 🔑 *Which satisfies Q3 without a second copy of the sentence rotting somewhere.*
3. **Rows keyed on `TemplateKey`, and the list must contain templates that are ENQUEUED, not ones that RENDER** —
   ⚠️ **`sick_leave` and `leave_teacher` still render for queued rows; an editor built from the renderer list
   would offer two messages the product no longer sends.**

### ⚠️ The honest cost, stated to @Porter rather than softened
🔴 **`REQ-086` needs the batch's FIRST migration.** **Everything in `REQ-085` shipped `35 = 35`; this will not.**
🚫 **Scope (a), the conversation messages, is deliberately NOT in the spec** — **genuinely free text, bilingual,
and its entry copy is with the customer right now.** 🔑 **An editor for a sentence they are still writing would be
its third version this week.**

### ❓ Two blocking questions to @Porter
**Which templates does the customer see** *(the four are theirs; there are others — "all of them" is a bigger
promise than `REQ-086` made)*, **and may they edit a TITLE that two messages SHARE** *(either we split the keys
deliberately, or one edit moves both and the UI must say so)*.
📌 **The shared-title finding arrives as a DESIGN DECISION now rather than the curiosity it was two tasks ago.**

**BALL: @Porter — `§5`'s copy, and `SPEC-078`'s two questions. Nothing is on @Jason.**

## [--:--] Porter (PM) — 🧹 Board hygiene: **48.8KB → 25.9KB.** 11 oversized cells trimmed to pointers; `## Project info` moved VERBATIM to `SYSTEM-FACTS`.
🚫 **Nothing deleted.** **Every trimmed cell's detail is in its TASK file, where @Sober writes it anyway.**
📌 **Second time I have moved a standing block off the board this week** (deploy rules on 09-08, project info
today) ⇒ **the board grows because DONE rows keep their evidence; the fix is a pointer, not a shorter report.**

## [--:--] Porter (PM) — ✅ `SPEC-078`'s three questions ANSWERED, and 🔴 I corrected a stale row: **`§5` is NOT with me.**
🔴 **`§5` was settled by the CUSTOMER** — `REQ-079 §17c` screen 2 IS the entry message (*"กรุณาพิมพ์ Next"*), the
role words are theirs (`ครู` · `แอดมิน`, CEO skipped), and `§17f` is my heading ruling. ⇒ **nothing waits on my
copy; `REQ-085` is buildable in full.** 🔻 **Same shape as `TASK-291`'s stale row — a stale line on the SA's
side becomes work nobody needed to wait for.**
✅ **Q1 — WHICH templates: THE FOUR `§7` FORMATS, named.** 🚫 **not `course_deduction`, not the reschedule/pause
notices.** 🔑 **He was right that "all of them" is a bigger promise than `REQ-086` made — and the promise is
MINE to size.** 📌 **The UI must NAME the four**: *"these four messages"* is a promise we keep; *"your
messages"* is one we break the first time they open the fifth.
✅ **Q3 — the shared title: SPLIT into two keys, identical defaults.** **`7.1` fires once per course, `7.3` per
session** ⇒ **they only HAPPEN to match today.** 🔑 **Splitting now changes nothing visible, removes the trap,
and is free — nothing is stored yet, so there is no override to migrate.** 🚫 **One key plus a warning makes the
UI explain a limitation we chose.**
✅ **Q2 — ONE global label screen, separate from titles.** 🔑 **Labels ARE global, so make it STRUCTURAL rather
than warn about it** — **a layout that has to be corrected by a warning is the wrong layout.**
🎯 **And his spec beat my brief: I gave four refusals and asked him to honour them; he made three IMPOSSIBLE.**
**No placeholders to mistype (labels do not name values) · the preview IS the real builder · `TemplateKey`
untouchable.** ⇒ ***"there is no text box" beats "we will be careful with the text box"***.
✅ **His override-only storage answers my own Q3 better than I did: "back to default" is the ABSENCE of a row.**
⚠️ **Accepted the honest cost as stated: `REQ-086` is the batch's FIRST MIGRATION** — **I would rather have that
sentence than a reassuring estimate, and it is what I will tell the owner.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid` (12 tasks, no migration). **@Tanya dispatched on the SCREENS only — and I said why the other half is invisible to her.**
🔑 **Four of the twelve change what a LINE MESSAGE says** (`284 · 303 · 304 · 305`) ⛔ **and LINE is out of scope
for QA on every box — the owner is the only person who can read them.** ⇒ **told her not to try AND not to
report their absence as a gap.** 🔻 **Half a batch she cannot see is exactly the shape that produced a wrong
`NOT_TESTED` before; naming it up front costs one paragraph.**
**Her five, ordered by what hurts a family most:** the stretched course's quota leave (**the owner's own bug —
a card promising `Leave 0/1` on a course that could not take it**) · the re-planned course's quota · `TASK-300`
· the expiry warning BEFORE the save, **naming the sessions, not counting them** · no skip at the empty start
and the first child (**a LATER child may still be skipped — told her not to file it**).
⚠️ **On `TASK-300` I told her the awkward truth rather than the tidy one: the OWNER tried twice and could not
reproduce it, and the team then PROVED it with a test and fixed it.** ⇒ **read the TASK for the conditions
before building a fixture.** 🔻 **I reported it to him as "not reproduced". It was real. That correction is
mine to carry, and I would rather she starts from the true story than from my earlier one.**

## [--:--] Porter (PM) — 🔴 **`TASK-301` NOT FIXED — the owner still cannot take the quota leave. The REFUSAL moved; the OUTCOME did not.**
**`มิลล่า`, `Leave 0/1`, `Ends 27 Oct 26`:** `คอร์สขยายเกินสัปดาห์ที่ 5 ไม่ได้` → **`คอร์สขยายเกินวันสิ้นสุดของ
คอร์ส (2026-10-27) ไม่ได้`.**
🟢 **Real progress: the check no longer uses a week number the course had outgrown.** 🔴 **But the admin still
cannot take the leave and the card still promises `Leave 0/1`.** ⇒ **the requirement was never "a more accurate
refusal"; it was "the family can use the leave it has".**
🔑 **Remaining cause: that course spent its ENTIRE stretch at creation, so the plan already runs to its own end
date** ⇒ **a quota leave now needs the END DATE to grow, and it does not.**
🔗 **The owner has ruled this shape THREE times** — *"วันหมดอายุก็งอกไปสิ"* (resume) · `TASK-282`'s derived
expiry · `§10`'s stretching ceiling — **and `§10` states it in one line: *the plan decides the dates; the dates
do not veto the plan*.** ⇒ **asked @Sober whether a POST-creation quota leave moves the end date too, and put
the alternative to the owner: if it must not, the card has to stop promising a leave that cannot be taken.**
🔻 **My omission, named: `TASK-301`'s DoD did not carry the owner's own reproduction — the exact course, the
exact click.** **I relayed his screenshot as a DESCRIPTION instead of as an ACCEPTANCE TEST**, and twice now
this has been reported fixed while he found it standing. ✅ **From here: when he reports with a screenshot, the
screenshot IS the DoD.**

## [--:--] Porter (PM) — 🔴🔴 **`§11`'s table was MY MISREADING. There is only ONE limit on leave: the QUOTA. `TASK-301`/`302` were aimed at the wrong thing — by me.**
**He said it plainly:** *"quota ลา มี แต่การยืดเวลาไม่มี quota … ถ้าเขาจะลา ต้องได้ เพราะเขามี quota ลา ส่วน
วันหมดอายุ ก็ให้ยืดตามไปเลย"*.
🔻 **My error, precisely: his sentence *"กฏ มีไว้คุมโควตาลา หลังจากสร้างเท่านั้น"* names ONE rule — the quota.
I read it as describing TWO, and wrote a table saying the week ceiling also applies and "refuses".**
⇒ **so `TASK-301` and `TASK-302` were both built to make the ceiling MORE ACCURATE, when it should not refuse at
all.** 🔑 **That is why the "better message" was not progress: an accurate refusal is still a refusal.**
✅ **Corrected as `§12`: if the family still has quota, the leave goes through and the DATES MOVE. No second
gate.** 📌 **It is `§10`'s principle with my exception deleted — at creation AND after it, identically.**
🆕 **`§12.1`: the expiry control must be ON THE CARD** — the `expires …` date top-right, clickable. **The
capability exists (`REQ-082`); he is telling us WHERE it must be reachable from.**
🔻 **Fourth time TODAY I read a structure into his prose that was not there** — the `HH:mm:ss` prefill · the
address as three fields · the "admin override" · now a second gate on leave. 🔑 **All four FITTED, and fitting
is not evidence.** ⚠️ **This one cost two engineers' tasks, so I told @Sober to RE-CUT from `§12` rather than
patch what exists** — **patching a task built on a wrong premise keeps the premise.**

## [--:--] Sober (SA) — 🔴🔴 **`REQ-085 §12`: the QUOTA is the ONLY gate on leave. Three of my tasks enforced a limit no requirement ever asked for.** ➡️ **TASK-308 re-cuts it.**

### 🔻 The correction, and my half is the larger half
**@Porter withdrew `§11`'s two-rule table as his own misreading of one sentence.** 🔴 **I RATIFIED it — and worse,
when he asked *"if the ceiling always stretches, what still bounds a course?"* I ANSWERED, confidently:
*"it refuses AUTOMATIC growth and yields to a DELIBERATE act."*** ⇒ **I invented a job for the ceiling so that his
table would be coherent.**
🔑 **The real answer was there all along: leave is finite because the QUOTA is finite.** `SPEC-028 §5 #2` feared a
leave extending a course indefinitely — **it cannot; at most `quota` make-ups.** ⇒ **the ceiling was a second
answer to a question that already had one.**
📌 ***A PM's misreading costs a message. An SA's ratification costs a sprint.*** **TASK-299, TASK-301 and TASK-302
were all shaped by it.**
⚠️ **The check I skipped: *"which REQ or ruling says this limit exists?"*** — **and the ceiling carried a CITATION,
which is what made it look sourced.** 🔑 **A limit whose citation does not support it is harder to find than one
with no comment at all.**

### 🔴 The live defect it left standing
**`มิลล่า` on `sid`: the refusal moved from `สัปดาห์ที่ 5` to `วันสิ้นสุดของคอร์ส (2026-10-27)` and the owner still
cannot take his leave.** 🔑 **An accurate refusal is still a refusal** — **TASK-301 improved the sentence and not
the outcome.**

### ✅ TASK-308 — four moves
**(a) delete the refusal · (b) the expiry GROWS instead, through the path that already records an expiry change ·
(c) 🔻 REVERT TASK-301's pre-allocated quota week · (d) the quota gate stays and must still work.**
🔑 **(c)'s reason: with nothing left to refuse there is nothing to leave room FOR, and a pre-allocated week makes
the card's `expires` claim time the family has not used.** ⇒ **stretch on demand is simpler and more honest.**
⚠️ **§4 may find `exceedsExtensionCeiling` has NO live caller** ⇒ **delete it rather than keep a gate that cannot
fire** *(the `EXPIRY_REQUIRED` shape, removed in TASK-287)*. 📌 **And an FE consequence NAMED not cut: the preview's
`exceedsCeiling` disables `Create plan`; if it can never be true that gate is dead FE code — @Fern's, once @Jason
says whether the field survives.**
✅ **Its FIRST DoD line is the owner's own reproduction, asserting BOTH the leave succeeding AND the expiry
moving.** 🔑 **@Porter's rule, adopted: *when he reports something with a screenshot, the screenshot IS the DoD.***
**TASK-301 lacked it because I relayed a description instead of a test.**

### ✅ Two things I had wrong that are now unblocked
🔻 **`§5` was NEVER with @Porter** — the customer's entry copy has been in `REQ-079 §17c` all along
(*"กรุณาพิมพ์ Next…"*), `ครูเอง`/`แอดมินเอง` are withdrawn, `CEO` skipped, and `§17f` says **bodies only, no
headings.** 📌 **Second stale row I handed him today.** 🔑 **The pattern is mine: I keep a state table in my message
and the FILES move underneath it** ⇒ **re-read `REQ-079` before asserting `§5`'s state again.**
✅ **`SPEC-078`'s three questions answered, and two of his answers beat my questions:** **FOUR templates, NAMED in
the UI** — *"'these four messages' is a promise we keep; 'your messages' is one we break the first time they open
the fifth"* · **SPLIT the shared title now, identical defaults** — *"they ARE different messages; today they
happen to match"*, and cheapest while nothing is stored · **one global label screen** — 🔑 ***"a layout that has to
be corrected by a warning is the wrong layout."***

**BALL: @Jason — TASK-308. Then `§5`, `§12.1` (@Fern) and `SPEC-078`'s first task.**

---

## Jason (BE) — ✅ TASK-308 DONE (code). The quota is the only gate. → @Sober **1854 pass / 0 fail**, 148 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**.

- [x] 🔑 **`มิลล่า`'s exact case: the admin CAN take the leave, AND the expiry MOVES** — both asserted
- [x] 🔑 **A course with no quota left is still REFUSED** — the gate that remains
- [x] **`EXTENSION_CEILING` never fires on a leave path** — asserted as an absence **across the whole service**
- [x] **The expiry change is RECORDED** through `recordExpiryChange`, the same writer an admin edit uses
- [x] **§4 answered** — the predicate is **NOT dead**; its one live caller is named below
- [x] **TASK-301's quota term reverted**, and **`courseBornCeiling` still covers the declared absences** (§10)
- [x] 🔑 **Break it and watch** — restored, suite green before this number

### §3 (a)+(b) ✅ The refusal became a stretch
The append loop collects the furthest date it reaches and, **once**, grows the expiry to it — so a course that
earns three make-ups records **one** expiry change rather than three. Written through `recordExpiryChange` with
a **null actor**, which is accurate: the system moved it, not a person. 🚫 No second way to move an expiry.

### §3 (c) 🔻 Reverted — and I reverted TASK-302's term as well
`courseBornCeiling` is back to three parameters. ⚠️ **I also reverted TASK-302's remaining-quota term in
`replanExpiry`**, which the task did not name. **The argument is identical** — it existed so a resumed course
would have room for a leave the ceiling would otherwise refuse — and leaving it would pre-allocate on the
**re-plan path only**, which is the inconsistency this task exists to end. `replan-quota-room.test.ts` is
rewritten as the record rather than deleted. **If you want it kept, it is one line back.**

### §4 🔴 The predicate is NOT dead — and the answer is more interesting than "delete it"
**`exceedsExtensionCeiling` has exactly one live caller: the creation preview's `exceedsCeiling` (`:2059`).**
⚠️ And it is **reachable**: the preview projects make-ups at a weekly cadence while `findFreeExtensionDate`
**searches**, so a taken slot can still push one past the projection. **It is not a gate that can never fire.**
🚫 Left in place — §6 says the FE reads that field and I may not change the DTO here.
📌 **But under §12's own principle that remaining creation-time refusal is questionable too:** *the plan decides
the dates; the dates do not veto the plan.* **Your call, not mine, and not in this task.**

### 🔴 Two things that were only dead once (a) landed — both removed, both named
1. **The `CANCEL_AT_CEILING` re-map.** It caught `EXTENSION_CEILING` on the cancel path to reword it. With
   nothing left to throw it, **it became a handler for an exception that cannot arrive** — §4's own rule, and
   `EXPIRY_REQUIRED`'s exact shape. Removed. ✅ **The reconcile still runs on a cancel**, so a cancel is still a
   reschedule and not a forfeit (SPEC-028 §11.3); only the refusal it could raise is gone.
2. **A comment at the creation site** saying this is *"where the MAX_WEEK ceiling is enforced"* and that a
   create is refused past it. **It is not, and no longer can be.** Corrected rather than left to mislead.

### ✅ Break it and watch
Restoring the refusal fails **seven** assertions — including *"a 4-session course, 3 declared absences: the
leave GOES THROUGH"* and *"the expiry MOVES to cover it"*. 🔑 **The owner's own reproduction is the one that
goes red**, which is what @Porter asked for: *the screenshot IS the DoD.*

### 🔑 The Question — **one limit with no authority at all, and one that just changed shape**
🔴 **`MAX_STUDENTS_PER_PARENT = 5`** (`parent.service.ts:13`, refuses at `:119`). Its comment reads
*"Business rule: a single phone may register at most 5 students"* — **and cites nothing.** No REQ, no SPEC, no
TASK, no owner ruling. ⚠️ The customer's own copy hardcodes 5 (`line-i18n.ts:142`), **but I cannot tell whether
the copy is the source or an echo of the code** — and that not being answerable from the repo is the finding.
📌 **It is the easier kind to catch:** the ceiling looked sourced, this one looks like a decision nobody wrote
down.

⚠️ **`MAX_EXTENSION_WEEKS_SCANNED = 26`** (`extension-slot.ts:7`) — **not a refusal, and that is newly the
problem.** `firstFreeWeeklySlot` scans 26 weeks and, finding nothing, **returns the last candidate anyway**; its
comment says the caller's ceiling is what refuses it — *"this function never silently invents a valid-looking
date."* 🔴 **That caller was the line I just deleted.** ⇒ on a slot booked solid for 26 weeks, a make-up now
lands half a year out and the expiry stretches to meet it, **silently.** Unlikely, and no longer impossible.
🚫 **Not changed** — it follows from §12 and the remedy is a decision, not a patch.

✅ **Traceable, checked:** `SLOT_TAKEN` · `SESSION_DELIVERED` (SPEC-028 §5) · `VOUCHER_PROGRAM_EXCLUDED`
(SPEC-030) · `TEACHER_CHANGE_TOO_LATE` (SPEC-029, and configurable) · `LEAVE_NOTICE_TOO_LATE` (SPEC-048) ·
`NO_OWED_SESSION` (SPEC-028 §2 / REQ-064) · `LEAVE_LOCKED` (the quota) · the pause/drop/end state guards.

🔑 **And the sharper form of your point:** the ceiling's citation was real — `SPEC-028 §5 #2` exists and says
what it says. **What it did not do was require a refusal**; it named a fear (*"a leave could extend a course
indefinitely"*) that the quota already prevented. ⇒ **the test that finds these is not *"does it cite
something?"* but *"does the cited source ask for a REFUSAL, or only name a worry?"*** The first is a grep; the
second is a read, and it is the one that would have caught this in TASK-093.

### §6 📌 For @Fern, when you route it
`exceedsCeiling` **survives on the DTO** — same shape, same field. 🚫 I changed nothing about the contract.

**BALL: @Sober — TASK-308 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — 🎉 **`§12` DONE: the owner can take his leave and the expiry moves.** 🔑 **He refused to call a predicate dead when I said it probably was — and was right.** ➡️ **TASK-309.**

**Reproduced:** `bun test` **1854 pass / 0 fail** (5173 expects, 148 files) · **35 = 35** · `tsc` 0 ·
**`EXTENSION_CEILING` gone from every live path.**
✅ **`มิลล่า`'s case is the FIRST assertion and covers BOTH halves** — the leave goes through **and** the expiry
moves. 🔑 **@Porter's rule worked on first use: restoring the refusal turns the owner's own reproduction red.**

### 🔑 §4 — my instruction was *"say what you find rather than assuming my read"*, and he took it literally
> *"It IS reachable: the preview projects at a weekly cadence while `findFreeExtensionDate` SEARCHES, so a taken
> slot can push one past the projection. It is not a gate that can never fire."*

✅ **My read was wrong. Second time today.** 🔑 **And he asked the better question rather than stopping:** *"under
§12's own principle that remaining creation-time refusal is questionable too."*
⇒ **RULING: it is. `§12` says the expiry stretches at creation AND after it identically** ⇒ **a plan refused
because a PROJECTED make-up exceeds a boundary computed from the same projection is `§12`'s defect in its last
costume.** 📌 **And it is the owner's own screen: *"reduce the planned absences or pick a different start date"* —
he was told to change what he wanted because a date could not move.**

### ✅ Scope he corrected on his own
**He reverted TASK-302's remaining-quota term too — not in my task.** *"Leaving it would pre-allocate on the
re-plan path only, which is the inconsistency this task exists to end."* 🔑 **Right, and I under-specified: I named
one term and forgot its twin.**
✅ **Two things only DEAD once the refusal went, both found and removed:** the `CANCEL_AT_CEILING` re-map
(*"a handler for an exception that cannot arrive"*) — **with the property that mattered kept: the reconcile still
runs on a cancel, so a cancel is still a reschedule and not a forfeit** — and a comment claiming the ceiling was
enforced at creation.
✅ **One expiry change for three make-ups, NULL actor** — *the system moved it, not a person.*

### 🔴 TASK-309 — the last refusal, and a silence `§12` created
**§1:** the creation preview's refusal goes; `exceedsCeiling` becomes always-false but **stays on the DTO** — the
FE reads it, and a removed field is a contract change. **Two repos, one order.**
**§3:** 🔴 **`MAX_EXTENSION_WEEKS_SCANNED = 26` — the search gives up and RETURNS THE LAST CANDIDATE ANYWAY, and
its comment says the caller's ceiling refuses it: the line we just deleted.** ⇒ **a make-up can land six months
out and the expiry stretches to meet it, silently.**
✅ **Ruled from his own rules rather than a new one: must NOT refuse (`§12`), must not be SILENT** ⇒ **the leave
succeeds and the ADMIN is told.** 🔑 ***A refusal is the owner's to grant; a warning is ours to owe.***
❓ **The threshold is the owner's — `26` was a scan limit, not a promise.**

### 🔑 His Question answer replaces mine
> *"The test is not 'does it cite something?' but 'does the cited source ask for a REFUSAL, or only name a
> worry?' The first is a grep; the second is a read."*

**`SPEC-028 §5 #2` was a REAL citation that named a FEAR the quota already prevented and never asked for a gate.**
⇒ **that is why three of my tasks defended it.** 📌 **`SYSTEM-FACTS`, in his words.**
🔴 **And he found one limit citing NOTHING: `MAX_STUDENTS_PER_PARENT = 5`** — with the customer's copy hardcoding
the same 5 and **no way to tell from the repo which came first.** ⇒ **routed to the owner via @Porter as two
questions.** ⚠️ **A family with six children would meet a wall nobody remembers building.**

**BALL: @Jason — TASK-309. Then `§5`, `§12.1` (@Fern), `SPEC-078`'s first task.**

---

## Jason (BE) — ✅ TASK-309 DONE (code) — and a correction to my own TASK-308 answer. → @Sober **1863 pass / 0 fail**, 149 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**.

- [x] 🔑 **The owner's `Create plan` case previews without refusal**, and the boundary covers the plan it describes
- [x] 🔑 **`exceedsCeiling` cannot be true** — asserted on the shape that makes it so — **and the field still exists**
- [x] 🔑 **A make-up pushed far out: the leave SUCCEEDS and the admin is TOLD** — both halves
- [x] **A normal make-up warns NOBODY** — including one that skipped a few busy weeks
- [x] **`extension-slot.ts`'s comment corrected**
- [x] 🔑 **Break it and watch** — both halves; restored, suite green before this number

New: `src/lib/makeup-far-out.test.ts` (9 tests), `searchExhausted` / `weeksBetween`, `makeup_far_out`.

### §2 ✅ And the answer to your design point is **yes — and it costs nothing**
You asked whether making `exceedsCeiling` false means the born ceiling must widen to the **searched** dates
rather than the projected ones. **It does, and the preview can do it without becoming the save: it has already
run the search** — it places the make-ups itself. So the boundary is now `max(bornCeiling, furthest session it
laid out)`, and nothing in that array can exceed a maximum taken over it.
🔑 **Preview and save still agree, from the other side:** TASK-308 grows the stored expiry to cover the make-ups
the reconcile appends, so both land on the finished course's own last date. **No second search anywhere.**
🚫 Left as the computation rather than a literal `false`, so the day someone narrows the ceiling again it starts
telling the truth instead of lying quietly.

### §3 ✅ Not refused, not silent — and the trigger is the only line nobody invented
`searchExhausted(fromDate, landed)` tells a found slot from a surrendered one by the one signal there is: the
distance. A success lands at or before `from + 26` weeks; exhaustion lands one week beyond.
⚠️ **I warn on EXHAUSTION, not on a distance I picked** — a threshold of my own choosing would be the same
mistake as the ceiling. 📌 The alert carries **how far** (`weeks`, `replaces`, `landedOn`), so @Porter can take a
smaller number to the owner and this becomes one constant.
🚫 The admin, not the parent: *they asked for a leave and got one; the date is our problem, not theirs.*
⚠️ **The wording is mine** — an admin-facing operational alert in the admin-alert convention (Thai, like
`ob_leave_admin` beside it), not one of the customer's §7 templates. **It states the fact and decides nothing.**
If @Porter wants different words it is one key.

---

## 🔴 The Question — and applying my own test EXONERATES the ceiling. I was wrong in TASK-308.

I picked the two closest in shape to the ceiling: both **notice-based refusals**.

**✅ `TEACHER_CHANGE_TOO_LATE` — passes cleanly.** `SPEC-028 §5 #3` does not name a worry; it specifies the
mechanism: *"a named `Record<…, days>` + accessor + pure `hasEnoughTeacherChangeNotice(…)` + message helper +
`conflict("TEACHER_CHANGE_TOO_LATE", …)` at the call site with admin `override`. **The number is DECIDED: 3 days
(owner)**"*. **The source asks for the refusal, names the code, and attributes the number.** Nothing to report.

**⚠️ `LEAVE_NOTICE_TOO_LATE` — I cannot close it.** `SPEC-048`'s own source is `REQ-047`, and what it asks for is
that the values become **EDITABLE**: *"The owner wants those values to be editable settings."* It **inherits**
the refusal and never asks for one. The refusal's authority is cited as **`UC-029`** — 🔴 **and `UC-029` does not
exist anywhere in this workspace.** The only file that mentions it is SPEC-048, referring to it.
⇒ **Its authority is a document I cannot read.** 📌 And it matters today: **`§12` says the QUOTA is the only
thing that may ever refuse a leave, and this refuses one.** It has an admin `override`, which softens it — but
a parent using LINE self-service has no override. 🚫 **Changed nothing.**

### 🔻 And the correction, which is the part I most want you to read
**My TASK-308 sentence — *"the ceiling was a limit no requirement ever asked for"* — is WRONG, and you are about
to put my formulation into `SYSTEM-FACTS`.**

Applying my own test to the ceiling, honestly, `SPEC-028 §5 #2` says:
> *"the reconcile's **append refuses** when the appended date would exceed `startDate + MAX_WEEK weeks`, with a
> reason (…). **Week-8 (size 6) is owner-confirmed** and load-bearing."*

⇒ **The source DID ask for a refusal, explicitly, and attributes the number to the owner.** The code comment I
read cited *"a leave could otherwise extend a course indefinitely"* — a fear — **but the SPEC one paragraph up
specifies the gate itself.** I judged the citation by the comment quoting it, not by the source.

🔑 **So the lesson is not the one I gave you.** The ceiling was **a genuine, owner-confirmed requirement that the
owner has now reversed** (`§12`). Nothing was invented. ⚠️ **The failure was not untraceable authority — it was
that three tasks treated a superseded rule as current**, and none of us noticed the owner had said something
that contradicted it.
📌 **My test still stands and I would still use it** — it passed `TEACHER_CHANGE_TOO_LATE` and caught
`LEAVE_NOTICE_TOO_LATE` on the same day. **But it does not explain the ceiling, and if it goes into
`SYSTEM-FACTS` attached to the ceiling it will teach the wrong thing.** ⇒ **the ceiling belongs under a
different heading: a rule can be correctly sourced, correctly built, and still be wrong because the owner
changed his mind — and the repo has no way to notice that.**

### §4 📌 For @Fern, when you cut it
`exceedsCeiling` **cannot be true and still exists on the DTO.** Same shape, same field.

**BALL: @Sober — TASK-309 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ✅ **TASK-309 DONE: no refusal left on any leave path.** 🔻 **And I CORRECTED `SYSTEM-FACTS` in two places — the ceiling was properly sourced and I said it was not.**

**Reproduced:** `bun test` **1863 pass / 0 fail** (5202 expects, 149 files) · **35 = 35** · `tsc` 0 ·
`searchExhausted` / `weeksBetween` / `makeup_far_out` present · `exceedsCeiling` still on the DTO.

### 🔻 The correction, and it was one hour from being permanent
**@Jason applied my own test to the ceiling and it EXONERATED it. I then read `SPEC-028 §5 #2` MYSELF** rather
than take either of our words — line 115:
> *"the reconcile's **append refuses** when the appended date would exceed `startDate + MAX_WEEK weeks`, with a
> reason. Week-8 (size 6) is **owner-confirmed** and load-bearing."*

🔑 **The SPEC specified the refusal, named its message, attributed its number to the owner. Nothing was invented.**
⚠️ **I judged it by the CODE COMMENT quoting the SPEC — which stated a FEAR — instead of the SPEC one paragraph
above.** ⇒ 🔴 **the failure was not untraceable authority; it was three tasks treating a SUPERSEDED rule as
current, and nothing making a new ruling collide with the shipped SPEC it contradicts.**
📌 **And `SPEC-028` line 102 saw all of it two months early:** ***"the ceiling and the quota already encode the same
limit: `MAX_WEEK = natural_end + leaveQuota` for every size."*** 🔴 **Nobody read past their own citation, me
first.**
✅ **Both wrong entries fixed. His test KEPT, attached to what it actually caught** — `TEACHER_CHANGE_TOO_LATE`
(passes) and `LEAVE_NOTICE_TOO_LATE` (cannot be closed) — **not to the ceiling.**
🔑 **His replacement heading is the one that stands: *a rule can be correctly sourced, correctly built,
owner-confirmed — and still be wrong because the owner changed his mind, and the repo has no way to notice a
reversal.***

### ✅ Two answers better than the task
1. **`exceedsCeiling` cannot be true rather than being set false** — the preview already runs the search, so the
   boundary is *"the furthest session it laid out"*. ✅ **Left as the COMPUTATION**, so *"the day someone narrows the
   ceiling again it starts telling the truth instead of lying quietly."* 🔑 **A field false by CONSTRUCTION beats a
   field assigned `false`.**
2. 🔑 **He refused to invent the threshold and found a signal nobody had to choose:** the search either FINDS a
   slot or SURRENDERS, and surrender is detectable by itself. ⇒ **`searchExhausted`, no number.** ✅ **And he still
   carried `weeks`/`replaces`/`landedOn` so a tighter number can become one constant later.**
   📌 **One of my two owner questions is WITHDRAWN** — the far-make-up threshold is a refinement, not a gap.

### 🔴 The new one for the owner, sharper than `MAX_STUDENTS_PER_PARENT`
**`LEAVE_NOTICE_TOO_LATE`:** `SPEC-048` inherits the refusal and never asks for one; **its authority is cited as
`UC-029`, and I verified five files mention it, all of them REFERRING to it — the document does not exist.**
🔴 **And it collides with `§12` today: the quota is meant to be the only thing that may refuse a leave.**
⚠️ **An admin has an `override`; a parent on LINE self-service does not.** ⇒ **a parent declaring a late leave is
refused by a rule whose source nobody can read.** 🚫 **Changed nothing.**

**BALL: @Porter — one question for the owner. `§5` is next and it is mine to cut.**

## [--:--] Porter (PM) — ✅ `TASK-309`: **no refusal left on any leave path** (1863/0, no migration). 🔻 **And @Sober corrected himself on the ceiling — it WAS properly sourced.**
🔑 **He told me an hour ago the ceiling was a limit nobody asked for. He then read `SPEC-028 §5 #2` himself
rather than trust either of us: the SPEC specified the refusal, named its message, and attributed the number to
the OWNER.** ⇒ **nothing was invented.** ⚠️ **He had judged it by a CODE COMMENT quoting the spec — which stated
a fear — instead of the spec one paragraph above.**
🔴 **So the real failure was not untraceable authority: THREE tasks treated a SUPERSEDED rule as current, and
nothing in the repo makes a new ruling COLLIDE with the shipped SPEC it contradicts.**
📌 **And `SPEC-028` line 102 saw all of it two months early:** *"the ceiling and the quota already encode the
same limit."* 🔑 **The redundancy `§12` removed was written down before any of us acted on it. Nobody read past
their own citation — me first.**
✅ **The six-month-silence question is WITHDRAWN without the number he was going to ask me for:** **@Jason found
a trigger nobody had to choose — the slot search either FINDS or SURRENDERS, and surrender detects itself.**
⇒ **the leave succeeds, the ADMIN is told, the alert says how far it landed.** 🔑 **A threshold that does not
have to be invented cannot be invented wrongly.**

## [--:--] Porter (PM) — 🔴 One question to the owner: **`LEAVE_NOTICE_TOO_LATE` vs `§12`.**
**`§12` says the QUOTA is the only thing that may refuse a leave. The late-notice cutoff refuses one.**
⚠️ **What makes it real rather than tidy: an ADMIN has an override; a PARENT using LINE self-service does not.**
⇒ **a parent who declares leave too close to the class is refused by a rule whose source document (`UC-029`)
does not exist in this workspace — five files cite it, none contains it.**
🚫 **Nothing is broken today** — it has presumably refused correctly for months. **This is a scope question about
his own new ruling, not a defect.**

## [--:--] Porter (PM) — 🔴 Owner's order: **`REQ-086` goes LAST. Finish his batch, `sid`, test, `uat` TODAY.**
🚫 **`SPEC-078`'s first task DEFERRED — not cancelled.** ✅ **The spec stands.** 🔑 **And it strengthens @Sober's
own ordering argument rather than overriding it: he said `§7`'s four formats ARE `REQ-086`'s shipped defaults**
⇒ **finishing the batch first IS building the editor's seed data.**
📌 **It is also the batch's ONLY migration** ⇒ **deferring it keeps today at `35 = 35`: no migration, no window,
no closed shop.** **That is the difference between a release that can ship today and one that cannot** — **and
I said it to him in those terms rather than as "the owner said so".**
▶️ **Closing list sent as one:** `§5` registration copy · **`§12` RE-CUT from the corrected rule** ·
`§12.1` the expiry control ON THE CARD · the dead FE `Create plan` gate. 🚫 **NOT `LEAVE_NOTICE_TOO_LATE` —
removing a refusal there is a REGRESSION, not the requirement.**
⏱️ **Asked for ONE message when the last item lands, not a running commentary** ⇒ **I release @Tanya once, on a
build that will not move under her.** 🔻 **I stopped her mid-round today because the build changed beneath my
own dispatch. Not twice.**
⚠️ **And asked him to tell me NOW if anything will not make today.** 🔑 **He can ship a SMALLER release today;
he cannot ship a LATE one.** **"This one slips" is a sentence I can act on; a delay discovered at deploy time
is not.**

## [--:--] Sober (SA) — ✅ **The closing list is CUT: TASK-310 (`§5`) and TASK-311 (`§12.1` + the dead gate).** 🟢 **`§12.2` regression check RUN, not promised.** ⚠️ **And I told @Porter which item can slip, before he asked twice.**

### 🟢 The check `§12.2` demanded
**`LEAVE_NOTICE_TOO_LATE` still throws at `scheduler.service.ts:2480` and `:2914`; the webhook still maps it at
`:838`.** ⇒ **today's `§12` work removed the EXTENSION ceiling and touched the notice cutoff nowhere.**
✅ **No regression**, and TASK-310 names it under *must not change* so it stays that way.
🔑 **@Porter's correction of his own `§12` wording is the sentence to keep:** ***among the reasons a leave may be
refused for BEING A LEAVE, the quota is the only one*** — **the cutoff is about WHEN THE FAMILY SPOKE, not how far
the calendar moves.** 📌 **His fifth over-wide reading today, and the first he caught before it reached a task.**

### 🔴 The trap in `§5`, flagged rather than discovered
**`REQ-079 §17c`'s copy is BILINGUAL on every screen — and `add_student_name_prompt` is screen 4, the string I had
@Jason change back to `t(…, lang)` an hour ago.**
🔑 **Both rulings are right and the reconciliation is `both()`'s own purpose: it is for a reader whose language is
not yet KNOWN, and during REGISTRATION it is not.** ⇒ **`§17c` supersedes my TASK-307 ruling for exactly these
screens, and the consistency he was protecting is satisfied in the other direction.**
⚠️ **Second time today my own instructions needed reconciling in front of an engineer** — **and both times he
would have hit it before I did.** 📌 **Flagging beats being caught.**

### ⏱️ The honest size read, given BEFORE it was asked for twice
🔴 **`§5` is the long pole and the only item that could slip:** **eight screens, the entry keyword changing to
`สมัคร`, and the role list coming out — on the path EVERY new parent walks.** 🔑 **The one place today where a
wrong string is met by a stranger rather than by staff.**
✅ **TASK-311 is small — two single-screen changes, one of them a deletion.** 📌 **If a smaller release is needed,
`§12.1` + the dead gate ships alone; `§5` is the one to drop — not because it matters least but because it is
least reversible in front of a customer.**
🟢 **NO MIGRATION in either** — **`35 = 35` holds for the whole release, and I asked @Porter to re-count rather
than trust me.**
⚠️ **He gets ONE message when the LAST lands — no running commentary.** **He had to stop @Tanya mid-round today
because a build moved under his own dispatch; I am not the reason that happens twice.**

**BALL: @Jason (`§5`, the long pole) and @Fern (TASK-311) in parallel.**

## [--:--] Porter (PM) — 🔴 Owner: **"ครึ่งเดียวฉันไม่คุย ทำให้จบ"** ⇒ **`§5` reordered to FIRST. It is the gate on his entire LINE round.**
**The four NOTIFICATION formats are on `sid` and testable now; the COMMAND side is not built, so `sid` still
shows the old *"คุณเป็นใครคะ?"* entry.** ⇒ **he refuses a half-coverage LINE round.**
🔑 **And he is right in a way that is worth writing down: a LINE round costs HIM a phone, an OA and his own
hands. It is the one test nobody on this team can do for him** — **so asking him to run it twice is asking for
the scarcest thing we have.**
▶️ **Reordered: `§5` ahead of everything, then `§12`'s re-cut, `§12.1`, the dead FE gate.** 🔑 **Told @Sober the
reason is DEPENDENCY, not urgency: nothing else in the batch unblocks a PERSON.**
⚠️ **And I asked whether `§5` alone can reach `sid` ahead of the rest** — **a split deploy I would not normally
ask for.** 🔻 **The alternative is he tests LINE twice or not at all, and that is a worse trade than an extra
deploy.**

## [--:--] Sober (SA) — ✅ **@Porter offered a split deploy for `§5`. There is nothing to split: the whole LINE surface is BACKEND.** 🔑 **And TASK-311 was never on the owner's path — @Fern told so explicitly, before she rushed for nothing.**

### ✅ The answer, checked rather than assumed
**Grepped the FE repo for `line-webhook` / `replyToken` / `enqueueLine` — nothing.** ⇒ 🔑 **`smart-scheduler-front`
serves the admin frontoffice and NOT ONE BYTE of LINE.**
| item | repo | on his LINE round? |
|---|---|---|
| **`§5` (TASK-310)** | back | 🔴 **it IS the round** |
| **`§7.1`–`§7.4`, `§12`, `§2`** | back | ✅ already in, reviewed, green |
| **`§12.1` + the dead gate (TASK-311)** | **front** | 🚫 **not on that path at all** |
⇒ **Deploying the BACKEND gives him the whole LINE surface.** ⚠️ **Honest caveat given: that ships ALL of today's
BE work (296–309), not `§5` alone** — **all reviewed, green, `35 = 35`.** 🔑 **Not a partial deploy; one unit that
already contains everything he asked for except two FE touches.**

### 🔑 The reorder passed on as a REASON, not as urgency
**@Porter's line, given to @Jason verbatim:** ***a LINE round costs him a phone, an OA and his own hands — it is
the one test nobody can do for him.***
⚠️ **And I explicitly did NOT tell him to hurry.** 🔴 **A rushed `§5` costs the owner a SECOND round, which is the
exact thing this reorder exists to prevent.** 📌 **Eight screens on the path every new parent walks — the one
place today where a wrong string is met by a STRANGER rather than by staff.**
✅ **Two things that make it smaller than it looks, and I said both: the WORDS are done** (`§17c` is the
customer's own copy — no wording decision is ours) **and the FE work cannot block it.**

### 📌 The part I am most glad I checked
**@Fern could reasonably have read "the owner will not test at half coverage" as making everything urgent.**
🔑 **Told her plainly it does not, with the grep behind it** — ⇒ **she is the one person on this batch who could
have been rushed for nothing**, and **a rushed expiry dialog would cost more than a late one.**
✅ **And her §1 still says LOOK before building: the capability EXISTS; the owner is naming WHERE it must be
reachable from.** 📌 **Twice in this batch a requirement named something already built, and both times the report
was still real because it was about the SCREEN.**

**BALL: @Jason — `§5`, and it is the only thing between the owner and testing anything in LINE.**

---

## Jason (BE) — ✅ TASK-310 DONE (code) — `§17c` verbatim, the role list GONE, and TASK-278's five judgements scored. → @Sober **1877 pass / 0 fail**, 150 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change.
⏱️ **It landed today** — I had nothing to tell @Porter that he could act on.

- [x] 🔑 **The eight screens pinned BYTE-FOR-BYTE against `§17c`** — new `src/lib/registration-copy-req079.test.ts`
- [x] 🚫 **No heading is sent** — asserted, and **screen 2's especially**
- [x] 🔴 **`ครู` / `แอดมิน` / `CEO` appear NOWHERE in a registering parent's messages** — asserted as an ABSENCE
- [x] **Entry keyword `สมัคร` / `register`; `Next` advances a PARENT** · **`CEO` is not a code path**
- [x] 🔑 **Bilingual, and all five `add_student_name_prompt` sites agree** — asserted on the EXPRESSION
- [x] **TASK-307's no-skip branch still works, and re-asks with the NEW copy** — both halves
- [x] 🔑 **Break it and watch** — two mutations, both restored, suite green before this number
- [x] 🚫 **The address flow untouched** · **`LEAVE_NOTICE_TOO_LATE` untouched**

### 🔑 §1 — the mechanism, and the one place your instruction needed a different shape than `both()`
You ruled these screens BILINGUAL and that is right. ⚠️ **But `both()` cannot produce them.** It stacks a whole
Thai body above a whole English one; **`§17c` alternates LINE BY LINE** — a Thai sentence, its English
sentence, and on screen 4 a `เบอร์โทรศัพท์ / Phone:` line in the middle of the pair. ⇒ **there is no pair of
`TH`/`EN` values `both()` could join to make their screen.**
✅ **So the STRING is bilingual and the call site keeps `t(key, lang)`** — which is the property TASK-307 was
protecting, satisfied in the other direction and more strongly: **every reader gets the identical screen.**
🔑 **And the guard is in the JOINER, not in a list of keys:** `both()` now returns a body once when both
languages render the same text. ⚠️ It had to go there — `` tb(`code_${role}`) `` renders a §17c screen for a
parent and one of OURS for a teacher **from one expression**, so no call site could have carried the rule.
📌 **A doubled screen passes every string pin**, so it is asserted on the ASSEMBLED screen 4 as well.

### 🔻 §3 — the buttons are gone, and that reverses half of TASK-251
**A role picker is a role list you cannot look away from.** No arrangement of buttons offers one choice and
hides two, so `rolePicker` is **deleted**, not edited — with a gravestone, because a deleted builder with no
note is one somebody re-adds. ✅ **What TASK-251 was actually about is untouched:** no digit is asked for or
accepted anywhere, a typed word still reaches ONE transition, and the postback namespace is still ours.
⚠️ **The `action=role` branch OUTLIVES the picker on purpose** — a quick reply already sitting in a parent's
chat when the deploy lands is still tappable, and answering it for one more conversation costs six lines.
🚫 `ครู` / `แอดมิน` stay ACCEPTED and are never advertised — `§17e-2`, the owner's knowing trade.

---

## 🔴 THE THREE THINGS I CHANGED THAT THEIR COPY DID NOT ASK FOR — say if any is wrong

**1. ⚠️ `ข้าม` is no longer ADVERTISED on screens 5 and 6.** Their sentences have no escape and `§2` says use
them verbatim. 🔑 **The behaviour is untouched — the parser still accepts `ข้าม` / `skip`** — and the
REJECTION (`add_birthdate_bad`, not a §17c screen) still names it AND still carries the example `02-12-2024`.
⇒ **a parent who cannot answer is refused once and told both.** 📌 That is the only thing that makes this a
cost rather than a trap, and it is asserted as a pair so nobody removes half of it.
**2. 🚫 Their screen-6 English is wrapped in a `"` … `"` pair in the document. Not reproduced.** A quotation
mark opening one line and closing another reads as a typo on a phone, and by `§17f`'s own reasoning it is
document punctuation rather than copy. **It is the only byte of theirs I changed.**
**3. 🔻 `welcome` lost OUR greeting and OUR command hint** — *"สวัสดีค่ะ ยินดีต้อนรับสู่ Smart Scheduler"* and
*"หลังผูกแล้ว: เพิ่มนักเรียน · เช็คอิน · ลา · qr"*. §17c screen 1 is two lines and has neither. 📌 The hint was
only ever shown to an UNLINKED chat, which cannot use any command it listed.

**And one I kept:** `menu_body` still follows screen 8. Their screen ends at *"type เพิ่มนักเรียน"*; dropping our
menu would take the command list away at the one moment a parent is finally linked. **Say the word and it goes.**

---

## 📌 THE QUESTION — how many of the eight already existed and merely differed?

**Five of the eight English sentences were ALREADY byte-correct**, and the reason is not that we wrote well:
**`§17b` was a transcript of the same document's English column**, so wherever their English did not move,
neither did ours. ⇒ *"our wording was right wherever we COPIED it"* — which answers the tone half of your
question and tells you almost nothing about `REQ-086`.

**🔴 Four were wrong in a way a parent WOULD have noticed. None of them is tone:**
| screen | what a parent met | why |
|---|---|---|
| **2 · role** | *"คุณเป็นใครคะ? ผู้ปกครอง · ครู · แอดมิน"* | 🔴 **the door AND the key** — `REQ-085 §5`'s entire subject |
| **6 · address** | *"จังหวัดที่อยู่"* — we asked for a PROVINCE | they ask for **เขต แขวง จังหวัด**; a parent types one word and their address is a third of an address |
| **8 · added** | our command menu | 🚫 **their *"type เพิ่มนักเรียน to add another"* was MISSING** — the invitation the screen exists for |
| **7 · labels** | `ชื่อ:` `วันเกิด:` `จังหวัด:` | their `ชื่อ / Name:` pairs, dropped **by an explicit TASK-278 judgement** |

### 🔑 And this is the part I think matters for `REQ-086`
**The errors did not cluster where we TRANSLATED. They clustered where we DECIDED.**
TASK-278 named five places their text *"must not be applied literally"*, each one defended in a comment.
**Scored against `§17c`: three were right, two were wrong** — and both misses are the same mistake:
- 🔻 **the field labels** — read as document furniture, like the numbered headings. ⚠️ **They are not the
  headings: they sit INSIDE the body, in the middle of the screen a parent reads**, and their own examples
  print them there. (`§17f` then ruled the HEADINGS out on that reasoning, correctly. Same reasoning, two
  different objects.)
- 🔻 **the address** — @Porter's own *"a prompt that lists parts is not a schema"*, which he caught in himself
  in `§17e` and I inherited from the other side: we had read their prompt as naming ONE part.

⇒ **An editor would not have prevented either.** Both were structural readings, not wording. 📌 **So for
`REQ-086`: the shipped defaults matter LESS than the review of what an engineer decided NOT to apply.**
🔑 **The cheap control is the one this task used** — pin the customer's text byte-for-byte in a test, and make
every deliberate departure a NAMED assertion with its reason. **Then a departure is a thing somebody can
disagree with, instead of a comment nobody reads.** Three of TASK-278's five are still in the repo as exactly
that, which is the only reason today's scoring was possible at all.

### 📌 Two small ones, recorded not raised
- **`{max}` no longer interpolates anywhere in the name prompt** — their sentence has no cap line. The cap is
  unchanged: `assertCanAddStudent` at the first step, and `added_atmax_note` still carries `{max}`.
- **Their screens 7 and 8 disagree about the example child** (`น้องส้ม` vs `น้องดีซี`). Harmless in a spec —
  ours echoes the name that was actually confirmed, which `§17d` already recorded.

**BALL: @Sober — TASK-310 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — 🟢 **`§5` IS IN. The backend is complete and HANDED OVER — the owner's LINE round is unblocked.** ⚖️ **Three deviations accepted, two routed to the customer.**

**Verified:** `bun test` **1877 pass / 0 fail** (5352 expects, 150 files) · **35 = 35, no migration anywhere in
the batch** · `tsc` 0 · backend only.

### 🔑 `both()` could not do what I asked, and his fix is better than my instruction
**`§17c` alternates LINE BY LINE; `both()` stacks whole bodies.** ⇒ **the STRING is bilingual and the call site
keeps `t(key, lang)`** — 🔑 **which satisfies TASK-307's property MORE strongly than my own ruling: every reader
gets the identical screen.**
✅ **And the guard is in the JOINER, not a list of keys** — `` tb(`code_${role}`) `` **renders a `§17c` screen for
a parent and one of OURS for a teacher from ONE expression**, so no call site could have carried it. ⚠️ **A
doubled screen passes every string pin; he asserted the ASSEMBLED screen.**

### ⚖️ The three deviations — accepted, and two go to the CUSTOMER not back to him
1. **`ข้าม` no longer advertised** (their copy has no escape line). ✅ **Behaviour unchanged and the rejection
   still names it** — 🔑 *that pair is what makes it a cost rather than a trap.* ⚠️ **But a parent who cannot
   answer must be refused ONCE to learn they may skip** ⇒ **routed to the customer to decide; not reversed.**
2. **A `"` pair not reproduced** — document punctuation by `§17f`'s own reasoning; **one byte, flagged.**
3. **`welcome` lost our greeting and command hint** — 🔑 **the hint was only shown to an UNLINKED chat that could
   not use a single command it listed.** **A defect fixed in passing.**
✅ **And the one he KEPT — `menu_body` after screen 8 — stands**, ⚠️ **but it is the one place we ADD to their
words, so @Porter has it too.**
✅ **`rolePicker` DELETED with a gravestone** — ***"a role picker is a role list you cannot look away from."***
**Keeping the `action=role` branch for quick replies already in a parent's chat is the right kind of six lines.**

### 🔑 The finding that changes what `REQ-086` BUYS
**Five of eight English sentences were already byte-correct — because `§17b` was a transcript of the same
document.** 🔴 **The four that were wrong were STRUCTURAL, not tone**, and two of them came from an explicit
engineering judgement (TASK-278) that read BODY TEXT as document furniture.
⇒ ***"The errors did not cluster where we TRANSLATED. They clustered where we DECIDED."***
⚠️ **An editor would have prevented NEITHER.** 📌 **Told @Porter before he sizes `REQ-086` for the owner a second
time: it is still worth building, and it buys something narrower than "we stop getting the words wrong".**

### ⏱️ The handover
🟢 **ONE message, as he asked.** 🚫 **No further backend changes until @Tanya has been through it — the build will
not move under her.** ✅ **TASK-311 is in the OTHER repo and cannot touch what she tests.**
📌 **Two things for the customer, neither blocking: the unadvertised `ข้าม`, and our menu after their screen 8.**

**BALL: @Porter — the deploy and @Tanya. @Fern has TASK-311; it waits on nobody.**

## [--:--] Porter (PM) — 🟢 **HANDOVER: the backend is COMPLETE. `§5` is in, and everything that reaches LINE ships in ONE backend deploy.**
`bun test` **1877/0** (150 files) · typecheck 0 · **`smart-scheduler-back` only.**
✅ **I RE-COUNTED the migrations myself rather than repeat his number, as I said I would:** **`ls drizzle/*.sql`
= 35, journal `"tag"` count = 35.** 🔑 **That is the 08-01 rule — *"no migration" is a claim, not a state* — and
a claim I pass on unverified is my claim, not his.**
✅ **In one deploy:** `§5` registration copy · `§7.1`–`§7.4` the four formats · `§2` the teacher's leave notice ·
`§12` quota-only · `§10` · `§6` · `§11.3`.
⚠️ **`TASK-311` (the expiry control on the CARD, the dead `Create plan` gate) is FE and NOT on his LINE path** ⇒
**it cannot delay the round and gets its own message.**
🔑 **@Sober froze the backend until @Tanya is through** — **"the build will not move under her"** — **after I
told him I had to stop her mid-round today for exactly that.** **That is a process point being applied back at
me, correctly.**

### 🔑 The finding that changes what `REQ-086` BUYS — and it arrived before I sized it a second time
**@Jason scored our eight screens against the customer's.** **FIVE English sentences were already byte-correct
— not because we wrote well, but because `§17b` was a transcript of the same document.**
🔴 **The FOUR that were wrong were not TONE. They were STRUCTURAL:** the role list · the address asked as ONE
part when they ask for three · their *"type เพิ่มนักเรียน"* invitation simply missing · and their field labels
dropped **by an explicit engineering judgement.**
🔑 ***"The errors did not cluster where we TRANSLATED. They clustered where we DECIDED."***
⇒ ⚠️ **An editor would have prevented NEITHER of the two misses.** ⇒ 📌 **`REQ-086` is still worth building, but
it buys something NARROWER than "we stop getting the words wrong".** 🔻 **And it lands on my own reasoning: I
told him the fix for "the agents forget the wording" is to stop routing wording through agents. That is true of
TRANSLATION and false of DECISIONS — and the decisions are where we actually lost.**
🔻 **Note also the address: I reported it as three fields, the owner corrected me to free text — and the
CUSTOMER asks for three parts. My reading was right about their intent and wrong about the field.** **Recorded
because "I was half right" is exactly the shape that produces a confident wrong answer next time.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid` (backend only). **@Tanya released on a FROZEN build; the owner runs his LINE round in parallel.**
🔑 **Told her the rule CHANGED since my last dispatch and that the change was MY error, not the team's:**
**the QUOTA is the only thing that may refuse a leave; the week ceiling must refuse NOTHING** ⇒ **"an accurate
refusal is still a failure."** 🔻 **My previous dispatch described a second gate that never existed** — **and a
tester working from a wrong rule reports the wrong verdict with perfect diligence.**
🚫 **Scoped her OFF the frontend (`TASK-311` is undeployed) and OFF LINE (the owner's, in parallel)** —
**including "do not report their absence as a gap."** 🔑 **Half a batch she cannot see has produced a wrong
`NOT_TESTED` before.**
⚠️ **Named the TWO correct behaviours she might otherwise file:** `LEAVE_NOTICE_TOO_LATE` still refuses (the
owner's `§12.2` ruling) · a LATER child may still be skipped.
🟢 **@Sober froze the backend until she is through** — **after I told him I had stopped her mid-round today
because the build moved.** **The process point came back applied, without me asking for it.**

## [--:--] Porter (PM) — 🔴 `§13`: **command keywords must accept ENGLISH. And the CUSTOMER'S OWN COPY already promised it — we were about to ship the English half of every screen as a lie.**
**Their words, unedited:** *Please type **"register"** to start* · *Please Type **"Confirm"** to save* ·
*Type **"Cancel"** to exit* · *please type **"Add Student"***.
🔴 **`§5` as shipped accepts `สมัคร` and REFUSES `register` — on the screen telling the parent to type
`register`.**
🔻 **This is my miss, and a specific kind:** **I recorded `§17c` verbatim, wrote `§17d`'s analysis, flagged four
things for the owner — and still read those four English sentences as TRANSLATIONS when they are
INSTRUCTIONS.** 🔑 **Exactly the class @Jason named this morning: *"the errors did not cluster where we
translated, they clustered where we DECIDED."*** ⇒ **I decided, silently, that the English half was decoration.**
✅ **Four pairs come from THEIR copy** (`register` · `Confirm` · `Cancel` · `Add Student`). 🔻 **`teacher` ·
`admin` · `skip` are MY proposals — their copy has no English for those — and marked as mine.**
📌 **Wrote the matching rules myself so nobody guesses:** case-insensitive and trimmed · **`Add Student` matched
with the space collapsed** (*a parent on a phone is not typing carefully*) · 🚫 **no fuzzy matching — a bot that
GUESSES is worse than one that asks again, and we cannot apologise to a parent it guessed wrong about** · **the
refusal must name BOTH forms.**
🔻 **And I recorded the thing that must NOT become the justification: English keywords happen to be ASCII, which
would unblock @Tanya's `adb` rig.** ⇒ **a CONSEQUENCE, not a reason.** **The product does not get shaped around
what our tools can type; the tools get fixed.**
⏱️ **Asked @Sober whether this lands before or after the owner's LINE round** — **if after, he re-tests a flow
he has already walked.**

## Tanya (QA) — ✅ **Owner's batch on `sid`: ALL FOUR CHECKS PASS**

✅ **CHECK 3 `TASK-300`** — built the owner's exact arrangement (4-session, absences weeks 2–4). Make-ups landed
`14/21/28 Oct`, **not** on the absent weeks; **PREVIEW and SAVE compared directly and agree** — the assertion the
task existed for.
✅ **CHECK 1** — stretched course (week 7 vs a card saying week 5) took its quota leave: **no refusal**, a
preview before committing, and **`Ends 28 Oct → 4 Nov`** with a fourth make-up. **Clicked end to end.**
✅ **CHECK 2** — re-planned course took its quota leave: 200, `liveEndDate 07/Oct → 14/Oct`. ⚠️ **API-driven;
the plan modal hung on `Loading…`. Declared rather than blurred into check 1's evidence.**
✅ **CHECK 4** — 🔻 **nearly filed as a failure.** No warning appeared in the dialog and **no preview request
fired** (so: never rendered, not missed). **But `TASK-298` is BACKEND with "no FE change — that half comes
later"**, and the dispatch was backend-only. **Tested the half that shipped:**
`POST /courses/:id/expiry/preview` → **200, `warn:true`, `outside[]` NAMING each session (id/date/status/time)**
— **before any save, and named rather than counted.** UI half `NOT_TESTED` by design.
📌 Not filed: that payload carries `startTime "14:00:00"` — the DEF-5 seconds shape, in a payload nothing
renders yet. Flagged so the FE half does not inherit it.
🟢 Footprint declared: one new course (฿4,790) as the check-1/3 fixture; expiry dialog **cancelled, never saved**;
no LINE, no `uat`.

## [--:--] Sober (SA) — 📋 **`§13`'s inventory is ONE file, and three of @Porter's four already work.** 🔴 **The fourth does not refuse — `Add Student` creates a child named "Student".**

### 📋 The inventory — `lib/line-commands.ts`, and nowhere else
**TASK-245 deliberately put the router's vocabulary and the reserved set in one list.** 🔑 **And it ALREADY
carries English almost everywhere** — `register` · `menu`/`help` · `courses` · `admin` · `children`/`students` ·
`qr` · `checkin` · `leave`/`sick` · `schedule` · `calendar` · `cancel` · `reopen` · `skip`/`no`/`done`, plus
`confirm`/`yes`/`ok` in `line-add-student.ts`.
🔴 **Exactly one Thai keyword has no English form: `ครู`.**

### 🔻 The correction, sent BEFORE it reached the owner
**@Porter's headline — *"`§5` accepts `สมัคร` and REFUSES `register`"* — is wrong: `CMD_REGISTER` contains
`register`.** **So are `Confirm` and `Cancel`.**
📌 **He wrote that neither of us saw `§5` was half-built while reading the copy twice.** 🔑 **The truer sentence:
most of it was never broken — and the one thing that IS broken is not what a reading would have found.**

### 🔴 The real defect, and it is worse than a refusal
`line-webhook.service.ts:1028` — `/^(?:เพิ่มนักเรียน|เพิ่มลูก|add)\s*(.*)$/i`, with `:1031` adding a student
named by the remainder.
**Screen 8 tells a parent to type *"Add Student"*** ⇒ **`add` matches, `Student` is captured, and a child called
`Student` is CREATED — silently and successfully.** ⚠️ **`Student` is not reserved (`students` is).**
🔑 **TASK-245's defect returned: the bot advertises a phrase and swallows part of it as data** — **the one that
cost the owner a student record he could not delete, and there is still no delete route and no archive flag.**
⇒ ***obey our own screen, in the language our own screen offers, and create a permanent record.***

### ⏱️ The timing answer, and I put the decision where it belongs
🔴 **Recommended: this ONE regex goes in NOW** — **he is mid-registration-round, screen 8 is the last screen of
the flow he is in, and the damage is a permanent record rather than a message.**
⚠️ **I said an hour ago that the build would not move under @Tanya.** 🔑 **That promise was to protect a tester
from a moving build; this is a defect that WRITES.** ⇒ **@Porter's call, made knowingly — I will hold if he says
hold.** 🟢 **No copy needed, no migration.**

### ✅ What I asked him for, and the limit I named
**Only `ครู` needs an English word, and I did not choose it** — 🚫 *an engineer choosing what a parent reads is
the TASK-278 mistake, two hours after his own scoring found it.*
✅ **His principle is the right test and it will be written from the LIST, not from pairs** — *a keyword added
next month fails until it has an English form.*
⚠️ **Named its limit honestly: `confirm` and the `Add Student` REGEX live outside that file, so the sweep cannot
see them** — **whether they should move is a design question for after the round, not during it.**

**BALL: @Porter — one word, and whether the `Add Student` line goes in now.**

## [--:--] Porter (PM) — ✅ @Tanya: **ALL FOUR CHECKS PASS.** 🔴 And @Sober caught that THREE of my four `§13` claims were FALSE — before they reached the owner.
**Her round:** `TASK-300` PASS (**she compared PREVIEW against SAVE directly — the assertion the task existed
for; either alone would have passed while the pair disagreed**) · the stretched course took its quota leave
**clicked end to end**, dates moved, **and the card then leave-locks — the quota refusing a SECOND leave is the
one refusal that should exist** · the re-planned course PASS **(server half only — she said so rather than let
one PASS borrow the other's evidence)** · the expiry preview **names each session rather than counting.**
🎯 **And she nearly filed check 4 as a failure and stopped herself with the standing rule:** **no preview
request fired at all** ⇒ **nothing was asked, so nothing could have rendered — not a timing miss** — **and the
FE half was never in this round by design.**

### 🔻 My `§13` claims were false, and confidently so
**I wrote that the bot accepts `สมัคร` and REFUSES `register`, same for `Confirm`/`Cancel`.** **All three have
been accepted for some time.** 🔑 **@Sober's sentence is the accurate one:** ***most of it was never broken —
and the one thing that IS broken is not the thing the reading would have found.***
🔻 **What I did: read the customer's English half, ASSUMED we had ignored it, and inferred a defect from a
DOCUMENT instead of the code.** ⚠️ **The same morning I told him *"I verified at source" is no longer acceptable
on its own*.** **This is that failure from the other side — I verified at NEITHER.**

### 🔴 The one real defect, and I shipped the fix mid-round
**`Add Student` → `add\s*(.*)` ⇒ the bot obeys its own screen and creates a child NAMED `Student`.** **No
refusal. Silent success. `Student` is not reserved (`students` is).** ⚠️ **PERMANENT — no delete route, no
archive flag.**
✅ **Authorised it to ship NOW, knowingly overriding @Sober's own build-freeze** — **because @Tanya had FINISHED,
so the promise it protected was discharged; because it WRITES; and because the owner is inside that flow right
now with our own screen telling him to type the phrase.** ⇒ **holding it meant watching him walk into it.**
📌 **Told him to declare the test's blind spot IN the test file** — it cannot see `confirm` or the `Add Student`
regex. 🔑 **An assertion that names what it does NOT cover is worth more than one that quietly covers less** —
**`TASK-288`'s green assertions through a live defect is the precedent, and he found that one himself.**

## [--:--] Sober (SA) — 🔴🔴 **`Add Student` was half of it: `admin` creates a child named `in`, and has since inline-add was built.** ✅ **TASK-312 cut, `§1` shipping now.**

### 🔴 The finding
`:1028` — `/^(?:เพิ่มนักเรียน|เพิ่มลูก|add)\s*(.*)$/i`. **`add` is a BARE PREFIX and `\s*` matches EMPTY.**
⇒ **`Add Student` → a child named `Student`** · 🔴 **`admin` → a child named `in`** · **`address` → `ress`.**
🔴 **`addMatch` runs at `:1028`, `CMD_ADMIN` at `:1040`** ⇒ **the English form of one of our OWN advertised
commands is unreachable and WRITES instead.** ⚠️ **None of them is reserved, and there is still no delete route
and no archive flag — every write is permanent.**
📌 **Sent to @Porter immediately because he was mid-warning: the owner must not type `admin` or `address` in
English either.**

### 🔑 Why it survived, and it is the reason `§13` exists at all
***An English-only defect on a Thai-speaking team is invisible by construction.*** **Everyone here types
`แอดมิน`.** 📌 **The customer has foreign parents — they are exactly who would have found this in production, by
following our own menu.** ⇒ **`§13` is not scope the owner added; it is a surface nobody here has ever walked.**

### ✅ The fix, in two parts, and I named the wrong one explicitly
**`add` may only be an inline prefix when a SEPARATOR follows** *(so `admin`/`address` fall through)* **and
`Add Student` is matched as the COMMAND before the bare `add`, case-insensitive and space-collapsed.**
🚫 **NOT by reordering the router** — 🔑 **that fixes `admin` and leaves `address` and every future `add…` word
broken. The PATTERN is the defect, not its position.**

### ✅ `§13` in `§2`, with @Porter's two conditions carried verbatim
**`teacher` confirmed** · **the sweep written from the LIST so a new keyword fails until it has an English form**
· 🔴 **the BLIND SPOT declared in the same file** (`confirm` and the `Add Student` regex are invisible to it) —
***an assertion that names what it does NOT cover is worth more than one that quietly covers less.***
🔑 **`§13.3` tested with `ConFiRM` and `AdD StUdEnT`, never `Confirm`** — *a `Confirm` test passes a
first-letter-only `toLowerCase()`.* 🚫 **`CONFIRMM` still refused.**

### 🔻 On @Porter's self-correction, and the asymmetry worth recording
**He inferred a defect from a DOCUMENT and was wrong; I have twice inferred one from CODE and been wrong.**
⇒ ***neither document nor code is enough alone — the pair is what settles it.***
📌 **And his false premise produced a true finding two levels down: it took his reading of the customer's English
half to make me look at that regex at all.** ⚠️ **Not luck to repeat — but the wrong question got asked in the
right place.**

**BALL: @Jason — `§1`, the shortest urgent thing I have sent him.**

---

## Jason (BE) — ✅ TASK-312 §1 + §2 DONE (code) — `Add Student` fixed, `admin` was NEVER broken (`adm` ≠ `add`), and `add child` is the next one. → @Sober **1894 pass / 0 fail**, 152 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change.

### 🔴 READ FIRST — `admin` was NEVER broken by this regex. `a-d-m` is not `a-d-d`.
**The headline of §1 is false and I can prove it with the mutation you asked for.** I restored the bare prefix
`/^add\s*(.*)$/i` and ran the three inputs through it:
```
admin → null | Add Student → {"name":"Student"} | address → {"name":"ress"}
```
**`admin` does not start with `add` — its third letter is `m`.** ⇒ `CMD_ADMIN` was reachable the whole time;
"the English half of one of our own advertised commands is unreachable" did not happen, and *"a child named
`in`"* could never have been written by this pattern. ⚠️ **The two REAL defects stand and were live:** `Add
Student` → a child named **`Student`** (our own screen 8's instruction, obeyed), and `address` → **`ress`**.
📌 So the DoD line *"show `admin` creating a child named `in`"* cannot be satisfied; **the mutation instead
caught `address` and `Add Student`, five assertions, for the reason expected.** Restored; suite green above.
🔑 **And the Question's premise needs the same correction:** the invisible-by-construction defect is real —
**but its evidence is `Add Student`, not `admin`.** (An English-speaking parent following screen 8 would have
found it; a Thai-speaking admin typing `แอดมิน` never could.)

### §1 ✅ The PATTERN is fixed; its position is not — `parseAddCommand` in `line-add-student.ts` (pure)
- **English: `add` is the command only when the input ENDS there or a SEPARATOR follows** — `add`, `add Emily`.
  `address` / `adding` / `addendum` fall through and write nothing.
- **`Add Student` is the COMMAND** — case-insensitive AND space-collapsed: `Add Student` · `AdD StUdEnT` ·
  `ADD STUDENT` · `addstudent` · `aDdStUdEnT` — all start the prompt, **none writes** (asserted on `name: null`,
  and the router's write pinned to `if (name)`).
- 📌 **DECISION, written in the parser's doc-block:** **`Add Student Emily` creates `Emily`.** The phrase is the
  command and takes a name after a separator exactly as bare `add` does — one shape, and it can never write
  `Student Emily`. **`addstudentemily` (no separator) matches nothing and writes nothing.**
- **Thai keeps its tolerance:** `เพิ่มนักเรียนน้องเอ` still reads `น้องเอ` — Thai is written without spaces and no
  other Thai command begins with `เพิ่มนักเรียน`, so the ambiguity that broke `add` does not exist there.
- 🚫 The check still sits ABOVE `CMD_ADMIN`; a source assertion pins the order so nobody "fixes" it by moving it.

### §2 ✅ `REQ-085 §13` — new `src/lib/english-keywords-req085.test.ts`
- 🔑 **The sweep is written FROM THE LIST** — it discovers every `CMD_*` export of `line-commands.ts` (13 of
  them) and fails any list with no English form. **A keyword added next month fails until it has one.**
- 🔴 **§13.3 with ABSURD casing** — every English form is run as `ConFiRM`-style, `SHOUT`, and padded, through
  the router's own comparison (`raw.toLowerCase()` + `inList`, both pinned in source). **`REGISTERR`, `admins`,
  `men` are refused** — case-insensitive is not forgiving.
- 🔴 **THE BLIND SPOT IS DECLARED AT THE TOP OF THE FILE** and asserted by hand beneath it: `confirm`/`cancel`/
  `skip` in `line-add-student.ts` (`ConFiRM` ✅, `CONFIRMM` ❌), and the `Add Student` regex. 🚫 Neither moved
  into the list.
- 🔻 **§2.1 — `teacher` was never missing, and I added nothing.** `ครู` is not in `line-commands.ts` at all —
  the inventory looked there and correctly found no `ครู`. **The only place the bot accepts `ครู` is
  `parseRoleChoice`, and `teacher` has sat beside it since TASK-251.** Asserted with `TeAcHeR` so §13.3 covers
  the role words too. ⚠️ Adding a `teacher` command keyword would have been inventing a command nobody asked for.

### 🔑 Break it and watch — the bare prefix restored (the output block above), 5 fail, restored, green.

---

## ❓ THE QUESTION — what ELSE has an English branch nobody here has ever walked? **Named. Fixed nothing.**

**1. 🔴 `add child` — our OWN English menu writes a child named `child`.** `menu_body` EN, line 1:
*"· add child — register a child (up to 5)"*. A parent who types what that line says gets `add` + separator +
`child` ⇒ **`parseAddCommand("add child") → { name: "child" }` ⇒ a permanent record named `child`.**
📌 **This is the same class as `Add Student`, is advertised by US rather than by the customer's copy, and it
was true before §1 and is still true after it** — §1 could not have caught it because `add child` IS the
correct shape for an inline add. **It needs a decision (rename the hint? reserve `child`? make `add child` the
phrase?) and that is @Porter's copy, not mine.** *Unwalked because the Thai menu says `เพิ่มนักเรียน`.*
**2. 🔴 `เช็คอิน 2` / `ลา 1` — the NUMBERED forms are Thai-only regexes.** `:1055` `/^เช็คอิน\s*(\d+)$/` and
`:1065` `/^ลา\s*(\d+)$/`. **`checkin 2` and `leave 1` match nothing** — the English parent gets whatever the
fallthrough gives an unknown word. The §2 sweep **cannot see these** (they are regexes, not lists) — a third
member of the declared blind spot, which I have NOT added to the declaration because you said name, not fix.
**3. ⚠️ Every English `type: …` instruction is now walked EXCEPT the two above.** I checked the EN strings that
tell a parent what to type: `register` ✅ · `reopen` ✅ · `cancel` ✅ · `skip` ✅ · `Confirm` ✅ · `Add Student` ✅
(as of §1) · **`add child` 🔴 (#1)**. The numbered forms are never advertised in English, which is why #2 is
unwalked rather than lied about.
**4. 📌 The refusal message names the Thai form only** (§13's matching rules, @Porter's last bullet). Not in the
TASK's DoD, so untouched — but it is an English branch that has never been read by an English reader.

**BALL: @Sober — TASK-312 §1 + §2 ready for review. ⛔ Nothing else is on me.**

## [--:--] Fern (FE) — ✅ **TASK-311 done, both halves, today.** 🔴 **§1 was NOT already built on the screen — it was a label with a 14px icon beside it.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  183 pass
                       0 fail          (was 170; +13, one new file)
bun run build     → ok
git status        →  9 modified · 1 new   🚫 contract.ts not among them
```

### §1 — I looked, and it was real
**The `expires …` line is a LABEL on the top-LEFT; the only control was a 14px subtle grey icon after it; the
top-RIGHT is the badges.** ⇒ the capability existed and the screen did not say so — the owner was right about
what he saw. ✅ **The date is the control now**: `expires {date}` + the icon are one `UnstyledButton` (dotted
underline, labelled *"Change expiry date"*) opening the same dialog from the same spot. 🚫 One dialog, asserted
mounted once; the icon not moved — it sits inside the button so it is one control, not two adjacent ones.
⚠️ **There are TWO top-right dates and I want him asked which he meant:** the plan modal's `SummaryBar` shows
**`Ends {date}` top-right — the derived LIVE END, not the expiry.** It should not become a control. If
*"ขวาบน"* meant that screen, the answer is still the card's control.

### §11.3 — the dialog asks BEFORE saving now
It was commit-then-show, and **its own header presented that as the design.** It was the constraint. Now every
date that differs from the current one calls **`POST /courses/:id/expiry/preview`** and renders what would
fall after it and the leave room — from the server's numbers, sentence written here, **SPENT case renders no
leave line** (TASK-298 §5, kept). 🚫 No `dayjs`, no `.filter`, no comparison in the block — asserted.
🔑 **`contract.ts`'s comment on the echoed `expiryDate` — *"so a caller cannot report one and have decided
another"* — is now load-bearing code:** the answer renders only if it is about the date on screen, and the guard
is the server's statement of what it answered, not my bookkeeping. **That was the break-and-watch: remove the
guard, one assertion fails naming it, 12 stay green, restored.**
🚫 Still not a gate — Save reads neither preview nor warning. 🚫 `ExpiryWarningAlert` untouched and **NOT reused
for the preview** — it says *"the date has been saved"*, which would be a lie before one. The block is its own.

### §2 — gone, and the sentence with it
`exceedsCeiling` is read nowhere on the FE. The state, the prop, the Alert, the `disabled` term **and the
tooltip branch** — `disabledHint` had no other source, and *a tooltip branch nothing can reach is the same dead
gate one level down*. `plan.ceilingRefusal` deleted in both languages. **The field stays where it is typed
(`types/app/scheduler:605`, optional) and `contract.ts` was not touched for either half — asserted both ways.**
⚠️ **Left for the day the field goes, deliberately:** the optional type, and **the mock still computes
`exceedsCeiling` by comparing dates** (`scheduler.mock.service.ts:892`) — dead, and a client heuristic, but it
mirrors a declared field. Delete the three together.

### Your Question — nothing cheap and general; three cheap and specific
1. 🔑 **The BE test that pins a field constant IS the notice** — the day Jason asserted *"false by
   construction"*, every FE reader was dead, and he knew it. ⇒ the rule is procedural: **a BE test that fixes a
   DTO field names its FE readers in the same notes.** One grep. It happened by habit this week.
2. 🔑 **Make the condition type-impossible and the class disappears** — TASK-295's `TimeSlot` return. The only
   mechanism that is cheap AND general, and only for gates over a closed set.
3. ⚠️ **A smell, not a test:** a DTO field the FE reads that **no FE test ever sets to its active value.**
   Nothing on this side had ever seen `exceedsCeiling` true. Where to point the eye, not proof.
🚫 **Not built:** a test that a branch "still CAN fire" by feeding it the value it guards — passes forever on
its own data. **The fake mechanism.** Named, nothing built.

### ⚠️ Not seen on a screen — as all week
Both halves are wiring-asserted and offline-exercisable via the mock. **@Tanya, `sid`: (a) on a course card,
click the `expires …` date itself — the dialog opens; (b) pick an EARLIER date — before pressing Save, the
dialog names the sessions it would cut and the leave room; press Save anyway — it saves; (c) create a course
with enough planned absences that the old build refused — `Create plan` is enabled and no week-N sentence
appears.** Timing: **both halves made today; nothing to hold `uat` on from my side.**

**Ball: @Sober.** TASK-292 · TASK-294 · TASK-286 remain, no clock.

## [--:--] Sober (SA) — 🔻🔻 **I RETRACTED `admin`. It was never broken — `a-d-m` is not `a-d-d`.** ✅ **TASK-312 and TASK-311 both PASS. The whole release is in.**

### 🔻 The retraction, first, because it reached the owner
**@Jason ran the pattern I asserted from. I then ran it MYSELF rather than take even his word:**
```
"admin" -> NO MATCH  |  "address" -> "ress"  |  "Add Student" -> "Student"
```
🔴 **I read a pattern instead of running it, wrote a DoD line that could not be satisfied, and sent it to
@Porter as URGENT while he was mid-warning to the owner.** ⚠️ **He relayed it because I said it with certainty.**
✅ **Retracted; `SYSTEM-FACTS` corrected in place.**
🔑 **What was REAL justified the ship on its own:** `Add Student` → a child named `Student`, and `address` →
`ress`. **Both permanent. Both fixed.**
📌 **And @Jason handled it the only way that works: he did not argue, he ran it and showed the output** — then
kept the mutation honest, reporting that my DoD line *"cannot be satisfied"* while the mutation caught the two
real cases for the reason expected.

### 🔻 Second error in the same message: `teacher` was never missing
**`ครู` is not in `line-commands.ts` at all — `parseRoleChoice` has had `teacher` since TASK-251.**
⇒ **my inventory searched ONE FILE and reported on the PRODUCT**, and **@Porter took a ratification question to
the owner that never needed asking.**
✅ **@Jason refused to add a `teacher` command keyword** — *"inventing a command nobody asked for"* — 🔑 **the fix
for my error was not to make my error true.**
📌 **Recorded: state the SCOPE of a sweep in the same breath as its result.** *"Nothing in `line-commands.ts`"*
and *"nothing in the product"* are different claims, and only one was true.

### 🔴 The new one, and it is OURS
**Our own English menu line 1: *"· add child — register a child (up to 5)"*** ⇒ **a parent typing what our menu
tells them gets a permanent record named `child`.**
🔑 **`§1` could NOT have caught it — `add child` IS the correct shape for an inline add.** 📌 **`Add Student` came
from the customer's copy; this one we wrote.** ⇒ **@Porter's, as a COPY decision.**
📌 **Also named, not cut: `เช็คอิน 2` / `ลา 1` are Thai-only regexes; `checkin 2` and `leave 1` match nothing.
Never advertised in English.**

### ✅ Both engineers, verified by me
| | |
|---|---|
| **TASK-312** (@Jason) | **1894/0**, 152 files, **35 = 35** · `parseAddCommand` pure, pattern fixed and POSITION pinned · `Add Student Emily` → `Emily`, decided in the doc-block · the §13 sweep DISCOVERS the 13 `CMD_*` exports · **blind spot declared at the top of the file** |
| **TASK-311** (@Fern) | **183/0**, 19 files · 🚫 `contract.ts` untouched, verified in `git status` · the `expires` line IS the control now · **`§11.3` asks BEFORE saving, computing nothing** |
🔑 **@Fern's best detail: the preview response ECHOES the expiry it answered, so a slow answer for an earlier
pick cannot land on a later one** ⇒ ***"that sentence is now load-bearing code"*** — **a contract comment that
existed as an explanation became a mechanism.** 📌 **The opposite of this week's other pattern.**
⚠️ **Her open question goes to @Porter, not answered by me:** **the plan modal's `SummaryBar` also shows a
top-right date — `Ends`, DERIVED — and it must not become a control.** **Editing it would mean editing a
derivation, which is what TASK-282 exists to protect.**

**BALL: @Porter — the retraction to the owner, and the `add child` copy call. Nothing is on either engineer.**

## [--:--] Porter (PM) — ▶️ Owner: cut `add child`. **The last item before his LINE round.**
✅ **Copy decided by me, not asked of him: the English menu advertises `Add Student`** — **the customer's own
phrase from screen 8, matched by the same rule.** 🔑 **We had THREE phrasings for one act and TWO were ours.**
⚠️ **Told @Sober `add child` must still be ACCEPTED** — **parents have seen it** ⇒ **it stops being ADVERTISED,
it does not start being REFUSED.** 🔻 **A fix that turns a working phrase into a refusal is a second defect
wearing the first one's clothes.**
🔑 **Asked for the assertion rather than the entry:** ***no word the product PRINTS may become a child's name***
⇒ **whatever we advertise is thereby reserved.** **`§1`'s fix cannot catch tomorrow's word — the SHAPE is right
and only the WORD is wrong.** ⚠️ **And asked him to SAY so if it ends up a hand-kept list, so I carry that limit
rather than discover it.**
📌 **Did NOT send the owner a retraction about `admin`** — **I never relayed that claim to him; my warning named
`Add Student` only.** 🔑 **An unnecessary correction spends his attention and teaches him to trust the next
warning less.** ✅ **But I DID tell him `teacher` already existed**, because he spent a decision on it.

## [--:--] Sober (SA) — 🔴 **Looking for the mechanism @Porter asked for found a LIVE defect: `add เมนู` creates a child named `เมนู`.** ✅ **TASK-313 cut — the last item before the owner's round.**

### ✅ His record correction, accepted
**He never relayed my `admin` claim — his warning to the owner named `Add Student` only.** ⇒ 🚫 **no retraction
to the owner, and I am not sending one.** 🔑 **His reason is better than my instinct:** ***an unnecessary
correction spends his attention and teaches him to trust the next warning less.***
📌 **My error was to HIM and is corrected to him. That is the right blast radius.**

### 🔴 The finding — TASK-245's own defect, live on the other door
| path | reserved check |
|---|---|
| name PROMPT (`:563`) | ✅ `isReservedWord` → strike |
| 🔴 **INLINE add (`:1035`)** | 🔴 **none** |
⇒ **`เมนู` at the prompt is refused; `add เมนู` writes a permanent child named `เมนู`.**
🔑 **TASK-245 exists BECAUSE the owner had `เมนู` stored as a child's name in a roster with no delete.** **The fix
went to the door he walked through.** ⇒ 📌 ***the fix went where the report came from*** — **second instance this
week, after the leave notice firing from one door of four.**

### 🔑 His principle, answered with the split he asked for rather than a promise
> ***No word the product PRINTS may become a child's name.*** *"If it ends up a hand-kept list, say so plainly
> and I will carry that limit rather than discover it."*

**Two-thirds mechanical, and the third is LABELLED in the test file:**
1. ✅ **structural** — the guard on both doors makes every reserved word unusable as a name by construction;
2. ✅ **mechanical for the MENU** — `menu_body` is `· word — description`, parseable in both languages ⇒ **it
   fails the day someone advertises a word without reserving it**;
3. 🔴 **hand-kept for the customer's eight screens** — **prose, not parseable**, and I will not build a parser
   that half-finds it. 📌 **Mitigation only: `§17c` is pinned byte-for-byte, so the words cannot change
   silently; a NEW instruction added later is what is unguarded.**

### ✅ The copy, exactly as he ruled
**EN menu says `Add Student`** — the customer's own phrase. 🚫 **No second English phrase for an act they have
already named.** ⚠️ **`add child` stays ACCEPTED** — *"it stops being advertised; it does not start being
refused"* — **and must add a child rather than name one `child`.**

**BALL: @Jason — TASK-313, and it is the last thing between the owner and a round he tests once.**

---

## Jason (BE) — ✅ TASK-313 DONE (code) — the inline door guarded, the add PHRASE reserved (the menu parser found it), and two more one-door guards named. → @Sober **1904 pass / 0 fail**, 153 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change.

- [x] 🔴 **`add เมนู` REFUSES and creates nothing** — the write is fenced behind the guard, asserted by ORDER and
      by count (`addStudentAndReply` appears once in the block, after the refusal)
- [x] **The SAME guard (`isReservedWord`) and the SAME `strikeOrPrompt`** on both doors — asserted on both slices
- [x] ✅ **EN menu advertises `Add Student`**; **`add child` still ACCEPTED and adds** — asserted (`{ name: "child" }`)
- [x] 🔑 **Every token the MENU advertises is reserved — parsed from the string, both languages** (6 + 6)
- [x] 🔴 **The prose limit DECLARED in the test file** — top comment AND a describe that names what green means
- [x] 🔑 **Break it and watch** — `if (name && isReservedWord(name))` → `if (false)`; restored; green above

### §2 ✅ The inline door, and one thing I had to add for the strike rule to be REAL there
`add <reserved word>` now hits `isReservedWord` → `strikeOrPrompt` with `add_name_reserved` — the one
definition, applied to the sibling door. ⚠️ **`strikeOrPrompt` counts on a session ROW, and a linked parent
typing an inline command may have none** — the update would be a silent no-op and the second reserved word
would never hand over. ✅ **So the refusal first drops the parent INTO the name prompt** (`AWAIT_STUDENT_NAME`,
exactly where bare `add` would have put them) and counts from there. **Keeps the strike behaviour honestly
rather than nominally**; asserted by order (`setStep` before `strikeOrPrompt`).

### §3 ✅ The three parts, built and labelled — and (2) needed the add PHRASE to be reserved
Parsing the menu found **two advertised tokens that were NOT reserved: `เพิ่มนักเรียน` and `Add Student`** —
the add phrase is a regex, not a list entry (TASK-312 §2.3 kept it so). ⇒ `add เพิ่มนักเรียน` would have written
a child named `เพิ่มนักเรียน`. ✅ **`isReservedWord` now also consults the ONE definition:** a bare add command
(`parseAddCommand(text)?.name === null`) is reserved. 🚫 The words are not copied into the list; the regex
stays the single source. **The mechanical test is what found this — it earned its place on the first run.**
🔴 **(3) is declared twice** — in the file's header and in a describe of its own: the eight `§17c` screens are
PROSE; what stands in is TASK-310's byte pins plus a hand-check of the instructions those screens give
TODAY. **A new instruction added later is not covered, and the file says so.**

### §1 ✅ And the honest sentence about `add child`
It is no longer printed anywhere and still works. ⚠️ **It still creates a child named `child` if a parent types
it bare** — `child` is not reserved, and §1 said accept, not refuse. **Whether `child` joins the reserved set is
@Porter's word, not mine** — recorded in the test as *"recorded, not fixed"* so nobody mistakes it for an
oversight.

---

## ❓ THE QUESTION — guards on ONE path when there are TWO. **Named; fixed nothing.**

**The same two doors — the WIZARD (`handleAddStudentStep`) and the INLINE add (`addStudentAndReply`) — have
TWO more of these, both older than today's:**
1. 🔴 **AC-9's duplicate check — `decideDuplicate` — runs only in the wizard.** `add น้องเอ` twice creates two
   students named `น้องเอ` in the same household; the wizard would have asked for more detail. **The AC that
   said *"a duplicate asks for MORE DETAIL, never demands a rename"* is true of one door.**
2. 🔴 **AC-11's admin notification — `notifyAdmins({ kind: "student_registered" })` — fires only from the
   wizard's confirm.** A child added inline is a child no admin is told about. **"The admin is told" (TASK-152's
   lesson, loud SKIPPED rows and all) is true of one door.**
3. 📌 **Weaker, and I name it because it is the same shape:** the wizard's `assertCanAddStudent` courtesy check
   at the FIRST step is not on the inline door — **but this one is fine**: the write's own precondition still
   enforces it, which is exactly why it was extracted. The difference is a worse message, not a missing rule.

🔑 **The pattern in all three plus today's: `addStudentAndReply` predates the wizard, and every rule written FOR
the wizard (TASK-233, TASK-245) was written INTO the wizard.** *"The fix went where the report came from"* — and
the report always came from the wizard, because that is the door with a prompt to get stuck in. **The inline
door never generates a report: it succeeds, wrongly, and silently — the same reason `Add Student` sat unnoticed.**

**BALL: @Sober — TASK-313 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ⏱️ **ONE line left on TASK-313 and I told @Porter NOW, not at the end.** 🔑 **His principle's test found a defect on its FIRST run.** 📋 **TASK-314 cut and kept OUT of the release.**

**Reproduced:** `bun test` **1904 pass / 0 fail** (5629 expects, 153 files) · **35 = 35** · typecheck 0 ·
🔴 **`add เมนู` no longer writes a child named `เมนู`.**

### 🔑 The mechanical test paid for itself immediately
**Parsing `menu_body` found TWO advertised tokens unreserved — `เพิ่มนักเรียน` and `Add Student`** ⇒
**`add เพิ่มนักเรียน` would have written a child named `เพิ่มนักเรียน`.**
📌 **A test written to satisfy @Porter's principle found a defect the principle had not been stated about** —
**and it was HIS call to demand an assertion rather than accept a list.**
✅ **Closed without a second list: `isReservedWord` CONSULTS `parseAddCommand`, so the regex stays the single
source.** 🔑 **A guard whose reach is DERIVED from what the product prints catches words nobody remembered.**

### ✅ And @Jason kept the strike honest rather than nominal, unasked
**`strikeOrPrompt` counts on a session ROW, and a linked parent typing inline may have none** ⇒ **the update would
be a silent no-op and the second reserved word would never hand over.** ✅ **So the refusal drops the parent into
`AWAIT_STUDENT_NAME` first and counts from there.** 🔑 **The difference between applying a rule and applying its
EFFECT.**

### ⚠️ The DoD line that was NOT met — and @Porter had already ruled it
**`add child` was left as accepted-and-creates-`child`.** **His instruction was more specific and I carried it
into §1 without emphasis:** *"…and when it is typed, it must add a child and NOT name one `child`."*
⇒ ✅ **`add child` becomes the PHRASE, exactly as `Add Student` is — one shape, three phrases.**
🚫 **`child` does NOT join `RESERVED_WORDS`** — ⚠️ **that would REFUSE a parent who typed the retired phrase
instead of SERVING them.** 🔑 **Reserving is for words we ADVERTISE.**
📌 **@Jason flagged it as *"recorded, not fixed — @Porter's word, not mine"*: right to flag, right not to decide.**
**It needed no new word from @Porter, only the emphasis I failed to give his existing one.**
⏱️ **Told @Porter it is one line and that the handover comes when it lands, not before** — **he asked to hear a
slip the moment I know, and this is the smallest possible version of that.**

### 📋 TASK-314 — the pattern behind FOUR defects this week, deliberately not in the release
🔑 **@Jason's sentence is the finding:** ***"`addStudentAndReply` predates the wizard, and every rule written FOR
the wizard was written INTO the wizard. The inline door never generates a report: it succeeds, wrongly, and
silently."***
🔴 **Two more:** **the duplicate check** (`add น้องเอ` twice ⇒ two identical PERMANENT records) **and the ADMIN
NOTIFICATION** — **a child added inline is one no admin is told about.** 📌 **The second first: *a roster that grows
without anyone being told is how the shop finds out from a parent.***
⚪ **And I recorded his third as NOT an instance** — the cap's courtesy check is wizard-only and harmless, because
the write's precondition still enforces it. **A worse message, not a missing rule.**
🔑 **All four were found by ASKING, none by failing** — **the argument for the DoD Questions, and equally the
reason none of them is urgent enough to move a deploy.**
⚠️ **TASK-314's Question invites him to tell me the pattern is NOT general if that is the truth** — **that would be
the more useful answer.**

**BALL: @Jason — one line, then the release is closed.**

---

## Jason (BE) — ✅ TASK-313 §5 DONE — `add child` is the phrase, not a name. → @Sober **1905 pass / 0 fail**, 153 files · 🚫 **35 = 35** · tsc 0 (`bunx --package typescript@5.6.3 tsc --noEmit`).
✅ **`add child` is the COMMAND, exactly as `Add Student` is** — one regex alternative (`student|child`), one shape, three phrases: `add child` · `AdD ChIlD` · `addchild` → the prompt; **`add child Emily` → `Emily`**; `addchildemily` → nothing. 🚫 `child` is NOT reserved (asserted) — the bare PHRASE is, through the one definition, like its siblings.
🔑 **Break it and watch:** `child` dropped from the alternative → `add child → {"name":"child"}` again, 2 fail (the §5 assertion in each of the two files); restored; green above.
📌 The parser's doc-block carries §5 next to the `Add Student Emily` decision, so the three phrases are explained in one place.

**BALL: @Sober — §5 done; the release is closed on my side. TASK-314 next, off the clock.**

---

## Jason (BE) — ✅ TASK-314 DONE (code) — two rules MOVED to two different homes, a green mutation caught, and the pattern is NOT general: add-student was the one feature with two WRITERS. → @Sober **1914 pass / 0 fail**, 154 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change. Off the clock, not in the release.

- [x] 🔑 **`add น้องเอ` twice behaves as the WIZARD does** — the inline door asks the same question and the parent
      continues in the wizard from its detail step · **AC-9's rule unchanged** (more detail, never a rename, not a strike)
- [x] 🔑 **A child added INLINE notifies the admin** — through the one `notifyAdmins`, so TASK-152's loud SKIPPED
      row (`skipReason: "no admin recipient configured"`) is inherited, not re-implemented
- [x] **The wizard's behaviour is byte-identical** — asserted by ORDER on both branches it touched
- [x] **Direction stated for each** — below, and they went to DIFFERENT homes on purpose
- [x] 🔑 **Break it and watch — twice, and the second one caught ME** (below)
- [x] 🚫 **The cap's courtesy check left alone** — asserted, with §3's reason in the test

### 🔑 §4 — the DIRECTION each rule moved, and why they did not move to the same place
**Both moved OUT of the wizard. Neither moved INTO the service. They landed in two different handler helpers —
and that is because they are two different KINDS of rule, not untidiness.**
- **AC-9 (duplicate → more detail) → `duplicateOutcomeFor` + `askMoreDetail`, handler helpers.** The rule's
  whole content is a QUESTION, and only a handler can ask one. The pure `decideDuplicate` did not move and has
  exactly one caller. ⚠️ On the inline door the question drops the parent into `AWAIT_STUDENT_DETAIL` — the
  wizard's own next step — so a duplicate inline add simply joins the wizard from there.
- **AC-11 (admin notified) → `createStudentFromLine`, the ONE LINE-side creator both doors call.** 🚫 **Not into
  `createStudentForParent`** — that service function is also the staff screen's write (`routes/api.ts:54`,
  `parent.service.createStudent`), and an admin adding a student would be notified of their own act. The rule
  is *"a parent registered a child over LINE"*; it lives on the LINE side. **`createStudentForParent` now has
  ONE caller in the handler and `student_registered` ONE site — the notification cannot be skipped by
  construction.** Row first, then message, as before.

### 🔴 Break it and watch — and the second mutation was green, which is the finding
- **A. the creator stops notifying** → 4 fail (two mine, two of TASK-233's AC-11 pins re-pointed). ✅
- **B. the inline door skips the duplicate question (`if (false && …)`)** → 🔴 **9 pass, 0 fail.** My two pins
  (`indexOf` ordering, `toContain` of the helper call) were satisfied by the TEXT of a disabled condition.
  **A green mutation proves nothing, and I nearly reported it as coverage.** ✅ Tightened to the exact guard
  line; re-run → 1 fail, the right one. The test carries the reason next to the pin.

---

## ❓ THE QUESTION — older path / newer path, where the newer got the rules. **The honest answer: the shape is NOT general here, and I can show why.**

I counted where each keyword door and each postback door LANDS. **Nine of eleven `do*` handlers are reached
from BOTH doors** — `doMenu` (4 ways), `doQr`, `doChildren`, `doCallAdmin`, `doMyCourses`, `doCheckinBooking`,
`doLeaveBooking`, `doTeacherSchedule`, `doTeacherCalendar` — the typed word and the tap converge on ONE function
within a line, and the rules live in that function. `doCheckin` / `doLeave` are single-door only because the
tapped form carries a booking id and goes straight to the `…Booking` sibling — same convergence, one hop later.
🔑 **Add-student was the ONE feature with TWO WRITERS** — `addStudentAndReply` (older, inline) and the wizard's
confirm (newer) — instead of two doors converging on one. **That is why four instances landed in one feature
in one week and none anywhere else.** After TASK-313/314 it has one writer too (`createStudentFromLine`).
📌 **Two pairs that DO exist, named so you know where to look — neither is a defect today:**
1. **`verifyAndLink`: existing parent (older) vs new parent (newer).** The 2FA branch exists only on the
   existing-parent path — correct, a new parent has nothing to protect — but it is the same shape: a rule on
   one branch of two. If 2FA ever becomes "on link", the new-parent branch is the one nobody will remember.
2. **`handleFollow` vs the unlinked-postback fallback** — both send `welcome`, converged today; they diverged
   once before (TASK-231's silence rules reached one and not the other) and were re-converged by hand.
⇒ **Look for TWO WRITERS, not two doors.** Two doors that converge are safe; a feature whose second entry
point grew its own write is where the next one is.

**BALL: @Sober — TASK-314 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — 🟢 **RELEASE CLOSED and HANDED OVER.** 🔑 **And @Jason's GREEN mutation is the week's most important finding — about MY method, not his code.**

**Verified by me:** backend **1914 pass / 0 fail** (154 files) · typecheck 0 · **35 = 35, no migration anywhere** ·
front end **183/0**, `contract.ts` untouched. ✅ **And I ran the parser myself:** `add child` → the prompt ·
`add child Emily` → `Emily` · `addchildemily` → nothing · 🚫 `child` not reserved.

### 🔴 A green mutation proves nothing
**Mutation B disabled the inline duplicate question (`if (false && …)`) and the suite came back 9 pass, 0 fail** —
**his pins were an `indexOf` ordering and a `toContain`, and both were satisfied by the TEXT of a condition that
can never run.**
> *"A green mutation proves nothing, and I nearly reported it as coverage."*

🔑 **That is the limit of the technique I have pushed hardest all week.** **Source-text pins are the only way to
assert a DB-bound path from a pure test — and they cannot tell a live line from a dead one.** ⇒ ***a source pin
proves a line EXISTS, never that it EXECUTES.***
✅ **He caught himself, tightened to the exact guard line, and put the reason beside the pin.** ⚠️ **Told @Porter,
because it applies to every "asserted by source" claim either of us has relayed this week.**
📌 **New rule recorded: chase a green mutation, never file it — it is evidence the ASSERTION is in the wrong
place.**

### 🔑 He told me the pattern I was hunting is NOT general — the answer I asked for
**I invited him to say so if it was not, and he COUNTED instead of reasoning:** **nine of eleven handlers are
reached from both doors and converge on ONE function within a line.**
⇒ ***add-student was the ONE feature with TWO WRITERS***, which is why four instances landed there in one week and
none elsewhere. ✅ **It has one writer now.**
📌 **His rule beats mine:** ***look for TWO WRITERS, not two doors.*** **Two doors that converge are safe.**
✅ **Two pairs named anyway, neither a defect** — and **`handleFollow` vs the unlinked fallback earns its place
because it DIVERGED once and was re-converged by hand.**

### ✅ §4's directions, and why they differ
**AC-9 → handler helpers**, *"the rule's whole content is a QUESTION, and only a handler can ask one"*.
**AC-11 → `createStudentFromLine`, NOT `createStudentForParent`** — 🔑 **that is also the STAFF screen's write, so
an admin would be notified of their own act.** ⇒ **one caller, one notify site, unskippable by construction** —
**third time this week he has answered "assert they agree" by removing the second one.**

### ⚠️ The honesty point in the handover
**TASK-314 was off the clock but its code IS in the tree, so it ships.** 🔑 **"Not in the release" meant "not
required for it", never "will not ship"** — **the backend is one unit.** ✅ **Reviewed, green, no migration.**
⚠️ **Holding it would be a REVERT of green reviewed work to satisfy a label; I advised against it and told
@Porter it is there rather than let him find it in a diff.**
📌 **What it changes: an inline add now notifies the admin, and an inline duplicate asks for detail instead of
silently creating a second identical permanent record.**

**BALL: @Porter — release the owner. The build will not move.**

## [--:--] Porter (PM) — 🟢 HANDOVER: **release CLOSED. 1914/0 backend · 183/0 front · re-counted myself: 35 = 35, no migration.** Telling the owner to deploy `sid` (back + front).
⚠️ **Carrying Sober's honesty point up unsoftened: `TASK-314` was "not in the release" but its code IS in the
backend tree and deploys with it** — reviewed, green, no migration. **What it changes: an inline-added child
now notifies the admin; an inline duplicate asks instead of silently writing a second record.** 🔑 **"Not
required for the release" is not "will not ship" — the owner should know it is there rather than find it in a
diff.**
🔑 **The method finding, which touches every "asserted by source" claim relayed to the owner this week: a
source-text pin proves a line EXISTS, never that it EXECUTES.** @Jason's mutation came back GREEN because the
pins were satisfied by the text of a disabled condition. **He caught himself.** 📌 **Recorded so no future
"asserted at source" is read as "runs".**
🔑 **And his better rule for the whole add-student saga: *look for TWO WRITERS, not two doors.*** Nine of
eleven handlers converge on one function; add-student was the one feature with two writers, which is why four
defects landed there and nowhere else. **It has one writer now.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid` (back + front). @Tanya released on `TASK-311`; the owner runs the LINE round in parallel.

## [--:--] Porter (PM) — 🔴→✅ `db:verify` went RED on the owner's deploy, then GREEN after `db:seed-ledger --apply`. **Schema was never wrong; the LEDGER was missing 7 rows.**
**Red:** journal 35 · **schema witnesses 35 applied** · ledger `drizzle.__drizzle_migrations_scheduling` 48 rows,
**7 migrations (`0028`–`0034`) not recorded** — and drizzle would have SKIPPED them silently because a newer
row existed. 🔑 **The TASK-085 failure mode, caught by the tool built for it.**
**Fix, exactly as the tool prescribed and the deploy rules record:** `db:seed-ledger` (dry run: 35 applied · 0
not · 0 need a human · 7 to insert) → `--apply` (7 rows inserted; **the shared `__drizzle_migrations` NOT
modified**) → `db:verify` ✅ **55 rows, 35 witnessed, every migration recorded AND witnessed.**
📌 **Two Postgres NOTICEs (`schema "drizzle" already exists` · `relation already exists`) are the seeder's
idempotent `CREATE IF NOT EXISTS` — harmless, expected.**
🔑 **Why this was safe to do without a ruling: the witnesses PROVE the schema — every one of the 35 was found by
its predicate, not by its existence.** **The ledger is bookkeeping about a fact already true in the database.**
⚠️ **Box not yet stated by the owner — asked, for the record only. The procedure is identical either way.**

## [--:--] Porter (PM) — ✅ The ledger repair was on **`sid`**. Deploy (back + front) now complete. **Told @Tanya to re-run `TASK-311` — she was released before the restart actually happened.**
🔻 **My sequencing error: I released her on "deploy sid แล้ว" before `db:verify` had been run, and it turned out
RED.** **She may have looked at the OLD build.** ⇒ **told her which run to report, rather than let a pass from
the wrong build stand.** 📌 **Lesson for me: "deployed" means `db:verify` ✅ AND restarted — release QA on that,
not on the message.**

## [--:--] Porter (PM) — ✅ **The owner walked the COMMAND side on `sid` — 8 screens, matches the customer's copy.** The `§13` fixes are VISIBLE in his transcript.
**`register` accepted · no role list on screen 2 · `CONFirm` mixed case accepted · `add student` (lowercase,
spaced) PROMPTED for a name instead of creating a child named `Student` · the English menu says `Add Student` ·
address free text accepted · `ยกเลิก` discards cleanly.**
🔑 **Three items in one transcript that were defects this morning — `Add Student`, case, `add child` — and all
three are visibly fixed in the owner's own hand.** **That is the acceptance shape I said I wanted: his
screenshot IS the DoD.**
📌 **Three small copy items, MINE, filed for the next batch, not this deploy:** the cancel hint is Thai-only on
bilingual screens (**an English reader is never told they can cancel** — wording supplied) · screen 8's Thai
line lacks the customer's ✅ · **screen 4 has an EXISTING-FAMILY branch the customer's copy never described** —
**ruled: keep it, "linked" is more truthful than "registered", and TELL the customer it exists.**
⚠️ **Gap: the NEW-parent branch of screen 4 was not exercised** (he used a phone already on file). **Asking him
for one run with a fresh number.** ❓ **And the transcript has no NOTIFICATION messages — asking whether that
half was tested or is still to come.**

## [--:--] Porter (PM) — ✅ Screen 4's NEW-parent branch exercised by the owner (`0900000091`) — **line for line the customer's copy.** The command side is fully walked, both branches.
📌 **The ✅ gap is a PATTERN, not a one-off: screens 4 and 8 both carry it on the English line and omit it on the
Thai.** **One fix, two screens — widened copy item 2 to @Sober.**

## [--:--] Porter (PM) — 🔴 Owner's screenshot: **re-linking a phone with FOUR children forces "add a student".** `§6`'s no-skip applied to a family that is not empty.
**He asked *"เหมือนบังคับเลยมั้ย"* and he is right.** 🔑 **`§6` exists because *"an account with no child can do
nothing"* — a family with four can do everything.** ⇒ **the rule applies at ZERO children only.** **Ruled: no
forced prompt for an existing family; show "found your family", then the menu, with the customer's own screen-8
invitation as the optional next step** — **the shape a LATER child already has.**
🚫 **Told @Sober NOT to fix it by adding a skip to the prompt — the prompt should not be there; a skip on a
prompt that should not exist is a second wrong thing.**
📌 **Copy reused from the customer's screen 8, not invented.** ⏱️ **Asked whether it lands before or after `uat`
— he is mid-round.**
🔻 **Why it slipped: `§6` was tested on the NEW-parent path and the wizard, never on the RE-LINK path — a
"two doors" case the morning's rule (*look for two writers, not two doors*) would not flag, because both doors
converge correctly on one writer and the wrong thing is the GATE before the door.**

## [--:--] Porter (PM) — ✅ `§6.1` ratified by the owner in one line, exactly as routed: **no forced add when the family already has children; mandatory when it has none.** Dispatched to @Sober.

## [--:--] Sober (SA) — 🔴 **`§6` shipped this morning and broke on the owner's phone this afternoon. TASK-315 cut; it goes BEFORE `uat`.** 🔑 **And @Jason's "two doors" lesson applied within the hour.**

### 🔴 The defect — his own screenshot
**A phone with FOUR existing children links and is dropped into *"กรุณาระบุชื่อนักเรียน"*.** **Owner:
*"เหมือนบังคับเลยมั้ย"*.** ⇒ **a family with four children made to add a fifth, `ยกเลิก` the only exit and not
advertised in English.**
✅ **Ratified rule: 0 children → mandatory prompt (unchanged, he re-confirmed it) · ≥1 child → NO prompt:
found-your-family → the screen-8 invitation → the menu.**
🔑 **`§6`'s justification was *"a parent account with no child can do nothing"*** ⇒ **a family that has children
can do everything, so the rule never reached them.**
🚫 **Not fixed with a skip:** ***the prompt should not be there; a skip on a prompt that should not exist is a
second wrong thing.***

### 🔴 TWO doors, and the second is invisible today
**`:1327` (link success) and `:1291` (the 2FA branch, which has ALREADY fetched the children).** ⚠️ **2FA is
unreachable while the setting is off and exists so *"switching it on is a setting change and not a rebuild"*.**
⇒ **fix only the reported door and the defect returns the day someone flips that switch, certain they changed
nothing else.**
🔑 **@Jason's own lesson from an hour ago — *look for TWO WRITERS, not two doors* — and here the two do NOT
converge.** ✅ **The task requires ONE decision reached by both.**

### ✅ Nothing to write, because @Porter reused the customer's sentence
**`add_another_hint` already holds screen 8's invitation in both languages, and it now says `Add Student`** —
**the phrase TASK-313 made accepted.** 🔑 **The sentence and the parser agree: that batch paying off the same
afternoon.**

### 🔻 The part that is MINE
**TASK-307 asserted both halves I asked for — *no children cannot skip*, *with a child can still skip* — both
true, both green.** 🔴 **Neither is this case: a family with children forced into the flow AT ALL.**
🔑 **The rule was right; its BOUNDARY was assumed, and I wrote the requirement that assumed it.**
📌 ***Tests written faithfully to a requirement cannot cover a case the requirement does not mention.***
⇒ **Recorded: when a rule is about a STATE, ask what the OTHER states do and write that into the requirement
rather than leaving it to the code.**
✅ **Asked @Jason to tell me plainly whether the gap was in the assertions or the requirement** — **and said that
if it is the requirement, it is mine to carry.** ⚠️ **Told @Porter the same, before he relays anything: this is
not carelessness, it is a requirement that did not name a case.**

⏱️ **Answered his timing question: BEFORE `uat` — it is minutes.** **He holds the owner until I send the go.**

**BALL: @Jason.**

## [--:--] Porter (PM) — ✅ **The NOTIFICATION half is walked — 3 of 4 formats confirmed on the owner's phone against the customer's spec.**
**`§7.1` course-wide: field for field, `Date : Tuesday` in ENGLISH, and `Remark` correctly ABSENT (this course
has none — `§8.1`'s `*ถ้ามี` rule).** **`§7.3` per-session: exactly their field list, English, `Remark` present,
no `Coach`.** **`§7.2` COMMAND: one language, `Remark`, and their COMMAND shape rather than the AUTO one.**
⚠️ **Two things I am NOT calling verified:** **the `(-)` empty-leave form** (every shot so far has leave dates)
· **`§7.4` LEAVE NOTICE**, which is the only NEW message and the only one that tests WHO RECEIVES IT.
📌 **Two copy deltas, mine:** **the daily header is `📅 Today's schedule` where the customer wrote
`⏱️TODAY'S SCHEDULE:`** — 🔑 **and `CONFIRMED SCHEDULE:` DID keep their form, so we are inconsistent with
ourselves, which matters more than which is right** · **`This week's schedule` is a FIFTH message they have
never seen** — **same ruling as the existing-family branch: keep it, and TELL them.**
🔻 **Held both as copy items rather than escalating.** **I have over-escalated three times in two days; a header
emoji is not a defect and saying so is part of the job.**

## [--:--] Porter (PM) — ✅ **`(-)` VERIFIED — `§8.1`'s two opposite rules are BOTH proven.** 🔴 **New: `ลา` scans TODAY only, which makes `§7.4` unreachable.**
**Course with no advance leave → `**Advance Leave Notice : (-)` AND no `Remark` line.** **Course with leave →
dates listed.** **Per-session with a note → `Remark : wwd`.** 🎯 **The trap I named before the build — one field
ALWAYS prints, the other NEVER prints when empty — confirmed in both directions on real data.**
🔴 **`§14` filed:** `ลา` answers *"วันนี้ไม่มีคาบที่แจ้งลาได้"* and stops ⇒ **a parent whose child has class on
Thursday is told on Tuesday there is nothing to do.** 🔑 **The feature only works on the day of the class —
which is also the day the cutoff most often refuses it** ⇒ **a parent can be TOO EARLY and TOO LATE at once and
get the same sentence.**
📌 **Recorded my boundaries as MINE:** offer only sessions still eligible under the cutoff · ask which child
only when more than one qualifies · list DATE/TIME/PROGRAM, never a bare number · and distinguish "nothing
upcoming" from "the cutoff has passed".
⚠️ **The cost is the headline, not the flow: `§7.4` LEAVE NOTICE is UNVERIFIED because its TRIGGER is
unreachable** — **the only new message of the four, and the only one testing WHO receives it.**
🔻 **Today's release therefore ships with `§7.4` unverified, and I will say that plainly rather than let "3 of 4
passed" round up to done.**

---

## Jason (BE) — ✅ TASK-315 DONE (code) — the returning family is no longer forced, ONE decision for both doors, and a failed RESTORE I caught by reading. → @Sober **1925 pass / 0 fail**, 155 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change, **no new key.**

- [x] 🔑 **The owner's exact case: FOUR children link and get NO name prompt** — the found-family line, the
      screen-8 invitation, then the menu
- [x] 🔑 **ZERO children still gets the mandatory prompt** — asserted, *and asserted that it gets NEITHER the
      invitation nor the menu*, so `§6` cannot quietly become "no prompt"
- [x] 🔴 **Both doors reach ONE decision** — `afterParentLink`, and **neither door decides for itself** (asserted
      as an absence on both slices)
- [x] **No session step is left set on the ≥1 path** — `clearSession`, asserted on the branch
- [x] **`add_another_hint` reused; no new i18n key** — asserted as an absence
- [x] 🔑 **Break it and watch** — restored, suite green before this number
- [x] 🚫 **The zero-child path and TASK-307's guard untouched**

### §3 ✅ One decision, and the tail carries its own separator
`afterParentLink(lineUserId, lang, known?)` returns the tail **with its leading separator**: a single newline for
the zero-child case, which keeps `§17c` screen 4 byte-for-byte (TASK-310), and a blank line for the returning
family — **who are not on that screen at all.** ⇒ both doors are now `head + tail` and neither contains
`setStep`, `add_student_prompt` or `kids.length`. 📌 The 2FA door passes the `kids` it had already fetched, so
the decision costs no second query and TASK-047's greet-by-name is untouched.

### 🔻 Two things I got wrong on the way, both caught by the discipline rather than by luck
**1. My first §3 assertion counted `setStep(…, "AWAIT_STUDENT_NAME", …)` and expected 3. There are 4** — the
inline add's prompt, TASK-313's reserved-word refusal and the `register` postback all set it legitimately.
⇒ **a count that includes them measures the wrong thing.** ✅ Replaced with the property I actually meant:
**neither door contains the decision**, asserted as an absence on both slices. **The wrong version is named in
the test so the next reader does not re-add it.**
**2. 🔴 My mutation script's RESTORE threw, and the `bun test` after it never ran** — the `1925 pass` I saw was
**stale output from before the mutation**, and the working tree still had `if (true)` in it. ⚠️ **I only caught
it because I read the file instead of trusting the number.** ✅ Restored, verified by reading the function back,
and the green above is from a run AFTER that. 📌 **The lesson is mine to carry: a restore that fails silently
turns "break it and watch" into "break it and ship it".** From here the restore is verified by READING the line,
not by the exit code of the script that wrote it.

---

## ❓ YOUR QUESTION — honestly: **the gap was in the REQUIREMENT, not in the assertions.** But it is not yours alone.

**TASK-307's two assertions were the right two for what `§6` said.** *"No children cannot skip"* and *"with a
child can still skip"* are both about **a parent already inside the add-child flow**. This defect is about
**whether a family should be in that flow at all** — 🔑 **a question the requirement never asked, so no
assertion could have been written against it.** ⇒ **it is not a missing test; it is a missing sentence.**

📌 **And I would name the SHAPE rather than the sentence, because the sentence is easy to write afterwards:**
**`§6` was specified for the population it was ABOUT — parents with no children — and silent about the
population it would also REACH.** 🔑 *A rule that names who it protects, and not who it touches, is a rule with
an assumed boundary.* **The mandatory prompt reached every linked customer; only one of the two groups was ever
considered.**

⚠️ **And I will not let this sit entirely on you.** I built `§6` and I read `verifyAndLink`'s customer branch —
**the line I changed today is the line I was standing on** — and I did not ask *"who else lands here?"*. **The
requirement was silent; the code was in front of me.** 📌 The cheap habit, and it is the same one that has paid
out four times this week: **when a rule is written for a population, ask what the OTHER population does at the
same call site.** ⇒ *"look for two writers"* found the sibling DOOR; this one is its twin — **look for the
sibling POPULATION.**
🔑 **Neither is a test you could have asked for. Both are a question asked at the right moment**, which is why I
think the honest answer to *"assertions or requirement"* is **requirement — and the fix is a reading habit, not
a coverage target.**

**BALL: @Sober — TASK-315 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — 🟢 **`§6.1` in, tree RE-VERIFIED from scratch, GO sent.** 🔴 **Because @Jason reported a near-miss that could have shipped a mutation.**

**Verified BY ME, not read from a report:** `bun test` **1925 pass / 0 fail** (5725 expects, 155 files) ·
typecheck **0** · **35 = 35** · **`grep "if (true)"` across `src` → nothing** · **`afterParentLink` called from
BOTH doors** (`:1332` with the `kids` the 2FA branch already had, `:1373` without).

### 🔴 The near-miss, reported against himself
> *"My mutation script's RESTORE threw, and the `bun test` after it never ran — the `1925 pass` I saw was STALE
> output, and the working tree still had `if (true)` in it. I only caught it because I read the file."*

🔑 ***"A restore that fails silently turns 'break it and watch' into 'break it and ship it'."***
📌 **Third methodological finding he has handed me in two days, and the most dangerous:** **the other two cost a
wrong BELIEF; this one costs a wrong DEPLOY.**
✅ **His rule: verify a restore by READING the line, not by the exit code of the script that wrote it.**
✅ **Mine, adopted from it: any report mentioning a mutation gets the suite RE-RUN by me rather than read.**
⚠️ **I applied it immediately — which is why this handover says "verified by me" and means it.**

### ✅ The fix
🔑 **`afterParentLink` returns the tail WITH its leading separator** — a single newline keeps `§17c` screen 4
byte-for-byte; a blank line serves the returning family, who are not on that screen. ⇒ **neither door contains
`setStep`, `add_student_prompt` or `kids.length`, asserted as an ABSENCE on both slices.**
✅ **The 2FA door passes the `kids` it had already fetched** — no second query, greet-by-name untouched.
📌 **And he recorded the assertion he REJECTED inside the test** (a `setStep` count that would have measured the
wrong thing) **so the next reader does not re-add it.** 🔑 **The obvious-but-wrong version is what a future reader
reaches for.**

### 🔑 His answer to my question, and it is better than the question
> *"`§6` was specified for the population it was ABOUT — parents with no children — and silent about the
> population it would also REACH."*

🔑 ***A rule that names who it protects, and not who it touches, is a rule with an assumed boundary.***
⇒ ***"look for two writers" finds the sibling DOOR; "look for the sibling POPULATION" finds this.***
✅ **He confirms the gap was the REQUIREMENT, not the assertions — *not a missing test; a missing sentence*** —
**and declined to leave it entirely with me: *"the line I changed today is the line I was standing on."***
📌 **I accepted the requirement half and did not split it further.**

**BALL: @Porter — GO. The build will not move again.**

## [--:--] Porter (PM) — 🟢 `§6.1` in: **a linked family with children gets NO forced name prompt; ZERO children still does.** Re-counted migrations myself: **35 = 35.**
**backend 1925/0 · front 183/0 · typecheck 0 · no migration.** ✅ **Both doors go through ONE decision, 2FA branch
included** ⇒ **turning that setting on later cannot bring the defect back.**
🔴 **@Sober re-ran the whole suite himself rather than accept a number, because @Jason reported a near-miss
AGAINST HIMSELF:** **his mutation script's RESTORE threw, the run after it never happened, and the number he
first saw was STALE — the tree still had the mutation in it.** **He caught it by READING the file.**
🔑 ***"A restore that fails silently turns 'break it and watch' into 'break it and ship it'."***
📌 **Sober adopted it as a review rule: any report mentioning a mutation gets the suite RE-RUN, not read.**
⚠️ **This is the only class of error this week that could have reached the owner as a BROKEN DEPLOY rather than
a wrong belief** — every other miss cost time; this one would have cost the product.
🔑 **And @Jason's answer on why `§6` slipped is better than the question I asked:** ***"`§6` was specified for the
population it was ABOUT — parents with no children — and silent about the population it would also REACH."***
⇒ ***a rule that names who it protects, and not who it touches, is a rule with an assumed boundary.***
📌 **Its twin to "look for two writers": LOOK FOR THE SIBLING POPULATION.** 🔻 **And it lands on me — I wrote
`§6` from his sentence about empty accounts and never asked who else would meet the prompt.**

## [--:--] Porter (PM) — 🔻 CORRECTION: **`§7.4` LEAVE NOTICE is NOT unreachable. It has TWO doors and I only looked at one.**
**I told the owner the leave notice could not be tested because `ลา` scans today only.** 🔴 **Wrong.**
**`TASK-306` made `sendLeaveNotice` a single builder with BOTH doors calling it — the LINE `ลา` path AND the
admin plan editor's `mark-absence`.** ⇒ **an admin marking a session absent in the WEB triggers the same
message, built by the same function, asserted by count.**
✅ **So the owner can verify `§7.4` right now, from the screen, without `§14`.** ⇒ **`uat` does not have to ship
it unverified, and `§14` stops being a blocker for anything.**
🔻 **The miss is mine and it is the SAME SHAPE as `§6`'s:** **I reasoned about the population I was looking at
(a parent in LINE) and was silent about the one that also reaches it (an admin on the web).** ⚠️ **@Jason named
that pattern three hours ago — *look for the sibling population* — and I walked into it while holding the
sentence.**
📌 **`§14` still stands on its own merits: a parent cannot declare leave except on the day of the class.**
**It is a real defect. It is just not the gate I said it was.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid` (back + front, `§6.1`). @Tanya released on the FINAL build for `TASK-311`; the owner runs three LINE/web checks in parallel.
**His three: the four-child family must NOT be forced to add · the LEAVE NOTICE via the ADMIN WEB door (not
`ลา`) · nothing else.** **Hers: the expiry control on the card, the warning before save naming sessions, the
dead `Create plan` gate.**
📌 **Told her to state which build she is reporting** — **she was released twice today on builds that moved
under her, both times because I released on the word "deployed" rather than on `db:verify` ✅ plus restart.**

## Tanya (QA) — `TASK-311` on the FINAL build: ✅ **check 3 PASS** · 🔴 **checks 1 & 2 blocked on the pane**

✅ **CHECK 3** — `POST /courses/preview {size:4, absentWeeks:[2,3,4]}` → **200**, **`exceedsCeiling: FALSE`**,
`liveCount 4`, expiry stretched to 2026-11-25. ⇒ **the dead week-ceiling gate is gone**, tested at the preview,
which is where the refusal used to happen. ⚪ **Limit: API route, not the `Create plan` button** (clicked in
Round 18, but a different build, so not carried forward).
🔴 **CHECKS 1 & 2 `NOT_TESTED`** — both pure-UI. **The pane authenticates via fetch (`/api/auth/session` → `qa`)
but every navigation bounces to `/login`; the `__Secure-` cookie is no longer stored for document requests.**
**Three attempts, three mechanisms; stopped per the standing rule — the pane is @Porter's to fix.**
🔑 **Reported NOTHING about those two screens** — I never reached a rendered page, so no negative observation
exists here to be mistaken for evidence.
📌 **Not an inconsistency:** make-ups landed `11/18/25 Nov` here vs `14/21/28 Oct` in Round 18 — **my own Round-18
fixture occupies those October slots**, and `TASK-300` §4 requires occupied slots to be skipped. **Box state, not
build difference.**
🟢 **Footprint nil** — a preview route computes and returns, it does not persist.

## [--:--] Porter (PM) — `TASK-311`: ✅ check 3 PASS (server half) · 🔴 **checks 1 and 2 `NOT_TESTED` — the PANE, not the product.**
**Check 3:** `POST /courses/preview {size:4, absentWeeks:[2,3,4]}` → **200**, `liveCount 4`, **`exceedsCeiling:
FALSE`** ⇒ **the very field the dead gate tripped on, and the refusal is gone at the point where it used to
happen.** ⚪ **API preview, not the BUTTON — she clicked the button successfully in Round 18 and refused to
carry it forward because I had told her that was a different build.** 🔑 **Correct, and it cost her a pass.**
🔴 **Checks 1 and 2 are pure UI and she never reached a rendered page:** the minted cookie authenticates
(`/api/auth/session` → `qa` from inside the page) **but every full navigation bounces to `/login`** — the
`__Secure-` cookie set from JS is no longer stored for DOCUMENT requests. **Three attempts, then she stopped
per the standing rule.**
🎯 **And she applied the other standing rule without being asked: she never reached a page, so she reported
NOTHING about what those screens show** ⇒ **no negative observation in the round to be mistaken for evidence.**
🎯 **She also pre-empted a false inconsistency:** make-ups landing on `11/18/25 Nov` vs Round 18's `14/21/28 Oct`
— **her own Round-18 course still occupies those slots, so the search skipped them, which `TASK-300 §4`
REQUIRES.** ⇒ **a difference in BOX STATE, not between builds.** **Flagged before anyone could ask.**
🔻 **The blocker is MINE and it is the third harness failure this week** — `claude-in-chrome` is still `[]`
(checked). **`REQ-080` (narrow the guard so QA can hold a session) has been `READY_FOR_SA` and unshipped since
before this release, and it has now cost two rounds.** ⇒ **it moves up the owner's batch list; it is no longer
a convenience.**

## [--:--] Porter (PM) — 📌 The owner clicked `Ends 27 Oct 26` in the plan modal and got a text selection. **Not a defect — the control is on the CARD (`expires …`), verified in the source. But it is a real finding.**
🔑 **TWO date-shaped things, one clickable, nothing distinguishing them:** the card's `expires 2026-10-24` (the
control) and the modal's `Ends 27 Oct 26` (derived, correctly inert — @Fern's own ruling).
🔴 **He is the SECOND person to trip on it** — @Fern named the risk before it existed, and he walked into it
within a minute of being told the feature was there. ⇒ **"nobody has complained" would have been worthless
evidence: he is the first person who tried.**
✅ **Copy fix, mine: the modal line becomes `Last session : 27 Oct 26`** — **it says what the value IS rather
than a near-synonym of "expiry"** ⇒ **`expires` on the card is left as the only expiry-shaped word in the
product.** 🚫 **Next batch; nothing is broken.**
🔻 **And my instruction was part of it: I told him "the `expires …` date top-right of a course CARD", then he
was inside the plan modal, where a date sits top-right too.** **A location described in words survives only
until the user is somewhere else.**
