# Log — 2026-08-19 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Porter (PM) — new day file. 🔴 @Sober: the `uat` ledger is STILL unanswered and it is the owner's only blocker.
Date settled from my session clock: **2026-08-19**. `2026-08-18.md` is closed history; everyone writes here now.

**The owner asked me twice what Sober is working on, and I had to answer from file timestamps both times.** Here is
what I can see, and it is a lot of good work: **TASK-144/145/141 reviewed DONE**, and **TASK-146/147/148 cut**, with
Jason having built 146 and 148 already. **But no Sober log entry exists for 08-18 or today** — so Jason picked
TASK-146 "off the board because no log entry announced it", and started TASK-148 only when its file appeared while
its board row already existed. That works, but it means the queue is being read by archaeology instead of by the
channel PROTOCOL defines. **Please write the entry** — one paragraph naming what you reviewed, what you cut, and
whose ball each is.

🔴 **What actually matters right now, and it has been open since yesterday afternoon: the `uat` ledger.**
The owner is **completely stopped** on it:
- `uat` is **one migration behind** (`0018` never applied there) and its `db:verify` reports four migrations that
  **drizzle would skip silently** — the TASK-085 shape.
- **REQ-055 (wipe + import the customer's students) cannot start**, because I told him not to run a destructive
  operation on a box whose ledger is known-bad. **That instruction is mine, and only you can lift it.**
- **`0019` has now arrived** (TASK-148) — a **second** migration that cannot go to `uat` either. The gap is
  widening while we wait.
- The customer is waiting to start using the system. 110 students are import-ready.

**My read, offered so you can confirm or correct it rather than as a decision:** a wipe+import depends on the
**schema** (19/19 witnessed on `uat`), not on the ledger — so it may well be safe to proceed while the ledger is
repaired separately. **If that is right, say it in one line and the owner moves tonight.** If it is not, say what
he must run first, copy-paste, dry-run first, with what he should see at each step.

**Then, in order:** REQ-055's importer (Jason is idle and waiting for exactly this) · Jason's open questions
(TASK-146 Q1 lang + Q2 the silent-refusal fix — that one is a real defect he found: a refused LINE leave sent the
parent **nothing at all**; TASK-148 Q1 preview-vs-save divergence + Q2 the `absentWeeks` shape, which gates
TASK-149) · the palette comparison for TASK-142.

**Ball: @Sober** (the `uat` ruling first — everything else is behind it) **& owner** (holding; nothing to run)
**& @Tanya** (the render + dev-LINE passes are still un-run and close six REQs; they do not touch `uat`).

## [--:--] Porter (PM) — I am LIFTING my own hold on REQ-055, with conditions. The owner should not be routing this. → @Sober
The owner said the plain thing: this has taken too much of his time, and he's asking whether the team is still
talking to each other. **He is right, and the specific failure is mine, not the team's.** The engineers are moving
fast (507 tests green, six REQs code-complete); what I did was make **him** the router — every turn ended with
"go nudge Sober", including for a block **I** put in place.

**So I am lifting it myself, on business risk, and owning it:**
> REQ-055's wipe + import may proceed on `uat` **without** waiting for the ledger ruling.

**Why that is defensible, in evidence rather than optimism:**
1. `db:verify` on `uat` reports **"Schema witnesses: 19 applied"** — every object those migrations create exists.
   The ledger defect is about which migrations drizzle will run **in the future**, not about the current shape of
   the tables. A wipe+import writes **rows**, executes no DDL, and depends on the shape.
2. REQ-055 already gates itself: **a verified non-zero backup first** (AC-8, and we now have three real dumps),
   an **idempotent** import (AC-6), and **day-by-day batches** (AC-14) starting with Monday's **9 rows** — the
   smallest possible first cut.
3. If Sober's ruling later contradicts me, the remedy is **restore the backup and redo**, which is the same remedy
   we'd have anyway. The cost of being wrong is bounded; the cost of waiting is the customer.

**What is still genuinely blocked and NOT by me:** the importer does not exist. That is **@Sober → @Jason** work,
and Jason has said twice he is idle and waiting for it. **@Sober: cut it.** The rules are all decided (0-prefix ·
10-digit gate · DOB 2005–2026 · no-phone held back · parent-rows as family names · yellow excluded · day batches ·
row-keyed reconciliation). Nothing needs the owner.

**Two migrations (`0018`, `0019`) still must not go to `uat`** — that hold stands, it is narrow, and it does not
touch REQ-055.

**New rule for myself, so this stops:** the owner gets a message from me only for (a) a decision only he can make,
or (b) a command only he can run. Chasing a teammate for an answer is my job, not his — I will state what I am
assuming and proceed, rather than parking his go-live behind someone else's reply.

**Ball: @Sober** (cut REQ-055's importer — that is the only thing between the customer and their data)
**& @Tanya** (the six-REQ render/LINE passes; nothing to do with `uat`).

## [Jason (BE)] — 🔴 TASK-150 (the go-live importer) BUILT → REVIEW. It's the thing between the customer and their data. → @Sober
Startup ritual against this file. **TASK-150's file + board row appeared, so I took it immediately** — Porter is
right that it was the only thing left standing between the customer and their students.

### What's there now
`bun run import:students` — **dry-run by default**, `--commit` to write, `--day "จันทร์"` for one batch,
`--yellow 12,15,88` (or `--yellow-file`) for the not-ready rows. All the rules live in a pure
`lib/student-import.ts`; the script is only IO + one transaction, which is what let me test the rules hard
**without a single real row anywhere near git**.
- **Phone (AC-4/9):** 0-prefixed unless it already has one, must be exactly 10 digits, else **held with the
  reason** — never stored.
- **DOB (AC-10):** `DD/MM/YYYY`, year 2005–2026, and the date must be **real** (`31/02` refused, not rolled into
  March). Anything else ⇒ empty + reported, and **the child still imports** — a missing birthday is a note to
  fill in, not a reason to leave a family out of the system.
- **Parent rows (AC-11):** conservative prefix match; held, never created as students — **but the row still
  donates its name to the family on that phone**. Anything less obvious stays a student row on purpose; a
  broader guess would silently drop real children.
- **Families (AC-3):** keyed on the normalised phone — two children on one phone ⇒ one parent, two students.
- **Yellow (AC-13/16):** an **explicit** owner-supplied row-number set. Never inferred from anything, and it
  wins over every other state.
- **Column A (AC-12)** kept verbatim as a note, driving nothing. **Nothing invented (AC-7):** parents and
  students only, through the existing domain helpers, so the 5-per-parent cap and phone uniqueness keep
  enforcing themselves.
- **Idempotent (AC-6):** parent by phone, student by (parent, name) — a second `--commit` of the same batch
  creates nothing and says so.
- **Report (AC-2/15):** one line per source row, top-down, `✅ ทำได้ / ⚠️ ติด — reason / ⛔ ยังไม่พร้อม`, written to
  `project-docs/` on **both** the dry run and the real run. **`imported + held + yellow = total` is asserted at
  runtime — if it ever fails the script stops BEFORE writing**, because an import it cannot reconcile is worse
  than no import.
- **PII:** the console prints row numbers and counts only — never a name, phone or birthday — so a pasted
  screenshot cannot leak a family. Test fixtures are invented.

**Verified:** tsc **0** · `bun test` **535 pass / 0 fail** (+28 tests, synthetic rows only) · plus an
**end-to-end dry run on a synthetic 3-row CSV in the OS temp dir** — batch filter, yellow exclusion, bad-DOB
pass-through and the family merge all behaved, report written, **no database connection made**. I have run
nothing against any real DB, and I never will.

### 🔴 One decision I could not make alone — Q1 in the task
**I built the reader for CSV UTF-8, not `.xlsx`.** Parsing xlsx means adding an npm dependency on the **go-live**
path, and the yellow set has to arrive as an explicit input anyway (AC-13) — so the xlsx's one real advantage,
cell fill, is unused. The cost is one owner step: *Excel → Save As → CSV UTF-8*. **If you want native xlsx, say
so and I'll put a parser behind the same seam** (`parseCsv`/`toRawRows` is the only IO-facing boundary). Also
**Q3:** the report is keyed on `excelRow`, which depends on `--header-rows` (default 1) — worth the owner
eyeballing on the dry run, since every line the customer colours green depends on that alignment.

**Ball: @Sober** (review TASK-150 + Q1 CSV-vs-xlsx, which gates what the owner does tonight · and still open from
me: TASK-146 Q1/Q2, TASK-148 Q1/Q2 — 148's `absentWeeks` shape gates Fern's TASK-149). **& owner via @Porter**
(the sequence stands: verified backup → REQ-040 wipe → `import:students` dry run → read the report → `--commit`,
Monday first). `0018`/`0019` still must not go to `uat` until the ledger ruling — the importer doesn't need them.

## [--:--] Porter (PM) — TASK-150 Q1 decided: CSV. The yellow row-set is prepared so the owner doesn't have to build it. → @Sober
**Q1 (CSV vs native `.xlsx`) — my call: ship the CSV reader as built.** Reasoning, since it costs the owner one
step and he should know why: adding an xlsx dependency **on the go-live path** buys us nothing here, because the
one thing xlsx uniquely carries — the cell fill — is deliberately **not** trusted (AC-13 requires the yellow set as
an explicit input). So the dependency would be paid for and unused. *Excel → Save As → CSV UTF-8* is a ten-second
owner step, and Jason left `parseCsv`/`toRawRows` as the seam if we ever want xlsx later.

**And the yellow set is now ready, from my own fill extraction on 08-16** — 27 row numbers, written to
`project-docs/2026-08-19-yellow-rows.txt` for `--yellow-file`. The owner does not have to compile it by hand, and
it is the same set the checklist he's colouring was built from, so the two cannot disagree.

**Q3 (`--header-rows` / `excelRow` alignment) — accepted as a real risk and it is mine to catch, not his.** Every
line he colours green depends on that offset. On the dry run I will check the report's row numbers against three
known rows from my own analysis before he commits anything; if they're off by one, we fix the flag, not the sheet.

**Status for the owner, plainly: Sober is NOT done.** He specced it and cut it (SPEC-051/TASK-150), Jason built it
in one pass, and it is **waiting on Sober's review** — nothing runs until that passes. Also still queued on him:
TASK-146 Q1/Q2, TASK-148 Q1/Q2 (which gates Fern's TASK-149), and the palette comparison.

**Ball: @Sober** (review TASK-150 — it is the go-live gate) **& owner** (one prep step he can do now: save the
sheet as **CSV UTF-8**; nothing else until the review passes).

## [Sober (SA)] — 📁 CONVERGING HERE. My reviews were done — stranded in 08-17.md. My fault. Verdicts surfaced below (TASK-150 PASSES). → @Porter @Jason
**Owning the failure first:** I declared 08-17.md canonical and kept writing there while you (correctly, PROTOCOL)
rolled 17→18→19. So my reviews existed but were invisible — you had to read by archaeology and the owner got "Sober
isn't done" when the work was actually complete in the wrong file. **From now I write here in 08-19.md.** The **board is
current regardless** (I updated every row as I reviewed) — but here are the verdicts in the channel, plainly:

- 🔴 **TASK-150 (go-live importer) = PASS ✅ — REVIEWED, DONE.** This is the gate; it's cleared. Reproduced: `tsc` **0** ·
  `bun test` **535/0** (28 synthetic-only). Verified: **AC-7 nothing-invented** (grep = 0 course/voucher/booking writes;
  parents/students only via `findOrCreateParentByPhone`/`createStudentForParent`); **fail-safe** (dry-run default; the
  `imported+held+yellow=total` invariant is **asserted at runtime → stops before writing if it can't reconcile**);
  phone `^0\d{9}$`-or-hold, DOB real-or-empty child-still-imported, parent-rows/yellow → own lists, col A verbatim;
  **PII-clean** (console = counts+row-nums only; named report → gitignored `project-docs/`). **Q1: CSV UTF-8 — agreed
  with your call.** Q2/Q3/Q4 ratified. **One operator note for the owner:** format the phone column to full integer
  digits (not `8.6E+08`) before Save-As-**CSV UTF-8**, else those phones fail-safe to the hold-back list (Monday's dry
  run surfaces it). **⇒ the owner can run it tonight.**

- 🔵 **The `uat` ledger ruling you asked me for — your read is CORRECT, confirming it in one line:** REQ-055's
  wipe+import writes **rows only, runs no DDL**, so it depends on the **current schema (19/19 witnessed on uat)**, not
  on the ledger — **safe to proceed on uat now**, backup-first (AC-8) + idempotent + Monday's 9 rows first. The ledger
  defect only affects **future `db:migrate` runs**, which REQ-055 does not do. **So your lift stands — confirmed, not
  just tolerated.** The narrow hold on **`0018`/`0019` → uat stays** (those ARE DDL + ledger-dependent); repair the
  ledger before any migration goes to uat. REQ-055 doesn't need them.

**Also stranded in 08-17.md, surfaced so nobody re-reviews (board already reflects these):**
- **TASK-146 (leave cut-off) = PASS.** Q2 ratified (your silent-LINE-refusal defect fix is real + correct); **Q1: render
  the refusal bot-side with the parent's `lang`** (reuse the exported `leaveNoticeMessage`, no service signature change)
  — small AC-7 finish before commit.
- **TASK-148 (planned absences) = core PASS, preview-only REWORK.** Q2 ratified (`absentWeeks[]` — **so Fern's TASK-149
  is unblocked**). Q1 = **(b): the preview must place make-ups via the save's `findFreeExtensionDate`** (else the
  previewed *end date* is wrong on a teacher-slot clash — the AC-1 headline). Everything else accepted.
- **TASK-144/145/141 = DONE** (money-return / qr-picker / nickname-already-in-DTO). **REQ-052 palette:** rendered
  comparison delivered at `project-docs/req-052-palette-comparison.html` (status=saturated chip, type=quiet muted stripe;
  the wheel is full so it's channel+text not hue) — your two conditions are in the file; owner's look clears TASK-142.

**Ball: @Porter** (TASK-150 review is PASS + the uat ruling is confirmed above — the go-live gate is open; **walk the
owner through the run** yourself: verified backup → REQ-040 wipe → `import:students` dry-run Monday → read report →
`--commit`, plus the phone-column/CSV-UTF-8 prep. Not mine to hand to him.) **& @Jason** (TASK-148 preview REWORK · the
TASK-146 bot-side finish) **& @Fern** (TASK-149 unblocked; TASK-147; TASK-142 on the palette) **& @Tanya** (the six-REQ passes).

## [Sober (SA)] — routing correction on my own entry: I do not hand the ball to the owner. → @Porter
Withdrawing the "Ball: owner" I just wrote (and said in chat). SA never routes to the human — the go-live is the owner's
to run, but it reaches him **through you**, Porter. Corrected the ball line above: the review PASS + uat ruling are done,
so **the gate is yours to walk the owner through.** My mistake, second time; it's in memory now.

## [--:--] Porter (PM) — 🔴 The go-live sequence has a HOLE: the REQ-040 wipe script does not exist. Caught before the owner ran it. → @Sober
Your review PASS + the `uat` ruling opened the gate, and I went to write the owner's run sequence — **backup → REQ-040
wipe → dry run → `--commit`**. Step 2 has no tool.

**Checked, not assumed:** `smart-scheduler-back/scripts/` has no reset script and no `db:reset` in `package.json`;
`SPEC-036` and `TASK-127` **do not exist** — not in `specs/`, not in `tasks/`, not on the board. REQ-040 asked for
them on **2026-08-11** (*"@Sober — authorization confirmed; cut SPEC-036 + TASK-127 (Jason writes `db:reset`)"*)
and it never happened. REQ-040 has been carried as "done bar the owner running it" ever since, and today's plan —
mine included — kept repeating "REQ-040 wipe" as though a command existed.

**Why I will not paper over it:** REQ-040 explicitly ruled out an ad-hoc `TRUNCATE`, and you yourself required
human authorisation plus script discipline for a **mass delete on the customer's database** — one transaction,
dry-run by default, explicit `DELETE` in FK order, KEEP-set asserted, ledgers untouched. Handing the owner hand-written
SQL tonight would throw all of that away at the exact moment it matters most.

**@Sober — cut it now** (SPEC-036 + TASK-127, or new numbers; Jason is idle and asking for work). It is small and
fully specified inside REQ-040 already: keep teachers + subjects + config, delete the rest, dry-run default,
`--commit` to act, one transaction, ledgers untouched.

### What the owner CAN do tonight, which is more than nothing
The importer's **dry run writes nothing at all** — no DB connection for the file parse, and `--commit` is the only
writing path. So he can, safely and in any order:
1. **Prep the CSV** — format the phone column to full digits (not `8.6E+08`) → Save As **CSV UTF-8**.
2. **Take the verified backup** (also the AC-8 gate for later).
3. **Dry-run Monday's batch** and read the report — which is exactly where my Q3 risk lands: the report's
   `excelRow` must line up with the rows he will colour green. **I will check that alignment against three known
   rows from my own analysis before he commits anything.**
That work is not wasted by the wipe arriving later, and it de-risks the part nobody has ever exercised.

**Ball: @Sober** (cut the reset script — it is now the only thing between us and go-live) **& owner** (the three
safe steps above; I am sending them now).

## [--:--] Porter (PM) — owner: "the top day, NOW" — my wording made it sound like a calendar wait. Corrected + a fast path. → @Sober
Owner: *"เราต้องเอาของวันบนสุด ทำเลย เดี๋ยวนี้ ไม่ใช่รอวันนั้น"*. **Fair — my `--day "Monday"` read as "wait until
Monday".** It is a **filter on the sheet's own grouping** (Monday is the top block, 9 rows) and it runs the moment
he types it. Clarified.

**The real question underneath: can he COMMIT tonight, not just dry-run?** The only thing in the way is the missing
wipe script — and I checked the ordering rather than hand-waving it: **the wipe must come first**, because REQ-040's
rule is "keep teachers + subjects + config, delete the rest" — run *after* an import, that same rule would delete
**the customer's real families**. So import-then-wipe is not a shortcut, it is a way to lose the data twice.

**Fast path I am putting to him instead of "wait for the script":** the test residue on `uat` is small and known —
Tanya's `QA-prod-student` + 1 parent + 2 courses + 1 voucher + their bookings, plus demo leftovers. He is the admin
and the only person allowed to touch that box anyway. **If it is genuinely a handful of records, he can delete them
through the app's own screens** — no mass-delete script, no hand-written SQL, every deletion going through the
product's own guards — and then commit Monday's 9 rows tonight.
**@Sober: still cut the reset script** — this fast path is for *tonight's* handful, not a replacement. The next
environment reset must not depend on someone clicking carefully.

**Sequence I am giving him now:** dry-run the top day (writes nothing, ~30 s) → he sends me the report → I verify
the `excelRow` alignment against three rows from my own analysis → he clears the test records by hand → `--commit`.

**Ball: owner** (dry run now + count the test records) **& @Sober** (the reset script, still) **& @Jason** (idle —
TASK-148 preview REWORK + the TASK-146 bot-side finish are his while he waits).

## [--:--] Porter (PM) — I mis-scoped REQ-055: it is BOTH environments, `sid` first. Reset script is now THE blocker. → @Sober
Owner: *"ให้นายทำการเคลียร์ข้อมูลทั้งหมดที่ sid และ uat … แล้วเราจะเอาข้อมูลขึ้น sid ดู ถ้ามัน work ก็จะเอาขึ้น uat"*

**I had scoped REQ-055 to the customer box only.** He is applying the rule he set for migrations to the *data*
operation as well: **rehearse on `sid`, prove it in the product, then `uat`.** That is better than what I wrote and
I should have proposed it — both the wipe and the importer run for the first time in their lives, and on `uat` a
mistake costs the customer their opening day while on `sid` it costs a re-run. Written into REQ-055 as **UPDATE 3 +
AC-17/AC-18**.

**This kills the "just delete them by hand tonight" fast path I offered an hour ago** — and it should. A
two-environment, repeatable clean-out is not a clicking exercise. ⇒ **The REQ-040 reset script (`db:reset`, never
built — no SPEC-036, no TASK-127) is now the single critical-path item for go-live.** Everything else is ready:
importer PASSed, rules decided, yellow set prepared, `uat` ruling confirmed.

**@Sober — cut it now.** Scope is fully written in REQ-040 already (keep teachers + subjects + config · delete the
rest · dry-run default · `--commit` writes · one transaction · FK order · ledgers untouched · KEEP-set asserted).
It now runs **twice**, on two environments, so the dry-run/report discipline earns its keep immediately. Jason is
idle and has asked for work twice.

**What the owner can still do this minute:** the importer's **dry run touches no database at all**, so he can
validate the CSV and the row alignment right now, on either env, before anything is wiped anywhere.

**Ball: @Sober** (the reset script — nothing else is blocking go-live) **& owner** (the dry run, and the file prep).

## [--:--] Porter (PM) — the owner sent a NEWER file. Re-analysed it; alignment verified; the yellow set still holds. → @Sober
Owner supplied `Student list - Sheet1.csv` + `Student list (1).xlsx` (both 2026-08-19 01:04). **I did not assume it
was the same data as Saturday's file** — re-extracted everything from the new one:

- **179 named rows now, up from 176** — the customer has **added 3 children** since. They are at the bottom
  (`Voucher` group went 5 → 8), which is why nothing above them shifted.
- **✅ 113 importable · ⚠️ 39 need the customer · ⛔ 27 not-ready (yellow).**
- 🟢 **The yellow row-numbers are byte-identical to Saturday's** (27 rows, same numbers, max = 170) — because the
  additions landed after them. **`2026-08-19-yellow-rows.txt` is still correct**, and I verified that rather than
  hoping for it: a shifted yellow set would have excluded the wrong children on the customer's own box.
- **Row alignment CONFIRMED — this was Jason's Q3 risk and it is now closed.** The CSV header spans **two physical
  lines** (the `Gender` field contains a newline), so a naive line-counter would be off by one for all 179 rows.
  Parsed as proper CSV records: **record #2 = `Monday / No.1` = xlsx row 2**, and record #11 = `Tuesday / No.10` =
  xlsx row 11, which is the first yellow row. ⇒ **with `--header-rows 1` the report's `excelRow` maps 1:1 to what
  the owner sees in Excel.** The dry run must reproduce that; if it shows the first student as row 3, the parser is
  counting lines rather than records and we fix the flag, not the sheet.
- Staged the CSV into gitignored `project-docs/` so the command line is short and no customer data sits in Downloads
  being passed around. **Fresh checklist: `project-docs/2026-08-19-student-import-checklist.md`** (day plan · the
  27 not-ready · the 39 to confirm · multi-child families), keyed to the new file.

**Day batches now:** Monday 9 · Tuesday 9 · Wed 4 · Thu 11 · Fri 3 · **Sat 55 · Sun 80** · Voucher 8.
Weekdays = 36 rows, so the rehearsal on `sid` is still a real rehearsal on ~20% before the weekend bulk.

**Ball: @Sober** (the reset script — still the only blocker) **& owner** (the dry run; command in chat).

## [--:--] Porter (PM) — 🔴 DRY RUN CAUGHT A REAL DEFECT in TASK-150: the day column is not carried down. 1 row instead of 9. → @Sober
The owner ran the dry run. Output: **`แถวทั้งหมด 1 · ทำได้ 1`** for `--day "Monday"`. **It should be 9.**

**Diagnosis (from the file, not a guess):** in the sheet, column A is a **merged cell per day group** — the CSV
export therefore carries `Monday` on the **first row of the block only**, and rows 3–10 have an **empty** column A.
The importer filters on the literal cell value, so `--day Monday` matched exactly **one** row. Verified against the
CSV records: record #2 = `Monday / No.1`, record #11 = `Tuesday / No.10`, record #30 = **empty** day. My own
analysis carried the value forward (that is how I produced 9/9/4/11/3/55/80/8); the importer does not.

**Why this matters more than a wrong count:** every batch would have imported **one child** and reported success.
Eight days × 1 row = **8 of 179 children**, with a clean-looking green report each time, and the owner colouring
his sheet from it. **This is precisely why the dry run exists and why we ran it before the wipe** — the failure was
invisible, self-consistent, and would have been discovered by the customer, not by us.

🟢 **The good news in the same output:** the one row it did match printed as **`✅ แถว 2`** — so `excelRow`
alignment is **exactly right** (record #2 = xlsx row 2, `--header-rows 1` correct, the two-line CSV header handled
properly). Jason's Q3 risk is closed.

**@Sober — one fix for Jason, plus one thing I want stated in the DoD:**
1. **Fill-down the day column** (carry the last non-empty value of column A forward, exactly as the merged cell
   means) before the `--day` filter runs.
2. **Make the count self-checking**: the batch's expected size is knowable from the file — a batch that returns
   **1 row for a 9-row day** should be loud, not silent. The `imported+held+yellow=total` invariant already exists
   for the *whole* file; the same discipline should apply **per batch**, so an empty-ish batch cannot look
   successful. That is the actual lesson here: the report was self-consistent and still wrong.

**Expected after the fix (my numbers, for verification):** Monday **9** (✅5 ⚠️4 ⛔0) · Tuesday 9 (4/2/3) ·
Wed 4 (4/0/0) · Thu 11 (7/0/4) · Fri 3 (2/1/0) · Sat 55 (41/10/4) · Sun 80 (44/20/16) · Voucher 8 (6/2/0).
**Total 179 = 113 ✅ + 39 ⚠️ + 27 ⛔.** If the fixed dry run doesn't reproduce those, one of us is wrong and we find
out before anything is written.

**Minor, worth one line:** the report went to `../project-docs/` relative to the **code repo** (`H:\scheduler\`),
not the workspace — fine (it is outside git and local-only), just flagging so nobody hunts for it in the wrong tree.

**Ball: @Sober** (this fix + the reset script — both now stand between us and go-live) **& owner** (nothing to run
until the fix lands; the dry run did its job).

## [Sober (SA)] — go-live: TASK-150 defect → REWORK (Jason) + the reset script CUT (SPEC-052/TASK-151, Jason). Both to Jason. → @Porter @Jason
Two things, both now Jason's.

**1. TASK-150 → REWORK — the dry run did its job.** You caught it: column A is a **merged cell per day-group**, so the
CSV carries the day only on the block's first row → `--day Monday` matched **1 of 9**, and every batch would have
imported 1 child with a self-consistent green report (8/179 total). Two fixes into TASK-150: **(1) fill-down column A**
(carry the last non-empty value forward — exactly what the merged cell means) before the `--day` filter; **(2)
per-batch count self-check** — the `imported+held+yellow=total` invariant guards the whole file today; apply it **per
batch** so an empty-ish batch is loud, not silent (that's the real lesson — self-consistent and still wrong). Core is
otherwise still accepted, and your `excelRow` alignment is confirmed right (record #2 = xlsx row 2).

**2. The REQ-040 reset script — CUT. SPEC-052 + TASK-151 (Jason).** You're right it never existed — authorized 08-11,
the SPEC/TASK were never cut (my gap; REQ-040 rode as "done bar the owner running it"). It's the single go-live blocker
now, and it runs on **both sid + uat, sid first**. Cut it as an **owner-run, dry-run-default** script (the *safer* path
than hand-SQL, which is exactly why REQ-040 forbade the ad-hoc TRUNCATE): keep teachers+subjects+config, delete the rest
in the FK-restrict order from my 08-11 design, one transaction, KEEP-set asserted + CLEAR=0 re-asserted before COMMIT,
schema + 3 ledgers untouched, idempotent, console counts-only. **Jason re-verifies the FK/keep split against `schema.ts`
before writing** (relations may have shifted since 08-11 — flag via me if so). No DDL → same as the importer, safe on
uat regardless of the ledger.

FYI, no action for me: the newer file (179 rows) + the both-envs scope (sid-first) are handled — the importer is
env-agnostic and the yellow set is byte-identical, per your re-analysis.

**Ball: @Jason** (🔴 **TASK-151 `db:reset` — the go-live blocker, first**; then **TASK-150 fill-down + per-batch check**;
your other two — TASK-148 preview REWORK + the TASK-146 bot-side finish — after) **& @Porter** (once TASK-151 + the
TASK-150 fix pass my review, the owner's sequence is whole: backup → `db:reset` dry-run→commit on sid → import dry-run→
commit on sid → prove in product → repeat on uat; the palette + REQ-051 decisions still sit with you). Not the owner's to
receive from me.

## [Jason (BE)] — 🔴 TASK-151 `db:reset` BUILT + TASK-150 REWORK FIXED → both REVIEW. Go-live path is code-complete. → @Sober
Took your order exactly: the blocker first, then the importer fix.

### TASK-151 — `bun run db:reset` (the wipe that never existed)
- **FK/keep split re-verified against `schema.ts` first, as instructed** — and it holds: **no KEEP table
  FK-references a CLEAR table**, so clearing cannot reach config or the roster. One edge is new since your 08-11
  design — `course_packages.subject_id` from my `0018` — and it is **CLEAR→KEEP**, i.e. safe and already in the
  right order. Nothing to reorder, nothing to flag.
- **Dry-run by default:** one transaction, count, print, **forced ROLLBACK** — a dry run cannot write even by
  accident. **`--commit`:** explicit `DELETE` per table in your FK order (never `TRUNCATE CASCADE`) → re-count →
  **KEEP unchanged AND every CLEAR = 0**, both asserted **before** COMMIT; any failure throws and the whole
  transaction rolls back with nothing deleted. **No DDL at all**, so schema + all three ledgers are untouched —
  which is also why this is safe on `uat` whatever the ledger ruling turns out to be.
- Idempotent (second run deletes 0 and says so). **Console prints counts only** — a test asserts the output
  contains no Thai word-runs, so PII can't creep into it later.
- ⚠️ **I have not run it against any database.** The plan, the assertions and the rollback path are what I can
  evidence; the execution is the owner's, sid first.

### TASK-150 — the merged-cell defect: both fixes in
- **Fill-down column A.** A merged cell means the value spans its block; the export only carries it on the first
  row. Now carried forward (a blank spacer inside a block doesn't end the merge), so `--day Monday` selects the
  whole day.
- **Per-batch self-check, counted against the FILE.** This is the part that matters: the old report was
  *self-consistent and still wrong*, so the check has to come from outside the batch. The file says Monday has 9
  rows ⇒ selecting 1 is a **defect** — the script prints every day-group size and **exits non-zero without
  writing or reporting**. An unknown `--day` fails the same way instead of quietly importing nothing.
- 8 new tests including the exact 1-of-2 case, and I re-ran the end-to-end dry run on a **synthetic** merged-cell
  CSV: `batch นี้ควรมี 3 แถว · ได้ 3 ✔` where the old code said 1.
- 🔴 **What I cannot verify and won't claim:** Porter's real per-day numbers (179 = 113 ✅ / 39 ⚠️ / 27 ⛔). That
  file stays on the owner's machine. **The fixed dry run either reproduces them or one of us is wrong — and
  that is precisely the check to run before the wipe.** His `excelRow` alignment finding (`✅ แถว 2`) still
  holds; I changed nothing about it.

**Verified for both:** `bunx tsc --noEmit` **0** · `bun test` **554 pass / 0 fail** (66 files; +20).

**Ball: @Sober** (review TASK-151 + the TASK-150 rework — together they complete the owner's go-live sequence:
backup → `db:reset` dry-run→commit on sid → `import:students` dry-run→commit on sid → prove → repeat on uat).
Still queued for me behind these, in your order: **TASK-148 preview REWORK**, then the **TASK-146 bot-side
finish**. Everything is uncommitted on `dong` — commit on the owner's word.

## [Sober (SA)] — 🔴 TASK-151 `db:reset` = PASS ✅. The go-live wipe now EXISTS and it's the safe one. → @Porter @Jason
Reviewed the reset as the mass-delete tool it is. Reproduced: `bunx tsc --noEmit` **0** · `bun test` **554/0** (12 new).
Every safety edge holds:
- **Dry-run cannot write** — it counts, then throws `DRY_RUN_ROLLBACK` **before the delete loop**, so no `DELETE` ever
  executes without `--commit`.
- **`--commit`** = explicit `delete from "<t>"` per table in the CLEAR FK-order (**never TRUNCATE CASCADE**; names from
  the `CLEAR_TABLES` constant, no injection), then `canCommit` asserts **KEEP unchanged AND every CLEAR = 0** → mismatch
  **throws → rollback**. One transaction; **no DDL** → safe on uat regardless of the ledger.
- Jason **re-verified the FK/keep split against `schema.ts`** (not the 08-11 memory) — no KEEP→CLEAR FK; the one new
  edge (`course_packages.subject_id`, my `0018`) is CLEAR→KEEP, so the order still holds. Idempotent; console counts-only
  (a test forbids Thai word-runs so PII can't creep in). Not run against any DB — the owner's to execute.

**⇒ the go-live wipe tool exists, and it's the safe path REQ-040 always wanted** (vs the hand-SQL we nearly shipped).
Only **TASK-150's fill-down fix** is still open before the whole sequence is green.

**Ball: @Jason** (TASK-150 fill-down + per-batch count-check — the last go-live item; then TASK-148 preview REWORK +
the TASK-146 bot-side finish) **& @Porter** (once TASK-150 re-passes, the owner's full sequence is walk-ready:
`db:backup` → `db:reset` dry-run→`--commit` on **sid** → `import:students` dry-run→`--commit` on sid → prove in the
product → repeat on **uat**; the palette + REQ-051 decisions still yours). Not the owner's to receive from me.

## [Jason (BE)] — 📌 TASK-150's fix is ALREADY IN (my 01:2x entry) · TASK-148 preview rework now done → REVIEW → @Sober
**First, a pointer so nothing gets built twice:** your `db:reset` PASS says "only TASK-150's fill-down fix is
still open" and puts it back on me — **it was already delivered in the same entry you were reviewing** (my
[Jason (BE)] entry above, the one that opens with TASK-151). Both fixes are in and the board row reads
**REVIEW — rework fixed**: (1) column A carried down through its merged block, (2) the per-batch count checked
**against the file**, so a day the file says has 9 rows must select 9 or the script prints every day-group size
and exits non-zero **without writing or reporting**. 8 tests, including the exact 1-of-2 case. Nothing further
needed from me there — it needs your eyes, not more code.

### TASK-148 — preview rework done, per your Q1(b)
Fixed by **sharing the placement rule instead of teaching the preview a second one**:
- New pure **`lib/extension-slot.ts` — `firstFreeWeeklySlot(fromDate, isOccupied, maxWeeks)`**. Step a week,
  ask the caller whether it's occupied. `findFreeExtensionDate` is now a thin wrapper over it with the same DB
  clash query, so **the save behaves exactly as before** and there is one definition of where a make-up lands.
- **`previewCoursePackage`** appends each make-up through that same `findFreeExtensionDate`, **sequentially**,
  passing the dates this preview has already claimed (new optional `alreadyTaken` set). That set is the actual
  fix for multi-absence: without it, two absences both land in the first free week and the previewed **end
  date** — the preview's headline — comes out too early.
- The `size` booked weeks stay at their weekly-chain dates on purpose: the save inserts those at exactly those
  dates and **refuses** a clash (SLOT_TAKEN) rather than shifting, so previewing them shifted would be the same
  lie pointing the other way.
- **TOCTOU accepted as you scoped it** (a booking made between preview and save) — the save is authoritative.

**Verified:** `bunx tsc --noEmit` **0** · `bun test` **559 pass / 0 fail** (+5). The new tests pin the thing that
was actually wrong — **two make-ups placed in sequence never share a slot** (09-08 then 09-15, not 09-08 twice)
— plus skip-a-taken-week, an async predicate, and scan-exhaustion handing back the last candidate so
`exceedsExtensionCeiling` refuses it instead of a silently valid-looking date.

**Ball: @Sober** (review the TASK-148 preview fix · and the TASK-150 rework that's been sitting in REVIEW since
my last entry — it's the last go-live item on your list). Next for me, unless you re-order: the **TASK-146
bot-side AC-7 finish** (Q1 — threading `lang` so a LINE parent reads the refusal in their own language). All
uncommitted on `dong`; `0019` still must not reach `uat`.

## [Sober (SA)] — TASK-148 preview rework = PASS ✅. REQ-045 BE complete. → @Jason @Fern @Porter
Reviewed Jason's Q1(b) preview rework. Reproduced: `bunx tsc --noEmit` **0** · `bun test` **559/0** (+5). He fixed it the
right way — **shared the placement rule, didn't teach the preview a second one**: `firstFreeWeeklySlot` extracted to
`lib/extension-slot.ts` (pure), `findFreeExtensionDate` now a thin wrapper with an `alreadyTaken` param, and
**`previewCoursePackage` calls the same `findFreeExtensionDate` the save calls**, sequentially, treating its own placed
dates as occupied → multi-absence end dates match the save. So **preview == save by construction (AC-2)**, not a second
implementation that could drift. (The `size` booked weeks stay at their weekly-chain dates deliberately — the save
refuses a clash there with `SLOT_TAKEN` rather than shifting; previewing them shifted would be the lie the other way.
Correct.) New tests pin two-make-ups-never-share-a-slot + skip-taken-week + exhaustion→ceiling-refusal.

**⇒ TASK-148 DONE. REQ-045 BE is complete** (birth-marker flag · quota-skip at both sites · ceiling reuse · shared
placement). It closes on **TASK-149 (FE, now fully unblocked)** + the owner-run `0019` deploy (⚠️ still: not to uat until
that box's `0018`/ledger is repaired).

**Ball: @Fern** (TASK-149 planned-absence FE — the preview shape is final; TASK-147 dict; TASK-142 on the palette)
**& @Jason** (the go-live's last item — **TASK-150 fill-down + per-batch check**; then the TASK-146 bot-side AC-7 finish)
**& @Porter** (`0018`/`0019` uat migration order; the palette; REQ-051 decisions). Not the owner's to receive from me.

## [--:--] Porter (PM) — go-live inventory for the owner: TASK-148 is a DIFFERENT stream. Both go-live items are now cut. → @Sober
Owner asked whether Sober's TASK-148 report was part of the go-live. **It is not** — TASK-148/149 are **REQ-045**
(planned absences at course creation), from the customer meeting. Good work, wrong queue for tonight; I told him so
plainly rather than letting "DONE ✅" read as go-live progress.

**Verified on disk, not from chat:** the wipe script I chased twice **has now been cut** — **SPEC-052 +
TASK-151** (`db:reset`, board row marked 🔴 GO-LIVE BLOCKER). Thank you. So the go-live is exactly two BE items,
both Jason's, both unstarted:
1. **TASK-150 fix** — day fill-down + the per-batch self-check (the defect the dry run caught).
2. **TASK-151** — `db:reset`, needed **twice** (`sid` then `uat`).

Nothing else is on the critical path: importer PASSed, rules decided, yellow set prepared and re-verified against
the newer file, row alignment proven, `uat` ruling confirmed, backup gate satisfied.

**Ball: @Jason** (150 fix → 151) **& owner** (nudge Jason; nothing to run until both land).

## [--:--] Porter (PM) — 🔴 @Sober: ONE thing. Review TASK-150's rework. It is the only item left in the whole go-live.
The owner is watching the ball bounce — Jason → you → Fern → back — and he is right to be annoyed. Here is the
state from the files, not from anyone's summary:

- **TASK-151 `db:reset` = DONE, SA-reviewed.** ✅ The wipe exists.
- **TASK-150 = the fix is BUILT and has been sitting in REVIEW since Jason's 01:2x entry** — its status line still
  reads `🔧 REWORK` because nobody has re-reviewed it. Since then you reviewed **TASK-151** and **TASK-148**, and
  routed the ball to **Fern**. Both of those were real work; **neither was the go-live.**

**So: 179 children are waiting on one code review.** Everything else is finished — importer rules, yellow set,
row alignment proven, `uat` ruling, backup, and now the wipe.

**@Sober — before anything else in your next session: re-review TASK-150's rework** (day fill-down + the
per-batch count check). On PASS, tell me and I hand the owner the full run sequence: `sid` backup → `db:reset` dry
→ `--commit` → `import:students` dry → `--commit` Monday → he checks the product → then the identical run on `uat`.

**Nothing else from me until then.** The palette, REQ-051, the `0018/0019` uat order — all of it can wait behind
one review. If your queue disagrees, say so in the log and I will re-prioritise with the owner rather than have him
watch the ball move sideways again.

**Ball: @Sober** (TASK-150 re-review — that is the entire critical path) **& owner** (nothing to run yet).

## [--:--] Porter (PM) — root cause of the ball bouncing: the board row's OWNER cell never flipped. Not a log problem. → @Sober @Jason
The owner asked me why this keeps going wrong and whether people are failing to read the board or writing in the
wrong log. **Neither, and I checked rather than theorised.** Both are fine now:
- Everyone **is** in `2026-08-19.md` (Jason, Fern, Sober and I have all written here today).
- The board row for TASK-150 **does** say `🔎 REVIEW — rework fixed (Jason 2026-08-19)`.

🔴 **The actual defect is one cell.** On that same row, the **"owner of next step" column still reads `Jason`**,
even though the status is `REVIEW` — and by PROTOCOL, `REVIEW` means the **SA Lead** is next. So anyone scanning
the board for "what is waiting for me" sees TASK-150 assigned to Jason:
- **Sober** scans, sees Jason's name, skips it — and correctly picks up other work (151, 148).
- **Jason** knows he's finished and says so in the log.
- The item is **invisible to both** while looking, to the owner, exactly like a ball being passed sideways.
A second, smaller inconsistency: the **task file header still says `🔧 REWORK`** while the board says REVIEW. Two
sources of truth disagreeing about the same task is how this hides.

**The fix is a rule, not a scolding — and it is cheap:** when BE moves a task to `REVIEW`, the **owner cell moves
to Sober in the same edit**, and the task file's own Status line moves with it. A status without an owner is a
task nobody is looking for. I am asking for it explicitly because this is the second time today the go-live item
specifically has stalled in a gap between two roles who were each working correctly.

**Not blaming the log split for this one.** That was real and it cost us yesterday, but it is fixed; this was a
different, smaller thing and it deserves its own diagnosis rather than being lumped in.

**Ball: @Sober** (re-review TASK-150 — still the only go-live item — and flip that owner cell as you do)
**& @Jason** (on your next `REVIEW`, move the owner cell too).

## [--:--] Fern (FE) — TASK-149 planned-absences-at-creation BUILT → REVIEW · one invented string flagged → @Sober
Caught up on this file (I'd been writing in 08-18 before Porter closed it). Took **TASK-149** — you marked it fully
unblocked and it's the last thing gating REQ-045 besides the owner-run `0019`.

### Built (5 files, uncommitted on `dong`)
I read **Jason's code** for the contract rather than the task's summary, and that paid off in one place: `POST /courses`
**refuses `sessions.length !== size`** (`validation.ts:149`). So the payload cannot be the visible draft once absences
add make-up rows. It now sends the **first `size` rows only** (the weekly chain) plus `absentWeeks`, and the BE appends
the make-ups — which is also the right split, because placement is availability-aware and belongs server-side. Per-row
moves survive. That contract is invisible from the UI, so **Q2 asks you to record it in SPEC-049**: if anyone later
"fixes" the FE to send the whole draft, create starts failing that refinement for a reason nobody will see coming.

- **One preview path** (`runPreview`) serves both the initial generate and every toggle → the rows on screen are always
  the server's answer for the current absence set. **AC-2 holds by construction**, not by a second implementation.
- The draft **renders as what it will become** — absent → `SICK_LEAVE`, appended make-up → `EXTENDED` — so every
  existing chip and label works unchanged and staff see the real plan before saving.
- The toggle lives in the row `⋯` menu (mirrors edit mode) and is **offered only on the `size` weekly rows**. A make-up
  exists *because* of an absence; letting staff declare one absent would be chasing their own tail.
- **AC-1** preview line `แผนใหม่: {n} คาบ · ไม่มา {d} · สิ้นสุด {date}` (tabular-nums, spinner while in flight).
  **AC-3** on `exceedsCeiling`: the REQ's sentence as an Alert **and** a disabled Confirm carrying the same sentence as
  a tooltip — a refusal with its reason, and no disabled button without one.
- **AC-4**: with no absences `absentWeeks` is `undefined`, so the request is byte-identical to today's; edit mode is
  untouched (everything new is behind `isCreate`). `leaveUsed` stays **0** — decision (B) is that a declared absence is
  free, and showing it as consumed would contradict the rule we're building.
- Mock updated to TASK-148's response shape so the flow is exercisable offline (naive make-up placement, noted in code —
  the availability-aware one is yours).

**Evidence:** `bunx tsc --noEmit` **0** · `bun run build` **ok** · `bun test src/lib/scheduler/` **15/0** ·
FRONTEND-STANDARD §3.5 on both changed components **0/0/0/0**.
🔴 **Rendered check + hallmark still not done** — PlanModal won't mount in this session's non-compositing pane. Rides
the @Tanya pass with 131/132/133/139/143.

### 🔴 Q1 — I wrote one user-facing string and I'm not hiding it
REQ-045 supplies the control label, the preview line and the ceiling sentence, but a **toggle needs an undo label** and
the REQ has none. I shipped `ยกเลิกการลาล่วงหน้า` / `Remove planned absence` as a **clearly-marked draft**. Same line I
held on TASK-132's Q1: I'd rather flag invented copy than let it quietly become the product's wording.

### Re the go-live the owner is pushing on
The wipe + import is **TASK-150/151 (Jason's, code-complete) run by the owner** — not FE and not mine to touch, and I
haven't. What I *can* do for it is stop being the thing that blocks it, so my queue is now: **TASK-147 next** (leave
cut-off settings FE — its BE landed), then **TASK-142 the moment Porter's palette clears**.

**Ball: @Sober** (review TASK-149 · Q1 to Porter for the undo wording · Q2 the payload contract line in SPEC-049)
**& @Porter** (palette → unblocks TASK-142; the C redirect wording still owed from TASK-143).

## [Sober (SA)] — 🟢 TASK-150 rework = PASS ✅. GO-LIVE BE IS COMPLETE. My miss owned. → @Porter @Jason
You're right, and it's on me: Jason's fill-down fix landed in his TASK-151 entry, and I reviewed 151 and 148 (real work,
but not go-live) while the one go-live item sat in REVIEW. Reviewed it now — **PASS.** Reproduced: `bunx tsc --noEmit`
**0** · `bun test src/lib/student-import.test.ts` **35/0** (+7). Both fixes confirmed in the code:
- **Fill-down of column A** — `lastDay` carries the merged-cell day forward; a blank spacer line is skipped **without
  resetting** the carried day, so `--day Monday` now selects the whole 9-row block (`student-import.ts:250-260`).
- **Per-batch self-check** — the file's own day-group size is the expected count; a mismatch prints every group size and
  **exits non-zero WITHOUT writing or reporting** (script `:87`), so an empty-ish batch can't look successful. Tests pin
  the exact 1-of-2 case.
Everything from the first review stands (AC-7 people-only · dry-run default · PII counts-only · phone/DOB/parent/yellow ·
excelRow alignment).

**⇒ The entire go-live BE is DONE: TASK-151 `db:reset` ✅ + TASK-150 importer ✅.** Nothing engineering-side remains on the
critical path — importer PASSed, rules decided, yellow set prepared, row alignment proven, `uat` ruling confirmed, backup
gate satisfied, wipe tool built and safe.

**Ball: @Porter** — both go-live BE items are reviewed and DONE; the owner's sequence is walk-ready, and it's yours to
carry to him: on **sid** — `db:backup` (verified non-zero) → `db:reset` dry-run→`--commit` → `import:students` dry-run
(top day) → read the report → `--commit` → prove in the product → then repeat on **uat**. Phone-column/CSV-UTF-8 prep as
before. Not the owner's to receive from me. (Off the critical path: TASK-149 FE is in REVIEW for me — REQ-045, not
go-live; TASK-146 bot-side finish; the palette; REQ-051 decisions — all after tonight.)

## [--:--] Porter (PM) — 🟢 GO-LIVE IS RUNNABLE. Handing the owner the full sequence, sid first. → @Sober
TASK-150 PASSed; with TASK-151 already DONE, **the entire go-live engineering is complete.** Nothing is waiting on
anyone but the owner now.

**Sequence I am giving him (one message, copy-paste, in order):** on **`sid`** — verified backup → `db:reset`
dry-run → read the KEEP/CLEAR counts → `--commit` → `import:students` dry-run on the top day → **check the report
says `แถวทั้งหมด 9`, not 1** → `--commit` → **open the product and look at the 5 families** → then the identical
run on **`uat`**.

**Three checkpoints I told him to stop at, because each one is a place where "looks fine" and "is fine" diverge:**
1. **After `db:reset` dry-run** — teachers/subjects/config counts must be **unchanged** and everything else headed
   to 0. That is the only moment before deletion where a wrong KEEP set is visible.
2. **After the import dry-run** — **9 rows for Monday**, first student at **row 2**. The 1-row bug proved this
   report can be self-consistent and wrong; the count is the tell.
3. **After the first `--commit`** — he looks at the **product**, not the script's output. Five families on the
   People screen is the only evidence that means anything to the customer.

**Explicitly NOT in tonight's sequence:** the `0018`/`0019` migrations to `uat` (still held behind the ledger
repair) and everything off the critical path (palette, REQ-051, TASK-146/147/149). The import needs none of them.

**Ball: owner** (the run) **& @Sober** (nothing on go-live — the ledger repair for `uat` is the next thing I will
ask for once the data is in, since `0018`/`0019` are stuck behind it).

## [Sober (SA)] — MEA CULPA + a rule proposal so this can't recur. Please codify. → @Porter
Owning two mistakes of mine that cost the owner time and made you route the ball by hand — both about the **channel**,
not the work (the work was fine; it was invisible or mis-ordered):

1. **I wrote reviews into a stale log file.** I declared `08-17.md` "canonical" and kept appending there while you
   correctly rolled the log 17 → 18 → 19. My verdicts existed but you couldn't see them, so the owner heard "Sober isn't
   done" when it was done in the wrong place.
2. **I reviewed whatever task file changed instead of your stated priority.** You said "🔴 ONE thing: review TASK-150's
   rework — the only go-live item," and I reviewed TASK-151 and TASK-148 first (real work, wrong order), leaving the
   actual blocker unreviewed.

**Two rules I'd like you to make standing (PROTOCOL.md or the board's rules card — your call, you own coordination):**
- **R1 — One canonical log = today's dated file.** Every role writes to `log/<YYYY-MM-DD>.md` (real current date). When
  the date rolls, whoever notices closes the old file with a one-line `📁 THIS FILE IS CLOSED → continue in <new>`
  pointer (you already do this) — and **nobody appends to a file the team has rolled past.** First action every session:
  `ls log/`, open the newest, honour any close-pointer.
- **R2 — The PM's stated priority is the work order.** A `🔴`/"ONE thing"/"only item left" from you outranks reviewing a
  task just because its file landed in REVIEW. System file-change notifications are input, not the queue. Each role's
  first move is to read your latest entries and do **your** order, not the order things happened to change in.

I've already put both into my own memory so I don't repeat them; asking you to make them team-wide so no role does.

**Ball: @Porter** (codify R1/R2 if you agree — or amend; you own PROTOCOL). Unchanged and more important than this:
the **go-live BE is DONE** (TASK-151 + TASK-150 both reviewed ✅) — the owner's sid→uat sequence is yours to walk him
through. Not the owner's to receive from me.

## [--:--] Porter (PM) — 🎉 FIRST REAL IMPORT LANDED on `sid`: 7 parents · 8 students. The pipeline works end to end.
Owner ran the full chain on `sid`: verified backup → `db:reset` dry (**KEEP unchanged, 252 rows to clear**) →
`--commit` (**KEEP 30→30 / 9→9 …, CLEAR all → 0, schema + ledger untouched**) → import dry-run → **`--commit`:
`ผู้ปกครองใหม่ 7 · นักเรียนใหม่ 8 · มีอยู่แล้ว 0`**.

**Every guard we argued for fired correctly:**
- **`batch นี้ควรมี 9 แถว · ได้ 9 ✔`** — the per-batch self-check Jason added after the dry run caught the
  fill-down bug. Two days ago this same command returned **1 row and called it success.**
- **`✅ แถว 2`** — row numbers align with the customer's sheet, so the green-colouring plan holds.
- Row 4 held for **no phone**, exactly as the owner decided (option ข).

**One number I owe the owner an explanation for, and it is my inconsistency, not the tool's:** my checklist said
Monday = ✅5 / ⚠️4; the importer says ✅8 / ⚠️1. **The tool is right and my checklist was stricter than my own
rule** — I counted "no DOB" as *stuck*, while AC-10 (which I wrote) says a missing birthday leaves the field empty
and **still imports the child**. Only a missing/invalid phone actually blocks. I told him plainly rather than
letting two numbers float around: **the run's report is the authority (AC-15), not my prediction.** The missing
birthdays remain a customer question, not a blocker.

**Next:** he verifies Monday **in the product** (7 families / 8 students, Thai names rendering), then the remaining
seven batches, then the identical sequence on `uat`.

**Ball: owner** (look at the product, then Tuesday → Voucher) **& @Sober** (nothing on go-live; the `uat` ledger
repair is next once the data is in, since `0018`/`0019` are still stuck behind it).

## [--:--] Porter (PM) — ✅ Monday verified IN THE PRODUCT on `sid`. One display gap found (not a blocker). → @Sober
Owner sent the People screen. **Everything the import promised is visible and correct:**
- **Thai renders properly** — ปั๋น · เรย์น · มิวสิค · เอแคลร์ · อาร์ตี้ (AC-5).
- **Phones carry the leading zero, 10 digits** — `0949263965`, `0928988998`, `0824146455`… (AC-4).
- 🎯 **The sibling merge worked in real data**: `0925509898` shows **one family with two students** (เอแคลร์ +
  อาร์ตี้), not two parents (AC-3). That was the rule most likely to be got wrong and it is right.
- **Ages appear where a DOB existed** (Age 2/5/6/7) and are **absent where it didn't** — exactly AC-10's
  "import the child, leave the birthday empty".

### 🟡 The one finding: every family card reads **"Parent name"**
Checked the code rather than guessing: the importer stores `parentName = column H` and **`null` when H is empty**
(`student-import.ts:154`) — which is honest and correct, since most rows have no parent name in the file. What the
owner is seeing is the **frontend's placeholder** for an empty name, repeated down the whole list.

**It is not a data problem and not a go-live blocker** — but it is what the customer will see on their first
screen, and "Parent name · Parent name · Parent name" reads like a broken import rather than a missing optional
field.

**My recommendation (FE display only, no data change): when a parent has no name, show
`ผู้ปกครองของ {ชื่อเด็กคนแรก}` / `Parent of {first child}`** — derived at render time, never written. It reads like
a real family, it is true, it costs nothing, and it **self-heals** the moment staff type a real name. I am
deliberately **not** proposing we write a derived name into the DB: that would invent data the customer never gave
us, which is the one thing REQ-055 forbids (AC-7).

I'll raise it as a small FE REQ **after** the import run — it must not interrupt tonight, and it is genuinely
cosmetic until the customer opens the screen.

**Ball: owner** (continue Tuesday → Voucher on `sid`) **& @Sober** (nothing — this becomes a REQ tomorrow).

## [--:--] Porter (PM) — moving to `uat`. One difference that matters: there, the "test data" is the CUSTOMER's own. → @Sober
Owner: *"ดูรวมๆ ฉันว่าก็ผ่านนะ แต่เราไปทำที่ uat เลยมั้ย … ที่ uat ก็จะมีแต่ข้อมูลที่ลูกค้าเทสนั่นแหละ"* — the `sid`
rehearsal did its job (wipe + first batch + verified in the product), so **we move.**

**The one thing I am treating differently on `uat`, and it is not a technical difference:** on `sid` the deleted
rows were **ours**. On `uat` they are **the customer's own trial records** — things they typed while trying the
product. They asked us to clear them, so we clear them; but the KEEP/CLEAR dry-run numbers there deserve a real
look rather than a glance, because a number bigger than "a bit of trying it out" would mean they used it more
seriously than anyone assumed, and that is a conversation, not a `--commit`.

He also said the team may help look at the data first. **The team cannot** — nobody but him touches that box — so
the mechanism is: **he runs the read-only dry run, pastes the counts, and I read them with him.** That is the
DATA REQUEST loop doing exactly what it exists for, and it costs one extra minute.

**Sequence on `uat` = identical to `sid`**, with the backup mattering far more (it is the customer's data, and
restore-from-dump is the only undo). Batches after Monday can run straight to `--commit` — the per-batch guard and
idempotency have now been exercised on real data.

**Ball: owner** (switch env → backup → `db:reset` dry-run → **paste the counts to me before committing**).

## [--:--] Porter (PM) — ✅ `sid` COMPLETE: all 8 batches. 109 parents · 130 students · and the whole file reconciles. → @Sober
Owner ran every batch on `sid`. Totals, added up from his output:

| | Mon | Tue | Wed | Thu | Fri | Sat | Sun | Vch | **รวม** |
|---|---|---|---|---|---|---|---|---|---|
| rows | 9 | 9 | 4 | 11 | 3 | 55 | 80 | 8 | **179** |
| ✅ imported | 8 | 6 | 4 | 7 | 2 | 45 | 52 | 6 | **130** |
| ⚠️ held | 1 | 0 | 0 | 0 | 1 | 6 | 12 | 2 | **22** |
| ⛔ yellow | 0 | 3 | 0 | 4 | 0 | 4 | 16 | 0 | **27** |
| parents | 7 | 6 | 4 | 6 | 2 | 39 | 41 | 4 | **109** |

**Three independent cross-checks, all of which had to hold and do:**
1. **130 + 22 + 27 = 179** — every row in the customer's file is accounted for (AC-2). Nothing vanished.
2. **⛔ 27 = exactly the yellow set I extracted from the workbook fills.** The importer never saw a colour; it was
   given row numbers. Two different methods, same 27 — the not-ready children are excluded and no one else is.
3. **130 students across 109 parents = 21 families with more than one child**, which is **precisely** the 21
   multi-child families my own analysis counted from the phone column. The sibling merge is right at file scale,
   not just in the one card the owner eyeballed.
- Bonus: `Sun` reports **42 families but 41 new parents** — one parent already existed from an earlier batch, i.e.
  a family whose children sit on different days merged **across** batches instead of duplicating (AC-6 in the wild).

**My earlier prediction (✅113/⚠️39) vs actual (✅130/⚠️22):** the 17-row difference is entirely missing-DOB rows,
which import by design (AC-10). My checklist was stricter than my own rule; the run is authoritative (AC-15).

⇒ **`sid` is a full rehearsal of the real thing: wipe, 8 batches, 179 rows, reconciled.** Nothing surprised us.

**Next: the identical sequence on `uat`**, with the dry-run counts read together before the wipe commits, because
there the deleted rows are the customer's own trial data. **The green-colouring list comes from the `uat` run**,
not this one — `sid` is a rehearsal, and "green" must mean "in the system the customer opens".

**Ball: owner** (`uat`: env → backup → `db:reset` dry-run → paste the counts).

## [--:--] Porter (PM) — owner is stopping at Monday+Tuesday on `uat` and letting the CUSTOMER check first. Good call. → @Sober
Owner: *"วันนี้ฉันรันแค่ Monday and Tuesday รอให้ลูกค้ามาดูก่อนว่าฉันทำถูกมั้ย หากถูกค่อยมารันอื่นๆ ทั้งหมดพรุ่งนี้"*

**Endorsed, and worth recording why rather than just agreeing:** `sid` proved the *mechanism*; only the customer
can confirm the *meaning* — that these are the right children, that the families look right to someone who knows
them, and that clearing their trial data was what they actually wanted. **18 rows is the cheapest possible sample
for that question**, and if the answer is "no, not like that", we have 18 rows to undo instead of 179.

**State after today:** `uat` wiped (247 rows, KEEP intact) · Monday + Tuesday to be committed · the remaining six
batches wait for the customer's word.

**What I owe him next:** once those two commit, I read the run reports myself (they are local files) and produce
**(a)** the exact rows to colour green in the customer's sheet, and **(b)** the held rows with a per-child reason,
for Mon+Tue only. Colour follows the run, never my prediction (AC-15).

**Ball: owner** (2 commits, then the customer looks) **& @Sober** (off the critical path now — the `uat` ledger
repair is the next thing I will ask for, since `0018`/`0019` are still held).

## [--:--] Porter (PM) — 🔴 Mon+Tue are live on `uat`, but the Tuesday batch imported a MOTHER as a student. Found before the customer looked. → @Sober
Owner committed Monday (7 parents / 8 students) and Tuesday (6/6) on `uat` and has already coloured the sheet green.
**Reading the run reports — which is my job, not his — row 14 is wrong.**

**Row 14 = `Ari3y(V)'MOM / Chandini Gulrajani`** — imported as a **student**. It is plainly a parent: the name says
`MOM`, and its phone `858091112` is the **same phone as row 16 `Ari Khosla (V)`**, the actual child. So `uat` now
contains a child who does not exist, in the customer's own first look, and it is coloured green in their sheet.

**Why the guard missed it:** AC-11's parent-row detection was implemented as a **conservative prefix match**
(`คุณแม่…`, `คุณพ่อ…`) — deliberately, so it would not swallow real children. `Ari3y(V)'MOM` carries the marker
**in the middle**, so it passed. **The conservative choice was right; the coverage is too narrow.**

🔴 **And the report handed us a second, better signal that nobody is using:** row 14's DOB was rejected as
`ปีเกิดไม่อยู่ในช่วง 2005–2026 (23/02/1990)` — **and the row was imported anyway.** A birth year in the 1980s–90s is
not a typo pattern, it is an **adult**. Row 12 (`มะเหมี่ยว`, `6/8/1985`) is the same shape.

**I scanned the whole file so we size this before Saturday's 55 and Sunday's 80:**
- **7 rows carry a parent marker** in the name: **14, 48, 86, 95, 144, 171, 179** — none of them yellow, so all
  seven are heading for import.
- **5 rows have an adult birth year**: **12, 14, 86, 144, 171** — three of which overlap the list above, which is
  exactly the corroboration that makes the signal trustworthy.

**@Sober — one small task before the remaining six batches run:**
1. **Widen the parent-row match to anywhere in the name** (`MOM`, `mom`, `แม่`, `พ่อ`, `DAD`) — not just a prefix.
2. **Treat an out-of-range birth year as a HOLD, not a silent import.** Today it is reported and ignored; it is the
   single strongest "this is not a child" signal in the file and it is currently free evidence being thrown away.
Both are the same shape as the fill-down fix: the run already knows something is odd and imports anyway.

**And for the owner, right now:** delete the student created from row 14 in the app and un-green that row. Row 12
(`มะเหมี่ยว`) I am **not** guessing about — the name reads like a child's nickname with a mistyped year; that is a
question for the customer, and it is the kind of thing his review today is for.

**Ball: @Sober** (the two-part fix — it gates Sat/Sun) **& owner** (delete row 14's student; ask the customer about
row 12's 1985).

## [--:--] Porter (PM) — ⛔ RETRACTING the parent-row fix I just asked for. The owner knows his customer's data; I did not. → @Sober
**Do not build the two-part change from my previous entry.** The owner corrected me:

> *"Ari3y(V)'MOM / Chandini Gulrajani อาจจะลูกค้าใหม่ ยังไม่ได้ลงชื่อลูกเลย เอาชื่อไลน์มา ถูกแล้ว"*

**That row is not an error — it is a new family whose child has no name recorded yet, entered under the mother's
LINE display name.** Importing it as a student is exactly what the school means by that row, and staff will replace
the name when the child is registered properly. So:
- ⛔ **Widening the parent-marker match would have been actively harmful** — it would have held back *real new
  customers* in the name of tidiness. **Jason's conservative prefix match was right**, and my instinct to broaden
  it would have broken the case it was protecting.
- ⛔ **"Adult birth year ⇒ hold" is wrong for the same reason.** And the current behaviour is already correct: the
  DOB is the *mother's*, the importer **refused it** (out of range) and imported the person **without a birthday**.
  Refusing the date while keeping the person is precisely the right split.

So the 7 MOM-ish rows and 5 adult-DOB rows I flagged as a looming problem are, as far as we know, **legitimate**.
Nothing to fix, nothing to hold, and the remaining six batches can run as-is.

**The lesson I'm recording rather than glossing:** I read a data pattern, inferred an error, and had a task cut
inside ten minutes — on data whose meaning belongs to the customer's business, not to the file. **The signal was
real; my interpretation of it was not.** Same failure mode as REQ-044 and REQ-055's column A: confident reading of
something I had no authority over. The right move was the one the owner's review was designed for — show him the
rows and ask, which is what row 12 is still doing.

**Row 12 (`มะเหมี่ยว`, DOB 6/8/1985) stays an open question to the customer** — same shape, and possibly the same
innocent answer.

**Ball: @Sober** (nothing from this — ignore the previous request; your queue is unchanged) **& owner** (nothing to
delete; the sheet's green on row 14 stands).

## [--:--] Porter (PM) — row 12 closed the same way. The "adult DOB" pattern is normal in this file, not a defect.
Owner on `มะเหมี่ยว` (DOB 6/8/1985): *"น่าจะเหมือนกันนั่นแหละ"* ⇒ same shape as row 14 — a family entered under the
parent's name/LINE identity while the child's own details are still missing.

**Closing this thread for good, and writing the conclusion where the next person will find it:** in this
customer's file, **an adult birth year on a student row is a normal, expected pattern**, not corruption. It means
"the parent registered, the child's details haven't been filled in yet". The importer's current behaviour —
**refuse the out-of-range date, import the person, report the reason** — is exactly right and needs no change.

Both open questions from the Mon+Tue run are now answered by the owner. **The remaining six batches can run
unchanged**, and nothing on `uat` needs correcting or un-greening.

**Ball: owner** (customer review, then the last six batches tomorrow) **& @Tanya** (the render + dev-LINE passes —
seven REQs close on them) **& @Sober** (`uat` ledger repair, which is what still blocks `0018`/`0019`).
