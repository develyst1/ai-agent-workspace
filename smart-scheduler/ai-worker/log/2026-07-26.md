# Log — 2026-07-26 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [21:55] Porter (PM) — session start, startup ritual done, standing by

- New PM session opened by the stakeholder ("work here as PM, understand & prepare;
  I'll assign work on `H:\scheduler`"). Ran the full startup ritual.
- Read: `PROTOCOL.md`, `PM.md` (role charter + Hard boundaries card), `board.md`
  (source of truth), and the most recent log `2026-07-20.md`. No log existed for
  today — created this file.
- **Current state (from board.md):**
  - REQ-001 (freelance budget-stock) — ✅ DELIVERED. Non-blocking ops left: register
    2 scheduled tasks + swap placeholder→real numbers.
  - REQ-002 (backoffice admin auth) — ✅ DELIVERED.
  - REQ-003 (unified teacher onboarding/offboarding sync) — ⏸️ ON HOLD (do NOT deploy;
    its ops sync is moot under the backoffice teardown).
  - REQ-004 (freelance limit standalone in frontoffice) — ✅ DELIVERED.
  - REQ-005 (standalone teacher management, rework of REQ-003 minus ops sync) — DRAFT,
    **waiting on Porter** to confirm scope/priority with คุณฟีน (may be absorbed by REQ-006).
  - REQ-006 (backoffice rebuild — universal `bo` item model on shared DB) — IN_SPEC,
    4/6 tasks DONE. Left: TASK-025 (data migration, Jason) in REVIEW; TASK-026
    (FE re-point, Fern) TODO. Hard deploy ordering noted (025 before 024 deploy).
- **Observations to reconcile when real work lands (not acting on these yet):**
  1. On this machine the code monorepo is at `H:\scheduler` (board project-info still
     lists `C:\Users\Admin\develyst\smart-scheduler` — a different machine). Will
     confirm the canonical path with the human before touching the board.
  2. `H:\scheduler\PROJECT-OVERVIEW.md` (dated 2026-07-25, written by a generic Claude
     Code session) still describes the **old** backoffice — separate DB
     `smart_backoffice_db`, `ops` schema, ports 3001/3002 — and does **not** mention the
     REQ-006 `bo`-schema rebuild or the confirmed port map (4006/3016/4010/3018). Likely
     the REQ-006 rebuild isn't fully landed in the H:\scheduler code yet, or the overview
     was a pre-rebuild sweep. A real REQ will need Sober to verify code state before any spec.
- **Nothing new assigned yet.** No requirement received → no REQ created (a prime/nudge
  is never a requirement). Standing by for the stakeholder's requirement, in Thai.

## [22:10] Porter (PM) — surveyed remaining work; REQ-006 integration gap found

- Stakeholder asked "what work is left that the team can do." Ran a read-only survey of
  all 5 repos (3 sub-agent sweeps: requirement hub, backoffice code-state, tech-debt).
  Findings below need **Sober's verification** before they drive any spec — not authoritative yet.
- **REQ-006 reconciled — the `bo` rebuild is only HALF-integrated in code:**
  - Landed: `bo` schema + migration `0004_bo_schema.sql`, `/api/v1/bo/*` routes, item/tag
    services; backoffice-front nav shows only bo screens (dashboard/items/tags). TASK-025
    migration script `backoffice-back/src/db/migrate-to-bo.ts` present (was REVIEW).
  - Gaps (untracked, team-doable): (1) frontoffice `smart-scheduler-back/src/lib/ops-client.ts`
    still calls ONLY old `ops` routes via `OPS_API_URL` — zero `bo` awareness; the cross-app
    freelance-cap / income / teacher-rate contract still runs on `ops`. (2) TASK-026 FE
    re-point NOT done — old freelance-budgets/ftpt-salary screens retired from nav but left
    orphaned, still wired to `/v1/catalog/items`. (3) old `ops` routes still fully mounted.
    (4) ports half-moved (backoffice-back still defaults 3002 in code; front dev still 3100).
  - Net: `bo` works INSIDE the backoffice pair, but `ops` is still the live wired contract
    with the frontoffice. Finishing REQ-006 = re-point frontoffice→backoffice to `bo` + retire `ops`.
- **Declared backlog (requirement.html): 11 Partial + 8 Planned = 19 open**, hub is stale
  (understates backoffice progress; no cards yet for the bo item-centric model). Team-doable
  alone: UC-016 (cap bar onto calendar), UC-027 (bulk confirm), UC-030 (time-of-day teacher
  hours), UC-031 (manual course extend/recompute), TC-012/TC-015 (tests). Human-gated:
  UC-024/API-023 (real FT/PT rates), UC-033 (Bike/Scooter split decision), UC-028/032/025
  parent-push (customer LINE brief), auto-cut deploy.
- **Tech debt confirmed (team-doable maintenance):** dead reschedule code (~18 files, medium;
  keep enum for legacy rows), wallet debit race = no row lock (small, money-correctness),
  FE lint broken on Next 16 (small, both FE), debug console.logs / wrong-port "3002" strings /
  stale FE mock / byte-identical copied capture script (all trivial).
- No REQ created — presented the survey to the stakeholder for prioritization (Thai).
  Nothing routed to Sober yet.
