# 2026-09-22 — smart-scheduler log

## Tanya (QA) — ✅ REQ-101 (ECA Manage plan) + REQ-102 (money mask/leak) PASS on sid (verify 50). REQ-102: set a real budget on a throwaway freelance teacher → super-admin figures VISIBLE; role w/o teachers.budget-view → all 4 figures NULL (booleans stay); with key → VISIBLE; ⭐LEAK: linked teacher acct + budget-view role → GET /teachers figures still ALL NULL (scoped masked regardless of grants — leak closed). REQ-101: create ECA 201; manage-read title/kind ECA/heads 12/time 14:00; confirm-all {confirmed:4}; add teacher fromDate → future rows A+B, earlier untouched (03-08/15 Ek, 03-22/29 Ek+Haris); remove {removed:2}; swap to busy coach → 409 SLOT_TAKEN naming date, nothing moved (rollback); add colliding date → 409 DATE_EXISTS; cancel-all WITHOUT key 58 → 403 FORBIDDEN, WITH key 58 → 200 {cancelled:4}. FE/owner-gated (not tested): Manage-plan toast, row→modal, Series-in-range, k-attended-kept, pre-backfill rows, ADDED/REMOVED coach LINE notices (offer demo-OA capture). Footprint: throwaway FL teacher archived, ECA cancel-all'd, roles deleted, 0 live swept. Report in inbox/PM.md.
# 2026-09-22 — smart-scheduler log

## REQ-101/102 PASS on sid; Tanya to capture coach LINE notices before uat
- Tanya: REQ-101 (Manage-plan: confirm-all/add-teacher-from-date/remove/swap-409/add-date-409/cancel-all key-58 gate) + REQ-102 (money mask, budget-view key, ⭐ GET /teachers leak closed for linked teacher accounts) all PASS on sid (verify 50).
- Owner: capture coach ADDED/REMOVED (+ cancel-all if bytes baked) notices on demo OA before uat. Dispatched @Tanya. cancel-all bytes: Sober baking (Porter to confirm before the uat build).

## Tanya (QA) — 📱 Coach LINE notices (demo OA, before uat): ADDED ✅ + REMOVED ✅ delivered, correct shape; cancel-all notice NOT sent (pending bytes). Linked demo teacher Toth → ECA series QA-ECA-notice (Ek primary, 3 dates). Add teacher → 📅 เพิ่มตารางสอน (ADDED) delivered: รายการ QA-ECA-notice, Time 14:00-15:00, 3 dates 05/12/19-04-2027 DD-MM-YYYY. Remove → ❌ นำออกจากตารางสอน (REMOVED) delivered, same block. Cancel-all → 200 cancelled:3 but NO coach bubble arrived (chat ends at re-add ADDED) = cancel-all bytes not baked (Sober pending) → noted, NOT failed (per Porter). Delivery+shape confirmed for ADDED/REMOVED (no silent wrong-path). TEXT=owner's read. Footprint: Toth LINE unlinked, ECA cancel-all'd, 0 live, 0 pending reqs, device authorized. Report in inbox/PM.md.

## Coach notices: ADDED/REMOVED PASS; cancel-all notice NOT firing → back to Sober
- Tanya demo-OA: ADDED + REMOVED delivered correctly (title/time/dates). Cancel-all = NO coach bubble (action 200 but no notice enqueued). Not just unbaked bytes — the notice isn't firing.
- Porter → @Sober: bake the cancel-all words AND make cancel-all enqueue the coach notice; re-run green → Tanya re-capture → uat. Only open item before uat.

## REQ-102 §6 addition (customer): separate coach-rate (ค่าสอน §13.3) key, view=edit coupled
- Customer 2026-09-22: the coach-rate EDIT menu needs its OWN permission, see⇔edit coupled (no see-only). Revises the §4 exclusion (§13.3 rate was "do not mask"). Recorded REQ-102 §6; dispatched @Sober to size + say whether it folds into the current build (before uat) or is a small follow.

## Sober — cancel-all notice cause found; REQ-102 §6 sized and folded
- **@Sober** — cancel-all notice gated to CONFIRMED rows (`other-series.service.ts:124`); Tanya's series PENDING ⇒ none. **TASK-430** (BE, S, defect): notice on every live row + REQ-101 §5 bytes. **REQ-102 §6/§7:** key 59 `action:bookings.coach-rate`, view ⇔ edit, independent of 57 — BE S (**TASK-431**, contract first) · FE S (**TASK-432**); folded before `uat` (rides the same re-test round). OTHER/GROUP rates kept out — flagged to Porter. Ball @Jason (430 then 431 contract).

## REQ-095 §13.4 (customer): DUO New-course UI tweaks
- Drop "(two kids)" from the toggle; add programs "Duo INLINE SKATE" + "Duo SURFSKATE"; filter the DUO Program dropdown to Duo-prefixed programs only. Recorded REQ-095 §13.4; dispatched @Sober to size + say whether the two new programs seed here or are backoffice subject-mgmt, and fold-vs-follow timing.

## Jason (BE) — ✅ TASK-430 CODE DONE: cancel-all's coach notice fires on every live row (PENDING included) — the CONFIRMED-only gate removed from the series path only; REQ-101 §5's owner-accepted CANCELLED bytes replace the placeholder (own EN labels `Program`/`Date`). 2691/0, tsc 0; 8/8 mutations bite. Awaiting review; TASK-431 contract next.

## §13.4a owner refinement: DUO programs separated by subject TYPE, not by word
- Owner: add a subject TYPE/category flag; filter the DUO Program dropdown by TYPE (not name-prefix "Duo"). Seed Duo INLINE SKATE / Duo SURFSKATE here now; future self-serve creation likely backoffice (price/money). Recorded REQ-095 §13.4a; told @Sober (supersedes §13.4 point 3's name-prefix filter).

## Jason (BE) — 📋 TASK-431 contract in the TASK: the §13.3 rate has two pure-mapper producers (`rate`, `classRateMinor`) and no LINE reader; the seam = ONE response mask at `/api/*` after the guard (the DTO builders have no viewer; scan-pinned two producers); the write check at the three routes (the discount precedent); a DUO create needs the key (its rate is required). Waiting on Sober.
- **@Sober** — 📥 QUEUED (owner's order: after Jason's current work): **REQ-095 §13.4a** — subjects get a TYPE flag (DUO), the two seeded DUO subjects tagged, the DUO Program dropdown filtered by TYPE not name-prefix; designed for a clean later move to backoffice. To size + fold-vs-follow once TASK-430/431 land. Noted, not started. (Jason's TASK-431 CONTRACT is in `inbox/SA.md` — read on the next nudge.)
- **@Sober** — TASK-430 re-verified (2691/0) ⇒ ✅ DONE — REVIEWED. TASK-431 contract CONFIRMED (response-mask seam + scan pin; route-level check; DUO create needs the key → owner via Porter). TASK-432 §0 set, Fern on pure parts. Ball @Jason (431 build).

## REQ-103 (customer): whole-voucher cancel (like whole-course cancel)
- Distinct from REQ-100 (single voucher booking cancel, done). Cancel the entire voucher entitlement; remaining-entitlement handling is the customer's, out of scope — just need the cancel. Recorded requirements/REQ-103; dispatched @Sober to analyse + size.
- **@Sober** — 📥 QUEUED (behind §13.4a): **REQ-103 whole-voucher cancel** — ANALYSE only (does the whole-course cancel/`END_REASONS` shape extend to a voucher; remaining balance · future placed sessions · past draws; reason reuse; a key). Size + owner decisions after TASK-431/432 land. Noted, not started.

## DEFECT (REQ-101): Manage-plan PAGE 502s on a backfilled series
- Owner repro sid: /scheduler/other/d2b80ca9 (backfilled "ABC/Balance Camp·ECA·New") → 502 origin error; rest of app fine (route-specific crash, not host down). Tanya's API 200 was on a freshly-created series; the fail is a BACKFILLED one → the page/endpoint likely assumes a field the backfill didn't set.
- Dispatched @Sober to debug+fix (BE GET /other-series/:key on backfilled key, or FE SSR). Blocks uat. Then sid re-check driving the actual PAGE on a backfilled series.

## Jason (BE) — ✅ TASK-431 CODE DONE: key 59 `action:bookings.coach-rate`, view ⇔ edit — ONE response mask at `/api/*` after the guard (exact keys `rate`/`classRateMinor`; the two-producer scan pinned), `assertMayEditCoachRate` at the three routes; independent of 57 both ways; linked never. 2700/0 (+9), tsc 0; 16/16 mutations bite. Awaiting review.
- **@Sober** — 📥 QUEUED at the FRONT (a DEFECT, blocks `uat`): **REQ-101 Manage-plan page 502 on a BACKFILLED series** (`sid`, key `d2b80ca9-…`, "ABC / Balance Camp · ECA · New"; a created series is 200). First read to do: `GET /other-series/:key` on a backfilled key (a null the header assumes — `templateOf` / `otherTitle` / rates on rows the script never touched?) vs the FE SSR. To dispatch on the next nudge, ahead of §13.4a and REQ-103. (Jason's TASK-431 DONE is in `inbox/SA.md` — review on the next nudge.)
- **@Sober** — TASK-431 re-verified (2700/0, key 59) ⇒ ✅ DONE — REVIEWED; Fern wiring 432. 🔴 502 on a backfilled Manage-plan page ⇒ **TASK-433** (BE debug on backfill-shaped fixtures; the page is client-fetched) + DATA REQUEST to the owner (FE server log + curl of the API on that key). Ball @Jason (433).

## Jason (BE) — 🔎 TASK-433 read: the API answers 200 on every backfill-shaped fixture through both of the page's calls (pinned, +4 tests, 4 mutations bite; 2704/0); the 502 is the Next server's — hypothesis: hard-load vs soft-nav (the created series by pasted URL settles it in a minute). Ball @Sober.
- **@Sober** — TASK-433: API cleared (Jason, 2704/0, +4 pins); FE route boundary read by me — clean. The 502 needs a `sid` fact: owner's hard-load check on the CREATED series' URL + FE server log + curl (+ is the front build the one with the route). Told Porter. Ball @Porter (owner's check). Fern wiring 432 meanwhile.

## 502 — owner steer: routing mismatch, not build/data
- Owner: he built fresh; asks whether the Manage-plan button routes to the wrong path. Porter hypothesis: button href `/scheduler/other/<key>` (singular) vs the actual page route (API is `other-series`) — a link↔page path mismatch would 502/404 on ANY key. Dispatched @Sober/Fern to verify the button href ⇔ the page route file match and fix. uat blocker; rides the TASK-430/432 round.

## RE-SPEC (owner): Manage plan = modal, retire the separate page (kills the 502)
- Owner: the separate /scheduler/other/[key] page is inconsistent (app is modal-based) and is the 502 source. Decision: Manage plan becomes a MODAL reached two ways — the OTHER-block BookingModal's button, and a clickable "Series in range" row — same modal, no navigation. Retire the page + seriesHref + ROUTE_ALIASES guard entry. Recorded REQ-101 §6; dispatched @Sober (supersedes the 502 debug TASK-433 — the route is deleted). Folds into the current pre-uat round.

## OWNER GATE: uat held until the whole list is complete (no piecemeal)
- Owner: this work (REQ-101 modal) AND all previously-listed items must ALL be done before uat — no per-item deploys. Told @Sober to treat everything as one batch.
- The list: (1) REQ-101 ECA (Manage-plan modal + cancel-all + notices + add-teacher); (2) REQ-102 freelance-ceiling key + key-59 coach-rate §6; (3) REQ-095 §13.4/§13.4a DUO UI + subject type; (4) REQ-103 whole-voucher cancel; (5) REQ-077/051 Rich menu / QR check-in (BIG, still draft — asked owner to confirm if in-gate).
- Camp/Group/single-voucher-cancel already live on uat (done earlier).

## REGRESSION (customer, uat): coach "session moved-out" notice stopped firing
- Khwan screenshot (Coach Bank OA): "📮 คาบสอนนี้ถูกย้ายออกจากตารางของคุณแล้ว · นักเรียน พอ ชิตวร · Private SURFSKATE · 27-09-2026 11:00-12:00" fired before, now doesn't ("ระบบไม่ยอมแจ้งย้ายคาบ"). This is the teacher-reassign/move notice (should fire, not the accepted date/time-move-silent case).
- Prime suspect: TASK-425 notice-builder fold (live on uat since 09-21). Dispatched @Sober to debug (repro move/reassign, git-compare the move/unassign path pre/post TASK-425), report cause, and say hotfix-now vs ride-the-batch.

## Owner: Rich menu/QR out of gate (next round); key-59 "separate everything"
- Rich menu/QR (REQ-077/051) = SEPARATE, NEXT round — not in this uat gate. Customer likely provides the rich menu for us to insert. uat gate now = 4 items: REQ-101 modal · REQ-102+key59 · REQ-095 §13.4 · REQ-103.
- key 59: owner "แยกแม่งให้หมด". Porter interpretation (pending owner confirm): (Q1) decouple create from the rate perm — coach rate OPTIONAL at create so a user without key 59 can still create a DUO; (Q2) gate ECA/Group per-teacher rates too, each as its own — every coach-rate/rate surface gated. Awaiting owner's yes before dispatching Sober.

## key-59 final rulings (owner accepted Porter rec) → Sober
- Q1: coach rate OPTIONAL at create (decouple; no key 59 can still create DUO/course, rate filled later by a key-59 holder). Q2: key 59 gates the coach rate EVERYWHERE incl ECA/Group per-teacher rates, ONE key (not per-type). Two independent keys total: budget-view + key59. Recorded REQ-102 §8; dispatched @Sober to expand §6 scope + size.
- **@Sober** — REQ-102 §8 (owner): key 59 everywhere incl. ECA/Group `teacherRates`; rate optional at DUO create ⇒ **TASK-434** (BE S, light contract) + TASK-432 §0b amendment (FE). Sizes to Porter. Ball @Jason (434).

## Jason (BE) — ✅ TASK-434 CODE DONE: key 59 on every rate surface — `teacherRates` joins the mask (OTHER/GROUP/series) and the write check (six more routes, nine total); `duo` alone passes, `duo.classRateMinor` optional at create; the producer scan extended, `rateMinor` has no DTO producer. 2711/0 (+7), tsc 0; 13/13 mutations bite. Awaiting review.
- **@Sober** — TASK-434 re-verified (2711/0) ⇒ ✅ DONE — REVIEWED. BE batch complete. Ball @Fern (432).

## [--:--] Fern (FE) — ✅ **TASK-432 done (to §0b) — every coach-rate surface behind key 59 (absent, not dashed); ONE pure `withoutRates` guard at all seven body sites; the DUO create door stays (the field alone absent); 57 ≠ 59 pinned; snapshot 58 → 59.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  533 pass / 0 fail   (was 528; +5)
bun run build     → ok
```
12 source modified · 15 tests re-pinned · 1 new. Built to §0 + §0b (TASK-431 + TASK-434). The 59th key after
`teachers.budget-view` (the BE's slot, by position). Notes in the TASK §2. 🚫 No deploy asked.
`withoutRates(body, canRate)` value-tested (strips exactly `classRateMinor · teacherRates · rateMinor` without the key, a
Clear too) — applied at the OTHER create, the Group create, the OTHER edit, the Manage-plan Add-teacher + Edit-header,
the move, the DUO create (`duoBody(d, canRate)`). The surfaces: the shared `OtherScheduleFields` rate inputs (one place),
the Manage-plan header rates + Add-teacher box, the session box + tag + Clear, the course card's default line, the DUO
field — each behind `can(coach-rate)`, absent not dashed. §0b: the DUO door stays, `duoReady` no longer demands the
rate, `duo: { coStudentId }` alone rides; `teacherRates: null` reads as absent. 57 and 59 independent (pinned).
🔑 **Break-and-watch ×15 in `try/finally`** ⇒ 1–3 each, none slipped; md5 identical on 10 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-432 re-verified (533/0, key 59) ⇒ ✅ DONE — REVIEWED. **Batch complete both sides** (BE 2711/0, FE 533/0, 50 = 50, 59 keys). Porter has the owner's list (sid restart, 502 fact, Tanya's round, uat steps). Ball @Porter.

## Reconciled Sober "done": 2 items NOT yet reflected — re-pinged
- Sober reported the REQ batch code-complete (BE 2711/0, FE 533/0). BUT his messages predate my (a) REQ-101 §6 MODAL re-spec and (b) the move-notice regression — his batch-complete still frames the 502 as a page-to-debug (superseded by the modal) and never mentions the regression.
- Porter did NOT declare done. Re-pinged @Sober to confirm the modal re-spec is being built (retire the page, no 502 chase) and the regression is being diagnosed (hotfix-vs-batch). Batch NOT ready for sid until both land.
- **@Sober** — ⚠️ missed three Porter messages last turn (read only the last block) — corrected: the batch is NOT ready. **TASK-435** (FE: Manage plan MODAL; page/`seriesHref`/alias retired; 502 chase withdrawn, DATA REQUEST cancelled). **TASK-436** (BE: `teacher_unassigned` regression — producer intact, no enqueue removed in git; suspects: worker render post-TASK-425 → notice-days gate → the popup door/Finding A; DATA REQUEST: `line_outbox` rows for the booking + which screen). Owner gate noted: one batch incl. §13.4a + REQ-103 (+ 077/051 pending). Ball @Jason (436); Fern on 435 in parallel.

## Jason (BE) — 🔎✅ TASK-436: delivery and the gate CLEAN by value; the DOOR is the cause — `moveBooking` never told the coaches. ONE `sendTeacherReassigned` helper now serves both doors (plan edit + move), date/time-only silent. 2718/0 (+7), tsc 0; 8/8 mutations bite. Hotfix-sized (one file). Ball @Sober.
- **@Sober** — TASK-436 re-verified (2718/0) ⇒ ✅ DONE — REVIEWED. Cause = the door: the Move popup never notified; one `sendTeacherReassigned` from both doors. Hotfix recommended (owner's call under the gate); outbox DATA REQUEST withdrawn; popup's notice-days gap ⇒ owner's list. Ball @Porter (hotfix decision). Fern on 435.

## Owner: regression rides the batch (no hotfix); B pending
- A: owner "รอไปด้วยกันหมด" → TASK-436 (moved-out notice fix) rides the batch, no hotfix. Told @Sober.
- B (notice-days gate on Move-session popup: fold vs leave) still pending owner; Porter recommends fold-in for consistency.

## B ruling: popup NOT gated by notice-days (admin affordance) — batch scope LOCKED
- Owner "ปล่อยไป มันเป็นเรื่องการใช้งานแอดมิน" → do not fold the notice-days block into the Move-session popup; the reassign NOTICE still fires (TASK-436), only the block isn't added. Recorded REQ-101 §7.
- Batch scope LOCKED: REQ-101 (modal + cancel-all + notices + add-teacher + reassign-notice) · REQ-102 (coach-rate key §4-§8) · REQ-095 §13.4/§13.4a · REQ-103. No hotfix. Awaiting TASK-435 (modal) + §13.4a + REQ-103, then sid → Tanya → single uat.
- **@Sober** — A/B rulings noted (no hotfix; popup un-gated). SPEC-089: §13.4a (`subjects.kind`, seed script, filter + server rule; BE S · FE S, `0050`) and REQ-103 (voucher `ended_*` + `/vouchers/:id/cancel` on the course-end shape, future draws cancelled, balance frozen; BE S–M · FE S, `0051`). Six owner decisions to Porter. Ball @Porter (rulings) — Fern on 435.

## SPEC-089 rulings (owner "เอาตามนั้น") → Sober cuts tasks
- §13.4a: hide DUO from Private dropdown (yes); server refuses DUO-on-non-DUO-subject (yes); subjects.kind + seed + FE filter; migration 0050. Recorded REQ-095 §13.4b.
- REQ-103: reuse bookings.course-cancel (no new key); coach notice = course-end + voucher line; family silent; card "ENDED · Nh left"; ended_at/by/reason + cancel/preview, future draws→CANCELLED, balance frozen; migration 0051. Recorded REQ-103 §4.
- Both fold the held batch (migrations → 52). Awaiting TASK-435 modal + these, then sid → Tanya → single uat.
- **@Sober** — SPEC-089 rulings all as recommended ⇒ TASK-437/438 (§13.4a, `0050`) + TASK-439/440 (REQ-103, `0051`) cut; contract-first on 437 and 439. Deploy list gains `ensure-subjects`. Ball @Jason (437 contract); Fern on 435.

## Jason (BE) — 📋 TASK-437 contract in the TASK: no subject editor exists (two readers carry `kind`; the seed + `add-subject --kind` write it); the rule before both course creates' tx, reverse refusal yes; the seed as an UPSERT (the uat rows exist as PRIVATE); `0050` with the CHECK as witness. Waiting on Sober.
- **@Sober** — TASK-437 contract CONFIRMED (reverse refusal; seed as upsert; `add-subject --kind`; single/trial left). TASK-438 §0 set. Porter: confirm the two DUO subject names; the courses-only scope note. Ball @Jason (437 build → 439 contract).

## Jason (BE) — ✅ TASK-437 CODE DONE: `subjects.kind` (`0050` ⇒ 51, the CHECK as witness ⇔ `SUBJECT_KINDS`), the rule before both course creates' tx (both refusals), the two readers carry `kind`, `subjects:ensure` upsert (planner pinned) + `add-subject --kind`. 2729/0 (+11), tsc 0; 14/14 mutations bite. Awaiting review; TASK-439 contract next.

## Jason (BE) — 📋 TASK-439 contract in the TASK: the course-end shape read (no date rule, no holds reconcile — flagged), the voucher twin = three pieces + the ONE sender generalised; no existing "Voucher" label ⇒ `<subject> 10 HR`; `voucherUsable` the ONE gate gains `endedAt`; `voucherStatus` created; the undo allowed; two corrections (preview field names, body `reason`). Waiting on Sober.
- **@Sober** — TASK-437 re-verified (2729/0, 51 = 51) ⇒ ✅ DONE — REVIEWED. TASK-439 contract CONFIRMED (split; no-new-bytes line; undo allowed; corrections; `cancel_reason`; course-end holds gap fixed as a line). TASK-440 §0 set. Ball @Jason (439 build). Fern on 435 → 438 → 440.

## Jason (BE) — ✅ TASK-439 CODE DONE: `vouchers.ended_at/by/reason` (`0051` ⇒ 52, the CHECK ⇔ `END_REASONS` as witness), `voucherUsable` refuses ENDED first (draw/picker/SOM), `voucherStatus` on the DTO, `endVoucher` + preview on the course-end shape through the shared `cancelDoomedRows` (holds now on the course end too, by value), ONE coach notice `voucher_ended` byte-identical to an END, no family notice, routes under `course-cancel`. 2749/0 (+20), tsc 0; 18/18 mutations bite. Awaiting review.
- **@Sober** — TASK-439 re-verified (2749/0, 52 = 52) ⇒ ✅ DONE — REVIEWED. **BE batch complete.** Porter has the final deploy list. Ball @Fern (435 → 438 → 440).

## [--:--] Fern (FE) — ✅ **TASK-435 done — Manage plan is a MODAL on the calendar: two entry points (the OTHER block's button · a `Series in range` row), ONE instance; a row hands off to the calendar's single BookingModal; the page, `seriesHref`, the alias and the toast link retired.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  533 pass / 0 fail
bun run build     → ok — no `/scheduler/other/[key]` in the route list
```
7 source modified · 1 renamed · 1 deleted · 3 tests re-pinned. No API change. Notes in the TASK §2. 🚫 No deploy asked.
`OtherSeriesModal` = the page's body in a Mantine `Modal`; a row inside CLOSES it and hands the booking to the calendar's
one `BookingModal` (`onOpenBooking`) — no modal-over-modal, no z-index (my call, pinned). The OTHER block's `Manage
plan` is a button (key AND host; closes the booking modal first); a `Series in range` row is a button; the calendar owns
`seriesKey`. Retired: the route (absent from the build, asserted), `seriesHref`, `ROUTE_ALIASES` (`navItemForPath`
byte-back; the nav pins never moved in 429 — an alias, not an item), the create toast's link (dropped — the new series
shows in `Series in range` and on its rows).
🔑 **Break-and-watch ×7 in `try/finally`** ⇒ 1 each (one slipped first — an `indexOf` pin — now the handler's own
sequence); md5 identical on 4 files. Next: TASK-438, then TASK-440.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.

## [--:--] Fern (FE) — ✅ **TASK-438 done — the DUO create's Program list = DUO subjects only (pure `subjectsFor`, the server's `kind`, never the name — scan-pinned); every other COURSE picker hides them; the toggle clears a stale selection.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  536 pass / 0 fail   (was 533; +3)
bun run build     → ok
```
6 source modified · 1 new test. Built to §0 (TASK-437). Notes in the TASK §2. 🚫 No deploy asked.
`subjectsFor` value-tested (both modes; a missing `kind` = PRIVATE; a DUO-named private stays private). The walk: the
New-course picker follows the toggle; `ImportBalanceModal` + the unmounted `CreateCourseModal` hide DUO; the calendar's
single/trial picker and the Bookings filters untouched (asserted). The 400s = the server's sentence. Next: TASK-440.
🔑 **Break-and-watch ×6 in `try/finally`** ⇒ 1 each, none slipped; md5 identical on 3 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.

## [--:--] Fern (FE) — ✅ **TASK-440 done — the voucher card's `Cancel voucher` door (the course's key) → the ONE cancel dialog for course + voucher; `Ended · Nh left` from the server's `status`; no client derivation (asserted). The held batch's FE side is complete: 435 · 438 · 440.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  543 pass / 0 fail   (was 536; +7)
bun run build     → ok
```
9 source modified · 2 new (`lib/scheduler/voucher.ts` + test) · 1 pin moved (sweep 96/36 → 97/37). Built to §0 (TASK-439). Notes in the TASK §2. 🚫 No deploy asked.
Pure `voucherDoors` (cancel = key AND not ENDED; book = ACTIVE, mirrors the BE `voucherUsable`) / `endedLabel` /
`voucherChip` value-tested. `EndCourseDialog` takes `target: { kind, id }` — the voucher pair or the course pair; the
voucher face lists the doomed draws + *Nh left — kept for the customer* (the server's `remaining`); the cancel body
pinned byte-equal to the course's. **Said plainly:** the FE voucher card has NO book door — a draw is booked from the
calendar's create form whose picker is the server's eligible list (already omits ENDED); nothing on the card to hide.
Copy +8 both languages. 📖 the Thai word (บัตรชั่วโมง as the TASK wrote vs the panel's วอยเชอร์) is Porter's — one line.
🔑 **Break-and-watch ×9 in `try/finally`** ⇒ 1–2 each, none slipped; md5 identical on 4 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-435/438/440 re-verified (543/0, tsc 0, route absent, no name rule) ⇒ ✅ DONE — REVIEWED ×3. **Locked batch complete both sides** (BE 2749/0, 52 = 52; FE 543/0). Porter has Tanya's headlines + the deploy list. Ball @Porter (sid restart → Tanya).

## Locked batch deployed to sid — Tanya big round released
- Owner sid: db:migrate (0050+0051) → verify ✅ 52 / ledger 85 / no 🔴 (no ledger skip). ensure-subjects --apply = 2 DUO subjects created (Duo INLINE SKATE / Duo SURFSKATE, kind DUO, balance-duo). backfill = 0 (all keyed). grants 57/58/59 + Teacher role, camp_reminder off. Code restarted.
- Batch on sid: REQ-101 (Manage-plan MODAL, page retired) + cancel-all + reassign-notice fix · REQ-102 (key 57/59) · §13.4a (subjects.kind + filter) · REQ-103 (whole-voucher cancel) + whole-course-cancel slot-hold fix.
- @Tanya big round released (inbox/QA.md). Last gate before single uat.

## Tanya (QA) — ✅ Big round API items ALL PASS (verify 52). §13.4a: DUO on non-DUO subj → 400 NOT_A_DUO_SUBJECT; DUO on DUO subj → 201 kind DUO; Private on DUO subj → 400 DUO_SUBJECT. key59 (bookings.coach-rate, isolated w/ all-keys-except-59): without → rate DTO null, PATCH classRateMinor 403, DUO create no-rate 201, DUO create with-rate 403; with → rate visible {eff50000}, PATCH 200 (teacherRates masked too). REQ-103 whole-voucher cancel: preview removedSessions:1, cancel 200 → future draw CANCELLED, used kept(1), remaining frozen(4), ENDED; new draw → 409 VOUCHER_ENDED (confirm-future-voucher one-off 500 didnt reproduce, not a finding). Whole-course cancel FREES slots: re-book same teacher/date/time → 201 (holds-fix). Round-57 green: key57 budget+leak, key58 cancel-all, manage-read 200 no-502. 🔴 BLOCKED: demo-OA LINE captures (cancel-all bubble + reassignment pair) — phone screen LOCKED (woke to lock screen w/ personal notifs = NOT demo OA, discarded; wont PIN/swipe). Owner: unlock CPH2735 + reopen SOM-Balance-Demo chat → I capture. ADDED/REMOVED already green (R58). Footprint: 0 live, teachers archived, roles/users cleaned. Report in inbox/PM.md.

## Tanya (QA) — 🟠 demo-OA retry: phone unlocked, demo OA up, but LINE PUSH notices NOT rendering (DND on). Re-linked Toth (bot replied to typed input instantly), approved (200 linked), ECA confirmed, cancel-all → 200 cancelled:3 (API works). BUT no approve-notice + no cancel-all bubble on the phone through ~1min (chat stuck at "link request sent"). Tell: bot REPLIES to typed msgs render instantly, server PUSHes don't → phone-side DND/push issue (moon icon active), not backend (R58 same pushes rendered fine). Didn't toggle DND / didn't exit to chat list (forbidden). Cancel-all bubble + reassignment pair still OPEN, blocked on push. Owner: turn OFF DND on CPH2735 + ping → I re-fire cancel-all + reassignment. ADDED/REMOVED green (R58); all REQ-101/102/103/§13.4a API PASS. Footprint: Toth unlinked, ECA cancelled, 0 live. Report in inbox/PM.md.

## Tanya (QA) — demo-OA retry: phone is on the notification shade / personal content, NOT the SOM-Balance-Demo chat. Discarded the screenshot (didn't read personal notifs); won't tap the shade or open LINE's chat list (personal chats). Need owner to OPEN the SOM-Balance-Demo LINE chat, then I capture cancel-all + reassignment. Nothing changed (no footprint).

## Tanya (QA) — 🔴 CEILING: demo phone won't render server-PUSH LINE notices (3 tries, DND off). Re-linked Toth clean (bot replied to typed steps instantly, link-sent 09:27), approved (200 linked), ECA confirmed, cancel-all → 200 cancelled:3. But NO approve-notice + NO cancel-all bubble on phone through ~2min. Diagnosis: bot REPLIES to typed input render instantly (connectivity fine) but server PUSHes don't (R58 same pushes DID render → env/state issue not code) — likely LINE per-OA push throttle or app notif block for this OA = phone/LINE-settings thing I may not touch. cancel-all + reassignment visual = owner on-phone (his LINE-text rule anyway). ADDED/REMOVED solid (R58); all REQ-101/102/103/§13.4a/course-slot API PASS. Rec: dont hold uat on these visuals. Footprint: Toth unlinked, ECA cancelled, 0 live. Report in inbox/PM.md.

## Customer full checklist (Khwan) — reconciled; 4 gap-checks to Sober (REQ-104)
- Mapped her list: ECA ✅(batch), เพดานเงิน ✅(57), แก้ค่าสอน ✅(59), Cancel Voucher ✅, Rich menu/QR = next round. 
- UNCERTAIN → gap-check @Sober (no build): (1) GROUP confirm/manage parity with ECA; (2) CAMP coach rate per-day; (3) CAMP check-in shows remaining credit + parent notify; (4) CAMP redeem lists only kids-with-credit. Recorded requirements/REQ-104. Did NOT tell owner "all done" — awaiting Sober's gap read.

## Owner plan: finish the 4 gaps, whole thing to sid, CUSTOMER tests on sid before uat
- Owner "ไปเก็บให้ครบก่อน แล้วขึ้น sid แล้วให้ขวัญเทสก่อน" → do NOT deploy uat yet. Finish REQ-104's 4 gaps (Group parity · Camp per-day rate · Camp check-in credit+parent-notify · Camp redeem filter) into the batch, deploy the whole set to sid, then Khwan does customer-UAT on sid (som.develyst.online), then the single uat deploy on her OK.
- Sequence: Sober gap-check → owner rulings → build → sid (whole batch incl gaps) → customer test on sid → uat.
- The current sid-passed batch (REQ-101/102/103/§13.4a) waits and ships together with the gap work.
- **@Sober** — REQ-104 gap-check ⇒ SPEC-090: GROUP partly done (standing ✅, per-child confirm ✅; weekly coach digest ❌ S; group manage surface ❌ BE S–M/FE S) · camp per-day rate ❌ (none exists; BE S–M `0052`/FE S) · camp check-in credit + family notice ❌ (Private's "tell mum" is the day-end deduction; BE S + S–M/FE S) · redeem picker ✅. Next slice BE M+ · FE M. Ball @Porter (owner decisions).

## SPEC-090 gap rulings (owner "เอาตามแนะนำ") → Sober builds into batch
- G: one-button confirm-whole-group · weekly coach digest (Mon, words drafted) · manage surface = REQ-101 modal generalised to group_key (cancel-all cascades to seats+families, add/remove coach, add dates, edit). C: coach rate per-day behind key 59 (new-day default 0) · scan shows remaining credit · camp deduction family notice at day-end (parity, words drafted). Redeem already done.
- Recorded REQ-104 §2 + notice words. Dispatched @Sober to build into the held batch. Then ONE sid deploy of everything → Khwan customer-UAT on sid → uat.
- **@Sober** — REQ-104 GO (missed one message; corrected) + English-only ruling ⇒ TASK-441/442 (GROUP parity, weekly digest) + TASK-443/444 (camp day rate `0052`, scan credit, camp deduction). Contract-first 441 → 443. Ball @Jason (441 contract).

## Jason (BE) — 📋 TASK-441 contract in the TASK: four OTHER branches ⇒ one module keyed `{ otherSeriesKey } | { groupKey }`; the seat cascade reuses `cancelSeatsOfGroup` + `sendClassCancelledToFamilies` (the per-row path); the group swap delegates to `swapGroupTeacher` (the series swap never moved seats — finding); the weekly digest = a new Task-Scheduler exe + internal route + job (the box registration is the human's), English constants, no flag recommended. Waiting on Sober.
- **@Sober** — TASK-441 contract CONFIRMED (one module; group swap delegates; seat path reused; no flag; CONFIRMED-only; English constants). TASK-442 §0 set. Deploy list: register the Monday 08:15 digest task. Ball @Jason (441 build → 443 contract).

## Jason (BE) — ✅ TASK-441 CODE DONE: the series service keyed `{ otherSeriesKey } | { groupKey }` (OTHER byte-identical), seats on the DTO, the cancel-all cascade through the existing seat + family path (coach once), confirm-whole-group via `confirmCourse`, the group swap delegated, nine `/group-series` routes; the Monday digest `weekly_schedule_teacher` (English constants pinned under both langs, send-once, `job_runs`, internal route + exe). 2766/0 (+17), tsc 0; 21/21 mutations bite. Awaiting review; TASK-443 contract next.
- **@Sober** — TASK-441 re-verified (2766/0) ⇒ ✅ DONE — REVIEWED. Fern wires 442. Ball @Jason (443 contract).

## Jason (BE) — 📋 TASK-443 contract in the TASK: 🔴 Private's deduction fires at the scan too (not day-end-only) ⇒ (a) true parity vs (b) day-end pass with `deduction_notified_at` (recommended, honours §0, idempotent); a camp row has one teacher ⇒ `teacher_rate_minor` only, the sync updates kept rows; the 59 write needs one route line (the read is masked by name already); the two scan payloads. Waiting on Sober.
- **@Sober** — TASK-443 contract CONFIRMED ((b) day-end pass + stamp; ABSENT same; `(total−used)/2`; simplifications; route line). TASK-444 §0 set. Porter told the Private-vs-camp timing fact. Ball @Jason (443 build); Fern on 442.

## Jason (BE) — ✅ TASK-443 CODE DONE: `0052` (the stamp + the rates table as witness) ⇒ 53; the day rate behind 59 (DTO 0 by absence, the upsert before the ONE sync, the rows carry it incl. the kept ones, the route line, the mask pinned); the two scan payloads; `camp_deduction` as the day-end pass after the cut, stamp-idempotent, ATTENDED | ABSENT, English constants under both langs. 2776/0 (+10), tsc 0; 19/19 mutations bite. Awaiting review.
- **@Sober** — TASK-443 re-verified (2776/0, 53 = 53) ⇒ ✅ DONE — REVIEWED. **BE batch complete (REQ-094…104).** Final deploy list to Porter. Ball @Fern (442 → 444).

## [--:--] Fern (FE) — ✅ **TASK-442 done — ONE series modal, two faces: the GROUP face on `/group-series/:key` (seats per row · Confirm whole group · the cascade line · a GROUP row's block + Series in range as entry points); the OTHER face unchanged.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  550 pass / 0 fail   (was 543; +7)
bun run build     → ok
```
11 source modified · 1 new test · 2 pins moved (the OTHER wire pins → `seriesPath`; the sweep 97 → 101). Built to §0 (TASK-441). Notes in the TASK §2. 🚫 No deploy asked.
`SeriesRef { kind, key }` is the modal's address; pure `seriesPath` / `kindLabelKey` / `swapBody` (GROUP `{ to }`) /
`seatCascade` / `pendingSeats` / `groupSeriesDoors` (confirm-whole-group = `course-confirm` + a PENDING row OR seat)
value-tested. 🚫 No client cascade logic: the dialog's line counts the DTO (seats · students); the FAMILIES number is the
server's only (`familiesTold` in the toast) — §1's "m families" on the pre-act line is not knowable on the FE (siblings),
said in the TASK. The group header PATCH never carries a kind. Copy +4 both languages.
🔑 **Break-and-watch ×9 in `try/finally`** ⇒ 1 each, none slipped; md5 identical on 5 files. Next: TASK-444.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.

## [--:--] Fern (FE) — ✅ **TASK-444 done — the camp day editor's per-coach rate box (key 59; the server's mask honoured; sent only when changed; stripped without the key) · the check-in page's ONE `Remaining` line from the response (camp days · sessions · hours; none on `already`). The REQ-104 gap build's FE side is complete: 442 · 444.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  555 pass / 0 fail   (was 550; +5)
bun run build     → ok
```
6 source modified · 1 new test · 2 pins moved (camp copy 77; the editor's PATCH line → `withoutRates`). Built to §0 (TASK-443). Notes in the TASK §2. 🚫 No deploy asked.
Pure `dayRates` / `dayPatch` (+`teacherRates` only when changed; an off-day coach never rides; masked ⇒ never) and
`remainingLine` (camp `3.5/10 days` as sent; sessions | hours by the server's `unit`; `null` ⇒ no line) value-tested; no
client arithmetic asserted. Snapshot unchanged (59). Copy +5 both languages.
🔑 **Break-and-watch ×9 in `try/finally`** ⇒ 1–2 each; md5 identical on 4 files (one mutation first written value-identical —
a no-op, replaced by `Math.floor` on the half day, which bites).

**Ball: @Sober.** Nothing else waits on FE. TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-442/444 re-verified (555/0) ⇒ ✅ DONE — REVIEWED ×2. **Whole batch (REQ-094…104) complete both sides** (BE 2776/0, 53 = 53; FE 555/0). Porter has Tanya's headlines + the final deploy list. Ball @Porter (sid deploy).

## Whole batch (REQ-094…104) deployed to sid — Tanya gap round
- Owner sid: db:migrate (0052_camp_day_rates) → verify ✅ 53 / ledger 86 / no 🔴. pm2 restarted. (ensure-subjects/backfill already done prior round.)
- BE 2776/0 · FE 555/0 · 59 keys · 25 kinds. @Tanya to verify the REQ-104 gap items, then invite Khwan to customer-UAT on sid, then single uat.

## Tanya (QA) — REQ-104 gap (verify 53): GROUP confirm-whole (200 confirmed:6, course_confirmed per child) ✅; GROUP cancel-all key58 (200 cancelled:6/seatsCancelled:6/familiesTold:6) ✅ [familiesTold:6 w/ 1 family = per-seat count, flag semantic to Sober]; CAMP day rate key-59 mask (super PATCH 200; no-key teacherRates null + PATCH 403) ✅. 🔴 FINDING: camp-week-create INCLUDING TODAY → 500 INTERNAL (future week → 201); likely unhandled clash w/ near-term EK sessions (should be 409 not 500) — Sober/Jason check camp-week-create clash path. 🟠 camp scan Remaining:X/10 days BLOCKED (needs today camp day; today-week 500s; future-day scan correctly 409 CAMP_DAY_NOT_TODAY). 🟠 weekly digest + 17:30 CAMP CREDIT + all family/coach LINE delivery = job-runs + push-render ceiling (owner/on-phone). Footprint: 13 group bookings swept, camp weeks closed, roles/users cleaned. Rec: fix/verify the today-camp 500 before Khwan customer-UAT. Report in inbox/PM.md.

## Tanya gap round: GROUP+camp-rate PASS; 🔴 camp-week-create-500-on-today; 🔎 group cancel-all familiesTold semantic
- PASS: group modal/confirm-whole-group/cancel-all(key58); camp day-rate key-59 mask+403. 
- 🔴 DEFECT: create a camp week including TODAY ⇒ 500 (future=201); likely clash throws 500 not 409. Blocks a customer making "this week's" camp + Tanya's scan-Remaining staging. → Sober fix to clean 409.
- 🔎 CONFIRM: group cancel-all familiesTold:6 with 1 family/6 seats — per-seat vs per-household; must de-dupe to ONE class_cancelled_parent per household. → Sober confirm/fix.
- Blocked on push-ceiling/job-run (owner): weekly digest, 17:30 CAMP CREDIT USED, cancel-all bubble, scan Remaining.
- HOLD the Khwan sid invite until the 500 is fixed.
- **@Sober** — Tanya's gap round: camp-week-today create 500 + `familiesTold` semantics ⇒ **TASK-445** (409 on the clash; per-row household de-dupe; `familyNotices`/`householdsTold`). Fern's one-line toast follow after. Owner note: a one-summary family bubble = new words, later. Ball @Jason (445).

## Jason (BE) — ✅ TASK-445 CODE DONE: the camp-week 500 = the sync's catch reading the coach's name on the tx the clash had just aborted (`25P02`) — names read before the loop now, 409 pinned by value + through the root app; past dates derive nothing; group cancel-all's families = one household set per row (siblings once), `familyNotices` / `householdsTold`. 2783/0 (+7), tsc 0; 9/9 mutations bite. Awaiting review.
- **@Sober** — TASK-445 re-verified (2783/0) ⇒ ✅ DONE — REVIEWED. Cause = a read on an aborted tx (25P02) ⇒ SYSTEM-FACTS rule. Fern's toast line (442b) next. Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-442b done (the TASK-445 follow): the group cancel-all toast reads `householdsTold` (distinct families) + `familyNotices`; `familiesTold` gone from the contract/mock/dialog (pinned absent).** → @Sober
tsc 0 · **555 pass / 0 fail** · build ok · 5 files touched (service · mock · dialog · dictionaries · the group test). Copy: `{notices}` added to `cancelledGroup` in both languages (no new key). 🔑 ×2 mutations bite, md5 identical. Notes in the TASK §2b. 🚫 No deploy asked.

**Ball: @Sober.** Nothing waits on FE.
- **@Sober** — 442b re-verified (555/0) ⇒ ✅ DONE. Batch whole again (BE 2783/0, FE 555/0). Ball @Porter (sid → Tanya re-check → Khwan).

## TASK-445 fix on sid (verify 53) — Tanya re-check released
- Owner deploy: verify 53, no migration (code-only). @Tanya re-check: camp today-week over busy coach ⇒ 409 (not 500); group cancel-all sibling de-dupe (one bubble/family, householdsTold:1); stage the previously-blocked camp scan Remaining. Then sid is clear for Khwan's customer-UAT.

## Tanya (QA) — 🔴 demo-OA LINE push STILL not rendering (4th attempt: owner unlocked+reopened+DND off). Fresh Toth link (bot replied to typed register/teacher/Toth instantly, link-sent 13:53), approved (200), ECA confirmed, cancel-all → 200 cancelled:3. But NO approve-notice + NO cancel-all bubble on phone. CONSISTENT: bot REPLIES to typed input render instantly; server PUSHes (approve/cancel-all) never render — 4×, across unlock/reopen/DND-off. R58 same pushes DID render → this is a LINE per-OA push THROTTLE after many test rounds today (needs time to clear), not a reopen-fixable thing. cancel-all API works, bytes baked, ADDED/REMOVED green (R58) = builder proven. Rec: don't hold uat on the cancel-all/reassignment VISUAL — owner eyeballs demo phone when pushes flow (may batch later), or retry after the throttle clears. Footprint: Toth unlinked, ECA cancelled, 0 live. Report in inbox/PM.md (prior entry stands).

## Tanya (QA) — ✅ TASK-445 re-check ALL PASS (verify 53). (1) Camp-week over busy coach → 409 SLOT_TAKEN "2026-09-22 11:00 ครูHaris มีคาบแล้ว — ไม่ได้บันทึกอะไร" (was 500, FIXED); today-week w/ free coach → 201 (create healthy). (2) Group cancel-all de-dupe: one child/six rows → householdsTold:1 + familyNotices:6 (honest) — my earlier familiesTold:6 resolved, toast reads distinct households. (3) Camp scan: today day → 200 {status:ATTENDED, credit:{remainingDays:9,totalDays:10}} = Remaining 9/10 days; 2nd scan → already:true. Push-ceiling items (weekly digest, 17:30 CAMP CREDIT, cancel/reassign bubbles) still on demo-OA push throttle (Porter: not my gate) — ADDED/REMOVED green R58, APIs+bytes in. sid API/logic CLEAN for Khwan customer-UAT. Footprint: camp weeks closed, test teachers archived, group cancel-all'd, 0 live. Report in inbox/PM.md.

## TASK-445 re-check ALL PASS — sid CLEAN, ready for Khwan customer-UAT
- Tanya: camp today-week over busy coach ⇒ 409 (was 500), free coach ⇒ 201; group cancel-all householdsTold:1 (one child six rows), familyNotices:6 honest; camp scan ⇒ Remaining 9/10, 2nd scan already:true. All REQ-094…104 (13 acceptance items) green on sid (verify 53).
- Push-ceiling items (weekly digest, 17:30 CAMP CREDIT USED, cancel/reassign bubbles) = owner eyeball / sanctioned job-run; ADDED/REMOVED proven R58, APIs+bytes in.
- NEXT: owner invites Khwan to customer-UAT on sid (som.develyst.online); on her OK → single uat deploy.

## 🎉 WHOLE BATCH (REQ-094…104) LIVE ON uat — 2026-09-23
- Owner uat: db:migrate 0049→0052 → verify 53 (NO ledger skip this time). ensure-subjects --apply = 2 DUO subjects. backfill 0. weekly-teacher-digest tested manually (sent 2/7, dup 0) — works; Task registered Mon 08:15. grants 57/58/59 + Teacher role, camp_reminder off. pm2 restarted.
- ALL 13 acceptance items (REQ-104 §4) delivered to uat: Group (confirm-whole / weekly digest / standing) · ECA (manage-plan modal / confirm / notify edit-move-remove / cancel-all) · Camp (grid daily coach + per-day rate / check-in remaining-credit + day-end parent notice / redeem credit-only) · perms (key57 ceiling-view / key59 coach-rate see=edit) · Cancel Voucher (single + whole).
- Push-ceiling LINE visuals (weekly digest bubble / 17:30 CAMP CREDIT USED / cancel+reassign bubbles) = owner eyeballs on the live phone; APIs+bytes+builder all proven (ADDED/REMOVED green R58).
- Rich menu / QR check-in = NEXT round (owner deferred; customer likely supplies the rich menu).
- Owner expects a follow-up fix list from Khwan later.
