# Log — 2026-07-31 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [00:08] Sober (SA Lead) — sequenced REQ-019/020, verified the security claims, cut TASK-047 (leak-stop) → @Jason

Porter handed me two new REQs (REQ-019 people management, REQ-020 secure LINE pairing) and left the sequencing
to me. I verified REQ-020's claims in code before deciding, because "anyone can impersonate a teacher" deserves
evidence rather than escalation.

- **Claims verified — all three are real:** teacher matching is `rows.find(...)` (`:151`) so **first match wins**
  and binds if that teacher has no link; the parent-link success reply interpolates
  `kids.map(k => k.name).join(", ")` (`:172`) so **typing a parent's phone returns that family's children's
  names** to an unauthenticated stranger; unknown phone → `findOrCreateParentByPhone` (`:174`) creates a record.
- **📌 One correction I'm routing back to @Porter:** the third one is **not a defect**. "Unknown phone creates a
  parent" **is** parent self-registration — which คุณฟีน confirmed must stay (REQ-020 #2, REQ-019 #5). The
  junk-record risk is mitigated by REQ-019's **view + suspend**, not by blocking creation. The REQ's problem
  statement reads like it's a bug, so please make sure that doesn't get built away by accident.
- **Sequencing decision (mine, recorded so nobody infers it) — REQ-020 is STAGED (SPEC-015):**
  - **Stage 1 = TASK-047, now.** The leak and the silent collision need **no staff UI**: replace the children's
    names in the parent-link confirmation with a **count** (keeps the "you hit the right account" purpose, drops
    the PII), and make a **multi-match teacher nickname bind nobody** with a "staff must complete this" reply.
    Backend-only, no migration. **This is BE priority #1** — it's a live disclosure and it's cheap.
  - **Stage 2 = the approval queue + unlink/suspend, after REQ-019.** REQ-020's own constraint puts the staff
    surface on REQ-019's people screens; building the queue first would gate teacher onboarding with **nowhere to
    approve**. So: REQ-019 (screens, also unblocks REQ-013 demographics) → then Stage 2.
  - The business scope is unchanged — this is a build-order call, which is mine; I'm flagging it rather than
    letting it look like scope creep.
- **@Porter — two things:** (1) the self-registration correction above; (2) **Q3 (dual-role: is anyone both a
  teacher and a parent?) is still unanswered and now blocks Stage 2** — "can one person hold both" changes the
  whole approval/pending model, so please get it before I design that. Also worth mentioning to คุณฟีน that the
  parent-link confirmation message will change wording (count instead of names).
- **@Jason: TASK-047 first**, then TASK-044 (`.ics` feed) which is still TODO.
- **Next in my spec queue:** REQ-019 (people screens + the 4 demographic fields + suspend), then REQ-020 Stage 2,
  with REQ-009 / REQ-013 / REQ-014 / REQ-017 behind them.
- **Nothing waiting on Sober** until TASK-047/044 reach REVIEW.

## [10:20] Jason (BE) — TASK-047 built → REVIEW → @Sober (🔐 PII leak + nickname collision)

- Both defects were exactly where you said. Fixed in `verifyAndLink`, with the two security decisions extracted
  into a new pure `lib/line-pairing.ts` so each rule is stated once and unit-tested.
  - **PII leak:** **retired the `verify_parent_students` `{names}` key (TH+EN)** and added
    `verify_parent_children_count` ("พบนักเรียน {n} คนในบัญชีนี้" / "{n} children on file");
    `parentChildrenNote(count, lang)` returns the count line, or `""` when 0. The link reply no longer touches
    names — a stranger who guesses a phone learns only a number.
  - **Collision:** `rows.find(...)` → `rows.filter(...)` + `decideTeacherMatch(count)` → **0** unchanged ·
    **1** binds exactly as before · **2+** binds **nobody** and returns the new `verify_teacher_ambiguous`
    (TH+EN, "staff must complete the pairing"). Session stays at `AWAIT_CODE` via the existing retry path, so
    they can retype or `สมัคร` again — not a loop.
- **Untouched deliberately:** parent self-registration on an unknown phone (per REQ-020 #2 / REQ-019 #5),
  existing links, TASK-046's move-the-link rule, the admin list, and everything Stage-2.
- **⚠️ Heads-up for your grep:** one `kids.map(k => k.name)` remains — it's `doChildren`, the post-link **"my
  children"** feature where the parent is resolved from **their own** `lineUserId`. Authenticated self-access,
  not a pre-link reply, so I left it. The link path has no name interpolation at all now.
- **Verify:** `bunx tsc --noEmit` clean; `bun test` **152/0** (+ `line-pairing.test.ts`: 0/1/many decision,
  TH+EN ambiguous reply, count-not-names, and — as retirement proof — `t("verify_parent_students")` now returns
  the raw key). ✅ Grep-confirmed no live use of the retired key and no name interpolation in link replies.
  DB/LINE = OA smoke; 4 smoke steps in the task notes.
- **@Sober — TASK-047 → REVIEW.** Next: **TASK-044** (`.ics` feed). **Nothing else waiting on Jason.**

## [13:40] Jason (BE) — TASK-044 built → REVIEW → @Sober (teacher `.ics` subscription feed)

- Built the per-teacher calendar feed. Read-only; **no Google OAuth** (stays its own REQ, per your Questions).
  - **Token:** `teachers.calendar_token` + migration **`0013_teacher_calendar_token.sql`** — `ADD COLUMN IF NOT
    EXISTS` + a **partial** unique index (`WHERE calendar_token IS NOT NULL`, so the NULLs don't collide),
    **hand-authored + journal-registered idx 13**, `db:generate` **not** run (per `drizzle/README.md`).
    Re-ran the audit: **14 entries = 14 `.sql`**, contiguous, no orphans. **Not applied** — human runs `db:migrate`.
  - **Feed:** `GET /api/calendar/:token.ics`, mounted **before** `authMiddleware` like `publicCheckin`; unknown
    **or rotated** → **404** (indistinguishable); `text/calendar` + `private, no-store`; window −30d/+90d; teacher
    resolved **from the token only**, never from the URL.
  - **Serializer (`lib/ics.ts`, pure):** stable **`UID: booking-<id>@…`** (edit updates, never duplicates),
    `SEQUENCE`/`LAST-MODIFIED` from `updatedAt`, **UTC stamps** (Thailand is UTC+7 no-DST → exact, no `VTIMEZONE`
    trap, no floating times), **`CANCELLED` included with `STATUS:CANCELLED`** so phones remove it, RFC 5545
    escaping, and **75-octet folding counted in UTF-8 bytes** — the detail that matters here, since Thai is
    3 bytes/char.
  - **Staff:** `POST /api/teachers/:id/calendar-link` (+`?rotate=true` → old link 404s).
  - **LINE:** keyword `ปฏิทิน`/`calendar` **and** a "ปฏิทินของฉัน / My calendar" quick reply on TASK-043's
    schedule reply → the `webcal://` link + a don't-share warning; TH+EN keys; `calendar` added to
    `KNOWN_POSTBACK_ACTIONS`.
  - **Config:** added `PUBLIC_CALENDAR_BASE_URL` to `.env.example` (falls back to the check-in base URL).
- **Verify:** `bunx tsc --noEmit` clean; `bun test` **173/0** (+21: `ics.test.ts` 14, `calendar-link.test.ts`,
  and a **route-level** test proving valid→200 `text/calendar` / unknown-or-rotated→**404** / non-`.ics`→404).
  ✅ Also pushed a **real generated feed** through a structural check: all lines ≤75 octets, CRLF-only,
  BEGIN/END balanced, `; , \` escaped, cancelled event retained — with a long Thai name and a `;`/`,` subject.
- ⚠️ **Deploy needs 2 things, not 1:** `bun run db:migrate` **and** `PUBLIC_CALENDAR_BASE_URL` — without the env
  var the link is relative and useless on a phone. Both in the task notes with the phone-smoke steps.
- **@Sober — TASK-044 → REVIEW.** **Nothing else waiting on Jason** (TASK-047 also in REVIEW).

## [00:41] Sober (SA Lead) — reviewed TASK-047 + TASK-044 → both DONE; REQ-017 SPEC_DONE, REQ-020 Stage 1 done

Two in REVIEW at once. Took the **security** one first.

**✅ TASK-047 (REQ-020 Stage 1) → DONE — the PII leak is closed.**
- `verify_parent_students` (the `{names}` string) is **retired**; the only traces are a comment and a test
  asserting `t("verify_parent_students")` now returns the **raw key** — a neat retirement proof. The link reply
  is a **count** (empty at 0). A stranger who guesses a phone now learns a number, not a family's children.
- **The one surviving `kids.map(k => k.name)` is correct to keep, and I checked rather than trusting the note:**
  it's inside `doChildren`, which resolves the parent from **their own** `findParentByLineUserId(lineUserId)` —
  an authenticated parent viewing their own data, not a pre-link reply. Flagging it for the reviewer was good practice.
- **Collision:** `rows.find` → `rows.filter` + `decideTeacherMatch`; I read the ordering and the `ambiguous`
  branch **returns before any `db.update`** ⇒ binds nobody. 0/1 paths unchanged; session stays retryable.
- Self-registration deliberately untouched, per the correction I routed yesterday.

**✅ TASK-044 (REQ-017) → DONE — and it passed the first real test of the TASK-042 migration rule.**
- `0013_teacher_calendar_token.sql` hand-authored, `IF NOT EXISTS` ×2, **registered as idx 13**; I re-ran the
  audit — **14 entries = 14 `.sql`**, contiguous, no orphans; `db:generate` not run; not applied here.
- Auth boundary verified in `index.ts`: `publicCalendar` mounts at `:43`, **before** `authMiddleware` at `:50`.
  404 on unknown *or rotated* token, `no-store`, token never logged, teacher id never read from the URL.
- The two things I called out in SPEC-014 are both there: **stable `UID`** (edits update, don't duplicate) and
  **`STATUS:CANCELLED` emitted** so a cancelled class actually disappears from the phone.
- **Two of his judgement calls I'd otherwise have had to ask for, both right:** **UTC (`…Z`) instead of
  `VTIMEZONE`** (Thailand is UTC+7, no DST — exact, and avoids the classic broken-feed cause), and **folding
  counted in UTF-8 octets** — with Thai at 3 bytes/char, char-based folding would emit over-long lines that
  stricter clients silently reject. That's a "works on my phone, not on theirs" bug avoided before it existed.
- **Verified myself (both tasks): tsc 0; full `bun test` 173/0** (up from 152).

**Deploy notes I made explicit on the board** — REQ-017 needs **three** steps, not one: `db:migrate` (0013) **+
set `PUBLIC_CALENDAR_BASE_URL`** (without it the link is relative and useless on a phone) **+ restart :4006**.
TASK-047 just needs the same `sid` deploy. Also reminded Porter to set expectations that **Google refreshes
subscribed feeds slowly (hours)** vs iPhone ~15 min.

**Open on Porter:** the REQ-017/047 deploy + acceptance, and **Q3 (dual-role) which still blocks REQ-020 Stage 2**.
**My spec queue:** REQ-019 (people screens — also unblocks REQ-013 and hosts Stage 2), then REQ-020 Stage 2,
then REQ-009 / REQ-013 / REQ-014.

## [morning] Porter (PM) — relaying ONE batched `sid` deploy (3 items) + asking the dual-role question that blocks REQ-020 Stage 2

- Three things are now waiting on a deploy, all in `smart-scheduler-back` on `sid`. The stakeholder has asked
  before to **test in one round** ("เทสทีเดียวได้"), so I'm relaying them as **one batch** rather than three:
  - **TASK-046** — role-change fix (already-linked user can finish `สมัคร`); post-delivery fix to REQ-015.
  - **TASK-047** — 🔐 REQ-020 **Stage 1**: the parent-link reply no longer leaks **children's names** (returns a
    count), and an ambiguous teacher nickname now **binds nobody** instead of silently taking the first match.
  - **TASK-044** — REQ-017 teacher `.ics` phone-calendar feed.
- **⚠️ REQ-017 needs THREE deploy steps, not one** (Sober was explicit, and I'm passing it on verbatim because
  the middle one is easy to miss): `bun run db:migrate` (applies 0013) **+ set `PUBLIC_CALENDAR_BASE_URL`**
  — without it the subscription link is relative and useless on a phone — **+ restart :4006**.
- **Expectation I'll set with the stakeholder up front** (Sober's reminder, and it would otherwise look like a
  bug): a subscribed calendar feed **does not update instantly** — iPhone refreshes roughly every 15 minutes,
  **Google can take hours**. Do not judge the feature by an immediate re-check.
- **Acceptance I'll verify from her results:** TASK-046 → an already-linked parent types `สมัคร` → `2` → the bot
  **asks for the teacher nickname**; TASK-047 → linking by phone replies with a **count, not names**, and a
  duplicated teacher nickname links **nobody** (asks to disambiguate); REQ-017 → a teacher gets a subscription
  link, adds it to their phone calendar, and their classes appear (allow for the refresh delay above).
- **Also routed to the stakeholder: REQ-020 Q3 (dual-role) — this is the one that blocks Stage 2.** Is there
  anyone at the school who is **both a teacher and a parent**? (a) no → keep one-role-only, simplest; (b) yes →
  they switch on demand; (c) yes → both surfaces at once. I'm not guessing this one; it decides the design.
- Ball: stakeholder (one batched deploy + acceptance; the dual-role answer) · @Sober (REQ-019 spec in flight).

## [morning-2] Porter (PM) — dual-role ANSWERED: nobody is both → REQ-020 Stage 2 UNBLOCKED → @Sober

- **คุณฟีน: "ไม่มี" — option (ก).** Nobody at the school is **both a teacher and a parent**. Answer written into
  **REQ-020 `## Questions` Q3**.
- **⇒ @Sober — Stage 2 is unblocked, and the answer *removes* work rather than adding it:** keep the current
  **one LINE account = one role** rule (TASK-046's move-the-link behaviour). **Do not build role switching and do
  not build a both-surfaces mode.** If the school ever hires a parent as a teacher, that comes back as a new REQ
  — we don't pre-build for it.
- REQ-020's remaining questions (what a pending user sees, notifying staff of new requests, notifying someone on
  unlink) are **all mine to answer with my leanings already attached** — they don't block your design, so pull
  them from the REQ when you spec Stage 2 and push back if any needs a real decision from คุณฟีน.
- Ball: @Sober (REQ-019 spec, then REQ-020 Stage 2 — now fully answered) · stakeholder (the batched `sid` deploy
  + acceptance from my previous entry). Nothing else on Porter.

## [morning-3] Porter (PM) — batch deployed: TASK-046 VERIFIED; REQ-017 blocked on real-device UX → parked → @Sober

- Stakeholder ran the migration + deploy, then tested. Recording what is **actually verified** vs not, since
  only part of the batch got exercised.
- **✅ TASK-046 — VERIFIED in the live chat.** She was already linked, typed `สมัคร` → `2`, and the bot **asked
  for the teacher nickname** (previously it bounced to the parent menu), then linked her as a teacher. That is
  precisely the repro from [2026-07-30 afternoon-8], now passing. The link **moved** rather than duplicating.
- **⏳ TASK-047 — not yet exercised.** Verifying it needs a parent-by-phone link (reply must show a **count**,
  not children's names). Not done; carried forward.
- **⏸️ REQ-017 — deployed and the link is issued, but acceptance is INCOMPLETE and the stakeholder has parked it**
  ("ช่างมันไปก่อน"). Board updated to **not delivered**. Two real-device problems, neither in the feed logic:
  1. **LINE does not linkify `webcal://`.** It links only from `develyst.online…`, dropping both the scheme and
     the **`som.` subdomain** → tapping goes nowhere. (Screenshot evidence.)
  2. **The Google Calendar mobile app cannot add a calendar by URL at all** — that's desktop-web only. So an
     Android/Google teacher cannot subscribe from their phone, which is most of the intended users.
  **Whether events actually render was never confirmed** — we never got past the subscribe step. I am not
  recording this as "works".
- **@Sober — feedback for whenever REQ-017 is picked back up (not urgent, stakeholder parked it):** the fix is
  probably not in the `.ics` at all. `calendarUrls()` **already returns both `https` and `webcal`** and
  `doTeacherCalendar` sends only `webcal`. Options worth weighing — send **both** with per-platform guidance;
  or a **small landing page** the LINE link opens, with an "add to calendar" button plus iPhone/Android
  instructions (my lean, because it's the only route that works for Google users). Your call on design.
  **Lesson worth keeping:** this passed tsc, 173 tests and your review — and still failed on the first real
  phone. Delivery for anything LINE-facing should assume a real-device check.
- Stakeholder is moving on to other work. Ball: @Sober (REQ-019 spec → REQ-020 Stage 2 → REQ-009/013/014);
  carried forward for Porter: TASK-047 verification, and REQ-017 when it's un-parked.

## [01:20] Sober (SA Lead) — took the REQ-017 lesson; spec'd REQ-019 → SPEC-016, 2 Qs routed → @Porter

**First, the REQ-017 miss — I'm taking that one.** It passed tsc, 173 tests and *my* review, then failed on the
first real phone (LINE opens `webcal://` in its in-app browser; the Google Calendar **mobile** app can't add a
calendar by URL at all). My reviews verify that the code does what the spec says — they cannot verify that the
spec's assumption about a third-party surface is true. I accepted "OA/device smoke = deploy-time" on a feature
whose **entire user journey is on a device**, which meant nobody checked the one step that mattered.
**Standard I'm adopting: for anything whose success depends on an external surface (LINE, phone calendars,
push), a real-surface check is part of the DoD — not a post-deploy afterthought — and I won't call it DONE, only
DONE-pending-device-check.** I wrote that into SPEC-016's non-functional section so it isn't just a note here.
- On the fix itself (for when REQ-017 is un-parked, not now): agree it isn't in the `.ics`. `calendarUrls()`
  already returns **both** `https` and `webcal` while `doTeacherCalendar` sends only `webcal`. Porter's lean —
  a **small landing page** the LINE link opens, with an add-to-calendar button plus per-platform instructions —
  is also mine: it's the only route that works for Google users, and it fixes the in-app-browser hand-off too.
  I'll spec it properly when it's picked back up.

**REQ-019 spec'd — SPEC-016 (design proposal; no tasks cut yet).**
- **Verified as-built:** the backend has **only** `GET /students?q=` and `POST /students` — **no parent endpoint
  exists at all**; the FE nav has no people screen; the four demographic fields and any suspend concept are
  absent. `parent.service.ts` does have the building blocks to compose (`listStudentsOfParent`,
  `createStudentForParent`, `searchStudents`, …).
- **Design:** one additive migration (hand-authored + journal-registered, **no `db:generate`** — the TASK-042
  rule), `GET/POST/PATCH /parents(/:id)`, `/parents/:id/students`, `PATCH /students/:id`, suspend/unsuspend, and
  a `/scheduler/people` screen (parents with students underneath, search, modals) reusing the Teachers-screen
  patterns. **Suspension enforced server-side**, never merely hidden in the UI. Nothing is ever deleted.
- **I deliberately did NOT stage this one** — and I want the contrast on record, because I *did* stage REQ-020
  the day before. There, Stage 1 closed a live leak with no UI, so splitting bought real safety. Here the
  obvious split buys nothing: REQ-020 Stage 2 is blocked on **its own** dual-role question regardless, so
  shipping screens early unblocks no one, while it would mean **building the same parent/student forms twice**.
- **Two load-bearing questions to @Porter (with my recommendations attached):** **Q1** what "suspend" actually
  blocks — I back his (a)+(b): LINE bot **and** new bookings, history untouched — because it dictates what the
  booking API and the bot must enforce; **Q2** whose demographics — I back **the student** — because it decides
  which table the migration touches and that's expensive to redo. **Q3 (optional fields) and Q4 (province
  dropdown + Thai/foreign flag) I am proceeding on** rather than manufacturing round-trips: mandatory fields
  would break LINE self-registration, which is already a confirmed constraint, and Q4 is cheap to change.
- Tasks get cut the moment Q1/Q2 land; the design is otherwise complete and ready.

**Nothing waiting on Sober** until those answers arrive. Behind REQ-019 in my queue: REQ-020 Stage 2 (needs the
dual-role answer + these screens), then REQ-009 / REQ-013 / REQ-014.

## [afternoon] Porter (PM) — answered SPEC-016 Q1/Q2, and ⚠️ raised a "จังหวัด" collision BEFORE tasks are cut → @Sober

- Answering both, and **naming them as my calls, not คุณฟีน's** — they're operational details, reversible, and
  you and I already agree, so blocking the build on a round-trip would be over-cautious. I've told the
  stakeholder what I decided so she can override.
- **Q1 — what "suspend" blocks: (a)+(b), confirmed.** Suspending a parent stops **their LINE bot access** and
  **new bookings for their students**; **history, bookings and students are untouched and still visible**, and it
  is reversible. Rationale from her own words — she asked only for "ดูและระงับได้" against junk/bad self-registered
  accounts, so *stop the account acting* is the intent; hiding or deleting would destroy the visibility she asked
  for. Agreed with your point that it must be **enforced server-side**, not just hidden in the UI.
- **Q2 — whose demographics: I'm refining your answer rather than just endorsing it.** Split them:
  - **gender · date of birth · nationality → the STUDENT.** These describe the person who trains; the dashboard's
    "% by gender / age" is about attendees.
  - **province → the PARENT (the household).** The 2026-07-25 meeting asked for "จังหวัดของลูกค้า (ไม่ใช่จังหวัดของ
    สาขา)" — that's an address, and addresses belong to the family. Putting it on the student means a family with
    two children stores it twice and the two copies can drift. The dashboard still reports students-by-province
    via `student → parent` (every student has a `parentId`, so the join is safe).
  - If that split costs more than it's worth technically, say so and I'll take all-four-on-student — but I'd
    rather you reject it knowingly than have me hand you a model that duplicates an address.
- **⚠️ NEW — a collision I found while reviewing the booking modal with the stakeholder, and it lands exactly on
  Q2, so please account for it before cutting tasks:** the frontoffice **already has a badge type literally named
  `จังหวัด`**, selectable **per booking** in the New-booking modal (alongside `สาขา`). Badge names are user data,
  which is why it's Thai amid the English chrome — not a bug. But once REQ-019 adds a **province on the person**,
  there will be **two "จังหวัด" with different meanings**, and staff will not know which to fill.
  - **What I'm asking of you:** design so REQ-013's demographics read **only** the person-level field, and make it
    unambiguous which one is which (naming/labelling, or a note that the badge is an ops tag). Don't silently let
    the dashboard pick one.
  - **What I still owe you:** I've asked คุณฟีน what the existing `จังหวัด` badge is *for* (branch province?
    customer province? leftover test data?). **Not answered yet** — if it turns out to be a duplicate of the
    demographic, retiring it is a separate small REQ, not something to fold in here.
- **Q3/Q4:** agreed with you proceeding — optional fields (mandatory would break LINE self-registration, a
  confirmed constraint) and the province dropdown + Thai/foreign flag.
- Also noted and appreciated: your new **"DONE-pending-device-check"** standard for anything riding on an
  external surface. That's the right lesson from REQ-017, and it's on you to hold — I'll stop treating a green
  review as ready-to-accept for LINE/phone features.
- Ball: @Sober (cut REQ-019 tasks — Q1/Q2 answered) · Porter (get the `จังหวัด`-badge answer from คุณฟีน).

## [afternoon-2] Porter (PM) — `จังหวัด`-badge answered: NOT a duplicate, my flag was a false alarm → @Sober

- **คุณฟีน's answer: the `สาขา` / `จังหวัด` badges are sample data she created herself** to prove the concept.
  Badges are meant as a **general-purpose tagging mechanism for future needs** — separating branches later, or
  tagging things like **onsite vs online** attendance, or any category she wants to count by.
- **⇒ My collision warning was a false alarm and I'm withdrawing it.** The badge `จังหวัด` is **not** a rival
  source of the demographic province; it's one example tag among many she may rename or drop. **REQ-019 /
  REQ-013 proceed unchanged** — person-level province is the demographic, badges are ops tags. Sorry for the
  detour; better a withdrawn flag than a silent one, but I should have asked what the badge was *for* before
  raising it as a conflict.
- **Still worth one line of care in the design (not a blocker):** two things visible to staff will be called
  "จังหวัด" — a booking badge and a person field. Worth labelling so nobody wonders which to fill; no structural
  change needed.
- **New work incoming from this conversation:** the stakeholder asked for a **weakness review of the badge
  system** — she already spotted that **badge types/values can be created but never removed**, and would accept
  an "inactive" instead of a hard delete. She asked what else is weak ("และเรื่องอื่น ๆ ของมัน"). I'm running a
  read-only audit now (lifecycle gaps, what happens to history, what the badge report can and can't do,
  and whether the frontoffice badges overlap the backoffice `bo` tag system). **A REQ will follow** — not
  written yet, because I want the findings first rather than guessing the scope.
- Ball: @Sober (REQ-019 tasks, unblocked and unaffected) · Porter (badge audit → then a REQ).

## [afternoon-3] Porter (PM) — badge audit done → REQ-021 written and PARKED at lowest priority (not for the queue)

- Ran the badge weakness review the stakeholder asked for. Presented it, and **she parked it: "priority หลังสุด
  ลูกค้าไม่รีบใช้"**. Captured as **REQ-021, status BACKLOG / LOW** — written up **only so the analysis isn't
  lost**, explicitly marked *do not start*. **@Sober: this is NOT in your queue** — REQ-019 → REQ-020 Stage 2 →
  REQ-009/013/014 is unchanged.
- **Her own observation was right, with a twist worth recording:** there is genuinely **no delete** anywhere, but
  **deactivate already exists** for both types and values — and it is **defective**: deactivating a value makes
  every booking using it **un-editable** (the modal drops it from the picker but still submits its id → 400).
  So the only removal path we have breaks the thing it's used on.
- **Cheapest finding:** rename / recolor / reorder are **already implemented in the API and validation** and have
  **no UI at all** — a typo'd badge name is permanent today. Pure FE work, backend already done.
- **The finding I'd rank first if the ranking were mine (it isn't):** the badge report **inner-joins the tag
  link**, so **untagged bookings vanish from the totals**. Badges are optional and never required, so the
  untagged pile will be large and invisible — the customer would read "onsite 40 / online 25" as complete. Wrong
  numbers presented confidently are worse than no numbers.
- **Two structural findings that matter beyond badges, flagged for whoever touches REQ-014:**
  1. **There are two independent tagging systems** — frontoffice badges (on bookings) and the backoffice `bo`
     tag groups (on items, from REQ-006). Same concept, built twice, sharing nothing; a tag made in one is
     invisible in the other. This is exactly the single flexible mechanism the customer described.
  2. **Badges can never carry money.** The frontoffice has no price field; revenue lives in the backoffice
     movement ledger which has no tag link ⇒ **"revenue by branch / onsite vs online" is structurally
     impossible**, which is precisely what "อยากดูยอด" implies. **Do not design REQ-014 as if badges could
     answer it** — and if both are ever built, design them together.
- Suggested phasing is written into the REQ (small UI fixes → reporting integrity → the architectural decision),
  so nobody has to re-derive it later.
- Ball: unchanged — @Sober (REQ-019). Nothing on Porter.

## [22:40] Sober (SA Lead) — Q1/Q2 answered → cut TASK-048 (BE) + TASK-049 (FE); one condition on Porter's Q2 refinement

Porter answered both questions and **refined** Q2 rather than rubber-stamping my answer. I checked his
refinement against the schema before accepting — he explicitly invited a knowing rejection, so it deserved a
real check rather than a thank-you.

- **Q1 (a)+(b) accepted as-is:** suspend stops LINE bot access **and** new bookings for their students; history,
  bookings and students stay intact and visible; reversible; **enforced server-side** (in the task's DoD, not
  just the UI).
- **Q2 — his refinement is right, and I'm accepting it *with one condition*.** Splitting gender/DOB/nationality
  onto the **student** and **province onto the parent** is the better model: an address belongs to the
  household, and putting it on the student would duplicate it across siblings and let the copies drift.
- **⚠️ The condition — the gap his split creates, which I found in the schema:** `students.parentId` is
  **nullable by design**. The schema comment says it outright: *"Nullable for walk-in/trial students created
  before a parent is known; the LINE flow always sets it."* So a **First-Trial / walk-in student can have no
  parent at all** — and that is precisely the acquisition cohort the SOM dashboard is meant to measure. With
  province on the parent, a naive `student → parent` join **silently drops them**.
  ⇒ I did **not** move the column back (that would reintroduce the duplication). Instead I wrote the rule into
  SPEC-016 **and** into TASK-048: **province-by-student must LEFT-join with an explicit "ไม่ระบุ / unknown"
  bucket.** If that bucket turns out large, the business fix is *capturing a parent at trial time* (its own REQ),
  not relocating the field.
  - Worth naming: this is **the same failure mode Porter just found in the badge report** (untagged bookings
    vanishing from the totals). Same class of bug, two features apart — so I've stated it in the spec where
    REQ-013 will inherit it, rather than leaving it to be rediscovered.
- **`จังหวัด` collision handled as he asked:** SPEC-016 now states that **REQ-013 reads `parents.province` only,
  never the badge**, and TASK-049 requires the field to be labelled unmistakably ("จังหวัดของผู้ปกครอง (ที่อยู่)")
  with the badge left untouched. Whether that badge is a duplicate to retire stays **his open question with
  คุณฟีน** and would be a separate small REQ — not folded in.
- **Tasks cut:** **TASK-048 (@Jason, BE)** — migration (idx 14, hand-authored + journal-registered, no
  `db:generate`, not applied), `GET/POST/PATCH /parents(/:id)`, `/parents/:id/students`, `PATCH /students/:id`,
  suspend/unsuspend **enforced server-side** (bot + booking creation). **TASK-049 (@Fern, FE)** — the
  `/scheduler/people` screen (dep 048's contract).
- **New standard applied for the first time:** TASK-049's DoD requires Fern to **actually open the screen in a
  browser** and exercise list → search → create → edit → suspend → un-suspend, and say what she clicked. A green
  `tsc`/`build` is not evidence the screen works — that's the REQ-017 lesson, now enforced in a task rather than
  living as a note.
- **Nothing waiting on Sober** until 048/049 reach REVIEW. Behind them: REQ-020 Stage 2 (still needs the
  dual-role answer), then REQ-009 / REQ-013 / REQ-014.
