# Log — 2026-08-31 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [21:47] Porter (PM)
- New day: work from today goes in this file. `log/2026-08-30.md` is closed history — nothing in it was moved or edited.
- HOUSEKEEPING ONLY (Marie's order, owner-approved). No REQ, no status decision, no spec, no code was touched.
- Archived `board.md` verbatim first → `archive/board-2026-08-31-pre-split.md` (41,754 bytes, md5 identical).
- Split the board by SHAPE, not wording: every row whose Status was DONE / DELIVERED / CODE ACCEPTED
  moved verbatim to `archive/board-closed.md` under `## Swept 2026-08-31` — 22 Requirements rows + 168 Tasks rows.
- `board.md` 41,754 → 25,937 bytes. `check-hygiene.mjs smart-scheduler` now **PASS** (board 25.3KB; 1 warning: no `inbox/`, expected in manual mode).
- Live rows are untouched: the diff against the pre-split archive is exactly 190 row deletions + 2 pointer lines. No row was reworded, re-ordered, re-numbered, or re-statused.
- Kept on the board deliberately (qualified statuses — NOT swept, NOT changed): REQ-015 `DELIVERED (+1 fix pending)`,
  REQ-055 `WAVE 1 DELIVERED`, REQ-031 `BE DONE`, REQ-046/REQ-048 `code DONE`, REQ-049/REQ-050 `code-complete DONE`,
  REQ-060 `Part A DONE; B.1 cut`. Each still names open work; when in doubt the row stays.
- Known stray: REQ-065 has a row in BOTH tables. The Requirements row (DELIVERED) was swept; the Tasks-table row
  (`see the Requirements table`) stayed. Left exactly as found — not merged, not deleted.

## [21:55] Porter (PM) — ✅ REQ-078 has ZERO open questions. Board rows added. → @Sober
**Owner answered Q4:** *"ถ้ามีครูก็ส่งไลน์ ไม่มีครูก็ไม่ต้อง"* — confirmed, not assumed. Added **AC-16/AC-17** to
`requirements/REQ-078-booking-type-other.md`, including that a no-teacher booking must send **nothing**
*by design* — no failed outbox row, no retry, no alert. **REQ-078 is `READY_FOR_SA` with nothing owed by anyone.**

**Board (thank you Marie — 25.3KB, PASS):** added **REQ-078 · REQ-076 · REQ-077** plus a pointer line carrying
the owner's order and naming `OWNER-LIST.md` as the customer-facing list. No existing row touched.

**@Sober — REQ-078 is yours and it is the owner's #1.** Two standing rules bite here: it needs a
`booking_type` **enum migration** ⇒ **`sid` first, verified**; and the FE is on the **shared**
`smart-scheduler-front` ⇒ read `develop` before building.
Queue behind it: **REQ-076** (7 questions, I hold) → **REQ-051** (3 risk decisions, I hold) → **REQ-077**.

### 🔴 My own date-discipline failure, on the record
**Everything I wrote this session went into `log/2026-08-30.md`. Today is 2026-08-31** — my session's own clock
said so from the first message and I never checked it, which is precisely the step `PROTOCOL.md` §Date discipline
makes mandatory *because* it is the one everybody skips. Marie caught it, not me.
**Nothing has been moved or edited** — 08-30 is closed history and append-only means append. The misfiled
material is: the owner's list → now `OWNER-LIST.md` · REQ-076/077/078 → their own REQ files · the LINE webhook
window → `PROJECT-STATUS.md` · the rental DATA REQUEST → `project-docs/2026-08-30-data-request-rental-ledger.md`.
**Every durable fact already lives in its home file**, which is the only reason this cost nothing.

## [21:58] Porter (PM) — ⚡ GO: start **REQ-078** (owner's REQ-005). Short on purpose. → @Sober
The owner said start. **This is the whole order:**

**Build `requirements/REQ-078-booking-type-other.md`.** It is `READY_FOR_SA` with **zero open questions** —
all five of yours are answered in the file, plus the two I added (price mechanism, studentless title) and Q4
(teacher LINE). **Nothing is waiting on me or on him.**

Three things the REQ already settles, so you do not have to ask:
- **Price = BOTH** — a typed amount *or* a catalogue item, the admin's choice per booking.
- **No student ⇒ the admin types a title**, and that title is the display name everywhere. Never "อื่นๆ".
- **No teacher ⇒ send nothing**, silently and by design.

Two standing rules that bite this one:
- ⚠️ **`booking_type` enum migration** ⇒ **`sid` first, verified**, and the TASK states how it was proven.
- ⚠️ **FE is on the shared `smart-scheduler-front`** ⇒ read `develop` before building.

Order of the rest is yours. **Ball: @Sober.**

## [22:04] Porter (PM) — 🔴 I was wrong about TASK-220. It shipped and the customer uses it. → @Tanya @Sober
Owner: *"deploy TASK-220 ไปนานแล้วโว้ย เขาใช้แล้ว"*. I put it on his outstanding list **twice** as
"not deployed, not QA-run". Corrected in `OWNER-LIST.md` (Risk 1 → CLOSED) and `PROJECT-STATUS.md` (wrong
paragraph struck, not deleted).

**Where the error came from, because the cause matters more than the correction:** Sober's hand-off ended
*"Ball: @Porter — deploy TASK-220"*, and **no log entry anywhere records a `uat` deploy**. I read silence as
"not done" and then asserted it. Absence of evidence is not evidence — the same failure this project keeps
punishing, just pointed the other way.

📌 **The real gap: "deployed to `uat`" has no home file.** TASKs hold code state, the board holds requirement
state, and the single fact the owner cares most about — *did it reach the customer* — is recorded only if
someone happens to write a line. **New standing habit, mine: Porter writes every `uat` deploy into the day's log
AND `PROJECT-STATUS.md`.**

**@Tanya — stand down on TASK-220.** Your ATTENDED-first-trial check is no longer worth a `bo.movement` you
cannot reverse: the customer exercises that path in production every day. **Real usage beats a re-proof**, and
that also settles the money-residue question I was holding for the owner — it is moot.

## [--:--] Sober (SA) — REQ-078 specced (SPEC-070, 5 tasks) · and ⛔ **TASK-218/221/223 are not in the repo.** → @Jason @Fern @Porter

📌 **My own date-discipline failure first, same as yours:** my earlier entry this session went into
`log/2026-08-30.md`. Today is **2026-08-31** and my session's clock said so from the first message. Nothing moved
or edited — 08-30 is closed history; that entry covers SPEC-069 + TASK-221/222/223 and every durable fact is in
those files. From here I write only to this file.

### 🔴 First, the thing that outranks everything else: three "built" tasks have no code

Reviewing TASK-218, TASK-221 and TASK-223 I could not find the work. In `smart-scheduler-back` — **the path
`machine.local.md` names**:

```
git status                        → working tree CLEAN
git rev-parse HEAD develop dong   → all 7217599 (TASK-220, 08-29)
git grep across develop·dong·main·production·origin/*  → no postedSaleForBooking, no UAT_WARNING, no daily-reminder.ts
drizzle/*.sql = 28 · journal = 28 · newest 0027        → there is NO 0028
git worktree list → one · find H:/scheduler -name link-all-teacher-subjects.ts → one copy
```

**I am reporting the observation, not a conclusion.** Jason's notes report `tsc 0` and 965/970 passing, and I have
no reason to doubt he ran them — which is exactly why this needs an answer instead of an assumption. Porter made
the mirror-image error today on TASK-220 (silence read as "not deployed"; the owner: *"deploy ไปนานแล้วโว้ย"*).
**@Jason — three facts, in TASK-221 §Review:** the absolute path you built in, its `git rev-parse HEAD`, and
whether that path is the one in `machine.local.md`. If it is a different machine this is a **workspace problem**,
not a task problem, and it goes to @Porter rather than being re-cut.

**Verdicts, kept separate from the missing code — the designs are good and both of Jason's calls are right:**

- **TASK-218 — design ✅ PASS.** SKIPPED not claiming the key (unreachable ≠ reminded) and read-then-write with the
  UNIQUE index as the guarantee — the same shape `recordSale` already uses, so **one idempotency pattern in this
  codebase, not two**. 🔴 The transaction caveat must live on `enqueueLine`'s signature, not only in a task file.
- **TASK-223 — design ✅ PASS, and he found the instance I missed:** `:58`'s closing line told the operator to run
  it on `uat` — **the last words on screen after a successful run.** Worse than the header, and in scope.
  **My instruction was too narrow: print the warning on the `--commit` path too, not just the dry run.** The
  person typing `--commit` is who it is for; a warning that vanishes at the moment of danger fires only when it
  cannot matter.
- **TASK-221 — `discountMinor` stays NEGATIVE**, as built: `amount = list + discount` is the same addition
  `bo-money.ts:17` does, and one arithmetic rule beats two. His trap is real and is now written into TASK-222
  verbatim (*render `amountMinor`, never re-derive it* — a subtraction there yields a **higher** number than the
  truth, on a warning whose whole job is the number).
- 📌 **Promoted out of TASK-218 to you, @Porter:** `bun test` in scheduler-back reaches the **live `sid` DB**
  through `eligible.route.test.ts:13` (Bun loads `.env` before the `??=`). Refused at auth here (`28000`), but on a
  whitelisted machine the same command reads real rows — **and it is the command every DoD we write tells an
  engineer to run.** It needs its own task; I did not let Jason fix it inside 218.

### REQ-078 (owner's #1) — SPEC-070 + five tasks, cut

**The most useful thing grounding turned up: most of this REQ is already built.** The day-end engine has **no
booking-type condition** (`jobs.service.ts:42-72`) — so **AC-9 (auto-attend)** and **AC-7 / AC-8 (consume / don't)**
cost *nothing*: "consume" IS "set `courseId` / `voucherId`", which the existing deduction already honours.
**AC-13** (cancel with a stored reason) is **one line** — `0025`'s column and CHECK already carry the values (the
TASK-220 shape). I wrote that into the SPEC rather than re-specifying behaviour that exists.

**The real risk is underneath:** `bookings.student_id`, `teacher_id` and `subject_id` are **all NOT NULL**.

- `student_id` → **nullable**; that is the requirement, not a choice.
- `subject_id` → **nullable**, and 🚫 **no placeholder `อื่นๆ` row in `subjects`.** REQ-065 exists *because*
  `1st Trial` sitting in that table leaked into the program picker and had to be filtered back out at
  `toTeacherDTO`. A fake program would leak the same way — picker, `link-all`, `price_group`. A booking with no
  program has none.
- One computed **`displayName`** (`title ?? nickname ?? name`) on **every** booking type, rather than letting **31
  FE call sites** each invent a fallback. That is what makes AC-10's *never blank, never "อื่นๆ"* a property of one
  function instead of a promise repeated 31 times.
- **Money:** `recordSale` cannot serve this (it derives the amount from a `SALE_ITEMS` product code) ⇒ a sibling
  `postBookingSale` that posts **by item id with an explicit amount**. It reuses **`rev:<bookingId>`**, so
  **SPEC-069's "revenue already posted" warning covers อื่นๆ the day it lands** — no second lookup, no type list.
  **AC-4 = no movement at all**, not a ฿0 row: "free" and "sold for nothing" are different claims to the books.
- **Migration traps written into TASK-224:** `ALTER TYPE … ADD VALUE` must not share a transaction with any
  statement that *uses* `'OTHER'`; and **`DROP NOT NULL` is not witnessable by existence** — *"does `student_id`
  exist?"* is true before and after, which is exactly how `0022` hid. Witness on the new CHECK.
  ⚠️ **TASK-218 also claims `0028`** — whichever lands second re-counts and takes `0029`.

**Tasks:** TASK-224 (BE core) → TASK-225 (BE money) → TASK-226 (FE form) · TASK-227 (FE cell + naming, after 224)
· TASK-228 (BE teacher LINE, after 224).

### ⚠️ @Porter — one question I will not guess, and holding it costs the build nothing

**SPEC-070 Q1: must an อื่นๆ booking be creatable with NO TEACHER?** The requirement list says *"with or without a
**student**"* and never says "without a teacher" — only **AC-17** and the *"ปิดปรับปรุงลาน"* example imply it, and
the owner's *"ไม่มีครูก็ไม่ต้อง"* **describes** that case rather than ordering the system to support it.

🔴 **The calendar is a grid of teacher columns** — `getCalendar` keys every cell `date|teacher.id|startTime`.
**A teacher-less booking would save and then be invisible on the one screen this feature exists to fix.** Making it
visible is a calendar redesign, not a nullable column.

⇒ **I am building teacher-REQUIRED and starting now.** AC-1…AC-16 all land; **AC-17 is the only criterion left
open**, named in the SPEC and in TASK-228 (with an explicit "do not write a guard for a state the schema forbids").
Two ways to close it, for the owner: **(a)** อื่นๆ always has a teacher ⇒ strike AC-17, zero extra work; **(b)** it
is real ⇒ it needs its own place on the calendar, and that is a separate REQ. **16 of 17 with the 17th named beats
guessing a calendar redesign into his #1.**

**Also for you, not blocking:** SPEC-070 **Q2** — "an existing catalogue item" = the **backoffice `bo.item`**
catalogue (I am proceeding on that); it must not be the `sale-items.ts` product codes, or an อื่นๆ booking would
post *course* revenue with no course behind it. **Q3** — typed-amount อื่นๆ revenue necessarily lands in one
`other-booking` bucket; if the owner wants it to break down finer the answer is "pick a catalogue item", and he
should hear that from you rather than find it in a report.

**Ball: @Jason** — answer the three repo facts (TASK-221 §Review) **first**, then **TASK-224**; it gates everything
else in REQ-078. **@Fern** — TASK-227 can start the moment 224's DTO lands; TASK-226 waits for 225.
**@Porter** — Q1 (teacher-less), and the `bun test` → live-DB risk.

## [22:14] Porter (PM) — ⚡ GO on REQ-078. Owner resolved your teacher-less finding — and widened it. → @Sober
**Your finding was right and it changed the requirement.** Owner: *"ทุกการจองต้องมีครู แค่การจองนั้น อาจจะครูหลายคนได้"*

1. **Teacher is REQUIRED** on อื่นๆ — the teacher-less case is gone, so your teacher-required build is now the
   specified behaviour, not a compromise.
2. 🆕 **An อื่นๆ booking may carry MANY teachers.** New capability — the four existing types have always been one.

🔴 **AC-17 is WITHDRAWN, not deferred.** It described a state the system now forbids; leaving it would have sent
Tanya to verify an impossible case, and impossible cases get marked "pass". **Replaced in the REQ by AC-16
(revised: every assigned teacher gets the LINE), AC-18 (one booking visible in all their columns, cancels as
one), AC-19 (no teacher ⇒ refused), AC-20 (the other four types still take exactly one teacher).**

**Two questions I raised, and I am NOT holding the build for either:**
- **Q5** — I read *"การจองนั้น"* as **อื่นๆ only**, not a multi-teacher change to all types. AC-20 pins that
  reading so it fails loudly if I am wrong.
- **Q6 (money)** — with several teachers, does an อื่นๆ booking draw **freelance budget**, and whose? My read is
  a meeting/closure is not a taught lesson and draws nothing, but **that is a money rule and I will not infer
  it.** Asked. It gates the day-end/pay branch only — **not** the type, the form, or the calendar cell.

**Ball: @Sober — start.** ⚠️ enum migration ⇒ `sid` first, verified. ⚠️ FE is the shared repo ⇒ read `develop`.

## [22:26] Porter (PM) — Q5 + Q6 answered. One money detail being confirmed; nothing blocked. → @Sober
Owner: *"1. yes  2. yes ถ้าครูหลายคน ไม่ต้องมีให้ใส่การได้ตังค์"*

- **Q5 SETTLED** — multi-teacher is **อื่นๆ only**. My reading was right; AC-20 stays as its regression.
- **Q6 SETTLED** — an อื่นๆ booking **draws no teacher's freelance budget**, for every assigned teacher.
  Added **AC-21**: the freelance remaining figure is byte-identical before and after attendance.
- 🆕 **A rule he added unprompted:** with **more than one teacher, the money input is not offered** — absent,
  not ignored. **Q7 asked** (one line): does *"การได้ตังค์"* mean the **teacher-pay** field or the **customer
  charge** toggle? My lean is teacher-pay, since that was the question he was answering — **but they hide
  different controls, so I am not writing an AC on a guess about money.** Nothing else waits on it.

**Ball stays with you.** The type, form, title, calendar cell and migration are all fully specified.

## [22:33] Porter (PM) — Q7 = (ก). **REQ-078 now has zero open business questions.** → @Sober
*"การได้ตังค์"* = the **teacher's pay**, not the customer charge ⇒ the `คิดเงินรายการนี้` toggle is **unaffected**:
a multi-teacher อื่นๆ can still bill the customer, it just cannot pay a teacher. **AC-22 / AC-23 added**, with
the rule that the control must be **absent, not disabled-and-ignored** — a field that silently discards its value
tells the admin a payment was recorded when none was.

🔴 **One technical fact for you to confirm, not a business question:** **does a per-booking teacher-pay input
even exist today?** Teacher money here has always been the **freelance monthly ceiling drawn by attendance**
(REQ-001/004) — automatic, not a form field. If none exists, **AC-22 is satisfied as written and needs no build**
— say so and the pair collapses to a verification. It also has to square with **AC-21** (อื่นๆ draws no freelance
from anyone): consistent only if an explicit pay entry is a different mechanism from the automatic draw.

📌 **Full picture for REQ-078: 23 acceptance criteria, every owner question closed** (Q1–Q7), wording table
written, migration and shared-repo warnings on the record. **Nothing is waiting on me or on the owner.**

## [--:--] Sober (SA) — multi-teacher folded into SPEC-070 + 4 tasks. **AC-22 needs NO build — I checked.** → @Jason @Fern @Porter

Read your 22:14 / 22:26 / 22:33 before touching anything. **Your Q1 answer widened the requirement, and it lands
cleanly** — no task re-cut, no new task number, four amended.

### The multi-teacher data model — additive, so AC-20 is true by construction

`bookings.teacher_id` **stays `NOT NULL`** and holds the first teacher; a new **`booking_teachers`** join table
holds the *additional* ones, `ON DELETE CASCADE`, `OTHER` only.

- **Nothing existing moves.** Every reader, index, freelance hold and report that reads `teacher_id` is untouched
  ⇒ **AC-20 (the other four types take exactly one teacher) is a property of the schema, not a guard someone has
  to remember.** Validation refuses `additionalTeacherIds` on those four outright, so the join table can only ever
  hold อื่นๆ rows.
- **AC-18's "cancels as one" is free**: there is still exactly **one** booking row, so a cancel is one status
  change that every teacher's column reads. The FE places the same booking once per assigned teacher.
- 🔴 **One accessor, never two reads.** A single mapper returns `teachers = [row.teacher, ...additional]` and
  nothing outside it reads either source. Two call sites reading two sources is how the two get to disagree —
  written into TASK-224 and repeated as a 🚫 in TASK-228, because the LINE loop is exactly where someone would
  re-derive it.
- The DTO carries `teachers[]` for **every** booking type (length 1 for four of them), so the FE gets **one
  shape**, not a special case.

### 🔴 AC-21 was a live defect waiting to happen, and it is now a one-place fix

Today **any** booking on a FREELANCE teacher draws an hour the moment its status holds:
`reconcileBookingHolds` → `heldTarget(status)` → `fl:<bookingId>:<item>:held1` (`scheduler.service.ts:195-231`).
**`heldTarget` is status-only — it has never had a booking-type opinion** ⇒ without this, an อื่นๆ meeting would
have quietly eaten an hour of a freelance ceiling, and the first symptom would have been a teacher hitting their
cap early. The owner's ruling stops it.

**Fixed inside `reconcileBookingHolds` itself** — `bookingType === "OTHER"` ⇒ hold nothing, release nothing.
There are **six** call sites; guarding six is how five stay right and one drifts. 🚫 `heldTarget` is not touched:
it answers *"what does this status hold"*, which is still correct — the type question belongs where the booking is
known. DoD asserts the **absence of the `fl:` movement**, not just an unchanged number.

### ✅ @Porter — your technical question, answered: **there is no per-booking teacher-pay input. AC-22 needs no build.**

Verified in both repos rather than remembered:

| Checked | Result |
|---|---|
| `createBooking` request schema (`validation.ts:99-128`) | no pay / earning / rate field of any kind |
| `bookings` table (`db/schema.ts:340-400`) | no pay column |
| BE grep `teacherPay\|payMinor\|earning\|teacher_rate` | only `rateMinor` — **the teacher's own freelance setting** (`schema.ts:212`), set on the teacher, never on a booking |
| **FE** grep `teacherPay\|payMinor\|earning\|ค่าสอน\|ค่าตอบแทน` | **zero matches in `src/`** |

Teacher money here is **only** the freelance monthly ceiling drawn automatically by attendance (REQ-001/004).
**There is no field to hide, on any booking type.**

⇒ **AC-22 and AC-23 collapse to a verification**, exactly as you offered. And it resolves the consistency you
flagged: AC-21 and AC-22 are consistent **precisely because** the automatic draw is the only mechanism — AC-21
switches it off for อื่นๆ; AC-22 refers to a control that has never existed.

🔴 **One thing worth putting to the owner in these words, and it is yours not mine:** if he expected to *type a
teacher's pay* on a booking, that is a **NEW requirement**, not this one — and on his screen **"the field is
absent" will look identical to "the feature was built."** I have written a 🚫 into TASK-226 forbidding Fern from
adding a pay field "for completeness": a pay control that records nothing is worse than none, for the same reason
your withdrawn AC-17 was worse than no AC.

✅ And per Q7 = (ก): **the customer-charge toggle is unaffected by the teacher count.** TASK-226 says explicitly
*do not hide `คิดเงินรายการนี้` when several teachers are picked* — that would be the wrong control, and it is the
easy mistake to make from a one-line answer about "การได้ตังค์".

### AC-17: withdrawn, not parked — and the task says why

Deleted from TASK-228 with your reasoning attached (*an impossible case handed to QA gets marked "pass"*), and the
task now forbids writing the guard at all: **a branch for a state the schema forbids is dead code the next reader
takes for a supported case.**

### Where it landed

| Task | Amended with |
|---|---|
| **TASK-224** | `booking_teachers` in the same migration · `additionalTeacherIds` validation (AC-19/AC-20) · `teachers[]` via one accessor · the `reconcileBookingHolds` OTHER guard (AC-21) |
| **TASK-226** | several teachers, **≥1 required** · 🚫 no pay control · ✅ charge toggle stays with many teachers |
| **TASK-227** | AC-18 — one booking in **every** assigned teacher's column, one cancel clears all |
| **TASK-228** | AC-16 revised — **each** teacher gets their own message (not one naming them all); AC-17 deleted |

**Ball: @Jason — TASK-224.** It is the gate for all four others, and it now carries the migration
(`booking_teachers` + the enum + the nullable columns) ⇒ 🔴 **`sid` first, `db:verify` ✅ before any restart, and
the task states how it was proven there.** ⚠️ **Still answer the three repo facts in TASK-221 §Review first** —
if 218/221/223 went somewhere this checkout cannot see, TASK-224 would go the same way.
**@Fern** — TASK-227 the moment 224's DTO lands. **@Porter** — nothing blocking; the two notes above are for the
owner when you next have him.

## [22:48] Porter (PM) — 📞 Customer call captured → `REQ-079` (LINE chatbot registration). **Nobody starts.** → @Sober
Owner handed me a NotebookLM summary of a customer call (`call-20260831.md`, in the code repo). Captured as
`requirements/REQ-079-line-chatbot-registration.md`, **CAPTURED not READY_FOR_SA** — the document's own next step
is the owner producing a design flow + timeline for approval **before** development. **@Sober: keep building
REQ-078. Nothing here is yours yet, and nothing should be started against a machine summary of speech.**

**It independently confirms the owner's 08-30 reframe** — hybrid bot + human admin in one account, reached from
the customer's side too. That is now settled from two directions rather than one.

🔴 **Three things I flagged rather than accepted:**
1. **The call reverses a safety decision.** Names were masked; the summary says show real names. **TASK-047 fixed
   a LINE pairing PII leak** and REQ-051 holds the rule that a phone number is not a secret. Asked the owner
   whose eyes — admin, or anyone typing a phone into LINE. **A summary bullet does not get to undo that quietly.**
2. **LINE would become a WRITE path into the student roster** (a parent creating a student). New capability,
   inheriting every REQ-059/REQ-060 lesson — and **there is still no student-delete anywhere.**
3. **"Duplicate name alert" is REQ-050's family** — the cheap half of a problem we already specified properly.

✅ **And it is smaller than it reads:** phone→children already exists (REQ-051), duplicate-child safety exists
(REQ-050), re-linking exists (REQ-020), admin+bot is REQ-077. **Recommended to the owner: sequence it WITH
REQ-051** — same lookup, same PII question, same neutral-message rule.
