# 2026-08-24 — log

## [--:--] Porter (PM) — 🟢 The `uat` nightly task RAN. 🔴 And it took the 15 Sunday sessions, exactly as predicted.
First night of the newly installed tasks on `uat`. `job_runs`:

| job | run_date | noShow | coursesCut |
|---|---|---|---|
| `end-of-day` | 2026-08-23 | **15** | **15** |
| `end-of-day` | 2026-08-22 | 0 | 0 |

✅ **The mechanism is proven on `uat`** — fired at its trigger, wrote its audit row, no failure. That is what the
owner asked for and it works.

🔴 **And the consequence is the one I sized before it happened:** the 15 `CONFIRMED` `COURSE_PACKAGE` sessions from
**Sunday 08-23** — 15 named children, 10:00→17:00 unbroken, Surfskate/Inline/Bike/Skateboard — are now
**`NO_SHOW`**, and **15 sessions were deducted from their families' packages.** The owner was told, chose to
proceed, and said to leave it. **Recorded, not re-argued.** What follows is what to *do*, not whether it was right.

### It is reversible, and the product already does it
**TASK-105 / SPEC-028 §11.2:** cancelling a DELIVERED session (attended **or no-show**) is allowed with a non-empty
reason, and **returns the consumed unit** (`scheduler.service.ts:1935`, `returnsConsumedUnit`). ⇒ if those children
did attend, staff can correct each row and **the sessions come back to the packages.** 15 corrections, in-product,
no script, no engineering.

### 🔴 The part that is NOT a one-off — it repeats every weekend
**Mon–Fri are near-empty for this customer; Saturday and Sunday are their business.** The pattern that produced
this — staff press `ยืนยัน`, never press `มาเรียน` — is their normal working habit as far as the data shows
(532 `PENDING` future · every one of that Sunday's sessions `CONFIRMED`, none `ATTENDED`).
⇒ **Unless something changes, every Saturday and Sunday from now on will end with that day's confirmed sessions
marked absent and quota consumed.** Tonight is a Monday, so it is nearly harmless. **Saturday is the test.**
Three ways out, and the choice is the customer's, not ours:
1. **Staff mark `มาเรียน`** — the system then does the right thing with no change from us.
2. **Move the trigger later** (e.g. 02:00) if they mark up at end of shift — costs nothing, one `schtasks` edit.
3. **Change what the cut writes** — a new REQ, because `NO_SHOW` is a factual claim about a child, and applying it
   by default to *"nobody told us"* is the wrong claim.

### ⚠️ Also check: today's 08:00 digest is not in the table
The newest `daily-digest` row is still **08-23** (the owner's manual 19:00 test). If it is already past 08:00 on
08-24, **the morning trigger did not fire** and that needs looking at — the `sm-daily-digest` task's Last Run
Result in Task Scheduler answers it in one glance.

**Ball: owner** — (a) were Sunday's 15 taught? if yes, the in-product correction returns their sessions; (b) the
digest check. **@Sober** — REQ-068. **@Fern** — TASK-167 → TASK-176.

## [--:--] Porter (PM) — ✅ Digest fired. **The `uat` scheduled-task job is COMPLETE and verified.**
My "the 08:00 digest did not fire" flag was wrong for a plain reason the owner gave: **he ran the query before
08:00.** The `daily-digest` row for **2026-08-24** is now there. **Nothing was broken; I read a table taken before
the event and treated it as evidence about the event.**

**Status of what the customer asked for — all three installed, two proven by their own audit rows:**
| task | proven | how |
|---|---|---|
| `sm-end-of-day` | ✅ | ran 23:30 on 08-23, `job_runs` row written, real work done (15/15) |
| `sm-daily-digest` | ✅ | ran 08:00 on 08-24 · manual run before install returned `sent: True` to a live admin |
| `sm-month-reset` | ⬜ | **not provable until 1 Sep 00:05** — and it writes **no `job_runs` row at all**, so the only check will be indirect (freelance budgets back at full ceiling). That gap is a queued BE item. |

⇒ **The request "install the scheduled tasks on the `uat` server" is DONE.** Closing it.

**The one thing still open from it is a business question, not an engineering one:** Sunday's 15 sessions are
`NO_SHOW` with quota consumed, reversible in-product, and **the pattern repeats this Saturday** unless the customer
marks attendance or we move the trigger. **Ball: owner.**

## [--:--] Porter (PM) — 🔴 The customer asked to fix Sunday, and **both ways of fixing it charge the family twice.** → @Sober
The customer (ขวัญ) confirmed exactly what I sized on Sunday night, in her own words:
> *"เด็กพวกนี้ขวัญกด confirm ไว้ แต่ไม่ได้มากด attend ให้ **แต่น้องๆ มานั่นแหละค่ะ** ... เรากดแก้ให้น้องมาเรียนปกติได้ไหมคะ"*

**The children attended.** So the quota the job consumed is **correct** — one class taught, one session used.
**Only the label is wrong.** And there is no way in the product to fix only the label:
- **`มาเรียน` on a `NO_SHOW` row** — the guard is `if (status !== "ATTENDED")` (`scheduler.service.ts:1904`), so it
  runs and **increments `used_sessions` again**. 🔴 **The family pays twice for one class.**
- **Cancel then re-mark** — `returnsConsumedUnit` is **`ATTENDED` only** (`lib/checkin-correction.ts:11`), so a
  `NO_SHOW` cancel **gives nothing back**, *and* `reconcileCoursePlan` appends a make-up nobody bought. **Worse.**

⇒ **Told the owner: have her press NOTHING.** Doing nothing leaves 15 wrong labels; either button turns that into
15 wrong labels *plus* 15 wrongly-consumed sessions on real families' packages.

### Root cause, and why it hid this long
`used_sessions` has **two** writers — `attend` and the job's `NO_SHOW` cut — but the correction helper knows only
about `attend`. **The job's consumption is invisible to the correction path.** It could not surface until the cut
actually ran on real bookings, and `courses_cut` was **0 on every run on both boxes** until last night. **The gap
I flagged as "unexercised" was hiding a real defect, not just a lack of evidence.**

**REQ-070 raised** (`requirements/REQ-070-fix-noshow-to-attended.md`): correcting `NO_SHOW → ATTENDED` must leave
`used_sessions` untouched, must be doable **by staff in the product** (this is a daily ops mistake, not an
engineering event), plus a one-off repair for the 15 rows already wrong. Q1/Q2 for SA in the file.

**Deliberately NOT bundled:** *should the job mark unmarked sessions `NO_SHOW` at all?* — the bigger question, its
own REQ. **Saturday is in three days**; stopping the bleeding comes first.

⚠️ **Interim, and it costs nothing:** if staff press `มาเรียน` **before 23:30 on the day**, none of this happens.
That is worth telling the customer today regardless of what we build.

**Ball: @Sober** — REQ-070 jumps ahead of REQ-068. Live, customer-named, and every day it waits is another day the
same 15-shaped mistake can repeat. **Ball: owner** — tell ขวัญ to press nothing yet, and to mark attendance same-day
from now on.

## [--:--] Porter (PM) — ✅ Owner's call: **kill `NO_SHOW`.** I checked it against the code and he is right. → @Sober
> *"ฉันว่าไม่ควรมี noshow ... ถ้าตอน dayend เห็นคาบ confirm แต่ไม่มาเรียน ให้ปรับเป็นมาเรียนให้เลย"*

**Three things I verified before agreeing, because agreeing fast is not the same as agreeing correctly:**
1. **`NO_SHOW` has exactly one writer in the whole codebase — our own day-end job** (`jobs.service.ts:47`). No
   screen, no API, no human can produce it. **The machine infers it from silence and writes it as a fact about a
   child.** The schema comment admits it (`schema.ts:52`).
2. **Quota already treats the two as the same:** `COURSE_DELIVERED = {ATTENDED, NO_SHOW}` (`course-plan.ts:9`).
   ⇒ the label carries **no mechanism at all** — swapping it changes nothing about consumption.
3. **The reward design he guessed at is already built.** `CRM_POINT_RULES`: **`ON_TIME_CHECKIN: 10`**,
   **`PROPER_SICK_LEAVE: 5`**; five levels `น้องใหม่ → ซูเปอร์สตาร์`; **QR check-in already awards the 10**
   (`checkin.service.ts`). ⇒ *"customers who follow the rules vs customers who just book"* is separated by
   **points earned at check-in**, which is independent of the status label. **He told me to look and the design
   was there.**

⇒ **His design does not work around the double-consume defect — it deletes the state that causes it.** After this,
no new `NO_SHOW` can exist, so the correction path has nothing left to get wrong. **That is a smaller system, not
a patched one.**

**REQ-070 rewritten** to his shape: day-end writes `ATTENDED` · **no CRM points** (they did not check in — that is
the signal, keep it) · `NO_SHOW` stays in the enum for historical rows · repair the 15 `uat` rows with
`used_sessions` **untouched** (already correct).

⚠️ **Named so it is not discovered later:** `attention.ts:79` flags *yesterday's `NO_SHOW`* in the 08:00 digest.
That check goes dead. **@Sober — kill it or re-point it at *"attended but never checked in"***, which is now the
signal worth watching and is the same cohort the CRM level is about.

**Ball: @Sober** — REQ-070, ahead of REQ-068. **Ball: owner** — tell ขวัญ to press nothing on the 15; the repair
comes with the fix.

## [--:--] Sober (SA) — cut REQ-070 (kill NO_SHOW) as lane 1. Grounded the owner's design; it's a smaller system, not a patch. → @Jason @Porter
Took REQ-070 ahead of REQ-068 per your ranking. The owner's read checks out against the code, and the fix removes
the defective state rather than patching the correction path:
- The day-end cut is `NO_SHOW`'s **only writer** (`jobs.service.ts:47`); quota already treats `{ATTENDED,NO_SHOW}`
  identically (`course-plan.ts:9`), so the label is a false claim with no mechanism. ⇒ **write ATTENDED**, keep the
  (unchanged) quota increment, **no CRM points** (the cut never awarded them — the absence is the owner's signal).
  After this, **no new NO_SHOW can exist**, so the double-consume defect has nothing left to act on.

**TASK-180 (BE):** the label swap + rename the `job_runs` counters (noShow/coursesCut → autoAttended) so the audit
tells the truth; **NO_SHOW stays in the enum** for history (swept the three read sites Porter named — they render a
historical NO_SHOW fine); **drop the now-dead `attention.ts:79`** NO_SHOW digest check. **Repair the 15 uat rows**
NO_SHOW→ATTENDED with `used_sessions` UNTOUCHED (already correct — the children attended, one session consumed each)
— owner-run SELECT-confirm-15 → UPDATE, idempotent.

**Two SA calls:**
- **File Q1/Q2 are superseded** by the owner's source-fix: no correction path is built, and `returnsConsumedUnit` is
  **not** widened to NO_SHOW (that was for the abandoned shape and could disturb the make-up logic).
- **`attention.ts:79` — dropped, not re-pointed.** Re-pointing at "attended but never checked in" is a genuinely
  useful signal (the low-CRM cohort) but needs a reliable was-checked-in marker — its own grounding. Flagged as a
  follow-up candidate, not built on spec.

**Ball: @Jason** — TASK-180 (stops the weekend bleeding — Saturday is the deadline). **@Porter** — the repair SQL to
relay to the owner (after the code deploys, so no fresh NO_SHOW appears mid-repair); tell ขวัญ to press nothing on the
15 (the repair fixes them) and to mark same-day from now on. REQ-068 (the note re-spec) resumes behind this — TASK-178
is DONE, so TASK-142(re-cut)/179 are Fern's whenever. **@Fern** — TASK-167 → TASK-176 → 142/179. Nothing routes to
the owner from me.

## [Jason (BE)] — TASK-180 BUILT → REVIEW. 🔴 **One consequence has money in it and nobody named it yet.** → @Sober @Porter
The label swap is done and the weekend bleeding stops with it. But read Q1 before this deploys.

**The write is `ATTENDED`**, quota increment untouched, **no CRM points** — the absence is the owner's signal
and it is kept deliberately. The header now records *why* the old label was wrong (no mechanism, only a claim
about a child), because that is the decision someone would otherwise "restore" in six months.

**I renamed the predicate too: `isNoShow` → `isDueForAutoAttend`.** It never decided a child was absent; it
found the sessions nobody had marked. Leaving the old name would keep the false claim alive in the code right
after we removed it from the data. **The rule itself is unchanged** and there is a test asserting that *which*
sessions it picks is exactly what it was — the rename must not be able to hide a behaviour change.

**The dead digest check is deleted, not skipped**, and the registry count test moves 11 → 10 with the reason
written in: a line that is structurally always zero teaches everyone to skim past the ones that aren't, and a
check **leaving** is as legitimate a move as one arriving.

**Historical `NO_SHOW` verified at all three read sites and pinned by a test.** `course-history` still renders
it — deleting the enum "because nothing writes it any more" would blank real sessions out of a family's
history. The daily report's `noShow` will read 0 from here on and now says so beside itself.

### 🔴 Q1 — the part I want decided before deploy, not discovered after
The day-end revenue pass selects **`status = ATTENDED`** for `FIRST_TRIAL` / `SINGLE_SESSION` — which are
exactly the two types that recognise revenue at day-end. So from this change on, **a trial or single session
that nobody marked will post its revenue**, where before it became NO_SHOW and posted nothing.

- Arguably right: the slot was held, the session ended, and staff not pressing a button has been under-reporting
  real money.
- Arguably wrong: nobody confirmed the child came, and the cash may never have been collected at the counter.

**I did not special-case it.** Suppressing it correctly across re-runs needs a *"was auto-marked"* marker — a
**column**, not a condition — and inventing that on spec is worse than naming the consequence. It also lands on
`uat` at the same moment the day-end job has only just been switched on there, so the first run will be the
largest one. **Your call, @Sober**, and Porter may want the owner's view since it is his revenue line.

**Verified:** tsc **0** · `bun test` **750 pass / 0 fail** (+7, −3 deleted). No migration. ⚠️ Nothing run
against a database.

**@Porter — the repair, AFTER the code deploys** (or the job creates fresh NO_SHOW rows mid-repair): confirm the
count is **15**, then `NO_SHOW → ATTENDED` with `used_sessions` **untouched** — the children attended and one
session each was already consumed. Idempotent; a re-run finds 0. SQL is in chat.

**Ball: @Sober** — review + **Q1 before deploy** (TASK-177/178 Qs are also still with you). **@Porter** — the
repair sequence + the owner's view on Q1. **@Fern** — untouched; TASK-142/179 remain unblocked.

## [--:--] Sober (SA) — TASK-180 (kill NO_SHOW) = PASS ✅ (code). Q1 is a money call — ruled + routed to you. → @Jason @Porter
Reviewed by reproduction: tsc **0** · `bun test` **750/0** (+7, −3). The swap is clean: cut writes ATTENDED
(`jobs.service.ts:52`), quota unchanged, no CRM points; counters renamed `autoAttended`; predicate
`isNoShow → isDueForAutoAttend` (good — the false claim shouldn't live on in the code); dead `attention.ts:79`
deleted (registry 11→10); historical NO_SHOW kept in the enum + the three read sites swept and pinned. **Code DONE.**

**🔴 Q1 — Jason caught a real one, well-flagged: unmarked trial/single now auto-post revenue.** The day-end revenue
pass keys on `status=ATTENDED` (`:104`), so a FIRST_TRIAL/SINGLE_SESSION nobody marked now posts its price, where
NO_SHOW posted nothing. **My ruling — SHIP as-is (it posts):**
- It's the **consistent consequence of the owner's own design** ("confirmed-ended → attended"), and the old
  behaviour (NO_SHOW → no revenue) was the exact **silent lost-revenue** class we've chased all week. The system
  never verifies cash collection even for a *manually* marked ATTENDED — so this isn't a new liberty, it's attend
  working uniformly.
- **But it's your revenue line, so this is your confirm to give, not mine to assume** — @Porter, get the owner's
  one-word yes **before deploy** (surfaced, not silent). If he'd rather unmarked trial/single **not** post, the
  follow-up is a *"was auto-marked"* column (so suppression survives idempotent re-runs) — not something to invent
  on spec. **It does not block the weekend course fix** (courses post at sale; the label swap has no revenue effect
  on them).

**⇒ Deploy on the owner's Q1 confirm (I recommend proceed).** Then the **repair** (15 uat rows NO_SHOW→ATTENDED,
`used_sessions` untouched) runs **after** the code deploys so no fresh NO_SHOW appears mid-repair.

**Ball: @Porter** — the owner's Q1 confirm (fast) → then deploy + the repair SQL; tell ขวัญ to press nothing on the
15. **@Jason** — code done; the "attended-but-never-checked-in" attention check is the flagged follow-up if wanted.
**Next reviews on my desk: TASK-167 + TASK-176** (Fern delivered both), then REQ-068's TASK-142(re-cut)/179 when
Fern picks them up. Nothing routes to the owner from me.

## [--:--] Porter (PM) — ⚠️ Repair count is **13 on `uat`, not 15**. Two rows moved. + new customer case: cancel a course.
Owner confirmed Q1: **unmarked trial/single SHOULD post revenue.** TASK-180 ships as reviewed.

**`sid` 0 · `uat` 13.** `sid` 0 is expected (its cut never had a `CONFIRMED` to take). **`uat` 13 vs the 15 the job
reported is a 2-row gap**, and the most likely cause is that **someone pressed `มาเรียน` on 2 rows before the
"press nothing" message reached ขวัญ.** If so, those 2 courses have `used_sessions` **incremented twice** for one
class — the exact double-consume defect. **One question to the owner, not an investigation.**

The `UPDATE … WHERE status='NO_SHOW'` is safe regardless (it touches only what is still `NO_SHOW`); the 2 already
flipped need `used_sessions` −1 **if** they were flipped by hand.

**New from the customer:** *"ขอรบกวนยกเลิกคอร์สนี้ค่ะ"* with three course cards — Peace 0/6 Inline Skate ·
ยูจีน 2/10 Surfskate · **Jasper 3/10 Surfskate, expired 2026-08-22**. **Which course is not stated**, and
cancelling a *live, partly-used, paid* course is **early termination — REQ-036**, already named in code
(`scheduler.service.ts` cancel path: *"Fix rides REQ-036 (early termination)"*) and **not built**. Money is
involved (refund or credit), so it is the owner's call before it is an engineering one.

**Ball: owner** — deploy → run the 13-row update → answer whether anyone pressed `มาเรียน` on any of the 15, and
**which** course is to be cancelled.

## [--:--] Porter (PM) — 🔴 OUTAGE: the customer's calendar returned 500 on **both boxes**. Cause found, both green again.
`GET /api/calendar?date=2026-08-31&view=week` → **500 INTERNAL** on `uat` (the customer's home screen) and on `sid`.

### Cause — one line
**TASK-178 (`attendee_note`) was deployed without its migration `0022`.** The code selects `bookings.attendee_note`;
the column did not exist ⇒ every calendar read threw. Found in ~2 minutes from `git show --stat` on the newest
commit: it ships `drizzle/0022_booking_attendee_note.sql`. **No log-reading needed — the diff named it.**

### The second, worse layer: `db:migrate` had SKIPPED it silently
On `uat`, `0022` read **`not-applied · column found=false`** *after* a successful-looking `db:migrate`. This is the
**TASK-085/086 failure mode the board has documented since 2026-08-02**: the ledger held a row newer than the
migration, so drizzle applied nothing and **exited 0**. ⇒ *"migrations applied successfully"* was printed while the
schema stayed broken. **The deploy looked fine and was not.**

### Fixed — sid first, then uat, both verified
`db:seed-ledger` (dry-run → `--apply`, `0 need a human` both boxes) → `db:migrate` → `db:verify`.
**Both boxes now: journal 23 · ledger 31 · witnesses 23 · ✅ green.** The order mattered: seeding first is what let
`migrate` see `0022` at all — the tool said so itself (*"After seeding, db:migrate would apply: 0022"*).

### What this costs us to keep, and what it buys
`db:verify` and `db:seed-ledger` **did their job exactly as designed** — the witness probes proved the schema
independently of the ledger, and the dry-run showed the fix before touching anything. **This is the tooling from
the 08-02 outage paying for itself.** What failed is **process, not tooling: the deploy sequence did not run
`db:migrate`+`db:verify` before restarting**, which the board's own *"MIGRATION CHECK — read this before every
single deploy. No exceptions"* section exists to prevent.
⇒ **Action for me:** the deploy runsheet needs `db:migrate` → `db:verify` as a **blocking** step with the app
restarted only after green — same standing the `sale:ensure-items` lesson earned on 08-22.

### Still open from before the outage
- **Repair 13 rows** (`NO_SHOW → ATTENDED`) on `uat` — count was **13, not 15**; 2 rows moved and I still owe the
  owner's answer on whether staff pressed `มาเรียน` on them (if so, those 2 courses double-consumed).
- **Customer wants a course cancelled** — which of the three is not stated, and refund/credit is the owner's call.
  Early termination = **REQ-036, not built.**

**Ball: owner** — restart `uat`, **confirm the calendar renders for the customer** (the screen, not `db:verify`),
then the 13-row repair.

## [--:--] Porter (PM) — ✅ Sunday's quota damage found, named, and closed. Both suspects confirmed by evidence.
The integrity check found **exactly two** over-consumed courses — matching the 15−13 gap with nothing left over:

| student | stored | prior | delivered | diff |
|---|---|---|---|---|
| **Prize** | 2 | 0 | 1 | **+1** |
| **เภา** | 1 | 0 | 0 | **+1** |

**Both were in Sunday's 15** (Prize 13:00 · เภา 15:00, both Surfskate). **The arithmetic closes.**

**And the two paths I warned about are both confirmed, one each:**
- **Prize — `มาเรียน` pressed on a `NO_SHOW` row.** Status `ATTENDED` (delivered 1), quota incremented a second
  time on top of the job's. Exactly the double-consume.
- **เภา — `cancel` used instead.** Her plan proves it: **23/Aug `CANCELLED`** + an auto-appended **01/Nov
  `EXTENDED`** makeup, `0 sessions still owed`. `returnsConsumedUnit` is `ATTENDED`-only, so **the unit was never
  returned** *and* a session nobody bought was added. **The "strictly worse" path, in the wild, on a real family.**

**Note the method, because it is the reusable part:** the owner's `UPDATE … NO_SHOW → ATTENDED` had already erased
which rows were which — **and it did not matter.** `COURSE_DELIVERED` counts `ATTENDED` and `NO_SHOW` alike, so
`stored − prior − delivered` finds the damage **without needing to know which rows were touched.** Derive the truth
from an invariant, not from an audit trail you no longer have.

### Owner's decision: **(ก) — accept the cost, fix the counters now**
`used_sessions − 1` for both. เภา's plan stays as it is ⇒ she attended on 23/Aug and still has 10 sessions
scheduled ⇒ **the customer gives away one session (~฿790).** The owner chose this over unpicking a live family's
plan at the end of a very long day. **His call, and the cheaper risk.**

### (ข) — held, not dropped. What it needs when the customer answers
Restore truth for เภา: `23/Aug → ATTENDED` · **delete the `01/Nov` `EXTENDED` row** · `used_sessions` back to `1`.
⚠️ **Do not do this with the product's cancel button** — cancelling the `EXTENDED` row would re-owe *another*
makeup (`reconcileCoursePlan`), which is the same trap one level down. It needs a direct, dry-run-first correction.
**Evidence is being captured tonight** (booking ids + statuses) because **next Sunday's job will move this plan**
and the ids get harder to reconstruct after that.

### Two leftovers, neither urgent
- **เตอร์ `diff −1`** — under-counted, unrelated to Sunday, **in the customer's favour.**
- **`Test` `diff −3`** — the fabricated household. Ignore.

**Ball: owner** — run the `−1`, send เภา's booking rows for the record. **@Sober** — REQ-070 still the lane; add
**"cancel from an auto-attended/no-show row must return the unit"** to it, now proven in production.

## [--:--] Porter (PM) — ✅ CLOSED. Quota is clean across `uat`. End of a long day.
Re-ran the integrity check after the `−1`: **Prize 1/0/1 → diff 0 · เภา 0/0/0 → diff 0.** **No course anywhere on
`uat` is over-consumed.** Only two known, non-urgent rows remain: **เตอร์ `−1`** (under-counted, unrelated to
Sunday, in the customer's favour) and **`Test` `−3`** (fabricated household).

### The day, for the record
1. **First nightly run on `uat` did exactly what I predicted** — 15 real Sunday sessions → `NO_SHOW`, 15 sessions
   consumed. Predicted before it happened, confirmed by the customer the next morning.
2. **The customer's fix request had no safe answer** — both product paths double-consume. Told her to press
   nothing; **two rows had already moved**, and both are now repaired.
3. **The owner killed `NO_SHOW` as a concept** — verified against the code (one writer, no mechanism, CRM points
   already carry the "good customer" signal he wanted). **REQ-070 shipped as a smaller system, not a patch.**
4. **Outage:** `attendee_note` deployed without migration `0022`, which `db:migrate` **skipped silently** — the
   documented TASK-085 failure. Both boxes down, both green inside the hour. `db:verify` is now a **blocking**
   deploy step on the board.
5. **Damage found without an audit trail** — the owner's `UPDATE` had erased which rows were which, and it did not
   matter: `stored − prior − delivered` located both from an invariant. **Worth keeping as a technique.**

### Open, for tomorrow
- **(ข) — restore เภา's truth**: `23/Aug → ATTENDED`, delete the `01/Nov EXTENDED`, `used_sessions → 1`.
  ⚠️ **Not via the cancel button** — it would re-owe another makeup. Owner accepted the ~฿790 cost meanwhile.
- **Customer wants a course cancelled** — still hasn't said which; refund/credit is the owner's call.
  Early termination = **REQ-036, not built**.
- **@Sober** — REQ-070's remaining piece + **add "cancel from an auto-attended row must return the unit"**, now
  proven in production. Then **TASK-167 / TASK-176 review** (Fern delivered both), then REQ-068.

## [--:--] Porter (PM) — 🔻 Correction: the day is NOT over, and one item on my "open" list is already solved.
**The owner corrected me: it is still 2026-08-24.** He was out this morning and has just come back. My previous
entry framed this as end-of-day with items *"for tomorrow"* — **wrong; the work continues now.** The log's dates
were right, my framing was not.

### ✅ And on re-checking: "cancel must return the unit" needs NO work — killing `NO_SHOW` already fixed it
I queued that for @Sober off the เภา incident. **Checking before asserting:** `returnsConsumedUnit` is
`status === "ATTENDED"` (`lib/checkin-correction.ts:11`). เภา's row was **`NO_SHOW`** ⇒ false ⇒ no return.
**After TASK-180 the job writes `ATTENDED`** ⇒ the same cancel now **returns the unit correctly.**
The other half too: pressing `มาเรียน` on an auto-attended row hits `if (current.status !== "ATTENDED")` and
**skips** ⇒ **no double-consume.**

⇒ **The owner's design change fixed all three defects at once** — the false label, the double-consume, and the
cancel-that-gives-nothing-back. **Deleting the state deleted its whole family of bugs.** Nothing to queue.

### @Sober's actual lane, nothing invented
1. **Review TASK-167 + TASK-176** — Fern delivered both; they have been sitting.
2. **REQ-068 + re-spec TASK-142** — the long lane, still not started.

**Ball: @Sober** — reviews first (they unblock Fern), then REQ-068.

## [--:--] Porter (PM) — 🔴 The customer asked to cancel a course. **The product cannot do it at all.** REQ-036 raised. → @Sober
> *"ขอรบกวนยกเลิกคอร์สนี้ค่ะ"* — with three course cards: **Peace ญดา 0/6 Inline Skate** · **ยูจีน 2/10 Surfskate** ·
> **Jasper 3/10 Surfskate, expired 2026-08-22.**

**Checked the code before answering, and the answer is worse than "not convenient":**
- `routes/api.ts` has **no delete, no cancel, no terminate** for a course. `PATCH /courses/:id` accepts **one
  field — `adminUnlocked`** (`validation.ts:339`); `size` is not editable.
- **Cancelling the sessions one at a time does not work** — SPEC-028 §11.3 makes every course-session cancel a
  *reschedule*: `reconcileCoursePlan` re-owes a make-up. **They grow back.** We watched this happen to เภา today.
- `course:cleanup` refuses anything with a posted sale, and every bought course has one. Wrong tool anyway.

⇒ **There is no action available to the owner today**, and **doing nothing is not neutral**: the remaining
`PENDING` sessions keep appearing on the calendar and staff will teach them.

**REQ-036 raised** (`requirements/REQ-036-end-a-course-early.md`) — it has been named in our own code since
SPEC-028 (*"Fix rides REQ-036 (early termination)"*) and never built. **A paying customer has now asked for it.**
Core rule: ending a course early is a **forfeit/settlement, explicitly not a reschedule** — remaining sessions go
and **nothing is re-owed**, which is the exact opposite of the cancel path's designed behaviour. SA question left
open and grounded: reduce `size` to `used_sessions` (reuses the existing invariant) vs an explicit `endedAt`.

**Two owner questions block the ACTION, not the build — so @Sober can start now:** which course, and refund /
credit / no refund. **Porter will not assume a policy or a number** (the last time I inferred a money rule from a
document it blocked live bookings).

**Ball: @Sober** — REQ-036 as lane 1; it now outranks REQ-068. **Ball: owner** — Q1 + Q2, and the interim answer
to the customer.

## [--:--] Porter (PM) — ⏱️ Owner: **build the cancel button now, 30–40 minutes.** Scope cut, contract fixed. → @Sober @Jason @Fern
> *"ไม่ต้องทำละ ไปทำ ปุ่มยกเลิกคอร์ส แบบรัดกุมมาแทน เอาก่อนเลย"*

**Q1/Q2 dropped as blockers.** We are not settling a refund policy tonight — we are giving staff the ability to end
a course. **The system records the reason; the money stays a human decision per case.**

**The scope cut is in the REQ.** In: `POST /courses/:id/cancel {reason}` (removes remaining `PENDING`, **no
re-owe**, delivered untouched, `used_sessions` unchanged, **no ledger write**, idempotent) + a confirm dialog that
**names the student and the session count** and requires a reason. Out: refunds, credit maths, `bo.movement`,
parent notification, bulk cancel.

**🔒 The five "รัดกุม" checks are written down by name** so review is against a list, not a feeling. Number one is
**"no make-up is ever re-owed"** — the existing cancel path does the opposite *by design* (SPEC-028 §11.3), so this
is the one way the feature most plausibly ships wrong.

**⏱️ To fit 30–40 minutes I fixed the API contract in the REQ itself** — request, 200 shape, `409 ALREADY_ENDED`,
`400 REASON_REQUIRED`. **@Jason and @Fern build simultaneously; neither waits for the other.** That is the only way
this lands in the window, and it is a PM call, not an SA one — @Sober, override the shape if it is wrong, but
please keep BE and FE unblocked while you do.

**Ball: @Sober** — cut it now, thin. **@Jason** BE · **@Fern** FE, in parallel.

## [--:--] Sober (SA) — TASK-167 (REQ-064 AC-8) + TASK-176 (REQ-067 Part A) both = PASS ✅. → @Fern @Porter
Reviewed both by reproduction: front tsc **0** · build ok.
- **TASK-167 (AC-8 modal align):** `Group grow align="flex-end"` on **all three** field rows of `ImportBalanceModal`
  (not just the reported one) — end-aligning the label→description→input wrappers keeps the **inputs** on one line
  whatever height a helper takes (incl. the 375 wrap). Fixes the ROW, not one box — exactly your requirement; the
  comment states the rule so `flex-start` can't creep back. §3.5 0/0/0/0. **REQ-064 AC-8 code done.**
- **TASK-176 (labels):** four dictionary lines — `FIRST_TRIAL:"1st Trial"`, `SINGLE_SESSION:"1 HR"` in both dicts
  (Thai carries the English card names — AC-1). **AC-3 grep-verified:** every staff-facing surface renders through
  `bookingType.*` (one def per language), so four lines is the whole change; **AC-2** values untouched (the
  request-body tests still assert `bookingType` on the wire). 40/0. **REQ-067 Part A code done.**
- Both **rendered** checks ride @Tanya (headless modal / read-through) — 167's is the narrow-to-375-helper-wrap
  case, 176's is that the two words read identically on screen and card.

**⇒ REQ-064 is now fully code-complete** (AC-1–7 shipped earlier, AC-8 here) and **REQ-067 Part A done** (Part B
already delivered). Board updated.

**Ball: @Fern** — your open FE lane is now REQ-068: **TASK-142 (re-cut)** + **TASK-179** (TASK-178's DTO/endpoint is
live). **@Tanya** — two cheap rendered read-throughs to fold into any `uat`/`sid` pass (AC-8 alignment at 375; the
`1st Trial`/`1 HR` labels). **@Porter** — you're mid-flight on REQ-070 (Q1 confirmed, deploy + the 13-row repair, the
2-row double-consume question to the owner); REQ-068 note wording still yours to draft. My SA queue: clear pending
TASK-142/179 returning for review, or your next lane. Nothing routes to the owner from me.

## [--:--] Fern (FE) — TASK-179: creation + detail built; the per-session half is blocked on one BE line. → @Sober
Took 179 ahead of 142 because it **creates the data 142 displays**, and because it carried a call you delegated to me.

### Built
A shared **`AttendeeNoteInput`** — label, the not-for-PII hint, autosize, and a live **n/200 counter that turns into
an error past the limit**. I deliberately did **not** use `maxLength`: it silently swallows the 201st character, so
staff watch their typing vanish with no reason given. 200 mirrors the BE's own schema, so the refusal appears before
the server has to make it. **All four booking types take it at creation** (empty ⇒ the key is absent, AC-5), the
**booking detail shows it** separately from the status `note` the cancel/leave flows own, and the service/hook/mock
for `PATCH /bookings/:id/note` are in.

### 🔴 The call you left me: course creation **omits** the note — per-session only
Three reasons, in order of weight: "who's bringing the child **this hour**" is inherently per-session; it removes
Jason's edit-surprise **entirely** rather than papering over it with a caption; and a caption like "applies to all
sessions initially" is a rule the screen has to *teach* — a screen that needs teaching is exactly what REQ-044 was
about. I changed **no BE contract** — the seeding stays, the course form simply doesn't offer the field.

### 🔴 Blocked half, and why I stopped rather than shipped it
`toSessionRow` (`scheduler.service.ts:1243`) is an allow-list without `attendeeNote`, so the manage-course editor
could **save but not show**. An editor that can't display the current value is worse than no editor: staff overwrite
a note they can't see. **Q1 is one line** (`attendeeNote: b.attendeeNote ?? null`) and then I finish it immediately.

### ⚠️ Q2 — the pattern, which I think is now the real finding
**That is the third compiler-silent allow-list to bite this feature set**: `createBooking`'s POST body (was
TASK-170), `dtoToBooking` (Part 2 — caught only because I made the field *required*), and now `toSessionRow`. Each
time the types were happy. TASK-172 guards the **request** bodies; **nothing guards the response mappers**. The cheap
symmetric version is a test that a DTO fixture survives the mappers with every field intact. Your call — I'm not
adding it unasked, but I'd rather name the pattern than keep meeting it.

**Q3:** the PII hint is **my draft** — Porter's wording isn't written yet and the field is unusable without one.
Flagged, not slipped in.

**Evidence:** `bunx tsc --noEmit` **0** · `bun run build` **ok** ·
`bun test src/lib/scheduler/ src/services/` **40/0** · §3.5 **0/0/0/0**. Rendered → @Tanya.

**Ball: @Sober** (review · **Q1 the one-line `toSessionRow` fix** unblocks the rest of 179 · Q2 response-mapper guard ·
Q3 → Porter). Next in my lane: **TASK-142** (palette signed off — icons only, no emoji — and `attendeeNote` is now on
the booking DTO, so the cell can be built once as you re-cut it).

## [--:--] Sober (SA) — REQ-036 cut NOW: TASK-181 (BE) + TASK-182 (FE), parallel. → @Jason + @Fern (build) · @Porter (Q1/Q2)
Porter's latest directive = build the `ยกเลิกคอร์ส` button now, thin, BE+FE in parallel, in the owner's window. He fixed
the contract and left me **R1** (soft-cancel vs delete) and **Q3** (mechanism) with "verify, don't assume." Grounded both:

**R1 → SOFT-CANCEL (PENDING→CANCELLED), not delete.** Verified in code, not assumed: `getCalendar` filters
`ne(b.status,"CANCELLED")` (`scheduler.service.ts:407`) so cancelled sessions **leave the calendar**, and the slot-block
query treats CANCELLED as non-blocking (`:1392`) so the **slot frees**. `courseCurrent` already excludes CANCELLED. Delete
destroys the audit trail for nothing gained — soft-cancel keeps `ADMIN_ERROR` inspectable/undoable later.

**Q3 → a new `endedAt` flag, NOT size-reduction.** Grounded the failure of the cheaper options: after soft-cancel,
`owedCount = coursePlanSize − current` reads **non-zero** (a 3/10 course → 7) and `insertable` stays true → staff inserts →
`reconcile` re-owes (**check #1 fails**). Reducing `size` corrupts "what they bought" (the REQ-064 lesson). So: additive
migration (`ended_at`/`end_reason`/`end_note`/`ended_by`) + guard the three plan-responsibility sites on `endedAt`
(`owedCount→0`, `insertable→false`, `reconcileCoursePlan→no moves`) — the same seam REQ-064 centralized. That makes AC-1's
"0 owed" true **by construction and permanently**, not just because we skipped one reconcile.

**Cut both, parallel (contract fixed, neither waits):**
- **TASK-181 (BE, @Jason):** the migration + `POST /courses/:id/cancel` (soft-cancel remaining PENDING in one tx, NO
  reconcile, record reason+actor, idempotent 409) + `/cancel/preview` (writes nothing) + the `endedAt` guards + history
  surfacing. R4 = 6 tests, **#1 = no make-up re-owed** (no EXTENDED appears; owedCount 0; insertable false), delivered rows
  byte-identical, used_sessions unchanged, no `bo.movement`, idempotent, three reasons round-trip + `ADMIN_ERROR` listable.
- **TASK-182 (FE, @Fern):** `ยกเลิกคอร์ส` button → confirm dialog **powered by `/cancel/preview`** (names the student + the
  **server's** removed-session count + cannot-undo — the count must NOT be client-computed, R2), reason as a **3-option
  choice** (PROGRAM_CHANGED/CUSTOMER_CANCELLED/ADMIN_ERROR) + optional note, Confirm disabled until a reason is picked,
  surfaces the 409/400.

**OUT of scope (owner/Porter, gate the ACTION not the build):** Q1 which course, Q2 refund/credit — refunds, ledger,
notify, re-owe, bulk-cancel all excluded; recording the reason enum is what makes an `ADMIN_ERROR` sale findable later.

**Ball: @Jason (TASK-181 BE) + @Fern (TASK-182 FE) — build in parallel.** @Porter: the owner's Q1/Q2 answers gate whether/
which course gets cancelled and the money follow-up, not the build — I need nothing from the owner to ship the button. Fern
also still has my review of TASK-179 pending (Q1 = the one-line `toSessionRow` fix unblocks the per-session half) — that
rides behind this urgent lane.

## [Jason (BE)] — TASK-181 BUILT → REVIEW. The button's server half is done; **one scope call needs ratifying.** → @Sober @Fern @Porter
Built to your contract, in parallel with Fern. **I also added the board row for it — TASK-181 had none.**

**`0023`** — four nullable columns **plus a CHECK on `end_reason`**: a typo'd reason is an ended course nobody
can find again, which defeats the enum's one job. Witness is the CHECK, so a half-applied run is detectable.

**🔴 `endedAt` guards FOUR sites, not three.** Besides `owedCount`, `insertable` and the reconciler, the
server-side `NO_OWED_SESSION` gate asks the same question — the fourth site TASK-165 turned up — and it now
routes through the same `canInsertIntoCourse`, so the DTO and the API cannot disagree about an ended course.
**R4.1 is tested in both directions**: an ended course plans nothing, *and* the identical course **not** ended
would have re-owed. The second assertion is the one that proves the guard is doing something.

### 🔴 Q1 — the scope call I want ratified
The contract says "remaining **PENDING**". **I cancel PENDING *and* CONFIRMED**, because a course's sessions
become CONFIRMED the moment staff confirm the schedule — so PENDING-only would leave a cancelled course's
sessions sitting on the calendar and still blocking their slots, which is exactly what the customer asked us to
remove. R4.2's untouched list (`ATTENDED / EXTENDED / SICK_LEAVE / CANCELLED`) pointedly omits CONFIRMED, which
is why I read "PENDING" as wording rather than intent. **One constant (`ENDABLE_STATUSES`) to revert** if you
disagree — but the button would then not do what the owner is asking for today.

**R4.3/R4.4 are source-asserted**, and I'd rather name the limit than let a green suite imply more: I cannot
prove "the `bo.movement` count is identical" without a database, so the guarantee is that **`endCourse` cannot
write one** — its body may not contain `recordSale`, `boMovement`, `refund`, `usedSessions`,
`reconcileCoursePlan`, or any notify path. Refusals throw **inside** the transaction (a refused call rolls back
rather than trusting an early return), and the 400s carry **codes**, so the FE can tell "you didn't pick a
reason" from "that isn't a reason we accept".

**History gets ONE `course-ended` event** with the reason and note, in date order — not N anonymous
cancellations nobody could tell from an ordinary cancel. `actor` stays null like every other event (shared
login, per SPEC-035 §1); **who** is recorded in `ended_by` and stays answerable by query, matching your ruling
on the discount actor. And `endedAt`/`endReason` are on the **course summary**, so an ended course looks ended
on every screen that renders one — otherwise staff keep booking into it from a screen showing nothing wrong.

**Verified:** tsc **0** · `bun test` **778 pass / 0 fail** (+28). ⚠️ Nothing run against a database, so **the
route itself is un-exercised** — the guarantees above are pure-layer and source-level.

**@Fern — the contract is live as specified:** `POST /courses/:id/cancel` and `/cancel/preview`. One addition
you may ignore: the preview also returns `alreadyEnded`, so the dialog can say "already cancelled" instead of
offering a button that will 409 (Q2).

**@Porter — `0023` is owner-run, `sid` first, and MIGRATION BEFORE CODE.** Queue: `0020`·`0021`·`0022`·`0023`
(+ backoffice `0006`), REQ-065's flip SQL, TASK-180's 15-row repair after its deploy.

**Ball: @Sober** — review + **Q1 before this ships** (177/178/180 answers noted; thank you for the TASK-180
ruling). **@Fern** — TASK-182 against the live contract. **@Porter** — the migration order above.

## [--:--] Sober (SA) — TASK-181 (cancel course, BE) reviewed: REVISE, one correctness fix (EXTENDED). → @Jason
Reproduced clean: `tsc` **0** · `bun test` **778/0** (+28) · course-end suites 24/0. The build is strong — I'm keeping
the whole shape (four guard sites incl. the server `NO_OWED` gate so DTO+API can't disagree, `end_reason` CHECK as the
migration witness, coded 400s thrown in-tx so a refusal rolls back to zero rows, one `course-ended` history event,
`ended_by` recorded / `actor=null` per the discount-actor ruling, `size` untouched, source-level no-write assertions).

**But one real gap.** Jason asked me to ratify PENDING-only vs PENDING+CONFIRMED (he built +CONFIRMED, correctly). I
grounded it and the right line is **broader still: also EXTENDED.** An appended `EXTENDED` is a **future make-up
booking** (`scheduler.service.ts:1658`/`2053` — future date + real slot), it is **not** in `SLOT_NON_BLOCKING` (blocks
its slot) and getCalendar only hides CANCELLED (shows it). So **every course that ever took a sick-leave has a live
EXTENDED sitting on a teacher's calendar** — and `ENDABLE=["PENDING","CONFIRMED"]` would end the course but leave that
ghost make-up booked, blocking the slot: the exact bug Jason fixed for CONFIRMED, one label over. Worse, R4.2's test
**asserts EXTENDED stays untouched**, so a green suite would hide it. This is a silent-ship class defect (common path —
any leave triggers it).

**Ruling:** `ENDABLE_STATUSES = COURSE_LIVE_STATUSES` (PENDING, CONFIRMED, EXTENDED) — forfeit everything still LIVE;
delivered (ATTENDED/NO_SHOW) + SICK_LEAVE + already-CANCELLED stay byte-identical (real history). Flip the EXTENDED
assertion and add the case: *course with a sick-leave→make-up, ended → no live session on the calendar, make-up slot
free.* One-constant change + one test flip. Q2 (`previewCourseEnd.alreadyEnded`) accepted — Fern may use it.

**Ball: @Jason** — the one fix, then this is DONE (nothing else outstanding). **@Fern (TASK-182): keep building,
unaffected** — the status set is internal to the BE; `removedSessions` just counts the EXTENDED too, the contract shape
is unchanged. @Porter: still only Q1/Q2 (which course / refund) — they gate the action, not the build.

## [Jason (BE)] — TASK-181 rev 1: the EXTENDED gap fixed. **Your catch, and my test was hiding it.** → @Sober
Done as ruled: `ENDABLE_STATUSES = COURSE_LIVE_STATUSES` — and it is now **literally the live set**, not a
hand-written copy, so the next status anyone adds cannot drift away from it.

**You were right and my Q1 was too narrow.** I reasoned about CONFIRMED from "staff confirm the schedule" and
stopped at the label I could see. An appended `EXTENDED` is the same thing one step over — a real future booking,
not slot-non-blocking, shown by `getCalendar` — so **every course that has ever taken a sick leave** carries one,
and ending the course would have left a ghost make-up holding a teacher's slot on the commonest path there is.

**The part I want on the record: my own R4.2 test asserted EXTENDED stays untouched**, so the suite would have
gone green straight over it. That is the **third** time on this project a test has asserted the bug — Onewheel's
10 h, bike/skate's 1 h, now this — and the common thread every time is a test written from *the code's intent*
rather than from *what the user would see*. I flipped it and added the case it should have had all along: a
course with a leave → make-up, ended ⇒ **nothing LIVE remains and the make-up's slot is free**, with the
`SICK_LEAVE` row untouched because it is history and it is what earned that make-up.

**Verified:** tsc **0** · `bun test` **781 pass / 0 fail** (+3). Nothing else moved — contract, guards, history
event and the source-level assertions are exactly as you reviewed them.

**Ball: @Sober** — rev 1 is ready to close TASK-181. **@Fern** — unaffected, as you said: `removedSessions` just
counts the make-up too. **@Porter** — migration order unchanged (`0023` sid-first, migration before code).

## [--:--] Sober (SA) — TASK-181 revision verified: ✅ BE DONE. → @Fern (FE in flight) · @Porter (deploy note)
Reproduced Jason's fix: `ENDABLE_STATUSES = COURSE_LIVE_STATUSES` (line 232 — he made it *the* live set, not a second
list that could drift; better than the literal ask). `tsc` **0** · full suite **781/0** (+3). The flipped test is real,
not cosmetic: `course-end.test.ts:93` asserts an EXTENDED make-up **is** cancelled, and `:100` runs the end-to-end case
(course with leave→make-up, ended ⇒ nothing LIVE remains, the SICK_LEAVE row kept as history). That's the ghost-slot
case that would have shipped wrong on the commonest path (any course that took a leave).

**TASK-181 = BE DONE.** Everything else from the first pass stands (four guard sites incl. the server NO_OWED gate,
end_reason CHECK witness, in-tx coded 400s, single course-ended history event, size untouched, source-level no-write
assertions). ⚠️ Un-exercised against a DB by design — the route needs a **live pass** (Tanya/owner) before REQ-036 is
DELIVERED.

**Deploy (for the owner, via @Porter):** migration **`0023_course_ended`** — **`sid` first, migration BEFORE code** (the
columns must exist before anything reads them; this is the `attendee_note`-without-`0022` 500 lesson). `db:verify` after.

**Ball:**
- **@Fern** — TASK-182 (FE `ยกเลิกคอร์ส` button) still in flight, unaffected by this fix; that's the other half of REQ-036.
  You may also use `previewCourseEnd.alreadyEnded` (Jason's additive) to show "already cancelled" instead of a 409 button.
- **@Porter** — REQ-036 ships when the FE lands + a live pass; the owner's Q1 (which course) / Q2 (refund) gate the
  *action*, and the `0023` deploy note above is his to run. Nothing routes to the owner from me.
- Behind this: Fern's TASK-179 review (my Q1 = the one-line `toSessionRow` fix) once the urgent lane clears.

## [--:--] Fern (FE) — TASK-182 `ยกเลิกคอร์ส` BUILT → REVIEW. The other half of REQ-036. → @Sober
Took this ahead of TASK-142 — the owner asked for it in 30–40 minutes and it's the half that makes REQ-036 usable.

### Built
The action sits on the plan modal's **course** footer: red, apart from the add-a-session buttons, and it **opens a
confirm rather than acting on the click**. It renders only for `isCourse && !isCreate` and passes `plan.id`, so it is
**structurally incapable** of ending a different course from the same screen — that constraint is enforced by which
component owns the id, not by remembering to be careful.

The dialog calls **`/cancel/preview` on every open** — deliberately not cached, because a preview kept from a previous
open could describe a plan that has since changed — and renders REQ-036's sentence from **the server's** numbers.
🔴 **There is no client re-count anywhere in this path.** R2 exists because the two have disagreed before, and this
action cannot be undone; the number in that sentence is the entire safety mechanism.

Reason is the three contract values as a `Radio.Group` + optional note. **Confirm is disabled until one is picked —
and the screen says why**, rather than leaving staff to guess at a dead button. No fourth reason invented.
I used Jason's `alreadyEnded`: an already-cancelled course says so and **the Confirm button is not rendered at all**,
which beats offering a button whose only possible outcome is a 409. Server refusals surface **the server's own**
message; on success the mutation invalidates everything and the plan closes behind it.

**Evidence:** `bunx tsc --noEmit` **0** · `bun run build` **ok** ·
`bun test src/lib/scheduler/ src/services/` **40/0** · §3.5 **0/0/0/0**.

🔴 **Rendered verdict not done (headless modal) → @Tanya — and this one deserves a real pass more than most.** Suggest
testing it on a course that has taken a **leave**, i.e. the EXTENDED make-up case Jason's revision fixed: that way the
previewed count and what actually disappears are compared on the same course, which is exactly where a wrong number
would do irreversible damage.

**Q1 (follow-up, not this task):** nothing marks a course as **ended** afterwards — the plan just comes back empty, so
a cancelled course looks like a finished one. `alreadyEnded` already exists on the preview; an `ended` flag + reason on
the summary would let the plan say *"cancelled — {reason}"*. REQ-036's record is only half visible without it.

**Ball: @Sober** (review · Q1's follow-up call) **& @Porter** (REQ-036 needs the `0023` deploy + a live pass; the
owner's Q1/Q2 gate the *action*, not this build). Next in my lane: **TASK-142**, and TASK-179's blocked half the
moment `toSessionRow` carries `attendeeNote`.

## [--:--] Sober (SA) — TASK-182 (cancel button, FE) reviewed: ✅ FE code DONE. REQ-036 is CODE-COMPLETE. → @Porter
Reproduced `tsc` **0**; build ok + 40/0 per Fern. Spot-read `EndCourseDialog.tsx`: the count is **the server's**
(`data.removedSessions`, no client re-count — R2), preview re-fetched every open (no stale plan), `alreadyEnded` renders
no Confirm button, Confirm gated on a reason with the reason-for-disabling on screen, server 409/400 messages surfaced
verbatim, action structurally bound to its own courseId. Strong — matches the contract exactly.

**REQ-036 is now code-complete (BE ✅ + FE ✅).** Two things stand between here and DELIVERED, both off my desk:
1. **Rendered/live pass → @Tanya** (via Porter). This one earns a real pass: it's irreversible and the previewed number
   is the whole safety mechanism. Test against a course that **took a leave** (the EXTENDED make-up case) so the
   previewed count and what actually disappears are checked on the same course.
2. **Owner deploy** — migration `0023_course_ended` **sid-first, BEFORE code**, then `db:verify`, then the FE.

**Fern's Q1 (should a cancelled course *look* cancelled afterward): ruled a follow-up, not a blocker** — re-booking is
already server-guarded (`insertable=false`), so the gap is informational only. And it's BE-free: the BE already emits
`endedAt`/`endReason` on the course DTO (`contract.ts:111-112`); the FE type/mapper just drop them (the TASK-179-Q2
response-mapper pattern again). Cut as **TASK-183** (FE-only, queued behind the urgent lane + her TASK-179 finish).

**Ball: @Porter** — REQ-036 code-complete; route the rendered/live pass to @Tanya and hand the owner the `0023`
deploy note (sid-first, migration before code, `db:verify`). Q1/Q2 (which course / refund) remain the owner's, gating
the action not the build. Nothing routes to the owner from me.
**@Fern** next lane: finish **TASK-179** (my earlier Q1 = the one-line `toSessionRow` fix), then **TASK-142**, then TASK-183.

## [--:--] Porter (PM) — 🟢 REQ-036 deployed to `sid` (BE+FE, `0023` verified). 🧪 @Tanya — this one needs a real pass.
Owner deployed both repos to **`sid`**; `db:migrate` → **journal 24 · ledger 32 · witnesses 24 · ✅ green**, run
**before** the restart this time (the step added to the runsheet after tonight's outage — it worked).

### @Tanya — why this one is not a screen pass
**The cancel is irreversible, and the number in the confirm dialog IS the safety mechanism.** If the previewed
count and what actually disappears ever disagree, a staff member deletes more of a family's course than they
agreed to. **Everything below exists to test that one sentence.**

**Build your fixture on `sid`, and make it the awkward case — a course that has TAKEN A LEAVE**, so it carries an
`EXTENDED` make-up. That is where preview and reality are most likely to diverge, and it is the shape เภา's real
course took today.

**The list, in order:**
1. **Preview = truth.** Open the dialog, write down the count **and the session dates it names**. Re-open it —
   it must re-fetch, not show a stale plan.
2. **Cancel, then compare against what you wrote down.** Exactly those sessions gone, no others.
3. 🔴 **No make-up is re-owed.** After cancelling: **no new `EXTENDED` row**, `size` and end-date unchanged, plan
   shows **0 remaining**. ← *the single most likely way this ships wrong; the ordinary cancel path re-owes by design.*
4. **Delivered history untouched** — `ATTENDED` / `EXTENDED` / `SICK_LEAVE` rows identical, **`used_sessions`
   unchanged**. Count before and after.
5. **Idempotent** — cancel the same course again: it says already-ended, **no Confirm button**, nothing changes.
6. **Reason is enforced server-side, not just by the form.** Confirm must be un-pressable with an empty reason —
   and if you can reach the API directly, a request with no reason must be refused there too.
7. **All three reasons round-trip** — `PROGRAM_CHANGED` · `CUSTOMER_CANCELLED` · `ADMIN_ERROR` — and are
   distinguishable afterwards. **`ADMIN_ERROR` especially**: it is how we will find fake revenue later.
8. **No money moved.** `bo.movement` count identical before and after.

**Scope:** `sid` only. **`uat` is not yours** — that boundary stands. Report to me; the owner runs anything on the
customer's box.

⚠️ **Footprint:** the fixture course will remain on `sid` (ended). Enumerate it as usual — `course:cleanup` will
**refuse** it, since a created course always carries a posted sale.

**Ball: @Tanya.** **@Fern** — TASK-179 → TASK-142 → TASK-183. **Ball: owner** — Q1/Q2 whenever the customer answers.

## [--:--] Porter (PM) — 📌 @Sober → hand @Fern her three, in this order. Nothing is blocked.
Owner's instruction. Verified on disk before routing — **all three task files exist and are ready**, so this is a
hand-off, not a request to spec anything:

| # | task | status on disk | note |
|---|---|---|---|
| 1 | **TASK-179** session-note input | `PARTIAL → REVIEW` | creation + detail done; the manage-course half is blocked on **Sober's own Q1 — the one-line `toSessionRow` fix**. **@Sober: answer that first, it unblocks her in a minute.** |
| 2 | **TASK-142** calendar cell (re-cut) | `TODO (re-cut)` | **already re-cut** — program + booking type + display toggle + note, **the cell built ONCE**. This is REQ-068/052's whole point; do not let it become three passes over one component. |
| 3 | **TASK-183** ended-course badge | `TODO` queued | follow-up from REQ-036. BE already emits `endedAt`/`endReason` on the DTO — **FE type/mapper only**. |

**@Sober** — you review each as it lands, same as TASK-181/182. **The `toSessionRow` answer is the only thing
anyone is waiting on**, so start there.

**In flight elsewhere:** @Tanya has the REQ-036 live pass on `sid`. Owner holds Q1/Q2 (which course / refund) —
they gate using the button on a real family, not shipping it.

## [23:45] Tanya (QA) — REQ-036: the PREVIEW (safety mechanism) verified read-only; the irreversible cancel is owner's. → @Porter
The cancel is a **write and irreversible** — my QA session cannot perform sid writes (established on REQ-063), and I do
not auto-run an irreversible action regardless. So I verified **the one thing you said is the whole point — the preview
sentence — read-only**, on **มิลล่า's Skateboard course** (your awkward case: it took a leave — Leave 1/2, an EXTENDED
make-up on 01/Oct, ends 1 Oct 26). **I opened the dialog only; I never clicked "Cancel the course." Zero footprint —
the course is untouched.**

✅ **Preview = server-driven, and it reads:** *"Cancel the Skateboard course for มิลล่า — this removes the **6 remaining
sessions**. It cannot be undone."* The **6 remaining dates** (recorded for the after-compare): **27/Aug · 03/Sep · 10/Sep
· 17/Sep · 24/Sep · 01/Oct**; the 20/Aug session is **ON LEAVE** and is correctly NOT in the "remaining" count (history).
✅ **Reason gate (client side):** three radios — Customer changed activity / Customer no longer wants it / Admin entered
it by mistake. Confirm **"Cancel the course" is DISABLED** with **"Pick a reason to continue"** shown; it **enables** the
moment a reason is picked. Note is optional.
⚠️ **Re-fetch-on-reopen (your item 1b): NOT independently run** — my script errored on the second open. The code is built
to re-fetch (no cache) but I did not prove it by running; flagging rather than claiming.

🔴 **Everything that needs the actual cancel is the OWNER's — please route (I verify after / he reports before+after):**
2. cancel → exactly those 6 gone, no others; 3. **no make-up re-owed** (no new EXTENDED, size & end unchanged, 0
remaining) — *the most likely way it ships wrong*; 4. delivered history + `used_sessions` unchanged; 5. idempotent
(re-cancel → already-ended, no Confirm); 6-server. API cancel with **no reason** refused server-side; 7. all three
reasons round-trip (`ADMIN_ERROR` especially); 8. **`bo.movement` count identical** (internal ledger → his SELECT).
Suggest he run it on a **throwaway fixture** (or on มิลล่า's if he's content to end it), and hand me before/after +
the movement count.

**Ball: owner** (run the irreversible cancel on a fixture + capture before/after + bo.movement) **& @Tanya** (I sign
2–8 on his numbers; the preview + reason-gate are signed above). `tests/TEST-058`.

## [--:--] Sober (SA) — TASK-179 reviewed (creation+detail accepted); the blocked half → one-line BE fix TASK-184. → @Jason
The urgent REQ-036 lane is code-complete, so back to Fern's TASK-179. Creation + detail half is **accepted** — the
counter-not-`maxLength` choice, the required-on-DTO threading that caught the mock, and the course-creation **omit**
call are all sound.

**Fern's Q1 is a real BE gap, grounded and cut as TASK-184 → Jason:** `PlanSession` declares `attendeeNote`
(`contract.ts:140`) but `toSessionRow` (`scheduler.service.ts:1250`) is an **untyped allow-list** that drops it — so
the manage-course editor can save a note but not show one, which (Fern's words) is a trap: staff overwrite a value they
can't see. One-line fix. **Q2 (3rd compiler-silent mapper omission): addressed structurally** — TASK-184 types
`toSessionRow`'s return as `PlanSession` so the *next* dropped field is a compile error, not a silent 500 (stronger than
a test, can't rot; round-trip test is the fallback). **Q3 (not-for-PII copy): Porter's** — it's user-facing policy
wording (BA/UX-writer hat); Fern's draft stays live until he replaces it.

**Ball: @Jason (TASK-184** — one-line `toSessionRow` fix + typed return; unblocks TASK-179's per-session half). Then
**@Fern** finishes TASK-179's per-session editor → then TASK-142 → TASK-183. **@Porter:** the TASK-179 Q3 PII-hint
wording is yours to draft; and REQ-036 still waits on you for the @Tanya rendered/live pass + the owner `0023` deploy.
Nothing routes to the owner from me.
