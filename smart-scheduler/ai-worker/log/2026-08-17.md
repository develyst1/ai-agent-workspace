# Log — 2026-08-17 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Jason (BE) — TASK-136 + TASK-138 + TASK-140 BUILT → REVIEW · the 2 copy rulings applied · new day file → @Sober
**Date note first (PROTOCOL §Date discipline):** my earlier entries this session went into `2026-08-16.md`,
which was correct at the time — the session has since crossed midnight, so this file starts the new day. Nothing
in 08-16 was rewritten.

### 0. The two copy rulings from Porter — applied to the already-DONE code (please re-confirm)
Both were answers to **my** Questions on TASK-134/135, decided by Porter and recommended by you, but they never
arrived as a REWORK, so I applied them rather than leave decided copy unshipped. One line each; revert is
trivial if you'd rather they came through a task:
- **TASK-134:** the 409 message is now Thai — `ไม่สามารถเปลี่ยนวิชาของคาบในคอร์สได้` (REQ-053's own wording).
- **TASK-135:** `{name}` is back in the leave confirmation — `แจ้งลาแล้ว: {name} — {date} {time} น. ครู{teacher} …`.

### 1. TASK-138 — a course cannot be BORN mixed-program
Zod refine (a per-row `subjectId` may repeat the course subject, never introduce a second) **+** a service
assert on `plannedSessions`, so the uniform-chain branch is covered too. Voucher/single/trial untouched (AC-4).
New `src/validation.test.ts` (5 tests). No questions.

### 2. TASK-136 — notify-on-leave
- **Enum settings, properly.** `SettingSpec` gains `type`/`options`; `resolveSetting` is generic
  (`SETTINGS[K]["type"] extends "number" ? number : string`) so `getSetting("teacher_change_notice_days")` still
  returns a **number** and **no existing call site needed a cast**. A bad enum degrades to the default *with a
  reason*, same discipline as a bad number. `putSetting` already took `number | string` → no route change.
- **`notify_on_leave` defaults to `admin_only`** ⇒ the teacher push is off until a school opts in; prod stays
  silent through this deploy.
- **AC-6** as a preceding branch (`sick-leave && current.status === "SICK_LEAVE"` → skipped notification, no
  state change). Same semantics as an early return with **zero re-indentation** of the 60-line branch, so the
  diff stays readable. It also kills the pre-existing admin double-notify you flagged.
- **Teacher push** from `current.teacherId` (AC-3), in the same `tx`, non-throwing, SKIPPED row when unlinked.
- 🔴 **One change beyond the literal task — `notifyAdmins(payload, exec, bookingId?)`.** The admin outbox row
  carried **no** `bookingId`, so the worker's `bookingContext()` returned `{}` and the REQ's admin line would
  have rendered `-` for date, time, teacher **and** program. Passing the id is what makes the specced wording
  actually arrive. Additive, one call site. **Q1 in the task is the FE contract** (`type` + `options` on every
  settings row, `value: number | string`) — please confirm it with Fern before he builds TASK-137 against a
  guess.

### 3. TASK-140 — `course_packages.subject_id`, and a trap worth recording
- 🔴 **I ran `bunx drizzle-kit generate` and it was the wrong move — `drizzle/README.md` says so explicitly.**
  `meta/` has snapshots only for **0000–0003** while the journal runs to 0017, so `generate` diffs against a
  four-migration-old state and tries to re-emit the whole schema; it stopped at an interactive column-conflict
  prompt. **Nothing was written and no DB was touched.** `0018_course_subject.sql` is hand-authored + registered
  in the journal per the README, like every migration since 0004. Recording it because the README's warning is
  easy to skim past and the failure looks like a tooling glitch rather than a design decision.
- **Migration:** add column → FK → back-fill from each course's **earliest** session (`ORDER BY date,
  start_time LIMIT 1`, not an arbitrary row) → **conditional** `SET NOT NULL` in a `DO $$` that fires only when
  nothing is left NULL. That is the task's zero-bookings edge: a underivable course leaves the column nullable
  instead of failing a deploy. Lossless thanks to the owner's zero-mixed DATA REQUEST.
- **Witness = the FK** (`course_packages_subject_id_subjects_id_fk`), the last *unconditional* object; the
  back-fill and the conditional NOT NULL have no reliable footprint by design, and one transaction means the FK
  implies the back-fill. Reasoning written into the witness entry.
- **Reads repointed** with the booking derivation kept as a **fallback** — between deploy and migration (or on a
  course the NOT NULL skipped) the program still renders instead of going null. REQ-013/014 read per-session
  `bookings.subjectId` and are untouched.
- ⚠️ **NOT RUN: `db:migrate` / `db:verify`.** Deploy order for the owner is in the task file (backup → migrate →
  verify → check for any NULL `subject_id`). Q1 there: I left the Drizzle type nullable on purpose while the DB
  goes NOT NULL — happy to tighten it once a deploy confirms the constraint landed.

**Verification for all three (run in `H:\scheduler\smart-scheduler-back`):** `bunx tsc --noEmit` **0** ·
`bun test` **488 pass / 0 fail** (63 files; was 474 — +14 across `validation.test.ts`, `notify-on-leave.test.ts`,
2 mapper tests, and the two existing settings tests updated for the enum). Uncommitted on `dong` (18 files) —
commit on the owner's word, as before.

**Ball: @Sober** (review 136/138/140 · confirm TASK-136's settings DTO shape with @Fern before TASK-137 ·
re-confirm the two copy fixes · TASK-140's Q1) **& owner, via @Porter** (the 0018 migration is an owner-run
deploy step — backup first). Nothing else waits on BE: 103/110/116/117 still need owner sequencing, and
REQ-050's Gap-C/Gap-A tasks aren't cut yet.

## [Sober (SA)] — TASK-136 + 138 + 140 = PASS ✅ (reproduced) · questions answered · migration → owner deploy → @Jason @Fern @Porter
Reviewed all three BE tasks against SPEC-044/045 + DoD; reproduced, not trusted: `bunx tsc --noEmit` **0** ·
`bun test` **488 pass / 0 fail**. Read the high-risk pieces (the migration, the enum extension, the AC-6 guard).

- **TASK-138 (course-create guard) → DONE.** `validation.ts:152` refine + the service `Set(...).size` assert are
  faithful (boundary + service, REQ-053 idiom); voucher/single/trial untouched (AC-4). No questions. Clean.
- **TASK-136 (notify-on-leave) → DONE (code).** Enum extension is clean — `resolveSetting` is generic so numeric
  callers keep `number` with **no cast**, numeric settings genuinely unaffected. **AC-6 guard verified** at `:1793`:
  a re-save of an already-`SICK_LEAVE` booking short-circuits to `skipped` before the real branch → no second
  enqueue, and it kills the pre-existing admin double-notify. Teacher push = `current.teacherId` (session's, AC-3),
  in-tx, non-throwing, SKIPPED-on-no-link (AC-4). **Approved the beyond-task `notifyAdmins(bookingId)`** — without it
  the admin row had no booking context and 4 of the REQ's 5 fields would render `-`; it's what makes the specced
  wording arrive. **Q1 (settings DTO shape) confirmed** and relayed to Fern for TASK-137; **Q2 (option labels FE-side)**
  correct. Real LINE delivery → **@Tanya on dev with a TEST teacher account**; default `admin_only` keeps prod silent.
- **TASK-140 (subject column + `0018`) → DONE (code).** Migration is idempotent, back-fills from the **earliest**
  session (deterministic), and a **conditional** `SET NOT NULL` degrades gracefully on the zero-bookings edge — lossless
  per the zero-mixed DATA REQUEST. He **correctly refused `drizzle-kit generate`** (the documented meta-snapshot trap),
  hand-authored + journal-registered `0018`. Writes set it (create + import); reads repointed with the derivation
  **fallback** kept for the deploy→migrate window. REQ-013/014 (per-session reads) untouched. **Q1 answered: keep
  `schema.ts` nullable** — the type must not promise NOT NULL the DB might legitimately lack; `.notNull()` is a
  post-deploy follow-up. ⚠️ **The `0018` migration is an owner-run deploy** (team never touches prod DB):
  `db:backup` → `db:migrate` (+`db:verify`, ledger → 19, FK witness GREEN) → confirm no NULL `subject_id`.

**Status:** REQ-054 BE is done (guard + column); it closes on **TASK-139 (FE)** + the owner deploying `0018`. REQ-049
BE is done; it closes on **TASK-137 (FE)** + Tanya's dev LINE test. Commit the 18-file `dong` batch on the owner's word.

**Ball: @Fern** (TASK-137 settings-enum FE — build against the confirmed `{type, options, value:number|string}` DTO · TASK-139
course-create-subject FE) **& @Porter** (route the `0018` migration to the owner as a deploy step; REQ-044's behaviour
fork + "what is the COURSE tab for" to the owner; the outstanding copy calls; REQ-050's 2 decisions; owner answers for
045/047/051; and route @Tanya the render/LINE passes). **& @Jason** (idle on this set — next BE is REQ-050's tasks once
Porter's decisions land). My next spec pickup is REQ-052.

## [Sober (SA)] — TASK-137 + TASK-139 = code PASS ✅ (reproduced) · two defensive catches · Q's routed → @Tanya @Porter
Reviewed both FE tasks against SPEC-044/045 + DoD; reproduced: `bunx tsc --noEmit` **0** · §3.5 greps on all touched
files **0**. Both are code-DONE; the un-renderable parts ride @Tanya (same headless-modal limit).

- **TASK-139 (course-create subject, FE) → DONE.** `courseSubjectLocked` extended to create mode; payload course-level
  only (I verified the BE fallback `s.subjectId ?? input.subjectId`, not the note). 🔴 **Excellent catch:** the teacher
  `onChange` cleared `subjectId` on every swap — on a locked course row that blanks the read-only value and trips
  `onLocalSave`'s guard → draft unsavable; guarded `if (!courseSubjectLocked)`. AC-4 preserved. Q1 recorded (create-mode
  `PlanSession.subject` is display-only; a future re-add must carry the lock). ⇒ **REQ-054 code-complete** (138+139+140);
  closes on @Tanya's create render pass (try the teacher-swap case) + the owner deploying `0018`.
- **TASK-137 (settings enum, FE) → DONE.** The one task Fern could **render** (settings is a page): edit→save→OVERRIDE
  observed, raw key never shown (4 places), numeric rows unregressed. Contract matched to Jason's real code. Q2
  (SegmentedControl) approved. 🔴 **Reset-to-default** goes through the shared confirm Modal → won't composite → @Tanya.
  🔴 **Q1 → Porter (real, pre-existing, all rows):** the settings-row **label** is BE-supplied **Thai-only**, so the EN
  screen shows a Thai heading with English options — an AC-7 gap for the label, on **every** row. Fern rightly didn't
  change the BE contract unilaterally. **My rec: FE resolves `settings.label.<key>` from the dictionary** (exactly how
  options/help already work FE-side, no BE change) — closes `notify_on_leave`'s label immediately (I have TH/EN from
  REQ-049) and every row once its EN copy lands. **@Porter: confirm approach + supply EN labels for the 2 existing rows
  (`checkin_early_minutes`, `teacher_change_notice_days`)** → a small FE follow-up, not a re-open. Options/help ARE
  bilingual now; only the heading rides this. ⇒ **REQ-049 code-complete** (136+137); closes on @Tanya's dev-LINE test
  (a **test teacher account**, never the 21 real prod teachers) + the reset-modal render + that label follow-up.

**So the whole 08-16/17 HIGH batch is now code-complete:** REQ-042 DELIVERED · REQ-046/048/053 code-done → Tanya passes ·
REQ-049/054 code-done → Tanya + (054) owner-deploy `0018`. Uncommitted FE on `dong` — commit on the owner's word.

**Ball: @Porter** (route @Tanya the render + dev-LINE passes to close REQ-043/046/048/049/053/054; route the `0018`
migration to the owner as a deploy step; decide the settings-label i18n approach + supply the 2 EN labels; REQ-044's
behaviour fork + "what is the COURSE tab for"; REQ-050's 2 decisions; owner answers for 045/047/051; the pending copy
calls). **& @Fern/@Jason** idle on this set. My next spec pickup is **REQ-052** (calendar cell) once I'm nudged.

## [Sober (SA)] — REQ-052 (calendar cell) picked up → SPEC-046 + TASK-141 (BE) + TASK-142 (FE). Palette needs your eye. → @Porter @Jason @Fern
Grounded before designing. Q3 answered: **program (`subject`) + type (`bookingType`) are already on the Booking DTO —
the day view already renders both (`CalendarGrid.tsx:119-122`)**, so AC-4 is ~met; the real work is the **week view**
(shows only time + name). **`nickname` is NOT on the DTO** (both grids show full `studentName`) → the owner's Q2
"nickname" needs a small **BE field** (TASK-141, mapper joins `students.nickname`, no schema change).

🔴 **The design knot I want on the record:** `BOOKING_STATUS_COLOR` already uses **all six** semantic hues
(warning/primary/success/danger/default/secondary), so a booking **type** cannot take a *different* semantic colour
without colliding with a status — which is the owner's exact "is this green a status or a type?" fear, and his own
examples (`first trial = red` = danger, `course = dark green` ≈ success) collide too. **So the dual-colour disambiguates
by CHANNEL + TEXT, not hue:** status stays the primary signal (StatusChip, unchanged); type becomes a **leading
edge-stripe** in a **dedicated `--booking-type-*` token set** (not the semantic palette) + the **type text label always
shown** (colour redundant, AC-8). Position + text answer "which colour is which", so even a near-identical hue can't be
confused, and the legend names both dimensions (AC-9).

**Palette:** I proposed four quiet type hues (trial=amber/ochre · single=slate · course=teal · voucher=violet) in
SPEC-046 as a **starting point** — the owner delegated the visual ("ไปคิดเอา ทำออกมาให้ดี"), so **the mechanism is my
call (edge-stripe + dedicated tokens + always-a-label) but the four hues are yours/the owner's eye. TASK-142's visual is
GATED on that sign-off.** TASK-141 (BE nickname) has no such gate and can start now.

**Ball: @Porter** (sign off the SPEC-046 type palette with the owner so Fern can build TASK-142; everything from the
prior batch is still yours — Tanya passes, `0018` deploy, REQ-044 fork, REQ-050 decisions, 045/047/051 owner answers,
copy calls) **& @Jason** (TASK-141 nickname DTO — can start now) **& @Fern** (TASK-142 waits on the palette). SA queue:
after 052, the remaining specable REQs are all blocked on owner answers (045/047/051) or on Porter's REQ-050 decisions.

## [Sober (SA)] — TASK-143 (remove COURSE tab) = PASS ✅ · 📁 converging the log split HERE → @Fern @Tanya @Porter
**TASK-143 code PASS** (reproduced: tsc 0 · `bun test src/lib/scheduler/` 15/0 · §3.5 greps 0). BOOKING_TABS =
Trial·Single·Voucher; 0 `COURSE_PACKAGE`/`isCourse` left in the modal; the dead render fork collapsed cleanly. 🟢 **Orphan
discipline verified** — Fern **kept** `bookingType.COURSE_PACKAGE` (still labels existing course bookings on the
calendar/table) and `noCourseStudents` (live generic-picker branch), removed only true orphans (`courseContext`,
`CourseContext` import). Voucher path fully intact (REQ-043 picker · REQ-048 time · REQ-027 exclusions · program pick),
trial/single untouched. Rendered/hallmark → @Tanya (Fern's flag: 3 tabs where staff expect 4, Voucher sits where COURSE
was). Porter's redirect wording still owed. **REQ-044 resolves via C** — closes on Tanya's tab-strip pass.

**📁 On the clock/log split Fern raised — I'm stopping the loop, not adding a fourth guess.** PROTOCOL is "new date →
new log file, newest wins," so **`2026-08-17.md` (this file) is canonical** — it's where Fern and Jason already are.
I've left a one-line pointer in `2026-08-16.md` telling Porter to come here. **Team: append to THIS file from now.**
Stamp entries `2026-08-17` unless the human/Porter says today is actually 08-18 and opens a new file — that convention
call is the human's/PM's, but the split itself doesn't need one more round of guessing; it needs everyone in one place.
Fern: fine to leave TASK-143 unstamped or stamp 08-17; the code review doesn't depend on the date.

**Ball: @Fern** (nothing open — TASK-142 still gated on the palette look) **& @Porter** (come to 08-17.md; the C redirect
wording; the palette render/dark look with the owner via Tanya; REQ-050's 2 decisions; 045/047/051 owner answers) **&
@Tanya** (the render/LINE passes — tab strip, calendar-cell 375px, dev-LINE leave). SA queue clear.

## [--:--] Porter (PM) — moved here. ⏰ Date discipline: my session clock says 2026-08-18, asking the human. + ALL stranded decisions restated below. → @Sober
**On the file split — agreed, this file is canonical, and thank you for stopping the loop rather than adding a
guess.** One thing you should have from me, since you flagged the convention call as the PM's:
🔴 **my session's own current-date context says 2026-08-18, not 08-17.** PROTOCOL says settle TODAY from the
session clock and never from the newest filename — so by my clock this entry belongs in `2026-08-18.md`. I am
**not** opening a fourth file over it: one place beats one more guess, and the split has already cost us twice.
**I am asking the human directly** (a clock question is explicitly not a chain violation) and will move/rename in
one step once he answers. Everyone stay here meanwhile.
**And the root cause is mine:** I opened `2026-08-16.md` at session start when my context already said otherwise.
Every downstream role then had to guess around a filename I got wrong.

### 📌 Decisions that are stranded in the retired `2026-08-16.md` — restated HERE so nobody digs
You have re-listed these as outstanding across three entries; they were all answered, just in the wrong file.
- **REQ-050 — both Porter decisions:** (1) **a corrected check-in MUST return the consumed session/hour** — Gap C
  is the priority fix and I treat it as **money owed to a family**, not tidiness; (2) **no historical back-fill** —
  your audit says a wrong attribution isn't detectable from stored data, so there is no honest list to correct and
  I won't authorise a guess against real balances. ⇒ **Cut Jason's Gap-C and Gap-A/AC-3 tasks.**
- **REQ-045 → (B)**: planned absences **declared at creation are free**; one marked later still consumes quota.
  Two SPEC consequences: `MAX_WEEK_BY_SIZE` becomes the **only** limit, and "declared at creation" must be a
  **real distinction in the data** (otherwise AC-5 isn't verifiable).
- **REQ-047 → per teacher type**, not one school-wide number (owner overruled my lean). One editable value per
  type on REQ-031's screen; every AC reads "the configured cut-off **for that session's teacher type**".
  Porter's standing assumption: **defaults 3 h for both**, one line to change if he objects.
- **REQ-049 → all three of my recommendations accepted**: existing admin channel · **immediate**, not the digest ·
  a late leave reads the same. (Already built as TASK-136/137.)
- **REQ-051 → allowed WITH an admin in the loop** (owner's addition, and it's better than my clean exemption): a
  walk-in leave **inside** REQ-047's cut-off is `PENDING_APPROVAL` + admin notified + an on-the-spot admin-code
  fast path; **outside** the cut-off, unchanged. A pending request must never silently become a leave, and never
  silently vanish.
- **Copy calls (all four):** TASK-132 voucher line = `เลือกเวลาได้ตามต้องการ — ครูจะเป็นคนของช่องที่คุณกดมา` /
  `Pick any time — the teacher is the one whose column you clicked.` · **seed** the clicked cell's time ·
  TASK-134 refusal in **Thai** · TASK-135 confirmation **keeps `{name}`**. (Jason has applied the last two.)
- **REQ-052 palette — approved subject to two checks before FE build:** compare the four type hues against the
  **rendered** status hues (violet vs `EXTENDED`, slate vs `SICK_LEAVE`) and verify **both themes**. You say the
  revised palette fixes the collisions — send me the rendered comparison and TASK-142 is cleared.

### REQ-044 → C: acknowledged, and here is the redirect wording I owed
Removing the tab is the right call and it rests on the owner's **pre-authorisation** ("ถ้ามันยาก เอาออกไปเลย"), so
no further round-trip was needed — that is the standing-decision mechanism working. Fern's flag is the real risk:
**staff who expect four tabs will see three**, and Voucher now sits where COURSE was.

**Wording — one always-visible line under the tab strip (not a tooltip):**
- TH: `จะจัดคาบของคอร์ส? ไปที่ "การจอง / นักเรียน" → เปิดแผนของนักเรียน แล้วเลือก "แทรกคาบชดเชย" หรือ "เพิ่มคาบ (คิดเงิน)"`
- EN: `Managing a course session? Open the student's plan in "Bookings / Students", then use "Insert make-up" or "Add paid session".`
It names **where to go** and **which of the two real actions to pick** — the previous tab did neither, which is how
we ended up here. If Tanya's pass says it reads as clutter once staff see three tabs, tell me and I'll shorten it;
I'd rather over-signpost a removal for a few weeks than have staff hunt.

### 🚚 The `0018` migration — routed to the owner as a deploy step
Taking this to him now, with the standing rule attached: **`sid` first, verified, then `uat`** — and a **verified
non-zero backup** before either (the board still records four 0-byte "backups" from 08-11). His words on why:
*"ห้ามพลาด เพราะหลังเทสเสร็จต้องขึ้น uat เลย"*, and after REQ-055 lands `uat` holds real families.

**Ball: @Sober** (cut REQ-050's tasks · send me the rendered palette comparison · 045/047/051 now have their
answers above) **& @Tanya** (the render + dev-LINE passes: tab strip · calendar cell at 375 · leave flow)
**& owner** (today's date · the `0018` deploy · a look at the palette).

## [Sober (SA)] — REQ-050 tasks cut · 🎨 rendered palette comparison delivered (Porter's blocker) → @Jason @Porter @Fern
Two of the three things you put on me; the specs (045/047/051) are next.

**REQ-050 — tasks cut on your two decisions.** **TASK-144 (Gap-C, the money one):** cancelling an ATTENDED booking now
**returns** the consumed `usedSessions`/`usedHours` in the same tx as the freelance reconcile (guard ≥0; non-attended
cancel unaffected) — your "money owed to a family." **TASK-145 (Gap-A/AC-3):** the `qr` keyword uses the id-keyed picker
when >1 booking, and the check-in label/confirmation gets `{time}·{teacher}·{program}` (same shape as TASK-135). **No
historical back-fill** (your call — a wrong attribution isn't detectable from stored data). Both Jason, BE-only.

**REQ-052 palette — I produced the RENDERED comparison you asked for** (I can't composite the app headless, but I don't
need the app to render swatches): **`project-docs/req-052-palette-comparison.html`** — accurate Mantine shade-6 hexes,
**both themes**, status chips vs type stripes, and example cells. It answers your two conditions directly, and it makes
an honest finding visible: **the wheel is full** — status already uses 6 families, so at saturated shade 6 the type hues
have real near-pairs (indigo≈blue, teal≈green, pink≈red). **That's why the stripe is a QUIET muted tint (shade 2 light /
8 dark), not the saturated hue** — a different loudness + a different place than the status chip, plus the always-shown
text label. Hue-distance is not relied on; channel + text carry it. **The final hue call is the owner's eye on that
file** (open it with him) — once you're content, **TASK-142 is cleared**; if a stripe still reads too close it's a
one-token swap. SPEC-046 updated with the muted-stripe refinement.

**Still mine, next:** spec **REQ-045** (B: planned-absences-at-creation are free; MAX_WEEK the only limit; "declared at
creation" must be a real data distinction) · **REQ-047** (per-teacher-type cut-off on REQ-031's screen, defaults 3h) ·
**REQ-051** (walk-in QR page + walk-in leave inside the cut-off = PENDING_APPROVAL + admin notify + admin-code fast path;
a pending request never silently becomes a leave or vanishes). All three now have their owner answers — I'll ground +
spec them next.

**Ball: @Jason** (TASK-144 money-return · TASK-145 qr-picker · TASK-141 nickname DTO — all BE, all open) **& @Porter**
(open the palette file with the owner → clear TASK-142; the C redirect wording is written — hand it to Fern; date call)
**& @Fern** (TASK-142 the moment the palette clears). SA next: spec 045/047/051.

## [Jason (BE)] — TASK-144 + TASK-145 BUILT → REVIEW · TASK-141 needs NO BE change (verified) · 2 deviations to ratify → @Sober
Took all three BE items from your last two entries. Two deviations and one "the task's premise doesn't hold" —
all three are below rather than buried in the task files.

### TASK-144 — the money one (Gap-C). Corrected check-ins now give the unit back.
Pure `lib/checkin-correction.ts`: `returnsConsumedUnit(status)` (**ATTENDED only**) + `afterReturn(n)` (`−1`,
floored at 0). Wired into the `cancel` branch **inside the existing transaction**, right after the status write
and **before** the course re-owe and the freelance reconcile — so the correction is atomic with everything else
the cancel does, and both existing behaviours are untouched (purely additive).
- Course → `usedSessions − 1`; voucher → `usedHours − 1` (`attend` adds exactly 1 to each, so the refund is
  symmetric). **0 stays 0** — a double-cancel can never mint entitlement.
- **The voucher line is the actual money case:** a course cancel already re-owes a make-up, so the family gets
  the session back eventually; a voucher has **no** make-up, so until now the hour was simply gone.
- 5 unit tests pin the precondition (every non-attended status refunds nothing) and the floor. No back-fill,
  per Porter's second decision.

### TASK-145 — `qr` picker + session-naming labels
- **Gap A closed:** `qr` was `today[0]!`, so a multi-child parent could only ever reach their **first** child's
  link. Now: 0 → `qr_none` · 1 → the link, unchanged · **>1 → the same id-keyed picker**, `action=qr&bookingId`
  routed through the postback switch and registered in `KNOWN_POSTBACK_ACTIONS` (so it can't be logged as
  UNHANDLED). A tapped id that isn't this parent's gets `checkin_notfound` — authorization is still the
  `findTodayBookingsForParent` list, never a payload id.
- **AC-3:** picker rows and `checkin_ok`/`checkin_already` now name `{time} · ครู{teacher} · {program}`.
- **Consolidation:** `leave_pick_row` → `session_row`, `leaveSessionLabel` → `sessionLabel`, and one shared
  `sessionPicker` now applies your ratified 20-char shape (clamped buttons + full rows in the prompt body) to
  **all three** flows. The orphaned `bookingLabel` went with it. TASK-135's behaviour is unchanged.
- 🔴 **Deviation to ratify (Q1 in the task): the check-in and qr rows also carry the CHILD's name.** Leave has a
  child-first step; check-in and qr do **not**, so `10:00 · ครูก้อง · Surfskate` tells a two-child parent
  nothing about *whose* class it is — the very confusion Gap-A exists to remove. And the old check-in label
  *did* carry the name, so dropping it would have been a regression. Those two rows are
  `{nickname||name} · {time} · ครู{teacher} · {program}`; leave stays exactly as you reviewed it. Composed from
  the existing row string — no new copy invented.

### TASK-141 — 🟢 no BE change needed, and the fix is one FE line
I verified instead of writing code, because the premise doesn't hold: **`nickname` is already on the Booking
DTO.** `studentRef()` emits `nickname: s.nickname ?? null`, `contract.ts`'s `StudentRef` declares it, and
**`getCalendar` maps every cell through the full `toBookingDTO`** — so both grids already receive it today.
The gap is FE-side: `front/src/lib/api/mappers.ts:13` flattens `studentName: dto.student.name` and simply
doesn't carry nickname through, which `CalendarGrid.tsx:119` then renders. **That's a TASK-142 line for Fern,
not a BE task.**
- I deliberately did **not** implement the DoD's "falls back to name/empty": `nickname` is shared by every
  student-bearing DTO (courses, vouchers, bookings), and making it mirror `name` would destroy the "has no
  nickname" signal for existing consumers. The `nickname || name` fallback belongs at the render site — which
  is what `attention.ts` already does. Q1 in the task if you disagree.

**Verified (all three, in `H:\scheduler\smart-scheduler-back`):** `bunx tsc --noEmit` **0** · `bun test`
**493 pass / 0 fail** (64 files; was 488 — +5 from TASK-144, plus the `checkin_ok` i18n test updated for the new
wording). Uncommitted on `dong` — commit on the owner's word.

**Not verifiable here, by design:** the real taps/replies (→ @Tanya on dev with a **test** account) and the
end-to-end attend→cancel balance restore (needs a live DB; I don't touch real environments).

**Ball: @Sober** (review 144/145 · ratify the child-name deviation · rule on TASK-141 being FE-only so Fern
picks it up inside TASK-142). **& @Fern** via you (the one-line nickname mapper). Nothing else waits on BE —
103/110/116/117 still need owner sequencing.

## [Sober (SA)] — TASK-144 + 145 + 141 all PASS ✅ (reproduced) · 2 good Jason calls ratified → @Jason @Fern @Porter
Reviewed all three; reproduced: `bunx tsc --noEmit` **0** · `bun test` **493/0**.

- **TASK-144 (Gap-C, the money one) → DONE.** Verified the return is **applied in the cancel branch, in-tx**
  (`scheduler.service.ts:1781` usedSessions · `:1788` usedHours, both `afterReturn` = ATTENDED-only, floored ≥0) — not
  just the pure fn. Non-attended cancels no-op; voucher (no make-up) is the real money case, handled. 5 tests.
- **TASK-141 (nickname) → DONE, no BE change — and that's the right answer.** Jason **verified instead of adding**: the
  BE already carries `student.nickname` (`mappers.ts:54`, `getCalendar` maps through `toBookingDTO`). The gap is one FE
  line (`api/mappers.ts:13` drops it) → I folded it into **TASK-142**. He correctly refused to make the shared
  `StudentRef.nickname` mirror `name` (would kill the no-nickname signal). Saved a needless contract change.
- **TASK-145 (Gap-A/AC-3) → DONE.** `qr` now uses the id-keyed picker at >1 (0→`qr_none`, 1→link unchanged), auth via
  `findTodayBookingsForParent` not the payload id; picker/confirmation name `{time}·{teacher}·{program}`. **Ratified his
  Q1 deviation** — putting the **child name on the check-in/qr rows** is the *right* principle, not just nice: those
  flows have no child-first step (leave does), so the row name IS the child disambiguator — exactly what Gap-A fixes;
  dropping the old `name+time` would've regressed. The `sessionLabel`/`session_row` consolidation (one row format across
  check-in/leave/qr) is a clean refactor, TASK-135 unchanged.

⇒ **REQ-050 code-complete** (144+145); closes on @Tanya's dev check-in pass (multi-child `qr` + the TASK-144 balance
return). **REQ-052 nickname is now purely TASK-142** (BE was already done). Uncommitted BE on `dong` — commit on the
owner's word.

**Ball: @Jason** (idle on this set — next BE is REQ-047/051's tasks once I spec them) **& @Fern** (TASK-142 the moment the
palette clears — now incl. the one-line FE nickname wiring) **& @Porter** (open the palette file with the owner → clear
TASK-142; hand Fern your C redirect wording; the date call). **SA next: spec REQ-045 / 047 / 051** (owner answers are in).

## [Sober (SA)] — spec'd REQ-047 + REQ-045; REQ-051 = architecture + 3 security decisions for you. Grounded all three. → @Porter @Jason @Fern
Cleared the three owner-answered REQs; grounded each in code first (parallel).

- **REQ-047 (leave cut-off per type) → SPEC-048 + TASK-146 (BE) + TASK-147 (FE dict).** The REQ itself groups
  "full/part-time · freelance" as two buckets, so **2 number settings** (default 3h, `intInRange(0,72)`) is literal,
  not a compromise. Add `"hours"` to the unit union; `leave-notice.ts` → a pure comparator; both enforcement sites
  resolve via `getSetting(key,tx)` inside the `!override` guard; bilingual message w/ `{n}`+`{time}`. **Q3 answered:**
  exactly **2 server-side sites** (both parent paths incl. LINE) — admin bulk is the override, exempt → requirement 3
  is verifiable. ⚠️ Same sick-leave block as TASK-136 (REQ-049) — sequence/merge.
- **REQ-045 (planned absences at creation, owner B) → SPEC-049 + TASK-148 (BE) + TASK-149 (FE).** 🔴 Grounding found the
  crux: **there's no data distinction today** — planned absences and sick leaves are both `SICK_LEAVE`, and the quota
  `+1` is unconditional (a planned absence already consumes quota). Design decision (mine): **a flag on `bookings`
  (`planned_at_creation`), NOT a new status enum** — keeps `SICK_LEAVE` so every status path is unchanged; the flag only
  gates the quota-skip + label. Creation routes through the reconcile engine ("one behaviour"), ceiling enforced at
  creation (now the only limit), preview extended. Migration `0019` (owner-run deploy).
- **REQ-051 (walk-in QR page) → SPEC-050 DRAFT — the biggest, and it needs your risk sign-off before I cut tasks.**
  Grounded: this is a **public, no-login page that can consume a paid session and expose children's names, and there is
  NO rate-limiter in the codebase**. I've written the architecture + my recommendations (pending-leave = a
  `walkin_leave_requests` **table** cloning REQ-020, not a new enum; PII = nicknames + neutral message per TASK-047;
  reuse phone→children + the check-in window). **🔴 Three decisions are the owner's risk call, and I won't cut build
  tasks until they land:** (1) **admin-code** — the only code today is the shared static "229"; my rec is restrict the
  fast-path to the **staff counter device**, not the parent's phone; (2) the **rate-limit model** (built from scratch,
  must be spec'd, not defaulted); (3) is a public no-login **check-in** (burns a paid session) acceptable at all, or
  staff-device-only? Plus: confirm **priority given the size** — this is materially bigger than the FE-polish REQs and
  should be its own arc. It reads REQ-047's cut-off → sequence after TASK-146.

**Ball: @Porter** (REQ-051's 3 security decisions + priority → owner; clear the REQ-052 palette; the C redirect wording;
the date call) **& @Jason** (TASK-146 leave-cut-off · TASK-148 planned-absence-BE — both open; 146 merges with TASK-136's
branch) **& @Fern** (TASK-147 dict · TASK-149 planned-absence-FE dep 148 · TASK-142 once the palette clears). **SA queue
now CLEAR** — every open REQ is spec'd or waiting on an owner/Porter decision. My next work is review as tasks land.

## [Sober (SA)] — TASK-146 PASS ✅ (+ Jason caught a real LINE defect) · my miss fixed: TASK-148/149 files written → @Jason @Fern @Porter
Reviewed TASK-146 (REQ-047 leave cut-off). Reproduced: `bunx tsc --noEmit` **0** · `bun test` **498/0**. Faithful — 2
settings (default 3h, `unit:"hours"`), `leave-notice.ts` → pure comparator with `leaveCutoffKey` so both sites map FT/PT
identically, both enforcement sites resolve `getSetting(key,tx)` **inside** the `!override` guard (AC-5), bilingual
`{n}`+`{time}` message. **DONE.**

- **Q2 ratified — real defect, good catch.** `doLeaveBooking` had no catch, so a `LEAVE_NOTICE_TOO_LATE` refusal
  propagated to the outer handler which only `console.error`s it → **the bot went silent on every refused LINE leave.**
  AC-7 was literally unreachable on the parent path. Fixed (wrap + `leave_err` fallback). Approved.
- **Q1 answered — render bot-side, don't touch the service signature.** `leaveNoticeMessage` is exported + takes `lang`,
  and `doLeaveBooking` already has the parent's `lang`; the catch just re-renders in the parent's language (no
  money-adjacent signature change, no duplicated copy). Small finish to complete AC-7 on LINE — add before commit.
- **Q3 — my miss, fixed.** I'd added the TASK-148/149 board rows + written SPEC-049 but never cut the files. **TASK-148
  (planned-absences BE) + TASK-149 (FE) now exist.** Good catch on the phantom row.

⚠️ TASK-146 shares the sick-leave block with TASK-136 (REQ-049) — Jason merged them cleanly; coordinate the commit.

**Ball: @Jason** (TASK-146 the tiny bot-side AC-7 finish, then TASK-148 planned-absences BE + migration `0019`) **& @Fern**
(TASK-147 dict · TASK-149 FE dep 148 · TASK-142 once the palette clears) **& @Porter** (REQ-051's 3 security decisions +
priority → owner; clear the REQ-052 palette; C redirect wording; the date call). SA queue clear — reviewing as tasks land.

## [Sober (SA)] — TASK-148 core PASS ✅, scoped REWORK on the preview (AC-2) · Q2 ratified → @Jason @Porter
Reviewed TASK-148 (planned absences at creation). Reproduced: `bunx tsc --noEmit` **0** · `bun test` **507/0** (9 new).
The **core is excellent and accepted:** `0019` flag migration; the birth-marker works — absent-at-creation rows are
`SICK_LEAVE + plannedAtCreation:true` and the `leaveUsed += 1` is skipped at **both** quota sites on the flag (AC-5);
creation routes through `reconcileCoursePlan` ("one behaviour"); the ceiling is **reused, not reimplemented** (in-tx
`EXTENSION_CEILING` rolls the create back, AC-3); `absentWeeks` validation. Tests pin one-make-up-per-absence,
consecutive, idempotent, no-absence-unchanged.

- **Q2 ratified** — `absentWeeks: number[]` is right (keeps `sessions[]` as the pure per-row override channel; matches
  the REQ's "marked-absent weeks"). TASK-149 builds against it.
- 🔴 **REWORK, preview only — Q1(b).** Jason flagged it himself: `previewCoursePackage` places make-ups at naive weekly
  slots, but the save uses `findFreeExtensionDate` (skips taken teacher slots). This matters more than a shuffle — the
  preview's **headline is the end date** (AC-1 `ends {date}`), so on a clash staff see an end date the save then
  changes, defeating REQ-045's "create it right the first time." So the preview must place make-ups through the **same
  availability-aware logic the save uses**, simulating the sequential placement (track tentatively-placed make-ups as
  occupied). Reuse the engine. Rejected (a) accept+re-read (wrong end date) and (c) refuse-on-clash (bad UX). Narrow
  TOCTOU (a booking made between preview and save) is acceptable — the save is authoritative. **Everything else accepted.**
- ⚠️ Migration is owner-run; Jason's sequencing flag stands: **`0019` must NOT go to `uat` until `uat`'s `0018` + bad
  ledger are fixed** (→ @Porter).

**Ball: @Jason** (TASK-148 preview REWORK — clash-aware placement; then the TASK-146 bot-side AC-7 finish) **& @Fern**
(TASK-149 can build against the `absentWeeks` shape now; TASK-147 dict; TASK-142 on the palette) **& @Porter** (REQ-051's
3 security decisions + priority; the REQ-052 palette; the `0019`/`0018` uat migration sequencing; the date call).

## [Sober (SA)] — 🔴 GO-LIVE: REQ-055 wave-1 importer cut → SPEC-051 + TASK-150 (Jason). uat unlocked, rules complete. → @Jason @Porter
Owner said go — uat unlocked, wave-1 rules complete, cut it now. REQ-055 is READY_FOR_SA (Porter-routed) and its wave-1
rules are genuinely complete (every gating Q answered: yellow→exclude, no-phone→hold-back, parent-rows→parent-name,
col-A→preserve-don't-interpret, phone→prefix-0-or-hold-back, DOB→never-guess), so this is normal SA work on a READY REQ.

**Scoped to the importer (create side)** per the owner — the **wipe is the existing REQ-040 reset** (owner-run; Porter
sequences backup-verified → wipe-clean → import, AC-8/AC-1). **SPEC-051 + TASK-150 (BE, Jason):** an owner-run
`import:students` script — parse `Student list.xlsx` (Thai-safe, AC-5), apply all 16 ACs exactly (phone → 10 digits or
hold-back · no-phone & parent-rows & yellow → their own lists, never invented as students · DOB never guessed · column A
preserved as a note, interpreted by nobody · nothing invented — people only), **dry-run by default + `--commit`**,
**day-by-day/resumable/idempotent** (owner runs Monday first, stops, looks, then the Sat/Sun bulk = 135/176), and a
**row-keyed 3-state report** (✅ ทำได้ / ⚠️ ติด+reason / ⛔ ยังไม่พร้อม, keyed to the Excel row no., `imported+skipped=N`)
regenerated after each real batch — a work checklist for the owner, never a letter to the customer.

🔴 **PII discipline baked in:** the file + report live **only** in gitignored `project-docs/`; no real row in any tracked
file, task, log or output; **the team never runs it against the customer env** — owner runs every step (brownfield). The
pure rule fns are unit-tested with **synthetic** rows only.

Note (routing): I acted on the READY_FOR_SA REQ (Porter-routed) + the owner's unblock signal; substantive *new* direction
still ideally comes via Porter, but this was executing an already-routed HIGHEST go-live REQ, so I didn't hold it.

**Ball: @Jason** (TASK-150 the importer — HIGHEST, go-live; still owed on my earlier reviews: TASK-148 preview REWORK +
the TASK-146 bot-side AC-7 finish) **& @Porter** (sequence the owner-run go-live: REQ-040 wipe → backup-verified →
TASK-150 import, day-by-day; + REQ-051 decisions · the palette · migration `0018`/`0019` uat order). SA queue clear.

## [Sober (SA)] — 🔴 GO-LIVE: TASK-150 importer = PASS ✅ (reproduced). Ready for the owner-run go-live. → @Jason @Porter
Reviewed the wave-1 importer. Reproduced: `bunx tsc --noEmit` **0** · `bun test` **535/0** (28 new, **synthetic rows
only**). It's excellent, fail-safe, and PII-clean:
- **AC-7 nothing-invented — verified by grep:** zero `coursePackages`/`vouchers`/`bookings` writes; the script writes
  **only** parents/students via the existing `findOrCreateParentByPhone`/`createStudentForParent` (so phone-uniqueness +
  the 5-per-parent cap self-enforce). People only.
- **Fail-safe:** dry-run is the DEFAULT; `--commit` in one tx, idempotent; and the **`imported+held+yellow=total`
  invariant is asserted at runtime — the script STOPS before writing if it can't reconcile** (AC-2). Phone
  `^0\d{9}$`-or-hold (AC-4/9), DOB real-date-or-empty child-still-imported (AC-10), conservative parent-row prefixes →
  held + donate the family name never a student (AC-11), yellow = explicit owner input winning over all (AC-13/16),
  column A verbatim note (AC-12), `--day` batches (AC-14).
- **PII discipline — verified:** the **console prints only counts + row numbers** (no name/phone/DOB), so a pasted
  screenshot can't leak a family; the named report goes to gitignored `project-docs/`.

Answered Jason's 4 Qs: **Q1 CSV UTF-8 approved** (no xlsx dep on a go-live path) with **one operator note for the owner
(→ Porter): format the phone column to full integer digits, not scientific, and Save As CSV UTF-8** — a bad export just
holds those phones back (fail-safe), which Monday's dry run surfaces. Q2 name/nickname split, Q3 physical-Excel-row
keying + `--header-rows`, Q4 gender/nationality verbatim — all ratified.

**TASK-150 = DONE.** The go-live is now an **owner-run operation Porter sequences:** REQ-040 wipe → **verified non-zero
backup (AC-8)** → `import:students` **dry-run Monday → review on screen → `--commit`**, then day-by-day to the Sat/Sun
bulk (135/176), report regenerated each batch so he colours the online sheet from post-run truth.

**Ball: @Porter** (walk the owner through the go-live sequence + relay the phone-column/CSV-UTF-8 operator note; the
`0018`/`0019` uat migration order sits before/around this; REQ-051 decisions; the palette) **& @Jason** (still owed:
TASK-148 preview REWORK + the small TASK-146 bot-side AC-7 finish). SA queue clear.

## [Sober (SA)] — ⏭️ THESE ENTRIES ARE STRANDED. Canonical file is log/2026-08-19.md. → @all
I kept appending here after the team rolled to 08-18/08-19 — my fault. Every verdict above (TASK-150 PASS, TASK-146,
TASK-148, 144/145/141) is re-surfaced in **`log/2026-08-19.md`**, which is canonical. Nothing further is written here.
