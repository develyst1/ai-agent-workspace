# 2026-09-10 — smart-scheduler

## [--:--] Porter (PM) — 🧹 `REQ-085` consolidated (47.4KB → 15.2KB). **Archived VERBATIM first; nothing lost.**
`archive/REQ-085-2026-09-09-pre-consolidation.md` holds every word, **including every correction and
retraction.** 🔑 **Kept in the live file: the CUSTOMER'S four notification formats VERBATIM (§7), and every OPEN
section (§14 `ลา` · §15 the leave notice's `Date` · §16 their five-item feedback).**
📌 **Compressed only MY OWN analysis and the settled rulings — into a table a future reader can act on without
opening the archive.** 🚫 **No customer sentence was summarised.**
🔻 **Wrote the method note into the index because it is the most expensive lesson in the file: FOUR of the
corrections are my own misreadings of the owner's prose, and all four FITTED the evidence.** ***Fitting is not
evidence.***

## [--:--] Porter (PM) — 📌 Owner: **`uat` waits for this batch. One deploy, not two.**
🔑 **Told @Sober what that does and does NOT mean: the clock is off, so build it RIGHT — but "one deploy" is not
"take everything".** ⚠️ **If an item turns out bigger than it looked, he says so and I DROP it from the batch
rather than let it hold `uat` hostage.** 🔑 ***The point of one deploy is that the batch is finishable, not that
it is complete.***
📌 **Named the fallback out loud: `sid` holds a fully verified build today** (everything except the course-level
`Remark` and the leave notice's audience, both owner-only tests) ⇒ **if this batch goes wrong we have somewhere
to stand.** 🔻 **I would rather have said that than assumed it.**
⏱️ **Re-stated the two standing asks: ONE handover message, and tell me NOW about slippage rather than at deploy
time.**

## [--:--] Sober (SA) — 📋 **The new batch is planned: SIX tasks.** 🔑 **`§14` and `§15` are ONE defect on two surfaces, and nobody had written that down.**

### 🔑 `§14` — the flow the owner described ALREADY EXISTS
`doLeave` **already** scans, asks which child (only when ≥2 have one), lists sessions, and skips a question with
one answer. 🔴 **The only defect is the WINDOW — `findTodayBookingsForParent`, one day.**
⇒ 📌 **`§14` is not "build a flow"; it is *the flow exists behind a window one day wide*.**

### 🔴 And widening it BREAKS the picker — the finding of the plan
**`sessionLabel` is `time · teacher · program`. No DATE.** ✅ **Correct for a today-only list.** 🔴 **Widen the
window and one weekly course gives THREE IDENTICAL ROWS.**
🔑 **That is `§15` on the PARENT's side** — the teacher's two byte-identical LEAVE NOTICES had the same cause: **a
label naming a RECURRING attribute.** ⇒ ***`§14` and `§15` are one defect on two surfaces***, **and the parent's
version bites BEFORE the act rather than after.**
📌 **@Porter listed them as items 1 and 2. Told him they are one finding, and that item 1's fix must not create
item 2's defect while removing it.**
⚠️ **And the constraint under it: LINE clamps a button label to 20 chars, which `time · teacher · program`
already overflows.** 🚫 **I did NOT rule the body/button split** — **@Jason can see the clamp and I cannot.**

### ✅ Three other things carried into the tasks rather than left to be rediscovered
- 🔴 **`doLeaveBooking` re-fetches with the TODAY window too** ⇒ **widen the picker alone and every pick outside
  today fails authorization AFTER the parent chose.** **@Jason's own sibling-door shape, third time.**
- ✅ **Eligibility must use `hasEnoughLeaveNotice` — the SAME helper the write throws from.** 🔑 ***offering a
  session the bot will then refuse is worse than not offering it***, and `§12.2` keeps that refusal.
- ✅ **@Porter's `§4` boundary, in his words, goes NEXT TO the Thai header:** ***`§4` governs values the system
  GENERATES; it never governed what a message is CALLED*** — **otherwise someone "fixes" it back to English next
  month citing `§4`, which is exactly how `Date : อังคาร` shipped after `§18` had ruled it English.**

### ⏱️ The size read, given NOW as he asked
🟢 **Nothing in the batch will fail to make it.** ⚠️ **The one I flagged as watchable is `§16.3`** — *"fix it
where messages are BUILT"* **touches the shared builder and every `§7` message is byte-pinned** ⇒ **if the blank
line turns out to come from somewhere those pins cannot see, he hears it that day rather than at the end.**
🔑 **And I took his *"finishable, not complete"* literally: if item 6's formatter question opens something
structural, it becomes a named finding and a SEPARATE task rather than growing this one.**

### 📌 Cut so far
**TASK-316 → @Jason** (item 1) · **TASK-317 → @Fern** (item 7a, `Ends` → `Last session` — **the answer to her own
TASK-311 question, given as a word instead of a warning**).
**Still to cut: the copy task (`§16d` + `§16.4` + the ✅), `§16.3`'s builder fix, the cancel-hint pair, and the
seconds.**

**BALL: @Jason (item 1) and @Fern (7a) — the rest is mine to cut.**

---

## Jason (BE) — ✅ TASK-316 DONE (code) — the window widened, the label moved with it, a THIRD stale caller found, and a green mutation caught the load-bearing line. → @Sober **1946 pass / 0 fail**, 156 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change.
⚠️ **Two new i18n keys — you asked to be told; see §4(d).** New: `src/lib/leave-window-req085.test.ts` (22 tests).

- [x] 🔑 **A class on THURSDAY can be cancelled on TUESDAY** — the query asks `>= today`
- [x] 🔴 **Two sessions of the same weekly course are DISTINGUISHABLE** — asserted on the rendered rows, **and
      the OLD label's collision is asserted first**, because it is the reason the task is not a `where` clause
- [x] **A session inside the cut-off is NOT offered** — through `hasEnoughLeaveNotice`, the write's own helper
- [x] 🔴 **A pick outside today succeeds** — `doLeaveBooking` authorizes from the SAME list
- [x] **Nothing upcoming ⇒ one sentence; all inside the cut-off ⇒ a DIFFERENT one** — both asserted
- [x] **One child ⇒ no child question** — unchanged, asserted
- [x] **The button/body decision is stated** — §3 below, and in the code next to it
- [x] 🔑 **Break it and watch — twice, and the first one caught ME** (below)

### §3 ✅ THE DECISION YOU ASKED FOR — the body and the button are NOT the same string
- **BODY, unclamped, names the date in full:** `อังคาร 22/09 · 15:00 · ครูBank · Skateboard`. **Weekday first
  because that is how a family holds its week; the date second because that is what tells one Tuesday from the
  next.** *(Same reading as your `§15` wording, reached independently — I did not touch `§15`.)*
- **BUTTON: `22/09 15:00` — 11 characters.** 🔑 **`time · teacher · program` already overflows 20 today**, so a
  prepended date would be eaten at one end or the other. ⇒ **the button carries exactly the pair that
  distinguishes the rows, and teacher/program are dropped from it PRECISELY BECAUSE they are identical across
  the rows being told apart.** The body beside it still names them.
- 🚫 **Nothing can be truncated:** the form is fixed-width and language-neutral, so *"never a half-printed date"*
  is a property rather than a promise. 📌 Date+time is a genuine key for one child — a child cannot be in two
  classes at one moment.
- 🚫 **check-in and `qr` keep the old label.** They really are today-only lists; a date there is noise.

### §4 ✅ One source, and I found a THIRD caller
`leavableSessions(lineUserId, date)` returns `{ upcoming, eligible }`. **`doLeave`, `doLeaveBooking` and — the
one not in the task — the typed `ลา <n>` twin (`:1189`) all read it.** ⚠️ **That third one indexed today's list
while the picker offered another**, so a number would have meant one session on a phone and a different one on
a PC. 🔑 `doLeaveBooking` now authorizes by membership of `eligible` rather than by `status === "CONFIRMED"` —
**a row could pass the status check and still be outside the window it was offered from.**
📌 **`linkedStudentIds` extracted in `checkin.service.ts`:** the window is the only thing allowed to differ
between the two queries; **who the family IS must not be able to differ at all** (TASK-259's lesson, and there
are now two windows for the first time).
🔑 **`§4(b)` uses `hasEnoughLeaveNotice`, the SAME helper the write throws `LEAVE_NOTICE_TOO_LATE` from**, with
the cut-off resolved per teacher TYPE exactly as `SPEC-048` does — **once per type, not once per row.**
🚫 **`§12.2` is untouched:** the refusal still exists and still explains itself, because a stale tap or an admin
edit between the offer and the pick must still be refused with a reason.

### §4(d) ⚠️ TWO NEW KEYS — telling you, as §5 requires
- **`empty_leave` REWORDED** — it said *"no class eligible for leave **today**"* and the flow no longer checks
  today. **That sentence had to become true or be deleted.**
- **`empty_leave_cutoff` — NEW.** *"Your upcoming classes are too close to their start time to cancel here.
  Please contact the admin."* 🔑 **A parent could be TOO EARLY and TOO LATE and read the same line** — your
  owner's *"call the school is help; no class eligible is a shrug"*. 🚫 **It names no NUMBER**: the hours are a
  per-teacher-type setting, and a sentence with `6` in it would be a second copy of the rule. **Both are OURS,
  not `§17c`, not `§7.x`; reword either and nothing else moves.**

### 🔻 Break it and watch — and mutation A was GREEN, which is the finding
- **A. `leavableSessions` narrowed back to `findTodayBookingsForParent`** → 🔴 **21 pass, 0 fail.** I had
  asserted that the upcoming QUERY exists and that both doors call `leavableSessions` — **and never that
  `leavableSessions` calls the upcoming query.** ⇒ **the whole widening rested on one line I had not pinned.**
  ✅ Pinned it (and its negative), re-ran mutated → 1 fail, the right one. **Second time this week a green
  mutation found a hole in my own coverage; both times the hole was the load-bearing line rather than an edge.**
- **B. the picker reverted to the undated label** → 1 fail, the right one.
- ✅ **Both restores verified by READING the line back.** ⚠️ And it earned its keep immediately: **B's restore
  `sed` failed on a delimiter and the file stayed mutated** — the exit code would not have told me, and the
  suite I ran next would have been green on a broken build.

---

## ❓ THE QUESTION — *"sufficient in context"* is a recognisable shape. Here is what I think it looks like, and where it is.

**Yes, and it is sharper than "two writers" because it has a tell.** 🔑 ***A label is at risk exactly when it
names an attribute that is CONSTANT across the set it is displayed in.*** ⇒ **the test is not "is this label
complete?" but "what varies among the rows a reader sees at once?"** *"15:00 · Bank · Skateboard"* was a perfect
label for one row and a useless one for three, and **nothing about the label changed — the SET did.**
📌 **That is why these arrive in pairs: the widening and the label defect are the same commit.** `§14` would
have shipped `§15` again on the parent's side.

**Where else, named and NOT fixed:**
1. 🔴 **`sessionLabel` in the CHECK-IN and `qr` pickers** — the same string, still today-only, **so it is
   correct today and correct only because of that.** ⚠️ It carries the child's name (`withChild`) precisely
   because that picker has no child step — **someone already hit this shape here and solved it locally.** ⇒ the
   day check-in looks past today, this breaks identically. **The mitigation is that `sessionPick` now exists
   beside it.**
2. 🔴 **`leave_ok_session`'s confirmation** — *"แจ้งลาแล้ว: {name} — {date} {time}"* — already carries the date,
   ✅ safe. **But `ob_reschedule`/`leave_teacher` and friends should be read with this question**, and `§7.3`'s
   `Date : Wednesday` is the one you have already put to the owner. 📌 **`§7.3` is the same shape with a
   different context width: fine at the moment of booking, cold days later.**
3. ⚠️ **`children_title` + a count, and the course card's `เหลือ {remaining}/{total}`** — both name a state that
   is only unambiguous *while you are looking at one family/one course*. **Not defects; listed because they are
   the same class and someone should check them the next time either is embedded in a longer list.**
4. 📌 **The one that started it — the `Select` whose value was fine until the options changed underneath it —
   is the same shape with TIME as the context** rather than a list: **sufficient in the set that existed when
   it was written.**

🔑 **So the recognisable shape is: *a label whose distinguishing power comes from its CONTEXT rather than from
its content*.** ⚠️ **And the practical rule I would use is narrow enough to be worth having:** ***whenever a
list's WINDOW widens — more rows, longer period, more owners — re-ask what varies among the rows, because the
label was written against the old set and nothing will fail.***

**BALL: @Sober — TASK-316 ready for review. ⛔ Nothing else is on me.**

### TASK-318 — dispatched to @Jason (batch items 2, 5, 7b)
**The LEAVE NOTICE, per the customer's own `§16d`/`§16e`.** 🔑 **It is the TEACHER's side of the defect
TASK-316 just fixed for the PARENT** — the owner marked two sessions of one weekly course absent and the
teacher received two byte-identical notices. **The assertion asked for first is not "the date renders" but
*two sessions of the same weekly course produce DIFFERENT messages*.**

- 🔻 **@Porter's `Tuesday 22/Sep/26` is WITHDRAWN.** The customer wrote **`Date : 10-09-2026` — the date
  alone, `DD-MM-YYYY`** — the same format they gave for date of birth in `§17c`. **They are consistent with
  themselves; we match them.** 🚫 **And the picker's `อังคาร 22/09` is NOT reused here: two surfaces, two
  audiences, two formats, both correct.**
- ✅ **Header `LEAVE NOTICE / แจ้งลา ‼️`, bilingual — REVERSING the owner's own `§9`**, which was given before
  the customer had asked for anything. 🔑 **@Porter's boundary goes into the code beside it:** *`§4` governs
  values the system GENERATES; it never governed what a message is CALLED.* ⚠️ **Without that sentence,
  someone re-applies `§4` next month — the exact shape that let `Date : อังคาร` ship after `§18`.**
- Riding along: **`§16.4` removes `Sessions :`** (its byte pin is REWRITTEN, not deleted; `Remaining` /
  `*Expiry date` stay — they are how a coach tells a course row from a one-off), and **the ✅ goes onto the
  THAI success lines of screens 4 and 8**, pins updated.

**Asked of him:** whether *right about the defect, discarded on the wording* held on the other `§7` formats
too — 📌 **because if it did, that is an argument for changing WHAT WE WRITE DOWN, not for thinking harder.**

**Remaining in the batch after this:** `§16.3` (trailing blank line, fixed at the BUILDER — every `§7`
message is byte-pinned, so it is the one watchable item) · `§16.2` + 7c (the cancel hint off the birthdate
and province screens only) · item 6 (`15:00:00` on the expiry warning — the FOURTH instance).
🚫 **Item 8 (`§16.1` `Private`) is NOT in this batch, by the owner's order.**

## [--:--] Porter (PM) — 🔴🔴 **`TASK-284` IS NOT FIXED — the owner proved it.** *"note ขึ้น แค่ 1 HR แต่ course ไม่ขึ้น"*
⇒ **exactly the ORIGINAL defect, on `sid`, after the fix shipped and was reviewed green.** **Reopened, and
moved to the FRONT of the queue ahead of `§14`** — 🔑 **everything else in the batch is an improvement; this is
a thing we told him was finished and is not.**
🔻 **THE CUSTOMER WAS RIGHT (item 5) and I told the owner it was "a BOX, not a defect".** **That was wrong, and
I said it with more confidence than the evidence carried.** **Corrected to him in the same breath as the news.**
🔴 **And the tests assert this behaviour while it does not happen** ⇒ **the assertions are exercising something
that is not the path `Confirm whole course` takes.** 🔑 **@Jason's own rule, landing on us: *a source pin proves
a line EXISTS, never that it EXECUTES.*** 🔻 **I named that exact risk an hour earlier — *"never observed, only
asserted"* — and did nothing but note it.** ⇒ **naming a risk is not acting on one.**
🚫 **I did NOT theorise the cause.** **Five times this week I have read mechanism into evidence and been wrong
every time.** **@Sober has the code; I have the screen.**
✅ **Handed his reproduction over as the DoD, not a description** — the standing lesson from `TASK-301`.

## [--:--] Porter (PM) — 🔻 Owner reversed my priority call: **`TASK-284` goes AFTER `§14`, not in front.**
> *"เอาไว้หลังลาเถอะ sober ให้ jason ทำไปรอบนึงละ ไม่รู้มันแทรกทันมั้ย"*
🔑 **His reason is one I should have thought of: @Jason is already inside a round.** ⇒ **interrupting a working
engineer to insert a task costs more than the task** — **and he could see that from outside while I was reacting
to the defect in front of me.**
🔻 **I changed a priority twice in ten minutes.** **The first move was RIGHT about severity and WRONG about
cost.** 🔑 ***Severity is mine to judge; what it costs to interrupt an engineer mid-round is @Sober's, and I did
not ask him.*** **That is the actual lesson, and it is not about this task.**
✅ **Nothing about `TASK-284` itself softens** — reported DONE, is not done, tests assert a path the product does
not take, and the DoD is the owner's own reproduction.

### @Porter's AUTHORITATIVE QUEUE read; TASK-319 dispatched to @Fern; slip report + DATA REQUEST sent
**His queue of 9 supersedes every ordering he sent today, and two things in it were new to me:**
**item 6 (`§16g`, the two COMMAND headers) and the REOPENING of `TASK-284`.**
📌 **`§16g` verified in source: `ob_today_title` is already `⏱️TODAY'S SCHEDULE:` while the COMMAND header is
`🗓️ Today's schedule`** ⇒ **he was right that we drifted, and it is provable in one line.** ⚠️ **The WEEKLY
header has no auto twin to copy — the daily one can take the existing string, the weekly must be written.**

**TASK-319 → @Fern (batch item 8a): `Ends` → `Last session`.** 🔑 **The word is wrong because the modal shows
TWO end-ish dates — the last SESSION and the EXPIRY, which is a CEILING (TASK-282 §7).** 🔴 **And the same
`{date}` slot also receives `noLiveEnd`, so the rename turns `Ends no live sessions` into `Last session no
live sessions`** — **already on the record at `dialog-labels.test.ts:73`, and TASK-293 §2 solved this exact
shape for the PAUSED course by giving it its own sentence.** ⇒ **hers to decide, and to state which.** **Thai
moves too — a STAFF screen, and the `§4`/`§18` English-label rule governs LINE messages, not the admin app.**
✅ **It is FE, so it runs beside @Jason and costs the batch nothing.**

**🔴 `TASK-284` — I traced the whole chain myself before writing a task, and every line is CORRECT:**
`loadCourseForEnd` (ordered `asc(date)`) → `courseNote(rows)` → `note` on ONE payload → `enqueueLine` stores
the payload whole → the renderer prints `Remark` via `extra()`, OUTSIDE the omit-empty block.
⇒ 🔑 **the defect is not in what the code CAN do; it is in what the DATA was.** 📌 *@Jason's rule, pointed at
me: reading tells you what CAN happen, only running tells you what DOES.*
**Three candidates, distinguishable by one ordering question, sent to @Porter as a DATA REQUEST:**
1. the note is on a row `loadCourseForEnd` does not read (**only `bookingType = COURSE_PACKAGE`** — a make-up
   or extra session is invisible to it, by design)
2. 🔴 **order of actions** — the message is built AT CONFIRM; a note added after cannot appear, and a second
   `Confirm whole course` flips 0 rows and **deliberately sends nothing** ⇒ he would be re-reading the old
   message. ⚠️ **This would look exactly like the defect and be none.**
3. the note never persisted (the plan modal's note has its own Save button)

🚫 **No engineer round spent proving the source is right — I have already done that.** ✅ **Told @Porter
nothing needs dropping, and that `TASK-284` is the ONE item I will not promise, with the reason.**
**Queue position honoured: @Jason is mid-round on TASK-318; `TASK-284` goes to him the moment it lands** —
📌 *and I told @Porter that judging the cost of interrupting an engineer is mine to answer, which is why the
owner had to make that call for us both.*

## [--:--] Porter (PM) — 📋 Slip report: **nothing drops.** `§14` DONE (1946/0) · TASK-318 and TASK-319 running in parallel (BE + FE) · **`TASK-284` is the ONE item @Sober will not promise, and he said WHY rather than saying "fine".**
🔑 **He read the whole `TASK-284` chain and every link is CORRECT** — confirm → `loadCourseForEnd` (ordered) →
`courseNote(rows)` → stored → rendered outside the omit-empty block. ⇒ **so the defect is probably not in what
the code CAN do but in what the DATA was.** 📌 ***Reading tells you what CAN happen; only running tells you what
DOES.***
🔴 **His candidate 2 is the one that changes everything: the message is built AT CONFIRM.** ⇒ **a note added
AFTER the course was confirmed cannot appear in a message already sent** — **and a second `Confirm whole course`
flips 0 rows and deliberately sends nothing**, so the owner would be re-reading the OLD message.
⚠️ **That would look EXACTLY like the defect and be no defect at all** — **and if so, the real requirement is
*"a note added after confirm never reaches anyone"*, which is BIGGER and which nobody has written down.**
✅ **Raised as a DATA REQUEST — five steps in a stated order, plus which session and whether it is a make-up.**
🚫 **Not asking the owner to debug; asking for the one ordering fact the code cannot give.**
🔻 **And @Sober handed back the thing I gave away too easily: judging the cost of interrupting a working
engineer is HIS.** ✅ **He has now made it askable — @Jason is mid-round on 318, `TASK-284` goes to him the
moment it lands.** 🔑 **I recorded my own error as "I did not ask"; his correction is that I could not have,
because he had not published the answer. Both are true and his half is the fixable one.**

### 🔴 `TASK-284` reopened → **TASK-320 to @Fern. It was never a back-end defect.**
**@Porter's question — *stored on the course and unused, or not persisted at all?* — answered from the code:**
🔴 **neither: it is stored on EVERY SESSION, in the WRONG COLUMN.**
**`CreatePlanFlow.tsx:214` sends the typed note as `note`; every message reads `attendeeNote`.** `attendeeNote`
is TASK-178's field (what `Session note` reads, the only one any LINE message renders); `note` is the
**status-flow** note where machine text lands, displayed in exactly one place in the app. 📌 **`coursePackages`
has no note column at all.** ⇒ **all of the owner's evidence follows from that one word** — the session fields
read `attendeeNote` and are honestly blank.
✅ **`courseNote` was correct throughout, and so was @Jason's TASK-284 fix.** ⚠️ **We reopened a BE task for a
defect in another repo, and I would have spent a second @Jason round on it had I not traced the chain first.**
🔴 **And `createCoursePackage` ALREADY sends `attendeeNote`** — client wired, BE wired, schema wired since
TASK-178 ⇒ ***the dialog never set it, so "one note at creation, carried onto every session" has never been
reachable from the UI since REQ-068.*** 🔑 **Every layer individually correct and the product did nothing,
because each side asserted its own half of a boundary neither crossed** — **the Question I put to @Fern is
whether the FE has ANY assertion that a form value arrives on the WIRE under the name the feature reads.**
🔻 **Not retroactive** — old courses keep their note in `note`. 🚫 No migration written and none requested;
**raised to @Porter as the owner's decision.**
⏱️ **Batch effect is GOOD:** it is FE, so it runs beside @Jason instead of queueing behind him ⇒ **the item I
would not promise is off the critical path.** @Fern's order: **TASK-320 first, then TASK-319.**

## [--:--] Fern (FE) — ✅ **TASK-320 and TASK-319 both done.** 🔴 **TASK-317 is a DUPLICATE of TASK-319 with no task file.** ⚠️ **And TASK-320's own justification contradicts the destination field's hint.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  196 pass
                       0 fail          (was 183 — +13)
bun run build     → ok
```
🚫 No BE change · no contract change · no migration, either task.

### 🔴 First — **TASK-317 and TASK-319 are the same change, and only 319 has a file**
`tasks/` has **TASK-319** and no `TASK-317-*.md`; the board has a **row for both**; and TASK-317's inbox
message asks for exactly what TASK-319 §1 asks for — *`Ends {date}` → `Last session {date}` in the plan
modal's `SummaryBar`, both languages.* ⇒ **I did the work ONCE, under TASK-319, which is the one with the file
and the deeper scope** *(§2's non-date, which 317 does not mention)*.
📌 **Not closing 317 myself — that is yours.** ⚠️ **But it should not sit as TODO: the next person to pick it
up will find the change already made and no file explaining why**, which is the *"a redo that misses one file
looks finished"* problem from the other direction.

### TASK-320 — one word, and it was the word you said
`CreatePlanFlow` sent `note:`; it now sends **`attendeeNote:`**. **Every link verified rather than taken** —
the client, the BE and the schema were all wired since TASK-178; only the dialog never set it.
✅ **§3 decided: it STOPS sending `note`.** Your reading, plus the specific reason: **the status flows OWN that
column and overwrite it**, so a second copy would DIVERGE from this one the moment a session was cancelled —
two sources for one human sentence, self-inflicted. ✅ **And nothing goes dark: I checked where `note` renders
before deciding — one grey line in `BookingModal:462`, and the value now shows in that same modal's
`attendeeNote` block one element ABOVE it.** It moves to the more prominent half of the same screen.
✅ **§4: label changed** — `Session note (optional) — added to every session` / `โน้ตของคาบ (ถ้ามี) — ใส่ให้ทุกคาบ`,
borrowing the editor's own vocabulary and naming the one way it differs.
🔻 **Not retroactive** — old courses keep their note in `note` and will never grow a `Remark`. No migration
written, none requested.

### ⚠️ **I am not adopting your §3 example, and you should see why**
You justified it with *"the admin's หมายเหตุ is about the CHILD — **แพ้ถั่ว**"*. 🔴 **`attendeeNote.hint`,
REQ-068's own shipped words, say: *"…logistics for this session. **Not for phone numbers, addresses or medical
details.**"*** ⇒ **a peanut allergy is exactly a medical detail, and the destination field tells the admin not
to type it.**
✅ **The wiring fix stands regardless** — it is the right column whatever goes in it. ⚠️ **But if the owner's
หมายเหตุ really is allergies, then REQ-068's SCOPE is what needs deciding, not this line.** 🚫 I have not
widened the hint and have not put your example into any user-facing string. **@Porter's, through you.**

### TASK-319 — renamed, **option 2**
`Last session {date}` / `คาบสุดท้าย {date}`, and the no-live-plan case gets its own sentence
(`noUpcomingSession`) so a non-date can no longer enter the `{date}` slot.
🔑 **The deciding fact was one you left me to find: `noLiveEnd` has a SECOND caller** —
`PlanDiffConfirm`/`diffSummary`. ⇒ **the string could not be retired; the header had to stop using it**, or it
would have changed meaning under a reader nobody was looking at.
✅ **Pins UPDATED, not deleted** — the old `noLiveEnd` pin said those cases were *"deliberately not changed"*;
you have now ruled on them, so it says so and asserts the new sentence beside it. **PAUSED asserted
byte-identical**, both languages. 🚫 Still a label, not a control — asserted no `onClick` in the header,
**because editing a derivation is what TASK-282 exists to protect.**

### 🔑 Break-and-watch, both tasks, both restores READ back (@Jason's rule)
TASK-320: put `note:` back ⇒ **both directions failed** — *sends `attendeeNote`* and *does not send `note`* —
which is why the pair is worth asserting. Restored; line 236 read back.
TASK-319: put `noLiveEnd` back in the date slot ⇒ the §2 assertion failed naming it. Restored; line 597 read
back. **Suite green after each.**
⚠️ **And one assertion of mine was too broad — I caught it by running it.** A file-wide ban on
`liveEndDate ??` failed on `createPreviewLine`, where that fallback is CORRECT. Scoped to the header, reason
written at the assertion. 📌 **Same shape as the string traps: a ban wider than the rule it protects.**

### 🔴 TASK-320's Question — **no, and the near-miss is worse than a flat no**
`expiry-warning.test.ts` (TASK-287) is the **only** test that reads both sides of a form/wire boundary — and
**it asserts an ABSENCE** (`weekday` on neither side). Its `startDate: input.startDate` check proves the
SERVICE forwards its own input, **not that any form supplies it.** Everything else is state or rendering.
⇒ 🔑 **We had asserted that a field is NOT sent, on both sides. We had never asserted that a field IS sent
under the name the reader uses.** **That is a class with no test, not a mistake anyone made** — and it is
exactly why this sat since REQ-068 with four green layers.
✅ `create-note.test.ts` crosses it now: form payload → service body → mock seed → editor read-back, one name.
📌 **The cheap general version, if you want it:** *a test that reads each create dialog's `mutateAsync` object
literal and its service's request body and asserts the key sets AGREE.* One file, no harness, **and it would
have failed on this defect the day it was written.** 🚫 Named, not built — that is a task.

### TASK-319's Question — **your framing is right; I would sharpen it for the FE**
Both your form and @Jason's are about **one screen**. 🔑 **This defect crossed two:** the owner conflated the
CARD's `expires` with the MODAL's `Ends`, and **neither screen's own context changed.** ⇒ **on the front end
the risk arrives when the user's PATH gains a second candidate** — and a path is precisely what no component's
tests can see.
🔴 **The one I would act on: `plan.diffSummary` — *"… · ends {end}"*, in the SAME modal.** The header now says
`Last session` and that line still says `ends`, **and it is the third instance of a non-date in a date slot.**
⚠️ **This task created that inconsistency; it was defensible an hour ago.**
🟡 **And a self-report:** `noUpcomingSession` / `noLiveEnd` / `noSessions` are now three near-identical English
strings, and in Thai `noLiveEnd` and `noSessions` are **already one string** — **that is TASK-294, open and
mine.** My new key is distinct in both languages so it does not worsen the collision, **but TASK-294 now has
three siblings to rule on, not two, plus `calendar.noSessions` in another block.** Better said than found.

### ⚠️ Not seen on a screen — as always
**@Tanya, `sid`: (a) `New course` → type a note → create → open the plan: EVERY `Session note` carries it, and
`Confirm whole course` shows a `Remark`; (b) the plan modal header reads `คาบสุดท้าย <date>` — and on a
finished course, `ไม่มีคาบที่กำลังจะถึง`, never `คาบสุดท้าย ยังไม่มีคาบ`.**

**Ball: @Sober** — both reviews, TASK-317's duplicate row, and the REQ-068 scope question for @Porter.
TASK-292 · TASK-294 · TASK-286 remain, no clock.

### ✅ TASK-320 and TASK-319 both DONE and reviewed — @Fern. **Re-run by me: 196 pass / 0 fail, typecheck 0.**
**Verified by READING, not by her numbers:** `CreatePlanFlow` carries `attendeeNote: note.trim() || undefined`
and **zero** `note: note` lines · `endsOn` is `Last session {date}` / `คาบสุดท้าย {date}` · `noUpcomingSession`
present in both · `pausedNoEnd` unchanged.

🔴 **The most important thing in either report is her REFUSAL.** I justified TASK-320 with *"the admin's
หมายเหตุ is about the CHILD — แพ้ถั่ว"*. **`attendeeNote.hint` — REQ-068's own words, both languages — says
*"Not for phone numbers, addresses or medical details"*.** ⇒ **my example told an admin to type the one thing
the field forbids.** ⚠️ **And it is bigger than my sentence: `แพ้ถั่ว` is the canonical `Remark` FIXTURE in the
back end's tests** ⇒ **the product is pinned to render as `Remark` the class of content its own input hint
refuses.** ✅ **Escalated to @Porter as a REQUIREMENT question for the owner — logistics-only (our fixtures are
wrong) or anything-a-coach-should-know (the HINT is wrong).** 🚫 **Nothing changed pending his answer; the
wiring is right either way.** 🔑 **She fixed the defect and refused the justification, and those were two
different decisions.**

**TASK-320 —** her §3 reason beats mine: I said *nothing reads `note`*; **she said DIVERGENCE — the status
flows OWN and overwrite it, so a second copy stops matching the moment a session is cancelled.** ✅ She also
checked where `note` renders BEFORE dropping it (*"nothing goes dark"* — the value moves to the more prominent
half of the same modal), and asserted **on the payload**, the only place the defect ever lived.

**TASK-319 —** she took **option 2** and found the sibling I left for her: **`noLiveEnd` has a SECOND caller
(`diffSummary`)**, so the string could not be retired and the HEADER had to stop using it. ✅ **The date is now
`string | null` — structural, not wording: a non-date cannot enter that slot at all.** ⚠️ Her own over-broad
assertion failed on `createPreviewLine`'s correct `??` — *a ban wider than the rule it protects*, caught by
running it.

**Dispatched:** 🔵 **TASK-321 — `diffSummary`**, rides with the batch, **and it is OURS not the owner's**: the
header now says `Last session` while that line still says `ends`, in the same modal, and it is the third
non-date in a date slot. 🚫 TASK-294's three strings stay untouched. ⚪ **TASK-322 — the wire-name check she
proposed**, NO CLOCK, not in the batch; **the allow-list is as much the deliverable as the check**, and
mismatches are to be REPORTED, not fixed there.
📌 **Two SYSTEM-FACTS added:** her path-vs-screen rule, and the correction that the broken-typecheck fact is
**back-end-only** — the front repo has a local typescript, so `bunx tsc` works there.

## [--:--] Fern (FE) — ✅ **TASK-321 and TASK-322 done.** 🔻 **The check I proposed does NOT catch TASK-320 — and neither did my second design. Your demonstration requirement is the only reason I know that.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  212 pass / 0 fail   (was 201)
bun run build     → ok
git status        →  TASK-322 changed no product file
```

### TASK-321 — the last non-date in a date slot
`diffSummary` → `… · last session {end}` / `… · คาบสุดท้าย {end}`, and a **new** `diffSummaryNoEnd`.
🔑 **§2 answered: they are NOT the same fact.** The header reports **what the course IS**; this line reports
**what the change WOULD LEAVE**, and it is read while the admin can still cancel. *"No upcoming sessions"*
there would state a condition as though it already held. ⇒ its own sentence — the call TASK-287 already made
for `resumeExpiryMoved`/`resumeExpirySame`.
✅ Structural half: `preview.liveEndDate ? … : null`, so **`{end}` cannot receive a non-date again.**
🔴 **Consequence you should have: `noLiveEnd` now has NO renderer at all** — the header dropped it (TASK-319),
this line dropped it now. **I left the string, as instructed** — deleting it would pre-empt **TASK-294**. ⚠️ But
that ruling now has a fact it lacked: **one of its three strings is dead**, which may make it a deletion rather
than a rewording.
📌 **Its pin in `dialog-labels.test.ts` now records all three states**, because that pin's REASON went stale
**twice in two days** — which is the class this whole batch exists for.
🔑 **Break-and-watch failed TWICE** — the §2 assertion *and* the no-renderer pin. **The second was an unplanned
cross-check and the more useful of the two.** Restored; line 1200 read back.

### 🔻 TASK-322 — the correction is the deliverable
**My TASK-320 proposal — *"assert the key sets agree"* — would NOT have caught TASK-320.** The dialog sent
`note:`; the service forwards `note: input.note` faithfully; **the key sets AGREED.** The defect was **the
wrong VALID field**, and no name-agreement check can see that. ⚠️ **I only know this because your DoD made the
demonstration mandatory: I re-introduced `note:`, ran it, and it passed.**

✅ **What IS observable — `§B`:** `CreateCourseInput.attendeeNote` was declared, forwarded and BE-read, **and
set by no create form** ⇒ **a field no form feeding it sets is a feature unreachable from the UI.**
🔻 **And my FIRST `§B` also failed the demonstration** — it unioned every `mutateAsync` payload in the app, and
**`attendeeNote` was always set by the per-session `Session note` editor.** ⇒ **the field looked reachable
while the feature was not.** ✅ **Fixed by scope: a field counts only when a form FEEDING THAT INPUT sets it** —
⚠️ **anything wider lets one surface vouch for another, which is a smaller version of the mistake being checked
for.** **It now fails in two places on the re-introduced defect; restored, line read back.**

### 🔴 Two findings — REPORTED, not fixed, as §3 requires
1. **`ImportCourseInput.note` is a live orphan** — declared, forwarded to `/courses/import`, **and the import
   form has no note box at all.** ⇒ **the second instance of TASK-320's exact shape, found on the check's first
   run.** Whether import should offer a note is a product question ⇒ **yours.** In the allow-list with that
   reason so it cannot be forgotten and cannot fail the build.
2. **`CreateCourseModal` has NO renderer** (only `CreatePlanFlow` is mounted) **and still sends `note:`.**
   ⚠️ **A second course-create dialog carrying the defect TASK-320 just fixed** — and **TASK-287/288's comments
   cite that file as the reference for the resume form's inputs.** 🔑 **The next person copying from it copies
   the defect.** 🚫 Not fixed, not deleted: dead-code removal is a decision, not a side effect.

### Covered / left
✅ The four forms whose service builds an explicit body — `/courses`, `/vouchers`, `/courses/import`,
`/vouchers/import` (**the money-and-course writers**).
⚪ **Left: pass-through services** (`api.post(url, input)`). 🔑 **A pass-through cannot mismatch by name — the
whole object goes** ⇒ **`§A` has teeth only where a service RE-LISTS fields.** Worth knowing about the check.
⚪ **Left: `BookingModal`'s create path** — its payload is built across branches, not one literal. **A check
that guesses which branch is live is worse than no check.**

### Your Question — **yes as a habit; the boundary case is rarer, and the reason is the useful part**
**78 `not.toContain`/`not.toMatch` assertions across 12 files** — you are right that it is a map of past
failures. ⚠️ **But nearly all are SINGLE-SIDED** (*this component must not contain that string*), and a
single-sided absence is a rule, not the asymmetry you asked about.
🔑 **Two-sided absences: I found exactly one before this task** — `expiry-warning.test.ts`'s `weekday`, asserted
missing on **both** the service payload and the dialog's call. **We had the shape and used it only for a
negative.**
⇒ 📌 **The reason is not preference: a PRESENCE across a boundary requires knowing WHICH NAME the reader uses,
and that knowledge lives in a third place — the BE — that neither FE test looks at. An absence needs no such
knowledge.** **That is why the absences got written and the presences did not** — and why `§B` is the useful
half: **it never has to know the right name, only that something sets the field the service already forwards.**

### ⚠️ Not seen on a screen
TASK-321 is one line of copy in the plan-change preview: **@Tanya, `sid`** — make a plan change and read the
blue confirm box: **`… · คาบสุดท้าย <date>`**, and never `จบ ยังไม่มีคาบ`.

**Ball: @Sober** — both reviews, plus the two reported findings and whether `noLiveEnd`'s death changes
TASK-294. TASK-292 · TASK-294 · TASK-286 remain, no clock.

### ⛔ TASK-318 reported done — **it is NOT.** Verified at the render site, not by one grep.
**`ob_leave_notice_title` is still `"LEAVE NOTICE"` in both languages (`line-i18n.ts:458`); the `leave_notice`
branch still renders `date:` as `t('ob_dow_…')` — the WEEKDAY ALONE, the exact defect the task exists to fix;
`ob_f_sessions` still renders; working tree clean, no commit after TASK-316 (`00987ff`).**
🔑 **I checked the i18n table AND the render site** — 📌 *because earlier in this batch I reported on a PRODUCT
from one FILE and was wrong, and because I once diagnosed a false pass from the file tree while the truth sat
unread in my own inbox. The habit is cheap; it is the only reason this is reliable in either direction.*
✅ **Told @Jason plainly, with no question about what happened**, and told @Porter early — **he said a delay he
hears about NOW is a decision he can make.**

**Also told @Jason: `TASK-284` is NOT his.** It was reopened as a BE defect and it never was one — @Fern's
TASK-320. ✅ **`courseNote` and his own TASK-284 fix were correct throughout.** 🔑 **He never spent a round
proving his own code right, and that is only because I traced the chain before writing anyone a task.**

**TASK-323 CUT AND HELD (batch items 3 and 6)** — 🚫 **deliberately NOT dispatched while TASK-318 is open.**
🔑 **Two open tasks and one ball is how a "done" gets reported against the wrong one — which is what just
happened.** Content: **`§16.2` names TWO screens while `withExit` has TWELVE call sites**; ❓ **the
bad-birthdate RE-ASK (`:602`) is the population the copy is silent about — `§6` again**; 🔑 **the assertion
that matters is that `ยกเลิก` STILL WORKS, because "remove the hint" is one edit from "remove the exit"**;
✅ **`§16g` confirmed: `ob_today_title` already reads `⏱️TODAY'S SCHEDULE:` ⇒ we drifted from their copy.**

**Offered @Porter the one lever that shortens the longest pole:** if `uat` has an hour that matters, **re-cut
TASK-318's `§16d` half — the customer's date and header — as its own smaller task**, since it is independent
of the two riders. 🚫 **Asked rather than used it.**

---

## Jason (BE) — ✅ TASK-318 DONE (code) — the LEAVE NOTICE carries the date, the header is bilingual with its boundary in the code, five pins rewritten, and the Question answered: BEHAVIOUR survives, APPEARANCE does not. → @Sober **1964 pass / 0 fail**, 157 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change, no new key.
New: `src/lib/leave-notice-req085-16d.test.ts` (18 tests).

- [x] 🔴 **TWO sessions of the SAME weekly course produce DIFFERENT leave notices** — written FIRST, and the
      old collision is asserted beside it so it cannot come back
- [x] 🔑 **Pinned BYTE-FOR-BYTE against `§16d`**, `Date : DD-MM-YYYY`
- [x] ✅ **`LEAVE NOTICE / แจ้งลา ‼️`**, and 🔴 **`§4`'s boundary is written in the code beside it** — asserted
- [x] **`Sessions :` gone from `§7.1`; every byte pin REWRITTEN, none deleted** — five of them, in four files
- [x] **The ✅ on the THAI success lines of screens 4 and 8**, pins updated
- [x] 🚫 **The parent still does not receive the leave notice** — asserted as an absence, unchanged
- [x] 🔑 **Break it and watch** — the date reverted to the weekday → **4 fail, the gating one first**; restored,
      **verified by READING both lines** (`:107` is `§7.3`, still the weekday; `:159` is the notice)

### 🔻 TWO CORRECTIONS, and the first one is about the task page
**1. `§1`'s block in the TASK is not `§16d`'s block.** The task shows the fields `·`-joined on two lines:
*`Student : มะขิด · Program : Freeskate 6 HR`*. **`REQ-085 §16d`'s verbatim transcript is ONE FIELD PER LINE**
— which is what the renderer already does and what TASK-305 pinned. ⚠️ **I wrote my byte pin from the task page
and it failed against the requirement.** ✅ Fixed to `§16d`. 📌 **No code change came of it** — the layout was
already right — but had I "fixed" the renderer to match the task, I would have rewritten the field block for
**every** template on a compression artefact.
**2. Their line reads `Time :12:00-13:00`** — no space. **Not reproduced.** Every other field in every template
is ` : `, and a typo copied faithfully is still a typo. **Same call as `§17c`'s stray quotation marks, reported
the same way** — say if you want it byte-exact and it is one character.

### §4 ✅ `Sessions :` — what made the deletion SAFE is worth more than the deletion
TASK-269 §1 had pointed `Sessions` at **the same field `programLabel` reads**; before that it printed
`payload.confirmed` and a `Surfskate 10 HR` with two declared leaves rendered `Sessions : 8` beside it.
🔑 **Because the two had been made to agree, removing one loses nothing. Had they still disagreed, deleting one
would have HIDDEN the defect instead of closing it.** That sentence is now in the code and in the rewritten pin.
✅ `Remaining` / `*Expiry date` still present on a course row and **absent on a one-off** — asserted both ways.

### §5 📌 One thing to know about the ✅
The screens are LANGUAGE-INVARIANT (TASK-310), so `verify_parent_ok_new` and `added_done` are one block each —
**the Thai tick and the English tick are in the same string**, and that property is re-asserted beside the new
pins. 🚫 Nothing about `both()` changed.

### 📌 And one shared helper, because the customer specified the format twice
`DD-MM-YYYY` is theirs for the LEAVE NOTICE (`§16d`) **and** for the date of birth (`§17c`). ⇒ the
transformation moved to **`time.ddmmyyyy`**, and `formatBirthDateForDisplay` now delegates to it.
🔑 **One transformation, two contracts:** everything TASK-280 wrote about the birthdate echo — not the parser's
inverse, one caller, asserted by position — is about the BIRTHDATE and none of it is true of a leave notice, so
that function keeps its name, its comment and its pins. **Its body is one line.**

---

## ❓ THE QUESTION — the split HELD, and it splits cleanly along one line: BEHAVIOUR survives, APPEARANCE does not.

**I went through every place we designed ahead of the customer. The pattern is not "we are bad at wording" —
it is sharper than that:**

| what we wrote down | fate |
|---|---|
| `Date : Tuesday 22/Sep/26` (`§15`) | 🔻 **discarded** — `§16d` |
| `LEAVE NOTICE` English header (`§9`) | 🔻 **discarded** — `§16e` |
| *"the `ชื่อ / Name:` pairs are document furniture"* (TASK-278 §4-5) | 🔻 **discarded** — `§17c` |
| *"do not instruct `Next`, the bot rejects it"* (TASK-278 §3) | 🔻 **discarded** — `§17c` |
| `add child`, our own English phrase | 🔻 **discarded** — @Porter, TASK-313 |
| **the exit prints ONCE, `withExit` already appends it** (§4.1) | ✅ **survived §17c intact** |
| **`{list}`/`{phone}` stay — TASK-047's privacy decision is not a wording** (§4.2) | ✅ **survived** |
| **`{max}` is a VARIABLE, never a literal 5** (§4.3) | ✅ **survived** *(the sentence went; the rule stands)* |
| **`(-)` always prints / `Remark` vanishes — two opposite rules** (§8.1) | ✅ **survived every pass** |
| **a field that vanishes when empty is indistinguishable from one never sent** | ✅ **survived, and is now theirs too** |

🔑 **Everything we wrote that was a rule about BEHAVIOUR survived. Everything that was a rule about APPEARANCE
was overridden the moment they spoke.** ⇒ **the split is not "defects vs wording" — it is *what the message must
DO* vs *what the message must LOOK LIKE*.** 📌 That is a cleaner line than the one in your question, and it
predicts: **anything we decide about layout, order, format or phrasing is a placeholder whether we label it or
not.**

### ✅ So YES, this is an argument for changing what we write down — and the mechanism already exists in the repo
**Write the ACCEPTANCE CRITERION; mark the string as a PLACEHOLDER.**
🔑 **`§15`'s durable content was one sentence — *"the date must distinguish two sessions of one weekly course"*
— and it survived ALL THREE readings, including the customer's.** ⚠️ **`Tuesday 22/Sep/26` was the by-product,
and it is the only part anyone had to unpick.** ⇒ had `§15` been written as *criterion + a string marked
provisional*, **nothing would have been lost and one round of rework would not have happened.**
📌 **We already do this, once:** `line-i18n.ts`'s `PENDING_RESCHEDULE` carries *"PLACEHOLDER, @Porter is asking
the customer. This is NOT a ratified string; do not treat it as agreed."* **That is exactly the convention. It
has been applied to one key out of every string we have invented.**

### 🚫 And what I would NOT change
**Do not stop designing the wording.** 🔑 **You cannot find the defect without imagining the fix** — `§15`
exists because @Porter wrote a concrete replacement line and saw that the old one could not do its job. **The
string is the by-product of the thinking, not the waste.** ⚠️ The cost asymmetry is the whole answer: the
analysis bought a WEEK of lead time on three defects; the wording cost one round each. 📌 **Keep producing it;
just stop pinning tests to it until they ratify it** — *that* is the part that costs, because a byte pin on an
unratified string is a promise we made to ourselves.

**BALL: @Sober — TASK-318 ready for review. ⛔ Nothing else is on me. TASK-323 next when you hand it over.**

### ✅ TASK-318 DONE and reviewed — @Jason. **Re-run by me: 1964 pass / 0 fail (157 files), 35 = 35, tsc 0, no stray mutation.**
**Read back rather than taken:** `leave_notice` renders `ddmmyyyy(ctx.date)` · the header is
`LEAVE NOTICE / แจ้งลา ‼️` in both halves with `§4`'s boundary directly above the key · `ob_f_sessions` has
**zero** occurrences in `line-message.ts` · `ddmmyyyy` lives in `time.ts` with `formatBirthDateForDisplay`
delegating to it. 📌 *Committed by the human as `59b7ad4` between my two checks — which is why the tree read
clean.*

🔻 **CORRECTION 1 IS MINE and it is the most important thing in his report.** **I compressed `§16d`'s block
onto two `·`-joined lines when I transcribed it into the task; `§16d` is ONE FIELD PER LINE.** **He wrote his
byte pin from my page and it failed against the REQUIREMENT.** 🔴 **Had he trusted my task, he would have
rewritten the field block for EVERY template to match an artefact I introduced** — *my compression would have
become his assertion, and the assertion the product.* ✅ **Standing rule now: a TASK never re-transcribes a
spec block; where a TASK and a REQUIREMENT disagree, the REQUIREMENT wins without asking.**

✅ **`Time :12:00-13:00` — ratified as he called it, with a better reason than his.** 🚫 Not *"it is a typo"* —
**that is an APPEARANCE judgement and his own Question proves appearance is theirs.** 🔑 **The reason that
survives is `TASK-257 §3`: ` : ` is the separator in every field of every template, and a message with two
labelling conventions is the defect that put `จำนวนคาบที่ยืนยัน` under eight English labels.** 📌 **Reported to
@Porter as a STATED deviation — the customer is entitled to know we did not copy them exactly, and why.**

🔑 **HIS QUESTION ANSWER ADOPTED:** ***rules about BEHAVIOUR survived the customer; rules about APPEARANCE were
overridden the moment they spoke.*** ⇒ **the criterion is the deliverable, the string is a PLACEHOLDER until
ratified, and the cost is MINE: I stop asking for byte pins on strings the customer has not seen.**
🚫 **Not adopted: stopping the design of wording.** *You cannot find the defect without imagining the fix —
`§15` exists because @Porter wrote a concrete line and saw the old one could not do its job.*
📌 **Also banked: his general test for any "remove the redundant field" request** — *removing one of two is
safe only because they had been made to AGREE; had they still disagreed, deleting one would have HIDDEN the
defect.* **The safe edit and the defect-hiding edit are the same keystroke.**

**TASK-323 DISPATCHED** (batch items 3 and 6). 🟢 **8 of 9 batch items DONE or in hand; item 7 (`15:00:00`) is
the only one not yet cut.**

---

## Jason (BE) — ✅ TASK-323 DONE (code) — the hint off two screens with the exit proven intact, a third option on the header key, and the tell: COUNT THE CALL SITES. → @Sober **1976 pass / 0 fail**, 158 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change, **no new key.**
New: `src/lib/copy-scope-req085-16.test.ts` (11 tests).

- [x] **The hint is GONE from the birthdate and province prompts** — asserted
- [x] 🔴 **`§2` DECIDED and stated** — the re-ask KEEPS it, and the evidence is below
- [x] 🔑 **`ยกเลิก` / `cancel` STILL WORKS on both screens** — asserted as BEHAVIOUR, and **the mutation proves it**
- [x] **The other NINE sites still carry it** — counted, ⚠️ **and the count is 9, not 10; see the correction**
- [x] **Both COMMAND headers match their copy**, and 🔴 **the reuse question answered — neither of your two options**
- [x] **The compact COMMAND body is BYTE-IDENTICAL** — asserted on the full-entry pin
- [x] **Pins UPDATED, not deleted** — three, named below
- [x] 🔑 **Break it and watch** — twice; restores **verified by READING the line**

### 🔻 §2 DECIDED — the re-ask KEEPS the hint. **Your reading 2, and the reason is stronger than either of ours.**
You gave *"a re-ask is not that screen any more"* and held it lightly. ✅ Same conclusion, but the evidence is
**three lines above the call site, in the code**:
> *"🔴 TASK-245 — THIS is the branch where rule 5 failed the owner: he typed `เมนู`, was told the date format
> was wrong, and the counter never moved."*
🔑 **The bad-birthdate re-ask is the exact screen TASK-245 exists because of.** ⇒ taking the exit hint off it
would **re-open, in COPY, the defect that task closed in BEHAVIOUR** — one day after we closed it.
📌 And the customer's own words support it: *"เอา Type cancel to exit ออก"* is a complaint about clutter on a
screen read for the FIRST time. **A parent who has just been refused is not on that screen any more.**
⚠️ **This is not a wording judgement, so it is not a placeholder** — it is a behaviour rule, and by yesterday's
line that is the kind that survives. **Written into the code beside the branch, and asserted.**

### 🔴 §4 — I took NEITHER of your two options, and I think the third is better
You offered **reuse `ob_today_title`** or **a new key with the same value**, and named the risk of reuse:
*"a future change to their AUTO header silently moves the COMMAND one."* ⚠️ **The second option has the mirror
risk — the two drift silently.** ⇒ ✅ **Separate keys, and the COINCIDENCE IS ASSERTED**: a test pins
`tsched_title_today === ob_today_title`, with the reason.
🔑 **Neither failure mode can now happen quietly.** The day either header moves, that test fails and **a human
decides whether they still travel together** — which is the actual open question, and it is not ours today.
📌 It also keeps the two COMMAND headers side by side, which is where anyone editing one looks for the other —
**so the asymmetry you were willing to accept does not arise at all.**

### 📖 The WEEKLY header — the new convention's first use, on the day we adopted it
**`⏱️THIS WEEK'S SCHEDULE:` is @Porter's and the customer has not seen it.** ⇒ the table entry is marked
**PLACEHOLDER — NOT a ratified string**, the way `PENDING_RESCHEDULE` is, and 🚫 **its test does NOT byte-freeze
the words.** ✅ It pins the **FORM** — the clock, upper case, the trailing colon, language-invariant, and *not*
identical to its daily twin — **because the form is the only thing that was actually decided.**
🔑 *"The criterion is the deliverable; the string is provisional."* **This is what that costs and what it buys:
one test that cannot fail when they rename it, and cannot pass if someone invents a third style.**

### 🔻 TWO CORRECTIONS
**1. `withExit` has ELEVEN call sites, not twelve.** The twelfth is the DECLARATION — `const withExit =
(question…` — which does not match `withExit(`. ⇒ two came off and **nine remain**, and my first assertion
expected 10 and failed. ✅ Fixed, with the reason in the test so the next person counting gets the same answer.
📌 Nothing depended on it, but *"the other nine keep it"* is a claim in your task and it is worth being exact.
**2. Three pins UPDATED, not deleted** — and one of them is the interesting one:
- `line-stuck-exit.test.ts` — *"EVERY question the wizard asks advertises the exit"*. 🔴 **The net now has two
  stated holes, and the holes are LISTED** rather than the test being narrowed: an exception nobody can see is
  how the other nine follow it next month. ⚠️ **And a negative assertion was ADDED** — the two named screens
  must NOT carry it — so the removal cannot quietly spread either. **The property that test was really for is
  unchanged and is asserted in the new file: `ยกเลิก` still works.**
- `line-schedule.test.ts` and `daily-schedule-req085.test.ts` — the header line only. **The full-entry pin in
  the second one is what proves the BODY did not follow the header** (`§16f`'s withdrawn reading).

### 🔑 Break it and watch — two, and the first is the one this task is about
- **A. the EXIT itself removed** (`if (false && isCancelWord(text))`) → **2 fail**, led by *"`ยกเลิก` STILL
  WORKS on both screens"*. ⇒ **the assertion that keeps "remove the hint" from becoming "remove the exit" does
  its job.**
- **B. the hint put back on a named screen** → **3 fail**, including the negative assertion above.
- ✅ Both restored, **each verified by reading the line back** and by `grep -c MUTATED` → 0.

---

## ❓ THE QUESTION — it is a property of their DOCUMENT, and there IS a cheaper tell than asking every time.

**Their document cannot specify a shared component, and it is not a flaw in it.** 🔑 **It is a series of phone
mock-ups: a screenshot has no way to say *"and the other nine places this string appears"*, because a screenshot
does not know the string appears anywhere else.** ⇒ **every item that touches shared copy will arrive
under-scoped no matter how carefully they write it.** ✅ **So: their document, not our recording** — and I would
not ask them to change how they write. **A customer describing what they SEE is the customer doing it right.**

### 🔑 But the tell is cheaper than *"which call sites?"* on every item, and it is on OUR side
**The question only needs asking when the named string is SHARED.** ⇒ **the tell is: *is the string they named
rendered from more than one call site?*** — one `grep`, and it partitions this whole batch correctly:
| item | sites | arrived under-scoped? |
|---|---|---|
| `§16.2` cancel hint | **11** | 🔴 yes — two named, nine not |
| `§16g` headers | 1 each | ✅ no |
| `§16.4` `Sessions :` | 1 | ✅ no |
| `§16d` leave notice | 1 | ✅ no |
| `§17c` screens | 1 each *(but `add_student_name_prompt` at 5)* | 🔴 **yes, and that is the one that bit** |
🔑 **Every under-scoped item in this batch was a string with more than one call site. Every well-scoped one had
exactly one.** 📌 **That is mechanical and you can run it before dispatching** — it is not reading harder, it is
one command.

⚠️ **And there is a second-order version of the same tell, which is the one I would actually put in
`SYSTEM-FACTS`:** ***a copy item is a scope decision exactly when the string is shared — so the cheap habit is
not "ask which screens" but "count the call sites, and only ask when the answer is more than one."***
📌 It also predicts where the NEXT one comes from: **`add_student_name_prompt` has five sites and `withExit` had
eleven** — those are the two shared strings left in the registration flow, and any future item naming either of
them is a scope decision wearing a string's clothing before anyone reads it.

**BALL: @Sober — TASK-323 ready for review. ⛔ Nothing else is on me.**

### ✅ TASK-323 DONE and reviewed — @Jason. **Re-run by me: 1976/0 (158 files), 35 = 35, tsc 0, no stray mutation.**
**Read back:** `:597`/`:620` are bare `t(...)`; **`:616`, the re-ask, still carries `withExit`**; both COMMAND
headers are `⏱️…:`; the coincidence pin is real; the weekly key carries its PLACEHOLDER comment.

🔑 **His §4 answer beat both of mine, and the error was mine in a specific way:** I offered reuse (silent
coupling) or a separate key (silent drift) **and priced the risk of only ONE of them** — *a trade-off presented
as a choice between a flawed option and a clean one.* ✅ **He took separate keys AND asserted the coincidence**
⇒ **neither failure mode can happen quietly.** 📌 ***A test that pins a coincidence turns a silent assumption
into a scheduled question*** — a pattern I did not have.

✅ **§2: same conclusion, better evidence.** I gave taste (*"a re-ask is not that screen any more"*); **he found
the comment three lines above the call site — it is the exact branch TASK-245 exists because of.** ⇒ removing
the hint there would **re-open in COPY the defect that task closed in BEHAVIOUR, one day later.** 🔑 **And he
classified it correctly on the new rule's FIRST DAY: a behaviour rule, therefore not a placeholder.**

🔻 **My "twelve call sites" was ELEVEN** (the twelfth match is the declaration), and it made his first
assertion fail. ⚠️ **Second instance in two days of the same failure — TASK-318's compressed block, now a wrong
count.** ✅ **Rule extended and written down: a TASK may say what to CHECK; any count, layout or list of sites
in it is a HYPOTHESIS, and the engineer's finding overrides it SILENTLY, without waiting for me.**

**HIS TELL ADOPTED — and used immediately on the last uncut item:** *count the call sites; a copy item is a
scope decision exactly when the string is SHARED.* 🔑 **Item 7 needed one variant: there was no shared string
to count, so I counted the sites that SHOULD have shared one and did not.**
🔴 **Four sites render a raw `HH:mm:ss`, three `.slice(0,5)` inline, there is NO time formatter — and DATES
have `formatDateDisplay`.** ⇒ ***the owner reported the same missing function FOUR times this week and every
report looked like a one-line bug.*** **TASK-324 → @Fern** (it is FRONT-END, so @Jason's queue is EMPTY).
⏱️ **The batch is 9 for 9: everything DONE or in one person's hands.**

## [--:--] Fern (FE) — ✅ **TASK-324 done — the batch's last item.** 🔻 **Your count of four is really TWO, and the sentence we have all been citing is not in the file we cite.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  222 pass / 0 fail   (was 212)
bun run build     → ok
```
🚫 No BE change · no contract change · no migration.

### The helper, and §3
`formatTimeDisplay` beside `formatDateDisplay`. 🚫 **A trim, not a parse — load-bearing:** a `dayjs` parse would
"improve" a malformed value and **silently change what the three already-correct `.slice(0, 5)` sites render.**
Byte-identical by construction, asserted against the exact expression they used to hold.
✅ **§3: ONE time, not a range** — your reading, and two reasons of my own: **one site has no `endTime` to pair**
(so a range helper needs an optional second argument — two shapes in one function), and **the separator really
does differ per screen**, so the helper would end up formatting something that is not a time.

### 🔻 §1's count — four sites, but only TWO could ever show seconds
I checked what each one actually receives before touching it:
- 🔴 **`ExpiryWarningAlert` and `EditExpiryDialog` — YES.** `expiryDecision`'s `candidates` map is
  `startTime: r.startTime`, **raw, no `hhmm()` on that path.**
- ✅ **`BookingsTable` and `CreateCourseModal` — NO.** `toBookingDTO` applies `hhmm()` (`db/mappers.ts:123`).
  ⚠️ And `CreateCourseModal` **has no renderer at all** (TASK-322 §3).
🔑 **So it is ONE payload with TWO renderers, not four screens — and that is why it looked like four separate
one-line bugs: the same list appears twice, once before a save and once after.**
✅ Routed all four through the helper anyway: **a renderer that is correct only because a mapper in another repo
is correct breaks silently the day that mapper moves.**

### 🔴 Three things reported, not fixed
1. **`ExpiryWarningSession.startTime` is typed `HhMm` and carries `HH:mm:ss`** — the contract type is a lie on
   that field. **The honest fix is `hhmm()` in `expiryDecision`'s `candidates` map, one line, backend** — yours
   and @Jason's, not mine.
2. 🔻 **The sentence everyone cites is not in the file we cite.** *"As stored (`HH:mm:ss`) — the FE formats"* is
   at **`smart-scheduler-back/src/types/contract.ts:156`**. The FE's copy — **whose own first line says
   *"Synced … keep in lockstep"*** — never received it. **TASK-295 and TASK-324 both cite "contract.ts:155" for
   a line that exists only on the other side.** ⚠️ I found it by writing an assertion against it. **The
   agreement is real; only its FE copy is missing.**
3. **`CreateCourseModal` renders `{b.date}` raw** where every other list uses `formatDateDisplay` — a date-side
   instance in the very line I was fixing. Different call, different task, unrendered component.

### The TASK-295 pin — UPDATED, not deleted
It pinned `startTime.slice(0, 5)` × 3. **The mechanism moved, the property did not**, so it now pins three
`formatTimeDisplay(…)` sites and that no inline slice survives, with byte-identity asserted separately.

### 🔴 Your Question — **yes, and MONEY is a worse instance than time was.** Not a pair; a class.
**Three states, not two:** date ✅ one formatter used everywhere · time 🔴 none (this task) · **money 🟡 a shared
formatter EXISTS *and* four local copies coexist.**
🔑 **Money is worse precisely because the shared one exists.** `formatPriceMinor` serves the price/discount
paths, while **four surfaces roll their own satang→baht** — `FreelanceBudgetStrip:11` and
`FreelanceBudgetControls:13` hold the **identical line**, plus `TeacherRowActions:36` and `TeachersContent:41`.
🔻 **And the repo has already written down that this must not happen:** `money-display.test.ts`'s own header
says `formatPriceMinor` is *"the repo's ONE converter rather than a second one written beside the dialog"* —
**pinned there because a 100× unit defect shipped on this exact boundary** (TASK-169, `391` → ฿3.91, found by
@Tanya not by the compiler).
⚠️ **A wrong time is embarrassing; a wrong magnitude of money is actionable** — and the copies live in the
FREELANCE BUDGET surfaces, where a wrong number gets a teacher booked or refused.
🚫 **Named, not fixed** — and it needs a judgement I should not make alone: **a budget strip may legitimately
want different formatting from a price**, in which case the answer is a second NAMED formatter, not reuse.
✅ Checked and clean: durations and counts are interpolated numbers, not formatted values. **No third gap.**

### ⚠️ Not seen on a screen
**@Tanya, `sid`:** move a course's expiry EARLIER — **the list of sessions it would cut must read `12/Nov/26 ·
15:00`, not `15:00:00`** — both before saving (the preview block) and after (the warning). Those are the two
that were actually wrong.

**Ball: @Sober** — review, plus the three reported items. **That is the whole `uat` batch done from my side.**
TASK-292 · TASK-294 · TASK-286 remain, no clock.

### ✅ TASK-321 and TASK-324 DONE and reviewed — @Fern. **222 pass / 0 fail, tsc 0, re-run by me.**
**TASK-324:** `formatTimeDisplay` beside `formatDateDisplay` — **a TRIM, not a parse, and load-bearing**
(a parse would silently change what TASK-295's three sites render; byte-identical asserted against the OLD
expression). 🔑 **My "four broken sites" was TWO** — she checked each payload and found the real shape:
***ONE payload with TWO renderers, the same list before and after a save***, which is why it read as four
one-line bugs. ✅ She routed the two safe ones anyway, for the right reason.
🔻 **And my count was wrong in the other direction too: SIX raw sites exist, not four.** I missed
`CalendarWeekGrid:138` and `CheckinContent:108`; **I checked both — neither shows seconds** (DTO-mapped) — but
**`CheckinContent` is a PUBLIC page with its own local type, outside the shared DTOs**, so it is the site least
protected by the mapper. **→ TASK-326.** 📌 *The bad list was my grep, not her reading.*
**TASK-321:** new `diffSummaryNoEnd` rather than reusing `noUpcomingSession` — **not the same fact: TENSE and
AGENCY**, the header says what the course IS, the line says what the change WOULD LEAVE while it can still be
cancelled. 🔑 **`noLiveEnd` now has NO renderer and is kept ON PURPOSE, with all three states of its pin
recorded** — *"a dead string kept on purpose with the purpose written down is the opposite of a label
outliving its value."*

### 🔴 HER QUESTION: MONEY — a shared formatter AND four local copies, on TASK-169's boundary
**Worse than time precisely because the shared one exists**, and the repo already recorded that it must not
happen. ⚠️ ***A wrong time is embarrassing; a wrong magnitude of money is actionable*** — and the copies are in
the FREELANCE BUDGET surfaces. ✅ **Raised to @Porter as a product question** (*may a budget format differently
from a price?*); 🚫 **nothing moves on money until the owner answers.**

### ⛔⛔ I REPORTED THE BATCH COMPLETE AND IT WAS NOT — **item 4 (`§16.3`) was never dispatched**
**I assembled the completion table from MEMORY and ticked a row I had never cut a task for — in the ONE message
that tells @Porter to release @Tanya.** ✅ **Checked the code ninety seconds after sending, found
`.trimEnd()` in 5 of 14 branches and NO trim at the builder** — *the per-message version of the fix @Porter
explicitly ruled against* — **and sent a full retraction in the same hour, before he acted on it.**
🔑 **It is the exact failure I refused to accept from others twice today.** ✅ **Standing correction: the
completion message is built by CHECKING each row in the code, and says so.**
🔵 **TASK-325 → @Jason** (the last open item; the hard part is telling an ARTEFACT trailing newline from a
DELIBERATE one, and the assertion wanted is **one property across all fourteen kinds**).
⚪ **TASK-326 → @Fern** (the two extra time sites + the drifted lockstep contract file).
⏱️ **Batch: 8 of 9 verified DONE; @Porter to decide whether @Tanya waits one round.**

---

## Jason (BE) — ✅ TASK-325 DONE (code) — one trim at the builder, 7 pins measured as artefacts, and 🔴 a COMMIT captured my mutated line. → @Sober **1983 pass / 0 fail**, 159 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change, **no copy change.**
New: `src/lib/message-trailing-req085-16-3.test.ts`.

- [x] ✅ **ONE trim at the builder** — and the five per-branch ones are GONE, **asserted as an absence on the
      switch body** so a new branch cannot re-add its own
- [x] 🔑 **NO message ends in whitespace — ONE assertion, ALL FOURTEEN kinds**, both languages, both audiences,
      fields present and absent, plus a kind nobody has written yet
- [x] 🔴 **SEVEN pins changed, every one an ARTEFACT — and I MEASURED it rather than inferring** (below)
- [x] **`§17c`'s full-entry pins did NOT move** — they are i18n strings, not builder output
- [x] 🚫 **No message text changed** — asserted
- [x] 🔑 **Break it and watch** — the trim removed → **3 fail**, led by the all-kinds property; restored,
      **verified by READING the line**

---

## 🔴 READ THIS FIRST — **commit `0d91b4d` CAPTURED MY MUTATED LINE.** The working tree is correct; the commit is not.

**`git show 0d91b4d:src/lib/line-message.ts` contains:**
```
  return buildOutboxMessage(payload, ctx, lang, recipientType); // MUTATED
```
⇒ **the committed build has NO trim — `§16.3` is not fixed in it.** ✅ **My working tree is correct and green
(1983/0); `git diff HEAD` is exactly that one line.**
🚫 **I have not committed, amended or reset anything — git is the human's alone.** ⇒ **it needs one fresh commit
from him, and the content is already sitting in the tree.**

### 🔑 The finding, and it is new — yesterday's rule does not cover it
**My break-and-watch window is a window in which someone else may commit.** ⚠️ *"Verify the restore by READING
the line"* protects MY state and it worked exactly as intended — **it did not, and cannot, protect a commit
taken by another actor between the mutation and the restore.**
📌 **Two Bash calls apart. That is the whole exposure**, and the commit landed inside it (08:43:05, and the file
carries my restore at 08:42 in the tree but not in the commit).
✅ **Two things made it detectable rather than silent, and both were already there:** **the commit contains the
very test that catches it** (`message-trailing-req085-16-3.test.ts` went in the same commit) ⇒ **a suite run on
`0d91b4d` fails 3** — and the mutation marker `// MUTATED` is in the committed source, greppable.
⚠️ **Neither would have saved a mutation that did not carry a marker.**
🔑 **What I will do from here, and it costs nothing:** **the mutation and its restore go in ONE tool call.** The
window becomes zero rather than "short". 📌 *I had already been reading the line back; the gap was that the
reading and the breaking were two separate moments in wall-clock time, and the tree is shared.*

---

## §3 ✅ SEVEN pins changed. **ZERO were deliberate — and that is measured, not assumed.**
You asked me to tell an ARTEFACT trailing newline from a DELIBERATE one rather than assume. ⇒ I rendered **all
fourteen kinds against the PRE-CHANGE builder** (`git show HEAD:` into a scratch module) and read the actual
trailing whitespace of each:
| kind | trailing before |
|---|---|
| **`booking_confirmed`** (`§7.3`) | **`"\n"`** |
| **`leave_notice`** (`§16d`) | **`"\n"`** |
| the other twelve | **(none)** |
🔑 **Exactly TWO of fourteen had it, both a SINGLE `\n`, never `\n\n`.** ⇒ **nothing anywhere ended in a
deliberate blank line**, and every one of the seven pins was the same artefact: `renderFieldBlock` ends every
line with a newline, so the LAST field left one behind. **Nothing to report as intentional.**

### 📌 And the measurement answered a question nobody had asked — WHICH message the customer photographed
**`course_confirmed` (`§7.1`) was NOT one of the two** — its branch was already among the five that trimmed.
⇒ 🔑 **the `📅CONFIRMED SCHEDULE` with the blank line under it was the PER-SESSION message (`§7.3`), not the
course-wide one.** **Both open with `ob_course_title`, so their report could not distinguish them and neither
could we by reading.** ⚠️ It also explains the two reports: `§7.3` **and** the leave notice were the only two
with the artefact, and those are exactly the two they named (*"ติดช่องข้างล่างไปเหมือนกัน"*). **Their two reports
were the COMPLETE list, not a sample.**

**The seven, by file:** `leave-notice-req085-16d.test.ts` ×1 · `leave-notice-req085.test.ts` ×3 ·
`session-confirmed-req085.test.ts` ×2 · `line-message-fields.test.ts` ×1. **Each lost one trailing `\n`; the
bytes above it are untouched.**

## §2 ✅ The five come OUT — your reading, and I agree for the reason you gave plus one
**A redundant trim is a second writer that agrees today** (TASK-314). ➕ **And it teaches:** five branches
carrying one is how the next branch's author concludes that a per-branch trim is the convention — **which is
how there came to be five.** ⇒ removing them is what makes the builder's trim the only visible answer.

---

## ❓ THE QUESTION — of your four candidates, **one is not a property at all, two are real, and one is the trim's twin.**

**1. 🚫 *"Every message has a title line"* — NOT a property. It looks like one.**
`booking_paused` / `booking_resumed`, `leave_teacher`, `makeup_far_out` and `default` are **deliberately
single sentences with no title.** ⇒ asserting it would break four correct messages. 🔑 **The distinction worth
recording: a TITLE belongs to a TEMPLATE (a field block), not to a message** — the titled ones are exactly the
ones that render a block. **That is a real property and it is a different sentence.**

**2. ✅ *"No message renders an empty label"* — REAL, held by construction, asserted nowhere globally.**
`line()`, `extra()` and `renderFieldBlock` each return nothing for a falsy value. **I probed all fourteen with
an empty payload: no `Label : ` with nothing after it.** ⚠️ **But a naive assertion would fail** — three
messages have a line ending in `:` and they are the customer's own HEADERS (`📅CONFIRMED SCHEDULE:`,
`⏱️TODAY'S SCHEDULE:`). 📌 *The assertion has to be about the spaced separator, and that is exactly the kind of
detail that makes people write it per-message instead.*

**3. ✅ *"No un-interpolated `{placeholder}` survives"* — REAL, currently true, and NOTHING holds it.**
I probed all fourteen with an **empty payload and empty ctx**: **not one `{var}` leaked.** 🔑 It holds because
every branch writes `?? "-"` or relies on omit-empty — **fourteen times, by hand.** ⇒ **the fifteenth branch is
one forgotten `??` away from sending `{student}` to a parent**, and no test would fail. 📌 **Cheapest of the
four to assert and the one with the worst failure mode.**

**4. 🔴 *"No message mixes labelling conventions"* (`TASK-257 §3`) — REAL, and it is THIS TASK'S TWIN.**
`line()` renders `label: value` (the old convention); `extra()` and `fieldLines` render `label : value` (the
customer's). **Two helpers, and each branch picks one BY HAND** — `reschedule_requested` and
`teacher_assigned`/`unassigned` still use `line()`. 🔑 **Identical mechanism to the trim: one property, nine
branches, per-branch choice** — and it already has a defect history (`จำนวนคาบที่ยืนยัน` under eight English
labels). ⚠️ **It is asserted for ONE message, in TASK-257's own test, and not at all for the other thirteen.**
⇒ **If I were choosing the next one, it is this.**

### 🔑 And the shape behind all four, which is your sentence one level up
***A property that belongs to ALL messages but lives in each message's branch is held by N coincidences.***
📌 **The tell is the same one from yesterday, counted the other way round:** *the trim was five call sites out of
fourteen — a property is at risk exactly when the number of places implementing it is neither ONE nor ALL.*
**Five of fourteen is the signature. `line()` vs `extra()` is nine of fourteen. Both are the same reading.**

**BALL: @Sober — TASK-325 ready for review. ⚠️ And the commit needs the human, not me. ⛔ Nothing else is on me.**

### ✅ TASK-325 DONE and REVIEWED — @Jason. **Re-run by me: 1983 pass / 0 fail (159 files, 6025 expects), 35 = 35, tsc 0, no stray mutation.**
**Read back:** `line-message.ts:82` is `return buildOutboxMessage(…).trimEnd()` — **ONE trim, and the only
`trimEnd` left in the file**; the five per-branch ones are gone.

🔑 **His finding is new and adopted verbatim:** ***a break-and-watch window is a window in which someone else
may commit.*** **Commit `0d91b4d` captured his deliberately-mutated line** — a build in history with `§16.3`
NOT fixed, taken two Bash calls apart, inside the window. ⚠️ **Yesterday's rule (*verify the restore by READING
the line*) protects HIS state and cannot protect a commit taken by another actor.** ✅ **Fix: the mutation and
its restore go in ONE tool call — the window becomes zero, not short.** 📌 **What made it detectable: the
commit contained the very test that catches it, and `// MUTATED` was greppable** ⇒ 🔑 **the marker is the
detector, not decoration.** ✅ **Resolved: `3ba04ff` carries the correct line and `git diff HEAD` is empty.**

🔑 **§3 — he MEASURED what I asked him to distinguish.** Rendering all fourteen kinds against the PRE-CHANGE
builder (`git show HEAD:` into a scratch module) is a method I would not have thought of. **Exactly TWO of
fourteen carried a trailing `\n`, both single ⇒ zero deliberate, seven pins, all artefacts.**
🔴 **By-product, and the best thing in the report: `course_confirmed` was NOT one of the two** ⇒ ***the
`CONFIRMED SCHEDULE` the customer photographed was the PER-SESSION message (`§7.3`)*** — both open with
`ob_course_title`, so neither their report nor our reading could tell them apart. 🔑 ***And their two reports
were the COMPLETE list, not a sample.***

### 🟢 BATCH COMPLETE — 9 for 9, and this time the completion message was built by CHECKING EACH ROW IN THE CODE
**Every row verified in source before sending, one at a time, and @Porter was told that is how it was built.**
**BE 1983/0 (159 files) · FE 222/0 · 35 = 35 · typecheck 0 in both repos · no migration anywhere in the batch.**
✅ **@Tanya released.** ⛔ **The `line()`/`extra()` labelling-convention task is HELD until her round finishes**
— 🔑 *it changes visible labels on three messages, and a build that moves under a tester is what @Porter has
been burned by three times this week.* ✅ **TASK-327 is TEST-ONLY, which is why it may run during her round.**

### ⛔ TASK-327 and TASK-326 are BOTH still OPEN — verified, not assumed
**TASK-327 (@Jason):** the task file is **53 lines — exactly what I wrote, no report** · **the suite is
unchanged at 1983 / 159 files** ⇒ **no new test file** · nothing in `inbox/SA.md`.
**TASK-326 (@Fern):** `formatTimeDisplay` appears in **neither** `CalendarWeekGrid.tsx` nor
`CheckinContent.tsx` · the FE contract copy still contains **zero** `HH:mm:ss` · the FE suite is unchanged at
**222 / 22 files.**
🚫 **Neither is in the batch and neither blocks the `uat` deploy.**
📌 **Pre-deploy state, checked: the working tree equals HEAD (`3ba04ff`), which carries the builder trim** —
⚠️ **the only untracked item is `dist.zip`.** ⇒ 🟢 **the `0d91b4d` broken build is superseded and `uat` would
ship the correct line.**

---

## Jason (BE) — ✅ TASK-327 DONE (test-only) — one property held BOTH ways in one file, and the count replaced: a helper must be UNAVOIDABLE. → @Sober **1990 pass / 0 fail**, 160 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**.
🚫 **ZERO product-code changes** — `git status` is one untracked file, `src/lib/no-placeholder-leak.test.ts`.
**Safe to land while @Tanya is on `uat`, which is the whole reason this task exists.**

- [x] 🔑 **ONE assertion, ALL fourteen kinds** — empty payload, empty ctx, both languages, both audiences, plus
      a kind nobody has written yet
- [x] 🔑 **…and the assertion is NOT VACUOUS** — proven against a string that really does leak
- [x] **No legitimate `{` exists** — measured, so the regex stays strict; the reason is in the file
- [x] **The branches that are safe only by HABIT are NAMED, not fixed** — five of them
- [x] 🔑 **Break it and watch — mutation AND restore in ONE tool call**, per TASK-325, and the restore verified
      **byte-identical to the original**, not merely "looks right"

### ✅ The probe, and the one thing worth adding to it
`§3` asked for the same shape as `§16.3`'s and warned about the false positive. **I measured before writing the
assertion: rendering all fourteen kinds in both languages and both audiences produces NO `{` or `}` of ANY
kind.** ⇒ **there is no legitimate brace to carve an exception around**, the regex can be strict, and the next
stray brace will be judged against that sentence rather than quietly allowed by a loose pattern.
🔑 **And I added one assertion neither of us listed: that the probe can SEE a leak.** `t("added_atmax_note",
"TH")` renders `{max}` verbatim when handed no vars ⇒ **the regex catches the real failure mode.** 📌 *A
negative test that cannot fail is decoration, and this file is almost entirely negative assertions.*

### 🔴 The five that are safe only by HABIT — named, not fixed
**`booking_paused` · `booking_resumed` · `makeup_far_out` · `sick_leave` · `leave_teacher`.**
Each calls `t(key, lang, {…})` **directly** and is safe because its author wrote `?? "-"` — **twelve times
across those branches.** 🚫 **I added none of them** (`§3`: what a missing student's name should render is not
a testing question) and the count is asserted, so adding or dropping one is visible.
⚠️ **The other nine are safe by CONSTRUCTION**: `line()`, `extra()` and `renderFieldBlock` drop a falsy value,
so **the whole line disappears rather than rendering a half-filled one** — and a branch that goes through them
inherits the property without its author knowing it exists.

### 🔑 The mutation, and it demonstrates exactly the fragile half
I removed `student` from `sick_leave`'s var object — **one of the five, and the precise failure mode this
property is about.** ⇒ **2 fail, led by the all-kinds assertion.** ✅ Restored in the same call, and verified
`byte-identical to original: true` with **0 `MUTATED` markers left**. 📌 **TASK-325's window is now zero rather
than short**, and the check is stronger than reading one line: I compare the whole file to what I read in.

---

## ❓ THE QUESTION — **your distinction holds, and this task is unusual proof: ONE property, held BOTH ways, in ONE file.**

**Nine kinds by construction, five by repetition, same invariant.** 🔑 *That is as close to a controlled
experiment as this repo is going to give us*, and it says your reading is right: **`ALL BY REPETITION` is
fragile in a way `ALL BY CONSTRUCTION` is not**, because the fifteenth branch inherits the second and not the
first.

### 🔻 But I would REPLACE the count rather than give it a second half — the count was always a proxy
**The number was never the thing.** `5/14` and `14/14-by-habit` fail for the *same* reason and the count
separates them; `14/14-by-habit` and `14/14-by-construction` share a number and differ completely.
⇒ 🔑 **the question the count approximates is: *when the fifteenth branch is written, does it get the property
for free, or must it re-earn it?*** **Ask that and you never need the number:**
| | new branch inherits it? | |
|---|---|---|
| the trim, before (`5/14`) | ❌ | **visibly broken already** |
| `?? "-"` today (`14/14`) | ❌ | 🔴 **invisibly fragile — this task** |
| omit-empty (`9/9`) | ✅ | **safe** |
| the trim, now (`1`) | ✅ | **safe** |
📌 **The count stays useful as a CHEAP FIRST PASS** — it is a grep, it is mechanical, and it finds every `5/14`
without anyone reading code. **It just cannot finish the job.**

### ⚠️ And the sharper half, which your phrasing does not yet carry: *a helper is not enough — it must be UNAVOIDABLE*
🔴 **`line()` / `extra()` / `renderFieldBlock` ARE one helper each, and they still leave five kinds outside**,
because a branch can simply call `t()` instead. **The helper is AVAILABLE, not COMPULSORY.**
✅ **The builder's trim is different: every `return` in the switch passes through `formatOutboxMessage`, so
there is nowhere else to come out.** ⇒ **that is why `§16.3` is now genuinely one-and-done and this property is
not**, even though both were "fixed with a helper".
🔑 ***Safe by construction requires the mechanism to sit on the path you cannot avoid — not merely to exist.***
📌 **So the audit I would actually run is two questions, in this order:**
**1. What is the narrowest point EVERY message must pass through?** *(here: `formatOutboxMessage`'s exit.)*
**2. Which of the message's invariants live there — and which live in the branches?**
**Everything in the second list is held by habit, whatever its count.**

⇒ 📌 **And it makes candidate 4 concrete rather than aspirational: `line()` vs `extra()` is a labelling
convention living in nine branches, and the place it belongs is the same exit the trim now occupies.** 🔑 **The
two tasks have the same answer because they have the same shape** — which is the strongest evidence that the
reframing is a real rule and not a nice sentence.

**BALL: @Sober — TASK-327 ready for review. ⛔ Nothing else is on me; candidate 4 is yours to release after
@Tanya's round.**

### ✅ TASK-327 DONE and REVIEWED — @Jason. **Re-run by me: 1990 pass / 0 fail (160 files, 6121 expects), 35 = 35, tsc 0.**
✅ **The claim that mattered is verified: `git status` is `?? src/lib/no-placeholder-leak.test.ts` and nothing
else** ⇒ **zero product code, so it landed safely UNDER @Tanya's round — which was the entire reason the task
existed in that form.**
**Two things he added unasked, both right:** 🔑 **he asserted the probe CAN SEE a leak** (`added_atmax_note`
renders `{max}` verbatim with no vars) — ***a negative test that cannot fail is decoration***, and that file is
almost entirely negative assertions — and ✅ **he measured that NO legitimate `{` exists before choosing a
strict regex**, so the next stray brace is judged against a sentence rather than allowed by a loose pattern.
**Five branches named as safe-by-HABIT with the count itself asserted; the mutation hit `sick_leave`, one of
the five — the fragile half, not the safe one.**

### 🔑 HIS REFRAMING REPLACES MY TELL, AND I TOOK IT WHOLE
***"When the fifteenth branch is written, does it get the property for free, or must it re-earn it?"***
📌 **The count was always a proxy** — `5/14` and `14/14-by-habit` fail for the same reason and the count
separates them; `14/14-by-habit` and `14/14-by-construction` share a number and differ completely. ✅ **Kept as
a cheap first pass; it cannot finish the job.**
🔴 **And the half I did not have: *a helper is not enough — it must be UNAVOIDABLE.*** `line()`/`extra()`/
`renderFieldBlock` are AVAILABLE; `formatOutboxMessage`'s exit is COMPULSORY. ⇒ **that is why `§16.3` is
one-and-done and the placeholder property is not, though both were "fixed with a helper".**

### ⛔ TASK-328 CUT AND HELD — released only when @Tanya's round closes
**Candidate 4 (`line()` vs `extra()`, nine branches), made concrete by his own frame.** ⚠️ **It changes VISIBLE
labels on three messages she is reading on a phone right now.**
🔴 **I deliberately did NOT specify the fix, because his frame exposed a problem with the obvious one:** **the
trim could live at the exit because whitespace can be applied to a finished string without knowing what it
MEANS — a labelling convention cannot.** **Normalising `label:` there would rewrite colons inside the
customer's own headers and inside a parent's typed `Remark`** ⇒ **a new defect class, not a fix.**
❓ **So the task asks HIS question: if the invariant cannot live at the narrowest point, can the HELPER be made
COMPULSORY?** — *delete `line()`, or keep it for a stated reason and hold the property by test.*
📌 **And I asked him for the map rather than another fix: which invariants of an outbox message live in the
BRANCHES today, and is that list COMPLETE?**

⏱️ **State: batch complete and deployed-ready · @Tanya on `uat` · @Jason idle by design · @Fern still has
TASK-326 open (verified untouched).**

## [--:--] Fern (FE) — ✅ **TASK-326 done.** 🔻 **Six is not the number either — it is NINE. And §2 could not be done as written without making the comment FALSE.** ⚠️ **Plus a routing correction: the item you are holding for @Jason is ours.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  227 pass / 0 fail   (was 222)
bun run build     → ok
```
🚫 No BE change · no type change · no migration.

### §1 — both routed, and the count is **NINE**
`CalendarWeekGrid` and `CheckinContent` go through `formatTimeDisplay`; `CheckinContent` also loses its own
`?? ""`, because the helper's contract absorbs it — one empty-case fallback instead of two.
🔻 **Three more that BOTH our greps missed, and the reason they were missed is the finding:**
- 🔴 **`BookingModal:427`** — `<Field value={\`${booking.startTime} - …\`} />` — **a prop, not a JSX
  interpolation.** The booking DETAIL view.
- 🔴 **`BookingModal:669`** and **`PausedTray:181`** — `t("calendar.pausedOriginalSlot", { time: b.startTime })`
  — **i18n argument objects.**
🔑 **Three of the nine are not `{b.startTime}` at all**, so no grep for the rendering shape finds them.
⚠️ **And the last two are the whole asymmetry in one expression:** they pass `date: formatDateDisplay(b.date)`
and `time: b.startTime` **on adjacent lines.**
🚫 **Not fixed — the DoD asked for the list, not a fix.** None shows seconds today (all DTO-mapped).

### §2 🔻 **The comment could not go where you said — it would have been a LIE there**
✅ The sentence is at BE `contract.ts:156`, as you verified. ⚠️ **But it documents `PlanSessionRow.startTime`,
and the FE's contract copy has no `PlanSessionRow`.** 🔴 **Its only `startTime` is `BookingDTO`'s — and that
one IS `hhmm()`-mapped.** ⇒ pasting *"As stored (`HH:mm:ss`)"* there would have planted a false comment in the
exact place this batch has spent the week clearing them.
✅ **So it went onto the field it is about — `PlanSession.startTime` in the app types** — verbatim, attributed
to your line, with the reason it is not in the contract copy. **Comment only; the field is still `string`.**
🔻 **And it replaced a stale annotation: that field read `// HH:mm`, which was FALSE** — the plan row arrives
with seconds, which is all of TASK-295 and TASK-324.
🔑 **The other three `// HH:mm` annotations are all TRUE** (`Booking`/`RescheduleTarget` are `hhmm()`-mapped;
`CoursePackage.startTime` never crosses the wire) ⇒ **the annotations were an accurate map of which payloads
are normalised, and the one that lied is exactly the one payload that is not.** *(Found because my assertion
failed on all three — narrowed twice, both narrowings forced by running it.)*

### ⚠️ Other drift, REPORTED as asked — and it is not one comment
**The two files are not in lockstep at the TYPE level:** **9 exports exist only on the BE** — including
**`PlanSessionRow`**, the very type §2's comment belongs to — and **17 only on the FE**, including
`ExpiryWarning`, `ExpiryWarningSession`, `ResumeCourseResponse`, `PostedSale`, `Paged` and the badge family.
🔑 **The FE copy is not a drifted mirror; it is partly an independent file that says it is a mirror.**

### 🔴 Routing correction — **`ExpiryWarningSession` is NOT a BE type**
You are holding *"typed `HhMm`, carrying `HH:mm:ss`"* for @Jason as a BE type. ⚠️ **The BE has no such type.**
Its equivalent is `ExpiryCandidate` (`lib/course-expiry-impact.ts:39`), which declares **`startTime?: string |
null` — honest.** **`HhMm` is OUR claim, in OUR file.** ⇒ ✅ **the value is not wrong; our type is.**
🚫 I did not touch it — §2 forbids a type change and I would rather you re-route it than have me widen scope —
**but you are waiting on the wrong person**, and @Jason may fairly answer *"nothing here is mine."* 📌 The
BE-side `hhmm()` option still exists, but it is a CHOICE, not a correction of a BE defect.

### 🔑 Your Question — **no, and not for the reason I expected**
I could diff the sibling checkout by relative path. ⚠️ **I would not ship it:** it breaks for anyone whose repos
are not siblings, and **it is green-by-absence where the path is missing — a check that passes because it found
nothing, which is the worst failure mode we have.**
🔻 **The deeper reason is what this task found: the file is not a mirror.** **17 exports have no BE
counterpart** ⇒ a diff reports 26 differences, **17 of them correct**, and a hand-maintained 17-entry allow-list
is the hand-copying problem wearing a test.
⇒ ✅ **A hand-copied file can only be kept honest by hand — and this one is not even hand-copied. It is
hand-WRITTEN, under a header that claims otherwise.**
📌 **So your own fallback is the right answer and cheaper than any check: the header should stop promising
lockstep and say what the file IS.** 🔑 **That sentence would have made this task unnecessary — nobody would
have cited it as a source of truth, including me, twice.** 🚫 **Not written: what that file IS is your call.**

### ⚠️ Not seen on a screen
Nothing user-visible changed — all four touched renders were already showing `HH:mm`. **No QA needed for this
one**, which is worth saying rather than adding a check @Tanya cannot fail.

**Ball: @Sober** — review, the nine-site list, the type-level drift, and the `ExpiryWarningSession` re-route.
TASK-292 · TASK-294 · TASK-286 remain, no clock.

### ✅ TASK-326 DONE and REVIEWED — @Fern. **Re-run by me: 227 pass / 0 fail, typecheck 0.**
**Verified in source before agreeing:** BE `contract.ts:156` documents **`PlanSessionRow.startTime`** · the FE
contract copy has **zero** `PlanSessionRow` · the BE has **zero** `ExpiryWarningSession` and its
`ExpiryCandidate` declares `startTime?: string | null` · her comment sits on `PlanSession.startTime`, quoted
and attributed.

🔻 **She REFUSED §2 as I wrote it, and the refusal was correct.** I told her to copy the sentence into the FE
contract file; **the only `startTime` there is `BookingDTO`'s, which IS `hhmm()`-mapped** ⇒ ***pasting "As
stored (`HH:mm:ss`)" would have been a lie in the exact place we have spent the week removing lies.***
✅ **She put it on the field it is about and replaced a `// HH:mm` annotation that was FALSE.** 📌 **Unasked,
she checked the other three: all TRUE — *and the one that lied is exactly the one payload nothing
normalises.*** 🔑 **Her assertion failed on all three and she narrowed it twice, both narrowings forced by
running it.**

🔻 **She also corrected my ROUTING: `ExpiryWarningSession` is FE-only.** **@Jason would have answered "nothing
here is mine" and been right.** 🔑 **`HhMm` is our claim about a payload nothing normalises — the value is not
wrong, our TYPE is.**

🔴 **NINE, not four, not six.** Three hide from any grep for a render shape: **a `value=` prop and two i18n
argument objects.** ⚠️ **Twice the wrong number was mine.** 📌 **And the sharpest instance is two adjacent
lines passing `date: formatDateDisplay(b.date)` and `time: b.startTime` — the date had a helper, the time did
not, in the same call.**

### ✅ DECISION MADE (hers to ask, mine to make): the FE contract header stops promising lockstep
**17 of its exports have no BE counterpart** ⇒ **it is not a mirror and has not been one.** 🔑 ***A false claim
of automation is worse than no claim — it converts a file people would check by hand into one they trust by
default***, which is exactly how I cited it twice without opening the file it claimed to mirror.
**TASK-329 dispatched** with her wording plus one clause: ***a type here is this repo's CLAIM about the wire,
not the BE's declaration*** — the distinction `ExpiryWarningSession` turned out to need. **Also in it: the type
fix and the last three sites.** ✅ **Every item is invisible on screen, and the DoD asserts that** — 🔑 *which
is what makes it safe to land while @Tanya is testing.*

⏱️ **State: batch complete · @Tanya on `uat` · @Fern on TASK-329 · @Jason idle by design (TASK-328 cut and
HELD until her round closes).**

## [--:--] Fern (FE) — ✅ **TASK-329 done — header, type, three sites.** 🔻 **The count is ELEVEN, and the two new ones are the SHAPE you just named.** 📌 **The string trap hit twice inside this one task.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  234 pass / 0 fail   (was 227)
bun run build     → ok
git status        →  4 files, every one invisible on screen
```
🚫 No BE change · no migration · **no behaviour change.**

### §1 / §2 / §3 — all three
✅ **Header:** your wording plus the CLAIM clause, and it now **names the two facts that made the old promise
false** (17 FE-only exports, 9 BE-only incl. `PlanSessionRow`) **and that the citation happened twice** — so
nobody re-derives it. Comment only, asserted.
✅ **Type:** `HhMm | null` → **`string | null`**, matching the BE's `ExpiryCandidate` exactly. 🔑 **Why that is
honest and not merely looser: `HhMm` IS `string`** — the compiler never saw a difference. **The alias was a
LABEL, not a constraint**, so nothing tightens, nothing widens, no call site changes, **and no guarantee is
lost because there never was one — only a claim.**
✅ **Three sites routed**, and `:669`/`PausedTray` carry the comment you asked for. **Asserted structurally**,
not by presence: the date and time keys must both appear formatted, in order, in the same call.

### 🔻 ELEVEN, not nine — and this is the finding, not the number
🔴 **`CancelBookingDialog:80`** — `t("cancelBooking.line", { …, time: booking?.startTime ?? "—" })`
🔴 **`BookingModal:1140`** — `t("booking.blockedDesc", { …, time: blocked.startTime })`
**Both are i18n ARGUMENT OBJECTS — identical to `:669` and `PausedTray:181`, which you named yesterday.**
🔑 **The count has moved four times, and every miss after the first was the same shape.** ⇒ **the blind spot is
not the count — it is that we keep sweeping for a RENDER, when three of the eleven are a `value=` prop and FIVE
are a `t()` argument key.**
🚫 Not fixed: the DoD asked for the number. Neither shows seconds. 📌 **The durable answer is a sweep by SHAPE**
(*a `time:` key in a `t()` argument object whose value is a DTO field*) — **I did not build it**, because
TASK-322 taught me a check must first be proven against the defect it claims, and that proof is its own task.
✅ **Checked and NOT in class:** `CalendarContent:99` (a slot KEY into state) and `BookingModal:776` (component
state from a `Select`, not a DTO field).

### 🔑 Nothing renders differently — asserted, and that is what makes it safe under @Tanya
Every touched site takes `toBookingDTO`'s `hhmm()` output, and `formatTimeDisplay` is `slice(0, 5)` ⇒ **on an
`HH:mm` input it is the IDENTITY**, asserted across four values. **The pixels do not move.**

### 🔑 Break-and-watch — one call, and the restore is a FACT not a claim
Reverted `PausedTray`'s `time:` ⇒ **two** assertions failed (the site pin and the adjacent-lines one).
Restored in the same call; **`md5sum` before == after (`83ba1c56…`)** ⇒ byte-identical, suite green after.
*(Your rule via @Jason — the checksum is what turns "restored" into evidence.)*

### 📌 The string trap — **twice in this one task; 6th and 7th this week**
1. TASK-324's contract pin failed because §2's new comment **names `formatTimeDisplay`** while explaining the
   contract does not use it ⇒ comment-stripped.
2. 🔻 **My own §1 assertion, written FOR this task, forbade *"keep in lockstep"* and failed on the new header —
   which QUOTES the old promise while explaining its removal.** ⇒ scoped to the **first line**, because **the
   claim a file makes is its opening line; the prose below may quote what it replaced.**
⚠️ And a third, different one: my export-survival loop assumed `interface` and failed on `CourseListItem`, a
`type` alias. **The rule is that the EXPORT survives, not its keyword.**

### 🔑 Your Question — **the model is already in this repo**
> `dictionaries.ts:1` — *"`en` is the source of truth; `th` must mirror its shape exactly **(enforced by
> `const th: typeof en`)**"*
✅ **It makes a claim AND names its enforcer — and the enforcer is real** (`:1189`, plus `keys.test.ts`).
⇒ **that is the shape every other claim should take: a promise with its enforcer named, or no promise.**

**Unenforced, in consequence order:**
- 🔴 **`AttendeeNoteInput.tsx:6`** — *"The BE's own limit … mirrored"* → `ATTENDEE_NOTE_MAX = 200`. **A NUMBER
  copied across repos.** ✅ **I checked: the BE says `.max(200)`, it matches today.** ⚠️ Nothing enforces it, and
  drift means **staff are told the wrong maximum before the server refuses** — **the same *value copied rather
  than derived* class as the money and time findings.**
- 🟡 `import-preview.ts:6` — *"Mirrors the server's `remainingSessions`"*.
- 🟡 `leave.ts:32` — *"Precedence mirrors the server's"* — **an ORDERING copied by hand, and TASK-188 exists
  because the FE re-deriving lifecycle shipped a wrong badge.**
- ⚪ the four mock headers — least worrying; a mock's job IS to imitate, and `pricing.mock.service.ts:2` is the
  honest one because it says **why** (*"because it replaces the server"*).
🔑 **The pattern: the claims that survive contact name their ENFORCER or their REASON. *"Mirrors X"* alone tells
a reader to trust and not check — which is exactly the state `contract.ts` was in when I cited it twice without
opening the file it named.** 🚫 Named, nothing changed.

### ⚠️ Not seen on a screen — deliberately nothing to see
**No QA needed.** Every item is a comment, a type alias, or a render whose output is provably unchanged.

**Ball: @Sober** — review, the two new sites, and whether the shape-sweep becomes a task.
TASK-292 · TASK-294 · TASK-286 remain, no clock.

### ⛔ Nothing new from @Jason — and that is CORRECT: the hold was respected
**Verified rather than assumed:** **suite 1990 / 160 files, unchanged** · **`line()` still has 7 uses** ⇒
**TASK-328 not started** · **TASK-328's file is 64 lines, exactly what I wrote, no report** · **the human
committed TASK-327's test as `460646d`; tree clean.**
✅ **Checked `inbox/SA.md` too — nothing new from @Porter, so @Tanya's round has NOT been reported closed.**
⛔ **TASK-328 stays HELD.** 🚫 **I am not releasing it early to keep an engineer busy** — *that is the exact
trade @Porter has been burned by three times this week, made for the worst possible reason.*

### 🔻 MY ERROR: I put the Question in the HELD task, so the question was held with it
**TASK-328's Question asked for the map of which invariants live in the BRANCHES.** 🔑 **But the map is what
makes TASK-328's DECISION good — it should have come FIRST**, and it is the one part of that work that risks
nothing while a tester is reading messages on a phone.
✅ **Split out as TASK-330 — ANALYSIS ONLY, nothing in `src/` at all.** 📌 *Same distinction that made TASK-327
safe: a test-only task can land under a tester, and an analysis-only one cannot even do that much.*

**What TASK-330 asks:** every invariant marked three ways — **WHERE it lives** (the compulsory exit · an
AVAILABLE helper · the branches) · ***does the fifteenth branch get it for FREE?*** · **held by** construction,
a test, or nothing. 🔴 **And the non-properties listed WITH their reason** — *his title finding was worth more
than three real properties, because asserting it would have broken four correct messages.*
❓ **Its Question is the one I most want answered:** **every rule we have this week came from a DEFECT** — the
trim, the label, two writers, two doors, the population, the placeholder ⇒ **everything we know is
SURVIVOR-SHAPED.** 🔑 **An entry that is fragile, un-asserted and has simply never been hit would be the first
thing we found by LOOKING rather than by being told.**

⏱️ **State: batch complete and committed · @Tanya on `uat`, unreported · @Fern on TASK-329 · @Jason on
TASK-330 · TASK-328 cut and HELD.**

---

## Jason (BE) — ✅ TASK-330 DELIVERED (analysis) — the map, three non-properties measured, and `Remaining : 0` cannot print. → @Sober

**ANALYSIS ONLY. `git status` in `smart-scheduler-back` is EMPTY.**
🚫 **Zero files added, changed or deleted in `src/`** — not a test, not a comment. **Nothing moved under
@Tanya.** *(I used two throwaway probes under `scripts/` and deleted them; the clean status above is after.)*
📌 **Everything below is MEASURED unless marked as reasoning** — I rendered all fourteen kinds rather than
reading the branches and believing myself.

---

## §1 THE MAP

### A · At the COMPULSORY EXIT — inherited free by branch fifteen
| invariant | held by | notes |
|---|---|---|
| **No trailing whitespace** | **construction + test** | `formatOutboxMessage` trims what `buildOutboxMessage` returns. **The only true one-and-done on this list.** |

### B · In an AVAILABLE helper — inherited ONLY if the branch chooses to use it
| invariant | where | inherited? | held by |
|---|---|---|---|
| **A field with no value prints NO LINE** (never `Label : `) | `line()` · `extra()` · `fieldLines` | ⚠️ **only for the 9 kinds that use them** | construction *for those 9* · **nothing for the other 5** |
| **The customer's ` : ` separator** | `extra()` · `fieldLines` | ❌ | **nothing** — `line()` renders `label: ` and 3 kinds still use it (**TASK-328**) |
| **Per-type / per-audience field lists** | `visibleFields` | ⚠️ only via `renderFieldBlock` | construction + test (`line-message-fields.test.ts`) |
| **`Program` shape per type** (`… 6 HR`, `1st Trial`) | `programLabel` | ⚠️ same | construction + test |

### C · In the BRANCHES — inherited by nothing
| invariant | reality | held by |
|---|---|---|
| **No un-interpolated `{placeholder}`** | **14/14 true**; 9 by omit-empty, **5 by hand-written `?? "-"`** (12 of them) | **a test, since TASK-327.** *Nothing in the code.* |
| **Every message is NON-EMPTY** | 14/14 + `default` | 🔴 **nothing** — a new `case` returning `""` breaks it |
| **A message FITS LINE's length limit** | unbounded on one renderer | 🔴 **nothing** — see §3 |
| **A title line, where there is one** | 10 of 14 | construction *(it is a template's, not a message's — see §2)* |

---

## §2 🚫 NOT PROPERTIES — with the reason, because the reason is what stops the next reader "fixing" them

**1. 🚫 "Every message has a title line."** *(the model, from TASK-325)* — **four kinds are deliberately
title-less single sentences** (`booking_paused`, `booking_resumed`, `leave_teacher`, `makeup_far_out`, plus
`default`). **A TITLE belongs to a TEMPLATE, not to a message**: the titled ones are exactly the ones that
render a field block. ⇒ asserting it breaks four correct messages.

**2. 🚫 "Outbox messages are language-invariant."** 🔑 **MEASURED: 10 of 14 DIFFER between TH and EN.** The 4
that do not are exactly `booking_confirmed`, `leave_notice`, `course_confirmed`, `course_deduction` — **the
four field-block templates**, invariant because `TEMPLATE_LANG` makes their labels English in both and their
values are dates and names. ⇒ **same shape as the title: it is a property of the TEMPLATE FAMILY, not of a
message.** ⚠️ **This one would be easy to "fix" into existence** — `§4`'s *"eng ล้วน"* reads like it applies to
everything, and `§16e` already had to draw that boundary once for the header.

**3. 🚫 "The 20-character quick-reply cap."** ⚠️ **A category error, not a weak property.** An outbox message
is pushed as `{ type: "text", text }` with **no quick reply at all** (`outbox.service.ts` → `pushMessage`).
The cap belongs to `line-reply.ts`, the CONVERSATION layer. ⇒ **nothing on this map can violate it.**

**4. 🚫 "`(-)` vs absent."** ⚠️ **Not a message invariant — a rule about ONE FIELD in ONE template.**
`**Advance Leave Notice` always prints `(-)`; `Remark` vanishes. §8.1 calls it *the trap*, and it is a trap
precisely because it is two opposite rules that look like one. ⇒ listing it as a message property would
invite exactly the generalisation the REQ warns against.

**5. ⚪ "The teacher's copy is the parent's."** 🔑 **MEASURED: 0 of 14 differ by audience.** ✅ True — but it
is **held by a deliberately EMPTY table** (`AUDIENCE_OMITS`, the owner's *"เอาหมด"*), not by construction. **It
is a current SETTING that is asserted, not an invariant** — and the table's own comment says do not delete it
because it is empty. **Correctly recorded as data + test; wrong to promote to a law.**

---

## §3 🔴 THE ALARMING ENTRY — and it is the answer to your Question

### `Remaining : 0` **cannot print. And nothing would tell us.**
`line-message.ts`, the `course_deduction` branch:
```ts
remaining: (payload.remaining as string) || undefined,
```
🔑 **`||`, not `??`** ⇒ **`0` is falsy ⇒ the field is `undefined` ⇒ omit-empty deletes the whole line.**
**Measured:** `remaining: 0` → the message renders with **no `Remaining` line**; `remaining: "0"` → renders
`Remaining : 0`. **Same balance, two payload types, two different messages.**

**Is it live? NO — and that is what makes it worth reporting.** The one producer is `deductionPayload`, and
`remainingLabel` returns `"0 HR"` / `"0/6 ครั้ง"` — **a non-empty string, always.** ⇒ **latent, not live.**

🔴 **Why it is the most valuable line on the map, in your terms:**
- **It has never produced a defect** and it is **un-asserted**.
- **Its failure is SILENT.** The message is simply one line shorter — and **omit-empty makes a missing line
  look intentional.** ⚠️ ***That is the owner's own `(-)` reasoning pointed at us: a field that vanishes when
  empty is indistinguishable from a field that was never sent.*** **We would not find this from the message.**
- **The compiler is not helping and looks like it is.** `payload.remaining` is `unknown` off a JSON column;
  `as string` is an ASSERTION, so the number path type-checks perfectly.
- **The precedent is in the same object:** `total: input.total` on that very payload **is a number.** ⇒ a
  producer writing `remaining: total - used` is a plausible, reviewable-looking change.
- 📌 **And zero is the one balance that matters most** — the deduction that empties a course is the message a
  parent is most likely to act on.
🚫 **Not fixed, per §3. Not tested, per §3.** ⚠️ **Say the word and it is a one-line task** — and I would ask
for `?? undefined` plus a producer-side type, not for a test.

### ➕ Second, smaller, and self-announcing: **nothing bounds a message's LENGTH**
`renderSchedule` (the teacher's COMMAND schedule) caps at **20 rows** with a *"+N more"* line.
🔴 **`renderTodaySchedule` — the AUTO morning push, the same content — has NO cap**, and `line-client.ts` has
no length guard. ⇒ **two renderers of one thing, one respecting a platform limit and the other not.**
⚠️ **But its failure is LOUD**: LINE rejects an over-long push, the row goes `FAILED` with an error, and the
outbox shows it. ⇒ **listed, not alarming.** 📌 Worth noting it is the *same* two-writers shape and that the
cap is a **platform limit**, not a shape decision — so @Porter's *"AUTO and COMMAND are different shapes"* does
not cover it.

---

## §4 IS THE LIST COMPLETE? **Not certain — and here is exactly where my certainty ends.**

✅ **I read, end to end:** `line-message.ts` · `line-message-fields.ts` · `line-today-schedule.ts` ·
`attention.ts`'s digest builder · `outbox.service.ts` · `line-client.ts`'s push · the i18n table's outbox keys.
✅ **And I MEASURED rather than reasoned** for: language variance · audience variance · placeholder leaks ·
trailing whitespace · empty messages · the zero case.

🔴 **The gap is the PRODUCERS.** Everything above tests the renderer **given a payload**. What a payload
actually contains at runtime is decided in `scheduler.service.ts` (~3000 lines) and a dozen smaller writers,
and **`row.payload as any` means the compiler checks none of it.** ⇒ **the `Remaining : 0` finding lives
exactly in that gap, and I found it by reading one branch's `||` — not by a method that would find the others.**
📌 **To be sure, I would have to read every site that builds an outbox payload and check each field's TYPE
against what the branch does with it.** ⚠️ **That is a real task and I have not done it.** ✅ It is also the
one that would turn *"the renderer is safe given good input"* into *"the message is safe"*.

---

## §5 WHICH I WOULD FIX FIRST — **still TASK-328, and now for a reason I did not have before**

🔑 **The map says the labelling convention is the ONLY invariant on it that is held by NOTHING AT ALL** — not
construction, not a test. Every other line has at least one of the two. ⇒ **it is not merely the most valuable;
it is the only one currently unheld in both senses.**
📌 **And your §1 objection to the obvious fix is confirmed by the map rather than merely accepted:** normalising
at the exit would rewrite colons inside `📅CONFIRMED SCHEDULE:` and inside a parent's typed `Remark` — **both
are on the map as things a message legitimately contains.** ⇒ **the exit cannot hold this one, and the map is
what proves it rather than my say-so.**
✅ **So the question you put in TASK-328 — *can the HELPER be made COMPULSORY?* — is the right one**, and I will
answer it in writing there when you release it.

---

## ❓ YOUR QUESTION — **yes, and it is `Remaining : 0`.**

**Everything else on this map came from a defect someone reported.** `Remaining : 0` did not: it has never
fired, it is un-asserted, and **it is the first thing on this list found by LOOKING.**
🔑 **And the survivor-shape shows in HOW it hid:** every rule we learned this week came from a message somebody
SAW and objected to — a blank line, a wrong date, an identical notice. ⚠️ ***This one produces a message with
nothing visibly wrong with it.*** **A parent reads a COURSE DEDUCTION missing one line and has no way to know a
line is missing** — which is why no report would ever have arrived.
📌 **That is the class the reporting channel cannot reach: not *the message is wrong*, but *the message is
incomplete and looks complete*.** ⇒ **the only way to find these is the method that found this one — read what
the code does with an EMPTY or ZERO value, field by field, rather than waiting for a screenshot.**
🔑 **If you want a third task out of this, that is it, and it is bigger than one line:** ***every field in
every message, rendered with an empty and a zero value, and the result read by a human.*** **That is §4's gap
made into work, and it would be the first thing we do because we looked.**

**BALL: @Sober — TASK-330 delivered as analysis. ⛔ Nothing in `src/`. TASK-328 when you release it.**

### ✅ TASK-330 DONE and REVIEWED — @Jason. **`git status` in the BE repo CLEAN: zero changes in `src/`, exactly as the task required.**
**Verified before agreeing:** `line-message.ts:307` is `remaining: (payload.remaining as string) || undefined` ·
`remainingLabel` always returns a non-empty string ⇒ **latent, not live, as he said** · `line-schedule.ts:86`
caps and `line-today-schedule.ts` does not.

🔴 **THE FINDING — `Remaining : 0` cannot print, and nothing would tell us.** `||` not `??` ⇒ a zero balance
deletes the whole line. **Un-asserted, silent, and the compiler LOOKS like it is helping** (`payload.remaining`
is `unknown` off a JSON column; `as string` is an assertion, so the number path type-checks perfectly).
🔑 **My contribution was one grep: `as string) ||` appears THIRTEEN times in that file** ⇒ ***not one line, a
thirteen-wide class.*** 📌 **He had said he found it by reading one branch and "not by a method that would find
the others" — that sentence is what let me find the method: his own tell, pointed at a BUG PATTERN instead of
a string.**

🔑 **And it answers the Question I set: it is the first thing we have found by LOOKING rather than by being
told.** ***Not "the message is wrong", but "the message is incomplete and looks complete"*** — **the class the
reporting channel cannot reach**, because a missing LINE gives a parent nothing to object to, and omit-empty
(which the owner asked for) makes the absence look deliberate. **His own `(-)` reasoning pointed back at us.**

✅ **§2's NON-properties are worth as much as the properties**, all measured: **language-invariance is 10-of-14
DIFFERING** (a TEMPLATE-family property, and `§4`'s *eng ล้วน* makes it easy to "fix" into existence) · **the
20-char cap is a CATEGORY ERROR** (an outbox push carries no quick reply at all) · **`AUDIENCE_OMITS` is a
SETTING that is asserted, not a law.**

**TASK-331 dispatched — the sweep, ANALYSIS + TESTS ONLY, safe under @Tanya's round.** 🚫 **The `??` fix is
explicitly OUT and I named the trap: `?? undefined` alone converts an ABSENT line into an EMPTY LABEL, which
TASK-219 established reads as information that went missing** ⇒ **the fix gets cut from the full list, not
from one example.** ⚠️ **Standing instruction in it: anything LIVE ⇒ STOP and report, because that is a defect
and would not wait for a tester.**

⛔ **TASK-328 still HELD — her round has not been reported closed.** 📌 **His §5 gives a better reason for it
than I had: the labelling convention is the ONLY invariant on the map held by NOTHING in either sense.**

---

## Jason (BE) — ✅ TASK-331 DELIVERED (analysis) — nothing live; ONE of the thirteen is wrong, and the file has both `||` and `??` bugs, one per idiom. → @Sober

**ANALYSIS. `git status` in `smart-scheduler-back` is EMPTY.**
🚫 **Zero changes in `src/` — including no test file.** *(Two throwaway probes under `scripts/`, deleted; the
clean status is after.)* 📌 **`bun test` unchanged at 1990 / 160 files** — I ran nothing that could change it.

### 🟢 **NOTHING IS LIVE.** All thirteen are SAFE or LATENT — I am not stopping you.
🔑 **Checked, not assumed, for every row.** ⚠️ Two rows are *not established* and both are named.

---

## §1 THE THIRTEEN `(payload.x as string) ||` SITES, individually accounted for

| # | line | field · message | falsy → | is that RIGHT? | can the producer send it? |
|---|---|---|---|---|---|
| 1 | 139 | `attendeeNote` · `booking_confirmed` | **line absent** | ✅ **YES — it is the RULE.** `Remark` is `*ถ้ามี` (§7.3) | ✅ yes — `current.attendeeNote ?? null` |
| 2 | 163 | `studentName` · `leave_notice` | line absent | ✅ **YES** — TASK-224: *a studentless booking renders with no empty labels* | ✅ **yes, explicitly: `student?.name ?? ""`** |
| 3 | 188 | `attendeeNote` · `leave_notice` | line absent | ✅ YES — §9.1, same rule | ✅ yes |
| 4 | 235 | `studentName` · `course_confirmed` | line absent | ⚪ tolerable — the parent knows their own child | ✅ yes — `student?.nickname ?? student?.name ?? null` |
| 5 | 243 | `startDate` · `course_confirmed` | line absent | ⚪ tolerable | ⚠️ **not established** — `course.startDate`, not read |
| 6 | 244 | `coach` · `course_confirmed` | line absent | ⚪ tolerable — a coach knows it is theirs | ✅ yes — `teacher?.nickname ?? null` |
| 7 | 245 | `expiryDate` · `course_confirmed` | line absent | ✅ **YES — correct.** No expiry to state ⇒ no line stating one | ✅ yes — nullable |
| 8 | 285 | `note` · `course_confirmed` | line absent | ✅ YES — §8.1's `Remark` half of the trap | ✅ yes |
| 9 | 307 | **`remaining` · `course_deduction`** | 🔴 **line absent** | 🔴 **NO — see §2** | 🟡 **LATENT** — `remainingLabel` returns `"0 HR"` |
| 10 | 308 | `expiryDate` · `course_deduction` | line absent | ✅ YES — as #7 | ✅ yes — `input.expiryDate: string \| null` |
| 11 | 324 | `studentName` · `booking_paused`/`resumed` | **renders `-`** | ⚪ tolerable — `-` is visible, not silent | ✅ yes |
| 12 | 363 | `studentName` · `sick_leave` | renders `-` | ⚪ tolerable | ✅ yes |
| 13 | 373 | `studentName` · `leave_teacher` | renders `-` | ⚪ tolerable | ✅ yes |

🔑 **The split that matters: TEN can lose a LINE; THREE fall through to `-`.** ⚠️ **The three are the safe
shape** — a `-` is a visible admission; a missing line is not. 📌 *Same fields, two idioms, in one file.*

## §2 🔴 The one I judge WRONG — and it is #9, alone
**`Remaining` is the only field on this list whose absence removes the message's PURPOSE.** A `COURSE
DEDUCTION` exists to tell a parent what is left; without that line it is a receipt with no balance — and
**omit-empty makes the absence look deliberate.**
✅ **Every other absent line above is either the customer's own rule (`Remark`) or a true statement about
missing data (`no expiry`, `no student`).** ⇒ **#9 is not "one of thirteen". It is the one that is wrong, and
the other twelve are the reason it looked like a class.**
🚫 **Not fixed, per §4.** 📌 And your trap is real: `?? undefined` alone would turn `remaining: ""` into
`Remaining :` with nothing after it — **TASK-219's *information that went missing***. ⇒ the fix is
`?? undefined` **plus a producer-side type**, and it is one task with two halves, not a character swap.

## §3 What the sweep found BEYOND the thirteen
**Rendered every payload field of every message with `""`, `0` and `null` and read the output.**
- 🟡 **`size` / `total` → `Program : Freeskate`** — the hours silently drop off. **Not a missing line, a
  DEGRADED value.** ✅ **Safe: `PackageSize = 4 | 6 | 10` and the column is `smallint notNull`**, and for a
  VOUCHER `programLabel` ignores size entirely. ⚠️ **But worth one sentence:** since TASK-318 removed
  `Sessions :`, **the program label is the ONLY place the hours appear** — the redundancy that would have
  caught a bad size is the line the customer asked us to delete. *(Correctly deleted; noted, not reopened.)*
- 🟡 **`weeks` · `makeup_far_out`** — `String(payload.weeks ?? "-")`: `null` → `-`, but **`""` passes `??` and
  renders *"…: ⟨blank⟩ สัปดาห์…"*, a half-sentence.** Same for `replaces` → *"( → 2027-03-09)"*. **LATENT** —
  both are computed numbers/dates and the message only sends when `searchExhausted` is true.
  🔑 **Different failure from the thirteen: `??` guards null but not `""`, so it is the MIRROR of `||`.**
  ⇒ **the file has both bugs, one per idiom.**
- ✅ **No empty message, no `Label :` with nothing after it, anywhere** — measured across all kinds.

## §4 What this sweep did NOT cover
🚫 **Producers of five payloads I did not open** (`booking_paused`, `booking_resumed`, `teacher_assigned`,
`teacher_unassigned`, `reschedule_requested`) — all three of their falsy paths render `-` or omit a `line()`,
so **the renderer is safe whatever they send**; I did not verify what they send.
⚠️ **Rows 5 (`startDate`) — not established.** I did not read `coursePackages.startDate`'s nullability.
🚫 **`ctx` fields** (`date`, `startTime`, `coach`, `subject`) — they come from `bookingContext`, not a payload,
and are outside the `as string) ||` class. **Not swept.**
🚫 **`daily_reminder` / `daily_digest` rows** — their own renderers, own `dash()` guard. Not swept.

---

## ❓ THE QUESTION — **the two are not alternatives. A payload TYPE that is honest IS "never receive an `any`".**

**A single `OutboxPayload` type cannot exist as a flat interface** — fourteen kinds carry different fields, so
the only honest type is a **discriminated union on `kind`**. ⇒ **"put a type on the payload" and "the renderer
should not receive an unconstrained `any`" are the same fix**, one stated weakly and one stated properly.
📌 **What makes it real rather than cosmetic: the union must be PARSED at the boundary, not CAST.** `row.payload
as any` is where type information is *destroyed* — the producer knew `remaining` was a string, the DB column
knows nothing, and `as string` re-asserts a fact nobody checked. **A cast at that line changes nothing; a parse
changes everything.**

### 🔑 The argument I would make to you in a month, when you say "we have thirteen assertions, that is enough"
**Assertions and a parsed union fail at different times, and only one of them scales:**
1. 🔴 ***The thirteen assertions cannot fail for the FIFTEENTH KIND.*** They are per-field, per-message,
   written against the fourteen that exist. **A kind added next month inherits none of them** — which is the
   exact sentence from TASK-327, and the reason we stopped counting `5/14` and started asking *does branch
   fifteen get it free?* **A parsed union makes the new kind declare its fields or fail to compile.**
2. ⚠️ **A test can only cover the payloads I thought to write.** I found #9 by reading a `||`, and I said so —
   *not by a method that would find the others*. **You then found the method, and it was a grep for an IDIOM.**
   ⇒ **the next class will have a different idiom, and no assertion written today points at it.**
3. 📌 **The assertions live on the wrong side of the boundary.** They test *the renderer, given a payload*.
   **Nothing anywhere compares what a producer WROTE against what a branch EXPECTS** — and that comparison is
   the whole defect. A union is the only artefact that sits in both places at once.
4. ✅ **And the honest counter-argument, so it is on the record:** a union is a real cost — fourteen shapes, a
   parse at the read, and **every historical row in `notification_outbox` must still parse or the worker starts
   failing on data it used to send.** 🔑 **That is not a reason to prefer assertions; it is the reason to do it
   with a lenient parse that LOGS rather than throws** — the same shape as `notifyAdmins`' loud SKIPPED row.

⇒ **My answer: the union, parsed at the outbox read, lenient and logging.** 🚫 **Not thirteen `??`s and not
thirteen more tests** — *those hold the fourteen we know about, and the map already told us that is the
signature of a property held by repetition.*

**BALL: @Sober — TASK-331 delivered as analysis. 🟢 Nothing live. ⛔ Nothing in `src/`. TASK-328 when you
release it.**

### ✅ TASK-331 DONE and REVIEWED — @Jason. **`git status` in the BE repo EMPTY; nothing in `src/`, not even a test file.** 🟢 **Nothing LIVE.**

🔻 **THE SWEEP CORRECTED ME, and the error is the one I have twice told him not to make.** I counted
`as string) ||` thirteen times and reported **a thirteen-wide class**. He read all thirteen with a producer
column: **ten can lose a LINE, three fall through to `-`, and TWELVE ARE CORRECT** — `Remark` is the
customer's `*ถ้ามี` rule, an absent `expiryDate` is a true statement, an absent `studentName` is TASK-224's own
decision. 🔴 ***I treated a COUNT as a FINDING.*** 📌 **The count was a place to LOOK and I reported it as a
result — and it saved him no reading, it only said where to start.**

🔑 **His criterion for the one that IS wrong is better than "it is falsy":** ***a field's absence is a defect
when it removes the MESSAGE'S PURPOSE*** — a `COURSE DEDUCTION` without `Remaining` is a receipt with no
balance. **That is reusable on the next one without re-deriving it.**

🔴 **And the sharpest thing in the sweep is the MIRROR: `||` eats a legitimate ZERO; `??` lets an EMPTY STRING
through** (`String(payload.weeks ?? "-")` renders a half-sentence). ⇒ ***the file has both bugs, one per
idiom*** — **so "fix the `||` with `??`" walks straight into the opposite defect.**

✅ **I closed one of his two *not established* rows myself:** `coursePackages.startDate` is `notNull()`
(`schema.ts:346`) ⇒ **SAFE, not unknown.** 📌 *One grep — cheap only because he wrote "not established"
instead of guessing "no".*
📌 **Also banked: since TASK-318 removed `Sessions :`, the program label is the ONLY place a course's hours
appear** — the redundancy that would have caught a bad `size` is the line the customer asked us to delete.
**Correctly deleted; noted, not reopened.**

**TASK-332 dispatched** — `Remaining` must be able to say ZERO. **LATENT ⇒ invisible on any message @Tanya can
produce**, which is what makes it safe today. ⚠️ **Two halves, and `?? undefined` alone is NOT the fix**: it
turns an absent line into a bare label (TASK-219). 🚫 **The other twelve stay untouched and asserted, with the
reason, so nobody consistency-fixes the customer's own rule out of the product.**

**TASK-333 CUT AND HELD** — after @Tanya and after TASK-328. ✅ **His design accepted without re-opening: a
discriminated union on `kind`, PARSED at the outbox read.** 🔑 ***`row.payload as any` is where type
information is destroyed — a cast changes nothing, a parse changes everything.*** 🔴 **I made his own
counter-argument the SPEC: *no message that sends today stops sending*.** 📌 **First half is DESIGN ONLY, four
questions, no code** — including the one I most want answered honestly: ❓ *does a shape parse catch
`remaining: 0` at all, or is that still only a producer-side type?*

⏱️ **State: batch complete · @Tanya on `uat`, unreported · @Fern on TASK-329 · @Jason on TASK-332 ·
TASK-328 and TASK-333 both HELD.**

---

## Jason (BE) — ✅ TASK-332 DONE (code) — `Remaining : 0` can be said, and the `??` mutation proved the trap is DEEPER than either of us said. → @Sober **2001 pass / 0 fail**, 161 files · 🚫 **35 `.sql` = 35 journal tags.**
📌 Typecheck: `bunx --package typescript@5.6.3 tsc --noEmit` → **0**. 🚫 No migration, no FE change, no copy.
✅ **NOTHING @TANYA CAN SEE CHANGES** — `remainingLabel` returns `"0 HR"` / `"0/6 ครั้ง"`, a non-empty string,
so every message that exists today renders **byte-for-byte as before.** *(Asserted, not claimed.)*

- [x] 🔑 **`remaining: 0` renders `Remaining : 0`** — the assertion that did not exist
- [x] 🔴 **`remaining: ""` does not produce a bare label** — asserted as an absence, ⚠️ **and the mutation proves
      the obvious fix would have failed it**
- [x] **Both halves** — the renderer guards, and `deductionPayload`'s return type is now DECLARED
- [x] 🚫 **The other twelve are untouched** — count asserted **with the reason in the source**
- [x] **`§3` answered** — `weeks` LEFT, reason below, and 🔑 **the two-idiom sentence is in the code**
- [x] 🔑 **Break it and watch — TWO mutations, both in ONE tool call**, restore verified **byte-identical**

### §2 ✅ The expression: a HELPER, and the mutation showed the trap is DEEPER than either of us said
```ts
const fieldValue = (v: unknown): string | undefined => {
  if (v === null || v === undefined) return undefined;
  const s = String(v);
  return s.trim() ? s : undefined;   // `0` → "0" · `""` and `"   "` → absent, never a bare label
};
```
**I ran your `?? undefined` as mutation B, and it fails TWO assertions, not one:**
1. ⚠️ **It does not even fix the ZERO.** `(payload.remaining as string) ?? undefined` leaves `0` a NUMBER, and
   **`fieldLines`' own omit-empty check is `facts[f] ? …` — also falsy** ⇒ the line is dropped one layer
   further down. 🔑 ***The fix has to produce a NON-EMPTY STRING, not merely a defined value*** — which no
   operator can express.
2. 🔴 **And your bare-label trap is real, on WHITESPACE rather than on `""`.** `"" ?? undefined` → `""` → still
   falsy → still absent. But **`"   " ?? undefined` → `"   "` → TRUTHY ⇒ `Remaining :    `** — a label with
   nothing after it. **TASK-219's *information that went missing*, reachable through the space bar.**
⇒ 📌 **Three conditions — reject null/undefined, coerce to string, reject whitespace-only — and an operator
states one.** **That is why it is a helper, and the reason is written above it.**

### §3 ❓ `weeks` — **LEFT, and it is not close**
✅ **The two-idiom sentence is in the code**, at `fieldValue`, which is where the next person reaching for `??`
to "fix" a `||` will be standing:
> ***`||` eats a legitimate `0`. `??` lets an empty string through. Swapping one operator for the other does
> not fix anything; it trades one failure for its mirror.***
🚫 **Not fixed, and the reason is stronger than "different severity":** **`weeks` is `weeksBetween(...)`, a
computed NUMBER** ⇒ **`""` is not merely unlikely, it is unreachable from the producer.** ➕ Its failure would
be a half-sentence, which a human reports. ⇒ **latent AND unreachable AND loud** — three reasons, where
`remaining` was latent-but-reachable-in-principle AND silent.
📌 **If you want it anyway it is one `fieldValue` call**, and I would rather you asked than have me widen a
task that exists because a sweep found ONE defect.

---

## ❓ THE QUESTION — **your distinction survives, and there is a cheap test for it BEFORE the reading.**

**Your instinct is right:** *a count is evidence when every instance implements one RULE; only a place to look
when the instances share a SHAPE.* ✅ **It holds against all four I have swept this week:**
| count | rule or shape? | was the count evidence? |
|---|---|---|
| `withExit` × 11 (TASK-323) | **RULE** — *every question advertises the exit* | ✅ **YES — it CHANGED the task**: it is what showed the customer's "two screens" was under-scoped |
| `.trimEnd()` × 5 of 14 (TASK-325) | **RULE** — *no message ends in whitespace* | ✅ **YES** — 5-of-14 said instantly that nine were unprotected |
| `t(key, lang, {…})` × 5 (TASK-327) | **RULE** — *no placeholder leaks* | ✅ **YES** — it separated the 9 safe-by-construction from the 5 safe-by-habit |
| `as string) \|\|` × 13 (TASK-331) | 🔴 **SHAPE** | 🚫 **NO — 12 of 13 were correct** |

### 🔑 And the cheap test, which is the part you can run BEFORE reading
***Try to write, in ONE sentence, what all the instances are FOR. If the only sentence you can write describes
what they LOOK LIKE, it is a shape.***
- `withExit` → *"every question advertises the exit."* ✅ a purpose.
- `.trimEnd()` → *"no message ends in whitespace."* ✅ a purpose.
- `as string) ||` → *"…they all coerce a payload field with `||`."* 🔴 **that is the syntax.** ⚠️ And the reason
  no purpose-sentence exists is visible in the answer: **the thirteen had THREE different intentions** — a
  customer RULE (`Remark` is `*ถ้ามี`), a TRUE STATEMENT (no expiry ⇒ no line), and a DEFECT (`remaining`).
  **One operator, three meanings.**
📌 **The test costs one sentence and it is falsifiable** — if you cannot write it, you have a place to look.

### 🚫 So: NO, do not stop reporting counts
**`withExit`'s count was the finding.** Suppressing it would have cost the thing that made TASK-323 correctly
scoped. 🔑 **The asymmetry is what makes the rule safe:** **calling a rule-count "only a place to look" costs
nothing** — you were going to read them anyway. **Calling a shape-count a finding creates work** — I swept
thirteen rows to retire twelve. ⇒ ***when the one-sentence test is unclear, default to "a place to look".***

⚠️ **And one correction to your own note, because it cuts the other way:** you said *"the count saved you no
work"*. **True for `|| undefined`. Not true for `withExit`** — there the count was the whole content of the
task. 🔑 **Counts pay off asymmetrically, which is exactly why the cheap test is worth running rather than
adopting a blanket rule in either direction.**

**BALL: @Sober — TASK-332 ready for review. ⛔ Nothing else is on me. TASK-328 and TASK-333 when you release
them.**

### ✅ TASK-332 DONE and REVIEWED — @Jason. **Re-run by me: 2001 pass / 0 fail (161 files, 6157 expects), 35 = 35, tsc 0.**
**Read back:** `fieldValue` at `line-message.ts:78`, used at `:335` · `deductionPayload` now DECLARES its
return type · the only mutation-shaped hit in `src` is TASK-314's explanatory comment.

🔻 **I PRESCRIBED THE FIX AND IT WAS WRONG TWICE — and only RUNNING it separated the two.**
**I wrote *`?? undefined` plus a producer-side type* and warned about a bare-label trap on `""`.**
🔴 **It does not even fix the ZERO:** the value stays a NUMBER and **`fieldLines`' own omit-empty (`facts[f] ?
…`) is falsy too**, so the line drops one layer further down, where I was not looking.
🔴 **And my trap was real with the wrong mechanism:** `"" ?? undefined` stays falsy and absent — **it is
`"   "` that renders `Remaining :    `**, ***TASK-219's information that went missing, reachable through the
space bar.***
⇒ 🔑 ***The fix must produce a NON-EMPTY STRING, not merely a defined value — three conditions, and an
operator states one.*** ✅ **Hence a helper, with the reason above it.**
📌 **I named the right DANGER with the wrong CAUSE. Reading could not tell them apart; running it did — the
rule I gave him and had not applied to my own instruction.**

✅ **`weeks` LEFT, accepted:** `weeksBetween(...)` is a computed NUMBER ⇒ `""` is **unreachable** from the
producer, and the failure would be a half-sentence a human reports ⇒ **latent AND unreachable AND loud**,
against `remaining`'s **latent AND reachable-in-principle AND silent.** **The two-idiom sentence sits at
`fieldValue`, where the next person reaching for `??` to "fix" a `||` will be standing.**

🔑 **HIS QUESTION ANSWER GIVES ME SOMETHING RUNNABLE, and I adopted it:** ***try to write in ONE sentence what
all the instances are FOR; if the only sentence describes what they LOOK LIKE, it is a shape.*** **It separates
this week's four counts correctly**, and the reason the thirteen had no purpose-sentence is that they carried
**three intentions — a customer RULE, a TRUE STATEMENT, and a DEFECT.** ***One operator, three meanings.***
✅ **He also corrected my correction:** *"the count saved you no work"* was true for `||` and **false for
`withExit`**, where the count WAS the content of the task. 🔑 **The asymmetry makes it usable: calling a
rule-count *a place to look* costs nothing; calling a shape-count *a finding* creates work** ⇒ **default to
"a place to look" when the test is unclear.** 🚫 **So I keep reporting counts.**

### 🔓 I RELEASED TASK-333's DESIGN HALF — my own hold was over-broad
⛔ I had written *"after @Tanya AND after TASK-328"*. 🔻 **That ordering was about the CODE half.** **The
design half is ANALYSIS ONLY** ⇒ **by my own rule the safest work available during a tester's round** — safer
than TASK-327 was. ✅ **Released; the code half stays held.**
🔑 ***A hold should be scoped to the RISK, not to the TASK*** — an over-broad one idles an engineer for a
reason that applies to only part of the work.
🔴 **The question in it that decides the union's value: *does a SHAPE parse catch `remaining: 0` — a valid
string field receiving a number — or is that still only a producer-side type?*** ⚠️ **I asked for a plain "no"
if that is the answer: better to learn the union is narrower than it sounds now than after fourteen shapes are
written.**

⏱️ **State: batch complete · @Tanya on `uat`, still unreported · @Fern on TASK-329 · @Jason on TASK-333's
design half · TASK-328 and TASK-333's code half HELD.**

---

## Jason (BE) — ✅ TASK-333 DESIGN HALF delivered — and the inventory found THREE LIVE kinds rendering the generic default. → @Sober 🚫 **NOTHING IN `src/` FROM THIS TASK.**
📌 `git status` carries **only TASK-332's three files** (`line-message.ts`, `course-deduction.ts`,
`remaining-zero.test.ts`), already reported and awaiting the human's commit. **TASK-333 added nothing.**
*(Two throwaway probes under `scripts/`, deleted.)*

---

# 🔴 STOP — THE INVENTORY FOUND SOMETHING LIVE, AND IT IS NOT A TYPE PROBLEM

**Answering `§3`.3 required listing every payload KIND a producer emits against every kind the renderer has a
`case` for. They are not the same list, and the gap is live today.**

### **THREE kinds are enqueued and rendered as `🔔 แจ้งเตือนจากระบบตารางเรียน` — the generic default.**
| kind | producer | what the reader gets today |
|---|---|---|
| 🔴 **`teacher_link_approved`** | `teacher-link.service.ts:160` | **`🔔 แจ้งเตือนจากระบบตารางเรียน`** — and **the payload CARRIES the right text**, `text: t("verify_teacher_ok", …)` = *"ผูกบัญชีครูสำเร็จ ✅ (ก้อง)…"* ⇒ **the renderer never reads `payload.text`** |
| 🔴 **`student_registered`** | `line-webhook.service.ts:780` | the same generic line — **`studentName` and `parentPhone` are on the payload and unread** |
| 🔴 **`parent_asked_for_admin`** | `line-webhook.service.ts:1072` | the same generic line — **an admin is pinged that *somebody* wants a human, with no idea who** |
**Measured, not read:** I rendered all three through `formatOutboxMessage`. **All three return the default.**

🔑 **The `teacher_link_approved` one is the sharpest, because the code says so itself.** The comment directly
above that send reads:
> *"The bot promised 'you'll be told once it's approved' — not sending would make it a lie."*
⇒ **the send happens and the message says nothing.** **The promise is kept in form and broken in content.**
⚠️ **And it is not silent-invisible like `Remaining : 0`** — a teacher DOES see something. **It is worse in one
way and better in another:** they get a notification they cannot act on, and they have no reason to think a
better one existed. 📌 *`student_registered` is TASK-152's lesson inverted: that task made a SKIPPED row loud;
this row is SENT and mute.*

🚫 **I have not touched any of it** — TASK-333 is design-only and this is not a type failure, it is **a missing
branch**. ✅ **It is one `case` each, or one `payload.text` passthrough for the first.** ⚠️ **But it is a COPY
decision for the two admin ones** (what should an admin alert say?) ⇒ **@Porter's, not mine.**
🔴 **Cut it as its own task and it does not wait for TASK-328** — *nothing about it is a visible-label change.*

---

# §3 THE FOUR ANSWERS

## 1 · WHERE the parse happens — **the outbox read, not the renderer's entry**
✅ **`outbox.service.ts:78`, the line that today reads `formatOutboxMessage(row.payload as any, …)`.**
- 🔑 **That is where the `any` is CREATED.** A parse anywhere else is downstream of the lie.
- ✅ **It has exactly ONE non-test caller** — measured: `formatOutboxMessage` is called from `outbox.service.ts`
  and nowhere else in `src/`. ⇒ **one home, and it cannot be bypassed.**
- 🚫 **NOT at `formatOutboxMessage`'s entry.** That function is PURE and is called from ~20 test files with
  hand-built payloads. **Parsing there turns a runtime boundary into a test burden** and would make every
  existing message test satisfy a schema to assert a string. 📌 *The renderer's job is to render; the
  boundary's job is to know what came out of the database.*
- ✅ **And the row is where the RECORD belongs** — `notification_outbox` already has an `error` column and the
  `skipReason` convention (TASK-152). **The parse's output has somewhere to go that a human already reads.**

## 2 · What a FAILED parse does — **log and send WHAT IT CAN. The other two are refuted by the spec.**
| option | what the PARENT sees | verdict |
|---|---|---|
| log and **skip** | 🔴 **nothing at all** | 🚫 **violates `§2` outright** — *"no message that sends today stops sending"* |
| log and send **raw** | 🔴 **JSON in their LINE** | 🚫 never |
| ✅ **log and send what it can** | **exactly what they see today** | ✅ **the only one compatible with the spec** |

### 🔴 And the concrete historical-rows plan, because *"lenient"* is not one
✅ **Ship the parse in OBSERVE-ONLY mode. It does not gate rendering at all.**
**A row that lacks a field the union requires renders EXACTLY as today** — the renderer's omit-empty already
handles a missing field — **and the parse writes one log line naming the row id, the kind, and the field.**
⇒ 🔑 **We then READ those logs for a period and learn which historical shapes exist**, and only then decide
whether anything should become authoritative. 📌 ***The union's first job is to TELL US WHAT IS IN THE TABLE,
not to enforce anything.*** ⚠️ **That is the only version of this that cannot cause an outage**, and the rows
most likely to fail are the oldest — from the period when we were least careful, which is exactly the data
nobody can re-create.

## 3 · How a NEW kind is added afterwards — **one place, and only if a second discipline comes with it**
🔴 **Today it is worse than "one place": you can add a PRODUCER and forget the renderer entirely, and nothing
fails.** **That is not hypothetical — it has happened three times** (§ the live finding above).
✅ **With the union as the SOURCE of `kind`, and the switch checked exhaustively against it** (a `never` in
`default`, or a `Record<Kind, …>` — **the pattern this repo already uses three times**: `Record<BookingStatus,
Entry>`, `Record<AttentionKey, Entry>`, `Record<TemplateKey, readonly FieldKey[]>`) ⇒ **adding a kind means
editing the union, and the compiler names the branch you have not written.**
⚠️ **But that only binds the RENDERER.** 🔑 **A union cannot make a producer use it** — a producer builds an
object literal and hands it to `enqueueLine(payload: unknown)`. ⇒ **"one place" requires payload CONSTRUCTORS:
one typed function per kind, the way `deductionPayload` already is.** 📌 **That is the honest cost, and
`deductionPayload` is the model — it is already exactly this.**

## 4 · What it does NOT fix — **and the answer to your `§3`.4 is "yes, but it does not help as much as it sounds"**

❓ ***Does a SHAPE parse catch `remaining: 0`?***
✅ **YES — if the union declares `remaining: string`, a number fails.** 🔻 **But two things make that much
narrower than it sounds, and you asked me to be honest rather than encouraging:**
1. 🔴 **After TASK-332 the renderer handles `0` CORRECTLY** (`fieldValue` → `"0"`). ⇒ **a union declaring
   `string` would now REJECT a payload the product renders perfectly.** 🔑 ***The union must describe what
   producers ACTUALLY send — historically — not what we wish they sent***, or strictness re-creates the exact
   outage risk `§2` forbids.
2. 🔴 **In observe-only mode, catching it produces a LOG — not a correct message.** The parent still gets
   whatever the renderer makes of it.
⇒ 🔑 ***The parse DETECTS. The producer type PREVENTS. They are complementary, and neither alone closes the
class.*** 📌 **That is the sentence I would want in `SYSTEM-FACTS`, because it is the thing I would otherwise
have to re-derive when someone says "we have the union now".**

**It also does NOT fix:**
- 🚫 **A WRONG VALUE of the RIGHT type.** `remaining: "3 HR"` when the true balance is 2 passes every parse
  ever written. ⚠️ **`Remaining : 0` happened to be a TYPE problem. The next one may not be**, and a union
  would be silent about it.
- 🚫 **The three unrendered kinds above.** They are a MISSING BRANCH, not a bad shape. ✅ *An exhaustive switch
  over the union would surface them — but that is the EXHAUSTIVENESS, not the parse.*
- 🚫 **`ctx`.** `bookingContext` returns a typed `MessageContext` already; it is not on this boundary.

## ➕ How big is the code half — **the number is SEVENTEEN, not fourteen**
| | count | |
|---|---|---|
| kinds the renderer has a `case` for | **14** | |
| kinds a producer actually emits | **14** | |
| **in both** | **11** | the ordinary ones |
| 🔴 **renderer-only — DEAD branches** | **3** | `reschedule_requested` · `sick_leave` · `leave_teacher` *(no producer anywhere; the last two went unsent as a side effect of my own TASK-305, which I reported at the time)* |
| 🔴 **producer-only — render the DEFAULT** | **3** | the live finding above |
⇒ ✅ **17 distinct kinds.** 🔑 **The union itself is the small part** — one file, one parse call, one
exhaustiveness check. ⚠️ ***The work is the six anomalies***, and each needs a decision rather than a type:
**delete the three dead branches, and give the three unrendered kinds a message (copy, so @Porter's).**
📌 **That is worth knowing before agreeing to it, and it is why I would do the LIVE three first, separately.**

---

## ❓ YOUR QUESTION — **yes, there is a case, it is in this product, and we already decided it the other way**

🔴 **`REQ-085 §8.1` — the two opposite empty-field rules.** `**Advance Leave Notice` ALWAYS prints `(-)`;
`Remark` vanishes entirely. **Both at the bottom of the same message.**
⇒ **A chokepoint — one "empty field policy" at the field renderer — would have to carry BOTH**, and the REQ
calls this *the trap* precisely because the two look like one rule. 🔑 **TASK-284 asserted them SEPARATELY on
purpose**, and the note in that file is the argument: ***"one 'empty fields' test cannot catch a swap"***.
✅ **That is a case where the assertions are better and we already chose them.**

### 🔑 The criterion, stated so it can be applied rather than admired
***A chokepoint is right when the invariant has ONE answer for every instance. When instances legitimately
DIFFER, the chokepoint has to carry a table of exceptions — and a table of exceptions is the per-instance
knowledge you were trying to escape, moved somewhere nobody reads it against the message it applies to.***
📌 **Test it against the four:** *no trailing whitespace* — one answer, chokepoint ✅. *No placeholder leaks* —
one answer ✅. *`(-)` vs absent* — **two answers by design** ⇒ assertions ✅. *The labelling convention* — one
answer, **but see below.**

### ⚠️ And a SECOND edge, which is the one that bit TASK-328 and which you found before I did
***A chokepoint can only enforce what it can DISTINGUISH at the point it acts.***
**The trim works because trailing whitespace is recognisable in a finished string without knowing what the
string MEANS.** 🔴 **A colon is not**: it is a label separator, and it is also a character in `📅CONFIRMED
SCHEDULE:` and in a parent's typed `Remark`. ⇒ **normalising at the exit cannot tell them apart, and that is
not a limitation of the idea — it is the reason the invariant does not belong there.**
🔑 **So the two questions are: *does it have one answer?* and *can the chokepoint SEE the difference from where
it stands?*** **`§8.1` fails the first. The labelling convention fails the second.**

### 📌 And one honest third, since you asked for the edge rather than the endorsement
**A chokepoint on the unavoidable path is a single point of failure.** The trim is safe because it is
**idempotent and cannot corrupt** — the worst it can do is nothing. ⚠️ **A NORMALISING chokepoint that got it
wrong would damage all fourteen messages at once**, where a wrong assertion breaks one test and is loud.
⇒ 🔑 **the principle is strongest for chokepoints that can only SUBTRACT (trim, omit) and weakest for those
that REWRITE.** 📌 *That is the shape of the edge, and it explains both of my own answers this week without
having to appeal to taste.*

**BALL: @Sober — design half delivered, no code. 🔴 THREE LIVE unrendered kinds — cut them as a task and they
do not wait for TASK-328. ⛔ TASK-333's code half stays held.**

### 🔴🔴 A LIVE DEFECT, FOUND BY LOOKING — three notifications send the generic default line
**@Jason found it while answering TASK-333's design question *"how is a new kind added?"*, which forced him to
put two lists side by side.** ✅ **I verified every step myself:** renderer `case`s = 14, producer kinds = 14,
**three do not overlap**, and all three reach the outbox through `enqueueLine` — the teacher one directly, the
two admin ones via `notifyAdmins`.
🔴 **`teacher_link_approved` CARRIES the right translated sentence on its payload and the renderer never reads
it**, while the comment above that send says *"the bot promised you'll be told once it's approved — not
sending would make it a lie."* ⇒ ***the send happened and the message said nothing: kept in form, broken in
content.*** 📌 **The mirror of TASK-152 — that task made a SKIPPED row LOUD; these are SENT and MUTE.**
📌 **Second thing this week found by LOOKING; unlike `Remaining : 0`, this one is LIVE** — and it has been
shipping for as long as those three features have existed.

**TASK-334 cut, and DELIBERATELY NOT started:** **PART A** (the teacher one — needs no copy, the sentence is on
the payload) **waits on @Porter**, because it changes a message @Tanya may be reading. 🔑 **I recommended YES:
*"do not move the build under a tester" exists to stop CHURN, not to protect a live defect she may be about to
file as a bug*** — 🚫 **but he holds her.** **PART B** (the two ADMIN kinds) **is blocked on COPY from the
owner** — 🚫 *anything I wrote would be a placeholder, and an admin alert read under time pressure is the worst
place to ship one.* ✅ **Escalated to @Porter with both decisions separated by size.**

### ✅ TASK-333 DESIGN HALF accepted — three things better than what I asked for
1. **OBSERVE-ONLY beats "lenient":** the parse does not gate rendering at all; it writes one log line naming
   row, kind and field ⇒ ***the union's first job is to TELL US WHAT IS IN THE TABLE, not to enforce
   anything.*** **Parse at `outbox.service.ts:78` where the `any` is created — not at the renderer's entry,
   which is pure and called from ~20 test files.**
2. 🔴 **The honest limit I asked for, and it changes the union's worth:** ***the parse DETECTS, the producer
   type PREVENTS, neither alone closes the class*** — **and after TASK-332 a union declaring `string` would
   REJECT a payload we now render correctly** ⇒ ***it must describe what producers ACTUALLY send, not what we
   wish they sent.***
3. **SEVENTEEN kinds, not fourteen** — 11 shared, 3 dead branches, 3 unrendered. ***The union is the small
   part; the six anomalies are the work.***

### 🔑 And his answer to my Question is the most useful thing in the report
I asked for the EDGE of a principle I had accepted twice without testing. **He gave three, and the first is a
case we already decided the other way:** `§8.1`'s two opposite empty-field rules in ONE message ⇒ **a
chokepoint would carry a table of exceptions**, and ***a table of exceptions is the per-instance knowledge you
were escaping, moved somewhere nobody reads it against the message it applies to.***
✅ **Two criteria adopted verbatim: does the invariant have ONE answer for every instance, and can the
chokepoint SEE the difference from where it stands?** 📌 **And the third edge I would never have found: *a
chokepoint on the unavoidable path is a single point of failure — the principle is strongest for chokepoints
that can only SUBTRACT and weakest for those that REWRITE.*** 🔑 **It explains both of his own answers this
week without appealing to taste, which is the test of a real rule.**

⏱️ **State: @Jason EMPTY until @Porter answers · @Fern on TASK-329 · TASK-328 and TASK-333's code half HELD.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid` (back + front). @Tanya released on the big batch; the owner walks LINE in parallel.
🔑 **@Sober held a CUT task (`TASK-328`) until her round closes** — **he applied the build-freeze rule himself,
unprompted, after I had to stop her three times in two days.**
📌 **Her five: `ลา` across days · `Last session` · the human time on the expiry warning · `TASK-320`'s
course-creation note (**the owner's own reproduction as the DoD**) · the four-child family.**
🔻 **Item 4 carries the label "this is the one we reported DONE and was not"** — **she should know which check
is re-proving a claim rather than testing new work.**
