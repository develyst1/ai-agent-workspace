# 2026-09-15

## Porter (PM) — owner: **`uat` deployed with `REQ-088` complete (§1–§10) + note cap 2000.** No migration (35=35). Board TASK-347…356 marked on `uat`. Two XS copy nits (Unlink label truncation, `1 child`) still with @Sober, ride the next deploy. QA: nothing on `uat` (LINE out of scope, read-only box). Owner verifies once on the customer's OA.

## Porter (PM) — 🔴 **customer batch (9 items, "before rent/other") recorded verbatim as `REQ-089`** with my reading per item; ❓ on 3 (delete = history-free only?), 4 (where to show), 5 (who/where), 8 (confirm today's behaviour). Owner asked me to say what I understand and ask what I don't. Nothing dispatched.

## Porter (PM) — `REQ-089` rulings in (§2): 6 out (owner's FE team), 3 = history-free delete only, 4 = admin schedule, 5 = `Last` badge on the schedule, 8 = yes. **Dispatched to @Sober, 8 items, order his.**

## Sober (SA) — REQ-089 read; order set; two tasks cut
- `§10` passed on the owner's phone; two copy defects (Unlink truncation, `1 children`) folded into **TASK-357 @Fern (S)** with item 0 `SOM SCHEDULE` everywhere.
- **TASK-358 @Jason (S)**: item 2 — `courseBornCeiling` = base ceiling + leave weeks (Kavya ⇒ week 11). Prior fact: TASK-301's "stretch from lastPlanned by absences" is the cause.
- Order for the rest sent to @Porter: 8 → 1 → 5 → 4 → 7 → 3 (delete last, destructive). Item 6 out (owner's FE team).

## [--:--] Fern (FE) — ✅ **TASK-357 done — `SOM SCHEDULE` everywhere the frontoffice reads the name (16/16 routes from the build); the Unlink label shortened AND wrapping; `1 child`.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  330 pass / 0 fail   (was 327; +3)
bun run build     → ok — 16 of 16 prerendered <title>s are SOM SCHEDULE; grep "Smart Scheduler" src = zero outside tests
```
Nine files. The six sites replaced (root title · login · `APP_NAME` · header fallback · `brand.appName` ×2); the two
page-level titles from TASK-355/356 REMOVED as redundant; TASK-356's test FLIPPED, not deleted — "exactly these two
pages, root stays" → "every route from the root, no page sets its own", history in the comment. A walk of every
non-test file under `src` (comments stripped) pins the old name at zero. 🚫 Filenames, `package.json`, env vars,
`ss.lang.*`, identifiers untouched — asserted. `<meta description>` left word for word. Notes in the TASK §4.

**§2** — EN `Unlink this family's LINE` (25) · TH `ปลด LINE ของครอบครัวนี้` (22); meaning kept (`family`/`ครอบครัว`
asserted). **How checked:** derived — 360 − 32 − 64 = 264 px button, −36 −16 −8 ⇒ ~204 px for the label at 14 px
≈ 29 chars is the cliff (old label 35); pin ≤ 26 both. Not a browser measurement ⇒ **both red buttons also WRAP**
(`WRAP_LABEL`), the safety net for whatever words @Porter chooses. **§3** — `alreadyLinkedToOne` `(1 child)`, one
ternary in the one place; TH the same sentence under both keys, untouched. 📌 `createdCount` still `child(ren)` —
not on the screenshot, named.
🔑 **Break-and-watch ×6** (root title back · page title again · header reads old name · 35-char label · wrap removed ·
`1 children`) ⇒ 2/1/1/1/1/1 fails; md5 identical on five files.

### Q — the BE's old name, as a parent reads it: **nothing.** ICS `PRODID:-//Smart Scheduler//…` is on the TEACHERS'
feed and `PRODID` is never displayed (the calendar name is `ตารางสอน <nickname>`); LINE messages carry no product
name; rich-menu names are LINE-API internals; no email exists; `external_source = 'smart-scheduler'` is a STORED
discriminator and must stay. One cosmetic line, @Jason's. 🚫 Named, not touched.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-357 DONE, reviewed (330/0). Told @Porter; deploy waits for TASK-358. `.next.zip` untracked in FE root flagged.

## 2026-09-15 — Jason (BE)

### ✅ TASK-358 — expiry with advance leave = the BASE ceiling + the leave weeks (`REQ-089 item 2`) — **DONE (code)**
**2121 pass / 0 fail**, 172 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · two source lines, five pins rewritten, one new test file.
🔑 **Before: `max(base, lastPlanned + absences)` — the stretch from the plan's LAST SESSION put the ceiling ON the last make-up and let the leaves eat the quota: Kavya = week 9.** **Now: `max(base, lastPlanned) + absences weeks` — the BASE (plan end + quota) plus the absent weeks: week 8 + 3 = week 11.** Pinned by the customer's numbers, with week 9 asserted absent; 0 leaves ⇒ week 8 unchanged; never shrinks. **Unit: DISTINCT WEEKS declared absent, asserted at the call.**
✅ **The resume path is the SAME function with `absences = 0` — `max(currentExpiry, lastSession)`, unchanged — not a copy;** `resumeCourse` has no arithmetic of its own; the preview uses the same function.
🔻 **Five pins moved UP from week 7 to week 8 with their claim intact; one is a REVERSAL and says so** — `REQ-089` reverses TASK-308's OUTCOME at creation, not its reasoning: no separate quota term, the stretch starts from the base. **SYSTEM-FACTS replacement sentence delivered; the older TASK-301/302 block was already stale (a four-argument signature TASK-308 removed) — flagged.**
🔑 **Three mutations bite.** 🔻 **TASK-342's rule caught its author: a bare `indexOf` end anchor made this task's own new region EMPTY on the first run.** Rewritten from the start index with a positive over the same region.
❓ **The Question is a DATA REQUEST for @Porter:** live `course_packages` with any booking `planned_at_creation = true` — every such course born under the old formula has a stored `expiry_date` below the new one; the sentence for the human and the paper arithmetic are in the TASK. 🚫 No cleanup proposed; `PATCH /courses/:id/expiry` exists for the owner's per-course call.
- TASK-358 DONE, reviewed (2121/0, 35=35). Facts updated (born ceiling = base + absent weeks). Told @Porter: deploy 357+358 released; DATA REQUEST for old-ceiling courses on `sid`.

## Porter (PM) — 🟢 **`REQ-089` items 0 + 2 IN** (TASK-357 FE 330/0 — `SOM SCHEDULE` 16/16 pages + both §10 copy nits · TASK-358 BE 2121/0 — expiry = base ceiling + advance-leave weeks, Kavya ⇒ week 11) · 35=35 no migration. **Deploy `sid` released to the owner. Sober GO on item 8.** 🔴 **DATA REQUEST carried up:** count live courses born under the old formula (`planned_at_creation = true` bookings) on `sid` AND `uat` — owner decides whether existing ceilings move (admin can PATCH each; no script). ⚠️ Untracked `.next.zip` in the FE repo root — owner's to look at.

## Porter (PM) — owner: `sid` deployed (357+358). @Tanya dispatched: title sweep + one advance-leave expiry course (`QA-089-expiry`, size 6, 3 leaves ⇒ week 11; a later leave must not move it).

## Tanya (QA) — `REQ-089` items 0 & 2 on `sid`: ✅ **item 0 PASS · 🔴 item 2 FAIL**
09-13 follow-up done: parent `88c2917c` note `null`→`พระโขนงเหนือ วัฒนา กทม` (API, instructed); province unchanged.
✅ **ITEM 0** — 11 admin surfaces all read `SOM SCHEDULE`, zero `Smart Scheduler`, footer `Back-office team`.
🔴 **ITEM 2 FAIL** — `QA-089-expiry` (size 6, start 23 Sep, 3 advance leaves): expiry `2026-11-18` = last session (week 9). **Spec §2 wants size+quota+advance = week 11 = 2026-12-02.** Baseline 0-leave 6-session expires `2026-11-11` = last+2 quota (buffer works); the advance-leave course drops the +2. **The "expiry=last session" bug §2 targets is still live for advance-leave courses.** Confirmed on the card.
🟡 Part (b): extra sick leave → expiry did NOT move (satisfies "must not move") BUT the makeup landed `25/Nov`, so expiry (`18/Nov`) now precedes the last session — a course expiring before its own last class (DEF-4 family).
⚠️ Method: creation-modal `⋯` menu flaky; created via `POST /courses {absentWeeks:[2,3,4]}` (identical op), corroborated on the card.
🟢 Footprint: `QA-089-expiry` left as evidence; baseline course cancelled; no LINE/uat/phone.
