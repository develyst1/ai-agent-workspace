# Log — 2026-09-03 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [Jason (BE)] ✅ TASK-245 → REVIEW. A flow with no exit now has one, and the strike counter finally counts. → @Sober

Repo **`smart-scheduler-back`** (path in `machine.local.md`), HEAD **`699c290`**. **Verified:**
`bunx --package typescript@5.6.3 tsc --noEmit` **exit 0** · `bun test` **1254 pass / 0 fail** ·
**no migration** — counted at the moment of writing: **32 `drizzle/*.sql` = 32 journal tags**, unchanged.
🚫 No SQL run, nothing sent to a real recipient, no rich-menu republish.

**What changed** (detail and reasoning in the TASK file):
- **New `src/lib/line-commands.ts`** — the router's vocabulary **is** the reserved set, one list. The 10 inline
  command arrays in the webhook service now read from it.
- **The exit** is checked **once, at the top of the wizard**, before any step reads the text as an answer; every
  question appends @Porter's ` · หรือพิมพ์ ยกเลิก เพื่อออก` through one helper. Cancelling deletes the session
  row the draft lives on — so *"cancelled"* and *"the half-typed record is gone"* are one act.
- **A reserved word at the name step is refused** with the word quoted back and the way round it named.
- **Every rejection now goes through `strikeOrPrompt`** (call sites 4 → 6), and `resetStrikes` runs on every
  accepted answer. **Face 2** is one append to `menu_body` in both languages.

📌 **The finding worth keeping.** The defect was not that `เมนู` became a name. Rule 5 — two strikes, then a
human — had **already shipped** and did not fire, because a rejected birthdate re-asked directly. My own
TASK-231 sentence is what allowed it: *"free-text steps have no 'unrecognised' to detect."* That was true of a
name the bot **accepts**. It was never true of a birthdate the bot **refuses**. ⇒ **A rejection is an unexpected
reply**, and the new test derives the refusal keys from the copy table rather than listing them, so the next one
is covered on the day it is written. *A net nobody has seen catch anything is not a net* — this one caught two
keys I would not have enumerated by hand.

⚠️ **Two of my own earlier tests failed on this change and were corrected, not loosened** — both had pinned the
old behaviour, including the bare `reply` that **is** the defect.

**Open for @Sober**, both in the TASK's §Questions, neither blocking:
- **Q1** — two skip vocabularies still exist (`line-add-student.ts`'s `SKIP` vs `CMD_SKIP`). `ไม่มี` / `-` skip
  but are not reserved; `เสร็จ` / `done` are reserved but do not skip. Nothing is broken and merging them
  changes shipped behaviour at the province step, so I did not. Same one-list argument, one level down.
- **Q2** — the post-link *"want to add a child?"* prompt still advertises `ข้าม` rather than `ยกเลิก`. Both
  work; it is the one question that names a different way out.

⚠️ **Half a DoD line, named rather than ticked quietly:** the owner's exact three-message sequence needs session
**rows**, so what is proven here is every link in that chain DB-free (rejection → `strikeOrPrompt` →
`shouldHandOver(2)` → mute + apology). The replay itself is @Tanya's.

📌 Still waiting, third ask: **TASK-103 / 110 / 116 / 117** are POST-GO-LIVE `TODO`s still addressed to me. Send
them or re-mark them `PARKED` — a TODO with my name on it that nobody intends to dispatch reads, to the next
session that loads this board with no memory, as work I have been sitting on.

**BALL: @Sober — TASK-245 review.**

## [--:--] Sober (SA) — ✅ **TASK-245 DONE.** Two structural choices turned four patches into one fix. → @Jason @Porter @Tanya

**Reproduced:** `tsc` **0** · `bun test` **1254 pass / 0 fail** · `line-commands.ts` present ·
`strikeOrPrompt` now at **11** call sites, up from 4 · no migration (32 = 32).

### 🔴 `line-commands.ts` — the file that makes the fix true rather than merely implemented

> *"A second copy is exactly how «the bot said `เมนู` is a command» and «the bot stored `เมนู` as a name» both
> became true at once."*

**That is the defect restated as a property**, and one list is the only thing that closes it. He repointed all
**10** inline command arrays at it rather than adding an eleventh list called "reserved" — the difference
between fixing this instance and making the class impossible. **A command added tomorrow is reserved the same
day.** And the three tests that pinned literal arrays now assert the property *through* the constants, which is
strictly stronger than the text they replaced.

### 🔴 The exit is checked ONCE, before any step reads the text

I asked for *"`ยกเลิก` works at every step"*. **He made it work at every step that will ever exist** — a step
added next year cannot forget, because it never gets the chance to look. Same shape as `withExit()` asserted as
**a net over the six question keys** instead of four hand-picked cases. Both turn *"we remembered everywhere"*
into **"forgetting is not reachable"**.
📌 And cancelling is **one act**: `clearSession` deletes the row the draft is a column on, so *"cancelled"* and
*"the half-typed record is gone"* cannot disagree.

### The judgement I want on the record — a duplicate name is NOT a strike

> *"The parent answered correctly and is being asked a further question; counting it would hand a two-child
> family to a human for having two children."*

**He read the intent past my wording.** My invariant said *every rejection path goes through `strikeOrPrompt`*,
and applied blindly that would have made the safety net fire on **the most ordinary event in the flow**. A strike
is for *"I did not understand you"*, never *"I understood you and need more"*. That distinction is now the rule.

### ✅ And the DoD line he marked HALF rather than ticked

Every link in the chain is asserted DB-free — rejection → `strikeOrPrompt` → `shouldHandOver(2)` → mute +
apology — but **the owner’s three messages end-to-end need session rows.** *The mechanism is proven; the scenario
is not.* Marked half, not ticked. **@Tanya — that exact sequence is the one to run**, and it is the thing the
owner will retry first.

### 🏁 Where everything stands

**REQ-079:** all code done — 230 · 231 · 232 · 233 · 234 · 243 · 245. **Nothing is waiting on me, @Jason or
@Fern.**

🔴 **@Porter, unchanged and all yours:**
1. **PENDING DEPLOY** — `0030`+`0031` on `sid` in one run · 🔴 **the two rich menus are NOT live** (I confirmed
   from source yesterday that the live one is the old REQ-015/042 menu; `linkKnownRichMenu` is still a silent
   no-op) · 2FA cannot be switched on at all.
2. **Owner questions:** 2FA **delivery** · `province` as a household field.
3. **The `เข้าใช้ระบบ` sentence** — one i18n key, yours as UX writer.
4. **REQ-078** is still `TEST_FAILED` until @Tanya passes it; TASK-242 🔒 held on your condition.
5. **Queued behind the deploy:** 🟢 TASK-244 (durable unlink trail) · 🟢 TASK-240 (course-search row-dropper).

**BALL: @Porter — the `sid` deploy.** It is the only thing standing between a finished build and a testable one.

## [08:0x] Porter (PM) — 🔴 I read YESTERDAY'S log and told the owner Jason was still working. He was finished.
**Third date-discipline failure by me in four days**, and this one produced a false status report to the owner.

**What happened:** he asked *"sober done?"*. I grepped **`log/2026-09-02.md`**, saw it end at Sober's
*"BALL: @Jason — TASK-245"*, and reported the ball as Jason's. **Jason's `TASK-245 → REVIEW` and Sober's
`TASK-245 DONE` were both already sitting in `log/2026-09-03.md` — today's file, which I never opened.**
📌 **I was not guessing; I was reading the wrong day.** That is worse, because a wrong file reads exactly like a
current one. **And my TASK-245 acceptance entry went into 09-02 as well** — it is there, misfiled, and I am not
moving it (append-only); the substance is re-stated below where the team will actually read it.

**The mechanism that keeps failing:** I grep a filename I remember instead of settling TODAY first.
`PROTOCOL.md` §Date discipline says exactly this and I have now broken it on 08-31, 09-02 and 09-03.
⇒ **New hard step, written into `PM.md`: before reading or writing any log, run `date` and `ls -t log/` and
confirm the newest filename matches. No exceptions, including "I just looked five minutes ago".**

### Re-stated here, in today's file, so it is not lost in yesterday's

**✅ TASK-245 accepted. REQ-079's code is COMPLETE.** `strikeOrPrompt` 4 → **11 call sites**, `tsc` 0,
**1254/0**, **no migration**.
📌 **Jason's judgement worth keeping: a duplicate name is not a strike.** Sober's invariant, applied literally,
would have **handed a two-child family to a human for having two children.** He read the intent past the
wording — *"a strike is for 'I did not understand you', never 'I understood you and need more'"*. **That
protects `REQ-079` §6a, which is my own design**: asking for a surname is the flow **working**.

**🔴 Asked the owner to deploy to `sid`.** No migration — ⚠️ **`db:verify` still runs before the restart.**
**After it: @Tanya's round on everything except the two-state menu**, which still waits on the owner's two
images (`REQ-079` §12). ⚠️ **The end-to-end line is half-ticked and that is correct** — the mechanism is proven
DB-free but the owner's own three-message sequence needs session rows. **@Tanya: that exact sequence is what he
will retry first** — type into an idle chat → get pulled into add-student → try to escape.

## [08:1x] Porter (PM) — 🧪 @Tanya — **REQ-079 is deployed on `sid` and the round is RELEASED.** → @Tanya @Sober
Owner deployed. `db:verify` ✅ — **32 migrations, unchanged**, which confirms Sober's "no migration in this
batch" from the data rather than from the claim. Contract: `requirements/REQ-079-line-chatbot-registration.md`.

### 🔴 Start with the owner's own sequence — it is what he will retry first
He found this himself, by poking, and it is the reason TASK-245 exists:
**type into an idle chat → get pulled into add-student → try to escape.**
1. **`ยกเลิก` must work at EVERY step** (name · birthdate · province · summary) and **each prompt must say so.**
   The cancel must **delete the draft and say it did** — a silent cancel is indistinguishable from a saved one.
2. **`เมนู` must never become a child's name.** It is a command everywhere now; the refusal names the escape
   (*"ถ้าเป็นชื่อน้องจริง ๆ รบกวนแจ้งแอดมิน"*). ⚠️ **Check the same for every other advertised word**
   (`นักเรียน` · `เช็คอิน` · `ลา` · `qr` · `เพิ่มนักเรียน`) — one list now backs all of them, so if one leaks
   they all do.
3. 🔴 **Two unexpected replies → handed to a human.** It did **not** fire on 09-02; the counter went 4 → 11 call
   sites. **This is the AC I trust least, because it is the one that has already been "implemented" once.**
4. **The command list ends with** `หรือพิมพ์คำถามเข้ามาได้เลยค่ะ เดี๋ยวแอดมินมาตอบนะคะ 🙏` — via **both**
   `เมนู` and `ช่วยเหลือ`.
5. 🔴 **A duplicate name must NOT be treated as a strike.** Two children with similar names is the flow
   **working**; if that hands the family to a human, it is a defect. *(Jason caught this in review — verify he
   was right in the product.)*

### ✅ Newly unblocked — Q18's fixture problem is solved by the build itself
**A family can now be LINKED** (Flow 1: phone → children). ⇒ **TASK-243's positive path is finally testable:**
link a family → the People screen shows it → clear it → **it can bind again, to a different family.**
📌 **That last hop is `REQ-079`'s headline outcome and NOBODY has ever proven it.**

### Also in scope
Flow 1 entry · เพิ่มนักเรียน (summary-before-write) · แจ้งลา · เช็คอิน · คอร์สของฉัน · empty states ·
the language toggle · **AC-1 silence by default** (re-confirm on this build; the owner proved it on the last one).

### 🚫 NOT in scope — and do not fail it
**The two-state rich menu (ยังไม่รู้จัก / รู้จักแล้ว) is still not published** — it needs the owner's two images.
**What is live is the OLD 6-cell parent menu** (@Sober read it from source, 09-02). ⇒ record menu ACs as
**`NOT_TESTED — blocked on images`**, by name.

### ⚠️ Footprint — this round can create things nobody can delete
- **A student created in LINE is permanent** (`SYSTEM-FACTS.md` — no student-delete anywhere). **Create as few
  as the ACs require, name them obviously** (`QA-079-…`), and declare every one.
- 📌 **Junk already on `sid` from the owner's own testing: `asda`, and possibly `เมนู`** if that draft was
  confirmed. **Report what you find rather than tidying it** — it is evidence of the very defect being fixed.
- **Never message `Haris`** (a real teacher linked on `sid`). **`Bank` is the owner** and is fine.
- **`uat` is not yours.**

## [08:2x] Porter (PM) — 🔴 RETRACTED: I released a round @Tanya cannot execute. She has no LINE account. → @Tanya
The owner asked the obvious question — *"เธอเทสไลน์ได้เหรอ"* — and the answer is **no**.

**She told me this herself yesterday** (09-02, `TEST-065`): *"It needs an inbound LINE message. That needs a
LINE account that has added the OA — I have none, and creating one is prohibited. There is no admin surface that
sends or simulates an inbound."* **I read that, agreed with it, and then released a round in which every single
item begins with typing into LINE.** Same failure as this morning's log: **I did not read what was already
written.**

### What this actually means — and it is structural, not a scoping slip
**QA cannot test LINE at all.** Every REQ-079 item — the escape sequence, `เมนู`-as-a-name, two strikes, the
command list wording, Flow 1 entry, add-student, leave, check-in, course view — **starts with an inbound
message.** Even the headline *link → clear → rebind* needs the **link** half done from a phone.
⇒ **The only LINE-capable tester on this project is the OWNER.** That has been true for weeks; nobody wrote it
down, so I keep planning around a capability that does not exist.

### The split that does work, and we have already used it twice
**The owner is the HANDS. @Tanya is the VERDICT.** He types and screenshots; she reads the evidence against the
ACs, checks the DB/admin screens, and writes the `TEST-*` file. That is exactly how AC-16 (the teacher LINE
message) and AC-1 (silence by default) were actually settled. **It is not a workaround — it is the process.**
⚠️ **Its cost, stated plainly: testing REQ-079 is gated on the owner's time, not QA's.** Every LINE regression,
forever, needs his hands. **Put to him as a planning fact, with two ways out** — a spare LINE account/device he
provides for QA, or a harness that posts synthetic webhook events (engineering work, and it tests our handler
rather than LINE itself).

### @Tanya — the round is REPLACED, not cancelled
**Yours now, and it needs no LINE:**
1. **The People screen** — TASK-243's dialog states link state before acting (re-confirm on this build).
2. **Regression from the deploy** — the screens you swept on 09-02, console clean.
3. **Everything else stays `NOT_TESTED — needs a LINE-capable tester`**, named that way, **not failed.**
**Then hold.** When the owner runs a sequence, I bring you the evidence and **the verdict is yours.**
📌 **You were right on 09-02 and I did not act on it. Recorded so the next Porter does not need you to say it a
third time.**

## [08:2x] Porter (PM) — 📸 ROUND A evidence from the owner's device — **4 of 4 as specified.** → @Tanya
He is acting as the hands; **the verdict is yours.** Screenshot, `sid`, 08:14–08:15. Transcribed verbatim:

| He typed | Bot replied | Against |
|---|---|---|
| `เมนู` | command list ending **`หรือพิมพ์คำถามเข้ามาได้เลยค่ะ เดี๋ยวแอดมินมาตอบนะคะ 🙏`** | **AC-22** ✅ |
| `เพิ่มนักเรียน` | `พิมพ์ชื่อนักเรียนที่ต้องการเพิ่ม (สูงสุด 5 คนต่อเบอร์) · หรือพิมพ์ ยกเลิก เพื่อออก` | **AC-19** (exit offered *in the prompt*) ✅ |
| `เมนู` | **`「เมนู」 เป็นคำสั่งของระบบค่ะ ถ้าเป็นชื่อน้องจริง ๆ รบกวนแจ้งแอดมินนะคะ`** | **AC-20** ✅ |
| `ยกเลิก` | `ยกเลิกแล้วค่ะ / ข้อมูลที่กรอกไว้ถูกลบทั้งแล้ว / ยังไม่ได้บันทึกอะไรลงระบบนะคะ` + the command list | **AC-19** (cancel deletes **and says so**) ✅ |

🔴 **This is the exact sequence that trapped him on 09-02, and it now has an exit at every step.** The reserved
word is refused *and* names the escape; the cancel states the draft is gone rather than leaving the parent to
wonder. **Jason's "one command list, one exit check" landed.**
📌 **Unasked-for and good:** the cancel **re-renders the command list**, so a parent who bails is not dropped
into a void wondering what to do next.

### ✏️ One wording defect — mine to call, and it is one string
**`ข้อมูลที่กรอกไว้ถูกลบทั้งแล้ว`** — **`ลบทั้งแล้ว` is not Thai.** It should be **`ลบทิ้งแล้ว`** (or
`ลบทั้งหมดแล้ว`). Small, but it sits in the **reassurance sentence** — the one place a worried parent is reading
closely to be told nothing was saved. **A garbled word there costs exactly the confidence the sentence exists to
give.** ⇒ **@Sober: one i18n string, no logic.** Everything else in Round A is verbatim correct.

**@Tanya — evidence for AC-19/20/22 is above; rounds B (two strikes) · C (duplicate ≠ strike) · D (silence)
are still to come from him.** Hold the verdict until all four land unless you want to rule on these three now.

## [08:3x] Porter (PM) — ✏️ WITHDRAWN: the `ลบทิ้งแล้ว` "typo" was my misreading. + ROUND B PASSES. → @Sober @Tanya
**@Sober — cancel the i18n fix I asked for.** The string is **`ข้อมูลที่กรอกไว้ถูกลบทิ้งแล้ว`** and it is
**correct**. I read `ทิ้ง` as `ทั้ง` off a screenshot and reported a defect that does not exist.
📌 **I invented work for an engineer from a low-resolution image**, and I did it in the one place I am supposed
to be strongest — Thai wording. **The rule I am taking from it: if a defect is a single character, confirm it
before routing it.** A wrong character in a screenshot and a wrong character in the product look identical.

### ✅ ROUND B — the AC I trusted least, and it PASSES
| He typed | Bot replied |
|---|---|
| `เพิ่มนักเรียน` | `พิมพ์ชื่อนักเรียน… · หรือพิมพ์ ยกเลิก เพื่อออก` |
| `QA ทดสอบ` | `วันเกิดของน้อง (ปปปป-ดด-วว) หรือพิมพ์ ข้าม ค่ะ · หรือพิมพ์ ยกเลิก เพื่อออก` |
| `อะไรก็ไม่รู้` | `รูปแบบวันเกิดไม่ถูกต้องค่ะ … · หรือพิมพ์ ยกเลิก เพื่อออก` ← strike 1 |
| `มั่วอีกที` | 🎉 **`ขอโทษค่ะ ขอส่งให้แอดมินช่วยดูนะคะ 🙏`** ← **rule 5 FIRES** |

**AC-21 ✅** — it did not fire at all on 09-02; the counter going 4 → 11 call sites is proven in the product, not
just in tests. **AC-19 ✅ more broadly than Round A showed:** the exit is offered at the name step, the birthdate
step, **and on the rejection re-prompt** — which is exactly the path that trapped him.

### 🔴 @Tanya — one check that needs NO LINE, and it is yours right now
He was mid-flow with the name **`QA ทดสอบ`** entered when the bot handed off. **Was a student created?**
He never confirmed, so **a saved record would be a defect** — and an unremovable one. **Check the students /
People screen.** This is the division working: he is the hands in LINE, you read the database side.

⚠️ **And I warned him before Round C: the bot should now be MUTED in his chat — silence there is CORRECT, not a
failure.** Waking it must take a **rich-menu button press**, not typing. That tests the wake path for free.

## [08:3x] Porter (PM) — ✅ AC-13 (mute) PROVEN on the owner's device. → @Tanya @Sober
08:16 the bot handed off (`ขอโทษค่ะ ขอส่งให้แอดมินช่วยดูนะคะ 🙏`). **08:18 he typed `เพิ่มนักเรียน` — no reply.**
Owner: *"ใช่ตอนนี้ พิมพ์ไปก็ไม่ทำงานละ"*.

🔴 **The strong form of the AC, and it is what actually matters:** the bot stayed silent **on a command word it
advertises itself.** ⇒ while an admin is mid-conversation, **a parent who reaches for a menu word cannot make
the bot barge in.** That is the whole trade `REQ-079` is built on — *the bot never talks over the human* — and
it is now demonstrated rather than designed. **AC-13's first half ✅.**
⚠️ **Second half still unproven: "and it works normally in EVERY OTHER family's chat."** One muted chat is not
evidence about the rest, and account-wide muting was the failure mode I refused. **Needs a second chat — @Tanya,
this is one for the eventual second linked recipient, recorded as `NOT_TESTED`, not assumed.**

📌 **A consequence of the design worth writing down before it surprises someone:** the wake path is a **button**,
and **LINE on PC has no rich menu.** ⇒ a **PC** parent who triggers the mute has nothing to tap; they wait for
the timeout (@Sober's ~30-minute window from the last message). **Not a defect — they have just been told a
human is coming, which is the point of the mute — but it means "muted on PC" reads as total silence for half an
hour.** Worth knowing before someone reports it as a hang.

**Next from the owner:** wake with a rich-menu button press (tests the wake path), then Round C (duplicate name
must NOT be a strike) and Round D (silence in an idle chat).

## [08:4x] Porter (PM) — ⚡ ORDER: **a muted chat must be re-openable by TYPING.** Owner found it. → @Sober
> *"มันต้องกลับมาพิมพ์ได้ เพราะลูกค้าอาจจะใช้คอม ไม่มี rich menu … ไปคิดกันเอา"*

**He is right and it is a real hole, not a nicety.** The wake path was a **button**; **LINE PC has no rich menu**
⇒ a PC parent who gets muted has **nothing to tap** and sits in silence until a timeout nobody told them about.
**Both mutes are affected** — the two-strikes handover *and* the parent pressing `คุยกับแอดมิน`.

**Written into `REQ-079` §14 with AC-23…AC-26.** The rule: **a muted chat re-opens on one specific typed word,
and the bot names that word in the same message that mutes the chat.**

🔴 **This does NOT reopen rule 2, and the distinction is the reason it is safe.** Rule 2 bans a keyword
**starting a flow out of an idle chat** — the customer's own *"commands triggered accidentally"* fear. This is
the opposite shape: **a deliberate act, by someone who was just told the word, that ends a state they are
already in. It starts nothing.**

**Word: `เปิดเมนู`** — my choice over his `เปิดคำสั่ง`. Two syllables nobody types by accident mid-conversation;
reuses vocabulary the parent already has rather than teaching "คำสั่ง", which is our word not theirs.
🔴 **And it must be DISTINCT from plain `เมนู`.** While muted, `เมนู` is deliberately ignored — that is the
point. If `เมนู` un-muted, a parent idly reaching for a familiar command would drop the bot back into a live
conversation with an admin. **The un-mute must be a thing you choose, not a thing you reach for.**

⚠️ **AC-25 exists to protect what we just proved:** the mute held against `เพิ่มนักเรียน` on his device at 08:18.
**That must survive this change** — exactly one word re-opens, everything else stays silent.
⚠️ **Your design call: button and `เปิดเมนู` should share one un-mute path** — the parent must not learn two
different ways back depending on their device.

📌 **Found by the owner reading a consequence I had written down as merely "worth knowing".** I noted the PC
gap and moved on; **he read it as a defect and he was right.** Noting an implication is not the same as
following it.

## [08:4x] Porter (PM) — 🔎 Round C blocked by the mute, and I think it is NOT a PC bug. → @Sober @Tanya
Owner's screenshot, 08:24–08:26 on LINE **PC**: he tapped a rich-menu button → **`นักเรียนของคุณ (3/5)` rendered
fine**. He then typed `เพิ่มนักเรียน` **three times** → **silence every time.** His read: *"น่าจะเป็นแค่ที่ line
PC"*.

**My hypothesis, and it is falsifiable rather than a guess:** this is **not** device-specific. The chat is
**still muted from the 08:16 handover**, and **a button press executes that button's action without clearing the
mute.** ⇒ the live state is **"muted = typed text ignored, buttons still work"**.

**On mobile it would behave identically — nobody would notice, because there is always a button to tap.**
⇒ **The distinguishing check, given to the owner:** on **mobile**, tap `นักเรียนของฉัน` (list renders), then
**type** `เพิ่มนักเรียน`. **Silence ⇒ mute-state, not PC.** A reply ⇒ a genuine PC-only defect, which is a
different and new finding.

📌 **Either way it strengthens §14:** if a button does not clear the mute, then on PC — where the chips vanish
once you type and there is no rich menu — **`เปิดเมนู` is the only way back at all.** ⚠️ **@Sober, add to your
§14 design call: should a button press ALSO clear the mute?** My lean is **yes** — otherwise a mobile parent
sits in a half-state where taps work and typing is ignored, with nothing telling them why.

**Round C is NOT blocked** — he can enter the flow from the **rich-menu `เพิ่มนักเรียน` button** instead of
typing, then type `มิลล่า`. **AC: a duplicate name must ask for a surname/nickname, NOT count as a strike and
NOT hand off to an admin.**

## [08:5x] Porter (PM) — 🔴 DEF-8 (blocker): **a muted chat invites you into a flow and then ignores your answer.** → @Sober
Owner, on **mobile**, `sid`. **Not a PC problem — my hypothesis is confirmed and the device question is closed.**

```
08:16  bot: ขอโทษค่ะ ขอส่งให้แอดมินช่วยดูนะคะ 🙏      ← chat muted (two-strikes)
08:27  [rich-menu button]  → นักเรียนของคุณ (3/5)      ← postback works
08:27  bot: พิมพ์ชื่อนักเรียนที่ต้องการเพิ่ม … หรือพิมพ์ ยกเลิก เพื่อออก   ← the BOT asked
08:28  มิลล่า                                          ← the parent ANSWERED
       (silence)                                       🔴
```

### Why this is worse than "muted means typing is ignored"
**The bot itself opened the flow and then refused its own question.** Buttons are handled; **text is swallowed
whole** — including text the bot explicitly asked for.

🔴 **And it takes the escape down with it.** If all text is swallowed, **`ยกเลิก` is swallowed too** — the exit
we built in TASK-245, and which the owner verified working at 08:15 this morning, **does not work in a muted
chat.** ⇒ **the parent is trapped again, by a different door.** Same class of defect we just closed, arriving
through the state machine instead of the command list.
⚠️ The prompt even *tells* them to type `ยกเลิก` — **so the bot is advertising an escape it will then ignore.**
That is the `เมนู` contradiction again: what the bot promises and what the bot does disagree.

### Porter's recommendation — one rule, and the whole class disappears
> **A button press clears the mute.**

A parent tapping the bot's own button **has chosen to engage it again**; the bot should come back **fully**, not
half. Today's half-state — taps handled, text ignored, no explanation — is not reachable from any user's mental
model. **This also makes §14's `เปิดเมนู` a belt-and-braces path for PC rather than the only way out.**
⚠️ **Design is yours** — if you keep the mute through a button press, then **at minimum a muted chat must not be
allowed to enter a flow at all**, because inviting someone in and refusing to answer is worse than staying quiet.

### Status
🔴 **Round C (duplicate name ≠ strike) is NOT tested — blocked by this.** The owner never got a reply to
`มิลล่า`. **@Tanya, record it `NOT_TESTED — blocked on DEF-8`, not failed.**
📌 **My error, on the record:** I told him to tap the button and continue Round C. **I already knew typed text
was being ignored and did not follow it through to "then the flow cannot continue either".** I sent him into the
dead end I had just described. **Noting an implication is not the same as following it — second time today.**

## [--:--] Sober (SA) — 🔴 **DEF-8 and §14 are ONE design. TASK-246 cut, with the rule stated as a principle.** → @Porter @Jason @Tanya

Your two entries are the same defect from two sides, so they get one task and one rule.

### The principle, and everything follows from it

> **A mute silences the bot’s INITIATIVE. It never silences the parent’s ability to get OUT, or to come BACK.**

Stated that way because a case nobody listed then resolves correctly on its own:
- **button / postback** → **un-mutes**, then runs normally (your recommendation, taken)
- **`เปิดเมนู`** → **un-mutes**, shows the list, **starts nothing**
- **`ยกเลิก`** → **honoured**, session cleared, **mute unchanged** — *"get me out" is not "let me back in"*.
  Two intents, two effects.
- **anything else, including plain `เมนู`** → **silence** (AC-25, which the owner already proved at 08:18 and
  which must not regress)

✅ **Your fallback needs no second guard.** You asked that, if a mute survives a button press, *a muted chat must
not be able to enter a flow at all.* With the button un-muting **first**, a flow is only ever entered unmuted —
**DEF-8 becomes unreachable by construction rather than by a check.** That is written at the site so nobody adds
the guard later and wonders why it never fires.

✅ **And `เมนู` must NOT un-mute — your reasoning, kept verbatim in the task:** *the un-mute must be a thing you
choose, not a thing you reach for.* A parent idly reaching for a familiar command must not drop the bot into a
live conversation with an admin.

### 🔴 The half of DEF-8 that is worse than the half you led with

You wrote that the bot *"opened the flow and then refused its own question"*. **The sharper consequence is that
it swallows `ยกเลิก`** — the escape TASK-245 built and the owner verified **working at 08:15 the same morning.**
⇒ **the parent is trapped again, through the state machine instead of the command list**, and the prompt is
**advertising an escape the bot will ignore.** That is the `เมนู` contradiction returning in a new place: *what
the bot promises and what the bot does disagree.*

So the task asserts the **ordering** — the mute gate must not sit in front of `ยกเลิก` — because a gate that
swallows the escape it advertises is exactly this bug, and an ordering nobody pinned is an ordering that drifts.

### One un-mute path, per your requirement
`เปิดเมนู` and the button both call the **same** `unmute()`, asserted by call-site count. *The parent must not
learn two different ways back depending on their device* — and two implementations is how the two diverge.

### ⚠️ One edge I flagged rather than decided
The handover **keeps** the session on purpose (TASK-231 — *"a person is about to read the whole conversation"*),
so un-muting can land a parent back inside a half-finished flow they have forgotten. TASK-231’s 30-minute
inactivity TTL covers most of it. **@Jason reports what happens; he does not invent a rule.** If un-muting
resurrects a stale step, I decide.

### 📌 On your two notes about yourself today
Both were the same shape — *"noting an implication is not the same as following it"* — and **I had the identical
failure two days ago** on the additional-teacher clash: I wrote it down as "not blocking", TASK-227 landed, and I
never revisited it. 📌 The pattern is worth more than the apology: **an implication written down without a
trigger is a note, and a note is not a control.** In both cases the thing that caught it was the owner using the
product.

### Round C
Agreed — `NOT_TESTED — blocked on DEF-8`, not failed. He never got a reply to `มิลล่า`, so nothing about
duplicate-name-≠-strike was exercised. It is the first thing worth re-running once TASK-246 lands.

**BALL: @Jason — TASK-246.** It is a blocker and it re-opens the escape we shipped this morning.

## [--:--] Jason (BE) — ✅ TASK-246 → REVIEW. DEF-8 is unreachable by construction, and "keep the mute" had a mechanical edge under it. → @Sober

Repo **`smart-scheduler-back`** (path in `machine.local.md`), HEAD **`699c290`**. **Verified:**
`bunx --package typescript@5.6.3 tsc --noEmit` **exit 0** · `bun test` **1273 pass / 0 fail** · **no
migration** — counted now, not remembered: **32 `drizzle/*.sql` = 32 journal tags**.
🚫 No SQL run, nothing sent to a real recipient, no rich-menu republish. New test:
`src/services/line-mute-exit.test.ts`.

**What changed** (reasoning in the TASK file):
- **A tap un-mutes BEFORE the postback is dispatched.** That is the whole of DEF-8: there is now no state in
  which a flow has been entered and the answer to its question is still muted. 🚫 No second guard — your
  fallback is satisfied by the ordering, and the site says so, so nobody adds dead code there later.
- **`ยกเลิก` is honoured ahead of the gate**, mute untouched. **`เปิดเมนู`** un-mutes and shows the list and
  starts nothing (asserted as an absence). **AC-25 unchanged**: the branch has exactly two doors and then a
  bare `return`, asserted by count — the thing the owner proved working at 08:18 cannot regress quietly.
- **AC-24**: both mute messages name the word, in both languages, and the test checks the advertised word is one
  `isReopenWord` actually accepts — copy that names a word the code rejects is the same lie, one layer up.
- **One un-mute**: one definition, and **no other writer clears a mute** (`mutedUntil: null` appears once) —
  stronger than counting the two paths.

🔴 **The finding worth keeping, and it is not the one I expected.** *"Cancel, keep the mute"* was not only a
policy call — **`clearSession` would have broken it mechanically.** The mute lives on the session ROW, so
deleting the row to end the flow **un-mutes as a side effect** and drops the bot straight back on top of the
admin who is typing. ⇒ `clearFlowKeepMute()` clears the flow **in place** and nulls the draft by hand,
*because the row that used to carry the draft away now survives.* 📌 **"Two intents, two effects" had a second
edge under it: two intents, two storage consequences.** A `clearSession` here would have passed every policy
test I wrote and still put the bot on top of a human.

**🔴 Two things need you, both in the TASK's §Questions:**
- **Q1 — my one judgement, one line to reverse: `สมัคร` while muted now UN-MUTES.** It is checked *above* the
  mute gate (TASK-231: the only way in must never be swallowed), which meant a muted chat was **asked for a role
  and then ignored the answer** — DEF-8's shape through a second door, live today. Your table does not name
  `สมัคร`, so I applied the principle: a mute never silences the ability to come **back**, and `สมัคร` is
  deliberate, not idly reached for. **Both shipped rules stay true only if this door un-mutes.** If you want it
  silent instead, TASK-231's *"works from ANY state"* assertion is the one being reversed and needs your
  amendment, not mine.
- **Q2 — the stale-step edge you asked me to report, now measured.** Un-muting **late** (>30 min) is safe: the
  session has already expired. Un-muting **early** leaves the row live, so the parent's **next free text lands
  back in the old step** and is answered by a question they may not remember. 🔴 And it cannot be avoided by
  writing less: `updated_at` carries `$onUpdate`, so **any** write to that row refreshes the TTL — the un-mute
  itself extends the flow's life by another 30 minutes. **Not a trap** (`ยกเลิก` works and every prompt says
  so) — a confusion. I implemented no rule; the two cheapest options are named in the task.

📌 **@Tanya's Round C is unblocked by this** — the `มิลล่า` reply he never got is the first thing worth
re-running, and duplicate-name-≠-strike has still never been exercised.

**BALL: @Sober — TASK-246 review + Q1/Q2.**

## [--:--] Jason (BE) — 📌 Correction, and it is the repo's own comment-vs-code trap: `strikeOrPrompt` is at **6** call sites, not 11. → @Sober

Not a defect and nothing to change in the code — but the number is now in **four** places in committed files
(`log/2026-09-02.md:2043`, `log/2026-09-03.md:124 · :150 · :254`, and it reached the owner through @Porter), and
the repo is the only memory. A wrong figure in a log is inherited as fact by the next session that has no
chat history.

**Measured, just now, in `src/services/line-webhook.service.ts`:**

```
grep -o 'strikeOrPrompt(' | wc -l   →  7   = 1 declaration + 6 call sites
grep -o 'strikeOrPrompt'  | wc -l   → 11   = the same 7 plus 4 mentions in COMMENTS
```

📌 **11 is the count of the identifier in prose, not in code** — a `grep -c` without the `(`. That is exactly
the trap this repo already has a convention for (`const code = (s) => s.replace(...)`, comments stripped before
structural assertions), and it has now caught **me four times and a review once**, which is the argument for the
convention rather than against anyone.

**The real figures for TASK-245:** `strikeOrPrompt` **4 → 6 call sites** (birthdate rejection + reserved name),
and the count is pinned in `line-silence.test.ts` as **7 matches including the declaration**, so it cannot drift
silently. Everything else in the acceptance stands: `tsc` 0 · **1254/0** at that point (**1273/0** now, after
TASK-246) · no migration.

⚠️ Naming it because the 4 → 11 figure was used as evidence that AC-21's fix is real. **It is real** — six sites
now strike where four did — but the evidence should be the six, not the eleven. I have not edited your entries
or @Porter's (append-only); this is the correction record.

## [--:--] Sober (SA) — TASK-246 code PASSES. 🔴 **He found that my "cancel keeps the mute" was mechanically impossible.** → @Jason @Porter @Tanya

**Reproduced:** `tsc` **0** · `bun test` **1273 pass / 0 fail** · `mutedUntil: null` appears **exactly once** in
the service — a stronger assertion than counting the two paths, and the one he reached for unprompted.

### 🔴 The finding that matters most

> *"`clearSession` would have broken it mechanically — the mute lives on the session ROW, so deleting the row to
> end the flow un-mutes as a side effect."*

**I wrote "cancel keeps the mute" as a policy. The existing mechanism made that policy unimplementable** — and
following my instruction literally would have **dropped the bot back on top of the admin who is typing**, the
exact thing the mute exists to prevent. `clearFlowKeepMute()` is right, and nulling the draft **by hand because
the row that used to carry it away now survives** is the detail that would otherwise have been the next defect.
📌 *"Two intents, two effects"* had a mechanical consequence I did not see when I wrote it.

### ✅ Q1 — `สมัคร` un-mutes. Approved, and he stated my principle better than I did.

He applied it to a door I never named. The alternative is worse: `สมัคร` above the gate **without** un-muting is
DEF-8 through a second door — asked for a role, then ignoring the answer — **live today**. Moving the gate above
it instead would reverse TASK-231's *"`สมัคร` works from ANY state"*, which exists because it is the only way in.

⇒ **The rule, sharpened to cover all three doors:**
> **No path may ENTER a flow while muted. A door that can start something either un-mutes, or is silent — never
> both halves.**

**That is what makes DEF-8 unreachable rather than merely fixed**, and it beats my original table because it
decides the *fourth* door before anyone builds it.

### 🔄 Q2 — the stale step. Decided, and wider than he scoped it.

His measurement was exactly what I asked for: mute 60 min, TTL 30 min, so an **early** un-mute leaves a live row
and the next free text lands in a step the parent has forgotten — and **`$onUpdate` means the un-mute itself
extends that flow's life**, a property of the column rather than of his line.

🔴 **But it is not only `เปิดเมนู`.** A **button** un-mute has the same hole: tapping `นักเรียนของฉัน` renders a
list and starts no flow, so the old step survives and swallows the next message. **Fixing one door would leave
the identical confusion behind the other — the shape of half-fix this whole day has been about.**

⇒ **`unmute()` clears any in-flight flow** — one rule, one site, all three doors. **The mute means a human took
over; whatever the parent was half-way through was superseded by that, so coming back is a fresh start.**
⚠️ It does not contradict TASK-231's *keep the session on handover*: the handover still keeps it, so an admin
reads the whole conversation. The clear happens only when **the parent chooses to return.**
🚫 Not the shorter-TTL option — that makes *"is this session live"* depend on mute state, i.e. two definitions of
one thing.

### 📌 Third time today the right distinction was drawn instead of ticked
The tap-un-mutes-first ordering is asserted structurally and the routing of the owner's third message is proven
purely; **the replay needs a muted session row** — @Tanya's. *Mechanism proven, scenario not.* @Jason has marked
three DoD lines "half" today rather than green, and every one of them was the honest call.

### 🧪 @Tanya, once this lands — the two the owner already blocked on
1. **DEF-8's exact sequence** (muted → button → the bot asks → type a name → **it is answered**).
2. **Round C**, still `NOT_TESTED — blocked on DEF-8`: a duplicate name must ask for a surname, **not** count as
   a strike and **not** hand off.
⚠️ And **AC-25 must not regress**: while muted, `เมนู` · `เพิ่มนักเรียน` · free text stay silent. The owner
proved that working at 08:18 — it is the thing this change is most likely to break.

**BALL: @Jason — the `unmute()` flow-clear.** Last line on TASK-246.

## [--:--] Jason (BE) — ✅ TASK-246 r2 → REVIEW. `unmute()` clears the flow, and the addition brought an ordering trap with it. → @Sober

Repo **`smart-scheduler-back`** (path in `machine.local.md`), HEAD **`699c290`**. **Verified:** `tsc --noEmit`
**0** · `bun test` **1278 pass / 0 fail** · **no migration** — re-counted: **32 `drizzle/*.sql` = 32 journal
tags**. 🚫 No SQL, nothing sent, no republish.

**Your addition, as instructed — one rule, one site, all three doors.** `unmute()` writes `FLOW_CLEARED`
(`step` → `MUTED` · `pendingRole` · `draft` · strikes) with `mutedUntil: null`, and `clearFlowKeepMute()` writes
**the same constant** — so *"no flow in progress"* has one definition and the only difference between the two
functions is the mute, which is exactly the difference between the two intents. The sharpened principle is at
the un-mute site, as you asked.

⚠️ **One decision inside your instruction, and it is the thing to look at in r2.** `unmute()` is called on
**every** postback, muted or not — so a literal *"unmute clears the flow"* would discard the half-finished
registration of a parent who was **never muted** and merely tapped `เช็คอิน` mid-flow. ⇒ the write is scoped to
a chat that is **muted right now** (`gt(mutedUntil, new Date())` — deliberately the same test `isMuted` applies,
so the two cannot disagree). Your rule holds for all three doors; what it no longer does is reach a chat that
was never away. **Unconditional if you say so** — one predicate.

🔴 **The addition brought a trap with it, now pinned by a test.** `สมัคร` used to write `CHOOSE_ROLE` and then
un-mute. With the clear added, that order **erases the step it just wrote** and swallows the parent's `1` — the
same silence, one door along. `unmute()` now runs first there, and the test asserts the ORDER rather than
trusting it. 📌 The class, worth more than the case: **a function that gains a side effect changes the meaning of
every call site's order** — and only one of the three doors had an order to get wrong, which is why reading all
three mattered more than reading the diff.

✅ Kept explicitly, both asserted as absences: TASK-231's *"the handover KEEPS the session"* (no clear in
`strikeOrPrompt`) and 🚫 **not** the shorter-TTL option (`isSessionExpired` never mentions mute, so *"is this
session live"* keeps one definition).

**BALL: @Sober — TASK-246 r2.**


## [--:--] Sober (SA) — ✅ TASK-246 **DONE (code)**. 📌 And the correction is mine as much as Jason's: I "verified" 11 by making the same mistake. → @Jason @Porter @Tanya

**Reproduced:** `tsc` **0** · `bun test` **1278 pass / 0 fail** · `FLOW_CLEARED` at `line-webhook.service.ts:197`
used by **both** writers (`:231` with `mutedUntil: null`, `:249` without) · migrations 32 = 32.

### 📌 The correction — and how it got past a review

Jason's figures are right: **`strikeOrPrompt` is at 6 call sites, not 11.** `grep -o 'strikeOrPrompt(' → 7`
(1 declaration + 6 calls); the bare identifier gives **11** because **four of them are in comments**.

🔴 **What I have to own is narrower and worse than repeating his number.** I ran my own count — `grep -c` on the
bare identifier — got 11, and reported it as *reproduced*. **Reproducing a measurement with the same flawed
method is not verification; it is the same mistake, twice, wearing the word "verified".** The review step added
no independent evidence at the exact point it was supposed to.

⇒ **The rule I am taking from it, and it is the convention this repo already has for tests:** a structural count
is only evidence if it can distinguish **code from prose**. `strikeOrPrompt(` — with the paren — is the
measurement; `strikeOrPrompt` is a word search. Same discipline as `const code = (s) => s.replace(...)` in the
source-assertion tests: **strip the comments before you count, including when the counter is me.**

✅ **AC-21's fix is still real** — six sites strike where four did, and it is pinned at 7-including-declaration in
`line-silence.test.ts`, so it cannot drift quietly. **The evidence is the six.** Board row for TASK-245 now
carries the corrected figure with the reason; the log entries stay as written (append-only) and this is the
correction record. @Porter — the 4→11 I sent up should read **4→6**; the conclusion does not change.

### ✅ r2 — the scoping decision was **better than my instruction**

> *"`unmute()` is called on every postback, muted or not — so a literal 'unmute clears the flow' would discard
> the half-finished registration of a parent who was never muted and merely tapped `เช็คอิน` mid-flow."*

**I wrote that rule assuming un-mute only happens when muted. It does not.** Taken literally, my own instruction
would have built a **new** version of the defect it was closing — a parent who never left, losing what they had
typed because they touched a button. Scoping the write to *muted right now*, **with the same predicate `isMuted`
uses so the two cannot disagree**, keeps the rule true for all three doors and stops it reaching a chat that was
never away. **Unconditional was my error, not his shortcut.**

### 🔴 The fix carried a trap, and the class is the keeper

`สมัคร` wrote `CHOOSE_ROLE` **then** un-muted; with the clear added, that order **erased the step it had just
written** and swallowed the parent's `1` — **the same silence, one door along, introduced by the fix.** Order now
runs un-mute first, and a test asserts the order rather than trusting it.

> **A function that gains a side effect changes the meaning of every call site's order.**

📌 **It was caught because he read all three call sites instead of the diff** — and only one of them had an order
to get wrong. That is the second time today that reading the neighbours beat reading the change.

### 🧪 @Tanya — unchanged and now unblocked
1. **DEF-8's exact sequence:** muted → button → the bot asks → type a name → **it is answered.**
2. **Round C** (`NOT_TESTED — blocked on DEF-8`): a duplicate name asks for a surname — **not** a strike, **not** a handoff.
3. ⚠️ **AC-25 must not regress:** while muted, `เมนู` · `เพิ่มนักเรียน` · free text stay silent. The owner proved
   that working at 08:18; it is what this change is most likely to break.
4. 🔄 **New in r2:** a parent who was **never muted**, mid-registration, taps `เช็คอิน` — **the draft survives.**

⏳ Still with @Porter: the `sid` deploy of `0030`+`0031`, the rich-menu images + `publishRichMenus`, 2FA delivery,
`province` as a household field, and the `เข้าใช้ระบบ` copy. Queued behind the deploy: TASK-244 · TASK-240 · TASK-242.

**BALL: @Tanya — the DEF-8 replay.** Nothing else moves until it passes.

## [09:0x] Porter (PM) — 🔀 Re-routing your BALL: @Tanya cannot run the DEF-8 replay. Two things come first. → @Sober @Tanya
TASK-246 accepted — `tsc` 0, **1278/0**, `unmute()` ordered before the state write with a test pinning the order.

🔴 **"BALL: @Tanya — the DEF-8 replay" cannot happen as written, for two reasons:**
1. **TASK-246 is not deployed.** The `sid` build is TASK-245's. **The replay would test the defect, not the fix.**
   ⚠️ **And your outstanding list is stale on one line:** `0030`+`0031` **are already applied and witnessed** on
   `sid` (`db:verify` ✅ this morning). **Nobody re-runs migrations for this** — TASK-246 is code only.
2. **@Tanya cannot send an inbound LINE message at all** — no account, and creating one is barred by her charter
   (`SYSTEM-FACTS.md`, established 09-03). **The owner is the hands; her verdict is the verdict.** I made this
   mistake yesterday by releasing an all-LINE round to her; **it is written down now so the routing stops
   drifting back.**

⇒ **Real order: owner deploys TASK-246 → owner runs the replay → @Tanya rules on the evidence.**

📌 **Your 4→11 correction is carried up:** I had passed **11** to the owner. It is **6**, and the reason is worth
more than the number — **you reproduced a count with the same flawed method and called it verified.** A word
search cannot tell code from a comment; `strikeOrPrompt(` can. **The conclusion is unchanged: six sites strike
where four did, pinned by a test.**

📌 **And r2's scoping was better than the instruction it came from** — an unconditional "un-mute clears the flow"
would have discarded the half-finished registration of a parent who was **never muted** and merely tapped a
button. **Your own rule would have rebuilt the defect it was closing.** Jason scoped it to *muted right now*,
using the same predicate `isMuted` uses so the two cannot disagree. **That is the second time today reading the
neighbouring call sites beat reading the diff.**

**@Tanya — your scope when the build lands, unchanged from Sober's four**, plus: 🔴 **AC-25 is the one this
change is most likely to break** (the owner proved the mute holding at 08:18 — it must still hold).

## [09:1x] Porter (PM) — ✅ TASK-246 deployed on `sid`. Asking the owner to run the DEF-8 replay. → @Tanya @Sober
Code only — `0030`/`0031` were already applied and witnessed this morning; **no migration re-run.**

**The replay I gave him — deliberately three steps, because it closes two things at once:**
1. Get muted (two wrong birthdate answers → handoff).
2. **Tap the `เพิ่มนักเรียน` button** → the bot asks for a name.
3. **Type `มิลล่า`** → 🔴 **it must ANSWER.** On the old build this was silence (DEF-8), and the answer also
   settles **Round C**: a duplicate name must ask for a surname — **not** a strike, **not** a handoff.
⚠️ Plus the regression Sober flagged as most at risk: **while muted, typed text must still be ignored** (the
owner proved that holding at 08:18 — AC-25).

**@Tanya — evidence comes to you from his device; the verdict is yours.** Nothing else on REQ-079 moves until
this passes.

## [23:1x] Porter (PM) — 🎉 DEF-8 FIXED + Round C PASSES + §14 shipped — **one screenshot, five things.** → @Tanya @Sober
Owner's device, `sid`, 23:06–23:07, on the TASK-246 build. Verbatim:

```
adwa    → วันเกิดของน้อง (ปปปป-ดด-วว) หรือพิมพ์ ข้าม · หรือพิมพ์ ยกเลิก เพื่อออก
adwdaw  → รูปแบบวันเกิดไม่ถูกต้องค่ะ …                              ← strike 1
dwdw    → ขอโทษค่ะ ขอส่งให้แอดมินช่วยดูนะคะ 🙏
          (ถ้าต้องการใช้บอทอีกครั้ง พิมพ์ เปิดเมนู ค่ะ)              ← 🎉 AC-24
[button] → พิมพ์ชื่อนักเรียนที่ต้องการเพิ่ม … · หรือพิมพ์ ยกเลิก เพื่อออก
มิลล่า   → มีน้องชื่อนี้อยู่แล้ว รบกวนใส่นามสกุลหรือชื่อเล่นเพิ่ม
           เพื่อไม่ให้สลับกันนะคะ · หรือพิมพ์ ยกเลิก เพื่อออก        ← 🎉 DEF-8 + Round C
```

| | |
|---|---|
| 🎉 **DEF-8 FIXED** | a muted chat entered by button now **answers its own question.** Yesterday: silence. |
| 🎉 **Round C ✅ (§6a)** | duplicate name **asks for a surname** — **not** a strike, **not** a handoff. **Jason's "a duplicate is not a strike" judgement is confirmed in the product**, and it protects my own §6a design. |
| 🎉 **AC-24 ✅** | the mute message **names `เปิดเมนู` in the same breath** — §14 delivered, one working day after the owner spotted the PC gap. |
| ✅ **AC-21** | two strikes → handoff, again. |
| ✅ **AC-19** | the exit is offered at **every** step, including the duplicate-name prompt. |

### 🔴 Still NOT tested — and both need one more short run from him
He left the mute by **tapping**, so neither of these was exercised:
1. **AC-25** — while muted, typed text must stay **silent.** ⚠️ Sober named this as what the fix was most likely
   to break, and the mute path was rewritten. **Unproven on this build.**
2. **AC-23 — `เปิดเมนู` itself.** **Nobody has ever typed it.** The word is advertised in the mute message, so if
   it does not work we are advertising an escape that does not exist — **the exact contradiction §14 exists to
   remove.** Asked for both in one two-message run.

### ✏️ Cosmetic, and deliberately NOT routed as work
`ถ้าต้องการใช้**บอท**อีกครั้ง` — **"บอท" is our word, not a parent's.** They experience it as a *menu*; my §14
wording was `กลับมาใช้เมนูอีกครั้ง`. **Not a defect, does not block, and must not consume a round on its own** —
bundle it with the next FE/i18n touch. 📌 Flagged cautiously on purpose: **this morning I routed a Thai "typo"
that did not exist, read off a low-resolution screenshot.** One character is not evidence; a word choice is.

## [23:1x] Porter (PM) — ✅ AC-23 + silence confirmed. **Evidence run COMPLETE — @Tanya, the verdict is yours.** → @Tanya @Sober
Final screenshot, 23:09–23:10, TASK-246 build:
`ยกเลิก` → draft deleted + command list · **`hello` ×4 and `hellohello` → total silence** · **`เปิดเมนู` → the
command list returns.**

- ✅ **AC-23 — `เปิดเมนู` WORKS.** Nobody had ever typed it. **The escape the bot advertises is real**, which is
  the whole point of §14. *(It also works from a non-muted chat — harmless, it just prints the list.)*
- ✅ **Silence holds AFTER the mute path was rewritten** — Sober named this as what TASK-246 was most likely to
  break. It did not.

### 🔴 One named gap — and I am NOT asking him for another run tonight
He reached this state by **cancelling**, not by being muted. ⇒ what is proven is **silence-by-default in an idle
chat (AC-1)**, not **silence while MUTED (AC-25)**. Those are different states and the mute path is the one that
was rewritten. **AC-25 was proven at 08:18 — on the PREVIOUS build.**
**Recorded as `NOT_TESTED` on this build rather than inferred.** @Tanya — **your call whether it needs closing**;
it is two messages from the owner whenever you want it.

### 📋 What the owner proved today, in one working day
**Exit from a flow · `เมนู` never becomes a child's name · two-strikes handoff · the mute keeps the bot off an
admin's conversation · DEF-8 · duplicate names ask instead of striking · `เปิดเมนู` · silence.**
**Eight, and five of them nobody else on this team could have tested** — QA has no LINE account
(`SYSTEM-FACTS.md`).

📌 **Worth stating once, because it changed how this went:** three of today's findings came from him **using**
the product, not from a plan — the swallowed command, the PC dead-end, and the trapped flow. **The test plan
verified what we expected; the poking found what we did not.**

**@Tanya — nothing is waiting on the owner, @Sober, @Jason or @Fern. Write the verdict.**

## [--:--] Tanya (QA) — 🏁 **REQ-079 verdict: `TEST_PASSED` on the 14 ACs exercised. It is NOT a REQ-079 pass.** → @Porter

Verdict + the full AC map: **`tests/TEST-065` §Round 2**. Your evidence run gave me eleven ACs; I closed three
more from the database side that no screenshot could show.

### ✅ Ruled from the owner's device — 11 ACs + DEF-8
**AC-1** silence · **AC-6** duplicate asks for a surname (and doesn't name the other family) · **AC-13** mute,
first half · **AC-14/21** two strikes → human, proven on two builds · **AC-19** an exit at *every* step incl. the
rejection re-prompt · **AC-20** `เมนู` refused · **AC-22** the promise line · **AC-23** `เปิดเมนู` works —
**nobody had ever typed it** · **AC-24** the mute names the way out · **AC-26** re-opening starts nothing ·
**DEF-8 fixed**.

### ✅ And three I closed myself — the half a screenshot cannot reach
- 🎉 **AC-9 — “a parent who abandons mid-flow leaves NO partial student anywhere.” PASS.** Your urgent check.
  `QA ทดสอบ` (Round B's handoff) → **no record**. `adwa` (the 23:06 handoff) → **no record**. **111 parents,
  unchanged.** A flow that hands off mid-way writes **nothing** — and you were right that a saved record there
  would have been unremovable.
- ✅ **AC-20's data half** — `เมนู` is not just refused in LINE, it is **absent from the database**.
- 📌 **I validated the search before trusting three negatives:** `มิลล่` returns the family *and* its three
  student names, so the search really does cover students. **Three "no match" results are evidence, not a broken
  search box** — I have been caught by exactly that shape twice this week.

### 🎉 And the fixture you said didn't exist — it does now, and it passes
**TASK-243's POSITIVE state is verified.** The owner's own runs created a linked family. `0900000092` →
**“This family has 1 LINE account linked.”**, then it explains that clearing **keeps** students, bookings, notes
and history, that the parent can re-link any time, and that the account **“can be linked to a family again —
including a different one.”** Only *then* does the destructive button appear. **Both states are now proven:**
`Cancel`-only when nothing is linked (09-02), the full dialog when something is. It even re-checks live
(*“Checking this family's LINE link…”*) instead of trusting the row.

🔴 **I did NOT press `Clear LINE link`.** That is **his own live account** — the fixture he is testing REQ-079
with. Clearing it would destroy his campaign mid-run and modify data I did not create. **The clear→rebind
headline stays unproven, and it is his to run when he's finished with it** (Q22).

### 🔴 AC-25 — you left it to my call. **I am asking for it.**
What 23:09 proved is silence **from an idle chat he had cancelled out of** — that is **AC-1**, a different state
from **muted**. The 08:18 silence-while-muted proof was on the **previous** build, and **TASK-246 rewrote the
mute path** — which @Sober himself named as the thing most likely to break. **Two messages** against the risk
that the bot starts talking over an admin, which is the one premise this whole REQ rests on. Not inferable.

### 🔴 What the verdict does NOT say
**12 of 26 ACs are untested** — AC-25 (asked for), AC-13's second half (**needs a second linked chat — the same
missing fixture as AC-16 in `TEST-064`; one decision unblocks two ACs across two REQs**), and **eleven never
exercised**: entry · unknown phone · one-account-one-family · 2FA · review-before-write · admin-notified · leave ·
never-infer · course view · typed `1`/`2` · and **AC-17's four notification regressions incl. the 08:15 daily**.
**Please do not let "TEST_PASSED" travel without that sentence** — what is proven is the *escape hatches, the
silence, the strike counter, the mute and the duplicate rule*. The flows themselves are unopened.

### 📌 On the division of labour — it worked, and its cost is now measurable
Your "**owner = hands, QA = verdict**" split produced eleven ACs in a day and my three closed the loop on them.
It is the process now, not a workaround. **Its cost: every remaining REQ-079 AC — and every future LINE
regression — is gated on his time.** The two ways out you named (a spare LINE account for QA, or a synthetic
webhook harness) are worth deciding **before** the next LINE REQ, not during it. Recorded in `TEST-065` rather
than left in a log entry.

### Footprint
**Read-only. Nothing created.** Two `Clear LINE link` dialogs opened and **cancelled**; six `People` searches.
No LINE message, no booking, no setting, no script, no restart, **no `uat` contact**. **AC-18 holds** — the two
linked accounts are Bank (him) and **Haris**, and every fixture I have ever confirmed used Bank/Camp/Dewy/Ek,
never Haris. The five REQ-078 fixtures in `TEST-064` §Park note are unchanged.

**Ball: @Porter** — Q20 (AC-25, I'm asking) · Q21 (a second linked chat closes two ACs across two REQs) ·
Q22 (clear→rebind, his to run). REQ-078's Q13–Q16 are still open too.

## [--:--] Porter (PM) — ✅ Verdict accepted **with your sentence attached.** Q20 approved, Q21/Q22 to the owner. → @Tanya @Sober
**Your framing is the correct one and I am carrying it verbatim:** `TEST_PASSED` on **14 of 26** — **not a
REQ-079 pass.** What is proven is the **escape hatches, the silence, the strike counter, the mute and the
duplicate rule.** **The flows themselves — entry, leave, check-in, course view, the notification regressions —
are unopened.** I will not let the two words travel without that.

🎉 **AC-9 is the result of the day and it was the one that could not be undone.** `QA ทดสอบ` and `adwa` both
handed off mid-flow and **wrote nothing**; 111 parents unchanged. **A flow that abandons leaves no record** — and
a record there would have been permanent. 📌 **And you validated the search before trusting three negatives**
(`มิลล่` returns the family *and* its students). **Three "no match" results are only evidence if the box works** —
that is the discipline that keeps a null result from being a false all-clear.

🎉 **TASK-243's positive state is proven** — and 📌 **you were right not to press `Clear LINE link`.** It is his
live fixture mid-campaign; clearing it would have destroyed the thing he is testing with, and it is data you did
not create. **Restraint was the correct call, not a gap in coverage.**

**Q20 — AC-25: approved, and I am asking him.** You are right that 23:09 proved **AC-1 (idle)**, not **muted**,
and that TASK-246 rewrote the mute path @Sober named as most at risk. **Two messages against the one premise
this REQ rests on** — the bot never talking over an admin. **Not inferable, and I should not have offered to
infer it.**

**Q21 — carried up with your sharper framing:** one decision (a second linked chat) **closes AC-13's second half
here AND AC-16's multi-teacher half in `TEST-064` — two ACs, two REQs.** That is a better argument than the one
I made and it is the one he is getting.
**Q22 — clear→rebind is his**, when he is finished using that account as a fixture.

📌 **On the division of labour:** eleven ACs in a day from *owner = hands, QA = verdict*, plus your three from
the database side. **It is the process now.** Its cost — every remaining AC and every future LINE regression
gated on his time — goes to him **as a decision to take before the next LINE REQ, not during it.**

## [23:4x] Porter (PM) — 🎉 **AC-25 PASSES (strong form) · AC-23 from a real mute · the CLEAR half of Q22 proven.** → @Tanya @Sober
Owner's device, `sid`, 23:35–23:43. Three screenshots.

**AC-25 — and it passed harder than I asked for.** Muted at 23:35, he then typed `awdw` · `สวัสดี` ·
**`นักเรียน`** · **`เมนู`** → **all silent, including two commands the bot advertises itself.**
🔴 **This is the premise the whole REQ rests on** — an admin can hold a conversation and the bot will not barge
in, even when the parent reaches for a menu word. **@Tanya: you were right to ask for it rather than let me
infer it from the idle-chat silence.** The mute path was rewritten; the idle path was not the same thing.

**AC-23 again, from a genuinely muted chat this time:** 23:43 `เปิดเมนู` → the command list returns.

**🎉 Q22's CLEAR half is PROVEN — and by accident, which is the best kind.** He cleared the link, then typed
`เช็คอิน` and got the **unlinked welcome** (*"พิมพ์ สมัคร เพื่อผูกบัญชี LINE"*). **I did not guess** — I asked
whether he had cleared first, because the same screen means *correct* or *defect* depending on one fact I did
not have. He confirmed: cleared.
📌 **It also proves something nobody specified: the bot carries no stale linked state.** The moment the link is
gone it treats him as a stranger. **The opposite would have been far worse than a broken button** — a cleared
account still showing another family's children.

**🎉 A SECOND LINE account exists** — `support08 Dong` followed the OA and started `สมัคร`. ⇒ **Q21's missing
fixture may now be available**, which is the one that closes **AC-13's second half here AND AC-16's
multi-teacher half in `TEST-064`** — two ACs across two REQs.

### Still open — and it is now three messages
**Q22's REBIND half.** Cleared ✅, re-binding ✗. **The AC that matters is binding to a DIFFERENT family**, since
that is what exercises `family_line_links_user_uq` — a clean move rather than a collision. Asked him for
`สมัคร` → `1` → **another family's phone**. Same phone still has value (proves re-binding) but not the move.

**@Tanya — evidence above is yours to rule on.** AC-25 and AC-23 are closed by it; Q22 is half.

## [23:5x] Porter (PM) — 🎉 **Q22 CLOSED — clear → rebind to a DIFFERENT family is PROVEN.** → @Tanya @Sober
The headline outcome of REQ-079, never exercised by anyone until tonight. Owner's device, 23:49:

```
สมัคร        → เลือกบทบาท 1/2/3
1            → กรุณาพิมพ์เบอร์โทรของผู้ปกครอง
0905622548   → ผูกบัญชีผู้ปกครองสำเร็จ ✅ (เบอร์ 0905622548)
               พบข้อมูลของคุณแล้วค่ะ — เตาไป้
```
**Same LINE account:** linked to `0900000092` (มิลล่า · มิลลิ · asda) → **cleared** → **re-bound to
`0905622548` (เตาไป้)**. 🔴 **A different family, and no trace of the old one** — the move is clean, not a
collision. `family_line_links_user_uq` doing exactly the job Sober kept it for when the code was cut.

📌 **It also exercises the entry flow end to end for the first time** — role → phone → children shown → linked.
**@Tanya, your call whether that closes AC-2**; it was reached by typing `สมัคร` rather than an
`[เข้าใช้ระบบ]` button, because that button does not exist until the menus are published.

### ⚠️ What this does NOT prove — and I am separating it deliberately
**Proven: clear-then-rebind.**
**NOT proven: AC-4 — an account already linked to family A entering family B's phone must NOT silently
re-point.** He cleared **first**. **That is the actual guard**, and it is the one that matters: if an already
linked account re-points on a bare phone number, **anyone who knows another household's number pulls themselves
into it** — the accepted risk of §2 becoming a great deal worse than the owner accepted.
⇒ **Recorded as `NOT_TESTED`, not folded into tonight's win.** It is one message from a linked state: enter a
different family's phone **without** clearing, and see whether it refuses.

### Tally — what the owner closed today, as the hands
**DEF-8 · AC-25 (strong form) · AC-23 · AC-24 · AC-19 · AC-20 · AC-21 · AC-6 · AC-1 · AC-26 · the clear half ·
the rebind half.** 🔴 **Three of those had been blocked for days and nobody else on this team could have run
them** — QA has no LINE account.

**Ball: @Tanya** — rule on tonight's evidence and update `TEST-065`. **@Porter holds:** AC-4 (one message),
Q21's second-chat pair (AC-13b + `TEST-064` AC-16), the rich-menu images, 2FA delivery, `province`.

## [23:5x] Porter (PM) — 🎉 **AC-4 PASSES — an already-linked account REFUSES to re-point.** → @Tanya @Sober
Owner, 23:55, still linked to `0905622548`:
```
สมัคร → 1 → 0900000092
→ บัญชี LINE นี้ผูกกับอีกครอบครัวหนึ่งไว้แล้วค่ะ หากไม่ถูกต้องกรุณาติดต่อแอดมิน
```
**Of the three outcomes I named, it took the cleanest: a refusal that states the situation and names the way
out.** No silent move, no half-linked state, no crash.

📌 **And it is better than the AC asked for, in a way worth recording:** the refusal **does not name the other
family, and does not reveal whether the entered number exists in the system at all.** ⇒ someone trying numbers
learns **nothing** — the same neutral-message discipline `REQ-051` specified for the walk-in page, applied here
without anyone asking for it. **The one thing it does say is how to get help.**

⇒ **`family_line_links_user_uq` is doing its job from both directions now:** it permits a **cleared** account to
move (proven 23:49) and **refuses** a linked one to jump (proven 23:55). **That pair is the whole guarantee that
a parent never sees another family's children**, and it is now demonstrated rather than argued.

### ✏️ And I withdraw an overstatement, on the record
I told the owner a silent re-point would mean *"anyone who knows another household's number could pull
themselves in"*. **That is already true and he already accepted it** when the 6-digit code was cut (§2). **I
re-dramatised a decided risk as if it were new** — he called it out by simply not understanding why it mattered.
**The real value of this test was smaller than I made it sound: confirming the system does not break or corrupt
its state.** It did not.

**Ball: @Tanya.** AC-4 is closed by the above; tonight's run also closed AC-25 · AC-23 · Q22 (both halves).

## [--:--] Porter (PM) — 🎉 **AC-13's SECOND HALF is already proven — the owner spotted it in evidence I had.** → @Tanya @Sober
He said *"ก็นี่ไง"* and he is right. **Two screenshots I had already received, never placed side by side:**

| time | account 1 (his main) | account 2 (`support08 Dong`) |
|---|---|---|
| **23:35** | muted → `awdw` · `สวัสดี` · `นักเรียน` · `เมนู` → **silent** | |
| **23:41** | still muted | `สมัคร` → **bot replies with the role picker** ✅ |
| **23:43** | `เปิดเมนู` → un-muted | |

⇒ **Between 23:35 and 23:43 one chat was silent while another was served.** **The mute is PER-CHAT, not
account-wide.**

🔴 **This is the failure mode I refused at design time** — *"one admin answering one parent must never silence
the bot for every family"* — and it was the specific reason I would not accept LINE's account-wide Bot/Chat
switch as an implementation. **180 families going quiet because an admin replied to one of them** was the
disaster case. **It does not happen.**

⚠️ **One nuance for your verdict, @Tanya, and it is yours to weigh:** account 2 was **unlinked** (it had just
followed the OA), so what is strictly proven is *"a muted chat does not silence a different chat"*. If you want
the fully-equivalent case — a muted chat and another **linked family's** chat — that needs account 2 linked as a
parent. **My read: the practical concern is closed**, because account-wide muting would have silenced account 2
regardless of its link state. @Sober can confirm the mute key is per-`lineUserId` from source if you want it
airtight without another run from the owner.

📌 **Third time today the owner found something I already had.** I reviewed each screenshot against the step I
had asked for and **never laid two of them next to each other** — even though I had written the question myself
and marked it `NOT_TESTED` hours earlier. **Evidence does not arrive labelled with which question it answers.**

## [--:--] Tanya (QA) — ✅ Ruled on the night run: **15 of 26 now pass** — and I verified the rebind in the DATA. → @Porter

All four rulings + the database check: **`tests/TEST-065` §Round 3.**

### ✅ Closed by the owner's evidence
- **AC-25 — PASS, strong form.** Muted, then `awdw` · `สวัสดี` · **`นักเรียน`** · **`เมนู`** → all silent,
  *including two commands the bot advertises itself.* 📌 Worth saying since you offered to infer it: asking was
  right. 23:09 proved **AC-1 (idle)**; TASK-246 had **rewritten the mute path**. Different state, different code.
- **AC-23 — upgraded.** Previously proven from a cancelled chat; now from a **genuinely muted** one.
- **AC-4 — PASS.** A refusal that states the situation and names the way out. 📌 And better than the AC asked:
  it does not name the other family **and does not reveal whether the number exists at all** — probing teaches
  nothing.
- **AC-13 second half — PASS.** One chat silent at 23:35 while another was **served** at 23:41 ⇒ if the mute were
  account-wide, account 2 would have been silent **regardless of link state**. The disaster case — 180 families
  going quiet because an admin replied to one — **is excluded by observation.**

### 🔴 Two places I did not take the easy close
1. **I decline the "@Sober confirms the mute key from source" offer.** **Reading code is not testing** — it is
   the one rule this role exists for. A source read would say the implementation *looks* right; it adds no fact
   to what the two chats already showed. If anyone wants the fully-equivalent case (muted chat + another
   **linked** family's chat), that is **one message from a second linked parent**, not a code review. Recorded as
   the named residual, which I judge very low.
2. **AC-2 stays OPEN, and I know the flow worked end to end.** Role → phone → children shown is genuinely
   proven, and I have recorded it as such. But AC-2 reads *"Given `[เข้าใช้ระบบ]`…"* and he reached it by
   **typing `สมัคร`**. **A rich-menu postback and a text message are different handler branches**, and
   **Rule 2 — only a button starts a flow — is exactly what a typed entry bypasses.** Closing AC-2 now would
   sign off the one path the REQ says is the only legitimate one. It closes when the button exists.

### 🎉 And the headline — I verified it in the DATA, not just in the bot's replies
The bot saying *"success"* and the admin screen agreeing are **two different facts**, and a half-written link is
exactly the kind of thing that hides between them. Both ends check out:
- **`0900000092`** (มิลล่า · มิลลิม · asda) — was *"1 LINE account linked"* this morning → now
  **"No LINE account is linked to this family."**
- **`0905622548`** (เตาไป้) → now **"This family has 1 LINE account linked."**
⇒ **the account moved between two different families, cleanly, with no stale link and no double-link.**

🎉 **And the dialog's promise is now verified rather than merely displayed.** It tells the admin *"Students,
bookings, notes and message history are all kept."* **All three students survived the clear.** That is the
sentence someone reads immediately before pressing a red button — it is true.

### Where REQ-079 stands
**15 of 26 pass:** AC-1 · 4 · 6 · 9 · 13 (both halves) · 14 · 18 · 19 · 20 · 21 · 22 · 23 · 24 · 25 · 26, plus
DEF-8, TASK-243 both states, and the clear→rebind lifecycle proven end to end.
**11 open:** AC-2 (partial) · 3 · 5 · 7 · 8 · 10 · 11 · 12 · 15 · 16 · **17**.

📌 **The sentence still travels:** proven = the **escape hatches, the silence, the mute, the strike counter, the
duplicate rule and the link lifecycle**. **Leave, check-in, course view and every regression AC are unopened.**

🔴 **AC-17 is the one I would not ship without, and it needs no new fixture** (Q23). It is the *existing*
notifications — teacher schedule · course-confirm · booking-confirm · **the 08:15 daily** — and this REQ rewired
the message path they share. The 08:15 job has been **"known-unverified" on this project since before I
arrived**. One morning's observation from his phone closes it, and it is the cheapest remaining AC by far.

**Q24:** `support08 Dong` exists and has followed the OA. **If it completes `สมัคร` as a parent on a third
family, AC-13's equivalent case AND `TEST-064`'s AC-16 multi-teacher half both fall out of one run** — worth
doing while that account is fresh.

### Footprint
**Read-only.** Two `Clear LINE link` dialogs opened and **cancelled** (`0900000092`, `0905622548`) — I pressed
`Clear` on neither. Two `People` searches. No LINE message, no booking, no setting, no script, **no `uat`
contact**. REQ-078's five fixtures (`TEST-064` §Park note) unchanged.

**Ball: @Porter** — Q23 (AC-17, the cheapest and the one I'd hold a ship for) · Q24 (one run closes two ACs
across two REQs). REQ-078's Q13–Q16 remain open.

## [--:--] Porter (PM) — ✅ Verdict accepted. 🔴 But Q24 has a role mix-up I am not passing on. → @Tanya
**15 of 26, and the sentence travels with it.** Carrying your framing unchanged: proven = **escape hatches ·
silence · mute · strike counter · duplicate rule · link lifecycle.** **Leave, check-in, course view and every
regression AC are unopened.**

🎉 **Verifying the rebind in the DATA was the right instinct and it is what makes it real.** *"The bot said
success"* and *"the admin screen agrees"* are two facts, and a half-written link hides precisely between them.
Both families check out, and **all three students survived the clear** — so the dialog's promise to the admin is
now **true**, not merely displayed. That sentence is read immediately before a red button is pressed.

📌 **Both of your refusals were right and I want them on the record:**
1. **You declined my "@Sober confirms the mute key from source" offer.** Correct — **reading code is not
   testing**, and a source read would have added no fact the two chats had not already shown. I offered you a
   shortcut out of my own impatience; you should keep refusing those.
2. **AC-2 stays open even though the flow demonstrably worked.** He reached it by typing `สมัคร`, and **Rule 2 —
   only a button starts a flow — is exactly what a typed entry bypasses.** Closing it would sign off the one
   path the REQ calls illegitimate. **It closes when the button exists.**

### 🔴 Q24 — I think the roles are crossed, and I am checking before it reaches him
You wrote that `support08 Dong` completing `สมัคร` **as a parent** would close AC-13's equivalent **and**
`TEST-064`'s **AC-16 multi-teacher half**. **The first, yes. The second I do not follow:** AC-16 revised is
*"every assigned TEACHER receives the LINE confirmation"* — to observe teacher #2 receiving it, account 2 must
be linked **as a teacher** and assigned to the อื่นๆ booking. **A linked parent receives no teacher confirmation.**
⇒ **They look like two different links to me, not one run** — parent for AC-13's equivalent, teacher for AC-16.
**If I am missing something, say so and I will carry your version.** I am not sending the owner a run that closes
one AC while he is told it closes two. *(I told him "link it as a teacher" earlier today — that is the half I am
confident about.)*

**Q23 — agreed, and it is going to him as the cheapest thing left.** AC-17 needs **no fixture at all**: the
existing notifications share the message path this REQ rewired, and **the 08:15 daily has been known-unverified
on this project since before you arrived.** One morning's look at his phone closes it. **I am also flagging your
"I would hold a ship for it" to him verbatim** — that is a QA position and he should hear it in your words.
