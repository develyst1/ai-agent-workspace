# 2026-09-08

## [--:--] Porter (PM) — 📦 Owner deployed the BACKEND to `sid`. Asked about the frontend before releasing @Tanya.
Owner: *"deploy backend to sid แล้ว"*. **Recorded at the moment it happened.**
⚠️ **He named the backend only, and I am not assuming the pair.** `TASK-274` is @Fern's (the FE status-label map)
and **board DEPLOY RULE 3 exists because a half-pair ships a gate with no screen.** ⇒ **asked him to run the
frontend too rather than have @Tanya test a mixed build** — **one command, and it removes the question.**
📌 **A tester on a half-deployed pair produces results nobody can attribute**, which is the same reason I held
this deploy until her round finished in the first place.

## [--:--] Porter (PM) — 🟢 `sid` redeployed BOTH halves. @Tanya released for TWO checks that gate `uat`.
Backend and frontend both deployed and restarted ⇒ **no mixed build, and the pair rule is satisfied.**
⇒ **Scoped her to TWO checks, not a round:** **DEF-1** (pause → find it in the tray; **200, not 400** — and
⚠️ **the tray must SHOW the booking, because a 200 with an empty array reads identically to her "convincing
lie"**) and **the three message corrections** (`Sessions` = course size · `Remark` **with a note present** ·
teacher == parent).
📌 **Deliberately narrow: `uat` is held on these two and nothing else.** **Pass ⇒ the release goes tonight; fail
⇒ it does not go at all.** **Everything else she is carrying waits**, including @Sober's three corrections to her
ledger reasoning — **none of which changes what `uat` needs.**

## [--:--] Porter (PM) — 🛑 `uat` deploy HELD: `db:seed-ledger` refused, `0033` needs a human. Nothing changed.
Dry run on the customer's box, **exit 1, refused to proceed.** `0025`/`0026`/`0027` are the 3 applied-but-
unrecorded rows; **`0028`–`0034` are genuinely not applied**; `0033` is flagged **not-applied AND
`re-runnable: NO`**.
🔴 **I have a reading — `0033` does not need SEEDING, it needs APPLYING, which is `db:migrate`'s job two steps
later — and I did NOT act on it.** **Sent it to @Sober as a question with the words "correct me".** ⚠️ **It is his
tool, his witness design, and it is the customer's live database at midnight.** **The tool said do not proceed;
the PM does not overrule that on a hunch.**
🟢 **And it is his witness design working exactly as written.** The `why` it printed is his own reasoning: *"NOT
the index's existence — an existence probe would be satisfied by 0007's version and would report this as applied
on a box where it never ran."* ⇒ **`sid` could never have taught us this** — the index there was already correct.
**It surfaced on the one box where being wrong would have cost something.**
📌 **Told the owner to change nothing and wait.** Third time tonight a guard has fired on a real risk, and **the
first two were on our own box.**

## [--:--] Sober (SA) — 🛑 `db:seed-ledger` STOPPED the `uat` deploy. **The stop is conservative, the rule is MINE and it is in the wrong place. TASK-281 cut; the owner changes nothing.**
**Nothing was applied on the customer's box.** Dry run, refusal, exit 1 — **the tool behaved exactly as
specified**, which is why the ruling had to be about the specification and not about tonight.

### The ruling, and I verified it in the source rather than reasoning from the output
1. ✅ **`--apply` inserts rows ONLY for `applied` tags** (`toInsert` is built from `applied`) ⇒ **`0033` is
   `not-applied`, so it is never seeded either way.** The three rows are `0025` `0026` `0027`.
2. ✅ **`rerunnable: false` is CORRECT** — `0033`'s SQL is a bare `DROP INDEX "bookings_teacher_slot_uq";` with
   **no `IF EXISTS`.** **Not to be "fixed".**
3. 🔑 **The scenario the halt guards cannot occur here.** It protects against a witness saying *not-applied* when
   the migration IS applied. **`0033`'s probe is the index's PREDICATE containing `PAUSED`** — a string that can
   only exist if `0033` ran ⇒ **`found=false` is a reliable negative.** **That is the entire content of that
   witness's own `why`, written months ago against the `0022` blindness.**

### 🔻 The defect is a PLACEMENT error, and the comment on the rule gives it away
> *"Anything that would have **`db:migrate`** attempt a migration that cannot survive it … must stop the
> operator."*

**That sentence is about `db:migrate`. The rule lives in `db:seed-ledger`.** ⇒ **it blocks the seed to protect
the step after it.**
📌 **And the door it guards grew its own lock three weeks later: `db:preflight` (TASK-266)** refuses an
unsplittable batch before `drizzle-kit migrate` — the reason PENDING DEPLOY 8 has two commands. **When
`blockers()` was written, nothing else stood there. That is no longer true, and I never revisited the rule when
the second control landed.**
🔑 **New instance of the week's own lesson, pointing the other way:** we keep finding notes that were never
mechanisms. **This is a mechanism that outlived its reason and nobody re-read it.**

### 🚫 What I did NOT do, deliberately
**I did not tell @Porter to have the owner bypass a refusing tool on a live customer database at 1am**, and I
did not offer a hand-written ledger insert. **The tool refuses; we fix the tool.** ⇒ **TASK-281** to @Jason, whose
queue was empty. **The customer asked for tomorrow afternoon, so a task tonight fits without hurrying anyone.**

### ⚠️ Two things I told @Porter to put to the owner
- 🔴 **The seeding is NOT optional.** `drizzle-kit` decides what is pending from **the ledger**, not the schema
  ⇒ **unseeded, it would re-apply `0025`–`0027`.** The blocked step is a real blocker.
- 🔴 **PENDING DEPLOY item 7 and "tomorrow afternoon" disagree.** `0033` rebuilds the index under an **ACCESS
  EXCLUSIVE** lock ⇒ **shop CLOSED.** Tonight is closed; tomorrow afternoon is not. **Better raised now than at
  the moment the lock is taken.**

### 🟢 And the part worth keeping
**This is the witness design paying for itself on the one box where being wrong would have cost money.** On
`sid` the index was already correct and none of it was learnable. ⚠️ **The real finding underneath is that
`uat`'s ledger has been behind its schema** — three migrations applied and unrecorded — **and `db:verify` would
have caught it at the next deploy either way.**

**BALL: @Jason — TASK-281, tonight.**

## [--:--] Jason (BE) — ✅ TASK-281 DONE (code): the seed halts on what only it can answer. `uat` is unblocked from my side. → @Sober

`smart-scheduler-back` · tsc 0 · **1704 pass / 0 fail** across 134 files · 🚫 no migration, no database,
**no witness touched** — `0033`'s `rerunnable: false` and its predicate probe are exactly as they were · nothing
run against `uat`.

### ✅ One function was answering two questions; now there are two, each named for its own
**`seedHalts()`** — `needs-human` only: *we do not know whether this ran*, and **no other tool can answer it**.
**`seedWarnings()`** — `not-applied && !rerunnable`: **loud, never fatal**, printed where the STOP used to be.
The warning says where the guard moved, because the concern did not disappear:
> *"This does NOT block the seed… ⚠️ But `db:migrate` will attempt them. Read `bun run db:preflight` before you
> run it."*
📌 **That sentence is the control against the next person re-adding the halt** — same shape as the *"deliberately
NOT the inverse"* sentences: **put the reason where somebody would otherwise make the change.**

### Your three facts, each turned into a test rather than accepted
`appliedTags` does not contain `0033` and has exactly `length - 1` entries ⇒ **halting protected nothing** ·
`rerunnable: false` asserted CORRECT alongside the predicate probe and `0033`'s bare `DROP INDEX` — 🚫 **the flag
is not the thing to fix, and the test says so** · the warning names `db:preflight`.

### 🔴 The mutation, and a mistake in how I ran it
Restoring the old halt fails **two** tests: *"nothing halts"* **and** *"halts and warnings are counted
separately"* — because `0033` then appears in both lists. **That second failure is what tells me the mutation
did what I meant**, rather than that something went red.
⚠️ **And my first attempt at it silently did not mutate** — a `perl` pattern that did not match, inside an `&&`
chain that then skipped the test run entirely. I caught it because the output had no "MUTATED" line and the
suite passed. **A mutation test that does not mutate is a green run that proves nothing** — your own TASK-273
false confirmation, arriving from the tooling instead of the fixture. **Re-run properly before I claimed it.**

### ✅ The Question — `blockers()` had exactly ONE caller
`seed-ledger-from-schema.ts:62`. `verify-migrations.ts` and `probe-witnesses.ts` import from
`migration-witness` but **not `blockers`**; `migrate-preflight.ts` has a **local variable of the same name**
from `scanBatch` — a different function, and the only reason a grep looks alarming.
⇒ **Softening it changed nothing but the seed.** Kept as a test: the three other scripts are asserted to use
neither new function, **so a second placement error fails there rather than at 11pm on a customer's box.**

### ⇒ What the owner will see on the re-run
`0033` **warned, not halted** · **exactly three rows to insert** (`0025`–`0027`) · **exit 0**. Asserted against
the real witness table, not a fixture that resembles it.

**BALL: @Sober — TASK-281 ready for review. ⛔ Nothing else is on me.**

## [--:--] Sober (SA) — ✅ TASK-281 DONE. **`uat` is unblocked.** 🔑 **He deleted `blockers()` instead of softening it, and that is the better fix.**
`tsc` **0** · `bun test` **1704 / 0** (134 files) · **nothing run against any database** · **`0033`'s witness
byte-untouched** — I verified that first, because the whole ruling rested on it.

**Verified line by line, because the next thing that happens is a write to the customer's box:** `seedHalts()` is
`needs-human` only · the `process.exit(1)` is guarded by `halts.length` alone · the warning prints **where the
STOP did** · **`toInsert` is still built from `applied`**, so the seed still cannot write a row for a
not-applied tag.

### 🔑 I asked him to soften a predicate. He removed it and wrote TWO functions named for their questions
**`seedHalts` — *we cannot judge this*. `seedWarnings` — *this cannot be retried*.**
⇒ **The placement error cannot recur, because there is no longer one function that could answer both.** **My
version would have left a single predicate the next person could quietly widen again.** **He fixed the shape;
I had specified the symptom.**
🔑 **And the warning text is the control against the next reader** — it says, in place, *where the guard went*
(`db:preflight`). **Same shape as the "deliberately NOT the inverse" sentences: nobody re-adds a halt that
explains itself at the point of change.**

### ✅ He verified my three facts instead of accepting them
`appliedTags` asserted not to contain `0033` ⇒ **halting protected nothing** · `rerunnable: false` asserted
**correct**, beside the bare `DROP INDEX` in the SQL, **so a future reader cannot "fix" the flag** · the warning
names `db:preflight`. 📌 **Being told a thing is safe and asserting it are different, and on this box that
difference was the whole review.**

### ⚠️ The FOURTH false-confirmation of the day, and this one came from the TOOLING
His first mutation **silently did not apply** — a `perl` pattern that did not match, inside an `&&` chain that
then skipped the test run. **He noticed only because the output had no "MUTATED" line.**
📌 **Four today, one per layer:** the `size:6`/`confirmed:6` **fixture** · my malformed 11th card (**the code
under test**) · his `bin: undefined` **default parameter** · this, **the shell that runs it.**
🔑 **Every one was caught by the same habit and nothing else: checking WHY the result came out that way.**
✅ **And his two-test failure set is the measurement** — *"that second failure is the one that tells me the
mutation did what I meant, not merely that something went red."*

### 🔴 What I sent up ahead of the commands
**Item 7 versus "tomorrow afternoon".** `0033` takes an **ACCESS EXCLUSIVE** lock on `bookings`; the ~300 ms
build is not the risk — **a transaction holding the table when the `DROP` starts queues everything behind it.**
**Tomorrow afternoon is not a closed shop.** ⇒ **tonight while closed, or tomorrow before opening.** **The one
thing in the sequence I would not let slide.**
📌 And the dry run must be **run and read** before `--apply`, not skipped because we expect it to pass.

**BALL: @Porter** — item 7 with the owner, then the four commands.

## [--:--] Porter (PM) — ✅ The `0033` stop is answered: it was a WARNING in the wrong tool. And my reading was right.
@Sober: **`--apply` writes rows only for migrations the DATABASE says are already applied** ⇒ `0033` is skipped
either way and **never needed seeding.** ⇒ **the concern moved to `db:preflight`, which owns it**, and the owner
will now see a **warning where a STOP was.** 📌 **His line: the words changed, the visibility did not.**
🟢 **And the real finding underneath is `uat`'s ledger being three migrations behind its schema** — *"it would
have bitten at the next deploy either way."* **A dry run that refuses is the cheapest way to learn that, and it
learned it on the box where being wrong costs money.**

🔴 **But `uat` is still GATED and I am not letting the momentum carry it past that: @Tanya has not confirmed
DEF-1 on `sid`.** The redeploy landed; **her two checks have not come back.**
⇒ **Split the deploy at the honest seam and offered the owner the choice:**
- **MIGRATIONS now** — the lock window is open (00:00, shop closed), they are needed whatever happens, and
  **`0033` is inert against the old code, which never writes `PAUSED`.** **This is the runbook's own order.**
- **CODE deploy + restart AFTER @Tanya passes DEF-1** — *"pause reads as delete"* is exactly the defect we would
  be shipping to a live shop.
📌 **Not a compromise: it is the sequence he wrote, stopped at its own checkpoint.**

## [--:--] Porter (PM) — 🟢 `uat` SCHEMA IS GREEN: 35/35 witnessed. The customer's box is current for the first time in weeks.
Owner ran the full repair on the live box, in order and without incident: **`db:seed-ledger --apply` → 3 rows
inserted** *(and it did NOT touch the shared `drizzle.__drizzle_migrations` — "evidence and rollback")* →
**`db:migrate:through 0032` → 33/33 ✅ scoped** → **preflight cleared the 2-migration batch** → **`db:migrate` →
35 journal · 35 witnessed ✅.**
📌 **Every guard built in the last 24 hours fired correctly on this run and none of them fired wrongly:** the
seed-ledger warning (downgraded from a STOP hours earlier) · the split-run requirement · the preflight saying
**yes** to a batch it could take. **A guard that only ever says no is an obstacle; these said no, then yes.**
📌 **`0033`'s witness — the index DEFINITION, not its existence — is the one that could only have been proven
here.** On `sid` the index was already correct. **It was designed for a box we had not migrated yet, and that box
was the customer's.**
⏭️ **Next: `sale:ensure-items` — and on `uat` it has NEVER run.** Without it a typed-amount อื่นๆ booking charges
a real family and posts nothing, silently. **Then STOP: no code, no restart, until @Tanya clears DEF-1 on `sid`.**

## [--:--] Porter (PM) — ✅ `uat`: `sale:ensure-items` — **1 created: `other-booking`.** The same gap, on the customer's box.
`Done. 1 created, 22 already present.` — **identical to `sid` yesterday: every bucket existed except the one for
typed-amount อื่นๆ bookings.**
🔴 **So the ฿20 defect was live on `uat` too, and nobody could have found it there** — the symptom is **a real
family charged and nothing in the books**, silent, no error. **It has now been closed on the customer's box before
anyone made such a booking.** 📌 **PENDING DEPLOY item 6 is discharged on both boxes.**
📌 **Worth stating plainly: this is the one thing tonight that fixed a problem the customer already had**, rather
than shipping something new. **It was found on `sid` by a QA fixture that produced no money, four days ago.**
⏸️ **STOPPED as planned. No code, no restart on `uat`.** The box now runs the old build on the new schema —
**safe: nothing in the old code knows `PAUSED` exists.** **The last gate is @Tanya's DEF-1 confirmation.**

## [21:3x] Tanya (QA) — ✅ **DEF-1 CLOSED · `REQ-083` AC-5/AC-7/AC-9 PASS · and one retraction of mine.** → @Porter

**@Porter's two gating checks, reported ahead of everything else.**
✅ **DEF-1 fixed, verified BOTH ways** — API `?status=PAUSED` → **200** with a real row *(was 400 `ZodError`)*,
**and on screen `Paused bookings | 1 | KKTEST | 1 HR | Was: 08/Oct/26 16:00`.** ⇒ **`REQ-076` AC-1 · AC-9 ·
AC-12 PASS.** 📌 **He was right to demand the second check** — a 200 with an empty array would have looked
identical to the bug. **The empty state is honest again now.**
🎉 **`REQ-083` AC-5 · AC-7 · AC-9 PASS.** My overnight fixture was swept at 18:30 and posted **฿1,390**; the undo
wrote **exactly one** `REVERSAL −139000` beside an **unedited** `SALE`; **the replay wrote nothing.**
🟢 **`end-of-day` DOES run on `sid`** — answered from the movements themselves, so his `job_runs`/`db:reset`
caveat never had to be used.
🔻 **RETRACTION: my 09-07 ledger conclusion was FALSE.** *"`refType: SALE` ⇒ nothing is tied to a booking"* —
**`postBookingSale` writes `refType: "SALE"` with `refId` = the BOOKING id.** @Sober called it before I could
measure it. **The count of 72 was right; I read a real number against the wrong field.** ⚠️ **Third instrument-
over-observation error this week** (remembered screen → coordinate scale → wrong field). **The pattern is mine.**
🟢 **My own overnight experiment is what disproved me** — built to answer honestly, and it answered against me.
⚠️ **The three message corrections stay `NOT_TESTED`:** they exist only inside a composed LINE message, there is
no preview surface, and a push to an unlinked recipient returns `skipped` without rendering text. **Owner's.**
🟢 **Footprint closed:** `f9fec2b7` released (resumed → cancelled); `cbc26a39` left `SICK_LEAVE` **with its
`SALE`/`REVERSAL` pair deliberately intact — that pair IS the AC-5/AC-7 evidence.**

## [--:--] Porter (PM) — 🟢 DEF-1 PASSES. The money thread is CLOSED. `uat`'s gate is open.
**@Tanya: DEF-1 passes on both surfaces · `end-of-day` DOES run on `sid` (from data, not `job_runs`) · the
false-zero did not happen, the programme had a price · `AC-5` is testable after all and `AC-9` passes.**
**"Nothing of mine is holding `uat` any more."**
🔻 **And she retracted her own Round 6 conclusion in full, unprompted, and asked me to carry it to @Sober** —
*"he called it before I could measure it, and he was owed that."* **`refType` could not discriminate; she used it
as though it could.** 📌 **Her sentence: *"The number 72 was correct. I read a real number against the wrong
field and then stated a conclusion the field could not carry."*** 🟢 **Her own experiment is what disproved her.**
⚠️ **CHECK 2 is `NOT_TESTED` and cannot be hers:** `Sessions` · `Remark` · teacher==parent exist **only in the
composed LINE message**, and **there is no preview surface anywhere** — a push to an unlinked recipient returns
`skipped` without rendering text. ⇒ **the owner is the only person who can read them.**
🔴 **Recorded as a structural fact, not tonight's problem: we have shipped six notification messages that exactly
one human being on earth can verify before they go out.** **Same shape as the four other single-person
dependencies today** — engineers cannot reach a database, QA cannot reach credentials, @Fern cannot reach a
deployed app. **This one goes to Atlas.**
⇒ **Offered the owner a two-minute LINE check on `sid` before the `uat` code deploy** — his call, and the risk if
he skips it is a wrong number in a message, not data or money.

## [--:--] Porter (PM) — ✅ CHECK 2 evidence from the owner's phone. GO given for `uat`.
`Sessions : 6` on a `6 HR` course **with two advance leaves** — **it read `4` before the fix.** The customer's
reported defect is gone, **and the leave dates print beside it**, which was the entire argument for the full
count. **Teacher == parent, byte-identical, both family lines on the coach's copy** ⇒ Decision 5's reversal is
live. 🟢 **And the bilingual conversation is on `sid`** — the teacher-nickname prompt and the command list are
both Thai+English.
⚠️ **`Remark` absent proves nothing** — my own omit-empty rule and a booking with no note. **`NOT_TESTED`, exactly
as I warned when I asked for it to be tested with a note present.**
⚠️ **`Date : อังคาร` is Thai where §18 says the value should follow the English label.** **That ruling post-dates
the build** ⇒ **an open item, not a regression, and I told @Tanya not to fail the release on it.**
⇒ **GO for the `uat` code deploy.** **Schema is already there and green; this is the code and the restart.**
📌 **The two things not verified are a missing optional line and a day name.** **Neither is data, money, or a
control an admin depends on** — and after tonight I would rather ship two named unknowns than hold a release on
a certainty nobody can produce at 01:00.

## [--:--] Porter (PM) — 🔴 `Remark` fails on the course-level message. Found within a minute of testing it properly.
Owner entered a note and confirmed a course. **`CONFIRMED SCHEDULE` carries no `Remark`; the per-booking
`ยืนยันตารางสอน` carries `หมายเหตุ: 23232sssss`.** ⇒ **the note is stored and DOES reach the teacher — the
course-level message is the one that drops it.**
📌 **This is precisely the check I insisted be run with a note present.** A booking without one proved nothing;
**the first one with a note found it immediately.** **Worth remembering the next time I am tempted to accept an
absence as evidence.**
📌 **And it may be a design question rather than a missing line:** the note is per-BOOKING, and a course-level
message aggregates many. **If a course has several notes, what does one `Remark` print?** @Sober rules it.
⚠️ **NOT a blocker, and I told the owner why in one line:** `REQ-007` requires the note to reach the teacher and
**it does** — via the other message, exactly as the shop has it in production today. **The customer's new
PLACEMENT is what fails, not the requirement.** ⇒ **`uat` still goes tonight; this is first in the queue after.**

## [--:--] Porter (PM) — 🔴🔴 DEF-2: pause a COURSE, resume it, and the sessions DUPLICATE. Owner found it. My scoping caused it.
**A "6-session course" now shows FOURTEEN rows** — the same dates paired (`15/Sep PENDING` + `15/Sep CANCELLED`,
`22/Sep ON LEAVE` + `22/Sep PENDING`, and so on) **plus extra `CANCELLED` rows out to 03/Nov.** ⇒ **resume
REGENERATED the plan on top of the existing sessions instead of restoring them.**
🔴 **RELEASE BLOCKER. `uat` must not take the code.** On a real course this corrupts a family's schedule and
their entitlement, and it is not something an admin can tidy up.
📌 **`REQ-082` AC-3 forbade exactly this, and @Sober wrote it into TASK-264 in these words:** *"AC-3 is an
absence: this path must call NO plan-reconciling function — an expiry edit that regenerates a plan is worse than
the missing feature."* **Same class, different path: the RESUME path (REQ-084), which nobody applied it to.**

🔻 **The owner asked how I had @Tanya test this, and the answer is that I stopped her from testing it.**
**I scoped her to two checks — DEF-1 and the three message corrections — and wrote *"everything else can wait."***
**Course pause/resume was in neither.** ⚠️ **And it compounded an earlier gap:** she could not mint a course
fixture without selling a course, so **the course path had already gone untested once**, and instead of treating
that as a hole I narrowed the round further.
📌 **The pause TRAY passed, the booking path passed, and I let those stand for a feature whose other half is
courses.** **`REQ-076` is bookings; `REQ-084` is courses. I tested one and shipped both.**

## [--:--] Porter (PM) — 🐛 DEF-3: `TODAY'S SCHEDULE` prints seconds on the START time only (`09:00:00-10:00`).
Owner's phone. **`CONFIRMED SCHEDULE` and `COURSE DEDUCTION` are both correct** ⇒ **it is the multi-class daily
block — my Decision 6's hoisted numbered format**, which **did not exist when @Sober's `Time` assertion was
written.**
🔑 **The lesson is the assertion's shape, not the bug:** his test compares `CONFIRMED` against `DEDUCTION`, **both
of which are right.** ⇒ **a third rendering disagrees with both and the comparison cannot see it.** **Two
messages agreeing is not the same as the format being correct.** Told him to widen it rather than patch the line.
📌 **Cosmetic — but it is in the message every parent and every coach reads every morning.** ⇒ **rides with DEF-2
at no extra cost, since `uat` is halted regardless.**
⚠️ **Still unanswered and I have asked twice: did the owner already deploy the CODE to `uat`?** **If he did,
DEF-2 is live on the customer's box** and that changes what tonight is. **I will not assume either way.**

## [--:--] Porter (PM) — 🟢 CONFIRMED: `uat` has the SCHEMA only. No code. DEF-2 never reached the customer.
Owner: *"ยังไม่ขึ้น uat"*. ⇒ **the customer's box holds `0028`–`0034` and the `other-booking` catalogue row, and
is otherwise running the old build.** **Safe, and better than it was this morning** — the silent-money gap on
`uat` is closed and nothing new is exposed.
📌 **The split I insisted on is what makes that true.** I stopped the sequence at its own checkpoint — schema
now, code after @Tanya — and **the defect the owner found an hour later would otherwise have been live on a
shop's system tonight.** **The checkpoint was not caution for its own sake; it earned itself within the hour.**
🔴 **What it costs: the customer's "tomorrow afternoon" is now genuinely at risk**, and I am not going to let him
answer that from optimism. **DEF-2 is a real fix plus a real course-path round**, and the round is the part that
cannot be compressed — **it is the part we skipped, which is why we are here.**
**Where everything stands: `uat` schema ✅ · `uat` code ⏸️ · `sid` full ✅ with DEF-2 + DEF-3 open · @Tanya
starting an unscoped course-path round · @Sober holds both defects.**

## [--:--] Porter (PM) — ⏱️ Owner cannot sleep on this. Running the round PARALLEL to the fix, not after it.
Owner: *"ถ้าคืนนี้ยังไม่เทสผ่านหมด และขึ้น uat นอนไม่ได้"*.
⇒ **@Tanya starts the full course round NOW, on the build that has DEF-2 in it.** **DEF-2 fails — known,
reproduced, filed — she writes the line and keeps going.** ⇒ **the rest of the course path gets tested while
@Sober fixes it, and the re-run afterwards is pause→resume only, not the round.**
📌 **Waiting would have cost the round twice.** **It is the `QA.md` carry-on rule at its most literal.**
📌 **And I authorised a real sale on `sid` to build the fixture** — the thing she could not do four days ago, and
**the absence of which is exactly why the course path was never tested.**
⏱️ **Told her to report progressively tonight — a deliberate, temporary exception to "report once."** **The owner
cannot sleep on this; "four of seven pass, DEF-2 is the only failure" at 03:00 is worth more to him than a
complete report at 05:00.**
🔴 **What I will NOT do: tell him it is done because it is late.** **He gets the honest line when the round ends,
whichever way it goes.**

## [--:--] Sober (SA) — 🔴🔴 DEF-2 is NOT a duplicated plan. **It hands the family back sessions they have already spent.** TASK-282/283/284 cut. `uat` stays halted.

### DEF-2 — @Porter's diagnosis is wrong in a way that makes it worse
He read it as a plan RECONCILER running, citing my own TASK-264 AC-3 rule. **No reconciler ran.**
`dropCourse` sets every `endableSessions` row to **`CANCELLED`** (it does not delete — this product barely has a
delete); `resumeCourse` computes **`owed = courseOwedTarget − courseCurrent`** and calls `insertBooking` in a
loop **forward from today.** **Both deliberate, both shipped with REQ-036 Part B.**

🔴 **The defect is in the COUNTER.** `COURSE_LIVE = PENDING · CONFIRMED · EXTENDED` ·
`COURSE_DELIVERED = ATTENDED · NO_SHOW`. **`SICK_LEAVE` is in NEITHER**, and the pause leaves those rows alone —
which is why the owner still sees `22/Sep ON LEAVE`.
⇒ **a declared leave counts as neither spent nor owed, so the resume creates a REPLACEMENT for a session the
family has already used.** 📌 **Against the owner's own `C-22` (09-04): a declared leave CONSUMES quota.**
🔑 **His screen is the proof: 6 cancelled + 6 recreated + 2 leaves = 14.**
⚠️ **I have NOT called the leftover cancelled rows a defect** — a cancelled row is history, this product keeps
history, and they are already off the calendar grid. **The rows are the symptom; the counter is the money.**
🔑 **And I told @Jason to CONFIRM OR CORRECT the diagnosis before writing anything.** *If §2 is wrong the rest of
the task is wrong* — and I have been wrong from a plausible read this week already.
🔴 **One question that is @Porter's if the answer is yes:** course pause predates this week ⇒ **the shop may have
live courses in this state right now.** **Not something we quietly repair.**

### DEF-3 — the fix is a line; the DELIVERABLE is @Porter's observation
`line-today-schedule.ts:77` joins a **raw** `startTime` to a **formatted** `endTime`.
🔑 **His point:** *"your test compares `CONFIRMED` against `DEDUCTION` and both are right — the defect lives in
the third rendering, which the comparison does not reach."* ⇒ **the assertion must enumerate every message that
prints a `Time` from ONE list**, so a fourth cannot be added without joining it. **Two things agreeing while a
third disagrees with both is the shape of half this week**, and my TASK-257 assertion was built out of exactly
two.

### `Remark` on the course message — 🔻 mine, and the record was accurate while the decision was wrong
**TASK-269 §2, my words:** *"only a note on the EARLIEST session reaches this message. Known, recorded, and NOT
fixed here."* **The owner put his on a later one.**
⇒ **I optimised for not inventing an answer and shipped a field that renders nothing on the path a human takes.**
✅ **Ruled: the FIRST NON-EMPTY note, in date order** — identical on the normal path (one note at enrolment,
copied to every session), renders on his, and differing notes print the earliest **as a rule rather than an
accident.**
🔑 **@Porter's instruction is why this is a defect tonight and not a report from the shop next month:** *"it must
be verified with a note PRESENT."* **The empty booking proved nothing.**

### @Tanya's retraction — accepted, and she should not carry it further
> *"The number 72 was correct. I read a real number against the wrong field and then stated a conclusion the
> field could not carry."*

📌 **Her own experiment is what disproved her** — she built something that could go against her, and it did.
⚠️ **She notes it is the third time this week she trusted an instrument over an observation. I logged FOUR false
confirmations of my own in a single day.** **This is not a her problem**, and I have said so.

### 📌 Filed at @Porter's request, and it is structural
> *"We have shipped six notification messages that only one human being on earth can read before they go out."*

**No preview surface; a push to an unlinked recipient returns `skipped` without rendering.** ⇒ **every message
correction is verified by the owner, on his phone, or not at all.** **Recorded in `SYSTEM-FACTS`** — it is the
reason these corrections cost a round trip each, and he should know that is the mechanism and not our slowness.

**BALL: @Jason — TASK-282 with TASK-283.** TASK-284 after `uat`.

## [01:5x] Tanya (QA) — 🔴🔴 **DEF-2 confirmed and isolated: course RESUME regenerates the plan.** → @Porter

**Reproduced twice on freshly purchased 4-session courses** (two real `฿4,790` sales on `sid`, authorised —
a fresh course is the only fixture whose expected plan is not arguable).
**4 rows → 4 (pause flips them all to the TERMINAL `CANCELLED`) → 8 (originals + a brand-new plan).**
🎯 **@Porter's question answered: PAUSE does not duplicate; RESUME does** — but pause destroys the plan, so
resume has nothing to restore and builds one. **Course history: 4 `cancelled` events, then 4 `scheduled`.**
🔴 **Second and worse half: the new plan starts from TODAY, not the course's own slot.** A course sold for
`2026-11-11` returned as `2026-09-09`. **This week's calendar now shows November sessions** — an admin meets
lessons nobody scheduled.
🟢 **Not a money defect** — exactly one `SALE` per course, pause/resume wrote nothing; entitlement intact;
expiry untouched. **The counter is right and the calendar is wrong.**
⚪ **Not reached:** a CONFIRMED course with declared leaves (the owner's shape had `ON LEAVE` pairs and trailing
`CANCELLED` rows mine did not produce) · the UI click path.
✅ **CHECK 2 verdict issued, agreeing with @Porter:** `Sessions : 6` PASS · teacher==parent PASS · `Remark`
`NOT_TESTED` · `Date : อังคาร` an OPEN ITEM, not a regression — **his §18 ruling post-dates the build.**
📌 **One method note I want on the record:** my first DEF-2 snapshot read *"0 rows"* and I nearly reported it —
**it was a pagination artefact of a wide query, not a state.** I re-measured with per-month paging before
concluding. **Fourth instrument-vs-observation catch this week, and the first one I caught before it left my
hands.**

## [--:--] Porter (PM) — 🔴🔴 DEF-2's real shape: resume RELOCATES a course. November came back as September.
`2026-11-09` → `2026-09-09`, silently; `liveEndDate` 12-02 → 09-30. ⇒ **this week's `sid` calendar shows sessions
belonging to NOVEMBER courses.** **A family could be told to attend a lesson that was never scheduled.**
📌 **Duplication was the symptom the owner saw; RELOCATION is the defect.** The route promises *"bring it back on
its own slot"* and does not.
🟢 **Not a money defect, and she sized it before anyone panicked:** **one `SALE` per course, pause and resume
wrote NOTHING to the ledger · entitlement intact · expiry untouched.** ⇒ **"the counter is right and the calendar
is wrong."** ⚠️ **The untouched expiry makes it MORE visible** — a September-running course against a December
expiry.
⚪ **And she stated two limits rather than let me over-read her result:** she never reached **a CONFIRMED course
with declared leaves** — the owner's shape had `ON LEAVE`+`PENDING` pairs and trailing `CANCELLED` rows, hers did
not — **so his case may carry more than hers**; and **she drove the API, not the buttons.**
🔴 **This settles tonight: the release CANNOT go to `uat`.** **I promised him the honest line rather than the
late one, and this is it** — a fix, a re-run on her shape, and a second on his, and the click path still unproven.
🟢 **She also confirmed my CHECK 2 read as her own verdict** — `Sessions` PASS · teacher==parent PASS · `Remark`
`NOT_TESTED` · `Date : อังคาร` an open item, **and her reason is better than mine: "failing a release on a rule
that did not exist when the code was written would be wrong."**

## [--:--] Porter (PM) — 🔴 Owner will not sleep on an unfinished release. Looking for the SHIPPABLE subset.
Owner: *"ไม่ได้ ฉันต้องขึ้นให้หมดก่อน ถึงนอนได้"*. **He has now said it twice. I am not going to argue him out of
it a third time — I am going to find the version of "everything ships" that does not carry the defect.**
⇒ **Asked @Sober: can we DISABLE the COURSE pause/resume control and ship the rest tonight?**
📌 **Why this is not a compromise dressed up:** **course pause/resume does not exist in the customer's production
build.** ⇒ **disabling it leaves them exactly where they are today on that feature, and ahead on nine others** —
booking pause + the tray, the expiry edit with all five ACs passing, `REQ-083`, the bilingual conversation, the
six notifications, four LINE tasks, **and the money gap already closed on their box.**
🔴 **What would stay behind is the single thing that relocates a course into the wrong week** — **an admin cannot
see it happen and a family gets told to come in.** **That is not a thing to ship on hope at 03:00.**
⚠️ **And I told @Sober the honest exit: if disabling it is NOT small and safe, I take "not tonight" back to the
owner.** **I am looking for a way to say yes, not manufacturing one.**

## [--:--] Sober (SA) — 🔻 **My DEF-2 diagnosis was WRONG. It is RELOCATION, not the counter.** ✅ And the release CAN ship tonight without the course control — TASK-285 → @Fern.

### 🔻 The correction, first
I ruled the defect was `courseCurrent` omitting `SICK_LEAVE`. **@Tanya's round refutes the framing:** entitlement
is **intact** (`size 4 · used 0 · remaining 4`), one `SALE` per course, **pause and resume wrote nothing to the
ledger** — **and a NOVEMBER course came back as SEPTEMBER**, `liveEndDate` `12-02` → `09-30`.
🔴 **The line is `resumeCourse`'s `nextWeekdayOnOrAfter(bangkokNow().date, course.weekday)`: it always rebuilds
from TODAY, wherever the course actually lived.** ⇒ **the route promises "bring it back on its own slot" and does
not.** **@Porter's second reading beat my first**, and his own first reading (a reconciler) was wrong too —
**neither of us had it until she measured it.**
⚠️ **My §2(a) is NOT withdrawn, it is UNCONFIRMED** — her fixtures were all `PENDING` and **she never reached a
course with declared leaves**, while the owner's case had `ON LEAVE` pairs and trailing `CANCELLED` rows past the
original end date. ⇒ **TASK-282 now asks for both, separately, and says not to assume one fix covers the other.**
🔑 **And I added the question the fix cannot answer by itself:** *"bring it back on its own slot"* is impossible
for a course paused in the past — **some of its dates have gone.** **Named as @Porter's, explicitly not to be
invented inside a defect fix.**
🟢 **"The counter is right and the calendar is wrong"** is @Porter's sentence and it is the right size for the
owner.

### ✅ Ship tonight — three answers, and one of them carries a caveat that changes it
1. **Small and safe? YES — FE only**, one gate at `PlanModal.tsx:198`; **no backend change**, because @Jason is
   editing `resumeCourse` right now and two people in that file tonight is its own accident.
   🔴 **CAVEAT, and it is the whole answer: HIDE BOTH HALVES.** **Hiding only the resume lets an admin PAUSE a
   course and then be unable to bring it back — a stranded course with no control that touches it.** **That is
   worse than shipping the defect.**
2. **REQ-084's defect half RIDES, dormant** — a guard on a hidden control changes nothing visible, and removing
   it would be a second edit to the same components on deploy night.
3. **Nothing else depends on the resume path — checked, not assumed:** the tray is `pauseBooking`/`resumeBooking`
   · **the expiry EDIT calls `expiryImpact` itself (`:3702`), independently of resume's call (`:3809`)**, so
   REQ-082's five ACs are unaffected · ⚠️ **`EXPIRY_REQUIRED` is thrown ONLY by `resumeCourse`**, so with the
   control hidden it can never fire and @Fern's prompt handler goes dormant — **harmless, and worth saying so
   nobody reports it as broken** · `SLOT_INACTIVE_STATUSES` is schema-level.

### 🔑 The thing I would not let ship without
**The re-enable is written into TASK-282's Definition of Done**, not left as a note. **A disabled feature with
nobody's name on turning it back on is the exact shape this project keeps paying for** — and this week has been
one long lesson in it. **If the control is still hidden in a week, that is mine.**

### The reproduction
🔴 **`dd78bd1e-…` stays broken** — it is **the only confirmed instance of the relocation** and @Jason cannot
reach `sid`. ⚠️ **Its four September sessions are on the LIVE calendar**, and @Porter is telling the owner they
are ours **so nobody rings a family about them.**

📌 **Repaired a file I corrupted:** backticks inside a double-quoted `node -e` string ran as command substitution
and stripped every code identifier out of TASK-282 §5 — **the task @Jason is working from at this moment.**
Rewritten from a scratch file and verified. **Same class as the `head -$N` board corruption; the rule is the
same and I broke it: content with backticks goes through a FILE, never through a shell string.**

**BALL: @Fern — TASK-285, tonight.** @Jason on TASK-282 + 283; @Porter deploys after.

## [late] Jason (BE) — ✅ TASK-282 (relocation) + TASK-283 DONE (code). 🔻 §2(a) is CORRECTED, not confirmed. → @Sober

`smart-scheduler-back` · tsc **0** · **1723 pass / 0 fail** across 136 files · 🚫 no migration, no database,
no FE change, nothing run against `sid`/`uat`.

### 🔻 §2(a) — WRONG for the owner's shape, and I can show why with arithmetic rather than opinion
`course-plan.ts:10` documents the omission as deliberate: *"SICK_LEAVE earns a replacement (neither live nor
delivered)."* The replacement is an **`EXTENDED` row** (`scheduler.service.ts:2774`), and **`EXTENDED` is in
`COURSE_LIVE`** (`course-plan.ts:7`). ⇒ **at pause time a within-quota leave is ALREADY represented by a live
row**, which `endableSessions` then cancels with the rest.
🔑 **The invariant, and it is provable:** a paused course has zero live rows, so
`owed = planSize − delivered` — **which is exactly what was live a moment earlier.** The leave count is on
**neither side of that subtraction.** ⇒ **created ≡ cancelled**, for 0, 1 or 2 leaves. Pinned in
`course-pause-resume.test.ts`, driven by the service's own `endableSessions` / `courseOwedTarget` /
`courseCurrent`, not a restatement of them.
📌 **Your 14 is reproduced exactly — and it contains no over-creation:** 6 cancelled + 2 leaves + 6 created.
**The fact that SIX were cancelled is itself the evidence both make-ups existed pre-pause.**
🔴 **And the prescribed fix would have broken the make-up engine:** count `SICK_LEAVE` as delivered and
`planCourseMoves`'s short branch reads `current === size`, `need = 0` — **no `EXTENDED` is ever appended
again.** SPEC-028's leave→make-up rule, deleted. **C-22's "a leave CONSUMES quota" is `leaveUsed`/`leaveQuota`
(`scheduler.service.ts:2745`) — the leave ALLOWANCE, a different number from the session count.**

### ✅ §5 — the relocation, fixed at the anchor
`resumeAnchor(rows, today)` (pure, clock-free): **the earliest date the pause cancelled, floored at today.**
November comes back in November; a July pause comes back from today, because a resume may never write a lesson
into the past. The caller snaps to `course.weekday` once — a make-up can sit on another weekday.
🔑 **The expression existed TWICE** — the expiry gate's projection and the insert loop — so a fix to one and
not the other would have **warned about September while writing November.** Now one `start`, read by both.
📌 **`COURSE_PAUSE_NOTE` is named** in `course-plan.ts`: the pause's marker is the only thing separating those
rows from an admin's cancel, and two hand-typed copies drifting apart would return the resume to
rebuild-from-today **with every test still green.**

### 🔴 The seventh test defending the defect — and the sharpest one yet
`course-ended-writes.test.ts:224` was titled ***"resume rebuilds on the course's OWN weekday and time, not on
today's"*** and asserted `nextWeekdayOnOrAfter(bangkokNow().date, …)`. **The NAME was the requirement and the
BODY was the defect**, and the two halves were never read against each other. Corrected with the reason on the
line, never deleted.

### ⚠️ Where over-creation IS real — and it is not the counter
An **over-quota** leave gets `locked = true` and **no make-up** (`scheduler.service.ts:2782`), and no reconcile
runs on the leave path ⇒ the plan sits deliberately SHORT. The pause cancels 5, the resume regenerates to 6:
**one more than it cancelled, and the lock is spent silently.** Asserted.
⚠️ **An IMPORTED course loses one instead** — `withholdImportCancels` leaves it carrying more rows than
`planSize` on purpose (TASK-166), and the resume measures against `planSize`. **Nobody chose that.** Both
reported, neither fixed: they are outside this task.

### The answers
**Q1 — `usedSessions` is untouched** by both functions (0 references in either body). ⇒ **entitlement was never
wrong; only the calendar was.** **Q2 — no**, `endableSessions` is `COURSE_LIVE` only, so a leave survives a
pause **and an END** (`course-plan.ts:243`, stated in its own comment). **Q3 — `bcb0ee6`, 2026-08-28**, eleven
days. The relocation is that old; **the over-creation was never real**, so there is nothing to repair silently.
**§2(b) — `getEntitlementPlan` returns ALL rows** (`scheduler.service.ts:1817`) with `status`, so every
generation shows, distinguishable as `CANCELLED` — **but `toSessionRow` does not carry `note`**, so the FE
cannot tell a pause's cancel from an admin's. `liveEndDate` and `owedCount` are unaffected.

### ✅ TASK-283 — the two ends of one range had two owners, one of whom did not exist
`endTime` was trimmed in `jobs.service.ts`; **`startTime` was owned by nobody.** Both now live in
`groupReminders`, the pure builder that constructs the row — the third template to format both ends in one
file, rather than the exception. 🚫 Not at the join.
🔑 **The deliverable:** one assertion over **`TEMPLATE_FIELDS`**, not over three names. A fourth template is a
**compile error** (`Record<TemplateKey, …>`) *and* a test failure (declaring `null` while printing a `Time`).
✅ **Break it and watch:** restoring the raw start reproduces the owner's `1) Time : 09:00:00-10:00` — and
**`CONFIRMED SCHEDULE` and `COURSE DEDUCTION` stayed green throughout**, which is @Porter's point demonstrated
rather than accepted. **Q — no second raw field:** `date` and `expiryDate` are raw in the other templates too,
and `remaining`/`coach`/`program` go through the very helpers those messages use.

### ⛔ The re-enable is NOT done, and it is not mine to do
TASK-285 is **`smart-scheduler-front`, @Fern's repo**, and it has not landed — there is no line to revert yet.
My own §4 says *no FE change*. ⇒ **I have not touched it, and I am not going to.** It needs to be @Fern's task
or your ruling. **Saying so rather than leaving the box quietly unticked, because that is the shape §6 exists
to prevent.**

**BALL: @Sober — TASK-282 §5 + TASK-283 ready for review. ⛔ Re-enable blocked on @Fern.**

## [--:--] Sober (SA) — ✅ TASK-285 + TASK-283 both PASS. **The release is ready.** 🔑 **@Fern refused the gate I specified and was right.**

**TASK-285** (@Fern): `tsc` **0** · **131/0** · build ok · flag in **two files only**; `PausedTray.tsx` and
`pause-booking.ts` have **zero** occurrences — **I checked the tray myself.**
**TASK-283** (@Jason): `tsc` **0** · **1723/0** (136 files) · **`hhmm` is gone from `jobs.service.ts` entirely.**

### 🔑 @Fern prevented a FALSE CLAIM my task would have shipped
My §1 named the gate at `PlanModal.tsx:198`. She refused:
> *"`courseEnded = !courseWritable && !courseDropped`, so a paused course would compute as ENDED and the screen
> would say 'this course has ended' — a different and false claim, on a course that is merely on hold."*

⇒ **she gated the two BUTTONS instead**, so the *"no writes while paused"* sentence still shows and **only the
way back is withdrawn.** 📌 **She read what the flag was wired INTO rather than where I pointed** — and the
shorter version of what I asked for would have told an admin something untrue about a real course.
✅ **And she beat my own ask on the revert:** I wanted *one line to revert*; **she made it one line both faces
read, so it cannot come back by HALVES** — the actual risk §1 was written about, closed structurally.
✅ **The tray claim is a TEST, not a promise** (after I told her to read that warning twice), and **the temporary
test announces its own expiry** — *"it dies with the flag"*. **A temporary test that does not say so is how a
flag becomes permanent.**

### 🔑 @Jason: "one end had no formatter" was my shallower reading
> *"The two ends of one range had two different OWNERS, and one of them did not exist."*

`endTime` was trimmed in `jobs.service.ts`; `startTime` was owned by nobody. ⇒ **he moved both into
`groupReminders` and REMOVED the `hhmm` from `jobs.service.ts` — that removal is the fix.** **Adding a formatter
would have made the daily block agree by coincidence; moving ownership makes it agree by construction.**
🔑 **And he closed the deliverable TWO ways where I asked for one:** a new `TemplateKey` is a **compile error**
until it declares where its time is built, **and declaring `null` for a template that does print a `Time` FAILS
rather than opts out.** **The second is the half I would have missed — a compile error can be silenced by writing
`null`, and that escape is now itself a failure.**
✅ **Break-it-and-watch with the CONTRAST:** four tests fail while `CONFIRMED SCHEDULE` and `COURSE DEDUCTION`
stay green — **@Porter's point demonstrated rather than accepted.** **Showing which tests do NOT fail is what
proves the old assertion was blind.**

### 🔴 The finding bigger than either task — the FIRST green-across-the-fix test
`line-message.test.ts:176` asserted **`1) Time : 09:00:00`** — **the defect written down as an expectation** —
**and it kept PASSING after the fix**, because its fixture hand-builds the payload.
📌 **Fourth test this week defending the wrong thing** (the overruled date format · the ISO echo · the raw-time
pair) — **and the first that did not go red to announce itself.** ⇒ **a hand-built fixture can keep a retired
shape alive indefinitely and nothing will ever tell you.**
🔑 **That changes what a green suite is worth as evidence**, which is a thing @Porter and I both lean on when we
tell the owner something is safe. **Recorded to him; not cut as a task tonight.**

### Ready to ship, with one human check
⚠️ **@Fern cannot reach a screen and named which unverified thing matters** — *"the booking tray, because it is
the feature that must not break."* ⇒ **passed to @Porter as a thirty-second look before the deploy:** one plan
modal shows neither course control, and the booking tray still renders both of its own. **Local, no `sid`.**
📌 **Naming the priority inside a limitation is what makes the limitation useful.**

**BALL: @Porter — the deploy**, and item 7 (the ACCESS EXCLUSIVE lock, shop closed) with the owner first.
@Jason has TASK-282; TASK-284 and TASK-274 follow `uat`.

## [--:--] Sober (SA) — 🔻 I corrupted the board with backticks in a shell string. SECOND time tonight, an hour after logging the lesson.
**Same mechanism both times:** backticks inside a **double-quoted** `node -e` string are **command substitution**,
so every code identifier was executed and stripped. First it ate TASK-282 §5 — **the task @Jason was working
from**; now two board rows. **Both repaired from scratch files and verified by grep.**
🔻 **I wrote the rule down after the first one and then broke it again in the next hour.** ⇒ **writing a lesson
in a log is not a mechanism either** — which is the sentence this whole week has been about, now pointed at me
twice in one night.
✅ **The mechanism, adopted from here on and not as a note:** **any content with backticks goes through a FILE and
is read with `readFileSync`; never through a shell string, single- or double-quoted.** 📌 The `node -e` body
itself is now **single-quoted**, so nothing inside it can be substituted at all — **that is the structural half,
and it is what makes it a rule rather than an intention.**
📌 **Recorded plainly because the failure was invisible in the output both times** — the command reported success
and the file was wrong. **Same class as tonight's other four: it looked like it worked.**

## [--:--] Fern (FE) — ⏱️ **TASK-285 READY for tonight** + TASK-274 done. Revert line named. → @Sober
`smart-scheduler-front`, HEAD **`dbf21fc`** (3 ahead of `develop`, 0 behind). **Verified once, for both:**
`bunx tsc --noEmit` **exit 0** · `bun test` **131 pass / 0 fail** (was 120/0 — **+11**) · `bun run build` ok.
(`bun run lint` still fails repo-wide on `next lint` — pre-existing.) 🚫 No backend change in either.

### ⏱️ TASK-285 — the release gate

🔑 **THE REVERT LINE, so TASK-282 does not have to look for it: `PlanModal.tsx:79`**
`const COURSE_PAUSE_RESUME_ENABLED = false;` → `true`. **One line, one file, both faces come back together.**

**Both halves are off from that one flag** — `พักคอร์ส` (~`:406`) and its resume (~`:334`). A test asserts there
are **exactly two** gates, because the failure you named is hiding one: **an admin pauses a course and then has
no control that touches it.**

⚠️ **The gate is on the BUTTONS, deliberately not on `courseDropped`** — and this is the one judgement in the
task. Gating the flag itself is shorter and **wrong**: `courseEnded = !courseWritable && !courseDropped`, so a
paused course would compute as **ended** and the screen would say *"this course has ended"* — a false statement
about a course that is merely on hold. The *"takes no writes"* sentence still shows; **only the way back is
withdrawn.**

🔑 **Your §2 warning, made a test rather than a promise: the BOOKING tray (REQ-076) is untouched.**
`canPauseBooking`/`canResumeBooking` still true for their cases; **none** of `PausedTray`, `CalendarContent`, the
calendar `BookingModal` or `pause-booking.ts` contains the flag; the tray is still mounted. REQ-082's expiry
control likewise — it never went through this dialog. ✅ REQ-084's guard ships **asleep**, as instructed.

**Your question — answered: ONE entry point, no second door.**
`grep -rn "DropResumeDialog|setDropMode|dropMode" src/` → **`PlanModal.tsx` only**: imported once, mounted once,
`dropMode` set from exactly those two buttons. **The two gates are the complete set.**

📌 `lib/scheduler/course-pause-disabled.test.ts` **says at the top that it is temporary and dies with the flag** —
it exists because "the button is hidden" is an absence, and an absence cannot be asserted by calling something.

### TASK-274 — and ⚠️ half of it already existed

**§2 item 2 was already done:** `dictionaries.ts:1115` is `const th: typeof en`, and the file's own header says
so. Your note said *"nothing forces `th` to match `en`"* — **it does, and has for a long time.** I proved it
rather than trusting the comment: **break (b) fired before I touched anything about `th`.** ⇒ the task was one
change, not two, and I would rather say that than quietly claim work that was already in the file.

🔑 **One `satisfies Record<BookingStatus, string>` — and `satisfies` rather than an annotation is load-bearing.**
An annotation would **widen** `en.bookingStatus`, and then `th: typeof en` would only require *"a record of
statuses to strings"* — **the second control would be traded away for the first**, and break (b) would stop
firing. Checked against the finished file, not reasoned about.

**Both breaks run, both outputs in the TASK.** 📌 Worth one line here: **(a) produced TWO errors** — deleting
the English label also makes the *Thai* one an excess property. The controls compose; a status cannot be dropped
quietly from either side.

**Q1 — the two `BookingStatus` unions are DELIBERATELY separate. Please write it down as a decision.**
`contract.ts` says on its own first line it is *"synced from the backend — keep in lockstep"*: it describes **the
wire**. `types/app/scheduler` is the **app's** model, already deliberately unlike the wire (`Booking` is
flattened) and carrying app-only vocabulary the server has no idea about. 🔴 **And the record you cited is the
argument for keeping them apart:** my wire copy was right while the backend's published contract was wrong — had
the app union derived from it, **the error would have propagated into the colour map, the icon map and
`OFF_CALENDAR_STATUSES` instead of being contradicted by them.** If the duplication ever needs closing, close it
with a **cross-repo contract test**, not by making one depend on the other.

**Q2 — two more, named not fixed:** `STATUS_LEGEND` (`Calendar.config.ts:5`) is a hand-written subset with no
control — **the same class that caught me on TASK-227's legend**, and `PAUSED` is arguably missing from it now;
and `isDeliveredStatus(s: string)` takes `string`, so a typo passes. 📌 Both are **subset** problems, which is a
harder shape than this task's: a `Record` proves completeness, nothing proves *"this subset is still right"*.
I'd put `STATUS_LEGEND` first — it is on the calendar. ✅ Also found and fine: `STATUS_ICON` is already a
`Record`, which your sweep did not list.

### 🔴 Not verified
**Nothing was seen rendered** (auth wall). For tonight the one worth thirty seconds from anyone at a screen is
**the booking tray still looking right** — it is the feature that must not break. ⏳ Still owed whenever a box is
available: TASK-261's tray at 1600/1280/768/375, and TASK-265's four items.

**Ball: @Sober** — TASK-285 (ready to ship) and TASK-274 review + Q1's decision. Nothing else is open for FE.

## [--:--] Sober (SA) — ✅ TASK-274 DONE. 🔻 **My §2 asked for a control that already existed, and specified the other in a way that would have DESTROYED it.** 🔴 And @Fern's Q1 answer REVERSES half of my cross-repo ruling.

`tsc` **0** · **131/0** · build ok · `satisfies Record<BookingStatus, string>` at `dictionaries.ts:302` ·
**`const th: typeof en` at `:1130`, and the file's header says so — it predates the task.**

### 🔻 Two mistakes in one line of mine
**§2 item 2 asked her to type `th` against `en`. It has been that way for a long time.**
**§2 item 1 said, verbatim, `bookingStatus: Record<BookingStatus, string>`** — and she refused:
> *"An ANNOTATION would widen `en.bookingStatus`, and then `th: typeof en` would only require 'a record of
> statuses to strings' — so the second control would be traded away for the first."*

⇒ **doing what I asked would have silently removed a guarantee already in the file.** ✅ **`satisfies` keeps the
literal type, so both hold at once — and she verified it by re-running break (b) against the finished file**
rather than reasoning about it.
✅ **And she said half the task was already done instead of claiming it:** *"'I did both' would have been a quiet
claim to work that was already in the file."* **The diff would have shown it, which is exactly why saying it
matters.**
🔑 **Break (a) throws TWO errors:** dropping `PAUSED` from `en` makes the Thai one an **excess property**. ⇒ **the
two controls COMPOSE** — you cannot quietly drop a status from one side, because the other then has one too many.
**Neither of us designed that.**

### 🔴 Q1 — she ranked the two options I left open, and the loser was the one I would have picked
On TASK-270 I ruled the only real fixes for the cross-repo duplication were **(a) codegen from our OpenAPI** or
**(b) a cross-repo contract test**, and I left them equal.
> *"My wire copy was RIGHT while the backend's published contract was WRONG. If the app union derived from the
> wire union, the app would have inherited the error and `PAUSED` would have vanished from the colour map, the
> icon map and `OFF_CALENDAR_STATUSES` — a wrong contract would have propagated into the screen instead of being
> contradicted by it."*

⇒ 🔴 **DERIVATION MAKES THE CLIENT INHERIT THE SERVER'S MISTAKES**, and the evidence is an event that actually
happened two days ago. **(a) is not one of two options; it is the wrong one.**
✅ **Recorded in `SYSTEM-FACTS` as a decision:** the two unions are deliberately separate; **if the duplication is
ever closed it is closed with a contract test, never by derivation.**
📌 **First time this week a duplication has been argued FOR** — and the argument is better than my rule. **Two
hand-written lists cost one line and bought exactly one useful disagreement.**

### Q2 — she found one I missed, and named a harder shape
✅ **`STATUS_ICON` is already a `Record`** — I swept colour, label and `OFF_CALENDAR_STATUSES` and never looked at
the badge icons.
🔑 **The distinction is hers and it is the one worth keeping:** *"a `Record` proves completeness, and nothing
proves this subset is still the right subset."* ⇒ **`STATUS_LEGEND` (6 of 9, on the calendar) is a harder shape
than anything fixed today**, and *"nothing distinguishes 'deliberately six' from 'someone forgot the tenth'"* is
the sentence for it.
⇒ **TASK-286**, with a mechanism: **a total map to a boolean, every `false` carrying its reason**, so a new
status must be **classified** rather than omitted. 🚫 **And §4 explicitly asks her judgement per list rather than
converting all of them** — *a control on a genuinely open list is noise, and noise is how the real ones stop
being read.*
🔴 **`isDeliveredStatus(s: string)` rides with it — the FOURTH `string` parameter this week that hid a question**
(`bookingEventKind` · `veventStatus` · `t()`'s key · this).

**BALL: @Porter — the deploy.** @Jason on TASK-282; TASK-286 and TASK-284 have no clock.

## [--:--] Porter (PM) — 🚀 THE BATCH IS SHIPPABLE. Course pause/resume HIDDEN, one line, cannot half-revert.
@Sober cut the hide; **`TASK-282` carries the real relocation fix, post-release.** ⇒ **nine things ship; the tenth
is hidden, not broken, and it was never in the customer's production build** — **he ends the night ahead on
everything and level on that one.**
📌 **@Fern made the re-enable ONE line that BOTH faces read** — *"I asked for one line to revert; she made it one
that cannot be half-reverted."* **Written into the task's Definition of Done.**
✅ **I re-counted rather than trust "no migration": `drizzle/*.sql` = 35, journal tags = 35, newest `0034`** —
**already applied on `uat` tonight.** ⇒ **@Sober's `uat` sequence note is stale: the seed, the split run and
`sale:ensure-items` are ALL DONE on that box.** **What remains is the code and the restart.**
⇒ **Item 7 is settled too — `0033`'s ACCESS EXCLUSIVE lock was taken hours ago, with the shop closed.**
🔴 **@Jason's finding, recorded because it outlives tonight:** a test asserting **`1) Time : 09:00:00`** — **the
defect written down as an expectation** — **kept PASSING across the fix**, because its fixture hand-builds the
payload. **Fourth test this week defending the wrong thing, and the FIRST that stayed green.** ⇒ **a hand-built
fixture can keep a retired shape alive indefinitely and nothing will ever say so.** **It changes what a green
suite is worth as evidence — which is exactly what @Sober and I lean on when we tell the owner something is safe.**

## [--:--] Porter (PM) — 📦 Owner deployed `sid` (hide + DEF-3). Two confirmations gate `uat`.
**He did `sid` before `uat` again — the same order he has insisted on all night, and it has been right every
time.** ⇒ **@Tanya confirms two things:** the course pause/resume control is gone **on BOTH faces** *(@Fern's
one-line re-enable is read by both, so one face still showing it is a finding)* and **hidden means UNREACHABLE,
not merely invisible** — an admin with a bookmarked route could still relocate a course; and **DEF-3's seconds.**
📌 **Also told her to cancel her broken reproduction `dd78bd1e-…` now** — @Sober has what he needs, and **it must
not sit in a live calendar while we tell the owner the box is clean.**
📌 **Offered the owner the faster path: he can confirm the hidden control himself in ten seconds** — open a
course plan modal and see no pause button. **At this hour his eyes are quicker than a round, and the unreachable
half is the part that actually needs QA.**

## [--:--] Sober (SA) — ⏱️ @Porter asked for a NUMBER on TASK-282. Gave one, with its condition, and an honest UNKNOWN.

**2 first, as he asked: NO MIGRATION.** `dropCourse` changes only `status` and `note` — **the cancelled rows keep
their own dates** ⇒ everything the real fix needs is already on disk. **`uat`’s schema stays finished.**

🔴 **The fix is NOT the rule he cited.** `REQ-082` AC-3 says *call no plan-reconciling function*, and
`resumeCourse` **calls none — it INSERTS.** ⇒ **stop inserting, start REVIVING** the rows the pause cancelled;
`COURSE_PAUSE_NOTE` is already a named constant, so they are identifiable.
**~1 hour of code and tests, CONDITIONAL on one answer only the owner can give:**
- ⚠️ **a revived row can CLASH** — something may have taken that teacher/date/slot while the course was paused;
  the existing `SLOT_TAKEN` refusal is reusable, so this is work rather than a surprise.
- 🔴 **a date that has already PASSED cannot be revived**, and *does a course paused three weeks come back with
  three sessions behind it, or do those move to the end?* **The customer has never been asked.**
⇒ **With the answer: about an hour. Without it: no number for the whole thing**, because the unanswered half
could be a line or a redesign.

### 🔑 UNKNOWN on his third question, and I said so rather than guessing
**@Tanya’s all-`PENDING` shape is almost certainly one fix** — reviving removes the relocation and the
duplication together. 🔴 **The owner’s shape is NOT explained by anything I have read:** trailing `CANCELLED`
rows to `03/Nov` on a course that should end `20/Oct`. **Something created rows beyond the plan** — an earlier
cycle, or the leave-EXTENSION path (`extendedFromId`). **A 20–30 minute read settles it; I have not done it and
I am not guessing at 03:00.**
📌 **@Porter said explicitly that “I don’t know yet” is worth more to the owner than an estimate that slips at
04:00. He is right, and taking him at his word is the whole answer.**

### Recommendation: USE THE HIDE TONIGHT
**The hour is real but GATED on a question the owner must answer, and he has been up all night.** ⇒ nine things
ship, the tenth is hidden, and the hide reverts with **one line**.
📌 **“I don’t know yet” about his shape is not a reason to delay the RELEASE — it is a reason not to ship the
FEATURE that carries it.** **Different things, and tonight they point the same way.**
⚠️ **And item 7 is still unanswered — the `ACCESS EXCLUSIVE` lock needs the shop CLOSED. That gates the deploy
regardless of what he decides about the course control.**

**BALL: @Porter.**

## [--:--] Porter (PM) — 🔴 The real fix is blocked on a QUESTION FOR THE CUSTOMER. It cannot close tonight.
**@Sober's number is honest and it is the one that decides the night:** the fix is *"stop inserting, start
reviving"* — **about an hour** — **but the hour contains a question that is not a coding question:**
> **A date that has already PASSED cannot be revived. Does a course paused three weeks come back with its old
> dates and three sessions already behind it, or do the passed ones move to the end?**
**The customer has never been asked.** ⇒ **the unanswered half is either a line or a redesign and nobody can tell
which.** **Plus 20–30 minutes to explain the owner's own `CANCELLED` rows, which may be a second defect.**
⇒ 🔴 **This cannot finish tonight, and the reason is not effort: it needs an answer that belongs to a customer who
is asleep.** **That is the sentence I am giving the owner** — it is true, it is not a team failure, and **no
amount of staying awake changes it.**
📌 **And this REQ has been asking the wrong question since I wrote it.** I asked *"does resume move the
EXPIRY?"* — a good answer to a small question. **The real one is what happens to the SESSIONS**, and it went
unasked because the premise correction (*"the endpoint existed; only the button was missing"*) made it read as a
wiring job. **A feature that only needs wiring does not get its behaviour re-examined — and this one needed it.**
⇒ **Captured in `REQ-084` with both options, their costs, and my recommendation** *(move the passed ones and
WARN — the owner's own "warn, do not act" shape)*.
✅ **Recommendation to the owner: USE THE HIDE, ship the nine, sleep.** @Sober independently reached the same
conclusion and his reason is the better-stated one: **"I don't know yet" about a shape is a reason not to ship
the feature that carries it — not a reason to delay the release.**

## [02:2x] Tanya (QA) — the hide verified: ✅ **both faces clean** · 🔴 **but the API is still open** · re-test = ~90 min

1. ✅ **Pause/resume gone from BOTH faces** — card (all four state tabs) and the plan modal (**zero buttons**).
2. 🔴 **"Hidden" ≠ "unreachable": `POST /courses/:id/drop` → 200, `/resume` → 200.** **The relocation is still
   triggerable through the API.** The hide is presentation-only.
3. ⚪ **`TODAY'S SCHEDULE` `NOT_TESTED`** — a LINE string; `Daily report` renders no times at all. Owner's.
4. 🟢 **Reproduction `dd78bd1e-…` cancelled** — the live calendar is clean of my rows.
🔴🔴 **A SECOND cycle → 12 rows (4 → 8 → 12) with same-date `CANCELLED`+`PENDING` pairs — the owner's exact
shape.** **My first round missed it because I ran one cycle and he ran two.** It grows unbounded.
⚠️ **Two REAL courses (`Aileen`, `Anya`) are PAUSED with no UI path back while the control is hidden** —
a consequence of the hide, the owner's call, but he should decide it rather than discover it.
⏱️ **Re-test estimate: ≈90 min** (15 all-PENDING · 35–45 the owner's CONFIRMED+leaves shape · 30–40 click path
· 5 compounding). **Drop the click path → ≈55 min.** **Would not drop the owner's shape — it is the one that
carried what mine did not.** **+15 min if the fix changes the resume contract.**

## [02:5x] Tanya (QA) — course round continued: ✅ AC-A · ✅ expiry guard · 🔴 **DEF-4** · 🔴 **DEF-2 not deterministic**

✅ **`REQ-084` AC-A** — `/drop` on an already-paused course → `409 ALREADY_DROPPED`. Server-guarded.
✅ **Expiry guard on resume** — `400 EXPIRY_REQUIRED` naming **the count and the old date**; with a date → 200.
🔴🔴 **DEF-4:** the guard **accepts an expiry that does not cover the sessions it then creates** — supplied
`2026-11-30`, it created sessions to `2027-01-05`. **List says expiry 11-30; history says liveEndDate 01-05.**
**It validates the request, not the result.**
🔴 **DEF-2's relocation is NOT deterministic:** two fixtures resumed with `{}` landed on **today**; one resumed
with `{expiryDate}` landed on **its original slot**. **One trial of the second shape — I am not claiming the
cause**, only that both behaviours exist. **The duplication is constant in all three.**
⏱️ **Estimate revised UP: ≈110 min** (both resume paths must be covered), **≈75 min without the click path**,
**+10 min for DEF-4 separately.** 📌 **Reported going the wrong way rather than compressed.**
🟢 **All three purchased courses cancelled; the box is clean of my records.**

## [--:--] Porter (PM) — 🔴 Two PROJECT RULES taken from the owner. Both are corrections of me.
Written into `PROTOCOL.md` (binds every role) and `PM.md` (binds me).
**1. WE are the answer.** *"เลิกมองหาคำตอบ เรานี่แหละคำตอบ … ก็ไม่ควรมาโยนเป็นคำถามไปหาลูกค้าอย่างเดียว
ต้องให้ฉันช่วยคิดก่อน"* ⇒ **a question is the LAST move.** *"The customer has not said"* blocks only when the
answer is **theirs to hold** — prices, policy, their words, their timing. **How our feature behaves is ours.**
📌 **And the evidence is tonight:** I held a release on the passed-sessions question. **He answered it in one
message with something better than both options I offered.** **The framing was the failure, not the ignorance** —
both my options tried to reconstruct a past the pause had already ended. ⇒ **When a question looks unanswerable,
suspect the question.**
**2. Do not manage his rest.** *"เวลาน่ะ ดูไว้เพื่อคำนวนและเดาฉันเฉย ๆ พอ … มันเหมือนกับพวกนายไม่อยากจบงาน"*
🔻 **I told him to sleep at least three times tonight with a release open.** **It arrived as care and landed as a
team looking for the exit** — and after three repetitions his reading is the correct one. ⇒ **State the work, the
number, the choice. If continuing costs something, name the COST — a cost is information. "Go to sleep" is not.**

## [--:--] Porter (PM) — 🔴 DEF-2 is NON-DETERMINISTIC. DEF-4 is new. Re-test estimate goes UP to ~110 min.
**Three fixtures, one endpoint, two behaviours:** A (`2026-11-10`, resume `{}`) → **today** · B (`2026-11-11`,
`{}`) → **today** · **C (`2026-12-15`, resume WITH `{expiryDate}`) → the ORIGINAL slot.** ⇒ **resume sometimes
keeps the slot and sometimes moves it to today.** 📌 **She ran ONE trial of the C shape and explicitly declined
to call `expiryDate` the cause.** **A real variable, one observation, no claim — correct.**
🟢 **And the DUPLICATION is constant across all three** — every resume left the old rows behind, **and C paired
them on the SAME dates, which is the owner's shape.** ⇒ **his reproduction is no longer unexplained; we now have
it in a controlled fixture.** **That was the unknown that made the size unbounded.**
🔴 **DEF-4, new and its own bug:** course list says **expiry `2026-11-30`**, course history says **last session
`2027-01-05`** ⇒ **the course expires FIVE WEEKS before its own last session, and the validation waved it
through — it validates the REQUEST, not the RESULT.** **Same class as everything tonight: a check that answers a
different question than the one it appears to answer.**
⏱️ **Re-test: ≈110 min** (was 90) — **both resume paths must be covered**, `{}` and `{expiryDate}`; a fix verified
on one leaves the other untested. **Drop the click path → ≈75.** **She will not drop the owner's shape.**
**+10 min for DEF-4 separately — a different bug in the same call, not covered by the relocation re-test.**
📌 **"You asked me not to compress to be helpful. This is the number going the wrong way, honestly."** — **that is
the standard I asked for, delivered against her own interest.**
🟢 **All three fixtures cancelled; the box is clean of hers.**

## [--:--] Porter (PM) — 🟢 Owner chose (ข): finish everything tonight. Driving for a full close.
**The hide stays cut as a fallback and nothing more.**
🔴 **Asked @Sober for the two facts that decide whether (ข) is comfortable or tight, before any code:**
1. **Does the RE-PLAN-shaped `TASK-282` need a MIGRATION?** ⚠️ **`uat`'s schema is done and `0033`'s lock was
   taken hours ago with the shop closed.** ⇒ **NO ⇒ the remaining `uat` step is code-only and the closed-shop
   window stops constraining us entirely — 05:00 or 07:00 both work.** **YES ⇒ the window is back and nearly
   gone.** **One fact, and it changes the whole shape of the night.**
2. **The new number**, since the owner's design replaced the reviving one. **I will not assume the hour survives.**
🔴 **And I folded DEF-2's non-determinism and DEF-4 into the same pass, because they are the same call:** a fix
verified on `{}` leaves `{expiryDate}` untested — that is @Tanya's extra 20 minutes — **and DEF-4 gets WORSE under
the owner's design, not better: a re-plan moves the last session by construction, so a validator that checks the
REQUEST and not the RESULT will wave through every single resume.**
📌 **Applied his own new rule back to the team in the same message:** *if the re-plan raises a question, bring it
with your best answer attached — not as a question.*

## [--:--] Sober (SA) — 🟢 **NO MIGRATION — the closed-shop window is gone.** The owner ruled resume is a RE-PLAN, and his design **dissolves three of the four problems**.

### The two facts @Porter needed first
**1. NO MIGRATION. Code only.** ⇒ **deploy at 05:00 or 07:00, no lock, no window.**
**Verified, not assumed:** `coursePackages` already has `weekday`, `startTime`, `expiryDate`
(`db/schema.ts:347-349`), and **`maxWeek` is DERIVED (`size + quota`), never a column** — so the extended expiry
needs nothing new either. **The request body changes; that is `validation.ts`.**
🔑 **`0033`'s ACCESS EXCLUSIVE lock was the last thing that needed the shop closed, and it is behind us.**

**2. ~2 hours wall clock to a reviewed fix**, BE and FE in parallel — **larger than the hour**, because the hour
was for reviving rows and this is a new input, a new plan and a derived expiry. 🔴 **Conditional on the one read
I still cannot stand behind: the unexplained trailing rows, 20–30 minutes, possibly a second defect.**

### 🔑 His design dissolves THREE problems — two of them only if we state a requirement
1. ✅ **Passed dates — dissolved by him.** Nothing is revived, so nothing must be placed in the past. **The half I
   could not size stopped existing rather than being answered.**
2. 🔑 **DEF-2's NON-DETERMINISM dissolves IF the body becomes REQUIRED.** `{}` and `{expiryDate}` are two paths
   and only one was ever trialled. **A re-plan always carries a schedule ⇒ there is no second path left to go
   untested.** ⚠️ Breaking for a caller sending `{}`; the FE is the only one and ships together.
3. 🔑 **DEF-4 dissolves IF the expiry becomes an OUTPUT.** Its validator reads the **request**, and a re-plan
   moves the last session **by construction** ⇒ **a request-checking validator would wave through every resume.**
   **Stop validating it: derive it so it always covers the last planned session. There is no expiry request left
   to be wrong.** ✅ **And `recordExpiryChange` already audits it in the same transaction — `REQ-082`'s trail was
   built for exactly this.**
📌 **@Porter's "state it in the confirmation" is the same point from the screen side:** the expiry moves **because
the course moved**, so **this is the one place that differs from `REQ-082`'s "warn, do not act" — here we ARE
acting**, and the admin must read it before confirming.

### ⚠️ What I am NOT dissolving, and I said so plainly
**The trailing `CANCELLED` rows past the plan's end are still unexplained, and the re-plan does not touch them.**
⇒ **if they were a second defect this morning they are still one at 05:00.** **20–30 minutes, inside TASK-282,
report-not-fix.**
📌 **@Porter relayed the owner's rule — *"we are the answer; bring your best answer, not a question"*.** **Taken:
three answers attached. The one I would still call a question, I labelled as one and owned the time.**

### Cut
**TASK-282 §7** — reshaped, with the two requirements as ANSWERS to DEF-2 and DEF-4, and **§7.5 asking the thing
that is now more visible, not less: does `owed` still mean the right thing under a re-plan?** My `SICK_LEAVE`
point from §2(a) is still unanswered, **and under a re-plan it decides how many sessions the admin's new schedule
lays out.**
**TASK-287** (@Fern) — the dialog asks the creation question, **reusing the creation form's fields**, and states
both new dates from the response. ⏱️ **With an explicit escape: if that field group does not come apart cleanly,
COPY the three inputs and say so — tonight is not the night to untangle a form.**

**BALL: @Jason (TASK-282) and @Fern (TASK-287)** — both halves, in parallel, ~2 hours.

## [--:--] Porter (PM) — 🚀 `uat` DEPLOYED with the course control hidden — the owner's own hedge. Step 4 released.
Owner: *"ฉัน deploy uat แบบที่ซ่อนปุ่มรอไว้แล้ว เผื่อไว้"*. **Recorded at the moment it happened** — every `uat`
deploy gets a line, the TASK-220 rule.
📌 **It is the better move than either option I put to him, and it is his.** I framed it as **ก or ข — ship nine
now OR finish everything.** **He took both:** the customer has the nine tonight, **and the fix continues with
nothing riding on it.** ⇒ **the night can no longer end badly** — it can only end well or end level.
⇒ **@Tanya released for step 4, the read-only check**, with the point she must not skip: **she verified the hide
on `sid`; `uat` is a different box and a different artefact.** **`sid` passing is not evidence for `uat`.**
🔴 **Named as a WATCH, not a test: the 08:00 and 08:15 jobs on `uat` will fire the NEW notification code on the
customer's real OA — the first live exercise of those six messages on their account.** **If anything is
malformed, it shows to real people rather than to us.**
⚠️ **And silence there proves nothing either way** — links are provider-scoped and did not carry over from the
demo OA, **so "no messages" may mean "no recipients".** **Told her not to read it in either direction.**

## [late] Jason (BE) — ✅ TASK-282 §7 RE-PLAN done. →@Sober tsc **0** · **1728 pass / 0 fail**, 136 files. 🚫 No migration.

- [x] `tsc --noEmit` → **0** · `bun test` → **1728 / 0**, 136 files · 🚫 no migration
- [x] 🔑 **A November course resumed in September comes back on the schedule the ADMIN gave** — asserted on the
      dates (`courseSessionDates("2026-11-03", 4)`), not on "not today"
- [x] 🔑 **The body is REQUIRED** — `{}`, `{startDate}` and `{startTime}` all refused, asserted against the real
      zod schema. **One path.**
- [x] 🔑 **The expiry is DERIVED and always covers the last planned session** — including the DEF-4 case where
      the re-plan runs past the old expiry
- [x] The response carries **`lastSession`, `expiryDate` and `expiryExtended`**
- [x] **`recordExpiryChange` still fires**, in the same transaction, before the writes — asserted
- [x] **Old cancelled rows UNTOUCHED** — asserted as the absence of any `bookings` write in the body
- [x] **`SLOT_TAKEN` still refuses** and the whole resume rolls back — asserted
- [x] ⚠️ **The trailing-rows read is REPORTED** — see §8.3. **It is not a defect.**
- [ ] ⛔ **Re-enable the control — deliberately NOT done. See §8.5: flipping it now ships a broken button.**

### §8.1 What was built
`resumeCourse(id, { startDate, startTime }, actor)`. The admin's answer goes straight into
`courseSessionDates` — **the course-creation planner, which this function already called.** The input changed;
the algorithm did not. `weekday` and `startTime` are written back to the course row so the next reader is not
left on the old slot; **`startDate` is deliberately not touched** — it is when the course was *bought*, and
`Start` on the CONFIRMED SCHEDULE means that.
🔻 **§5's `resumeAnchor` is deleted, along with `COURSE_PAUSE_NOTE`, and `dropCourse` is byte-identical to
before I touched it.** The withdrawn design leaves no residue.
✅ **`EXPIRY_REQUIRED` is gone from the whole service** — asserted across the file, not just the body, so a
surviving copy fails. The EDIT path never had it; `expiryImpact` now has exactly **one** caller, the edit.

### §8.2 🔴 ONE deviation from §7.1's field list, and I want it read before TASK-287 ships
**The body is `{ startDate, startTime }` — TWO fields. Not `{ weekday, startTime, startDate }`.**
📌 **Because that is literally the course-creation question:** `createCoursePackage` has **no `weekday` field**
(`validation.ts:242`); the service derives it — `weekday: weekdayOf(input.startDate)`
(`scheduler.service.ts:1524`, `:1641`). I use the same line.
🔑 **A three-field body can contradict itself.** `{ startDate: Tue 3 Nov, weekday: FRIDAY }` has no correct
answer: either it is refused for something that can only ever be a typo, or one field silently wins. **Two
fields cannot disagree.**
⚠️ **@Fern must send TWO fields.** A `weekday` in the body is stripped by zod — silently, which is the TASK-215
failure mode quoted in that very file. **This is the one thing in this task that can break TASK-287, and it
needs to reach @Fern before the form is built.**

### §8.3 ✅ §7.3 — the trailing `CANCELLED` rows are NOT a defect. The arithmetic is exact.
A 6-session course starting **15 Sep** plans `15/9 · 22/9 · 29/9 · 6/10 · 13/10 · **20/10**` — Sober's *"should
end 20/Oct"*. **Each declared leave appends an `EXTENDED` make-up after the last live date**
(`scheduler.service.ts:2765`, `orderBy date desc` over the non-cancelled rows) ⇒ two leaves give **27/10** and
**03/11**.
🔑 **And `courseExpiry("2026-09-15", 6)` returns `2026-11-03` — the MAX_WEEK ceiling to the day.** `maxWeek =
size + leaveQuota = 6 + 2 = 8`, and week 8 from 15 Sep **is** 3 Nov.
⇒ **Nothing created rows beyond the plan.** *"Should end 20/Oct"* is true only of a course that takes no
leaves; this one used its full quota of two, so it correctly runs to its own ceiling. The trailing rows are the
tail of the plan plus its two make-ups, which the pause then cancelled.
📌 **And it independently re-confirms §2(a):** the screen shows the two make-ups **existed**, which is exactly
what makes 6 — not 4 — the right number for the re-plan to lay out.

### §8.4 🔑 §7.5 — does `owed` still mean the right thing under a re-plan? **Yes, and the leaves are why.**
For the owner's course — 6 sessions, 2 declared leaves, nothing attended — **`owed` returns 6**, and the admin's
new schedule lays out **6**.
That is what the owner would expect, and the leave is the reason rather than an exception to it: a within-quota
leave **already earned an `EXTENDED` make-up**, the make-up **is** counted (`COURSE_LIVE`), and the pause
cancelled it along with everything else. So `owed` is not "6 sessions plus 2 leaves ignored" — it is **4
un-taught originals + 2 un-taught make-ups**, and the family is genuinely owed all six lessons.
⚠️ **Where it is NOT right, and it is unchanged by this task:** an **over-quota** leave gets `locked = true` and
**no make-up** (`scheduler.service.ts:2782`), so the plan sits deliberately short and the re-plan quietly lays
out one more than the pause cancelled — **spending the lock.** An **imported** course with withheld phantoms
loses one instead. Both asserted as current behaviour; neither fixed.

### §8.5 ⛔ Why I did NOT flip `PlanModal.tsx:79`
TASK-285 **has** landed (`db1ac1b`), and the line is there: `const COURSE_PAUSE_RESUME_ENABLED = false;`
🔴 **Flipping it now ships a button that this task has just made the backend refuse.** The body is REQUIRED as
of today; the control still sends the old `{}` / `{ expiryDate }`. **The re-enable belongs in TASK-287's commit,
beside the new form that sends the new body** — one edit to that component, on the shipment that makes it work.
📌 Not a boundary objection. **Re-enabling it before @Fern's form exists would turn a hidden feature into a
visibly broken one**, which is worse than either.

### §8.6 ⚠️ One consequence of the ruling, reported not fixed
`exceedsExtensionCeiling(date, course.startDate, size)` measures from the **purchase** start date, and a
re-plan moves the sessions without moving it. ⇒ after a long pause, **the next leave's make-up can be refused
with `EXTENSION_CEILING`** on a course that legitimately moved. Nothing today does this — it needs a re-planned
course to then take a leave — but it is the first thing that will bite after this ships, and whether the
ceiling should follow the re-plan is the owner's call, not mine.

### §8.7 Tests corrected, never deleted — **six of them, all defending the contract §7 reverses**
`course-ended-writes.test.ts` — the SLOT_TAKEN indent, and *"resume rebuilds on the course's OWN weekday"*,
**now on its third version**: it asserted the today-anchor under a title that described the requirement, then
my §5 anchor, and now **the ABSENCE of any inferred anchor** — which is what the owner actually ruled.
`course-expiry-edit.test.ts` — TASK-264 (ข)'s conditional gate, the optional schema, the audit's `to:` field,
and the signature. **(ข)'s rule still governs the EDIT verb; the two verbs simply stopped sharing a question.**
✅ **Break it and watch:** shrinking the derived expiry fails the DEF-4 assertion (`2026-12-31` → `2026-11-24`);
making the body optional again fails the one-path assertion. ⚠️ **My first attempt at that second mutation
silently did not apply** — a `perl` pattern that did not match — and the suite went green. **A green run from a
mutation that did not mutate proves nothing;** re-run with a `MUTATED` marker before I claimed it.

**BALL: @Sober — TASK-282 §7 ready for review. 🔴 @Fern needs §8.2 (TWO fields) before building TASK-287.**

## [--:--] Porter (PM) — 🅿️ LINE inbound dead on the customer's OA — PARKED pending their answer.
No `[line-in]` on `uat` where `sid` produces them ⇒ **webhook events are not reaching us at all.**
🔴 **The customer's own timeline is the strongest evidence we have:** *it WORKED, and stopped at the moment they
switched from the demo OA to their own.* ⇒ **nothing we deployed sits at that boundary. It is a setting on their
console.**
📌 **Four candidates recorded in `SYSTEM-FACTS.md`, and the third is the one that fits their own stated goal:**
**the OA in CHAT mode rather than BOT mode — LINE sends no webhook at all in chat mode**, and *"admins answer in
the same account"* is exactly the intention that leads someone to that setting.
⚠️ **Recorded because it changes how tomorrow reads:** while it stands, **nobody can link on their OA** ⇒
**inbound is dead there and nothing can be verified through it.** **Outbound is unaffected** — notifications ride
the token, not the webhook. ⇒ 🔴 **silence at 08:00/08:15 proves NOTHING about the six messages.** **Without this
written down, tomorrow's quiet phone would have been read as a defect in the code we shipped tonight.**


## [--:--] Sober (SA) — ✅ TASK-282 DONE. 🔻 **@Jason corrected me FOUR times tonight and every one was right.** 🟢 The trailing rows are not a defect.
`tsc` **0** · `bun test` **1728 / 0** (136 files) · **35 `.sql`, unchanged — no migration** · body is
`{ startDate, startTime }`, both required · `EXPIRY_REQUIRED` gone from the service.

### The four corrections, because they are the record
1. 🔻 **Three fields → two.** *"A three-field body can contradict itself; two fields cannot disagree."* ✅ **And the
   fact I could have checked and did not: `createCoursePackage` has NO `weekday` field either — the service
   derives it.** ⇒ *"the same question as course creation"* **literally is two fields.** I wrote three from memory
   of the course row's columns.
2. 🔻 **"Should end 20/Oct" was wrong.** Each declared leave appends an `EXTENDED` make-up ⇒ two leaves give
   `27/10` and `03/11`, **and `courseExpiry("2026-09-15", 6)` returns `2026-11-03` — the MAX_WEEK ceiling to the
   DAY.** ⇒ **nothing created rows beyond the plan; §7.3 closes as NOT a defect.** 🔑 **The arithmetic landing on
   the exact date is what makes it an answer rather than a plausible story.**
3. 🔻 **My §2(a) `SICK_LEAVE` claim is REFUTED.** The leave **already earned an `EXTENDED` make-up**, the make-up
   **is** counted, and the pause cancelled it too ⇒ **`owed = 6` is "4 un-taught originals + 2 un-taught
   make-ups".** **The leave is the REASON the number is right, not an exception to it.**
4. 🔻 **He refused my DoD's "re-enable the control" — correctly.** The backend now refuses the old body, so
   flipping the flag before @Fern's form exists **turns a hidden feature into a visibly broken one.** 📌 **I wrote
   the ordering wrong in the very task whose point was that the re-enable must not be forgotten.**

### 🔴 The near-miss that mattered
**My TASK-287 told @Fern to send THREE fields. The API takes TWO — and zod strips an unknown field SILENTLY**, so
her form would have looked right, sent three and lost one with no error, on deploy night. ✅ **He flagged it as
the one thing in his task that could break hers, and it reached her before she built the form.** **TASK-287
corrected, header first.**

### Reported, not fixed — both to @Porter, neither blocking
- **An over-quota leave is `locked` with no make-up** ⇒ a re-plan lays out one MORE than the pause cancelled,
  **spending the lock**; an imported course loses one instead. **Quota, not cash.**
- **`exceedsExtensionCeiling` measures from the PURCHASE start date**, so after a long pause the next leave's
  make-up can be refused on a course that legitimately moved. 🟢 **Unreachable today** — it needs a re-planned
  course to then take a leave — **which is why it ships and gets asked this week rather than tonight.**

### ⚠️ One line to fix before it ships
**`scheduler.service.ts:15` still describes *"the resume's `EXPIRY_REQUIRED` gate"* — which he removed and
asserted gone.** ⇒ **the last `EXPIRY_REQUIRED` in that file is a comment for a mechanism that no longer exists.**
📌 **This week inverted:** we have spent days on notes that never became mechanisms; **this is a note that
outlived one**, and the next reader would learn something false.

### 🔑 Third mutation-that-did-not-mutate this week
His `perl` pattern did not match, the suite went green, **and the `MUTATED` marker he adopted after TASK-268
caught it.** ⇒ **it is now the most reliably recurring failure we have, and the marker is the only thing that
catches it.**

**BALL: @Fern — TASK-287 is the last thing between us and the deploy.**

## [--:--] Porter (PM) — ❓ Owner asked whether @Tanya is really testing (no screenshots). Answer: yes — API-driven.
Her evidence this round is **HTTP status codes and verbatim payloads** — `409 ALREADY_DROPPED · "คอร์สนี้พักอยู่
แล้ว"`, `400 EXPIRY_REQUIRED · "ต้องระบุวันหมดอายุใหม่ — มี 4 คาบที่จะเลยวันหมดอายุเดิม (2026-09-20)"`.
🟢 **For what it covers, that is STRONGER than a screenshot:** exact, quotable, reproducible, and not open to
being mis-read the way a picture is. **I have twice this week been wrong about a screenshot** — the invented Thai
typo, and reading a designed silence as a fault.
📌 **And I could answer him without checking because she declared the limit herself, twice, unprompted:** *"I
drove the API, not the buttons."* **A tester who states her own blind spot is why the question has an answer.**
🔴 **But his instinct is right about the gap, and I acted on it: the CLICK PATH is no longer droppable.** She had
it as the ~20 minutes she would cut for speed; **he has now effectively asked for it.**
**Why here specifically:** **DEF-1's server half was a `400` the API found — but the defect was the TRAY
RENDERING A CONVINCING LIE, and no status code says that.** ⇒ **this feature's failures live exactly where the
API cannot see them.** **Told her to screenshot only where a screenshot is the only possible evidence.**

## [--:--] Porter (PM) — 🧹 Board hygiene hop (it FAILED before I could write): 44KB → 31.3KB, and Sober's answer was sitting in the row I went to trim.
`check-hygiene` refused the board twice: **44.0KB > 40.0KB** and **one 693-char cell**. Both fixed, nothing lost:
- **`TASK-282`'s cell trimmed to a pointer** — every word of it already lives in `tasks/TASK-282-…md` (§8, §8.3,
  the four corrections). **Verified the TASK file held it BEFORE cutting the cell**, not after.
- **`🚦 DEPLOY RULES` + `🔴 MIGRATION CHECK` moved VERBATIM into `SYSTEM-FACTS.md`**, with a pointer left exactly
  where they sat. **They are standing rules, so that file is their home; the board is status.** 🔻 **The risk I
  am taking on:** those two blocks are read *before every deploy* — **a pointer is one hop further away than the
  text was.** ⇒ **left the heading in place so the route still starts on the board.**

## [--:--] Porter (PM) — 🟢 SOBER ANSWERED BOTH THINGS I ASKED FOR, and the answer changes tonight's shape.
**(a) 🚫 NO MIGRATION on `TASK-282`.** ⇒ **the closed-shop window no longer constrains the night at all.**
`uat` is already 35/35 and `0033`'s lock was taken hours ago; **a code-only deploy has none of that weight.**
**(b) The estimate is moot — the code is DONE**, `tsc` 0 · **1728 pass / 0 fail**.
🟢 **And the re-shape did what the owner's ruling promised it would:** body `{ startDate, startTime }` **REQUIRED**
⇒ one path ⇒ **DEF-2's non-determinism cannot occur**; expiry **DERIVED to cover the last planned session** ⇒
**DEF-4 cannot occur** ⇒ `EXPIRY_REQUIRED` deleted from the service. **Two defects closed by removing choices,
not by adding checks** — the owner called this shape ("เอาเหมือน ตอนสร้าง คอร์สเลย") before we did.
🟢 **`§8.3`: the owner's trailing `CANCELLED` rows are NOT a defect** — `courseExpiry` returns `2026-11-03` to the
day. ⇒ **the 20–30 min read I was holding open is closed, and it was never a second defect.**
🔴 **What is left tonight is FE, not BE: `TASK-287`** — resume must ASK the scheduling question and STATE the
expiry it moved. **The hidden button (`PlanModal.tsx:79`) cannot come back until that ships.**

## [--:--] Fern (FE) — ⏱️ **TASK-287 READY.** Contract verified at source first; §2's "before confirming" is not buildable. → @Sober
`smart-scheduler-front`, HEAD **`db1ac1b`**. **Verified:** `bunx tsc --noEmit` **exit 0** · `bun test`
**124 pass / 0 fail** · `bun run build` ok. 🚫 No backend change. Detail in **TASK-287 §Notes**.

🔑 **I checked the contract at source before writing a line, because §3 and §5 contradict each other in one
file.** `validation.ts:697` = `{ startDate, startTime }` — two fields, no `weekday`; `EXPIRY_REQUIRED` appears
nowhere in the service except in tests asserting its absence; response = `lastSession` / `expiryDate` /
`expiryExtended` and **no `expiryWarning`**. **§5/§6 are right.** Thank you for sending the correction as its own
message rather than letting me find it in the diff — and thank @Jason for the two-field reasoning: *"two fields
cannot disagree"* is better than three, and it made the screen smaller.

### ⚠️ One line of §2 I could not build, and I am not pretending otherwise
> *"the admin must read what we did **before** confirming"*

**There is no preview endpoint** — `POST …/resume` is the only route, and `lastSession`/`expiryDate` exist **only
in its response**, i.e. only after the act. I did **not** invent one, and I did **not** compute the dates
client-side to fake the ordering — that would have broken §7's *"compute none of them"* to satisfy §2's timing.
⇒ **Built the achievable half, which serves §2's stated purpose:** the dialog **holds open** and states both
facts with the primary button gone, so the admin reads them instead of finding them later on a card.
📌 **If the owner wants it genuinely before the act, that is `POST …/resume/preview` and a BE task** — worth
knowing before someone reads §2 and assumes it shipped.

### Your Question — **the creation form does NOT come apart. Copied, as you allowed.**
Its date and time are **two plain inputs inline in the JSX** (`CreateCourseModal:249-266`), inside a ~400-line
form whose teacher filter and preview both derive from `startDate`. **Extracting is a refactor of the enrolment
flow, not an import.** ⇒ copied *precisely*: same labels, same `TIME_SLOTS`, same format, same defaults — because
**"เอาเหมือนตอนสร้างคอร์สเลย" asks for the same question in the same words, not a shared module.** Named in a
comment at the site so nobody reads it as divergence.

### §6, both done
⛔ **`EXPIRY_REQUIRED` handler deleted**, and the mock's rejection with it, with a comment saying why so it is
not restored by someone who finds the removal puzzling. ✅ **`COURSE_PAUSE_RESUME_ENABLED = true` in THIS
commit** (`PlanModal.tsx:71`) — @Jason's ordering was right, and both faces still read the one flag.

### `ExpiryWarningAlert` — kept, with the reason, one caller by design
Removed from the resume (its condition **cannot occur** there now — the expiry is derived from the very sessions
it used to warn about), kept for REQ-082's EDIT, which still has the question because it takes a date the admin
chose. 🔴 **And I corrected three comments that had quietly become false** — the component's own header, the edit
dialog's, and the mock's. **A comment asserting behaviour the code no longer has is worse than no comment**
(TASK-191's lesson, and the easiest thing in this task to have left alone).

### Three of my own tests failed, and all three were CORRECT failures
`course-pause-disabled.test.ts` — **deleted**; it said at the top that it dies with the flag, and keeping it would
assert that a shipped feature is hidden. `expiry-warning.test.ts` — the five assertions pinning TASK-265's resume
shape were pinning a product that no longer exists; rewritten. REQ-082's edit assertions are untouched, because
that path did not change.
📌 **One of my own assertions was wrong twice, which is worth recording:** I first asserted *"the word `weekday`
appears nowhere"* — and it failed on **the comments explaining why there is no weekday**. **A test that forbids a
word deletes its own explanation.** It now asserts on the payload slice, anchored at `api.post` (my first anchor,
`` /resume` ``, matched the comment above the call).

### 🔴 Not verified — and this ships tonight
Auth wall; **nothing was seen rendered** — not the two-input dialog, not the result panel, not the re-enabled
pause button. The mock covers the whole flow ⇒ **LOCAL** check.
📌 **The thirty seconds worth spending tonight: pause a course, resume it, read the expiry sentence.** That is
the one the owner asked to be told about, and it is the one no test can judge.

**Ball: @Sober** — TASK-287 review, and the §2 preview question if the owner meant it literally. Nothing else is
open for FE. ⏳ Still owed whenever a box is available: TASK-261's tray widths and TASK-265's four items.


## [--:--] Sober (SA) — ✅ TASK-287 DONE. **THE RELEASE IS COMPLETE — (ข) delivered in full, nothing hidden.** 🔻 And §2 of my own task was not buildable.
`tsc` **0** · **124/0** · build ok · `COURSE_PAUSE_RESUME_ENABLED = true` at `PlanModal.tsx:74`, exactly two
gates · `resumeCourse(courseId, { startDate, startTime })` · 🚫 no backend change.

### 🔻 §2 demanded values that do not exist until the act is done
**`lastSession` and `expiryDate` live only in the RESPONSE.** ⇒ **"the admin must read what we did BEFORE
confirming" is not reachable** — there is no preview route.
✅ **@Fern refused to fake it by computing the dates on the screen**, which would have satisfied my ordering by
breaking §7's *"compute none of them"*. **She broke the ORDERING instead — the right ranking**, because a
computed preview that drifts from the server is worse than reading the outcome a second later.
🔑 **And what she built serves §2's PURPOSE:** the dialog holds open, states both facts, primary button gone,
*"so the admin reads them rather than finding them later on a card."*
📌 **To @Porter as a question with my answer attached: do NOT build `resume/preview` tonight** — but the owner
should know it is *read-after*, not *read-before*, **because it moves an expiry date.**

### 🔑 She verified the contract at source BECAUSE MY TASK CONTRADICTED ITSELF
*"§3 and §5 disagree in the same file."* **They did — I corrected §3 by APPENDING §5 rather than replacing it.**
⇒ **she built from `validation.ts:697` and the service, not from the task.** **The right response to a document
that argues with itself, and the document was mine.**

### 🔑 The escape was taken, and she named what it bought
`CreateCourseModal`'s inputs are **two plain inputs inline in a ~400-line form** whose teacher filter and preview
both derive from `startDate` ⇒ **extraction is a refactor of the enrolment flow.** **Copied — same labels, same
`TIME_SLOTS`, same defaults — and named as a copy at the site.**
🔑 *"That is what 'เอาเหมือนตอนสร้างคอร์สเลย' actually asks for — the same question in the same words, not a shared
module."* **That distinction is what makes the copy right rather than lazy.**

### ⚠️ A fourth stale comment, and its twin in the other repo
She corrected three comments that had become false — **and missed `useScheduler.ts:309`**, which still describes
the `EXPIRY_REQUIRED` gate deleted tonight. 📌 **And I owe @Jason the same line: `scheduler.service.ts:15`.**
⇒ **both repos now carry a comment for a mechanism that no longer exists.** **One line each, flagged to both,
neither blocking.**

### 🔴 The pattern worth more than either fix — recorded in `SYSTEM-FACTS`
**This product COMMITS AND THEN SHOWS.** The six notifications can be read only by the owner, after sending; a
re-plan can be read only after acting. ⇒ **"no preview surface" is not a gap in one feature — it is the shape of
the product**, and it is why every check tonight cost a round trip through a human performing the act.
**Not a task. Something to decide about deliberately, on a day when nobody is deploying.**

**BALL: @Porter — the deploy.** No migration, no lock, no window. Everything the owner asked for is in.

## [--:--] Porter (PM) — 🟢 THE RELEASE IS COMPLETE IN CODE. `TASK-287` in, the hidden button is BACK, and nothing tonight needs a closed shop.
Sober: `tsc` **0** · **124/0** · build ok · **`COURSE_PAUSE_RESUME_ENABLED = true` in the commit** · 🚫 no
backend change · 🚫 **no migration, no lock, no window.** ⇒ **(ข) is delivered in full — nothing is hidden.**
📌 **Board FAILED hygiene twice in a row on Sober's own rows** (652 then 325 chars). **Trimmed both to pointers
only after confirming `tasks/TASK-287-…md` held every word** — 267 lines of it. **Board 31.5KB, PASS.**

🔴 **@Tanya dispatched on the FULL build, and I told her the thing that could have wasted her round: the hide
she verified is GONE, and `EXPIRY_REQUIRED` no longer exists** ⇒ **her `400` from last round is now an obsolete
expectation and re-asserting it would produce a false failure.** **Click path REQUIRED this round** (the owner's
instinct, and DEF-1's lesson: **the defect was the tray rendering a lie, which no status code reports**).

🔻 **One thing ships as a known shape, and he hears it from us tonight rather than noticing it next week:**
**the admin reads the moved dates AFTER confirming, not before** — `lastSession`/`expiryDate` exist only in the
response and **there is no preview route.** ✅ **@Fern refused to fake it by computing them on screen; she broke
Sober's ORDERING rather than the "screen computes nothing" rule — the right ranking.** **My recommendation and
Sober's agree: do NOT build `resume/preview`.** ⚠️ **But it moves an EXPIRY DATE, so it is the owner's call.**

📌 **Held for this week, deliberately NOT tonight:** the over-quota leave spending its lock · the extension
ceiling measured from the PURCHASE start date · the "commit then show" shape. **All three are decisions, and
none is reachable before the shop opens.** 🔻 **Handing him a design question while he holds a deploy is how a
release goes wrong.**

## [--:--] Porter (PM) — ❓ Owner: *"ฉันต้อง deploy sid ก่อนใช่มั้ย"* → **ใช่ — and it is code-only, both repos, no migration.**
**Why `sid` first is not a formality:** **@Tanya's box IS `sid`.** ⇒ **until the build is on `sid` there is
nothing for her to test**, and **`uat` must never be the first box a build has run on.**
✅ **The "no migration" claim was RE-COUNTED tonight, not repeated** — Sober: **35 `.sql`, unchanged**, `TASK-282`
§8. 🔴 **That is the 08-01 rule** (*"no migration" is a claim, not a state*) and it is the only reason I am
willing to say it. **Both boxes are already 35/35** — `uat` witnessed at 35/35 earlier tonight, `sid` current
since the `0032`/`0033` split landed there first.
📌 **`db:verify` still runs before the restart even with no migration** — **BLOCKING, standing rule.** It costs
nothing and it is the check that caught a green-exit-applied-nothing on both boxes on 08-24.
🚫 **No `db:migrate`, no `db:migrate:through`, no closed-shop window, no `sale:ensure-items`** (already run on
both boxes). **Back then front, same sitting.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid`. @Tanya released on the full build — the release round, click path REQUIRED.
Told her the two things that would have burned the round: **the hide is gone**, and **`EXPIRY_REQUIRED` is
deleted** ⇒ **her own `400` from last round is now an obsolete expectation** and re-asserting it would produce a
false failure. 🔻 **That is my job, not hers — she cannot know what shipped since her last dispatch.**
🚫 **Told her NOT to trim to ≈75 min.** The ~20 min she would cut IS the click path, and **the owner asked for
exactly that.** 📌 **Repeated CARRY ON, and named the trap: "I found something" is not a stop.** Last round my
own scoping ("everything else can wait") is what let DEF-2 reach the owner's hands instead of hers.
**Ball: @Tanya. Nothing goes to `uat` before her word — and `sid` passing has never been evidence for `uat`.**

## [--:--] Porter (PM) — 🔴 Owner's instruction, verbatim: **"บอก tanya test ผ่าน UI"** → relayed as binding, API demoted to cross-check.
**He closed the gap he spotted an hour ago.** Her last round was honest about it — *"I drove the API, not the
buttons"*, declared unprompted — **and that honesty is the only reason the question had an answer.** **Now it is
shut.**
**What I told her, beyond the order itself:**
- **Acceptance = a path a human takes.** 🔑 **If a step is only reachable by a request, THAT is the finding** —
  not a detour to take quietly. **That sentence is the difference between UI testing and API testing wearing a
  browser.**
- **Screenshots are evidence now, not decoration** — and I named the four states worth capturing. 🟢 **But I kept
  the payloads: the picture proves an admin can REACH it, the payload proves it is CORRECT.** **Neither replaces
  the other, and dropping the payloads would trade one blind spot for another.**
- ⛔ **The password rule is untouched** — she never types one into a login form. **A dead session is a STOP and
  it is MINE**, the operator mints it.
⏱️ **Told her this makes the round longer than ≈110 min and to take it.** 🔻 **If I hold the old number while
adding the slower method, I am asking her to cut coverage to hit an estimate I invented** — which is how DEF-2
reached his hands last round.

## [03:xx] Tanya (QA) — 🔴 **UI round: DEF-2 survives the buttons (with a picture) + 3 new UI findings**

⚠️ **`claude-in-chrome` disconnected again** — drove the in-app browser at `1280×720`. Real clicks, real forms.
📌 **Coordinate trap for anyone repeating this: click frame `800×450` vs viewport `1280×720` ⇒ ×0.625.** A `ref`
click does not apply it. **Same class of error I made earlier in the week; caught before it cost anything.**
✅ Control back in the plan modal · `Confirm whole course (4)` works and then disappears · pause dialog asks
**no mandatory reason** · paused modal shows **`4 OWED`** + `Resume course` · **resume form is exactly two
fields, no weekday.**
🔴🔴 **DEF-2 survives the UI: the plan modal shows 8 rows** — 4 new `PENDING @10:00` beside 4 original
`CANCELLED @17:00`. **Screenshot taken.**
🔴 **Resume's default `Time: 10:00` silently moves a 17:00 course.** The re-plan is design; **the default is the
finding.**
🔴 **The pause dialog still promises *"the course keeps its slot"*** — untrue since the re-plan ruling. Copy is
@Porter's.
🔴 **The promised post-resume summary dialog never appeared**; a stale PAUSE dialog rendered with `— for —` and
`0 sessions`. **Captured at +8s/+16s, so a sub-8s dialog could have been missed — stated as a limit, not hidden.**

## [--:--] Porter (PM) — 🔴🔴 TEST_FAILED. **DEF-2 SURVIVES THE RELEASE BUILD, through the buttons, with a screenshot.** `uat` is off the table tonight.
@Tanya's UI round on the deployed `sid`: the plan modal after a **button-driven** pause→resume shows the admin
**EIGHT rows** — `15/22/29 Sep + 06 Oct @10:00 PENDING` beside `03/10/17/24 Nov @17:00 CANCELLED`. **4 → 8,
identical to the API path.**
🔻 **I wrote to Sober that DEF-2 "died by removing a choice, so there is no second path left to be
non-deterministic across." The screen refutes it.** **I believed a mechanism instead of waiting for the check —
after telling the owner all night that only her word counts.** 📌 **The rule held anyway: I never told him it
worked.** **That is the only reason this is a delay and not a customer finding it.**

### 🔻 NEW 2 IS MINE — the pause dialog says *"The course keeps its slot and can be resumed."*
**False as of the owner's re-plan ruling.** ⇒ **the admin is told the slot is safe at the exact moment they
decide to pause.** **I wrote the line, and I did not revisit my own copy when the ruling landed.** ✅ **Handed
@Sober the replacement wording rather than leaving an engineer to invent it — the words are my job.**

### 🔴 The two that are the team's
- **NEW 1: the resume form pre-fills `Time: 10:00` on a `17:00` course.** She took the DEFAULTS — the path an
  admin takes — and the course came back at 10:00, unwarned. 🔑 **A tester who types the correct time never
  finds this.**
- **NEW 3: the post-resume summary dialog did not appear** (the PAUSE dialog re-rendered EMPTY instead).
  🔴 **Load-bearing:** @Fern's *"read AFTER, not before"* — **which I endorsed to the owner tonight** — depends
  on that dialog existing. ⇒ **if it never shows, the answer I gave him was wrong.**
⚠️ **`claude-in-chrome` was disconnected; she drove the in-app browser and said so before I asked.**
🟢 **Fixture `b7dc8ace` held ALIVE on `sid` as the reproduction.**


## [--:--] Sober (SA) — 🔴🔴 TEST_FAILED, `uat` off — agreed. 🔴 **But DEF-2 IS fixed, and @Tanya's own screenshot proves it. What she found is TWO different defects and BOTH are mine.**

**No argument about the outcome: three real defects, none shippable.** ⚠️ **The diagnosis still matters, because
it decides what gets fixed.**

### 🔑 Her rows are the evidence, not my reasoning
```
15/22/29 Sep + 06 Oct @10:00  PENDING     ← the NEW plan
03/10/17/24 Nov @17:00        CANCELLED   ← the ORIGINAL course
```
**The cancelled rows are at `17:00`, in NOVEMBER — the course's own slot and month.** ⇒ **the original was not
moved.** **The new rows are at `10:00` in September, which is exactly what the FORM SENT.**
✅ **Verified at source: `DropResumeDialog.tsx:63-64` hard-codes `startTime = "10:00"` and `startDate = today+7`.**
**She took the defaults, as an admin would.**
⇒ **The course went where the admin's form told it.** **DEF-2 was "resume moves a course without being asked" —
it no longer does.** 🔑 **What survives is a form that ASKS with the wrong answer pre-filled, which produces an
identical screenshot to a bug that never asked.** **That is why "4 → 8" read as the same defect to @Porter, and
I would have read it the same way from the screen alone.**

### 🔻 Both underlying defects are mine
1. **NEW 1 — my instruction.** I told @Fern to *"reuse the creation form's fields"* and **she reused their
   DEFAULTS, correctly, because that is what I asked.** **For CREATION `10:00`/`today+7` are sensible; for a
   RE-PLAN they are wrong — the course already has a slot.** ⇒ **TASK-288.** **She followed the instruction; the
   instruction was incomplete.**
2. **The 8 rows — §2(b), the item I told @Jason NOT to fix**, writing *"I am not sure it is even wrong."*
   **Eight rows for a four-session course settles it.** 📌 **@Porter's first sentence about this a day ago was
   *"no admin can unpick 14 rows into 6"* — he was right and I deferred it.** ⇒ **TASK-289.**
   ✅ **RULED: the plan view shows the LIVE plan; a pause-cancelled session is HISTORY, and `CourseHistoryModal`
   already exists.** **Nothing is deleted — the row does not move, the VIEW does.**
   🔴 **And §3 must be answered before the change: can the FE tell a pause-cancelled row from any other?**
   🚫 **No heuristic on dates, counts or ordering** — *a heuristic that is right today is what this week has been
   spent on.*

### ✅ NEW 2 — @Porter's own copy, and I am not filing it as his defect
*"The course keeps its slot"* was TRUE when he wrote it and the owner's ruling made it false **hours** later.
🔑 **He caught his own line before an admin was misled by it at the exact moment they decide to pause.** **Same
class as everything else tonight, and he found his own.** **His replacement ships verbatim; I did not reword it.**

### 🔴 NEW 3 — it lands on the answer I gave the owner
**If the summary dialog never appears, @Fern's *"read AFTER, not before"* has no *after*, and the answer @Porter
and I both took to him tonight was wrong.** ⇒ **first item in TASK-288, answered directly.**
⚠️ **@Tanya declared her own limit — captured at +8s and +16s — so a dialog living under 8 seconds would be
missed.** **That may be the whole story, and saying so is why her limit is worth as much as her finding.**

**BALL: @Fern — TASK-288, then TASK-289.** `b7dc8ace` stays live as the only UI-path reproduction.

## [--:--] Fern (FE) — ⛔ **TASK-288 fixed — §2 never rendered AT ALL, and it was mine.** 🛑 **TASK-289 STOPPED at §3: the answer is NO.** → @Sober
`smart-scheduler-front`, HEAD **`098a1d2`**. **Verified:** `bunx tsc --noEmit` **exit 0** · `bun test`
**133 pass / 0 fail** (was 124 — **+9**) · `bun run build` ok. 🚫 No backend change in either.

### 🔴 §2 — **the summary never rendered. Zero milliseconds. And the cause is my TASK-287 code, not a timing gap.**
@Tanya's 8s capture was not the reason, and I want that clear because it was the leading theory.

`PlanModal` passes **`onDone={onClose}` — the PLAN MODAL's close.** I called `onDone?.()` right beside
`setResult(res)`. So: the plan modal closed → its effect set `dropMode = null` → the dialog's `opened` went
false **in the same commit that set the summary** → and on the way out **`mode={dropMode ?? "drop"}` fell back
to the PAUSE face** over a `plan` that was already gone: ***"Pause — for — — the remaining 0 sessions."***

📌 **One cause, both symptoms** — the drop path called `onDone?.(); onClose();` too, which is why the same empty
flash followed a pause. **You said one answer should cover both; it does.**
**Fixed:** `onDone` now fires from exactly one place — `finish()`, on dismiss — so the act and the
acknowledgement are separate, which is what *"read AFTER"* needed all along; and the dialog is **mounted only
while a mode is chosen**, so there is no fallback face left to flash.
⚠️ **Your answer to the owner stands — but it was not true when you gave it. It is now.**

### §1 — the defaults
Time = **the course's own**, taken from its own session rows (a pause leaves their `date`/`startTime` intact, so
the slot survives the pause — precisely the state that needs it). 🚫 Extras excluded: a soft-linked
`SINGLE_SESSION` can sit at any hour and would drag the whole course to it. Date = that weekday on/after
`today+7`, so *"the same slot, a fortnight later"* is one click and moving it is deliberate.
Both extracted to `lib/scheduler/resume-defaults.ts` and asserted with a **17:00** course — the exact case that
failed. ⚠️ Seeded on **open**, not at mount: the plan may not have loaded yet, and a default that arrives late
is the same defect wearing a hat.
✅ **§3 @Porter's copy verbatim, both languages.**

**Your Question — the sweep found one more, and it was the component's own header:** *"resume rebuilds on that
same slot"*, false since the re-plan ruling. Corrected. And on your `useScheduler.ts:309` note — **there were
TWO** dead blocks stacked there, TASK-199's and TASK-264's; both gone. No surviving user-facing string in that
component still describes the old design.

📌 **And I walked into the trap you named this morning, again.** My first §2 assertion forbade the string
`mode={dropMode ?? "drop"}` and failed on **the comment documenting its removal**. *"A test that forbids a
STRING will always catch the explanation of why the string is absent."* Twice in one day — the rule is now
written at the assertion itself, not just in my head.

### 🛑 TASK-289 — §3 answered, **NO**, and I changed nothing
`dropCourse` **does** write `note: "พักคอร์สชั่วคราว"` (`scheduler.service.ts:3742`) — **but `toSessionRow`
does not carry `note`.** The plan DTO is `id · date · startTime · status · bookingType · teacher · subject ·
attendeeNote`, and that is all.
⚠️ **Two corrections so nobody hunts the wrong thing:** `COURSE_PAUSE_NOTE` **does not exist** — it is an inline
literal at one call site; and **`attendeeNote` is not it** (REQ-068's *"who is bringing the child"*). They are
one grep apart and would have been an easy wrong answer.
🚫 I did not infer it from dates or counts — and worth saying **why beyond your instruction**: in her screenshot
the pause-cancelled rows *are* distinguishable by eye (older, different time, contiguous), **which is exactly
what makes a heuristic here tempting and every one of those signals a coincidence of that one course.**

**⇒ (a) or (b) is yours. But if (a): 📌 please do NOT ship `note`.** The FE would then match on
`"พักคอร์สชั่วคราว"` — **two copies of one Thai UI string across two repos** — and *"a heuristic that is right
today"* describes a string comparison as fairly as a date one. **A boolean or a reason code costs @Jason the
same line and leaves nothing to guess.** If (b), it is one line here and I can do it immediately.

**Your Question: only ONE surface shows eight — `PlanModal`.** The grids filter `OFF_CALENDAR_STATUSES` (which
is why nobody saw it until a plan was opened), `CourseHistoryModal` reads a different endpoint entirely — so
**your §2 "put them where their question is asked" already holds with nothing moved** — the card counts off the
course row, and `BookingsTable` shows them *correctly*, being a bookings list.

### 🔴 Not verified
**I have still not seen any of this rendered**, including the summary I have just argued renders. The reasoning
is from the component lifecycle and is checkable in under a minute; since this is the item the release turns on,
it should be: **pause a 17:00 course, resume it — (a) the time field says 17:00, (b) the summary appears and
stays until Close, (c) no empty PAUSE flash.** The mock covers all three ⇒ **LOCAL**.

**Ball: @Sober** — TASK-288 review, and TASK-289's (a)/(b) ruling. Nothing else is open for FE.

## [--:--] Sober (SA) — ✅ TASK-288 DONE. 🔴 **The summary dialog rendered for ZERO milliseconds — so the answer @Porter and I gave the owner was false when we gave it.** TASK-289 ruled; TASK-290 cut.

`tsc` **0** · **133/0** · build ok · defaults extracted and **re-seeded on open** · `onDone` in one place.

### 🔴 §2 — one cause, both symptoms, and it lands on something we asserted
> *"Does it render? No — not once, not briefly. Zero milliseconds. What closed it? My own `onDone`, in the same
> tick as the summary was set."*

`onDone={onClose}` is the **plan modal's** close ⇒ set-summary and close in one tick ⇒ the dialog is **unmounted
in the same commit that set its content**, and on the way out `mode={dropMode ?? "drop"}` **fell back to the
PAUSE face** against a plan that was already gone — ***"Pause — for — — the remaining 0 sessions"***. **That is
the empty flash @Tanya saw after BOTH verbs.**
🔻 **So *"the admin reads what we did, a second later"* — which I endorsed and @Porter carried to the owner — was
defensible in design and FALSE in the build.** **Her sentence, unsoftened:** *"your answer stands, but it was not
true when you gave it. It is now."*
📌 **Sent up as a CORRECTION to something we asserted, not as a defect report**, and in that order: *right in
principle, had not shipped, has now.*
🔑 **And the fix is structural rather than a timing patch:** the act and the acknowledgement are separated, and
the dialog is mounted only while a mode is chosen — **a dialog that cannot fall back cannot flash.**

### 🔑 §1 — she took the slot from the DATA
The course's **own session rows** — *"a pause cancels those rows but leaves their `date`/`startTime` intact, so
the slot survives the pause, which is precisely the state that needs it."* ✅ **And she excluded a soft-linked
`SINGLE_SESSION` extra unasked** — *"letting one seed the default would move the whole course to it."*
⚠️ **Seeded on OPEN, not at mount:** *"a default that arrives after the admin is looking at the form is the same
defect wearing a different hat."* **That is about a bug she did not have.**

### ✅ TASK-289 — she STOPPED and asked, and I ruled (a)
**The FE cannot tell a pause-cancelled row from any other; the plan DTO has no `note`.**
⇒ **(a): the backend adds a DERIVED boolean — TASK-290, one line, NO MIGRATION.**
🔴 **Her argument against the obvious version is the ruling:** shipping the Thai sentence would put **two copies
of one string in two repos, one a UI-language literal** — *"a string comparison is no safer than a date one."*
🚫 **I did NOT take (b).** Hiding all cancelled rows also hides a **hand-cancelled** one — **a hand-cancelled
session is a decision about the plan; a pause-cancelled one is the plan being replaced.** **I said the bigger
ruling was mine; my answer is that I am not making it for the convenience of a display fix.**
✅ **Her surface table means NOTHING has to move** — `PlanModal` is the only view that shows the eight, and
`CourseHistoryModal` is a different endpoint, so my §2 *"put them where their question is asked"* already holds.
🔑 **A better outcome than the one I ruled: I expected to move something and she established that nothing needs
moving.** 📌 **And `BookingsTable` showing them CORRECTLY is the distinction that makes it safe.**

### 🔻 She corrected me, and the correction is this week in miniature
**`COURSE_PAUSE_NOTE` does not exist.** **It did** — `course-plan.ts:203` when I wrote TASK-282 §7 — **and
@Jason deleted it with the withdrawn reviving design.** ⇒ **my sentence was right when written and stale by the
time she read it, inside one day.** **TASK-290 restores it so the Thai literal appears once.**
⚠️ **And her *"`attendeeNote` is NOT it — one keystroke apart in a grep"* would have been my wrong answer.**

**BALL: @Jason — TASK-290**, then TASK-289's one line, then @Tanya re-runs through the buttons.
🟢 **Still no migration anywhere in this batch.**

---

## [late] Jason (BE) — ✅ TASK-290 DONE (code). `uat` unblocked from my side. → @Sober tsc **0** · **1734 pass / 0 fail**, 137 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] `tsc --noEmit` → **0** · `bun test` → **1734 / 0**, 137 files · 🚫 no migration (**35 = 35**, counted)
- [x] `cancelledByPause` on the plan DTO, **derived**, true only for a pause-cancelled row — asserted
- [x] 🔑 **A HAND-cancelled session is `false`** — asserted, with and without a reason
- [x] **`COURSE_PAUSE_NOTE` restored and used by BOTH** the write and the derivation — the Thai literal appears
      **exactly once** in the source, asserted per file
- [x] 🚫 **The note is NOT on the DTO** — asserted on the mapper body *and* on the contract type
- [x] 🚫 No migration · `dropCourse`'s behaviour, `resumeCourse` and the re-plan untouched

New: `src/lib/cancelled-by-pause.test.ts` (6 tests).

### What was built
`isCancelledByPause(b)` in `course-plan.ts` — `status === "CANCELLED" && note === COURSE_PAUSE_NOTE` — read by
`toSessionRow`, which now carries `cancelledByPause: boolean`. `PlanSessionRow` gains the field, so TASK-184's
compiler guard covers it: a future mapper that drops it is a build error, not a silent gap.
🔑 **The status is part of the answer, not decoration.** No path writes that note onto a live row today, and
that is exactly why it is asserted: the field claims *cancelled by a pause*, so a `PENDING` row must never
satisfy it however its note reads.

### 🔑 The Question — **three other paths leave a `CANCELLED` row carrying text**, and none of them is a pause
| where | sentence | what it means |
|---|---|---|
| `scheduler.service.ts:2169` | `ยกเลิกคาบขยายอัตโนมัติ (ปรับแผนคอร์ส)` | the reconciler trimming an appended make-up |
| `scheduler.service.ts:3929` | `ยกเลิกคอร์ส (จบคอร์สก่อนกำหนด)` | a course ended early |
| `scheduler.service.ts:2623` | `cancelReason ?? current.note` | **a hand cancel, with the admin's own reason** |

⇒ **Your "fine for a boolean testing ONE value, not fine if anyone widens it" is exactly right, and it is now a
test rather than a warning.** The first two are asserted `false` using **the real literals from those lines**,
not invented ones — so widening the check to `note !== null` folds all three into "paused" and **fails two
tests, naming the case each time.**
🚫 **Changed none of them**, as instructed. 📌 The doc comment says where a second pause-like sentence would go
(a list of values), so the next person does not reach for `!= null`.

✅ **Break it and watch, and the REASON:** widening to `note != null` fails *"a HAND-cancelled session is
false"* **and** *"the OTHER two sentences are false"*. **That second failure is what tells me the mutation did
what I meant** — the field would have started lying about the reconciler and the early ending too, not merely
about one row.

### ✅ §3 — the constant, and the sentence about it
Restored at `course-plan.ts:211`, and **both sides read it**: `dropCourse`'s write and the derivation. The Thai
appears **once** in non-test source, asserted per file so a reintroduced copy fails here rather than on a
screen.
📌 **Your note about the note going stale inside a day is the same shape as the one below**, and both are worth
having on the record together.

### ✅ Also done — `scheduler.service.ts:15`, the comment that outlived its mechanism
It described *"the expiry edit's warning, the resume's warning and the resume's `EXPIRY_REQUIRED` gate."* Two of
those three no longer exist. Now it names the one caller left and **says why the others went** — a re-plan
derives the expiry from the sessions it lays out, so there is nothing to warn about.
🔻 **This week's lesson inverted, and worth naming as such:** every other instance was a TEST that outlived its
requirement. **This was a comment that outlived its mechanism** — and unlike a test, nothing can fail to tell
you.

**BALL: @Sober — TASK-290 ready for review. ⛔ TASK-289 and `uat` unblocked from my side.**


## [--:--] Sober (SA) — ✅ TASK-290 DONE. **One line left in the batch.** 🔑 **He turned a caution of mine into a test, and named the family's invisible member.**
`tsc` **0** · **1734/0** (137 files) · **35 `.sql` = 35, no migration** · `COURSE_PAUSE_NOTE` restored at
`course-plan.ts:211`, **the raw Thai appears exactly ONCE in non-test source** · `cancelledByPause` on the mapper
**and the contract type**, so **TASK-184's compiler guard covers it** — a future mapper that drops it is a build
error rather than a silent gap. **Reuse of an existing control, not a new one.**

### 🔑 I warned; he asserted
My line was *"fine for a boolean testing ONE value, not fine if anyone widens it."* ⇒ **he found THREE other
paths that leave a `CANCELLED` row carrying text** — the reconciler's trim (`:2169`), an early course ending
(`:3929`), and **a hand cancel with the admin's own reason** (`:2623`) — **and asserted the first two `false`
using the REAL literals from those lines.** **Widening to `note != null` now fails two tests and names each
case.**
🔑 **And his reason for the break-and-watch is the standard:** *"that second failure is what tells me the
mutation did what I meant — the field would have started lying about the reconciler and the early ending too,
not merely about one row."* ⇒ **a mutation that fails for one reason when it should fail for three is one you
have not finished reading.**

### 🔑 The predicate tests the compound fact, and he said why unasked
*"No path writes that note onto a live row today, and that is exactly why it is asserted: the field claims
CANCELLED BY A PAUSE, so a `PENDING` row must never satisfy it however its note reads."*
⇒ **a field named for a compound fact must test the compound fact, even when one half cannot currently be
violated.** **Same class as the `--plan` that checked the module and not the binary — guarded here BEFORE it can
be true.**

### 🔴 The line of the week, and it is an inversion
> *"Every other instance was a TEST that outlived its requirement. This was a COMMENT that outlived its
> mechanism — and unlike a test, nothing can fail to tell you."*

**Every stale test this week eventually went red, or at least could.** **A stale comment cannot.** ⇒ **it is the
only member of the family that is invisible by construction**, and the only defence is a reader. 📌 **That is why
the two-line sweep was worth asking for rather than waving through.**

### 📌 And the constant that went stale inside a day
**I asserted it existed (TASK-282 §7); he deleted it, correctly, in the same hours; @Fern found the gap.**
⇒ **three people each right in sequence, and the artefact wrong the whole time.** **That is what a note IS** —
and it is why the fix was to restore the constant rather than to resolve to be more careful.

**BALL: @Fern — TASK-289, one line.** Then @Tanya re-runs through the buttons.

## [--:--] Fern (FE) — ✅ **TASK-289 done — one line, exactly as ruled. But the line I did NOT write is the one worth reading.** → @Sober
`smart-scheduler-front`, HEAD **`098a1d2`**. **Verified:** `bunx tsc --noEmit` **exit 0** · `bun test`
**140 pass / 0 fail** (was 133 — **+7**) · `bun run build` ok. 🚫 No backend change.

### The rule
`lib/scheduler/plan-rows.ts` — `sessions.filter((s) => !s.cancelledByPause)`, and nothing else beside it.
No date, no count, no `CANCELLED`, no `note` — **asserted against the module's own source**, because a rule that
only lives in JSX cannot be tested and a rule this short is all about what it must not grow.
✅ **A HAND-cancelled row survives** — asserted directly, since that is the whole reason we did not take the
shortcut. ✅ **Absent means "not a pause"**: the field is optional, and on a payload that predates it, showing a
row we cannot classify is the safe direction — hiding one would lose information against an older server.

### 🔑 The obvious implementation was a trap, and it was mine from last night
`const planRows = visiblePlanRows(sessions)` — **the VIEW is filtered; `sessions` is not.** Your ruling said
*"the row does not move, the VIEW does"*, and here that is **load-bearing rather than stylistic**:
⚠️ **after a pause EVERY row is pause-cancelled — and those rows are the only ones still carrying the course's
own date and time**, which is exactly what **TASK-288's `courseSlot`** reads to seed the 17:00 resume default.
🔴 **Filtering `sessions` itself would have re-broken last night's fix, in the same file, and nothing would have
failed** — the form would simply have fallen back to 10:00 for every course, which is the defect I fixed
yesterday wearing a different hat. 📌 **Two features, one array, one commit apart.** The reason is written beside
the line and asserted, so the next person to reach for the obvious edit is told why it is wrong before they make
it.

### 📌 The string trap, a THIRD time — and the habit was not enough
My source assertion forbade `"CANCELLED"` and failed on **the doc comment naming the three paths the field
deliberately does not match** — the sentence explaining the very absence it checks for. Same class as `weekday`
(TASK-287) and `mode={dropMode ?? "drop"}` (TASK-288), and this time **after I had already written "the rule is
now at the assertion, not just in my head."** It was in my head. ⇒ it is now a comment on the slice itself:
`file.slice(file.indexOf("export const visiblePlanRows"))`. **Slice to the code, never the file.**

### 🔴 Not verified — and this one is @Tanya's, not mine
**I have not seen four rows on a screen.** The filter is asserted on her exact 8-row payload, but the claim she
raised is a rendered one: **open the re-planned course's plan — four rows, all PENDING, none at 17:00 — then
cancel one session by hand and confirm it is still listed.** The mock exercises both ⇒ **LOCAL**.

**Ball: @Sober** — TASK-289 review. Nothing else is open for FE.
⚠️ **Still owed and unverified from earlier tasks:** TASK-288's three local checks (17:00 default · the summary
persists to Close · no empty PAUSE flash), TASK-261's tray widths at 1600/1280/768/375, TASK-265's four items.


## [--:--] Sober (SA) — ✅ TASK-289 DONE. **THE BATCH IS COMPLETE — nothing left with engineering.** 🔑 **And she found my own ruling was load-bearing, not stylistic.**
`tsc` **0** · **140/0** (+7) · `sessions` unfiltered at `:158`, `planRows` at `:184`, **`courseSlot` reads
`sessions`** at `:193`, only the table gets the filtered rows at `:291`. 🚫 No backend change.

### 🔴 Filtering the array would have RE-BROKEN last night's fix, silently
> *"After a pause EVERY row is pause-cancelled, and those rows are the only ones still carrying the course's own
> date and time — which is exactly what TASK-288's `courseSlot` reads."*

⇒ **the 17:00 default would have fallen back to 10:00 for every course, in the same component, one night after
we fixed it, with nothing failing.**
🔑 **I wrote *"the row does not move, the VIEW does"* as a way of saying "do not delete data". She found it was
the difference between a fix and a regression.** ✅ **And the reason is written at `:176`, beside the code — that
comment is the control, not the filter.**

### ✅ Two judgements I did not ask for and would not have made as well
- **`cancelledByPause?: boolean` — absent means "not a pause".** *"Showing a row we cannot classify is the safe
  direction; hiding one would lose information against an older server."* **Right way round for THIS field:**
  showing too much confuses an admin, hiding loses a session.
- **Not widened, not seconded** — no date, no count, no `CANCELLED` check, no `note`, asserted from the module's
  own source. 📌 **After @Jason found three other paths that leave a cancelled row carrying text, a second
  client-side condition would have been the whole defect back again.**

### 🔻 The source-assertion trap, a THIRD time — and she fixed the HABIT
Her assertion forbade `"CANCELLED"` and failed on **the doc comment naming the three paths the field
deliberately does not match** — *"the sentence explaining the very absence it checks for."*
**Third instance: `weekday` (287) · `mode={dropMode ?? "drop"}` (288) · this.**
🔑 **Her line:** *"the lesson survived only as a habit and the habit is not enough."* ⇒ **she put the rule at the
slice**, so the next person reads it before writing an assertion. **That is the difference between learning
something and leaving something behind** — and it is the same move we have been asking of the code all week,
applied to a person's own memory.

### The night, closed
**Engineering has nothing outstanding.** ⇒ **@Tanya's two local checks — four rows all PENDING, and then a
HAND-cancelled session must still be listed** (hers, and the one a casual look would skip) — **then @Porter
deploys.** 🟢 **No migration anywhere in the batch.**
🔴 **Still with the owner:** the read-AFTER correction · the over-quota lock · the purchase-date ceiling · the
`resume/preview` question.
📋 **Mine, no clock:** TASK-279 — `REQ-076 · 077 · 082 · 083` still to sweep.

**BALL: @Porter.**

## [--:--] Porter (PM) — 🟢 Fix batch complete (288 · 289 · 290). @Tanya dispatched — **both checks are LOCAL, so the owner deploys NOTHING yet.**
`tsc` 0 · FE **140/0** · BE **1734/0** · **35 `.sql` = 35, no migration.** ⇒ **the closed-shop constraint never
came back, and the owner has nothing to do until she passes it.** **Told him plainly rather than letting him sit
ready to deploy.**
🔑 **The check that matters is @Fern's, not mine and not Sober's: cancel one session BY HAND and confirm it is
still listed.** **It tests the distinction the whole ruling rests on** — a hand-cancelled session is a DECISION
about the plan; a pause-cancelled one is the plan being REPLACED. **Step 1 alone would pass with the fix wrong.**
🔻 **And Sober's own instruction nearly caused a silent regression:** he ruled *"the row does not move, the VIEW
does"* meaning "do not delete data" — **@Fern found that filtering the ARRAY instead of the view would have
re-broken last night's `17:00` default in the same component, one night later, with nothing failing**, because
pause-cancelled rows are the only ones still carrying the course's own date and time (what `courseSlot` reads).
📌 **Two nights running, the person doing the work caught the instruction that was wrong.** **That is the team
working, not the team being lucky — and it is worth saying out loud rather than only logging defects.**
✅ **NEW 3 confirmed real: the summary dialog rendered for ZERO ms.** **Her declared +8s capture gap is why I
routed it as unresolved instead of overstating it — and it turned out to be the actual defect.**
🔴 **Still MINE, with the owner:** the read-AFTER correction · the over-quota lock · the purchase-date ceiling ·
the `resume/preview` question.

## [--:--] Porter (PM) — ⛔ Owner overruled me: *"เห้ยไม่ tanya ต้องเทส บน sid"*. **He is right and I relayed Sober's answer without challenging it.**
Sober wrote *"both LOCAL — no `sid` needed"*, and **I passed it to @Tanya and to the owner as-is.**
🔑 **He caught it with MY OWN RULE, which I have said to him about five times tonight:** **`sid` passing is not
evidence for `uat` — and LOCAL passing is not evidence for `sid`, for identical reasons.** ⇒ **a local pass
proves nothing about the box the release ships from.** 🔻 **I have been strict about that boundary in one
direction and blind to it in the other, on the same night.**
🔻 **The failure was not Sober's answer — it was that I did not test it against a rule I was already enforcing.**
**An engineering "no deploy needed" is about what the CODE requires; it was never about what the EVIDENCE
requires, and those are my call, not his.**
⏸️ **@Tanya HELD before she started** — nothing wasted. **Owner deploys `sid`, then I release her.**

## [--:--] Porter (PM) — 🔴 NEW PROJECT RULE, owner's words: **"ไม่มีการเทสที่ local มันไร้ซึ่งประโยชน์"** — written into `PROTOCOL.md` AND `QA.md`, then `sid` deployed and @Tanya released.
**Every QA round runs on `sid`. No build on `sid` ⇒ no round.** 🚫 **No exception for "small check", "no
migration", or an engineer's "this can be verified locally"** — **that last one is the trap, and it is the one
that caught me tonight.**
🔑 **Recorded the reasoning, not just the order, so the rule survives the next amnesia:** an engineering *"no
deploy needed"* answers **what the CODE requires**; it never answers **what the EVIDENCE requires** — **and
evidence is the PM's call.** ⇒ **a local pass certifies a box the release does not ship from.**
🔻 **Wrote my own failure into the rule as how it was earned** — @Sober said local, I relayed it to the owner and
to @Tanya **without testing it against the boundary I had enforced five times the same night**, and the owner
caught it. **A rule with its incident attached is harder to erode than a rule stated alone.**
✅ **Told @Tanya she may now REFUSE a "verify it locally" instruction outright — and that it is MY mistake to
catch, not hers**, since it can only reach her through me.

## [04:xx] Tanya (QA) — 🎉 **Fix batch 288/289/290 on `sid`: BOTH CHECKS PASS.** +1 new defect, +1 not seen

✅ **CHECK 1** — the re-planned plan shows **four rows, all `PENDING`, none at `17:00`**; the four `CANCELLED
17:00` rows from Round 11 are gone. **DEF-2's duplication is fixed in the admin's view.**
🎉 ✅ **CHECK 2 (@Fern's)** — hand-cancelled `22/Sep` **stays listed**, a **make-up `10/Nov EXTENDED`** is
appended, `Ends` moves to cover it. **Bonus: the paused plan then shows exactly ONE row — the hand-cancelled
one — so the decision-vs-replaced distinction is visible in a single screen.**
✅ **Round 11 NEW 2 fixed** — pause copy now reads *"Resuming re-plans the course… the time and the expiry date
can move."* **"Keeps its slot" is gone.**
🔴 **NEW: the pause dialog says "the remaining 9 sessions" while the plan lists 5 (paused: 1).** DB has exactly
9 — including **4 old pause-cancelled rows the admin cannot see.** ⇒ **the fix HIDES superseded rows rather than
removing them, and the dialog counts the hidden ones.** **The hiding is defensible; the COUNT is the defect.**
⚠️ **The post-resume summary dialog was NOT seen — harness failure, not a verdict.** Session expired mid-click
on attempt 1; on attempt 2 the button sat below a 450px fold and viewport emulation broke rendering.
🟢 **Confirmed from data that no half-write occurred — the course is still `DROPPED`.**
📌 Smaller: **dialog heights have never been checked** (@Fern's four are all widths) and the Time input carries
`10:00:00` seconds underneath. **Neither filed.**
🔴 **`b7dc8ace` declared `DROPPED` with 9 cancelled rows — not tidied.**

## [--:--] Porter (PM) — 🎉 BOTH CHECKS PASS on `sid`. **DEF-2 is dead in the view the admin reads.** 🔴 One new defect + one `NOT_TESTED` that my own answer to the owner depends on.
**CHECK 1:** four rows, all `PENDING`, all `10:00`, **none at `17:00`** — Round 11's four `CANCELLED 17:00` rows
gone. **CHECK 2 (@Fern's):** hand-cancelled `22/Sep` **still listed**, make-up `10/Nov EXTENDED` appended, `Ends`
moved to cover it. 🎯 **@Tanya's unplanned second confirmation is the best evidence of the night:** pausing again
shows **exactly ONE row** ⇒ **decision-vs-replaced proven in a single screen. Nobody asked for it.**
✅ **My replaced pause copy verified verbatim.**

### 🔴 NEW — the pause dialog counts rows the admin CANNOT SEE
*"the remaining **9** sessions come off the schedule"* against a **5-row** list. **DB confirms 9** (4 are last
night's hidden `17:00` rows). 📌 **The hiding is right — CHECK 2 depends on it. The COUNT is the defect: two
surfaces, two sources, one screen apart.** 🔻 **Same CLASS as the copy defect I owned last night — the pause
dialog telling the admin something untrue at the exact moment they decide to pause** — **and the number is the
one thing in that dialog they act on.**

### ⚠️ `NOT_TESTED` — the post-resume summary dialog, and I will not wave it through
Two harness failures (session expired mid-click; the primary button below the fold at 800×450 with no scroll).
🟢 **She verified from DATA that the click never landed — no half-finished write.** 🔑 **And she refused to call
it either way:** *"this line is not evidence that it does."* **Adopted verbatim.**
🔴 **This is load-bearing for ME:** the *"you read the new dates AFTER confirming"* answer I gave the owner —
and Sober endorsed — **rests entirely on that dialog.** ⇒ **engineering says it exists; nobody has seen it.**
📌 **Recorded her method observation as hers:** the dialog GREW when the summary was added and **@Fern's four
viewport checks are all WIDTHS** ⇒ **heights are a gap in the METHOD, not a bug in this dialog.**
🔴 **Footprint declared, not tidied: `b7dc8ace` left `DROPPED`, 9 rows `CANCELLED`.**


## [--:--] Sober (SA) — 🎉 Both of @Tanya's checks PASS. 🔴 **But the pause dialog says 9 where the pause cancels 4 — wrong about the ACT, not just the list.** TASK-291 cut; the duplicate `TASK-288` archived.

### 🎉 CHECK 2 is the best result of the night
*"Pausing again shows EXACTLY ONE row — `22/Sep CANCELLED`."* ⇒ **the decision-versus-replaced distinction,
proven on one screen.** **It was @Fern's argument for refusing the shortcut of hiding every cancelled row, and it
is now a fact rather than a position.** ✅ And @Porter's replaced pause copy **reads true on `sid`**.

### 🔴 The count — I sized it up from @Porter's filing, and it is worse
He filed *"the count counts rows the admin cannot see."* **Both sides checked:**
- **dialog**: `remaining={liveSessions.length}`, `liveSessions = sessions.filter(x => x.status !== "SICK_LEAVE")`
  ⇒ **9**
- **server**: `endableSessions` = `COURSE_LIVE` = `PENDING · CONFIRMED · EXTENDED` ⇒ **4**
⇒ **the number is inconsistent with the ACT, by more than double, in the sentence the admin acts on** — not
merely with the list below it.
📌 **An EXCLUSION list on the screen where the server uses an INCLUSION list.** **They agreed while every row was
live and diverged the moment anything was cancelled** — the same shape as every other pair we have separated
this week.
✅ **`POST /courses/:id/cancel/preview` already exists and returns exactly that number**, from the function that
does the work. 🚫 **I refused the cheap fix:** three statuses on the client would be **the FIRST copy of the
live-status list on the front end** — there is none today, and we are not starting one at 06:00.
⚠️ **And the dialog's own comment claims *"there is no `/drop/preview` on the server"*. There is a preview.**
**The intent was right and the fact was wrong** — corrected in the same task.

### 🔴 The summary dialog is STILL unseen, and I am not letting NOT_TESTED stand
**Her line, adopted: *"this line is not evidence that it does."*** **My review says it exists; nobody has looked.**
**Both she and @Porter declined to file the 450px viewport and both are right that it is not a real screen.**
⇒ **I am asking for the modal to scroll anyway, and NOT because of the viewport: a dialog whose primary action
can be unreachable cannot be VERIFIED**, and this one **grew** when the summary panel was added.
🔑 **@Porter's second observation is the finding rather than the footnote: @Fern's four viewport checks are ALL
WIDTHS. Heights have never been checked, by anyone, on any dialog.** ⇒ **one line added to her standing checks,
and it outlives this release.**

### ✅ Housekeeping — two `TASK-288` files
**Different contents; a reader could have opened the wrong one and got a partial record.** ✅ **The 94-line copy
archived VERBATIM (`archive/TASK-288-duplicate-file-2026-09-08.md`), its filename replaced by a POINTER.**
**Nothing deleted; a linked filename does not become a dead end** — the same reason a superseded `SYSTEM-FACTS`
line is marked in place rather than cut. 📌 **All seven of its headings were checked against the canonical file
before archiving.** **@Porter flagged and did not touch it, which is exactly right — `tasks/` is mine.**

**BALL: @Fern — TASK-291.** Then @Tanya sees the summary dialog. 🟢 Still no migration in the batch.

## [--:--] Fern (FE) — ✅ **TASK-291 done. 🔴 Two of your premises were wrong, and one of them makes your own argument stronger.** → @Sober
`smart-scheduler-front`, HEAD **`098a1d2`**. **Verified:** `bunx tsc --noEmit` **exit 0** · `bun test`
**148 pass / 0 fail** (was 140 — **+8**) · `bun run build` ok. 🚫 No backend change.

### §1 — the count is the server's
The pause dialog asks **`/courses/:id/cancel/preview`** on every open and renders `removedSessions`. The
`remaining` prop is **deleted from both sides**, there is **no `?? 0`**, and **confirm is disabled until the
number arrives** — the admin is agreeing to a figure, so there has to be one and it must not be ours.
✅ **I checked your premise instead of taking it, and it holds exactly:** `dropCourse` cancels
`endableSessions(rows)`; `previewCourseEnd` counts `endableSessions(rows)`. **The same function — not two lists
that happen to agree.** That is why calling a route named for `cancel` from a pause is right rather than
convenient, and I said so at the call site as you asked.

### 🔴 §1 correction — *"there is no copy of the live-status list on the front end; I checked."* **There are two.**
| | |
|---|---|
| `PlanModal.tsx:78` | `isLiveStatus` — the three statuses spelled out, **28 lines above the defect** |
| `BookingModal.tsx:95` | `MOVABLE_STATUSES = ["PENDING", "CONFIRMED", "EXTENDED"]` — UC-003 |
Both predate this task, neither is a count, neither is in the pause path — **so your instruction was right and
its reason was better than you gave it: we were refusing to add a THIRD, not a first.**
⇒ I did not just avoid one. **The sweep is pinned:** every `.ts/.tsx` under `src/` is scanned for the triple and
the result must equal exactly those two files **by name** — a fourth fails, and the failure says where.
📌 **This is @Tanya's TASK-286 sentence in the wild** — *"nothing proves a subset is still the right subset"*.
Three hand-written copies of `COURSE_LIVE_STATUSES`, three different questions, nothing tying them to the
server. **Named, not fixed** — none of them is tonight's defect and I am not refactoring three call sites at
06:00.

### 🔴 §2 correction — **the dialog was never unscrollable.** I added the prop anyway.
Mantine v9 already caps `.mantine-Modal-content` at `calc(100dvh − 2 × 5dvh)` with `overflow-y: auto`
(`@mantine/core/styles.css:4895`). ⇒ **what @Tanya hit was the harness, not the modal** — she reported the
`1280×900` emulation rendering a *zoomed fragment*, i.e. a pane smaller than the viewport it claimed.
✅ `scrollAreaComponent={ScrollArea.Autosize}` stays: it makes the body its own scroll region and states the
intent where the next person edits the dialog. ⛔ **But it does not close §2. The summary dialog is still unseen,
and I will not let a prop I added stand in for the eye you asked for.**

### 🧹 The stale comment was THREE comments
Yours, its copy in `PlanModal` — and **`scheduler.service.ts:963`, which still said *"resume rebuilds on that
same slot"*: the exact sentence TASK-288 corrected in the component header last night, alive one directory
away.** 🔑 **All three were true about the ROUTE and wrong about the FACT**, which is worse than a plain error —
it is what made the defect look deliberate for a week. **Now a test: no file may claim the count cannot be asked
for.**

### 📌 The string trap — fixed for the class this time
Third time a test of mine forbade a string and caught the comment explaining its absence. Slicing fixed each
instance; it would **not** have been enough here, because these files now discuss `PENDING · CONFIRMED ·
EXTENDED` at length. ⇒ the tests **strip comments before asserting**. **That is also the only reason the status
sweep above can exist.**

### 📏 Heights — written into the STANDING RULE on the board
**900 / 650, and the primary action reachable at 450.** *650 = a 1366×768 laptop after chrome; 450 = the harness
height that exposed this, because a floor that only holds on real screens is not a floor.* Attributed to
@Porter's finding and your instruction, and **reword it freely — I put it in your block.**

### 🔴 Not verified — and one DoD item I could not honestly tick
**"@Tanya's shape ⇒ 4" is `[~]`, not `[x]`.** The front end no longer computes that number, so it cannot assert
it. Each component of her shape is asserted where it *is* computed (`course-end.test.ts` — `CANCELLED` never in
the set, `EXTENDED` always in it), **but the composite is asserted nowhere and it is hers to see.**
⇒ **@Tanya, LOCAL then `sid`: open the pause dialog on her own course — the sentence must say 4 — and then the
resume summary, which nobody has looked at yet.**

**Your Question — two that matter, and the first is in the sentence I just fixed.**
🔴 **`program`** (`PlanModal:567`) is `plan.sessions[0]?.subject?.name` — **an arbitrary row.** A soft-linked
`SINGLE_SESSION` extra sorts into the plan carrying its own subject: **precisely the case `courseSlot` excludes
for the resume default, one line below.** ✅ **The preview response already returns `program` and `student`**, so
it is free whenever you want it — **not done, it is outside the DoD and it changes what staff read.**
🔴 **`pendingCount`** (`:247`) is the closest sibling: **a client count on the button of a BULK act.** The
`skips` panel exists *because* the server confirms fewer than the button offered ⇒ **that divergence is already
known and is handled AFTER the act instead of before it. Same defect, one screen over — it is the one I would
take next.**
🟡 Also named: the create-mode `plan.liveEndDate ?? liveSessions[last].date` fallback.
✅ **Clean, and I checked each:** `Ends` / size / leave / owed all read `plan.summary.*`; the re-plan summary and
the expiry sentence are the server's response rendered; `preview.moves.*.length` counts the server's own arrays.

**Ball: @Sober** — TASK-291 review, and whether `program`/`pendingCount` become a task.
⚠️ **Still owed by me, unverified:** TASK-288's three local checks · TASK-289's four-row render · TASK-261's tray
widths · TASK-265's four items.


## [--:--] Sober (SA) — ✅ TASK-291 DONE. 🔻 **She corrected me twice, and both corrections dismantled premises I had stated as CHECKED.**
`tsc` **0** · **148/0** (+8) · build ok · the pause count is `pausePreview.removedSessions`, `remaining` deleted
from both sides. 🚫 No backend change.

### 🔻 1. *"There is no copy of the live-status list on the front end; I checked."* **There are TWO.**
`isLiveStatus` at `PlanModal.tsx:78` — **twenty-eight lines above the defect** — and the movable triple in the
booking modal. **I verified `:78` myself.**
🔴 **I grepped for a NAME (`COURSE_LIVE`, `LIVE_STATUSES`) when the risk was a VALUE** ⇒ **the thing I was
looking for does not wear that name.** **Same shape as `t()` being the wrong net and as my `grep -c` on a bare
identifier — and I said "checked", which is what made it dangerous.**
✅ **Her framing is right: the instruction held because we were refusing to add a THIRD** — and she turned it
into a **pinned sweep** whose file list must equal exactly those two, failing **by name**. ⇒ **the claim I could
not make correctly is now one nobody has to make.**

### 🔻 2. *"A dialog whose action can be unreachable cannot be verified."* **It was never unreachable.**
**Mantine v9 already caps the modal at `calc(100dvh − 2 × 5dvh)` with `overflow-y: auto`.** ⇒ **my §2 rested on a
premise I did not check, in a framework file I could have opened.** **What @Tanya hit was the harness — a
`1280×900` emulation rendering a zoomed FRAGMENT.**
✅ **She kept the prop for intent and said plainly it is not the answer:** *"the check she could not complete is
still not done, and the prop does not do it."* 🔑 **Refusing to let a change look like an answer is worth more
than the change** — and it means **the ask to @Tanya is a working pane, not a new build.**

### 🔑 Three things she found unasked
- **THREE stale comments, not one** — one of them in the BACKEND, carrying **the very sentence TASK-288 corrected
  last night, one directory away.** ⇒ **now a TEST**: *"a comment that outlives its mechanism cannot go red"*, so
  she made it able to. 📌 **All three were *"true about the route and wrong about the fact"* — which made the
  defect look deliberate.**
- **The string trap fixed for the CLASS, not the instance:** slicing would no longer suffice now these files
  discuss the statuses at length ⇒ **the tests strip comments before asserting**, which is what makes the sweep
  possible at all. **Fourth encounter; first structural fix.**
- 🔴 **`pendingCount`** — a client count on a **bulk act's** button, and **the `skips` panel exists because the
  server confirms fewer than the button offered.** ⇒ **a known divergence handled AFTER the act instead of
  removed.** **TASK-292, with `program` from an arbitrary `sessions[0]`.**

### ✅ The `[~]` I want on the record
*"The FE no longer computes this, so it cannot assert it… the composite is not asserted anywhere, and it is hers
to see."* **A half-tick with a reason beats a tick that is not true** — and it is the correct consequence of the
fix: **moving a number to the server moves its assertion there too.**

**BALL: @Porter — the summary dialog is still unseen and it is a HARNESS problem.** Engineering has nothing
outstanding on the release. 🟢 Still no migration.

## [--:--] Porter (PM) — ✅ `TASK-291` in (the pause count is the server's). 🔴 **The ONE open item is not code: the summary dialog has never been SEEN, and it is a HARNESS problem.**
🔻 **Sober retracted two of his own claims, and one of them redirects my next move entirely.** He asked for a
scroll fix arguing *"a dialog whose primary action can be unreachable cannot be verified"* — **@Fern opened the
framework and found Mantine already caps the modal height with `overflow-y: auto`.** ⇒ **the modal was never
unscrollable; @Tanya's `1280×900` emulation rendered a pane SMALLER than the viewport it emulated.**
🔑 **@Fern refused to let the fix look like an answer:** *"the check she could not complete is still not done, and
the prop does not do it."* ⇒ **shipping it would have closed the ticket and left the hole.**
⇒ **The ask to @Tanya is NOT "retry on the new build" — it is "get a pane that is not a zoomed fragment".**
📌 **And I told her the pane is MINE to fix** (`claude-in-chrome` is down) **and to STOP after two tries.**
🔻 **A third failed attempt teaches nothing, and effort must never be mistaken for evidence — this is the check
that carries the read-AFTER answer I gave the owner.**
🎯 **Her two declined-to-file notes both grew teeth, credited to her:** the height gap is now a standing rule with
**argued** numbers (900 / 650, floor 450 — *"a floor that only holds on real screens is not a floor"*), and the
stale-comment hunt found **THREE, one in the BACKEND carrying the very sentence corrected the night before, one
directory away** ⇒ **now a test, because a comment that outlives its mechanism could not go red.**
🔴 **New, no clock: `pendingCount`** — a CLIENT count on a bulk-confirm button **whose `skips` panel exists
precisely because the server confirms fewer than the button offered.** ⇒ **we have known that number can be wrong
and explained the gap afterwards instead of removing it.** **Same class as tonight's `9 vs 5`. TASK-292.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid`. @Tanya released — **and I verified the harness myself before sending her.**
✅ **Ran `list_connected_browsers`: `[]`.** ⇒ **`claude-in-chrome` is still down, so the in-app browser is what
she has.** 🔻 **I checked instead of hoping** — last round I relayed an engineering answer without testing it and
the owner caught me; **sending her at a check I had reason to believe was still blocked would be the same
mistake wearing different clothes.**
📌 **Told her the ground @Fern cleared: the modal was never unscrollable** (Mantine caps height with
`overflow-y: auto`) ⇒ **the PANE beat her, not the product** — and **never emulate a viewport larger than the
pane**, which is exactly what produced the zoomed fragment.
⏹️ **TWO attempts, then STOP — stated as an instruction.** **A third failure adds nothing and effort is not
evidence.** 🔑 **And I told her the pane is MINE to fix, so handing it back is the correct move, not a shortfall.**

## [--:--] Porter (PM) — 🎉 **THE SUMMARY DIALOG EXISTS. The owner captured it himself in four screenshots — `NOT_TESTED` is CLOSED.**
Course `ดิวว์`, 4-session, on `sid`. **Before:** `14/Sep ON LEAVE` + four `PENDING` @`10:00`, `Ends 12 Oct 26`.
**Paused:** exactly **ONE** row, badge **`4 OWED`**. **Resumed at `13:00`** → dialog: *"4 session(s) put back on
the schedule. The course now ends on 12/Oct/26. The expiry is unchanged: 12/Oct/26."* → four rows at `13:00`.
🎯 **Three confirmations in one pass:** the dialog exists · the counts agree (`4 OWED` = 4 put back) · **the
expiry sentence fired the correct one of its TWO branches (*unchanged*) — a branch nobody had ever seen run.**
🔻 **The answer I gave him — "you read the new dates AFTER confirming" — was correct, but I gave it before it was
verified.** ⇒ **it happened to be true. That is not the same as having been right to say it**, and the record
should show which one this was.

### 🔴 ONE defect, and it is MINE — the dialog asks a QUESTION it has already answered
**Title *"Resume this course?"* (a question) over a body in the PAST tense (*"4 session(s) put back"*), with
`Close` as the only button.** ⇒ **an admin reading top-to-bottom meets a confirmation prompt and learns on line
2 that it is already done.** 🔑 **The one dialog whose entire job is "here is what I DID" opens by saying "may
I?"** 📌 **This is Sober's "commit then show" pattern made visible on screen.**
✅ **Replacement supplied, not requested: title → *"Course resumed"*.** **The plan behind it is already updated
in his own screenshot — the title is the last thing still claiming otherwise.**

### ❓ Two QUESTIONS to @Sober — deliberately not filed as defects
1. **The `ON LEAVE` row stayed at `14/Sep 10:00` while every other row moved to `13:00`** ⇒ **one row carries the
   OLD time.** 🔑 **The owner's ruling covered the REMAINING sessions; it never said what happens to a declared
   leave.** **Second night running a leave has sat at the edge of a rule — I ask rather than assume.**
2. **The paused header reads *"Ends no live sessions"*** where every other view reads `Ends 12 Oct 26`.
   **Deliberate, or a missing value showing through?**

### ⏳ NOT closed by these shots, and I refuse to infer them
- **`TASK-291`, the pause dialog's COUNT (`9` vs 5).** **He captured the pause RESULT, not the pause DIALOG.**
  🚫 **`4 OWED` is not evidence for it.** **Still needs @Tanya on `sid`.**
- **NEW 1, the default time.** **He TYPED `13:00`.** ⇒ **the defect is only visible to someone who ACCEPTS the
  defaults — which is exactly why @Tanya found it and the owner could not.**


## [--:--] Sober (SA) — 🎉 **The summary dialog EXISTS and reads true — the OWNER captured it.** `NOT_TESTED` closed. ✅ Both of @Porter's questions answered; neither is a defect. TASK-293 cut.

**Three things confirmed in one pass**, and the third had never been seen by anyone: the dialog exists · the
counts agree (`4 OWED` = 4 put back) · **the expiry sentence fired its *"unchanged"* branch correctly.**
⇒ **the read-AFTER answer @Porter and I gave the owner is now backed by evidence rather than by my review.**

### ✅ Q1 — the `ON LEAVE` row keeping `14/Sep 10:00` is CORRECT
**The re-plan moves what is OWED; it does not move what has HAPPENED.** A declared leave is a session the family
**already spent** ⇒ re-dating it would rewrite a fact about a date that has passed.
🔑 **Same distinction CHECK 2 proved the night before** — *a hand-cancelled session is a decision about the plan;
a pause-cancelled one is the plan being replaced.* **A leave is a decision.** ⇒ **one rule, confirmed twice from
two directions, and it is why `endableSessions` never touches a leave.** **Recorded in `SYSTEM-FACTS`.**
📌 **@Porter was right to ask rather than assume** — *"the second night a leave has sat at the edge of a rule"*
is what made it worth stating as a rule instead of leaving it as behaviour.
⚠️ **The mixed-time plan is real and TRUE. Showing that row at 13:00 would be the lie.**

### 🟡 Q2 — *"Ends no live sessions"*: the VALUE is right, the LABEL is wrong
`deriveLiveEndDate` returns nothing because there is no live plan — **correct, and it must not change; the expiry
is a ceiling, not an end.** ⇒ **a category error: the answer does not fit the question the label asks.**

### 🔴 TASK-293 — @Porter's own defect, his copy verbatim
*"The one dialog whose whole job is to say 'here is what I DID' opens by sounding like 'may I?'"* — title
*"Resume this course?"* over a body in the past tense with only a `Close` button. ⇒ **Title: "Course resumed".**
🔑 **His own screenshot settles it: the plan behind the dialog is ALREADY updated** — the title is the only thing
on the screen still claiming otherwise.
📌 **Both items in that task are ONE class — a LABEL that outlived its VALUE** — the same family as the three
stale comments and his pause copy, **except a user reads these.**

### ✅ And his discipline on what the shots do NOT close
**The pause COUNT still needs @Tanya** — the owner captured the pause RESULT, not the pause dialog — **and NEW 1
is untouched because he TYPED `13:00`.**
🔑 ***"The default-time defect is only visible to someone who accepts the defaults, which is why she found it and
he could not."*** **That belongs in how QA rounds are scoped from now on: the owner tests like an owner, and the
defaults are exactly what he will not exercise.**

**BALL: @Fern — TASK-293.** @Tanya still owes TASK-291's count on `sid`.

## [--:--] Fern (FE) — ✅ **TASK-293 done. 📌 §1 needed no new string: @Porter's replacement was already in the dictionary.** 🔴 **And the word sweep found a THAI-only defect.** → @Sober
`smart-scheduler-front`, HEAD **`098a1d2`**. **Verified:** `bunx tsc --noEmit` **exit 0** · `bun test`
**159 pass / 0 fail** (was 148 — **+11**) · `bun run build` ok. 🚫 No backend change.

### §1 — the title, and the string that was already there
✅ **`endCourse.resumeDone` has said *"Course resumed"* since TASK-287** — it is the toast fired the instant the
re-plan lands. **@Porter's replacement is word-for-word a string this dialog already owned.** ⇒ **reused, not
copied.** *Two strings for one fact is precisely the class we spent this week separating*, and here the toast
and the title are the same sentence about the same act. *(Thai had it too: `กลับมาเรียนแล้ว`.)*
⚠️ **`isDrop` is tested first, and that is the real assertion.** `result` is only ever set on the resume face,
so the pause title was already safe — **writing the branch this way makes it structural instead of
incidental**, and the test pins the branch shape rather than only the strings. **Three titles in that dialog;
one changed.**

### §2 — the paused header
`plan.pausedNoEnd` — *"Paused — no dates until it resumes"* / *"พักอยู่ — ยังไม่มีวันเรียนจนกว่าจะกลับมาเรียน"* —
on `plan.summary.status === "DROPPED"`, **the server's lifecycle field, the same one the badge reads.**
🚫 No date invented, **no fall back to the expiry**, and the header's only date source is still `plan.liveEndDate`
— all asserted. Vocabulary taken from `course.DROPPED` / `course.droppedNoWrites` rather than phrased a third
way, as you asked.

### 🔑 §4 — I asserted "must not change" instead of promising it
**The four body sentences the owner actually read are pinned byte-for-byte, both languages** — including
`resumeExpirySame`, **the branch nobody had ever seen run until his screenshot.** ⇒ **those strings are evidence
now, not copy**, and a reword has to walk through a failing test. @Porter's pause line is pinned the same way.

### 🔴 Your Question — the word sweep. Three more, and one of them no English-side review can see.
1. **`plan.noLiveEnd`** — *"Ends no live sessions"* is **still there for COMPLETED and never-started plans.**
   Identical category error, other causes. **You ruled on paused, so I changed only paused** — but it is unfixed
   in two more states and I would rather say so than let the tick imply otherwise.
2. 🔴 **THAI: `noSessions` and `noLiveEnd` are the SAME STRING — both `ยังไม่มีคาบ`** — for **opposite facts**:
   *"No sessions yet"* (never started) against *"no live sessions"* (none live NOW). 📌 **REQ-036 already had to
   add a branch because *"ยังไม่มีคาบ"* on an ENDED course is "the opposite of the truth" — that fixed the ended
   case and left the collision.** ⚠️ **A Thai reader cannot tell them apart at all, and the English dictionary
   looks perfectly fine.**
3. 🟡 **`Paused` is one word for two mechanisms** — `course.DROPPED` and `bookingStatus.PAUSED`. Accurate for
   each, ambiguous across them; `PlanModal`'s own flag comment already says *"they share a word and nothing
   else."* Not a defect today — **it is why future copy about "paused" has to name which.**
✅ Checked and clean: `dropDone` (past tense, after the act — **the mirror of §1**), both confirm buttons,
`Close`, and `owed` / `size` / `leave`, each naming a value a pause does not change.

📌 **And the method answer, since you asked for the same sweep and not the same list:** the NUMBER sweep could be
automated — a figure either reads a field or computes one. **A WORD sweep cannot.** Nothing distinguishes
*"Ends"* over a date from *"Ends"* over a sentence except reading it. ⇒ **what I could pin, I pinned as
byte-exact tests; the rest is an eye — and item 2 needed a Thai one.**

**Ball: @Sober** — TASK-293 review. TASK-292 and TASK-286 are the two still open for FE, both marked no-clock.
⚠️ **Still owed by me, unverified:** TASK-291's pause count on `sid` (@Tanya) · TASK-289's four-row render ·
TASK-261's tray widths · TASK-265's four items.


## [--:--] Sober (SA) — ✅ TASK-293 DONE. 🔑 **@Porter's replacement copy already existed in the product.** 🔴 **And her sweep found a defect invisible in English.**
`tsc` **0** · **159/0** (+11) · build ok · 🚫 no backend change.

### 🔑 §1 needed no new string
**`endCourse.resumeDone` has been *"Course resumed"* — word for word — since TASK-287**, as the toast that fires
when the re-plan lands. ⇒ **she reused it instead of adding a second copy of one sentence.** **Two strings for
one fact is the drift class this week has been spent on.** 📌 **@Porter's supplied copy matched a string already
in the product, one component away.**
✅ **And `isDrop` tested FIRST makes the pause title's safety structural rather than incidental** — *"the test
pins the branch shape, not just the strings."*

### 🔴 The Question answer — `ยังไม่มีคาบ` means two OPPOSITE things
**Verified at source:** `plan.noLiveEnd` (*"no live sessions"* — **none left**) and `plan.noSessions` (*"No
sessions yet"* — **not started**) are **the identical Thai string**.
⚠️ **`REQ-036` already hit it** — a branch added because *"ยังไม่มีคาบ"* on an ENDED course *"is the opposite of
the truth."* 🔴 **That fixed the CASE and left the COLLISION.**
📌 ***"No English-side review would catch it"* — and every reviewer here, me included, reads the English
column.** ⇒ **TASK-294.**
✅ **And she changed only what §2 ruled on:** `noLiveEnd` still answers `Ends` with a sentence for COMPLETED and
never-started plans. **Named, not quietly widened.**

### 🔑 She turned the owner's screenshots into TESTS
**The four sentences he read are pinned byte-for-byte in both languages, including `resumeExpirySame` — the
branch nobody had seen fire until his screenshot.** *"They are evidence now, not copy."*
⇒ **the right thing to do with a verification that cost the owner a night**, and **the unseen branch is exactly
the one a later tidy-up would take.** 🔑 **Evidence decays into copy unless something holds it in place.**

### 📌 The boundary of the whole week's method, stated by the person who hit it
> *"The numbers sweep could be automated… a label sweep cannot: nothing distinguishes 'Ends' over a date from
> 'Ends' over a sentence except reading it… the rest is an eye, and the Thai one needed a Thai eye."*

**We have made numbers, statuses, keys and comments fail on their own.** **A LABEL cannot be made to fail** — it
is right or wrong only against a MEANING, and meaning does not compile. ⇒ **that is where the controls stop and
a reader has to start**, and it took a defect only a Thai reader could see to prove it.

**BALL: @Porter — @Tanya still owes TASK-291's pause count on `sid`.** Everything else has no clock.

## [--:--] Porter (PM) — ✅ TASK-293 in. 🔴 **Sober answered ONE of my two questions — I am chasing the other, because it is on the owner's release path.**
❓ **STILL OPEN: the `ON LEAVE` row kept `10:00` while every other row moved to `13:00`** ⇒ **one row in the plan
carries a time the course no longer has.** 🔑 **The owner's ruling covered "the remaining sessions" — it never
said what happens to a DECLARED LEAVE.** 📌 **Third time this week a leave has sat at the edge of a rule**
(`SICK_LEAVE` §2(a), refuted · the over-quota lock · now this). 🔻 **I am neither filing it nor assuming it is
right — I want the ruling stated first, then I decide whether it reaches him.**
🟢 **My replacement title was REDUNDANT — `endCourse.resumeDone` has said "Course resumed" since TASK-287.**
**@Fern reused the existing string instead of adding a second copy of the same sentence.** ⇒ **the drift lesson
applied rather than noted, and the product agreed with my instinct before we did.**
🔴 **Her find, and it is a class I could not have caught: `plan.noLiveEnd` and `plan.noSessions` are the SAME
Thai sentence** — *"ยังไม่มีคาบ"* meaning both **"finished with them"** and **"has not begun."** **Opposite.**
📌 **`REQ-036` already fixed the CASE and left the COLLISION** ⇒ **a fix that satisfies the report is not a fix
of the class.** ✅ **Agreed with Sober it is NOT for the owner: ours, small, off his path.**
🎯 **The owner's four sentences are now byte-for-byte tests — including `resumeExpirySame`, the branch nobody had
seen fire until his screenshot.** 🔑 **A branch nobody has seen is exactly the one a future tidy-up deletes.**
⇒ **his night became a regression net.**
📌 **Boundary line worth keeping, from @Fern:** *"a count either reads a field or computes one… a LABEL cannot be
made to fail."* **We made numbers, statuses, keys and comments fail on their own this week. Meaning does not
compile.** ⇒ **that is where the method stops, named by the person who hit the wall.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid` (TASK-293). @Tanya released on **the last release item: the pause dialog's COUNT.**
**One check.** 🚫 **Told her again that `4 OWED` from the owner's screenshots is NOT evidence for it** — he
captured the pause **RESULT**, not the pause **DIALOG**. ⇒ **her eyes on that dialog, or it does not pass.**
⛔ **Explicitly told her the summary dialog is CLOSED and to spend NOTHING on it** — **her `NOT_TESTED` stands as
a result, not as a debt she owes me.** 🔻 **A tester who thinks an unfinished check is a personal debt will find
a way to finish it, and that is exactly how a guess enters a report.**
❓ **Still chasing @Sober on the `ON LEAVE` row that kept `10:00`.** **Not filed, not assumed.**

## [06:xx] Tanya (QA) — ✅ **count PASSES on TASK-293** · 🔴🔴 **DEF-5 survived — the count was not the last item**

✅ **Count check (my eyes, on the dialog, as required):** `Aileen — plan`, pause dialog says **6**; plan shows
**9 rows** = 2 `ON LEAVE` + 1 `CANCELLED` + 3 `CONFIRMED` + 3 `EXTENDED`. **6 = CONFIRMED + EXTENDED, and all
three excluded rows are still on screen.** **Cancelled out — no write; Aileen untouched.**
🎯 **Stronger than Round 13's pass: two excluded categories instead of one, both visible.**
✅ **Copy fix landed:** paused header now reads `Paused — no dates until it resumes` (was `Ends no live
sessions`, flagged Round 11).
🔴🔴 **DEF-5 SURVIVED TASK-293** — resume still returns the raw Zod `startTime`/`HH:mm` error. **A paused course
cannot be brought back by any admin.** 🟢 No partial write; `b7dc8ace` still `DROPPED`.
⛓️ **The "free confirmation" (title should read `Course resumed`) is BLOCKED, not failing** — the resume never
succeeds so the success state never renders. **Flagged so nobody files it against @Fern's string.**
🔴 **Told @Porter plainly that his "only thing standing between us and `uat`" is no longer accurate**, and that
**`Aileen` and `Anya` — real students — are already PAUSED and cannot be resumed while DEF-5 is live.**

## [--:--] Porter (PM) — ✅ The COUNT passes. 🔴🔴 **DEF-5 survived TASK-293 and BLOCKS the release: no admin can complete a course resume through the UI.**
**Count:** `Aileen — plan`, dialog *"the remaining **6** sessions"* vs a **9-row** plan (`ON LEAVE 2` +
`CANCELLED 1` + `CONFIRMED 3` + `EXTENDED 3`) ⇒ **6 = 3 CONFIRMED + 3 EXTENDED, all three excluded rows still on
screen** — **the admin reconciles 9 → 6 without leaving the dialog.** 🎯 **A harder case than the one that
failed** (two excluded categories, not one), on a real student, **with `Cancel` pressed — no write.**
✅ **`Ends no live sessions` is now `Paused — no dates until it resumes`** — one of my two questions, answered on
screen rather than in a thread.

### 🔴 DEF-5: `startTime` fails `^([01]\d|2[0-3]):[0-5]\d$` — *"ต้องเป็นรูปแบบ HH:mm"*
🔴 **`Aileen` and `Anya` are already `PAUSED` on `sid` — two REAL students — and not one of them can be resumed
from the product.** 🟢 No partial write.
🔻 **THE OWNER'S SUCCESSFUL RESUME IS NOT A REFUTATION, and I said so before anyone reached for it: he TYPED
`13:00`.** ⇒ **a typed value is `HH:mm`; the PREFILL carries `HH:mm:ss`.** **The product works for whoever
overrides the default and fails for whoever accepts it — on the field an admin is likeliest to skim.**
🔻 **@Tanya reported those seconds in Round 12 and declined to file them — *"displays as `10:00`, so harmless as
seen"* — and I RECORDED IT AS HISTORY.** **She was right about what she could SEE. I never asked what CONSUMED
that value.** ⇒ **that is the miss, and it is mine.** ⚠️ **Third seconds bug this week (DEF-3 was `09:00:00`)**
— **I asked whether the CLASS has a home, without prescribing the fix.**
⛓️ **Protected @Fern from a false hunt at @Tanya's request:** the title still reads *"Resume this course?"*
**because the resume never SUCCEEDS, so the success state never renders** ⇒ **`Course resumed` is `NOT_TESTED`,
blocked by DEF-5, not a copy defect.**
🔻 **Corrected my own dispatch to Sober's team, who are working from my words:** I called the count *"the only
thing standing between us and `uat`"*. **It was not.**

## [--:--] Porter (PM) — 🔻 **I WAS WRONG ABOUT DEF-5. The owner settled it with screenshots. Retracted to @Sober and @Tanya before either could build on it.**
**My claim:** the prefill carries `HH:mm:ss`, so the product works for whoever types over it and fails for
whoever accepts the default. **Reality: the `Time` field is EMPTY.** ⇒ **submitting the form as it opens sends an
empty `startTime`.** **There are no seconds in this build anywhere.**
🔻 **I built the mechanism out of @Tanya's Round-12 seconds note, and it FIT — which is exactly why I should have
checked it instead of asserting it.** **A theory that explains the evidence is not the same as the cause.**
📌 **I sent the retraction loudly, to both, because my words reach Sober's engineers** — **an hour spent hunting
seconds that do not exist would have been my cost, paid by someone else.**

### 🔴 What his shots actually prove — three things, all FE-shaped
1. **`First session date` is PREFILLED and carries the required `*`. `Time` has NEITHER.** ⇒ **the mandatory
   field is the one the form marks optional and leaves blank.** 🔑 **The admin submits it empty because the form
   told them they could.**
2. 🔴🔴 **The error is dumped RAW at the admin** — a zod array and a regex in a red box.
   **The Thai message sits INSIDE it and nothing extracts it.** ⇒ **the worse half: even once the field is
   fixed, the NEXT validation failure looks like this.**
3. ✅ **`Course resumed` renders correctly once a time is entered** — title, toast, body, four `12:00` rows.
   ⇒ **@Fern's string was always right, @Tanya predicted it, and that `NOT_TESTED` is CLOSED by his shot.**

### ❓ Left explicitly UNEXPLAINED rather than guessed twice
**@Tanya saw `10:00:00` in Round 12; the field is BLANK now** ⇒ **something changed between those builds.**
🚫 **I did not theorise a second time.** **Asked @Sober; asked her to report what the field contains on open,
next build only.**

## [--:--] Sober (SA) — 🔑 **DEF-5 diagnosed at source. TASK-295 → @Fern, TASK-296 → @Jason.** ⚠️ **And I wrote the wrong task first; the contract refuted me before it left my hands.**

### 🔑 The cause, one chain
`time("start_time")` ⇒ **`"17:00:00"`** → `toSessionRow` ships it **raw** and `contract.ts:155` **documents that
on purpose** (*"the FE formats"*) → `PlanModal:573` → `resumeDefaultTime` returns it **verbatim** →
`<Select value="17:00:00">` over **`TIME_SLOTS = "09:00" … "17:00"`**.
⇒ **A `Select` given a value that is not one of its options renders EMPTY.**
🔑 **The field is not missing a default. It HAS one, and the control cannot display it. Empty IS the seconds,
rendered.**
🔴 **And `startTime` being truthy means `disabled` never fired** ⇒ **the admin could submit**, `"17:00:00"` went
up, the `TIME` regex refused it. **The owner was not clicking through a warning.**

### 🔑 Every observation now reconciles — including @Tanya's, eleven rounds early
**Her Round-12 `10:00:00` was the VALUE; the owner's empty field is the same value through a control that cannot
show it.** ⚠️ **She reported the CAUSE of tonight's defect on 09-08 round 12 and nobody — me first — connected
it, because the two symptoms do not resemble each other.** 📌 **Not an unexplained note. An unread one.**
✅ **His typed `13:00` worked because a typed value is a real option** ⇒ **the feature works for whoever overrides
the default and fails for whoever accepts it.**

### ⚠️ I wrote the BACKEND task first, and it was wrong
**TASK-295 as drafted told @Jason to make `toSessionRow` apply `hhmm()`.** I deleted it unsent. The refutation
was in the codebase:
- **`contract.ts:155` documents `HH:mm:ss` and names the FE as the formatter** ⇒ a **kept promise**, not an
  oversight;
- **the FE already keeps its half three times** — `PlanModal:689`, `:1143`, `:1210`, all `.slice(0, 5)`.
⇒ 🔑 **The defect is not that the value has seconds. It is that the format rule was applied everywhere a human
READS the value and skipped where a CONTROL must MATCH it.**
⇒ **I was one command from breaking three working call sites and a documented contract to fix one that forgot to
call the function.** **What stopped it was the step I have made every engineer take all week — name the
consumers before you change the shape.** 📌 **@Porter corrected himself loudly last night; this is that debt paid
in the other direction, and I told him so.**

### 🔴 The red box is bigger than @Porter thought — TASK-296
`zValidator` on **every route** has **no error hook** ⇒ it answers directly and **`app.onError` is never
reached**; the `ZodError`'s `.message` **is** the issue array. ⇒ **every 400 in this product, on every screen,
has always looked like that.**
📌 **Unseen because our forms normally gate the button. DEF-5 is the first time one let a bad value through** ⇒
**the owner did not find a resume defect; he found the first door onto a hole that was always there.**
🔑 **Third time this week that a thing "nobody would ever hit" was hit the moment one gate moved.**
⚠️ **`app.onError` exists and is correct and was bypassed.** **A handler that is not reached is worse than a
missing one — it looks handled.**

### 📌 The lesson of the night, and it is mine
**A `Select` fails SILENTLY.** No error, no warning, no compile failure — it shows nothing and reports nothing.
⇒ **that is why this cost the owner a night, why @Tanya's early sighting could not be connected, and why the
next one will do the same.** 🔑 **Everything we have built this week makes things fail LOUDLY. A control that
renders empty is the one failure mode none of it can catch.**

**BALL: @Fern — TASK-295, the release is on it.**

## [--:--] Fern (FE) — ✅ **DEF-5 / TASK-295 fixed at one seam. 🔴 §3 was real — the MOVE dialog has been empty all along.** 📌 **And my OWN test stayed green through the break.** → @Sober
`smart-scheduler-front`, HEAD **`098a1d2`**. **Verified:** `bunx tsc --noEmit` **exit 0** · `bun test`
**168 pass / 0 fail** (was 159 — **+9**) · `bun run build` ok. 🚫 No backend change.

### The seam — `lib/scheduler/time-slot.ts`
`toTimeSlot(value, fallback): TimeSlot` — slice, then **check membership**, else a **visible** fallback.
🔑 **The return type is `TimeSlot`, not `string`**, so *"the result is always a slot"* holds **by construction**;
`FALLBACK_TIME` is typed the same way, so the compiler proves the fallback is a member too. **No caller can hand
a `Select` a value it cannot render.**
⚠️ **I did not put it in `resume-defaults.ts` as §4(a) said, and the reason is §3:** once the move dialog turned
out to have it too, this stopped being a rule about resuming. **One rule, two callers** — and a shared rule
filed under one caller's name is how the next stale comment gets written. `resume-defaults` delegates.

### 🔑 Break it and watch — and the finding is not the failure
The failure is exactly the defect: **`Expected: "17:00"` · `Received: "17:00:00"`**, and the same for @Tanya's
`10:00:00`.
📌 **The part worth your time: TASK-288's own assertions stayed GREEN through the break.** I wrote
`resumeDefaultTime("17:00") === "17:00"` — **and `"17:00"` is the one shape the server never sends.** ⇒ **I
proved the rule against a value that does not exist in production, and that is what let this ship.**
**Same family as everything else this week** — a test that is right about a world the code does not live in.

**What the SCREEN does**, since you asked and the compiler never saw it: **nothing.** Mantine finds no option
matching `"17:00:00"` and draws an empty field — **no error, no warning, no console line** — and because the
value is truthy the submit guard never fires. **The control reported a missing value that was present.**

### 🔴 §3 — YES, and it has been live the whole time
`PlanModal:828` seeded the move dialog from a plan row into the same `TIME_SLOTS` `Select`. Fixed through the
same seam.
⚠️ **On the evidence, plainly: I still cannot open the app** (the auth wall — every rendered check this week has
gone to @Tanya). **But this is not theory: the mechanism is now OBSERVED** — the owner saw it — and this call
site feeds an identical control from an identically-formatted value.
🔑 **Why nobody reported it while the resume cost the owner his night: on a MOVE you came to change the time, so
an empty field reads as "pick one". On a RESUME the promise is "the same slot, later" — there, empty reads as
broken.** **Same mechanism, opposite intent, and only one of them looked like a defect.**
⚠️ It also handed `"17:00:00"` to `useSlotAvailability` and would have submitted it.

### ✅ And you were right to check the consumers before sending it to @Jason
**Your §2 is the whole answer and I want to say it back:** `db/mappers.ts:123` ships the **booking** DTO through
`hhmm()`; `toSessionRow:1781` ships the **plan** DTO raw. **Two mappers, one field, two formats — and
`contract.ts:155` documents only the exception.** ⇒ **`hhmm()` on `toSessionRow` would have broken three working
display sites and a documented contract to fix one place that forgot to call the rule.**

### Your Question — **only two Selects take a server value against `TIME_SLOTS`, and they are the two I fixed**
✅ `BookingModal`'s time Selects are **safe because the booking mapper normalises** — 🔑 **and the proof is not
my grep: `CalendarGrid.tsx:66` matches `b.startTime === time` against a `TIME_SLOTS` string. If bookings shipped
seconds, the calendar would render NO bookings at all.** **The main screen working is the assertion.**
✅ REQ-076's booking-resume time starts `null` ⇒ **a placeholder.** ⚠️ **That distinction is worth keeping: a
`null` renders as *"choose one"*; a wrong-format value renders as nothing.**
✅ `CreatePlanFlow` and `ImportBalanceModal` seed a literal `"10:00"`.
⚪ The id-based Selects can't format-mismatch — **but a stale id would go blank just as silently.**
📌 **Your point is the durable half: a `Select` fails silently — no error, no console line, and the value is
still in state — so it survives a review, a typecheck, a unit test AND a screenshot taken by anyone who typed
their own answer.** ⇒ **the only general defence is to stop handing controls values they cannot render**, which
is why `toTimeSlot` returns `TimeSlot` and not `string`.

**Ball: @Sober** — DEF-5 review, then @Tanya on `sid`: **open a paused 17:00 course's resume form (Time reads
17:00, not blank) and open ANY session's move dialog (Time reads its own time).**
⚠️ **Still owed and unverified by me:** TASK-291's pause count · TASK-289's four-row render · TASK-261's tray
widths · TASK-265's four items. **Dropped on your instruction, not forgotten:** TASK-292, TASK-294, TASK-286.

## [--:--] Fern (FE) — 🔴 **@Tanya's Round 13 landed while I was writing. She is right, the DoD did not cover her half, and I have fixed it too.** → @Sober
**Re-verified:** `bunx tsc --noEmit` **exit 0** · `bun test` **170 pass / 0 fail** (was 168) · `bun run build` ok.

### 🔑 There is no hidden third input — and her sentence is still exactly right
**I checked every path: this repo has ONE course-resume submitter** (asserted — exactly one `resume.mutateAsync`
in that component). No second dialog, no stray field.
⇒ **what she read in the DOM was the `Select`'s own internals.** `searchable` renders **a search box whose text
is not the value**: type `13:00`, do not click the filtered option, and on blur the box reverts while
`startTime` stays what it was. ⇒ ***"the field the admin edits is not the field submitted"* is a precise
description of `searchable`** — she was wrong only about it being a stray field, and **right about everything
that matters.**
📌 **It also resolves her contradiction with the owner:** **he typed AND PICKED. She typed.** ⚠️ **On a field
that was rendering empty, typing is exactly what the control invites** — so the two of them met different halves
of one defect again, for the second time in two days on this same field.

### ✅ `searchable` removed from the resume `Time`
Nine options never needed a search box, and while the field was blank it read as one that had to be typed into.
⚠️ **This matters MORE after my value fix, not less:** with a valid default now showing, a typed-but-unpicked
value would no longer produce a red box — **it would submit the wrong time silently.** ⇒ **the error would have
become a silent wrong lesson**, which is the exact class we have spent the week separating.
⚠️ **Beyond the written DoD and I am saying so rather than burying it** — §4 neither listed nor forbade it.
**Revert it in one line if you disagree.** My reason for not waiting: **the release is blocked on this dialog,
and she re-tests by typing** — shipping the value fix alone would have failed her round again at 06:00.
📌 **`CreateCourseModal` still has `searchable` on its copy of these two inputs** (TASK-287 §1 copied them
verbatim). **Untouched — not my task, and its field has never rendered empty. Yours to rule on.**

### ⚠️ And the honest limit
**I have not seen either field on a screen.** The mechanism above is read from the component and from her DOM
evidence — **it is not observed.** ⇒ **her re-test decides it, and it needs BOTH halves: (a) open the resume
form and read `Time` without touching it — it must show the course's own time; (b) change it and submit — what
lands must be what was shown.**

**Ball: @Sober.**

## [--:--] Porter (PM) — 🔴 Owner discarded uncommitted changes in `smart-scheduler-front`. **Checked the repo before reporting the loss, instead of relaying his description of it.**
✅ **Every commit is intact — `HEAD` = `01203d3` (TASK-293), the exact build he deployed to `sid` and @Tanya
tested.** ⇒ **nothing shipped or tested is affected.** Reflog clean back through `db1ac1b`.
🟢 **Two UNTRACKED files survived** (a discard does not touch untracked): `src/lib/scheduler/time-slot.ts` (38
lines) + its test (99 lines). 🔑 **Nothing in `src` imports either yet** ⇒ **they read as the START of TASK-295:
the helper and its tests exist; the WIRING is what was lost.**
🔴 **Gone: every TRACKED file modified since `01203d3`.** 🚫 **Git keeps no record of a discarded uncommitted
change** ⇒ **I told @Sober plainly that I cannot list them and neither can he — @Fern is the only source of
truth.** 📌 **And warned against reconstructing the list from the TASK files: a reconstruction that misses one
file is WORSE than starting clean, because it looks finished.**
🔻 **Offered my scope read (TASK-295, maybe 296) explicitly as a starting point, not a finding.** **Both tasks
are still open** ⇒ **this costs TIME, not knowledge.**
📌 **Asked that @Fern be told her two files survived** — **the redo is the wiring, not the thinking.** 🔑 **A
redo that feels like erasure gets rushed; one that starts from surviving work does not.**
🚫 **No blame recorded anywhere. It is a click.** **Git is the owner's alone and I did not touch a thing —
read-only inspection only.**

## [--:--] Porter (PM) — 🔴 CORRECTION on the discard: it destroyed **`TASK-295` FINISHED**, not work in flight. **The board was still showing it `DONE`.**
🔻 **I reported the loss before reading my own board.** **It was complete — `tsc 0 · 168/0 (+9) · build ok` —
and never committed.** ✅ **Board now reads `CODE LOST → REDO`.** 🔑 **A green row for code that no longer exists
is the most dangerous line on a board: it is precisely what lets something ship unbuilt.**
🔻 **And I retracted my own advice within the hour.** I told @Sober *"a reconstruction that misses one file is
worse than starting clean"* — **true of reconstructing from a DoD, FALSE here**, because @Fern wrote her
implementation into the TASK file and **those notes are the map**: the `toTimeSlot` seam (**the file itself
SURVIVED, untracked, with its 99-line test**), `required` on `Time`, `PlanModal:828` (the everyday MOVE dialog,
empty all along), and **`searchable` removed** — *"the search box is not its value"*, which would have submitted
a wrong time **silently** once the value bug was fixed.
⇒ **The thinking survived in TWO places — her notes and her helper. Only the wiring is gone.**
🚫 **I still refuse to hand her a FILE LIST:** **I can see what she built; I cannot see every file she touched.**
**Her notes are a starting point for HER, not a spec for someone else.**

## [--:--] Sober (SA) — 🔴 **TASK-295 RETURNED. The design is right; the edits are not on disk.** ⚠️ **I reproduced before saying so.**

```
bunx tsc --noEmit → exit 0        (true — the new file is self-consistent)
bun test          → 170 tests, 166 pass, 4 FAIL   (reported as "170 pass / 0 fail")
git status        → only ?? time-slot.ts and ?? time-slot.test.ts — NOTHING modified
```
`resume-defaults.ts:24` still returns verbatim · `PlanModal.tsx:828` unchanged · `DropResumeDialog.tsx:303` still
`searchable`. ⇒ **`toTimeSlot` has no caller in `src/` but its own test.** **The four failures are @Fern's own
assertions, failing correctly.**

### 🔑 The mechanism, and it is mine from three days ago
**The report quotes the first two failures as the output of "break it and watch."** ⇒ **the break was performed,
watched, and never undone** — and the two source-text assertions were written for edits never applied.
**The count `170` is exactly right, so the run happened. The verdict beside it did not.**
📌 **TASK-273: I called a mutation a pass when `tsc` had failed on my own malformed stub.** ⇒ that is why the DoD
says *"break it AND check it broke for the reason you expected"* — 🔑 **and the half that catches THIS is the one
I never wrote down: the break must be UNDONE, and the suite green, before a number is reported.**
⚠️ **"Break it and watch" adds a step where the tree is deliberately red. A method that manufactures a red tree
owes a rule about restoring it.** ⇒ **going into `SA-Lead.md`, because the method is mine and so is the gap.**

### ✅ What I explicitly did NOT send back — the thinking is better than the task I wrote
- **`toTimeSlot` returns `TimeSlot`, not `string`** ⇒ *"no caller can hand a `Select` a value it cannot render"* —
  **a stronger guarantee than the membership check I asked for.**
- **He REFUSED my §4(a) placement** (`resume-defaults.ts`) because §3 turned out real: **one rule, two callers,
  and the second is the everyday MOVE path.** *"A shared rule filed under one caller's name is how the next stale
  comment gets written."* ✅ **This week's lesson applied before I noticed it applied.**
- 🔑 **The Question:** `db/mappers.ts:123` uses `hhmm()`, `scheduler.service.ts:1781` does not ⇒ **two mappers,
  one field, two formats — and `contract.ts:155` documents only the exception.** **A reader of that comment would
  fairly take it for the rule.**
- 🔑 **And his proof for the booking Selects is not a grep:** `CalendarGrid` matches `b.startTime === time`
  against a `TIME_SLOTS` string ⇒ **if bookings shipped seconds the calendar would render NO bookings. The main
  screen working IS the assertion.**
- ✅ **`null` renders a PLACEHOLDER; a wrong-format value renders NOTHING.** → SYSTEM-FACTS.

### 🔑 §6 — @Tanya's Round 13 has no hidden input, and her sentence is still exactly right
There is **one** resume submitter in the repo. **What she read in the DOM was `searchable`'s own search box —
text that is NOT the value.** ⇒ ***"the field the admin edits is not the field submitted"* is a precise
description of the control**; only the cause was the control rather than a stray field.
📌 **It resolves her contradiction with the owner: he typed AND PICKED. She typed — on a field rendering EMPTY,
which is exactly what typing is for.**
✅ **`searchable` removed — APPROVED, beyond the DoD, and he flagged it rather than burying it.** His reason is
the ruling: **with a valid default showing, a typed-but-unpicked value would no longer ERROR — it would submit
the wrong time silently.** ⇒ **the red box would have become a silent wrong lesson.**
✅ **`CreateCourseModal`'s copy stays** — a new course has no server value to mismatch, **so the mechanism is
absent**, and changing an unaffected screen on deploy night is how a fix becomes a second defect.

**BALL: @Fern — apply the four edits, re-run, paste the `0 fail` line rather than the total.**

## [--:--] Sober (SA) — 🔻 **CORRECTION: my TASK-295 diagnosis was WRONG. The code was not unwritten — the owner's `discard` wiped it, and @Porter had already said so in my inbox.**

**What I wrote one entry above** — that @Fern's break-and-watch was never restored and a false pass was reported
— **is not what happened. I have corrected it to her directly and retracted the standing rule I minted from it.**

🔑 **The owner accidentally ran `discard` in `smart-scheduler-front`.** Every **tracked** modification since
`01203d3` was wiped; **untracked files survived** ⇒ **precisely the state I reproduced**: `time-slot.ts` and its
test present, every wiring edit gone, four of her own assertions red. **Her `tsc 0 · 168/0 · build ok` was true
when she ran it.**

### ⚠️ Both stories fit every fact I had. The true one needed a fact I never collected.
**It was in `inbox/SA.md`, from @Porter, while I was in the repo reproducing.** ⇒ 🔑 **I checked the tree and not
the board.** **The tree tells you the state; it does not tell you who put it there.**
🔴 **And I wrote the judgement before the check** — about an engineer who **cannot answer back up the chain.**
📌 **A nudge means re-read the board, today's log AND `inbox/SA.md`. I have had that rule since the first day and
I skipped the third file.** → **`SA-Lead.md`, replacing the rule I had just written from the wrong diagnosis.**
⚠️ **That is the part worth keeping: a wrong diagnosis does not stop at the message. Mine had already become a
standing process rule before I read the correction.**

### ✅ The review itself stands — only the cause was wrong
`toTimeSlot` returning `TimeSlot`; the seam kept OUT of `resume-defaults.ts`; the two-mapper finding
(`db/mappers.ts:123` vs `scheduler.service.ts:1781`); the `CalendarGrid` behavioural proof; **`searchable`
approved**; `CreateCourseModal` left alone. **None of that depended on how the tree got that way.**

📌 **@Porter also corrected his own first loss report** (*"work in flight"* → **finished work that never reached
a commit**), and fixed a `DONE` row on the board. **His line is the one to keep: *"a green row for code that no
longer exists is the most dangerous line on the board."*** 🔑 **Two of us, one hour, both corrected loudly. That
is the mechanism working, not failing.**

**BALL: @Fern — the redo is the wiring, not the thinking.**

## [--:--] Sober (SA) — ✅ **Replied to @Porter on both messages. 🔴 And raised the one thing tonight that no agent here can fix.**

### 🔴 The systemic item, routed UP because it is not ours
**A completed, reviewed, verified task existed only as uncommitted changes in a working tree** — `tsc 0 · 168/0 ·
build ok`, board row `DONE` — **and one click ended it.**
🔑 **Nothing in our process would have caught that, because nothing in our process is wrong.** The task was done.
The row was true. ⇒ **the exposure is the WINDOW between a green row and a commit, and that window is entirely
the owner's** — **git is his alone and no agent here commits.**
📌 **Raised to @Porter as an option for the owner, explicitly not as a correction.** ⚠️ **He lost an hour of
someone else's work; he does not need a process lecture on top of it.** **The framing I asked Porter to keep:
*a `discard` is one click, and the only unusual thing was that finished work was standing in its path.***

### 🔑 The line from tonight worth keeping is @Porter's, not mine
> *"A green row for code that no longer exists is the most dangerous line on the board."*

**He wrote it while correcting his own loss report** — he had called it *"work in flight"* before reading his own
board and finding it was finished work. ⇒ **the board row, not the lost code, is what could actually have
shipped this to `uat` unbuilt.**

### ⏳ State
⛔ **TASK-295 REDO → @Fern** — the helper and its 99-line test survived (untracked); **the redo is the wiring, not
the thinking**, and her own §5/§6 notes are the map ·
⛔ **TASK-296 → @Jason**, untouched by the discard, different repo ·
🔴 **TASK-291's count on `sid` → @Tanya** ·
🔑 **after the redo @Tanya needs BOTH halves: read `Time` untouched, THEN change it and submit** — the value that
lands must be the value shown ·
📋 no clock: TASK-294 · 292 · 286 · 284 · 279 · 🟢 no migration.

📌 **Correction carried to @Tanya via @Porter: there is no hidden third input.** She read the `Select`'s own
`searchable` box — **text that is not the value.** ***"The field the admin edits is not the field submitted" was
exactly right; only the cause was the control.***

**BALL: @Fern.**

## [--:--] Fern (FE) — ✅ **TASK-295 re-applied. `0 fail`, and the components are MODIFIED.** 🔑 **The failure had a shape, and the shape said "discard".** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  170 pass
                       0 fail          ← the line you asked for
bun run build     → ok
git status --short
 M src/components/partials/Bookings/DropResumeDialog.tsx
 M src/components/partials/Bookings/PlanModal.tsx
 M src/lib/scheduler/resume-defaults.ts
?? src/lib/scheduler/time-slot.test.ts   ?? src/lib/scheduler/time-slot.ts
```
✅ **Break-and-watch, said explicitly as you asked: it ran, it failed for the right reason, and it WAS
RESTORED.** The §5 quote is from that run and the suite was green after it.

### 🔑 Check this before anyone redoes more — **TASK-289, 291 and 293 were NOT lost**
**HEAD is `01203d3`, *"update dialog titles and labels for paused courses"* — that IS TASK-293, committed.**
⇒ only TASK-295's wiring was uncommitted, and only that went. 📌 ***"Every tracked modification since
`01203d3`"* could be read as a wider loss than it was**, and re-doing already-committed work is its own defect.
**Your own numbers prove it:** `plan-rows`, `pause-preview` and `dialog-labels` assert those three tasks'
wiring **by source text** — had any of it gone, they would have failed too. **They passed. All four failures
were TASK-295's.**

### 📌 The diagnostic I would not have thought to ask for
**Untracked survives a discard; tracked does not.** So the new pure module lived and every caller of it died —
and the suite failed as ***"a rule with no caller"*: four failures, every one a WIRING assertion, not one a RULE
assertion.** `toTimeSlot`'s behavioural tests were all green, because the logic was in the file that survived.
🔑 **Rule-green + wiring-red is a LOST EDIT, not a bad change.** A bad change fails the other way round, or
fails both. ⇒ **the source-text assertions I write to prove absences (the AC-8 pattern) turn out to detect a
vanished edit as well** — I did not design them for that, and it gave the redo a checklist that was not my
memory: **the four failures were the four missing edits, one each.**

### On anything in flight that is not written down
**None.** The five files are named in §5/§6, four of them are pinned by the surviving test, and the fifth is
that test's own module. ⚠️ **The only thing that class of check could miss is a file I touched but never
asserted — there was none.**

### On your first message
✅ **You withdrew it before I read either, so there is nothing to answer** — and the line worth keeping is the
second one's, not the first one's about me: ***"the tree tells you the state; it does not tell you who put it
there."***

**Ball: @Sober** — then @Tanya, both halves: **(a)** open a paused 17:00 course's resume form and read `Time`
**without touching it** — it must show 17:00, not blank; **(b)** change it and submit — **what lands must be
what was shown.** Plus the move dialog's `Time` on any session.
⚠️ **Still owed and unverified by me:** TASK-291's pause count · TASK-289's four-row render · TASK-261's tray
widths · TASK-265's four items. **Open, not started:** TASK-292, TASK-294, TASK-286.

## [--:--] Sober (SA) — ✅ **TASK-295 RE-APPLIED and PASSED. Verified by me, not reported to me.** 🔑 **And @Fern's §7 gave us a diagnostic worth more than the fix.**

**Reproduced:** `bunx tsc --noEmit` **0** · `bun test` **170 pass / 0 fail** (399 expects, 18 files) ·
`git status` — **three components MODIFIED** · `smart-scheduler-back` **0 changes** · three `.slice(0, 5)` sites
intact · `searchable` gone, `required` on, both call sites through `toTimeSlot`.

### 🔑 *"Rule-green + wiring-red is a lost edit, not a bad change"*
**Four failures, every one a WIRING assertion; not one a RULE assertion.** `toTimeSlot`'s own behavioural tests
were green throughout **because the logic was in the untracked file that survived.** ⇒ **a broken change fails
the other way round, or fails both.**
🔴 **I had that pattern in front of me and read it as a false pass.** **The shape was legible and I did not read
the shape.** → SYSTEM-FACTS, in her words.
✅ **And she is straight about the luck in it:** those source-text assertions were written to prove **absences**
(the AC-8 pattern) and **detected a vanished edit, which she did not design them for.** ⇒ **the redo had a
checklist that was not her memory** — **the four failures WERE the four missing edits, one each.**
🔑 **A test that catches what you did not aim it at is worth more than one that passes for the reason you
expected.**

### ✅ She tightened @Porter's loss boundary, and it mattered
*"Every tracked file modified since `01203d3`"* is accurate and **reads wider than the loss was**: **`01203d3` IS
TASK-293** ⇒ **TASK-289, TASK-291, TASK-293 all survived inside that commit.**
📌 **Proved behaviourally** — those three are pinned by source-text tests; **had any been lost they would have
failed too. They passed.** ⚠️ **The wider reading would have had someone redo COMMITTED work, which is its own
defect.** 🔑 **Second time tonight she proved a thing with a test that would have had to fail, rather than a
grep.**

### 🔴 What is not done, and it is not the code
**Nobody has seen this on a screen.** ⇒ **@Tanya, and she needs BOTH halves:** **(a)** open the resume form and
read `Time` **untouched** — it must show the course's own time *(DEF-5 as the owner hit it)*; **(b)** change it
and submit — **the value that lands must be the one shown** *(her Round 13)*.
⚠️ **(a) does not imply (b), and (b) is the half that turned dangerous:** with a valid default now showing, a
typed-but-unpicked value **would no longer error — it would submit the wrong time silently.**

📌 **Carried to @Tanya via @Porter: there is no hidden third input, and her sentence was still exactly right.**
*"The field the admin edits is not the field submitted"* **is a precise description of a `searchable` Select.**
**She read the DOM correctly; only the cause was the control.**

**BALL: @Porter — @Tanya and the screen.**

## [--:--] Porter (PM) — ✅ `TASK-295` rebuilt after the discard (`tsc` 0 · **170/0**, four wiring edits). 🔴 **Corrected @Sober: `TASK-291`'s COUNT is NOT outstanding — @Tanya passed it hours ago.**
**His release table still listed it under her.** ⇒ **a stale row on the SA's side becomes a re-test on QA's** —
**and she has spent enough of tonight re-proving things.** **She has TWO checks, not three.**
✅ **@Fern corrected my loss report and she is right: `01203d3` IS TASK-293** ⇒ **289, 291, 293 all survived
inside it; only TASK-295's wiring was ever at risk.** 🔑 **She PROVED it — those three are pinned by source-text
tests, so had any been lost those would have failed too; all four failures were TASK-295's.** 🔻 **My "every
tracked file modified since `01203d3`" was accurate and read WIDER than the loss** ⇒ **a wider reading would
have had someone redo committed work, which is its own defect.**
🔑 **Ordered her round (a) → (b) and refused to let them collapse into one:** ⚠️ **with a valid default finally
showing, a typed-but-unpicked value would no longer ERROR — it would submit the WRONG TIME SILENTLY** ⇒ **(b) is
the only check that proves the `searchable` removal.**
✅ **Told her the third input does not exist AND that her sentence was still exact** — *"the field the admin
edits is not the field submitted"* **describes a `searchable` Select precisely.** **Told her not to soften that
report.** 🔻 **Her Round-12 `10:00:00` was this defect ELEVEN rounds early — I filed it as history and then
built a wrong theory on top of it.**
⛔ **Release still blocked by something that is NOT hers: `TASK-296`, the raw zod box, @Jason, BE repo.**
**Told her so, so she does not hunt harder to clear a blocker she cannot reach.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid` (TASK-295 rebuilt). @Tanya released on **(a) then (b)**, count struck.
**Repeated the one thing a tester would reasonably collapse:** ⚠️ **a valid default now shows, so a
typed-but-unpicked value would no longer ERROR — it would submit the WRONG TIME SILENTLY.** ⇒ **(b) is the only
proof of the `searchable` removal, and (a) passing says nothing about it.**
🚫 **Told her again not to re-run the count** — **she passed it; the stale row was @Sober's table, not her debt.**
⛔ **And told her the release stays blocked by `TASK-296` regardless of her result** — 🔻 **a tester who believes
she is the last gate hunts harder than the evidence supports.**

## [07:xx] Tanya (QA) — 🎉 **DEF-5 FIXED. (a) PASS · (b) PASS · summary dialog SEEN.**

✅ **(a)** `Time` on open: **prefilled `10:00`**, visible input **`readOnly`**, hidden value identical, **no
seconds**. 🔑 **Answers @Porter's question: the CONTROL was replaced, not patched** — `10:00:00` (R12) → blank
(owner) → `10:00` + readOnly (now). **An admin can no longer type into it at all.**
✅ **(b)** Picked **`14:00`** (distinct from both the default and the original 17:00 so no result could be
coincidence) → **shown 14:00, hidden 14:00, landed 14:00 on all four sessions**, API-confirmed. **No silent
substitution.** 🟢 The hand-cancelled `22/Sep 10:00` still sits beside the new `22/Sep 14:00` — **@Fern's
distinction survives a re-plan.**
🎉 **Summary dialog SEEN: title `Course resumed`**, body states *4 session(s) put back · ends 06/Oct/26 · expiry
unchanged 01/Dec/26*, holds open with `Close`. **Every number true against the API.** ⇒ **my Round-12
`NOT_TESTED` closed by observation, and @Fern's string was fine all along.**
📌 **Watch, not filed:** 13 `CANCELLED` rows now accumulate in the DB, hidden from the plan; **the count
correctly excludes them**, but nothing prunes them.
⚠️ **`b7dc8ace` is ACTIVE again — the reproduction could not survive check (b), since (b) IS a resume.**
Declared, not restored. **`TASK-296` (raw zod box) still blocks the release and is not mine.**

## [--:--] Porter (PM) — 🎉 **(a) PASS · (b) PASS · DEF-5 FIXED · summary dialog SEEN.** ⛔ **Every QA check on this release is green. The release is not — `TASK-296` is the only blocker left.**
**(a)** `Time` on open: visible `10:00` · `readOnly: true` · hidden value identical · no seconds — **the course's
own time.** **(b)** She picked **`14:00`**, **deliberately distinct from BOTH the `10:00` default and the
original `17:00`** ⇒ **no mechanism could have produced that pass by accident.** **Landed on all four rows,
API-confirmed. No silent substitution.**
🟢 **Unasked-for check: the hand-cancelled `22/Sep 10:00` still sits beside the new `22/Sep 14:00`** ⇒
**@Fern's decision-vs-replaced distinction survives a RE-PLAN, not merely a pause.**
🎉 **Summary dialog seen, `Course resumed`, all three sentences, numbers checked against the API** ⇒ **her R12
`NOT_TESTED` closed BY OBSERVATION, not by assurance.**

### 🔑 She resolved the three-way contradiction I could not
**`10:00:00` (her R12) → BLANK (owner) → `10:00` + `readOnly` (now)** ⇒ **THE CONTROL WAS REPLACED, NOT
PATCHED.** **Her seconds and his empty field were BOTH real**, and my theory could never have been right about a
single build. 🔻 **The thing that settled it was reading the DOM rather than reasoning about it — which is
exactly what I failed to do when I invented the prefill mechanism.**

### 📌 Held for the owner as DECISIONS, not defects — deliberately not tonight
**13 hidden `CANCELLED` rows accumulate with nothing pruning them** (the count correctly excludes them, so this
is not the old defect) · **the over-quota leave spending its lock** · **the purchase-date ceiling** ·
**`resume/preview`.**
⚠️ **Footprint: `b7dc8ace` is `ACTIVE` again — check (b) IS a resume, so the reproduction could not survive it.**
**She declared it rather than quietly restoring it.**

## [--:--] Sober (SA) — 🎉 **DEF-5 CLOSED on a screen, both halves.** 🔴 **And I corrected @Porter's reading of the DOM: the control was PATCHED, not replaced.**

**@Tanya on `sid`:** **(a)** `Time` on open — visible `10:00`, hidden `10:00`, **identical, no seconds**, the
course's own time · **(b)** picked **`14:00`**, **deliberately distinct from BOTH the `10:00` default and the
original `17:00` so no result could be coincidence** → visible → hidden → **landed on all four rows,
API-confirmed.** 🟢 **No silent substitution** ⇒ **the `searchable` removal is proven, not assumed.**

### 🔴 *"The control was replaced, not patched"* — checked, and it is one prop
`@mantine/core/esm/components/Select/Select.mjs:134`:
```
readOnly: readOnly || !searchable
```
🔑 **`readOnly: true` IS the observable signature of @Fern removing `searchable`.** ⇒ **@Tanya read the mechanism
of the fix out of the DOM without being told what to look for** — **stronger evidence than the PASS itself.**

**And the three readings that never fit together are ONE build plus one fix:**
| **R12 `10:00:00`** | the **hidden** input — the value, with seconds |
|---|---|
| **owner: BLANK** | the **visible** input of the **same build** — no option matched, nothing rendered |
| **now `10:00` == `10:00`, readOnly** | `toTimeSlot` landed it on a real slot; `!searchable` made the box unwritable |
⇒ 🔑 **the value and its rendering, the two ends of one control.** ⚠️ **That is precisely what made it invisible:
a screenshot shows one end, a DOM read shows the other** — **and three people each held one end for a week.**
📌 **@Porter's own lesson survives the correction: reading the DOM beat reasoning about it. Only his conclusion
needed fixing, not his method.**

### ✅ Struck from my table — @Porter was right to push back
**TASK-291's COUNT passed four hours ago:** `Aileen — plan`, **6 = 3 CONFIRMED + 3 EXTENDED against 9 rows**,
**two excluded categories both visible**, `Cancel` pressed. ⇒ **@Tanya had TWO items, not three.** 🔻 **A stale
row on my side becomes a re-test on hers, and she has spent tonight re-proving things.**

### ✅ Two things nobody asked for, both held
🟢 **The hand-cancelled `22/Sep 10:00 CANCELLED` still sits beside the new `22/Sep 14:00 PENDING`** ⇒ **@Fern's
decision-vs-replaced distinction SURVIVES a re-plan** — **unasked on both sides.**
🎉 **The summary dialog has been SEEN** — `Course resumed`, three sentences, every number checked against the
API. ✅ **Her string was right BEFORE the fix, and I have told her so.**

### ⛔ ONE blocker left: TASK-296, @Jason, BE
🔴 **An admin's next validation failure is still a regex in a red box — on every endpoint, not this one.**
✅ **QA does not need `b7dc8ace` restored:** **a resume is now a poor way to test 296** — the check is *"any
refused form shows a sentence, not an array"*, **and the cheapest reproduction is any form QA can fail without a
paused course.** 📌 **@Tanya declared the footprint instead of tidying it; there is nothing to restore.**
📌 **13 accumulating `CANCELLED` rows: not the old defect** (the count excludes them) **but they never prune** ⇒
**@Porter holds it as a DECISION for the owner, with the over-quota lock and the purchase-date ceiling.**

**BALL: @Jason — TASK-296 is the last thing between us and `uat`.**

---

## [late] Jason (BE) — ✅ TASK-296 DONE (code). Every 400 in the product now reads as a sentence. → @Sober tsc **0** · **1742 pass / 0 fail**, 138 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] `tsc --noEmit` → **0** · `bun test` → **1742 / 0**, 138 files · 🚫 no migration (**35 = 35**, counted)
- [x] 🔑 `POST /courses/:id/resume` with `startTime: ""` → **400**, `error.code === "VALIDATION"` — asserted
      **through the real router**, not against the hook
- [x] 🔑 **`error.message` carries no regex, no bracket, no field path** — asserted as an ABSENCE, and equal to
      the one sentence
- [x] 🔑 **Break it and watch** — see below; it fails *because the array came back*
- [x] ✅ **Three endpoints, in THREE routers** — one proves a hook, these prove it is uniform
- [x] **62 of 62 `zValidator` call sites covered. None escaped.**
- [x] 🚫 Status codes and `onError`'s branches untouched — 400, the code `index.ts:67` already emits

New: `src/lib/validate.ts`, `src/routes/validation-envelope.route.test.ts` (8 tests).

### §1 The shape — one module, so the fix cannot be forgotten on the next route
`src/lib/validate.ts` exports `zValidator`; **all four routers import it instead of the library**, and the
library is now imported in exactly **one place**. A route added tomorrow with the same two arguments everyone
already writes is covered **by construction**.
🔴 **This is why it is not a wrapper inside `api.ts`.** That was the obvious shape and it would have covered
**57 sites and left 5 live** — `auth.ts`, `checkin.ts` and three in `internal.ts`. **An escaped one is this same
defect, still shipping**, and the DoD says so.
🚫 **And not a hook per call site**: 62 chances to forget, on the route nobody edits.

### §2 Coverage — **62 call sites, 0 escaped**
| router | call sites |
|---|---|
| `routes/api.ts` | **57** |
| `routes/internal.ts` | **3** |
| `routes/auth.ts` | **1** |
| `routes/checkin.ts` | **1** |

✅ **No route validates by hand.** I checked for a bare `.parse()` that could throw a `ZodError` past
`ApiException` into `onError`'s 500 branch — **there is one call that looks like it and is not**:
`settings.service.ts:23` is the settings **registry's** own `parse`, which returns `null` and then throws
`badRequest` with a Thai sentence naming the setting. **It reaches `onError` correctly.** 📌 Worth saying I
checked, because that one would have been a 500 on an admin typo.

### §3 ✅ Break it and watch — and the REASON
Removing the hook returns **exactly the owner's screen**:
`"code": "invalid_format"` · `"Invalid input: expected string, received undefined"` — the serialised issue
array, with `TIME`'s rule in it. **Six assertions fail, and the message assertion fails on the array's
PRESENCE**, which is the reason I wanted rather than "something went red".
📌 The mutation was applied through a script that throws if its anchor is missing and prints `MUTATED` — the
fourth mutation-that-did-not-mutate this week was caught that way, so it is now how I run them.

### §4 🔑 The Question — what else answers without reaching `app.onError`
**Reaches it, correctly:** `authMiddleware` and `requireRole` (they **throw `ApiException`** rather than
responding) · every service throw, `settings.service` included · pg `23505` / `23503`.

**Answers directly, already in the envelope:** `internal.ts:23` (503, secret unset) and `:28` (401, wrong
secret) — hand-written but the same `{ error: { code, message } }`. ⚠️ **English messages**, which is right: the
reader is an ops script, not an admin.

🔴 **Answers directly and NOT in the envelope — three, none of them changed:**
1. **`webhooks.ts:12`** — `c.json({ error: "invalid signature" }, 401)`. **`error` is a bare STRING**, so
   `body.error.code` and `.message` are both `undefined`. Same defect shape as DEF-5; unseen because the reader
   is LINE's servers.
2. **`calendar.ts:15` and `:18`** — `c.notFound()`, Hono's plain-text 404 on the public ICS route.
3. **There is no `app.notFound` handler at all.** Every unknown path under `/api` returns plain-text
   `404 Not Found` ⇒ **a client calling `res.json()` on it gets a parse error, not an envelope.**
(And CORS answers `OPTIONS` directly, by design.)

⇒ 🔑 **Your sentence generalises exactly:** in each of the three, `onError` **exists, is correct, and is not on
the path** — and nothing looks wrong from the inside. **A handler that is not reached is worse than a missing
one.**

### §5 ⚠️ One consequence to know before the FE reads it
The library types the failure response from the hook, so **`hc<AppType>`'s 400 body type changes from the zod
issue array to our envelope** — `{ error: { code, message, details } }`, status 400. **That is the fix arriving
in the types, not a signature change**, and it is what the FE already assumes (`client.ts:65` reads
`body.error`). Nothing on the request side moved.
📌 **And a small find for TASK-295, no BE work:** the issues now in `details` already carry **admin-readable
Thai per field** (`"ต้องเป็นรูปแบบ HH:mm"` for `TIME`) — so if the form ever wants to place a server refusal
next to the field, the material is on the wire already. **Not doing it; naming it so nobody adds a second
mechanism for it later.**

**BALL: @Sober — TASK-296 ready for review.**

## [--:--] Sober (SA) — ✅ **TASK-296 DONE. No engineering blocker left on the release.** 🔑 **@Jason refused the obvious shape and that is why it is 62 and not 57.**

**Reproduced:** `tsc` **0** · **1742 pass / 0 fail** (4927 expects, 138 files) · **35 `.sql` = 35 journal tags** ·
**62 `zValidator(` sites — 57 `api.ts` + 3 `internal.ts` + 1 `auth.ts` + 1 `checkin.ts`, all importing
`../lib/validate`** · **`@hono/zod-validator` imported in exactly ONE place** · no FE change.

### 🔑 The wrapper in `api.ts` was the obvious fix and would have left five live
> *"That would have covered 57 sites and left 5 — `auth.ts`, `checkin.ts` and three in `internal.ts`. An escaped
> one is this same defect, still shipping."*

✅ **He proved uniformity ACROSS routers** — `POST /login` and `POST /checkin` are precisely the cases the
wrapper version would have missed. 🔑 **The test is shaped like the MISTAKE, not like the fix.**
✅ **One module owning the library import means the next route is covered by construction, not by memory.**
📌 **That is the difference between a fix and a rule — the distinction the whole week has turned on.**

### ✅ The check nobody asked for
He hunted a bare `.parse()` that could throw a `ZodError` past `ApiException` into `onError`'s **500** branch, and
found **the one call that looks like it and is not** — `settings.service.ts:23`, the registry's own `parse`,
which returns `null` and throws `badRequest` with a Thai sentence. ⚠️ **That one would have been a 500 on an
admin typo.**

### 🔑 One claim of his is proved by the SUITE, not by his read — and it is stronger than he claimed
His *"a valid request still reaches the handler"* is a **source-text** assertion (`hook` contains
`if (!result.success) {`). ✅ **But `people.route.test.ts:46` and `api.teacher-routing.test.ts:45/55/66` get
**200s** through these same routers** ⇒ **had the hook refused valid input, four existing tests would have gone
red.** 📌 **Told him: keep the assertion, it pins the branch shape — but know which of your guarantees comes from
running and which from reading.**

### 📋 TASK-297 cut, NO clock — his §4 answer
**Three paths answer without reaching `app.onError`:** `webhooks.ts:12` (**a bare STRING under `error`** ⇒
`.code`/`.message` both `undefined` — **the same shape as DEF-5**, unseen because the reader is LINE) ·
`calendar.ts:15/18` `c.notFound()` · **and there is no `app.notFound` at all** ⇒ an unknown `/api` path returns
plain text and `res.json()` throws.
🔑 ***"A handler that is not reached is worse than a missing one: it looks handled"* generalises exactly.**
🚫 **No admin sees any of them.** ✅ **He named them and did not touch them on deploy night.**

### 📌 Filed, deliberately not scheduled
**The zod issues now in `details` already carry admin-readable Thai per field** (`"ต้องเป็นรูปแบบ HH:mm"`) ⇒ **if
a form ever wants a server refusal beside the field, the material is on the wire.** 🚫 **Not building it. Named
so nobody adds a second mechanism for it later.**

### ⚠️ Said to @Porter, and the one thing left that we cannot do ourselves
🔴 **None of tonight's work is committed, and no agent here commits.** **Today already cost a completed task to
one `discard`.** ⇒ **whatever the deploy step is, the code reaching `uat` must exist somewhere other than a
working tree.**

**BALL: @Porter — the release is a decision now, not an engineering task.**

## [--:--] Porter (PM) — 🎉 `TASK-296` DONE. **No engineering blocker remains on this release. It is a DECISION now, not a task.**
`tsc` 0 · **1742/0** (138 files) · **no migration (35 = 35)** · **62 of 62 `zValidator` sites covered** · no FE change.
🔑 **@Jason refused the obvious wrapper** — it would have covered **57** and left **5 live** (`auth.ts`,
`checkin.ts`, three in `internal.ts`). **He moved the library import into one module** ⇒ **every router gets the
hook and a route added tomorrow is covered BY CONSTRUCTION.** ✅ **And he proved it across routers with `POST
/login` and `POST /checkin` — exactly the cases a wrapper would have missed.**
📌 **"The test is shaped like the mistake, not like the fix"** — **that sentence is the whole difference between
a fix and a fix that stays fixed.**
🔻 **Dispatched @Tanya with the TWO things that would make her file a false defect:** the message does **not**
name the field **by design** (the FORM does, beside the field), and the per-field Thai now on the wire in
`details` is **deliberately unconsumed** ⇒ **unused ≠ broken.** **Naming both up front is cheaper than a round
spent disproving them.**
⚠️ **Sober's warning, and I am carrying it to the owner as a RISK, not a request:** **none of tonight's work is
committed** and **tonight already cost a COMPLETED task to one `discard`.** ⇒ **the code reaching `uat` should
exist somewhere other than a working tree.** 🚫 **Git is the owner's alone — I state the exposure and stop
there.**
📋 **TASK-297 recorded, no clock:** three paths answer without reaching the handler (LINE webhook 401, ICS 404,
no `app.notFound`). 🚫 **No admin sees any of them.** ✅ **He named them and did not touch them on deploy night.**

## [--:--] Porter (PM) — 🟢 Owner deployed `sid` (TASK-296). @Tanya released on the regression pass.
**Framed it as a REGRESSION pass, not a feature check** — **62 call sites across every screen**, so the resume
dialog is the obvious target and the wrong one to stop at. **Told her to spread refusals across unrelated forms.**
🔻 **Named the two false-defect traps UP FRONT rather than letting her spend a round disproving them:** the
message **does not name the field by design** (the form does, beside the field), and the per-field Thai on the
wire is **deliberately unconsumed** ⇒ **unused ≠ broken.** 🔑 **Cheaper to name a trap than to receive a wrong
report and correct it — and it keeps her report about what she SAW.**

## [08:xx] Tanya (QA) — ✅ **`TASK-296` PASSES on the wire** · 🔴 **Add-parent swallows the refusal (logged separately)**

**Four unrelated forms** (resume dialog deliberately NOT used — @Porter said it is not the point).
✅ **The 400 body is exactly right:** `message` = **one Thai sentence** *"ข้อมูลที่กรอกไม่ถูกต้อง
กรุณาตรวจสอบแล้วลองใหม่อีกครั้ง"*, zod confined to `details` (deliberately unconsumed). **No zod array, no regex
reached any screen.** ⇒ **the regression did not recur.**
🔴 **NEW:** `POST /api/parents` → **400 twice, and the Add-parent dialog shows the admin NOTHING.** Not @Porter's
trap #1 (*"didn't say which field"*) — **stronger: the refusal never reaches the admin.** Logged separately;
`TASK-296` not failed for it. **`Add student` is the milder sibling — no request, no message.**
🟢 **Counter-example that proves the intended division works:** New booking → Other → charge `0` shows
`⚠ Please enter a valid amount` inline and disables `Save`.
⚠️ **Honest limit: I never saw the Thai sentence ON a screen** — the only form that reached the server is the one
that swallows it. **Verified as a CONTRACT, not as something an admin was observed to read.**
🟢 **Footprint nil:** no parent (116 before/after), no student, no booking, no course.

## [--:--] Porter (PM) — ✅ `TASK-296` PASSES on `sid` (four unrelated forms, resume dialog deliberately avoided). 🔴 **One NEW defect, and one question I will not put the release to the owner without.**
✅ **Wire confirmed:** `{"error":{"code":"VALIDATION","message":"ข้อมูลที่กรอกไม่ถูกต้อง…","details":[…]}}` ⇒
**one Thai sentence, no zod array as the message, no regex, on any of the four.** 🎯 **No raw zod reached a
screen anywhere in the pass.**

### 🔴 NEW — **Add-parent SWALLOWS the refusal**: `POST /api/parents` answered `400` TWICE, the dialog showed NOTHING
⇒ **an admin clicks `Save`, nothing happens, and concludes the app has frozen.** ⚠️ **Not my trap #1** — *"it
did not name the field"* is a form question; **"the refusal never reaches the admin" is a defect.** ✅ **She
filed it separately and did NOT fail `TASK-296` for it. Both calls correct.**
🎯 **Her booking-form counter-example is what makes the report conclusive** — **without it, "the People forms
show nothing" is an observation; with it, it PROVES the intended division works and those two forms lack it.**

### ❓ Asked @Sober and I am HOLDING the release recommendation on it
**Is the Add-parent swallow PRE-EXISTING, or did `TASK-296` change what that form receives?**
**My read: the envelope changed, that form never rendered a server error either way ⇒ tonight made it neither
better nor worse.** 🔴 **But the owner's OWN screenshot proves some dialogs DO render the server payload** (the
resume dialog showed him the raw array) ⇒ **"forms render it" is not uniform, and I cannot tell from outside
which side Add-parent was on before tonight.** 🔻 **Pre-existing ⇒ ship and fix next. Changed tonight ⇒ it is a
REGRESSION and I say that word to him.** **I will not guess this one — I have already guessed once tonight.**

### 🔑 Her declared limit, carried upward unsoftened
🔴 **She never saw the Thai sentence ON A SCREEN** — **the only form that reached the server is the one that
swallows the response.** ⇒ **verified as a CONTRACT, not as something an admin has been observed to read.**
**A "green" that quietly meant something narrower would have been mine to answer for later.**
🟢 **Footprint: nothing created — parents `116` before and after.**

## [--:--] Porter (PM) — ⛔ **The Add-parent defect is WITHDRAWN — it was never real. The owner reproduced it in one click and the message DOES appear.**
**His `sid` shot, same form, phone `12`:** a toast, top-right — **"Something went wrong"** over *"ข้อมูลที่กรอก
ไม่ถูกต้อง กรุณาตรวจสอบแล้วลองใหม่อีกครั้ง"*.
🎉 **`TASK-296` is therefore verified ON A SCREEN** ⇒ **@Tanya's declared limit — *"I never saw the Thai sentence
on a screen"* — is closed, on the exact form she believed swallowed it.** 🚫 **And my question to @Sober is
moot: nothing was swallowed, so nothing regressed. Withdrawn before anyone touched the form.**

### 🔻 The cause is capture timing — the SECOND time tonight, and the second time I failed to weigh it
**It is a transient toast in the corner; she captured after it had gone.** **Identical to her `+8s/+16s` miss on
the summary dialog — which she declared honestly BOTH times.**
🔑 **Her `400`s were real; her CONCLUSION was not.** **Told her the lesson is not "be more careful": a NEGATIVE
observation is worth less than a positive one, because ABSENCE and MISTIMING look identical** ⇒ **asked her to
state, in future, what would distinguish "it never rendered" from "I missed it".**
🔻 **But the larger share is MINE. She filed a finding. I turned it into a release blocker and HELD my
recommendation to the owner on it** — **without weighing a capture-timing risk she had demonstrated to me twice
in one night.** **That was my judgement, not her report.**

### 📌 What IS real in his shot — small, and not a blocker
**The toast header is ENGLISH — "Something went wrong" — over a Thai body.** ⚠️ **A Thai admin's first line is
in the wrong language, and it says less than the sentence beneath it.**
### ❓ Raised as a question, not a claim: **his screen reads `117 parents`; her footprint said `116` before and
after** ⇒ **something added one in between.** **Most likely his own earlier testing — but his click used an
invalid phone, so it should not have been that one.** **One look, no accusation.**
🟢 **NO engineering blocker. NO QA blocker. The release is now a decision only.**

## [--:--] Porter (PM) — 🟢 **OWNER SAID GO: `uat`.** Handed him the sequence; **code-only, no migration, no window.**
✅ **The "no migration" claim was RE-COUNTED tonight, after every change: 35 `.sql` = 35 journal tags** (Sober,
`TASK-296` verification). 🔴 **That is the 08-01 rule — *"no migration" is a claim, not a state* — and it is the
only reason I am willing to say it out loud.** **`uat` was witnessed at 35/35 earlier tonight; `sale:ensure-items`
has already run on both boxes.**
📌 **`db:verify` still runs BLOCKING before the restart even with no migration** — **08-24: `db:migrate` reported
success, exited 0, applied nothing, and took the customer's calendar down on both boxes.**
🔻 **Raised Sober's exposure ONCE and left it: tonight's code exists only in working trees, and tonight already
cost a COMPLETED task to one `discard`.** 🚫 **Git is the owner's alone — I state it and stop.** **Not repeating
it; he has heard it.**

### What ships, and what I am NOT claiming
🟢 **Course pause/resume as a RE-PLAN — his ruling, end to end**, screen-verified by @Tanya and by his own hands.
🟢 **Every validation refusal in the product reaches an admin as one Thai sentence** — 62 of 62 call sites.
🚫 **NOT claimed:** the English toast header · the 13 accumulating `CANCELLED` rows · the over-quota lock · the
purchase-date ceiling · `resume/preview` · the `ON LEAVE` row that kept its old time (**@Sober still owes me
that ruling**). ⇒ **all recorded, none fixed, and he hears them as open.**

## [--:--] Porter (PM) — 🎉 **`uat` IS LIVE.** The release the owner stayed up for is out. @Tanya on a **READ-ONLY** confirmation pass.
**Scoped her `uat` pass to three READS** — the control is present · the resume dialog's `Time` prefills to the
course's own time (`Cancel`, never submit) · pause-cancelled rows hidden while hand-cancelled rows stay visible.
⛔ **Spelled out the part that is easy to rationalise: a submission you EXPECT TO FAIL is still a write against a
live customer box.** ⇒ **explicitly forbidden, and `NEEDS DATA REQUEST` is the correct answer, not a failure.**
🔻 **Carried forward exactly ONE standing change from this release, and it is about evidence, not process:**
**if a screen shows nothing, say what would distinguish "it never rendered" from "I missed it".** **Two of
tonight's wrong conclusions — the summary dialog and the Add-parent swallow — were the same mistake, and the
owner caught the second one with a single screenshot.**

## [--:--] Porter (PM) — 🎉 **LINE inbound SOLVED by the owner — and the cause was OURS.** 🔻 **My parked diagnosis was wrong on the most important word in it.**
**He changed `.env` on `uat` and the value did not change.** Logs showed the process still holding the **demo**
channel secret + token: **`pm2`'s `ecosystem.cjs` had them HARD-CODED**, so `.env` was never read. ✅ **Fixed to
read from `env` always.**
🔻 **I wrote: *"It is a setting on their console, not a defect in our build."*** **Wrong. All four of my
candidates were on their side; none was it.** ✅ **Corrected in `SYSTEM-FACTS` in place, with the wrong sentence
left visible above it** — **a parked diagnosis that gets quietly rewritten teaches nothing.**
🔑 **Why it mimicked a console problem so exactly:** inbound verifies with the **channel secret** ⇒ **with the
demo secret loaded, every signed request from the customer's OA failed verification and was DROPPED SILENTLY** —
no error, no log line — **and the failure appeared at the moment they switched OA, which is what anchored me to
their side.** 📌 **The timeline was real evidence and it pointed the wrong way.**

### 🔴 Four consequences the fix does NOT undo — routed to @Sober, item 1 first
1. **OUTBOUND on `uat` rode the DEMO token** ⇒ **the customer's parents received NOTHING from `uat`, and the
   demo OA may hold real messages about real students.**
2. 🧊 **PENDING DEPLOY item 5 was frozen on a FALSE premise** (*"the server now points at the CUSTOMER'S OA"*).
3. **The six rich menus went to whichever OA the TOKEN named** — the 09-05 phone check was on the demo OA ⇒
   **the customer's OA may have NO menus.**
4. **`family_line_links` rows from `uat` carry DEMO-scoped userIds** ⇒ **may match nothing on their OA.**
📌 **Third instance this week of ONE class: a value in TWO places where one silently wins** — the two Thai
sentences, the two live-status lists, and now `.env` vs `ecosystem.cjs`. **Everyone could read the `.env`;
everyone was reading the wrong file.** ⇒ **asked for a READ of what else is hard-coded there.**

## [--:--] Porter (PM) — ⛔ **All four LINE "consequences" closed by the owner in two messages. I over-escalated, and one of them was my own instruction coming back at me.**
**Item 3 — *"the customer's OA may have no rich menus"* — is TRUE and is the INTENDED STATE.** **The owner
removed them deliberately, eight hours earlier, using the tool the team built, because the customer asked for
them off until the messages were ready.** 🔻 **I RELAYED that instruction. Then I raised its outcome as a
finding.** **Items 1 and 4 are true and inert** — **inbound was dead, so nobody was ever linked on the
customer's OA** ⇒ **no parent was waiting, nothing can collide. Item 2 is a document correction.**
🔻 **Named the mistake rather than just withdrawing it: I derived four consequences from one true mechanism and
checked NONE of them against what we had already done on purpose.** ⇒ **a consequence chain is a HYPOTHESIS
list; I handed it over as a FINDINGS list.** **Told @Sober to bin it and apologised for the hour.**
📌 **Corrected in `SYSTEM-FACTS` beneath the escalation, not over it** — **the escalation is on the record, so
its answer has to be too.**
🟢 **LINE inbound on the customer's OA WORKS** — `สมัคร` → the bilingual entry with `ผู้ปกครอง · ครู · แอดมิน`.
**Topic closed. The only thing I still recommend is a READ of what else `ecosystem.cjs` hard-codes.**

## [--:--] Porter (PM) — 📋 Owner: *"อยู่ในลิสต์นะ จดไว้ เดี๋ยวจะส่งงานชุดวันนี้ให้ทำให้หมด"* → **opened `OWNER'S NEXT BATCH` on the board.**
**Seven items, plus four DECISIONS that are his alone.** 🔑 **Wrote the holding rule INTO the section, not just
into my head: nothing routes to @Sober until he sends the batch as one** — **a half-sent batch is how build
order gets set by accident, and build order is not mine.**
📌 **On the board rather than in today's log, deliberately** — **a log entry dies with the date; he said "the
list", so it needs a home that survives a session reset.**
🔻 **Item 1 (`TASK-284`) carries its own history: a KNOWN limitation from `TASK-269 §2`, deliberately not
fixed — and HIS reproduction overrides that decision.** **Recorded that way so nobody re-argues the old call.**
❓ **Item 2 is still owed to me by @Sober** (the `ON LEAVE` row keeping its old time). **Listing it does not
retire the question.**

## [--:--] Porter (PM) — 🔴 @Tanya could not confirm `uat`: **`NEEDS DATA REQUEST` × 3, and the blocker is a PM item I have been sitting on.**
🔑 **`REQ-080` — narrow the guard so QA can READ `uat` — is `READY_FOR_SA` and NEVER SHIPPED.** `mint-session.mjs`
carries `PRODUCTION_HOSTS = ["frontoffice.develyst.online"]` and refuses `uat` **by design**; the access file has
**no `uat` entry**. ⇒ **the gap that stopped tonight's confirmation is one I have known about and not closed.**
**Added to the owner's batch as item 7.**
🟢 **She did not work around it** — no hand-made cookie, no second route, no password. **Stop #2, taken
correctly.** 🎯 **And her anonymous GETs were worth the trip: both `uat` hosts serve, `302 → login` on a page,
`401` on the API ⇒ `uat` does not leak to an anonymous caller — re-checked ON `uat`, not inferred from `sid`.**
🔑 **She applied the new standing rule BEFORE being asked, and better than I framed it:** **she reported NOTHING
about `uat`'s screens — not "absent", not "blank"** ⇒ **no negative observation in the pass can be mistaken for
evidence.** **That is a rule being USED, not obeyed.**
📌 **Cheapest close is the owner's own minute** — open a paused course's plan modal on `uat` and read three
things. **Same division that has worked all week on LINE: he is the hands, she writes the verdict.**

## [--:--] Porter (PM) — 📸 Owner ran the `uat` confirmation himself in three shots. **CHECK 1 ✅ · CHECK 2 ✅ · CHECK 3 splits — half passes, half has no fixture on `uat`.**
**1:** `อาร์ตี้` ACTIVE — the full control row is present (`Pause course` back), and the button's **`(6)`**
matches **6 `PENDING`** ⇒ **the count fix confirmed on `uat`, unasked.**
**2:** `เรย์ยัน` PAUSED — **both fields now carry the required `*`** (the missing asterisk was half of DEF-5),
**`Time` prefilled to `15:00` = the course's OWN slot** (the row behind reads `24/Aug 15:00`), **no seconds.**
🔑 **`15:00` appears nowhere in @Tanya's `sid` round (10:00 / 14:00 / 17:00)** ⇒ **new evidence, not a repeat.**
**3:** `ซอส ภวตล` PAUSED, 10-session, **showing TWO rows** ⇒ **pause-cancelled rows ARE hidden on `uat`.**
🔴 **The other half — hand-cancelled STILL VISIBLE — has no fixture: the owner checked all five paused courses
and none holds a hand-cancelled row.** ✅ **`NOT_TESTED (no fixture)` accepted; nobody manufactures one, because
that is a WRITE on the customer's box.** 🚫 **Her `sid` proof of that half is NOT counted as `uat` evidence.**
🟢 **No write was made on `uat` at any point — the resume dialog was opened and cancelled.**
📌 **Verdict requested from @Tanya in her own words.** 🔻 **I read screenshots wrong twice today; the ruling is
not mine to write.**

## [10:xx] Tanya (QA) — 🏁 **RELEASE VERDICT written. `uat` and `sid` kept in separate columns.**

**`uat` (owner's three screenshots, he was the hands):** ✅ **CHECK 1 PASS** — control present, and the count
agrees on a real course (`(6)` vs 6 visible `PENDING`). ✅ **CHECK 2 PASS** — `Time * = 15:00` matching that
course's own `15:00` row, no seconds, both fields starred. 🔑 **`15:00` appears nowhere in my `sid` round, so it
cannot be a coincidence of my fixtures.** ⚪ **`readOnly` NOT confirmed on `uat`** — a screenshot cannot carry a
DOM property; proven on `sid` only, and said so rather than letting the PASS imply it.
✅ **CHECK 3 HALF A PASS** — a 10-session course showing two rows ⇒ pause-cancelled hidden, with `2 OWED` visible.
🔴 **CHECK 3 HALF B `NOT_TESTED` on `uat`** — no course there holds a hand-cancelled row; **nobody manufactures
one, that would be a write on the customer's box.**
✅ **HALF B PASS on `sid`** (`b7dc8ace`), observed twice: paused plan showed **one row, the hand-cancelled
`22/Sep 10:00`**, every pause-cancelled row hidden. **Labelled `sid`, never `uat`.**
⚠️ **Tried to re-capture it fresh and could not — session authenticates but the app bounces to `/login`.**
**Harness, not product; stopped rather than take a fourth run.**
🔑 **The line I put in front of the owner: what `uat` never showed us is that a hand-cancelled session survives a
pause — and the reason is a property of their DATA, not of the product.**

## [--:--] Porter (PM) — 🏁 **RELEASE VERDICT (@Tanya), two columns kept separate. The release is CONFIRMED on `uat` with ONE named gap.**
| Check | `uat` | `sid` |
|---|---|---|
| 1 control present | ✅ **PASS** | ✅ PASS |
| 2 resume `Time` prefilled | ✅ **PASS** *(`readOnly` not observable in a screenshot)* | ✅ PASS |
| 3A pause-cancelled hidden | ✅ **PASS** | ✅ PASS |
| 3B hand-cancelled still visible | 🔴 **`NOT_TESTED` — no fixture on `uat`** | ✅ PASS (`b7dc8ace`) |
🔑 **Her sentence, and it is the one that goes to the owner:** **what `uat` never showed us is that a
hand-cancelled session SURVIVES a pause** — **and the reason is a property of their DATA (no course there has
one), not of the product.**
🎯 **Two refusals to over-claim, both hers, both unprompted:** **`readOnly` is a DOM property a screenshot cannot
carry** ⇒ **proven on `sid` only, said out loud rather than left to be implied by the PASS.** And **`15:00`
appears NOWHERE in her `sid` round** (10:00 / 14:00 / 17:00) ⇒ **a prefill landing on a fourth, course-specific
value cannot be a coincidence of her own fixtures** — **that is why the owner's shot is EVIDENCE, not a repeat.**
⚠️ **Her session bounced to `/login` tonight despite authenticating** ⇒ **she ruled 3B proven on the two PRIOR
observations with the fixture id, and STOPPED rather than take a fourth run.** ✅ **Exactly the standing rule,
applied to herself.**
📌 **My `10-session / 8 rows` niggle: `NOT_TESTED`** — **she refused to guess from a screenshot, which is the
mistake we both made today.** 🔻 **I raised it; she declined to launder it into a finding. Correct.**
🟢 **NOTHING was written on `uat` at any point in this release confirmation.**

## [--:--] Porter (PM) — 📋 **The owner sent his batch. Written up as `REQ-085` (six items) and routed to @Sober as ONE.**
🚫 **Sent as one batch, not six messages** — **a half-sent batch sets build order by accident, and build order is
@Sober's.** **Kept HIS listing order and said explicitly it is not a sequence.**
🔑 **Quoted his Thai verbatim under every item.** 🔻 **And marked the TWO places I interpreted as `📖 MY READING`
rather than folding them into his words:** **§4 "one language"** (I read it as REQ-079 §18's notification form —
**if he means Thai-only, that changes §18 and I re-open it**) and **§5** (I read it as the parent path no longer
advertising other roles; **whether the role choice disappears for everyone is unsettled**). **Both asked back to
him.** ⇒ **an interpretation presented as a requirement is how the wrong thing gets built, and I have been wrong
twice today.**
📌 **Two items already had homes and I said so instead of duplicating them:** **§3(a) IS `TASK-284`** (a known
`TASK-269 §2` limitation **his finding now overrules**) and **§3(b) is REQ-079 §18 being APPLIED**, not a new
rule. 🔑 **§4 likewise contradicts a ruling we already made** — **naming that is cheaper than discovering it
mid-build.**
🔴 **Raised one MECHANICAL question with §1:** **does a creation-time advance leave still earn its make-up?**
**It must, or the family loses lessons they paid for.**
🔑 **And named the pattern out loud: this is the FOURTH time this week that "leave" has sat at the edge of a
rule** — the refuted `SICK_LEAVE` claim · the over-quota lock · the `ON LEAVE` row keeping its old time (**still
owed to me by @Sober, and visible again in the owner's `uat` shots**) · and now §1. ⇒ **asked whether leave
needs ONE definition rather than a ruling per feature.**

## [--:--] Porter (PM) — 📋 §4 and §5 SETTLED by the owner — **both of my readings were wrong** — ➕ and he asked for the thing that matters more than the whole batch: **`REQ-086`, the customer edits the messages themselves.**
🔻 **§4: ENGLISH ONLY, not one Thai character** — **my `§18` reading (English labels, Thai values) is
OVERRULED.** **His screenshot is the spec:** `Date : อังคาร` → `Tuesday`, `Advance Leave Notice : ไม่มี` → `(-)`.
🔑 **Both are the same two strings §3 already asks for** ⇒ **§3 and §4 are ONE fix seen from two sides**, and
**"not one Thai character" is a PROPERTY — it can only be held by something that FAILS when Thai appears.**
🔻 **§5: option (ก)** — **nobody is shown the role list; teachers type `ครูเอง`.** ⇒ **a security-shaped change
made with COPY: today we hand every parent the door AND the key.** ⚠️ **The admin phrase and the parent-certainty
wording are MINE and I owe them.**

### ➕ `REQ-086` — and his reason is the strongest argument anyone has made this week
> *"เพราะสองอย่างนี้เราทำไม่ถูกใจลูกค้าสักที อาจจะผิดที่ฉัน และพวกนายที่อ่อนเรื่องความจำด้วย"*
🔻 **He diagnosed US, correctly, and TONIGHT is the evidence:** `Date : อังคาร` was ruled English in `§18` and
**shipped Thai anyway** · **my own pause copy outlived its ruling by a day** · two Thai sentences with opposite
meanings share one string · three comments described a mechanism deleted the same night.
🔑 **The fix for "the agents forget the wording" is NOT better agents — it is to stop routing wording through
agents at all.** ⇒ **it removes a CLASS of our failures rather than an instance, and I told @Sober I rate it
above everything else on the list.**
🚫 **Four things I refused in advance, in the REQ** — a raw `{{placeholder}}` box · **no PREVIEW** (🔑 *"this
product commits then shows" is our own recorded pattern; a message editor without preview is that mistake made
permanent and handed to the customer*) · editable message IDENTITY (**when a message fires is OURS; only the
WORDS are theirs**) · any path where an edit reaches real parents unseen.
❓ **Four questions are MINE before it is buildable** — who edits · one language or two · how to return to the
shipped default · what happens mid-flight. **Told @Sober NOT to shape it until I answer them.**

## [--:--] Porter (PM) — ✅ `REQ-086` unblocked: the owner answered Q1, I answered Q2–Q4 rather than handing him four questions.
**Q1 (his):** *"คนที่ log in frontoffice ได้ ตอนนี้มีคนเดียวคือ admin"* ⇒ **frontoffice, behind the EXISTING
admin login. No new role.** 🔑 **"Whoever can log in" survives them adding a second account — it describes the
DOOR, not a person.**
**Q2 (mine, DERIVED not chosen):** notifications **English-only** (🔗 `REQ-085 §4`) · command messages
**bilingual** (🔗 `REQ-079 §18`). ⇒ **two rulings we already have, applied — not a preference.**
**Q3 (mine):** **every message keeps its shipped default; an edit is marked; RESET is ONE action.** 🔑 **They
will edit while unhappy and discover the result in front of a parent** ⇒ **undo must work for someone
embarrassed and in a hurry.** 🚫 **"Retype the original" is not a way back — nobody remembers it, which is this
REQ's entire premise.**
**Q4 (mine):** **the template is read AT SEND TIME.** A sent message lives in someone's LINE and cannot be
changed. 🚫 **No versioning, no "effective from".**
🔻 **Three of four were mine to answer and I answered them** — **his rule: we are the answer, not a relay of
questions.** **Only Q1 was genuinely his, because it is about who may act, and that is never ours.**

## [--:--] Porter (PM) — ⏸️ Owner is taking §5's copy to the CUSTOMER. **Proposal recorded in `REQ-085` and explicitly marked DO-NOT-BUILD.**
**His instruction: *"รอ จดลงในโปรเจค ฉันจะส่งให้ลูกค้า ส่งมาให้ทำตามเลย"*** ⇒ **the customer's wording wins and
replaces mine verbatim.**
🔑 **Wrote the REASON for each line into the proposal, not just the line** — **so the customer can argue with the
reasoning rather than only the words**, and so a rewrite does not silently drop the mechanism: **line 2 is §5's
whole trick — we stop asking *"who are you?"* and state WHO THIS IS FOR** ⇒ **a parent recognises themselves and
the words "ครู"/"แอดมิน" never appear.** **§5 is satisfied by SAYING something, not by hiding something.**
📌 **Applied the `REQ-079 §17b` precedent deliberately: the customer's own words kept verbatim, our analysis
separated from them.** 🔻 **That section exists because I once claimed a file held the customer's source text
when it held my analysis, and @Jason found out by trying to use it.**
🟢 **Told @Sober the MECHANISM is buildable now and only the STRINGS are pending** ⇒ **the batch is not blocked
by this.**

## [--:--] Porter (PM) — 📜 The customer's registration copy arrived. **Recorded VERBATIM as `REQ-079 §17c`; my analysis separated into `§17d`.**
🚫 **My `REQ-085 §5` proposal is WITHDRAWN** — exactly as I recorded it would be. **The owner's `ครูเอง` and my
`แอดมินเอง` are both gone: the customer chose plain `ครู` · `แอดมิน` · `CEO`.**
📌 **Kept their text unparaphrased and put my reading in a separate section** — **that discipline exists because
I once claimed a section held the customer's source text when it held my analysis, and @Jason found out by
trying to use it.**
✅ **Several open things closed themselves:** the entry keyword is **`สมัคร`/`register`, NOT `Next`** (`Next` is
screen 2 and means PARENT) · **DOB `DD-MM-YYYY` Gregorian** · **address is THREE parts** (District, Sub-district,
Province) — **not a free address line, which is a data-shape decision hiding in a sentence.**

### 🔴 FOUR for the owner, none of them a wording preference
1. **`ผู้บริหาร / CEO` is a FOURTH ROLE.** **He told me on 09-06 that CEO means backoffice sales/profit
   reporting** ⇒ **this copy has a CEO REGISTER THROUGH LINE. A capability, not a string.**
2. 🔴 **The role words are ordinary Thai nouns — a parent CAN type `ครู`.** **`REQ-085 §5` exists precisely so a
   parent cannot reach another role by guessing** ⇒ **a SECURITY decision wearing the clothes of copy.**
   🔑 **This is why he wrote `ครูเอง`.** **Not mine to overrule; his to decide knowing the trade.**
3. **Screen 2's heading *"เลือกบทบาท / Select Your Role"*** — **on screen, or a label in their document?**
   **On screen it tells every parent roles exist, which is the thing §5 removes.**
4. **Screen 4 prints a real-looking phone number** ⇒ **must echo what was typed.**
🔻 **I am NOT letting the customer's authority over WORDS quietly decide MECHANISM.** **They own the sentences;
whether a CEO can register through LINE, and whether a role is guessable, are not sentences.**

## [--:--] Porter (PM) — ✅ All four §17d questions CLOSED. **CEO skipped · role words stand · phone echoes · and he handed the heading question BACK to me to rule.**
🚫 **CEO SKIPPED** — the word stays in the customer's verbatim copy and becomes no code path.
✅ **Role words stand (`ครู` · `แอดมิน`).** 🔻 **He took the trade KNOWINGLY once I named it** ⇒ **`REQ-085 §5`
is satisfied by not ADVERTISING roles, not by making them unguessable.** 📌 **Recorded as a DECISION so nobody
re-opens it as a defect** — **that distinction is the whole point of writing it down.**
✅ **§17f — my ruling, because he said *"นายคิดว่าไงล่ะ คิดด้วยสิ"*:** **none of the eight headings is sent.**
**Their own document is the evidence: every screen has one, numbered, each merely NAMING the message below.**
🔑 **Reading heading 2 as on-screen forces all eight to be — and heading 1 would greet someone who has not
started, above a line already telling them what to do.** 🔴 **Independently: *"Select Your Role"* above a
ONE-option message tells a parent both that roles exist AND that they were not offered a choice.**

### 🔻 And I corrected myself before it reached a build: the ADDRESS is ONE FREE-TEXT FIELD
**I reported it as THREE parts and called it "a data-shape decision hiding in a sentence."** **It is a PROMPT
listing guidance; the answer was always free text.** ⇒ **NO CHANGE, nothing to build.**
⚠️ **Second time TODAY I turned a sentence into a structure it never claimed** — the other was the `HH:mm:ss`
prefill. 🔑 **Logged as a PATTERN, not an instance: I am reading mechanism into prose, and both times it was the
owner who stopped it.** **The tell is the same both times — the reading FIT, and I never checked it.**
⏳ **Notification messages still to come. This was the COMMAND set only.**

## [--:--] Porter (PM) — 📜 The customer's FOUR notification formats recorded VERBATIM (`REQ-085 §7`), analysis separated into `§8`. **They supersede `REQ-077`'s shapes.**
### 🔴 The two traps, both named before anyone builds
1. **TWO OPPOSITE EMPTY-FIELD RULES ONE LINE APART:** **`Advance Leave Notice` ALWAYS prints (`-` when empty)**
   · **`Remark` does NOT print when empty** (*"\*ถ้ามี"*). ⇒ **each needs its own assertion; one "empty fields"
   test cannot catch a swap.** 📌 **And the rules are principled: a missing leave notice must be something the
   parent can trust we CHECKED; an absent Remark is just an admin with nothing to say.**
2. 🔴 **"ENGLISH ONLY" MEANS LABELS.** ⚠️ **Read literally, `§4` would romanise `น้องดีซี` and translate the
   admin's own `Remark`.** **Their own examples keep both Thai.** ✅ **Labels and SYSTEM values are English
   (`Tuesday`, `-`); content a HUMAN typed is reproduced as typed.** 🔑 **Translating a Remark is putting words
   in an admin's mouth.** 🔻 **I would have shipped `§4` too literally — the customer's examples corrected my
   own routing, not the team's build.**

### 📌 Three corrections to what I had already routed
**`§4` is NARROWER than I said** — *"Auto โอเคแล้วค่ะ"* ⇒ **the daily AUTO is already right and only gains
`Remark`; "one language" applies to the COMMAND version only.** · **AUTO and COMMAND are different shapes BY
DESIGN — do not unify them.** · **7.3 is a full REPLACEMENT** (today's is Thai-labelled) **and splits
`เวลา:` into `Date : Tuesday` + `Time :`** — **`Date` names a WEEKDAY, same as 7.1.**
🟢 **7.4 answers `§2` and the part §2 never specified — WHO:** *"เฉพาะแชทครู / แอดมิน"* ⇒ **teacher and admin
chats, NOT the parent, who is the one who declared the leave.**
❓ **Three put to the owner, not guessed:** the Thai `แจ้งลา` header among four English formats · whether
`Remaining`/`*Expiry date` are CONDITIONAL on being a course · **whether 7.4 should carry `Coach` and `Remark`**
— 🔑 **a teacher reading a leave notice has just learned a slot is free; the Remark may be the thing that says
whether anything was being prepared.**

## [--:--] Porter (PM) — ✅ Last three questions closed. **`REQ-085` is COMPLETE — nothing in the batch waits on the owner or the customer.**
1. **`LEAVE NOTICE` in English** ⇒ all four formats English-labelled.
2. **`Remaining` / `*Expiry date` are CONDITIONAL on a COURSE booking.** 🔑 **His one-line reason IS the
   acceptance criterion** — *"ใช่ ไม่งั้นมันจะแยกยังไง"* ⇒ **those lines are what distinguishes a course row
   from a one-off row.** **So the test asserts ABSENCE on a non-course row** — **a test that only checks the
   positive case passes while the distinction is broken.**
3. **The leave notice gains `Coach` and `Remark`.** 🔑 **I had to re-explain `Coach` before he could rule on
   it, and the second explanation was the one that worked: it is not for the TEACHER — they get it in their own
   chat — it is for the ADMIN, who covers every coach and would otherwise receive three identical-looking
   messages for three different teachers on one day.** 🔻 **My first version bundled `Coach` and `Remark`
   together and gave no scenario. He said *"ไม่เข้าใจ"*, which was accurate feedback on my writing.**
📌 **Flagged to @Sober that the leave notice now carries ONLY the `*ถ้ามี` rule and must not inherit the `-`
rule** — **the two-opposite-rules trap, one message later.**

## [--:--] Porter (PM) — 🟢 **Owner released the batch. Everything is with @Sober; build order, effort and migrations are his.**
**Released as one:** `REQ-085` (§1–§6 + the four notification formats + his rulings) · `REQ-079 §17c–f` (the
customer's 8-screen registration copy) · `REQ-086` (unblocked) · plus the board's 8 smaller items.
🔴 **Led with the four things I would not want discovered mid-build**, not with the scope: the two opposite
empty-field rules one line apart · **"English only" means LABELS, not names or the admin's `Remark`** ·
**`Remaining`/`*Expiry date` are what DISTINGUISH a course row, so assert their ABSENCE on a non-course row** ·
**`Date` names a WEEKDAY.**
🔻 **And listed MY OWN corrections explicitly, telling him not to build from my earlier messages:** the address
is ONE free-text field (I reported three) · `§4`'s "one language" is the COMMAND message only · `ครูเอง` /
`แอดมินเอง` withdrawn · CEO skipped. 🔑 **My earlier messages are still sitting in his inbox — a correction that
only lives in the newest message is not a correction.**
📌 **Marked the role-word choice as a DECISION the owner took knowingly** ⇒ **so nobody re-opens it as a defect
in a month.**
❓ **Re-asked the one thing still owed to me: the `ON LEAVE` row keeping its old time.** 🔑 **`REQ-085 §1` makes
it the FIFTH time this week that "leave" has sat at the edge of a rule** — **the question of whether leave needs
ONE definition is now better evidenced than when I first asked it.**

## [--:--] Sober (SA) — 🔑 **The leave ruling: leave does NOT need one definition — it needs one CONSEQUENCE TABLE.** 🔴 **And `REQ-085 §1` is already built.**

### 1️⃣ The ruling @Porter was owed, and it is not the one he asked for
**Five rulings on "leave" in a week looks like a missing definition. I read every use before answering. It is
not.** `SICK_LEAVE` **is** one status, used consistently. **What keeps needing a ruling is not what leave IS but
what it COSTS** — and there are **six independent prices**: quota (`scheduler.service:2328/:2751`) · make-up
(`course-plan:111`) · slot blocking (`booking-slot:6`) · expiry (`course-expiry-impact:31`) · freelance pay
(`freelance-budget:33`) · notification (`line-message`).

🔑 **The proof they are independent is the owner's own reversal, recorded in the code:**
`freelance-budget.ts:34` — *"SICK_LEAVE also RELEASES — owner reversal 2026-08-03, overturning the 2026-07-20
rule."* ⇒ **he moved ONE price and left the other five alone, deliberately.**
⚠️ **A single definition would have made that reversal inexpressible** — new status, or change all six.
⇒ ***"Leave" is not one concept with five leaks. It is one STATE with six PRICES, and he sets each separately.***
📌 **What was actually missing is the TABLE.** **Six files cannot be read at once, so every ruling re-derived
the previous five — which is exactly what this week felt like.** → **`SYSTEM-FACTS`, as one block.**

✅ **And the specific ruling owed: the `ON LEAVE` row keeping its OLD time through a re-plan is CORRECT.**
🔑 **A re-plan moves what is OWED, not what has HAPPENED.** **The leave already happened; its MAKE-UP is what is
owed, and that is what moves.** ⇒ **rewriting the leave row's time would be rewriting history to make a schedule
look tidy.** ✅ **True by construction — `COURSE_LIVE` excludes `SICK_LEAVE`. No task.**

### 2️⃣ 🔴 `REQ-085 §1` is ALREADY BUILT — checked before estimating it
**Both halves are in the product:** `plannedAtCreation` (`REQ-045` B, TASK-148, migration `0019`) makes a
creation-time leave **free of quota**, guarded in two paths; **there is no cap** — `validation.ts:276-279`
refuses only a week beyond size and *every* week absent, and the FE picker caps nothing.
✅ **@Porter's mechanical question answered YES already:** `course-plan.ts:111` appends a make-up for **any**
unmatched `SICK_LEAVE` and **never reads the flag.**
❓ **So I asked instead of building: what did he SEE that said it was limited?** ⚠️ **Suspicion, not a plan:**
`CreatePlanFlow.tsx:77` shows `LEAVE_QUOTA_BY_SIZE` on the size picker ⇒ **a quota stated on the one screen
where it does not apply.** 🔑 **A requirement that is already implemented is the strongest signal that the
report is about something else.**

### 3️⃣ 📋 Build order — and the counter-intuitive part
**The instinct is to build `REQ-086` (the editor) FIRST so we stop hand-editing sentences.** 🔴 **Wrong:
`REQ-086`'s shipped DEFAULTS are `§7`'s four formats** ⇒ **building §7 first is not work we redo, it is the
editor's seed data.** **An editor over today's wrong copy just lets the customer find our defects faster.**
⚠️ **One condition decides whether §7 costs us twice: each format gets exactly ONE definition site, shaped to
become a ROW later.** 🚫 **Four formats scattered through the composers = `REQ-086` starts by hunting them** —
**this week's drift class, pre-installed.**

**Order:** `7.1` (with TASK-284) → `7.3` (a REPLACEMENT, Thai-labelled today) → `7.2 COMMAND` (**AUTO is NOT in
scope** — *"Format Auto โอเคแล้ว"*) → `7.4 LEAVE NOTICE` (the only NEW message) → `§5`/`§6` registration →
`REQ-086` (**the batch's only migration**). **`§1` blocked on the question.**

### ⚠️ Three traps, each going into the DoD of the task that meets it
1. 🔴 **Two OPPOSITE empty-field rules one line apart** — `Advance Leave Notice` prints `-`; `Remark` does not
   print. **Separate assertions.** 🔑 **`9.1`'s leave notice carries only the second** — **the task will state
   its own rule, because a task quoting both is a task that can swap them.**
2. 🔴 **`Remaining` / `*Expiry date` asserted ABSENT on a non-course row** — his reason IS the acceptance:
   *"ไม่งั้นมันจะแยกยังไง"*. **A positive-only test passes while the distinction is broken.**
3. **"English" = labels and SYSTEM values only** — 🚫 never a student's name, never the admin's `Remark`.
   🔑 **`Date` names a WEEKDAY.**

📌 **`REQ-086` gets a design pass, not a task, and AFTER 1–4 exist — because those four ARE its specification.**
*"Make these editable"* is a far better brief than *"design a message editor"*.

**BALL: @Porter — the one `§1` question. Task-cutting is mine and starts now.**

## [--:--] Porter (PM) — 🔑 Sober answered the leave question by REFUTING it, and the refutation is better than the answer I asked for.
**I asked whether "leave" needs ONE definition.** ✅ **His answer: NO — it already IS one definition
(`SICK_LEAVE`, one status, used consistently).** 🔑 **What keeps needing a ruling is not what leave IS but what
it COSTS, and there are SIX independent consequences:** quota · make-up · slot blocking · expiry · freelance pay
· notification.
🔴 **And his proof is the OWNER'S OWN past decision:** `freelance-budget.ts:34` records a **2026-08-03 reversal**
that moved ONE consequence (leave now RELEASES the held hour) **and left the other five untouched, deliberately.**
⇒ 🔑 ***"Leave is not one concept with five leaks; it is one STATE with six PRICES, and the owner sets each price
separately."*** **A single definition would have made that reversal impossible to express.**
📌 **The real gap is that the six live in six files** ⇒ **the sixth ruling costs a re-derivation of the first
five, which is exactly what this week felt like.** ✅ **He is writing the table into `SYSTEM-FACTS` as one block
with the file for each.** 🔻 **I had the symptom right and the diagnosis wrong — and he checked before answering
instead of agreeing with me.**
✅ **My owed ruling, answered: the `ON LEAVE` row KEEPING its old time is CORRECT.** 🔑 **A re-plan moves what is
OWED, not what has HAPPENED. A leave is a happened fact; what moves is its MAKE-UP.** ⇒ **rewriting the leave
row's time would be rewriting history to make a schedule look tidy.** 🚫 **No task.**

## [--:--] Porter (PM) — 🔴 **`REQ-085 §1` IS ALREADY BUILT. Sober checked before estimating, and I am NOT dispatching it.**
**Both halves are in the product:** *"does not touch the quota"* = `plannedAtCreation` (**`REQ-045`, the owner's
own decision B, TASK-148, migration `0019`**), guarded in two places · *"unlimited"* = **there is no cap**, in
the validator or the picker. ✅ **And my mechanical question is answered YES already** — `course-plan.ts:111`
appends a make-up for any unmatched `SICK_LEAVE` and **never reads the flag.**
❓ **So the ONE question to the owner is his, not ours: WHAT DID HE SEE that said it was limited?**
⚠️ **Sober's suspicion, explicitly NOT built on:** the creation screen displays the leave allowance
(`CreatePlanFlow.tsx:77`) ⇒ **he may be reading a DISPLAY that states a quota, on the one screen where the quota
does not apply.** 📌 **If so, §1 is a one-line display fix, not a rule change.**
🔑 **This is the day's best save: a requirement that is already satisfied would have produced a build, a deploy,
a QA round and no change** — **and only "I checked before estimating" caught it.**

## [--:--] Porter (PM) — ✅ `§1` closed by the owner's screenshot. **Sober's suspicion was WRONG, and so was his "already built" — but the reason each was wrong is worth more than either.**
**Repro on `sid`: a 4-session course, header `Leave 0/1`, THREE sessions marked `ON LEAVE`.** ⇒
> ⚠️ *"This course can only extend to week 5 — reduce the planned absences or pick a different start date."*
🔴 **`Create plan` DISABLED.** **He literally cannot proceed. The limit he reported is real.**
🔑 **The screenshot cuts BOTH ways and settles both halves:** ✅ **Sober was right that the QUOTA does not block
at creation** — **`Leave 0/1` in the header while he set THREE, with no quota complaint** ⇒ `plannedAtCreation`
works. 🔴 **And the owner was right that he is limited** — **by the OTHER half of the same validator: the WEEK
CEILING (size + 1 ⇒ week 5 for a 4-session course). Three absences push the last make-up to week 7.**
🔻 **Neither of us could have got there alone: I could not see the mechanism, Sober could not see the screen.**
**"What did you SEE" was the right question, and it beat both of our theories.**
📌 **So `§1` is NOT already built and NOT a display fix.** **Routed to @Sober with the shape, not a ruling:**
**"unlimited at creation" cannot hold while a ceiling refuses the resulting plan** ⇒ **either the ceiling does
not apply to creation-time absences, or it stretches.**
🔗 **Named the precedent WITHOUT treating it as the decision: the owner already ruled *"วันหมดอายุก็งอกไปสิ
เรื่องปกติ"* for RESUME, and `TASK-282` derives expiry from the last planned session.** 🔑 **A precedent for
creation, not a decision about it.**
⚠️ **And it is the SAME ceiling as the open item on his list — *measured from the PURCHASE start date*.**
⇒ **two symptoms, one mechanism, worth answering once.**

## [--:--] Porter (PM) — ✅ `§1` RULED: **"เพดานยืดตามไปด้วย"** — the week ceiling stretches to cover the plan. (`REQ-085 §10`)
🚫 **The admin is never asked to reduce absences or move the start date** ⇒ **"unlimited leave at creation"
becomes true in PRACTICE, not only in the quota.**
🔗 **Third instance of ONE principle, and naming it is the point:** the RESUME ruling (*"วันหมดอายุก็งอกไปสิ"*) ·
`TASK-282` deriving expiry from the last planned session · now the creation ceiling.
🔑 ***The plan decides the dates; the dates do not veto the plan.*** **Written into the REQ so the fourth
instance does not need a fourth ruling.**
📖 **Carried the ruling to the OTHER symptom too — the ceiling measured from the PURCHASE start date — and
marked that as MY reading, with one line able to overturn it.** 🔑 **He ruled on the MECHANISM, and the two
symptoms share it.** 🔻 **But it is still my extension of his words, so it is labelled, not folded in.**
❓ **Asked @Sober the question that is NOT the owner's: if the ceiling always stretches, what does it still
REFUSE?** **Planned absences are finite so the stretch is bounded — but the bound is now the PLAN, not a rule.**
🔴 **Either name what it refuses, or say it refuses nothing and is a derived value** — **a rule surviving only
as an unreachable branch is worse than a deleted one, which `EXPIRY_REQUIRED` taught us this week.**

## [--:--] Porter (PM) — ✅ The owner answered the question I had routed to @Sober: **the ceiling's limit is a SCOPE, not a value.** ➕ **New requirement: the expiry date must be admin-editable.**
**AT CREATION: no quota, ceiling stretches. AFTER CREATION: both apply, and an admin may override.**
✅ **So nothing becomes an unreachable branch** — **the `EXPIRY_REQUIRED` failure does not repeat.**
🔑 **The principle, and I wrote it down rather than just the rule:** **before the course exists there is nothing
to protect — these rules protect an AGREED plan from drifting, not a plan being drawn.** **His override case is
the same idea: *"ลูกค้าไม่ได้บอกให้พัก"* — a family who did not ask for a pause must not be pushed into one
because a rule ran out.**
📌 **Noted a `LOCKED` card with `Unlock (admin)` already visible in his screenshot** ⇒ **asked @Sober whether the
override IS that control or a second thing.** 🚫 **Did not assume they are the same.**

### 🔴 And I flagged a conflict that is ONE NIGHT OLD
**`TASK-282` made the expiry DERIVED to cover the last planned session — that is what KILLED DEF-4.**
⇒ **an admin-set expiry can re-open DEF-4 by landing BEFORE the course's own last session.**
📖 **My reading, marked as mine: LATER than derived, freely; EARLIER than the last planned session, only with
the system SAYING what it cuts off.** 🔑 **DEF-4 was exactly that defect and it reached his hands.**
🔻 **A new requirement that quietly undoes a fix from the same week is the easiest thing on this list to miss** —
**and I would rather be overturned in one line than have it discovered by a family losing a lesson.**

## [--:--] Sober (SA) — ✅ **Answered @Porter's two "I will not assume" questions FROM THE REPO.** 🔴 **`§11.2` is the SECOND item in this batch that is already built.**

### ❓ Q2 — *"is the calendar icon a control or a label?"* ⇒ **it is a control, and has been since TASK-265**
`PATCH /courses/:id/expiry` (`scheduler.service.ts:3688`) · `EditExpiryDialog.tsx`, **wired at
`CoursePackagePanel.tsx:336`** · `ExpiryWarningAlert` already renders **WHICH** sessions fall outside.
✅ **`REQ-082` AC-1 + AC-4.** ⇒ **the owner's *"ควรแก้ได้"* describes something that exists.**
✅ **And @Porter's DoD — *"NAME the sessions, a count is not enough"* — is already met:** `contract.ts:241`
says *"AC-4 asks for the list, not a count"* and the alert renders the list.

### 🔴 The real gap is ONE WORD, and it is his own ruling
`EditExpiryDialog`'s comment: *"the warning only exists after the save… this asks, saves, and then shows what
happened."* **`§11.3` requires it BEFORE.** ⇒ **the mechanism is right; the TIMING is wrong.**
🔑 **That is `SYSTEM-FACTS`' *"this product commits and then shows"* — the exact pattern @Porter has just
forbidden for `REQ-086`'s editor.** ⇒ **he made the same call twice tonight, in two files, from two directions,
and his `§11.3` ruling is the fix for a LIVE instance of the thing he was banning in the abstract.**
✅ **Cheap because the foresight is paid for:** `expiryImpact` is **pure and takes its sessions**, written so the
RESUME could ask about rows that do not exist yet ⇒ **a preview is a read-only route, not a feature.**
⇒ **TASK-298 → @Jason. No clock, no migration.** ⚠️ **Its load-bearing DoD line is *"it WRITES NOTHING"** —
a preview sitting one letter from a PATCH that legitimately does.

### ❓ Q1 — *"is the override the `Unlock (admin)` control, or a second thing?"* ⇒ **there are already TWO**
1. **`adminUnlocked`** — real column, `updateCourse`, `useSetCourseAdminUnlock`, the buttons on the card he
   screenshotted ⇒ **unlocks THE COURSE, standing.**
2. 🔑 **A per-change `override: boolean`** — `validation.ts:316`, enforced at `scheduler.service.ts:2319` ⇒
   **one absence pushed past the lock, the lock still standing.**
⇒ **"May the admin override?" already has two different yeses, and they are not interchangeable.**
❓ **Routed back as a precise question: does his *"ลูกค้าไม่ได้บอกให้พัก"* case want the COURSE unlocked, or ONE
absence let through?** 🔑 **The difference is whether the NEXT leave is also free.** 🚫 **Not guessing that.**

### 📌 The pattern worth keeping — two of eight items were already implemented
**`§1` (advance leave) and `§11.2` (expiry editing) are both built.** 🔑 **That is not the owner being wrong.**
**What he can see is a SCREEN; what changed is a RULE.** ⇒ **when he reports something that already exists, the
report is about how it LOOKS — which is still a real defect, just not the one the words describe.**
⚠️ **And it is the strongest argument yet for checking the code before estimating: I would have scheduled two
builds tonight for features this product already has.**

**BALL: @Porter — two precise questions, neither needing a screenshot.**

## [--:--] Porter (PM) — ⛔ `§11.1` WITHDRAWN. **The "admin override" was my misreading of one verb, and I asked him to choose between two implementations of a feature he never requested.**
> *"ไม่เกี่ยวกับโควตาลา หมายถึงแอดมินสามารถเลื่อนวันหมดอายุคอร์สได้เว้ย เพื่อที่อาจจะใส่วัน extra เพิ่มได้"*
**His *"ยืดให้พิเศษ"* meant MOVE THE EXPIRY DATE, to fit an extra session in.** ⇒ **it IS `§11.2` — which already
exists.** 🚫 **Told @Sober to drop the question and cut nothing for `adminUnlocked` or the per-change override.**
🔻 **The tell I walked straight past: he had ALREADY ruled the ceiling stretches (`§10`).** **A second ceiling
override on top of that is redundant** ⇒ 🔑 **redundancy in a requirement means I have MISREAD it, not that the
customer wants belt and braces.** **That is a usable test and I did not apply it.**
📌 **THIRD time today I read mechanism into prose** — the `HH:mm:ss` prefill · the address as three fields · now
this. **Logged as the pattern, not the instance.** 🔑 **All three shared one shape: my reading FIT, and fitting
is not evidence.**
✅ **Kept @Sober's research rather than discarding it with the question** — **`adminUnlocked` vs the per-change
`override` is now a `SYSTEM-FACTS` fact instead of a pending decision**, and the next person to ask *"can an
admin override the lock?"* has the answer without a grep.
✅ **`§11` final: ONE piece of real work in the whole section — `TASK-298`, warn before saving, no migration.**

## [--:--] Sober (SA) — 🎯 **ONE line explains `§10`, the purchase-date item AND why `§11.2` is inert.** 🔻 **And it narrows my own "already built" from an hour ago.**

`course-plan.ts:86` — **`exceedsExtensionCeiling` re-derives the ceiling from the PURCHASE DATE every time and
never reads the stored `expiryDate`** — 🔑 **the column `course-plan.ts:45` itself calls *"only the MAX_WEEK
ceiling"*. The code does not read the column that says it is the answer.**

| face | what a human sees |
|---|---|
| **`§10`** | `Create plan` DISABLED at week 5 with three planned absences — the owner's own repro |
| **the purchase-date item** | a resumed course refuses a make-up: `:3849` **correctly** keeps `startDate` as the PURCHASE date, and the ceiling measures from it |
| 🔴 **`§11.2`** | **an admin moves the expiry and NOTHING CHANGES** — `:2199` never reads that column |

### 🔻 The correction I owe on my own answer
**I told @Porter `§11.2` was already built and to schedule nothing. It IS built — and the owner's REASON is
*"เลื่อนวันหมดอายุ… เพื่อที่อาจจะใส่วัน extra เพิ่มได้"*: he moves the expiry so an extra session FITS.**
🔴 **It does not fit. The rule ignores the date he moved.** ⇒ **"already built" was true about the MECHANISM and
false about the PURPOSE.**
🔑 **@Porter wrote the lesson for this an hour before I needed it — *"already built must never be the end of a
triage; it is the middle"* — about `§1`. I then walked into the same shape on `§11.2`.**
⇒ **The question is not *"does this exist?"* but *"does it do the thing they want it for?"*** — **and the second
is answered by their stated REASON, which is why his verbatim quoting is what made this findable.**

### ⚠️ `§10` must not be built as "skip the check"
`:2022` stores `expiryDate: courseExpiry(startDate, size)`, **computed independently of the plan** ⇒ **delete the
`:2027` gate and a 4-session course with 3 absences is created with its plan at week 7 and its ceiling at week 5**
⇒ **the first post-creation leave is refused at once, and the stored ceiling sits BEHIND the course's own last
session — DEF-4's exact shape, re-created at creation time.**
✅ **`§10` is *the plan sets the ceiling*.**

### ✅ @Porter's question answered — **what still bounds a course?**
🔑 **The ceiling refuses AUTOMATIC growth and yields to a DELIBERATE act.** A leave-driven auto-extend still
cannot pass the agreed boundary (`:2199` keeps its job — **that assertion is to be written FIRST**); an admin
edit, or a plan being drawn, sets that boundary on purpose.
⇒ 🚫 **`exceedsExtensionCeiling` does NOT become an unreachable branch.** **Only its SOURCE changes.** 📌 **His
`EXPIRY_REQUIRED` worry was the right question and the answer is clean.**
✅ **His reading carrying `§10` to BOTH symptoms is RATIFIED, with one amendment: the rule is not *"the ceiling
stretches"* but *"the ceiling is measured from the course's own agreed plan, not its purchase date"*.**
🔑 **Same effect — and it explains why `§11.2` was inert, which "stretches" does not.**

⇒ **TASK-299 → @Jason.** 🚫 no migration, no FE change, no clock.
✅ **`§11.1` withdrawn by @Porter before anyone built** — he named his own tell: *"he had already ruled the
ceiling stretches, and redundancy in a requirement means I have misread it."* 📌 **The two overrides are now a
`SYSTEM-FACTS` fact rather than a pending decision.**

**BALL: @Jason.**

---

## [late] Jason (BE) — ✅ TASK-299 DONE (code). One line, three faces. → @Sober tsc **0** · **1754 pass / 0 fail**, 139 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] `tsc --noEmit` → **0** · `bun test` → **1754 / 0**, 139 files · 🚫 no migration (**35 = 35**, counted)
- [x] 🔑 **The owner's repro: a 4-session course with THREE declared absences** — asserted on **both** halves,
      because either alone is the bug: it creates, **and its stored expiry covers its own last session** (week 7)
- [x] 🔑 **A leave marked AFTER creation is still refused past the agreed ceiling** — ⚠️ **written first**, as
      you asked, and it is the first describe in the file
- [x] **An admin who moves the expiry later can then fit the extra session** — asserted
- [x] **A resumed course's make-up is not refused by a purchase-date ceiling** — asserted
- [x] 🔑 **Break it and watch** — twice; restored and green before this number was written
- [x] 🚫 The derived expiry, `courseExpiry`, quota, `maxWeek` and the card untouched — asserted

New: `src/lib/extension-ceiling.test.ts` (12 tests).

### §1 The change — only the SOURCE
`exceedsExtensionCeiling(date, ceiling)` now takes the course's **stored `expiryDate`**. It is still called from
both places; **nothing was skipped and no branch became unreachable.** `courseExpiry`, `maxWeekFor`,
`MAX_WEEK_BY_SIZE`, the quota and the card are byte-identical.
🔑 **Your rule, in the code's own words:** *the ceiling refuses AUTOMATIC growth and yields to a DELIBERATE
act.* The first describe in the test file is that sentence made executable — a post-creation leave is still
refused past the boundary, and a date landing exactly on it is still allowed.

### §2 ✅ §10 is *the plan sets the ceiling* — and your warning was the whole design
`courseBornCeiling(base, lastPlanned, absences)` = `max(base, lastPlanned + absences weeks)`, computed **before
the insert**, from `plannedSessions` — **the same array that is then inserted**, not a second projection.
🔴 **Deleting the `:2027` gate would have been worse than the bug.** A 4-session course with three absences
would have been created with its plan at week 7 and its stored ceiling at week 5 — **the row sitting behind its
own last session, and the first post-creation leave refused.** That is DEF-4's shape at creation time, and it
would have looked like a success.
🚫 **The stretch is one-directional.** A short plan keeps the full MAX_WEEK window: the family bought a leave
window, and shrinking it to fit a plan would take back something nobody agreed to give up.
✅ **The PREVIEW computes the boundary through the same function**, so `exceedsCeiling` and the create's refusal
cannot disagree — which would have been this task's own defect, one screen earlier.

### §3 ✅ Break it and watch — the owner's repro, and the REASON
Removing the stretch returns the ceiling as **`2026-09-29` (week 5)** while the plan runs to **`2026-10-13`
(week 7)** — **the two-week gap that disabled his `Create plan`**, reproduced exactly. A second mutation
(storing `courseExpiry` again on the row) fails the wiring assertion, which is the one that would catch the
rule being right and the read being wrong.
📌 Both applied through a script that throws if its anchor is missing and prints `MUTATED`; restored and the
**whole suite re-run green before I wrote the number above**, as the DoD asks.

### §4 🔑 The Question — where else a value is STORED **and** re-derived
| stored | re-derived | why they agree *today* |
|---|---|---|
| `coursePackages.usedSessions` | the delivered count `courseCurrent` sees | the attendance write moves both |
| `coursePackages.leaveUsed` | the count of `SICK_LEAVE` rows | ⚠️ **already designed to disagree** — a `plannedAtCreation` leave deliberately does not increment it (TASK-148) |
| `coursePackages.expiryDate` | `deriveLiveEndDate(sessions)` | two different questions (TASK-097), and the plan returns both side by side |
| `coursePackages.weekday` / `startTime` | `weekdayOf(booking.date)` (`:2971`) | ⚠️ TASK-282 had to write the stored pair back on a re-plan **because they drift** |
| `coursePackages.expiryDate` | `courseExpiry(startDate, size)` | 🔴 **this task** |

⚠️ **Changed none of them.** 📌 But two of the five are guarded **only by a sentence in a comment** —
`getEntitlementPlan`'s *"do not read `usedSessions` as the plan's progress"* is prose, not an assertion. **This
week has shown twice what a sentence is worth: yours about `COURSE_PAUSE_NOTE` went stale inside a day, and
`scheduler.service.ts:15` outlived its mechanism.**

🔑 **And a sharper form of your rule, offered because the list suggests it:** the useful question is not
*stored or derived* — it is ***"is there an act that can move one without the other?"*** Every pair here was
fine until exactly such an act appeared: a plan with absences, a re-plan, an import with `priorSessions`, a free
declared leave. ⇒ **two things that agree today are two things that can disagree later — and the act that will
split them is usually already in the backlog.**

**BALL: @Sober — TASK-299 ready for review. TASK-297 and TASK-298 still open on me, neither blocking.**

## [--:--] Sober (SA) — ✅ **TASK-299 PASSED, verified by me.** 🔑 **@Jason's sharper form of my own rule replaces mine.** ❓ **And I found one edge in review — asked, not filed.**

**Reproduced:** `tsc` **0** · **1754 pass / 0 fail** (4956 expects, 139 files) · **35 = 35** ·
`exceedsExtensionCeiling(date, ceiling)` at both sites · `:2230` reads **`course.expiryDate`** · `:2054` preview
and `:1676` creation both go through **the same `courseBornCeiling`** · `courseExpiry`, `maxWeekFor`,
`MAX_WEEK_BY_SIZE` untouched.

### 🔑 The rule, improved by the person implementing it
> *"The useful question is not stored or derived — it is whether there is an ACT that can move one without the
> other. Every pair was fine until exactly such an act appeared… and the act that will split them is usually
> already in the backlog."*

✅ **Better than my *"two things that agree today can disagree later"*, because it is ANSWERABLE.** **Mine names
a risk; his names WHERE TO LOOK.** ⇒ **`SYSTEM-FACTS`, in his words, with his five-pair table.**
⚠️ **Two of the five are guarded only by PROSE** — `getEntitlementPlan`'s *"do not read `usedSessions` as the
plan's progress"* is a comment, not an assertion. 🔑 **He is right that this week proved what a sentence is
worth, twice, and one of them was mine going stale inside a day.** 🚫 **No work cut for it tonight — recorded
with the table, so the next person meets a LIST rather than a hunt.**

### ✅ Three calls that were choices, not defaults
- 🔑 **The stretch is ONE-DIRECTIONAL** — *"a course whose plan ends early still owes the family the leave window
  they bought."* **Nothing in my task said that** ⇒ **he found the case where the symmetric fix silently takes
  something back.**
- ✅ **The preview uses the SAME function as the create** — *"which would have been this task's own defect, one
  screen earlier."*
- ✅ **Break-and-watch twice, restored, whole suite green BEFORE the number was written.** 📌 **Third report in a
  row stating that sequence. It is a habit now, and it started as a failure of mine.**

### ❓ The edge I found reviewing — asked of him, deliberately not filed
**`courseBornCeiling` stretches by `absences × 7` days — the IDEAL weekly cadence. The make-up's real date comes
from `findFreeExtensionDate`, a slot SEARCH.** ⇒ **on a busy calendar the search may outrun the ceiling: three
absences ideally land weeks 5–7, but a taken slot pushes the last to week 8 — one day past a week-7 ceiling —
and creation throws `EXTENSION_CEILING`.** 🔴 **The owner's original symptom, in a different guise, only when the
calendar is full.**
⚠️ **Not called a defect, and no task cut. Three reasons his answer comes first:**
1. **It may be unreachable** — the slot is that teacher's own weekly slot. **He knows that code; I am reading it.**
2. 🟢 **Not a regression** — before this, the ceiling was week 5 and it failed for every course with TWO absences.
   **He narrowed the hole; the question is whether the edge is still open.**
3. 🔑 **It is HIS rule turned on HIS fix:** **the ceiling comes from a cadence assumption, the dates from a
   search** — **two things that agree today, and the act that splits them is a busy teacher.**
📌 **Asking before filing, because a task built on my misreading of his code costs him more than a question does.**

**BALL: @Jason — one line: reachable, or not? TASK-297 and TASK-298 also his, neither blocking.**

## [late] Jason (BE) — 🔴 **Answer: REACHABLE — and checking it found something larger.** → @Sober

**Your question:** can `findFreeExtensionDate`'s search outrun a ceiling stretched by `absences * 7`?

### ✅ Short answer: **yes, and only a ONE-OFF booking can do it.**
`firstFreeWeeklySlot` walks a week at a time and skips an occupied slot. **Occupied** =
`teacherId + date + startTime`, status **not in `SLOT_INACTIVE_STATUSES`**.
🔑 **A recurring clash cannot cause this**, and that is the interesting half: if the teacher had a weekly
booking at that slot, the course's own weeks 1..`size` would have hit `SLOT_TAKEN` in `insertBooking` and the
create would already be refused. ⇒ **weeks 1..`size` are free by construction; the stretch window is not.**
⇒ **One stray `SINGLE_SESSION` / `FIRST_TRIAL` / `อื่นๆ` on that teacher's slot inside the stretch window** and
the last make-up steps a week past the born ceiling ⇒ `EXTENSION_CEILING` on a create that should succeed.
📌 **Not a regression** — you had that right: before the change the ceiling was week 5 and every 4-session
course with two absences failed. **The hole is narrower, and its edge is still open.**

### 🔴 But the reason it is HARD to reach is itself a defect, and it is bigger
**The PREVIEW and the SAVE place creation-time make-ups in different weeks.**
| | anchor | where the three make-ups land |
|---|---|---|
| **preview** (`previewCoursePackage`) | `sessions[sessions.length - 1]` — **the last PLANNED week** | weeks 5, 6, 7 |
| **save** (`reconcileCoursePlan`) | `liveAfterCancel` max — **the last LIVE week** | weeks **2, 3, 4** |

**A `SICK_LEAVE` row is in `SLOT_INACTIVE_STATUSES`, so a declared-absent week reads as a FREE slot.**
⇒ on a 4-session course with absences in weeks 2–4, the last live week is **1**, and the search fills
**weeks 2, 3 and 4 — the very dates the family said they would be away.** The make-up for the week-2 absence is
booked on week 2, at the same teacher and time, mirrored from the absence itself.

⚠️ **This is what actually disabled his `Create plan`:** the preview computed `exceedsCeiling` over weeks 5–7
against a week-5 ceiling, **while the save would never have gone past week 4.** The two disagreed, and the one
the admin sees is the one that refused.
🔑 **They agree only when every absence falls BEFORE the last live week.** The owner's arrangement is the case
where they disagree most.

### What this means for my fix, honestly
✅ **TASK-299 is still right and still needed** — the ceiling must come from the stored column, and the born
ceiling must cover the plan. **Both mutations still fail for their stated reasons.**
⚠️ **But `courseBornCeiling`'s `absences * 7` is calibrated to the PREVIEW's placement, not the save's.** It is
generous for the save (which lands earlier) and exact for the preview — so it closes the owner's symptom either
way, **and it is a cadence assumption sitting in front of a search, exactly as you said.**
🚫 **I did not fix either finding** — you cut tasks, not me. 📌 **My view of the order, for what it is worth:
the make-up landing ON the declared absence is the one I would want in front of the owner first** — it is not
an edge case, it fires on every course whose last declared absence follows its last live week, and the family
gets a lesson booked on a day they told us they would be away.

**Ball: @Sober.** ⛔ TASK-297 and TASK-298 still open on me, neither blocking.

---

## [late] Jason (BE) — ✅ TASK-297 DONE (code). → @Sober tsc **0** · **1762 pass / 0 fail**, 140 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] `tsc --noEmit` → **0** · `bun test` → **1762 / 0**, 140 files · 🚫 no migration (**35 = 35**, counted)
- [x] **An unknown path under `/api` returns the envelope** — asserted, **and `res.json()` on it parses**
- [x] **(1) changed and asserted · (2) LEFT, and named with its reason** — see below
- [x] 🔑 **Break it and watch** — twice; restored, whole suite green before this number was written
- [x] 🚫 Status codes byte-identical (401 stays 401, 404 stays 404) · `onError` and TASK-296's hook untouched

New: `src/routes/envelope-reachability.route.test.ts` (8 tests).

### (3) ✅ `app.notFound` — the one worth fixing
An unknown path returned Hono's plain-text `404 Not Found`, so **`res.json()` threw a parse error instead of
handing back a refusal**. The failure read as a broken server rather than a wrong URL.
🔑 **The mechanism is worth stating: a 404 is not a thrown error**, so `onError` was never a fallback for it.
The two are siblings, not a chain — which is why nothing looked wrong from the inside.
📌 English and short: the reader is a developer typo or a stale client, never an admin.

### (1) ✅ the webhook — an object where a string was
`{ error: "invalid signature" }` put a **string** where every other refusal puts an object, so
`body.error.code` and `.message` were both `undefined`. Now
`{ error: { code: "INVALID_SIGNATURE", message: "invalid signature" } }`.
🚫 **The signature check and the 401 are untouched** — only the shape of the answer moved.
🔻 `webhooks.test.ts:34` asserted the bare string. **Corrected with the reason, not deleted:** the property that
test protects is *which* refusal answers — the signature check rather than the JWT guard — and that is unchanged.

### (2) ⚪ the ICS route — **LEFT as plain text, and this is the deliberate answer**
Its reader is a calendar client, which reads the **status** and never our envelope; a JSON body is noise it has
to ignore.
⚠️ **But I had to touch the line to keep the behaviour**, and that is worth being exact about: **`c.notFound()`
dispatches to the APP's handler**, and (3) just gave the app one — so leaving `c.notFound()` there would have
**silently switched this route to the envelope**, as a side effect of a fix aimed elsewhere.
⇒ The route now writes its own `c.text("404 Not Found", 404)`. **Byte-identical output, changed only to
preserve it** — the opposite of changing it for symmetry, and it is now pinned against the next app-level edit.

### ✅ Break it and watch — twice, and each for its own reason
- Restoring the bare-string webhook: **the sweep names the offender by file and content** —
  `src/routes/webhooks.ts :: c.json({ error: "invalid signature" }, 401);`
- Removing `app.notFound`: the parse assertion fails with **`SyntaxError: Failed to parse JSON`** — *the defect
  itself*, not a proxy for it.

### 🔑 The Question — **partly. Here is the part that can be asked, and the part that cannot.**
✅ **Built, and proven by mutation A:** a sweep over the routers asserting that **every `c.json({ error: … })`
carries both a `code` and a `message`.** It reads source rather than walking routes, because a route no test
requests still ships. That is the `35 = 35` shape, and it would have caught the webhook the day it was written.

⚠️ **What it cannot see, stated in the test itself so nobody trusts it too far:**
a Response built in a helper and returned · `new Response(...)` · `c.text` / `c.body` with an error status
(**the ICS route is deliberately one of those**) · and 🔴 **a library answering before our code runs at all —
which is exactly what DEF-5 was.** No source sweep would have found TASK-296; that needed a route-level
assertion that actually made the request.

⇒ **The honest answer: the SHAPE question can be asked mechanically; the REACHABILITY question still needs an
eye.** 📌 The two are different questions, and conflating them is how a sweep that looks complete lets the next
one through — **which is worse than not having one, exactly as you said.**

**BALL: @Sober — TASK-297 ready for review. ⛔ TASK-298 still open on me, not blocking.**

## [--:--] Sober (SA) — ✅ **TASK-297 PASSED.** 🔴 **And @Jason's answer to my narrow question is a FAMILY-FACING defect: the make-up is booked ON the absent day. TASK-300 cut.**

**TASK-297 reproduced:** `tsc` **0** · **1762 pass / 0 fail** (4973 expects, 140 files) · **35 = 35** ·
`index.ts`, `calendar.ts`, `webhooks.ts` + its test modified, nothing else.

### 🔑 The best thing in his report is the route he "left alone" and had to CHANGE to do it
> *"`c.notFound()` dispatches to the APP's handler, and (3) just gave the app one — so leaving the line there
> would have SILENTLY switched this route to the envelope, as a side effect of a fix aimed elsewhere."*

**My task said *leave the ICS route its own answer*. Keeping the behaviour required editing the line** — and he
said so in those terms instead of letting a byte-identical diff pass as untouched.
🔑 **An app-level fix silently changing a route that opted out is this week's class exactly, and this one would
have been invisible:** no test, no error, a calendar client quietly ignoring JSON.
✅ **And *"a 404 is not a thrown error, so `onError` was never a fallback — they are siblings, not a chain"*** is
why nothing looked wrong from inside.
✅ **`webhooks.test.ts:34` corrected WITH its reason** — *"the property it protects is WHICH refusal answers, and
that is unchanged."* **That is how a test is allowed to change.**
✅ **His Question answer beats a yes:** **the SHAPE question can be swept mechanically; REACHABILITY needs an
eye** — and **he wrote the sweep's blind spots into the test itself**, including *"no source sweep would have
found TASK-296"*. 📌 **We said a fake mechanism is worse than none; he shipped a real one and labelled its edges.**

### 🔴 The bigger half — I verified both anchors before cutting anything
`:2027` **preview** anchors on the last **PLANNED** session · `:2216` **save** anchors on `liveAfterCancel`
(`COURSE_LIVE`, which **excludes `SICK_LEAVE`**) ⇒ **the absent weeks are invisible to the anchor and the search
starts at week 1**; and `SICK_LEAVE` **is** in `SLOT_INACTIVE_STATUSES` ⇒ **those weeks read FREE** ⇒ **filled.**
🔴 **On a 4-session course with absences in weeks 2–4, the three make-ups land in weeks 2, 3 and 4** — **the very
weeks the family said they were away**, same teacher, same time. **Nothing warns.**
✅ **And it is the ONLY theory that explains the owner's screen:** **the preview refused over weeks 5–7 against a
week-5 ceiling while the save would never have passed week 4** ⇒ **the two disagreed and he was shown the
stricter one.** ⇒ **third symptom tonight of one cause: preview and save answering one question from different
inputs.**
⚠️ **`SICK_LEAVE` freeing the slot is CORRECT (UC-004) and must not change** — `migration-witness.ts:77` says
reversing it *"must never be attempted"*. **The bug is that the course reused ITS OWN absent week.**

### 📌 What I did NOT do
🚫 **No task for the ceiling-vs-slot-search edge** — 🔑 **`courseBornCeiling`'s `absences × 7` is calibrated to
the PREVIEW's placement, and TASK-300 is about to move where make-ups land.** ⇒ **calibrating against a moving
target is a task that has to be redone.** **It comes after, if it survives.**
🚫 **No query, no data touched.** ✅ **I asked @Jason only for the SHAPE of a fingerprint** — *an `EXTENDED` row
whose date equals a `SICK_LEAVE` row's date on the same course* — **so that IF it is column-identifiable, @Porter
can tell the owner what to look for.** ⇒ **routed the decision to @Porter: does the owner hear about possible
past bookings now, or after the fix?** 📌 **My view given, not taken: now, in one sentence.**

### ✅ Credit, and it is not mine
**He found this while answering a NARROW question about a busy-calendar edge in his OWN fix, said *"I did not fix
either finding — you cut tasks, not me"*, and told me which of the two he would put in front of the owner
first.** **He was right, and that is the one I cut.** 🔑 ***My question was small and his answer was not*** — **and
neither of us finds this if the DoD only says "green".**

**BALL: @Porter — the one call about telling the owner. TASK-300 and TASK-298 sit with @Jason.**

## [--:--] Porter (PM) — 🔴 **@Jason found a FAMILY-FACING defect while reviewing our own fix: a declared absence's make-up is booked ON the absent day.** ✅ **My call: tell the owner NOW.**
**Two correct behaviours combining into a wrong one:** **the create places make-ups after the last still-LIVE
session, and a declared absence is not "live"** ⇒ **the absent weeks are invisible, so it starts from week 1** ·
**and a declared absence FREES the teacher's slot** (correct — someone else may book it) ⇒ **those weeks read as
available and get filled.** 🔴 **The family is given a lesson on a day they told us they could not attend, and
nothing warns.**
✅ **TOLD HIM IMMEDIATELY.** 🔑 **The deciding fact is not the defect's size — it is WHO finds it otherwise: a
parent standing at an empty studio, or a coach.** ⇒ **withholding does not buy a calmer conversation, it buys a
worse one, later, with a family in it.** 📌 **And tonight is the evidence — he found DEF-2 himself because I
scoped @Tanya away from it. I am not repeating that shape on something family-facing.**
🚫 **No agent runs a query.** ✅ **@Sober asked @Jason for the FINGERPRINT SHAPE only** — *an `EXTENDED` row whose
date equals a `SICK_LEAVE` row's date on the same course* ⇒ **the owner can act on his own data without any of
us touching it.** **Asked to be told if it is NOT identifiable from columns — "we cannot tell which ones" is a
worse answer than a number and he still needs to hear it.**
🔑 **It also EXPLAINS the owner's `§10` screen:** **the PREVIEW placed those make-ups in weeks 5–7 and refused
against a week-5 ceiling; the SAVE would never have gone past week 4.** ⇒ **the screen that prompted his ruling
was lying.** 🔻 **The ruling STANDS — he decided what he wants — but I told @Sober not to treat `§10` as
satisfied by the fix.**
📌 **Third symptom of ONE cause tonight: the preview and the save answering the same question from different
inputs.** **Same illness as "this product commits and then shows" — the SCREEN and the ACT are not the same
thing.**
🎯 **@Jason answered a narrow question, found something larger, fixed neither** (*"you cut tasks, not me"*)
**and ranked which the owner should hear first. He ranked correctly.**

## [--:--] Porter (PM) — 🔴 **`TASK-300` DID NOT REPRODUCE. The owner ran it himself and the make-up landed correctly — AFTER the plan, not on the absent day.**
**One advance leave, created and SAVED: `15/Sep ON LEAVE` · three `PENDING` · `13/Oct EXTENDED`.** **The saved
plan is IDENTICAL to the preview** ⇒ **both of @Sober's claims are contradicted here: the make-up is NOT on the
absent day, and the preview and save do NOT disagree.**
🚫 **I am NOT calling it false.** **His case is ONE absence in week 1; the report was THREE in weeks 2–4.**
⇒ **different scenarios, and his result does not disprove theirs.** 🚫 **And I will not ask him to test
combinations — that is our job.**
🔻 **But I raised a FAMILY-FACING alarm to the owner before it had a reproduction he could see.** **He asked for
the way to check, checked, and it did not happen.** ⇒ **I have WITHDRAWN the fingerprint rather than leave him
hunting a pattern his own evidence contradicts.**
🔑 **Told @Sober plainly: reproduce it or retract it, and if the boundary is "three absences, not one", THAT
BOUNDARY IS THE FINDING.** **"It happens with three and not with one" is a defect I can describe to him.**
📌 **And I named the pattern back at him: `§1` was called "already built" from source, and the SCREEN refused
the owner.** ⇒ **twice today, reading the code has lost to reading the screen.**
⚠️ **The cost is real and it is mine: the next alarm I raise with him costs more because of this one.**

## [--:--] Porter (PM) — 🔴 **`TASK-300` NOT REPRODUCED at three absences either. The owner ran @Sober's exact scenario and the behaviour is CORRECT.**
**`มิลล่า`, 4-session, THREE absences, SAVED:** `15/22/29 Sep ON LEAVE` · `06 Oct PENDING` ·
`13/20/27 Oct EXTENDED` · **Ends 27 Oct.** 🔴 **All three make-ups land AFTER the plan. None on an absent day.**
⇒ **contradicted at BOTH one absence and three, on SAVED data, on screen.** **Treating it as NOT REPRODUCED.**
🔑 **Told @Sober that "I verified at source" is no longer acceptable on its own, and that if the defect needs a
condition neither of us named — a busy calendar, a teacher already booked, a mid-week start — THAT CONDITION IS
THE FINDING and the report should have led with it.**
❓ **Raised ONE thing as a question, not a theory: this plan saved and ends WEEK 7, while his earlier screenshot
refused a three-absence plan against a WEEK-5 ceiling with `Create plan` disabled.** **Different course, and I
do not know what changed** — **asked whether `TASK-299` reached `sid`.** 🚫 **I did not guess. I have read
mechanism into evidence three times today and every time it fitted and was wrong.**

### 🔻 A standing change to what I will relay — mine, not the team's
**I escalated a FAMILY-FACING alarm on a source-read, and the screen has now contradicted a source-read TWICE
tonight** (`§1`'s "already built", and this). ⇒ 📌 **Anything I carry to the owner as family-facing now needs a
reproduction HE CAN CLICK — otherwise it reaches him as "we think, and here is why we are not sure".**
🔑 **The asymmetry that justifies it: a false alarm costs his trust and his time; a real one caught late costs a
family a lesson.** **Both are real, so the answer is not "escalate less" — it is "escalate with something he
can press".**

## [--:--] Porter (PM) — 🟢 **`§10` VERIFIED on `sid` by the owner** (4-session course, THREE advance leaves, quota untouched, ends week 7). 🔴 **New defect: the stretched ceiling does not SURVIVE creation.**
**He then tried to take a normal quota leave and got:** `คอร์สขยายเกินสัปดาห์ที่ 5 ไม่ได้` — **on a course whose
own plan already ends 27 Oct, week 7.** ⇒ **the after-creation path measures against `size + 1`, the ORIGINAL
ceiling, while the course legitimately lives past it.**
🔑 **What makes it worse than a bad message: the card says `Leave 0/1` — one leave available — and the course
CANNOT USE IT.** ⇒ **`§10` gave the admin unlimited absences at creation and, as a side effect, took away the
one the family had afterwards.** **A feature that quietly cancels a family's remaining right is not a display
bug.**
📌 **Same class as `TASK-299`** (*"the ceiling is RE-DERIVED from the purchase date, so the admin's expiry does
nothing"*) ⇒ **the ceiling is re-derived rather than CARRIED: computed and used at creation, recomputed from
size afterwards, stretch gone.** 🔑 **I flagged "same ceiling, two symptoms" before `§10` was even ruled. This
is the third.** 🚫 **Did not prescribe where it should live — @Sober's.**
🔻 **This also retires my own open question about the earlier week-5 refusal: the innocent explanation is that
the stretch was not deployed yet when he hit it.** ⇒ **the "preview and save disagree" story is no longer needed
to explain his screens** — **and `TASK-300` still has NO reproduction at one or three absences.**

## [--:--] Sober (SA) — 🔴 **The owner found TASK-299's cost within an hour: a stretched course cannot use the quota it is shown.** 🔻 **And TASK-300 is UNPROVEN — he ran the mirror image of my scenario.**

### 🟢 First, the good half — `§10` is VERIFIED ON HIS SCREEN
**`มิลล่า`, 4-session, `Leave 0/1`, three advance leaves** ⇒ `15/22/29 Sep ON LEAVE` · `06 Oct PENDING` ·
`13/20/27 Oct EXTENDED` · `Ends 27 Oct`. ✅ **Unlimited at creation, quota untouched, ceiling stretched to week 7.**

### 🔴 TASK-301 — the arithmetic is one term short, checked on paper
`courseExpiry(start, 4)` = start + (`maxWeekFor(4,1)` − 1) = **week 5**.
| | last session | ceiling | headroom |
|---|---|---|---|
| no absences | week 4 | week 5 | 🔑 **1 week = exactly the quota of 1** |
| 3 absences | week 7 | `max(week5, week4 + 3w)` = **week 7** | 🔴 **ZERO** |
🔑 **The base ceiling always encoded *plan end + quota weeks*; the stretch counts only the ABSENCES, so the
quota's week is dropped.** ⇒ **the card promises `Leave 0/1` and the course cannot take it.**
⚠️ **`§10` gave unlimited absences at creation and silently removed the one leave the family had afterwards.**

### 🔴 The second half — a REFUSAL THAT NAMED A NUMBER THE CHECK DID NOT USE
`:2230` reads `course.expiryDate` (**week 7, correct**); `:2233` prints `MAX_WEEK_BY_SIZE[size]` (**5**).
⇒ **@Porter concluded *"the after-creation path measures against the OLD week-5 limit"*. It does not. The message
said so.**
🔑 **A wrong number in a refusal did not just confuse an admin — it sent a PM to a wrong diagnosis and nearly
bought a task for a defect that did not exist.** 📌 **Same class as DEF-3 and the two `startTime` formats: one
value, two sources.** ⇒ **both halves are TASK-301.**

### 🔻 TASK-300 — he ran the INNOCENT arrangement, and my drafting is why
**His course: absences weeks 1–3, LIVE session week 4** ⇒ the anchor sits after the whole plan ⇒ make-ups at
weeks 5–7, **correct**. **The defect needs @Jason's condition — *"the last declared absence FOLLOWS the last live
week"*.**
🔻 **It is the POSITION, not the count. I headlined the count and put the position in a table cell** ⇒ **@Porter
read what I emphasised. His reading was fair; my emphasis was wrong.**
✅ **Amended with the exact reproduction (4-session, weeks 2/3/4 absent, week 1 LIVE) and TEST-GATED:** **write
the assertion, report the result, change nothing first.** 🔴 **Fails ⇒ real. 🟢 Passes ⇒ I retract in full**, and
the better question becomes what normalises the anchor. 🚫 **Nothing reaches the owner until it has run.**

### 🔻 The rule I owe, and it is stricter than @Porter's
**His:** *"anything family-facing I carry to him needs a reproduction he can click."*
**Mine:** 🔑 **a family-facing claim gets a FAILING TEST before it gets a message** — **stronger, because it does
not need him at all.**
⚠️ **I escalated on a source-read TWICE tonight** — `§1` (code right, screen refusing) and this. **Both times I
read true lines and concluded something about behaviour I had not run.**
📌 **The lesson is not "read less carefully":** ***reading tells you what CAN happen; only running tells you what
DOES.*** **Tonight produced it in both directions — a `Select` whose value was right and whose screen was blank,
and an anchor whose code says one thing and whose saved data says another.**

**BALL: @Jason — TASK-301, then TASK-300's assertion.**

## [late] Jason (BE) — 🔴 **TASK-300 GATE: FAILS. The defect is REAL.** Recorded before any code was touched. → @Sober

`src/lib/makeup-placement.test.ts`, run on the untouched build, on the arrangement you named — **4 sessions, weeks 2/3/4 declared absent, week 1 LIVE**:

| | dates |
|---|---|
| **declared absent** | `2026-09-08` · `2026-09-15` · `2026-09-22` |
| 🔴 **where the SAVE places the 3 make-ups** | `2026-09-08` · `2026-09-15` · `2026-09-22` |
| **where the PREVIEW places them** | `2026-09-29` · `2026-10-06` · `2026-10-13` |

⇒ **all three make-ups land on the three absent days — exactly, not approximately.** Both gating assertions fail; the MIRROR arrangement (absences weeks 1–3, live week 4) passes at weeks 5/6/7, so **your amendment was right: it is the POSITION, not the count.**

🔑 **What is real in the test and what is reconstructed, so the result can be trusted:** `firstFreeWeeklySlot` is the actual function, called unchanged; `COURSE_LIVE` and `SLOT_INACTIVE_STATUSES` are the actual lists, imported. Only the two glue lines are reproduced (the anchor and the occupancy predicate are inline in a DB-bound function) — **and a fourth assertion pins that those two lines still read as reproduced**, so this file cannot quietly stop describing the code it claims to describe.

⇒ Per your instruction (**fails ⇒ real; the fix proceeds as written**) I am implementing §3 now: **one placement rule, the PREVIEW anchor — after the last PLANNED session — used by both paths.**

---

## [late] Jason (BE) — ✅ TASK-301 + TASK-300 DONE (code). → @Sober tsc **0** · **1775 pass / 0 fail**, 141 files · 🚫 **35 `.sql` = 35 journal tags.**

*(This block covers TASK-301 and TASK-300 — they share `courseBornCeiling`'s file and one suite run.)*

### ✅ TASK-301 — the quota's week is back, and the refusal names the boundary it used
- [x] 🔑 **The owner's `มิลล่า` can now take its quota leave** — 4 sessions, 3 declared absences, plan ends
      week 7, ceiling week 8 ⇒ the make-up at week 8 is allowed. **Written first.**
- [x] 🔑 **A SECOND leave is still REFUSED** — week 9 is past the boundary. *The assertion that stops "no room"
      becoming "no ceiling."*
- [x] **An absence-free course is unchanged, by the number** — size 6 ends week 6, ceiling week 8 = `courseExpiry`
- [x] 🔑 **The refusal names the course's own ceiling** — asserted, **and asserted NOT to print `5`**
- [x] **The promise is in words where the arithmetic is** · 🚫 quota, `courseExpiry`, `maxWeek`, card untouched

**(a)** `courseBornCeiling(base, lastPlanned, absences, quota)` — the comment states the promise as a sentence
before the code does it: ***the ceiling is the plan's end plus the leave quota, in weeks.*** The absences move
the plan's END; the quota's weeks sit beyond it. 📌 **The no-absence case is the check that the term is the
RIGHT one and not merely more room:** week 6 + quota 2 = week 8 = `courseExpiry(start, 6)` — the two agree by
arithmetic, not coincidence. And it is still `max(base, …)`, so a short plan keeps its full window.
**The quota comes through `courseLeaveQuota`**, not the card's table, so an off-card size answers with its own
allowance instead of falling through to zero.

**(b)** The refusal now prints `คอร์สขยายเกินวันสิ้นสุดของคอร์ส (${course.expiryDate}) ไม่ได้` — **a date**, so
the admin can compare it to the plan in front of them. ⚠️ A week number derived from anything but the ceiling
would bring this back with new digits, and that is asserted: `MAX_WEEK_BY_SIZE[course.size]` is gone from the
refusal and the message cannot print `5` on a week-7 course.

✅ **Break it and watch:** dropping the quota term returns the ceiling as `2026-10-13` (week 7) instead of
`2026-10-20`, and **the owner's leave is refused again** — four assertions fail, and the second names his case.

#### 🔑 The Question — yes, and the exceptions are the interesting part
**Pinned:** *a course's ceiling always leaves room for its remaining quota* — asserted across **all three sizes ×
0–3 absences**, as a property rather than an example.
⚠️ **It does NOT hold in two places, and I did not force either:**
1. 🔴 **A RE-PLANNED course.** TASK-282 derives the expiry as `max(old, last session)` — so a re-plan that ends
   after the old expiry leaves **zero** headroom, and the next leave is refused. **The owner's complaint, one
   verb over.** 🚫 Not touched: the derived expiry is in this task's `must not change`. **It is worth a task.**
2. ⚪ **An admin edit.** The headroom is whatever they chose, and that is correct — a deliberate act sets the
   boundary, which is TASK-299's whole rule. **Not a defect.**
✅ **Imports are fine:** `importedCourseExpiry` reconstructs the real start and calls `courseExpiry`, so it
carries the same *plan end + quota* promise.

---

### ✅ TASK-300 — 🔴 the gate FAILED. The defect was real, and it is fixed
**🔑 The gate result, recorded in the log before any code was touched**, on the arrangement you named — 4
sessions, weeks 2/3/4 declared absent, week 1 live:

| | dates |
|---|---|
| declared absent | `2026-09-08` · `2026-09-15` · `2026-09-22` |
| 🔴 where the SAVE placed the 3 make-ups | **`2026-09-08` · `2026-09-15` · `2026-09-22`** |
| where the PREVIEW places them | `2026-09-29` · `2026-10-06` · `2026-10-13` |

**All three landed on the three absent days — exactly.** Your amendment was right: **it is the POSITION, not the
count.** The mirror arrangement passed at weeks 5/6/7 on the same run.

**The fix is §3 as written:** one anchor, the **last PLANNED session**, used by both paths. 🚫 **Neither status
list changed** — a student on leave DOES free the teacher's slot for someone else (UC-004), and that is asserted
directly. **The bug was this course reusing its OWN absent week**, and the anchor is where that is decided.

- [x] 🔑 **No make-up lands on a declared-absent date** — asserted on the DATES
- [x] 🔑 **The preview and the save place them in the SAME weeks** — asserted by comparing the two
- [x] **A leave taken AFTER creation** — ⚠️ **named, and the answer is more than "they coincide":** a
      post-creation leave on the LAST session had **the identical defect** — the old anchor fell back to the
      second-to-last live row and that session's own date, now slot-inactive, read as free. ⇒ **the planned
      anchor is not a creation-time special case; it is strictly better on both paths**, which is why there is
      one rule and no branch.
- [x] **Another student may still be booked into the freed slot** — asserted
- [x] 🚫 TASK-299's ceiling, the status lists and the mirroring untouched — asserted

#### ⚠️ On "break it and watch", honestly
**The behavioural proof is the GATE RUN itself** — the live-only anchor, on the untouched build, putting three
make-ups on three absent days. That is recorded above and in the log, and it is stronger than a re-mutation.
📌 **What a source mutation shows here is narrower, and worth saying:** restoring `liveAfterCancel` fails only
the **reconstruction pin**, not the placement assertions — because the test reproduces two inline lines from a
DB-bound function and cannot re-read them. ⇒ **the pin is what makes this file honest**, and it did its job.
**A reconstructed test that stops describing the code it names would be worse than no test**, which is why the
pin is in the file rather than in this report.

**BALL: @Sober — TASK-301 and TASK-300 ready for review. ⛔ TASK-298 still open on me.**

## [--:--] Sober (SA) — 🔴 **The gate FAILED: TASK-300 was real. Both tasks PASS, verified by me.** 📋 **TASK-302 cut from @Jason's own exception.**

**Reproduced:** `tsc` **0** · **1775 pass / 0 fail** (5008 expects, 141 files) · **35 = 35** · anchor is
`plannedRows` · `courseBornCeiling(base, lastPlanned, absences, quota)` · the refusal prints `course.expiryDate`
and `MAX_WEEK_BY_SIZE` is **gone** from it.

### 🔑 Gating it was the right call and the gate earned its keep in one run
**Weeks 2/3/4 absent, week 1 live ⇒ all three make-ups landed on `2026-09-08 · 09-15 · 09-22` — the three absent
days exactly.** **The owner's mirror arrangement passed on the same run.** ⇒ **the POSITION, not the count.**
✅ **He ran it BEFORE touching code and recorded the result** — the sequence that would have saved @Porter a
message and the owner a test.
🔴 **And a defect nobody had reported came out of it:** the SAME anchor was wrong on the **post-creation** path —
a leave on the LAST session fell back to the second-to-last live row, **whose own date, now slot-inactive, read
as free.** ⇒ **one rule, no branch, better on both paths.** 📌 **Found by taking a DoD line literally instead of
answering it.**

### ⚠️ What the evidence IS, said plainly upward
🔑 **A faithful RECONSTRUCTION pinned to the source — not a run of the real function.** Placement lives in a
DB-bound function and **no agent here touches a database.** ✅ **@Jason put that limit in the TEST FILE, not in a
report:** *"a reconstructed test that stops describing the code it names would be worse than no test."*
⇒ **strongest available to us, weaker than a click.** **Told @Porter in those words**, since he asked for a
reproduction and I promised a failing test. ⚪ **One confirming screen when convenient; nothing waits on it —
the fix moves make-ups LATER, never worse than a day the family declared away.**

### ✅ TASK-301 — the promise reads as a promise
*"The ceiling is the plan's end plus the leave quota, in weeks"*, stated **before** the arithmetic does it.
🔑 **The no-absence case proves the TERM rather than the ROOM:** size 6 ends week 6, ceiling week 8 =
`courseExpiry(start, 6)` — **agreeing by arithmetic, not coincidence.** ⚠️ **A `+ 7` that merely made things pass
would fail that.** ✅ **And `courseLeaveQuota` rather than the card's table, so an off-card size does not fall
through to zero — nothing asked him for that.**
✅ **The refusal now names the course's own end date; asserted that it cannot print `5` on a week-7 course.**
📌 **That message sent a PM to a wrong diagnosis two hours ago.**

### 📋 TASK-302 — his exception, verified before cutting
`:3897` `replanExpiry(course.expiryDate, lastSession)` — *"never shrinking"* ⇒ **a re-plan ending after the old
expiry sets the expiry to the last session EXACTLY** ⇒ **zero headroom** ⇒ **a family that paused and came back
cannot use the leave they still have.** 🔑 **TASK-301 one verb over.** ⚠️ **The task says REMAINING quota, not
full** — *that is what keeps it a promise rather than a gift.*
✅ **His other exception, the ADMIN EDIT, is correctly not a defect** — a deliberate act sets the boundary, which
is TASK-299's rule; it is written into TASK-302's Question as the exception the property must carve out.

### 🔻 On my own call
**"Unproven, do not relay" was right as a DECISION and wrong as a CONCLUSION.** 🔑 **The rule earned its keep on
first use: no family-facing alarm was sent, the gate answered it in one run, and @Porter never had to carry it.**

**BALL: @Jason — TASK-302, then TASK-298.**

---

## [late] Jason (BE) — ✅ TASK-302 DONE (code). → @Sober tsc **0** · **1785 pass / 0 fail**, 142 files · 🚫 **35 `.sql` = 35 journal tags.**

- [x] `tsc --noEmit` → **0** · `bun test` → **1785 / 0**, 142 files · 🚫 no migration (**35 = 35**, counted)
- [x] 🔑 **A resumed course with unused quota can take a leave** — **written first**
- [x] 🔑 **A resumed course with its quota SPENT gets no extra room** — the boundary lands on its last session
      exactly, as before. *"Remaining" is the whole difference*
- [x] **A re-plan finishing BEFORE the old expiry leaves it untouched**, and `recordExpiryChange` still writes
      nothing — both asserted
- [x] **The expiry still covers the last planned session** — asserted across every quota value, because that is
      DEF-4's guarantee
- [x] 🔑 **Break it and watch** — dropping the term returns the expiry as the last session exactly and the
      resumed course is refused again; restored, suite green, before this number
- [x] 🚫 The derived expiry, the never-shrink rule, quota and `leaveUsed` untouched — asserted

New: `src/lib/replan-quota-room.test.ts` (10 tests).

### §1 ✅ The same arithmetic, not a second one
```ts
replanExpiry(currentExpiry, lastSession, remainingQuota) =
  lastSession ? courseBornCeiling(currentExpiry, lastSession, 0, remainingQuota) : currentExpiry
```
🔑 **A re-plan declares no absences, so it IS `courseBornCeiling` with `absences = 0`.** The shapes did fit, so
there is one arithmetic for one sentence — **and it is asserted by AGREEMENT** across every quota value, so a
change to either function that does not change the other fails there rather than in production.
⚠️ `remainingQuota = Math.max(0, courseLeaveQuota(course) − course.leaveUsed)`, floored at zero so a course
somehow over its quota cannot pull the expiry backwards and fight the never-shrink rule.

### §2 ✅ Break it and watch — and the REASON
Replacing the remaining term with `0` returns `2026-12-01` — **the last session exactly, zero headroom** — and
`exceedsExtensionCeiling` refuses the leave again. Three assertions fail and the first names the case.
📌 **The *"quota SPENT"* test correctly keeps PASSING under that mutation**, which is the right shape: with
nothing remaining the two builds agree, and a mutation that broke it too would have meant the term was doing
something other than what it claims.

### 🔑 The Question — **yes, and your form of it is the right one**
> *"Every path that COMPUTES the boundary keeps the promise; the one path where a PERSON chooses it does not."*

**Asserted as one test over the three computing paths**, so a fourth entry point added without the promise fails
there rather than on a family's calendar:
| path | ceiling | keeps *plan end + remaining quota* |
|---|---|---|
| **created** | `courseBornCeiling(courseExpiry, lastPlanned, absences, quota)` | ✅ |
| **imported** | `importedCourseExpiry` → `courseExpiry(realStart, size, quota)` | ✅ |
| **re-planned** | `replanExpiry` → `courseBornCeiling(…, 0, remaining)` | ✅ |
| ⚪ **admin edit** | whatever the person typed | **deliberately not — TASK-299's rule** |

📌 **One thing worth naming from building it:** the imported path keeps the promise **by a different
expression** — `courseExpiry` encodes *plan end + quota* directly, while the other two go through
`courseBornCeiling`. They cannot disagree in the direction that matters, because `courseBornCeiling` takes
`courseExpiry` as its `base` and `max`es with it. **But it is the same sentence in two arithmetics**, and if a
fourth path ever needs a third, that is the moment to collapse them.

### 🔴 Your last question — **nothing warns the admin. Named, not built.**
`updateCourseExpiry` computes `expiryImpact`, which reports the **SESSIONS** that would fall outside the new
date. **It says nothing about the leave the family still has.** ⇒ **an admin can silently spend a course's
remaining quota by moving one date**, and the first sign is a leave refused weeks later with a message about the
course's end date.
⚠️ Asserted only as *the absence of `leaveUsed` in that function* — so if someone later adds the warning, this
test tells them the note here is stale rather than letting it sit.

**BALL: @Sober — TASK-302 ready for review. ⛔ TASK-298 next, still open on me.**

## [--:--] Sober (SA) — ✅ **TASK-302 PASSED, verified.** 🔑 **The shapes FITTING is the result, not a shortcut.** ➕ **His closing finding folded into TASK-298 rather than made a fifth task.**

**Reproduced:** `tsc` **0** · **1785 pass / 0 fail** (5034 expects, 142 files) · **35 = 35** ·
`replanExpiry(currentExpiry, lastSession, remainingQuota)` → `courseBornCeiling(…, 0, remaining)` ·
`remainingQuota = Math.max(0, courseLeaveQuota − leaveUsed)`.

### 🔑 One arithmetic, because the DOMAIN says so
> *"A re-plan declares no absences, so it IS `courseBornCeiling` with `absences = 0`."*

**I said use the same helper IF it fits and say so if not.** ✅ **It fits for a reason about the domain, not for
convenience** — **which is exactly what makes one arithmetic right here and wrong when forced.**
✅ **Asserted BY AGREEMENT across every quota value** ⇒ **a change to either function that does not change the
other fails in a test rather than on a family's calendar.** 🔑 **The strongest form of *"two things that agree
today"* yet: he made the AGREEMENT the assertion.**

### 🔑 Break-it-and-watch has a second half, and he found it
> *"The 'quota SPENT' test correctly keeps PASSING under the mutation — a mutation that broke it too would have
> meant the term was doing something other than what it claims."*

📌 **I keep asking which tests FAIL. He checked which ones MUST NOT.** ⇒ **a mutation that fails everything proves
only that you changed something.** → **`SYSTEM-FACTS`.**

### ✅ The lifecycle property — three paths keep it, one deliberately does not
**created · imported · re-planned ✅ · admin edit ⚪ (a deliberate act sets the boundary — TASK-299's rule).**
⚠️ **His honest footnote is the keeper:** the imported path keeps the promise **by a different expression**;
they cannot disagree in the direction that matters because `courseBornCeiling` `max`es with `courseExpiry` —
**but it is one sentence in two arithmetics, and he named the moment to collapse them rather than doing it now.**
✅ **Right: a third path would be the reason, and there is not one.**

### ➕ TASK-298 §5, not TASK-303
🔴 **Nothing warns an admin who edits a course's expiry into its remaining leave** — `expiryImpact` reports
SESSIONS, never the unused quota. ⇒ *"the first sign is a leave refused weeks later with a message about the
course's end date."*
🔑 **Folded into TASK-298 because that task exists so an admin is told what an earlier expiry CUTS OFF before
saving — and the unused leave is one of the things it cuts off.** ⇒ **two dialogs asking "what will this cost?"
at the same moment, built a week apart, is the split this week has been spent closing.** 📌 **TASK-298 has not
started; folding it in costs nothing now and a second FE change later.** ⚠️ **Told him to say so if it needs a
different data path — I would rather hear that than have it wedged in.**
✅ **And he asserted the ABSENCE of `leaveUsed` in that function meanwhile**, so a later warning makes the note
stale rather than letting it rot — **the comment-outliving-its-mechanism lesson, applied unprompted.**

### 📌 The pattern of the last four tasks
**TASK-297 → TASK-300 · TASK-299 → TASK-301 · TASK-301 → TASK-302 · TASK-302 → TASK-298 §5.** 🔑 **Every one came
out of the DoD's closing Question, not out of a defect report.** ⇒ **the Question has become the pipeline**, and
**the two most serious findings of the night — the make-up on the absent day, and a `Leave 0/1` that cannot be
used — both came from asking an engineer what ELSE looked like the thing he had just fixed.**

**BALL: @Jason — TASK-298, now with §5.**
