
## 2026-09-13 — Sober (SA Lead)

### 🟢 `REQ-088` PROVEN ON THE OWNER'S PHONE — rounds 1–2 inside LINE
**Consent → phone → "Found your family" (4 children) → link; a NEW parent "Gekko" added.** 🔑 **The boundary
nobody could test from a desk is tested.**
🔻 **Two setup facts were wrong first — one of them MINE before it was @Porter's:** the link parents tap is
`https://liff.line.me/<LIFF_ID>` (the endpoint opens a plain browser, `no-id-token`); and **scope must include
`openid`** — I wrote *"`profile` yields the `userId`"* the same day as a design that uses the TOKEN, and did
not check the two against each other. 📌 **Eighth correction of my text this batch, and the first found by the
owner.** ✅ *Both go into `.env.example`'s comment so the customer's instructions and the code agree in one
place.*

### ▶️ OWNER: BUILD `§7a` + `§7b` NOW — he has paused testing and will test ONCE → TASK-349
🔑 ***"A parent who will not type `สมัคร` will not type `08-09-2020` or an address"*** ⇒ **every field that can
be a tap should be a tap.** 🚫 **Stored values UNCHANGED — the picker is entry, not storage. No migration.**
**Four things, one page, one deploy, one report:** the DOB picker (S — a typed path stays) · the cascading
address (M — **the cost is DATA**: source, licence, bytes, loaded only on `/register`; labels follow the
province; the three picks JOIN into the chat's own string) · nickname only when it differs · the truncated
button, pinned by length not bytes.
✅ **The escape hatch — @Porter asked me to decide rather than make the owner wait:** **a `Type it instead`
toggle revealing the existing free-text field.** Three paths, one stored shape. *Not "other" in the picker —
that still needs a text box and invites `กทม · other · other`; not "just optional" — a parent who cannot find
their sub-district leaves blank what they wanted to give.*
🚫 **Nothing else cut. The chain stays stopped.**

⏱️ **State: @Fern on TASK-349 · @Jason EMPTY · the owner waiting to test once · the list at 16 with @Porter.**

## [--:--] Fern (FE) — ✅ **TASK-349 done — all four, one page, one deploy. Stored values unchanged; nothing else changed.** 🔻 **And one thing found by reading `develop`: two writers, one column, one report.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  305 pass / 0 fail   (was 285; +20 — new `entry.test.ts`, date block re-cut)
bun run build     → ok — `○ /register` still static beside `○ /checkin`
```
🚫 No BE · no migration · `api.ts` / `liff.ts` / `/checkin` / `register/page.tsx` byte-identical. ⚠️ **One new dependency:
`thai-address-universal@2.2.0` (ISC), pinned exact, dynamically imported.** Full notes in the TASK file §6.

### §7a — YEAR-FIRST, and what the widget needs, exactly
`DatePickerInput` with `defaultLevel="decade"` — the year grid opens first; 2–15 years back is one or two taps.
🔑 **Mantine v9 emits `YYYY-MM-DD` as a STRING** ⇒ `toCustomerDate` is `split("-").reverse().join("-")` — **a
reorder, not a parse; no `dayjs(`, no `new Date(`.** 🔴 **Rule-1 absence re-scoped ON PURPOSE:** the page and
`api.ts` keep the FULL TASK-348 absence unchanged; the one `split("-")` lives in the new `entry.ts`, held by its own
test to a narrower absence (exactly one split, no dayjs/Date/`isNaN`/`throw`/`minDate`). Mutation 5 — a
`Date.parse` guard added — fails it. ✅ Typed mask kept behind `พิมพ์เอง / Type it instead` · confirm echoes · blank
omitted. *Your §5 question: mine are the "cannot produce nonsense" kind, not the pre-validating kind.*

### §7b — the DATA, sized from the build
**77 / 928 / 7,211 with the official geocodes.** 🔑 **Lookups by GEOCODE, never by name — `จอมทอง` is in both Bangkok
and Chiang Mai**; asserted on the real rows. **Labels follow the province** (geocode `10` ⇒ เขต/แขวง). **The join is the
customer's example byte-for-byte — `พระโขนงเหนือ วัฒนา กทม`**; the abbreviation rule is *the EVERYDAY name* (only
กรุงเทพมหานคร has one that differs from its formal name; `ชม.`-style forms are plates and post, not addresses) — one
row, derived, asserted absent for the rest.
📦 **Bytes:** `/register` first load **334 KB gz** (`/checkin` 270); the dataset **417,590 raw / 127,496 gz in five
LAZY chunks**, fetched only when the form is on screen in pick mode. **Admin bundle asserted clean two ways:** the
package is named in exactly ONE source file as `await import(...)` (a walk of `src/**`; mutation 4 — a static import
— fails it), and in the build none of the 18 prerendered pages references any of the five chunks. ⚠️ *Honest cost:
the code-keyed API also loads the English names + geocode table (~55 KB gz of the 127). Named, not built: a
build-time Thai-only slice would halve it.*
✅ **Your escape hatch, as decided** — plus one automatic path to it: if the chunk cannot be fetched, the typed field
takes over rather than three dead selects.

### §4 — `มิลล่า`, not `มิลล่า (มิลล่า)` · `Link this LINE account` / `ผูกบัญชี LINE นี้`, **LENGTH ≤ 28 pinned** (was 43)
### `.env.example` — `https://liff.line.me/<LIFF_ID>` and `openid`, in the comment, asserted.
### 🔑 Break-and-watch — five mutations in one call, each restored, **md5 identical**.

### 🔻 Found by reading `develop`, not remembering it — top of the owner's list, not a task
**`parents.province` has TWO writers:** the admin's `ParentFormModal` (SPEC-016) stores the FULL name from the repo's
own `TH_PROVINCES`; the chat (REQ-079) and now the page store `พระโขนงเหนือ วัฒนา กทม`. **`som-report.service.ts`
groups demographics by that column** ⇒ one Bangkok family lands in two buckets, and every LINE family in a bucket
of its own. **Pre-exists this task; I changed nothing (stored value unchanged, as instructed).** ✅ I pinned that the
dataset's 77 names equal `TH_PROVINCES` exactly, so this side cannot be the cause of a split. 📌 **Which form the
column should hold is the owner's decision, with the report in view.** ⚠️ QA note: the admin Select fed a value not
in its list — whether it shows blank is a screen check.

### Q — where else is a TYPE really a PICK: `OTHER` booking title (becomes `displayName`) · discount reason ·
country · badge type name. Not: names, notes, phones, the search boxes. 🚫 Named, nothing built.

**Ball: @Sober** — review; one deploy; the owner tests once. `BookingModal:1140` still the last known-open time ·
TASK-294, TASK-286 remain, no clock.

### ✅ TASK-349 DONE and REVIEWED — @Fern. **305/0, tsc 0, build ok, re-run by me.** 🟢 **All four in; the owner tests once. ONE handover message with @Porter.**
**Verified, not taken:** the dataset imported in exactly ONE file (`entry.ts`) · the admin form stores the FULL
province name via `TH_PROVINCES` · `som-report.service.ts:120` groups by `parent.province` · the chat/page write
free text to the same column at `line-register.service.ts:192`.

🔑 **The one idea in code:** ***the pickers are ENTRY; they cannot produce nonsense; they do not pre-validate
anything.*** `toCustomerDate` is a REORDER not a parse; the Rule-1 absence re-scoped ON PURPOSE (page and API
on the full absence; `entry.ts` on its own narrow one), proven by a mutation that adds a validation.
✅ **`§7b`: `thai-address-universal@2.2.0` (ISC, pinned exact), 127 KB gz in five LAZY chunks, admin bundle
asserted clean two ways; lookups by GEOCODE — `จอมทอง` is in two provinces; tier words keyed off geocode `10`;
the join is the customer's line byte for byte, and the abbreviation rule DERIVED: *the everyday name, and
`กรุงเทพมหานคร` is the one province whose everyday name is not its formal name.*** ✅ **My hatch, plus one she
found: a failed dataset fetch drops to the typed field by itself.**
✅ **The button pinned at ≤ 28 with its pixel derivation** — *a bound that can be recomputed.*

### 🔴 HER FINDING GOES TO THE TOP OF THE OWNER'S LIST — verified by me at all four links
**`parents.province` has TWO WRITERS storing TWO FORMS** — the admin's full name vs the chat's/page's free-text
line — **and the SOM report groups demographics by it** ⇒ ***a Bangkok family registered by an admin and one
from LINE land in two buckets of the same report, and every LINE family in a bucket of its own.*** 📌 **Not new
(the chat since REQ-079), but the first time the two writers were put side by side — the `Remaining : 0`
shape.** ✅ **She did as told, no migration, and pinned the dataset's 77 names EQUAL to the admin's list so the
page can never be the cause.** ❓ **The OWNER'S decision — which form the column holds — with a report reading
it, and one QA check (the admin `Select` fed a value not in its rows).** 🚫 **Not cut. Item 17.**
📌 *Her correction-before-sending — she first read the STUDENT form and said there was no address field; it is on
the PARENT form — is the discipline, applied to her own draft.*

⏱️ **State: nothing in flight · both engineers EMPTY · the owner testing once · the list at 17 with @Porter,
the province decision at the top.**
# 2026-09-13 — smart-scheduler

## [--:--] Porter (PM) — 🟢 `REQ-088 §7` all four in (FE 305/0). Owner can test once. 🔴 **One decision surfaced: `parents.province` has TWO WRITERS storing TWO FORMS, and the demographics report groups on it.**
**DOB:** year-grid first (a birth year 2–15 back is one tap), `Type it instead`, stored value unchanged.
**Address:** three cascading lists, **tier words flip to เขต/แขวง on กรุงเทพมหานคร and อำเภอ/ตำบล elsewhere**,
confirm shows the customer's own `<แขวง> <เขต> กทม`, typed fallback, **and drops to typed by itself if the
dataset cannot load.** 📐 **Dataset `thai-address-universal@2.2.0`, 77/928/7,211, 127 KB gzipped, LAZY — a
parent who only links never fetches it.** 🔑 **Lookups by GEOCODE — `จอมทอง` is a district in Bangkok AND Chiang
Mai.**
🔴 **The decision:** the ADMIN's parent form stores `กรุงเทพมหานคร`; the CHAT and now the PAGE store
`พระโขนงเหนือ วัฒนา กทม` — **and `som-report.service.ts` groups DEMOGRAPHICS by that column.** ⇒ **a Bangkok
family registered by an admin and one from LINE land in different buckets, and every LINE family in a bucket of
its own.** 📌 **Not new — the chat has done it since REQ-079 — but the picker makes the stored FORM a choice.**
✅ **@Fern did as told and pinned the dataset's 77 names EQUAL to the admin's list, so this side can never be
the cause of a split.** 🔑 **The right kind of guard: it makes the future fix cheap without pre-empting the
decision.**

### 🟢 `§7` PASSED on the owner's phone (pick mode default confirmed) → 🆕 `§8`: a prominent TH/EN toggle → TASK-350
**Owner:** *"ทำปุ่มเด่นๆ ให้เปลี่ยนภาษาได้ ให้เหมาะสำหรับคนไทย และชาวต่างชาติ"*. 🔑 **@Porter's reasoning stands: a
bubble can be tall; a form that doubles every label doubles the page** ⇒ **one language at a time, a visible
control above the first field.**
✅ **Sized S from the code, not a guess:** `I18nProvider` already carries `lang` + `setLang` with a saved
preference, and the dictionary is `th: typeof en` ⇒ **the toggle calls `setLang`; the mechanism exists.**
📖 **Default = the LINE app's language, only when no preference is saved.** ✅ **Tier words STAY THAI in both**
— proper nouns a foreign parent sees on their own mail, and what the law calls them. ✅ **Two nits folded in.**
⚠️ **Asked her to CHECK one thing rather than assume: whether the saved preference is one key for the whole
origin — a parent's toggle on `/register` flipping an admin's screen on the same phone would arrive later as a
mystery report.**
📝 **The 29 placeholders become 29 PAIRS; the report will split them by whose English it is, so @Porter's one
visit carries the right question for each.**
🚫 **Nothing else cut. The chain stays stopped.**

⏱️ **State: @Fern on TASK-350 · @Jason EMPTY · the list at 17 with @Porter, the province decision at the top.**

## [--:--] Fern (FE) — ✅ **TASK-350 done — one language at a time; the toggle is the first thing on the page; it changes `lang` and nothing else.** 🔻 **§6: `ss.lang` WAS one key per origin — `/register` now has its own.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  312 pass / 0 fail   (was 305; +7)
bun run build     → ok — `○ /register` still static
```
🚫 No BE · no migration · **no new dictionary key** · `api.ts` / `entry.ts` / the pickers / the join / the length bound
untouched. 6 files modified, 1 new (`lib/register/locale.ts`). Full notes in the TASK file §7.

### The toggle — the app's own, at the head of the `Paper`, every phase, and it does ONE thing
`<LanguageToggle size="md" fullWidth />` is the first child of the page's `Stack` — above the title, the loading
text, the phone field; it stays through form, confirm, done. Two props added to the shared component with the old
defaults, so the admin header is byte-for-byte the same on screen. 🔑 **`lang` appears exactly twice on the page**
(the destructure and the page's own `DatesProvider` locale) — no `[lang]` effect, no `lang ===` branch. Asserted.

### The default — the LINE app's language on a FIRST visit; a saved tap wins; and where the read lives
**`lib/register/locale.ts`** owns `phoneLanguage()` (`liff.getLanguage()`, `th*` ⇒ `th`, else `en`); **`liff.ts` stays
the credential module** — it exports the loaded SDK (`loadLiff`) and never calls `getLanguage` (asserted). Applied
through a new additive **`I18nProvider.setLangIfUnset`**, which reads storage at CALL time (LIFF answers long after
mount), returns on a saved value, and **never writes** — a device default is not a preference. Mutation 2 proves it.

### 🔻 §6 — yes, it was one key for the whole origin, and it is not any more
`ss.lang` in `localStorage` is per origin: a parent's tap on `/register` would have written the admin's key, and an
admin's choice would have been the parent's default, on the same browser (the owner testing the LIFF URL in Chrome
on the phone he runs the back office on is exactly that). ✅ `I18nProvider` takes an optional `storageKey` (default
unchanged); `register/page.tsx` mounts a **nested** provider on **`ss.lang.register`** — the page has its own language
and its own memory; the admin's `ss.lang` is neither read nor written from `/register`. Mutation 3 proves it. The
page also wraps its own `DatesProvider`, so the picker's month names follow the toggle, not the admin's setting.

### §3 tier words Thai in both (no `t(`/`lang` in `tierWordsFor`; mutation 4 ⇒ 2 fails) · §4 nits both asserted
(`addrProvince` en/th pinned · the screen-6 sentence in TYPED mode only, exactly once in the form; mutation 5).

### 📝 Pairs — whose English half is whose (full list in the TASK)
**21 VERBATIM: both halves the customer's `§17c` — nothing to ask.** 2 ADAPTED (one question each) · 2 borrowed ·
**36 PLACEHOLDER pairs (29 + TASK-349's 7): both halves @Porter's**, except `FAMILY_FULL` (TH is the server's own
sentence), `foundConfirm` (EN is @Porter's already; TH mine, marked) and `typeInstead` (@Sober's pair).

### 🔑 Break-and-watch — five in one call (toggle below the title · phone overrides a saved choice · key removed ·
English tier words · instruction back in pick mode) ⇒ 1/1/1/2/1 fails; **md5 identical** on all four files after.

### Q — `/checkin`: no TOGGLE needed (one sentence, one button, reached from a message in the parent's language) —
**but it has the DEFAULT problem:** root provider, `DEFAULT_LANG = "en"`, and `ss.lang` is written only by the
admin header ⇒ **a Thai parent sees English unless that browser holds an admin's saved choice.** Not LIFF, so
`navigator.language` would be the read; the same nested-provider shape as here, ~10 lines. 🚫 Named, not built.

**Ball: @Sober** — review; one deploy; the owner tests. `BookingModal:1140` still the last known-open time ·
TASK-294, TASK-286 remain, no clock.

### ✅ TASK-350 DONE and REVIEWED — @Fern. **312/0, tsc 0, build ok, re-run by me.** 🟢 **`§8` in; he tests once.**
**Verified:** the nested provider with `ss.lang.register` · the root key still `ss.lang` · `liff.ts`'s only
`getLanguage` is the SDK TYPE, not a call — the read lives in `locale.ts` · `/checkin` under the ROOT provider
with `DEFAULT_LANG = "en"` and no key.

🔴 **The `§6` check I asked her to make rather than assume WAS a defect:** one key per origin — a parent's tap
on `/register` would have written the admin's preference, and vice versa, on the same browser. ✅ **Fixed with a
nested provider and its own key — and the consequence I did not ask about, handled: the page wraps its own
`DatesProvider`, so the picker's month names follow the toggle, not the back office.** 🔑 *The difference
between fixing the question and fixing the problem.* ✅ **A device default is never SAVED; only a tap is —
mutation 2 proves a saved choice beats the phone.**
✅ **The toggle does one thing, asserted on both sides: `lang` appears exactly twice on the page.** ✅ **The
credential module stayed a credential module — one additive export, never a `getLanguage` call.**
📝 **The pairs split by whose English it is — 21 · 2 · 2 · 36 with three named exceptions** ⇒ @Porter's one visit
carries a different question per set.

### 🔴 HER QUESTION FOUND THE TWIN ON `/checkin`, AND IT IS LIVE — TOP OF THE OWNER'S LIST
***A Thai parent tapping the check-in link sees ENGLISH unless that browser happens to hold a saved admin
preference — and the only thing that writes it is the admin header.*** ✅ **Verified: root provider, default
`en`, no key.** 🔑 **Not a toggle — the DEVICE DEFAULT, ten lines, the shape she just built with
`navigator.language`.** 🚫 **Not cut; the chain is stopped.** 📌 **Said once to @Porter: it is the only LIVE item
on the list and smaller than anything above it.**

⏱️ **State: both engineers EMPTY · the owner testing `§8` once · the list at 18 with @Porter — `/checkin`'s
default and the province split at the top.**

## [--:--] Porter (PM) — 🟢 `§8` in (FE 312/0): TH/EN control first on the page, LINE-language default, sticks across reload, changes language and nothing else. 🔴 **And the check found a LIVE defect on `/checkin`.**
🔴 **Before this task, a parent's TH/EN tap on `/register` wrote the ADMIN'S saved language key — same browser,
same key.** ✅ **Fixed: `/register` has its own scope.** 🔑 **And the same shape is LIVE on `/checkin` today:
a Thai parent tapping the check-in link sees ENGLISH unless that browser holds a saved admin preference** —
verified, default `en`, no key of its own. **Ten lines: device-language default, own key. Not a toggle.**
📌 **@Sober said once it is the one he would take — the only list item a customer is meeting right now, and
smaller than anything above it.** ✅ **Putting it to the owner with the deploy, not deciding it.**
📝 **The copy visit is now: 21 verbatim (nothing to ask) · 2 adapted ("one clause changed — fine?") ·
36 placeholder PAIRS ("approve both").**

### 🟢 `§8` PASSES on the phone, both directions. 🔻 One reversal → TASK-351 (XS)
**EN mode, pick mode: `Province · อำเภอ · ตำบล` — one English label over two Thai ones.** 🔻 **@Porter's "stay
Thai" ruling was about the VALUES (proper nouns on a parent's own mail) and was applied to the LABELS — and I
agreed with it in writing for the same reason.** ✅ **Corrected: labels follow the language (EN `Province ·
District · Sub-district`; TH `จังหวัด · เขต/อำเภอ · แขวง/ตำบล` with the Bangkok flip); values Thai in both.**
🔑 **TASK-350's *"tier words unchanged by the toggle"* is now wrong BY REQUIREMENT — rewritten with the reversal
inside, not deleted.** 📌 **Ninth correction of my text this batch; the second found by the owner's screen.**
✅ **Default-mode question withdrawn — pick IS the default; the shots were out of order. Nothing checked.**
🚫 **Nothing else cut. Chain stopped.**

## [--:--] Fern (FE) — ✅ **TASK-351 done — labels follow the language, values stay Thai; TASK-350's assertion rewritten with the reversal inside it.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  314 pass / 0 fail   (was 312; +2)
bun run build     → ok — `○ /register` still static
```
🚫 No BE · no dependency · no stored-value change · `entry.ts` untouched: the four Thai literals and the geocode-`10`
flip are still the SOURCE. Three files: the page (six lines), two dictionary keys, the test. Notes in the TASK §4.

**The page:** `tierTh = tierWordsFor(code)` (Thai, with the flip) · `tier = lang === "th" ? tierTh : { District,
Sub-district }` — @Porter's words as `register.addrDistrict` / `addrSubDistrict`, PLACEHOLDER, pinned by FORM. EN:
`Province · District · Sub-district` · TH: `จังหวัด · เขต/อำเภอ · แขวง/ตำบล` — **all four combinations asserted; values
Thai in both asserted** (`nameTh` only, `nameEn` nowhere, no `lang` reaches the join).
🔴 **The reversed test is REWRITTEN, not deleted:** a four-line comment states the ruling, why it was wrong (a
sentence about VALUES applied to LABELS; the owner's `Province · อำเภอ · ตำบล`), and the half kept. TASK-350's *"`lang`
exactly twice"* became *three, each named*, with its rule restated: `lang` renders, never sets state.
🔑 **Break-and-watch ×3** — the old ruling back · values following the language · Porter's EN word replaced by Thai
⇒ 2/2/2 fails; md5 identical after.

### Q — label vs value sharing a rule elsewhere on `/register`
Month names: a label, already follow `lang`; value is digits ✅ · `กทม` in the confirm line: a VALUE, shown as stored ✅
· 🔻 **the one real one: the typed-mode instruction (`provinceLabel` EN, the customer's verbatim §17c) gives a
ROMANISED example — `Prakanueng Nuea, Wattana, BKK` — so an English parent who follows it types a spelling the
picker never stores (`พระโขนงเหนือ วัฒนา กทม`), into the column `som-report` groups by.** Pre-exists in the chat's
screen 6; the customer's text, not mine to change. 📌 Owner's list, beside the two-writers item. 🚫 Named, not built.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.

### ✅ TASK-351 DONE and REVIEWED — @Fern. **314/0, tsc 0, build ok, re-run by me.**
**Six lines on the page, two keys, the Thai SOURCE untouched** — all four label combinations asserted; values
Thai in both. 🔑 **The rewritten assertion is the model for a reversal:** name says *reversed by*, the wrong
ruling and its reason above it, the right half kept, the wrong half inverted, the `lang`-count pin moved to
three with each member NAMED. ✅ **Mutation 2 — the OVER-correction, values following the language — fails,
so the fix cannot be extended into the thing that would split the stored column.**

### 🔴 Her Question found the TASK-349 finding one level down — in the customer's own words
**`§17c` screen 6's EN half, verbatim — *Eg. Prakanueng Nuea, Wattana, BKK* — is a LABEL whose example is a
romanised VALUE** ⇒ **an English parent who takes the hint stores `Prakanueng Nuea, Wattana, BKK` while the
picker two taps away stores `พระโขนงเหนือ วัฒนา กทม` for the same address — two spellings of one place from ONE
page, into the column the report groups by.** ✅ **She changed nothing (the customer's text; pre-existing in
the chat) and named it.** 📌 **List item 19, BESIDE item 17 — the same decision, not a new one.**
🔑 **Her method is the reusable part: walk every rendered string against ONE split — *a label follows `lang`; a
value is what is stored* — and the three she CLEARED are worth as much as the one she found.**

⏱️ **State: both engineers EMPTY · the list at 19 with @Porter — 17 and 19 are one decision; 18 (`/checkin`'s
English default) is the only LIVE one · the chain has held for two days.**

### ✅ ITEMS 17 + 19 RULED by the owner — *province holds the province; the address goes into the parent's note*
> *"เก็บจังหวัดลงจังหวัด และเอาจังหวัด อำเภอ ตำบล มาต่อกัน แล้วเซฟลง note แทน"*
🔑 **Better than any of the three options put to him: the report groups correctly, the customer's line
survives, no migration, and the address lands in the field staff already read.**
✅ **Sized S from the writer, not a guess:** `createStudentFromLine` is the ONE writer for both doors and
`parents.note` already exists beside `province` ⇒ **one branch: a picked province → `province` (the admin's
full name — the dataset's 77 are already pinned EQUAL to the admin's list); the line, joined or typed →
`note`, APPENDED.** **The chat changes by construction — it already calls this writer with only the typed line
⇒ `note` only, `province` untouched. BE S · FE XS (send the picked province as its own field) · one optional
contract field · no migration.**
⚠️ **Two things @Porter did not name, sent up:** 🔴 **EXISTING ROWS keep the line in `province` — the code
fixes the future; the past needs a one-off DATA REQUEST for the human (move `province` → `note` where it
contains a space), not a migration and not ours** · the admin `Select` QA note becomes moot forward but stays
true for those rows. ✅ **Item 19 harmless now — the romanised example lands in `note`; the customer's sentence
stays.**
🚫 **Not cut — he asked for a size in one line. Waiting on "cut".**

## [--:--] Porter (PM) — ✅ `§9` cut (S, one writer). **Existing LINE-registered rows have the address in `province` — a one-off DATA REQUEST for the owner, not a migration and not the team's.**
🔑 **The chat changes by construction — it already calls the one writer.** **That is the payoff of every
"one writer, two doors" argument this week: the fourth door is free.**

### ▶️ `§9` CUT — one ruling, two halves. And `§9.1`: the owner left `province` DIRTY on purpose.
**TASK-352 (BE, @Jason, S):** `createStudentFromLine`'s input gains `address`; `province` now means the PICKED
name. PICKED ⇒ `province` ← name, `note` ← the line; TYPED ⇒ `note` only. 🔴 **`note` APPENDED, never
overwritten.** 🔑 **The chat changes by construction — it already calls the one writer.** `PROVINCE_UNKNOWN`
refuses a bad client. **Field names to @Fern FIRST.**
**TASK-353 (FE, @Fern, XS + one check):** the page sends the picked province's FULL name as its own field
while `กทม` stays in the line — *two forms of one province on one request, each in the field that wants it.*
🔴 **The check is `§9.1`'s repair path:** *"ปล่อยจังหวัดพัง ให้เขาเจอ dashboard พัง แล้วให้เขาไปไล่แก้เอง"* — **the
owner moved the existing addresses himself and is leaving `province` dirty so the broken dashboard gets fixed
by the admins who know the family.** ⇒ **the admin `Select`, fed a dirty value, MUST let an admin pick a real
province and save — verified on the component, fixed if not.** 🚫 **No cleanup, no repair button, no banner —
the broken dashboard IS the warning, by his decision. If anyone proposes a script, `§9.1` is the answer.**
🚫 **Nothing else cut. Chain stopped.**

⏱️ **State: @Jason on TASK-352 · @Fern on TASK-353 · one message when both land; he tests once with the LIFF
page · the list at 17 — 17 and 19 CLOSED by the ruling; 18 (`/checkin`'s English default) the only live one.**

## [--:--] Porter (PM) — 📝 **Pre-reset handover written: `PROJECT-STATUS.md` → `🔵 RESUME HERE — 2026-09-13`.** The owner warned the context is near full.
**It carries: where `uat` and `sid` are · the twelve rulings that must not be re-derived, each with its file ·
every open item with its holder · nine standing lessons · the next four expected moves.** 🔑 **The first move
after reset is the owner's OLD customer-facing LINE note, which he is about to hand over for rewriting.**
📌 **Nothing in it is new — every line points at a REQ, TASK or log entry that already holds it.** **A resume
section that invents is worse than none.**

## 2026-09-13 — Jason (BE)

### ✅ TASK-352 — `province` holds the PROVINCE; the address goes into `note` (`REQ-088 §9`) — **DONE (code)**
**2099 pass / 0 fail**, 170 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · codes only. **Field names sent for @Fern BEFORE the writer was touched.**
🔑 **THE RULE IS A PURE FUNCTION (`householdPatch`), so the DoD is asserted with VALUES:** PICKED ⇒ `province` = the name, `note` ← the line · TYPED ⇒ `note` ← the string, **`province` UNTOUCHED — asserted as the KEY ABSENT from the patch, not `null`, because `province: null` would CLEAR what an admin set** · APPENDED never overwritten, twice over · ข้าม ⇒ an empty patch. **The writer reads the ROW's current note**, not the argument the chat loaded at the start of its wizard.
🔑 **The CHAT changed by CONSTRUCTION — one word at its call site — and never writes `province`, by absence.** ✅ `PROVINCE_UNKNOWN` refused in the WRITER for every caller and as the named code in the composition — one function, twice. The 77 live in the BE, full form; the FE pins equal to it. 🚫 §9.1 asserted with the owner's sentence beside it. **Six mutations bite.**
🎯 **THE QUESTION — *does anything READ `parents.note` and assume it is one thing?*** 🔴 **Yes: the ADMIN FORM'S VALIDATOR, `max(500)`. The machine appends with no cap ⇒ after enough registrations the admin CANNOT SAVE THAT PARENT AT ALL** — the note the system wrote fails the admin's own rule on the next edit. *One line either way — raise the validator or cap the append — the owner's to pick; Jason would raise it.* 🟡 And a last-write-wins race with the admin's wholesale overwrite, named not proposed. ✅ Nothing prints `parents.note` on a message; nothing parses it in the BE.

⏱️ **State: `REQ-088 §9` BE half done; @Fern's half is the other; the owner's list holds the `max(500)` question.**

### ✅ TASK-352 DONE and REVIEWED — @Jason. **2099/0 (170 files), 35 = 35, tsc 0, re-run by me.** 📜 **Field names sent to @Fern BEFORE the writer, by pointer to his block.**
🔑 **A pure `householdPatch` asserted with VALUES — and the distinction that makes "untouched" true:** *the key
ABSENT from the patch, not `null`; a `null` would CLEAR an admin's value.* **The writer reads the ROW's note,
not the argument — a staff note written mid-registration survives** (mutation F re-introduces the trust).
✅ **The chat changed by ONE WORD at its call site** — *both doors got `§9` from one edit to the writer.*
✅ **`PROVINCE_UNKNOWN` in the writer for every caller; the BE holds the 77 and the FE pins equal to it.**
🚫 **`§9.1` asserted in the test with the owner's sentence beside it.**

### 🔴 His Question found a CONSEQUENCE OF THE OWNER'S OWN RULING, and it went up before it bites
**The admin's parent form caps `note` at 500; the machine now APPENDS with no cap** ⇒ ***after ~five
re-registrations the admin CANNOT SAVE that parent at all.*** ✅ **Verified at `validation.ts:339`.** 🔑 **One
line either way — raise the validator (recommended: *a form that refuses to save what the system wrote is
worse than a long note*) or cap the append (silently dropping the address — what `§9.1` was chosen to avoid).**
**The owner's pick; rides in the same deploy if he says so; not cut.** 📌 *Also named, rare: the admin's
wholesale overwrite is a last-write-wins race with the machine.*

⏱️ **State: @Fern on TASK-353 against the live route · @Jason EMPTY · the 500-cap decision with @Porter · one
message when both halves land.**

## [--:--] Fern (FE) — ✅ **TASK-353 done — the province travels on its own (full name), the line goes to `address`; the §9.1 check FAILED on (b) and is FIXED: a dirty province now shows AS ITSELF in the admin form.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  321 pass / 0 fail   (was 314; +7 — new `people/province-repair.test.ts`)
bun run build     → ok — `○ /register` still static
```
🚫 No migration · no cleanup · the join, pickers, toggle, confirm echo untouched · `ParentFormModal`'s 77 untouched.
✅ **Field names confirmed against TASK-352's `📜` block AND the live route (`routes/register.ts:37-38,65,122-133`)
before wiring.** Notes in the TASK §5.

**§2 — the send, asserted all three:** PICKED ⇒ `province: provPick.nameTh` (`กรุงเทพมหานคร`) + `address:` the join
(`พระโขนงเหนือ วัฒนา กทม`) · TYPED ⇒ `address` only (`pickedProvince` is `""` outside pick mode) · blank ⇒ neither.
Two forms of one province, each in its field — and neither swapped, asserted. `api.ts` gained one forwarding line
and no rule (no `includes(`, no list, no join — asserted; mutation 3). `PROVINCE_UNKNOWN` is the 17th code, both
languages, `{province}` interpolated, back to the form. The confirm still echoes the LINE.

### 🔴 §9.1 — checked on the component; (b) failed; fixed in 8 lines
Rendered Mantine's `Select` as the modal composes it, fed `พระโขนงเหนือ วัฒนา กทม`: **(a)** no throw ✅ · **(b) BEFORE:
an EMPTY input with the placeholder `เลือกจังหวัด` — the dirty value lived only in a hidden input. That is a bad value
hidden as blank, the exact shape the owner refused.** ⇒ `provinceOptions(current)` beside `TH_PROVINCES`: a value not
in the 77 is prepended as a `disabled` option; the 77 are returned by reference otherwise. **AFTER: the visible input
reads the dirty line; it cannot be re-picked; a real province replaces it.** Asserted on rendered HTML; mutation 4.
🚫 No button, no cleanup, no banner. **(c)** `onChange={setProvince}` · `province: province ?? null` unchanged; the
BE's admin update patches as sent (no 77-check on that door) ⇒ the pick lands ✅. *Corollary: saving something else
on a dirty parent re-sends the dirty value — nothing silently cleared.*

### 🔑 The pin, re-pointed and said so
The server's `lib/thai-provinces.ts` is the source; a test here cannot read another repo's file (per-machine paths).
Pin = dataset == `TH_PROVINCES` (77, distinct) with the comment naming `TH_PROVINCES` as this repo's CLAIM of the BE
list — **checked equal by hand today, 77 = 77, no diff.** `contract.ts`'s rule: keeping the claim in step is a human job.

### ❓ @Jason's question — no: **the parents list does not render `note` at all**; `province` on the card wraps, never
truncates (so a dirty province shows in full — the "broken dashboard" in the list too). `note` appears only in the
modal's Textarea, whole. ⚠️ Which makes his item 1 the real one: a 600-char note the Textarea shows and the 500-cap
validator will not save. Owner's list.

### 🔑 Break-and-watch ×4 — TYPED sends province · `กทม` to the column · a rule in `api.ts` · dirty hidden again ⇒
1/2/2/1 fails; md5 identical after.

### Q — bad stored value rendered as BLANK, elsewhere: 🔴 `startTime` over `TIME_SLOTS` seeded from a stored row
(`PlanModal`, `DropResumeDialog`) — **TASK-295's class**: coerced onto the grid at the seed, but a time not on the
grid at all still hides · 🟡 `PlanModal`'s `teacherId` over `bookableTeachers` (an inactive teacher opens blank —
who it WAS is invisible) · 🟡 `subjectId` over the teacher's subjects (unlocked path) · 🟡 `size` over `sizeOptions`
(the guard blanks on purpose). Not the shape: the create flows (fresh state), table filters, the nationality
control (coerced to `none`, a different smell). 🚫 Named, nothing built.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.

### ✅ TASK-353 DONE and REVIEWED — @Fern. **321/0, tsc 0, build ok, re-run by me.** 🟢 **`§9` COMPLETE, both halves. One message with @Porter.**
**Verified:** `provinceOptions(current)` puts a non-77 value in front, disabled · `ParentFormModal` feeds it ·
`api.ts` has no `joinAddress`/`TH_PROVINCES`/`includes(` · `PeopleContent` renders no `.note`.

🔴 **The `§9.1` check FAILED on its first run — which is why it was in the task.** A Mantine `Select` fed the
chat's dirty line showed an EMPTY box with the placeholder `เลือกจังหวัด`; the value lived only in a hidden
input. ***"A bad value hidden as blank — the exact shape the owner is refusing."*** ✅ **Fixed, eight lines
and one prop: the dirty value shown AS ITSELF, greyed, unpickable, above the 77 — the visible value IS the
warning.** 🚫 *No button, no banner, no cleanup.* ✅ **And the corollary stated: editing anything else on a dirty
parent re-sends the dirty value unchanged — a repair path, not a trap.**
✅ **The edge that matters: `กทม` reaching the column fails two tests** — the mutation that would have
re-created the split from the new side.
✅ **The cross-repo pin re-pointed HONESTLY: a test here cannot read the BE's file, so `TH_PROVINCES` is named
as this repo's CLAIM of the BE's list, checked equal by hand — the `contract.ts` rule applied to a list.**
✅ **@Jason's question answered with a NO that sharpens his finding:** the parents list does not render the note;
only the modal's `Textarea` does, whole ⇒ *it will SHOW a 600-character note the form cannot SAVE.* **The
500-cap is the real one; still with the owner.**

### 📌 Her Question — the `§9.1` shape at FOUR more `Select`s, on the list as ONE item
🔴 **`startTime` over the slot grid, seeded from a stored row — TASK-295's defect in a variant it did not cover:
a stored time OFF the grid still renders BLANK** in the plan editor and the drop/resume dialog · `teacherId`
(a teacher since made unbookable ⇒ empty, the admin cannot see who it WAS) · `subjectId` · `size`. **The
nationality control named as NOT the shape — it COERCES, a different smell.** 🔑 **`provinceOptions` is the
model for all four.**

⏱️ **State: `§9` complete · both engineers EMPTY · the 500-cap with the owner · one screen check for @Tanya via
@Porter · the list holds the four-`Select` item and `/checkin`'s English default (the live one).**

## [--:--] Porter (PM) — 🟢 `§9` complete (BE 2099/0 · FE 321/0 · 35 = 35). **Decided the 500-char note cap myself: RAISE it** — the machine now appends, so the cap is being hit by our own mechanism, not a human.
🎯 **@Fern found the `§9.1` repair path BROKEN by the component's default: a dirty province rendered as an
EMPTY box with the placeholder `เลือกจังหวัด`** — **the exact shape the owner refused, a bad value hidden as
blank.** ✅ **Fixed: shown as itself, greyed, unpickable — the visible value IS the warning.**
📌 **Four more `Select`s hide an off-list stored value as blank** (`startTime` off-grid · `teacherId`
unbookable · `subjectId` · `size`) — **on the list, with the owner's own ruling attached.**

## Tanya (QA) — `REQ-088 §9` province check on `sid`: ✅ **all three PASS**
Fixture found by reading the data: one parent (`88c2917c`, `0823351752`, LINE-linked) with an address line in
`province`. **List card shows it in full · Edit box shows it greyed at the top of the dropdown (not a blank
`เลือกจังหวัด`) · pick `กรุงเทพมหานคร` → Save → reopen ⇒ `กรุงเทพมหานคร`.**
🔴 **One write on a real record, instructed by @Porter, declared with the original value**
(`พระโขนงเหนือ วัฒนา กทม`) — **and flagged that this row's `note` was empty, so the address now lives only in
the report.** No LINE, no `uat`. **Next: the `adb` phone round, device check first.**

## Porter (PM) — accepted @Tanya's `§9` result: ✅ **`REQ-088 §9`/`§9.1` QA PASS on `sid`.** Board TASK-353 marked. **Told her to restore the one address line (`88c2917c`) into `note` — one write on `sid`, per the §9 ruling that the address lives in `note`.** Remaining on `REQ-088`: the owner's own phone test (link → pick address → admin shows province + note), and the customer's LIFF app + two env values for `uat`.

## Porter (PM) — 🟢 **`REQ-088` CLOSED on `sid` — the owner's phone test PASSED.** Registered `0900000096` via the LIFF link, picked an address ⇒ admin card `📍 กาญจนบุรี`, Edit form province = `กาญจนบุรี`, Note = `รางหวาย พนมทวน กาญจนบุรี`. Exactly `§9`. Observation only (same as the chat flow, not a defect): the parent's NAME is empty — the card falls back to "Parent name"; neither door asks for it. **Remaining for `uat`: the customer's LIFF app + `LINE_LOGIN_CHANNEL_ID` / `NEXT_PUBLIC_LIFF_ID` on uat.**
