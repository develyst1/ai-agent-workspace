# Log — 2026-08-20 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [02:20] Tanya (QA) — LINE leave-flow verification half (read-only, sid): effects correct; one anomaly + one DATA REQUEST. → @Porter
Verified the DB effects of the owner's 02:05 leave flow. Read-only, no writes.

✅ **The three leaves you described are recorded as `SICK_LEAVE`, each its own row (distinct ids):**
- มิลล่า **10:00** Bank · Skateboard (COURSE_PACKAGE) — the course session
- มิลล่า **11:00** Bank · Inline Skate (SINGLE_SESSION)
- มิลลิม **14:00** Dewy · Surfskate (SINGLE_SESSION)
มิลล่า's 10:00 and 11:00 are **separate rows**, and the sibling's 14:00 is its own — no cross-contamination.

✅ **Course make-up + quota (REQ-030) exact.** มิลล่า's Skateboard course (id 5d36e8c9…): **size 6 · leaveUsed 1 ·
remaining 6 (total unchanged) · liveEndDate 2026-10-01** — the make-up appended at the tail, and **2026-10-01 is
exactly the "คาบขยาย" date the bot announced.** Plan +1, count unchanged. ✅
✅ **Single session did NOT extend.** The 11:00 row is `SINGLE_SESSION`, courseId null — no course, no plan change. ✅
✅ **notify_on_leave = `admin_only`** (default, not overridden) — config confirms teacher is never messaged on a leave.

🔴 **ANOMALY — a FOURTH `SICK_LEAVE` I did not expect from your account of three leaves:** มิลลิม has **two**
14:00 Dewy sessions — Surfskate **and** Skateboard — **both now SICK_LEAVE**. That's the same child, same teacher,
same 14:00 slot, twice. Either the owner created two overlapping single sessions for มิลลิม (the system allowed a
same-student same-time overlap — itself worth a look) and both were left, or one leave tap marked two rows. **Which
was it?** If he only tapped มิลลิม once, a single leave marking two sessions would be a real defect. If he set up two
on purpose, it's fixture noise — but the same-student/same-slot overlap still shouldn't have been bookable.

🟡 **DATA REQUEST (one thing I cannot verify read-only):** "admin notified · **no** teacher notified" — the
notification **outbox has no read endpoint**. The setting is `admin_only` (above), and the unit test covers it, but
to prove it on THIS live run I need the owner to run, on sid, e.g.:
`SELECT recipient_type, count(*) FROM <outbox_table> WHERE booking_id IN ('05684f7e-...','bc89c620-...','1e85f21a-...') GROUP BY recipient_type;`
Expect `admin` rows present, **zero `teacher` rows.** (Table name is Sober's to confirm.)

**Net:** REQ-046's recorded effects match the bot on the three intended leaves — mechanically correct. **Verdict held
at IN_TEST pending** (1) your read on the extra มิลลิม row (defect vs fixture) and (2) the outbox DATA REQUEST for the
teacher-notification proof.

**Ball: @Porter** (the มิลลิม-double question → owner; route the outbox SELECT) **& @Tanya** (I close the verdict the moment those two land).

## [--:--] Porter (PM) — the มิลลิม double-leave is the one that matters. Reading the owner's own screenshots against it. → @Sober @Tanya
Tanya's verification is otherwise exact — three leaves, three distinct rows, course +1 with the total unchanged,
`liveEndDate 2026-10-01` **matching the date the bot announced**, single session correctly not extending,
`notify_on_leave = admin_only`. That is REQ-046 and REQ-030 agreeing with each other on live data.

🔴 **Her anomaly deserves more weight than "fixture noise", and here is why — from the owner's own screenshots:**
When he tapped **มิลลิม**, the bot went **straight to the confirmation** — *"แจ้งลาแล้ว: มิลลิม — 2026-08-20 14:00 น.
ครูDewy"* — **with no session picker**. The picker only appears when a child has **more than one** leavable session.
So **the bot saw ONE session for มิลลิม** — yet the database now holds **TWO** `SICK_LEAVE` rows for her at 14:00.

**Those two facts cannot both be innocent.** Either:
- **(a)** one tap marked **two** rows — a real defect, and precisely the class REQ-046 exists to prevent (a leave
  hitting a session the parent never chose); or
- **(b)** the second row was already `SICK_LEAVE`/not `CONFIRMED` before the tap — in which case the bot correctly
  saw one, and the fixture simply had a pre-cancelled row. **Innocent, but then the same-child/same-teacher/same-14:00
  overlap should not have been bookable in the first place** — Tanya is right to flag that separately.

**I am not guessing between them.** Two questions to the owner, both answerable in seconds:
1. **Did he create two sessions for มิลลิม at 14:00 with ครูDewy** (Surfskate *and* Skateboard), or one?
2. **How many times did he tap แจ้งลา for มิลลิม** — once, or more? (His screenshot ends at *"Unread messages
   below"*, so I can see one confirmation and cannot rule out a second.)

**@Sober — do not start on this yet**; if the answer is "I made two and tapped twice" there is no defect, only the
overlap question. If it is "one tap", it is a **HIGH** defect on a path that has just been declared live-tested,
and it blocks closing REQ-046.

**Tanya's DATA REQUEST (teacher-notification proof)** — the table is `notification_outbox` (it appears in
`db:reset`'s CLEAR list). I will not guess its columns, so I am asking the owner for the column list first, then I
compose the exact count query. Two short read-only steps beat one wrong query.

**Ball: owner** (the two มิลลิม questions + the column-list query) **& @Tanya** (verdict stays `IN_TEST` — correct)
**& @Sober** (standby).

## [--:--] Porter (PM) — ✅ The มิลลิม anomaly is EXPLAINED, and the explanation is a feature we shipped. No defect. → @Tanya @Sober
Owner's answers close it:
1. He created **two** sessions for มิลลิม — **one was a mistake**. He **cancelled the wrong one via LINE earlier**
   (*"รีบกดลาไป แล้วจองใหม่"*) and booked a replacement.
2. He tapped `แจ้งลา` for มิลลิม **once**, at the end — and it went **straight through with no child question**,
   because **มิลล่า had no leavable sessions left by then**, so only one child qualified.

**Every observation now lines up, and the bot was right at every step:**
- **No child picker** on the third tap → correct: the picker fires only when **≥2 children** have a leavable
  session, and มิลล่า's two were already `SICK_LEAVE`.
- **No session picker** for มิลลิม → correct: she had **one** `CONFIRMED` session left; the other was already
  `SICK_LEAVE` from his earlier mistake-cancel.
- **Two `SICK_LEAVE` rows** for her at 14:00 → correct: **one from earlier, one from the tap.** One tap did **not**
  mark two rows. **No defect.**

🟢 **And Tanya's second worry — "the same child/teacher/14:00 slot shouldn't have been bookable twice" — has a real
answer too, and it is deliberate:** migration **`0007_leave_overbook_slot_index`** makes the teacher-slot uniqueness
index **exclude `SICK_LEAVE`** (its own witness reads *"index definition contains SICK_LEAVE"*). **A cancelled
session releases its slot on purpose**, so the replacement booking is exactly the behaviour that migration exists
to allow. The "overlap" was the product working as designed.

**@Tanya — the anomaly is resolved; nothing to re-test.** One confirmation still worth having because it costs
nothing and the owner offered: the two rows' timestamps should show one cancelled **before** 02:05 and one **at**
02:06. I am asking for the column list first rather than guessing column names — same discipline as the outbox
query, and I can compose both from one answer.

**What is left before you close REQ-046 + REQ-047:** only the **outbox proof** (admin rows present, zero teacher
rows). Everything else you verified stands.

**Ball: owner** (one column-list query — it unlocks both remaining checks) **& @Tanya** (close on the outbox
result) **& @Sober** (stand down — no defect, and the "overlap" is `0007` behaving correctly).

## [--:--] Porter (PM) — composed the two closing queries from the real column list (+ one oddity worth a look). → @Tanya @Sober
Owner returned the column list; I built both remaining checks from it rather than guessing:
- **Teacher-notification proof:** `public.notification_outbox` grouped by `recipient_type` + `status` for today.
- **The มิลลิม timestamps:** `bookings` for 2026-08-20 with `created_at` / `updated_at`, scoped by date only —
  **no name join**, so the output carries uuids and times, not children's names. Same PII discipline as everything
  else that has gone through chat this week.

🟡 **Oddity I noticed and am flagging rather than silently ignoring:** the column list shows
`notification_outbox` columns **duplicated** — `id`, `channel`, `payload`, `status`, `error`, `created_at`,
`sent_at` each appear twice, while `recipient_type` / `recipient_ref` / `booking_id` appear once. The query did
**not** filter `table_schema`, so the most likely reading is **two tables of that name in two schemas** (`public`
and `bo` — this DB deliberately hosts both, REQ-006). If so it is harmless and expected, and it is why I have
scoped my query to **`public.`** explicitly. **@Sober — worth one look**: if the `bo` copy is a leftover rather
than a real backoffice outbox, that is a small cleanup; if it is real, nothing to do. Not urgent, not blocking.

**@Tanya** — on the first query's result you have what you need to close: **admin rows present, zero `teacher`
rows** ⇒ REQ-049's AC (teacher silent by default) is proven live, and with your earlier verification **REQ-046 and
REQ-047 can move to `TEST_PASSED`**.

**Ball: owner** (two read-only queries) **& @Tanya** (close the verdicts) **& @Sober** (the schema-duplicate look,
whenever).

## [--:--] Porter (PM) — ✅ มิลลิม story PROVEN by timestamps · 🔴 but the outbox is EMPTY: no admin notification fired. → @Sober @Tanya
**1. The anomaly is closed with evidence, not with an explanation.** The `bookings` rows for 2026-08-20:
- มิลลิม `1e85f21a` — created **19:22:24**, updated **19:22:33** → cancelled **nine seconds** after creation. That
  is the mis-booking he cancelled last night.
- มิลลิม `a1adabae` — created **19:22:55**, updated **2026-08-20 02:06:43** → the test tap.
- มิลล่า's two — updated **02:06:08** (11:00) and **02:06:18** (10:00) → the two test taps.
⇒ **Four rows, four distinct update times, one tap each. One leave marked exactly one session.** No defect, and
the owner's account matches the database to the second.

🔴 **2. But the first query returned NOTHING — `public.notification_outbox` has zero rows since 2026-08-20 00:00.**
Owner: *"อันบนไม่มี มีแต่อันล่าง"*. So on three real leaves, **not one notification was enqueued — not even for the
admin.** REQ-049's whole point is that a leave tells somebody.

**What I can and cannot conclude, kept apart:**
- ✅ **"No teacher was messaged" is proven** — trivially, since nothing at all was queued. `notify_on_leave` =
  `admin_only` did its job in the sense that matters least.
- 🔴 **"The admin is notified" is NOT proven — it appears not to have happened.** That is REQ-049's core AC.

**@Sober — one question, and it decides whether this is a defect or a fixture artefact:** does `notifyAdmins`
enqueue only when an **admin recipient exists** (a linked admin LINE id / a configured recipient list)? **`sid` was
wiped tonight** — parents, links and sessions all cleared — so if admin recipients come from data rather than
config, there may legitimately have been **nobody to notify**, and the code is fine. If recipients come from
config, then **three leaves produced zero rows and TASK-136 has a real hole.** Please answer from the code, not
from the test suite: the unit test proves the branch runs, not that a row lands.

**@Tanya — this does not block REQ-046/REQ-047** (neither depends on notification); those close on your verdict.
**REQ-049 stays open** with its central AC unproven, and it is exactly the kind of thing that would have shipped
looking green — the feature is "code-complete, SA-reviewed" and has now been exercised live for the first time.

**Ball: @Sober** (the recipient-source question) **& @Tanya** (close 046/047; hold 049) **& owner** (nothing —
this is ours).

## [Sober (SA)] — REQ-049 empty-outbox: answered from the code. Config artefact, NOT a TASK-136 hole — but a real silent-drop gap. → @Porter
Your question, answered from `lib/line-admin.ts` (not the tests):

**Recipients are config-in-`app_settings`, populated by an admin LINE-link.** `notifyAdmins(payload, exec, bookingId)`
(`:34-43`) does `const ids = await getAdminLineUserIds()` → reads **`app_settings.line_admin_user_ids`** (an array),
then **loops** `for (const userId of ids) enqueueLine(...)`. An admin id lands in that array only via
`addAdminLineUserId`, called when someone **types the admin code (229) in the LINE bot** (`verifyAndLink`).

**So the empty outbox is a CONFIG artefact, not a TASK-136 hole:**
- The loop iterates the id list. **Zero ids → the body never runs → zero rows enqueued** (not even a SKIPPED). The
  branch ran (Tanya confirmed the SICK_LEAVE rows); there was simply **nobody to enqueue to.**
- `app_settings` **survives the wipe** (it's in `db:reset`'s KEEP set), so this isn't the wipe clearing recipients —
  it means **no admin has ever LINE-linked on `sid`** (the array was never populated there). Your instinct — "if
  recipients come from data there may legitimately be nobody" — is right, with the precision that the "data" is the
  admin-link, kept in config.

**⇒ Two things, cleanly separated:**
1. **The immediate unblock (config, owner-run, no code):** an admin must **type `229` in the bot on `sid`** once →
   they're registered in `line_admin_user_ids` → then a leave enqueues a real admin row. **REQ-049 AC-1 can only be
   proven with an admin linked** — the fixture needs that step first, then re-tap a leave. Same step is required on
   `uat`/customer before go-live, or REQ-049 is silently a no-op there.
2. 🔴 **A real gap your test exposed — and REQ-049's own AC-4 forbids it:** with zero admins, a leave leaves **no
   trace at all**, whereas the *teacher* path writes a visible SKIPPED row on no-link. A silent leave in a
   mis-configured env is exactly "a leave that tells nobody." **Cut TASK-152 (BE, small):** when `notifyAdmins` finds
   zero recipients, enqueue **one visible SKIPPED admin row** ("no admin recipient configured") so it's loud, not
   silent — mirrors the teacher SKIPPED behaviour. Closes the hole and would have made tonight's empty outbox
   *self-explaining* instead of a mystery.

So: **not a defect in the enqueue logic** (it correctly did nothing for zero recipients), **but** REQ-049 stays open
correctly — its AC-1 needs an admin linked to be exercised, and AC-4's "never silent" needs TASK-152.

**Ball: @Jason** (TASK-152 — the visible-when-none fix) **& @Porter** (relay the config step: an admin taps `229` on
`sid` → re-run one leave → the admin row should appear; and the same is a **go-live checklist item** for uat/customer)
**& @Tanya** (close 046/047 as you said; 049 re-tests after the admin is linked + TASK-152). Not the owner's from me.

## [--:--] Porter (PM) — Sober's read confirmed (I verified the code myself) + a cheaper path than another webhook flip. → @Sober @Tanya
**Yes, he is right on both counts, and I checked rather than trusting it:**
`line-webhook.service.ts:143` → `const expected = process.env.LINE_ADMIN_VERIFY_CODE ?? "229"`. So the admin
registers **through the bot** (`สมัคร` → role **3 = แอดมิน** → the code), and admin recipients therefore come from
**data, not config**. **With no admin registered, `notifyAdmins` legitimately has nobody to write to — the enqueue
logic is not broken.** ⚠️ One caveat to relay: **`229` is the fallback**; if `LINE_ADMIN_VERIFY_CODE` is set in that
box's `.env`, the code is whatever that says.

**And TASK-152 is the right call — it is my own AC-4 read correctly.** REQ-049 AC-4 says a notification failure must
be **visible to staff, never a silent drop**. "Zero recipients" is a silent drop, and tonight is the proof: three
leaves, an empty outbox, and two roles spending an hour deciding whether the feature was broken. A **SKIPPED admin
row** would have answered that in one query.

🔴 **The finding that outlives tonight, and I want it in the go-live checklist, not just this log:** on `uat`,
**if no admin is registered, REQ-049 is silently a no-op for the customer.** They would deploy notify-on-leave and
receive nothing, forever, with no error anywhere.

**Where I differ from Sober, on sequencing only — because it saves the owner a second webhook flip at 02:30:**
he suggests registering an admin **on `sid`** and re-tapping. The webhook is already **back on `uat`**, and flipping
again is the step most likely to be left flipped. Instead:
1. **Register an admin on `uat` now** — the webhook is already there, it takes a minute, and **it is a genuine
   go-live task**, not a test fixture. It closes the hole above permanently.
2. **Verify the admin row lands** as part of **wave 2**, when `uat` has real sessions to take leave on — the same
   "expensive now, free later" trade I used for REQ-053 AC-2 and REQ-054 AC-6, and it groups the three deferred
   checks into one honest pass.
**TASK-152 proceeds regardless** — it is what makes the next empty outbox explain itself.

**@Tanya** — close **REQ-046 + REQ-047**; **REQ-049 stays open** with AC-1 (admin actually notified) deferred to
wave 2 and AC-4 pending TASK-152. Add both to the wave-2 list you are already carrying.

**Ball: @Sober** (TASK-152 to Jason) **& owner** (register an admin on `uat` — `สมัคร` → 3 → the admin code)
**& @Tanya** (close 046/047).

## [--:--] Porter (PM) — owner ran Wed+Thu on `uat` and thought rows were missing. They are not — it is paging. → @Tanya
Owner imported **Wed + Thu** on `uat` and asked why the People response looked short. **Nothing is missing.**

**The response's own `"total": 23` is the answer**, and it reconciles exactly with the four batches:
**Mon 7 + Tue 6 + Wed 4 + Thu 6 = 23 parents.** The array he was reading stops at **20 objects** because the
endpoint pages at 20 — the last three Thursday parents are on page 2. Expected students: **8 + 6 + 4 + 7 = 25**.

**Cross-checks that make this a fact rather than arithmetic that happens to fit:**
- The `createdAt` stamps cluster into **four distinct runs** — `08-18T19:14:15` (Mon), `19:14:28` (Tue),
  `08-19T19:20:50` (Wed), `19:21:12` (Thu). One timestamp per batch, exactly as the importer writes them.
- Each student carries its **source row + day** in `note` — *"แถว 21 · วันเรียน(จากไฟล์): Wed"* — so every record
  traces back to a line in the customer's sheet. **That is AC-12 and AC-15 doing their job on live data**: the
  owner can audit any child back to the spreadsheet row he coloured green, without asking us.
- The multi-child merges survived on `uat` too: `0925509898` → เอแคลร์ + อาร์ตี้; `0809682228` → ซุปซุป + ซอส —
  **one parent, two students**, from rows 29 and 32, i.e. **rows that are not adjacent in the sheet.**

**One definitive check for the owner** (read-only, two counts) so he never has to eyeball a paged list again:
`SELECT count(*) FROM parents;` → **23** · `SELECT count(*) FROM students;` → **25**.

**Where `uat` now stands: 4 of 8 batches in** (Mon · Tue · Wed · Thu). Remaining: **Fri · Sat · Sun · Voucher**
(≈ 128 rows, of which ~93 import and the rest are held/yellow).

**Ball: owner** (the two counts if he wants certainty, then the last four batches when the customer is happy).

## [Sober (SA)] — TASK-146 AC-7 LINE finish = PASS ✅. REQ-047 BE complete. → @Jason @Tanya @Porter
(Your REQ-049 confirmation + the uat-sequencing call are noted — TASK-152 was already cut and is on Jason's queue;
registering the admin on uat is yours/the owner's, and I've got nothing to add there.)

Re-reviewed the small AC-7 finish Jason added. Reproduced: `bunx tsc --noEmit` **0** · `bun test` **559/0**. It's exactly
the Q1 ruling: `doLeaveBooking`'s catch (`line-webhook.service.ts:334-338`), on `LEAVE_NOTICE_TOO_LATE`, **re-renders
`leaveNoticeMessage(cutoffHours, b.startTime, lang)` in the parent's language** — reuses the exported fn (no duplicated
copy), resolves the cut-off via `getSetting(leaveCutoffKey(teacherType))` (no service-signature change), matches the
"LINE renders bot-side" pattern. Other refusals surface the server message + `leave_err` fallback — never the old
silence. **⇒ AC-7 met on the LINE parent path; TASK-146 fully DONE; REQ-047 BE is complete** (both enforcement sites
gated · per-type editable settings · bilingual `{n}`+`{time}` refusal). Closes on @Tanya's live cut-off leave check
once the sid deploy lands.

**Ball: @Jason** (TASK-152 the visible-when-none admin fix — still open) **& @Tanya** (REQ-047 live cut-off check on
the sid deploy; close 046/047 per Porter) **& @Porter** (nothing owed from me right now — TASK-152 routed, REQ-049
sequencing is yours; the uat ledger STEP-1 dry-run still pending the owner). SA queue clear.

## [Jason (BE)] — TASK-152 BUILT → REVIEW · a leave can no longer vanish without a trace → @Sober
Picked it up off the board (TODO · Jason · no deps). Writing here — `2026-08-20.md` is the current file.

**The hole:** `notifyAdmins` looped over the configured admin ids, so with **zero** admins the loop body never
ran and a leave enqueued **nothing** — no send, and no record that a send was ever due. That's the silent drop
REQ-049's AC-4 exists to prevent, and it's why three real leaves on `sid` produced zero rows.

**The fix:** zero recipients now enqueues **one visible `SKIPPED` row** through the existing `enqueueLine`
(null recipient), carrying the `bookingId` and the payload — the same shape the teacher path has always written
on a no-link. The ≥1-admin loop is byte-for-byte unchanged.

**One small addition I want on the record:** I gave `enqueueLine` an optional **`skipReason`** (default
unchanged). Without it that row would have read `no line userId`, which is **false** — nobody's link is missing;
the *environment* has no admin configured. It now reads **`no admin recipient configured`**, so whoever reads
the outbox learns the actual fault instead of chasing a phantom link problem. One optional field; every existing
caller untouched.

**Idempotency:** untouched — TASK-136's AC-6 re-save guard still short-circuits before `notifyAdmins`, so a
re-saved leave still enqueues nothing at all.

**Verified:** `bunx tsc --noEmit` **0** · `bun test` **564 pass / 0 fail** (+5 stub-exec tests: zero admins →
exactly one SKIPPED row with the real reason · **no settings row at all** behaves the same, so a fresh
environment is loud rather than empty · one admin → one PENDING row and **no** extra skipped row · several
admins → one each · the `bookingId`/payload ride the skipped row so the trace points back at the leave).

⚠️ **Worth being precise about what this does and doesn't do:** it makes the miss **visible**; it does not
deliver the message. The live fix on `sid`/`uat` is still the config step — an admin LINE-links with code 229
per environment. On the re-test, the tell is that the same leave produces a **PENDING** row instead of a
SKIPPED one.

Board row flipped to `REVIEW` with the owner cell moved to **Sober** in the same edit, per Porter's rule.

**Ball: @Sober** (review TASK-152; my TASK-146 AC-7 finish is also still in REVIEW from last night). Everything
remains uncommitted on `dong` — including `db:reset` and `import:students`, so a fresh clone won't have them.

## [--:--] Porter (PM) — owner will run the last 4 batches himself when the customer confirms. Status doc refreshed.
Owner: *"รอฉันจะรันเองตอนที่ลูกค้าแจ้ง"* — recorded. **`uat` stops at 4 of 8 batches** (Mon·Tue·Wed·Thu = 23 parents
/ 25 students) until the customer blesses them. That is the plan we started with, and stopping there is the point
of having started with it.

**Refreshed `PROJECT-STATUS.md`** — it was two very heavy days stale, and it is the only thing that travels between
machines. It now carries: the environment table and the migration rule · **the UAT gate** (both roles' signatures,
and the explicit list of things that are *not* a green light) · Green Light #1 and what it still waits on · where
the import actually stands on each box · the ranked open work · **the deferred tests named rather than dropped** ·
the two questions the owner owes · and everything he has parked, so nobody re-raises it at him.

**One line in it I want to be able to point at later:** *"a status without an owner, or work without a log line, is
how three items went missing this week."* That is not editorialising — it is the wipe script that was never cut,
the palette file written to the wrong folder, and the TASK-150 rework that sat in REVIEW because a board cell still
named Jason.

**Ball: owner** (customer → last 4 batches; and when he has a minute: register an admin on `uat`, glance at the
palette) **& @Jason** (TASK-152) **& @Tanya** (close REQ-046/047).

## [Sober (SA)] — TASK-152 = PASS ✅. REQ-049 AC-4 (never silent) closed. → @Jason @Tanya @Porter
Reviewed the visible-when-none fix. Reproduced: `bunx tsc --noEmit` **0** · `bun test` **564/0** (+5). `notifyAdmins`:
`if (!ids.length)` enqueues **one** SKIPPED row (`recipientLineUserId: null`, `skipReason: "no admin recipient
configured"`, carrying the bookingId + payload) then returns; the ≥1-admin loop is byte-for-byte unchanged. The new
**optional** `skipReason` on `enqueueLine` (default "no line userId") is the right call — the row states the *actual*
fault (no admin in this env), not a misleading "no link". Idempotency intact (rides the sick-leave path; the AC-6
re-save guard short-circuits first). Tests pin all five cases incl. no-settings-row and the bookingId riding the row.

**⇒ REQ-049 AC-4 "never a silent drop" is closed** — a leave now always leaves a trace; tonight's mystery outbox would
have been one query. It correctly does **not** deliver the message: **AC-1 (admin actually notified) still needs the
config step** (an admin LINE-links `229` per env, on `uat` per your call) + the wave-2 live re-test.

**Ball: @Porter** (nothing owed from me — REQ-049 now: AC-4 closed in code, AC-1 on your wave-2 list + the admin-link
config step; the uat ledger STEP-1 dry-run still pending the owner) **& @Tanya** (screens/cut-off checks on the sid
deploy; 049 AC-1 in wave 2) **& @Jason** (idle on this set). SA queue clear — every open engineering item is reviewed
or waiting on a deploy/owner step. Not the owner's to receive from me.

## [--:--] Porter (PM) — engineering queue is EMPTY. Two stale lines corrected; one nudge left for Tanya. → @Sober @Tanya
**@Sober — TASK-152 accepted, and it is the right shape:** a SKIPPED row that names the **actual** fault ("no admin
recipient configured") rather than the generic "no line userId". Tonight's empty outbox cost two roles an hour;
that row would have answered it in one query. **REQ-049 AC-4 closed.**

**Two corrections so nobody works from them:**
1. Your ball line says *"the uat ledger STEP-1 dry-run still pending the owner"* — **it is done.** The owner ran
   STEP 1, then (on your pre-stated condition: 19 witnessed, ledger 4 behind, **0 blockers**) backup →
   `db:seed-ledger --apply` → `db:migrate`. **`uat` is now `Journal 20 · ledger 24 rows · 20 witnesses · GREEN`,
   and `0019` is applied.** Nothing about the ledger is outstanding.
2. `uat` has also had **4 of 8 import batches** since (Mon–Thu, 23 parents / 25 students).

**State of play — every engineering item is reviewed and nothing is waiting on an engineer:**
- Code-complete + green-lit, waiting only on a **deploy**: REQ-043 · 044 · 045 · 048 · 053 · 054.
- REQ-046 · REQ-047: **live-tested and verified** — waiting only on **@Tanya's formal verdict**, which I asked for
  after the outbox result came back. **@Tanya: close them.** The outbox being empty does not touch either REQ.
- REQ-049: AC-4 closed in code; **AC-1 deferred to wave 2** + the owner's admin-link step on `uat`.
- REQ-052: TASK-142 gated on the owner's look at the palette. TASK-147 outstanding (small, FE).
- REQ-051: blocked on **me and the owner** — three security decisions, not an engineering problem.

**So: the only things left are an owner action, a customer answer, or my decisions.** That is the healthiest the
board has looked since 08-16, and it is worth saying out loud after a week where the queue kept hiding work in
gaps between roles.

**Ball: @Tanya** (close REQ-046 + REQ-047) **& owner** (customer → last 4 batches · admin link · palette · deploy).

## [03:22] Tanya (QA) — ✅ REQ-046 + REQ-047 CLOSED: TEST_PASSED (sid). Formal verdict. → @Porter
Closing both, as asked. Evidence = the ratified split: owner's live LINE taps (bot side, his screenshots) + my
read-only DB verification (system side). Detail: `tests/TEST-056-line-leave-flow-req046-req047.md`.

✅ **REQ-046 = TEST_PASSED.** Child-first step (AC-3) · session picker `time·teacher·program` (AC-2) · confirmation
names child+session (AC-1) · refusal explains itself (AC-4) — all seen on the real OA. My DB half confirms the
**system did what the bot said**: right sessions `SICK_LEAVE` as separate rows, course make-up **+1 at the tail /
total unchanged / end 2026-10-01** (the exact date the bot announced), single session did NOT extend, quota moved
(leaveUsed 1 of 6). The มิลลิม "double leave" you explained is recorded as **not a defect**.
✅ **REQ-047 = TEST_PASSED.** The refusal carries the **configured** cut-off (3h) and the **session's own** time
(17:00), not hardcoded; the cut-off setting renders on the Settings screen (from the six-REQ pass).

**Two honest scope notes so the green light is clean:**
1. **sid-only.** DELIVERED for both still needs the customer-prod re-check after deploy, as usual.
2. **Leave-*notification* delivery is NOT in this verdict.** On the live run the outbox was empty — no admin
   notification fired (correctly no teacher either). That's **REQ-049** (silent-drop, AC-4 closed by TASK-152), and I
   have **not** re-verified a notification actually firing (needs an admin LINE recipient configured on a box). So
   REQ-046/047 pass on their leave-flow + message ACs; REQ-049's notification *delivery* stays its own open verdict.

**Ball: @Porter** (REQ-046/047 are yours to move toward DELIVERED with the rest of the green-lit set on the next
deploy) **& @Tanya** (idle — nothing waiting on me; REQ-049 notification-delivery re-check is available once an admin
recipient exists on a test box).

## [--:--] Porter (PM) — FE build failed on `@mantine/charts`. Not a code defect — a missing install on this machine.
Owner's `bun run build` in `smart-scheduler-front` failed: `Module not found: Can't resolve '@mantine/charts'`
(and its `styles.css`), from `OverviewContent.tsx` and `layout.tsx`.

**Checked before answering:** `@mantine/charts` **is** declared in `package.json` (`^9.3.2`), and
`node_modules/@mantine/` contains **core · dates · hooks · notifications · store — but not `charts`.** So the
dependency is correct and the **install on this machine is incomplete** — expected, since he moved machines and
this repo's `node_modules` predates that package being added. **No code change, no task for Fern.**

**Fix given: `bun install` in that repo, then rebuild.** If the install itself errors, that output is the next
thing I need — `@mantine/charts` pulls a charting peer, and a failed peer resolution would look exactly like this
afterwards.

**Worth one line for the deploy checklist:** the sid deploy earlier today shipped a **backend** build; this is the
first **frontend** build on this machine. **A fresh machine needs `bun install` in every repo before its first
build** — cheap to write down, and it just cost a build cycle at 03:30.

**Ball: owner** (`bun install` → `bun run build`).

## [--:--] Porter (PM) — owner asked ME for a cleanup script. Not mine to write — REQ-057 raised instead. → @Sober
Owner: *"เขียนสคริปเคลียร์ข้อมูลที่จะเกิดขึ้นหลังจากสร้างคอร์สให้หน่อย พอดีจะลองสร้างที่ uat"*.

**I am not writing it, and the reason is not procedural fussiness:** this is a **delete tool aimed at the box that
now holds the customer's real families**. A script written by the PM at 03:30, unreviewed, that removes rows by
hand is exactly the shape of the thing this project has spent a week building guards against — and we already have
two tools (`db:reset`, `import:students`) that do destructive work *properly*: dry-run by default, one transaction,
a readable report, `--commit` to act. **The third one should look like its siblings, not like a favour.**

**Raised as REQ-057 (HIGH)** — scoped to **one course by id**, dry-run default, removes the course and what it
created, **keeps the student and parent** (real people, not test data), and **refuses if any session is ATTENDED or
money/quota has moved**. Q1/Q2 to Sober: the real FK graph, and whether "money has moved" is cheaply detectable —
I explicitly told him not to take my table list as the spec.

**This gap has now cost us three times** — Tanya on REQ-053's guard, Tanya again on the LINE fixture, and now the
owner on `uat`. It is worth building once, properly.

### What I told the owner to do tonight instead
**Create the test course on `sid`, not `uat`.** Residue there is free: we can `db:reset` and re-import all 8
batches in about a minute, and have done exactly that twice. `uat` stays clean for the customer's review, and he
loses nothing but the environment name in the URL.

**Ball: @Sober** (REQ-057 — small, and Jason is idle) **& owner** (test on `sid` tonight).

## [--:--] Porter (PM) — owner wants to try the course flow on `uat` too. One fact changes the answer. → @Sober
Owner tested on `sid` and wants `uat` as well. Before arguing about residue, the thing that actually matters:

🔴 **`uat` is still running the OLD build.** Green Light #1 has not been deployed — he was building the frontend
minutes ago and it failed on the missing `@mantine/charts` install. So **a course created on `uat` right now
exercises the pre-deploy behaviour**: no course-level subject at creation (REQ-054), no planned absences at
creation (REQ-045), no read-only วิชา (REQ-053). **It would tell him nothing about the work he wants to check**,
while leaving residue on the customer's box. That is the whole answer.

**So the order is: deploy first, then test on `uat` — and by then REQ-057 may well exist.**

**If he wants to try it on `uat` anyway (his call, and I will record it as his):** do it with the smallest possible
blast radius —
1. **Create a throwaway parent + student** (an obviously fake phone), **never one of the customer's real children**
   — so no real family gets a course they never bought;
2. **Put the sessions far in the future**, outside the weeks the customer is reviewing;
3. Accept that **the sessions stay visible on the teachers' calendar** until REQ-057 lands — the product has no
   delete, and *suspending* the parent hides the family but **not** the calendar entries.

**@Sober — this raises REQ-057's priority in practice:** it is now the difference between the owner testing on the
customer's environment cleanly or not at all. Small tool, Jason idle.

**Ball: owner** (deploy first — that is also what makes the test meaningful) **& @Sober** (REQ-057).

## [--:--] Porter (PM) — owner has installed, built and DEPLOYED. Green Light #1 is out → Tanya's post-deploy re-check is now due. → @Tanya
Owner: *"ฉัน install แล้ว build และ deploy ทั้งหมดแล้ว"*. **One question I am asking rather than assuming: which box —
`uat`, or both?** It changes who has to verify what, and after this week I am not guessing at environments.

**Assuming `uat`: Green Light #1 has shipped**, which means the eight REQs it covers (**043 · 044 · 045 · 046 ·
047 · 048 · 053 · 054**) are now **live for the customer** — and per the gate rule, `TEST_PASSED` on `sid` becomes
**DELIVERED** only after the **post-deploy re-check on `uat`**. That check is the last leg and it is **@Tanya's**.

**@Tanya — post-deploy re-check on `uat`** (`frontoffice.develyst.online`), same scope as your `sid` pass, plus
the two things that only a deployed environment can show:
1. **The pages load at all** — especially `/scheduler/bookings`. **DEF-4 was exactly this**: a frontend talking to
   a stale backend crashed the page, and nothing in the code was wrong. **Confirm both halves are the new build.**
2. **The 3-tab strip, the voucher time field, วิชา read-only, the settings rows** — the six you already passed,
   re-seen on the customer's box.
3. `uat` holds **real families**: read freely, create nothing. If a check needs a course, **say so and stop** —
   REQ-057 (the scoped cleanup tool) does not exist yet and there is still no delete in the product.

**Owner's own 60-second smoke, before Tanya even starts** (it catches the DEF-4 shape immediately): open
`/scheduler/bookings` → does it render? Open the booking modal → **3 tabs**? Settings → is the leave cut-off there?
If any of those is wrong, stop and tell me rather than continuing into the course test.

**Two things he can now do that were pointless an hour ago:** the **course-creation test on `uat`** (it now runs
the new code, which is what he wanted to see), and **registering an admin** so REQ-049 stops being a silent no-op
there.

**Ball: owner** (which box + the 60-second smoke) **& @Tanya** (post-deploy re-check — this is what turns eight
`TEST_PASSED` into **DELIVERED**).

## [--:--] Porter (PM) — confirmed: deployed to `uat`. Both boxes now carry the same build. → @Tanya
Owner: **`uat`**. So `sid` (deployed earlier today, and the build Tanya passed) and `uat` are now on the **same
code** — which is the state that makes her `sid` verdicts meaningful for the customer's box rather than merely
indicative.

**Green Light #1 is deployed.** Eight REQs are live for the customer: **043 · 044 · 045 · 046 · 047 · 048 · 053 ·
054**. Per the gate rule, they sit at `TEST_PASSED` until the **post-deploy re-check on `uat`** — the last leg, and
**@Tanya's**. Brief unchanged from my previous entry; the one thing I want first is simply **that the pages render**
(the DEF-4 shape), because a half-deployed pair looks like a code bug and wastes an evening.

**Still owed on `uat`, both the owner's and both one-minute jobs:** register an admin through the bot (REQ-049 is
a silent no-op there until someone is registered), and — when he wants — the course-creation test, which now
actually exercises the new behaviour.

**Ball: @Tanya** (post-deploy re-check on `uat` → this converts eight `TEST_PASSED` into **DELIVERED**)
**& owner** (nudge her; then the admin registration whenever).

## [--:--] Porter (PM) — 🔴 The customer's "can't create a course" was almost certainly US. Cause found, and the deploy fixes it. → @Sober @Tanya
Owner reports the customer complained **before the deploy** that they **could not create a course** on `uat`, and
wondered whether the database was out of step with the backend. **It was, and I can show the mechanism:**

**`0018_course_subject.sql` ends with a conditional tightening:**
```sql
IF NOT EXISTS (SELECT 1 FROM course_packages WHERE subject_id IS NULL) THEN
  ALTER TABLE course_packages ALTER COLUMN subject_id SET NOT NULL;
```
On `uat` we had **wiped every course** an hour earlier, so there were **zero rows**, so **"no NULLs" was trivially
true** and **`NOT NULL` was applied.** From that moment the **old backend** — which knows nothing about
`subject_id` — inserted a course without it and hit a **NOT NULL violation**. **The customer could not create a
course because we had migrated the database ahead of the code that satisfies the new constraint.**

**We caused it, in a window of our own making, and the deploy the owner just did should close it** — the new
backend writes `subject_id` (TASK-138/140). **Not to be assumed: it must be verified.**

**@Tanya — add this to the post-deploy re-check, as its first item:** **create a course on `uat` and confirm it
succeeds.** It is the customer's own reported failure, so it is the one check that matters most, and it now
exercises REQ-054's create path for real. ⚠️ It **writes** — so do it only if the owner asks, and coordinate with
him; there is still no delete (REQ-057).

### 📌 Standing rule this exposes, and it is not the one we already had
Our rule says *migrations run on `sid` first, then `uat`*. It says nothing about **ordering against the app
deploy** — and that is the gap that bit the customer. Adding to the board:
> **A migration that TIGHTENS a constraint (NOT NULL, CHECK, FK, unique) must not reach an environment before the
> build that satisfies it.** Migrate-then-deploy is safe for additive changes and **unsafe for tightening ones**;
> when in doubt, deploy the code first, or run both in one window. **The `uat` gap this time was ~14 hours.**

**And a second-order lesson worth writing down:** the wipe is what *enabled* the tightening — with real courses
present, one un-derivable row would have left the column nullable and nothing would have broken. **The safest-
looking state (an empty table) is what made the constraint bite.**

**Ball: @Tanya** (post-deploy re-check, course-creation first) **& @Sober** (REQ-057 — the owner is asking for the
cleanup tool again, and the test above will create exactly the residue it is meant to remove).

## [--:--] Porter (PM) — ✅ Customer CAN create a course now. Diagnosis confirmed by the fix. Cleanup is the open ask. → @Sober @Tanya
Owner: *"ลูกค้าสร้างได้ละ น่าจะเป็นอย่างที่คิดนั่นแหละ"*. ⇒ **The `0018` NOT-NULL-ahead-of-the-deploy explanation is
confirmed by the only test that counts: deploying the matching code fixed it.** The customer went from "cannot
create a course" to creating one, with no other change. Closed.
*(The owner declined the board-rule edit I was making, so the lesson lives in this log entry rather than as a
standing rule. His call — but for whoever reads this later: **a migration that tightens a constraint must not
land on an environment before the build that satisfies it.** ~14 h gap here, and the customer felt it.)*

**His outstanding ask, which he has now made twice and I have not delivered: remove the test data.** I want it
plainly on the record that this is **not** me ignoring him — it is that **the product has no delete for courses or
bookings**, and I will not hand-write a DELETE against the box holding the customer's real families at 04:00.
**REQ-057 is the answer and it is small.** @Sober: Jason has been idle for hours; this is now the thing standing
between the owner and a clean customer environment.

**Useful preparation while REQ-057 is built — @Tanya, read-only:** list the **course ids on `uat`** with their
student, program, created_at and session count, so the moment the tool exists we know exactly which ones are test
residue and which are the customer's own. Doing that discovery *now* costs nothing and means the cleanup is one
command instead of an investigation. **Do not delete anything, and do not create anything.**

**Ball: @Sober** (REQ-057 → Jason) **& @Tanya** (the read-only course inventory on `uat`) **& owner** (nothing —
this one is genuinely ours).

## [--:--] Porter (PM) — 🔴 SITUATION RESET. Date settled = 2026-08-20. Customer is LIVE on `uat` and has built on our data. → @Sober @Tanya @everyone
The owner has changed the situation materially. **Read this before anything else today.**

### 0. Date + file discipline
**Today is 2026-08-20.** This file (`log/2026-08-20.md`) is canonical — Tanya stamped `[02:20]`/`[03:22]` and Jason
wrote here overnight. `2026-08-19.md` is closed history. **Everyone writes here.** If your session's clock says
otherwise, say so in the log rather than opening a fifth file; we lost most of a day to that already.

### 1. What changed: the customer is USING `uat`, and their work now sits on top of our imported rows
- They have **created courses** against imported students.
- ⇒ **"wipe and re-import" is dead as a correction mechanism on `uat`.** Deleting a student would take a real
  course with it. Every fix from here is an **update in place**.
- The **source Excel has been edited** by them since our import, and will keep changing.

### 2. 🔴 The owner reports the import "didn't do gender at all — the customer had to fill it in by hand"
**I am not passing this on as fact, because the evidence I hold says otherwise and I would rather be corrected than
send the team chasing a phantom.** The `uat` People API dump he pasted a few hours ago shows **`gender` populated
on every student** — `"gender": "Male"`, `"Female"`, straight from column F. So one of these is true:
**(a)** gender did import and the customer was filling something *else* (province? nickname? a field the screen
shows next to it); **(b)** the students they had to fix are ones **they created themselves**, not ours;
**(c)** a later batch behaved differently; or **(d)** my evidence is stale and something cleared it.
**I have asked the owner which he saw.** No task until we know — that is the third time this week the difference
between "a real signal" and "my reading of it" mattered.

### 3. Two REQs raised now, because both block correcting live data
- **REQ-059 — the importer must UPDATE in place** (create new · update changed · **never blank a value a human
  typed** · never touch courses/bookings/LINE links · per-row report). This is the mechanism for every correction
  from here on, gender included, whatever the answer to §2 turns out to be.
- **REQ-058 — nine new programs** the customer needs: `Bike` · `Balance Cruiser` · `Balance Bike` · `Scooter` ·
  `Inline Skate & Bike` · `Surfskate & Bike` · `Surfskate & Inline Skate` · `Bike & Scooter` ·
  `Surfskate & Freeskate`. 🔴 **And the structural finding: `subjects` rows exist only in `db/seed.ts` — there is no
  screen, no API, no way for the school to add a program.** Nine rows is today's symptom; "an engineer for every
  future program" is the defect. Also flagged: a program with **no teacher attached is unbookable**, and
  `price_group` drives money — so this is not an INSERT, it is a small piece of setup with three owner questions
  (price group per program — **especially the five combined ones** · which teachers · voucher eligibility).

### 4. Still open from before, unchanged
REQ-057 (scoped course cleanup — the owner has asked twice) · Tanya's post-deploy re-check on `uat` · the last four
import batches · REQ-052's palette · REQ-051's three decisions.

**Ball: @Sober** (REQ-058 + REQ-059 — both block the customer; REQ-057 still open) **& @Tanya** (post-deploy
re-check; and hold the course-inventory list) **& owner** (the Excel, and which field the customer was really
filling in).

## [--:--] Porter (PM) — 📁 THIS FILE IS CLOSED — and several of tonight's entries were written into it BY MISTAKE.
**The owner has corrected me: today is 2026-08-22.** I had settled on 08-20 and I was wrong.

**Two things follow, and the second one may matter more than the filing:**
1. **Everything I appended "tonight" — the `uat` deploy, Green Light #1 shipping, Tanya's REQ-046/047 verdicts,
   the LINE flow evidence, REQ-057/058/059 — is physically in THIS file but happened on 2026-08-22.** I am not
   rewriting entries (mine or anyone's) to hide that. **Read them here; continue in `log/2026-08-22.md`.**
2. 🔴 **There may be a real clock problem, and this is a scheduling product.** The filesystem stamps my writes
   tonight as **08-22 02:00**, the owner says **08-22** — yet during the LINE test the bot treated sessions dated
   **2026-08-20** as **"วันนี้"**, and his Windows clock read **8/19** in screenshots a few hours earlier.
   **If a server's date is wrong, then "today's sessions", the check-in window, REQ-047's leave cut-off, and the
   08:00 digest are all computed on the wrong day** — silently. I am asking the owner to compare the date on his
   machine, on the `sid` server and in the app before anyone treats this as a filing mistake only.

**Team: write in `log/2026-08-22.md` from now.**
