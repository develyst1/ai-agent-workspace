# 2026-08-10 — smart-scheduler

> **Continued from `log/2026-08-04.md`** (that file had lagged across several real days; the machine clock is
> authoritative — 2026-08-10). New entries go here. Board is the durable index.

## [Porter (PM)] — Day open. Driving the essential set (REQ-038 #1–5) to 100%. Live ball-state → ALL
Nothing ran overnight; picking up from yesterday's EOD. Where we stand:

### ✅ Shipped & DELIVERED (deployed sid + zero-defect acceptance)
REQ-030 · REQ-037 · OBS-3 · REQ-009 · REQ-022.

### 🎯 Essential set (REQ-038 #1–5) — ~90%, two things to close
- **#1 ✅** deployed · **#2 ✅** closed (TASK-121, calendar course-picker) · **#5 🔄** deduction-history: TASK-119 (BE)
  ✅ DONE, **TASK-120 (FE) in REVIEW** — waiting on @Sober.
- **#3 / #4 🟡** — `sid` runtime verify still owed by @Tanya: **#3** REQ-011 search present on the timetable/schedule
  page (not only bookings); **#4** REQ-029 voucher shows its class/subject in the FE.

### The two moves that get us to 100%
1. **@Sober — review TASK-120** (deduction-history FE). On PASS, #5 is build-complete and the essential set's only
   real build is done.
2. **@Tanya — `sid`-verify #3 + #4.** If either is a gap, it's a small FE add; flag @Porter.
→ When both land: **essential set = 100% → @Porter prepares the customer-production deploy** (their real env is
behind on code AND migrations — a bigger event than the sid push; I'll get the deploy-gate + prod migration state
from @Sober before writing that runbook).

### Parallel / not on the 1–5 critical path
- **@Jason — TASK-123** (rental price, greenlit) · fast-follow FE (102/107/109) already PASS.
- **🟡 OWNER (phone only):** `tests/CLICK-SCRIPTS-owner.md` Scripts 4 + 5 → closes REQ-020 + REQ-023.
- **@Tanya backlog (unhurried):** REQ-037 revenue re-check after next day-end · `kind=voucher` plan shape · DEF-1 (MINOR).

**Ball: @Sober** (TASK-120 review) **& @Tanya** (#3/#4 verify) — the two that close the essential set. Chain: I set
priority; Sober dispatches build.
## [Sober (SA)] — TASK-120 PASS ✅ → **essential set #5 build-complete.** Move 1 done; ball → @Tanya + @Jason
(Reviews were recorded in `2026-08-04.md`; carrying the conclusions forward here since day rolled. Board is DONE on all.)
- **TASK-120 (deduction-history FE) = PASS** — read-only server-rendered timeline; verified the one real risk (the
  hyphen-key transform `no-show→kindNo-show`, `makeup-appended→kindMakeup-appended`) — **all 9 kind labels present
  EN+TH**, none falls through to a raw key. Actor "who not tracked" note shown. tsc 0 reproduced. With TASK-119 (BE,
  PASS), **#5 is build-complete.**
- **Essential set #1–5 = BUILD-COMPLETE.** #1 shipped · #2 (TASK-121) done · #5 (119/120) done · **#3/#4 = verify-only
  on `sid` → @Tanya** (your Move 2). That's the whole remaining gate to 100%.

### Fast-follow (parallel, off the 1–5 path) — all SA-reviewed
- TASK-107 (voucher picker) · TASK-109 (rental FE) · TASK-102-unblock via TASK-122 (settings DELETE/reset) — **all
  PASS**. Only open build: **TASK-123** (rental *price* display) — greenlit @Jason; rental recording already works,
  this is display-only.

### For your customer-prod deploy runbook — the deploy-gate + prod migration state you asked me for
- **Migration ledgers are split per repo** (REQ-032): scheduling → `__drizzle_migrations_scheduling`, backoffice →
  `__drizzle_migrations_bo`. `db:migrate` = `drizzle-kit migrate && db:verify` (the verify guard is what caught the
  sid ledger gap). **Customer prod is behind on BOTH code and migrations** — so the deploy is `db:migrate` per repo
  (witness-based `db:seed-ledger-from-schema` only if verify flags orphan/again-wrong-hash rows, as on sid), then app.
- I'll write the per-repo deploy-gate checklist when you're ready to sequence it — say the word.

**Ball: @Tanya** (#3/#4 `sid` verify + accept #5 → closes the essential set) **& @Jason** (TASK-123). Nothing on SA.

## [Tanya (QA)] — #4 PASS · **#3 is a real GAP (`TEST_FAILED`)** · #5 can't be accepted — not deployed. Voucher shape closed. → @Porter

Picked up Move 2 the moment the day opened. Read-only round on `sid`: **nothing created, nothing sent.**
Full write-up `tests/TEST-REQ038-essential-set-3-4-5.md`; evidence `../project-docs/qa-2026-08-10/`.
(Clock question from yesterday answers itself — you rolled the log to **2026-08-10** and the machine agrees.
That's what I'm dating evidence by from here.)

### 🔴 #3 — "Search Bar หน้าตาราง" is **NOT built**. The board's mapping doesn't hold.
The BA map reads **"#3 = REQ-011 DELIVERED 🟢"**, and REQ-011 *is* delivered — but it is a **different
thing**: it fixed the **student picker not filtering while typing inside the New Booking modal**
(accepted 2026-07-29). The customer's #3 asks for a search bar **on the timetable page itself**.
On `sid`, `/scheduler/calendar` has exactly three controls: **Teacher** (`All teachers`), **Type**
(`All types`), **Badge** (`All badges`) — **no student search anywhere on the page**, and no other search
affordance (buttons are just `Sign out · 11 Aug 2026 · Today` + the slot cells). The same search **does**
exist on the bookings page, which is exactly why this reads as done from the board.
- **Verdict: #3 `TEST_FAILED` — a gap, not a regression** (nothing existing is broken; it's unbuilt).
  Filed as **DEF-2, MEDIUM**, with the full control listing + screenshot as evidence.
- 🔑 **This is the one worth pausing on:** it was mapped to an existing DELIVERED REQ, so it would have
  gone to the customer as "done" without a runtime look. The map wasn't careless — REQ-011 really is about
  student search — it's just that "search students" and "search students *from the timetable*" are two
  screens. **@Porter — scope it as its own task, or fold it into REQ-038's build?** Your call; it's a
  small FE add per your own note.

### ✅ #4 — voucher shows its class/subject → `TEST_PASSED`
API: `GET /bookings?type=VOUCHER` → **5/5 rows carry a subject**. FE renders it in both places a staff
member would look: the All-bookings table (`Student · **Subject** · Teacher · Date · Time · Type · Status`
→ **1st Trial · Skateboard · Skateboard · Inline Skate · Surfskate**, no blanks) and the voucher plan
modal per session (`5 Jan · 09:00 · Bank · **Surfskate**`).

### ⛔ #5 — deduction history: **cannot accept, it isn't deployed**
`GET /courses/:id/history` → **404** on `sid` (tried a real course and my QA one). TASK-119 + TASK-120 are
build-complete and SA-PASS, but the build isn't on the server, so there's nothing for me to run. **Deploy
step, not a defect.** It's a read-only timeline, so acceptance will be one short pass the moment it lands.

### 🎁 Backlog item closed — the `kind=voucher` plan shape is now VERIFIED
This sat `NOT TESTED` since 2026-08-04 because the only live vouchers were other people's rows. My own QA
voucher serves, and the check is read-only: DTO is the voucher discriminant
(`{kind:"voucher", totalHours:5, usedHours:0, hoursRemaining:5, expiryDate:"2026-04-05"}`, `liveEndDate=null`,
**`insertable=false`**); the modal renders **`VOUCHER · 5 / 5 h left · Ends no live sessions`** — not the
course shape; its **only action is `Edit`** (no Insert, no Mark absence, no Add extra); and it tells the
user why: *"Voucher sessions can be moved one at a time (no make-up chain)."* **PASS across the board.**

### Where the essential set actually stands, from my side
| Item | State |
|---|---|
| #1 | ✅ shipped + accepted (REQ-030) |
| #2 | ✅ TASK-121 done (not separately QA'd — it's a dropdown label; say the word if you want it verified) |
| #3 | 🔴 **`TEST_FAILED` — unbuilt** (DEF-2) |
| #4 | ✅ **`TEST_PASSED`** |
| #5 | ⛔ build-complete but **not deployed** — acceptance blocked |

So **100% needs two things that aren't mine**: build #3, and deploy #5. Neither is blocked on QA.

**Ball: @Porter** — (1) route #3 (own task or into REQ-038), (2) get #5 deployed and I'll accept same day.
Unchanged and unhurried on my side: REQ-037 revenue re-check after a day-end · DEF-1 (voucher Manage at
375, MINOR) · Scripts 4 + 5 still waiting on the owner's phone for REQ-020/023.

## [Porter (PM)] — Owning the #3 mapping miss. Build #3 + sequence the essential-set-completion deploy → @Sober
Good catch, Tanya — and thank you for **not trusting the board map without a runtime look.** **My BA error:** I mapped
REQ-038 #3 to REQ-011 (DELIVERED) on the board; REQ-011 is the **student picker inside the New Booking modal**, a
different screen from a **search bar on the timetable/calendar page**. #3 is genuinely **unbuilt** (DEF-2). Correcting
the REQ-038 map now. The lesson stands for the rest of REQ-038: a REQ-name match is a hypothesis, not a verdict —
runtime confirms.

**Two moves to get the essential set to 100% (both mine to route, neither blocked on QA):**
1. 🔴 **#3 — build the timetable-page student search.** It's a REQ-038 essential item (not a separate REQ) — folding
   it in. **@Sober — please scope it small:** a **student search on `/scheduler/calendar`** that filters the visible
   schedule by student (alongside the existing Teacher/Type/Badge filters). Likely an FE add reusing the existing
   student-search the bookings page already has. Cut the task → Fern (via you). Route any design call to me.
2. ⛔ **#5 — deduction history is built + SA-PASS but not on `sid` (404).** It needs deploying so Tanya can accept.
   **Plan (efficient): ONE sid deploy that completes the essential set** — bundle **#5 (119/120) + #2 (121) + the
   built #3** + the ready fast-follow (107/109/102, and 123 if it lands) — then Tanya accepts the lot in one pass →
   essential set 100% on `sid` → then the customer-prod deploy.
   - **@Sober — give me the deploy-gate for that bundle** (migrations? which apps? same-sitting?) once #3 is built,
     same as the REQ-030 batch. If #3 drags, I'll deploy #5 on its own so acceptance isn't held — but the clean path
     is one deploy.
- **#4 ✅ accepted.** **#2** — it's a dropdown label; I'll take Tanya's offer to spot-verify it in the same deploy pass
  rather than separately. **`kind=voucher` plan shape closed** (bonus) — nice.

**Ball: @Sober** (scope+cut #3 → Fern · deploy-gate for the completion bundle) **& @Jason** (TASK-123). Then a sid
deploy (me → owner) + Tanya's one-pass acceptance closes REQ-038 1–5.

## [Porter (PM)] — ✅ Deployed to `sid` (#5 deduction-history + fast-follow). Acceptance active → @Tanya
Owner deployed the scheduling build to `sid` 2026-08-10 (same flow; `db:migrate` self-verify — flag me only if it went
red). Now LIVE: **#5 deduction-history (`/courses/:id/history`)** + fast-follow (voucher picker 107 · rental 109 ·
settings screen 102/122).
- **@Tanya — accept #5 now** (you said one short read-only pass): open a course's history timeline on `sid`, confirm
  it renders (what · when · reason; the 9 kind labels EN+TH; the "who not tracked" note) and that a real course
  returns 200 (was 404 pre-deploy). Set **#5 `TEST_PASSED`**.
- Also in this pass (quick): **spot-verify #2** (TASK-121 — the calendar course-picker dropdown when a student has >1
  course) and a smoke of the fast-follow (voucher picker omits Onewheel/Balance · rental entry · settings screen loads
  + reset works).
- After this: **the ONLY thing left for REQ-038 1–5 = #3 (timetable search)** — being built by Fern (via Sober).

**Ball: @Tanya** (accept #5 + spot #2/fast-follow) **& @Sober/@Fern** (build #3). #5 accepted + #3 built+deployed = 1–5 at 100%.

## [Tanya (QA)] — ✅ **#5 `TEST_PASSED`** · #2 spot-verified · fast-follow (107/109/102/122) all smoke-clean → @Porter

Ran it the moment you posted the deploy. Detail in `tests/TEST-REQ038-essential-set-3-4-5.md` (round 2
appended); harnesses `tests/harness/sid-req038-5*.mjs`, `sid-req038-2-and-rental.mjs`; screenshots
`../project-docs/qa-2026-08-10/`.

### #5 deduction history → **`TEST_PASSED`**
`GET /courses/:id/history` is live (404 this morning → **200** now), returning `{courseId, summary, events}`.
On my QA course: **8 events, 6 distinct kinds** — `scheduled · sick-leave · attended · cancelled ·
makeup-appended · extra-session-added` — every one traceable to something I actually did to that course on
08-04, which is the best evidence the timeline is real and not decorative. Summary
`{size:4, usedSessions:1, leaveUsed:1, remaining:3, liveEndDate:"2026-09-08"}` matches the plan.
The modal renders `USED: 1 OF 4 · LEAVE USED: 1 · REMAINING: 3 · ENDS: 8 SEP 2026` then dated entries with
teacher/subject. **The one risk SA flagged — the hyphen-key transform — holds: I scanned the rendered text
for `kind[A-Z]…` / `history.…` and found no raw key.** The actor note is shown verbatim: *"Who made each
change isn't tracked yet — the branch shares one login."*
- **Scope stated plainly:** I exercised **6 of the 9** kind labels — the ones this course's history
  produces. `no-show`, `freelance-drawn`, `freelance-refunded` were SA-verified in code; I did not
  manufacture events just to photograph their labels.

### #2 (TASK-121) → spot-verified ✅, with one small observation
The picker offers one entry per course, labelled with subject **and** used/size:
`QA-expv · Bike / Scooter / Balance Cruiser (1/4)`. Exactly the intent.
- ⚠️ **OBS-5 (LOW), and my data is what exposed it:** my student's two courses share subject, size **and**
  progress, so both options render as the **identical string**. Nothing is booked wrong (the value is keyed
  by `courseId`), but a human can't tell them apart. The customer's real case is two *different* programs,
  where the label works — so I'm flagging an edge, not a defect. **Worth a tiebreaker in the label (start
  date?), or leave it?** Your call.

### Fast-follow smoke — all clean
- **TASK-107** ✅ and verified the *right* way: I derived the excluded set from the server's own
  `voucherAllowedGroups=["bike-skate"]` ⇒ **Onewheel E-Skate · Balance Play (Private) · Balance Play
  (Group)** — **none offered** in the voucher picker. `1st Trial` (no price group) *is* offered **by
  design** (`sellable.ts`: don't over-hide what the FE can't classify) and the server is the backstop —
  confirmed live: booking a voucher onto it → **409 `VOUCHER_PROGRAM_EXCLUDED` "วอยเชอร์ใช้กับคลาส Onewheel
  หรือ Balance Play ไม่ได้"**, nothing created. REQ-027(b) is now proven end-to-end.
- **TASK-109** ✅ "Record rental" on the All-bookings tab opens `Record an equipment rental · Equipment * ·
  Hours *`.
- **TASK-102 / 122** ✅ settings screen loads with both rules and their defaults; the reset round-trip works:
  `PUT value=5` → `{value:5, isOverridden:true}` → `DELETE` → **`{value:3, isOverridden:false}`**.
  **Environment restored and verified back to exactly what I found** (that override is the only write I made
  today, and it is undone).

### 🔍 Four FAILs in my first pass were MINE, not the product's — all re-run and green
`entries` vs the real key **`events`** · a regex that matched "Balance **Cruiser**" (an *allowed* program) ·
looking for the rental button on the calendar when it lives on the All-bookings tab · typing in the search
box without opening the Select beneath it. I'm recording them because **a red result from QA costs someone a
debugging session** — a FAIL is a claim about the product and has to survive checking before it leaves my
desk. Fixes are in the harnesses (the TASK-107 one now derives the excluded set from the server instead of
guessing from names, so it can't drift).

### Where the essential set stands
| Item | State |
|---|---|
| #1 | ✅ accepted (REQ-030) |
| #2 | ✅ spot-verified (OBS-5 noted) |
| #3 | 🔴 **the only open item** — `TEST_FAILED`/unbuilt, in build via Sober/Fern |
| #4 | ✅ `TEST_PASSED` |
| #5 | ✅ **`TEST_PASSED`** |

**So 1–5 hits 100% the moment #3 lands and is deployed** — I'll verify it the same day it ships.

**Ball: @Porter** — nothing of mine is blocking. Open questions: OBS-5 (label tiebreaker?) and the standing
two: REQ-037 revenue re-check after a day-end · Scripts 4 + 5 for the owner's phone (REQ-020/023).

## [Sober (SA)] — #3 scoped+cut (TASK-124 → @Fern) · completion-bundle deploy-gate · OBS-5 ruled → @Porter @Fern
### 🔴 #3 (timetable student search) — scoped small, cut to Fern. **FE-only, no BE/migration.**
Traced the calendar page: the badge filter (`CalendarContent.tsx:35` `byBadge`) is already a **client-side per-booking
predicate** over the loaded calendar, and `Booking.studentName` exists (types :118). So #3 is the **same shape** — add
a `byStudent` filter + a search box in the existing filter row (`CalendarHeader`). No endpoint, no server round-trip
(bookings are already in hand). **TASK-124** written → @Fern:
- `CalendarContent`: `studentQuery` state + `byStudent(byBadge(...))` into the day/week memos.
- `CalendarHeader`: a clearable `TextInput` (name substring, case-insensitive) beside Teacher/Type/Badge; i18n
  `calendar.studentSearch` EN+TH. STANDING RULE — measure the 4-control row at 1600/1280/768/375.
Any design call (match-only column collapse) routes to you; I kept the cut minimal (filter bookings, leave columns).

### 🚀 Completion-bundle deploy-gate (you asked, same as the REQ-030 batch)
Because #5 + fast-follow (107/109/102/122) are **already deployed + accepted on sid**, the completion bundle is now small:
- **#3 (TASK-124)** — **scheduler-front only. NO migration, no BE.** Deploy = rebuild/redeploy the front.
- **TASK-123 (rental price)** if it lands in time — **scheduling-back** (a computed response field from constants,
  **NO migration**) **+ scheduler-front** (consume). Still no schema change.
- **Gate:** ✅ **no `db:migrate` needed** for this bundle (nothing touches schema). Apps = **scheduler-front** (always)
  **+ scheduling-back** (only if 123 is bundled). Not same-sitting-critical — no ordering hazard without a migration.
  Standard app redeploy; `db:verify` will simply confirm the ledgers are already green (they are, from the 08-10 push).
- If #3 drags, deploying it alone later is safe (FE-only). One clean deploy is still preferable per your plan.

### 🟡 OBS-5 (label tiebreaker) — SA ruling: **cheap FE-only expiry tiebreaker, LOW, off the critical path.** TASK-125.
Two courses identical in subject+size+progress render identical TASK-121 labels. The customer's real case (distinct
programs) already works — this is a synthetic-data edge — but "bought two of the same package" is plausible. Clean fix
uses `expiryDate` **already in `CourseContext`** (zero BE): when a student has 2+ course entries, append the expiry so
identical packages differ. **TASK-125** cut (LOW/optional) → @Fern; it rides the next FE deploy, **does not gate the
essential set.** Residual: same package bought same day → same expiry → still fungible; accepted (no ugly courseId
fragment).

**Ball: @Fern** (TASK-124 = the last essential-set build; TASK-125 LOW after) **& @Jason** (TASK-123). When 124 lands
→ SA review → your one-deploy completion → Tanya same-day accept → **REQ-038 1–5 = 100%** → customer-prod deploy.

## [Sober (SA)] — TASK-124 (#3) PASS ✅ + TASK-125 (OBS-5) PASS ✅ → **REQ-038 essential set 1–5 is BUILD-COMPLETE.** → @Porter @Tanya
### 🟢 TASK-124 — REQ-038 #3 timetable student search. PASS (reproduced).
- Built to the two-edit spec exactly: `CalendarContent` composes `byStudent(byBadge(...))` into **both** day/week
  memos with `studentQuery` in the deps; case-insensitive substring on `Booking.studentName`; teacher columns left
  as-is (matched student's sessions appear under their teachers). `CalendarHeader` gets a clearable `TextInput`
  (Search icon, `CloseButton` one-click clear) first in the filter row; `calendar.studentSearch(+Placeholder)` i18n
  **EN+TH**. `Search` icon imported. Pure client-side — **no BE/endpoint/migration**.
- **Evidence:** `bunx tsc --noEmit` → **0** (ran it). **The one open item — the live 4-width pixel measure of the now
  4-control filter row — is a NextAuth-gated runtime check → @Tanya** (same as prior width passes). Structurally the
  row degrades by **wrapping** (`flex-wrap` + `min-w-*` + `basis-0 grow`), not clipping — the same proven pattern as
  the existing 3 controls, so I'm passing the code and routing the pixel confirmation to your acceptance pass, not
  blocking on it. **#3 build-complete.**

### 🟢 TASK-125 — OBS-5 expiry tiebreaker. PASS (reproduced). LOW/off critical path.
`eligibleLabel` appends ` · exp <expiryDate>` **only** when the same student has 2+ course entries
(`eligible.filter(x=>x.id===e.id).length>1`); single-course and voucher rows untouched; `value` still `courseId`,
no BE. Accepted residual (same package, same day → same expiry) documented. tsc 0.

### ✅ REQ-038 essential set 1–5 = BUILD-COMPLETE
#1 shipped · #2 done · #3 (TASK-124) built · #4 accepted · #5 (119/120) accepted. **#3 is the last build.** It just
needs to reach `sid`.
- **Deploy-gate (restated for #3):** **scheduler-front only, NO migration, no BE.** If TASK-123 lands, add
  scheduling-back (computed field, still no migration). No ordering hazard; `db:verify` confirms the already-green
  ledgers. Standard app redeploy.
- **@Porter:** deploy #3 to `sid` → **@Tanya** one-pass accept (#3 runtime + the calendar filter-row width at
  1600/1280/768/375) → **essential set = 100%** → the customer-prod deploy you flagged (that one DOES carry the code+
  migration backlog for their env — I'll write that per-repo runbook when you sequence it).

**Ball: @Porter** (deploy #3 → completion) **& @Tanya** (accept #3 + width) **& @Jason** (TASK-123, optional in the
same deploy). Nothing on SA except the customer-prod runbook on your go.

## [Porter (PM)] — Deploying #3 to `sid` (scheduler-front only). TASK-123 NOT bundled (still @Jason). → owner, @Tanya
Fast work — #3 (TASK-124) + OBS-5 (TASK-125) both SA-PASS; essential set 1–5 is BUILD-COMPLETE. Handing the owner the
#3 deploy: **scheduler-front ONLY · NO migration · no BE** (checked TASK-123 = still greenlit-not-built, so it's not in
this deploy; it's fast-follow, follows later). Simplest deploy in the project: `bun run build` (front) → copy → `pm2
restart scheduler-front`. No `db:migrate` (front doesn't touch schema).
- **@Tanya — on "it's up," accept #3:** the timetable student search on `/scheduler/calendar` (clearable box beside
  Teacher/Type/Badge; case-insensitive name substring filters the visible schedule) + the **STANDING-RULE 4-control
  filter-row width at 1600/1280/768/375** (Sober passed the code; the pixel measure is the NextAuth-gated runtime bit).
  On PASS → **REQ-038 1–5 = 100%.**
- Then **@Porter → @Sober:** the word to write the **customer-prod per-repo deploy runbook** (that env carries the
  code+migration backlog, unlike sid) — I'll sequence it the moment #3 is accepted.

**Ball: OWNER** (deploy scheduler-front) → **@Tanya** (accept #3 + width) → 1–5 at 100% → customer-prod.

## [Tanya (QA)] — ✅ **#3 `TEST_PASSED` — DEF-2 closed. REQ-038 essential set 1–5 is 100% accepted.** → @Porter

The front deploy landed and I ran the acceptance. Detail in `tests/TEST-REQ038-essential-set-3-4-5.md`
(round 3); harnesses `sid-req038-3-accept.mjs` + `sid-req038-3-dayview.mjs`; screenshots in
`../project-docs/qa-2026-08-10/`. **Read-only round — nothing created.**

### #3 timetable student search → `TEST_PASSED`
The page that had **three** filters this morning now has **four**: `Find student` (placeholder *"Search by
student name"*), first in the row.
- **It filters for real:** week view **6 cells → 2**, both `QA-expv-student`; the other four students'
  sessions disappear. Case-insensitive (`QA-EXP` = `qa-exp` = 2). A nonsense query → **0 cells** (it filters
  rather than silently ignoring the box). Clear → back to **6**.
- **Day view too** — the bit most likely to be missed: on 2026-08-11 the day grid paints 2 sessions, a
  matching query keeps them and a non-matching one empties the grid. (Both that day belong to one student,
  so the *discriminating* proof is the week view's 6 → 2; the day view proves the filter is wired into that
  memo at all.)

### STANDING RULE — the row is now four controls, measured on the deployed build
| Viewport | Find student | Teacher | Type | Badge | Lines | Narrowest | h-scroll |
|---|---|---|---|---|---|---|---|
| 1600 | 218 | 366 | 220 | 220 | 2 | 218 | none |
| 1280 | 208 | 233 | 132 | 132 | 2 | 132 | none |
| 768 | 208 | 207 | 115 | 624 | 3 | 115 | none |
| **375** | **317** | 247 | 247 | 247 | **4** | 247 | none |

✅ Nothing collapses — the defect this rule exists for was **26/36 px** — and the row **wraps** (2→3→4
lines) instead of crushing. No page overflow at any width. Sober's structural call was right; here are the
pixels behind it.

### OBS-5 → resolved by TASK-125
The two same-program courses now read distinctly: `… (1/4) · exp 2026-09-15` vs `… · exp 2026-09-21`. My
observation from this afternoon is closed. (SA's documented residual — same package bought the same day →
same expiry — isn't reachable with my data and stays accepted.)

### 🏁 REQ-038 essential set 1–5 — 100% accepted from QA
| #1 | #2 | #3 | #4 | #5 |
|---|---|---|---|---|
| ✅ accepted (REQ-030) | ✅ spot-verified | ✅ **`TEST_PASSED`** | ✅ `TEST_PASSED` | ✅ `TEST_PASSED` |

**Nothing in the essential set is open on my side.** DEF-2 (this morning's gap) is closed the same day it
was found.

### 🔍 Two more harness false-negatives I caught before they reached you
1. The **day grid looked empty** — the week view renders `10:00 | Student`, the day view renders
   `Student | Subject | TYPE` **with no leading time**, so my time-anchored regex read it as empty. That
   would have been filed as "the search doesn't work in the day view" — a defect that does not exist.
2. **"API says 3 sessions, the grid paints none"** — my day-picker counted **cancelled** bookings, which
   the grid correctly doesn't draw.
Both were re-checked before anything was written down. The rule I keep applying: **when a check says the
product is broken, prove the harness isn't first** — that's now three such catches today, and it is the
cheapest quality work I do.

**Ball: @Porter** — 1–5 is done from QA; the customer-prod runbook is yours to sequence. Still standing and
unhurried on my side: REQ-037 revenue re-check after a day-end · DEF-1 (voucher Manage at 375, MINOR) ·
Scripts 4 + 5 waiting on the owner's phone (REQ-020/023) · TASK-123 (rental price) whenever it ships.

## [Porter (PM)] — 🏁 REQ-038 1–5 = 100% ACCEPTED. Sequencing the CUSTOMER-PROD deploy — Sober, write the runbook → @Sober
Milestone: the customer's essential set is done, deployed to `sid`, and zero-defect accepted end-to-end. Per the owner's
plan, the next move is **deploying to the customer's real production** (`frontoffice.develyst.online`) so they actually
use it. **@Sober — the word is given: please write the per-repo customer-prod deploy runbook** you offered.
- ⚠️ **This is NOT the sid push — it's the riskier one.** Customer prod is **behind on both code AND migrations**, real
  customers use it, and it's the "first-serve planned event." The runbook needs: **per-repo `db:migrate`** (scheduling
  + backoffice), the **`db:verify` gate**, and the **witness-based `db:seed-ledger-from-schema` fallback** if verify
  flags orphan/wrong-hash rows (as it did on sid) — with a clear STOP-and-tell-Porter at each gate.
- **Scope of this deploy = everything built since the customer's last prod build** (it's ~weeks behind): REQ-030/037/031/
  027/028 + OBS-3 + REQ-038 1–5 + fast-follow. Please state the **exact app set** (I expect all 4: scheduling back+front
  **and** backoffice back+front, since prod is far behind) and any **same-sitting gates** across them.
- **Prod migration state is unknown** — the runbook should establish it (verify), not assume it. Flag if prod needs a
  pre-deploy DB snapshot/backup step (real customer data — I'd want one before migrating).

**Ball: @Sober** (customer-prod runbook) → then @Porter walks the owner through it. Not touching it until your gate is
written — prod + real customers = we do this carefully, once, right.

## [Sober (SA)] — 📕 Customer-prod deploy runbook WRITTEN → @Porter (your go given)
`smart-scheduler/DEPLOY-CUSTOMER-PROD-2026-08-10.md` — grounded in the real scripts/configs, not improvised. The
answers to your specific asks:
- **App set = ALL FOUR** (backoffice back+front + scheduling back+front), because prod is weeks behind — confirmed by
  reading each `package.json`/`drizzle.config.ts`.
- **Both backends migrate**, one shared prod DB (both read `DATABASE_URL`), **two separate ledgers**
  (`__drizzle_migrations_bo` / `__drizzle_migrations_scheduling`) — REQ-032, so they can't clobber each other.
- 🔴 **ORDER gate I found and made loud:** `backoffice-back` **must migrate before** `scheduling-back` —
  `backoffice/drizzle/0004_bo_schema.sql` *creates* the `bo` schema and scheduling's `0017_entitlement_source`
  *references* `bo.*`. Wrong order → `relation "bo.…" does not exist`. Restart order mirrors it (backoffice pair → scheduling pair, same sitting).
- **Per-repo `db:migrate` = `drizzle-kit migrate && db:verify`**; 🔴 STOP-and-tell-Porter on any red; **witness fallback**
  `db:seed-ledger` (`scripts/seed-ledger-from-schema.ts`) → re-`db:verify` if verify flags orphan/wrong-hash rows (the
  exact sid failure mode). Targets stated: backoffice newest `0005_bo_item_external_ref`, scheduling `0017_entitlement_source`.
- **GATE 0 (prod-only, not on the sid runbook):** ✅ **mandatory prod DB snapshot/backup BEFORE any migrate** (your
  instinct — real customer data); confirm both `.env` `DATABASE_URL`s point at **prod** and the **same** instance;
  set `PUBLIC_CALENDAR_BASE_URL` (TASK-044 ics) + prod env. "Prod migration state is unknown → the `db:verify` steps
  **establish** it, they don't assume it."
- Mechanism unchanged from every prior deploy (build local → copy → migrate-local-hits-remote → pm2 restart same
  sitting) + a **rollback posture** section (snapshot restore only with you+owner in the loop; migrations don't auto-down).
- **Brownfield:** I did **not** run anything against prod — owner executes, you walk it, I'm on call for any 🔴 gate.

TASK-124's open runtime item (calendar filter-row width 1600/1280/768/375) is folded into the post-deploy Tanya smoke
so it closes in the same prod acceptance pass.

**Ball: @Porter** (walk the owner through the runbook; STOP→me at any red gate) **& @Jason** (TASK-123 — bundle it iff
SA-reviewed before you deploy, else next). Nothing else on SA until a gate fires.

## [Porter (PM)] — Owner is mid-deploy at GATE 0. Needs a `db:backup` script (good call) → @Sober
Walking the owner through the customer-prod runbook. Status: **`.env` → prod done** (owner updated the DATABASE_URL;
I reminded to confirm BOTH backends' `.env`). Blocked at **GATE 0 backup** — I gave a raw `pg_dump "<url>"` one-liner
and the owner rightly pushed back: *"ทำไมไม่ทำเป็น package.json script … ให้มันอ่านจาก env ไปเลย."* Agreed — pasting the
prod connection by hand is exactly what we avoid.
- **@Sober — please add a `db:backup` script** to the backend `package.json` (I'd put it in `smart-scheduler-back`,
  or wherever you prefer — it's the one shared DB): **`"db:backup": "pg_dump \"$DATABASE_URL\" > sm-prod-backup-$(date
  +%Y%m%d-%H%M%S).sql"`** — `bun run` auto-loads `.env`, so `$DATABASE_URL` resolves without the owner ever typing the
  connection. Owner then runs **`bun run db:backup`** at GATE 0. Small tooling add; blocks the prod deploy's backup gate.
  If you want a safer default (custom format for selective restore), your call — the owner just needs one `bun run`.
- Everything else on the runbook is ready; this is the only thing between the owner and starting the migrate steps.

**Ball: @Sober** (cut the `db:backup` script → @Jason) → owner runs it → GATE 0 done → STEP 1 (backoffice migrate).

## [Sober (SA)] — `db:backup` cut as a robust script (not the inline one-liner) → @Jason (HIGH, owner waiting)
Agreed with the owner's push-back — no hand-pasted connection. But Porter's inline `pg_dump … > backup-$(date …).sql`
has two traps I fixed in the spec:
- 🔴 **Owner is on Windows** — `$(date +%Y%m%d-%H%M%S)` is unreliable under Bun's shell there → broken filename.
- 🔴 **`>` redirect writes a file even if `pg_dump` fails** → a partial file that looks like a backup.
**TASK-126** = `scripts/backup.ts` (same mold as `verify-migrations.ts`/`seed-ledger-from-schema.ts`) + a
`"db:backup": "bun run scripts/backup.ts"` line. It reads `DATABASE_URL` from the bun-loaded `.env`, writes a
**custom-format `-Fc`** archive via `pg_dump -f` (pg_dump owns the file — no false-positive on failure), **exits
non-zero** if pg_dump is missing/fails or the file is 0 bytes, and prints the path + size + the `pg_restore` restore
command. Full copy-paste content is in the task so it's a trivial Jason turn. **Precondition:** `pg_dump` on PATH —
I've added that to GATE 0 in the runbook, and pointed GATE 0 at `bun run db:backup`.

**Ball: @Jason** (drop in TASK-126, `tsc`, → me for a quick review) → owner runs `bun run db:backup` → GATE 0 clears →
STEP 1 backoffice migrate. This is the only thing between the owner and the migrate steps.

## [Sober (SA)] — pre-deploy diligence while GATE 0 waits on TASK-126: both migration journals are internally consistent ✅ → @Porter
Checked both backends' `drizzle/meta/_journal.json` against their `.sql` files (de-risks STEP 1/2 before the owner is
mid-prod-migrate):
- **backoffice-back:** journal == files, **0000–0005** (6), newest `0005_bo_item_external_ref`, `bo` schema at 0004. ✅
- **scheduling-back:** journal == files, **0000–0017** (18), newest `0017_entitlement_source`. ✅
- No orphan journal entries, no un-journaled `.sql` — so `drizzle-kit migrate` can't hit a missing-file error and the
  runbook's stated targets are exact. The **only** prod-side unknown is whether prod's ledger *rows* match those files
  (pre-existing tables the ledger may not witness — the sid failure mode); STEP 1/2 `db:verify` establishes that and the
  `db:seed-ledger` witness fallback resolves it. Nothing new to fix here — it's a green pre-check.

**Ball unchanged: @Jason** (TASK-126 `db:backup` — HIGH, owner at GATE 0; then TASK-123) → I review 126 the moment it
lands → owner runs `bun run db:backup` → GATE 0 clears → STEP 1. Nothing else actionable on SA until 126 posts.

## Jason (BE) — TASK-126 → REVIEW. `db:backup` for GATE 0. **This is the only thing between the owner and STEP 1.** → @Sober
Posted the moment I saw it was blocking the prod deploy. tsc 0.
- **`scripts/backup.ts`** — built verbatim from your spec (same mold as `verify-migrations.ts`). Reads `DATABASE_URL`
  from the bun-loaded `.env` (never a hand-pasted connection); **Windows-safe timestamp** via
  `new Date().toISOString().replace(/[:.]/g,"-")` (pure JS — no shell `date` strftime trap); `pg_dump -Fc -f` so
  **pg_dump owns the file** (no partial-`>`-on-failure false positive); **exits non-zero** on: no `DATABASE_URL`,
  pg_dump missing/failed (`!proc.success` → `exitCode||1`), or a 0-byte file; on success prints path + size (MB) + the
  `pg_restore --clean --if-exists` restore command.
- **`package.json`** — `"db:backup": "bun run scripts/backup.ts"` beside the other `db:*`.
- **Not executed here** — `db:backup` connects to a real DB, which the brownfield rule forbids me to do; the guard
  paths (no-URL / pg_dump-fail / 0-byte) are verified by inspection and the DoD explicitly waives a unit test (it
  shells out). Precondition stands: **`pg_dump` on the owner's PATH** — it's in your GATE 0.

**@Sober: 126 ready for your quick review** → owner runs `bun run db:backup` → GATE 0 clears → STEP 1 backoffice
migrate. **Ball: @Sober** (126 review, HIGH — owner waiting). My earlier apologies — I was working off the 08-04 board
and reported "queue drained"; I hadn't pulled the 08-10 deploy state where this HIGH task was waiting. Fixed now.
Next after this: **TASK-123** (rental prices — already posted to REVIEW on 08-04; re-confirming it's in your queue).

## [Sober (SA)] — caught a skipped review: TASK-102 (settings screen) was LIVE-but-unreviewed → now **PASS ✅** → @Porter @Fern
While GATE 0 waits on Jason's TASK-126, I swept the board and found I'd **never SA-reviewed TASK-102** (the settings
screen) — Fern pushed it to REVIEW (08-04 → @Sober), Tanya smoke-tested it on sid, and it deployed in the fast-follow,
but it skipped my code review while I chased the essential-set critical path. Reviewed it now:
- **Renders from the API list** (`rows.map` over `useSettings()`), one Card per rule → the "a 3rd rule needs no FE
  change" AC holds **by construction** (not a hardcoded form). ✅
- **Override(orange)/Default(gray)** badge straight off `row.isOverridden` — unmistakable. **Edit** → `NumberInput` →
  PUT; a rejected value shows the **server's exact reason** in a red Alert (never silently accepted). NumberInput is
  `min=0` only (no per-key max) — **deliberate: the server is the bound authority**, no duplicated bounds table on the
  FE (same no-second-copy discipline as the price card). ✅
- **Reset** shown **only when overridden** → confirm Modal → `api.delete('/settings/:key')` (TASK-122, the honest
  reset). Both `useUpdateSetting`/`useResetSetting` **invalidate `SETTINGS_KEY`** on success → the badge flips to
  Default with no manual refetch. Per-row spinner keyed by `reset.variables===row.key`. ✅
- **Evidence:** `bunx tsc --noEmit` → **0** (ran it). **TASK-102 DONE.** REQ-031 (settings) is now fully SA-reviewed
  end-to-end (mechanism 101 · reset 122 · screen 102).

🧹 **Housekeeping note (non-urgent):** several REQ-030-batch task files (092/093/096/097/098/099/113/115) still show
`Status: REVIEW` in their headers though the work is **DELIVERED + accepted** (board is truth; I verified those
in-code during the ship). Stale bookkeeping, not open work — I'll reconcile the headers to the board on a quiet tick
unless you want them left as-is.

**Ball unchanged on the deploy: @Jason** (TASK-126 `db:backup`, owner at GATE 0). TASK-102 was the one genuine
SA-actionable item off the critical path — now closed.

## [Sober (SA)] — TASK-126 REVIEW = **PASS ✅** (reproduced). **GATE 0 unblocked** → owner runs the backup → @Porter @Jason
`scripts/backup.ts` + `"db:backup"` built exactly to spec — every guard present:
- missing `DATABASE_URL` → exit 1; `pg_dump -Fc -f <file>` (pg_dump owns the file → no false-positive on failure);
  `proc.success` false → non-zero exit with a "do NOT proceed past GATE 0" message; **0-byte guard**; prints path +
  size + the `pg_restore` restore line. Timestamp via `new Date().toISOString()` — **Windows-safe** (no shell `date`).
- Jason correctly did **not** execute it (brownfield — it connects to a real DB). `bunx tsc --noEmit` → **0** (I ran it).

**GATE 0 is unblocked.** @Porter → owner: from `smart-scheduler-back` (its `.env` already pointed at prod), run:
```
bun run db:backup
```
Proceed only on the printed `✅ Backup written … (NN MB)` + exit 0 (precondition: `pg_dump` on PATH). Then **STEP 1 —
backoffice-back `db:migrate`** (owns `bo.*`, migrates before scheduling), STOP→me on any `db:verify` red.

**Ball: @Porter/owner** (run `db:backup` → STEP 1) **& @Jason** (TASK-123 — bundle iff SA-reviewed before deploy).
I'm on call for any 🔴 gate through the migrate steps.

## [Porter (PM)] — 🛑 GATE 0 BLOCKED: prod DB rejects the laptop's IP. Owner wants to run ON the server. Runbook needs a variant → @Sober
The `db:backup` guard fired correctly (no bad backup written) — but the real blocker: **prod DB
`154.197.124.29` has no `pg_hba.conf` entry for the owner's laptop IP** (`110.171.40.169`). So the runbook's core
assumption — *"migrate locally hits the remote prod DB"* — **does not hold for prod** (sid allowed the IP; prod doesn't).
- **Owner's setup:** he **owns the prod server**; the **prod DB + all 4 apps live on it.** He **declines IP-whitelisting**
  (doesn't want to manage `pg_hba`/tunnels) and — correctly, security-wise — wants to **run backup + migrate ON the
  server**, where the DB is `localhost` and already allowed. That's the *more* secure posture: prod DB never accepts a
  remote connection at all.
- 🔴 **The catch (why this needs you):** the server holds only the **compiled apps** (`bun build --compile` .exe / `.next`)
  — **no source, no `drizzle/` migrations, no `scripts/`, possibly no `bun`.** So `bun run db:migrate` / `db:backup`
  can't run there as-is. **@Sober — please write a "run-on-the-Windows-prod-server" variant of the deploy runbook:**
  - What to get onto the server (the two backend repos' `drizzle/` + `scripts/` + `package.json` + a `bun install`? or
    the full source? minimal set preferred), how to install/portable-`bun` on Windows Server, and `DATABASE_URL=localhost`.
  - Whether **backup** can just be plain **`pg_dump` on the server → localhost** (if `pg_dump` is present) rather than the
    bun script, while **migrate** uses the drizzle tooling once it's on the box.
  - Same gates as before: backoffice-back migrate **before** scheduling-back · `db:verify` green or STOP · witness fallback.
  - I've asked the owner for two facts to ground it: `pg_dump --version` + `bun --version` **on the server**, and whether
    he reaches it via **RDP or SSH**. I'll relay them.

**Ball: @Sober** (author the run-on-server runbook variant, grounded in the owner's two answers) **& owner** (report
`pg_dump`/`bun` presence on the server + RDP/SSH). Deploy is paused at GATE 0 until this path is set — correctly.

## [Porter (PM)] — Owner's two facts back: server HAS both tools. Green light to author the run-on-server runbook → @Sober
On the prod Windows server (owner is on it via **RDP**, `cmd.exe` as Administrator):
- ✅ **`pg_dump --version` → PostgreSQL 18.4** — present. Backup can run on the server against `localhost`.
- ✅ **`bun -v` → 1.3.14** — present. So `bun run db:migrate` / `db:backup` **can run on the server** once the tooling
  (the two backends' `drizzle/` + `scripts/` + `package.json` + `bun install`) is there. `DATABASE_URL=localhost`.
- **Access = RDP** (a desktop session on the box; not SSH/scp — file transfer is RDP clipboard/drive-redirect, or the
  same copy method the owner already uses to ship builds to this server).

**@Sober — you have what you need; please write the run-on-server runbook variant.** Concretely for the owner (who
says he doesn't know DB infra — keep it copy-paste, cmd.exe-friendly):
- The **minimal set to place on the server** for each backend (I'd guess the repo folder minus heavy build output, or
  just `drizzle/ + scripts/ + package.json + src` enough for drizzle-kit + the scripts) and the **`bun install`** step.
- **`.env` on the server = `DATABASE_URL` pointing at `localhost`** (the prod DB is local there) — confirm the exact
  local DSN shape (port 5432, db `smart_scheduler`, user `postgres`).
- **GATE 0 backup on the server:** `bun run db:backup` (now local → allowed) — or plain `pg_dump -Fc` to a file if
  simpler; your call which is cleaner given both are present.
- Then the unchanged gates **on the server**: backoffice-back `db:migrate` → verify → scheduling-back `db:migrate` →
  verify (STOP on red, witness fallback), then the apps (already built + copied) just `pm2 restart` in order.
- Windows/cmd.exe notes (path separators, how `bun run` picks up `.env` there).

**Ball: @Sober** (write `DEPLOY-CUSTOMER-PROD-ON-SERVER.md`, copy-paste for cmd.exe) → I walk the owner through it on
the server. Everything else on the deploy is ready; this is the last setup before GATE 0 clears.

## [Porter (PM)] — 🧭 LIVE DEPLOY-STATE HANDOFF (session restart mid-deploy). Owner chose Option A. → @Sober, next-Porter
Session is being restarted (my tool calls were malforming). **We are MID customer-prod deploy, paused at GATE 0.** Pick
up exactly here:

### Where we are
- Deploying **everything since the customer's last prod build** to `frontoffice.develyst.online` (real customers).
  Runbook: `smart-scheduler/DEPLOY-CUSTOMER-PROD-2026-08-10.md`. REQ-038 1–5 is 100% accepted on `sid`.
- 🔴 **GATE 0 blocker:** prod DB `154.197.124.29` rejects the owner's laptop IP (`no pg_hba.conf entry for host
  110.171.40.169`). Migrate/backup **from the laptop** can't reach prod. (Sid works because sid's `pg_hba` allows **any
  host with the password**; prod's doesn't.)
- **DECISION — Owner chose Option A:** temporarily open prod's `pg_hba` like sid (allow-all-with-password), run the
  deploy from the laptop as usual, then **CLOSE it again after**. Server: **PG 18.4 · bun 1.3.14 · access = RDP.** Prod DB
  already listens on the network (the error reached the `pg_hba` stage) → **only a `pg_hba.conf` line + reload** is
  needed, no `listen_addresses` change.

### Immediate next action for the fresh session
- **@Sober — give the owner EXACT copy-paste steps (PG 18, Windows Server, RDP):** (1) the `pg_hba.conf` line to ADD
  (e.g. `host all all 0.0.0.0/0 scram-sha-256` — confirm method vs existing entries) + the file location on Windows PG18;
  (2) the **reload** command (no restart); (3) the **revert/close** step to run AFTER the deploy. Ground it, don't guess.
- Then the deploy runs on the LAPTOP with the existing runbook, unchanged: `bun run db:backup` → backoffice-back
  `db:migrate` → verify → scheduling-back `db:migrate` → verify → build all 4 → copy → `pm2 restart` (backoffice pair →
  scheduling pair). STOP→Porter on any `db:verify` red (witness fallback `db:seed-ledger`).
- **After deploy:** Tanya smoke on `frontoffice.develyst.online`; then **owner CLOSES the `pg_hba` line** (don't forget).

**Ball: @Sober** (exact temp-open/reload/close pg_hba commands) → Porter walks owner through the deploy → close pg_hba
after. This one line is all that stands between us and GATE 0.

## [Sober (SA)] — GATE 0 unblocked WITHOUT opening prod: run-on-server runbook written → @Porter
The temp-open-`pg_hba` path (Option A) is a **prod security loosening**, and I won't author it off a decision that
lives only in the workspace files — that needs the real human's explicit go-ahead (my harness blocked it, correctly).
It's also unnecessary: the owner is already **on the server via RDP** with **pg_dump 18.4 + bun 1.3.14**, and the prod
DB is **local** there. So backup + migrate run **on the server against `localhost`** — already allowed — and prod's
`pg_hba` is **never opened to a remote IP**. This was the owner's own first instinct.

**`smart-scheduler/DEPLOY-CUSTOMER-PROD-ON-SERVER.md`** — grounded, cmd.exe copy-paste:
- **Clean split:** build the 4 apps on the **laptop** (no DB needed) + copy as usual; run the **DB ops on the server**
  (localhost). One shared DB → **one `db:backup`** covers everything.
- One-time: copy each **backend's** source (minus node_modules) to the box → `bun install` → `.env` DATABASE_URL =
  `postgresql://postgres:<PWD>@localhost:5432/smart_scheduler` (+ `PUBLIC_CALENDAR_BASE_URL`). Fronts don't migrate.
- Gates unchanged: STEP 0 `db:backup` → STEP 1 **backoffice-back** `db:migrate`+verify (owns `bo.*`) → STEP 2
  **scheduling-back** `db:migrate`+verify → STEP 3 `pm2 restart` (backoffice pair → scheduling pair). 🔴 STOP→Porter on
  any `db:verify` red; witness fallback `db:seed-ledger`→verify. Targets: bo `0005`, scheduling `0017`.
- Rollback posture + "no prod ingress opened" footer.

If the owner still prefers deploying from the laptop, the ONLY safe form is a **laptop-`/32`** temp-open (never
`0.0.0.0/0`) with a mandatory close+verify — but it needs the real human's explicit authorization; I've flagged that
up. The run-on-server path avoids the question entirely and I recommend it.

**Ball: @Porter** (walk the owner through `DEPLOY-CUSTOMER-PROD-ON-SERVER.md` on the box) → STEP 0 backup → STEP 1.
I'm on call for any 🔴 gate. @Jason: TASK-123 still open, off the deploy path.
