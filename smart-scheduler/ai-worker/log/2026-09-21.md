# 2026-09-21 — smart-scheduler log

## DUO re-spec (REQ-095 §13) — RESOLVED
- Owner forwarded Porter's A/B question to customer (Khwan). Customer answered **B** ("ต้องมาเรียนพร้อมกัน ทุกคาบ"), then pinned down: **"1 คอร์ส 2 คน"**, apologised for earlier "คนละคอร์ส" confusion.
- Owner asked the concrete consequence verbatim (shared hours/leave/expiry pool, one kid's leave cuts the shared pool) → customer **"ใช่ค่ะ"**. LOCKED.
- Customer's own "easy way" + example: create as ONE Private course, both names inline — e.g. course **"Duo Inline Skate 10 Hr"**, students **"น้องคราม & พราว"**, 10 Hr shared.
- Recorded to `REQ-095 §13.1/§13.2` (locked spec). Current Stage-2a "2 linked courses" DUO is superseded → rework to 1-course/1-entitlement/2-students.
- Dispatched to @Sober (inbox/SA.md): READ + contract + sizes for the rework; no build until contract confirmed. GROUP (3+) untouched.
- **@Sober** — Porter: DUO re-spec LOCKED by the customer (one shared course, two kids). Wrote **SPEC-087** (co_student on course + sessions, course-level rate, one sale, ~12 reader/notice sites, retire the GROUP-row DUO for creation) and cut **TASK-420** (BE L, contract first, `0048` ⇒ 49) + **TASK-421** (FE M–L). Six owner points + a DATA REQUEST (count of uat DUO groups). Ball @Jason (420 contract).

## Jason (BE) — 📋 TASK-420 contract in the TASK: §1's table of 16 student-resolving sites with line numbers; 🔴 A: no chokepoint for course facts on a row today (insertBooking + two direct clones, `inheritCourseRental` per writer) — proposed insertBooking reads the course's `co_student_id` + the clones copy it; 🔴 B: the LIFF leave flow authorizes by `studentId` only — child 2's family cannot take leave on a DUO session (❓ in scope); C: a fourth private family-accounts read in course-deduction ⇒ ONE union helper; D: the price through the existing DUO branch, no second mapping; SOM counts both children. Four ❓ to Sober; waiting on confirm before code.
- **@Sober** — TASK-420 contract from @Jason CONFIRMED (four yes: `insertBooking` + clones as the chokepoint with an `insert(bookings)` census; Finding B — the LIFF leave for child 2 — in scope; the 4th family-accounts copy retired into `householdLineUserIds`; `duo`+`groupKey` ⇒ 400). TASK-421 §0 rewritten. Porter told the two finds. Ball @Jason (build).

## Jason (BE) — ✅ TASK-420 CODE DONE: DUO = ONE course, TWO kids. `0048_duo_course` (49 = 49; preflight `[]`); 2630/0 (+47 tests), tsc 0; the `insert(bookings)` census pinned (inserter reads the course once, two clones copy); `householdLineUserIds` replaces the four private family reads (none remains, by source); notices/reminder/deduction/CRM/LIFF/search/SOM reach or name both children; the rate course-level (PATCH course + session move; Private ⇒ NOT_DUO; never posted); group DUO retired for creation only; 40/40 mutations bite. Two facts flagged to @Sober (a DUO leave is one leave; `familiesNotified` counts batches). Awaiting review.
- **@Sober** — TASK-420 re-verified (2630/0, tsc 0, 49 = 49; one `householdLineUserIds`; `GROUP_KINDS_CREATABLE`) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-421. Porter told the owner point (a leave by either family cancels for both; the other family not told — a new placeholder kind if wanted) + Tanya's DUO checks. Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-421 done — DUO = one course, two kids: the Private/DUO toggle (second child + rate, the DUO card by name), `A & B` everywhere through ONE pure `studentLabel` in the ONE mapper, the rate box on Move-session + the course card, `Create group` = Group only.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  515 pass / 0 fail   (was 505; +10)
bun run build     → ok
```
12 source modified · 1 test re-pinned · 3 new. Built to §0 + Jason's final lines (TASK-420). Snapshot unchanged (56, no
key). Notes in the TASK §2. 🚫 No deploy asked.
`studentLabel` value-tested (a Private byte-identical; a DUO `A & B`, the co's nickname else name), applied in the ONE
mapper (`displayName`, the course view's `studentName`) ⇒ both grids, the Bookings table, the modal header, the trays
and the course card show both names with no call-site edit. New course: the segmented toggle (default Private, hidden
inside a group) ⇒ second `StudentSelect` + a baht rate box; the DUO card by its group name `balance-duo` (no number on
the FE); `duo` in the body only when on, never beside a `groupKey`; the same-child hint. Move-session: the rate box on a
DUO session only, prefilled from the course, sent only when changed. The course card: `DUO` tag + `Rate n ฿` with a
pencil by `course-edit` ⇒ `PATCH /courses/:id` only when changed. `Create group`: Group only; existing DUO series still
render. 📌 Two readings stated: no course cards on the People page (the course list is one list searched by either
child); the plan title reads `coStudent` if the plan reader sends it (optional). Copy `course` +11 ×2.
🔑 **Break-and-watch ×19 in `try/finally`** ⇒ 1–5 each (one slipped first — the rate line's gate shadowed by the tag's —
pinned to two gates); md5 identical on 8 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-421 re-verified (515/0, tsc 0; one `studentLabel`) ⇒ **✅ DONE — REVIEWED**. Fern's two readings checked: no People course cards (fine); `getEntitlementPlan` lacks `coStudent` — verified ⇒ **TASK-422** (XS) to @Jason. Porter told. Ball @Jason (422).

## Jason (BE) — ✅ TASK-422 CODE DONE: the entitlement plan carries `coStudent · classRateMinor · courseKind` through ONE `duoCourseFacts` builder shared with the course DTO; 2634/0 (+4), tsc 0, 49 = 49; census +3; 4/4 mutations bite. Shape note (the plan's thin `student` ref vs the mapper's `coStudent` ref) flagged to @Sober. Awaiting review.
- **@Sober** — TASK-422 re-verified (2634/0, tsc 0, 49 = 49; one `duoCourseFacts` builder) ⇒ **✅ DONE — REVIEWED**. **The `sid` batch is whole** (095 incl. §11 + §13 · 096 · 097 · 098 · 099 · the 094 fix; migrate ⇒ 49). Porter told the deploy steps and the open items. Chain stopped; ball @Porter.

## DUO rework BUILT (Sober → SPEC-087, TASK-420/421/422)
- Shape: DUO = a Private course + a 2nd child on the same rows (`course_packages.co_student_id` + `class_rate_minor`; `bookings.co_student_id` on every session). Shared pool by construction (session = the unit). Work was in readers/notices (~12 sites; coach line reads "คราม & พราว"). One sale at the DUO card price. GROUP (3-12) untouched. GROUP-row DUO retired for creation.
- BE 2634/0, tsc 0, `0048_duo_course` verify 49. FE 515/0, build ok. New-course Private/DUO toggle (2nd child + ค่าสอน), rate editable on course card + session popup, `Create group` offers Group only. Child 2's family can take leave via LINE (gap Jason's table found + fixed). Household notices reach BOTH households via one helper (4 private copies retired).
- Whole sid batch ready: REQ-095(1→3b + §11 camp-grid + §13 DUO) · 096 · 097(+500 fix) · 098 · 099(+year) · 094 fix. Deploy: BE+FE → db:migrate (0042→0048, verify 49) → sale:ensure-items (--dry-run 8, then apply) → grants (menu:camp+camp.*, calendar.group-series, people.parent-archive) + Teacher role + links; camp_reminder_enabled OFF until owner says.

### OPEN — owner decisions (nothing else builds until):
1. ⭐ CUSTOMER-FACING: confirm customer understood the shared-pool rule — EITHER family's leave cancels that session for BOTH kids; one make-up carries both. Sober: the one place a family could feel short-changed.
2. New notice? tell the OTHER family when the partner takes leave (currently NOT told). Recommend yes (S, needs owner's words).
3. SPEC-087 §3 six points — all Sober-recommended defaults: (1) sale→child1's household; (2) rate course-level, editable from course + move dialog; (3) CRM points to BOTH on DUO check-in; (4) reports = one course/two students; (5) existing uat Stage-2a DUO groups = let run out (no migration); (6) = rule #1 above.
4. DATA REQUESTs (owner runs, read-only uat): (a) `SELECT count(*) FROM bookings WHERE booking_type='GROUP' AND other_kind='DUO';` (b) ledger dump (TASK-085).
5. Parked: disable-frees-teacher-link question.

## DUO shared-pool leave rule — CUSTOMER CONFIRMED (2026-09-21)
- Owner forwarded the rule verbatim (either family's leave cancels that session for BOTH; one shared make-up; cannot let one attend alone). Customer confirmed. → SPEC-087 §3(6) locked. GREEN-LIT sid deploy of the whole batch.

## sid DEPLOY DONE + Tanya dispatched
- Owner ran on sid: db:migrate hit TASK-085 ledger silent-skip (0044/0045/0046) → db:seed-ledger --apply (3 rows) → db:migrate → db:verify = ✅ 49 applied / ledger 80 / witness 49 / no 🔴. sale:ensure-items dry-run = 32/32 present, 0 to create (DUO items already there) → no apply. Code built + restarted (restart done).
- Dispatched @Tanya (inbox/QA.md): full round, DUO one-course as priority + Sober's checklist, plus §11 camp-grid / 096 / 094 / 097 / 098 / 099. Report per item; only her PASS closes.

## Tanya (QA) — full round on sid (verify 49). ⭐DUO one-course (SPEC-087) PASS: one course/2 kids (coStudent,courseKind DUO), grid cell carries BOTH student+coStudent, one shared deduction, CRM to BOTH (+5 each: KKTEST 95→100, Prao 0→5), leave⇒one shared make-up, rate editable course-card+session-popup (Private⇒400 NOT_DUO), create-group DUO⇒400. Open DUO bits = LINE-delivery(confirm to both families, make-up both-names on notice, family-2 LIFF leave) + exact sale 14,200 (bo ledger auth 404'd) — closeable on demo OA. Minor: /api/bookings (table surface) coStudent:null/single-name while /api/calendar (grid) carries both — quick FE/BE check. Also: REQ-099 birthday+year PASS (paired-range guards 400, wrap ok, noDob 35); REQ-100 voucher cancel PASS (remaining 5 unchanged); §11 camp-grid PASS (10:00-15:00 block per teacher, clash 409 SLOT_TAKEN naming camp, close-week⇒0 blocks); 098 re-PASS (409 PARENT_HAS_SESSIONS→200); 094 re-PASS (EXTENDED confirm 200); 097 validated (re-confirm token glitch 401, not a defect); 096 job-gated. Footprint: 0 live; left voucher 0dcaffdd, closed camp week, archived Prao/098 families, re-disabled Bank acct. Report in inbox/PM.md.

## Tanya QA round — PASS on sid (verify 49)
- ⭐ DUO one-course (SPEC-087): PASS on logic/DB/grid — one course/two kids (duo+groupKey⇒400), both names on the GRID (/api/calendar carries student+coStudent), shared pool one deduction, CRM to BOTH (+5 each), leave=shared (one make-up EXTENDED), rate editable course-level + session popup + survives teacher move (Private+classRateMinor⇒400 NOT_DUO), Create group offers Group only (group DUO⇒400).
- REQ-099(+year) PASS · REQ-100 voucher-cancel PASS (balance unchanged, draws at check-in) · §11 camp-grid PASS core (block 10-15, clash 409 naming camp, close week⇒gone) · REQ-098 archive re-PASS · REQ-094 EXTENDED confirm re-PASS · REQ-097 validated (re-confirm 401 = test token glitch, not defect) · REQ-096 job-gated (owner verifies on uat).
- 🟠 OPEN (Tanya can close on demo-OA rig on go): both families get confirm LINE / make-up notice shows both names / family-2 leave via LIFF; exact sale ฿14,200 (backoffice-ledger read, bo auth 404'd — confirm via demo-OA text or re-crack bo).
- 🔎 MINOR (to Sober): /api/bookings (Bookings TABLE) returns coStudent:null + single displayName for a DUO session, while /api/calendar (grid) carries both. Grid correct; table surface may show one name only. Quick FE/BE check — "both names wherever a child is named" should include the Bookings table.

## Owner: build §13.3 to completion; uat holds & ships together
- Owner ruling: Sober finishes the per-session rate model (§13.3) + folds in the Bookings-table two-names fix. The uat deploy WAITS and ships with this. Dispatched to @Sober (inbox/SA.md): contract → tasks → BE/FE → sid re-check (Tanya) → single uat.
- Parallel: Tanya to run the demo-OA pass (LINE delivery + exact ฿14,200) meanwhile.
- **@Sober** — Porter: §13.3 per-session coach rate (customer: the substitute week's session = 3, the course stays 7) + Tanya's Bookings-table names note; GO, the `uat` deploy waits. Cut **TASK-423** (BE, contract first: default on the course, override on the session, `effectiveRateMinor` pure; reuse `teacher_rate_minor` if its readers allow) + **TASK-424** (FE S: relabelled popup, the course default, the table names). Ball @Jason (423 contract).

## Tanya (QA) — 🔴 Demo-OA DUO pass BLOCKED: adb devices ⇒ BUR8GMX4C6CYNNIF UNAUTHORIZED (USB-debug auth revoked; "Allow USB debugging?" dialog on CPH2735 needs OWNER to accept + tick always-allow; I can't tap a consent dialog / can't screencap unauthorized). Staged & ready: family B (co-student) parent 0105ce47 phone 0870000200 kid Prao c461da4b; DUO course 81aac5b1 (kid1 KKTEST + coStudent Prao). On re-auth I run register 0870000200 → confirm DUO session (screencap = co-student family confirm delivered) → family-2 leave via OA (screencap = pair cancelled + make-up BOTH names). One-LINE limit: can show co-student family confirm (new behavior); family A not simultaneously (BE sends both households by householdLineUserIds). Exact ฿14,200 also blocked: bo host = Next/Mantine frontend, /api/* 404, bo API base not in machine.local.md; demo-OA text needs device. Code/tests confirm 10h DUO=14,200 / ONE sale course-balance-duo-10 (code fact, not a run). Footprint: family B + course 81aac5b1 left LIVE (staged); cancel+archive if device can't return. Report in inbox/PM.md.

## Jason (BE) — 📋 TASK-423 contract in the TASK: `teacher_rate_minor` REUSED for the per-session override (7 readers listed; the only printer is type-gated) ⇒ no migration; `effectiveRateMinor` + `rateFacts` in `lib/coach-rate.ts`; 🔴 Finding A — `displayName` has three copies (DTO / reminder / slot-clash) ⇒ one `displayNameOf`; `getBookings` gains the co-student join; a make-up is born with the default; two corrections to §2 (body key stays `classRateMinor`; `rate{}` on COURSE_PACKAGE rows only). Waiting on Sober's confirm.
- **@Sober** — TASK-423 contract from @Jason CONFIRMED (reuse `teacher_rate_minor`, no migration; `rate` on course rows only; ONE `displayNameOf` for three copies — the DTO's never joined names; `q` widened; make-up born with the default). TASK-424 §0 rewritten. Porter told. Ball @Jason (build).

## Tanya (QA) — 📱 Demo-OA DUO pass DONE (device re-authorized by owner). ✅ Co-student family confirm LINE delivered (linked demo LINE to family B/Prao 0870000200, confirmed DUO session → "📅 CONFIRMED SCHEDULE" arrived = householdLineUserIds reaches co-student household). ✅ Family-2 LINE leave WORKS (Finding B): family B typed "leave" → shared session SICK_LEAVE (cancelled for pair) + ONE make-up EXTENDED 2030-09-24 + leaveUsed:1 one shared pool. 🔴 FINDING: both delivered notices name only "Student: KKTEST" (kid1) — NOT both / not the receiving family's own child (Prao). Confirm: "Student: KKTEST, Private FREESKATE 10 HR"; leave: "แจ้งลาแล้ว: KKTEST — 2030-07-16". Same co-student gap as /api/bookings table surface (grid carries both; notices+table don't). SPEC-087 "A & B everywhere / make-up both names" → family-facing LINE text DIVERGES → fold notice-builder into Sober's §13.3 + Bookings-table two-names fix. 🔴 ฿14,200 still not runtime-confirmed: confirm text has no amount; bo API base unknown (Mantine frontend, /api/* 404); code/tests confirm 10h=14,200/course-balance-duo-10. Footprint clean: demo LINE cleared/free, both DUO courses cancelled, family B archived, 0 live, device authorized. Report in inbox/PM.md.

## Jason (BE) — ✅ TASK-423 CODE DONE: the coach rate per session — `effectiveRateMinor`/`rateFacts` in `lib/coach-rate.ts` (one spelling, scan-pinned); `bookings.teacher_rate_minor` reused as the override (no migration, 49 = 49); the DTO's `rate{}` on course rows only; session write on the row (null clears, non-course ⇒ 400), course write = the default for any course; `getBookings` + co-student join and `q`; ONE `displayNameOf` for four former copies. 2647/0 (+16), tsc 0; 19/19 mutations bite. Awaiting review.
- **@Sober** — TASK-423 re-verified (2647/0, tsc 0, 49 = 49; `lib/coach-rate.ts`; `displayNameOf` at the mappers/jobs/scheduler; the remaining `otherTitle ??` hits are plain nulls) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-424. Porter told (the default not clearable; Tanya's checks). Ball @Fern.

## Tanya (QA) — ✅ Backoffice access WORKS (owner right): logged into backoffice-som.develyst.online web UI with admin/admin (earlier 404 was API-path probing, not a password). ฿14,200 CONFIRMED: CATALOG "Course 10h (balance-duo)=฿14,200" (+6h 9,360/4h 6,800/1h 1,900); Revenue by-customer shows real DUO one-course "คราม & พราว — 1 คอร์ส — ฿9,360" (both names + DUO price). ⇒ DUO one-course = ONE sale at DUO card price, both names in backoffice. 🔑 Sharpens both-names finding: backoffice DOES show both names, but LINE notices showed only KKTEST ⇒ gap is specifically the LINE notice-builder + /api/bookings table surface (not sale/grid). Footprint: sale-check DUO course cancelled + family archived; still logged into bo browser (read-only, no sign-out). Report in inbox/PM.md.

## [--:--] Fern (FE) — ✅ **TASK-424 done — the session popup edits THIS session's rate (relabelled; the (default)/(override) tag from the server's three facts; `Clear` ⇒ null), the course card edits the DEFAULT for any course, `displayName` carried straight through (the server joins `A & B`).** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  516 pass / 0 fail   (was 515; +1, three TASK-421 cases rewritten to §13.3)
bun run build     → ok
```
9 source modified. Built to §0 + Jason's final lines (TASK-423). Snapshot unchanged (56). Notes in the TASK §2. 🚫 No
deploy asked.
The mapper carries `displayName` straight through again; `studentLabel` and the server's string agree (pinned by value —
the FE join would now read `A & B & B`). Pure `rateTag` (reads `overrideMinor` alone) + `sessionRateChange` (a body only
when typed ≠ EFFECTIVE; Clear ⇒ null only on an override; 0 is a rate); no `override ?? default` anywhere (asserted). The
popup: the box on any COURSE row, *This session's rate (default|override)*, the hint names the default, Clear on an
override only. The card: *Default coach rate* on any course, PATCH only when changed, never null. The Bookings table
reads `displayName`. Copy `course` +7 ×2.
🔑 **Break-and-watch ×12 in `try/finally`** ⇒ 1 each (two slipped first — fixtures where effective≠default coincided
with override≠null; diverged and pinned); md5 identical on 5 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-424 re-verified (516/0, tsc 0; the FE join removed, `studentLabel` ≡ the server's string) ⇒ **✅ DONE — REVIEWED**. **§13.3 complete both sides; the FULL batch whole and green** (BE 2647/0 · FE 516/0 · 49 = 49). Porter told: to `sid` for Tanya's re-check, then `uat`. Chain stopped; ball @Porter.

## §13.3 BUILT + owner rulings + sid re-deploy
- Sober §13.3 complete (TASK-423 BE 2647/0, TASK-424 FE 516/0, 49=49, no migration). Rate = course default + per-session override on booking (popup edits session only, Clear→default, make-up born with default). Display name folded to ONE `displayNameOf` (4 hand copies retired incl. the additional-teacher clash sentence); Bookings table now shows "คราม & พราว".
- Tanya demo-OA: ฿14,200 DUO sale CONFIRMED live (catalog + a real "คราม & พราว" revenue row); backoffice access works (admin/admin web UI). Narrowed the both-names gap to LINE notice + /api/bookings only (backoffice + grid already correct).
- Owner rulings 2026-09-21: (1) course default rate settable to 0 but NOT clearable to null = OK; (2) a substituted session's make-up is born with the course DEFAULT (override stays only on the substituted row) = OK/as intended.
- Owner: deploy sid. Runbook re-run (0042→0048 already applied earlier on sid; only new code this round — no new migration since §13.3 had none, so verify stays 49). ⚠️ WATCH for the LINE notice both-names on Tanya's re-check.

## sid re-deploy DONE — Tanya re-check released
- Owner: db:migrate → verify = ✅ 49 / ledger 80 / no 🔴 (no new migration, no ledger skip). Code built + restarted (restart done).
- @Tanya re-check released (inbox/QA.md): priority §13.3 per-session rate + the LINE-notice both-names watch + Bookings-table names, plus the per-REQ re-confirm. Last gate before the single uat deploy.

## Tanya (QA) — 🔴 uat GATE HELD. §13.3 per-session rate ✅ PASS (default ฿7→override one ฿3, others stay 7→change default ฿8, override stays, others read 8→clear→back to 8; Private takes default too; rule override??default, stored-only). Bookings table ✅ FIXED (/api/bookings displayName "KKTEST & Prao" + coStudent populated). 🔴 LINE NOTICE STILL ONE NAME: demo-OA, fresh DUO (KKTEST+Prao), confirm AFTER deploy → "📅 CONFIRMED SCHEDULE Student: KKTEST" only, NOT "KKTEST & Prao". Notice-builder uses own text path, not the DTO displayName Sober fixed → SPEC-087 "A & B everywhere" NOT met on LINE messages. Leave notice couldn't re-capture (bot correctly refused leave inside start cut-off) but Round-54 leave was single-name too. DEFECT — do not close (per Porter). → back to Sober: fold notice-builder (confirm+leave/make-up, lead with recipient's own child). uat waits. Footprint: demo LINE cleared, course cancelled, family archived, device authorized, bo browser read-only. Report in inbox/PM.md.

## Tanya re-check — §13.3 PASS, Bookings table PASS, LINE notice FAIL → gate held
- ✅ §13.3 per-session rate: exact to spec (eff/ovr/def; override survives a default change; Clear→default; Private course now takes a default rate; stored-only).
- ✅ Bookings-table both-names FIXED (/api/bookings displayName "KKTEST & Prao").
- 🔴 LINE NOTICE still ONE name: demo-OA confirm after deploy read "Student : KKTEST" only. The notice-builder uses its own text path, separate from the DTO displayName Sober folded → confirm + leave/make-up notices still name the primary child only. SPEC-087 "A & B everywhere" not met on family-facing LINE.
- Porter: uat GATE HELD. Back to @Sober (inbox/SA.md) to fold the notice-builder (confirm + leave/make-up) into the two-names source; then sid re-check (Tanya LINE re-capture) → single uat. The watch-flag I set on the re-check caught it before uat.
- **@Sober** — Porter: DEFECT — LINE notices show one child on a DUO (Tanya, demo OA); `uat` gate held. Read: the outbox worker's `bookingContext` loads `student` only and overrides the payload — a fifth name path + two payload writers. Cut **TASK-425** (BE S: `displayNameOf` in the worker + writers, a `studentName:` census pin, stable pair order). Ball @Jason.

## Jason (BE) — ✅ TASK-425 CODE DONE: the LINE notices name both DUO children — `studentNamesOf` (the student part of the ONE rule) in the outbox worker's context + the leave/course-confirmed payloads + three more one-name sites the census found (check-in QR, ICS, teacher's LINE schedule); stable pair order; a `studentName:` census pin with a named per-child allow-list; six renderers by value. 2659/0 (+12), tsc 0; 16/16 mutations bite. Flagged: nickname-first now applies to notices. Awaiting review; `sid` for Tanya's re-capture.
- **@Sober** — TASK-425 re-verified (2659/0, tsc 0; `studentNamesOf` in the worker + writers + three more sites the census found; the scan pin) ⇒ **✅ DONE — REVIEWED**. Batch green; to `sid` for Tanya's LINE re-capture. Flagged to Porter: LINE notices now nickname-first for Private children (the one rule) — the owner's call before `uat`. Ball @Porter.

## LINE notice both-names FIXED (TASK-425) + owner name rulings
- Sober TASK-425: the outbox worker (bookingContext) was a 5th name path overriding the payload; folded it + 2 payload writers + 3 more one-name sites (LIFF check-in, teacher ICS, teacher "my schedule" reply) into the one nickname-first rule; scan pin added. BE 2659/0, FE 516/0, 49=49, no migration. Both families read "KKTEST & Prao" in the course's stable order.
- Owner rulings 2026-09-21: (1) NICKNAME-first on family-facing LINE notices = OK (as built; a Private child with a nickname now reads by nickname on notices/check-in/ICS/teacher reply — consistent with calendar/reminder/deduction); (2) same name order to both families = OK. Both match the build → no extra code.
- Next: deploy TASK-425 to sid (no migration, verify stays 49) → Tanya LINE re-capture on demo OA → single uat.

## sid re-deploy (TASK-425) DONE — Tanya LINE re-capture released
- Owner: db:migrate → verify = ✅ 49 / ledger 80 / no 🔴 (no new migration). Code built + restarted.
- @Tanya LINE re-capture released: every family-facing surface must read "KKTEST & Prao" (confirm/leave/make-up/cancel/deduction/LIFF check-in/ICS/teacher reply). Last gate before single uat.

## Tanya (QA) — ✅ GATE PASSED: TASK-425 FIXED LINE notice both-names. Demo OA, fresh DUO (KKTEST+Prao), post-deploy: 📅 CONFIRMED SCHEDULE "Student: KKTEST & Prao" ✅ · 💡 COURSE DEDUCTION "KKTEST & Prao … Remaining 3/4 HR" ✅ (both names + one shared deduction) · ❌ CLASS CANCELLED "KKTEST & Prao … ระบบเพิ่มคาบชดเชยให้แล้ว" ✅ · make-up CONFIRMED SCHEDULE "KKTEST & Prao" ✅. Round-55 defect (confirm one name) CLOSED. Not independently captured: LIFF check-in screen (webview; but deduction that check-in fires = both names) + coach "my schedule" reply (needs teacher LINE) — same fixed builder, flagged honestly. Also this round: §13.3 rate PASS, Bookings table PASS. Footprint: demo LINE cleared, course cancelled, family archived, device authorized. Verdict: DUO family-facing text green for confirm/deduction/cancel/make-up; LIFF+coach un-captured (Porter's call for uat). Report in inbox/PM.md.

## Tanya LINE re-capture PASS + owner GO uat
- Tanya: confirm/deduction/cancel/make-up all read "KKTEST & Prao" on demo OA (closes Round-55). LIFF-screen + coach-reply un-captured (same fixed builder + scan pin) — owner accepted as non-blocking.
- Owner drafted a customer UAT note (test DUO on sid som.develyst.online) then said GO uat. Deploying the full batch to uat.
- Batch to uat: REQ-095 (Stages 1→3b · §11 camp-grid · §13 DUO one-course · §13.3 per-session rate) · 096 · 097(+500 fix) · 098 · 099(+year) · 094 fix · Bookings-table names · LINE notice both-names (TASK-425). uat at 42 ⇒ 0042→0048, verify 49. Expect TASK-085 ledger skip (multi-migration) → seed-ledger --apply routine.

## uat DEPLOY DONE — batch LIVE
- Owner ran uat: db:migrate (only 0047+0048 pending — 0043-0046 already there; NO ledger skip this time) → verify ✅ 49 / ledger 70 / witness 49 / no 🔴. sale:ensure-items dry-run = 32/32 present, 0 to create → no apply. Code built + restarted; super admin grants/Camp menu/Teacher role present; camp_reminder_enabled OFF.
- LIVE on uat: REQ-095 (Stages 1→3b · §11 camp-grid · §13 DUO one-course · §13.3 per-session rate) · 096 · 097(+500 fix) · 098 · 099(+year) · 094 fix · Bookings-table names · LINE notice both-names (TASK-425).
- Open (non-blocking): partner-leave notice decision; ledger dump (DATA REQUEST); uat DUO-group count (Sober: no migration, let old Stage-2a DUO groups run out); disable-frees-link question; LIFF-screen + coach-reply both-names un-captured (same fixed builder + scan pin).

## BATCH CLOSED — customer notified
- Owner sent the status update to the customer (Khwan): DUO one-course + names both places, per-session rate, camp on grid, birthday filter+year, EXTENDED confirm, teacher login/archive — all LIVE on uat.
- Parked (non-blocking, await owner): (1) partner-leave notice; (2) ledger dump DATA REQUEST (permanent TASK-085 fix); (3) disable-frees-teacher-link question; (4) LIFF check-in + coach "my schedule" both-names un-captured (same fixed builder + scan pin).

## TWO new customer requests recorded (REQ-101, REQ-102) — sent to Sober to analyse
- REQ-101 ECA: Manage-plan page + whole-course Confirm + notify-on-edit/move/remove + Cancel-all; customer Q = add-teacher onto existing ECA (Manage-plan + Add-teacher button) vs create-new. Recorded verbatim in requirements/REQ-101.
- REQ-102 RBAC: permission to view money figures + Freelance salary ceiling visibility. Recorded in requirements/REQ-102.
- Dispatched @Sober (inbox/SA.md) for feasibility + sizes; owner rulings pending; no engineer dispatch until owner rules.
- **@Sober** — Porter: REQ-101 + REQ-102 analyse, no build. Wrote **SPEC-088**: (A) an ECA has no series object — teachers only at creation ⇒ a series key (`0049`) + a Manage-plan page (confirm-all, cancel-all, add/remove/swap teacher, add dates), BE M+ · FE M; (B) no money-visibility key exists; six money surfaces; the `GET /teachers` budget leak to teacher accounts; ONE fail-closed `action:money.view` masked at the DTO, BE S–M · FE S, no migration. Decisions listed. Ball @Porter.

## REQ-101 + REQ-102 — owner rulings + GO build (SPEC-088)
- Owner "เอาตามแนะนำ" on all 7 decisions. REQ-102 SCOPE NARROWED by owner to the Freelance ceiling only (frontoffice) + the GET /teachers leak fix — NOT a system-wide money mask (prices/discounts/sales/reports out of scope). Notice words (REQ-101 item 4) = Porter TH+EN draft, owner-accepted.
- Dispatched @Sober (inbox/SA.md): build REQ-102 first (BE S–M/FE S, no migration), then REQ-101 (0049⇒50, Manage-plan page + Confirm-all/Cancel-all/Add-Remove-Swap-teacher/Add-dates, two notice kinds, generalises to Free/KOL). Contract first each; then engineers; both into the next sid→Tanya→uat cycle.
- **@Sober** — Porter: GO REQ-102 (narrowed to the freelance ceiling) first, then REQ-101 (all four §A3 as recommended). Cut **TASK-426** (BE, contract first: key 57, ONE mask at the teacher-DTO builder, booleans stay, writes double-gated, linked accounts always masked) + **TASK-427** (FE S). REQ-101's tasks follow 426. Ball @Jason (426 contract).

## Jason (BE) — 📋 TASK-426 contract in the TASK: ONE builder (`attachFreelanceBudgets`) feeds three readers; the builder gains a REQUIRED viewer (fail closed) and the mask lives there; `RouteAccess.action` → `key | key[]` for the double-gated writes; the attention line is the one figure outside the builder; three ❓ (attention label · `hourlyRate` · threading). Waiting on Sober.
- **@Sober** — TASK-426 contract from @Jason CONFIRMED (the required-viewer builder; `hourlyRate` masked too; the dashboard label without the number, the digest unchanged; explicit param over a request context). TASK-427 §0 rewritten. Porter told the two owner-facing points. Ball @Jason (build).

## Jason (BE) — ✅ TASK-426 CODE DONE: the freelance ceiling behind key 57 (`action:teachers.budget-view`) — ONE mask at the ONE builder (`attachFreelanceBudgets(dtos, viewer)`, viewer required), the four figures null without the key / for a linked account / for null, the booleans stay; the two budget writes double-gated (`RouteAccess.action` may be a list, the guard requires every); the leak closed by value; the near-cap label figure-less without the key. 2670/0 (+11), tsc 0, 49 = 49; 16/16 mutations bite. Awaiting review.
- **@Sober** — TASK-426 re-verified (2670/0, tsc 0, 49 = 49; the mask names four fields; key 57; writes double-gated) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-427. Cut **TASK-428** (REQ-101 BE, contract first: `0049` series key + backfill, the series routes, key 58, two notice kinds with §4's bytes). Porter told (grant `budget-view` after deploy). Ball @Jason (428 contract) / @Fern (427).

## [--:--] Fern (FE) — ✅ **TASK-427 done — budget figures draw `—` when the server nulls them (one pure `moneyOrDash`; zero stays `฿0`), the budget doors need BOTH keys, snapshot 56 → 57.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  521 pass / 0 fail   (was 516; +5)
bun run build     → ok
```
4 source modified · 11 tests re-pinned · 2 new. Built to §0 + Jason's final lines (TASK-426). The 57th key after
`teachers.budget` (the BE's slot, by position). Notes in the TASK §2. 🚫 No deploy asked.
The walk scans every component for a `฿{… ?? 0}` money print — three readers found and routed through the helper (the
Teachers card's rate line + remaining/budget line, the calendar's freelance strip); the card's block now shows for every
freelancer (masked ⇒ `— / —`, never hidden), the badge + override switch from the booleans; the strip shows a grey
`— / —` when masked (rendered both ways); the doors ask both keys, hidden never disabled; no client masking (asserted);
the not-bookable rule untouched. The dashboard's near-cap label is rendered as given.
🔑 **Break-and-watch ×10 in `try/finally`** ⇒ 1–3 each, none slipped; md5 identical on 5 files.

**Ball: @Sober** (REQ-101's Manage-plan task next, per your note). TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-427 re-verified (521/0, tsc 0; key 57 in the snapshot) ⇒ **✅ DONE — REVIEWED**. REQ-102 complete both sides. Porter told Tanya's checks. Ball @Jason (428 contract).

## Jason (BE) — 📋 TASK-428 contract in the TASK: no series key exists today (the create mints one, `0049`); reads/writes by key with the existing clash + swap shapes; 🔴 Finding A — `moveBooking` tells no coach for any type (only the plan edit does, on a teacher change); backfill keyed with `teacher_id`; three corrections (`fromDate` on add/remove/swap; the notice carries dates; kinds 20 → 22/23); ❓ cancel-all summary kind. Waiting on Sober.
- **@Sober** — TASK-428 contract CONFIRMED (cancel-all = one summary kind, placeholder; `fromDate` on remove/swap; Finding A separate → owner's list; date line `DD-MM-YYYY` corrected). TASK-429 (REQ-101 FE, M) cut with the contract in §0. Porter: cancel-all words + Finding A for the owner. Ball @Jason (428 build) — Fern on 429's pure parts in parallel.

## Jason (BE) — ✅ TASK-428 CODE DONE: the OTHER SERIES key (`0049` ⇒ 50, preflight `[]`), `other-series.service.ts` (reads + confirm-all / cancel-all / add / remove / swap / add-dates / header PATCH by key, one tx each, the clash rolls back), key 58, three notice kinds (ADDED/REMOVED the owner's bytes, CANCELLED a placeholder), the backfill script (pure grouping pinned; dry-run not run — sid's DB is the human's). 2690/0 (+20), tsc 0; 25/25 mutations bite. Awaiting review.
- **@Sober** — TASK-428 re-verified (2690/0, tsc 0, 50 = 50, key 58, kinds 23) ⇒ **✅ DONE — REVIEWED**. Fern told to wire TASK-429 (+ `DATE_EXISTS`, the 404 text). Porter: deploy steps (migrate 50 → backfill dry-run → apply; grant 58). Ball @Fern (429).

## [--:--] Fern (FE) — ✅ **TASK-429 done — the Manage-plan page `/scheduler/other/:key`: rows + the existing modal per row, the series doors by their keys (cancel-all by key 58 alone), the row → page link, the create toast's link, the series-in-range strip; snapshot 57 → 58.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  528 pass / 0 fail   (was 521; +7)
bun run build     → ok — `ƒ /scheduler/other/[key]`
```
10 source modified · 14 tests re-pinned · 9 new. Built to §0 + Jason's final lines (TASK-428). The 58th key after
`group-series` (the BE's slot, by position). Notes in the TASK §2. 🚫 No deploy asked.
Pure `seriesDoors` value-tested (each door by its grant; key 58 ALONE gates cancel-all; confirm-all needs a PENDING row,
cancel-all a LIVE row); `cancelAllBody`, `fromDateDefault`, `withFromDate`, `seriesHref`. The page is guarded as
`menu:calendar` through a `ROUTE_ALIASES` entry (the nav pins untouched); the doors ask `can()` at the site + the pure
row conditions, hidden never disabled; a row opens the EXISTING modal by id (the rows loaded through the same
`GET /bookings`, matched by id). Seven dialogs' worth of doors in two files; every 409 = the server's sentence, the
input stays, no code-switching; refetch on 2xx only; the header PATCH has no `startTime`. The row → page link only with
a key; the create toast offers the page (`notify` grew a `link`); a collapsed `Series in range` strip under the
calendar header (fetched only while open; not for a linked account). Copy `otherSeries` 32 ×2.
🔑 **Break-and-watch ×16 in `try/finally`** ⇒ 1–2 each, none slipped; md5 identical on 8 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-429 re-verified (528/0, tsc 0, 58 in the snapshot) ⇒ **✅ DONE — REVIEWED**. REQ-101 complete both sides; the whole batch green (BE 2690/0, FE 528/0, 50 = 50). Porter has Tanya's checks + the deploy steps. Ball @Porter (owner's `sid` restart).

## REQ-101/102 final rulings + cancel-all words → sid deploy
- Owner accepted A (REQ-102 masks rate+ceiling+remaining+reorder, one key) + B (REQ-101 swap notifies, date/time move silent) + the cancel-all notice draft. Recorded REQ-101 §5, REQ-102 §5. Sent words to @Sober to bake.
- REQ-101 adds migration 0049 ⇒ verify 50; backfill-other-series script (dry-run→apply); grant key 58 (cancel-all) + teachers.budget-view to the pay-managing role after deploy. REQ-102 no migration, key 57.

## sid deploy (REQ-101 + REQ-102) DONE — Tanya round released
- Owner ran sid: db:migrate hit TASK-085 ledger skip (0047/0048) → seed-ledger --apply (2 rows) → migrate → verify ✅ 50 / ledger 83 / no 🔴. backfill-other-series --apply = 12 keys on 29 rows (per-teacher split correct). sale:ensure-items 32/32, no apply. Code restarted; grants key 58 + teachers.budget-view.
- @Tanya released (inbox/QA.md): REQ-101 Manage-plan + REQ-102 money mask/leak. Last gate before single uat.
