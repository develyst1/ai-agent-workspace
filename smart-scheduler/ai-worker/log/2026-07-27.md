# Log — 2026-07-27 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [21:47] Porter (PM) — reviewed today's code changes + team channel per stakeholder

- Stakeholder ran work "elsewhere" and asked Porter to review the project's code changes
  + the team's communication today. Re-read board + logs and swept git across all 5 repos.
- **ai-worker team channel TODAY (2026-07-27): NO activity.** No role session (Sober/
  Jason/Fern) wrote anything — no new SPEC/TASK, no log entries, board unchanged. My
  REQ-007 + REQ-008 are still **READY_FOR_SA, unpicked** (no SPEC written). The board is
  now **stale relative to the actual code** (see below).
- **Code TODAY: exactly ONE commit** — `smart-scheduler-backoffice-back` `52871f9`
  (author `dong <nichaphon@smartalliance.co.th>`, 18:01), message references **"TASK-027"**
  (a number NOT in our board). What it did:
  - `.env.example`: `DATABASE_URL` → **shared `smart_scheduler` DB** (retire the legacy
    separate `smart_backoffice_db`). Resolves the shared-DB intent of REQ-006.
  - `src/routes/api.ts`: **retired ALL old `ops` routes** — backoffice-back now serves
    only `/auth` + `/bo`. (Comment: ops tables don't exist in the shared DB, so those
    routes would 500; their files stay dormant, deleted later.)
  - `src/db/migrate-to-bo.ts`: guarded for an absent `ops` schema (skip ops passes; the
    `public.freelance_budgets → bo` pass is the essential one).
  - Added `src/db/verify-bo-migration.ts` + `verify:bo` script.
  - = real **REQ-006** progress that partly closes the integration gap I flagged 2026-07-26,
    but done **OUTSIDE the ai-worker flow** (untracked "TASK-027", direct commit).
- **Wider git picture:** all substantive code is committed **directly by dev identities**
  (`dong`, `neeeeroooo`, `dev`) across 07-20…07-27 — NOT produced through role-session TASK
  files. REQ-006 in **code** is much further along than the board (bo schema+API+front done
  07-23/24; shared-DB + ops-retire done today). The board's REQ→SPEC→TASK tracking has
  **drifted** from the real repo.
- ⚠️ **RISK to verify (SA/BE):** retiring backoffice `ops` routes may break any frontoffice
  code still calling them — `smart-scheduler-back/src/lib/ops-client.ts` still targets old
  `ops` routes (`/catalog/items`, `/recurring-costs`, `/parties`, `/internal/teacher-sync`).
  The as-built freelance drawdown uses the **local** `bo.item` in scheduling's own DB (likely
  unaffected), but other ops-client calls could now 500. Needs a code check before relying on it.
- **No board mutation yet** — surfaced the drift + the risk to the stakeholder (Thai) and
  asked how to reconcile (update board to reality? route REQ-007/008 through the flow or
  build directly?). Nothing routed to Sober beyond the already-open REQ-007/008.

## [22:15] Porter (PM) — ops-client risk VERIFIED (HIGH but scoped) → @Sober; REQ-006 reconcile pending Sober

- Stakeholder confirmed the backoffice rebuild (separate DB → **shared `smart_scheduler` DB**,
  "easy + scalable") is **their own directive** and observed it working from another machine.
  Asked Porter to (1) reconcile the board once Sober verifies it's true, (2) check the ops-client risk.
- **Read-only risk check done (Porter, sub-agent).** After TASK-027 retired all `ops` routes
  (backoffice-back now serves only `/auth`+`/bo`):
  - ✅ **Core flows FINE** (matches what the stakeholder saw): the live freelance cap at booking
    confirm/cancel uses the **LOCAL `bo.item`** in scheduling's own DB (`scheduler.service.ts`
    `drawCeilingHour`/`attachFreelanceBudgets`, TASK-024) — NOT the remote ops-client. `recordSale`
    income posting is best-effort (degrades). The remote freelance/quota ops-client fns are dead
    code (tests only).
  - 🔴 **HIGH but scoped break — teacher lifecycle mutations.** `createTeacher`/`updateTeacher`/
    `archiveTeacher`/`reactivateTeacher` (`scheduler.service.ts:941/974/1003/1015`) call
    `/internal/teacher-sync/*` via `opsSyncOr502`, which THROWS 502 + rolls back the tx on any
    non-2xx. With ops routes retired those 404 → **teacher add/edit/archive will 502 once the
    ops-retired backoffice is deployed**. `OPS_API_URL` blank does NOT rescue it (`ops-client.ts:171`
    throws on unset too). = the already-flagged **"Teacher CRUD not standalone"** gap (REQ-005
    territory), now concrete/urgent. REQ-003 teacher-sync was meant to be ON HOLD but the calls
    were never de-wired.
- **@Sober — 2 asks (route any fix through the chain to BE; engineers must NOT guess — ask up):**
  1. **Verify the real REQ-006 code state** vs the board (bo schema+API+front done 07-23/24;
     TASK-027 shared-DB + ops-routes-retired 07-27, direct commit not in this board) and report so
     Porter reconciles the board.
  2. **Confirm the teacher-CRUD break** and propose the fix (de-wire/guard the `opsSyncOr502`
     teacher-sync calls so teacher CRUD is standalone) — this is the REQ-005 "standalone teacher
     management" work, now a **pre-deploy blocker** for the backoffice rebuild. Business call → `@Porter`.
- Board updated: REQ-006 owner → Sober (verify+reconcile, code advanced beyond board); the
  teacher-CRUD risk row escalated to 🔴 ACTIVE. REQ-006 status left unchanged pending Sober's
  verify (per stakeholder: "if it's true, then reconcile"). REQ-005 not yet promoted — awaiting
  stakeholder's go to make it the fix.

## [22:20] Porter (PM) — CORRECTION: my board edit did NOT land; board synced ahead from the other machine

- My [22:15] board edit **failed on a write conflict** — `board.md` had just synced newer content
  from the other machine. So the changes I described above did NOT land. Re-read the current board:
- **The team (other machine) is already AHEAD on REQ-006:**
  - **TASK-027 = DONE** (shared-DB topology fix + retire ops routes — exactly the 07-27 commit I found).
  - **REQ-006 → SPEC_DONE (fix applied) → re-deploy.**
  - They found a REAL **REQ-006 bug: freelance drawdown not idempotent** → new **TASK-028**
    (reconcile-to-target invariant) + a locked state-machine rule (CONSUMING=pay: CONFIRMED/ATTENDED/
    **SICK_LEAVE**; RELEASING: NO_SHOW/CANCELLED) — which **changes REQ-004** (SICK_LEAVE now KEEPS the
    drawdown). REQ-006 not delivered until TASK-028 lands. This is being handled by Sober/Jason there.
- **So the board "reconcile" is largely already done by the team** — no action needed from me there.
- **Still MISSING on the board (my distinct finding):** TASK-027 retiring ops routes now makes the
  **"Teacher CRUD not standalone"** row (teacher add/edit/archive/reactivate → 502 via `opsSyncOr502`)
  an ACTIVE pre-deploy break, not a hypothetical one. That link isn't recorded yet.
- ⚠️ **Concurrency/governance:** the board is being edited from another machine concurrently (I hit a
  write conflict) — effectively two Porter contexts on one board. **Holding all board edits** to avoid
  clobbering the team's work; surfaced to the stakeholder to decide who owns the board + whether I add
  the teacher-CRUD link. No further board mutation from this session until the stakeholder says.

## [22:30] Porter (PM) — stakeholder cleared me to edit (other machine idle until next week) → board updated + @Sober

- Stakeholder: the other machine isn't working now (resumes next week) — go ahead and edit. So I
  reconciled the one still-missing piece onto the current (team-synced) board:
- **Board edits (landed):**
  1. **Teacher-CRUD risk row → 🔴 NOW ACTIVE.** TASK-027 (07-27) retired the ops routes, so
     `createTeacher`/`updateTeacher`/`archiveTeacher`/`reactivateTeacher`
     (`scheduler.service.ts:941/974/1003/1015` → `opsSyncOr502` → `/internal/teacher-sync/*`) will
     **502 + roll back on deploy** of the ops-retired backoffice. `OPS_API_URL` blank doesn't rescue it
     (`ops-client.ts:171` throws on unset). Verified by Porter read-only code-check. Core booking/
     freelance flow is unaffected (local `bo.item`).
  2. **REQ-005 → HIGH / READY_FOR_SA.** Its "not urgent" premise is dead — it's now a **pre-deploy
     blocker** for the REQ-006 re-deploy. Immediate deliverable = de-wire/guard the teacher-sync calls
     so teacher CRUD is standalone.
- **@Sober — please pick up REQ-005 (READY_FOR_SA) and write the SPEC:** verify the teacher-sync break
  above in code, then de-wire/guard the `opsSyncOr502` teacher-sync calls so teacher add/edit/archive/
  reactivate work standalone (no ops dependency), and route the build to Jason. **This must land before
  the ops-retired backoffice (REQ-006) is re-deployed.** Per stakeholder standing rule: if anything is
  unclear or a business/scope question arises, write it in the REQ `## Questions` and route `@Porter` —
  do NOT guess.
- Left untouched (team owns, other machine): TASK-028 (REQ-006 drawdown-idempotency bug) + the REQ-006
  re-deploy steps — those are mid-flight on the other machine and resume next week.
- Queue reminder for smart-scheduler (my side): REQ-007 + REQ-008 still READY_FOR_SA (feature backlog);
  UC-031 pending stakeholder's course-6-week confirm; UC-030 last. REQ-005 now jumps ahead of these as
  the pre-deploy blocker.

## [22:45] Porter (PM) — wrote the REQ-005 file (was board-only) → fully ready for @Sober

- Stakeholder chose to KEEP the role model (Sober→Jason do the fixes; I stay PM). So I made sure the
  work is pick-up-ready: **REQ-005 had no file** (board row only) — wrote
  `requirements/REQ-005-standalone-teacher-management.md` (READY_FOR_SA, HIGH).
- Scope = remove the obsolete ops teacher-sync so teacher add/edit/change-type/archive/reactivate stand
  alone (no 502 when ops is gone); preserve all existing REQ-003/TASK-016 behaviors; pre-deploy blocker
  for REQ-006. Verified break points + the "ops-sync is obsolete under bo (local `bo.item`)" rationale
  are in the REQ Constraints. `## Questions` carries the stakeholder's "ask up, don't guess" rule.
- **Handoff complete on the PM side.** When the Sober session runs (startup ritual → reads board + this
  log), REQ-005 is READY_FOR_SA with a full REQ file; @Sober is directed to verify the break, write the
  SPEC, and route the build to Jason. Nothing else from Porter blocks it.
- Deploy note for the stakeholder (relayed in chat, Thai): deploy is easy on their side
  (`bun run build` + copy to the Windows server), but must wait until REQ-005 + TASK-028 land, else
  teacher CRUD 502s and the freelance drawdown is buggy.

## [22:55] Porter (PM) — stakeholder DEPLOYED early; live acceptance in progress (human runs, Porter verifies)

- Stakeholder reports they **already deployed** the current build to the real server and core usage
  works. ⚠️ This went out **BEFORE REQ-005 + TASK-028 landed** — so the teacher-CRUD 502 risk and the
  known freelance-drawdown idempotency bug are potentially **live in production**.
- Stakeholder offered a link + username/password for Porter to log into the live site. **Declined to
  log in with their credentials** — (a) safety (I don't enter passwords / authenticate as the user),
  (b) it matches the established brownfield pattern: for REQ-001/002 the **human ran live acceptance,
  Porter verified the reported results**. Asked the stakeholder not to send the password.
- **Ball = stakeholder** to run a risk-ordered acceptance checklist (Thai); Porter verifies results →
  marks deliverables / routes issues to Sober. Priority check = **teacher add/edit/archive/reactivate
  → does it 502?** That confirms whether REQ-005 is an active production break or the assessment needs
  revisiting. Core booking/freelance + the known drawdown-bug behavior also on the list.