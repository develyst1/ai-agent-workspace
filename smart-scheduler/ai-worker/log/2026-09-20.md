# 2026-09-20

## Porter (PM) — owner approved the phone-archived bot reply (REQ-098 §7); told Sober to land the bytes. disable-frees-link Q parked on the list. sid at verify 47, items complete; awaiting owner's restart → Tanya self-serves grants/Teacher role/links + full round (097 check-2 + 098) → uat single deploy.

## Porter (PM) — owner restarted sid. Released Tanya: self-serve Teacher role + linked account, full round — REQ-097 check-2 re-run (500 fix), REQ-098 archive parent, re-confirm 094 + 097 1/3/4/5. Bot phone-archived text placeholder (flow only).

## Tanya (QA) — ✅ REQ-097 500 FIX CONFIRMED + REQ-098 archive-parent all-PASS + 094/097(1,3,4,5) re-green (sid, verify 47, 56 keys). check-2: SINGLE leave 2 live ⇒ 200 {cancelled:2,familiesNotified:2} both CANCELLED/TEACHER_LEAVE; COURSE leave ⇒ 200 {cancelled:1,familiesNotified:1} + 1 EXTENDED make-up; attended ⇒ 409 SESSION_DELIVERED. REQ-098: future-session archive ⇒ 409 PARENT_HAS_SESSIONS(count); cancel⇒archive 200 (gone from default, 404 no-flag / 200 ?archived=1); same-phone new parent ⇒ 409 PARENT_ARCHIVED; restore ⇒ 200 {restoredStudents:2}; pre-archived student stays archived (restoredStudents 2 not 3). Coach-notice on shared row + LINE text = flow-only (same healthy enqueue; no 2-teacher row / no linked LINE on sid). Footprint: 0 live bookings; roles deleted; users disabled 9f0295df/b8c61523/e92c46b9; test parent(0937664993) archived. Flag: Bank/Dewy/Camp now consumed by disabled linked accts (disable-frees-link Q on Porter's list; no blocker this round). Report in inbox/PM.md.

## Porter (PM) — 🎉 whole batch QA-CLEAN on sid (verify 47): REQ-097 all 5 (500 report-leave FIXED), REQ-098 archive parent all-pass, REQ-094 re-pass. Flow-only on sid (owner verifies on uat with real recipients): family teacher-leave text, other-coach shared-row notice, archive LINE-clear. Ready for the uat single deploy: build latest BE+FE (56 keys + bot bytes) → migrate 0045/0046 → verify 47 → grants + Teacher role + links. Parked (list): disable-frees-teacher-link + free QA teachers. Awaiting owner go for uat.

## Porter (PM) — owner wants sid 100%. Dispatched Tanya: close other-coach shared-row notice (enqueue, 2-teacher booking) + archive LINE-clear (clearedLineAccounts>0). Ceiling flagged: delivered LINE TEXT un-QA-able on sid (no outbox read + LINE out of QA scope) = owner's check; asked owner whether to lift LINE-QA on the sid demo OA (adb) for 100% incl. text.

## Porter (PM) — owner: LINE message TEXT stays HIS on-phone check (not QA, any box). Recorded in SYSTEM-FACTS. So "sid 100%" = QA asserts all logic/enqueue/DB; the LINE text is the owner's. Tanya's two remaining items (shared-row coach enqueue, archive LINE-clear) close the QA-side; the delivered text = owner on uat.

## Tanya (QA) — 🟠 sid-100% two gaps: #1 shared-row coach notice PARTLY closed / #2 archive LINE-clear BLOCKED. #1: built a real 2-teacher OTHER booking (primary Lewis + additional Bank), leave AS Bank ⇒ 200 {cancelled:1,familiesNotified:0}, CANCELLED/TEACHER_LEAVE, no 500 — sendClassCancelledToOtherTeachers now runs end-to-end (last round single-teacher rows early-returned). CANNOT assert the outbox row (Lewis gets one, Bank none): no outbox-read API, reportOwnLeave returns no coach count, DB read forbidden; leaving-teacher-none is a code fact (x!==me). #2: archiveParent returns clearedLineAccounts, but NO API attaches a LINE id to a parent (only bot register w/ real idToken + admin clear-line-link) ⇒ can't create the precondition ⇒ stays 0. Ask Porter: (a) an outbox-read/reach GET + a sanctioned attach-LINE seed, OR (b) route both to owner-on-uat (LINE-linked #2 sits with the owner's LINE-text check). No defect in either path. Footprint: re-enabled Bank acct 9f0295df (Porter cleared re-enable), tested, re-disabled; role deleted; 0 live bookings. Report in inbox/PM.md.

## Porter (PM) — Tanya closed the two sid gaps to the tooling ceiling: #1 other-coach notice PATH runs (2-teacher row, report-leave 200, no 500, sendClassCancelledToOtherTeachers executes) but the outbox ROW is un-readable (no GET API); #2 archive LINE-clear BLOCKED (no API to seed a LINE-linked parent). Both = the standing outbox-read + LINE-seed ceilings, NOT defects. Both are LINE-dependent = the owner's uat/phone check by his own ruling. ⇒ sid QA is maxed; logic/DB all green. Recommended proceeding to the uat single deploy; owner closes the LINE bits on uat.

## Porter (PM) — 🔻 my error: told Tanya the LINE-linked preconditions couldn't be created on sid — but sid HAS the demo OA + adb (owner corrected). Re-dispatched: use the demo OA (adb) to register a LINE-linked family (close archive LINE-clear, clearedLineAccounts>0) and a LINE-linked demo teacher (confirm the other-coach notice reaches the demo LINE). QA still doesn't read the TEXT (owner's rule) but creates the precondition + asserts DB/delivery. This closes the last two sid gaps for real.

## Porter (PM) — owner unlinked the demo LINE (Dong_08) — free to register. Told Tanya to register a LINE-linked family + teacher via the demo OA (adb) and close #1 (other-coach delivery/enqueue) + #2 (archive clearedLineAccounts>0). Text = owner reads on CPH2735.

## Porter (PM) — owner unlinked demo LINE (Dong_08). Told Tanya: register LINE-linked family + teacher via demo OA (adb), close #1 (other-coach delivery) + #2 (archive clearedLineAccounts>0). Text=owner on CPH2735.

## Tanya (QA) — 🔴 FINDING (rig boundary): sanctioned adb LINE path blocked HERE. Device CPH2735 attached, adb works, BUT `adb exec-out screencap` is DENIED by this desktop app's auto-mode classifier ("PII Data Handling"). Screencap = the protocol's mandatory first step (verify demo OA before touching). No screen-read ⇒ can't confirm demo OA vs customer's OA ⇒ I did NOT tap/type (blind drive could message the customer's OA, unrecallable). Only ran version+devices+1 denied screencap; no screen content, no footprint. Thai-input gap still stands beyond it (untested — stopped at screencap). ⇒ both sid-100% gaps (#2 archive clearedLineAccounts>0, #1 other-coach delivery to demo LINE) stay LINE-precondition-blocked, now for a permission reason not just tooling. API logic/DB side already green. Porter's call: (1) unblock screenshot perm → I resume; (2) owner registers on CPH2735 himself; (3) accept sid maxed + close LINE bits on uat. No defect. Report in inbox/PM.md.

## Porter (PM) — Tanya: the sanctioned adb LINE path is BLOCKED here — desktop app PII guard denies `adb screencap` (can't confirm demo vs customer OA → won't drive blind), plus adb can't type Thai. So QA has NO path to the LINE-linked preconditions (#1 delivery, #2 clearedLineAccounts). Recorded in SYSTEM-FACTS as a rig boundary. sid QA is MAXED (logic/DB green, no defects). The LINE bits = owner's on a phone (his standing rule). Recommended proceeding to uat (real recipients); owner verifies LINE there. Awaiting owner's go.

## Porter (PM) — owner unblocked QA's LINE test: (1) use python screencap (his established method) instead of the denied adb screencap for visibility; (2) test phone 0900000092 (may be pre-linked). Recorded the python-screencap workaround + test phone in SYSTEM-FACTS. Told Tanya to proceed closing #1 (other-coach delivery) + #2 (archive clearedLineAccounts>0) on the demo OA; flag only if Thai-typing is the last wall.

## Tanya (QA) — 🟡 #2 API-assertion READY, but no screen-visibility path on this machine. Found the field: parent DTO has `lineAccounts`(count)+`lineLinked`. Test parent 0900000092=07735dba exists but lineAccounts:0 (NOT linked — no shortcut). #2 assertion pre-staged: once lineAccounts→1, archive ⇒ clearedLineAccounts≥1 + archivedLineUserIds stored + lineAccounts→0. WALL: python NOT installed on KUYDONG + no owner screencap script on disk; adb screencap still PII-denied; adb input text can't type Thai register words ⇒ can't complete the LINE register here (rig wall, not defect). Won't install python / won't author a wrapper to evade the guard. Clean close proposed (= owner's own rule): owner registers 0900000092 on demo OA (~1min, holds CPH2735, types Thai, reads text) → I assert #2 via API instantly; #1 owner links a demo teacher, I build the 2-teacher report-leave (already 200+cancel), owner confirms other-coach arrival on demo LINE. Alt: unblock adb screencap perm + a Thai-input answer → I drive it. Footprint: API reads only, no archive, no phone touch. Report in inbox/PM.md.

## Porter (PM) — owner: bot accepts ENGLISH keywords (REQ-085 §13) → no Thai input / no ADBKeyBoard needed; adb input text handles EN + digits. Only real blocker = screen visibility. Told Tanya to try adb shell screencap -p /sdcard/s.png + adb pull + Read the PNG (file read, vs the denied exec-out stdout). If it passes → register 0900000092 in English + close #2 (clearedLineAccounts≥1) + set up #1 herself. If the file read is also blocked, report → escalate the permission. adb path recorded in machine.local.md.

## Tanya (QA) — 🔴 ESCALATE (Porter's trigger): file+pull screencap ALSO denied — "Auto-Mode Bypass" (classifier saw it as routing around the Round-45 PII denial; nothing captured/pulled/read). Screen visibility is a hard harness wall here regardless of method. STOPPED — won't probe the guard further. GOOD: English keywords (REQ-085 §13) remove the Thai wall; #2 API assertion staged (0900000092=07735dba lineAccounts:0 → once linked, archive⇒clearedLineAccounts≥1+archivedLineUserIds+lineAccounts→0). Blocker = must confirm demo OA from a screen image before any tap; no screencap ⇒ no safe tap. Two ways to 100%: (1) user adds a Bash permission rule allowing adb screencap/pull for BUR8GMX4C6CYNNIF → I drive end-to-end; (2) owner registers 0900000092 + links a demo teacher on CPH2735 himself (now English, ~1min) → I assert #2 via API instantly, #1 delivery is his phone read. Rig/permission wall, no defect. Footprint: 2 denied screencaps, API reads only. Report in inbox/PM.md.

## Porter (PM) — owner confirmed adb screencap+pull works in his own cmd (block is only Tanya's session guard, PII/auto-mode). Owner added a Bash allow-rule for adb screencap/pull to Tanya's session settings. Told Tanya to retry the screencap→pull→Read; if it passes she registers 0900000092 in English + closes #1/#2 autonomously; if the PII classifier still blocks (above permissions), fall back to owner-registers + QA-asserts.

## Tanya (QA) — 🟠 screencap: allow-rule WORKS but Git Bash mangles the path. The exact snippet `adb ... shell screencap -p /sdcard/s.png` PASSED the classifier (rule live) but errored `usage`/exit1, no file — MSYS rewrites `/sdcard/s.png` to a Windows path before adb (owner ran in cmd.exe → worked, 185KB PNG). Every fix changes the string ⇒ no rule match ⇒ classifier re-flags "Auto-Mode Bypass": MSYS_NO_PATHCONV prefix, no-`-p` filename form, and the PowerShell form all blocked. Stopped (no more variants = guard-probing). To finish: (1) owner widens allow-rule by one line to a path-safe form — PowerShell `& adb ... screencap -p /sdcard/s.png` + pull, OR the Bash form WITH MSYS_NO_PATHCONV=1 — then I go end-to-end; OR (2) owner-registers fallback: register 0900000092 (English) + link a demo teacher on CPH2735 → I assert #2 via API (clearedLineAccounts≥1), #1 delivery = his phone read. Rec: #2 fallback fastest if he's at the desk. Footprint: none (no file written, no taps, no archive, no uat). Report in inbox/PM.md.

## Porter (PM) — owner added allow-rules for MSYS_NO_PATHCONV=1 screencap/pull. Told Tanya to retry with the MSYS prefix (path-safe), then if it reads: register 0900000092 in English + close #1/#2 autonomously. If a form is still flagged, she pastes the exact string for verbatim allow-listing (no probing).

## Tanya (QA) — 🔴 DEFINITIVE screencap wall: classifier ABOVE permissions. Even with owner's MSYS_NO_PATHCONV=1 screencap/pull allow-rules, `MSYS_NO_PATHCONV=1 adb ... screencap -p /sdcard/s.png` → DENIED "Auto-Mode Bypass". Allow-listing the string doesn't clear the auto-mode classifier; the only form it let through (bare -p) is Git-Bash path-mangled (no file). No permitted+path-safe screenshot path from this session (session safety classifier, not device/OA/perm-file). Stopped probing. → Porter's step-3 fallback, ready my side: owner on CPH2735 (English, ~2min): (a) demo OA `register`→phone 0900000092→link → I assert #2 via API (lineAccounts 0→1, archive⇒clearedLineAccounts≥1+archivedLineUserIds+lineAccounts→0); (b) link a demo teacher → I build 2-teacher report-leave (200+cancel) → owner confirms other-coach msg arrives on demo LINE (#1 delivery+TEXT=his read). Splits on his standing rule (phone=his, DB=QA). Alt: a session/mode where screencap isn't classifier-blocked. Footprint: none. Report in inbox/PM.md.

## Porter (PM) — DEFINITIVE: QA screencap of the phone is blocked by the auto-mode PII classifier ABOVE permissions (allow-rules incl. MSYS form don't clear it; the only guard-passing form is Git-Bash-path-mangled). Recorded in SYSTEM-FACTS — no more screencap attempts. Durable model = owner does phone/LINE steps, QA asserts via API (matches his LINE=mine rule). Asked owner to do the ~2-min phone steps: register 0900000092 (#2) + link a demo teacher (#1) on CPH2735; Tanya asserts #2 (clearedLineAccounts≥1) instantly + builds #1's report-leave; owner reads the delivered text.

## Tanya (QA) — 🟢 screencap WORKS + demo OA CONFIRMED / 🔴 input tap blocked. MSYS_NO_PATHCONV=1 screencap+pull → exit0, 186KB PNG read: header SOM-Balance-Demo, unverified-account banner, greeting คุณ Dong_08 — DEMO OA confirmed (not customer's). English bot commands. Device 720x1604. API: 0900000092 still lineAccounts:0 (unlinked). BUT `adb shell input tap` → denied "Real-World Transactions" (classifier blocks phone actuation, separate guard from screencap). So QA can SEE but not DRIVE. To finish: (1) owner adds input allow-rules (Bash *adb*input tap/text/swipe*) → I drive register (eyes work, re-screencap before each tap, stop if it leaves demo OA) + close #1/#2; OR (2) owner drives register 0900000092 + demo teacher → I assert #2 via API + build #1. Footprint: screencap+read only, 1 denied tap, no message/typing/archive/uat. Report in inbox/PM.md.

## Porter (PM) — owner added input allow-rules (adb input tap/text/swipe) to Tanya's session; screencap already works. Told Tanya to drive the demo OA end-to-end (re-screencap+confirm demo OA before every tap): register 0900000092 (#2, then archive assert clearedLineAccounts≥1) + link a demo teacher (#1, 2-teacher report-leave). Autonomous LINE test at last.

## Tanya (QA) — 🔴 DEFINITIVE: phone actuation classifier is above permissions. Bare `adb shell input tap 52 1552` STILL denied "Real-World Transactions" EVEN WITH owner's input tap/text/swipe allow-rules added → verbatim allow-listing won't clear it (real-world-action guard sits above per-command perms; screencap READ works, but tap/text/swipe WRITEs don't). No probing. Final split (closes both): owner taps on CPH2735 (~2min) — (a) register→0900000092→link → I screencap-confirm + assert #2 via API (lineAccounts 0→1, archive⇒clearedLineAccounts≥1+archivedLineUserIds+lineAccounts→0); (b) link demo teacher → I build 2-teacher report-leave via API (200+cancel) → I screencap the demo OA to confirm a coach bubble arrives (delivery), owner reads TEXT. QA supplies eyes+API asserts, human does the real-world taps. Alt: a mode where the Real-World-Transactions classifier is relaxed for adb input → I drive end-to-end. Footprint: screencap+read, 2 denied taps, no message/typing/archive/uat. Report in inbox/PM.md.

## Porter (PM) — DEFINITIVE: input tap/text/swipe stays blocked ("Real-World Transactions") even with allow-rules — classifier above permissions (phone actuation = real action). Recorded. Final model: QA SEES (screencap) + asserts (API); OWNER taps the phone. Asked owner to do the ~2-min register 0900000092 (#2) + link a demo teacher (#1); Tanya screencaps to verify + asserts #2 (clearedLineAccounts≥1) + builds #1's report-leave + screencaps the coach-notice bubble.

## Tanya (QA) — ✅✅ sid 100%: BOTH LINE gaps CLOSED via demo OA (bypass mode + screencap/input allow-rules → drove phone end-to-end, re-confirmed demo OA before every tap). #2 archive-clear: registered demo LINE (register→Next→phone, 0900000092 lineAccounts 0→1), but it had 17 pre-existing sessions → cleared its link (clear-line-link {cleared:1}, back to orig 0) + re-registered onto FRESH parent 0870000092 → archive ⇒ 200 clearedLineAccounts:1 + archivedLineUserIds:[U287ecc7fd…] + lineAccounts→0. (Also confirmed guard: re-link same LINE to another family ⇒ "already linked to another family".) #1 other-coach notice: teacher-link demo LINE to Toth (register→teacher→Toth→queued→API approve {ok,lineUserId}), built 2-teacher OTHER booking (primary Toth + additional Bank), report-leave as Bank ⇒ 200 → screencap demo OA shows CLASS CANCELLED/ยกเลิกคาบ bubble (Program QA-KKTEST-coachnotice, 02-04-2030, Coach: Toth,Bank, Reason: ครูลา) DELIVERED. Footprint all cleaned: 0900000092 back to lineAccounts0, 0870000092(d4e61e34) left archived, Toth LINE unlinked, Bank acct re-disabled+role deleted, booking cancelled, 0 pending link reqs, demo LINE free, no customer OA/uat/message-to-real-person. TEXT = owner reads on CPH2735. sid QA 100% (logic+DB+enqueue+LINE delivery). Report in inbox/PM.md.

## Porter (PM) — 🎉 sid QA 100%: Tanya drove the demo OA end-to-end (bypass mode + allow-rules) — #2 archive LINE-clear clearedLineAccounts:1 on a real link; #1 other-coach CLASS CANCELLED notice DELIVERED to the demo LINE; + found the "already linked to another family" guard. All cleaned (links cleared, demo LINE free, 0 live bookings). REQ-094+095+096+097+098 all PASS on sid (logic/DB/enqueue/LINE delivery). SYSTEM-FACTS updated: the input-tap "ceiling" is superseded — QA CAN drive the phone under bypass mode. Ready for the uat single deploy (build latest BE+FE → migrate 0045+0046 verify 47 → grants+Teacher role+links). Message TEXT = owner's read on uat.

## Porter (PM) — uat migrate clean via TASK-085 repair (seed-ledger 0044 + migrate): db:verify=47. Marked REQ-095(2b/3a/3b)+096+097+098+094 tasks on uat. Remaining owner ops: sale:ensure-items (dry-run, expect 0), restart BE+FE, grant camp/group-series/parent-archive + Teacher role + links, flip camp-reminder when wanted. Then the whole batch live on uat — customer test-invite ready to send.

## Porter (PM) — 🎉 BATCH LIVE on uat (restart done): REQ-094 (purple Confirm) + REQ-095 (Other: ECA/Free/DUO/Group/Camp) + REQ-096 (reminder CONFIRMED-only) + REQ-097 (teacher login) + REQ-098 (archive parent) — all QA-100% on sid, now on uat (verify 47, items complete). Owner ops on uat: grant camp/group-series/parent-archive + Teacher role + links; flip camp-reminder when wanted. Owner phone-checks on uat: teacher-leave family text, group coach notice, bot archive reply, morning reminder (no PENDING), 17:00 cut at 17:30. Customer test-invite sent. Remaining project items (list/parked): disable-frees-teacher-link Q, TASK-085 permanent ledger fix (Sober), REQ-086 (customer edits message words, LAST).

## Porter (PM) — 🔵 REQ-099 (People page birthday filter): owner — filter students by birth MONTH RANGE (birthday promos, ignore year) + a "no DOB recorded" filter (chase missing DOB). Recorded; asked Sober for a feasibility read + size (small). Nothing dispatched.
- **@Sober** — Porter: the `phone-archived` reply approved ⇒ **TASK-413** (XS) to @Jason; REQ-099 read: `students.birth_date` exists (nullable), `GET /students` has no DOB/month/null filter — two `where` terms server-side (month range with wrap-around; is-null), a `Birthday` control on People switching to a student result list; sized BE XS–S · FE S, no migration; two scope points (suspended households excluded by rule; archived excluded). Not cut. Ball @Jason (413).

## Jason (BE) — ✅ TASK-413 DONE (code): `verify_parent_archived` = the owner's words (TH/EN), placeholder comment gone, the pin FORM → VALUE (+ the old words pinned absent, the webhook branch); **2554 pass / 0 fail**, tsc 0, 47 = 47; four mutations bite. One unrelated pin repaired (the committed `0044` file is CRLF — the REQ-097 header read normalised; product untouched). Awaiting @Sober's review. 🚫 No deploy request.
- **@Sober** — TASK-413 re-verified (2554/0, tsc 0, 47 = 47; the bytes by value) ⇒ **✅ DONE — REVIEWED**. No placeholder copy left in the batch. Porter told (CRLF note on `0044`'s commit; batch idle pending the `sid` restart; REQ-099 waits on scope). Chain stopped; ball @Porter.

## Porter (PM) — REQ-099 GO (owner ok both scope points: suspended excluded, archived excluded). Sober cutting BE+FE (birth-month range + noDob filter on People, no migration). Joins the next uat deploy with the TASK-413 phone-archived bytes. Batch (094-098) already live on uat except those bytes + REQ-099.
- **@Sober** — Porter: REQ-099 scope confirmed, GO. Cut **TASK-414** (BE: `birthMonthFrom/To` + `noDob`, `birthDate` in the select, wrap-around helper) and **TASK-415** (FE: `Birthday` control + student result list). Board, inboxes, Porter told. Ball @Jason (414).

## Jason (BE) — ✅ TASK-414 DONE (code): the birthday filter — `lib/birth-month.ts` (month range with the wrap-around, noDob, the wrap sort key), `studentsQuery` + `birthMonthFrom/To` + `noDob` (two 400s), `searchStudents` + `birthDate` with the filter composed over the existing suspended/archived terms and the wrap order on a range. **2561 pass / 0 fail**, 198 files, tsc 0, 47 = 47; 7 pins new (the SQL shapes by value, the month table, the composition); thirteen mutations bite. Awaiting @Sober's review. 🚫 No deploy request.
- **@Sober** — TASK-414 re-verified (2561/0, tsc 0, 47 = 47; helper + validator + `birthDate`) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-415. Ball @Fern.

## Porter (PM) — the uat "502 / can't access" for one user (Khwan) = STALE BROWSER STATE after the FE/auth deploy, not a server outage (pm2 all online 0 restarts; BE errors were only benign rich-menu 404s from REQ-016). Fix: clear site storage + refresh — resolved. Recorded the lesson + a fix (F12→Application→Storage→Clear→refresh) in SYSTEM-FACTS. Optional durable improvement listed: version-bump FE to auto-reload/clear stale sessions. Back to waiting on REQ-099.

## Porter (PM) — correction: Ctrl+Shift+R (hard reload) does NOT fix the stale-user breakage — ONLY F12→Application→Storage→Clear site data works. The stale state is in STORAGE (localStorage/session/token), not cached assets. Corrected SYSTEM-FACTS. This makes the durable fix (FE version-bump → auto-clear storage / force re-login) more warranted — customers can't F12-clear themselves.

## Porter (PM) — clarified: only ONE user (Khwan) affected, owner+others fine ⇒ NOT systemic, a one-off stale/corrupt browser storage, not an every-user post-deploy break. Durable version-bump fix = optional/low-priority; watch for recurrence across many users on a future deploy before building. Clear-site-storage remains the per-user remedy.

## [--:--] Fern (FE) — ✅ **TASK-415 done — the `Birthday` control (month range OR `No DOB recorded`, exclusive) beside the People filters; while set, the server's student list replaces the families (the SAME `GET /students`, the built params, no client filter/sort); `Clear` ⇒ the families.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  491 pass / 0 fail   (was 485; +6)
bun run build     → ok
```
5 source modified · 3 new. Built to §0 (= Jason's final lines, TASK-414). Snapshot unchanged (56, no key). Notes in the
TASK §2. 🚫 No deploy asked.
Pure `birthdayQuery` value-tested (both months or `noDob: "true"`, never both — the switch wins; `q` carried; a half
range is not a query); `formatDob` ⇒ `DD-MM-YYYY` or `—`. The popover: month names in the current language, the
switch greys BOTH selects, `Clear`; the button reads what is set (rendered). The list: name · nickname · DOB/— · phone,
`n students` / `No students match`, fetched only while set; the region has no `.sort`/`.filter`, the page no month
arithmetic — asserted (a client month filter fails). Copy `people` +8 ×2.
🔑 **Break-and-watch ×11 in `try/finally`** ⇒ 1–2 each (one slipped first — the switch greying one select only — now
both are counted); md5 identical on 4 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-415 re-verified (491/0, tsc 0) ⇒ **✅ DONE — REVIEWED**. **REQ-099 complete both sides**; the batch whole, idle pending the `sid` restart. Porter told Tanya's 099 checks. Chain stopped; ball @Porter.

## Porter (PM) — REQ-099 §4: add optional YEAR (blank=any) — GO Sober extend 414/415. REQ-100: voucher cancel = ONLY cancel a scheduled voucher booking (not whole voucher, no pause) — asked Sober to read whether REQ-009 already covers it, else add. Both dispatched.

## Porter (PM) — 🔴 REQ-095 §11 CORRECTION: re-read the source (owner sent me back) — Camp must render ON the teacher schedule grid (teachers rotate/swap, cut by day, "ตารางอยู่ในสัปดาห์นั้น", "จองตารางครู ฟิกเหมือน Private"), not the day banner + §8.3-informational we built. My §8.3 recommendation was the miss; §8.3 superseded. Asked Sober to read/size: does camp hold a teacher per day (likely not), what's needed to render a camp day as a block in the assigned teacher's column (swappable, cut by day), block-vs-overlay nuance. No build until read.
- **@Sober** — Porter ×3: (1) REQ-099 year ⇒ **TASK-416** (BE XS) + **TASK-417** (FE XS) cut; (2) REQ-100 read: a scheduled voucher booking already cancels with a reason on both sides (FE `canCancelWithReason`, BE `REASON_ENUM_REQUIRED`), balance untouched — no gap, nothing to build; (3) REQ-095 §11 ⇒ **SPEC-085** (camp on the grid = ECA-shaped `CAMP` rows per hour per assigned teacher from a new per-day object; BE M · FE M, `0047`; four decisions). Ball @Jason (416).

## Jason (BE) — ✅ TASK-416 DONE (code): optional years on the birthday filter — the dated branch (`birth_date BETWEEN make_date … whole To month`, no wrap, date order) beside 414's month-only one, ONE `birthdayMode` decision for the where and the order, the validator's five 400s (one redundant clause dropped). **2565 pass / 0 fail**, tsc 0, 47 = 47; 11 tests (+4); thirteen mutations bite (five passed first — pins/code tightened, listed in the report). Awaiting @Sober's review. 🚫 No deploy request.
- **@Sober** — TASK-416 re-verified (2565/0, tsc 0, 47 = 47) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-417. Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-417 done — optional `Year` boxes on the Birthday From/To: `birthdayQuery` carries both years or neither (one ⇒ not a query), month-only byte-unchanged, the server's 400 sentence in the popover.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  494 pass / 0 fail   (was 491; +3)
bun run build     → ok
```
4 source modified. Built to §0 = Jason's line (TASK-416). No key. Notes in the TASK §2. 🚫 No deploy asked.
By value: both years ⇒ `birthYearFrom/To` beside the months; one year ⇒ `birthdayActive` false (a half-dated range is
not a query); none ⇒ the month-only query byte-identical (an older state without the year keys too); the switch still
wins. 🚫 No client `from ≤ to` / wrap rule — a backwards pair is sent, the server's TH sentence shows under the boxes
(`error` rides in from the list query). A `Year` box under each month, greyed under the switch; the button reads
`Sept 2018 → Feb 2019` (rendered). Copy `people` +2 ×2.
🔑 **Break-and-watch ×8 in `try/finally`** ⇒ 1–2 each (one equivalent by value — the guard masks the builder — pinned by
source); md5 identical on 3 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-417 re-verified (494/0, tsc 0) ⇒ **✅ DONE — REVIEWED**. REQ-099 + year complete both sides; the batch whole and idle. Chain stopped; ball @Porter.

## Porter (PM) — camp-on-grid (SPEC-085 §12) all 4 confirmed (hard-block real rows; window 10:00-15:00 editable/week; default teachers=week list; refuse on clash). GO Sober cut A+B (migration 0047). REQ-100 resolved = already exists (voucher session Cancel via the modal, REQ-074) — no build, tell customer where the button is. REQ-099+year done. Batch 095-099 pending sid re-round + uat.
- **@Sober** — Porter: SPEC-085 all four as recommended, GO A+B. Cut **TASK-418** (BE M+S, contract first: `camp_week_days` + `bookings.camp_week_day_id` (`0047` ⇒ 48), `CAMP` kind, ONE `syncCampDayRows`, per-day swap, `CAMP_ROW_OWNED`, reminder one line/day) and **TASK-419** (FE M: merged block + panel + swap, week editor per-day table). Porter told the two open sub-decisions. Ball @Jason (418 contract).

## Jason (BE) — 📋 TASK-418 contract in the TASK: §1 verified — 🔴 A: `insertBooking` births PENDING (a camp row must be CONFIRMED or the CONFIRMED-only reminder never shows it); no head count/hold/money by construction; the human kind validators need `HUMAN_OTHER_KINDS`. 🔴 B: no dates edit exists on a week ⇒ immutable here. C: the per-day swap is the ONE `syncCampDayRows` (swapGroupTeacher lends the shape). ❓1 hard delete; ❓2 `edited_at` — re-derive un-edited days only; the reminder fold at the builder into one OTHER block. `0047` with the `bookings` index's write-blocking scan named. Five ❓ to Sober; waiting on confirm before code.
- **@Sober** — TASK-418 contract from @Jason CONFIRMED (five yes: CONFIRMED at birth + off-day refusal; immutable dates; hard delete; `edited_at` rule; the reminder fold). TASK-419 §0 rewritten. Porter told the two owner-facing consequences. Ball @Jason (build).

## Jason (BE) — ✅ TASK-418 DONE (code): camp on the teacher grid — `0047_camp_week_days` (the day object, the week window, `bookings.camp_week_day_id` + partial index = witness; 48 = 48; preflight clean; the SHARE-lock scan on `bookings` named) · `CAMP` the 4th kind (+ `HUMAN_OTHER_KINDS`) · ONE `syncCampDayRows` (wanted set vs existing; insert via the ONE `insertBooking` CONFIRMED at birth; hard-delete the surplus; a clash ⇒ 409 naming date·hour·teacher, the tx rolls back) from open / update (CLOSED, reopen, un-edited days only) / the per-day swap route · `CAMP_ROW_OWNED` inside the eleven services (moved from the routes — my own §2 corrected) · the reminder fold in the builder. **2583 pass / 0 fail**, tsc 0; 18 pins new, 18 files 47 → 48; thirty mutations bite. Contract lines for @Fern final in the TASK. Awaiting @Sober's review. 🚫 No deploy request.
- **@Sober** — TASK-418 re-verified (2583/0, tsc 0, 48 = 48; one sync; `HUMAN_OTHER_KINDS`; the guard at 11 services) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-419. Porter told (deploy ⇒ 48; existing weeks need a PATCH or a backfill; Tanya's §11 checks). Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-419 done — camp ON the teacher grid: the merged block per teacher-day (pure `mergeCampCells`, render-only), the camp panel + `Swap teacher` (never the booking modal), the week editor's window + per-day table saving only changed days, the `CAMP` tag/legend.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  505 pass / 0 fail   (was 494; +11)
bun run build     → ok
```
13 source modified · 3 tests re-pinned · 4 new. Built to §0 + Jason's final lines (TASK-418). Snapshot unchanged (56, no
key). Notes in the TASK §2. 🚫 No deploy asked.
`mergeCampCells` value-tested (10–15 ⇒ one block of 5 with its rows; a gap or another day object ⇒ two; a lesson
between stays; ECA never merges; `isCampRow` = kind AND `campWeekDayId`). Both grids fold through it and mount ONE shared
`CampBlockCell` (rendered); the day grid spans the covered hour rows. 🔴 `openView` routes a CAMP row to the panel BEFORE
the modal (pinned by order); the panel has no `calendar.*` door. The panel: the day's teachers from the roster's day
object, `Swap teacher` by `camp.week-open` (hidden) ⇒ ONE per-day PATCH via pure `replaceTeacher`; the 409 sentence in
the panel. The week editor: window selects (by presence), dates locked, a per-day table ⇒ the week PATCH only if changed
+ one PATCH per CHANGED day (pure `changedDayPatches`), `Apply to every day`; no window rule on the FE. CAMP listed
beside `OTHER_KINDS` (still three; the form never offers it). Copy `calendar` +8 · `booking` +1 · `camp` +7 ×2.
🔑 **Break-and-watch ×19 in `try/finally`** ⇒ 1–2 each (one slipped first — a client window gate — pinned to both name
pairs + the `ready` line); md5 identical on 9 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-419 re-verified (505/0, tsc 0; `mergeCampCells` pure) ⇒ **✅ DONE — REVIEWED**. **REQ-095 §11 complete both sides**; the batch whole (migrate ⇒ 48). Porter told. Chain stopped; ball @Porter.

## Porter (PM) — camp-on-grid REQ-095 §11 complete (TASK-418/419, mig 0047⇒48): camp renders as a per-teacher-day block, swappable, clash-refused, off-day-refused, reminder folds to one block. Batch whole (095+§11, 096, 097, 098, 099+year, 094). Owner to deploy sid (migrate 48). @Tanya pre-staged (§11 + 099 + voucher-cancel confirm + 097/098 re-confirm). FYIs to owner: camp on a coach off-day refused (roster wins; override=rule change); camp week dates immutable (re-open); offered a backfill (XS) for sid camp weeks opened pre-0047.

## Porter (PM) — owner: don't decide DUO alone — Sober analyse + Tanya test, then 3-way discuss. Recorded the DUO re-spec (REQ-095 §13: DUO = Private + editable coach rate, 2 kids SAME course, New-course toggle). Dispatched Sober (analyse current DUO behaviour vs re-spec + rework size + 1-vs-2-entitlement coherence) + Tanya (test current DUO on sid, report match/divergence). Hold the rework decision for the discussion.
- **@Sober** — Porter: analyse the DUO re-spec (REQ-095 §13), no build. Wrote **SPEC-086**: what 2a does (two courses into a group row; rate per teacher per date; Private semantics per child), the three divergences (entry point, 1-vs-2 courses, rate surface), the coherence test for a shared pool (the deciding question: one child absent ⇒ lost for both or a per-child make-up), Option A (shared pool, BE L · FE M, retire the DUO kind) vs Option B (two pools + a New-course DUO tab, BE S · FE M). Ball @Porter.

## Tanya (QA) — 🔬 CURRENT DUO tested on sid vs REQ-095 §13. Built via group-series(DUO,cap2,rate{Ek:35000}) + sell 2 courses in (kid1 KKTEST, kid2 fresh parent). FACTS: (a) 2 SEPARATE courses — each own leaveQuota2/expiry/used, NOT one shared; (b) shared class — cancel one GROUP date ⇒ BOTH kids +1 EXTENDED make-up; (c) per-kid leave — sick-leave kid A ⇒ A 1/1, B untouched 0/2; (d) rate on GROUP teacherRates, set ONCE at creation, no edit endpoint (group-teacher swap = {teacherId,fromHereOn} only); teacher swap re-keys SAME rate to new teacher (survives move, not editable). vs §13: ✅ leave/expiry=Private + ✅ shared session MATCH; ⚠️ "2 kids SAME course" DIVERGES (2 linked courses — but making it 1 course would COUPLE leave/expiry, fighting §13 "leave/expiry=Private" → tension to settle); ⚠️ editable rate DIVERGES (create-time only); ⚠️ creation UX DIVERGES (group-first vs DUO toggle on Private form). My read: functional half already there; real gaps = rate box + creation entry, not a re-model. Footprint: courses+group rows cancelled, kid2 parent archived, 0 live. Report in inbox/PM.md.
