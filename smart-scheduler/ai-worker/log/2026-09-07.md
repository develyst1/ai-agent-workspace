# 2026-09-07

## [--:--] Porter (PM) — 🔴 @Tanya found a REAL hole in the owner's own two rulings. Quota can be laundered.
She cannot mint a disposable course booking — **`เพิ่มการจอง` offers `1st Trial · 1 HR · Voucher · อื่นๆ` and no
`คอร์ส`.** Course sessions come from the course plan, so **the only way to make a fresh one is to enrol a
student, which SELLS sessions and posts money.** ⇒ **Not a defect: it is the design, and it matches `REQ-076`
AC-3 leaving course bookings to REQ-071.** **She refused to create a real sale to make a test fixture. Correct.**
⇒ She is running the cycle **backwards** on an existing `ลา/ป่วย` session instead.

🔴 **And that surfaced the finding, which is mine to answer and I am not deciding it alone:**
- **`C-22` (owner, 09-04):** a leave **declared** after the course was created **consumes quota.**
- **`REQ-083` AC-4 (owner, 09-05):** a **correction** `ATTENDED → SICK_LEAVE` consumes **none**.
⇒ **Two roads to the identical `SICK_LEAVE` state, different quota cost.** **KKTEST would end `0/2` having
started `1/2` — the cycle is not state-neutral and the card cannot be restored by re-running it.**
🔴 **The consequence nobody has said out loud: an admin can take a leave WITHOUT spending quota** — mark the
session attended, then correct it to sick leave. **Same absence, no quota.** ⚠️ **That is exactly what AC-4 asks
for, so it is not a defect on its face** — **it is a business hole, and only the owner can say whether he meant
it.** **Put to him.**
📌 **She flagged it instead of deciding it, and said she will DECLARE the end state rather than quietly restore
the card.** Both right — **a tester who tidies away the evidence of her own run is the thing you cannot audit.**

## [--:--] Porter (PM) — 🔻 The owner has had to say "ทำต่อให้จบสิ" repeatedly. That is MY failure, now fixed in `QA.md`.
Owner: *"เธอหยุดไปเลยตลอด ฉันต้องบอกว่า ทำต่อให้จบสิ ตลอด ๆ"*.
🔻 **He is right, and the cause is that no rule ever told her otherwise.** I kept writing *"Ball: yours"* and she
kept coming back after every obstacle — **each one a full round trip through me to a man with a shop to run.**
⇒ **`QA.md` now carries "WHEN TO STOP, AND WHEN TO CARRY ON".** **The default is CARRY ON.** Stopping is five
named cases: a write on `uat` · a credential she cannot have · real money or a real message · **a question that
changes WHAT SHE WOULD TEST NEXT** · destructive on data she did not create.
📌 **The distinction that does the work: a FINDING is not a STOP.** *"C-22 and AC-4 differ"* is a paragraph in
the report; a blocked fixture is a `NOT_TESTED` line **and the next AC.** **Four criteria with three runnable is
three results — stopping at the first obstacle produces zero.**
⚠️ **And I said plainly that this touches none of her refusals** — not enrolling a student to mint a fixture, not
routing around the classifier, not restoring her own end state. **Every one was right.** **The rule is about what
happens AFTER the finding is written down.**
✅ **Owner answered (ก): the quota difference is INTENDED.** A correction is an admission the record was wrong,
not a new leave request. **`AC-4` stands; KKTEST ends `0/2` and she declares it.** ⚠️ **The consequence is
recorded as knowingly accepted** — an admin can avoid spending quota by marking attended then correcting.
**Not a defect, and not hers to re-raise.**

## [--:--] Porter (PM) — 📨 Owner sent two LIVE `CONFIRMED SCHEDULE` messages. Two defects, both traceable to me.
🔴 **`Sessions` subtracts the advance leaves** — `10 HR` printed `8`, `6 HR` printed `5`. **The customer says it
must be the full count.** 📌 **And their reasoning is better than the implementation's:** the line sits in a
message **that also prints the leave dates**, so a remainder makes the reader subtract twice. **`Sessions` is the
size of the course as bought.**
🔴 **The booking note is missing and they name it `Remark`.** **`REQ-007` already requires it to reach the teacher
on LINE** ⇒ a line that fell out, not new scope. 🔻 **The label is my omission for the SECOND time in two days** —
I confirmed keeping the note **without naming it**, exactly as I did with `Sessions`. ⚠️ **Its absence from both
samples is my own omit-empty rule working** (neither booking had a note) — **so it must be verified with a note
present, or we ship a field nobody has watched render.**
⏸️ **The teacher's missing `*Expiry date` / `**Advance Leave Notice` is MY Decision 5**, flagged reversible when I
made it. **The owner is asking ⇒ it goes back to the customer as a question, not as something we quietly did.**
📌 My read: the coach needs the **leave notice**, not the **expiry** — **two fields, two decisions.**
⚠️ **My own observation, not filed as a defect:** teacher `Date : อาทิตย์` vs parent `Date : Monday`. **Consistent
with the language switch and probably correct** — but they asked for *"English labels, Thai values"*.

## [--:--] Porter (PM) — 🔻 Owner: *"เอาหมด"* — Decision 5 REVERSED. The teacher's copy is the parent's again.
**Both lines go back.** ⇒ the customer's 09-05 draft was **intent, not copy-paste**, and I overrode it.
🔻 **The shape of my error, which is the part worth keeping:** I saw two byte-identical messages and read them as
a mistake. **They looked exactly like meaning it, too.** ⇒ **the artefact was equally consistent with both
readings, which makes it a QUESTION, not a judgement call** — and I turned it into a decision with a note attached.
📌 **The note is what saved it.** Labelled mine and reversible ⇒ this costs one line and no argument. **The lesson
is not "stop deciding" — he told me to design.** It is **"when the evidence fits two intents equally, ask."**
📌 **And my reasoning is overruled, not withdrawn:** a coach seeing a family's expiry is still family information
on a staff phone. **The customer knows their own shop.** Recorded once; not re-raised.
⇒ **All three items are now one pass over one message:** `Sessions` counts the leaves · `Remark :` last, **tested
with a note present** · teacher == parent. **Asked @Sober whether `AUDIENCE_OMITS` still has anything to omit
here** — machinery that looks live and is not is worse than none.

## [--:--] Porter (PM) — 🟢 The carry-on rule is working. @Tanya is reporting findings without stopping.
Three findings and a self-clearing measurement in one message, **and she kept going.** ⇒ **the `QA.md` rule
landed.** She cleared herself on `ดิววี่` by reading the live DOM (`มาเรียน` occupies **1190–1292**; her three
clicks were ~117px and ~99px away) **and still refuses to withdraw the DATA REQUEST** — *"I would rather be
confirmed than believed"*, footprint entry stays. **That is the standard I want quoted back at me.**

### 🔴 The finding I am escalating, because it is a live product risk, not a test artefact
**`มาเรียน` ends at x=1292. The `⋮` menu starts at x=1300. EIGHT PIXELS.** And **`มาเรียน` marks attendance,
consumes entitlement and posts revenue.**
🔴 **AND IT MAY CONTRADICT A SHIPPED REQ.** She reports **no confirmation step**. **The owner's REQ-006 (✅
Completed) required a popup confirm on `มาเรียน`** — *"จากหน้าแรก"*. ⇒ **If the confirm exists on the home page
and NOT in the booking modal, a shipped safety control is missing on one of the two paths to the same act.**
⚠️ **I am not calling it: she was in the modal, REQ-006 named the home page.** **One read settles it and it is
worth doing before anyone argues about pixels.**
📌 **Her sentence is the argument:** *"I nearly charged myself with a real write because of that gap, and I was
clicking measured coordinates. A tired admin at 23:00 has worse aim than I do."*

### Not filed, correctly
The booking modal would not close (Escape, and `ปิด` by reference, ×3). **Mantine keeps FIVE `Modal-root` nodes
mounted**, so a reference click can hit a hidden one ⇒ **plausible harness artefact, not filed.** She needs one
**coordinate** click, which is the call the mode refused. 🔴 **If it reproduces it is serious — an admin who opens
a booking cannot leave it.**
🟢 **Network capture is now ON** — the `ดิววี่` ambiguity existed only because it was off. **Nothing further will
be unattributable.**

## [--:--] Porter (PM) — 🟢 Owner granted @Tanya FULL access on both `sid` hosts, backoffice included.
> *"ฉันให้สิทธิ์ … แบบนี้เราจะไม่ได้ไปไหนกันพอดี"*
⇒ **The backoffice grant retires the blocker that has held `AC-5/6/7/9` for days** — she can now read the money
movements herself instead of every revenue AC dying as `NOT_TESTED`. **`uat` is untouched by this: read-only,
every write still a DATA REQUEST.**
🔴 **He pasted the credentials into chat.** **They are NOT in any tracked file** — not this log, not `inbox/QA.md`,
not `TEST-066`. **I pointed her at `project-docs/sm-test-access.txt`, which is git-ignored and is the home
`QA.md` rule 6 already names.** ⇒ **Asked him to put the backoffice line there rather than in a message.** Said
once, not laboured — **the grant was the right call; only the channel was wrong.**
📌 **And I told her to close her OWN two open items now that she can:** the `ดิววี่` audit timestamp — **she can
read it herself, which retires her DATA REQUEST on her own terms rather than on my say-so** — and the one
coordinate click that decides whether the un-closable modal is real or a harness artefact.

## [--:--] Sober (SA) — 🔴 The message contradicted ITSELF. `Program : 10 HR` and `Sessions : 8`, one message, two derivations. TASK-269 cut.
@Porter brought three `CONFIRMED SCHEDULE` corrections off two live `sid` messages. **All three are one pass over
one renderer** — `SPEC-072` §8, `TASK-269` → @Jason. **No migration, no database, no new message kind.**

**1. `Sessions`.** `confirmCourse` printed the count of rows it FLIPPED; an advance leave is `SICK_LEAVE`, never
`PENDING` ⇒ `10 HR` with two leaves printed `8`. 🔴 **The fact that decides the fix was in neither report:** the
same message already prints `Program : Surfskate 10 HR`, built from `course.size`. **One message, two
derivations of one number, disagreeing, in front of a parent.** ⇒ `Sessions` reads **`size`** — the field
`programLabel` reads — **so they cannot disagree again.**
🚫 **Not `confirmed + plannedLeaveDates.length`,** which was the obvious patch: it fixes the reported symptom and
leaves **a re-confirm printing `0`** and **a budget failure subtracting silently.** 📌 **A derived figure can
disagree with the one printed beside it; a shared field cannot.**
🔑 **And `confirmed` still gates the send.** *"Nothing changed ⇒ nothing to announce"* and *"how big is this
course"* are two questions that were **one variable** — separating them is most of the change.

**2. `Remark`.** Label only; it was already the last line. ✅ **Asserted with a note PRESENT**, as @Porter asked —
its absence from both samples was his own omit-empty rule working, so absence proved nothing.
📌 **One limit recorded rather than fixed:** the note is `rows[0].attendeeNote`, the **earliest** session's. One
note at enrolment lands on every session, so the normal path is exact; a note **edited on one session** reaches
this message only from the earliest row. **A course summary has no true answer to "which session's note" when
they differ, and inventing one is worse than the limitation.**

**3. 🔻 Decision 5 reversed — and the consequence nobody had seen.** `AUDIENCE_OMITS.teacher = []`.
🔴 **`advanceLeave` resolves to `ไม่มี` BEFORE the omit-empty rule can reach it** ⇒ **the teacher now gets
`**Advance Leave Notice : ไม่มี` on every course with no leaves — reversing TASK-206's teacher-side reasoning.**
**Ruled: field for field includes the empty case.** *Silence cannot be told from a missing feature* is at least
as true for a coach checking whether a rostered child will be absent, and **"identical except when empty" is a
third rule nobody asked for.** ⚠️ **Sent to @Porter to say out loud to the owner** — the same shape as his
Decision 5: a defensible call the owner has not been told about. **Not re-opened; declared.**

**@Porter's question — `AUDIENCE_OMITS` is empty. KEPT.** 🚫 Not deleted: **its one line is what made this
reversal cost one line.** That is the mechanism doing its job, on a field the customer has now changed their mind
about twice; deleting it re-threads `audience` through five signatures next time. ⇒ **empty, comment rewritten,
and pinned by the REQUIREMENT rather than by an assertion about the table** — *the same payload rendered to
teacher and to parent must be byte-identical.* **An assertion that repeats the table proves the table equals
itself.** 📌 **`audience` is not dead** — `line-schedule.ts` still splits `คาบสอน` / `คาบเรียน` by it, and the
comment now says both halves. ✅ **`TODAY'S SCHEDULE` stops differing too** (the teacher regains `*Expiry date`),
which is what the customer's own 09-05 draft asked for — the half @Porter correctly refused to assume.

**The day-name question — by design, and not a default.** `lineLang` is `null → TH` and is **seeded from that
person's own LINE profile locale** on linking. ⇒ **the parent who read `Monday` has an English LINE app**; a
Thai-preference family always gets `อาทิตย์`. **Nothing to fix.** ⚠️ **Caveat handed over, not buried:** *"English
labels, Thai values"* is literally true only for a **TH** recipient — for an **EN** recipient the message is
English throughout. **If the customer meant the day name is always Thai, that is a different rule and one line.**
**A question, not a defect, and @Porter's to put if it is worth the owner's time.**

**BALL: @Jason — TASK-269.** PENDING DEPLOY 8 (the `sid` split run) is untouched and still @Porter's; TASK-268
still with @Jason and blocks nothing.

## [--:--] Jason (BE) — ✅ TASK-269 DONE (code): `Sessions` is the course as bought · `Remark` · the teacher's copy is the parent's. → @Sober

`smart-scheduler-back` · tsc 0 · **1578 pass / 0 fail** across 123 files · 🚫 no migration (35 = 35), no
database, nothing sent, no new kind and no new payload.

### 🔴 The fixture could not have caught the defect, and that is worth more than the fix
`line-message-fields.test.ts` had **`size: 6` AND `confirmed: 6`.** The two agreed, so the test passed whichever
one `Sessions` read — **it could not tell the defect from the fix.** Changed to `confirmed: 8`, with the reason
on the line: *a field left on the payload that nothing renders is still worth a fixture that would notice if
something started rendering it again.*
📌 **Same shape as the `sid` message itself: two numbers that agree until they do not.** The live case had a
`10 HR` course with two declared leaves, so the fixture's agreement was luck, not coverage.

The decisive test asserts `Program : Surfskate 10 HR` **and** `Sessions : 10` on **ONE** rendering — two
separately-pinned figures can both drift and stay green — plus the two symptoms the reported fix would have
left: a **re-confirm** (`confirmed: 0` ⇒ still `10`) and an **absent `size`** (⇒ no line, never `Sessions : 0`).

### 🔴 EIGHT tests pinned the old audience rule, not four — and your own §3 says why
Your list named four; the suite failed eight. The two you had not listed are **`course-deduction.test.ts`** and
**`line-today-schedule.test.ts`**. ⇒ **The table is per-AUDIENCE, not per-template** — you wrote that sentence
about `*Expiry date` returning to `TODAY'S SCHEDULE`, and it is the same sentence that moved `COURSE DEDUCTION`.
**Three templates changed; the tests found the third.**
✅ All eight rewritten as the **requirement** — `expect(teacher).toBe(parent)` — not as a restatement of the
table. **An assertion that repeats the table proves the table equals itself.**
📌 And one is now a claim that came true: *"reverting one of @Porter's decisions is one line"* → *"really did
cost one line"*, with the date and the owner's word on it. **It has stopped being a promise.**
🔴 The `ไม่มี` reversal is **its own case, TH and EN** — it must not arrive as a side effect of a table going
empty. Mutation-tested: putting `teacher: ["expiry","advanceLeave"]` back fails **12** tests.

### Q1 — no second reader; your grep was right
`payload.confirmed` had exactly one, the `Sessions` line. The only other hits are two test fixtures, neither
asserting on it. ⚠️ **In-flight outbox rows are safe**: `size` has been on that payload since TASK-253, so a row
queued before this deploy renders correctly after it. (TASK-257's `endTime` needed that check because the field
was NEW; this one is a removal of a field the renderer stops reading.)

### Q2 — partial confirm: real, narrow, and the OLD number was not the calendar either
Two sessions failing on budget leaves the parent reading `Sessions : 10` with 8 on the calendar.
📌 **But `Sessions` is a fact about the COURSE, not the calendar** — the size the family bought, true whatever
the confirm did. The old number was *"rows I just flipped"*, which excluded declared leaves **and** failures, so
it never meant *"sessions scheduled"* either.
✅ **And the failure is not silent to staff:** `confirmCourse` returns `skipped` plus a per-session `results`
list with reasons, and the send is gated on `confirmed > 0`. **The admin who caused it is told; the parent is
told the truth about their course.**
⇒ **My read: nothing to fix.** If the owner wants the message to state what is on the calendar, that is a
**different field** (`Scheduled`, beside `Sessions`) and his call — not ours inside a rendering change.
**Reported, not fixed, per your instruction.**

**BALL: @Sober — TASK-269 ready for review. TASK-268 is still with me and blocks nothing; I take it next.**

## [--:--] Sober (SA) — ✅ TASK-269 DONE. 🔴 **The test fixture had the same defect as the message: two numbers that agreed.**
`tsc` **0** · `bun test` **1578 / 0** (123 files) · 35 `.sql` unchanged · nothing applied. **Reviewed and mutated
by both of us.**

🔑 **@Jason's finding, and it is worth more than the fix.** The existing fixture had **`size: 6` AND
`confirmed: 6`** — the two agreed, so the test **passed whichever field `Sessions` read.** ⇒ **the suite could
not tell the defect from the fix, and nothing about it looked wrong.** *A green test over a fixture where both
answers are correct is not a control.* 📌 **Same shape as the `sid` message itself** — two numbers that agree
until they do not. Changed to `confirmed: 8`.
✅ **And the decisive assertion is on ONE rendering** — `Program : Surfskate 10 HR` and `Sessions : 10` together —
because *"two separately-pinned figures can both drift and stay green."*

🔻 **My omission, and the shape of it is the useful part.** I listed **four** tests pinning the old audience rule.
**Eight failed.** The two I missed were **`course_deduction`** and **`todays_schedule`** — ⇒ **I wrote *"the table
is per-AUDIENCE, not per-template"* in §3 and then enumerated the tests per-template.** **The sentence was right
and the list under it was not.** Second time this week a task's own stated principle caught the task's own
omission.

**I broke it twice and watched**, per DEPLOY RULE 6's habit even though nothing here deploys by hand:
- `teacher: ["expiry","advanceLeave"]` restored ⇒ **7 fail across four files** — all three templates and the
  `ไม่มี` case. 🔻 **Jason's report says 12; I measure 7.** The mutation does everything he claims — **only the
  number is off**, corrected in the task. **A number in a report is a claim**, and this week has already cost us
  two (my `grep -c`, his `strikeOrPrompt`).
- `Sessions` → `size - 2` (the old behaviour) ⇒ **4 fail**, including the one-rendering test and the re-confirm.
- Both reverted; clean **1578 / 0**, `git diff --stat` only his two files.

**Q1** — his sweep beat my grep: one reader, two inert fixtures, removed. ✅ **In-flight rows are safe and he
said why:** `size` has been on the payload since TASK-253, so a row queued before the deploy renders correctly
after it — *"TASK-257 needed that check because the field was NEW; this one is not."*

⏸️ **Q2 — I am HOLDING it, and declaring the hold.** `Sessions : 10` with 8 on the calendar after an
`INSUFFICIENT_BUDGET` failure. **Nothing got worse** — the old number excluded leaves *and* failures, so it never
meant *"sessions scheduled"*. The admin gets `skipped` + `results`; the parent is told the truth about their
course. ⇒ **Not a task and not a question for the owner** — it needs a budget failure during a course confirm,
it is a non-regression, and @Porter is already carrying §8.3's `ไม่มี` change to him. **Recorded in `SPEC-072`
§8.6, and @Porter is told that I am holding it** — *a hold nobody knows about is indistinguishable from an
oversight.* ✅ If the owner ever wants the calendar count it is a **second field** (`Scheduled`), not a third
meaning for the first.

📌 **Nothing to deploy by hand** — renderer only, ships with the next `smart-scheduler-back` deploy. **PENDING
DEPLOY 8 untouched.**

**BALL: @Porter — the `sid` split run** (PENDING DEPLOY 8). TASK-268 is with @Jason and blocks nothing.

## [--:--] Porter (PM) — ✅ TASK-269 done: all three message fixes in. Redeploy HELD until @Tanya finishes.
`Sessions` reads the course size (`10 HR` → `10`, both audiences) · **`Remark :`** last and **asserted with a note
PRESENT**, as I insisted · **teacher == parent byte-identical** across `CONFIRMED SCHEDULE`, `TODAY'S SCHEDULE`
**and `COURSE DEDUCTION`**.
🔴 **Renderer-only ⇒ it needs a `smart-scheduler-back` redeploy on `sid`, and @Tanya is mid-round on that box.**
⇒ **The deploy waits for her report.** **Restarting the backend under a tester produces results nobody can
attribute** — and she turned network capture on an hour ago for exactly that reason.
✅ **Upheld @Sober's hold** on the partial-confirm case (`Sessions : 10`, 8 on the calendar). His reasoning holds;
**I added only that the person who could be confused is the PARENT, not staff.** **His `Scheduled`-as-a-second-
field answer is right when it comes** — **a third meaning for one field is how `Sessions` broke in the first
place.**
📌 **His fixture finding is the keeper:** the test that should have caught the `Sessions` bug had **`size: 6` AND
`confirmed: 6`** — **the numbers agreed, so it passed whichever field the message read.** **The suite carried the
same defect as the product.** ⇒ **A fixture whose fields all agree cannot tell you which one you read**, and
nothing about such a test looks wrong from the outside.

## [02:3x] Tanya (QA) — ✅ **TEST-066 round COMPLETE: 18 PASS · 1 FAIL (blocking) · `REQ-083` AC-3 is FIXED.** → @Porter

Report in `inbox/PM.md`; evidence in `tests/TEST-066-req076-082-083-084-sid-release-round.md` (Rounds 1-5 +
DEF-1); footprint declared in `tests/DEV-SERVER-FOOTPRINT.md`.

🎉 **`REQ-083` AC-3 PASSES — the owner's own reported defect.** With AC-1 · AC-2 · AC-4 · AC-6 · AC-8.
✅ **`REQ-082` — all five.** ✅ **`REQ-076` — AC-3 · AC-4 · AC-10 · AC-11 · AC-13 · AC-14 · AC-17.**
🔴 **DEF-1, release-blocking: a paused booking is invisible.** The tray's own query 400s because **`PAUSED` is
missing from the status validator's enum**, and the tray then shows its ordinary empty state. **Pause reads as
delete.** 🟢 **`AC-10` passes** — it does not leak into the grid; **`REQ-078`'s DEF-4 has not recurred in
reverse**, which is the half @Porter asked me to watch.
⚠️ **`NOT_TESTED`, with reasons:** AC-5/AC-7 need a booking that actually posted — the sweep had passed, and
**course money posts once at enrolment, not per session, so AC-5 may be untestable as written** · **AC-9 has no
history surface at all** (four endpoints 404 on a booking; courses have two) · **the FE width checks** — the
viewport could not be changed, and DEF-1 makes the 1280 question unanswerable until the fix lands.
🟢 **No LINE sent** (used `Ek`, deliberately avoided the linked `Haris`) · **no `uat`** · **backoffice
read-only.** ⚠️ **Someone else was writing to `sid` throughout; every verdict rests on a same-window
before/after with an unmoved control card.**
🟢 **The `ดิววี่` scare is resolved** by three independent lines — click geometry, a clean network capture, and
an empty ledger. **The DATA REQUEST for the audit timestamp stays open anyway; I would rather be confirmed than
believed.**
📌 **Two near-misses worth keeping:** I almost filed *"the modal cannot be closed"* — **the bug was a coordinate
scale factor of mine (viewport 1920 vs click frame 1568)**; and I twice picked a fixture by **subject name**
when only **`course.id`** disambiguates. **Both were caught by re-observing rather than by trusting an
instrument.**

## [--:--] Porter (PM) — 🔴🔴 @Tanya's round: DEF-1 BLOCKS THE RELEASE. And a ledger finding bigger than the ฿20.
### 🔴🔴 DEF-1 — one missing enum value, and it makes pause read as DELETE
`POST …/pause` → **200, `PAUSED`, row in the DB** 🟢 — **but `GET /api/bookings?status=PAUSED` returns 400
`ZodError`: `PAUSED` is missing from the status query validator's enum.** ⇒ **the tray renders
`ไม่มีรายการที่พักไว้` while a paused booking exists.**
⚠️ **The empty state is a convincing lie — it is the SAME wording that correctly passes `AC-11`**, so an admin
cannot tell *"nothing is paused"* from *"the tray is broken."* 🔴 **No calendar, no tray, no list filter: only an
API call reaches a paused booking.** **`uat` does not go until this is fixed.**
🟢 **`AC-10` PASSES** — absent from a 167 KB calendar payload ⇒ **REQ-078's DEF-4 has not recurred in reverse.**
✅ **`REQ-082`: all five PASS**, including `AC-2` being **readable** (`expiry-history` → who · when · from · to) —
**the one she pre-flagged as `NOT_TESTED` if it were not.**

### 🔴 No per-booking revenue has EVER been written on `sid` — 75 movements, all package `SALE`s
`refType`: **`SALE` 72 · `null` 3, nothing else.** ⇒ **`AC-5` has no precondition and is not testable; `AC-7`
falls with it.** 🟢 **`AC-6` gets stronger** — holds across all 75, not one fixture.
✅ **My ruling on her question: NEW INFORMATION, not a re-run.** **The owner parked a TEST, not the right to learn
something** — and this reshapes the ฿20 from *"the `อื่นๆ` path misses a posting"* to **"the per-booking posting
path produces nothing on this box."**
🔴 **AND IT WAS ALREADY ON OUR OWN BOARD.** *"Course & voucher revenue was never recorded, in all history"* has
been sitting in Blocked/waiting for weeks. **She found from the ledger what the board already said and nobody
had connected.** ⇒ **carried to @Sober as one question, not two.**
🟢 **Her experiment settles it free:** `cbc26a39-…` `1 HR` · `Ek` · `CONFIRMED` · today 15:00–16:00. **Tonight's
sweep answers whether ANY attendance writes a movement.** Confirm returned `notification: skipped` — **nobody
messaged, and she checked before relying on it.**
🧹 Board was over cap ⇒ **16 resolved rows swept** from Blocked/waiting, verbatim, to `archive/board-closed.md`.

## [--:--] Sober (SA) — 🔴🔴 DEF-1 cut as TASK-270 (mine to have caused). 🔴 **And the ledger "finding" is not a defect — the answer was in `SYSTEM-FACTS.md`, in @Porter's own words, since 08-23.**

### DEF-1 — ⛔ `uat` holds. TASK-270 → @Jason.
🔻 **My error, written first in the task.** TASK-260 §8, my words: *"the tray's list · `GET
/bookings?status=PAUSED` · **existing endpoint, existing filter (`scheduler.service.ts:756`, verified)**"*.
**The filter was verified. The request never reaches it** — `zValidator` rejects it one layer in front of the
line I checked. ⇒ **I verified the half that could not fail and called the whole thing verified.**
📌 **The same sentence as TASK-267's `--plan`: a check that stops one step short of the risky part is a
comfort.** **Third instance this week, and the first I caused after writing the rule down.** ⚠️ @Jason concurred
(*"no objection to any of the three"*) — **not a second fault; that is what a claim labelled "verified" does to
the next reader.**

🔴 **The defect is FOUR copies of the booking-status list, not a missing value.** DB enum **9** ·
`validation.ts` **7** · `types/contract.ts` **7** · `openapi/document.ts` **7**. **`PAUSED` reached one.**
🔴 **And `NO_SHOW` has the identical gap, never found by anyone** — no writer since TASK-180, **but historical
rows render and cannot be listed**. ⇒ **the fix is ONE source (the DB enum) + a test that enumerates it.**
✅ **Checked before specifying it: `BOOKING_STATUS` has exactly one use and it is a READ filter** — the write
path takes verbs — **so widening it opens no write hole.** 🚫 **Not to be merged with `SLOT_INACTIVE_STATUSES` /
`CALENDAR_HIDDEN_STATUSES`:** those answer *questions* and may differ; **these four are the same list, four
times.**

### 🔴 The ledger finding — RULED: not a defect, and our own file already said so
`SYSTEM-FACTS.md` **line 329, @Porter, 2026-08-23**: *"Course, voucher and rental revenue post **AT SALE**;
`FIRST_TRIAL`/`SINGLE_SESSION` **only at day-end once `ATTENDED`**."*
**Confirmed in the code:** the day-end select is `inArray(bookingType, ["FIRST_TRIAL","SINGLE_SESSION","OTHER"])`
— **`COURSE_PACKAGE` and `VOUCHER` are excluded deliberately**, because re-posting would double-count money the
family already paid. ⇒ **on a box whose bookings are all course sessions, ZERO `rev:` movements is the design.**
📌 **We escalated as a possible defect a behaviour our own facts file describes.** **Not @Tanya's error** — she
classified and flagged rather than diagnosed, which is exactly right. **The file answers it and was not read.**

**🔴 And a correction that matters more than the ruling:** *"`refType` SALE 72 · null 3 ⇒ not one movement is
tied to a booking"* **does not follow.** `recordSale` / `postBookingSale` write **`refType: "SALE"` with `refId`
= the BOOKING id** and `idempotency_key = rev:<bookingId>`. ⇒ **a `rev:` movement IS a `SALE`.** The
discriminators are the **key** and **`ref_id`**, never `refType`. **Same shape as my `grep -c` and his
`strikeOrPrompt`: a real number read against the wrong field.**

**The question that IS open** — and it is not about posting code: **has `end-of-day` ever run on `sid`?**
`runEndOfDayJob` has **one caller**, the internal route, **invoked by a per-box Windows Task**. There is **no
scheduler in the app.** ⇒ an unregistered task — or `INTERNAL_JOB_SECRET` unset (**@Porter's own fact, line 61:
503 while Task Scheduler reports SUCCESS**) — explains everything with no code defect. **Evidence: `job_runs`
rows for `job='end-of-day'`**, with his caveat that `db:reset` wipes them.

⚠️ **Her overnight experiment can produce a FALSE zero, and @Porter has been asked to tell her tonight.** A
`SINGLE_SESSION` needs **a price group for its program** (bike/skate have no 1-hour rate ⇒ posts nothing, loudly)
**and a seeded `bo.item`** (**`sale:ensure-items` is PENDING DEPLOY 6, owed on every box, never run**). ⇒ **three
independent causes of "no movement", all of which print `[sale] NOT POSTED` with the reason.** **Read the LOG,
not the absence of a row.**

**REQ-083 AC-5:** `NOT_TESTED` is right, *"not testable"* is too strong — **course sessions can never satisfy it
by design; her KKTEST `1 HR` is exactly the fixture that reaches it.** ✅ AC-6 across all 75 kept.

### Also
✅ `SPEC-072` §8.6 takes @Porter's line — **the parent, not staff, is who could be confused.**
✅ **A standing ⚠️ in `SYSTEM-FACTS` resolved from the code:** the day-end does **not** skip `OTHER` when
**selecting**; only `job_runs.byBookingType`'s **report** omits it. **Reporting and selecting were different
questions and the answers differ.**
📌 Four new facts written to `SYSTEM-FACTS.md` (the four status lists · `refType` cannot discriminate · zero
`rev:` on a course-only box is correct · the three causes of a silent non-posting).

**BALL: @Jason — TASK-270.** ⏸️ Deploy of 269 + 270 waits for @Tanya's round — @Porter's call.

## [--:--] Jason (BE) — ✅ TASK-270 DONE (code): one status list, derived from the database — and a FIFTH copy found on a teacher's phone. → @Sober

`smart-scheduler-back` · tsc 0 · **1588 pass / 0 fail** across 124 files · 🚫 no migration (35 = 35), no
database, pause/resume guards and `scheduler.service.ts:756` untouched, `updateStatus`'s verbs untouched.

### ✅ The three derive; the test enumerates the DB enum
`z.enum(bookingStatus.enumValues)` · `(typeof bookingStatus.enumValues)[number]` · `[...bookingStatus.enumValues]`.
The guard **reads the enum**, so it grows by itself — a hand-written list in the test would have been a fifth
copy that passes for every value someone remembered to type. ✅ And deriving did not become *"accept anything"*:
`NOT_A_STATUS`, lowercase `paused` and `""` are still clean 400s. **Broke it and watched:** removing `PAUSED`
from the derivation fails 3 tests.

### ⚠️ `tsc` did not light up — and the reason is not that it is safe
You predicted exhaustive switches at `course-history.ts:54,60`. **They are not exhaustive:** `bookingEventKind`
takes `{ status: string }`, not `BookingStatus`, so the compiler had nothing to say. Its `default:` returns
`"scheduled"` — **a paused session would read as `scheduled` in a course's history.**
✅ **It cannot happen, and not because of the type:** `pauseBooking` refuses any booking with a `courseId` (AC-3),
and course-history only sees course bookings. ⇒ **The safety comes from a guard three files away.**
📌 Worth recording because **a silent `tsc` reads as "nothing to decide", and what it meant was "nothing was
asking."**

### 🔴 A FIFTH enumeration, and it is live on a teacher's phone
Not one of the four, not touched — **reported under your §3 rule.**
`line-i18n.ts` has **six** `status_*` labels (no `PAUSED`, no `PENDING_RESCHEDULE`, no `CANCELLED`);
`line-schedule.ts` renders `t(\`status_${r.status}\`)`, and `t()` **returns the key** for an unknown one; and
`findBookingsForTeacher` filters `ne(status, "CANCELLED")` — **a hand-written single-status exclusion, the exact
shape TASK-260 named as *"how the next status gets missed"***.
⇒ **A paused booking appears in a teacher's `ตาราง` and renders as the literal string `status_PAUSED`.** Two
defects at once: it should not be on that list at all, and the label is a raw key.
🔴 **`PENDING_RESCHEDULE` has had the identical bug for far longer and nobody found it** — unfiltered, unlabelled.
**Never reported because a B.1 row is rare and a coach reading `status_PENDING_RESCHEDULE` would assume it was
our jargon.**
🚫 **Not fixed: both halves are product decisions** — hide a paused session from the teacher's schedule or show
it as `พัก`, and what `PENDING_RESCHEDULE` should read as to a coach. *"I would rather scope a second task than
have you guess at what a screen should do with a paused row"* — so I did not.

### Q1 — safe, and better than either shape you offered
`import type` + `typeof` means **`types/contract.ts` gains no runtime import at all** — the derivation is erased
at compile time, so nothing that pulls the contract can pull a database. Asserted, plus a check that the file has
no value `import {`. 📌 And even the value imports are harmless: **`db/schema.ts` imports only
`drizzle-orm/pg-core`**; the connection is in `db/index.ts`. ⇒ **the tiny-tuple module is unnecessary.**

### 🔴 Q2 — a fifth copy across the wire, and it is the BACKEND that was wrong
`smart-scheduler-front/.../contract.ts:13` is a hand-written union of **all nine**, `PAUSED` and `NO_SHOW`
included — @Fern added `PAUSED` for REQ-076 with a comment.
⇒ **The frontend's contract was correct while our own published one said `PAUSED` was not a status and our
validator rejected it.** For the whole of REQ-076 the FE knew about a status our OpenAPI denied.
**It agrees today and has no mechanism to keep agreeing** — this task's sentence, with a network hop in it. Not
touched; yours to cut for @Fern. 📌 `BookingStatusAction` matches ours: **the write path has never had this
problem on either side.**

**BALL: @Sober — TASK-270 ready for review; ⛔ `uat`'s blocker is cleared from my side. TASK-268 still mine.**

## [--:--] Sober (SA) — ✅ TASK-270 DONE. 🔴 **And `CALENDAR_HIDDEN_STATUSES`'s own comment describes the bug we just found in another file.** TASK-271 cut; the two ship together.
`tsc` **0** · `bun test` **1588 / 0** (124 files) · nothing applied. ⛔ **`uat` still holds** — 270 alone would
ship a pause that works in the tray and renders as `status_PAUSED` on a coach's phone.

**Mutation:** I removed `"PAUSED"` from the `pgEnum` ⇒ **2 fail**. @Jason reports **3** because he mutated *the
derivation*, which also trips *"no literal status list remains"*. ⇒ **Two mutations, two failure sets, both
caught — the defect AND the shortcut that reintroduces it.** 📌 **Checked before calling it a discrepancy**,
which is the thing I got wrong on TASK-269.

**🔑 His finding, and I would have missed it:** I predicted `tsc` would light up on `course-history.ts`. **It did
not** — `bookingEventKind` takes `{ status: string }`, so it was **never exhaustive.** *"A silent `tsc` reads as
'nothing to decide'; what it meant was 'nothing was asking'."* ✅ **And he did not stop at the type:** a `PAUSED`
row would fall into that `default:` and render as `scheduled` — **prevented by `pauseBooking` refusing a
`courseId`, three files away.** **Naming where the safety actually lives beats concluding "it compiles".**

### 🔴 TASK-271 — the fifth enumeration is a LIVE LINE defect
Six `status_*` labels; `t()` returns the key on a miss; `checkin.service.ts:114` filters one status by hand.
⇒ **a paused session appears in a teacher's `ตาราง` and renders as the literal `status_PAUSED`.**
🔴 **And `PENDING_RESCHEDULE` has had the label half far longer** — never reported because a B.1 move is rare and
a coach would read it as our jargon.

🔻 **The finding worth keeping:** `CALENDAR_HIDDEN_STATUSES` exists **because of this bug**, and its own comment
says *"a hand-written `ne(status, 'CANCELLED')` … is exactly how the next status gets missed."* **TASK-260 wrote
that sentence, fixed the instance it found, and an identical line survived in another file.** ⇒ **Naming a
pattern and fixing one instance of it is not fixing the pattern.**
✅ **The mechanism is the COMPILER, not a test** — labels become `Record<BookingStatus, …>`, so the next status
added to the database fails the build. **That only became possible because TASK-270 landed an hour earlier and
made that type derive from the enum.** One fix made the next one cheap.
✅ **I swept the other five hand-written `CANCELLED` exclusions before writing the task — they are NOT five
copies.** Each answers a different question and **four already differ correctly**; `:1206` (a voucher's bookings)
should keep counting a paused session, because *no money, no entitlement* means the hour stays held. **One site
changes; two get a read and no edit.**
⚠️ **`PENDING_RESCHEDULE`'s label is a PLACEHOLDER** (`รอย้ายคาบ`), labelled as one in the code, @Porter asking
the customer. **The `Remark` shape again — caught before it shipped this time.**

### The FE fifth copy — declared, deliberately NOT cut
`smart-scheduler-front`'s list has **all nine**, `PAUSED` included (@Fern, REQ-076). ⇒ **the client's contract
was right while our own published OpenAPI said the status did not exist.**
🚫 **No FE task.** There is no defect — the list is correct — and the only real mechanisms are **(a)** codegen
from our OpenAPI (finally a truthful source as of today) or **(b)** a cross-repo contract test. **Both are
decisions about how the repos relate, not bug fixes**, and a task saying *"make them agree"* with no mechanism is
a note pretending to be a control. **Recorded; offered to @Porter as an option, not urgent.**
📌 And @Jason's note that `BookingStatusAction` matches on both sides is worth as much as the finding — **the
write path has never had this problem, and knowing where a defect ISN'T bounds it.**

**BALL: @Jason — TASK-271.** TASK-268 still his and still blocks nothing.

## [--:--] Jason (BE) — ✅ TASK-271 DONE (code): the compiler now asks the question, and it fails with the DATABASE's list. → @Sober

`smart-scheduler-back` · tsc 0 · **1597 pass / 0 fail** across 125 files · 🚫 no migration (35 = 35), no
database, no FE change, `t()`'s fall-through untouched, the five §4 sites other than `checkin.service.ts`
untouched.

### 🔑 The control, shown failing
Deleting the `PAUSED` entry from `STATUS_LABELS` gives:
> `TS2741: Property 'PAUSED' is missing … but required in type 'Record<"ATTENDED" | "CANCELLED" | "CONFIRMED" |
> "EXTENDED" | "NO_SHOW" | "PAUSED" | "PENDING" | "PENDING_RESCHEDULE" | "SICK_LEAVE", Entry>'`

🔴 **Read the union in that message: it is the DATABASE enum.** TASK-270's derivation showing through — the
compiler is asking about the nine values Postgres accepts, not the seven someone once typed. **Your sentence
*"one fix made the next one cheap"*, proved by an error message.**
The tests assert the CONSEQUENCE — no status renders as a raw key, every enum value, TH and EN — and enumerate
`bookingStatus.enumValues` to do it, so they cannot become a second copy.

### ✅ The filter reuses the list whose comment predicted this
`notInArray(b.status, [...CALENDAR_HIDDEN_STATUSES])`. Asserted that **`CANCELLED`'s behaviour is unchanged** —
a fix that quietly started showing cancelled classes to coaches would be worse than the bug. And
`PENDING_RESCHEDULE` **stays visible**, rendered, with the placeholder label and its *"@Porter is asking"*
comment asserted in the source — because a label nobody marked as provisional becomes ratified by silence.

### §4 — my read on your two, neither edited
🔴 **`getDailyReport.totalBooked` — leave it, and the reason is the opposite of the obvious one.** It is the
report's only NEGATIVE counter and there is **no `paused` bucket**, so excluding `PAUSED` would make a paused
session **vanish from the report entirely**, indistinguishable from a booking that never existed. **A number
that silently drops when an admin pauses something is worse than one that counts a held session.** ⇒ if this is
wrong the fix is to ADD a bucket, which is a report-design decision, not a line I should move.
⚠️ `badge.service.ts:163` — **genuinely ambiguous and I will not guess.** Including keeps the count stable, but
counts the session on a date it will not be taught; excluding makes a dashboard number drop on a pause.
🔑 **Both are the SAME question — *"does a paused booking count in a report?"* — and they deserve ONE answer,
not two of my guesses.** Neither is money-adjacent and neither is urgent. **@Porter's, together.**
✅ **I confirm your other three reads**, including that `:1206` must keep counting a paused session: *no money,
no entitlement* means the voucher hour stays held, and excluding it would **silently return entitlement**.

### 🔴 The Question — four dynamic `t()` sites; one other has the same latent shape
`ob_dow_${weekday}` ✅ (7/7, bounded by a `smallint` 0–6) · `code_${role}` ✅ (3/3, closed by TASK-251) ·
`status_${…}` 🔴 (6/9 — this task) · **`att_${c.key}` ⚠️ 10/10 today, and nothing keeps it so.**
⇒ **The eleventh attention card ships with `att_my_new_card` as its heading on the dashboard.** 🚫 Not fixed.
📌 It is the same one-line mechanism (`Record<AttentionKey, …>`) — **and unlike the status one it needs no
derivation to exist first**, because the card keys are already one array in one file.
📌 **And one the sweep cannot catch, different mechanism, same family:** `ics.ts:95` writes
`Status: ${b.status}` **raw** into the calendar description a teacher subscribes to — a paused session already
reads `Status: PAUSED` on a phone calendar today. **Arguably fine in a technical ICS field; named so it is a
decision somebody made rather than one nobody saw.**

**BALL: @Sober — TASK-271 ready for review; it ships with 270. TASK-268 still mine and still blocks nothing.**

## [--:--] Sober (SA) — ✅ TASK-271 DONE. 🔴 **Three instances of ONE class in one day, and the third is already on coaches' phones.** TASK-272 + TASK-273 cut.
`tsc` **0** · `bun test` **1597 / 0** (125 files) · nothing applied. **I ran the `tsc` mutation myself** — delete
the `PAUSED` label entry and the compiler fails, **with the nine-value DATABASE enum in the error message.**
📌 **That error message states TASK-270's point better than my sentence did.**

**The class, named properly at last: a raw enum reaching a person.** Not *"a `t()` key that might be missing"* —
that net was mine and it was the wrong shape, which @Jason proved by finding the exception himself.
| where | state | found by |
|---|---|---|
| LINE `status_*` label | 🔴 broken | @Tanya's pause, then reading |
| `.ics` `STATUS:` | 🔴 broken, **already published** | @Jason reading past his own finding |
| attention-card `att_*` | ✅ correct, **nothing keeping it so** | @Jason's sweep |
⇒ **Two of the three were found only because somebody happened to look.**

### 🔴 TASK-272 — the worst of the week, and it is DEF-1's mirror image
He flagged `ics.ts:95` (raw `Status:` in the DESCRIPTION). **The line beneath it is the real one:**
`veventStatus` returns **`CONFIRMED`** for anything but `CANCELLED`/`PENDING` ⇒ **a paused session publishes to a
coach's subscribed phone calendar as a CONFIRMED class.**
🔴 **`calendar.service.ts`'s own comment is the argument:** the feed filters **no** status **on purpose**, because
**`STATUS:CANCELLED` is its ONLY channel for taking a class off a phone.** **Pause was never taught it.**
⇒ **DEF-1 made pause look like a DELETE; this makes it look like NOTHING HAPPENED** — and unlike the tray, **the
wrong answer is already in a coach's pocket and stays until something republishes.**
✅ `PAUSED → CANCELLED`, **with the resume round trip asserted** (stable `icsUid`, `SEQUENCE` from `updatedAt`) —
*a removal that cannot be undone is worse than the defect.* `veventStatus` made total; **every other status keeps
today's value, written out so it is visible rather than defaulted.**
📌 **It takes `status: string`** — the identical reason `bookingEventKind` was never asked, one file over. **Same
sentence, second file, same day.**

### 🔑 His `totalBooked` reasoning beat my question
I asked *"is a paused booking 'booked'?"*. He answered the question that decides it: **`totalBooked` is the
report's only NEGATIVE counter and there is no `paused` bucket**, so excluding a paused session would make it
**vanish entirely — indistinguishable from a booking that never existed.** *"A number that silently drops when an
admin pauses something is a number nobody can reconcile."* **Adopted.**
✅ **And the shape of his fix is right: ADD a bucket, do not change the exclusion** — that makes the row visible
instead of choosing which lie to tell. ⇒ **`totalBooked` and the badge count are the SAME question and go to
@Porter as ONE**, not two guesses. **Neither is urgent or money-adjacent; asked to raise it only when something
else takes him to the owner.**

### TASK-273 — `att_${c.key}`, cut without asking, blocks nothing
Ten keys, ten labels, **complete today and nothing holding it** ⇒ the eleventh card ships `att_my_new_card` as a
dashboard heading. `Record<AttentionKey, …>` with the key type **derived from the array**.
📌 **Worth the line precisely BECAUSE it is not broken** — the only one of the three where the control is free
and there is no defect to argue about first. **@Porter told, not asked: there is no product decision in it.**

**BALL: @Jason — TASK-272**, then 273, then 268. ⛔ **270 + 271 + 272 are one shipment; `uat` holds.**

## [--:--] Jason (BE) — ✅ TASK-272 DONE (code): the paused class comes OFF the phone, and it comes back. → @Sober

`smart-scheduler-back` · tsc 0 · **1609 pass / 0 fail** across 126 files · 🚫 no migration (35 = 35), no
database, no FE change; the feed's window, token rule, `icsUid`, `sequenceOf` and its **include-every-status**
decision all untouched — that decision is what makes the fix possible.

### ✅ `PAUSED → CANCELLED`, and the round trip is the half that matters
Same `UID`, strictly rising `SEQUENCE`, `CONFIRMED → CANCELLED → CONFIRMED` — asserted as one sequence, not as a
pause test and a resume test. **A one-way removal would have been worse than the defect**: the coach loses a
class that came back and never sees it again.
✅ **The eight other statuses are asserted byte-identical to before**, against the old chain of ifs reproduced as
the expectation — because writing a map out in full is exactly when something changes by accident, and *"no-op"*
is a claim.

### 🔑 The `Record` alone would NOT have been enough
`veventStatus` took `status: string` — **the identical observation as `bookingEventKind`** — and with a `string`
parameter the map still needs a fallback, **and the fallback is the defect.** ⇒ I typed the INPUT as well as the
map. That is what turns a silent default into a compiler question.
📌 And typing `IcsBooking.status` broke exactly one thing: the existing fixture helper, which could construct a
booking whose status no longer exists. **The type doing its job on its first day** — fixed by typing the helper,
not by casting.

### §5 was small, so it is done
`findBookingsForCalendarToken` already returns the teacher ⇒ `lineLang === "EN" ? "EN" : "TH"` at the route, the
same `null → TH` every other reply uses. `DESCRIPTION:Status: พัก` / `Status: Paused`, asserted in both
languages **and** asserted that **no** status publishes as its raw enum name in either.

### 🔴 The Question — `t()` was the wrong net, and so is any grep
**The right question is: which modules COMPOSE text a human reads?** There are exactly two in this backend —
**the LINE layer and the ICS feed** — and both are now fixed. Everywhere else the backend **emits data and the
frontend labels it**, which is correct and is a different thing.
Swept every `${…status…}` interpolation across `src` and `scripts`: the remaining hits are **`res.status`** (an
HTTP code in an error or a log) and two SQL index predicates. **Nothing else reaches a person.**
⚠️ The one raw enum still reaching a screen is **the API payload itself** — every booking DTO carries
`status: "PAUSED"` — **and that is correct**: it is the contract, and the FE labels it. Noted so the sweep reads
as complete rather than as having stopped at the wire.
📌 **The generalisation worth keeping:** *a raw enum reaching a person* is bounded by **where text is composed**,
not by which helper composes it. **That is a checkable statement; "grep for `t()`" was not** — and it is why the
ICS feed sat outside the first sweep while being the worse of the two.

**BALL: @Sober — TASK-272 ready for review. 270 + 271 + 272 are the shipment. Next: TASK-273, then 268.**

## [--:--] Sober (SA) — ✅ TASK-272 DONE. **The pause shipment is complete.** 🔑 **And the class is finally named properly: it is not a missing value, it is a `string` where a status type belonged.**
`tsc` **0** · `bun test` **1609 / 0** (126 files) · nothing applied. **I deleted the `PAUSED` entry from
`VEVENT_STATUS` myself ⇒ `tsc` fails**, with the database's nine-value union in the error. **Second day running
that an error message is the deliverable.**

⛔ **270 + 271 + 272 ship together and are ready.** A paused session now appears in the tray, is absent from the
teacher's `ตาราง` with real labels, and is **removed from the coach's subscribed phone calendar — and returns on
resume** (same UID, rising `SEQUENCE`, asserted as a round trip, because a one-way removal would be worse than
the defect).

### 🔑 @Jason typed the INPUT as well as the map — the half I under-specified
> *"With a `string` parameter the map would still have needed a fallback, and the fallback is the defect."*

**He is right and I asked for half the control.** 📌 **Third time in two days that a `string` parameter was what
hid the question** — `bookingEventKind`, `veventStatus`, and `t()`'s key. ⇒ **The general form is the PAIR:
a total map AND a typed input.** Written down here because I got it wrong twice by writing only the first half.
🔑 **And typing it broke a fixture that could construct a booking whose status no longer existed** — fixed by
typing the helper's return, **not by casting.** *A fixture that can build an impossible object is how a suite
goes green over a state the product cannot reach.*

### 🔑 He replaced my Question with a checkable one
I asked *"does anything else publish a status without a label?"* and offered a grep-shaped net.
> *"`t()` was the wrong net, and so is any grep for `t()`. The right question is: which modules COMPOSE text a
> human reads? There are exactly two in this backend."*

⇒ **A bounded claim about the architecture instead of an unbounded one about strings.** ✅ **And naming the API
payload's raw `status: "PAUSED"` as CORRECT** — it is the contract, and the FE labels it — **is what makes a
sweep read as complete rather than as having stopped at the wire.** **Adopted as the standing form of this
question.**

### 🔴 I took the sweep across the wire — a fourth instance, still correct
- ✅ **`BOOKING_STATUS_COLOR` on the FE is already `Record<BookingStatus, …>`** — @Fern's control, already there.
- 🔴 **`dictionaries.ts` is not.** Nine labels in **both** languages today, and
  `export const dictionaries = { en, th }` carries **no type annotation** ⇒ nothing forces coverage of
  `BookingStatus`, **and nothing forces `th` to match `en`.**
⇒ **The tenth status ships unlabelled on the admin's daily screen, in two languages, green build.**
**TASK-274 → @Fern**, one line, blocks nothing. 📌 **His claim that "the FE labels it" was TRUE — I checked —
and what I found is that it is true BY CARE rather than BY CONSTRUCTION.** **That distinction is the whole of
today, so it was worth the ten minutes.**

### The day, counted
**Four instances of one class:** the tray's status filter (broken, @Tanya) · the LINE schedule label (broken) ·
the phone calendar (**broken and already published**) · the attention cards + the FE label map (**correct, and
nothing keeping them so**). **Two were found only because somebody happened to look.**
📌 **TASK-273** (@Jason, `att_` keys) and **TASK-274** (@Fern) both cut without asking — **no product decision in
either**, and @Porter is told rather than consulted.

**BALL: @Porter — the deploy window after @Tanya's round.** TASK-273 · TASK-274 · TASK-268 all with the
engineers and none of them blocking.

## [--:--] Porter (PM) — ✅ DEF-1 fixed — and @Sober found THREE more of the same class, one already in production.
Tray filter (@Tanya's) · **the LINE schedule label** · **the coach's subscribed phone calendar — broken AND
ALREADY PUBLISHED** · plus two places that were correct **with nothing keeping them so**.
🔑 **His diagnosis, and it is the finding of the week:** the class is **not** *"somebody forgot a value"*. It is
**"a place that decides something about every status, where nothing asks the question"** — and in three of four
the thing hiding it was a **`string` where a status type belonged.** ⇒ **the fix each time was the COMPILER, not
a test.** 📌 **A test can only fail on a case someone imagined; a type fails on the case nobody did.**
📌 **@Fern's contract was RIGHT while our own published OpenAPI was WRONG** — she had `PAUSED` for REQ-076 while
the backend still said the status did not exist. **Worth the owner knowing: the FE was not the one out of step.**
⇒ **`REQ-076` now: paused appears in the tray · is absent from the teacher's `ตาราง` · is removed from the coach's
phone calendar and returns on resume.**
⏳ **Deploy window is mine to place.** ⚠️ **@Tanya's `cbc26a39` experiment sweeps at 18:30 tonight** — **a restart
near that hour risks the one question the whole money thread now hangs on.** ⇒ **deploy well before, or after.**

## [--:--] Porter (PM) — 🔴 LANGUAGE SPLITS IN TWO. Owner: finish LINE tonight. → @Sober
**Conversation → BILINGUAL** (registration, commands, prompts, errors — the customer's own 8-screen copy is the
source text; **use their English, not a translation of ours**). **Notifications → English labels, unchanged.**
🔻 **This reverses yesterday's single ruling — narrowly, and his own reasoning survives where it was strongest:**
*"สองภาษาจะยาวเกินไป"* still holds for a coach's eight-class `TODAY'S SCHEDULE`. **It does not hold for a one-line
prompt.** ⇒ **`REQ-077` needs nothing from this.**
✅ **Decided the gap their copy cannot fill: generated values in a notification follow the LABELS — English.**
`Date : Monday`, never `อาทิตย์`. **His two live samples disagreed on that exact field because it followed
`line_lang`** — one rule replaces the disagreement. **Names stay as they are.**
🔴 **Raised, not decided: `line_lang` and the `ภาษา` menu cell may now switch NOTHING.** If the conversation is
bilingual and notifications are English, **what is left for it to change?** **A switch that switches nothing is a
control that lies — and it owns a rich-menu cell.**
⏱️ **He wants LINE closed tonight, so I asked @Sober to scope it honestly** — **which subset lands and which does
not.** **Better to tell him "the registration flow is bilingual, the rest follows" than to let him find it
half-done.**
🧹 `REQ-079` was over cap ⇒ **§13–14 consolidated to their surviving RULES** (48.5→39.1KB), verbatim archive at
`archive/REQ-079-2026-09-07-pre-consolidation.md`. **Both are shipped and phone-proven; the write-ups were
history, the rules are not.**

## [--:--] Sober (SA) — 🔴 §18: the conversation goes BILINGUAL. `SPEC-077` + `TASK-275` → @Jason, tonight. **And @Porter's `ภาษา` question has a concrete answer: a 20-character limit.**

### 🔑 The `ภาษา` switch — it survives exactly where bilingual does not fit
**LINE caps a quick-reply / postback label at 20 characters**, and our button labels go through the same
`t(key, lang)` as message bodies (`line-webhook.service.ts:684 · 910 · 914 · 921`). ⇒ **`ตารางวันนี้ / Today`
does not fit.** **Rich-menu cells cannot change at all — the words are in the artwork.**
⇒ **After tonight `line_lang` governs exactly two things: the BUTTON LABELS and the MENU IMAGE.**
🔑 **It is NOT a control that lies — but its MEANING has narrowed** from *"the language the bot speaks to me"* to
*"the language of my buttons"*, **and a user cannot know that from the cell.** ⇒ put to the customer as **ก)** keep
it, meaning buttons + menu, or **ข)** Thai-only buttons and **retire it** — freeing a menu slot and deleting
`line_lang`, the `parent-th`/`parent-en` pair and three images. 🚫 Nothing ripped out tonight; ⚠️ **and not left
standing with an unstated new meaning either.**

### 🔴 @Porter's notification-values rule is right in intent and CANNOT be one line — HELD
*"Generated values follow the labels: English"* meets three things:
1. **The customer's OWN template writes `ไม่มี`** (`REQ-077`: `{วันลาที่แจ้งไว้ / ไม่มี}`) ⇒ **their copy
   contradicts the rule on the one generated value the message contains.**
2. **`Remaining : 4/6 ครั้ง` is hard-coded Thai OUTSIDE `t()`** (`course-deduction.ts:28`) ⇒ **flipping the
   parameter never reaches it. The rule would be BELIEVED implemented while one value stayed Thai** — *a note,
   not a mechanism*, fourth time this week.
3. **`booking_confirmed` is byte-frozen and owner-verified — and it is a notification.** Forcing English rewrites
   it. **Carved out, or no longer frozen: somebody must say which.**
⇒ **Not shipped.** *One line of code, three unanswered questions, six live messages, and he reviewed one of them
yesterday.* 📌 **So "REQ-077 needs no work" is narrowly wrong: the labels need none, the generated values need a
decision.** **Recommendation given: keep `ไม่มี` — it is their own word in their own draft — and read the rule as
covering values WE derive (the day name).** ⚠️ **Same hold covers the ICS description**, which followed `lineLang`
as of this afternoon — **TASK-272 §5 superseded within the day by §18.**

### The build — cheaper than it looks, and the signature carries the rule
✅ **All 110 conversational keys already have both languages** (157 total, 47 `ob_*`). **A rendering change, not
a translation project.** 🔑 **`tb(key)` takes NO `lang`** — *a body helper that cannot receive a language cannot
silently render one; a label helper that still receives one cannot silently become 40 characters.* **@Jason's
TASK-272 lesson applied BEFORE the defect — except here the control is the parameter's ABSENCE.**
⚠️ **The one thing that could make it not-tonight:** a body composed from several keys would interleave
TH/EN/TH/EN. **He is to look for that first and report.** ⇒ **if short, cut WHOLE FLOWS, registration first** —
a half-bilingual flow reads as a bug; a Thai-only flow beside a bilingual one reads as staging.

**BALL: @Jason — TASK-275, tonight.** 273 · 274 · 268 all wait. @Porter owes the customer's ก/ข and the three
answers above; the pause shipment (270+271+272) waits on @Tanya then his deploy window.

## [--:--] Jason (BE) — ✅ TASK-275: the REGISTRATION flow is bilingual. The rest is named, not half-done. → @Sober

`smart-scheduler-back` · tsc 0 · **1621 pass / 0 fail** across 127 files · 🚫 no migration, no database, no FE
change; **the six notifications and the ICS feed untouched**, asserted.

### 🔻 I checked the reversal before building it
The task builds the thing the owner **refused on 09-06** (*"มันจะยาวเกินไป"*). `REQ-079` **§18 exists**, dated
09-07, and reverses it **narrowly** — conversation only, notifications keep the length objection. ⇒ correctly
grounded, and I verified rather than assumed, because *"build the thing the owner rejected yesterday"* is exactly
the instruction that should be read twice.

### 🔴 §3's `tb(key)` is wrong for most of this bot, and Q1 is why
I built it, then found the reason it cannot be the primitive: **four keys are suffix FRAGMENTS that BEGIN with a
newline** — `verify_parent_children_count`, `verify_parent_found`, `leave_extline`, `leave_lockline` — and each
is **appended to another body**. Rendering one as `TH\nEN` inserts an English fragment into the middle of a Thai
message. **No separator choice rescues it: the fragment is not a message.**
✅ ⇒ **`both(build: (lang) => string)`** is the primitive and `tb(key)` is defined in terms of it, so there is one
joining rule. **Compose the whole body once per language, join once.**
🔑 **Your control is intact and unchanged in kind:** `both()` and `tb()` take **no `lang`** — asserted by their
`.length` — so neither can render one language; labels keep `t(key, lang)` so they cannot become 40 characters.
Measured, not argued: `tb("btn_children")` is **over 20 characters**. That is why the rule is split at all.

### 🔴 Q2 — the interleaving is most of the flow, and one case is silent
Named in a table in the TASK. The one I want said out loud: **`children_title` + `(3/5)`** — the count is
appended AFTER the key, so under per-key joining **it lands on the English line only**. Nothing errors; a Thai
reader simply never sees how many children they have.
⇒ **`VerifyResult.message` became `(lang) => string`.** `verifyAndLink` does database work and cannot be called
twice, so the language had to move to the SEND. That is what let the verify path land tonight at all.

### ⏱️ Scope — cut as a WHOLE FLOW
**Landed: registration, end to end** — welcome · role prompt · the three code prompts · the whole verify path ·
2FA · add-student (prompt, summary+confirm, cancelled, added, skip, error).
**Not landed, each a whole flow:** the menu/command list · check-in · leave · QR · the children list · the
teacher's `ตาราง` · the handover/mute replies.
📌 **A parent registering tonight reads both languages throughout; after linking, the menu is still Thai-only.**
That is your *"reads as staging"* trade, taken deliberately rather than shipping a half-bilingual registration.

### 🔴 The §17 English — the list is ONE key, and not for the reason either of us expected
**Exactly one of the customer's English strings exists anywhere in this repo:** *"Please type 'register' to
start."* Applied verbatim in `welcome`.
**Their 8-screen copy is not transcribed.** §17 is @Porter's **analysis** of it — a table of *"their step vs
today"* — not the strings. ⇒ **all 110 conversational keys ship OUR English**, because there is nothing of theirs
to prefer.
📌 **A document that exists outside the repo doing work inside it** — the same shape as *"a note is not a
mechanism"*. **If @Porter transcribes the eight screens, applying them is cheap and mechanical.** Until then
*"the customer's English"* is one sentence, and I would rather say so than let that DoD line read as covered.

**BALL: @Sober — TASK-275's registration slice is ready for review, and @Porter needs the 8-screen transcript
before the rest of their English can ship. Remaining flows, 273 and 268, all still mine.**

## [--:--] Sober (SA) — ✅ Registration is bilingual, tonight. 🔻 **And we shipped the MECHANISM, not the customer's WORDS — because I wrote a DoD line citing a source I never opened.**
`tsc` **0** · `bun test` **1621 / 0** (127 files) · **notifications and ICS byte-identical**, asserted — the
`SPEC-077` §5 hold held.

### 🔻 My error, and it is the third of the same kind this week
@Jason went to apply the customer's English and found there was almost none. **I grepped `REQ-079`: 503 lines,
not one English string.** **§17 is @Porter's ANALYSIS of their 8 screens, not a transcript.**
⇒ **The single sentence he could apply — *"Please type 'register' to start."* — came from @Porter's INBOX
MESSAGE**, and **I quoted it in `SPEC-077` §4 as though I had read it in the REQ.** All 110 other conversational
strings ship **our** English.
📌 **Third time this week I have cited a source without opening it:** the superseded `SYSTEM-FACTS` line · the
*"verified"* filter one layer behind the validator · this. 🔴 **The pattern is not carelessness about facts —
it is trusting a citation because I am the one who wrote the sentence containing it.**
🔑 **@Jason's framing, which I took to @Porter verbatim:** *"a document that exists outside the repo doing work
inside it."* **That is `CLAUDE.md`'s own rule from the other end — if a fact is not in a file in this repo, it
does not exist — and I broke it in a Definition of Done.**
📨 **DATA REQUEST raised:** @Porter transcribes the 8 screens **verbatim** into `REQ-079` §17. **Applying them
afterwards is cheap and mechanical** — the keys exist, each takes their sentence in place of ours.
⚠️ **And @Porter is told plainly what NOT to say tonight:** *"registration is bilingual; the English is ours
until their copy is in our hands."* **If the owner tells the customer we used their words, the customer will
open it and see that we did not.**

### 🔑 He replaced my mechanism, correctly
I specified `tb(key) → TH\nEN`. **Four keys are suffix FRAGMENTS beginning with `\n`, appended to other bodies**
— *"there is no separator choice that rescues it; the fragment is not a message."* ⇒ **`both(build)` composes the
whole body per language and joins once.**
✅ **The control survived the redesign intact** — `both()` takes a builder, not a language, so a body still
**cannot** render one; labels keep `t(key, lang)` and cannot become 40 characters. **Changing the mechanism
without weakening the property it existed for is the harder half.**
✅ **Measured, not argued:** `tb("btn_children")` is over 20 characters — *that* is why labels are not bilingual.
🔴 **Best catch in the set: `children_title` + `(3/5)` lands on the ENGLISH line only** — silent, looks fine in
Thai, invisible to any test that reads one language.

### The scope call — taken the way I asked
**Registration, whole**, five flows named as not landing, **each a whole flow.** ⇒ **TASK-276, no clock, and NOT
before TASK-273.** ⚠️ **§2 of it is a real design question:** the teacher's `ตาราง` is **block-per-language, not
row-per-language** — **the owner's length objection survives for LISTS even though it lost for prompts** — with a
5000-character ceiling for a busy coach that would be a decision, not a workaround.

📌 Also checked and NOT reported: I went after his arity assertions with the wrong grep and nearly called them
missing. **They are there.** **Checking before saying so has paid twice today.**

**BALL: @Porter — the transcript, and what he tells the owner tonight.**

## [--:--] Porter (PM) — 🔻 §17 never held the customer's strings. I said it did. Transcribed now as §17b.
**@Sober asked for the copy as a DATA REQUEST; I had it in chat and had never put it in a file.** I told him
*"§17 holds the source text"* — **§17 holds my ANALYSIS, with fragments.** He wrote a spec and a DoD line pointing
at it **without opening it**, and **@Jason found it by trying to USE it.** **Three links; the first is mine.**
🔑 **@Jason named the class and it is our own rule inverted:** *"a document that exists outside the repo doing work
inside it."* **`CLAUDE.md`: if a fact is not in a file in this repo, it does not exist.** **I broke it while
quoting it — and the fact in question was the customer's own words, which was the entire point of the request.**
✅ **Fixed: `REQ-079` §17b carries all eight screens verbatim**, plus **the four places their copy is already
superseded** (typed `Next` → buttons · their dots → our dashes · the numbered lists were an example · their
`1. 2. 3.` headings are section titles, not message text — **that last one would have shipped as chat text**).
📌 **And I told him not to re-do tonight's work as though it were wrong.** **It was correct against the only
source that existed at the time.** **The source was the defect, not the pass.**

## [--:--] Porter (PM) — 🔴 The CUSTOMER asked for a delivery date: *"เราจะใช้ได้แล้วเป็นช่วงบ่าย ถูกรึป่าวคะ"*
**The owner needs an answer he can send, so this is the honest state, not the hopeful one.**

🔴 **The structural blocker that decides it, and it has nothing to do with how fast anyone works: the `uat`
deploy CANNOT happen during business hours.** `0033` rebuilds the slot index under **ACCESS EXCLUSIVE** on a
live table, and @Sober refused to hand me *"it should be quick"*. **The owner's own rule: run it when the shop is
closed.** ⇒ **"ช่วงบ่าย" is not a window we can use — the earliest is after 18:30 on the day we deploy.**

**What is actually true tonight:**
- **`uat` has NONE of this.** It has never had `0028`–`0034`; the customer is on a build from before REQ-078.
- **`sid` has it, but the round did not finish clean:** `REQ-082` all five PASS ✅ · **`REQ-076` FAILED on DEF-1**
  (fixed in code, **not yet redeployed and not yet re-tested**) · **`REQ-083` AC-5/AC-7 not testable at all** —
  no per-booking revenue has ever been written on that box.
- **The bilingual conversation is not built yet**, and the customer's own strings only entered the repo tonight.
🔴 **And one open question is bigger than the schedule:** **no attendance has ever written a revenue movement on
`sid`.** **Nobody has established what `uat` does.** ⇒ **before telling a shop to run their business on this, I
want that answered** — @Tanya's 18:30 experiment may already have.

⇒ **Given him a shape he can send: tomorrow EVENING for the deploy, usable the morning after** — and the two
things that would change it. **Not a promise of an afternoon we cannot make.**

## [--:--] Sober (SA) — 📨 The customer's copy is in the repo (`REQ-079` §17b). 🔴 **And mapping it found an OWNER RULING from 09-06 that never became a task — in the flow we shipped tonight.**

@Porter transcribed the eight screens verbatim after the DATA REQUEST, and named the four places their copy is
already superseded. **That is the whole fix for the earlier failure and it took one file.**

### 🔴 The find, and it is mine
**`REQ-079` §17 was CLOSED by the owner on 2026-09-06 with TWO rulings.** I carried one (*"adding students:
unchanged"*) and **never cut a task for the other**: **the birth date is `วัน-เดือน-ปี` (`DD-MM-YYYY`)**, and
§17 says in terms *"the deployed prompt and parser both change."*
**Searched `tasks/`, `specs/`, `board.md` tonight: ZERO hits.** **Verified in the source:**
`add_birthdate_prompt` still says `ปปปป-ดด-วว`, and `parseBirthDate` (`line-add-student.ts:61`) matches
**four-digit-year-first only.**
🔻 **And TASK-275 made that prompt bilingual a few hours ago ⇒ we translated a string the owner had already
overruled.**
📌 **A decision written down and never turned into a task is this week's class exactly — a note is not a
mechanism — except this note was an owner's ANSWER, sitting in the requirement the whole time.**
⇒ **TASK-277, before the copy pass**, because the format decides what that key must say. **@Porter is to tell him
it is being fixed rather than wait to be asked** — it is a day old and it is on the screen a new parent meets.

### 🔻 Two failures on the same page, and only one has an excuse
@Porter's framing of the copy gap is right and I am using it: *"it was correct against the only source that
existed at the time — the source was the problem, not the work."*
⚠️ **The §17 miss is NOT that.** **The source existed, in the REQ, and I did not act on it.**

### The copy pass — TASK-278, and five places their text cannot be literal
Mapped screen by screen. 🚫 **Screen 2 contributes NOTHING** — their only string is the typed-`Next` instruction
and we ship quick-reply buttons (TASK-251), the fix for their own `1/2/3` complaint. **Applying it would instruct
a word the bot does not accept.** **Said out loud rather than skipped quietly.**
Five rules we already own, each of which their literal text would break:
1. ***"Type "Cancel" to exit."*** — `withExit` **already appends it to every question** ⇒ printed **twice**, on
   the one step that used to have it inline. **The exact bug TASK-245 was written to kill.**
2. ***"Registration completed ✅"*** has **no children line**; ours does, and it is **TASK-047's privacy
   decision** — a COUNT, never names, because anyone can type a phone number.
3. **"up to 5"** is a setting here, not a literal.
4. **The DOB format and `skip`** must survive their shorter sentence.
5. 🔴 **Their `ชื่อ / Name:` slash-pairs are DOCUMENT formatting, not message format** — the same shape as the
   headings @Porter already ruled are section titles. ⇒ **the summary stays block-per-language.** **A message
   with two bilingual conventions is TASK-257 §3 exactly.**

📌 **Order for @Jason: TASK-273 → 277 → 278 → 276 → 268.** 277 jumps 273 only if the owner is asking about the
date; otherwise **273 stays first — it is the one that stops a defect being born.**

**BALL: @Jason — TASK-273, then 277 · 278 · 276 · 268. Five tasks, all cut, all unblocked.**
🔻 **Correction (Sober, same night): I first wrote the ball as @Porter because I had just sent him a message. Wrong — nothing waits on him for work to move.** His items are his alone to carry, and the deploy window waits on @Tanya, not on him. **A queue is not the ball.**
📌 With @Porter, blocking nothing: Still with him: the `ภาษา` ก/ข · the three
notification-value answers · the report count (`totalBooked` + badge, one answer) · the `PENDING_RESCHEDULE`
word · the pause deploy window after @Tanya.

## [--:--] Jason (BE) — ✅ TASK-273 DONE (code): the eleventh card cannot ship without a heading. → @Sober

`smart-scheduler-back` · tsc 0 · **1628 pass / 0 fail** across 128 files · 🚫 no migration, no database, no FE
change; the ten labels' text, the card order and every card's logic unchanged, and `t()`'s fall-through untouched.

### 🔑 The control, failing
Appending an 11th card with no label:
> `TS2741: Property 'my_new_card' is missing … but required in type 'Record<… | "my_new_card" | …, Entry>'`

**`my_new_card` is in the required union because the type is DERIVED from the array I just appended to.** The
card and its heading are one decision now, and the compiler is the one asking.

### ⚠️ The obvious version does not work, and the reason is worth a line
`as const satisfies readonly AttentionCheck[]` on the EXPORTED array **narrows away the optional
`namesPeopleInDigest`** — four readers broke, because every one would have to prove the property exists before
asking. ⇒ the `as const` lives on a private `CHECKS` and `ATTENTION_CHECKS` is re-exported as
`readonly AttentionCheck[]`. **One array, two views** — literal keys for the type, the interface for the readers.
📌 And `satisfies` turned out to be load-bearing rather than decorative: without it the `run: async (ctx) => …`
callbacks lose contextual typing and every parameter becomes implicitly `any`. **I learned that by removing the
annotation and watching seven `TS7006`s** — which is the honest way to find out what an annotation was doing.

### ➕ One addition: `titleKey` is now asserted to equal `att_` + `key`
Two spellings of one fact, sitting next to each other in the same object, and **nothing had ever compared them.**
⚠️ A card whose `titleKey` had a typo would render a raw key **even with this task's Record in place**, because
the Record is keyed on `key` while the digest builds its own `att_${c.key}`. They agree today; now they cannot
silently stop.

### 🔴 The Question — not persisted, but they cross the wire
✅ **Nothing stores a card key**: no column, no enum, no setting, no dismissal record. The two `sales_not_posted`
hits outside `attention.ts` are **comments** — I opened both.
⚠️ **But the frontend holds a full second copy** — `attention.service.ts` has its own `key` AND `titleKey`, and
`dictionaries.ts` its own TH/EN headings for the same ten. ⇒ **typing them here is free and safe** (nothing is
renamed, the FE is untouched), **but a future RENAME would break the FE silently** — the same across-the-wire
duplication as `BookingStatus`.
🚫 **No task from me**, because you have already ruled on that class: *"what is missing is a mechanism, and the
only real ones are codegen or a cross-repo contract test — decisions, not bug fixes."* Recorded because *"a type
is free, but discovering that a key is also something else after renaming one is not"* — **and the something else
here is another repo.**

**BALL: @Sober — TASK-273 ready for review. Next: 277 → 278 → 276 → 268, in your order.**

## [--:--] Sober (SA) — ✅ TASK-273 DONE. 🔻 **And my own mutation gave a FALSE CONFIRMATION — the lesson is worth more than the task.**
`tsc` **0** · `bun test` **1628 / 0** (128 files) · `smart-scheduler-front` **clean, 0 files** · nothing applied.
`AttentionKey` derives from the card array; the headings are a `Record<AttentionKey, Entry>` ⇒ **an eleventh card
without a heading fails the build.**

### 🔻 The false confirmation, recorded because I would have believed it
I appended an 11th card with `run: async () => null`. **`tsc` went red** — and the errors were **two `TS2322`s
about my stub's return type**, with **no `TS2741` about the missing label anywhere.** ⇒ **the card was malformed,
`satisfies` broke first, and the control I was testing never ran.**
**Re-ran with a well-formed card ⇒ exactly his `TS2741`**, `"my_new_card"` in the required union.
📌 **A red `tsc` would have "confirmed" his claim while proving nothing about it** — **the same shape as his
`Bun.resolveSync` printing a ✓ over the exact failure on TASK-267, inverted.** ⇒ **A mutation that fails for a
different reason than the one under test is not evidence, and it is indistinguishable from evidence.**
🔑 **The general form, since this is the second time this week it has bitten someone:** *break the thing, then
check that it broke FOR THE REASON YOU EXPECTED.* **A pass/fail is not the measurement; the reason is.**

### 🔑 Two things he did that I did not ask for, and both close real holes
1. **`as const satisfies` on the EXPORTED array narrowed away an optional field** (`namesPeopleInDigest`) and
   broke four readers. ⇒ **private `CHECKS` with the literals, `ATTENTION_CHECKS` re-exported as the interface —
   one array, two views**, not two lists. **And he established what `satisfies` was load-bearing FOR by removing
   it and watching seven `TS7006`s.** *"The honest way to learn what an annotation was doing."*
2. ➕ **`titleKey` asserted to equal `att_` + `key`.** 🔑 **That is the hole my task left:** the Record is keyed on
   `key`, but the digest builds its own `att_${c.key}`, **so a typo'd `titleKey` would still render a raw key
   WITH the Record in place.** **Second time this week he has closed the half of a control I specified** — the
   other was typing `veventStatus`'s input.

### The FE answer — he applied my own ruling back to me, correctly
Card keys are **persisted nowhere** (both `sales_not_posted` hits are comments; he checked), **but there is a
full second copy of the keys AND a third copy of the labels in `smart-scheduler-front`.** ⇒ **no task**, on my
`BookingStatus` ruling: *the only real mechanisms are codegen or a cross-repo contract test — decisions, not bug
fixes.* 📌 **Third occurrence of that class in two days** — status list, status labels, attention keys. **It
strengthens the codegen option for whenever @Porter has room to put it to the owner.**

**BALL: @Jason — TASK-277**, then 278 · 276 · 268.

## [--:--] Jason (BE) — ✅ TASK-277 DONE (code): day-first in, ISO out — and the SUITE was defending the overruled format. → @Sober

`smart-scheduler-back` · tsc 0 · **1639 pass / 0 fail** across 129 files · 🚫 no migration, no database, no FE
change; skip, the strike behaviour, `withExit`, `both()`, the label cap, the notifications and the ICS feed all
untouched.

### 🔴 The two tests that pinned the format the owner overruled
`line-add-student.test.ts` asserted `parseBirthDate("2018-04-02") → "2018-04-02"` **and, four lines later,
`parseBirthDate("02-04-2018").ok === false`.** ⇒ **the suite was actively defending the thing §17 closed on
09-06.** A green suite was part of why the ruling could sit unbuilt for a day: *nothing was failing.*
✅ Rewritten as the requirement, not deleted — the round-trip guard, the skip path and the "impossible date"
property all kept, restated day-first.

### 🔑 The refusal is by SHAPE, and that is the whole of §3
```ts
if (/^\d{4}[-/]\d{1,2}[-/]\d{1,2}$/.test(raw)) return { ok: false };   // the retired order, caught FIRST
```
`2024-12-02` would have failed anyway — day 2024 is out of range — **but a range failure and a format failure
are the same `{ ok: false }` to the caller, and the parent reads a different sentence.** Matching the shape
first makes the refusal be *about the order*.
**Mutation-tested, and I checked WHY it went red** (your correction from TASK-273, applied): removing that line
fails the `2024-12-02` test **because the string starts parsing**, not because something else broke.
🚫 Both orders are not accepted — asserted with `03-04-2024` → `2024-04-03` and nothing else.

### The confirm step now says why it exists
Comment where the summary is built: *"LOAD-BEARING FOR CORRECTNESS, not just for review… the parser is
unambiguous; the person typing is not."* Asserted by a test, **so the sentence cannot be deleted quietly
either** — which is the difference between a comment and a control.

### 🔴 The Question — one more, same section, same shape
**`REQ-079` §17 §3c: *"phone shown formatted (`082-503-1502`)"*, closed as *"cosmetic, cheap, do them"*, never
built.** `verify_parent_ok_*` interpolate `{phone}`, and `{phone}` is `normalizePhone(code)` — **the raw digits.**
No formatter exists anywhere; I grepped. ⇒ a parent registering today reads `0825031502` in the message that
confirms their account.
✅ **The other two items of §3c ARE built** — `ยืนยัน`/`confirm` accepted, children listed back at the
add-student step — **so it is one omission, not a forgotten paragraph.**
🚫 **Not built here:** it is a display decision inside a message TASK-278 is about to rewrite, and doing both in
one diff is how two changes become one unreviewable one. **Named, per your instruction.**
⚠️ Not counted: §18's own *"the `ภาษา` control may now switch nothing"* — **you raised that in the same
document** and it is with @Porter.

**BALL: @Sober — TASK-277 ready for review. Next: 278 → 276 → 268.**

## [--:--] Sober (SA) — ✅ TASK-277 DONE. 🔻 **And his answer found a SECOND unbuilt ruling in the same file, which makes it a PROCESS failure. Mechanism written into `SA-Lead.md`.**
`tsc` **0** · `bun test` **1639 / 0** (129 files) · nothing applied. The four-digit-first shape is refused
**before** the day-first pattern is tried; `ข้าม` survives; the stored value is still `YYYY-MM-DD`.

### 🔑 His reasoning beat my DoD line
> *"`2024-12-02` would have failed anyway — day 2024 is out of range — but the point is which sentence the parent
> reads, and a range failure and a format failure are the same `{ ok: false }` to the caller."*

**The behaviour was already correct; the MESSAGE was not.** ⇒ **a control that returns the right verdict for the
wrong reason is one refactor away from returning the wrong verdict**, and it reads as fine until then.
✅ **And he mutation-tested it AND checked the failure reason** — *"it fails for the right reason: the string
starts parsing instead of being rejected."* 📌 **That is the rule I wrote four hours ago after my own false
confirmation on TASK-273, applied without being told.**

### 🔴 Two tests were DEFENDING the overruled format
`parseBirthDate("2018-04-02") → "2018-04-02"` and, four lines later, `("02-04-2018").ok === false`.
⇒ **the suite was actively holding the format the owner overruled on 09-06** — **which is why a day of green
builds hid the miss.** 📌 **The sharpest argument yet for rewriting tests AS THE REQUIREMENT rather than deleting
them:** a test that pins yesterday's decision is indistinguishable from one that pins today's.

### 🔻 The second miss makes this a process failure, not two slips
**`REQ-079` §3c — *"cosmetic, cheap, do them: phone shown formatted"*. Two of its three items were built; this
one was not.** A parent reads **`0825031502`** in the message confirming their account, while **their own screen
4 shows `082-503-1502`.**
⇒ **Two decisions in one requirement, closed and never carried, both found by @Jason while looking for something
else.** 🔑 **Once is a slip. Twice in one file is that there is no mechanism** — and *"a note is not a
mechanism"* is this week's whole lesson, so **it applies to my workflow, not only to code.**
✅ **`SA-Lead.md` now carries the rule:** *a section marked ✅ CLOSED must name the TASK carrying each ruling, or
say "no work" — at the moment of closing, which is the only moment anyone re-reads it.* ⚠️ **"No work" is a real
and common answer, and writing it is the point** — an unmentioned ruling and one that needs nothing look
identical later.
✅ **`TASK-279` is the backlog, and it is MINE** — the rule protects only what is closed from today. 🚫 No code,
no tasks cut mid-sweep, and **where it stops is written down, by file.**
📌 **@Jason was right to name it and right not to sweep** — I had told him the requirements folder is mine.

### 🔻 And I changed my mind in front of him
He said the phone formatting did not belong in TASK-277. **Right — and I went further: it belongs in TASK-278**,
because **§17b's screen 4 shows the formatted number**, so rendering their copy faithfully *is* the formatting.
**Folded in as §6 rather than cut as a third task touching one string**, with 278's *"every Thai string
byte-identical"* line now naming the one intended exception — **so the assertion stays honest instead of being
quietly relaxed later.**

**BALL: @Jason — TASK-278**, then 276 · 268. TASK-279 is mine and blocks nothing.

## [--:--] Jason (BE) — ✅ TASK-278 DONE (code): their English applied — and the confirm step echoes a format the parent did not type. → @Sober

`smart-scheduler-back` · tsc 0 · **1659 pass / 0 fail** across 130 files · 🚫 no migration, no database, no FE
change; the six notifications and the ICS feed untouched and asserted; `both()`, `withExit`, the label cap and
the strike behaviour all untouched.

### ✅ The copy, and the five places it is deliberately not literal
Each mapped key asserted against §17b screen by screen. The five §4 rules each have their own test: the exit
prints **once** (`add_summary_confirm` asserted to contain no `cancel`/`ยกเลิก`, `withExit` still the one append
site) · `{list}` and `{phone}` survive · `{max}` is a variable **and** asserted not to be a literal 5 · the DOB
format and `skip` survive their sentence · **no summary label contains an inline ` / `**.
📌 The last one is the one I would have got wrong without §4: their `ชื่อ / Name:` reads like a message format
and is a document format. **One message, one convention** — TASK-257 §3, again.

### §3 — screen 2 has no applicable string, and I am saying so rather than skipping it
Their only screen-2 line is the typed-`Next` instruction; we ship buttons. **Applying it would instruct a word
`parseRoleChoice` rejects.** Asserted that `role_prompt` contains no `Next` in either language.

### §6 — the formatter is display-only by contract, not by convention
It sits **beside `normalizePhone`** so the pair is read together, carrying 🚫 *"deliberately NOT the inverse, and
must never be used as one."* Three call sites, all asserted to be inside a `t("verify_parent_ok…")`
interpolation **by position**, and asserted that `parent.service`, `scheduler.service` and `routes/api` never
call it. ⚠️ `021234567` · `66825031502` · `12345` · `""` pass through unchanged — **we accept what people type,
so the display must not invent a shape for a number it does not recognise.**

### 🔴 The Question — one difference is NOT cosmetic, and it lands on the step TASK-277 just made load-bearing
**Their screen 7 echoes the date as `02.12.2024` — the order the parent typed. Ours echoes `2024-12-02`**,
because `summaryLines` prints `draft.birthDate`, the stored ISO value.
⚠️ TASK-277's ruling leans on that step: *"`03-04-2024` is ambiguous to a human — what saves it is the summary
printing the date back."* **But it prints it back in a format the parent did not use.** Someone who typed
`03-04-2024` meaning 4 March sees `2024-04-03` and has to re-derive the order to check it. **The guard exists;
it is weaker than the ruling assumes.**
🚫 **Not built** — you said name them and build none, and this is a decision (echo the input format, or accept
the re-derivation). **It belongs with the DOB ruling, not inside a copy pass.**

### The rest of the answer, both directions
**Steps we have that their copy has no screen for:** `skip` (never mentioned in their document) · the exit on
EVERY question (theirs has `Cancel` on screen 7 only — ours is TASK-245 and is the right divergence) · the
add-another loop · the children-on-file line · ⚠️ **the 2FA step, which is switched OFF today so the flows
match — but switching it on is a SETTING, and the customer has never seen that screen** · two-strikes handover.
**Steps their copy has that we do not: none.** Their dots and their numbered headings were already ruled out in
§17b's own departures; I did not re-litigate either.

**BALL: @Sober — TASK-278 ready for review; the date-echo question is for @Porter with the DOB ruling.
Next: 276 → 268.**

## [--:--] Sober (SA) — ✅ TASK-278 DONE: the customer's words are in the bot. 🔴 **And his last finding lands on the step TASK-277 declared load-bearing — I RULED it rather than sending it up.** `REQ-079` fully swept (§19).
`tsc` **0** · `bun test` **1659 / 0** (130 files) · nothing applied · notifications and ICS byte-identical.

### 🔴 The confirm step echoes a format the parent did not type — TASK-280
A parent types `02-12-2024` and is shown **`2024-12-02`** (`summaryLines` prints the stored ISO).
⚠️ **TASK-277 made that step load-bearing for correctness PRECISELY because `03-04-2024` is ambiguous to a
human** — **and then it prints the date back in the other order**, so the reader performs the conversion the step
exists to spare them. 🔴 **On this field that is close to no guard at all, and my TASK-277 ruling assumed a
guard stronger than the one we had.**
✅ **RULED day-first, and deliberately NOT sent to @Porter:** it applies two decisions we already hold — **the
owner's input order** (§17) and **the customer's own screen 7 (`02.12.2024`)**. **Both point the same way;
nothing new is chosen.** ⇒ display only, stored value stays ISO, formatter asserted **by position** and carrying
the same sentence as his phone formatter: *it is deliberately NOT the inverse of the parser.*
📌 **@Porter told as a statement**, the `ไม่มี` shape again.

### 🔑 His guardrails on the phone formatter were better than the ones I wrote
**Placed beside `normalizePhone` with *"it is NOT the inverse and must never be used as one"*** — **that sentence
is the control**, because the obvious future mistake is reaching for it to undo the normaliser. ✅ **Asserted by
POSITION, not by counting call sites**, which survives someone adding a fourth.

### ⚠️ Six things in our flow the customer has never seen — carried to @Porter, none a defect
`skip` · **the exit on every question** (their copy shows `Cancel` only on screen 7; ours appends it everywhere
because **the owner himself got stuck** — a deliberate and correct divergence) · the add-another loop · the
children-on-file line · **2FA, which is OFF BY A SETTING, not by a build — so "turn it on" is a sentence he could
say at any moment and they have never seen that screen** · two-strikes-then-a-human.
📌 *"The customer has not seen it"* **is a different risk from** *"we did it wrong"*, **and only one of them is
ours to fix.** ✅ **Clean negative: no step in their copy that we lack** — a claim only worth anything because he
had both documents open at once.

### ✅ TASK-279 — `REQ-079` swept, written into the REQ as §19
**Eleven closed rulings; each now carries a task id or the sentence saying it needs no work.** **Two had
neither** — the two @Jason found. ⚠️ **The other requirements are NOT swept** (`REQ-076` · `077` · `082` · `083`
next), **and where it stopped is written down** — *a sweep nobody finishes is worse than one that is honestly
partial: it makes the backlog look checked.*

**BALL: @Jason — TASK-280**, then 276 · 268. TASK-279 continues and blocks nothing.

## [--:--] Jason (BE) — ✅ TASK-280 DONE (code): what a parent types is what a parent is shown. → @Sober

`smart-scheduler-back` · tsc 0 · **1669 pass / 0 fail** across 131 files · 🚫 no migration, no database, no FE
change; `parseBirthDate`, the stored value, the skip path, `withExit`, the strike behaviour, the notifications
and the ICS feed all untouched.

### ✅ Asserted END TO END, because the formatter being right is not the claim
`02-12-2024` → stored `2024-12-02` → **shown `02-12-2024`**, through the real parser and the real
`summaryLines`. And the ambiguous case gets its own test **with its mirror beside it**: `03-04-2024` and
`04-03-2024` echo differently — **that difference is the property**, not either string.
📌 One case worth naming: `2-4-2018` echoes `02-04-2018`, padded. Not the literal keystrokes, and right — **it is
what the system understood, which is what the parent is being asked to confirm.**

### 🔴 The suite was pinning the ISO echo — second time in two tasks, same file
`line-add-student.test.ts:44` asserted `"วันเกิด: 2018-04-02"`, so **the weakened guard was green.** TASK-277
found the same file defending the overruled input order; this one found it defending the echo. **Corrected, not
deleted, with the reason on the line.**
✅ Mutation-tested **and it fails for the right reason**: reverting the echo fails the three round-trip tests —
the ones about what a parent SEES — and leaves the display-only ones passing.

### The control is the placement, not just the function
`formatBirthDateForDisplay` sits **immediately after `parseBirthDate`**, carrying your sentence: *"deliberately
NOT the inverse, and must never be used as one."* Asserted that they sit in that order in the file — **the
sentence has to be where the person about to make the mistake is already looking.** Only caller asserted **by
position**, plus four modules asserted not to call it at all.

### 🔴 The Question — the confirm step is the only place, and the reason generalises
Swept every reader of `birthDate`: `attention.ts` asks *is it missing* (presence, never displayed) ·
`som-report` derives an age band (a computation) · the schema is storage · `student-import` parses and does not
echo for verification.
📌 **And every other date a parent or teacher reads — `Start`, `Date`, the leave dates — is one the SYSTEM chose,
not one they typed.** Your distinction holds exactly: *an echo whose format differs from the input is only a
defect where somebody is verifying.* **Nobody is verifying their own keystrokes anywhere else.**

### ➕ 🔑 And the sweep turned up a fact that reframes TASK-277
**`parseImportDob` has been DAY-FIRST all along** — `(\d{1,2})[/.-](\d{1,2})[/.-](\d{4})`, dots accepted, which
is literally the `02.12.2024` the customer wrote.
⇒ **The product had TWO date conventions before this week: the admin import took the customer's order; the LINE
flow demanded ISO.** **TASK-277 did not create a divergence — it removed one.** The owner's ruling aligned LINE
with what the import had been doing since it was written. 🚫 Nothing to change; recorded because it makes the
ruling read as a correction rather than a preference.

**BALL: @Sober — TASK-280 ready for review. Next: 276 (I will read its §2 first), then 268.**

## [--:--] Sober (SA) — ✅ TASK-280 DONE. **The registration flow is complete and correct.** 🔑 **And his sweep showed the date ruling was a CORRECTION, not a preference.**
`tsc` **0** · `bun test` **1669 / 0** (131 files) · nothing applied. **A parent types `02-12-2024` and the confirm
step shows `02-12-2024`** — the guard TASK-277 declared load-bearing actually holds now.

### 🔑 His test is better than my Definition of Done
I asked that `03-04-2024` echo itself. **A formatter that returned its input unchanged would have passed that.**
He asserted **`03-04-2024` beside `04-03-2024`, that the two echoes DIFFER** — *"that is the property, not the
string."* ⇒ **the difference between testing the output and testing the guard.**

### 🔴 A test was pinning the weakened guard — second time in two tasks, same file
`line-add-student.test.ts:44` asserted `"วันเกิด: 2018-04-02"`, holding the ISO echo green. **First the input
order, now the echo, both in `line-add-student.test.ts` in one night.** 📌 **A test written for a decision keeps
defending it after the decision changes, and nothing about it looks stale.** ✅ Mutation-tested with the failure
set checked: reverting the echo fails **the three round-trip tests — the ones about what a parent SEES** — and
not the display-only ones.

### 🔑 THE finding: `parseImportDob` has been DAY-FIRST since it was written
The admin CSV import has accepted **`02.12.2024`** — day-first, dots included, **the customer's exact format** —
all along. ⇒ **the product had TWO date conventions before this week: the import took the customer's order, the
LINE flow demanded ISO.** **TASK-277 did not create a divergence; it REMOVED one.**
🔑 **And that changes what @Porter tells the owner:** *"we changed LINE to match what you asked"* is a preference
honoured; ***"the import already worked that way, LINE was the odd one out"*** **is a correction** — truer, and
far easier to defend if the customer ever asks why it was wrong before. **Sent up in his words.**
⚠️ **Recorded, not acted on: the import accepts DOTS and LINE does not.** Deliberate — the owner ruled *"their
order with OUR dash"* — so **the two now agree on ORDER and differ on SEPARATOR.** 📌 **Written down because the
obvious future tidy-up is to make them identical, which would quietly reverse half of a one-day-old ruling.**

### Where the LINE work stands
✅ **Registration: bilingual · the customer's own English · their date order · the confirm step echoing what the
parent typed.** End to end, the flow a new parent meets.
🚫 Still single-language: the `เมนู` list · check-in/leave/QR · the children list · the teacher's `ตาราง` · the
handover replies — **TASK-276, no clock, whole flows on purpose.**
⏸️ Still held on @Porter's three answers: the notification values and the ICS description.

**BALL: @Jason — TASK-276**, then 268. 🔴 **And the pause shipment (270+271+272) has been ready for hours,
waiting on @Tanya then @Porter's deploy window.**

## [--:--] Jason (BE) — ✅ TASK-276 DONE (code): the conversation is bilingual end to end. → @Sober

`smart-scheduler-back` · tsc 0 · **1683 pass / 0 fail** across 132 files · 🚫 no migration, no database, no FE
change; **the six notifications and the ICS feed asserted byte-identical AGAIN rather than inherited**; postback
keys, `line_lang`, the `ภาษา` cell and the menu images untouched.

### §2 — the size question, answered with a number instead of a judgement
**It fits.** Measured on the real composer: 20 rows (its own cap), long Thai names, long programme, a note on
**every** row ⇒ **3,848 characters doubled**, against LINE's 5,000.
📌 **The bound is the CAP, not the data** — 40 rows measures the same 3,848, because `renderSchedule` caps at 20
and appends its "and N more" line. ⇒ **no decision needed, and it is safe by mechanism rather than by an
assumption about how busy a coach gets.** Asserted with a 40-row fixture, so raising the cap fails here.
🔑 **And `both()` is what keeps a ROW single-language**: `renderSchedule` runs once per language, so `status_*`
renders Thai inside the Thai block and English inside the English one. **The one-line-per-class layout survives
for free** — a bilingual key would have put the English status inside the Thai row.

### §3 — the silent one, asserted per LINE
`children_title` + `(3/5)` is asserted to have the count on **both** lines, not to "contain (3/5)" — the weaker
assertion passes on the broken version. The broken shape is written into the test and asserted against, with the
sentence worth keeping: **the Thai reader never sees the count and nothing about the Thai output looks wrong.**
✅ Mutation-tested: reverting that one line to `${tb("children_title")} (…)` fails the children-list test.

### 🔴 The Question — one shared body, and the shape we chose for another reason is what protects it
**`tsched_empty` is used by BOTH** `line-schedule.ts` (the teacher's `ตาราง`) and `line-today-schedule.ts` (the
`daily_reminder` outbox message). It is the only one — I compared every key on the bot side against every key on
the outbox side.
🔑 **It is safe, and not by luck: the composition happens at the CALLER, not at the key.**
`both((l) => renderSchedule(rows, l, range))` makes the teacher's reply bilingual; the notification still calls
`t("tsched_empty", lang)` itself and is untouched.
🔴 **Under a per-key `tb("tsched_empty")` the notification would have gone bilingual too — silently, in a message
`SPEC-077` §5 explicitly holds.** ⇒ **the shape TASK-275 chose for a completely different reason (the suffix
fragments) is what protects the held boundary here.**
📌 Same lesson as `ob_l_note` serving `booking_confirmed`, reached from the other end: **there, two callers
forced a second key; here, one function let one key serve both.**
⚠️ 🚫 Not changed, as instructed — **but it is now load-bearing.** `tsched_empty` is a key whose two callers must
each keep choosing their own language, and **if the notifications are ever unheld it is the first thing to look
at.**

**BALL: @Sober — TASK-276 ready for review. Next and last of mine: TASK-268.**

## [--:--] Sober (SA) — ✅ TASK-276 DONE. **The conversation is bilingual END TO END.** 🔴 **And his `tsched_empty` finding shows MY OWN SPEC contradicted itself.**
`tsc` **0** · `bun test` **1683 / 0** (132 files) · notifications and ICS **asserted byte-identical again, not
inherited** · nothing applied.

### 🔴 `SPEC-077` §3 would have broken `SPEC-077` §5
**§3 specified `tb(key)` → `TH\nEN` per key. §5 held the six notifications single-language.**
**`tsched_empty` is called by BOTH** — `line-schedule.ts:42` (the teacher's `ตาราง`, conversational) and
`line-today-schedule.ts:64` (the daily-reminder outbox). ⇒ **a per-key `tb()` would have made a HELD notification
bilingual, silently.**
🔑 **@Jason replaced `tb()` with `both()` on TASK-275 for a completely different reason — the four `\n`-prefixed
fragments — and THAT is what kept the hold.** **The composition happens at the CALLER, not at the key**, so one
key serves two boundaries and each caller keeps choosing its own language.
🔻 **I wrote both sections of that spec, two hours apart, and did not see it.** **It was saved by a redesign made
for an unrelated reason — which is luck**, and that is exactly why it goes in the log rather than being filed as
a success. ✅ **Recorded in `SYSTEM-FACTS` as load-bearing:** *if the notification decision is ever unheld, that
key is the first thing to look at.*
📌 **Same lesson as `ob_l_note` serving `booking_confirmed`, from the other direction** — there two callers forced
a second key; here one function let one key serve both. **His sentence, and it is the better statement of it.**

### 🔑 He answered the size question with a number and then made the number irrelevant
**3,848 characters doubled against LINE's 5,000** — measured on the real composer, 20 rows, long Thai names, a
long programme, a note on every row. 🔑 **Then: *"the bound is the CAP, not the data — 40 rows measures the same
3,848."*** ⇒ **the safety is a mechanism, not an assumption about how busy a coach gets**, ✅ **and a 40-row
fixture makes a RAISED CAP fail here.** **Better than the answer I asked for.**

### The children count — asserted per LINE, not per string
*"Asserted the count is on BOTH lines, not that the string contains `(3/5)` — that weaker assertion passes on
the broken version."* ✅ **And the broken shape is written INTO the test**, with the sentence that keeps it:
**the Thai reader never sees the count and nothing about the Thai output looks wrong.**

### Where the work stands
✅ **Conversation: bilingual, five flows, each whole** — plus registration in the customer's own English, their
date order, and a confirm step echoing what the parent typed.
⏸️ **The notification half stays held** on @Porter's three answers — **and tonight showed the hold was closer to
being lost than either of us knew**, so closing them is worth more than it was this morning.
🔴 **The pause shipment (270+271+272) has been ready since the evening** and is now **the oldest thing on the
board**, waiting on @Tanya then @Porter's window.

**BALL: @Jason — TASK-268**, the last item in his queue.

## [--:--] Jason (BE) — ✅ TASK-268 DONE (code): `bunx` is out of the deploy path. My queue is empty. → @Sober

`smart-scheduler-back` · tsc 0 · **1694 pass / 0 fail** across 133 files · 🚫 nothing applied, no database
touched, no migration; the node-walk module check kept — **two different questions, both now asked.**

### 🔴 §3's `node_modules/.bin/drizzle-kit` does not exist on this machine
`.bin` holds **`drizzle-kit.exe`** and **`drizzle-kit.bunx`** — Bun's Windows shims — while POSIX gets a bare
symlink. ⇒ *"resolve the `.bin` entry"* means guessing a per-platform filename, which is one more thing that can
be wrong.
✅ **I read the package's own `bin` field instead:** `"bin": { "drizzle-kit": "./bin.cjs" }` — **authoritative,
one file, identical on every platform**, and what every shim ultimately executes. Run as `bun <path> migrate`.
📌 **Read the FIELD, not the shim's bytes.** The `.bunx` shim happens to contain `drizzle-kit\bin.cjs` and would
have given the same answer today — **but reverse-engineering a shim is the "agrees today" shape this task exists
to remove.**

### Break it and watch — four ways, because there are four ways to fail
`resolveDrizzleKitBin` takes `exists` and `readJson`, so each failure is a case rather than a mutation: no
package · no `bin` field · a `bin` naming a missing file · an unreadable `package.json` (returns `null`, never
throws — *a preflight that crashes is a preflight whose message nobody reads*). Plus the script's refusal branch.
📌 ⚠️ **And one of those four caught a mistake in my own fixture:** `tree({ bin: undefined })` hit the helper's
**default parameter** and got the default back, so *"no `bin` field"* was testing the happy path. Fixed with
`"bin" in opts`, reason on the line. **A test that cannot express the case it names is worse than no test** —
and it took running it to see, which is the week's own lesson landing on me again.

### 🔑 PENDING DEPLOY 8 is unchanged, and asserted
`bun run db:migrate:through 0032_booking_paused_status` · `bun run db:migrate` — both package scripts, `bunx` was
internal, and the only place it surfaced was `--plan`'s *"would run"* line. **@Porter carries nothing new.**
Asserted as an equality on both script bodies, so a future edit to either fails here.

### 🔴 The Question — zero `bunx`, and the third resolution named rather than glossed
✅ Swept `scripts/`, `src/` and `package.json`: **no `bunx` in any command.** Kept as a test over all three
scripts and every `package.json` script.
⚠️ **The honest remainder: `db:migrate` still runs a bare `drizzle-kit migrate`**, resolved by **bun's script
runner** prepending `node_modules/.bin` to `PATH` — a **third** resolution, different from both the module walk
and the `bin` field.
🔑 **Not the same risk, and the difference is the point:** that PATH is **local-only and never network-capable**,
which was the specific hazard `bunx` carried into a deploy step. ⇒ **not changed** — §4 forbids touching those
two commands, and the risk is a smaller, different one.
📌 **But `--plan` does not check it either**, because `--plan` belongs to `db:migrate:through`. **It is the last
unchecked resolution in the deploy path.** If you want it covered that is its own small task — **naming it rather
than letting the sweep sound complete.**

**BALL: @Sober — TASK-268 ready for review. 🔴 My queue is EMPTY.** The pause shipment (270+271+272) is still the
oldest thing on the board, waiting on @Tanya then @Porter's window.

## [--:--] Sober (SA) — ✅ TASK-268 DONE. **The deploy path is closed and @Jason's queue is EMPTY.** 🔴 **The pause shipment is now the oldest thing on the board.**
`tsc` **0** · `bun test` **1694 / 0** (133 files) · **zero `bunx` in any command** · **I ran `--plan` myself** —
exit 0, nothing contacted, scratch folder gone, and it now prints the `bin.cjs` it will execute · **PENDING
DEPLOY 8 unchanged**, asserted as an equality on both script bodies.

### 🔑 He did not build what §3 asked for, and was right not to
**`node_modules/.bin/drizzle-kit` does not exist** — `.bin` holds `drizzle-kit.exe` and `drizzle-kit.bunx` here,
a bare symlink on POSIX ⇒ *"resolving the `.bin` entry means guessing a per-platform filename, a second thing
that can be wrong."*
✅ **He read the package's own `bin` field** (`{"drizzle-kit": "./bin.cjs"}` — I checked it): authoritative, one
file, identical everywhere.
🔑 **And the sentence that makes it a decision:** *"I read the field rather than the shim's bytes —
reverse-engineering a shim is exactly the 'agrees today' shape this task exists to remove."* **He refused to
solve the task with the technique the task exists to eliminate.** I did not put that in the task.

### 🔴 The third resolution — RULED no task, and the reason is structural
`db:migrate` runs a bare `drizzle-kit migrate` through bun's script runner (`node_modules/.bin` on `PATH`).
**He named it rather than letting the sweep sound complete, which is why it could be ruled on at all.**
⇒ **The `.bin` shim is GENERATED from the same `bin` field `--plan` now reads.** **One fact seen twice, not two
facts that happen to agree** — and this project's rule has always been about *independent* facts that can
disagree tomorrow. **A derived value cannot drift from its source.**
✅ His own point kept: the script-runner `PATH` is **local-only and never network-capable** — the specific hazard
`bunx` carried — so its failure is **loud and local**, never a silently different version.

### ⚠️ His fixture bug is the THIRD of that shape today, and he found it himself
`tree({ bin: undefined })` hit a **default parameter**, so the *"no `bin` field"* case was testing the happy
path. ⇒ **a test that cannot express the case it names is worse than no test — it is a green light with a label
on it.**
📌 **Three today:** the `size: 6` / `confirmed: 6` fixture · my malformed 11th card failing `tsc` for the wrong
reason · this. **All three were visible only to somebody who checked WHY the result came out that way.**

### 🔴 Where the whole board now stands
| waiting on | what |
|---|---|
| **@Porter** | **the pause shipment (270+271+272), ready since the evening**, after @Tanya · the `ภาษา` ก/ข · the three notification-value answers · the report count · the `PENDING_RESCHEDULE` word |
| @Fern | TASK-274, one line |
| **me** | TASK-279 — `REQ-079` swept, `REQ-076 · 077 · 082 · 083` to go |
| @Jason | **nothing** |

⚠️ **I am not pressing for the window** — holding it under @Tanya's round was @Porter's call and a good one.
**But the queue behind it is empty now**, so the wait has stopped buying parallel work.

**BALL: @Porter.**

## [--:--] Porter (PM) — 🟢 THE MONEY SCARE IS OVER. It is the design, and the evidence is my own file.
@Sober ruled `REQ-083`'s ledger finding **not a defect**, citing **`SYSTEM-FACTS.md` line 329 — my own entry from
2026-08-23:** course, voucher and rental revenue post **AT SALE**; `FIRST_TRIAL`/`SINGLE_SESSION`/`OTHER` only at
day-end once `ATTENDED`; **attending a course session posts NOTHING, deliberately** — posting again would
double-count money the family already paid.
⇒ **On a box whose bookings are course sessions, "money enters only as a package SALE" is the design.**
📌 **The answer was in a file I wrote two weeks ago, and neither @Tanya nor I looked.** **That is what the file is
FOR** — and it is the second time tonight the repo already held the answer *(the board's "course & voucher revenue
was never recorded" row was the first)*.
🔴 **And he corrected an inference of hers that mattered more than the conclusion:** `postBookingSale` writes
`refType: "SALE"` with `refId` = the **booking** id ⇒ **a `rev:` movement IS a `SALE`; `refType` cannot
discriminate.** **Her conclusion may still be right — it is not yet measured.** ⚠️ **Told her before she reports
it**, and warned her that her overnight experiment **can return a FALSE zero** if the programme has no price
group. *(The `bo.item` half is satisfied — the owner ran `sale:ensure-items` twice today.)*
⇒ **The open question is now narrow and answerable from data she has: has `end-of-day` EVER run on `sid`?**
`job_runs where job='end-of-day'` — **with my own caveat that `db:reset` wipes it, so empty proves nothing.**
🟢 **Nothing here blocks the `uat` deploy.** **The condition predates this release and we did not make it worse.**
