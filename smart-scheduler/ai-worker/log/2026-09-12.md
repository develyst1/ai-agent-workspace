# 2026-09-12 — smart-scheduler

## [--:--] Porter (PM) — 🆕 `REQ-088` written, NOT dispatched: **registration by LINK, not by typing.** The customer reports parents do not follow the instruction to type `สมัคร`.
🔑 **The finding under the request matters more than the request:** **the 8-screen chat registration finished
four days ago has a failure mode no copy fixes — it needs the parent to TYPE A WORD, and they will not.**
⇒ **We spent a week getting the WORDS right; the problem was the ACT of typing.** 🔻 **Not the customer changing
their mind. The customer reporting that the surface does not work with their users.**
**Their flow: tap link → allow LINE connect → enter phone → family found: tap child to link / none: fill "the
form ขวัญ sent" → done.** 🔑 **Steps 2–3 are the whole change (a tap and a field replace three typed words);
4a/4b are the SAME branches the chat flow already has.**
🔴 **Two things I do not have and cannot shape without:** **the NEW-parent form ("ที่ขวัญส่งให้") — never seen** ·
**whether the CHAT flow stays** (my reading: yes, marked as mine).
⚠️ **Named the cost before anyone hears "just a link": a new surface, on the customer's OA, needing a LINE
Login / LIFF app registered in THEIR console — a setting only they can make, same class as the webhook URL.**
🚫 **Not dispatched, not sized — the owner sends batches as one.**

## 2026-09-12 — Sober (SA Lead)

### 🔴 `§17g` — I re-ran the checks on the owner's tree, as @Porter told me to, and his edit cost two things
**Commit `baa6015`: he COMMENTED OUT both triggers of the unsolicited welcome** — the `follow` dispatch
(`handleFollow` is now dead) and the unlinked-postback fallback at `:1533`. ✅ **The ruling is met and the
`สมัคร` keyword still works** (the vocabulary test passed).
🔴 **Re-run: 2053 pass / 1 FAIL** — a bilingual SOURCE PIN asserting the welcome is sent via `tb()`; **its
claim is now vacuous because `welcome` is never sent.** *A pin that outlived its line, not a behaviour
failure — but the suite is RED on his tree.*
🔴 **And the real one: that line did TWO JOBS.** It sent the welcome **and it was the guard** keeping unlinked
users out of the customer flow. ⇒ **an unregistered parent tapping `เช็คอิน` / `นักเรียน` now falls through to
`doCheckin` / `doChildren` and is told "you have no classes / no children" instead of "please register".**
✅ **Verified each landing.** 🚫 **Not a crash; a wrong reply, on the deployed tree.** 🔑 **Fix-forward is one
line — keep the guard, drop the send — and it is HIS edit, so it is @Porter's call who does it.** 🚫 **I
touched nothing. Logged verbatim, as he asked.**
📌 ***A line that does two jobs cannot be commented out to remove one of them.***

### 📐 `REQ-088` — registration by LINK — SHAPED AND SIZED, one message, nothing built
**Owner:** *"ลูกค้าของลูกค้าไม่ค่อยทำตามคำแนะนำแอดมิน คำสั่ง สมัคร เลยอยากให้เปลี่ยนเป็นกดลิ้ง"*. 🔑 **@Porter's
reframing stands: the 8-screen chat flow has a failure mode no copy fixes — it needs a parent to TYPE a word.**
✅ **From the code: the LOGIC exists; the SURFACE does not.** `verifyAndLink` · `bindFamilyLine` ·
`createStudentFromLine` (the ONE writer) · `afterParentLink` · TASK-313/314's guards — all present.
🔴 **What does NOT exist: any LIFF / LINE Login — zero hits.** ⇒ ***the page's one job is to turn a tap into a
`lineUserId`; from there it calls what the chat calls.***
⚠️ **The honest cost is the WRAPPING, not the page:** the decision lives inside the webhook service wrapped in
reply-tokens and session steps ⇒ **the TASK-315 extraction, once more** — *and if the page grows its own copy
of any rule, it is wrong.*
**Size: FE M (`/register`, the sibling of `/checkin`) + BE M (verify the LIFF ID token, then the extracted
decision) · NO migration · customer: ONE LIFF app, three values back.**
✅ **His item-3 correction (admin sends ONE stable URL) removes a whole class of cost** — the LIFF login
identifies the parent, not the link; no token table.
⚠️ **Named for the page: 2FA must honour the same setting or it is a second door that ignores it; the page
must not leave a chat session step set; its copy is a PLACEHOLDER until the customer sees it.**
🚫 **Nothing built. Nothing cut. The chain stays stopped.**

⏱️ **State: `uat` deployed by the owner · suite RED on his tree (1 pin) · the `§17g` guard regression with
@Porter · `REQ-088` sized and with @Porter · both engineers EMPTY.**

## [--:--] Porter (PM) — 🔴 The owner's own `§17g` edit (commit `baa6015`) removed a GUARD along with the send. **Suite red (1 vacuous pin); unregistered users fall through into the customer switch.**
**He commented out `if (linked !== "customer") return send(replyToken, [textReply(tb("welcome"), lang)])` —
one line, two jobs.** ✅ **The send is gone, as the customer asked.** 🔴 **So is the guard: an unregistered parent
who types `เช็คอิน` or `นักเรียน` now gets `ไม่มีคาบ` / `children_none` instead of "please register".** @Sober
verified each landing. **Not a crash — a WRONG REPLY to an unregistered person.**
🔑 **Fix-forward is one line: keep the guard, drop the send.** ⚠️ **It is HIS edit, so I am asking him who
does it, not assigning it.** 📌 **Exposure today is limited — the customer's rich menus are removed from their
OA — but a typed command reaches it.**
✅ **`REQ-088` sized: M (FE) + M (BE) · NO migration · ONE LIFF app on the customer's side (they create a LINE
Login channel on the same provider, add a LIFF app pointing at our page, send us the LIFF ID + channel ID).**
🔑 **The logic all exists** — `verifyAndLink`, `bindFamilyLine`, `createStudentFromLine`, `afterParentLink` —
**the page's one job is to turn a tap into a `lineUserId`.** **The honest cost is not the page: it is
extracting the link/create DECISION out of the webhook's reply-token wrapping, once, so two doors reach one
writer** — the `TASK-315` shape again.

## [--:--] Porter (PM) — ▶️ Owner: **GO on `REQ-088`.** Dispatched with the guard fix as item 0.
🔑 **Asked for the LIFF endpoint URL EARLY — before the page exists** ⇒ **the customer's 5 console clicks run
in parallel with our build, not after it.** 📌 **The only external dependency in the REQ, moved to the front.**
✅ **Copy rule set: reuse `§17c`'s approved sentences wherever a form field matches the chat field; anything new
is a PLACEHOLDER the owner takes to them.** 🔑 **A form label is the same sentence in a smaller box.**
🔻 **The guard fix goes to @Jason — the owner did not claim it when asked and said "go".** **Read as "the team
does it"; logged against his commit so the diff has a task behind it.**

### ▶️ OWNER: GO on `REQ-088`, and fix the `§17g` guard first — THREE TASKS CUT
**@Porter's reading of the owner's "go" with no claim on the guard: *the team does it*.** ✅ **TASK-346 → @Jason,
FIRST, one line — keep `if (linked !== "customer") return …`, drop only the `welcome` send, rewrite the vacuous
pin, log the diff against `baa6015` so the record shows a task behind the diff.** ⏱️ **Ships in the next
deploy; not held for `REQ-088`.** ❓ *The one decision in it: what the guard REPLIES — my reading is `§17c`
screen 1's text, which is SOLICITED because they tapped.*

**`REQ-088` cut as shaped — M + M, no migration:**
- **TASK-347 (BE, @Jason, after 346):** the link/create DECISION extracted ONCE from the webhook's reply-token
  wrapping — TASK-315's shape, again — with the chat asserted BYTE-IDENTICAL after; **the LIFF ID token
  VERIFIED server-side against the customer's Login channel ID** (zero LINE Login code exists today; *a page
  that trusts the client's claim is TASK-047 by a new route*); `line_parent_2fa` READ; no dangling chat step;
  public routes beside `/checkin` where the token IS the credential. 🔑 **THE CONTRACT FIRST**, so @Fern
  builds in parallel.
- **TASK-348 (FE, @Fern):** `/register`, sibling of `/checkin` — LIFF init → ID TOKEN → the API; ***the page
  owns WORDS, the server owns DECISIONS*** — no rule on the page, by absence; copy is @Porter's, marked
  PLACEHOLDER, pinned by FORM, listed for one customer visit; `found` renders a child list + an ADD action,
  never a forced form (`§6.1`). **Shell and LIFF init now; the rest when the contract lands.**
🔑 **@Porter's six rules are in both DoDs as assertions, rule 1 verbatim:** ***the page calls the SAME writer
the chat calls; if the page grows a rule of its own, it is wrong.***

### 🔗 THE URL, sent to @Porter immediately so the customer's console clicks run TODAY
**Path `/register`; origin = the same `PUBLIC_CHECKIN_BASE_URL` every check-in link already resolves through**
(`checkin-token.ts:37`) — **the owner set it, it is in his env.** ⇒ **LIFF endpoint =
`<PUBLIC_CHECKIN_BASE_URL>/register`.** ✅ **The exact console clicks are in TASK-347 so they match what the
code will verify: a Login channel on the SAME provider · a LIFF app with that endpoint, scope `profile` · TWO
values back — the LIFF ID and the Login channel ID.** ⚠️ *Told him the channel ID is not optional: it is what
lets us VERIFY the token rather than trust it.*

⚠️ **Flagged to @Porter consciously: TASK-347 is the SECOND door built onto a writer extracted for ONE** —
the chat's decisions move out, its replies stay, and its byte-identity is asserted rather than assumed ⇒ *if
@Tanya has a window before the next deploy, the chat's 8 screens are the thing to walk.*
🚫 **Nothing else cut. The chain stays stopped.**

⏱️ **State: @Jason on TASK-346 then 347 · @Fern on TASK-348's shell · the owner's list still with @Porter ·
the picker/heading boundary still open.**

## 2026-09-12 — Jason (BE)

### ✅ TASK-346 — the `§17g` guard: keep the check, drop the send — **DONE (code)**
**2061 pass / 0 fail**, 167 files (+1) — 🔑 **GREEN on the owner's tree, from RED 2053/1.** tsc 0 ·
🚫 no migration (**35 = 35**) · no FE, no key, no copy. **Net source change: ONE line uncommented.**

### 📌 The diff against `baa6015` — because a diff against the owner's commit must show a task
His `follow` removal — 🚫 **left exactly as he made it.** His postback guard `:1533` — ✅ **restored, BOTH jobs,
with a comment at the line naming his commit, both jobs, and the restore.** The bilingual pin that went
vacuous — ✅ **rewritten, not deleted, and the vacuity is named in it.**
🔑 ***A line that does two jobs cannot be commented out to remove one of them*** — **and the test that would
have said so now exists: one file, ONE ASSERTION PER JOB.** *The gate is asserted over CODE-STRIPPED source,
so his commented-out line does not satisfy it — that is the assertion that was missing when he edited.*

### ❓ The one decision, and the alternative named so it is visibly rejected
✅ **The guard replies with `welcome` — which IS `§17c` screen 1's text — in reply to a TAP, and a reply to a
tap is SOLICITED.** The ruling was *never unsolicited*, not *never*.
⚠️ **@Porter's *"drop only the `welcome` send"* read literally gives a SILENT gate.** Built as mutation B and
rejected: his own next sentence is *"told to register again"*, and **a silent refusal of a tap is AC-16's
stray-text silencing applied to a person who just PRESSED A BUTTON** — an empty reply to a button reads as a
broken bot. 📌 *One-token change if he meant the silent gate; the intent as written points the other way.*
✅ **`handleFollow` stays DEAD with the reason above it** — the ruling is the customer's and may move again.

### 🔑 Mutation — the OWNER'S EDIT, re-applied, now fails THREE tests
**A · his edit re-applied ⇒ 3 fail** · **B · the silent gate ⇒ 4 fail** · **C · `follow` re-dispatched ⇒ 1
fail.** All restores byte-identical in a `finally`. 📌 **A is the point of the whole task: the exact edit that
shipped on his tree now fails three tests instead of one vacuous pin going quiet.**

### 🎯 THE QUESTION — *other `return`s that both REPLY and GATE?* One real instance, and its pin would not catch it
🔑 **17 lines in the webhook have the shape; sorted by what commenting one out would DO, because that is what
decides whether anyone would.**
🔴 **THE ONE THAT MATTERS — the SUSPENDED-HOUSEHOLD check (TASK-048), in TWO places** (`handleMessage` and
`handlePostback`). ⚠️ **A person told *"stop sending the suspended notice"* comments one out — and a suspended
household can use the bot again.** *Same shape, same silence, same wrong reply.*
🔴 **AND ITS PIN WOULD NOT CATCH IT:** `line-menus-flows.test.ts:289`, named *"a suspended household is still
refused every postback"*, is `toContain` over the **RAW, UN-STRIPPED, WHOLE FILE** ⇒ ***comment out either site,
or BOTH, and it stays green.*** 📌 ***It is the `welcome` pin's vacuity, already in place, waiting for the same
edit.*** 🚫 Not fixed — named. *Size: one test, ~ten lines.*
⚪ The `!x` guards fail by CRASH (loud — not on the list) · 🟡 the empty-list guards render an empty menu
silently but nobody removes a "no items" reply (shape matches, motive does not) · ⚪ the `เมนู` replies lose
nothing but the menu.
🔑 ***The pattern under both: a refusal that is also a message. When the message is the thing someone wants
to change, the refusal is what they will accidentally remove.***

⏱️ **Next: TASK-347's CONTRACT, to @Sober first, so @Fern starts in parallel.**

### 📜 TASK-347 — THE CONTRACT delivered to @Sober before the extraction (for @Fern via him)
🔑 **Three principles: the server returns CODES, never a `message` · the page NEVER says who it is (`idToken` on every call, `sub` is the user, no ids in any body) · every decision is the chat's function CALLED, named beside its code.**
🔴 **The interesting token case is separated on purpose: `TOKEN_WRONG_CHANNEL` — `aud` decoded locally and compared to `LINE_LOGIN_CHANNEL_ID` BEFORE calling LINE — logged loudly, because a page on ANOTHER channel pointed at our API is a misconfiguration or an impersonation, not a typo.**
🔑 **`lookup` writes nothing · `link` binds (re-checks every refusal at the write, never trusting the lookup; creates the parent for a NEW phone; links the rich menus; clears any chat session — Rule 4) · `create` runs the chat's guards in the chat's ORDER (the cap FIRST) into the ONE writer `createStudentFromLine`.**
⚠️ **Two decisions flagged for @Sober: `/link` creates the parent so `/create` has one meaning; and `birthDate` is the customer's `DD-MM-YYYY` text through the chat's own `parseBirthDate` — which refuses ISO on purpose — rather than ISO through a second validator.** 🔑 *The one place the contract could have grown a second rule, and does not.*
⏱️ **Extraction next; it does not wait on the routes' shape.**

### ✅ TASK-346 DONE and REVIEWED — @Jason. **2061/0 (167 files), GREEN on the owner's tree from RED. Re-run by me.**
**ONE line uncommented — both jobs back.** The guard gates unlinked users out of the customer switch AND replies
with `§17c` screen 1's text, **solicited because they tapped.** ✅ **He built the silent-gate reading of
@Porter's "drop the send" as a mutation and rejected it** — *an empty reply to a button reads as a broken bot*
— **and I confirmed to @Porter that his NEXT sentence ("told to register again") is the ruling.**
✅ **`handleFollow` DEAD BY RULING, KEPT BY DECISION**, with the reason and *"do not re-dispatch without a
task"* asserted. 🔑 **The assertion that was MISSING when the owner edited now exists — the guard over
CODE-STRIPPED source — so his exact edit re-applied fails three tests instead of one pin going quiet.**
📌 *The diff against `baa6015` is in the code and the report: the record shows a task behind it.*

🔴 **His Question found the SUSPENDED-HOUSEHOLD refusal has the same shape at two sites, and its pin checks the
RAW WHOLE FILE — comment out either or both and it stays green.** ***A refusal that is also a message: when the
message is the thing someone wants to change, the refusal is what they will accidentally remove.*** ✅ **Named,
not fixed — on the owner's list, sized one test / ten lines.** *He sorted all 17 such lines by MOTIVE, not just
shape; this is the only one where both match.*

### 📜 TASK-347's CONTRACT accepted as written — forwarded to @Fern by POINTER, not re-typed
**Three principles: codes not words · the ID TOKEN is the identity and no body carries an id · every decision
is the chat's, called not copied.** ✅ **Both of his decisions taken as he preferred:** `/link` creates the
parent so `/create` has ONE meaning; `birthDate` is `DD-MM-YYYY` text through the chat's parser, ISO refused.
🔑 **Named as right so they are not re-argued:** `TOKEN_WRONG_CHANNEL` distinguished by a LOCAL decode trusted
for nothing but the error code · `LINE_BOUND_TO_OTHER_FAMILY` pre-checked on `lookup`, RE-CHECKED at the write ·
no `lineUserId`/`parentId`/`familyId` in any body. ⚠️ **One thing to CHECK during extraction: the chat's order
of `linkRoleRichMenu`/`linkKnownRichMenu` vs `clearSession`, so a page-registered parent lands in exactly the
chat's end state.**
🚫 **I forwarded the contract to @Fern as a POINTER to `§C0`–`§C5`** — *a task that re-transcribes a spec is
how I compressed `§16d` and broke a pin.*

⏱️ **State: @Jason extracting (TASK-347) · @Fern on the page (TASK-348) · the suspended-household pin on the
owner's list · the copy list forming for one customer visit.**

### ✅ TASK-347 — @Jason · `REQ-088` back end: the decision extracted ONCE, and a page that can prove who it is — **DONE (code)**
**2086 pass / 0 fail**, 169 files (+2) · tsc 0 · 🚫 **no migration (35 = 35)** · no FE · codes only, no copy.
**New:** `line-register.service.ts` (the one home of the registration decisions) · `line-id-token.ts` · `routes/register.ts` · two test files. **Changed:** the webhook keeps its REPLIES and loses its DECISIONS; one mount line; 14 pins in 7 files followed the decisions, all rewritten.
🔑 **RULE 1 held by ABSENCE on both doors** — thirteen decision strings are in the home, not in the chat, not in the page. ⚠️ **One exception NAMED in the test:** the chat still CALLS `isReservedWord`/`assertCanAddStudent` at its name step (same functions, not copies; it asks one step at a time) — so the ORDER is asserted equal on both sources instead.
✅ **The chat is BYTE-IDENTICAL:** every pre-existing reply pin green unmodified; the five `verify_parent_*` keys map one line each from the extracted outcomes.
🔴 **RULE 2:** `TOKEN_WRONG_CHANNEL` decided from a local `aud` decode BEFORE calling LINE and logged loudly; nine unit tests with a fake LINE — including *our-`aud`-but-LINE-refuses is INVALID*, which keeps the local decode from ever becoming the verifier.
🔻 **TWO BUGS CAUGHT BY THE TESTS BEFORE MUTATION:** `parseBirthDate("")` is a REFUSAL (only the word `ข้าม` is a skip) — the first composition would have refused every page create without a birthdate; **and the first `linkFamilyByPhone` had a guard the chat does not** (refusing an already-bound account on a NEW phone). Mirrored exactly — ⚠️ **and the gap NAMED for @Sober:** an account bound to family A entering a new phone creates an orphan parent B today, on the chat; fixing it in the home would change the chat. *His to cut.*
🔑 **Seven mutations, `finally`, checksum — all bite.** `§17c` by name: two files diffed, neither a `§17c` pin.
🎯 **THE QUESTION — *what is still a DECISION in the webhook?* Not "replies only":** the admin code compare · the teacher claim routing · **the two-strikes rule — and the page has NO strikes, a real difference between the doors, named not papered** · **`afterParentLink`'s *children ⇒ no forced add*, rendered on two sides from one fact — the softest point** · the guard ORDER held by assertion · who-is-talking. 📌 ***The line: every decision BOTH doors need is in one place; every decision that is a property of the CHAT SURFACE stays in the webhook. Recorded as a LINE, so the next door is asked "does the new surface need this?" rather than "is it in the webhook?"***
⏱️ **@Fern can build TASK-348 against §C0–§C5 as accepted; the contract did not move during the extraction.**

### ✅ TASK-347 DONE and REVIEWED — @Jason. **2086/0 (169 files), 35 = 35, tsc 0, re-run by me.**
**Verified:** `/api/register/{lookup,link,create}` mounted BEFORE the JWT guard beside `publicCheckin` · two
env lines, both the customer's, neither optional · **zero** decision strings in the webhook · the local-decode
caveat verbatim in `line-id-token.ts`.

🔑 **Rule 1 held by ABSENCE on both doors, with the one exception NAMED:** `isReservedWord` and
`assertCanAddStudent` still called from the chat's name step — the SAME functions — because the chat asks one
step at a time; **the guard ORDER is asserted equal in both sources.** *"Held by assertion, not by construction
— the seam is visible."* 📌 *A seam you can see is one someone can check.*
✅ **Byte-identical, proven the right way: every pre-existing reply pin green UNMODIFIED**, and the two
non-reply differences DECLARED.

🔴 **Two catches before the mutation stage:** `parseBirthDate("")` is a REFUSAL, not a skip — every page create
without a birthdate would have been `BIRTHDATE_INVALID` (fixed; told @Fern to OMIT the field). **And he added a
guard the chat does not have, REMOVED it to match the chat, and NAMED the gap:** *an account bound to family A
entering a NEW phone creates an orphan parent B.* ✅ **Mirrored per Rule 1, recorded in the code, on the
owner's list — not fixed, because fixing it changes the chat and the chain is stopped.**

### 🎯 His §8 is the rule for the third door, and I recorded it verbatim
I asked whether the webhook was now "replies only". **He refused to round it and listed SIX decisions that
remain, each with its reason.** ***"Every decision BOTH doors need is in one place; every decision that is a
property of the CHAT SURFACE stays in the webhook. Ask 'does the new surface need this decision?', not 'is it
in the webhook?'"*** 📌 **And the difference stated rather than papered over: *a chat that hands over to a
human after two failures is a property of the chat surface; a page can be retried forever.***
**Owner's list +2: the orphan-parent gap (one check, both doors) · `mustAddChild` from the home (one line).**

⏱️ **State: @Fern building the page against the LIVE route — the last thing between `REQ-088` and done ·
@Jason EMPTY · the owner's list at 13 with @Porter.**

## [--:--] Fern (FE) — ✅ **TASK-348 done — `/register` is built against the LIVE route, holds no rule, and two mutations prove it.** 🔻 **`LIFF_ID` cannot be the env var name.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  285 pass / 0 fail   (was 245; +40, one new file)
bun run build     → ok — `○ /register` beside `○ /checkin`, both static public pages
```
🚫 No BE change · no migration · `/checkin` and `contract.ts` untouched (asserted). ⚠️ **One new dependency:
`@line/liff@2.31.0`** — LIFF init cannot be done without it; imported dynamically so no other route carries it.

### 🔴 RULE 1 and the identity rule — both held by ABSENCE and both PROVEN by mutation
No reserved list, no duplicate logic, no cap, no date parsing, no phone normalising — on the page AND the API
module, asserted. **The one branch that looks like a rule (`children.length === 0 → form`) is the §6.1 fact
READ off the response**, not derived; asserted by name.
✅ **Mutation 1 — the page grows a client-side cap of 5 ⇒ 1 fail.** ✅ **Mutation 2 — a body carries `userId`
⇒ 1 fail.** Both in one call, both restored byte-identical by `md5sum`.
`TOKEN_EXPIRED` ⇒ re-init and retry ONCE — no loop, and `TOKEN_WRONG_CHANNEL` is never retried. Asserted.

### 🔻 The env var is `NEXT_PUBLIC_LIFF_ID`, and it could not have been `LIFF_ID`
**A Next.js client bundle only sees `NEXT_PUBLIC_*`.** A bare `LIFF_ID` would have looked absent on every
environment forever — the page would have rendered its "not set up" message with a correct value sitting in
the env. Same shape as `NEXT_PUBLIC_API_URL`, which `/checkin` reads. Documented in `.env.example`, with the
note that the server's `LINE_LOGIN_CHANNEL_ID` is separate and the page never sees it.
✅ **Absent ⇒ a MESSAGE, not a blank** — decided before the SDK is even loaded. Asserted.

### ✅ 16 codes, each rendered in both languages — pinned to the ROUTE's table
`REGISTER_CODES` equals `routes/register.ts`'s `REFUSAL` table plus the four token failures. 📌 `keys.test.ts`
cannot see a templated `t(\`register.code.${code}\`)`; this pin is what covers it. 🚫 No server `message` is
rendered anywhere — the API module does not contain the word.

### 🔴 The date — text, echoed, omitted when blank
A `TextInput` with a mask — **no picker, no ISO.** 🔑 **TASK-277: the confirm screen echoes the date exactly
as typed** (§17c 7a/7b as a screen). 🔴 **A blank is the SKIP and the key is OMITTED, never `""`** — TASK-347
§5.1's trap, not re-created on this side; asserted both in `api.ts` and at the call.

### 📝 Copy — 21 VERBATIM (pinned byte-for-byte) · 2 ADAPTED (change named) · 2 borrowed · **29 PLACEHOLDER**
The 29 are listed in the TASK, each tagged at its line. ⚠️ **The ones @Porter most needs to write:** the phone
button (*"Next"* was typed), the link tap (`foundConfirm`), the add-a-child button, the confirm/back buttons,
the four `TOKEN_*` sentences, and **`FAMILY_FULL` in English** (the Thai is the server's own sentence,
verbatim). 🚫 Nothing invented without the mark.

### ✅ Verified, not assumed
`proxy.ts` matches only `/scheduler/:path*` ⇒ `/register` is public by construction, exactly as `/checkin`.
The request shapes were read from the shipped route; the contract matched it.

### 🔑 Your Question — **share the credential, not the shell**
I lean where you do, and from having built both I can say why: **the two pages duplicate ~15 lines** (the
`API_BASE` derivation — ⚠️ *a value copied into two places* — a `Paper` wrapper, a loader view). A shell saves
those and costs a coupling: **a shell change for one page deploys the other, and these are the two pages on a
parent's phone with nobody in front of them.** 🔑 **And they differ in the one thing that matters — the
credential** (query token vs LIFF ID token). A shell that abstracts that hides each page's security model.
**A third is likely — `REQ-062`, leave from LINE, the highest-frequency parent act — and if it arrives on
LIFF, the thing to share is `lib/register/liff.ts`, the CREDENTIAL module, which already has no page in it.**
🚫 Named, nothing built.

### ⚠️ Not seen on a screen — and this one genuinely cannot be, from here
**A LIFF page yields an ID token only inside the LINE app, against a real LIFF ID and channel.** No mock for
that boundary, and I did not fake one. ⇒ **@Tanya on `sid`, from a phone with the customer's LIFF ID:** known
number → children listed → tap links · new number → form → `DD-MM-YYYY` echoed on confirm → done · blank date
⇒ no `BIRTHDATE_INVALID` · duplicate name ⇒ the detail hint, resubmit works · `NEXT_PUBLIC_LIFF_ID` removed
⇒ a message, not a blank.

**Ball: @Sober** — review, the 29 placeholders to @Porter, and the env var name for whoever sets `sid`.
`BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.

### ✅ TASK-348 DONE and REVIEWED — @Fern. **285/0, tsc 0, build ok, re-run by me.** 🟢 **`REQ-088` IS COMPLETE END TO END.**
**Verified:** Rule-1 strings absent from the page and the API module (the only hits are the comments saying
so) · identity strings absent from code · `proxy.ts` matches only `/scheduler/:path*` ⇒ `/register` public by
construction · `NEXT_PUBLIC_LIFF_ID` in `.env.example` · `@line/liff@2.31.0` in `package.json`.

🔻 **She corrected my env name and it would have been invisible forever:** **a Next.js page only sees
`NEXT_PUBLIC_*`** ⇒ *a bare `LIFF_ID` would have looked absent on every environment, and "not configured" would
have been the page's only path.* 📌 **Seventh correction of my text this batch — and the first that would have
shipped a page that never worked.** *Told @Porter with the corrected name in a table, and put the BE's stale
`.env.example` comment on the list.*

✅ **Rule 1 held by ABSENCE and PROVEN by mutation** — and the one branch that looks like a rule
(`children.length === 0 → form`) is `§6.1`'s decision READ OFF THE RESPONSE, asserted by name so nobody
"moves the rule to the client" later. ✅ **The ID TOKEN is the identity, proven by mutation.** ✅ **The date: a
masked `TextInput`, echoed back on confirm (TASK-277), OMITTED when blank, `done` shows the server's echo** —
*four places the page could have grown a date rule, and it grew none.*
📝 **Copy split exactly as the rule asks: 21 verbatim (byte-pinned — the customer approved them) · 2 adapted
with the change named · 2 borrowed · 29 PLACEHOLDER pinned by FORM** — @Porter's single customer visit.

### 🎯 Her Question answer: *share the CREDENTIAL, not the shell*
**~15 duplicated lines against a coupling where a change to the shell for one page is a deploy of the other —
on the two pages parents open with no admin in front of them.** 🔑 ***They differ in the one thing that
matters — the credential — and a shell that abstracts that hides each page's security model, which is the
wrong thing to hide.*** **She named `REQ-062` as the likely third, and `lib/register/liff.ts` — already
page-free — as the thing to share.** 📌 Recorded; nothing built.

### ⚠️ The honest limit, hers first: the one test nobody can run from a desk
**A LIFF page yields an ID token only inside the LINE app, against a real LIFF ID and channel — no mock, none
faked.** ✅ **Her five-step phone script went to @Porter as written**, for @Tanya on `sid` after the customer's
two values are set. 🔑 **The customer's console is now the critical path, not us.**

⏱️ **State: `REQ-088` complete from our side · both engineers EMPTY · the owner's list at 16 with @Porter ·
the chain held through the whole of `REQ-088`.**

## [--:--] Porter (PM) — 🟢🟢 **`REQ-088` COMPLETE end to end** (BE 2086/0 · FE 285/0 · no migration). **The guard (`TASK-346`) is in — suite green on the owner's tree from red.** ⏱️ **The customer's console is now the critical path.**
🔑 **The page holds NO rule — asserted by absence AND proven by mutation (a client-side cap fails a test).**
**The server holds every decision once; the chat is byte-identical.** ✅ **Exactly the DoD I set.**
🔻 **@Fern caught a config error of @Sober's that would have been invisible forever:** a Next.js page can only
read `NEXT_PUBLIC_*` variables — **a bare `LIFF_ID` would have shown "not configured" on every environment no
matter what the owner set.** ⇒ **the value is `NEXT_PUBLIC_LIFF_ID`.** 📌 **Caught before the owner typed it.**
⚠️ **The ONE test needs a PHONE, inside LINE, against a REAL LIFF ID — there is no mock for that boundary and
@Fern did not fake one.** ⇒ **@Tanya cannot start until the customer's five clicks are done and both values
are set.** 🔑 **We are no longer the critical path; the customer's console is.**
📝 **29 PLACEHOLDER strings are MINE to write** — titles, buttons, the "found your family" heading, the done
screen, one line per error. **21 are verbatim from `§17c`, pinned. Their answer is 29 strings and nothing else
moves.**

## [--:--] Porter (PM) — ✅ Owner set `NEXT_PUBLIC_LIFF_ID` and `LINE_LOGIN_CHANNEL_ID`. **Values NOT recorded here** — they are not secrets, but the standing rule is no credential-shaped value in a tracked file, and the rule is cheaper to keep than to argue.
📌 **Advised the LIFF settings: Add friend = On (Normal) · Scan QR = OFF · Module mode = OFF** — the screenshot
had the last two ON; Module mode ON changes LIFF behaviour and is for embedding in third-party apps.
📱 **@Tanya dispatched for the phone round via `adb` — holding for the `sid` deploy.** 🔑 **First real use of
the phone rig; the boundaries in `QA.md` are the whole reason it is allowed.**

## [--:--] Porter (PM) — Owner: **build `§7a`/`§7b` first, test once when they land.** Rounds 3–4 paused on his own call.
🔑 **He applied my own rule to me — a build that changes under a tester wastes the round.** ✅ **Bundled the
two family-screen copy fixes into the same deploy so his one test covers everything.**
📌 **Delegated `§7b`'s escape-hatch decision to @Sober rather than hold him on a dropdown detail** — **a sane
default beats a PM answer that arrives after the build.**
