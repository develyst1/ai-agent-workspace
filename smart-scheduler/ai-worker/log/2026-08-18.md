# Log — 2026-08-18 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Porter (PM) — ⏰ DATE SETTLED = 2026-08-18. This file is canonical. `0018` applied + verified. → @Sober @Tanya
**The human confirmed: today is 2026-08-18.** So per PROTOCOL (settle TODAY from the clock, never from the newest
filename) **this file is where every role writes from now on.** `2026-08-16.md` and `2026-08-17.md` are closed
history — nothing in them gets rewritten, and the decisions stranded there were already restated in 08-17.md's
Porter entry, which is why I am not re-copying them a third time.

**How the split happened, on the record:** I opened `2026-08-16.md` at session start when my own session context
already said otherwise. Jason and Fern then correctly rolled to `08-17` at midnight, and Sober correctly stopped
the loop by converging everyone into one file rather than adding a fourth guess. **The only actual error in the
chain was mine, at the start.** Three roles then spent effort routing around it. The rule exists precisely because
this is cheap to get wrong and expensive to unpick.

## [--:--] Porter (PM) — 🟢 `0018` migration applied and verified GREEN. One question before it goes further.
Owner ran it. Results, verbatim:
- `db:backup` → `sm-prod-backup-2026-08-18T14-21-48-407Z.dump` (**0.1 MB, non-zero** — the AC-8 gate the board has
  been carrying since four 0-byte "backups" on 08-11).
- `db:migrate` → applied cleanly. The three NOTICEs are all benign idempotency messages (`schema drizzle already
  exists`, `ledger table already exists`, `constraint … does not exist, skipping` — that last one is the
  DROP-IF-EXISTS before the FK is created, exactly the shape Jason wrote).
- `db:verify` → **Journal 19 · ledger 23 rows · 19 schema witnesses · "every migration is recorded in the ledger
  AND witnessed in the schema."** (The ledger carrying more rows than the journal is the expected residue of the
  08-11 witness-seeding recovery, and the verifier accounts for it.)
- `SELECT count(*) FROM course_packages WHERE subject_id IS NULL;` → **0.** So the conditional `SET NOT NULL`
  fired, the back-fill covered **every** course, and no course was left underivable. ⇒ **TASK-140 is proven on a
  real database**, and REQ-054's data half is done.

🔴 **@Sober — the one thing I do not know, and will not assume: WHICH environment that ran against.** The owner's
shell is the same one that, on 08-16, was confirmed pointed at **`uat`'s** database (that is how we diagnosed
REQ-042). The standing rule he himself set yesterday is **`sid` first, verified, then `uat`** — so either this was
the `sid` rehearsal and `uat` is still pending, or it went straight to the customer box and the rehearsal was
skipped. **Asking him now.** Outcome-wise the run is clean either way; the rule is about the next migration, not
this one.

**Ball: owner** (which env — and then the same four steps on the other one) **& @Sober** (write here from now; cut
REQ-050's tasks; send me the rendered palette comparison) **& @Tanya** (the render + dev-LINE passes).

## [--:--] Porter (PM) — answered: `0018` ran on **`sid`** (env switched first). The rule held. → @Sober
Owner: *"รันที่เครื่องนี้ และแก้ env เป็นของ sid แล้วค่อยรัน"* ⇒ **it was the `sid` rehearsal, not the customer box.**
The standing rule he set yesterday — `sid` first, verified, then `uat` — was followed on its first real outing,
including on a migration that was safe enough to be tempting to shortcut. Worth recording as a rule that works
rather than a rule that exists.

**State:** `sid` = `0018` applied, verify GREEN, `subject_id IS NULL` → 0. **`uat` = NOT yet migrated** — same four
steps once he switches the env back (backup → migrate → verify → NULL check). Until then the two environments are
one migration apart, which is expected and fine, but it means **nothing that assumes `course_packages.subject_id`
may be deployed to `uat` before that run.** @Sober, please make sure any TASK that reads the new column says so.

**Ball: owner** (the same four steps on `uat`) **& @Sober** (REQ-050 tasks · the rendered palette comparison ·
REQ-055 spec) **& @Tanya** (render + dev-LINE passes — they close REQ-043/046/048/049/053/054).

## [--:--] Porter (PM) — 🔴 STOP on `uat`: `db:verify` FAILED — the TASK-085 silent-skip shape. Owner halted. → @Sober URGENT
Owner ran the same four steps against **`uat`**. `db:backup` fine (0.1 MB, non-zero). `drizzle-kit migrate` reported
**"migrations applied successfully"** — and then **`db:verify` failed and exited 1**:

```
Journal: 19 migration(s) · ledger __drizzle_migrations_scheduling: 19 row(s)
Schema witnesses: 19 applied
🔴 4 migration(s) in the journal are NOT recorded as applied:
  0011_freelance_budgets      when=1783000000007  ⚠️ drizzle would SKIP this silently (ledger newest created_at=1783000000014 ≥ when)
  0015_teacher_link_requests  when=1783000000011  ⚠️ same
  0016_subjects_price_group   when=1783000000012  ⚠️ same
  0017_entitlement_source     when=1783000000013  ⚠️ same
⚠️ ...the TASK-085 failure mode. Check `migrationsTable` in drizzle.config.ts and run `bun run db:seed-ledger`.
Deploy is NOT complete. Do not restart the app against this schema.
```

**What I have told the owner: stop, change nothing, restart nothing** — including **not** running the
`db:seed-ledger` the script suggests. That is a **write against the customer's database**, and the last time this
family of problem appeared (2026-08-02, REQ-032/TASK-085) the dry-run stopped the operator **twice** before
anything was corrupted. The suggestion is sound in general; whether it is right *here* is an SA call on evidence,
not a copy-paste.

**Facts, kept separate from interpretation:**
- **Schema witnesses: 19 applied** — every object the 19 migrations create appears to exist on `uat`.
- **Ledger: 19 rows**, but four journal entries have no matching record, and the ledger's newest `created_at`
  (…014) is **≥** their `when` ⇒ drizzle will **skip them forever, silently**, which is exactly the trap REQ-032
  was built to expose.
- The same command on **`sid` an hour earlier was GREEN** (ledger 23 rows / 19 witnesses). **Different result on
  the same script = the env really did change**, so this is a genuine `uat` state, not a mis-pointed run.
- Plausible origin (SA to confirm, not my call): the 2026-08-11 emergency **witness-based `db:seed-ledger`
  recovery** ran against this very box, and the rows it wrote carry timestamps that now shadow those four.

🟢 **Worth saying plainly: this is the guard working.** `drizzle-kit` said "applied successfully"; the schema looks
complete; nothing visibly broke. Without `db:verify` this would have been a clean-looking deploy hiding four
migrations that can never apply — the exact silent failure that cost this project an outage on 2026-08-02. The
tooling REQ-032 bought us just paid for itself on the customer's box.

**@Sober — this is yours and it is the top of the queue, above REQ-055.** What the owner needs from you: whether
`uat`'s schema is genuinely complete or only witness-complete, whether `0018` actually landed there, and then a
**copy-paste, owner-run** remedy with a dry-run first and an explicit "what you should see" for each step. He runs
everything; nobody on the team touches that box. If the honest answer is "restore the backup and redo it", say so.

**Ball: @Sober** (diagnose + owner-run remedy) **& owner** (holding — nothing to run until Sober answers).
**⚠️ Nothing may be deployed to `uat` until this is resolved** — and `REQ-055`'s wipe/import must not start on a
box whose migration ledger is in a known-bad state.

## [Jason (BE)] — idle on tasks, but 🔴 a DATABASE DUMP has been COMMITTED AND PUSHED to the code repo → @Sober
Startup ritual done against **this** file (date settled = 2026-08-18; my 08-17 entry is closed history). Nothing
is waiting for BE: **TASK-144 / TASK-145 / TASK-141 are all with you in REVIEW**, and 103/110/116/117 still need
the owner's sequencing. The `uat` ledger failure is yours + owner-run by definition — I don't touch that box, and
I have deliberately run nothing.

While checking the repo state I found something that is not on anyone's board:

### 🔴 `sm-prod-backup-2026-08-18T14-21-48-407Z.dump` is committed in `smart-scheduler-back` and pushed
- Commit **`0b8966c`** ("Implement feature X…") contains **one file: the 117 KB `.dump`**, and
  `git branch -r --contains 0b8966c` → **`origin/dong`**, i.e. it is already on
  `github.com/develyst1/smart-scheduler-back`.
- **Two more dumps are sitting untracked in the repo root right now** (`…14-47-10-669Z.dump`,
  `…14-48-17-134Z.dump`) — one `git add -A` away from the same thing.
- **What that file is, from my own script (`scripts/backup.ts`, TASK-126):** `pg_dump -Fc` of whatever
  `DATABASE_URL` pointed at — **full schema *and* all table data**. For this DB that means the `parents`,
  `students`, `teachers` rows: names, **phone numbers**, and **LINE userIds**. Per today's log the 14-21 backup
  was taken during the **`sid`** run, but `sid` is seeded from real people, and the same command on the customer
  box would produce the same artifact.
- 🟢 **Not secrets:** no `.env` is involved — `.env`, `.env.sid`, `.env.uat` are all correctly git-ignored
  (`f07d136`). This is *data* in git history, not credentials.

**Root cause is in my code, and I'll own the fix when you task it:** `backup.ts` writes
`sm-prod-backup-<stamp>.dump` to the **current working directory**, which for `bun run db:backup` is the repo
root. The runbook has the owner run it from inside the repo, so the script has been dropping customer data into
a git working tree by design since TASK-126. `.gitignore` has no `*.dump` line, so nothing stopped it.

**What I did NOT do:** I did not edit `.gitignore`, did not delete or move any dump, and did not touch git
history. Removing a pushed file is a history rewrite on the owner's repo — his call, via you and Porter, not
mine to start. **Proposed remedy, in the order I'd do it:**
1. **`*.dump` in `.gitignore`** (one line, stops the next accident — I can do it the moment you say so).
2. **`backup.ts` writes outside the repo** by default (e.g. an explicit `--out` or a git-ignored `backups/`
   dir), so the runbook can't put data in a working tree again. That's a small BE task against TASK-126.
3. **The pushed commit** — purge from history (`git filter-repo` / GitHub support) vs. accept it on a private
   repo. That is a **security decision for the owner**, with the honest caveat that anything pushed should be
   treated as disclosed to everyone who has had repo access since.

**Ball: @Sober** (review 144/145 + the TASK-141 ruling · and route this dump finding to @Porter → owner; tell me
which of 1/2 to build). Nothing else on BE.

## [--:--] Porter (PM) — 🔴 Routing Jason's dump finding to the owner as a SECURITY decision + status check on @Sober
**Relaying Jason's find to the owner now.** Framed for him as three separate things, because they have different
urgency and only one of them is genuinely his to decide:
1. **Stop the bleeding (no decision needed):** `*.dump` in `.gitignore`, and **two more dumps are sitting
   untracked in the repo root at this moment** — one `git add -A` from being pushed too. I am telling him to move
   those two files out of the repo folder **before** he touches git again.
2. **Fix the cause (a small BE task):** `backup.ts` writes to the CWD, and the runbook has him run it *inside* the
   repo — so the script has been dropping customer data into a git working tree **by design** since TASK-126.
   Jason owns this and has correctly not started it without a task.
3. **The already-pushed commit `0b8966c` — the owner's call, and the only real decision.** Purge from history vs.
   accept it. I am giving him the honest version: **anything pushed must be treated as disclosed to everyone who
   has had access to that repo since**, and a history rewrite does not un-disclose it — it only stops further
   copies. What it *does* change is whether the file keeps propagating to every future clone.

**What is in it, in business terms (this is the part the owner needs, not the git mechanics):** parents',
students' and teachers' **names, phone numbers and LINE userIds** — real people, mostly children. 🟢 **No
credentials**: `.env`, `.env.sid`, `.env.uat` are correctly ignored. So this is a **personal-data** exposure, not
an account-takeover one. I am also asking him whether that GitHub repo is **private**, because it changes the size
of the problem by an order of magnitude and nobody on the team can see that from here.

**Good process note, recorded deliberately:** Jason found this while idle, on a repo nobody asked him to audit,
and then **stopped** — no `.gitignore` edit, no file deletion, no history rewrite — because those are the owner's
repo and the owner's call. That is exactly the judgement this project's rules are meant to produce.

### @Sober — status, since the owner asked what you are working on
Visible from your artifacts (no log entry yet this session): **TASK-144** (check-in correction returns the hour —
REQ-050 Gap C) and **TASK-145** (QR/keyword picker + labels — Gap A/AC-3) are **cut**, and 141/142 were revised at
22:31. Good — that clears the REQ-050 decisions I gave you.
🔴 **But the `uat` ledger failure is still unanswered and it outranks all of it.** The owner is holding with a
half-verified customer database and cannot run REQ-055, deploy anything, or restart the app until you rule on it.
**Please answer that first**, then the dump routing above, then the palette. And please **write the log entry** —
the owner asked me "what is Sober doing" and I had to reconstruct it from file timestamps.

**Ball: @Sober** (uat ledger — urgent · then tell Jason which of his 1/2 to build) **& owner** (move the two loose
dumps out of the repo · decide on the pushed commit · tell us if the repo is private).

## [--:--] Fern (FE) — first entry in the canonical file · TASK-143 DONE · my own date error owned + stamps corrected · FE repos are clean of the dump → @Sober
Re-read board + this file. Three things.

### 1. My date error — owned, and the stamps are fixed
I took `2026-08-16` from the **newest log filename** when this session opened, which is the one thing PROTOCOL's date
rule names explicitly as forbidden. That is my error, not an inherited one: my session's own context said 2026-08-18
all along and I did not check it. When I hit the three-way conflict I stopped and asked the human the clock question
rather than stamping a fourth guess into an artifact — but the right moment to look was at startup, not six tasks in.
Now that Porter has settled it: I have corrected **my own** stamps only — 5 on `board.md` and the Implementation-Notes
headers on TASK-131/132/133/137/139 — from `Fern 2026-08-16` → `Fern 2026-08-18`. **I did not touch anyone else's
lines** (Sober's `SA-reviewed Sober 2026-08-16` stamps stand as written), and I did not rewrite the closed 08-16/08-17
logs. Verified by diff: 5 changed lines, all mine.

### 2. TASK-143 — built, and you've already reviewed it PASS
It never got a log entry because I was holding the clock question; the board row and the task file carried it, which is
why you found it. For the record: `BOOKING_TABS` → **Trial · Single · Voucher**, the `isCourse` payload/validity branch,
the course teacher/time row and the `useEligibleStudents("COURSE_PACKAGE")` query removed, and the dead render fork
**collapsed** rather than left as a `{cond ? x : null}` pretending a choice remains. Orphans: `CourseContext` import and
`booking.courseContext` gone (0 readers); `bookingType.COURSE_PACKAGE` and `noCourseStudents` **kept on purpose** —
the first still labels EXISTING course bookings on the calendar/table, the second is still a live branch of the generic
`EligibleStudentSelect`. `tsc` **0** · build ok · tests **15/0** · §3.5 0/0/0/0. Thanks for checking the orphan calls
individually — that was the part most likely to break something invisible.
⏸️ Still owed and not mine to write: **Porter's C redirect wording** (the in-modal pointer to the plan screen). The task
makes it conditional on him supplying the copy, so the tab is simply gone until then.

### 3. Jason's dump finding — I checked my own repos rather than assume it stopped at the BE
`git ls-files` on **both** my repos (`smart-scheduler-front`, `smart-scheduler-backoffice-front`): **zero** tracked
`.dump` / `.sql` / `.bak` files. ⚠️ **Correction, same session:** `git ls-files` only lists what is tracked *now* — a dump
that was committed and later deleted would not show, so that check did not support the conclusion I drew from it. Re-ran
it properly over **all branches and every commit** (`git log --all --diff-filter=A --name-only`): still **zero** such
files ever added. The conclusion stands; the first check didn't earn it. So whatever the blast radius of that commit is, **it does not include the two FE
repos** — that narrows what the owner has to rotate/purge. Routing it to you rather than to Porter, per the chain.

### FE queue
**TASK-142 is the only FE work left and it is still gated** — the depends line names Porter's palette sign-off, and I
can't find a sign-off in this file (your rendered comparison is still listed as owed to him). So I'm not starting it.
**Q: step 0 of TASK-142 (the nickname wiring) now has NO dependency** — TASK-141 closed as no-change, and it's one line
at `lib/api/mappers.ts:13` plus `nickname || studentName` in the cells. Do you want that split out and done now, or kept
in one pass with the dual-colour cell so QA sees the cell change once? I'd rather ask than half-ship a task.

**Ball: @Sober** (the step-0 question above; the palette comparison to Porter to unblock 142) **& @Porter** (palette
sign-off with the owner · the C redirect wording for the removed COURSE tab).

## [--:--] Porter (PM) — owner: park the dump issue, focus on go-live. Recorded as HIS decision + the critical path. → @Sober
Owner: *"ช่างมันเถอะ รีบโฟกัสเรื่องเอาข้อมูลขึ้นกันเถอะ จะเคลียร์ข้อมูลเทสออกจาก uat แล้วเอาข้อมูลนักเรียนขึ้นให้ลูกค้า"*

**Recorded as an explicit owner decision, not an oversight:** the pushed `.dump` (commit `0b8966c`) stays as it is
for now; no history rewrite, no security work tonight. It stays on the housekeeping list and I will raise it once
more when go-live is done — **not** repeatedly, and not as a blocker. It is his repo, his customer, his call.

**One exception I put to him, because it is not a security argument — it is tonight's-work argument:** REQ-055's
**first step is a backup**, and `backup.ts` writes into the repo working tree. So **the very next thing we do
creates another customer-data dump inside the git repo**, on top of the two already sitting there loose. A one-line
`*.dump` in `.gitignore` costs nothing and stops tonight's operation from making the same mess bigger. I asked for
that one line only, and left the rest parked.

### The critical path to "the customer's students are in the system" — nothing else matters until these land
1. 🔴 **@Sober: rule on the `uat` ledger.** The owner is holding a customer database where `db:verify` says four
   migrations can never apply. **Whether that blocks the wipe/import is your call, not mine** — my read is that a
   data wipe+import depends on the *schema* (19/19 witnessed) rather than the ledger, so it may well be safe to
   proceed while the ledger is fixed separately. **Say so explicitly if that is right**, because right now the
   owner is stopped on my "do not start REQ-055 on a known-bad box" and I will not lift that on my own guess.
2. 🔴 **@Sober: spec REQ-055.** The wipe half needs nothing from anyone. The import half has every rule it needs:
   0-prefix phones · 10-digit gate · DOB sanity range 2005–2026 · hold-back list for no-phone · parent-rows become
   the family name · **yellow rows excluded entirely** · **day-by-day batches, top-down** · row-keyed
   reconciliation (AC-15/16). **110 rows are import-ready today**, 39 need the customer, 27 are not-ready.
3. Then Jason builds the importer, the owner runs it one day at a time, and I hand him the row list to colour.

Everything else — palette, Tanya's passes, REQ-050 tasks, the dump — waits behind those two. **@Sober, if you only
do one thing this session, make it (1); the owner cannot move at all until it is answered.**

**Ball: @Sober** (the two items above, in that order) **& owner** (the one `.gitignore` line, then wait for the
REQ-055 steps).

## [--:--] Porter (PM) — owner added `*.dump` to `.gitignore`. Recurrence stopped; the pushed file is unchanged. → @Sober
Owner ran `echo "*.dump" >> .gitignore` in `smart-scheduler-back`. **What that does and does not do, stated so
nobody later assumes the problem is closed:**
- ✅ The two loose dumps in the repo root are now ignored, and **tonight's REQ-055 backup cannot be committed by
  accident** — which was the entire reason I asked for it.
- ❌ It does **not** untrack or remove `sm-prod-backup-2026-08-18T14-21-48-407Z.dump` from commit `0b8966c` — a
  `.gitignore` never affects an already-tracked file. That one stays on `origin/dong` exactly as it was, **parked
  by the owner's explicit decision**, and stays on the housekeeping list for after go-live.
- Jason's item 2 (make `backup.ts` write outside the repo) is still unbuilt and still the real fix; parked too.

**Ball: @Sober** — nothing on the owner now except waiting for you: **(1) rule on the `uat` ledger so he can start,
(2) spec REQ-055.** He is otherwise idle and the customer is waiting.

## [Jason (BE)] — TASK-146 BUILT → REVIEW (picked it up off the board — no log entry announced it) + a silent-bot defect fixed → @Sober
Re-read board + this file. **TASK-144/145/141 all came back DONE — thank you.** You cut **TASK-146/147/148** as
files and board rows but wrote no log entry today, so I took 146 from the board (`TODO` · Jason · deps clear),
which is the documented way to pick up work. **TASK-148 has a board row but no task file** — I did not start it
(Q3 in 146).

### TASK-146 — the leave cut-off is now staff-editable
- **Two number settings, default 3h**, `unit:"hours"`, bounds 0–72: `leave_cutoff_hours_fulltime` (FULL_TIME
  **and** PART_TIME) and `leave_cutoff_hours_freelance`. They render on the Settings screen automatically —
  **TASK-147 needs no new component for these two**, only the dictionary copy.
- **`lib/leave-notice.ts` stopped owning the numbers.** The 60/60/120 constants are gone; it is now
  `hasEnoughLeaveNotice(date, startTime, **cutoffHours**)` with the **`>=` boundary kept** (AC-3), plus
  `leaveCutoffKey(type)` so neither enforcement site can map FT/PT differently, and the TH+EN refusal carrying
  the configured `{n}` and the session `{time}` (AC-7).
- **Both sites** (plan-editor mark-absence + sick-leave) resolve the setting **at action time, in the booking's
  tx, inside the `!override` guard** — an admin cancel doesn't even read it (AC-5). It merged cleanly with
  TASK-136's block in the same branch, as you flagged.
- **Old tests rewritten** (they asserted the deleted constants): boundary at 3h (2h59 refused · exactly 3h
  allowed), *the same booking flipping answer when the setting changes* (AC-4), the key mapping, a 0h cut-off
  still refusing an already-started class, and the message text TH+EN.
- **tsc 0 · `bun test` 498 pass / 0 fail** (was 493).

### 🔴 The thing I'd have missed if I hadn't checked AC-6/AC-7 on the real path
`doLeaveBooking` called `updateBookingStatus` with **no catch**. Every refusal — the new cut-off,
`LEAVE_LOCKED`, anything — propagated to the webhook's outer handler, which only `console.error`s. **The parent
received no reply whatsoever; the bot just went quiet.** So AC-7's "the refusal explains why" was unreachable on
the one path parents actually use. Fixed: the server's message is now sent back (with a `leave_err` fallback
modelled on the existing `checkin_err`). Success path unchanged. **Q2 in the task asks you to ratify it** — it's
beyond the task's six items, but the AC can't be met without it.

**Q1 (yours to decide):** that refusal reaches a LINE parent in **Thai**, because `updateBookingStatus` doesn't
know the caller's language. Full AC-7 bilinguality needs either `lang` threaded into the service (touches a
money-adjacent signature) or the message re-rendered bot-side from the typed code + the setting (duplicates the
copy). I did neither on my own.

**Ball: @Sober** (review TASK-146 · Q1 lang · Q2 the silent-refusal fix · Q3 the missing TASK-148 file). Standing
context, not mine: the `uat` ledger is still the owner's blocker and REQ-055's importer isn't cut yet — the
moment it is, I'm free. The dump items stay parked by the owner's decision.

## [Jason (BE)] — TASK-148 BUILT → REVIEW (its file appeared, so I took it) · `0019` is a new owner-deploy step → @Sober
TASK-148's task file exists now, so I picked it up off the board. **TASK-146 is still with you** (Q1 lang · Q2
the silent-refusal fix).

### TASK-148 — a course can now be born with declared absences, and they're free
- **`0019_planned_at_creation`** — one `ADD COLUMN IF NOT EXISTS ... NOT NULL DEFAULT false`, hand-authored +
  journal-registered (the `db:generate` trap), witness = the column itself. ⚠️ **Careful with the witness map:**
  it is **order-checked against the journal**, so appending 0019 *before* 0018 failed the suite — worth knowing
  before someone else adds one.
- **Input shape = `absentWeeks: number[]` (1-based), not a per-row flag on `sessions[]`.** `sessions[]` is
  TASK-095's *override* channel (date/teacher/subject/time); putting status in it mixes two concerns and muddies
  the `length === size` rule. **Q2 asks you to ratify** — TASK-149 builds against whichever you pick.
- **One behaviour, not a second appender:** the absent weeks are inserted `SICK_LEAVE + plannedAtCreation`, then
  **`reconcileCoursePlan` runs once** — the very engine the plan editor uses. Live sessions == `size`, end date
  moves out a week per absence.
- **Ceiling (AC-3) reuses reconcile's own guard.** I deliberately did not write a second ceiling check: its
  `EXTENSION_CEILING` throw rolls the whole create back, so an over-ceiling request is **refused, never
  trimmed**. The preview also returns `exceedsCeiling` so the FE can stop the user before they commit.
- **Quota (AC-5):** `leaveUsed += 1` is now skipped for a `plannedAtCreation` row at **both** sites; creation
  never touches `leaveUsed` at all ⇒ a course born with absences has **`leaveUsed = 0`, not locked**, while an
  ordinary sick leave still consumes exactly as before.
- **tsc 0 · `bun test` 507 pass / 0 fail** (was 498). The new tests pin the part I was least willing to assume:
  the engine owes **exactly one** make-up per declared absence, keyed to that week; two consecutive absences give
  two; a re-run appends nothing; and no-absence creates are byte-identical to today (AC-4).

### 🔴 Q1 — the one AC I can't honestly tick, so I'm not ticking it
**AC-2 "saved plan matches the preview exactly."** The preview computes make-ups as the next weekly slots; the
save uses `findFreeExtensionDate`, which **skips a week when that teacher's slot is taken**. No clash → identical;
clash → the saved make-up lands later than shown. Three ways out — (a) accept it and have the FE re-read the
saved plan (what every other append already does), (b) make the preview clash-aware, (c) make the save refuse
rather than shift. That's a behaviour decision, so it's yours, not mine.

⚠️ **Deploy, for @Porter via you:** `0019` is a **new owner-run migration**. It must NOT go to `uat` while that
box is one migration behind on `0018` with a ledger `db:verify` calls known-bad. `sid` first, as the owner's own
rule says.

**Ball: @Sober** (review TASK-148 + TASK-146 · Q1 the AC-2 preview/save divergence · Q2 the `absentWeeks` shape,
which gates TASK-149). Still not cut: REQ-055's importer — the moment it lands I'm free for it.

## [--:--] Porter (PM) — 📁 THIS FILE IS CLOSED. Continue in `log/2026-08-19.md`.
The clock has rolled: **today is 2026-08-19** and that file is now canonical (PROTOCOL: new date → new file).
Nothing further is written here; nothing here is rewritten. Whatever your session thinks the date is, **append to
`2026-08-19.md`** — Porter's live entry, the `uat` ruling status, and the REQ-055 unblock are all there.
