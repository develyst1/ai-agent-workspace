# 2026-08-04

## Jason (BE) — TASK-095 rework FIXED + TASK-094 DONE → both REVIEW. **REQ-030 BE is complete.** → @Sober

Picked up Sober's ball: the 095 one-liner + TASK-094 (finishing REQ-030 BE). Both in one pass; tsc 0 · **421/0**.

### TASK-095 rework — clash set aligned to the index
- New pure `lib/booking-slot.ts`: `SLOT_NON_BLOCKING = ['CANCELLED','PENDING_RESCHEDULE','SICK_LEAVE']` +
  `holdsSlot(status)` — the **same** set the partial unique index (`0007_leave_overbook_slot_index.sql`) excludes.
  One named constant now, so preview-clash and index-clash can't drift apart again.
- `getSlotAvailability`'s clash query → `notInArray(status, [...SLOT_NON_BLOCKING])`. A SICK_LEAVE / reschedule-freed
  slot no longer previews `BOOKED` and no longer hides a free teacher.
- Added `booking-slot.test.ts` (leave/cancelled/reschedule → non-blocking; active statuses → hold the slot).

### TASK-094 — teacher-change 3-day notice + dual-teacher notify (SPEC-028 §5 #3)
- New pure `lib/teacher-change-notice.ts`, a direct mirror of `leave-notice.ts` but in **whole DAYS** (floored via
  `minutesUntilClass(...)/1440` — 2d 23h is **not** 3 days). `DEFAULT_TEACHER_CHANGE_NOTICE_DAYS = 3`, and the
  threshold is a **passed-in `days` param** ⇒ REQ-031/TASK-101 can feed a DB override in with **zero `app_settings`
  coupling** — the lib stays pure, exactly the "defaults in code, values passed in" design Porter pinned on REQ-031.
- Enforced in `applyPlanChange`'s **move** branch, **only when the teacher actually changes** (a date/subject-only
  edit is untouched): `throw conflict("TEACHER_CHANGE_TOO_LATE", teacherChangeNoticeMessage())` unless admin
  `override`. The notice is keyed to the (possibly moved) class date, i.e. the date the new teacher inherits.
- Dual-notify in the same tx via `enqueueLine`: old teacher `teacher_unassigned` ("off your schedule") + new teacher
  `teacher_assigned` ("now yours"). Rendered TH/EN in `line-message.ts` (2 new i18n keys); missing lineUserId →
  SKIPPED row as usual, so an unlinked teacher never blocks the swap.
- Tests: notice boundary (exactly 3 passes, 2 fails, 2d23h floors to 2), override bypass, REQ-031-style custom
  threshold, both render kinds (TH + EN).

**@Sober: TASK-094 + the TASK-095 rework are ready for review.** With this, **REQ-030 BE is done** —
092/093/096 DONE · 094/095/097 REVIEW. The FE (098/099) is fully unblocked once you clear 095/097.
**Next in my queue:** TASK-101 (settings mechanism — it registers the teacher-change-notice this task just left
override-ready), then 100/104/105/106; 103 post-go-live. **Ball: @Sober** (094 + 095 review).

## Sober (SA Lead) — TASK-094 + TASK-095-rework → both DONE. 🎉 REQ-030 BE is complete → @Jason, @Fern
Code-verified both, ran the suite myself — **tsc 0 · 421/0**.
- **095 rework ✅** — `SLOT_NON_BLOCKING = [CANCELLED, PENDING_RESCHEDULE, SICK_LEAVE]` in `lib/booking-slot.ts`
  matches the 0007 index **exactly**, the clash query uses it, and `booking-slot.test.ts` pins it. Preview-clash and
  index-clash are now **one named constant** — they can't drift apart again. Good fix, and better than the raw
  one-liner I asked for (the constant + test is the durable version).
- **094 ✅** — the notice + dual-notify fire **only on an actual teacher change** (the trap I'd have flagged — a
  date-only edit must not demand 3 days' notice; it doesn't). Pure lib, floored whole days (2d23h ≠ 3), threshold a
  **param** → REQ-031 feeds the override with **zero `app_settings` coupling**, exactly the seam I specced. Both
  teachers notified in-tx, unlinked-teacher → SKIPPED (never blocks the swap).

### 🎉 REQ-030 BE is DONE end-to-end: **092/093/094/095/096/097 all Sober-verified.**
The whole FE is unblocked: **@Fern — TASK-099 (the shared plan-modal) is a full go**, then 098 (create wrapper) on
top of it. The engine (092), the atomic applier + guards (093), availability/preview/override (095), the notice
(094), the DTO (097), and the orphaned-check (096) are all in and verified.

**Ball: @Jason** — you're on **101** next (it registers the notice 094 just left override-ready — clean handoff),
then 100/104/105/106. **@Fern** — 099 is go. I'll review 101/104/105/106/106-FE as they land; REQ-028/034 remain my
design backlog for any quiet stretch.

## Jason (BE) — TASK-101 → REVIEW. Settings mechanism + the two go-live rules wired. **Unblocks FE TASK-102.** → @Sober
Took the ball. Built the mechanism SPEC-029 specced, wired both rules through the edge, tsc 0 · **429/0**.

### The mechanism (defaults in code, overrides in `app_settings`, pure fns stay pure)
- **`lib/settings.ts` (pure)** — a hand-listed registry, **not** a framework. Exactly the **two** go-live keys
  (`teacher_change_notice_days` def 3, `checkin_early_minutes` def 30), each `{ default, unit, label, parse }` where
  `parse` = integer-in-bounds (0–30 / 0–240). Plus pure `resolveSetting(key, rawFromDb)` → parsed override, else the
  **coded default with `isDefault:true` + a reason**. A missing **or malformed** override degrades to the default,
  **never** to zero/null/"no rule" (AC #4). `isSettingKey` is `hasOwnProperty`-based so `__proto__` can't smuggle in.
- **`settings.service.ts` (the only DB edge)** — mirrors `lib/line-admin.ts`: `getSetting` reads the row + runs the
  pure resolver; `setSetting` validates via the registry's `parse` and **rejects malformed with a 400 + reason**
  (never writes junk); `onConflictDoUpdate` upsert. `listSettings` returns `{key,label,unit,value,default,isOverridden}`.
- **API** — `GET /api/settings` + `PUT /api/settings/:key` (unknown key → 400; body shape-checked, registry owns the
  real bounds). Nothing in `lib/` learned about the DB.

### Wiring the two rules (values read at ACTION time — AC #5, no retroactive invalidation)
- **Check-in window:** promoted `CHECKIN_EARLY_MINUTES` to an `earlyMinutes` **param** on `isWithinCheckinWindow` +
  `checkinWindowMessage` (still pure, still default to the constant); `checkin.service` (`checkinByToken` + the QR
  path via `formatCheckinPayload`) resolves `checkin_early_minutes` and passes it. Test proves a raised setting opens
  check-in earlier with **no deploy**.
- **Teacher-change notice:** registered the key; `applyPlanChange` resolves `teacher_change_notice_days` (in-tx) and
  passes it to TASK-094's `hasEnoughTeacherChangeNotice(..., noticeDays)` + `teacherChangeNoticeMessage(noticeDays)`.
  **SPEC-029 provides the value, REQ-030 enforces — one seam, no double-wire** (exactly the boundary you specced).

**Deferred on purpose:** the other six configurable-looking constants stay in code — a setting nobody's asked to
change is a constant with extra steps. Adding a 3rd later = **one registry entry, no schema change** (AC).

**@Sober: TASK-101 ready for review.** **@Fern: TASK-102 (Settings screen) is unblocked** — the `GET/PUT
/api/settings` shape is final (`{key,label,unit,value,default,isOverridden}`; PUT 400s with a Thai reason on a bad
value). **Next in my queue:** TASK-100, then 104/105/106; 103 post-go-live. **Ball: @Sober** (101 review) + **@Fern** (102).

## Sober (SA Lead) — TASK-101 → DONE. REQ-031 BE mechanism is in; FE screen unblocked → @Fern, @Jason
Code-verified, ran the suite — **tsc 0 · 429/0**.
- **`resolveSetting` is exactly AC #4** — no-override → default+reason; **malformed → default+reason, never
  zero/null/"no rule"**; valid → parsed. `intInRange` bounds-checked; `isSettingKey` is `hasOwnProperty`-safe.
- **`settings.service` is the only DB edge** — `setSetting` 400s a malformed value with a Thai reason (never writes
  junk); `onConflictDoUpdate` upsert (line-admin pattern). Nothing in `lib/` reads the DB.
- 🔑 **The seam I specced holds** — `applyPlanChange` resolves `teacher_change_notice_days` **in-tx** (`:1495`) and
  passes it to TASK-094's pure fn; check-in resolves `checkin_early_minutes` at both paths. **Values read at action
  time (AC #5), no double-wire** — SPEC-029 provides, REQ-030 enforces.
- **Scope held** — exactly the two keys; the other six stay constants; a 3rd rule = one registry entry (AC).

**@Fern — TASK-102 (Settings screen) is a full go** (shape final: `{key,label,unit,value,default,isOverridden}`,
PUT 400s with a reason). That's the load-bearing half of REQ-031 — *"in the DB" isn't "easy to change."*

**Ball: @Jason** (100 → 104/105/106) **& @Fern** (099 + 102). REQ-030 BE is complete; REQ-031 BE is complete. My
review queue: 100/102/104/105/106/107 as they land. REQ-028/034 remain my design backlog when it's quiet.

## Sober (SA Lead) — review queue quiet → pulled REQ-028 forward: SPEC-031 (rental revenue) → @Jason, @Porter
No task in REVIEW, so I specced the next READY_FOR_SA item routed to me. **`specs/SPEC-031-equipment-rental-revenue.md`**
+ **TASK-108/109**. The REQ's own finding holds after I checked `sale-post.ts`: a rental is **four product codes
through the existing `recordSale` path — no new money mechanism** (the constraint, and the exact failure shape we
cleaned up on 08-01).
- **Four codes** in `lib/sale-items.ts` (`rental-set` 200 · `rental-ride` 150 · `rental-helmet` 50 · `rental-pads`
  50, VAT-incl), marked **`revenueKind="RENTAL"`** so P&L / REQ-014 separate rental from tuition (AC #3; rental
  isn't program-attributed). Seeded by `sale:ensure-items` (re-run on deploy, insert-only).
- **`POST /rentals`** → `recordSale(code, hours, {refId, idempotencyKey})`, idempotent (double-submit posts once).
- 🔑 **The one SA nuance:** unlike a booking's sale, the rental post is **not** downstream best-effort — it *is* the
  event. So the endpoint **surfaces the result** (`duplicate`=ok, `error`/`item-missing`=a real failure staff must
  see), never a silent 200. That's the one place `void recordSale` would be wrong.
- **Both entry points supported** (refId present = session add-on; absent = standalone) — the model's identical.

### ❓ @Porter → owner (Q2, non-blocking — TASK-108 doesn't need it; TASK-109 FE does)
Where do staff record a rental — **(a)** an add-on at booking/attendance, **(b)** a standalone quick-sale, or
**both**? Porter's lean was (a)+(b); the `POST /rentals` contract already supports both, so this only picks the FE
surface. Confirm before TASK-109's entry UI.

**Ball: @Jason** (TASK-108 dep-free, startable alongside 100/104/105/106) **& @Porter** (the Q2 entry-point).
Design backlog now down to **REQ-034** (dashboard filter, MEDIUM). Review queue: 100/102/104/105/106/107/108 as they land.

## Jason (BE) — TASK-100 BE → REVIEW. Orphan-impact preview for a workDays change. **FE half → Fern.** → @Sober, @Fern
Took the top of my queue. BE half of the soft-warning; tsc 0 · **434/0**.
- **Pure core in `lib/work-days.ts`:** `removedWorkDays(current, next)` = the weekdays a teacher stops working, and
  it honours the **"empty = all seven days"** convention on **both** sides — so narrowing all-days → weekdays-only
  correctly reports Sat+Sun removed (the subtle bit; fully pure-tested). Plus `sessionsOnRemovedDays`.
- **`GET /teachers/:id/work-days/impact?workDays=1,2,3`** → `{ removedDays, removedDaysLabel, orphanCount, sessions[] }`
  over **future LIVE** (PENDING/CONFIRMED/EXTENDED) bookings only — a delivered/cancelled session can't be orphaned.
  It **does not mutate** — the FE previews, warns, then applies via the existing `PATCH .../work-days`. Adding a day
  / reorder / no-op → `orphanCount:0` (no warning path). **Not a hard block** — TASK-096 stays the after-the-fact net.
- Refactored `COURSE_LIVE` to also export a typed `COURSE_LIVE_STATUSES` tuple (one source of truth) so the
  `inArray(status, …)` query typechecks against the enum column.

**@Fern:** the contract's ready for the confirm dialog — call the impact endpoint with the intended workDays before
`PATCH`, and when `orphanCount > 0` show *"orphans N upcoming sessions — proceed?"* (proceed applies; they also
surface via TASK-096). **@Sober: BE ready for review.** **Next in my queue:** TASK-104 (SICK_LEAVE no longer draws
the freelance ceiling — the owner reversal), then 105/106; TASK-108 (rental) is dep-free whenever. **Ball: @Sober**
(100-BE review) **& @Fern** (100-FE).

## Sober (SA Lead) — TASK-100 BE → DONE. The empty=all-days convention is the whole trick, and it's right → @Fern, @Jason
Code-verified, ran the suite — **tsc 0 · 434/0**. The one place this could have been subtly wrong is the
convention: workDays `[]` means "works every day" (`teacherWorksOnDay`), so narrowing **all-days → a subset** must
report the *complement* as removed. `removedWorkDays` expands **both** sides through that convention (`expandWorkDays`
= empty→all seven), so all-days→Mon-Wed correctly reports Thu/Fri/Sat/Sun removed; widen/reorder/add/no-op → empty.
Pure-tested. The impact endpoint composes it over **future LIVE** bookings only, **read-only** (FE previews then
`PATCH`es). `COURSE_LIVE_STATUSES` extracted as a typed tuple — one source of truth for the status set. **BE DONE.**

**@Fern — 100-FE is yours** (confirm dialog when `orphanCount > 0`, not a hard block), alongside 099/102.
**Ball: @Jason** — TASK-104 next (the SICK_LEAVE-releasing reversal — **live money, give it the careful pass**; I'll
review closely), then 105/106/108. Review queue: 100-FE/102/104/105/106/107/108/109 as they land.

## Jason (BE) — TASK-104 → REVIEW. SICK_LEAVE now RELEASES the freelance hour (owner reversal). → @Sober
Careful pass on live money, as flagged. tsc 0 · **434/0**. The change is deliberately **one line of classification**:
- **`freelance-budget.ts:36`** — dropped `SICK_LEAVE` from `FREELANCE_CONSUMING_STATUSES` → now `{CONFIRMED,
  ATTENDED, EXTENDED}`. So `heldTarget(SICK_LEAVE)=0`; a CONFIRMED→SICK_LEAVE **refunds** the held hour.
- **The reconcile-to-target machinery is untouched** (`reconcileDelta`/`reconcileBookingHolds`/`planHoldMoves`,
  TASK-091) — it's driven by `heldTarget`, so the whole money path corrects itself from the one classification flip.
  Nothing in `applyPlanChange`/`updateBookingStatus` needed a code change — the mark-absence `reconcileBookingHolds`
  call now targets 0 and refunds automatically.
- **`reconcileCoursePlan` NOT touched** (as the task pins): a sick-leave still appends a makeup, and that EXTENDED
  draws on its **own** confirm. Net: a sick-leave costs **1** freelance hour (the makeup), not 2 — which **resolves
  the TASK-093 owner-flag** (the "SICK_LEAVE draws 2 ceiling-hours" cross-rule consequence).
- **Round-trip re-pinned:** CONFIRMED→SICK_LEAVE→ATTENDED→SICK_LEAVE now holds **1→0→1→0**, ends releasing (held 0,
  full ceiling back); idempotency/no-inflate still green. Flipped the classification + round-trip tests to the new
  rule; updated the stale "keeps the draw" comments in `freelance-budget.ts` + `scheduler.service.ts`; added a
  reversal pointer on TASK-028's board row (TASK-028 stays DONE — its machinery is unchanged).
- **Fuel allowance:** no code (task confirms) — the manual `bo` EXPENSE path already exists; left un-seeded.

**@Sober: TASK-104 ready for review** (live money — the careful pass you asked for). **Next in my queue:** TASK-105
(every course cancel re-owes a makeup + DELIVERED-cancel needs a reason + releases the hour — the bigger reversal,
BE+FE), then 106; TASK-108 (rental) dep-free whenever. **Ball: @Sober** (104 review).

## Sober (SA Lead) — TASK-104 → DONE. An owner reversal that cost one classification line → @Jason
Careful pass on the live money, as I flagged: read the constant + tests, ran the suite — **tsc 0 · 434/0**
(`freelance-budget` 21/21).
- **One-line flip, machinery self-corrects** — `FREELANCE_CONSUMING_STATUSES = {CONFIRMED, ATTENDED, EXTENDED}`;
  `reconcileDelta`/`reconcileBookingHolds`/`planHoldMoves` untouched. This is the reconcile-to-target design earning
  its keep: a business-rule reversal is a *classification* change, not a code change. That's exactly why we built it
  that way.
- **Tests pin the NEW rule** (not just removed the old): `heldTarget("SICK_LEAVE")=0`, `reconcileDelta(1,·)=-1`
  (refund), round-trip `[1,0,1,0]` ending released. `reconcileCoursePlan` untouched → makeup still appends → a
  sick-leave costs **1** hour, not 2. **The TASK-093 owner-flag is resolved.**
- Fuel allowance = manual `bo` EXPENSE (already supported, no code).

**Ball: @Jason** — on to **TASK-105** (the bigger reversal: every course cancel re-owes + delivered-cancel needs a
reason + releases the hour). That one wires `reconcileCoursePlan` into the cancel path for the FIRST time and touches
the live `updateBookingStatus:cancel` — **give it the same careful pass; I'll review closely** (the re-owe-on-cancel
is the load-bearing behaviour). Review queue: 099/102/105/106/107/108/109 + 100-FE as they land.

## Jason (BE) — TASK-105 BE → REVIEW. Cancel now re-owes + delivered-cancel needs a reason. **FE → Fern.** → @Sober, @Fern
Careful pass, as flagged — this touches live cancel money + wires re-owe for the first time. tsc 0 · **437/0**.
Change is confined to `updateBookingStatus:cancel`:
1. **Delivered-cancel door opens (audited):** a DELIVERED (ATTENDED/NO_SHOW) cancel is now allowed **only with a
   non-empty reason** → else `REASON_REQUIRED` ("ต้องระบุเหตุผลในการยกเลิกคาบที่เรียนไปแล้ว"). Extracted a pure guard
   `requiresCancelReason(status) = isDelivered(status)` (course-plan.ts) so the rule is pinned independently. The
   reason is stored in `note` (already surfaced in the booking DTO). **Actor isn't captured** — there's no per-user
   auth today (one shared login; same limit REQ-031 named), so "who cancelled" can't be recorded honestly yet; the
   *why* + *when* (`updatedAt`) are. Flagging that gap, not faking an actor.
2. **Re-owe wired into cancel for the first time (§11.3):** after setting CANCELLED, `if (courseId)
   reconcileCoursePlan(tx, courseId)` — a per-session cancel is a **reschedule, not a forfeit**, so `current` returns
   to `size` by appending a makeup. Works for BOTH a live and a delivered cancel because both end as status
   CANCELLED (out of plan). The makeup mirrors teacher/subject/time from a live session; it's MAX_WEEK-bounded (the
   existing ceiling check in `reconcileCoursePlan`).
3. **Money:** unchanged path — the existing `reconcileBookingHolds(…, "CANCELLED")` at the tail releases the held
   hour (CANCELLED is releasing since TASK-104); the appended makeup draws on its **own** confirm. Net: a cancel
   costs **0** freelance hours until the makeup is taught.
4. **NO_SHOW still consumes** — the no-show *action* is untouched (it counts toward `current`, no makeup). Only the
   *cancel* action re-owes. **Edit/move of a delivered session stays `SESSION_DELIVERED`** (guards in
   moveBooking/applyPlanChange untouched — this opened only the cancel door).

Pure tests added: `requiresCancelReason` (delivered→true, live→false); re-owe appends one makeup for BOTH a
CONFIRMED→CANCELLED and an ATTENDED→CANCELLED; NO_SHOW-left-as-is is a no-op.

**@Fern:** on a delivered-session cancel, prompt for a **required reason** before the call; show the server's
`REASON_REQUIRED` if missing; surface the stored reason (the booking's `note`) where cancellations are shown.
**@Sober: BE ready — the careful review you asked for** (re-owe-on-cancel is the load-bearing bit; the pure test
pins both cancel kinds append). 🔴 The post-go-live gap stands: **no cancel-course / refund / early-termination
flow** — since a per-session cancel now never shrinks a course, ending one early has no path (already flagged
@Porter; not go-live-blocking). **Next in my queue:** TASK-106 (voucher program exclusion), then TASK-108 (rental).
**Ball: @Sober** (105-BE review) **& @Fern** (105-FE).

## Sober (SA Lead) — TASK-105 BE → DONE. Careful pass done; one cross-rule edge for the owner → @Porter, @Jason
Read the `cancel` branch line by line (it wires re-owe into live cancel for the first time) + ran the suite —
**tsc 0 · 437/0**.
- **Reason-gate** — `requiresCancelReason=isDelivered`; delivered cancel with empty/whitespace reason (`.trim()`) →
  `REASON_REQUIRED`; reason stored in `note`.
- **Re-owe correct + idempotent** — `reconcileCoursePlan` on cancel re-owes a makeup for **both** live and delivered
  cancels (count-driven append → `current`=`size`); `current==size`→no-op so a re-cancel doesn't double-append. Only
  NO_SHOW consumes; edit/move of delivered still blocked.
- **Money releases** at `:1705` (post-status CANCELLED → held 0); makeup draws on its own confirm.

### Two notes
1. **Actor not captured — accepted.** No per-user auth (shared login), so Jason stored *why*+*when* and flagged the
   *who* rather than faking it. Right call; actor lands with separate-logins (the REQ-031 prerequisite), not here.
2. 🔴 **@Porter — one cross-rule edge for owner awareness (not blocking):** a course-session cancel is **refused
   with `EXTENSION_CEILING`** when the course is already at `MAX_WEEK` — the re-owe makeup can't fit within the
   ceiling. Rare (only a fully-extended course) and arguably correct, **but the message talks about *extension* on a
   *cancel* — confusing.** Escape today is admin `override`; the proper home is REQ-036 (early termination). Same
   shape as the earlier sick-leave-2-hours flag: it's the two owner rules (re-owe + hard ceiling) meeting, not a bug.

**FE half (reason prompt + surface the reason) is @Fern's.** **Ball: @Jason** — 106 → 108 next; **@Porter** (the
ceiling-edge awareness + it strengthens the REQ-036 case). Review queue: 099/102/105-FE/106/107/108/109/100-FE as they land.

## Jason (BE) — TASK-106 → REVIEW. Vouchers can't book Onewheel / Balance Play. **Unblocks FE TASK-107.** → @Sober, @Fern
tsc 0 · **440/0**.
- **Pure data next to the card** (`lib/sale-items.ts`): `VOUCHER_EXCLUDED_GROUPS = {onewheel, balance-private,
  balance-group}` + `voucherAllowsProgram(group) = !!group && !excluded` + `voucherAllowedGroups()` (derived from
  `PRICE_GROUPS`). Unit-tested: excluded groups → false, bike-skate → true, null/undefined/"" → false.
- **Enforced at `insertBooking`** — the **single chokepoint every VOUCHER booking passes** (createBooking and any
  future voucher-insert path), not just `prepareVoucherBooking`: `if bookingType==="VOUCHER" &&
  !voucherAllowsProgram(await resolvePriceGroup(subjectId))` → `conflict("VOUCHER_PROGRAM_EXCLUDED", "วอยเชอร์ใช้กับ
  คลาส Onewheel หรือ Balance Play ไม่ได้")`. A **null price group is refused via the same path** (1st Trial etc. — no
  special case, matches the null-group DoD). Enforced at BOOKING time, not attendance.
- **Already-sold vouchers untouched** (AC #5) — this only refuses the new excluded *booking*; nothing rewrites voucher
  hours.
- **Exposed for the FE:** `GET /sellable-packages` now returns `voucherAllowedGroups` so the picker filters from one
  source, never a hardcoded list.

**@Fern: TASK-107 unblocked** — omit the excluded programs from the voucher picker using `voucherAllowedGroups` from
`/sellable-packages`, and surface the server's `VOUCHER_PROGRAM_EXCLUDED` if one slips through. **@Sober: 106 ready
for review.** **Next in my queue:** TASK-108 (equipment rental as revenue — 4 codes + `POST /rentals` via
`recordSale`, idempotent, surfaces the result). **Ball: @Sober** (106 review) **& @Fern** (107).

## Sober (SA Lead) — TASK-106 → DONE (REQ-027 (b)). Better-placed than I specced → @Jason, @Fern
Code-verified, ran the suite — **tsc 0 · 440/0**. Excluded set `{onewheel, balance-private, balance-group}` matches
the voucher card; `voucherAllowsProgram(null)=false` refuses **1st Trial via the null-group path** (no special case);
already-sold vouchers untouched.
- **You improved on my spec** — enforcing at **`insertBooking:761`** (the single chokepoint every VOUCHER passes)
  instead of `prepareVoucherBooking` means no future voucher-insert path can bypass it. Good call.
- **`voucherAllowedGroups()` is derived** from `PRICE_GROUPS` and exposed on `/sellable-packages` — the FE filters
  from one source, can't drift from the excluded set.

**REQ-027 is now closeable once TASK-107 (FE picker) ships** — part (a) was already live, (b) is done here.
**@Fern: TASK-107 unblocked.** **Ball: @Jason** (TASK-108 rental next — dep-free) **& @Fern** (107 + 099/102/105-FE/100-FE).
Review queue: 099/102/107/108/109 + the FE halves as they land. REQ-034 remains my last design-backlog item.

## Jason (BE) — TASK-108 → REVIEW. Equipment rental as revenue (`POST /rentals`). **Unblocks FE TASK-109.** → @Sober, @Fern
No new money mechanism — four codes through the existing `recordSale`. tsc 0 · **444/0**.
- **Four codes in `SALE_ITEMS`** (`lib/sale-items.ts`): `rental-set` 200 · `rental-ride` 150 · `rental-helmet` 50 ·
  `rental-pads` 50, VAT-inclusive per hour, each `metadata.revenueKind = "RENTAL"` so reports split rental from
  tuition (AC #3, not program-attributed). `SaleItemSeed` gained an optional `metadata`; `ensure-sale-items` now
  **merges** it over the seed defaults. ⚠️ **Deploy: re-run `bun run sale:ensure-items`** (insert-only) to create the
  four items.
- **`POST /rentals` `{code, hours>0, refId?}`** → `recordSale(code, hours, {refId, idempotencyKey})` with
  `idempotencyKey = rental:{refId ?? uuid}:{code}` (SPEC format) so a session add-on double-click posts once; a
  standalone walk-in mints a fresh id (its own sale, no natural key).
- 🔴 **The result is SURFACED, not `void`d** — a rental has no other artifact, so a skipped post = no rental:
  `recorded` → **201**, `duplicate` → **200** (idempotent replay = success), item-missing/unknown/error →
  **`RENTAL_NOT_POSTED` 502** (staff must see it, never a silent 200). This is the one place the best-effort default
  is wrong, and I inverted it here only.
- **Both entry points** supported (refId present = session add-on; absent = standalone) — one money model; the FE
  surface is the Q2 choice. Validation: `code ∈ the four` (via `isRentalCode`), `hours` positive int.
- Pure tests: the four prices, the RENTAL marker, `hours×price` → correct signed movement (`saleMovement`),
  `isRentalCode`, and the idempotency-key format. Out of scope (per REQ): stock/deposits/damage.

**@Fern: TASK-109 BE contract is ready** (`POST /rentals`, surfaces `recorded`/`duplicate`/`RENTAL_NOT_POSTED`) —
still gated on owner **Q2** (entry-point surface) via @Porter, but the endpoint supports both so you're not blocked on
the model. **@Sober: 108 ready for review** (the surfaced-result inversion is the one SA nuance). **Next in my
queue:** my BE queue for the REQ-028/030/031 wave is now **clear** — remaining Jason items are TASK-103 (post-go-live)
and whatever Sober routes next (REQ-034 design is his). **Ball: @Sober** (108 review) **& @Porter** (rental Q2 for 109).

## Sober (SA Lead) — TASK-108 → DONE (add-on path); one AC-#4 gap on standalone → @Jason, @Porter
Code-verified, ran the suite — **tsc 0 · 444/0**. The four codes/prices, the `RENTAL` marker, and the SA nuance I
specced — **surface the result** (201/200/502 `RENTAL_NOT_POSTED`, never a silent 200) — are all right. No new money
mechanism; straight through `recordSale`. Deploy note carried: re-run `sale:ensure-items` (insert-only) for the 4 items.
- 🔴 **One required addition, and it's contingent on Q2:** idempotency keys on `refId ?? crypto.randomUUID()`
  (`rental.service.ts:12`). A **session add-on** (refId=bookingId) is idempotent — double-click posts once ✅. A
  **standalone** walk-in mints a fresh uuid per request → a double-submit **double-posts / double-charges** (AC #4
  unmet). A standalone rental has no natural key (unlike course/voucher sales), so **the client must supply one.**
  **Fix (small):** accept an optional `idempotencyKey` in the body → `idBase = refId ?? body.idempotencyKey ?? uuid`;
  **TASK-109 generates one per rental action** and sends it for the standalone surface. **If only the add-on (a)
  ships, no change needed** — it's already idempotent. So this rides on @Porter's Q2 answer.

**Ball: @Porter** (rental Q2 — and it now carries the standalone-idempotency consequence: choosing (b)/both means the
client-key addition) **& @Jason** (the small client-key add if/when standalone is in scope). **REQ-028 is done
end-to-end pending TASK-109 (FE, Q2-gated).**

My BE review queue is now clear. **@Fern's FE tasks (099/102/107/105-FE/100-FE/109) are the remaining go-live path**
— I'll review each as it lands. My last design-backlog item is **REQ-034** (dashboard filter); I'll pick it up next quiet turn.

## [Tanya (QA)] — TASK-099 measured & rendered (locally). One MAJOR defect. **All `sid` work is BLOCKED — no access.** → @Porter

Picked up the two things routed to me: (1) TASK-099's live render + the STANDING-RULE 4-width
measurement, (2) the batch acceptance on `sid`. **I got (1) partly. I got none of (2), and I am not
rounding that up.** Full write-up: `tests/TEST-099-plan-modal-render-and-widths.md`.

### 🔴 First, the blocker, because it decides everything else
**`H:\sm-test-access.txt` is unreachable — drive `H:` is not mounted on this machine** (only `C:` exists).
That file holds the dev URL, the staff login and `AUTH_SECRET`. No staff login ⇒ no backend token ⇒
**TASK-090's `mint-session.mjs` cannot run** (it requires a real token; blank exits 1 by design, and
forging one would mint a session with no authority — the exact failure Sober built the guard for).

So: **the entire batch acceptance — REQ-009 / 020 / 022 / 023 / 024 and the painted-UI checks (REQ-022
expired-voucher red alert, REQ-024 collapsed date input, REQ-026 nav) — was not started.** Not failed,
not partly done: **not started.** They stay open.

`sid` is up and reachable (`/scheduler/bookings` → 302 login, `/api/health` → 401). **The block is
credentials, not connectivity.** I did not hunt for credentials elsewhere and did not attempt the login.

### What I could still do, and how (no real env, no credential)
Ran the FE **locally** in a **real compositing Chrome** (playwright `channel:"chrome"`) against
`next dev` on `:3016` in **mock mode** — in mock mode the NextAuth provider accepts any local input
(`auth.ts`), so no password and no real environment is involved. Harnesses are committed and
re-runnable: `tests/harness/plan-modal-widths.mjs`, `tests/harness/voucher-manage-375.mjs`;
screenshots in `../project-docs/qa-2026-08-04/`.

### 🔴 DEF-1 (MAJOR) — the voucher table's new "Manage" column is UNREACHABLE at 375
The exact column Fern asked to have measured.

| Viewport | th widths (Student·Total·Usage·Expiry·Status·**Manage**) | Manage btn | table scrollW | card clientW | overflow-x | hit-testable |
|---|---|---|---|---|---|---|
| 1600 | 178·198·287·188·184·**260** | 113 @ x=1331 | 1294 | 1294 | hidden | ✅ |
| 1280 | 134·149·216·142·138·**195** | 113 @ x=1076 | 974 | 974 | hidden | ✅ |
| 768 | 99·109·160·104·101·**145** | 113 @ x=614 | 718 | 718 | hidden | ✅ |
| **375** | 87·72·160·82·76·**145** | 113 @ **x=512–625** | **624** | **341** | **hidden** | 🔴 **NO** |

At 375 the table is **283 px wider than the card that clips it**, the card is `overflow-x: hidden`, and
the page has **no horizontal scroll** (`scrollWidth == clientWidth == 375`). `elementFromPoint()` at the
button's centre returns **`null`** — it is off the painted surface, along with the Status column. The
screenshot shows the table cut mid-Expiry (`voucher-table-375.png`). So at phone width the **only** way
into the voucher plan modal doesn't exist.
The plan modal's own session table is fine at 375 (560 > 306 but `Table.ScrollContainer`, `overflow-x:
auto` ⇒ scrolls, both buttons reachable) — **the difference is that wrapper**, which `VoucherPanel`'s
table doesn't have. Course card "Manage plan": 379 / 273 / 310 / **301** px — full-width, never
collapsed ✅.

### What rendered correctly (local, mock)
Course "Manage plan" → modal with the server-derived summary (`COURSE · 10-session course · Leave 1/3 ·
7 OWED · Ends 6 Oct 26`), 3 session rows, Edit + Mark absence per row, "Insert make-up" + the owed hint;
Edit opens the editor with Date/Time/Teacher/Subject and the "Who's free at this slot" availability row.
No page errors on `/scheduler/bookings`.

### ⚠️ What is NOT TESTED — and please don't let this get read as a pass
Mock data **cannot refuse anything**, so every behavioural AC is untested: **edit/move applying ·
mark-absence · insert · the availability BOOKED/NO_BUDGET/clash states (mock says everyone is free) ·
delivered rows read-only (no delivered row in the fixture) · and "refusals show the server's exact
reason", which is the one I most wanted.** Also **the voucher plan shape itself**: the mock returns a
**course** DTO for every id (`scheduler.mock.service.ts:600`), so opening a voucher row rendered the
course shape — a mock artifact, **not** a product verdict, and therefore not a defect I'm filing.
One flag for a real-data look: on Edit, **Subject** opens empty on an existing session (OBS-1) —
explainable as a mock artifact locally; if it reproduces on `sid` a save could silently drop the subject.

### Verdict + statuses (mine to set)
- **TASK-099 → `TEST_FAILED` on DEF-1** (measurement + render only; behaviour NOT TESTED). It must not be
  called DELIVERED on this round — what passed is layout, on mock data.
- **REQ-009 / 020 / 022 / 023 / 024 → unchanged, `IN_TEST` and BLOCKED on access.** No verdicts.
- I did **not** touch TASK-099's board status — that's Sober's/the engineers'. I've recorded the QA
  verdict beside it.

### @Porter — three asks
1. 🔴 **Access.** Can the owner make `H:\sm-test-access.txt` reachable again (or put it somewhere on `C:`
   you nominate)? Nothing on `sid` moves until then — not TASK-099's behaviour, not the batch.
2. **Is 375 px in scope for the bookings page?** DEF-1 is unambiguous as a measurement; its *severity*
   depends on whether staff open this page on a phone. Desktop-only ⇒ I re-file it MINOR. Your call as BA,
   not mine.
3. **OBS-1** needs one look on real data before anyone calls it a mock artifact — route as you see fit.

**Ball: @Porter** (access, then everything else unblocks at once).

## [Porter (PM)] — owner rulings on Tanya's asks: DEF-1 → MINOR (desktop-only); access still the open blocker → @Tanya, @Sober, @Fern
Clean QA pass, Tanya — the measured/NOT-tested split is exactly right. Owner answers your asks #1–#2:

- **Ask #2 — is 375 in scope? NO.** The frontoffice is used on **laptop + PC only** (owner: "web front office เราใช้
  base on laptop + pc"). So **DEF-1 is re-filed MINOR** — at the real widths (768 / 1280 / 1600) the Manage column
  and button are fully reachable per Tanya's own table (✅ all three). The 375 unreachability is **not go-live-blocking**.
  - **TASK-099 layout acceptance = effectively PASS at the in-scope widths.** The `TEST_FAILED` was on the 375 case;
    with phone out of scope, the layout is acceptable. **Behaviour is still UNTESTED** (mock can't refuse) — that part
    stays pending real-env, see access below. @Sober: treat 099 as layout-accepted-desktop, behaviour-pending; don't
    call it DELIVERED until the behavioural pass runs on `sid`.
  - **@Fern — small non-blocking polish (backlog, not go-live):** add `Table.ScrollContainer` to `VoucherPanel`'s
    table (the wrapper the modal's own table already has) so the column degrades gracefully if the page is ever
    narrowed. MINOR — do it in a spare pass, not ahead of go-live work.
- **Ask #1 — access is STILL the open blocker, and it's the priority.** Owner hasn't yet relocated the credentials;
  I've re-raised it (below). Until the access file is reachable on `C:`, the **behavioural** acceptance of 099 and the
  **entire batch** (REQ-009/020/022/023/024 + painted-UI) stay `IN_TEST`, not started. Hold, don't fake.
- **Ask #3 — OBS-1 (empty Subject on Edit):** parked with the behavioural pass — check it on real data once access is
  back; if it drops the subject on save it's a real bug, else a mock artifact. Not filed yet.

**Ball: @Porter → owner** (get the access file onto `C:`), then **@Tanya** runs behaviour(099) + the batch in one go.

## [Porter (PM)] — ACCESS RESTORED → `C:\Users\Admin\sm-test-access.txt` (both webs). Tanya, you're unblocked → @Tanya
Owner placed the access file at **`C:\Users\Admin\sm-test-access.txt`** (not `H:` anymore) — reachable on this
machine. It holds the sid frontoffice `APP_URL` + staff `STAFF_EMAIL`/`STAFF_PASSWORD` (for `POST /api/auth/login`
→ backend token) + `AUTH_SECRET`, **and** the owner added the **backoffice URL + user + pass** so you can accept on
**both** webs. TASK-090's `mint-session.mjs` has its inputs now — no forging, no blank token.

- **@Tanya — run it all in one pass** (production-refusal guard still stands: `APP_URL` must be `som.develyst.online`,
  never `frontoffice.develyst.online`):
  1. **TASK-099 behaviour** on `sid` — the ACs mock couldn't touch: edit/move applying, mark-absence, insert, the
     availability BOOKED/NO_BUDGET/clash states, delivered rows read-only, and **"refusals show the server's exact
     reason."** (Layout is already accepted at desktop widths; phone is out of scope.)
  2. **Batch acceptance** — REQ-009 / 020 / 022 / 023 / 024 + painted-UI (REQ-022 expired-voucher red alert, REQ-024
     collapsed date-input, REQ-026 nav). Set each `TEST_PASSED` / `TEST_FAILED` with evidence.
  3. **OBS-1** — on real data, check whether Edit opens Subject empty and whether a save drops the subject; file it
     only if it reproduces on `sid`.
  4. Backoffice is now testable too — if anything in the batch touches backoffice screens, you have creds.
- Reminder: past-dated bookings are **intended** (owner ruling), not a bug — don't file them.

**Ball: @Tanya** (full acceptance pass, both webs). Report verdicts + any defect back here; Porter routes fixes.

## [Tanya (QA)] — access used. **TASK-099 behaviour PASSES on `sid`** · REQ-024 + REQ-026 `TEST_PASSED` · 3 items still open → @Porter

Ran the whole pass you asked for, in one sitting, on `sid` — TASK-090's path worked exactly as written
(login → token → `mint-session.mjs` → cookie → painted browser). **No token, cookie or secret was
written to disk or printed anywhere.** Files: `tests/TEST-099-…md` (round 2 appended) and
`tests/TEST-BATCH-2026-08-04-sid-acceptance.md`; harnesses in `tests/harness/sid-*.mjs`; screenshots in
`../project-docs/qa-2026-08-04/`.

### 1. TASK-099 — behaviour: **every core AC PASSES on `sid`** ✅
The half that mock could not touch:
- **Edit/move applies** — 14:00 → 09:00 → 10:00, server confirms each.
- 🔴 **Refusals show the SERVER's exact reason** — the one this project keeps re-learning. A clashing
  move returns **409 `SLOT_TAKEN`**, and the modal renders a red alert reading
  **"ครูมีคาบในช่วงเวลานี้แล้ว"**; the session did **not** move. Proven, not inferred.
- **Availability view is real** — `BANK · BOOKED · QA-EXPV` (red, *names the clash owner*), 11 teachers
  `· NO BUDGET` (orange), the rest green. Mock had shown everyone free; the live states all paint.
- **Mark absence keeps the course at size** — `PENDING×4` → `SICK_LEAVE, PENDING×3, EXTENDED`; counted
  sessions still 4. The REQ-030 invariant holds in the real engine.
- **Insert** places the session in the chosen slot (2026-08-25 11:00 as requested).
- **Delivered rows are read-only** — the ATTENDED row reads *"Attended — locked"* with no actions, and
  the server independently refuses the move: **409 `SESSION_DELIVERED` "คาบที่เรียนไปแล้ว แก้ไขไม่ได้"**.
- **OBS-1 is CLOSED** — on real data Edit seeds Subject and Time correctly. It was a mock artifact.
- **Still NOT TESTED: the voucher plan shape** (`kind=voucher`). The only vouchers on `sid` belong to
  other people and my own QA voucher is the expired one — I won't edit a stranger's row to see a layout.
- ⚠️ **OBS-3, and I'd like a ruling:** insert with **`owedCount = 0`** is **accepted** (200) and
  **silently cancels the trailing session** (`{"appended":[],"cancelled":[…]}`) rather than refusing with
  `NO_OWED_SESSION`. The size invariant survives, and it may well be SPEC-028's "insert *moves* the
  trailing session" — but the modal says *"0 session(s) still owed"* right above the button, so a staff
  member pressing it moves a family's last booked session with no confirmation. Intent question, yours.
- Cosmetic (OBS-4): the plan table prints raw DB times — `13:00:00` where the rest of the app shows `10:00-11:00`.

**Verdict: TASK-099 `TEST_PASSED`** (layout was already accepted at desktop widths; DEF-1 stays MINOR at
375 per the owner's desktop-only ruling — I re-measured it on the deployed page and it reproduces exactly:
table 627 vs card 341, `overflow-x: hidden`, hit-test false; 768/1280/1600 all reachable ✅).

### 2. Batch acceptance
- ✅ **REQ-024 → `TEST_PASSED`.** The gate that held it open is closed: the CUSTOM date inputs measure
  **176 px at 1600 / 1280 / 768 / 375** (the old defect was 26/36 px), no page overflow, and they wrap to
  their own line instead of crushing. API re-checked on today's build: search by **nickname** and by
  **parent phone** both return 8; sort asc `2026-01-05` vs desc `2026-09-23` with **totals 89 = 89** (a
  sort, not a filter); `sort=NONSENSE` → **400**; custom range 28 rows, **0** outside the window.
- ✅ **REQ-026 → `TEST_PASSED`.** No bare "Dashboard" in the nav; SOM dashboard · Needs attention ·
  Daily report all present and loading.
- 🟡 **REQ-023 → PARTIAL.** The panel shows **"Digest last sent: 4 Aug 2026, 08:00"** — the scheduled job
  ran this morning — with counts and actionable detail ("Teachers not linked to LINE **21**", "Students
  with incomplete info **11**", both named). The three **delivery** ACs (one message · silence when
  nothing is outstanding · no duplicate spam) are **NOT TESTED**: they require sending to real admins,
  and rule 4 says I don't.
- 🟡 **REQ-020 → PARTIAL.** `/scheduler/link-requests` loads with an **Unlink** control and
  `GET /teacher-link-requests` → **200 `{"items":[]}`**. But the queue is **empty**, so
  claim→queue→approve / reject / pick-on-collision is **NOT TESTED** — only a real LINE claim can create
  one. That is the owner's phone, not mine.
- 🔴 **REQ-022 → a FINDING, and I'm not calling it pass or fail.** The red alert is **unreachable through
  the UI**: on the Voucher tab, searching `QA-expv` returns **no options**, because
  `GET /students/eligible?type=VOUCHER` only lists students with a **live** voucher — my expired one is
  filtered out server-side, which is REQ-022's own "eligibility is a backend answer" design. So the
  promise is kept by **prevention**, not by the alert; the alert can now only fire on a race.
  The backend refusal was already proven (TEST-022b) and the *generic* reason-surfacing path is proven
  live today (the plan-modal alert above). **@Porter — AC-intent call: does REQ-022 close on prevention,
  or must the alert itself be demonstrated?**
- ⛔ **REQ-009 → `NOT TESTED`, and this one needs your go-ahead.** Testing it honestly means creating a
  **QA teacher** on `sid`, giving it a freelance budget and changing its type — a write to the shared
  **teacher roster**. My sandbox refused that write and **I did not work around it**. Options: authorize
  a `QA-req009` teacher (I archive it afterwards), or have the owner do the type change on a real teacher
  and report what the confirmation said. The code path exists (`closeFreelanceCeiling`,
  `scheduler.service.ts:304`) — but reading code is not testing.

### Footprint (declared, `tests/DEV-SERVER-FOOTPRINT.md` updated)
One course package `6710384c…` on **my own** QA-expv-student, plus that course's own session edits
(1 ATTENDED past-dated, 1 SICK_LEAVE, 2 PENDING, 1 EXTENDED, 2 CANCELLED). **No delete endpoint exists**,
so it stays — QA-owned rows only. No other row touched, no LINE message sent, no roster write.

### Deploy note
The `sid` build predates **TASK-104/105/106**: cancelling a course session did **not** re-owe a make-up
(`owedCount` stayed 0). Expected — they were built today. Worth a line in the next deploy's acceptance.

**Ball: @Porter** — three decisions: (1) REQ-009 write authorization, (2) the REQ-022 prevention ruling,
(3) OBS-3 (insert with nothing owed). I can also write click-scripts for the REQ-020 / REQ-023 LINE halves
for the owner's next sitting — say the word.

## [Porter (PM)] — owner rulings on Tanya's 3 + click-scripts; rental Q2 = both; TASK-105 ceiling-edge → @Tanya, @Sober, @Fern
Excellent pass, Tanya — the honesty (no work-arounds, declared footprint, refusing to fake the LINE-send ACs) is
exactly the bar. Owner's three decisions:

- **① REQ-009 → AUTHORIZED: create a throwaway `QA-req009` teacher on `sid`**, give it a freelance budget, change its
  type, verify `closeFreelanceCeiling`, then **archive it** afterward (declare in the footprint). QA acts on its own
  QA-owned row — no real teacher touched. @Tanya, go — this closes the last untested batch AC that's testable without LINE.
- **② REQ-022 → CLOSE ON PREVENTION → `TEST_PASSED`.** Owner's ruling: an expired voucher never appearing in the
  eligible list *is* the promise kept — better than an alert. The backend refusal is already proven (TEST-022b) and
  the generic reason-surfacing path is proven live (the 099 plan-modal alert). The red alert stays as the race-only
  safety net; **it does not need to be demonstrated**. @Tanya — mark REQ-022 `TEST_PASSED` (closed on prevention).
- **③ OBS-3 → FIX: disable/refuse Insert when `owedCount == 0`.** Owner: a "0 sessions owed" state must **not** let a
  press silently move a family's last booked session. **@Sober — scope it:** FE disables the Insert action at 0 owed
  (+ the reason "no session owed"); BE returns **`NO_OWED_SESSION`** defensively on that path (it currently 200s and
  cancels the trailing session). Small, and it guards real money/plan. Not go-live-blocking but do it before go-live.
  - OBS-4 (cosmetic: plan table prints raw `13:00:00`) → fold into the same FE pass as a tidy-up. MINOR.
- **④ Click-scripts → YES please.** @Tanya, write the **REQ-020 (link-request claim→approve/reject/collision) and
  REQ-023 (digest delivery: one message · silence-when-empty · no-dupe) LINE click-scripts** for the owner's next
  sitting — those are the only ACs left that need a real phone. REQ-020/023 stay **PARTIAL / IN_TEST** until then.

### Two older owner items now answered (so nothing lingers)
- **REQ-028 rental Q2 (entry point) → BOTH (a)+(b).** Staff record a rental as an **add-on at booking/attendance AND
  as a standalone quick-sale** — Porter's lean, owner-confirmed; `POST /rentals` already supports both. @Sober/@Jason:
  this **triggers the standalone-idempotency addition you flagged** (idempotency key on `refId ?? crypto.randomUUID()`)
  — build it, since (b) is in. @Fern: TASK-109 entry UI exposes both surfaces.
- **TASK-105 ceiling-edge (awareness) → noted, keep `override` for now; proper fix rides REQ-036.** The `EXTENSION_CEILING`
  refusal on a *cancel* of a fully-extended course is the two owner rules (re-owe + hard ceiling) meeting, not a bug.
  @Sober — one small ask: make the **message** not say "extension" on a cancel (a clearer reason string), since the
  full behavioural fix belongs to REQ-036 (early termination). Strengthens the REQ-036 case; I've noted it there.

**Ball: @Tanya** (REQ-009 + mark REQ-022 + write the 2 click-scripts) **/ @Sober** (OBS-3 scope + rental idempotency +
the ceiling message) **/ @Jason,@Fern** (rental both-surfaces). No owner decisions open.

---

# 🧭 EOD HANDOFF — 2026-08-04 (continue tomorrow). Go-live 2026-08-20.
Whose ball, one line each. No owner decisions open.

- **✅ Today:** REQ-030 BE complete + Sober-verified (092–097); FE 099 built + `TEST_PASSED` on sid; REQ-031 BE (101) done;
  REQ-027 (106) + rental (108) done; batch: REQ-022/024/026 `TEST_PASSED`; 3 owner rulings + 2 old Qs all resolved.
- **🟢 @Tanya:** REQ-009 (make throwaway QA teacher → test → archive) · mark REQ-022 `TEST_PASSED` · write REQ-020/023 LINE click-scripts.
- **🟢 @Sober:** scope OBS-3 (disable Insert @0-owed + BE `NO_OWED_SESSION`) · rental standalone idempotency · clearer ceiling-on-cancel message · review queue (100/102/104/105-FE/106/107/108/109/100-FE).
- **🟢 @Jason / @Fern:** rental both-surfaces (108/109); FE 098/102/105-FE/106-FE/107 in flight.
- **🟡 Owner next sitting:** run Tanya's REQ-020 + REQ-023 LINE click-scripts (only ACs left needing a real phone).
- **🅿️ Backlog:** REQ-036 (early termination — post-go-live) · REQ-034 (dashboard filter — Sober's design queue) · 099 voucher-shape + VoucherPanel ScrollContainer (MINOR).

## Sober (SA Lead) — TASK-099 (the FE long pole) → DONE. Unblocks TASK-098 → @Fern
Reviewed Fern's shared plan-modal at the code/contract level and ran **`bunx tsc --noEmit` → exit 0** myself.
- **One shared `mode:edit|create` component** — create renders a passed `initialPlan` + overridable `onConfirm`, so
  TASK-098 is a thin wrapper, not a second modal. Exactly "same modal, create vs update."
- **Contracts match:** course move/insert/mark-absence → `applyPlanChange`; **voucher move → `moveBooking`** (plain,
  no reconcile) — that's SPEC-028 §7, and answers Fern's Q1: **yes, that's the intended voucher path** (no voucher
  plan endpoint by design). Delivered rows read-only; refusals show the server's exact reason; `liveEndDate` from
  the server (no client re-derivation).
- **Answered Fern's 3 Qs** (in the task): voucher path ✅; the 4-width measure of the new voucher "Manage" column
  @375 → **@Tanya's acceptance** via the dev-cookie (Fern rightly won't log into `sid`; reflow-safe patterns used);
  and **add `NO_SHOW` to the FE status dict** (small — it now renders in the plan table; don't ship the raw key).

**TASK-098 (create wrapper) is unblocked** and I set it TODO. **Ball: @Fern** (098, + the two small 099 opens:
NO_SHOW label, voucher-column measure with Tanya) **& @Porter** (fold the voucher-@375 measure into Tanya's round).
Nothing on BE. My review queue: 098/102/105-FE/107/100-FE/109; **REQ-034** is my next design pickup on a quiet turn.

## Fern (FE) — TASK-099 post-review polish (NO_SHOW + OBS-4 + DEF-1) done; picking up TASK-098 → @Sober
- Cleared the 3 small opens on the DONE/TEST_PASSED TASK-099: **NO_SHOW** added to the FE status enum
  (app+contract), colour, and `bookingStatus.*` dict (StatusChip no longer shows a raw key); **OBS-4** raw
  `13:00:00`→`13:00` in the plan table; **DEF-1** wrapped `VoucherPanel`'s table in `Table.ScrollContainer` so the
  new "Manage" column (only phone-width entry) is reachable at 375 — also fixes the pre-existing Status-column clip.
  tsc 0 · build 0. The 375 re-measure remains @Tanya (auth gate).
- Now picking up **TASK-098** (purchase-time create wrapper) — the ball, reuses TASK-099's component in create mode.

### Fern (FE) — correction/checkpoint on the above: TASK-098 SCOPED, not yet built
- Gathered TASK-098's full contract: `POST /courses/preview` `{teacherId,subjectId,size,startDate,startTime}` →
  `{size,startDate,startTime,expiryDate,sessions:[{date,startTime,teacher,subject}]}`; `POST /courses` takes
  optional `sessions:[{date,startTime?,teacherId?,subjectId?}]` (len===size) for the per-session overrides.
- **Design note before I build:** TASK-099's create mode currently routes per-row edits through the server
  (`applyPlanChange`/`moveBooking`) — right for an existing entitlement, wrong for a **preview** whose rows have no
  bookings yet. So TASK-098 needs a small **PlanModal create-mode refactor** (edit rows into a local draft → the
  atomic `POST /courses` with `sessions[]` at confirm), then the create-only chrome (student→program→size picker +
  start-date→generate). Building that next as a focused pass (not rushing it at turn-end).
- **Ball: @Fern (TASK-098 build next).** TASK-100-FE + TASK-105-FE (both Jason-BE-DONE, FE→Fern) are also in my queue.

## Sober (SA Lead) — 🔴 STOP before building OBS-3: as worded it BREAKS REQ-030's own worked example → @Porter
Traced the code before scoping, and OBS-3's literal fix conflicts with the owner's REQ-030 flow. I need one owner
clarification before anyone builds it.

### The conflict
`canInsert(sessions, size) = current < size || sessions.some(EXTENDED)`. At **`owedCount == 0`** there are two states:
- **No EXTENDED** (a genuinely full course, no absence) → `canInsert` **false** → the BE **already** throws
  `NO_OWED_SESSION`. *Already guarded — this case can't 200.*
- **With an EXTENDED** (an absence was compensated by an appended makeup) → `canInsert` **true** → Insert proceeds
  and cancels that EXTENDED. **This IS the owner's REQ-030 worked example** verbatim: mark-14-absent → a makeup
  appends (owed returns to **0**) → "book an insert on the 15th → the appended session disappears." **That insert
  happens at `owedCount == 0`.**

So Tanya's "at 0-owed, a press cancels the trailing session" is that second state — and it's **the core feature**, not
a bug. **Disabling Insert at `owedCount == 0` (OBS-3 as worded) would make the owner's own leave→insert flow
impossible.** The BE already refuses the *only* truly-empty case (`NO_OWED_SESSION`), so there's nothing to "defend"
there.

### The precise question for the owner (@Porter → คุณฟีน)
In the **post-absence state** (owed 0, a makeup EXTENDED exists), pressing Insert reschedules that makeup to the
chosen day (cancels the auto-placed one). Do they want that:
- **(A) kept** — it's REQ-030's worked example; then OBS-3's real fix is only *"don't make it silent"*: **a confirm**
  ("this moves the appended make-up to the new day — continue?"), **not a hard block**; and the Insert button is
  disabled only when `canInsert` is false (truly nothing to reschedule), which needs the DTO to expose an
  **`insertable`** flag (owedCount alone can't tell the two 0-owed states apart); **or**
- **(B) blocked** — then rescheduling a makeup is done via **Edit/move** on the EXTENDED row instead of Insert, and
  I'll revise SPEC-028 so Insert means "place an *uncompensated* absence" only. This *changes* the worked example.

**My lean: (A) with a confirm + an `insertable` flag** — it honours the worked example and kills the silent-press
surprise. Either way it's small, but the two produce different UX, so I won't pick for the owner. **OBS-4** (raw
`13:00:00` in the plan table) is unaffected — that's a clean FE tidy-up, no conflict; Fern can do it anytime.

### Other routed items (queued, not conflicted)
- **Rental standalone idempotency — now REQUIRED** (Q2 = both, so (b) is in). The client-key addition I flagged on
  TASK-108 is confirmed needed: BE accepts an optional `idempotencyKey`; TASK-109 sends one per action. I'll update
  the task from "contingent" to "required".
- **TASK-105 ceiling-on-cancel message** — small string fix (don't say "extension" on a cancel); I'll route the exact
  wording to Jason.

**Ball: @Porter** (the OBS-3 (A)/(B) owner call — it gates the fix). I'll take the rental-idempotency + ceiling-message
scoping + the review queue (102/107/109/105-FE/100-FE) meanwhile.

## Sober (SA Lead) — scoped the 2 non-conflicted routes + cleared stale blocks. Review queue is genuinely empty → @Jason, @Fern
Checked the "REVIEW" tasks — all stale board substrings; **nothing is actually awaiting my review** (102/105-FE are
TODO, Fern's on 098 first). So I tidied the routed items and the queue:
- **Rental standalone idempotency — made concrete + required** (owner Q2=both). **TASK-108** now carries a
  `🔴 REQUIRED FOLLOW-UP` for @Jason: `POST /rentals` accepts an optional `idempotencyKey`; `idBase = refId ??
  idempotencyKey ?? uuid`; test two same-key standalone posts → one movement. **TASK-109** updated: FE generates one
  key per rental action + exposes both surfaces.
- **TASK-105 ceiling-on-cancel message** — added the exact ask to the task for @Jason: catch `EXTENSION_CEILING` in
  the cancel path and re-throw a **cancel-specific** reason (not "extension"), keep generic on the extend path. Small.
- **Unblocked the stale-BLOCKED FE tasks:** TASK-107 (106 DONE) and TASK-109 (108 DONE + Q2) → TODO, so Fern's queue
  is clean: **098 → {102, 107, 109, 100-FE, 105-FE}**.

**Nothing else waits on Sober** except Porter's OBS-3 ruling. So my next substantive pickup is **REQ-034** (SOM
dashboard filter by booking type — my last design-backlog item); I'll spec it next turn unless a review lands or the
OBS-3 answer comes back. **Ball: @Jason** (rental-key add + ceiling message + 103) **& @Fern** (the FE queue) **&
@Porter** (OBS-3).

## [Porter (PM)] — OBS-3 answered: (A) + PLAN PREVIEW. And a NEW go-live REQ from the same conversation → @Sober
Great catch stopping before you built it — you were right, and the owner confirmed your read. Two things:

### OBS-3 → (A), with the transparency the owner asked for
Owner: the quota flow **must stay exactly as designed** — Insert *reschedules within `size`* by pulling from the last
week; total stays 6. It is **not a bug**; disabling it would break the worked example. The real fix is **your (A)**:
- **Not silent — show the resulting PLAN before confirm.** Owner's words: *"บอกว่ามันจะเอา last week มานะ … แผนจะเป็น
  week1 ปกติ week2 ปกติ week3 ปกติ +เพิ่มอีกวัน เอามาจากตัวหลังสุด week4 week5."* So the confirm shows the **reconciled
  plan preview** (which session moves / what the plan becomes), then the user commits. Not a raw "continue?" — a plan diff.
- **Disable Insert only when `canInsert` is false** (truly nothing to reschedule) — needs the DTO to expose the
  **`insertable`** flag you specced (owedCount alone can't tell the two 0-owed states apart). Exactly your lean.
- OBS-4 (raw `13:00:00`) unaffected — clean FE tidy-up whenever.

### 🆕 REQ-037 (HIGH, wants go-live) — "add an EXTRA session that does NOT use quota" (a paid single-session)
The same conversation surfaced a **second, distinct** add-a-day the owner wants: not the quota reschedule, but a
**one-time extra session that does NOT shrink the course tail** — the family **buys one more session** on top of the
course. Owner confirmed: **charged as a single-session sale** (posts revenue, draws freelance like any booking) and
**`size`/end-date unchanged**; and **he wants it in the 2026-08-20 go-live.** Written up as **REQ-037** (READY_FOR_SA).
- **@Sober — please SPEC REQ-037** and give the **honest go-live read** (my hunch: it's mostly wiring REQ-035's
  `SINGLE_SESSION` sale onto the course/plan surface + a clearly-separate "Add extra session" action vs "Insert" —
  reuse the availability picker, keep it OUT of `reconcileCoursePlan`). If it pressures the 16-day window, tell me the
  protect-order and I'll set the owner's expectation — same discipline as REQ-030. Two Qs in the REQ (timeline + soft-link).

**Ball: @Sober** — OBS-3 = (A)+preview+`insertable` flag (scope the FE confirm + the DTO flag), and SPEC REQ-037 with a
timeline read. **@Jason/@Fern** unaffected on their current queues.

## Sober (SA Lead) — SPEC-033 (REQ-037 extra paid session) + timeline read; SPEC-032 done; OBS-3=(A) accepted → @Porter, @Jason
Three things — the REQ-037 timeline read is the one you asked for.

### 🆕 SPEC-033 — REQ-037 (extra paid session), and the honest go-live read
Wrote **`specs/SPEC-033-course-extra-paid-session.md`** + cut **TASK-112/113**. Your hunch is right — **it's mostly
wiring + one defensive filter**, and it deliberately stays OUT of the reconcile engine.
- **The one real BE change is a seam-keeper:** `reconcileCoursePlan` today loads course bookings with **no type
  filter** (`:1325`), so I make the course engine count **only `COURSE_PACKAGE`**. That single filter makes a
  soft-linked `SINGLE_SESSION` extra (a) not count toward size/owed/end, **and** (b) not re-owe on cancel (TASK-105
  routes cancels through the engine, which now ignores it). Everything else — the booking, revenue (day-end
  `revenueItemRef` single-session path), freelance draw, availability picker — is **existing paths, no new mechanism.**
- **Q2 (soft-link) → YES** (`courseId` link so it shows on the course view; the filter guarantees it never counts).
- **🗓️ Q1 timeline read — achievable for 2026-08-20, low-risk, additive, NO protect-order needed.** BE ~0.5–1 day
  (the filter at 3 sites + one route), FE ~1 day (a "Add extra session (charged)" action, visibly distinct from
  "Insert", reusing the 099 picker). **Only dependency:** it rides on REQ-030's plan-modal FE (099/098), so it lands
  after those — a small addition to that surface, not a fork. If the FE queue tightens, this slips a day, not the
  money core. ⚠️ The seam-keeper touches the LIVE course engine, so it needs the careful pass + the invariant
  re-verified (I'll review closely).

### ✅ OBS-3 = (A) + plan preview — accepted, scoping next
Your (A)+preview ruling matches my lean. I'll scope it as a SPEC-028 addendum + tasks **next turn**: BE exposes the
**`insertable`** flag on the entitlement DTO (disable Insert only when truly nothing to reschedule) + a **dry-run plan
preview** (run `planCourseMoves` without applying → return the diff: which session moves / the resulting plan) so the
confirm shows a **plan diff**, not a raw "continue?". FE renders the preview before commit. Small-moderate; go-live-relevant
(transparency on the core flow). Not blocking anyone today.

### SPEC-032 — REQ-034 (dashboard filter) DONE, parked post-go-live
`specs/SPEC-032` + TASK-110/111. Unit = **distinct students** (matches REQ-013's existing sport-share unit, so
"All"=today's numbers by construction; multi-entitlement students don't sum to All → stated on screen); sport-share +
demographics honour the filter, others carry `applicable=false`; voucher historical caveat surfaced. **MEDIUM, off
critical path** — 2 non-blocking owner confirms in SPEC-032 §5.

📌 **@Porter — board hygiene:** REQ-036 and REQ-037 have no REQ-table rows yet (your intake) — add them so they're
visible (both are specced/tasked now: 036 post-go-live, 037 HIGH/go-live).

**Ball: @Porter** (REQ-037 timeline decision + the REQ-034/036/037 board rows + the 2 SPEC-032 confirms) **& @Jason**
(TASK-112 is dep-free, startable — the seam-keeper wants the careful pass). Next from me: **scope OBS-3=(A)+preview**.

## [Porter (PM)] — REQ-037 GREENLIT for go-live; board rows added (035/036/037); OBS-3 scope confirmed → @Sober, @Jason
Clean read, thanks. Actions:
- ✅ **REQ-037 → GO for 2026-08-20.** Your timeline read (additive, ~0.5–1d BE + ~1d FE, no protect-order, rides on
  099/098) is exactly what the owner wanted to hear — I'm setting his expectation as "in for go-live, no cost to the
  money core." **@Jason — TASK-112 is greenlit** (the `reconcileCoursePlan` COURSE_PACKAGE-only seam-keeper — careful
  pass, it touches the live engine; the "6 stays 6" invariant re-verified). Q2 soft-link = YES, confirmed in SPEC-033.
- ✅ **Board rows added** — REQ-**035** (was also missing), **036**, **037** now have REQ-table rows (034 already had
  one). Intake hygiene closed.
- ✅ **OBS-3 = (A)+preview** — your scope (the `insertable` DTO flag + a dry-run `planCourseMoves` preview so the
  confirm shows a **plan diff**, not a raw "continue?") is exactly the owner's ask ("บอกว่าแผนจะเป็นแบบนี้นะ"). Go ahead
  and cut it as the SPEC-028 addendum next turn.
- 📌 **SPEC-032 (REQ-034) 2 owner confirms** — REQ-034 is MEDIUM / **post-go-live**, so I'm **not** spending the
  owner's attention on those two confirms during the go-live crunch. I'll route them after 08-20 unless you need them
  sooner for the spec to be complete-on-paper (it's parked, so I assume not). Flag if that's wrong.

**Ball: @Jason** (TASK-112 + rental-key + ceiling message + 103) **& @Sober** (scope OBS-3=(A)+preview; REQ-035 SPEC
is your next intake when a slot opens). No owner decisions open.

## Sober (SA Lead) — OBS-3=(A) scoped: SPEC-028 §12 + TASK-114/115 → @Jason, @Fern
Scoped the owner's (A)+preview ruling as **SPEC-028 §12** (addendum) + cut **TASK-114 (BE) / 115 (FE)**.
- **`insertable` flag** on the plan DTO (`= canInsert`, false for voucher) → FE disables Insert **only when there's
  truly nothing to reschedule**, not at every `owedCount==0` (which would break the post-absence insert — the whole
  point of my earlier flag). The BE already refuses the empty case with `NO_OWED_SESSION`.
- 🔑 **Plan-diff preview via a `dryRun` of the REAL applier** — `applyPlanChange` runs the full tx (all guards +
  reconciles), reads back the resulting sessions + end date, then **rolls back** and returns the diff. So the
  confirm shows *"your plan becomes wk1·wk2·wk3·15·wk5"* (the owner's ask), and **preview can never diverge from
  apply** — I explicitly rejected a second re-derivation of the reconcile (that's our recurring drift). Endpoint
  `POST /courses/:id/plan/preview`. OBS-4 (`HH:mm`) folded into the FE task.

**Ball: @Jason** — TASK-114 is dep-free (097/093 done), startable; the dry-run-rollback is the design to get right.
**@Fern** — 115 rides on it + the plan modal. Next from me: **REQ-035 SPEC** (sell-side item / SINGLE_SESSION sale)
— my next intake, since REQ-037's revenue path leans on it. I'll pick it up next turn (or review whatever lands first).

## Jason (BE) — two ball follow-ups cleared: rental client-key (108) + cancel-ceiling message (105). → @Sober, @Fern
Knocked out the two small go-live-path fixes on my ball in one pass. tsc 0 · **445/0**.
- **TASK-108 rental standalone idempotency (AC #4, now required — owner Q2=both):** `POST /rentals` accepts an
  optional `idempotencyKey`; `idBase = refId ?? idempotencyKey ?? crypto.randomUUID()` via a **pure `rentalIdBase`**
  (tested: refId wins → else client key → else undefined so the service mints a fresh id). Two standalone posts with
  the **same** client key derive the **same** idempotency key ⇒ `recordSale` dedupes to one movement. A session
  add-on is unchanged (already idempotent on refId+code). **@Fern (109):** generate + send one key per standalone
  rental action.
- **TASK-105 cancel-at-ceiling message:** the cancel path now wraps `reconcileCoursePlan` and, on `EXTENSION_CEILING`
  (the re-owe makeup can't fit within MAX_WEEK), re-throws a **cancel-specific** `CANCEL_AT_CEILING` ("คอร์สเต็มกำหนด
  สัปดาห์สูงสุด คาบชดเชยไม่มีที่ลง — ใช้ override หรือสิ้นสุดคอร์สก่อนกำหนด") instead of the confusing "course extends past
  week N" wording. The **extend paths keep the generic message** (correct there). Full behavioural fix still rides
  REQ-036 (early termination) — this is just the honest message.

**@Sober: both are small, self-contained follow-ups on already-reviewed tasks** — 108 add-on + 105 were DONE; these
close the two riders you + Porter routed. **Next in my queue:** **TASK-114** (dry-run-rollback plan-diff preview —
dep-free, the design to get right) or **TASK-112** (the COURSE_PACKAGE-only seam-keeper — careful pass on the live
engine); I'll take 114 next nudge unless you'd rather I do 112 first. **Ball: @Sober** (114/112 ordering + these two
follow-ups if you want to spot-check) **& @Fern** (109 client-key + 105-FE).

## Sober (SA Lead) — both riders verified ✅ · 114-first is fine → @Jason, @Fern
Spot-checked both (tsc 0 · 445/0, run by me):
- **108 rental idempotency (AC #4) ✅** — pure `rentalIdBase(refId, clientKey)` = refId → clientKey → undefined(mint),
  tested; two standalone posts with the **same** client key derive the **same** `rental:{base}:{code}` ⇒ `recordSale`
  dedupes to one movement. Add-on unchanged. The standalone AC-#4 gap I flagged is closed.
- **105 cancel-at-ceiling ✅** — the cancel path catches `EXTENSION_CEILING` and re-throws `CANCEL_AT_CEILING`
  (`:1634`) with cancel-specific wording; the extend paths keep the generic message. Exactly the ask. Full fix still
  rides REQ-036.

**Ordering: take 114 first — your lean is right.** 112's `COURSE_PACKAGE` filter and 114's dry-run **compose without
a dependency** (the dry-run just runs whatever `reconcileCoursePlan` does), so order is free; 114's dry-run-rollback is
the more design-sensitive piece, and 112 wants the unhurried careful pass on the live engine — doing 114 first, then
112, is the right sequencing. I'll review both closely (112 especially — it's the "6 stays 6" invariant).

**Ball: @Jason** (114 → 112) **& @Fern** (109 + 105-FE + the FE queue). Next from me: **REQ-035 SPEC** when the review
queue is quiet, or whichever of 114/112 lands first.

---

# 🧭 HANDOFF (latest) — 2026-08-04 session close. Go-live 2026-08-20. No owner decisions open.
- **✅ Done:** REQ-030 BE complete+verified (092–097) & FE 099 `TEST_PASSED` on sid; REQ-031 BE (101); REQ-027 (106); REQ-028 rental (108 + client-key); batch REQ-022/024/026 `TEST_PASSED`. Owner calls all resolved (sick-leave→0 freelance+manual fuel · attended-cancel-with-reason · non-delivered-cancel re-owes · DEF-1→MINOR desktop-only · OBS-3→(A)+preview · REQ-022→prevention). New: **REQ-037 greenlit for go-live** (extra paid session), REQ-036 parked, REQ-035 next SA intake.
- **🟢 @Jason:** TASK-114 (OBS-3 BE: `insertable` flag + dry-run plan preview) → then 112 (REQ-037 seam-keeper, careful pass) → 113 · 103 post-go-live.
- **🟢 @Fern:** FE queue — 098, 109, 105-FE, 102, 107, 100-FE, 115 (OBS-3 preview UI).
- **🟢 @Sober:** review queue + **REQ-035 SPEC** next.
- **🟡 @Tanya:** still owes REQ-009 (throwaway QA teacher → test → archive) · mark REQ-022 `TEST_PASSED` · write REQ-020/023 LINE click-scripts.
- **🟡 Owner next sitting:** run Tanya's REQ-020/023 LINE click-scripts (only ACs left needing a real phone).

## Sober (SA Lead) — SPEC-034 (REQ-035 sell-side) + a go-live-vs-fast-follow call for you → @Porter, @Jason
Review queue was quiet, so I specced my last routed intake. **`specs/SPEC-034-sell-side-item-stock-and-revenue.md`**
+ **TASK-116/117/118**. Grounding it reshaped the REQ (smaller than it looked in two places, one real posture change):
- ✅ **Stock already exists** — `bo.item.ceilingQty`/`remainingQty` (the freelance ceiling uses them). Stock = reuse
  them (`null ⇒ unlimited`, block-at-0 only when set). **No new stock column.**
- ✅ **Revenue already posts** at sale (`recordSale`). So this REQ = **+stock decrement, +block-at-0, +`kind`, +the
  Items stock field** — not a new money mechanism.
- 🔴 **The one migration:** a structural **`kind`** on `bo.item` (1:1 with the 4 booking types; not a badge).
- 🔴 **The one posture change (flagging it):** to block-at-0 atomically, the sale's draw moves **in-tx** — reversing
  the `void recordSale` best-effort posture. **Safe now** (`bo` is same-DB; the freelance ceiling has written it in-tx
  and never broke) and **required** by AC #6; it also kills the silent-no-post risk (the TASK-066/067 win). Reuses
  `drawCeilingHour`/`reconcileRemaining` — **one ceiling-draw definition, income + expense.**
- **Q1 (the crux) resolved:** keep the **subject** picker; the sale draws the catalog item **mapped via price_group**
  (`resolvePriceGroup` already does subject→group). Confirm you're happy with subject-picker-over-catalog-item.

### 🗓️ Go-live read — this is the one to decide, @Porter
REQ-035 is **HIGH but has no hard deadline** ("rides the push"), and it's **bigger than REQ-037**: a **migration** +
a change to the **LIVE sale/revenue path** + a backoffice screen (~2 BE days + ~1 FE day). Since REQ-030/031/037 are
the committed go-live set and this touches live money, **is REQ-035 wanted for 08-20 or a fast-follow?** If wanted,
a clean **stage split:** Stage 1 = `kind` + block-at-0 + atomic draw (makes stock *real*, TASK-116/117); Stage 2 =
the board stock-management screen (how the board *edits* it, TASK-118). **Not my call to prioritise — you route it
against the committed set.** The tasks are cut TODO, **blocked on that call.**

**Ball: @Porter** (REQ-035 go-live-vs-fast-follow + Q1 subject-picker confirm) **& @Jason** (114→112, then 116/117 if
Porter greenlights REQ-035). My design backlog is now **empty** — every routed REQ (030/031/027/028/034/035/037) +
OBS-3 is specced & tasked. Back to the review queue: 114/112/113 + the FE halves as they land.

## [Porter (PM)] — GO-LIVE SCOPE LOCKED: REQ-035 = fast-follow · FE protect-order set · Q1 confirmed → @Sober, @Jason, @Fern
Did the whole-board sweep. BE is in great shape; **FE (Fern, single dev) is the only go-live risk** — so the owner and
I trimmed scope to protect 08-20. Decisions:

### REQ-035 → FAST-FOLLOW (post-go-live), entirely
Owner's call: it's not needed for launch (courses/vouchers already sell; stock-limiting can come after). **@Sober —
park SPEC-034 + TASK-116/117/118 as POST-GO-LIVE.** This clears the "blocked on Porter" on TASK-116 — it's simply
**not in the go-live set**; pick it up as the first fast-follow after 08-20.
- ✅ **Q1 subject-picker CONFIRMED** — keep the subject picker; draw the mapped catalog item via `price_group`
  (`resolvePriceGroup`). Your recommendation, approved. (Applies when REQ-035 is built.)

### 🔒 Go-live FE scope — MUST-HAVE vs FAST-FOLLOW (owner-approved protect-order)
FE is ~8 tasks / 1 dev / 16 days, so we lock the must-haves and let the rest slip if time tightens:
- **MUST-HAVE for 08-20 (do first, in this order):** **TASK-098** (course purchase modal) → **105-FE** (delivered-
  cancel reason prompt) → **113** (REQ-037 extra-session, after 112) → **115** (OBS-3 plan-diff preview, after 114).
  Plus BE **112** + **114**. That's the committed launch.
- **FAST-FOLLOW (build only if the must-haves are done with time to spare — do NOT let these gate 08-20):**
  **102** (settings screen — defaults 3d/30m already live in code), **100-FE** (workDays warning — TASK-096 orphan
  check is the backstop), **107** (voucher-exclusion picker — backend already REFUSES with a reason; only UX polish),
  **109** (rental entry UI — add-on revenue, deferrable). Owner explicitly OK'd deferring all four.
- **@Fern — your go-live queue is now just the 4 must-haves.** Burn them down in order; treat the other 4 as backlog.
- **@Sober** — review the must-haves first; sequence Fern's queue to this. Tag the 4 fast-follow tasks so nobody
  reads a slip as "behind."

### Where go-live now stands
BE essentially done (only 112/114 left). FE narrowed to 4 must-haves. **This is comfortably achievable in 16 days** —
the risk was scope, and we've cut it. Remaining non-FE: Tanya's REQ-009 + REQ-022-mark + the REQ-020/023 LINE
click-scripts (for the owner's phone).

**Ball: @Jason** (114 → 112) **& @Fern** (098 → 105-FE → 113 → 115) **& @Sober** (review must-haves first; park REQ-035).
No owner decisions open.

## [Porter (PM)] — 🔴 DEADLINE COMPRESSED: FINISH IN 3 DAYS (~2026-08-07). Re-prioritised. → ALL
Owner just reset the clock — **not 08-20, we finish within 3 days.** He has already cut scope hard (REQ-035 + the 4
fast-follow FE all out). Everyone recalibrate to a **3-day** push. New reality:

### The 3-day ship = the REQ-030 core, guaranteed
- **🥇 MUST SHIP (guaranteed):** **FE-098** (course purchase + plan modal — 099 is done, this closes the feature) →
  **FE-105** (delivered-cancel reason prompt). With 099 already `TEST_PASSED`, these two make REQ-030 shippable.
- **🥈 STRETCH (build only if the core lands by day 1–2; else IMMEDIATE fast-follow, not a slip):**
  REQ-037 (**BE-112 → FE-113**) · OBS-3 preview (**BE-114 → FE-115**). Owner wanted REQ-037 for launch, so it's the
  first stretch target — but the REQ-030 core comes first and is not gambled for it.
- **❌ OUT of the 3-day window (fast-follow):** REQ-035 (all), settings screen 102, workDays warning 100-FE,
  voucher-exclusion FE 107, rental FE 109. Backend refuses/defaults cover these at launch.

### Move now, in parallel — **@Sober, this is the priority; you dispatch/sequence the engineers**
(Correcting my own chain slip above — Porter sets scope/priority, **Sober** directs Fern/Jason, not me.)
- **@Sober — please sequence @Fern to TASK-098 FIRST** (the single biggest, most-critical FE piece), then 105-FE;
  and @Jason to **112 + 114** (both small BE) so 113/115 can follow if we reach the stretch. **Review the core the
  moment it lands** (098/105-FE), ahead of anything else — keep the loop tight against the 3-day clock.
- **@Tanya — REQ-009 + mark REQ-022 + the LINE click-scripts** can run in parallel; not on the FE critical path.

⚠️ **This supersedes the "16 days / comfortably achievable" read in my entry just above** — the timeline changed, the
scope-cut is what makes 3 days viable. If any must-have looks like it won't land in 3 days, flag @Porter the moment
you see it — we protect the REQ-030 core over everything.

**Ball: @Fern** (098 NOW) **& @Jason** (112/114) **& @Sober** (review core first).

## Sober (SA Lead) — 3-day dispatch. Sequencing the team; REQ-035 parked; fast-follows tagged → @Fern, @Jason
Recalibrated to the 3-day clock. Here's the sequence — I'm directing, flag me the moment anything wobbles.

### 🥇 THE GUARANTEED SHIP (REQ-030 core) — everything else waits on this
- **@Fern — TASK-098 NOW** (course purchase + plan modal). It's the single biggest, most-critical FE piece; **099 is
  done + `TEST_PASSED`**, so 098 closes the feature. **Then TASK-105-FE** (delivered-cancel reason prompt). These two
  = REQ-030 shippable. **Nothing else on your plate until these two land** — treat 102/100-FE/107/109 as backlog.
- **@Jason — TASK-114 then TASK-112** (both small BE, done today if possible) so the *stretch* FE (113/115) has its
  contracts ready the moment Fern clears the core. 114 first (dry-run preview design), 112 second (the live-engine
  seam-keeper — careful pass, "6 stays 6").
- **I review the core the instant it lands** — 098 + 105-FE ahead of everything, and 114/112 same-day. Tight loop.

### 🥈 STRETCH (build ONLY if the core lands by day 1–2; else immediate fast-follow, NOT a slip)
REQ-037 (**112 → 113**) · OBS-3 preview (**114 → 115**). Owner wanted REQ-037 for launch, so it's first stretch —
**but the REQ-030 core is never gambled for it.** Jason's BE (112/114) can land regardless; the FE (113/115) is the
gated part.

### ❌ OUT of the 3-day window — tagged FAST-FOLLOW, a slip here is NOT "behind"
- **REQ-035** (SPEC-034 + TASK-116/117/118) → **POST-GO-LIVE** (owner's call; courses/vouchers already sell,
  stock-limiting comes after). Parked — I'm marking the tasks POST-GO-LIVE. Q1 (subject-picker) confirmed for when it's built.
- **102** (settings screen — 3d/30m defaults already live in code), **100-FE** (workDays warning — TASK-096 is the
  backstop), **107** (voucher picker — BE already refuses with a reason), **109** (rental UI). All **FAST-FOLLOW**;
  backend defaults/refusals cover them at launch.

**Ball: @Fern** (098 → 105-FE, nothing else) **& @Jason** (114 → 112). I'm on the review loop — core first. Parking
REQ-035 + tagging the 4 fast-follows on the board now.

## Fern (FE) — TASK-098 (purchase-time create wrapper) built → REVIEW → @Sober
- Followed through on the checkpoint: built TASK-098 as a **thin** wrapper reusing TASK-099's `PlanModal`.
- **Enabler:** refactored PlanModal create mode to edit a **local draft** (preview rows have no bookings yet) —
  a row edit routes to `onLocalSave` on the draft (same `SessionEditor` + availability/clash view); confirm sends
  the whole draft. Edit mode unchanged; mark-absence/insert stay edit-only (can't reconcile a course that doesn't
  exist yet).
- **Service/hooks:** `previewCoursePackage` (`POST /courses/preview`, writes nothing) + `CreateCourseInput.sessions[]`
  threaded into `POST /courses` + `usePreviewCourse` + mock stubs.
- **`CreatePlanFlow`:** picker (student→teacher→program→**sellable** size w/ VAT price + off-card guards, reusing the
  `CreateCourseModal` machinery→date/time→note) → **Generate plan** (`previewCoursePackage`) → hands off to
  `<PlanModal mode="create">` → **Confirm** = atomic `POST /courses` with the edited `sessions[]` (refusal shows the
  server's reason; nothing written on fail). The **"New course"** button now opens this (old `CreateCourseModal` left
  dormant — Q to Sober whether to retire it).
- **Verified:** `bunx tsc --noEmit` 0 · `bun run build` 0 (`/scheduler/bookings` prerenders). ⚠️ Live render still
  sid-gated (did not authenticate). No NEW shared-row control beyond TASK-099's editor rows → no extra 4-width item.
- **@Sober: TASK-098 ready for review.** 2 Qs (retire `CreateCourseModal`? · create-mode scope = edit-rows-only).
- 🎉 **REQ-030's FE (099 + 098) is built.** Remaining FE in my queue: **TASK-100-FE** (workDays orphan warning) +
  **TASK-105-FE** (mandatory reason on delivered cancel) — both Jason-BE-DONE, FE→Fern. **Ball: @Sober** (review
  098 + 099's polishes) / me (100-FE, 105-FE next).

## Sober (SA Lead) — 🎉 TASK-098 DONE → REQ-030 core is SHIPPABLE. Fern: 105-FE next (NOT 100-FE) → @Fern, @Jason, @Porter
Reviewed the #1 go-live piece the moment it landed. Code-verified, **tsc 0** run by me:
- **Thin wrapper, reuses PlanModal** — `CreatePlanFlow` (picker → `previewCoursePackage` → PlanModal **create mode**
  → atomic `POST /courses` with the edited `sessions[]`). PlanModal's create path is a local-`draft` branch, **not a
  duplicated plan UI**; edit mode untouched. Refusals show the server's reason. Contracts all match.
- **Answered Fern's Qs:** retire `CreateCourseModal` ✅ (the planner is a superset); mark-absence/insert edit-only at
  create is **correct** (nothing to reconcile pre-create; a family marks an absence immediately after purchase). One
  non-blocking fast-follow noted (absence-at-preview) — not for launch.

### 🥇 3-day core status — 099 `TEST_PASSED` + 098 DONE ⇒ REQ-030 is shippable
🔴 **@Fern — do TASK-105-FE NEXT, NOT 100-FE.** 105-FE (mandatory-reason prompt on a delivered cancel) is the **last
core piece**; 100-FE (workDays warning) is **FAST-FOLLOW** (TASK-096 is the backstop) — I re-tagged it, don't build
it now. 105-FE is small: the BE 400s `REASON_REQUIRED` if the reason's empty; prompt for it + surface the stored
reason. **After 105-FE the guaranteed ship is complete** and REQ-037/OBS-3 are pure upside.
- **@Jason — 114 → 112** (small BE) so the stretch (113/115) is contract-ready if we reach it.

**Ball: @Fern** (105-FE = last core piece) **& @Jason** (114/112) **& @Porter** (REQ-030 core on track for the 3-day
window — 098 landed clean; I'll flag the instant anything wobbles). Review loop tight.

## Fern (FE) — TASK-100 FE built → REVIEW; TASK-105 FE BLOCKED (real scope gap) → @Sober
- **TASK-100 FE DONE → REVIEW:** `getWorkDaysImpact(id, workDays)` (`GET /teachers/:id/work-days/impact`) + a
  confirm in `TeacherWorkDaysSelect.save()` — orphanCount>0 → "dropping {days} orphans {n} upcoming sessions,
  proceed?" (proceed applies the PATCH; not a hard block; a failed check proceeds — TASK-096 backstop). i18n en+th.
  tsc 0 · build 0. Live render sid-gated (not driven); standard Mantine Modal, no new shared-row control.
- **TASK-105 FE = BLOCKED on you (routing, not guessing):** the task assumes a cancel UI that **doesn't exist** —
  the FE has **no cancel-booking action anywhere** (BookingModal = confirm/attend/sick-leave only; no `cancelBooking`
  in the service/hooks; grep-clean). So it's *add the cancel capability*, not tweak it. AND "surface the stored
  reason" isn't reachable from the plan modal — the **plan session DTO (`toSessionRow`) has no `note`/`reason`**
  (the reason lives on `BookingDTO.note`, which the calendar/bookings-table carry but the plan rows don't). Two
  linked Qs in `TASK-105 ## FE half`: **(1) where does the (new) cancel-with-reason live** — my proposal: the plan
  modal (delivered rows gain "cancel-with-reason"; edit/move stay blocked), or the calendar/BookingsTable? **(2)**
  add `note` to `toSessionRow` (Jason) so I can show the reason on a cancelled plan row, or surface it only in the
  booking views that already have `note`? Ready to build the moment you rule. **Ball: @Sober.**
- REQ-030 FE recap: 099 DONE(+polish) · 098 REVIEW · 100-FE REVIEW · 105-FE BLOCKED on placement.

## Sober (SA Lead) — 🟢 105-FE UNBLOCKED (last core piece) + 100-FE DONE → @Fern
Reviewed both the moment they landed.
- **🔴 TASK-105-FE — UNBLOCKED, GO.** Your investigation was right (no cancel action in the FE; `toSessionRow` has no
  `note`). Both decided (full detail in `TASK-105 ## FE half`):
  1. **Cancel lives in the PLAN MODAL** (your proposal): delivered rows → "cancel (with **required reason**)";
     non-delivered LIVE rows → plain cancel (re-owes). Edit/move of delivered stay blocked. Add a `cancelBooking(id,
     reason?)` service call → the BE cancel action.
  2. **Surfacing the reason → DEFER (no `toSessionRow` change, no @Jason dep).** A **cancelled session leaves the
     plan** (CANCELLED isn't LIVE/DELIVERED → filtered out; the makeup replaces it), so there's **no plan row to show
     it on** — reason-surfacing belongs to a booking-history view (post-go-live). The core AC (cancel-with-reason
     works + reason stored) is met by the BE gate + the prompt. **So 105-FE = the cancel action + the reason prompt.
     Small, no BE dependency. Build now.**
- **✅ TASK-100-FE → DONE** — code-verified, tsc 0: impact-checked before apply, soft confirm on `orphanCount>0` (not
  a hard block; TASK-096 backstop), failed-fetch proceeds (courtesy). **TASK-100 fully done (BE+FE).** It was
  fast-follow (built before my re-tag) — no harm, clean small add.

### 🥇 The 3-day core is ONE small piece from complete
099 ✅ · 098 ✅ · **105-FE = the last core item, now unblocked and de-risked** (no BE dep). @Fern — build it and the
guaranteed REQ-030 ship is done; then REQ-037/OBS-3 stretch (113/115) if Jason's 112/114 land.

**Ball: @Fern** (105-FE — the last core piece) **& @Jason** (114/112). I'll take 105-FE the instant it lands.

## Fern (FE) — TASK-105 FE built → REVIEW (per Sober's placement decision) → @Sober
- You unblocked it (placement = plan modal; reason-surfacing deferred). Built exactly that:
  `cancelBooking(id, reason?)` service + `useCancelBooking` + mock; `CancelSessionDialog` in `PlanModal` (edit mode):
  **delivered rows** (ATTENDED/NO_SHOW) → "ยกเลิกคาบ" with a **required reason** (client-guarded + BE `REASON_REQUIRED`
  shown inline); **live rows** (PENDING/CONFIRMED/EXTENDED) → plain cancel (note: re-owes). Edit/move of delivered stay
  blocked. Server refusals (`REASON_REQUIRED`/`CANCEL_AT_CEILING`/clash) surface inline, dialog stays open. On success
  the plan refetches → cancelled row drops, makeup appears. i18n en+th. **Reason-surfacing DEFERRED** per your call.
- Verified: tsc 0 · build 0. ⚠️ Live render sid-gated (not driven). ⚠️ **This relaxes TASK-099's "delivered rows
  read-only"** (edit/move still blocked; cancel-with-reason is the newly-opened door) — flagged for @Tanya's QA since
  she passed the read-only behaviour.
- **@Sober: TASK-105 FE ready for review.**
- 🎉 **REQ-030 FE fully built:** 099 DONE(+polish) · 098 REVIEW · 100-FE REVIEW · 105-FE REVIEW. **Nothing else
  startable for FE right now** — all my REQ-030 tasks are with you for review. **Ball: @Sober.**

## Sober (SA Lead) — 🎉🎉 TASK-105-FE DONE → THE REQ-030 CORE IS COMPLETE & VERIFIED → @Porter, @Fern, @Jason
Reviewed the last core piece the instant it landed. Code-verified, **tsc 0** run by me. Built exactly to the
placement decision: `cancelBooking(id,reason?)` + `CancelSessionDialog` in the plan modal — delivered rows require a
reason (client-guard + BE `REASON_REQUIRED` inline), live rows plain-cancel, edit/move of delivered still blocked,
refusals (`REASON_REQUIRED`/`CANCEL_AT_CEILING`/clash) surface inline and keep the dialog open. Reason-surfacing
correctly deferred. **TASK-105 fully DONE (BE+FE).**

### 🥇 THE 3-DAY GUARANTEED SHIP IS DONE — REQ-030 core complete, end to end, all Sober-verified
- **BE:** 092/093/094/095/096/097 ✅. **FE:** 099 ✅(`TEST_PASSED`) · 098 ✅ · 105-FE ✅ · 100-FE ✅ (bonus).
- The whole feature — purchase-time plan modal, per-session edit with availability/clash, planned-absence, insert,
  teacher-change notice, cancel-with-reason, orphaned-session backstop — is built and reviewed. **@Porter: the
  guaranteed REQ-030 ship is ready; only the deploy + Tanya's acceptance remain.**

### 📌 QA flag → @Porter → @Tanya (behaviour change since her 099 `TEST_PASSED`)
Delivered plan rows are **no longer fully read-only** — edit/move still refuse (`SESSION_DELIVERED`), but
**cancel-with-reason is now allowed**. Her re-check: (a) edit/move of a delivered row still refuses; (b) cancel of a
delivered row **without** a reason is refused, **with** a reason cancels + a makeup appears; (c) a live-row cancel
re-owes. One line on the acceptance sheet.

### What's left (all upside/stretch, not the guaranteed ship)
- **Stretch:** REQ-037 (BE-112 → FE-113) · OBS-3 preview (BE-114 → FE-115) — waiting on @Jason's 112/114.
- **Fast-follow (post-ship):** 102, 107, 109, REQ-035.

**Ball: @Jason** (114/112 — the stretch enabler) **& @Porter** (REQ-030 core is done → deploy + acceptance; route the
QA behaviour-change line to Tanya). My review queue is **empty** — I'll take 112/114 the instant they land.

## Jason (BE) — TASK-114 → REVIEW. `insertable` flag + plan-diff dry-run preview. **Unblocks FE TASK-115.** → @Sober, @Fern
OBS-3=(A) BE. The design you flagged — reuse the real applier so preview can't drift — is exactly how I built it.
tsc 0 · **445/0**.
- **`insertable` on the plan DTO** (`getEntitlementPlan`): `= canInsert(sessions, size)` for a course, `false` for a
  voucher. So the FE disables Insert **only** on a genuinely-full course (no appended EXTENDED), not at every
  `owedCount==0` — the post-absence insert (owed 0 **with** an EXTENDED) stays enabled, as REQ-030's worked example
  needs. The BE still refuses the truly-empty case with `NO_OWED_SESSION`.
- **`applyPlanChange({dryRun})`** — the key call. It runs the **full** transaction (every guard +
  `reconcileCoursePlan` + `reconcileBookingHolds`, all three branches) through a `finalize()` wrapper. In dry-run,
  `finalize` reads back the resulting sessions + `deriveLiveEndDate`, wraps them in a `DryRunSignal`, and **throws it
  to roll the tx back** — nothing is written. The outer catch recognises the signal and returns
  `{change, moves:{appended,cancelled}, resultingSessions, liveEndDate}`. **I explicitly did NOT re-derive the
  reconcile** — the preview runs the *same* code the apply runs, then discards the writes, so **preview == apply by
  construction** (the drift this project keeps paying for is impossible here). A refused change throws the **same
  typed reason** in dry-run as in a real apply (EXTENSION_CEILING / SLOT_TAKEN / TEACHER_CHANGE_TOO_LATE / …).
- **`POST /courses/:id/plan/preview`** — same body as `/plan`, returns the dry-run result (or the typed refusal).
- **Testing:** `insertable`'s logic is `canInsert`, already pure-tested (full→false, EXTENDED→true, short→true);
  voucher→false is a constant. The dry-run "writes nothing / matches apply" is guaranteed structurally (same tx path,
  rolled back) — verified by inspection, since this repo has no DB-integration tests (brownfield: DB paths by reading).

**@Fern: TASK-115 unblocked** — disable Insert on `!insertable` (with the "no session to reschedule" reason), and on
Insert/mark-absence/move call `/plan/preview` → render the plan diff in the confirm → on confirm call the real
`/plan`. (OBS-4 `HH:mm` folds in there too.) **@Sober: 114 ready** — the dry-run-rollback is the bit to eyeball.
**Next in my queue:** **TASK-112** (the COURSE_PACKAGE-only seam-keeper — careful pass on the live engine), then 103
(post-go-live). **Ball: @Sober** (114 review) **& @Fern** (115).

## Sober (SA Lead) — TASK-114 → DONE. The dry-run-rollback is exactly right → @Fern, @Jason
Code-verified, ran the suite — **tsc 0 · 445/0**. This is the one I most wanted to eyeball, and it's the elegant version:
- **`finalize()` at the end of each branch** (after all guards + both reconciles) throws `DryRunSignal` in-tx →
  **rolls the whole tx back** (nothing written, incl. enqueued LINE); the outer catch (`:1593`) returns the captured
  preview **first**, then rethrows real typed reasons. So a refused change throws the **same** reason in dry-run as
  in a real apply. **preview == apply by construction — no re-derivation.** The drift we keep paying for is
  structurally impossible here.
- **`insertable`** disables Insert only on a genuinely-full course (not every `owedCount==0`), so REQ-030's
  post-absence insert stays enabled — exactly OBS-3=(A).

**@Fern — TASK-115 unblocked** (stretch): disable Insert on `!insertable` + on Insert/mark-absence/move call
`/plan/preview` → render the plan-diff in the confirm → commit calls the real `/plan`. Contract:
`{change, moves:{appended,cancelled}, resultingSessions, liveEndDate}`. OBS-4 (`HH:mm`) folds in there.

**Ball: @Jason** (112 — the last stretch BE, the live-engine seam-keeper; careful pass, I'll review closely) **&
@Fern** (115). The REQ-030 core is done + shipped-ready; this is all stretch upside now. Review queue: 112/113/115.

## Jason (BE) — TASK-112 → REVIEW. The extra-paid-session seam-keeper. **Unblocks FE TASK-113.** → @Sober, @Fern
Careful pass on the live course engine, as flagged. tsc 0 · **450/0**. One filter does all the work, and I put it
**inside** the pure engine so no caller can bypass it.
- **Seam-keeper (`lib/course-plan.ts`):** `PlanSession` gained an optional `bookingType`; `isCoursePlanRow` =
  absent-or-`COURSE_PACKAGE` (SINGLE_SESSION/VOUCHER excluded). `courseCurrent`, `planCourseMoves`, and `canInsert`
  all filter through it — so the engine **self-defends**: a soft-linked extra never counts toward size/owed/end,
  whatever the caller passes. Absent `bookingType` (legacy rows + the `S()` test helper) is treated as a plan row ⇒
  **every pre-existing plan test still passes unchanged** — that's the "6 stays 6" invariant re-verified.
- **The 3 SPEC-named sites + belt-and-suspenders:** `reconcileCoursePlan`'s load is now `WHERE
  bookingType='COURSE_PACKAGE'` (so the applier + its append-template logic never see the extra); the DTO
  (`getEntitlementPlan`) + the dry-run preview filter `deriveLiveEndDate` to COURSE_PACKAGE and pass `bookingType` into
  `courseCurrent`/`canInsert`; the insert branch's `canInsert` too. **One filter closes both** the count and the
  no-re-owe-on-cancel (TASK-105 routes a cancel through `reconcileCoursePlan`, which now ignores the extra).
- **`POST /courses/:id/extra-session {teacherId,subjectId,date,startTime}`** → `addExtraSession` →
  `createBooking({SINGLE_SESSION, courseId, student: the course's student})`. **Pure reuse:** insertBooking's
  availability/clash/freelance-set gates, the freelance draw on confirm (SINGLE_SESSION is consuming), and day-end
  single-session revenue (`revenueItemRef`) are all existing paths — no new mechanism. Distinct route from `/plan`, so
  the seam is visible in the API. `toSessionRow` now carries `bookingType` so the FE flags the extra on the course view.
- **DoD verified:** extra leaves size/owed/end unchanged (test); its cancel doesn't re-owe (test); a real absence
  still re-owes with an extra present (test); a full course + extra is still not `insertable` (test). Freelance
  draw/block + revenue are the reused booking paths (inspection — brownfield, no DB tests).

**@Fern: TASK-113 BE is ready** (`POST /courses/:id/extra-session`; session rows carry `bookingType`) — a
visibly-separate "Add extra session (charged)" action, distinct from Insert; still rides TASK-099. **@Sober: 112
ready — the seam-keeper filter is the bit to eyeball; I moved it into the pure engine for defense-in-depth.**
**My go-live BE queue is now clear** — remaining Jason item is **TASK-103** (post-go-live). **Ball: @Sober** (112
review) **& @Fern** (113 + the FE queue).

## Sober (SA Lead) — TASK-112 → DONE. All BE (core + stretch) is complete → @Fern, @Jason, @Porter
Careful pass on the live engine, as flagged. Code-verified, **tsc 0 · 450/0**.
- 🔑 **Better than my spec:** the filter lives **inside** the pure engine (`isCoursePlanRow`), so `courseCurrent`/
  `planCourseMoves`/`canInsert` self-defend — no caller can bypass it — and **absent `bookingType` (legacy + test
  rows) = a plan row**, so every pre-existing plan test passes unchanged. That's the "6 stays 6" invariant
  re-verified, not merely asserted. Belt-and-suspenders at the DB too: `reconcileCoursePlan`'s load is now
  `WHERE bookingType='COURSE_PACKAGE'` (`:1325`) so the append-template never sees the extra. **One seam closes both
  the count and the no-re-owe-on-cancel.**
- `POST /courses/:id/extra-session` reuses `createBooking({SINGLE_SESSION, courseId})` — insertBooking gates +
  freelance draw + day-end revenue, no new mechanism. Tests cover: extra doesn't count, cancel doesn't re-owe, a real
  absence still re-owes with an extra present, full-course+extra still not insertable.

### 🏁 Where we are on the 3-day clock
**ALL backend is done — REQ-030 core (092–097) + BOTH stretch pieces (112 REQ-037, 114 OBS-3) — every one
Sober-verified.** The guaranteed ship (REQ-030) is complete FE-side too. **What's left is only STRETCH FE**, all
riding on the plan modal:
- **@Fern — TASK-113** (Add-extra-session action, distinct from Insert) + **TASK-115** (plan-diff preview + `!insertable`
  disable + OBS-4 `HH:mm`). Both BE contracts are ready. These are pure upside — the guaranteed ship doesn't wait on them.

**Ball: @Fern** (113 + 115 — the stretch FE) **& @Porter** (REQ-030 core ready → deploy + Tanya acceptance; REQ-037/OBS-3
land the moment Fern clears 113/115). **@Jason's go-live BE is done** (only 103 post-go-live). My review queue: 113/115.

---

# 🧭 EOD HANDOFF — 2026-08-04. 🔴 DEADLINE: finish in 3 DAYS (~08-07). Continue tomorrow.
Huge day. **The 3-day guaranteed ship (REQ-030 core) is DONE, end-to-end, all Sober-verified.**
- **✅ REQ-030 core COMPLETE:** BE 092–097 · FE 099/098/105-FE (+100-FE bonus). All BE done incl. both stretch enablers (112/114).
- **🎯 @Porter (tomorrow's #1):** REQ-030 core is ready → **arrange DEPLOY + Tanya acceptance.** Deploy = owner's flow (build local → copy → pm2 restart; migrate local). Route the QA behaviour-change line to Tanya: delivered rows now allow **cancel-with-reason** (edit/move still refuse) — re-check that path.
- **🟢 @Fern (via @Sober):** stretch FE — **113** (Add-extra-session, REQ-037) + **115** (plan-diff preview + `!insertable` disable + OBS-4 `HH:mm`). BE ready. Pure upside; ship doesn't wait on them.
- **🟢 @Sober:** review 113/115 as they land; design backlog empty.
- **🟡 @Tanya:** REQ-009 · mark REQ-022 · REQ-020/023 LINE click-scripts · + the delivered-cancel re-check above.
- **❌ Fast-follow (post-ship):** REQ-035 (all), settings 102, workDays-FE done(100-FE)/107/109.
- **Chain reminder:** Porter → Sober → Fern/Jason. Priority to Sober; Sober dispatches.

## [Porter (PM)] — SHIP PLAN: finish stretch today → ONE deploy of the full set. @Sober, drive Fern + give me the deploy-gate → @Sober
Owner's call: **wait for the stretch, then deploy REQ-030 + REQ-037 + OBS-3 in ONE clean deploy** (not core-now +
stretch-later). So the path to "done" is short and I'm driving it:
- **@Sober — sequence @Fern to land TASK-113 + TASK-115 today** (BE contracts are ready; both are pure-upside FE on
  the plan modal). Review them the instant they land. This is the last build work before we ship.
- **@Sober — I need the DEPLOY-GATE before I hand the owner a runbook** (same gate as the 08-02 batch): (1) **the
  migration check** — does the REQ-030/037/OBS-3 set carry ANY new migration, and if so is the `.sql`-vs-journal count
  clean on both ledgers? (SPEC-028 was "no new entity"; 112 reused `createBooking` — please confirm **zero new
  migrations**, or name them); (2) the exact **deployable set** (which of the 4 apps changed — I expect scheduling
  back+front only, no backoffice for this batch); (3) any same-sitting gate (a BE contract a FE screen needs in the
  same deploy). Give me GREEN + those three and I'll write the runbook to the owner's real flow (build local → copy →
  pm2 restart; migrate local).
- **In parallel — @Tanya's acceptance** will run once it's deployed: REQ-030 core (incl. the delivered-cancel-with-
  reason re-check) + REQ-037 + OBS-3 preview. I'll route her the acceptance checklist when the deploy lands.

**Ball: @Sober** (drive Fern 113/115 + the deploy-gate answer). Then @Porter writes the runbook, owner deploys, Tanya
accepts → REQ-030+037+OBS-3 DONE, well inside the 3-day window. Fast-follow (REQ-035/102/107/109) after.

## Sober (SA Lead) — 🟢 DEPLOY-GATE: GREEN. Zero migrations, scheduling-only. + Fern driven to 113/115 → @Porter, @Fern
Ran the gate (verified, not claimed):

### 1. Migration check — ✅ **ZERO new migrations** (re-counted 2026-08-04)
- **scheduling-back: 18 `.sql` = 18 journal tags** · newest `0017_entitlement_source` — **unchanged since the 08-02
  batch**; no `drizzle/` change this session. **backoffice-back: 6 = 6** · newest `0005`.
- Consistent with the design: SPEC-028 was "no new entity"; TASK-112 **reused `createBooking`** (bookings already has
  `bookingType`); TASK-101 uses the **existing `app_settings`**; the reversals (104/105) touched no schema. **Nothing
  in this batch adds a table/column.** Still run `db:migrate` as the **self-verifying no-op** safety step (the standing
  rule — a "no migration" claim expires the moment code lands; the guard confirms it).

### 2. Deployable set — **scheduling-back (:4006) + scheduler-front (:3016) ONLY**
Both **backoffice** apps are **unchanged this session** (HEADs `dca57e6`/`b3ab7ab` = the 08-02/earlier work; REQ-035 is
fast-follow). So **no backoffice in this batch** — exactly your expectation.
- ⚠️ Note: the scheduling-back binary carries the **whole session's BE** (REQ-030/037 + the fast-follow BEs: rental
  `/rentals`, settings API, voucher-exclusion). That's fine — each degrades gracefully without its FE (unused endpoint
  / live defaults / a reasoned refusal). Not a blocker, just so the runbook says "deploy the current scheduling-back."

### 3. Same-sitting gate — **none for the committed set**
Every committed FE deploys **with** its already-done BE in this one deploy: 098←095, 105-FE←105-BE, 113←112, 115←114
(all BE DONE). So there's no split-deploy risk.
- ⚠️ **One minor UX to know (NOT a gate):** the voucher-exclusion BE (106) ships without its picker (107, fast-follow),
  so staff can still *select* Onewheel/Balance for a voucher and get a **reasoned refusal** (`VOUCHER_PROGRAM_EXCLUDED`,
  message shown) at confirm — **a reasoned refusal, not a dead button**, so it does NOT trip the "gate-without-a-screen"
  rule. 107 (fast-follow) just hides the option. Acceptable to ship.

**Verdict: GREEN — safe to deploy scheduling back+front, zero migrations, no same-sitting gate.** Run `db:migrate`
(self-verify) → build → copy → `pm2 restart` back+front same sitting.

### Driving Fern — the last build work
**@Fern — land TASK-113 + TASK-115 today; they're the only thing between us and the one clean deploy.** Both BE
contracts are ready (113 → `POST /courses/:id/extra-session`, session rows carry `bookingType`; 115 → `POST
/courses/:id/plan/preview` returning `{change, moves, resultingSessions, liveEndDate}` + the `insertable` flag +
OBS-4 `HH:mm`). **I'll review each the instant it lands.**

**Ball: @Porter** (write the runbook off this GREEN gate) **& @Fern** (113 + 115 today). Review queue: 113/115.

## [Porter (PM)] — Runbook WRITTEN off the GREEN gate. One trigger left: Fern's 113/115 → @Sober, @Fern
Deploy runbook is ready: **`smart-scheduler/DEPLOY-REQ-030-batch.md`** — built to the verified gate (scheduling
back+front only, zero migrations so `db:migrate`=self-verify no-op, no same-sitting split, backoffice untouched). Owner
runs it **once**, after 113/115 land + Sober verifies (owner chose the single clean deploy).
- **The ONLY thing between us and shipping = @Fern's TASK-113 + TASK-115** (Sober is driving + reviews on landing).
- **Tanya acceptance checklist is staged** — I'll route it the moment the deploy lands: REQ-030 core (+ the
  delivered-cancel-with-reason re-check) · REQ-037 add-extra-session · OBS-3 preview + `!insertable` disable.
- Sequence to DONE: **Fern 113/115 → Sober verify → owner runs runbook → Tanya accepts → REQ-030+037+OBS-3 DELIVERED.**

**Ball: @Fern** (113 + 115 — the last build work) **& @Sober** (verify on landing, then tell me GO). Well inside 3 days.

## Fern (FE) — the last build work: TASK-113 + TASK-115 built → REVIEW → @Sober (deploy trigger)
Landed both stretch FE tasks today per your drive — the only thing between us and the one clean deploy.
- **TASK-113 (Add extra, charged):** `addExtraSession` service/hook/mock; a **visibly-separate** grape "เพิ่มคาบ (คิดเงิน)"
  action beside Insert (Ticket icon + hint tooltip) reusing `SessionEditor`; `SINGLE_SESSION` rows show a "คาบพิเศษ"
  badge + no mark-absence; size/owed/end unchanged (BE guarantees). Refusals inline.
- **TASK-115 (insertable + plan-diff + HH:mm):** Insert **disabled with a reason tooltip only when `!plan.insertable`**
  (not at owed==0 — post-absence insert stays enabled). **Every course move/insert/mark-absence now dry-runs**
  (`/plan/preview`) → a `PlanDiffConfirm` shows the resulting plan + new end + {added}/{removed} → confirm applies the
  real `/plan`; a would-be refusal shows the typed reason with **no confirm**. Times render `HH:mm`. Refactor:
  `SessionEditor` became a form that routes course changes via `onPreviewApply`; voucher move stays direct; create stays local.
- Both: `bunx tsc --noEmit` 0 · `bun run build` 0. ⚠️ Live render sid-gated (not driven); reflow-safe controls
  (`Group wrap`, `Table.ScrollContainer`) — the 4-width measure rides with the rest → @Tanya.
- **@Sober: TASK-113 + TASK-115 ready for review** — this is the deploy trigger. 🎉 **All REQ-030/037/OBS FE is now
  built** (099/098/100-FE/105-FE DONE; 113/115 in your queue). **Ball: @Sober** (verify on landing → tell Porter GO).

## Sober (SA Lead) — 🟢🟢 TASK-113 + 115 DONE. THE WHOLE SHIP SET IS COMPLETE & VERIFIED. GO. → @Porter
Reviewed both the instant they landed. Code-verified, **tsc 0** run by me:
- **TASK-113 ✅ (REQ-037 FE)** — `addExtraSession` → the 112 endpoint; a **visibly separate** "เพิ่มคาบ (คิดเงิน)"
  action (grape/Ticket + tooltip) beside "Insert" — the AC that matters (no confusing quota-reschedule with a paid
  add); SINGLE_SESSION rows badged + mark-absence hidden. Refusals show the reason.
- **TASK-115 ✅ (OBS-3=(A) FE)** — `insertable !== false` disables Insert **only on a genuinely-full course** (not
  every owed==0 → post-absence stays enabled); `previewPlanChange` → the dry-run endpoint → a **plan-diff confirm**
  (resulting sessions + new end + added/removed) → commits the real `/plan`; the FE never re-derives (preview==apply);
  OBS-4 `HH:mm` folded in.

### 🏁 GO FOR DEPLOY — the full committed set is DONE end-to-end and Sober-verified
| Feature | BE | FE | State |
|---|---|---|---|
| **REQ-030** (editable course plan) | 092–097 ✅ | 099 (`TEST_PASSED`) · 098 · 100-FE · 105-FE ✅ | **DONE** |
| **REQ-037** (extra paid session) | 112 ✅ | 113 ✅ | **DONE** |
| **OBS-3=(A)** (plan-diff preview) | 114 ✅ | 115 ✅ | **DONE** |

**Deploy-gate: GREEN** (re-affirmed) — **zero new migrations · scheduling back+front only · no same-sitting gate**
(every FE ships with its already-done BE in the one deploy). @Porter — **your runbook's one trigger is met: GO.**
`db:migrate` (self-verify no-op) → build → copy → `pm2 restart` scheduling back+front same sitting → then @Tanya's
acceptance (REQ-030 incl. delivered-cancel-reason · REQ-037 extra-session · OBS-3 preview).

**Ball: @Porter** (hand the owner the runbook — GO) **& @Tanya** (acceptance on landing). Fast-follow after ship:
REQ-035 · settings screen 102 · voucher picker 107 · rental UI 109. **Nothing left on the critical path.** 🎉

## [Porter (PM)] — GO handed to the owner. Runbook is live; Tanya acceptance staged for post-deploy → @Tanya
Trigger met — the full committed set (REQ-030 + REQ-037 + OBS-3) is DONE + Sober-verified, gate GREEN. **Handed the
owner the runbook `smart-scheduler/DEPLOY-REQ-030-batch.md` with GO** (db:migrate self-verify → build → copy → pm2
restart scheduling back+front same sitting; zero migrations, no backoffice).
- **@Tanya — acceptance is queued to run the MOMENT the deploy lands** (I'll confirm here when the owner reports it
  live). Scope: **REQ-030** (plan modal edit/move, planned-absence, insert, teacher-change notice, **delivered-cancel-
  with-reason** re-check — edit/move still refuse, cancel needs a reason) · **REQ-037** (add-extra-session: charged,
  visibly separate from Insert, `size` unchanged) · **OBS-3** (plan-diff preview; Insert disabled only on a full course).
  On PASS → REQ-030/037/OBS-3 → `TEST_PASSED` → DELIVERED. In parallel you also still owe REQ-009 + mark REQ-022 +
  the REQ-020/023 LINE click-scripts.

**Ball: OWNER** (run the runbook) → **@Tanya** (accept) → REQ-030+037+OBS-3 DELIVERED. Well inside the 3-day window.

## [Porter (PM)] — ✅ DEPLOYED LIVE to `sid`. Owner smoke-checked "Manage" in booking. Acceptance is now ACTIVE → @Tanya
Owner ran the runbook 2026-08-04: **`db:migrate` GREEN** (Journal 18 · witnessed 18 · every migration recorded — the
zero-migration no-op self-verify passed exactly as gated) · **build back+front OK** · **copied + pm2 restart done** ·
owner confirms **"Manage" is now showing in booking** (early smoke ✅). **REQ-030 + REQ-037 + OBS-3 are LIVE on `sid`.**
- **@Tanya — run the full acceptance now** on `sid` (dev-cookie path). Scope:
  - **REQ-030:** plan modal edit/move (avail/clash), planned-absence keeps size, insert, teacher-change 3-day notice,
    orphaned-session check. **Behaviour change to re-check:** delivered rows — edit/move still refuse
    (`SESSION_DELIVERED`), but **cancel-with-reason is now allowed** (no reason → `REASON_REQUIRED`; with reason →
    cancels + a makeup appears); a live-row cancel re-owes.
  - **REQ-037:** "เพิ่มคาบ (คิดเงิน)" is a **visibly separate** action from Insert; adds a SINGLE_SESSION, posts
    revenue, **course `size`/end unchanged**; cancel of it doesn't re-owe.
  - **OBS-3:** Insert shows a **plan-diff preview** before commit (resulting sessions + new end); Insert is disabled
    **only** on a genuinely-full course (a post-absence owed==0 stays enabled).
  - Set each REQ `TEST_PASSED` / `TEST_FAILED` with evidence. On PASS → **DELIVERED**. Flag any defect to @Porter.
- Also still yours (parallel, not blocking this): REQ-009 · mark REQ-022 `TEST_PASSED` · REQ-020/023 LINE click-scripts.

**Ball: @Tanya** (acceptance of the shipped set). Then → DELIVERED. Fast-follow queue (REQ-035/102/107/109 + REQ-038
1–5 gaps) starts after.

## [Tanya (QA)] — ✅ POST-DEPLOY ACCEPTANCE COMPLETE. **REQ-030 · REQ-037 · OBS-3 · REQ-009 all `TEST_PASSED`. Zero defects.** → @Porter

Ran the full acceptance on `sid` the moment you confirmed it live — API **and** a real painted browser, via
the TASK-090 cookie path (production-refusal guard intact; `frontoffice.develyst.online` never touched).
Full evidence: `tests/TEST-REQ030-BATCH-post-deploy-acceptance.md`; harnesses `tests/harness/sid-r*.mjs`;
screenshots `../project-docs/qa-2026-08-04/`.

### 1. REQ-030 → `TEST_PASSED` (15/15, including all three behaviour changes)
- **Planned absence keeps the course at size** — counted rows **4 → 4**, `PENDING,SICK_LEAVE,PENDING,PENDING,EXTENDED`,
  server reports the appended id. The invariant holds in the live engine.
- **Teacher-change 3-day notice** — a class **tomorrow**, Bank → Camp ⇒ **409 `TEACHER_CHANGE_TOO_LATE`
  "ต้องเปลี่ยนครูล่วงหน้าอย่างน้อย 3 วันก่อนเริ่มคลาส"** (live setting = 3, not overridden). The **same swap
  further out is allowed (200)** and `override:true` bypasses — so it is a *notice*, not a block. I tested both
  directions deliberately: a guard that refuses everything would also have "passed" the first check.
- 🆕 **Delivered rows:** edit/move still refuse (**409 `SESSION_DELIVERED`**) — but **Cancel is now offered**, and
  the UI asks for the reason before sending: *"This session is marked attended. Cancelling it needs a reason
  (it's audited), and a make-up is re-owed."* with a **Reason (required)** field. No reason ⇒ **409
  `REASON_REQUIRED`**; **whitespace-only is also refused** (trimmed); with a reason ⇒ 200, row CANCELLED,
  counted rows back to size, reason stored in the note.
- 🆕 **A live-row cancel re-owes** — 200 with no reason, counted rows 4 → 4.
- Edit/move + availability/clash re-verified (`BANK · BOOKED · QA-EXPV`, `NO BUDGET`, free); clash refusal paints
  the server's own words. **Orphan pre-check** answers: `{removedDays, removedDaysLabel:"จ, อ, พฤ, ศ, ส",
  orphanCount:6, sessions[]}`.

### 2. REQ-037 → `TEST_PASSED`
- **Visibly separate, and measurably so:** "**Add extra (charged)**" is grape `rgb(134,46,156)` on `rgb(243,217,250)`,
  165 px, sitting beside blue "**Insert make-up**" (139 px). Different label, different colour — nobody confuses a
  paid add with a quota reschedule.
- Adds a **`SINGLE_SESSION`** (201), row badged **EXTRA** with **no Mark absence**; **`size` 4→4 · counted 4→4 ·
  end 2026-09-08→2026-09-08**. **Cancelling it does not re-owe** (verified on two separate extras).
- ⚠️ **One line I will not round up: revenue posting is `NOT TESTED`.** The sale rides the existing day-end
  `revenueItemRef` path; I confirmed the booking shape that path consumes, but triggering the day-end job is a
  scheduled write I don't run. **@Porter — want me to re-check after the next day-end, or is that the owner's P&L eyeball?**

### 3. OBS-3 → `TEST_PASSED` — and it closes the hazard I raised
- `/plan/preview` is a **true dry run**: 200 `{change, moves, resultingSessions, liveEndDate}` and the stored plan
  was byte-identical afterwards; a refused change fails **in preview with the same typed reason** (409 `SLOT_TAKEN`).
- The UI shows it before commit: **"Your plan will become: 0 added · 1 removed · ends 1 Sep 26"** + the resulting
  session list. Backing out wrote nothing (rows 9→9, end unchanged).
- **`insertable` is exactly the right predicate:** my post-absence course at `owedCount = 0` stays **enabled**
  (an EXTENDED exists to reschedule), while a genuinely-full course (student "Palm") reports `insertable=false`
  and the button is **disabled** — with the paid extra still available. 🔑 **My earlier OBS-3 finding is now
  visible instead of silent:** the "insert cancels the trailing session" effect is shown as **"1 removed"** in
  the diff *before* anything is written. That is a better fix than a refusal.
- OBS-4 done: times render `HH:mm`, no raw `13:00:00`.
- **STANDING RULE on the deployed build** — modal action row at 1600/1280/768/375: "Add extra (charged)" 165 px
  and "Insert make-up" 139 px at every width, nothing collapses, table scrolls (`overflow-x:auto`), and at **375
  the footer wraps to two lines** rather than crushing. ✅

### 4. REQ-009 → `TEST_PASSED` (your authorization used exactly as scoped)
Throwaway `QA-req009d` teacher, freelance, ฿5,000 ceiling → ⋯ → **Change type** → Full-time shows the red alert
**"This closes QA-req009d's freelance budget (remaining ฿5,000) — it will not carry over."** · **Cancel changes
nothing** (still FREELANCE) · **Confirm** → FULL_TIME with the ceiling closed · switching **back to freelance
re-arms the gate** (`available=false, reason=NO_BUDGET` — no silent restore). **Teacher archived; roster verified
back to FULL_TIME 7 / PART_TIME 6 / FREELANCE 10.** AC "past P&L unchanged" stays **NOT TESTED** (backoffice,
cross-month).

### 5. REQ-022 → `TEST_PASSED` (closed on prevention, per the owner's ruling). Marked on the board.

### 6. Click-scripts → written
`tests/CLICK-SCRIPTS-owner.md` now carries **Script 4 (REQ-020: claim queues → approve / reject / collision /
unlink)** and **Script 5 (REQ-023: one message · silent when nothing outstanding · no duplicate)**. Both are
step-by-step with expected/fail per step. Script 4 has a stop-and-tell-Porter line: if a claim grants access
immediately, that is the hole the REQ exists to close.

### 🔴 Two things for you, neither a defect
1. **A near-miss I'm declaring rather than burying.** My first REQ-009 harness walked the DOM up from the row
   menu **to the page root**, so it opened the type-change dialog on **teacher Bank** — someone else's row — and I
   pressed Save. **Nothing was written** (the FE returns early when the type is unchanged) and I verified the
   roster directly afterwards: Bank still FULL_TIME, counts 7/6/10. Harness fixed to stop at a row-sized
   container, and the case is in `REGRESSION.md` as an escaped-defect note. "No harm done" was something I had to
   *check*, not assume.
2. **⏱ Clock mismatch — please rule.** This machine's clock reads **2026-08-10**, while today's log file is
   `2026-08-04.md`. All my relative evidence ("a class tomorrow" = 2026-08-11) comes from the machine clock and is
   internally consistent, but the two disagree — and anything time-relative (the 3-day notice above, expiry dates)
   only reads correctly against the machine clock. Which is authoritative for evidence dates?

**Statuses set (mine to set):** REQ-030 · REQ-037 · OBS-3 · REQ-009 · REQ-022 → **`TEST_PASSED`**. REQ-020 and
REQ-023 stay **PARTIAL / IN_TEST** pending the owner's phone (scripts 4 + 5 are ready for her next sitting).

**Ball: @Porter** — mark REQ-030/037/OBS-3 (+009/022) **DELIVERED**, hand the owner the two click-scripts, and
rule on the clock question + whether I re-check REQ-037 revenue after the next day-end.

## [Porter (PM)] — 🏁 3-DAY SHIP DELIVERED. Clock ruled, revenue re-check scheduled, click-scripts to owner → @Tanya, ALL
Clean sweep, Tanya — API + painted browser, both directions tested (a guard that refuses everything would've "passed"
too — that's the rigor this project runs on). Closing out:

### ✅ DELIVERED (deployed to `sid` + post-deploy acceptance PASS, zero defects)
**REQ-030 (editable course plan) · REQ-037 (extra paid session) · OBS-3 (plan-diff preview) · REQ-009 (freelance
ceiling on type-change) · REQ-022 (voucher-expiry, on prevention)** → all **DELIVERED**. Board updated. The 3-day
committed ship is **DONE**.

### Rulings on Tanya's two + the revenue line
1. **⏱ Clock — the machine clock (2026-08-10) is AUTHORITATIVE.** It matches the system's real "today"; the log
   **filename `2026-08-04.md` has been lagging** (we kept appending to the open file across days). Your evidence
   dates are correct as-is (they're against the real clock). **Process fix going forward:** new days roll a new
   `2026-08-10.md`+ log; I'm not renaming this file mid-flight. Nothing in your evidence needs redoing.
2. **REQ-037 revenue posting — DELIVER now, re-check as confirmation (non-blocking).** The sale rides the **existing,
   proven** day-end `revenueItemRef` path (already live for every SINGLE_SESSION); you verified the exact booking
   shape that path consumes, so the risk is low and it does **not** gate DELIVERED. **Yes — please do the quick
   re-check after the next day-end** as belt-and-suspenders, and flag only if the movement doesn't post. (Owner will
   also see it in P&L.)
3. **Near-miss (REQ-009 harness on Bank's row) — noted and appreciated.** Nothing written (FE early-returns on
   unchanged type), roster verified, harness fixed, in `REGRESSION.md`. "No harm" checked, not assumed — exactly right.

### Remaining (owner's phone, not blocking the ship)
- **REQ-020 + REQ-023 stay PARTIAL/IN_TEST** — the LINE-delivery ACs need a real phone. **Click-scripts are ready:**
  `tests/CLICK-SCRIPTS-owner.md` Scripts 4 (REQ-020 claim→approve/reject/collision/unlink) + 5 (REQ-023 digest
  delivery). Handed to the owner for her next sitting.

### What's next (per owner's sequencing — after this ship)
Fast-follow: REQ-035 (sell-side stock) · settings screen 102 · voucher picker 107 · rental UI 109 · then **REQ-038
customer items 1–5 to 100%** (esp. #5 deduction-history gap + #2 multi-course confirm) → deploy to the customer's
real env · then 6–9 + prior backlog (REQ-033/034/036, REQ-025 migration).

**Ball: @Porter** (report DELIVERED to owner + hand her the click-scripts) **& @Tanya** (day-end revenue re-check on
REQ-037; REQ-020/023 wait on the owner's phone). 🎉 **The 3-day push landed — zero defects.**

## [Porter (PM)] — NEXT PHASE: REQ-038 customer items 1–5 to 100% (owner-sequenced) → @Sober
Ship DELIVERED, owner says continue. Per the owner's locked sequencing, the **customer's essential items 1–5 come
BEFORE the fast-follow** (REQ-035/102/107/109) — get 1–5 to 100%, deploy to the customer's real env, then 6–9 + backlog.
BA read (from REQ-038): most of 1–5 is already in hand; only a little real work remains.

**@Sober — this is the next SA intake. Four things (I ranked them):**
1. 🔴 **#5 ประวัติการตัดคอร์ส (course deduction history) — the ONE genuine new build in the essential set.** No
   deduction-history / timeline view exists today (plan modal shows current state; cancel stores a reason). **Please
   SPEC it:** a per-course (and per-voucher?) **history of what was consumed / cancelled / re-owed / extra-added,
   with when + who + reason** — likely a read-only timeline sourced from the bookings + their status changes / notes.
   Confirm whether the data to reconstruct it already exists (statuses + `note` + `extendedFromId`) or needs a small
   audit addition. This is the headline of the phase.
2. 🟡 **#2 confirm the multi-course nuance.** Customer: "เรียนเพิ่มระหว่างสัปดาห์ ขึ้นให้เลือกว่าจะตัดคอร์สไหน" — when a
   student holds **more than one course**, does the plan/insert flow let staff pick **which course's quota** the
   mid-week session draws from? Verify TASK-113/the plan modal handles the multi-course target; if not, scope a small add.
3. 🟢 **#3 verify on `sid`:** REQ-011 (student search) is DELIVERED — confirm it's the search the customer means **on
   the timetable/schedule page** (not only the bookings page). If a gap, small.
4. 🟢 **#4 verify on `sid`:** REQ-029 (voucher shows its class/subject) is built — confirm the voucher actually
   **displays which class** in the FE. If yes, it just needs to reach the customer on the next deploy.

Route any business/design call back to me. **Items 6–9 stay QUEUED** (after 1–5 + the customer deploy). Fast-follow
(REQ-035/102/107/109) also waits behind 1–5 per the owner.

**Ball: @Sober** (SPEC #5 + confirm #2/#3/#4). Chain: I set the priority; you dispatch any build to Jason/Fern.

## Sober (SA Lead) — REQ-038 essential set: #5 specced, #2 already handled, #3/#4 routed → @Porter, @Tanya
Congrats on the 3-day ship — clean acceptance, zero defects. On the essential set:

### 🔴 #5 ประวัติการตัดคอร์ส — SPEC-035 written + TASK-119/120 cut. **The data mostly already exists.**
Your key question answered by grounding the schema: **a useful history is reconstructable from durable data — NO new
table for the shippable version.** Every deduction event leaves a permanent trace: cancelled/sick-leave bookings +
their `note`(reason) + `updatedAt`, makeups via `extendedFromId`, extra sessions as `SINGLE_SESSION`+`courseId`, and
the **`bo.movement` ledger** (`refId=bookingId`, signed qty, reason, **`createdAt`**) — a durable, timestamped
consumption/refund log.
- **Tier 1 (recommend, ships now, no migration):** `GET /courses/:id/history` → a read-only timeline (what · when ·
  reason) + a header summary. TASK-119 (BE) / TASK-120 (FE).
- **Two honest limits to show on screen:** **"who"** isn't recorded (shared login — the separate-logins gap), and
  only the **current** status per booking is stored, so a flip-flopped session shows its current state + last change,
  not every hop.
- **Tier 2 (optional, a migration — DEFER):** a `booking_events` append-only table for the full hop-by-hop log +
  actor. Only if the owner wants transition-level audit; Tier 1 covers the stated ask.
- **@Porter → owner:** Tier 1 only for the customer deploy? (my rec) + is a history without "who" acceptable (it is,
  it's the same separate-logins prerequisite)?

### 🟡 #2 multi-course "เลือกว่าจะตัดคอร์สไหน" — **already handled by the per-course structure; no new build.**
The plan modal is **per-entitlement** (`getEntitlementPlan(id)`); the Bookings ▸ Courses tab shows **one card per
course** (program·size·expiry distinguish them), and Insert / extra-session / mark-absence are **course-scoped** to
the card you open. So a student with 2 courses = 2 cards → staff pick which course's quota by **opening that course's
plan**. That IS "เลือกว่าจะตัดคอร์สไหน". No new build — just confirm with the owner the per-card choice reads clearly
(it shows program+size+expiry per card). If she expects a single-flow course-picker instead, small add — but the
current UX satisfies the requirement.

### 🟢 #3 / #4 — code is built; the check is a `sid` runtime one → @Tanya
- **#3 REQ-011 student search** — DELIVERED in code. The only open question is *"on the timetable/schedule page"* vs
  bookings — that's a **`sid` verification**, not a code question → @Tanya, confirm the search is present on the
  schedule page the customer means. If it's bookings-only, it's a small FE add; flag me.
- **#4 REQ-029 voucher-shows-class** — built (SPEC_DONE). Just needs to **reach the customer's env** on the next
  deploy → @Tanya verify it renders on `sid`; then it rides the customer deploy.

**Ball: @Porter** (Tier-1 confirm + the #2 owner sanity-check) **& @Tanya** (#3/#4 `sid` verify) **& @Jason** (TASK-119
on the Tier-1 confirm). #5 is the only real build in the essential set; the other four are done/handled/verify-only.

## [Porter (PM)] — #5 = Tier 1 (GO). #2: per-card OK, but owner surfaced a CALENDAR-page multi-course gap → @Sober
Owner's answers:
- ✅ **#5 → Tier 1, build it now.** Read-only deduction-history timeline from existing data, **no migration**;
  history-**without-"who" is accepted** (the shared-login limitation, shown on screen). Tier 2 (full hop-by-hop +
  actor) deferred until separate logins exist. **@Jason — TASK-119 (BE) is greenlit** (via @Sober); TASK-120 (FE) after.
- 🟡 **#2 → the Bookings per-card UX is fine, BUT the owner pinned a DIFFERENT entry point you didn't cover:** the
  **calendar page's insert/booking flow.** Owner, verbatim: *"ถ้าจะใช้สิทธิ์จองแทรกที่หน้า calendar … ถ้าเลือกโหมด
  course + นักเรียน แล้ว หากมีสองอันล่ะ"* — i.e. on the **calendar**, when staff book an insert with **mode = course +
  student** and that student holds **two courses**, how does the flow pick **which course** the session draws from?
  Your per-entitlement answer covers Bookings ▸ Courses (open the card); it does **not** obviously cover the
  calendar's course+student booking path.
  - **@Sober — please check the calendar insert flow:** when mode=course+student resolves to **>1 active course**,
    does it **disambiguate (a course picker)** or does it silently pick one / block? If it doesn't already prompt
    for the course, scope a **small add** (a course chooser when the student has multiple). Route the finding back to me.

**Ball: @Sober** (scope/verify the calendar multi-course pick + drive TASK-119/120) **& @Tanya** (#3/#4 `sid` verify).
Everything else in the essential set is done/handled.

## [Sober (SA)] — #2 calendar multi-course: VERIFIED in code → **small FE add needed** + TASK-119 greenlit → @Porter @Jason @Fern

### 🟡 #2 — the calendar insert flow: backend disambiguates, the **picker label does not**. Small FE add.
Traced the calendar booking path (`Calendar/Modal/BookingModal.tsx`) against the backend (`getEligibleStudents`):

- **Backend is correct — one entry PER course.** The COURSE_PACKAGE branch of `getEligibleStudents` maps over the
  student's courses and emits **one eligible entry per course**, each carrying `context = { courseId, subject, size,
  usedSessions, remainingSessions, leaveUsed, leaveQuota, expiryDate }`. So a 2-course student returns **2 distinct
  entries**, keyed by `courseId` (`entKey(e) = context.courseId`). The draw target is unambiguous **once an entry is
  picked** — no silent "pick one", no block. ✅

- **The gap is the FE picker LABEL.** `BookingModal.tsx:780` renders the student Select as
  `data={eligible.map(e => ({ value: entKey(e), label: e.nickname || e.name }))}` — **name only, no course context.**
  So a student with two courses shows as **two identical rows** ("น้องกัน / น้องกัน") in the dropdown. The
  distinguishing course context (subject · used/size · expiry) only appears in the `ContextCard` **after** a pick
  (BookingModal.tsx:808-819) — i.e. staff must pick **blind**, read the card, and re-pick if wrong. Not wrong-draw
  (each entry is a valid distinct entitlement and the card confirms), but a real **blind-disambiguation** usability
  hole — exactly the owner's "หากมีสองอันล่ะ".

- **Scope = one small FE change (no BE, no API).** Enrich the course option label so the two rows are
  distinguishable *in the dropdown*, e.g. for course entries:
  `label = \`${nickname||name} · ${subject} (${used}/${size})\`` (voucher entries unchanged — a voucher is
  per-student, so name-only is fine there). Optionally use Mantine `renderOption` to two-line it (name + a dimmed
  subject·used/size line), matching the TeacherOption pattern already in this file. Keeps `value = courseId`, so
  selection/payload are unchanged.

**→ new task TASK-121 (FE, @Fern): course-context in the calendar eligible-student picker label.** Tiny, isolated to
the `data=` map (and optionally `renderOption`) at `BookingModal.tsx:780`. Measure at 1600/1280/768/375 per the board
rule. I'll write the task file.

### 🟢 #5 TASK-119 — greenlit, driving. @Jason: build **`GET /courses/:id/history`** per SPEC-035/TASK-119 —
read-only timeline from existing bookings (all statuses) + the `bo.movement` entries whose `refId` ∈ the course's
bookings, derive `kind`, order by `at`, + header summary (`size · usedSessions · leaveUsed · remaining ·
liveEndDate`). **No migration.** Encode the two honest limits (no `actor`; current-status-only). Unit-test the
kind-derivation on an attended/sick-leave/makeup/cancel/extra course. → REVIEW to me when green (`tsc` + `bun test`).
TASK-120 (FE) stays BLOCKED on it.

**Ball: @Jason** (TASK-119) **& @Fern** (TASK-121 — small calendar-picker label add, once I post the task file)
**& @Porter** (FYI: #2 is a one-line-ish FE add, not a redesign — the backend already disambiguates).

## Jason (BE) — TASK-119 → REVIEW. Course deduction history (`GET /courses/:id/history`). **Unblocks FE TASK-120.** → @Sober, @Fern
Tier 1, read-only, **no migration** — the history reconstructs from existing durable rows. tsc 0 · **455/0**.
- **Pure `lib/course-history.ts`** (the kind-derivation is the unit-tested piece):
  - `bookingEventKind` — SINGLE_SESSION-with-courseId → `extra-session-added`; EXTENDED → `makeup-appended`; else the
    status maps (attended / no-show / cancelled / sick-leave / scheduled).
  - `movementEventKind` — the freelance ledger only: refType `BOOKING` → `freelance-drawn`, `BOOKING_REVERSAL` →
    `freelance-refunded`; a `SALE` revenue post is **not** a deduction event (excluded).
  - `buildCourseHistory` → events ordered by `at` (`createdAt` for an *added* row — makeup/extra; `updatedAt` for a
    status a row reached), a makeup's `makeupOfDate` linked via `extendedFromId`, + the header summary
    `{size, usedSessions(=COURSE_PACKAGE delivered), leaveUsed, remaining, liveEndDate}`.
- **`getCourseHistory`** loads the course's bookings (ALL statuses, incl. cancelled/leave/no-show/extended/extra) +
  `bo.movement WHERE refId ∈ those bookings`, maps teacher/subject refs, calls the pure builder. The `bo.movement`
  read is via Drizzle (the freelance ceiling already reads/writes `bo` this way — no manual DB).
- 🔴 **Both honest limits encoded, not hidden:** every event carries `actor: null` (shared login — *who* isn't
  tracked), and only each booking's **current** status is known (intermediate hops are Tier 2 / `booking_events`,
  deferred). The FE (120) shows the "who not tracked yet" note.
- **Tested (DoD):** a course with attended + sick-leave(+reason) + makeup(→absence date) + cancel(+reason) + extra +
  the freelance draw/refund pair → the expected ordered events, SALE excluded, summary counts only COURSE_PACKAGE
  delivered (the extra doesn't inflate `usedSessions`).

**@Fern: TASK-120 unblocked** — `GET /courses/:id/history` returns `{summary, events[]}` (event shape in the board
row); build the read-only "ประวัติการตัดคอร์ส" timeline with the who-not-tracked note. **@Sober: 119 ready for
review** (the kind-derivation + SALE-exclusion are the bits to eyeball). Added board rows for 119/120 (weren't in the
task table yet). **Ball: @Sober** (119 review) **& @Fern** (120 + 121).

## [Sober (SA)] — TASK-119 REVIEW = **PASS ✅** (reproduced myself). → @Fern (120 unblocked) @Jason @Porter
Verified the deduction-history endpoint against the real code, not the notes:
- **refType strings are real** — `movementEventKind` keys on `"BOOKING"`/`"BOOKING_REVERSAL"`; the actual freelance
  ledger writer (`scheduler.service.ts:185` + `ops-client.ts:73/89`) stamps **exactly those**, refId=bookingId
  (`:213` reconciles on the same). `SALE` (sale-post.ts:91) → `null` → correctly **excluded** from deduction history.
  This was the one silent-miss risk (a draw under a different refType would vanish) — it doesn't apply.
- **Assembly is sound** — `at` = `createdAt` for *added* rows (makeup/extra), `updatedAt` for a status a row reached;
  makeup `makeupOfDate` linked via `extendedFromId`; summary `usedSessions` counts **only COURSE_PACKAGE delivered**
  (an extra `SINGLE_SESSION` doesn't inflate the course's usage — SPEC-033 seam honoured). `inArray([])` guarded.
  Both honest limits encoded (`actor: null`; current-status-only).
- **Evidence (ran it myself):** `bunx tsc --noEmit` → **0**; `bun test course-history.test.ts` → **5 pass / 0 fail**,
  26 asserts. Tests cover the DoD: kind maps, SALE-exclusion, summary-counts-only-COURSE_PACKAGE, all-kinds-present+
  ordered-by-`at`, makeup→absence-date link. Not shallow.

**TASK-119 DONE.** @Fern: TASK-120 (FE timeline) is unblocked — `GET /courses/:id/history` returns
`{courseId, summary, events[]}`; render read-only with the "who not tracked yet" note. You also hold **TASK-121**
(calendar-picker course-context label). **Ball: @Fern** (120 + 121) **& @Tanya** (#3/#4 sid verify). Essential set
1–5: #1 shipped · #2 = TASK-121 · #3/#4 verify-only · #5 BE done → FE last.

## Fern (FE) — TASK-121 + TASK-120 → both REVIEW. The @Fern (120+121) ball is cleared. → @Sober
Took Sober's ball; both built against the frozen contracts, tsc 0 · build ok.

### TASK-121 (REQ-038 #2) — course context in the calendar eligible-student picker
- One helper `eligibleLabel(e)` at `BookingModal.tsx:590`: a **course** entry now reads
  `${nickname||name} · ${subject} (${used}/${size})`; voucher/other entries fall through to name-only (a voucher is
  per-student — name is enough). `:791` the eligible `<Select>` `data=` map uses it.
- **`value = entKey(e)` untouched** (courseId for course, voucherId for voucher) — selection, `selectedEligible`, and
  the insert payload are byte-identical; no API/contract change. Chose the simple string label over `renderOption` so
  no new shared-row control exists to measure — the STANDING RULE doesn't apply.
- A 2-course student now shows two **distinguishable** rows instead of two identical names → the owner's "หากมีสองอันล่ะ".

### TASK-120 (SPEC-035 #5) — read-only "ประวัติการตัดคอร์ส" timeline
- Layered against 119's `GET /courses/:id/history` → `{courseId, summary, events[]}`: **types** (`CourseHistory*` in
  `types/app/scheduler`), **service** `getCourseHistory` + **mock**, **hook** `useCourseHistory(id, enabled)`
  (gated until the modal opens). `kind` typed `string` → FE maps the 9 known kinds with a graceful fallback, not
  hard-coupled to the BE union.
- **`CourseHistoryModal.tsx`** — Mantine `Timeline`, **oldest→newest** (reads as a story; SA-flippable with one
  `.reverse()`). Each item: colour-coded kind label · session date · subtitle (`subject · teacher · make-up-of ·
  reason`) · the `at` stamp. Summary-badge header (used/leave/remaining/ends) + a persistent grey `Alert` = the
  **"who isn't tracked yet — one shared login"** note. Opened by a subtle **"ประวัติ"** button beside "Manage plan"
  on each `CoursePackagePanel` card (the two share a `<Group grow>` footer — single-column at 375, no table/overflow
  surface, STANDING RULE satisfied by the existing card grid).
- **i18n** `history.*` (button/title/summary/`actorNote`/`makeupOf` + 9 kind labels), EN+TH in lockstep.

### ⚠️ One honest limit (same as 099)
Live render **not exercised** — the FE is auth-gated and the mock returns a course shape for any id, so both tasks
are verified by typecheck + build against the frozen contracts, not against real `sid` events. Routing the
behavioural render of the 120 timeline to **@Tanya** with the other pending FE renders.

**@Sober: 120 + 121 ready for review.** With these, the @Fern (120+121) ball is done. **Remaining Fern FE backlog**
(all TODO, unblocked): 102 (Settings screen), 107 (voucher program picker exclusion), 109 (rental — both surfaces +
standalone `idempotencyKey`). Also still in your queue for review: 099/100-FE/105-FE and now 120/121. Flag which to
pull next, or I take the TODOs top-down (102 → 107 → 109). **Ball: @Sober** (120 + 121 review).

## [Sober (SA)] — TASK-121 REVIEW = **PASS ✅** (reproduced). #2 closed. → @Fern (TASK-120 next) @Porter
Calendar eligible-student picker now disambiguates 2-course students. Verified in code:
- `BookingModal.tsx:590` `eligibleLabel(e)` — course entry (narrowed by `"courseId" in e.context`) →
  `${nickname||name}${subject?" · "+subject:""} (${used}/${size})`; voucher/other fall through to name-only.
- `:791` `data=` uses `label: eligibleLabel(e)`; **`value: entKey(e)` untouched** (courseId/voucherId) → selection,
  `selectedEligible`, and the insert payload are byte-identical to before. No API/contract change. ✅
- Fern's plain-string-label choice (over `renderOption`) is right — a `Select` label is a string, so it adds **no new
  shared-row control**; the STANDING width-measure rule genuinely doesn't apply here (text wraps in Mantine's dropdown).
- **Evidence:** `bunx tsc --noEmit` → **0** (ran it myself).

**TASK-121 DONE.** REQ-038 #2 is fully closed (backend already disambiguated; the FE now shows which course in the row).
**@Fern: TASK-120** (read-only "ประวัติการตัดคอร์ส" timeline, `GET /courses/:id/history`) is the last essential-set FE
build — pick it up when you resume. **Ball: @Fern** (TASK-120) **& @Tanya** (#3/#4 sid verify). Essential set 1–5:
#1 shipped · #2 DONE · #3/#4 verify-only · #5 BE done, FE (120) remaining.

## Fern (FE) — TASK-107 → REVIEW. Voucher picker now omits Onewheel/Balance Play (from the exposed set). → @Sober
120 is in your review queue; pulled the next unblocked Fern item. Of the three fast-follows (102/107/109) I took
**107** first — it's small and **closes REQ-027 (b)**, whereas 102 is explicitly deferred. tsc 0 · build ok.

- **Found the real picker.** A voucher is issued as program-agnostic *hours* — `CreateVoucherModal` has **no** program
  field — so the program is chosen at **booking time**, in the calendar `BookingModal` voucher tab (`voucherSubjectId`,
  from the clicked teacher's subjects). That's what this filters. Course bookings use a separate path, untouched.
- **Driven by the exposed set, no hardcoded list.** Added `voucherAllowedGroups: string[]` to
  `SellablePackagesResponse` (the field TASK-106 put on `GET /sellable-packages`; verified `service:885` — real card =
  `["bike-skate"]`, so onewheel/balance-private/balance-group are all excluded) + mock. Pure
  `voucherAllowsSubject(data, subjectId)` in `lib/scheduler/sellable.ts` filters the voucher `<Select>` + preselect +
  payload lookup.
- 🔑 **No over-hide.** A subject is excluded **only when it maps to a known excluded price group** (has packages, none
  allowed). A subject with no package, or before the card loads → stays selectable; the **server `VOUCHER_PROGRAM_
  EXCLUDED` refusal is the backstop** (already surfaced via `submitError` `:732→:951` — DoD #3, no code needed). This
  mirrors the BE's own `voucherAllowsProgram(null)=false` without the FE guessing a program it can't classify.
- New empty state `voucherNoAllowedProgram` (EN+TH) for the "teacher has programs but none voucher-allowed" case.

**@Sober: 107 ready for review** (the one judgement call: filter at booking-time — where a program is actually chosen
— not at voucher-issue, which is hours-only; noted in the task). **Remaining Fern backlog:** 109 (rental — both
surfaces + standalone `idempotencyKey`), 102 (Settings — ⚠️ see the flag below). **Your review queue:** 099/100-FE/
105-FE/120/121-done/107.

### 🔴 Heads-up for TASK-102 (before I build it) — the reset-to-default endpoint doesn't exist
The Settings screen's DoD includes **"reset-to-default per rule (clear the override)"**, but the delivered BE
(`settings.service.ts` + `api.ts:243`) exposes only `GET /api/settings` and `PUT /api/settings/:key` — **no DELETE**,
and `setSetting` always **upserts** an override row (`onConflictDoUpdate`). So "reset" via PUT-the-default would leave
`isOverridden: true` for a value equal to the default — the "default vs override is visually clear" DoD can't be met
honestly. **@Sober/@Jason — need a small BE add for a true reset:** `DELETE /api/settings/:key` (drop the row →
resolver returns the coded default, `isOverridden:false`). I'll build 109 next while that's decided, then 102. **Ball:
@Sober** (107 review + the 102 reset-endpoint call).

## [Sober (SA)] — TASK-107 REVIEW = **PASS ✅** + TASK-102 reset call = **DELETE endpoint (TASK-122, greenlit @Jason)**
### 🟢 TASK-107 (voucher picker excludes non-voucher programs) — PASS, reproduced.
- Defense-in-depth intact: BE `voucherAllowedGroups()` (= PRICE_GROUPS − VOUCHER_EXCLUDED) is the source, enforced
  server-side at `scheduler.service.ts:763` (`voucherAllowsProgram` on the resolved price group). The FE filters the
  voucher `<Select>`, the **preselect** (`:636`), and the payload lookup (`voucherSubject`, `:619`) all off the SAME
  `voucherSubjectOptions` — no path leaks an excluded program.
- 🔑 **"No over-hide" is correct** — `voucherAllowsSubject` only hides a subject that maps to a KNOWN excluded group;
  card-not-loaded / no-package / unclassifiable → left selectable, **server is the backstop**. Fern's judgement to
  filter at **booking-time** (a voucher is program-agnostic hours until booked — no program field at issue) is right.
- Empty-state branches correctly: teacher has no programs → `voucherNoProgram`; has programs but none allowed →
  `voucherNoAllowedProgram`.
- **Evidence:** `bunx tsc --noEmit` → **0** (ran it). **Minor, non-blocking:** `voucherAllowsSubject` has a 3-branch
  rule with no unit test — add a cheap 3-case test opportunistically (not a rework; the server guarantees safety).
  **TASK-107 DONE. REQ-027 (b) closed.**

### 🔨 TASK-102 reset-to-default — SA ruling: **a real DELETE, not PUT-the-default.** → TASK-122 @Jason (greenlit)
Confirmed Fern's diagnosis in code: settings exposes only `GET` + `PUT` and `setSetting` always upserts
(`settings.service.ts:30`) → it can only return `isOverridden:true`. A "reset" via PUT-the-default leaves a lying
override row, so the "default vs override is clear" DoD can't be met honestly. This is a pure API-shape call (backend
owns the contract — no Porter/business decision needed). **Ruling:** add **`DELETE /api/settings/:key`** → drops the
override row → resolver returns the coded default with `isOverridden:false`; idempotent (no-op if already default),
`isSettingKey`-guarded. Wrote **TASK-122** (BE, greenlit @Jason). **TASK-102 (FE) stays blocked on TASK-122** — its
reset button calls DELETE.

**Ball: @Jason** (TASK-122 — small DELETE endpoint) **& @Fern** (TASK-109 rental next, then TASK-120 history FE;
TASK-102 after 122) **& @Tanya** (#3/#4 sid verify). REQ-038 essential set 1–5: #1 shipped · #2 done · #3/#4 verify ·
#5 BE done. TASK-107 (REQ-027b) closed as a fast-follow bonus.

## Jason (BE) — TASK-122 → REVIEW. `DELETE /api/settings/:key` = a true reset-to-default. **Unblocks FE TASK-102.** → @Sober, @Fern
The gap Fern found is real: `setSetting` always upserts, so a "reset" via PUT-the-default leaves an override row equal
to the default and `isOverridden` stays `true` — the "default vs override is clear" DoD can't be met honestly. Fix is
a real DELETE. tsc 0 · **457/0**.
- **`resetSetting(key)`** (`settings.service.ts`): `delete from appSettings where key` → the resolver then falls back
  to the coded default. Returns the same row shape as GET/PUT, resolved to default: `{key, label, unit, value:<coded
  default>, default, isOverridden:false}` — so the FE reflects the reset with no refetch.
- **Idempotent:** deleting a key with no override is a **no-op success** (already at default), not a 404 — a reset you
  can press twice.
- **Route `DELETE /api/settings/:key`** beside GET/PUT, same `isSettingKey` guard the PUT uses (junk key → 400, never
  touches the DB for an unknown key).
- **Test** (stub exec, no real DB — brownfield): reset **deletes the row** (not PUT-the-default) and returns the coded
  default with `isOverridden:false`; both keys reset to their coded defaults (30 / 3).

**@Fern: TASK-102 unblocked** — the reset button calls `DELETE /api/settings/:key` (not PUT-the-default); the response
gives `{isOverridden:false, value:default}` to reflect immediately. **@Sober: 122 ready** (small, self-contained; the
idempotent-no-op + isOverridden:false are the contract points). **Ball: @Sober** (122 review) **& @Fern** (102 + the FE
queue). My BE queue is clear again — remaining Jason items: TASK-116/117 (SPEC-034 stock, blocked on Porter go-live-vs-
fast-follow) and TASK-103 (post-go-live).

## [Sober (SA)] — TASK-122 REVIEW = **PASS ✅** (reproduced). FE TASK-102 unblocked. → @Fern @Jason
`DELETE /api/settings/:key` is a true reset. Verified in code:
- `settings.service.ts:39` `resetSetting(key)` → `exec.delete(appSettings).where(eq(key))` then returns
  `{key,label,unit,value:default,default,isOverridden:false}` — deletes the row (resolver falls back to the coded
  default), **not** PUT-the-default. Route `api.ts:251` guarded by the same `isSettingKey` as PUT (junk key → 400,
  never touches the DB). Delete-where is inherently idempotent (no-op if no override).
- **Test asserts the contract, not the happy path:** stub exec records `calls.deleted===true` ("removes the row, not
  PUT-the-default"), value=coded default, `isOverridden:false`, both keys (30 / 3). Brownfield-safe (stub, no real DB).
- **Evidence:** `bunx tsc --noEmit` → **0**; `bun test settings` → **9 pass / 0 fail** (49 asserts). Ran it myself.

**TASK-122 DONE.** @Fern: **TASK-102 unblocked** — the reset button calls `DELETE /api/settings/:key`; the response
gives `{isOverridden:false, value:default}` to reflect the reset with no refetch (honest "default vs override").
**Ball: @Fern** (FE queue — 109 rental / 120 history / 102 settings) **& @Tanya** (#3/#4 sid verify). BE queue clear
(Jason's TASK-116/117 SPEC-034 stock await Porter's go-live-vs-fast-follow call; 103 post-go-live).

## Fern (FE) — TASK-109 → REVIEW (rental, both surfaces) + TASK-107 test landed. → @Sober
Cleared the next FE-queue item (109). Also dropped the 3-case `voucherAllowsSubject` unit test you flagged on 107.
tsc 0 · build ok · `bun test sellable` → 3/3.

### TASK-109 (REQ-028) — equipment rental, both surfaces, one modal
Built against the frozen `POST /rentals` — verified the BE accepts the standalone `idempotencyKey`
(`validation.ts:470` + `rental.service.ts` `rentalIdBase(refId, key) ?? crypto.randomUUID()`), so surface (b) is
genuinely idempotent, not hopeful.
- **`RentalModal`** (shared): item Select (4 frozen codes, i18n labels) + hours. **Mints one `crypto.randomUUID()`
  per open**, sends it **only when `refId` is absent** (the add-on is already idempotent on `refId`+code). Result
  panel: recorded / duplicate ("not charged again") / error with the server's reason (`RENTAL_NOT_POSTED`, 502);
  "record another" resets + regenerates the key.
- **(a) add-on** = an "Add rental" item in the existing-booking kebab (`BookingModal`), `refId=booking.id`,
  `contextName=studentName`. **(b) standalone** = a "Record rental" button on Bookings → *All*, no `refId`.
- types/service/mock/hook (`useRecordRental` → invalidates the daily report; rental is revenue) + `rental.*` i18n EN+TH.

### 🟡 One deliberate gap — rental prices aren't shown (routing, not guessing)
*What to build* says "VAT-inclusive prices shown as-is", but the four prices live only in BE `sale-items.ts`
(`RENTAL_PRICES`) with **no endpoint exposing them** (they're not on `/sellable-packages` — that's courses/vouchers).
This repo's rule is explicit — *"the FE must never carry a second copy of the price card"* (the whole point of the
sellable-packages design) — so I did **not** hardcode 200/150/50/50 into the FE. Recording is fully functional without
a price (the server computes revenue from `code`); price display isn't a DoD checkbox. **@Sober/@Jason — small ask to
show prices: expose the rental items** (`rentalItems:{code,label,priceMinor}[]` on `/sellable-packages`, or a tiny
`GET /rentals/items`). Same shape as the TASK-122 reset gap — I'll wire the price display the moment it's exposed.

### TASK-107 follow-up
Added `lib/scheduler/sellable.test.ts` — allowed-group → true, excluded-only → false, and the **no-over-hide** cases
(unpriced / unknown / card-not-loaded → true, server is the backstop). 3 pass. Closes your minor non-blocking note.

**Remaining Fern queue: TASK-102 (Settings)** — now unblocked by your TASK-122 (`DELETE /api/settings/:key` for a true
reset). I'll take it next. **@Sober: 109 ready for review** (+ the price-endpoint call). **Ball: @Sober** (109 review +
rental-price endpoint) **& @Fern** (102 next).

## [Sober (SA)] — TASK-109 REVIEW = **PASS ✅** (reproduced) + rental-price call = **TASK-123 (greenlit @Jason)**. 107-note closed.
### 🟢 TASK-109 (rental FE — both surfaces + standalone idempotency) — PASS, reproduced.
- **Idempotency is honest end-to-end**, not hopeful: BE `rentalIdBase = refId ?? clientKey ?? undefined`
  (`sale-items.ts:144`) — add-on idempotent on **refId+code**, standalone on the **client key**, both `?? randomUUID`
  only when neither exists. The FE mints one `crypto.randomUUID()` per open (regenerated on reset) and sends it
  **only when `refId` is absent** (`RentalModal.tsx:56`) — so a double-submit standalone posts once, and the add-on
  never needs a key. This is exactly the TASK-108 fix, wired correctly.
- **Codes match FE↔BE** (`["rental-set","rental-ride","rental-helmet","rental-pads"]`, both frozen `as const`).
  Two surfaces present: (a) add-on in `BookingModal` kebab (`refId=booking.id`), (b) standalone "Record rental" on
  Bookings→All (no `refId`). Errors surfaced (`RENTAL_NOT_POSTED`/502), never a dead button.
- **Evidence:** `bunx tsc --noEmit` → **0**. **107 follow-up:** Fern added `lib/scheduler/sellable.test.ts` (allowed/
  excluded-only/no-over-hide) → **3 pass** — my minor non-blocking note is closed. **TASK-109 DONE.**

### 🔨 Rental-price display — SA ruling: **extend `/sellable-packages`, not a new endpoint.** → TASK-123 @Jason (greenlit)
Fern's gap is real and correctly handled: the 4 prices live only in BE `RENTAL_PRICE` (200/150/50/50), not on any
endpoint; the repo rule forbids a **second copy of the price card** on the FE, so Fern did NOT hardcode them.
**Ruling** (API-shape, I own it): add `rentalItems:{code, priceMinor}[]` to the existing `GET /sellable-packages`
(the one "what's sellable + price" card the FE already loads) — **not** a new `GET /rentals/items` (avoids a new
endpoint + hook, keeps one price authority). code+priceMinor only; the FE owns labels via i18n. Wrote **TASK-123**
(BE, greenlit @Jason). Recording already works without it — this is display-only, non-blocking for the rental feature.

**Ball: @Jason** (TASK-123 — small `rentalItems` field) **& @Fern** (TASK-102 Settings next, then wire the rental
price off TASK-123) **& @Tanya** (#3/#4 sid verify). REQ-028 rental recording is DONE; only the price *display* awaits 123.

## [Tanya (QA)] — 🧭 QA HANDOFF, end of day. Nothing is half-run; two answers and one phone are what's left → @Porter

Stopping here. My side of the shipped batch is closed out — board, `REGRESSION.md`, the footprint ledger and
`tests/TEST-REQ030-BATCH-post-deploy-acceptance.md` are all current, so tomorrow starts from files, not memory.

### Where I left the statuses
- ✅ **`TEST_PASSED`:** REQ-030 · REQ-037 · OBS-3 (via TASK-115) · REQ-009 · REQ-022. **Zero defects found.**
- 🟡 **PARTIAL / IN_TEST, and only the owner can move them:** **REQ-020** (a real LINE claim must exist before
  approve / reject / collision can be exercised) and **REQ-023** (the digest ACs are about what lands on a
  phone). Both now have step-by-step scripts — `tests/CLICK-SCRIPTS-owner.md` **Script 4** and **Script 5**.
- ⚪ **Two lines I deliberately did NOT round up:** REQ-037's **revenue posting** (needs a day-end run) and
  REQ-009's **past-P&L immutability** (backoffice, cross-month).

### Open for me tomorrow — all of it waits on you, none of it is blocked work I can start
1. **The clock ruling.** Machine clock = **2026-08-10**, today's log file = **2026-08-04**. Everything
   time-relative in my evidence (the 3-day notice, expiries) reads against the machine clock. Until you rule,
   I won't re-date any evidence — re-labelling results after the fact is how a record stops being one.
2. **REQ-037 revenue** — re-check after the next day-end, or is it the owner's P&L eyeball?
3. **Scripts 4 + 5** — hand them to the owner when she next has her phone; I'll take the results and close
   REQ-020 / REQ-023.
4. Backlog I own, unchanged and unhurried: the **`kind=voucher` plan shape** (still unexercised — the only live
   vouchers belong to other people) and **DEF-1** (voucher "Manage" clipped at 375, MINOR, desktop-only ruling).

### Environment state, so nobody wonders tomorrow
Nothing left running: no local dev server, no browser session, no minted cookie anywhere on disk. `sid`
footprint is declared in `tests/DEV-SERVER-FOOTPRINT.md` — QA-owned rows only, all four throwaway teachers
**archived**, roster verified **FULL_TIME 7 / PART_TIME 6 / FREELANCE 10**. Production untouched.

**Ball: @Porter** (mark REQ-030/037/OBS-3 DELIVERED · the clock ruling · the revenue question · the two scripts
to the owner). Nothing on my side is mid-flight.

---

# 🧭 EOD HANDOFF (real date 2026-08-10; log file lags). Continue tomorrow.
Big day: the 3-day ship DELIVERED **and** the team raced into the essential set + fast-follow.
- **✅ DELIVERED (deployed sid + acceptance, zero defects):** REQ-030 · REQ-037 · OBS-3 · REQ-009 · REQ-022.
- **Essential set (REQ-038 #1–5) — ~90%:** #1 ✅ deployed · **#2 ✅ closed** (calendar multi-course course-picker = TASK-121, PASS) · #3/#4 🟡 need `sid` verify (search-on-timetable-page / voucher-shows-class) · **#5 🔄** deduction-history: **TASK-119 BE PASS**, **TASK-120 FE in REVIEW** (Tier-1, read-only timeline, no migration, no "who").
- **🎁 Fast-follow bonus done ahead of schedule:** TASK-107 (voucher picker) PASS · TASK-109 (rental FE) PASS · TASK-102 (settings screen) unblocked via TASK-122 (DELETE/reset endpoint, PASS). New small: TASK-123 (rental price) greenlit @Jason.
- **🟢 @Sober:** review TASK-120 (deduction FE) + TASK-123; then essential set is build-complete.
- **🟢 @Jason:** TASK-123 (rental price).
- **🟡 @Tanya:** `sid`-verify #3/#4 · accept deduction-history when 120 lands · REQ-037 revenue re-check after next day-end. (Clock ruled: **2026-08-10 authoritative** — already in the DELIVERED entry above.)
- **🟡 OWNER (only phone item):** run `tests/CLICK-SCRIPTS-owner.md` Scripts 4 + 5 → closes REQ-020 + REQ-023.
- **🎯 @Porter next:** when #5 FE passes + #3/#4 verified → essential set = 100% → **deploy to the customer's real env** (their production, not just sid). Then 6–9 + prior backlog.

## [Sober (SA)] — TASK-120 REVIEW = **PASS ✅** (reproduced). **Essential set #5 build-complete.** → @Porter @Tanya @Fern
`CourseHistoryModal` (the "ประวัติการตัดคอร์ส" timeline) — verified against the real code:
- **Read-only, server-rendered** — the FE maps `data.events` straight to a Mantine `<Timeline>` in the order the
  server returns; **no client recompute, no re-sort** (SPEC-035 Tier-1 discipline held). Summary badges = used/size ·
  leave · remaining · liveEndDate from `data.summary`.
- 🔑 **The one real risk — cleared.** `kindLabel` builds i18n keys by hyphenated transform: `no-show`→
  `history.kindNo-show`, `makeup-appended`→`history.kindMakeup-appended`, etc. I checked the dictionary: **all 9**
  BE `HistoryKind` values have labels, **EN and TH** (attended/no-show/cancelled/sick-leave/scheduled/makeup-appended/
  extra-session-added/freelance-drawn/freelance-refunded) — no kind falls through to a raw key on screen (would've hit
  Thai users first). Subtitle shows subject · teacher · make-up-of-date · reason.
- **Actor limit shown, not hidden** — the gray `history.actorNote` Alert ("who made each change isn't tracked yet —
  shared login") renders on every view (SPEC-035 §1). `useCourseHistory(id, enabled && !!id)` guards a null course
  (no stray fetch); trigger is a per-course **History** button on `CoursePackagePanel`.
- **Evidence:** `bunx tsc --noEmit` → **0** (ran it). **TASK-120 DONE.**

**Essential set (REQ-038 #1–5) is now BUILD-COMPLETE:** #1 shipped · #2 done (TASK-121) · #5 BE+FE done (119/120) ·
#3/#4 are **verify-only** on `sid` (@Tanya). **@Porter:** once @Tanya green-lights #3/#4 + accepts the deduction
history on `sid`, the essential set = 100% → the **customer-env deploy** you flagged. **Ball: @Tanya** (#3/#4 verify +
accept 120 on sid) **& @Jason** (TASK-123 rental price — the only open build) **& @Fern** (TASK-102 settings; then wire
rental price off 123). Nothing on SA.

## Fern (FE) — TASK-102 → REVIEW. Settings screen — the load-bearing half of REQ-031 is now real. → @Sober
Took the ball. Built on your TASK-122 `DELETE` so **reset-to-default is honest** (not PUT-the-default). tsc 0 · build
ok · `/scheduler/settings` route generated.

- **Renders from the API list, not a hardcoded form.** `GET /settings` → one Card per rule, so the "a 3rd rule needs
  no FE change" AC holds by construction. Each card: label · current value+unit · default+unit · an **Override
  (orange) / Default (gray)** badge — default-vs-override is unmistakable at a glance.
- **Edit** = inline `NumberInput` → `PUT /settings/:key`. A rejected value shows the **server's exact reason** in a red
  Alert (the mock mirrors the registry bounds 0–30 / 0–240 so the reject path is exercisable offline) — never silently
  accepted.
- 🔑 **Reset** (shown only when overridden) → confirm Modal → **TASK-122 `DELETE /settings/:key`**; the response's
  `isOverridden:false` + `value:default` flips the badge back with no manual refetch. This is the piece PUT-the-default
  couldn't do honestly — thanks for the endpoint.
- Full layer: `types/app/settings`, `settings.service` + mock, `useSettings`/`useUpdateSetting`/`useResetSetting`,
  `partials/Settings/SettingsContent`, the page, a `nav.settings` entry (Settings2 icon). Route auto-guarded by the
  existing `/scheduler/:path*` proxy — no proxy change. `settings.*` i18n EN+TH.
- STANDING RULE n/a — a single stacked `NumberInput`, no shared-row control to measure.

**@Sober: 102 ready for review.** That's the **whole Fern build queue clear** — 099/100-FE/105-FE/107/109/120/121/102
all delivered (120/121/107 already DONE; 099/100-FE/105-FE/109/102 in your review queue). **Remaining Fern item:**
wire the rental **price display** once @Jason's **TASK-123** exposes the rental items (I flagged the missing endpoint on
109; 123 is the fix). I'll pick that up when 123 lands. **Ball: @Sober** (102 + the FE review queue) **& @Jason**
(TASK-123 → unblocks my rental-price wire).

## Fern (FE) — TASK-124 → REVIEW. Student search on the calendar — **the last essential-set build.** → @Sober
Spotted it on the board (SA-scoped 2026-08-10, the REQ-038 #3 build Tanya's sid pass found genuinely unbuilt, DEF-2).
Built to your two-edit spec exactly. tsc 0 · build ok.
- **`CalendarContent`** — `studentQuery` state + `byStudent(list)` (case-insensitive substring on `b.studentName`,
  empty → pass-through), composed as `byStudent(byBadge(...))` inside **both** the day and week memos (with
  `studentQuery` added to each dep array). Composes with Teacher/Type/Badge, doesn't replace them.
- **`CalendarHeader`** — optional `studentQuery`/`onChangeStudentQuery` props; a clearable `TextInput` (Search icon +
  a `CloseButton` shown only when non-empty) placed **first** in the existing `teachers.length>0` filter row.
  `calendar.studentSearch`(+placeholder) EN+TH.
- Left the teacher columns as-is — a matched student's sessions just appear under their teachers (the intended
  "where is this student" view); no match-only column collapse (your no-scope-creep note).
- **STANDING RULE:** the filter row is the same `flex-wrap` + per-control `min-w` + `basis-0 grow` pattern the 3
  existing controls already use; the 4th (TextInput `min-w-52 grow-[3]`) shares the row when wide and **wraps** to its
  own line when the `min-w` sum exceeds the container (≤~768) — it degrades by wrapping, never clips. **Live pixel
  measure at 1600/1280/768/375 → @Tanya** (the calendar is NextAuth-gated; same routing as TASK-099's width pass — I
  can't drive the authenticated page from here).

**@Sober: 124 ready for review.** With this, **REQ-038 #3 is built** — the essential set's last FE build is done; #3/#4
now just need Tanya's `sid` verify. **Ball: @Sober** (124 review) **& @Tanya** (#3 verify + the 124 filter-row widths).
Still-open Fern items: TASK-125 (LOW/optional eligible-label tiebreaker) and the rental-price wire (blocked on 123).

## Fern (FE) — TASK-125 → REVIEW. Expiry tiebreaker (OBS-5) — cleaned up the last LOW item while in the file. → @Sober
Already in `BookingModal` for 124, so I folded in the LOW/optional OBS-5 tiebreaker too. tsc 0 · build ok. One line in
`eligibleLabel`: `multiCourse = eligible.filter(x => x.id === e.id).length > 1` → append ` · exp ${c.expiryDate}`
**only** then, so two courses identical in subject+size+progress are distinguishable while single-course/distinct rows
stay clean. `value` still `courseId`; no BE (`expiryDate` was already in context). Accepted residual (per the task):
same-package-same-day pair → same expiry → still identical; no courseId fragment (user-hostile).

**@Sober: 125 ready** (LOW, rides the next FE deploy). **Now the entire Fern board is clear** — every FE task is DONE
or in your review queue (099/100-FE/105-FE/102/109/124/125). **The only remaining Fern work is blocked:** the rental
**price** wire waits on @Jason's **TASK-123** (rental-items endpoint). **Ball: @Sober** (FE review queue) **& @Jason**
(123). Nothing else unblocked for Fern.

## Jason (BE) — TASK-123 → REVIEW. Rental prices on `/sellable-packages`. **Unblocks Fern's rental-price wire.** → @Sober, @Fern
Small, display-only — one price authority, no FE copy. tsc 0 · **458/0**.
- **`rentalPriceList()`** (`sale-items.ts`): derives `{code, priceMinor}[]` from `RENTAL_CODES` + `RENTAL_PRICE` (the
  same constants `RENTAL_ITEMS` already maps — so the displayed price and what `recordSale` charges can't drift).
- Added to the `GET /sellable-packages` payload as **`rentalItems`** — **code + priceMinor only**; no name/i18n
  crosses the wire (the FE owns labels via `rental.item*`, per the repo rule). The BE response is Hono-RPC-inferred, so
  the field flows into the FE's inferred type automatically.
- Pure test: the four codes at 200/150/50/50 THB (satang), each matching its seed item's `unitPriceMinor`.

**@Fern: rental price wire unblocked** — read `rentalItems` off `/sellable-packages` (update the FE mock to carry it);
no FE-side price table. **@Sober: 123 ready** (trivial; the no-second-copy derivation is the point). **Ball: @Sober**
(123 review) **& @Fern** (rental price display + the FE review queue). My BE queue is clear again — remaining Jason
items are TASK-116/117 (SPEC-034 stock, blocked on Porter go-live-vs-fast-follow) and TASK-103 (post-go-live).

## Jason (BE) — no startable work; used the gap for a cross-task self-scrutiny of the live-money wave. → @Sober, @Porter
BE queue is genuinely drained (all mine in REVIEW; 103/110 post-go-live; 116/117 blocked on Porter), so instead of
forcing a blocked live-money migration I ran an **integration** pass over 104/105/112 — they each touch the same
course-engine + freelance money and were built incrementally, so their *interactions* are what per-task review misses.
Suite green **458/0**, tsc 0. Traced every cross-task path by inspection:
- **Cancel an extra (112+105):** `reconcileCoursePlan` loads COURSE_PACKAGE-only → the SINGLE_SESSION extra is
  invisible → **no re-owe**; its own hold releases via `reconcileBookingHolds(id, CANCELLED)`. ✓
- **Cancel a NO_SHOW (105):** was DELIVERED (counted) → CANCELLED drops `current` → **re-owes one makeup**; money
  already 0 (NO_SHOW releasing). ✓
- **Cancel a SICK_LEAVE (104+105):** the leave was already out-of-plan AND already had a makeup, so `current` is
  unchanged → **no double re-owe**; hold already 0 (104) → the money reconcile is a no-op. ✓
- **Append-vs-money ordering:** the makeup `reconcileCoursePlan` appends is a NEW booking id; the tail
  `reconcileBookingHolds(id, …)` only touches the cancelled booking → **no double-draw**; the makeup draws on its own
  confirm. ✓

**No defect.** One 🟡 **product edge for an owner confirm (not a bug), routed @Sober→@Porter:** cancelling a
`SICK_LEAVE` that already spawned a makeup **leaves the makeup in place** (`current` stays == size). Defensible (the
student keeps their earned makeup), but if "undo a leave" should also drop the makeup, that's a rule call — rare
action, low priority, safe to defer.

**@Porter — the SPEC-034 read to unblock my last stream (TASK-116→117):** 116 is a **`bo.item` migration** (`kind`
column) + reseed; 117 is 🔴 **live money** (atomic sale stock-draw + block-at-0 + revenue in-tx — the income mirror of
the freelance ceiling). Honest BE read: **(a) go-live** = the sell-side gets the same atomic guarantee the freelance
ceiling already has (no more best-effort `void recordSale` for sellables), but it's a migration + a careful live-money
pass in the go-live week — real risk to schedule. **(b) fast-follow** = ships days after 08-20; the money core is
already solid without it (revenue posts; only *stock caps* are missing, and most items are unlimited). **My lean:
fast-follow** — it's not on the critical path for the 08-20 customer value, and live-money changes deserve a calmer
window than go-live week. Your call; I build 116→117 the moment you rule (careful pass either way). **Ball: @Sober**
(my review queue) **& @Porter** (SPEC-034 timing).

## Fern (FE) — rental **price display** wired (TASK-109 gap closed via TASK-123). → @Sober, @Jason
Took the unblock the moment 123 landed. tsc 0 · build ok. The price gap I flagged on 109 is closed **without** a second
FE price copy — exactly the derivation Jason built for.
- **`SellablePackagesResponse.rentalItems`** (`{code, priceMinor}[]`) added to the FE type + mock (mirrors the real
  card 200/150/50/50). `RentalModal` now `useSellablePackages()` and `priceOf(code)`.
- **Display:** each item Select option shows its VAT-incl price (`label · ฿NNN`), and a live **`Total: ฿(price ×
  hours)`** line appears once an item + hours are chosen. `rental.total` i18n EN+TH.
- **No FE price table** — the prices are read from the server's `rentalItems`; if a price changes on the BE, the FE
  follows with no code change (the whole point of 123's server-side derivation). Thanks @Jason.

**@Sober: the rental price display is ready** (folds into the existing TASK-109 review). **With this the Fern board is
fully clear — every FE task is DONE or in your review queue, and nothing is blocked.** No open Fern build remains.
**Ball: @Sober** (FE review queue: 099/100-FE/105-FE/102/109/124/125 + this price add) **& @Tanya** (`sid` verify #3/#4
+ the 124 filter-row widths). If nothing new is routed to Fern, I'm idle pending reviews.
