# 2026-09-19

## [--:--] Fern (FE) — ✅ **TASK-398 done — DUO/Group: the group cell (`n/cap` from `group.seats`, the DUO/Group tag), `Create group` by the 50th key on the shared fields + picker, sell-a-course-into-a-group through the EXISTING course form (three locked, `groupKey`), the swap (own route, "no message is sent" in one line), the roster, `In group:` on a seat.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  442 pass / 0 fail   (was 435; +7)
bun run build     → ok
```
23 modified · 5 new. Built to §0 byte-for-byte + Jason's two notes. Notes in the TASK §2. 🚫 No deploy asked.
`BookingType` gained `GROUP` — the compiler found every `Record<BookingType>` (icon, a teal `--booking-type-group`, chip,
`bookingType.GROUP` ×2); the two hand-written arrays got their line. The cell: `GroupSeatsLine` = `Seats n/cap` from
`group.seats` (pure `seatsLabel`, any status counts) + names; the Stage 1 tag grows DUO/Group from `group.kind`; the legend's
two tags; the calendar partials read no `groupId` (walk-pinned — the server hides seats). `Create group` beside `Create series`
by `action:calendar.group-series` ⇒ DUO's cap LOCKED at 2 (the one client rule, pure), the ONE `OtherScheduleFields` (`ratesOnly`)
+ the SHARED `MultiDateField` (extracted from the OTHER series this task; one `type=multiple` across the three files) ⇒
`POST /bookings/group-series` via pure `groupSeriesBody`; a 409 keeps the ticks. Sell into the group = the EXISTING
`CreatePlanFlow` with a `group` prop (teacher/date/time prefilled + locked, `groupKey` in the literal), by
`bookings.course-create`. Swap = `PATCH /bookings/:id/group-teacher { teacherId, fromHereOn }` with the "no message is sent"
line (both languages, pinned). Roster with `StatusChip`s linked to the Bookings page; cap/rates via the Stage 1 editor
reading `group`; `In group:` on a seat. Keys `booking` +21 · `calendar` +2 · `bookingType` +1 ×2. Snapshot 50 = 50 vs the BE.
🔑 **Break-and-watch ×14 in `try/finally`** ⇒ 1–3 each (two slipped first — a CONFIRMED-only fixture, a `group: null`
lesson — both pinned); md5 identical on 9 files.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-398 re-verified (442/0, tsc 0, build ok; one `type="multiple"` in the shared `MultiDateField`; snapshot 50 = 50) ⇒ **✅ DONE — REVIEWED**. **REQ-095 Stage 2a complete both sides**; the `sid` batch (396 + 394/395 + 397/398) declared ready to @Porter with `db:migrate 0040–0041` (0041 closed shop), verify 42, and Tanya's round for all three. Chain stopped; ball @Porter.
# 2026-09-19

## Porter (PM) — 🟢 REQ-095 Stage 2a (DUO/Group course-based) complete (TASK-397 BE mig 0041 + 398 FE). sid batch ready: 396 + Stage 1 (394/395) + Stage 2a (397/398). FE 442/0 · BE 2466/0 · migrations to 42 (0041 = closed-shop index rebuild). Owner to deploy sid (0040+0041, verify 42, quiet minute); Tanya tests all three. Open owner decisions (gate Stage 2b/3, not the batch): swap-teacher notice words; Seats/Heads label; Stage 2b per-session price (§4.4); Camp "open week" (§3.4). 396's uat-alone option still available for the team if owner wants it sooner.

## Porter (PM) — owner: sid deployed, db:verify=42 (0040+0041 clean single run). @Tanya GO on 396 + Stage 1 + Stage 2a.

## Tanya (QA) — 🟡 **sid batch: Stage 1 (ECA/Free) & Stage 2a (DUO/Group) PASS; TASK-396 BLOCKED (internal job unreachable).** (a) 396: /api/internal/jobs/end-of-day 404 via frontoffice + secret-gated, no INTERNAL_JOB_SECRET ⇒ NOT_TESTED, DATA REQUEST (owner runs the job / unit test). (b) Stage 1: 1) OTHER KOL/12/฿500 ⇒ DTO other{kind,headCount:12,teacherRates:{t:50000},ratePostedAt:null}, ledger untouched. 2) other-series 3 dates ⇒ created:3; taken date ⇒ 409 SLOT_TAKEN "…ไม่ได้สร้างรายการใด" (all-or-nothing). 3) PATCH /bookings/:id/other headCount 12→20, no LINE. 4) no action:calendar.other-series ⇒ 403 "ไม่มีสิทธิ์ทำรายการนี้" (also group-series). (c) Stage 2a: 1) DUO cap 2, Seats 0/2; seatCap 3 ⇒ 400. 2) sell 6-session course in ⇒ dates 4→6 extend. 3) 2nd ⇒ 2/2; 3rd ⇒ 409 GROUP_FULL "วันที่… กลุ่มเต็ม (2/2)". 4) cancel GROUP date ⇒ both children make-ups. 5) group-teacher swap fromHereOn ⇒ 200 notification none (no LINE), refuses non-working teacher. 6) seat "In group:" = FE render, seats hidden from calendar list by design (couldn't read via list). 7) PAUSED-slot-FREE = slot FE detail, not exercised. 8) 08:15 reminder Seats:2/2+names = outbox, owner-on-uat. Footprint: swept 16 GROUP/4 OTHER/2 courses (none live); qa-rbac-admin disabled. Report in inbox/PM.md.

## Porter (PM) — batch QA: Stage 1 (ECA/Free) & Stage 2a (DUO/Group) PASS on sid. TASK-396 not live-testable on sid (internal job secret-gated) — but unit-pinned by value in code. 🔴 Flagged: 396 (customer's 17:00 bug) is coupled to the cutover — current HEAD carries RBAC+Other needing 0036-0041, so 396 can't reach uat without the full migration set unless Sober cuts a standalone patch on the uat baseline. Put 3 options to owner: (1) do the full cutover now, (2) 396 waits for cutover, (3) Sober cuts a standalone 396 patch for a quick uat BE deploy.

## Porter (PM) — owner chose (1): FULL uat cutover now. Gave the complete checklist: quiet hour (0041 closed-shop + shared-login death) → build HEAD → db:migrate 0036-0041 (if ledger mismatch, seed-ledger→--apply→migrate; verify 42) → BOOTSTRAP_ADMIN_* (8+ chars) in frontoffice\back env → restart → first login super admin → create roles+ALL staff accounts in one sitting → remove ADMIN_*. No sale:ensure-items (5th tier already on uat). Post-cutover owner checks: super-admin/staff logins, 17:00 cut at 17:30 tomorrow, rental+group LINE text. Everything (RBAC option C + 093/094/§14 + Other Stage1/2a + 396) ships in this one cutover.

## Porter (PM) — uat cutover migrate: hit the TASK-085 ledger skip again (0036_users row missing, schema had all 42). Documented repair ran clean: seed-ledger dry-run (42 applied in schema, 1 ledger row to insert) → --apply (inserted 1) → db:migrate (0 pending) → db:verify = 42 CLEAN. Migrations done on uat. Remaining: BOOTSTRAP_ADMIN_* (8+) in frontoffice\back env → restart BE+FE → first login creates super admin → create roles+staff → remove ADMIN_*. (The uat ledger-behind pattern recurs every big deploy — already in SYSTEM-FACTS.)

## Porter (PM) — 🎉 uat CUTOVER COMPLETE. Super admin (admin) bootstrapped on frontoffice.develyst.online, Users+Roles visible, migrations at 42. LIVE on uat: RBAC option C (all 4 stages) · REQ-093 archive · REQ-094 extended auto check-in · REQ-091 §14 (course-confirm rental line, pay-per-session, remove-remaining) · TASK-396 start-based end-of-day (customer's 17:00 bug) · REQ-095 Other Stage 1 (ECA/Free) + Stage 2a (DUO/Group). Operational remainder (owner): create staff accounts + roles before opening; remove old ADMIN_*. Post-cutover checks: 17:00 cut at tomorrow's 17:30 run; rental/group/teacher LINE text. Still pending: Other Stage 2b (per-session, needs §4.4 price) + Stage 3 Camp (needs "open week" def).

## Porter (PM) — REQ-095 §6: DUO/Group prices (DUO 1hr 1900/4-6-10 6800-9360-14200; Group 1hr 1090/6-10 5290-7790), per-session=1hr walk-in; teacher rate=backoffice, student revenue posts here like Private (Sober confirms products/gap). Camp = day-based, unused=paused credit no expiry, admin opens each week, credit redeemed into future open week. GO Sober Stage 2b (after product check) + WRITE Stage 3 Camp spec+sizes (don't cut yet).
- **@Sober** — Porter: REQ-095 §6 (prices; GO 2b after a product check; Camp SPEC, don't cut). Catalogue read: **Group prices exist** (`balance-group` 1,090/5,290/7,790 = the card); **DUO net-new** (`balance-duo` 1,900/6,800/9,360/14,200, 4 items via `sale:ensure-items`) and the price must follow the GROUP's kind, not the subject — confirmation asked. Wrote **SPEC-082** (Camp: `camp_weeks`/`camp_packages`/`camp_days`, half-day units, no expiry column, day cut in the end-of-day job; 3a BE L·FE L, 3b BE M·FE S; 8 owner decisions). Nothing cut. Ball @Porter.

## Porter (PM) — REQ-095 §8: owner "เอาตามแนะนำ" — DUO/Group price by group-kind (resolvePriceGroup); half-day units; camp informational on teacher slots (3a); no-show consumed; early bird = discount at sale. Camp prices locked (11500/5900/2600/1300, EB 10500). GO Sober: Stage 2b (balance-duo price group + per-session seat) + Stage 3a Camp (SPEC-082, 4 products, day-based credit/pause). All on sid.
- **@Sober** — Porter: all §8 decisions "เอาตามแนะนำ", GO 2b + 3a. Cut **TASK-399** (2b BE, contract first: `balance-duo` + `resolvePriceGroup` by group kind + walk-in seat, no extend), **TASK-400** (2b FE), **TASK-401** (Camp 3a BE, contract first, `0042_camp`, 4 items, 13/54 keys), **TASK-402** (Camp 3a FE). Board rows; both inboxes. Told Porter the assumed §4 items (capacity nullable, sale-only revenue, no credit↔hours) and the walk-in no-extend rule. Ball @Jason (TASK-399 contract).

## Jason (BE) — 📋 TASK-399 contract in the TASK: §1 verified — `resolvePriceGroup` has FIVE callers, the day-end revenue sweep is the one the list missed (a DUO walk-in would post the Private price); `balance-duo` via the same card/flatMap; the three-branch resolver with every caller by source; the walk-in seat on `SINGLE_SESSION` only, the cap through the same count, no extend by construction. Three ❓ to Sober. Waiting on confirm; 401's contract after.
- **@Sober** — TASK-399 contract from @Jason CONFIRMED: his fifth caller (the day-end revenue sweep) was the real catch — without it a DUO walk-in would post ฿1,390 (subject) instead of ฿1,900; pinned by value. Added: `--dry-run` on `ensure-sale-items`; `GROUP_NO_DATE` dropped; group DTO gains `priceGroup`. TASK-400 §0 updated to the confirmed shapes. Ball @Jason (build).

## Jason (BE) — ✅ TASK-399 DONE (code): `balance-duo` price group (four items by the same card/flatMap), `resolvePriceGroup(subjectId, groupKind)` with FIVE callers incl. the day-end sweep (a DUO walk-in posts ฿1,900 — pinned by value), the walk-in seat (`SINGLE_SESSION` + `groupId`: 404 / 400 / the ONE cap count / no extend), the group DTO's `priceGroup`, `ensure-sale-items --dry-run`. 2480/0, 193 files, tsc 0, 42 = 42. Seventeen mutations bite (one passed first — the OTHER fixture's kind; pinned). Moving to TASK-401's contract.

## Jason (BE) — 📋 TASK-401 contract in the TASK: §1 verified (the voucher's sale shape reused, a fifth `assertMayDiscount` site, the cut in the same end-of-day tx, both guards, the registries 13/54); §2 with corrections (`0042_camp` witness = the unique index, no expiry by absence, the students-FK blink named; the units rule; the ONE redeem writer; the five 409s; CANCELLED only before the day). Six ❓ to Sober. Waiting on confirm.
- **@Sober** — TASK-399 re-verified (2480/0, tsc 0, 42 = 42; five `resolvePriceGroup` sites; `--dry-run`) ⇒ **✅ DONE — REVIEWED**; Fern released on TASK-400. **TASK-401 contract CONFIRMED** (six answers: FULL_WEEK 5, nav after Badges, no early-bird date rule, own prices route, Camp card via `/camp/packages`, 3a transitions); TASK-402 §0 rewritten to the confirmed DTOs. Porter told (the sweep catch; the deploy step). Ball @Jason (TASK-401 build).

## Jason (BE) — ✅ TASK-401 DONE (code): Balance camp — `0042_camp` (three tables, UNIQUE (package, date) index = witness, no expiry column by structure, 43 = 43, preflight clean alone and as the 0038 → 0042 batch) · `lib/camp.ts` pure rules (half-day units, credit = total − used − planned, ATTENDED | ABSENT consume, the 3a transitions incl. ATTENDED ↔ ABSENT, CANCELLED refused on/after the date) · four camp items (11,500 / 5,900 / 2,600 / 1,300) · `menu:camp` + four `action:camp.*` + nine access rows · `/api/camp` (prices · weeks · roster · packages in the voucher's sale shape · redeem through the ONE writer with 409s naming the date · mark) · `cutCampDays` inside the end-of-day tx · `campWeeks` on the calendar. **2499 pass / 0 fail**, 194 files, tsc 0; 19 pins new, 14 files re-pointed 42 → 43, registries 13 / 54, `SALE_ITEMS` + 4; twenty-one mutations bite (byte-identical restores); `sale:ensure-items --dry-run` shows the four camp items (+ TASK-399's four still missing on my box). Contract lines for @Fern final in the TASK. Awaiting @Sober's review. 🚫 No deploy request; the human runs `db:migrate` (verify 43) + `sale:ensure-items` after the deploy.
- **@Sober** — TASK-401 re-verified (2499/0, tsc 0, 43 = 43; no expiry column — the only `expir` hits are the header saying so; `menu:camp` registered) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-402 (after 400) with Jason's final DTO lines. Porter told the deploy steps (`db:migrate` ⇒ 43, `ensure-items` +4/+4) and Tanya's 3a checks. Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-400 done — the course form inside a group prices by the GROUP's card (`group.priceGroup`); `Walk-in seat` through the EXISTING single-session form (locked + `groupId`); the `1h` chip.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  446 pass / 0 fail   (was 442; +4)
bun run build     → ok
```
10 modified · 1 new. Notes in the TASK §2. 🚫 No deploy asked.
📌 The card question answered: it is NOT keyed by subject only — every package carries `priceGroup`, and the four
`balance-duo` packages carry NO subject (no program is priced as a DUO), so the solo readers cannot see them. Two pure
readers (`courseSizesForGroup` / `packageForGroup`) pick by the DTO's `group.priceGroup` (1h excluded; value-tested DUO
4/6/10 · Group 6/10); the FE never maps kind → group and holds no price (negatives). No BE change needed. The course form
inside a group prices by that card (sizes, ราคาเต็ม, the discount base); solo untouched. `Walk-in seat` on the GROUP row by
`calendar.book` ⇒ the SAME create form via the overbook door's shape: no tabs, one teal line, teacher + time locked,
`groupId` in the single-session literal only; the form's own slot pre-check is skipped on a walk-in (it would find the
group row) — the server decides 404 / 400 / `GROUP_FULL`. `1h` chip from `courseId: null`. `booking` +3 ×2.
🔑 **Break-and-watch ×10 in `try/finally`** ⇒ 1–2 each; md5 identical. Moving to TASK-402.

## [--:--] Fern (FE) — ✅ **TASK-402 done — the `Camp` menu (weeks · roster · Mark · Redeem · Sell · open/edit/close), the student's Camp card (credit in days with the ½), the calendar day banner from `campWeeks`.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  454 pass / 0 fail   (was 446; +8)
bun run build     → ok — `○ /scheduler/camp`
```
17 modified · 10 new. Built to §0 + Jason's final DTO lines (TASK-401). Registries 54 = 54 actions · 13 = 13 menus vs the BE by
script, in order (`menu:camp` after Badges; the four `camp.*` after `badges.*`). Notes in the TASK §2. 🚫 No deploy asked.
`lib/camp/units.ts` pure + value-tested (units ⇒ days + ½ by floor/mod; the banner rows = OPEN weeks covering the date with that
day's server count; the sell/redeem bodies). The Camp menu: weeks by month → open / edit / close (`week-open`) → the roster
(a card per date, `count/cap` shown not enforced, kind/half + status chips, `Mark` = the three server words ⇒ `PATCH
/camp/days/:id`, `Redeem into this week` ⇒ packages with credit ⇒ half ⇒ the SHARED `MultiDateField` limited to the week ⇒ one
call, a 409 keeps the ticks; `Sell a camp` = kind × plan, the price from `GET /camp/prices` (× days on DAILY), the EXISTING
discount block, an optional first week ⇒ one call). The student's Camp card on every People row (`n½ days left` from the
server's units; no expiry, asserted). The calendar day banner above the DAY grid only; every camp write invalidates the calendar.
Keys `camp` 60 · `nav.camp` ×2. Pins moved with the reason (`MENU_KEYS` 13, snapshot 54 + the `camp` area, the sweep 86 / 33,
the matrix 16 columns, TASK-365's gap start).
🔑 **Break-and-watch ×16 in `try/finally`** ⇒ 1–4 each (one slipped first — an `aria-disabled` cap — pinned); md5 identical.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-400 (446/0) and TASK-402 (454/0, `/scheduler/camp`, 13 = 13 / 54 = 54) re-verified ⇒ both **✅ DONE — REVIEWED**. **REQ-095 complete through Stage 3a both sides.** Told @Porter: `sid` batch ready, deploy steps (`db:migrate` ⇒ 43, `ensure-items` 8, grant the new keys), Tanya's round for 2b + 3a; 3b waits on the owner's words. Chain stopped; ball @Porter.

## Porter (PM) — 🟢 REQ-095 Stage 2b + 3a complete (TASK-399/400 2b · 401/402 3a): DUO price group net-new (balance-duo, resolvePriceGroup by group-kind, pinned so the day-end sweep prices right), per-session walk-in seat; Camp SPEC-082 (0042_camp 3 tables verify 43, half-day units, credit/pause no expiry, early bird = typed ฿1000 discount, admin opens weeks). sid deploy: migrate 0042 → sale:ensure-items +8 (4 DUO+4 camp) → super admin ticks menu:camp + camp.* + group-series. @Tanya pre-staged. FYI owner: walk-in can't extend a group (course-only); early bird has no <1-month code rule (owner's list). Stage 3b (words/undo/QR/per-day revenue) waits on owner's §4.6 words.

## Porter (PM) — sid Camp batch migrate hit the TASK-085 ledger-skip AGAIN (6 rows 0036-0041 shadowed by 0042; schema witnesses 43 correct). Walked owner through the seed-ledger repair (3rd time this week). Flagged to Sober: this recurs on every multi-migration deploy — needs a permanent fix (migrationsTable/created_at ordering), sized for the list.

## Porter (PM) — owner: Tanya's access-file cred is SUPER ADMIN, full access on sid — self-serve users/roles/permissions, test freely (it's sid). No camp permission-tick needed. sale:ensure-items done (8 created: 4 DUO + 4 camp), restarted. @Tanya GO on REQ-095 Stage 2b + 3a. Recorded standing QA-access in QA.md.

## Tanya (QA) — ✅ **REQ-095 Stage 2b (DUO walk-in) + Stage 3a (Camp) — reachable checks ALL PASS; two 17:30-job parts BLOCKED.** 2b: DUO prices 4/6/10=6800/9360/14200 + walk-in 1900 (balance-duo, source); walk-in seat 201, 3rd ⇒ 409 GROUP_FULL "…เต็ม (2/2)"; no-group walk-in ⇒ 404 "ไม่พบกลุ่ม"; session-balance-duo ฿1900 ledger = 17:30 job BLOCKED. 3a Camp: week Mon-Fri cap2; sell FULL/FULL_WEEK totalUnits10/credit10 + early-bird ฿1000 discount; redeem 5 ⇒ credit0 (0 days left)/planned5; NO expiry field; mark Friday(future) CANCELLED ⇒ credit back 2 (1 day left); mark today(started) CANCELLED ⇒ 409 CAMP_DAY_STARTED "วันแคมป์เริ่มแล้ว — บันทึกขาดแทน", ABSENT ⇒ ok; HALF pkg FULL day spends 2 units; CAMP_FULL ⇒ 409 "วันที่ 2029-10-01 เต็ม (2/2)"; day banner = dayCounts DTO; today-planned⇒attended = 17:30 job BLOCKED. Footprint: DUO groups/walk-ins cancelled; camp weeks+packages left (isolated, no calendar/LINE/expiry); camp parent b228c487 SUSPENDED. Standing gap: QA can't run internal jobs / read outbox on sid (blocks 2b ฿1900, camp attend, TASK-396, reminder text) — a QA INTERNAL_JOB_SECRET or owner-run would close them. Report in inbox/PM.md.

## Porter (PM) — REQ-095 Stage 2b + 3a QA-clean on sid (all reachable checks; DUO prices, walk-in/GROUP_FULL, camp open/sell/redeem/cancel/CAMP_FULL/half-units/no-expiry). Blocked: the two 17:30-job effects (ledger post, planned→attended) — internal job unreachable on sid (code+unit pinned; owner/uat verifies). Proposed to owner: give QA a sid INTERNAL_JOB_SECRET (or owner runs end-of-day on sid once) to close the recurring job/outbox QA gap. Left: Stage 3b (needs owner §4.6 words); this batch's uat deploy (migration 0042). 

## Porter (PM) — owner: hold uat — finish ALL stages on sid first, then one uat deploy. So REQ-095 Stage 3b (reminder/LINE words, undo attended day, QR, per-day revenue — needs owner §4.6 words) is the last piece before the uat batch. Nothing to uat until 3b lands + QA. Awaiting owner's §4.6 words.

## Porter (PM) — owner: Stage 3b = items 1,2,3 (not 4). GO Sober: camp LINE reminder (parent+teacher, copy gated), undo attended camp day (returns credit, no money), camp QR. §9 recorded. Last REQ-095 piece; then the whole Other batch → uat single deploy.

## Porter (PM) — 🔴 REQ-096 (customer bug): 08:15 morning reminder includes PENDING bookings, should be CONFIRMED-only. Owner asked to sweep all notify paths for the same "wrong status sent" pattern. Asked Sober to READ+report which paths are affected before fixing; fold into the Stage 3b notification round if same code. Live-facing on uat.
- **@Sober** — Porter ×3: (a) REQ-096 read — the 08:15 reminder's `REMINDABLE` has carried PENDING since birth (not a decision); every other send is event-driven; recommend CONFIRMED-only incl. dropping EXTENDED; folded into (b) **TASK-403** (Camp 3b BE, contract first: undo, QR, reminder behind a settings flag, REQ-096) + **TASK-404** (FE, S); (c) ledger-skip hypothesis = hash drift from editing applied `.sql` headers; DATA REQUEST for the ledger dump; fix sized S+S (byte-freeze manifest + verify explains). Ball @Jason (TASK-403 contract).

## Jason (BE) — 📋 TASK-403 contract in the TASK: §1 verified (`REMINDABLE`'s two readers and no other status-list send; the session QR's columns are on `bookings` ⇒ `0043_camp_checkin_token` needed, 44 = 44; the undo = one transition clause + the existing delta; `marked_by` never read by `markDay`). §2 confirmed with four corrections: EXTENDED dropped with PENDING (REQ-094's ruling); the undo's reason as a nullable `undo_reason` column in 0043 (no audit-log table exists) with the floor at 0 and the same act key; the token issued LAZILY on the first QR view (not at redeem), expiry 23:59:59 of the date, `409 CAMP_DAY_NOT_TODAY` on a wrong-day scan, `410` expired, the SAME `markDay` consumes; the reminder flag as a settings key `camp_reminder_enabled` (off | on, default off) with `cp_*` placeholder labels. Four ❓ to Sober; waiting on confirm before code.
- **@Sober** — TASK-403 contract from @Jason CONFIRMED (four yes: `undo_reason` column; lazy token/23:59 expiry/`CAMP_DAY_NOT_TODAY`; 410 camp-only; no CRM points; `0043_camp_checkin_token` ⇒ 44). TASK-404 §0 rewritten to the confirmed shapes. Sent Porter the camp-reminder COPY DRAFT in the house format (teacher + parent blocks, TH label options) with two decisions (names under Kids; timing). Ball @Jason (build).

## Jason (BE) — ✅ TASK-403 DONE (code): REQ-096 `REMINDABLE = {CONFIRMED}` (PENDING + EXTENDED out; one constant, both audiences, the seats) · `0043_camp_checkin_token` (three NULL columns on `camp_days` + the partial unique token index = witness; 44 = 44; preflight clean alone and as the 0038 → 0043 batch) · the undo (ATTENDED | ABSENT → PLANNED with a required reason in `undo_reason`, `used` floored at 0, same act key, no money) · the camp QR (lazy token on the first view, 23:59:59 expiry, ONE `checkinUrl`, public `POST /checkin/camp` through the SAME `markDay`, idempotent, 410 expired / 409 wrong day, no CRM) · the camp-day reminder send path (pure `campReminderSends`, separate `camp_days` select, `cp_*` PLACEHOLDER labels, `camp_reminder_enabled` off | on default off ⇒ nothing enqueued). **2516 pass / 0 fail**, 195 files, tsc 0; 17 pins new, 15 files re-pointed 43 → 44, the kind census 18 → 19, the settings census; twenty-six mutations bite (byte-identical restores). Contract lines for @Fern final in the TASK. Awaiting @Sober's review. 🚫 No deploy request; the human runs `db:migrate` (verify 44); the flag waits for Porter's copy.
- **@Sober** — TASK-403 re-verified (2516/0, tsc 0, 44 = 44; `REMINDABLE = {CONFIRMED}`; the flag registered) ⇒ **✅ DONE — REVIEWED**. REQ-096 fixed in the same build. Fern released on TASK-404. Porter told (deploy ⇒ 44; the reminder path dark until the copy; Tanya's 3b checks). Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-404 done — `Undo` with a reason in the roster's Mark menu, the day's check-in QR through ONE shared `QrDialog` (built here — the FE had none), the camp token on the public check-in page (`/checkin/camp`), the `camp_reminder_enabled` row with its pending note.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  466 pass / 0 fail   (was 454; +12)
bun run build     → ok — `○ /checkin/camp`
```
12 modified · 4 new · + `qrcode.react@4.2.0`. Built to §0 + Jason's final lines (TASK-403). **Snapshot unchanged 54 / 13** —
the undo rides `camp.day-mark`. Notes in the TASK §2. 🚫 No deploy asked.
📌 §1 said "the same QR component the session uses" — **there was none on the FE** (the session's link is server-minted
over LINE; nothing drew a QR; no `qrcode` dependency). So the shared one was built: `components/common/QrDialog.tsx`,
walk-pinned as the only `qrcode.react` importer; it draws the URL it is given and composes nothing.
`Undo (back to planned)` after a divider in the SAME Mark menu, ATTENDED|ABSENT only ⇒ a dialog with a required reason
(presence only; 3..200 is the server's sentence, the text stays) ⇒ ONE `PATCH /camp/days/:id { status: PLANNED, reason }`
via the pure `markBody` (a reason never rides a mark); the entry shows `· undone: <reason>`. A QR icon on a PLANNED entry
only ⇒ lazy `GET /camp/days/:id/checkin` (enabled only while the dialog names a day). The public page: ONE
`CheckinContent` with `kind` from the ROUTE (`app/checkin/camp/page.tsx` ⇒ `POST /checkin/camp`), the day shape + the
undone line; `410 CAMP_TOKEN_EXPIRED` ⇒ the clock by code; the session's page untouched. Settings: the server's row
renders generically (nothing row-specific on the page, asserted); `opt.{off,on}` + the note through `settingHelp`, both
languages; a mock row with Jason's label. Copy `camp` 60→69 · `qr` 3 · `checkin` +5.
🔑 **Break-and-watch ×16 in `try/finally`** ⇒ 1–2 each, none slipped; md5 identical on 9 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-404 re-verified (466/0, tsc 0; one `qrcode.react` importer = the new shared `QrDialog`; `/checkin/camp`) ⇒ **✅ DONE — REVIEWED**. **REQ-095 complete both sides (1 → 3b) + REQ-096.** Told @Porter: `sid` batch ready (migrate ⇒ 44, ensure-items 8, grants; reminder flag off), Tanya's 3b checks, `uat` needs only 0042+0043 (no closed shop). Waiting: the copy + the ledger dump. Chain stopped; ball @Porter.

## Porter (PM) — Stage 3b copy: owner — label `Students` not `Kids` (customer may be teen/adult), teacher block Students+count+breakdown+names(≤12,+n), parent `Student :`; timing 08:15; Sober's QR/undo/no-points rulings stand. Jason drops bytes (XS) → deploy sid → Tanya 3b. REQ-095 then complete on sid; then the whole Other batch + REQ-096 → uat single deploy.

## Porter (PM) — Stage 3b copy: label `Students` not `Kids` (teen/adult), 08:15, Sober's rulings stand. Jason drops bytes → sid → Tanya 3b. Then whole Other batch + REQ-096 → uat single deploy.
- **@Sober** — Porter: the owner's camp-reminder copy (`Students` not `Kids`; names beneath ≤12 +n; parent `Student/Camp/Date/Time`; 08:15). Cut **TASK-405** (XS) to @Jason — placeholders → final bytes, pins by value, `Kids` pinned absent. Ball @Jason.

## Jason (BE) — ✅ TASK-405 DONE (code): the camp-day reminder's placeholders replaced by the owner's words (both languages; `Students` never `Kids`; teacher block Camp / Date / Students n (Full · AM · PM) / the children ≤12 then `+n`; parent block Student / Camp / Date / Time; under `⏱️TODAY'S SCHEDULE:`, blank-line blocks); the builder rows gained `date` + (teacher) `names` — a correction to the TASK's "already carries the children"; the date renders `DD-MM-YYYY` per REQ-087 (the TASK's sketch wrote a slash — flagged to Sober). **2518 pass / 0 fail**, tsc 0, 44 = 44; three pins moved FORM → VALUE, +2 tests; fourteen mutations bite. Flag stays off. Awaiting @Sober's review. 🚫 No deploy request.
- **@Sober** — TASK-405 re-verified (2518/0, tsc 0, 44 = 44; `Kids` only in the never-Kids comments) ⇒ **✅ DONE — REVIEWED**. Jason's `DD-MM-YYYY` deviation accepted — the owner's REQ-087 §7 rule, my sketch was wrong. **The `sid` batch is whole (REQ-095 1 → 3b + REQ-096)**; Porter told the deploy steps + the flag. Chain stopped; ball @Porter.

## Porter (PM) — TASK-405 in: camp reminder copy (Students not Kids, names ≤12+n, parent Student/Camp/Date/Time, 08:15, DD-MM-YYYY per §7). REQ-095 Stages 1→3b + REQ-096 whole on sid batch (BE 2518/0, FE 466/0, mig 0043 verify 44). Owner to deploy sid (migrate 0043, ensure-items, grant camp perms, flip camp-reminder setting to test). @Tanya pre-staged for 3b+096. Camp reminder setting off by default. Then whole Other batch + REQ-096 → uat single deploy.

## Porter (PM) — owner: sid deployed, verify 44 (0043 clean, no ledger drift this time). @Tanya GO on 3b + REQ-096.

## Tanya (QA) — ✅ **REQ-095 Stage 3b (undo+QR) PASS; REQ-096 + camp-reminder text NOT_TESTED (08:15 job/outbox).** 3b undo: mark ATTENDED, undo WITH reason ⇒ 200 credit back + undoReason on roster; WITHOUT reason ⇒ 400 (refine "การยกเลิกการบันทึกต้องระบุเหตุผล…" in details). 3b QR: GET /camp/days/:id/checkin ⇒ {token,url /checkin/camp?token=,expires 23:59:59}; POST /api/checkin/camp: today ⇒ already:false (attended), again ⇒ already:true, tomorrow ⇒ 409 CAMP_DAY_NOT_TODAY "…คือวันที่ 2026-09-20…" (names date); expired ⇒ 410. REQ-096 inclusion (PENDING absent/CONFIRMED present/EXTENDED absent) = reminder builder, BLOCKED (job 404+secret, outbox unreadable) ⇒ owner-on-uat. Camp reminder text (Students n (Full·AM·PM)+names, parent block, DD-MM-YYYY) BLOCKED same. Footprint: camp week QA-3b+package left (isolated). Standing gap: QA can't run internal jobs / read outbox on sid — a QA INTERNAL_JOB_SECRET or owner-run closes 2b ฿1900, camp attend, TASK-396, REQ-096, reminder text. Report in inbox/PM.md.

## Porter (PM) — REQ-095 Stage 3b PASS (undo+QR); REQ-095 ALL stages QA-clean on sid bar the internal-job/outbox items (REQ-096 inclusion, camp reminder text, 2b ledger, camp attend, 396) — code-verified, verified on uat where real recipients + owner job-runs exist. REQ-095 + REQ-096 COMPLETE on sid. Presented the held single uat deploy (migrate 0042+0043 verify 44, ensure-items +8, grant camp perms, flip camp-reminder setting) + recommended a QA sid INTERNAL_JOB_SECRET to close the recurring job/outbox test gap. Awaiting owner go for uat.

## Porter (PM) — owner will deploy uat TONIGHT. Confirmed 17:00/17:30 fix = TASK-396 (start-based), in tonight's build → live on uat; 17:00 cut at the 17:30 run from tomorrow. Two morning checks on uat tomorrow: REQ-096 (no PENDING in 08:15 reminder) + 396 (17:00 cut). uat deploy = migrate 0042+0043 (seed-ledger if drift) verify 44, ensure-items +8, grant camp/group-series perms, flip camp-reminder setting.

## Porter (PM) — uat 2nd deploy: migrate 0042+0043 clean (verify 44, no ledger drift this time); sale:ensure-items +8 (4 DUO + 4 camp) done. Remaining: restart BE+FE + grant camp/group-series perms + flip camp-reminder setting. Then uat live for all of REQ-095 + REQ-096.

## Porter (PM) — 🎉 uat 2nd deploy COMPLETE (restart done). ALL of REQ-095 (Other: ECA/Free · DUO/Group course+per-session · Camp) + REQ-096 (morning reminder CONFIRMED-only) + TASK-396 (start-based end-of-day) now LIVE on uat. uat and sid level at migration 44. Remaining owner ops: grant camp/group-series perms to staff roles as needed; create staff accounts; remove old ADMIN_*; flip camp-reminder setting when wanted. Tomorrow AM checks: REQ-096 (no PENDING in 08:15), 396 (17:00 cut at 17:30), camp/group/rental LINE text.

## Porter (PM) — 🔵 REQ-097 (teacher login, own calendar): customer wants teachers to log in, see/manage only their own calendar. Proposed extending option C with a user↔teacher link + a calendar:scope=all|own grant + a teacher self-manage action set. Asked owner A(view)/B(+check-in own)/C(+report own leave) — recommend C; create/cancel/move stays admin. Asked Sober feasibility (user↔teacher link, calendar filter by assigned teacher, scope bite points, sizes). Nothing dispatched.

## Porter (PM) — owner: REQ-097 = C (view own + check-in own + report own teacher-leave; admin keeps create/cancel/move). Told Sober to deliver the read + sizes + build plan (user↔teacher link, calendar:scope, scoped check-in + report-leave). Then I take sizes to owner and cut.
- **@Sober** — Porter: REQ-097 = option C, deliver read + plan. Wrote **SPEC-083**: no user↔teacher link exists (net-new `users.teacher_id`, `0044`); "own" derivable from `bookings.teacher_id` + `booking_teachers`; **scope = the link, not a grant** (fails closed); ONE `ownScope` predicate; `attend` only; report-own-leave = the teacher-cancel path with a new `TEACHER_LEAVE` code (none exists today) + the 55th key. Sizes C-1 BE M·FE M, C-2 BE S·FE S. Six owner decisions. No task cut. Ball @Porter.

## Porter (PM) — REQ-097 §7 owner "เอาตามแนะนำ": admin-created teacher account+picker; 2 accounts for admin-teacher; report-leave=cancel w/ new TEACHER_LEAVE reason; whole-day leave w/ per-session un-tick; swapped teacher sees group from swap date; no advance cutoff. GO Sober C-1 (link+view+check-in own) + C-2 (report own leave), migration 0044, 55th key, one sid deploy.
- **@Sober** — Porter: REQ-097 §7 all as recommended, GO C-1 + C-2. Cut **TASK-406** (BE M+S, contract first: `0044` link, `ownScope`, attend-only, `POST /teachers/me/leave` + `TEACHER_LEAVE`, key 55) and **TASK-407** (FE M+S). Board, inboxes, Porter told the deploy notes (role "Teacher", the picker). Ball @Jason (contract).

## Jason (BE) — 📋 TASK-406 contract in the TASK: §1 verified — 🔴 one premise false: NO family notice exists on a cancel (coach-only `class_cancelled_teacher`; `enqueueParentCopies` only on confirms; the leave renderers are dead kinds) ⇒ a NEW placeholder kind `class_cancelled_parent` on the leave path proposed; the reads = grid · tray · `getBookings` page + count · two by-id reads, ONE drizzle condition (OR-EXISTS) in both shapes; the action gate at the route, the row 404 in the service; the public QR before the auth guard; `END_REASONS` copied in the validator (to be re-pointed). Four corrections (my column only; the status body at the route; other teachers told on a leave; `REASON_ENUM_REQUIRED` unchanged) and five ❓ to Sober incl. size M + M. Waiting on confirm before code.
- **@Sober** — TASK-406 contract from @Jason CONFIRMED (M+M). His read overturned SPEC-083 §1.4: **no family notice exists on any cancel today** (coach-only) — the leave builds a NEW placeholder kind `class_cancelled_parent`; the admin's cancel stays coach-only pending the owner (SPEC-083 §5 + §3.7 added). Copy draft for the family notice sent to Porter with two decisions. TASK-407 §0 updated (my column only; the allowed read set = the page's load calls). Ball @Jason (build).

## Porter (PM) — 🔵 REQ-098 (archive PARENT): customer — duplicate leftover phone/parent after a family re-registers on a new number; student archive exists but parent only suspends (still listed). Proposed extending REQ-093's archive to parents (hide parent+students, history kept, restorable, no hard delete, archivable when no live sessions). Drafted customer reply. Asked owner to confirm; asked Sober a read (what suspend does, archive feasibility, cascade to students, scope). Not dispatched — queued behind REQ-097.

## Porter (PM) — 🔴 REQ-094 REOPENED (customer, uat): purple EXTENDED still not cut at 17:30. My read: TASK-389 only made bulk-confirm accept EXTENDED; the job is CONFIRMED-only, so an unconfirmed make-up isn't cut — but customer wants auto-cut with NO manual step. Escalated to Sober: (1) deploy timing of today's 17:30 run; (2) should the job attend EXTENDED directly now that it's start-based (reversing the earlier "no EXTENDED in job")? Live complaint; read before cutting.

## Jason (BE) — ✅ TASK-406 DONE (code): teacher login / own calendar — `0044_user_teacher_link` (`users.teacher_id` NULL FK RESTRICT + partial unique = witness; 45 = 45; preflight clean alone and as the 0038 → 0044 batch) · `lib/own-scope.ts` (the link is the scope; ONE OR-EXISTS predicate on the grid, tray, list page + count; my column; my camp weeks; by-id 404 at the route; the public QR unscoped) · `TEACHER_ALLOWED` in the guard, fails closed (eight routes from the calendar page; pinned with real tokens: all 55 keys ⇒ 200 on the eight, 403 on the other 95) · users page `teacherId` + `409 TEACHER_LINKED`, `/me.teacherId` · the leave `POST /teachers/me/leave` (key 55; `409 SESSION_DELIVERED` before the tx; `TEACHER_LEAVE` the 4th reason; the other teachers' coach notice; the FAMILY's NEW `class_cancelled_parent` placeholder kind with ONE producer — the admin's cancel stays coach-only). **2535 pass / 0 fail**, 196 files, tsc 0; 17 pins new, 16 files 44 → 45, the Stage-2 exclusion regex anchored; thirty mutations bite. Contract lines for @Fern final in the TASK. Awaiting @Sober's review. 🚫 No deploy request; `db:migrate` (verify 45) + the Teacher role/links are the human's.
- **@Sober** — TASK-406 re-verified (2535/0, tsc 0, 45 = 45; `TEACHER_ALLOWED` in the guard; `END_REASONS` four; validator reads the set) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-407 with the eight-route allowed set. Porter told (deploy ⇒ 45, the Teacher role; Tanya's checks; the open decisions). Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-407 done — the `Teacher` picker on Users, the scoped calendar/modal (`Check in` alone under `me.teacherId`), `Report leave` by the 55th key, `TEACHER_LEAVE` on the tray.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  478 pass / 0 fail   (was 466; +12)
bun run build     → ok
```
16 source modified · 10 tests re-pinned · 3 new. Built to §0 + Jason's final lines (TASK-406). **Snapshot 54 → 55**
(Jason's slot). Notes in the TASK §2. 🚫 No deploy asked.
**One gate, extended:** `can()` under `me.teacherId` passes only `calendar.status` + `teacher-leave` (a linked super
admin too) ⇒ every mutate door on every page hides through the gate that already exists — value-tested over the whole
snapshot. **🔴 The calendar page's load path = Jason's allowed set, walked:** the test lists the data hooks the page
mounts (`useBadges · useCalendar · usePausedBookings · useTeachers`) and each one's route; a fifth hook fails the walk;
`RentalSection` (`GET /sellable-packages`) is not mounted under the flag. The scoped calendar renders the columns the
payload came with (no id comparison of my own), drops the teacher/type pickers; the modal keeps `Check in` alone
(`canAttend`; the rest on `canStatus = canAttend && !scoped`, absent never disabled). `Report leave` beside `Today`,
linked holders only ⇒ date · my sessions from the scoped day (ATTENDED un-ticked) · reason by presence · the families
line ⇒ ONE `POST /teachers/me/leave` via pure `leaveBody` (`sessionIds` only for a subset); the toast reads the server's
`cancelled` / `familiesNotified`. Users: the `Teacher` picker on create + edit, `teacherId` by presence / by change,
null clears, `Teacher: name` under the display name. `TEACHER_LEAVE`: a READ set of four for the tray's label; the
dialogs keep the admin's three (asserted). Copy `teacherLeave` 10 · `users` +4 · `endCourse.TEACHER_LEAVE`.
📌 **Two assumptions for @Jason (via you):** `GET /me` is not in his eight — I assume it sits before the scope guard
(it is how the page learns the flag); the login body has no `teacherId`, so a linked account's doors show for ONE
request until `/me` lands (the server refuses anything pressed) — carrying it in the login body removes the flash.
🔑 **Break-and-watch ×18 in `try/finally`** ⇒ 1–2 each, none slipped; md5 identical on 11 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-407 re-verified (478/0, tsc 0) ⇒ **✅ DONE — REVIEWED**. Checked Fern's two assumptions in the BE: `/me` exempt ✓; login body lacks `teacherId`. Reading the guard found the early exit for `/users|roles` runs before the scope check ⇒ a linked super admin slips it — cut **TASK-408** (XS) to @Jason for both. Porter told (sid batch after 408: migrate ⇒ 45, items +8, the grants). Ball @Jason.

## Jason (BE) — ✅ TASK-408 DONE (code): the `/users*` + `/roles*` scope refusal moved BEFORE the guard's early exit (a linked super admin no longer reaches user/role admin; `/auth`, `/me`, `/permissions` exempt) — pinned with real tokens; the login body's `teacherId` was ALREADY there by construction (`userDTO` since TASK-406) — pinned by value, nothing re-added. **2537 pass / 0 fail**, tsc 0, 45 = 45; six mutations bite. Awaiting @Sober's review. 🚫 No deploy request.
- **@Sober** — TASK-408 re-verified (2537/0, tsc 0; the scope line before the early exit; login DTO carries `teacherId` already — my task's premise 2 was wrong, Jason pinned instead) ⇒ **✅ DONE — REVIEWED**. **The `sid` batch is whole: REQ-095 + 096 + 097.** Porter told the deploy steps + grants + the Teacher role. Chain stopped; ball @Porter.

## Porter (PM) — REQ-097 (teacher login) COMPLETE both sides (BE 2537/0, FE 478/0, mig 0044→45). Sid batch whole: REQ-095(1-3b)+096+097. Answered owner: Sober did NOT yet address the mid-build items — REQ-094 reopen (purple/EXTENDED-in-job) and REQ-098 (archive parent) are queued; nudged Sober to read both next. REQ-097 open owner decisions: family-notice copy (gated, Sober to draft), §3.7 (do shop cancels notify families?), reason-line (show ครูลา, hide shop codes — Sober rec). Awaiting Sober's REQ-094/098 reads + the copy draft.

## Porter (PM) — REQ-094 reopen RESOLVED by owner screenshots (screen beats code, again): the EXTENDED booking modal has NO Confirm button (only Attended); PENDING has Confirm+LINE. So "just confirm it" was impossible. Owner ruling: EXTENDED confirms like PENDING → FE adds Confirm+LINE to the EXTENDED modal (BE single-confirm already accepts EXTENDED); day-end stays CONFIRMED-only (do NOT make the job attend EXTENDED). §4 recorded. Dispatched the FE fix to Sober; cancelled the job-change line. My earlier "just confirm the purple" reply corrected.
- **@Sober** — Porter ×3: (1) REQ-094 reopen — verified the BE confirm accepts EXTENDED (only `confirmedAt` checked); the bug is `BookingModal.tsx:717` PENDING-only ⇒ **TASK-409** (FE XS) cut; the job untouched. (2) REQ-098 read ⇒ **SPEC-084** (archive a parent: REQ-093's shape, ~8 bite points, clear the LINE id on archive, cascade to students; BE S+ · FE S; 5 decisions). (3) Re-sent the family-notice copy draft + the two decisions. Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-409 done — `Confirm + LINE` for EXTENDED as for PENDING: `CONFIRMABLE_STATUSES = [PENDING, EXTENDED]` beside `MOVABLE_STATUSES`, read by the pure `canOfferConfirm` at the one door; value-tested both ways.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  480 pass / 0 fail   (was 478; +2)
bun run build     → ok
```
One source file for the fix (`BookingModal.tsx`), same handler and notice, no second `===`, the Attended door untouched
(asserted). **Also, as asked (TASK-408 follow-up, not a task): the login body's `teacherId` now rides auth → token →
session → the `/me` seed, so a linked account's doors never flash before `/me`** (auth.ts · auth.config.ts ·
next-auth.d.ts · useMe.ts · the login type; pinned). 🔑 ×6 mutations in `try/finally` ⇒ 1–3 each, none slipped, md5
identical. Notes in the TASK §2. 🚫 No deploy asked.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-409 re-verified (480/0, tsc 0; `CONFIRMABLE_STATUSES` + `canOfferConfirm` at the door) ⇒ **✅ DONE — REVIEWED**. The `sid` batch whole again (+ the REQ-094 fix, FE-only — deployable ahead if the owner wants). Porter told. Chain stopped; ball @Porter.

## Porter (PM) — Sober delivered all 3: REQ-094 fix TASK-409 IN (FE Confirm+LINE on EXTENDED, FE-only no migration — offerable to uat alone); REQ-098 SPEC-084 read (archive parent, BE S+/FE S mig 0045, rec: keep phone, clear LINE id on archive, cascade students, coexist w/ suspend); REQ-097 family-notice copy drafted (gated). Consolidated to owner: A) ship REQ-094 to uat alone now? B) approve copy + §3.7 (shop cancels notify families, rec yes) + reason-line (teacher-leave only); C) cut REQ-098 + rec edges. sid batch (095+096+097+094) built, awaiting deploy+QA.

## Porter (PM) — A/B/C: A=owner deploys REQ-094 FE-only to uat now (customer's purple Confirm). B=REQ-097 copy approved + §3.7 YES (shop cancels notify families) + reason-line teacher-leave-only → Jason bytes (XS). C=REQ-098 archive parent GO as SPEC-084 recommended (keep phone, clear LINE id, cascade students, coexist suspend, mig 0045, key 56). B+C build on sid → join batch. §8 (097) + §6 (098) recorded.

## Porter (PM) — REQ-094 (purple Confirm button, TASK-409) LIVE on uat via FE-only deploy (owner ran no migrate; uat stays at 44). Customer's "purple not cut" fixed — they now have Confirm+LINE on EXTENDED. REQ-097 (teacher login) + B (teacher-leave copy bytes) + C (REQ-098 archive parent) are a SEPARATE later batch — B/C still building at Sober, not yet QA'd; they ship to uat with migrate (45→46) once done + tested. sid: FE also updated; sid BE/migration state for REQ-097 to confirm at that batch's deploy.

## Porter (PM) — both boxes now at verify 45 (sid via seed-ledger+migrate; uat 0044 already applied, 0 pending). REQ-094 (purple Confirm) LIVE sid+uat — customer fixed. REQ-097 (teacher login) CODE + 0044 also live on uat (came with the HEAD build) but DORMANT — no Teacher role, no linked teachers. NOT yet QA'd by Tanya. B (teacher-leave copy bytes) + C (REQ-098 archive parent) still building at Sober/Jason — not in this build. Before ACTIVATING teacher login on uat: (1) Tanya QA REQ-097 on sid, (2) B bytes land. Proposed to owner: release Tanya on REQ-097 sid now; activate uat teacher login later.

## Porter (PM) — owner: เทสเลย. Released Tanya on REQ-097 (teacher login) + REQ-094 fix on sid (she self-serves the Teacher role + linked account via super-admin cred). Family-notice text is placeholder (B pending) — flow only.

## Tanya (QA) — 🔴 **REQ-097 NOT_TESTED (deploy-lag: sid BE stale, pre-REQ-097); REQ-094 fix PASS.** Deployed sid BE = 54 action keys, NO action:calendar.teacher-leave (role create 400); createUser {teacherId} does NOT persist (2nd same-teacher account 201 not TEACHER_LINKED; linked super admin opens /users 200 not SCOPE_TEACHER); POST /api/teachers/me/leave 404 even as admin. Local repo = 55 keys + teacher-leave + leave route + createUser.teacherId (commit b72f88b TASK-406). ⇒ stale BE zip (FE may be current), same fingerprint as Round 23/24 expiry. Owner must redeploy current BE on sid (55 keys, 0044) then I run all 5 REQ-097 checks. REQ-094: single confirm on EXTENDED make-up ⇒ 200 CONFIRMED (purple Confirm works). Footprint: test users left disabled (44b1d795/3c9a83b5/ee01ed71/e551f4fb); no role (400); bookings cancelled. Report in inbox/PM.md.

## Porter (PM) — REQ-094 fix QA PASS on sid (purple Confirm). 🔴 REQ-097 BLOCKED — sid BE is a STALE zip (pre-REQ-097): /api/permissions = 54 keys not 55 (missing action:calendar.teacher-leave), /teachers/me/leave 404, createUser doesn't persist teacherId. Migration 0044 applied but the BE binary is old (stale-zip, same as Round 23/24). Told owner to rebuild current BE (55 keys) → deploy sid → restart (verify /api/permissions = 55), then Tanya runs the 5 REQ-097 checks. Flagged uat BE may also be stale but REQ-097 is dormant there (no Teacher role/links) so no impact until activation; REQ-094 (FE) on uat unaffected — customer fix stands.

## Porter (PM) — owner: BE redeployed sid. Released Tanya on REQ-097 (step 0 = verify /api/permissions = 55, then the 5 checks).

## Tanya (QA) — 🔴 REQ-097 check 2 FAILS: `POST /api/teachers/me/leave` ⇒ 500 INTERNAL (rolls back; sessions stay CONFIRMED, no make-ups). Reproduced 3× fresh-token; BOTH single-session AND course. STEP 0 ✅ (55 keys — deploy-lag gone). Checks 1·3·4·5 PASS, REQ-094 PASS. Isolated: pre-check (SESSION_DELIVERED) fires fine before tx; admin-cancel of CONFIRMED = 200 (outbox healthy) ⇒ throw is in reportOwnLeave's NEW notice path (sendClassCancelledToOtherTeachers / class_cancelled_parent enqueue, commit b72f88b, scheduler.service.ts:2935). Real runtime bug, not deploy-lag/test-data — @Sober/@Jason to read the server error (logs blocked to me). Footprint: 0 live bookings, roles deleted, user 609072d7 disabled. Flag: a DISABLED linked account keeps its teacher link → Lewis/Kowjoe/Haris consumed; asked to free a teacher before next round. Report in inbox/PM.md.

## Porter (PM) — REQ-097 QA: 4/5 PASS (own-column view, attend-only, dup-account 409, linked-super-admin 403, out-of-scope 404) + REQ-094 re-pass. 🔴 check 2 report-own-leave = 500 rollback — real bug in reportOwnLeave tx (scheduler.service.ts:2935, new b72f88b code: sendClassCancelledToOtherTeachers / class_cancelled_parent enqueue); both booking types; pre-check + admin-cancel fine. Escalated to Sober/Jason (read server log). Also: disabled linked account keeps teacher_id (can't relink) — asked owner if intended + asked to free 2-3 teachers for QA. REQ-094 stays live on uat.
- **@Sober** — Porter: A/B/C GO + the teacher-leave 500 + the disabled-link question. **The 500 read: `0025`'s CHECK on `cancel_reason` (three codes) rejects `TEACHER_LEAVE` ⇒ 23514 — a third copy of the closed set**; cut **TASK-410** (S: `0045` redefines the CHECK + the CHECK ⇔ `END_REASONS` pin + the family-notice final copy + §3.7 YES). SYSTEM-FACTS rule written. **TASK-411/412** (REQ-098) cut contract-first behind it. Disabled accounts keeping the link: recommended as intended; DATA REQUEST for QA's unlink. Ball @Jason (410).

## Jason (BE) — ✅ TASK-410 DONE (code): the teacher-leave 500 closed — `0045_cancel_reason_teacher_leave` redefines `bookings_cancel_reason_chk` with `TEACHER_LEAVE` (DROP → ADD NOT VALID → VALIDATE; witness = the constraint's definition, a new `constraint-def` probe); the CHECK's list pinned ⇔ `END_REASONS`; the family notice in the owner's words by value (TH/EN; Reason only for a teacher's leave; Note by shape); §3.7 — the admin's cancel now tells the family through the ONE sender the leave uses (CONFIRMED-gated, per seat). **2540 pass / 0 fail**, tsc 0, 46 = 46, preflight clean; 17 files 45 → 46; seventeen mutations bite. Awaiting @Sober's review. 🚫 No deploy request. ▶️ TASK-411 contract next.

## Jason (BE) — 📋 TASK-411 contract in the TASK: §1 verified — 🔴 Finding A: the ghost stays resolvable through `family_line_links` unless the archive uses the existing `clearFamilyLine` (proposed, with `archived_line_user_ids text[]` as the audit); 🔴 Finding B: `parents_phone_uq` — keeping the phone means an admin create ⇒ `409 PARENT_ARCHIVED` and the LINE register needs a `phone-archived` outcome (placeholder reply) or it 500s. 16 parent-resolving sites listed for the ONE `activeParentWhere`; REQ-093's count/write lifted for the cascade; suspend needs no extra term. §2 with corrections + five ❓ to Sober. Waiting on confirm before code.
- **@Sober** — TASK-410 re-verified (2540/0, tsc 0, 46 = 46; `NOT VALID`+`VALIDATE`; the CHECK ⇔ `END_REASONS` pin; producers 2) ⇒ **✅ DONE — REVIEWED**. TASK-411 contract CONFIRMED (Finding A: `family_line_links` first ⇒ `clearFamilyLine` in the tx; Finding B: unique phone ⇒ `PARENT_ARCHIVED` + a `phone-archived` LINE outcome with placeholder copy). TASK-412 §0 updated. Porter told (migrate ⇒ 46 for check 2; the placeholder copy). Ball @Jason (411 build).

## Jason (BE) — ✅ TASK-411 DONE (code): archive a parent — `0046_parent_archive` (three NULL columns incl. `archived_line_user_ids text[]` + the partial index = witness; 47 = 47; preflight clean) · `lib/parent-archive.ts`'s ONE `activeParentWhere` at every working read (list/search/count share one where, the phone lookup, the notice recipients, line-lang, the nag, the SOM) + `assertParentActive` at every by-id write · the archive (household count ⇒ 409 before the tx; ONE tx: `clearFamilyLine` → the columns + the audit list → the cascade `parent:<id>`; idempotent) and the restore (only the cascaded children; LINE not restored) · Finding B: `409 PARENT_ARCHIVED` on the admin's create, `phone-archived` on both LINE register doors (placeholder reply), never a silent restore · REQ-093's count/write lifted and shared. **2554 pass / 0 fail**, tsc 0; 14 pins new, 17 files 46 → 47; twenty-seven mutations bite. Contract lines for @Fern final in the TASK. Awaiting @Sober's review. 🚫 No deploy request.
- **@Sober** — TASK-411 re-verified (2554/0, tsc 0, 47 = 47; the predicate at every site; `clearFamilyLine` in the tx; `phone-archived` before any insert) ⇒ **✅ DONE — REVIEWED**. Fern released on TASK-412. Porter told (Tanya's 098 checks; deploy ⇒ 47 + grants). Ball @Fern.

## [--:--] Fern (FE) — ✅ **TASK-412 done — Archive / Restore a FAMILY on the family card (the 56th key), the archived families under the SAME `Show archived` toggle, the badge.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  485 pass / 0 fail   (was 480; +5)
bun run build     → ok
```
7 source modified · 7 tests re-pinned · 2 new. Built to §0 + Jason's final lines (TASK-411). **Snapshot 55 → 56**, the key
after `student-archive` (the BE's slot, asserted by position). Notes in the TASK §2. 🚫 No deploy asked.
The family card (there is no separate parent page — the card is the detail): a red Archive icon by the 56th key ⇒ a
confirm that says every student goes with the parent and the LINE link is cleared ⇒ ONE `POST /parents/:id/archive`;
the toast names the server's `archivedStudents` + `clearedLineAccounts`; a 409 is the sentence in the dialog. ONE toggle,
two lists: `Show archived` now also fetches `GET /parents?archived=1` (only while on, the same search — pure
`archivedParentsQuery`) ⇒ dimmed cards, the badge, the children's names, `Restore` (same key) ⇒ a confirm that says the
LINE link is NOT restored ⇒ ONE `POST /parents/:id/unarchive`; the toast names `restoredStudents`. Nothing on the page
reads `archivedAt` (TASK-393's walk pin holds). Copy `people` +11 ×2.
🔑 **Break-and-watch ×12 in `try/finally`** ⇒ 1–2 each, none slipped; md5 identical on 6 files.

**Ball: @Sober.** TASK-294, TASK-286 remain, no clock.
- **@Sober** — TASK-412 re-verified (485/0, tsc 0; 56th key in the snapshot) ⇒ **✅ DONE — REVIEWED**. **REQ-098 complete both sides; the `sid` batch is whole** (095 · 096 · 097 · 098 · the 094 fix). Porter told the deploy steps (migrate ⇒ 47, items +8, grants + Teacher role) and Tanya's round; `uat` needs 0042 → 0046 later. Chain stopped; ball @Porter.

## Porter (PM) — Sober done: 500 fix (TASK-410, reportOwnLeave — the new CHECK/kind), family notice + §3.7 admin-cancel notifies family; REQ-098 archive parent (TASK-411/412, mig 0046, verify 47) with ghost-unlink-in-tx + phone-unique handling (bot phone-archived + admin PARENT_ARCHIVED-restore). sid batch WHOLE: 095+096+097(+410)+098+094. Deploy sid: migrate 0042→0046 verify 47, ensure-items +8, grants (camp, group-series, parent-archive, Teacher role) + links. Asked owner: approve phone-archived bot reply (placeholder); free 2-3 teachers for QA + rule on disable-keeps-link. Then Tanya full round (097 check-2 re-run + 098) → uat single deploy.
