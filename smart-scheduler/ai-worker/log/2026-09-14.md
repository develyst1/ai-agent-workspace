# 2026-09-14

## Porter (PM) — 🔴 **`REQ-088 §10`: three owner changes** (title `SOM SCHEDULE` · EN address options · already-linked warning + UNLINK). Recorded verbatim in the REQ, dispatched to @Sober. 📌 **Fact from his screenshot: the LIFF page is LIVE on `uat` (`frontoffice.develyst.online`)** — stop saying "waiting for the customer's LIFF"; the link works there today.

## 2026-09-14 — Sober (SA Lead)

### 🟢 `REQ-088` closed on `sid` (09-13): the owner's phone test passed — province `กาญจนบุรี`, note `รางหวาย พนมทวน กาญจนบุรี`; @Tanya's `§9.1` screen check passed.
✅ **The 500 cap: @Porter's call, RAISE — *"a cap sized for a human typing is being hit by a mechanism we added;
the owner should not be asked to bless a consequence of our own design."*** **The number is mine: 2000** — the
column is unbounded `text`, but the note is read by a human in a textarea; ~25 lines, room for ten appended
addresses plus a staff note, still a guard.

### 🔴 `REQ-088 §10` — three changes from the owner's phone; the page is LIVE on `uat` → TASK-354 (BE) + TASK-355 (FE), one deploy
**Both confirmations @Porter asked for, from the code:** `§10.2` costs nothing — the dataset already carries
`nameEn`, loaded with the Thai (TASK-349's bytes) ⇒ a display switch on the geocode-keyed row; **STORED stays
Thai — `province` the admin list's spelling (the `§9.1` repair path depends on it), `note` the Thai line.**
🔻 **Reverses TASK-351's "values stay Thai" for DISPLAY only; the `nameEn` assertions get rewritten with the
reversal inside, the join and the send kept Thai.** `§10.3`'s unlink IS the admin's `Clear LINE link` —
`clearFamilyLine`, one writer, one more door.
🔑 **`§10.3` IS ITEM 12.** *"เชื่อมรหัสไปแล้วครั้งนึง แต่กดลิ้งอีกแล้วพอกดเบอร์อื่น … เงียบไปเฉยๆเลย"* — **the
orphan-parent gap @Jason found on TASK-347, mirrored from the chat per Rule 1, listed for two days as "found by
looking" — the owner found it by USING the page.** ✅ **His fix is better than the refusal on the list: TELL
them, offer UNLINK.** ⚠️ **And the task ALSO closes the silent path in the writer for both doors: a bound
account + a NEW phone is refused at `link`.** Contract: `status` (masked phone on the server, no names —
TASK-047) + `unlink` (actor stated). **Item 12 comes off when this lands.**
`§10.1` `SOM SCHEDULE` is XS; I asked @Porter whether `/checkin` should follow, and told @Fern to name it, not
change it.
🚫 **Nothing else cut. The chain held.**

⏱️ **State: @Jason on TASK-354 (contract first) · @Fern on TASK-355 (`§10.1`+`§10.2` now) · one deploy, he
tests once · the list at 16 with `/checkin`'s English default the only live one.**

## 2026-09-14 — Jason (BE)

### ✅ TASK-354 — `REQ-088 §10.3`: a linked account is TOLD, can UNLINK through the one writer; item 12 closed; the cap raised — **DONE (code)**
**2114 pass / 0 fail**, 171 files (+1) · tsc 0 · 🚫 **no migration (35 = 35)** · codes only · contract sent first · no pin moved · `§17c` zero touched.
🔴 **ITEM 12 — the orphan-parent gap TASK-347 mirrored and named, and the owner then found on his phone — closed in the ONE writer for both doors:** a bound account entering a phone that is not its family's is refused BEFORE any row exists, even for a new phone. ⚠️ **The chat changed with it: `verify_parent_ok_new` + an orphan row → `verify_parent_other_family`, the reply that already meant this. No new key.**
🔑 **`status` masks on the SERVER (`08x-xxx-xxxx`, last four asserted hidden, non-standard masked entirely) and hands the page no ids and no names.** 🔑 **`unlink` is the admin's own `clearParentLineLink` with actor `line:<sub>` — no second unlink path, by absence — and the consequence is written beside the writer: it clears the FAMILY's binding, every account.** ✅ The parent's note cap → 2000, the other six `max(500)`s asserted untouched. **Six mutations bite.**
🎯 **THE QUESTION — *found by USE vs never found?* Two KINDS, not two priorities:** 🟢 **found by use** (the owner is the only test): item 12 ✅ · the `max(500)` cap ✅ (closed before he met it) · the voucher `Program` line with no size · the picker's third date format. ⚪ **never found by use** (properties of the suite/types/tooling): the suspended-household pin · 18 empty regions · the two output checks · the branded date · `mustAddChild` · the restore helper · `6 HR` counting sessions · the note race. 📌 ***Green first — each one he finds costs a deploy and trust. But "never found by use" means "found by a PARENT in production as the first symptom of something else" — the worst finder. The green class is where the OWNER is our QA; the white class is where the SUITE is, and the suite is cheaper than him.***

### ✅ TASK-354 DONE and REVIEWED — @Jason. **2114/0 (171 files), 35 = 35, tsc 0, re-run by me.** 📜 **Contract to @Fern first, with the family-wide consequence in front.**
**Verified:** two `max(2000)`, six `max(500)` · `unlink` through `clearParentLineLink` — the admin's own service —
with no direct `clearFamilyLine(` and no `lineUserId: null` in the home · the mask shows two digits and hides a
non-standard phone entirely.

🔴 **ITEM 12 CLOSED in the one writer, both doors:** `familyOfLineUser` checked BEFORE any parent row exists ⇒
no orphan can be created. ✅ **The chat's change NAMED rather than discovered:** a linked parent typing `สมัคร`
with another family's phone now gets `verify_parent_other_family` (SPEC-071's own reply) where it used to get
`ok_new` plus an orphan — *"the chat gains a refusal it never had, and I have not read a way for that to be
wrong"; nor have I.* ✅ **The NAMED-NOT-FIXED note from TASK-347 asserted GONE — the right way to close a list
item.**

🔴 **THE SENTENCE THAT MATTERS, and he refused to narrow it:** ***unlink clears the FAMILY's binding — every
account the family holds, not only the one that tapped; that is what the admin's button does and what "the
same writer" means.*** ✅ **To @Fern as the meaning her copy must carry ("this family's LINE connection"); to
@Porter as a DECISION for the owner BEFORE he taps it — family-wide as built, or a per-account unlink as a
second writer and another task.** *A household with two linked parents is exactly the case he will not think
of on a phone with one.*
✅ **`status` masked in the HOME, not the route — the raw phone never reaches it; a non-standard phone masked
entirely: a leak by exception is still a leak.**

### 🎯 His §6 is the priority FRAME for the list, sent up as such
**Two KINDS of item: 🟢 FOUND BY USE (the owner is our QA; each costs a report and a deploy) vs ⚪ NEVER FOUND BY
USE (the suite's, the types', the tooling's).** ***"The green class is where the OWNER is our QA; the white class
is where the SUITE is — and the suite is cheaper than him."*** ⚠️ **And his correction of my framing: "never
found by use" means found by a PARENT in production as the first symptom of something else — the worst finder
there is.** 📌 **Green first (the voucher `Program` line; the picker's third format); white sized by what the
suite would catch that he cannot.**

⏱️ **State: @Fern on TASK-355 against the live route · @Jason EMPTY · the family-wide-unlink decision with the
owner · one deploy when her half lands.**

## [--:--] Fern (FE) — ✅ **TASK-355 done — `SOM SCHEDULE` · English options in EN (display only) · a linked account is TOLD, with a two-tap family-wide UNLINK — against the live route.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  327 pass / 0 fail   (was 321; +6)
bun run build     → ok — the built /register HTML reads <title>SOM SCHEDULE</title>; /checkin still Smart Scheduler
```
🚫 No migration · nothing stored changes · the join, the send, the `§9.1` repair path, `liff.ts`, the toggle rule
untouched · `api.ts` +2 calls, no rule. Six files. Notes in the TASK §11.

**§10.1** — page-level `metadata`, the root's name untouched. 📌 `/checkin` SHOULD follow (same audience, same
reason: the header shows the document title) — named, not changed.
**§10.2** — `AreaPick.nameEn` rides the row (already loaded, no new bytes); `asOptions` label = `lang === "th" ?
nameTh : nameEn`, value = the geocode. **`pickedProvince`, the join and the confirm line read `nameTh`** — asserted;
`nameEn` occurs on the page exactly once. 🔴 TASK-351's `nameEn` assertions REWRITTEN with the owner's sentence and
the reason inside, join + send + confirm kept; the `lang`-count pin 3 → 4, the fourth named. Mutation 2 (the join
following the language) fails four tests.
**§10.3** — on open, after the token: `status` ⇒ `linked` ⇒ the screen with the SERVER's masked phone + count (no
names, no `ChildList`); else the phone field. The page branches on `st.linked` only (Rule 1) and never masks
(asserted by absence; mutation 3). **UNLINK is two taps:** plain close path above · red OUTLINE first tap · red
`Alert` with the family-wide warning + red filled *"Yes, unlink the family"* + *"Keep the link"* (mutation 4 =
one-tap fails). Unlink ⇒ `withToken(unlink)` ⇒ the phone field with a notice, `unlinked` true or false alike.
🔴 **The writer's consequence is in the copy and pinned:** *"Unlink this family's LINE connection"* /
*"ปลดการเชื่อมต่อ LINE ของครอบครัวนี้"*; the warning names the WHOLE family and the other parent (mutation 5 —
"Unlink my phone" — fails). 📝 7 keys × 2, all PLACEHOLDER, tagged.
🔑 **Break-and-watch ×5** ⇒ 1/4/1/1/1 fails; md5 identical after.
⚠️ Not seen: the two-step inside LIFF; and the dataset's English spellings are its romanisations (`Khnong Tan
Enue`) — if they look wrong on the owner's screen, that is the dataset, not a mapping; a fix-up would be a task.

### Q — a rule that predicts the switch? **Yours holds on every rendered string on the page, 0 exceptions, and
it flags TASK-351's anomaly (the romanised example in a label).** *Choose-between follows `lang`; identifies stays
as stored.* But "values stay Thai" also sounded like a rule and was wrong twice — so: **the rule is the default,
the screen is the test, in that order.** Edge worth writing down: when a choice's label IS its stored value (a
province), the screen shows the language and stores the Thai — two forms on purpose, which `§9` + `§10.2` now do.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.

### ✅ TASK-355 DONE and REVIEWED — @Fern. **327/0, tsc 0, build ok, `<title>SOM SCHEDULE</title>` in the built HTML, re-run by me.** 🟢 **`§10` COMPLETE, both halves. One deploy; he tests once. One message with @Porter.**
✅ **`§10.2` DISPLAY only, proven from four sides:** `nameEn` on the page exactly once (the label); the pick is
the geocode; `pickedProvince`, the join, the confirm line all `nameTh`; **the JOIN following the language fails
FOUR tests.** 🔴 **TASK-351's assertion REWRITTEN with the reversal inside — the second reversal in three days,
handled the same way; the `lang`-count pin 3 → 4, the fourth named.**
✅ **`§10.3` — the destructive act looks like one:** TWO taps, red, the close path above; the copy carries the
writer's family-wide meaning; the page never masks or holds a full number; no names; it does not branch on
`unlinked` true/false. **Mutations "one tap" and "Unlink my phone" both fail.**
⚠️ **Her caveat went up BEFORE the owner tests: the dataset's English names are romanisations and some are rough
(`Khnong Tan Enue`) — the dataset, not a mapping error; a fix-up table is a task.** 🔑 *Saying it first is what
stops it arriving as a bug.* ❓ **`/checkin`'s title — her reading is it should follow; named for the owner.**

### 🎯 Her Question — the language rule, and the caveat that keeps it honest
***"What a parent must CHOOSE between follows the language; what merely IDENTIFIES stays as stored"*** —
**0 exceptions on the page, and it would have predicted both reversals and TASK-351's anomaly.** ✅ **Recorded
WITH her second sentence: *the rule is the default and the screen is the test, in that order* — because "values
stay Thai" also sounded like a rule and was wrong twice.**

⏱️ **State: `REQ-088` `§10` complete · the family-wide-unlink decision still with the owner (told to him before
he tests) · both engineers EMPTY · the chain held through the whole of `REQ-088`.**

## Porter (PM) — 🟢 `§10` complete (BE 2114/0 · FE 327/0 · 35=35 · note cap 500→2000). Decided `/checkin` also gets `SOM SCHEDULE` (XS, lands before the deploy). **Open with the owner: family-wide vs per-account unlink** — recommend family-wide as built. Pre-warned: English address names are the dataset's romanisations (some rough — a fix-up table would be a task).

## Porter (PM) — 🟢 `§10` complete (BE 2114/0 · FE 327/0 · 35=35 · note cap 500→2000). Decided `/checkin` also gets `SOM SCHEDULE` (XS, lands before the deploy). **Open with the owner: family-wide vs per-account unlink** — recommend family-wide as built. Pre-warned: English address names are the dataset's romanisations (some rough — a fix-up table would be a task).

## Porter (PM) — owner: family-wide unlink accepted. Waiting on Sober's `/checkin` title, then deploy `sid`.

## Sober — TASK-356 cut: `/checkin`'s title is `SOM SCHEDULE` too
- @Porter decided (owner's reason, the LIFF header): `REQ-088 §10.1` now covers BOTH LINE-opened pages. **TASK-356 → @Fern, XS**: page-level `metadata` on `src/app/checkin/page.tsx`, root layout's `Smart Scheduler` untouched, asserted from the build like TASK-355.
- Owner accepted FAMILY-WIDE unlink as built ("ได้ละ") — no per-account task; recorded.
- Deploy is held by @Porter until I say the `/checkin` title is in. Chain stopped; nothing else cut.

## [--:--] Fern (FE) — ✅ **TASK-356 done — `/checkin` reads `SOM SCHEDULE`; proven from the build; two files, nothing else.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  327 pass / 0 fail   (the TASK-355 title test re-cut, not added to)
bun run build     → ok — built HTML: /register + /checkin `<title>SOM SCHEDULE</title>`; the other 14 routes `Smart Scheduler`
```
One line on `src/app/checkin/page.tsx` (page-level `metadata`, reason beside it); the test now walks every `page.tsx`
under `src/app` and asserts exactly these two set a title. 🔑 Break-and-watch ×2 (title back · a third page titled)
⇒ 1/1 fails; md5 identical. 🚫 Nothing else in the diff. Notes in the TASK §3.

### Q — what else says `Smart Scheduler` to a parent inside LINE: 🔻 **the `<meta name="description">` — the root's
English tagline, inherited by BOTH parent pages, and there are NO `og:` tags — so a LINE link preview shows `SOM
SCHEDULE` over *"Tutoring schedule & attendance management system"*.** One line per page, the customer's words —
a task. 📌 The LIFF app's own name/description live in the customer's LINE Developers console — the owner's to
check. ✅ No manifest/home-screen name · the chat never says it · ICS `PRODID` is teachers' · `/login` and the admin
header are admin-only. 🚫 Named, nothing built.

**Ball: @Sober** — deploy. `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- **TASK-356 DONE, reviewed** (327/0, tsc 0, 2-file diff verified by me). Told @Porter the `/checkin` title is in → deploy released to him. @Fern's `<meta description>` observation → owner's list via Porter, not a task.

## Porter (PM) — 🟢 **`REQ-088 §10` + note cap READY for `sid`:** FE TASK-355+356 (327/0) · BE TASK-354 (2114/0) · 35=35, no migration. `/checkin` title in. **Released the deploy to the owner** (back + front). List item, not a task: `<meta description>` still an English tagline under the LINE link preview — one line per page if he wants it in his words.

## Porter (PM) — owner: **`sid` deployed (back + front) with `§10` + cap.** Owner tests once on the phone (LINE is out of QA scope). Nothing dispatched.

## Porter (PM) — ✅ **`REQ-088 §10` OWNER PASS on `sid`** (4 screenshots: title, already-linked screen, EN lists, Thai confirm line, child added). 🔴 Two XS copy defects sent to @Sober: Unlink button label truncated on the phone (`…LINE conn`), `1 children`. Not blocking.

## Porter (PM) — ✅ **`REQ-088 §10` OWNER PASS on `sid`** (4 screenshots: title, already-linked screen, EN lists, Thai confirm line, child added). 🔴 Two XS copy defects sent to @Sober: Unlink button label truncated on the phone (`…LINE conn`), `1 children`. Not blocking.
