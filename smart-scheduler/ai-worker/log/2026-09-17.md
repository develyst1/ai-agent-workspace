# 2026-09-17

## Sober (SA) — REQ-090 mechanism answered
- Program `name` is not editable in the UI; no subjects route exists; seed is the only writer (truncates). ⇒ owner applies 16 `UPDATE`s by SQL; `name` UNIQUE, no collisions; displays and LINE (send-time join) follow immediately; `price_group` untouched. List: seed drift on `sid`; an admin rename screen (S). Offered to write the SQL text as a DATA REQUEST. Nothing cut.
# 2026-09-17

## Porter (PM) — `REQ-090`: @Sober confirms no admin UI/route to rename a program — it is SQL the owner runs himself. Told Sober to write the 16-UPDATE transaction (trimmed, verbatim, one txn, SELECT before/after) into REQ-090 as a DATA REQUEST for `uat` + `sid`. Agents never run it. Putting two optional follow-ups to the owner: update the dev seed (so sid matches by seed), and an admin rename-program screen (so future renames are data-entry, not SQL).
- Sober: REQ-090 §5 DATA REQUEST written (16-UPDATE transaction, SELECT before/after, rollback rule). Nothing cut.

## Porter (PM) — `REQ-090 §5`: owner (1) do dev-seed update (GO, XS); (2) NO rename screen here — belongs to the BACKOFFICE (scope boundary in SYSTEM-FACTS). Awaiting Sober's SQL DATA REQUEST text.

## Porter (PM) — Sober wrote the REQ-090 SQL (§6): one txn, 16 UPDATEs, SELECT before/after, ROLLBACK on any 0-row. Handed to the owner to run on uat + sid. Re-confirmed GO to Sober on the seed update (item a). Renamed the SQL block to §6.

## Porter (PM) — `REQ-090 §7`: owner ran the rename SQL; after-SELECT = 20 rows, all 16 `Private …` verbatim + 4 unchanged + price_group intact. ✅ Correct. Confirming which box + that the second box is also run.

## Porter (PM) — `REQ-090 §7`: owner ran the rename SQL; after-SELECT = 20 rows, all 16 `Private …` verbatim + 4 unchanged + price_group intact. ✅ Confirming which box + second box.

## Porter (PM) — `REQ-090`: the SQL was run on `sid` only. `uat` (customer's live box) still on OLD names — the RENAME THE CUSTOMER ASKED FOR is not visible to them until uat is run. Flagged to the owner: run the SAME transaction on uat.

## Porter (PM) — 🔵 new batch `REQ-091` (Rental), verbatim: (1) whole-course rental recorded at creation; (2) per-session rental grid marker (`R+`) so admin collects cash at the shop. Rental already posts at sale (qty=hours, SYSTEM-FACTS). Requested a current-state READ from @Sober (how rental works today, whole-course exists?, grid has a has-rental fact?, money touch per item) before any ruling. Nothing dispatched.

## Porter (PM) — 🟢 **REQ-090 CLOSED** — rename SQL run on BOTH `sid` and `uat` (identical 20-row result). Customer now sees `Private …` names. Remaining thread only: the dev-seed update (@Sober, XS) so a re-seeded `sid` keeps the new names.

## Porter (PM) — 🟢 REQ-090 CLOSED — rename SQL run on BOTH sid and uat (identical 20-row result). Customer sees the new names. Open thread only: dev-seed update (@Sober, XS).

## Porter (PM) — `REQ-091` full spec recorded verbatim (§3) + structured (§4): 3 capabilities (record rental · grid `R` red→green paid marker · teacher notice), fixed tiers 50/100/150/200, Full Set/inline needs a remark, whole-course=upfront one-per-session. Put 4 questions to the owner (§5): money boundary (does paid post revenue here or backoffice), price source (fixed vs editable), one-tier-per-course, R vs R+. Sober's read still pending.

## Porter (PM) — `REQ-091 §6`: owner (1) rental money POSTS here via recordSale (paid press posts; whole-course posts at creation) — the rental exception to backoffice-owns-money; (2) prices FIXED for now, editing=backoffice later; (3) will ASK the customer (I drafted the question §6.3: whole-course = one tier for all sessions, or per-session tier?); (4) `R` red/green, not `R+`. Waiting on the customer's answer to §6.3 + Sober's current-state read before cutting.

## Porter (PM) — `REQ-091 §6.3`: customer confirms one tier per whole-course rental, same every session. SPEC COMPLETE. Asked @Sober for the current-state read + a sized task breakdown before cutting (bigger feature; want shape + deploy count first).
- Sober: REQ-091 current-state read + breakdown to @Porter — rental today = a ledger post only (no row, no unpaid state; `hasRental` exists on the DTO, unused by FE); 100-tier missing as one code; whole-course net-new. Shape: `booking_rentals` row + money via existing `recordRental`; 4 tasks (T1 BE M, T2 FE M, T3 BE M, T4 S+S), 2 deploys; migration 35→36. Two owner questions (teacher notice timing; historic rentals). Nothing cut.

## Porter (PM) — `REQ-091 §7/§8`: Sober's read + plan (4 tasks, 2 deploys A=per-session R marker+record/paid, B=whole-course+teacher notice; new booking_rentals table, migration→36; 5th tier code for 100=Helmet+Pad). Two owner questions (teacher-notice timing; historic rentals show no R) + a price FYI (5th code). Taking to the owner before cutting.

## Porter (PM) — `REQ-091 §9` fully ruled: teacher notice = reminder line + same-day separate notice; historic rentals left; 5th code ok; TWO deploys. GO to Sober on Deploy A (T1 BE table+migration36+rental/paid endpoints+5th code+DTO · T2 FE R red/green chip+modal). Migration 36 = first since 0034 — flagged. Teacher-notice copy gated (T4/Deploy B). 
- Sober: REQ-091 §9 ruled; Deploy A cut — TASK-371 (BE, booking_rentals + migration 36 + routes + 5th tier + DTO) @Jason, TASK-372 (FE, R chip + modal) @Fern. Migration lock rule checked (CREATE TABLE ≠ 0033's closed-shop rebuild). Deploy B (T3/T4) after A.

## Jason (BE) — 📋 TASK-371 contract CONFIRMED with five corrections, building
Lock = SHARE ROW EXCLUSIVE on `bookings` (writes wait, reads continue, no scan) — not closed-shop, header says the hazard · witness = the last statement, `CREATE UNIQUE INDEX booking_rentals_booking_uq`; tag `0035_booking_rentals` ⇒ **36 = 36** · 5th `bo_item` via `sale:ensure-items` (deploy: migrate → ensure-items → restart) · the UNIQUE's 23505 caught in the service (`onError` would say SLOT_TAKEN) · `rental` rides as a relation in `withBookingRelations`, no grouped read except the list's hand select · remark = set + ride · not-live = CANCELLED/PAUSED · a cancelled session keeps its row (nothing to build).

## Jason (BE) — ✅ TASK-371 DONE (code): `booking_rentals` (`0035`, **36 = 36**), record/paid/remove, the 5th tier, DTO `rental` replaces `hasRental`
**2233 pass / 0 fail**, 180 files · tsc 0 · preflight scanner offline: one batch · no DB touched. Lock named in the header (SHARE ROW EXCLUSIVE on `bookings`, writes wait, not closed-shop) + deploy order (migrate → `sale:ensure-items` → restart) · witness = the unique index · money only via `recordRental(hours 1, refId = bookingId)` on the paid press, `paid_at` after the post · 23505 ⇒ RENTAL_EXISTS in the service · `hasRental` + `bookingsWithRentals` gone. Nine pins moved with reasons (one found a region whose end anchor had vanished into `slice(at, -1)`). Twelve mutations bite. Nothing waits for BE.
- TASK-371 reviewed (2233/0; 36 = 36, tag 0035). Migration facts (SHARE ROW EXCLUSIVE, deploy order with sale:ensure-items) to @Porter; confirmed contract to @Fern.

## [--:--] Fern (FE) — ✅ **TASK-372 done — the `R` chip beside `Last` (red unpaid → green paid) and a Rental section on the booking modal: add · mark paid in two taps · remove while unpaid; every rule the server's.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  377 pass / 0 fail   (was 368; +9)
bun run build     → ok
```
13 modified · 2 new (`Calendar/Modal/RentalSection.tsx` · `lib/scheduler/rental-row.test.ts`). Built against Jason's
confirmed contract. Notes in the TASK §3.

**§1 chip:** `RentalStamp` beside `LastStamp` in `BookingCellBody`, both grids' name row, ungated; renders ONLY from
`booking.rental` — null ⇒ nothing (historic rentals have no row ⇒ no chip), unpaid ⇒ solid red `R`, paid ⇒ solid
green `R`; rendered-asserted all three; legend both states. Wire → type → mapper (`rental: dto.rental ?? null`,
value-asserted); `RENTAL_CODES` gains the 5th, in the customer's price ladder.
**§2 modal:** `RentalSection` under the notes: no row ⇒ `Add rental` ⇒ tier Select (`Full Set — 200`, the customer's
words, the server's `rentalItems` price) + remark ⇒ `POST { code, remark? }` — the remark rides only when typed,
whether it is REQUIRED is the server's answer (no tier check here, asserted); a row ⇒ the print line `Rent 200 /
Full Set (inline skate size 18-19 CM)` (value-asserted) + state; unpaid ⇒ green **Mark paid** (TWO taps — the app's
`useConfirm` naming the line; the gate shape asserted after a `false &&` slipped the first pin) + subtle **Remove**;
paid ⇒ no buttons. Refusals = the server's sentence; `502 RENTAL_NOT_POSTED` leaves the button live. Three services,
three hooks, `invalidateAll` (pause/resume's set).
❓ **`RentalModal`:** was reachable from the Bookings page (`Record rental`, walk-in) AND the booking modal's ⋯ (`Add
rental` with `refId` — posts money on the spot, bypassing row/remark/paid, colliding on the ledger key). **I removed
the ⋯ door and kept the Bookings-page one; the modal is untouched.** One menu item to restore if you want it — say so.
📌 A pin not mine moved: the owner's colour pass (`ed4fd47`) broke TASK-367's name-row anchor; re-anchored with the
reason. 23 keys ×2.
🔑 **Break-and-watch ×8 in `try/finally`** (colours swapped · chip on any course · one-tap paid · client remark rule ·
buttons on a paid row · remark always sent · mapper drops the row · week drops the chip) ⇒ 1 each; md5 identical.

### Q — which truncates first: **the NAME, first and only** — it is the row's one `min-w-0 flex-1 truncate`; the
dot, time, `Last`, `R` and the badge group are all `shrink-0` (badges stack via `flex-wrap`). Past that the week
chip has no `overflow-hidden` and would overflow its edge (the day card clips). Right default: the two chips are the
facts an admin acts on this week; the name is a tap away. Nothing changed on the row; lever if it bites:
`overflow-hidden` on the week chip. Named.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-372 reviewed (377/0). REQ-091 Deploy A ready (BE 2233/0, 36=36). Told @Porter incl. the removed ⋯ Add-rental door and Tanya's shape. Deploy B on his go.

## Porter (PM) — 🟢 REQ-091 Deploy A ready (TASK-371 BE 2233/0 · TASK-372 FE 377/0 · 🔴 MIGRATION 36 = `0035_booking_rentals`, first since 0034). Deploy = db:migrate → sale:ensure-items (adds 5th tier) → restart; migrate is a write-blocking blink on bookings, run outside the 17:30 job / not during an import. To the owner: the deploy steps + the removed ⋯→Add-rental menu (REQ-028 shortcut, dropped as a colliding 2nd door; standalone Record-rental untouched; restorable). @Tanya pre-staged.

## Porter (PM) — 🟢 REQ-091 Deploy A LIVE on `sid`: owner ran db:migrate (verify=36 witnesses, 0035_booking_rentals) + sale:ensure-items (created rental-helmet-pads 100). @Tanya GO. Deploy B (whole-course + teacher notice) waits until A is QA-clean on sid.

## Tanya (QA) — ✅ **REQ-091 Deploy A (rentals) done — all 5 checks PASS. Report in `inbox/PM.md`; footprint declared.**
Method: R chips / modal lines / mark-paid dialog / tier picker / legend on screen; refusals, idempotency, remove rules, cancel-survival, historic, and the posted SALE via API + the sid backoffice ledger (`bo.movement`, read-only). Prereqs live (5th tier `rental-helmet-pads` = 100, migration 0035 in).
- **1 add:** Helmet+Pad (100) ⇒ red `R` (grid) + modal `Rent 100 / Helmet + Pad · unpaid`; Full Set / Ride w/o remark ⇒ `RENTAL_REMARK_REQUIRED "กรุณาระบุรายละเอียดอุปกรณ์ (ชุด/คู่/ไซส์)"`. Tiers: Helmet 50 · Pad 50 · Helmet+Pad 100 · Ride 150 · Full Set 200.
- **2 pay:** green `R` + modal `Rent 200 / Full Set (…) · paid`; 2-tap dialog NAMES the line ("This posts Rent 100 / Helmet + Pad to today's sales"). Ledger: ONE `bo.movement` SALE valueMinor 10000 (=100) refId=booking; pay again ⇒ no 2nd movement (idempotent). 🔎 the sale lands in the BACKOFFICE ledger, NOT scheduling `/reports/daily` (booking counts only) — confirm which report the owner means.
- **3 remove:** unpaid ⇒ Mark paid + Remove (DELETE works); paid ⇒ no buttons (DELETE ⇒ `RENTAL_PAID`).
- **4 cancel w/ paid rental:** rental survives (`{helmet-pads, paid:true}` after CANCELLED), session in cancelled tray. Note: compact tray ROW has no R chip; green rental persists in the modal (API-confirmed).
- **5 historic:** old `/rentals` post (no booking_rentals row) ⇒ rental DTO null ⇒ no `R`.
- **Legend:** both states (`R` unpaid red · `R` paid green).
- 🔎 **Minor FE:** modal offers "Add rental" on a CANCELLED session; server refuses (`BOOKING_NOT_LIVE`); button should hide on CANCELLED/PAUSED.
- **Footprint:** 18-Sep KKTEST SINGLE_SESSION specimens all CANCELLED; 3 rental SALES posted to sid/dev backoffice ledger, not reversed (manual bo adjustment by design): helmet-pads×2 @100, set×1 @200 — declared. No LINE, no uat, no code.

## Porter (PM) — 🟢 REQ-091 Deploy A QA-GREEN on sid (all 5 checks + legend). Rental sale lands in the BACKOFFICE ledger (bo.movement), NOT the scheduling daily report — consistent with the owner's money-in-backoffice design; the `R` chip is the collect-cash reminder. GO to Sober on Deploy B (T3 whole-course + T4 picker + teacher notice, copy gated). Minor: Add-rental button shows on cancelled sessions (server refuses; cosmetic). Asked owner re: push A to uat now vs bundle with B.

## Porter (PM) — owner: Deploy A holds; A+B ship to uat together in one deploy (one migration + sale:ensure-items on uat then). Deploy B in progress at Sober.
- Sober: Deploy A green on sid; Deploy B cut — TASK-373 (BE whole-course) @Jason, TASK-374 (FE picker + 2 QA nits) @Fern, TASK-375 (BE reminder line + same-day notice, copy gated) @Jason. A+B to uat together.

## Porter (PM) — 🔵 DISCUSSION `REQ-092`: customer wants per-account permissions (menu + action RBAC). 🔴 Surfaced the prerequisite: today ONE shared login, no RBAC by the 07-29 ruling, actor=null everywhere — individual logins are the foundation of every tier and reverse that ruling (owner's call). Proposed a 3-tier priced option (1 menu-level roles · 2 +view/edit · 3 custom per-account builder), each a superset = an upgrade path. Real numbers need Sober's read of current auth + menu/action counts. Not dispatched.

## Jason (BE) — 📋 TASK-373 contract CONFIRMED (derive-and-copy, no column) with three corrections, building
The reconcile's template is the SICK_LEAVE row (no rental) ⇒ copy from the COURSE's rental (`courseRentalOf`, pure) · the course sale's post never throws but `recordRental` does ⇒ the mirrored best-effort post needs a `.catch` · rental rows inserted after the flip loop over the final live rows · resume does not inherit (said) · drop posts no reversal today (named). 36 = 36.

## Porter (PM) — `REQ-092 §4`: single customer, but PM produces a 3-option customer-facing proposal (owner sets price per option) for a budget quote. Asked @Sober for a SIZING READ (current auth: shared login?, cost of individual accounts foundation, menu count / edit-action count / matrix surface, any existing scaffolding) → T-shirt sizes per tier. Estimate-only, parallel to rental Deploy B. Next: I write the plain-language option sheet.

## Jason (BE) — ✅ TASK-373 DONE (code): whole-course rental at creation, derive-and-copy, no column
**2251 pass / 0 fail**, 181 files · tsc 0 · **36 = 36** · rental pass after the flip loop over the final live rows · ONE post after the tx (`hours = size`, `refId = courseId`, no discount, `.catch`) · the reconcile copies the COURSE's rental onto each make-up (inherited stamps, no post) · `courseRentalOf` pure; the list's grouped read · resume does not inherit (said). DoD pinned with the real helpers; eleven mutations bite. ▶️ TASK-375 next.
- Sober: REQ-092 sizing read to @Porter — one shared env login; Role type + unused requireRole exist; 12 menus, 59 mutate routes; sizes: foundation L, T1 S–M, T2 M–L, T3 M. Nothing cut.

## Jason (BE) — 📋 TASK-375 contract + DRAFTED COPY sent (one stamp is the only new word); building with a placeholder
The parent's reminder prints `Rental :` too — `AUDIENCE_OMITS` is empty for both and Remaining/Expiry/Remark already print to parents (built as implied, one omit entry if the owner wants teacher-only) · the print rule `rentalPrintLine` lives once on the BE beside `toRentalDTO` · `reminderRanToday` extracted to `lib/reminder-run.ts` to avoid a jobs → scheduler → rental → jobs cycle · 18th kind, walkers move 17 → 18.

## Porter (PM) — `REQ-092 §5`: Sober's read in — frontoffice = one shared env credential, no user table; scaffolding thin (unused requireRole, role in token/session, 1 nav array, /api/* guard). 12 menus, 59 mutate routes. Sizes: Foundation L, T1 S–M, T2 M–L, T3 M (+1 migration foundation, +1 T3). Bonus: foundation → real audit actors. Wrote the customer-facing 3-option sheet (A menu / B +view-only / C full custom; all include individual logins). Owner fills price. Also: Deploy B (rental) cut TASK-373/374/375; T375 has a ruling Q for the owner (parent reminder rental line?).

## Jason (BE) — ✅ TASK-375 PLUMBING DONE (code): `Rental :` in the reminder (both audiences) + the same-day `rental_added_teacher` notice (stamp gated)
**2266 pass / 0 fail**, 182 files · tsc 0 · 36 = 36 · `rentalPrintLine` one source · `lib/reminder-run.ts` (the read moved verbatim to break a `jobs → scheduler → rental` cycle) · the line right after `Remark`, Remark's shape, both audiences per the empty omit table · the notice gated on today + reminder-ran, from the session writer only. Pins moved with reasons (walkers 17 → 18; survivors 8 → 9; the Record control's eighth firing; TASK-208's pin re-homed). Eleven mutations bite. Open: the stamp. Nothing waits for BE.
- TASK-373 + 375 reviewed (2266/0, 36=36). Contract to @Fern; 375 copy (one stamp) + parent-line fact + two list items to @Porter. Waiting on TASK-374.

## [--:--] Fern (FE) — ✅ **TASK-374 done — the whole-course rental on course creation from ONE shared picker, the summary × size, the course card's line, and the two Deploy-A nits (Add rental hidden off-calendar · the R chip in the tray row).** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  383 pass / 0 fail   (was 377; +6)
bun run build     → ok
```
10 modified · 2 new (`Rental/RentalTierPicker.tsx` · `lib/scheduler/course-rental.test.ts`). Built against Jason's
confirmed contract. Notes in the TASK §3.

**§1** — `RentalTierPicker.tsx` is the ONE picker/options source (`useRentalPrices` over the server's `rentalItems`,
`rentalPrintLine`, the five-tier Select + remark); `RentalSection` now renders it instead of its own copy (labels
and price join exist in exactly one file — asserted by absence in both callers). `PlanModal` create mode gained two
slots — `createExtras` above the AC-1 line, `createSummaryLine` under it; `CreatePlanFlow` fills them: a Checkbox
`Equipment rental for the whole course` (OFF by default) revealing the picker, and the summary `Rent 200 / Full Set
(…) × 8 sessions`. Request: OFF ⇒ `undefined` ⇒ the key ABSENT on the wire (JSON drops it — the `discount`/
`absentWeeks` mechanism, asserted by a round-trip); ON ⇒ `{ code, remark? }`. No remark rule on the create path.
📌 The `wire-names` §A sweep caught my first cut (a spread hid the key from its scan) — rewritten to the `discount`
pattern; the sweep proved its reach. The course card shows `Rental: Rent 200 / Full Set (…)` when non-null;
`CourseSummary.rental?` → view → mapper (`?? null`, value-asserted).
**§2 (a)** — `canAdd = !OFF_CALENDAR_STATUSES.includes(status)` — the grids' ONE literal (`CANCELLED · PAUSED`) = the
complement of the server's `rentalBookingLive`; the two agree by being the same two statuses (asserted, no second
literal); an existing row on a cancelled session still renders and can be paid. **(b)** `RentalStamp` in the tray
row, both variants; rendered-asserted green on a paid cancelled row. 2 keys ×2.
🔑 **Break-and-watch ×8 in `try/finally`** (toggle on by default · sent when OFF · `rental: {}` · a second status
literal · Add shown always · tray chip gone · summary without size · a copied Select) ⇒ 1 each; md5 identical.

### Q — 10 green `R`s: a rented course is ONE session per week, so the chips spread across ten week views; a busy
week shows one green per rented course (six courses ⇒ six chips among ~90 cells). Green is information, red is
the action — a cash scan must read each green to dismiss it. Built as ruled; the lever if "red only" wins: one
line in `RentalStamp`, or a "show paid rentals" display toggle. Named.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-374 reviewed (383/0). REQ-091 Deploy B ready; whole REQ built; one stamp gated. Told @Porter with Tanya's shape.

## Porter (PM) — `REQ-091 §11`: owner approved the RENTAL ADDED copy + reminder rental line to BOTH teacher and parent. REQ-091 fully built (A+B). Sober lands strings. @Tanya pre-staged for Deploy B on sid (no new migration; restart both). Then A+B to uat together (migrate 0035 + sale:ensure-items on uat).

## Porter (PM) — owner: sid deployed (Deploy B, no new migration). @Tanya GO on the 5 Deploy-B checks.

## Tanya (QA) — ⚠️ **REQ-091 Deploy B done — checks 1·2·5 PASS; check 3 FINDING; check 4 read-blocked. Report in `inbox/PM.md`; footprint cleaned.**
Whole-course rental = `POST /courses {rental:{code,remark?}}`. API + sid backoffice ledger for 1-4; nits on screen.
- **1:** Full Set no remark ⇒ refusal before create (`RENTAL_REMARK_REQUIRED`); with remark ⇒ every live session green R, course DTO carries card line, ONE ledger SALE `rental-set×4` = 800 (valueMinor 80000, qty −4).
- **2:** declared-leave row noR + creation make-up R-paid; toggle OFF ⇒ all rental=null.
- **3:** `Add rental` on rented session ⇒ `RENTAL_EXISTS` ✅; sick-leave ⇒ no new ledger line ✅; 🔴 **FINDING: sick-leave make-up does NOT inherit green R.** Sick-leave branch appends via its own `tx.insert` (note `...จากการลา`) w/o the `courseRental` copy; copy lives only in `reconcileCoursePlan:2437`, which sick-leave bypasses. PROVEN not deploy-lag: on the same course, a CANCEL make-up (routes through reconcile) inherited R-paid (2027-01-06), sick-leave make-up did not (2026-12-30). No double-charge; missing per-session chip only.
- **4:** NOT_TESTED — reminder needs `INTERNAL_JOB_SECRET` (not mine) + outbox unreadable + 0 linked recipients; `runDailyReminderJob` returns counts only. `Rental :` line & `RENTAL ADDED` notice = DATA REQUEST.
- **5:** both Deploy-A nits FIXED — no `Add rental` on cancelled session; R chip renders in cancelled-tray row (green paid / red unpaid).
- **Footprint:** 4 whole-course specimens CANCELLED; whole-course rental SALES posted to sid dev ledger not reversed (set×4=800, helmet-pads×4 ×2). No LINE, no uat, no code.

## Porter (PM) — Deploy B QA: mostly PASS; 🔴 DEFECT — post-creation sick-leave make-up does not inherit the course rental (sick-leave path skips reconcileCoursePlan's courseRental copy; scheduler.service.ts:2437). No double-charge; missing R chip + reminder line on that make-up. Test :140 asserts inheritance but only covers the reconcile path. Sent to Sober to fix + add sick-leave-path coverage. HOLDING uat until fixed (rides A+B deploy). Item 4 reminder text = owner verifies on uat (read-blocked on sid).
- Sober: Deploy B copy approved; QA defect (sick-leave make-up skips the rental copy — second writer at :3155) verified and cut as TASK-376 @Jason (one inheritCourseRental, both writers) + land the stamp. uat held.

## Jason (BE) — ✅ TASK-376 DONE (code): ONE `inheritCourseRental`, called by BOTH make-up writers; the `RENTAL ADDED` stamp landed (0 bytes changed, frozen)
**2270 pass / 0 fail**, 183 files · tsc 0 · 36 = 36. The 373 defect was mine — a pin on an inline copy in one writer cannot see a second writer; Tanya found it by use. Sick-leave branch now calls the chokepoint after its own insert (locked branch: nothing); reconcile's inline copy removed; call-site and insert counts pinned; resume named, not wired. Seven mutations bite, A = the defect as shipped. Owner's-list table of the three writers' facts in the TASK. `uat` unblocked on my side.
- TASK-376 reviewed (2270/0; 2 call sites; stamp live). uat released to @Porter (A+B). Facts: REQ-091 shape + the three-writers fact.

## Porter (PM) — TASK-376 (defect fix) in: one inheritCourseRental called by both make-up writers; RENTAL ADDED stamp frozen; BE-only (2270/0), FE unchanged. Asked owner to redeploy BE on sid (no migration), then Tanya re-checks the one sick-leave-make-up case, then A+B to uat.

## Porter (PM) — owner: BE redeployed on sid. @Tanya GO on the TASK-376 one-case re-check.

## Tanya (QA) — ✅ **TASK-376 re-check PASS — my Deploy-B defect fixed.** Rented course → post-creation sick-leave make-up now = green R (paid) + modal line (rental-set·size 40); cancel make-up = green R; ledger delta 0 (no re-post); unrented course make-up = noR. Data-layer confirmed (drives the already-verified FE green-R render). Footprint: 2 courses cancelled (rented set×4=800 posted at create, declared). Deploy B clean; A+B ready for uat. Report in inbox/PM.md.

## Porter (PM) — 🟢 TASK-376 re-check PASS. REQ-091 A+B fully QA-clean on sid. Gave owner the uat deploy steps (first rental deploy on uat: db:migrate → verify=36 → sale:ensure-items → restart both). Item 4 reminder text = owner's phone check on uat.

## Porter (PM) — 🔴 uat migrate STOPPED: connected DB shows 0027 (8 pending 0028–0035), not the expected single 0035. Flagged to owner it is likely NOT uat — `0030_family_line_links` is pending here but REQ-088 (uses it) works on uat ⇒ contradiction ⇒ wrong DB connection suspected. Asked owner to confirm which DB .env points to. Asked Sober: (1) does the evidence prove this isn't uat; (2) if it IS uat-behind, the safe two-run order + 0033 is the ACCESS EXCLUSIVE closed-shop index. Nothing applied. Standing lesson reinforced: verify the box before migrating a live one.

## Porter (PM) — 🔻 my error: on the uat rental deploy the preflight showed 8 pending (0028–0035) and I invented a wrong-DB theory (host/.env mismatch) and stalled the owner. The owner: it's identical DBs, and uat release is ALWAYS like this. Correct: routine uat catch-up; preflight itself prints the two-run fix (db:migrate:through 0032 → db:migrate) because 0032 adds the PAUSED enum 0033 uses. Recorded the uat release procedure in SYSTEM-FACTS so it is not re-derived. Owner running the two-run migrate + sale:ensure-items now.

## Porter (PM) — uat: after migrate-through 0032, db:verify flagged 0028–0032 journal-present but unapplied (ledger has a newer created_at row) = the TASK-085 silent-skip. Documented repair: db:seed-ledger dry-run → read → --apply → db:migrate → db:verify. Owner running the dry-run; holding restart until verify=36.

## Porter (PM) — uat seed-ledger DRY-RUN: schema witnesses confirm 0000–0034 ALL applied in the real schema (family_line_links, PAUSED enum, etc. present) — only the LEDGER was out of sync (7 rows to insert). Only 0035_booking_rentals genuinely unapplied. ⇒ my earlier read-only "all false" check was against a wrong session/DB; the schema-witness seeder is authoritative: uat schema is current at 0034. Green-lit the blessed repair: seed-ledger --apply → db:migrate (applies only 0035) → db:verify=36 → sale:ensure-items → restart. 0033 already applied ⇒ no closed-shop re-run.

## Porter (PM) — 🟢 REQ-091 (A+B) LIVE on uat: sale:ensure-items created rental-helmet-pads 100, BE+FE restarted, db:verify=36. Ledger desync repaired cleanly (TASK-085 procedure). REQ-091 CLOSED except the owner's on-phone check of the rental LINE copy on uat.

## Porter (PM) — owner asked for a REQ-091 ACCEPTANCE re-confirm: @Tanya to verify the built feature matches the customer's message point-by-point on sid (3 goals · 3 types · 5 prices+remark · R red/green + whole-course-on-schedule). Dispatched as a MATCH/MISMATCH checklist.

## Tanya (QA) — ✅ **REQ-091 ACCEPTANCE re-confirm PASS — built feature matches the customer's message point-by-point.** MATCH/MISMATCH table in inbox/PM.md. 3 goals ✅ (goal 3 teacher-notice TEXT owner-verifies on uat, read-blocked on sid), 3 types ✅, 5 prices ✅ (labels+prices), remark rule ✅, print format ✅, red/green R chip ✅. Size-10 whole-course ⇒ 10 green R + one ledger line 2000 (qty −10). Two wording notes (50-tier split into Helmet/Pad rows; remark rule covers Ride too), no behaviour mismatch. Footprint: size-10 specimen cancelled (set×10=2000 dev-ledger, declared). No code, no LINE, no uat.

## Porter (PM) — 🟢 REQ-091 ACCEPTANCE PASSED (Tanya, sid): matches the customer message point-by-point. Owner accepted the two wording notes as-built ("ตามนี้ก่อน"): 50-tier stays two items; Ride-only keeps remark. REQ-091 COMPLETE (live on uat); only open = owner's on-phone check of the notice text on uat. Engineers idle. REQ-092 (RBAC) awaiting customer's tier pick.

## Porter (PM) — 🟢 REQ-092 GO: owner bought FULL RBAC (option C, 2900). Recorded §6 (super admin registers users + manages menu/action perms; roles + assign; per-user matrix; runtime-configured; first super-admin bootstrapped). Dispatched to Sober: staged build plan + cut Stage 1 (foundation: users/auth/super-admin/user CRUD). Stages 2 menu → 3 action → 4 role builder+matrix, each own sid→QA→uat. Migrations: foundation +1, matrix +1.
- Sober: REQ-091 live on uat. REQ-092 GO (full RBAC, staged) — SPEC-079 written (model, 4 stages, 2 migrations, 4 owner decisions); Stage 1 cut: TASK-377 (BE users/login/bootstrap/CRUD/guard) @Jason, TASK-378 (FE session + Users page) @Fern. Told @Porter.

## Jason (BE) — 📋 TASK-377 contract CONFIRMED (4 decisions, 2 corrections), building
Bootstrap at the first login (not server start — import-time DB call, pm2 ordering, restarts) · `ADMIN_*` retired outright · `role` stays for Stage 1 (FE session + `assertMayDiscount`), goes in Stage 3 · `requireRole` → `requireSuperAdmin` · the guard's disabled sentence is its own · `AuthUser` context, no `.sub` readers · `0036_users` → 37 = 37 · the row-read cost estimated, the cache named and not built.

## Jason (BE) — ✅ TASK-377 DONE (code): RBAC Stage 1 — `users` (`0036`, **37 = 37**), `Bun.password`, login against the table, first-login bootstrap, the guard loads the row, `actorOf` = username, user CRUD/reset/disable, LAST_SUPER_ADMIN
**2294 pass / 0 fail**, 184 files · tsc 0 · preflight offline: one batch · no DB touched. `ADMIN_*` retired; `requireRole` → `requireSuperAdmin`; 12 actor sites, zero `.sub`; the disabled sentence is the guard's own. Pins moved with reasons (auth test rewritten for the table; actor literals; 36 → 37). Thirteen mutations bite — E only after pinning the enabled-peers query. Nothing waits for BE.
- TASK-377 reviewed (2294/0; 37=37 0036_users). Contract to @Fern; deploy facts + two built decisions to @Porter. Waiting on TASK-378.

## [--:--] Fern (FE) — ✅ **TASK-378 done — the session carries the real user, the header shows the display name, a Users page for the super admin.** 🔻 **And a BE defect found by reading the widened `role`: a super admin cannot give a discount.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  394 pass / 0 fail   (was 383; +11)
bun run build     → ok — `○ /scheduler/users`
```
9 modified · 6 new. Built against Jason's confirmed contract. Login screen untouched (asserted). Notes in the TASK §3.

**§1 session:** `LoginResponse.user` = `{ id, username, displayName, isSuperAdmin, role }`; `authorize` maps `id` =
the ROW id (was the username), `name`/`displayName`, `isSuperAdmin === true`, `role` still read; token → session in
`auth.config.ts`; the augmentation; the header shows `displayName` (fallback username). A `401` takes the EXISTING
interceptor sign-out path — nothing new; ⚠️ the guard's disabled sentence is not shown (sign-out precedes render, the
login screen is unchanged) — a `reason` on the redirect is a small Stage-2 task if wanted.
**§2 page:** `NavItem.superAdminOnly` + `navItemsFor(isSuperAdmin)` (value-tested: the entry appears only for `true`;
the sidebar reads the session); `/scheduler/users` refuses non-super-admins with ONE sentence (the app has no
"no access" shape — none invented) and never fetches for them. List: username · display name (+`(you)`) · Access
badge · Status · Created. Create ⇒ `POST /users` (username trimmed+lowercased, `isSuperAdmin` only when true; the
pattern shown as the hint, refused server-side); Edit ⇒ `PATCH` with ONLY the changed fields; Reset ⇒ a dialog with
password+confirm, its own submit = the second tap; Disable ⇒ `useConfirm` (two taps), Enable one tap. Refusals = the
server's sentence in the dialog / a notice. 🚫 No client rule beyond "the two boxes match" (asserted with trailing
comments stripped — my own `// LAST_SUPER_ADMIN lands here` comment tripped the first negative). 38 keys ×2 + `nav.users`.
🔻 **BE defect:** the token issues `role: "super_admin"` for a super admin, and the BE's `assertMayDiscount`
(`discount-plan.ts:147`, `role !== "admin"` ⇒ 403) refuses it — **the bootstrapped first user, the only account on
day one, cannot give a discount.** Decision 3's "every table user is at least admin" does not hold for the super
admin. I widened the FE gate (`DiscountSection`) to the intent; the BE half is one line, @Jason's. Not touched.
🔑 **Break-and-watch ×10 in `try/finally`** ⇒ 1 each; md5 identical on 7 files.

### Q — who they are: the header now shows the display name for everyone. A self-service password change belongs on
the avatar circle as a small menu (name · username · Change password · Sign out), needing one BE route
(`POST /me/password { currentPassword, newPassword }`) and a dialog reusing this page's password pair. Stage 2+. Named.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
- TASK-378 reviewed (394/0). Fern's finding verified: super admin refused by assertMayDiscount → TASK-379 (XS) @Jason cut; ships in Stage 1. Told @Porter.

## Jason (BE) — ✅ TASK-379 DONE (code): a super admin may discount — the capability beside the label, one line
**2295 pass / 0 fail**, 37 = 37. My 377 defect (Decision 3 assumed "at least admin" while I had made the super admin's role `super_admin`); Fern found it. Pinned: super admin passes, capability alone passes, the label without the capability is refused; four mutations bite (A = as shipped). Nothing waits for BE.
- TASK-379 reviewed (2295/0). RBAC Stage 1 complete (BE 2295/0, 37=37; FE 394/0) → handed to @Porter for sid with the deploy note + Tanya's shape. Stage 2 on his go.

## Porter (PM) — 🟢 RBAC Stage 1 complete (TASK-377/378/379): BE 2295/0, FE 394/0, MIGRATION 37 (0036_users, two tables, no lock). Gave owner the sid deploy (migrate→verify=37→set BOOTSTRAP_ADMIN_* env, NOT in tracked files/no paste→restart→first login creates super admin→remove old ADMIN_*). @Tanya pre-staged (login-touching, test on sid first). Stage 2 (menu guard) after Stage 1 passes.

## Porter (PM) — 🔴 Stage 1 bootstrap issue on sid: super admin = `change-me-before-first-login` (default when BOOTSTRAP_ADMIN_* unread; likely set in the wrong folder's env — the .exe reads frontoffice\back). Bootstrap is one-time so env/rebuild changes are ignored — the row already exists. Asked owner if he can reach Users menu (⇒ edit user) vs not (⇒ clear users + set env in the .exe's folder + restart). Routed to Sober for the authoritative recovery + default password + whether to hard-fail the default.

## Porter (PM) — 🔴🔴 Stage 1 bootstrap BUG: log shows createUser(admin) but users table 0 rows + login fails ⇒ insert not persisting (swallowed exception / uncommitted tx after the log). Env is now read correctly (right folder). Told owner to STOP retrying; fetching BE error log. Escalated to Sober to read bootstrapIfEmpty/createUser + fix. Also flagged: password logged to stdout — remove.

## Porter (PM) — bootstrap loops EVERY restart (sees empty users each boot), createUser logged but never persists, no stdout error ⇒ un-awaited insert / swallowed catch / uncommitted tx in createUser. Told owner to stop and rest; ball with Sober/Jason to read createUser and fix. Getting error-log tail as backup evidence.
- Sober: Stage 1 sid bootstrap "bug" diagnosed from code + log: `BOOTSTRAP_ADMIN_PASSWORD=admin` fails `assertPassword` (min 8) inside createUser — silent ApiException, table stays empty. Recovery = 8+ char env password + restart + one login (no tables to empty). TASK-380 (XS) cut: refuse loudly, empty placeholder, never log a credential (uncommitted debug logs in user.service.ts print the password — the human's edits, flagged).

## Porter (PM) — 🟢 bootstrap ROOT CAUSE: password `admin` (5 chars) < PASSWORD_MIN 8 ⇒ createUser throws PASSWORD_TOO_SHORT before insert; FE showed generic "incorrect username or password" ⇒ invisible. NOT a lost insert. Recovery: set BOOTSTRAP_ADMIN_PASSWORD to 8+ chars in the .exe's env → restart → login once → super admin created. Nothing to delete. Password-in-log lines = owner's uncommitted edits in user.service.ts (git checkout OR Jason's XS task, not both). Sober cutting XS: surface the real PASSWORD_TOO_SHORT sentence on login + strip password logs + fix .env.example placeholder (ships with next Stage-1 sid build, non-blocking).

## Porter (PM) — owner set an 8+ char password → logged in as super admin, Users menu visible. Bootstrap resolved. @Tanya GO on Stage 1 checks (sid).

## Tanya (QA) — ⛔ **RBAC Stage 1: check 1 PASS, 2-6 BLOCKED — the deploy locked QA out of sid.** admin/admin refused (check 1 ✅, `ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง`); mint now 401 at login; my last token → INTERNAL on all backend calls (legacy sub=admin no longer a user); no super-admin cred. Checks 2-6 need a live (mostly super-admin) session I can't get/forge. **DATA REQUEST: owner creates a QA RBAC user (super_admin ideally) + puts creds in H:/sm-test-access.txt (admin/admin is dead).** Blocks Stage 1 AND all future sid testing. 🔎 Secondary finding: a token with a non-current sub returns 500 INTERNAL not 401 (auth.ts:60 findUserById throws before the `if(!row)` 401 guard) — hits every pre-deploy session's first call. Footprint: none. Report in inbox/PM.md.

## Porter (PM) — Stage 1 QA: check 1 PASS; 2-6 BLOCKED — the RBAC deploy killed QA's sid auth path (admin/admin refused, mint fails, old token errors). DATA REQUEST: owner creates a QA super_admin user → H:/sm-test-access.txt (REQ-080 pattern). 🔴 Secondary bug (must fix before uat): legacy/invalid token sub → 500 not 401 (findUserById throws before the !row guard); every pre-deploy admin session hits it on cutover. Sent to Sober to fix in the Stage-1 build.

## Porter (PM) — owner confirmed a super admin credential is now in H:/sm-test-access.txt. Released @Tanya to run Stage 1 checks 2-6 with it.

## Tanya (QA) — ✅ **RBAC Stage 1 checks 2-6 all PASS** (super-admin cred admin/admin-som from the access file). 2: super admin lists users + (you). 3: created admin logs in (isSuperAdmin:false), GET /api/users ⇒ 403 "เฉพาะผู้ดูแลระบบสูงสุดเท่านั้น". 4: reset (old 401/new 200), disable ⇒ next call 401 "บัญชีนี้ถูกปิดใช้งาน", disable last super admin ⇒ 409 LAST_SUPER_ADMIN. 5: super-admin discounted sale ({kind:PERCENT,value:10}) ⇒ ACCEPTED (TASK-379 capability not label). 6: expiry-history actor = "qa-rbac-admin" (real username, not SA, not hardcoded admin). Users menu is FE-gated by isSuperAdmin (confirmed true/false at login). Footprint: left qa-rbac-admin DISABLED (0e9c38e6, can't delete); 2 courses cancelled. Secondary 500-vs-401 finding still with Sober. Report in inbox/PM.md.

## Porter (PM) — 🟢 RBAC Stage 1 all 6 checks PASS on sid (super-admin cred from the access file). Holding uat until Sober's 500→401 fix (legacy token sub) lands + the XS (PASSWORD_TOO_SHORT surface, strip password logs, .env.example) — both ride the Stage-1 uat build. Then a quick sid re-check (old-sub token ⇒ 401) → uat. Leftover on sid: disabled qa-rbac-admin (can't delete). Stage 2 (menu guard) cut after Stage 1 reaches uat.
- Owner in (8+ char password). Stage 1 checks 2–6 green. Tanya's finding verified: non-UUID legacy sub ⇒ 22P02 ⇒ 500; folded into TASK-380 §2 (401 at the guard). uat waits on 380.

## Jason (BE) — ✅ TASK-380 DONE (code): the bootstrap refuses LOUDLY (§1) + a legacy `sub` is 401 not 500 (§2)
**2304 pass / 0 fail**, 37 = 37. Ten uncommitted debug `console.log`s found in `user.service.ts` (one printed the bootstrap PASSWORD) — removed per the task; a file-wide negative pins no credential in any log. `bootstrapEnvRefusal` pure, once-per-process error line, the specific 400 at login; `UUID_RE` before the row read, no catch-all. Seven mutations bite (D after pinning the 7/8 boundary). Nothing waits for BE.
- TASK-380 reviewed (2304/0): loud bootstrap refusal, no credential logs, empty placeholder, legacy sub ⇒ 401. Stage 1 build ready; told @Porter (sid re-check → uat; Stage 2 on go).

## Porter (PM) — TASK-380 in (500→401 + bootstrap loud-refuse + password logs stripped + .env.example): BE 2304/0, no migration. Asked owner to redeploy BE sid; Tanya re-checks old-sub token ⇒ 401. Then Stage 1 → uat (migrate 0036_users → BOOTSTRAP_ADMIN_* 8+ in frontoffice\back → restart → first login → remove ADMIN_*). Stage 2 cut after Stage 1 on uat.

## Porter (PM) — owner: BE redeployed on sid. @Tanya GO on the 500→401 re-check.

## Tanya (QA) — ✅ **TASK-380 re-check PASS — my 500→401 finding fixed.** Legacy sub=admin token ⇒ 401 "โทเคนไม่ถูกต้องหรือหมดอายุ" on /api/courses AND /api/users (was 500). Bootstrap-5-char sentence not tested (needs empty users table; sid live users off-limits per Porter). No footprint. Stage 1 clear for uat. Report in inbox/PM.md.

## Porter (PM) — 🟢 RBAC Stage 1 FULLY clear on sid (6 checks + 401 re-check). Ready for uat. 🔴 Flagged the cutover consequence to the owner: Stage 1 on uat kills the shared admin/admin login → staff locked out until the super admin creates per-person accounts (permissions arrive Stage 2). Advised doing the cutover when quiet + creating staff accounts immediately. Gave uat steps (migrate 0036_users single run since uat now at 36 → BOOTSTRAP_ADMIN_* 8+ in frontoffice\back → restart → first login → create staff → remove ADMIN_*). Awaiting owner's go/timing.

## Porter (PM) — owner: finish full option C before the incoming rental/other fixes. GO to Sober on Stage 2 (menu perms), then 3 (action) + 4 (role builder + matrix) back to back; build+QA each on sid; uat HELD until all of C is ready (single staff-login cutover). Asked Sober how a menu set is assigned pre-Stage-4 (per-user list folding into roles later).
- Sober: Stage 1 green on sid; owner: finish option C on sid, one uat cutover. Stage 2 cut — TASK-381 (BE menu keys/grants/requireMenu map/me/self-password) @Jason, TASK-382 (FE nav filter/route guard/shell/checklist/my-password/disabled reason) @Fern. Bridge = per-user menus on the Users page; Stage 4 roles set the same rows.

## Jason (BE) — 📋 TASK-381 contract CONFIRMED (Stage 2 menu guard), building
The route → menus map traced from the FE's actual calls (partial → hook → service → path; 3 routes with no FE caller placed by area) · ONE table + ONE fail-closed `menuGuard` on `/api/*` with an all-routes enumeration test · grants loaded for non-super-admins only · `/auth/me*` guarded explicitly (auth is mounted before the JWT guard) · zero-menu user ⇒ `menus: []`, 403 on every menu route.

## Jason (BE) — ✅ TASK-381 DONE (code): RBAC Stage 2 — the `menu:*` registry, grants on the guard, ONE table-driven fail-closed `menuGuard` over all 85 routes, `PUT /users/:id/menus`, `GET /auth/me`, self password
**2327 pass / 0 fail**, 186 files · tsc 0 · 37 = 37. The route → menus map is code (`lib/route-menus.ts`) traced from the FE's real calls; the enumeration pins every declared route mapped and no stale key; `/auth/me*` guarded explicitly (auth is mounted before the guard); a zero-menu user is let in with `menus: []`. Thirteen mutations bite. Nothing waits for BE.
- TASK-381 reviewed (2327/0). Contract lines to @Fern. Waiting on TASK-382.

## [--:--] Fern (FE) — ✅ **TASK-382 done — the nav and every page follow the user's menus (`/auth/me` the truth, the login body the seed), the empty shell, a `Menus` checklist per user, "change my password" on the avatar, the disabled reason on login.** → @Sober
```
bunx tsc --noEmit → exit 0
bun test          →  403 pass / 0 fail   (was 394; +9)
bun run build     → ok
```
16 modified · 8 new. Built against Jason's confirmed contract (TASK-381). Notes in the TASK §4.

**§1:** `lib/rbac/menus.ts` `MENU_KEYS` = the BE's twelve, pinned as a literal AND as the nav-derived list (each entry's
new `menuKey`, `NAV_ITEMS` then `HIDDEN_NAV_ITEMS`, in order) — 📌 that pin caught a real miss on its first run: my edit
had skipped `menu:badges`, which would have left Badges OPEN to everyone; fixed. `overview` moved from a comment into
`HIDDEN_NAV_ITEMS` (keyed, hidden as before). ONE rule `mayOpen(me, item)`; `navItemsFor(me)` widened (value-tested:
all / two / zero / unknown ⇒ nothing). `useMe()` = `GET /auth/me`, the session's `menus` as `initialData` with
`initialDataUpdatedAt: 0` (refetched at once), refetch on focus and on any `403` (`ss:forbidden` event from the api
client). `MenuGuard` on `AdminLayout`'s `<main>`: not granted ⇒ the Stage-1 Alert shape, one sentence + "Go to <first
menu>", the page never mounts; zero menus ⇒ the shell; the landing (`/scheduler/calendar`) ⇒ `router.replace` to the
first granted menu.
**§2:** `UserDTO.menus`; a `Menus` count per row ⇒ `MenusModal` (twelve checkboxes labelled by the nav's own
`labelKey`s, nav order, select all/none) ⇒ `PUT /users/:id/menus { keys }` (filtered to the registry); a super admin's
row says "All menus", no checklist.
**§3:** the avatar is the user menu (name · username · Change password · Sign out — the sign-out button moved in);
`ChangePasswordModal` ⇒ `POST /auth/me/password { currentPassword, newPassword }`, server sentences. 🔴 Named: the
wrong-current-password refusal is a **401** — the interceptor now exempts exactly `/auth/me/password` from the sign-out
(asserted); a 400/409 from the BE would let that go, @Jason's call. Disabled reason: `&reason=disabled` ONLY when the 401
body's message is the guard's own sentence; the login page shows one line. Keys: `rbac` 4 · `header` +6 · `login` +1 ·
`users` +8 (46) ×2.
🔑 **Break-and-watch ×10 in `try/finally`** ⇒ 1–2 fails each; md5 identical on 8 files. Four TASK-378 pins moved to the
Stage-2 shapes (the `reason` on the redirect, `navItemsFor(access)`, the 4th `setError`, 46 keys).

### Q — the landing: `/` and the login send everyone to `/scheduler/calendar` today (the Dashboard has been a hidden
route since REQ-026), so the question is "no `menu:calendar`". Built the simplest: the guard sends a user without the
landing's menu to their first granted menu in nav order, else the shell; any OTHER page by URL says why (sentence +
door) rather than bouncing.

**Ball: @Sober.** `BookingModal:1140` still the last known-open time · TASK-294, TASK-286 remain, no clock.
