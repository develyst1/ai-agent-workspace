# Log — 2026-08-16 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Porter (PM) — session opened on a new machine/session; startup ritual done
- Read `PROTOCOL.md` + `PM.md` (hard boundaries card), `board.md`, `PROJECT-STATUS.md`
  (reconciled 2026-08-11) and the previous log `log/2026-08-11.md`.
- Confirmed state: no open engineering blockers. Last delivery arc (2026-08-11) closed —
  customer-prod essential set (REQ-030 / REQ-037 / REQ-038 #1–5 / OBS-3 / REQ-040) and
  REQ-041 (FE-standard conformance, items 1–5,7,8) both DELIVERED and live on
  `frontoffice.develyst.online`. DEF-1/2/3 all CLOSED.
- Ball on the owner side for: REQ-035 go-live-vs-fast-follow call · REQ-036 model decision
  (hours-based course idea) · REQ-034/039/033 sequencing · LINE Scripts 4+5 (owner-run) ·
  prod `pg_hba` close + QA `QA-prod-*` residue cleanup.
- No new requirement received yet this session; nothing routed to `@Sober` or `@Tanya`.

## [--:--] Porter (PM) — owner reports LINE broken after the `sid` → **UAT** move → REQ-042 created, READY_FOR_SA
Owner report (Thai, in chat): LINE worked before the move. After moving the test site to **UAT**, his own account —
linked as **teacher** (2-cell menu) — typed `สมัคร`, chose **ผู้ปกครอง**, and **the rich menu never switched** to the
parent menu. He also asks whether `bun run line:publish-menus` has to be re-run after the environment move.

- Created **`requirements/REQ-042-line-uat-role-change-menu-switch.md`**, status **READY_FOR_SA**, priority HIGH,
  with 6 ACs (the repro both directions · one-role-only · negative path · "no dead taps on UAT" · the 2026-07-30
  regression set) and the owner-approved TH/EN wording for the failed-match path.
- **Two leads written into the REQ as things to CONFIRM, not assume** (I am not diagnosing): **(1) TASK-046** —
  DONE + Sober-verified 2026-07-30, fixes exactly this branch-order repro, and `PROJECT-STATUS.md` still lists it as
  **awaiting deploy + repro re-check**, so the UAT build may simply predate the fix; **(2)** the 2026-07-30 LINE
  outage was **environmental** — the webhook pointed at a stale server. Also recorded that the **teacher menu is
  2 cells by design** (parent = 6), so "ครูมีแค่ 2 ปุ่ม" is not the defect.
- Board: added the REQ-042 row and a 🆕 note at the top of the **ENVIRONMENTS** section — there is now a **third
  environment (UAT)** and every "testing happens on `sid`" statement in that section is suspect until the owner
  answers Q1–Q3 (same OA channel? webhook target? which build?).
- Asked the owner those three questions in Thai in chat; answers will land in the REQ's `## Questions`.
- **@Sober: please pick up REQ-042.** The deliverable the owner needs is an **exact, ordered operator list he runs
  himself** on UAT (including whether `line:publish-menus` is required and any read-only `line:inspect-menus` check
  to prove state before/after) — nobody on the team touches UAT. Q4 in the REQ is yours.

**Ball: @Sober** (REQ-042) + **owner** (Q1–Q3).

## [Sober (SA)] — REQ-042 picked up → SPEC-038 (owner-run runbook, Q4 answered). Diagnosed from code: env/OA-config, not a regression. → @Porter
Picked up REQ-042, set **IN_SPEC**, wrote **`specs/SPEC-038-line-uat-role-change-menu-switch.md`**. Grounded the whole
thing in the real `dong` code before deciding anything (SA-Lead §3), not in the board's memory of it.

### Diagnosis (confirmed against `smart-scheduler-back`, not assumed)
- **TASK-046 is present in the running branch.** `line-webhook.service.ts`: `สมัคร` resets to `CHOOSE_ROLE` from any
  state (`:398`), in-progress linking wins over already-linked routing (`:406-408`), role change **moves** the link
  (`moveRosterLink` `:171/:178` → AC-2). So the branch-order bug the board flagged as lead (1) is **fixed in code**.
- 🔑 **Root-cause shape found:** the menu switch `linkRoleRichMenu(...)` at `:461` is inside a `try/catch` that only
  `console.error`s and **swallows** the failure (`:462-464`). So when the switch fails, **the role link still completes
  but the rich menu silently stays put** — an exact match for what the owner saw. That failure happens when the stored
  menu ids don't resolve on UAT's OA.
- **Why the move breaks it:** rich-menu ids are **per-OA and live in the DB** (`app_settings.line_rich_menu_ids`, written
  only by `line:publish-menus`), **not in `.env`**. `linkRoleRichMenu` (`line-rich-menu.ts:201`) reads them and calls
  `POST /user/{id}/richmenu/{id}`. Four candidate causes, none assumed: **(A)** UAT is a **different OA token** → stored
  ids 404 → throw → swallowed; **(B)** UAT is a **fresh/other DB** → ids empty → silent no-op; **(C)** the **webhook
  still points at `sid`** (the 2026-07-30 class of outage); **(D)** UAT runs a **pre-TASK-046 build**.

### Answer to the owner's literal question (Q4)
Re-run `bun run line:publish-menus` on UAT **iff** UAT uses a **different OA token or a different/fresh DB** than where
the menus were last published (Cause A/B) — then the old ids are useless and every switch fails silently. If UAT is the
**same OA + same DB**, publishing is not it → check the **webhook target** (C) and the **build** (D). **`bun run
line:inspect-menus` is read-only and tells you which world you're in before any write** — always inspect first.

### Deliverable = the owner-run runbook (in SPEC-038, copy-paste ready)
STEP 1 inspect (read-only) → STEP 1b optional per-user link check → STEP 2 (healthy menus ⇒ webhook/build) → STEP 3
republish on UAT's OA (write) → STEP 4 re-verify mapped to AC-1..AC-6. Every step **owner-run on UAT** (team never
touches UAT); secrets never hit a tracked file. Images for publish are present in the repo, so preflight passes.

**No engineering TASK cut — deliberately.** The `dong` code already satisfies AC-1..4/6; this is config/ops. A TASK is
cut only if the runbook surfaces a real gap, or if Q3 shows an **old build** — in which case the action is a **redeploy
of the current `dong` build**, not new code (the fix already shipped to the branch).

Note for PROJECT-STATUS: it still lists TASK-046/047 as "awaiting deploy + repro re-check" — this runbook IS that repro
re-check on UAT; update the status once the owner runs STEP 4.

**Ball: @Porter** (hand SPEC-038's runbook to the owner; relay Q1–Q3 answers into the REQ — they pre-select the branch
but don't block the inspect). **& owner** (run the read-only STEP 1 first, send the output back up). SA queue clear
pending the runbook results.

## [--:--] Porter (PM) — relayed SPEC-038's runbook to the owner in Thai (STEP 1 read-only first)
- Read SPEC-038 and handed the owner the runbook **in Thai, copy-paste ready**, framed as: run the **read-only
  `line:inspect-menus` first**, send me the output, and the output itself decides which of Sober's four causes
  (A different OA · B fresh DB · C webhook still on `sid` · D old build) it is — nothing is written until then.
- **One deviation from SPEC-038's text, on stakeholder policy:** its STEP 1b hands the owner a
  `psql "$DATABASE_URL" -c "…"` wrapper. Per PROTOCOL "hand **PURE SQL only** — the raw `SELECT …;`" (stakeholder
  preference 2026-08-03, he runs it in his own already-open psql session), I relayed that step as plain SQL and
  dropped the connection wrapper. Behaviour identical; no change requested to the SPEC.
- Told him STEP 1b is optional and that his own LINE userId / token / webhook URL must **not** be pasted back to me
  (secrets discipline) — the inspect output he sends should have the userId removed.
- Q1–Q3 still unanswered; per Sober they pre-select the branch but do not block STEP 1, so I did not hold the runbook
  waiting for them.
- Also flagged to the owner that Sober found the real tell: the menu-switch call is wrapped in a `try/catch` that
  swallows the error — which is why the link can succeed while the menu silently stays put. Business meaning relayed:
  "the bot can look half-broken while nothing in the code is actually wrong" — worth a follow-up REQ (make that
  failure visible) **after** the environment is fixed; I am NOT opening it now to keep the runbook clean.

**Ball: owner** (run STEP 1, send output) → then @Sober picks the branch.

## [--:--] Porter (PM) — owner asks to whitelist a new IP (`49.237.170.101`) for DB access → @Sober (authoritative steps)
Owner: *"ฉัน access database ไม่ได้ 49.237.170.101 ช่วยสอนฉันเพิ่ม ip นี้เข้าไปให้มีสิทธิ์"* — he has changed machines, so
this is a **new client IP** needing DB access (the old whitelisted one was `110.171.40.169`, and that temp-open line on
prod was closed 2026-08-11).

**Porter is NOT authoring this.** Opening `pg_hba` to a remote IP is a **security loosening on a real environment** —
exactly the step **@Sober refused to author on 2026-08-10** without the human's explicit go-ahead, and he then removed
the need for it entirely with `smart-scheduler/DEPLOY-CUSTOMER-PROD-ON-SERVER.md` (run DB ops **on the server against
`localhost`**; prod's `pg_hba` never opened). That precedent stands and I relayed it as the recommended path.

What I did instead:
- **Asked the owner two blocking questions** before anything runs: **(a) which server** — UAT or customer-prod? (prod is
  live customer data; UAT is not) — and **(b) confirm `49.237.170.101` is his own machine's public IP**, not the DB host
  he is trying to reach. The two readings need opposite fixes.
- **Pointed out he may not need this at all right now:** REQ-042's STEP 1 (`bun run line:inspect-menus`) is run **on the
  UAT server** with UAT's `.env`, so it needs no remote DB access from his laptop. Opening a DB to the internet to run a
  command that doesn't need it is the wrong trade — the LINE runbook should not be blocked on this.
- **Relayed, clearly labelled as recorded history and not as my instruction**, the procedure this team actually executed
  on prod on 2026-08-11 (log/2026-08-11): a single-host line `host all all <IP>/32 scram-sha-256` in the `pg_hba.conf`
  reported by `SHOW hba_file;` → `SELECT pg_reload_conf();` → verify via `pg_hba_file_rules` → **and close it again after**.
  Stressed: never `0.0.0.0/0`, `/32` only, and it is temporary by design.

**@Sober — please own this one.** If the owner confirms he genuinely needs remote DB access from `49.237.170.101`, write
the authoritative steps for **that specific server** (grounded, copy-paste, incl. the close step and how to verify it is
closed), or — preferred, per your own 2026-08-10 call — the run-on-server alternative that makes it unnecessary. Fold it
into a runbook artifact rather than leaving it in chat; last time this path lived only in a Porter log entry.

**Ball: owner** (answer a+b; run STEP 1 on the UAT server meanwhile — it needs no DB whitelist) **& @Sober** (authoritative
IP-whitelist / run-on-server steps for the named environment).

## [--:--] Porter (PM) — STEP 1 output is in → 🛑 I am HOLDING STEP 3 (publish = a write to a LIVE OA). @Sober decides
Owner ran STEP 1. Relaying the output verbatim (menu ids only — no token, no userId):

- **Stored ids in the DB:** `(none stored — has line:publish-menus been run against this DB?)`
- **Channel default:** `richmenu-529756f4…0348` → flagged `⚠️ NOT one of our stored ids`
- **Menus that exist on the OA: EIGHT**, named exactly like ours and all flagged `created outside our publish`:
  `parent-th` ×2 (areas=6) · `parent-en` ×2 (areas=6) · `teacher-th` ×2 (areas=2) · `teacher-en` ×2 (areas=2)

**⚠️ Two things Porter will not let pass unexamined — both are for @Sober to rule on, not me:**

1. **Where did this run?** The prompt shows `KUYDONG@DESKTOP-V5GJILT … /h/scheduler/smart-scheduler-back (dong)` — i.e.
   **the owner's own machine**, not the UAT server. So "none stored" describes **whatever DB that shell's `.env` points
   at**, which may not be UAT's DB at all. Before anyone concludes "Cause B", confirm which DB was read. (This also
   explains the earlier DB-access question — he is working from the laptop.)
2. 🛑 **STEP 3 (`line:publish-menus`) is a WRITE to a live LINE OA, and I am holding it.** The channel already carries
   **8** menus with our exact names — meaning our menus HAVE been published against this OA before (by a run whose ids
   were stored in a different DB), and someone has published **at least twice** already. Republishing would create a
   **third** duplicate set **and reset the channel default menu**. If this OA is the **same OA the customer's users are
   on**, that is a change real users can see. **Business risk = not Porter's to accept silently, and not the owner's to
   absorb by surprise.** Sober: is the correct move to publish again, or to **adopt/store the existing ids** so the code
   stops silently no-op'ing? Also: are the 8 menus safe to leave as orphans, or do they need cleanup?

**Diagnostic value of the output (my read as PM, for Sober to confirm/correct):** the OA is clearly *not* empty and the
menus themselves look right (areas 6/6/2/2 match the parent/teacher layouts), while the DB holds **no ids** — which fits
the shape Sober described: the code can't find a target id, `linkRoleRichMenu` silently does nothing, the link completes,
**the menu never switches.** That is the owner's exact symptom.

**@Sober — please rule on: (1) which DB was actually read and whether STEP 1 must be re-run on the UAT server itself;
(2) publish-again vs adopt-existing-ids; (3) what to do with the 8 duplicates; (4) whether the OA behind this shell is
the customer-facing one — if it is, the write needs the owner's explicit, informed go-ahead and a stated blast radius.**

**Ball: @Sober** (the ruling above — STEP 3 stays held until then) **& owner** (still owed: which server/DB this shell
points at + REQ-042 Q1–Q3).

## [Sober (SA)] — REQ-042 ruling on the STEP-1 output + the IP question. Fix = ADOPT ids (TASK-130), not blind publish. → @Porter @Jason
Good hold, Porter — do not publish blind. Ruling on all four points, grounded in the code + the output.

### The diagnosis is now CONFIRMED (not just "shape")
DB has **no** `line_rich_menu_ids`, OA has our 4 menus (×2). So `linkRoleRichMenu` reads an empty map → **no target →
silent no-op** while the link completes → menu never switches = the owner's exact symptom. Corroborated by the
2026-08-11 log: `app_settings` was **"already empty"** on the shared DB before REQ-040. ⇒ this has been silently dead on
**any** environment whose DB lacks the ids — **likely customer-prod too**, not only UAT. (The swallowed `catch` at
`line-webhook.service.ts:462` is why nobody saw an error.)

### Porter's 4 questions
1. **Which DB was read / re-run on UAT?** The shell was the owner's **laptop** (`DESKTOP-…`), not the UAT server — so
   "none stored" is about the laptop `.env`'s DB, which may not be UAT's runtime DB. **Re-run STEP 1 on the UAT server
   itself** (or confirm the laptop `.env` `DATABASE_URL` **and** `LINE_CHANNEL_ACCESS_TOKEN` are UAT's). The fix must land
   in the exact DB+OA the UAT webhook uses at runtime. (Also answers Q1/Q2 implicitly: the token → which OA, the URL → which DB.)
2. **Publish vs adopt → ADOPT.** Since the OA already has the 4 correct menus, we do **not** need to republish. Cut
   **TASK-130 (`line:adopt-menus`, Jason)**: read the OA, upsert the existing ids into `app_settings` — **zero OA write,
   no default reset, no new duplicates.** It fixes the exact bug (the switch just needs the ids in the DB) and is safe
   **regardless of (4)**, so it doesn't wait on the customer-OA question. `publish-menus` stays a fallback **only** for a
   fresh, isolated UAT OA where you also want the default (re)set — never against the customer OA.
3. **The 8 duplicates = harmless orphans.** LINE allows many menus per OA; an unlinked/non-default menu does nothing. No
   delete helper exists today; cleanup is optional, non-blocking, and can be a tiny later script if the clutter matters.
4. **Is it the customer-facing OA?** Still owner's to confirm (Q1) and it matters for blast radius — but the ADOPT fix
   needs no OA write, so it's safe to proceed **before** the answer. What the answer changes: **if UAT is sharing the
   customer OA, that is itself a misconfiguration** (a UAT env should have its own channel) worth fixing properly — flag
   it, don't paper over it. If it IS the customer OA, note that adopting also fixes prod's silent breakage — a real win,
   still zero OA write.

**Net:** STEP 3 blind-publish stays **held** (agree). The path forward = Jason builds TASK-130 → owner runs
`bun run line:adopt-menus` **on the UAT server** → STEP 4 re-verify (AC-1..6). SPEC-038 updated with STEP-1 findings +
the adopt-vs-publish decision.

### The IP-whitelist request (`49.237.170.101`) — my ruling
- **You do NOT need it for REQ-042.** STEP 1 and `line:adopt-menus` both run **on the UAT server against localhost** — no
  remote DB whitelist required. Do not loosen `pg_hba` to unblock the LINE work; that's the wrong trade (Porter's read is right).
- **Preferred path stays run-on-server** (localhost), per my 2026-08-10 call and `DEPLOY-CUSTOMER-PROD-ON-SERVER.md` —
  prod `pg_hba` never opened. RDP/SSH to the box and run DB commands there.
- **I will not pre-author a prod-loosening runbook.** If the owner genuinely needs remote DB access, I need his answers
  to Porter's two blockers first — **(a) which server (UAT or customer-prod?)** and **(b) confirm `49.237.170.101` is his
  own machine's public IP** — then I fold the exact `/32` temp-open + **close-and-verify** steps for that **named**
  environment into a proper runbook artifact (never `0.0.0.0/0`, temporary by design). Not before those two facts.

**Ball: owner** (re-run STEP 1 **on the UAT server** + answer Q1–Q3 and the IP a/b) **& @Jason** (TASK-130 `line:adopt-menus`).
Once TASK-130 lands, owner runs adopt on UAT → STEP 4 re-verify.

## [--:--] Porter (PM) — relayed Sober's ADOPT ruling to the owner; 🔴 escalating the prod implication as a business item
Relayed to the owner in Thai: the hold on blind-publish stands, the fix is **adopt the ids that already exist on the OA**
(TASK-130, zero OA write, no new duplicates, no default reset), his three answers + the STEP-1 re-run **on the UAT server**
are what unblocks the rest, and the IP whitelist is **not needed** for any of this.

🔴 **The part I am NOT letting stay a technical footnote — Sober's finding that this is probably broken on customer-prod
too.** If `app_settings.line_rich_menu_ids` is empty on prod's DB as well (the 2026-08-11 log records `app_settings` as
"already empty" on the shared DB), then **every real parent/teacher on the customer OA has silently never had their menu
switched** — on role change and on language toggle — since the menus were published. In business terms: a user who
switches to English keeps a Thai menu; a parent who becomes a teacher keeps the parent menu. Nobody reported it because
the failure is swallowed and the *default* menu still shows, so the OA looks fine.
- Told the owner plainly, and told him the check is **read-only** (`line:inspect-menus` run on the prod server) — the same
  command he just ran, pointed at prod. No write, no deploy, no customer impact.
- **Not opening a REQ for it yet** — it is the same defect as REQ-042, just a second environment. If prod's inspect comes
  back empty, the honest move is to widen REQ-042's scope (or cut a sibling REQ) and run the same ADOPT there, and I'll
  say so at that point rather than pre-emptively creating paperwork.
- Consequence for the record: **REQ-015's "DELIVERED" claim covers only what was tested on `sid`** — the menu-switch leg
  has never actually been exercised on prod. Flagging that in the open rather than leaving the delivery story overstated.

**Ball: owner** (STEP 1 re-run on the UAT server · Q1–Q3 · IP a/b · optional read-only prod inspect) **& @Jason** (TASK-130).

## [--:--] Porter (PM) — 🔴 Q1–Q3 answered: "UAT" and "customer-prod" are the SAME server. Cause B CONFIRMED. → @Sober
Owner's answers (now recorded in REQ-042 `## Questions` as Q1/Q2/Q3 + a new Q5):

1. **Where STEP 1 ran — resolved.** He added his IP on the server, pointed the shell at **UAT's database**, and ran it
   there. ⇒ `(none stored)` is a **real fact about UAT's DB**, not a local-`.env` artifact. **Cause B CONFIRMED.**
2. **The OA inspected IS UAT's OA.** ⇒ the 4 menus (×2) on it are UAT's own.
3. **Webhook → `frontoffice.develyst.online`, and the owner states that host IS the UAT server.** ⇒ **Cause C ruled out.**
4. **One build only**, i.e. the 2026-08-11 deploy ⇒ it **contains TASK-046** ⇒ **Cause D ruled out.**

### 🔴 The finding that matters more than the bug: our environment naming was wrong
`frontoffice.develyst.online` is called **"customer-production"** in the board's ENVIRONMENTS section, in
`PROJECT-STATUS.md`, and throughout the 2026-08-11 deploy log. The owner's model is **two servers: `sid` and UAT**, and
**UAT *is* `frontoffice.develyst.online`.** So "UAT" and "customer-prod" are **one box**. Nothing about the box changed —
only our label. Consequences, all of which I have now written into the board's ENVIRONMENTS block:
- **My earlier escalation ("this is probably silently broken on prod too") collapses into REQ-042** — there is no second
  environment to fix. The menu-switch has been dead **on the customer-facing box**, which is the one being fixed. TASK-130's
  adopt therefore fixes the customer-facing environment directly, still with **zero OA write**.
- Every deferred housekeeping row that says "prod" (`pg_hba` temp-open, QA `QA-prod-*` residue) refers to **this same
  server** — no separate cleanup exists elsewhere.
- ⚠️ **New security item I own the reminder for:** the owner has just opened a **remote DB whitelist for his own machine**
  on that server. It is temporary by intent and **must be closed + verified closed** when REQ-042's work finishes — the
  board still lists the 2026-08-11 temp-open as unclosed, so this is the second one. I will not let it quietly persist.
- Still open as a *question*, not an assumption: whether **real customer users** (parents/teachers) are actively on that
  OA today. It does not gate TASK-130 (no OA write), but it decides the blast radius of anything that ever touches the
  OA's menus or default — I am asking the owner rather than inferring it.

**@Sober — all four of your branch questions are now answered: B confirmed, C and D ruled out, and the environment the
fix lands on is the customer-facing one. Nothing in your ruling changes as far as I can see (adopt is still zero-write),
but the blast-radius framing in SPEC-038 was written on a "UAT ≠ prod" assumption that is now false — please re-read it
with that identity and correct the SPEC if it matters.**

**Ball: @Jason** (TASK-130 `line:adopt-menus` — nothing blocks it now) **& @Sober** (re-read SPEC-038's blast radius under
the corrected environment identity) **& owner** (one question: are real customer users on that OA today?).

## [Sober (SA)] — SPEC-038 reconciled to the "one box, customer-facing OA" identity. Ruling UNCHANGED: adopt is the fix. → @Porter @Jason
Re-read SPEC-038 under the corrected identity (UAT = `frontoffice.develyst.online` = the box we'd been calling
customer-prod; one OA, customer-facing). **My ruling does not change — it gets stronger.**
- **Cause B is the confirmed and only live cause** (C and D ruled out by the owner's answers; A collapses since it's the
  same OA). Corrected SPEC-038 STEP-1 findings to "resolved," marked **STEP 2 (webhook/build) RULED OUT**, and struck
  **STEP 3b (publish) as 🚫 not-for-this-environment** — there is no isolated OA, so a publish/set-default would change
  what real followers see. **STEP 3a ADOPT (TASK-130) is the fix, full stop.**
- **Adopt is *more* clearly right now, not less:** it's zero-OA-write, so it's safe on the customer-facing OA
  **regardless** of whether real users are on it today. Porter's open question to the owner (are customers active on the
  OA?) only ever gated an OA *write* — adopt is not one. So it does not block TASK-130.
- **This fixes the customer-facing box directly.** The menu-switch on role change **and** the language-toggle re-link
  (`line-webhook.service.ts:500`) have been silently dead there since the menus were published, because the DB never held
  the ids. No second environment to fix — Porter's earlier "probably broken on prod too" correctly collapses into this one REQ.
- **Security note I'm seconding, not owning:** the owner's remote-DB whitelist (`49.237.170.101`) on that server is a
  temp opening on the customer-facing host and **must be closed + verified closed** when REQ-042 is done — same discipline
  as the still-unclosed 2026-08-11 temp-open. Porter owns the reminder; I'm on record that STEP 1 / adopt both run
  server-side against localhost and never needed it.

SPEC-038 and the board reflect this. Nothing further blocks the build.

**Ball: @Jason** (TASK-130 `line:adopt-menus` — go; run against the OA + DB on that server, upsert the 4 ids, zero OA
write) **& owner** (Porter's one question: real users on the OA today? — informational, doesn't block; and close the DB
whitelist when done). After TASK-130 lands → owner runs `line:adopt-menus` on the server → STEP 4 re-verify (AC-1..6).

## [--:--] Jason (BE) — TASK-130 `line:adopt-menus` BUILT → REVIEW. Zero OA write, evidence below. → @Sober
Session opened on the owner's new machine; startup ritual done (PROTOCOL + BE.md + board + this log + the
2026-08-11 log). Picked up **TASK-130** (the only thing waiting for BE), built it against SPEC-038 STEP 3a.

- **New:** `scripts/line-adopt-menus.ts` — `bun run line:adopt-menus`. Token guard → `listRichMenus()` →
  pure `selectMenuIds()` → **missing canonical name ⇒ print what's missing and exit 1 having written nothing**
  → else ONE upsert of the four ids → print them. Duplicate names (the OA has 2 of each): **last occurrence in
  `/richmenu/list` order wins**, documented in the comment; re-runs pick the same ids ⇒ idempotent.
- **New:** `src/lib/line-adopt-select.test.ts` — 4 tests (all four present · duplicate-name determinism +
  re-run stability · missing name · foreign/OA-Manager menus ignored).
- **Modified (6 lines):** `src/lib/line-rich-menu.ts` — extracted the existing upsert into exported
  `storeMenuIds(ids)`, `publishRichMenus` now calls it. Chose the task's "reuse a shared helper" option so
  `MENU_IDS_KEY` + the `onConflictDoUpdate` stay in one place. No behaviour change to publish.
- **Modified:** `package.json` — the `line:adopt-menus` entry alongside the other `line:*`.
- **Evidence:** `bunx tsc --noEmit` **0** · `bun test` **462 pass / 0 fail** (59 files) · grep proof —
  `createRichMenu|uploadRichMenuImage|linkRichMenuToUser|setDefaultRichMenu` = **no matches** in the script.
  Imports are only `listRichMenus`, `storeMenuIds`, the 4 menu defs and the `MenuIds` type.
- ⚠️ **I did not execute the script.** Its guard reads `LINE_CHANNEL_ACCESS_TOKEN` and Bun auto-loads the
  checked-in `.env` (the PROTOCOL trap I self-reported on 2026-08-02) — so even a "it'll just exit" run would
  reach a real OA + DB. The runtime half of the DoD is the owner's STEP 3a run on the server, by design.
- **One flag for STEP 4 (in the TASK's `## Questions`):** adopt does **not** set the channel default, and the
  owner's STEP-1 output showed the default is not one of our ids. So post-adopt `line:inspect-menus` will show
  the 4 ids `FOUND` **but the default line may still read `⚠️ NOT one of our stored ids`** — expected, not a
  failed adopt. Making the default ours would be an OA write on the customer-facing channel; not mine to fold in.
- **Housekeeping for the board (@Sober):** § Project info still gives the monorepo root as
  `C:\Users\Admin\develyst\smart-scheduler`; on the owner's current machine it is **`H:\scheduler`**. Your
  artifact, so flagging rather than editing.
- Changes are **uncommitted** on `dong` (4 files) — deliberately, so a commit doesn't race your review.

**Ball: @Sober** (review TASK-130 + answer Q1 + the board path fix) → then owner runs `bun run line:adopt-menus`
on the server → STEP 4 re-verify. Also still mine and untouched, awaiting your review from 2026-08-04:
**TASK-095** (REVIEW). TASK-103/110/116/117 stay TODO — they wait on owner sequencing / confirms, not on me.

## [Sober (SA)] — TASK-130 REVIEW = **PASS ✅ (reproduced)** · Q1 answered · TASK-095 was stale-not-owed · board path fixed → @Jason @Porter
Reviewed TASK-130 against SPEC-038 §3a + DoD — reproduced, not trusted.
- **Reproduced myself:** `bunx tsc --noEmit` = **0** · `bun test src/lib/line-adopt-select.test.ts` = **4/0** · full
  `bun test` = **462 pass / 0 fail** (the shared `storeMenuIds` extraction caused no regression).
- **Zero-OA-write confirmed:** grep for `createRichMenu|uploadRichMenuImage|linkRichMenuToUser|setDefaultRichMenu` in
  `scripts/line-adopt-menus.ts` → **no matches**. Read + one upsert, as specced.
- **Selector correct:** `NAME_TO_KEY` built from the `RichMenuDef.name`s themselves (can't drift from publish);
  duplicate-name → **last wins, deterministic + idempotent** (tested); missing name → `main` **exits 1 writing nothing**
  (no half map); foreign menus ignored. `storeMenuIds` extracted surgically, `publishRichMenus` unchanged. **DONE.**
- **Not executed = correct** — running it hits the real OA/DB via the checked-in `.env`; that's the **owner's STEP 3a**
  on the server. Told Jason to commit `dong` on the owner's word (he held it to avoid racing the review).

**Jason's Q1 (default menu) — answered, doesn't block.** REQ-042 is the **per-user** switch (`linkRichMenuToUser`), which
adopt fixes. The channel **default** only affects an unlinked follower, and the owner's STEP-1 output showed that default
already has correct areas — so a default that isn't "one of our stored ids" is **bookkeeping, not a failure.** Adopt must
NOT set-default (that's a customer-visible OA write). **Corrected SPEC-038 STEP 4.1** so the re-verify reads "4 ids FOUND;
default line may still say ⚠️ NOT one of ours" as the **expected pass**. Making the default owned is a separate,
owner-gated `setDefaultRichMenu` — not this REQ.

**TASK-095 — not an owed review, a stale board marker.** The task file already reads `Status: DONE (Sober 2026-08-04 —
rework verified)` with a full `## Review` (I ran tsc 0 · 421/0 then). Only the board **headline** still said "REVIEW —
rework fixed" while its own body said "✅ DONE Sober 2026-08-04." Reconciled the headline to DONE; no re-review needed.
(Good instinct to re-flag it, Jason — but it was recorded, just not surfaced.)

**Board path fixed:** § Project info monorepo root updated `C:\Users\Admin\develyst\smart-scheduler` → **`H:\scheduler`**
(owner's current machine).

**Net REQ-042 status:** SPEC-038 done · TASK-130 **DONE** (the fix is built) · the only thing left is **operator + verify**:
owner runs `bun run line:adopt-menus` on the server → STEP 4 re-verify AC-1..6 → then close REQ-042 + close the DB
whitelist. No engineering work remains.

**Ball: owner** (run `bun run line:adopt-menus` on the server, then STEP 4 re-verify; and close the temp DB whitelist when
done) **& @Jason** (commit `dong` on the owner's word — TASK-130's 4 files). SA queue clear.

## [Sober (SA)] — ROUTING CORRECTION on my own last entry: the ball is NOT mine to hand to the owner. → @Porter
Correcting myself: the entry above ended "Ball: owner …", which is a chain violation — **SA never routes to the human
directly; everything to/from the owner goes through you (Porter).** Withdrawing that. What I actually have is a **handoff
to you**, and you decide what reaches the owner.

**REQ-042 — engineering is complete; it's now yours to drive with the owner:**
- SPEC-038 done · **TASK-130 `line:adopt-menus` = DONE** (SA-reviewed: tsc 0 · full suite 462/0 · zero-OA-write confirmed).
  The fix is built. Nothing engineering-side remains.
- **What the owner needs to do (please relay):** run **`bun run line:adopt-menus`** on the server (the same box/env he
  ran STEP 1 on) → then **STEP 4 re-verify** (AC-1..6: `สมัคร`→ผู้ปกครอง switches to the 6-cell parent menu, reverse
  direction, negative path, no dead taps, regressions). On green → REQ-042 can close.
- **STEP 4.1 caveat to pass on:** after adopt, `line:inspect-menus` will show the 4 ids `FOUND`, but the channel-default
  line may still read `⚠️ NOT one of our stored ids` — **that is expected, not a failure** (adopt doesn't touch the
  default; REQ-042 is the per-user switch).
- **Housekeeping (yours to track with the owner, not blocking):** the temp DB whitelist (`49.237.170.101`) on that
  customer-facing host should be **closed + verified closed** once REQ-042 is done — you already own that reminder.
- **@Jason** may commit the TASK-130 `dong` changes (4 files) on the owner's word — relay whenever the owner's ready.

**Ball: @Porter** (relay the owner's `adopt` run + STEP 4 re-verify; then close REQ-042 and the whitelist). SA queue clear.

## [--:--] Porter (PM) — TASK-130 is built; relayed the owner's run + STEP 4 re-verify (in Thai). REQ-042 now waits on the operator only
Noted Sober's routing correction — accepted, and it's the right call: the handoff came to me and I decide what reaches
the owner. Relayed to him in Thai:
- **`bun run line:adopt-menus` — run on the server**, the same box/env he ran STEP 1 on. What it does in his words:
  reads the ids off the OA, writes them into the DB once, touches nothing on LINE.
- **STEP 4 re-verify (AC-1..6)** as the actual proof: `สมัคร` → ผู้ปกครอง must land him on the **6-cell parent menu**,
  reverse direction, wrong-input path, no dead taps, plus the language toggle (which has been silently dead too).
- **Passed on Jason's caveat verbatim**, because it is exactly the kind of thing that makes an owner think the fix
  failed: after adopt, `line:inspect-menus` shows the **4 ids FOUND** but the **channel-default line may still read
  `⚠️ NOT one of our stored ids` — that is expected**, not a failure. Owning the default would be a customer-visible
  OA write and is deliberately not in this REQ.
- **Commit:** Jason held the 4 `dong` files so as not to race the review; told the owner it's his word to give.
- **Whitelist:** repeated that the temp DB opening (`49.237.170.101`) on the customer-facing host closes when this is
  done — I am tracking it, and the 2026-08-11 one is still listed unclosed, so that's two.

**Not doing:** I am **not** marking anything DELIVERED off the team's report. REQ-042 closes on the owner's own STEP 4
result. Note this project's Tester (Tanya) is not in this loop — REQ-042 has been an owner-run operator task from the
start; if his STEP 4 comes back ambiguous, the honest next move is to route it to her rather than call it green myself.

**Ball: owner** (run adopt on the server → STEP 4 re-verify → report back; say the word on the commit; answer the open
"real users on the OA?" question when convenient).

## [--:--] Porter (PM) — ADOPT RAN CLEAN on the customer-facing box. Ids stored + verified. STEP 4 (the real proof) still pending. → @Sober one question
Owner ran `bun run line:adopt-menus` then `line:inspect-menus`. Result, relayed as evidence not as a pass:

- **Adopt output:** all four stored — `parent-TH`, `parent-EN`, `teacher-TH`, `teacher-EN` — with the script's own
  closing line **"No menu was created, linked, deleted, or made default."** The zero-OA-write promise held **in practice**,
  not just in Sober's grep.
- **Inspect now shows the 4 ids `FOUND` with their real definitions:** parent menus 2500×1686, **areas=6**
  (`checkin · leave · children · register · lang · help`), teacher menus 2500×843, **areas=2** (`schedule · lang`), all
  `action.type=postback` — i.e. the ids we adopted are genuinely the right menus, not same-named strangers.
- **Channel default still reads `⚠️ NOT one of our stored ids`** — **expected**, exactly as Jason flagged and Sober
  ratified. I pre-warned the owner so he wouldn't read it as a failed run.
- The 4 adopted ids now print **without** the `created outside our publish` warning; the other 4 duplicates still carry it
  — consistent with "we adopted one of each pair, the rest are harmless orphans."

**🔴 STEP 4 has NOT been run yet, and REQ-042 stays open.** Storing ids is the mechanism; the requirement is that a real
role change switches a real user's menu. Nothing is `TEST_PASSED` and nothing is `DELIVERED` on the strength of a
successful script run — that is precisely the "SPEC_DONE means built, not working" trap PROTOCOL warns about.

**@Sober — one question before the owner reads a failed STEP 4 as a failed fix:** the ids were written to the DB from the
owner's shell, but **the LINE webhook is served by the long-running process on that box**. Does `linkRoleRichMenu` read
`app_settings.line_rich_menu_ids` **per call**, or is the map cached in process memory at startup? If it is cached, the
running server will keep no-op'ing until it is restarted, and the owner's STEP 4 would "fail" for a reason that has
nothing to do with the fix. If a restart IS needed, say so plainly (and whether `pm2 restart sched-back` alone is enough)
— I'd rather hand him one extra step than have him conclude the work was wasted.

**Ball: owner** (STEP 4 in real LINE — `สมัคร` → ผู้ปกครอง → 6-cell menu, reverse direction, wrong-input path, language
toggle, no dead taps) **& @Sober** (the cache/restart question above — answer before he re-tests if you can).

## [--:--] Porter (PM) — 🟢 REQ-042 fix WORKS on the owner's own run · new REQ-043 from his booking-modal question
**STEP 4 result (owner, in real LINE):** role change works for **both** directions — teacher ✅ and parent ✅ — and
**no dead taps**. Combined with the adopt/inspect output (4 ids stored, correct defs, script's own "nothing created,
linked, deleted or made default"), the REQ-042 mechanism is proven end-to-end on the customer-facing box.

**I did NOT mark it DELIVERED.** Two ACs were never reported: **AC-4** (wrong input mid-`สมัคร` → clear message, menu
unchanged, retry) and the **language-toggle re-link** half of AC-6 — which matters because that leg was silently dead
for the same reason and is the one a real user hits most. Board row now reads **FIXED & OWNER-VERIFIED, not DELIVERED**,
with the two gaps named. Offered the owner the choice: confirm those two himself, or I route it to **@Tanya** for the
formal verdict. Also still open before close: the temp DB whitelist (`49.237.170.101`) closed **and verified closed**.

### New requirement from the owner's screenshots → REQ-043 (READY_FOR_SA)
He asked why the New-booking modal picks a student **two different ways**: Trial/Single = one combined
`search / add new` control; Weekly course/Voucher = a search box **plus** a separate `Select a student with a course`.
- Wrote **`requirements/REQ-043-booking-modal-one-student-picker-across-tabs.md`** — one picker, same on all four tabs,
  **presentation only**: inline add-new on Trial/Single, eligibility-only lists on Course/Voucher, and REQ-029's
  distinguishable-entitlements result are all written in as **regressions**, not things to "clean up".
- **Traceability worth stating: this is the unfinished half of REQ-029 item 3**, which asked for Course/Voucher search
  *"consistent with the Trial/Single picker"*. The search shipped; the consistency didn't. The owner noticing it is us
  being caught, not a new whim — recording it that way so the same half-delivery is visible next time.
- Q2 to Sober: if the split exists because the option identity is an **entitlement** rather than a student, say so and
  I re-decide the trade in the open rather than letting AC-3 (two courses must stay distinguishable) quietly regress.
- Owner-approved TH/EN placeholder + empty-state wording is in the REQ (UX-writer hat), so nobody drafts copy.

⚠️ **Board hygiene, self-reported:** while inserting the REQ-043 row I overwrote **REQ-036's row** (line numbers had
shifted under me after Sober's edits). Caught it on the row-count check, recovered REQ-036 verbatim from git, and
re-verified that **no REQ row present in HEAD is missing**. Nothing else was touched.

**Ball: @Sober** (REQ-043) **& owner** (AC-4 + language toggle on LINE, or say the word and I hand REQ-042 to Tanya ·
close the DB whitelist · give Jason the go-ahead to commit TASK-130's 4 files).

## [Sober (SA)] — cache Q answered · REQ-043 → SPEC-039 + TASK-131 (Fern), Q2 answered · zero-BE, zero-behaviour → @Porter @Fern
Two things.

### 1. Porter's cache/restart question (REQ-042) — answered from code
**No restart is needed; the read is per-call.** `linkRoleRichMenu` → `getMenuIds()` does a live
`db.query.appSettings.findFirst(...)` on **every** call (`line-rich-menu.ts:129-135`) — nothing is cached in
process memory at startup. So the ids the owner adopted are picked up on the very next `สมัคร`/toggle with no
`pm2 restart`. This is confirmed empirically too: the owner's STEP 4 worked without a restart. (If it had ever
mattered, `pm2 restart sched-back` would have sufficed — but it doesn't here.)

### 2. REQ-043 picked up → SPEC-039 + TASK-131 (Fern)
Grounded in the real modal (`BookingModal.tsx` `CreateForm`) before speccing.
- **Q2 answered (in the REQ):** the split is not arbitrary — (1) the option identity **is an entitlement**
  (`courseId`/`voucherId`), one student = several rows (AC-3), and (2) the search is **server-side** because it
  matches **parent phone**, which a Mantine local `searchable` can't (the TASK-088/REQ-024 fix). **But neither
  forces two boxes:** the app already has the unified pattern — `StudentSelect` is one `Combobox` that fires a
  **server** search (`useStudentSearch`) as you type. The Course/Voucher picker just needs the same against
  `useEligibleStudents(type, enabled, q)` — which already takes a server `q`. **So there's no trade to
  re-decide; unification is free.**
- **The fix (SPEC-039 / TASK-131, Fern):** new sibling `EligibleStudentSelect` (one Combobox → server
  `useEligibleStudents`, entitlement rows via the existing `entKey`/`eligibleLabel` lifted to shared, no
  add-new, explicit empty/no-match states), swap the two-box `TextInput`+`Select` branch for it, align wording.
  **FE-only, zero BE change, zero payload/behaviour change** — every AC-3..6 item is written as a
  regression-to-preserve. Fern self-runs `hallmark audit` before REVIEW (§4).
- No dependency; it's a single task. REQ-043 set **IN_SPEC**.

**Ball: @Fern** (TASK-131 — build it; self-audit before REVIEW → SA UI-lens review) **& @Porter** (the cache
answer above closes your REQ-042 question; REQ-042 itself is yours to drive to DELIVERED with the owner — AC-4 +
language-toggle re-verify, then close the DB whitelist). SA queue clear after handing 131 to Fern.

## [--:--] Porter (PM) — 🛑 HOLD on REQ-043 / TASK-131 (owner's word) + the "จองคอร์สมีไปทำไม" question is BACK. → @Sober
**@Sober — the owner has explicitly said "อย่าเพิ่งแก้" — please HOLD REQ-043 and do not let TASK-131 start.** SPEC-039
and TASK-131 stand as written (your Q2 answer is good and I've read it: unification is free, zero BE change, AC-3
survives) — this is not a rejection of the design, it is the owner pausing the *screen* until a bigger question about
that screen is settled. Nothing to redo; just don't ship it yet.

### What he's actually asking — and it is the same question he asked before REQ-030
Looking at a plan modal (`แผนของ pole`, 6-session course, ลา 7/2, สิ้นสุด 18 Oct 26), he asks: the course plan **never
leaves a hole** — he tried marking leave, he tried cancelling, and the session **just regrows at the end** — so **what
is the "คอร์ส / Weekly course" tab on the New-booking modal even for?**

**He is right about the behaviour, and it is by design, not a bug** (REQ-030's model, quoted from the REQ): a course is
**a fixed number of sessions owed**; an absence or a cancel **moves** a session to the tail, it never removes it — "the
appended session and the inserted session are **the same session, moved**… never changes to that number." So a plan is
always full by construction.

**And our own records already answer the tab question — he asked it once before.** REQ-030's Analysis says verbatim:
*"The owner asked 'จองคอร์สมีไปทำไม' about that tab; **this REQ is its answer**: it is the mechanism for adjusting a
plan, and it is what REQ-025's migration will use to place a mid-course child's remaining sessions."* i.e. the Course
tab = **book an insert / make-up on a chosen day+teacher**, which pulls the tail session earlier — it is not "buy a
course" and not "add a session". REQ-030 §4 deliberately kept **two entry points, one behaviour**: the calendar and the
plan screen. The plan modal's `แทรกคาบชดเชย` button in his screenshot is the *other* entry point to the same action.

### My read as PM/BA/UX — this is a NAMING failure, not a redundant feature
The tab is labelled **"คอร์ส / Weekly course"**, which reads as *"สมัครคอร์ส"*. Its real job is *"แทรกคาบชดเชยให้เด็กที่มี
คอร์สอยู่แล้ว"*. The owner — the person who commissioned it — could not tell what it was for **twice**; staff have no
chance. That the answer exists in REQ-030 is not a defence: **if the screen needs a REQ to explain it, the screen is
wrong.** My recommendation is (c) below.

Options I am putting to the owner (his call, business-side, so mine to frame — not to decide alone):
- **(a) Keep both entry points as designed** (status quo; REQ-030 §4 wanted the calendar entry because staff work from
  the calendar and want to drop a make-up onto a visible free slot).
- **(b) Remove the Course tab** — all inserts go through the plan modal only. Cheapest to explain, but takes the
  make-up action away from the calendar where staff actually are, and REQ-025's migration path leans on it.
- **(c) ⭐ Keep it, rename and reframe it** so the tab says what it does (insert a make-up for an existing course), with
  the wording written by me. Fixes the confusion without losing the entry point.

**No REQ written yet — deliberately.** The owner asked to talk before paperwork, and the right artifact depends on his
answer: (c) is a small UX/copy REQ that REQ-043 should absorb (same screen, one FE pass); (b) is a scope removal.

**Ball: owner** (pick a/b/c) → then me (write it up; REQ-043 resumes with it). **@Sober: hold only — no rework needed.**

## [--:--] Porter (PM) — owner picked **option (c)** → REQ-044 written; REQ-043 HOLD LIFTED. → @Sober
Owner's decision on the "จองคอร์สมีไปทำไม" question: **(c) keep the tab, rename and reframe it.** Option (b) (remove
the tab) is **rejected** — recorded in REQ-044's Out of Scope so nobody re-opens it in three weeks.

- Wrote **`requirements/REQ-044-booking-modal-course-tab-says-what-it-does.md`**, READY_FOR_SA. Tab becomes
  **`แทรกคาบชดเชย` / `Make-up session`**, with an **always-visible** explainer line (not a tooltip — a tooltip is
  where explanations go to die) saying it is for a student who already has a course and that **the total number of
  sessions does not change**, plus a confirmation that names the plan's **new end date** so the effect is visible
  without opening the plan modal.
- **All TH/EN strings are in the REQ** — tab, explainer, save button, confirmation. Engineers draft no copy.
- **Wording-only, and I said so three ways** (Requirement 4, AC-4/AC-5, Constraints): eligibility, what gets written,
  quota, and the plan modal's own `แทรกคาบชดเชย` button are untouched, and REQ-030 §4's "two entry points, one
  behaviour" is preserved. Any proposal that changes the *action* stops and comes back to me.
- Added Requirement 5 on my own judgement as UX writer: review the **other three tab labels** for the same failure —
  in particular that **"คาบเดี่ยว / Single session" is REQ-037's paid extra session**, which is the honest answer for
  a family who wants a lesson *beyond* what they bought. Correct only what misleads; leave what's already right.
- **Two questions back to the owner, both wording, neither blocking spec work:** confirm `แทรกคาบชดเชย` is the word
  his staff actually use (their vocabulary beats mine), and whether the confirmation should also name which session
  moved (my lean: no — the end date is enough; more text on a routine action is noise).

**@Sober — REQ-043's HOLD is lifted; REQ-044 joins it.** They are separate requirements on **one FE surface**, so they
will collide in the same files — **whether they ship in one pass is your call, not mine** (build order is yours; I'm
only flagging the collision). REQ-043's SPEC-039/TASK-131 stand as specced.

**Ball: @Sober** (REQ-044 + resume REQ-043) **& owner** (2 wording confirms · still owed from earlier: LINE AC-4 +
language toggle or hand REQ-042 to Tanya · close the DB whitelist · green-light Jason's TASK-130 commit).

## [--:--] Porter (PM) — customer-meeting intake: 4 new REQs (045–048). One is BLOCKED on a reversed decision. → @Sober
Owner brought four items back from a customer meeting. Written up as four separate REQs (one deliverable outcome each),
all with ACs a tester can execute and all TH/EN wording supplied.

- **REQ-045 — planned absences at course CREATION** (🛑 **BLOCKED, do not spec**). Admins can only *change a date* at
  creation today, so per the owner they mostly don't bother; they want the plan editor's one-click planned absence
  before saving, with the OBS-3 preview. **Blocked because the owner reversed a REQ-030 decision:** he said planned
  absences must **not** consume the leave quota, but REQ-030 Q1 was answered *"ควรสิ"* — they **do** consume it, and
  that consumption is **what earns the extension**; going over simply never locks. I wrote the three readings
  (A/B/C) into the REQ with my lean (**B — free when declared up front at creation, quota still applies later**) and
  put it to him. Speccing now would bake in a guess about what families are owed.
- **REQ-046 — a leave must name the SESSION, not the day.** His own plan modal shows a student with `04/Oct 10:00`
  **and** `11:00`, so "leave on the 4th" is undefined for them, and the guess propagates into what is re-owed, the
  extension, the teacher's calendar, the freelance draw and attendance. **Q1 is deliberately an investigation before
  design** — I would rather publish "we checked, it was already session-level" than have the team build a fix for a
  problem that isn't there. **Q2:** if it is day-level, existing ambiguous records are a **data question for the
  owner**, not something to silently reinterpret.
- **REQ-047 — leave closes 3 h before, as a setting.** ⚠️ Not a new field: REQ-031 already recorded
  `lib/leave-notice.ts` as **FT/PT ≥1h · freelance ≥2h**, so today's cut-off varies by employment type. Asked the
  owner whether 3 h **replaces** that split (my lean: one school-wide number people can state out loud) or stays
  per-type. Wrote in: server-side enforcement, staff keep an admin override, and REQ-031's own rule that a DB row
  **without a settings screen is not "editable"**.
- **REQ-048 — voucher booking must let staff choose the time** (pinned to 09:00 today). A voucher is exactly the
  product used *whenever the family can come*. REQ-029/027 regressions written in as ACs. Asked SA to state whether
  09:00 is a hardcoded default or a missing control — worth knowing before we trust similar defaults on that modal.

**Priority call (PO hat):** 046 and 048 are **HIGH** — 046 because it silently corrupts what a family is owed, 048
because it blocks normal use of a product the school sells. 045 is HIGH but **gated on the owner**. 047 is
MEDIUM–HIGH: it is a commercial rule the customer is actively deciding, and it rides REQ-031's screen.
**Build order remains Sober's** — I am stating value, not sequence. Note 048 lands on the **same FE surface** as
REQ-043/044; three requirements, one screen.

**Ball: @Sober** (046 · 047 · 048 — and REQ-043/044 already with you; 045 is NOT yours yet) **& owner** (REQ-045's
A/B/C quota answer · REQ-047 Q1 · plus still owed: LINE AC-4 + language toggle or hand REQ-042 to Tanya · close the
DB whitelist · green-light Jason's TASK-130 commit).

## [--:--] Porter (PM) — REQ-049 (notify on leave, configurable recipients) → @Sober. Flagged it as untestable-by-default
Fifth item from the same customer meeting, written up as **REQ-049**, READY_FOR_SA.

- **The gap in business terms:** a parent cancels in LINE, the session vanishes from the plan, and **nobody is told**
  — the admin finds out only if they look at the calendar, and the coach can travel to the centre for a student who
  isn't coming. Setting has exactly the two values the owner said: `แจ้งแอดมินอย่างเดียย` → corrected in the REQ to
  `แจ้งแอดมินอย่างเดียว` / `แจ้งทั้งแอดมินและครู`, on **REQ-031's** settings screen (same mechanism as REQ-047 — I am
  not letting a second settings system appear).
- **AC-3 is the trap I wrote in on purpose:** notify the **session's current teacher**, not the course's original
  one. After a REQ-030 plan edit those differ, and the obvious implementation gets it wrong silently.
- Also written in: the notification **never blocks the leave** (AC-4 — a send failure must not cost a family their
  cancellation), exactly one message per recipient (AC-6), and staff-initiated cancels obey the same setting (AC-5)
  so a coach's information doesn't depend on which door the cancellation came through.
- ⚠️ **I flagged this as untestable unless designed for:** it sends to **real teachers**. Tanya's charter forbids
  messaging real recipients, and the board already records the teacher-change dual-LINE case as excluded from
  production testing for exactly this reason. So the SPEC **must** include test recipients / dry-run / log-only.
  Raising it at intake rather than discovering it at test time — that is the cheapest moment for it.
- Three owner questions with my leans (who counts as "the admin" → reuse REQ-023's existing admin channel, don't
  invent a second address book · immediate vs folded into the 08:00 digest → **immediate**, a leave for today's
  10:00 is worthless tomorrow · does a late leave read differently → no, the timestamp says it), and one SA
  investigation (Q4: what is sent **today**, if anything).

**Ball: @Sober** (REQ-046 · 047 · 048 · 049, plus 043/044 — 045 still not yours) **& owner** (REQ-045 A/B/C ·
REQ-047 Q1 · REQ-049 Q1–Q3 · and the still-open LINE AC-4 + language toggle · DB whitelist · TASK-130 commit).

## [--:--] Porter (PM) — two more from the owner → REQ-050 (check-in attribution) + REQ-051 (walk-in QR page). → @Sober
- **REQ-050 — does a check-in land on the right child?** A LINE account is a **parent**, not a child. **Framed as
  money, because it is:** attendance consumes a paid session **and** draws the freelance budget, so a check-in on
  the wrong child takes a lesson from a family that never used one — and both records look perfectly normal
  afterwards. Same failure family as REQ-046 (leave); told Sober he may design them together, which is his call.
  **Q1 is an investigation before design** (what does it resolve today?), and **Q2 deliberately forbids the team
  from querying real data** — if history may contain wrong attributions, Sober hands me the query and I put it to
  the owner as a DATA REQUEST. Wrong check-ins in the past are a **business conversation** (Q3: correct them or
  note them — my lean: correct; a family that lost a lesson never agreed to lose it), not a silent cleanup.
  AC-6 protects the common case: single-child families must gain **no extra step**.
- **REQ-051 — walk-in QR check-in page.** Nannies, grandparents, forgotten phones ⇒ sessions never get checked in.
  Scan a printed QR → phone number → pick child → check in. **I put the security/privacy trade at the top of the
  REQ as an owner decision (Q1), not as a technical footnote**, because it is genuinely his to make: the page has
  **no login** and a phone number **is not a secret** — so (a) a stranger typing a phone could burn a family's
  paid lesson, and (b) if typing a phone lists that family's children, the page becomes a **PII harvester for
  children's names** (this team already fixed one such leak on the pairing path, TASK-047).
  **My recommendation, written as the default: "narrow window, minimum disclosure"** — only sessions inside the
  existing check-in window, **nicknames** not full names, and the **same neutral message** for "nothing right now"
  and "unknown number" (AC-3), so the page cannot be used to test whether a number belongs to a customer. Rate-limit
  behaviour must be **stated in the SPEC**, not inherited from a library default (AC-6). Walk-in check-ins must be
  **distinguishable** in staff views (AC-7), and staff must be able to correct one (REQ-050 AC-5).
  Also asked: does it reuse the existing `/checkin?token=` path (Q4 to SA), and — my lean is **no** — should the
  page double as a leave button (Q2 to owner: one job per screen; leave has its own cut-off rules).

**Intake note (PO hat):** that is **seven** REQs from today's meeting (045–051). I am not pretending they can all be
next. When the owner asks for sequencing I will put value in this order: **050 (money correctness) → 046 (leave
attribution) → 048 (voucher time, blocks selling) → 049 (notify) → 051 (walk-in) → 047 (cut-off setting) → 045
(blocked on him)** — but **build order stays Sober's**, and REQ-043/044 are already in his queue on the same FE
surface as 048.

**Ball: @Sober** (043 · 044 · 046 · 047 · 048 · 049 · 050 · 051 — 045 still withheld) **& owner** (REQ-045 A/B/C ·
REQ-047 Q1 · REQ-049 Q1–Q3 · REQ-050 Q3 · REQ-051 Q1–Q3 · LINE AC-4 + language toggle · DB whitelist · TASK-130 commit).

## [--:--] Porter (PM) — REQ-052: the calendar cell must say what the session IS. → @Sober
Eighth item from the meeting. Today a cell reads `09:00 QA-prod-student` — a time and a name. It does not say the
**program** (Surfskate? Onewheel? Bike/Scooter?) or the **booking type** (trial · single · course · voucher), so
staff open bookings one by one and a coach reading the week can't tell a trial from a course session — different
lesson to prepare, different money.

- Wrote **REQ-052**, READY_FOR_SA. TH/EN labels supplied in **short and full** form (`ทดลอง / คาบเดี่ยว / คอร์ส /
  บัตร`), and I explicitly **forbade inventing abbreviations for program names** — if a name doesn't fit, that is a
  layout problem for the SPEC, not a new vocabulary for staff to memorise.
- **Two constraints I put in because this screen has bitten us before:** (1) type must be **text/label, never
  hue-only** — the FRONTEND-STANDARD rule REQ-041 adopted after the "AI-generated UI" grading; (2) **AC-3 re-asserts
  0 truncated / 0 clipped / no h-scroll at 1440·768·375**, which REQ-041 closed and which adding two fields to a
  narrow weekly cell is the most likely way to re-open.
- **AC-5 guards the real risk:** the grid already carries a **status** legend (ยืนยันแล้ว/มาเรียน/รอยืนยัน/ลา/
  ขยายคาบ/รอย้าย). Type and status must be readable at once and not learnable-as-colour, or we trade one confusion
  for another.
- Asked the owner the honest layout trade up front (**Q1**: if only one fits in a weekly cell, type or program? my
  lean: **type** — it changes what the session means commercially; **Q2**: nickname vs full name, my lean nickname)
  rather than letting the FE discover the space problem mid-build. **Q3 to SA:** does the cell already receive
  program + type from the API, or is this a data-shape change too — that decides whether it is FE-only.

**Ball: @Sober** (043 · 044 · 046 · 047 · 048 · 049 · 050 · 051 · 052 — 045 still withheld pending the owner)
**& owner** (REQ-052 Q1/Q2 plus the queue of answers already listed in my previous entry).

## [--:--] Porter (PM) — REQ-052 answers folded in (dual-colour, nickname) + REQ-053: วิชา must be read-only on a course session
**REQ-052 — owner answered both questions, and expanded Q1 rather than choosing:** neither field is dropped; **type
gets a colour as a SECOND channel** (status = primary colour, type = a smaller/quieter marker), and he offered a
second idea — a **view-mode switch** (`สถานะ > ประเภท` / `ประเภท > สถานะ`) — then delegated the visual design:
*"ไปคิดเอา ทำออกมาให้ดี"*. Q2: **nickname**.
- Folded into the REQ as requirement 6 + **AC-8/AC-9**, with guardrails I am not willing to leave to interpretation:
  the **text label stays** (colour is redundant reinforcement, never the carrier — greyscale/colour-blind readers
  lose nothing), **no status hue may be reused for a type**, nobody has to memorise a colour code, palette comes
  from the existing tokens (no inline hex, per REQ-041), and the legend must name **both** dimensions. His
  `first trial = red / course = dark green` is recorded as an **example**, not a fixed palette — the SPEC picks the
  set and shows me before build.
- **Scoping call I made on his delegation (PO hat), stated in the open:** build the **dual-colour cell now**, and
  **defer the view-mode switch** (recorded in Out of Scope as deferred, *not rejected*). It adds a persisted
  preference, a second visual language and a toolbar control whose value nobody can judge until staff have used the
  simpler version. If they still want it after a week of real use, that is a small follow-up REQ raised from
  experience instead of a guess. "ไปคิดเอา" is permission to design, not permission to build everything at once.

**REQ-053 — new, HIGH.** From his `แก้ไขคาบ` screenshot: the panel offers **วันที่ · เวลา · ครู · วิชา**. The first
three are exactly what REQ-030 exists for; **วิชา is not** — a course *is* a program. His words: the course is fixed
to one activity at creation. Consequences of allowing the edit are all silent: the family's entitlement stops
matching what they bought, and 💰 **REQ-013 (sport share) + REQ-014 (revenue by activity) both read the session's
program** — the same defect class REQ-029 fought on the voucher path. Written in: **server-side refusal (AC-2)**, not
a hidden field; read-only display **plus** a one-line explanation naming the correct alternative (single session /
voucher / new course) so it reads as a rule, not a broken control; and **Q2 forbids the team from querying real
data** — if mixed-program rows already exist, Sober hands me the query and it becomes a DATA REQUEST and an owner
decision, because those rows have been feeding the reports wrong numbers.

**Ball: @Sober** (043 · 044 · 046 · 047 · 048 · 049 · 050 · 051 · 052 · 053 — 045 still withheld) **& owner** (the
standing answer queue: REQ-045 A/B/C · REQ-047 Q1 · REQ-049 Q1–Q3 · REQ-050 Q3 · REQ-051 Q1–Q3 · plus LINE AC-4 +
language toggle · DB whitelist · TASK-130 commit).

## [Sober (SA)] — worked the HIGH-priority specable queue: 048/046/053 spec'd + 050 audited. Grounded each in real code first. → @Fern @Jason @Porter
Ten REQs landed at once — too many to spec well in one pass, and 045/047/049/051 are blocked on owner answers you
already asked for. So I took the **HIGH, specable-now** ones, grounded every one in the actual code before designing
(parallel investigation), and left the rest triaged (below).

- **REQ-048 (voucher time) → SPEC-040 + TASK-132 (Fern).** Q1 answered: **not hardcoded** — the voucher branch just
  never renders the time `<Select>` the other tabs have; `startTime` is wired to the clicked cell. **FE-only** — the BE
  already accepts any in-schema time for a voucher. Fix = render the existing `TIME_SLOTS` Select + read `startTime`;
  teacher stays the clicked column (domain rule intact). REQ-029/027/expiry preserved.
- **REQ-046 (leave→session) → SPEC-041 + TASK-135 (Jason).** Q1/Q2 investigated and answered in the REQ: **already
  session-level on every path** (web + LINE; LINE already shows a tappable per-booking picker when >1). **No day-level
  record exists → no data cleanup.** It only *feels* day-level because the picker label + confirmation don't name the
  session. Fix = enrich LINE label (time·teacher·program) + confirmation names the cancelled session + child-first step
  when ≥2 children have sessions. **BE-only; web already compliant (AC-6).** Advance/bulk leave stays out of scope; Q3
  (undo) non-blocking.
- **REQ-053 (course subject read-only) → SPEC-042 + TASK-133 (FE Fern) + TASK-134 (BE Jason).** Surface = PlanModal
  `SessionEditor`. Fix = FE read-only วิชา on a course-session edit + **BE refuse** a subject change on a COURSE_PACKAGE
  session (`COURSE_SUBJECT_LOCKED`); voucher/single/trial stay editable (AC-4). 🔴 **Architectural finding:**
  `course_packages` has **no** subject column — the program is *derived* from `bookings[0].subject`, so an edit can
  silently flip the whole course's program (the REQ-013/014 corruption class). **DATA REQUEST for you** (owner-run):
  detect existing mixed-program courses — query shape in SPEC-042. A canonical course-subject column = a separate future
  hardening REQ, deliberately not built here.
- **REQ-050 (check-in attribution) → SPEC-043, AUDIT DONE, 2 decisions for you before I cut tasks.** 🟢 The owner's main
  worry is a **non-issue**: the primary LINE button flow is **id-keyed** (tappable picker for >1) and the QR token is
  per-booking — no wrong-child bug where it counts (Q1). Silent history isn't detectable from stored data (Q2). But 3
  real gaps: 🔴 **C (money)** — correcting a check-in does **not** return the consumed `usedSessions`/`usedHours` (never
  decremented) — the priority fix; 🟠 **A** the `qr` keyword picks the first child; 🟡 AC-3 thin LINE copy. **@Porter — 2
  decisions gate the build:** (1) confirm a correction should return the hour (money semantics, money side); (2) REQ-050
  Q3 (historical back-fill?). On your go I cut the Gap-C (BE correctness) + Gap-A/AC-3 (BE LINE) tasks.

### Triage of the rest of the queue (so nothing is lost)
- **Specable next, MED-HIGH (mine, next turn):** **REQ-044** (rename course tab → make-up + explainer) and **REQ-052**
  (calendar cell shows program+type, dual-colour) — both share FE surfaces with in-flight tasks (044 with the BookingModal
  set; 052 needs a colour-token design I show you before build + its Q3 grounded). Not started yet — flagging, not dropping.
- **Blocked on owner (your answer queue, not mine to guess):** REQ-045 (A/B/C), REQ-047 (Q1), REQ-049 (Q1–Q3),
  REQ-051 (Q1–Q3). I won't spec these on assumptions.
- **File-collision note:** REQ-043/044/048 all touch `BookingModal.tsx` (`CreateForm`); 053 touches PlanModal. Fern
  should sequence TASK-131 → 132 → (133) to avoid merge churn on one file — I noted it in each task.

**Ball: @Fern** (TASK-132 voucher-time · TASK-133 course-subject-readonly — sequence after TASK-131 on the shared file)
**& @Jason** (TASK-134 course-subject-refuse BE · TASK-135 LINE-leave BE — independent, can start now) **& @Porter**
(REQ-050's 2 decisions + the REQ-053 mixed-program DATA REQUEST → owner; and REQ-044/047/049/051/045 owner answers so I
can spec them next). SA queue: 044 + 052 are my next pickups once the shared-file tasks settle.

## [--:--] Fern (FE) — session opened on the owner's new machine · TASK-131 BUILT → REVIEW. 3 DoD boxes I could not tick. → @Sober
Startup ritual done (PROTOCOL + FE.md hard-boundaries card + board + this log + the 2026-08-11 log + `FRONTEND-STANDARD.md`).
Repos located at **`H:\scheduler`** (both my FE repos clean on `dong`; the REQ-041 work is committed — `09007fd`).
TASK-131 was the only thing waiting for FE, so I took it.

### Built (5 files, uncommitted on `smart-scheduler-front@dong`)
- 🆕 `lib/scheduler/eligible.ts` — `entKey`/`eligibleLabel` lifted out of `CreateForm`, **one** definition shared by the
  new component and the form's selected-row lookup, so AC-3's label rules can't drift between two call sites.
- 🆕 `components/common/EligibleStudentSelect.tsx` — the one-Combobox picker: debounced typing → **server**
  `useEligibleStudents(type, true, q)` (no Mantine local `searchable` anywhere — that's the parent-phone path,
  TASK-088/REQ-024), options keyed by entitlement, no add-new, explicit loading / no-match / nobody-eligible states.
- 🆕 `lib/scheduler/eligible.test.ts` — 8 pure tests.
- `BookingModal.tsx` — the two-box `TextInput`+`Select` branch swapped for the one field; orphaned state/imports removed
  (**−45 lines net**).
- `dictionaries.ts` — REQ-043's owner-approved copy + new `noMatchStudent`; two dead keys deleted. No invented strings.

### Evidence
`bunx tsc --noEmit` **0** · `bun run build` **ok** · `bun test src/lib/scheduler/` **15 pass / 0 fail** (8 new) ·
FRONTEND-STANDARD §3.5 on all three source files: inline hex **0** · `transition-all` **0** · `font-family` **0** ·
`z-[` **0**. **AC-6 verified by the diff being empty** in the payload block — `studentId`/`studentName`/`courseId`/
`voucherId` still come from `selectedEligible` exactly as before.

### One thing I checked instead of assumed
Keeping `selectedEligible` derived from `CreateForm`'s own (unsearched) query means the searched pick must always exist
in the unsearched list. That's true **because `GET /students/eligible` is unpaged by design** — the BE says paging it
"would silently truncate" (`scheduler.service.ts:502`). Had it been paged, a searched pick would have silently produced
a `null` payload. Written into the code as a comment rather than left as a coincidence.

### 🔴 What I did NOT verify — and am not going to pretend I did
**AC-1 · AC-5 · responsive 375/768/960 · the `hallmark audit` verdict all need the modal RENDERED, and it will not mount
here.** I ran the app **locally in mock mode** with the API forced to a dead port, logged in, calendar rendered fine —
but `.mantine-Modal-root` stays **empty**: this session's browser pane isn't displayed, nothing composites, and Mantine's
rAF-driven `Transition` never completes. Same limitation Fern hit on 2026-08-01 and 2026-08-04; it's the harness, not the
code. I did a code-level pass against `anti-patterns.md` (the change strictly *removes* a control and reuses a shipped
pattern — no card-in-card, no hover-only affordance, no new colour/type language), but that is **not** the audit §4 asks
for. **Q1 in the TASK is yours: accept the code review and route the visual/interaction pass to QA via Porter, or hold
131 in REVIEW until someone can drive it in a composited browser?**

### ⚠️ Environment hazard, reported not fixed (outside my TASK, and it's the owner's file)
`smart-scheduler-front/.env.local` on this machine points **`NEXT_PUBLIC_API_URL` and `AUTH_URL` at
`https://frontoffice.develyst.online`** — the customer-facing box. Starting the dev server and opening a protected route
**redirected the browser onto the production login page**. I entered nothing, submitted nothing, navigated away at once,
and re-ran everything with the URLs overridden to localhost. Anyone doing local FE work on this machine will silently
point at prod, which PROTOCOL forbids — worth someone deciding what the local default should be. I did not edit the file.

**Ball: @Sober** (review TASK-131 · Q1 the rendered-pass call · Q3: do I start TASK-132 on this uncommitted tree or wait
for your review, since 131/132/133 collide on `BookingModal.tsx`).

## [Sober (SA)] — TASK-131 REVIEW = code PASS ✅ (reproduced) · visual pass → @Tanya · Fern proceeds 132/133 · ⚠️ prod .env hazard → @Porter
Reviewed TASK-131 against SPEC-039 + DoD — reproduced, not trusted.
- **Reproduced myself:** `bunx tsc --noEmit` **0** · `bun test src/lib/scheduler/eligible.test.ts` **8/0** · FE-standard
  greps (`bg-[#`/`transition-all`/`font-family`) on all 3 changed/new files **0** · orphan grep in BookingModal
  (`eligibleSearch`/`debouncedEligible`/`pickCourse|VoucherStudent`) **none** — clean removal.
- **Faithful to spec:** `entKey`/`eligibleLabel` lifted to `lib/scheduler/eligible.ts` (one definition; the TASK-121/125
  multi-course label logic preserved + unit-pinned = AC-3). `EligibleStudentSelect` mirrors `StudentSelect`, drives the
  **server** `q` (no local `searchable` → AC-2 parent-phone intact), no add-new, distinguishes loading/no-match/
  nobody-eligible (AC-5 logic). Payload untouched (AC-6). `StudentSelect` only re-worded (AC-4).
- **Fern's two-query design decision: accepted.** He kept `CreateForm`'s unsearched eligible query to resolve the
  selected row, and **verified** (not assumed) the endpoint is unpaged by design (`scheduler.service.ts:502`), so a
  searched pick is always in the superset — written as a comment. Good instinct; the minor cost beats widening the prop.
- 🔴 **AC-1/AC-5 render · 375/768/960 · hallmark verdict — not verified, and Fern rightly refused to tick them on a code
  read.** The modal won't composite in a headless session (the known 08-01/08-04 Mantine-`Transition` limit). **Q1
  answered: option (a)** — I accept the code review; **the rendered visual/interaction pass goes to @Tanya via Porter**
  (same pattern as TASK-128's visual confirm). TASK-131 = **code DONE**; REQ-043 closes on Tanya's pass.
- **Q2 (deleted orphan dict keys):** approved. **Q3 (sequencing):** Fern **proceeds — TASK-132 then TASK-133 on this
  tree** (132 is the voucher branch, a different region than 131's picker swap; 133 is PlanModal, a different file). Keep
  it uncommitted on `dong`; commit the review-passed batch on the owner's word.

### ⚠️ Environment hazard Fern found (flagging to Porter — owner's file, not mine to edit)
`smart-scheduler-front/.env.local` on this machine points `NEXT_PUBLIC_API_URL` **and** `AUTH_URL` at
**`frontoffice.develyst.online`** — the customer-facing box. Starting the dev server + hitting a protected route
**redirected the browser onto the production login page** (Fern entered nothing, overrode to localhost for the session).
This means anyone doing local FE work here silently points at prod, which PROTOCOL forbids. **@Porter: raise with the
owner** — the `.env.local` should default to localhost (FE devs override to prod deliberately, never by default). Same
"everything points at the one prod box" theme as REQ-042.

**Ball: @Fern** (TASK-132 voucher-time → then TASK-133 course-subject-readonly, on this tree; TASK-134/135 are Jason's)
**& @Porter** (route TASK-131's rendered pass to @Tanya to close REQ-043; raise the `.env.local`→prod hazard with the
owner; + still pending from before: REQ-050's 2 decisions, the REQ-053 mixed-program DATA REQUEST, and the owner answer
queue for REQ-044/045/047/049/051). **& @Jason** (TASK-134/135 can start now).

## [--:--] Fern (FE) — TASK-132 + TASK-133 BUILT → REVIEW. 2 wording/scope calls that are not mine. → @Sober
Took your Q3 answer and ran the sequence on one tree: **131 (already reviewed) → 132 → 133.**

### TASK-132 — voucher booking picks its own time
Voucher branch now renders the **same** `TIME_SLOTS` `<Select>` the course/trial branches use (AC-6), payload and
validity read `startTime` instead of `createSlot.time`, teacher still the clicked column (the domain rule was "can't
pick a **teacher**" — it never said anything about the time; that reasoning is a comment in the file now).
**AC-2 verified by tracing, not assumed:** `handleSubmit`'s `detect` pre-check reads `input.startTime`, so it checks the
**chosen** time and a hit renders the existing `blockedTitle/blockedDesc` — no second refusal phrasing introduced.
🔴 **One thing I could not do without authoring copy — Q1 in the TASK.** `booking.voucherNoSlot` read *"…doesn't pick a
teacher **or fixed time** — it uses this slot: {teacher} · {time}"*. After this change that sentence is **false on the
screen**. REQ-048 gives the field label and placeholder but no replacement for the teacher line. I would not ship a lie
and I would not quietly write staff-facing copy, so the build carries a clearly-flagged **draft** (`voucherNoTeacherPick`)
and the decision goes to **Porter through you**.
Also flagged, because both are better found now than at acceptance: **REQ-048 requirement 4 says Porter would rather the
time start EMPTY, SPEC-040 said keep the seed** — I followed your SPEC, and it's a one-line change if his lean wins; and
**taken slots are still offered and refused at save** (the REQ allows either shape, the SPEC picked refusal).

### TASK-133 — วิชา read-only on a course session
One predicate does all three effects: `plan.kind === "course" && target.kind === "move" && !onLocalSave`. วิชา becomes an
`Input.Wrapper` label + read-only text + REQ-053's TH/EN explanation verbatim (a subject-less session shows a labelled
`—`, never a blank), and `subjectId` is **omitted from the move payload** rather than merely disabled in the UI.
Insert/extra, the voucher move path and single/trial are untouched (AC-4); date/time/teacher stay editable.
**The third clause is the judgement call:** the same `SessionEditor` is reused by **create mode**, where the course's
program is still being chosen — locking there would have made the new-course flow unable to set a subject at all.
🔴 **Q1 in the TASK, and it's a real hole in REQ-053's intent, not a nitpick:** `CreatePlanFlow` posts **per-row**
`sessions[].subjectId`, so a brand-new course can be born mixed-program and its *derived* program becomes whatever
`bookings[0]` happens to be — the same REQ-013/014 corruption, one door earlier. I did **not** widen the lock (out of
task scope, and it needs different UX — the program belongs to the course-level field, not the row). Your call whether
that is a new REQ or an accepted residual written into SPEC-042.

### Evidence (both tasks)
`bunx tsc --noEmit` **0** · `bun run build` **ok** · `bun test src/lib/scheduler/` **15 pass / 0 fail** · FRONTEND-STANDARD
§3.5 on both touched files: inline hex **0** · `transition-all` **0** · `font-family` **0** · `z-[` **0** · no orphaned
keys (`voucherNoSlot` has zero readers and is gone from both dictionaries).
🔴 **Rendered checks + `hallmark audit` still not done for either** — same non-compositing browser pane as TASK-131.
Both ride the @Tanya visual pass you already routed; I'm not ticking a rendered box from a code read.

### Housekeeping, reporting not acting
While I was on 133 the **owner committed TASK-131 + TASK-132 as `c6db8ca`** (5 files). You'd said keep the batch
uncommitted until review — nothing is lost, I didn't touch the commit, and **TASK-133 is now the only uncommitted work**.

**Ball: @Sober** (review 132 + 133 · route TASK-132's Q1 wording to Porter · rule on TASK-133's Q1 create-mode hole ·
TASK-132 Q2 seed-vs-empty). FE queue is otherwise **empty** — 111 still BLOCKED on Jason's TASK-110, and 118 waits on
the owner's REQ-035 call.

## [Sober (SA)] — TASK-132 + TASK-133 = code PASS ✅ (reproduced) · questions routed · a create-mode gap flagged → @Fern @Jason @Porter
Reviewed both against their SPECs + DoD; reproduced, not trusted (on `c6db8ca` + the uncommitted 133):
`bunx tsc --noEmit` **0** · `bun test src/lib/scheduler/` **15/0** · §3.5 greps on both files **0** · `voucherNoSlot`
fully removed. Read both changed regions — faithful to SPEC-040/042.

**TASK-132 (voucher time) → code DONE.** Voucher branch now renders the standard `TIME_SLOTS` Select; payload +
validity read `startTime`; teacher still the clicked column; AC-2 clash verified at the `detect` call site (chosen
time, existing wording); AC-3/4/5 regions untouched.
- **Q1 (copy) → @Porter:** the old `voucherNoSlot` string became false ("or fixed time"); Fern shipped a faithful
  **draft** `voucherNoTeacherPick` rather than a lie or silently-authored copy — right call. Porter approves final wording.
- **Q2 (seed vs empty) → @Porter, non-blocking, my rec = keep the seed** (a default to the clicked cell ≠ the old pin,
  since it's now editable). REQ-048 R4 leaned empty; it's Porter's UX call; one-line either way; build ships the seed.
- **Q3:** accepted as-specced — taken slots offered + refused on save satisfies REQ-048 R2; greying-out is a bigger
  follow-up (flagged, not built).

**TASK-133 (course subject read-only, FE) → code DONE.** `courseSubjectLocked` predicate → read-only `Input.Wrapper` +
explanation, `subjectId` omitted from the course move payload; AC-4 preserved (insert/extra/voucher untouched). Q2
approved (read-only text over disabled Select — the right read of "not a hidden field").
- 🔴 **Q1 is a real finding — accepted as scoped-out, routed as a follow-up.** The same corruption is reachable in
  **create mode**: `CreatePlanFlow` posts per-row `sessions[].subjectId` (`:152`), so a brand-new course can be born
  mixed-program, and TASK-134's guard only covers edits. **Out of REQ-053's scope** (that's `แก้ไขคาบ` = editing) and it
  needs a different shape (subject as a course-level field at creation + a BE guard). **Not widened here** (discipline);
  recorded in SPEC-042 §follow-ups and routed to Porter for a small sibling REQ.

**Both rendered/hallmark checks are genuinely un-doable headless** (modal won't composite) → both roll into the same
**@Tanya** visual pass I routed for TASK-131. REQ-048 closes on that pass; REQ-053 closes on that pass **+ TASK-134 (BE)**.

Housekeeping ack: owner committed 131+132 as `c6db8ca` — fine, both passed review; 133 is the only uncommitted FE work,
commit on the owner's word.

**Ball: @Jason** (TASK-134 course-subject-refuse BE — the other half of REQ-053; + TASK-135 LINE-leave — both can run now)
**& @Porter** (route @Tanya the rendered pass for TASK-131/132/133 to close REQ-043/048/053; TASK-132 Q1 copy + Q2 seed
decisions; the REQ-053 create-mode sibling REQ + the mixed-program DATA REQUEST; still pending: REQ-050's 2 decisions,
the `.env.local`→prod hazard, and the owner answer queue for REQ-044/045/047/049/051). SA queue clear — my next specable
pickups are REQ-044 + REQ-052 once owner answers/decisions land.

## [--:--] Jason (BE) — TASK-134 + TASK-135 BUILT → REVIEW. 3 calls that are copy/UX, not mine. → @Sober
Re-read board + this log first: TASK-130 came back **PASS/DONE** (thanks — and the owner has committed it as
`9d01efd`, so nothing of mine is uncommitted from that), TASK-095 was a stale board headline not an owed review,
and the board path is fixed. Both new BE tasks were unblocked, so I did both.

### TASK-134 — server refuses a subject change on a course session (REQ-053's real half)
- **One pure predicate**, `changesCourseSubject()` in the new `src/lib/course-subject-lock.ts`, called from
  **both** write paths (`applyPlanChange` move branch + `moveBooking`) — the `lib/booking-slot.ts` idiom, so the
  two guards can't drift. Throws `conflict(COURSE_SUBJECT_LOCKED, …)` = **409** with the typed code the FE's
  `ApiClientError` already surfaces.
- Refuses only when `subjectId` present **and** `courseId != null` **and** it differs. So a no-op passes
  (idempotent), a date/time/teacher move passes, and voucher / single / trial keep an editable subject (**AC-4**).
- Both guards sit **after** the `SESSION_DELIVERED` check and **before** any write (`moveBooking`'s is before it
  opens the transaction — it's a pure read of the row already loaded, same guarantee, no write attempted).
- **tsc 0 · `bun test` 467/0** (was 462; the 5 new tests are the delta).
- **Left alone deliberately:** create-mode (`sessions[].subjectId`) can still birth a mixed-program course —
  that's your own TASK-133 Q1 finding, routed to Porter as a sibling REQ. I didn't widen the guard into it.

### TASK-135 — LINE leave names the session
- New pure `src/lib/line-leave.ts` (`needsChildStep`, `childrenWithSessions`, `leaveSessionLabel`), new
  `childPicker()` reply builder, **3 i18n keys TH+EN**; `doLeave` gains the child step, `doLeaveBooking`'s
  confirmation names date · time · ครู{teacher}.
- **`updateBookingStatus` is untouched** — quota / extension / make-up / freelance reconcile all unchanged. The
  single-session-per-day path is the old path (**AC-5**), and the **check-in** picker's label is unchanged.
- **tsc 0 · `bun test` 474/0** (7 new tests: 1-child-2-sessions ⇒ no child step · 2 children ⇒ child step ·
  single session ⇒ no step · one entry per child · TH + EN label · a row with no teacher/subject still renders).

### 🔴 The one finding I want you to rule on (Q1 on TASK-135)
**LINE clamps a quick-reply label to 20 characters.** `09:00 · ครูก้อง · Surfskate` is **26** — the existing
`clampLabel` cuts at 19, and what it eats is exactly the **program**, the field REQ-046 is about. So I put the
full `time · teacher · program` line **in the picker's prompt body** as well (same i18n row, no new copy) and
left the buttons clamped. Alternative if you prefer: make the button `{time} · {program}` and drop the teacher.
I didn't want to tick "labelled time · teacher · program" while shipping a string truncated at 19 chars.

### Two copy questions (Porter's call via you, both one-liners, neither blocking)
- **134 Q1:** the specced refusal message is English (`A course session's subject cannot be changed`) while every
  neighbouring conflict in that service is Thai and the FE shows the server message to Thai-speaking staff. I
  shipped the specced wording rather than authoring copy.
- **135 Q2:** the specced confirmation **drops the child's name** the old `leave_ok` carried. Fine on the
  child-step path, but on a single-session day the name is gone. `{extended}`/`{locked}` are appended unchanged,
  so the make-up date and the quota-lock warning are not lost. (`leave_ok` is now unused by this path; left in
  place — not mine to delete.)

Both changes are **uncommitted** on `dong` (8 files across the two tasks) — same discipline as last time, I'll
commit on the owner's word rather than race your review.

**Ball: @Sober** (review TASK-134 + TASK-135; rule on the 20-char label shape; route the two copy questions to
Porter). Nothing else waits on BE: TASK-103/110/116/117 still need owner sequencing/confirms, and REQ-050's BE
tasks aren't cut yet (they're behind Porter's 2 decisions).

## [Sober (SA)] — TASK-134 + TASK-135 = PASS ✅ (reproduced) · 20-char shape ratified · 2 copy Qs → Porter → @Jason @Porter
Reviewed both BE tasks against their SPECs + DoD; reproduced, not trusted: `bunx tsc --noEmit` **0** · the 2 new test
files **12/0** · full suite **474 pass / 0 fail** (no regression). Read the key modules.

**TASK-134 (course-subject refuse, BE) → DONE.** `changesCourseSubject()` is **exactly** the specced predicate
(`!!requested && courseId != null && requested !== current.subjectId`) — one shared pure fn called from both
`applyPlanChange` move (`scheduler.service.ts:1632`) and `moveBooking` (`:1921`), throwing 409 `COURSE_SUBJECT_LOCKED`
before any write. No-op passes (idempotent), voucher/single/trial pass (AC-4). Correctly **not** widened to create-mode
(my follow-up). **Q1 (English vs Thai refusal copy) → @Porter, my rec = Thai** — the English was my SPEC's miss (siblings
are Thai; FE surfaces it to Thai staff); the FE lock already carries the bilingual explanation so this 409 is defensive.
One-line, non-blocking. **⇒ REQ-053 is now code-complete (FE 133 + BE 134); closes on @Tanya's FE visual pass.**

**TASK-135 (LINE leave, BE) → DONE (code).** `needsChildStep`/`childrenWithSessions`/`leaveSessionLabel` wired into
`doLeave` (child step when ≥2 children have sessions; enriched picker); single-session day byte-for-byte the old path
(AC-5); `updateBookingStatus` untouched (quota/extension/make-up/freelance intact). **Q1 (LINE's 20-char button clamp) —
ratified Jason's shape:** the full `{time} · ครู{teacher} · {program}` goes in the picker **prompt body**, buttons carry
the clamped label (time still distinguishes) — the right call, don't drop teacher/program to fit the button. **Q2
(confirmation drops the child name on the single-session path) → @Porter, my rec = add `{name}` back** (strictly more
informative, a token; non-blocking, ships specced meanwhile). Real taps need a live OA + real parent → **@Tanya's LINE
pass**; REQ-046 closes on it.

**Where the HIGH batch stands:** REQ-048 (132) · REQ-046 (135) · REQ-053 (133+134) are all **code-complete**, waiting
only on the two @Tanya passes (a modal render pass for 131/132/133, a live-LINE pass for 135) + a few Porter copy calls.
No BE or FE work is open on me.

**Ball: @Porter** (route @Tanya the two passes to close REQ-043/048/053/046; decide the 3 copy Qs — TASK-132 voucher
line + seed, TASK-134 refusal language, TASK-135 confirmation `{name}`; the REQ-053 create-mode sibling REQ + mixed-program
DATA REQUEST; REQ-050's 2 decisions; the `.env.local`→prod hazard; and the owner answer queue for REQ-044/045/047/049/051
so I can spec them). **& @Jason** (idle on this REQ set — next BE is REQ-050's tasks once Porter's decisions land, or the
create-mode guard REQ). SA queue clear; my next specable pickups are REQ-044 + REQ-052 when the owner answers land.

## [--:--] Porter (PM) — cleared the whole Porter queue: 4 copy calls · REQ-050 decisions · REQ-054 cut · @Tanya routed · env hazard raised
Read Sober's four entries and Fern's/Jason's. Everything that was waiting on **me** is answered here; nothing is
parked.

### 1. Copy / UX decisions (my hat — engineers correctly refused to author these)
- **TASK-132 Q1 (voucher line, the old `voucherNoSlot` became a lie):** approved Fern's instinct to ship a faithful
  draft rather than a false string. **Final wording** — TH: `เลือกเวลาได้ตามต้องการ — ครูจะเป็นคนของช่องที่คุณกดมา` ·
  EN: `Pick any time — the teacher is the one whose column you clicked.`
- **TASK-132 Q2 (seed vs empty):** **seed with the clicked cell's time** — and I recorded in REQ-048 R4 that this
  **corrects my own earlier lean**. "Empty and deliberate" was aimed at a *pinned* 09:00; the pin is what this REQ
  removes. Once editable, seeding from the cell staff actually clicked matches the other tabs and saves a tap.
- **TASK-134 Q1 (409 refusal language):** **Thai**, matching its siblings and the staff who see it —
  `ไม่สามารถเปลี่ยนวิชาของคาบในคอร์สได้` (already the wording in REQ-053).
- **TASK-135 Q2 (confirmation drops the child's name):** **add `{name}` back.** A parent with two children reading
  "แจ้งลาแล้ว" without a name has to guess whether they cancelled the right child's session — which is the entire
  point of REQ-046.

### 2. REQ-050 — both decisions, so Jason can be cut tasks
- **(1) A corrected check-in MUST return the consumed session/hour. Yes.** Gap C is the priority fix and I am
  treating it as **money owed to a family**, not a tidiness issue: today, correcting a wrong check-in leaves the
  lesson consumed, so the family silently loses a lesson they never took. That is not a defensible state to ship,
  and it is the same "reconcile-to-target" discipline TASK-028 already established for the freelance draw.
- **(2) Historical back-fill (REQ-050 Q3): nothing to do.** Sober's audit says a wrong attribution is **not
  detectable** from stored data — so there is no honest list to correct, and I will not authorise a guess against
  real families' balances. Recorded as answered-and-closed rather than left hanging. **If** the money fix later
  surfaces a detectable signal, it comes back to the owner as a fresh decision.

### 3. REQ-054 cut (the create-mode sibling Sober flagged)
Adopted his TASK-133 Q1 finding as **REQ-054**, HIGH. His scoping was right to keep it out of 053 — but I did widen
one thing deliberately and said so in the REQ: **his "optional hardening" (a course-level program field) is IN
scope**, because without it requirement 2 guards a value that does not exist. Today the course's program is
`bookings[0].subject` — an accident of row order, which is *why* both defects were possible. Q1 gives him the exit:
if it needs a migration and readers repointed, say so and I split it rather than let the REQ swell.

### 4. @Tanya — two passes, and they are the only thing between four REQs and DONE
**@Tanya:** three FE tasks and one BE task are code-complete and SA-reviewed, but their **rendered/live** halves are
genuinely un-doable headless (the modal won't composite; LINE taps need a live OA). Please take:
- **Pass A — booking-modal render pass** on the current `dong` FE tree: TASK-131 (one student picker on all four
  tabs), TASK-132 (voucher time selectable, seeded from the clicked cell), TASK-133 (course session's วิชา read-only
  with its explanation). Closes **REQ-043 · REQ-048 · REQ-053's FE half**. Check at **375 / 768 / 1440** against
  FRONTEND-STANDARD as you did for TASK-128.
- **Pass B — live LINE leave pass** for TASK-135: enriched picker label (time · teacher · program), the child-first
  step when ≥2 children have sessions, and the confirmation naming the cancelled session **plus the child's name**
  (my decision above — verify the built version matches). Closes **REQ-046**.
  ⚠️ Pass B touches the **customer-facing OA** (REQ-042 established that "UAT" and the customer box are one server).
  **Do not message real parents/teachers.** If you cannot do it without touching real people, say `NOT_TESTED` and
  tell me — I will get the owner to run it and convert his ✅/❌, exactly like Script 6.

### 5. ⚠️ Raised to the owner: `.env.local` points local FE work at the customer box
Fern found `smart-scheduler-front/.env.local` pointing `NEXT_PUBLIC_API_URL` **and** `AUTH_URL` at
**`frontoffice.develyst.online`** — so starting a local dev server and hitting a protected route **redirected the
browser to the production login page**. He entered nothing and overrode to localhost. This is the same theme as
REQ-042: everything on this machine points at the one live box by default. Raising it with the owner as a
**default-should-be-localhost** change (a developer may opt *into* prod deliberately; never by accident).

**Ball: @Sober** (REQ-054 · then REQ-044 + REQ-052, which are unblocked — 052's owner answers are folded in already;
plus cut Jason's REQ-050 Gap-C/Gap-A tasks now that both decisions are made) **& @Tanya** (Pass A + Pass B)
**& @Jason** (REQ-050 tasks once Sober cuts them) **& owner** (the DATA REQUEST below + the answer queue:
REQ-045 A/B/C · REQ-047 Q1 · REQ-049 Q1–Q3 · REQ-051 Q1–Q3 · REQ-044 wording confirms · `.env.local` fix · close the
DB whitelist · commit word for TASK-133).

## [--:--] Porter (PM) — 🌙 END OF DAY: PROJECT-STATUS.md + .html rewritten for 2026-08-16. Owner is pausing.
Owner is taking a break and asked for the day's situation written down before he stops. Rewrote **both** resume
artifacts (they are the cross-machine memory — local `.claude/memory/` does not travel, git does):

- **`PROJECT-STATUS.md`** (AI-readable, the file every role reads at startup) — reconciled against the board and
  today's log, not against REQ headers. Now carries: the REQ-042 root cause in one paragraph · the **environment
  identity correction** (UAT = `frontoffice.develyst.online` = the box we called customer-prod; **one** box) · a
  table of everything waiting on the **owner** with my lean for each · the two **@Tanya** passes and exactly which
  REQs they close · the unblocked team queue (052/054/050/044) · housekeeping · the DATA REQUEST SQL inline.
- **`PROJECT-STATUS.html`** (the human dashboard he actually reads) — same content, Thai, rebuilt on the existing
  design tokens: kept the light/dark palette and the card/chip system, added a `<pre>` block for the SQL, and
  fixed a latent layout bug while I was in there (`.body` had no `flex:1;min-width:0`, so a long note could push
  the chip off — the exact truncation class REQ-041 closed).

**Two judgement calls I want on the record, since a status page is where wishful thinking usually creeps in:**
1. **REQ-042 is shown as fixed but NOT delivered**, with the two untested ACs named (wrong input mid-`สมัคร`, and
   the **language toggle** re-linking the menu). It would have been easy to write "LINE ✅ done" — the owner ran it
   and it worked. But nobody has exercised those two paths, and the language-toggle leg failed for the *same*
   swallowed-error reason, so it is exactly the leg most likely to still be broken.
2. **The numbers on the dashboard are counts of real states, not achievements.** "4 โค้ดเสร็จ รอ QA" is not four
   deliveries; the page says so in words next to the number.

Also stated plainly on both pages: the **three things only the owner can do** — fix `.env.local` (local FE work
currently points at the customer box), close **two** open DB whitelists (today's `49.237.170.101` plus the
`110.171.40.169` line the board has listed as unclosed since 08-11), and run the mixed-program DATA REQUEST.

**Ball: owner** (rest; then the answer queue + the 3 hands-on items) **& @Tanya** (Pass A + Pass B — they close
REQ-043/046/048/053) **& @Sober** (REQ-052 · REQ-054 · REQ-044 · cut REQ-050's tasks — all unblocked).

## [--:--] Porter (PM) — owner answered ALL five. 045 unblocked · 047 reshaped · 049 as-recommended · 051 concept confirmed + leave added · 044 his wording wins. → @Sober
Every answer is now recorded in its REQ's `## Questions` **and** appended to its board row, so no one has to read
this log to know the decision.

1. **REQ-045 → (B). UNBLOCKED, now READY_FOR_SA.** Planned absences **declared at creation are free**; one marked
   later still consumes quota per REQ-030. **Two things the SPEC must handle, and they are not cosmetic:**
   (a) `MAX_WEEK_BY_SIZE` is now the **only** limit on creation-time absences, so AC-3's ceiling refusal became
   load-bearing; (b) **"declared at creation" has to be a real distinction in the data**, not a timing coincidence —
   a session absent from birth is a different event from one cancelled in week 3, and without that distinction AC-5
   cannot be verified and the rule will rot the first time someone edits a course.
2. **REQ-047 → keep it PER TEACHER TYPE** (*"เอาเป็น setting ครู เหมือนเดิมก็ได้"*). **My "one school-wide number"
   lean is overruled** — and he is right that his staff already think in ครูประจำ vs ฟรีแลนซ์. Requirement 2 rewritten:
   **one editable value per type**, both server-enforced, both on REQ-031's screen; every AC now reads "the
   configured cut-off **for that session's teacher type**", never a literal 3.
   **One assumption I am proceeding on rather than blocking him for:** the customer asked for **3 hours**, so
   **defaults become 3 h for both types** (up from 1 h / 2 h). The alternative reading — keep 1/2 h and merely make
   them editable — would ship the customer's actual request **unmet**, which is exactly the kind of thing that
   quietly doesn't happen. Stated in the REQ so he can veto in one line.
3. **REQ-049 → all three recommendations accepted.** Existing admin channel · **immediate** · late leave reads the
   same. Noted for the SPEC: with immediate sending, **AC-6 (one message per recipient) is now the real risk** —
   retries, a re-save, or a staff cancel following a parent cancel must not each fire.
4. **REQ-051 → concept confirmed, and he added `แจ้งลา` to the page.** He asked what I was worried about; I answered
   plainly (no login + a phone number is not a secret ⇒ a stranger can burn a paid lesson, and a phone→children
   lookup is harvestable — this team already fixed one such leak, TASK-047) and then **built what he asked for**:
   phone → that phone's children with a session today → per child, `เช็คอิน` **or** `แจ้งลา`. **His sibling case
   (one comes, one doesn't) is a better argument than my "one job per screen"**, so the leave action is in.
   **Three safeguards I kept, because not one of them costs him a tap:** today's sessions only · nicknames not full
   names · a single neutral message for "nothing today" and "unknown number" plus a rate limit. Told him they are
   removable on request.
   🔴 **And I raised a new Q1 that his simplification exposes:** REQ-047's cut-off would **refuse a counter leave
   minutes before the session** — which kills the exact case he asked for. **My lean: the walk-in page is exempt**
   (a human is physically present, it records `source = walk-in`, the session still re-owes). This is a genuine
   collision between two of today's REQs, not a detail; better found now than by a nanny at the counter.
5. **REQ-044 → his label wins: `คอร์ส(ชดเชย)` / `Course (make-up)`.** Better than my `แทรกคาบชดเชย` for a reason
   worth writing down: staff **scan the tab strip for the word "คอร์ส"** — his keeps that anchor and adds what makes
   it different, mine renamed the thing out of recognition. `แทรกคาบชดเชย` stays as the **action** name (plan-modal
   button + the explainer line), so the tab is findable *and* the action is named. AC-1 updated.
   Q2 (name the moved session in the confirmation) went unanswered → proceeding on **end date only**; likewise
   REQ-046 Q3 (undo) → **staff fix it**, and REQ-045 Q2 (consecutive weeks) → **yes**. All three are one-line
   reversals if he disagrees; none of them justifies holding a build.

**Ball: @Sober** — the owner queue is **empty of blockers**: 044 · 045 · 047 · 049 · 051 · 052 · 054 all have their
answers, plus REQ-050's tasks to cut. **& @Tanya** (Pass A + Pass B — still the only thing between REQ-043/046/048/053
and DONE) **& owner** (REQ-051's new Q1 · the 3 hands-on items: `.env.local`, the two open DB whitelists, the
mixed-program DATA REQUEST · TASK-133 commit word).

## [--:--] Porter (PM) — REQ-051 Q1 answered (ก + admin approval) · owner's housekeeping decisions recorded · DATA REQUEST query was wrong, fixed. → @Sober
**REQ-051 Q1 → allowed, WITH an admin in the loop.** The owner picked (ก) — a counter leave inside REQ-047's
cut-off is permitted — but added the part I had not proposed and which makes it sound: *"แอดมินต้องรับทราบและอนุมัติ"*,
either an **admin code** typed on the spot or an **approval like the REQ-020 teacher-link claim**. Folded in as
**requirement 8 + AC-10/AC-11 + the pending wording**: inside the cut-off a leave is a **request**
(`PENDING_APPROVAL`) with the admin notified immediately and an on-the-spot admin-code fast path; **outside** the
cut-off nothing changes and the easy case stays one tap. Two failure modes written in explicitly because they are
where this design would rot: a pending request must **never silently become a leave**, and must **never silently
vanish** — if nobody approves before the session, it stays pending and the session follows normal attendance.
His instinct here was better than my clean "exempt the page": exemption alone would have made the cut-off rule a
suggestion, since anyone could walk to the counter to bypass it.

### Owner's decisions on the housekeeping list — recorded so nobody re-raises them
1. **`.env.local` → he declines the change**: *"ไม่จำเป็น… ฉัน build บ่อยๆ อยู่แล้ว ไปเทสบนนั้นกันเลยก็ได้"*. Recorded as
   an **explicit owner decision**, not an oversight — @Sober, tell Fern not to re-raise it. I stated the risk once
   (local dev pointing at the customer box is how accidents happen, and Fern's browser did land on the production
   login page) and it is his call to make; he lives on that box daily. Fern keeps overriding to localhost for his
   own work, which is enough for the team's safety.
2. **The two open DB whitelists → deferred by the owner** ("เดี๋ยวฉันทำเองทีหลังจบงานหมด"). Still on the housekeeping
   list; I will raise it again when the REQ batch closes, not before.
3. **DATA REQUEST → my query was wrong.** `ERROR: function min(uuid) does not exist` — `student_id` is a **uuid**,
   and `min()` has no uuid overload. My mistake, not his DB. Corrected query given (dropped the `min(student_id)`
   column entirely — it was a convenience, not part of the question).
4. **Commits → he does them himself.** Fine; TASK-133 is his to land.

### Two open items he answered
- **REQ-035 (sell-side) → do it LAST.** Recorded; it stays specced-and-unbuilt at the bottom of the queue.
- **The LINE language-toggle check → he questioned what I even meant, fairly.** Clarified to him: switching
  language obviously changes the bot's *replies* — that has always worked. The thing nobody has verified is whether
  the **rich menu itself swaps to the English menu**, because that re-link goes through the **same menu-id lookup
  that was empty and silently failing** (`line-webhook.service.ts:500`, the sibling of the `:461` call REQ-042
  fixed). So the check is: switch to EN → **do the buttons become the English menu**, or stay Thai?

**Ball: @Sober** (REQ-051 now has its answer — spec it with the approval shape; plus 044/045/047/049/052/054 and
REQ-050's tasks) **& @Tanya** (Pass A + Pass B) **& owner** (re-run the corrected SQL when convenient; and the one
LINE check above — or say the word and Tanya takes it).

## [--:--] Porter (PM) — 🟢 DATA REQUEST result: ZERO mixed-program courses. REQ-053/054 are now purely preventive. → @Sober
Owner ran the corrected query: **no rows.** No course in the live database has sessions carrying more than one
program.

**What this closes, and what it does not.**
- **Closed:** REQ-053 Q2/Q3 and REQ-054 Q2. There is **no historical corruption**, nothing to correct, and **no
  owner decision pending**. It also means REQ-013 (sport share) and REQ-014 (revenue by activity) were **never
  distorted by this** — worth stating plainly, because I raised that possibility loudly and it turned out not to
  have happened. Good news deserves the same clarity as bad news.
- **Not closed:** both REQs still stand. The door was open — `แก้ไขคาบ` offered วิชา on a course session, and
  `CreatePlanFlow` posts a per-row `subjectId` — and nobody having walked through it yet is luck plus a small user
  base, not a safeguard. Closing it now is the cheapest it will ever be.
- **A real bonus for @Sober's REQ-054 design:** with every existing course verified internally consistent,
  requirement 4 (the course-level program field) can be **back-filled by derivation** from each course's own
  sessions with **no ambiguity and no data-migration risk** — no "which program did they actually buy?" judgement
  calls, which is exactly what would have made that migration expensive.

**Ball: @Sober** (044 · 045 · 047 · 049 · 051 · 052 · 053-remaining · 054 + REQ-050's tasks — nothing of mine is
owed to you) **& @Tanya** (Pass A + Pass B) **& owner** (the one LINE check: switch to EN and see whether the
**menu buttons** become English — or hand it to Tanya).

## [--:--] Porter (PM) — 🏁 REQ-042 DELIVERED. Owner ran the language check on the live OA; the menu itself swaps. → @Sober @Tanya
Owner sent four screenshots from the real OA. What they show:
- **The rich menu swaps with the language** — the leg I refused to assume, because it goes through the **same
  menu-id lookup that was empty and silently failing** (`line-webhook.service.ts:500`, sibling of the `:461` call
  the fix repaired). Parent menu EN `Check-in · Leave · My children · Add child · Language · Help` ↔ TH
  `เช็คอิน · แจ้งลา · นักเรียนของฉัน · เพิ่มนักเรียน · ภาษา · ช่วยเหลือ`; teacher menu EN `My schedule ·
  Language/Help` ↔ TH `ตารางของฉัน · ภาษา/ช่วยเหลือ`. **Both role menus, both directions.**
- **A role change completing end-to-end again** on the deployed build: phone → *"ผูกบัญชีผู้ปกครองสำเร็จ ✅ พบนักเรียน
  3 คน"* → the 6-cell parent menu. That re-confirms AC-1/AC-2 on the live OA, not just from this morning's run.

**⇒ Marking REQ-042 DELIVERED**, on the stakeholder's own acceptance on the live environment.

**What I am NOT pretending, and put on the board in the same breath:** **AC-4 (wrong input mid-`สมัคร`) was never
exercised**, and **no Tanya verdict exists on this REQ**. It closes on owner acceptance — which is legitimate, he is
the stakeholder and it is his live box — but the negative path is carried as a **regression line for @Tanya's next
LINE pass** rather than quietly counted as covered. `TEST_PASSED` remains hers alone to say; DELIVERED here rests on
the owner, and the board says exactly that.

🎁 **Unasked-for evidence in the same transcript, worth capturing before it scrolls away:** a teacher claim replies
*"ส่งคำขอผูกบัญชีให้เจ้าหน้าที่แล้ว ✅ รอการอนุมัติ"* — i.e. the claim **queues instead of auto-granting**, which is
exactly the behaviour **REQ-020 / QA Script 4** has been waiting to verify. That is the first half of Script 4
evidenced by the owner on the live OA; the approve/reject half at `/scheduler/link-requests` is still unverified.
**@Tanya** — treat this as a head start, not a pass; the verdict is still yours.

**Ball: @Sober** (the eight-REQ queue — nothing owed from me) **& @Tanya** (Pass A · Pass B · plus REQ-042's AC-4
and Script 4's approve/reject half when you're in there) **& owner** (nothing blocking — REQ-035 sits last by his
call, and housekeeping is deferred by his call).

## [Sober (SA)] — worked the queue: REQ-049 + REQ-054 spec'd, REQ-044 BLOCKED (its premise is false). Grounded all three first. → @Jason @Fern @Porter
Took three of the queue this pass, grounding each in real code before designing (parallel investigation).

- **REQ-049 (notify on leave) → SPEC-044 + TASK-136 (BE) + TASK-137 (FE).** Q4 answered: today a leave notifies **admin
  only** (`notifyAdmins`), never the teacher — so this extends that path, no parallel system. Two design calls I made:
  (1) the setting is a real **two-value enum** on REQ-031's screen — that mechanism is numeric-only, so the SPEC extends
  it to enums rather than hacking a `0|1` int (the REQ demands named options); (2) **AC-6 idempotency** = an early-return
  when `current.status === "SICK_LEAVE"` (mirrors confirm/attend), which also fixes a latent admin double-notify. Teacher
  = the **session's** teacher (AC-3). **Testing without messaging real coaches:** default `admin_only` (prod safe) +
  verify the teacher path on dev with a **test teacher LINE account**, never the 21 real prod teachers — the REQ's
  test-recipient constraint satisfied without a new dry-run subsystem.
- **REQ-054 (course subject fixed at create) → SPEC-045 + TASK-138 (BE guard) + TASK-139 (FE) + TASK-140 (BE column).**
  Q1 sized: kept req 4 (the column) **in scope but as its own task** so the guard ships fast. Guard (zod refine +
  service assert that all created sessions share the subject) + FE (course-level subject only) close the create hole;
  the `course_packages.subject_id` column + `0018` migration is the source-of-truth hardening, **lossless** because the
  DATA REQUEST found zero mixed courses. REQ-013/014 read per-session → unaffected.
- 🔴 **REQ-044 (rename the COURSE tab) → BLOCKED, and I'm glad I grounded it.** The REQ's premise is **false against the
  code:** the tab does a plain **ADD (+1 session)** (`createBooking`→`insertBooking`), NOT a make-up move — it never
  consumes the tail, never keeps total constant, never recomputes an end date. REQ-030's real make-up-move is a
  **different** path (plan modal → `applyPlanChange`→`reconcileCoursePlan`). So "two entry points, one behaviour" is
  untrue today, and shipping the requested wording (`คอร์ส(ชดเชย)`, "tail moves, total unchanged", "ends {date}") would
  make the screen **lie** — the exact failure this REQ meant to fix. Making it true is a **behaviour change** (route
  through make-up insert; requires an *owed* session), which the REQ's own constraints send back to you. **Decision fork
  in the REQ's Q3 (A authorize the behaviour change / B honest "extra session" wording / C revisit removing the tab), and
  the prior question underneath it: what is the COURSE tab even FOR?** The owner asked "จองคอร์สมีไปทำไม" twice; the code
  answer — "it appends an extra course session" — may itself be unintended. I will not ship a self-misdescribing screen.

**Queue state:** specable-and-done this pass = 049, 054. BLOCKED on owner/Porter = 044 (behaviour fork), 045 (owner
quota), 047 (1 owner Q), 050 (Porter's 2 decisions), 051 (1 owner Q). Not yet picked = **052** (calendar cell — my next,
needs a colour-token design I show you before build + Q3 grounded). REQ-053 = code-complete → Tanya.

**Ball: @Jason** (TASK-136 notify-on-leave BE · TASK-138 course-create-guard BE · TASK-140 subject-column BE — all can
start; 140 is a migration, flag for owner-run deploy) **& @Fern** (TASK-137 settings-enum FE · TASK-139 course-create FE)
**& @Porter** (REQ-044's behaviour fork + "what is the tab for" to the owner; the copy calls from the last batch;
REQ-050's 2 decisions; the owner answers for 045/047/051; and route @Tanya the render + LINE passes). My next spec pickup
is REQ-052.

## [--:--] Fern (FE) — TASK-139 + TASK-137 BUILT → REVIEW. One of them I actually saw run. → @Sober
Read your queue entry, took both FE tasks. TASK-136 was already DONE, so TASK-137's dependency was satisfied and I did
not have to guess a contract.

### TASK-139 — course create can no longer be born mixed-program
This is the hole I flagged as TASK-133's Q1 — thanks for turning it into REQ-054 instead of parking it.
`courseSubjectLocked` dropped its `!onLocalSave` clause, so a create-mode draft row now shows the **same** read-only
field TASK-133 built, seeded from the course-level subject (draft rows are built from the preview that ran with that
subject — one seeding path, so the two cannot disagree). `confirmCreate` stopped posting per-row `subjectId`.
**I verified the BE fallback instead of trusting the task note:** `scheduler.service.ts:1040` is
`subjectId: s.subjectId ?? input.subjectId`, so every row resolves to the course subject and the client *cannot* emit a
mixed course.
🔴 **And the change had a trap in it that the obvious version would have shipped:** the teacher `onChange` cleared
`subjectId` on every teacher swap — right when the subject comes from the teacher's programs, wrong on a locked course
row, where it blanks the read-only value and trips `onLocalSave`'s "pick a teacher and subject" guard, leaving a draft
row unsavable. Guarded now. I've called that case out for QA because it is exactly what a rendered pass should try.

### TASK-137 — notify-on-leave enum on the settings screen · 🟢 verified RENDERED
The settings screen is a **page**, not a modal, so for the first time this session I could actually run the thing.
Local, `NEXT_PUBLIC_USE_MOCK=true`, API pinned to a dead port. **Observed, not inferred:** the row reads
`แจ้งเตือนเมื่อมีการลา · Current: Admin only · Default: Admin only` with the help line and a `DEFAULT` badge; **no
`admin_only` appears anywhere on screen**; numeric rows unregressed (`3 days`, `30 minutes`); Edit → `Choose an option`
with exactly two segments while the underlying inputs still carry the **raw** keys; picking `Admin and teacher` + Save →
re-read as `Current: Admin and teacher`, badge flipped to `OVERRIDE`, `Reset to default` appeared.
Contract came from **Jason's actual code** (`lib/settings.ts:37`, and `settings.service.ts:63-65` literally states the
type/options contract) — no coordination guess. I also rewrote the settings **mock** to carry both rule kinds and the
BE's option-naming refusal, otherwise the new editor is unreachable offline. And the raw key is resolved through the
dictionary in **four** places, not just the control — the list line, the segments, **and both toasts** — because a toast
saying "admin_only" leaks the storage key just as badly as the screen would.
🔴 **Reset-to-default is still unverified** (shared confirm Modal, same non-mounting pane), and **TASK-139's create flow
end-to-end likewise** — both to @Tanya with 131/132/133.

### Evidence (both)
`bunx tsc --noEmit` **0** · `bun run build` **ok** · FRONTEND-STANDARD §3.5 on all 8 touched files: hex **0** ·
`transition-all` **0** · `font-family` **0** · `z-[` **0**.

### One thing I found and deliberately did NOT fix — Q1 in TASK-137
The settings row **label** comes from the BE registry and is **Thai only**, so on the English screen that row shows a
Thai heading above English options — visible in my own evidence above, and REQ-049 asked for a bilingual label. This
affects **every** settings row, not just mine. The fix is either "BE returns a label key the FE translates" (contract
change, Jason's) or "FE keeps a per-key label override" (duplicated copy, Porter's). Both are decisions above my line,
so I rendered the label exactly as every other row does and am handing you the choice rather than half-fixing one row.

**Ball: @Sober** (review 137 + 139 · TASK-137 Q1 the Thai-only settings label · TASK-139 Q1 one line in SPEC-045).
**FE queue is now empty** — 111 still BLOCKED on TASK-110, 118 waits on the owner's REQ-035 call, and 052's FE work
isn't cut yet (your next spec pickup).

## [--:--] Porter (PM) — REQ-044: Sober is right and I was wrong. Correcting it to the owner. + your answers are already on disk. → @Sober
**Two things, and the first one is mine to own.**

### 1. REQ-044's premise was false, and I am the one who wrote it
Sober grounded the tab in code: it does a plain **ADD (+1 session)** — `createBooking` → `insertBooking`. It does
**not** consume the tail, does **not** keep the course total constant, does **not** recompute an end date. The real
make-up move lives somewhere else entirely (plan modal → `applyPlanChange` → `reconcileCoursePlan`).

**Where my error came from, so the team can distrust the right thing:** I took REQ-030's *Analysis* sentence —
*"the Course tab … is the mechanism for adjusting a plan"* — and relayed it to the owner as **current behaviour**.
It was a design intention written in a REQ, never verified against the shipped code, and I repeated it twice: once
in chat as the answer to *"จองคอร์สมีไปทำไม"*, and once as the foundation of REQ-044. **Good catch, Sober — and the
right call to block rather than ship the wording.** Had it shipped, the screen would have said "the tail session
moves here, the total doesn't change" while quietly adding a free session to a course the family paid a fixed
number for. That is worse than the confusing label we started with.

**Correcting it with the owner now**, with the fork (A authorize the behaviour change · B honest "extra session"
wording · C revisit removing the tab) **and** Sober's deeper question — *what is that tab for at all?* — put to him
with the real facts this time. Notable context for his decision: the plan modal already carries **both** honest
actions as buttons — `แทรกคาบชดเชย` (the real make-up move) and `เพิ่มคาบ (คิดเงิน)` (REQ-037's paid extra) — so the
calendar's Course tab is a **third path** doing a bare, unpriced +1 that neither of those does.
**Porter's lean, stated for the record: C or A, not B.** A tab that silently adds an uncharged session to a fixed
-size course is not a labelling problem; B would just describe the hazard accurately and leave it in place.

### 2. @Sober — the answers you are still listing as blockers are already on disk, above your entry
Not a criticism, just routing: you listed *"REQ-050's 2 decisions · the copy calls · owner answers for 045/047/051"*
as outstanding. All of them landed earlier in **this same log file**:
- **REQ-050 — both decided:** (1) a corrected check-in **must** return the consumed session/hour (money owed to a
  family); (2) **no historical back-fill** — undetectable from stored data, so no guessing against real balances.
  ⇒ **You can cut Jason's Gap-C and Gap-A/AC-3 tasks now.**
- **Copy calls — all four answered:** TASK-132 voucher line wording + **seed** the clicked cell's time · TASK-134
  refusal in **Thai** · TASK-135 confirmation **adds `{name}` back**.
- **REQ-045 → (B)** (free at creation, quota still applies later; "declared at creation" must be a real distinction
  in the data, and `MAX_WEEK_BY_SIZE` becomes the only limit). **REQ-047 → per teacher type**, values become
  settings; my one-number lean overruled; defaults 3 h for both unless the owner objects. **REQ-051 → allowed, with
  admin approval** — inside the cut-off a walk-in leave is `PENDING_APPROVAL` + admin notified + an admin-code fast
  path at the counter; outside the cut-off, unchanged.
Each is also written into its REQ's `## Questions` and appended to its board row, which is the durable copy.

**Ball: @Sober** (045 · 047 · 050 · 051 · 052 — all unblocked; 044 waits on the owner's fork) **& @Tanya** (Pass A ·
Pass B · REQ-042 AC-4 · Script 4's approve/reject half) **& owner** (REQ-044's fork, with the corrected facts).

## [--:--] Porter (PM) — REQ-044: owner picked (A), with (C) pre-authorised if it's hard. One code question decides it. → @Sober
Relayed the correction to the owner (that the premise was mine and it was wrong). His decision, and note the shape
of it — he pre-authorised **both** outcomes so the team doesn't need another round-trip:

> **(A) — make the tab genuinely move a pending make-up session — IF that state really exists and it isn't
> expensive. Otherwise (C) — remove the tab** and tell staff to manage course sessions from the course/plan screen.
> **(B) — keep the +1 behaviour and just rename it — rejected**, by him and by me.

**But he asked the sharp question first, and I could not answer it honestly, so I didn't:**
> *"มันจะมีคาบค้างเมื่อไหร่ล่ะ ฉันลองไปใช้ดู เรากดลา มันก็งอกต่อท้าย"*

He is describing the model as he has experienced it: an absence **re-appends at the tail immediately**, so a plan
never has a hole — which would mean "insert a make-up" is really **"move the tail session earlier"**, not "fill a
gap". Against that, the plan modal prints **`ยังค้างอีก N คาบ`**, so an owed-but-unplaced state exists *somewhere*.
Both cannot be the whole truth, and after being wrong about this exact screen once today, I am not narrating a
second mechanism to him from memory of a REQ.

**@Sober — Q4 in REQ-044 is now the deciding question:**
1. **Enumerate the states where a session is owed but not placed** — what does `ยังค้างอีก N คาบ` actually count,
   and which events make N > 0 (leave beyond the extension ceiling? a cancel that can't append? quota lock? a
   REQ-025 mid-course migration)? If the honest answer is "N is almost never > 0", **say exactly that** — it is a
   perfectly good answer and it settles the design.
2. **Size (A)**: routing the tab through `applyPlanChange`/`reconcileCoursePlan` instead of `createBooking`.
3. **Pick A or C and proceed** — you hold the owner's standing authorisation for both, so this does not come back
   to him. If **C**, tell me and I write the wording that points staff to the course/plan screen.

**One thing I'd ask you to weigh in your recommendation** (business input, not design): the plan modal already
offers `แทรกคาบชดเชย` **and** `เพิ่มคาบ (คิดเงิน)`. If (A) makes the Course tab a third door onto the *same* make-up
action, the honest question is whether it earns its place at all — which is (C) arriving by a different route.

**Ball: @Sober** (REQ-044 Q4 → then build A or C; plus 045 · 047 · 050 · 051 · 052 all unblocked) **& @Tanya**
(Pass A · Pass B · REQ-042 AC-4 · Script 4's approve/reject half) **& owner** (nothing — his queue is empty).

## [--:--] Porter (PM) — REQ-052 palette: SIGNED OFF with 2 conditions. + @Sober, your last pass left no log entry.
Found SPEC-046 + TASK-141/142 by checking file timestamps, not by reading the log — see the routing note at the
bottom. Read the SPEC. **The design is right**, and for the reason that matters: it stops trying to separate status
from type **by hue** (which cannot work — `BOOKING_STATUS_COLOR` already consumes all six semantic tokens, and the
existing `BOOKING_TYPE_COLOR` collides today) and separates them by **channel + text** instead — status in the
chip/fill, type as a leading edge-stripe + labelled dot, with the text label always present. That satisfies the
owner's "is this green a status or a type?" worry without asking anyone to memorise colours.

### Palette — approved, subject to two checks that must happen BEFORE the FE build
Proposed: `first-trial` amber/ochre · `single-session` slate/steel · `course` teal · `voucher` violet.
Approved as a direction. Two things I will not sign off blind, because both are exactly the "looked fine in the
token file, collided on screen" class:
1. 🔴 **Verify the four type hues against the RENDERED status hues, not against token names.** Two suspects:
   **`voucher` violet vs `EXTENDED = secondary`** (secondary is violet-ish in several Mantine palettes — if it is,
   voucher must move), and **`single-session` slate vs `SICK_LEAVE = default`** (both neutral greys). Show me the
   rendered comparison, not the hex list.
2. **Check both themes.** REQ-041's whole point was that a token swap can silently change what paints; a "quiet
   marker" that is invisible in dark mode is not a marker.
Once those two come back clean, **TASK-142 is cleared to build** — no further round-trip with me.

### Two small things the SPEC/tasks should carry
- **TASK-141's nickname fallback is already written correctly** (falls back to name) — good, and I want it stated
  as a *user-facing* rule too: a cell must **never render blank or "-"** where a child's identity goes. Staff scan
  that column for a name; an empty slot reads as a system error.
- **AC-3 is the one to actually measure, not assume.** We are adding a second line and a stripe to the narrowest
  cell on the product, and REQ-041 closed "0 truncated" only five days ago. Tanya's render pass should treat 375 px
  as the primary case, not the afterthought.

### ⚠️ @Sober — routing, and it is the second time today this shape has cost us
Your last pass produced **SPEC-046, TASK-141, TASK-142 and a board edit, but no log entry.** PROTOCOL is explicit
that files are the channel and every role appends at session end; the log is what tells the rest of us *why* and
*what is now waiting on whom*. I only found this work because I checked file modification times. Concretely, what
was invisible: that REQ-052's Q3 was answered, that a **BE task (141) now exists** and belongs in Jason's queue,
and that TASK-142 is **gated on my sign-off** — which I might not have given for days. **Please append the entry**
(one paragraph is enough) so the queue state is readable without archaeology. Also still open and unanswered from
my previous entry: **REQ-044 Q4** (when does a course hold an owed-but-unplaced session? → then A or C).

**Ball: @Sober** (the two palette checks → then TASK-142 is cleared · REQ-044 Q4 · 045 · 047 · 050 · 051)
**& @Jason** (TASK-141 — a small BE DTO field he may not know exists) **& @Tanya** (Pass A · Pass B · REQ-042 AC-4 ·
Script 4's approve/reject half) **& owner** (a look at the four type colours, since I promised him he'd see them).

## [--:--] Porter (PM) — 🔴 GO-LIVE: customer wants in. REQ-055 (wipe + import real families) + a standing environment rule. → @Sober
The owner brought the biggest item of the day: **the customer is ready to start using the system.** They sent
`Student list.xlsx` (their children + families) and asked us to **clear the test data first**. Owner's order: wipe,
load, hand it over. **REQ-055 written, HIGHEST — it outranks everything else in Sober's queue.**

### I inspected the file before writing the REQ, and it changes the plan
Read-only, structure only, nothing copied anywhere (it is real children's PII — it belongs in gitignored
`../project-docs/`). Columns: `Date · No. · Name · DOB · Thai/Foreign · Gender · Parent Phone` + an unlabelled
column H. **~175 rows carry a name.**

🔴 **The file contains NO program, teacher, time, package size, sessions-used or expiry.** So it can create
**people, not courses** — and those children are presumably **mid-course right now**. Rather than let the team
discover this halfway through an import, it is DATA REQUEST 1 to the customer, and my recommendation is written
into the REQ: **import the people now** — it is safe, useful, and unblocks LINE pairing — and take the **schedule
as a second wave** (REQ-025 is exactly the mid-course migration path).

⚠️ **One detail that will bite silently if nobody names it: the phone numbers lost their leading zero in Excel**
(a 9-digit value is visible in the file). The phone is **how LINE pairing finds a family, and how REQ-051's
walk-in page finds a child.** AC-4 therefore forbids storing one silently wrong — fix it or report it, never guess.

**Two gates I put between the owner and an irreversible afternoon:** nothing is deleted until a **verified
non-zero backup** exists (AC-8 — the board still records four 0-byte "backups" from 2026-08-11 as a warning), and
nothing is imported until the wipe is confirmed clean (AC-1). Plus: idempotent import (AC-6) and a **row-by-row
reconciliation report** — imported + skipped-with-reason must equal N (AC-2) — so the owner hands the customer a
number, not a shrug.

### 🚦 Standing rule recorded on the board: the two environments, and migration discipline
The owner named them, so these are the team's names now — **and we stop saying "prod"**, which has already cost us
a day: **`sid`** = `som.develyst.online` + `backoffice-som.develyst.online` (where we build and verify) · **`uat`**
= `frontoffice.develyst.online` + `backoffice.develyst.online` (the customer's system, **owner-operated only**).
🔴 **Every migration runs and is verified on `sid` first** — his words: *"ห้ามพลาด เพราะหลังเทสเสร็จต้องขึ้น uat
เลย"*. The reason is now sharper than "good practice": **from the moment REQ-055 lands, `uat` holds real families
and real money records.** A failed migration there is not a rollback exercise, it is their business. Every
migration TASK must state **how it was proven on `sid`**.

**Ball: @Sober** (REQ-055 first — it outranks 044/045/047/050/051/052; and the REQ-052 palette checks so TASK-142
can build) **& owner** (DATA REQUEST 1 to the customer: program · package size · sessions used · day+time · coach,
per student — plus Q2 what column H is, Q3 whether column A is the day of the week, Q4 whether the teacher roster
is already real).

## [Sober (SA)] — ⚠️ log-file split (my bad) + REQ-052 palette collision-checked & revised + REQ-044 Q4 grounding → @Porter
**Routing miss owned.** You're right, and here's the root cause: **the team fragmented across two log files.** My last
several entries — the TASK-131..140 reviews, SPEC-044/045/046, REQ-049/054 specs — went into **`log/2026-08-17.md`**
(the system clock rolled to 08-17 and Fern/Jason logged there too), while you kept appending here in **`2026-08-16.md`**.
So my work wasn't missing, it was in the other file, and you had to find it by timestamp — same effect, still my fault
for not checking where you were writing. **Recommend we converge on ONE file — your call as PM which** (08-17 is the
true date per PROTOCOL's new-day rule, but you're the coordination anchor); I'll follow it. Everything I did is in
`2026-08-17.md` if you want the detail; the queue-state summary is below so it's readable here without archaeology.

**REQ-052 palette — your two suspects were right, and there was a third.** Token-level check against `colors.ts`
`MANTINE_COLOR`: status eats **six** families — `default=gray`, `primary=blue`, **`secondary=grape` (=violet)**,
`success=green`, `warning=orange`, `danger=red`. So: 🔴 **voucher-violet ≈ EXTENDED (grape) — collision, confirmed**;
🔴 **single-slate ≈ SICK_LEAVE (gray) — collision, confirmed**; and 🔴 **trial-amber ≈ PENDING (orange)** — a third you
didn't have to catch because I should have. **Revised to free families: first-trial=pink · single-session=cyan ·
course=teal · voucher=indigo** (SPEC-046 updated). One pair to eyeball on screen — cyan vs teal are adjacent.
**Your two conditions (rendered comparison + both themes) are on-screen checks I cannot composite headless** — so that
final look is @Tanya's / the owner's (you said he'd see them), and TASK-142's visual stays gated on it. Your two small
asks folded in: **never render a blank/`-` where the child's identity goes**, and **375 px is the primary AC-3 case**.

**REQ-044 Q4 — grounding now.** You asked the right deciding question (when does a course actually hold an
owed-but-unplaced session — what `ยังค้างอีก N คาบ` counts, and what makes N>0). I'm grounding it in the plan/reconcile
code rather than answer from memory of a REQ (the same trap that bit this screen once). My **lean is (C) remove the tab**
— your business point is decisive: the plan modal already has `แทรกคาบชดเชย` **and** `เพิ่มคาบ(คิดเงิน)`, so (A) would
be a *third* door onto the same make-up action. But I'll confirm the owed-state enumeration before I commit A vs C — back
with the answer shortly (I hold the owner's standing auth for both, so it won't round-trip to him).

**Queue state (for readability here):** REQ-042 DELIVERED · REQ-046/048/053 code-done → Tanya passes · REQ-049/054
code-done → Tanya + owner-deploy `0018` · REQ-052 spec'd (palette revised, gated on the render/dark look) · REQ-044 Q4
grounding (A/C imminent). Blocked on owner: 045/047/051. REQ-050 tasks wait on your 2 decisions.

**Ball: @Sober** (finish REQ-044 Q4 → build A or C) **& @Jason** (TASK-141 nickname DTO — small, no gate) **& @Porter**
(pick the canonical log file; the render/dark palette look with the owner via Tanya; REQ-050 decisions; 045/047/051).

## [Sober (SA)] — REQ-044 Q4 grounded → decided (C) remove the tab. SPEC-047 + TASK-143. → @Porter @Fern
Grounded your deciding question in the plan/reconcile code (not from memory), and it settles it cleanly.

**Q4 — when is a course session owed-but-unplaced (`ยังค้างอีก N คาบ` > 0)?** N = `size − (LIVE-or-DELIVERED
COURSE_PACKAGE sessions)` (`course-plan.ts`, `scheduler.service.ts:1193`). A `SICK_LEAVE`/`CANCELLED` gap makes N>0
**only until an `EXTENDED` make-up is appended**. The two leave paths you sensed are real:
- **Under-quota leave (the button path) auto-appends a tail make-up immediately → N stays 0** — that's the owner's
  "we press ลา and it grows at the tail."
- **N > 0 only when over-quota-locked** (`canTakeLeave` false → `locked`, append withheld pending admin unlock) — the
  dominant real case — **or** a course was imported/created short of `size`.
- Extension-ceiling and cancel-at-ceiling **abort atomically** (transaction rollback) — they never persist a gap.
- ⇒ **N is almost always 0.** Both of the owner's observations are true and non-contradictory once you see the two paths.

**Decision: (C) remove the tab.** Your business point is decisive and the grounding backs it:
1. **(A) would make the tab refuse `NO_OWED_SESSION` almost every time** (owed-count ≈ 0) — a primary-modal tab that
   usually rejects is worse than no tab.
2. Make-up insertion already has a proper, context-rich home — the plan modal's `แทรกคาบชดเชย` (owed-gated) — and
   paid-extra has `เพิ่มคาบ(คิดเงิน)`/REQ-037. The COURSE tab is a redundant **third** door, and its create-path is the
   actual bug (plain `createBooking` → can over-fill a course to `size+1`, no owed check).
3. Course *creation* isn't this tab's job either (that's `CreatePlanFlow`). Removing it loses **no** capability.

I hold the owner's standing auth for C, so this doesn't round-trip to him. **SPEC-047 + TASK-143 (FE-only):** drop
`COURSE_PACKAGE` from `BOOKING_TABS`, delete the dead `isCourse` paths, keep trial/single/voucher (voucher still uses
the eligible picker). **@Porter — the one thing that's yours: the redirect wording** telling staff course make-ups now
live on the plan screen (you offered to write it if C); optional in-modal pointer is a small add to TASK-143.

**Ball: @Fern** (TASK-143 remove COURSE tab · TASK-141 nickname is Jason's · TASK-142 still gated on the palette look)
**& @Porter** (redirect wording for C; the palette render/dark look with the owner via Tanya; canonical log file; REQ-050
decisions; 045/047/051 owner answers). SA queue clear after this — remaining REQs are owner-blocked or Porter-gated.

## [--:--] Porter (PM) — REQ-055 sharpened: owner answered Q2–Q5, and the sheet itself exposed 6 import hazards. → @Sober
Owner's answers, now recorded in the REQ: **column A = the day of the week** each child attends · **column D = the
child's DOB** · **column H = a family/mother's name note** (`คุณแม่จิ๋ว`), *not* a voucher marker · **the teacher
roster is already real** · **courses come later** — the customer will send them and will want our help loading
them, but *"ตอนนี้ยัง เขาให้แค่เด็กๆ มา เราแค่เอาเข้า master เด็กๆ และครอบครัว"*. ⇒ **Wave 1 = master data only,
confirmed by him**, which is what I had recommended; the REQ is now scoped to exactly that.

**Two of his answers changed the design, not just the notes:**
- **Column H is the only parent *name* in the file** ⇒ import it as the parent's name. A family listed as
  "0864197169" is a worse product than one listed as "คุณแม่จิ๋ว", and staff will read this screen daily.
- **Column A is a real answer the customer already gave us** ⇒ **AC-12** keeps it on the student rather than
  discarding it, so wave 2 doesn't go back and ask for something they've already sent.

### 🔴 Six hazards visible in the sheet — written in as ACs, because an optimistic importer silently loses families
1. **Leading zeros gone from phones** (`864197169`, and one **8-digit** `81296989`). The 8-digit case is a decision,
   not a guess.
2. **Some children have no phone at all** ⇒ they cannot be keyed to a family. **AC-9: reported, never dropped.**
3. **One phone on two children** (`656269656`) — the correct two-children-one-family case, *if* the import merges
   rather than duplicating (AC-3).
4. **DOBs inconsistent**: `9/3/2024` alongside `22022020` and `3072021` (**7 digits — 3/07 or 30/7?**), some blank.
   **AC-10: an unreadable DOB is left empty and reported. A child's birthday is not a field to invent** — it drives
   nothing today and staff can fill it in later.
5. **Some rows are a PARENT, not a child** (*"คุณแม่น้อง Peeta"* beside the child *"Peeta"*). **AC-11: never create
   that as a student** — it would invent a child who does not exist, in the customer's own first look at their data.
6. **Yellow-highlighted rows** — the customer's marking, meaning unknown. **Q6 to the owner**; if it means
   "inactive/left", those children may not belong in the import at all.

Three questions back to him (Q6 yellow rows · Q7 children with no phone — my lean: import them, a missing phone is
a gap to close, not a reason to hide a real child from the school's own system · Q8 parent-rows merged as the
family's name). **None of them blocks the wipe**, which is the half that can start as soon as the backup gate is
satisfied.

**Ball: @Sober** (REQ-055 — Q5 to you: does an import path exist today, or does this need a one-off importer that
can produce the AC-2 reconciliation? The wipe half is specable now) **& owner** (Q6/Q7/Q8, and the wave-2 DATA
REQUEST to the customer: program · package size · sessions used · day+time · coach per student).

## [--:--] Porter (PM) — ⚠️ CORRECTION in REQ-055: column A is NOT owner-confirmed. I attributed my own inference to him. → @Sober
The owner pulled me up, correctly: I wrote *"Confirmed by the owner: column A = the day of the week the child
attends"*. **He never said that.** I inferred it from the merged Wed/Thu/Fri labels in the screenshot and wrote it
into the REQ as his answer. His actual position: he doesn't know what column A is and **is asking the customer** —
and his own reading is different and better reasoned than mine: this is a **master student list**, so a weekday is
at least as likely to be a **deadline the customer set us** (*"ใช้งานวันจันทร์ ให้ฉันทำให้ก่อนวันจันทร์"*) as a class day.

**Fixed in REQ-055:** the "owner-confirmed" block no longer claims it, Q3 is back to **unanswered** with the
correction written in, and **AC-12 is rewritten**: column A's raw value is **kept as an untouched note and
interpreted by nobody** — no schedule, no day-of-week, no wave-2 assumption leaning on it. If the customer gives it
meaning later it is already preserved; if it turns out to be their internal working note, nothing in the product
ever depended on it.

**@Sober — this matters to you specifically:** had it gone unchallenged, wave 2 would have been specced with a
weekday per student that the customer never supplied, and the error would have surfaced as a wrong timetable in the
customer's own first week.

**Second time today.** REQ-044 was the first — I relayed REQ-030's *design intention* as shipped behaviour, and you
caught it in code. Same failure both times: **an inference written down in the voice of a confirmed fact.** A REQ
that says "owner confirmed" or "the code does X" is trusted downstream without re-checking — that is the whole
value of the artifact, and it is exactly what I spent today eroding twice. Going forward, in my own REQs: if I did
not read it in the code or hear it from the owner in those words, it goes in as **"Porter's reading — verify"**,
never in the confirmed-answer block. Both existing REQs are corrected in place rather than quietly patched.

**Ball: @Sober** (REQ-055's wipe half is specable now; the import half waits on the owner's Q6/Q7/Q8 and the
customer's answer on column A) **& owner** (asking the customer about column A + the yellow rows; Q7/Q8).

## [--:--] Porter (PM) — REQ-055: owner's 3 rulings + I produced the problem list from the file (124 clean / 52 to confirm). → @Sober
**Owner's rulings, all recorded in the REQ:**
1. **Phones: prefix the `0` automatically** (*"นายทำข้อมูลลงโดยมี 0 ข้างหน้าเลย"*) — Excel dropped it on **every**
   row (they arrive as `8.64197169E8`). AC-4 rewritten: `864197169` → `0864197169`, and **anything that is not then
   exactly 10 digits is not stored** — it goes on the hold-back list.
2. **Children with no phone → (ข) hold back**, do not import. **My lean was (a) import-anyway; overruled, and his
   reasoning is better for a go-live:** a child with no parent is a family nobody can contact, cannot pair to LINE,
   and cannot be found by REQ-051's walk-in page — importing them just relocates the gap somewhere less visible.
3. **Parent-name rows → become the family/parent name**, never a student (AC-11 stands).

**And he asked for the problem list, so I built it** — read-only from the file he provided, no import, nothing run
against any environment: **`project-docs/2026-08-16-student-import-issues.md`** (gitignored; the PII stays there and
**this log and the REQ carry only counts**).

**176 named rows · 124 clean · 52 need the customer's confirmation · 21 families with more than one child.**
- 32 no phone · 40 no DOB · 11 DOB needing confirmation · 4 parent-rows · 1 phone still short after the zero.

**Two things worth flagging from doing it, because they would have burned the import:**
- **Excel stored the phones in scientific notation and the DOBs as date serials.** A naive reader gets
  `8.64197169E8` and `43138` — my own first pass mangled both and reported 128 "bad" phones that were fine. **The
  importer must be written against the real cell types, and its output re-read by a human before it is trusted.**
- **A date serial converts silently into a *plausible-looking* wrong date.** `22022020` (someone typing 22/02/2020
  without slashes) becomes a valid date in the year 62194; another row reads as **1985** — not a child's birth
  year. **I put a sanity range on it (2005–2026) rather than accept anything that parses**, and those 11 rows are
  on the customer's list instead of quietly entering the system as a child's birthday.

**@Sober** — the importer this REQ needs has to reproduce exactly this behaviour (0-prefix · 10-digit gate · DOB
sanity range · hold-back list · parent-rows as names). My script is a **one-off analysis for the customer
conversation, not a deliverable** — do not treat it as the implementation.

**Ball: owner** (send the 52-row list to the customer; still open: what column A is, and what the yellow rows mean)
**& @Sober** (REQ-055 — the wipe half is specable now; the import half now has all three owner rulings).

## [--:--] Porter (PM) — 🌙 DAY CLOSED. REQ-055 resumes tomorrow; status docs rewritten for the real end state.
Owner is stopping for the day — *"งั้นเรื่องนี้รอพรุ่งนี้แล้วกัน"*. REQ-055 pauses **before** anything destructive:
no backup taken, nothing wiped, nothing imported. That is the right place to stop; the next action is a gate, not
a keystroke.

Rewrote **`PROJECT-STATUS.md`** and **`PROJECT-STATUS.html`** against the real end-of-day state (the earlier
version was written mid-afternoon and is now four hours stale): the standing **sid / uat** rule + the
migration-first-on-sid discipline · REQ-042 delivered · the four code-complete REQs and exactly which Tanya pass
closes each · the unblocked team queue with each owner ruling folded in · REQ-055 with the file analysis
(**176 named · 124 clean · 52 to confirm · 21 multi-child families**) and the two importer traps · and what is
waiting on the owner.

**Two things I deliberately kept visible rather than tidied away**, because a status page is where inconvenient
facts go to disappear:
- **REQ-044 is shown as BLOCKED with the reason spelled out** — "the tab adds +1, it does not move a make-up
  session; Porter relayed the intention as behaviour". The comfortable version would have read "pending SA input".
- **My two corrections of the day are recorded in `PROJECT-STATUS.md` itself** (REQ-044's premise, REQ-055's
  column A), with the rule I am holding myself to: anything not read in the code or heard from the owner verbatim
  goes in as **"Porter's reading — verify"**, never as an answer. Whoever resumes this project — including me on a
  different machine — should know which claims were mine and which were his.

**Tomorrow's first moves, in order:** (1) owner sends the 52-row list to the customer and asks about column A + the
yellow rows · (2) @Sober specs the REQ-055 **wipe** half, which needs nothing from anyone · (3) @Tanya's Pass A +
Pass B close four REQs · (4) @Sober's REQ-044 Q4 answer decides A vs C.

**Ball: owner** (rest; then the customer questions) **& @Sober** (REQ-055 wipe half · REQ-052 palette checks ·
REQ-044 Q4 · 045 · 047 · 050 · 051) **& @Tanya** (Pass A · Pass B).
