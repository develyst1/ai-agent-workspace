# Log — 2026-08-30 — smart-scheduler

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Tanya (QA) — 🧪 REQ-058 taken off `IN_TEST`: **TEST_PASSED on `sid`**, two ACs I cannot run. → @Porter

Fresh session. Read `PROTOCOL.md`, `QA.md`, `board.md`, `log/2026-08-29.md`. Nothing in yesterday's log was
addressed to me (last order to QA was the REQ-004 rental retest, closed at 13:52 with `TEST-062`; the day ended
with the ball on the owner and @Sober). So I took **the one row on the board still sitting in my status**:
**REQ-058 — `IN_TEST`**.

### Why it was worth running rather than closing on the data
The board's evidence for REQ-058 was *"19 live both boxes, `sid`=`uat`=19"* — a **row count**. REQ-058's own
AC-2 and AC-3 ask for something a count cannot answer: *is each program selectable when you create a course /
single session / trial / voucher*, and *does booking one to a teacher actually succeed*. Neither had ever been
exercised. That is the exact shape of the gap this role exists for, so I ran it in the product.

### Result — `tests/TEST-063-req058-nine-programs-bookable.md`

**All nine are real and work.** On the deployed `sid` build:
- **AC-1** — all nine present with the customer's exact spelling, no near-miss (`Bike` · `Balance Cruiser` ·
  `Balance Bike` · `Scooter` · `Inline Skate & Bike` · `Surfskate & Bike` · `Surfskate & Inline Skate` ·
  `Bike & Scooter` · `Surfskate & Freeskate`). 19 selectable programs in total.
- **AC-2** — selectable on **all four** paths: Course · 1 HR · 1st Trial · Voucher. The Voucher list shows
  **16**, correctly dropping `Onewheel E-Skate` + both `Balance Play` — REQ-027's exclusions still hold, and
  all nine are eligible.
- **AC-3** — **actually booked, not just listed:** `Bike & Scooter` as a 1 HR (Bank, 31 Aug) and
  `Surfskate & Freeskate` as a 1st Trial (Bank, 1 Sep). Both saved, both rendered on the calendar, both
  cancelled afterwards. Bank is linked to all nine ⇒ every new program has a teacher.
- **AC-4** — `Surfskate & Inline Skate` prices **4,790 / 6,490 / 9,790** at sizes 4/6/10, "price includes VAT",
  leave quota 1→wk5 · 2→wk8 · 3→wk13. Byte-for-byte the customer's `bike-skate` card.
  📌 **And the 1-hour gap REQ-058 itself flagged is closed:** a 1 HR on `Bike & Scooter` prices at **฿1,390**
  and saves. REQ-058's note 3 said `bike-skate` had no 1-hour price; REQ-066/TASK-174 evidently landed. This is
  the running-system proof of it.
- **AC-5** — pre-existing programs untouched; `Surfskate` (pre-existing) still 6,490 at size 6, i.e. the nine
  joined the line without moving it; the KEPT combined `Bike / Scooter / Balance Cruiser` is still selectable
  and still carries its live course.
- **AC-6** — `Surfskate & Skateboard`, added by the owner on 08-29, is live in **all four** pickers.

### 🔴 What I could NOT test — naming the gaps rather than rounding up
1. **AC-9 / AC-10 (`teacher-subjects:link-all` bulk + dry-run) — `NOT_TESTED`.** That is a CLI run on the
   server. Running it would make me an operator of `sid`, which my charter forbids. It needs the owner.
2. **AC-3 was executed on 1 HR and 1st Trial only.** Course and Voucher are proven *selectable*, not *booked* —
   a course has no delete and a voucher sale writes a `bo.movement` I cannot reverse, so the footprint was not
   worth the marginal evidence once two types already passed.
3. **`sid` only.** Nothing here is evidence about `uat`.

⇒ **Verdict: `TEST_PASSED`**, board row moved off `IN_TEST`, with those three limits written into the verdict
so nobody reads it as more than it is. **Whether that is enough to call REQ-058 `DELIVERED` is yours**, and I
would not do it before the AC-9/AC-10 answer.

### Two things I saw in passing — neither is a defect, both are worth your eye
- **The cancel dialog says nothing about posted revenue.** Exactly the hole you ordered work on yesterday
  ("warn when cancelling a booking whose revenue has ALREADY posted"). Confirmed **not yet built** on `sid`.
  My rows had no posted sale, so nothing was mis-stated to me — this is corroboration of your finding, on a
  running system.
- **🔴 TASK-220 is still unverified on `sid`.** `Cancel booking` **is** present and works on a `1st Trial` — I
  used it to clean up. But Sober's stated check is *cancel an **ATTENDED** first trial* (→ CANCELLED + reason
  stored + freelance hold released). Mine was **PENDING**. Marking one ATTENDED posts revenue at day-end and I
  cannot reverse a `bo.movement`, so I did not do it on my own initiative. **If TASK-220 goes to `uat` on the
  strength of "Fern's half + Jason's line + tsc 0", it goes without a QA sign-off** — say the word and I will
  run it, but I need to know the money row is acceptable residue first.

### Footprint — declared in full
Two bookings created on `sid`, **both CANCELLED and verified CANCELLED** in Bookings → All bookings. No new
student or parent (I reused the QA-owned `QA-req072-fixture` precisely because students cannot be deleted).
**No `bo.movement`, no LINE message, no script, no restart, no setting changed, no row touched that I did not
create, and no contact with `uat` of any kind.** Full ledger appended to `tests/DEV-SERVER-FOOTPRINT.md`.

### Three questions, all in `TEST-063` — @Porter
1. Have the owner re-run `teacher-subjects:link-all` on `sid` **dry-run first**, and paste the counts
   (teachers × programs × created/skipped, then **0 created** on a re-run)? That closes AC-9/AC-10 with
   evidence. I can read the output; I cannot produce it.
2. Confirm AC-6's *"no code change, no redeploy"* half — I can see the 20th program live, but the "no deploy"
   claim is your 08-29 entry, not something I ran.
3. Do you want the **ATTENDED**-first-trial TASK-220 check run, and is the resulting money row acceptable
   residue (or will the owner reverse it in the backoffice)?

**Ball: @Porter.** Nothing else on the board is waiting on QA — REQ-020 and REQ-023 are still blocked on the
**isolatable LINE test recipient** the human owes (`../project-docs/`), and REQ-028's open item is a DATA
REQUEST with the owner, not a test.

## [--:--] Jason (BE) — TASK-223 BUILT → REVIEW. **The revoked policy was also in the console, not just the header.** → @Sober
Comment + console output only. No schema, no flag, no migration, and the script was **not executed** — not even
a dry run. tsc **0** · `bun test` **970 pass / 0 fail** (+5).

### 🔴 Two lines beyond the two you named — and they were the loud ones
You named `scripts/link-all-teacher-subjects.ts:2` and `:13`. The **operator-facing `console.log`s** carried the
same revoked policy, and those are what someone reads at the moment of deciding:
- **`:58`, the closing line of a successful run** ended with *"…don't forget to run on both sid and uat"*.
  **The last words on screen after a successful run told the operator to go and do the dangerous thing.** Now:
  `uat` is off-limits, a program is added there by an owner-approved named list.
- The DRY RUN footer now ends `--commit (เฉพาะ sid)`.

In scope by DoD box 1 — *"neither file states **or implies**"* — and a `console.log` states it far more loudly
than a header nobody has open. Flagging it because it is more than the two line numbers you listed.

### The revoked wording is now absent from the repo — including as a quotation
My first draft quoted the old sentences verbatim to explain what changed, and **the test I had just written
failed on my own header.** That is the right answer: a grep for the revoked wording must find nothing, or the
next person greps, hits it, and reads the old policy out of context. Both files now describe what used to be
claimed without restating it, and a test enforces it.

### Item 3 is IN — Porter has not struck it
Built as you wrote it: one line above the plan, **dry-run path only**, saying `sid`-only · **can never unlink** ·
`uat` = named list. ⚠️ **Consequence:** a run that goes straight to `--commit` prints no warning; only the header
covers it. That follows your wording and I did not widen it — one word (`if (!commit)`) is the difference.
**Your call.** A test also asserts **no box-detection guard was added** (it greps for `process.env.NODE_ENV`,
`DATABASE_URL`, `isUat`, `--force`) — your 🚫 written where it cannot be quietly undone.

### ⚠️ Four TODO rows still sit against my name and I did NOT start them
`TASK-103` · `TASK-110` · `TASK-116` · `TASK-117` — all marked POST-GO-LIVE / SPEC-028·032·034, parked for
weeks while every recent hand-off says the lane is clear. **I am not treating a stale board row as a live
order**, and I am not silently ignoring four TODOs either: **@Sober, tell me to pick them up, or re-mark them
`PARKED` so the board stops claiming work is waiting on me.** Ambiguity there is how one of them gets started
on the wrong week.

**Ball: @Sober** — TASK-218, TASK-221, TASK-223 all in REVIEW. Two decisions are yours before FE moves:
the `discountMinor` **sign** (gates TASK-222) and item 3's **scope**. Nothing else is waiting for me.

## [--:--] Porter (PM) — answering @Tanya's 3 (one I can close myself), + ⚠️ my earlier entry today was overwritten.
📌 **Housekeeping fact, not a complaint:** I wrote a Porter entry into this file earlier today; the file was then
**recreated with the header** by a later session and my entry is gone. Append-only means *append* — create the
day's file only if it does not exist. Restating the load-bearing parts here so they are not lost twice.

**@Tanya — your Q2, I can answer without the owner: YES, no code change and no redeploy.**
`Surfskate & Skateboard` went in on 08-29 via `subjects:add --group bike-skate` + a named-list link INSERT —
scripts against the DB, no build, no `pm2 restart`. That is my own record of running it (`log/2026-08-29.md`,
last two entries), so treat it as PM evidence, not a test result. Your Q1 and Q3 need the owner; routing both.

**@Tanya — your TASK-220 catch is the important one and I am not shipping past it.** The board says `DONE`
(Sober, 08-29) and `DONE` means *built*, not *works* — the ATTENDED first-trial path has never been run.
⇒ **TASK-220 does not go to `uat` without your sign-off.** The money-residue question is the owner's, not
yours to absorb: asking him whether a `bo.movement` row on `sid` is acceptable residue **before** you run it.

**Outstanding with me/the owner** (nothing here is waiting on @Sober, @Jason or @Fern):
1. REQ-028 rental — the ledger `SELECT` (checks 1 & 5) · 6 QA rental rows on `sid` with no reversal endpoint.
2. TASK-220 — deploy to `sid` → @Tanya's ATTENDED check → UAT gate. **Not deployed. Not QA-verified.**
3. REQ-058 — AC-9/AC-10 need the owner's `link-all` dry-run counts on `sid` before I call it `DELIVERED`.
4. The `uat` remote-DB whitelist line (board ⚠️ 08-16) — LINE work is done ⇒ close it and verify closed.
5. REQ-005 §OTHER — parked; his two remaining answers are recorded as owed in the REQ file.
⚠️ **board.md is 42.3KB, past the 40KB hygiene line** — compaction is Marie's, flagging it rather than trimming.

**Ball: owner.** @Sober has TASK-218/221/223 in REVIEW + two calls @Jason named (the `discountMinor` sign, item-3 scope).

## [--:--] Porter (PM) — 🔴 I answered "what is outstanding" twice without reading `PROJECT-STATUS.md`. It is MY file.
The owner had to point me at it. My startup was PROTOCOL → PM.md → board → log; **the resume-here file was not in
it**, and it is the one artifact written specifically so a fresh session knows the whole picture. Both answers I
gave were therefore **incomplete, not wrong** — and incomplete is what this project keeps getting hurt by.

**Six open items live only in `PROJECT-STATUS.md` and appear on no board row I had read:**
`ชวินท์`'s ฿9,790 sale never posted · **REQ-062** blocked on the owner's Q4 · **REQ-057 cleanup tool built but
never run on `uat`** (the `Test` / `SOM Team` household is still in the customer's system) · the `sm-jobs` secret
in **plaintext, committed to git** · LINE reach 2/20 teachers, 0/180 parents · and the four **known-unverified**
items, including ⚠️ **a manual reminder trigger before 08:15 silently eats that day's reminders for real families
on `uat`.**

**Two of my own files disagreed and I own both.** REQ-063: the REQ file said `READY_FOR_SA`, the board said
`TEST_PASSED`, `PROJECT-STATUS` listed it live on `uat`. Reconciled in the REQ file: **code live, requirement
NOT `DELIVERED`** — the owner's assumptions are still unconfirmed by the customer.

**Fixed:** `PROJECT-STATUS.md` now carries a `2026-08-30` section (REQ-058 verdict · TASK-220 never deployed ·
TASK-218's migration `0028` · TASK-221/222/223 · the board-size FAIL · the log overwrite).
**Added to my own startup ritual: `PROJECT-STATUS.md` comes before the board, not after the log.**

## [--:--] Porter (PM) — 📋 The owner gave me the CUSTOMER-FACING list. It is now `OWNER-LIST.md`. → @Sober @Tanya
He had to re-explain the team and his own numbering to me. **That is a PM failure, not his.** His list
(REQ-001…012 · REQ-BO-001…006 · FIX-001…007) existed only in `../project-docs/2026-08-23-timeline-current-picture.md`
— **stale on status since 08-23** — and nowhere in `ai-worker/`. Now written verbatim + mapped to board numbers in
**`ai-worker/OWNER-LIST.md`**, and pointed at from `PROJECT-STATUS.md`.

**The real headline: 🏁 ระยะที่ 1 (frontoffice) is 10 of 12 done.** His 08-23 plan estimated 16–19 rounds left in
that phase; **two items remain** — REQ-004 (Testing) and REQ-005 (ToDo). His own sequencing rule says the
backoffice phase waits for a stable frontoffice ⇒ **that gate is about to open, and nobody has planned the six
`REQ-BO` items.**

**Three disagreements between his list and our records — all mine to close:**
1. **REQ-009 "Completed" but a 1st Trial still cannot be cancelled on `uat`** — TASK-220 code-complete, **never deployed**.
2. **REQ-003 (ส่วนลด) "Completed"** while REQ-063 records the four requirement answers as **his assumptions,
   unconfirmed by the customer.**
3. **REQ-001 "Completed"** while REQ-058's AC-9/AC-10 are `NOT_TESTED` (owner-run CLI).

**@Sober — no order yet, and deliberately so.** The next lane is the owner's call between REQ-005 and the
`REQ-BO` block; I am putting it to him rather than picking for him. What I *will* ask you for, once he answers,
is a **scoping pass on the six `REQ-BO` items — sizing and order, not a build.** Note REQ-BO-006 is REQ-009's
twin and the reason enum already exists.

## [--:--] Porter (PM) — ✍️ Owner: *"พิมพ์ไม่รู้เรื่องเข้าใจยาก"*. Rule written into PM.md.
Three long answers in a row. The lesson was already in `PROJECT-STATUS.md` for log entries and I did not
apply it to chat. Now a standing rule in `PM.md` §"How Porter writes to the owner": under 15 lines, plain
Thai, lead with what he must decide, one decision at a time, reasoning stays in the files.

## [--:--] Porter (PM) — ⚡ ORDER: the owner's lane is **finish the frontoffice**. → @Sober
His words: *"เก็บหน้าบ้านอ่ะ ให้จบ"*. Not the backoffice block. Two items close ระยะที่ 1 (see `OWNER-LIST.md`):

**1. REQ-005 "booking type OTHER (อื่นๆ)" — this is the lane.** 3 of your 5 questions are ANSWERED and now
written in `requirements/REQ-005…md` §"Owner's answers + PARK": flexible per instance (charged or not,
consumes or not, with or without a student); day-end auto-attends it like everything else.
**2 still open and I am asking him now:** (a) if charged, what price mechanism — typed amount vs catalogue
item; (b) what names the calendar cell + the teacher's LINE line when there is no student.
Ground what is settled; leave those two as explicit holes. **Do not invent either** — how you sequence it is yours.

**2. Owner-side, no work for you:** REQ-004 needs one `SELECT` from him to close (`tests/TEST-062`), and
**TASK-220 still needs deploying** — until it does, his REQ-009 "Completed" is not true for a 1st Trial.

## [--:--] Porter (PM) — ✅ DATA REQUEST answered by the owner. @Tanya — checks 1 & 5 are yours to sign.
Result saved verbatim: `../project-docs/2026-08-30-data-request-rental-ledger.md`.

**6 rows on `sid`.** Values match SPEC-031's table (set 20000 · ride 15000 · helmet 5000 · pads 5000 ·
ride3 45000 = 3h × ฿150), `revenue_kind = RENTAL` on every row, and the `-dupe` key appears **exactly once**.

🔴 **I am NOT calling this a pass.** Reading a query result is not a verdict — only you set `TEST_PASSED`
(`PROTOCOL.md`). Please sign or reject checks 1 & 5 in `tests/TEST-062` and give the REQ its status.

📌 **He also ran it on `uat`: no rows.** Two separate facts: (1) your footprint never reached the customer's
system — good; (2) it says **nothing** about rentals working on `uat`, which has never been exercised there.
If the owner ever wants `uat` covered, that is a fresh question, not an extension of this one.

⚠️ The 6 rows stay on `sid` (no rental-reversal endpoint). Keys are in the file if he wants them removed.

## [--:--] Porter (PM) — 🔴 STOP on REQ-004. + two new REQs captured. → @Tanya @Sober
**@Tanya — do NOT sign checks 1 & 5, and REQ-004 does not move toward Completed.** The owner relayed
*"เรื่องของเช่า rent ยังไม่ถูกนะ ลูกค้าแจ้ง นำกลับมาแก้"* **on the same day** the ledger came back matching
SPEC-031 exactly. Both are true: **we verified the mechanism; the customer is reporting the outcome.** Your run
was not wrong and neither was the query — the spec is what is in question. Your evidence stands as-is; hold the
verdict. **What is wrong is not yet known** and I am asking for the customer's own words rather than guessing.

**@Sober — no order from this yet.** Two new REQs are captured as DRAFT, deliberately without acceptance
criteria, because every AC would encode a guess:
- **`REQ-076`** (owner's REQ-013) — pause a 1HR / Voucher / 1st Trial like a course pause, plus a box of paused
  items an admin can re-schedule any time. **7 questions**, the sharp one being what happens to money already
  posted, since a 1HR / 1st Trial bills at day-end.
- **`REQ-077`** (owner's REQ-014) — the real LINE OA, rich menu, staff replying by hand, and a complete
  notification set. **The customer still owes the notification list.** 🔴 And the fact that decides its worth:
  **reach is 0 of 180 parents** — every parent notification in it currently reaches nobody. Raised to the owner
  as a precondition, not a sub-task.

⚠️ New scope arrived while REQ-005 is mid-build. **I am not re-ordering the lane on my own** — the owner ranks it.
⚠️ Neither REQ is on `board.md`: it is still over the hygiene limit (40.8KB, FAIL). Rows go on after compaction.

## [--:--] Porter (PM) — 🔢 The owner set the order. REQ-005 → REQ-013 → REQ-014 → REQ-004 → backoffice. → @Sober
*"หลังบ้าน ไว้หลังเลย ทำหน้าให้จบก่อน"*. Written into `OWNER-LIST.md` §THE ORDER.

**@Sober — REQ-005 stays your lane, unchanged.** Nothing above re-orders what you are on. Next after it is
`REQ-076` (พักการจอง), and I will not hand it over until its 7 questions are answered — the sharp one is that a
1HR / 1st Trial **bills at day-end**, so "pause after it was attended" is a money decision, not a UI state.

🔴 **`REQ-077` (LINE) — do not treat it as "extend the LINE work".** The owner corrected the premise today:
*"admin เขาจะสิงไลน์นั้นแหละ ไปตอบลูกค้า"* — a **human lives in that account**. Everything we built assumes a bot
talking to parents with nobody else in the thread. He is not asserting it is wrong (*"อาจจะ"*), he is refusing to
assume it is right. ⇒ **a design conversation precedes scope.** Captured in the REQ file.

📌 **REQ-004 is deliberately parked to LAST by him** — *"ยังไม่ต้องหยิบมาคุย"*. @Tanya, that confirms the hold:
no signature, no verdict, and it is not a queue you are blocked on. Nothing for QA on it.

## [--:--] Porter (PM) — ➕ REQ-051 enters the queue as the owner's **REQ-015**. Order updated. → @Sober
He asked for a number in his own list: **REQ-015 = หน้าเช็คอิน QR walk-in** (= board `REQ-051`). Next free
number in his frontoffice list is **REQ-016**. Written into `OWNER-LIST.md`.

**Final order: REQ-005 → REQ-013 → REQ-015 → REQ-014 → REQ-004 → the `REQ-BO` block.**

**@Sober — still no change to your lane (REQ-005).** For when REQ-015 arrives: SPEC-050's architecture is
already yours and done; it is blocked on **the owner's three risk decisions**, not on design. I hold those and
will bring them to him at the front of the queue — admin code (the only one today is the shared static `229`),
the rate-limit model (nothing exists in the codebase), and whether a public no-login check-in that **burns a
paid session** is acceptable at all. Do not cut build tasks before they land; you were right to hold in August.

## [--:--] Porter (PM) — 📝 Rewrote the owner's customer-facing note (he pastes it into the customer's LINE).
Added **REQ-013 · REQ-014 · REQ-015** in his format and his voice, customer-readable, no internal vocabulary.
**One status changed: REQ-004 `In Progress (Testing)` → `In Progress`** — the customer themself reported it is
wrong, so "Testing" would tell them we are still checking something they have already judged.
**REQ-009 left as `Completed`** on purpose: its written scope is 1HR/Voucher and that scope IS done. TASK-220
(1st Trial) was never in the customer's sentence — correcting the note there would invent a promise.
UX-writer note: kept his numbering, his line breaks, his words. Nothing else touched.

## [--:--] Porter (PM) — ✅ The customer's notification list arrived. **5 of 6 already exist.** → @Sober @Tanya
Verbatim + gap analysis in `requirements/REQ-077…md`. The headline for the team:

**Only ONE of the six is new work** — the parent's **check-in message** (น้อง<ชื่อเล่น>เข้าเรียน · คอร์ส · ครู ·
เหลือกี่ครั้ง · วันหมดอายุ). REQ-050 today names **child + session time only**; the remaining count, program,
teacher and expiry are in no check-in message anywhere.
The other five shipped between 08-22 and 08-29: course-confirm to parent (REQ-072) · 08:15 daily (REQ-072 3B) ·
teacher course+booking confirm (REQ-072 / TASK-219) · teacher daily schedule (same job, FIX-004/REQ-067B) ·
leave notification (REQ-049).

🔴 **"Built" is not "working", and two of those five are flagged known-unverified:** the **08:15 job has never
been watched actually delivering** (it carries TWO of the customer's six asks), and **REQ-049's firing was never
re-verified**. **@Tanya — when the owner gives the word, watching one real 08:15 run is the cheapest and highest
-value thing on this REQ.** I am not ordering it yet; the owner's lane is REQ-005.

🔴 **And the reason the customer has not noticed any of it: 0 of 180 parents are LINE-linked.** Two of their three
parent notifications have been live for days and reached nobody. **The deliverable here is linking, not messages.**
That is now the customer's own evidence, not my argument — put to the owner as Q8.

## [--:--] Porter (PM) — 🔴 Owner overruled my LINE-reach argument. I was answering the wrong question. → @Sober
*"ช่างมันเถอะหน่า ลูกค้าลองใช้ที่ sid อยู่ หมายถึงลูกค้าเรานะ ไม่ใช่ลูกค้าของลูกค้า"*

I argued twice in `REQ-077` that "0 of 180 parents linked" made the notification work worthless. **Wrong
audience.** The people evaluating are **the customer's own staff, on `sid`** — not 180 families on `uat`. My
position is struck in the REQ (kept, marked superseded, nothing deleted). Lesson recorded there: a true number
can still yield a false conclusion when the population behind it is the wrong one.

⚠️ **One real thing falls out of the correction, and it is not a re-litigation of the reach point:** our board
records the **LINE webhook as pointing at `uat`**. If the trial is happening on `sid`, either a second OA/webhook
exists that we never wrote down, or LINE cannot be exercised on `sid` at all. **Asked the owner — not guessed.**
Getting it wrong means the customer presses buttons, sees nothing, and concludes the feature is broken.

## [--:--] Porter (PM) — 📌 How LINE testing works here, from the owner. Recorded. → @Tanya @Sober
*"webhook point ไป uat ค้างไว้รอตลอด … จะเทสไลน์ เราจะค่อยปรับกลับมา แปปๆ ตอนดึกๆ"* — the webhook sits on `uat`
permanently; he flips it to `sid` **briefly, late at night**, owner-only, then flips it back.
Written into `REQ-077` and `PROJECT-STATUS.md`.

**@Tanya — this is the concrete shape of the LINE blocker you have carried since 08-04.** You cannot schedule a
LINE **inbound** test yourself; it happens inside his window. But the distinction matters and I had been sloppy
about it: **inbound** (linking, เช็คอิน tap, ลา, rich menu) needs the webhook; **outbound** (course-confirm,
booking-confirm, the 08:15 reminder) is a push on the channel token and does **not**. Most of the customer's
six-item list is outbound ⇒ possibly testable without a window.

🔴 **One question to the owner before anyone acts on that:** does `sid` share `uat`'s LINE channel/token? If yes,
an outbound "test" from `sid` messages **real linked people** — and 2 real teachers are linked. Until he answers,
nobody fires an outbound LINE test from `sid`.

## [--:--] Porter (PM) — ✅ REQ-005 UNBLOCKED → board **REQ-078**, `READY_FOR_SA`. @Sober: please pick it up.
The owner closed both open answers: **price = BOTH** (typed amount *or* catalogue item — his option ค, a third
choice I had not offered) and **the studentless booking is named by the admin typing a title.**

**`requirements/REQ-078-booking-type-other.md`** — full ACs written, 15 of them, including the negatives (no
title · bad amount · **both price sources set at once**) and two regressions (the four existing types unchanged;
the REQ-052/068 calendar cell still correct and an อื่นๆ cell visually distinguishable). Wording table included —
**the admin's typed title is the display name; the system must never substitute "อื่นๆ" for it.**

It moved out of `REQ-005-standalone-teacher-management.md` into its own REQ, as I said it would the day he
unparked it. That file now points here. **The owner still calls it REQ-005** — use his number when talking to him.

⚠️ **It needs a migration** (`booking_type` enum has no `OTHER`) ⇒ standing rule: **`sid` first, verified.**
⚠️ **FE is on the shared `smart-scheduler-front`** — read `develop` before building.

📌 **One question is open and deliberately NOT blocking:** does an อื่นๆ booking notify the teacher on LINE. My
lean is in the REQ (teacher assigned ⇒ yes; no teacher ⇒ nothing). It is one branch at the end; answer needed
before delivered, not before started.
