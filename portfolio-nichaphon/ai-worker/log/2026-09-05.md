# Log — 2026-09-05 — portfolio-nichaphon

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [00:31] Porter (PM)
- Date from the dispatcher clock: **2026-09-05**. Inbox PM: **9** from Sober read, acted, **deleted**
  (each one placed or routed). One unit: **the REQ-002 acceptance pass**.
- **REQ-002 stays `SPEC_DONE`, NOT `DELIVERED` — 1 of 8 AC ticked.** AC4 (quotes) met on TASK-011 §3.
  Every other criterion adjudicated in writing; none passed on silence.
- **Owner's:** AC2 (he has not seen the site), AC5 (the 14-label letter-case change — whether case is
  "altered wording" under R4 is his rule to read, not mine), AC7 (held on Q19).
- **Two gaps in my acceptance evidence, neither a defect nor a failing of TASK-011:** AC3 —
  REGRESSION H1/H6/H7/H8 last ran 2026-08-30, before TASK-006…012 landed, and AC3 says "re-running,
  not asserted"; AC6 — R9's strings were never checked on the five non-Home routes. Fern's
  byte-identical A/B proves REQ-002 *introduced* nothing; it cannot prove absence.
- **AC1 / AC8 PARTIAL:** greps, build, console and links are strong and Sober re-verified them, but
  AC1's *look* half is unseen by anyone and every link click was at 1280 — **360 nav verified by nobody**.
- **TEST-004 requested from Tanya:** full REGRESSION S1-S13 + H1-H8, the R9 sweep on six routes, the
  7 SQ8 eyes. Outcomes only, method + verdict hers; TEST-003 stays separate. @Tanya: it is with you.
- **DEF-1 is not called fixed** — S13 stands FAIL 2026-09-03 and its re-run is the authority.
- @Sober: nothing waiting; FYI only — AC6 has no check in any SPEC/TASK, so it went to QA, not to you.
- No git write, no deploy, no code, no spec/task file touched. Next: the human, in Thai.

## [00:39] Porter (PM)
- Inbox PM: **empty** (nothing new). One unit: **N5 opened as `REQ-003`** after the owner
  re-raised it — `เมื่อไหร่จะอัปเดต portfolio ฉันเสร็จ` + the same four repos and two live URLs,
  byte-for-byte the material he handed over on 2026-09-02.
- **Status `DRAFT — BLOCKED (waiting: Human — Q20, Q21, Q22)`. Not `READY_FOR_SA`, and it carries
  no acceptance criteria on purpose:** none can be made testable while the facts do not exist and
  permission does not exist. Sending it to Sober now would produce a spec that invents his career.
- **Settled without asking:** scope (Q16 `ทั้งเว็บ`) and material (Q17). The 2026-09-02
  `ong-match-front`/`-back` link typo is **CLOSED by his own words** — today's message lists all
  four repos as plain text. Not a guess; his message is the source.
- **New: Q22, a DATA REQUEST** — his role · dates · client · result, per project. Needed whichever
  way Q21 goes, because code cannot supply those four.
- **Not resolved by assumption:** re-sending source code *reads* like "the team drafts", but that is
  Q21's answer and it is his to give. Written into the REQ, not acted on.
- Still nobody has cloned or read any of the four repos. No git write, no deploy, no code, no
  spec/task file touched.
- @Sober: FYI only, **do not pick up REQ-003** — it is DRAFT/BLOCKED on the owner, not on you.
- Next: the human, in Thai — Q20/Q21/Q22, plus the honest answer to "เมื่อไหร่เสร็จ".

## [01:00] Tanya (QA)
- Inbox QA: 3 messages read, acted, **deleted**. One unit: **TEST-004** — the REQ-002 acceptance
  round. Ran on **localhost:3021** (port 3000 held by an orphan `next` for this repo, PID 8508 —
  not mine, not touched); my server stopped, `front/.next` left as found (dev output, no BUILD_ID).
- **Verdict `TEST_FAILED`.** Not rounded up: **AC6 PASSES** (R9 clean — 0 hits in 96 text + 96
  source checks across all six routes, both viewports; never checked off Home before today).
  **AC3 does not**: 20 of 23 checks re-run and pass, but **H8 FAILS** and H5 + S11 are NOT_TESTED.
- **DEF-1 is CLOSED on evidence.** All 9 `/about` lightbox images painted at 1280 and 360 —
  non-zero box AND decoded pixels, legible in the shots. S13 flips FAIL→PASS. Porter may now say
  "fixed" to the owner; that sentence was waiting on this run.
- **DEF-2 (MAJOR, new)** — Home at 360x740: CTA 2 "Get in touch" is sliced by the fold (706–750)
  and the hero quote is off-screen (782–809). H8 passed 2026-09-02. **I name no cause** — I did not
  test a pre-step-up tree and I do not reason about git. **DEF-3 (MAJOR, new)** — `/services` at
  360: an 887px table in a 303px scroller, only the "Service" column visible, no affordance.
- **All 7 SQ8 eyes answered, all positive** — incl. **SQ12 settled in Sober's favour** (the field
  border does change on focus; Fern's reading was taken unfocused) and mobile nav verified at 360.
- What made the difference: headed Chrome + real scrolling + real `Tab`. Written into REGRESSION.
- @Porter: QQ4 (H8 — regression or re-scope?), QQ5 (why S11 was left to TEST-003), QQ6 (DEF-3
  scope), QQ7 (H5 has no baseline and is unrunnable as written). TEST-003 still open and separate.
- No product code touched, no REQ/SPEC/TASK edited, no git, no deploy, production never opened.

## [02:10] Porter (PM)
- Inbox PM: **1** from Tanya read, acted, **deleted**. One unit: **the TEST-004 intake**.
- **Verdict `TEST_FAILED` accepted exactly as written.** No check re-scoped, nothing rounded down.
  **QQ4-QQ7 all answered** in the TEST file, none deferred and none answered by assumption.
- **AC6 → TICKED** (0/96 text + 0/96 source, six routes, both viewports). REQ-002 is now
  **2 of 8** AC ticked; it stays `SPEC_DONE`, still **NOT** `DELIVERED`.
- **DEF-1 CLOSED on evidence** — S13 FAIL→PASS, nine `/about` images painted at both viewports. The
  sentence "it is fixed" was gated on this re-run since 2026-09-03; the owner is told today.
- **DEF-2** (Home hero cut at 360x740, fails owner-accepted H8) and **DEF-3** (`/services` table
  strands 2 of 3 columns at 360) recorded in REQ-002 §TEST-004 intake. **No cause named by anyone** —
  no pre-step-up tree was tested and no role reads git. Owner decides fix-vs-accept: **Q23 / Q24**.
- **S11** stays TEST-003's (QA's routing round the orphan on port 3000 was correct). **H5 is
  unrunnable as written** — no baseline exists; I do not count it against AC3; rewrite is Tanya's
  own call in her own file, and the owner is told in one line (**Q25**).
- **SQ12 settled in Sober's favour**; SQ7's drawer half works for a real visitor at 360. The SQ7
  modal-look gate is untouched and still Sober's.
- @Sober: DEF-2 + DEF-3 routed as REQ-level concerns — **not REWORK, not a task order**, and both
  held pending Q23/Q24 so nothing is built on my assumption. @Tanya: next unit is **TEST-003**.
- No git write, no deploy, no code, no spec/task file touched.

## [01:20] Porter (PM)
- One unit: **hygiene housekeeping + the owner's five answers relayed**. No spec/task/test/code file
  touched, no git write, no deploy. Inbox PM: empty, nothing to delete.
- **Gate FAIL (3) → `RESULT: PASS (5 warning(s))`.** Fixed exactly the three FAILs, nothing else.
- **Archived verbatim before compacting, both diffed byte-for-byte:** dispatcher runs -a/-b/-c → `archive/`
  (8 runs → 5); REQ-001 → `archive/REQ-001-…-2026-09-05-preconsolidation.md` before consolidating its
  §New asks (45.3KB → 41.6KB) — **R1–R10, every AC and every canonical quote string byte-identical**;
  every shortened fact found alive in REQ-002/REQ-003 first. 2 board cells → pointers, likewise verified.
- **Owner answered five items**, recorded verbatim before interpreting — REQ-002 §Owner's answers —
  2026-09-05: `AC2=ผ่าน เท่ขึ้นมาก` · `Q23=แก้ก่อนส่ง` · `Q24=ใส่สัญญาณเลื่อน` · `AC5=รับได้` · `AC7=เอาออก`.
- **REQ-002 now 5 of 8 AC ticked** (AC1/AC2/AC4/AC5/AC6), still `SPEC_DONE`, still NOT `DELIVERED`.
  **AC1's tick is mine, not his words** — declared in the file so he overrules it in one line.
  **AC7 NOT ticked** though Q19 closed: "toggle is dead code" is a source read and H1 ran on Home only.
- @Sober: **hold lifted** — DEF-2 fixed before DELIVERED, DEF-3 gets a scroll signal, Q19 lands on the
  five-route shell. Not a REWORK, not a task order; no cause named, DEF-3's provenance stays UNKNOWN.
- Still the owner's, untouched here: **Q18**, **Q25**, REQ-003 **Q20/Q21/Q22**.

## [01:31] Sober (SA)
- Inbox SA: **4** from Porter read, acted, **deleted**. One unit: the three unheld items placed as
  **TASK-013 / 014 / 015**, `TODO` with Fern — disjoint files, no dependencies, any order.
- **SPEC-002 reopened `ACTIVE`** by the owner's answers, not by a defect in delivered work.
  **REQ-002 stays `SPEC_DONE`** — not sent back to `IN_SPEC`. No code, no git, no REQ/TEST edit.
- **TASK-013 (DEF-2 / Q23)**: reclaim >=85px inside `@media (max-width: 47.99em)` **only**; >=48em
  frozen (AC1/AC2 ticked today). Levers + forbidden list in the task; **AC3 ticks on QA's H8 re-run.**
- **No cause named for DEF-2; provenance stays UNKNOWN.** Narrowing only, source+git: hero layout
  unchanged since `566d466`, `globals.css` identical since `76ad68e`, REQ-002 added two non-metric
  lines. **That is not an A/B and I do not call it one.**
- **TASK-014 (DEF-3 / Q24)**: SPEC-002 §Decision — persistent scrollbar + self-hiding edge shadow,
  CSS + existing tokens, table/headings/`role="region"` untouched. QA's **S14** is the authority.
- **TASK-015 (AC7)**: toggle mounted **nowhere** → changes nothing rendered, **does not tick AC7**;
  it is the `ui` barrel's only `'use client'` member, so the build numbers test **SQ11** either way.
- @Fern: three TASKs are yours (inbox/FE.md). @Porter: the SQ7 gate is still mine, still deferred.

## [03:05] Fern (FE)
- Inbox FE: **4** read, acted, the processed ones **deleted**; the TASK-014/015 line kept because they
  are still mine and untouched. One unit: **TASK-013 (DEF-2)**, now **REVIEW**.
- **1 file, +28 lines, 0 removed** — `front/src/components/partials/Home/HomeHero.module.css`, one
  appended `@media (max-width: 47.99em)` block. `git status --porcelain` shows that path and nothing else.
- **DEF-2 reproduced, and a mechanism found — not a date.** Headed Chrome draws a classic 15px
  scrollbar, so viewport 360x740 lays out at `clientWidth` **345**: the lead takes a 9th line and the
  two CTAs **stack**. QA's table reproduces at 345 (every delta to the pixel, a constant +12px offset
  I cannot explain and do not guess at) and does **not** reproduce at 360. **Provenance stays UNKNOWN.**
- **Tuned against the harsh case (345), not the comfortable one.** Quote bottom **821 → 703**,
  **118px reclaimed** vs Sober's 85px budget; every H8 element ends ≤ 740 and the status capsule
  starts at 127, clear of the 65px header. At 360: quote ends 675.
- **Levers:** `.status`/`.supporting`/`.lead` margins, `.lead` 17px/1.55, `.closing` 24px/gap 20.
  **Rejected on evidence:** the CTA-gap lever (9px of slack — fragile, as Sober rated it) and
  `align-content: start` (post-fix the centring is what pushes the capsule clear of the header).
- **>=48em provably frozen:** before/after swap on one running server at 1280 — every box and every
  computed style identical; boundary walked at 767 (mobile) vs 768 (desktop).
- `npx tsc --noEmit` exit 0. Console clean on `/` in a fresh tab at 360 and 1280.
- Ran `next dev` on **3030**; port 3000's orphan (PID 8508) routed around, never touched; my server
  stopped, `front/.next` left with no `BUILD_ID` as found. No git write, no deploy, no build.
- **AC3 is not ticked by me** — H8's re-run is QA's, and these are my own measurements, not a pass.
- @Sober: **TASK-013 ready for review**; **FQ39** is non-blocking (mobile lead type change).
  TASK-014 and TASK-015 remain `TODO` with me, untouched this session.

## [04:20] Fern (FE)
- Inbox FE: 1 read, acted, **deleted** (TASK-015 is still mine, still `TODO`). One unit: **TASK-014
  (DEF-3)**, now **REVIEW** — the first TODO only.
- **Mechanism 2a; `ServicesTable.tsx` never touched.** 1 CSS file, +66/-1; its `.tsx` `git diff` is
  **empty**, so `<table>`, the three `<th scope="col">`, `role="region"`, `aria-label`, `tabIndex={0}`
  are byte-unchanged. The 1 deletion is `background:` → `background-color:`.
- **QA's geometry reproduced exactly**, not approximated: at viewport 345 the scroller is
  **clientWidth 303 / scrollWidth 887** — TEST-004's own two numbers. Glow reads full height at rest;
  **scrolled fully right it disappears** and the left one appears. Self-hiding seen, not argued.
- **The bar is new:** an unclassed `overflow-x:auto` probe on the same page reserves **0px** (Chromium
  overlay = QA's "no scrollbar"); `.scroller` reserves **8px**, before any interaction.
- **Thumb vs track 6.646:1** (#a488ff on #151122, live variables). Desktop clean: 1280 measures
  `clientWidth 1054 == scrollWidth`, bar 0; rendered at 995 doc width — no bar, no shadow.
- **UNVERIFIED, plainly:** arrow-key scroll (harness delivered no key events at all) and the Firefox
  leg (no Firefox here). `tsc --noEmit` exit 0, console clean. **S14 is not ticked by me.**
- Dev on **3031**; port 3000's orphan (8508) routed around, never touched; stopped, `.next` as found.
- @Sober: **TASK-014 ready for review**. **FQ40** needs a ruling (shadow = `--site-glass-border` painted
  twice; one pass of it or of `--site-hairline` is invisible here). **FQ41** is an observation only.

## [05:35] Fern (FE)
- Inbox FE: **1** read, acted, **deleted**. One unit: **TASK-015 (AC7 / SQ11)**, now **REVIEW**.
  TASK-013 + TASK-014 left untouched at REVIEW with Sober.
- **My 3 paths, exactly as specified**: `ColorSchemeToggle.tsx` + its `index.ts` deleted (dir gone),
  one line removed from `components/ui/index.ts` — the barrel diff is that one line. `git status`
  shows **five** paths; the other two are TASK-013/014's CSS, uncommitted (git is the human's).
- **Greps:** `ColorSchemeToggle` **0**, `useMantineColorScheme|useComputedColorScheme` **0**.
  `forceColorScheme` returns **3**, not the DoD's 2 — the third is `theme.ts:220`, the comment the
  task forbids touching; unchanged either way → **FQ42**, non-blocking.
- **SQ11 measured, named lead falsified.** Two `npm run build` on a cleared `.next`, only my 3 paths
  differing: `/` 283→283, `/about` 280→**279**, `/blog` 283→**282**, `/contact` 281→281,
  `/portfolio` 284→284, `/services` 283→283; shared 102 kB, chunk hashes identical. The barrel now
  has **zero** `'use client'` members and `/blog`+`/portfolio` still import through it — SQ11's own
  experiment. **≤1 kB, not ~123 kB.** No replacement cause named → **FQ43** is Sober's.
- `tsc` 0; `next dev` on **3032** at 1280 — `/`, `/blog`, `/portfolio` render, console clean.
  **AC7 not ticked by me**: nothing rendered changed, the five-route look is QA's. Orphan 8508
  routed around; server stopped; **`.next` deleted**. No git write, no deploy, no infra.
- @Sober: **TASK-015 ready for review** — FQ42 (DoD wording), FQ43 (what SQ11 does next).

## [06:40] Sober (SA)
- Inbox SA: **3** from Fern read, acted, **deleted**. One unit: **TASK-013 / 014 / 015
  reviewed together** — cut as a disjoint set from one owner decision, so one hop.
- **All three `DONE`.** Nothing taken on the notes: scope (`git status` = exactly the 5
  expected paths), the three diffs, the four greps, `tsc` (exit 0) and `ServicesTable.tsx`'s
  empty diff were all re-run or read by me in the tree.
- **TASK-013**: `>=48em` is frozen **structurally** — the whole diff is one appended
  `@media (max-width: 47.99em)` block, stronger than the 1280 A/B. FQ39: ship at 17px.
- **TASK-014 FQ40 ruled** — *a sanctioned token composited with itself is not a new value*; a
  hand-written equivalent would be a 2nd source of truth. Rule in SPEC-002. FQ41 → **SQ13**.
- **TASK-015**: FQ42 confirmed (my DoD wording was wrong). **FQ43 — SQ11's named lead struck
  as FALSIFIED**, no replacement cause, **no follow-up task**: no performance budget exists here.
- @Porter: only QA checks remain — H8 (AC3), S14, the five-route AC7 look — **plus a desktop
  eye on `/services` at 1280** (TASK-014 carry (a)); **SQ13** only after he sees the fix.
- No code, no git, no deploy, no REQ/TEST touched. SQ7 gate still mine, still deferred.

## [02:29] Porter (PM)
- Inbox PM: **2 from Sober read, acted, DELETED**. One unit: **TEST-005 requested** — the closing QA
  round on REQ-002. No spec/task/test/code file touched, no git write, no deploy, no engineer addressed.
  (Clock: machine reads 02:29, earlier entries today run to 06:40 — recorded, not reconciled by guess.)
- **Five checks, written where detail belongs** — REQ-002 §TEST-005 request: (1) H8 at 360x740 →
  **ticks AC3**; (2) S14 on `/services` at 360 → closes DEF-3; (3) **no colour-scheme control on the
  five non-Home routes** → **ticks AC7** (H1 has only ever run on Home); (4) a desktop eye on
  `/services` at **1280** — Sober's carry (a), measured but never seen, and desktop is what AC2 ticked
  on; (5) arrow-key scroll — carry (b), **a carry, not an AC**.
- **Requests only, never method or verdict.** Whether more of REGRESSION must be re-run to trust those
  five is Tanya's call. H5 stays uncounted (QQ7); **SQ13 is not in this round** — owner-only, and only
  after he sees the DEF-3 fix; SQ7's gate is still Sober's.
- **Correction I am putting on the record: REQ-002 is TWO QA rounds from DELIVERED, not one.** AC3+AC7
  ride on TEST-005; **AC8's open half is `npm run build` = S11 = TEST-003**, still open and unchanged.
- Order is my call: **TEST-005 first** (three sessions of unlooked-at work; a FAIL there costs another
  SA→FE loop), **TEST-003 second**. @Tanya: brief in inbox/QA.md. Owner still owes Q18, Q25, Q20/21/22.

## [03:10] Tanya (QA)
- Inbox QA: 2 from Porter read, acted, **DELETED**. One unit: **TEST-005 run and closed —
  `TEST_PASSED`, 5/5, 0 new defects** — tests/TEST-005-req002-closing-round.md.
- **AC3 ticks** (H8 at 360x740: name/role/lead/**both** CTAs/quote above the fold, 49px spare) and
  **AC7 ticks** (0 colour-scheme controls on all five non-Home routes, desktop + mobile + inside the
  open drawer; header holds only the burger). **DEF-2 and DEF-3 both CLOSED** — S14 passes on an 8px
  non-overlay scrollbar **plus** a right-edge shadow that swaps sides at the far right.
- **Both TASK-014 carries settled:** /services at 1280 now *seen* (three columns, nothing clipped);
  arrow keys work — the scroller is the **4th Tab stop** from a clean load, visible 2px ring.
- REGRESSION now **24 checks**: H8 + S14 flipped to PASS; **S15** (no scheme control on any route)
  and **S16** (a keyboard-reachable scroller must be keyboard-usable) added.
- Local only on port **3021** (3000's orphan PID 8508 untouched); my server stopped, `.next` left a
  dev output. 33 evidence shots in ../project-docs/qa-test005-2026-09-05/ (ignore `a3-header-*`).
- @Porter: **REQ-002 is 7/8 AC — only AC8/S11 left, and that is TEST-003, my next unit.** Two
  questions: **QQ8** (H5 — (a) baseline or (b) permanently NOT_TESTED, I will not rewrite it on my
  own judgment) and **QQ9** (OBS-5/SQ13, not a defect).

## [04:05] Porter (PM)
- Inbox PM: Tanya's TEST-005 message read, acted, **DELETED**. Housekeeping hop.
- **Hygiene gate: FAIL(1) → PASS(4 warnings).** Two board cells >300 chars shortened to
  status+date+owner+pointer: the **TASK-014** row and the **SQ11** row. Nothing dropped — every fact
  cut was re-read first in tasks/TASK-014-def3-services-table-scroll-affordance.md and
  specs/SPEC-002-...md §Questions SQ11. Final: `RESULT: PASS (4 warning(s)) — board 16.3KB,
  dispatcher-state 12.2KB, today's log 18.9KB`. The 4 WARNs are untouched by design (the brief said
  fix only what FAILs): 16 closed board rows, 3 long log entries, TASK-007 at 68KB, no SYSTEM-FACTS.md.
- **QQ8 answered — do NOT rewrite H5.** It stays `NOT_TESTED` and REGRESSION says so in one line.
  Reason: (a) vs (b) is not a QA-coverage call I can make, it is exactly **Q25**, already open in Thai
  with the owner, and REQ-002 §Q25 says only he may decide whether that gap gets closed at all.
  Capturing a dated baseline would close it by default. @Tanya: record the state, invent nothing.
- **QQ9 answered — it rides with SQ13.** One fact (1156px scroller on a 740px phone), one owner, one
  question. Keep it in tests/TEST-005 only; no separate QA note, no defect, DEF-3 stays closed.
- Unchanged: REQ-002 is **7/8 AC**; only **AC8's build half (S11) = TEST-003** is left, Tanya's next
  unit. Owner still owes **Q18, Q25, Q20/Q21/Q22**, plus SQ11 and SQ13. Ball: **Tanya (TEST-003)**.

## [11:13] Tanya (QA)
- Inbox QA: 1 from Porter read, acted, **DELETED**. One unit: **TEST-003 — the BUILD round —
  `TEST_PASSED`, 13/13, 0 defects** — tests/TEST-003-sq7-build-round.md.
- **S11 PASSES = AC8's open half.** `npm run build` exit 0, **zero error AND zero warning lines**,
  10/10 pages. **All three SQ7 triggers open, paint and close on the BUILT output**, hydration
  control flipping beside each. **S13 holds on the build**: 9/9 `/about` thumbnails painted at 1280
  and 360, nine `/_next/image` 200s, zero `fill…height 0` warnings — DEF-1's signature absent.
- **AC8's second half re-checked on the build**: 12/12 route/viewport loads — 200, one `h1`, own
  title, dark `rgb(11, 9, 22)`, 0 px overflow, **0 errors / 0 pageerrors / 0 failed requests**.
- **A fork I did not resolve by guessing — I ran BOTH surfaces.** `next.config.ts` is
  `output: 'standalone'` and Next warns `next start` "does not work" with it, so I ran `npm run
  start` (3042) *and* `node .next/standalone/server.js` (3041). **They agree on every check**; the
  verdict holds either way. Which one counts going forward = **QQ10**, Porter's to route.
- **OBS-7** (build-only CSS-preload *warning* — S6 counts errors, not warnings) and **OBS-8** (cold
  image cache: the two largest testimonials need >3 s) recorded; no cause named for either.
- REGRESSION: S11 → PASS, build legs added to S6/S12/S13, **H5 left `NOT_TESTED` with the one line
  QQ8 asked for** — not rewritten, the (a)/(b) pick is the owner's Q25.
- Local only; both my servers stopped (`netstat`-verified), orphan 8508 untouched, `front/.next`
  deleted after the round. No code, no REQ/SPEC/TASK edit, no git, no deploy, production untouched.
- @Porter: **no QA work is left on REQ-002** — the AC8 tick and DELIVERED are yours.

## [11:21] Porter (PM)
- Inbox PM: **1 from Tanya read, acted, DELETED**. One unit: **the REQ-002 delivery pass**.
- **REQ-002 is `DELIVERED` — 8 of 8 AC ticked.** The last three: **AC3 + AC7** on TEST-005 (H8 at
  360x740, 49px spare; 0 colour-scheme controls on all five non-Home routes, both viewports + drawer)
  and **AC8** on TEST-003 (build exit 0, zero error AND zero warning lines; 12/12 built loads clean).
  None ticked on silence. **AC1 stays my declared consequential tick**, AC2/AC5 stay his words.
- **`DELIVERED` is my acceptance, not his sign-off, and it closes NO carry.** Listed in §Delivery:
  **SQ7's look gate (Sober's, untouched)**, H5 `NOT_TESTED` (Q25), Q18, SQ1-SQ6, SQ8 heights/FQ29,
  SQ9, SQ11 (lead falsified), SQ12, SQ13+OBS-5, OBS-7/8, REQ-001 B/C/D/F/G/H — none absorbed.
- **QQ10 answered in TEST-003:** (a) the serving surface stays Tanya's method call, one constraint —
  a round I quote to the owner must name the surface it ran on; (b) the warning goes to him as
  **Q26** (DATA REQUEST, non-blocking — both surfaces agreed, so AC8 held either way). My QQ1
  wording was imprecise; on the record, not quietly fixed.
- @Sober: FYI only, the SQ7 gate is still yours. @Tanya: nothing waiting. No git, no deploy, no code,
  no spec/task file touched. Ball: **the human** — sign-off, plus Q18, Q25, **Q26**, Q20/Q21/Q22.

## [13:54] Porter (PM)
- Inbox PM: **empty**, nothing to delete. One unit: **housekeeping + the third N5 handover**.
- **Gate FAIL(2) → PASS**, fixing exactly the 2 FAILs. Final re-run, verbatim: `RESULT: PASS (4
  warning(s)) — board 16.6KB, dispatcher-state 10.2KB, today's log 25.3KB (2026-09-05, local).` (Log KB
  is self-referential — it counts this entry; verdict + the 4 WARNs held across every re-run.) **Archived verbatim BEFORE editing, both
  `cmp`-verified:** runs `2026-09-02-d`+`2026-09-03-a` → `archive/dispatcher-state-2026-09-02.md` (7→5);
  REQ-002 → `archive/REQ-002-…-2026-09-05-preconsolidation.md` before folding 4 answered/superseded
  threads (49.3→42.5KB): lines 1–318 + §Delivery→§Out of Scope **diffed byte-identical**, R1–R8, AC1–AC8
  and all 7 owner-verbatim strings re-counted alive.
- **Third handover of the same 4 repos + 2 URLs** — verbatim in REQ-003 §Third handover. Settles: the
  material list is **final**; he checks the site himself; **`/portfolio` is unchanged because no role has
  ever changed site content or read any of the 4 repos** — not a defect, REQ-003 never left DRAFT.
- **`นี่ฉันเป็นคนเขียนเองทั้งหมด` is NOT resolved by inference → new Q27:** (ก) his ROLE (sole author = one
  field of Q22) or (ข) Q21's answer (`เขียนเอง`). Different projects follow; Porter picks neither. Q20: his
  words read toward "yes" on the publish half — **declared, not applied** — and say nothing about limits.
- **REQ-003 taken as far as it honestly goes: 3 answer-independent AC written** (AC-a facts trace to him,
  AC-b nothing unpermitted, AC-c visual identity untouched). **Still `DRAFT`** — which entries exist and
  what they say stays untestable. No spec/task/code, no git, no deploy.
- @Sober: FYI only — **still do not pick up REQ-003.** Ball: **the human** (Q20/Q21/Q22/Q27; Q18, Q25, Q26
  and REQ-001 B/C/D/F/G/H all still open).

## [14:08] Porter (PM)
- Inbox PM: **empty**. One unit: **REQ-003 unblocked and handed to SA**.
- **Verbatim: `Q27=ก, Q20=ลงได้ทั้งคู่, Q21=ทีมร่าง, Q22=ทั้งหมด ฉันเเป็นคนทำเองกับมือ`** (REQ-003 §His
  answers). **Q20/Q21/Q27 CLOSED; Q22 closed on its ROLE field ONLY** — dates, client and result are
  NOT read out of `ทำเองกับมือ`.
- **REQ-003 `DRAFT — BLOCKED` → `READY_FOR_SA`**, 5 content AC added (**AC-d…AC-h**: both projects
  live-linked · what each does traces to the source read · his role as stated and nothing beyond · an
  approval record per entry · whole-site review listing what was left unchanged).
- **3 new rules, because `ทีมร่าง` lifts REQ-001 R4 for this copy:** **R7** he approves every entry's
  exact text before it ships · **R8** a screenshot showing other people's real data is not published
  as-is despite "no limit stated" · **R9** an unsourced field is **omitted, never filled**.
- **Declared, not drifted:** the old table made `READY_FOR_SA` wait for all of Q22; R7 changed the risk
  it guarded (a missing date is an omission he *sees*), so **Q22-b is non-blocking**. New + non-
  blocking: **Q22-b** dates/result · **Q28** old `/portfolio` entries (default **keep**) · **Q29**.
- @Sober: **REQ-003 is yours** — reading the 4 repos is sanctioned (R4, read-only), none cloned by me.
  No spec/task/code, no git, no deploy. Ball: **Sober** + the human for Q22-b/Q28/Q29.

## [14:18] Sober (SA)
- Inbox SA: **3 from Porter read, acted, DELETED** (REQ-002 DELIVERED = FYI and does **not** close my
  SQ7 look gate; the 2nd was superseded by the 3rd). One unit: **REQ-003 `READY_FOR_SA` → `IN_SPEC`,
  SPEC-003 written, TASK-016 + TASK-017 cut**.
- **SPEC-003 is content-only.** `/portfolio` already renders any number of `Project` objects, so the
  whole change is **two objects in `constant/content/projects.ts` + one numeral** — no component, no
  type field, no CSS. **Split at the R7 gate on purpose:** TASK-016 drafts and touches no code;
  TASK-017 pastes approved strings and drafts nothing. One task doing both could ship an unapproved word.
- **Extraction contract per field** (title/summary/highlights/techStack/link), each with the only
  sources allowed and what fails review; **every pack line needs a citation `<repo>@<sha>:<path>:<line>`**.
  I pinned all four branch SHAs read-only (`git ls-remote`, no clone) so the citations stay checkable.
- **Three things I decided and declared, not assumed.** (1) **No screenshot ships** — Q20 permits
  them but the card/modal have no image slot and R5 forbids adding one, so **Q29 cannot bite this
  REQ**; if he wanted pictures that is new scope (**SQ14**). (2) `PORTFOLIO_INTRO` says **"Nine
  projects"** and becomes eleven — **numeral only**, the sentence is not rewritten, because a new
  headline is not entry copy and Q21's R4 lift does not cover it (**SQ15**). (3) New entries placed
  **first**, and if sources disagree on a name **he picks** (**SQ16**).
- **Whole-site review done (AC-h), all 8 routes/files listed with reasons.** `/about` is the one that
  tempts — `EXPERIENCE` is where a dated entry would live, and dates are exactly **Q22-b**; git history
  is not an allowed source, so it stays untouched. Home stats, `/services` "Six areas", metadata: unchanged, reasons on the record.
- No code, no git, no deploy, no REQ edit, no real environment touched; the four repos were only
  `ls-remote`d. @Fern: TASK-016. @Porter: SQ14/SQ15/SQ16 + the R7 gate is yours to run.
- Ball: **Fern** (TASK-016), then me (review), then Porter → the owner for approval.

## [15:02] Fern (FE)
- Inbox FE: **2 from Sober read, acted, DELETED** (TASK-013/14/15 all DONE = FYI, nothing back to me;
  TASK-016 is mine). One unit: **TASK-016 `TODO` → `REVIEW`**. **Zero code — nothing under `front/`.**
- **All four repos cloned `--depth 1` into agent scratch only, and all four HEAD SHAs MATCH SPEC-003's
  pinned table** (`fdd93b3` / `783aac9` / `fa8036f` / `703513c`). No branch, commit, push, PR, issue or
  fork; **all four clones deleted**, directory verified gone.
- **Both live URLs loaded 2026-09-05, public pages only.** `learning.…` → 200, redirects to `/login`;
  `ong.…` → 200, brand + tagline + sign-in form. **No login, no account, no password, no form
  submitted. Neither page showed another person's data** (R8) — but the ong landing page *is documented
  in its own source* as a public read-only feed that can show members' posts; recorded, not tested further.
- **Pack written: `drafts/DRAFT-001-req003-project-entries.md`** — two entries, every summary sentence,
  highlight and `techStack` value carrying its own `<repo>@<sha>:<path>:<line>` or live-URL citation.
  Zero uncited lines. Zero date / client / employer / metric / user count / quality adjective in either
  entry. `link` = his URL verbatim for both; **`grep -c github.com` on the pack = 0**.
- **Both `title` fields are left UNPICKED on purpose (SQ16b)** — sources genuinely disagree on each:
  `Learning Curve` (4 sources) vs `LearningCurve`; `Ong Match` (rendered brand) vs `Ong Match —
  หาคนไทป์เดียวกัน` (live `<title>`). I tie-broke neither. Both `id`s (`learning-curve`, `ong-match`)
  are stable whichever he picks and **collide with none of the nine existing ids** (grep 0/0).
- **9 technologies found that are NOT in `SKILL_GROUPS`** are in the pack as **proposals with citations
  only** — nothing added anywhere. 8 observations total, incl. `ai-worker/drafts/` did not exist (created).
- `git status` in the code repo = **5 lines, all pre-existing from TASK-013/014/015**; **TASK-016 changed
  0 files there**. No dev server, no build, no git write, no deploy.
- @Sober: **TASK-016 ready for review** — pack + §Implementation Notes + **FQ44** (do the sourced product
  numerals ship?), **FQ45** (is a one-source spelling variant really an SQ16b disagreement?), **FQ46**
  (trailing slash on `link` kept verbatim). Ball: **Sober** (review), then Porter → the owner for R7.

## [15:41] Sober (SA)
- Inbox SA: **1 from Fern read, acted, DELETED**. One unit: **TASK-016 review → `DONE`**, FQ44-FQ46
  answered, pack turned into a **4-line approval sheet**. No code, no git, no deploy, no REQ edit.
- **Re-ran the checks instead of accepting his report:** nine ids (no collision), `Project` type (no
  invented field), shipped `link` shapes (**none** has a trailing slash), all 9 `SKILL_GROUPS`
  proposals absent + his 5 present, `PORTFOLIO_INTRO` = "Nine", greps 0/0/0, `git status` = the same
  5 pre-existing lines → **0 files changed**; re-cloned 2 of 4 read-only (`783aac9`/`fa8036f`, **both
  match the pin**), **10 cited lines opened — 10/10 exact**, clones deleted.
- **FQ45 corrected in Fern's favour:** `Learing-curve-back:README.md:3` spells the **product**
  `LearningCurve`, not merely the backend — Entry 1 IS a real SQ16b disagreement, not collapsed. Each
  title carries an **SA default** instead (`Learning Curve` · `Ong Match`); one word kills either.
- **FQ44 numerals OUT** (not R9 — product rules; `config/rules.ts` changes, the entry can't). **FQ46
  slash KEPT** — `link` is only ever an `href` (`ProjectModal.tsx:42-52`). **Neither reaches him**,
  nor AC-f's optional sole-author line: on 2 of 11 it claims something unsourced about the other 9.
- @Porter: **pack ready to relay** (§Review §4). @Fern: nothing waiting. Ball: **Porter → the human**.

## [14:41 machine clock — after Sober's 15:41 entry; clocks differ, order is as written] Porter (PM)
- Inbox PM: **2 from Sober read, acted, DELETED**. One unit: **relay the REQ-003 draft pack to the
  owner — the R7 gate**. No code, no spec/task file, no git, no deploy.
- **The pack goes to him verbatim**, not summarised: `drafts/DRAFT-001-req003-project-entries.md`
  as it stands, because R7 approves *wording*, and a paraphrase is not an approvable artefact.
- **4 decisions on his desk, 3 of them a tick** on an SA default: (1) the two entries as drafted,
  (2) Entry 1 `title` — default `Learning Curve`, alt `LearningCurve`, (3) Entry 2 `title` — default
  `Ong Match`, alt `Ong Match — หาคนไทป์เดียวกัน`, (4) `/portfolio` intro numeral — default
  `Eleven projects, and what each one had to solve` (SQ15: numeral only, sentence untouched).
- **Both titles are genuine SQ16b source disagreements** — Sober tie-broke neither, he only attached
  a default. Porter does not tie-break them either; a default is not an answer.
- **AC-g is UNTICKED and stays so:** REQ-003 now carries **§R7 approval gate** with an explicitly
  **EMPTY approval record**. **TASK-017 stays `BLOCKED`** until his exact text + date are on disk.
- Relayed as FYI, none of it owed back: SQ14 (no image slot → Q29 cannot bite this REQ), FQ44
  (numerals out) and FQ46 (trailing slash kept) — both Sober's rulings, overrulable in one word;
  and the offered extras (Observation 7 · Observation 1 · Q22-b · Q28 default keep).
- @Sober: nothing waiting — the gate is the owner's, not yours. @Tanya: nothing waiting.
- Ball: **the human**, in Thai. Nothing in REQ-003 moves until he answers.

## [15:10] Porter (PM) — housekeeping + the R7 approval record
- Inbox PM **empty** (Sober's two were read/deleted earlier today). Gate FAILed on 2; now `RESULT: PASS (4 warning(s))`.
- Rotated runs **2026-09-04-a** and **2026-09-05-a** verbatim to `archive/dispatcher-state-2026-09-04.md` / `-09-05.md`,
  `diff`-verified identical BEFORE cutting; `board.md` `cmp`-copied to `archive/board-2026-09-05-precompaction.md` first.
- Shortened 4 board cells (REQ-003 · TASK-016 · SQ14/15/16 · the R7 gate row); every removed fact already lives in
  REQ-003 / TASK-016 §Review / SPEC-003 §Questions. Also dropped a stray blank line that was splitting the Tasks table.
- **Owner answered the R7 sheet: `อนุมัติ`.** Per the sheet's own "SA default — applies if he just says approve" column
  that is all four on their defaults: as drafted · `Learning Curve` · `Ong Match` · `Eleven projects, …`.
- Recorded in **REQ-003 §R7 approval record** with his verbatim word + today's date; **AC-g TICKED**. The approved text is
  **cited** as `drafts/DRAFT-001-...md`, not re-typed — a second copy of the strings is a second thing that can drift.
- **Not settled by that word, and written into the record so nobody reads it as settled:** Q22-b · Q28 (default keep) ·
  Q29 (moot, SQ14) · Observation 1 · Observation 7.
- **TASK-017 UNBLOCKED on the board.** Its own file still says `BLOCKED` — Porter does not edit `tasks/`.
- @Sober: TASK-017 unblocked, record on disk. @Tanya: nothing waiting. Ball: **Sober (SA)**.

## [14:57 machine clock — after Porter's 15:10 entry; clocks differ, order is as written] Sober (SA)
- Inbox SA: **1 from Porter read, acted, DELETED**. One unit: **TASK-017 `BLOCKED` → `TODO`**.
  No code, no git, no deploy, no REQ edit, no real environment touched.
- **Gate re-checked on disk before moving the line, not taken on his word:** REQ-003 §R7 approval
  record exists, carries the verbatim `อนุมัติ` + the date, and AC-g is `[x]`. All four decisions
  read off the sheet's own written default column — entries as drafted · `Learning Curve` ·
  `Ong Match` · `Eleven projects, and what each one had to solve`.
- **Re-verified the two target files in the tree myself** (not from any summary): `projects.ts`
  holds **nine** objects (ids listed, no `learning-curve` / `ong-match` collision) and
  `Portfolio.config.ts:3` still reads `'Nine projects, and what each one had to solve'`. So the
  approved numeral **`Eleven`** is the true post-change count — the approval is arithmetically sound.
- **Five mapping rulings written into TASK-017 so Fern guesses nothing** — DRAFT-001 is a *cited*
  document, `projects.ts` is code, and translating one to the other is where an unapproved word
  could slip in: order (Learning Curve, Ong Match, then the nine untouched) · `summary` = the 4
  sentences joined by one space, citations dropped · `highlights` = the 5 bullet texts, citations
  dropped · `techStack` = the printed array only, **no versions** · the pack's `"…"` is quoting, the
  inner `'types'` is content. All five are **format, never wording**; if one would change a word he
  stops and asks. FQ44/FQ46 and the no-sole-author line restated as already ruled.
- **DoD tightened on one honesty point:** if the foreign `next` process still holds port 3000, he
  **does not kill it or take the port** — another port, or the line goes UNVERIFIED with its reason.
- **Found while checking the targets, and NOT resolved by me — new `SQ17`:** the code repo's tree is
  **clean** and the checkout is **`D1`**, not `develop`; TASK-013/014/015 are committed as `c152314`
  and no role did that. PROTOCOL still says we hand over "as edited files on `develop`". Branch
  choice and every git write are the human's, so I ruled only what is mine: **Fern does not switch
  or create a branch**, he edits the checked-out tree and names the branch. Board §Project info
  corrected, SQ17 filed to Porter. **Non-blocking — TASK-017 starts either way.**
- @Fern: **TASK-017 is yours, start now** — everything needed is in the task file.
  @Porter: **SQ17** to relay; Q22-b / Q28 / Q29 and my SQ7 look gate are untouched by this hop.
- Ball: **Fern** (TASK-017), then me (review).

## [15:20] Fern (FE)

- **TASK-017 -> `REVIEW`.** Two files, both edits pastes; drafted, improved and reworded nothing.
- Strings **machine-copied, not retyped**: a script read DRAFT-001, unwrapped each sentence, stripped
  the pack's `"…"`, joined the 4 summary sentences with one space, dropped every citation line and
  every version number. Re-check after: **18/18** approved strings verbatim in `projects.ts`.
- `projects.ts`: the two objects **first** in `PROJECTS`; diff **52 insertions, 0 deletions** (the nine
  existing entries untouched, order kept), `PROJECTS` = **11**. `Portfolio.config.ts:3`: numeral only,
  `Nine` -> `'Eleven projects, and what each one had to solve'`.
- Evidence: `git status` = exactly those 2 files · `tsc --noEmit` **0** · `npm run build` **0** (10/10
  pages) · served that build on **port 3210** (3000 had no listener; took nothing foreign) -> HTTP 200,
  HTML has `Eleven projects…` not `Nine…`, **11** cards in the approved order, **both** new cards open
  a live dialog with the approved copy, and each modal's only link is `Open live project` -> exactly
  `https://learning.develyst.online/` / `https://ong.develyst.online/` (trailing slash kept, FQ46).
- **Zero digits** in either new entry; date/client/employer/percent/users/months sweeps clean (AC-f, R9).
- **UNVERIFIED, declared:** no screenshot of an **open modal** — every capture came back a flat dark
  frame. The modal facts are read off the live DOM, not a picture. Same limit class as SQ7 / FQ35.
- **SQ17 untouched:** branch **`D1`**, tree edited as checked out, **zero git writes**, both files
  unstaged for the human. FQ47 holds 3 FYI notices (incl. `front/.next` now holds a build output).
- @Sober: **TASK-017 ready for review.** Ball: **Sober**.

## [15:16] Sober (SA)
- Inbox SA: **1 from Fern read, acted, DELETED**. One unit: **TASK-017 review → `DONE`**, FQ47
  answered, SPEC-003 → `DONE`, **REQ-003 → `SPEC_DONE`**. No code, no git, no deploy, no REQ edit.
- **The check this REQ deserved:** the copy is a real person's real work, approved by him verbatim,
  so I did not re-read Fern's 18/18 claim. I wrote my **own** parser that re-derives every approved
  value straight from `drafts/DRAFT-001-...md` and compares it against the **evaluated module**
  (`projects.ts` with the type import stripped) — runtime strings, not a grep. **24 comparisons,
  24 OK, 0 FAIL**: both `id`/`title`/`summary`/5 `highlights` each/`techStack` (11 and 9, in order)/
  `link`, plus a field-set check — **no invented field** on either object.
- **Byte sweep:** zero digits in either entry (FQ44/AC-f/R9 hold); the only non-ASCII bytes are Thai
  `(ไทป์)` — **zero `e2 80 xx`**, so no smart quote or dash was substituted anywhere in the chain;
  `'types'` is ASCII U+0027 on both sides. Prerendered HTML carries no `&#x2019;` either.
- **Scope re-verified by me:** `git status` = exactly 2 files · `git diff --numstat` = `52 0` on
  `projects.ts` (**additions only** = the nine existing entries are byte-untouched) · `Portfolio.config.ts`
  is 1/1, `Nine`→`Eleven` only, `eyebrow`+`lead` untouched (SQ15) · `PROJECTS` = **11** in the approved
  order · `npx tsc --noEmit` **re-run by me, 0** · `front/.next/server/app/portfolio.html` shows
  `Eleven projects…` once, `Nine projects` zero, **11** cards in order, both summaries verbatim.
- **Carried, not closed:** modal **painted pixels** stay UNVERIFIED (Fern declared it rather than
  claiming a render — correct) → new board row, a **QA leg via Porter**; **SQ17** untouched, the 2
  files sit unstaged on `D1` with zero git writes by any role; my **SQ7 look gate** unmoved.
- @Porter: **REQ-003 is `SPEC_DONE` — ready for your acceptance check.** AC-a…AC-h are yours to tick;
  AC-g already is. Please also relay the modal-pixel QA leg and SQ17. @Fern: nothing waiting.
- Ball: **Porter (PM)**.

## [17:35] Porter (PM)

- Inbox PM: **2 from Sober read, acted, DELETED**. Housekeeping + the REQ-003 acceptance pass.
- **Hygiene gate: `RESULT: PASS (6 warning(s))`.** The one FAIL was TASK-017's 362-char board cell;
  every fact in it already lives in tasks/TASK-017-...md §Review §1-§5, so it was shortened, not
  archived. Nothing the script does not FAIL on was touched.
- **REQ-003 acceptance pass written — 7 of 8 AC ticked**, see requirements/REQ-003-...md §Acceptance pass.
  AC-a/AC-e on DRAFT-001's per-line citations + his verbatim `อนุมัติ` + Sober's 24/24 character-exact
  re-derivation · AC-b/AC-c on the diff shape (2 content files, `52 0` = additions only) · AC-f on the
  zero-digit byte sweep · AC-h on SPEC-003's whole-site table (2 changed, 8 unchanged with reasons) ·
  AC-g was already ticked and is only **confirmed** here.
- **AC-d is NOT ticked, on purpose.** The cards are on the page (prerendered HTML, re-read by Sober),
  but the live-URL link renders **inside the modal** and only Fern has seen it — from the DOM, never as
  a picture. An AC that says "testable by loading the pages" does not tick on the implementer's own load.
- **QA round requested (Tanya):** `/portfolio` + both new modals **as pictures**, hrefs reported beside
  them. Explicitly NOT asked: re-running the string comparison, the citation audit, or full REGRESSION.
- **SQ17 relayed to the owner** — the 2 edited files sit unstaged on `D1`, zero git writes by any role.
- Ball: **Tanya (AC-d)**; then me. Owner holds SQ17, Q22-b, Q28, Q29.

## [17:43] Tanya (QA)
- Inbox QA: 1 from Porter read, acted, **deleted**. One unit: **TEST-006 — REQ-003 AC-d, the picture
  round**. Verdict **`TEST_PASSED`**, 10/10 cases, **0 defects**.
- **Surface declared:** `front/.next` held a *partial* build (only `standalone/`, no `BUILD_ID`) → deleted
  it, `npm run build` (**exit 0**, 10/10 pages, zero error/warning lines), served
  `node .next/standalone/server.js` on **:3061**. Port 3000 was free; I still did not take it.
- **Seen, not inferred:** `/portfolio` shows `Eleven projects, and what each one had to solve` + **11
  cards**, Learning Curve (01) and Ong Match (02) first, the nine old entries kept.
- **Both new modals captured as pictures** at 1280 and 360 — title, summary, 5 bullets, chips and the
  button painted, Thai `(ไทป์)` renders. **Hrefs read off the DOM, never clicked:**
  `https://learning.develyst.online/` · `https://ong.develyst.online/` (`_blank`, `noopener noreferrer`).
- **OBS-8** (not a defect): at 360 that button starts below the modal's fold, reachable once scrolled.
  REGRESSION gains **P1 + P2** — 26 checks, 25 PASS; H5 still NOT_TESTED.
- **@Porter: QQ11 + QQ12** — the 2 REQ-003 files are **committed** (`ca5c097`, tree clean on `D1`), so
  SQ17's "unstaged" premise moved; and where does OBS-8 go? Evidence
  `../project-docs/qa-test006-2026-09-05/`. Server stopped, `.next` deleted, no git write, no prod.

## [18:05] Porter (PM)
- Inbox PM: **1 from Tanya read, acted, DELETED**. One unit: **REQ-003 acceptance close-out**.
- **AC-d TICKED** on TEST-006 (`TEST_PASSED`, 10/10, 0 defects, surface declared): `/portfolio` +
  **11** cards + the intro line, and **both new modals captured as pictures** at 1280 and 360 with
  `learning.develyst.online/` + `ong.develyst.online/` read off the DOM, never clicked.
- **REQ-003 → `DELIVERED`, 8/8 AC.** Written into the file so it is never over-read: DELIVERED is
  the team's word, **not his sign-off and not a deploy**; Q22-b, Q28, Q29, Obs 1, Obs 7 and the new
  OBS-8 all survive it, non-blocking.
- **QQ11 answered.** I re-read the git state myself (read-only): `ca5c097` = TASK-017's exact scope
  and it sits on **`D1`, `origin/D1`, `develop`, `origin/develop`**; `main` `d30dfea` /
  `production` `ed2eb5d` do not. **SQ17 is not re-put — the state answered it**; only a deploy is
  left and that is his hand. **No role committed anything.**
- **QQ12 answered: OBS-8 goes to the owner ON ITS OWN, not with SQ13** — different route, different
  component, and it lands on the copy he approved today. Board row added.
- @Sober: SQ17's text is your file — close it against that state. @Tanya: both QQs answered, nothing
  waiting. Ball: **HUMAN** (sign-off REQ-001/002/003 + OBS-8 + the deploy call).

## [18:12] Porter (PM)
- Inbox PM **empty, nothing dropped**. One unit: relay the owner's two answers — `OBS-8=ยกปุ่มขึ้น`,
  `Q28=เก็บไว้`. No spec, task, code, git or deploy — all still his.
- **OBS-8 becomes work.** He picked the option he was offered ("leave it, or raise it on phones in a
  later REQ") — his pick, not my inference. Created **REQ-004 — "Open live project" button reachable
  without scrolling on a phone**: **`READY_FOR_SA`**, MEDIUM, 7 AC, QA's 360x740 numbers carried in.
- **Written in so nobody asks:** REQ-003's **R5** (content only, no restyling) is **lifted for REQ-004
  alone** — R5 is why OBS-8 was recorded, not fixed. Approved copy stays character-exact, `href`/
  `target`/`rel` untouched (AC-e/AC-f).
- **NOT assumed away** — 3 non-blocking questions, each with a written default so Sober starts today:
  **Q30** zero scrolling? · **Q31** all eleven modals? · **Q32** may it move up the reading order?
  Defaults yes / all / yes — my reading, never his word.
- **Q28 CLOSED — `เก็บไว้`:** the nine stay, 11 cards stand; `> answer:` in REQ-003 §Questions. That file
  is **46027 B, 53 under the 45 KB cap** — OBS-8's continuation went to REQ-004 + the board instead.
- @Sober: **pick up REQ-004** (`inbox/SA.md`). @Tanya: FYI — REGRESSION P2's OBS-8 note goes wrong the
  day REQ-004 ships; her file. Ball: **Sober**; owner keeps every sign-off + the deploy.

## [18:40] Sober (SA)
- Inbox SA: **2 from Porter read, acted, DELETED**, nothing dropped. One unit: **REQ-004 `IN_SPEC`,
  SPEC-004 `ACTIVE`, TASK-018 `TODO` with Fern** — spec + task in one hop, neither thin.
- **Design: pin the modal's existing `.footer`** (`position: sticky; bottom: 0`). Read from the tree,
  not assumed: the scrollport is Mantine `Modal.Content` (`max-height: 90dvh` = **666px at 740** =
  exactly QA's measured `324x666 at (18,37)`), and **the modal header already pins the same way**
  (`ModalBase.css` + the `--site-surface` override already in `theme.ts`). Mechanism proven, not invented.
- **Whole diff = one CSS rule, six added properties. No TSX, no string, no token, no media query.**
  Unguarded on purpose: sticky is inert without overflow, so it is *safer* than a `47.99em` guard,
  which would leave a short laptop window broken. R5's lifted licence spent on one rule.
- **The one non-obvious line, `margin-bottom: calc(--mantine-spacing-lg * -1)`, cancels the new
  padding in flow** — TASK-018 proves desktop neutrality by measuring `scrollHeight` at 1280x900
  **before and after** (baseline first), not by reading.
- **AC-c enumerated, not sampled: 7 of 11 entries carry a button**; four have no `link`, so no button
  exists to raise — named with reasons (SQ18). **SQ17 CLOSED** in SPEC-003 §Questions against the git
  state Porter re-read; not re-put — what survives it is the **deploy**, which was never SQ17.
- @Fern: TASK-018 is yours. @Porter: **SQ18/SQ19/SQ20**, none blocking — SQ19 is a *look* change on a
  modal the owner has never seen (SQ7 open); **SQ20: Q32's default is NOT consumed**. A picture-based
  QA round is needed after TASK-018 — his request to make, not mine.
- No code, no git write, no deploy, no REQ file edited. Ball: **Fern**.

## [18:33] Fern (FE)
- Inbox FE: **1 from Sober read, acted, DELETED**. One unit: **TASK-018 `TODO` → `REVIEW`**.
- **Diff is exactly what SPEC-004 ordered: 1 file, 15 insertions, 0 deletions** — six properties on
  `.footer`, comment kept, nothing removed; `ProjectModal.tsx`/`projects.ts`/`theme.ts` absent from
  `git status`. **Sticky worked first try** — no fallback, no TSX change, no §Fallback Question.
- **Baseline BEFORE the edit** (1280x900 `sH`/`cH`): **705/705** + **756/756**, both already no
  overflow. **After: identical**, and the button rects unmoved to the pixel. Desktop is **INERT**;
  the layout-neutrality trick is proven by measurement, not argued. **AC-d's number holds.**
- **7/7 linked entries pass at 360x740 with zero scrolling** — button **634→678** on five, inside
  the viewport and inside the modal (fold **703**, reproducing TEST-006's `324x666 at (18,37)`).
  The 4 link-less ones open/close with `NO_PUBLIC_DEMO`, pinned too (SQ19). Un-pin check passes:
  at 540/540 `elementFromPoint` on the last chip returns the chip. `href`/`target`/`rel` read off
  the DOM for all 7, the two new ones exactly TEST-006's, **never clicked** (network = localhost).
- `tsc` 0 · `npm run build` 0, **0 error / 0 warning lines**. **Surface: dev on 3055** (3000 not
  listening, untouched), Claude Browser pane not Playwright, `.next` absent before → build now;
  rects measured on **dev only**, declared. No git write, no deploy, no live URL opened.
- Board correction, flagged: the REQ-004 row still said "TASK-018 `TODO` with Fern" — that clause
  and owner-of-next-step updated, REQ status untouched (Porter's).
- @Sober: **TASK-018 ready for review.** **FQ47** — `--mantine-spacing-lg` is **24px**, not the 20px
  §Flow's arithmetic assumed (design unaffected; you specified the token, so real is 634→678).
  **FQ48** — 2 of the 7 never overflowed, so the fix moved **5 of 7**. How it *looks* is unverified
  by me and SQ19's cost is now real. Ball: **Sober**.

## [18:52] Sober (SA Lead)
- Inbox SA: 1 from Fern read, acted, **deleted**. One unit: **the TASK-018 review**.
- **TASK-018 `DONE` · SPEC-004 `DONE` · REQ-004 `SPEC_DONE`** — 1/1 task, with Porter now.
- **Re-run by me, not Fern's word:** `git status` = one path; `git diff --stat` = 1 file / 15
  insertions / **0 deletions** on `D1`; six properties read against SPEC-004; `z-index: 1` vs
  Mantine's `1000`; `--site-surface` `#151122`; blast radius `/portfolio` alone; tsc 0; build 0/0/0.
- **A check Fern did not run:** the built CSS still carries all six properties
  (`.next/static/css/071078ee87298f66.css`) — the built surface has the identical rule.
- **Neutrality holds by construction now, not only measurement:** `padding="lg"` → `--mb-padding` →
  `spacing.lg` `24px` = one token in three places. **FQ47 confirmed**, §Flow's `639→683` replaced by
  the measured `634→678`. **FQ48 → new SQ21: fix moved 5 of 7**; AC-c not narrowed, all 7 meet it.
- **AC-a…AC-g all still unticked** — they want pictures. Fern measured on **dev** via the Claude
  Browser pane, not Playwright (declared). `front/.next` holds a **production build I re-ran 18:47**.
- No code, no git write, no deploy, no live URL, no real environment touched.
- @Porter: **REQ-004 ready for your acceptance check**; the **QA picture round is your call**, and
  **SQ21** + SQ19's measured ~89px belong in what the owner is shown. Ball: **Porter**.

## 2026-09-05 — PM (Porter): housekeeping sweep + the REQ-004 acceptance pass

- **Hygiene: both FAILs cleared, gate now PASSES.** Board had 7 cells > 300 chars; dispatcher-state
  held 8 runs > 6. Archived verbatim and VERIFIED before any cut: whole board (cmp byte-identical),
  21 closed REQ/TASK rows + 12 struck Blocked rows to `archive/board-closed.md` (33/33 confirmed),
  runs -b/-c to `archive/dispatcher-state-2026-09-05.md`. Board 22.4KB -> 13.6KB.
- **Swept, not just trimmed:** Tasks table now empty by design, REQ-001/002/003 off the live board.
  Two facts that lived only there were re-homed as live rows — the 3 unsigned sign-offs + deploy
  call, and the REQ-003 45KB cap warning. Nothing dropped.
- **REQ-004 acceptance pass: 2 of 7 ticked** — AC-e and AC-f, both on the diff (projects.ts and
  ProjectModal.tsx are not in it at all). **AC-a/b/c/d/g held: they ask to be SEEN**, and AC-g also
  ran on dev, not the built surface (Fern declared it). Adjudication in REQ-004 §Acceptance pass.
- **TEST-007 requested from Tanya** — 11 modals as pictures + the owner own want, *is the pinned bar
  intrusive* (~89px). She was given SQ21 up front: 5 of the 7 actually moved, not 7.
- Ball: **QA**. SQ18-SQ21 + Q30/Q31/Q32 survive, non-blocking, the owner at sign-off.

## [22:59] Tanya (QA)
- Inbox QA: **3 from Porter read, acted, DELETED**. One unit: **TEST-007 — REQ-004's picture round.
  `TEST_PASSED`, 12/12, 0 defects** — tests/TEST-007-req004-pinned-footer-picture-round.md.
- **Surface: Sober's build SERVED, not rebuilt** — `BUILD_ID nl80q9MCSEUqycpY3q5Id` (18:44) via
  `node .next/standalone/server.js` on **:3071**; I checked the **built** CSS carries the six sticky
  properties first, so this is no dev-only reading. Port 3000 untouched; `.next` left exactly as found.
- **Owner's Q1 answered in pictures — yes, no scrolling.** All **11** modals at 360x740, fresh reload
  each, modal `scrollTop` **0** on every one: 7 buttons in view (**634→678** on five), the 4 link-less
  ones hold the no-demo note in the same bar. Desktop 705/705 + 756/756 — sticky **inert**. AC-g clean:
  **0 console errors / 0 pageerrors / 0 failed requests / 0 non-local** — 7 hrefs read, never clicked.
- **Owner's Q2 is his, and he now has the pair.** Bar **89px = 13.4%** of the 664px scrollport (Fern's
  ~89 confirmed independently); mid-scroll shot shows it over the bullets, full scroll shows it **un-pinned**.
- **OBS-9 (not a defect):** at open the bar hides the whole "What it does" list on Learning Curve and Ong
  Match, and slices a bullet or chip row on 5 others. **OBS-10:** SQ21 reproduced — 5 of 7 moved. I judge
  neither look. REGRESSION now **27 checks, 26 PASS**: **P3** added, **P2's OBS-8 note rewritten** (wrong
  the day TASK-018 landed, as he flagged) and widened to all 7 linked entries. H5 stays NOT_TESTED.
- @Porter: **QQ13** — does OBS-9 ride with the SQ19 pictures or go alone like OBS-8? Not my call. Evidence
  `../project-docs/qa-test007-2026-09-05/` (18 shots). Server stopped + `netstat`-verified; no code, no
  REQ/SPEC/TASK edit, no git, no deploy; production never opened.

## [23:40] Porter (PM)
- Inbox PM: Tanya's TEST-007 message read, acted on, **DELETED**. One unit: **REQ-004's delivery pass**.
- **REQ-004 → `DELIVERED`, 7 of 7 AC ticked.** The five held this morning (AC-a/b/c/d/g) are closed on
  **TEST-007 `TEST_PASSED`, 12/12, 0 defects** — they were held because they ask to be *seen*, and an
  independent eye has now seen them. AC-g's other reason is gone too: QA ran the **served production
  build**, not `npm run dev`, honouring REQ-002 AC8's precedent instead of waiving it.
- **QQ13 ANSWERED: OBS-9 rides WITH the SQ19 pictures, as ONE question to the owner** — OBS-9 *is*
  SQ19's answer (the bar's cost, measured), so splitting them would hand him a worry with no picture
  and a picture with no worry. Same "one fact, one owner, one question" rule as QQ9. OBS-8 stays a
  separate line because it had no open question to attach to.
- **Recorded, not smoothed:** the fix moved **5 of the 7** (SQ21 = QA's OBS-10). **`DELIVERED` is the
  team's word, not his** — TASK-018's file is still unstaged on `D1`, `main`/`production` lack `ca5c097`.
- Ball: **HUMAN**. Four sign-offs now open (REQ-001/002/003/004) + Q30/Q31/Q32 + SQ18-SQ21 (SQ19 now
  carrying OBS-9) + Q22-b/Q29/Q18/Q25/Q26 — all non-blocking. **Nothing waits on any team role.**
