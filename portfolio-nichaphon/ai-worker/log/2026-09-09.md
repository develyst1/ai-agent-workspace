# Log — 2026-09-09 — portfolio-nichaphon

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [11:34] Porter (PM)

- Inbox PM: **empty**. One unit: **intake of the owner's new requirement** (resume → profile
  MD, frontend fact fixes, `back/` Bun+Hono on his LLM gateway, "ask the AI about me" on Home).
- Source secured first: both resume files copied to `../project-docs/resume-2026-09-06/`,
  transcribed to `../project-docs/resume-2026-09-06-transcript.md`. **Its two references
  (names + phones) deliberately NOT transcribed** — barred from any knowledge base.
- **Three REQs, all DRAFT + BLOCKED on the owner:** REQ-005 (profile MD + 8 fact conflicts),
  REQ-006 (`back/` + one recorded real gateway call), REQ-007 (Home, 3+ stacked calls as
  thinking steps; depends on both). Ideas he asked for are REQ-007 §Ideas A–I, recommended
  v1 = A+B+C+D+G+H, lead idea *show the three thinking steps running*.
- **Read, not guessed:** gateway contract from `develyst-ai`'s `CLAUDE.md` + Bruno. **No
  `.env` opened, no call fired** — spending his provider credit is Q36, his call.
- The 8 conflicts are facts about his own life, so none was resolved: sharpest is the GFAI
  kiosk (site **two weeks** vs resume **about three weeks**) and `CAREER_STATS` "3+ years".
- Scope change recorded as **his** on the board: the project gains a backend; a database does
  not (Q44, default none). Hygiene: 1 board cell + 1 stale run cleared.
- Ball: **HUMAN** — 9 blocking answers, one line each. Nothing waits on Sober, Fern or Tanya.

## [14:10] Porter (PM)

- Inbox PM **empty**. One unit: **record the owner's 9 answers; all three REQs unblocked**.
  **REQ-005/006/007 are `READY_FOR_SA`** — @Sober, take them 005 → 006 → 007 (`inbox/SA.md`).
- Each REQ gained **§Owner decisions**: his verbatim Thai, then what it settles. Nothing
  resolved by inference — where his answers did not reach, the REQ says so.
- REQ-005: resume is the base. **C6 was my misread** — "3+ years" was always his career total
  and is **now 4 years**; it exists in no other source, so R2 gained a third citation class
  (his own recorded decision). C1 = three weeks. **C1 and C3 each move TWO strings.**
  Q41 `ห้าม` **overrides the resume on C7** — no home district, anywhere, incl. the profile MD.
- REQ-006: `back/` = **client** of `ai.develyst.online`, **no auth**, **5 real calls** total.
- REQ-007: **real WebSocket, owner-mandated** — reaches REQ-006 R3 and adds 2 AC-e failures.
- **Two answers of his collided; neither papered over:** idea **H** stays but its trigger
  changes (Q38 removed the cap it degraded from); **Q50** = Q39's language default vs Q45
  omitting idea F — **default deliberately unapplied**, built as one isolated switch.
- Answered his question back: a per-visitor cap needs **no Postgres**, memory suffices.
  **Q49 raised:** his 5 calls cover REQ-006 only; one visitor question is 3+ calls.
- Ball: **SA (Sober)**. Q47/Q48/Q49/Q50 are the owner's, all non-blocking.

## [14:12] Sober (SA)

- Inbox SA: Porter's hand-off read. **One unit: the REQ-005 leg only.** REQ-006 + REQ-007
  legs deliberately left **undeleted and unconsumed** in `inbox/SA.md` for the next SA hop.
- **REQ-005 `IN_SPEC`** · **SPEC-005 `ACTIVE`** · **TASK-019 + TASK-020 `TODO` (Fern)** ·
  **TASK-021 `BLOCKED`** on one owner approval that I unblock myself.
- **Read the code, did not trust the REQ's counts — and the counts were wrong.** AC-c says
  C1 is two strings; grep says **five**: the two named, plus `HOME_LEAD` (**visible copy on
  the homepage**) and the `metadata.description` of `layout.tsx` + `about/page.tsx` (indexed
  by search engines). All five contradict the resume → all five in scope under R4. C3=2,
  C2/C4/C6=1 each, confirmed. → **SQ22**, Porter records it; I may not edit a REQ.
- **Q47 and Q48 NOT consumed as defaults.** They ride into TASK-020's single approval sheet
  with the profile text and the add-list — he reads the exact words before they carry his name.
- **The REQ contradicts itself and I did not paper over it:** Q42/R5 invites Education +
  Languages, §Out of Scope bars new UI, and neither exists on the site today. Resolution that
  needs no answer: **profile file yes, site no** — the site half is his call (**SQ24**).
- **SQ25** answered by me: keep the footer year a **constant** — an auto year freezes at build
  time on a static site, turning a visible stale number into an invisible one.
- **SQ26 (FYI):** profile at `back/knowledge/PROFILE.md` — **text only, no code, no dependency**;
  split clean body / sibling citation file so REQ-007's LLM never reads a citation aloud.
- @Porter: **SQ22-SQ26** in `inbox/PM.md` — SQ22 and SQ24's routing fact need your hand.
- @Fern: **TASK-019 + TASK-020** in `inbox/FE.md`, both startable now, different files.
- Ball: **Fern**. Nothing blocks him. No DATA REQUEST — all sources were already in `project-docs/`.

## [16:40] Fern (FE)

- Inbox FE: Sober's TASK-019 + TASK-020 note read. **One unit: TASK-019 only** — the
  TASK-020 leg is deliberately left **undeleted and unconsumed** in `inbox/FE.md`.
- **TASK-019 `TODO` → `REVIEW`.** All ten decided edits applied, 4 files, `git diff --stat`
  = **10 insertions / 10 deletions**, one line per tabled edit and nothing else. `site.ts`
  never opened; "Chatuchak" appears nowhere.
- Evidence: the three DoD greps all return **zero**; `npx tsc --noEmit` exit 0, no output;
  `npm run build` exit 0 **before and after**, and grepping each full log for
  `error|warn|failed` returns **nothing in either run**. Route table identical byte-for-byte.
- Seen on a dev server (port 3011, mine, now stopped): `/` hero "about three weeks" +
  both `<meta description>`s; `/about` carries all six of its strings. **One honest gap —
  the `4 Years experience` stat is rendered-DOM-verified (opacity 1, in viewport) but every
  screenshot of that region came back unpainted, so I have NOT seen the "4" as pixels.**
- **@Sober — FQ32, and it needs your routing.** No sixth "two weeks": grep confirms exactly
  **five**, all tabled, so **SQ22's count of five stands**. But C6's `3+`→`4` now contradicts
  visible copy the table never listed — `About.config.ts:3` still reads **"Three years of
  shipping…"** while `/` says **"4 Years experience"**. Same fact, two numbers, both shipping.
  **I did not touch it**: not in the table, it is copy about a real person, and the rewrite is
  a wording choice that is not mine to make. Grep says this one line is the whole of it.
- **Correction I am logging against myself:** I thought `sed -i` had rewritten the four files
  LF→CRLF and "fixed" them to LF. **`grep -c $'\r'` lied** — a byte count says the working
  copy is legitimately **CRLF** (`autocrlf=true`), so `sed` was innocent and my fix was the
  only real corruption. **Restored to CRLF**, then re-ran diff/greps/tsc/build clean *after*
  the restore. Trap for the next engineer: count CR bytes with `node`, never grep.
  Also: `front/.next` deleted before `npm run dev`.
- No commit, no stage, no branch touched — git stays the human's. Tree was **clean** before I
  started, so the board's "TASK-018's file is still unstaged" line no longer matches git.
- Ball: **Sober** — review TASK-019 and route FQ32. Nothing of mine is blocked; TASK-020 is
  still `TODO` and mine for the next hop.

## [17:35] Sober (SA)

- Inbox SA: Fern's TASK-019 `REVIEW` read and **deleted**. **One unit: the TASK-019 review +
  FQ32 routing.** Porter's **REQ-006 + REQ-007 legs deliberately left unconsumed** — next hop.
- **TASK-019 `REVIEW` → `DONE`.** Nothing quoted from Fern's notes; I re-ran it all: diff read
  line by line (10 ins / 10 del, exactly the ten tabled edits, `site.ts` absent), four greps
  zero, `tsc` exit 0, `npm run build` exit 0 with **no** `error|warn|failed` line, CRLF counted
  by **bytes** on the four changed files + two untouched siblings (CR == LF == CRLF on all six).
- **His one honest gap is closed at the markup level.** He could not photograph the `4`, so I
  read the **built** HTML rather than the dev DOM: `.next/server/app/index.html` ships
  `<dd …>4</dd><dt …>Years experience</dt>`. Also confirmed there: `about three weeks` ×2 in
  each of `/` and `/about`, `Smart Solutions` = 0, both new roles. **Pixels stay QA's** — he
  was right to refuse to claim them.
- **FQ32 verified, not accepted on trust.** My own sweep for every year-count phrasing
  (`(one|…|ten|[0-9]+) +? years?`, `yrs`, `decade`, `since 20`) returns **one** line — his.
  **SQ22's five stands.** The contradiction is real in shipped bytes: `/` = "4 Years
  experience", `/about` `<h1>` = "Three years of shipping…".
- **Routed with no new hop and no new owner question:** it becomes **C9** in SPEC-005 Group B,
  so it rides into the sheet TASK-020 is already drafting. **That sheet is now 5 lines, not 4.**
  TASK-020 §(d) + DoD and TASK-021 (step 6 + DoD) edited to match.
- **What I refused to decide, and said so in the files:** whether "three years" ever meant his
  career *total* or the length of one *pattern of work*. Both readings + two candidate wordings
  go to him; reverting the `4` is **not** offered — it is his own Q40 decision.
- @Porter: **SQ27** in `inbox/PM.md`, plus one verified hygiene line — the board's git bullet is
  stale: the human committed TASK-018 as **`6c17609`**, so `D1` = `origin/D1` = `6c17609` while
  `develop` = `ca5c097`. I read git; I did not rewrite his bullet.
- @Fern: TASK-019 verdict + FQ32 answer + the grown TASK-020 scope in `inbox/FE.md`.
- Ball: **Fern** — TASK-020, startable now, nothing blocks it. No DATA REQUEST. **No gateway
  call spent — the owner's 5 are untouched.**

## [18:20] Fern (FE)

- Inbox FE: Sober's TASK-020 leg read, acted on, **DELETED** (his TASK-019 verdict + FQ32 answer read first). **One unit: TASK-020.** Inbox now empty.
- **TASK-020 `TODO` → `REVIEW`.** `drafts/DRAFT-002-req005-profile-pack.md`: the `PROFILE.md` body, its citation file, the Q42 Class 1 add-list, C5+C8+C9, and the 5-line sheet. **8/8 DoD evidenced.**
- **Zero code touched, proved by comparison not memory:** `git status --short` + `git diff --stat` captured **before** drafting and again after — identical (the four TASK-019 files, 10 ins/10 del). `--untracked-files=all` adds nothing; `back/` still 0 entries.
- Privacy gates: `grep -ic chatuchak` over the pack = **0** (absent from my own prose too — it says "the home district"). Every capitalised token in the bounded body extracted by script, classified by hand: **145 distinct, only person names = Nichaphon, Sayvav, Dong.** Body citation markers: **0**.
- **All nine Class 1 candidates proposed** — none added, none dropped; all nine grep to zero in `about.ts`. `nginx`/`pm2` casing follows the resume and is flagged, not decided.
- **@Sober — three questions, none guessed at.** **FQ33** the eleven `projects.ts` entries are NOT in the profile (not in your section list; would double the file) — REQ-005 or REQ-007? **FQ34** the add-list leaves `Generative AI (Gemini, OpenAI)` naming 2 providers in a phrase while 3 more sit beside it as chips; the tidy fix **edits a shipped string**, which Class 1 bars — I did not do it. **FQ35** six body claims are site-only, no resume backing (2 awards, "promoted faster than anyone", "ongoing", Vue/Angular/ASP.NET, ways-of-working) — legal under R2, but the REQ-007 AI will repeat them as fact; kept and listed, not dropped silently.
- Refused one inference outright: I do **not** claim the resume's "AI API Center — LLM Gateway" is the site's "Develyst AI Gateway" or REQ-006's endpoint. No allowed source says so.
- No dev server, no build, no commit, no branch touched. `front/.next` left as Sober left it.
- Ball: **Sober** — review TASK-020, answer FQ33/34/35. Nothing of mine is blocked.

## [21:05] Sober (SA)

- Inbox SA: Fern's TASK-020 `REVIEW` read and **deleted**. **One unit: the TASK-020 review +
  FQ33/FQ34/FQ35.** Porter's **REQ-006 + REQ-007 legs deliberately left unconsumed** — next hop.
- **TASK-020 `REVIEW` → `REWORK`.** The pack itself is good and I re-verified every DoD box
  myself, not from his notes: `git status` = TASK-019's four files and nothing else, both
  chatuchak greps **0**, ten citation rows spot-checked at random against transcript + shipped
  code (**all ten hold**), all nine add-list items genuinely absent from `SKILL_GROUPS`, both C5
  replacement sentences diffed word by word, C9 delivered with no pick and no revert option.
- **The one thing his sheet got wrong is the thing the sheet is FOR.** He flagged "six claims
  with no resume backing"; I counted the rows in his own table with no `[R:` and no `[O:` —
  **22 of 66**, five actually flagged. The 16 missed include **his four certificates** (his
  resume names none) and "GFAI had no product of its own and resold third-party hardware", a
  claim about a **named third-party company**. He would have signed line 1 blind.
- **Kept, not struck (FQ35).** All 22 are copy already published in his name; deleting them from
  the file his AI answers out of would be us editing his life. **Sheet → 6 lines**; line 6 names
  them in his language and asks keep-all or name-what-to-remove. DoD already allowed 6.
- **Second fix, same class:** struck "**The same work shipped as**" from the body — no source
  says the resume's Text-to-SQL CRM assistant and the site's RAG chatbot are one project. It is
  the identical inference Fern correctly refused for the gateway; he built this bridge anyway.
- **FQ33 → SPEC D5:** the eleven `projects.ts` entries stay out of `PROFILE.md` and become
  REQ-007's second knowledge file — written down so `PROFILE.md` never silently becomes the AI's
  only source. **FQ34 → D3 amended (Class 1b):** I did **not** widen Class 1; exactly one named
  shipped string may move, only on his word, offered as two shapes inside line 2 — no 7th line.
- TASK-021 gained the two matching conditional steps + DoD (Class 1b; delete-what-he-names).
- @Porter: **SQ28** in `inbox/PM.md` — FYI + the sheet you deliver is now 6 lines. @Fern: rework
  brief in `inbox/FE.md`. Ball: **Fern**. No DATA REQUEST. **No gateway call spent — his 5 are
  untouched.**

## [--:--] Fern (FE)

- Time is `--:--` on purpose: this machine reads 14:59, irreconcilable with the 21:05 entry above.
  Appended **after** Sober's. Inbox FE: his TASK-020 `REWORK` brief read, acted on, **DELETED**.
- **One unit: the TASK-020 rework. `REWORK` → `REVIEW`, 5/5 rework DoD evidenced.**
- **Fix 1 by script, not by eye:** every (b) row citing `[S:` with no `[R:` and no `[O:` now carries
  exactly `**site-only, no resume backing**` (three older flag wordings normalised to it). Re-scan:
  **66 rows, 22 unbacked, 22 flagged, 0 mismatch**. "Six claims" → a **22 of 66** statement plus a
  six-group breakdown that sums to 22.
- **Fix 2:** `The same work shipped as` struck (`grep -c` = **0**), his replacement sentence verbatim.
  **Fix 3:** sheet is **6 numbered lines**; line 6 his wording, line 2 carries FQ34's option 1 /
  option 2 with both exact strings and no recommendation.
- **Zero code, proved by comparison:** git status / diff --stat / log -1 / `ls -A back` captured
  **before** and re-run after — identical (four TASK-019 `M` files, 10 ins/10 del, `6c17609`, `back/`
  empty). `grep -ic chatuchak` = **0**; citation markers inside the body = **0**.
- **@Sober, one correction:** his "16 unflagged" is mechanically **17** (22 minus the 5 already bold —
  the extra-Core-AI-items row my prose named but never flagged). No dispute; all 22 are flagged.
- **@Sober, FQ36 (non-blocking):** the strike leaves two adjacent ICM bullets each describing a
  four-month CRM AI build, so **two separate projects** is now inferable — which no source states
  either. I propose no fix; every fix asserts same-or-different, and that is the owner's to say.
- Ball: **Sober** — re-review TASK-020, answer FQ36; TASK-021 is his to unblock. No code, no git.

## [--:--] Sober (SA)

- Time `--:--` on purpose (clock irreconcilable with 21:05). Inbox SA: Fern's TASK-020 rework read
  and **DELETED**. **One unit: the TASK-020 re-review + FQ36.** Porter's **REQ-006 + REQ-007 legs
  stay unconsumed** in `inbox/SA.md` — next SA hop takes REQ-006.
- **TASK-020 `REVIEW` → `DONE`. The REQ-005 approval pack is READY for the owner** — 6-line sheet.
- **Re-scanned by my own script, not his count:** (b) = **22 unbacked, 22 flagged, 0 missing, 0
  flags on a backed row**; the six-group breakdown sums to 22 and matches its rows; bridge clause
  `grep` = **0**, replacement bullet verbatim; repo untouched (four `M` files, `6c17609`, `back/`
  empty). Fresh spot-checks on rows I skipped in round 1: the four certificates match `about.ts`
  and the resume names **none** → rightly flagged; the xAI bullet is resume-verbatim → rightly not.
- **Two numbers of mine were wrong, both corrected on the record:** his "17, not 16" is right
  (22 − 5), and "73 rows" was my parser against his correct 66 (the extra 7 = the breakdown table).
- **FQ36: change nothing in the body, no third pass.** Fix 2 struck an unsourced *same*; every
  repair asserts *same* or *different*, both unsourced. **No 7th sheet line**: it reaches Porter as
  **SQ29** (one clause on line 1) and becomes **TASK-021 step 9** — and SPEC-005, since **D5** sends
  that same bullet into REQ-007's knowledge file. Ball: **Porter**. No DATA REQUEST, no call spent.
