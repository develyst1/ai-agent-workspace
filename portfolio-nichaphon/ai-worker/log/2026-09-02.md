# Log — 2026-09-02 — portfolio-nichaphon

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [07:55] Porter (PM)

- **Date:** dispatcher said 2026-08-30; this machine's clock and the session context both
  say **2026-09-02**, so this file is 2026-09-02 (PROTOCOL §Date discipline). Flagged, not resolved.
- Inbox: Tanya's QA handover. Read, acted, deleted. Board/TEST-001/REQ-001 read first-hand.
- **A and E annotated CLOSED** in REQ-001 §Home acceptance review with her evidence; **C**
  now carries her 1280x600 / 360x740 measurements — the call is still his.
- **Q1-Q3 answered** in TEST-001 §Questions. Q1 footer `© 2025` → non-blocking item **H**
  for the owner (I did not infer the year). Q2 before/after → no, no DATA REQUEST. Q3
  harness-in-repo → not hers, not mine; parked and named for after acceptance.
- **New fact, not interpreted:** the owner committed his own code after QA (HEAD `76ad68e`
  — quotes, PullQuote, HomeStatement). His `ตอนนี้ฉันแก้ไข ok แล้ว` is ambiguous, so nothing
  on his list is ticked from it. R5's canonical Thai may no longer match what ships, and
  TEST-001 is now evidence about the pre-`76ad68e` build only — both written into
  REQ-001 §Owner's own change, with N1-N3 for him.
- **REQ-001 stays SPEC_DONE.** No spec/task/code, no git, no deploy, no new QA round.
- @Tanya: answers are in TEST-001; hold — a re-verify depends on N3. Ball: **the human**.

## [--:--] Porter (PM)

- Owner's instruction, verbatim: `เลิกสนใจการ commmit code ของฉันตั้งใจทำงานกันไป`.
  Recorded as a team-wide **Standing rule** in REQ-001: no role inspects, reports on or
  asks about his commits/HEAD/authorship. The build is what we look at.
- **N1-N3 WITHDRAWN**, not answered, and not re-asked. Nothing on his A-H / AC1 / AC2
  list is ticked by this — a criterion is ticked by his answer to that criterion.
- The two real consequences are now handled by QA instead of by him: **R5 canonical Thai
  vs. what renders** → QA reports the diff to Porter (R5 is not edited by anyone until he
  rules); **TEST-001 staleness** → QA re-run.
- **QA re-verify round issued** (REQ-001 §QA re-verify round): 4 cases only — R5 strings,
  R5 count, item C fold at 1280x600 + 360x740, R7 build/console. Reason stated
  commit-agnostically: TEST-001 predates the current build of PullQuote/HomeStatement.
- Date question closed: 2026-09-02 confirmed correct. REQ-001 stays SPEC_DONE.
- @Tanya: go — brief is in REQ-001, inbox/QA.md updated. @Sober: FYI standing rule only.
- Still owed by the owner: **AC1, AC2** (blocking) and B, C, D, F, G, H. Ball: **Tanya**,
  and **the human** for AC1/AC2 in parallel.

## [08:15] Tanya (QA)

- Inbox read, acted on, deleted. Ran the 4-case re-verify round (REQ-001 §QA re-verify
  round) on **local only**; production never opened. Standing rule observed — no git
  looked at, only what the page renders.
- Port 3000 was already held by a process I did not start (404s on `/`), so my
  `npm run dev` ran on **3001** and the round used that. Declared in the TEST file.
- **R5 count PASS** (two of four: quote 4 hero, quote 2 band). **R7 PASS** — build exit 0,
  10/10 pages, and `/` logs no errors or warnings at 1280x900 and 360x740.
- **Item C re-measured and re-shot**, both viewports; the 2026-08-30 picture is confirmed
  unchanged. Still evidence only — the call stays the owner's.
- **R5 character-for-character NOT TICKED.** The band renders `ผมไม่ทำงาน "ให้" ใคร…`
  (42 chars); R5 canonical is `ผมไม่ได้ทำงาน "ให้" ใคร…` (45). Missing word `ได้` — a word,
  not punctuation. Not ticked, not filed as a defect, nothing edited, per Porter's brief.
- Verdict: **TEST_PASSED (partial)**. REGRESSION H3 now has no agreed baseline.
- @Porter: **Q4** (which Thai is canonical — page or R5?) and **Q5** (does the English pair
  need re-confirming, or does it fold into AC2?) are in TEST-001 §Questions. Ball: **Porter**,
  and the human for AC1/AC2 + B, C, D, F, G, H in parallel.

## [--:--] Porter (PM)

- Inbox read, acted on, deleted. Owner answered three things in chat; all recorded verbatim
  where they were asked (REQ-001 §Home acceptance review, TEST-001 §Questions).
- **AC1 = yes** (`ใช่ ไม่เหมือน claude แล้ว`) → identity criteria ticked. **AC2 = `อนุมัติ`**
  → Porter's English for quote 2 is final. Both blocking items are closed.
- **Q4 = `เอาตามเว็บ เพราะนั่นฉันแก้เองกับมือ`** → the page is canonical. **R5's quote-2 Thai
  updated to the 42-char string QA measured; no code touched, no defect.** Q5 folds into AC2.
- **10 of 11 acceptance criteria now ticked.** The character-for-character one is left open
  on purpose: I changed R5, so I do not tick it — one-case **§QA confirm tick** issued.
  H3's baseline is R5 as it now stands.
- His "but" is **new scope, not a defect**: N4 (make it `เท่ / เบียว` further) and N5 (the
  portfolio content is out of date). Parked in REQ-001 §New asks with **Q14-Q17** to him —
  scope, a concrete anchor for "เท่/เบียว", and a DATA REQUEST for the real new material.
  **No REQ-002 is written until he answers**; R6's five-route REQ is held for the same reason.
- REQ-001 stays SPEC_DONE. Still his and non-blocking: B, C, D, F, G, H.
- @Tanya: one confirm case, brief in REQ-001. Ball: **Tanya**, and **the human** for Q14-Q17.

## [19:45] Tanya (QA)

- Inbox read, acted on, deleted. Ran the **one-case confirm-tick round**
  (REQ-001 §QA confirm tick) on **local only**; production never opened.
- Port 3000 was **free** this time, so the round ran on `http://localhost:3000`
  (not 3001). Harness `tests/harness/confirm-tick-2026-09-02.cjs`; Playwright
  still installed outside the repo, `front/package.json` untouched.
- **PASS.** All three quote strings on `/` are R5-exact against R5 as it now
  stands: quote 4 (29 chars, hero), quote 2 Thai (42, band, `lang="th"`),
  quote 2 English (46, band, `lang="en"`). Count still two of four; console clean.
- The 88-char "mismatches" the harness prints are ancestor containers
  (Thai leaf + English leaf concatenated, first diff at index 42) — not findings.
  Noted in the TEST file so nobody re-reports them.
- **Criterion ticked in REQ-001** per Porter's written delegation, with an
  evidence pointer. TEST-001 is now **TEST_PASSED** (partial closed).
  **REGRESSION H3 baselined and PASS** — baseline is R5 as of 2026-09-02.
- @Porter: all 11 AC are ticked; DELIVERED is your call. One boundary note in
  TEST-001 §Questions — QA edited REQ text only because you instructed it.
  Ball: **Porter**, and **the human** for B, C, D, F, G, H and Q14-Q17.

## [--:--] Porter (PM)

- Inbox read, acted on, deleted. Board, REQ-001 §Acceptance Criteria and TEST-001
  re-read first-hand before moving anything.
- **REQ-001 is DELIVERED (2026-09-02).** Verified, not assumed: **11 `[x]`, 0 `[ ]`**
  counted in §Acceptance Criteria; 5/5 SPEC-001 TASKs DONE; TEST-001 `TEST_PASSED`;
  REGRESSION H3 baselined+PASS. This REQ's own gate — "stays SPEC_DONE until every
  blocking item is answered" — is met: AC1 and AC2 are both answered by the owner.
- New **§Delivery** section in REQ-001 records what DELIVERED rests on **and what it
  does not close**: B, C, D, F, G, H stay the owner's open calls (D is a check only
  he can run), N4/N5 + Q14–Q17 stay parked, quotes 1/3 English stays unconfirmed.
  Stale prose in §History and the §Acceptance Criteria note corrected.
- **Boundary ruling for QA** answered in TEST-001 §Questions: **QA does not edit REQ
  text**; the confirm-tick delegation was a one-off and is spent. Her tick stands —
  nothing about that round is undone.
- No spec/task/code touched, no git, no deploy. R6's five-route REQ stays held.
- @Tanya: TEST-001 closed with REQ-001; nothing queued for you. @Sober: REQ-001
  DELIVERED, no new work — next REQ waits on Q14–Q17. Ball: **the human**.

## [--:--] Porter (PM)

- **Housekeeping hop only — no project work.** Inbox `PM.md` empty; nothing to
  delete. Hygiene gate was FAIL (2); both FAILs fixed, nothing else touched.
- **dispatcher-state.md**: 9 runs -> 5. Runs `-d`, `-e`, `-f`, `-g` archived
  **byte-for-byte** (verified with `diff`) to
  `archive/dispatcher-state-2026-09-02.md`; header rotation note updated.
- **REQ-001** 47.5KB -> 40.8KB. Original archived verbatim (`cmp`-verified) to
  `archive/REQ-001-ui-visual-redesign-2026-09-02-preconsolidation.md` first.
  Consolidated: Q1-Q13 folded into an index pointing at R1-R10 (both decision
  records kept — Q10's scope note, Q12's unrecorded-question gap); the four QA
  sections merged into one §QA rounds record. **R1-R10 numbering, all 11 `[x]`
  AC and the 6 open owner slots (B, C, D, F, G, H) are unchanged.**
- Re-run: `RESULT: PASS (1 warning(s))`. No specs/tasks/tests/code/board touched.
  Ball: **the human** (Q14-Q17 still gate REQ-002 and R6's five-route REQ).

## [20:13] Porter (PM)

- Inbox `PM.md` empty — nothing to delete. Board, REQ-001 §New asks and PROTOCOL re-read
  first-hand before writing.
- **Q14–Q17 answered by the owner and CLOSED** in REQ-001 §New asks, verbatim.
  Q14 `ทั้งเว็บ` · Q15 `เอาแนว AI + robotic + IOT` · Q16 `ทั้งเว็บ` · Q17 = two of his real
  projects handed over as **source material** (learning.develyst.online, ong.develyst.online
  + four GitHub repos, branches verified reachable read-only). Nothing cloned.
- **REQ-002 opened, `READY_FOR_SA`**: whole-site visual step-up in the AI/robotic/IoT
  direction, **merged with R6's five-route rebuild** — Q14's sequencing half was never
  answered, so the merge is stated as Porter's scope for the owner to overrule, not a guess.
- **Boundary written down** so no role trips on it: the standing rule covers his commits on
  *this* repo — it does not forbid reading the four handed-over repos. Read-only, no logins,
  no credentials ever; a login-gated screen is a DATA REQUEST (he screenshots).
- **N5 still has no REQ.** Scope is answered, sources arrived, but copy cannot be written
  until **Q20** (publish permission / NDA) and **Q21** (who writes it + role, dates, client,
  results — facts no repo contains) are answered. R4 is not lifted by a repo link.
- New with the owner: **Q18** (AI/robotic/IoT = visual only, or positioning too?) and
  **Q19** (toggle on the other five routes — REQ-001 Q13 decided Home only). Neither blocks
  spec start. No spec/task/code/git/deploy touched.
- @Sober: REQ-002 is yours. Ball: **Sober**, and **the human** for Q18–Q21.

## [20:21] Sober (SA)

- Inbox `SA.md` read, acted on, deleted (3 messages: standing rule, REQ-001 DELIVERED, REQ-002
  ready). Board, PROTOCOL, SA-Lead, REQ-002 and SPEC-001 re-read first-hand before designing.
- **REQ-002 IN_SPEC. SPEC-002 written (ACTIVE)** — one step-up layer applied once and inherited
  by six routes, not six route redesigns: token/theme extension, three shared devices
  (machine ground, `route` aurora, `GlassPanel tone="node"`), then the five route partials.
- **Read the real code before designing.** The retired patterns SPEC-001 named are still alive
  on all five routes; SPEC-002 lists them by file and line so review can grep for them.
- **Fact found in the build, not assumed:** `forceColorScheme="dark"` is set globally
  (`UIProvider.tsx` + `layout.tsx`) and the toggle is mounted nowhere — the five routes have
  **no** light/dark control today, so Q19 is being asked about a state that has already moved.
- Designed OUT rather than guessed: ordinal numerals (`01`, `02`) on cards with no number in
  content — a rendered numeral is user-visible text and R4 is absolute (SQ2).
- q3 -> About, q1 -> Services (R5 permits the distribution). Their English is still unconfirmed
  by the owner — SQ3, per REQ-002 R5 that is yours to put to him at review.
- No code, no git, no deploy, no DATA REQUEST. TASK-006..011 are planned in the SPEC and are
  my next unit of work.
- @Porter: SQ1-SQ6 are in SPEC-002 §Questions — none blocks the build starting. Ball: **Sober**
  (write TASK-006/007), and **Porter** for the six notices.

## [--:--] Sober (SA)

- Inbox `SA.md` empty. Board, SPEC-002 and the real code re-read first-hand before cutting.
- **TASK-006 + TASK-007 written (TODO, Fern)** — SPEC-002's foundation layer, which
  008-011 all depend on. 008-011 stay planned only; that is the next SA unit.
- **006** = 5 new tokens (both resolver blocks) + Accordion/Modal/Input/Alert skins +
  `TechChip` 4px -> pill. **Verified by grep, not assumed:** none of those five renders
  anywhere under `partials/Home/`, so R6 is safe by construction.
- **007** = `MachineGround`, `AuroraBackdrop variant="route"`, `GlassPanel tone`,
  `RouteHero`, plus the L 0.046 measurement and a Home non-regression pass.
- **I corrected my own SPEC:** D1 at `z-index: 0` would have painted the lattice *over*
  the page (a positioned z-0 box paints above in-flow content). Now **-1**, reason written in.
- **Two SA calls, recorded not guessed:** `Drawer` deliberately NOT skinned (mobile nav =
  on Home, R6 binds); TASK-007 pilots `RouteHero` on **About's opening block only**.
- Ordinals stay designed out (SQ2). No code, no git, no deploy, no DATA REQUEST.
- @Fern: 006 then 007. @Porter: SQ1-SQ6 open, none blocking. Ball: **Fern**, then Porter.

## [--:--] Fern (FE)

- Inbox `FE.md` read, acted on, cleared. PROTOCOL, FE.md, board, SPEC-002, TASK-006 re-read.
- **TASK-006 implemented, REVIEW.** Two files only: `theme.ts` (5 tokens both blocks +
  Accordion/Modal/Input/Alert skins) and `TechChip.module.css` (4px -> 999px pill).
  No route markup, no Drawer, no string, no new hex.
- Evidence: build clean, typecheck clean, six routes on dev **port 3010** with an empty
  console, five variables read back non-empty, 6-row contrast table measured (lowest 7.52:1).
- **Home non-regression as a real A/B**, not an assertion: pristine HEAD restored into the tree,
  every body element diffed — **0 differences** at 1280x800 (122 el) and 360x740 (124 el).
  Tree restored, no git writes.
- **Pre-existing defect, NOT fixed (FQ13): Modal AND Drawer never open** anywhere — state flips,
  Mantine never mounts the content. Proved against pristine develop. So the Modal skin is UNVERIFIED.
- FQ14-FQ18 = look/scope calls I refused to guess (alert ink, accordion gap, modal header,
  error-alert discriminator, accordion hover). All in the TASK §Questions.
- @Sober: TASK-006 ready for review; FQ13 needs your call. Ball: **Sober**.

## [--:--] Sober (SA)

- Inbox `SA.md` read, acted on, deleted. Board, PROTOCOL, SA-Lead, TASK-006 and the **real
  diff** re-read first-hand. One unit: the TASK-006 review.
- **TASK-006 DONE.** Re-verified by me, not inherited: `git diff` = 2 files/+83-3, all five
  tokens in BOTH resolver blocks at the exact spec values; R6 Home grep re-run (exit 1);
  `npx tsc --noEmit` exit 0; no `.next` left; Drawer/Button absent from the diff. Build,
  console, contrast and the 0-diff Home A/B **accepted on Fern's evidence, not re-run**.
- **DONE does not tick the Modal skin** — ground/edge/header/overlay stay UNVERIFIED, so
  nobody ticks them from the code later.
- **FQ13 -> SQ7:** no Modal and no Drawer opens on `develop` = no portfolio detail, no
  lightbox, no mobile nav. Functional defect **outside REQ-002**, so I did NOT cut a fix
  TASK (that is inventing scope) — Porter gets the owner's scope call + a QA repro ask.
- **FQ14/FQ17/FQ18 carried to TASK-010** (alert ink to one hue, measured; positive
  `color === 'green'` discriminator; accordion hover off Mantine's opaque grey).
  **FQ15/FQ16 confirmed as built.** All written into SPEC-002 §Tasks.
- No code, no git, no deploy, no DATA REQUEST. @Fern: TASK-007 next. @Porter: SQ7.
  Ball: **Fern**, and **Porter** for SQ7.

## [22:10] Fern (FE)

- Inbox `FE.md` read, acted on, cleared. PROTOCOL, FE.md, board, TASK-007 and TASK-006 §Review
  re-read first-hand. One unit: TASK-007.
- **All four devices built + About pilot mounted — TASK-007 is BLOCKED, not REVIEW (FQ19).**
  Added `MachineGround` and `RouteHero` (both server components); `AuroraBackdrop` gained
  `route`, `GlassPanel` gained `tone`. `.hero`/`.band`/`.panel` untouched. 2 new dirs, 7 files
  modified, +85/-12. No string added, altered or translated. No git write, no deploy, no `.next` left.
- Evidence: `tsc` exit 0; `build` exit 0 with **no warning line**; six routes on dev **port 3010**
  with `onlyErrors` -> **"No console logs."**; 360px no h-scroll on `/` and `/about`.
- **Home A/B is real, not asserted:** my 7 files reverted to HEAD, every `<body>` element diffed
  at both viewports — **exactly one added element (D1's ground), zero other differences**,
  115 -> 116 el. Panel `className`s byte-identical before/after.
- **STOP-AND-ASK (FQ19).** D1 composited under Home's hero: peak **L 0.05477 → 4.12:1**
  (was 0.03883 → 4.86:1). Ceiling 0.046 is crossed at **2** lattice layers (0.04706). `/about`'s
  RouteHero **clears** (0.03961 / 0.03655) and Home's band clears (0.04409). Sampler validated:
  lattice-off reproduces TASK-004's 0.03883 exactly. **I retuned nothing** — five levers named
  (grid-line alpha ~0.022 clears; masking D1; accepting a 1px breach) — all yours.
- Non-blocking, recorded not guessed: FQ20 About's RouteHero is 1243px tall (h1 at 136px, pre-existing);
  FQ21 pre-existing `H2 -> H4` skip in `AboutValues.tsx:19`; FQ22 `tone="node"` has no consumer
  so it is computed-verified only; FQ23 reduced-motion flag not emulable this session.
- @Sober: TASK-007 needs your FQ19 call before it can go to REVIEW. Ball: **Sober**.

## [22:35] Sober (SA)

- Inbox `SA.md` read, acted on, deleted. PROTOCOL, SA-Lead, board, TASK-007 (full), TASK-006,
  SPEC-002 and **SPEC-001's ceiling clause** re-read first-hand. One unit: answer FQ19-FQ23.
- **FQ19 answered — TASK-007 unblocked, ceiling NOT relaxed.** Before deciding I checked
  whether the rule is still header-band-scoped (if it were, most breaching points sit below
  the band and there is no breach): it is **not** — SPEC-001 was widened 2026-08-30
  (TASK-004 §FQ6) to *"no point of the composited hero aurora may exceed L 0.046"*, and
  REQ-001 §Non-functional carries it. Fern's whole-box scan is the correct scan.
- **Lever 1, value set by me: `--site-grid-line` dark `0.045` → `0.02`** (`theme.ts:262`
  only; light `theme.ts:232` untouched — dark-only site). Not Fern's 0.022: Chrome quantises
  alpha to 8 bits, so 0.022 renders **up** to 6/255 (~1% headroom) and 0.02 renders **down**
  to 5/255 → ≈L 0.0438, ≈4.60:1, ~4.7% headroom. Levers 2-5 rejected with reasons recorded
  once in FQ19 (lever 3 fails on mechanics: D1 is `fixed`, so a mask is viewport-relative).
- Fern re-applies + re-measures in one pass; **> 0.046 = stop and ask again**, no retuning.
- **FQ20 → SQ8** (About's opening block is 1243px; h1 scale is REQ-001-accepted, untouched —
  owner's eye, TASK-008/009/010 will each report their height). **FQ21 → TASK-008** (fix the
  pre-existing `H2→H4` skip in `AboutValues.tsx`); TASK-007's box stays unticked.
  **FQ22/FQ23 confirmed** UNVERIFIED-by-eye — nobody ticks them from the code.
- Verified Fern's import-cycle claim in the repo myself; deep `ui → common` import approved
  and written into SPEC-002 as a standing rule. TASK-006 §Review flagged stale on that token.
- No code, no git, no deploy, no DATA REQUEST. @Fern: TASK-007 is yours again.
  @Porter: SQ8 + two QA eye checks. Ball: **Fern**.

## [23:20] Fern (FE)

- Inbox read + cleared; PROTOCOL/FE.md/board/TASK-007 (all five FQ answers) re-read. One unit:
  apply FQ19's value + re-measure. Detail and full tables: TASK-007 §Second pass + §FQ24.
- **Applied exactly what you set:** `theme.ts:262` `0.045` -> `0.02`; line 232 and
  `MachineGround.module.css` untouched, nothing else changed. `tsc` 0; `build` 0, **no warning
  line**; `.next` deleted; dev on 3010, stopped. No git write, no deploy.
- **Quantisation confirmed on the engine:** `0.02` and `0.021` both = **5/255 = 0.019608**; `0.022`
  would have rendered **up**. Sampler rebuilt and re-validated — lattice off reproduces TASK-004's
  **0.03883 at (202,81)** and 0.03873 exactly.
- `/about` and Home's band **clear everywhere** (worst 0.03081 / 0.04019). Home's hero **passes at
  your two viewports**: **0.04575 (4.509:1)** at 1280x800 — *inside your (0.0450, 0.046] strip,
  flagged as instructed* — and 0.04462 at 360x740.
- **STOP-AND-ASK (FQ24).** D1 is `fixed`, so lattice phase is free in y and moves with width in x.
  At **1217x800** the hero's peak column lands on a 64px line: **L 0.04671 -> 4.464:1, over.**
  Measured, not argued; **I retuned nothing.** Sweep: 5/255 OVER · 4/255 0.04510 (in the strip) ·
  3/255 0.04351 clear — only three renderings exist.
- **A/B re-run:** 0.02 vs 0.045 in-browser = **zero element differences** (119 v 119, both
  viewports); ground on/off = **exactly one element**. File-revert A/B blocked this session; first
  pass's stands. Perceptibility: 3 of 4 captures unrendered -> **no claim**.
- @Sober: BLOCKED on FQ24 — one alpha and I re-measure all three widths in one pass. Ball: **Sober**.

## [23:45] Sober (SA)

- Inbox `SA.md` read, acted on, cleared. PROTOCOL, SA-Lead, board, TASK-007 §FQ24, SPEC-002
  and **the real CSS in the repo** re-read first-hand. One unit: end the FQ24 loop.
- **I did not give Fern a third alpha.** Two values were set against sampled viewports and
  both failed at an unsampled one; a third would fail the same way. The breach is a *phase*
  coincidence (D1 is `fixed`, the aurora is sized in %), so width is a free variable and alpha
  only scales its coefficient. **The mistake was mine at FQ19** — recorded as mine.
- **Lever changed, read from the code not inferred:** D1 reaches the hero only because
  `HomeHero .hero` and `RouteHero .hero` have **no background**. `HomeStatement .band` already
  carries opaque `--site-surface` — the pattern exists; two blocks forgot it.
- **Decision (3 CSS edits, no numbers from me):** `--site-grid-line` dark back to **`0.045`**
  (my `0.02` withdrawn); both `.hero` parents get `background-color: var(--mantine-color-body)`
  (= `obsidian[9]` `#0b0916` — the exact bare ground Fern sampled). **Not on `.aurora`** —
  `inset:0` inside `.band` too, it would repaint the band's surface = an R6 regression.
- **The loop is closed by construction:** with the parents opaque there is no lattice term
  left, so the hero must fall back to TASK-004's `0.03883` at every width. **Bounded third
  pass:** strip flag retired; PASS = D1-on vs D1-removed equal for all three aurora blocks at
  1217/1280/360 → REVIEW **without asking**; on FAIL Fern retunes nothing and it goes to
  **Porter**, not back to me. Band is the control — its 0.04409/0.04019 discrepancy is flagged
  to be *reported as measured*, not explained away by either of us.
- Standing rule written into SPEC-002 §Edge cases (binds TASK-008..011). **SQ9** to Porter:
  the lattice will not read behind Home's hero or a route's title block — owner's eye only,
  gates nothing; relaxing the ceiling stays the owner's call, never mine.
- No code, no git, no deploy, no DATA REQUEST. @Fern: TASK-007 is yours, third and last pass.
  @Porter: SQ9 (plus SQ7/SQ8 still open). Ball: **Fern**.

## [--:--] Fern (FE)

- Inbox cleared; board + TASK-007 §FQ24 re-read. One unit: the bounded third pass.
  **PASS — TASK-007 is REVIEW and no value comes back to you.** Tables: §Third pass.
- Your three CSS edits applied verbatim; nothing on `.aurora`; no other file touched.
- **D1 contributes exactly zero** (as rendered vs ground removed at runtime) for all three
  aurora blocks at **1217/1280/360**: hero **0.03884/0.03883/0.03873**, band
  **0.04114/0.04116/0.04111**, `/about` **0.02324/0.02324/0.02321** — all ≤ 0.046, worst
  ratio 4.736:1. Your predicted fallback to TASK-004's 0.03883 is met.
- **Not only the sampler:** grid forced opaque red — zero red inside the hero, red in the
  1px row above it; removing only the new `.hero` background floods it.
- **Band control as measured: 0.04116, never moves.** Your 0.04409/0.04019 were taken over
  canvas+lattice, not the band's opaque `--site-surface` (that base reproduces 0.04348).
- **R6 as budgeted:** 114 v 113 el, two differences — D1's ground, `.hero` bg. `tsc` 0;
  `build` 0 no warning; six routes on **3010** "No console logs."; no h-scroll at 360;
  `.next` deleted; dev stopped; no git write, no deploy.
- Unticked on purpose: `/about` semantics (FQ21 → TASK-008), reduced motion (FQ23); no
  perceptibility claim. @Sober: ready for review, no question attached. Ball: **Sober**.

## [--:--] Sober (SA)

- Inbox cleared. PROTOCOL, SA-Lead, board, TASK-007 (third pass, DoD, FQ19-FQ24) and
  **the real repo** re-read first-hand. One unit: the TASK-007 review.
- **TASK-007 DONE.** My FQ24 gate is met by falsification, not assertion: D1 contributes
  **0** to all three aurora blocks at 1217/1280/360, worst **0.04116 ≤ 0.046**, hero back
  at TASK-004's 0.03883 — the prediction I published was testable and it survived.
- **Re-verified by me:** the three CSS edits are exactly mine and nothing else; **no
  `background-color` on `.aurora`**, `HomeStatement.module.css` absent from `git status`;
  dark `--mantine-color-body` = `obsidian[9]` `#0b0916` checked in the palette; `tsc` 0;
  hex grep exit 1; one `"use client"` under `ui/`, as at HEAD. **Mechanism too:** `.aurora`
  is `absolute; z-index: 0`, so an opaque parent cannot occlude it — only D1 (`fixed`, `-1`)
  is cut; Fern's opaque-red render agrees. Build/console/A-B/luminance **accepted, not re-run**.
- **Band discrepancy resolved against me:** 0.04409/0.04019 were my sampler's base being
  wrong, not the page moving — neither number is quoted again.
- **DONE ticks nothing extra:** FQ21 → TASK-008, FQ23 → QA via SQ8; `tone="node"`, the Modal
  skin and perceptibility stay UNVERIFIED. REQ-002 stays `IN_SPEC` (008-011 unwritten).
- No code, no git, no deploy, no DATA REQUEST. @Porter: SQ7/SQ8/SQ9 still open.
  Ball: **Sober** (write TASK-008..011).

## [--:--] Sober (SA)

- Inbox `SA.md` empty. PROTOCOL, SA-Lead, board, SPEC-002 and **the real code of all five
  routes** read first-hand before cutting. One unit: TASK-008..011, the rest of SPEC-002.
- **008** About+Services (q3, q1, FQ21 heading fix) · **009** Portfolio+Blog · **010** Contact
  + the FQ14/FQ17/FQ18 theme follow-ups (the only TASK allowed to touch `theme.ts`) ·
  **011** the six-route evidence sweep. All TODO, Fern, order 008→009→010→011.
- **Three SA corrections to my own SPEC, each with the reason written in.** (a) `ServicesTable`
  **stays a `<table>`** — cards would delete the three `<th scope="col">` strings and the
  `role="region"` structure (R4 + §Non-functional). (b) The **label recipe is pinned to exact
  values** — "SectionHeading's label treatment" was a pointer five files would read five ways.
  (c) `RouteHero` renders from `app/portfolio|blog/page.tsx`, not inside the `"use client"`
  components — otherwise the whole opening block joins the client bundle.
- **Two facts found in the build, not assumed.** New pre-existing **`H1 → H3` skip** at
  `PortfolioGrid.tsx:19` (fixed in 009, same class as About's). And **SQ2's premise was wrong,
  mine to correct:** position-derived ordinals already ship on `/portfolio` and on Home
  (REQ-001-accepted). Existing ones kept byte for byte; **no new one added** until Porter
  answers — an accepted layout is not a rule I may infer.
- Designed out, not gold-plated: About's certificates + testimonials stay as they are (no
  retired pattern, no card surface). SQ7 gates every Modal look in 008/009 — unticked by design.
- No code, no git, no deploy, no DATA REQUEST. @Fern: 008 first. @Porter: SQ2 update + SQ7/8/9.
  Ball: **Fern**, and **Porter** for the notices.

## [23:40] Fern (FE)

- **TASK-008 built and verified — status BLOCKED, not REVIEW**, on two stop-and-asks.
  12 files, all under `partials/About` + `partials/Services`; nothing else in `src` differs
  from the pre-task snapshot (`theme.ts`, `globals.css`, `components/ui/` untouched).
- Evidence: `npx tsc --noEmit` 0 · `npm run build` 0 with **no warning line**, `.next` deleted ·
  six routes clean of errors AND warnings at 1280x800 and 360x740 (dev on **port 3010**) ·
  **R6 Home A/B: 119 elements before, 119 after, zero difference at both widths** ·
  no horizontal scroll at 360 · all `aria-*` / `role` / `scope` / `lang` / tab order preserved.
- **D1 contributes exactly 0** on `/services`' new opening block at 1217 / 1280 / 360 —
  peak L **0.02322-0.02323** against the 0.046 ceiling. `RouteHero`'s opaque ground holds.
- SQ8 heights, reported not acted on: `/about` **1243.03 / 532.88 px**, `/services`
  **742.59 / 371.52 px** (1280x800 / 360x740).
- **FQ25 (stop-and-ask):** `.organisation` drops **4.56 -> 4.23:1** once entries sit on glass —
  under the 4.5 bar. Three options written out; **I changed nothing**. Every other pair passes.
- **FQ26 (stop-and-ask):** A2 says keep `.marker` uppercase, the DoD grep says return nothing —
  one line, one contradiction. Same root: the label recipe's `text-transform: none` case-changes
  8 skill labels + 3 table headers in `innerText` while **no source string changed**
  (`diff -r src/constant` clean), so the R4 box as worded cannot be ticked either.
- FQ27: the `use client` DoD box says "four files"; the repo has 10 — identical before/after.
- SQ7 honoured: nothing about the lightbox's open state is ticked, measured or worked around.
- No git write, no deploy, no `pm2`, no ssh, no DATA REQUEST.
  @Sober: TASK-008 needs FQ25 + FQ26 answered before it can go to REVIEW. Ball: **Sober**.
