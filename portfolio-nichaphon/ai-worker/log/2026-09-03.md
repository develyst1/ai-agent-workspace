# Log — 2026-09-03 — portfolio-nichaphon

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [08:05] Porter (PM)
- Date note: this machine's clock reads **2026-09-03 08:0x**; the dispatcher labelled the run
  `2026-09-02-d`. PROTOCOL §Date discipline says settle TODAY from the real clock, never from a
  file — so this file is 2026-09-03. Flagged to the human, not resolved by me.
- Inbox PM: empty (7 SA notices were cleared in the previous housekeeping hop). Nothing dropped.
- Owner answered **SQ7**, verbatim: `ให้ Tanya reproduce ก่อน`. Recorded in
  `requirements/REQ-002-...md` §Questions SQ7 (not in specs/ — that is Sober's file).
- Read strictly: the **QA repro is granted and comes first**; the **scope half of SQ7 (fix now as
  its own defect REQ vs ship REQ-002 visual-only with the modal look UNVERIFIED) is NOT answered**.
  I am not inferring it — it goes back to the owner once a verdict exists.
- @Tanya: **TEST-002** requested — local, clean `develop`, three triggers (Portfolio card,
  About/testimonial lightbox, mobile burger). Open the TEST file yourself and own its status;
  a cannot-reproduce is a real verdict. See inbox/QA.md.
- @Sober: your SQ7 gate stands unchanged — modal look never ticked from source, no route-file
  workaround, TASK-008..011 continue. See inbox/SA.md.
- Board: SQ7 row waits on Tanya then the owner; TEST-002 row added; REQ-002 header aligned to IN_SPEC.

## [08:15] Tanya (QA)
- Inbox QA: 2 messages read and deleted. REQ-001 boundary note answered (QA does not edit REQ
  text) — accepted, nothing owed. TEST-002 requested by Porter — done, this entry.
- **TEST-002 = CANNOT_REPRODUCE.** Ran the three SQ7 triggers on local `npm run dev` (port 3000,
  free; `.next` cleared first). All three OPEN, paint and close: `/portfolio` ProjectModal,
  `/about` certificate + testimonial lightboxes (both kinds), `/` @390px burger Drawer. Portal
  is not empty — `[role=dialog]`=1, content=1, overlay=1, scroll-locked, then back to 0 on close.
- Hydration control on every page (`header[data-scrolled]` false→true on scroll) so a dead modal
  could not have been masked by a dead bundle. 0 console errors. Run twice, same both times.
- **Scope I did NOT cover, stated so silence is not read as a pass:** the modal/drawer LOOK is
  still UNVERIFIED (Sober's gate, untouched); only `next dev` was exercised, never a build; and
  checkout state is asserted by nobody (standing rule) — QQ1.
- **DEF-1 (MAJOR), found in passing, NOT SQ7:** all 9 `ImageLightbox` thumbnails on `/about`
  render at 0x0 px — four certificates and five conversations show no image at all, only the
  hint bar. Images load fine (natural sizes read) and display correctly inside the modal, so the
  collapse is the thumbnail frame. 9 console warnings, `fill` + height 0.
- Verdict, evidence (11 screenshots), footprint and QQ1-QQ3 in
  `tests/TEST-002-sq7-modal-drawer-repro.md`; shots in `../project-docs/qa-test002-2026-09-03/`.
  Harness `tests/harness/test002-modal-drawer-2026-09-03.cjs`; `front/package.json` untouched.
- @Porter: verdict + DEF-1 routing + QQ1 (want a `next build` round?) are yours. Dev server stopped.

## [08:19] Porter (PM)
- Inbox PM: Tanya's TEST-002 message read, acted on, deleted. Nothing dropped.
- **TEST-002 = CANNOT_REPRODUCE accepted as written** — I do not remap it onto PROTOCOL's four
  TEST statuses: `TEST_PASSED` would claim the modal *look* was accepted (it was not),
  `NOT_TESTED` would be false. Vocabulary gap noted, PROTOCOL not mine to edit.
- **QQ1 = yes.** Build round granted as **TEST-003** (`npm run build` + `npm run start`, local
  only): dev is not the shape that ships, and a stale `.next` build output sat there before her
  run, so the built surface is untested by anyone. Same 3 triggers + observe DEF-1's thumbnails.
- **QQ2 = (b), a new finding.** Grepped specs/ + tasks/: a collapsed thumbnail is recorded
  nowhere. Relayed to @Sober to place; I propose no fix. **Nobody has established** whether DEF-1
  pre-existed or arrived with TASK-008's built files — not assumed by me.
- **QQ3 = stays the owner's** — the verdict does not tick the modal look. **Whether it lifts the
  gate is @Sober's call**, relayed as a fact, not an instruction.
- Owner asked in Thai: SQ7 scope half (ship with the look UNVERIFIED or hold); does DEF-1's repair
  sit inside REQ-002; do the older opens (Q18/Q19/SQ8/SQ9/N5 Q20-Q21) get answered this round.
- Files: REQ-002 §Questions (SQ7 + DEF-1), TEST-002 §Questions (answers only), board.md
  (TEST-002, +TEST-003, SQ7 retitled, DEF-1 re-owned), inbox PM/SA/QA.

## [--:--] Sober (SA)
- Date flag: my context says **2026-09-04**, dispatcher says **09-03**; wrote 09-03 to stay with the
  day's thread — @Porter, third wobble, put it to the human. Inbox SA: 4 read, acted, cleared.
- **TASK-008 UNBLOCKED → IN_PROGRESS.** FQ25 = **(a)**: one token swap at `AboutExperience.module.css:35`
  (`--site-ink-faint` → `--mantine-color-dimmed`, 7.52/5.84 from Fern's own table). (b) rejected —
  theme.ts is TASK-010's and would move 5 unbuilt call sites blind; (c) rejected — 4.23 is a
  regression A2 caused and I wrote A2.
- FQ26: **A2 beats the blanket grep**, and a CSS-casing change is not an R4 text change (no source
  string moved) — both boxes re-worded, neither waived. FQ27: my "four files" was wrong (11) — fixed
  in TASK-008 **and** 009/010/011 so it cannot bounce three more times.
- New SA rule: `--site-ink-faint` never carries text on a GlassPanel — swap per route CSS module,
  never theme.ts (TASK-009/010 §0). Its pre-existing 3.64 page-ground fail → TASK-011 known-fail.
- **DEF-1 placed as TASK-012**, blocked on scope only. Cause: `.frame` is an inline `<span>`, so
  `width`/`aspect-ratio` never apply and `fill` resolves against 0x0 — the modal frame is a `<div>`
  and works, exactly QA's observation. **Provenance established by git**: those files last changed at
  `7a58154` (2026-08-27), tree clean vs HEAD, no `span` rule touched in globals.css → predates the
  team, did **not** arrive with TASK-008. Limit: a static read, not a render.
- **SQ7 gate unchanged**; whether TEST-002 lifts it is deferred to a hop after TEST-003 (on board).
- @Fern: TASK-008 close-out = 1 CSS line + 3 re-ticks, then REVIEW. @Porter: **SQ10** (is DEF-1's
  repair inside REQ-002?) is all that blocks TASK-012; FYI 11 visible labels change letter case.
