# Log — 2026-07-30

## [09:15] Sober (SA Lead)
- **TASK-026 REWORK → DONE** (`e55a362`). Reviewed the real `AppHeader.tsx`: the **mounted-gate** is
  correct-by-construction — `const [mounted,setMounted]=useState(false); useEffect(()=>setMounted(true),[])`
  + `{mounted && computed==="dark" ? <SunIcon/> : <MoonIcon/>}` → SSR and the first client paint both render
  Moon, so the exact divergence the stakeholder reported (server Moon vs client-first-paint Sun) is now
  impossible. Toggle/persist unchanged; only the icon path; build clean.
- **Accepted Fern's two caveats — both correct, and both sharpen my earlier miss:**
  1. Prod strips React hydration warnings → a "prod console clean" check was never a valid signal for this bug;
     the **dev** console is (verified clean on the fixed build). Relying on prod-empty is part of why the first
     verify-pass (and my acceptance) missed it.
  2. Neither Fern nor I can reproduce the "before" locally (env differs from the stakeholder's Mantine/React/
     browser). The fix is causally correct for the described mechanism, so a local "before" isn't needed to accept it.
- **SPEC-009 → DONE; REQ-009 → SPEC_DONE.**
- @Porter: **REQ-009 fix is in — but the final gate is the STAKEHOLDER**, since they're the only one who
  reproduced it. Please have them confirm the `Hydration failed` (dark persisted → reload, dev console) is
  **gone on their environment** before marking DELIVERED. The fix (`mounted`-gate, `e55a362`) removes the exact
  divergence they reported; it can only remove, never add, an icon mismatch. Minor inherent tradeoff: a ~1-frame
  Moon→Sun swap post-mount on dark loads (standard mounted-gate; a flash-free CSS-driven variant is possible if
  they mind it).
- **Lesson logged (kept from yesterday):** for a stakeholder-reported bug, a "cannot-reproduce" verdict must
  exercise the reported trigger (dark persisted) in the meaningful console (dev, not prod) — don't accept a null
  result that didn't test the stated condition.
- Board: REQ-001–008 DELIVERED · REQ-009 **SPEC_DONE → Porter/stakeholder confirm**. Jason + Fern idle. Deploy parked.

## [01:11] Porter (PM)
- Nudge "go" -> re-read board + log. REQ-009: Fern applied the **mounted-gate** fix (`e55a362`,
  AppHeader.tsx only) — server + first client paint render Moon unconditionally, real icon resolves
  post-mount → hydration divergence is structurally impossible. Toggle switch+persist unchanged.
- Honest caveat from the team: neither Fern nor Sober could reproduce the "before" locally (env
  differs; same Mantine 9.5/React 19.2/Next 16.2 but no trigger) → no before/after diff; also learned
  the PROD console is not a valid test (React strips hydration warnings in prod) — only the DEV console counts.
- Relayed to the stakeholder to **confirm on THEIR env** (fix hot-reloaded / restart dev, dark persisted,
  hard refresh, check dev console). Not accepting REQ-009 until they confirm `Hydration failed` is gone.
  If it still fires: need their exact browser+version and reload type to reproduce.
- Waiting on: stakeholder (REQ-009 confirm). Deploy still parked.
