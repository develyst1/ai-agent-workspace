# Log — 2026-08-25 — code-report

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Jason (BE)
- **One unit: startup ritual + assessed the BE queue. No BE TASK is startable this
  session; the ball is with Sober.** Read PROTOCOL.md + BE.md, board.md, the
  2026-08-24 log. Created today's log file (2026-08-25) — did not touch yesterday's.
- **TASK-027 is in REVIEW at `23df16f`** (committed by the prior BE session) — only
  Sober advances it to DONE; not mine to move.
- **TASK-028 is TODO but `Depends on: TASK-027`, which is not DONE (it is in REVIEW).**
  Per BE.md #1 ("respecting Depends on: order") + the Statuses rule, 028 is not yet
  startable: 027 could still return as REWORK, which would change the very
  `CuriosityInvestigator` pass-through seam + worker construction site 028 swaps into.
  Same sequencing class as Q-BE-25/Q-BE-26 — I do not build across an unsettled
  boundary. No code written, no SQL, no environment touched.
- **Other BE tasks are not startable either:** TASK-001 IN_PROGRESS (DoD-close is
  Sober's review call); TASK-014 BLOCKED on the human via Porter; TASK-009 is a joint
  BE+FE acceptance run pending Sober's re-scope.
- **@Sober: nothing new from me — over to you.** TASK-027 awaits your review at
  `23df16f` (and Q-BE-27, NON-BLOCKING, wants your scope confirm on the D2
  header/appendix effects). The moment you mark TASK-027 DONE, TASK-028 becomes my
  startable top. @Porter/@Fern: nothing new is yours from me.

## [--:--] Sober (SA)
- **One unit: reviewed TASK-027 (5-stage pipeline + client flip + worker wiring) →
  `DONE` at `23df16f`.** Startup ritual done (PROTOCOL, SA-Lead, board, 2026-08-25 +
  2026-08-24 logs). No code written, no SQL, no environment touched — review only.
- **Verified, not trusted.** Re-ran the gates myself in `code-report-back` (clean tree
  at `23df16f`): `bun run typecheck` exit 0; `bun test` **259 pass / 0 fail** (730
  expect, 19 files). Traced the conformance read-only: §0 client flip (both fields
  required + always emitted), five internal stages, `understandingMessages` = own
  reasoning (not pass-through), `parseTopicPlan` capped/deduped/degrades-to-1 no-throw,
  D2 = pure concat (no stitch). **D-wire mapping (the Q-BE-26 risk area) traced
  end-to-end:** `WIRE_STAGE_BY_INTERNAL` + `reportStage` dup-collapse produce exactly
  the six `JOB_STAGES` in order; worker test L178 (`stages`===`[...JOB_STAGES]`) + L188
  (`[1..6]`) intact and strong against a real temp repo.
- **Answered Q-BE-27 (Jason's NON-BLOCKING scope flag).** He implemented SPEC-007 D2 to
  the letter and surfaced two user-facing effects instead of guessing — correct, and
  NOT a rework trigger (both stem from my SPEC, not his code). But both cross into
  business scope, so I do not resolve them by assumption: (1) the report header is now
  `formatReportParams` verbatim (prompt-style `REPORT PARAMETERS:` labels); (2) SPEC-001's
  mandated Contributors + Commit appendix (`REPORT_STRUCTURE`/`stage3System`, now
  orphaned) are no longer guaranteed. **Routed both to Porter as Q-SA-24 in SPEC-007
  §Questions — NON-BLOCKING.**
- **@Jason: TASK-028 (the real AI_CURIOUSNESS text-action loop) is UNBLOCKED and
  startable now** — 025/026/027 all DONE. The pass-through seam + `CuriosityInput` you
  swap into are live at `23df16f`.
- **@Porter: Q-SA-24 (SPEC-007 §Questions) awaits your business call — NON-BLOCKING.**
  Prompt-style report header vs a reader-facing header; and whether dropping
  Contributors/Commit-appendix is intended for the 5-stage report. Neither gates
  TASK-028 or FE REQ-009. If the stakeholder wants either changed it is a SPEC-007
  amendment + a NEW task, not a rework of TASK-027.
- **My own next SA unit (NOT this session):** design REQ-008 Req-7 fallback-model into
  SPEC-007/config (incl. `deepseek-v4-flash`=30000 cap). @Fern: nothing new.

## [--:--] Jason (BE)
- **One unit: implemented TASK-028 (real AI_CURIOUSNESS loop) → REVIEW at `75acb5f`.**
  Startup ritual done (PROTOCOL, BE.md, board, 2026-08-25 + 2026-08-24 logs). TASK-028
  was unblocked earlier this session by Sober (TASK-027 DONE at `23df16f`), so it was my
  startable top.
- **New `src/ai/curiosity.ts`** = the real `curiosityInvestigator`: env-bounded loop
  (`maxIterations`), D4 text-action protocol (`list_tree`/`read_file`/`search`/`done`)
  parsed from a fenced-or-bare JSON block via exported `parseActions`, each action driven
  through the injected `RepoInspector` (TASK-026 — already path-confined + capped),
  results fed back wrapped as REPO DATA (`repoBlock`) on the next turn. **Safe exit on
  done / unparseable / no-actionable-request / cap; `JSON.parse` in try/catch — a
  malformed reply never throws the job.** Returns accumulated findings unwrapped
  (`understandingMessages` wraps once; `""` when nothing gathered).
- **Worker swap only:** `src/reports/worker.ts` now injects `curiosityInvestigator` in
  place of TASK-027's `passThroughInvestigator` at the same construction site. No
  `WorkerOptions`/`routes.ts`/mapping/DB/wire change — `progress.total` stays 6.
- **Evidence:** `bun run typecheck` exit 0; `bun test` **274 pass / 0 fail** (was 259/0;
  +15 new — 14 in `test/ai-curiosity.test.ts`, 1 in `reports-worker.test.ts` proving the
  now-live AI_CURIOUSNESS call carries its env model/max_tokens, REQ-008 AC 3). Committed
  `75acb5f`, tree clean. No SQL, no environment touched.
- **No questions, no assumptions:** the D4 vocabulary + bounds are SPEC-007 verbatim; the
  stage system prompt is model-facing engineering (like the other stage prompts), not a
  user-facing string, so nothing was invented or guessed.
- **@Sober: TASK-028 ready for review at `75acb5f`.** It is the last SPEC-007 task; only
  you advance it to DONE. Q-SA-24 (D2 header / Contributors-appendix) is TASK-027's
  business-scope item, untouched here. @Porter/@Fern: nothing new is yours from me.

## [09:57] Sober (SA)
- **One unit: reviewed TASK-028 (real AI_CURIOUSNESS text-action loop + worker swap) →
  `DONE` at `75acb5f`.** Startup ritual done (PROTOCOL, SA-Lead, board, 2026-08-25 +
  2026-08-24 logs). Review only — no code written, no SQL, no environment touched.
- **Verified, not trusted.** Re-ran the gates myself in `code-report-back` (clean tree at
  `75acb5f`): `bun run typecheck` exit 0; `bun test` **274 pass / 0 fail** (771 expect, 20
  files) — matches Jason's evidence exactly (was 259/0 at `23df16f`). Traced conformance
  read-only against SPEC-007 §Flow 3 + D4 + D5 and all four DoD rows: env-bounded loop
  (default 5) with safe exit on done/unparseable/no-actionable/cap; `JSON.parse` in
  try/catch so a malformed reply is a safe `stop`, never a job abort; D4 vocabulary exact
  (unknown/incomplete dropped, paths/words trimmed); inspector output fed back via
  `repoBlock` (`REPO_OPEN`/`REPO_CLOSE`) as DATA not instructions (D5); investigator returns
  **raw** findings and `understandingMessages` wraps once (no double-fence, verified in
  `prompts.ts` L332-337); **worker swap is the single allowed edit** — no
  `WorkerOptions`/`routes.ts`/mapping/DB/wire change, `progress.total` stays 6; env
  model+`max_tokens` reach every AI_CURIOUSNESS call body (REQ-008 AC 3, the now-live stage
  asserted in both new tests). No new user-facing string, no scope guessed; `CURIOSITY_SYSTEM`
  is a model-facing stage prompt and the JSON vocabulary is SPEC-007 D4 verbatim.
- **REQ-008 stays `IN_SPEC` — deliberately NOT SPEC_DONE.** TASK-028 was the last *existing*
  SPEC-007 task, but REQ-008 **Req-7** (the `deepseek-v4-pro`/`deepseek-v4-flash` fallback-model
  behaviour, added when the human answered Q-REQ008-1 on 2026-08-24) is still un-specced and
  un-tasked. Marking SPEC_DONE now would falsely report the requirement as fully built.
  Designing Req-7 into SPEC-007/config (incl. `deepseek-v4-flash`=30000 cap) is my **separate
  next SA unit**, not this one (one unit per session).
- **@Jason: no startable BE task right now** — all four SPEC-007 tasks (025/026/027/028) are
  DONE; the next SPEC-007 work is my Req-7 fallback design, after which a new TASK will come to
  you. @Porter: Q-SA-24 (SPEC-007 §Questions, NON-BLOCKING) still awaits your business call on
  the D2 report header + dropped Contributors/Commit-appendix — it does not gate anything.
  @Fern: nothing new is yours.

## [--:--] Sober (SA)
- **One unit: designed REQ-008 Req-7 (model-level fallback) into SPEC-007 + created
  TASK-029.** Startup ritual done (PROTOCOL, SA-Lead, board, 2026-08-25 + 2026-08-24 logs).
  Design only — no code written, no SQL, no environment touched. Read the real backend
  (`code-report-back` `client.ts`/`config.ts`/`pipeline.ts` call sites + `AI-API-CENTER.md`)
  before designing, not in a vacuum.
- **Placement (D6): fallback lives INSIDE `AiClient`, not the pipeline.** Every stage
  (pipeline + curiosity) already calls the one seam `client.chat({stage,model,max_tokens,
  messages})`, and `createHttpAiClient` already owns the retry loop + failure classification —
  so the model-level fallback sits there, above the existing per-model retry, and
  `pipeline.ts`/`curiosity.ts`/`worker.ts` stay **untouched**. Two distinct resilience layers
  kept separate: the AI API CENTER's own *provider* fallback (deepseek→xai→gemini→openai, free,
  unchanged) vs **our** *model* fallback (this).
- **Trigger (D6a):** advance to the next fallback model on any provider/model-side exhaustion —
  the family that today throws `AI_UNAVAILABLE` (timeout / 5xx `http-error` / `service-error`
  `success:false` / network) — but **NOT** on a non-retryable **4xx** (our own malformed
  request; a different model fails identically). **We do not distinguish credit-exhaustion from
  a generic error for routing:** the documented failure is a bare `{success:false,error}`/HTTP
  500 with no structured quota code, and Req-7 fires on *either* — so both simply trigger the
  same fallback; any credit-vs-error labelling is log-only, best-effort.
- **Clamp (D6b):** `deepseek-v4-pro`/`deepseek-v4-flash` cap at 30000/call but CURIOUSNESS/
  WRITING run at 50000 (UNDERSTANDING 40000), so a fallback call sends
  `min(request.max_tokens, 30000)` (REQ-008 AC "each call stays within its per-call cap"). A
  40000/50000-budget stage therefore **degrades to a shorter completion rather than failing** —
  a trade-off in the proposal. **Chain (D6c):** `deepseek-v4-pro`→`deepseek-v4-flash`, de-duped
  against the primary, empty `AI_FALLBACK_MODELS` disables it.
- **Did NOT guess the business call — routed Q-SA-25 to @Porter (NON-BLOCKING).** Req-7
  explicitly requires proposing the mechanism back before ship. Q-SA-25 asks the stakeholder to
  confirm: (1) the trigger rule + whether he wants a *true* credit-vs-error distinction (which
  needs a real out-of-credit payload → an **optional DATA REQUEST**, mechanism ships without
  it); (2) pro→flash order; (3) the 30000-clamp degradation vs fail; (4) the empty-list disable.
  All knobs are env, so his answer changes config not code — same gate/precedent as Q-REQ008-1
  and the D3 defaults, so **TASK-029 is buildable now**.
- **Config surface:** added `AI_FALLBACK_MODELS` to SPEC-007 §Configuration (default
  `deepseek-v4-pro,deepseek-v4-flash`, validated against approved ids, empty=disabled) and
  `deepseek-v4-flash`=30000 to the `APPROVED_MODEL_CAPS` note (Q-REQ008-4 confirmed).
- **REQ-008 stays `IN_SPEC` — deliberately NOT SPEC_DONE.** Req-7 is now specced + tasked but
  **not built**; SPEC_DONE waits until TASK-029 is implemented + SA-reviewed (one unit/session).
- **@Jason: TASK-029 (model-level fallback) is your STARTABLE top** — only `config.ts`/
  `.env.example`/`client.ts`/`routes.ts`+tests change; `pipeline.ts`/`curiosity.ts`/`worker.ts`
  are explicitly out. Full design in SPEC-007 §Fallback + D6; DoD in TASK-029.
- **@Porter: Q-SA-25 (SPEC-007 §Questions) awaits your business call — NON-BLOCKING** (does not
  gate TASK-029's build or FE REQ-009). Q-SA-24 also still open with you. @Fern: nothing new.

## [--:--] Jason (BE)
- **One unit: implemented TASK-029 (model-level fallback, Req-7) → REVIEW at `4bfc21e`.**
  Startup ritual done (PROTOCOL, BE.md, board, 2026-08-25 + 2026-08-24 logs). TASK-029 was my
  startable top (dep TASK-027 DONE; Sober designed it into SPEC-007 §Fallback/D6 earlier today).
- **Fallback lives inside `createHttpAiClient` — pipeline/curiosity/worker untouched (D6).** The
  existing per-model retry loop was extracted into `runModel` (returns success or last `Failure`,
  never throws); `chat()` now walks the ordered, de-duped chain from `buildModelChain(primary,
  fallbacks)`, clamps each call to `min(request.max_tokens, cap)` (D6b), and advances only when
  `shouldFallBack(failure)` = `failure.retryable === true` (D6a — the exact fallback-eligible
  family; a non-retryable 4xx throws as today). Empty chain ⇒ `[primary]` ⇒ byte-for-byte
  today's behaviour (all 8 prior client tests still green).
- **Config:** `deepseek-v4-flash`=30000 in `APPROVED_MODEL_CAPS`; `AI_FALLBACK_MODELS` parsed into
  `Config.fallbackModels` (default `pro,flash`; **explicit empty/blank = disabled**; unknown id =
  fatal `ConfigError`), added to `describeConfig` + `.env.example`. `routes.ts` wires
  `config.fallbackModels` + `APPROVED_MODEL_CAPS` into the client.
- **Evidence:** `bun run typecheck` exit 0; `bun test` **287 pass / 0 fail** (was 274/0; +13 — 7
  client, 6 config). Committed `4bfc21e`, tree clean. No SQL, no DB, no environment (fallback tests
  use a fake model-routed `fetchImpl`).
- **Q-BE-29 raised (NON-BLOCKING).** Two transparency notes, both already resolved by the DoD: (1)
  `src/ai/log.ts` is the one file outside the DoD's named list — edited only to add the D6d
  `fallback:true` log marker the task body explicitly requires (the log-entry type lives there);
  (2) DoD item-1 prose ("empty/absent = []") conflicts with the DoD checklist + SPEC table
  ("default pro,flash; empty = []") on the *absent* case — I built the checklist/SPEC reading
  (absent ⇒ default; explicit-empty ⇒ disabled). Neither blocks review.
- **@Sober: TASK-029 ready for review at `4bfc21e`.** It is the last SPEC-007 task; on DONE,
  REQ-008 can go SPEC_DONE (Req-7 was the last un-built piece). @Porter/@Fern: nothing new is yours.

## [--:--] Sober (SA)
- **One unit: reviewed TASK-029 (model-level fallback, Req-7) → `DONE` at `4bfc21e`; SPEC-007 →
  DONE; REQ-008 → `SPEC_DONE`.** Startup ritual done (PROTOCOL, SA-Lead, board, 2026-08-25 +
  2026-08-24 logs). Review only — no code written, no SQL, no environment touched.
- **Verified, not trusted.** Re-ran the gates myself in `code-report-back` (clean tree at
  `4bfc21e`, HEAD confirmed): `bun run typecheck` exit 0; `bun test` **287 pass / 0 fail** (801
  expect, 20 files) — matches Jason's evidence exactly (was 274/0 at `75acb5f`). `git show --stat
  4bfc21e` = the five named files (`config.ts`/`.env.example`/`client.ts`/`routes.ts`/`log.ts`) +
  2 tests only; `pipeline.ts`/`curiosity.ts`/`worker.ts`/`WorkerOptions`/wire/DB absent (DoD row 3).
- **Conformance traced read-only vs SPEC-007 §Fallback + D6 and all four DoD rows.** D6a: the
  `shouldFallBack(f)=f.retryable===true` predicate cross-checked against the `Failure`
  classification (`client.ts` L199–252 + `parseResult`) — the retryable set is exactly
  timeout/network/5xx/`service-error success:false` (the SPEC fallback family), while a
  non-retryable **4xx** and the malformed-response cases are `retryable:false` → throw as today,
  never a wasted second-model call. D6b: `min(request.max_tokens, cap)` clamp (uncapped model
  keeps its budget). D6c: `buildModelChain` prepends primary + appends fallbacks de-duped in
  order; empty ⇒ `[primary]` ⇒ byte-for-byte today's retry-then-throw. D6d: `fallback:true` on
  non-primary attempts only (primary log lines byte-unchanged) + tried model logged on failures.
  Extracted `runModel` preserves the old per-model retry loop verbatim. Six DoD test cases (a–f)
  + de-dup + marker all green; config validation (unknown id ⇒ fatal `ConfigError`) covered.
- **Answered Q-BE-29 (Jason's two NON-BLOCKING transparency notes) — confirmed, neither a
  rework.** (1) `log.ts` is a legitimate in-scope edit: D6d cannot be satisfied without touching
  `AiCallLogEntry`, which lives there; the change is one optional field + logging the tried model.
  (2) The absent-vs-empty resolution is correct — the conflicting line was in the TASK's own "What
  to do" prose (my wording slip); the SPEC §Configuration table + `.env.example` govern (absent ⇒
  default `pro,flash`; explicit-empty ⇒ `[]`). Both recorded in TASK-029 §Questions + §Review.
- **REQ-008 → `SPEC_DONE`.** TASK-029 was the last un-built SPEC-007 piece (Req-7), so per SA-Lead
  resp. #7 all five TASK-025..029 DONE ⇒ SPEC-007 DONE ⇒ REQ-008 SPEC_DONE. **`SPEC_DONE` = built
  + SA-reviewed, NOT shipped:** Q-SA-25 (fallback policy sign-off) + Q-REQ008-1 (D3 model defaults)
  are **pre-ship** gates Porter clears before `DELIVERED` — the same gate class the SPEC records.
- **@Porter: REQ-008 is ready for your acceptance check** (`SPEC_DONE`). Q-SA-24 (D2 report header
  / dropped Contributors+Commit-appendix) and Q-SA-25 (Req-7 fallback policy) both still await your
  business call — both NON-BLOCKING, neither gates anything now. **@Jason: no startable BE task
  right now** — all SPEC-007 work DONE; next BE work is whatever Porter routes (e.g. TASK-014
  Phase B once the human's run data lands). @Fern: nothing new is yours.

## [10:24] Porter (PM)
- **One unit: PM acceptance check on REQ-008 (`SPEC_DONE`).** Startup ritual done (PROTOCOL,
  PM.md, board, 2026-08-25 + 2026-08-24 logs). No specs/tasks/code touched; edits limited to
  REQ-008 (mine), SPEC-007 §Questions (answering, permitted), board, this log.
- **Checked the built work against REQ-008's Acceptance Criteria.** All *technical* ACs are
  satisfied and SA-verified (Sober re-ran the gates at `4bfc21e`: `typecheck` exit 0, `bun test`
  287 pass / 0 fail): five-stage order; explicit `model`+`max_tokens` per call, env-configurable;
  AI_PROJECT ×1 (tree + `.md` digest + extra context); AI_COMMITS 20/batch sequential;
  AI_CURIOUSNESS env loop (default 5) with file/tree/search + early stop; AI_UNDERSTANDING
  own-reasoning text before writing; AI_WRITING env pass-limit by-topic → one report; default
  `max_tokens`; approved model ids + per-call cap enforcement; fallback mechanism built.
- **One AC gates ship and is UNMET:** *"the fallback design was confirmed back to the stakeholder
  before ship"* (Req-7). So **REQ-008 stays `SPEC_DONE`, NOT `DELIVERED`.** Three pre-ship business
  sign-offs routed to the human 2026-08-25 (in Thai) — I do not guess business calls:
  **Q-REQ008-1 (D3)** the proposed per-stage model→stage default mapping; **Q-SA-24** two
  user-facing report changes from D2 (prompt-style header; Contributors section + Commit appendix
  no longer guaranteed); **Q-SA-25** the Req-7 fallback policy (trigger rule / pro→flash order /
  30000-clamp acceptance / empty-list disable). All three are env/config or wording decisions —
  answers change config or a small string, not the built code.
- **NON-BLOCKING for the team:** every SPEC-007 BE task (025..029) is DONE; nothing waits on an
  engineer. The three gates block only the final `DELIVERED`.
- Marked Q-SA-24 + Q-SA-25 in SPEC-007 §Questions as "routed to human, pending" (I will transcribe
  his decision there when it lands; any change = SPEC-007 amendment + new task for @Sober, not a
  rework of the DONE tasks).
- **@Sober:** no action needed now — on the human's answer I'll relay it; Q-SA-24/Q-SA-25 changes
  (if any) come to you as an amendment + new task. **@Jason/@Fern:** nothing new is yours.
- **Not this unit (flagged for a later PM unit):** raise **REQ-009** (surface AI_CURIOUSNESS +
  AI_UNDERSTANDING as their own progress steps — Q-SA-23 answered "yes, show them"). Separate FE
  requirement; does not affect backend-only REQ-008.

## [10:45] Porter (PM)
- **One unit: processed the stakeholder's three pre-ship sign-offs on REQ-008 → `DELIVERED`.**
  Startup ritual done (PROTOCOL, PM.md, board, 2026-08-25 + 2026-08-24 logs). Edits limited to
  REQ-008 (mine), SPEC-007 §Questions (answering — permitted), board rows, this log. No specs
  design/tasks/code touched. Read `code-report-back/src/config.ts` (read-only) to verify built
  defaults against the confirmed mapping — reading is not communicating.
- **Q-REQ008-1 (D3 model→stage defaults) CONFIRMED** — *"ตามนั้น"*. Verified the built
  `STAGE_MODEL_DEFAULTS` at `4bfc21e` matches byte-for-byte: AI_PROJECT/AI_COMMITS `gpt-4.1-mini`,
  AI_CURIOUSNESS `grok-4-latest`, AI_UNDERSTANDING/AI_WRITING `gpt-4.1`; fallback default
  `deepseek-v4-pro,deepseek-v4-flash`; caps + token defaults all correct. Closes Q-REQ008-1.
- **Q-SA-25 (Req-7 fallback policy) ACCEPTED AS PROPOSED** — *"ก"* (option a / as proposed) then
  *"ไปเลย"*. This clears REQ-008's one ship-gating AC. He did NOT ask for a true credit-vs-error
  distinction → the optional DATA REQUEST is **not** opened. All four sub-items ship as designed;
  build already matches, no code change.
- **Q-SA-24 ACCEPTED with two stakeholder-led follow-ups (neither blocks delivery):** header
  accepted as-is *"รับได้ ขอดูก่อนค่อยกลับมาแก้"* (any later polish = new request, not a rework);
  Contributors + Commit appendix *"ถ้าไม่มีประโยชน์ต่อรายงาน เอาออกไป"* — usefulness judgment
  delegated to the team. Transcribed both into SPEC-007 §Questions for @Sober.
- **REQ-008 → `DELIVERED` 2026-08-25.** All ACs met + SA-verified (Sober's gates at `4bfc21e`:
  typecheck 0, `bun test` 287/0). Updated REQ-008 status header + §Questions DELIVERY note, board
  REQ-008 + SPEC-007 rows. Stakeholder informed in Thai (via the report).
- **@Sober: one NON-BLOCKING design-judgment follow-up is yours (a later unit, not now):** assess
  whether the orphaned Contributors/Commit-appendix (`REPORT_STRUCTURE`/`stage3System` in
  `prompts.ts`) add value to the 5-stage report; if not, a SPEC-007 amendment + a NEW task removes
  the dead code — **not** a rework of the DONE TASK-025..029. Nothing waits on it; REQ-008 is
  delivered regardless.
- **@Jason/@Fern: nothing new is yours.** **Later PM unit (not this one):** raise REQ-009
  (progress-UI for the two new reasoning stages, Q-SA-23 "yes, show them").

## [11:05] Porter (PM)
- **One unit: raised REQ-009 — surface AI_CURIOUSNESS + AI_UNDERSTANDING in the report
  progress bar (Q-SA-23 = "yes, show them"). Status `READY_FOR_SA`.** Startup ritual done
  (PROTOCOL, PM.md, board, 2026-08-25 + 2026-08-24 logs). Wrote only REQ-009 (mine), its
  board row, this log line. No specs/tasks/code, no SQL, no environment, no engineer
  addressed.
- **Captured the requirement, did NOT design it.** REQ-009 states WHAT the user must see:
  `AI_CURIOUSNESS` and `AI_UNDERSTANDING` shown as their own distinct, current-step-
  highlightable steps; Thai-primary labels (REQ-007); no `-1`/raw-key state; report content
  unchanged (display-only). Grounded in the verbatim Q-SA-23 answer
  *"ต้อง นิดหน่อย แสดง พอสมควร ให้รู้ว่ากำลังทำสเต็ปไหนอยู่"* (show them a bit / reasonably,
  enough to know the current step).
- **Recorded the known constraint without designing it:** SPEC-007 deliberately kept the wire
  at six stages (`AI_CURIOUSNESS`→`AI_COMMITS`, `AI_UNDERSTANDING`→`AI_WRITING`) to keep
  REQ-008 backend-only, so surfacing the two stages needs the wire to also carry the two new
  stage strings — a coordinated **BE+FE** change. **How** (SPEC-007 amendment + new BE task, or
  a fresh spec) is @Sober's design call; REQ-008 stays DELIVERED, this is additive, not a rework
  of TASK-025..029.
- **Did NOT invent user-facing strings.** The two step labels are a user-facing string I refuse
  to guess → **Q-REQ009-1 (Porter→human, NON-BLOCKING):** exact Thai-primary/English-secondary
  labels for the two steps; team drafts + stakeholder confirms before ship (same pattern as
  REQ-007). SA can spec structure against i18n keys without the final text, so it blocks nothing.
- **@Sober: please pick up REQ-009** (`READY_FOR_SA`) — spec the BE wire change (emit the two
  new stages) + the FE progress-ledger update. Q-REQ009-1 is NON-BLOCKING for your design.
  @Jason/@Fern: nothing new is yours except as Sober's TASKs.

## [--:--] Sober (SA)
- **One unit: picked up REQ-009 (`READY_FOR_SA` → `IN_SPEC`); wrote SPEC-008 + TASK-030 (BE) +
  TASK-031 (FE).** Startup ritual done (PROTOCOL, SA-Lead, board, 2026-08-25 + 2026-08-24 logs).
  Design only — no code written, no SQL, no environment touched. Read the real backend
  (`code-report-back/src/reports/jobs.ts` + `worker.ts` + `ai/stages.ts` + `ai/pipeline.ts`) and
  frontend (`code-report-front/src/types/api/main/report.ts` + `components/partials/ReportView/
  ReportProgress.tsx` + `constant/text/dictionaries.ts`) before designing, not in a vacuum.
- **Design = un-fold SPEC-007's deliberate D-wire fold.** REQ-008 kept the wire at six stages by
  folding the two reasoning stages onto existing ones (`WIRE_STAGE_BY_INTERNAL`:
  `AI_CURIOUSNESS→AI_COMMITS`, `AI_UNDERSTANDING→AI_WRITING`). But the pipeline **already announces
  both stages unconditionally** (`pipeline.ts` L180/L193), so surfacing them is not new computation —
  it is un-folding: grow `JOB_STAGES` (BE) and `REPORT_STAGES` (FE) six→**eight** in pipeline-announce
  order (`…AI_PROJECT, AI_COMMITS, AI_CURIOUSNESS, AI_UNDERSTANDING, AI_WRITING`) and make the mapping
  an **identity** map. `progress.total` is derived from `JOB_STAGES.length` (`stageProgress`), so it
  becomes 8 with no literal change; `ReportProgress` renders one row per `REPORT_STAGES` entry and
  derives current/total from `job.progress`, so it needs **no structural change** — two list entries +
  two labels + stale-comment fixes only. Pipeline flow, report content, model/token config, and
  schema are all UNCHANGED (display-only; REQ-008 stays DELIVERED, this is additive not a rework).
- **Two independently-startable tasks.** The wire strings (`AI_CURIOUSNESS`/`AI_UNDERSTANDING`) are
  fixed by SPEC-008, and FE keeps its own copy of the stage list, so TASK-030 (BE) and TASK-031 (FE)
  each build + pass their own gates without waiting on the other. Full REQ-009 acceptance (the live
  highlight tracking a real run through all eight steps) is a combined local check once both are DONE
  — same shape as TASK-009, not a per-task DoD gate. Scoped the exact test edits for BE
  (`reports-worker.test.ts` `[1..6]`→`[1..8]`, `total` 6→8; `reports-routes.test.ts` total 6→8; new
  `AI_CURIOUSNESS`=6/8 + `AI_UNDERSTANDING`=7/8 assertions) and the stale "six" doc comments across
  `jobs.ts`/`worker.ts`/`stages.ts`/`pipeline.ts`/`report.ts`/`ReportProgress.tsx`.
- **Did NOT invent the shipped user-facing labels (SA boundary + DISPATCHER rule).** REQ-009 Req 2 +
  Q-REQ009-1 reserve the final wording for the stakeholder. I drafted Thai-primary/English labels in
  the voice of the existing step labels and routed them to Porter as **Q-SA-26 (SPEC-008 §Questions,
  NON-BLOCKING)** to confirm-or-replace: `AI_CURIOUSNESS`="สำรวจโค้ดเพิ่มเติม"/"Exploring the
  codebase"; `AI_UNDERSTANDING`="ทำความเข้าใจข้อมูล"/"Making sense of the findings". FE builds the
  i18n keys with these drafts now; the final text, if changed, is a one-line dictionary edit — it
  gates nothing structural.
- **@Jason: TASK-030 (BE wire six→eight + identity map) is STARTABLE now** — full design in SPEC-008
  §Wire/§Flow, DoD in TASK-030; only `jobs.ts`/`worker.ts` + stale comments + the worker/routes tests
  change, everything else explicitly out. **@Fern: TASK-031 (FE ledger + draft labels) is STARTABLE
  now** — `report.ts`/`dictionaries.ts` + `ReportProgress` doc comments only, render logic unchanged.
- **@Porter: Q-SA-26 (SPEC-008 §Questions) awaits your relay to the stakeholder — NON-BLOCKING**
  (does not gate either task's build; only the shipped wording depends on it). Q-SA-24 (SPEC-007
  Contributors/Commit-appendix — my optional cleanup assessment) also still open on my side.

## [--:--] Jason (BE)
- **One unit: implemented TASK-030 (un-fold D-wire, wire stage list six→eight) → REVIEW at
  `803a44c`.** Startup ritual done (PROTOCOL, BE.md, board, 2026-08-25 + 2026-08-24 logs).
  TASK-030 was my startable top (TODO, no deps; Sober wrote SPEC-008 + marked it STARTABLE
  earlier today). No code written outside the TASK's named scope, no SQL, no environment touched.
- **`src/reports/jobs.ts`** — inserted `AI_CURIOUSNESS` + `AI_UNDERSTANDING` into `JOB_STAGES` in
  pipeline-announce order (now eight); `progress.total` stays derived from `JOB_STAGES.length`
  (no literal 6/8 anywhere). Doc comment six→eight. **`src/reports/worker.ts`** —
  `WIRE_STAGE_BY_INTERNAL` is now an **identity** map; `reportStage` consecutive-dup collapse left
  as-is (still needed for per-batch `AI_COMMITS`); 3 stale "six/folded" doc comments updated.
  **`src/ai/stages.ts` + `src/ai/pipeline.ts`** — comment-only (wire is eight / identity; the
  StagePosition "(six)" note → eight). Pipeline logic untouched.
- **Un-fold proved end-to-end:** the real fake-AiClient worker run now reports **eight distinct**
  wire stages in order (`CLONING, READING_CODEBASE, READING_COMMITS, AI_PROJECT, AI_COMMITS,
  AI_CURIOUSNESS, AI_UNDERSTANDING, AI_WRITING`). New test asserts a run reports both reasoning
  stages with `AI_CURIOUSNESS`=`{6,8}` and `AI_UNDERSTANDING`=`{7,8}` (REQ-009 AC 1/2). Routes +
  worker count tests moved six→eight.
- **Evidence:** `bun run typecheck` exit 0; `bun test` **288 pass / 0 fail** (807 expect, 20 files;
  was 287/0 at `4bfc21e` — delta is exactly the one new un-fold test). Committed `803a44c`
  (parent `4bfc21e`, own commit), `git status --porcelain` clean. Out of scope and untouched
  (confirmed by diff): pipeline flow, report markdown, model/token config, `WorkerOptions`,
  `routes.ts`, DB/schema.
- **No questions, no assumptions.** The two wire stage strings are fixed by SPEC-008 (identical to
  the internal names); I invented no user-facing string — the Thai/English step **labels** are FE's
  (TASK-031) and their final wording is Q-SA-26 (Porter→human), which does not touch BE.
- **@Sober: TASK-030 ready for review at `803a44c`.** Only you advance it to DONE. Full REQ-009
  acceptance (live highlight through all eight steps) is the combined check once TASK-031 (FE) is
  also DONE. @Porter/@Fern: nothing new is yours from me.

## [11:15] Sober (SA)
- **One unit: reviewed TASK-030 (un-fold D-wire, wire stage list six→eight + identity mapping) →
  `DONE` at `803a44c`.** Startup ritual done (PROTOCOL, SA-Lead, board, 2026-08-25 + 2026-08-24
  logs). Review only — no code written, no SQL, no environment touched.
- **Verified, not trusted.** Re-ran the gates myself in `code-report-back` (HEAD = `803a44c`,
  `git status --porcelain` clean): `bun run typecheck` exit 0; `bun test` **288 pass / 0 fail**
  (807 expect, 20 files) — matches Jason's evidence exactly (baseline 287/0 at `4bfc21e`; delta is
  the one new un-fold test). `git show --stat 803a44c` = exactly the six named files
  (`jobs.ts`/`worker.ts`/`stages.ts`/`pipeline.ts` + the two tests), own commit off `4bfc21e`.
- **Conformance traced read-only vs SPEC-008 §Wire/§Flow + all DoD rows.** `JOB_STAGES` = the eight
  stages in pipeline-announce order; `WIRE_STAGE_BY_INTERNAL` is a true identity map; **no literal
  6/8 count anywhere** — `stageProgress` derives `total` from `JOB_STAGES.length` and `JobStage` is
  `(typeof JOB_STAGES)[number]`, so type + total both track the list (grepped `src/` for stray
  `six`/`total: 6`/`toBe(6)`: only unrelated hits — `markdown.ts` dir depth, `jobs.ts` report-params
  keys — plus the SPEC-008 comment itself). Stale "six/seven/folded" doc comments updated in all
  four files; `pipeline.ts`/`stages.ts` are comment-only (logic untouched). New test proves the
  un-fold end-to-end: a real fake-AiClient run reports `AI_CURIOUSNESS`={6,8} and
  `AI_UNDERSTANDING`={7,8} as their own stages (REQ-009 AC 1/2). `reportStage` consecutive-dup
  collapse correctly kept (still needed for per-batch `AI_COMMITS`). Out of scope + untouched
  (confirmed by diff): pipeline flow, report markdown, model/token config, `WorkerOptions`,
  `routes.ts`, DB/schema. No questions either side; no user-facing string invented (the Thai/English
  labels are FE's — TASK-031, gated by Q-SA-26).
- **REQ-009 stays `IN_SPEC` — deliberately NOT SPEC_DONE.** TASK-031 (FE ledger + draft labels) is
  still `TODO`; SPEC-008 needs both tasks DONE, and full REQ-009 acceptance is the combined
  live-highlight run through all eight steps once FE also lands. Marking SPEC_DONE now would falsely
  report the requirement as fully built. Verdict/evidence recorded in TASK-030 §Review; board
  TASK-030 + REQ-009 rows updated.
- **@Fern: TASK-031 (FE eight-stage ledger + two draft labels) is STARTABLE now** — TASK-030 (BE)
  is DONE, the wire now emits all eight stages as themselves. `report.ts` `REPORT_STAGES` six→eight
  (verbatim wire order) + `dictionaries.ts` two new `reports.view.stage.*` keys (draft labels,
  pending Q-SA-26 — final = one-line edit) + `ReportProgress.tsx` doc comments only. Full design in
  SPEC-008, DoD in TASK-031. On your DONE the combined REQ-009 acceptance check can run.
- **@Porter: Q-SA-26 (SPEC-008 §Questions, NON-BLOCKING) still awaits your relay to the stakeholder**
  — the two draft step labels (`AI_CURIOUSNESS`="สำรวจโค้ดเพิ่มเติม"/"Exploring the codebase";
  `AI_UNDERSTANDING`="ทำความเข้าใจข้อมูล"/"Making sense of the findings"). Does not gate TASK-031's
  build; only the shipped wording depends on it. Q-SA-24 (SPEC-007 Contributors/Commit-appendix — my
  optional cleanup assessment) also still open on my side. @Jason: nothing new is yours right now
  (no startable BE task — the next SPEC-008 work is FE).

## [--:--] Fern (FE)
- **One unit: implemented TASK-031 (eight-stage progress ledger + two draft labels) → REVIEW at
  `e75346e`.** Startup ritual done (PROTOCOL, FE.md, board, 2026-08-25 + 2026-08-24 logs). TASK-031
  was my startable top (TODO/STARTABLE, no blocking deps; TASK-030 BE DONE at `803a44c` so the wire
  now emits all eight stages as themselves). No code outside the TASK's named scope, no SQL, no
  environment, no backend touched.
- **Three files in `code-report-front` (branch `develop`, parent `68a1475`):** (1)
  `src/types/api/main/report.ts` — `REPORT_STAGES` six→**eight**, inserting `AI_CURIOUSNESS` +
  `AI_UNDERSTANDING` after `AI_COMMITS` / before `AI_WRITING` in SPEC-008 wire order; stale
  "seven-step"/six-item comment rewritten to the eight-stage un-folded list. (2)
  `src/constant/text/dictionaries.ts` — two new `reports.view.stage.*` keys in **both** `th` and
  `en` (drafts from SPEC-008 §Labels: "สำรวจโค้ดเพิ่มเติม"/"Exploring the codebase";
  "ทำความเข้าใจข้อมูล"/"Making sense of the findings"). Since `MessageKey = keyof typeof th` and
  `en: Record<MessageKey,string>`, both langs must carry both keys → no raw stage key can ever show
  (REQ-009 AC 3). (3) `ReportProgress.tsx` — doc comments only six→eight; render logic is
  list-driven and untouched (no structural change, per SPEC-008/TASK-031).
- **Evidence (DoD gates, in `code-report-front`):** `bun run typecheck` (`tsc --noEmit`) exit 0;
  `bun run build` (`next build`, Next 16.3.2) Compiled successfully, all 5 pages generated, no
  errors. Committed `e75346e`, `git status --porcelain` clean — exactly the three named files, no
  `AGENTS.md`/`next dev` block churn.
- **No questions, no assumptions of my own.** The wire strings + draft labels are all fixed by
  SPEC-008; the labels remain DRAFTS pending Q-SA-26 / Q-REQ009-1 (final = one-line dict edit, not
  rework) exactly as the task instructs. End-to-end live-highlight through all eight steps is the
  combined REQ-009 acceptance run (TASK-030 also DONE) — a follow-up, not this FE task's DoD.
- **@Sober: TASK-031 ready for review at `e75346e`.** Only you advance it to DONE; on DONE the
  combined REQ-009 live-highlight check can run. @Porter/@Jason: nothing new is yours from me.

## [14:49] Sober (SA)
- **One unit: reviewed TASK-031 (FE eight-stage progress ledger + two draft labels) → `DONE` at
  `e75346e`; SPEC-008 → DONE; REQ-009 → `SPEC_DONE`.** Startup ritual done (PROTOCOL, SA-Lead,
  board, 2026-08-25 + 2026-08-24 logs). Review only — no code written, no SQL, no environment touched.
- **Verified, not trusted.** `code-report-front` HEAD = `e75346e`, `git status --porcelain` clean;
  `git show --stat e75346e` = exactly the three named files (`report.ts` +9/−1, `dictionaries.ts` +4,
  `ReportProgress.tsx` +4/−3), no `AGENTS.md`/`next dev` churn. Gates **re-run by me** in
  `code-report-front`: `bun run typecheck` exit 0; `bun run build` (Next 16.3.2) Compiled successfully,
  all 5 pages generated — matches Fern's evidence.
- **Conformance traced read-only vs SPEC-008 + all DoD rows.** (1) `REPORT_STAGES` = the eight stages
  in wire order and **byte-for-byte equal to the backend `JOB_STAGES` at `code-report-back` `803a44c`**
  (TASK-030 DONE) — cross-checked both lists directly; `ReportStage` follows the list, no literal count.
  (2) Both new keys (`reports.view.stage.AI_CURIOUSNESS`/`AI_UNDERSTANDING`) in **both** `th`+`en`;
  because `MessageKey = keyof typeof th` and `en: Record<MessageKey,string>`, a missing key would fail
  typecheck — green typecheck proves no raw stage key can ever show (REQ-009 AC 3, the one hard
  correctness point). (3) `ReportProgress` render is list-driven and unchanged — maps over
  `REPORT_STAGES`, `indexOf(job.stage)` never returns `-1` for a real stage now both are in the list,
  labels via `t()`; only the six→eight doc comments changed. Out of scope untouched (no sub-progress /
  streaming / restyle); optional stale "six" comments in `ReportViewContent.tsx`/`globals.css`
  correctly left (dynamic numerals). No questions either side; no user-facing string invented beyond
  the SPEC-008 drafts.
- **REQ-009 → `SPEC_DONE`.** Both SPEC-008 tasks (030 BE + 031 FE) now DONE ⇒ SPEC-008 DONE ⇒ per
  SA-Lead resp. #7, REQ-009 `SPEC_DONE`. **`SPEC_DONE` = built + SA-reviewed, NOT shipped:** the
  combined end-to-end live-highlight acceptance run (a real job tracked through all eight steps,
  current-step correct at `AI_CURIOUSNESS`/`AI_UNDERSTANDING`, Thai labels, no `-1`/raw-key — same
  shape as TASK-009's acceptance run) is Porter's pre-ship check, and the labels are still DRAFTS
  pending Q-SA-26. Neither is an SA gate; I do not run the app for acceptance (SA boundary + no
  deployed env). Verdict/evidence in TASK-031 §Review; board REQ-009 + SPEC-008 + TASK-031 rows updated.
- **@Porter: REQ-009 is ready for your acceptance check** (`SPEC_DONE`). Two NON-BLOCKING items sit
  with you and neither gates anything: **(a) Q-SA-26** (SPEC-008 §Questions) — relay the two draft step
  labels to the stakeholder for confirm-or-replace (final wording = one-line dict edit); **(b)** the
  combined live-highlight acceptance run through all eight steps (needs both repos running locally — a
  human/Porter pre-ship check, same pattern as TASK-009). Q-SA-24 (SPEC-007 Contributors/Commit-appendix
  — my optional cleanup design assessment) also still open on my side.
- **@Jason: no startable BE task right now** — SPEC-008's BE work (TASK-030) is DONE; next BE work is
  whatever Porter routes (e.g. TASK-014 Phase B once the human's run data lands). **@Fern: nothing new
  is yours** — TASK-031 was the last SPEC-008 task.

## [--:--] Porter (PM)
- **One unit: PM acceptance check on REQ-006 (`SPEC_DONE`, the oldest awaiting acceptance).** Startup
  ritual done (PROTOCOL, PM.md, board, 2026-08-25 + 2026-08-24 logs). No specs/tasks/code touched;
  edits limited to REQ-006 (mine), board, this log. Read the two SPEC-005 commits read-only in
  `code-report-front` (`develop`) to verify the built work — reading is not communicating.
- **Re-verified both diffs.** TASK-022 `859148a` = `NewReportFields.tsx`, 2 lines (`sm:items-end`,
  layout-only, Req 1). TASK-023 `68a1475` = `globals.css` (+52) + `NewReportFields.tsx` (+88/−15): a
  read-face overlay renders the value via the shared `formatIsoDate()` in `DD/MMM/YY`, native
  `type=date` kept for the picker, wire (`dateFrom`/`dateTo`) stays `YYYY-MM-DD`, no new string/dep
  (Req 3). Both commits present on `develop`.
- **Verdict vs the ACs:** **AC 3 (no value / 366-day / gating / string / other-screen change) PASSES**
  on the diffs. **ACs 1/2/4 are VISUAL/render criteria — NOT closeable from code alone** (typecheck/
  tests are inert to a CSS/className diff, Sober's own note). **AC 2 is by its own wording the
  stakeholder's to satisfy** ("opening the same screen again, no longer sees the mismatch he
  photographed"). This project has **no deployed environment** and the new-report screen is admin-gated
  behind the real backend (team may not touch), so Porter cannot produce an honest render either —
  same shape as the TASK-009 / REQ-009 acceptance runs.
- **Did NOT guess the visual outcome.** Routed **Q-REQ006-1** to the human (Thai, in REQ-006
  §Questions) to open the rebuilt new-report screen and confirm (a) both load buttons now line up with
  their dropdowns and (b) the date reads one format in the inputs + summary card. **REQ-006 stays
  `SPEC_DONE`; on his "ok" I set it `DELIVERED`.**
- **BLOCKS `DELIVERED` only — NON-BLOCKING for the team.** SPEC-005 is fully built; no engineer or SA
  waits. Writes: REQ-006 (status header + §"PM acceptance check" + §Questions Q-REQ006-1), board
  REQ-006 row + a Next entry, this log line.
- **@Sober/@Jason/@Fern: nothing new is yours.** **Later PM units (not this one):** relay **Q-SA-26**
  (REQ-009 draft step labels) and run the **REQ-009** combined live-highlight acceptance check — both
  NON-BLOCKING; REQ-009 is the next `SPEC_DONE` awaiting acceptance after REQ-006.

## [15:40] Porter (PM)
- **One unit: full housekeeping — hygiene gate FAILs cleared, zero semantic change.**
  Archived verbatim BEFORE every compaction:
  `archive/board-2026-08-25-pre-compaction.md` (board 144.8KB),
  `archive/dispatcher-state-2026-08-25.md` (runs 2026-08-20-a…2026-08-24-g),
  `archive/REQ-001-2026-08-25-pre-consolidation.md` (full Q&A trail).
- Board rewritten state-only (one-line cells, pointers into TASK/REQ files);
  header now notes `ai-worker/inbox/` (delivery channel, read-then-delete; log =
  history only). Dispatcher-state keeps header + last 5 runs. REQ-001 rewritten
  as the clean current requirement: answered Q&A folded into the numbered text
  (Req 1–18 numbering UNCHANGED); only Q41 remains open (NON-BLOCKING).
- Sizes: board 144.8KB→~15KB; dispatcher-state 78.5KB→12.4KB; REQ-001
  76.4KB→18.5KB. `node check-hygiene.mjs code-report` → RESULT: PASS.
- No specs/tasks/code touched; no status moved; no engineer addressed.

## [16:48] Porter (PM)
- **One unit: routed the stakeholder's blanket reply across the four pending
  items in the digest** (Q-REQ006-1, Q-SA-26, Q-SA-22, TASK-014 Phase B
  hand-over). Verbatim reply: *"ให้คิดแล้วเอาที่ควรนั้นแหละ"* (think it
  through and go with whatever's appropriate). Startup ritual done
  (PROTOCOL, PM.md, board, 2026-08-25 + 2026-08-24 logs). Interpreted per-item
  rather than applied blindly — a blanket "use your judgment" only substitutes
  for the human where the open item is itself a judgment/wording call the team
  can make; it cannot substitute where the ask is his own eyes or his own
  real-world data.
- **Q-SA-22 (SPEC-006 §Questions) — ANSWERED, accepted as proposed.** Wording
  call inside an already-settled policy (Q37). Also closes the Q41 sign-off
  for this reword pass. TASK-024 unblocked for Sober/Fern.
- **Q-SA-26 (SPEC-008 §Questions) — ANSWERED, confirmed as already shipped.**
  Drafted labels stand; no dictionary edit needed.
- **Q-REQ006-1 (REQ-006 §Questions) — STILL OPEN, blanket reply not usable.**
  AC 2 explicitly requires the stakeholder's own eyes on the rebuilt screen;
  "go with what's appropriate" cannot stand in for a visual confirmation.
  REQ-006 stays `SPEC_DONE`. Will re-ask narrowly whether he meant to waive
  the look or still wants to check.
- **TASK-014 evidence hand-over — STILL OPEN, blanket reply not usable.** This
  needs actual Run A/Run B command and log output pasted back — real-world
  data only he can produce, not a decision the team can make for him.
- Writes: SPEC-006 §Questions (Q-SA-22 answer — precedent: Porter answers
  Questions addressed to Porter inside SPEC files, per Q-SA-24/Q-SA-25 in
  SPEC-007), SPEC-008 §Questions (Q-SA-26 answer), REQ-006 §Questions (mine),
  board rows (REQ-006/007/009, SPEC-006/008, Blocked/waiting table), this log.
  No tasks/code touched, no engineer addressed directly.
- **@Sober: TASK-024 (SPEC-006) is unblocked — Q-SA-22 answered.** No other
  status moved by me. **@Jason/@Fern: nothing new is yours except as Sober's
  TASKs.**
- **Still open, mine to chase with the human next:** Q-REQ006-1 (narrow
  re-ask) and the TASK-014 Run A/Run B data hand-over.
