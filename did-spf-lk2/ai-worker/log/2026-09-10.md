# Log — 2026-09-10 — did-spf-lk2

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [09:30] Sober (SA)

**Date-discipline correction first, because it affects how this log reads.**
The real current date is **2026-09-10**. All of this project's entries so far —
mine, Jason's and Porter's — sit in `log/2026-09-07.md`, and my own earlier entries
today (the SPEC-001 hand-off and the TASK-001 review) went in there too. That was
wrong: PROTOCOL's Date discipline says settle TODAY from the session's own
current-date context and never from the newest filename. I inherited 09-07 from the
existing file instead of checking the clock. I am not editing the old file — it is
append-only history and rewriting others' entries is forbidden — so the record
stands as: **REQ-001 and TASK-001 work is logged under 2026-09-07; treat those
timestamps as unreliable, the work itself is unaffected.** @Porter: worth a look at
whether your entries need the same caveat; I have not touched your file.

**Work done this session — REQ-002 picked up, specced, TASK cut.**

- Read REQ-002 in full (`READY_FOR_SA`), plus the board and both logs.
- Wrote `specs/SPEC-002-own-basicauth-section-for-license-transit.md` (ACTIVE) and
  `tasks/TASK-002-own-basicauth-section-for-license-transit.md` (TODO, Jason).
- **REQ-002 item 6 is delivered, not delegated.** The stakeholder asked *me* to
  write the `services` SQL (*"ให้ sober เขียนให้เลย"*), so turning it into a TASK
  would have handed my own assignment to Jason. It is a SPEC deliverable:
  `specs/SPEC-002-linkage-management-services-row.sql`.

**The finding that shaped the SPEC — read the auth filter instead of assuming.**
`BasicAuthFilter` resolves the section with a **plain dictionary indexer**
(`_config.BasicAuth[_sectionName]`, `Filters/BasicAuthFilter.cs:24`) — not
`TryGetValue`. So a **missing** `LicenseTransit` section throws
`KeyNotFoundException` → **HTTP 500 on every call, not 401.** Three consequences,
all in SPEC-002 and on the board as a release note:
1. The controller change and the `appsettings.json` change are **one atomic
   release**. REQ-001 had *zero* code/config coupling — REQ-002 reintroduces it.
   That is inherent to the requirement, so I documented it rather than pretending
   it could be engineered away.
2. `_build-out-lk2/appsettings.json` flips from harmless (REQ-001) to **dangerous**:
   it is a settings file lacking a now-required section. Still out of scope to edit,
   but it must be in the release note.
3. A 500 on this endpoint is diagnostic — "section missing", not "auth failed".

Also verified in the same file, because two acceptance criteria depend on it:
- Lines 55-60 compare username **and** password with `Ordinal` equality. Every
  section shares `Username = "Linkage Service"`, so the old credential is rejected
  **purely because the password differs**. The "old credential → 401" criterion is
  therefore only meaningful if the new password is genuinely distinct — if the two
  ever matched, the old credential would keep working and the check would pass for
  the wrong reason. TASK-002 makes that an explicit, shown check.
- Line 61 sets `HttpContext.Items["BasicAuthSection"] = _sectionName`, so the audit
  field follows the attribute argument. No logging code changes.

**On the SQL — authored by reading, labelled as untested.** I derived the row from
`V1__init_schema.sql`, `V10`, `V15`, `V17` and `ServiceRepository`/`LogService`, and
said so in a Provenance block. Two traps are documented inline because both look
like bugs to a tidy reader:
- **`is_deleted = true` is mandatory.** `findHiddenByProviderCode`
  (`ServiceRepository.java:25`) filters `isDeleted = true`, and both
  `licenseNameMap()` and `buildLicenseIndex()` read through it — a row inserted with
  `is_deleted = false` is **invisible to the dashboard**, the exact opposite of the
  intent.
- **`seq` must be `MAX(seq)+1` across all rows of provider DID including
  soft-deleted ones**, because `uq_services_provider_seq` (V1:39) is table-wide,
  unlike the *partial* `uq_services_service_code_active` (V17).
The file carries a banner saying no agent has run it or connected to that database,
plus a read-only pre-check, an idempotent INSERT, a verification SELECT and a
commented-out undo. I raised two decisions for the stakeholder: their repo uses
**Flyway**, so hand-run SQL is absent from migration history (a `V18__` migration
may be the better home — theirs to choose, Q2-4 keeps that repo out of our scope),
and the row should land **before or with** the LK2 release or new records display
the raw code.

**Carried forward from the TASK-001 incident:** TASK-002's DoD mandates
`dotnet run --no-launch-profile` with `ASPNETCORE_ENVIRONMENT=Production`, and says
plainly that my failure to state it last time is what caused Jason to reach the real
Logstash. Its auth checks are all runnable **without a database** — `BasicAuthFilter`
runs before the controller is constructed — so unlike TASK-001 I have not written a
DoD that requires crossing a hard boundary.

- Board: REQ-002 `READY_FOR_SA` → `IN_SPEC`, SPEC-002 ACTIVE, TASK-002 `TODO`.

- @Jason: **TASK-002 is `TODO`.** Three edits: add the `LicenseTransit` section to
  `appsettings.json`, point the attribute at it, and **replace the comment you added
  in TASK-001** — it now says the opposite of the truth, which is my doing, not
  yours. You generate the password (`openssl rand -hex 20`, stakeholder authorised)
  and report the value in your notes so it can reach them through the chain; it must
  differ from every other section's, and the DoD asks you to show that. One check is
  unusual and deliberate: temporarily break the section name and confirm you get
  **500** rather than 401, then revert — that is the failure mode above, and seeing
  it once proves your section name really resolves.

- @Porter: **the SQL deliverable is ready to relay now** —
  `specs/SPEC-002-linkage-management-services-row.sql`. Please pass it as text and
  keep the "not run, not tested by us" caveat attached; it is a reviewed draft
  derived from reading their code, not verified SQL. Two things in it need the
  stakeholder's decision (Flyway `V18__` vs ad-hoc, and landing the row
  before/with our release). Also please carry the **500-not-401 release note** into
  whatever you tell them about deployment — code and `appsettings.json` must ship
  together, and the stale `_build-out-lk2/appsettings.json` will take the endpoint
  down if code is deployed against it. REQ-001's outstanding response-body evidence
  is unaffected by any of this.

## [09:40] Jason (BE)

**Date-discipline note, since it applies to me too.** My TASK-001 entry sits in
`log/2026-09-07.md` under a 16:59 timestamp. Sober's correction above is right and
I made the same mistake for the same reason — I took the date from the existing
file. This session I settled TODAY from the clock (`2026-09-10`) before writing.
Not editing the old file; the caveat stands as Sober wrote it.

**TASK-002 done and set to REVIEW.** `TODO` → `IN_PROGRESS` → `REVIEW`.

- Read SPEC-002 and the TASK, then **verified your `BasicAuthFilter` findings in the
  source myself** rather than taking them on trust: plain indexer at `:24`, `Ordinal`
  compare of username *and* password at `:55-56`, `Items["BasicAuthSection"]` set
  from the attribute argument at `:61`. All three hold.
- Three edits, exactly as specified: new `LicenseTransit` section **added** to
  `appsettings.json` (`LicenseShipment` untouched — no `-` line for it in the diff),
  attribute → `[BasicAuth("LicenseTransit")]`, and the now-false "ห้ามเปลี่ยน"
  comment replaced with the REQ-002 wording. `git diff --cached --stat` = **exactly
  2 files**, nothing else.
- **Build: 277 warnings / 0 errors**, `--no-incremental`, unchanged from baseline.
  No test project — no test claim.
- **Password generated** (`openssl rand -hex 20`, 40-char hex, username
  `"Linkage Service"`). Distinct from all others. **@Sober: the value is in
  TASK-002's `## Implementation Notes`** — deliberately not written here or on the
  board, since it has to travel to the stakeholder through you and Porter.
- Uniqueness check needed a correction before it meant anything: the raw count
  showed 13 password lines / 12 distinct, which looks like a collision. The
  duplicate is the **two Oracle placeholders**, committed as `""` on purpose.
  Excluding those: 11 of 11 distinct. So "old credential → 401" cannot pass for the
  wrong reason.
- **Auth verified live**, run exactly as you mandated (`--no-launch-profile`,
  `ASPNETCORE_ENVIRONMENT=Production`, Logstash off). Startup logged
  `Hosting environment: Production` with **no** Logstash connection line —
  `appsettings.Local.json` never loaded, no real infrastructure touched this session.
  New credential → **500** (past auth, Oracle ctor as SPEC-002 predicts);
  old `LicenseShipment` → **401**; garbage → **401**; same on `-sandbox`.
- **I changed your missing-section check, because as written it proved nothing.**
  The new credential already 500s from the Oracle constructor, so "rename the key,
  expect 500" returns 500 whether the section resolves or not. I used a **garbage**
  credential instead, which 401s only when the section resolves: key present →
  **401**, key renamed → **500** (`KeyNotFoundException`). That flips on exactly the
  question. Both observed, then **reverted and the revert re-verified by behaviour**
  (401/401/500 restored, `grep -c TEMPBROKEN` → 0).
- **Logstash `basicAuthSection = "LicenseTransit"` NOT observed** — ran with
  Logstash off by rule. It follows from `BasicAuthFilter:61` but that is inference,
  not evidence. Raised as **Q-BE-3**, non-blocking.
- **Git moved under me and I want it on the record.** The human committed TASK-001
  as `cd67b90`, then staged and committed my TASK-002 edits as `5295aea` **while I
  was mid-verification**. I ran no mutating git command. Because the commit landed
  during my broken-key window, I checked the **committed content**: no `TEMPBROKEN`,
  `LicenseShipment` still present, tree identical to HEAD, exactly 2 files. Clean —
  but worth a reviewer knowing.
- Raised **Q-BE-4** (documentation only): TASK-002 §5 and SPEC-002's Out of Scope
  both refer to a **`TASK-003`** that does not exist — your log and SPEC-002's Tasks
  section correctly say the `services` SQL is a SPEC deliverable, not a TASK. No
  work affected.

- @Sober: **TASK-002 is `REVIEW`.** Two things need you: the **password relay**
  (value in the TASK's Implementation Notes — it has to reach the stakeholder via
  Porter) and **Q-BE-3 / Q-BE-4**. Everything in the DoD ran; the only unobserved
  item is the Logstash field, which I will not claim.

## [09:45] Sober (SA)
- Reviewed TASK-002. **Verdict: `DONE`.** Re-verified on the tree myself:
  - Read `git show 5295aea` in full. Exactly the specified change. **`LicenseShipment`
    still carries password `68ec2218…` — the same value I read at the start of
    REQ-001**, so it is genuinely untouched, not just diff-clean.
  - `git show --stat`: **2 files**, +10/−5. `git status`: only the 4 known IDE
    artifacts. Build re-run by me: **277 Warning(s), 0 Error(s)**, unchanged
    baseline, 0 LK2 warnings. Passwords: **11 non-empty, 11 distinct**, new value
    appears once. No `TEMPBROKEN` left anywhere.
- **Q-BE-4: Jason was right and it was my mistake.** When I decided REQ-002 item 6
  was mine to author rather than a TASK, I updated SPEC-002's Tasks section and then
  failed to sweep two other places that still pointed at the `TASK-003` I had just
  deleted — TASK-002 section 5 and SPEC-002's Out of Scope. **Both fixed**, each with
  a note saying what changed. He caught it by reading my artifacts *against each
  other* instead of just following the nearest instruction, which is exactly right.
- **Q-BE-3 answered: it goes UP, not back to him.** `basicAuthSection =
  "LicenseTransit"` is REQ-002 acceptance criterion 4, and BE may not enable
  Logstash (real server). Raised as **SA-3 DATA REQUEST** in REQ-002's Questions.
- **The thing I most want on the record: Jason fixed a broken check of mine.**
  My missing-section DoD step ("rename the key, expect 500") **was not a check at
  all** — the new credential *already* 500s from the Oracle constructor, so that
  step returns 500 whether the section resolves or not. It would have passed on a
  completely broken config. He replaced it with a **garbage** credential, whose
  result flips on precisely the question asked: **401** when the section resolves,
  **500** when it does not. Both states observed, then reverted — and he verified the
  revert *by behaviour* (re-running all three cases), not by eye. I confirmed
  independently. **Rule I am taking forward: a step whose result is identical whether
  or not the property holds is documentation, not verification.** That is twice now
  he has improved a DoD I wrote; both times the flaw was mine.
- Mid-session the human committed both TASK-001 (`cd67b90`) and TASK-002 (`5295aea`).
  Git is theirs; neither of us touched it. The real risk was a commit capturing his
  deliberately-broken intermediate — he checked, and so did I: no `TEMPBROKEN` in
  `HEAD`, `LicenseShipment` present, commit touching exactly the 2 intended files.
  Clean. One accuracy note passed to him: his evidence used `git diff --cached`,
  which reads the index — he staged nothing, the human's commit did, so
  `git show <sha> --stat` is the unambiguous form when someone commits underneath you.
- **Password relayed by pointer, not by copy.** @Porter: the value is in
  `tasks/TASK-002-…md` → `## Implementation Notes` ("Generated password"), verified
  distinct from all 10 other sections. I deliberately did **not** copy it into the
  REQ, the board or this log — it is already committed in `appsettings.json` and one
  location is enough; a credential duplicated across coordination files ends up
  somewhere nobody tracks. Please pass it to the stakeholder **together with the new
  URL** — Linkage Center 2 needs both, and per REQ-001 Q7 they still have neither.
- Board: TASK-002 → `DONE`, SPEC-002 → `DONE`, REQ-002 `IN_SPEC` → `SPEC_DONE`.
- @Jason: **TASK-002 is DONE — nothing to rework.** Both of your questions were
  correct and both landed on me: the phantom TASK-003 was my sloppy edit, and the
  broken check was my bad DoD. Keep doing exactly that. Nothing is queued for BE.
- @Porter: **REQ-002 is `SPEC_DONE` — ready for your acceptance check.** 6 of 7
  criteria evidenced. **Criterion 4 (audit log shows the new `basicAuthSection`) is
  code-verified, NOT observed** — do not report it as confirmed; it needs SA-3.
  Three things to carry to the stakeholder, all in REQ-002's Questions:
  (1) **SA-3 DATA REQUEST** — one Kibana record after the REQ-002 build is deployed,
  plus two cheap checks in the same pass: whether the `services` row has been run
  (if not, the dashboard correctly shows the raw code `LicenseTransit`, which is the
  visible proof of why the row is needed), and whether the deployed
  `appsettings.json` actually has the new section.
  (2) The **500-not-401 release note** — code and `appsettings.json` must ship
  together; the stale `_build-out-lk2/appsettings.json` lacks the new section, so
  deploying code against it takes the endpoint down. A 500 there means a settings
  file was left behind, not that the code is wrong.
  (3) The **password**, by the pointer above.
  REQ-001's outstanding response-body evidence is unaffected and still with the human.

## [10:02] Porter (PM)
- Clock settled with the stakeholder before writing anything: **TODAY = 2026-09-10**.
  My earlier entries genuinely belong to `2026-09-07.md` (that was a different
  working day); nothing was misfiled. This is my first entry for today.
- Ran my own acceptance check on REQ-002 rather than accepting the team's summary:
  `appsettings.json` now has section **`LicenseTransit`** (username `Linkage Service`,
  own password) at line 85; **`LicenseShipment` is still present** at line 81,
  unused, reserved for form 18; the controller is `[BasicAuth("LicenseTransit")]`;
  all three `license-transit` routes intact. **6 of 7 criteria evidenced.**
- Read `specs/SPEC-002-linkage-management-services-row.sql`. Sober scoped it
  correctly — text only, and he states plainly it is a reviewed draft derived from
  reading the migrations, never run against a live schema. Relaying it as such to
  the stakeholder, not as tested SQL.
- Relayed to the stakeholder, in Thai, in one plain list:
  the password (by pointer — `appsettings.json` → `LicenseTransit`, also in
  TASK-002's Implementation Notes), the SQL file, the **500-not-401 release note**,
  and the two pieces of evidence still outstanding.
- **Both REQs are now waiting on the same person for the same kind of thing** —
  observation, not code:
  - REQ-001: the response **body** of the SA-2 calls (Kibana can never supply it —
    `responseBody` is a fixed `{message,statusCode,traceId}` shape).
  - REQ-002: one Kibana record showing `basicAuthSection = "LicenseTransit"` (SA-3),
    which can only exist after the REQ-002 build is deployed.
  Neither blocks any code. Neither REQ goes to `DELIVERED` without them.
- Nothing is queued for SA or BE. Both idle.

## [10:14] Porter (PM)
- Stakeholder delivered the outstanding SA-2 evidence. Archived verbatim at
  `../project-docs/2026-09-10-SA-2-response-body-and-services-row.md`.
- **`services` row confirmed done and correct:** `LicenseTransit` at `seq 11`
  (created 2026-09-10 03:08 UTC), `LicenseShipment` untouched at `seq 10`, both
  `is_active = true` / `is_deleted = true`, same Thai name. No `seq` collision. The
  cross-system dependency in REQ-002 is satisfied, and historical log records keep
  resolving because the old row survived.
- **Live response body checked by me field-by-field against
  `Models/LicenseTransitModel.cs`** — not accepted on sight. The model declares
  exactly `CitizenId, IssueDate, ExpireDate, JuristicId, LicenseNo, ProductName,
  TraderName`; the response carries exactly those seven, camelCase, same order,
  nothing added or missing, wrapped in the standard `ResponseResult<T>`
  (`responseCode "000"`), dates still `yyyyMMdd`.
- **REQ-001: all 8 acceptance criteria now OBSERVED → status `DELIVERED`.**
  Board updated, blocked row closed.
- Correcting a stale expectation rather than letting it stand: SA-2 predicted this
  call would return `data: []` (form-9 rows at `STATUS=30`). It returned **two real
  rows** — licences have since reached `STATUS=40`. The prediction was stale, not
  wrong when written, and live rows are stronger evidence than an empty list.
- **REQ-002 moved to 6/7 evidenced.** The same call authenticated with the **new**
  `LicenseTransit` credential and returned 200, which observes criterion 2 and also
  proves the 500-not-401 release trap did not fire — the deployed settings do carry
  the new section.
- **One thing left in the whole project: REQ-002 criterion 4** (SA-3) — a Kibana
  record showing `basicAuthSection = "LicenseTransit"`. The stakeholder's call at
  ~03:08–onwards must have produced one; it simply has not been read back.
- @Sober: **REQ-001 is DELIVERED.** Your SPEC-002 SQL was run by the stakeholder and
  the row landed exactly as you wrote it — `seq 11`, old row kept, flags copied.
  Nothing queued for you or Jason; REQ-002 needs only the SA-3 record from the human.

## [10:24] Porter (PM)
- Stakeholder supplied the SA-3 record. Archived at
  `../project-docs/2026-09-10-SA-3-kibana-licensetransit.md`.
- Two records, both `GET api/v1/license-transit` → `200`, carrying
  **`basicAuthSection = "LicenseTransit"`**. Criterion 4 **observed**.
  The before/after pair is now on the record in one place: 2026-09-07 records show
  the same `endpointTemplate` with `basicAuthSection = LicenseShipment`; today's
  show `LicenseTransit`. Old records still resolve because the old `services` row
  was kept — the exact outcome Q2-3 was raised to protect.
- Completed the acceptance check for the remaining criteria myself rather than
  closing on the team's word: `appsettings.json` has both sections (81 / 85); the
  stale "never rename this key" comment is gone and now explains the `services`
  dependency and why `LicenseShipment` is reserved for form 18; `git status` over
  the solution shows only pre-existing IDE/publish artifacts, so no other endpoint
  moved. Criterion 3 (old credential → 401) I am recording as **BE-verified,
  SA-reviewed, not re-run by me** — auth resolves before any DB call, so it was
  legitimately testable locally.
- Wrote the full 8-row verdict table into REQ-002 with the evidence source per row,
  including which ones I did not personally observe.
- **REQ-002 → `DELIVERED`. Both REQs are now delivered; the board has nothing
  blocked.**
- Only stakeholder-side follow-up left, and it is not ours to close: **Linkage
  Center 2 has still not been told** the URL and password changed (REQ-001 Q7).
  The old route and old credential are both gone from the deployed build, so that
  consumer is broken until they are told. Flagged again to the stakeholder.
- @Sober: **REQ-001 and REQ-002 are both DELIVERED.** Nothing queued for you or
  Jason. For the record, the two things you flagged as most likely to bite —
  the 500-not-401 config coupling and the `services` row — both came through clean
  on the real environment.

## [10:34] Porter (PM)
- Stakeholder said they build Linkage Center 2 themselves, so no notification is
  needed, and asked what else must change in `did-045-api-linkage-management`.
  Surveyed both that repo and the wider `sa-project` tree, read-only.
- **In `did-045-api-linkage-management`: exactly one gap.** The `LicenseTransit`
  row exists only in the running DB — it was inserted by hand. `V15` seeds up to
  `LicenseShipment` and the newest migration is `V17`, so a rebuilt-from-scratch
  environment will not have the row and its dashboard will show the raw code.
  Fix is a `V18__seed_license_transit_service.sql` written **idempotently**
  (`WHERE NOT EXISTS ... service_code`, V15's own pattern) so it does not fail on
  the dev DB that already has the row. Sober had predicted this exact item in
  SPEC-002's notes.
- Ruled out three other candidates in that repo, each checked rather than assumed:
  it never calls the LK2 API (only hit for any LK2 term is V15 — so no URL and no
  password to update), `service_id_mappings` (V13/V16) contains no License rows,
  and V17 made `service_code` uniqueness partial on `is_deleted = false` while the
  license rows are `is_deleted = true`, so no constraint conflict.
- **Found a real leftover our own change created, outside the acceptance scope:**
  `spf/lk2-client-tool/index.html` — a browser test client living in the `did-spf`
  repo but outside `DidSpf.WebApi.LK2`, which is why neither Sober's LK2-scoped
  sweep nor REQ-001's acceptance search covered it. It still hardcodes
  `base:"license-shipment"`, `auth:"LicenseShipment"` and the **old** password
  (line 181/191), so it is broken against the deployed API right now.
  Its label is also wrong independently of our work — "หนังสืออนุญาตขนย้าย
  (Shipment)"; ขนย้าย is form อ.10 (move), not ผ่านแดน.
- Reported to the stakeholder with the exact lines and offered to open **REQ-003**
  for it. Not creating the REQ unasked — it is their tool and a scope decision.
- No other caller of the LK2 API exists anywhere in `sa-project`; the remaining
  matches are docs, the LK2 project itself, and stale published binaries
  (`_build-out-lk2/`, `spf-build/`).
- REQ-001 and REQ-002 remain DELIVERED; this changes neither.

## [10:38] Porter (PM)
- Stakeholder approved opening the REQ (*"เอา เปิด REQ-003 เลย"*). Created
  `requirements/REQ-003-point-lk2-client-tool-at-license-transit.md`,
  status `READY_FOR_SA`. Board row added.
- Scope kept deliberately narrow: only the form-9 entry in
  `lk2-client-tool/index.html` — route → `license-transit`, auth key →
  `LicenseTransit`, password copied from
  `DidSpf.WebApi.LK2/appsettings.json` (authoritative source named in the REQ so
  nobody invents one), and the wrong Thai label fixed. The other nine licence
  entries are explicitly out of scope.
- Two things written into the REQ so they are not discovered the hard way:
  (1) it is a static HTML/JS file, **not** built by `dotnet build`, so "it builds"
  is not evidence here — verification is loading the page;
  (2) the file already holds all ten Basic-auth passwords in plaintext by design —
  **do not** refactor that under cover of this change.
- Also flagged the repo boundary honestly: the file sits at the `did-spf` solution
  root, outside `DidSpf.WebApi.LK2`. The stakeholder's intent is clear, but I asked
  SA to confirm the boundary once rather than let it pass silently — that boundary
  is exactly why the earlier sweeps missed this file.
- **REQ-001 Q7 closed.** The stakeholder authors Linkage Center 2 themselves, so
  there is no external consumer to notify. Board's blocked table updated to say so,
  and to keep their own `V18` migration item visible.
- @Sober: **REQ-003 is `READY_FOR_SA`.** Small one. The trap here is the opposite of
  REQ-001's: this file is outside the LK2 project, so an LK2-scoped grep will report
  it clean while the tool is still broken — scope the acceptance search to the file
  itself.

## [10:45] Sober (SA)
- Picked up REQ-003. Read the file before designing, not after. Wrote
  `specs/SPEC-003-point-lk2-client-tool-at-license-transit.md` (ACTIVE) and
  `tasks/TASK-003-point-lk2-client-tool-at-license-transit.md` (TODO, Jason).
  Board updated: REQ-003 `READY_FOR_SA` → `IN_SPEC`.
- **Repo boundary — answered, as you asked.** Yes, `lk2-client-tool/index.html` is
  this team's code: it is in the `did-spf` repo, it exists only to exercise the LK2
  API we own, and the stakeholder asked. Being outside the `DidSpf.WebApi.LK2`
  folder made it invisible to an LK2-scoped grep — **that is a flaw in how I scoped
  REQ-001's acceptance search, not evidence the file is someone else's.** Recorded
  in SPEC-003 and REQ-003's Questions so it is not re-argued. The rule I am taking
  from it: an acceptance search must cover **everything the change can break**, not
  the folder the change happens in.
- **The real trap in this file, found by reading it.** The tool holds two parallel
  structures joined by name: `CREDS` is keyed by the catalog's `auth` value, and
  line 251 is `CREDS[g.auth] || ""`. Rename one without the other and the password
  becomes an **empty string** — the page still renders perfectly and the call just
  401s with no visible cause. TASK-003's checks therefore assert programmatically
  that the `auth` key resolves in `CREDS` to a 40-char value, instead of asking
  anyone to eyeball two names.
- Also confirmed `base` is the entire route (`"/api/v1/" + g.base`, line 214) and
  the tool never appends `-sandbox`/`-health-check` — so there is no second place
  to update.
- **Tightened the label, staying inside the requirement.** REQ-003 offered
  "หนังสืออนุญาตผ่านแดน (Transit)" as an example and asked for the name used
  everywhere else; everywhere else form 9 is
  **"หนังสืออนุญาตผ่านแดนซึ่งยุทธภัณฑ์"** (= `SwaggerTags.LICENSE_TRANSIT`), and the
  sibling `license-export` entry in this same catalog already uses its own Swagger
  tag verbatim. So the TASK specifies the canonical name.
- **`dotnet build` is explicitly barred as evidence in this TASK** — the file is not
  compiled. I replaced it with a real gate: extract the `<script>` block and
  `node --check` it. **I ran that command on the current file before putting it in
  the TASK** — rc 0, 10 `GROUPS` entries — so Jason has a known-good baseline and I
  am not handing him an instruction I never tried.
- The password is **not written into the SPEC or the TASK**. It must be copied from
  `appsettings.json`, and the DoD check compares the two files programmatically
  rather than against a number I retyped. One source, and the check varies with the
  answer.
- @Jason: **TASK-003 is `TODO`.** Four values, two adjacent lines, one static file
  at the solution root. Two things to be careful of: `CREDS` key and `auth` must be
  renamed **together** (there is a check for it — an empty password is invisible on
  the page), and **do not fix the `LicenseExport` password** even though you will
  see it is wrong; that is SA-4 with Porter, not yours. `dotnet build` is not
  evidence here — use the `node --check` command in the DoD, which I verified works.
- @Porter: **SA-4 raised in REQ-003's Questions, non-blocking.** Verifying that form
  9 was the only stale entry, I reconciled **all ten** tool passwords against
  `appsettings.json` instead of assuming. Nine match; **`LicenseExport` does not**
  (`da26c2e922…` in the tool vs `adda97fb54…` in config), so that button 401s today
  too. **Pre-existing drift, nothing to do with REQ-001/002** — the tool's form-9
  password still matched the old value exactly, so form 9 worked until we moved the
  route. I kept it out of TASK-003 because Requirement 4 says the other nine stay
  untouched. **Recommendation: fold it in** — one 40-char string, no risk, and
  reporting "the tool works again" while a second button silently 401s means the
  stakeholder finds it the hard way. Their tool, their call; TASK-003 proceeds
  either way and a "yes" is one added line.

## [10:57] Jason (BE)

**TASK-003 done and set to REVIEW.** `TODO` → `IN_PROGRESS` → `REVIEW`.

- Two lines, four values, exactly as specified: `CREDS` key + password (181), and
  the catalog entry's label/auth/base (191). `params:"flat"` untouched. Password
  **copied programmatically from `appsettings.json`**, not retyped, not taken from
  TASK-002 or any log.
- **Your `node --check` gate passes on the edited file** — rc 0, 10 `GROUPS`
  entries, same as the baseline you ran before writing the TASK. I did not cite
  `dotnet build` anywhere.
- `grep -ni "shipment"` on the file → **no output**. The `CREDS`/`auth` join
  resolves (`LicenseTransit`, length 40) and matches `appsettings.json` byte for byte.

- **Three DoD checks could not run: the file is GITIGNORED.** `.gitignore:40`
  excludes `lk2-client-tool/index.html` and git has never tracked it, so
  `git diff -- <file>`, "the other nine lines are byte-identical to `HEAD`" and
  "`git diff --stat` shows one file" all return **empty** — not because nothing
  changed, but because there is no tracked baseline. An empty result there looks
  exactly like a pass, which is why I am not reporting it as one.
- **Substituted a check that actually discriminates**, and it is stronger than the
  diff would have been: I reconciled **all ten** tool passwords against
  `appsettings.json`. Nine MATCH; the one mismatch is **exactly `LicenseExport`**
  (`da26c2e922…`), the pre-existing SA-4 drift you told me to leave alone. Any other
  entry I had disturbed would surface as an unexpected DIFFERS. That proves "only
  form 9 changed" **and** "SA-4 untouched" in one check.
- **Visual check done** — opened the file as a local `file://` static page (no
  server, no API, no real infrastructure). Dropdown lists 10 entries; form 9 reads
  "หนังสืออนุญาตผ่านแดนซึ่งยุทธภัณฑ์"; selecting it populates the password box with
  **40 chars starting `30cb2c`** — the empty-password trap observed *absent* in the
  running page, not just in the file; the tool builds `/api/v1/license-transit`; no
  "shipment" anywhere in the rendered text.
- Alignment kept: all ten `CREDS` values at column 28; my catalog line's columns
  match `license-export` exactly.
- Did **not** attempt an end-to-end 200 (needs Oracle) and did not start the API at
  all this session.

- @Sober: **TASK-003 is `REVIEW`.** Two questions, one of which I think matters
  beyond this TASK:
  **Q-BE-5** — because the file is gitignored, **this fix does not travel with the
  repo**. TASK-001/002 were committed by the human and propagate; this one exists
  only in this working copy, so a fresh clone still has the broken form-9 entry —
  structurally the same gap as the `V18` migration Porter found. Also, REQ-003 and
  SPEC-003 both settle the repo-boundary question on the premise that the file "is
  in the `did-spf` repository"; it is in the working directory but excluded from the
  repository. The ruling may still be right, but the premise is not. I have not
  touched `.gitignore` — repo policy, not mine, and the TASK says leave git alone.
  **Q-BE-6** (minor) — DoD steps 5–7 ask for git output on an untracked file;
  suggest replacing them with the password-reconciliation check if this is ever
  reused as a template.

## [11:22] Sober (SA)
- Reviewed TASK-003. **Verdict: `DONE`.** Re-ran every check on the file myself:
  `node --check` rc 0 / 10 catalog entries; `grep -ni shipment` silent; form-9
  `auth` = `LicenseTransit` **resolves in `CREDS`** with a 40-char password;
  password **byte-identical** to `appsettings.json`; all ten reconciled → 9 MATCH
  with the single `DIFFERS` being `LicenseExport` (the SA-4 drift, correctly left
  alone). Also checked two things nobody asked for: the file is still **LF-only**
  (a careless editor would have flipped the whole file to CRLF) and the 9 original
  routes are present **in original order** with form 9 still in slot 5.
- **The git checks were void and I could still diff anyway.** Because I had read
  this file's `CREDS` block and catalog earlier in the session *before* the change,
  I used those captured values as the baseline: **9 of 9 other credentials
  unchanged**, `LicenseShipment` key gone. So "only form 9 moved" is evidenced.
  (One self-correction: I briefly flagged a 464→465 line change — that was my own
  arithmetic, not the file. 464 newlines before and after; nothing was added.)
- **Q-BE-5 — Jason was right on both counts, and one of them corrects me.**
  Verified independently before answering: `git check-ignore -v` → `.gitignore:40`,
  `git ls-files --error-unmatch` fails, `git log --all` on the path is **empty**.
  The file has **never been tracked**.
  1. I settled the repo boundary partly on *"it is inside the `did-spf`
     repository"*. **That premise was wrong** — working directory yes, repository
     no. The **ruling stands** on the reasons that were actually load-bearing
     (team's tool, exercises our API, stakeholder asked), but I have written the
     correction into SPEC-003 rather than leave a wrong premise looking verified.
  2. The fix **does not propagate** — same shape as the `V18` gap Porter found.
     That is a stakeholder/repo-policy decision, so it went up as **SA-5**.
- **Q-BE-6 — accepted, and I struck the three steps in the DoD** with a note saying
  why and what replaces them, so no future reader follows them into a false pass.
  His all-ten reconciliation is adopted as the real check; it is stronger than my
  diff would have been because it separates "only form 9 changed" from "SA-4 left
  untouched" in one result.
- **Naming my own pattern, because it is now three for three.** TASK-001: a DoD only
  satisfiable by breaking BE's hard boundary. TASK-002: a check that returned the
  same answer either way. TASK-003: three checks against a baseline that does not
  exist. Every time, Jason caught it and substituted something discriminating.
  **Standing rule I now apply before handing over any TASK:** for each DoD step,
  state what the result would be if the property were **false** — if that equals the
  passing result, or the step cannot run in the environment the assignee actually
  has, it is not a check and does not go in. And verify the command on the current
  tree first; that is exactly why the `node --check` gate held here.
- Board: TASK-003 → `DONE`, SPEC-003 → `DONE`, REQ-003 `IN_SPEC` → `SPEC_DONE`.
- @Jason: **TASK-003 is DONE — nothing to rework.** Both questions were right and
  both landed on me: the wrong premise in my boundary ruling and three unrunnable
  checks. You were also right to leave `.gitignore` alone and to report the empty
  git output instead of pasting it as a pass — that is the whole ballgame. Nothing
  queued for BE.
- @Porter: **REQ-003 is `SPEC_DONE`, but read SA-5 before you call it delivered.**
  All acceptance criteria are met **on this working copy only** — the file is
  gitignored and was never tracked, so a fresh clone still has the broken form-9
  entry. **Please do not tell the stakeholder "the tool now works" without the words
  "on this machine".** The decision for them, in REQ-003's Questions:
  (a) un-ignore and commit the file so everyone gets it — **but it carries all ten
  Basic-auth passwords in plaintext, which is very likely why it was ignored**, so I
  am **not** recommending that blind; settling the credentials question first would
  be a new REQ, not a quiet edit under this one; or
  (b) accept local-only and write the limitation down.
  My read is (b) as the safe default. **SA-4 is still open too** (the
  `LicenseExport` password drift, non-blocking) — worth putting both to them in the
  same conversation, since both are "this tool is more broken than the REQ covers".

## [10:52] Porter (PM)
- Ran my own acceptance check on REQ-003 and **verified both of SA's flags myself
  rather than relaying them**:
  - **The tool is fixed on this working copy.** Line 181 now holds the
    `LicenseTransit` password, byte-identical to
    `DidSpf.WebApi.LK2/appsettings.json`; line 191 uses `auth:"LicenseTransit"` and
    the transit route; the label now reads "หนังสืออนุญาตผ่านแดนซึ่งยุทธภัณฑ์" — the
    wrong "ขนย้าย" is gone; no `license-shipment` remains in the file.
  - **SA-5 is correct.** `git check-ignore -v` → `.gitignore:40`;
    `git ls-files --error-unmatch` fails; `git log --all` on the path is empty.
    The file is ignored and has **never been tracked** — the fix exists only here.
  - **SA-4 is correct.** The tool's `LicenseExport` password ends `…07af4a`;
    `appsettings.json` ends `…d70e2c`. Different — that button 401s today, and it
    has nothing to do with REQ-001/002.
- Credit where due: SA caught that his own "it lives in the `did-spf` repository"
  premise was wrong and corrected it in the same answer instead of leaving it
  looking verified. That is the behaviour I want on this project.
- **REQ-003 is NOT being marked DELIVERED yet**, and I will not describe the tool
  as "working again" without the words *on this machine*. The code side is complete
  and evidenced; where it lands is a stakeholder decision.
- Put both questions to the stakeholder in Thai, plainly:
  - **SA-5** — was the intent "fix it for everyone" (then the file must stop being
    gitignored, but it carries all ten Basic-auth passwords in plaintext, which is
    very likely why it was ignored — a security call, not a convenience one), or
    "fix it on my machine" (then this is finished, with the limitation recorded)?
    I relayed SA's read, and share it: local-only is the safe default, and
    un-ignoring it should be its own REQ after the credentials question is settled.
  - **SA-4** — fix the stale `license-export` password in the same pass? One
    40-char string, mechanical, and otherwise they discover a second broken button
    on their own.
- @Sober: both relayed, neither answered yet. Nothing for you or Jason meanwhile.

## [11:35] Porter (PM)
- Both of SA's questions answered by the stakeholder. **REQ-003 → `DELIVERED`,
  explicitly scoped as "this working copy only".**
- **SA-4 — closed: do not touch** (*"เรื่อง2 ปล่อยไป ไม่แตะ"*). The tool's stale
  `LicenseExport` password stays; that button keeps returning 401, accepted
  knowingly. Requirement 4 unchanged. No follow-up TASK.
- **SA-5 — closed: local-only.** Stakeholder pointed at the file and said
  *"อันนี้อ่ะแก้ได้เลย"*. `.gitignore:40` untouched, nothing committed, no agent went
  near git. Written into REQ-003 as a **standing limitation**: the form-9 fix exists
  only here; a fresh clone or another machine still has the broken entry. I flagged
  in the file that I inferred option 2 from a short reply, so it can be corrected.
- Un-ignoring the file remains available but is deliberately **not** done under this
  REQ — it would publish ten plaintext Basic-auth passwords to git, which is a
  security decision needing its own REQ.
- Answered the stakeholder's direct question about `did-045-api-linkage-management`:
  **no code change is needed there at all** — I verified it never calls the LK2 API
  (the only LK2-related hit in the whole repo is the V15 seed), holds no LK2 URL or
  credential, and has no `service_id_mappings` row for the licence services. The one
  outstanding item is theirs: the `V18` idempotent migration, so the `LicenseTransit`
  row survives a rebuild.
- Board cleaned: all four resolved rows struck through with their outcomes,
  everything now shows delivered.
- @Sober: **REQ-003 is DELIVERED (local-only).** SA-4 answered "leave it" and SA-5
  answered "local only" — no further work on either. Correcting your own repo-boundary
  premise in the same answer that used it was the right call, and it is recorded.
  Nothing queued for you or Jason; all three REQs are closed.
