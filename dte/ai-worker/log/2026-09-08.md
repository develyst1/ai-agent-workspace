# Log — 2026-09-08 — dte

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Sober (SA Lead)
- Reviewed **TASK-003** (antd v6 install + theme bridge): **REVIEW → DONE, no rework**.
  Verdict, the 13 checks I re-ran myself, and what I accepted on Fern's evidence:
  `tasks/TASK-003-antd-install-and-theme-bridge.md` §Review.
- Re-verified on the tree, not from the notes: exact pins, the fixed nesting, `metadata.title`
  still untouched (REQ-004's), the 9 mirrored tokens + drift guard, both wrappers, no `ui/` root
  barrel, probe gone, `npm run build` green, `tsc --noEmit` exit 0, emoji baseline 124 with zero
  hits in Fern's files, `.gitignore` line. The AFTER bundle numbers reproduced **exactly**.
- Answered all 4 questions in that TASK's §Questions: **Q1 `cssVar` CLOSED** (keep antd v6's
  default; never pass `cssVar`/`hashed`) · **Q2/Q3 the `+100.8 kB` shared-chunk cost** — his
  escalation was right, cost accepted as shipped, `ssr:false` lazy-loading **rejected**, remedy
  (route-group scoping) still **open and mine**, gated on a compressed delta nobody has yet ·
  **Q4 `Alert` `message`→`title`** — I carry it into every screen TASK that renders `Alert`.
- New **SPEC-001 §Decision 6** records that open item; also on the board's Blocked/waiting as
  mine. It does **not** block TASK-004+.
- Carried openly: `UNVERIFIED — the gzip/Brotli First Load JS delta`, and the BEFORE
  uncompressed table cannot be reproduced from the current tree.
- Not done, still one-unit-per-session queue: REQ-003, REQ-004 (both READY_FOR_SA, mine),
  the Decision 6 measurement TASK, TASK-004 (`/login`).
- Ball: **@Sober** — my own queue. @Fern told TASK-003 is closed; @Porter given the bundle
  finding as an FYI for the owner (no question attached).

## [--:--] Sober (SA Lead)
- Picked up **REQ-004** (product-name rename): `READY_FOR_SA` → **IN_SPEC**. Wrote
  **`specs/SPEC-002-product-name-rename.md`** (ACTIVE) + **TASK-011 (FE)** and **TASK-012 (BE)**, both
  `TODO`, independent. Its own SPEC because SPEC-001 forbids `back/` changes; SPEC-001's
  `TASK-0NN (title rename)` placeholder is struck as superseded. Numbering starts at 011 —
  TASK-004…010 stay reserved for SPEC-001's screens.
- **Enumerated the repo FIRST** (REQ-004 §Req 3) — command + verbatim output in SPEC-002 §The
  enumeration: **14 lines / 9 files** → `front/` 3 · `back/` **7 in 6 files** · docs 1 · owner-only 3.
  REQ-004 Q1/Q2 answered in the REQ: `back/` IS affected — Swagger `/docs` title + the Thai AI prompt.
- Edge cases pre-decided: banner width (both names are 22 chars), the two SQL hits are line-2
  comments so **no DB, no migration**, `creator` keeps its hyphen.
- **Deliberately not edited:** `README.md:3` reads "Disrupt**ing**…", a verb phrase neither form
  fits (Q1); the title rule **deletes two Thai taglines** — flagged, not quietly shipped (Q2);
  `DTE.md` ×3 + everything outside the repo = the owner-only list (Q3). None of the three blocks a TASK.
- Ball: **@Fern** + **@Jason** in parallel; **@Porter** carries Q1–Q3 to the owner.

## [--:--] Porter (PM)
- Inbox: 2 messages from Sober, both processed and DELETED — the antd `+100.8 kB` FYI (no question
  attached; carried to the owner in chat, stays Sober's open item) and SPEC-002's 3 owner questions.
- Brought the owner's answers back, **verbatim, written to `SYSTEM-FACTS.md` BEFORE replying** —
  new §"Owner's answers to SPEC-002's three rename questions", **A24–A26**:
  **Q1 "เปลี่ยนทั้งประโยค"** = option (a), the whole `README.md:3` tagline sentence (English verb
  phrase + Thai descriptor) → the bare `Develyst The Education`; ⚠️ he ruled on the copy, not on the
  line's Markdown decoration — if the exact literal isn't obvious, that comes back to me.
  **Q2 "ตัดได้"** = deleting the two Thai page-title taglines is approved; **Rule T unchanged**,
  TASK-011 needs no edit on this account.
  **Q3 "เดี๋ยวจัดการเอง"** = he does the owner-only occurrences himself (`DTE.md` ×3 + everything
  outside the repo) → **REQ-004 §AC 6 is MET**; nobody edits them and nobody chases him.
- Recorded the same three in `requirements/REQ-004-…md` §Questions, plus one consequence so it is
  never reported as a failure: **AC 2's "zero occurrences" is scored over `front/` + `back/` +
  `README.md`** — `DTE.md` keeping the old name until he edits it is EXPECTED, not a defect.
- Board: REQ-004 row updated (all 3 answered, owner of next step = Sober); the "Owner-only rename
  occurrences + 2 copy questions" Blocked row **removed** with a closing note.
- Ball: **@Sober** — fold A24–A26 into SPEC-002 (Q1 gives `README.md:3` a real edit; Q2/Q3 need no
  change), then TASK-011/TASK-012 run as written. Nothing on REQ-004 waits on the owner.

## [--:--] Sober (SA Lead)
- Inbox: Porter's SPEC-002 Q1-Q3 answers — processed and DELETED. Folded **A24-A26 into
  `specs/SPEC-002-product-name-rename.md`** (now "answers folded in 2026-09-08").
- **A24 -> new Rule S** in §Overview: `README.md:3`'s WHOLE tagline sentence -> the bare
  `Develyst The Education`. Explicitly **not a third naming form** — the string is the A22 form;
  only how much of the line it replaces differs. Rule S touches that one line and nothing else.
- **Exact literal decided and its reasoning written down** (§Edge cases): `> **Develyst The
  Education**` — blockquote and bold PRESERVED, because dropping either would be *changing*
  decoration, which A24 forbids; the Thai descriptor is copy and goes. Flagged for Porter to
  contradict rather than left silent.
- **New `tasks/TASK-013-rename-product-name-readme.md`** — the one-line edit, fully specified,
  status **BLOCKED**: **it has no assignee.** `README.md` is at the repo ROOT, and PROTOCOL gives
  `back/` to Jason and `front/` to Fern only; I will not stretch that rule by assumption, and SA
  writes no repo files. That is **SPEC-002 §Questions Q4 -> Porter** (routing, not an owner question).
- **A25 -> TASK-011 edited**: "ตัดได้" confirmed, Rule T unchanged, the follow-up it warned Fern
  about will not come. **A26 -> §Flow step 3**: AC 2 scored over `front/`+`back/`+`README.md`;
  `DTE.md` keeping the old name is EXPECTED, AC 6 MET, nobody chases him. **TASK-012 unaffected.**
- Not done, one unit per session: REQ-003, the SPEC-001 §Decision 6 measurement TASK, TASK-004.
- Ball: **@Fern** + **@Jason** (TASK-011/012, unblocked, parallel); **@Porter** for Q4 only.

## [--:--] Fern (FE)
- Inbox: 3 from Sober, all processed and DELETED. TASK-003 acknowledged DONE; per his instruction I ran **no** bundle measurement and did not touch antd.
- **TASK-011 executed** — the 3 specified line edits only: `layout.tsx:22` + `page.tsx:18` →
  `DTE — Develyst The Education` (Rule T, both Thai taglines cut per A25); `layout.tsx:26`
  `creator` substring-only, hyphen kept (Rule N). No other `front/` file opened.
- Evidence, all output in the TASK §Implementation Notes: `git status --short front/` = exactly
  those 2 files · `grep -i disrupt front/` = **no output** · em dash confirmed as bytes
  `342 200 224` (U+2014) via `od -c`, not by eye · `npm run build` exit **0** (13 routes) ·
  `npx tsc --noEmit` exit **0** · no-emoji harness **124 — the TASK-002 baseline exactly**.
- **Browser VERIFIED**: `/` tab reads `DTE — Develyst The Education`, codepoint `2014` read from
  the live DOM. Caveat in the TASK: `npm run dev` refused (leftover PID 8400 holds :3000), so I
  served the build with `next start`. `/courses` loads unchanged (own title, no old name).
- **Q1, non-blocking**: other routes carry `… | DTE Platform` titles — no old name, so outside
  SPEC-002, but maybe against REQ-004 Req 2 "no suffix". Changed nothing; a copy/scope call.
- TASK-011 `TODO` → **REVIEW**. Ball: **@Sober** (also in `inbox/SA.md`).

## [03:20] Jason (BE)
- Inbox: 2 from Sober (TASK-012 is TODO; unchanged by A24-A26) — processed and DELETED.
- **TASK-012 executed** — one rule, seven times: the substring `Disrupt Thai Education` ->
  `Develyst The Education` in `back/db/schema.sql:2`, `db/seed.sql:2`, `package.json:4`
  (`description` only), `README.md:1`, `src/index.ts:47` (Swagger `info.title`) + `:111` (banner),
  `src/routes/ai.ts:73` (Thai AI prompt). Nothing else in `back/` opened; `front/`, root
  `README.md` and `DTE.md` untouched.
- Evidence, all output pasted in the TASK §Implementation Notes: `git status --short back/` =
  exactly those 6 files · `git diff --stat` = 7 insertions / 7 deletions · `grep -i disrupt back/`
  = **no output** · the 7 new-name lines · em dash read as bytes `342 200 224` · `JSON.parse` on
  `package.json` = `ok`.
- **Ran the server locally** (`bun run src/index.ts`, env passed inline, no `.env` created) and
  pasted the banner verbatim; `GET /health` = **200**. **`/docs` VERIFIED in a browser** — the
  rendered heading and `document.title` both read `DTE API — Develyst The Education`.
- **No database touched** — no migration, no seed, no SQL; the two SQL hits are line-2 comments.
  Carried honestly: `UNVERIFIED — any route that actually queries PostgreSQL`.
- **Q1, non-blocking**: the console banner box was **already** ragged before this task (the
  `Running on` line overruns; `🎓` is double-width). My line's width is unchanged (49 codepoints
  before and after, measured against `HEAD`). I changed nothing about it — a scope call.
- Footprint: `bun install --frozen-lockfile` was needed (`node_modules` absent); `bun.lock`
  unchanged, `node_modules/` git-ignored.
- TASK-012 `TODO` -> **REVIEW**. Ball: **@Sober** (also in `inbox/SA.md`).

## [--:--] Sober (SA Lead)
- Reviewed **TASK-011 (`front/`) + TASK-012 (`back/`) as one unit** — same SPEC, same two rules,
  two halves of one rename. Both **REVIEW -> DONE, no rework**; verdicts in each TASK §Review.
- Re-verified on the tree, not from their notes: enumeration now **zero "disrupt" hits in `front/`
  + `back/`** · both titles' em dash as bytes `342 200 224` (U+2014), `creator` kept its hyphen ·
  all 7 `back/` substitutions with the Thai AI prompt and `package.json` `name`/`scripts` intact ·
  banner line 49 cp = its borders · `tsc` exit 0 · emoji harness **124 / 45** = TASK-002 baseline.
- Accepted on their pasted output: `npm run build` green; the two browser reads (`/` tab and
  `/docs`). Fern's `next start` caveat is **stronger** evidence, not weaker. Jason's
  `UNVERIFIED — routes that query PostgreSQL` does **not** travel to the owner: nothing queryable changed.
- Both engineers' Q1 answered in their TASKs: Fern's `| DTE Platform` suffix = **out of SPEC-002**
  (holds no old name) -> new owner question **SPEC-002 Q5**; Jason's ragged banner = pre-existing,
  dev-facing, not copy -> **no TASK**.
- **REQ-004 stays IN_SPEC** only because **TASK-013 has no assignee** (Q4 = Porter's routing call); `DTE.md` keeping the old name is expected (A26), not a defect.
- Ball: **@Porter** (Q4 + carry Q5). @Fern / @Jason told their TASKs are closed.

## [--:--] Porter (PM)
- Inbox: 2 from Sober, processed and **DELETED** (SPEC-002 Q4 + the TASK-011/012 DONE note).
- **Answered SPEC-002 §Questions Q4 — a routing call, not an owner question.** A21 ("ทุกที่") put
  `README.md` in the team's surface and A26 kept only `DTE.md` + everything outside the repo for the
  owner. **TASK-013 → Jason (BE)**, one-off for that one line: he already made the identical Rule-N
  edit to `back/README.md:1`, and Fern is on REQ-001's critical path.
- **NOT done:** `PROTOCOL.md` is not amended, Jason's ownership is not extended, no repo-root
  precedent — the boundary that sentence protects (neither engineer enters the other's directory) is
  untouched. Reasoning: `requirements/REQ-004-product-name-rename-everywhere.md` §Questions.
- **Literal CONFIRMED, TASK-013 unchanged** — I do not contradict Sober: `> **Develyst The
  Education**` is right; A24 ruled on copy, decoration stays.
- **Q5 carried to the owner** in Thai (copy, not a defect; "unify" = a NEW REQ), plus the Q4 routing
  so he can overturn it. Nothing new in `SYSTEM-FACTS.md` — my decision is not an owner-stated fact.
- Board: REQ-004 + TASK-013 rows updated, Q4 row closed, Q5 row now waits on him.
- Ball: **@Sober** — assign TASK-013 to Jason and unblock (also `inbox/SA.md`). Owner: Q5 only.

## [--:--] Sober (SA Lead)
- Inbox: Porter's **SPEC-002 Q4** routing answer — processed and **DELETED**. One unit this session:
  **assign TASK-013 and unblock it.** Nothing else started.
- **TASK-013 → Jason (BE), `BLOCKED` → `TODO`** (`tasks/TASK-013-rename-product-name-readme.md`).
  The edit itself is unchanged: Porter **confirmed** the literal `> **Develyst The Education**`, so
  my §Edge-cases reading of A24 stands and I changed no instruction.
- Wrote a new **§"Scope of this assignment"** into the TASK so the limits travel with the work and
  not just in a REQ: one-off for this one line · **`PROTOCOL.md` NOT amended** · Jason's ownership
  still `back/` only · **no repo-root precedent** — the next root file is a fresh routing question ·
  the owner was told in Thai and can overturn it, which would change only the Owner line.
- **SPEC-002 §Questions Q4 marked ANSWERED** with the outcome + what I did; the question as asked is
  kept verbatim underneath. SPEC status line now reads: only TASK-013 left, and it is `TODO`.
- Board: TASK-013 row → `TODO`/Jason; REQ-004 row's owner of next step → **BE (Jason)**. REQ-004
  stays `IN_SPEC` until TASK-013 is reviewed. **Q5 still waits on the owner and blocks nothing.**
- Not done, one unit per session: REQ-003, the SPEC-001 §Decision 6 measurement TASK, TASK-004.
- Ball: **@Jason** — TASK-013 is in `inbox/BE.md`. Nothing waits on Porter or on me.

## [--:--] Jason (BE)
- Inbox: 2 from Sober (TASK-012 closed DONE + my Q1 answered; TASK-013 `TODO` and mine) — processed
  and **DELETED**. One unit: **execute TASK-013**.
- **TASK-013 executed** — Rule S on the single root line `README.md:3`: the whole tagline sentence →
  `> **Develyst The Education**`. Blockquote + bold preserved, Thai descriptor deleted, no new
  tagline invented, bare A22 form (no `DTE —`/em dash — that is Rule T). Scope honoured as written:
  one-off, PROTOCOL not amended, my ownership still `back/`, no precedent; `front/`, `back/` and
  `DTE.md` not opened.
- Evidence, all output in the TASK §Implementation Notes: tree clean before · `git status --short` =
  `M README.md` only · `sed -n '3p'` = the exact literal, `cat -A` shows LF and no trailing space ·
  `grep -n -i disrupt README.md` = **no output** · `git diff --stat` = **1 file, 1+ 1-**. **Nothing
  built or run** — documentation, per the DoD. (`git`'s CRLF warning is this machine's
  `core.autocrlf` about a future checkout; the file on disk is still LF.)
- **Q1, non-blocking, changed nothing**: `README.md:5` and `:157` hold a **U+FFFD** (corrupted
  emoji), present in `HEAD` before this task, no product name in them → AC 2 unaffected. Out of
  scope per REQ-004 C4; flagged, not silently fixed.
- TASK-013 `TODO` → **REVIEW**. Ball: **@Sober** (also in `inbox/SA.md`).

## [--:--] Porter (PM)
- Inbox: empty (my own previous note) — nothing was waiting. One unit: **bring the owner's two
  answers back**, both written to `SYSTEM-FACTS.md` **verbatim, BEFORE replying** — new
  §"Owner's answers to the site-wide page-title question and the TASK-013 routing", **A27–A28**.
- **SPEC-002 Q5 → "ให้เหมือนกันทั้งเว็บ" (A27) = unify.** Exactly as I framed the question to him,
  that makes it a **NEW REQ, never a widening of SPEC-002** — wrote
  `requirements/REQ-005-unify-page-titles-site-wide.md` (`front/` only, copy only, enumerate first).
- **REQ-005 is `DRAFT`, deliberately NOT `READY_FOR_SA`:** "เหมือนกัน" says the titles must *match*,
  not what they match *to* — (a) `DTE — Develyst The Education` on every route, or (b) page name +
  one unified tail. Two different tabs on every page; **I will not pick one**, and REQ-004 Req 2's
  "no suffix" phrase is a rule about the replacement string, not an answer. Asked of him in Thai;
  it is REQ-005 §Open questions Q1 and blocks only REQ-005.
- **TASK-013 routing → "โอเค" (A28)** — not overturned. An accepted one-off: `PROTOCOL.md` still
  unamended, Jason's ownership still `back/`, **no repo-root precedent**. TASK-013 stays at REVIEW.
- **REQ-004 is untouched by all of this** — those titles never held the old name, so AC 2 does not
  score them and neither TASK was wrong. Recorded in REQ-004 §Questions.
- Board: REQ-005 row added (owner) · the old Q5 Blocked row replaced by the REQ-005 shape question ·
  REQ-004 row's Q5 pointer corrected · closing note written.
- Ball: **@Sober** — mark SPEC-002 Q5 ANSWERED (no TASK changes) and review TASK-013.
  **Owner:** REQ-005 Q1 only. Nothing waits on me.

## [--:--] Sober (SA Lead)
- Inbox: 2 (Jason's TASK-013 at REVIEW; Porter's Q5/A27 + A28 note) — processed and **DELETED**.
  One unit: **close SPEC-002** — review TASK-013 and mark Q5 ANSWERED.
- **TASK-013 `REVIEW` → DONE, no rework.** Verified on the tree, not from the notes: line 3 read as
  bytes (`od -c`) = `> **Develyst The Education**` — blockquote + bold preserved, Thai descriptor
  gone, **no em dash / no `DTE —`** (Rule S, not Rule T), no trailing space, LF; exactly one line
  changed, no other file in the repo modified; line 1's `🚀` and the stale NestJS/Prisma content
  untouched (C4). Accepted "nothing built or run" — it is the DoD's own instruction for a Markdown
  line, so **not** an UNVERIFIED and nothing travels to the owner.
- **Enumeration re-run by me** (SPEC-002's own command): **3 hits, all `DTE.md` (`:1/:936/:938`)** —
  zero in `front/`, `back/`, `README.md`. Per §Flow step 3 that is **AC 2 MET**; `DTE.md` is
  owner-only (A26) and **EXPECTED, not a defect**.
- **Jason's Q1 answered — no rework, no TASK, no REQ invented**: the U+FFFD on `README.md:5/:157` is
  pre-existing (confirmed against the committed file), holds no product name, and repairing it is
  out of REQ-004 **C4** + a copy call that is the owner's. Recorded in the TASK so it is findable.
- **SPEC-002 §Questions Q5 marked ANSWERED (A27 "ให้เหมือนกันทั้งเว็บ")** — became **REQ-005**, and
  **nothing in SPEC-002 changed**: no TASK edited or added, and neither TASK-011 nor TASK-012 was
  wrong. **SPEC-002 → DONE** (all 3 TASKs closed, all 5 questions closed); **REQ-004 → `SPEC_DONE`**.
- Not done, one unit per session: REQ-003, the SPEC-001 §Decision 6 measurement TASK, TASK-004.
  REQ-005 is not mine until Porter sets it `READY_FOR_SA`.
- Ball: **@Porter** — REQ-004 acceptance check. @Jason told TASK-013 is closed. Nothing waits on me
  from another role.

## [--:--] Porter (PM)
- Inbox: 1 from Sober (TASK-013 DONE → SPEC-002 CLOSED → REQ-004 `SPEC_DONE`) — processed and
  **DELETED**. One unit: **the REQ-004 acceptance check**. Verdict table, one pointer per criterion:
  `requirements/REQ-004-…md` §"Porter's acceptance check (2026-09-08)". **I ran nothing myself** —
  every verdict is scored against a command + output already recorded by Sober or an engineer.
- **AC 1/2/3/4/6 MET**: enumeration written before any edit · zero old-name hits in
  `front/`+`back/`+`README.md` · the `/` tab read in a browser (codepoint `2014` from the live
  DOM) · Rule-N/Rule-S literals re-verified as bytes · owner-only list accepted (A26).
- **AC 5 is the one gap — engineer half MET, owner's-eyes half OUTSTANDING.** No QA role here, so
  nobody may tick it for him. **REQ-004 therefore stays `SPEC_DONE`, NOT `DELIVERED`**; one word
  from him closes it. ⚠️ Told him plainly: nothing is deployed, the live site still shows the old
  name until he ships it himself.
- Recorded as **non-failures** so no session reopens them: `DTE.md` ×3 · the pre-existing U+FFFD on
  `README.md:5`/`:157` · the `… | DTE Platform` titles (REQ-005) · Jason's PostgreSQL `UNVERIFIED`.
- Board: REQ-004 row + new Blocked row for his eyes. Nothing new in `SYSTEM-FACTS.md` — he stated
  no fact this session. Ball: **Human** (his eyes + REQ-005 Q1). **@Sober**: no rework, REQ-003 his.

## [--:--] Porter (PM) — HOUSEKEEPING hop
- Hygiene gate reported `board.md has 3 table cell(s) > 300 chars`. Fixed exactly that; no
  requirement processed, no REQ written, no work routed this hop.
- Compacted 3 board cells to the one-line form (status + date + owner + pointer): the REQ-004
  Requirements row, and the two Blocked rows (REQ-005 title shape · owner's eyes on REQ-004).
- No fact was dropped. Everything in those cells already lived in
  `requirements/REQ-004-product-name-rename-everywhere.md` §"Porter's acceptance check" and
  `requirements/REQ-005-unify-page-titles-site-wide.md` §Open questions Q1 — except the exact
  two-part ask put to the owner (tab string + pages still render), which I moved into REQ-004's
  ⚠️ paragraph **before** shortening the cell.
- Re-ran `node check-hygiene.mjs dte`: **PASS** (1 warning — today's log has 6 entries > 20 lines;
  logs are append-only, so that binds the next entries, not these).
- Inbox empty; nothing was acted on from it. Ball unchanged: **Human** (his eyes on REQ-004 AC 5,
  and REQ-005 Q1). Sober's queue is untouched.

## [--:--] Porter (PM)
- Inbox: empty (last session's message already processed + deleted). One unit: **process the owner's
  REQ-005 answer** — he wrote `Q2=ข` (his digest's numbering; REQ-005 has only one open question).
- **Written to `SYSTEM-FACTS.md` FIRST, before replying — A29:** the unified title is **page name +
  ONE common tail** (option ข). A single fixed string on every tab (ก) is rejected; **A19 is not
  rewritten** and REQ-004 stays closed.
- REQ-005 §Open questions: **Q1 marked ANSWERED** with his verbatim word and the A29 pointer.
- **REQ-005 stays `DRAFT`, NOT `READY_FOR_SA`** — he answered with one letter and never stated the
  string. Two things are still his and nobody here may infer them (PM.md: a copy rule is a fact only
  when the owner states it): **(1)** the exact separator + word order + whether the tail is the full
  `DTE — Develyst The Education` or a short `DTE`; **(2)** what `/` reads, since it has no page name
  of its own — my option-(ข) text said it stays A19, but he did not restate it. Both are now
  REQ-005 §Open questions **Q2**, offered as ก/ข/ค/ง so one line closes it.
- Acceptance criteria updated to hang off Q2 instead of Q1; added one AC for the two-part form.
- Board: REQ-005 row + its Blocked row re-pointed to Q2. Nothing else touched; no TASK/SPEC read or
  written, nothing routed to Sober.
- Ball: **Human** — REQ-005 Q2, plus his still-outstanding eyes on REQ-004 AC 5 / TASK-001 /
  TASK-002. **@Sober:** nothing new from me; REQ-003 and SPEC-001's next unit remain his.

## [--:--] Sober (SA Lead)
- Inbox: 1 (Porter — REQ-004 accepted, no rework) — processed and **DELETED**. One unit:
  **REQ-003 → SPEC-003 + TASK-014**; REQ-003 `READY_FOR_SA` → **`IN_SPEC`**.
- **Enumerated against the real tree**: 4 route dirs, 2 constants files (one importer each), 4 links
  in 2 files. **REQ-003's Footer path is stale** — it is `src/components/layout/Footer.tsx` since
  TASK-002. Navbar clean; no sitemap/robots.
- **3 traps written into the TASK**: `front/src/services/` is the API client, **not** the
  `/services` route · "Portfolio" in Thai copy ×4 is prose · the 2 stray `about/` files are out of
  REQ-003's reach.
- **My calls per REQ-003 §5/§3**: `redirects()` in `next.config.ts` (no middleware, no stub pages) ·
  **`permanent: false` = 307, NOT 308** — 308 caches in real visitors' browsers forever and cannot
  be undone on a live site · Footer links deleted not repointed · verify-email Help Text removed.
- **Flagged, not guessed:** removing `/contact` leaves **no support-contact link on the site** —
  the owner's copy call; SPEC-003 §Questions Q1 to Porter, **non-blocking**.
- Nothing built, run, queried or deployed — design only. Ball: **@Fern** TASK-014 `TODO`;
  **@Porter** SPEC-003 Q1+Q2, FYI only.
