# Log — 2026-09-13 — dte

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [12:50] Porter (PM)
- One unit: **processed the owner's A–F reply** (verbatim in `SYSTEM-FACTS.md`, written BEFORE replying):
  **A47** `/` PASSES (SPEC-006 Phase 1 gate MET) · **A48** `/courses` FAILS — category filter does not
  work for him + "ไม่สวยพอ" (Phase 3 stays gated) · **A49** `/about` copy delegated to the team, emoji out ·
  **A50** unverified login → "บอกตามจริง". **B (REQ-003 AC 7) and C (REQ-006 AC 6) were NOT answered** —
  nothing read into the silence; both re-asked, A46 stands, both REQs stay `SPEC_DONE`.
- REQ-001 §Q9 answered + CLOSED; §Owner's-eyes gates updated (Phase 1 MET, Phase 2 FAILED with his
  two reasons). New **REQ-008** (About copy + no emoji) and **REQ-009** (tell the unverified user the
  truth + resend), both `READY_FOR_SA`; REQ-001 unwidened. Priorities are my call, one word overturns.
- Board: REQ-001/003/006 rows + 2 new rows; Blocked: Phase-1, `/login`-resend and `/about`-DATA-REQUEST
  rows closed, Phase-2 row replaced by FAILED (waiting Sober). `inbox/PM.md` drained; `@Sober` in `inbox/SA.md`.
- Asked the owner (Thai): B, C again; for D — which category he clicked and what happened, and what
  "ไม่สวยพอ" points at. Not guessed. No specs/, tasks/, code, engineer, git, prod touched.
- Ball: **SA (Sober)** — the `/courses` failure, then REQ-007/008/009 in his order. Owner: B, C, D-specifics.

## [12:58] Sober (SA)
- One unit, Porter's stated priority first: **A48 reason 1 — "the `/courses` category filter does not
  work" — DIAGNOSED from the code on `develop`.** It has **never worked**: `CoursesContent.tsx:44-57`
  renders 14 `<button>` pills with no `onClick`/state/`href`, the active pill is hard-coded to `ทั้งหมด`,
  the grid always maps all of `mockSkills`, and the file is a server component. The home cards'
  `/courses?category=web|ai|design|business` links are read by nothing and name no category.
  Pre-existing and already recorded before he looked (TASK-006 §Findings 2) — **not a TASK-006 defect**.
- Wiring it is **new scope**, not a visual-pass fix: written as REQ-001 §Questions **Q10** asking
  Porter to home it as its own REQ, with the three owner facts it needs (what a pill does; whether the
  home `?category=` links land pre-filtered; the grid heading copy). No TASK, no SPEC, no code written.
- **Reason 2 ("ไม่สวยพอ")**: untouched — awaiting his specifics; nothing designed on an unread mind.
  Phase 2 stays FAILED, Phase 3 stays gated. UNVERIFIED: no browser opened — a code read, but the
  finding needs none (nothing exists that could respond to a click).
- Board: REQ-001 row + Phase-2 Blocked row updated. `inbox/SA.md` drained; `@Porter` in `inbox/PM.md`.
- Ball: **PM (Porter)** — Q10 (a REQ or a "no"). My next unit: REQ-007, then REQ-008, REQ-009.

## [13:20] Sober (SA)
- One unit, next in my queue and gated on nothing: **REQ-007 → `IN_SPEC`**. Wrote
  `specs/SPEC-007-remove-dead-forgot-password-link.md` + `tasks/TASK-022-remove-forgot-password-link.md`
  (FE, `TODO`). Code read on `develop`: the only occurrence in all of `front/` is
  `partials/Login/LoginContent.tsx:154-156` (REQ-007's `login/page.tsx:151` predates TASK-004's move);
  no redirect, no other reference. The change is a pure 3-line deletion; the `จดจำฉัน` checkbox row and
  `Link` import stay.
- **Sequencing call, written with the reason:** standalone TASK now, NOT folded into SPEC-006 Phase 3
  (`/login`'s look), which is gated on the owner's eyes — and Phase 2 just failed. A ruled one-line
  removal is not held hostage to that gate. No look rule engaged; no control's classes touched.
- DoD: build/tsc/grep-zero/emoji 110/DOM has no anchor/before-after geometry in Fern's real-Chrome
  Playwright harness/wrong-password + demo login still behave/deletion-only diff/2 screenshots for AC 3.
  Row-height-unchanged is a stated hypothesis, measured in DoD 6 — not asserted.
- Board: REQ-007 row + new TASK-022 row. `@Fern` in `inbox/FE.md`. Nothing for Porter this hop
  (REQ-007 AC 3 — his eyes — is carried when TASK-022 is `DONE`). Reason 2 of A48 still untouched.
- Ball: **FE (Fern)** — TASK-022. My next unit: REQ-008, then REQ-009.

## [13:02] Sober (SA)  _(real clock; the 13:20 stamp above is the earlier session's own)_
- One unit, next in my queue: **REQ-008 → `IN_SPEC`**. Wrote `specs/SPEC-008-about-page-copy-and-emoji.md`
  + `tasks/TASK-023-about-copy-and-emoji-to-icons.md` (FE, `TODO`, after TASK-022). Code read on
  `develop`: `/about` is still the un-migrated `app/about/page.tsx` (no `partials/About/`), 18 emoji
  (checker sees 17 — `⏰` U+23F0 is the known gap; DoD 4 greps it separately), `DTE Platform`/`Disrupt` absent.
- **The copy is drafted verbatim by me in SPEC-008 §Copy** (25 rows, BEFORE→AFTER, each with its `DTE.md`
  or on-page source) — A49 authorises the team; Fern pastes, invents nothing. Three design calls written
  with reasons: (1) copy first, **no `partials/` migration this unit** — his correction round is cheapest
  as a string diff; (2) emoji → lucide (Decision 5), the 5 Heroicons stay until the screen converts, the
  8 table-cell emoji deleted not replaced; (3) the unsourced numbers block (50+/1,000+/10,000+) and
  `รอครู 1-3 วัน` **dropped** — §Q1 for Porter to say in one line to the owner; verbatim return if wanted.
- DoD: build/tsc/emoji 0 on the file + 110→93 on `src`/U+23xx grep/forbidden+required strings/DOM
  text + `svg.lucide` count/before-after h4 geometry (shrink is a hypothesis, measured)/2 screenshots/1-file diff.
- Board: REQ-008 row + new TASK-023 row. `@Fern` in `inbox/FE.md`; `@Porter` in `inbox/PM.md` (§Q1/§Q2,
  non-blocking). No code, no browser, no DB, no git, no prod touched.
- Ball: **FE (Fern)** — TASK-022 then TASK-023; Porter reads SPEC-008 §Copy. My next unit: REQ-009.

## [14:10] Fern (FE)
- One unit: **TASK-022 built → `REVIEW`.** `partials/Login/LoginContent.tsx` — the three
  `<Link href="/forgot-password">` lines deleted, nothing else; wrapper `div`, `BaseCheckbox`, `Link`
  import untouched. CRLF preserved (228→225 CRLF, 0 bare LF; no `sed -i`). `git diff --stat`: 1 file, 3 deletions.
- All 9 DoD run, real output in the TASK: build exit 0 / 9 routes · tsc clean · repo grep 0 · emoji 110 ·
  DOM anchor count 0 + no `ลืมรหัสผ่าน` text (both themes) · **geometry BEFORE/AFTER identical** — row
  22 px, checkbox and submit rects byte-equal, SPEC-007's hypothesis confirmed by measurement.
- DoD 7 (wrong password banner; demo login → `/` + `dte_user` set) passes **on a mocked `/auth/login`**
  (`page.route`) — no backend runs here; stated as UNVERIFIED against the real API in the notes.
- New instrument `tests/harness/measure-login-link.mjs` (coordination repo, nothing in `front/`);
  4 screenshots `login-{BEFORE,AFTER}-{light,dark}.png` — the AFTER pair is for the owner's AC 3.
- Board TASK-022 row → `REVIEW`; `inbox/FE.md` drained (TASK-023 message kept, unprocessed); `@Sober`
  in `inbox/SA.md`. No SQL, no DB, no git writes, no prod. TASK-023 not started (one unit per session).
- Ball: **SA (Sober)** — review TASK-022. My next unit: TASK-023.

## [15:05] Fern (FE)
- One unit: **TASK-023 built → `REVIEW`.** `app/about/page.tsx` only — SPEC-008 §Copy rows #1/#4/#9/#12/#16/#18–21/#23
  pasted byte-for-byte, 10 emoji spans → lucide (`Eye Target CheckCircle Bot BookOpen Lightbulb Rocket`, exact
  classes, `aria-hidden`), 8 table-cell emoji deleted, the `ตัวเลขที่น่าสนใจ` section removed. Heroicons, `ui/`,
  metadata, stray `about/` files untouched. CRLF kept (211→189, 0 bare LF; Node exact-match edits, no `sed -i`).
- All 9 DoD run, real output in the TASK: build 9 routes · tsc clean · emoji 0 on file / **110→93** on src ·
  U+23xx grep no match (control on HEAD saw `⏰`) · forbidden 0 / required each present · DOM: sentence present,
  numbers absent, **10 `svg.lucide`** · **h4 40→32 px** both cards, both themes (SPEC's shrink hypothesis
  confirmed), bullet 24 px unchanged · 2 AFTER screenshots `tests/harness/about-after-{light,dark}.png`.
- Two things reported, not hidden: **(Q2)** DoD 6's `Extended_Pictographic` test is `true` — solely the Footer's
  `©` U+00A9, not an emoji, outside scope, `main` tests false — Sober rules; **(Q1)** at 1280 px the new hero
  sentence wraps inside the word `เรียนรู้` (Chrome Thai line-break, no class changed) — observation for Porter.
- `git diff --stat` shows TASK-022's LoginContent.tsx too (in REVIEW, uncommitted — not reverted); scoped diff 1 file.
- New instrument `tests/harness/measure-about.mjs` (coordination repo). `inbox/FE.md` drained; `@Sober` in `inbox/SA.md`.
  No SQL, no DB, no git writes, no prod. UNVERIFIED: the owner's eyes (REQ-008 AC 5).
- Ball: **SA (Sober)** — review TASK-022 + TASK-023, rule Q2. My queue is empty until a new TASK.

## [15:40] Sober (SA)
- One unit: **reviewed TASK-022 and TASK-023 → both `DONE`** (one hop: both small, same file set, same
  instrument). Evidence re-run by me where a code read allows — TASK-022 DoD 3 grep = 0, the 3 lines gone,
  `Link` import still used; TASK-023 DoD 3/4/5 re-run (emoji 0 on the file, src 110→93, no U+23xx, every
  §Copy string present once, founder line byte-identical, 10 lucide + 10 `aria-hidden`). Both AFTER
  screenshot pairs looked at. No browser opened by me; no code, DB, git, prod touched.
- **Rulings:** TASK-023 §Q2 — DoD 6 reads "on `main`", PASSES; the over-broad `document.body` wording was
  **my** defect, Fern reported the literal `true` correctly. §Q1 — the `เรียน|รู้` wrap is real, Chrome's
  Thai word boundary, page-wide behaviour; **not fixed** — the owner's eye decides; carried as SPEC-008 §Q3
  (fix if flagged: U+2060 in one string, own TASK). TASK-022 DoD 7 stays UNVERIFIED vs a real backend.
- **REQ-007 → `SPEC_DONE`, REQ-008 → `SPEC_DONE`**; SPEC-007/008 `DONE`. Board: 2 REQ + 2 TASK rows.
  `inbox/SA.md` drained; `@Porter` in `inbox/PM.md` (AC 3, AC 5, SPEC-008 §Q1–Q3).
- Ball: **PM (Porter)** — acceptance checks to the owner. My next unit: REQ-009 (`back/` read first).

## [16:20] Porter (PM)
- One unit: **inbox drained (3 Sober messages) + acceptance checks + Q10 homed.** REQ-001 §Q10 ANSWERED:
  the never-worked `/courses` category filter is now **REQ-010** (`DRAFT`, BLOCKED on the owner — his three
  facts are its §Questions Q1–Q3, asked in Thai; my reading of "what a pill does" is written as a reading, not
  a fact). Phase 2 stays FAILED on both reasons; reason 2 still awaits his specifics.
- **REQ-007 acceptance check**: AC 1–2 MET on evidence (TASK-022), login-vs-real-API UNVERIFIED (mocked, carried);
  AC 3 (his eyes) OPEN → REQ-007 §Q1. Stays `SPEC_DONE`.
- **REQ-008 acceptance check**: AC 1–4 MET on evidence (TASK-023) + my read of SPEC-008 §Copy for the name/wording
  rules — nothing flagged; AC 5 (his eyes + corrections) OPEN → REQ-008 §Q1, disclosing SPEC-008 §Q1 (numbers block
  dropped), §Q2 (the one new sentence + 3 labels turned Thai), §Q3 (`เรียน|รู้` wrap). Stays `SPEC_DONE`.
- Board: REQ-001/007/008 rows, new REQ-010 row, Phase-2 Blocked row + 2 new owner's-eyes rows. `inbox/PM.md`
  drained; `@Sober` in `inbox/SA.md` (nothing for him until the owner answers REQ-010).
- Digest to the owner (Thai): REQ-007 §Q1, REQ-008 §Q1, REQ-010 Q1–Q3, reason-2 specifics, plus the still-unanswered
  REQ-003 Q4 and REQ-006 Q1. No specs/, tasks/, code, engineer, git, prod touched.
- Ball: **HUMAN (owner)** — 7 labelled answers. Sober: REQ-009 (unchanged).

## [13:40] Sober (SA)
- One unit, next in my queue: **REQ-009 → `IN_SPEC`**. Wrote `specs/SPEC-009-login-unverified-truth-and-resend.md`
  + `tasks/TASK-024-login-surface-unverified-and-resend.md` (FE, `TODO`). **Established: FE-only.** Code read
  on the repo: `auth.ts:122-131` already answers an unverified login with **403 + `requiresEmailVerification:true`**
  (401 for wrong creds); `api.ts:108` keeps only the message and `AuthContext.login():83-85` swallows it to
  `false`. Fix = `ApiRequestError` carrying status+body, `login()` returns `{ok}|{kind:'unverified'|'other'}`,
  `LoginForm` switches on the kind (string-sniff at `:51` deleted); the resend button/handler already exist.
- **Three out-of-scope BE findings, routed not fixed (SPEC-009 §Findings, §Q1–Q3 for Porter → owner):**
  F1 no mailer exists — register/resend only `console.log` the token, so the resend's "ส่งลิงก์…แล้ว" is untrue;
  F2 `db.ts:21` uses `postgres.toCamelCase`, not an export of postgres 3.4.9 → no transform → (b) `safeUser`
  never strips `password_hash` (leaks on login/profile), (c) `/verify-email` reads `record.userId` = undefined →
  can never verify anyone. All code reads, UNVERIFIED by run; nothing designed on them.
- Strings S1–S6 listed for the owner (SPEC-009 §Strings) — all exist today, none invented, reused per REQ req. 4.
  DoD 5 requires a real local `back/` + local Postgres (register → unverified user); 5-alt mocked = AC 1 UNVERIFIED.
- Board: REQ-009 row + new TASK-024 row. `inbox/SA.md` drained; `@Fern` in `inbox/FE.md`, `@Porter` in `inbox/PM.md`.
  No code, browser, DB, git, prod touched. Seed password is `Demo1234!`, not the on-screen `password123` (§Q6).
- Ball: **FE (Fern)** — TASK-024. Porter: SPEC-009 Q1–Q4 to the owner. My next unit: review TASK-024.

## [14:05] Fern (FE)
- One unit: **TASK-024 built → `REVIEW`** — **DoD 5 on the real stack, nothing mocked.** 3 files in `front/src`
  (`api.ts` `ApiRequestError`; `AuthContext` `LoginResult` + 403/`requiresEmailVerification` classification;
  `LoginContent` switches on `result.kind`, string-sniff deleted). Exact-match edits, CRLF kept, no `back/` change.
- Local stack, footprint declared: the owner's 5432 cluster needs a password I do not have and did not guess, so I
  `initdb`'d my own throwaway cluster (scratchpad, trust, :5433), migrate + seed, `back/` on :4013 via env only
  (no `back/.env` created), `next start -p 3001`. All stopped and the cluster deleted afterwards.
- All 9 DoD with real output in the TASK: build 9 routes · tsc clean · sniff grep 0 / `ApiRequestError` in 2 files ·
  emoji 93 · register 201 + `[DEV]` token line (F1 confirmed by run) · login **403** body exact · banner
  `กรุณายืนยันอีเมลก่อนเข้าสู่ระบบ` + 1 button, both themes (2 screenshots) · resend POST observed, 200,
  **antd toast renders** (SPEC-009 hypothesis confirmed) · 401 paths: generic banner, 0 buttons; `Demo1234!` → `/`
  + `dte_user` · `/register` resend alert + `/verify-email?token=bogus` show the backend's strings via the new seam.
- Reported, not hidden: DoD 8a's grep also lists the `authApi.login` *definition* (`api.ts:129`) — one caller still.
- New instrument `tests/harness/measure-login-unverified.mjs` + 3 PNGs. Board row → `REVIEW`; `inbox/FE.md`
  drained; `@Sober` in `inbox/SA.md`. No SQL against any real DB, no git writes, no prod. UNVERIFIED: owner's eyes (AC 5).
- Ball: **SA (Sober)** — review TASK-024. My queue is empty until a new TASK.

## [13:57] Sober (SA)  _(real clock; earlier stamps in this file are each session's own)_
- One unit: **reviewed TASK-024 → `DONE`; REQ-009 → `SPEC_DONE`; SPEC-009 `DONE`.** Re-run by me (read-only):
  `git diff` of the 3 files is byte-for-byte SPEC-009 §Frontend changes, nothing extra; DoD 3 greps, DoD 4 (93),
  `tsc` exit 0, CRLF/bare-LF 0 in all three, `back/.env` absent, `handleApiError` rethrows `Error` so the seam
  holds. All three screenshots looked at. No browser, DB, git write, prod touched by me.
- Evidence standard met: DoD 5 on a **real** local stack (Fern's own throwaway cluster, env-only, torn down and
  declared) — AC 1–4 evidenced, nothing mocked. **Two hypotheses settled by measurement:** the antd toast DOES
  render on `/login` (SPEC-009 §Flow, confirmed); **F1 (no email is ever sent) is now run-confirmed**, not a code read.
- Rulings: DoD 8a's extra grep line is the `authApi.login` method head — **my grep, not Fern's defect**; 8c settled
  by source grep + curl; `#terms` harness workaround pre-existing, noted only.
- Board: REQ-009 + TASK-024 rows, new Blocked row "REQ-009 AC 5". `inbox/SA.md` drained; `@Porter` in `inbox/PM.md`.
- Ball: **PM (Porter)** — REQ-009 acceptance check + AC 5 to the owner, with SPEC-009 §Q1 (the resend's "sent"
  is untrue on the screen he will see) and Q2–Q4. My queue: empty until REQ-010 `READY_FOR_SA` / A48 reason 2.

## [14:01] Porter (PM)  _(real clock; the 16:20 stamp above is that session's own)_
- One unit: **inbox drained (Sober's two REQ-009 messages) + REQ-009 acceptance check.** REQ-009 stays
  **`SPEC_DONE`**: AC 1–4 MET on evidence (TASK-024 on a real local throwaway stack, re-run by Sober); **AC 5
  (his eyes) OPEN → REQ-009 §Q1 G1**. Not `DELIVERED` also because AC 2's "sent" toast is, by the team's own
  run, untrue — no email is ever sent (SPEC-009 F1). Detail: REQ-009 §"Porter's acceptance check (2026-09-13)".
- Sober's three backend findings stated plainly to the owner in the same §Q1: **G2** no mailer, resend says "sent"
  (a/b/c: wire real sending as its own REQ with his SMTP creds / accept / reword) · **G3** `/verify-email` can never
  verify anyone (code read) · **G4** `password_hash` returned to the browser (code read, security — my recommendation:
  fix first) · **G5** strings S1–S5 keep/replace. SPEC-009 §Q6 demo-line disclosed, no action asked.
- Sober's "your call" on §Q2/§Q3: **no BE REQ until he says yes** (answered in REQ-009 §Questions; REQ-011/012 on
  his yes). F1–F3 recorded as `SYSTEM-FACTS.md` **A51/A52 — NOT owner-stated**, deliberate-or-not asked.
- Board: REQ-009 row, REQ-009-AC-5 Blocked row replaced + 1 new Blocked row (back/ auth findings). `inbox/PM.md`
  drained; `@Sober` in `inbox/SA.md` (that file is 6.8 KB of Sober's own drain notes — his housekeeping).
- Digest to the owner (Thai): REQ-009 G1–G5, plus the still-open REQ-007 §Q1, REQ-008 §Q1, REQ-010 Q1–Q3, A48
  reason-2 specifics, REQ-003 Q4, REQ-006 Q1. No specs/, tasks/, code, engineer, git, prod touched.
- Ball: **HUMAN (owner)** — labelled answers. Sober: idle until he answers.
