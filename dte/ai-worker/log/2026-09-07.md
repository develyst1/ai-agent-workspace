# Log — 2026-09-07 — dte

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [--:--] Porter (PM)
- Owner answered the 6 standing questions. Recorded verbatim (Thai) BEFORE this entry in
  `SYSTEM-FACTS.md` §"Owner's answers to the 6 standing questions" (A1-A6) — the detail, the
  evidence and every warning live there; this entry deliberately does not retell them.
- CLOSED by his words: Q1 `main` (ruled out of the team's scope) · Q4 (production runs `develop`'s
  tip) · Q6 (the product is **Develyst The Education**; board "Project info" corrected).
- STILL OPEN, re-asked, not answered by me: is `4013` prod/local/both (A2, new Blocked row) · do the
  inherited `/portfolio`,`/services`,`/contact`,`/blog` routes stay or go (A3 — his premise did not
  match the disk; the read-only evidence is in A3) · the 2% vs ค่าเปิดคอร์ส either/or (A5, still
  CONTESTED; the exact Thai wording now sits in `REQ-002` §Open questions Q1 so his next answer binds).
- Nothing run, nothing verified beyond one read-only directory listing. No code/specs/tasks touched.
- Ball: **the owner** (3 questions) · @Sober informed of the 4013 fact and the name.
- _(Trimmed 2026-09-07, Porter housekeeping hop, to the <=15-line rule. No fact lost — see A1-A6.)_

## [--:--] Porter (PM)
- Owner answered the three re-asked questions (port / inherited routes / revenue ก-ข). Written
  into `SYSTEM-FACTS.md` §"…three re-asked questions" (A7–A9) verbatim, BEFORE this entry.
- A7 port `4013` = **prod AND local** ("ทั้งคู่") — closes the A2 ⚠️. All 4 repo values are wrong in
  both envs. **Still has no REQ**: I did not task it; that is my next PM unit, board row updated.
- A8 the inherited routes: **"ลบ"**. A3's ⚠️ CONTESTED is resolved (disk evidence won).
  Captured as `requirements/REQ-003-remove-inherited-portfolio-routes.md`, READY_FOR_SA.
- A9 revenue: **"ก"** — 2% of paid-course sales, **no separate ค่าเปิดคอร์ส**. Binds, because the
  ก/ข text was in REQ-002 before I asked. Reading (ข) superseded; the CONTESTED line is resolved.
- New questions raised, none answered by me: REQ-003 Q1 `/about` (never asked — not covered by
  "ลบ"), Q2 redirect-vs-404 on a live site, Q3 priority; REQ-002 Q1 sub-questions (2% of what,
  who pays the gateway fee). All written verbatim in the files first.
- Read-only survey of `front/src` only (no writes, nothing run): `Footer.tsx` links to
  /services,/portfolio,/contact and `verify-email/page.tsx` links to /contact — recorded in REQ-003.
- ⚠️ `inbox/SA.md` is 2.4 KB — messages are piling up unprocessed. Flagged to Sober.
- Ball: **the owner** (5 open questions) · **@Sober** for REQ-003 (also delivered to inbox/SA.md).

## [--:--] Porter (PM)
- Owner answered the five questions left open by the previous session. Written into
  `SYSTEM-FACTS.md` §"…five questions left open by session 8" (A10–A14) verbatim, BEFORE this entry.
- A10 `/about` — **"about for DTE"**: it STAYS, as DTE's About page. Closes the A8 ⚠️(a).
  He gave no About copy, so no page text is written; that is a content DATA REQUEST (new board row).
- A11 old links — **"redirect"**: the question offered only one target (home), so it binds —
  removed routes redirect to `/`, not 404. Closes A8 ⚠️(b). Mechanism stays Sober's call.
- A12 order — **"foundation first"**: REQ-001 before REQ-003. Priority set on the board; the two
  are NOT merged (REQ-003 C4 stands).
- A13/A14 revenue — **"ราคาขายเต็ม"** + **"platform"**: 2% of the GROSS sale price, and the
  platform absorbs the payment-gateway fee. REQ-002 §Open questions **Q1 is now CLOSED in full**.
- REQ-003 updated: new §Requirement 5 (redirect) and 6 (`/about` kept), 2 new acceptance criteria,
  §Out of Scope rewritten, §Questions all answered. Still READY_FOR_SA — now with nothing pending.
- Not done, deliberately (one unit per session): the `4013` port REQ still unwritten.
- Nothing run, no browser, no code/specs/tasks touched. No new questions raised this session.
- Ball: **@Sober** — REQ-003 is clear to spec (also delivered to `inbox/SA.md`).

## [--:--] Sober (SA Lead)
- Drained `inbox/SA.md` (7 Porter pointers, was 3.2 KB → empty). Nothing in them changed the plan:
  REQ-001 first ("foundation first"), REQ-003 waits, the `4013` port has a fact but no REQ.
- Wrote **`specs/SPEC-001-frontend-foundation.md`** for REQ-001 (now **IN_SPEC**). Designed
  against the real code, read-only, on `develop` as it sits on disk — **nothing built, nothing run.**
- Decisions in it: **Ant Design v6 proposed** (runner-up HeroUI; Mantine ruled out because no
  pattern skill exists for it — the C1 tension REQ-001 Q1 asked to be stated) · **upgrade to
  Next 16 + Tailwind 4** · **Tailwind kept**, `themes.css` stays the one palette and antd's
  `ConfigProvider` is fed from it inside the existing `ThemeProvider` · **lucide-react** as the
  single icon set · per-screen order login→register→verify-email→home→courses→teach→classroom.
- Answered **REQ-001 Q4** in the REQ. Deliberate, stated deviations from the house pattern:
  NextAuth 4 and React Query are **not** adopted under REQ-001, and `contexts/` is not renamed.
- Wrote **TASK-001** (Next 16 + Tailwind 4) with rollback + blast radius, **BLOCKED on the owner
  seeing it** (C5(b)). TASK-002 (skeleton + conventions doc + no-emoji harness) is next session's
  unit; TASK-003+ cannot be written until the library gate closes.
- Baseline: **15 files, ~160 emoji** in `front/src`; two pre-existing defects found by reading
  only, both **UNVERIFIED** — SPEC-001 §Decision 3. Not fixed on purpose: `layout.tsx` still
  ships the superseded product name in the page `<title>` (owner's word, not mine).
- Ball: **@Porter** — carry the library proposal (C5(a)) and TASK-001 (C5(b)) to the owner.
## [--:--] Porter (PM) — housekeeping
- Housekeeping hop only (hygiene gate was FAIL). No requirement processed, no REQ written, no
  work routed; `specs/`, `tasks/` and code untouched.
- Rotated `dispatcher-state.md` from 9 runs to the last 5. Runs 2026-09-06-a/-b/-c/-d moved
  **verbatim** (byte-compared) to `archive/dispatcher-state-2026-09-07.md`; nothing reworded,
  nothing dropped. Board "Project info" now points at the archive.
- Trimmed the oversized entry in this file (first Porter entry, 21 lines) to 14 by pointing at
  `SYSTEM-FACTS.md` A1-A6 and `REQ-002` §Open questions Q1, which already hold every fact it
  retold. Checked before trimming; the trim is marked inside the entry.
- ⚠️ For the owner/dispatcher: this rewrote an append-only file. PROTOCOL.md calls `log/` and
  `dispatcher-state.md` append-only, and `check-hygiene.mjs` itself says logs are "never
  rewritten by housekeeping" (which is why it warns, not fails). I did it because this hop's
  instruction was explicit. **The standing rule needs a decision, not my assumption.**
- `inbox/PM.md` read; Sober's SPEC-001 pointer left unprocessed on purpose (next PM unit).
- Ball: **PM** — carry Sober's library proposal (C5(a)) + TASK-001 to the owner next hop.

## [--:--] Porter (PM)
- Owner answered four items. Verbatim in `SYSTEM-FACTS.md` §"…the SPEC-001 gates and the
  housekeeping rule" (A15–A18), written BEFORE this entry; every detail and ⚠️ lives there.
- A15 library **"antd"** → REQ-001 **C5(a) CLOSED**. Library approved, not a schedule — Q2
  "ค่อยย้ายทีละหน้า" stands, migration stays screen by screen.
- A16 upgrade **"รันเลย"** → **C5(b) CLOSED**; he has seen TASK-001. Run = run **locally on
  `develop`**; production still his alone. **REQ-001 has no open owner gate left.**
- A17 title **"เปลี่ยน"** → cleared to the settled "Develyst The Education" (A6). ⚠️ exact
  `<title>` string + whether other occurrences are included: NOT stated, asked back, new Blocked row.
- A18 **"housekeeping แก้ได้"** → housekeeping MAY rewrite `log/` + `dispatcher-state.md`;
  retro-authorises the 2026-09-07 hop. Standing rule on the board; FAIL-vs-WARN scope is
  `DISPATCHER.md`'s, not his.
- Files: REQ-001 status + C5 (both gates ✅, old text kept as history) + §Out of Scope exception;
  board REQ-001 row, 2 resolved Blocked rows removed, 1 added, standing rule added.
- **Deliberately NOT done:** TASK-001's row still reads BLOCKED — a TASK status is Sober's to move,
  not mine. Flagged in `inbox/SA.md`. The `4013` REQ stays unwritten (next unit).
- Nothing run; no code/specs/tasks touched. Ball: **@Sober**.

## [--:--] Sober (SA Lead)
- Drained `inbox/SA.md` (Porter's SPEC-001 gate message). Both REQ-001 owner gates are closed, so
  this session was the gate follow-through plus the next TASK. Nothing built, nothing run.
- **TASK-001 → TODO** (BLOCKED was mine to move, per Porter). Old status kept as history inside the
  file; the ⛔ banner replaced with the "run LOCALLY on `develop`, production is his" reading of A16.
- **Wrote `tasks/TASK-002-…-emoji-harness.md`** (TODO, FE, depends on TASK-001): folder skeleton,
  exactly 6 component moves with their importers listed, `front/FRONTEND-CONVENTIONS.md`, and
  `tests/harness/check-no-emoji.mjs`. Deliberately installs **no** library — antd is TASK-003.
- Importer counts in it were read off the code 2026-09-07, read-only: `layout.tsx` imports
  Navbar+Footer (so a bad move breaks every route), `AIChat.tsx` has **no importer at all**.
- **SPEC-001 → ACTIVE**; Decision 1 marked approved with Porter's verbatim answer under Q1;
  §Tasks rewritten (TASK-003 ungated, just unwritten).
- **My call, as Porter left it:** the page-`<title>` rename gets **its own TASK**, not folded into
  TASK-001/002 — a copy change should not ride a framework rollback. It stays **unwritten**: the
  owner cleared the change but not the exact string nor whether other occurrences are included.
  Nobody invents that. SPEC-001 §Non-functional.
- Ball: **@Fern** — TASK-001 then TASK-002 (delivered to `inbox/FE.md`). @Porter informed.

## [--:--] Porter (PM)
- Drained `inbox/PM.md` (both Sober messages: SPEC-001 ACTIVE + the rename kept as its own
  unwritten TASK). One unit this session: closing the rename DATA REQUEST.
- Owner answered the two A17 ⚠️ items. Written to `SYSTEM-FACTS.md` **before** replying:
  - **A19** — exact page `<title>` = **`DTE — Develyst The Education`**, character for character
    (spaced em dash, no tagline, no added suffix).
  - **A20** — scope **"ทุกที่"** = every occurrence of "Disrupt Thai Education" in `front/`; the
    question he answered enumerated `<title>`, metadata description, OG tags, header, footer.
- ⚠️ Deliberately NOT inferred, so asked back to him: whether "ทุกที่" reaches **outside `front/`**
  (`back/`, repo docs, deploy config, the live site), and whether non-`<title>` places take the
  full A19 string or the bare settled name. Kept as a Blocked row; not blocking REQ-001.
- Files: `SYSTEM-FACTS.md` (new A19/A20 section), REQ-001 §Out of Scope exception, board
  (REQ-001 row, the title DATA REQUEST row replaced by the narrower residual one).
- Not done: the `4013` port REQ still unwritten (its own unit). No specs/tasks/code touched;
  nothing run. Ball: **@Sober** — the rename TASK is now writable (delivered to `inbox/SA.md`).

## [--:--] Fern (FE)
- Drained `inbox/FE.md` (2 Sober pointers). One unit: **TASK-001** — `front/` Next 15.5.4 → **16.2.9**
  and Tailwind 3.4.17 → **4.3.0**, run LOCALLY only. `back/` untouched, production never contacted,
  no git write, both `.bak` files deleted. TASK-001 → **REVIEW**.
- Baseline build was green BEFORE the upgrade (Next 15, 15/15 pages) and green after (Next 16, 14/14).
  All five packages pinned exact, one React (`19.2.7`). Async `params` did not apply — every route
  reads params via client hooks, no `async Page({params})` in the tree.
- **Two silent Tailwind-4 regressions found and fixed** — both would have shipped invisible:
  (a) v4 flipped the plugin-vs-theme name collision, turning `text-secondary` from stone-600 text into
  stone-100 (near-invisible); (b) v4's cascade layers let `globals.css`'s unlayered `a {}` beat every
  utility. Proven by compiling the same config with Tailwind 3 and diffing the emitted CSS —
  commands, before/after values and browser measurements are in TASK-001 §Implementation Notes.
- Tailwind config **kept via `@config`**, not ported to `@theme` (it holds a JS plugin `@theme` cannot
  express). hex-vs-rgb defect fixed by **dropping the `rgb()` wrapper**; `themes.css` stays the one palette.
- **UNVERIFIED, said plainly:** `/teach` and `/classroom/[id]` sit behind the auth guard and redirect
  to `/login`, so light+dark on `/classroom/[id]` was NOT done. Below-the-fold appearance on every
  route is also UNVERIFIED — my browser tool's screenshots go stale after scrolling (proved with a
  fixed probe element). The owner's own eyes settle both.
- Next 16 rewrote `next-env.d.ts` + `tsconfig.json` itself (`jsx: react-jsx`) — recorded, not hand edits.
- Ball: **@Sober** — review TASK-001 and answer Q1–Q4 there (branch is `D1` not `develop`; whether my
  collision fix is approved; two pre-existing defects; one build warning). Delivered to `inbox/SA.md`.

## [01:31] Porter (PM)
- `inbox/PM.md` empty. One unit: the owner's answers to my two rename-scope questions.
- Written to `SYSTEM-FACTS.md` **before** replying (new §"Owner's answers to the two rename-scope
  questions"): **A21** — "ทุกที่" **does** reach outside `front/` (the question enumerated `back/`,
  `README.md`, `DTE.md`, deploy config, the live site); **A22** — outside the page `<title>`, the
  name is the bare **`Develyst The Education`**. A20's two ⚠️ items are CLOSED. The rename is now
  fully specified: A19 string · A20 `front/` · A21 beyond `front/` · A22 form.
- Because it now spans `back/` + repo docs, the rename **left REQ-001** (which excludes backend work)
  and became **`requirements/REQ-004-product-name-rename-everywhere.md` — READY_FOR_SA**.
- ⚠️ Deliberately NOT decided by me, written into REQ-004 instead: *which* files outside `front/`
  contain the old name (Sober enumerates from code — nobody guesses), and any occurrence that is not
  obviously one of the two forms (a sentence, a legal line, an email template) comes back to the owner.
- ⚠️ Boundary recorded, not a narrowing of his answer: occurrences in production / deploy / the live
  site are in scope of the rename but are **the owner's own hand** — listed for him, never executed.
  Board's rename Blocked row rewritten to that.
- Files: `SYSTEM-FACTS.md`, new `requirements/REQ-004-…md`, REQ-001 §Out of Scope, `board.md`.
- Not done (own units): the `4013` port REQ, REQ-002 Q2–Q7. Nothing run; no specs/tasks/code touched.
- Ball: **@Sober** — REQ-004 is READY_FOR_SA (delivered to `inbox/SA.md`).

## [07:55] Sober (SA Lead)
- One unit: **reviewed TASK-001 → DONE**. Verdict, my cross-checks, the residual risk and the
  answers to Q1–Q4 all live in `tasks/TASK-001-…md` §Review + §Questions. Not retold here.
- Checked the **evidence, not the claim**, read-only: `git status` (exactly 7 files, no `.bak`),
  `D1`==`develop`==`253eeda9…`, the pins, the three diffs, and the one grep the collision fix
  rests on — Fern's removed colour keys cost no class any page uses. **Q2 APPROVED.**
- Q3 → no TASKs, both become conventions (the `themes.css` double import is **load-bearing**);
  Q4 → the build warning is **accepted**, not silenced.
- **I ran no build, no dev server, no browser** — Fern's measurements are his alone. Accepted
  UNVERIFIED, owed to the owner's eyes: `/teach`, `/classroom/[id]` (auth guard) and below-the-fold
  everywhere; two new Blocked rows on the board.
- **Not resolved by me, deliberately:** the checkout is on `D1`, not `develop` — git is the human's
  and nobody here knows why `D1` exists. A question, not an assumption (TASK-001 §Questions Q1).
- TASK-002 unblocked; its conventions doc gains a 7th section (two Tailwind-4 cascade traps).
- Not done (own units): REQ-003 and REQ-004, both still READY_FOR_SA.
- Ball: **@Fern** TASK-002 · **@Porter** the 3 owner items. Both delivered to their inboxes.

## [--:--] Fern (FE)
- Drained `inbox/FE.md`. One unit: **TASK-002** → **REVIEW** — skeleton (7 folders + barrels), the
  6 `git mv` moves, `front/FRONTEND-CONVENTIONS.md` (7 sections, 127 lines), and
  `tests/harness/check-no-emoji.mjs`. No antd, no `back/`, no commit, production never touched.
- Importer set matched TASK-002's table **exactly**. All six are default exports, so barrels
  re-export them named and **no component's export was edited**.
- Evidence in `tasks/TASK-002-…md` §Implementation Notes: build green 14/14 · `tsc --noEmit` exit 0
  before *and* after · old-path grep empty · `/` walked dark **and** light, console clean.
- **Emoji baseline: 124 occurrences in 14 files.** SPEC-001's "~160" was not wrong — its per-file
  counts match mine to the unit; the gap is `about/page.tsx.backup` (33), an unscanned extension.
- **UNVERIFIED:** below-the-fold appearance — I proved the Footer exists and is styled by reading
  the live DOM, not by looking at it (screenshots go stale after scrolling). Owner's eyes.
- Finding: TASK-001's 7 files are **no longer uncommitted** — committed to `D1` as `d466ba4`;
  board's Blocked row rewritten, the branch call stays the human's.
- Ball: **@Sober** — review TASK-002, answer Q1–Q3 there. Delivered to `inbox/SA.md`.

## [--:--] Sober (SA Lead)
- One unit: **reviewed TASK-002 → DONE**. Verdict, the table of what I re-ran, the accepted
  UNVERIFIED items and the answers to Q1–Q3 all live in `tasks/TASK-002-…md` §Review / §Questions.
- **Evidence, not the claim:** I re-ran six of the eight DoD checks myself, read-only — the folder
  tree, the old-path grep (empty), `npx tsc --noEmit` (exit 0), the emoji checker on `front/src`
  (**124 in 14 files, 39 scanned** — reproduced to the unit), the zero case (exit 0) and
  `git status`. I also read the checker's source against SPEC-001's stated ranges. Build and the
  browser walk-through are **Fern's own measurements**; I ran neither.
- Accepted **UNVERIFIED**, owed to the owner's eyes: below-the-fold after the moves · routes other
  than `/` (an inference from a green build, and `layout.tsx` is the file that changed) · `/`'s
  bundle weight through the `common` barrel (folded into TASK-003, not waved away).
- Q1 → **keep the barrel** (AIChat's only deps are already on `/` via SearchAI). Q2 → `D1` ==
  `develop` == `origin/develop` == `d466ba4`, so that Blocked row is **closed**. Q3 → the
  `.gitignore` line rides into TASK-003.
- ⚠️ **New finding, not mine to resolve:** `origin/production` = `253eeda`, one commit **behind**
  `develop` — against `SYSTEM-FACTS.md` A4 "production runs `develop`'s tip". Read-only, no fetch,
  no live site touched. New Blocked row; @Porter for the owner.
- Not done (own units): TASK-003 (antd) unwritten · REQ-003 and REQ-004 still READY_FOR_SA.
- Ball: **@Porter** — the two owner items. **@Fern: nothing waiting**, do not start TASK-003.

## [--:--] Porter (PM)
- Drained `inbox/PM.md` (both Sober messages processed; file back to empty). One unit: the owner's
  **standing ruling on git**, recorded and propagated.
- Written to `SYSTEM-FACTS.md` **before** replying — new §"Owner's STANDING RULING", **A23**:
  *"เลิก ยุ่งกับการ commit หรือgit ของฉัน ทำงาน code  กันไป"*. Branches, commit hashes, what is
  merged/pushed/uncommitted, which branch production runs = **out of the team's scope**; nobody
  asks, reports or gates work on them again. Extends **A1** from `main` to the whole subject.
- **Unchanged, restated in the same fact:** no agent commits, pushes, merges, deploys, runs the
  workflow scripts, or touches production / the production DB. Work still lands as files on `develop`.
- Closed by it: Sober's `origin/production`-vs-`develop` finding and *"does A4 still hold?"* —
  **A4 stands as he stated it and is no longer a live question.** Pointer lines added under A4 and
  at the end of §Branches so neither is re-raised from an old read.
- Board: that Blocked row **struck** (with the reason), the "Branch" line rewritten as a 🔴 standing
  rule, the owner's-eyes row de-git-ed — it is about his eyes, not about a merge.
- ⚠️ Not mine: the same item still sits in `tasks/TASK-002-…md` §Review/§Questions — `tasks/` is
  Sober's; asked him to strike it. Nothing run; no specs/tasks/code touched.
- Not done (own units): the `4013` port REQ, REQ-002 Q2–Q7.
- Ball: **@Sober** — strike the closed item, then his queue (TASK-003 unwritten, REQ-003 + REQ-004
  READY_FOR_SA). Nothing waiting on the owner from this hop.

## [--:--] Sober (SA Lead)
- Drained `inbox/SA.md` of Porter's A23 message. First: **struck the closed git item** in
  `tasks/TASK-002-…md` — both places (§Review "One finding…" and §Questions Q2) — with the reason
  and the unchanged hard guard written under each. It will not be carried again.
- One unit: **TASK-003 written** — `tasks/TASK-003-antd-install-and-theme-bridge.md`, status TODO,
  owner Fern. Detail is in the TASK, not retold here.
- Design calls (in the TASK, made after reading the real code): exact pins and **no other
  dependency** · `constants/theme-tokens.ts` as a **mirror of `themes.css` with a grep
  drift-guard** (antd cannot read CSS vars at SSR) · fixed provider nesting · **two** wrappers,
  no `ui/` root barrel · a throwaway probe route **deleted** before close, so the evidence is a
  browser and not a code read.
- Deferred on purpose, named so nobody guesses: antd `locale` + `dayjs` (the first
  locale-sensitive component's TASK decides it, with me) and `cssVar`. Fern may not decide either.
- Folded in: TASK-002's deferred `/` bundle measurement (BEFORE/AFTER route tables) and its
  `.gitignore` `*.tsbuildinfo` line. `metadata.title` stays untouched — that is REQ-004's.
- **I ran nothing** — no install, no build, no browser. Read-only reads of `front/` only.
- Not done (own units): REQ-003 and REQ-004, both still READY_FOR_SA.
- Ball: **@Fern** — TASK-003 is TODO (delivered to `inbox/FE.md`). Nothing waiting on Porter.

## [22:55] Fern (FE)
- Ran **TASK-003** end to end; **TODO → REVIEW**. Every DoD item has its command and its real
  output in `tasks/TASK-003-antd-install-and-theme-bridge.md` §Implementation Notes.
- `antd@6.4.3` + `@ant-design/nextjs-registry@1.3.0` at exact pins, no other dep, single React,
  v5 shim absent. Added `constants/theme-tokens.ts` (mirror + grep guard), `AntdConfigProvider`,
  `ui/Button` + `ui/Input`; wired `layout.tsx` in the fixed nesting order.
- **Browser-proved in both themes** by computed style: light primary `#0EA5E9` (not `#1677ff`),
  dark `colorBgBase`/`colorTextBase` exactly `#1C1917`/`#F5F5F4`, console clean. Tailwind
  preflight does **not** flatten antd here — no `globals.css` fix made. Dark-reload light flash
  confirmed, pre-existing.
- Probe route deleted; AFTER build green, `tsc --noEmit` 0, emoji baseline 124 with **zero hits
  in my files**; `.gitignore` line + `FRONTEND-CONVENTIONS.md` §2/§3 done.
- @Sober — 4 questions, none guessed: **+100.8 kB uncompressed First Load JS on every route**
  (the shared-chunk jump Step 8 said to escalate) · antd v6 emits `cssVar` **by default** ·
  compressed delta UNVERIFIED (Next 16 prints no size table) · v6 renamed Alert `message`→`title`.
- Ball: **@Sober** — review TASK-003 (pointer in `inbox/SA.md`).
