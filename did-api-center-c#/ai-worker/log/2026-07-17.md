# Log — 2026-07-17 — did-api-center-c#

> Append-only. Every role adds an entry at session end. Format: see PROTOCOL.md.

## [00:00] System
- Workspace initialized: protocol, role charters, board, and folders created.
- Old `conversation-20260717.md` replaced by this daily log convention.
- @Porter: waiting for the first requirement from the stakeholder.

## [00:30] System
- Protocol updated with two new rules (see PROTOCOL.md):
  1. **Language** — Porter ↔ human in Thai; all artifacts and role-to-role
     communication in English.
  2. **Brownfield / DATA REQUEST** — assume partial knowledge of the system;
     never run SQL or touch real databases/environments; missing real-world
     data is requested from the human via BE → Sober → Porter (in Thai), and
     answers land in `../project-docs/`.

## [--:--] Porter (PM)
- Session started. Read PROTOCOL.md, PM.md, board.md, and today's log.
- Stakeholder confirmed the code repository:
  `C:\Users\Admin\sa-project\spf\DidSpf.WebApi.Center` — updated board.md.
- No REQ yet; waiting for the first requirement from the stakeholder.

## [--:--] Jason (BE)
- Session started. Read PROTOCOL.md, BE.md, board.md, and today's log.
- Confirmed code repository exists at
  `C:\Users\Admin\sa-project\spf\DidSpf.WebApi.Center` (ASP.NET Core WebApi:
  Controllers / Services / Models / Middleware present).
- No TASK with status TODO on the board yet. Standing by for tasks from @Sober.

## [--:--] Jason (BE)
- Picked up TASK-001 (fully unblocked per Porter/Sober). Set IN_PROGRESS → REVIEW.
- Read TASK-001, SPEC-001 (§A–E), and the real code before editing:
  `Models/Dashboard/DashboardMoveLicenseModel.cs`, `Controllers/DashboardMoveLicenseController.cs`.
  Confirmed the controller lines match SPEC §E exactly (`:53` region, `:70` weapon_category, `:88-89`).
- Implemented the full mapping — **JSON-key rename only**, 2 files:
  1. Model: §A request body (11), §B dropdown containers (7), §C chart containers (3), §D table row (per SPEC).
     C# property names, comments, and shared classes (`DashboardChartData*`, `DropdownDDL*`) left untouched.
  2. Controller §E: renamed the 3 cascade `[FromQuery(Name=...)]` binding strings only (kept C# param identifiers).
- **Verified:** `dotnet build` from `spf/DidSpf.WebApi.Center` → **0 Error(s), 184 Warning(s)** (all pre-existing,
  unrelated files). Static grep: no old keys remain in the model, no old query-param `Name=` in the controller;
  `[JsonProperty]` count 68 = identical to before (strings only). Details in TASK-001 Implementation Notes.
- **One DoD caveat:** the live-response spot-check needs a running Center + real Oracle DB — out of my brownfield
  scope, so I verified statically instead. If a live JSON capture is required for sign-off, it's a DATA REQUEST
  (stakeholder runs `/dashboard-move-license/table`, drops output in `../project-docs/`).
- @Sober: **TASK-001 ready for review.** Please check the §A–E mapping against SPEC-001 and confirm the
  static-verification approach is acceptable for DoD item 5 (or route the live-capture DATA REQUEST via @Porter).

## [11:44] Sober (SA Lead)
- Session started. Read PROTOCOL.md, SA-Lead.md, board.md, and today's log.
- Surveyed the code repository `C:\Users\Admin\sa-project\spf\DidSpf.WebApi.Center`:
  ASP.NET Core net8.0 WebApi, Dapper + Oracle (3 schemas via IUnitOfWorkSPF /
  SPFCenter / Einternet), ~80 controllers, JWT auth (SPF officer / ISPF trader),
  AES-encrypted ids on the wire, `ResponseResult<T>` envelope. Project has its
  own `CLAUDE.md` documenting conventions — will design specs against it.
- No REQ with status READY_FOR_SA on the board. Standing by.
- @Porter: ping me when a REQ is ready for spec work.

## [12:10] Porter (PM)
- Received requirement from stakeholder: DASHBOARD_LICENSE_MOVE (อ.10 dashboard)
  response keys don't match DB column names — hard for frontend to consume.
- Clarified with stakeholder: scope = ALL responses of the page (dropdown, chart,
  table + request body); target key style = **lowercase snake_case of the column
  name** (e.g. `LICENSE_NO` → `license_no`), NOT exact UPPER_CASE Oracle spelling.
- Created `requirements/REQ-001-move-license-response-keys-match-db-columns.md`,
  status READY_FOR_SA.
- Left two open questions in the REQ for SA: Q1 naming rule for computed fields with
  no backing column (`key`, `dateFormatted`, `buyerGroupLabel`, `moveQty`, etc.);
  Q2 whether renaming the inbound request body is in scope vs. keeping the
  frontend CriteriaValues contract.
- @Sober: please pick up REQ-001. If Q1/Q2 need a stakeholder decision, ask here and
  I'll relay in Thai.

## [12:25] Porter (PM)
- Stakeholder answered REQ-001 open items:
  - Q2 → **request body IS in scope.** Frontend named those keys wrong originally;
    fix all of them to proper DB-column snake_case. Updated REQ-001 Requirement #2
    and the Q2 answer.
  - Added a "DB data availability" note to REQ-001: stakeholder will provide any DB
    table's column names / structure on request. @Sober — if you need real column
    names to build the exact mapping, raise a DATA REQUEST (table or SQL) and @Porter;
    I'll get it from the stakeholder into ../project-docs/.
- Q1 (naming rule for computed/no-column fields) still open — @Sober, propose a rule
  and I'll confirm with the stakeholder.

## [12:10] Sober (SA Lead)
- Picked up REQ-001, set IN_SPEC. Read the real code before designing:
  `Controllers/DashboardMoveLicenseController.cs`, `Services/DashboardMoveLicenseService.cs`,
  `Models/Dashboard/DashboardMoveLicenseModel.cs`, the query DTO
  `QueryResult/DashboardMoveLicenseQueryResult.cs`, and the SQL
  `Repositories/TTLicenseDtlRepository.GetMoveLicenseDashboard`.
- **No DATA REQUEST needed.** The repository SQL aliases every column explicitly
  (`L.LICENSE_NO AS LicenseNo`, `LM.ORIGIN_PLACE_NAME AS OriginPlaceName`, …), so every
  response field is traceable to its physical Oracle column from code alone. Built the full
  field→column map without touching the DB.
- Key findings baked into SPEC-001 (ACTIVE):
  1. The change is almost entirely `[JsonProperty("...")]` string edits in ONE file
     (`DashboardMoveLicenseModel.cs`). Service/DTO/SQL and C# property names do NOT change.
  2. **Hard constraint:** `DashboardChartData`/`DashboardChartCategory`/`DashboardChartSeriesItem`
     are **shared with the License Book dashboard** (`DashboardLicenseBookModel.cs:103`). Renaming
     their inner chart keys would change that other dashboard → out of scope. Only the 3 chart
     *container* names on `DashboardMoveLicenseChartsResponse` are renamed.
  3. `DropdownDDLData`/`DropdownDDLItem` inner keys are a shared generic type (~40 files) — left as-is.
  4. Correction to REQ: `weaponLabel` is NOT computed — it maps to real column `PRODUCT_NAME`.
- Wrote `specs/SPEC-001-...` (full A–D mapping tables + 4 naming rules) and
  `tasks/TASK-001-rename-move-license-json-keys.md` (TODO, assignee Jason).
- @Porter: proposed the Q1 naming rule in REQ-001 `## Questions` (label→`_name`, computed→
  descriptive/`_formatted`, containers→`_ddl`/`bar_chart_data*`, region→`dest_area_name`).
  Please confirm with the stakeholder. Also raised **Q2**: are the cascade GET query-params
  (`region`/`weapon_category`/`unit`) in scope too? SA recommends yes.
- @Jason: TASK-001 is TODO. The R1 direct-column keys (~40, the bulk) are unambiguous — safe to
  start now. Keys marked † in SPEC-001 depend on Porter's Q1 answer; apply the proposed names but
  don't move to REVIEW until Q1 is confirmed. Hold the cascade query-param rename until Q2.

## [13:05] Porter (PM)
- Relayed SPEC-001 Q1 & Q2 to stakeholder; got decisions (answers written into REQ-001
  and SPEC-001 `## Questions`):
  - **Q1 → CONFIRMED in full.** Accept Sober's R2/R3/R4 naming rules as proposed.
    Region field = **`dest_area_name`** (keep `dest_` prefix, not bare `area_name`).
    `weaponLabel`→`product_name` correction accepted. All † keys approved as written.
  - **Q2 → YES, cascade GET query params are in scope.** Rename `region`→`dest_area_name`,
    `weapon_category`→`product_type_group_code`, `unit`→`quantity_unit_id`, and update the
    controller `[FromQuery(Name=...)]`. Frontend will update its URLs.
- Cleared both items from the board's Blocked/waiting table.
- @Jason: **fully unblocked.** Apply all † keys with the confirmed names and do the cascade
  query-param rename too. Nothing left pending from Porter — you can take TASK-001 through to REVIEW.
- @Sober: stakeholder approved your spec as-is; no spec changes needed from the answers.

## [12:20] Sober (SA Lead)
- Both Q1 & Q2 confirmed by stakeholder — finalized the spec/task to remove all gating:
  - SPEC-001: naming-rules heading now "CONFIRMED by stakeholder 2026-07-17"; † legend updated
    (no longer pending); added **§E** = cascade GET query-param rename (Q2 in scope) with exact
    controller line refs (`:53` region, `:70`/`:88` weapon_category, `:89` unit); updated Flow +
    Non-functional to include the `[FromQuery(Name=...)]` changes. (Porter's earlier note said "no
    spec changes needed" re: the *mapping* — correct; these are just scope-confirmation edits, the
    Q2 cascade section was always conditional and is now activated.)
  - TASK-001: replaced the "Gating note" with a "Status note" (both questions resolved → implement
    the full §A–E mapping, no hold); made the cascade query-param rename a mandatory step + DoD item.
- Net scope for Jason (unchanged mapping, one file + 3 controller params):
  §A request body (11 keys), §B dropdown containers (7), §C chart containers (3),
  §D table row (40), §E cascade query params (3).
- @Jason: TASK-001 is fully unblocked and final. Implement SPEC-001 §A–E, `dotnet build`, spot-check
  one response, fill Implementation Notes, set REVIEW. I'll review against the mapping tables.
- Board: TASK-001 stays TODO (Jason to move IN_PROGRESS); REQ-001 IN_SPEC until all tasks DONE.

## [13:38] Sober (SA Lead)
- Reviewed TASK-001 (was REVIEW) against SPEC-001 §A–E by reading the actual changed code, not
  just Jason's notes:
  - `DashboardMoveLicenseModel.cs` — §A (11 request keys), §B (7 `*_ddl` containers), §C (3 chart
    containers), §D (38 table props) **all match the mapping exactly**. `key` unchanged;
    `weaponLabel`→`product_name` correction applied.
  - `DashboardMoveLicenseController.cs` — §E cascade params correct (`:53` dest_area_name, `:70`/`:88`
    product_type_group_code, `:89` quantity_unit_id); C# identifiers preserved.
  - Hard constraints upheld: shared `DashboardChartData*` inner keys and `DropdownDDL*` untouched;
    no C#-property / service / DTO / SQL change (build green proves references still bind).
  - Accepted Jason's brownfield deferral of the live-DB spot-check — a `[JsonProperty]` rename is
    deterministic serialization with zero logic change; build + static verification is sufficient
    sign-off. No live capture required.
- **Verdict: DONE.** Wrote the verdict into TASK-001 `## Review`. TASK-001 → DONE, SPEC-001 → DONE.
- All tasks of SPEC-001 are DONE → set **REQ-001 → SPEC_DONE**.
- @Porter: **REQ-001 is ready for your acceptance check.** Implementation matches every REQ-001
  acceptance criterion. Note for your stakeholder summary (Thai): this is a JSON-key rename only —
  the frontend must update the keys it sends (request body: `move_request_type`, `transport_type_code`,
  `trader_id`, `dest_area_name`, …) AND the cascade endpoint URL query params
  (`?dest_area_name=`, `?product_type_group_code=`, `?quantity_unit_id=`). Values/behaviour unchanged.

## [14:10] Porter (PM)
- Acceptance check on REQ-001 (was SPEC_DONE; TASK-001 DONE). Independently verified the
  actual changed code, not just the notes:
  - Model `DashboardMoveLicenseModel.cs`: grep confirms 0 old keys remain; new snake_case keys
    present (`license_no`, `issue_date_formatted`, `product_name`, `move_request_type_name`,
    `authority_group_no_name`, `dest_area_name` x2, `bar_chart_data1`, `move_request_type`, …).
  - Controller `DashboardMoveLicenseController.cs`: 0 old cascade query params (`region`/
    `weapon_category`/`unit`) remain; new present (`dest_area_name`, `product_type_group_code` x2,
    `quantity_unit_id`).
  - Build 0 Error(s) (Jason + Sober). Live-DB capture deferred per brownfield rule — accepted.
- All 4 REQ-001 acceptance criteria met → **REQ-001 = DELIVERED.** Wrote an Acceptance section
  into REQ-001; board updated.
- Summarized outcome to stakeholder in Thai, incl. the **breaking-contract heads-up**: frontend
  must update the keys it *sends* (POST body + cascade GET URL query params), not just what it reads.
- @Sober @Jason: REQ-001 closed. Thanks — clean single-file rename, green build, no rework.

## [14:40] Porter (PM)
- New requirement from stakeholder: the DASHBOARD_LICENSE_MOVE weapon-type dropdown
  (`product_type_group_code_ddl`) is hardcoded (PTG01-04) in
  `Services/DashboardMoveLicenseService.cs:34-40`; make its code list configurable in
  appsettings.json. Expose only PTG01/PTG02/PTG03 now; extensible later.
- Clarified with stakeholder:
  - Scope = **dropdown options only.** No server-side data filter added (dashboard data is
    already filtered by the value the user picks from this dropdown).
  - Config stores **codes only**; the Thai label stays from the existing in-code map.
- Created `requirements/REQ-002-move-license-weapon-type-ddl-configurable.md`, READY_FOR_SA.
- Left one open item for SA (Q1): behaviour when a configured code has no in-code label
  (future PTG05 case) — doesn't affect the initial PTG01-03 set.
- @Sober: please pick up REQ-002. If Q1 needs a stakeholder decision, ask here and I'll relay.

## [14:11] Sober (SA Lead)
- Picked up REQ-002, set IN_SPEC. Read the real code + existing config wiring before designing:
  `Services/DashboardMoveLicenseService.cs` (WEAPON_TYPES array ~L35-41, used ~L64),
  `Models/ConfigurationsModel.cs`, `appsettings.json` (`Configurations` section),
  `Program.cs` (`Configure<ConfigurationsModel>` block L191-209, service reg L128).
- **Design (SPEC-002, ACTIVE) — reuse the existing config pattern, add no new subsystem:**
  1. Add key `Configurations:MoveLicenseWeaponTypeCodes` = `["PTG01","PTG02","PTG03"]` in appsettings.
  2. Add `List<string> MoveLicenseWeaponTypeCodes` to `ConfigurationsModel`; map it in the existing
     `Configure<ConfigurationsModel>` block (one line, `GetSection(...).Get<List<string>>()`).
  3. Inject `IOptions<ConfigurationsModel>` into `DashboardMoveLicenseService` (already registered by
     the Configure call; service stays Scoped). Keep `WEAPON_TYPES` as the **label source** (labels
     stay in code per REQ); rewrite the dropdown build to iterate the **configured codes in order**,
     resolve label from the map, skip unlabeled codes.
- Confirmed REQ Out-of-Scope honoured: **no server-side data filter added** — config only limits the
  dropdown options; chart/table still filter by the user's selection.
- Net change is tiny: 1 config key + 1 model prop + 1 Program.cs line + 1 ctor param + rewrite 1
  statement. 4 files, no DI/registration change.
- Wrote `specs/SPEC-002-...` and `tasks/TASK-002-...` (TODO, assignee Jason).
- @Porter: answered Q1 in REQ-002 — recommend **skip** unknown/unlabeled configured codes (alt:
  show code-as-label); default **no** warning-log to stay minimal. Please confirm with stakeholder.
  **Non-blocking** — it's a 1-line behaviour, Jason can implement the rest of TASK-002 now.
- @Jason: TASK-002 is TODO and startable. Follow SPEC-002 "Code design" exactly; don't add a data
  filter or move labels into config. `dotnet build` + paste output; fill Implementation Notes; REVIEW.

## [--:--] Jason (BE)
- Picked up TASK-002. Set IN_PROGRESS → REVIEW. Read SPEC-002 + all 4 target files before editing
  (`Services/DashboardMoveLicenseService.cs`, `Models/ConfigurationsModel.cs`, `Program.cs` config
  block, `appsettings.json`). Confirmed `appsettings.Development.json` has **no** `Configurations`
  block → only appsettings.json needs the key.
- Implemented SPEC-002 "Code design" exactly, 4 files:
  1. `appsettings.json` — `Configurations.MoveLicenseWeaponTypeCodes = ["PTG01","PTG02","PTG03"]`.
  2. `ConfigurationsModel.cs` — `List<string> MoveLicenseWeaponTypeCodes`.
  3. `Program.cs` — one mapping line in the existing `Configure<ConfigurationsModel>` block.
  4. Service — `using Microsoft.Extensions.Options;`, ctor takes `IOptions<ConfigurationsModel>`,
     stores codes in `_weaponTypeCodes`; rewrote the weapon-type dropdown build to iterate the
     configured codes **in order**, resolve label from `WEAPON_TYPES` (kept as label source), skip
     unknown/unlabeled codes. **No data filter added; no label moved to config; DI unchanged.**
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** appsettings.json validated with a
  JSONC-aware parser → `MoveLicenseWeaponTypeCodes = ['PTG01','PTG02','PTG03']`, `UrlImage` intact.
  Static trace of the dropdown branch confirms exactly 3 items in order (กระสุน/อาวุธปืน/วัตถุระเบิด),
  PTG04 absent; reorder/add-PTG04/unknown-code all behave per DoD with no code change. The weapon-type
  branch touches no DB, so static trace is conclusive (a full live `SearchFilter()` needs Oracle →
  brownfield, out of scope). Details in TASK-002 Implementation Notes.
- **Non-blocking note on SPEC-002 Q1:** implemented the SA-recommended **skip** for unlabeled codes.
  Doesn't affect the PTG01–03 set. If stakeholder prefers "show code as label" it's a 1-line change;
  a skip-warning log would need an `ILogger` injection (none today).
- @Sober: **TASK-002 ready for review.** Please check the 4-file change against SPEC-002 and confirm
  the Q1 "skip" behavior is acceptable (or route the alternative via @Porter).

## [15:20] Porter (PM)
- Relayed SPEC-002 Q1 to stakeholder — got a **design change**, not just an edge-case answer:
  - The weapon-type dropdown **label must come from the DATABASE**, not the in-code map. Config
    supplies codes only; the dropdown looks them up in the DB ("เหมือน query where in
    (code1,code2,code3)"). Code in DB → shown with DB Thai name; bogus code not in DB → naturally
    not shown. **No warning log.**
  - This supersedes SPEC-002's "keep WEAPON_TYPES as the label map" approach.
- Updated REQ-002 (Requirement #3, Constraints, Acceptance Criteria) + answered Q1 in REQ-002 and
  SPEC-002 `## Questions`. Set REQ-002 owner → Sober; flagged on the board's Blocked/waiting.
- @Sober: please **revise SPEC-002** to source the label from the DB (lookup keyed by the configured
  codes). If the code→Thai-name table/view isn't obvious from existing repos (`VwProductRepo` etc.),
  raise a **DATA REQUEST** (exact table/SQL) and @Porter — stakeholder will provide it. Then re-task.
- @Jason: **HOLD TASK-002.** It's at REVIEW but built against the old in-code-label design, which is
  now changed. Don't take it further until Sober revises SPEC-002 — expect REWORK to a DB lookup.
  (Config-as-codes-only and dropdown-order-from-config are unchanged.)

## [14:31] Sober (SA Lead)
- Handled the REQ-002 design change (label source: in-code map → DB). Investigated the code BEFORE
  raising a DATA REQUEST — and found the source is **already wired**, so **no DATA REQUEST needed**:
  - `TMProductTypeGroupEntity` = `[Table("T_M_PRODUCT_TYPE_GROUP")]`, key `ProductTypeGroupCode`
    (`PRODUCT_TYPE_GROUP_CODE`) + `ProductTypeGroupName` (`PRODUCT_TYPE_GROUP_NAME`).
  - `TMProductTypeGroupRepository.GetDataAll()` / `GetDataByProductTypeGroupCode(code)`.
  - Exposed as `IUnitOfWorkSPF.TMProductTypeGroupRepo`. (Same table already used in
    `TTLicenseDtlRepository.GetTraderLicenseDtl` as `PTG.PRODUCT_TYPE_GROUP_NAME` — authoritative.)
- **Revised SPEC-002** (rewritten, still ACTIVE): label now sourced from the DB via
  `TMProductTypeGroupRepo.GetDataAll()` → dict `code→ProductTypeGroupName` → emit configured codes
  **in config order**, WHERE-IN semantics (code not in DB ⇒ not shown, no error/log). Config plumbing
  unchanged. `GetDataAll()` (1 call, tiny master table) over per-code lookups.
- **TASK-002 → REWORK** (not a defect — a spec change). Kept Jason's config plumbing verbatim
  (appsettings key, `ConfigurationsModel` prop, `Program.cs` mapping, `IOptions` injection,
  `_weaponTypeCodes`); the only change is: **remove the now-orphaned `WEAPON_TYPES` array** and swap the
  dropdown label lookup to the DB repo (exact snippet in TASK-002 "What to CHANGE" + Review).
- Reconfirmed REQ Out-of-Scope still honoured: no server-side data filter; only the dropdown options
  change.
- Board: REQ-002 IN_SPEC (owner Jason — rework), TASK-002 REWORK, Blocked/waiting cleared (no data req).
- @Jason: TASK-002 REWORK is ready and fully specified — keep your plumbing, swap the label source to
  `TMProductTypeGroupRepo` per the task, remove `WEAPON_TYPES`, `dotnet build`, set back to REVIEW.
- @Porter: no DATA REQUEST required after all — the DB code→name table was already in the codebase.

## [--:--] Jason (BE)
- Handled TASK-002 REWORK (design change, not a defect). Set REWORK → IN_PROGRESS → REVIEW.
- Verified the DB source exists before editing (read the real code): `IUnitOfWorkSPF.TMProductTypeGroupRepo`
  (`IUnitOfWorkSPF.cs:17`), `TMProductTypeGroupRepository.GetDataAll()` → `List<TMProductTypeGroupEntity>`,
  entity `ProductTypeGroupCode` `[Key]` + `ProductTypeGroupName` (both non-null). Key ⇒ unique ⇒ `ToDictionary` safe.
- Change (1 file, `Services/DashboardMoveLicenseService.cs`): **removed** the `WEAPON_TYPES` array; **rewrote**
  the `SearchFilter()` weapon-type build to `var ptgGroups = await _uowSPF.TMProductTypeGroupRepo.GetDataAll();`
  → dict `code→ProductTypeGroupName` → emit configured codes **in config order**, WHERE-IN (code not in DB ⇒
  skipped, no error/log). Config plumbing (appsettings key / `ConfigurationsModel` / `Program.cs` / `IOptions`
  injection / `_weaponTypeCodes`) kept verbatim.
- Left `using static ...ConstantSPF;` per the TASK instruction ("leave unless build flags it") — build didn't
  flag it. Flagging it as now-possibly-orphaned rather than silently removing.
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** Grep: `WEAPON_TYPES`/`labelByCode` gone; new
  build uses `TMProductTypeGroupRepo.GetDataAll()` + `nameByCode`. Static trace confirms config drives codes+order,
  DB drives labels, unknown codes skipped. Literal label text now depends on live DB rows — a live capture is a
  DATA REQUEST if needed for sign-off (weapon-type branch behavior itself is conclusive from code).
- @Sober: **TASK-002 back to REVIEW.** Please check the DB-label swap against the revised SPEC-002. One flag:
  `using static ...ConstantSPF;` may now be orphaned — left in place per your task note; say the word if you want it pruned.

## [14:39] Sober (SA Lead)
- Reviewed the TASK-002 REWORK against revised SPEC-002 by reading the actual code
  (`DashboardMoveLicenseService.cs`), not the notes:
  - Weapon-type build (L62-69): `TMProductTypeGroupRepo.GetDataAll()` → `nameByCode`
    (`ProductTypeGroupCode`→`ProductTypeGroupName`) → configured codes **in config order**, `ContainsKey`
    = WHERE-IN (code not in DB ⇒ skipped, no error/log), `Value=code`, `Label=DB Thai name`. Matches
    SPEC-002 + every REQ-002 acceptance criterion.
  - `WEAPON_TYPES` array removed (L35-39 = only `BUYER_GROUP_MAP` now); `_weaponTypeCodes` comment
    updated. Config plumbing kept; DI unchanged; no data filter; other dropdowns untouched. Build 0 errors.
  - Accepted the brownfield deferral of a live capture — the DB-label behaviour is conclusive from code.
- **Orphaned-using check (Jason's flag):** grepped the file — the only two `ProductTypeGroupCode` hits
  (L65, L148) are an **entity property** and a **repo method name**, not `ConstantSPF` members, and no
  other `ConstantSPF` symbol is used. So `using static ...ConstantSPF;` IS now orphaned. It's a
  warning-level nit; **recommended** pruning it next time the file is touched — **not** worth a REWORK
  cycle on its own. Noted in TASK-002 Review.
- **Verdict: DONE.** TASK-002 → DONE, SPEC-002 → DONE. All tasks of SPEC-002 DONE → **REQ-002 → SPEC_DONE**.
- @Porter: **REQ-002 ready for your acceptance check.** For the stakeholder summary (Thai): the
  weapon-type dropdown is now driven by `appsettings.json` → `Configurations:MoveLicenseWeaponTypeCodes`
  (currently `["PTG01","PTG02","PTG03"]`); labels come from the DB (`T_M_PRODUCT_TYPE_GROUP`); editing the
  list (add/remove/reorder) is a config-only change (app restart), and a code not in the DB simply won't
  show. No other dashboard behaviour changed.
- @Jason: clean rework, thanks. Optional tiny follow-up: drop the orphaned `using static ConstantSPF`.

## [15:55] Porter (PM)
- Acceptance check on REQ-002 (was SPEC_DONE; TASK-002 DONE after the DB-label rework).
  Independently verified the actual code + ran a build myself:
  - `DashboardMoveLicenseService.cs`: `WEAPON_TYPES` removed (grep=0); dropdown built from
    `_weaponTypeCodes` (config) with labels from `TMProductTypeGroupRepo.GetDataAll()` → `nameByCode`,
    `.Where(ContainsKey)` (WHERE-IN), config order.
  - `appsettings.json` = `["PTG01","PTG02","PTG03"]`; `ConfigurationsModel.MoveLicenseWeaponTypeCodes` present.
  - `dotnet build` (PM-run): **Build succeeded, 0 Error(s).**
  - Live literal-label capture deferred per brownfield rule — accepted (label-from-DB behaviour is
    conclusive from code).
- All REQ-002 acceptance criteria met → **REQ-002 = DELIVERED.** Wrote an Acceptance section into
  REQ-002; board updated; Blocked/waiting cleared.
- Sober's non-blocking nit noted (orphaned `using static ...ConstantSPF;` in that file) — left as a
  future trivial cleanup, not worth a rework cycle.
- @Sober @Jason: REQ-002 closed. Both dashboard REQs (001, 002) delivered. Good turnaround on the
  mid-flight design change.

## [16:30] Porter (PM)
- Stakeholder asked to replicate the move-license work on the License Book dashboard ("ทำแบบนี้ด้วย
  ที่ license book"). Confirmed scope = **both** REQ-001-style key rename AND REQ-002-style config
  dropdown, with **DB-sourced labels**.
- Surveyed License Book code: same 3 endpoints (search-filter/chart/table); response keys are
  camelCase/snake_case mix (rename applies); hardcoded dropdown = `BOOK_TYPES` (FORM_ID→"อ.x") in
  `DashboardLicenseBookService.cs:34`; candidate DB label source `TRLicenseFormRepo`/`T_R_LICENSE_FORM`.
- Drafted two REQs:
  - REQ-003 — License Book key rename (mirror of REQ-001; same shared-class HARD CONSTRAINT; pivot
    summary `a8_paid`/… flagged as computed).
  - REQ-004 — License Book book-type dropdown config-driven + DB label (mirror of REQ-002; label from
    DB by FORM_ID; value-vs-label filter-contract detail flagged).
- **Then stakeholder said: "รอ ไม่ต้องทำแล้ว พักก่อน" → PAUSED.** Set REQ-003 & REQ-004 to
  **DRAFT (ON HOLD)** on the board and in the files; NOT handed to SA. Drafts kept so no analysis is
  lost when resumed. REQ-004 Q1 (which FORM_IDs initially) remains unanswered — ask on resume.
- @Sober @Jason: **do NOT pick up REQ-003/REQ-004** — on hold per stakeholder. Board otherwise clear.

## [17:10] Sober (SA Lead)
- REQ-003 & REQ-004 resumed (stakeholder "ลงมือทำเลย"). Picked both up, set IN_SPEC. Read the real
  License Book code before designing: `DashboardLicenseBookModel.cs`, `DashboardLicenseBookController.cs`,
  `DashboardLicenseBookService.cs`, DTO `DashboardLicenseBookQueryResult.cs`, SQL
  `TTLicenseRepository.GetLicenseBookDashboard`, plus `TRLicenseFormEntity/Repository` + `IUnitOfWorkSPF`.
- **No DATA REQUEST needed for either** — columns trace from the dashboard SQL (`L.LICENSE_NO`,
  `L.FORM_ID`, `L.TRADER_NAME`, `P.PAID_STATUS`, `P.AMOUNT`, …); and the REQ-004 label column is settled
  by the in-repo data dictionary (`_docs/db-schema/DATADIC.md:397-398`): `FORM_CODE` = "รหัสแบบฟอร์ม
  (อ.2, อ.2-1)" = the "อ.x" code (matches current display); `LICENSE_NAME` = long name.
- **SPEC-003 (ACTIVE)** — key rename mirroring SPEC-001; full §A–E mapping in the spec. Reused the
  confirmed R1–R4 rules + the same shared-class HARD CONSTRAINT (`DashboardChartData*`/`DropdownDDL*`
  inner keys untouched — `DashboardChartData` is shared with move-license). Q1 answer: **keep the pivot
  `a8_paid…a17_unpaid` as-is** (computed cells, no column). Q2 (SA judgment): mix-bar `name`→`trader_name`.
- **SPEC-004 (ACTIVE)** — config dropdown + DB label mirroring SPEC-002, with two decisions:
  1. **value = FORM_ID** (not the "อ.x" label) → the filter in `BuildFilteredRows` changes to compare
     FORM_ID (allowed by REQ-004's consistency constraint).
  2. **label = `FORM_CODE`** from `TRLicenseFormRepo.GetDataByFormId` (WHERE-IN via null-skip; ≤4 point
     lookups, no DAL change). `BOOK_TYPES` **kept** — still used by chart/table labels (out of REQ-004
     scope). Config `LicenseBookFormIds = [8,10,16,17]` (Porter-confirmed: keep all four).
- **Sequencing:** TASK-003 (rename, model) → TASK-004 (service+config). Same dashboard, different files;
  the `form_id` request contract = SPEC-003 key + SPEC-004 value, so they must land together for the FE.
- Wrote SPEC-003/004 + TASK-003 (TODO) + TASK-004 (TODO, depends on TASK-003). Board updated.
- @Porter: two non-blocking confirmations — REQ-003 Q1/Q2 (keep pivot; `name`→`trader_name`) and
  REQ-004 Q2 (label=`FORM_CODE` vs `LICENSE_NAME`; value=FORM_ID; FE sends FORM_IDs in `form_id`).
  Jason can implement meanwhile; all are ≤1-line adjustments if the stakeholder differs.
- @Jason: **TASK-003 is startable now** (pure `[JsonProperty]` rename per SPEC-003 §A–E). Then TASK-004
  (config + DB label + value=FORM_ID filter per SPEC-004). Don't remove `BOOK_TYPES`; keep chart/table
  labels as-is. `dotnet build` + fill Implementation Notes + REVIEW each. I'll review against the specs.

## [16:50] Porter (PM)
- Stakeholder resumed License Book: "ลงมือทำเลย". Lifted the ON HOLD on REQ-003 & REQ-004 →
  **READY_FOR_SA**.
- Answered REQ-004 Q1 with a default (stakeholder gave no trim): keep all four FORM_IDs
  **8, 10, 16, 17** in order; config makes it changeable later. Updated REQ-004 Requirement #2 + Q1.
- @Sober: please pick up **REQ-003** (License Book key rename — mirror of REQ-001; same shared-class
  HARD CONSTRAINT; pivot summary `a8_paid`/… flagged) and **REQ-004** (book-type dropdown
  config-driven + DB label — mirror of REQ-002; candidate label source `TRLicenseFormRepo`/
  `T_R_LICENSE_FORM`; value-vs-label filter-contract detail flagged). Both touch the same files —
  sequence to avoid conflicts. Raise DATA REQUEST / questions here and I'll relay in Thai.

## [--:--] Jason (BE)
- Picked up TASK-003 (License Book key rename). Set IN_PROGRESS → REVIEW. Read SPEC-003 §A–E + the real
  model `Models/Dashboard/DashboardLicenseBookModel.cs` before editing.
- **JSON-key rename only**, 1 file. §A request body (4), §B dropdown containers (2), §C chart containers
  (5), §D mix-bar `name`→`trader_name` (kept `a8_paid`…`a17_unpaid`), §E table row (7 renamed, `key`/
  `amount` kept). C# property names, comments, shared classes (`DashboardChartData*`, `DropdownDDL*`)
  untouched; `form_id` values unchanged (SPEC-004 flips those).
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** Grep: no old License-Book keys remain;
  `a8_paid`…`a17_unpaid` = 8 kept; bare `name` gone; `trader_name` present twice (§D+§E). Details in
  TASK-003 Implementation Notes. Live-DB capture deferred per brownfield (deterministic rename).
- **Holding TASK-004** — it's `Depends on: TASK-003` and touches the SAME file + the service. Rather than
  entangle changes while TASK-003 is unreviewed, I'll start TASK-004 once TASK-003 clears review (or you
  say go). If you'd prefer I proceed now to land both together, say so and I'll pick it up immediately.
- @Sober: **TASK-003 ready for review.** Please check the §A–E mapping vs SPEC-003. TASK-004 held on the
  dependency per protocol — ping me (or set TASK-003 DONE) and I'll take TASK-004 straight away.

## [17:40] Sober (SA Lead)
- Reviewed TASK-003 against SPEC-003 by reading the actual `DashboardLicenseBookModel.cs` line-by-line —
  all §A–E keys match exactly: §A request (4), §B dropdown (2), §C chart containers (5), §D mix-bar
  `name`→`trader_name` + `a8_paid…a17_unpaid` (8) kept, §E table (7 renamed, `key`/`amount` kept). No
  shared-class (`DashboardChartData*`/`DropdownDDL*`) key touched; C# property names preserved (build
  green confirms binding); build 0 errors. Accepted the brownfield deferral of the live capture.
- **Verdict: DONE.** TASK-003 → DONE, SPEC-003 → DONE. Its only task is done → **REQ-003 → SPEC_DONE**.
- **TASK-004 is now unblocked** (its `Depends on: TASK-003` is satisfied). @Jason: go ahead — implement
  TASK-004 per SPEC-004 (config `LicenseBookFormIds=[8,10,16,17]`, DB label from `TRLicenseFormRepo`
  `FORM_CODE`, **value=FORM_ID** + fix the `BuildFilteredRows` filter to compare FORM_ID, **keep
  `BOOK_TYPES`** for chart/table labels). Q2 stays non-blocking.
- @Porter: **REQ-003 ready for your acceptance check.** Stakeholder summary (Thai): License Book
  response/request keys now match DB columns in snake_case (same as the move dashboard) — pure key
  rename, values unchanged. Note: the `form_id` request field's *values* flip to FORM_IDs when REQ-004
  lands, so the frontend should adopt the new keys (REQ-003) and the new `form_id` values (REQ-004)
  together.

## [--:--] Jason (BE)
- TASK-003 DONE (Sober) → picked up TASK-004 (unblocked). Set IN_PROGRESS → REVIEW.
- Verified DB source before editing: `IUnitOfWorkSPF.TRLicenseFormRepo.GetDataByFormId(int)` →
  `TRLicenseFormEntity?` (`[Table("T_R_LICENSE_FORM")]`, `FormId`/`FormCode`/`LicenseName`). No new wiring.
- Implemented SPEC-004, 4 files (config plumbing mirrors TASK-002):
  1. `appsettings.json` — `Configurations.LicenseBookFormIds = [8,10,16,17]` (Dev has no Configurations block).
  2. `ConfigurationsModel.cs` — `List<int> LicenseBookFormIds`.
  3. `Program.cs` — one mapping line in the existing `Configure<ConfigurationsModel>` block.
  4. `DashboardLicenseBookService.cs` — inject `IOptions<ConfigurationsModel>` → `_bookTypeFormIds`;
     rewrote the book-type dropdown build to loop config FORM_IDs in order, `GetDataByFormId`, skip nulls
     (WHERE-IN), **value=FORM_ID / label=DB FORM_CODE**; fixed `BuildFilteredRows` filter to compare
     FORM_ID (`InList(req.BookTypes, r.FormId?.ToString())`). **Kept `BOOK_TYPES`** — still used by
     chart/table labels (out of scope).
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** appsettings.json valid (JSONC parser):
  `LicenseBookFormIds=[8,10,16,17]`. Grep: BOOK_TYPES kept (chart L95/L107, table L132); dropdown DB-sourced
  value=FORM_ID; filter compares FormId. Static trace confirms config drives FORM_IDs+order, DB drives labels,
  missing FORM_IDs skipped. Literal FORM_CODE text is DB-dependent → live capture is a DATA REQUEST if needed.
- **Non-blocking (SPEC-004 Q2):** implemented label=FORM_CODE, value=FORM_ID as specified; LICENSE_NAME is a
  one-word swap if stakeholder differs. Frontend must send FORM_IDs in `form_id` (SPEC-003 keys + SPEC-004
  values land together).
- @Sober: **TASK-004 ready for review.** With this, the whole License Book pair (TASK-003 keys + TASK-004
  dropdown/value) is complete. Please check against SPEC-004; confirm the value=FORM_ID filter contract.

## [18:05] Sober (SA Lead)
- Reviewed TASK-004 against SPEC-004 by reading the actual code:
  - Config plumbing verified across all 4 files (`appsettings.json:43`, `ConfigurationsModel.cs:22`,
    `Program.cs:213-214`, service ctor `IOptions` → `_bookTypeFormIds`).
  - Dropdown (`DashboardLicenseBookService.cs:65-72`): loops config FORM_IDs in order →
    `TRLicenseFormRepo.GetDataByFormId` → `if null continue` (WHERE-IN) → `value=FORM_ID`,
    `label=FormCode`. ✓
  - Filter (`BuildFilteredRows` L153): `InList(req.BookTypes, r.FormId?.ToString())` — matches FORM_ID,
    consistent with the new value. ✓
  - `BOOK_TYPES` kept (L35-38) for chart grouping (L95) + table `form_id_name` — chart/table label source
    unchanged. No data filter added; DI unchanged; build 0 errors.
- **Verdict: DONE.** TASK-004 → DONE, SPEC-004 → DONE. All tasks of SPEC-004 done → **REQ-004 → SPEC_DONE**.
- @Porter: **REQ-004 ready for your acceptance check.** Stakeholder summary (Thai): the License Book
  book-type dropdown is now driven by `appsettings.json:Configurations.LicenseBookFormIds` (currently
  `[8,10,16,17]`); labels come from the DB (`T_R_LICENSE_FORM.FORM_CODE`, the "อ.x" code); the dropdown's
  **value is now the FORM_ID** (8/10/16/17), and the table's filter matches on FORM_ID accordingly. So —
  together with REQ-003 — the frontend must send the `form_id` field with **FORM_IDs** (e.g. `["8","10"]`),
  not `"อ.8"`. Config-only to change the list later.
- **Both License Book REQs are through SA** (REQ-003 & REQ-004 SPEC_DONE). @Jason: clean work across all
  four — no rework on 003/004. Nothing pending on my side; waiting on Porter's acceptance + the two
  non-blocking stakeholder confirmations (pivot-keys / `name`→`trader_name`; FORM_CODE vs LICENSE_NAME).

## [--:--] Porter (PM) — resumed 2026-07-20
- Resumed after a lost session. Re-read PROTOCOL, board, log, REQ/SPEC/TASK 003-004 per startup ritual.
- **Acceptance check REQ-003** (License Book key rename) vs its AC: all response/request keys renamed to
  DB snake_case per SPEC-003 §A–E; `a8_paid…a17_unpaid` kept; shared classes untouched; no value/logic
  change; `dotnet build` 0 errors (Jason evidence, Sober line-by-line review). Live-DB capture deferred
  (brownfield, deterministic `[JsonProperty]` rename). **Criteria met → REQ-003 DELIVERED.**
- **Acceptance check REQ-004** (config-driven book-type dropdown) vs its AC: dropdown driven by
  `appsettings.json:Configurations.LicenseBookFormIds=[8,10,16,17]`; DB-sourced label (`T_R_LICENSE_FORM.
  FORM_CODE`); value=FORM_ID; `BuildFilteredRows` matches FORM_ID; `BOOK_TYPES` kept for chart/table;
  config-only to change; build 0 errors (Sober verified actual code). **Criteria met → REQ-004 DELIVERED.**
- Board: REQ-003 & REQ-004 → DELIVERED. All four REQs (001-004) now delivered.
- **Relaying to stakeholder (Thai)** the two non-blocking confirmations + the frontend hand-off note:
  (1) REQ-003 keep pivot `a8_paid…` as-is + `name`→`trader_name`; (2) REQ-004 label=`FORM_CODE` vs
  `LICENSE_NAME`, value=FORM_ID; (3) FE must send `form_id` with FORM_IDs (`["8","10"]`), not `"อ.8"`,
  and adopt the new snake_case keys (REQ-003+004 land together). If stakeholder picks `LICENSE_NAME`,
  it's a one-word follow-up REQ — implemented as `FORM_CODE` per spec.
- @Sober @Jason: clean work on 003/004, no rework. Nothing pending for the team; awaiting stakeholder
  answers to the non-blocking confirmations only.

## [--:--] Porter (PM) — 2026-07-20
- Stakeholder answered the three non-blocking confirmations: (1) ok, (2) correct, (3) ok.
- Recorded answers in REQ-003 §Questions and REQ-004 §Questions (`> answer (Porter, 2026-07-20 …)`):
  - REQ-003: keep pivot `a8_paid…a17_unpaid` as-is + `name`→`trader_name` — both approved.
  - REQ-004: label=`FORM_CODE` (not `LICENSE_NAME`), value=FORM_ID, frontend sends FORM_IDs — confirmed.
  - FE hand-off note acked by stakeholder.
- Board: cleared the Blocked/waiting confirmations; added a "Resolved confirmations (2026-07-20)" note.
  All four REQs DELIVERED, nothing pending for any role. Everything shipped matches stakeholder intent —
  no rework, no follow-up REQ.
- @Sober @Jason: License Book work fully signed off. Idle until the next requirement from the human.

## [--:--] Porter (PM) — 2026-07-20 (new requirement)
- Stakeholder pointed at the frontend `/officer/dashboard-move-a10` ("ยอดการขนย้าย/ส่งมอบอาวุธหรือ
  วัตถุ ตามแบบ อ.10") and asked to build it ("filter เหมือนๆ กับ move license").
- Inspected the live test frontend (read-only, not logged in): page runs on **mock** (no dashboard API
  call; API base = `/didapicenter/api/v1/officer/`). Captured filters/charts/table/cascades to
  `project-docs/dashboard-move-a10-frontend-capture.md`.
- Clarified with stakeholder: **NEW dashboard, separate** from move-license; deliverable = **full
  working backend**. Key difference: this dashboard is *actual movement/delivery per transaction*
  (columns วันที่ขนย้าย / ครั้งที่ขนย้าย / จำนวนที่ขนย้าย; primary date filter = move date), whereas
  move-license was permitted amounts.
- Wrote **REQ-005-dashboard-move-a10-backend.md**, status READY_FOR_SA. Reuse the move-license pattern;
  keys DB-column snake_case from the start (per current convention) so no later rename REQ.
- Flagged the main risks as SA Questions (likely DATA REQUESTs back to me): Q1 source of actual-movement
  data (was a 0-gap on move-license), Q2 transport/move-type dropdown sources (were empty on
  move-license), Q3 exact frontend column contract (2 "ประเภทการขออนุญาต" headers).
- @Sober: please pick up **REQ-005** — write SPEC + TASKs. Raise DATA REQUESTs (Q1/Q2) via @Porter and
  I'll relay to the stakeholder in Thai with ready-to-run SQL.

## [--:--] Sober (SA Lead) — 2026-07-20
- Picked up REQ-005, set IN_SPEC. Read the frontend-capture doc + investigated the schema before asking:
  move-license code (reuse pattern), `TTLicenseMoveEntity`, DATADIC (`T_T_LICENSE_MOVE`,
  `MOVE_REQUEST_TYPE`, `TRANSPORT_TYPE_CODE`), and the SPF DAL for move/transport entities.
- **Designed everything derivable from code** (SPEC-005): the 6 endpoints, filters, 3 cascades, and the
  license-side table columns all mirror the delivered move-license — no data gap there.
- **Two genuine gaps → DATA REQUESTs (investigated first, not guessed):**
  - **DATA REQUEST 1 (CRITICAL).** The actual movement data (วันที่ขนย้าย / ครั้งที่ขนย้าย /
    จำนวนที่ขนย้าย) — the whole point of this dashboard — **has no source in the known schema.**
    `T_T_LICENSE_MOVE` is **1 row per license** (authorization: addresses, MOVE_REQUEST_TYPE,
    START/END permitted window, CONDITION1..5) — no move seq / moved-qty / actual-move-date column; no
    `_MOVE_DTL` or delivery table in `DID_SPF`. Same gap that made move-license return `moveQty=0`.
    → @Porter: relay to stakeholder (Thai) with the candidate-inventory SQL in REQ-005/SPEC-005. **Blocks
    TASK-006** (table movement columns + move-date filter + chart measure).
  - **DATA REQUEST 2 (small).** ประเภทการขนย้าย (`TRANSPORT_TYPE_CODE`) → FK to `T_R_TRANSPORT_TYPE`,
    which exists but is **not in the SPF DAL**. BE will add the entity (`spf-add-entity`) — confirm its
    columns. **Blocks TASK-007.**
- **Resolved without a DATA REQUEST:** ประเภทการขออนุญาต (`MOVE_REQUEST_TYPE`) = documented 0/1 enum
  (DATADIC) → fixed map (improves on move-license's empty dropdown). Q3 (keys) → define by the delivered
  snake_case convention (no later rename); table cols 5/6 = `move_request_type_name` +
  `transport_type_code_name`.
- Split into 3 tasks so BE isn't blocked on the whole thing:
  - **TASK-005 (UNBLOCKED, TODO)** — scaffold controller+models+service+DI + search-filter + 3 cascades +
    the MOVE_REQUEST_TYPE enum dropdown. Mirrors move-license; chart/table stubbed with `// TASK-006` seams;
    the 3 movement keys declared on the table row. **@Jason: start this now.**
  - **TASK-006 (BLOCKED on DATA REQUEST 1)** — movement chart/table with the real source.
  - **TASK-007 (BLOCKED on DATA REQUEST 2)** — T_R_TRANSPORT_TYPE entity + ประเภทการขนย้าย dropdown.
- Board updated: REQ-005 IN_SPEC (partial), TASK-005 TODO, TASK-006/007 BLOCKED, 2 DATA REQUESTs in
  Blocked/waiting. @Porter: DATA REQUEST 1 is the critical-path item — please get it to the stakeholder first.

## [--:--] Porter (PM) — 2026-07-20 (relaying Sober's DATA REQUESTs)
- Read Sober's SA response on REQ-005 + board. SPEC-005 written; TASK-005 (scaffold + search-filter +
  cascades) unblocked & started by Jason. TASK-006/007 blocked on data.
- Collected the 2 open DATA REQUESTs and **relayed to the stakeholder in Thai** (with ready-to-run SQL):
  - **DATA REQUEST 1 (critical):** source of per-move/delivery transaction data (วันที่ขนย้าย /
    ครั้งที่ขนย้าย / จำนวนที่ขนย้าย). `T_T_LICENSE_MOVE` = 1 row/license (authorization only), no detail
    table found. Blocks TASK-006 (movement table cols + move-date filter + chart measure).
  - **DATA REQUEST 2 (small):** confirm `T_R_TRANSPORT_TYPE` columns (code + Thai name) so BE adds the
    SPF entity for the ประเภทการขนย้าย dropdown. Blocks TASK-007.
- Asked the stakeholder to drop results into `project-docs/`. Awaiting answers.

## [--:--] Jason (BE)
- Picked up TASK-005 (scaffold dashboard-move-a10; the only unblocked REQ-005 task — TASK-006/007 wait on
  DATA REQUESTs). Set IN_PROGRESS → REVIEW. Read SPEC-005, TASK-005, the frontend capture doc, and the
  move-license controller/service/interface/model as templates before writing.
- Created 4 new files mirroring `DashboardMoveLicense*`: `Controllers/DashboardMoveA10Controller.cs`
  (6 officer endpoints), `Models/Dashboard/DashboardMoveA10Model.cs`, `Services/DashboardMoveA10Service.cs`,
  `Services/Interfaces/IDashboardMoveA10Service.cs`. Additive-only to 2 existing files:
  `TextConstant.cs` (+1 SwaggerTag `DASHBOARD_MOVE_A10`), `Program.cs` (+1 `AddScoped` DI line).
- Delivered now: SearchFilter + 3 cascades (verbatim copy of move-license); ประเภทการขออนุญาต dropdown from
  the fixed 0/1 enum map; weapon-type from `TMProductTypeGroupRepo.GetDataAll()`. Left clean seams:
  ประเภทการขนย้าย dropdown empty (`// TASK-007`, no invented source), chart/table return empty envelopes
  (`// TASK-006`); table row declares `move_date`/`move_seq_no`/`moved_qty`.
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** Grep confirms 6 endpoints, 4 new files,
  movement seam keys, TASK-006/007 markers. Shared classes + other dashboards untouched. Live capture deferred
  (brownfield; cascade logic is a verbatim copy of shipped move-license paths).
- **One flagged deviation (not silent) — @Sober please confirm:** I omitted `move_qty` from the A10 table row
  (move-license has it as a 0-placeholder for "actual moved qty") because the new `moved_qty` *is* that field and
  the FE capture shows only one such column — carrying both = two identical-meaning keys. If you want a strict
  verbatim mirror, it's a one-line add back. Details in TASK-005 `## Questions`.
- @Sober: **TASK-005 ready for review.** TASK-006 (chart/table real data) and TASK-007 (transport-type dropdown)
  remain BLOCKED on DATA REQUEST 1 & 2 respectively — nothing more I can do on REQ-005 until those land in
  project-docs/. I'm idle after this unless you have other work.

## [--:--] Porter (PM) — 2026-07-20 (DATA REQUEST results)
- Stakeholder ran the SQL; saved raw results to `project-docs/data-req-2026-07-20-move-a10-results.md`.
- **DATA REQUEST 2 ANSWERED** → `T_R_TRANSPORT_TYPE` = `TRANSPORT_TYPE_CODE` (code/value) +
  `TRANSPORT_TYPE_NAME` (Thai label). Answered in REQ-005 §Questions. **TASK-007 unblocked** (BLOCKED→TODO).
  @Jason: build the entity with those two columns.
- **DATA REQUEST 1 progressed, not closed.** Confirmed `T_T_LICENSE_MOVE` is authorization-only (no
  move date/seq/qty). Inventory surfaced the **`INFORM_MOVE` family** (`T_T_INFORM_MOVE` +
  `T_T_INFORM_MOVE_DTL` + `V_INFORM_MOVE_DTL(_LOT)` + `RPT_*`) as the likely actual-movement source.
  Opened **DATA REQUEST 1b**: asked the stakeholder (Thai) for the column lists of `T_T_INFORM_MOVE`,
  `T_T_INFORM_MOVE_DTL`, `V_INFORM_MOVE_DTL_LOT` so Sober can confirm date+seq+qty + license link and
  pick the grain. TASK-006 stays BLOCKED until then.
- @Sober: with TASK-005 in REVIEW, please (a) confirm the INFORM_MOVE family is the intended อ.10
  movement source once columns arrive, and (b) note TASK-007 is now unblocked for Jason.

## [--:--] Porter (PM) — 2026-07-20 (DATA REQUEST 1 resolved)
- Stakeholder ran the round-2 SQL; saved INFORM_MOVE column lists + a candidate mapping to
  `project-docs/data-req-2026-07-20-move-a10-results.md` (§ DATA REQUEST 1b).
- **Movement source RESOLVED = the INFORM_MOVE family** (การแจ้งขนย้าย), not the license tables:
  `T_T_INFORM_MOVE_DTL` carries **MOVE_DATE / MOVE_SEQ / QUANTITY** (+ ALLOWED_QUANTITY, REF_LICENSE_NO
  → อ.10, PRODUCT_*, BUYER_*, QUANTITY_UNIT_ID); header `T_T_INFORM_MOVE` has TRADER_ID /
  INFORM_REQUEST_TYPE; `V_INFORM_MOVE_DTL_LOT` = ready-joined view.
- Answered REQ-005 §Questions (all DATA REQUESTs now closed). **TASK-006 no longer blocked on data.**
- @Sober: this changes SPEC-005's data backbone (license tables → INFORM_MOVE). Please **revise SPEC-005**
  accordingly and hand TASK-006 to Jason. 3 design points left to you (non-blocking): อ.10 scoping via
  REF_LICENSE_NO, the row's move-type source, and grain (DTL vs _LOT view).
- Net board state for REQ-005: TASK-005 in REVIEW (Sober), TASK-007 unblocked (Jason), TASK-006 waiting
  on the SPEC-005 revision + TASK-005. No open items on Porter/stakeholder.

## [--:--] Sober (SA Lead) — 2026-07-20
- **Reviewed TASK-005 (scaffold) → DONE.** Verified the model + service: request/dropdown/table keys mirror
  move-license SPEC-001; date key = `move_date_range`; 3 movement fields declared; ประเภทการขออนุญาต =
  MOVE_REQUEST_TYPE 0/1 enum (improvement); ประเภทการขนย้าย empty with a `// TASK-007` seam; chart/table
  stubbed `// TASK-006`; shared classes untouched; build 0 errors. Answered Jason's Q: **agreed to drop the
  dead `move_qty` placeholder, keep `moved_qty`**; flagged `move_seq_no`→`move_seq` (matches column `MOVE_SEQ`)
  for TASK-006.
- **Revised SPEC-005 data backbone → the INFORM_MOVE family** (DATA REQUEST 1 answered):
  - Backbone = `T_T_INFORM_MOVE_DTL` (grain = per moved item) + header `T_T_INFORM_MOVE`. Chose the base
    tables over `V_INFORM_MOVE_DTL_LOT` (the view is per-*lot* → would multiply `QUANTITY`).
  - Movement keys finalized: `move_date`←MOVE_DATE (also the date filter), `move_seq`←MOVE_SEQ,
    `moved_qty`←DTL.QUANTITY (actual), `quantity`←ALLOWED_QUANTITY (permitted; keeps move-license key parity).
  - **Resolved the 3 residual design points from code (no new DATA REQUEST):** dest region/province, row
    move-type (`TRANSPORT_TYPE_CODE`), and อ.10 scoping all come from a **license back-join on
    `REF_LICENSE_NO`** (`T_T_LICENSE`/`T_T_LICENSE_MOVE`/`V_PROVINCE`/`T_T_LICENSE_DTL`, `+ FORM_ID=10`) —
    mirroring how move-license sourced them. Trader/unit/buyer-group via `T_M_*` joins.
  - Flagged 4 assumptions to verify at build/test (not blockers): REF_LICENSE_NO↔LICENSE_NO join;
    INFORM_REQUEST_TYPE == 0/1 enum; FORM_ID=10 scoping; the table has no address columns (scaffold's
    address block is harmless extra — only dest region/province are needed, for filtering).
- **Expanded + unblocked both remaining tasks:**
  - **TASK-006 (TODO)** — add the SPF DAL entities/repo + `GetMoveA10Dashboard` query (joins per SPEC-005)
    + implement `TableData`/`ChartData` + move-date filter + rename `move_seq_no`→`move_seq`. Depends on TASK-005 (done).
  - **TASK-007 (TODO)** — add `T_R_TRANSPORT_TYPE` entity (`TRANSPORT_TYPE_CODE`+`TRANSPORT_TYPE_NAME`,
    confirmed) + wire the ประเภทการขนย้าย dropdown + table `transport_type_code_name`.
- Board: REQ-005 IN_SPEC (data resolved), TASK-005 DONE, TASK-006/007 TODO, no blocked items.
- @Jason: both tasks are startable. TASK-007 is small/independent; TASK-006 is the meaty one (new DAL query
  on INFORM_MOVE). Report back if any of the 4 SPEC-005 assumptions doesn't hold on first run — don't patch
  semantics silently. @Porter: nothing pending on your side.

## [--:--] Jason (BE)
- TASK-005 DONE (Sober; agreed to drop move_qty). DATA REQUEST 1&2 answered → TASK-006/007 unblocked.
  Did **TASK-007** first (no dependency; TASK-006 consumes its repo). Set IN_PROGRESS → REVIEW.
- Used the `spf-add-entity` skill. Added SPF DAL `T_R_TRANSPORT_TYPE` (natural-key ref table, mirrors
  TRLicenseForm*): `Entities/TRTransportTypeEntity.cs` (`[Key] TRANSPORT_TYPE_CODE` int + `TRANSPORT_TYPE_NAME`
  + audit) and `Repositories/TRTransportTypeRepository.cs` (`GetDataAll()` + `GetDataByCode(int)`). Wired into
  all 3 spots: `IUnitOfWorkSPF` property, `UnitOfWorkSPF` lazy property, `resetRepositories()` TR group.
- Wired the ประเภทการขนย้าย dropdown in `DashboardMoveA10Service.SearchFilter()` (replaced the `// TASK-007`
  empty stub): `TRTransportTypeRepo.GetDataAll()` → `{ value = TRANSPORT_TYPE_CODE, label = TRANSPORT_TYPE_NAME }`,
  ordered by code. Updated the class doc note.
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** Grep confirms the repo in all 3 DAL spots +
  the dropdown now DB-sourced. Live capture deferred (brownfield; same GetDataAll→DropdownDDLItem shape as
  shipped weapon-type/book-type dropdowns).
- @Sober: **TASK-007 ready for review.** The table's `transport_type_code_name` resolution is TASK-006's job —
  the repo (`GetDataAll`/`GetDataByCode`) is ready for it to build the code→name dict.
- Next: **TASK-006** (INFORM_MOVE backbone chart+table) — depends on TASK-005 (done); will consume this repo.
  Checking SPEC-005 has the revised "Movement data backbone" join section before I start.

## [--:--] Jason (BE)
- Did **TASK-006** (dashboard-move-a10 real table + charts on the INFORM_MOVE backbone). Set IN_PROGRESS → REVIEW.
  Read SPEC-005's revised "Movement data backbone", the data-req results, and the move-license query DTO +
  `GetMoveLicenseDashboard` SQL as templates. Confirmed trader_name = `T_M_TRADER.TRADER_NAME`.
- SPF DAL added: entities `TTInformMoveEntity` (header) + `TTInformMoveDtlEntity` (detail, grain), DTO
  `DashboardMoveA10QueryResult`, repos `TTInformMoveRepository` (ctor) + `TTInformMoveDtlRepository`
  (`GetMoveA10Dashboard(moveDateStart, moveDateEnd)` — the full SPEC-005 join, move-date range in SQL,
  `L.FORM_ID=10` อ.10 scoping). Both repos wired into `IUnitOfWorkSPF`/`UnitOfWorkSPF` (property + reset).
- Service: `TableData`/`ChartData` now real — filter mirrors move-license (`InList`/`MatchEq`); movement fields
  `move_date`/`move_seq`/`moved_qty` from `MOVE_DATE`/`MOVE_SEQ`/`QUANTITY`; `quantity`=`ALLOWED_QUANTITY`;
  `transport_type_code_name` from TASK-007's `T_R_TRANSPORT_TYPE` dict; charts measure `SUM(moved_qty)`
  (`top5_by_buyer_unit` top-5, `by_buyer_group`, `by_trader`). Renamed model key `move_seq_no`→`move_seq`.
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** Grep confirms repos wired, backbone query used,
  real movement mapping, key rename, no stub left.
- **Flagged (not silent) 4 data-dependent assumptions** I can't confirm without a live DB (brownfield): (1)
  `REF_LICENSE_NO`↔`LICENSE_NO` match — highest risk (INNER join drops all if format diverges); (2)
  `INFORM_REQUEST_TYPE`==0/1 enum; (3) `L.FORM_ID=10` scoping (kept per SPEC); (4) `LDTL` join multiplicity on
  LICENSE_ID+PRODUCT_CODE could duplicate rows. All are in the SQL exactly as SPEC-005 specifies. Details in
  TASK-006 Implementation Notes.
- @Sober: **TASK-006 ready for review.** This is the one task whose correctness is genuinely data-dependent — a
  5–10 row live capture of `/dashboard-move-a10/table` (move-date range) would confirm assumptions 1–4 at once;
  raise it as a DATA REQUEST via @Porter if you want it before sign-off.
- **REQ-005 status:** all three tasks (005/006/007) now in REVIEW/DONE — the อ.10 movement dashboard backend is
  functionally complete pending your reviews. I'm idle after this unless there's more work.

## [--:--] Sober (SA Lead) — 2026-07-20 (TASK-006 & TASK-007 review)
- Reviewed both by reading the actual code (SQL/DTO/service/entity), not the notes:
  - **TASK-007 → DONE.** `TRTransportTypeEntity` (`TRANSPORT_TYPE_CODE`+`TRANSPORT_TYPE_NAME`) mirrors the
    proven `TRLicenseForm` natural-key pattern; repo wired into `IUnitOfWorkSPF`/`UnitOfWorkSPF`; the
    ประเภทการขนย้าย dropdown + TASK-006's code→name dict both use it. Low data risk. Build 0 errors.
  - **TASK-006 → DONE (code).** `GetMoveA10Dashboard` SQL matches SPEC-005's backbone exactly (DTL+header
    INNER, master LEFT joins, license back-join `LICENSE_NO=REF_LICENSE_NO` + `FORM_ID=10` → LM/V_PROVINCE/
    LDTL, move-date range in SQL); DTO aliases 1:1; service filters mirror move-license; `move_date`/
    `move_seq`/`moved_qty` from real columns; `quantity`=ALLOWED_QUANTITY; charts = `SUM(moved_qty)`;
    `move_seq_no`→`move_seq` done. Build 0 errors.
- **Key SA call:** REQ-005 is the FIRST task in this project whose correctness is genuinely **data-dependent**
  — static review can't confirm it (unlike the 001–004 renames/config). Jason correctly flagged 4 assumptions;
  the highest-risk is the **INNER join `LICENSE_NO = REF_LICENSE_NO`** (a string-format mismatch ⇒ **empty
  dashboard**, not a partial error). Per REQ-005 AC #6 this needs a live capture, so I raised **DATA REQUEST 3**
  (Porter → stakeholder hits `/dashboard-move-a10/table`+`/chart`, drops JSON in project-docs; confirm rows
  non-empty + movement fields populated + no dup rows + move_request_type_name resolves).
- Status: SPEC-005 DONE; TASK-005/006/007 DONE (code); **REQ-005 → SPEC_DONE, NOT DELIVERED until DATA
  REQUEST 3 confirms the data** (if a check fails, TASK-006 reopens for a targeted SQL fix). Board + REQ-005
  updated; DATA REQUEST 3 in Blocked/waiting.
- @Porter: please run DATA REQUEST 3 as the REQ-005 acceptance step. @Jason: nothing to do unless the capture
  fails an assumption — then it's a small TASK-006 SQL fix. Good work on flagging the assumptions rather than
  guessing.

## [--:--] Porter (PM) — 2026-07-20 (nudge "ไปเลย" → relay DATA REQUEST 3)
- Re-read board + log. REQ-005 code complete (TASK-005/006/007 DONE, reviewed by Sober, build 0 err);
  SPEC-005 revised to the INFORM_MOVE backbone. Only open item for my role = **DATA REQUEST 3**
  (REQ-005 acceptance gate — data-dependent, needs a live capture; brownfield, team can't self-run).
- **Relayed DATA REQUEST 3 to the stakeholder (Thai):** call `POST /dashboard-move-a10/table` + `/chart`
  with a `move_date_range` over a month known to have movements, drop the JSON in `project-docs/`, and
  confirm the 4 checks from Sober: (1) rows non-empty (the `REF_LICENSE_NO = LICENSE_NO AND FORM_ID=10`
  INNER join — highest risk), (2) `move_date`/`move_seq`/`moved_qty` populated (not 0), (3) no duplicated
  rows (LICENSE_DTL join), (4) `move_request_type_name` resolves (0/1 enum).
- REQ-005 stays SPEC_DONE until the capture confirms. If any check fails → TASK-006 REWORK. Ball on
  stakeholder; nothing else pending for the team.

## [--:--] Porter (PM) — 2026-07-20 (REQ-005 acceptance on live capture → REWORK)
- Stakeholder ran the DATA REQUEST 3 live capture (`/table`+`/chart`, move_date_range=2026-06). Saved +
  analyzed: `project-docs/data-req-3-2026-07-20-move-a10-live-capture.md`.
- **Acceptance NOT PASSED.** Core is solid: 7 rows (the `REF_LICENSE_NO=LICENSE_NO` INNER join works —
  top risk cleared), `move_date`/`move_seq`/`moved_qty` populated, `move_request_type_name` 0/1 enum
  resolves, charts SUM moved_qty correctly (31130 นัด, table↔chart consistent).
- **Confirmed bug (Sober's predicted #3):** duplicate rows — `key:6`==`key:7` (license 80/2569, P-0672,
  move_seq 3, moved_qty 0) from the `T_T_LICENSE_DTL` `LICENSE_ID+PRODUCT_CODE` join.
- **Blank columns flagged for triage:** `transport_type_code_name` (all), `authority_group_no(_name)`
  (all → by_buyer_group chart collapses to "ไม่ระบุ"), `expiry_date` (all), dest region/province for
  80/2569. Bug vs genuine DB gap — Sober/Jason to decide (targeted DATA REQUEST if data-side).
- **TASK-006 → REWORK; REQ-005 stays SPEC_DONE.** @Jason (via @Sober): dedupe first, then triage the
  four empties; re-run the same capture to confirm before Porter closes REQ-005 → DELIVERED.

## [--:--] Sober (SA Lead) — 2026-07-20 (REWORK triage)
- The live-capture gate did its job — it caught the dup-row defect I flagged, before REQ-005 shipped.
  Triaged all findings against the capture data; split into code-fixes vs data-source questions.
- **Two code fixes (Jason now, no data needed):**
  1. **Dup rows (confirmed bug):** the `LEFT JOIN T_T_LICENSE_DTL ON LICENSE_ID+PRODUCT_CODE` multiplies
     when a license has >1 DTL line for a product → replace with a **correlated scalar subquery**
     (`MAX(TRANSPORT_TYPE_CODE)`), which can't multiply rows.
  2. **`expiry_date` blank (mapping bug):** it came from the null denormalized `DTL.REF_LICENSE_EXPIRY_DATE`
     → source `issue_date`/`expiry_date` from the already-joined `T_T_LICENSE L` (`L.ISSUE_DATE`/`L.EXPIRY_DATE`).
- **Two data-source questions → DATA REQUEST 4 (routed via @Porter, don't guess):**
  3. **Buyer group (important — `by_buyer_group` chart is dead):** likely `T_T_INFORM_MOVE_DTL.BUYER_AUTHORITY_ID`
     is null → the `T_M_PRIMARY_BUYER_AUTHORITY` FK join misses. Asked for a populated-count + a sample join,
     and the real group source if the id is null.
  4. **ประเภทการขนย้าย:** the LDTL join matched (caused the dups) but `TRANSPORT_TYPE_CODE` is null → asked
     whether it's ever set for อ.10 lines or a genuine blank.
- **`80/2569` dest blank = real data** (no `T_T_LICENSE_MOVE` row for that license; LEFT join → null) — not a bug.
- Updated SPEC-005 ("Live-capture REWORK" section) + TASK-006 (REWORK, the 2 fixes + the 2 gated items) +
  board (TASK-006 REWORK code-fixes for Jason; DATA REQUEST 4 for Porter).
- @Jason: do the 2 code fixes now (dedup + expiry from L.*), build, set REVIEW — the buyer-group wiring waits
  on DATA REQ 4 #1. @Porter: please run DATA REQUEST 4 (2 quick column checks) so we can finish the buyer-group
  chart + confirm the transport-type gap. After both, re-run the same capture.

## [--:--] Porter (PM) — 2026-07-20 (relay DATA REQUEST 4)
- Read Sober's REWORK triage. Jason has 2 code-only fixes in flight (dedup via correlated scalar subquery;
  issue/expiry date from joined `T_T_LICENSE L`). `80/2569` dest blank = real data (no T_T_LICENSE_MOVE
  row) — not a bug. No Porter action on those.
- **Relayed DATA REQUEST 4 to the stakeholder (Thai)** with ready SQL — 2 column checks:
  (1) is `T_T_INFORM_MOVE_DTL.BUYER_AUTHORITY_ID` populated + does it join to
  `T_M_PRIMARY_BUYER_AUTHORITY`? (the `by_buyer_group` chart is dead) — if null, what's the real
  buyer-group source for movement rows? (2) is `T_T_LICENSE_DTL.TRANSPORT_TYPE_CODE` ever set for อ.10
  (FORM_ID=10), or a genuine blank?
- Asked stakeholder to drop results in project-docs/. Buyer-group wiring waits on #1; re-run the same
  capture after the fixes + this answer land.

## [--:--] Jason (BE)
- Handled TASK-006 REWORK (live capture failed acceptance: duplicate rows + blank expiry_date). Set
  IN_PROGRESS → REVIEW. Applied Sober's 2 triaged code fixes in `TTInformMoveDtlRepository.GetMoveA10Dashboard`:
  1. **Dedup:** dropped the `LEFT JOIN T_T_LICENSE_DTL LDTL` (LICENSE_ID+PRODUCT_CODE multiplication — the
     `key:6==key:7` dup, exactly assumption #4 I'd flagged). `transport_type_code` now via a correlated scalar
     subquery `(SELECT MAX(LDTL.TRANSPORT_TYPE_CODE) … )` → one value/row, grain strictly per-moved-item.
  2. **Dates:** `issue_date`/`expiry_date` now from the joined `T_T_LICENSE L` (`L.ISSUE_DATE`/`L.EXPIRY_DATE`),
     not the null `DTL.REF_LICENSE_ISSUE_DATE`/`REF_LICENSE_EXPIRY_DATE`.
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** Grep: LDTL join gone, scalar subquery present,
  dates sourced from L.
- **Left as-is per triage (not code bugs):** empty `transport_type_code_name` (C) + empty `authority_group_no(_name)`
  / `by_buyer_group` (D) → **DATA REQUEST 4** (Porter→stakeholder: is BUYER_AUTHORITY_ID populated? is
  TRANSPORT_TYPE_CODE ever set for อ.10?). Dest blank for `80/2569` (G) = real data. Didn't guess sources.
- @Sober: **TASK-006 back to REVIEW.** The 2 acceptance-blocking defects are fixed; a re-run of the same live
  capture should show dupes gone + expiry/issue populated. Buyer-group + transport-type remain gated on DATA
  REQUEST 4 — flag if you want me to wire anything once those columns are confirmed.

## [--:--] Porter (PM) — 2026-07-20 (DATA REQUEST 4 answers)
- Stakeholder ran the 3 diagnostics. Saved + analyzed:
  `project-docs/data-req-4-2026-07-20-buyergroup-transport.md`. Answered REQ-005 §Questions.
- **#2 ประเภทการขนย้าย → NOT a data gap.** `T_T_LICENSE_DTL.TRANSPORT_TYPE_CODE` for FORM_ID=10 =
  9407/13362 (~70%) populated. The capture blank was a resolution bug covered by Jason's rework
  (correlated subquery + T_R_TRANSPORT_TYPE). Keep the column; no data action.
- **#1 buyer group → join key WRONG (design gap, @Sober).** `BUYER_AUTHORITY_ID` 100% populated
  (12886/12886) but does NOT match `T_M_PRIMARY_BUYER_AUTHORITY.ID` (group+name null for all; IDs
  77/59/65/375). Have BUYER_NAME, not the group. move-license join doesn't apply to the movement flow.
  Routed to Sober: find the real FK target + group source from DATADIC/code, else DATA REQUEST 5.
  `by_buyer_group` chart stays "ไม่ระบุ" until resolved.
- Net: Jason's 2 code fixes (dedup + date) + transport-type resolution proceed; buyer-group wiring now
  waits on Sober. Re-run the same capture once fixes + buyer-group source land.

## [--:--] Jason (BE)
- Read the board + DATA REQUEST 4 results. Status check on REQ-005:
  - **Transport type (C):** DATA REQ 4 says `TRANSPORT_TYPE_CODE` is ~70% populated — not a data gap. My REWORK
    (correlated scalar subquery on `T_T_LICENSE_DTL` + `T_R_TRANSPORT_TYPE` dict) will now surface it. A re-run
    of the live capture should show `transport_type_code_name` populated on most rows. Nothing more from me here.
  - **Buyer-group (D):** DATA REQ 4 says `BUYER_AUTHORITY_ID` is 100% populated but does **NOT** match
    `T_M_PRIMARY_BUYER_AUTHORITY.ID` for the movement flow (IDs 77/59/65/375 → group/name null). My A10 SQL
    currently uses the move-license pattern `LEFT JOIN T_M_PRIMARY_BUYER_AUTHORITY BA ON BA.ID = DTL.BUYER_AUTHORITY_ID`
    — that's the join producing the null group. **This is the exact fix site** once the correct FK target /
    group source is known: swap that BA join + the `BUYER_GROUP_MAP` resolution. Per PROTOCOL I won't guess the
    real source — it's assigned to @Sober (find via DATADIC/code, else DATA REQUEST 5).
- **No BE-actionable task open.** TASK-001–005 + 007 DONE; TASK-006 in REVIEW (rework applied). The only open
  REQ-005 item (buyer-group source) sits with @Sober. Standing by — @Sober, ping me when you've identified the
  buyer-group FK/source and I'll wire it into `GetMoveA10Dashboard` + the service map (small, targeted change).

## [--:--] Sober (SA Lead) — 2026-07-20 (buyer-group source found + rework #1 review)
- **Reviewed Jason's rework #1 (dedup + date) — GOOD.** Read the SQL: LDTL join dropped, `TransportTypeCode`
  via correlated scalar subquery (no row multiplication); `issue_date`/`expiry_date` now from the joined
  `L.ISSUE_DATE`/`L.EXPIRY_DATE`. Both correct. (Transport-type = real data per DATA REQ 4; surfaces after dedup.)
- **Found the buyer-group source in DATADIC — no DATA REQUEST 5 needed.** There are TWO buyer masters:
  `T_M_PRIMARY_BUYER_AUTHORITY` (what move-license — and my SPEC — wrongly borrowed) and **`T_M_BUYER_AUTHORITY`**
  = "ข้อมูลหน่วยผู้ซื้อ **ใช้กับแบบ อ.10**" (DATADIC:90), which carries `AUTHORITY_GROUP_NO` (1=ทหาร/2=ตำรวจ/
  3=สมาคม/9=อื่นๆ). The move flow's FK (`T_T_REQUEST_MOVE.BUYER_AUTHORITY_ID`, DATADIC:911) targets
  `T_M_BUYER_AUTHORITY`. So `T_T_INFORM_MOVE_DTL.BUYER_AUTHORITY_ID` → `T_M_BUYER_AUTHORITY.ID` (explains the
  77/59/65/375 not matching PRIMARY). **My SPEC-005 mistake** — I mirrored move-license's PRIMARY join; corrected now.
- **TASK-006 → REWORK #2 (buyer master swap):**
  1. SQL 1-line swap `T_M_PRIMARY_BUYER_AUTHORITY BA` → `T_M_BUYER_AUTHORITY BA` (raw-SQL join; no entity) →
     `by_buyer_group` chart + `authority_group_no(_name)` populate.
  2. Add a `TMBuyerAuthority` SPF entity/repo (`spf-add-entity`) + point the SearchFilter buyer group+unit
     dropdowns at it (so the หน่วยผู้ซื้อ options match the table's `BUYER_NAME`).
- Verification folded into the planned re-run capture (confirms `BUYER_AUTHORITY_ID`↔`T_M_BUYER_AUTHORITY.ID`);
  only escalate to DATA REQ 5 if the chart still comes back null (low likelihood given DATADIC).
- Updated SPEC-005 (REWORK item 4 resolved), TASK-006 (REWORK #2), board. @Jason: **go** — small SQL swap +
  the buyer-authority entity/dropdowns; build + REVIEW; then @Porter re-runs the same capture to close REQ-005.

## [--:--] Jason (BE)
- TASK-006 REWORK #2 (buyer-group master swap — Sober resolved the FK from DATADIC: อ.10 uses
  `T_M_BUYER_AUTHORITY`, not `T_M_PRIMARY_BUYER_AUTHORITY`). Set IN_PROGRESS → REVIEW. 3 changes:
  1. SPF DAL: new `TMBuyerAuthorityEntity` (`T_M_BUYER_AUTHORITY`: `ID`/`AUTHORITY_GROUP_NO`/`AUTHORITY_NAME`,
     read-only plain `[Key]`) + `TMBuyerAuthorityRepository` (ctor); wired into `IUnitOfWorkSPF`/`UnitOfWorkSPF`
     (property + reset). Used `spf-add-entity`.
  2. SQL `GetMoveA10Dashboard`: 1-line join swap `T_M_PRIMARY_BUYER_AUTHORITY BA` → `T_M_BUYER_AUTHORITY BA`
     → `AUTHORITY_GROUP_NO` resolves → `by_buyer_group` chart + `authority_group_no(_name)` column populate.
  3. Service `SearchFilter()`: buyer-group + buyer-unit dropdowns now from `TMBuyerAuthorityRepo.GetAllAsync()`
     so options match the table's `BUYER_NAME`.
- **Verified:** `dotnet build` → **Build succeeded, 0 Error(s).** Grep: repo wired in all 3 DAL spots, SQL joins
  the correct master, dropdowns swapped. Rework #1 (dedup + dates) intact.
- @Sober: **TASK-006 back to REVIEW** (rework #2 done). All 3 acceptance defects now addressed in code
  (dup rows, blank dates, dead buyer-group chart). Re-running the same live capture should show: dupes gone,
  expiry/issue populated, `by_buyer_group` + `authority_group_no(_name)` populated, transport-type surfaced.
  If `BUYER_AUTHORITY_ID`↔`T_M_BUYER_AUTHORITY.ID` still misses on live data, that's the DATA REQUEST 5
  escalation (low likelihood per DATADIC) — ping me and I'll adjust.

## [--:--] Sober (SA Lead) — 2026-07-20 (TASK-006 rework #2 review → DONE)
- Reviewed the rework #2 code (entity + SQL + service): `TMBuyerAuthorityEntity`
  (`[Table("T_M_BUYER_AUTHORITY")]`, `Id`/`AuthorityGroupNo`/`AuthorityName`) ✓; SQL join swapped to
  `T_M_BUYER_AUTHORITY BA ON BA.ID = DTL.BUYER_AUTHORITY_ID` (repo L65) ✓; SearchFilter buyer group+unit
  dropdowns → `TMBuyerAuthorityRepo.GetAllAsync()` (service L83) ✓; UoW wired; build 0 errors. Rework #1
  (dedup + dates) intact.
- **Verdict: TASK-006 DONE (code).** All three live-capture defects (dup rows, blank dates, dead buyer-group
  chart) now have verified fixes; transport-type surfaces via the scalar subquery. Nothing left to guess.
- **REQ-005 → SPEC_DONE; final acceptance = the re-run live capture (Porter).** The only remaining unknown is
  data-only (does `BUYER_AUTHORITY_ID` match `T_M_BUYER_AUTHORITY.ID` on live data — strong DATADIC inference).
- @Porter: please **re-run the same capture** (`/table`+`/chart`, `move_date_range` 2026-06). Expect: no dup
  rows, `expiry_date` present, `by_buyer_group` + `authority_group_no(_name)` populated, `transport_type_code_name`
  on most rows. All pass → **REQ-005 DELIVERED**. Only if the buyer-group chart is still null → DATA REQ 5
  (Jason on standby for the 1-line adjust). Nothing pending on SA.

## [--:--] Porter (PM) — 2026-07-20 (nudge "work" → request re-run capture)
- Re-read board + log. All REQ-005 rework DONE + reviewed: Jason fixed dedup (correlated subquery),
  issue/expiry from `T_T_LICENSE L`, and swapped the buyer-group master to `T_M_BUYER_AUTHORITY` (Sober
  resolved the FK from DATADIC); build 0 errors; Sober verdict DONE. REQ-005 SPEC_DONE.
- **Asked the stakeholder (Thai) to re-run the same capture** (`/table`+`/chart`, move_date_range=2026-06)
  and confirm the 3 fixes landed: no dup rows, `expiry_date` present, `by_buyer_group` +
  `authority_group_no(_name)` populated, `transport_type_code_name` on most rows.
- All pass → Porter closes **REQ-005 → DELIVERED**. Only if buyer-group still null on live data →
  DATA REQUEST 5 (Jason on standby, low likelihood). Nothing pending on SA/BE.

## [--:--] Porter (PM) — 2026-07-20 (acceptance round 2 → 3/4 fixed, DATA REQ 5)
- Stakeholder re-ran the capture. Analysis: project-docs/data-req-5-2026-07-20-move-a10-recapture.md.
- ✅ **Dup rows fixed** (6 rows, 80/2569 seq3 once). ✅ **expiry_date fixed** (all rows). ✅ **Buyer-group
  chart fixed** — authority_group_no/_name resolve (T_M_BUYER_AUTHORITY); by_buyer_group = ทหาร 30330 +
  ไม่ระบุ 800, table↔chart consistent.
- ⚠️ **transport_type_code_name still blank** on the 2 captured licenses (81/2569, 80/2569). Inconclusive
  (DATA REQ 4: ~70% of อ.10 lines have a code). Raised **DATA REQUEST 5** (small SQL) to disambiguate real-
  blank vs resolution bug. NULL ⇒ accept; populated ⇒ @Jason 1-line.
- Minor (@Sober, non-blocking): buyer-group code "0" (foreign buyer, row 6) has no label in the 1/2/3/9 map.
- REQ-005 held at SPEC_DONE pending DATA REQ 5. All primary movement features now verified on live data.

## [--:--] Porter (PM) — 2026-07-20 (DATA REQ 5 result → transport-type is a bug)
- DATA REQ 5 result: `81/2569 P-0048→0`, `80/2569 P-0672→3` AND `→1`. **Codes exist → the blank
  `transport_type_code_name` is a CODE BUG, not missing data.** Acceptance check #4 fails.
- Diagnostic passed to BE: (1) `80/2569 P-0672` has 2 DTL lines (codes 3 & 1) → the correlated scalar
  subquery on LICENSE_ID+PRODUCT_CODE is non-unique → blank; (2) `81/2569 P-0048` single line code `0`
  still blank → also confirm code `0` exists in `T_R_TRANSPORT_TYPE` (`SELECT TRANSPORT_TYPE_CODE,
  TRANSPORT_TYPE_NAME FROM T_R_TRANSPORT_TYPE;` if needed).
- **TASK-006 → REWORK (transport-type resolution only).** @Jason via @Sober: fix the subquery grain/pick +
  name lookup; re-run the same capture. Other 3 fixes (dedup, dates, buyer-group) stay accepted.
  REQ-005 held at SPEC_DONE. Nothing pending on stakeholder right now.

## [--:--] Sober (SA Lead) — 2026-07-20 (transport-type diagnosis → DATA REQUEST 6)
- Good news first: the re-run confirms **3/4 fixes on live data** — dedup, dates, and the buyer-group chart
  (my `T_M_BUYER_AUTHORITY` DATADIC call was right: "ทหาร" 30330, table↔chart consistent).
- **Corrected the transport-type root-cause theory before prescribing a fix (debug discipline).** The board/
  DATA REQ 5 note said "the `MAX` subquery is non-unique → blank" — that's **wrong**: `MAX(3,1)` returns `3`,
  not blank. Since **all** rows are blank (single-line `0` AND multi-line `3/1`), the cause is systematic —
  one of two, which I can't tell apart from code:
  - **(B) PRODUCT_CODE correlation mismatch** (INFORM_MOVE_DTL vs LICENSE_DTL format) → subquery NULL every
    row → fix = TRIM/normalize the join.
  - **(A) ref-table/name-lookup miss** (codes `0`/`3` absent from `T_R_TRANSPORT_TYPE`; `0` may = "none") →
    blank is partly real.
- **Did NOT guess a fix.** Raised **DATA REQUEST 6** (2 short queries: the `T_R_TRANSPORT_TYPE` contents +
  reproducing our exact correlated subquery for the 2 licenses) — the result deterministically picks (A) vs
  (B), so we fix once. Bundled a minor question: is buyer-group code `0` (foreign "…Sdn Bhd") a real group.
- TASK-006 stays REWORK, **gated on DATA REQ 6**; other 3 defects remain fixed; REQ-005 held SPEC_DONE.
- Updated SPEC-005 / TASK-006 / board / REQ-005. @Porter: please run DATA REQ 6 (quick). @Jason: standby —
  it's a 1-line fix (TRIM the correlation) or an accepted-real-blank, decided by the query.

## [--:--] Porter (PM) — 2026-07-20 (relay DATA REQUEST 6)
- Read Sober's transport-type diagnosis. Sober corrected the root-cause theory (the subquery uses `MAX`,
  so 2 rows ≠ blank) → all-rows-blank = one systematic cause: (A) name-lookup miss (codes 0/3 not in
  `T_R_TRANSPORT_TYPE`) or (B) PRODUCT_CODE correlation mismatch (INFORM_MOVE_DTL vs LICENSE_DTL format).
- **Relayed DATA REQUEST 6 to the stakeholder (Thai)** with Sober's 2 queries: (a) dump
  `T_R_TRANSPORT_TYPE` codes+names; (b) reproduce the exact correlated subquery for 81/2569 & 80/2569 →
  `resolved_code` NULL ⇒ (B) fix TRIM/normalize the join; 0/3 present but absent from (a) ⇒ (A)
  accepted-blank/data note. Bundled the minor buyer-group code `0` question.
- TASK-006 stays REWORK gated on DATA REQ 6; Jason on standby (1-line fix or accepted-blank). Other 3
  defects remain accepted; REQ-005 held SPEC_DONE. Nothing else pending on the team.

## [--:--] Porter (PM) — 2026-07-20 (DATA REQ 6 → root cause = empty ref table)
- Results (project-docs/data-req-6-2026-07-20-transport-type-rootcause.md):
  (a) `T_R_TRANSPORT_TYPE` = **0 rows (empty)**; (b) the correlated subquery **resolves the code** on every
  row (81/2569→0, 80/2569→3) → cause (B) format mismatch RULED OUT.
- **Root cause = empty code→name reference table**, not a SQL/join bug. Broader impact: the
  ประเภทการขนย้าย dropdown (TASK-007) reads the same empty table → also empty.
- Routed to **@Sober (design call, nothing for Jason to "fix" in the join):** accept as an environment
  data-gap (deliver + note; populates when `T_R_TRANSPORT_TYPE` is seeded) **or** hardcode a fixed enum for
  0/1/3 (like MOVE_REQUEST_TYPE) — resolve meanings from DATADIC, else a small stakeholder question via Porter.
- REQ-005 held SPEC_DONE pending Sober's decision. Dedup/dates/buyer-group remain accepted. Minor
  buyer-group code `0` label still unanswered (non-blocking).

## [--:--] Porter (PM) — 2026-07-20 (stakeholder gave the real ประเภทการขนย้าย source)
- Stakeholder challenged `T_R_TRANSPORT_TYPE` ("ไม่น่าได้ใช้") and gave the real source:
  **ประเภทการขนย้าย = `SELECT MOVE_REQUEST_TYPE FROM T_T_REQUEST_MOVE`.** So the empty-ref-table was a
  wrong-source, not a data gap. Recorded in project-docs/data-req-6-...md + REQ-005 §Questions.
- @Sober: **re-source the ประเภทการขนย้าย column + dropdown** to `T_T_REQUEST_MOVE.MOVE_REQUEST_TYPE`;
  trace `T_T_REQUEST_MOVE` structure + join key to INFORM_MOVE/license + whether MOVE_REQUEST_TYPE is
  code/label. ⚠️ same column name as ประเภทการขออนุญาต's 0/1 enum — confirm it's a different set so the two
  fields aren't conflated. Drop/repurpose TASK-007's `T_R_TRANSPORT_TYPE` entity. DATA REQUEST via Porter
  if the join/enum isn't in code.
- REQ-005 held SPEC_DONE; ball now with Sober (redesign) → Jason (rework) → Porter (re-run capture).
  Other 3 defects stay accepted; buyer-group code `0` label still minor/open.

## [--:--] Porter (PM) — 2026-07-20 (ประเภทการขนย้าย name mapping resolved)
- Stakeholder dumped `T_S_COMMON_CODE`. The name lookup for ประเภทการขนย้าย = group
  **`GROUP_CODE='MoveRequestType'`** (GROUP_NAME "ประเภทการขนย้าย"), codes 0–5
  (0=ขนย้ายให้หน่วยงานตามมาตรา 7, 1=ขายและขนย้ายให้บุคคลอื่นนอกหน่วยงานฯ, 2=ทำลาย, 3=ทดสอบ, 4=จัดแสดง,
  5=กลับโรงงาน). Captured codes now resolve (81/2569→0, 80/2569→3).
- Full wiring in hand: source `T_T_REQUEST_MOVE.MOVE_REQUEST_TYPE` + name via `T_S_COMMON_CODE`/MoveRequestType.
  Saved to project-docs/data-req-6-...md; answered REQ-005 §Questions. **No more stakeholder data needed.**
- @Sober: wire via the common-code table (likely already in DAL — drop `T_R_TRANSPORT_TYPE`); trace the
  `T_T_REQUEST_MOVE` join; ⚠️ verify MoveRequestType isn't the same source as ประเภทการขออนุญาต's 0/1 map.
  → Jason implements → re-run the same capture → Porter closes REQ-005. Minor buyer-group `0` label still open.

## [--:--] Sober (SA Lead) — 2026-07-20 (transport-type re-source designed)
- Designed the ประเภทการขนย้าย re-source (SPEC-005 "REWORK #4"). **Traced the join from code/DATADIC — no new
  DATA REQUEST:** `T_T_INFORM_MOVE_DTL.REF_LICENSE_NO` → `T_T_LICENSE L (FORM_ID=10)` → `L.REQUEST_ID`
  (DATADIC:631, confirmed on the entity) → `T_T_REQUEST_MOVE.REQUEST_ID` (DATADIC:906) → `MOVE_REQUEST_TYPE`.
  Resolve via a **scalar subquery** on REQUEST_ID (avoids the multiplication trap). Name via the existing
  move-license pattern `TSCommonCodeRepo.GetDataActiveByGroupCode("MoveRequestType")` (0–5) — **already in the
  DAL, no new entity.**
- **Dropped `T_R_TRANSPORT_TYPE`** (TASK-007 → SUPERSEDED): it was the wrong source AND empty. Its
  entity/repo/UoW wiring is now dead → removed in TASK-006 REWORK #4 (Karpathy: remove what the change orphaned).
  DATA REQ 2's column facts were fine; the table just wasn't the source.
- ⚠️ **Naming-trap flag → Porter (non-blocking):** col5 ประเภทการขออนุญาต (`INFORM_REQUEST_TYPE` 0/1, works
  today, stays) and col6 ประเภทการขนย้าย (`MOVE_REQUEST_TYPE` via `MoveRequestType`) read near-identical at
  0/1; col6 diverges at 2–5. Confirm the stakeholder wants both distinct columns (they're both on the page) —
  verify at the re-run. Did NOT change col5 (stakeholder said it's fine).
- TASK-006 → REWORK #4 (all data in hand, no gate): SQL subquery swap + common-code name/dropdown + remove
  `T_R_TRANSPORT_TYPE`. The other 3 defects stay fixed.
- Updated SPEC-005 / TASK-006 / TASK-007 (superseded) / board. @Jason: **go** — re-source per TASK-006 #4, build,
  REVIEW → @Porter re-runs the same capture to close REQ-005. @Porter: please also relay the col5/col6 flag.
